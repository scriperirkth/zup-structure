
#Область ПрограммныйИнтерфейс

Функция ОбязательныеРеквизиты(Шаблон, ИдентификаторЭтапа) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ШаблоныПроцессовОбязательныеРеквизиты.ИмяТабличнойЧасти КАК ИмяТабличнойЧасти,
	|	ШаблоныПроцессовОбязательныеРеквизиты.ИмяРеквизита КАК ИмяРеквизита
	|ИЗ
	|	Справочник.ШаблоныПроцессов.ОбязательныеРеквизиты КАК ШаблоныПроцессовОбязательныеРеквизиты
	|ГДЕ
	|	ШаблоныПроцессовОбязательныеРеквизиты.Ссылка = &Ссылка
	|	И ШаблоныПроцессовОбязательныеРеквизиты.ИдентификаторЭтапа = &ИдентификаторЭтапа";
	Запрос.УстановитьПараметр("Ссылка", Шаблон);
	Запрос.УстановитьПараметр("ИдентификаторЭтапа", Новый УникальныйИдентификатор(ИдентификаторЭтапа));
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция ОбязательныеРеквизитыПервичныхДокументов(Шаблон, ИдентификаторЭтапа) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ШаблоныПроцессовОбязательныеРеквизиты.ИдентификаторПечатнойФормы КАК ИдентификаторПечатнойФормы,
	|	ШаблоныПроцессовОбязательныеРеквизиты.ИмяТабличнойЧасти КАК ИмяТабличнойЧасти,
	|	ШаблоныПроцессовОбязательныеРеквизиты.ИмяРеквизита КАК ИмяРеквизита
	|ИЗ
	|	Справочник.ШаблоныПроцессов.ОбязательностьПервичныеДокументы КАК ШаблоныПроцессовОбязательныеРеквизиты
	|ГДЕ
	|	ШаблоныПроцессовОбязательныеРеквизиты.Ссылка = &Ссылка
	|	И ШаблоныПроцессовОбязательныеРеквизиты.ИдентификаторЭтапа = &ИдентификаторЭтапа";
	Запрос.УстановитьПараметр("Ссылка", Шаблон);
	Запрос.УстановитьПараметр("ИдентификаторЭтапа", Новый УникальныйИдентификатор(ИдентификаторЭтапа));
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Функция ПризнакПроведенности(Шаблон, ИдентификаторЭтапа, Статус) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ШаблоныПроцессовТребованияКПроведению.ПризнакПроведенности КАК ПризнакПроведенности
	|ИЗ
	|	Справочник.ШаблоныПроцессов.ТребованияКПроведению КАК ШаблоныПроцессовТребованияКПроведению
	|ГДЕ
	|	ШаблоныПроцессовТребованияКПроведению.Ссылка = &Ссылка
	|	И ШаблоныПроцессовТребованияКПроведению.ИдентификаторЭтапа = &ИдентификаторЭтапа
	|	И ШаблоныПроцессовТребованияКПроведению.Статус = &Статус";
	
	Запрос.УстановитьПараметр("Ссылка", Шаблон);
	Запрос.УстановитьПараметр("ИдентификаторЭтапа", Новый УникальныйИдентификатор(ИдентификаторЭтапа));
	Запрос.УстановитьПараметр("Статус", Статус);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.ПризнакПроведенности;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция УсловиеВыполнения(Шаблон, ИдентификаторЭтапа, Статус) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ШаблоныПроцессовТребованияКВыполнению.УсловиеВыполнения КАК УсловиеВыполнения
	|ИЗ
	|	Справочник.ШаблоныПроцессов.ТребованияКВыполнению КАК ШаблоныПроцессовТребованияКВыполнению
	|ГДЕ
	|	ШаблоныПроцессовТребованияКВыполнению.Ссылка = &Ссылка
	|	И ШаблоныПроцессовТребованияКВыполнению.ИдентификаторЭтапа = &ИдентификаторЭтапа
	|	И ШаблоныПроцессовТребованияКВыполнению.Статус = &Статус";
	
	Запрос.УстановитьПараметр("Ссылка", Шаблон);
	Запрос.УстановитьПараметр("ИдентификаторЭтапа", Новый УникальныйИдентификатор(ИдентификаторЭтапа));
	Запрос.УстановитьПараметр("Статус", Статус);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.УсловиеВыполнения;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

Функция ОписаниеТиповСРазыменовкой() Экспорт
	
	Возврат Новый ОписаниеТипов(Новый ОписаниеТипов(
			Справочники.ТипВсеСсылки(),
			ПланыСчетов.ТипВсеСсылки().Типы()),
			ПланыВидовХарактеристик.ТипВсеСсылки().Типы());
	
КонецФункции

Функция НепроверяемыеРеквизиты() Экспорт
	
	Результат = Новый Соответствие;
	Результат.Вставить("Проведен", Истина);
	Результат.Вставить("Ссылка", Истина);
	Результат.Вставить("ПометкаУдаления", Истина);
	Результат.Вставить("НомерСтроки", Истина);
	Результат.Вставить("ИДИсточника", Истина);
	Результат.Вставить("ПодписанЭП", Истина);
	Результат.Вставить("ИмяПредопределенныхДанных", Истина);
	Результат.Вставить("Предопределенный", Истина);
	Результат.Вставить("Родитель", Истина);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти