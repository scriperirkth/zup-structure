///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2026, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются 
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Подсистема "Интернет-поддержка пользователей".
// ОбщийМодуль.ИнтернетПоддержкаПользователейКлиент.
//
// Клиентские процедуры и функции Интернет-поддержки пользователей:
//  - определение настроек подключения;
//  - переход к подключению Интернет-поддержки пользователей;
//  - переход на страницы интегрированных сайтов;
//  - обработка событий приложения.
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

#Область ОбщегоНазначения

// Возвращает настройки соединения с серверами Интернет-поддержки.
//
// Возвращаемое значение:
//  Структура - настройки соединения. Поля структуры:
//      * УстанавливатьПодключениеНаСервере - Булево - Истина, если подключение
//        устанавливается на сервере 1С:Предприятие;
//      * ТаймаутПодключения - Число - таймаут подключения к серверам в секундах;
//      * ДоменРасположенияСерверовИПП - Число - если 0, устанавливать подключение
//        к серверам ИПП в доменной зоне 1c.ru, если 1 - в доменной зоне 1c.eu.
//
Функция НастройкиСоединенияССерверами() Экспорт
	
	ПараметрыРаботыКлиента = СтандартныеПодсистемыКлиент.ПараметрыРаботыКлиента().ИнтернетПоддержкаПользователей;
	Результат = Новый Структура("ДоменРасположенияСерверовИПП");
	ЗаполнитьЗначенияСвойств(Результат, ПараметрыРаботыКлиента);
	Результат.Вставить("УстанавливатьПодключениеНаСервере", Истина);
	Результат.Вставить("ТаймаутПодключения"               , 30);
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область АутентификацияВСервисахИнтернетПоддержки

// Выполняет подключение к сервису Интернет-поддержки: ввод данных
// аутентификации (логина и пароля) для подключения к сервисам
// Интернет-поддержки.
// При успешном завершении возвращается введенный логин через
// объект ОписаниеОповещения.
//
// Параметры:
//  ОповещениеОЗавершении - ОписаниеОповещения - обработчик оповещения о
//          завершении. В обработчик оповещения возвращается значение:
//          Неопределено - при нажатии пользователем кнопки Отмена;
//          Структура, при успешном вводе логина и пароля.
//          Поля структуры:
//            * Логин - Строка - введенный логин;
//  ВладелецФормы - ФормаКлиентскогоПриложения - владелец формы подключения
//          Интернет-поддержки. Т.к. форма подключения Интернет-поддержки открывается
//          в режиме "Блокировать окно владельца", рекомендуется заполнять
//          значение этого параметра;
//
Процедура ПодключитьИнтернетПоддержкуПользователей(
		ОповещениеОЗавершении = Неопределено,
		ВладелецФормы = Неопределено) Экспорт
	
	Если СтандартныеПодсистемыКлиент.ПараметрыРаботыКлиента().РазделениеВключено Тогда
		
		ОповещениеАвторизацияНедоступна = Новый ОписаниеОповещения(
			"ПриНедоступностиПодключенияИПП",
			ЭтотОбъект,
			ОповещениеОЗавершении);
		
		ПоказатьПредупреждение(
			ОповещениеАвторизацияНедоступна,
			НСтр("ru = 'Использование Интернет-поддержки пользователей недоступно при работе в модели сервиса.'"));
		Возврат;
		
	КонецЕсли;
	
	// Проверить права пользователя для интерактивной авторизации.
	Если Не ДоступноПодключениеИнтернетПоддержки() Тогда
		
		ОповещениеАвторизацияНедоступна = Новый ОписаниеОповещения(
			"ПриНедоступностиПодключенияИПП",
			ЭтотОбъект,
			ОповещениеОЗавершении);
		
		ПоказатьПредупреждение(
			ОповещениеАвторизацияНедоступна,
			НСтр("ru = 'Недостаточно прав для подключения Интернет-поддержки пользователей. Обратитесь к администратору.'"));
		Возврат;
		
	КонецЕсли;
	
	// Открыть форму подключения ИПП.
	ОткрытьФорму("ОбщаяФорма.ПодключениеИнтернетПоддержки",
		,
		ВладелецФормы,
		,
		,
		,
		ОповещениеОЗавершении);
	
КонецПроцедуры

// Определяет, доступно ли текущему пользователю выполнение интерактивного
// подключения Интернет-поддержки в соответствии с текущим режимом работы
// и правами пользователя.
//
// Возвращаемое значение:
//  Булево - Истина - интерактивное подключение доступно,
//           Ложь - в противном случае.
//
Функция ДоступноПодключениеИнтернетПоддержки() Экспорт
	
	ПараметрыРаботыКлиента = СтандартныеПодсистемыКлиент.ПараметрыРаботыКлиента().ИнтернетПоддержкаПользователей;
	Возврат ПараметрыРаботыКлиента.ДоступноПодключениеИнтернетПоддержки;
	
КонецФункции

#КонецОбласти

#Область ПереходПоСсылкеНаПортал

// Открывает страницу сайта, система аутентификации которого интегрирована с
// Порталом 1С:ИТС.
// В зависимости от текущего режима работы информационной базы и наличия у
// текущего пользователя информационной базы соответствующих прав
// страница сайта может быть открыта с учетными данными пользователя Портала 1С:ИТС,
// для которого подключена Интернет-поддержка.
//
// Параметры:
//  URLСтраницыСайта - Строка - URL страницы сайта;
//  ЗаголовокОкна - Строка - заголовок окна для методов:
//      - ИнтернетПоддержкаПользователейКлиентПереопределяемый.ОткрытьИнтернетСтраницу()
//      - ИнтеграцияПодсистемБИПКлиент.ОткрытьИнтернетСтраницу(),
//        если используется.
//  ОтключитьАвторизацию - Булево - отключает авторизацию пользователей
//   при переходе на страницы интегрированных ресурсов.
//  ДополнительныеПараметры - Структура - настройки открытия страницы сайта:
//    * ИгнорироватьБлокировку - Булево - когда установлено Истина
//        и заблокирована работе с Интернет-сервисами - отключает авторизацию пользователей.
//        Когда установлено Ложь и заблокирована работе с Интернет-сервисами
//        и не отключена авторизация будет сообщение об ошибке.
//        По умолчанию, Истина.
//
Процедура ОткрытьСтраницуИнтегрированногоСайта(
		URLСтраницыСайта,
		ЗаголовокОкна = "",
		ОтключитьАвторизацию = Ложь,
		ДополнительныеПараметры = Неопределено) Экспорт
	
	Настройки = ИнтернетПоддержкаПользователейКлиентСервер.НастройкиИнтернетПоддержкиПользователей();
	ПараметрыОткрытияСтраницы = Новый Структура;
	ПараметрыОткрытияСтраницы.Вставить("ИгнорироватьБлокировку", Истина);
	Если ДополнительныеПараметры <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыОткрытияСтраницы, ДополнительныеПараметры);
	КонецЕсли;
	
	ИспользоватьАвторизацию = Не ОтключитьАвторизацию
		И Не Настройки.ОтключитьАвторизацию;
	
	Если ИспользоватьАвторизацию Тогда
		
		Состояние(, , НСтр("ru = 'Пожалуйста, подождите...'"));
		РезультатПолученияURL =
			ИнтернетПоддержкаПользователейВызовСервера.СлужебнаяURLДляПереходаНаСтраницуИнтегрированногоСайта(
				URLСтраницыСайта);
		Состояние();
		
		Если Не ПустаяСтрока(РезультатПолученияURL.КодОшибки)
			И РезультатПолученияURL.КодОшибки <> "НеверныйЛогинИлиПароль" Тогда
			
			ПоказатьОповещение = Истина;
			ТекстПояснения = НСтр("ru = 'Ошибка входа на Портал 1С:ИТС.
				|Подробнее см. в журнале регистрации.'");
			
			Если РезультатПолученияURL.КодОшибки = "ЗапрещенДоступКИнтернетСервисам" Тогда
				Если ПараметрыОткрытияСтраницы.ИгнорироватьБлокировку Тогда
					ПоказатьОповещение = Ложь;
				Иначе
					ТекстПояснения = НСтр("ru = 'Доступ к Интернет-сервисам в приложении запрещен администратором (""Интернет-поддержка и сервисы"" — ""Разрешить доступ к Интернет-сервисам"").'");
				КонецЕсли;
				
			КонецЕсли;
			
			Если ПоказатьОповещение Тогда
				
				ПоказатьОповещениеПользователя(
					,
					,
					ТекстПояснения,
					БиблиотекаКартинок.Ошибка32);
				Возврат;
				
			КонецЕсли;
			
		КонецЕсли;
		
		URL = РезультатПолученияURL.URL
		
	Иначе
		URL = URLСтраницыСайта;
	КонецЕсли;
	
	СтандартнаяОбработка = Истина;
	ИнтеграцияПодсистемБИПКлиент.ОткрытьИнтернетСтраницу(
		URL,
		ЗаголовокОкна,
		СтандартнаяОбработка);
	ИнтернетПоддержкаПользователейКлиентПереопределяемый.ОткрытьИнтернетСтраницу(
		URL,
		ЗаголовокОкна,
		СтандартнаяОбработка);
	
	Если СтандартнаяОбработка = Истина Тогда
		// Открытие Интернет-страницы стандартным способом.
		ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(URL);
	КонецЕсли;
	
КонецПроцедуры

// Открывает главную страницу Портала.
//
Процедура ОткрытьГлавнуюСтраницуПортала() Экспорт
	
	ОткрытьСтраницуИнтегрированногоСайта(
		ИнтернетПоддержкаПользователейКлиентСервер.URLСтраницыПорталаПоддержки(
			,
			НастройкиСоединенияССерверами().ДоменРасположенияСерверовИПП),
		НСтр("ru = 'Портал 1С:ИТС'"));
	
КонецПроцедуры

// Открывает страницу Портала для регистрации нового пользователя.
//
Процедура ОткрытьСтраницуРегистрацииНовогоПользователя() Экспорт
	
	ОткрытьСтраницуИнтегрированногоСайта(
		ИнтернетПоддержкаПользователейКлиентСервер.URLСтраницыСервисаLogin(
			"/registration",
			НастройкиСоединенияССерверами()),
		НСтр("ru = 'Регистрация'"),
		Истина);
	
КонецПроцедуры

// Открывает страницу Портала для восстановления пароля.
//
Процедура ОткрытьСтраницуВосстановленияПароля() Экспорт
	
	ОткрытьСтраницуИнтегрированногоСайта(
		ИнтернетПоддержкаПользователейКлиентСервер.URLСтраницыСервисаLogin(
			"/remind_request",
			НастройкиСоединенияССерверами()),
		НСтр("ru = 'Восстановление пароля'"),
		Истина);
	
КонецПроцедуры

#КонецОбласти

#Область ИнтеграцияСБиблиотекойСтандартныхПодсистем

// См. процедуру ОбщегоНазначенияКлиентПереопределяемый.ПриНачалеРаботыСистемы.
//
Процедура ПриНачалеРаботыСистемы(Параметры) Экспорт
	
	// ПодключениеСервисовСопровождения
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.ПодключениеСервисовСопровождения") Тогда
		МодульПодключениеСервисовСопровожденияКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ПодключениеСервисовСопровожденияКлиент");
		МодульПодключениеСервисовСопровожденияКлиент.ПриНачалеРаботыСистемы();
	КонецЕсли;
	// Конец ПодключениеСервисовСопровождения
	
	// ПолучениеОбновленийПрограмм
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.ПолучениеОбновленийПрограммы") Тогда
		МодульПолучениеОбновленийПрограммыКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ПолучениеОбновленийПрограммыКлиент");
		МодульПолучениеОбновленийПрограммыКлиент.ПриНачалеРаботыСистемы();
	КонецЕсли;
	// Конец ПолучениеОбновленийПрограммы
	
	// МониторПортала1СИТС
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.МониторПортала1СИТС") Тогда
		МодульМониторПортала1СИТСКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("МониторПортала1СИТСКлиент");
		МодульМониторПортала1СИТСКлиент.ПриНачалеРаботыСистемы();
	КонецЕсли;
	// Конец МониторПортала1СИТС
	
	// СПАРКРиски
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.СПАРКРиски") Тогда
		МодульСПАРКРискиКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СПАРКРискиКлиент");
		МодульСПАРКРискиКлиент.ПриНачалеРаботыСистемы();
	КонецЕсли;
	// Конец СПАРКРиски
	
	// ОблачныйАрхив20
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.ОблачныйАрхив20") Тогда
		МодульОблачныйАрхив20Клиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ОблачныйАрхив20Клиент");
		МодульОблачныйАрхив20Клиент.ПриНачалеРаботыСистемы(Параметры);
	КонецЕсли;
	// Конец ОблачныйАрхив20
	
	// РаботаСКлассификаторами
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.РаботаСКлассификаторами") Тогда
		МодульРаботаСКлассификаторамиКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("РаботаСКлассификаторамиКлиент");
		МодульРаботаСКлассификаторамиКлиент.ПриНачалеРаботыСистемы();
	КонецЕсли;
	// Конец РаботаСКлассификаторами
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#Область ОбщегоНазначения

// Преобразует значение из фиксированного типа.
// Параметры:
//  ЗначениеФиксированногоТипа - Произвольный - значение фиксированного типа
//       из которого необходимо получить значение нефиксированного типа.
//
// Возвращаемое значение:
//  Произвольный - полученное значение аналогичного нефиксированного типа.
//
Функция ЗначениеИзФиксированногоТипа(ЗначениеФиксированногоТипа) Экспорт

	Результат = Неопределено;
	ТипЗначения = ТипЗнч(ЗначениеФиксированногоТипа);

	Если ТипЗначения = Тип("ФиксированнаяСтруктура") Тогда

		Результат = Новый Структура;
		Для каждого КлючЗначение Из ЗначениеФиксированногоТипа Цикл
			Результат.Вставить(КлючЗначение.Ключ, ЗначениеИзФиксированногоТипа(КлючЗначение.Значение));
		КонецЦикла;

	ИначеЕсли ТипЗначения = Тип("ФиксированноеСоответствие") Тогда

		Результат = Новый Соответствие;
		Для каждого КлючЗначение Из ЗначениеФиксированногоТипа Цикл
			Результат.Вставить(КлючЗначение.Ключ, ЗначениеИзФиксированногоТипа(КлючЗначение.Значение));
		КонецЦикла;

	ИначеЕсли ТипЗначения = Тип("ФиксированныйМассив") Тогда

		Результат = Новый Массив;
		Для каждого ЭлементМассива Из ЗначениеФиксированногоТипа Цикл
			Результат.Добавить(ЗначениеИзФиксированногоТипа(ЭлементМассива));
		КонецЦикла;

	Иначе

		Результат = ЗначениеФиксированногоТипа;

	КонецЕсли;

	Возврат Результат;

КонецФункции

// Получает значение параметра приложения.
//
// Устанавливает значение параметра приложения.
//
// Параметры:
//  ИмяПараметра - Строка - идентификатор параметра;
//  ЗначениеПоУмолчанию - Произвольный - значение параметра по умолчанию;
//
// Возвращаемое значение:
//  Произвольный - значение параметра.
//
Функция ЗначениеПараметраПриложения(ИмяПараметра, ЗначениеПоУмолчанию = Неопределено) Экспорт

	ПараметрыБиблиотеки = ПараметрыПриложения.Получить("ИнтернетПоддержкаПользователей");
	Если ПараметрыБиблиотеки = Неопределено Тогда
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;

	ЗначениеПараметра = ПараметрыБиблиотеки.Получить(ИмяПараметра);
	Возврат ?(ЗначениеПараметра = Неопределено, ЗначениеПоУмолчанию, ЗначениеПараметра);

КонецФункции

// Устанавливает значение параметра приложения.
//
// Параметры:
//  ИмяПараметра - Строка - идентификатор параметра;
//  ЗначениеПараметра - Произвольный - новое значение параметра;
//
Процедура УстановитьЗначениеПараметраПриложения(ИмяПараметра, ЗначениеПараметра) Экспорт

	ПараметрыБиблиотеки = ПараметрыПриложения.Получить("ИнтернетПоддержкаПользователей");
	Если ПараметрыБиблиотеки = Неопределено Тогда
		ПараметрыБиблиотеки = Новый Соответствие;
		ПараметрыПриложения.Вставить(
			"ИнтернетПоддержкаПользователей",
			ПараметрыБиблиотеки);
	КонецЕсли;

	ПараметрыБиблиотеки.Вставить(ИмяПараметра, ЗначениеПараметра);

КонецПроцедуры

// Возвращает текст заголовка элемента формы из строки или форматированной строки.
//
Функция ТекстФорматированногоЗаголовка(Заголовок) Экспорт
	
	Если ТипЗнч(Заголовок) <> Тип("ФорматированнаяСтрока") Тогда
		Возврат Заголовок;
	КонецЕсли;
	
	ФДок = Новый ФорматированныйДокумент;
	ФДок.УстановитьФорматированнуюСтроку(Заголовок);
	Возврат ФДок.ПолучитьТекст();
	
КонецФункции

// Устанавливает видимость кнопки формы "Копировать в буфер".
//
// Параметры:
//  Элементы - ВсеЭлементыФормы - элементы формы владельца.
//  ФорматДанных - СтандартныйФорматДанныхБуфераОбмена - Формат данных планируемый для передачи в буфер
//
Процедура УстановитьОтображениеКнопкиКопироватьВБуфер(Элементы, ФорматДанных) Экспорт
	
	Если СредстваБуфераОбмена.ИспользованиеДоступно()
		И СредстваБуфераОбмена.ПоддерживаетсяФорматДанных(ФорматДанных) Тогда
		КопированиеДоступно = Истина;
	Иначе
		КопированиеДоступно = Ложь;
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы,
		"КопироватьВБуфер",
		"Видимость",
		КопированиеДоступно);
	
КонецПроцедуры

#КонецОбласти

#Область ПользовательскоеСоглашение

// Выполняет открытие формы пользовательского соглашения.
//
// Параметры:
//  ПараметыОткрытия - Структура - Параметры открытия формы пользовтаельского соглашения:
//    * Параметры - Структура, Неопределено - Параметры формы.
//    * Владелец - ВладелецФормы - форма или элемент управления другой формы.
//    * ОписаниеОповещения -  ОписаниеОповещения, Неопределено - Содержит описание процедуры,
//      которая будет вызвана при закрытии формы.
//    * РежимОткрытияОкна - РежимОткрытияОкнаФормы - Указывает режим открытия окна формы клиентского приложения.
//
Процедура ОткрытьПользовательскоеСоглашение(ПараметрыОткрытия) Экспорт
	
	ОткрытьФорму(
		"ОбщаяФорма.ПользовательскоеСоглашение",
		ПараметрыОткрытия.Параметры,
		ПараметрыОткрытия.Владелец,
		,
		,
		,
		ПараметрыОткрытия.ОписаниеОповещения,
		ПараметрыОткрытия.РежимОткрытияОкна);
	
КонецПроцедуры

#КонецОбласти

#Область АутентификацияВСервисахИнтернетПоддержки

// Выполнение проверки и подключение к Интернет-поддержке пользователей.
// Параметры:
//  ОбработкаЗавершения - ОписаниеОповещения - Процедура, которая будет либо выполнена сразу, если вход в ИПП выполнен,
//    либо после вывода пользователю диалога о необходимости подключения к ИПП и ввода учетных данных.
//    Параметр Результат процедуры-обработчика может принимать значение Ложь,если пользователь отказался от подключения
//    к ИПП или отменил ввод учетных данных, либо Истина, если вход в ИПП выполнен.
//
Процедура НачатьПроверкуИПодключениеИнтернетПоддержкиПользователей(
		Знач ОбработкаЗавершения = Неопределено,
		Знач ВопросПользователю = Неопределено) Экспорт
	
	Если ПроверитьПодключениеИнтернетПоддержкиПользователей() Тогда
		
		Если ОбработкаЗавершения <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОбработкаЗавершения, Истина);
		КонецЕсли;
		
	Иначе
		
		ПоказатьВопросПодключенияИнтернетПоддержкиПользователей(ОбработкаЗавершения, ВопросПользователю);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ПереходПоСсылкеНаПортал

// Открывает личный кабинет пользователя в обозревателе.
//
Процедура ОткрытьЛичныйКабинетПользователя() Экспорт
	
	ОткрытьСтраницуИнтегрированногоСайта(
		ИнтернетПоддержкаПользователейКлиентСервер.URLСтраницыПорталаПоддержки(
			"/software",
			НастройкиСоединенияССерверами().ДоменРасположенияСерверовИПП),
		НСтр("ru = 'Личный кабинет пользователя'"));
	
КонецПроцедуры

// Открывает страницу официальной поддержки.
//
// Параметры:
//  Якорь - Строка, Неопределено - якорь для открытия необходимого раздела.
//
Процедура ОткрытьСтраницуОфициальнаяПоддержка(Якорь = Неопределено) Экспорт
	
	ОткрытьСтраницуИнтегрированногоСайта(
		ИнтернетПоддержкаПользователейКлиентСервер.URLСтраницыПорталаПоддержки(
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				"/support%1",
				Якорь),
			НастройкиСоединенияССерверами().ДоменРасположенияСерверовИПП),
		НСтр("ru = 'Официальная поддержка'"));
	
КонецПроцедуры

#КонецОбласти

#Область ОтображениеСекретныхДанныхНаФорме

// Устанавливает видимость кнопки отображения символов.
//
// Параметры:
//  Элемент  - ПолеФормы - элемент формы с секретными данными.
//
Процедура ПриИзмененииСекретныхДанных(Элемент) Экспорт
	
	Если Элемент.КнопкаВыбора = Неопределено Тогда
		Элемент.КартинкаКнопкиВыбора = БиблиотекаКартинок.ВводимыеСимволыВидны;
		Элемент.КнопкаВыбора = Истина;
	КонецЕсли;
	
КонецПроцедуры

// Изменяет режим отображения символов поля с секретными данными.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма, на которой обрабатываются секретные данные.
//  Элемент - ПолеФормы - элемент формы с секретными данными.
//  ИмяРеквизита - Строка - имя реквизита формы для обновления данных.
//
Процедура ОтобразитьСекретныеДанные(Форма, Элемент, ИмяРеквизита) Экспорт
	
	Элемент.РежимПароля = Не Элемент.РежимПароля;
	Форма[ИмяРеквизита] = Элемент.ТекстРедактирования;
	Если Элемент.РежимПароля Тогда
		Элемент.КартинкаКнопкиВыбора = БиблиотекаКартинок.ВводимыеСимволыВидны;
	Иначе
		Элемент.КартинкаКнопкиВыбора = БиблиотекаКартинок.ВводимыеСимволыСкрыты;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОтправкаСообщенияВМессенджеры

// Описание новых параметров отправки в мессенджеры.
//
// Возвращаемое значение:
//  Структура - описывает параметры отправки в мессенджеры.
//    * Мессенджер - Строка - идентификатор мессенджера, может принимать следующие значения:
//      "Телеграм", "WhatsApp".
//    * Получатель - Строка - контактные данные получателя.
//    * СписокПолучателей - СписокЗначений - список контактных данных из которых необходимо произвести выбор.
//
Функция НовыйПараметрыОтправкиВМессенджеры() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Мессенджер", "");
	Результат.Вставить("Получатель", "");
	Результат.Вставить("СписокПолучателей", Новый СписокЗначений);
	
	Возврат Результат;
	
КонецФункции

// Инициализирует процесс открытия URL для передачи сообщения пользователю посредством мессенджеров.
//
// Параметры:
//  ТекстСообщения - Строка - текст сообщения для пользователя.
//  СсылкаДляПередачи - Строка - URL для пользователя.
//  ПараметрыОтправки - Структура - Описывает параметры отправки сообщения
//    * Мессенджер - Строка - идентификатор мессенджера, может принимать следующие значения:
//      "MAX", "Телеграм", "WhatsApp".
//    * Получатель - Строка - контактные данные получателя (не учитываются для Телеграм).
//    * СписокПолучателей - СписокЗначений - список контактных данных
//      из которых необходимо произвести выбор(не учитываются для Телеграм).
//
Процедура ОткрытьURLОтправкиВМессенджеры(ТекстСообщения, СсылкаДляПередачи, ПараметрыОтправки) Экспорт
	
	Если ЗначениеЗаполнено(ПараметрыОтправки.СписокПолучателей) Тогда
		
		ПараметрыСообщения = Новый Структура;
		ПараметрыСообщения.Вставить("ТекстСообщения", ТекстСообщения);
		ПараметрыСообщения.Вставить("СсылкаДляПередачи", СсылкаДляПередачи);
		ПараметрыСообщения.Вставить("ПараметрыОтправки", ПараметрыОтправки);
		
		ПараметрыОтправки.СписокПолучателей.ПоказатьВыборЭлемента(
				Новый ОписаниеОповещения(
					"ПриВыбореПолучателяСообщенияДляМессенджера",
					ЭтотОбъект,
					ПараметрыСообщения),
				НСтр("ru = 'Выбор получателя сообщения'"));
	Иначе
		ОткрытьURLОтправкиВМессенджерыЗавершение(ТекстСообщения, СсылкаДляПередачи, ПараметрыОтправки);
	КонецЕсли;
	
КонецПроцедуры

// Выполняет обработку выбора получателя пользователем,
// продолжает процесс открытия URL для передачи сообщения пользователю посредством мессенджеров.
//
Процедура ПриВыбореПолучателяСообщенияДляМессенджера(ВыбранныйЭлемент, ПараметрыСообщения) Экспорт
	
	Если ВыбранныйЭлемент <> Неопределено Тогда
		ПараметрыСообщения.ПараметрыОтправки.Получатель = ВыбранныйЭлемент.Значение;
	КонецЕсли;
	
	ОткрытьURLОтправкиВМессенджерыЗавершение(
		ПараметрыСообщения.ТекстСообщения,
		ПараметрыСообщения.СсылкаДляПередачи,
		ПараметрыСообщения.ПараметрыОтправки);
	
КонецПроцедуры

#КонецОбласти

#Область РаботаСБуферомОбмена

// Выполняет попытку помещения строки в буфер обмена.
//
// Параметры:
//  Текст - Строка - текст для помещения в буфер.
//
// Возвращаемое значение:
//  Булево - результат помещения в буфер. Истина если помещение выполнено успешно.
//
Асинх Функция ПоместитьСтрокуВБуферОбмена(Текст) Экспорт
	
	Если СредстваБуфераОбмена.ИспользованиеДоступно() Тогда
		ФорматДанных = СтандартныйФорматДанныхБуфераОбмена.Текст;
		Если Ждать СредстваБуфераОбмена.ПоддерживаетсяФорматДанных(ФорматДанных) Тогда
			ПомещаемыеДанные = Новый ЭлементБуфераОбмена(ФорматДанных, Текст);
			Возврат Ждать СредстваБуфераОбмена.ПоместитьДанныеАсинх(ПомещаемыеДанные);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область АутентификацияВСервисахИнтернетПоддержки

// Обработка оповещения недоступности подключения Интернет-поддержки.
//
// Параметры:
//  ОповещениеОЗавершении - ОписаниеОповещения - оповещение для вызова.
//
Процедура ПриНедоступностиПодключенияИПП(ОповещениеОЗавершении) Экспорт

	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
	КонецЕсли;

КонецПроцедуры

// См. ИнтернетПоддержкаПользователейВызовСервера.ЗаполненыДанныеАутентификацииПользователяИнтернетПоддержки
//
Функция ПроверитьПодключениеИнтернетПоддержкиПользователей()
	
	Возврат ИнтернетПоддержкаПользователейВызовСервера.ЗаполненыДанныеАутентификацииПользователяИнтернетПоддержки();
	
КонецФункции

// Выполняет вызов вопроса пользователю оповещения после окончания процесса подключения ИПП
//
// Параметры:
//  ОбработкаЗавершения - ОписаниеОповещения - Содержит описание оповещения,
//   которое необходимо вызвать после ответа пользователя.
//  ВопросПользователю - Строка, Неопределено - текст вопроса пользователю
//
Процедура ПоказатьВопросПодключенияИнтернетПоддержкиПользователей(
		Знач ОбработкаЗавершения,
		Знач ВопросПользователю)
	
	Параметры = Новый Структура;
	Параметры.Вставить("ОбработкаЗавершения", ОбработкаЗавершения);
	
	ОбработкаОтвета = Новый ОписаниеОповещения("ПоказатьВопросПодключенияИнтернетПоддержкиПользователейЗавершение", ЭтотОбъект, Параметры);
	
	Если ЗначениеЗаполнено(ВопросПользователю) Тогда
		ТекстВопроса = ВопросПользователю;
	Иначе
		ТекстВопроса = НСтр("ru='Подключение к Интернет-поддержке пользователей не обнаружено.
			|Подключиться сейчас?'");
	КонецЕсли;
	
	ПоказатьВопрос(ОбработкаОтвета, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

// Выполняет вызов обработки оповещения после окончания процесса подключения ИПП
//
// Параметры:
//  Ответ - КодВозвратаДиалога - Содержит ответ пользователя.
//  Параметры - Структура - содержит описание оповещения при закрытии формы.
//
Процедура ПоказатьВопросПодключенияИнтернетПоддержкиПользователейЗавершение(
		Знач Ответ,
		Знач Параметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		
		ОбработкаПодключения = Новый ОписаниеОповещения(
			"НачатьПодключениеИнтернетПоддержкиПользователейЗавершение",
			ЭтотОбъект,
			Параметры);
		НачатьПодключениеИнтернетПоддержкиПользователей(ОбработкаПодключения);
		
	Иначе
		
		Если Параметры.ОбработкаЗавершения <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(Параметры.ОбработкаЗавершения, Ложь);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// См. ИнтернетПоддержкаПользователейКлиент.ПодключитьИнтернетПоддержкуПользователей
//
Процедура НачатьПодключениеИнтернетПоддержкиПользователей(
		Знач ОбработкаПодключения = Неопределено)
	
	ПодключитьИнтернетПоддержкуПользователей(
		ОбработкаПодключения,
		ЭтотОбъект);
	
КонецПроцедуры

// Выполняет вызов обработки оповещения после окончания процесса подключения ИПП
//
// Параметры:
//  ДанныеПодключения - Структура - Содержит данные аутентификации пользователя,
//    пустая если они не заданы.
//  Параметры - Структура - содержит описание оповещения при закрытии формы
//
Процедура НачатьПодключениеИнтернетПоддержкиПользователейЗавершение(
		Знач ДанныеПодключения,
		Знач Параметры) Экспорт
	
	Если Параметры.ОбработкаЗавершения <> Неопределено Тогда
		
		Подключено = (ДанныеПодключения <> Неопределено);
		ВыполнитьОбработкуОповещения(Параметры.ОбработкаЗавершения, Подключено);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОтправкаСообщенияВМессенджеры

// Выполняет открытие URL для передачи сообщения пользователю посредством мессенджеров.
//
// Параметры:
//  ТекстСообщения - Строка - текст сообщения для пользователя.
//  СсылкаДляПередачи - Строка - URL для пользователя.
//  ПараметрыОтправки - Структура - Описывает параметры отправки сообщения
//    * Мессенджер - Строка - идентификатор мессенджера, может принимать следующие значения:
//      "Телеграм", "WhatsApp".
//    * Получатель - Строка - контактные данные получателя (не учитываются для Телеграм).
//    * СписокПолучателей - СписокЗначений - список контактных данных
//      из которых необходимо произвести выбор(не учитываются для Телеграм).
//
Процедура ОткрытьURLОтправкиВМессенджерыЗавершение(
		Знач ТекстСообщения,
		СсылкаДляПередачи,
		ПараметрыОтправки)
	
	Если ЗначениеЗаполнено(ПараметрыОтправки.Получатель) Тогда
		ПараметрыОтправки.Получатель = СтрЗаменить(ПараметрыОтправки.Получатель, "-", "");
		ПараметрыОтправки.Получатель = СтрЗаменить(ПараметрыОтправки.Получатель, " ", "");
		ПараметрыОтправки.Получатель = СтрЗаменить(ПараметрыОтправки.Получатель, "+", "");
	КонецЕсли;
	
	Если ПараметрыОтправки.Мессенджер = "MAX" Тогда
		ТекстСообщения = СтрЗаменить(ТекстСообщения, Символы.ПС, "%20");
		ОткрытьURLОтправкиВMax(
			ИнтернетПоддержкаПользователейВызовСервера.СтрокаВURLКодировке(ТекстСообщения));
	ИначеЕсли ПараметрыОтправки.Мессенджер = "Телеграм" Тогда
		ТекстСообщения = СтрЗаменить(ТекстСообщения, Символы.ПС, "%20");
		ОткрытьURLОтправкиВТелеграм(
			ТекстСообщения,
			ИнтернетПоддержкаПользователейВызовСервера.СтрокаВURLКодировке(СсылкаДляПередачи));
	ИначеЕсли ПараметрыОтправки.Мессенджер = "WhatsApp" Тогда
		ОткрытьURLОтправкиВWhatsApp(
			ИнтернетПоддержкаПользователейВызовСервера.СтрокаВURLКодировке(ТекстСообщения),
			ПараметрыОтправки.Получатель);
	КонецЕсли;
	
КонецПроцедуры

// Выполняет открытие URL для передачи ссылки пользователю посредством MAX.
//
// Параметры:
//  ТекстСообщения - Строка - текст сообщения для пользователя.
//
// Возвращаемое значение:
//  Строка - Ссылка для передачи сообщения пользователю посредством MAX.
//
Процедура ОткрытьURLОтправкиВMax(ТекстСообщения)
	
	URLОтправки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		"https://max.ru/:share?text=%1",
		ТекстСообщения);
	
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(URLОтправки);
	
КонецПроцедуры

// Выполняет открытие URL для передачи ссылки пользователю посредством Telegram.
//
// Параметры:
//  ТекстСообщения - Строка - текст сообщения для пользователя.
//  СсылкаДляПередачи - Строка - URL для получателя.
//
// Возвращаемое значение:
//  Строка - Ссылка для передачи сообщения пользователю посредством WhatsApp.
//
Процедура ОткрытьURLОтправкиВТелеграм(
		ТекстСообщения,
		СсылкаДляПередачи)
	
	URLОтправки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		"https://t.me/share/url?url=%1&text=%2",
		СсылкаДляПередачи,
		ТекстСообщения);
	
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(URLОтправки);
	
КонецПроцедуры

// Выполняет открытие URL для передачи ссылки пользователю посредством WhatsApp.
//
// Параметры:
//  ТекстСообщения - Строка - текст сообщения для пользователя.
//  Получатель - Строка -  телефон получателя сообщения
//
// Возвращаемое значение:
//  Строка - Ссылка для передачи сообщения пользователю посредством WhatsApp.
//
Процедура ОткрытьURLОтправкиВWhatsApp(
	ТекстСообщения,
	Получатель)
	
	Если ЗначениеЗаполнено(Получатель) Тогда
		URLОтправки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			"https://api.whatsapp.com/send?phone=%1&text=%2",
			Получатель,
			ТекстСообщения);
	Иначе
		URLОтправки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			"https://api.whatsapp.com/send/?text=%1",
			ТекстСообщения);
	КонецЕсли;
	
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(URLОтправки);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти
