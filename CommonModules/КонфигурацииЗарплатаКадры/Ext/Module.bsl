////////////////////////////////////////////////////////////////////////////////
// Подсистема "КонфигурацииЗарплатаКадры".
//
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.БазоваяФункциональность

// См. ОбщегоНазначенияПереопределяемый.ПриОпределенииОбщихПараметровБазовойФункциональности.
Процедура ПриОпределенииОбщихПараметровБазовойФункциональности(ОбщиеПараметры) Экспорт
	КонфигурацииЗарплатаКадрыВнутренний.ПриОпределенииОбщихПараметровБазовойФункциональности(ОбщиеПараметры)
КонецПроцедуры

// Конец СтандартныеПодсистемы.БазоваяФункциональность

// СтандартныеПодсистемы.БизнесПроцессыИЗадачи

// См. БизнесПроцессыИЗадачиПереопределяемый.ПриНачальномЗаполненииОбъектовАдресацииЗадач.
Процедура ПриНачальномЗаполненииОбъектовАдресацииЗадач(КодыЯзыков, Элементы, ТабличныеЧасти) Экспорт
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "Организации",
		"ru = 'Организации'");
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.Организации");
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ПодразделенияОрганизаций",
		"ru = 'Подразделения организаций'");
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.ПодразделенияОрганизаций");
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "СтруктураПредприятия",
		"ru = 'Структура предприятия'");
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.СтруктураПредприятия");
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ШаблоныДокументов",
		"ru = 'Шаблоны документов'");
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.ШаблоныДокументов");
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.БизнесПроцессыИЗадачи

// СтандартныеПодсистемы.Пользователи

// См. ПользователиПереопределяемый.ПриУстановкеНачальныхНастроек
Процедура ПриУстановкеНачальныхНастроек(НачальныеНастройки) Экспорт
	КонфигурацииЗарплатаКадрыВнутренний.ПриУстановкеНачальныхНастроек(НачальныеНастройки)
КонецПроцедуры

// Конец СтандартныеПодсистемы.Пользователи

// СтандартныеПодсистемы.КонтактнаяИнформация

// См. УправлениеКонтактнойИнформациейПереопределяемый.ПриНачальномЗаполненииЭлементов.
Процедура ПриНачальномЗаполненииЭлементов(КодыЯзыков, Элементы, ТабличныеЧасти) Экспорт
	
	// Справочник "Организации".
	РеквизитыГруппы = Новый Структура("ЭтоГруппа, Используется", Истина, Истина);
	ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "СправочникОрганизации",
		"ru = 'Контактная информация справочника ""Организации""'", РеквизитыГруппы);
	
	Реквизиты = Новый Структура;
	Реквизиты.Вставить("Используется", Истина);
	Реквизиты.Вставить("Родитель", Справочники.ВидыКонтактнойИнформации.СправочникОрганизации);
	Реквизиты.Вставить("ОтображатьВсегда", Истина);
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ЮрАдресОрганизации",
		"ru = 'Юридический адрес'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.ТолькоНациональныйАдрес = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ФактАдресОрганизации",
		"ru = 'Фактический адрес'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ТелефонОрганизации",
		"ru = 'Телефон'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.РазрешитьВводНесколькихЗначений = Истина;
	Элемент.ТелефонСДобавочнымНомером = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ФаксОрганизации",
		"ru = 'Факс'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Факс;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.РазрешитьВводНесколькихЗначений = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "EmailОрганизации",
		"ru = 'Email'", Реквизиты); // "ru = 'Электронная почта'".
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.РазрешитьВводНесколькихЗначений = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ПочтовыйАдресОрганизации",
		"ru = 'Почтовый адрес'", Реквизиты); // "ru = 'Электронная почта'".
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Адрес;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ДругаяИнформацияОрганизации",
		"ru = 'Другое'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Другое;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.РазрешитьВводНесколькихЗначений = Истина;
	
	
	// Справочник "Пользователи".
	Реквизиты = Новый Структура;
	Реквизиты.Вставить("Используется", Истина);
	Реквизиты.Вставить("Родитель", Справочники.ВидыКонтактнойИнформации.СправочникПользователи);
	Реквизиты.Вставить("ОтображатьВсегда", Истина);
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "EmailПользователя",
		"ru = 'Email'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.РазрешитьВводНесколькихЗначений = Истина;
	
	Элемент = ОбновлениеБЗК.ОписаниеПредопределенного(КодыЯзыков, Элементы, "ТелефонПользователя",
		"ru = 'Телефон'", Реквизиты);
	Элемент.Тип = Перечисления.ТипыКонтактнойИнформации.Телефон;
	Элемент.МожноИзменятьСпособРедактирования = Истина;
	Элемент.РазрешитьВводНесколькихЗначений = Истина;
	
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.КонтактнаяИнформация

// СтандартныеПодсистемы.КонтрольВеденияУчета

// См. КонтрольВеденияУчетаПереопределяемый.ПриОпределенииПроверок.
Процедура ПриОпределенииПроверок(ГруппыПроверок, Проверки) Экспорт
	
	Проверка = Проверки.Найти("СтандартныеПодсистемы.ПроверкаНезаполненныхОбязательныхРеквизитов", "Идентификатор");
	Если Проверка <> Неопределено Тогда
		Проверки.Удалить(Проверка);
	КонецЕсли;
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.КонтрольВеденияУчета

// СтандартныеПодсистемы.МашиночитаемыеДоверенности

// См. МашиночитаемыеДоверенностиФНСПереопределяемый.ПриЗаполненииРеквизитовКонтрагента.
Процедура ПриЗаполненииРеквизитовКонтрагента(Контрагент, Реквизиты) Экспорт
	Если ТипЗнч(Контрагент) <> Тип("СправочникСсылка.Контрагенты") Или Не ЗначениеЗаполнено(Контрагент) Тогда
		Возврат;
	КонецЕсли;
	
	ИменаРеквизитов = "ИНН, КПП, ЮридическоеФизическоеЛицо, КодПоОКПО, Наименование, НаименованиеПолное";
	СведенияОКонтрагенте = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Контрагент, ИменаРеквизитов);
	ЭтоИП = (СведенияОКонтрагенте.ЮридическоеФизическоеЛицо = Перечисления.ЮридическоеФизическоеЛицо.ФизическоеЛицо);
	
	Реквизиты.НаименованиеСокращенное          = СведенияОКонтрагенте.Наименование;
	Реквизиты.НаименованиеПолное               = СведенияОКонтрагенте.НаименованиеПолное;
	Реквизиты.ЭтоИндивидуальныйПредприниматель = ЭтоИП;
	Реквизиты.ИНН                              = СведенияОКонтрагенте.ИНН;
	Реквизиты.КПП                              = СведенияОКонтрагенте.КПП;
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.МашиночитаемыеДоверенности

// СтандартныеПодсистемы.РаботаВМоделиСервиса.ОчередьЗаданий

// См. ОчередьЗаданийПереопределяемый.ПриОпределенииПсевдонимовОбработчиков.
Процедура ПриОпределенииПсевдонимовОбработчиков(СоответствиеИменПсевдонимам) Экспорт
	
	// ИнтеграцияС1СДокументооборотом
	МетаданныеРегламентногоЗадания = Метаданные.РегламентныеЗадания.Найти("ОбменДаннымиС1СДокументооборотом");
	Если МетаданныеРегламентногоЗадания <> Неопределено Тогда
		СоответствиеИменПсевдонимам.Вставить(МетаданныеРегламентногоЗадания.ИмяМетода);
	КонецЕсли;
	// Конец ИнтеграцияС1СДокументооборотом
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.РаботаВМоделиСервиса.ОчередьЗаданий

// ТехнологияСервиса

// См. ВыгрузкаЗагрузкаДанныхПереопределяемый.ПриЗаполненииТиповИсключаемыхИзВыгрузкиЗагрузки.
Процедура ПриЗаполненииТиповИсключаемыхИзВыгрузкиЗагрузки(Типы) Экспорт
	ИсключенныеПланыОбмена = Новый Структура(
	"АвтономнаяРабота,
	|ОбменВРаспределеннойИнформационнойБазе,
	|ОбменЗарплата3Бухгалтерия3,
	|ОбменЗГУБГУ1,
	|ОбменЗГУБГУ2,
	|СинхронизацияДанныхЧерезУниверсальныйФормат,
	|СинхронизацияДанныхЧерезУниверсальныйФорматБО");
	Для Каждого КлючИЗначение Из ИсключенныеПланыОбмена Цикл
		МетаданныеПланаОбмена = Метаданные.ПланыОбмена.Найти(КлючИЗначение.Ключ);
		Если МетаданныеПланаОбмена <> Неопределено
			И Типы.Найти(МетаданныеПланаОбмена) = Неопределено Тогда
			Типы.Добавить(МетаданныеПланаОбмена);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

// См. ЛичныйКабинетСервисаПереопределяемый.ПриОпределенииПараметров.
Процедура ЛичныйКабинетБТСПриОпределенииПараметров(Параметры) Экспорт
	Параметры.Использовать = Истина;
	Параметры.ОтображатьВПанелиРазделов = Истина;
КонецПроцедуры

// Конец ТехнологияСервиса

// РегламентированнаяОтчетность

// См. РегламентированнаяОтчетностьПереопределяемый.ИДКонфигурации.
Функция ИДКонфигурации() Экспорт
	Возврат КраткоеИмяПрограммы(Модуль().СведенияОКонфигурации());
КонецФункции

// См. РегламентированнаяОтчетностьПереопределяемый.КраткоеНазваниеПрограммы.
Функция КраткоеНазваниеПрограммы() Экспорт
	Возврат КраткоеНазваниеПрограммыСлужебный();
КонецФункции

// См. РегламентированнаяОтчетностьПереопределяемый.ПолучитьУИДКонфигурации.
Процедура ПолучитьУИДКонфигурации(УИДКонфигурации) Экспорт
	СведенияОКонфигурации = Модуль().СведенияОКонфигурации();
	Если СведенияОКонфигурации.Хозрасчетная Тогда
		УИДКонфигурации = "4759bb58-0ce1-11df-85fa-001b24e002fe";
	ИначеЕсли СведенияОКонфигурации.Бюджетная Тогда
		УИДКонфигурации = "bd688976-15b9-11e1-82b8-001b24e002fe";
	КонецЕсли;
КонецПроцедуры

// Конец РегламентированнаяОтчетность

// См. СтандартныеПодсистемыСервер.ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод
Процедура ПриОпределенииМетодовРазрешенныхДляВызоваКакПроизвольныйКод(Методы) Экспорт
	Методы.Вставить("ОбновитьРасписаниеОбменаСКонтролирующимиОрганамиПриОбновлении");
	Методы.Вставить("ИсправитьВидыКонтактнойИнформации");
	Методы.Вставить("ОбновитьВидыКонтактнойИнформации");
КонецПроцедуры

#КонецОбласти

#Область УстаревшиеПроцедурыИФункции

// Устарела. Следует обновить БИД и использовать процедуру ДополнитьСоответствиеТипов.
// См. ИнтеграцияС1СДокументооборотПереопределяемый.ПриОпределенииПодходящегоТипа.
Процедура ПриОпределенииПодходящегоТипа(ТипОбъектаXDTO, ТипыКандидаты, ПодходящийТип) Экспорт
	
	// АПК:278-выкл Базовые подсистемы могут условно вызывать расширенные.
	Если ОбщегоНазначения.ПодсистемаСуществует("ЗарплатаКадрыПриложения.КонфигурацииЗарплатаКадрыРасширенная") Тогда
		Модуль = ОбщегоНазначения.ОбщийМодуль("КонфигурацииЗарплатаКадрыРасширенный");
		Модуль.ПриОпределенииПодходящегоТипа(ТипОбъектаXDTO, ТипыКандидаты, ПодходящийТип);
	КонецЕсли;
	// АПК:278-вкл
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти


#Область СлужебныйПрограммныйИнтерфейс

#Область ОбновлениеИБ

// Возвращает идентификатор интернет-поддержки подсистемы ЗарплатаКадрыПриложения.КонфигурацииЗарплатаКадры.
//
// Возвращаемое значение:
//   Строка - Пустая строка.
//
Функция ИдентификаторИнтернетПоддержки() Экспорт
	Конфигурация = Модуль().СведенияОКонфигурации();
	
	Если Конфигурация.Хозрасчетная Тогда
		Если Конфигурация.Базовая Тогда
			Возврат "HRMBase";
		ИначеЕсли Конфигурация.Корпоративная Тогда
			Возврат "HRMCorp";
		Иначе
			Возврат "HRM";
		КонецЕсли;
	ИначеЕсли Конфигурация.Бюджетная Тогда
		Если Конфигурация.Базовая Тогда
			Возврат "StateHRMBase";
		ИначеЕсли Конфигурация.Корпоративная Тогда
			Возврат "StateHRMCorp";
		Иначе
			Возврат "StateHRM";
		КонецЕсли;
	КонецЕсли;
	
	Возврат "";
КонецФункции

// Регистрирует обработчики обновления, необходимые данной подсистеме.
//
// Параметры:
//   Обработчики - ТаблицаЗначений - См. ОбновлениеИнформационнойБазы.НоваяТаблицаОбработчиковОбновления().
//
Процедура ЗарегистрироватьОбработчикиОбновления(Обработчики) Экспорт
	
	Обработчик = Обработчики.Добавить();
	Обработчик.Процедура = "КонфигурацииЗарплатаКадры.ОбновитьВидыКонтактнойИнформации";
	Обработчик.НачальноеЗаполнение = Истина;
	
	Обработчик = Обработчики.Добавить();
	Обработчик.Версия          = "3.1.10.103";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Идентификатор   = Новый УникальныйИдентификатор("348217de-98c7-11e9-80cd-4cedfb43b11a");
	Обработчик.Процедура       = "КонфигурацииЗарплатаКадры.ОбновитьРасписаниеОбменаСКонтролирующимиОрганамиПриОбновлении";
	Обработчик.Комментарий     = НСтр("ru = 'Обновление расписания обмена с контролирующими органами.'");
	
	УправлениеДоступом.ДобавитьОбработчикОбновленияДляВключенияУниверсальногоОграничения("", Обработчики, Истина);
	
	Если ЗарплатаКадрыКлиентСервер.ИспользуютсяРегистрацииВОрганеСтатистики() Тогда
		Обработчик = Обработчики.Добавить();
		Обработчик.Версия = "3.1.29.4";
		Обработчик.РежимВыполнения = ОбновлениеБЗК.ОсновнойРежимВыполненияОбновления();
		Обработчик.Процедура = "ЗарплатаКадрыВнутренний.ЗаполнитьРегистрацииВОрганеСтатистики";
		Обработчик.Идентификатор = Новый УникальныйИдентификатор("9e86e8ed-df57-400a-bf13-5de7462edc52");
		Обработчик.Комментарий = НСтр("ru = 'Заполнение регистраций в органе статистики'");
	КонецЕсли;
	
	Обработчик = Обработчики.Добавить();
	Обработчик.Версия = "3.1.34.28";
	Обработчик.Процедура = "КонфигурацииЗарплатаКадры.ИсправитьВидыКонтактнойИнформации";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("1703a031-4aca-11f0-815a-c84d4423fb75");
	Обработчик.Комментарий = НСтр("ru = 'Заполнение видов контактной информации'");
	
КонецПроцедуры

// Обновляет все виды контактной информации.
//   См. также УправлениеКонтактнойИнформациейПереопределяемый.ПриНачальномЗаполненииЭлементов.
Процедура ОбновитьВидыКонтактнойИнформации() Экспорт
	
	ОбновлениеИнформационнойБазы.ОбновитьПредопределенныеЭлементы(Метаданные.Справочники.ВидыКонтактнойИнформации);
	
КонецПроцедуры

// Обработчик обновления на 3.1.10.103.
Процедура ОбновитьРасписаниеОбменаСКонтролирующимиОрганамиПриОбновлении(ПараметрыОбновления = Неопределено) Экспорт
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиОбновлениеИнформационнойБазы.ВключитьАвтоматическийОбменСКонтролирующимиОрганамиПриОбновлении();
	Если ПараметрыОбновления <> Неопределено Тогда
		ПараметрыОбновления.ОбработкаЗавершена = Истина;
	КонецЕсли;
КонецПроцедуры

// Обработчик обновления на 3.1.34.28.
Процедура ИсправитьВидыКонтактнойИнформации(ПараметрыОбновления = Неопределено) Экспорт
	
	Если Метаданные.Языки.Количество() = 1 Тогда
		ТекстЗапроса =
		"ВЫБРАТЬ
		|	ВидыКонтактнойИнформации.Ссылка КАК Ссылка,
		|	ВидыКонтактнойИнформации.ИмяПредопределенныхДанных КАК ИмяПредопределенныхДанных
		|ИЗ
		|	Справочник.ВидыКонтактнойИнформации КАК ВидыКонтактнойИнформации
		|ГДЕ
		|	ВидыКонтактнойИнформации.ЭтоГруппа = ЛОЖЬ
		|	И (ЕСТЬNULL(ВидыКонтактнойИнформации.ИдентификаторДляФормул, """") = """"
		|			ИЛИ ЕСТЬNULL(ВидыКонтактнойИнформации.ВидРедактирования, """") = """"
		|			ИЛИ ЕСТЬNULL(ВидыКонтактнойИнформации.ИмяГруппы, """") = """"
		|			ИЛИ ВидыКонтактнойИнформации.ОтображатьВсегда = ЛОЖЬ)";
	Иначе
		ТекстЗапроса = 
		"ВЫБРАТЬ
		|	ВидыКонтактнойИнформации.Ссылка КАК Ссылка,
		|	ВидыКонтактнойИнформации.ИмяПредопределенныхДанных КАК ИмяПредопределенныхДанных
		|ИЗ
		|	Справочник.ВидыКонтактнойИнформации КАК ВидыКонтактнойИнформации
		|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ВидыКонтактнойИнформации.Представления КАК ПредставленияВида
		|		ПО (ПредставленияВида.Ссылка = ВидыКонтактнойИнформации.Ссылка)
		|ГДЕ
		|	ВидыКонтактнойИнформации.ЭтоГруппа = ЛОЖЬ
		|	И (ЕСТЬNULL(ВидыКонтактнойИнформации.ИдентификаторДляФормул, """") = """"
		|			ИЛИ ЕСТЬNULL(ВидыКонтактнойИнформации.ВидРедактирования, """") = """"
		|			ИЛИ ЕСТЬNULL(ВидыКонтактнойИнформации.ИмяГруппы, """") = """"
		|			ИЛИ ВидыКонтактнойИнформации.ОтображатьВсегда = ЛОЖЬ
		|			ИЛИ ПредставленияВида.Ссылка ЕСТЬ NULL)"
	КонецЕсли;
	Запрос = Новый Запрос(ТекстЗапроса);
	Таблица = Запрос.Выполнить().Выгрузить();
	
	МассивСсылок = Новый Массив;
	Для Каждого СтрокаТаблицы Из Таблица Цикл
		Если Не СтрНачинаетсяС(СтрокаТаблицы.ИмяПредопределенныхДанных, "Удалить") Тогда
			МассивСсылок.Добавить(СтрокаТаблицы.Ссылка);
		КонецЕсли;
	КонецЦикла;
	
	Если МассивСсылок.Количество() > 0 Тогда
		ПараметрыОбновленияПредопределенных = ОбновлениеИнформационнойБазы.ПараметрыОбновленияПредопределенныхЭлементов();
		ПараметрыОбновленияПредопределенных.Элементы = МассивСсылок;
		ОбновлениеИнформационнойБазы.ОбновитьПредопределенныеЭлементы(Метаданные.Справочники.ВидыКонтактнойИнформации,
			ПараметрыОбновленияПредопределенных);
	КонецЕсли;
	
	Если ПараметрыОбновления <> Неопределено Тогда
		ПараметрыОбновления.ОбработкаЗавершена = Истина;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

// Возвращает модуль, реализующий базовый контракт служебного программного интерфейса.
Функция Модуль() Экспорт
	Если ОбщегоНазначения.ПодсистемаСуществует("ЗарплатаКадрыПриложения.КонфигурацииЗарплатаКадрыРасширенная") Тогда
		Возврат ОбщегоНазначения.ОбщийМодуль("КонфигурацииЗарплатаКадрыРасширенный");
	Иначе
		Возврат КонфигурацииЗарплатаКадры;
	КонецЕсли;
КонецФункции

// Возвращает краткое название конфигурации.
// Длина возвращаемого значения не должна превышать 30 символов.
//
// Возвращаемое значение:
//   Строка - краткое название конфигурации.
//
// Пример:
//   Возврат "1С:Бухгалтерия";
//
Функция КраткоеНазваниеПрограммыСлужебный() Экспорт
	
	СведенияОКонфигурации = Модуль().СведенияОКонфигурации();
	
	КраткоеНазвание = КраткоеИмяПрограммы(СведенияОКонфигурации);
	Если Не ЗначениеЗаполнено(КраткоеНазвание) Тогда
		Возврат "";
	КонецЕсли;
	
	Если СведенияОКонфигурации.Корпоративная Тогда
		КраткоеНазвание = КраткоеНазвание + " КОРП";
	КонецЕсли;
	
	Возврат "1С:" + КраткоеНазвание;
	
КонецФункции

#Область БазовыйКонтракт

// Возвращает сведения о конфигурации.
//
// Возвращаемое значение:
//   Структура - Сведения о конфигурации
//       * Хозрасчетная - Булево
//       * Бюджетная - Булево
//       * Базовая - Булево
//       * Корпоративная - Булево
//
Функция СведенияОКонфигурации() Экспорт
	УстановитьПривилегированныйРежим(Истина);
	
	Сведения = Новый Структура("Хозрасчетная, Бюджетная, Базовая, Корпоративная");
	
	Сведения.Хозрасчетная  = Истина;
	Сведения.Бюджетная     = Ложь;
	Сведения.Корпоративная = Ложь;
	Сведения.Базовая       = СтандартныеПодсистемыСервер.ЭтоБазоваяВерсияКонфигурации();
	
	Возврат Сведения;
КонецФункции

#КонецОбласти

#Область ЗарплатаКадрыПереопределяемый

// См. ЗарплатаКадрыПереопределяемый.ПриОпределенииИмениКлиентскогоПриложения.
Процедура ПриОпределенииИмениКлиентскогоПриложения(ИмяПриложения, СтандартнаяОбработка) Экспорт
	Если Не СтандартнаяОбработка Тогда
		Возврат;
	КонецЕсли;
	ИдентификаторИнтернетПоддержки = ИдентификаторИнтернетПоддержки();
	Если ЗначениеЗаполнено(ИдентификаторИнтернетПоддержки) Тогда
		ИмяПриложения = ИмяПриложения + " " + ИдентификаторИнтернетПоддержки + " " + Метаданные.Версия;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#КонецОбласти


#Область СлужебныеПроцедурыИФункции

#Область ОбработчикиСобытий

// См. ПодпискаНаСобытие.ОбновитьРасписаниеОбменаСКонтролирующимиОрганами (СправочникОбъект.Организации.ПриЗаписи).
Процедура ОбновитьРасписаниеОбменаСКонтролирующимиОрганами(Источник, Отказ) Экспорт
	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(Источник) Тогда
		Возврат;
	КонецЕсли;
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПриЗаписиОрганизации(Источник, Отказ);
КонецПроцедуры

// См. ПодпискаНаСобытие.НеИспользоватьНесколькоОрганизацийПриЗаписи (КонстантаМенеджерЗначения.НеИспользоватьНесколькоОрганизаций.ПриЗаписи).
Процедура НеИспользоватьНесколькоОрганизацийПриЗаписи(Источник, Отказ) Экспорт
	
	РазрешеноДобавлениеОрганизации = Не СтандартныеПодсистемыСервер.ЭтоБазоваяВерсияКонфигурации();
	Константы.РазрешитьДобавлениеОрганизацииЗарплатаКадры.Установить(Источник.Значение И РазрешеноДобавлениеОрганизации);
	
КонецПроцедуры

#КонецОбласти

Процедура УстановитьФункциональнуюОпциюРазрешитьДобавлениеОрганизацииЗарплатаКадры() Экспорт
	
	РазрешеноДобавлениеОрганизации = Константы.НеИспользоватьНесколькоОрганизаций.Получить() И Не СтандартныеПодсистемыСервер.ЭтоБазоваяВерсияКонфигурации();
	Константы.РазрешитьДобавлениеОрганизацииЗарплатаКадры.Установить(РазрешеноДобавлениеОрганизации);	
	
КонецПроцедуры

// Возвращает краткое имя программы без уточнения.
Функция КраткоеИмяПрограммы(СведенияОКонфигурации)
	Если СведенияОКонфигурации.Хозрасчетная Тогда
		Возврат "ЗУП";
	ИначеЕсли СведенияОКонфигурации.Бюджетная Тогда
		Возврат "ЗГУ";
	Иначе
		Возврат "";
	КонецЕсли;
КонецФункции

#КонецОбласти
