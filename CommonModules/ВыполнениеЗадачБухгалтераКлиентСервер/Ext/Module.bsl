
#Область ПрограммныйИнтерфейс

// Возвращает список команд для выполнения задачи.
//
Функция НовыеПараметрыКомандЗадачи() Экспорт
	
	ПараметрыКоманд = Новый Структура;
	ПараметрыКоманд.Вставить("Задача",                      Неопределено);
	ПараметрыКоманд.Вставить("ИдентификаторЗадачи",         "");
	ПараметрыКоманд.Вставить("Организация",                 Неопределено);
	ПараметрыКоманд.Вставить("РегистрацияВНалоговомОргане", Неопределено);
	ПараметрыКоманд.Вставить("Срок",                        '00010101');
	ПараметрыКоманд.Вставить("НачалоВыполнения",            '00010101');
	ПараметрыКоманд.Вставить("Правило",                     Неопределено);
	ПараметрыКоманд.Вставить("ИдентификаторПравила",        "");
	ПараметрыКоманд.Вставить("ПериодСобытия",               '00010101');
	ПараметрыКоманд.Вставить("Периодичность",               Неопределено); 
	ПараметрыКоманд.Вставить("Наименование",                "");
	ПараметрыКоманд.Вставить("Действие",                    Неопределено);
	ПараметрыКоманд.Вставить("ЕстьИнформацияНаИТС",         Ложь);
	ПараметрыКоманд.Вставить("АдресНаИТС",                  ЗадачиБухгалтераКлиентСервер.СсылкаНаИТС()); // Адрес по умолчанию
	ПараметрыКоманд.Вставить("СпособОплаты",                Неопределено);
	ПараметрыКоманд.Вставить("НачальныйСрок",               '00010101');
	ПараметрыКоманд.Вставить("ВыполнитьЕдинымПомощником",   Ложь);
	ПараметрыКоманд.Вставить("КоличествоЗадач",             1);
	
	Возврат ПараметрыКоманд;
	
КонецФункции

// Возвращает структуру параметров для списка задач бухгалтера.
//
Функция НовыйПараметрыФормыПомощника() Экспорт
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Организация",                 Неопределено);
	ПараметрыФормы.Вставить("РегистрацияВНалоговомОргане", Неопределено);
	ПараметрыФормы.Вставить("ПериодСобытия",               Дата('00010101'));
	ПараметрыФормы.Вставить("Правило",                     Неопределено);
	ПараметрыФормы.Вставить("Срок",                        Дата('00010101'));
	ПараметрыФормы.Вставить("Описание",                    "");
	ПараметрыФормы.Вставить("ЕстьИнформацияНаИТС",         Ложь);
	ПараметрыФормы.Вставить("ИдентификаторЗадачи",         "");
	ПараметрыФормы.Вставить("НачальныйСрок",               Дата('00010101'));
	ПараметрыФормы.Вставить("ВызовИзЕдиногоПомощника",     Ложь);
	ПараметрыФормы.Вставить("СуммаЗачетаЕНС",              Ложь);
	
	Возврат ПараметрыФормы;
	
КонецФункции

// Определяет является ли правило задачи бухгалтера документом СЭДО
//
// Параметры:
//  Правило - Ссылка - см. измерение Правило регистра сведений ЗадачиБухгалтера
// 
// Возвращаемое значение:
//  Булево - результат проверки
//
Функция ЭтоДокументСЭДО(Правило) Экспорт
	
	Признак = Ложь;
	ВыполнениеЗадачБухгалтераКлиентСерверПереопределяемый.ПриОпределенииЭтоДокументСЭДО(Признак, Правило);
	Возврат Признак;
	
КонецФункции

#КонецОбласти
