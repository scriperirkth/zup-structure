#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗначенияДляЗаполнения = Новый Структура;
	ЗначенияДляЗаполнения.Вставить("Организация",	"Объект.Организация");
	ЗначенияДляЗаполнения.Вставить("Подразделение",	"Объект.Подразделение");
	ЗарплатаКадры.ЗаполнитьПервоначальныеЗначенияВФорме(ЭтотОбъект, ЗначенияДляЗаполнения);
	
	УстановитьДоступностьЭлементов(ЭтотОбъект);
	ПроверитьДоступностьДоговора();
	
	ЗарплатаКадрыПереопределяемый.ЗаполнитьРеквизитОрганизацияПриОднофирменномУчете(ЭтотОбъект, СтандартнаяОбработка, "Организация");
	
	Если СтандартнаяОбработка Тогда
		Если Не ПолучитьФункциональнуюОпцию("ИспользоватьНесколькоОрганизацийЗарплатаКадрыБазовая") 
			И Не ЗначениеЗаполнено(Объект.Организация) Тогда 
			Объект.Организация = Справочники.Организации.ОрганизацияПоУмолчанию();
		КонецЕсли;
	КонецЕсли;
	
	Элементы.ПрикреплениеСотрудниковКомментарий.Видимость = Ложь;
	Элементы.ОткреплениеСотрудниковКомментарий.Видимость = Ложь;
	
	ИспользоватьДополнительныеРеквизитыИСведения = ПолучитьФункциональнуюОпцию("ИспользоватьДополнительныеРеквизитыИСведения");
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПараметрыРазмещения = ПодключаемыеКоманды.ПараметрыРазмещения();
	ПараметрыРазмещения.КоманднаяПанель = Элементы.МедицинскоеСтрахованиеКоманднаяПанель;
	ПараметрыРазмещения.Источники = МедицинскоеСтрахование.Отбор.ДоступныеПоляОтбора.НайтиПоле(Новый ПолеКомпоновкиДанных("Ссылка")).Тип;
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект, ПараметрыРазмещения);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
	// СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов") Тогда
		МодульУчетОригиналовПервичныхДокументов = ОбщегоНазначения.ОбщийМодуль("УчетОригиналовПервичныхДокументов");
		МодульУчетОригиналовПервичныхДокументов.ПриСозданииНаСервере_ФормаСписка(ЭтотОбъект, Элементы.МедицинскоеСтрахование, Элементы.МедицинскоеСтрахованиеКомментарий);
		МодульУчетОригиналовПервичныхДокументов.УстановитьУсловноеОформлениеВФормеСписка(ЭтотОбъект, Элементы.МедицинскоеСтрахование);
	КонецЕсли;
	// Конец СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	
КонецПроцедуры

&НаСервере
Процедура ПриЗагрузкеДанныхИзНастроекНаСервере(Настройки)
	ПриИзмененииОсновныхПолейНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "Запись_ПрикреплениеКПрограммамМедицинскогоСтрахования" Тогда
		ПрикреплениеКПрограммамСтрахованияЗаполнить();
	ИначеЕсли ИмяСобытия = "ИзменениеДанныхФизическогоЛица" Или ИмяСобытия = "Запись_ИзменениеУсловийМедицинскогоСтрахования" Тогда
		ИзмененияПерсональныхДанныхЗаполнить();
	ИначеЕсли ИмяСобытия = "Запись_ОткреплениеОтПрограммМедицинскогоСтрахования" Или ИмяСобытия = "Запись_РешениеОбУвольнении" Тогда
		ПрикреплениеКПрограммамСтрахованияЗаполнить();
		ОткреплениеОтПрограммСтрахованияЗаполнить();
	ИначеЕсли ИмяСобытия = "Запись_ДоговорыСтрахования" И Источник = Объект.ДоговорСтрахования Тогда 
		ПроверитьДоступностьДоговора();
	КонецЕсли;
	
	// СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов") Тогда
		МодульУчетОригиналовПервичныхДокументовКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("УчетОригиналовПервичныхДокументовКлиент");
		МодульУчетОригиналовПервичныхДокументовКлиент.ОбработчикОповещенияФормаСписка(ИмяСобытия, ЭтотОбъект, Элементы.МедицинскоеСтрахование);
	КонецЕсли;
	// Конец СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ОрганизацияПриИзменении(Элемент)
	ПриИзмененииОсновныхПолейНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура СтраховаяКомпанияПриИзменении(Элемент)
	ПриИзмененииОсновныхПолейНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ДоговорСтрахованияПриИзменении(Элемент)
	ПриИзмененииОсновныхПолейНаСервере(Истина);
КонецПроцедуры

&НаКлиенте
Процедура ПодразделениеПриИзменении(Элемент)
	ПриИзмененииОсновныхПолейНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ГруппаСотрудниковНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ПараметрыОткрытия = Новый Структура;
	ПараметрыОткрытия.Вставить("РежимВыбора", Истина);
	ПараметрыОткрытия.Вставить("ТекущаяСтрока", ГруппаСотрудников);
	
	ОписаниеОповещенияОЗакрытии = Новый ОписаниеОповещения("УсловияСтрахованияСотрудниковЗакрытие", ЭтотОбъект);
	ОткрытьФорму("Справочник.ГруппыСотрудников.ФормаСписка", ПараметрыОткрытия, ЭтотОбъект,,,, ОписаниеОповещенияОЗакрытии);
	
КонецПроцедуры

&НаКлиенте
Процедура ГруппаСотрудниковПриИзменении(Элемент)
	ПриИзмененииОсновныхПолейНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура УсловияСтрахованияСотрудниковПриИзменении(Элемент)
	
	Если УсловияСтрахованияСотрудников = 0 Тогда
		ГруппаСотрудников = Неопределено;
		ПриИзмененииОсновныхПолейНаСервере();
	КонецЕсли;
	
	УстановитьДоступностьЭлементов(ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыПрикреплениеСотрудников

&НаКлиенте
Процедура ПрикреплениеСотрудниковВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если Поле.Имя = "ПрикреплениеСотрудниковКоманда" Тогда
		
		ТекущиеДанные = Элемент.ТекущиеДанные;
		Если ТекущиеДанные = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
		Если ТекущиеДанные.Команда = "Отложить" Тогда
			ТекущиеДанные.ПрикреплениеКПрограммеСтрахованияОтложено = Истина;
			ОтложитьСотрудника(ТекущиеДанные.Сотрудник);
			ТекущиеДанные.Команда = НСтр("ru = 'Возобновить'");
		Иначе
			ТекущиеДанные.ПрикреплениеКПрограммеСтрахованияОтложено = Ложь;
			ВозобновитьСотрудника(ТекущиеДанные.Сотрудник);
			ТекущиеДанные.Команда = НСтр("ru = 'Отложить'");
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыОткреплениеСотрудников

&НаКлиенте
Процедура ОткреплениеСотрудниковВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если Поле.Имя = "ОткреплениеСотрудниковКоманда" Тогда
		
		ТекущиеДанные = Элемент.ТекущиеДанные;
		Если ТекущиеДанные = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
		Если ТекущиеДанные.Команда = "Отложить" Тогда
			ТекущиеДанные.ОткреплениеОтПрограммыСтрахованияОтложено = Истина;
			ОтложитьСотрудника(ТекущиеДанные.Сотрудник);
			ТекущиеДанные.Команда = НСтр("ru = 'Возобновить'");
		Иначе
			ТекущиеДанные.ОткреплениеОтПрограммыСтрахованияОтложено = Ложь;
			ВозобновитьСотрудника(ТекущиеДанные.Сотрудник);
			ТекущиеДанные.Команда = НСтр("ru = 'Отложить'");
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыИзменениеПерсональныхДанных

&НаКлиенте
Процедура ИзменениеПерсональныхДанныхВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ОткрытьФормуИзмененияОтправленныхДанных();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыМедицинскоеСтрахование

&НаСервереБезКонтекста
Процедура МедицинскоеСтрахованиеПриПолученииДанныхНаСервере(ИмяЭлемента, Настройки, Строки)
	
	// СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов") Тогда
		МодульУчетОригиналовПервичныхДокументов = ОбщегоНазначения.ОбщийМодуль("УчетОригиналовПервичныхДокументов");
		МодульУчетОригиналовПервичныхДокументов.ПриПолученииДанныхНаСервере(Строки);
	КонецЕсли;
	// Конец СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	
КонецПроцедуры

&НаКлиенте
Процедура МедицинскоеСтрахованиеВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	// СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов") Тогда
		МодульУчетОригиналовПервичныхДокументовКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("УчетОригиналовПервичныхДокументовКлиент");
		МодульУчетОригиналовПервичныхДокументовКлиент.СписокВыбор(Поле.Имя, ЭтотОбъект, Элементы.МедицинскоеСтрахование, СтандартнаяОбработка);
	КонецЕсли;
	// Конец СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
	
КонецПроцедуры

&НаКлиенте
Процедура МедицинскоеСтрахованиеПриАктивизацииСтроки(Элемент)
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПодключаемыеКомандыКлиент.НачатьОбновлениеКоманд(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ВыполнитьКоманду(Команда)
	ПодключаемыеКомандыКлиент.ВыполнитьКоманду(ЭтотОбъект, Команда, Элементы.МедицинскоеСтрахование);
КонецПроцедуры

&НаСервере
Процедура Подключаемый_ВыполнитьКомандуНаСервере(Контекст, Результат) Экспорт
	ПодключаемыеКоманды.ВыполнитьКоманду(ЭтотОбъект, Контекст, Элементы.МедицинскоеСтрахование, Результат);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКоманды()
	ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Элементы.МедицинскоеСтрахование);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

&НаКлиенте
Процедура ПрикреплениеСотрудниковПоказыватьОтложенныхСтрахователей(Команда)
	
	Элементы.ПрикреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка = Не Элементы.ПрикреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	ПоказыватьОтложенныхСтрахователей = Элементы.ПрикреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	ИсключитьПрикрепленныхСотрудников = Элементы.ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников.Пометка;
	УстановитьОтборСтрокПрикреплениеСотрудников(Элементы, "ПрикреплениеСотрудников", ИсключитьПрикрепленныхСотрудников, ПоказыватьОтложенныхСтрахователей);
	Элементы.ПрикреплениеСотрудниковКомментарий.Видимость = Элементы.ПрикреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	
КонецПроцедуры

&НаКлиенте
Процедура ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников(Команда)
	
	Элементы.ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников.Пометка = Не Элементы.ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников.Пометка;
	ИсключитьПрикрепленныхСотрудников = Элементы.ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников.Пометка;
	УстановитьОтборСтрокПрикреплениеСотрудников(Элементы, "ПрикреплениеСотрудников", ИсключитьПрикрепленныхСотрудников, ПоказыватьОтложенныхСтрахователей);
	Если Не ИсключитьПрикрепленныхСотрудников Тогда
		ПрикреплениеКПрограммамСтрахованияЗаполнить();	
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей(Команда)
	
	Элементы.ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка = Не Элементы.ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	ПоказыватьОтложенныхСтрахователей = Элементы.ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	УстановитьОтборСтрок(Элементы, "ОткреплениеСотрудников", "ОткреплениеОтПрограммыСтрахованияОтложено", ПоказыватьОтложенныхСтрахователей);
	Элементы.ОткреплениеСотрудниковКомментарий.Видимость = Элементы.ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	
КонецПроцедуры

&НаКлиенте
Процедура ПрикреплениеСотрудниковОбновить(Команда)
	ПрикреплениеКПрограммамСтрахованияЗаполнить();
КонецПроцедуры

&НаКлиенте
Процедура ПрикреплениеСформировать(Команда)
	
	Отказ = ПроверитьНаКлиенте();
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	МассивСотрудников = Новый Массив;
	
	ПараметрыОтбораСтрок = Новый Структура;
	ПараметрыОтбораСтрок.Вставить("ПрикреплениеКПрограммеСтрахованияОтложено", Ложь);
	НайденныеСтроки = Объект.ПрикреплениеСотрудников.НайтиСтроки(ПараметрыОтбораСтрок);
	Для каждого НайденнаяСтрока Из НайденныеСтроки Цикл
		МассивСотрудников.Добавить(НайденнаяСтрока.Сотрудник);
	КонецЦикла;
	
	ОткрытьПрикреплениеСотрудников(, МассивСотрудников);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткреплениеСформировать(Команда)
	
	Отказ = ПроверитьНаКлиенте();
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	МассивСотрудников = Новый Массив;
	ДатыОткрепления = Новый Соответствие;
	
	ПараметрыОтбораСтрок = Новый Структура;
	ПараметрыОтбораСтрок.Вставить("ОткреплениеОтПрограммыСтрахованияОтложено", Ложь);
	НайденныеСтроки = Объект.ОткреплениеСотрудников.НайтиСтроки(ПараметрыОтбораСтрок);
	Для каждого НайденнаяСтрока Из НайденныеСтроки Цикл
		МассивСотрудников.Добавить(НайденнаяСтрока.Сотрудник);
		ДатыОткрепления.Вставить(НайденнаяСтрока.Сотрудник, НайденнаяСтрока.ДатаОткрепления);
	КонецЦикла;
	
	ОткрытьОткреплениеСотрудников(, МассивСотрудников, ДатыОткрепления);
	
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьСтрокуПрикрепления(Команда)
	
	ТекущиеДанные = Элементы.ПрикреплениеСотрудников.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ОткрытьПрикреплениеСотрудников(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткреплениеСотрудниковОбновить(Команда)
	ОткреплениеОтПрограммСтрахованияЗаполнить();
КонецПроцедуры

&НаКлиенте
Процедура ИзменениеЛичныхДанныхОбновить(Команда)
	ИзмененияПерсональныхДанныхЗаполнить();
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьСтрокуОткрепления(Команда)
	
	ТекущиеДанные = Элементы.ОткреплениеСотрудников.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ДатыОткрепления = Новый Соответствие;
	ДатыОткрепления.Вставить(ТекущиеДанные.Сотрудник, ТекущиеДанные.ДатаОткрепления);
	
	ОткрытьОткреплениеСотрудников(ТекущиеДанные, , ДатыОткрепления);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьИзменениеПерсональныхДанных(Команда)
	
	Если Элементы.ИзменениеПерсональныхДанных.ВыделенныеСтроки.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	МассивЗастрахованных = Новый Массив;
	
	Для Каждого ВыделеннаяСтрока Из Элементы.ИзменениеПерсональныхДанных.ВыделенныеСтроки Цикл
		СтрокаДанных = Объект.ИзменениеПерсональныхДанныхСотрудников.НайтиПоИдентификатору(ВыделеннаяСтрока);
		Если СтрокаДанных = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		МассивЗастрахованных.Добавить(СтрокаДанных.Застрахованный);
	КонецЦикла;
	
	ПараметрыОткрытия = Новый Структура;
	ЗначенияЗаполнения = Новый Структура;
	ЗначенияЗаполнения.Вставить("Организация", Объект.Организация);
	ЗначенияЗаполнения.Вставить("СтраховаяКомпания", Объект.СтраховаяКомпания);
	ЗначенияЗаполнения.Вставить("ДоговорСтрахования", Объект.ДоговорСтрахования);
	ЗначенияЗаполнения.Вставить("Подразделение", Объект.Подразделение);
	ЗначенияЗаполнения.Вставить("МассивЗастрахованных", МассивЗастрахованных);
	ПараметрыОткрытия.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
	ОткрытьФорму("Документ.ИзменениеУсловийМедицинскогоСтрахования.ФормаОбъекта", ПараметрыОткрытия, ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура ИгнорироватьИзменениеПерсональныхДанных(Команда)
	
	ТекущиеДанные = Элементы.ИзменениеПерсональныхДанных.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ИгнорироватьИзменениеПерсональныхДанныхНаСервере(ТекущиеДанные.Застрахованный);
	ИзмененияПерсональныхДанныхЗаполнить();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтложитьСотрудниковВПрикреплении(Команда)
	
	Если Элементы.ПрикреплениеСотрудников.ВыделенныеСтроки.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ОтложитьСотрудниковВПрикрепленииЗавершение", ЭтотОбъект);
	ОткрытьФорму("Обработка.МедицинскоеСтрахование.Форма.КомментарийДляОтложенногоСотрудника", , ЭтотОбъект, , , , Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВозобновитьСотрудниковВПрикреплении(Команда)
	
	Если Элементы.ПрикреплениеСотрудников.ВыделенныеСтроки.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	МассивСотрудников = Новый Массив;
	
	Для Каждого ВыделеннаяСтрока Из Элементы.ПрикреплениеСотрудников.ВыделенныеСтроки Цикл
		СтрокаДанных = Объект.ПрикреплениеСотрудников.НайтиПоИдентификатору(ВыделеннаяСтрока);
		Если СтрокаДанных <> Неопределено Тогда
			СтрокаДанных.ПрикреплениеКПрограммеСтрахованияОтложено = Ложь;
			СтрокаДанных.Комментарий = "";
			МассивСотрудников.Добавить(СтрокаДанных.Сотрудник);
		КонецЕсли;
	КонецЦикла;
	
	ВозобновитьСотрудников(МассивСотрудников);
	УстановитьТекстЗаголовкаПрикрепление();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтложитьСотрудниковВОткреплении(Команда)
	
	Если Элементы.ОткреплениеСотрудников.ВыделенныеСтроки.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ОтложитьСотрудниковВОткрепленииЗавершение", ЭтотОбъект);
	ОткрытьФорму("Обработка.МедицинскоеСтрахование.Форма.КомментарийДляОтложенногоСотрудника", , ЭтотОбъект, , , , Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВозобновитьСотрудниковВОткреплении(Команда)
	
	Если Элементы.ОткреплениеСотрудников.ВыделенныеСтроки.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	МассивСотрудников = Новый Массив;
	
	Для Каждого ВыделеннаяСтрока Из Элементы.ОткреплениеСотрудников.ВыделенныеСтроки Цикл
		СтрокаДанных = Объект.ОткреплениеСотрудников.НайтиПоИдентификатору(ВыделеннаяСтрока);
		Если СтрокаДанных <> Неопределено Тогда
			СтрокаДанных.ОткреплениеОтПрограммыСтрахованияОтложено = Ложь;
			СтрокаДанных.Комментарий = "";
			МассивСотрудников.Добавить(СтрокаДанных.Сотрудник);
		КонецЕсли;
	КонецЦикла;
	
	ВозобновитьСотрудников(МассивСотрудников);
	УстановитьТекстЗаголовкаОткрепление();
	
КонецПроцедуры

&НаКлиенте
Процедура ОписаниеИзменений(Команда)
	
	ОткрытьФормуИзмененияОтправленныхДанных();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ОтложитьСотрудника(Сотрудник)
	
	НаборЗаписей = РегистрыСведений.ОтложенноеМедицинскоеСтрахованиеСотрудников.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
	НаборЗаписей.Отбор.Сотрудник.Установить(Сотрудник);
	
	НоваяЗапись = НаборЗаписей.Добавить();
	НоваяЗапись.ДоговорСтрахования = Объект.ДоговорСтрахования;
	НоваяЗапись.Сотрудник = Сотрудник;
	НоваяЗапись.ФизическоеЛицо = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Сотрудник, "ФизическоеЛицо");
	НоваяЗапись.Организация = Объект.Организация;
	
	НаборЗаписей.Записать();
	
	УстановитьТекстЗаголовкаПрикрепление();
	
КонецПроцедуры

&НаСервере
Процедура ВозобновитьСотрудника(Сотрудник)
	
	НаборЗаписей = РегистрыСведений.ОтложенноеМедицинскоеСтрахованиеСотрудников.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
	НаборЗаписей.Отбор.Сотрудник.Установить(Сотрудник);
	
	НаборЗаписей.Записать();
	
	УстановитьТекстЗаголовкаПрикрепление();
	
КонецПроцедуры

&НаСервере
Процедура ПриИзмененииОсновныхПолейНаСервере(ИзменятьПериодСтрахования = Ложь)
	
	МедицинскоеСтрахованиеФормы.ПрикреплениеПрочитатьНастройки(ЭтотОбъект, ИзменятьПериодСтрахования);
	
	ПрикреплениеКПрограммамСтрахованияЗаполнить();
	
	ПоказыватьОтложенныхСтрахователей = Элементы.ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Пометка;
	ИсключитьПрикрепленныхСотрудников = Элементы.ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников.Пометка;
	УстановитьОтборСтрокПрикреплениеСотрудников(Элементы, "ПрикреплениеСотрудников", ИсключитьПрикрепленныхСотрудников, ПоказыватьОтложенныхСтрахователей);
	
	ОткреплениеОтПрограммСтрахованияЗаполнить();
	УстановитьОтборСтрок(Элементы, "ОткреплениеСотрудников",
		"ОткреплениеОтПрограммыСтрахованияОтложено", ПоказыватьОтложенныхСтрахователей);
	
	ИзмененияПерсональныхДанныхЗаполнить();
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
		МедицинскоеСтрахование, "Организация", Объект.Организация,,, ЗначениеЗаполнено(Объект.Организация));
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
		МедицинскоеСтрахование, "Подразделение", Объект.Подразделение,,, ЗначениеЗаполнено(Объект.Подразделение));
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
		МедицинскоеСтрахование, "СтраховаяКомпания", Объект.СтраховаяКомпания,,, ЗначениеЗаполнено(Объект.СтраховаяКомпания));
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
		МедицинскоеСтрахование, "ДоговорСтрахования", Объект.ДоговорСтрахования,,, ЗначениеЗаполнено(Объект.ДоговорСтрахования));
	
	ПроверитьДоступностьДоговора();
	
КонецПроцедуры

&НаСервере
Процедура ПроверитьДоступностьДоговора()
	
	ДоговорЗакрыт = Ложь;
	ДоговорДоступен = Ложь;
	
	Если ЗначениеЗаполнено(Объект.ДоговорСтрахования) Тогда
		ДанныеДоговора	= Справочники.ДоговорыСтрахования.ДанныеДоговора(Объект.ДоговорСтрахования);
		ДоговорЗаполнен	= ДанныеДоговора <> Неопределено;
		ДоговорЗакрыт	= ДоговорЗаполнен И (ДанныеДоговора.ДоговорЗакрыт Или ДанныеДоговора.ПометкаУдаления);
		ДоговорДоступен	= ДоговорЗаполнен И Не ДоговорЗакрыт;
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, "ДоговорСтрахованияЗакрытГруппа", "Видимость", ДоговорЗакрыт);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, "ПрикреплениеСотрудниковСформировать", "Доступность", ДоговорДоступен);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, "ПрикреплениеСотрудниковСформировать", "Доступность", ДоговорДоступен);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, "СотрудникиРедактироватьСтрокуПрикрепления", "Доступность", ДоговорДоступен);
	
КонецПроцедуры

&НаСервере
Процедура ПрикреплениеКПрограммамСтрахованияЗаполнить(Сотрудники = Неопределено)
	
	СтруктураИменТаблиц = МедицинскоеСтрахованиеФормы.СтруктураИменТаблиц();
	СтруктураИменТаблиц.ИмяТаблицыСотрудники = "ПрикреплениеСотрудников";
	
	МедицинскоеСтрахованиеФормы.ПрикреплениеКПрограммамСтрахованияЗаполнить(ЭтотОбъект, СтруктураИменТаблиц, Ложь, Ложь, Ложь, Сотрудники);
	
	УстановитьТекстЗаголовкаПрикрепление();
	УстановитьДоступностьКомандыОтложенныеСотрудникиПрикрепления();
	УстановитьДоступностьКомандыИсключитьПрикрепленныхСотрудников();
	
КонецПроцедуры

&НаСервере
Процедура ОткреплениеОтПрограммСтрахованияЗаполнить()
	
	Объект.ОткреплениеСотрудников.Очистить();
	
	// Получим сведения по сотрудникам, не прикрепленных к программам страхования
	
	Если Не ЗначениеЗаполнено(Объект.Организация)
	 Или Не ЗначениеЗаполнено(Объект.СтраховаяКомпания)
	 Или Не ЗначениеЗаполнено(Объект.ДоговорСтрахования) Тогда
		// Показываем только сводные сведения
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	Запрос.УстановитьПараметр("Организация", Объект.Организация);
	Запрос.УстановитьПараметр("СтраховаяКомпания", Объект.СтраховаяКомпания);
	Запрос.УстановитьПараметр("ДоговорСтрахования", Объект.ДоговорСтрахования);
	Запрос.УстановитьПараметр("ДатаОкончанияСтрахования", Объект.ДатаОкончанияСтрахования);
	Запрос.УстановитьПараметр("ГруппаСотрудников", ГруппаСотрудников);
	Запрос.УстановитьПараметр("ОтборПоГруппеСотрудников", ЗначениеЗаполнено(ГруппаСотрудников));
	
	ПараметрыПолученияСотрудников = КадровыйУчет.ПараметрыПолученияСотрудниковОрганизацийПоСпискуФизическихЛиц();
	ПараметрыПолученияСотрудников.Организация = Объект.Организация;
	Если ЗначениеЗаполнено(Объект.Подразделение) Тогда
		ПараметрыПолученияСотрудников.Подразделение = Объект.Подразделение;
	КонецЕсли;
	ПараметрыПолученияСотрудников.НачалоПериода 	= Объект.ДатаНачалаСтрахования;
	ПараметрыПолученияСотрудников.ОкончаниеПериода	= Объект.ДатаОкончанияСтрахования;
	ПараметрыПолученияСотрудников.КадровыеДанные	= "Организация, ДатаУвольнения";
	
	КадровыйУчет.СоздатьВТСотрудникиОрганизации(Запрос.МенеджерВременныхТаблиц, Истина, ПараметрыПолученияСотрудников);
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СотрудникиОрганизации.Сотрудник КАК Сотрудник
	|ИЗ
	|	ВТСотрудникиОрганизации КАК СотрудникиОрганизации";
	
	МассивСотрудников = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Сотрудник");
	
	Если ОткреплятьСотрудниковВОтпускеПоУходуЗаРебенком Тогда
		
		Состояния = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Перечисления.СостоянияСотрудника.ОтпускПоУходуЗаРебенком);
		СостоянияСотрудников.СоздатьВТСостоянияСотрудников(
			Запрос.МенеджерВременныхТаблиц, МассивСотрудников, Состояния, Объект.ДатаНачалаСтрахования, Объект.ДатаОкончанияСтрахования);
	Иначе
		
		Запрос.Текст =
		"ВЫБРАТЬ ПЕРВЫЕ 0
		|	ЗНАЧЕНИЕ(Справочник.Сотрудники.ПустаяСсылка) КАК Сотрудник,
		|	NULL КАК Начало
		|ПОМЕСТИТЬ ВТСостоянияСотрудников";
		Запрос.Выполнить();
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ГруппаСотрудников) Тогда
		ГруппыСотрудников.СоздатьВТГруппыСотрудников(Запрос.МенеджерВременныхТаблиц, МассивСотрудников);
	Иначе
		
		Запрос.Текст =
		"ВЫБРАТЬ
		|	NULL КАК Сотрудник,
		|	NULL КАК ФизическоеЛицо,
		|	NULL КАК Группа
		|ПОМЕСТИТЬ ВТГруппыСотрудников";
		
		Запрос.Выполнить();
		
	КонецЕсли;
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	Сотрудники.Сотрудник КАК Сотрудник,
	|	Сотрудники.ФизическоеЛицо КАК ФизическоеЛицо,
	|	Сотрудники.ДатаОткрепления КАК ДатаОткрепления,
	|	Сотрудники.ЕстьРешениеОбУвольнении КАК ЕстьРешениеОбУвольнении,
	|	Сотрудники.ДатаУвольнения КАК ДатаУвольнения
	|ПОМЕСТИТЬ ВТОткрепляемыеСотрудникиПредварительно
	|ИЗ
	|	(ВЫБРАТЬ
	|		СотрудникиОрганизации.Сотрудник КАК Сотрудник,
	|		СотрудникиОрганизации.ФизическоеЛицо КАК ФизическоеЛицо,
	|		СотрудникиОрганизации.ДатаУвольнения КАК ДатаОткрепления,
	|		ЛОЖЬ КАК ЕстьРешениеОбУвольнении,
	|		СотрудникиОрганизации.ДатаУвольнения КАК ДатаУвольнения
	|	ИЗ
	|		ВТСотрудникиОрганизации КАК СотрудникиОрганизации
	|			ЛЕВОЕ СОЕДИНЕНИЕ ВТГруппыСотрудников КАК ГруппыСотрудников
	|			ПО СотрудникиОрганизации.Сотрудник = ГруппыСотрудников.Сотрудник
	|	ГДЕ
	|		СотрудникиОрганизации.ДатаУвольнения <> ДАТАВРЕМЯ(1, 1, 1)
	|		И (НЕ &ОтборПоГруппеСотрудников
	|				ИЛИ ГруппыСотрудников.Группа = &ГруппаСотрудников)
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		СостоянияСотрудников.Сотрудник,
	|		СостоянияСотрудников.Сотрудник.ФизическоеЛицо,
	|		СостоянияСотрудников.Начало,
	|		ЛОЖЬ,
	|		NULL
	|	ИЗ
	|		ВТСостоянияСотрудников КАК СостоянияСотрудников
	|			ЛЕВОЕ СОЕДИНЕНИЕ ВТГруппыСотрудников КАК ГруппыСотрудников
	|			ПО СостоянияСотрудников.Сотрудник = ГруппыСотрудников.Сотрудник
	|	ГДЕ
	|		(НЕ &ОтборПоГруппеСотрудников
	|				ИЛИ ГруппыСотрудников.Группа = &ГруппаСотрудников)
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ВсеСотрудники.Сотрудник,
	|		ВсеСотрудники.ФизическоеЛицо,
	|		РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.ДатаОткрепления,
	|		ИСТИНА,
	|		РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.Регистратор.ДатаУвольнения
	|	ИЗ
	|		(ВЫБРАТЬ
	|			СотрудникиОрганизации.Сотрудник КАК Сотрудник,
	|			СотрудникиОрганизации.ФизическоеЛицо КАК ФизическоеЛицо
	|		ИЗ
	|			ВТСотрудникиОрганизации КАК СотрудникиОрганизации
	|				ЛЕВОЕ СОЕДИНЕНИЕ ВТГруппыСотрудников КАК ГруппыСотрудников
	|				ПО СотрудникиОрганизации.Сотрудник = ГруппыСотрудников.Сотрудник
	|		ГДЕ
	|			(НЕ &ОтборПоГруппеСотрудников
	|					ИЛИ ГруппыСотрудников.Группа = &ГруппаСотрудников)) КАК ВсеСотрудники
	|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования КАК РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования
	|			ПО ВсеСотрудники.Сотрудник = РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.Сотрудник
	|				И (РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.Организация = &Организация)
	|				И (РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.ДоговорСтрахования = &ДоговорСтрахования)
	|	ГДЕ
	|		РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.ФизическоеЛицо ЕСТЬ НЕ NULL 
	|		И РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.ДатаОткрепления <= &ДатаОкончанияСтрахования
	|		И РешенияОбОткрепленииСотрудниковМедицинскогоСтрахования.Откреплять = ИСТИНА) КАК Сотрудники
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Сотрудник,
	|	ФизическоеЛицо,
	|	ЕстьРешениеОбУвольнении
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Сотрудники.Сотрудник КАК Сотрудник,
	|	Сотрудники.ФизическоеЛицо КАК ФизическоеЛицо,
	|	МИНИМУМ(Сотрудники.ДатаОткрепления) КАК ДатаОткрепления,
	|	Сотрудники.ЕстьРешениеОбУвольнении КАК ЕстьРешениеОбУвольнении,
	|	Сотрудники.ДатаУвольнения КАК ДатаУвольнения
	|ПОМЕСТИТЬ ВТОткрепляемыеСотрудники
	|ИЗ
	|	ВТОткрепляемыеСотрудникиПредварительно КАК Сотрудники
	|ГДЕ
	|	(Сотрудники.Сотрудник, Сотрудники.ФизическоеЛицо, Сотрудники.ЕстьРешениеОбУвольнении) В
	|			(ВЫБРАТЬ
	|				Сотрудники.Сотрудник КАК Сотрудник,
	|				Сотрудники.ФизическоеЛицо КАК ФизическоеЛицо,
	|				МАКСИМУМ(Сотрудники.ЕстьРешениеОбУвольнении) КАК ЕстьРешениеОбУвольнении
	|			ИЗ
	|				ВТОткрепляемыеСотрудникиПредварительно КАК Сотрудники
	|			СГРУППИРОВАТЬ ПО
	|				Сотрудники.Сотрудник,
	|				Сотрудники.ФизическоеЛицо)
	|
	|СГРУППИРОВАТЬ ПО
	|	Сотрудники.Сотрудник,
	|	Сотрудники.ФизическоеЛицо,
	|	Сотрудники.ЕстьРешениеОбУвольнении,
	|	Сотрудники.ДатаУвольнения
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СотрудникиНеПрикрепленные.Сотрудник КАК Сотрудник,
	|	ОтложенноеМедицинскоеСтрахованиеСотрудников.Комментарий КАК Комментарий
	|ПОМЕСТИТЬ ВТОтложенныеСотрудники
	|ИЗ
	|	ВТОткрепляемыеСотрудники КАК СотрудникиНеПрикрепленные
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ОтложенноеМедицинскоеСтрахованиеСотрудников КАК ОтложенноеМедицинскоеСтрахованиеСотрудников
	|		ПО СотрудникиНеПрикрепленные.Сотрудник = ОтложенноеМедицинскоеСтрахованиеСотрудников.Сотрудник
	|			И (ОтложенноеМедицинскоеСтрахованиеСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ПрограммыМедицинскогоСтрахованияСотрудников.Организация КАК Организация,
	|	ПрограммыМедицинскогоСтрахованияСотрудников.ДоговорСтрахования КАК ДоговорСтрахования,
	|	СотрудникиОрганизации.Сотрудник КАК Сотрудник,
	|	ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо КАК ФизическоеЛицо,
	|	ПрограммыМедицинскогоСтрахованияСотрудников.Родственник КАК Родственник,
	|	МАКСИМУМ(ПрограммыМедицинскогоСтрахованияСотрудников.Период) КАК Период
	|ПОМЕСТИТЬ ВТМаксимальныеПериодыПрограммСтрахования
	|ИЗ
	|	ВТОткрепляемыеСотрудники КАК СотрудникиОрганизации
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ПрограммыМедицинскогоСтрахованияСотрудников КАК ПрограммыМедицинскогоСтрахованияСотрудников
	|		ПО (ПрограммыМедицинскогоСтрахованияСотрудников.Организация = &Организация)
	|			И (ПрограммыМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|			И СотрудникиОрганизации.ФизическоеЛицо = ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо
	|ГДЕ
	|	ПрограммыМедицинскогоСтрахованияСотрудников.Период <= &ДатаОкончанияСтрахования
	|
	|СГРУППИРОВАТЬ ПО
	|	ПрограммыМедицинскогоСтрахованияСотрудников.Организация,
	|	ПрограммыМедицинскогоСтрахованияСотрудников.ДоговорСтрахования,
	|	СотрудникиОрганизации.Сотрудник,
	|	ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо,
	|	ПрограммыМедицинскогоСтрахованияСотрудников.Родственник
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.Организация КАК Организация,
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.ДоговорСтрахования КАК ДоговорСтрахования,
	|	СотрудникиОрганизации.Сотрудник КАК Сотрудник,
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо КАК ФизическоеЛицо,
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.Родственник КАК Родственник,
	|	МАКСИМУМ(РасширенияПрограммМедицинскогоСтрахованияСотрудников.Период) КАК Период
	|ПОМЕСТИТЬ ВТМаксимальныеПериодыРасширенийСтрахования
	|ИЗ
	|	ВТОткрепляемыеСотрудники КАК СотрудникиОрганизации
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.РасширенияПрограммМедицинскогоСтрахованияСотрудников КАК РасширенияПрограммМедицинскогоСтрахованияСотрудников
	|		ПО (РасширенияПрограммМедицинскогоСтрахованияСотрудников.Организация = &Организация)
	|			И (РасширенияПрограммМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|			И СотрудникиОрганизации.ФизическоеЛицо = РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо
	|ГДЕ
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.Период <= &ДатаОкончанияСтрахования
	|
	|СГРУППИРОВАТЬ ПО
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.Организация,
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.ДоговорСтрахования,
	|	СотрудникиОрганизации.Сотрудник,
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо,
	|	РасширенияПрограммМедицинскогоСтрахованияСотрудников.Родственник
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СотрудникиОрганизации.Сотрудник КАК Сотрудник,
	|	ВЫБОР
	|		КОГДА СотрудникиОрганизации.ДатаОткрепления < ПрограммыМедицинскогоСтрахованияСотрудников.Период
	|			ТОГДА ДОБАВИТЬКДАТЕ(ПрограммыМедицинскогоСтрахованияСотрудников.Период, ДЕНЬ, 1)
	|		КОГДА СотрудникиОрганизации.ДатаОткрепления < РасширенияПрограммМедицинскогоСтрахованияСотрудников.Период
	|			ТОГДА ДОБАВИТЬКДАТЕ(РасширенияПрограммМедицинскогоСтрахованияСотрудников.Период, ДЕНЬ, 1)
	|		ИНАЧЕ СотрудникиОрганизации.ДатаОткрепления
	|	КОНЕЦ КАК ДатаОткрепления,
	|	СотрудникиОрганизации.ФизическоеЛицо КАК ФизическоеЛицо,
	|	ВЫБОР
	|		КОГДА ОтложенныеСотрудники.Сотрудник ЕСТЬ НЕ NULL 
	|			ТОГДА ИСТИНА
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ КАК ОтложенныйСотрудник,
	|	СотрудникиОрганизации.ЕстьРешениеОбУвольнении КАК ЕстьРешениеОбУвольнении,
	|	СотрудникиОрганизации.ДатаУвольнения КАК ДатаУвольнения,
	|	ОтложенныеСотрудники.Комментарий КАК Комментарий
	|ИЗ
	|	ВТОткрепляемыеСотрудники КАК СотрудникиОрганизации
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПрограммыМедицинскогоСтрахованияСотрудников КАК ПрограммыМедицинскогоСтрахованияСотрудников
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТМаксимальныеПериодыПрограммСтрахования КАК МаксимальныеПериодыПрограммСтрахования
	|			ПО ПрограммыМедицинскогоСтрахованияСотрудников.Период = МаксимальныеПериодыПрограммСтрахования.Период
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо = МаксимальныеПериодыПрограммСтрахования.ФизическоеЛицо
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.Родственник = МаксимальныеПериодыПрограммСтрахования.Родственник
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.Организация = МаксимальныеПериодыПрограммСтрахования.Организация
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = МаксимальныеПериодыПрограммСтрахования.ДоговорСтрахования
	|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПрограммыМедицинскогоСтрахованияСотрудников КАК ПрограммыМедицинскогоСтрахованияСотрудниковУвольнение
	|			ПО (ПрограммыМедицинскогоСтрахованияСотрудников.Организация = &Организация)
	|				И (ПрограммыМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо = ПрограммыМедицинскогоСтрахованияСотрудниковУвольнение.ФизическоеЛицо
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.Родственник = ПрограммыМедицинскогоСтрахованияСотрудниковУвольнение.Родственник
	|				И ПрограммыМедицинскогоСтрахованияСотрудников.Период = ПрограммыМедицинскогоСтрахованияСотрудниковУвольнение.Период
	|				И (ПрограммыМедицинскогоСтрахованияСотрудниковУвольнение.Регистратор ССЫЛКА Документ.ОткреплениеОтПрограммМедицинскогоСтрахования)
	|		ПО (ПрограммыМедицинскогоСтрахованияСотрудников.Организация = &Организация)
	|			И (ПрограммыМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|			И СотрудникиОрганизации.ФизическоеЛицо = ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.РасширенияПрограммМедицинскогоСтрахованияСотрудников КАК РасширенияПрограммМедицинскогоСтрахованияСотрудников
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТМаксимальныеПериодыРасширенийСтрахования КАК МаксимальныеПериодыРасширенийСтрахования
	|			ПО РасширенияПрограммМедицинскогоСтрахованияСотрудников.Период = МаксимальныеПериодыРасширенийСтрахования.Период
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо = МаксимальныеПериодыРасширенийСтрахования.ФизическоеЛицо
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.Родственник = МаксимальныеПериодыРасширенийСтрахования.Родственник
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.Организация = МаксимальныеПериодыРасширенийСтрахования.Организация
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = МаксимальныеПериодыРасширенийСтрахования.ДоговорСтрахования
	|			ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.РасширенияПрограммМедицинскогоСтрахованияСотрудников КАК РасширенияПрограммМедицинскогоСтрахованияСотрудниковУвольнение
	|			ПО (РасширенияПрограммМедицинскогоСтрахованияСотрудников.Организация = &Организация)
	|				И (РасширенияПрограммМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо = РасширенияПрограммМедицинскогоСтрахованияСотрудниковУвольнение.ФизическоеЛицо
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.Родственник = МаксимальныеПериодыРасширенийСтрахования.Родственник
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудников.Период = РасширенияПрограммМедицинскогоСтрахованияСотрудниковУвольнение.Период
	|				И (РасширенияПрограммМедицинскогоСтрахованияСотрудниковУвольнение.Регистратор ССЫЛКА Документ.ОткреплениеОтПрограммМедицинскогоСтрахования)
	|		ПО (РасширенияПрограммМедицинскогоСтрахованияСотрудников.Организация = &Организация)
	|			И (РасширенияПрограммМедицинскогоСтрахованияСотрудников.ДоговорСтрахования = &ДоговорСтрахования)
	|			И СотрудникиОрганизации.ФизическоеЛицо = РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТОтложенныеСотрудники КАК ОтложенныеСотрудники
	|		ПО СотрудникиОрганизации.Сотрудник = ОтложенныеСотрудники.Сотрудник
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ОткреплениеОтПрограммМедицинскогоСтрахования.Сотрудники КАК СотрудникиОткрепленийОтПрограммМедицинскогоСтрахования
	|		ПО СотрудникиОрганизации.Сотрудник = СотрудникиОткрепленийОтПрограммМедицинскогоСтрахования.Сотрудник
	|		И СотрудникиОткрепленийОтПрограммМедицинскогоСтрахования.Ссылка.Проведен 
	|		
	|ГДЕ
	|	(ПрограммыМедицинскогоСтрахованияСотрудников.ФизическоеЛицо ЕСТЬ НЕ NULL 
	|				И ПрограммыМедицинскогоСтрахованияСотрудниковУвольнение.ФизическоеЛицо ЕСТЬ NULL
	|			ИЛИ РасширенияПрограммМедицинскогоСтрахованияСотрудников.ФизическоеЛицо ЕСТЬ НЕ NULL 
	|				И РасширенияПрограммМедицинскогоСтрахованияСотрудниковУвольнение.ФизическоеЛицо ЕСТЬ NULL)
	|	И СотрудникиОткрепленийОтПрограммМедицинскогоСтрахования.Сотрудник ЕСТЬ NULL
	|
	|УПОРЯДОЧИТЬ ПО
	|	СотрудникиОрганизации.Сотрудник";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.СледующийПоЗначениюПоля("Сотрудник") Цикл
		НоваяСтрока = Объект.ОткреплениеСотрудников.Добавить();
		НоваяСтрока.Сотрудник = Выборка.Сотрудник;
		НоваяСтрока.ДатаОткрепления = Выборка.ДатаОткрепления;
		НоваяСтрока.Комментарий = Выборка.Комментарий;
		НоваяСтрока.ОткреплениеОтПрограммыСтрахованияОтложено = Выборка.ОтложенныйСотрудник;
		НоваяСтрока.Команда = ?(Выборка.ОтложенныйСотрудник, НСтр("ru = 'Возобновить'"), НСтр("ru = 'Отложить'"));
		Если Выборка.ЕстьРешениеОбУвольнении Тогда
			НоваяСтрока.Основание = СтрШаблон(НСтр("ru = 'Решение об увольнении %1, открепление %2'"),
				Формат(Выборка.ДатаУвольнения, "ДЛФ=Д"),
				Формат(Выборка.ДатаОткрепления, "ДЛФ=Д"));
		ИначеЕсли ЗначениеЗаполнено(Выборка.ДатаУвольнения) И Выборка.ДатаУвольнения < Выборка.ДатаОткрепления Тогда
			НоваяСтрока.Основание = СтрШаблон(НСтр("ru = 'Увольнение %1, открепление %2'"),
				Формат(Выборка.ДатаУвольнения, "ДЛФ=Д"),
				Формат(Выборка.ДатаОткрепления, "ДЛФ=Д"));
		Иначе
			НоваяСтрока.Основание = СтрШаблон(НСтр("ru = 'Увольнение %1'"),
				Формат(Выборка.ДатаОткрепления, "ДЛФ=Д"));
		КонецЕсли;
	КонецЦикла;
	
	Объект.ОткреплениеСотрудников.Сортировать("Сотрудник");
	
	УстановитьТекстЗаголовкаОткрепление();
	
КонецПроцедуры

&НаКлиенте
Функция ПроверитьНаКлиенте()
	
	Отказ = Ложь;
	Если Не ЗначениеЗаполнено(Объект.ДатаНачалаСтрахования) Или Не ЗначениеЗаполнено(Объект.ДатаОкончанияСтрахования) Тогда
		ТекстОшибки = НСтр("ru = 'Не указан период страхования.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстОшибки, , , , Отказ);
	КонецЕсли;
	
	Возврат Отказ;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Процедура УстановитьОтборСтрок(Элементы, ИмяТаблицы, ИмяСвойства, ПоказыватьОтложенныхСтрахователей)
	
	СтруктураОтбора = ?(ПоказыватьОтложенныхСтрахователей, Неопределено, Новый ФиксированнаяСтруктура(ИмяСвойства, Ложь));
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, ИмяТаблицы, "ОтборСтрок", СтруктураОтбора);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УстановитьОтборСтрокПрикреплениеСотрудников(Элементы, ИмяТаблицы, ИсключитьПрикрепленныхСотрудников, ПоказыватьОтложенныхСтрахователей)
	
	ПредварительнаяСтруктураОтбора = Новый Структура;
	Если ПоказыватьОтложенныхСтрахователей = Ложь Тогда
		ПредварительнаяСтруктураОтбора.Вставить("ПрикреплениеКПрограммеСтрахованияОтложено", Ложь);
	КонецЕсли;
	Если ИсключитьПрикрепленныхСотрудников Тогда
		ПредварительнаяСтруктураОтбора.Вставить("СотрудникПрикреплен", Ложь);
	КонецЕсли;
	
	СтруктураОтбора = Новый ФиксированнаяСтруктура(ПредварительнаяСтруктураОтбора);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, ИмяТаблицы, "ОтборСтрок", СтруктураОтбора);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьТекстЗаголовкаПрикрепление()
	
	ТекстЗаголовка = НСтр("ru = 'Прикрепление к программам'");
	СтруктураОтбора = Новый Структура("ПрикреплениеКПрограммеСтрахованияОтложено", Ложь);
	НайденныеСтроки = Объект.ПрикреплениеСотрудников.НайтиСтроки(СтруктураОтбора);
	КоличествоСотрудников = НайденныеСтроки.Количество();
	Если КоличествоСотрудников > 0 Тогда
		ТекстЗаголовка = ТекстЗаголовка + " (" + КоличествоСотрудников + ")";
	КонецЕсли;
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ГруппаПрикреплениеКПрограммамСтрахования", "Заголовок", ТекстЗаголовка);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьДоступностьКомандыОтложенныеСотрудникиПрикрепления()
	
	СтруктураОтбора = Новый Структура("ПрикреплениеКПрограммеСтрахованияОтложено", Истина);
	НайденныеСтроки = Объект.ПрикреплениеСотрудников.НайтиСтроки(СтруктураОтбора);
	ЕстьОтложенныеСотрудникиВПрикреплении = ?(НайденныеСтроки.Количество() > 0, Истина, Ложь);
	Элементы.ПрикреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Видимость = ЕстьОтложенныеСотрудникиВПрикреплении;
	
КонецПроцедуры

&НаСервере
Процедура УстановитьДоступностьКомандыИсключитьПрикрепленныхСотрудников()
	
	СтруктураОтбора = Новый Структура("СотрудникПрикреплен", Истина);
	НайденныеСтроки = Объект.ПрикреплениеСотрудников.НайтиСтроки(СтруктураОтбора);
	ЕстьПрикрепленныеСотрудникиВПрикреплении = ?(НайденныеСтроки.Количество() > 0, Истина, Ложь);
	Элементы.ПрикреплениеСотрудниковИсключитьПрикрепленныхСотрудников.Видимость = ЕстьПрикрепленныеСотрудникиВПрикреплении;
	
КонецПроцедуры	

&НаСервере
Процедура УстановитьДоступностьКомандыОтложенныеСотрудникиОткрепления()
	
	СтруктураОтбора = Новый Структура("ОткреплениеОтПрограммыСтрахованияОтложено", Истина);
	НайденныеСтроки = Объект.ОткреплениеСотрудников.НайтиСтроки(СтруктураОтбора);
	ЕстьОтложенныеСотрудникиВОткреплении = ?(НайденныеСтроки.Количество() > 0, Истина, Ложь);
	Элементы.ОткреплениеСотрудниковПоказыватьОтложенныхСтрахователей.Видимость = ЕстьОтложенныеСотрудникиВОткреплении;
	
КонецПроцедуры

&НаСервере
Процедура УстановитьТекстЗаголовкаОткрепление()
	
	ТекстЗаголовка = НСтр("ru = 'Открепление от программ'");
	СтруктураОтбора = Новый Структура("ОткреплениеОтПрограммыСтрахованияОтложено", Ложь);
	НайденныеСтроки = Объект.ОткреплениеСотрудников.НайтиСтроки(СтруктураОтбора);
	КоличествоСотрудников = НайденныеСтроки.Количество();
	Если КоличествоСотрудников > 0 Тогда
		ТекстЗаголовка = ТекстЗаголовка + " (" + КоличествоСотрудников + ")";
	КонецЕсли;
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ГруппаОткрепление", "Заголовок", ТекстЗаголовка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьПрикреплениеСотрудников(ДанныеСтроки = Неопределено, Сотрудники = Неопределено)
	
	ПараметрыОткрытия = Новый Структура;
	ЗначенияЗаполнения = Новый Структура;
	ЗначенияЗаполнения.Вставить("Организация", Объект.Организация);
	ЗначенияЗаполнения.Вставить("СтраховаяКомпания", Объект.СтраховаяКомпания);
	ЗначенияЗаполнения.Вставить("ДоговорСтрахования", Объект.ДоговорСтрахования);
	ЗначенияЗаполнения.Вставить("Подразделение", Объект.Подразделение);
	Если Сотрудники = Неопределено Тогда
		Если ДанныеСтроки <> Неопределено Тогда
			ЗначенияЗаполнения.Вставить("Сотрудники", ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДанныеСтроки.Сотрудник));
		Иначе
			ЗначенияЗаполнения.Вставить("Сотрудники", Новый Массив);
		КонецЕсли;
	Иначе
		ЗначенияЗаполнения.Вставить("Сотрудники", Сотрудники);
	КонецЕсли;
	ПараметрыОткрытия.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
	
	ОткрытьФорму("Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ФормаОбъекта", ПараметрыОткрытия, ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьОткреплениеСотрудников(ДанныеСтроки = Неопределено, Сотрудники = Неопределено, ДатыОткрепления = Неопределено)
	
	ПараметрыОткрытия = Новый Структура;
	ЗначенияЗаполнения = Новый Структура;
	ЗначенияЗаполнения.Вставить("Организация", Объект.Организация);
	ЗначенияЗаполнения.Вставить("СтраховаяКомпания", Объект.СтраховаяКомпания);
	ЗначенияЗаполнения.Вставить("ДоговорСтрахования", Объект.ДоговорСтрахования);
	ЗначенияЗаполнения.Вставить("Подразделение", Объект.Подразделение);
	Если Сотрудники = Неопределено Тогда
		Если ДанныеСтроки <> Неопределено Тогда
			ЗначенияЗаполнения.Вставить("Сотрудники", ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДанныеСтроки.Сотрудник));
			ЗначенияЗаполнения.Вставить("ДатыОткрепления", ДатыОткрепления);
		Иначе
			ЗначенияЗаполнения.Вставить("Сотрудники", Новый Массив);
		КонецЕсли;
	Иначе
		ЗначенияЗаполнения.Вставить("Сотрудники", Сотрудники);
		ЗначенияЗаполнения.Вставить("ДатыОткрепления", ДатыОткрепления);
	КонецЕсли;
	ПараметрыОткрытия.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
	
	ОткрытьФорму("Документ.ОткреплениеОтПрограммМедицинскогоСтрахования.ФормаОбъекта", ПараметрыОткрытия, ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура УсловияСтрахованияСотрудниковЗакрытие(Результат, ДополнительныеПараметры) Экспорт
	
	ГруппаСотрудников = Результат;
	ПриИзмененииОсновныхПолейНаСервере();
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УстановитьДоступностьЭлементов(Форма)
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Форма.Элементы, "ГруппаСотрудников", "Доступность", Форма.УсловияСтрахованияСотрудников <> 0);
	
КонецПроцедуры

&НаСервере
Процедура ИгнорироватьИзменениеПерсональныхДанныхНаСервере(Застрахованный)
	
	НаборЗаписей = РегистрыСведений.ИзменениеОтправленныхДанныхЗастрахованных.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
	НаборЗаписей.Отбор.Застрахованный.Установить(Застрахованный);
	НаборЗаписей.Записать();
	
	НаборЗаписей = РегистрыСведений.ИзменениеОтправленнойКонтактнойИнформацииЗастрахованных.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
	НаборЗаписей.Отбор.Застрахованный.Установить(Застрахованный);
	НаборЗаписей.Записать();
	
	Если ИспользоватьДополнительныеРеквизитыИСведения Тогда
		НаборЗаписей = РегистрыСведений.ИзменениеОтправленныхДополнительныхРеквизитовИСведенийЗастрахованных.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
		НаборЗаписей.Отбор.Застрахованный.Установить(Застрахованный);
		НаборЗаписей.Записать();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ИзмененияПерсональныхДанныхЗаполнить()
	
	Объект.ИзменениеПерсональныхДанныхСотрудников.Очистить();
	
	Если Не ЗначениеЗаполнено(Объект.ДоговорСтрахования) Тогда
		КоличествоЗастрахованныхСИзменениями = "";
		Возврат;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ИзменениеОтправленныхДанныхЗастрахованных.Застрахованный КАК Застрахованный,
		|	ИСТИНА КАК ИзменилисьДанные,
		|	ЛОЖЬ КАК ИзменилисьКонтакты,
		|	ЛОЖЬ КАК ИзменилисьДопРеквизиты
		|ПОМЕСТИТЬ ВТИзменения
		|ИЗ
		|	РегистрСведений.ИзменениеОтправленныхДанныхЗастрахованных КАК ИзменениеОтправленныхДанныхЗастрахованных
		|ГДЕ
		|	ИзменениеОтправленныхДанныхЗастрахованных.ДоговорСтрахования = &ДоговорСтрахования
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ИзменениеОтправленнойКонтактнойИнформацииЗастрахованных.Застрахованный,
		|	ЛОЖЬ,
		|	ИСТИНА,
		|	ЛОЖЬ
		|ИЗ
		|	РегистрСведений.ИзменениеОтправленнойКонтактнойИнформацииЗастрахованных КАК ИзменениеОтправленнойКонтактнойИнформацииЗастрахованных
		|ГДЕ
		|	ИзменениеОтправленнойКонтактнойИнформацииЗастрахованных.ДоговорСтрахования = &ДоговорСтрахования
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ИзменениеОтправленныхДополнительныхРеквизитовИСведенийЗастрахованных.Застрахованный,
		|	ЛОЖЬ,
		|	ЛОЖЬ,
		|	ИСТИНА
		|ИЗ
		|	РегистрСведений.ИзменениеОтправленныхДополнительныхРеквизитовИСведенийЗастрахованных КАК ИзменениеОтправленныхДополнительныхРеквизитовИСведенийЗастрахованных
		|ГДЕ
		|	ИзменениеОтправленныхДополнительныхРеквизитовИСведенийЗастрахованных.ДоговорСтрахования = &ДоговорСтрахования
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТИзменения.Застрахованный КАК Застрахованный,
		|	МАКСИМУМ(ВТИзменения.ИзменилисьДанные) КАК ИзменилисьДанные,
		|	МАКСИМУМ(ВТИзменения.ИзменилисьКонтакты) КАК ИзменилисьКонтакты,
		|	МАКСИМУМ(ВТИзменения.ИзменилисьДопРеквизиты) КАК ИзменилисьДопРеквизиты
		|ИЗ
		|	ВТИзменения КАК ВТИзменения
		|
		|СГРУППИРОВАТЬ ПО
		|	ВТИзменения.Застрахованный";
	Запрос.УстановитьПараметр("ДоговорСтрахования", Объект.ДоговорСтрахования);
	ЗастрахованныеСИзменениямиДанных = Запрос.Выполнить().Выгрузить();
	КоличествоЗастрахованныхСИзменениями = ЗастрахованныеСИзменениямиДанных.Количество();
	Для Каждого СтрокаЗастрахованный Из ЗастрахованныеСИзменениямиДанных Цикл
		НоваяСтрока = Объект.ИзменениеПерсональныхДанныхСотрудников.Добавить();
		НоваяСтрока.Застрахованный = СтрокаЗастрахованный.Застрахованный;
		НоваяСтрока.Описание = ТекстОписаниеИзменений(СтрокаЗастрахованный.ИзменилисьДанные,
			СтрокаЗастрахованный.ИзменилисьКонтакты,
			СтрокаЗастрахованный.ИзменилисьДопРеквизиты);
		КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция ТекстОписаниеИзменений(ИзменилисьДанные, ИзменилисьКонтакты, ИзменилисьДопРеквизиты)
	
	ТекстИзменилисьДанные = ?(ИзменилисьДанные,
		НСтр("ru = 'кадровые данные'"),
		"");
	ТекстИзменилисьКонтакты = ?(ИзменилисьКонтакты,
		НСтр("ru = 'контактная информация'"),
		"");
	ТекстИзменилисьДопРеквизиты = ?(ИзменилисьДопРеквизиты,
		НСтр("ru = 'дополнительные реквизиты и сведения'"),
		"");
	РазделительДанныеИКонтакты = ?(ИзменилисьДанные И ИзменилисьКонтакты,
		НСтр("ru = ', '"),
		"");
	РазделительКонтактыИДопРеквизиты = ?(ИзменилисьКонтакты И ИзменилисьДопРеквизиты,
		НСтр("ru = ', '"),
		"");
		
	Описание = СтрШаблон("%1%2%3%4%5",
		ТекстИзменилисьДанные,
		РазделительДанныеИКонтакты,
		ТекстИзменилисьКонтакты,
		РазделительКонтактыИДопРеквизиты,
		ТекстИзменилисьДопРеквизиты);
	Возврат Врег(Лев(Описание,1)) + Нрег(Сред(Описание,2));
	
КонецФункции

&НаКлиенте
Процедура ОтложитьСотрудниковВПрикрепленииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	МассивСотрудников = Новый Массив;
	
	Для Каждого ВыделеннаяСтрока Из Элементы.ПрикреплениеСотрудников.ВыделенныеСтроки Цикл
		СтрокаДанных = Объект.ПрикреплениеСотрудников.НайтиПоИдентификатору(ВыделеннаяСтрока);
		Если СтрокаДанных <> Неопределено Тогда
			СтрокаДанных.ПрикреплениеКПрограммеСтрахованияОтложено = Истина;
			СтрокаДанных.Комментарий = Результат;
			МассивСотрудников.Добавить(СтрокаДанных.Сотрудник);
		КонецЕсли;
	КонецЦикла;
	
	ОтложитьСотрудников(МассивСотрудников, Результат);
	УстановитьТекстЗаголовкаПрикрепление();
	УстановитьДоступностьКомандыОтложенныеСотрудникиПрикрепления();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтложитьСотрудниковВОткрепленииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	МассивСотрудников = Новый Массив;
	
	Для Каждого ВыделеннаяСтрока Из Элементы.ОткреплениеСотрудников.ВыделенныеСтроки Цикл
		СтрокаДанных = Объект.ОткреплениеСотрудников.НайтиПоИдентификатору(ВыделеннаяСтрока);
		Если СтрокаДанных <> Неопределено Тогда
			СтрокаДанных.ОткреплениеОтПрограммыСтрахованияОтложено = Истина;
			СтрокаДанных.Комментарий = Результат;
			МассивСотрудников.Добавить(СтрокаДанных.Сотрудник);
		КонецЕсли;
	КонецЦикла;
	
	ОтложитьСотрудников(МассивСотрудников, Результат);
	УстановитьТекстЗаголовкаОткрепление();
	УстановитьДоступностьКомандыОтложенныеСотрудникиОткрепления();
	
КонецПроцедуры

&НаСервере
Процедура ОтложитьСотрудников(Сотрудники, Комментарий)
	
	Для Каждого Сотрудник Из Сотрудники Цикл
		НаборЗаписей = РегистрыСведений.ОтложенноеМедицинскоеСтрахованиеСотрудников.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
		НаборЗаписей.Отбор.Сотрудник.Установить(Сотрудник);
		
		НоваяЗапись = НаборЗаписей.Добавить();
		НоваяЗапись.ДоговорСтрахования = Объект.ДоговорСтрахования;
		НоваяЗапись.Сотрудник = Сотрудник;
		НоваяЗапись.ФизическоеЛицо = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Сотрудник, "ФизическоеЛицо");
		НоваяЗапись.Комментарий = Комментарий;
		НоваяЗапись.Организация = Объект.Организация;
		
		НаборЗаписей.Записать();
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура ВозобновитьСотрудников(Сотрудники)
	
	Для Каждого Сотрудник Из Сотрудники Цикл
		НаборЗаписей = РегистрыСведений.ОтложенноеМедицинскоеСтрахованиеСотрудников.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.ДоговорСтрахования.Установить(Объект.ДоговорСтрахования);
		НаборЗаписей.Отбор.Сотрудник.Установить(Сотрудник);
		
		НаборЗаписей.Записать();
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуИзмененияОтправленныхДанных()
	
	ТекущиеДанные = Элементы.ИзменениеПерсональныхДанных.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыИзменений = Новый Структура;
	ПараметрыИзменений.Вставить("ДоговорСтрахования", Объект.ДоговорСтрахования);
	ПараметрыИзменений.Вставить("Застрахованный", ТекущиеДанные.Застрахованный);
	
	ОткрытьФорму("Обработка.МедицинскоеСтрахование.Форма.ИзменениеОтправленныхДанных", ПараметрыИзменений);
	
КонецПроцедуры

// СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов
&НаКлиенте
Процедура Подключаемый_ОбновитьКомандыСостоянияОригинала()
	
	ОбновитьКомандыСостоянияОригинала()
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьКомандыСостоянияОригинала()
	
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект);
	
КонецПроцедуры

//Конец СтандартныеПодсистемы.УчетОригиналовПервичныхДокументов

#КонецОбласти

