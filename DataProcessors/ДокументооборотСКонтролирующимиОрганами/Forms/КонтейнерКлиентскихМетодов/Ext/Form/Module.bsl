&НаСервере
Перем ТаблицаАвтообменаПоУчетнымЗаписямДокументооборота Экспорт;

&НаСервере
Перем ПолученныеТранспортныеСообщения Экспорт;

&НаСервере
Перем ПолученныеПисьма Экспорт;

&НаСервере
Перем ЗапросыПоКоторымПолученыОтветы Экспорт;

&НаКлиенте
Перем КомпонентаЗагружена Экспорт;

&НаКлиенте
Перем ПрисутствуетЗаполненныйПараметр Экспорт;

&НаКлиенте
Перем ПараметрыКриптографии;

&НаКлиенте
Перем СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи Экспорт;

&НаКлиенте
Перем ПарольДоступаКЗакрытомуКлючу;

&НаКлиенте
Перем ПараметрыРасшифровки;

&НаКлиенте
Перем ОписаниеОповещенияПослеОтправкиЗаявления;

&НаКлиенте
Перем ПараметрыПодключаемогоОбработчикаЗаявления;

&НаКлиенте
Перем ПараметрыПодтвержденияОблачнойПодписи;

&НаКлиенте
Перем КонтекстОтложеннойОперации;

&НаКлиенте
Перем ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации_ВходящийКонтекст;

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ПоказатьКритическиеОшибкиЦиклаОбмена(ЦиклОбмена) Экспорт
	
	ТипыОшибок = Новый Массив;
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР);
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС);
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС);
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 2
						  |	ТранспортноеСообщение.Ссылка
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |	И ТранспортноеСообщение.Тип В (&Тип)");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	Запрос.УстановитьПараметр("Тип", ТипыОшибок);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Сообщение = Выборка.Ссылка;
		Если НЕ Выборка.Следующий() Тогда
			Возврат Сообщение;
		Иначе
			ТипыОшибок = Новый СписокЗначений;
			ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
			ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР);
			ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС);
			ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС);
			
			Отбор = Новый Структура();
			Отбор.Вставить("ЦиклОбмена", ЦиклОбмена);
			Отбор.Вставить("Тип", ТипыОшибок);

			Возврат Отбор;
		КонецЕсли;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, СДанными = Ложь, ФильтрПоТипу = Неопределено, ИмяФайла = Неопределено, САдресом = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, СДанными, ФильтрПоТипу, ИмяФайла, САдресом);
		
КонецФункции

&НаСервереБезКонтекста
Функция ТранспортноеСообщениеИмеетВложениеЗаданногоТипа(ТранспортноеСообщение, Тип) Экспорт
	
	ВложенияТранспортногоСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, , Тип);
	Возврат ВложенияТранспортногоСообщения.Количество() > 0;
		
КонецФункции

&НаСервереБезКонтекста
Функция ИмяВложенияТранспортногоСообщения(ТранспортноеСообщение, Тип) Экспорт
	
	ВложенияТранспортногоСообщения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, , Тип);
	
	Если ВложенияТранспортногоСообщения.Количество() > 0 Тогда
		Возврат ВложенияТранспортногоСообщения[0].ИмяФайла;
	Иначе
		Возврат "";
	КонецЕсли;
		
КонецФункции

&НаСервереБезКонтекста
Функция СведенияПоОтправляемымОбъектам(Ссылка) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СведенияПоОтправляемымОбъектам(Ссылка);
	
КонецФункции

&НаКлиенте
Процедура ПоказатьКритическиеОшибкиПоСсылке(Знач Ссылка, ДополнительныеПараметры = Неопределено) Экспорт
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("Отправка") Тогда
		ЦиклОбмена = ДополнительныеПараметры.Отправка;
	Иначе
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Ссылка);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Результат = ПоказатьКритическиеОшибкиЦиклаОбмена(ЦиклОбмена);
		
		Если ТипЗнч(Результат) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда 
			ПоказатьЗначение(, Результат);
		ИначеЕсли ТипЗнч(Результат) = Тип("Структура") Тогда
			ПараметрыФормы = Новый Структура("Отбор", Результат);
			ОткрытьФорму("Документ.ТранспортноеСообщение.ФормаСписка", ПараметрыФормы);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВопросПредупреждение(
		ОсновнойТекст,
		ТекстПереключатель1 = Неопределено,
		ТекстПереключатель2 = Неопределено,
		ВыполняемоеОповещение = Неопределено,
		КартинкаДиалога = Неопределено,
		КнопкиДиалога = Неопределено) Экспорт
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ОсновнойТекст", 		ОсновнойТекст);
	ПараметрыФормы.Вставить("ТекстПереключатель1", 	ТекстПереключатель1);
	ПараметрыФормы.Вставить("ТекстПереключатель2", 	ТекстПереключатель2);
	ПараметрыФормы.Вставить("КартинкаДиалога", 		КартинкаДиалога);
	ПараметрыФормы.Вставить("КнопкиДиалога", 		КнопкиДиалога);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВопросПредупреждениеЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ВопросПредупреждение",
		ПараметрыФормы,,,,,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ВопросПредупреждениеЗавершение(Результат, ВыполняемоеОповещение) Экспорт
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьПустуюДатуЗавершенияЦиклаОбмена() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// РАБОТА С ТРАНСПОРТНЫМИ КОНТЕЙНЕРАМИ И ИХ СОДЕРЖИМЫМ
//

&НаКлиенте
Функция НовыйКонтекстРасшифровки(Контекст, ПараметрыПоказа = Неопределено)
	
	Если ПараметрыПоказа = Неопределено Тогда
		Возврат Новый ФиксированнаяСтруктура("ПриРасшифровке", Контекст);
	Иначе
		Возврат Новый ФиксированнаяСтруктура("ПриРасшифровке, ПараметрыПоказа", Контекст, ПараметрыПоказа);
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеТранспортногоСообщения(Сообщение) Экспорт
	
	Если ЗначениеЗаполнено(Сообщение.ЦиклОбмена) Тогда
		Возврат "" + Сообщение.Тип + ", связанное с циклом обмена " + Сообщение.ЦиклОбмена;
	Иначе
		Возврат Сообщение;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция СоздатьИЗаполнитьОтветноеСообщение(Основание)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СоздатьИЗаполнитьОтветноеСообщение(Основание);
	
КонецФункции

&НаКлиенте
Процедура ВвестиПодтверждение(ОповещениеОЗавершении, ЦиклОбмена, Основание = Неопределено) Экспорт
	
	Если ТипЗнч(Основание) = Тип("ПеречислениеСсылка.ТипыТранспортныхСообщений") Тогда
		СообщениеОснование = ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, Основание);
	Иначе
		СообщениеОснование = Основание;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СообщениеОснование) Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	СоздатьПодтверждение(ОповещениеОЗавершении, СообщениеОснование);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция НужноСоздатьИЗаполнитьОтветноеСообщение(Основание) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.НужноСоздатьИЗаполнитьОтветноеСообщение(Основание);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтветноеСообщениеПоСообщениюОснованию(Основание) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтветноеСообщениеПоСообщениюОснованию(Основание);
	
КонецФункции

&НаСервере
Функция ОрганизацияПодключенаКИнспекцииПоКодуИФНС(Организация, КодИФНС, КПП = "") Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОрганизацияПодключенаКИнспекцииПоКодуИФНС(Организация, КодИФНС, КПП);
	
КонецФункции

&НаСервере
Функция КППИзpackageDescription(Опись) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.КППИзpackageDescription(Опись);
КонецФункции

&НаКлиенте
Функция НаправлениеОтправкиСообщенияПодключено(ОповещениеОЗавершении, ТранспортноеСообщение, ТихийРежим = Ложь) Экспорт

	Свойства = ЗначенияРеквизитовОбъекта(ТранспортноеСообщение, "Тип, ЦиклОбмена");
	Если Свойства.Тип = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ДокументНО") Тогда
		
		ЦиклОбмена = ЗначенияРеквизитовОбъекта(Свойства.ЦиклОбмена, "ВнешняяОрганизация.Код, Организация, Предмет");
		
		Подключена = 
			ОрганизацияПодключенаКИнспекцииПоКодуИФНС(
				ЦиклОбмена.Организация, 
				ЦиклОбмена.ВнешняяОрганизацияКод);
				
		ЕстьПраво = МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ЦиклОбмена.Предмет);
				
		Если НЕ Подключена ИЛИ НЕ ЕстьПраво Тогда
			
			Если ТихийРежим Тогда
				
				ИФНС = СведенияПоОтправляемымОбъектам(ТранспортноеСообщение).КодКонтролирующегоОргана;
				
				Результат = РезультатПодтверждения();
				Результат.ТекстОшибки = НСтр("ru = 'Не подключена ИФНС '") + Строка(ИФНС);
				
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
				Возврат Ложь;
			КонецЕсли;
			
			ЭтоОтправкаИзПанели = Ложь;
			ПроверитьЧтоЭтоОтправкаИзПанелиРекурсивно(ОповещениеОЗавершении, ЭтоОтправкаИзПанели);
			
			Если ЭтоОтправкаИзПанели Тогда
			
				ДополнительныеПараметры = Новый Структура();
				ДополнительныеПараметры.Вставить("СсылкаНаОбъект", ТранспортноеСообщение);
				
				СообщитьОНеподключенномНаправлении(ДополнительныеПараметры);//+
				Возврат Ложь;
				
			Иначе
				
				// Ничего не делаем.
				// Ошибка будет выдана методом ИФНСВходящейОписиПодключено(СообщениеОснование, Объект)
				
			КонецЕсли;
			
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Истина;
		
КонецФункции

&НаКлиенте
Процедура ПроверитьЧтоЭтоОтправкаИзПанелиРекурсивно(Значение, Результат) Экспорт
	
	Если ТипЗнч(Значение) = Тип("ОписаниеОповещения") Тогда
		
		ПроверитьЧтоЭтоОтправкаИзПанелиРекурсивно(Значение.ДополнительныеПараметры, Результат);
		Возврат;
		
	КонецЕсли;
	
	Если ТипЗнч(Значение) = Тип("Структура") Тогда
		
		Если Значение.Свойство("ЭтоОтправкаПодтвержденияИзПанелиОтправки") Тогда
			Результат = Истина;
			Возврат;
		КонецЕсли;
		
		Для каждого ЭлементСтруктуры Из Значение Цикл
			
			Если ТипЗнч(ЭлементСтруктуры.Значение) = Тип("Структура") Тогда
			
				ПроверитьЧтоЭтоОтправкаИзПанелиРекурсивно(ЭлементСтруктуры.Значение, Результат);
				Возврат;
				
			КонецЕсли;
			
			Если ТипЗнч(ЭлементСтруктуры.Значение) = Тип("ОписаниеОповещения") Тогда
			
				ПроверитьЧтоЭтоОтправкаИзПанелиРекурсивно(ЭлементСтруктуры.Значение, Результат);
				Возврат;
				
			КонецЕсли;
		
		КонецЦикла; 
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждение(ОповещениеОЗавершении, Основание) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// создаем на сервере
	НужноСоздать = НужноСоздатьИЗаполнитьОтветноеСообщение(Основание);
	
	ОтветноеСообщениеНайдено = Ложь;
	
	Если НужноСоздать Тогда
		
		Подключено = НаправлениеОтправкиСообщенияПодключено(ОповещениеОЗавершении, Основание);
		Если НЕ Подключено Тогда
			Возврат;
		КонецЕсли;
		
		ОтветноеСообщение = СоздатьИЗаполнитьОтветноеСообщение(Основание);
		Если ОтветноеСообщение = Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		ИначеЕсли ОтветноеСообщение.УжеСуществует Тогда
			ОтветноеСообщение        = ОтветноеСообщение.СуществующееСообщение;
			ОтветноеСообщениеНайдено = Истина;
		КонецЕсли;
	КонецЕсли;
		
	Если НЕ НужноСоздать ИЛИ ОтветноеСообщениеНайдено Тогда
		
		Если НЕ ОтветноеСообщениеНайдено ИЛИ ОтветноеСообщение = Неопределено Тогда
			ОтветноеСообщение = ОтветноеСообщениеПоСообщениюОснованию(Основание);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, ОтветноеСообщение);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = Новый Структура("Подтверждение", ОтветноеСообщение.Ссылка);
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("Подтверждение", ОтветноеСообщение.Ссылка);
	Оповещение = Новый ОписаниеОповещения(
		"СоздатьПодтверждениеПослеПодписанияСообщения", ЭтотОбъект, Контекст);
	
	// различные действия на сервере в зависимости от типа сообщения
	ТипОснования = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(Основание, "Тип");
	Если ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПротоколПФР Тогда
		
		ПодписатьСообщение("ПФР", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПисьмоВходящееПФР Тогда
		
		ПодписатьСообщение("ПФР", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеДатыОтправкиФСГС Тогда
		
		ПодписатьСообщение("ФСГС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", , ОтветноеСообщение.УчетнаяЗапись, Основание);
	
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПротоколВходногоКонтроляОтчетностиФСГС Тогда
		
		СформироватьИзвещениеОПолученииФСГС(ОтветноеСообщение.Ссылка);
		ПодписатьСообщение("ФСГС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПисьмоВходящееФСГС Тогда
		
		СформироватьИзвещениеОПолученииФСГС(ОтветноеСообщение.Ссылка);
		ПодписатьСообщение("ФСГС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Руководитель", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеДекларацияНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеФорма2НДФЛНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеОбращениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеПредставлениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеЗапросНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаДекларацияНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаФорма2НДФЛНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаОбращениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаПредставлениеНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаЗапросНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиДекларацияНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗапросНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПисьмоНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РассылкаНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	ИначеЕсли ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПодтверждениеЗаявлениеНО
		ИЛИ ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаЗаявлениеНО 
		
		ИЛИ ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗаявлениеНО
		
		ИЛИ ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗаявлениеРФНО
		ИЛИ ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_СообщениеОбОтзывеЗаявлениеРФНО
		ИЛИ ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗаявлениеТСНО
		ИЛИ ТипОснования = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ДокументНО Тогда
		
		Если НЕ СформироватьИзвещениеОПолученииНаСервере(ОтветноеСообщение.Ссылка) Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			Возврат;
		КонецЕсли;
		
		ПодписатьСообщение("ФНС", Оповещение, КонтекстОтправки, ОтветноеСообщение.Ссылка, ОтветноеСообщение.Тип, "Шифрование", , ОтветноеСообщение.УчетнаяЗапись, Основание);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждениеПослеПодписанияСообщения(Результат, ВходящийКонтекст) Экспорт
	
	ВыполнитьОбработкуОповещения(
		ВходящийКонтекст.ОповещениеОЗавершении, 
		?(Результат = Истина, ВходящийКонтекст.Подтверждение, Неопределено));
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьИзвещениеОПолученииНаСервере(Сообщение)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СформироватьИзвещениеОПолучении(Сообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьОтветНаЗапросИОН(Запрос, ИмяФайла, Данные, Размер = Неопределено, ТипСодержимогоФайла = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ДобавитьОтветНаЗапросИОН(Запрос, ИмяФайла, Данные, Размер, ТипСодержимогоФайла);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОтветыНаЗапросИОН(парамЗапрос, СДанными = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОтветыНаЗапросИОН(парамЗапрос, СДанными);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ЗаписатьСтатусОтправкиОбъекта(докСсылка, Статус) Экспорт

	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДО.ЗаписатьСтатусОтправкиОбъекта(докСсылка, Статус);
	
КонецПроцедуры
 
&НаСервереБезКонтекста
Функция РассчитатьСтатусЦиклаОбмена(ЦиклОбмена) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РассчитатьСтатусЦиклаОбмена(ЦиклОбмена);
	
КонецФункции

&НаСервереБезКонтекста
Функция РассчитатьСтатусОтправкиОбъекта(Объект, Знач ЦиклОбмена = Неопределено) Экспорт
	
	Если ЦиклОбмена = Неопределено Тогда
		ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Объект);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат РассчитатьСтатусЦиклаОбмена(ЦиклОбмена);
	Иначе
		Возврат Перечисления.СтатусыОтправки.ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Объект, СтрогоПоДанномуЦиклуОбмена = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ОбновитьСтатусыСвязанногоОбъектаПриНеобходимости(Объект, СтрогоПоДанномуЦиклуОбмена); 
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьЭлементДаты(МассивЭлементов, ИндексЭлемента)
	
	Если МассивЭлементов.Количество() <= ИндексЭлемента Тогда
		Возврат 0;
	Иначе
		ЭлементДаты = МассивЭлементов[ИндексЭлемента];
		Если ПустаяСтрока(ЭлементДаты) ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЭлементДаты) Тогда
			Возврат 0;
		Иначе
			Возврат Число(ЭлементДаты);
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ДатаПоСтроке(Знач Стр) ЭКспорт
	
	Попытка
		Если СтрНайти(Стр, "-") <> 0 Тогда
			Если СтрДлина(Стр) > 10 Тогда
				Возврат Дата(Число(СокрЛП(Сред(Стр, 1, 4))),
							Число(СокрЛП(Сред(Стр, 6, 2))),
							Число(СокрЛП(Сред(Стр, 9, 2))),
							Число(СокрЛП(Сред(Стр, 12, 2))),
							Число(СокрЛП(Сред(Стр, 15, 2))),
							Число(СокрЛП(Сред(Стр, 18, 2))));
			Иначе
				Возврат Дата(Число(СокрЛП(Сред(Стр, 1, 4))),
							Число(СокрЛП(Сред(Стр, 6, 2))),
							Число(СокрЛП(Сред(Стр, 9, 2))));
			КонецЕсли;
		Иначе
			Если СтрНайти(Стр, ":") <> 0 Тогда
				Стр = СтрЗаменить(Стр, " ", ",");
				Стр = СтрЗаменить(Стр, ":", ",");
				Стр = СтрЗаменить(Стр, ".", ",");
				Стр = СтрЗаменить(Стр, ",,", ",");
				МассивЭлементов = Новый Массив;
				ВхождениеРазделителя = СтрНайти(Стр, ",");
				Пока ВхождениеРазделителя <> 0 Цикл
					МассивЭлементов.Добавить(СокрЛП(Лев(Стр, ВхождениеРазделителя - 1)));
					Стр = СокрЛП(Сред(Стр, ВхождениеРазделителя + 1));
					ВхождениеРазделителя = СтрНайти(Стр, ",");
				КонецЦикла;
				Возврат Дата(ПолучитьЭлементДаты(МассивЭлементов, 2),
							ПолучитьЭлементДаты(МассивЭлементов, 1),
							ПолучитьЭлементДаты(МассивЭлементов, 0),
							ПолучитьЭлементДаты(МассивЭлементов, 3),
							ПолучитьЭлементДаты(МассивЭлементов, 4),
							ПолучитьЭлементДаты(МассивЭлементов, 5));
			Иначе
				Возврат Дата(Число(СокрЛП(Сред(Стр, 7, 4))),
							Число(СокрЛП(Сред(Стр, 4, 2))),
							Число(СокрЛП(Сред(Стр, 1, 2))));
			КонецЕсли;
		КонецЕсли;
	Исключение
		Возврат '00010101';
	КонецПопытки;
	
КонецФункции

&НаСервереБезКонтекста
Функция РасширениеФайла(ИмяФайла) Экспорт
	
	ДлинаИмени = СтрДлина(ИмяФайла);
	Для Инд = 1 По ДлинаИмени Цикл
		ТекИнд = ДлинаИмени - Инд + 1;
		Если Сред(ИмяФайла, ТекИнд, 1) = "." Тогда
			Возврат Сред(ИмяФайла, ТекИнд + 1);
		КонецЕсли;
	КонецЦикла;
	Возврат Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция ТипСодержимогоПоИмениФайла(ИмяФайла, ДляПФР = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТипСодержимогоПоИмениФайла(ИмяФайла, ДляПФР);
		
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВложенияПисьма(Письмо, ИмяФайла, Данные, Тип, Размер = Неопределено) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Письмо) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	НачатьТранзакцию();
	
	МенеджерЗаписи = РегистрыСведений.ВложенияНеформализованныхДокументов.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.НеформализованныйДокумент = Письмо;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Тип = Тип;
	МенеджерЗаписи.Размер = Размер;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда
		ДвоичныеДанныеФайла = Данные.Получить();
	ИначеЕсли ТипДанных = Тип("Строка") Тогда
		РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
			Данные, "Данные", "ДобавитьВложенияПисьма");
		//@skip-check 1c-fresh-audit
		ДвоичныеДанныеФайла = Новый ДвоичныеДанные(Данные);
	Иначе
		ДвоичныеДанныеФайла = Данные;
	КонецЕсли;
	АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайла);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", 						Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов", 				Письмо);
	ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
	ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
	
	ИнформацияОбОбновляемомФайле = Новый Структура;
	ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
	ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
	ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
	ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		
		МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			Письмо,
			"ПерепискаСКонтролирующимиОрганамиПрисоединенныеФайлы",
			ИмяФайла);
		
		Если МассивФайлов.Количество() > 0 Тогда
			ПрисоединенныйФайл = МассивФайлов[0];
			РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
			
		Иначе
			ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла);
		КонецЕсли;
		
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		
		ПрисоединенныйФайлОбъект.Тип = Тип;
		ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ИмяФайла;
		
		ПрисоединенныйФайлОбъект.Записать();
	Исключение
		ОтменитьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось сохранить в информационной базе вложение %1 письма %2!%3'"),
																					ИмяФайла, Письмо, ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция XMLЗначениеВнутр(Тип, Значение)
	
	Попытка
		Возврат XMLЗначение(Тип, Значение)
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена, ТипыСообщений = Неопределено, ПомеченныеНаУдаление = Ложь) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортноеСообщение.Ссылка,
						  |	ТранспортноеСообщение.Статус,
						  |	ТранспортноеСообщение.Тип,
						  |	ТранспортноеСообщение.ПометкаУдаления,
						  |	ТранспортноеСообщение.Дата,
						  |	ТранспортноеСообщение.ИдентификаторСообщения,
						  |	ТранспортноеСообщение.ДатаТранспорта,
						  |	ТранспортноеСообщение.Основание,
						  |	ТранспортноеСообщение.ПротоколСОшибкой,
						  |	ТранспортноеСообщение.Тема
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	
	Если НЕ ПомеченныеНаУдаление Тогда
		Запрос.Текст = Запрос.Текст + "
							  |	И ТранспортноеСообщение.ПометкаУдаления = &ПометкаУдаления";
		Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	КонецЕсли;
	
	Если ТипыСообщений <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
							  |	И ТранспортноеСообщение.Тип В (&ТипыСообщений)";
		Запрос.УстановитьПараметр("ТипыСообщений", ТипыСообщений);
	КонецЕсли;
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

&НаСервереБезКонтекста
Функция ИзвлечьИменаФайловНФДИзОписания(ФайлОписания) Экспорт
	
	// выгружаем файл на диск, при необходимости
	ТипЗнчФайлОписания = ТипЗнч(ФайлОписания);
	Если ТипЗнчФайлОписания = Тип("ДвоичныеДанные") Тогда
		ВремФайл = ПолучитьИмяВременногоФайла();
		ФайлОписания.Записать(ВремФайл);
		НеобходимоУдалитьФайлОписания = Истина;
	ИначеЕсли ТипЗнчФайлОписания = Тип("ХранилищеЗначения") Тогда
		ВремФайл = ПолучитьИмяВременногоФайла();
		ФайлОписания.Получить().Записать(ВремФайл);
		НеобходимоУдалитьФайлОписания = Истина;
	Иначе
		ВремФайл = ФайлОписания;
		НеобходимоУдалитьФайлОписания = Ложь;
	КонецЕсли;
		
	// читаем содержимое
	Текст = НовыйЧтениеТекстаНаСервере(ВремФайл);
	СодержимоеФайлаОписания = Текст.Прочитать();
	Текст.Закрыть();
	
	// удаляем файл, при необходимости
	Если НеобходимоУдалитьФайлОписания Тогда
		// Временный файл создается локально.
		//@skip-check 1c-fresh-audit
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
	КонецЕсли;
	
	// анализируем файл
	Результат = Новый Соответствие;
	Для Инд = 1 По СтрЧислоСтрок(СодержимоеФайлаОписания) Цикл
		ТекСтр = СтрПолучитьСтроку(СодержимоеФайлаОписания, Инд);
		ВхождениеДвоеточия = СтрНайти(ТекСтр, ":");
		Если ВхождениеДвоеточия <> 0 Тогда
			Ключ = Лев(ТекСтр, ВхождениеДвоеточия - 1);
			Значение = Сред(ТекСтр, ВхождениеДвоеточия + 1);
			Если НЕ ПустаяСтрока(Ключ) И НЕ ПустаяСтрока(Значение) Тогда
				Результат.Вставить(СокрЛП(Ключ), СокрЛП(Значение));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СообщениеРасшифровано(Сообщение) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	ИСТИНА КАК Поле1
						  |ИЗ
						  |	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |ГДЕ
						  |	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = &ТранспортноеСообщение");
	Запрос.УстановитьПараметр("ТранспортноеСообщение", Сообщение);
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПредставлениеСообщения(Сообщение) Экспорт
	
	Тема = ?(ПустаяСтрока(Сообщение.Тема), "<тема не указана>", """" + Сообщение.Тема + """");
	Если ЗначениеЗаполнено(Сообщение.Дата) Тогда
		Возврат Тема + " от " + Сообщение.Дата;
	Иначе
		Возврат Тема;
	КонецЕсли;
	
КонецФункции
       
&НаСервереБезКонтекста
Функция ПолучитьАтрибутыУзла(Узел) Экспорт
	
	УзлыАтрибуты = Узел.Строки.НайтиСтроки(Новый Структура("Тип", "А"));
	
	АтрибутыУзла = Новый Структура;
	Для Каждого УзелАтрибут Из УзлыАтрибуты Цикл
		АтрибутыУзла.Вставить(УзелАтрибут.Имя, УзелАтрибут.Значение);
	КонецЦикла;
	
	Возврат АтрибутыУзла;
	
КонецФункции

&НаСервереБезКонтекста
Функция СоздатьДеревоСтруктурыXML() Экспорт
	
	ДеревоСтруктуры = Новый ДеревоЗначений;
	ДеревоСтруктуры.Колонки.Добавить("Имя");
	ДеревоСтруктуры.Колонки.Добавить("Тип");
	ДеревоСтруктуры.Колонки.Добавить("Значение");
	Возврат ДеревоСтруктуры;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрокаBase64, Знач ЗаданнаяКодировкаТекста = Неопределено, Знач АвтоопределениеКодировкиXML = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрокаBase64, ЗаданнаяКодировкаТекста, АвтоопределениеКодировкиXML);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПрочитатьТекстИзФайла(Знач ИмяФайла,
			Знач ЗаданнаяКодировкаТекста = Неопределено,
			АвтоопределениеКодировкиXML = Ложь) Экспорт
	
	ТребуетсяУдалитьВременныйФайл = Ложь;
	Если ЭтоАдресВременногоХранилища(ИмяФайла) Тогда
		ТребуетсяУдалитьВременныйФайл = Истина;
		ИмяФайлаНаДиске = ПолучитьИмяВременногоФайла();
		ДвДанные = ПолучитьИзВременногоХранилища(ИмяФайла);
		ДвДанные.Записать(ИмяФайлаНаДиске);		
	Иначе
		РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
			ИмяФайла, "ИмяФайла", "ПрочитатьТекстИзФайла");
		//@skip-check 1c-fresh-audit
		ДвДанные = Новый ДвоичныеДанные(ИмяФайла);
		ИмяФайлаНаДиске = ИмяФайла;
	КонецЕсли;
	
	Если АвтоопределениеКодировкиXML Тогда
		КодировкаДанных = ОпределитьКодировку(ДвДанные);
		ТекстИзФайла = ПрочитатьТекстИзФайла(ИмяФайлаНаДиске, КодировкаДанных);
	Иначе
		ОбъектЧтение = НовыйЧтениеТекстаНаСервере(ИмяФайлаНаДиске, ЗаданнаяКодировкаТекста);
		ТекстИзФайла = ОбъектЧтение.Прочитать();
		ОбъектЧтение.Закрыть();
	КонецЕсли;
	
	Если ТребуетсяУдалитьВременныйФайл Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаНаДиске);
	КонецЕсли;
	
	Возврат ТекстИзФайла;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗагрузитьСтрокуXMLВДеревоЗначений(СтрокаXML, ОписаниеОшибки = Неопределено, ЧтениеXML = Неопределено, Знач ТекУзел = Неопределено) Экспорт
	
	ПерваяИтерация = (ТекУзел = Неопределено);
	Если ПерваяИтерация Тогда
		ТекУзел = СоздатьДеревоСтруктурыXML();
		Попытка
			ЧтениеXML = Новый ЧтениеXML;
			ЧтениеXML.УстановитьСтроку(СтрокаXML);
		Исключение
			ОписаниеОшибки = "Ошибка разбора XML: " + ИнформацияОбОшибке().Описание + ".";
			Возврат Неопределено;
		КонецПопытки;
	КонецЕсли;
	
	Попытка
		Пока ЧтениеXML.Прочитать() Цикл
			ТипУзла = ЧтениеXML.ТипУзла;
			Если ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				НовУзел = ТекУзел.Строки.Добавить();
				НовУзел.Имя = ЧтениеXML.Имя;
				НовУзел.Тип = "Э";
				НовУзел.Значение = ЧтениеXML.Значение;
				Пока ЧтениеXML.ПрочитатьАтрибут() Цикл
					НовАтрибут = НовУзел.Строки.Добавить();
					НовАтрибут.Имя = ЧтениеXML.Имя;
					НовАтрибут.Тип = "А";
					НовАтрибут.Значение = ЧтениеXML.Значение;
				КонецЦикла;
				ЗагрузитьСтрокуXMLВДеревоЗначений(СтрокаXML, ОписаниеОшибки, ЧтениеXML, НовУзел);
				Если ЗначениеЗаполнено(ОписаниеОшибки) Тогда
					Возврат Неопределено;
				КонецЕсли;
			ИначеЕсли ТипУзла = ТипУзлаXML.КонецЭлемента Тогда
				Прервать;
			ИначеЕсли ТипУзла = ТипУзлаXML.Текст И ТипЗнч(ТекУзел) = Тип("СтрокаДереваЗначений") И ТекУзел.Тип = "Э" Тогда
				ТекУзел.Значение = ЧтениеXML.Значение;
			КонецЕсли;
		КонецЦикла;
	Исключение
		ОписаниеОшибки = "Ошибка разбора XML: " + ИнформацияОбОшибке().Описание + ".";
		Возврат Неопределено;
	КонецПопытки;
	
	Если ПерваяИтерация Тогда
		Возврат ТекУзел;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗагрузитьXMLВДеревоЗначений(ИмяФайлаXML = Неопределено, ТекстXML = Неопределено, ОписаниеОшибки = Неопределено) Экспорт
	
	// считываем текст из файла
	Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
		ТекстXML = ПрочитатьТекстИзФайла(ИмяФайлаXML, , Истина);
		Если НЕ ЗначениеЗаполнено(ТекстXML) Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	// восстанавливаем дерево
	Возврат ЗагрузитьСтрокуXMLВДеревоЗначений(ТекстXML, ОписаниеОшибки);
	
КонецФункции

&НаСервере
Функция ОбработатьСодержимоеТранспортногоКонтейнера(Объект, ПеремещенВАрхив = Ложь) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОбработатьСодержимоеТранспортногоКонтейнера(Объект, ДокументыПакета, ТаблицаРаспакованныхФайлов, ПеремещенВАрхив);

КонецФункции

&НаСервере
Функция ДополнитьТранспортноеСообщениеПолучателями(ТранспортноеСообщение, АдресФайлаДокумента) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ДополнитьТранспортноеСообщениеПолучателями(ТранспортноеСообщение, АдресФайлаДокумента);

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОтпечатокСертификатаПолучателя(ТранспортноеСообщениеСсылка)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОтпечатокСертификатаПолучателя(ТранспортноеСообщениеСсылка);

КонецФункции

&НаСервереБезКонтекста
Функция РазархивироватьФайлИзВременногоХранилища(
		АдресВоВременномХранилище,
		ВыводитьОшибку = Истина,
		ТипАрхива = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РазархивироватьФайлИзВременногоХранилища(
		АдресВоВременномХранилище,
		ВыводитьОшибку,
		ТипАрхива);
	
КонецФункции

&НаКлиенте
Процедура РасшифроватьТранспортноеСообщение(
		Объект,
		ВыполняемоеОповещение = Неопределено,
		ПараметрыПоказа = Неопределено,
		ПараметрыОперации = Неопределено) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(Объект);
	
	ТипСообщения = Неопределено;
	ФорматДокументооборота = Неопределено;
	
	СсылкаНаОбъект = ?(ТипЗнч(Объект) = Тип("ДокументСсылка.ТранспортноеСообщение"), Объект, Объект.Ссылка);
			
	// Получаем Контейнер, распаковываем файлы, разбираем XML
	// возвращает "Булево" или "тип сообщения"
	ПеремещенВАрхив = Ложь;
	Результат = ОбработатьСодержимоеТранспортногоКонтейнера(СсылкаНаОбъект, ПеремещенВАрхив);
	
	ЕстьПраво = МультирежимВызовСервера.ЕстьПравоНаТранспортноеСообщение(СсылкаНаОбъект);
	Если НЕ ЕстьПраво Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	АлгоритмШифрованияОбязателен = Ложь;
	Если ТипЗнч(Результат) = Тип("Булево") Тогда 
		Если ПеремещенВАрхив = Истина И ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыОперации, "РасшифровкаОдного") = Истина Тогда
			ПолучениеПеремещенныхФайлов(ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(СсылкаНаОбъект), Неопределено, "Расшифровка", Неопределено);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	ИначеЕсли ТипЗнч(Результат) = Тип("Структура") Тогда 
		Если НЕ Результат.Свойство("ТипСообщения", ТипСообщения) Тогда 
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
			Возврат;
		КонецЕсли;
		Результат.Свойство("ФорматДокументооборота", ФорматДокументооборота);
		Результат.Свойство("АлгоритмШифрованияОбязателен", АлгоритмШифрованияОбязателен);
	КонецЕсли;
	
	Если ДокументыПакета.Количество() = 0 Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	Если ТипСообщения = "ЦБ" Тогда
		ФайлОписанияСведений = Новый Массив;
		ИмяФайлаСтатуса = ВРЕГ("status.xml");
		ИмяФайлаИнформацииСтатуса = ВРЕГ("StatusInfo.xml");
		Для каждого СтрокаРаспакованныхФайлов Из ТаблицаРаспакованныхФайлов Цикл
			ИмяРаспакованногоФайла = ВРЕГ(СтрокаРаспакованныхФайлов.Имя);
			Если ИмяФайлаСтатуса = ИмяРаспакованногоФайла ИЛИ ИмяФайлаИнформацииСтатуса = ИмяРаспакованногоФайла Тогда
				ФайлОписанияСведений.Добавить(СтрокаРаспакованныхФайлов);
			КонецЕсли;
		КонецЦикла;
		
	Иначе
		ФайлОписанияСведений = ТаблицаРаспакованныхФайлов.НайтиСтроки(Новый Структура("Имя", "packageDescription.xml"));
	КонецЕсли;
	
	Если ФайлОписанияСведений.Количество() = 0 Тогда
		Если ТипСообщения = "ПФР" Тогда
			Для каждого РаспакованныйФайл Из ТаблицаРаспакованныхФайлов Цикл
				СтруктураИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(РаспакованныйФайл.Имя);
				ИмяФайлаВРег = СокрЛП(СтруктураИмениФайла.ИмяБезРасширения);
				ИмяФайлаВРег = ВРег(ИмяФайлаВРег);
				РасширениеФайла = СокрЛП(СтруктураИмениФайла.Расширение);
				Если (Лев(ИмяФайлаВРег, 4) = "ПФР_" ИЛИ Лев(ИмяФайлаВРег, 4) = "СФР_")
					И СтрНайти(ИмяФайлаВРег, "_ОСП_") > 0 И нрег(РасширениеФайла) = ".xml" Тогда
					
					ФайлОписанияСведений.Добавить(РаспакованныйФайл);
					ФорматДокументооборота = ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК");
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Если ФайлОписанияСведений.Количество() = 0 Тогда
			ТекстСообщения = ?(ТипСообщения = "ПФР",
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания пакета (ПФР_*_ОСП_*.xml или packageDescription.xml).'"),
				?(ТипСообщения = "ЦБ",
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания сведений (status.xml или StatusInfo.xml).'"),
				НСтр("ru = 'Ошибка распаковки пакета: не найден файл описания пакета (packageDescription.xml).'")));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	// расшифровываем содержимое
	// из свойств транспортного сообщения получим учетную запись, оттуда получим отпечаток сертификата получателя, которым зашифрован пакет
	СертификатПолучателя = ПолучитьОтпечатокСертификатаПолучателя(СсылкаНаОбъект);
	
	КонтекстРасшифровки = Новый Структура;
	КонтекстРасшифровки.Вставить("СертификатПолучателя", СертификатПолучателя);
	КонтекстРасшифровки.Вставить("СсылкаНаОбъект", СсылкаНаОбъект);
	КонтекстРасшифровки.Вставить("ТипСообщения", ТипСообщения);
	КонтекстРасшифровки.Вставить("ФорматДокументооборота", ФорматДокументооборота);
	КонтекстРасшифровки.Вставить("ТекущийДокумент", 0);
	КонтекстРасшифровки.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	КонтекстРасшифровки.Вставить("ПараметрыПоказа", ПараметрыПоказа);
	КонтекстРасшифровки.Вставить("АлгоритмШифрованияОбязателен", АлгоритмШифрованияОбязателен);
	
	Если ЭтоОблачнаяПодпись(СертификатПолучателя) 
		ИЛИ ЭтоПодписьСервиса(СертификатПолучателя) Тогда
	// обходим ограничения платформы на размер стека рекурсивного вызова оповещений
		КонтекстОтложеннойОперации = КонтекстРасшифровки;
		ПодключитьОбработчикОжидания("ВыполнитьРасшифровкуСообщенийПоследовательноОтложенно", 0.1, Истина);
	Иначе
		ВыполнитьРасшифровкуСообщенийПоследовательно(КонтекстРасшифровки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьСообщенияСЭДО(
	ОписаниеОповещения,
	Организация,
	НомерОбъекта,
	ДополнительныеПараметры)
	
	КоличествоРасшифрованных 	= ДополнительныеПараметры.КоличествоРасшифрованных;
	КоличествоНеРасшифрованных 	= ДополнительныеПараметры.КоличествоНеРасшифрованных;
	ВсегоСообщений				= ДополнительныеПараметры.ВсегоСообщений;
	
	Для ИндексСообщения = НомерОбъекта + 1 По СообщенияТребующиеРасшифровки.Количество() - 1 Цикл
		Сообщение = СообщенияТребующиеРасшифровки[ИндексСообщения];
		Если ЗначениеЗаполнено(Сообщение.ТранспортноеСообщение)
			ИЛИ НЕ Организация = Сообщение.Организация Тогда
			Прервать;
		КонецЕсли;
		НомерОбъекта = НомерОбъекта + 1;
	КонецЦикла;
	ДополнительныеПараметры.НомерОбъекта = НомерОбъекта;
	
	ТекущиеУчетныеЗаписиСтрахователяФСС = ПолучитьТекущиеУчетныеЗаписиСтрахователяФСС();
	Если НЕ ТекущиеУчетныеЗаписиСтрахователяФСС.Найти(Организация) = Неопределено Тогда
		
		ДополнительныеПараметрыЭтапаРасшифровки = Новый Структура();
		ДополнительныеПараметрыЭтапаРасшифровки.Вставить("КоличествоПройденныхЭтапов", 	КоличествоРасшифрованных + КоличествоНеРасшифрованных);
		ДополнительныеПараметрыЭтапаРасшифровки.Вставить("ОбщееКоличествоЭтапов", 		ВсегоСообщений);
		ДополнительныеПараметрыЭтапаРасшифровки.Вставить("ТекущаяОрганизация", 			Организация);
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаРасшифровки(ДополнительныеПараметрыЭтапаРасшифровки);
		
		// Получаем содержимое входящих сообщений по организации.
		Оповещение = Новый ОписаниеОповещения("ПолучитьВходящиеСообщенияСЭДОФССПоОрганизациямПослеЗапроса",
			ЭтотОбъект,
			ДополнительныеПараметры);
			Организации = ЗначениеВКоллекцию(Организация);
		
		ПолучитьВходящиеСообщенияСЭДОФСС(Оповещение, Организации, Ложь, Ложь);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ВосстановитьИсходныеИменаФайлов(Объект)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();	
	КонтекстЭДОСервер.ВосстановитьИсходныеИменаФайлов(Объект, ДокументыПакета);

КонецПроцедуры

&НаСервереБезКонтекста
Функция ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(ОбъектСсылка, ПроанализироватьРассылкиПриАвтоподтверждении = Ложь)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();	
	Возврат КонтекстЭДОСервер.ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(ОбъектСсылка, ПроанализироватьРассылкиПриАвтоподтверждении);
	
КонецФункции

&НаКлиенте
Процедура СоздатьПодтверждениеПриНеобходимости(ОповещениеОЗавершении, СсылкаНаОбъект, Вручную = Ложь)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Результат = ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(СсылкаНаОбъект, Истина);
	
	Если ЗначениеЗаполнено(Результат.ОписаниеОшибки) Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(Результат.ОписаниеОшибки);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ЗапрещеноАвтоподтверждение = НЕ Результат.Автоподтверждение;
	
	Если ЗапрещеноАвтоподтверждение И НЕ Вручную Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ТипСообщения = Результат.Тип;
	
	// формируем подтверждение на расшифрованное сообщение при необходимости
	Если Результат.ТребуетсяПодтверждение Тогда
	 
 		Контекст = Новый Структура;
		Контекст.Вставить("ОповещениеОЗавершении", 	ОповещениеОЗавершении);
		Контекст.Вставить("СсылкаНаОбъект", 		СсылкаНаОбъект);

		Оповещение = Новый ОписаниеОповещения(
			"СоздатьПодтверждениеПриНеобходимостиПослеСоздания",
			ЭтотОбъект,
			Контекст);
			
		ЭтоОтправкаПодтвержденияИзПанелиОтправки = 
			ТипЗнч(ОповещениеОЗавершении.ДополнительныеПараметры) = Тип("Структура")
			И ОповещениеОЗавершении.ДополнительныеПараметры.Свойство("ЭтоОтправкаПодтвержденияИзПанелиОтправки");
			
		Если ЭтоОтправкаПодтвержденияИзПанелиОтправки Тогда

			Если СообщениеРасшифровано(СсылкаНаОбъект) Тогда
				СоздатьПодтверждение(Оповещение, СсылкаНаОбъект);
			Иначе
				
				ДополнительныеПараметры = Новый Структура();
				ДополнительныеПараметры.Вставить("Оповещение", 		Оповещение);
				ДополнительныеПараметры.Вставить("СсылкаНаОбъект", 	СсылкаНаОбъект);
				ДополнительныеПараметры.Вставить("ЭтоОтправкаПодтвержденияИзПанелиОтправки", Истина);
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"СоздатьПодтверждениеПослеРасшифровки", 
					ЭтотОбъект, 
					ДополнительныеПараметры);
					
				РасшифроватьТранспортноеСообщение(СсылкаНаОбъект, ОписаниеОповещения);
				
			КонецЕсли;
			
		Иначе
			СоздатьПодтверждение(Оповещение, СсылкаНаОбъект);
		КонецЕсли;
		
	Иначе
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждениеПослеРасшифровки(Результат, ВходящийКонтекст) Экспорт
	
	Оповещение 		= ВходящийКонтекст.Оповещение;
	СсылкаНаОбъект 	= ВходящийКонтекст.СсылкаНаОбъект;
	
	Если Результат = Истина // Может быть Неопределено 
		И СообщениеРасшифровано(СсылкаНаОбъект) Тогда
		
		СоздатьПодтверждение(Оповещение, СсылкаНаОбъект);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(Оповещение.ДополнительныеПараметры.ОповещениеОЗавершении);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждениеПриНеобходимостиПослеСоздания(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось создать подтверждение к сообщению 
                                                                                       |%1'"), ПредставлениеТранспортногоСообщения(ВходящийКонтекст.СсылкаНаОбъект));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
		
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Результат);
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОбработатьНеформализованныеДокументы(ОбъектСсылка) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();	
	Возврат КонтекстЭДОСервер.ОбработатьНеформализованныеДокументы(ОбъектСсылка);
	
КонецФункции

&НаСервере
Функция ПоместитьФайлыВРегистрСодержимого(ОбъектСсылка) // помещаем файлы в регистр содержимого
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();	
	Возврат КонтекстЭДОСервер.ПоместитьФайлыВРегистрСодержимого(ОбъектСсылка, ДокументыПакета);
	
КонецФункции

&НаСервере
Процедура ЗафиксироватьПопыткуРасшифровки(ОбъектСсылка, Очистить = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();	
	КонтекстЭДОСервер.ЗафиксироватьПопыткуРасшифровки(ОбъектСсылка, Очистить);
	
КонецПроцедуры


&НаСервереБезКонтекста
Функция ДвоичныеДанныеИзСтроки64НаСервере(Данные)
	
	Возврат Base64Значение(Данные);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение,
													Тип,
													Данные,
													ИмяФайла,
													Размер = Неопределено,
													ЭЦПИмяПодписанногоФайла = Неопределено,
													ЭЦПСертификат = Неопределено,
													ЭЦПСтатусПроверки = Неопределено,
													ЭЦПЭтоПодписьАбонента = Неопределено,
													ТипФайлаОтчетностиПФР = Неопределено,
													Идентификатор = Неопределено,
													ТипСодержимогоФайла = Неопределено) Экспорт
	
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение,
													Тип,
													Данные,
													ИмяФайла,
													Размер,
													ЭЦПИмяПодписанногоФайла,
													ЭЦПСертификат,
													ЭЦПСтатусПроверки,
													ЭЦПЭтоПодписьАбонента,
													ТипФайлаОтчетностиПФР,
													Идентификатор,
													ТипСодержимогоФайла);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьРазмер(Данные) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьРазмер(Данные);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьТранспортныйКонтейнер(ТранспортноеСообщение, СДанными = Ложь) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.ПолучитьТранспортныйКонтейнер(ТранспортноеСообщение, СДанными);
	
КонецФункции

&НаСервереБезКонтекста
Процедура УдалитьТранспортныйКонтейнер(Сообщение) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Сообщение) Тогда
		Возврат;
	КонецЕсли;
	
	НаборЗаписей = РегистрыСведений.ТранспортныеКонтейнеры.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ТранспортноеСообщение.Установить(Сообщение);
	НаборЗаписей.Записать();
	
	МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
		Сообщение,
		"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы");
	Для каждого ПрисоединенныйФайл Из МассивФайлов Цикл
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		ПрисоединенныйФайлОбъект.УстановитьПометкуУдаления(Истина);
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция УдалитьТранспортныйКонтейнерНаСервереИПолучитьПараметрыОповещения(Сообщение)
	
	УдалитьТранспортныйКонтейнер(Сообщение);
	
	Возврат Новый Структура("Сообщение, ЦиклОбмена, Предмет", Сообщение, Сообщение.ЦиклОбмена, ПолучитьПредметыЦиклаОбмена(Сообщение.ЦиклОбмена));
	
КонецФункции

&НаКлиенте
Процедура УдалитьТранспортныйКонтейнерНаКлиенте(Сообщение) Экспорт
	
	ПараметрОповещения = УдалитьТранспортныйКонтейнерНаСервереИПолучитьПараметрыОповещения(Сообщение);
	
	#Если Клиент Тогда
	Оповестить("Изменение сообщения", ПараметрОповещения);
	#КонецЕсли
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьИменаТранспортныхКонтейнеров(Сообщение) Экспорт
	
	ТранспортныеКонтейнеры = ПолучитьТранспортныйКонтейнер(Сообщение);
	Возврат ТранспортныеКонтейнеры.ВыгрузитьКолонку("ИмяФайла");
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// РАБОТА С ПАРАМЕТРАМИ КРИПТОГРАФИИ
//

&НаСервереБезКонтекста
Функция ПолучитьСвойстваЦиклаОбмена(ТранспортноеСообщение)
	
	СвойстваЦиклаОбмена = Новый Структура("Вид, ФорматДокументооборота, УчетнаяЗапись");
	ЦиклОбмена = ТранспортноеСообщение.ЦиклОбмена;
	СвойстваЦиклаОбмена.Вид = ЦиклОбмена.Вид;
	СвойстваЦиклаОбмена.ФорматДокументооборота = ЦиклОбмена.ФорматДокументооборота;
	СвойстваЦиклаОбмена.УчетнаяЗапись = ТранспортноеСообщение.УчетнаяЗапись;
	Возврат СвойстваЦиклаОбмена;
	
КонецФункции

&НаКлиенте
Процедура ПоставитьПодписьОтветственногоЛица(ОповещениеОЗавершении, ТранспортноеСообщение, ОтветственноеЛицо, ИмяСгенерированногоФайлаЭЦП = Неопределено) Экспорт
	
	СвойстваЦиклаОбмена = ПолучитьСвойстваЦиклаОбмена(ТранспортноеСообщение);
	
	КонтекстОтправки = Новый Структура;
	Если СвойстваЦиклаОбмена.Вид = СтруктураСсылочныхДанных.Перечисления_ВидыЦикловОбмена_ЦиклОбменаСПФР Тогда
		ПодписатьСообщение("ПФР", ОповещениеОЗавершении, КонтекстОтправки, ТранспортноеСообщение, , ОтветственноеЛицо, ИмяСгенерированногоФайлаЭЦП, СвойстваЦиклаОбмена.УчетнаяЗапись);
		Возврат;
	ИначеЕсли СвойстваЦиклаОбмена.Вид = СтруктураСсылочныхДанных.Перечисления_ВидыЦикловОбмена_ЦиклОбменаСФСГС Тогда
		ПодписатьСообщение("ФСГС", ОповещениеОЗавершении, КонтекстОтправки, ТранспортноеСообщение, , ОтветственноеЛицо, ИмяСгенерированногоФайлаЭЦП, СвойстваЦиклаОбмена.УчетнаяЗапись);
		Возврат;
	ИначеЕсли СвойстваЦиклаОбмена.ФорматДокументооборота = СтруктураСсылочныхДанных.Перечисления_ФорматыДокументооборотаСФНС_Приказ534 Тогда
		ПодписатьСообщение("ФНС", ОповещениеОЗавершении, КонтекстОтправки, ТранспортноеСообщение, , ОтветственноеЛицо, ИмяСгенерированногоФайлаЭЦП, СвойстваЦиклаОбмена.УчетнаяЗапись);
		Возврат;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСообщениеПослеВыполнения(Результат, ВходящийКонтекст) Экспорт
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Результат);
	
КонецПроцедуры

&НаКлиенте
Функция ПредставлениеСертификатаПоСвойствам(СвойстваСертификата) Экспорт
	
	Возврат ПредставлениеСертификатаПоСвойствамНаСервере(СвойстваСертификата);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеСертификатаПоСвойствамНаСервере(Знач СвойстваСертификата) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПредставлениеСертификатаПоСвойствам(СвойстваСертификата);
	
КонецФункции

//
// АВТОМАТИЧЕСКАЯ НАСТРОЙКА ПАРАМЕТРОВ УЧЕТНОЙ ЗАПИСИ (ПОДДЕРЖКА РОКИ)
////////////////////////////////////////////////////////////////////////////////


////////////////////////////////////////////////////////////////////////////////
// РАБОТЫ С ЦИФРОВЫМИ СЕРТИФИКАТАМИ
//

&НаКлиенте
Процедура ВыбратьСертификатОтветственногоЛицаДляПодписания(НачальноеЗначениеВыбора = Неопределено, Организация = Неопределено, ПутьКОбъекту = Неопределено, ВыполняемоеОповещение = Неопределено) Экспорт
	
	Если ПутьКОбъекту = Неопределено Тогда 
		ПутьКОбъекту = "Обработка.ДокументооборотСКонтролирующимиОрганами";
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("РежимВыбора", Истина);
	Если Организация <> Неопределено Тогда 
		СтруктураПараметров.Вставить("Организация", Организация);
	КонецЕсли;
	Если НачальноеЗначениеВыбора <> Неопределено Тогда 
		СтруктураПараметров.Вставить("НачальноеЗначениеВыбора", НачальноеЗначениеВыбора);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ВыполняемоеОповещение);	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыбратьСертификатОтветственногоЛицаДляПодписанияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ВыборСертификатаОтветственногоЛица", СтруктураПараметров, Новый УникальныйИдентификатор,,,, ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьСертификатОтветственногоЛицаДляПодписанияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ВЫГРУЗКА ДОВЕРЕННОСТИ 534

&НаСервереБезКонтекста
Функция ПолучитьПредметыЦиклаОбмена(ЦиклОбмена) Экспорт
	
	Предметы = ЦиклОбмена.ДополнительныеПредметы.ВыгрузитьКолонку("Предмет");
	Предметы.Вставить(0, ЦиклОбмена.Предмет);
	Возврат Предметы;
	
КонецФункции

&НаСервере
Функция СписокДопустимыхОрганизацийВОбъектахОбмена() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОрганизацииПоУчетнымЗаписямПользователя();
	
КонецФункции

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "Получение контекста ЭДО" И ТипЗнч(Параметр) = Тип("Структура") Тогда
		Параметр.КонтекстЭДО = ЭтаФорма;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПредметТранспортногоСообщения(ТранспортноеСообщение)
	
	Возврат ТранспортноеСообщение.ЦиклОбмена.Предмет;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьПриложениеПФР(ТранспортноеСообщение, СтрВложение, ВыполняемоеОповещение = Неопределено) Экспорт
	
	Если СтрВложение.ВАрхиве Тогда
		
		ПоказатьУведомлениеАрхивныхФайлов(, 27, 3);
		
		Если ВыполняемоеОповещение <> Неопределено Тогда 
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ЭтоPDF = ЭтоPDFФайл(СтрВложение.Адрес, СтрВложение.ИмяФайла);
	
	Если ТребуетсяОсобыйСпособОтображения(СтрВложение.ИмяФайла, СтрВложение.ТипСодержимогоФайла) И НЕ ЭтоPDF Тогда
		
		СсылкаНаОтчет = ПредметТранспортногоСообщения(ТранспортноеСообщение);
		ПоказатьКакТабличныйДокумент(СсылкаНаОтчет, СтрВложение.ИмяФайла, СтрВложение.Адрес);

	ИначеЕсли СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_html
		ИЛИ (СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_xml
		И НЕ ЭтоВебКлиент() 
		И НЕ ОбщегоНазначенияКлиент.ЭтоLinuxКлиент() 
		И НЕ ОбщегоНазначенияКлиент.ЭтоMacOSКлиент() 
		И НЕ ЭтоPDF) Тогда
		
		ПоказатьHTML(СтрВложение.ИмяФайла, СтрВложение.Данные, , СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_xml);
		
	ИначеЕсли ЭтоВебКлиент() ИЛИ ОбщегоНазначенияКлиент.ЭтоLinuxКлиент() ИЛИ ОбщегоНазначенияКлиент.ЭтоMacOSКлиент() Тогда
		
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(СтрВложение.Адрес, СтрВложение.ИмяФайла);
		
	ИначеЕсли СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст1251 Тогда
		
		ПоказатьТекстИзСтроки(СтрВложение.Данные, СтрВложение.ИмяФайла, "windows-1251");
		
	ИначеЕсли СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст866 Тогда
		
		ПоказатьТекстИзСтроки(СтрВложение.Данные, СтрВложение.ИмяФайла, "cp866");
		
	Иначе
		
		ИмяФайлаВложения = ?(нрег(Прав(СтрВложение.ИмяФайла, 4)) = ".enc",
			Лев(СтрВложение.ИмяФайла, СтрДлина(СтрВложение.ИмяФайла) - 4), СтрВложение.ИмяФайла);
		
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(СтрВложение.Адрес, ИмяФайлаВложения);
		
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда 
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьТекстИзСтроки(СтрокаСодержимого, ИсходноеИмяФайла, КодировкаТекста = Неопределено, ПечатьРазрешена = Истина) Экспорт
	
	Если ПечатьРазрешена Тогда
		СтруктураПараметров = Новый Структура("ИмяФайла, Содержимое, Кодировка", ИсходноеИмяФайла, СтрокаСодержимого, КодировкаТекста);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрТекст", СтруктураПараметров,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	Иначе
		Текст = Новый ТекстовыйДокумент;
		Текст.УстановитьТекст(СтрокаСодержимого);
		Текст.ТолькоПросмотр = Истина;
		Текст.Показать(ИсходноеИмяФайла + " - просмотр", ИсходноеИмяФайла);
	КонецЕсли;
	
КонецПроцедуры

// Просмотр XML в веб-клиенте и на Linux не работает
&НаКлиенте
Процедура ПоказатьHTML(ИмяФайла, Содержимое, ОткрытьМодально = Истина, ЭтоXML = Ложь) Экспорт
	
	ЗначениеСодержимое = Содержимое;
	
	#Если НЕ ВебКлиент Тогда
		Если ЭтоXML И НЕ ОбщегоНазначенияКлиент.ЭтоLinuxКлиент() И НЕ ОбщегоНазначенияКлиент.ЭтоMacOSКлиент() Тогда
			Если ТипЗнч(ЗначениеСодержимое) = Тип("Строка") И ЭтоАдресВременногоХранилища(ЗначениеСодержимое) Тогда
				ЗначениеСодержимое = ПолучитьИзВременногоХранилища(ЗначениеСодержимое);
			КонецЕсли;
			
			Если ТипЗнч(ЗначениеСодержимое) = Тип("ДвоичныеДанные") Тогда
				ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
				ЗначениеСодержимое.Записать(ИмяВременногоФайла);
				ЗначениеСодержимое = ИмяВременногоФайла;
			КонецЕсли;
		КонецЕсли;
	#КонецЕсли
	
	СтруктураПараметров = Новый Структура("ИмяФайла, Содержимое", ИмяФайла, ЗначениеСодержимое);
	
	Если ОткрытьМодально Тогда
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрHTML", СтруктураПараметров,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрHTML", СтруктураПараметров);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьXMLИзСтроки(ИмяФайлаСодержимого, ИсходноеИмяФайла) Экспорт
	
	ПоказатьHTML(ИсходноеИмяФайла, ИмяФайлаСодержимого, , Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСообщениеЦиклаОбмена(ЦиклОбмена, ТипСообщения, Основание = Неопределено) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортноеСообщение.Ссылка
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |	И ТранспортноеСообщение.Тип = &Тип");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	Запрос.УстановитьПараметр("Тип", ТипСообщения);
	
	Если Основание <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
						  |	И ТранспортноеСообщение.Основание = &Основание";
		Запрос.УстановитьПараметр("Основание", Основание);
	КонецЕсли;
	
	Запрос.Текст = Запрос.Текст + "
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортноеСообщение.Дата УБЫВ";
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Документы.ТранспортноеСообщение.ПустаяСсылка();
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	Иначе
		Возврат Документы.ТранспортноеСообщение.ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбработатьСодержаниеОснования(Содержание)
	
	Результат = "";
	Для Инд = 1 По СтрЧислоСтрок(Содержание) Цикл
		Результат = Результат + "> " + СтрПолучитьСтроку(Содержание, Инд) + Символы.ПС;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции


////////////////////////////////////////////////////////////////////////////////
// Работа с сообщениями

&НаСервереБезКонтекста
Процедура ДополнитьСообщения(Сообщения, МассивСообщений)
	
	Если ЭтоАдресВременногоХранилища(Сообщения) Тогда 
		ТаблицаСообщений = ПолучитьИзВременногоХранилища(Сообщения);
	Иначе 
		ТаблицаСообщений = Сообщения;
	КонецЕсли;
	
	Для Каждого ДополнительноеСообщение Из МассивСообщений Цикл
		НовСтр = ТаблицаСообщений.Добавить();
		НовСтр.Сообщение = ДополнительноеСообщение.Сообщение;
		НовСтр.Ошибка = ДополнительноеСообщение.Ошибка;
	КонецЦикла;
	
	Если ЭтоАдресВременногоХранилища(Сообщения) Тогда 
		Сообщения = ПоместитьВоВременноеХранилище(ТаблицаСообщений, Сообщения);
	КонецЕсли;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура ДобавитьСообщение(Сообщения, Сообщение, Ошибка = Истина)
	
	Если ЭтоАдресВременногоХранилища(Сообщения) Тогда 
		ТаблицаСообщений = ПолучитьИзВременногоХранилища(Сообщения);
		НовСтр = ТаблицаСообщений.Добавить();
		НовСтр.Сообщение = Сообщение;
		НовСтр.Ошибка = Ошибка;
		Сообщения = ПоместитьВоВременноеХранилище(ТаблицаСообщений, Сообщения);
	Иначе
		Если Сообщения <> Неопределено Тогда
			НовСтр = Сообщения.Добавить();
			НовСтр.Сообщение = Сообщение;
			НовСтр.Ошибка = Ошибка;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СоздатьТаблицуСообщений(ВозвращатьАдрес = Ложь) Экспорт
	
	Сообщения = Новый ТаблицаЗначений;
	Сообщения.Колонки.Добавить("Сообщение");
	Сообщения.Колонки.Добавить("Ошибка");
	Если ВозвращатьАдрес Тогда
		Возврат ПоместитьВоВременноеХранилище(Сообщения, Новый УникальныйИдентификатор);
	Иначе
		Возврат Сообщения;
	КонецЕсли;
	
КонецФункции

// Работа с сообщениями
////////////////////////////////////////////////////////////////////////////////

&НаКлиенте
Процедура ВыбратьОрганизации(НачальноеЗначениеВыбора, МножественныйВыбор = Истина, ВыполняемоеОповещение = Неопределено) Экспорт
	
	СтруктураПараметров = Новый Структура("НачальноеЗначениеВыбора, МножественныйВыбор",
											НачальноеЗначениеВыбора, МножественныйВыбор);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыбратьОрганизацииЗавершение", ЭтотОбъект, ВыполняемоеОповещение);										
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ВыборОрганизацииОбмена", СтруктураПараметров, Новый УникальныйИдентификатор,,,, ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры
								
&НаКлиенте								
Процедура ВыбратьОрганизацииЗавершение(Результат, ВыполняемоеОповещение) Экспорт
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

// Работа с сообщениями
////////////////////////////////////////////////////////////////////////////////


////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ СПРАВОЧНИКА ПЕРЕПИСКАСКОНТРОЛИРУЮЩИМИОРГАНАМИ

&НаСервереБезКонтекста
Функция ПолучитьВложенияНеформализованногоДокумента(ДокНФД, ИмяФайла = Неопределено, СДанными = Ложь) Экспорт //Из модуля
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьВложенияНеформализованногоДокумента(ДокНФД, ИмяФайла, СДанными);
	
КонецФункции

&НаСервереБезКонтекста
Функция  ОбработкаЗаполненияПисьма(Объект, Основание, НастройкиОтвета)
	
	ЭтоРетроконверсия = (НастройкиОтвета <> Неопределено);
	// если это ретроконверсия, то обработка особая
	Если ЭтоРетроконверсия Тогда
		
		ВариантПодтверждения = НастройкиОтвета.ВариантПодтверждения;
		ПодтвержденныеФайлыСведений = НастройкиОтвета.ФайлыСведений;
		
		// заполняем свойства (кроме вложений)
		Объект.Наименование = "Re:" + СокрЛП(Основание.Наименование); // требование приемного ПО Калуга-Астрал
		Объект.Тип = Основание.Тип;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Отправитель = Основание.Получатель;
		Объект.Получатель = Основание.Отправитель;
		Объект.ДатаСообщения = ТекущаяДатаСеанса();
		Объект.ИдентификаторОснования = Основание.Идентификатор;
		Объект.Ретроконверсия = Основание.Ретроконверсия;
		
		ОбработанноеСодержимоеОснования = ОбработатьСодержаниеОснования(Основание.Содержание);
		Если ВариантПодтверждения = 2 Тогда // отклонение сведений
			Объект.Содержание = "Сведения содержат ошибки." + Символы.ПС + Символы.ПС + ОбработанноеСодержимоеОснования;
		Иначе
			
			Если ВариантПодтверждения = 3 Тогда // частичное подтверждение
				Объект.Содержание = "Сведения подтверждены лишь частично." + Символы.ПС + Символы.ПС + ОбработанноеСодержимоеОснования;
			Иначе
				Объект.Содержание = "Сведения верны." + Символы.ПС + Символы.ПС + ОбработанноеСодержимоеОснования;
			КонецЕсли;
			
			Вложения = ПолучитьВложенияНеформализованногоДокумента(Основание, , Истина);
			
			НачатьТранзакцию();
			
			Попытка
				Объект.Записать();
			Исключение
				ОтменитьТранзакцию();
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось создать ответ:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецПопытки;
			
			// копируем вложения
			Для Каждого Вложение Из Вложения Цикл
				Если ПодтвержденныеФайлыСведений.Найти(Вложение.ИмяФайла) <> Неопределено Тогда
					Если НЕ ДобавитьВложенияПисьма(Объект.Ссылка, Вложение.ИмяФайла, Вложение.Данные, Вложение.Тип, Вложение.Размер) Тогда
						ОтменитьТранзакцию();
						Возврат Неопределено;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			
			ЗафиксироватьТранзакцию();
			
		КонецЕсли;
		
		Возврат Объект;
		
	Иначе
	
		// заполняем свойства (кроме вложений)
		Объект.Наименование = "Ответ на: " + СокрЛП(Основание.Наименование);
		Объект.Тип = Основание.Тип;
		Объект.Статус = Перечисления.СтатусыПисем.Исходящее;
		Объект.Отправитель = Основание.Получатель;
		Объект.Получатель = Основание.Отправитель;
		Объект.Содержание = Символы.ПС + Символы.ПС + ОбработатьСодержаниеОснования(Основание.Содержание);
		Объект.ДатаСообщения = ТекущаяДатаСеанса();
		Объект.ИдентификаторОснования = Основание.Идентификатор;
		Объект.Ретроконверсия = Основание.Ретроконверсия;
		
		Возврат Объект;
		
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция СоздатьОтветНаПисьмо(ОснованиеСсылка, НастройкиОтвета = Неопределено) Экспорт
	
	НовоеПисьмо = Справочники.ПерепискаСКонтролирующимиОрганами.СоздатьЭлемент();
	Попытка
		НовоеПисьмо = ОбработкаЗаполненияПисьма(НовоеПисьмо, ОснованиеСсылка, НастройкиОтвета);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось создать ответ:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	Если НовоеПисьмо <> Неопределено Тогда
		Попытка
			НовоеПисьмо.Записать();
			Возврат НовоеПисьмо.Ссылка;
		Исключение
			Возврат Неопределено;
		КонецПопытки;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтветНаПисьмоСуществует(Ссылка) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтветНаПисьмоСуществует(Ссылка);
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Работа с кэшем файлов настроек

&НаСервереБезКонтекста
Процедура СохранитьВКэшеФайлНастроек(УчетнаяЗапись,
			ИмяРесурсаСНастройками, ФайлНастроек, Расшифрован, ДатаФайла) Экспорт
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота") Тогда
		УчетнаяЗаписьСсылка = ?(УчетнаяЗапись.ЭтоНовый(), УчетнаяЗапись.ПолучитьСсылкуНового(), УчетнаяЗапись.Ссылка);
	Иначе
		УчетнаяЗаписьСсылка = УчетнаяЗапись;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗаписьСсылка) Тогда
		Возврат;
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.ФайлыСведенийРОКИ.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.УчетнаяЗапись = УчетнаяЗаписьСсылка;
	МенеджерЗаписи.ИмяРесурса = ИмяРесурсаСНастройками;
	МенеджерЗаписи.Расшифрован = Расшифрован;
	МенеджерЗаписи.ДатаФайла = ДатаФайла;
	
	Если ЭтоАдресВременногоХранилища(ФайлНастроек) Тогда
		Данные = ПолучитьИзВременногоХранилища(ФайлНастроек);
	Иначе
		Если РегламентированнаяОтчетностьКлиентСервер.ЭтоИмяВременногоФайла(ФайлНастроек) Тогда
			Возврат;
		КонецЕсли;
		//@skip-check 1c-fresh-audit
		Данные = Новый ДвоичныеДанные(ФайлНастроек);
	КонецЕсли;
	
	Попытка
		МенеджерЗаписи.Размер = ОпределитьРазмер(ФайлНастроек);
		МенеджерЗаписи.Данные = Новый ХранилищеЗначения(
			Данные, Новый СжатиеДанных);
		МенеджерЗаписи.Записать(Истина);
	Исключение
	КонецПопытки;
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// Работа с сервером

Функция ПолучитьНастройкиПрокси() Экспорт
	
	ИмяПользователя = ХранилищеОбщихНастроек.Загрузить("ЗащищенныйДокументооборотСФНС_Прокси_ИмяПользователя");
	Пароль = ХранилищеОбщихНастроек.Загрузить("ЗащищенныйДокументооборотСФНС_Прокси_Пароль");
	Возврат Новый Структура("ИмяПользователя, Пароль", ИмяПользователя, Пароль);
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// АВТОМАТИЧЕСКАЯ НАСТРОЙКА ПАРАМЕТРОВ УЧЕТНОЙ ЗАПИСИ (ПОДДЕРЖКА РОКИ)
//

&НаКлиенте
Процедура ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(
		ОбъектСсылка,
		ВыполняемоеОповещение,
		СтруктураДляИнформацииОСервереОнлайнПроверки = Неопределено) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	УчетнаяЗапись = УчетнаяЗаписьПоОбъектуОтправки(ОбъектСсылка);
	Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗапись, ,ВыполняемоеОповещение, , , Истина, , , , СтруктураДляИнформацииОСервереОнлайнПроверки);
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ОбновлятьАвтонастройкуПоВремени(УчетнаяЗапись)
	
	Если СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи = Неопределено Тогда
		СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи = Новый Соответствие;
	КонецЕсли;
	ДатаВремяПоследнегоОбновления = СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи[УчетнаяЗапись];
	
	ТекДата = ТекущаяДатаНаСервере();
	
	Если ДатаВремяПоследнегоОбновления <> Неопределено И ТекДата < (ДатаВремяПоследнегоОбновления + 10*60) Тогда
		Возврат Ложь;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПреобразоватьВСсылкуНаУчетнуюЗапись(ОбъектДоПреобразования)
	
	ТипОбъекта = ТипЗнч(ОбъектДоПреобразования);
	Если ТипОбъекта = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота")  Тогда
		Возврат ОбъектДоПреобразования; 
	ИначеЕсли ТипОбъекта = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота")  Тогда
		Возврат ОбъектДоПреобразования.Ссылка;
	ИначеЕсли ТипОбъекта = Тип("СправочникСсылка.Организации")  Тогда
		Возврат УчетнаяЗаписьОрганизации(ОбъектДоПреобразования);	
	КонецЕсли;	
	
КонецФункции 

&НаСервереБезКонтекста
Функция УчетнаяЗаписьПоОбъектуОтправки(Знач ОбъектСсылка)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.УчетнаяЗаписьПоОбъектуОтправки(ОбъектСсылка);
	
КонецФункции 

&НаКлиенте
Процедура АвтоматическаяНастройкаУчетнойЗаписи(Знач УчетнаяЗапись,
											Организация = Неопределено,
											ВыполняемоеОповещение = Неопределено,
											УведомлятьТолькоОбОшибках = Ложь,
											ИгнорироватьРежимАвтонастройки = Ложь,
											ТихийРежим = Ложь,
											ЗапрашиватьПараметрыОрганизацииПриНеобходимости = Ложь,
											ВызовИзМастераПодключенияК1СОтчетности = Ложь, 
											ТекстОшибокДляМастераПодключенияК1СОтчетности = "",
											СтруктураДляИнформацииОСервереОнлайнПроверки = Неопределено,
											ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки = Истина) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
											
	Если ТипЗнч(УчетнаяЗапись) <> Тип("СправочникСсылка.УчетныеЗаписиДокументооборота")  Тогда
		УчетнаяЗаписьСсылка = ПреобразоватьВСсылкуНаУчетнуюЗапись(УчетнаяЗапись);
	Иначе
		УчетнаяЗаписьСсылка = УчетнаяЗапись;
	КонецЕсли;
	
	ВызовДляПолученияИнформации = (СтруктураДляИнформацииОСервереОнлайнПроверки <> Неопределено);
	ОбращениеКСерверуОтключено = Ложь;
	СохранениеВИБОтключено = Ложь;
	
	Если НЕ ИгнорироватьРежимАвтонастройки И НЕ ОбновлятьАвтонастройкуПоВремени(УчетнаяЗаписьСсылка) Тогда
		
		Если ВызовДляПолученияИнформации Тогда
			ОбращениеКСерверуОтключено = Истина;
		Иначе
			Если ВыполняемоеОповещение <> Неопределено Тогда
				РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок, СтруктураДляИнформацииОСервереОнлайнПроверки", Неопределено, ТекстОшибокДляМастераПодключенияК1СОтчетности, СтруктураДляИнформацииОСервереОнлайнПроверки);
				РезультатНастройки.Вставить("ОписаниеРезультата", "Не заполнена информация о сервере онлайн-проверки");
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	НастройкиУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	
	// если настройки недоступны или учетка для обмена напрямую, то прервемся
	Если НастройкиУчетнойЗаписи = Неопределено ИЛИ НастройкиУчетнойЗаписи.ОбменНапрямую Тогда
		Если ВыполняемоеОповещение <> Неопределено Тогда
			РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок, СтруктураДляИнформацииОСервереОнлайнПроверки", Неопределено, ТекстОшибокДляМастераПодключенияК1СОтчетности, СтруктураДляИнформацииОСервереОнлайнПроверки);
			Если НастройкиУчетнойЗаписи = Неопределено Тогда
				РезультатНастройки.Вставить("ОписаниеРезультата", "В ИБ отсутствуют настройки учетной записи");
			ИначеЕсли НастройкиУчетнойЗаписи.ОбменНапрямую Тогда
				РезультатНастройки.Вставить("ОписаниеРезультата", "В настройках учетной записи включен обмен напрямую");
			КонецЕсли;			
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НастройкиУчетнойЗаписи.СпецОператорСвязи = СтруктураСсылочныхДанных.Перечисления_СпецоператорыСвязи_Прочие Тогда
		
		Если ВыполняемоеОповещение <> Неопределено Тогда
			РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок, СтруктураДляИнформацииОСервереОнлайнПроверки", Неопределено, ТекстОшибокДляМастераПодключенияК1СОтчетности, СтруктураДляИнформацииОСервереОнлайнПроверки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Автонастройка не выполняется для прочих спецоператоров связи");
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НастройкиУчетнойЗаписи.СпецОператорСвязи = СтруктураСсылочныхДанных.Перечисления_СпецоператорыСвязи_Такском Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 							ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", 	ТекстОшибокДляМастераПодключенияК1СОтчетности);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", 									УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("УведомлятьТолькоОбОшибках", 						УведомлятьТолькоОбОшибках);
		ДополнительныеПараметры.Вставить("УчетнаяЗаписьСсылка", 							УчетнаяЗаписьСсылка);
		ДополнительныеПараметры.Вставить("Организация", 									Организация);
		ОписаниеОповещения = Новый ОписаниеОповещения("АвтоматическаяНастройкаУчетнойЗаписиЗавершение",
			ЭтотОбъект, ДополнительныеПараметры); 

		ПолучитьНастройкиУчетнойЗаписиССервера(УчетнаяЗапись,
			Организация,
			УведомлятьТолькоОбОшибках,
			ИгнорироватьРежимАвтонастройки,
			ТихийРежим,
			ЗапрашиватьПараметрыОрганизацииПриНеобходимости,
			ОписаниеОповещения);
			
	Иначе
		
		ОбновленияПризнак = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(НастройкиУчетнойЗаписи.СпецОператорСвязи, "ОбновленияПризнак");
		
		Если НЕ ИгнорироватьРежимАвтонастройки Тогда
			Если НастройкиУчетнойЗаписи.РежимАвтонастройки = СтруктураСсылочныхДанных.Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_Отключена
				ИЛИ НастройкиУчетнойЗаписи.РежимАвтонастройки = СтруктураСсылочныхДанных.Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_ПустаяСсылка Тогда
					
					Если ВызовДляПолученияИнформации Тогда
						СохранениеВИБОтключено = Истина;
					Иначе
						Если ВыполняемоеОповещение <> Неопределено Тогда
							РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок, СтруктураДляИнформацииОСервереОнлайнПроверки", Неопределено, ТекстОшибокДляМастераПодключенияК1СОтчетности, СтруктураДляИнформацииОСервереОнлайнПроверки);
							РезультатНастройки.Вставить("ОписаниеРезультата", "Не заполнена информация о сервере онлайн-проверки");
							ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
						КонецЕсли;
						Возврат;
					КонецЕсли;
					
			КонецЕсли;
		КонецЕсли;
		
		Если ОбновленияПризнак <> Неопределено И (ОбновленияПризнак = "Истина" ИЛИ ОбновленияПризнак = Истина) Тогда
			
			ИдентификаторАбонента      = НастройкиУчетнойЗаписи.ИдентификаторАбонента;
			ИдентификаторСпецоператора = НастройкиУчетнойЗаписи.ИдентификаторСпецоператора;
			ИдентификаторАбонента      = Нрег(Сред(ИдентификаторАбонента, СтрДлина(ИдентификаторСпецоператора)+1));
			
			ПараметрыОбработатьОбновление = ПараметрыОбработатьОбновление();
			ПараметрыОбработатьОбновление.ИдентификаторДокументооборота 							= НастройкиУчетнойЗаписи.ИдентификаторДокументооборота;
			ПараметрыОбработатьОбновление.ТелефонМобильныйДляАвторизации 							= НастройкиУчетнойЗаписи.ТелефонМобильныйДляАвторизации;
			ПараметрыОбработатьОбновление.ИдентификаторАбонента 									= ИдентификаторАбонента;
			ПараметрыОбработатьОбновление.СпецОператорСвязи 										= НастройкиУчетнойЗаписи.СпецОператорСвязи;
			ПараметрыОбработатьОбновление.УчетнаяЗапись 											= УчетнаяЗапись;
			ПараметрыОбработатьОбновление.ТихийРежим 												= ТихийРежим;
			ПараметрыОбработатьОбновление.РежимАвтонастройки 										= Истина;
			ПараметрыОбработатьОбновление.ВызовИзМастераПодключенияК1СОтчетности 					= ВызовИзМастераПодключенияК1СОтчетности;
			ПараметрыОбработатьОбновление.ТекстОшибокДляМастераПодключенияК1СОтчетности 			= ТекстОшибокДляМастераПодключенияК1СОтчетности;
			ПараметрыОбработатьОбновление.ОбращениеКСерверуОтключено 								= ОбращениеКСерверуОтключено;
			ПараметрыОбработатьОбновление.СохранениеВИБОтключено 									= СохранениеВИБОтключено;
			ПараметрыОбработатьОбновление.СтруктураДляИнформацииОСервереОнлайнПроверки 				= СтруктураДляИнформацииОСервереОнлайнПроверки;
			ПараметрыОбработатьОбновление.ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки 	= ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки;
			ПараметрыОбработатьОбновление.ИгнорироватьРежимАвтонастройки						 	= ИгнорироватьРежимАвтонастройки;
			
			МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(НастройкиУчетнойЗаписи);
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ПараметрыОбработатьОбновление);
			
			ПовторятьСоединениеДляПолученияАктуальногоОбновления = (ИгнорироватьРежимАвтонастройки
				ИЛИ ВызовИзМастераПодключенияК1СОтчетности ИЛИ СтруктураДляИнформацииОСервереОнлайнПроверки <> Неопределено);
			
			ДополнительныеПараметры = Новый Структура("УчетнаяЗаписьСсылка, ВыполняемоеОповещение", УчетнаяЗаписьСсылка, ВыполняемоеОповещение);
			ОписаниеОповещения = Новый ОписаниеОповещения("АвтоматическаяНастройкаУчетнойЗаписиПослеОбработкиОбновленияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ОбработатьОбновление(ПараметрыОбработатьОбновление, ОписаниеОповещения, ПовторятьСоединениеДляПолученияАктуальногоОбновления);
		Иначе
			Если ВыполняемоеОповещение <> Неопределено Тогда
				РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок, СтруктураДляИнформацииОСервереОнлайнПроверки", Неопределено, ТекстОшибокДляМастераПодключенияК1СОтчетности, СтруктураДляИнформацииОСервереОнлайнПроверки);
				Если ОбновленияПризнак = Неопределено Тогда
					РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось найти ""признак обновления"" в параметрах спецоператора");
				ИначеЕсли ОбновленияПризнак = "Ложь" Или ОбновленияПризнак = Ложь Тогда
					РезультатНастройки.Вставить("ОписаниеРезультата", "Параметр спецоператора ""признак обновления"" установлен в ""ложь""");
				КонецЕсли;
				
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическаяНастройкаУчетнойЗаписиПослеОбработкиОбновленияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	РезультатОбновления = Результат.РезультатОбновления;
	ПараметрыОбработатьОбновление = Результат.ПараметрыФункции;
	УчетнаяЗаписьСсылка = ДополнительныеПараметры.УчетнаяЗаписьСсылка;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ТекстОшибокДляМастераПодключенияК1СОтчетности = ПараметрыОбработатьОбновление.ТекстОшибокДляМастераПодключенияК1СОтчетности;
	СтруктураДляИнформацииОСервереОнлайнПроверки  = ПараметрыОбработатьОбновление.СтруктураДляИнформацииОСервереОнлайнПроверки;
	
	Если РезультатОбновления = Истина Тогда
		Если СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи = Неопределено Тогда
			СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи = Новый Соответствие;
		КонецЕсли;
		СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи.Вставить(УчетнаяЗаписьСсылка, ТекущаяДатаНаСервере());
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок, СтруктураДляИнформацииОСервереОнлайнПроверки", РезультатОбновления, ТекстОшибокДляМастераПодключенияК1СОтчетности, СтруктураДляИнформацииОСервереОнлайнПроверки);
		ОписаниеИсходнойОшибки = Неопределено;
		
		Если Результат.Свойство("ОшибкаПолученияДанныхССервера", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось получить настройки с сервера спецоператора" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаСозданияКаталога", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось создать временный каталог для сохранения настройки с сервера спецоператора" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаПоискаСертификатовПередУстановкой", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось выполнить поиск сертификатов перед установкой в системное хранилище для завершения автонастройки" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаЗаписиСертификатаВФайл", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось записать сертификат в файл перед установкой" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаУстановкиЛичногоСертификата", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось установить личный сертификат" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаОпределенияПутиКонтейнера", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось определить путь к контейнеру закрытого ключа" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаУстановкиСертификатаВХранилище", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось установить сертификат в системное хранилище" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаПропущенаРасшифровка", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не выполнялась расшифровка зашифрованной части файла автонастройки" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаВыполненияРасшифровки", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось выполнить расшифровку зашифрованной части файла автонастройки" + Подробнее);
			
		ИначеЕсли Результат.Свойство("ОшибкаАктивированияУчетнойЗаписиОблачнойПодписи", ОписаниеИсходнойОшибки) Тогда
			Подробнее = ?(ОписаниеИсходнойОшибки = Неопределено, "", ". " + ОписаниеИсходнойОшибки);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось активировать учетную запись электронной подписи" + Подробнее);
			
		ИначеЕсли РезультатОбновления <> Истина Тогда
			РезультатНастройки.Вставить("ОписаниеРезультата", "Не удалось выявить причину ошибки выполнения автонастройки");
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическаяНастройкаУчетнойЗаписиЗавершение(ТаблицаИсправлений, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ТекстОшибокДляМастераПодключенияК1СОтчетности = ДополнительныеПараметры.ТекстОшибокДляМастераПодключенияК1СОтчетности;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	УведомлятьТолькоОбОшибках = ДополнительныеПараметры.УведомлятьТолькоОбОшибках;
	УчетнаяЗаписьСсылка = ДополнительныеПараметры.УчетнаяЗаписьСсылка;
	Организация = ДополнительныеПараметры.Организация;
	
	Если ТаблицаИсправлений = Неопределено ИЛИ ТаблицаИсправлений.Количество() = 0 Тогда
		Если ВыполняемоеОповещение <> Неопределено Тогда
			РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок", Неопределено, ТекстОшибокДляМастераПодключенияК1СОтчетности);
			РезультатНастройки.Вставить("ОписаниеРезультата", "Нет изменений настроек учетной записи");
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, УчетнаяЗаписьСсылка, ТекстОшибокДляМастераПодключенияК1СОтчетности", ВыполняемоеОповещение, УчетнаяЗаписьСсылка, ТекстОшибокДляМастераПодключенияК1СОтчетности);
	ОписаниеОповещения = Новый ОписаниеОповещения("АвтоматическаяНастройкаУчетнойЗаписиПослеПримененияИзмененийЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ПрименитьИзмененныеНастройки(
		УчетнаяЗапись,
		ТаблицаИсправлений,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		Организация);
		
КонецПроцедуры

&НаКлиенте
Процедура АвтоматическаяНастройкаУчетнойЗаписиПослеПримененияИзмененийЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	УчетнаяЗаписьСсылка = ДополнительныеПараметры.УчетнаяЗаписьСсылка;
	ТекстОшибокДляМастераПодключенияК1СОтчетности = ДополнительныеПараметры.ТекстОшибокДляМастераПодключенияК1СОтчетности;
	
	ТаблицаИсправлений = Результат.ТаблицаИсправлений;
	РезультатПрименения = Результат.РезультатПрименения;
	
	Если РезультатПрименения = Истина Тогда
		Если СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи = Неопределено Тогда
			СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи = Новый Соответствие;
		КонецЕсли;
		СоответствиеДатаВремяАвтонастройкиПоУчетнойЗаписи.Вставить(УчетнаяЗаписьСсылка, ТекущаяДатаНаСервере());
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		РезультатНастройки = Новый Структура("РезультатНастройки, ТекстОшибок", РезультатПрименения, ТекстОшибокДляМастераПодключенияК1СОтчетности);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатНастройки);
	КонецЕсли;
		
	
КонецПроцедуры

//&НаСервереБезКонтекста
&НаКлиенте
Процедура ПолучитьНастройкиУчетнойЗаписиССервера(Знач УчетнаяЗапись,
											Знач Организация,
											УведомлятьТолькоОбОшибках,
											ИгнорироватьРежимАвтонастройки,
											ТихийРежим,
											ЗапрашиватьПараметрыОрганизацииПриНеобходимости,
											ВыполняемоеОповещение)
	
	// проверяем, включена ли автонастройка
	Если НЕ ИгнорироватьРежимАвтонастройки Тогда
		
		// учетной записью может оказаться организация
		Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
			Если УчетнаяЗапись.ВидОбменаСКонтролирующимиОрганами <> СтруктураСсылочныхДанных.Перечисления_ВидыОбменаСКонтролирующимиОрганами_ОбменВУниверсальномФормате Тогда
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
				Возврат;
			КонецЕсли;
			ссылкаУчетнаяЗапись = Организация.УчетнаяЗаписьОбмена;
		Иначе
			ссылкаУчетнаяЗапись = УчетнаяЗапись;
		КонецЕсли;
		
		// получаем настройки учетной записи
		// если настройки недоступны или учетка для обмена напрямую, то прервемся
		НастройкиУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(ссылкаУчетнаяЗапись);
		Если НастройкиУчетнойЗаписи = Неопределено ИЛИ НастройкиУчетнойЗаписи.ОбменНапрямую Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		
		// если включено принудительное игнорирование режима автонастройки
		// или отключена автонастройка в свойствах учетной записи, то прервемся
		Если НастройкиУчетнойЗаписи.РежимАвтонастройки = СтруктураСсылочныхДанных.Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_Отключена
		ИЛИ НастройкиУчетнойЗаписи.РежимАвтонастройки = СтруктураСсылочныхДанных.Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_ПустаяСсылка Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	// получаем файл настроек с сервера или из кэша
	ПараметрыФайлаНастроек = ПолучитьФайлНастроекУчетнойЗаписиССервераРОКИСУчетомКэша(УчетнаяЗапись, Организация, УведомлятьТолькоОбОшибках, ИгнорироватьРежимАвтонастройки, ТихийРежим, ЗапрашиватьПараметрыОрганизацииПриНеобходимости, УникальныйИдентификатор);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаНастроек) Тогда // если Неопределено, то файл недоступен
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлНастроек = ПараметрыФайлаНастроек.Файл;
	
	ДатаОбновленияФайлаНастроекНаСервере = ПараметрыФайлаНастроек.Дата;
	ФайлНастроекРасшифрован = ПараметрыФайлаНастроек.Расшифрован;
	ИмяРесурсаСНастройками = ПараметрыФайлаНастроек.ИмяРесурса;
	
	// если файл настроек зашифрован, то расшифровываем
	Если НЕ ФайлНастроекРасшифрован Тогда
		
		// объявляем вспомогательные переменные
		Сообщения = СоздатьТаблицуСообщений(Истина);
		
		ПризнакОшибкиВФайлеНастроек = Ложь;
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 					ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", 							УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ФайлНастроекРасшифрован", 				ФайлНастроекРасшифрован);
		ДополнительныеПараметры.Вставить("ИмяРесурсаСНастройками", 					ИмяРесурсаСНастройками);
		ДополнительныеПараметры.Вставить("ФайлНастроек", 							ФайлНастроек);
		ДополнительныеПараметры.Вставить("ДатаОбновленияФайлаНастроекНаСервере", 	ДатаОбновленияФайлаНастроекНаСервере);
		ДополнительныеПараметры.Вставить("Сообщения", 								Сообщения);
		ДополнительныеПараметры.Вставить("УведомлятьТолькоОбОшибках", 				УведомлятьТолькоОбОшибках);
		ДополнительныеПараметры.Вставить("Организация", 							Организация);
		Оповещение = Новый ОписаниеОповещения(
			"ПолучитьНастройкиУчетнойЗаписиССервераПослеРасшифрованияТранспортногоКонтейнераРОКИЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
			
		Сертификат = Новый Структура;
		КонтекстМоделиХраненияКлюча(Ложь, Сертификат);
		КриптографияЭДКОКлиент.РасшифроватьPKCS7(Оповещение, ФайлНастроек, , Сертификат, Ложь);
		Возврат;
		
	КонецЕсли;
	
	// формируем таблицу исправлений на основе расшифрованного файла настроек на диске
	СформироватьТаблицуИсправленийНаОсновеФайлаНастроек(
		ФайлНастроек,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		УчетнаяЗапись,
		Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьНастройкиУчетнойЗаписиССервераПослеРасшифрованияТранспортногоКонтейнераРОКИЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 					= ДополнительныеПараметры.ВыполняемоеОповещение;
	УчетнаяЗапись 							= ДополнительныеПараметры.УчетнаяЗапись;
	ФайлНастроекРасшифрован 				= ДополнительныеПараметры.ФайлНастроекРасшифрован;
	ИмяРесурсаСНастройками 					= ДополнительныеПараметры.ИмяРесурсаСНастройками;
	ФайлНастроек 							= ДополнительныеПараметры.ФайлНастроек;
	ДатаОбновленияФайлаНастроекНаСервере 	= ДополнительныеПараметры.ДатаОбновленияФайлаНастроекНаСервере;
	Сообщения 								= ДополнительныеПараметры.Сообщения;
	УведомлятьТолькоОбОшибках 				= ДополнительныеПараметры.УведомлятьТолькоОбОшибках;
	Организация 							= ДополнительныеПараметры.Организация;
	
	Если НЕ Результат.Выполнено Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Не удалось расшифровать контейнер с настройками!'"));
	КонецЕсли;
	
	ПризнакОшибкиВФайлеНастроек = НЕ Результат.Выполнено;
	РасшифрованныйФайлНастроек = ?(Результат.Выполнено, Результат.РасшифрованныйФайл, Неопределено);
	
	Если НЕ ФайлНастроекРасшифрован Тогда
		
		Если РасшифрованныйФайлНастроек = Неопределено Тогда
			// кэшируем файл настроек, если в нем при расшфровке не была обнаружена ошибка
			Если НЕ ПризнакОшибкиВФайлеНастроек Тогда
				СохранитьВКэшеФайлНастроек(УчетнаяЗапись, ИмяРесурсаСНастройками, РасшифрованныйФайлНастроек, Истина, ДатаОбновленияФайлаНастроекНаСервере);
			КонецЕсли;
			ВывестиСообщения(УчетнаяЗапись, Сообщения, УведомлятьТолькоОбОшибках);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		Иначе
			ФайлНастроек = РасшифрованныйФайлНастроек;
			СохранитьВКэшеФайлНастроек(УчетнаяЗапись, ИмяРесурсаСНастройками, ФайлНастроек, Истина, ДатаОбновленияФайлаНастроекНаСервере);
		КонецЕсли;
		
	КонецЕсли;
	
	// формируем таблицу исправлений на основе расшифрованного файла настроек на диске
	СформироватьТаблицуИсправленийНаОсновеФайлаНастроек(
		ФайлНастроек,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		УчетнаяЗапись,
		Организация);
	
КонецПроцедуры

&НаКлиенте
Функция ОпределитьОрганизациюПоУчетнойЗаписиКлиент(УчетнаяЗапись) Экспорт
	
	Возврат ОпределитьОрганизациюПоУчетнойЗаписи(УчетнаяЗапись);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьОрганизациюПоУчетнойЗаписи(УчетнаяЗапись)
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		Возврат УчетнаяЗапись;
	КонецЕсли;
	
	УчетнаяЗаписьСсылка = ПреобразоватьВСсылкуНаУчетнуюЗапись(УчетнаяЗапись);
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	Организации.Ссылка
						  |ИЗ
						  |	Справочник.Организации КАК Организации
						  |ГДЕ
						  |	Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена
						  |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
						  |	И Организации.ГоловнаяОрганизация = &ПустаяГоловнаяОрганизация
						  |
						  |ОБЪЕДИНИТЬ ВСЕ
						  |
						  |ВЫБРАТЬ ПЕРВЫЕ 1
						  |	Организации.Ссылка
						  |ИЗ
						  |	Справочник.Организации КАК Организации
						  |ГДЕ
						  |	Организации.УчетнаяЗаписьОбмена = &УчетнаяЗаписьОбмена
						  |	И Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
						  |	И Организации.ГоловнаяОрганизация <> &ПустаяГоловнаяОрганизация");
	Запрос.УстановитьПараметр("УчетнаяЗаписьОбмена", УчетнаяЗаписьСсылка);
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("ПустаяГоловнаяОрганизация", Справочники.Организации.ПустаяСсылка());
	Выборка = Запрос.Выполнить().Выбрать();
	Возврат ?(Выборка.Следующий(), Выборка.Ссылка, Неопределено);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьОрганизациюПоИННиКППНаСервере(ИНН, КПП, НаходитьПомеченныеНаУдаление = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьОрганизациюПоИННиКПП(ИНН, КПП, НаходитьПомеченныеНаУдаление);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПараметрыСоединенияССервером(
		УчетнаяЗапись,
		Организация,
		ЗапрашиватьПараметрыОрганизацииПриНеобходимости,
		Сообщения)
	
	Если УчетнаяЗапись = Неопределено ИЛИ НЕ УчетнаяЗапись.ЯвляетсяУчетнойЗаписьюУполномоченногоПредставителя Тогда
		Если НЕ ЗначениеЗаполнено(Организация) Тогда
			Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота") Тогда
				Организация = ОпределитьОрганизациюПоУчетнойЗаписи(УчетнаяЗапись);
			КонецЕсли;
			Если НЕ ЗначениеЗаполнено(Организация) Тогда
				Если НЕ ЗапрашиватьПараметрыОрганизацииПриНеобходимости Тогда
					ДобавитьСообщение(Сообщения, НСтр("ru = 'Не удалось определить ИНН\КПП налогоплательщика!'"));
					Возврат Неопределено;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		Если ТипЗнч(Организация) = Тип("СправочникСсылка.Организации") Тогда
			Если НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация) Тогда
				Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "ИННФЛ");
				ИНН = СокрЛП(Сведения.ИННФЛ);
				КПП = "";
			Иначе
				Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "ИННЮЛ, КППЮЛ");
				ИНН = СокрЛП(Сведения.ИННЮЛ);
				КПП = СокрЛП(Сведения.КППЮЛ);
			КонецЕсли;
		Иначе
			ИНН = Организация["ИНН"];
			КПП = Организация["КПП"];
		КонецЕсли;
		ПредставлениеОрганизация = ?(ТипЗнч(Организация) = Тип("СправочникСсылка.Организации"), "" + Организация, "");
	Иначе
		ИНН = СокрЛП(УчетнаяЗапись.ИННУполномоченногоПредставителя);
		КПП = СокрЛП(УчетнаяЗапись.КППУполномоченногоПредставителя);
		ПредставлениеОрганизация = СокрЛП(УчетнаяЗапись.ПолноеНаименованиеУполномоченногоПредставителя);
	КонецЕсли;
		
	Если СтрДлина(ИНН) <> 10 И СтрДлина(ИНН) <> 12 Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректно задан ИНН организации '") + ПредставлениеОрганизация + "!");
		Возврат Неопределено;
	ИначеЕсли СтрДлина(ИНН) = 10 И СтрДлина(КПП) <> 9 Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректно задан КПП организации '") + ПредставлениеОрганизация + "!");
		Возврат Неопределено;
	КонецЕсли;
	
	Если СтрДлина(ИНН) = 12 Тогда
		ИННКПП = ИНН;
	Иначе
		ИННКПП = ИНН + КПП;
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ИмяСервераРОКИ", "cert01.taxcom.ru");
	Результат.Вставить("ИмяРесурсаДТ", "/config/sprinter/" + ИННКПП + "/dt.txt");
	Результат.Вставить("ИмяРесурсаСНастройками", "/config/sprinter/" + ИННКПП + "/config.xml.enc");
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьНастройкиУчетнойЗаписиИзИБ(Знач УчетнаяЗапись)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	
КонецФункции

&НаКлиенте
Процедура ПрименитьИзмененныеНастройки(
		УчетнаяЗапись,
		ТаблицаИсправлений,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		Организация = Неопределено) Экспорт
	
	Сообщения = СоздатьТаблицуСообщений(Истина);
	
	СледуетНастроить = Истина;
	ПрименитьИзмененныеНастройки_Настройка(
		СледуетНастроить,
		ТаблицаИсправлений,
		УчетнаяЗапись,
		Сообщения,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ПрименитьИзмененныеНастройки_Настройка(
		СледуетНастроить,
		ТаблицаИсправлений,
		УчетнаяЗапись,
		Сообщения,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		Организация = Неопределено)
	
	Если СледуетНастроить <> Истина Тогда
		Результат = Новый Структура("РезультатПрименения, ТаблицаИсправлений, УчетнаяЗапись", Ложь, ТаблицаИсправлений, УчетнаяЗапись);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// настраиваем
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("УведомлятьТолькоОбОшибках", УведомлятьТолькоОбОшибках);
	ДополнительныеПараметры.Вставить("ТаблицаИсправлений", ТаблицаИсправлений);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ПрименитьИзмененныеНастройки_НастройкаПослеНастройкиПоРОКИ", ЭтотОбъект, ДополнительныеПараметры);
	НастроитьПоРОКИ(ОписаниеОповещения, УчетнаяЗапись, ТаблицаИсправлений, Сообщения, Организация);
		
КонецПроцедуры

&НаКлиенте
Процедура ПрименитьИзмененныеНастройки_НастройкаПослеНастройкиПоРОКИ(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	УведомлятьТолькоОбОшибках 	= ДополнительныеПараметры.УведомлятьТолькоОбОшибках;
	ТаблицаИсправлений          = ДополнительныеПараметры.ТаблицаИсправлений;
	
	УчетнаяЗапись               = Результат.УчетнаяЗапись;
	
	Если НЕ Результат.Выполнено Тогда
		ВывестиСообщения(УчетнаяЗапись, Результат.Сообщения, УведомлятьТолькоОбОшибках);
		РезультатСтруктура = Новый Структура("РезультатПрименения, ТаблицаИсправлений, УчетнаяЗапись", Ложь, ТаблицаИсправлений, УчетнаяЗапись);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатСтруктура);
		Возврат;
	КонецЕсли;
	
	ВывестиСообщения(УчетнаяЗапись, Результат.Сообщения, УведомлятьТолькоОбОшибках);
	РезультатСтруктура = Новый Структура("РезультатПрименения, ТаблицаИсправлений, УчетнаяЗапись", Истина, ТаблицаИсправлений, УчетнаяЗапись);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатСтруктура);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ВыполнитьНастройкиПоРОКИ(
		УчетнаяЗапись,
		ТаблицаНеобходимыхИзменений,
		Сообщения,
		СоответствиеКомандРегистрацииСертификатовХранилищам,
		СоответствиеКомандРегистрацииСертификатовПредставлениям,
		СоответствиеИдентификаторовХранилищПредставлениям,
		Организация = Неопределено)
	
	НачатьТранзакцию();
	ТранзакцияПустая = Истина;
	
	// получаем объект - учетную запись
	Если УчетнаяЗапись = Неопределено Тогда
	    УчетнаяЗаписьОбъект = Справочники.УчетныеЗаписиДокументооборота.СоздатьЭлемент();
		УчетнаяЗаписьОбъект.СпецоператорСвязи = Перечисления.СпецоператорыСвязи.Такском;
		УчетнаяЗаписьОбъект.ОбменНапрямую = Ложь;
		УчетнаяЗаписьОбъект.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ВключенаВТихомРежиме;
		УчетнаяЗаписьОбъект.ИспользоватьСервисОнлайнПроверкиОтчетов = Истина;
		УчетнаяЗаписьОбъект.ЗаписатьПользователяПоУмолчанию = Истина;
		УчетнаяЗаписьОбъект.НеФормироватьПодтвержденияАвтоматически = Ложь;
		УчетнаяЗаписьОбъект.ИдентификаторСистемыОтправителяПФР = "taxcom";
		УчетнаяЗаписьОбъект.ЭтоЗаписьНового = Истина;
		УчетнаяЗаписьОбъект.ДатаПодключения = ТекущаяДатаСеанса();
	ИначеЕсли ТипЗнч(УчетнаяЗапись) = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота") Тогда
		УчетнаяЗаписьОбъект = УчетнаяЗапись;
	Иначе
		УчетнаяЗаписьОбъект = УчетнаяЗапись.ПолучитьОбъект();
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка сервера документооборота
	
	Если ЗначениеЗаполнено(УчетнаяЗаписьОбъект.СерверДокументооборота) 
		И УчетнаяЗаписьОбъект.СерверДокументооборота.ПолучитьОбъект() <> Неопределено Тогда
		СерверДокументооборотаОбъект = УчетнаяЗаписьОбъект.СерверДокументооборота.ПолучитьОбъект();
	Иначе
		СерверДокументооборотаОбъект = Справочники.СерверыДокументооборота.СоздатьЭлемент();
	КонецЕсли;
	
	// изменяем адреса сервера документооборота
	КомандаИзменитьАдресПочтовогоСервераФНС  = НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьАдресПочтовогоСервера", "Настройка");
	КомандаИзменитьАдресПочтовогоСервераПФР  = НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьАдресПочтовогоСервераПФР", "Настройка");
	КомандаИзменитьАдресПочтовогоСервераФСГС = НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьАдресПочтовогоСервераФСГС", "Настройка");
	
	Если КомандаИзменитьАдресПочтовогоСервераФНС <> Неопределено ИЛИ КомандаИзменитьАдресПочтовогоСервераПФР <> Неопределено ИЛИ КомандаИзменитьАдресПочтовогоСервераФСГС <> Неопределено Тогда
		
		// меняем адрес для обмена с ФНС при необходимости
		Если КомандаИзменитьАдресПочтовогоСервераФНС <> Неопределено Тогда
			СерверДокументооборотаОбъект.АдресЭлектроннойПочтыФНС = нрег(КомандаИзменитьАдресПочтовогоСервераФНС.НовоеЗначение);
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен адрес электронной почты сервера документооборота для обмена с ФНС.'"), Ложь);
		КонецЕсли;
		
		// меняем адрес для обмена с ПФР при необходимости
		Если КомандаИзменитьАдресПочтовогоСервераПФР <> Неопределено Тогда
			СерверДокументооборотаОбъект.АдресЭлектроннойПочтыПФР = нрег(КомандаИзменитьАдресПочтовогоСервераПФР.НовоеЗначение);
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен адрес электронной почты сервера документооборота для обмена с СФР (бывш. ПФР).'"), Ложь);
		КонецЕсли;
		
		// меняем адрес для обмена с Росстатом при необходимости
		Если КомандаИзменитьАдресПочтовогоСервераФСГС <> Неопределено Тогда
			СерверДокументооборотаОбъект.АдресЭлектроннойПочтыФСГС = нрег(КомандаИзменитьАдресПочтовогоСервераФСГС.НовоеЗначение);
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен адрес электронной почты сервера документооборота для обмена с ФСГС.'"), Ложь);
		КонецЕсли;
		
	КонецЕсли;
	
	// изменяем сертификат сервера документооборота
	КомандаИзменитьСертификатПочтовогоСервера = НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатПочтовогоСервера", "Настройка");
	Если КомандаИзменитьСертификатПочтовогоСервера <> Неопределено Тогда
		СерверДокументооборотаОбъект.Сертификат = нрег(КомандаИзменитьСертификатПочтовогоСервера.НовоеЗначение.Отпечаток);
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен сертификат сервера документооборота.'"), Ложь);
	КонецЕсли;
	
	// записываем изменения в свойствах сервера документооборота
	Если СерверДокументооборотаОбъект.Модифицированность() Тогда
		Попытка
			СерверДокументооборотаОбъект.Записать();
			ТранзакцияПустая = Ложь;
		Исключение
			ОтменитьТранзакцию();
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Ошибка обновления настроек учетной записи:'") + Символы.ПС + ИнформацияОбОшибке().Описание);
			Возврат Ложь;
		КонецПопытки;
	КонецЕсли;
	
	// при необходимости, изменяем ссылку на сервер документооборота у учетной записи
	Если УчетнаяЗаписьОбъект.СерверДокументооборота <> СерверДокументооборотаОбъект.Ссылка Тогда
		УчетнаяЗаписьОбъект.СерверДокументооборота = СерверДокументооборотаОбъект.Ссылка;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'В свойствах учетной записи сервер документооборота изменен на вновь созданный.'"), Ложь);
	КонецЕсли;
	
	// Настройка сервера документооборота
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка налоговых органов
	
	// регистрируем налоговые органы
	КомандыЗарегистрироватьИФНС = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ЗарегистрироватьИФНС");
	Для Каждого СтрКоманда Из КомандыЗарегистрироватьИФНС Цикл
		
		// параметры команды
		КодИФНС = СокрЛП(СтрКоманда.НовоеЗначение);
		НаименованиеИФНС = СтрКоманда.ДопПараметр;
		
		// создаем новый элемент справочника, присваиваем код и наименование, записываем
		НалоговыйОрганОбъект = Справочники.НалоговыеОрганы.СоздатьЭлемент();
		НалоговыйОрганОбъект.Код = КодИФНС;
		НалоговыйОрганОбъект.Наименование = НаименованиеИФНС;
		НалоговыйОрганОбъект.Записать();
		
		ТранзакцияПустая = Ложь;
		
		// генерируем сообщение
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Создан новый элемент справочника ""Налоговые органы"" с кодом ""%1"" и наименованием ""%2"".'"),
			КодИФНС,
			НаименованиеИФНС),
			Ложь);
	КонецЦикла;
	
	// меняем сертификаты
	КомандыИзменитьСертификатИФНС = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ИзменитьСертификатИФНС");
	Для Каждого СтрКоманда Из КомандыИзменитьСертификатИФНС Цикл
		
		// параметры команды
		СертификатыИФНС = СтрКоманда.НовоеЗначение;
		КодИФНС = СокрЛП(СтрКоманда.ДопПараметр);
		
		// ищем налоговый орган в справочнике по коду и меняем сертификат
		СсылкаНалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодИФНС);
		Если ЗначениеЗаполнено(СсылкаНалоговыйОрган) Тогда
			
			ОбъектНалоговыйОрган = СсылкаНалоговыйОрган.ПолучитьОбъект();
			
			ОбъектНалоговыйОрган.Сертификаты.Очистить();
			Для Каждого СертификатИФНС Из СертификатыИФНС Цикл
				НовСтрСертификат = ОбъектНалоговыйОрган.Сертификаты.Добавить();
				НовСтрСертификат.Сертификат = СертификатИФНС.Отпечаток;
			КонецЦикла;
			
			ОбъектНалоговыйОрган.Записать();
			
			ТранзакцияПустая = Ложь;
			
		КонецЕсли;
		
		// генерируем сообщение
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Изменен сертификат налогового органа с кодом ""%1"".'"), КодИФНС),
			Ложь);
		
	КонецЦикла;
	
	// Настройка налоговых органов
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка основного налогового органа
	
	КомандаИзменитьОсновнуюИФНС = НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьОсновнуюИФНС", "Настройка");
	Если КомандаИзменитьОсновнуюИФНС <> Неопределено Тогда
		КодОсновногоНалоговогоОргана = СокрЛП(КомандаИзменитьОсновнуюИФНС.НовоеЗначение.Код);
		СсылкаНаОсновнойНалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодОсновногоНалоговогоОргана);
		УчетнаяЗаписьОбъект.НалоговыйОрган = СсылкаНаОсновнойНалоговыйОрган;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'В свойствах учетной записи изменен налоговый орган.'"), Ложь);
	КонецЕсли;
	
	// Настройка основного налогового органа
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка отделений ПФР
	
	// регистрируем отделения ПФР
	КомандыЗарегистрироватьОрганПФР = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ЗарегистрироватьОрганПФР");
	Для Каждого СтрКоманда Из КомандыЗарегистрироватьОрганПФР Цикл
		
		// параметры команды
		КодОрганаПФР = СокрЛП(СтрКоманда.НовоеЗначение);
		НаименованиеОрганаПФР = СтрКоманда.ДопПараметр;
		
		// создаем новый элемент справочника, присваиваем код и наименование, записываем
		ОрганПФРОбъект = Справочники.ОрганыПФР.СоздатьЭлемент();
		ОрганПФРОбъект.Код = КодОрганаПФР;
		ОрганПФРОбъект.Наименование = НаименованиеОрганаПФР;
		ОрганПФРОбъект.Записать();
		
		ТранзакцияПустая = Ложь;
		
		// генерируем сообщение
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Создан новый элемент справочника ""Органы СФР (бывш. ПФР)"" с кодом ""%1"" и наименованием ""%2"".'"),
			КодОрганаПФР,
			НаименованиеОрганаПФР,
			Ложь));
	КонецЦикла;
	
	// меняем сертификаты
	КомандыИзменитьСертификатОрганаПФР = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ИзменитьСертификатОрганаПФР");
	Для Каждого СтрКоманда Из КомандыИзменитьСертификатОрганаПФР Цикл
		
		// параметры команды
		СертификатыОрганаПФР = СтрКоманда.НовоеЗначение;
		КодОрганаПФР = СокрЛП(СтрКоманда.ДопПараметр);
		
		// ищем орган ПФР в справочнике по коду и меняем сертификат
		СсылкаОрганПФР = Справочники.ОрганыПФР.НайтиПоКоду(КодОрганаПФР);
		Если ЗначениеЗаполнено(СсылкаОрганПФР) Тогда
			
			ОбъектОрганПФР = СсылкаОрганПФР.ПолучитьОбъект();
			
			ОбъектОрганПФР.Сертификаты.Очистить();
			Для Каждого СертификатОрганаПФР Из СертификатыОрганаПФР Цикл
				НовСтрСертификат = ОбъектОрганПФР.Сертификаты.Добавить();
				НовСтрСертификат.Сертификат = СертификатОрганаПФР.Отпечаток;
			КонецЦикла;
			
			ОбъектОрганПФР.Записать();
			
			ТранзакцияПустая = Ложь;
			
		КонецЕсли;
		
		// генерируем сообщение
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Изменен сертификат органа СФР (бывш. ПФР) с кодом ""%1"".'"), КодОрганаПФР),
			Ложь);
	
	КонецЦикла;
	
	// меняем сертификаты
	КомандыИзменитьСертификатОрганаПФРЭДОК = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ИзменитьСертификатОрганаПФРЭДОК");
	ИндексКоманды = 0;
	Для каждого СтрКоманда Из КомандыИзменитьСертификатОрганаПФРЭДОК Цикл
		
		// параметры команды
		СертификатыОрганаПФР = СтрКоманда.НовоеЗначение;
		КодОрганаПФР = СокрЛП(СтрКоманда.ДопПараметр);
		
		// ищем орган ПФР в справочнике по коду и меняем сертификат
		СсылкаОрганПФР = Справочники.ОрганыПФР.НайтиПоКоду(КодОрганаПФР);
		Если ЗначениеЗаполнено(СсылкаОрганПФР) Тогда
			
			ОбъектОрганПФР = СсылкаОрганПФР.ПолучитьОбъект();
			
			ОбъектОрганПФР.СертификатыЭДОК.Очистить();
			Для Каждого СертификатОрганаПФР Из СертификатыОрганаПФР Цикл
				НовСтрСертификат = ОбъектОрганПФР.СертификатыЭДОК.Добавить();
				НовСтрСертификат.Сертификат = СертификатОрганаПФР.Отпечаток;
			КонецЦикла;
			
			ОбъектОрганПФР.Записать();
			
			ТранзакцияПустая = Ложь;
			
		КонецЕсли;
		
		// генерируем сообщение
		Если ИндексКоманды = 0 Тогда
			ДобавитьСообщение(Сообщения,
				НСтр("ru = 'Изменен сертификат СФР (бывш. ПФР) для ЭДОК.'"),
				Ложь);
		КонецЕсли;
		ИндексКоманды = ИндексКоманды + 1;
		
	КонецЦикла;
	
	// меняем сертификаты
	КомандыИзменитьСертификатОрганаПФРЕЦП =
		НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ИзменитьСертификатОрганаПФРЕЦП");
	ИндексКоманды = 0;
	Для каждого СтрКоманда Из КомандыИзменитьСертификатОрганаПФРЕЦП Цикл
		
		// параметры команды
		СертификатыОрганаПФР = СтрКоманда.НовоеЗначение;
		КодОрганаПФР = СокрЛП(СтрКоманда.ДопПараметр);
		
		// ищем орган ПФР в справочнике по коду и меняем сертификат
		СсылкаОрганПФР = Справочники.ОрганыПФР.НайтиПоКоду(КодОрганаПФР);
		Если ЗначениеЗаполнено(СсылкаОрганПФР) Тогда
			
			ОбъектОрганПФР = СсылкаОрганПФР.ПолучитьОбъект();
			
			ОбъектОрганПФР.СертификатыЕЦП.Очистить();
			Для Каждого СертификатОрганаПФР Из СертификатыОрганаПФР Цикл
				НовСтрСертификат = ОбъектОрганПФР.СертификатыЕЦП.Добавить();
				НовСтрСертификат.Сертификат = СертификатОрганаПФР.Отпечаток;
			КонецЦикла;
			
			ОбъектОрганПФР.Записать();
			
			ТранзакцияПустая = Ложь;
			
		КонецЕсли;
		
		// генерируем сообщение
		Если ИндексКоманды = 0 Тогда
			ДобавитьСообщение(Сообщения,
				НСтр("ru = 'Изменен сертификат СФР (бывш. ПФР) для ЕЦП.'"),
				Ложь);
		КонецЕсли;
		ИндексКоманды = ИндексКоманды + 1;
		
	КонецЦикла;
	
	// устанавливаем признаки документооборота по учетке через ФНС и ПФР
	
	// Настройка отделений ПФР
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка территориальных отделений ФСГС
	
	// регистрируем отделения ФСГС
	КомандыЗарегистрироватьОрганФСГС = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ЗарегистрироватьОрганФСГС");
	Для Каждого СтрКоманда Из КомандыЗарегистрироватьОрганФСГС Цикл
		
		// параметры команды
		КодОрганаФСГС = СокрЛП(СтрКоманда.НовоеЗначение);
		НаименованиеОрганаФСГС = СтрКоманда.ДопПараметр;
		
		// создаем новый элемент справочника, присваиваем код и наименование, записываем
		ОрганФСГСОбъект = Справочники.ОрганыФСГС.СоздатьЭлемент();
		ОрганФСГСОбъект.Код = КодОрганаФСГС;
		ОрганФСГСОбъект.Наименование = НаименованиеОрганаФСГС;
		ОрганФСГСОбъект.Записать();
		
		ТранзакцияПустая = Ложь;
		
		// генерируем сообщение
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Создан новый элемент справочника ""Органы ФСГС"" с кодом ""%1"" и наименованием ""%2"".'"),
			КодОрганаФСГС,
			НаименованиеОрганаФСГС,
			Ложь));
	КонецЦикла;
	
	// меняем сертификаты
	КомандыИзменитьСертификатОрганаФСГС = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", "ИзменитьСертификатОрганаФСГС");
	Для Каждого СтрКоманда Из КомандыИзменитьСертификатОрганаФСГС Цикл
		
		// параметры команды
		СертификатыОрганаФСГС = СтрКоманда.НовоеЗначение;
		КодОрганаФСГС = СокрЛП(СтрКоманда.ДопПараметр);
		
		// ищем орган ФСГС в справочнике по коду и меняем сертификат
		СсылкаОрганФСГС = Справочники.ОрганыФСГС.НайтиПоКоду(КодОрганаФСГС);
		Если ЗначениеЗаполнено(СсылкаОрганФСГС) Тогда
			
			ОбъектОрганФСГС = СсылкаОрганФСГС.ПолучитьОбъект();
			
			ОбъектОрганФСГС.Сертификаты.Очистить();
			Для Каждого СертификатОрганаФСГС Из СертификатыОрганаФСГС Цикл
				НовСтрСертификат = ОбъектОрганФСГС.Сертификаты.Добавить();
				НовСтрСертификат.Сертификат = СертификатОрганаФСГС.Отпечаток;
			КонецЦикла;
			
			ОбъектОрганФСГС.Записать();
			
			ТранзакцияПустая = Ложь;
			
		КонецЕсли;
		
		// генерируем сообщение
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Изменен сертификат территориального органа ФСГС с кодом ""%1"".'"), КодОрганаФСГС),
			Ложь);
	
	КонецЦикла;
	
	// Настройка отделений ФСГС
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка учетной записи непосредственно
	
	// настраиваем учетную запись
	КомандаПредназначенаДляДокументооборотаСФНС		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьПризнакДокументооборотаСФНС",  "Настройка");
	КомандаПредназначенаДляДокументооборотаСПФР		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьПризнакДокументооборотаСПФР",  "Настройка");
	КомандаПредназначенаДляДокументооборотаСФСГС	= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьПризнакДокументооборотаСФСГС", "Настройка");
	
	КомандаИзменитьИспользуетсяЕЦППФР				= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьИспользуетсяЕЦППФР", "Настройка");
	КомандаИзменитьТипыДокументовЕЦППФР				= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьТипыДокументовЕЦППФР", "Настройка");
	КомандаИзменитьОператорРегНомерПФР				= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьОператорРегНомерПФР", "Настройка");
	КомандаИзменитьОператорНаименованиеПолное		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьОператорНаименованиеПолное", "Настройка");
	КомандаИзменитьОператорНаименованиеКраткое		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьОператорНаименованиеКраткое", "Настройка");
	КомандаИзменитьОператорИНН						= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьОператорИНН", "Настройка");
	КомандаИзменитьОператорКПП						= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьОператорКПП", "Настройка");
	КомандаИзменитьСерверPOP3						= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСерверPOP3", "Настройка");
	КомандаИзменитьСерверPOP3						= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСерверPOP3", "Настройка");
	КомандаИзменитьПортPOP3							= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьПортPOP3", "Настройка");
	КомандаИзменитьИмяПользователяPOP3				= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьИмяПользователяPOP3", "Настройка");
	КомандаИзменитьАдресЭлектроннойПочты			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьАдресЭлектроннойПочты", "Настройка");
	КомандаИзменитьИдентификаторАбонента			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьИдентификаторАбонента", "Настройка");
	КомандаИзменитьИдентификаторСпецоператора		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьИдентификаторСпецоператора", "Настройка");
	КомандаИзменитьПарольPOP3						= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьПарольPOP3", "Настройка");
	КомандаИзменитьСерверSMTP						= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСерверSMTP", "Настройка");
	КомандаИзменитьПортSMTP							= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьПортSMTP", "Настройка");
	КомандаИзменитьФлагТребуетсяSMTPАутентификация	= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьФлагТребуетсяSMTPАутентификация", "Настройка");
	КомандаИзменитьСертификатРуководителя			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатРуководителя", "Настройка");
	КомандаИзменитьСертификатГлавногоБухгалтера		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатГлавногоБухгалтера", "Настройка");
	КомандаИзменитьСертификатДляШифрования			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатДляШифрования", "Настройка");
	
	КомандаЗарегистрироватьНаправлениеФСС 			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ЗарегистрироватьНаправлениеФСС", "Настройка");
	КомандаИзменитьСертификатФСС 					= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатФСС", "Настройка");
	КомандаИзменитьСертификатФССЭЛН 				= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатФССЭЛН", "Настройка");
	КомандаЗарегистрироватьНаправлениеФСРАР 		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ЗарегистрироватьНаправлениеФСРАР", "Настройка");
	КомандаИзменитьКодРегионаФСРАР 					= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьКодРегионаФСРАР", "Настройка");
	КомандаИзменитьСертификатФСРАР 					= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатФСРАР", "Настройка");
	КомандаИзменитьСертификатФСРАРСубъектаРФ 		= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатФСРАРСубъектаРФ", "Настройка");
	КомандаЗарегистрироватьНаправлениеРПН 			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ЗарегистрироватьНаправлениеРПН", "Настройка");
	КомандаИзменитьСертификатРПН 					= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ИзменитьСертификатРПН", "Настройка");
	КомандаЗарегистрироватьНаправлениеФТС 			= НайтиСтрокуВМассивеСтруктур(ТаблицаНеобходимыхИзменений, "ЗарегистрироватьНаправлениеФТС", "Настройка");
	
	// изменяем признак Назначение
	Если КомандаПредназначенаДляДокументооборотаСФНС <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ПредназначенаДляДокументооборотаСФНС = КомандаПредназначенаДляДокументооборотаСФНС.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен признак возможности документооборота с ФНС по учетной записи.'"), Ложь);
	КонецЕсли;
	Если КомандаПредназначенаДляДокументооборотаСПФР <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ПредназначенаДляДокументооборотаСПФР = КомандаПредназначенаДляДокументооборотаСПФР.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен признак возможности документооборота с СФР (бывш. ПФР) по учетной записи.'"), Ложь);
	КонецЕсли;
	Если КомандаПредназначенаДляДокументооборотаСФСГС <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ПредназначенаДляДокументооборотаСФСГС = КомандаПредназначенаДляДокументооборотаСФСГС.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен признак возможности документооборота с Росстатом по учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем признак возможности отправки в ЕЦП СФР (бывш. ПФР)
	Если КомандаИзменитьИспользуетсяЕЦППФР <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ИспользуетсяЕЦППФР = КомандаИзменитьИспользуетсяЕЦППФР.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен признак возможности отправки в ЕЦП СФР (бывш. ПФР).'"), Ложь);
	КонецЕсли;
	
	// изменяем типы документов, отправляемых в ЕЦП СФР (бывш. ПФР)
	Если КомандаИзменитьТипыДокументовЕЦППФР <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ТипыДокументовЕЦППФР = КомандаИзменитьТипыДокументовЕЦППФР.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменены типы документов, отправляемых в ЕЦП СФР (бывш. ПФР).'"), Ложь);
	КонецЕсли;
	
	// изменяем регистрационный номер СФР (бывш. ПФР) оператора
	Если КомандаИзменитьОператорРегНомерПФР <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ОператорРегНомерПФР = КомандаИзменитьОператорРегНомерПФР.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен регистрационный номер СФР (бывш. ПФР) оператора.'"), Ложь);
	КонецЕсли;
	
	// изменяем адрес электронной почты
	Если КомандаИзменитьОператорНаименованиеПолное <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ОператорНаименованиеПолное = КомандаИзменитьОператорНаименованиеПолное.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменено полное наименование оператора.'"), Ложь);
	КонецЕсли;
	
	// изменяем адрес электронной почты
	Если КомандаИзменитьОператорНаименованиеКраткое <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ОператорНаименованиеКраткое = КомандаИзменитьОператорНаименованиеКраткое.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменено краткое наименование оператора.'"), Ложь);
	КонецЕсли;
	
	// изменяем адрес электронной почты
	Если КомандаИзменитьОператорИНН <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ОператорИНН = КомандаИзменитьОператорИНН.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен ИНН оператора.'"), Ложь);
	КонецЕсли;
	
	// изменяем адрес электронной почты
	Если КомандаИзменитьОператорКПП <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ОператорКПП = КомандаИзменитьОператорКПП.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен КПП оператора.'"), Ложь);
	КонецЕсли;
	
	// изменяем идентификатор абонента
	Если КомандаИзменитьИдентификаторАбонента <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ИдентификаторАбонента = КомандаИзменитьИдентификаторАбонента.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен идентификатор абонента в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем идентификатор оператора электронного документооборота
	Если КомандаИзменитьИдентификаторСпецоператора <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ИдентификаторСпецоператора = КомандаИзменитьИдентификаторСпецоператора.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен идентификатор оператора электронного документооборота в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем адрес электронной почты
	Если КомандаИзменитьАдресЭлектроннойПочты <> Неопределено Тогда
		УчетнаяЗаписьОбъект.АдресЭлектроннойПочты = КомандаИзменитьАдресЭлектроннойПочты.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен адрес электронной почты в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем адрес сервера POP3
	Если КомандаИзменитьСерверPOP3 <> Неопределено Тогда
		УчетнаяЗаписьОбъект.СерверPOP3 = КомандаИзменитьСерверPOP3.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен адрес сервера POP3 в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем имя пользователя POP3
	Если КомандаИзменитьИмяПользователяPOP3 <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ИмяПользователяPOP3 = КомандаИзменитьИмяПользователяPOP3.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменено имя пользователя POP3 в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем пароль POP3
	Если КомандаИзменитьПарольPOP3 <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ПарольPOP3 = КомандаИзменитьПарольPOP3.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен пароль POP3 в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем порт POP3
	Если КомандаИзменитьПортPOP3 <> Неопределено Тогда
		// Не обновляем порты, если они уже были заполнены раньше
		Если НЕ ЗначениеЗаполнено(КомандаИзменитьПортPOP3.СтароеЗначение) Тогда
			УчетнаяЗаписьОбъект.ПортPOP3 = КомандаИзменитьПортPOP3.НовоеЗначение;
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен порт POP3 в свойствах учетной записи.'"), Ложь);
		КонецЕсли;
	КонецЕсли;
	
	// изменяем адрес сервера SMTP
	Если КомандаИзменитьСерверSMTP <> Неопределено Тогда
		УчетнаяЗаписьОбъект.СерверSMTP = КомандаИзменитьСерверSMTP.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен адрес сервера SMTP в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем порт SMTP
	Если КомандаИзменитьПортSMTP <> Неопределено Тогда
		// Не обновляем порты, если они уже были заполнены раньше
		Если НЕ ЗначениеЗаполнено(КомандаИзменитьПортSMTP.СтароеЗначение) Тогда
			УчетнаяЗаписьОбъект.ПортSMTP = КомандаИзменитьПортSMTP.НовоеЗначение;
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен порт SMTP в свойствах учетной записи.'"), Ложь);
		КонецЕсли;
	КонецЕсли;
	
	// изменяем признак необходимости авторизации на сервере SMTP
	Если КомандаИзменитьФлагТребуетсяSMTPАутентификация <> Неопределено Тогда
		УчетнаяЗаписьОбъект.ТребуетсяSMTPАутентификация = КомандаИзменитьФлагТребуетсяSMTPАутентификация.НовоеЗначение;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен признак необходимости авторизации на сервере SMTP в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем сертификат руководителя
	Если КомандаИзменитьСертификатРуководителя <> Неопределено Тогда
		УчетнаяЗаписьОбъект.СертификатРуководителя = КомандаИзменитьСертификатРуководителя.НовоеЗначение.Отпечаток; //+
		ОтпечатокСертификатаРуководителя = КомандаИзменитьСертификатРуководителя.НовоеЗначение.Отпечаток;
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен сертификат руководителя в свойствах учетной записи.'"), Ложь);
	Иначе
		ОтпечатокСертификатаРуководителя = УчетнаяЗаписьОбъект.СертификатРуководителя; //+
	КонецЕсли;
	
	// изменяем сертификат главного бухгалтера
	Если КомандаИзменитьСертификатГлавногоБухгалтера <> Неопределено
		И Строка(УчетнаяЗаписьОбъект.СертификатГлавногоБухгалтера) <>
		Строка(КомандаИзменитьСертификатГлавногоБухгалтера.НовоеЗначение.Отпечаток) Тогда
		
		УчетнаяЗаписьОбъект.СертификатГлавногоБухгалтера = КомандаИзменитьСертификатГлавногоБухгалтера.НовоеЗначение.Отпечаток; //+
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен сертификат главного бухгалтера в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// изменяем сертификат для шифрования
	Если КомандаИзменитьСертификатДляШифрования <> Неопределено
		И Строка(УчетнаяЗаписьОбъект.СертификатДляШифрования) <>
		Строка(КомандаИзменитьСертификатДляШифрования.НовоеЗначение.Отпечаток) Тогда
		
		УчетнаяЗаписьОбъект.СертификатДляШифрования = КомандаИзменитьСертификатДляШифрования.НовоеЗначение.Отпечаток; //+
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Изменен сертификат для шифрования в свойствах учетной записи.'"), Ложь);
	КонецЕсли;
	
	// если учетная запись была изменена, то пишем
	Если УчетнаяЗаписьОбъект.Модифицированность() Тогда
		Попытка
			УчетнаяЗаписьОбъект.Записать();
			ТранзакцияПустая = Ложь;
		Исключение
			ОтменитьТранзакцию();
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Ошибка обновления настроек учетной записи:'") + Символы.ПС + ИнформацияОбОшибке().Описание);
			Возврат Ложь;
		КонецПопытки;
	КонецЕсли;
	
	// Настройка учетной записи непосредственно
	///////////////////////////////////////////////////////////////////////////////
	
	
	///////////////////////////////////////////////////////////////////////////////
	// Фиксация изменений в ИБ
	
	// пытаемся зафиксировать изменения в ИБ
	Если НЕ ТранзакцияПустая Тогда
		Попытка
			ЗафиксироватьТранзакцию();
		Исключение
			ДобавитьСообщение(Сообщения, НСтр("ru = 'Ошибка обновления настроек учетной записи:'") + Символы.ПС + ИнформацияОбОшибке().Описание);
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецПопытки;
	Иначе
		ОтменитьТранзакцию();
	КонецЕсли;
	
	// Фиксация изменений в ИБ
	///////////////////////////////////////////////////////////////////////////////
	
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка направления ФСС
	
	КонтекстЭДОСервер = Неопределено;
	ОрганизацияСсылка = Организация;
	
	Если КомандаЗарегистрироватьНаправлениеФСС <> Неопределено ИЛИ КомандаИзменитьСертификатФСС <> Неопределено
		ИЛИ КомандаИзменитьСертификатФССЭЛН <> Неопределено
		ИЛИ КомандаЗарегистрироватьНаправлениеФСРАР <> Неопределено ИЛИ КомандаИзменитьКодРегионаФСРАР <> Неопределено
		ИЛИ КомандаИзменитьСертификатФСРАР <> Неопределено ИЛИ КомандаИзменитьСертификатФСРАРСубъектаРФ <> Неопределено
		ИЛИ КомандаЗарегистрироватьНаправлениеРПН <> Неопределено ИЛИ КомандаИзменитьСертификатРПН <> Неопределено
		ИЛИ КомандаЗарегистрироватьНаправлениеФТС <> Неопределено
		ИЛИ КомандаИзменитьСертификатРуководителя <> Неопределено Тогда
		
		Если ТипЗнч(ОрганизацияСсылка) = Тип("Структура") И ОрганизацияСсылка.Свойство("ИНН")
			И ОрганизацияСсылка.Свойство("КПП") Тогда
			
			ОрганизацияСсылка = ОпределитьОрганизациюПоИННиКППНаСервере(ОрганизацияСсылка.ИНН, ОрганизацияСсылка.КПП, Истина);
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ОрганизацияСсылка) И ЗначениеЗаполнено(УчетнаяЗапись) Тогда
			ОрганизацияСсылка = ОпределитьОрганизациюПоУчетнойЗаписи(УчетнаяЗапись);
		КонецЕсли;
		
		Если КонтекстЭДОСервер = Неопределено Тогда
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		КонецЕсли;
		
	КонецЕсли;
	
	Если (КомандаЗарегистрироватьНаправлениеФСС <> Неопределено ИЛИ КомандаИзменитьСертификатФСС <> Неопределено
		ИЛИ КомандаИзменитьСертификатФССЭЛН <> Неопределено ИЛИ КомандаИзменитьСертификатРуководителя <> Неопределено)
		И ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		
		ОтпечатокСертификатаФСС = ?(КомандаИзменитьСертификатФСС <> Неопределено,
			КомандаИзменитьСертификатФСС.НовоеЗначение.Отпечаток, Неопределено);
		МассивСертификатовФСС = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФСС),
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаФСС), Неопределено);
		
		ОтпечатокСертификатаФССЭЛН = ?(КомандаИзменитьСертификатФССЭЛН <> Неопределено,
			КомандаИзменитьСертификатФССЭЛН.НовоеЗначение.Отпечаток, Неопределено);
		МассивСертификатовФССЭЛН = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФССЭЛН),
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаФССЭЛН), Неопределено);
		
		СтруктураАвтонастройкиФСС = КонтекстЭДОСервер.НастроитьОбменФСС(
			МассивСертификатовФСС,
			МассивСертификатовФССЭЛН,
			ОтпечатокСертификатаРуководителя,
			ОрганизацияСсылка,
			Ложь,
			Истина);
		
		НастройкиФССЗаданы = КомандаЗарегистрироватьНаправлениеФСС <> Неопределено
			ИЛИ ЗначениеЗаполнено(ОтпечатокСертификатаФСС) ИЛИ ЗначениеЗаполнено(ОтпечатокСертификатаФССЭЛН);
		ИспользоватьАвтонастройкуФСС = НЕ СтруктураАвтонастройкиФСС.ИспользоватьОбменВНастройках
			ИЛИ НЕ СтруктураАвтонастройкиФСС.ФлагСообщения ИЛИ СтруктураАвтонастройкиФСС.ИспользоватьОбменВНастройках
			И СтруктураАвтонастройкиФСС.ИспользоватьАвтонастройкуВНастройках;
		Если (СтруктураАвтонастройкиФСС.ИспользоватьОбменВНастройках ИЛИ НастройкиФССЗаданы)
			И СтруктураАвтонастройкиФСС.ФлагАвтозаполнения И ИспользоватьАвтонастройкуФСС Тогда
			
			ОтпечатокСертификатаФСС = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФСС), ОтпечатокСертификатаФСС,
				СтруктураАвтонастройкиФСС.СертификатФССОтпечатокВНастройках);
			ОтпечатокСертификатаФССЭЛН = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФССЭЛН), ОтпечатокСертификатаФССЭЛН,
				СтруктураАвтонастройкиФСС.СертификатФССЭЛНОтпечатокВНастройках);
			
			ОбновитьРегистрыСведенийНастройкиОбменаФСС(
				ОрганизацияСсылка,
				ОтпечатокСертификатаРуководителя,
				ОтпечатокСертификатаФСС,
				ОтпечатокСертификатаФССЭЛН,
				Ложь);
			
			// генерируем сообщение
			Если НЕ СтруктураАвтонастройкиФСС.ИспользоватьОбменВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Включен обмен с СФР (бывш. ФСС).'"),
					Ложь);
			КонецЕсли;
			
			Если ОтпечатокСертификатаФСС <> СтруктураАвтонастройкиФСС.СертификатФССОтпечатокВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Изменен сертификат СФР (бывш. ФСС).'"),
					Ложь);
			КонецЕсли;
			
			Если ОтпечатокСертификатаФССЭЛН <> СтруктураАвтонастройкиФСС.СертификатФССЭЛНОтпечатокВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Изменен сертификат СФР (бывш. ФСС) ЭЛН.'"),
					Ложь);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// Настройка направления ФСС
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка направления ФСРАР
	
	Если (КомандаЗарегистрироватьНаправлениеФСРАР <> Неопределено ИЛИ КомандаИзменитьКодРегионаФСРАР <> Неопределено
		ИЛИ КомандаИзменитьСертификатФСРАР <> Неопределено ИЛИ КомандаИзменитьСертификатФСРАРСубъектаРФ <> Неопределено
		ИЛИ КомандаИзменитьСертификатРуководителя <> Неопределено) И ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		
		КодРегионаФСРАР = ?(КомандаИзменитьКодРегионаФСРАР <> Неопределено,
			КомандаИзменитьКодРегионаФСРАР.НовоеЗначение, Неопределено);
		
		ОтпечатокСертификатаФСРАР = ?(КомандаИзменитьСертификатФСРАР <> Неопределено,
			КомандаИзменитьСертификатФСРАР.НовоеЗначение.Отпечаток, Неопределено);
		МассивСертификатовФСРАР = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФСРАР),
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаФСРАР), Неопределено);
		
		ОтпечатокСертификатаФСРАРСубъектаРФ = ?(КомандаИзменитьСертификатФСРАРСубъектаРФ <> Неопределено,
			КомандаИзменитьСертификатФСРАРСубъектаРФ.НовоеЗначение.Отпечаток, Неопределено);
		МассивСертификатовФСРАРСубъектаРФ = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФСРАРСубъектаРФ),
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаФСРАРСубъектаРФ), Неопределено);
		
		СтруктураАвтонастройкиФСРАР = Новый Структура;
		СтруктураАвтонастройкиФСРАР.Вставить("ПропускатьНезаданные", 		Истина);
		СтруктураАвтонастройкиФСРАР.Вставить("ПрочитатьНастройкиОбмена", 	Истина);
		СтруктураАвтонастройкиФСРАР.Вставить("ЗаписатьНастройкиОбмена", 	Ложь);
		КонтекстЭДОСервер.РегистрацияРегистрФСРАР(
			ОтпечатокСертификатаРуководителя,
			МассивСертификатовФСРАРСубъектаРФ,
			МассивСертификатовФСРАР,
			КодРегионаФСРАР,
			ОрганизацияСсылка,
			Ложь,
			СтруктураАвтонастройкиФСРАР);
		
		НастройкиФСРАРЗаданы = КомандаЗарегистрироватьНаправлениеФСРАР <> Неопределено ИЛИ ЗначениеЗаполнено(КодРегионаФСРАР)
			ИЛИ ЗначениеЗаполнено(ОтпечатокСертификатаФСРАР) ИЛИ ЗначениеЗаполнено(ОтпечатокСертификатаФСРАРСубъектаРФ);
		ИспользоватьАвтонастройкуФСРАР = НЕ СтруктураАвтонастройкиФСРАР.ИспользоватьОбменВНастройках
			ИЛИ НЕ СтруктураАвтонастройкиФСРАР.ФлагСообщения ИЛИ СтруктураАвтонастройкиФСРАР.ИспользоватьОбменВНастройках
			И СтруктураАвтонастройкиФСРАР.ИспользоватьАвтонастройкуВНастройках;
		Если (СтруктураАвтонастройкиФСРАР.ИспользоватьОбменВНастройках ИЛИ НастройкиФСРАРЗаданы)
			И СтруктураАвтонастройкиФСРАР.ФлагАвтозаполнения И ИспользоватьАвтонастройкуФСРАР Тогда
			
			ОтпечатокСертификатаФСРАР = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФСРАР), ОтпечатокСертификатаФСРАР,
				СтруктураАвтонастройкиФСРАР.СертификатФСРАРОтпечатокВНастройках);
			ОтпечатокСертификатаФСРАРСубъектаРФ = ?(ЗначениеЗаполнено(ОтпечатокСертификатаФСРАРСубъектаРФ),
				ОтпечатокСертификатаФСРАРСубъектаРФ, СтруктураАвтонастройкиФСРАР.СертификатФСРАРСубъектаРФОтпечатокВНастройках);
			КодРегионаФСРАР = ?(ЗначениеЗаполнено(КодРегионаФСРАР), КодРегионаФСРАР,
				СтруктураАвтонастройкиФСРАР.КодРегионаФСРАР);
			
			МассивСертификатовФСРАР = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаФСРАР);
			МассивСертификатовФСРАРСубъектаРФ =
				ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаФСРАРСубъектаРФ);
			
			НастройкиФСРАР = Новый Структура;
			НастройкиФСРАР.Вставить("ПропускатьНезаданные", 	Истина);
			НастройкиФСРАР.Вставить("ПрочитатьНастройкиОбмена", Ложь);
			НастройкиФСРАР.Вставить("ЗаписатьНастройкиОбмена", 	Истина);
			КонтекстЭДОСервер.РегистрацияРегистрФСРАР(
				ОтпечатокСертификатаРуководителя,
				МассивСертификатовФСРАРСубъектаРФ,
				МассивСертификатовФСРАР,
				КодРегионаФСРАР,
				ОрганизацияСсылка,
				Ложь,
				НастройкиФСРАР);
			
			// генерируем сообщение
			Если НЕ СтруктураАвтонастройкиФСРАР.ИспользоватьОбменВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Включен обмен с Росалкогольтабакконтролем.'"),
					Ложь);
			КонецЕсли;
			
			Если ОтпечатокСертификатаФСРАР <> СтруктураАвтонастройкиФСРАР.СертификатФСРАРОтпечатокВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Изменен сертификат Росалкогольтабакконтроля.'"),
					Ложь);
			КонецЕсли;
			
			Если ОтпечатокСертификатаФСРАРСубъектаРФ <> СтруктураАвтонастройкиФСРАР.СертификатФСРАРСубъектаРФОтпечатокВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Изменен сертификат субъекта РФ Росалкогольтабакконтроля.'"),
					Ложь);
			КонецЕсли;
			
			Если КодРегионаФСРАР <> СтруктураАвтонастройкиФСРАР.КодРегионаФСРАР Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Изменен код региона Росалкогольтабакконтроля.'"),
					Ложь);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Настройка направления ФСРАР
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка направления РПН
	
	Если (КомандаЗарегистрироватьНаправлениеРПН <> Неопределено ИЛИ КомандаИзменитьСертификатРПН <> Неопределено
		ИЛИ КомандаИзменитьСертификатРуководителя <> Неопределено) И ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		
		ОтпечатокСертификатаРПН = ?(КомандаИзменитьСертификатРПН <> Неопределено,
			КомандаИзменитьСертификатРПН.НовоеЗначение.Отпечаток, Неопределено);
		МассивСертификатовРПН = ?(ЗначениеЗаполнено(ОтпечатокСертификатаРПН),
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОтпечатокСертификатаРПН), Неопределено);
		
		СтруктураАвтонастройкиРПН = Неопределено;
		КонтекстЭДОСервер.РегистрацияРегистрРПН(
			ОтпечатокСертификатаРуководителя,
			МассивСертификатовРПН,
			ОрганизацияСсылка,
			Ложь,
			СтруктураАвтонастройкиРПН,
			Истина);
		
		НастройкиРПНЗаданы = КомандаЗарегистрироватьНаправлениеРПН <> Неопределено
			ИЛИ ЗначениеЗаполнено(ОтпечатокСертификатаРПН);
		ИспользоватьАвтонастройкуРПН = НЕ СтруктураАвтонастройкиРПН.ИспользоватьОбменВНастройках
			ИЛИ НЕ СтруктураАвтонастройкиРПН.ФлагСообщения ИЛИ СтруктураАвтонастройкиРПН.ИспользоватьОбменВНастройках
			И СтруктураАвтонастройкиРПН.ИспользоватьАвтонастройкуВНастройках;
		Если (СтруктураАвтонастройкиРПН.ИспользоватьОбменВНастройках ИЛИ НастройкиРПНЗаданы)
			И СтруктураАвтонастройкиРПН.ФлагАвтозаполнения И ИспользоватьАвтонастройкуРПН Тогда
			
			ОтпечатокСертификатаРПН = ?(ЗначениеЗаполнено(ОтпечатокСертификатаРПН), ОтпечатокСертификатаРПН,
				СтруктураАвтонастройкиРПН.СертификатРПНОтпечатокВНастройках);
			
			ОбновитьРегистрыСведенийНастройкиОбменаРПН(
				ОрганизацияСсылка,
				ОтпечатокСертификатаРуководителя,
				ОтпечатокСертификатаРПН,
				Ложь,
				СтруктураАвтонастройкиРПН.EMail,
				СтруктураАвтонастройкиРПН.Пароль);
			
			// генерируем сообщение
			Если НЕ СтруктураАвтонастройкиРПН.ИспользоватьОбменВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Включен обмен с Росприроднадзором.'"),
					Ложь);
			КонецЕсли;
			
			Если ОтпечатокСертификатаРПН <> СтруктураАвтонастройкиРПН.СертификатРПНОтпечатокВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Изменен сертификат Росприроднадзора.'"),
					Ложь);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// Настройка направления РПН
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Настройка направления ФТС
	
	Если (КомандаЗарегистрироватьНаправлениеФТС <> Неопределено ИЛИ КомандаИзменитьСертификатРуководителя <> Неопределено)
		И ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		
		СтруктураАвтонастройкиФТС = Неопределено;
		КонтекстЭДОСервер.РегистрацияРегистрФТС(
			ОтпечатокСертификатаРуководителя,
			ОрганизацияСсылка,
			Ложь,
			СтруктураАвтонастройкиФТС,
			Истина);
		
		НастройкиФТСЗаданы = КомандаЗарегистрироватьНаправлениеФТС <> Неопределено;
		ИспользоватьАвтонастройкуФТС = НЕ СтруктураАвтонастройкиФТС.ИспользоватьОбменВНастройках
			ИЛИ НЕ СтруктураАвтонастройкиФТС.ФлагСообщения ИЛИ СтруктураАвтонастройкиФТС.ИспользоватьОбменВНастройках
			И СтруктураАвтонастройкиФТС.ИспользоватьАвтонастройкуВНастройках;
		Если (СтруктураАвтонастройкиФТС.ИспользоватьОбменВНастройках ИЛИ НастройкиФТСЗаданы)
			И СтруктураАвтонастройкиФТС.ФлагАвтозаполнения И ИспользоватьАвтонастройкуФТС Тогда
			
			ОбновитьРегистрыСведенийНастройкиОбменаФТС(
				ОрганизацияСсылка,
				ОтпечатокСертификатаРуководителя,
				Ложь,
				СтруктураАвтонастройкиФТС.Логин,
				СтруктураАвтонастройкиФТС.Пароль,
				СтруктураАвтонастройкиФТС.МЧД);
			
			// генерируем сообщение
			Если НЕ СтруктураАвтонастройкиФТС.ИспользоватьОбменВНастройках Тогда
				ДобавитьСообщение(
					Сообщения,
					НСтр("ru = 'Включен обмен с ФТС.'"),
					Ложь);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// Настройка направления ФТС
	///////////////////////////////////////////////////////////////////////////////
	
	///////////////////////////////////////////////////////////////////////////////
	// Регистрация сертификатов в системе
	
	// регистрируем сертификаты
	СоответствиеКомандРегистрацииСертификатовХранилищам = Новый Соответствие;
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКорневойГНИВЦ", "ROOT");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКорневойТакском", "ROOT");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКроссГНИВЦ", "CA");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатНалоговогоОргана", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатСОС", "AddressBook");
	
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатОрганаПФР", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатОрганаПФРЭДОК", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатОрганаПФРЕЦП", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКорневойПФР", "ROOT");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКРОССПФР", "CA");
	
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатОрганаФСГС", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКорневойФСГС", "ROOT");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатКРОССФСГС", "CA");
	
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатФСС", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатФССЭЛН", "AddressBook");
	
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатФСРАР", "AddressBook");
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатФСРАРСубъектаРФ", "AddressBook");
	
	СоответствиеКомандРегистрацииСертификатовХранилищам.Вставить("ЗарегистрироватьСертификатРПН", "AddressBook");
	
	СоответствиеКомандРегистрацииСертификатовПредставлениям = Новый Соответствие;
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКорневойГНИВЦ", "корневой сертиифкат УЦ ГНИВЦ ФНС РФ");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКорневойТакском", "корневой сертификат УЦ ""Такском""");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКроссГНИВЦ", "кросс-сертификат УЦ");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатНалоговогоОргана", "сертификат налогового органа");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатСОС", "сертификат спецоператора");
	
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатОрганаПФР", "сертификат органа СФР");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатОрганаПФРЭДОК", "сертификат СФР для ЭДОК");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатОрганаПФРЕЦП", "сертификат СФР для ЕЦП");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКорневойПФР", "корневой сертификат, используемый при обмене с СФР");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКРОССПФР", "кросс-сертификат, используемый при обмене с СФР");
	
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатОрганаФСГС", "сертификат территориального органа ФСГС");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКорневойФСГС", "корневой сертификат, используемый при обмене с Росстатом");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатКРОССФСГС", "кросс-сертификат, используемый при обмене с Росстатом");
	
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатФСС", "сертификат ФСС");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатФССЭЛН", "сертификат ФСС ЭЛН, ПОВЭД, СЭДО");
	
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатФСРАР", "сертификат Росалкогольтабакконтроля");
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатФСРАРСубъектаРФ", "сертификат субъекта РФ Росалкогольтабакконтроля");
	
	СоответствиеКомандРегистрацииСертификатовПредставлениям.Вставить("ЗарегистрироватьСертификатРПН", "сертификат Росприроднадзора");
	
	СоответствиеИдентификаторовХранилищПредставлениям = Новый Соответствие;
	СоответствиеИдентификаторовХранилищПредставлениям.Вставить("ROOT", "Доверенные корневые центры сертификации");
	СоответствиеИдентификаторовХранилищПредставлениям.Вставить("CA", "Промежуточные центры сертификации");
	СоответствиеИдентификаторовХранилищПредставлениям.Вставить("AddressBook", "Прочие пользователи");
	
	УчетнаяЗапись = УчетнаяЗаписьОбъект.Ссылка;
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура НастроитьПоРОКИ(ОповещениеОЗавершении, УчетнаяЗапись, ТаблицаНеобходимыхИзменений, Сообщения, Организация)
	
	СоответствиеКомандРегистрацииСертификатовХранилищам = Неопределено;
	СоответствиеКомандРегистрацииСертификатовПредставлениям = Неопределено;
	СоответствиеИдентификаторовХранилищПредставлениям = Неопределено;
	
	Если ВыполнитьНастройкиПоРОКИ(УчетнаяЗапись,
		ТаблицаНеобходимыхИзменений,
		Сообщения,
		СоответствиеКомандРегистрацииСертификатовХранилищам,
		СоответствиеКомандРегистрацииСертификатовПредставлениям,
		СоответствиеИдентификаторовХранилищПредставлениям,
		Организация) = Ложь Тогда
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Новый Структура("Выполнено, Сообщения, УчетнаяЗапись", Ложь, Сообщения, УчетнаяЗапись));
		Возврат;
		
	КонецЕсли;
	
	МассивСообщений = Новый Массив;
	
	ВходящийКонтекст = Новый Структура();
	ВходящийКонтекст.Вставить("СоответствиеКомандРегистрацииСертификатовХранилищам", СоответствиеКомандРегистрацииСертификатовХранилищам);
	ВходящийКонтекст.Вставить("СоответствиеКомандРегистрацииСертификатовПредставлениям", СоответствиеКомандРегистрацииСертификатовПредставлениям);
	ВходящийКонтекст.Вставить("СоответствиеИдентификаторовХранилищПредставлениям", СоответствиеИдентификаторовХранилищПредставлениям);
	ВходящийКонтекст.Вставить("ТаблицаНеобходимыхИзменений", ТаблицаНеобходимыхИзменений);
	ВходящийКонтекст.Вставить("Сообщения", Сообщения);
	ВходящийКонтекст.Вставить("МассивСообщений", МассивСообщений);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ОписаниеОповещения = Новый ОписаниеОповещения("НастроитьПоРОКИПослеРегистрацииВсехСертификатов", ЭтотОбъект, ДополнительныеПараметры);
	
	МассивВидовКоманд = Новый Массив;
	// цикл по всем возможным командам регистрации сертификатов
	Для Каждого Эл Из СоответствиеКомандРегистрацииСертификатовХранилищам Цикл
		МассивВидовКоманд.Добавить(Эл.Ключ);
	КонецЦикла;
	
	ВходящийКонтекст.Вставить("МассивВидовКоманд", МассивВидовКоманд);
	ВходящийКонтекст.Вставить("НомерТекущегоВидаКоманд", 0);
	
	ЗарегистрироватьСертификатыПоследовательно(ОписаниеОповещения, ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатыПоследовательно(ОповещениеОЗавершении, ВходящийКонтекст)
		
		НомерТекущегоВидаКоманд = ВходящийКонтекст.НомерТекущегоВидаКоманд;
		МассивВидовКоманд 		= ВходящийКонтекст.МассивВидовКоманд;
		СоответствиеКомандРегистрацииСертификатовХранилищам = ВходящийКонтекст.СоответствиеКомандРегистрацииСертификатовХранилищам;
		ТаблицаНеобходимыхИзменений = ВходящийКонтекст.ТаблицаНеобходимыхИзменений;
		
		ВходящийКонтекст.Вставить("ОповещениеОЗавершенииЦиклаПоВидамКоманд", ОповещениеОЗавершении);

		Если НомерТекущегоВидаКоманд <= МассивВидовКоманд.ВГраница() Тогда
			
			ТекущийВидКоманд = МассивВидовКоманд[НомерТекущегоВидаКоманд];
			ИмяХранилища = СоответствиеКомандРегистрацииСертификатовХранилищам[ТекущийВидКоманд];
			
			// поиск команд текущего вида в таблице изменений
			МассивКоманд = НайтиСтрокиМассиваСтруктур(ТаблицаНеобходимыхИзменений, "Настройка", ТекущийВидКоманд);
			
			ВходящийКонтекст.Вставить("ТекущийВидКоманд", 		ТекущийВидКоманд);
			ВходящийКонтекст.Вставить("ИмяХранилища", 			ИмяХранилища);
			ВходящийКонтекст.Вставить("МассивКоманд", 			МассивКоманд);
			ВходящийКонтекст.Вставить("НомерТекущейКоманды", 	0);
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВходящийКонтекст", ВходящийКонтекст);
			ОписаниеОповещения = Новый ОписаниеОповещения("ЗарегистрироватьСертификатыПоследовательноПослеРегистрацииПоВсемКомандамОдногоВида", ЭтотОбъект, ДополнительныеПараметры);
			
			ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательно(ОписаниеОповещения, ВходящийКонтекст);
			Возврат;
			
		Иначе
			РезультатСтруктура = Новый Структура;
			РезультатСтруктура.Вставить("Выполнено", Истина);
			РезультатСтруктура.Вставить("Сообщения", ВходящийКонтекст.Сообщения);
			РезультатСтруктура.Вставить("МассивСообщений", ВходящийКонтекст.МассивСообщений);
			
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершенииЦиклаПоВидамКоманд, РезультатСтруктура);
		КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательно(ОповещениеОЗавершении, ВходящийКонтекст)
		
		НомерТекущейКоманды = ВходящийКонтекст.НомерТекущейКоманды;
		МассивКоманд = ВходящийКонтекст.МассивКоманд;
		
		ВходящийКонтекст.Вставить("ОповещениеОЗавершенииЦиклаПоКомандам", ОповещениеОЗавершении);
		
		Если НомерТекущейКоманды <= МассивКоманд.ВГраница() Тогда
			
			ТекущаяКоманда = МассивКоманд[ВходящийКонтекст.НомерТекущейКоманды];
			
			Сертификат64 = ТекущаяКоманда.НовоеЗначение.Сертификат;

			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВходящийКонтекст", ВходящийКонтекст);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательноПослеЗаписиСертификата64Вфайл", ЭтотОбъект, ДополнительныеПараметры);
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(ОписаниеОповещения, Сертификат64);
			Возврат;
			
		Иначе
			
			РезультатСтруктура = Новый Структура;
			РезультатСтруктура.Вставить("Сообщения", ВходящийКонтекст.Сообщения);
			РезультатСтруктура.Вставить("МассивСообщений", ВходящийКонтекст.МассивСообщений);
			
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершенииЦиклаПоКомандам, РезультатСтруктура);
			
		КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательноПослеЗаписиСертификата64Вфайл(Результат, ДополнительныеПараметры) Экспорт
	
	ВходящийКонтекст = ДополнительныеПараметры.ВходящийКонтекст;
	
	Если НЕ Результат.Выполнено Тогда
		ДобавитьСообщение(ВходящийКонтекст.Сообщения,
					СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось зарегистрировать %1 в системном хранилище ""%2""!'"),
					ВходящийКонтекст.СоответствиеКомандРегистрацииСертификатовПредставлениям[ВходящийКонтекст.ТекущийВидКоманд],
					ВходящийКонтекст.СоответствиеИдентификаторовХранилищПредставлениям[ВходящийКонтекст.ИмяХранилища]));

		РезультатСтруктура = Новый Структура;
		РезультатСтруктура.Вставить("Выполнено", Ложь);
		РезультатСтруктура.Вставить("Сообщения", ВходящийКонтекст.Сообщения);
		РезультатСтруктура.Вставить("МассивСообщений", ВходящийКонтекст.МассивСообщений);
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершенииЦиклаПоВидамКоманд, РезультатСтруктура);
		Возврат;
	КонецЕсли;	
	
	ФайлСертификата = Результат.ИмяФайла;

	// импортируем сертификат
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВходящийКонтекст", ВходящийКонтекст);

	ОписаниеОповещения = Новый ОписаниеОповещения("ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательноПослеИмпортированияСертификата", ЭтотОбъект, ДополнительныеПараметры);
	
	КриптографияЭДКОКлиент.ИмпортироватьСертификат(ОписаниеОповещения, ФайлСертификата, ВходящийКонтекст.ИмяХранилища);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательноПослеИмпортированияСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВходящийКонтекст = ДополнительныеПараметры.ВходящийКонтекст;
	
	Если НЕ Результат.Выполнено Тогда
		
		ДобавитьСообщение(ВходящийКонтекст.Сообщения,
					СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось зарегистрировать %1 в системном хранилище ""%2""!'"),
					ВходящийКонтекст.СоответствиеКомандРегистрацииСертификатовПредставлениям[ВходящийКонтекст.ТекущийВидКоманд],
					ВходящийКонтекст.СоответствиеИдентификаторовХранилищПредставлениям[ВходящийКонтекст.ИмяХранилища]));

		РезультатСтруктура = Новый Структура;
		РезультатСтруктура.Вставить("Выполнено", Ложь);
		РезультатСтруктура.Вставить("Сообщения", ВходящийКонтекст.Сообщения);
		РезультатСтруктура.Вставить("МассивСообщений", ВходящийКонтекст.МассивСообщений);
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершенииЦиклаПоВидамКоманд, РезультатСтруктура);
		Возврат;
	КонецЕсли;	
	
	ПредставлениеСертификата = ВходящийКонтекст.СоответствиеКомандРегистрацииСертификатовПредставлениям[ВходящийКонтекст.ТекущийВидКоманд];
	
	ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1%2 успешно зарегистрирован в системном хранилище ""%3"".'"),
	ВРЕГ(Лев(ПредставлениеСертификата, 1)),
	Сред(ПредставлениеСертификата, 2),
	ВходящийКонтекст.СоответствиеИдентификаторовХранилищПредставлениям[ВходящийКонтекст.ИмяХранилища]);
	
	ДобавленноеСообщение = Новый Структура("Сообщение, Ошибка", ТекстСообщения, Ложь);
	ВходящийКонтекст.МассивСообщений.Добавить(ДобавленноеСообщение);
	
	ВходящийКонтекст.НомерТекущейКоманды = ВходящийКонтекст.НомерТекущейКоманды + 1;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВходящийКонтекст", ВходящийКонтекст);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗарегистрироватьСертификатыПоследовательноПослеРегистрацииПоВсемКомандамОдногоВида", ЭтотОбъект, ДополнительныеПараметры);

	ЗарегистрироватьСертификатыПоВсемКомандамОдногоВидаПоследовательно(ОписаниеОповещения, ВходящийКонтекст)
	
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатыПоследовательноПослеРегистрацииПоВсемКомандамОдногоВида(Результат, ДополнительныеПараметры) Экспорт
	
	ВходящийКонтекст 		= ДополнительныеПараметры.ВходящийКонтекст;
	
	ВходящийКонтекст.Вставить("Сообщения", Результат.Сообщения);
	ВходящийКонтекст.Вставить("МассивСообщений", Результат.МассивСообщений);
	
	ВходящийКонтекст.Вставить("НомерТекущегоВидаКоманд", ВходящийКонтекст.НомерТекущегоВидаКоманд + 1);
	
	ЗарегистрироватьСертификатыПоследовательно(ВходящийКонтекст.ОповещениеОЗавершенииЦиклаПоВидамКоманд, ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура НастроитьПоРОКИПослеРегистрацииВсехСертификатов(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОЗавершении = ДополнительныеПараметры.ОповещениеОЗавершении;
	
	// перед возвратом дополним Сообщения
	ДополнитьСообщения(Результат.Сообщения, Результат.МассивСообщений);
	
	РезультатСтруктура = Новый Структура;
	РезультатСтруктура. Вставить("Выполнено", Результат.Выполнено);
	РезультатСтруктура. Вставить("Сообщения", Результат.Сообщения);
	РезультатСтруктура. Вставить("УчетнаяЗапись", ДополнительныеПараметры.УчетнаяЗапись);
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатСтруктура);
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьСтруктуруСсылочныхДанных();
	
	ВключитьПакетнуюПодпись = Истина;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция СтрокаОС() Экспорт
	
	СисИнфо = Новый СистемнаяИнформация;
	Если СисИнфо.ТипПлатформы = ТипПлатформы.Linux_x86 Тогда
		Возврат "Linux32";
	ИначеЕсли СисИнфо.ТипПлатформы = ТипПлатформы.Linux_x86_64 Тогда
		Возврат "Linux64";
	ИначеЕсли СисИнфо.ТипПлатформы = ТипПлатформы.MacOS_x86 Тогда
		Возврат "MacOS32";
	ИначеЕсли СисИнфо.ТипПлатформы = ТипПлатформы.MacOS_x86_64 Тогда
		Возврат "MacOS64";
	ИначеЕсли СисИнфо.ТипПлатформы = ТипПлатформы.Windows_x86 Тогда
		Возврат "Windows32";
	Иначе
		Возврат "Windows64";
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ПолучитьФайлНастроекУчетнойЗаписиССервераРОКИСУчетомКэша(Знач УчетнаяЗапись,
																Знач Организация = Неопределено,
																УведомлятьТолькоОбОшибках = Ложь,
																ИгнорироватьРежимАвтонастройки = Ложь,
																ТихийРежим = Ложь,
																ЗапрашиватьПараметрыОрганизацииПриНеобходимости = Ложь,
																УникальныйИдентификатор = Неопределено) Экспорт
	
	// учетной записью может оказаться организация
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		Если УчетнаяЗапись.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
			Возврат Неопределено;
		КонецЕсли;
		Организация = УчетнаяЗапись;
		УчетнаяЗапись = Организация.УчетнаяЗаписьОбмена;
	КонецЕсли;
	
	// получаем настройки учетной записи
	НастройкиУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	
	// если настройки недоступны или учетка для обмена напрямую, то прервемся
	Если НастройкиУчетнойЗаписи = Неопределено ИЛИ НастройкиУчетнойЗаписи.ОбменНапрямую Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// если включено принудительное игнорирование режима автонастройки
	// или отключена автонастройка в свойствах учетной записи, то прервемся
	Если НЕ ИгнорироватьРежимАвтонастройки
	И (НастройкиУчетнойЗаписи.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена
	ИЛИ НастройкиУчетнойЗаписи.РежимАвтонастройки = Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ПустаяСсылка()) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем вспомогательную таблицу сообщений
	Сообщения = СоздатьТаблицуСообщений();
	
	// инициализурем переменную, в которой будет храниться указатель на форму с информацией
	ФормаПрогресса = Неопределено;
	
	// соединяемся с сервером
	ПараметрыСоединенияССервером = СформироватьПараметрыСоединенияССервером(
		УчетнаяЗапись,
		Организация,
		ЗапрашиватьПараметрыОрганизацииПриНеобходимости,
		Сообщения);
		
	Если ПараметрыСоединенияССервером <> Неопределено Тогда
		
		ФайлНастроекРасшифрован = Ложь;
		ДатаОбновленияФайлаНастроекНаСервере = Неопределено;
	
		// устанавливаем соединение
		ОписаниеОшибкиУстановкиСоединения = "";
		Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(ПараметрыСоединенияССервером.ИмяСервераРОКИ, ОписаниеОшибкиУстановкиСоединения);
		Если Соединение = Неопределено Тогда
			ДобавитьСообщение(
				Сообщения, 
				СтрШаблон(НСтр("ru = 'Не удалось установить соединение с сервером ""Такском"":
                                |%1'"), ОписаниеОшибкиУстановкиСоединения));
			Возврат Неопределено;
		КонецЕсли;
		
		// если соединение прошло успешно, то получаем дату обновления файла настроек
		Если Соединение <> Неопределено Тогда
			
			ДатаОбновленияФайлаНастроекНаСервере = ПолучитьДатуОбновленияФайлаНастроекНаСервере(Соединение, ПараметрыСоединенияССервером, Сообщения);
			Если НЕ ЗначениеЗаполнено(ДатаОбновленияФайлаНастроекНаСервере) Тогда
				ВывестиСообщения(УчетнаяЗапись, Сообщения, УведомлятьТолькоОбОшибках);
				Возврат Неопределено;
			КонецЕсли;
			
			ФайлНастроек = ПолучитьЗакэшированныйФайлНастроек(
				УчетнаяЗапись,
				ПараметрыСоединенияССервером,
				ДатаОбновленияФайлаНастроекНаСервере,
				ФайлНастроекРасшифрован,
				Сообщения);
			
			// если в кэше нет файла с такой датой, то получаем его с сервера
			Если ФайлНастроек = Неопределено Тогда
				
				ИнформацияОбОшибке = Неопределено;
				ФайлНастроек = ПолучитьФайлССервера(Соединение, ПараметрыСоединенияССервером.ИмяРесурсаСНастройками, ИнформацияОбОшибке, Истина);
				Соединение = Неопределено;
				
				Если ФайлНастроек = Неопределено Тогда
					ДобавитьСообщение(Сообщения, НСтр("ru = 'Ошибка получения с сервера файла с информацией о настройках учетной записи:'") + Символы.ПС + ИнформацияОбОшибке.Описание);
					ВывестиСообщения(УчетнаяЗапись, Сообщения, УведомлятьТолькоОбОшибках);
					Возврат Неопределено;
				КонецЕсли;
				
				// сохраняем полученный файл в кэше
				СохранитьВКэшеФайлНастроек(УчетнаяЗапись, ПараметрыСоединенияССервером.ИмяРесурсаСНастройками, ФайлНастроек, Ложь, ДатаОбновленияФайлаНастроекНаСервере);
				ФайлНастроекРасшифрован = Ложь;
				
			КонецЕсли;
			
			// закрываем соединение
			Соединение = Неопределено;
			
		Иначе
			ФайлНастроек = ПолучитьЗакэшированныйФайлНастроек(
				УчетнаяЗапись,
				ПараметрыСоединенияССервером,
				ДатаОбновленияФайлаНастроекНаСервере,
				ФайлНастроекРасшифрован,
				Сообщения);
		КонецЕсли;
		
		Если НЕ РегламентированнаяОтчетностьКлиентСервер.ЭтоИмяВременногоФайла(ФайлНастроек) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		// на клиента возвращаем файл
		// Добавлена проверка на то, что файл во временной папке.
		//@skip-check 1c-fresh-audit
		ФайлНастроек = ПоместитьВоВременноеХранилище(
			Новый ДвоичныеДанные(ФайлНастроек), УникальныйИдентификатор);
		
		Возврат Новый Структура("Файл, Дата, Расшифрован, ИмяРесурса",
						ФайлНастроек, ДатаОбновленияФайлаНастроекНаСервере, ФайлНастроекРасшифрован, ПараметрыСоединенияССервером.ИмяРесурсаСНастройками);
		
	Иначе
		ВывестиСообщения(УчетнаяЗапись, Сообщения, УведомлятьТолькоОбОшибках);
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервере
Процедура ЗаполнитьСтруктуруСсылочныхДанных()
	
	СтруктураСсылочныхДанных = Новый Структура;
	СтруктураСсылочныхДанных.Вставить("Перечисления_ФорматыДокументооборотаСФНС_Приказ534", Перечисления.ФорматыДокументооборотаСФНС.Приказ534);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_НалоговаяИлиБухгалтерскаяОтчетность", Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ИОН", Перечисления.ТипыЦикловОбмена.ИОН);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ОтчетностьПФР", Перечисления.ТипыЦикловОбмена.ОтчетностьПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ОтчетностьФСГС", Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ОтчетностьЦБ", Перечисления.ТипыЦикловОбмена.ОтчетностьЦБ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ЗапросыНаИнформационноеОбслуживание", Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_НеформализованныеДокументыНалоговогоОргана", Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ПисьмоНО", Перечисления.ТипыЦикловОбмена.ПисьмоНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_НеформализованныеДокументыНалогоплательщика" , Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика );
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_НеформализованнаяПерепискаПФРИсходящие", Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_НеформализованнаяПерепискаПФРВходящие", Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_РассылкаПФР", Перечисления.ТипыЦикловОбмена.РассылкаПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ИндивидуальноеИнформированиеФСГС", Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_РассылкаФСГС", Перечисления.ТипыЦикловОбмена.РассылкаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_РассылкаШаблоновФСГС", Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ПисьменноеОбращениеВФСГС", Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_Форма2НДФЛ", Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_Рассылка", Перечисления.ТипыЦикловОбмена.Рассылка);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_РассылкаГрупповая", Перечисления.ТипыЦикловОбмена.РассылкаГрупповая);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_Заявление", Перечисления.ТипыЦикловОбмена.Заявление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_Документ", Перечисления.ТипыЦикловОбмена.Документ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ОбращениеНП", Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_Представление", Перечисления.ТипыЦикловОбмена.Представление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ИОС", Перечисления.ТипыЦикловОбмена.ИОС);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_КритическаяОшибка", Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_Прочее", Перечисления.ТипыТранспортныхСообщений.Прочее);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ДекларацияНП", Перечисления.ТипыТранспортныхСообщений.ДекларацияНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеДекларацияНО", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПодтверждениеНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеДекларацияНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаДекларацияНО", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеРезультатПриемаНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиДекларацияНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеРезультатОбработкиНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеФНС", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_Форма2НДФЛНП", Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеФорма2НДФЛНО", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеФорма2НДФЛНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаФорма2НДФЛНО", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ОбращениеНП", Перечисления.ТипыТранспортныхСообщений.ОбращениеНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеОбращениеНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаОбращениеНО", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеОбращениеНО", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоНО", Перечисления.ТипыТранспортныхСообщений.ПисьмоНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РассылкаНО", Перечисления.ТипыТранспортныхСообщений.РассылкаНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПисьмоНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеРассылкаНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ЗапросНП", Перечисления.ТипыТранспортныхСообщений.ЗапросНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеЗапросНО", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеЗапросНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаЗапросНО", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗапросНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПервичноеСообщениеСодержащееОтчетностьПФР", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеПолученияОтчетностиПФР", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПротоколПФР", Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПротоколКвитанцияПФР", Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеПФР", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоВходящееПФР", Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоВходящееКвитанцияПФР", Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоИсходящееПФР", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоИсходящееКвитанцияПФР", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПервичноеСообщениеСодержащееЗапросПФР", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеПолученияЗапросаПФР", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ОтветНаЗапросПФР", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ОтветНаЗапросКвитанцияПФР", Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПервичноеСообщениеСодержащееОтчетностьФСГС", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеДатыОтправкиФСГС", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПодтверждениеДатыОтправкиФСГС", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеОПолученииОтчетностиФСГС", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПротоколВходногоКонтроляОтчетностиФСГС", Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеФСГС", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоВходящееФСГС", Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РассылкаФСГС", Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РассылкаШаблоновФСГС", Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПисьмоВходящееФСГС", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПисьмоИсходящееФСГС", Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПисьмоИсходящееФСГС", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС);

	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_КритическаяОшибка", Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеПФР", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеФНС", Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ЗаявлениеНП", Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеЗаявлениеНО", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеЗаявлениеНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаЗаявлениеНО", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗаявлениеНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗаявлениеРФНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеРезультатОбработкиРФНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_СообщениеОбОтзывеЗаявлениеРФНО", Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеОбОтзывеЗаявлениеРФНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗаявлениеТСНО", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеРезультатОбработкиТСНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ДокументНО", Перечисления.ТипыТранспортныхСообщений.ДокументНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеДокументНП", Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаДокументНП", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеРезультатПриемаНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПредставлениеНП", Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ИзвещениеПредставлениеНО", Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатПриемаПредставлениеНО", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПодтверждениеПредставлениеНО", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ПервичноеСообщениеСодержащееОтчетностьЦБ", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьЦБ);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ФорматыДокументооборотаСФНС_Приказ534", Перечисления.ФорматыДокументооборотаСФНС.Приказ534);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ВидыЦикловОбмена_ЦиклОбменаСПФР", Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ВидыЦикловОбмена_ЦиклОбменаСФНС", Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ВидыЦикловОбмена_ЦиклОбменаСФСГС", Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ВидыЦикловОбмена_ЦиклОбменаССОС", Перечисления.ВидыЦикловОбмена.ЦиклОбменаССОС);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОтчетности", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ЭлектронноЦифроваяПодпись", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Сертификат", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Сертификат);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОписанияКонтейнера", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияКонтейнера);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеОбОтправке", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОбОтправке);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПротоколВходногоКонтроля", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколВходногоКонтроля);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_НеОпределен", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.НеОпределен);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_НеформализованныйДокумент", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.НеформализованныйДокумент);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ТекстовоеСообщениеНеформализованногоДокумента", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТекстовоеСообщениеНеформализованногоДокумента);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеНеформализованногоДокумента", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеНеформализованногоДокумента);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ЗапросИОН", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОН);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОтветНаЗапросИОН", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросИОН);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ЗашифрованноеСодержимое", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗашифрованноеСодержимое);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбУточненииНБО", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииНБО);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Протокол2НДФЛ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Протокол2НДФЛ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Реестр2НДФЛ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Реестр2НДФЛ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОтчетностиПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеПолученияОтчетностиПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПротоколПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПротоколПриложениеПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОписанияСведенийПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияСведенийПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеОшибкиПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибкиПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеОшибочногоПакетаПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибочногоПакетаПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Письмо", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПисьмоПриложение", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеПисьма", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьма);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеПолученияПисьма", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияПисьма);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ТранспортнаяИнформация", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ТранспортнаяИнформация);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеСведений", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Доверенность", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Доверенность);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеДатыОтправки", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправки);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ИзвещениеОПолучении", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолучении);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_КвитанцияОПриеме", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриеме);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтказе", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ИзвещениеОВводе", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОВводе);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбУточнении", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточнении);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеОшибкиФНС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибкиФНС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеОшибочногоПакетаФНС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибочногоПакетаФНС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Форма2НДФЛ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Форма2НДФЛ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеСведений2НДФЛ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведений2НДФЛ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СведенияПриложениеПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Обращение", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеДатыПолучения", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыПолучения);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеСведенийОбращениеПисьмоРассылка", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийОбращениеПисьмоРассылка);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Рассылка", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Рассылка);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Запрос", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Ответ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Ответ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеСведенийЗапрос", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗапрос);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Заявление", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Заявление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеСведенийЗаявление", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеСведенийЗаявление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_КвитанцияОПриемеЗаявления", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеЗаявления);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОПростОтметки", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПростОтметки);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтказеОтметки", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтметки);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОПроверке", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОПроверке);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОбОтзывеЗаявления", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОбОтзывеЗаявления);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОНесоответствиях", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СообщениеОНесоответствиях);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Документ", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеДокумент", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеДокумент);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтказеДокумент", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_КвитанцияОПриемеДокумент", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Представление", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеПредставление", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПредставление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеДатыОтправкиПредставление", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеДатыОтправкиПредставление);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ДоверенностьПредставление", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОтчетностиФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеФайлаОтчетностиФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеОператораФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеОператораФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ИзвещениеОПолученииДокументаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОПриемеВОбработкуОтчетаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОПриемеВОбработкуОтчетаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОНесоответствииФорматуОтчетаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОНесоответствииФорматуОтчетаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбУточненииОтчетаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбУточненииОтчетаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтказеОтчетаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеОтчетаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтклоненииОтчетаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтклоненииОтчетаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПисьмоФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_РассылкаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.РассылкаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ШаблонФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеРассылкиШаблоновФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеРассылкиШаблоновФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеПисьмаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПриложениеПисьмаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеОшибкиФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибкиФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОписаниеОшибочногоПакетаФСГС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеОшибочногоПакетаФСГС);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ЗапросИОС", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОписанияЗапросаПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОписанияЗапросаПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ПодтверждениеПолученияЗапросПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияЗапросПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОтветНаЗапросПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветНаЗапросПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ОтветПриложениеНаЗапросПФР", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОтветПриложениеНаЗапросПФР);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_СтатусыПроверкиЭЦП_Валидна", Перечисления.СтатусыПроверкиЭЦП.Валидна);
	СтруктураСсылочныхДанных.Вставить("Перечисления_СтатусыПроверкиЭЦП_ПустаяСсылка", Перечисления.СтатусыПроверкиЭЦП.ПустаяСсылка());
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыФайловОтчетностиПФР_ПачкаРСВ", Перечисления.ТипыФайловОтчетностиПФР.ПачкаРСВ);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоФайлов_xml", Перечисления.ТипыСодержимогоФайлов.xml);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоФайлов_html", Перечисления.ТипыСодержимогоФайлов.html);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоФайлов_Текст1251", Перечисления.ТипыСодержимогоФайлов.Текст1251);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыСодержимогоФайлов_Текст866", Перечисления.ТипыСодержимогоФайлов.Текст866);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_Включена", Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Включена);
	СтруктураСсылочныхДанных.Вставить("Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_Отключена", Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.Отключена);
	СтруктураСсылочныхДанных.Вставить("Перечисления_РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика_ПустаяСсылка", Перечисления.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.ПустаяСсылка());
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ВидыОбменаСКонтролирующимиОрганами_ОбменВУниверсальномФормате", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_СтатусыЗаявленияАбонентаСпецоператораСвязи_Подготовлено", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено);
	СтруктураСсылочныхДанных.Вставить("Перечисления_СтатусыЗаявленияАбонентаСпецоператораСвязи_Отправлено", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено);
	СтруктураСсылочныхДанных.Вставить("Перечисления_СтатусыЗаявленияАбонентаСпецоператораСвязи_Отклонено", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено);
	СтруктураСсылочныхДанных.Вставить("Перечисления_СтатусыЗаявленияАбонентаСпецоператораСвязи_Одобрено", Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКриптоПровайдеров_VipNet", Перечисления.ТипыКриптоПровайдеров.VipNet);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКриптоПровайдеров_CryptoPro", Перечисления.ТипыКриптоПровайдеров.CryptoPro);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_СпецоператорыСвязи_ПустаяСсылка", Перечисления.СпецоператорыСвязи.ПустаяСсылка());
	СтруктураСсылочныхДанных.Вставить("Перечисления_СпецоператорыСвязи_КалугаАстрал", Перечисления.СпецоператорыСвязи.КалугаАстрал);
	СтруктураСсылочныхДанных.Вставить("Перечисления_СпецоператорыСвязи_Такском", Перечисления.СпецоператорыСвязи.Такском);
	СтруктураСсылочныхДанных.Вставить("Перечисления_СпецоператорыСвязи_Прочие", Перечисления.СпецоператорыСвязи.Прочие);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ФСС", Перечисления.ТипыКонтролирующихОрганов.ФСС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ФНС", Перечисления.ТипыКонтролирующихОрганов.ФНС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ПФР", Перечисления.ТипыКонтролирующихОрганов.ПФР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ФСГС", Перечисления.ТипыКонтролирующихОрганов.ФСГС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ФСРАР", Перечисления.ТипыКонтролирующихОрганов.ФСРАР);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_РПН", Перечисления.ТипыКонтролирующихОрганов.РПН);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ФТС", Перечисления.ТипыКонтролирующихОрганов.ФТС);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтролирующихОрганов_ЦБ", Перечисления.ТипыКонтролирующихОрганов.ЦБ);
	
	СтруктураСсылочныхДанных.Вставить("Справочник_Организации_ПустаяСсылка", Справочники.Организации.ПустаяСсылка());
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыКонтактнойИнформации_АдресЭлектроннойПочты", Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыВладельцевЭЦП_Руководитель", Перечисления.ТипыВладельцевЭЦП.Руководитель);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыВладельцевЭЦП_ГлавныйБухгалтер", Перечисления.ТипыВладельцевЭЦП.ГлавныйБухгалтер);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыВладельцевЭЦП_ДругойСотрудник", Перечисления.ТипыВладельцевЭЦП.ДругойСотрудник);
	
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП", Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыТранспортныхСообщений_РезультатОбработкиЗапросаНаВыпискуЕГРЮЛ_ЕГРИП", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП);
	СтруктураСсылочныхДанных.Вставить("Перечисления_ТипыЦикловОбмена_ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП", Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЭтоФорматПриказ534(ЦиклОбмена) Экспорт
	
	Возврат ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьДокументыЦикловОбмена(
		ПечатаемыеЦиклыОбмена, 
		ЭтоПечатьБезВыбораВидаПротоколов = Ложь, 
		ПечатаемыеТранспортныеСообщения = Неопределено, 
		РезультатНастройки = Неопределено) Экспорт
	
	Если ТипЗнч(ПечатаемыеЦиклыОбмена) <> Тип("Массив") Тогда
		ЦиклыОбмена = Новый Массив;
		ЦиклыОбмена.Добавить(ПечатаемыеЦиклыОбмена);
	Иначе
		ЦиклыОбмена = ПечатаемыеЦиклыОбмена;
	КонецЕсли;
	Если ЦиклыОбмена.Количество() = 0 Тогда
		ПоказатьПредупреждение(, "Не выбраны циклы обмена для печати!");
		Возврат;
	КонецЕсли;
	
	Если ЭтоПечатьБезВыбораВидаПротоколов Тогда
		
		// Печатаем без выбора вида протоколов
		Если ТипЗнч(ПечатаемыеТранспортныеСообщения) <> Тип("Массив") И ПечатаемыеТранспортныеСообщения <> Неопределено Тогда
			ТранспортныеСообщения = Новый Массив;
			ТранспортныеСообщения.Добавить(ПечатаемыеТранспортныеСообщения);
		Иначе
			ТранспортныеСообщения = ПечатаемыеТранспортныеСообщения;
		КонецЕсли;
		
		Если РезультатНастройки = Неопределено Тогда 
			РезультатНастройки = ПротоколыДляПечати(ЦиклыОбмена, ТранспортныеСообщения);
		КонецЕсли;
		 
	 	// Определяем, есть ли протоколы для печати
		ЕстьПротоколыДляПечати = Ложь; 
		Для каждого ВыбранныйПротокол Из РезультатНастройки Цикл
			Если ВыбранныйПротокол.Значение = Истина Тогда
				ЕстьПротоколыДляПечати = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если НЕ ЕстьПротоколыДляПечати Тогда
			ПредупредитьОбОтсутствииПротокола();
			Возврат;
		КонецЕсли;
		
		Если ТранспортныеСообщения <> Неопределено 
			И ТипЗнч(ТранспортныеСообщения) = Тип("Массив")
			И ТранспортныеСообщения.Количество() = 1 Тогда
			РезультатНастройки.Вставить("ТранспортноеСообщение", ТранспортныеСообщения[0]);
		КонецЕсли;

		СформироватьИПоказатьПротоколы(РезультатНастройки, ЦиклыОбмена);
		 
	Иначе
		// Печатаем с выбором вида протокола
		
		// анализируем формат документооборота
		// для ФНС 534 вызываем отдельную форму настройки
		Если ЭтоФорматПриказ534(ЦиклыОбмена[0]) Тогда 
			
			Оповещение = Новый ОписаниеОповещения("СформироватьИПоказатьПротоколы", ЭтотОбъект, ЦиклыОбмена);
			
			СтруктураПараметров = Новый Структура("ЦиклыОбмена", ЦиклыОбмена);
			ОткрытьФорму(ПутьКОбъекту + ".Форма.НастройкиГрупповойПечати534", СтруктураПараметров,  ,,,, Оповещение, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
			
		Иначе
				
			Оповещение = Новый ОписаниеОповещения("СформироватьИПоказатьПротоколы", ЭтотОбъект, ЦиклыОбмена);
			
			СтруктураПараметров = Новый Структура("ЦиклыОбмена", ЦиклыОбмена);
			ОткрытьФорму(ПутьКОбъекту + ".Форма.НастройкиГрупповойПечати", СтруктураПараметров, ,,,, Оповещение, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбОтсутствииПротокола() Экспорт
	
	ТекстПредупреждения = НСтр("ru = 'Просмотр протоколов возможен только после отправки
					|и получения ответа от контролирующих органов'");
	ПоказатьПредупреждение(,ТекстПредупреждения);
	
КонецПроцедуры


&НаСервере
Функция ПротоколыДляПечати(ЦиклыОбмена = Неопределено, ТранспортныеСообщения = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПротоколыДляПечати(ЦиклыОбмена, ТранспортныеСообщения);

КонецФункции

&НаКлиенте
Процедура СформироватьИПоказатьПротоколы(РезультатНастройки, ЦиклыОбмена) Экспорт
	
	Если РезультатНастройки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтоФорматПриказ534(ЦиклыОбмена[0]) Тогда
		РезультатНастройки.Вставить("ФорматДокументооборота", СтруктураСсылочныхДанных["Перечисления_ФорматыДокументооборотаСФНС_Приказ534"]);
	КонецЕсли;
	
	Для Инд = 1 По ЦиклыОбмена.Количество() Цикл
		СформироватьИПоказатьПечатныеДокументы(ЦиклыОбмена[Инд - 1], РезультатНастройки);
		ОбработкаПрерыванияПользователя();
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтчетыОСостоянииЦикловОбмена(ПечатаемыеЦиклыОбмена) Экспорт
	
	Если ТипЗнч(ПечатаемыеЦиклыОбмена) <> Тип("Массив") Тогда
		ЦиклыОбмена = Новый Массив;
		ЦиклыОбмена.Добавить(ПечатаемыеЦиклыОбмена);
	Иначе
		ЦиклыОбмена = ПечатаемыеЦиклыОбмена;
	КонецЕсли;
	
	Для Инд = 1 По ЦиклыОбмена.Количество() Цикл
		СформироватьИПоказатьПечатныеДокументы(ЦиклыОбмена[Инд - 1], Новый Структура("ПечататьСостояние", Истина));
		ОбработкаПрерыванияПользователя();
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИПоказатьПечатныеДокументы(ЦиклОбмена, Настройки) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ЦиклОбмена", ЦиклОбмена);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СформироватьИПоказатьПечатныеДокументыПослеПолученияПечатныхДокументов",
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	// Настройки дополняем недостающими свойствами
	ДополнитьНастройкиПечати(Настройки);
	
	СформироватьПечатныеДокументы(ЦиклОбмена, Настройки, ОписаниеОповещения);
	
КонецПроцедуры

&НаСервере
Процедура ДополнитьНастройкиПечати(Настройки) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ДополнитьНастройкиПечати(Настройки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИПоказатьПечатныеДокументыПослеПолученияПечатныхДокументов(ПечатныеДокументы, ВходящийКонтекст) Экспорт
	
	Если ПечатныеДокументы = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	#Если ВебКлиент Тогда
		ЭтоВебКлиент = Истина;
	#Иначе
		ЭтоВебКлиент = Ложь;
	#КонецЕсли
	
	ЦиклОбмена = ВходящийКонтекст.ЦиклОбмена;
	
	НачальноеЗначениеВыбора = Новый Структура("ЦиклОбмена, ПечатныеДокументы, ЭтоВебКлиент", 
		ЦиклОбмена, ПечатныеДокументы, ЭтоВебКлиент);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредварительныйПросмотрПечатныхФорм", НачальноеЗначениеВыбора,,Новый УникальныйИдентификатор);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьРеквизитНаСервере(ОбъектДляЧтения, ИмяРеквизита) Экспорт
	МетаданныеОбъекта = ОбъектДляЧтения.Метаданные();
	Если МетаданныеОбъекта.Реквизиты.Найти(ИмяРеквизита) <> Неопределено Тогда 
		Возврат ОбъектДляЧтения[ИмяРеквизита];
	Иначе
		Возврат Неопределено;
	КонецЕсли;
КонецФункции

&НаСервере
Процедура ПоместитьПодписиВТаблицу()

	// помещаем подписи абонента в результирующую таблицу
	Если ЗначениеЗаполнено(ПодписиАбонента) Тогда
		Для Каждого ПодписьАбонента Из ПодписиАбонента Цикл
			
			Если ТаблицаПодписей.НайтиСтроки(Новый Структура("Файл", ПодписьАбонента.Файл)).Количество() = 0 Тогда
				
				НовСтр = ТаблицаПодписей.Добавить();
				НовСтр.ИмяФайла = ПодписьАбонента.ИмяФайла;
				НовСтр.Файл     = ПодписьАбонента.Файл;
				НовСтр.ТранспортноеСообщение = ПодписьАбонента.ТранспортноеСообщение;
				НовСтр.ЭЦПЭтоПодписьАбонента = Истина;
				
				Отправитель = ПолучитьРеквизит(ПодписьАбонента.ТранспортноеСообщение, "Отправитель");
				ОтправительЭтоАбонент = ТипЗнч(Отправитель) = Тип("СправочникСсылка.Организации");
				
				Если ОтправительЭтоАбонент Тогда
					НовСтр.Порядок = 1;
				Иначе
					НовСтр.Порядок = 2;
				КонецЕсли;
				
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// помещаем подписи контрагента в результирующую таблицу
	Если ЗначениеЗаполнено(ПодписиКонтрагента) Тогда
			
		Для Каждого ПодписьКонтрагента Из ПодписиКонтрагента Цикл
			Если ТаблицаПодписей.НайтиСтроки(Новый Структура("Файл", ПодписьКонтрагента.Файл)).Количество() = 0 Тогда
				
				НовСтр = ТаблицаПодписей.Добавить();
				НовСтр.ИмяФайла = ПодписьКонтрагента.ИмяФайла;
				НовСтр.Файл     = ПодписьКонтрагента.Файл;
				НовСтр.ТранспортноеСообщение = ПодписьКонтрагента.ТранспортноеСообщение;
				НовСтр.ЭЦПЭтоПодписьАбонента = Ложь;
				
				Отправитель = ПолучитьРеквизит(ПодписьКонтрагента.ТранспортноеСообщение, "Отправитель");
				ОтправительЭтоНеАбонент = ТипЗнч(Отправитель) <> Тип("СправочникСсылка.Организации");
				
				Если ОтправительЭтоНеАбонент Тогда
					НовСтр.Порядок = 1;
				Иначе
					НовСтр.Порядок = 2;
				КонецЕсли;
				
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	ТаблицаПодписей.Сортировать("ТранспортноеСообщение, Порядок");

КонецПроцедуры
               
&НаСервереБезКонтекста
Функция ПолучитьСведенияДоверенности(РегистрацияВИФНС, Организация = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСведенияДоверенности(РегистрацияВИФНС, Организация);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьФИОДоверенности(Доверенность, ТипСубъекта)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьФИОДоверенности(Доверенность, ТипСубъекта);
	
КонецФункции

&НаСервереБезКонтекста
Функция УчетнаяЗаписьОрганизации(Организация) Экспорт
	
	Возврат ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.УчетнаяЗаписьОрганизации(Организация);
	
КонецФункции

&НаСервереБезКонтекста
Функция НастройкиУчетнойЗаписиОрганизации(Организация) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НастройкиУчетнойЗаписиОрганизации(Организация);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ИзвлечьЭлектронноеПредставлениеИзХранилища(Владелец, ИмяФайла, Данные, ВозможныеТипыФайла = Неопределено) Экспорт
	
	ТипыПриложенийБО = Новый Массив;
	ТипыПриложенийБО.Добавить(Перечисления.ТипыФайловБухОтчетности.АудиторскоеЗаключение);
	ТипыПриложенийБО.Добавить(Перечисления.ТипыФайловБухОтчетности.ЗаявлениеСоюзаСельхозПроизводителей);
	ТипыПриложенийБО.Добавить(Перечисления.ТипыФайловБухОтчетности.ПояснительнаяЗаписка);
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла,
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.Данные
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление)
						  |				И	(СостояниеОбъектов.ИмяФайла = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов))
						  |
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление
	                      |	И НЕ (ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ТипФайлаОтчетности В(&ТипыПриложенийБО))");
	Запрос.УстановитьПараметр("ТипыПриложенийБО", ТипыПриложенийБО);
	Если ЗначениеЗаполнено(ВозможныеТипыФайла) Тогда
		Запрос.Текст = Запрос.Текст + "
	                      |	И ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ТипФайлаОтчетности В (&ВозможныеТипыФайла)";
		Запрос.УстановитьПараметр("ВозможныеТипыФайла", ВозможныеТипыФайла);
	КонецЕсли;
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		Выборка = РезультатЗапроса.Выбрать();
		Если Выборка.Следующий() Тогда
			ИмяФайла = СокрЛП(Выборка.ИмяФайла);
			Если Выборка.ВАрхиве Тогда
				Данные = Неопределено;
				Возврат;
			КонецЕсли;
			Данные = Выборка.Данные;
			Возврат;
		КонецЕсли;
	КонецЕсли;
	ИмяФайла = "";
	Данные = Неопределено;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ИзвлечьЭлектронныеПредставленияИзХранилища(Владелец, Знач ЛимитЗаписей = 0) Экспорт
	
	ТаблицаРезультат = Новый ТаблицаЗначений;
	ТаблицаРезультат.Колонки.Добавить("ИмяФайла");
	ТаблицаРезультат.Колонки.Добавить("Данные");
	ТаблицаРезультат.Колонки.Добавить("ТипФайлаОтчетности");
	ТаблицаРезультат.Колонки.Добавить("ВАрхиве");
	
	Запрос = Новый Запрос;
	ТекстЗапроса = "ВЫБРАТЬ РАЗРЕШЕННЫЕ //$Ограничение
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла,
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ТипФайлаОтчетности
						  |	, ВЫБОР
						  |		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА Ложь
						  |		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА Ложь
						  |	ИНАЧЕ
						  |		Истина
						  |	КОНЕЦ ВАрхиве
	                      |ИЗ
	                      |	РегистрСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов КАК ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов
						  |
						  |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						  |			ПО 		(СостояниеОбъектов.Объект = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление)
						  |				И	(СостояниеОбъектов.ИмяФайла = ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ИмяФайла)
						  |				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов))
						  |
	                      |ГДЕ
	                      |	ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.ЭлектронноеПредставление = &ЭлектронноеПредставление";
	
	Если ЛимитЗаписей > 0 Тогда
		ТекстОграничения = "ПЕРВЫЕ " + СокрЛП(Формат(ЛимитЗаписей, "ЧДЦ=; ЧН=; ЧГ="));
	Иначе
		ТекстОграничения = "";
	КонецЕсли;
	
	Запрос.Текст = СтрЗаменить(ТекстЗапроса, "//$Ограничение", ТекстОграничения);	 
	
	Запрос.УстановитьПараметр("ЭлектронноеПредставление", Владелец);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Количество() = 0 Тогда
		Возврат Неопределено;
	Иначе
		
		Пока Выборка.Следующий() Цикл
			НоваяСтрока = ТаблицаРезультат.Добавить();
			НоваяСтрока.ИмяФайла 			= Выборка.ИмяФайла;
			НоваяСтрока.ТипФайлаОтчетности 	= Выборка.ТипФайлаОтчетности;
			НоваяСтрока.ВАрхиве 			= Выборка.ВАрхиве;
			Если Выборка.ВАрхиве Тогда
				НоваяСтрока.Данные 			= Base64Значение("");
			Иначе
				НоваяСтрока.Данные 			= ДанныеОдногоФайлаЭлектронногоПредставления(Владелец, Выборка.ИмяФайла);
			КонецЕсли;
		КонецЦикла;
		
		Возврат ТаблицаРезультат;
		
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Процедура НараститьСчетчикВИмениФайлаВыгрузкиПриНеобходимости(ИмяФайла, Организация, ДатаОкончанияПериода, КодНО)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.НараститьСчетчикВИмениФайлаВыгрузкиПриНеобходимости(ИмяФайла, Организация, ДатаОкончанияПериода, КодНО);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ИзвлечьЭлектронноеПредставлениеИзХранилищаИНараститьСчетчикПриНеобходимости(Контекст, ТекстВыгрузки, ИмяФайлаВыгрузки)
	
	// готовим массив возможных типов файлов
	ВозможныеТипыФайлов = Новый Массив;
	ВозможныеТипыФайлов.Добавить(Неопределено);
	ВозможныеТипыФайлов.Добавить(Перечисления.ТипыФайловБухОтчетности.ОсновнойФайлОтчетности);
	ВозможныеТипыФайлов.Добавить(Перечисления.ТипыФайловБухОтчетности.ПустаяСсылка());
	
	// получаем электронное представление
	ХранилищеВыгрузки = "";
	ИмяФайлаВыгрузки = "";
	ИзвлечьЭлектронноеПредставлениеИзХранилища(Контекст.Документ, ИмяФайлаВыгрузки, ХранилищеВыгрузки, ВозможныеТипыФайлов);
	Если ИмяФайлаВыгрузки = "" ИЛИ ХранилищеВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не обнаружена информация для отправки!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// пишем электронное представление в файл
	ВремФайл = ПолучитьИмяВременногоФайла();
	ХранилищеВыгрузки.Получить().Записать(ВремФайл);
	ОбъектЧтениеТекста = Новый ЧтениеТекста(ВремФайл, ?(нрег(Прав(ИмяФайлаВыгрузки, 4)) = ".xml", "windows-1251", "cp866"));
	ТекстВыгрузки = ОбъектЧтениеТекста.Прочитать();
	ОбъектЧтениеТекста.Закрыть();
	УдалитьФайлы(ВремФайл);
	
	// наращиваем счетчики в имени файла выгрузки и в содержимом
	НараститьСчетчикДокументовПриНеобходимости(ТекстВыгрузки, ИмяФайлаВыгрузки, Контекст.Организация, Контекст.ДатаОкончанияПериодаОтчета);
	НараститьСчетчикВИмениФайлаВыгрузкиПриНеобходимости(ИмяФайлаВыгрузки, Контекст.Организация, Контекст.Документ.ДатаОкончания, Контекст.КодНалоговогоОргана);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИзвлечьЭлектронноеПредставлениеФСГСИзХранилища(Контекст, ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайла = Неопределено)
	
	// получаем электронное представление
	ХранилищеВыгрузки = "";
	ИмяФайлаВыгрузки = "";
	ИзвлечьЭлектронноеПредставлениеИзХранилища(Контекст.Документ, ИмяФайлаВыгрузки, ХранилищеВыгрузки);
	Если ИмяФайлаВыгрузки = "" ИЛИ ХранилищеВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не обнаружена информация для отправки!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// пишем электронное представление в файл
	ВремФайл = ПолучитьИмяВременногоФайла();
	ДанныеПредставления = ХранилищеВыгрузки.Получить();
	ДанныеПредставления.Записать(ВремФайл);
	
	Если СтрНайти(ИмяФайлаВыгрузки, "NO_BUH") = 1 ИЛИ СтрНайти(ИмяФайлаВыгрузки, "NO_BOUPR") = 1 ИЛИ СтрНайти(ИмяФайлаВыгрузки, "NO_BOUCH") = 1 Тогда
		КодировкаФайла = "windows-1251";
	Иначе
		КодировкаФайла = ОпределитьКодировку(ДанныеПредставления);
	КонецЕсли;
	ОбъектЧтениеТекста = Новый ЧтениеТекста(ВремФайл, КодировкаФайла);
	ТекстВыгрузки = ОбъектЧтениеТекста.Прочитать();
	ОбъектЧтениеТекста.Закрыть();
	УдалитьФайлы(ВремФайл);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Процедура НараститьСчетчикДокументовПриНеобходимости(ТекстВыгрузки, ИмяФайла, Организация, ДатаОкончанияПериода)
	
	РасширениеСТочкой = нрег(Прав(ИмяФайла, 4));
	Если РасширениеСТочкой = ".xml" Тогда
		Возврат;
	КонецЕсли;
	
	КлючеваяФраза = Символы.ПС + "ИдДок:";
	Для Стр = 1 По СтрЧислоСтрок(ТекстВыгрузки) Цикл
		ТекСтр = СтрПолучитьСтроку(ТекстВыгрузки, Стр);
		Если Лев(ТекСтр, 6) <> "ИдДок:" Тогда
			Продолжить;
		КонецЕсли;
		СчетчикДокументов = РегламентированнаяОтчетность.ПолучитьСчетчикВыгруженныхДокументов(Организация, ДатаОкончанияПериода) + 1;
		ЭтоАлко = (СтрНайти(ТекстВыгрузки, Символы.ПС + "ТипИнф:11520") <> 0);
		Если ЭтоАлко Тогда
			НовСтр = Лев(ТекСтр,СтрДлина(ТекСтр) - 4) + Прав(Формат(СчетчикДокументов, "ЧДЦ=0; ЧВН=; ЧГ=0"), 2) + Прав(ТекСтр, 2);
		Иначе
			НовСтр = Лев(ТекСтр,СтрДлина(ТекСтр) - 8) + Формат(СчетчикДокументов, "ЧЦ=8; ЧВН=; ЧГ=0");
		КонецЕсли;
		ТекстВыгрузки = СтрЗаменить(ТекстВыгрузки, Символы.ПС + ТекСтр, Символы.ПС + НовСтр);
		РегламентированнаяОтчетность.УстановитьСчетчикВыгруженныхДокументов(Организация, СчетчикДокументов, ДатаОкончанияПериода);
		Прервать;
	КонецЦикла;
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция РегистрацияВИФНСПоОрганизацииИКодуНО(
		Организация,
		КодНО,
		КПП = "",
		ЭтоЮридическоеЛицо = Неопределено,
		СведенияОбОрганизации = Неопределено,
		ЛюбойКПП = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РегистрацияВИФНСПоОрганизацииИКодуНО(
		Организация,
		КодНО,
		КПП,
		ЭтоЮридическоеЛицо,
		СведенияОбОрганизации,
		ЛюбойКПП);
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьДоверенностьИНараститьСчетчикФайловПриНеобходимости(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьДоверенностьИНараститьСчетчикФайловПриНеобходимости(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСвойстваФайлаВыгрузки(
		ОбъектСсылка,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		УникальныйИдентификатор = Неопределено,
		Приложения = Неопределено)
	
	СвойстваФайлаВыгрузки = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ВыгрузитьДокументСервер(
		ОбъектСсылка,
		УникальныйИдентификатор);
	Если СвойстваФайлаВыгрузки = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТипЗнч(СвойстваФайлаВыгрузки) = Тип("Массив")
		ИЛИ ТипЗнч(СвойстваФайлаВыгрузки) = Тип("ФиксированныйМассив") Тогда
		
		МассивФайловВыгрузки = СвойстваФайлаВыгрузки;
		СвойстваОсновногоФайлаВыгрузки = Неопределено;
		Для каждого СвойстваФайлаВыгрузки Из МассивФайловВыгрузки Цикл
			Если СтрНачинаетсяС(СвойстваФайлаВыгрузки.ТипФайлаВыгрузки, "Приложение") Тогда
				Приложение = Новый Структура;
				Приложение.Вставить("ТипФайлаВыгрузки", СвойстваФайлаВыгрузки.ТипФайлаВыгрузки);
				Приложение.Вставить("АдресФайлаВыгрузки", СвойстваФайлаВыгрузки.АдресФайлаВыгрузки);
				Приложение.Вставить("ИмяФайлаВыгрузки", СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки);
				Приложение.Вставить("КодировкаФайлаВыгрузки", СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки);
				Если ТипЗнч(Приложения) <> Тип("Массив") Тогда
					Приложения = Новый Массив;
				КонецЕсли;
				Приложения.Добавить(Приложение);
				
			Иначе
				СвойстваОсновногоФайлаВыгрузки = СвойстваФайлаВыгрузки;
			КонецЕсли;
		КонецЦикла;
		
		СвойстваФайлаВыгрузки = СвойстваОсновногоФайлаВыгрузки;
	КонецЕсли;
	
	Если СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки = КодировкаТекста.ANSI Тогда
		КодировкаФайлаВыгрузки = "windows-1251";
	ИначеЕсли СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки = КодировкаТекста.OEM Тогда
		КодировкаФайлаВыгрузки = "cp866";
	Иначе
		КодировкаФайлаВыгрузки = СвойстваФайлаВыгрузки.КодировкаФайлаВыгрузки;
	КонецЕсли;
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(СвойстваФайлаВыгрузки.АдресФайлаВыгрузки);
	ДвоичныеДанныеФайла.Записать(ВремФайл);
	ОбъектЧтениеТекста = Новый ЧтениеТекста(ВремФайл, КодировкаФайлаВыгрузки);
	
	ТекстВыгрузки = ОбъектЧтениеТекста.Прочитать();
	ОбъектЧтениеТекста.Закрыть();
	УдалитьФайлы(ВремФайл);
	
	ИмяФайлаВыгрузки = СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправки(Контекст, ВыполняемоеОповещение, ДополнительныеДанные = Неопределено)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	
	// выгружаем отчет
	ТипОтправляемогоДокумента = Контекст.ТипОтправляемогоДокумента;
	Приложения = Неопределено;
	Если ТипОтправляемогоДокумента = "РегламентированныйОтчет"
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"СправкиНДФЛДляПередачиВНалоговыйОрган")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"ЗаявлениеОПодтвержденииПраваНаЗачетАвансовПоНДФЛ")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"СведенияОДоходахСотрудникаДляСоцВыплат") Тогда
		
		// выгружаем
		ТекстВыгрузки = "";
		ИмяФайлаВыгрузки = "";
		КодировкаФайлаВыгрузки = "";
		ТипФайлаВыгрузки = "";
		РезультатВыгрузки = СдачаОтчетностиЧерезСервисСпецоператораКлиент.ВыгрузитьРегламентированныйОтчет(
			Контекст.Документ,
			Ложь,
			ТекстВыгрузки,
			ИмяФайлаВыгрузки,
			КодировкаФайлаВыгрузки,
			ТипФайлаВыгрузки,
			Приложения,
			Контекст.УникальныйИдентификатор);
		Если НЕ РезультатВыгрузки Тогда
			
			ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
			
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВыполняемоеОповещение) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
			Иначе
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			КонецЕсли;
			
			Возврат;
			
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ТекстВыгрузки) И ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") И ИмяФайлаВыгрузки.Количество() = 1
			И ТипЗнч(Приложения) = Тип("Массив") И Приложения.Количество() > 0 И ТипЗнч(Приложения[0]) = Тип("Структура") Тогда
			ТекстВыгрузки = Приложения[0].АдресФайлаВыгрузки;
			ИмяФайлаВыгрузки = ИмяФайлаВыгрузки[0];
			ТипФайлаВыгрузки = Приложения[0].ТипФайлаВыгрузки;
			Приложения = Неопределено;
		КонецЕсли;
		
	ИначеЕсли ТипОтправляемогоДокумента = "УведомлениеОПолучателеДокументов" Тогда
		
		Если ДополнительныеДанные = Неопределено Тогда
			СертификатыДолжностныхЛиц = СертификатыДолжностныхЛицПолучателяДокументов(Контекст.Документ);
			
			Если СертификатыДолжностныхЛиц <> Неопределено 
				И ЭтоЛокальнаяПодпись(СертификатыДолжностныхЛиц)
				И ЗначениеЗаполнено(СертификатыДолжностныхЛиц.ОтпечатокСертификатаДляШифрования) Тогда
				
				СвойстваСертификатаДляШифроания = Новый Структура("Отпечаток", СертификатыДолжностныхЛиц.ОтпечатокСертификатаДляШифрования);
				
				ДополнительныеПараметры = Новый Структура("Контекст, ВыполняемоеОповещение", Контекст, ВыполняемоеОповещение);
				Оповещение = Новый ОписаниеОповещения(
					"ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеПоискаСертификатаДляШифрования", ЭтотОбъект, ДополнительныеПараметры);
				КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, СвойстваСертификатаДляШифроания,, Ложь);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		ВладелецСертификатаОтветственногоЗаШифрование = ?(ДополнительныеДанные <> Неопределено,
			ДополнительныеДанные.ВладелецСертификатаОтветственногоЗаШифрование, Неопределено);
		
		ФайлУведомленияОПолучателеДокументов = СформироватьФайлУведомленияОПолучателеДокументов(
			Контекст.Документ,
			ВладелецСертификатаОтветственногоЗаШифрование,
			Контекст.УникальныйИдентификатор,
			Истина);
		
		Если ФайлУведомленияОПолучателеДокументов = Неопределено Тогда
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
		
		ТекстВыгрузки = ФайлУведомленияОПолучателеДокументов.ТекстФайлаУведомления;
		ИмяФайлаВыгрузки = ФайлУведомленияОПолучателеДокументов.ИмяФайлаУведомления;
		Приложения = Неопределено;
		
	ИначеЕсли ТипОтправляемогоДокумента = "МашиночитаемыеДоверенностиФНС"
		ИЛИ ТипОтправляемогоДокумента = "ЗаявленияОбОтзывеМЧДФНС" Тогда
		
		ОпределятьКодНалоговогоОрганаПолучателя = Истина;
		ФорматДоверенностиПоПриказуФНС 			= Истина;
		ПараметрыВозврата = Новый Структура;
		ПараметрыВозврата.Вставить("ВозвращатьИмяФайлаСРасширением", 	Истина);
		ПараметрыВозврата.Вставить("ВозвращатьПриложеннуюПодпись", 		Истина);
		Если ТипОтправляемогоДокумента = "МашиночитаемыеДоверенностиФНС" Тогда
			РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьМЧД(
				Контекст.Документ,,,
				ОпределятьКодНалоговогоОрганаПолучателя,
				ФорматДоверенностиПоПриказуФНС,
				Истина,
				ПараметрыВозврата);
		Иначе
			РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьЗаявлениеОбОтзывеМЧДФНС(
				Контекст.Документ,,,
				ОпределятьКодНалоговогоОрганаПолучателя,
				ФорматДоверенностиПоПриказуФНС,
				Истина,
				ПараметрыВозврата);
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(РезультатыВыгрузки) Тогда
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
		
		Если ТипЗнч(РезультатыВыгрузки) = Тип("Массив") Тогда
			Если РезультатыВыгрузки.Количество() <> 0 Тогда
				ТекстВыгрузки 		= РезультатыВыгрузки[0].Адрес;
				ИмяФайлаВыгрузки 	= РезультатыВыгрузки[0].Имя;
				Приложения 			= Новый Массив;
				Для ИндексПриложения = 1 По РезультатыВыгрузки.Количество() - 1 Цикл
					Приложение = Новый Структура;
					Приложение.Вставить("АдресФайлаВыгрузки", 		РезультатыВыгрузки[ИндексПриложения].Адрес);
					Приложение.Вставить("ИмяФайлаВыгрузки", 		РезультатыВыгрузки[ИндексПриложения].Имя);
					Приложение.Вставить("ТипФайлаВыгрузки", 		"");
					Приложение.Вставить("КодировкаФайлаВыгрузки", 	"");
					Приложения.Добавить(Приложение);
				КонецЦикла;
			КонецЕсли;
			
		Иначе
			ТекстВыгрузки 		= РезультатыВыгрузки.Содержимое;
			ИмяФайлаВыгрузки 	= РезультатыВыгрузки.ИмяФайла;
			Приложения 			= Неопределено;
		КонецЕсли;
		
	ИначеЕсли ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"БухгалтерскийОтчетГосударственногоУчреждения")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"ЗаявлениеОВвозеТоваров")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"УведомлениеОбОстаткахПрослеживаемыхТоваров")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"УведомлениеОВвозеПрослеживаемыхТоваров")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"УведомлениеОПеремещенииПрослеживаемыхТоваров")
		ИЛИ ТипОтправляемогоДокумента =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных(
		"ДокументацияПоКонтролируемымСделкам") Тогда
		
		// выгружаем
		ТекстВыгрузки = "";
		ИмяФайлаВыгрузки = "";
		Приложения = Неопределено;

		РезультатВыгрузки = ПолучитьСвойстваФайлаВыгрузки(
			Контекст.Документ,
			ТекстВыгрузки,
			ИмяФайлаВыгрузки,
			Контекст.УникальныйИдентификатор,
			Приложения);
		Если НЕ РезультатВыгрузки Тогда
			
			ТекстОшибки = ИнформацияОбОшибке().Описание;
			Если ЗначениеЗаполнено(ТекстОшибки) Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			КонецЕсли;
			
			ТекстСообщения = НСтр("ru = 'Не удалось получить файл выгрузки. Отправка отчета прервана.'");
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
				
			Иначе
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			КонецЕсли;

			Возврат;
		КонецЕсли;
		
	Иначе // внешнее электронное представление
		
		// выгружаем
		ТекстВыгрузки = "";
		ИмяФайлаВыгрузки = "";
		Приложения = ПолучитьПриложенияКОтчету(Контекст.Документ);
		Если НЕ ИзвлечьЭлектронноеПредставлениеИзХранилищаИНараститьСчетчикПриНеобходимости(Контекст, ТекстВыгрузки, ИмяФайлаВыгрузки) Тогда
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	// 
	Контекст.Вставить("ТекстВыгрузки", ТекстВыгрузки);
	Контекст.Вставить("ИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
	
	ПрефиксИмениФайла = Лев(ИмяФайлаВыгрузки, 9);
	Если ВРег(ПрефиксИмениФайла) = "NO_NDFL2_" Тогда
		Если ЭтоАдресВременногоХранилища(ТекстВыгрузки) Тогда
			ДанныеВыгрузки = ПолучитьИзВременногоХранилища(ТекстВыгрузки);
			СтрокаВыгрузки = ПолучитьСтрокуИзДвоичныхДанных(ДанныеВыгрузки, "windows-1251");
		Иначе
			СтрокаВыгрузки = ТекстВыгрузки;
		КонецЕсли;
		// С 1 июля 2023 2-НДФЛ с форматом, действовавшим по 2015 год включительно, отправлять письмом (ДО 02)
		Если (СтрНайти(СтрокаВыгрузки, "ВерсФорм=""5.01""") <> 0 ИЛИ СтрНайти(СтрокаВыгрузки, "ВерсФорм=""5.02""") <> 0
			ИЛИ СтрНайти(СтрокаВыгрузки, "ВерсФорм=""5.03""") <> 0 ИЛИ СтрНайти(СтрокаВыгрузки, "ВерсФорм=""5.04""") <> 0)
			И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.2-НДФЛ 2015 ДО 02") Тогда
			
			ДополнительныеПараметры = Новый Структура("Контекст", Контекст);
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеПредупреждения2НДФЛ",
				ЭтотОбъект,
				ДополнительныеПараметры);
			ПоказатьПредупреждение(ОписаниеОповещения,
				НСтр("ru = 'Внимание! 2-НДФЛ в формате, действовавшем по 2015 год включительно, принимается налоговым органом в виде письма.
						   |Далее откроется письмо c файлом выгрузки 2-НДФЛ, письмо следует отправить в налоговую. Протокол обработки и реестр поступят ответным письмом налоговой.'"));
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Неопределено);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Приложения) Тогда
		ИндексПриложения = 0;
		Пока ИндексПриложения < Приложения.Количество() Цикл
			Если Приложения[ИндексПриложения].ТипФайлаВыгрузки = "ПояснительнаяЗапискаКЕНВД" Тогда
				ТекстСообщения = НСтр("ru = 'Внимание! Далее откроется письмо с пояснительной запиской в формате PDF, письмо следует отправить в налоговую для завершения отправки отчета.'");
				Контекст.Вставить("ДополнительноеСообщениеПриУдачнойОтправке", ТекстСообщения);
				
				ДополнительныеРеквизитыДляОтправки = Новый Структура("КодНалоговогоОргана", Контекст.КодНалоговогоОргана);
				Контекст.Вставить("ДополнительныеРеквизитыДляОтправкиПриУдачнойОтправке", ДополнительныеРеквизитыДляОтправки);
				
				ДополнительныеВложенияДляОтправки = Новый СписокЗначений;
				ДополнительныеВложенияДляОтправки.Добавить(Приложения[ИндексПриложения].АдресФайлаВыгрузки,
					Приложения[ИндексПриложения].ИмяФайлаВыгрузки);
				Контекст.Вставить("ДополнительныеВложенияДляОтправкиПриУдачнойОтправке", ДополнительныеВложенияДляОтправки);
				
				Приложения.Удалить(ИндексПриложения);
				
			Иначе
				ИндексПриложения = ИндексПриложения + 1;
			КонецЕсли;
		КонецЦикла;
	
		Контекст.Вставить("Приложения", Приложения);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеВыгрузкиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	// выгружаем приложения к бухгалтерской отчетности
	ВыгрузитьПриложенияКБухгалтерскойОтчетности(Контекст, ОписаниеОповещения);

КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеПоискаСертификатаДляШифрования(Результат, ДополнительныеПараметры) Экспорт
	
	Контекст 				= ДополнительныеПараметры.Контекст;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ВладелецСертификатаОтветственногоЗаШифрование = ?(Результат.Выполнено И Результат.СертификатНайден,
		Результат.СвойстваСертификата.Владелец, Неопределено);
	ДополнительныеДанные = Новый Структура("ВладелецСертификатаОтветственногоЗаШифрование", ВладелецСертификатаОтветственногоЗаШифрование);
	
	ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправки(Контекст, ВыполняемоеОповещение, ДополнительныеДанные);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьПриложенияКОтчету(ЭлектронноеПредставление) 
	
	ВсеФайлы = ИзвлечьЭлектронныеПредставленияИзХранилища(ЭлектронноеПредставление);
	
	Если ВсеФайлы = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		Приложения = Новый Массив;
		
		Для Каждого Выборка Из ВсеФайлы Цикл
			
			Если Выборка.ТипФайлаОтчетности <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение Тогда
				Продолжить;
			КонецЕсли;
			
			Если Выборка.ВАрхиве Тогда
				ДанныеПредставления = Base64Значение("");
			Иначе
				ДанныеПредставления = Выборка.Данные;
			КонецЕсли;
		
			Приложение = Новый Структура("ТипФайлаВыгрузки, АдресФайлаВыгрузки, ИмяФайлаВыгрузки, КодировкаФайлаВыгрузки", "", "", "", "");
			Приложение.ИмяФайлаВыгрузки = Выборка.ИмяФайла;			
			Приложение.АдресФайлаВыгрузки = ПоместитьВоВременноеХранилище(ДанныеПредставления, Новый УникальныйИдентификатор);
			Приложения.Добавить(Приложение);
		КонецЦикла;
		
		Возврат Приложения;
	КонецЕсли;
		
КонецФункции

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеВыгрузкиЗавершение(Результат, ДополнительныеПараметры) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Контекст = Результат.Контекст;
	РезультатВыгрузки = Результат.РезультатВыгрузки;
	
	Если РезультатВыгрузки Тогда
		// выгружаем доверенность
		Если НЕ ВыгрузитьДоверенностьИНараститьСчетчикФайловПриНеобходимости(Контекст) Тогда
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
		
		// корректируем сведения о подписантах в файле бухгалтерской отчетности, при необходимости
		ИзменитьСоставТеговПодписантВБухгалтерскойОтчетностиПриНеобходимости(Контекст);
		
		Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
	Иначе
		Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьСоставТеговПодписантВБухгалтерскойОтчетностиПриНеобходимости(Контекст)
	
	// определяем, является ли файл бухотчетностью
	ПрефиксИмениФайлаБО = "NO_BUHOTCH_";
	Если Лев(Контекст.ИмяФайлаВыгрузки, СтрДлина(ПрефиксИмениФайлаБО)) <> ПрефиксИмениФайлаБО Тогда
		Возврат;
	КонецЕсли;
	
	// модифицируем файл выгрузки
	ИзменитьСоставТеговПодписантВБухгалтерскойОтчетностиПриНеобходимостиНаСервере(Контекст);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьФИОПодписантовИзСвойствУчетнойЗаписи(Контекст)
	
	// составляем массив отпечатков сертификатов подписантов
	СертификатыПодписантов = Новый Массив;
	СертификатыПодписантов.Добавить(Контекст.ОбъектСертификатРуководителя);
	Если ЗначениеЗаполнено(Контекст.СертификатГлавногоБухгалтера) И Контекст.СертификатГлавногоБухгалтера <> Контекст.СертификатРуководителя Тогда
		СертификатыПодписантов.Добавить(Контекст.ОбъектСертификатГлавногоБухгалтера);
	КонецЕсли;
	
	// получаем свойства сертификатов
	ФИОПодписантов = Новый Массив;
	Для Инд = 0 По СертификатыПодписантов.Количество() - 1 Цикл
		
		ОбъектСертификат = СертификатыПодписантов[Инд];
		
		// получаем ФИО из свойств сертификата
		СвойстваВладельца = РазобратьСтрокуСвойств(СокрЛП(ОбъектСертификат.Владелец));
		ФИОПодписантаИзСертификата = СокрЛП(СвойстваВладельца["CN"]);
		Если ЗначениеЗаполнено(ФИОПодписантаИзСертификата) И ФИОПодписантов.Найти(ФИОПодписантаИзСертификата) = Неопределено Тогда
			ФИОПодписантов.Добавить(ФИОПодписантаИзСертификата);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат ФИОПодписантов;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьСтрокуСвойств(Знач СтрокаСвойств)
	
	СоответствиеРезультат = Новый Соответствие;
	ИсходнаяДлинаСтрокиСвойств = СтрДлина(СтрокаСвойств);
	
	Для ОбрИнд = 0 По ИсходнаяДлинаСтрокиСвойств - 1 Цикл
		Инд = ИсходнаяДлинаСтрокиСвойств - ОбрИнд;
		Если Сред(СтрокаСвойств, Инд, 1) = "," Тогда
			СтрокаСвойства = Сред(СтрокаСвойств, Инд + 1);
			СтрокаСвойств = Лев(СтрокаСвойств, Инд - 1);
			ВхождениеРавно = СтрНайти(СтрокаСвойства, "=");
			Если ВхождениеРавно <> 0 Тогда
				ИмяПараметра = СокрЛП(Лев(СтрокаСвойства, ВхождениеРавно - 1));
				ЗначениеПараметра = СокрЛП(Сред(СтрокаСвойства, ВхождениеРавно + 1));
				Если НЕ ПустаяСтрока(ИмяПараметра) Тогда
					СоответствиеРезультат.Вставить(ИмяПараметра, ЗначениеПараметра);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	Если НЕ ПустаяСтрока(СтрокаСвойств) Тогда
		ВхождениеРавно = СтрНайти(СтрокаСвойств, "=");
		Если ВхождениеРавно <> 0 Тогда
			ИмяПараметра = СокрЛП(Лев(СтрокаСвойств, ВхождениеРавно - 1));
			ЗначениеПараметра = СокрЛП(Сред(СтрокаСвойств, ВхождениеРавно + 1));
			Если НЕ ПустаяСтрока(ИмяПараметра) Тогда
				СоответствиеРезультат.Вставить(ИмяПараметра, ЗначениеПараметра);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	Возврат СоответствиеРезультат;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ИзменитьСоставТеговПодписантВБухгалтерскойОтчетностиПриНеобходимостиНаСервере(Контекст)
	
	КНДБО = Новый Массив;
	КНДБО.Добавить("0710099");
	КНДБО.Добавить("0710098");
	КНДБО.Добавить("0710097");
	КНДБО.Добавить("0710096");
	
	ИмяТегаКНДБО = "КНД";
	
	ТекстВыгрузки = Контекст.ТекстВыгрузки;
	
	// загружаем файл XML в дерево
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , ТекстВыгрузки);
	Если ДеревоXML = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// убеждаемся еще раз, что файл является файлом выгрузки бухгалтерской отчетности
	РезультатПоиска = ДеревоXML.Строки.Найти(ИмяТегаКНДБО, "Имя", Истина);
	Если РезультатПоиска = Неопределено ИЛИ КНДБО.Найти(РезультатПоиска.Значение) = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// ищем теги Подписант
	СоответствиеФИОТегамПодписант = Новый Соответствие;
	Подписанты = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "Подписант", "Э"), Истина);
	Если Подписанты.Количество() <= 1 Тогда
		Возврат;
	КонецЕсли;
	
	// получаем ФИО подписантов из абонентских сертификатов подписи
	ФИОПодписантовИзСертификатов = ПолучитьФИОПодписантовИзСвойствУчетнойЗаписи(Контекст);
	
	// определяем ФИО для каждого из узлов Подписант
	Для Каждого Подписант Из Подписанты Цикл
		
		УзелФИО = Подписант.Строки.Найти("ФИО", "Имя");
		Если УзелФИО = Неопределено Тогда // неизвестный формат файла выгрузки
			Возврат;
		КонецЕсли;
		
		УзелФамилия = Подписант.Строки.Найти("Фамилия", "Имя", Истина);
		УзелИмя = Подписант.Строки.Найти("Имя", "Имя", Истина);
		УзелОтчество = Подписант.Строки.Найти("Отчество", "Имя", Истина);
		
		СтрФамилия = ?(УзелФамилия = Неопределено, "", СокрЛП(УзелФамилия.Значение));
		СтрИмя = ?(УзелИмя = Неопределено, "", СокрЛП(УзелИмя.Значение));
		СтрОтчество = ?(УзелОтчество = Неопределено, "", СокрЛП(УзелОтчество.Значение));
		
		СтрФИО = СокрЛП(СтрФамилия + " " + СтрИмя + " " + СтрОтчество);
		
		СоответствиеФИОТегамПодписант.Вставить(СтрФИО, Подписант);
		
	КонецЦикла;
	
	// ищем узлы Подписант, соответствующие ФИО в сертификатах
	УзлыПодписантовИзСертификата = Новый Массив;
	Для Каждого ФИОПодписантаИзСертификата Из ФИОПодписантовИзСертификатов Цикл
		УзелПодписантаИзСертификата = СоответствиеФИОТегамПодписант[ФИОПодписантаИзСертификата];
		Если УзелПодписантаИзСертификата <> Неопределено Тогда
			УзлыПодписантовИзСертификата.Добавить(УзелПодписантаИзСертификата);
		КонецЕсли;
	КонецЦикла;
	
	// вычисляем, не удалим ли мы все узлы Подписант, если очистим от несоответствующих сертификатам
	Если УзлыПодписантовИзСертификата.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	// удаляем узел, не соответствующий подписанту из сертификата
	Для Каждого Подписант Из Подписанты Цикл
		Если УзлыПодписантовИзСертификата.Найти(Подписант) = Неопределено Тогда
			Подписант.Родитель.Строки.Удалить(Подписант);
		КонецЕсли;
	КонецЦикла;
	
	// пишем полученное дерево в тот же файл, откуда считали
	ДеревоXML.Колонки.Имя.Имя = "Код";
	ТекстВыгрузки = ВыгрузитьДеревоВXML_ПФР(ДеревоXML);
	
	Контекст.Вставить("ТекстВыгрузки", ТекстВыгрузки);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьПриложенияКБухгалтерскойОтчетности(Контекст)
	
	Док = Контекст.Документ;
	
	Если ТипЗнч(Док) <> Тип("ДокументСсылка.РегламентированныйОтчет")
	ИЛИ (Док.ИсточникОтчета <> "РегламентированныйОтчетБухОтчетность" И Док.ИсточникОтчета <> "РегламентированныйОтчетБухОтчетностьМП") Тогда
		Возврат Истина;
	КонецЕсли;
	
	ТекстВыгрузки = Контекст.ТекстВыгрузки;
	
	// объявляем массив-результат
	ПриложенияКБО = Новый Массив;
	
	// получаем сведения о расположении файлов с приложениями из документа
	Попытка
		СтруктураПриложения = Док.ДанныеОтчета.Получить().СписокДополнительныхФайлов;
	Исключение
		ТекстСообщения = НСтр("ru = 'Ошибка получения сведений о приложениях из документа бухгалтерской отчетности.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	ЗаписьРегистраСведений = РегистрыСведений.ДополнительныеФайлыРегламентированныхОтчетов.СоздатьМенеджерЗаписи();
	ЗаписьРегистраСведений.РегламентированныйОтчет = Док.Ссылка;
	ЗаписьРегистраСведений.ВидДополнительногоФайла = "Пояснительная записка";
	ЗаписьРегистраСведений.Прочитать();
	Если НЕ ПустаяСтрока(ЗаписьРегистраСведений.ВидДополнительногоФайла) Тогда
		СтрПояснительнаяЗаписка = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого, ВХранилище");
		СтрПояснительнаяЗаписка.Тип = "ПояснительнаяЗаписка";
		ДанныеДопФайла = ЗаписьРегистраСведений.СодержимоеФайла.Получить();
		СтрПояснительнаяЗаписка.ПолноеИмяФайла = ПоместитьВоВременноеХранилище(ДанныеДопФайла, Контекст.УникальныйИдентификатор);
		СтрПояснительнаяЗаписка.ВХранилище = Истина;
		ПриложенияКБО.Добавить(СтрПояснительнаяЗаписка);
		
	ИначеЕсли СтруктураПриложения.Свойство("ИмяФайлаПояснительнойЗаписки")
	И ЗначениеЗаполнено(СтруктураПриложения.ИмяФайлаПояснительнойЗаписки) Тогда
		СтрПояснительнаяЗаписка = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого, ВХранилище");
		СтрПояснительнаяЗаписка.Тип = "ПояснительнаяЗаписка";
		СтрПояснительнаяЗаписка.ПолноеИмяФайла = СтруктураПриложения.ИмяФайлаПояснительнойЗаписки;
		СтрПояснительнаяЗаписка.ВХранилище = Ложь;
		ПриложенияКБО.Добавить(СтрПояснительнаяЗаписка);
	КонецЕсли;
	
	ЗаписьРегистраСведений = РегистрыСведений.ДополнительныеФайлыРегламентированныхОтчетов.СоздатьМенеджерЗаписи();
	ЗаписьРегистраСведений.РегламентированныйОтчет = Док.Ссылка;
	ЗаписьРегистраСведений.ВидДополнительногоФайла = "Аудиторское заключение";
	ЗаписьРегистраСведений.Прочитать();
	Если НЕ ПустаяСтрока(ЗаписьРегистраСведений.ВидДополнительногоФайла) Тогда
		СтрАудиторскоеЗаключение = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого, ВХранилище");
		СтрАудиторскоеЗаключение.Тип = "АудиторскоеЗаключение";
		ДанныеДопФайла = ЗаписьРегистраСведений.СодержимоеФайла.Получить();
		СтрАудиторскоеЗаключение.ПолноеИмяФайла = ПоместитьВоВременноеХранилище(ДанныеДопФайла, Контекст.УникальныйИдентификатор);
		СтрАудиторскоеЗаключение.ВХранилище = Истина;
		ПриложенияКБО.Добавить(СтрАудиторскоеЗаключение);
		
	ИначеЕсли СтруктураПриложения.Свойство("ИмяФайлаАудиторскогоЗаключения")
	И ЗначениеЗаполнено(СтруктураПриложения.ИмяФайлаАудиторскогоЗаключения) Тогда
		СтрАудиторскоеЗаключение = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого, ВХранилище");
		СтрАудиторскоеЗаключение.Тип = "АудиторскоеЗаключение";
		СтрАудиторскоеЗаключение.ПолноеИмяФайла = СтруктураПриложения.ИмяФайлаАудиторскогоЗаключения;
		СтрАудиторскоеЗаключение.ВХранилище = Ложь;
		ПриложенияКБО.Добавить(СтрАудиторскоеЗаключение);
	КонецЕсли;
	
	ЗаписьРегистраСведений = РегистрыСведений.ДополнительныеФайлыРегламентированныхОтчетов.СоздатьМенеджерЗаписи();
	ЗаписьРегистраСведений.РегламентированныйОтчет = Док.Ссылка;
	ЗаписьРегистраСведений.ВидДополнительногоФайла = "Заявление союза сельхозпроизводителей";
	ЗаписьРегистраСведений.Прочитать();
	Если НЕ ПустаяСтрока(ЗаписьРегистраСведений.ВидДополнительногоФайла) Тогда
		СтрЗаявлениеРевизионногоСоюза = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого, ВХранилище");
		СтрЗаявлениеРевизионногоСоюза.Тип = "ЗаявлениеРевизионногоСоюза";
		ДанныеДопФайла = ЗаписьРегистраСведений.СодержимоеФайла.Получить();
		СтрЗаявлениеРевизионногоСоюза.ПолноеИмяФайла = ПоместитьВоВременноеХранилище(ДанныеДопФайла, Контекст.УникальныйИдентификатор);
		СтрЗаявлениеРевизионногоСоюза.ВХранилище = Истина;
		ПриложенияКБО.Добавить(СтрЗаявлениеРевизионногоСоюза);
		
	ИначеЕсли СтруктураПриложения.Свойство("ИмяФайлаЗаявленияРевизионногоСоюза")
	И ЗначениеЗаполнено(СтруктураПриложения.ИмяФайлаЗаявленияРевизионногоСоюза) Тогда
		СтрЗаявлениеРевизионногоСоюза = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого, ВХранилище");
		СтрЗаявлениеРевизионногоСоюза.Тип = "ЗаявлениеРевизионногоСоюза";
		СтрЗаявлениеРевизионногоСоюза.ПолноеИмяФайла = СтруктураПриложения.ИмяФайлаЗаявленияРевизионногоСоюза;
		СтрЗаявлениеРевизионногоСоюза.ВХранилище = Ложь;
		ПриложенияКБО.Добавить(СтрЗаявлениеРевизионногоСоюза);
	КонецЕсли;
	
	// получаем сведения об именах файлов с приложениями из текста выгрузки
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , ТекстВыгрузки, ОписаниеОшибки);
	Если ДеревоXML = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка разбора XML файла выгрузки бухгалтерской отчетности:
                               |'" + ОписаниеОшибки);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелНаимФайлПЗ = ДеревоXML.Строки.Найти("НаимФайлПЗ", "Имя", Истина);
	Если УзелНаимФайлПЗ <> Неопределено И ЗначениеЗаполнено(СтрПояснительнаяЗаписка) Тогда
		СтрПояснительнаяЗаписка.ИмяФайла = СокрЛП(УзелНаимФайлПЗ.Значение);
	КонецЕсли;
	
	УзелНаимФайлАЗ = ДеревоXML.Строки.Найти("НаимФайлАЗ", "Имя", Истина);
	Если УзелНаимФайлАЗ <> Неопределено И ЗначениеЗаполнено(СтрАудиторскоеЗаключение) Тогда
		СтрАудиторскоеЗаключение.ИмяФайла = СокрЛП(УзелНаимФайлАЗ.Значение);
	Иначе
		УзелНаимФайлАО = ДеревоXML.Строки.Найти("НаимФайлАО", "Имя", Истина);
		Если УзелНаимФайлАО <> Неопределено И ЗначениеЗаполнено(СтрАудиторскоеЗаключение) Тогда
			СтрАудиторскоеЗаключение.ИмяФайла = СокрЛП(УзелНаимФайлАО.Значение);
		КонецЕсли;
	КонецЕсли;
	
	УзелНаимФайлЗСХП = ДеревоXML.Строки.Найти("НаимФайлЗСХП", "Имя", Истина);
	Если УзелНаимФайлЗСХП <> Неопределено И ЗначениеЗаполнено(СтрЗаявлениеРевизионногоСоюза) Тогда
		СтрЗаявлениеРевизионногоСоюза.ИмяФайла = СокрЛП(УзелНаимФайлЗСХП.Значение);
	КонецЕсли;
	
	Для Каждого Стр Из ПриложенияКБО Цикл
		Стр.ТипСодержимого = ТипСодержимогоПоИмениФайла(Стр.ИмяФайла);
	КонецЦикла;
	
	Контекст.Вставить("ПриложенияКБО", ПриложенияКБО);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьПриложенияКБухгалтерскойОтчетностиИзХранилищаВнешнихЭлектронныхПредставлений(Контекст)
	
	СоответствиеТипаПриложенияСтроковомуИдентификатору = Новый Соответствие;
	СоответствиеТипаПриложенияСтроковомуИдентификатору.Вставить(Перечисления.ТипыФайловБухОтчетности.АудиторскоеЗаключение, "АудиторскоеЗаключение");
	СоответствиеТипаПриложенияСтроковомуИдентификатору.Вставить(Перечисления.ТипыФайловБухОтчетности.ПояснительнаяЗаписка, "ПояснительнаяЗаписка");
	СоответствиеТипаПриложенияСтроковомуИдентификатору.Вставить(Перечисления.ТипыФайловБухОтчетности.ЗаявлениеСоюзаСельхозПроизводителей, "ЗаявлениеРевизионногоСоюза");
	
	ПриложенияКБО = Новый Массив;
	
	СтрПредставления = ИзвлечьЭлектронныеПредставленияИзХранилища(Контекст.Документ);
	Для Каждого СтрПредставление Из СтрПредставления Цикл
		Если СоответствиеТипаПриложенияСтроковомуИдентификатору[СтрПредставление.ТипФайлаОтчетности] <> Неопределено Тогда
			СтрПриложение = Новый Структура("Тип, ИмяФайла, ПолноеИмяФайла, ТипСодержимого");
			СтрПриложение.Тип = СоответствиеТипаПриложенияСтроковомуИдентификатору[СтрПредставление.ТипФайлаОтчетности];
			СтрПриложение.ИмяФайла = СтрПредставление.ИмяФайла;
			СтрПриложение.ПолноеИмяФайла = ПоместитьВоВременноеХранилище(СтрПредставление.Данные, Контекст.УникальныйИдентификатор);
			СтрПриложение.ТипСодержимого = ТипСодержимогоПоИмениФайла(СтрПредставление.ИмяФайла);
			ПриложенияКБО.Добавить(СтрПриложение);
		КонецЕсли;
	КонецЦикла;
	
	Контекст.Вставить("ПриложенияКБО", ПриложенияКБО);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьПриложенияКБухгалтерскойОтчетности(Контекст, ВыполняемоеОповещение)
	
	ТипОтправляемогоДокумента = Контекст.ТипОтправляемогоДокумента;
	Если ТипОтправляемогоДокумента = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
		Если НЕ ВыгрузитьПриложенияКБухгалтерскойОтчетностиИзХранилищаВнешнихЭлектронныхПредставлений(Контекст) Тогда // сервер
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Иначе
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
	Иначе
		
		// получаем имена файлов приложений к бухгалтерской отчетности, если они есть
		РезультатПолучения = ПолучитьПриложенияКБухгалтерскойОтчетности(Контекст);
		Если РезультатПолучения = Неопределено Тогда
			Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
		
		Результат = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗаписатьXMLВФайл(ТекстДляЗаписи, Знач ФайлДляЗаписи) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЗаписатьXMLВФайл(ТекстДляЗаписи, ФайлДляЗаписи);
	
КонецФункции

&НаСервереБезКонтекста
Функция СоздатьДеревоXML() Экспорт
	
	ДеревоXML = Новый ДеревоЗначений;
	ДеревоXML.Колонки.Добавить("Код");
	ДеревоXML.Колонки.Добавить("Тип");
	ДеревоXML.Колонки.Добавить("Значение");
	ДеревоXML.Колонки.Добавить("ЭтоCDATA");
	Возврат ДеревоXML;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьУзелXML(
		Родитель,
		Код,
		ЭтоАтрибут = Ложь,
		Значение = Неопределено,
		Необязательный = Ложь,
		ЭтоCDATA = Ложь,
		МаксимальнаяДлинаСтрокиССокращением = Неопределено) Экспорт
	
	Если Необязательный И НЕ ЗначениеЗаполнено(Значение) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПриведенноеЗначение = Значение;
	Если ЗначениеЗаполнено(МаксимальнаяДлинаСтрокиССокращением) Тогда
		ПриведенноеЗначение = СокрЛП(ПриведенноеЗначение);
		Если МаксимальнаяДлинаСтрокиССокращением <> -1 Тогда
			ПриведенноеЗначение = Лев(ПриведенноеЗначение, МаксимальнаяДлинаСтрокиССокращением);
		КонецЕсли;
		ПриведенноеЗначение = XMLСтрока(ПриведенноеЗначение);
	КонецЕсли;
	
	НовСтр = Родитель.Строки.Добавить();
	
	НовСтр.Код = Код;
	НовСтр.Значение = ПриведенноеЗначение;
	Если ТипЗнч(ЭтоАтрибут) = Тип("Булево") Тогда
		НовСтр.Тип = ?(ЭтоАтрибут, "А", "Э");
	Иначе
		НовСтр.Тип = ЭтоАтрибут;
	КонецЕсли;
	
	ТипЗнчРодитель = ТипЗнч(Родитель);
	Если (ТипЗнчРодитель = Тип("ДеревоЗначений") И Родитель.Колонки.Найти("ЭтоCDATA") <> Неопределено)
	ИЛИ (ТипЗнчРодитель = Тип("СтрокаДереваЗначений") И Родитель.Владелец().Колонки.Найти("ЭтоCDATA") <> Неопределено) Тогда
		НовСтр.ЭтоCDATA = ЭтоCDATA;
	КонецЕсли;
	
	Возврат НовСтр;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗаписатьУзелДереваВXML_ПФР(СтрокаДерева, ПотокXML, ИмяКорневогоЭлемента)
	
	Если ТипЗнч(СтрокаДерева) = Тип("ДеревоЗначений") Тогда
		Если ЗначениеЗаполнено(ИмяКорневогоЭлемента) Тогда
			ПотокXML.ЗаписатьНачалоЭлемента(ИмяКорневогоЭлемента);
		КонецЕсли;
		Для каждого Стр Из СтрокаДерева.Строки Цикл
			ЗаписатьУзелДереваВXML_ПФР(Стр, ПотокXML, ИмяКорневогоЭлемента);
		КонецЦикла;
		Если ЗначениеЗаполнено(ИмяКорневогоЭлемента) Тогда
			ПотокXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	Иначе
		Если СтрокаДерева.Тип = "А" ИЛИ СтрокаДерева.Тип = "A" Тогда 
			ПотокXML.ЗаписатьАтрибут(СтрокаДерева.Код, Строка(СтрокаДерева.Значение));
		Иначе
			ПотокXML.ЗаписатьНачалоЭлемента(СтрокаДерева.Код);
			Для каждого Лист из СтрокаДерева.Строки Цикл
				ЗаписатьУзелДереваВXML_ПФР(Лист, ПотокXML, ИмяКорневогоЭлемента);
			КонецЦикла;
			ПотокXML.ЗаписатьТекст(Строка(СтрокаДерева.Значение));
			ПотокXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	КонецЕсли;

КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьДеревоВXML_ПФР(ДеревоВыгрузки, ИмяКорневогоЭлемента = Неопределено)
	
	ПотокXML = РегламентированнаяОтчетность.СоздатьНовыйПотокXML();
	ЗаписатьУзелДереваВXML_ПФР(ДеревоВыгрузки, ПотокXML, ИмяКорневогоЭлемента);
	ТекстДляЗаписи = ПотокXML.Закрыть();
	ТекстДляЗаписи = "<?xml version=""1.0"" encoding=""windows-1251""?>" + Сред(ТекстДляЗаписи, СтрНайти(ТекстДляЗаписи, Символы.ПС));
	Возврат ТекстДляЗаписи;
	
КонецФункции

&НаСервереБезКонтекста
Функция СгенерироватьИмяФайлаДокументаВСоставеПакета(РасширениеБезТочки = "bin")

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СгенерироватьИмяФайлаДокументаВСоставеПакета(РасширениеБезТочки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКаталогВременныхФайлов()
	
	ВременныйКаталог = КаталогВременныхФайлов();
	ВременныйКаталог = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ВременныйКаталог);
	Возврат ВременныйКаталог;
	
КонецФункции
 
&НаСервереБезКонтекста
Функция СформироватьОписаниеСведенийДляСообщенияФНС(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьОписаниеСведенийДляСообщенияФНС(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СгенерироватьUUID() Экспорт
	
	Возврат нрег(СтрЗаменить(Строка(Новый("УникальныйИдентификатор")), "-", ""));
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОтпечаток(УчетнаяЗапись, ТипСертификата, Объект) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОтпечаток(УчетнаяЗапись, ТипСертификата, Объект);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСертификатСубъекта(Объект, ТипСертификата = Неопределено) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСертификатСубъекта(Объект, ТипСертификата);
	
КонецФункции
    
&НаСервереБезКонтекста
Функция СформироватьПакетФНС_ОбщиеПроверкиПередФормированием(Контекст)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_ОбщиеПроверкиПередФормированием(Контекст);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателя(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки.УчетнаяЗапись);
	
	Если ТипЗнч(КонтекстОтправки.СертификатПолучателя) <> Тип("Массив") Тогда
		Отпечатки = Новый Массив;
		Отпечатки.Добавить(КонтекстОтправки.СертификатПолучателя);
	Иначе
		Отпечатки = КонтекстОтправки.СертификатПолучателя;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(КонтекстОтправки.СертификатСервераДокументооборота)
		И Отпечатки.Найти(КонтекстОтправки.СертификатСервераДокументооборота) = Неопределено Тогда
		
		Отпечатки = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(Отпечатки);
		Отпечатки.Добавить(КонтекстОтправки.СертификатСервераДокументооборота);
	КонецЕсли;
	
	СертификатыДляПоиска = Новый Массив;
	Для Каждого Отпечаток Из Отпечатки Цикл
		НовыйСертификат = Новый Структура("Отпечаток", Отпечаток);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, НовыйСертификат);
		СертификатыДляПоиска.Добавить(НовыйСертификат);
		
	КонецЦикла;
	
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("Отпечатки", Отпечатки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах = Неопределено,
		ТипСообщения = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах,
		ТипСообщения);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	НайденныеСертификаты 				= ?(Результат.Свойство("Сертификаты"), Результат.Сертификаты, Неопределено);
	НенайденныеСертификаты 				= ?(Результат.Свойство("НенайденныеСертификаты"), Результат.НенайденныеСертификаты, Неопределено);
	СсылкаПолучатель 					= ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель;
	СертификатПолучателя 				= ВходящийКонтекст.КонтекстОтправки.СертификатПолучателя;
	СертификатСервераДокументооборота 	= ВходящийКонтекст.КонтекстОтправки.СертификатСервераДокументооборота;
	СсылкаСерверДокументооборота 		= ВходящийКонтекст.КонтекстОтправки.СсылкаСерверДокументооборота;
	ТипСообщения 						= ВходящийКонтекст.КонтекстОтправки.ТипСообщения;
	
	ИнформацияОПолучателяхИСертификатах = Новый Структура;
	ИнформацияОПолучателяхИСертификатах.Вставить("СертификатСервераДокументооборота", 	СертификатСервераДокументооборота);
	ИнформацияОПолучателяхИСертификатах.Вставить("СсылкаСерверДокументооборота", 		СсылкаСерверДокументооборота);
	ИнформацияОПолучателяхИСертификатах.Вставить("СертификатПолучателя", 				СертификатПолучателя);
	РезультатПроверки = СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Результат.Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах,
		ТипСообщения);
	Если РезультатПроверки = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить(
		"ОбъектСертификатПолучателя", РезультатПроверки.НайденныеСертификатыПолучателя);
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить(
		"ОбъектСертификатСервераДокументооборота", РезультатПроверки.НайденныйСертификатСервераДокументооборота);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетФНС_ПроверкиНаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_ПроверкиНаСервере(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетФНС_ПодготовитьСертификатыДляПоиска(Знач УчетнаяЗапись, Знач СертификатРуководителя, Знач СертификатГлавногоБухгалтера, Знач СертификатОтветственногоЗаШифрование)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_ПодготовитьСертификатыДляПоиска(
				УчетнаяЗапись,
				СертификатРуководителя,
				СертификатГлавногоБухгалтера,
				СертификатОтветственногоЗаШифрование);

КонецФункции

&НаКлиенте
Процедура СформироватьПакетФНС_ПроверкиСертификатов(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если КонтекстОтправки.Свойство("ОбъектСертификатОтветственногоЗаШифрование") Тогда // возможна ситуация, когда сертификаты уже получены и проверены
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Истина);
		Возврат;
	КонецЕсли;
	
	СертификатыДляПоиска = СформироватьПакетФНС_ПодготовитьСертификатыДляПоиска(
		КонтекстОтправки.УчетнаяЗапись,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатГлавногоБухгалтера,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если СертификатыДляПоиска = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("ВыполнятьПроверку", Истина);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(Знач Сертификаты, Знач СертификатРуководителя, Знач СертификатГлавногоБухгалтера, Знач СертификатОтветственногоЗаШифрование) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Сертификаты, СертификатРуководителя, СертификатГлавногоБухгалтера, СертификатОтветственногоЗаШифрование);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	ОбъектыСертификатов = СформироватьПакетФНС_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Результат.Сертификаты,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатГлавногоБухгалтера,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если ОбъектыСертификатов = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
		
	///////////////////////////////////////////////////////////////////////////////
	КонтекстОтправки = ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки;
	Для каждого ОбъектСертификат Из ОбъектыСертификатов Цикл
		КонтекстОтправки.Вставить(ОбъектСертификат.Ключ, ОбъектСертификат.Значение);
	КонецЦикла;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры
 
&НаСервереБезКонтекста
Функция СформироватьПакетФНС_СформироватьДеревоОписанияДокументовПакета(Контекст)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_СформироватьДеревоОписанияДокументовПакета(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетПФР_СформироватьОписаниеДокументовПакета(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетПФР_СформироватьОписаниеДокументовПакета(Контекст);
	
КонецФункции
 
&НаКлиенте
Процедура СформироватьПакетФНС_Подписание(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется подписание пакета с документами..."); // ФНС Универсальная
	
	// формируем массив объектов-сертификатов, которыми будем подписывать
	МассивОбъектовСертификатов = Новый Массив;
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ДекларацияНП")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ЗаявлениеНП")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.Форма2НДФЛНП")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ОбращениеНП")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПредставлениеНП")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ЗапросНП")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП") Тогда
		МассивОбъектовСертификатов.Добавить(КонтекстОтправки.ОбъектСертификатРуководителя);
		Если ЗначениеЗаполнено(КонтекстОтправки.СертификатГлавногоБухгалтера)
			И КонтекстОтправки.СертификатГлавногоБухгалтера <> КонтекстОтправки.СертификатРуководителя Тогда
			МассивОбъектовСертификатов.Добавить(КонтекстОтправки.ОбъектСертификатГлавногоБухгалтера);
		КонецЕсли;
	Иначе
		МассивОбъектовСертификатов.Добавить(КонтекстОтправки.ОбъектСертификатОтветственногоЗаШифрование);
	КонецЕсли;
	
	// генерируем подписи руководителя
	ПодписатьСообщение("ФНС", ОповещениеОЗавершении, КонтекстОтправки,
		КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки.ТипТранспортногоСообщения, 
		МассивОбъектовСертификатов, , КонтекстОтправки.УчетнаяЗапись, КонтекстОтправки.СообщениеОснование);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетФНС_ЗаключительныеДействияНаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетФНС_ЗаключительныеДействияНаСервере(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетПФР_ЗаключительныеДействияНаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетПФР_ЗаключительныеДействияНаСервере(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИзвещениеОПолученииДляСообщенияФНС_НаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьИзвещениеОПолученииДляСообщенияФНС(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетПФР_ОбщиеПроверкиПередФормированием(Контекст)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетПФР_ОбщиеПроверкиПередФормированием(Контекст);
	
КонецФункции

#Область СформироватьПакетФНС
 
// Формирует пакет в формате приказа ФНС 534.
//
&НаКлиенте
Процедура СформироватьПакетФНС(ОповещениеОЗавершении, ТранспортноеСообщение, Оповещать = Ложь, ПараметрыОперации = Неопределено) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Оповещать);
	
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(КонтекстОтправки, ПараметрыОперации, Истина);
	КонецЕсли;	
	
	// общие проверки
	Если НЕ СформироватьПакетФНС_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда // сервер
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
		
	// получение и проверки сертификатов
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФНСПослеПроверкиИПолученияСертификатовПолучателя", ЭтотОбъект, Контекст);
		
	СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателя(Оповещение, КонтекстОтправки); // клиент
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФНСПослеПроверкиИПолученияСертификатовПолучателя(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Результат Тогда
		Если НЕ СформироватьПакетФНС_ПроверкиНаСервере(КонтекстОтправки) Тогда // универсальная
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		// проверки на клиенте (сертификаты)
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетФНСПослеПроверкиСертификатов", ЭтотОбъект, ВходящийКонтекст);
			
		СформироватьПакетФНС_ПроверкиСертификатов(Оповещение, КонтекстОтправки);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФНСПослеПроверкиСертификатов(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ДекларацияНП
		ИЛИ КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ЗаявлениеНП
		ИЛИ КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_Форма2НДФЛНП
		ИЛИ КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ОбращениеНП
		ИЛИ КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ПредставлениеНП
		ИЛИ КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_ЗапросНП Тогда
		
		РезультатФормирования = СформироватьОписаниеСведенийДляСообщенияФНС(КонтекстОтправки); // сервер
		Если НЕ РезультатФормирования Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
	Иначе
		Если НЕ КонтекстОтправки.ТипСообщения = СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_РезультатПриемаДокументНП Тогда
			РезультатФормирования = СформироватьИзвещениеОПолученииДляСообщенияФНС_НаСервере(КонтекстОтправки); // сервер
			
			Если НЕ РезультатФормирования Тогда
				ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
			КонецЕсли;
			
		КонецЕсли;
	КонецЕсли;

	// подписываем ЭЦП руководителя и главного бухгалтера
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФНСПослеПодписания", ЭтотОбъект, Контекст);
	СформироватьПакетФНС_Подписание(Оповещение, КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФНСПослеПодписания(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;

	// формируем дерево описания документов пакета (сервер)
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется формирование описания пакета с документами..."); // ФНС Универсальная
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если НЕ СформироватьПакетФНС_СформироватьДеревоОписанияДокументовПакета(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	РежимПодтвержденияОблачнойПодписи("СформироватьПакетФНС", КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки, Ложь);
	
	// отрабатываем отложенные команды на клиенте (шифрование)
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФНСПослеВыполненияОтложенныхКомандНаКлиенте", ЭтотОбъект, ВходящийКонтекст);
	СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(Оповещение, КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФНСПослеВыполненияОтложенныхКомандНаКлиенте(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура СформироватьПакетФНСПослеВыполненияОтложенныхКоманд(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// формируем файл описания пакета, создаем архив и помещаем в ИБ
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	СведенияОШифровании = ПараметрыПриложения["КриптографияЭДКО.СведенияОШифровании"];
	Если СведенияОШифровании <> Неопределено Тогда
		КонтекстОтправки.Вставить("СведенияОШифровании", СведенияОШифровании);
		ПараметрыПриложения["КриптографияЭДКО.СведенияОШифровании"] = Неопределено;
	КонецЕсли;
	Если НЕ СформироватьПакетФНС_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.Оповещать Тогда
		Оповестить("Изменение сообщения", Новый Структура("Сообщение, ЦиклОбмена, Предмет",
														КонтекстОтправки.ТранспортноеСообщение,
														КонтекстОтправки.ЦиклОбмена));
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция ОпределитьПолучателяТранспортногоКонтейнера(Сообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьПолучателяТранспортногоКонтейнера(Сообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьСертификатПоПериоду(Знач ОбъектСертификат)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПроверитьСертификатПоПериоду(ОбъектСертификат);
	
КонецФункции

&НаСервереБезКонтекста
Функция СоздатьДеревоОписанияДокументовПакета() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СоздатьДеревоОписанияДокументовПакета();
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоместитьВоВременноеХранилищеДанныеСКлиента(Знач Строка64, АдресВоВременномХранилище)
	
	Возврат ПоместитьВоВременноеХранилище(ДвоичныеДанныеИзСтроки64НаСервере(Строка64), АдресВоВременномХранилище);
	
КонецФункции

&НаСервереБезКонтекста
Функция ХранилищеЗначенияВВидеСтроки(Значение)
	
	ДвоичныеДанныеЗначения = Значение.Получить();
	Возврат Base64Строка(ДвоичныеДанныеЗначения);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеПакетаФНС(ОписаниеДокументов, UUIDПакета = Неопределено, КодТипаДокументоооборота = "01", ТипДокументооборота = "Декларация", КодТипаТранзакции = "01", ТипТранзакции = "ДекларацияНП", КодОтправителя = "", КодСОС = Неопределено, КодПолучателя = "", ТипПолучателя = "налоговыйОрган", ИдентификаторДляВременногоХранилища = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьОписаниеПакетаФНС(
				ОписаниеДокументов,
				UUIDПакета,
				КодТипаДокументоооборота,
				ТипДокументооборота,
				КодТипаТранзакции,
				ТипТранзакции,
				КодОтправителя,
				КодСОС,
				КодПолучателя,
				ТипПолучателя,
				ИдентификаторДляВременногоХранилища);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьТранспортныйКонтейнер(ТранспортноеСообщение, Данные, ИмяФайла, Размер = Неопределено) Экспорт
	
	Если Размер = Неопределено Тогда
		Размер = ОпределитьРазмер(Данные);
	КонецЕсли;
	
	НачатьТранзакцию();
	
	МенеджерЗаписи = РегистрыСведений.ТранспортныеКонтейнеры.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.ТранспортноеСообщение = ТранспортноеСообщение;
	МенеджерЗаписи.ИмяФайла = ИмяФайла;
	МенеджерЗаписи.Размер = Размер;
	
	ТипДанных = ТипЗнч(Данные);
	Если ТипДанных = Тип("ХранилищеЗначения") Тогда
		ДвоичныеДанныеФайла = Данные.Получить();
	ИначеЕсли ТипДанных = Тип("Строка") Тогда
		Если ЭтоАдресВременногоХранилища(Данные) Тогда
			ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(Данные);
		Иначе
			РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
				Данные, "Данные", "ДобавитьТранспортныйКонтейнер");
			//@skip-check 1c-fresh-audit
			ДвоичныеДанныеФайла = Новый ДвоичныеДанные(Данные);
		КонецЕсли;
	Иначе // если двоичные данные
		ДвоичныеДанныеФайла = Данные;
	КонецЕсли;
	АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, Новый УникальныйИдентификатор);
	
	КомпонентыИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайла);
	УникальноеИмяФайлаБезРасширения = ОбщегоНазначенияЭДКОКлиентСервер.УникальнаяСтрока(КомпонентыИмениФайла.ИмяБезРасширения, 150);
	РасширениеФайлаБезТочки = Сред(КомпонентыИмениФайла.Расширение, 2);
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", 						Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов", 				ТранспортноеСообщение);
	ПараметрыФайла.Вставить("ИмяБезРасширения", 			УникальноеИмяФайлаБезРасширения);
	ПараметрыФайла.Вставить("РасширениеБезТочки", 			РасширениеФайлаБезТочки);
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", 	Неопределено);
	
	ИнформацияОбОбновляемомФайле = Новый Структура;
	ИнформацияОбОбновляемомФайле.Вставить("АдресФайлаВоВременномХранилище", АдресФайла);
	ИнформацияОбОбновляемомФайле.Вставить("АдресВременногоХранилищаТекста", Неопределено);
	ИнформацияОбОбновляемомФайле.Вставить("ИмяБезРасширения", 				УникальноеИмяФайлаБезРасширения);
	ИнформацияОбОбновляемомФайле.Вставить("Расширение", 					РасширениеФайлаБезТочки);
	
	Попытка
		МенеджерЗаписи.Записать(Истина);
		
		МассивФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			ТранспортноеСообщение,
			"ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы",
			ИмяФайла);
		
		Если МассивФайлов.Количество() > 0 Тогда
			ПрисоединенныйФайл = МассивФайлов[0];
			РаботаСФайлами.ОбновитьФайл(ПрисоединенныйФайл, ИнформацияОбОбновляемомФайле);
			
		Иначе
			НоваяСсылкаНаФайл = РаботаСФайлами.НоваяСсылкаНаФайл(ТранспортноеСообщение, "ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы");
			ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, АдресФайла,,, НоваяСсылкаНаФайл);
		КонецЕсли;
		
		ПрисоединенныйФайлОбъект = ПрисоединенныйФайл.ПолучитьОбъект();
		
		ПрисоединенныйФайлОбъект.ИсходноеИмяФайла = ИмяФайла;
		
		ПрисоединенныйФайлОбъект.Записать();
	Исключение
		ОтменитьТранзакцию();
		УдалитьИзВременногоХранилища(АдресФайла);
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось сохранить в информационной базе транспортный контейнер сообщения %1!%2'"),
			Символ(34) + ТранспортноеСообщение.Ссылка + Символ(34),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	УдалитьИзВременногоХранилища(АдресФайла);
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ФИОИндивидуальногоПредпринимателя(Организация, СведенияОбОрганизации = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ФИОИндивидуальногоПредпринимателя(Организация, СведенияОбОрганизации);
	
КонецФункции
      
&НаСервереБезКонтекста
Функция СформироватьСведенияОбУчастникеДокументооборота(Участник, ТранспортноеСообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьСведенияОбУчастникеДокументооборота(Участник, ТранспортноеСообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция ИмяФайлаБезРасширения(ИмяФайлаСРасширением)
	
	ДлинаИмениФайла = СтрДлина(ИмяФайлаСРасширением);
	Для Инд = 1 По ДлинаИмениФайла Цикл
		Если Сред(ИмяФайлаСРасширением, ДлинаИмениФайла - Инд + 1, 1) = "." Тогда
			Возврат Лев(ИмяФайлаСРасширением, ДлинаИмениФайла - Инд);
		КонецЕсли;
	КонецЦикла;
	Возврат ИмяФайлаСРасширением;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыделитьПрефиксИзИмениФайлаОснования(ИмяФайлаДИВ)
	
	РасширениеФайлаДИВ = нрег(РасширениеФайла(ИмяФайлаДИВ));
	Если РасширениеФайлаДИВ = "txt" Тогда // если это текстовый файл, то префикс - это то, что до первой цифры, кроме случая протокола 2-НДФЛ
		
		ПрефиксПротокол2НДФЛ1 = "ПРОТ_ДОХОД_2НДФЛ"; // префикс протокола 2-НДФЛ
		ПрефиксПротокол2НДФЛ2 = "PROT_NO_NDFL2"; // префикс протокола 2-НДФЛ
		ПрефиксРассылки = "IU_RASS"; // префикс файла рассылки
		Если Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксПротокол2НДФЛ1)) = ПрефиксПротокол2НДФЛ1 Тогда
			Возврат ПрефиксПротокол2НДФЛ1;
		ИначеЕсли Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксПротокол2НДФЛ2)) = ПрефиксПротокол2НДФЛ2 Тогда
			Возврат ПрефиксПротокол2НДФЛ2;
		ИначеЕсли Лев(ИмяФайлаДИВ, СтрДлина(ПрефиксРассылки)) = ПрефиксРассылки Тогда
			Возврат ПрефиксРассылки;
		Иначе
			СтрЦифры = "0123456789";
			Для Инд = 1 По СтрДлина(ИмяФайлаДИВ) Цикл
				ТекСимв = Сред(ИмяФайлаДИВ, Инд, 1);
				Если СтрНайти(СтрЦифры, ТекСимв) <> 0 Тогда
					Возврат Лев(ИмяФайлаДИВ, Инд - 1);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
	Иначе
		
		Разделитель = "_";
		ИндексВхожденияРазделителя = 0;
		Для Инд = 1 По СтрДлина(ИмяФайлаДИВ) Цикл
			ТекСимв = Сред(ИмяФайлаДИВ, Инд, 1);
			Если ТекСимв = Разделитель Тогда
				ИндексВхожденияРазделителя = ИндексВхожденияРазделителя + 1;
				Если ИндексВхожденияРазделителя = 2 Тогда
					Возврат СтрЗаменить(Лев(ИмяФайлаДИВ, Инд - 1), "_", "");
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИдентификаторПолучателя(ТранспортноеСообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьИдентификаторПолучателя(ТранспортноеСообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИдентификаторОтправителя(ТранспортноеСообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьИдентификаторОтправителя(ТранспортноеСообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьТекстВФайл(Текст)
	
	КодировкаТекстаФайла = "windows-1251";
	ИмяФайлаСохранения = ПолучитьИмяВременногоФайла();
	ОбъектЗапись = Новый ЗаписьТекста(ИмяФайлаСохранения, КодировкаТекстаФайла);
	ОбъектЗапись.Записать(Текст);
	Попытка
		ОбъектЗапись.Закрыть();
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	Возврат ИмяФайлаСохранения;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПодтверждениеПолученияДляСообщения(Контекст)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПодтверждениеПолученияДляСообщения(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьТипСодержимогоПриложенияКПисьму(СтрВложение)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьТипСодержимогоПриложенияКПисьму(СтрВложение);
	
КонецФункции

&НаСервереБезКонтекста
Функция СтрокаСоответствуетШаблону(Стр, СтрШаблон)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СтрокаСоответствуетШаблону(Стр, СтрШаблон);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеСведенийДляСообщения(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьОписаниеСведенийДляСообщения(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеПисьмаДляСообщения(Контекст)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьОписаниеПисьмаДляСообщения(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьТранспортнуюИнформациюИСохранитьВИБ(Сообщение, СертификатBase64)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьТранспортнуюИнформациюИСохранитьВИБ(Сообщение, СертификатBase64);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПрочитатьBase64ИзФайла(ИмяФайла)
	
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ИмяФайла, "ИмяФайла", "ПрочитатьBase64ИзФайла");

	МассивФайловПослеРазбиения = РазделитьФайл(ИмяФайла, 1024 * 1024 * 3);
	КоличествоФайловВМассиве = МассивФайловПослеРазбиения.Количество();
	СтрокаЗамены = Символ(13) + Символ(10);
	СтрокаРезультат = "";
	
	Для НомерФайла = 0 По КоличествоФайловВМассиве - 1 Цикл
		
		ВремФайл = МассивФайловПослеРазбиения[НомерФайла];
		
		//@skip-check 1c-fresh-audit
		ВременнаяСтрокаРезультата = Base64Строка(Новый ДвоичныеДанные(ВремФайл));
		ВременнаяСтрокаРезультата = СтрЗаменить(ВременнаяСтрокаРезультата, СтрокаЗамены, "");
		СтрокаРезультат = СтрокаРезультат + ВременнаяСтрокаРезультата;
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
		
	КонецЦикла;
	
	Возврат СтрокаРезультат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СообщениеЗашифровано(Сообщение) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	ИСТИНА КАК Поле1
	                      |ИЗ
	                      |	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
	                      |ГДЕ
	                      |	ТранспортныеКонтейнеры.ТранспортноеСообщение = &ТранспортноеСообщение");
	Запрос.УстановитьПараметр("ТранспортноеСообщение", Сообщение);
	Возврат НЕ Запрос.Выполнить().Пустой();
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьНастройкуУчетнойЗаписи(УчетнаяЗапись, Настройка) Экспорт
	
	Запрос = Новый Запрос;
	
	ПутьКДанным = "";
	Если Настройка = "ОбмениватьсяВАвтоматическомРежиме" Тогда
		ПутьКДанным = "(УчетныеЗаписиДокументооборота.ИнтервалАвтообмена <> 0) КАК АвтообменВключен";
	ИначеЕсли Настройка = "ИнтервалОбменаВАвтоматическомРежиме" Тогда
		ПутьКДанным = "УчетныеЗаписиДокументооборота.ИнтервалАвтообмена";
	ИначеЕсли Настройка = "НЕФормироватьПодтвержденияАвтоматически" Тогда
		ПутьКДанным = "УчетныеЗаписиДокументооборота.НеФормироватьПодтвержденияАвтоматически";
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
	УчетныхЗаписейНесколько = (ТипЗнч(УчетнаяЗапись) <> Тип("СправочникСсылка.УчетныеЗаписиДокументооборота"));
	Если УчетныхЗаписейНесколько Тогда
		Результат = Новый Соответствие;
	КонецЕсли;
	
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	УчетныеЗаписиДокументооборота.Ссылка,
	                      |	" + ПутьКДанным + "
	                      |ИЗ
	                      |	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
						  |ГДЕ
						  |	УчетныеЗаписиДокументооборота.Ссылка В (&УчетныеЗаписи)";
	Запрос.УстановитьПараметр("УчетныеЗаписи", УчетнаяЗапись);
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Если УчетныхЗаписейНесколько Тогда
			Результат.Вставить(Выборка.Получить(0), Выборка.Получить(1));
		Иначе
			Возврат Выборка.Получить(1);
		КонецЕсли;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИнициализироватьСвойстваПочты(Знач УчетнаяЗапись, ДляПолучения = Истина, ДляОтправки = Истина) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ИнициализироватьСвойстваПочты(УчетнаяЗапись, ДляПолучения, ДляОтправки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьТранспортноеСообщение(Сообщение, ПочтовыйПрофиль = Неопределено, ТихийРежим = Ложь, ПараметрыГрупповойОбработки = Неопределено) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправитьТранспортноеСообщение(
					Сообщение,
					ПочтовыйПрофиль,
					ТихийРежим,
					ПараметрыГрупповойОбработки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьСообщение(ПочтовыйПрофиль, ПочтовоеСообщение) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();	
	Возврат КонтекстЭДОСервер.ОтправитьСообщение(ПочтовыйПрофиль, ПочтовоеСообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПодключитьсяКПочтовомуСерверу(ПочтовыйПрофиль, ТихийРежим = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПодключитьсяКПочтовомуСерверу(ПочтовыйПрофиль, ТихийРежим);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтключитьсяОтПочтовогоСервера(Почта, ТихийРежим = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтключитьсяОтПочтовогоСервера(Почта, ТихийРежим);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки_НаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(Контекст);
	
КонецФункции

&НаКлиенте
Функция ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(Контекст)
	
	// отправляем и пишем статус отправки
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки_НаСервере(Контекст) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Оповестить("Изменение сообщения", Новый Структура("Сообщение, ЦиклОбмена, Предмет",
													Контекст.ТранспортноеСообщение,
													Контекст.ЦиклОбмена,
													Контекст.Документ));
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ПодтвердитьПовторнуюОтправку(Документ, КонтролирующийОрган, ОписаниеОповещения)

	ЭтоГрупповаяОтправка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ОписаниеОповещения);
	Если ЭтоГрупповаяОтправка Тогда
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
		Возврат;
	КонецЕсли;

	ДополнительныеПараметры = Новый Структура("ПолучатьДаты, ПолучатьОшибкиОтправки", Ложь, Ложь);
	СостояниеОтправки 		= ТекущееСостояниеОтправки(Документ, КонтролирующийОрган, ДополнительныеПараметры);
	
	Если СостояниеОтправки <> Неопределено И СостояниеОтправки.ТекущийЭтапОтправки <> Неопределено Тогда
		СостояниеСдачиОтчетности = СостояниеОтправки.ТекущийЭтапОтправки.СостояниеСдачиОтчетности;
	Иначе
		СостояниеСдачиОтчетности = Неопределено;
	КонецЕсли;

	Если СостояниеСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СостояниеСдачиОтчетности.ДокументооборотНачат") Тогда
		ТекстВопроса = НСтр("ru = 'Отчет уже отправлен, результат отправки не получен.
								  |Вы действительно хотите отправить отчет повторно?'");
		ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);
		
	ИначеЕсли СостояниеСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СостояниеСдачиОтчетности.ПоложительныйРезультатДокументооборота") Тогда
		ТекстВопроса = НСтр("ru = 'Отчет уже отправлен и успешно сдан.
								  |Вы действительно хотите отправить отчет повторно?'");
		ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
	КонецЕсли;

КонецПроцедуры


#Область ОтправкаРегламентированногоОтчетаВФНС

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНС(Док, ВыполняемоеОповещение) Экспорт
	
	Контекст = Новый Структура();
	Контекст.Вставить("Док",                   Док);
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФНСПослеПодтвержденияПовторнойОтправки",
		ЭтотОбъект,
		Контекст);
	
	ПодтвердитьПовторнуюОтправку(Док, "ФНС", ОписаниеОповещения);

КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСПослеПодтвержденияПовторнойОтправки_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Результат = КодВозвратаДиалога.Да;
	Параметры_ВходящийКонтекст = ВходящийКонтекст;
	
	ОтправкаРегламентированногоОтчетаВФНСПослеПодтвержденияПовторнойОтправки(Параметры_Результат, Параметры_ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСПослеПодтвержденияПовторнойОтправки(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть аналогиная процедура ОтправкаРегламентированногоОтчетаВФНС(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Док = ВходящийКонтекст.Док;
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ОтправкаОбъектаЗапрещена = ДокументооборотСКОВызовСервера.ОтправкаОбъектаЗапрещенаНаТарифеПромоЕНС(Док);
	Если ОтправкаОбъектаЗапрещена Тогда
		ДокументооборотСКОКлиент.СообщитьОНевозможностиОтправкиНаТарифе(Док);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;

	Если НЕ ОтправкаРегламентированногоОтчетаВФНС_ПередВыгрузкойФайла(КонтекстОтправки, Док, УникальныйИдентификатор) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаРегламентированногоОтчетаВФНСПослеПодтвержденияПовторнойОтправки_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
				
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ОповещениеОЗавершении, Ложь); //+
			
		Иначе
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС

		КонецЕсли;
		
		Возврат;
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ОповещениеОЗавершении, Ложь); //+
		Возврат;
		
	КонецЕсли;
	
	// Выгрузка
	КоличествоФайловВКомплекте = КоличествоФайловВКомплекте(Док);
	
	Если КоличествоФайловВКомплекте > 1 
		И КонтекстОтправки.ВидОтправляемогоОтчета = "РегламентированныйОтчетУведомлениеОКонтролируемыхСделках" 
		И КонтекстОтправки.ТипОтправляемогоДокумента = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
		
		КонтекстДлительнойОперации = Новый Структура;
		КонтекстДлительнойОперации.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
		КонтекстДлительнойОперации.Вставить("КонтекстОтправки", 	 КонтекстОтправки);
		
		ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
		ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
		ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 7;
		ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
		
		Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка эл. предст. отчета в ФНС
			Возврат;
		КонецЕсли;
		
		ПодключитьОбработчикОжидания("Подключаемый_ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплекте", 0.1, Истина);
	Иначе
		
		Оповещение = Новый ОписаниеОповещения("ОтправкаРегламентированногоОтчетаВФНСПослеВыгрузки", ЭтотОбъект, ВходящийКонтекст);
		
		ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
		// выгружаем отчет и доверенность, при необходимости
		ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправки(КонтекстОтправки, Оповещение);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСПослеВыгрузки(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	
	РезультатВыгрузки     = Результат.РезультатВыгрузки;
	КонтекстОтправки      = Результат.Контекст; // Обновленный по результатам выгрузки контекст.
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	Если РезультатВыгрузки = Неопределено Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
		
	ИначеЕсли НЕ РезультатВыгрузки Тогда
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФНСПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док", Док);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", ОписаниеОповещения);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 7;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФНС
		Возврат;
	КонецЕсли;

	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаРегламентированногоОтчетаВФНСПослеВыгрузки", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаРегламентированногоОтчетаВФНСПослеВыгрузки() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Док 				= КонтекстДлительнойОперации.Док;
	ОписаниеОповещения 	= КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи..."); // ФНС рег. отчет
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФНС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.КонтекстОтправки.Документ, ВходящийКонтекст.КонтекстОтправки.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// получаем свойства сертификатов абонента
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется проверка сертификатов..."); // ФНС рег. отчет

	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФНСПослеПроверкиСертификатов", ЭтотОбъект, ВходящийКонтекст);
	
	СформироватьПакетФНС_ПроверкиСертификатов(Оповещение, ВходящийКонтекст.КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСПослеПроверкиСертификатов(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		
		Возврат;
		
	КонецЕсли;
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	КонтекстОтправки      = ВходящийКонтекст.КонтекстОтправки;
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРегламентированногоОтчетаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		
		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФНСПослеФормированияПакета", ЭтотОбъект, Контекст);
	
	ТекстСведений = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(КонтекстОтправки, "ТекстСведений");
	ПараметрыОперации = Новый Структура("ТекстСведений", ТекстСведений);
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС", ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт 

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);

		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		
		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(ВходящийКонтекст.КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС

		Возврат;
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
	Если ВходящийКонтекст.КонтекстОтправки.Свойство("ДополнительноеСообщениеПриУдачнойОтправке") Тогда
		ДополнительныеПараметры.Вставить("ДополнительноеСообщение",
			ВходящийКонтекст.КонтекстОтправки.ДополнительноеСообщениеПриУдачнойОтправке);
	КонецЕсли;
	Если ВходящийКонтекст.КонтекстОтправки.Свойство("ДополнительныеРеквизитыДляОтправкиПриУдачнойОтправке") Тогда
		ДополнительныеПараметры.Вставить("ДополнительныеРеквизитыДляОтправки",
			ВходящийКонтекст.КонтекстОтправки.ДополнительныеРеквизитыДляОтправкиПриУдачнойОтправке);
	КонецЕсли;
	Если ВходящийКонтекст.КонтекстОтправки.Свойство("ДополнительныеВложенияДляОтправкиПриУдачнойОтправке") Тогда
		ДополнительныеПараметры.Вставить("ДополнительныеВложенияДляОтправки",
			ВходящийКонтекст.КонтекстОтправки.ДополнительныеВложенияДляОтправкиПриУдачнойОтправке);
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФНС.
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВФНС_ПередВыгрузкойФайла(Контекст, Знач Документ, Знач УникальныйИдентификатор)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ТекстСообщения = "";
	Результат = КонтекстЭДОСервер.ОтправкаРегламентированногоОтчетаВФНС_ПередВыгрузкойФайла(Контекст, Документ, УникальныйИдентификатор, ТекстСообщения);
	
	Если ЗначениеЗаполнено(ТекстСообщения) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправкаРегламентированногоОтчетаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст);
	
КонецФункции

&НаКлиенте
Процедура Подключаемый_ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплекте() Экспорт
	
	ВходящийКонтекст = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	МассивИменФайловКомплекта = ПолучитьИменаФайловЭлектронныхПредставленийВХранилище(КонтекстОтправки.Документ);
	
	ВходящийКонтекст.Вставить("МассивИменФайловКомплекта", МассивИменФайловКомплекта);
	ВходящийКонтекст.Вставить("ТекущиеИмяФайлаКомплекта", 0);
	
	ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплектеПоследовательно(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплектеПоследовательно(ВходящийКонтекст)
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если ВходящийКонтекст.ТекущиеИмяФайлаКомплекта < ВходящийКонтекст.МассивИменФайловКомплекта.Количество() Тогда
		ИмяФайла = ВходящийКонтекст.МассивИменФайловКомплекта[ВходящийКонтекст.ТекущиеИмяФайлаКомплекта];
		
		ФайлКомплекта = Новый Структура;
		ДлительнаяОтправкаКлиент.ВывестиСостояние("Получение документа из базы данных..."); // ФНС рег. отчет
		ФайлКомплекта.Вставить("ТекстВыгрузки", АдресХранилищаФайлаЭлектронногоПредставления(КонтекстОтправки.Документ, ИмяФайла));
		ФайлКомплекта.Вставить("ИмяФайлаВыгрузки", ИмяФайла);
		
		КонтекстОтправки.Вставить("ТекстВыгрузки", ФайлКомплекта.ТекстВыгрузки);
		КонтекстОтправки.Вставить("ИмяФайлаВыгрузки", ФайлКомплекта.ИмяФайлаВыгрузки);
		
		// выгружаем доверенность
		Если НЕ ВыгрузитьДоверенностьИНараститьСчетчикФайловПриНеобходимости(КонтекстОтправки) Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
			Возврат;
			
		КонецЕсли;
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		// отражаем изменения в базе
		Если НЕ ОтправкаРегламентированногоОтчетаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
			
			Возврат;
			
		КонецЕсли;
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплектеПоследовательноПослеФормированияПакета", ЭтотОбъект, ВходящийКонтекст);
		
		Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС");
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФНС.
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплектеПоследовательноПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);

		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС

		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		
		Возврат;
		
	КонецЕсли;
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	УдалитьИзВременногоХранилища(КонтекстОтправки.ТекстВыгрузки);
	
	ВходящийКонтекст.ТекущиеИмяФайлаКомплекта = ВходящийКонтекст.ТекущиеИмяФайлаКомплекта + 1;
	ОтправкаРегламентированногоОтчетаВФНСОтправкаФайловВКомплектеПоследовательно(ВходящийКонтекст);
	
КонецПроцедуры

#КонецОбласти

#Область ПодписьXMLDSig

// конец блока

&НаКлиенте
Процедура ПодписатьФайлXMLDSig(
		СертификатПодписанта,
		ФайлДанных,
		ВыполняемоеОповещение,
		Знач ПодписанныйФайл = Неопределено,
		Кодировка = Неопределено,
		НастройкиПодписи = Неопределено) Экспорт
	
	СкорректированныйПодписанныйФайл = ПодписанныйФайл;
	Если СкорректированныйПодписанныйФайл <> Неопределено И СтрНайти(СкорректированныйПодписанныйФайл, "|") Тогда
		СоставляющиеПути = СтрРазделить(СкорректированныйПодписанныйФайл, "|");
		УникальныйИдентификаторВНижнемРегистре = НРег(Новый УникальныйИдентификатор);
		СкорректированныйПодписанныйФайл = СоставляющиеПути[0]
			+ СтрЗаменить(УникальныйИдентификаторВНижнемРегистре, "-", "") + СоставляющиеПути[1];
	КонецЕсли;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатПодписанта);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 					ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ФайлДанных", 								ФайлДанных);
	ДополнительныеПараметры.Вставить("ПодписанныйФайл", 						СкорректированныйПодписанныйФайл);
	ДополнительныеПараметры.Вставить("СертификатПодписанта", 					СертификатПодписанта);
	ДополнительныеПараметры.Вставить("Кодировка", 								Кодировка);
	ДополнительныеПараметры.Вставить("НастройкиПодписи", 						НастройкиПодписи);
	ДополнительныеПараметры.Вставить("СодержимоеФайлаXMLВBase64", 				Неопределено);
	ДополнительныеПараметры.Вставить("АдресСодержимогоПодготовленногоФайлаXML", Неопределено);
	ДополнительныеПараметры.Вставить("АдресДанныхДляПодписиФайлаXML", 			Неопределено);
	ДополнительныеПараметры.Вставить("ДанныеДляПодписиФайлаXML", 				Неопределено);
	ДополнительныеПараметры.Вставить("ИмяКанонизированногоФайлаКлючаXML", 		Неопределено);
	ДополнительныеПараметры.Вставить("ВключатьХэшСертификата", 					Неопределено);
	ДополнительныеПараметры.Вставить("ПодписьКлючаДляИзвлеченияХешаВBase64", 	Неопределено);
	ДополнительныеПараметры.Вставить("ИмяКанонизированногоФайлаXML", 			Неопределено);
	ДополнительныеПараметры.Вставить("ХешСтрокаВBase64Ключа", 					Неопределено);
	ДополнительныеПараметры.Вставить("ХешСтрокаВBase64", 						Неопределено);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	ДанныеОблачнойПодписи = СвойствоСтруктуры(НастройкиПодписи, "ДанныеОблачнойПодписи");
	Если ДанныеОблачнойПодписи = Неопределено Тогда
		ПодготовитьОблачнуюПодпись("Данные для подписи", ".xml", Истина, ДополнительныеПараметры);
	Иначе
		ДополнительныеПараметры.Вставить("ДанныеОблачнойПодписи", ДанныеОблачнойПодписи);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеОпределенияАлгоритма", ЭтотОбъект, ДополнительныеПараметры);
	ПараметрыОпределенияАлгоритма = Новый Структура("Сертификат", СертификатПодписанта);
	КриптографияЭДКОКлиент.ОпределитьАлгоритм(
		Оповещение,
		МестоХраненияКлюча,
		ПараметрыОпределенияАлгоритма,
		Ложь);
		
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеОпределенияАлгоритма(Результат, ДополнительныеПараметры) Экспорт
	
	ФайлДанных = ДополнительныеПараметры.ФайлДанных;
	
	Алгоритм = ?(Результат.Выполнено И ЗначениеЗаполнено(Результат.Алгоритм), Результат.Алгоритм,
		КриптографияЭДКОКлиентСервер.АлгоритмПоУмолчанию());
	ДополнительныеПараметры.Вставить("Алгоритм", Алгоритм);
	ДополнительныеПараметры.Вставить("ТипКриптопровайдераВладельца",
		?(Результат.Выполнено, Результат.ТипКриптопровайдераВладельца, Неопределено));
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЧтенияФайлаXMLЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64", Истина, Неопределено, ФайлДанных);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	ИначеЕсли ЭтоАдресВременногоХранилища(ФайлДанных) Тогда
		ФайлДанных64 = ПолучитьАдресСтрокой64(ФайлДанных);
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64", Истина, Неопределено, ФайлДанных64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ФайлДанных);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЧтенияФайлаXMLЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	НастройкиПодписи = ДополнительныеПараметры.НастройкиПодписи;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СодержимоеФайлаXMLВBase64 = Результат.СтрокаBase64;
	
	ИсключитьПовторноеПодписывание = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(НастройкиПодписи,
		"ИсключитьПовторноеПодписывание", Ложь);
	Если ИсключитьПовторноеПодписывание Тогда
		Содержимое64 = ?(ЭтоАдресВременногоХранилища(СодержимоеФайлаXMLВBase64),
			ПолучитьАдресСтрокой64(СодержимоеФайлаXMLВBase64), СодержимоеФайлаXMLВBase64);
		ДанныеСодержимого = Base64Значение(Содержимое64);
		ТекстСодержимого = ПолучитьСтрокуИзДвоичныхДанных(ДанныеСодержимого, "utf-8");
		СодержимоеПодписано = СтрНайти(ТекстСодержимого, "<X509Certificate") <> 0;
		Если СодержимоеПодписано Тогда
			Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиПодписанногоФайлаXMLЗавершение",
				ЭтотОбъект, ДополнительныеПараметры);
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
				Истина, Неопределено, ДополнительныеПараметры.ФайлДанных);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	НеВключатьИнформациюОСертификате = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("НеВключатьИнформациюОСертификате"),
		НастройкиПодписи.НеВключатьИнформациюОСертификате, Ложь);
	
	ДополнительныеПараметры.Вставить("СодержимоеФайлаXMLВBase64", СодержимоеФайлаXMLВBase64);
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЭкспортаСертификатаПолучателяЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ НеВключатьИнформациюОСертификате Тогда
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатПодписанта);
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СтрокаBase64", Истина, Неопределено, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЭкспортаСертификатаПолучателяЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПолучателяВBase64 = Результат.СтрокаBase64;
	ДополнительныеПараметры.Вставить("СертификатПолучателяВBase64", СертификатПолучателяВBase64);
	
	НастройкиПодписи = ДополнительныеПараметры.НастройкиПодписи;
	ДобавитьШтампВремени = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДобавитьШтампВремени"),
		НастройкиПодписи.ДобавитьШтампВремени, Ложь);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиФайлаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ДобавитьШтампВремени Тогда
		Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
			ДанныеСертификата = Base64Значение(СертификатПолучателяВBase64);
			АдресСертификата = ПоместитьВоВременноеХранилище(ДанныеСертификата, Новый УникальныйИдентификатор);
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, АдресСертификата);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СертификатПолучателяВBase64);
		КонецЕсли;
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиФайлаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаСертификата = Результат.ИмяФайла;
	ДополнительныеПараметры.Вставить("ИмяФайлаСертификата", ИмяФайлаСертификата);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеХешированияСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ИмяФайлаСертификата = Неопределено Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, Хеш", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры),
			ИмяФайлаСертификата,,,,
			ДополнительныеПараметры.Алгоритм,
			Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеХешированияСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПодписанта 				= ДополнительныеПараметры.СертификатПодписанта;
	НастройкиПодписи 					= ДополнительныеПараметры.НастройкиПодписи;
	ИмяФайлаСертификата 				= ДополнительныеПараметры.ИмяФайлаСертификата;
	
	Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) И ИмяФайлаСертификата <> Неопределено Тогда
		УдалитьИзВременногоХранилища(ИмяФайлаСертификата);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ХешСертификата = Результат.Хеш;
	Если ХешСертификата <> Неопределено Тогда
		ИсключитьИдентификаторСсылкиРодителя = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(НастройкиПодписи,
			"ИсключитьИдентификаторСсылкиРодителя", Ложь);
		Если НастройкиПодписи.Свойство("ИдентификаторРодителя")
			И ЗначениеЗаполнено(НастройкиПодписи.ИдентификаторРодителя) Тогда
			
			ИдентификаторСсылкиРодителя = "";
			Если НЕ ИсключитьИдентификаторСсылкиРодителя Тогда
				УникальныйИдентификаторПодписи = Строка(Новый УникальныйИдентификатор);
				ИдентификаторСсылкиРодителя = СтрШаблон(
					"<referenceId>xmldsig-%1-ref0</referenceId>",
					УникальныйИдентификаторПодписи);
			КонецЕсли;
			
			ДополнениеОбъекта = СтрШаблон(
				"<authorities xmlns=""urn:ru:fss:integration:types:signature:v01"""
				+ " xmlns:ns2=""http://www.fss.ru/integration/types/common/v01"""
				+ " xmlns:ns3=""urn:ru:fss:integration:types:mchd:v01"">"
				  + "<authority>"
				    + "%1"
				    + "<ns3:powerOfAttorneyLink>"
				      + "<ns3:uuid>%2</ns3:uuid>"
				      + "</ns3:powerOfAttorneyLink>"
				  + "</authority>"
				+ "</authorities>",
				ИдентификаторСсылкиРодителя,
				НастройкиПодписи.ИдентификаторРодителя);
		Иначе
			ДополнениеОбъекта = Неопределено;
		КонецЕсли;
		
		НастройкиПодписи.Вставить("СертификатПодписанта", 			СертификатПодписанта);
		НастройкиПодписи.Вставить("ХешСертификата", 				ХешСертификата);
		НастройкиПодписи.Вставить("УникальныйИдентификаторПодписи", Новый УникальныйИдентификатор);
		НастройкиПодписи.Вставить("ДополнениеОбъекта", 				ДополнениеОбъекта);
	КонецЕсли;
	
	ПодстрокаУзлаДляИнформацииОПодписи = ?(НастройкиПодписи <> Неопределено
		И НастройкиПодписи.Свойство("ПодстрокаУзлаДляИнформацииОПодписи"),
		НастройкиПодписи.ПодстрокаУзлаДляИнформацииОПодписи, "");
	
	Если ЗначениеЗаполнено(ПодстрокаУзлаДляИнформацииОПодписи) Тогда
		НастройкиПодписи.Вставить("СертификатПодписанта", СертификатПодписанта);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеПоискаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если ЗначениеЗаполнено(ПодстрокаУзлаДляИнформацииОПодписи) И (НЕ СертификатПодписанта.Свойство("Владелец")
		ИЛИ НЕ СертификатПодписанта.Свойство("Поставщик") ИЛИ НЕ СертификатПодписанта.Свойство("ДействителенС")
		ИЛИ НЕ СертификатПодписанта.Свойство("ДействителенПо")) Тогда
		
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатПодписанта);
		Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатПодписанта.Отпечаток, "MY");
			
		Иначе
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				СвойстваСертификатаПодписанта =
					ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СертификатПодписанта.Отпечаток, "MY");
			Иначе
				СвойстваСертификатаПодписанта = ПолучитьСвойстваОблачногоСертификата(СертификатПодписанта.Отпечаток, "MY");
			КонецЕсли;
			РезультатВыполнения = Новый Структура("Выполнено, СертификатНайден, СвойстваСертификата",
				Истина, СвойстваСертификатаПодписанта <> Неопределено, СвойстваСертификатаПодписанта);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		КонецЕсли;
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, СертификатНайден, СвойстваСертификата",
			Истина, Истина, СертификатПодписанта);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеПоискаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	Кодировка 							= ДополнительныеПараметры.Кодировка;
	СодержимоеФайлаXMLВBase64 			= ДополнительныеПараметры.СодержимоеФайлаXMLВBase64;
	НастройкиПодписи 					= ДополнительныеПараметры.НастройкиПодписи;
	Алгоритм 							= ДополнительныеПараметры.Алгоритм;
	СертификатПолучателяВBase64 		= ДополнительныеПараметры.СертификатПолучателяВBase64;
	
	Если НЕ Результат.Выполнено ИЛИ НЕ Результат.СертификатНайден Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Трансформация1_1 = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("Трансформация1_1"),
		НастройкиПодписи.Трансформация1_1, Ложь);
	ВключатьХэшСертификата = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ВключатьХэшСертификата"),
		НастройкиПодписи.ВключатьХэшСертификата, Ложь);
	ДанныеВнутриПодписи = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДанныеВнутриПодписи"),
		НастройкиПодписи.ДанныеВнутриПодписи, Ложь);
	ДобавляемыеАтрибуты = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДобавляемыеАтрибуты"),
		НастройкиПодписи.ДобавляемыеАтрибуты, Ложь);
	ПодстрокаУзлаДляИнформацииОПодписи = ?(НастройкиПодписи <> Неопределено
		И НастройкиПодписи.Свойство("ПодстрокаУзлаДляИнформацииОПодписи"),
		НастройкиПодписи.ПодстрокаУзлаДляИнформацииОПодписи, "");
	
	Если ЗначениеЗаполнено(ПодстрокаУзлаДляИнформацииОПодписи) Тогда
		НастройкиПодписи.Вставить("СертификатПодписанта", Результат.СвойстваСертификата);
	КонецЕсли;
	
	ХэшВсегдаГОСТ2012 = Трансформация1_1 И ВключатьХэшСертификата;
	
	АдресСодержимогоПодготовленногоФайлаXML = Неопределено;
	АдресДанныхДляПодписиФайлаXML = АдресДанныхДляПодписиФайлаXMLНаСервере(
											СодержимоеФайлаXMLВBase64,
											АдресСодержимогоПодготовленногоФайлаXML,
											СертификатПолучателяВBase64,
											Кодировка,
											НастройкиПодписи,
											?(ХэшВсегдаГОСТ2012, "GOST R 34.10-2001", Алгоритм));
	Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
		ДанныеДляПодписиФайлаXML = АдресДанныхДляПодписиФайлаXML;
	Иначе
		ДанныеДляПодписиФайлаXML = ПолучитьИзВременногоХранилища(АдресДанныхДляПодписиФайлаXML);
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("АдресСодержимогоПодготовленногоФайлаXML", АдресСодержимогоПодготовленногоФайлаXML);
	ДополнительныеПараметры.Вставить("АдресДанныхДляПодписиФайлаXML", 			АдресДанныхДляПодписиФайлаXML);
	ДополнительныеПараметры.Вставить("ДанныеДляПодписиФайлаXML", 				ДанныеДляПодписиФайлаXML);
	ДополнительныеПараметры.Вставить("ВключатьХэшСертификата", 					ВключатьХэшСертификата);
	ДополнительныеПараметры.Вставить("ДанныеВнутриПодписи", 					ДанныеВнутриПодписи);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибуты", 					ДобавляемыеАтрибуты);
	ДополнительныеПараметры.Вставить("ХэшВсегдаГОСТ2012", 						ХэшВсегдаГОСТ2012);
	
	Если ВключатьХэшСертификата Тогда
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаКлючаXMLЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОКлючахXMLВBase64);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОКлючахXMLВBase64);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеПодписанияКанонизированногоФайлаКлючаXMLЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаКлючаXMLЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяКанонизированногоФайлаКлючаXML = Результат.ИмяФайла;
	
	// Из подписи извлечем DigestValue ключа
	ДополнительныеПараметры.Вставить("ИмяКанонизированногоФайлаКлючаXML", ИмяКанонизированногоФайлаКлючаXML);
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеПодписанияКанонизированногоФайлаКлючаXMLЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоПодписьСервиса(ДополнительныеПараметры) И НЕ ДополнительныеПараметры.ХэшВсегдаГОСТ2012 Тогда
		КриптографияЭДКОКлиент.ПодписатьPKCS7(Оповещение, СертификатПодписанта, ИмяКанонизированногоФайлаКлючаXML, Ложь,, Ложь);
		
	ИначеЕсли ЭтоОблачнаяПодпись(ДополнительныеПараметры) Тогда
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			КонтекстМоделиХраненияКлюча(ДополнительныеПараметры),
			ИмяКанонизированногоФайлаКлючаXML,,
			Ложь,,
			?(ДополнительныеПараметры.ХэшВсегдаГОСТ2012, "GOST R 34.10-2001", ДополнительныеПараметры.Алгоритм));
	Иначе
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			КонтекстМоделиХраненияКлюча(ДополнительныеПараметры),
			ИмяКанонизированногоФайлаКлючаXML,,
			Ложь,,
			?(ДополнительныеПараметры.ХэшВсегдаГОСТ2012, "GOST R 34.10-2001", СертификатПодписанта));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеПодписанияКанонизированногоФайлаКлючаXMLЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ДанныеДляПодписиФайлаXML = ДополнительныеПараметры.ДанныеДляПодписиФайлаXML;
	ВключатьХэшСертификата = ДополнительныеПараметры.ВключатьХэшСертификата;
	ДанныеВнутриПодписи = ДополнительныеПараметры.ДанныеВнутриПодписи;
	
	Если Результат <> Неопределено И НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ПодписьКлючаДляИзвлеченияХешаВBase64 = Неопределено;
	Если Результат <> Неопределено Тогда
		ЭтоШестнадцатеричноеПредставлениеХеша = НЕ ЭтоПодписьСервиса(ДополнительныеПараметры)
			ИЛИ ДополнительныеПараметры.ХэшВсегдаГОСТ2012;
		
		ПодписьКлючаДляИзвлеченияХешаВBase64 = ?(НЕ ЭтоШестнадцатеричноеПредставлениеХеша, Результат.ФайлПодписи, Результат.Хеш);
	КонецЕсли;
	
	КанонизированныйОбъектXMLВBase64 = ?(ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи, 
											ДанныеДляПодписиФайлаXML.КанонизированныйУзелИнформацияОбОбъектеXMLВBase64, 
											ДанныеДляПодписиФайлаXML.КанонизированныйФайлXMLВBase64);
	
	ДополнительныеПараметры.Вставить("ПодписьКлючаДляИзвлеченияХешаВBase64", ПодписьКлючаДляИзвлеченияХешаВBase64);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаXMLЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, КанонизированныйОбъектXMLВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, КанонизированныйОбъектXMLВBase64);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаXMLЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяКанонизированногоФайлаXML = Результат.ИмяФайла;
	
	// Из подписи извлечем DigestValue
	
	ДополнительныеПараметры.Вставить("ИмяКанонизированногоФайлаXML", ИмяКанонизированногоФайлаXML);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеПодписанияКанонизированногоXMLЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) И НЕ ДополнительныеПараметры.ХэшВсегдаГОСТ2012 Тогда
		ИмяФайлаПодписи = ПоместитьВоВременноеХранилище(Неопределено, Новый УникальныйИдентификатор());
		КриптографияЭДКОКлиент.ПодписатьPKCS7(Оповещение, СертификатПодписанта, ИмяКанонизированногоФайлаXML, Ложь, ИмяФайлаПодписи, Ложь);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			МестоХраненияКлюча,
			ИмяКанонизированногоФайлаXML,,
			Ложь,,
			?(ДополнительныеПараметры.ХэшВсегдаГОСТ2012, "GOST R 34.10-2001", ДополнительныеПараметры.Алгоритм));
		
	Иначе
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			МестоХраненияКлюча,
			ИмяКанонизированногоФайлаXML,,
			Ложь,,
			?(ДополнительныеПараметры.ХэшВсегдаГОСТ2012, "GOST R 34.10-2001", СертификатПодписанта));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеПодписанияКанонизированногоXMLЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 					= ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресДанныхДляПодписиФайлаXML 			= ДополнительныеПараметры.АдресДанныхДляПодписиФайлаXML;
	ДанныеДляПодписиФайлаXML 				= ДополнительныеПараметры.ДанныеДляПодписиФайлаXML;
	ПодписьКлючаДляИзвлеченияХешаВBase64 	= ДополнительныеПараметры.ПодписьКлючаДляИзвлеченияХешаВBase64;
	ДобавляемыеАтрибуты 					= ДополнительныеПараметры.ДобавляемыеАтрибуты;
	НастройкиПодписи 						= ДополнительныеПараметры.НастройкиПодписи;
	ПодписьСервиса 							= ЭтоПодписьСервиса(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ЭтоШестнадцатеричноеПредставлениеХеша = НЕ ПодписьСервиса ИЛИ ДополнительныеПараметры.ХэшВсегдаГОСТ2012;
	
	ПодписьДляИзвлеченияХешаВBase64 = ?(НЕ ЭтоШестнадцатеричноеПредставлениеХеша, Результат.ФайлПодписи, Результат.Хеш);
	
	ДополнительныеПараметры.Вставить("ПодписьДляИзвлеченияХешаВBase64", ПодписьДляИзвлеченияХешаВBase64);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаПодписанныхСвойств",
		ЭтотОбъект, ДополнительныеПараметры);
	ДобавитьШтампВремени = ?(НастройкиПодписи <> Неопределено И НастройкиПодписи.Свойство("ДобавитьШтампВремени"),
		НастройкиПодписи.ДобавитьШтампВремени, Ложь);
	Если ДобавитьШтампВремени Тогда
		Если ПодписьСервиса Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
				Истина, Неопределено, ДанныеДляПодписиФайлаXML.КанонизированныйУзелПодписанныхСвойствВBase64);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение,
				ДанныеДляПодписиФайлаXML.КанонизированныйУзелПодписанныхСвойствВBase64);
		КонецЕсли;
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаПодписанныхСвойств(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяКанонизированногоФайлаПодписанныхСвойств = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ИмяКанонизированногоФайлаПодписанныхСвойств",
		ИмяКанонизированногоФайлаПодписанныхСвойств);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеХешированияКанонизированногоФайлаПодписанныхСвойств",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ИмяКанонизированногоФайлаПодписанныхСвойств = Неопределено Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, Хеш", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			ЭтоПодписьСервиса(ДополнительныеПараметры),
			ИмяКанонизированногоФайлаПодписанныхСвойств,,
			Ложь,,
			?(ЭтоПодписьСервиса(МестоХраненияКлюча) ИЛИ ЭтоОблачнаяПодпись(МестоХраненияКлюча),
				ДополнительныеПараметры.Алгоритм, СертификатПодписанта),
			Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеХешированияКанонизированногоФайлаПодписанныхСвойств(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 					= ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресДанныхДляПодписиФайлаXML 			= ДополнительныеПараметры.АдресДанныхДляПодписиФайлаXML;
	ДанныеДляПодписиФайлаXML 				= ДополнительныеПараметры.ДанныеДляПодписиФайлаXML;
	ПодписьКлючаДляИзвлеченияХешаВBase64 	= ДополнительныеПараметры.ПодписьКлючаДляИзвлеченияХешаВBase64;
	ДобавляемыеАтрибуты 					= ДополнительныеПараметры.ДобавляемыеАтрибуты;
	НастройкиПодписи 						= ДополнительныеПараметры.НастройкиПодписи;
	ПодписьДляИзвлеченияХешаВBase64 		= ДополнительныеПараметры.ПодписьДляИзвлеченияХешаВBase64;
	ПодписьСервиса 							= ЭтоПодписьСервиса(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ХешСтрокаПодписанныхСвойств = Результат.Хеш;
	Если ЗначениеЗаполнено(ХешСтрокаПодписанныхСвойств) Тогда
		ДанныеХешСтрокиПодписанныхСвойств = ПолучитьДвоичныеДанныеИзHexСтроки(ХешСтрокаПодписанныхСвойств);
		ХешСтрокаПодписанныхСвойствВBase64 = ПолучитьBase64СтрокуИзДвоичныхДанных(ДанныеХешСтрокиПодписанныхСвойств);
		ХешСтрокаПодписанныхСвойствВBase64 = СтрЗаменить(ХешСтрокаПодписанныхСвойствВBase64, Символы.ВК, "");
		ХешСтрокаПодписанныхСвойствВBase64 = СтрЗаменить(ХешСтрокаПодписанныхСвойствВBase64, Символы.ПС, "");
	Иначе
		ХешСтрокаПодписанныхСвойствВBase64 = Неопределено;
	КонецЕсли;
	НастройкиПодписи.Вставить("ХешСтрокаПодписанныхСвойствВBase64", ХешСтрокаПодписанныхСвойствВBase64);
	
	ЭтоШестнадцатеричноеПредставлениеХеша = НЕ ПодписьСервиса ИЛИ ДополнительныеПараметры.ХэшВсегдаГОСТ2012;
	
	Если НЕ ЭтоШестнадцатеричноеПредставлениеХеша Тогда
		РезультатХэширования = ХешСтрокаВBase64КанонизированногоФайлаXMLИзПодписиНаСервере(
			ПодписьКлючаДляИзвлеченияХешаВBase64,
			ПодписьДляИзвлеченияХешаВBase64,
			АдресДанныхДляПодписиФайлаXML,
			Истина,
			ДополнительныеПараметры.Алгоритм);
		
		ХешСтрокаВBase64Ключа = ?(РезультатХэширования.Свойство("ХешСтрокаВBase64Ключа"), РезультатХэширования.ХешСтрокаВBase64Ключа, Неопределено);
		ХешСтрокаВBase64 = РезультатХэширования.ХешСтрокаВBase64;
		
	Иначе
		ХешСтрокаВBase64Ключа = ПодписьКлючаДляИзвлеченияХешаВBase64;
		ХешСтрокаВBase64 = ПодписьДляИзвлеченияХешаВBase64;
	КонецЕсли;
	
	// Вставим сформированную хеш-строку в канонизированный подписываемый узел
	СконвертироватьИзШестнадцатеричногоПредставленияХеша = ЭтоШестнадцатеричноеПредставлениеХеша;
	СтрокаНаПодписьBase64 = КанонизированныйУзелПодписьВBase64СПодставленнымХешНаСервере(
		ДанныеДляПодписиФайлаXML.КанонизированныйУзелПодписьВBase64,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		СконвертироватьИзШестнадцатеричногоПредставленияХеша,
		ДобавляемыеАтрибуты,
		НастройкиПодписи);
	
	Если СтрокаНаПодписьBase64 = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ХешСтрокаВBase64Ключа", ХешСтрокаВBase64Ключа);
	ДополнительныеПараметры.Вставить("ХешСтрокаВBase64", ХешСтрокаВBase64);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если ПодписьСервиса ИЛИ ЭтоАдресВременногоХранилища(СтрокаНаПодписьBase64) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, СтрокаНаПодписьBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СтрокаНаПодписьBase64, ".xml");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ВключатьХэшСертификата = ДополнительныеПараметры.ВключатьХэшСертификата;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	НастройкиПодписи					= ДополнительныеПараметры.НастройкиПодписи;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаКанонизированногоУзлаXML = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ИмяФайлаКанонизированногоУзлаXML", ИмяФайлаКанонизированногоУзлаXML);
	
	Если ВключатьХэшСертификата Тогда
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеПодписанияФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
			
		ПараметрыПодписи = Неопределено;
		Если ЭтоОблачнаяПодпись(ДополнительныеПараметры) Тогда
			ПараметрыПодписи = ДополнительныеПараметры.ДанныеОблачнойПодписи;
		КонецЕсли;
		
		КриптографияЭДКОКлиент.ПодписатьPKCS7(Оповещение, СертификатПодписанта, ИмяФайлаКанонизированногоУзлаXML, Ложь,
			?(НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры), Неопределено, ".p7s"), Ложь, , , ПараметрыПодписи);
			
	Иначе
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеПолученияАлгоритмовЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, Алгоритмы", Истина, Неопределено, Неопределено);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			КриптографияЭДКОКлиент.ПолучитьАлгоритмы(
				Оповещение,
				Ложь,,
				СертификатПодписанта);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеПолученияАлгоритмовЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	Алгоритм 							= ДополнительныеПараметры.Алгоритм;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Крипт = Результат.МенеджерКриптографии;
	Алгоритмы = Результат.Алгоритмы;
	
	// Находим нужный алгоритм хеширования
	ПодстрокаАлгоритмаХеширования = ?(Алгоритм = "GOST R 34.10-2012-256", "2012 256",
									?(Алгоритм = "GOST R 34.10-2012-512", "2012 512",
												 "94"));
	Если Алгоритмы <> Неопределено Тогда
		ИдентификаторНайденногоАлгоритма = 0;
		Для каждого Алгоритм Из Алгоритмы Цикл
			Если ИдентификаторНайденногоАлгоритма = 0 И СтрНайти(Алгоритм.Имя, "34.11") > 0
				И СтрНайти(Алгоритм.Имя, ПодстрокаАлгоритмаХеширования) > 0
				И СтрНайти(Алгоритм.Имя, "MAC") = 0 Тогда
				
				ИдентификаторНайденногоАлгоритма = Алгоритм.Идентификатор;
			КонецЕсли;
		КонецЦикла;
	Иначе
		ИдентификаторНайденногоАлгоритма = КриптографияЭДКОКлиентСервер.СвойстваАлгоритма(Алгоритм).АлгоритмХеширования;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("Крипт", 								Крипт);
	ДополнительныеПараметры.Вставить("ИдентификаторНайденногоАлгоритма", 	ИдентификаторНайденногоАлгоритма);
	
	ПараметрыПодписи = Неопределено;
	Если ЭтоОблачнаяПодпись(ДополнительныеПараметры) Тогда
		ПараметрыПодписи = ДополнительныеПараметры.ДанныеОблачнойПодписи;
	КонецЕсли;
	ДополнительныеПараметры.Вставить("ПараметрыПодписи", 	ПараметрыПодписи);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеОпределенияИдентификатораАлгоритма",
		ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеОпределенияИдентификатораАлгоритма(Результат, ДополнительныеПараметры) Экспорт
	
	// Подписываем канонизированный узел "SignedInfo" файла XML, содержащий хеш-строку
	Оповещение = Новый ОписаниеОповещения(
		"ПодписатьФайлXMLDSigПослеПодписанияФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);
	ПараметрыКриптографии = Новый Структура("АлгоритмХеширования",
		ДополнительныеПараметры.ИдентификаторНайденногоАлгоритма);
		
	ПараметрыПодписи = Неопределено;
	Если ЭтоОблачнаяПодпись(ДополнительныеПараметры) Тогда
		ПараметрыПодписи = ДополнительныеПараметры.ДанныеОблачнойПодписи;
	КонецЕсли;
	
	КриптографияЭДКОКлиент.Подписать(
		Оповещение,
		ДополнительныеПараметры.СертификатПодписанта,
		ДополнительныеПараметры.ИмяФайлаКанонизированногоУзлаXML,
		ПараметрыКриптографии,
		?(НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры), Неопределено, ".bin"),
		Ложь,
		ДополнительныеПараметры.Крипт, 
		ПараметрыПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеПодписанияФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВключатьХэшСертификата 				= ДополнительныеПараметры.ВключатьХэшСертификата;
	НастройкиПодписи 					= ДополнительныеПараметры.НастройкиПодписи;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеВопросаПриОшибкеПодписи",
			ЭтотОбъект, ДополнительныеПараметры);
		
		Если ЭтоПодписьСервиса(ДополнительныеПараметры) ИЛИ НастройкиПодписи = Неопределено
			ИЛИ НЕ НастройкиПодписи.Свойство("Применение") ИЛИ НастройкиПодписи.Применение <> "ОтправкиФСС"
			ИЛИ ДополнительныеПараметры.ВключатьХэшСертификата Тогда
			
			ВыполнитьОбработкуОповещения(Оповещение, КодВозвратаДиалога.Отмена);
			
		Иначе
			ПоказатьВопросПриОшибкеКриптооперации(
				Оповещение,
				"Подписать",
				Результат.ОписаниеОшибки,
				ДополнительныеПараметры.ТипКриптопровайдераВладельца);
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если ДополнительныеПараметры.Свойство("Крипт") Тогда
		ДополнительныеПараметры.Удалить("Крипт");
	КонецЕсли;
	
	ИмяФайлаПодписи = ?(ВключатьХэшСертификата, Результат.ФайлПодписи, Результат.ИмяФайлаПодписи);
	
	ДополнительныеПараметры.Вставить("ИмяФайлаПодписи", ИмяФайлаПодписи);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЧтенияПодписанногоФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоПодписьСервиса(ДополнительныеПараметры) ИЛИ ЭтоАдресВременногоХранилища(ИмяФайлаПодписи) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64", Истина, Неопределено, ИмяФайлаПодписи);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ИмяФайлаПодписи);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеВопросаПриОшибкеПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Повторить Тогда
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеОпределенияИдентификатораАлгоритма",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
		
	Иначе
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЧтенияПодписанногоФайлаКанонизированногоУзлаXMLСПодставленнымХэшЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 						= ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресСодержимогоПодготовленногоФайлаXML 	= ДополнительныеПараметры.АдресСодержимогоПодготовленногоФайлаXML;
	ДанныеДляПодписиФайлаXML 					= ДополнительныеПараметры.ДанныеДляПодписиФайлаXML;
	ИмяКанонизированногоФайлаКлючаXML 			= ДополнительныеПараметры.ИмяКанонизированногоФайлаКлючаXML;
	ИмяКанонизированногоФайлаXML 				= ДополнительныеПараметры.ИмяКанонизированногоФайлаXML;
	ИмяКанонизированногоФайлаПодписанныхСвойств = ДополнительныеПараметры.ИмяКанонизированногоФайлаПодписанныхСвойств;
	ХешСтрокаВBase64Ключа 						= ДополнительныеПараметры.ХешСтрокаВBase64Ключа;
	ХешСтрокаВBase64 							= ДополнительныеПараметры.ХешСтрокаВBase64;
	ИмяФайлаКанонизированногоУзлаXML 			= ДополнительныеПараметры.ИмяФайлаКанонизированногоУзлаXML;
	ИмяФайлаПодписи 							= ДополнительныеПараметры.ИмяФайлаПодписи;
	ПодписанныйФайл 							= ДополнительныеПараметры.ПодписанныйФайл;
	ВключатьХэшСертификата 						= ДополнительныеПараметры.ВключатьХэшСертификата;
	НастройкиПодписи 							= ДополнительныеПараметры.НастройкиПодписи;
	ЛокальнаяПодпись 							= ЭтоЛокальнаяПодпись(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ПодписанныеДанныеВBase64 = Результат.СтрокаBase64;
	
	Если НЕ ЭтоПодписьСервиса(ДополнительныеПараметры) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяКанонизированногоФайлаXML);
		Если ВключатьХэшСертификата Тогда
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяКанонизированногоФайлаКлючаXML);
		КонецЕсли;
		Если ЗначениеЗаполнено(ИмяКанонизированногоФайлаПодписанныхСвойств) Тогда
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяКанонизированногоФайлаПодписанныхСвойств);
		КонецЕсли;
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаКанонизированногоУзлаXML);
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаПодписи);
	КонецЕсли;
	
	КанонизированныйУзелЗначенияПодписиВBase64 =
		?(ДанныеДляПодписиФайлаXML.Свойство("КанонизированныйУзелЗначенияПодписиВBase64"),
		ДанныеДляПодписиФайлаXML.КанонизированныйУзелЗначенияПодписиВBase64, Неопределено);
	НастройкиПодписи.Вставить("КанонизированныйУзелЗначенияПодписиВBase64", КанонизированныйУзелЗначенияПодписиВBase64);
	СодержимоеПодписанногоФайлаXMLВBase64 = ПодписанныйФайлXMLВBase64НаСервере(
		АдресСодержимогоПодготовленногоФайлаXML,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		ПодписанныеДанныеВBase64,
		НЕ ЛокальнаяПодпись,
		ВключатьХэшСертификата,
		НастройкиПодписи);
	
	ДополнительныеПараметры.Вставить("СодержимоеПодписанногоФайлаXMLВBase64", СодержимоеПодписанногоФайлаXMLВBase64);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаЗначенияПодписи",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ЗначениеЗаполнено(НастройкиПодписи.КанонизированныйУзелЗначенияПодписиВBase64) Тогда
		Если НЕ ЛокальнаяПодпись Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
				Истина, Неопределено, НастройкиПодписи.КанонизированныйУзелЗначенияПодписиВBase64);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение,
				НастройкиПодписи.КанонизированныйУзелЗначенияПодписиВBase64);
		КонецЕсли;
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиКанонизированногоФайлаЗначенияПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяКанонизированногоФайлаЗначенияПодписи = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ИмяКанонизированногоФайлаЗначенияПодписи",
		ИмяКанонизированногоФайлаЗначенияПодписи);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеХешированияКанонизированногоФайлаЗначенияПодписи",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ИмяКанонизированногоФайлаЗначенияПодписи = Неопределено Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, Хеш", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры),
			ИмяКанонизированногоФайлаЗначенияПодписи,,
			Ложь,,
			?(ЭтоПодписьСервиса(МестоХраненияКлюча) ИЛИ ЭтоОблачнаяПодпись(МестоХраненияКлюча),
				ДополнительныеПараметры.Алгоритм, СертификатПодписанта),
			Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеХешированияКанонизированногоФайлаЗначенияПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 						= ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресСодержимогоПодготовленногоФайлаXML 	= ДополнительныеПараметры.АдресСодержимогоПодготовленногоФайлаXML;
	ДанныеДляПодписиФайлаXML 					= ДополнительныеПараметры.ДанныеДляПодписиФайлаXML;
	ИмяФайлаКанонизированногоУзлаXML 			= ДополнительныеПараметры.ИмяФайлаКанонизированногоУзлаXML;
	ИмяФайлаПодписи 							= ДополнительныеПараметры.ИмяФайлаПодписи;
	ПодписанныйФайл 							= ДополнительныеПараметры.ПодписанныйФайл;
	ВключатьХэшСертификата 						= ДополнительныеПараметры.ВключатьХэшСертификата;
	НастройкиПодписи 							= ДополнительныеПараметры.НастройкиПодписи;
	СодержимоеПодписанногоФайлаXMLВBase64 		= ДополнительныеПараметры.СодержимоеПодписанногоФайлаXMLВBase64;
	ЛокальнаяПодпись 							= ЭтоЛокальнаяПодпись(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ХешСтрокаЗначенияПодписи = Результат.Хеш;
	
	ТолькоСвойстваБезШтампаВремени = ?(НастройкиПодписи <> Неопределено
		И НастройкиПодписи.Свойство("ТолькоСвойстваБезШтампаВремени"),
		НастройкиПодписи.ТолькоСвойстваБезШтампаВремени, Ложь);
	Если ЗначениеЗаполнено(ХешСтрокаЗначенияПодписи) И НЕ ТолькоСвойстваБезШтампаВремени Тогда
		СодержимоеПодписанногоФайлаXMLВBase64 = ПолучитьШтампВремениИПодставитьВПодписанныйФайлXMLВBase64НаСервере(
			СодержимоеПодписанногоФайлаXMLВBase64,
			ХешСтрокаЗначенияПодписи,
			НЕ ЛокальнаяПодпись);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлXMLDSigПослеЗаписиПодписанногоФайлаXMLЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЛокальнаяПодпись Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, СодержимоеПодписанногоФайлаXMLВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	ИначеЕсли ЭтоАдресВременногоХранилища(ДополнительныеПараметры.ФайлДанных) Тогда
		АдресПодписанногоФайлаXML = ПоместитьВоВременноеХранилищеДанныеСКлиента(СодержимоеПодписанногоФайлаXMLВBase64,
			?(ПодписанныйФайл = Неопределено, Новый УникальныйИдентификатор, ПодписанныйФайл));
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, АдресПодписанногоФайлаXML);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		Если ПодписанныйФайл = Неопределено Тогда
			ПодписанныйФайл = ".xml";
		КонецЕсли;
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СодержимоеПодписанногоФайлаXMLВBase64, ПодписанныйФайл);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлXMLDSigПослеЗаписиПодписанногоФайлаXMLЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ПодписанныйФайл = Результат.ИмяФайла;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ПодписанныйФайл);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьВопросПриОшибкеКриптооперации(
		ОповещениеОЗавершении = Неопределено,
		ТипОперации = "Подписать",
		ОписаниеОшибки = "",
		ТипКриптопровайдераВладельца = Неопределено) Экспорт
	
	ЭтоПредупреждение = Ложь;
	МассивСообщений = Новый Массив;
	ТекстПереводСтроки = "
		|";
	СсылкаАвторизованныеУЦ = Новый ФорматированнаяСтрока(НСтр("ru = 'ссылке'"),,,,
		"https://v8.1c.ru/buhv8/1s-otchetnost/");
	ТекстПриКритическойОшибке = НСтр("ru = 'Проверьте, что криптопровайдер зарегистрирован, лицензия не истекла.'")
		+ " " + НСтр("ru = 'При продолжении ошибки переустановите криптопровайдер и перезагрузите компьютер.'");
	РекомендуемаяМинимальнаяВерсияКриптоПроCSP = "5.0.11635";
	СсылкаНаКриптоПроCSP = Новый ФорматированнаяСтрока(НСтр("ru = 'КриптоПро CSP'"),,,, "https://cryptopro.ru/");
	ТекстВерсииОбновленияКриптоПроCSP = СтрШаблон(
		НСтр("ru = 'до версии %1 или выше'"),
		РекомендуемаяМинимальнаяВерсияКриптоПроCSP);
	ТекстРекомендацииОбновитьКриптопровайдер = НСтр("ru = 'Рекомендуется обновить криптопровайдер'");
	ТекстТехническиеПодробности = НСтр("ru = 'Технические подробности ошибки записаны в журнал регистрации.'");
	СсылкаНаСтатьюФСС = Новый ФорматированнаяСтрока(НСтр("ru = 'статье'"),,,,
		"https://its.1c.ru/bmk/elreps/fss_cert");
	СсылкаНаИнструкциюФСС = Новый ФорматированнаяСтрока(НСтр("ru = 'инструкцией'"),,,,
		"https://its.1c.ru/bmk/elreps/fss_cert");
	СсылкаНаВопросыОтветыФСС = Новый ФорматированнаяСтрока(НСтр("ru = 'вопросами и ответами'"),,,,
		"https://its.1c.ru/bmk/elreps/eln");
	ТекстНастройкиОбменаСФСС = Новый ФорматированнаяСтрока(
		НСтр("ru = 'Ознакомьтесь с'") + " ",
		СсылкаНаИнструкциюФСС,
		" " + НСтр("ru = 'по установке и настройке сертификатов обмена с ФСС'") + ", ",
		СсылкаНаВопросыОтветыФСС,
		".");
	
	Если ТипОперации = "ПодписатьPKCS7" И КриптографияЭДКОКлиентСервер.ЭтоОшибкаНеавторизованногоУЦ(ОписаниеОшибки) Тогда
		ЭтоПредупреждение = Истина;
		
		ОписаниеОшибкиСкорректированное = СтрЗаменить(ОписаниеОшибки, "'", """");
		МассивСообщений.Добавить(ОписаниеОшибкиСкорректированное);
		МассивСообщений.Добавить(ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Подробнее о настройках подключения читайте в'") + " ");
		МассивСообщений.Добавить(СсылкаНаСтатьюФСС);
		МассивСообщений.Добавить(".");
		
	ИначеЕсли ТипОперации = "Подписать" ИЛИ ТипОперации = "ПодписатьPKCS7" Тогда
		ТекстСообщения = Новый ФорматированнаяСтрока(
			ОписаниеОшибки,,
			ЦветЭлементаСтиля("ЦветОшибкиВПротоколеБРО"));
		МассивСообщений.Добавить(ТекстСообщения);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Попробуйте выполнить следующие действия.'"));
		
		Если КриптографияЭДКОКлиентСервер.ЭтоКритическаяОшибкаКриптопровайдера(ОписаниеОшибки) Тогда
			МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
			МассивСообщений.Добавить(ТекстПриКритическойОшибке);
			
		Иначе
			НомерСообщения = 1;
			НомерСообщенияСтрокой = Формат(НомерСообщения, "ЧДЦ=; ЧН=; ЧГ=");
			МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
			МассивСообщений.Добавить(НомерСообщенияСтрокой + ". "
				+ НСтр("ru = 'Вставьте токен (флешку) в USB-порт или удостоверьтесь, что есть доступ к закрытому ключу, и повторите попытку.'"));
			
			Если ТипКриптопровайдераВладельца <> Неопределено Тогда
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'При использовании программы на нескольких компьютерах повторите попытку на том, где настроен %1.'"),
					?(ТипКриптопровайдераВладельца = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet"),
						НСтр("ru = 'ViPNet CSP'"),
						?(ТипКриптопровайдераВладельца = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro"),
						НСтр("ru = 'КриптоПро CSP'"), Строка(ТипКриптопровайдераВладельца))));
					
				НомерСообщения = НомерСообщения + 1;
				НомерСообщенияСтрокой = Формат(НомерСообщения, "ЧДЦ=; ЧН=; ЧГ=");
				МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
				МассивСообщений.Добавить(НомерСообщенияСтрокой + ". " + ТекстСообщения);
			КонецЕсли;
			
			НомерСообщения = НомерСообщения + 1;
			НомерСообщенияСтрокой = Формат(НомерСообщения, "ЧДЦ=; ЧН=; ЧГ=");
			ТекстСообщения = НомерСообщенияСтрокой + ". " + НСтр("ru = 'Перенесите закрытый ключ с другого компьютера.'");
			СсылкаНаИнструкциюViPNetCSP = "https://its.1c.ru/bmk/elreps/key_trans_vipnet";
			СсылкаНаИнструкциюКриптоПроCSP = "https://its.1c.ru/bmk/elreps/key_trans_cryptopro";
			
			Если ТипКриптопровайдераВладельца <>
				ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet")
				И ТипКриптопровайдераВладельца <>
				ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro") Тогда
				
				СсылкаНаИнструкциюViPNetCSP = Новый ФорматированнаяСтрока(НСтр("ru = 'ViPNet CSP'"),,,,
					СсылкаНаИнструкциюViPNetCSP);
				СсылкаНаИнструкциюКриптоПроCSP = Новый ФорматированнаяСтрока(НСтр("ru = 'КриптоПро CSP'"),,,,
					СсылкаНаИнструкциюКриптоПроCSP);
				ТекстСообщения = Новый ФорматированнаяСтрока(
					ТекстСообщения,
					" ",
					НСтр("ru = 'Инструкция по переносу для'") + " ",
					СсылкаНаИнструкциюViPNetCSP,
					" " + НСтр("ru = 'и'") + " ",
					СсылкаНаИнструкциюКриптоПроCSP,
					".");
				
			Иначе
				СсылкаНаИнструкцию = Новый ФорматированнаяСтрока(НСтр("ru = 'Инструкция по переносу'"),,,,
					?(ТипКриптопровайдераВладельца = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet"),
					СсылкаНаИнструкциюViPNetCSP, СсылкаНаИнструкциюКриптоПроCSP));
				ТекстСообщения = Новый ФорматированнаяСтрока(
					ТекстСообщения,
					" ",
					СсылкаНаИнструкцию,
					".");
			КонецЕсли;
			
			МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
			МассивСообщений.Добавить(ТекстСообщения);
		КонецЕсли;
		
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстТехническиеПодробности);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстНастройкиОбменаСФСС);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Повторить попытку подписи?'"));
		
	ИначеЕсли ТипОперации = "Зашифровать" Тогда
		ТекстСообщения = Новый ФорматированнаяСтрока(
			ОписаниеОшибки,,
			ЦветЭлементаСтиля("ЦветОшибкиВПротоколеБРО"));
		МассивСообщений.Добавить(ТекстСообщения);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Попробуйте выполнить следующие действия.'"));
		
		ВерсияКриптоПроCSP = ?(ЭтоВебКлиент(), "",
			ОбщегоНазначенияЭДКОКлиент.ИнформацияОКриптопровайдерах().ВерсияКриптоПроCSP);
		ВерсияКриптоПроCSP = ОбщегоНазначенияЭДКОКлиентСервер.ПриведеннаяВерсия(ВерсияКриптоПроCSP, 3);
		
		Если ЗначениеЗаполнено(ВерсияКриптоПроCSP) И ОбщегоНазначенияКлиентСервер.СравнитьВерсииБезНомераСборки(
			ВерсияКриптоПроCSP, РекомендуемаяМинимальнаяВерсияКриптоПроCSP) < 0 Тогда
			
			ТекстРекомендацииОбновитьКриптоПроCSP = Новый ФорматированнаяСтрока(ТекстРекомендацииОбновитьКриптопровайдер + " ",
				СсылкаНаКриптоПроCSP,
				" " + ТекстВерсииОбновленияКриптоПроCSP + ".");
			МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
			МассивСообщений.Добавить(ТекстРекомендацииОбновитьКриптоПроCSP);
		КонецЕсли;
		
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстПриКритическойОшибке);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстТехническиеПодробности);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстНастройкиОбменаСФСС);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Повторить попытку шифрования?'"));
		
	ИначеЕсли ТипОперации = "Расшифровать" Тогда
		ТекстСообщения = Новый ФорматированнаяСтрока(
			ОписаниеОшибки,,
			ЦветЭлементаСтиля("ЦветОшибкиВПротоколеБРО"));
		МассивСообщений.Добавить(ТекстСообщения);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Попробуйте выполнить следующие действия.'"));
		
		ВерсияКриптоПроCSP = ?(ЭтоВебКлиент(), "",
			ОбщегоНазначенияЭДКОКлиент.ИнформацияОКриптопровайдерах().ВерсияКриптоПроCSP);
		ВерсияКриптоПроCSP = ОбщегоНазначенияЭДКОКлиентСервер.ПриведеннаяВерсия(ВерсияКриптоПроCSP, 3);
		
		Если ЗначениеЗаполнено(ВерсияКриптоПроCSP) И ОбщегоНазначенияКлиентСервер.СравнитьВерсииБезНомераСборки(
			ВерсияКриптоПроCSP, РекомендуемаяМинимальнаяВерсияКриптоПроCSP) < 0 Тогда
			
			ТекстРекомендацииОбновитьКриптоПроCSP = Новый ФорматированнаяСтрока(ТекстРекомендацииОбновитьКриптопровайдер + " ",
				СсылкаНаКриптоПроCSP,
				" " + ТекстВерсииОбновленияКриптоПроCSP + ". ",
				НСтр("ru = 'Если после обновления ошибка продолжит воспроизводиться, следует сгенерировать новые ключи для страхователя и получить новый сертификат страхователя на обновленной версии КриптоПро CSP.'"));
			МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
			МассивСообщений.Добавить(ТекстРекомендацииОбновитьКриптоПроCSP);
			
		ИначеЕсли ЗначениеЗаполнено(ВерсияКриптоПроCSP) Тогда
			ТекстРекомендацииПереиздатьСертификат = СтрШаблон(
				НСтр("ru = 'Если ключи сертификата страхователя были созданы при КриптоПро CSP версии меньше %1, рекомендуется сгенерировать новые ключи для страхователя и получить новый сертификат страхователя на обновленной версии КриптоПро CSP.'"),
				РекомендуемаяМинимальнаяВерсияКриптоПроCSP);
			МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
			МассивСообщений.Добавить(ТекстРекомендацииПереиздатьСертификат);
		КонецЕсли;
		
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстПриКритическойОшибке);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстТехническиеПодробности);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(ТекстНастройкиОбменаСФСС);
		МассивСообщений.Добавить(ТекстПереводСтроки + ТекстПереводСтроки);
		МассивСообщений.Добавить(НСтр("ru = 'Повторить попытку расшифровки?'"));
		
	КонецЕсли;
	
	ТекстСообщения = Новый ФорматированнаяСтрока(МассивСообщений);
	КнопкиДиалога = Новый СписокЗначений;
	Если ЭтоПредупреждение Тогда
		КнопкиДиалога.Добавить(, НСтр("ru = 'ОК'"));
	Иначе
		КнопкиДиалога.Добавить(, НСтр("ru = 'Повторить'"));
		КнопкиДиалога.Добавить(, НСтр("ru = 'Отмена'"));
	КонецЕсли;
	ВопросПредупреждение(
		ТекстСообщения,,,
		ОповещениеОЗавершении,
		БиблиотекаКартинок.ОшибкаОтправки,
		КнопкиДиалога);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЦветЭлементаСтиля(ИмяЦветаСтиля)
	
	Возврат ЦветаСтиля[ИмяЦветаСтиля];
	
КонецФункции

&НаСервереБезКонтекста
Функция КанонизированныйУзелПодписьВBase64СПодставленнымХешНаСервере(
		КанонизированныйУзелПодписьВBase64,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		СконвертироватьИзШестнадцатеричногоПредставленияХеша,
		ДобавляемыеАтрибуты = Неопределено,
		НастройкиПодписи = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.КанонизированныйУзелПодписьВBase64СПодставленнымХеш(
		КанонизированныйУзелПодписьВBase64,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		СконвертироватьИзШестнадцатеричногоПредставленияХеша,
		ДобавляемыеАтрибуты,
		НастройкиПодписи);
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресДанныхДляПодписиФайлаXMLНаСервере(
		СодержимоеФайлаXMLВBase64,
		АдресСодержимогоПодготовленногоФайлаXML = Неопределено,
		СертификатПолучателяВBase64 = Неопределено,
		Кодировка = Неопределено,
		НастройкиПодписи = Неопределено,
		Алгоритм = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.АдресДанныхДляПодписиФайлаXML(
		СодержимоеФайлаXMLВBase64,
		АдресСодержимогоПодготовленногоФайлаXML,
		СертификатПолучателяВBase64,
		Кодировка,
		НастройкиПодписи,
		Алгоритм);
	
КонецФункции

&НаСервереБезКонтекста
Функция ХешСтрокаВBase64КанонизированногоФайлаXMLИзПодписиНаСервере(
		ПодписьКлючаВBase64,
		ПодписьВBase64,
		АдресДанныхДляПодписиФайлаXML,
		УдалитьПодписиИзВременногоХранилища = Ложь,
		Алгоритм = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Возврат КонтекстЭДО.ХешСтрокаВBase64КанонизированногоФайлаXMLИзПодписи(
		ПодписьКлючаВBase64,
		ПодписьВBase64,
		АдресДанныхДляПодписиФайлаXML,
		УдалитьПодписиИзВременногоХранилища,
		Алгоритм);
	
КонецФункции

&НаСервереБезКонтекста
Функция СтрокаВШестнадцатеричномПредставленииВBase64НаСервере(СтрокаВШестнадцатеричномПредставлении)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Возврат КонтекстЭДО.СтрокаВШестнадцатеричномПредставленииВBase64(СтрокаВШестнадцатеричномПредставлении);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПодписанныйФайлXMLВBase64НаСервере(
		АдресСодержимогоПодготовленногоФайлаXML,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		ПодписанныеДанныеВBase64,
		ВозвращатьАдресВоВременномХранилище,
		ВключатьХэшСертификата,
		НастройкиПодписи = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПодписанныйФайлXMLВBase64(
		АдресСодержимогоПодготовленногоФайлаXML,
		ХешСтрокаВBase64Ключа,
		ХешСтрокаВBase64,
		ПодписанныеДанныеВBase64,
		ВозвращатьАдресВоВременномХранилище,
		ВключатьХэшСертификата,
		НастройкиПодписи);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьШтампВремениИПодставитьВПодписанныйФайлXMLВBase64НаСервере(
		СодержимоеПодписанногоФайлаXMLВBase64,
		ХешСтрокаЗначенияПодписи,
		ВозвращатьАдресВоВременномХранилище)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьШтампВремениИПодставитьВПодписанныйФайлXMLВBase64(
		СодержимоеПодписанногоФайлаXMLВBase64,
		ХешСтрокаЗначенияПодписи,
		ВозвращатьАдресВоВременномХранилище);
	
КонецФункции

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSig(
		СертификатАбонента,
		ПодписанныйФайл,
		ВыполняемоеОповещение,
		ЗаписыватьОшибкиВЖурналРегистрации = Ложь,
		УдалятьЛишниеАтрибуты = Ложь,
		ИспользоватьПреобразованиеККаноническомуXML = Ложь) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатАбонента);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("СертификатАбонента", 					СертификатАбонента);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ЗаписыватьОшибкиВЖурналРегистрации", 	ЗаписыватьОшибкиВЖурналРегистрации);
	ДополнительныеПараметры.Вставить("УдалятьЛишниеАтрибуты", 				УдалятьЛишниеАтрибуты);
	ДополнительныеПараметры.Вставить("ИспользоватьПреобразованиеККаноническомуXML", ИспользоватьПреобразованиеККаноническомуXML);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеЧтенияПодписанногоФайла",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64", Истина, Неопределено, ПодписанныйФайл);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПодписанныйФайл);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеЧтенияПодписанногоФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗаписыватьОшибкиВЖурналРегистрации 	= ДополнительныеПараметры.ЗаписыватьОшибкиВЖурналРегистрации;
	УдалятьЛишниеАтрибуты 				= ДополнительныеПараметры.УдалятьЛишниеАтрибуты;
	ИспользоватьПреобразованиеККаноническомуXML =
		ДополнительныеПараметры.ИспользоватьПреобразованиеККаноническомуXML;

	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	XMLDSigВBase64 = Результат.СтрокаBase64;
	
	// разобрать XMLDSig
	
	ИнформацияXMLDSig = РазобратьXMLDSigНаСервере(
		XMLDSigВBase64,
		ЗаписыватьОшибкиВЖурналРегистрации,
		УдалятьЛишниеАтрибуты,
		ИспользоватьПреобразованиеККаноническомуXML);
		
	Если ИнформацияXMLDSig = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ИнформацияXMLDSig", ИнформацияXMLDSig);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаКанонизированнойИнформацииОбОбъекте",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ИнформацияXMLDSig.КанонизированныйУзелИнформацияОбОбъектеXMLВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ИнформацияXMLDSig.КанонизированныйУзелИнформацияОбОбъектеXMLВBase64, ".xml");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаКанонизированнойИнформацииОбОбъекте(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	
	Если НЕ Результат.Выполнено Тогда
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.СертификатПодписантаВBase64,
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелИнформацияОбОбъектеXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаКанонизированнойИнформацииОбОбъекте = Результат.ИмяФайла;
	
	// проверить хеш
	
	ИспользоватьСертификатАбонентаДляВычисленияХеша = Ложь;
	ДополнительныеПараметры.Вставить("ИспользоватьСертификатАбонентаДляВычисленияХеша",
		ИспользоватьСертификатАбонентаДляВычисленияХеша);
	ДополнительныеПараметры.Вставить("ИмяФайлаКанонизированнойИнформацииОбОбъекте",
		ИмяФайлаКанонизированнойИнформацииОбОбъекте);
	
	Оповещение = Новый ОписаниеОповещения(
		"ПроверитьПодписьФайлаXMLDSigПослеПодписанияКанонизированнойИнформацииОбОбъекте",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) И ИспользоватьСертификатАбонентаДляВычисленияХеша Тогда
		ИмяФайлаПодписиДляИзвлеченияХеша = ПоместитьВоВременноеХранилище(Неопределено, Новый УникальныйИдентификатор());
		КриптографияЭДКОКлиент.ПодписатьPKCS7(
			Оповещение,
			СертификатАбонента,
			ИмяФайлаКанонизированнойИнформацииОбОбъекте,
			Ложь,
			ИмяФайлаПодписиДляИзвлеченияХеша,,,,
			"*");
		
	Иначе
		СертификатПодписанта = Новый Структура("Сертификат", ИнформацияXMLDSig.СертификатПодписантаВBase64);
		
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			МестоХраненияКлюча,
			ИмяФайлаКанонизированнойИнформацииОбОбъекте,,
			Ложь,,
			СертификатПодписанта);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеПодписанияКанонизированнойИнформацииОбОбъекте(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЛокальнаяПодпись = ЭтоЛокальнаяПодпись(МестоХраненияКлюча);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	ИмяФайлаКанонизированнойИнформацииОбОбъекте = ДополнительныеПараметры.ИмяФайлаКанонизированнойИнформацииОбОбъекте;
	ИспользоватьСертификатАбонентаДляВычисленияХеша =
		ДополнительныеПараметры.ИспользоватьСертификатАбонентаДляВычисленияХеша;
		
	Если ЛокальнаяПодпись Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаКанонизированнойИнформацииОбОбъекте);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		Если НЕ ЛокальнаяПодпись Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.СертификатПодписантаВBase64,
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелИнформацияОбОбъектеXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если Результат.Свойство("Алгоритм") И ЗначениеЗаполнено(Результат.Алгоритм) Тогда
		Алгоритм = Результат.Алгоритм;
		
	Иначе
		Алгоритм = АлгоритмСертификата(ИнформацияXMLDSig.СертификатПодписантаВBase64, МестоХраненияКлюча, ЛокальнаяПодпись);
	КонецЕсли;
	ДополнительныеПараметры.Вставить("Алгоритм", Алгоритм);
	
	ЭтоШестнадцатеричноеПредставлениеХеша = ЛокальнаяПодпись ИЛИ НЕ ИспользоватьСертификатАбонентаДляВычисленияХеша;
	
	ПодписьДляИзвлеченияХешаВBase64 = ?(НЕ ЭтоШестнадцатеричноеПредставлениеХеша, Результат.ФайлПодписи, Результат.Хеш);
	
	Если НЕ ЭтоШестнадцатеричноеПредставлениеХеша Тогда
		ХешСтрокаВBase64 = ХешСтрокаВBase64КанонизированногоФайлаXMLИзПодписиНаСервере(
			Неопределено,
			ПодписьДляИзвлеченияХешаВBase64,
			ИнформацияXMLDSig,
			Истина,
			ДополнительныеПараметры.Алгоритм).ХешСтрокаВBase64;
			
	Иначе
		ХешСтрокаВBase64 = СтрокаВШестнадцатеричномПредставленииВBase64НаСервере(ПодписьДляИзвлеченияХешаВBase64);
		
	КонецЕсли;
	
	Если ХешСтрокаВBase64 <> ИнформацияXMLDSig.ХешВBase64 Тогда
		Если НЕ ЛокальнаяПодпись Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.СертификатПодписантаВBase64,
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаСертификатаПодписанта",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЛокальнаяПодпись Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ИнформацияXMLDSig.СертификатПодписантаВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ИнформацияXMLDSig.СертификатПодписантаВBase64, ".cer");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаСертификатаПодписанта(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	
	Если НЕ Результат.Выполнено Тогда
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.СертификатПодписантаВBase64,
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаСертификатаПодписанта = Результат.ИмяФайла;
	
	// зарегистрировать сертификат подписанта
	
	ДополнительныеПараметры.Вставить("ИмяФайлаСертификатаПодписанта", ИмяФайлаСертификатаПодписанта);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеИмпортаСертификатаПодписанта",
		ЭтотОбъект, ДополнительныеПараметры);
	ЗарегистрироватьСертификатВСистемеНачало(Оповещение, МестоХраненияКлюча, ИмяФайлаСертификатаПодписанта, "AddressBook");
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеИмпортаСертификатаПодписанта(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	ИмяФайлаСертификатаПодписанта = ДополнительныеПараметры.ИмяФайлаСертификатаПодписанта;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаСертификатаПодписанта);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.СертификатПодписантаВBase64,
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ОтпечатокСертификатаПодписанта = Результат.Отпечаток;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьИзВременногоХранилища(ИнформацияXMLDSig.СертификатПодписантаВBase64);
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ОтпечатокСертификатаПодписанта", ОтпечатокСертификатаПодписанта);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеПоискаСертификатаПодписанта",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СвойстваСертификатаПодписанта = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечатокСертификатаПодписанта, "AddressBook");
		Иначе
			СвойстваСертификатаПодписанта = ПолучитьСвойстваОблачногоСертификата(ОтпечатокСертификатаПодписанта, "AddressBook", Истина);
		КонецЕсли;
		Если СвойстваСертификатаПодписанта <> Неопределено Тогда
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификатаПодписанта);
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				РезультатПроверки = ПроверитьСертификатНаСервере(СвойстваСертификатаПодписанта, МестоХраненияКлюча);
			Иначе
				РезультатПроверки = СвойстваСертификатаПодписанта.Валиден;
			КонецЕсли;
			СвойстваСертификатаПодписанта.Вставить("Валиден", РезультатПроверки = Истина);
		КонецЕсли;
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, СвойстваСертификатаПодписанта <> Неопределено, СвойстваСертификатаПодписанта);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, ОтпечатокСертификатаПодписанта, "AddressBook", Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеПоискаСертификатаПодписанта(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗаписыватьОшибкиВЖурналРегистрации 	= ДополнительныеПараметры.ЗаписыватьОшибкиВЖурналРегистрации;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig 					= ДополнительныеПараметры.ИнформацияXMLDSig;
	ОтпечатокСертификатаПодписанта 		= ДополнительныеПараметры.ОтпечатокСертификатаПодписанта;
	
	Если НЕ Результат.Выполнено ИЛИ НЕ Результат.СертификатНайден ИЛИ НЕ Результат.СвойстваСертификата.Валиден Тогда
		Если Результат.Выполнено И НЕ Результат.СертификатНайден Тогда
			ТекстОшибки = НСтр("ru = 'Не удалось найти сертификат подписанта с отпечатком %1 для проверкии валидности электронной подписи.'");
			ТекстОшибки = СтрШаблон(ТекстОшибки, ОтпечатокСертификатаПодписанта);
			Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
				ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
					"Ошибка",
					ТекстОшибки,,
					Истина);
			Иначе
				ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстОшибки);
			КонецЕсли;
			
		ИначеЕсли Результат.Выполнено И НЕ Результат.СвойстваСертификата.Валиден Тогда
			ТекстОшибки = НСтр("ru = 'Не удалось проверить подпись по причине невалидности сертификата ""%1"".'");
			ПредставлениеСертификатаПодписанта = ПредставлениеСертификатаПоСвойствам(Результат.СвойстваСертификата);
			ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеСертификатаПодписанта);
			Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
				ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи XMLDSig'"),
					"Ошибка",
					ТекстОшибки,,
					Истина);
			Иначе
				ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстОшибки);
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПодписанта = Результат.СвойстваСертификата;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатПодписанта);
	
	ДополнительныеПараметры.Вставить("СертификатПодписанта", СертификатПодписанта);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаКанонизированнойПодписываемойИнформации",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаКанонизированнойПодписываемойИнформации(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча	= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	
	Если НЕ Результат.Выполнено Тогда
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаКанонизированнойПодписываемойИнформации = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ИмяФайлаКанонизированнойПодписываемойИнформации", ИмяФайлаКанонизированнойПодписываемойИнформации);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаПодписи", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ИнформацияXMLDSig.ПодписьВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ИнформацияXMLDSig.ПодписьВBase64);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеЗаписиФайлаПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	ИмяФайлаКанонизированнойПодписываемойИнформации = ДополнительныеПараметры.ИмяФайлаКанонизированнойПодписываемойИнформации;
	
	Если НЕ Результат.Выполнено Тогда
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ИнформацияXMLDSig.ПодписьВBase64,
				ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
		Иначе
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаКанонизированнойПодписываемойИнформации);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаПодписи = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ИмяФайлаПодписи", ИмяФайлаПодписи);
	
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьФайлаXMLDSigПослеПроверкиПодписи", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ПроверитьПодпись(
		Оповещение,
		СертификатПодписанта,
		ИмяФайлаПодписи,
		ИмяФайлаКанонизированнойПодписываемойИнформации,,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьФайлаXMLDSigПослеПроверкиПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИнформацияXMLDSig = ДополнительныеПараметры.ИнформацияXMLDSig;
	ИмяФайлаКанонизированнойПодписываемойИнформации = ДополнительныеПараметры.ИмяФайлаКанонизированнойПодписываемойИнформации;
	ИмяФайлаПодписи = ДополнительныеПараметры.ИмяФайлаПодписи;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьАдресаИзВременногоХранилища(
			ИнформацияXMLDSig.ПодписьВBase64,
			ИнформацияXMLDSig.КанонизированныйУзелПодписываемаяИнформацияXMLВBase64);
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаКанонизированнойПодписываемойИнформации);
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ИмяФайлаПодписи);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ПодписьВалидна = Результат.ПодписьВалидна;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция РазобратьXMLDSigНаСервере(XMLDSigВBase64,
		ЗаписыватьОшибкиВЖурналРегистрации = Ложь,
		УдалятьЛишниеАтрибуты = Ложь,
		ИспользоватьПреобразованиеККаноническомуXML = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.РазобратьXMLDSig(
		XMLDSigВBase64,
		ЗаписыватьОшибкиВЖурналРегистрации,
		УдалятьЛишниеАтрибуты,
		ИспользоватьПреобразованиеККаноническомуXML);
	
КонецФункции

&НаСервереБезКонтекста
Процедура УдалитьАдресаИзВременногоХранилища(
		Адрес1ИлиМассив,
		Адрес2 = Неопределено,
		Адрес3 = Неопределено,
		Адрес4 = Неопределено,
		Адрес5 = Неопределено,
		Адрес6 = Неопределено,
		Адрес7 = Неопределено,
		Адрес8 = Неопределено,
		Адрес9 = Неопределено,
		Адрес10 = Неопределено)
	
	Если ЗначениеЗаполнено(Адрес1ИлиМассив) И ТипЗнч(Адрес1ИлиМассив) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес1ИлиМассив);
		
	ИначеЕсли ЗначениеЗаполнено(Адрес1ИлиМассив) И (ТипЗнч(Адрес1ИлиМассив) = Тип("Массив")
		ИЛИ ТипЗнч(Адрес1ИлиМассив) = Тип("ФиксированныйМассив")) Тогда
		
		Для каждого ЭлементМассиваАдресов Из Адрес1ИлиМассив Цикл
			УдалитьИзВременногоХранилища(ЭлементМассиваАдресов);
		КонецЦикла;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес2) И ТипЗнч(Адрес2) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес2);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес3) И ТипЗнч(Адрес3) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес3);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес4) И ТипЗнч(Адрес4) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес4);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес5) И ТипЗнч(Адрес5) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес5);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес6) И ТипЗнч(Адрес6) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес6);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес7) И ТипЗнч(Адрес7) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес7);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес8) И ТипЗнч(Адрес8) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес8);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес9) И ТипЗнч(Адрес9) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес9);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Адрес10) И ТипЗнч(Адрес10) = Тип("Строка") Тогда
		УдалитьИзВременногоХранилища(Адрес10);
	КонецЕсли;
	
КонецПроцедуры

// Блок для пакетной обработки файлов для подписи
// На вход поступает готовый файл для подписи
// 
// Результат массив адресов из временного хранилища с подписанных файлов
//
// готовить файл для подписи (подстановки и трансформация
// Параметры:
//	ФайлыДанных - массив адресов временного хранилища
//

&НаКлиенте
Процедура ПодписатьФайлыXMLDSig(
		СертификатПодписанта,
		ФайлыДанных,
		ВыполняемоеОповещение,
		Кодировка = Неопределено,
		НастройкиПодписи = Неопределено) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатПодписанта);
	ДанныеДляПодписи = Новый Массив;
	
	Для Каждого СтрокаМассива Из ФайлыДанных Цикл
		НоваяСтрока = Новый Структура;
		НоваяСтрока.Вставить("ХешЗначениеКлюча", Неопределено);
		НоваяСтрока.Вставить("ХешЗначениеСодержимого", Неопределено);
		НоваяСтрока.Вставить("Подпись", Неопределено);
		НоваяСтрока.Вставить("Результат", Неопределено);
		НоваяСтрока.Вставить("КанонизированныйУзелИнформацияОбОбъектеXML", Неопределено);
		НоваяСтрока.Вставить("КанонизированныйУзелПодпись", Неопределено);
		НоваяСтрока.Вставить("КанонизированныйФайлXML", Неопределено);
		НоваяСтрока.Вставить("КанонизированныйКлюч", Неопределено);
		НоваяСтрока.Вставить("СодержимогоПодготовленногоФайлаXML", Неопределено);
		НоваяСтрока.Вставить("КанонизированныеДанные", Неопределено);
		НоваяСтрока.Вставить("ВозвращатьАдресВоВременномХранилище", Ложь);
		
		Если ЭтоАдресВременногоХранилища(СтрокаМассива) Тогда
			НоваяСтрока.Вставить("ВозвращатьАдресВоВременномХранилище", Истина);
			НоваяСтрока.Вставить("АдресДанных", СтрокаМассива);
		Иначе
			ДанныеФайла = Base64Значение(СтрокаМассива);
			НоваяСтрока.Вставить("АдресДанных", ПоместитьВоВременноеХранилище(ДанныеФайла, УникальныйИдентификатор));
		КонецЕсли;
		
		ДанныеДляПодписи.Добавить(НоваяСтрока);
	КонецЦикла;	
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ФайлыДанных", ДанныеДляПодписи);
	ДополнительныеПараметры.Вставить("СертификатПодписанта", СертификатПодписанта);
	ДополнительныеПараметры.Вставить("Кодировка", Кодировка);
	ДополнительныеПараметры.Вставить("НастройкиПодписи", НастройкиПодписи);
	ДополнительныеПараметры.Вставить("ВсегоФайлов",	ФайлыДанных.Количество());
	ДополнительныеПараметры.Вставить("ИндексФайл", 0);
	ДополнительныеПараметры.Вставить("Трансформация1_1", СвойствоСтруктуры(НастройкиПодписи, "Трансформация1_1", Ложь));
	ДополнительныеПараметры.Вставить("ВключатьХэшСертификата", СвойствоСтруктуры(НастройкиПодписи, "ВключатьХэшСертификата", Ложь));
	ДополнительныеПараметры.Вставить("ДанныеВнутриПодписи", СвойствоСтруктуры(НастройкиПодписи, "ДанныеВнутриПодписи", Ложь));
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибуты", СвойствоСтруктуры(НастройкиПодписи, "ДобавляемыеАтрибуты", Ложь));
	ДополнительныеПараметры.Вставить("НеВключатьИнформациюОСертификате", СвойствоСтруктуры(НастройкиПодписи, "НеВключатьИнформациюОСертификате", Ложь));
	ДополнительныеПараметры.Вставить("Алгоритм", СвойствоСтруктуры(НастройкиПодписи, "Алгоритм", ""));
	ДополнительныеПараметры.Вставить("ИдентификаторыПодписываемыхДанных", СвойствоСтруктуры(НастройкиПодписи, "ИдентификаторыПодписываемыхДанных", Новый Массив));
	ДанныеОблачнойПодписи = СвойствоСтруктуры(НастройкиПодписи, "ДанныеОблачнойПодписи", Новый Структура);
	Если ДанныеОблачнойПодписи = Неопределено Тогда
		ПодготовитьОблачнуюПодпись("Список файлов", ".xml", Истина, ДополнительныеПараметры);
	Иначе
		ДополнительныеПараметры.Вставить("ДанныеОблачнойПодписи", ДанныеОблачнойПодписи);
	КонецЕсли;
		
	ХэшВсегдаГОСТ2012 = ДополнительныеПараметры.Трансформация1_1 И ДополнительныеПараметры.ВключатьХэшСертификата;
	ДополнительныеПараметры.Вставить("ХэшВсегдаГОСТ2012", ХэшВсегдаГОСТ2012);
	ДополнительныеПараметры.Вставить("СертификатПолучателяВBase64", "");
	ДополнительныеПараметры.Вставить("ИдентификаторНайденногоАлгоритма", Неопределено);
	ДополнительныеПараметры.Вставить("ПакетнаяПодпись", ЭтоОблачнаяПодпись(МестоХраненияКлюча));
	
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigПослеОпределенияАлгоритма", ЭтотОбъект, ДополнительныеПараметры);
	Если ЗначениеЗаполнено(ДополнительныеПараметры.Алгоритм) Тогда
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения(Истина, "Алгоритм", ДополнительныеПараметры.Алгоритм));
		
	Иначе	
		ПараметрыОпределенияАлгоритма = Новый Структура("Сертификат", СертификатПодписанта);
		КриптографияЭДКОКлиент.ОпределитьАлгоритм(
			Оповещение,
			МестоХраненияКлюча,
			ПараметрыОпределенияАлгоритма,
			Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigПослеОпределенияАлгоритма(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	НеВключатьИнформациюОСертификате = ДополнительныеПараметры.НеВключатьИнформациюОСертификате;
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	Алгоритм = ?(Результат.Выполнено И ЗначениеЗаполнено(Результат.Алгоритм), Результат.Алгоритм,
		КриптографияЭДКОКлиентСервер.АлгоритмПоУмолчанию());
	ДополнительныеПараметры.Алгоритм = Алгоритм;
	
	ИдентификаторНайденногоАлгоритма = КриптографияЭДКОКлиентСервер.СвойстваАлгоритма(Алгоритм).АлгоритмХеширования;
	ДополнительныеПараметры.ИдентификаторНайденногоАлгоритма = ИдентификаторНайденногоАлгоритма;
	
	Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigПослеЭкспортаСертификатаПолучателяЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ НеВключатьИнформациюОСертификате Тогда
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатПодписанта);
	Иначе
		РезультатВыполнения = РезультатВыполнения(Истина, "СтрокаBase64", "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigПослеЭкспортаСертификатаПолучателяЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Кодировка 							= ДополнительныеПараметры.Кодировка;
	НастройкиПодписи 					= ДополнительныеПараметры.НастройкиПодписи;
	ХэшВсегдаГОСТ2012					= ДополнительныеПараметры.ХэшВсегдаГОСТ2012;
	Алгоритм							= ДополнительныеПараметры.Алгоритм;
	ВключатьХэшСертификата				= ДополнительныеПараметры.ВключатьХэшСертификата;
	ИдентификаторыПодписываемыхДанных 	= ДополнительныеПараметры.ИдентификаторыПодписываемыхДанных;
	
	ДополнительныеПараметры.СертификатПолучателяВBase64 = Результат.СтрокаBase64;
	
	ДанныеФайловДляПодписиВПакете(Результат.СтрокаBase64, 
								ДополнительныеПараметры.ФайлыДанных, 
								НастройкиПодписи, 
								?(ХэшВсегдаГОСТ2012, "GOST R 34.10-2001", Алгоритм),
								Кодировка,
								ИдентификаторыПодписываемыхДанных);
								
	Если ВключатьХэшСертификата Тогда
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклХешЗначенияКлюча", ЭтотОбъект, ДополнительныеПараметры);
	Иначе
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклХешЗначенияСодержимого", ЭтотОбъект, ДополнительныеПараметры);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения(Истина));
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigЦиклХешЗначенияКлюча(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	ИндексФайл = ДополнительныеПараметры.ИндексФайл;
	
	Если ИндексФайл <> 0 Тогда
		ТекущийФайл = ДополнительныеПараметры.ФайлыДанных[ИндексФайл - 1];
		ТекущийФайл.ХешЗначениеКлюча = Результат.Хеш;
	КонецЕсли;
	
	ИндексФайл = ИндексФайл + 1;
	ДополнительныеПараметры.ИндексФайл = ИндексФайл;
	
	Если ИндексФайл <= ДополнительныеПараметры.ВсегоФайлов Тогда
		ТекущийФайл = ДополнительныеПараметры.ФайлыДанных[ИндексФайл - 1];
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклХешЗначенияКлюча", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			КонтекстМоделиХраненияКлюча(ДополнительныеПараметры),
			ТекущийФайл.КанонизированныйКлюч,,
			Ложь,,
			ДополнительныеПараметры.Алгоритм);
	Иначе
		ДополнительныеПараметры.ИндексФайл = 0;
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклХешЗначенияСодержимого", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения(Истина));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigЦиклХешЗначенияСодержимого(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	ИндексФайл = ДополнительныеПараметры.ИндексФайл;
	ВключатьХэшСертификата = ДополнительныеПараметры.ВключатьХэшСертификата;
	ДанныеВнутриПодписи = ДополнительныеПараметры.ДанныеВнутриПодписи;
	
	Если ИндексФайл <> 0 Тогда
		ТекущийФайл = ДополнительныеПараметры.ФайлыДанных[ИндексФайл - 1];
		ТекущийФайл.ХешЗначениеСодержимого = Результат.Хеш;
	КонецЕсли;
	
	ИндексФайл = ИндексФайл + 1;
	ДополнительныеПараметры.ИндексФайл = ИндексФайл;
	
	Если ИндексФайл <= ДополнительныеПараметры.ВсегоФайлов Тогда
		ТекущийФайл = ДополнительныеПараметры.ФайлыДанных[ИндексФайл - 1];
		КанонизированныйОбъектXML = ?(ВключатьХэшСертификата ИЛИ ДанныеВнутриПодписи, 
												ТекущийФайл.КанонизированныйУзелИнформацияОбОбъектеXML, 
												ТекущийФайл.КанонизированныйФайлXML);
		
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклХешЗначенияСодержимого", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.ХешироватьДанные(
			Оповещение,
			КонтекстМоделиХраненияКлюча(ДополнительныеПараметры),
			КанонизированныйОбъектXML,,
			Ложь,,
			ДополнительныеПараметры.Алгоритм);
	Иначе
		ДополнительныеПараметры.ИндексФайл = 0;
		Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклПодпись", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения(Истина));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigЦиклПодпись(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПодписанта = ДополнительныеПараметры.СертификатПодписанта;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИндексФайл = ДополнительныеПараметры.ИндексФайл;
	ВсегоФайлов = ДополнительныеПараметры.ВсегоФайлов;
	Алгоритм = ДополнительныеПараметры.Алгоритм;
	НастройкиПодписи = ДополнительныеПараметры.НастройкиПодписи;
	ПакетнаяПодпись = ДополнительныеПараметры.ПакетнаяПодпись;
	ДобавляемыеАтрибуты = ДополнительныеПараметры.ДобавляемыеАтрибуты;
	ДанныеОблачнойПодписи = ДополнительныеПараметры.ДанныеОблачнойПодписи;
	ВключатьХэшСертификата = ДополнительныеПараметры.ВключатьХэшСертификата;
	
	ПодготовитьДанныеДляПодписиВПакете(ДополнительныеПараметры.ФайлыДанных, ДобавляемыеАтрибуты);
	
	ФайлыДанных = ДополнительныеПараметры.ФайлыДанных;
	
	Если ПакетнаяПодпись И ВключитьПакетнуюПодпись Тогда
		ИмяФайла = СвойствоСтруктуры(НастройкиПодписи, "ИмяФайла", "Данные для подписи");
		ОписаниеСледующее = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклПослеОблачнойПодписи", ЭтотОбъект, ДополнительныеПараметры);

		МассивДокументов = Новый Массив;
		Для Счетчик = 0 По ФайлыДанных.ВГраница() Цикл
			СтрокаМассива = ФайлыДанных[Счетчик];
			НовыйДокументПакетнойПодписи(
				МассивДокументов,
				СтрокаМассива.КанонизированныеДанные,
				ИмяФайла,
				"xml",
				Счетчик);
		КонецЦикла;

		ПараметрыОперации = Новый Структура("ПодтверждатьОперацию", СвойствоСтруктуры(ДанныеОблачнойПодписи, "ПодтверждатьПринудительно", Ложь));
		ПакетнаяПодписьОблачнойПодписи(ОписаниеСледующее, МестоХраненияКлюча, МассивДокументов, СертификатПодписанта, ПараметрыОперации);
		
	Иначе
		Если ИндексФайл <> 0 Тогда
			ТекущийФайл = ФайлыДанных[ИндексФайл - 1];
			ТекущийФайл.Подпись = Результат.ИмяФайлаПодписи;
		КонецЕсли;
		
		ИндексФайл = ИндексФайл + 1;
		ДополнительныеПараметры.ИндексФайл = ИндексФайл;
		
		Если ИндексФайл <= ВсегоФайлов Тогда
			ТекущийФайл = ДополнительныеПараметры.ФайлыДанных[ИндексФайл - 1];
			
			ИдентификаторНайденногоАлгоритма = КриптографияЭДКОКлиентСервер.СвойстваАлгоритма(Алгоритм).АлгоритмХеширования;
			ПараметрыКриптографии = Новый Структура("АлгоритмХеширования", ИдентификаторНайденногоАлгоритма);

			Оповещение = Новый ОписаниеОповещения("ПодписатьФайлыXMLDSigЦиклПодпись", ЭтотОбъект, ДополнительныеПараметры);
			
			ПараметрыОперации = Неопределено;
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
				ИмяФайла = ДанныеОблачнойПодписи.ИмяФайла;
				ИмяФайла = ИмяФайла + " - " + ИндексФайл;
				ПараметрыОперации = ПодготовитьОблачнуюПодпись(ИмяФайла, "xml", ДанныеОблачнойПодписи.ПодтверждатьПринудительно);
			КонецЕсли;
			
			КриптографияЭДКОКлиент.Подписать(Оповещение, СертификатПодписанта, ТекущийФайл.КанонизированныеДанные, ПараметрыКриптографии,
				?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча), Неопределено, ".bin"), Ложь, Неопределено, ПараметрыОперации);
		Иначе
			РезультатыПодписи = ПодготовитьРезультатПодписиВПакете(ФайлыДанных, ВключатьХэшСертификата);
			
			Для Счетчик = 0 По ВсегоФайлов - 1 Цикл
				ФайлыДанных[Счетчик].Результат = РезультатыПодписи[Счетчик];
			КонецЦикла;	
			
			ДополнительныеПараметры.ФайлыДанных = ФайлыДанных;
			
			ПодписатьФайлыXMLDSigЗавершение(РезультатВыполнения(Истина), ДополнительныеПараметры);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigЦиклПослеОблачнойПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВключатьХэшСертификата = ДополнительныеПараметры.ВключатьХэшСертификата;
	ВсегоФайлов = ДополнительныеПараметры.ВсегоФайлов;
	ФайлыДанных = ДополнительныеПараметры.ФайлыДанных;
	
	ВсеПодписи = Результат.Результат;
	
	Для Счетчик = 0 По ВсегоФайлов - 1 Цикл
		ФайлыДанных[Счетчик].Подпись = ВсеПодписи[Счетчик];
	КонецЦикла;	
	
	РезультатыПодписи = ПодготовитьРезультатПодписиВПакете(ФайлыДанных, ВключатьХэшСертификата);
	
	Для Счетчик = 0 По ВсегоФайлов - 1 Цикл
		ФайлыДанных[Счетчик].Результат = РезультатыПодписи[Счетчик];
	КонецЦикла;	
	
	ДополнительныеПараметры.ФайлыДанных = ФайлыДанных;
	
	ПодписатьФайлыXMLDSigЗавершение(РезультатВыполнения(Истина), ДополнительныеПараметры);

КонецПроцедуры

&НаКлиенте
Процедура ПодписатьФайлыXMLDSigЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВсегоФайлов = ДополнительныеПараметры.ВсегоФайлов;
	ФайлыДанных = ДополнительныеПараметры.ФайлыДанных;
	
	Результат = Новый Массив;
	Для Счетчик = 0 По ВсегоФайлов - 1 Цикл
		Результат.Добавить(ФайлыДанных[Счетчик].Результат);
	КонецЦикла;	
	
	ИсключитьПоля = СтрРазделить("АдресДанных,Результат", ",", Ложь);
	// очистить временное хранилище
	Для Каждого СтрокаМассива Из ФайлыДанных Цикл
		Для Каждого СтрокаКлюча Из СтрокаМассива Цикл
			Если ИсключитьПоля.Найти(СтрокаКлюча.Ключ) <> Неопределено Тогда
				Продолжить;	
			ИначеЕсли ЗначениеЗаполнено(СтрокаКлюча.Значение)
				И ЭтоАдресВременногоХранилища(СтрокаКлюча.Значение) Тогда
				УдалитьИзВременногоХранилища(СтрокаКлюча.Значение);
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Функция РезультатВыполнения(Выполнено = Истина, ИмяКлюча = Неопределено, ЗначениеКлюча = "")
	
	Результат = Новый Структура("Выполнено", Выполнено);
	Если ЗначениеЗаполнено(ИмяКлюча) Тогда
		Результат.Вставить(ИмяКлюча, ЗначениеКлюча);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДанныеФайловДляПодписиВПакете(СертификатПолучателяВBase64, ФайлыДанных, НастройкиПодписи, Алгоритм, Кодировка, ИдентификаторыПодписываемыхДанных)
	
	ВсеНастройки = ОбщегоНазначения.СкопироватьРекурсивно(НастройкиПодписи);
	Для Счетчик = 0 По ФайлыДанных.Количество() - 1 Цикл
		СтрокаМассива = ФайлыДанных[Счетчик];
		АлгоритмЦикла = Алгоритм;
		КодировкаЦикла = Кодировка;
		АдресСодержимогоПодготовленногоФайлаXML = Неопределено;
		Если ИдентификаторыПодписываемыхДанных.Количество() > Счетчик Тогда
			ВсеНастройки.Вставить("ИдентификаторПодписываемыхДанных", ИдентификаторыПодписываемыхДанных[Счетчик]);
		Иначе
			ВсеНастройки.Вставить("ИдентификаторПодписываемыхДанных", Неопределено);
		КонецЕсли;	
		ДанныеДляПодписиФайлаXML = АдресДанныхДляПодписиФайлаXMLНаСервере(
												СтрокаМассива.АдресДанных,
												АдресСодержимогоПодготовленногоФайлаXML,
												СертификатПолучателяВBase64,
												КодировкаЦикла,
												ВсеНастройки,
												АлгоритмЦикла);
		СтрокаМассива.КанонизированныйУзелИнформацияОбОбъектеXML = СвойствоСтруктуры(ДанныеДляПодписиФайлаXML, "КанонизированныйУзелИнформацияОбОбъектеXMLВBase64");
		СтрокаМассива.КанонизированныйУзелПодпись = СвойствоСтруктуры(ДанныеДляПодписиФайлаXML, "КанонизированныйУзелПодписьВBase64");
		СтрокаМассива.КанонизированныйФайлXML = СвойствоСтруктуры(ДанныеДляПодписиФайлаXML, "КанонизированныйФайлXMLВBase64");
		СтрокаМассива.СодержимогоПодготовленногоФайлаXML = АдресСодержимогоПодготовленногоФайлаXML;
		СтрокаМассива.КанонизированныйКлюч = СвойствоСтруктуры(ДанныеДляПодписиФайлаXML, "КанонизированныйУзелИнформацияОКлючахXMLВBase64");
	КонецЦикла;
											
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПодготовитьРезультатПодписиВПакете(ФайлыДанных, ВключатьХэшСертификата)
	
	Результат = НовыЙ Массив;
	
	Для Каждого СтрокаМассива Из ФайлыДанных Цикл
		СодержимоеПодписанногоФайлаXMLВBase64 = ПодписанныйФайлXMLВBase64НаСервере(
			СтрокаМассива.СодержимогоПодготовленногоФайлаXML,
			СтрокаМассива.ХешЗначениеКлюча,
			СтрокаМассива.ХешЗначениеСодержимого,
			?(ТипЗнч(СтрокаМассива.Подпись) = Тип("ДвоичныеДанные"), ПоместитьВоВременноеХранилище(СтрокаМассива.Подпись), СтрокаМассива.Подпись),
			СтрокаМассива.ВозвращатьАдресВоВременномХранилище,
			ВключатьХэшСертификата);
		Результат.Добавить(СодержимоеПодписанногоФайлаXMLВBase64);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПодготовитьДанныеДляПодписиВПакете(ФайлыДанных, ДобавляемыеАтрибуты)
	
	Результат = НовыЙ Массив;
	
	Для Каждого СтрокаМассива Из ФайлыДанных Цикл
		СтрокаНаПодпись = КанонизированныйУзелПодписьВBase64СПодставленнымХешНаСервере(
			СтрокаМассива.КанонизированныйУзелПодпись,
			СтрокаМассива.ХешЗначениеКлюча,
			СтрокаМассива.ХешЗначениеСодержимого,
			Истина,
			ДобавляемыеАтрибуты);
			
		СтрокаМассива.КанонизированныеДанные = СтрокаНаПодпись;
	КонецЦикла;

	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ПодписьSOAP

&НаКлиенте
Функция ПараметрыОбменаСФСС(Организация) Экспорт
	
	ИспользуетсяОбменСФСС 	= Ложь;
	ВключенаАвтонастройка 	= Ложь;
	ВУниверсальномФормате	= Ложь;
	УчетнаяЗапись       	= Неопределено;
	
	ПолучитьСвойстваОрганизацииДляФССНаСервере(
		Организация,
		ВУниверсальномФормате,
		ИспользуетсяОбменСФСС,
		ВключенаАвтонастройка,
		УчетнаяЗапись);
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация", Организация);
	ДополнительныеПараметры.Вставить("ИспользуетсяОбменСФСС", ИспользуетсяОбменСФСС);
	ДополнительныеПараметры.Вставить("ВключенаАвтонастройка", ВключенаАвтонастройка);
	ДополнительныеПараметры.Вставить("ВУниверсальномФормате", ВУниверсальномФормате);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	
	Возврат ДополнительныеПараметры;
		
КонецФункции

&НаКлиенте
Процедура ПоказатьОшибкуЕслиОрганизацияНеИспользуетОбменСФСС(Организация, ВыполняемоеОповещение) Экспорт
	
	ПараметрыОбменаСФСС = ПараметрыОбменаСФСС(Организация);
	
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВыполняемоеОповещение) Тогда
		ТекстСообщения = ДлительнаяОтправкаКлиентСервер.Сообщение1СОтчетностьНеПодключено();
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
	Иначе
		Если НЕ ПараметрыОбменаСФСС.ВУниверсальномФормате Тогда
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение(ПараметрыОбменаСФСС.Организация);
		Иначе
			ПоказатьПредупреждение(, НСтр("ru = 'По организации отчета не включена возможность электронного документооборота с ФСС!'"));
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФСС(
		ВыполняемоеОповещение,
		ОрганизацияСсылка,
		РегистрационныйНомерФСС,
		ВыгрузкаЭЛН,
		ЗашифроватьSOAP = Истина,
		НастройкиОперации = Неопределено) Экспорт
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("ПроверкиИАвтонастройкаВыполнены", 	Ложь);
	НастройкиВызова.Вставить("СертификатСтрахователя", 				Неопределено);
	НастройкиВызова.Вставить("СертификатФСС", 						Неопределено);
	НастройкиВызова.Вставить("МЧД", 								"");
	НастройкиВызова.Вставить("ПараметрыФорматаЭЛН", 				Неопределено);
	Если НастройкиОперации <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, НастройкиОперации);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(НастройкиОперации);
	Иначе
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Ложь);
	КонецЕсли;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НастройкиВызова);
	
	Если НЕ НастройкиВызова.ПроверкиИАвтонастройкаВыполнены Тогда
		ОрганизацияИспользуетОбменСФСС 						= Ложь;
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи 	= Ложь;
		НастроенОбменВУниверсальномФормате 					= Ложь;
		УчетнаяЗаписьДокументооборота 						= Неопределено;
		
		ПараметрыФорматов = ?(НастройкиВызова.ПараметрыФорматаЭЛН = Неопределено,
			Новый Структура("ПараметрыФорматаЭЛН", Неопределено), Неопределено);
		ПолучитьСвойстваОрганизацииДляФССНаСервере(
			ОрганизацияСсылка,
			НастроенОбменВУниверсальномФормате,
			ОрганизацияИспользуетОбменСФСС,
			ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,
			УчетнаяЗаписьДокументооборота,
			ПараметрыФорматов);
		НастройкиВызова.ПараметрыФорматаЭЛН = ?(НастройкиВызова.ПараметрыФорматаЭЛН = Неопределено,
			ПараметрыФорматов.ПараметрыФорматаЭЛН, НастройкиВызова.ПараметрыФорматаЭЛН);
		
		Если НЕ ОрганизацияИспользуетОбменСФСС Тогда
			ПоказатьОшибкуЕслиОрганизацияНеИспользуетОбменСФСС(ОрганизацияСсылка, ВыполняемоеОповещение);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ВыполнятьАвтонастройку = 
		НЕ НастройкиВызова.ПроверкиИАвтонастройкаВыполнены 
		И НастроенОбменВУниверсальномФормате
		И ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи;
		
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 		ОрганизацияСсылка);
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ВыгрузкаЭЛН", 			ВыгрузкаЭЛН);
	ДополнительныеПараметры.Вставить("ЗашифроватьSOAP", 		ЗашифроватьSOAP);
	ДополнительныеПараметры.Вставить("НастройкиВызова", 		НастройкиВызова);
	ДополнительныеПараметры.Вставить("ВыполнятьАвтонастройку", 	ВыполнятьАвтонастройку);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если ВыполнятьАвтонастройку Тогда
		
		АвтоматическаяНастройкаУчетнойЗаписи(
			УчетнаяЗаписьДокументооборота,,
			Оповещение,,,
			Истина);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПроверкиЛицензииИСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	Если ВходящийКонтекст.ВыполнятьАвтонастройку Тогда
		
		Организация = ВходящийКонтекст.ОрганизацияСсылка;
		
		ПроверитьСертификатИЛицензию(
			ОписаниеОповещения, 
			Неопределено, 
			Организация, 
			"ФСС");
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПроверкиЛицензииИСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОрганизацияСсылка 		= ДополнительныеПараметры.ОрганизацияСсылка;
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН 			= ДополнительныеПараметры.ВыгрузкаЭЛН;
	ЗашифроватьSOAP 		= ДополнительныеПараметры.ЗашифроватьSOAP;
	НастройкиВызова 		= ДополнительныеПараметры.НастройкиВызова;
	
	ПараметрыФорматаЭЛН = ?(НастройкиВызова.ПараметрыФорматаЭЛН = Неопределено,
		ПараметрыФорматаЭЛННаСервере(), НастройкиВызова.ПараметрыФорматаЭЛН);
	ДополнительныеПараметры.НастройкиВызова.ПараметрыФорматаЭЛН = ПараметрыФорматаЭЛН;
	ЭтоВерсия1X = Лев(ПараметрыФорматаЭЛН.Версия, 2) = "1.";
	
	ПространствоИменЗапроса = ?(ЭтоВерсия1X, "http://ru/ibs/fss/ln/ws/FileOperationsLn.wsdl",
		"http://www.fss.ru/integration/types/eln/ins/v01");
	ПространствоИменАтрибутов = ?(ЭтоВерсия1X, Неопределено, "http://www.fss.ru/integration/types/eln/v01");
	ДобавляемыеАтрибутыЗаписи = "xmlns:ds=""http://www.w3.org/2000/09/xmldsig#"""
		+ " xmlns:eln=""" + ПространствоИменЗапроса + """"
		+ " xmlns:sch=""http://gost34.ibs.ru/WrapperService/Schema"""
		+ " xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"""
		+ ?(ЗначениеЗаполнено(ПространствоИменАтрибутов), " xmlns:v011=""http://www.fss.ru/integration/types/eln/v01""", "")
		+ " xmlns:wsse=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-secext-1.0.xsd"""
		+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""";
	ДобавляемыеАтрибутыПодписи = "xmlns=""http://www.w3.org/2000/09/xmldsig#""";
	
	СкорректированнаяВыгрузкаЭЛН = СкорректироватьВыгрузкуЗапросаОперацииДляSOAP(
		ВыгрузкаЭЛН,
		ПространствоИменЗапроса,
		ПространствоИменАтрибутов);
	
	СкорректированнаяВыгрузкаЗапросаЭЛН = Новый Структура;
	СкорректированнаяВыгрузкаЗапросаЭЛН.Вставить("РегистрационныйНомерФСС", РегистрационныйНомерФСС);
	СкорректированнаяВыгрузкаЗапросаЭЛН.Вставить("ВыгрузкаЭЛН", 			СкорректированнаяВыгрузкаЭЛН);
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	РезультатИнициализации = АдресПодписанногоSOAPЗагрузкиЭЛНВФССИнициализацияНаСервере(
		ОрганизацияСсылка,
		СкорректированнаяВыгрузкаЗапросаЭЛН,
		ДобавляемыеАтрибутыЗаписи,
		ЗашифроватьSOAP,
		КонтекстМоделиХраненияКлюча(НастройкиВызова),
		НЕ СертификатыИзвестны);
	
	Если РезультатИнициализации = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.РегистрационныйНомерФСС = РезультатИнициализации.РегистрационныйНомерФСС;
	
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗаписи", 		ДобавляемыеАтрибутыЗаписи);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 		ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("СкорректированнаяВыгрузкаЭЛН", 	СкорректированнаяВыгрузкаЭЛН);
	ДополнительныеПараметры.Вставить("РезультатИнициализации", 			РезультатИнициализации);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПредупрежденияПриТестовомРежиме",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ НастройкиВызова.ПроверкиИАвтонастройкаВыполнены И НЕ СертификатыИзвестны
		И РезультатИнициализации.ТестовыйСерверФССЭЛН Тогда
		
		ТекстВопроса = НСтр("ru = 'Внимание! Включен режим тестирования ЭЛН!
			|
			|В режиме тестирования отчетность отправляется через тестовый шлюз СФР (бывш. ФСС).
			|Сдача отчетности засчитана не будет.
			|
			|Продолжить отправку?'");
		ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, КодВозвратаДиалога.Да);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПредупрежденияПриТестовомРежиме(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 			= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОрганизацияСсылка 				= ДополнительныеПараметры.ОрганизацияСсылка;
	РегистрационныйНомерФСС 		= ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН 					= ДополнительныеПараметры.ВыгрузкаЭЛН;
	ЗашифроватьSOAP 				= ДополнительныеПараметры.ЗашифроватьSOAP;
	НастройкиВызова 				= ДополнительныеПараметры.НастройкиВызова;
	ДобавляемыеАтрибутыЗаписи 		= ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи 		= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	СкорректированнаяВыгрузкаЭЛН 	= ДополнительныеПараметры.СкорректированнаяВыгрузкаЭЛН;
	РезультатИнициализации 			= ДополнительныеПараметры.РезультатИнициализации;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	НомерЗаписиЭЛН = 1;
	МассивЗаписейЭЛН64 = РезультатИнициализации.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности = РезультатИнициализации.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 = Новый Массив;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РезультатИнициализации);
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	Если СертификатыИзвестны Тогда
		СертификатСтрахователя 		= НастройкиВызова.СертификатСтрахователя;
		СертификатФССЭЛНОтпечаток 	= Неопределено;
		СертификатФССЭЛН 			= НастройкиВызова.СертификатФСС;
		МЧД 						= НастройкиВызова.МЧД;
		
	Иначе
		СертификатСтрахователяОтпечаток = РезультатИнициализации.СертификатСтрахователяОтпечаток;
		СертификатСтрахователя 			= РезультатИнициализации.СертификатСтрахователя;
		СертификатФССЭЛНОтпечаток 		= РезультатИнициализации.СертификатФССЭЛНОтпечаток;
		СертификатФССЭЛН 				= РезультатИнициализации.СертификатФССЭЛН;
		МЧД 							= РезультатИнициализации.МЧД;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", 				РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ВыгрузкаЭЛН", 							СкорректированнаяВыгрузкаЭЛН);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛНОтпечаток", 				СертификатФССЭЛНОтпечаток);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛН", 						СертификатФССЭЛН);
	ДополнительныеПараметры.Вставить("МЧД", 									МЧД);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 					ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ЗашифроватьSOAP", 						ЗашифроватьSOAP);
	ДополнительныеПараметры.Вставить("НастройкиВызова", 						НастройкиВызова);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗаписи", 				ДобавляемыеАтрибутыЗаписи);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 				ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("НомерЗаписиЭЛН", 							НомерЗаписиЭЛН);
	ДополнительныеПараметры.Вставить("МассивЗаписейЭЛН64", 						МассивЗаписейЭЛН64);
	ДополнительныеПараметры.Вставить("МассивНомеровЛистковНетрудоспособности", 	МассивНомеровЛистковНетрудоспособности);
	ДополнительныеПараметры.Вставить("МассивПодписейЭЛН64", 					МассивПодписейЭЛН64);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 						ОрганизацияСсылка);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПоискаСертификатаСтрахователя",
		ЭтотОбъект, ДополнительныеПараметры);
	Если СертификатыИзвестны ИЛИ НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, Истина, СертификатСтрахователя);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатСтрахователяОтпечаток, "MY");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция СкорректироватьВыгрузкуЗапросаОперацииДляSOAP(
		ВыгрузкаЗапросаОперации,
		ПространствоИменЗапроса,
		ПространствоИменАтрибутов = Неопределено)
	
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапросаОперации,
		" xmlns=""" + ПространствоИменЗапроса + """", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса,
		" xmlns:xs=""http://www.w3.org/2001/XMLSchema""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса,
		" xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""", "");
	
	ПространствоИменЗамены = ПространствоИменЗапроса;
	Для НомерПространстваИмен = 1 По 2 Цикл
		Пока Истина Цикл
			ПозицияПространстваИмен = СтрНайти(СкорректированнаяВыгрузкаЗапроса, "=""" + ПространствоИменЗамены + """");
			Если ПозицияПространстваИмен = 0 Тогда
				Прервать;
			КонецЕсли;
			ПозицияПрефикса = ПозицияПространстваИмен - 1;
			Пока ПозицияПрефикса > 1 Цикл
				Если Сред(СкорректированнаяВыгрузкаЗапроса, ПозицияПрефикса - 1, 1) = ":" Тогда
					ПрефиксПространстваИмен = Сред(СкорректированнаяВыгрузкаЗапроса,
						ПозицияПрефикса, ПозицияПространстваИмен - ПозицияПрефикса);
					СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса,
						" xmlns:" + ПрефиксПространстваИмен + "=""" + ПространствоИменЗамены + """", "");
					СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса,
						" " + ПрефиксПространстваИмен + ":", " ");
					СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса,
						"<" + ПрефиксПространстваИмен + ":", "<");
					СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса,
						"</" + ПрефиксПространстваИмен + ":", "</");
					Прервать;
				КонецЕсли;
				ПозицияПрефикса = ПозицияПрефикса - 1;
			КонецЦикла;
		КонецЦикла;
		
		Если ПространствоИменАтрибутов = Неопределено Тогда
			Прервать;
		КонецЕсли;
		ПространствоИменЗамены = ПространствоИменАтрибутов;
	КонецЦикла;
	
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xsi:type=""xs:string""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xsi:type=""xs:dateTime""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xsi:type=""xs:boolean""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xsi:type=""xs:double""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xsi:type=""xs:int""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xsi:nil=""true""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, "/>", " xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance"" xsi:nil=""true""/>");
	
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, Символы.Таб, "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, Символы.ВК, "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, Символы.ПС, "");
	
	Возврат СкорректированнаяВыгрузкаЗапроса;
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресПодписанногоSOAPЗагрузкиЭЛНВФССИнициализацияНаСервере(
		Знач ОрганизацияСсылка,
		Знач ВыгрузкаЗапросаЭЛН,
		Знач ДобавляемыеАтрибутыЗаписи,
		Знач ПолучитьСертификатФССЭЛН = Ложь,
		Знач МестоХраненияКлюча = Неопределено,
		Знач ВозвращатьСертификаты = Истина)
	
	СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
		ОрганизацияСсылка,
		НЕ ЗначениеЗаполнено(ВыгрузкаЗапросаЭЛН.РегистрационныйНомерФСС),
		Ложь);
	
	Результат = Новый Структура;
	Результат.Вставить("РегистрационныйНомерФСС", Неопределено);
	
	Результат.РегистрационныйНомерФСС = ?(СвойстваОрганизации.Свойство("РегистрационныйНомерФСС"),
		СвойстваОрганизации.РегистрационныйНомерФСС, ВыгрузкаЗапросаЭЛН.РегистрационныйНомерФСС);
	
	МассивНомеровЛистковНетрудоспособности = Неопределено;
	МассивЗаписейЭЛН64 = ЗаписиЭЛНДляПодписиSOAP64НаСервере(
		ВыгрузкаЗапросаЭЛН.ВыгрузкаЭЛН,
		ДобавляемыеАтрибутыЗаписи,
		МассивНомеровЛистковНетрудоспособности);
	Если МассивЗаписейЭЛН64 = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'ЭЛН не заполнен'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		Возврат Неопределено;
	КонецЕсли;
	
	Если МассивЗаписейЭЛН64.Количество() > 30 Тогда
		ТекстОшибки = НСтр("ru = 'Допустимо не более 30 листков нетрудоспособности, разделите реестр ЭЛН на несколько'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		Возврат Неопределено;
	КонецЕсли;
	
	Если МестоХраненияКлюча = Неопределено ИЛИ ВозвращатьСертификаты Тогда
		РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
		СвойстваОрганизации = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОрганизацияСсылка, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
		Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
			И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена) Тогда
			МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
		ИначеЕсли МестоХраненияКлюча = Неопределено Тогда
			МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Ложь);
		КонецЕсли;
	КонецЕсли;	
	
	Если ВозвращатьСертификаты Тогда
		ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	КонецЕсли;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Для ИндексЗаписиЭЛН = 0 По МассивЗаписейЭЛН64.Количество() - 1 Цикл
			ДвоичныеДанныеЗаписиЭЛН = Base64Значение(МассивЗаписейЭЛН64[ИндексЗаписиЭЛН]);
			МассивЗаписейЭЛН64[ИндексЗаписиЭЛН] = ПоместитьВоВременноеХранилище(ДвоичныеДанныеЗаписиЭЛН, Новый УникальныйИдентификатор);
		КонецЦикла;
	КонецЕсли;
	
	Если ВозвращатьСертификаты Тогда
		ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка);
		Если ОтпечаткиСертификатов = Неопределено ИЛИ НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток) Тогда
			ТекстОшибки = НСтр("ru = 'Не выбран сертификат страхователя в настройках обмена с ФСС для 1С-Отчетности'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			Возврат Неопределено;
		КонецЕсли;
		Если ПолучитьСертификатФССЭЛН И НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток) Тогда
			ТекстОшибки = НСтр("ru = 'Не выбран сертификат ФСС ЭЛН в настройках обмена с ФСС для 1С-Отчетности'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			Возврат Неопределено;
		КонецЕсли;
		
		СертификатСтрахователя = Неопределено;
		СертификатФССЭЛН = Неопределено;
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
			Иначе
				СертификатСтрахователя = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY");
			КонецЕсли;
			Если СертификатСтрахователя = Неопределено Тогда
				ТекстОшибки = НСтр("ru = 'Не найден сертификат страхователя'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
				Возврат Неопределено;
			КонецЕсли;
			
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
			СертификатСтрахователя.Вставить("КлючевойКонтейнер", ИдентификаторДокументооборота);
			
			Если ПолучитьСертификатФССЭЛН Тогда
				Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
					СертификатФССЭЛН = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
				Иначе
					СертификатФССЭЛН = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
				КонецЕсли;
				Если СертификатФССЭЛН = Неопределено Тогда
					ТекстОшибки = НСтр("ru = 'Не найден сертификат ФСС ЭЛН'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
					Возврат Неопределено;
				КонецЕсли;
				ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФССЭЛН);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Результат.Вставить("МассивЗаписейЭЛН64", 						МассивЗаписейЭЛН64);
	Результат.Вставить("МассивНомеровЛистковНетрудоспособности", 	МассивНомеровЛистковНетрудоспособности);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Если ВозвращатьСертификаты Тогда
		Результат.Вставить("СертификатСтрахователяОтпечаток", 	ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
		Результат.Вставить("СертификатСтрахователя", 			СертификатСтрахователя);
		Результат.Вставить("СертификатФССЭЛНОтпечаток", 		ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
		Результат.Вставить("СертификатФССЭЛН", 					СертификатФССЭЛН);
		Результат.Вставить("ТестовыйСерверФССЭЛН", 				ОтпечаткиСертификатов.ТестовыйСерверФССЭЛН);
		Результат.Вставить("МЧД", 								ОтпечаткиСертификатов.МЧД);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПоискаСертификатаСтрахователя(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС 				= ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН 							= ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатФССЭЛНОтпечаток 				= ДополнительныеПараметры.СертификатФССЭЛНОтпечаток;
	СертификатФССЭЛН 						= ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение 					= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP 						= ДополнительныеПараметры.ЗашифроватьSOAP;
	НастройкиВызова 						= ДополнительныеПараметры.НастройкиВызова;
	МестоХраненияКлюча 						= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи 				= ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи 				= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН 							= ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 						= ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности 	= ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 					= ДополнительныеПараметры.МассивПодписейЭЛН64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	СертификатСтрахователя = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", СертификатСтрахователя);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПоискаСертификатаФСС",
		ЭтотОбъект, ДополнительныеПараметры);
	Если СертификатыИзвестны ИЛИ НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, ЗашифроватьSOAP, СертификатФССЭЛН);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатФССЭЛНОтпечаток, "AddressBook");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПоискаСертификатаФСС(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС 				= ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН 							= ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя 					= ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение 					= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP 						= ДополнительныеПараметры.ЗашифроватьSOAP;
	НастройкиВызова 						= ДополнительныеПараметры.НастройкиВызова;
	ДобавляемыеАтрибутыЗаписи 				= ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи 				= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН 							= ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 						= ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности 	= ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 					= ДополнительныеПараметры.МассивПодписейЭЛН64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	СертификатФССЭЛН = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатСтрахователя, НСтр("ru = 'Сертификат страхователя'"));
	Если ЗашифроватьSOAP Тогда
		СертификатыОбмена.Добавить(СертификатФССЭЛН, НСтр("ru = 'Сертификат ФСС ЭЛН'"));
	КонецЕсли;
	
	Если НЕ СертификатыИзвестны И НЕ СертификатыГодятсяДляПодписанияОтчетностиФСС(СертификатыОбмена) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("СертификатФССЭЛН", СертификатФССЭЛН);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЭкспортаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатСтрахователя);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЭкспортаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН = ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности = ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 = ДополнительныеПараметры.МассивПодписейЭЛН64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПолучателя64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("СертификатПолучателя64", СертификатПолучателя64);
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		АдресПодписанногоSOAPЗагрузкиЭЛНВФССОблачнаяПодпись(ДополнительныеПараметры);
	Иначе
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССВЦикле",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССВЦикле(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХроаненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН = ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности = ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 = ДополнительныеПараметры.МассивПодписейЭЛН64;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	
	Если НомерЗаписиЭЛН <= МассивЗаписейЭЛН64.Количество() Тогда
		ЗаписьЭЛН64 = МассивЗаписейЭЛН64[НомерЗаписиЭЛН - 1];
		
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЗаписиФайла",
			ЭтотОбъект, ДополнительныеПараметры);
			
		Если НЕ ЭтоЛокальнаяПодпись(МестоХроаненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ЗаписьЭЛН64);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ЗаписьЭЛН64);
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыЗаписи", 	ДобавляемыеАтрибутыЗаписи);
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыПодписи", 	ДобавляемыеАтрибутыПодписи);
	НастройкиОперации.Вставить("ВозвращатьТекстВBase64", 		ЗашифроватьSOAP);
	НастройкиОперации.Вставить("МЧД", 							ДополнительныеПараметры.МЧД);
	РезультатОперации = АдресSOAPЗагрузкиЭЛНВФССПоМассивуПодписейНаСервере(
		РегистрационныйНомерФСС,
		ВыгрузкаЭЛН,
		МассивПодписейЭЛН64,
		МассивНомеровЛистковНетрудоспособности,
		СертификатПолучателя64,
		НастройкиОперации);
	
	Если НЕ ЗашифроватьSOAP Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		
	Иначе
		ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", РезультатОперации);
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЗаписиФайлаПодписанногоSOAP",
			ЭтотОбъект, ДополнительныеПараметры);
			
		Если НЕ ЭтоЛокальнаяПодпись(МестоХроаненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, РезультатОперации);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, РезультатОперации);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЗаписиФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН = ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности = ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 = ДополнительныеПараметры.МассивПодписейЭЛН64;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	ОрганизацияСсылка = ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлЗаписиЭЛН = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлЗаписиЭЛН", ФайлЗаписиЭЛН);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПодписания",
		ЭтотОбъект, ДополнительныеПараметры);
	
	ДобавляемыеАтрибутыПодписиИЗаписи = ДобавляемыеАтрибутыПодписи
		+ ?(ДобавляемыеАтрибутыПодписи <> "" И ДобавляемыеАтрибутыЗаписи <> "", " ", "")
		+ ДобавляемыеАтрибутыЗаписи;
	НомерЗаписиЭЛНСтрокой = "ELN_" + МассивНомеровЛистковНетрудоспособности[НомерЗаписиЭЛН - 1];
	
	ДополнительныеТрансформации = Новый Массив;
	ДополнительныеОбратныеТрансформации = Новый Массив;
	
	// прямые и обратные замены
	ДополнительныеТрансформации.Добавить("<eln:");
	ДополнительныеТрансформации.Добавить("<");
	ДополнительныеОбратныеТрансформации.Добавить("<eln:/");
	ДополнительныеОбратныеТрансформации.Добавить("</eln:");
	
	ДополнительныеТрансформации.Добавить("</eln:");
	ДополнительныеТрансформации.Добавить("</");
	ДополнительныеОбратныеТрансформации.Добавить("<");
	ДополнительныеОбратныеТрансформации.Добавить("<eln:");
	
	НастройкиПодписи = Новый Структура;
	НастройкиПодписи.Вставить("Трансформация1_1", 						Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 				Ложь);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 				Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 						Неопределено);
	НастройкиПодписи.Вставить("НормализацияОтключена", 					Истина);
	НастройкиПодписи.Вставить("ДанныеВнутриПодписи", 					Истина);
	НастройкиПодписи.Вставить("ИдентификаторПодписываемыхДанных", 		НомерЗаписиЭЛНСтрокой);
	НастройкиПодписи.Вставить("ДобавляемыеАтрибуты", 					ДобавляемыеАтрибутыПодписиИЗаписи);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОСертификате", 		Истина);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОбОбъекте", 			Истина);
	НастройкиПодписи.Вставить("ДополнительныеТрансформации", 			ДополнительныеТрансформации);
	НастройкиПодписи.Вставить("ДополнительныеОбратныеТрансформации", 	ДополнительныеОбратныеТрансформации);
	НастройкиПодписи.Вставить("Применение", 							"ОтправкиФСС");
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ЗагрузкаЭЛН", ОрганизацияСсылка); 
		ПодготовитьОблачнуюПодпись("Реестр ЭЛН", ".xml", ТребуетПодтверждениеОперации, НастройкиПодписи);
	КонецЕсли;
	
	ПодписатьФайлXMLDSig(
		СертификатСтрахователя,
		ФайлЗаписиЭЛН,
		Оповещение,,,
		НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеПодписания(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН = ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивПодписейЭЛН64 = ДополнительныеПараметры.МассивПодписейЭЛН64;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	ФайлЗаписиЭЛН = ДополнительныеПараметры.ФайлЗаписиЭЛН;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьИзВременногоХранилища(ФайлЗаписиЭЛН);
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлЗаписиЭЛН);
	КонецЕсли;
	
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Не удалось подписать файл выгрузки ЭЛН!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлПодписиЭЛН", ПолноеИмяПодписанногоФайла);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЧтенияФайла",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЧтенияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН = ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивПодписейЭЛН64 = ДополнительныеПараметры.МассивПодписейЭЛН64;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	ФайлПодписиЭЛН = ДополнительныеПараметры.ФайлПодписиЭЛН;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлПодписиЭЛН);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	МассивПодписейЭЛН64.Добавить(Результат.СтрокаBase64);
	
	НомерЗаписиЭЛН = НомерЗаписиЭЛН + 1;
	
	ДополнительныеПараметры.Вставить("НомерЗаписиЭЛН", НомерЗаписиЭЛН);
	ДополнительныеПараметры.Вставить("МассивПодписейЭЛН64", МассивПодписейЭЛН64);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССВЦикле",
		ЭтотОбъект, ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЗаписиФайлаПодписанногоSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС 			= ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН 						= ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя 				= ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН 					= ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ДобавляемыеАтрибутыЗаписи 			= ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи 			= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН 						= ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 					= ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивПодписейЭЛН64 				= ДополнительныеПараметры.МассивПодписейЭЛН64;
	СертификатПолучателя64 				= ДополнительныеПараметры.СертификатПолучателя64;
	ПодписанныйSOAP64 					= ДополнительныеПараметры.ПодписанныйSOAP64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(ВыполняемоеОповещение.ДополнительныеПараметры) = Тип("Структура") Тогда
		ВыполняемоеОповещение.ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", ПодписанныйSOAP64);
	КонецЕсли;
	
	ФайлПодписанногоSOAP = Результат.ИмяФайла;
	
	ЭтоВерсия1X = ?(ДополнительныеПараметры.НастройкиВызова.ПараметрыФорматаЭЛН = Неопределено, Истина,
		Лев(ДополнительныеПараметры.НастройкиВызова.ПараметрыФорматаЭЛН.Версия, 2) = "1.");
	
	ШаблонЗапросаSOAP =
		"<SOAP-ENV:Envelope xmlns:SOAP-ENV=""http://schemas.xmlsoap.org/soap/envelope/"">
		|	<SOAP-ENV:Header/>
		|	<SOAP-ENV:Body>
		|%1
		|	</SOAP-ENV:Body>
		|</SOAP-ENV:Envelope>";
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ШаблонЗапросаSOAP", ШаблонЗапросаSOAP);
	НастройкиОперации.Вставить("Применение", 		"ОтправкиФСС");
	Если НЕ ЭтоВерсия1X Тогда
		НастройкиОперации.Вставить("ПоместитьВУзелX509CertificateСертификатПолучателя", Истина);
	КонецЕсли;
	НастройкиОперации.Вставить("СценарийФСС", Истина);
	
	ЗашифроватьФайлXML(
		СертификатФССЭЛН,
		СертификатСтрахователя,
		ФайлПодписанногоSOAP,
		ВыполняемоеОповещение,,
		Истина,
		НастройкиОперации);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗаписиЭЛНДляПодписиSOAP64НаСервере(
		Знач ВыгрузкаЭЛН,
		Знач ДобавляемыеАтрибутыЗаписи,
		МассивНомеровЛистковНетрудоспособности = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ЗаписиЭЛНДляПодписиSOAP64(
		ВыгрузкаЭЛН,
		ДобавляемыеАтрибутыЗаписи,
		МассивНомеровЛистковНетрудоспособности);
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресSOAPЗагрузкиЭЛНВФССПоМассивуПодписейНаСервере(
		Знач РегистрационныйНомерФСС,
		Знач ВыгрузкаЭЛН,
		Знач МассивПодписейЭЛН64,
		Знач МассивНомеровЛистковНетрудоспособности,
		Знач СертификатПолучателя64,
		Знач НастройкиОперации = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.АдресSOAPЗагрузкиЭЛНВФССПоМассивуПодписей(
		РегистрационныйНомерФСС,
		ВыгрузкаЭЛН,
		МассивПодписейЭЛН64,
		МассивНомеровЛистковНетрудоспособности,
		СертификатПолучателя64,
		НастройкиОперации);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПараметрыФорматаЭЛННаСервере() Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПараметрыФорматаЭЛН();
	
КонецФункции

#Область ПодписьSOAPСЭДОФСС

&НаКлиенте
Функция СкорректироватьВыгрузкуЗапросаОперацииДляSOAPСЭДОФСС(ВыгрузкаЗапросаОперации, ПространствоИменЗапроса)
	
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапросаОперации, " xmlns=""" + ПространствоИменЗапроса + """", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xmlns:xs=""http://www.w3.org/2001/XMLSchema""", "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, " xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""", "");
	
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, Символы.Таб, "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, Символы.ВК, "");
	СкорректированнаяВыгрузкаЗапроса = СтрЗаменить(СкорректированнаяВыгрузкаЗапроса, Символы.ПС, "");
	
	Возврат СкорректированнаяВыгрузкаЗапроса;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗапросОперацииСЭДОФССДляПодписиSOAP64(
		Знач ВыгрузкаЗапросаОперацииСЭДО) Экспорт
		
	ДвоичныеДанныеЗапроса = ДвоичныеДанныеИзТекстаНаСервере(ВыгрузкаЗапросаОперацииСЭДО, "utf-8");
	ТекстXMLЗапроса64 = Base64Строка(ДвоичныеДанныеЗапроса);
	
	Возврат ТекстXMLЗапроса64;
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресSOAPЗапросОперацииСЭДОФССПоПодписиНаСервере(
		Знач РегистрационныйНомерФСС,
		Знач ВыгрузкаЗапроса,
		Знач ПодписьЗапросаЭЛН64,
		Знач СертификатПолучателя64,
		Знач НастройкиОперации = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.АдресSOAPЗапросОперацииСЭДОФССПоПодписи(
		РегистрационныйНомерФСС,
		ВыгрузкаЗапроса,
		ПодписьЗапросаЭЛН64,
		СертификатПолучателя64,
		НастройкиОперации);
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресПодписанногоSOAPОперацииСЭДОФССИнициализацияНаСервере(
		Знач ОрганизацияСсылка,
		Знач ВыгрузкаЗапросаОперацииСЭЛН,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач РегистрационныйНомерФСС,
		Знач ПолучитьСертификатФССЭЛН = Ложь,
		Знач СвойстваОбменаПоСЭДОЧерезОператора = Неопределено,
		Знач Настройки = Неопределено)
	
	ЗапросОперацииСЭДО64 = ЗапросОперацииСЭДОФССДляПодписиSOAP64(
		ВыгрузкаЗапросаОперацииСЭЛН);
	
	Если ЗапросОперацииСЭДО64 = Неопределено Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Данные операции СЭДО не заполнены'"));
		Возврат Неопределено;
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МЧДОтпечатокРуководителя = ДокументооборотСКОВызовСервера.ЗапоминаниеОшибок(,, Истина, "МЧДОтпечатокРуководителя");
	Если ЗначениеЗаполнено(МЧДОтпечатокРуководителя) Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Ложь);
	Иначе
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	КонецЕсли;
	УчетнаяЗапись = РеквизитыУчетнойЗаписи.Ссылка;
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ДвоичныеДанныеЗапросаОперацииСЭЛН = Base64Значение(ЗапросОперацииСЭДО64);
		ЗапросОперацииСЭДО64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеЗапросаОперацииСЭЛН, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка);
	Если ОтпечаткиСертификатов = Неопределено
		ИЛИ (НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток)
			И (Настройки = Неопределено ИЛИ НЕ Настройки.Свойство("СодержимоеПодписанногоЗапросаSOAP"))) Тогда
		
		ТекстСообщения = НСтр("ru = 'Не выбран сертификат страхователя в настройках обмена с ФСС для 1С-Отчетности'");
		ТекстСообщения = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(ТекстСообщения, Истина);
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ПолучитьСертификатФССЭЛН Тогда
		Если СвойстваОбменаПоСЭДОЧерезОператора <> Неопределено
			И НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			
			СвойстваОрганизацииИУчетнойЗаписи = СвойстваОбменаПоСЭДОЧерезОператора.СвойстваОрганизацииИУчетнойЗаписи;
			ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток = ?(СвойстваОрганизацииИУчетнойЗаписи <> Неопределено
				И ЗначениеЗаполнено(СвойстваОрганизацииИУчетнойЗаписи.СерверДокументооборота),
				ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СвойстваОрганизацииИУчетнойЗаписи.СерверДокументооборота, "Сертификат"),
				"");
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток)
			И ДокументооборотСКОВызовСервера.ЗапоминаниеОшибок(,, Истина, "МЧДТолькоПодписание") <> Истина Тогда
			
			Если СвойстваОбменаПоСЭДОЧерезОператора = Неопределено
				ИЛИ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
				ТекстСообщения = НСтр("ru = 'Не выбран сертификат шифрования ЭЛН, ПОВЭД, ССВ, СЭДО в настройках обмена с ФСС для 1С-Отчетности'");
				ТекстСообщения = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(ТекстСообщения, Истина);
			Иначе
				ТекстСообщения = НСтр("ru = 'Не указан сертификат сервера документооборота в свойствах учетной записи 1С-Отчетности'");
			КонецЕсли;
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	СертификатСтрахователя = Неопределено;
	СертификатФСССЭДО = Неопределено;
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Если Настройки = Неопределено ИЛИ НЕ Настройки.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(
					ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
			Иначе
				СертификатСтрахователя = ПолучитьСвойстваОблачногоСертификата(
					ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY");
			КонецЕсли;
			Если СертификатСтрахователя = Неопределено Тогда
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Не найден сертификат страхователя'"));
				Возврат Неопределено;
			КонецЕсли;
			
			СертификатСтрахователя.Вставить("КлючевойКонтейнер", ИдентификаторДокументооборота);
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
		КонецЕсли;
		
		Если ПолучитьСертификатФССЭЛН Тогда
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				СертификатФСССЭДО = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
			Иначе
				СертификатФСССЭДО = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
			КонецЕсли;
			Если СертификатФСССЭДО = Неопределено Тогда
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Не найден сертификат шифрования ЭЛН, ПОВЭД, ССВ, СЭДО'"));
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ЗапросОперацииСЭДО64", 					ЗапросОперацииСЭДО64);
	Результат.Вставить("УчетнаяЗапись", 						УчетнаяЗапись);
	Результат.Вставить("СертификатСтрахователяОтпечаток", 		ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
	Результат.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	Результат.Вставить("СертификатФСССЭДООтпечаток", 			ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
	Результат.Вставить("СертификатФСССЭДО", 					СертификатФСССЭДО);
	Результат.Вставить("ТестовыйСерверФСССЭДО", 				ОтпечаткиСертификатов.ТестовыйСерверФССЭЛН);
	Результат.Вставить("МЧД", 									ОтпечаткиСертификатов.МЧД);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция АдресПодписанногоSOAPОперацииСЭЛНВФССИнициализацияНаСервере(
		Знач ОрганизацияСсылка,
		Знач ВыгрузкаЗапросаОперацииСЭЛН,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач РегистрационныйНомерФСС,
		Знач ПолучитьСертификатФССЭЛН = Ложь)
	
	ЗапросОперацииСЭЛН64 = ЗапросОперацииСЭЛНДляПодписиSOAP64НаСервере(
		ВыгрузкаЗапросаОперацииСЭЛН,
		ДобавляемыеАтрибутыЗапроса,
		РегистрационныйНомерФСС);
	Если ЗапросОперацииСЭЛН64 = Неопределено Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Больничный лист не заполнен'"));
		Возврат Неопределено;
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	УчетнаяЗапись = РеквизитыУчетнойЗаписи.Ссылка;
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
		ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		ДвоичныеДанныеЗапросаОперацииСЭЛН = Base64Значение(ЗапросОперацииСЭЛН64);
		ЗапросОперацииСЭЛН64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеЗапросаОперацииСЭЛН, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка);
	Если ОтпечаткиСертификатов = Неопределено ИЛИ НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток) Тогда
		ОбщегоНазначения.СообщитьПользователю(
			НСтр("ru = 'Не выбран сертификат страхователя в настройках обмена с ФСС для 1С-Отчетности'"));
		Возврат Неопределено;
	КонецЕсли;
	Если ПолучитьСертификатФССЭЛН И НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток) Тогда
		ОбщегоНазначения.СообщитьПользователю(
			НСтр("ru = 'Не выбран сертификат ФСС ЭЛН в настройках обмена с ФСС для 1С-Отчетности'"));
		Возврат Неопределено;
	КонецЕсли;
	
	СертификатСтрахователя = Неопределено;
	СертификатФССЭЛН = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
		ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
		Иначе
			СертификатСтрахователя = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY");
		КонецЕсли;
		Если СертификатСтрахователя = Неопределено Тогда
			ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Не найден сертификат страхователя'"));
			Возврат Неопределено;
		КонецЕсли;
		
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
		СертификатСтрахователя.Вставить("КлючевойКонтейнер", ИдентификаторДокументооборота);
		
		Если ПолучитьСертификатФССЭЛН Тогда
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				СертификатФССЭЛН = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
			Иначе
				СертификатФССЭЛН = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
			КонецЕсли;
			Если СертификатФССЭЛН = Неопределено Тогда
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Не найден сертификат ФСС ЭЛН'"));
				Возврат Неопределено;
			КонецЕсли;
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФССЭЛН);
		КонецЕсли;
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ЗапросОперацииСЭЛН64", 					ЗапросОперацииСЭЛН64);
	Результат.Вставить("УчетнаяЗапись", 						УчетнаяЗапись);
	Результат.Вставить("СертификатСтрахователяОтпечаток", 		ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
	Результат.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	Результат.Вставить("СертификатФССЭЛНОтпечаток", 			ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
	Результат.Вставить("СертификатФССЭЛН", 						СертификатФССЭЛН);
	Результат.Вставить("ТестовыйСерверФССЭЛН", 					ОтпечаткиСертификатов.ТестовыйСерверФССЭЛН);
	Результат.Вставить("МЧД", 									ОтпечаткиСертификатов.МЧД);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Возврат Результат;
	
КонецФункции

#Область АдресПодписанногоSOAPОперацииСЭЛНВФСС

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФСС(
		ВыполняемоеОповещение,
		ВыгрузкаЗапросаОперацииСЭЛН,
		ЗашифроватьSOAP = Истина) Экспорт
	
	ОрганизацияИспользуетОбменСФСС 						= Ложь;
	ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи 	= Ложь;
	НастроенОбменВУниверсальномФормате 					= Ложь;
	УчетнаяЗаписьДокументооборота 						= Неопределено;
	
	ПараметрыФорматов = Новый Структура("ПараметрыФорматаЭЛН", Неопределено);
	ПолучитьСвойстваОрганизацииДляФССНаСервере(
		ВыгрузкаЗапросаОперацииСЭЛН.Организация,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,
		УчетнаяЗаписьДокументооборота,
		ПараметрыФорматов);
	
	Если НЕ ОрганизацияИспользуетОбменСФСС Тогда
		ПоказатьОшибкуЕслиОрганизацияНеИспользуетОбменСФСС(ВыгрузкаЗапросаОперацииСЭЛН.Организация, ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ВыполнятьАвтонастройку = 
		НастроенОбменВУниверсальномФормате 
		И ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ВыгрузкаЗапросаОперацииСЭЛН", ВыгрузкаЗапросаОперацииСЭЛН);
	ДополнительныеПараметры.Вставить("ЗашифроватьSOAP", 			ЗашифроватьSOAP);
	ДополнительныеПараметры.Вставить("ПараметрыФормата", 			ПараметрыФорматов.ПараметрыФорматаЭЛН);
	ДополнительныеПараметры.Вставить("ВыполнятьАвтонастройку", 		ВыполнятьАвтонастройку);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если ВыполнятьАвтонастройку Тогда
		АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗаписьДокументооборота, , Оповещение, , , Истина);
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПроверкиЛицензииИСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	Если ВходящийКонтекст.ВыполнятьАвтонастройку Тогда
		
		Организация = ВходящийКонтекст.ВыгрузкаЗапросаОперацииСЭЛН.Организация;
		
		ПроверитьСертификатИЛицензию(
			ОписаниеОповещения, 
			Неопределено, 
			Организация, 
			"ФСС");
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПроверкиЛицензииИСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыгрузкаЗапросаОперацииСЭЛН = ДополнительныеПараметры.ВыгрузкаЗапросаОперацииСЭЛН;
	ЗашифроватьSOAP 			= ДополнительныеПараметры.ЗашифроватьSOAP;
	ПараметрыФормата 			= ДополнительныеПараметры.ПараметрыФормата;
	
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	ПространствоИменЗапроса = ?(ЭтоВерсия1X, "http://ru/ibs/fss/ln/ws/FileOperationsLn.wsdl",
		"http://www.fss.ru/integration/types/eln/ins/v01");
	ДобавляемыеАтрибутыЗапроса = "xmlns:ds=""http://www.w3.org/2000/09/xmldsig#"""
		+ " xmlns:fil=""" + ПространствоИменЗапроса + """"
		+ " xmlns:sch=""http://gost34.ibs.ru/WrapperService/Schema"""
		+ " xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"""
		+ " xmlns:wsse=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-secext-1.0.xsd"""
		+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""";
	ДобавляемыеАтрибутыПодписи = "xmlns=""http://www.w3.org/2000/09/xmldsig#""";
	
	СкорректированнаяВыгрузкаЗапроса = СкорректироватьВыгрузкуЗапросаОперацииДляSOAP(
		ВыгрузкаЗапросаОперацииСЭЛН.ТекстЗапроса,
		ПространствоИменЗапроса);
	
	РезультатИнициализации = АдресПодписанногоSOAPОперацииСЭЛНВФССИнициализацияНаСервере(
		ВыгрузкаЗапросаОперацииСЭЛН.Организация,
		СкорректированнаяВыгрузкаЗапроса,
		ДобавляемыеАтрибутыЗапроса,
		ВыгрузкаЗапросаОперацииСЭЛН.РегистрационныйНомерФСС,
		ЗашифроватьSOAP);
	
	Если РезультатИнициализации = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗапроса", 			ДобавляемыеАтрибутыЗапроса);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 			ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("СкорректированнаяВыгрузкаЗапроса", 	СкорректированнаяВыгрузкаЗапроса);
	ДополнительныеПараметры.Вставить("РезультатИнициализации", 				РезультатИнициализации);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПредупрежденияПриТестовомРежиме",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если РезультатИнициализации.ТестовыйСерверФССЭЛН Тогда
		ТекстВопроса = НСтр("ru = 'Внимание! Включен режим тестирования ЭЛН!
			|
			|В режиме тестирования обмен выполняется через тестовый СФР (бывш. ФСС).
			|Данные не соответствуют действительности и не засчитываются СФР.
			|
			|Продолжить обмен?'");
		ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, КодВозвратаДиалога.Да);
	КонецЕсли;
	
КонецПроцедуры


&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПредупрежденияПриТестовомРежиме(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыгрузкаЗапросаОперацииСЭЛН 		= ДополнительныеПараметры.ВыгрузкаЗапросаОперацииСЭЛН;
	ЗашифроватьSOAP 					= ДополнительныеПараметры.ЗашифроватьSOAP;
	ДобавляемыеАтрибутыЗапроса 			= ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи 			= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	СкорректированнаяВыгрузкаЗапроса 	= ДополнительныеПараметры.СкорректированнаяВыгрузкаЗапроса;
	РезультатИнициализации 				= ДополнительныеПараметры.РезультатИнициализации;
	ПараметрыФормата 					= ДополнительныеПараметры.ПараметрыФормата;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ЗапросОперацииСЭЛН64 = РезультатИнициализации.ЗапросОперацииСЭЛН64;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РезультатИнициализации);
	УчетнаяЗапись = РезультатИнициализации.УчетнаяЗапись;
	
	СертификатСтрахователяОтпечаток = РезультатИнициализации.СертификатСтрахователяОтпечаток;
	СертификатСтрахователя 			= РезультатИнициализации.СертификатСтрахователя;
	СертификатФССЭЛНОтпечаток 		= РезультатИнициализации.СертификатФССЭЛНОтпечаток;
	СертификатФССЭЛН 				= РезультатИнициализации.СертификатФССЭЛН;
	МЧД 							= РезультатИнициализации.МЧД;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", 			ВыгрузкаЗапросаОперацииСЭЛН.РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ВыгрузкаЗапроса", 					СкорректированнаяВыгрузкаЗапроса);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛНОтпечаток", 			СертификатФССЭЛНОтпечаток);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛН", 					СертификатФССЭЛН);
	ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ЗашифроватьSOAP", 					ЗашифроватьSOAP);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗапроса", 			ДобавляемыеАтрибутыЗапроса);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 			ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("ЗапросОперацииСЭЛН64", 				ЗапросОперацииСЭЛН64);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", 						УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("ПараметрыФормата", 					ПараметрыФормата);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПоискаСертификатаСтрахователя",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, Истина, СертификатСтрахователя);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатСтрахователяОтпечаток, "MY");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПоискаСертификатаСтрахователя(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатФССЭЛНОтпечаток = ДополнительныеПараметры.СертификатФССЭЛНОтпечаток;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", СертификатСтрахователя);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПоискаСертификатаФСС",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, ЗашифроватьSOAP, СертификатФССЭЛН);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатФССЭЛНОтпечаток, "AddressBook");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПоискаСертификатаФСС(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатФССЭЛН = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатСтрахователя, НСтр("ru = 'Сертификат страхователя'"));
	Если ЗашифроватьSOAP Тогда
		СертификатыОбмена.Добавить(СертификатФССЭЛН, НСтр("ru = 'Сертификат ФСС ЭЛН'"));
	КонецЕсли;
	
	Если НЕ СертификатыГодятсяДляПодписанияОтчетностиФСС(СертификатыОбмена) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("СертификатФССЭЛН", СертификатФССЭЛН);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЭкспортаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатСтрахователя);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЭкспортаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЗапросОперацииСЭЛН64 = ДополнительныеПараметры.ЗапросОперацииСЭЛН64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПолучателя64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("СертификатПолучателя64", СертификатПолучателя64);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЗаписиФайла",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ЗапросОперацииСЭЛН64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ЗапросОперацииСЭЛН64);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЗаписиФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗапроса = ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлЗапросаОперацииСЭЛН = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлЗапросаОперацииСЭЛН", ФайлЗапросаОперацииСЭЛН);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПодписания", ЭтотОбъект, ДополнительныеПараметры);
	
	ДобавляемыеАтрибутыПодписиИЗаписи = ДобавляемыеАтрибутыПодписи
		+ ?(ДобавляемыеАтрибутыПодписи <> "" И ДобавляемыеАтрибутыЗапроса <> "", " ", "")
		+ ДобавляемыеАтрибутыЗапроса;
	ИдентификаторПодписываемыхДанных = "REGNO_" + РегистрационныйНомерФСС;
	
	НастройкиПодписи = Новый Структура;
	НастройкиПодписи.Вставить("Трансформация1_1", 						Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 				Ложь);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 				Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 						Неопределено);
	НастройкиПодписи.Вставить("НормализацияОтключена", 					Истина);
	НастройкиПодписи.Вставить("ДанныеВнутриПодписи", 					Истина);
	НастройкиПодписи.Вставить("ИдентификаторПодписываемыхДанных", 		ИдентификаторПодписываемыхДанных);
	НастройкиПодписи.Вставить("ДобавляемыеАтрибуты", 					ДобавляемыеАтрибутыПодписиИЗаписи);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОСертификате", 		Истина);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОбОбъекте", 			Истина);
	НастройкиПодписи.Вставить("ДополнительныеТрансформации", 			Неопределено);
	НастройкиПодписи.Вставить("ДополнительныеОбратныеТрансформации", 	Неопределено);
	НастройкиПодписи.Вставить("Применение", 							"ОтправкиФСС");
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ОперацииСЭЛН", УчетнаяЗапись); 
		ПодготовитьОблачнуюПодпись("Реестр ЭЛН", ".xml", ТребуетПодтверждениеОперации, НастройкиПодписи);
	КонецЕсли;
	
	ПодписатьФайлXMLDSig(
		СертификатСтрахователя,
		ФайлЗапросаОперацииСЭЛН,
		Оповещение,,,
		НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеПодписания(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФайлЗапросаОперацииСЭЛН = ДополнительныеПараметры.ФайлЗапросаОперацииСЭЛН;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьИзВременногоХранилища(ФайлЗапросаОперацииСЭЛН);
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлЗапросаОперацииСЭЛН);
	КонецЕсли;
	
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Не удалось подписать файл запроса ЭЛН!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлПодписиЗапросаОперацииСЭЛН", ПолноеИмяПодписанногоФайла);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЧтенияФайла",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЧтенияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЗапроса = ДополнительныеПараметры.ВыгрузкаЗапроса;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗапроса = ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	ФайлПодписиЗапросаОперацииСЭЛН = ДополнительныеПараметры.ФайлПодписиЗапросаОперацииСЭЛН;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлПодписиЗапросаОперацииСЭЛН);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлПодписиЗапросаОперацииСЭЛН64 = Результат.СтрокаBase64;
	
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыЗапроса", 	ДобавляемыеАтрибутыЗапроса);
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыПодписи", 	ДобавляемыеАтрибутыПодписи);
	НастройкиОперации.Вставить("ВозвращатьТекстВBase64", 		ЗашифроватьSOAP);
	НастройкиОперации.Вставить("МЧД", 							ДополнительныеПараметры.МЧД);
	РезультатОперации = АдресSOAPЗапросОперацииСЭЛНВФССПоПодписиНаСервере(
		РегистрационныйНомерФСС,
		ВыгрузкаЗапроса,
		ФайлПодписиЗапросаОперацииСЭЛН64,
		СертификатПолучателя64,
		НастройкиОперации);
	
	Если НЕ ЗашифроватьSOAP Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЗаписиФайлаПодписанногоSOAP",
			ЭтотОбъект, ДополнительныеПараметры);
		
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, РезультатОперации);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, РезультатОперации);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭЛНВФССПослеЗаписиФайлаПодписанногоSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыФормата = ДополнительныеПараметры.ПараметрыФормата;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлПодписанногоSOAP = Результат.ИмяФайла;
	
	ЭтоВерсия1X = Лев(ПараметрыФормата.Версия, 2) = "1.";
	
	ШаблонЗапросаSOAP =
		"<SOAP-ENV:Envelope xmlns:SOAP-ENV=""http://schemas.xmlsoap.org/soap/envelope/"">
		|	<SOAP-ENV:Header/>
		|	<SOAP-ENV:Body>
		|%1
		|	</SOAP-ENV:Body>
		|</SOAP-ENV:Envelope>";
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ШаблонЗапросаSOAP", ШаблонЗапросаSOAP);
	НастройкиОперации.Вставить("Применение", 		"ОтправкиФСС");
	Если НЕ ЭтоВерсия1X Тогда
		НастройкиОперации.Вставить("ПоместитьВУзелX509CertificateСертификатПолучателя", Истина);
	КонецЕсли;
	НастройкиОперации.Вставить("СценарийФСС", Истина);
	
	ЗашифроватьФайлXML(
		СертификатФССЭЛН,
		СертификатСтрахователя,
		ФайлПодписанногоSOAP,
		ВыполняемоеОповещение,,
		Истина,
		НастройкиОперации);
	
КонецПроцедуры

// необходима для формирования пакетной режима подписи DSS
&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССОблачнаяПодпись(ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности = ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 = ДополнительныеПараметры.МассивПодписейЭЛН64;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОрганизацияСсылка = ДополнительныеПараметры.ОрганизацияСсылка;
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССОблачнаяПодписьЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);
		
	ДобавляемыеАтрибутыПодписиИЗаписи = ДобавляемыеАтрибутыПодписи
		+ ?(ДобавляемыеАтрибутыПодписи <> "" И ДобавляемыеАтрибутыЗаписи <> "", " ", "")
		+ ДобавляемыеАтрибутыЗаписи;
		
	ИдентификаторыПодписываемыхДанных = Новый Массив;
	Для Счетчик = 0 По МассивНомеровЛистковНетрудоспособности.Количество() - 1 Цикл
		ИдентификаторыПодписываемыхДанных.Добавить("ELN_" + МассивНомеровЛистковНетрудоспособности[Счетчик]);
	КонецЦикла;
	
	ДополнительныеТрансформации = Новый Массив;
	ДополнительныеОбратныеТрансформации = Новый Массив;
	
	// прямые и обратные замены
	ДополнительныеТрансформации.Добавить("<eln:");
	ДополнительныеТрансформации.Добавить("<");
	ДополнительныеОбратныеТрансформации.Добавить("<eln:/");
	ДополнительныеОбратныеТрансформации.Добавить("</eln:");
	
	ДополнительныеТрансформации.Добавить("</eln:");
	ДополнительныеТрансформации.Добавить("</");
	ДополнительныеОбратныеТрансформации.Добавить("<");
	ДополнительныеОбратныеТрансформации.Добавить("<eln:");
	
	НастройкиПодписи = Новый Структура;
	НастройкиПодписи.Вставить("Трансформация1_1", 						Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 				Ложь);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 				Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 						Неопределено);
	НастройкиПодписи.Вставить("НормализацияОтключена", 					Истина);
	НастройкиПодписи.Вставить("ДанныеВнутриПодписи", 					Истина);
	НастройкиПодписи.Вставить("ДобавляемыеАтрибуты", 					ДобавляемыеАтрибутыПодписиИЗаписи);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОСертификате", 		Истина);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОбОбъекте", 			Истина);
	НастройкиПодписи.Вставить("ИдентификаторыПодписываемыхДанных", 		ИдентификаторыПодписываемыхДанных);
	НастройкиПодписи.Вставить("ДополнительныеТрансформации", 			ДополнительныеТрансформации);
	НастройкиПодписи.Вставить("ДополнительныеОбратныеТрансформации", 	ДополнительныеОбратныеТрансформации);
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ЗагрузкаЭЛН", ОрганизацияСсылка); 
		ПодготовитьОблачнуюПодпись("Реестр ЭЛН", ".xml", ТребуетПодтверждениеОперации, НастройкиПодписи);
	КонецЕсли;
	
	ПодписатьФайлыXMLDSig(
		СертификатСтрахователя,
		МассивЗаписейЭЛН64,
		Оповещение,,
		НастройкиПодписи);
		
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPЗагрузкиЭЛНВФССОблачнаяПодписьЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЭЛН = ДополнительныеПараметры.ВыгрузкаЭЛН;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФССЭЛН = ДополнительныеПараметры.СертификатФССЭЛН;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗаписи = ДополнительныеПараметры.ДобавляемыеАтрибутыЗаписи;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	НомерЗаписиЭЛН = ДополнительныеПараметры.НомерЗаписиЭЛН;
	МассивЗаписейЭЛН64 = ДополнительныеПараметры.МассивЗаписейЭЛН64;
	МассивНомеровЛистковНетрудоспособности = ДополнительныеПараметры.МассивНомеровЛистковНетрудоспособности;
	МассивПодписейЭЛН64 = Результат;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыЗаписи", 	ДобавляемыеАтрибутыЗаписи);
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыПодписи", 	ДобавляемыеАтрибутыПодписи);
	НастройкиОперации.Вставить("ВозвращатьТекстВBase64", 		ЗашифроватьSOAP);
	НастройкиОперации.Вставить("МЧД", 							ДополнительныеПараметры.МЧД);
	НастройкиОперации.Вставить("ОблачнаяПодпись", 				Истина);
	
	РезультатОперации = АдресSOAPЗагрузкиЭЛНВФССПоМассивуПодписейНаСервере(
		РегистрационныйНомерФСС,
		ВыгрузкаЭЛН,
		МассивПодписейЭЛН64,
		МассивНомеровЛистковНетрудоспособности,
		СертификатПолучателя64,
		НастройкиОперации);
	
	Если НЕ ЗашифроватьSOAP Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		
	Иначе
		ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", РезультатОперации);
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPЗагрузкиЭЛНВФССПослеЗаписиФайлаПодписанногоSOAP",
			ЭтотОбъект, ДополнительныеПараметры);
			
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, РезультатОперации);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, РезультатОперации);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция ЗапросОперацииСЭЛНДляПодписиSOAP64НаСервере(
		Знач ВыгрузкаЗапросаОперацииСЭЛН,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач РегистрационныйНомерФСС)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ЗапросОперацииСЭЛНДляПодписиSOAP64(
		ВыгрузкаЗапросаОперацииСЭЛН,
		ДобавляемыеАтрибутыЗапроса,
		РегистрационныйНомерФСС);
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресSOAPЗапросОперацииСЭЛНВФССПоПодписиНаСервере(
		Знач РегистрационныйНомерФСС,
		Знач ВыгрузкаЗапроса,
		Знач ПодписьЗапросаЭЛН64,
		Знач СертификатПолучателя64,
		Знач НастройкиОперации = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.АдресSOAPЗапросОперацииСЭЛНВФССПоПодписи(
		РегистрационныйНомерФСС,
		ВыгрузкаЗапроса,
		ПодписьЗапросаЭЛН64,
		СертификатПолучателя64,
		НастройкиОперации);
	
КонецФункции

#Область ПроверитьПодписьSOAPОтветаЭЛНВФСС

&НаКлиенте
Процедура ПроверитьПодписьSOAPОтветаЭЛНВФСС(
		ВыполняемоеОповещение,
		ОрганизацияСсылка,
		АдресПодписанногоОтветаSOAP,
		РасшифроватьSOAP = Истина,
		ЗаписыватьОшибкиВЖурналРегистрации = Ложь,
		ТипОперацииОбработки = "",
		ЭтоОперацияСЭДОФСС = Ложь,
		Настройки = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("ПодписьВалидна", Ложь);
	Результат.Вставить("АдресРасшифрованногоОтветаSOAP", "");
	
	ЭтоОперацияСОКВЭД = ЗначениеЗаполнено(ТипОперацииОбработки);
	
	ЗаписыватьОшибкиВЖурналРегистрации = ЭтоОперацияСОКВЭД ИЛИ ЭтоОперацияСЭДОФСС;
	РезультатИнициализации = ПроверитьПодписьSOAPОтветаЭЛНВФССИнициализацияНаСервере(
		ОрганизацияСсылка,
		АдресПодписанногоОтветаSOAP,
		НЕ РасшифроватьSOAP,
		ЗаписыватьОшибкиВЖурналРегистрации,
		ТипОперацииОбработки,
		ЭтоОперацияСЭДОФСС,
		Настройки);
	
	Если РезультатИнициализации = Неопределено Тогда
		Результат.ОписаниеОшибки = НСтр("ru='Ошибка инициализации подсистемы криптографии.'");
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ПодписанныйОтветSOAP64 = РезультатИнициализации.ПодписанныйОтветSOAP64;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РезультатИнициализации);
	УчетнаяЗапись = РезультатИнициализации.УчетнаяЗапись;
	
	СертификатСтрахователяОтпечаток = РезультатИнициализации.СертификатСтрахователяОтпечаток;
	СертификатСтрахователя = РезультатИнициализации.СертификатСтрахователя;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ПодписанныйОтветSOAP64", 				ПодписанныйОтветSOAP64);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("РасшифроватьSOAP", 					РасшифроватьSOAP);
	ДополнительныеПараметры.Вставить("ЗаписыватьОшибкиВЖурналРегистрации", 	ЗаписыватьОшибкиВЖурналРегистрации);
	ДополнительныеПараметры.Вставить("ТипОперацииОбработки", 				ТипОперацииОбработки);
	ДополнительныеПараметры.Вставить("ЭтоОперацияСОКВЭД", 					ЭтоОперацияСОКВЭД);
	ДополнительныеПараметры.Вставить("ЭтоОперацияСЭДОФСС", 					ЭтоОперацияСЭДОФСС);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", 						УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("Результат", 							Результат);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПоискаСертификатаСтрахователя",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, Истина, СертификатСтрахователя);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатСтрахователяОтпечаток, "MY");
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПроверитьПодписьSOAPОтветаЭЛНВФССИнициализацияНаСервере(
		Знач ОрганизацияСсылка,
		Знач АдресПодписанногоОтветаSOAP,
		Знач ВозвращатьТекстВBase64 = Ложь,
		Знач ЗаписыватьОшибкиВЖурналРегистрации = Ложь,
		Знач ТипОперацииОбработки = "",
		Знач ЭтоОперацияСЭДОФСС = Ложь,
		Знач Настройки = Неопределено)
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	УчетнаяЗапись = РеквизитыУчетнойЗаписи.Ссылка;
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	Если ВозвращатьТекстВBase64 И НЕ (НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) И ЭтоОперацияСЭДОФСС) Тогда
		СохранитьАдресОтветаSOAP = ЗаписыватьОшибкиВЖурналРегистрации;
		
		ТекстПодписанногоОтветаSOAP = ПолучитьИзВременногоХранилища(АдресПодписанногоОтветаSOAP);
		Если НЕ СохранитьАдресОтветаSOAP Тогда
			УдалитьИзВременногоХранилища(АдресПодписанногоОтветаSOAP);
		КонецЕсли;
		
		ИмяФайлаОтвета = ПолучитьИмяВременногоФайла();
		ЗаписьТекстаОтвета = Новый ЗаписьТекста(ИмяФайлаОтвета, "utf-8");
		ЗаписьТекстаОтвета.Записать(ТекстПодписанногоОтветаSOAP);
		ЗаписьТекстаОтвета.Закрыть();
		
		ДвоичныеДанныеОтвета = Новый ДвоичныеДанные(ИмяФайлаОтвета);
		ПодписанныйОтветSOAP64 = Base64Строка(ДвоичныеДанныеОтвета);
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаОтвета);
		
	Иначе
		ПодписанныйОтветSOAP64 = АдресПодписанногоОтветаSOAP;
	КонецЕсли;
	
	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка);
	Если ОтпечаткиСертификатов = Неопределено
		ИЛИ (НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток)
			И (Настройки = Неопределено ИЛИ НЕ Настройки.Свойство("СодержимоеПодписанногоЗапросаSOAP"))) Тогда
		
		ТекстОшибки = НСтр("ru = 'Не выбран сертификат страхователя в настройках обмена с ФСС для 1С-Отчетности'");
		Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами.'") + " "
					+ ?(ЗначениеЗаполнено(ТипОперацииОбработки),
						?(ТипОперацииОбработки = "ПодтверждениеВидаДеятельности", НСтр("ru = 'Проверка подписи ПОВЭД'"),
							?(ТипОперацииОбработки = "РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам",
							НСтр("ru = 'Проверка подписи реестра стимулирующих выплат медицинским и социальным работникам'"),
							НСтр("ru = 'Проверка подписи ФСС'"))),
						НСтр("ru = 'Проверка подписи реестра ЭЛН'")),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстОшибки);
		Иначе
			ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
		Если НЕ ВозвращатьТекстВBase64 Тогда
			УдалитьИзВременногоХранилища(АдресПодписанногоОтветаSOAP);
		КонецЕсли;
		Возврат Неопределено;
	КонецЕсли;
	
	СертификатСтрахователя = Неопределено;
	Если (ЭтоОблачнаяПодпись(МестоХраненияКлюча)
			ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча))
		И (Настройки = Неопределено ИЛИ НЕ Настройки.Свойство("СодержимоеПодписанногоЗапросаSOAP")) Тогда
		
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
		Иначе
			СертификатСтрахователя = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY");
		КонецЕсли;
		Если СертификатСтрахователя = Неопределено Тогда
			ТекстОшибки = НСтр("ru = 'Не найден сертификат страхователя'");
			Если ЗаписыватьОшибкиВЖурналРегистрации Тогда
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами.'") + " "
						+ ?(ЗначениеЗаполнено(ТипОперацииОбработки),
							?(ТипОперацииОбработки = "ПодтверждениеВидаДеятельности", НСтр("ru = 'Проверка подписи ПОВЭД'"),
								?(ТипОперацииОбработки = "РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам",
								НСтр("ru = 'Проверка подписи реестра стимулирующих выплат медицинским и социальным работникам'"),
								НСтр("ru = 'Проверка подписи ФСС'"))),
							НСтр("ru = 'Проверка подписи реестра ЭЛН'")),
					УровеньЖурналаРегистрации.Ошибка,,,
					ТекстОшибки);
			Иначе
				ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
			КонецЕсли;
			Если НЕ ВозвращатьТекстВBase64 Тогда
				УдалитьИзВременногоХранилища(АдресПодписанногоОтветаSOAP);
			КонецЕсли;
			Возврат Неопределено;
		КонецЕсли;
		
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
		СертификатСтрахователя.Вставить("КлючевойКонтейнер", ИдентификаторДокументооборота);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ПодписанныйОтветSOAP64", 				ПодписанныйОтветSOAP64);
	Результат.Вставить("УчетнаяЗапись", 						УчетнаяЗапись);
	Результат.Вставить("СертификатСтрахователяОтпечаток", 		ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
	Результат.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПоискаСертификатаСтрахователя(Результат, ДополнительныеПараметры) Экспорт
	
	ПодписанныйОтветSOAP64 = ДополнительныеПараметры.ПодписанныйОтветSOAP64;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	РасшифроватьSOAP = ДополнительныеПараметры.РасшифроватьSOAP;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	РезультатОперации = ДополнительныеПараметры.Результат;
	ЭтоОперацияСЭДОФСС = ДополнительныеПараметры.ЭтоОперацияСЭДОФСС;
	
	Если НЕ Результат.Выполнено Тогда
		Если РасшифроватьSOAP Тогда
			УдалитьИзВременногоХранилища(ПодписанныйОтветSOAP64);
		КонецЕсли;
		Если ЭтоОперацияСЭДОФСС Тогда
			РезультатОперации.ОписаниеОшибки = НСтр("ru='Ошибка поиска сертификата.'");
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", СертификатСтрахователя);
	
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПослеЗаписиФайлаПодписанногоОтветаSOAP",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ РасшифроватьSOAP Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ПодписанныйОтветSOAP64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПодписанныйОтветSOAP64);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПослеЗаписиФайлаПодписанногоОтветаSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	ПодписанныйОтветSOAP64 = ДополнительныеПараметры.ПодписанныйОтветSOAP64;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	РасшифроватьSOAP = ДополнительныеПараметры.РасшифроватьSOAP;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	РезультатОперации = ДополнительныеПараметры.Результат;
	ЭтоОперацияСЭДОФСС = ДополнительныеПараметры.ЭтоОперацияСЭДОФСС;
	
	Если НЕ Результат.Выполнено Тогда
		Если РасшифроватьSOAP Тогда
			УдалитьИзВременногоХранилища(ПодписанныйОтветSOAP64);
		КонецЕсли;
		Если ЭтоОперацияСЭДОФСС Тогда
			РезультатОперации.ОписаниеОшибки = НСтр("ru='Файловая ошибка при записи файла с подписью ФСС.'");
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ПодписанныйОтветSOAP = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ПодписанныйОтветSOAP", ПодписанныйОтветSOAP);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПослеРасшифровки",
		ЭтотОбъект, ДополнительныеПараметры);
	Если РасшифроватьSOAP Тогда
		МассивРодительскихУзловЗашифрованныхДанных = Новый Массив;
		МассивРодительскихУзловЗашифрованныхДанных.Добавить("Envelope");
		МассивРодительскихУзловЗашифрованныхДанных.Добавить("Body");
		НастройкиОперации = Новый Структура;
		НастройкиОперации.Вставить("Применение", "ОтправкиФСС");
		НастройкиОперации.Вставить("СценарийФСС", Истина);
		РасшифроватьФайлXML(
			СертификатСтрахователя,
			ПодписанныйОтветSOAP,
			Оповещение,
			Истина,,
			МассивРодительскихУзловЗашифрованныхДанных,
			НастройкиОперации);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, ПодписанныйОтветSOAP);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПослеРасшифровки(РасшифрованныйФайл, ДополнительныеПараметры) Экспорт
	
	ПодписанныйОтветSOAP64 				= ДополнительныеПараметры.ПодписанныйОтветSOAP64;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	РасшифроватьSOAP 					= ДополнительныеПараметры.РасшифроватьSOAP;
	ЗаписыватьОшибкиВЖурналРегистрации 	= ДополнительныеПараметры.ЗаписыватьОшибкиВЖурналРегистрации;
	ЭтоОперацияСОКВЭД 					= ДополнительныеПараметры.ЭтоОперацияСОКВЭД;
	ЭтоОперацияСЭДОФСС					= ДополнительныеПараметры.ЭтоОперацияСЭДОФСС;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатСтрахователя 				= ДополнительныеПараметры.СертификатСтрахователя;
	ПодписанныйОтветSOAP 				= ДополнительныеПараметры.ПодписанныйОтветSOAP;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	
	СохранитьАдресОтветаSOAP = ЗаписыватьОшибкиВЖурналРегистрации;
	
	Если РасшифроватьSOAP Тогда
		Если НЕ СохранитьАдресОтветаSOAP Тогда
			УдалитьИзВременногоХранилища(ПодписанныйОтветSOAP64);
		КонецЕсли;
	ИначеЕсли ЭтоЛокальнаяПодпись(МестоХраненияКлюча) И РасшифрованныйФайл <> ПодписанныйОтветSOAP Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ПодписанныйОтветSOAP);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(РасшифрованныйФайл) Тогда
		Если ЭтоОперацияСЭДОФСС Тогда
			РезультатОперации.ОписаниеОшибки = НСтр("ru='Ошибка расшифровки ответа ФСС.'");
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("РасшифрованныйОтветSOAP", РасшифрованныйФайл);
	ДополнительныеПараметры.Вставить("Результат", РезультатОперации);
	ДополнительныеПараметры.Вставить("ЭтоОперацияСЭДОФСС", ЭтоОперацияСЭДОФСС);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПроверкиПодписи",
		ЭтотОбъект, ДополнительныеПараметры);
		
	ЗаписыватьОшибкиВЖурналРегистрации = Истина;
	УдалятьЛишниеАтрибуты = ЭтоОперацияСОКВЭД;
	
	ИспользоватьПреобразованиеККаноническомуXML = Ложь;
	Если ЭтоОперацияСЭДОФСС Тогда
		ИспользоватьПреобразованиеККаноническомуXML = Истина;
	КонецЕсли;
	
	Событие = "Проверять подпись СЭДО";
	ПроверятьПодпись = НЕ ЭтоОперацияСЭДОФСС
		ИЛИ (ЭтоОперацияСЭДОФСС И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило(Событие));
	Если НЕ ПроверятьПодпись Тогда
		ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПроверкиПодписи(Истина, ДополнительныеПараметры);
		Возврат;
	КонецЕсли;
	
	ПроверитьПодписьФайлаXMLDSig(
		СертификатСтрахователя,
		РасшифрованныйФайл,
		Оповещение,
		ЗаписыватьОшибкиВЖурналРегистрации,
		УдалятьЛишниеАтрибуты,
		ИспользоватьПреобразованиеККаноническомуXML);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьSOAPОтветаЭЛНВФССПослеПроверкиПодписи(ПодписьВалидна, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	РасшифрованныйОтветSOAP = ДополнительныеПараметры.РасшифрованныйОтветSOAP;
	
	ДополнительныеПараметры.Вставить("ПодписьВалидна", ПодписьВалидна <> Ложь);
	Оповещение = Новый ОписаниеОповещения("ПроверитьПодписьSOAPОтветаЭЛНВФССПослеЧтенияФайлаРасшифрованногоОтветаSOAP",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, РасшифрованныйОтветSOAP);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, РасшифрованныйОтветSOAP);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьПодписьSOAPОтветаЭЛНВФССПослеЧтенияФайлаРасшифрованногоОтветаSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	РасшифрованныйОтветSOAP = ДополнительныеПараметры.РасшифрованныйОтветSOAP;
	ПодписьВалидна = ДополнительныеПараметры.ПодписьВалидна;
	РезультатОперации = ДополнительныеПараметры.Результат;
	ЭтоОперацияСЭДОФСС = ДополнительныеПараметры.ЭтоОперацияСЭДОФСС;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, РасшифрованныйОтветSOAP);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		Если ЭтоОперацияСЭДОФСС Тогда
			РезультатОперации.ОписаниеОшибки = НСтр("ru='Файловая ошибка чтения расшифрованного ответа ФСС.'");
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	РасшифрованныйОтветSOAP64 = Результат.СтрокаBase64;
	
	АдресРасшифрованногоОтветаSOAP = ПоместитьВоВременноеХранилищеТекстСКлиента(
		РасшифрованныйОтветSOAP64,
		НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
		?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча), РасшифрованныйОтветSOAP64, Новый УникальныйИдентификатор()));
		
	РезультатОперации.Выполнено = Истина;
	РезультатОперации.ПодписьВалидна = ПодписьВалидна;
	РезультатОперации.АдресРасшифрованногоОтветаSOAP = АдресРасшифрованногоОтветаSOAP;
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция ПоместитьВоВременноеХранилищеТекстСКлиента(Знач Строка64, ЭтоАдресДвоичныхДанныхВоВременномХранилище = Ложь, АдресВоВременномХранилище)
	
	ИмяФайлаТекста = ПолучитьИмяВременногоФайла();
	Если ЭтоАдресДвоичныхДанныхВоВременномХранилище Тогда
		ДвоичныеДанныеТекста = ПолучитьИзВременногоХранилища(Строка64);
	Иначе
		ДвоичныеДанныеТекста = Base64Значение(Строка64);
	КонецЕсли;
	ДвоичныеДанныеТекста.Записать(ИмяФайлаТекста);
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяФайлаТекста, "utf-8");
	ТекстСКлиента = ЧтениеТекста.Прочитать();
	ЧтениеТекста.Закрыть();
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаТекста);
	
	Возврат ПоместитьВоВременноеХранилище(ТекстСКлиента, АдресВоВременномХранилище);
	
КонецФункции

&НаКлиенте
Процедура ПроверитьУЦСертификатаСтрахователя(
		Оповещение,
		// замена на перечисление МодельРаботыСКлючам
		МестоХраненияКлюча,
		ВыбранныйСертификатСтрахователя) Экспорт
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) И ВыбранныйСертификатСтрахователя.Свойство("ПоставщикСтруктура")
		И ВыбранныйСертификатСтрахователя.ПоставщикСтруктура.Свойство("O")
		И ВыбранныйСертификатСтрахователя.Свойство("Отпечаток") Тогда
		
		ЭтоАвторизованныйУЦ = Ложь;
		ОрганизацияСертификатаСтрахователя = ВыбранныйСертификатСтрахователя.ПоставщикСтруктура.O;
		
		АвторизованныеУЦ = КриптографияЭДКОСлужебныйКлиентСервер.АвторизованныеУдостоверяющиеЦентры();
		Для каждого АвторизованныйУЦ Из АвторизованныеУЦ Цикл
			Если АвторизованныйУЦ.Идентификаторы.Найти(ОрганизацияСертификатаСтрахователя) <> Неопределено Тогда
				ЭтоАвторизованныйУЦ = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если НЕ ЭтоАвторизованныйУЦ Тогда
			ПодписиВJSON = КриптографияЭДКОСлужебныйКлиент.ПодписиСертификатовДокументооборотаВJSON(
				ВыбранныйСертификатСтрахователя);
			Если ЗначениеЗаполнено(ПодписиВJSON) Тогда
				ЭтоАвторизованныйУЦ = Истина;
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ ЭтоАвторизованныйУЦ Тогда
			ТекстСсылки = Новый ФорматированнаяСтрока(НСтр("ru = 'статье'"),,,,
				"https://its.1c.ru/bmk/elreps/fss_cert");
			ТекстВопроса = Новый ФорматированнаяСтрока(
				НСтр("ru = 'Обратите внимание, для выбранного сертификата не найдено подключение к 1С-Отчетности.
				|Подробнее о настройках подключения читайте в'") + " ",
				ТекстСсылки,
				".
				|
				|" + НСтр("ru = 'Выбрать сертификат?'"));
			ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(Оповещение, КодВозвратаДиалога.Да);
	
КонецПроцедуры

&НаКлиенте
Процедура ОпределитьЗадаваемыеСертификатыФСС(
		ОповещениеОЗавершении,
		СертификатСтрахователя,
		СертификатФССОтпечаток,
		СертификатФССЭЛНОтпечаток,
		МестоХраненияКлюча) Экспорт
		
	ЛокальнаяПодпись = ЭтоЛокальнаяПодпись(МестоХраненияКлюча);
	
	Если ОбщегоНазначенияКлиент.ДатаСеанса() >= '20191008' ИЛИ НЕ ЛокальнаяПодпись Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИнформацияОСертификатахФСС = ИнформацияОСертификатахФССНаСервере(НЕ ЛокальнаяПодпись);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	ДополнительныеПараметры.Вставить("СертификатФССОтпечаток", 				СертификатФССОтпечаток);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛНОтпечаток", 			СертификатФССЭЛНОтпечаток);
	ДополнительныеПараметры.Вставить("ИнформацияОСертификатахФСС", 			ИнформацияОСертификатахФСС);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ОпределитьЗадаваемыеСертификатыФССПослеОпределенияАлгоритма",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если ИнформацияОСертификатахФСС = Неопределено ИЛИ (ЗначениеЗаполнено(СертификатФССОтпечаток)
		И СертификатФССОтпечаток <> ИнформацияОСертификатахФСС.ФСССертификатОтпечаток
		И СертификатФССОтпечаток <> ИнформацияОСертификатахФСС.ФСССертификатГОСТ2012Отпечаток
		И ЗначениеЗаполнено(СертификатФССЭЛНОтпечаток)
		И СертификатФССЭЛНОтпечаток <> ИнформацияОСертификатахФСС.ФССЭЛНСертификатОтпечаток
		И СертификатФССЭЛНОтпечаток <> ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Отпечаток) Тогда
		
		РезультатВыполнения = Новый Структура("Выполнено", Ложь);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ПараметрыОпределенияАлгоритма = Новый Структура("Сертификат", СертификатСтрахователя);
		
		КриптографияЭДКОКлиент.ОпределитьАлгоритм(
			Оповещение,
			МестоХраненияКлюча,
			ПараметрыОпределенияАлгоритма);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОпределитьЗадаваемыеСертификатыФССПослеОпределенияАлгоритма(Результат, ВходящийКонтекст) Экспорт
	
	ЗадатьСертификатыФСС = Результат.Выполнено И (Результат.Алгоритм = "GOST R 34.10-2012-256"
		ИЛИ Результат.Алгоритм = "GOST R 34.10-2001");
	
	Если ЗадатьСертификатыФСС Тогда
		
		ИнформацияОСертификатахФСС = ВходящийКонтекст.ИнформацияОСертификатахФСС;
		
		СертификатКорневогоУЦ = Новый Структура;
		СертификатУЦ = Новый Структура;
		СертификатФСС = Новый Структура;
		СертификатФССЭЛН = Новый Структура;
		
		Если Результат.Алгоритм = "GOST R 34.10-2012-256" Тогда
			СертификатКорневогоУЦ.Вставить("Отпечаток", ИнформацияОСертификатахФСС.КорневойСертификатУЦГОСТ2012Отпечаток);
			СертификатКорневогоУЦ.Вставить("Данные", ИнформацияОСертификатахФСС.КорневойСертификатУЦГОСТ2012Данные);
			
			СертификатУЦ.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФСССертификатУЦГОСТ2012Отпечаток);
			СертификатУЦ.Вставить("Данные", ИнформацияОСертификатахФСС.ФСССертификатУЦГОСТ2012Данные);
			
			СертификатФСС.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФСССертификатГОСТ2012Отпечаток);
			СертификатФСС.Вставить("Данные", ИнформацияОСертификатахФСС.ФСССертификатГОСТ2012Данные);
			
			СертификатФССЭЛН.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Отпечаток);
			СертификатФССЭЛН.Вставить("Данные", ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Данные);
			
			ОтпечатокСертификатаФССДругогоАлгоритма = ИнформацияОСертификатахФСС.ФСССертификатОтпечаток;
			ОтпечатокСертификатаФССЭЛНДругогоАлгоритма = ИнформацияОСертификатахФСС.ФССЭЛНСертификатОтпечаток;
			
		Иначе // "GOST R 34.10-2001"
			Если ОбщегоНазначенияКлиент.ДатаСеанса() >= '20190816' Тогда
				Возврат;
			КонецЕсли;
			
			СертификатКорневогоУЦ.Вставить("Отпечаток", ИнформацияОСертификатахФСС.КорневойСертификатУЦОтпечаток);
			СертификатКорневогоУЦ.Вставить("Данные", ИнформацияОСертификатахФСС.КорневойСертификатУЦДанные);
			
			СертификатУЦ.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФСССертификатУЦОтпечаток);
			СертификатУЦ.Вставить("Данные", ИнформацияОСертификатахФСС.ФСССертификатУЦДанные);
			
			СертификатФСС.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФСССертификатОтпечаток);
			СертификатФСС.Вставить("Данные", ИнформацияОСертификатахФСС.ФСССертификатДанные);
			
			СертификатФССЭЛН.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФССЭЛНСертификатОтпечаток);
			СертификатФССЭЛН.Вставить("Данные", ИнформацияОСертификатахФСС.ФССЭЛНСертификатДанные);
			
			ОтпечатокСертификатаФССДругогоАлгоритма = ИнформацияОСертификатахФСС.ФСССертификатГОСТ2012Отпечаток;
			ОтпечатокСертификатаФССЭЛНДругогоАлгоритма = ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Отпечаток;
		КонецЕсли;
		
		ЗадатьСертификатФСС = (НЕ ЗначениеЗаполнено(ВходящийКонтекст.СертификатФССОтпечаток)
			ИЛИ ВходящийКонтекст.СертификатФССОтпечаток = ОтпечатокСертификатаФССДругогоАлгоритма)
			И ЗначениеЗаполнено(СертификатФСС.Отпечаток);
		ЗадатьСертификатФССЭЛН = (НЕ ЗначениеЗаполнено(ВходящийКонтекст.СертификатФССЭЛНОтпечаток)
			ИЛИ ВходящийКонтекст.СертификатФССЭЛНОтпечаток = ОтпечатокСертификатаФССЭЛНДругогоАлгоритма)
			И ЗначениеЗаполнено(СертификатФССЭЛН.Отпечаток);
		ЗадатьСертификатыФСС = ЗадатьСертификатФСС ИЛИ ЗадатьСертификатФССЭЛН;
		
		Если ЗадатьСертификатыФСС Тогда
			УстанавливаемыеСертификаты = Новый Массив;
			
			СертификатКорневогоУЦ.Вставить("Хранилище", "ROOT");
			УстанавливаемыеСертификаты.Добавить(СертификатКорневогоУЦ);
			
			СертификатУЦ.Вставить("Хранилище", "CA");
			УстанавливаемыеСертификаты.Добавить(СертификатУЦ);
			
			Если ЗадатьСертификатФСС Тогда
				СертификатФСС.Вставить("Хранилище", "AddressBook");
				УстанавливаемыеСертификаты.Добавить(СертификатФСС);
			КонецЕсли;
			
			Если ЗадатьСертификатФССЭЛН Тогда
				СертификатФССЭЛН.Вставить("Хранилище", "AddressBook");
				УстанавливаемыеСертификаты.Добавить(СертификатФССЭЛН);
			КонецЕсли;
			
			ВходящийКонтекст.Вставить("СертификатФССОтпечаток", 	?(ЗадатьСертификатФСС, СертификатФСС.Отпечаток, ""));
			ВходящийКонтекст.Вставить("СертификатФССЭЛНОтпечаток", 	?(ЗадатьСертификатФССЭЛН, СертификатФССЭЛН.Отпечаток, ""));
		КонецЕсли;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ОпределитьЗадаваемыеСертификатыФССПослеУстановкиСертификатов",
		ЭтотОбъект, ВходящийКонтекст);
	
	Если ЗадатьСертификатыФСС Тогда
		УстановитьСертификаты(
			Оповещение,
			УстанавливаемыеСертификаты,
			КонтекстМоделиХраненияКлюча(ВходящийКонтекст));
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОпределитьЗадаваемыеСертификатыФССПослеУстановкиСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		Если НЕ ЭтоЛокальнаяПодпись(ВходящийКонтекст) Тогда
			МассивАдресовДанныхСертификатов = Новый Массив;
			Для каждого ИнформацияОСертификатеФСС Из ВходящийКонтекст.ИнформацияОСертификатахФСС Цикл
				Если Прав(ИнформацияОСертификатеФСС.Ключ, 6) = "Данные" И ИнформацияОСертификатеФСС.Значение <> Неопределено Тогда
					МассивАдресовДанныхСертификатов.Добавить(ИнформацияОСертификатеФСС.Значение);
				КонецЕсли;
			КонецЦикла;
			УдалитьАдресаИзВременногоХранилища(МассивАдресовДанныхСертификатов);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения = Новый Структура;
	РезультатВыполнения.Вставить("СертификатФССОтпечаток", 		ВходящийКонтекст.СертификатФССОтпечаток);
	РезультатВыполнения.Вставить("СертификатФССЭЛНОтпечаток", 	ВходящийКонтекст.СертификатФССЭЛНОтпечаток);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
	
КонецПроцедуры

&НаСервере
Функция ИнформацияОСертификатахФССНаСервере(ВозвращатьАдреса = Ложь, ВозвращатьТестовые = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Если КонтекстЭДОСервер = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Результат = Новый Структура;
	
	ИменаМакетовСертификатов = Новый Массив;
	ИменаМакетовСертификатов.Добавить("ФСССертификат");
	ИменаМакетовСертификатов.Добавить("ФСССертификатГОСТ2012");
	ИменаМакетовСертификатов.Добавить("ФССЭЛНСертификат");
	ИменаМакетовСертификатов.Добавить("ФССЭЛНСертификатГОСТ2012");
	Если ВозвращатьТестовые Тогда
		ИменаМакетовСертификатов.Добавить("ФССЭЛНТестовыйСертификат");
		ИменаМакетовСертификатов.Добавить("ФССЭЛНТестовыйСертификатГОСТ2012");
	КонецЕсли;
	ИменаМакетовСертификатов.Добавить("ФСССертификатУЦ");
	ИменаМакетовСертификатов.Добавить("ФСССертификатУЦГОСТ2012");
	ИменаМакетовСертификатов.Добавить("КорневойСертификатУЦ");
	ИменаМакетовСертификатов.Добавить("КорневойСертификатУЦГОСТ2012");
	
	МетаданныеМакетовОбработки = КонтекстЭДОСервер.Метаданные().Макеты;
	Для каждого ИмяМакетаСертификата Из ИменаМакетовСертификатов Цикл
		ОтпечатокСертификата = "";
		ДанныеСертификата = Неопределено;
		
		ОбъектМетаданныхМакет = МетаданныеМакетовОбработки.Найти(ИмяМакетаСертификата);
		Если ОбъектМетаданныхМакет <> Неопределено Тогда
			КомментарийМакета = ОбъектМетаданныхМакет.Комментарий;
			ВхождениеДвоеточия = СтрНайти(КомментарийМакета, ":");
			ОтпечатокСертификата = Сред(КомментарийМакета, ВхождениеДвоеточия + 1);
			ОтпечатокСертификата = СокрЛП(ОтпечатокСертификата);
			ОтпечатокСертификата = нрег(ОтпечатокСертификата);
			
			ДанныеСертификата = КонтекстЭДОСервер.ПолучитьМакет(ИмяМакетаСертификата);
		КонецЕсли;
		
		Результат.Вставить(ИмяМакетаСертификата + "Отпечаток", ОтпечатокСертификата);
		ДанныеСертификата = ?(ДанныеСертификата = Неопределено, Неопределено,
			?(ВозвращатьАдреса, ПоместитьВоВременноеХранилище(ДанныеСертификата, Новый УникальныйИдентификатор),
			Base64Строка(ДанныеСертификата)));
		Результат.Вставить(ИмяМакетаСертификата + "Данные", ДанныеСертификата);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура УстановитьСертификаты(
		ОповещениеОЗавершении,
		УстанавливаемыеСертификаты,
		МестоХраненияКлюча)
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("УстанавливаемыеСертификаты", 			УстанавливаемыеСертификаты);
	ДополнительныеПараметры.Вставить("ИндексСертификата", 					0);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("УстановитьСертификатыВЦикле", ЭтотОбъект, ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьСертификатыВЦикле(Результат, ВходящийКонтекст) Экспорт
	
	Если ВходящийКонтекст.ИндексСертификата >= ВходящийКонтекст.УстанавливаемыеСертификаты.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("УстановитьСертификатыПослеПоискаСертификата", ЭтотОбъект, ВходящийКонтекст);
	
	ОтпечатокСертификата = ВходящийКонтекст.УстанавливаемыеСертификаты[ВходящийКонтекст.ИндексСертификата].Отпечаток;
	ХранилищеСертификата = ВходящийКонтекст.УстанавливаемыеСертификаты[ВходящийКонтекст.ИндексСертификата].Хранилище;
	
	Если НЕ ЭтоЛокальнаяПодпись(ВходящийКонтекст) Тогда
		Если ЭтоПодписьСервиса(ВходящийКонтекст) Тогда
			СвойстваСертификата = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечатокСертификата, ХранилищеСертификата);
		Иначе
			СвойстваСертификата = ПолучитьСвойстваОблачногоСертификата(ОтпечатокСертификата, ХранилищеСертификата);
		КонецЕсли;
		РезультатВыполнения = Новый Структура("Выполнено, СертификатНайден", Истина, СвойстваСертификата <> Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, ОтпечатокСертификата, ХранилищеСертификата);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьСертификатыПослеПоискаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если Результат.СертификатНайден Тогда
		ВходящийКонтекст.ИндексСертификата = ВходящийКонтекст.ИндексСертификата + 1;
		Оповещение = Новый ОписаниеОповещения("УстановитьСертификатыВЦикле", ЭтотОбъект, ВходящийКонтекст);
		
		ВыполнитьОбработкуОповещения(Оповещение);
		
	Иначе
		ДанныеСертификата64 = ВходящийКонтекст.УстанавливаемыеСертификаты[ВходящийКонтекст.ИндексСертификата].Данные;
		
		Оповещение = Новый ОписаниеОповещения("УстановитьСертификатыПослеЗаписиФайлаСертификата",
			ЭтотОбъект, ВходящийКонтекст);
			
		Если НЕ ЭтоЛокальнаяПодпись(ВходящийКонтекст) Тогда
			ДанныеСертификата = Base64Значение(ДанныеСертификата64);
			АдресСертификата = ПоместитьВоВременноеХранилище(ДанныеСертификата, Новый УникальныйИдентификатор);
			РезультатВыполнения = Новый Структура("Выполнено, ИмяФайла", Истина, АдресСертификата);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ДанныеСертификата64);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьСертификатыПослеЗаписиФайлаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаСертификата = Результат.ИмяФайла;
	ХранилищеСертификата = ВходящийКонтекст.УстанавливаемыеСертификаты[ВходящийКонтекст.ИндексСертификата].Хранилище;
	
	ВходящийКонтекст.Вставить("ИмяФайлаСертификата", ИмяФайлаСертификата);
	Оповещение = Новый ОписаниеОповещения("УстановитьСертификатыПослеИмпортаСертификата", ЭтотОбъект, ВходящийКонтекст);
	
	Если ЭтоПодписьСервиса(ВходящийКонтекст) Тогда
		РезультатРегистрации = ЗарегистрироватьСертификатВСистемеНаСервере(ИмяФайлаСертификата, ХранилищеСертификата);
		РезультатВыполнения = Новый Структура("Выполнено", РезультатРегистрации);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	ИначеЕсли ЭтоОблачнаяПодпись(ВходящийКонтекст) Тогда
		РезультатРегистрации = ЗарегистрироватьСертификатВХранилищеПолучателей(ИмяФайлаСертификата, ХранилищеСертификата);
		РезультатВыполнения = Новый Структура("Выполнено", РезультатРегистрации);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		КриптографияЭДКОКлиент.ИмпортироватьСертификат(Оповещение, ИмяФайлаСертификата, ХранилищеСертификата);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьСертификатыПослеИмпортаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если ЭтоЛокальнаяПодпись(ВходящийКонтекст) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ВходящийКонтекст.ИмяФайлаСертификата);
	Иначе
		УдалитьИзВременногоХранилища(ВходящийКонтекст.ИмяФайлаСертификата);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ВходящийКонтекст.ИндексСертификата = ВходящийКонтекст.ИндексСертификата + 1;
	Оповещение = Новый ОписаниеОповещения("УстановитьСертификатыВЦикле", ЭтотОбъект, ВходящийКонтекст);
	
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

#КонецОбласти

#Область ШифрованиеSOAP

&НаКлиенте
Процедура ЗашифроватьФайлXML(
		СертификатПолучателя,
		СертификатШифрованияОтвета,
		ФайлДанных,
		ВыполняемоеОповещение,
		ЗашифрованныйФайл = Неопределено,
		ВозвращатьАдрес = Ложь,
		НастройкиОперации = Неопределено) Экспорт
		
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатШифрованияОтвета);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("СертификатПолучателя", 				СертификатПолучателя);
	ДополнительныеПараметры.Вставить("СертификатШифрованияОтвета", 			СертификатШифрованияОтвета);
	ДополнительныеПараметры.Вставить("ФайлДанных", 							ФайлДанных);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ЗашифрованныйФайл", 					ЗашифрованныйФайл);
	ДополнительныеПараметры.Вставить("ВозвращатьАдрес", 					ВозвращатьАдрес);
	ДополнительныеПараметры.Вставить("НастройкиОперации", 					НастройкиОперации);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеСозданияКаталога", ЭтотОбъект, ДополнительныеПараметры);
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.СоздатьКаталогНаКлиенте(Оповещение);
	Иначе
		РезультатОперацииСФайлами = Новый Структура("Выполнено, ДвоичныеДанные, ИмяКаталога", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатОперацииСФайлами);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеСозданияКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	КаталогРезультатовШифрования = Результат.ИмяКаталога;
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		КаталогРезультатовШифрования = Новый Структура;
		КаталогРезультатовШифрования.Вставить("ЗашифрованныйФайл", 			Новый УникальныйИдентификатор);
		КаталогРезультатовШифрования.Вставить("ПубличныйКлючОтправителя", 	Новый УникальныйИдентификатор);
		КаталогРезультатовШифрования.Вставить("СессионныйКлюч", 			Новый УникальныйИдентификатор);
		КаталогРезультатовШифрования.Вставить("ИнициализационныйВектор", 	Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("КаталогРезультатовШифрования", КаталогРезультатовШифрования);
	
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЗаданияФайловКаталога",
		ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЗаданияФайловКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	НастройкиОперации = ДополнительныеПараметры.НастройкиОперации;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыОперации = Новый Структура;
		Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(НастройкиОперации, "СценарийФСС", Истина) Тогда
			ПараметрыОперации.Вставить("ПараметрыШифрования", Новый Структура("UseFssScenario", "tr"));
		КонецЕсли;
		
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЗашифрованияОблачнойПодписью", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.ЗашифроватьXML(
			Оповещение,
			МестоХраненияКлюча,
			ДополнительныеПараметры.ФайлДанных,
			ДополнительныеПараметры.СертификатПолучателя,
			ДополнительныеПараметры.КаталогРезультатовШифрования,
			, ПараметрыОперации);
	Иначе
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЗашифрования", ЭтотОбъект, ДополнительныеПараметры);
		ПараметрыКриптографии = Новый Структура("АлгоритмШифрования, АлгоритмКлюча, Режим", 0, 26142, 1);
		КриптографияЭДКОКлиент.Зашифровать(
			Оповещение,,
			ДополнительныеПараметры.ФайлДанных,
			ДополнительныеПараметры.СертификатПолучателя,
			ПараметрыКриптографии,
			ДополнительныеПараметры.КаталогРезультатовШифрования,
			Ложь);
	КонецЕсли;	
	
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЗашифрования(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиОперации 					= ДополнительныеПараметры.НастройкиОперации;
	КаталогРезультатовШифрования 		= ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеВопросаПриОшибкеЗашифрования",
			ЭтотОбъект, ДополнительныеПараметры);
			
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ НастройкиОперации = Неопределено
			ИЛИ НЕ НастройкиОперации.Свойство("Применение") ИЛИ НастройкиОперации.Применение <> "ОтправкиФСС" Тогда
			
			Если Результат.Свойство("КаталогРезультатаИлиАдреса") Тогда
				ДополнительныеПараметры.Вставить("КаталогРезультатаИлиАдресаВМоделиСервиса", Результат.КаталогРезультатаИлиАдреса);
			КонецЕсли;
			
			ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
			
		Иначе
			ПоказатьВопросПриОшибкеКриптооперации(
				Оповещение,
				"Зашифровать",
				Результат.ОписаниеОшибки);
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если Результат.Свойство("Алгоритм") Тогда
		Алгоритм = Результат.Алгоритм;
		
	ИначеЕсли ДополнительныеПараметры.СертификатПолучателя.Свойство("Сертификат") Тогда
		Сертификат64 = Base64Строка(ДополнительныеПараметры.СертификатПолучателя.Сертификат);
		Алгоритм = АлгоритмСертификата(Сертификат64, МестоХраненияКлюча);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	ДополнительныеПараметры.Вставить("Алгоритм", Алгоритм);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		КаталогРезультатовШифрования = Результат.КаталогРезультатаИлиАдреса;
		ДополнительныеПараметры.Вставить("СимметричныйКлючВBase64", 		КаталогРезультатовШифрования.СессионныйКлюч);
		ДополнительныеПараметры.Вставить("ПубличныйКлючВBase64", 			КаталогРезультатовШифрования.ПубличныйКлючОтправителя);
		ДополнительныеПараметры.Вставить("ИнициализационныйВекторВBase64", 	КаталогРезультатовШифрования.ИнициализационныйВектор);
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЧтенияЗашифрованныхДанных",
			ЭтотОбъект, ДополнительныеПараметры);
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, КаталогРезультатовШифрования.ЗашифрованныйФайл);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЧтенияСимметричногоКлюча",
			ЭтотОбъект, ДополнительныеПараметры);
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, КаталогРезультатовШифрования + "_skey.bin");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеВопросаПриОшибкеЗашифрования(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Повторить Тогда
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЗаданияФайловКаталога",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
		
	Иначе
		Если НЕ ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
			КаталогРезультатовШифрования = ?(ДополнительныеПараметры.Свойство("КаталогРезультатаИлиАдресаВМоделиСервиса"),
				ДополнительныеПараметры.КаталогРезультатаИлиАдресаВМоделиСервиса, Неопределено);
			Если КаталогРезультатовШифрования <> Неопределено Тогда
				УдалитьАдресаИзВременногоХранилища(
					КаталогРезультатовШифрования.ЗашифрованныйФайл,
					КаталогРезультатовШифрования.ПубличныйКлючОтправителя,
					КаталогРезультатовШифрования.СессионныйКлюч,
					КаталогРезультатовШифрования.ИнициализационныйВектор);
			КонецЕсли;
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ДополнительныеПараметры.КаталогРезультатовШифрования);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЧтенияСимметричногоКлюча(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	КаталогРезультатовШифрования = ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СимметричныйКлючВBase64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("СимметричныйКлючВBase64", СимметричныйКлючВBase64);
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЧтенияПубличногоКлюча",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, КаталогРезультатовШифрования + "_pubkey.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЧтенияПубличногоКлюча(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	КаталогРезультатовШифрования = ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ПубличныйКлючВBase64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("ПубличныйКлючВBase64", ПубличныйКлючВBase64);
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЧтенияИнициализационногоВектора",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, КаталогРезультатовШифрования + "_iv.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЧтенияИнициализационногоВектора(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	КаталогРезультатовШифрования = ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИнициализационныйВекторВBase64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("ИнициализационныйВекторВBase64", ИнициализационныйВекторВBase64);
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЧтенияЗашифрованныхДанных",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, КаталогРезультатовШифрования + "_encrypt.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЧтенияЗашифрованныхДанных(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатШифрованияОтвета 			= ДополнительныеПараметры.СертификатШифрованияОтвета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	КаталогРезультатовШифрования 		= ДополнительныеПараметры.КаталогРезультатовШифрования;
	СимметричныйКлючВBase64 			= ДополнительныеПараметры.СимметричныйКлючВBase64;
	ПубличныйКлючВBase64 				= ДополнительныеПараметры.ПубличныйКлючВBase64;
	ИнициализационныйВекторВBase64 		= ДополнительныеПараметры.ИнициализационныйВекторВBase64;
	СертификатПолучателя				= ДополнительныеПараметры.СертификатПолучателя;
	НастройкиОперации 					= ДополнительныеПараметры.НастройкиОперации;
	
	ПоместитьВУзелX509CertificateСертификатПолучателя = 
		ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
			НастройкиОперации,
			"ПоместитьВУзелX509CertificateСертификатПолучателя",
			Ложь);
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ЗашифрованныеДанныеВBase64 = Результат.СтрокаBase64;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ЗашифрованныеДанныеВBase64", ЗашифрованныеДанныеВBase64);
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЭкспортаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);

	Если ПоместитьВУзелX509CertificateСертификатПолучателя Тогда
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатПолучателя);
	Иначе
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатШифрованияОтвета);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЭкспортаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифрованныйФайл 					= ДополнительныеПараметры.ЗашифрованныйФайл;
	ЗашифрованныеДанныеВBase64 			= ДополнительныеПараметры.ЗашифрованныеДанныеВBase64;
	ВозвращатьАдрес 					= ДополнительныеПараметры.ВозвращатьАдрес;
	НастройкиОперации 					= ДополнительныеПараметры.НастройкиОперации;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатШифрованияОтветаВBase64 = Результат.СтрокаBase64;
	
	Если НастройкиОперации <> Неопределено Тогда
		НастройкиОперации = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(НастройкиОперации);
	Иначе
		НастройкиОперации = Новый Структура;
	КонецЕсли;
	НастройкиОперации.Вставить("ШаблонРезультата", 	НастройкиОперации.ШаблонЗапросаSOAP);
	НастройкиОперации.Удалить("ШаблонЗапросаSOAP");
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ЗашифрованныйXML = АдресЗашифрованногоXMLПоДаннымОблачнойПодписи(
			ЗашифрованныеДанныеВBase64,
			СертификатШифрованияОтветаВBase64,
			Ложь,
			НастройкиОперации);
	Иначе
		СимметричныйКлючВBase64 			= ДополнительныеПараметры.СимметричныйКлючВBase64;
		ПубличныйКлючВBase64 				= ДополнительныеПараметры.ПубличныйКлючВBase64;
		ИнициализационныйВекторВBase64 		= ДополнительныеПараметры.ИнициализационныйВекторВBase64;
		Алгоритм 							= ДополнительныеПараметры.Алгоритм;
		НастройкиОперации.Вставить("Алгоритм", 			Алгоритм);
	
		ЗашифрованныйXML = АдресЗашифрованногоXMLПоДаннымНаСервере(
			СимметричныйКлючВBase64,
			ПубличныйКлючВBase64,
			ИнициализационныйВекторВBase64,
			ЗашифрованныеДанныеВBase64,
			СертификатШифрованияОтветаВBase64,
			НЕ ВозвращатьАдрес И ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
			НастройкиОперации);
	КонецЕсли;
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьАдресаИзВременногоХранилища(
			ЗашифрованныеДанныеВBase64,
			ПубличныйКлючВBase64,
			СимметричныйКлючВBase64,
			ИнициализационныйВекторВBase64);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("НастройкиОперации", 		НастройкиОперации);
	Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
			НастройкиОперации, "ВозвращатьАдресПодписанногоЗапроса", Ложь) Тогда
		ДополнительныеПараметры.Вставить("ВозвращатьАдресПодписанногоЗапроса", Истина);
		ДополнительныеПараметры.Вставить("АдресПодписанногоЗапросаSOAP",
			НастройкиОперации.АдресПодписанногоЗапросаSOAP);
	КонецЕсли;
		
	Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЗаписиФайла",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ВозвращатьАдрес И ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ЗашифрованныйXML, ЗашифрованныйФайл);
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ЗашифрованныйXML);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЗаписиФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиОперации 		= ДополнительныеПараметры.НастройкиОперации;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
			ДополнительныеПараметры,
			"ВозвращатьАдресПодписанногоЗапроса", Ложь) Тогда
		РезультатШифрования = Новый Структура;
		РезультатШифрования.Вставить("АдресПодписаногоЗапросаSOAP", ДополнительныеПараметры.АдресПодписанногоЗапросаSOAP);
		РезультатШифрования.Вставить("АдресЗашифрованногоЗапросаSOAP", Результат.ИмяФайла);
		Если НастройкиОперации <> Неопределено И НастройкиОперации.Свойство("ПодписанноеСообщение64") Тогда
			РезультатШифрования.Вставить("ПодписанноеСообщение64", НастройкиОперации.ПодписанноеСообщение64);
		КонецЕсли;
	Иначе
		РезультатШифрования = Результат.ИмяФайла;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатШифрования);
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXML(
		СертификатПолучателя,
		ЗашифрованныйФайл,
		ВыполняемоеОповещение,
		ЗаданАдрес = Ложь,
		РасшифрованныйФайл = Неопределено,
		МассивРодительскихУзловЗашифрованныхДанных = Неопределено,
		НастройкиОперации = Неопределено) Экспорт
		
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатПолучателя);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("СертификатПолучателя", 						СертификатПолучателя);
	ДополнительныеПараметры.Вставить("ЗашифрованныйФайл", 							ЗашифрованныйФайл);
	ДополнительныеПараметры.Вставить("ЗашифрованныйФайл", 							ЗашифрованныйФайл);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 						ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ЗаданАдрес", 									ЗаданАдрес);
	ДополнительныеПараметры.Вставить("РасшифрованныйФайл", 							РасшифрованныйФайл);
	ДополнительныеПараметры.Вставить("МассивРодительскихУзловЗашифрованныхДанных", 	МассивРодительскихУзловЗашифрованныхДанных);
	ДополнительныеПараметры.Вставить("НастройкиОперации", 							НастройкиОперации);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЭкспортаСертификатаСтрахователя",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатПолучателя, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЭкспортаСертификатаСтрахователя(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатПолучателя 	= ДополнительныеПараметры.СертификатПолучателя;
	ЗашифрованныйФайл 		= ДополнительныеПараметры.ЗашифрованныйФайл;
	ЗаданАдрес 				= ДополнительныеПараметры.ЗаданАдрес;
	
	СертификатПолучателя64 = ?(Результат.Выполнено, Результат.СтрокаBase64, Неопределено);
	
	ДополнительныеПараметры.Вставить("СертификатПолучателя64", СертификатПолучателя64);
	
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЧтенияЗашифрованногоФайла", ЭтотОбъект, ДополнительныеПараметры);
	Если ЗаданАдрес 
		ИЛИ НЕ ЭтоЛокальнаяПодпись(СертификатПолучателя) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, ЗашифрованныйФайл);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ЗашифрованныйФайл);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЧтенияЗашифрованногоФайла(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча							= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатПолучателя 						= ДополнительныеПараметры.СертификатПолучателя;
	ВыполняемоеОповещение 						= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗаданАдрес 									= ДополнительныеПараметры.ЗаданАдрес;
	МассивРодительскихУзловЗашифрованныхДанных 	= ДополнительныеПараметры.МассивРодительскихУзловЗашифрованныхДанных;
	СертификатПолучателя64 						= ДополнительныеПараметры.СертификатПолучателя64;
	ОблачныйСертификат 							= НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча);
	НастройкиОперации 							= ДополнительныеПараметры.НастройкиОперации;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ЗашифрованныйФайлВBase64 = Результат.СтрокаBase64;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ДанныеЗашифрованногоXML = ДанныеПоАдресуЗашифрованногоXMLНаСервереОблачнойПодписи(
			ЗашифрованныйФайлВBase64,
			Ложь,
			МассивРодительскихУзловЗашифрованныхДанных,
			Истина,
			СертификатПолучателя64);
	Иначе
		ДанныеЗашифрованногоXML = ДанныеПоАдресуЗашифрованногоXMLНаСервере(
			ЗашифрованныйФайлВBase64,
			НЕ ЗаданАдрес И НЕ ОблачныйСертификат,
			МассивРодительскихУзловЗашифрованныхДанных,
			НЕ ОблачныйСертификат,
			СертификатПолучателя64);
	КонецЕсли;
		
	Если ДанныеЗашифрованногоXML = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		ДополнительныеПараметры.Вставить("КаталогРезультатовШифрования", 	Неопределено);
		ДополнительныеПараметры.Вставить("ФайлЗашифрованныхДанных", 		ДанныеЗашифрованногоXML.ЗашифрованныеДанныеВBase64);
		ДополнительныеПараметры.Вставить("ФайлСимметричногоКлюча", 			ДанныеЗашифрованногоXML.СимметричныйКлючВBase64);
		ДополнительныеПараметры.Вставить("ФайлПубличногоКлюча", 			ДанныеЗашифрованногоXML.ПубличныйКлючВBase64);
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайлаИнициализационногоВектора",
			ЭтотОбъект, ДополнительныеПараметры);
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ДанныеЗашифрованногоXML.ИнициализационныйВекторВBase64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ДополнительныеПараметры.Вставить("КаталогРезультатовШифрования", 	Неопределено);
		ДополнительныеПараметры.Вставить("ФайлЗашифрованныхДанных", 		ДанныеЗашифрованногоXML);
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеРасшифрования", ЭтотОбъект, ДополнительныеПараметры);
		ФайлРасшифрованныхДанных = Новый УникальныйИдентификатор;
		
		ПараметрыОперации = Новый Структура;
		Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(НастройкиОперации, "СценарийФСС", Истина) Тогда
			ПараметрыОперации.Вставить("ПараметрыШифрования", Новый Структура("UseFssScenario", "tr"));
		КонецЕсли;
		
		КриптографияЭДКОКлиент.РасшифроватьXML(
			Оповещение,
			ДанныеЗашифрованногоXML,
			ФайлРасшифрованныхДанных,
			СертификатПолучателя,
			МестоХраненияКлюча,
			, ПараметрыОперации);
		
	Иначе
		ДополнительныеПараметры.Вставить("ЗашифрованныеДанныеВBase64", 		ДанныеЗашифрованногоXML.ЗашифрованныеДанныеВBase64);
		ДополнительныеПараметры.Вставить("СимметричныйКлючВBase64", 		ДанныеЗашифрованногоXML.СимметричныйКлючВBase64);
		ДополнительныеПараметры.Вставить("ПубличныйКлючВBase64", 			ДанныеЗашифрованногоXML.ПубличныйКлючВBase64);
		ДополнительныеПараметры.Вставить("ИнициализационныйВекторВBase64", 	ДанныеЗашифрованногоXML.ИнициализационныйВекторВBase64);
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеСозданияКаталога", ЭтотОбъект, ДополнительныеПараметры);
		ОперацииСФайламиЭДКОКлиент.СоздатьКаталогНаКлиенте(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеСозданияКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифрованныеДанныеВBase64 = ДополнительныеПараметры.ЗашифрованныеДанныеВBase64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	КаталогРезультатовШифрования = Результат.ИмяКаталога;
	
	ДополнительныеПараметры.Вставить("КаталогРезультатовШифрования", КаталогРезультатовШифрования);
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайлаЗашифрованныхДанных",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ЗашифрованныеДанныеВBase64, КаталогРезультатовШифрования + "_encrypt.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЗаписиФайлаЗашифрованныхДанных(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СимметричныйКлючВBase64 = ДополнительныеПараметры.СимметричныйКлючВBase64;
	КаталогРезультатовШифрования = ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлЗашифрованныхДанных = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлЗашифрованныхДанных", ФайлЗашифрованныхДанных);
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайлаСимметричногоКлюча",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СимметричныйКлючВBase64, КаталогРезультатовШифрования + "_skey.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЗаписиФайлаСимметричногоКлюча(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПубличныйКлючВBase64 = ДополнительныеПараметры.ПубличныйКлючВBase64;
	КаталогРезультатовШифрования = ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлСимметричногоКлюча = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлСимметричногоКлюча", ФайлСимметричногоКлюча);
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайлаПубличногоКлюча",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПубличныйКлючВBase64, КаталогРезультатовШифрования + "_pubkey.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЗаписиФайлаПубличногоКлюча(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИнициализационныйВекторВBase64 = ДополнительныеПараметры.ИнициализационныйВекторВBase64;
	КаталогРезультатовШифрования = ДополнительныеПараметры.КаталогРезультатовШифрования;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлПубличногоКлюча = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлПубличногоКлюча", ФайлПубличногоКлюча);
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайлаИнициализационногоВектора",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ИнициализационныйВекторВBase64, КаталогРезультатовШифрования + "_iv.bin");
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЗаписиФайлаИнициализационногоВектора(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	КаталогРезультатовШифрования 		= ДополнительныеПараметры.КаталогРезультатовШифрования;
	СертификатПолучателя 				= ДополнительныеПараметры.СертификатПолучателя;
	
	Если НЕ Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,КаталогРезультатовШифрования);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлИнициализационногоВектора = Результат.ИмяФайла;
	
	ФайлРасшифрованныхДанных = ?(ЭтоПодписьСервиса(СертификатПолучателя), Новый УникальныйИдентификатор, Неопределено);
	
	ДополнительныеПараметры.Вставить("ФайлИнициализационногоВектора", 	ФайлИнициализационногоВектора);
	ДополнительныеПараметры.Вставить("ФайлРасшифрованныхДанных", 		ФайлРасшифрованныхДанных);
	
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайловДляРасшифрования",
		ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЗаписиФайловДляРасшифрования(Результат, ДополнительныеПараметры) Экспорт
	
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеРасшифрования",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.РасшифроватьФайл(
		Оповещение,
		ДополнительныеПараметры.СертификатПолучателя,
		ДополнительныеПараметры.ФайлЗашифрованныхДанных,
		ДополнительныеПараметры.ФайлПубличногоКлюча,
		ДополнительныеПараметры.ФайлСимметричногоКлюча,
		ДополнительныеПараметры.ФайлИнициализационногоВектора,
		ДополнительныеПараметры.ФайлРасшифрованныхДанных,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеРасшифрования(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиОперации 					= ДополнительныеПараметры.НастройкиОперации;
	КаталогРезультатовШифрования 		= ДополнительныеПараметры.КаталогРезультатовШифрования;
	ФайлЗашифрованныхДанных 			= ДополнительныеПараметры.ФайлЗашифрованныхДанных;
	
	Если НЕ Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеВопросаПриОшибкеРасшифрования",
			ЭтотОбъект, ДополнительныеПараметры);
		
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) ИЛИ НастройкиОперации = Неопределено
			ИЛИ НЕ НастройкиОперации.Свойство("Применение") ИЛИ НастройкиОперации.Применение <> "ОтправкиФСС" Тогда
			
			ВыполнитьОбработкуОповещения(Оповещение, КодВозвратаДиалога.Отмена);
			
		Иначе
			ПоказатьВопросПриОшибкеКриптооперации(
				Оповещение,
				"Расшифровать",
				Результат.ОписаниеОшибки);
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		ФайлСимметричногоКлюча 				= ДополнительныеПараметры.ФайлСимметричногоКлюча;
		ФайлПубличногоКлюча 				= ДополнительныеПараметры.ФайлПубличногоКлюча;
		ФайлИнициализационногоВектора 		= ДополнительныеПараметры.ФайлИнициализационногоВектора;
		
		УдалитьАдресаИзВременногоХранилища(
			ФайлЗашифрованныхДанных,
			ФайлСимметричногоКлюча,
			ФайлПубличногоКлюча,
			ФайлИнициализационногоВектора);
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, КаталогРезультатовШифрования);
	КонецЕсли;
	
	ФайлРасшифрованныхДанных = Результат.РасшифрованныйФайл;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ФайлРасшифрованныхДанных = СкорректироватьXMLПослеРасшифровкиВоВременномХранилище(ФайлРасшифрованныхДанных);
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлРасшифрованныхДанных", ФайлРасшифрованныхДанных);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиРасшифрованногоФайла",
			ЭтотОбъект, ДополнительныеПараметры);
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ФайлРасшифрованныхДанных);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЧтенияРасшифрованногоФайла",
			ЭтотОбъект, ДополнительныеПараметры);
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ФайлРасшифрованныхДанных);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеВопросаПриОшибкеРасшифрования(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Повторить Тогда
		Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиФайловДляРасшифрования",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
		
	Иначе
		Если ЭтоПодписьСервиса(ДополнительныеПараметры) Тогда
			УдалитьАдресаИзВременногоХранилища(
				ДополнительныеПараметры.ФайлЗашифрованныхДанных,
				ДополнительныеПараметры.ФайлСимметричногоКлюча,
				ДополнительныеПараметры.ФайлПубличногоКлюча,
				ДополнительныеПараметры.ФайлИнициализационногоВектора);
		ИначеЕсли ЭтоЛокальнаяПодпись(ДополнительныеПараметры) Тогда
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ДополнительныеПараметры.КаталогРезультатовШифрования);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЧтенияРасшифрованногоФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РасшифрованныйФайл = ДополнительныеПараметры.РасшифрованныйФайл;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ФайлРасшифрованныхДанных = ДополнительныеПараметры.ФайлРасшифрованныхДанных;
	
	ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ФайлРасшифрованныхДанных);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ТекстРасшифрованногоФайла64 = Результат.СтрокаBase64;
	
	ТекстРасшифрованногоФайла64 = СкорректироватьXMLПослеРасшифровки(ТекстРасшифрованногоФайла64, Истина);
	
	Оповещение = Новый ОписаниеОповещения("РасшифроватьФайлXMLПослеЗаписиРасшифрованногоФайла",
		ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ТекстРасшифрованногоФайла64, РасшифрованныйФайл);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция СкорректироватьXMLПослеРасшифровки(ТекстРасшифрованногоXML, ИспользуетсяBase64 = Ложь)
	
	Результат = ТекстРасшифрованногоXML;
	Если ИспользуетсяBase64 Тогда
		ДанныеРасшифрованногоXML = Base64Значение(Результат);
		Результат = ПолучитьСтрокуИзДвоичныхДанных(ДанныеРасшифрованногоXML, "windows-1251");
	КонецЕсли;
	
	ДлинаТекста = СтрДлина(Результат);
	ДлинаПоследнегоБлока = ДлинаТекста % 8;
	ДлинаПоследнегоБлока = ?(ДлинаПоследнегоБлока = 0 И ДлинаТекста > 8, 8, ДлинаПоследнегоБлока) + ?(ДлинаТекста > 8, 1, 0);
	
	ТекстЗавершенияРасшифрованногоФайла = Сред(Результат, ДлинаТекста - ДлинаПоследнегоБлока + 1);
	ПозицияЗакрытияУзла = СтрНайти(ТекстЗавершенияРасшифрованногоФайла, ">");
	Если ПозицияЗакрытияУзла > 0 Тогда
		Результат = Лев(Результат, ДлинаТекста - ДлинаПоследнегоБлока)
			+ Лев(ТекстЗавершенияРасшифрованногоФайла, ПозицияЗакрытияУзла);
	КонецЕсли;
	
	Если ИспользуетсяBase64 Тогда
		ДанныеРасшифрованногоXML = ПолучитьДвоичныеДанныеИзСтроки(Результат, "windows-1251");
		Результат = Base64Строка(ДанныеРасшифрованногоXML);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СкорректироватьXMLПослеРасшифровкиВоВременномХранилище(АдресРасшифрованногоXML)
	
	ВременныйФайлРасшифрованногоXML = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеРасшифрованногоXML = ПолучитьИзВременногоХранилища(АдресРасшифрованногоXML);
	ДвоичныеДанныеРасшифрованногоXML.Записать(ВременныйФайлРасшифрованногоXML);
	ЧтениеТекстаРасшифрованногоXML = Новый ЧтениеТекста(ВременныйФайлРасшифрованногоXML, "windows-1251");
	ТекстРасшифрованногоXML = ЧтениеТекстаРасшифрованногоXML.Прочитать();
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлРасшифрованногоXML);
	
	ТекстРасшифрованногоXML = СкорректироватьXMLПослеРасшифровки(ТекстРасшифрованногоXML);
	
	ВременныйФайлРасшифрованногоXML = ПолучитьИмяВременногоФайла();
	ЗаписьТекстаРасшифрованногоXML = Новый ЗаписьТекста(ВременныйФайлРасшифрованногоXML, "windows-1251");
	ЗаписьТекстаРасшифрованногоXML.Записать(ТекстРасшифрованногоXML);
	ЗаписьТекстаРасшифрованногоXML.Закрыть();
	ДвоичныеДанныеРасшифрованногоXML = Новый ДвоичныеДанные(ВременныйФайлРасшифрованногоXML);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйФайлРасшифрованногоXML);
	Возврат ПоместитьВоВременноеХранилище(ДвоичныеДанныеРасшифрованногоXML, АдресРасшифрованногоXML);
	
КонецФункции

&НаКлиенте
Процедура РасшифроватьФайлXMLПослеЗаписиРасшифрованногоФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	РасшифрованныйФайл = Результат.ИмяФайла;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РасшифрованныйФайл);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция АдресЗашифрованногоXMLПоДаннымНаСервере(
		Знач СимметричныйКлючВBase64,
		Знач ПубличныйКлючВBase64,
		Знач ИнициализационныйВекторВBase64,
		Знач ЗашифрованныеДанныеВBase64,
		Знач СертификатШифрованияОтветаВBase64,
		Знач ВозвращатьТекстВBase64 = Ложь,
		Знач НастройкиОперации = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.АдресЗашифрованногоXMLПоДанным(
		СимметричныйКлючВBase64,
		ПубличныйКлючВBase64,
		ИнициализационныйВекторВBase64,
		ЗашифрованныеДанныеВBase64,
		СертификатШифрованияОтветаВBase64,
		ВозвращатьТекстВBase64,
		НастройкиОперации);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеПоАдресуЗашифрованногоXMLНаСервере(
		Знач АдресЗашифрованногоXML,
		Знач ПереданТекстВBase64 = Ложь,
		Знач МассивРодительскихУзловЗашифрованныхДанных = Неопределено,
		Знач ВозвращатьТекстВBase64 = Истина,
		Знач СертификатПолучателя64 = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ДанныеПоАдресуЗашифрованногоXML(
		АдресЗашифрованногоXML,
		ПереданТекстВBase64,
		МассивРодительскихУзловЗашифрованныхДанных,
		ВозвращатьТекстВBase64,
		СертификатПолучателя64);
	
КонецФункции

&НаСервереБезКонтекста
Функция АлгоритмСертификата(Сертификат64, МестоХраненияКлюча, ЭтоСтрокаBase64 = Истина)
	
	Возврат КриптографияЭДКО.АлгоритмКриптосообщенияИлиСертификата(
		Сертификат64,
		Истина,
		ЭтоСтрокаBase64,
		МестоХраненияКлюча);
	
КонецФункции

&НаКлиенте
Процедура ЗашифроватьФайлXMLПослеЗашифрованияОблачнойПодписью(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	СертификатШифрованияОтвета = ДополнительныеПараметры.СертификатШифрованияОтвета;
	НастройкиОперации = ДополнительныеПараметры.НастройкиОперации;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
	Иначе
		ДополнительныеПараметры.Вставить("ЗашифрованныеДанныеВBase64", Результат.ЗашифрованныйФайл);
		ПоместитьВУзелX509CertificateСертификатПолучателя = 
			ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
				НастройкиОперации,
				"ПоместитьВУзелX509CertificateСертификатПолучателя",
				Ложь);
		
		Оповещение = Новый ОписаниеОповещения("ЗашифроватьФайлXMLПослеЭкспортаСертификата",
			ЭтотОбъект, ДополнительныеПараметры);
		Если ПоместитьВУзелX509CertificateСертификатПолучателя Тогда
			КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатПолучателя);
		Иначе
			КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатШифрованияОтвета);
		КонецЕсли;
		
	КонецЕсли;	
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция АдресЗашифрованногоXMLПоДаннымОблачнойПодписи(
		Знач ЗашифрованныеДанныеВBase64,
		Знач СертификатШифрованияОтветаВBase64,
		Знач ВозвращатьТекстВBase64 = Ложь,
		Знач НастройкиОперации = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	ЗашифрованныеДанныеXML = ЗашифрованныеДанныеВBase64;
	
	Если ЭтоАдресВременногоХранилища(ЗашифрованныеДанныеXML) Тогда
		ЗашифрованныеДанныеXML = ПолучитьИзВременногоХранилища(ЗашифрованныеДанныеXML);
	КонецЕсли;
	
	Если ТипЗнч(ЗашифрованныеДанныеXML) = Тип("ДвоичныеДанные") Тогда
		ЗашифрованныеДанныеXML = ПолучитьСтрокуИзДвоичныхДанных(ЗашифрованныеДанныеXML);
	КонецЕсли;
	
	Возврат КонтекстЭДО.АдресЗашифрованногоXMLПоДаннымОблачнойПодписи(
		ЗашифрованныеДанныеXML,
		СертификатШифрованияОтветаВBase64,
		ВозвращатьТекстВBase64,
		НастройкиОперации);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеПоАдресуЗашифрованногоXMLНаСервереОблачнойПодписи(
		Знач АдресЗашифрованногоXML,
		Знач ПереданТекстВBase64 = Ложь,
		Знач МассивРодительскихУзловЗашифрованныхДанных = Неопределено,
		Знач ВозвращатьТекстВBase64 = Истина,
		Знач СертификатПолучателя64 = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Возврат КонтекстЭДО.ДанныеПоАдресуЗашифрованногоXMLОблачнойПодписи(
		АдресЗашифрованногоXML,
		ПереданТекстВBase64,
		МассивРодительскихУзловЗашифрованныхДанных,
		ВозвращатьТекстВBase64,
		СертификатПолучателя64);
	
КонецФункции

#КонецОбласти

#Область ОбменСЭДОСФСС

#Область СлужебныеФункцииОбменСЭДОФСС

&НаКлиенте
Функция СвойстваОбменаПоСЭДОЧерезОператора(Организация = Неопределено, СброситьБезПолучения = Ложь)
	
	Возврат ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.СвойстваОбменаПоСЭДОЧерезОператора(
				Организация, СброситьБезПолучения);
	
КонецФункции

&НаСервереБезКонтекста
Функция НовыеСообщенияСЭДОФСС(Организация,
			Знач Отбор = Неопределено,
			Знач ТипВзаимодействия = Неопределено,
			Знач МинимальнаяДата = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	НовыеСообщения = КонтекстЭДО.НовыеСообщенияСЭДОФСС(Организация, Отбор, ТипВзаимодействия, МинимальнаяДата);
	Возврат ОбщегоНазначения.ТаблицаЗначенийВМассив(НовыеСообщения);
	
КонецФункции

&НаСервереБезКонтекста
Процедура СохранитьДатуОтправкиПодтвержденияОПрочтении(Организация, ИдентификаторыСообщений)

	Для каждого ИдентификаторСообщения Из ИдентификаторыСообщений Цикл
		Запись = РегистрыСведений.ВходящиеСообщенияСЭДОФСС.СоздатьМенеджерЗаписи();
		Запись.Организация = Организация;
		Запись.Идентификатор = ИдентификаторСообщения;
		Запись.Прочитать();
		Если НЕ Запись.ПодтверждениеОтправлено Тогда
			Запись.Организация = Организация;
			Запись.Идентификатор = ИдентификаторСообщения;
			Запись.ДатаОтправкиПодтверждения = ТекущаяДатаСеанса();
			Запись.ПодтверждениеОтправлено = Истина;
			Запись.Записать();
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Функция РезультатОтправитьСообщениеСЭДОФСС()
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("ИдентификаторЗапроса", Неопределено);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СтраховательПодписанНаУведомленияПоЭЛН(Организация)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СтраховательПодписанНаУведомленияПоЭЛН(
		Организация);
	
КонецФункции

&НаКлиенте
Функция ОставитьТолькоЦифрыВСНИЛС(СНИЛС)
	
	Возврат СтрЗаменить(СтрЗаменить(СНИЛС, " ", ""), "-", "");
	
КонецФункции

&НаКлиенте
Функция ТипФормыКлиентскогоПриложения()
	
	Возврат Тип("ФормаКлиентскогоПриложения");
	
КонецФункции

&НаКлиенте
Процедура ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации)
	
	Если ТипЗнч(ФормаДлительнойОперации) = ТипФормыКлиентскогоПриложения() Тогда
		Если ФормаДлительнойОперации.Открыта() Тогда
			ФормаДлительнойОперации.Закрыть();
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ФормаДлительнойОперацииОткрыта(ФормаДлительнойОперации)
	
	Если ТипЗнч(ФормаДлительнойОперации) = ТипФормыКлиентскогоПриложения() Тогда
		Возврат ФормаДлительнойОперации.Открыта();
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьЗапросСервисуСЭДОФССНаСервере(
		ЗНАЧ Организация,
		ЗНАЧ Операция,
		ЗНАЧ АдресЗашифрованногоЗапросаSOAP,
		ЗНАЧ ОбменПоСЭДОНапрямую = Неопределено)
		
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьЗапросСервисуСЭДОФСС(
		Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP,
		ОбменПоСЭДОНапрямую);
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗначениеНеобязательногоСвойства(Данные,
		ИмяСвойства,
		ПривестиКТипу = "",
		ЗначениеПоУмолчанию = Неопределено)
	
	Если ЕстьСвойство(Данные, ИмяСвойства) Тогда
		Если НЕ ПустаяСтрока(ПривестиКТипу) Тогда
			Возврат XMLЗначение(Тип(ПривестиКТипу), Данные[ИмяСвойства]);
		Иначе
			Возврат Данные[ИмяСвойства];
		КонецЕсли;
	Иначе
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЕстьСвойство(Данные, Имя) Экспорт
	
	Если НЕ ТипЗнч(Данные) = Тип("ОбъектXDTO") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат НЕ Данные.Свойства().Получить(Имя) = Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗначениеВКоллекцию(Значение)
	
	Если ТипЗнч(Значение) <> Тип("Массив")
		И ТипЗнч(Значение) <> Тип("ТаблицаЗначений")
		И ТипЗнч(Значение) <> Тип("СписокXDTO") Тогда
		
		Коллекция = Новый Массив;
		Коллекция.Добавить(Значение);
		Возврат Коллекция;
		
	КонецЕсли;
	
	Возврат Значение;
	
КонецФункции

&НаКлиенте
Функция ПараметрыОтправитьСообщениеСЭДОФСС() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ТипСообщения", 				0);
	Результат.Вставить("СодержимоеСообщения", 		"");
	Результат.Вставить("Организация", 				Неопределено);
	Результат.Вставить("ОповещениеОбратногоВызова", Неопределено);
	Результат.Вставить("ОписаниеОшибки", 			"");
	Результат.Вставить("ЭтоПользовательскийЗапрос", Ложь);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СохранитьДанныеВходящихСообщенийСЭДОФСС(
		Организация,
		ДанныеСообщений,
		ТипВзаимодействия = Неопределено,
		ОбменПоСЭДОНапрямую = Истина)

	Сообщения = ЗначениеВКоллекцию(ДанныеСообщений);
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СохранитьДанныеВходящихСообщенийСЭДОФСС(
		Организация,
		Сообщения,
		ТипВзаимодействия,
		ОбменПоСЭДОНапрямую);
	
КонецФункции

&НаСервереБезКонтекста
Процедура СохранитьДанныеРассылокСЭДОФСС(
		Организация,
		ДанныеСообщений,
		ОбменПоСЭДОНапрямую = Истина)

	Сообщения = ЗначениеВКоллекцию(ДанныеСообщений);
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДО.СохранитьДанныеРассылокСЭДОФСС(
		Организация,
		Сообщения,
		ОбменПоСЭДОНапрямую);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция ОперацияПолученияСпискаВходящихСЭДОФСС()
	
	Возврат ЭлектронныйДокументооборотСФССКлиентСервер.ОперацияПолученияСпискаВходящихСЭДО();
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОперацияОтправкиСообщенияСЭДОФСС()
	
	Возврат ЭлектронныйДокументооборотСФССКлиентСервер.ОперацияОтправкиСообщенияСЭДО();
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ОперацияПолученияСообщенияСЭДОФСС()
	
	Возврат ЭлектронныйДокументооборотСФССКлиентСервер.ОперацияПолученияСообщенияСЭДО();
	
КонецФункции

&НаКлиенте
Функция XMLБулево(Значение)
	
	Если Значение Тогда
		Возврат "true";
	Иначе
		Возврат "false";
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПросклонятьКоличествоЭлементовВСписке(СписокСНИЛС, СклоняемаяСтрока)
	
	Количество = СписокСНИЛС.Количество();
	ЧислоСотрудников = ПолучитьСклоненияСтрокиПоЧислу(СклоняемаяСтрока, Количество, "",
		"ЧС=Количественное", "ПД=Родительный");
	
	Возврат ЧислоСотрудников[0];
	
КонецФункции

#Область РазборОтветов

&НаСервереБезКонтекста
Функция РазобратьОшибкуСервераСЭДОФСС(СодержимоеОтвета)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("Код", "");
	Результат.Вставить("Описание", "");
	Результат.Вставить("ДеталиКод", "");
	Результат.Вставить("ДеталиОписание", "");
	
	// Пример ответа сервера:
	//<soap:Body wsu:Id="OGRN_1027739443236">
	//	<soap:Fault>
	//		<faultcode>soap:Server</faultcode>
	//		<faultstring>Найдены ошибки при проверке реквизитов организации</faultstring>
	//		<detail>
	//			<ns3:internalFault xmlns:ns2="http://www.fss.ru/integration/types/common/v01" xmlns:ns3="http://www.fss.ru/integration/types/fault/v01" xmlns:ns4="http://www.fss.ru/integration/types/sedo/v01">
	//				<ns3:uuid>ccd9c84e-91cb-4dc6-97ba-043d31c2ab1b</ns3:uuid>
	//				<ns3:code>E_SEDO_1025 </ns3:code>
	//				<ns3:message>Пара рег.номер из запроса и ОГРН из сертификата не найдены в справочнике страхователей</ns3:message>
	//			</ns3:internalFault>
	//		</detail>
	//	</soap:Fault>
	//</soap:Body>
	
	Чтение = Новый ЧтениеXML;
	Чтение.УстановитьСтроку(СодержимоеОтвета);
	ОшибкаЧтения = Ложь;
	Попытка
		ДанныеОтвета = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Исключение
		ОшибкаЧтения = Истина;
	КонецПопытки;
	
	Если ОшибкаЧтения ИЛИ ДанныеОтвета = Неопределено Тогда
		Шаблон = НСтр("ru='Не удалось разобрать ответ сервера ФСС с ошибкой: ""%1""'");
		ТекстДляЖурнала = СтрШаблон(Шаблон, СодержимоеОтвета);
		ЖурналРегистрации.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных СЭДО ФСС'"),
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстДляЖурнала);
		Результат.ОписаниеОшибки = НСтр("ru='С сервера СЭДО получена неизвестная ошибка'");
		Возврат Результат;
	КонецЕсли;
	
	Если НЕ ЕстьСвойство(ДанныеОтвета.Body, "Fault") Тогда
		Шаблон = НСтр("ru='Не удалось разобрать ответ сервера ФСС с ошибкой (отсутствует узел Fault): ""%1""'");
		ТекстДляЖурнала = СтрШаблон(Шаблон, СодержимоеОтвета);
		ЖурналРегистрации.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных СЭДО ФСС'"),
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстДляЖурнала);
		Результат.ОписаниеОшибки = НСтр("ru='С сервера СЭДО получена неизвестная ошибка'");
		Возврат Результат;
	КонецЕсли;
	
	УзелДанных = ДанныеОтвета.Body.Fault;
	ОписаниеОшибки = УзелДанных.faultstring;
	Результат.Код = УзелДанных.faultcode;
	
	Если УзелДанных.Свойства().Получить("detail") <> Неопределено Тогда
		Детали = УзелДанных.detail;
		Подробности = Неопределено;
		Если Детали.Свойства().Получить("internalFault") <> Неопределено Тогда
			Подробности = Детали.internalFault;
		ИначеЕсли Детали.Свойства().Получить("digitalSignFault") <> Неопределено Тогда
			Подробности = Детали.digitalSignFault;
		ИначеЕсли Детали.Свойства().Получить("decryptFault") <> Неопределено Тогда
			Подробности = Детали.decryptFault;
		ИначеЕсли Детали.Свойства().Получить("tmsFault") <> Неопределено Тогда
			Подробности = Детали.tmsFault;
		КонецЕсли;
		Если Подробности <> Неопределено Тогда
			Если Подробности.Свойства().Получить("code") <> Неопределено Тогда
				Результат.ДеталиКод = Подробности.code;
			КонецЕсли;
			Если Подробности.Свойства().Получить("message") <> Неопределено Тогда
				Результат.ДеталиОписание = Подробности.message;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ПустаяСтрока(Результат.ДеталиОписание) ИЛИ ОписаниеОшибки = Результат.ДеталиОписание Тогда
		Шаблон = НСтр("ru='Ответ СЭДО: ""%1""'");
		Результат.ОписаниеОшибки = СтрШаблон(Шаблон, ОписаниеОшибки);
	Иначе
		Шаблон = НСтр("ru='Ответ СЭДО: ""%1: %2""'");
		Результат.ОписаниеОшибки = СтрШаблон(Шаблон, ОписаниеОшибки, Результат.ДеталиОписание);
	КонецЕсли;
	
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьОтветСЭДОФСССИдентификатором(СодержимоеОтвета)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("Идентификатор", Неопределено);
	
	// Пример ответа сервера:
	//<soap:Body wsu:Id="OGRN_1027739443236">
	//    <ns2:sedoUuidResponse xmlns:ns2="http://www.fss.ru/integration/types/sedo/v01"
	//        xmlns:ns3="http://www.fss.ru/integration/types/fault/v01">
	//        <result>
	//            <uuid>40a1a9d6-529e-45c8-ad04-c802fb3e1e97</uuid>
	//        </result>
	//    </ns2:sedoUuidResponse>
	//</soap:Body>
	
	Чтение = Новый ЧтениеXML;
	Попытка
		Чтение.УстановитьСтроку(СодержимоеОтвета);
		ДанныеОтвета = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Исключение
		Результат.ОписаниеОшибки = НСтр("ru='Ошибка разбора ответа сервера СЭДО.'");
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ПодробноеПредставлениеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
		ТекстОшибки = НСтр("ru='Ошибка разбора ответа сервера СЭДО:'") 
			+ Символы.ПС + ПодробноеПредставлениеОшибки
			+ Символы.ПС + НСтр("ru='Содержимое ответа:'") + Символы.ПС + СодержимоеОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Отправка сообщения СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
		Возврат Результат;
	КонецПопытки;
	
	УзелДанных = ДанныеОтвета.Body.sedoUuidResponse;
	
	Если УзелДанных.Свойства().Получить("errorList") <> Неопределено Тогда
		Ошибки = Новый Массив;
		Для каждого Ошибка Из ЗначениеВКоллекцию(УзелДанных.errorList.message) Цикл
			Ошибки.Добавить(Ошибка);
		КонецЦикла;
		Результат.ОписаниеОшибки = СтрСоединить(Ошибки, Символы.ПС);
		Возврат Результат;
	КонецЕсли;
	
	Результат.Идентификатор = УзелДанных.result.uuid;
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИзвлечьТекстОшибкиИзТелаОтветаСЭДО(ТелоОтвета)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ИзвлечьТекстОшибкиИзТелаОтветаСЭДО(ТелоОтвета).ТекстОшибки;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьОтветПолучитьСообщениеСЭДОФСС(СодержимоеОтвета, ДатаСообщения = Неопределено, ИдентификаторЗапроса = "")
	
	ДанныеСообщения = ЭлектронныйДокументооборотСФССКлиентСервер.НовыеДанныеСообщенияСЭДО();
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("Ошибки", Новый Массив);
	Результат.Вставить("ДанныеСообщения", ДанныеСообщения);
	Результат.Вставить("КонвертSOAP", СодержимоеОтвета);
	Результат.Вставить("КодыОшибок", Новый Массив);
	Результат.Вставить("СообщениеНеНайдено", Ложь);
	
	Чтение = Новый ЧтениеXML;
	Попытка
		Чтение.УстановитьСтроку(СодержимоеОтвета);
		ДанныеОтвета = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Исключение
		Чтение.Закрыть();
		ОписаниеОшибки = НСтр("ru='Ошибка разбора ответа сервера с данными сообщения СЭДО.'");
		Результат.Ошибки.Добавить(ОписаниеОшибки);
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ПодробноеПредставлениеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
		ТекстОшибки = НСтр("ru='Ошибка разбора ответа сервера СЭДО:'") 
			+ Символы.ПС + ПодробноеПредставлениеОшибки
			+ Символы.ПС + НСтр("ru='Содержимое ответа:'") + Символы.ПС + СодержимоеОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
		Возврат Результат;
	КонецПопытки;
	Чтение.Закрыть();
	
	// Пример ответа сервера.
	//<soap:Body wsu:Id="OGRN_1027739443236">
	//	<ns2:sedoMessageResponse xmlns:ns2="http://www.fss.ru/integration/types/sedo/v01" xmlns:ns3="http://www.fss.ru/integration/types/fault/v01">
	//		<result>
	//			<expiryDate>2020-10-10+03:00</expiryDate>
	//			<uuid>d675d4bf-abc6-49cd-ac97-5d8e7b9ff360</uuid>
	//			<messageType>5</messageType>
	//			<recipientId>7423022153</recipientId>
	//			<regNumSfr>1007665854</regNumSfr>
	//			<message>UEd4dVUzUmhkR1ZEYUdGdVoyVStQR3h1WDJOdlpHVStPVEF3TURBd01EQXdNREExUEM5c2JsOWpiMlJsUGp4emJtbHNjejR3TURBd01EQTJNREF3TlR3dmMyNXBiSE0rUEd4dVgzTjBZWFJsUGpBek1Ed3ZiRzVmYzNSaGRHVStQQzlzYmxOMFlYUmxRMmhoYm1kbFBnPT0=</message>
	//			<needConfirm>false</needConfirm>
	//		</result>
	//	</ns2:sedoMessageResponse>
	//</soap:Body>
	// Или при типе взаимодействия 3 "Организация" (МЧД, для ИП в узле "org" передаются узлы "ogrnip", "inn", "snils"):
	//<soap:Body wsu:Id="OGRN_1027739443236">
	//	<ns2:sedoMessageResponse xmlns:ns2="http://www.fss.ru/integration/types/sedo/v01" xmlns:ns3="http://www.fss.ru/integration/types/fault/v01">
	//		<result>
	//			<uuid>48189069-027a-4a45-ad41-b495b0048964</uuid>
	//			<messageType>211</messageType>
	//			<org>
	//				inn>3123415674</inn>
	//				<ogrn>1173123022313</ogrn>
	//				<kpp>312301001</kpp>
	//			</org>
	//			<message>UEd4dVUzUmhkR1ZEYUdGdVoyVStQR3h1WDJOdlpHVStPVEF3TURBd01EQXdNREExUEM5c2JsOWpiMlJsUGp4emJtbHNjejR3TURBd01EQTJNREF3TlR3dmMyNXBiSE0rUEd4dVgzTjBZWFJsUGpBek1Ed3ZiRzVmYzNSaGRHVStQQzlzYmxOMFlYUmxRMmhoYm1kbFBnPT0=</message>
	//			<needConfirm>false</needConfirm>
	//		</result>
	//	</ns2:sedoMessageResponse>
	//</soap:Body>
	
	// Сообщение с ошибкой.
	// <soap:Envelope xmlns:ds="http://www.w3.org/2000/09/xmldsig#"
	// xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/"
	// xmlns:wsse="http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-secext-1.0.xsd"
	// xmlns:wsu="http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd">
	//	<soap:Header>
	//	</soap:Header>
	//	<soap:Body wsu:Id="OGRN_1027739443236">
	//		<ns24:sedoMessageResponse xmlns:ns10="http://www.fss.ru/integration/types/vsr/v01"
	//			xmlns:ns11="http://www.fss.ru/integration/types/commonUq/v01" xmlns:ns12="http://www.fss.ru/integration/types/uv/pattern/v01"
	//			xmlns:ns13="http://www.fss.ru/integration/types/kp/docrequire/v01" xmlns:ns14="http://www.fss.ru/integration/types/uv/claimrefund/v01"
	//			xmlns:ns15="http://www.fss.ru/integration/types/rvz/docrequire/v01" xmlns:ns16="http://www.fss.ru/integration/types/person/v01"
	//			xmlns:ns17="http://www.fss.ru/integration/types/eln/event/v01" xmlns:ns18="http://www.fss.ru/integration/types/eln/v01"
	//			xmlns:ns19="http://www.fss.ru/integration/types/eln/mse/v01" xmlns:ns2="http://www.fss.ru/integration/types/common/v01"
	//			xmlns:ns20="http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd"
	//			xmlns:ns21="http://ws.fss.ru/integration/types/rust/poved/v01" xmlns:ns22="http://www.fss.ru/integration/types/tms/v01"
	//			xmlns:ns23="http://www.fss.ru/integration/types/pvso/notice/v01" xmlns:ns24="http://www.fss.ru/integration/types/sedo/v01"
	//			xmlns:ns25="http://www.fss.ru/integration/types/lk/insurer/v01" xmlns:ns26="http://www.fss.ru/integration/types/sedo/arm/v01"
	//			xmlns:ns3="http://www.fss.ru/integration/types/fault/v01" xmlns:ns4="http://fz122.fss.ru" xmlns:ns5="http://www.fss.ru/integration/types/wsClient/v01"
	//			xmlns:ns6="http://www.fss.ru/integration/types/prvn/fpm/v01" xmlns:ns7="http://www.fss.ru/integration/types/fssDepartment/v01"
	//			xmlns:ns8="http://www.fss.ru/integration/types/identityDocument/v01" xmlns:ns9="http://www.fss.ru/integration/types/f4/v01">
	//			<errorList>
	//				<ns2:code>E_SEDO_3000</ns2:code>
	//				<ns2:message>Внутренняя ошибка</ns2:message>
	//			</errorList>
	//		</ns24:sedoMessageResponse>
	//	</soap:Body>
	// </soap:Envelope>
	//
	// Пример ошибки без узла sedoMessageResponse.
	// <soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//	<soap:Body>
	//		<soap:Fault>
	//			<faultcode>soap:Server</faultcode>
	//			<faultstring>java.lang.IllegalStateException: Error checking EDS</faultstring>
	//			<detail>
	//				<digitalSignFault xmlns="http://www.fss.ru/integration/types/fault/v01">
	//					<uuid>39c87e32-ec65-406a-b3cb-bbf9b526cc48</uuid>
	//					<code>E_SEDO_1020</code>
	//					<message>Ошибка при попытке проверки ЭЦП. 7Ошибка при проверке сертификата.VALID_SIGNATURE ЭП действительна; ERROR_BUILDING_CERT_PATH При проверке сертификата ЭП произошла ошибка. Ошибка построение цепочки сертификатов  java.security.cert.CertificateExpiredException: NotAfter: Wed Sep 13 15:17:41 MSK 2023</message>
	//				</digitalSignFault>
	//			</detail>
	//		</soap:Fault>
	//	</soap:Body>
	// </soap:Envelope>
	//
	// Пример ответа сервера оператора (ответ всегда пакетный, даже при обычном запросе).
	//	<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//	<soap:Body xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//		<v01:sedoMessageResponse xmlns:v01="http://www.fss.ru/integration/types/sedo/v01">
	//			<packageResult>
	//				<sedoMessageList>
	//					<sedoMessage>
	//						<uuid>1882a16b-e21b-4817-8aef-446bee0ab92e</uuid>
	//						<messageType>111</messageType>
	//						<recipientId>3101011870</recipientId>
	//						<needConfirm>false</needConfirm>
	//						<expiryDate>2099-01-01</expiryDate>
	//						<message>PD94bWwgdmVyc..</message>
	//					</sedoMessage>
	//				</sedoMessageList>
	//				<errorList/>
	//			</packageResult>
	//		</v01:sedoMessageResponse>
	//	</soap:Body>
	//</soap:Envelope>
	// 
	// Сообщение с ошибкой от оператора (ответ всегда пакетный, даже при обычном запросе)
	// <soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//	<soap:Body xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//		<v01:sedoMessageResponse xmlns:v01="http://www.fss.ru/integration/types/sedo/v01">
	//			<packageResult>
	//				<sedoMessageList/>
	//				<errorList>
	//					<errorPackage>
	//						<uuid>1976dea6-790c-458d-8658-7b338bd8ecfd</uuid>
	//						<error>
	//							<com:code xmlns:com="http://www.fss.ru/integration/types/common/v01">E_SEDO_3010</com:code>
	//							<com:message xmlns:com="http://www.fss.ru/integration/types/common/v01">Указанное сообщение не найдено</com:message>
	//						</error>
	//					</errorPackage>
	//				</errorList>
	//			</packageResult>
	//		</v01:sedoMessageResponse>
	//	</soap:Body>
	//</soap:Envelope>
	
	ЕстьУзелСообщения = ЕстьСвойство(ДанныеОтвета.Body, "sedoMessageResponse");
	Если НЕ ЕстьУзелСообщения Тогда
		// Определяем тело сообщения для записи в журнал.
		ТелоОтвета = ИзвлечьТелоОтветаИзКонтейнераСЭДО(СодержимоеОтвета);
		ТекстОшибки = НСтр("ru='С сервера СЭДО СФР получен ответ с ошибками:'") + Символы.ПС + ТелоОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
		Ошибка = ИзвлечьТекстОшибкиИзТелаОтветаСЭДО(ДанныеОтвета.Body);
		Результат.Ошибки.Добавить(Ошибка);
		Возврат Результат;
	КонецЕсли;
	
	УзелДанных = ДанныеОтвета.Body.sedoMessageResponse;
	УзлыОшибокИлиОшибокПакета = Неопределено;
	Если УзелДанных.Свойства().Получить("errorList") = Неопределено Тогда
		Если УзелДанных.Свойства().Получить("packageResult") <> Неопределено Тогда
			УзелПакета = УзелДанных.packageResult;
			Если УзелПакета.Свойства().Получить("errorList") <> Неопределено Тогда
				УзелОшибок = УзелПакета.errorList;
				Если УзелОшибок.Свойства().Получить("errorPackage") <> Неопределено Тогда
					УзлыОшибокИлиОшибокПакета = УзелОшибок.errorPackage;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		УзлыОшибокИлиОшибокПакета = УзелДанных.errorList;
	КонецЕсли;
	
	КодОшибкиСообщениеНеНайдено = "E_SEDO_3010";
	Если УзлыОшибокИлиОшибокПакета <> Неопределено Тогда
		
		// Возвращен список ошибок.
		Для каждого УзелОшибокИлиОшибокПакета Из ЗначениеВКоллекцию(УзлыОшибокИлиОшибокПакета) Цикл
			Если УзелОшибокИлиОшибокПакета.Свойства().Получить("error") = Неопределено Тогда
				УзелОшибок = УзелОшибокИлиОшибокПакета;
			Иначе
				УзелОшибок = УзелОшибокИлиОшибокПакета.error;
			КонецЕсли;
			
			Если УзелОшибок.Свойства().Получить("message") <> Неопределено Тогда
				Для каждого Ошибка Из ЗначениеВКоллекцию(УзелОшибок.message) Цикл
					Результат.Ошибки.Добавить(Ошибка);
				КонецЦикла;
			КонецЕсли;
			Если ЕстьСвойство(УзелОшибок, "code") Тогда
				Для каждого Код Из ЗначениеВКоллекцию(УзелОшибок.code) Цикл
					Результат.КодыОшибок.Добавить(Код);
					Если Код = КодОшибкиСообщениеНеНайдено Тогда
						Результат.СообщениеНеНайдено = Истина;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
		
		ТелоОтвета = ИзвлечьТелоОтветаИзКонтейнераСЭДО(СодержимоеОтвета);
		ТекстОшибки = НСтр("ru='С сервера СЭДО СФР получен ответ с ошибками:'") + Символы.ПС + ТелоОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
		
		Возврат Результат;
		
	КонецЕсли;
	
	Если УзелДанных.Свойства().Получить("result") = Неопределено Тогда
		УзелДанныеСообщения = УзелДанных.packageResult.sedoMessageList.sedoMessage;
	Иначе
		УзелДанныеСообщения = УзелДанных.result;
	КонецЕсли;
	
	РазобратьУзелСДаннымиСообщенияСЭДО(УзелДанныеСообщения, ДанныеСообщения, ДатаСообщения, ИдентификаторЗапроса);
	
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьУзелСДаннымиСообщенияСЭДО(УзелДанныеСообщения, ДанныеСообщения, ДатаСообщения, ИдентификаторЗапроса = "")

	ДанныеСообщения.Идентификатор = УзелДанныеСообщения.uuid;
	ДанныеСообщения.Тип = УзелДанныеСообщения.messageType;
	ЭлектронныйДокументооборотСФСС.ЗаполнитьОписаниеТипаВДанныхСообщенияСЭДО(ДанныеСообщения);
	
	Если УзелДанныеСообщения.Свойства().Получить("recipientId") = Неопределено
		И УзелДанныеСообщения.Свойства().Получить("org") <> Неопределено Тогда
		Если УзелДанныеСообщения.org.Свойства().Получить("ogrnip") = Неопределено Тогда
			ДанныеСообщения.Получатель = ?(УзелДанныеСообщения.org.Свойства().Получить("ogrn") = Неопределено,
				"", УзелДанныеСообщения.org.ogrn);
		Иначе
			ДанныеСообщения.Получатель = УзелДанныеСообщения.org.ogrnip;
		КонецЕсли;
		ДанныеСообщения.ИНН = УзелДанныеСообщения.org.inn;
		Если УзелДанныеСообщения.org.Свойства().Получить("kpp") <> Неопределено Тогда
			ДанныеСообщения.КПП = УзелДанныеСообщения.org.kpp;
		КонецЕсли;
		Если УзелДанныеСообщения.org.Свойства().Получить("snils") <> Неопределено Тогда
			ДанныеСообщения.СНИЛС = ?(ТипЗнч(УзелДанныеСообщения.org.snils) = Тип("ОбъектXDTO"),
				"", УзелДанныеСообщения.org.snils);
		КонецЕсли;
	ИначеЕсли УзелДанныеСообщения.Свойства().Получить("recipientId") <> Неопределено Тогда
		ДанныеСообщения.Получатель = УзелДанныеСообщения.recipientId;
	КонецЕсли;
	Если ЕстьСвойство(УзелДанныеСообщения, "regNumSfr") Тогда
		ДанныеСообщения.РегистрационныйНомерСФР = УзелДанныеСообщения.regNumSfr;
	КонецЕсли;
	ДанныеСообщения.ТребуетсяПодтверждение = ЗначениеНеобязательногоСвойства(УзелДанныеСообщения, "needConfirm", "Булево", Ложь);
	Если ЕстьСвойство(УзелДанныеСообщения, "expiryDate") Тогда
		ДанныеСообщения.СрокДействия = XMLЗначение(Тип("Дата"), УзелДанныеСообщения.expiryDate);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДатаСообщения) Тогда
		ДанныеСообщения.Дата = ДатаСообщения;
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ИдентификаторЗапроса) Тогда
		ДанныеСообщения.ИдентификаторЗапроса = ИдентификаторЗапроса;
	КонецЕсли;
	
	СообщениеBase64 = УзелДанныеСообщения.message;
	Данные = Base64Значение(СообщениеBase64);
	
	Чтение = Новый ЧтениеДанных(Данные);
	ТекстСообщения = Чтение.ПрочитатьСимволы();
	Чтение.Закрыть();
	
	// В некоторых случаях текст сообщения приходит без кодирования в Base64.
	Если ПустаяСтрока(ТекстСообщения) Тогда
		ТекстСообщения = СообщениеBase64;
	КонецЕсли;
	
	ДанныеСообщения.ТекстСообщения = ТекстСообщения;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИзвлечьТелоОтветаИзКонтейнераСЭДО(СодержимоеОтвета)
	
	Открытие = "<soap:Body";
	Закрытие = "</soap:Body>";
	ТелоОтвета = "";
	Если СтрНайти(СодержимоеОтвета, Открытие) > 0 Тогда
		Начало = СтрНайти(СодержимоеОтвета, Открытие);
		Окончание = СтрНайти(СодержимоеОтвета, Закрытие) + СтрДлина(Закрытие);
		Длина = Окончание - Начало;
		ТелоОтвета = Сред(СодержимоеОтвета, Начало, Длина);
	Иначе
		ТелоОтвета = СодержимоеОтвета;
	КонецЕсли;
	
	Возврат ТелоОтвета;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьОтветВходящиеСообщенияСЭДОФСС(СодержимоеОтвета, ДатаСообщений, СвойстваОбменаПоСЭДОЧерезОператора = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.РазобратьОтветВходящиеСообщенияСЭДОФСС(СодержимоеОтвета, ДатаСообщений, , СвойстваОбменаПоСЭДОЧерезОператора);
	
КонецФункции

&НаСервереБезКонтекста
Функция ТребованияСФР(Организация) Экспорт
	
	Запрос = Новый Запрос;
	
	Если ЗначениеЗаполнено(Организация) Тогда
		Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ВходящийДокументСЭДОФСС.Ссылка КАК Требование
		|ИЗ
		|	Документ.ВходящийДокументСЭДОФСС КАК ВходящийДокументСЭДОФСС
		|ГДЕ
		|	ВходящийДокументСЭДОФСС.Организация = &Организация
		|	И (ВходящийДокументСЭДОФСС.ТипСообщения = ""303""
		|			ИЛИ ВходящийДокументСЭДОФСС.ТипСообщения = ""308"")";
	Иначе
		Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ВходящийДокументСЭДОФСС.Ссылка КАК Ссылка
		|ИЗ
		|	Документ.ВходящийДокументСЭДОФСС КАК ВходящийДокументСЭДОФСС
		|ГДЕ
		|	(ВходящийДокументСЭДОФСС.ТипСообщения = ""303""
		|			ИЛИ ВходящийДокументСЭДОФСС.ТипСообщения = ""308"")";
	КонецЕсли;
	
	Запрос.УстановитьПараметр("Организация", Организация);
	
	Требования = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Требование");
	
	Возврат Требования;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьФормуВыбораТребованияСФР(
		ОписаниеОповещения, 
		Организация = Неопределено, 
		Знач ВидДокумента = Неопределено,
		Требования = Неопределено) Экспорт
	
	ВидыДокумента = Новый Массив;
	Если ВидДокумента = Неопределено Тогда
		ВидыДокумента.Добавить("303");
		ВидыДокумента.Добавить("308");
	Иначе
		ВидыДокумента.Добавить(ВидДокумента);
	Конецесли;	
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВидыДокумента", 	ВидыДокумента);
	ДополнительныеПараметры.Вставить("Организация", 	Организация);
	ДополнительныеПараметры.Вставить("Требования", 		Требования);
	
	ОткрытьФорму("Документ.ВходящийДокументСЭДОФСС.Форма.ФормаВыбораТребования", 
		ДополнительныеПараметры,,,,,ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры
	
&НаКлиенте
Процедура СоздатьОтветНаТребованиеСФРСВыборомТребования(
		СписокВходящихДокументов = Неопределено, // Если указано, то список требований берется из описи
		Организация 			= Неопределено, // Если указана, то в список фильтруется по ней
		ВходящийКонтекст 		= Неопределено) Экспорт
	
	// Если требование одно, то форму выбора не показываем
	Если СписокВходящихДокументов = Неопределено Тогда
		Требования = Неопределено;
	Иначе
		Требования = ТребованияСФР(Организация);
		Если Требования.Количество() = 1 Тогда
			СоздатьОтветНаТребованиеСФР(Требования[0], ВходящийКонтекст);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СоздатьОтветНаТребованиеСФР",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	ПоказатьФормуВыбораТребованияСФР(ОписаниеОповещения, Организация, , Требования);
	
КонецПроцедуры
	
&НаКлиенте
Процедура СоздатьОтветНаТребованиеСФР(Требование, Знач ДополнительныеПараметры = Неопределено) Экспорт
	
	ВидТребования = Неопределено;
	Если Требование <> Неопределено Тогда
		
		СоздатьОтветНаТребованиеДокументовСФР(Требование);
		
	КонецЕсли;
	
	Если ДополнительныеПараметры <> Неопределено
		И ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ВыполняемоеОповещение")
		И ДополнительныеПараметры.ВыполняемоеОповещение <> Неопределено Тогда
		
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, ВидТребования);
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура СоздатьОтветНаТребованиеДокументовСФР(Требование)
	
	СформированныйОтвет = СфомироватьОтветНаСервере(Требование);
	
	Если СформированныйОтвет =
			ПредопределенноеЗначение("Документ.ОтветСтрахователяНаТребованиеПроверкиФСС.ПустаяСсылка")
			ИЛИ СформированныйОтвет = Неопределено Тогда
			
			Возврат;
	КонецЕсли;
		
	ПараметрыФормы = Новый Структура("Ключ", СформированныйОтвет);
	ОткрытьФорму(
		"Документ.ОтветСтрахователяНаТребованиеПроверкиФСС.Форма.ФормаДокумента",
		ПараметрыФормы);
		
	Оповестить("Создан ответ страхователя СФР");
	
КонецПроцедуры
	
&НаСервереБезКонтекста
Функция СфомироватьОтветНаСервере(Требование)
	
	Результат = Неопределено;
	
	Попытка
		Ответ = Документы.ОтветСтрахователяНаТребованиеПроверкиФСС.СоздатьДокумент();
		
		Ответ.Дата 		= ТекущаяДатаСеанса();
		Ответ.Основание	= Требование;
		
		Ответ.УстановитьВремя();
		Ответ.УстановитьНовыйНомер();
		
		Ответ.Заполнить(Требование);
		
		Ответ.Записать();
		
		Результат = Ответ.Ссылка;
	Исключение
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Невозможно сформировать ответ.'"));
	КонецПопытки;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПроверитьДлинуРегистрационныйНомерФСС(Результат, ТипВзаимодействия, Организация)
	
	ЭтоТипВзаимодействияОрганизацияСЭДО = (ТипВзаимодействия =
		ДокументооборотСФССКлиентСервер.ТипВзаимодействияОрганизацияСЭДО());
	ПолучитьОГРН 			= ЭтоТипВзаимодействияОрганизацияСЭДО;
	ПолучитьИННКППиСНИЛС 	= ЭтоТипВзаимодействияОрганизацияСЭДО;
	СвойстваОрганизации		= СвойстваОрганизацииДляОбмена(
		Организация,,
		ПолучитьОГРН,,
		ПолучитьИННКППиСНИЛС);
	РегистрационныйНомерФСС = СвойстваОрганизации.РегистрационныйНомерФСС;
	РезультатПроверкиРегистрационногоНомераФСС = 
		ЭлектронныйДокументооборотСФССКлиентСервер.ПроверитьДлинуРегистрационныйНомерФСС(
			СвойстваОрганизации,
			РегистрационныйНомерФСС,
			Ложь,
			Результат);
			
	Возврат РезультатПроверкиРегистрационногоНомераФСС;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ЗаполнитьПараметрыОтправкиПоОрганизации(ПараметрыОтправкаСообщения, ОрганизацияСсылка)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Если КонтекстЭДОСервер = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ДатыСообщений = ПараметрыОтправкаСообщения.ДатыСообщений;

	ПараметрыОтправкаСообщения.ТипСообщения = "330";
	ПараметрыОтправкаСообщения.Организация = ОрганизацияСсылка;
	ПараметрыОтправкаСообщения.СодержимоеСообщения =
		КонтекстЭДОСервер.СформироватьЗапросИсторическихСообщенийЗаДатуАбонентом(
			ОрганизацияСсылка,
			ДатыСообщений);
	
КонецПроцедуры

#КонецОбласти

#Область СодержимоеСообщений

&НаКлиенте
Функция СодержимоеСообщенияПодтверждениеПрочтенияСообщений(ИдентификаторыСообщений) Экспорт

	Шаблон = "<ns4:confirmMessageReadRequest xmlns:ns4=""http://www.fss.ru/integration/types/sedo/arm/v01""
		|xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""
		|xmlns:ns=""http://www.fss.ru/integration/types/common/v01""
		|xmlns:ns1=""http://www.fss.ru/integration/types/sedo/v01""
		|xmlns:ns2=""http://www.fss.ru/integration/types/organization/v01""
		|xmlns:ns3=""http://www.fss.ru/integration/types/dic/errors/v01"">
		|	<!--(uuid*)-->
		|	<uuids>
		|%1
		|	</uuids>
		|</ns4:confirmMessageReadRequest>";

	Строки = Новый Массив;
	Для каждого Идентификатор Из ИдентификаторыСообщений Цикл
		ШаблонИД = "		<uuid>%1</uuid>";
		ЭлементИД = СтрШаблон(ШаблонИД, Идентификатор);
		Строки.Добавить(ЭлементИД);
	КонецЦикла;
	Идентификаторы = СтрСоединить(Строки, Символы.ПС);
	
	Результат = СтрШаблон(Шаблон, Идентификаторы);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СодержимоеСообщенияОтписыванияСтрахователяОтОповещенийОбИзмененииСтатусаЭЛНВМО(РегистрационныйНомерФСС) Экспорт
	
	Шаблон = 
	"<unsubscribeInsRequest xmlns=""http://www.fss.ru/integration/types/eln/event/v01"">
	|	<event>CHANGE_STATE_MO</event>
	|	<regNum>%1</regNum>
	|</unsubscribeInsRequest>";
	
	Результат = СтрШаблон(Шаблон, РегистрационныйНомерФСС);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СодержимоеСообщенияПодпискиСтрахователяНаОповещенияОбИзмененииСтатусаЭЛНВМО(РегистрационныйНомерФСС) Экспорт
	
	Шаблон = 
	"<subscribeInsRequest xmlns=""http://www.fss.ru/integration/types/eln/event/v01"">
	|	<event>CHANGE_STATE_MO</event>
	|	<regNum>%1</regNum>
	|</subscribeInsRequest>";
	
	Результат = СтрШаблон(Шаблон, РегистрационныйНомерФСС);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СодержимоеСообщенияПодпискиРаботниковНаУведомлениеСтрахователяОбИзмененииСтатусаЭЛНВМО(
		РегистрационныйНомерФСС,
		СписокСНИЛС,
		ТипСобытия) Экспорт
	
	Шаблон =
	"<subscribeRecipientRequest xmlns=""http://www.fss.ru/integration/types/eln/event/v01"">
	|	<regNum>%1</regNum>
	|	<agreementReceived>true</agreementReceived>
	|	<commitExclude>true</commitExclude>
	|	<event>%2</event>
	|	<snilsList>";
	Результат = СтрШаблон(Шаблон, РегистрационныйНомерФСС, ТипСобытия);
	
	Шаблон = "
		|		<snils force=""%1"">%2</snils>";
	Для каждого Элемент Из СписокСНИЛС Цикл
		ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(
			"СодержимоеСообщенияПодпискиРаботниковНаУведомлениеСтрахователяОбИзмененииСтатусаЭЛНВМО",
			"ЭлементСпискаСНИЛС",
			Элемент,
			Тип("Структура"));
		СНИЛС = ОставитьТолькоЦифрыВСНИЛС(Элемент.СНИЛС);
		Результат = Результат + СтрШаблон(Шаблон, XMLБулево(Элемент.ПринудительноОткрепить), СНИЛС);
	КонецЦикла; 
	
	Результат = Результат + "	</snilsList>
	|</subscribeRecipientRequest>";
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СодержимоеСообщенияОтпискиРаботниковНаУведомлениеСтрахователяОбИзмененииСтатусаЭЛНВМО(
		РегистрационныйНомерФСС,
		СписокСнилс,
		ТипСобытия) Экспорт
	
	Шаблон =
	"<unsubscribeRecipientRequest xmlns=""http://www.fss.ru/integration/types/eln/event/v01"">
	|	<regNum>%1</regNum>
	|	<agreementReceived>true</agreementReceived>
	|	<commitExclude>true</commitExclude>
	|	<event>%2</event>
	|	<snilsList>";
	Результат = СтрШаблон(Шаблон, РегистрационныйНомерФСС, ТипСобытия);
	
	Шаблон = "
		|		<snils>%1</snils>";
	Для каждого СНИЛС Из СписокСнилс Цикл
		ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(
			"СодержимоеСообщенияПодпискиРаботниковНаУведомлениеСтрахователяОбИзмененииСтатусаЭЛНВМО",
			"ЭлементСпискаСНИЛС",
			СНИЛС,
			Тип("Строка"));
		СНИЛС = ОставитьТолькоЦифрыВСНИЛС(СНИЛС);
		Результат = Результат + СтрШаблон(Шаблон, СНИЛС);
	КонецЦикла; 
	
	Результат = Результат + "	</snilsList>
	|</unsubscribeRecipientRequest>";
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция ДвоичныеДанныеИзТекстаНаСервере(Текст, Кодировка = Неопределено)
	
	Поток = Новый ПотокВПамяти;
	ЗаписьДанных = Новый ЗаписьДанных(Поток, Кодировка);
	ЗаписьДанных.ЗаписатьСимволы(Текст);
	ЗаписьДанных.Закрыть();
	Данные = Поток.ЗакрытьИПолучитьДвоичныеДанные();
	
	Возврат Данные;
	
КонецФункции

&НаКлиенте
Функция ТекстЗапросаПолученияСообщенияСЭДОФСС(
		РегистрационныйНомерФСС,
		ИдентификаторСообщения,
		Настройки = Неопределено) Экспорт
	
	НастройкиВызова = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Если Настройки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, Настройки);
	КонецЕсли;
	
	Если НастройкиВызова.ТипВзаимодействия = Неопределено Тогда
		НастройкиВызова.ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	ИнформацияОПрограмме = ИнформацияОПрограмме();
	
	ТекстПараметровТипаВзаимодействия = ДокументооборотСФССКлиентСервер.ТекстПараметровТипаВзаимодействияСЭДО(
		РегистрационныйНомерФСС, НастройкиВызова);
	
	Если НастройкиВызова.ОбменПоСЭДОНапрямую Тогда
		Шаблон = "
			|   <soapenv:Body xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"""
			+ " xmlns:v01=""http://www.fss.ru/integration/types/sedo/v01"""
			+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd"""
			+ " wsu:Id=""REGNO_%2"">
			|      <v01:getMessageRequest>
			|         <systemInfo>
			|            <specVersion>1.0</specVersion>
			|            <software>%4</software>
			|            <softwareVersion>%5</softwareVersion>
			|         </systemInfo>
			|         <interactionType>%1</interactionType>
			|         <uuid>%3</uuid>%6
			|      </v01:getMessageRequest>
			|   </soapenv:Body>";
		
	Иначе
		Шаблон = "
			|   <soapenv:Body xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"""
			+ " xmlns:v01=""http://www.fss.ru/integration/types/sedo/v01"""
			+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd"""
			+ " wsu:Id=""REGNO_%2"">
			|      <v01:getMessageRequest>
			|         <systemInfo>
			|            <specVersion>1.0</specVersion>
			|            <software>%4</software>
			|            <softwareVersion>%5</softwareVersion>
			|         </systemInfo>
			|         <interactionType>%1</interactionType>
			|         <uuidList>
			|            <uuid>%3</uuid>
			|         </uuidList>%6
			|      </v01:getMessageRequest>
			|   </soapenv:Body>";
	КонецЕсли;
	
	Текст = СтрШаблон(Шаблон,
		НастройкиВызова.ТипВзаимодействия,
		РегистрационныйНомерФСС,
		ИдентификаторСообщения,
		ИнформацияОПрограмме.ИДКонфигурации,
		ИнформацияОПрограмме.ВерсияКонфигурации,
		ТекстПараметровТипаВзаимодействия);
	
	Возврат Текст;
	
КонецФункции

&НаКлиенте
Функция ТекстЗапросаОтправкиСообщенияСЭДОФСС(
		РегистрационныйНомерФСС,
		ТипСообщения,
		ТекстСообщения = Неопределено,
		Настройки = Неопределено) Экспорт
	
	НастройкиВызова = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Если Настройки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, Настройки);
	КонецЕсли;
	
	Если НастройкиВызова.ТипВзаимодействия = Неопределено Тогда
		НастройкиВызова.ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСЭДО(ТипСообщения);
	КонецЕсли;
	ИнформацияОПрограмме = ИнформацияОПрограмме();
	
	Если ТекстСообщения = Неопределено Тогда
		СообщениеBase64 = "%Message%";
	Иначе
		Данные = ДвоичныеДанныеИзТекстаНаСервере(ТекстСообщения);
		СообщениеBase64 = Base64Строка(Данные);
		Если СтрДлина(СообщениеBase64) <= 1 Тогда
			ТекстОшибки = НСтр("ru='Ошибка при кодирование текста сообщения СЭДО.'");
			Шаблон = ТекстОшибки + " " + НСтр("ru='Результат кодирования: %1. Текст сообщения:'")
				+ Символы.ПС + "%2";
			ТекстОшибкиЖурнал = СтрШаблон(Шаблон, СообщениеBase64, ТекстСообщения);
			ЗаписатьСобытиеСЭДОВЖурнал("Ошибка", ТекстОшибкиЖурнал);
			ТекстОшибки = ТекстОшибки + " " + НСтр("ru='См. подробности в журнале регистрации.'");
			ВызватьИсключение(ТекстОшибки);
		КонецЕсли;
	КонецЕсли;
	
	ТекстПараметровТипаВзаимодействия = ДокументооборотСФССКлиентСервер.ТекстПараметровТипаВзаимодействияСЭДО(
		РегистрационныйНомерФСС, НастройкиВызова);
	
	Если НастройкиВызова.ОбменПоСЭДОНапрямую Тогда
		Шаблон = "
			|   <soapenv:Body xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/""
			|      xmlns:v01=""http://www.fss.ru/integration/types/sedo/v01""
			|      xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""
			|      wsu:Id=""REGNO_%4"">
			|      <v01:putMessageRequest>
			|         <systemInfo>
			|            <specVersion>1.0</specVersion>
			|            <software>%5</software>
			|            <softwareVersion>%6</softwareVersion>
			|         </systemInfo>
			|         <interactionType>%1</interactionType>%7
			|         <messageType>%3</messageType>
			|         <message>%2</message>
			|      </v01:putMessageRequest>
			|   </soapenv:Body>";
		
		Текст = СтрШаблон(Шаблон,
			НастройкиВызова.ТипВзаимодействия,
			СообщениеBase64,
			ТипСообщения,
			РегистрационныйНомерФСС,
			ИнформацияОПрограмме.ИДКонфигурации,
			ИнформацияОПрограмме.ВерсияКонфигурации,
			ТекстПараметровТипаВзаимодействия);
		
	Иначе
		Шаблон = "
			|   <soapenv:Body xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/""
			|      xmlns:v01=""http://www.fss.ru/integration/types/sedo/v01""
			|      xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""
			|      wsu:Id=""REGNO_%4"">
			|      <v01:putMessageRequest>
			|         <systemInfo>
			|            <specVersion>1.0</specVersion>
			|            <software>%5</software>
			|            <softwareVersion>%6</softwareVersion>
			|         </systemInfo>
			|         <interactionType>%1</interactionType>%7
			|         <messageType>%3</messageType>
			|         <messagePackageList>
			|            <messagePackage>
			|               <uuid>%8</uuid>
			|               <message>%2</message>
			|            </messagePackage>
			|         </messagePackageList>
			|      </v01:putMessageRequest>
			|   </soapenv:Body>";
		
		Текст = СтрШаблон(Шаблон,
			НастройкиВызова.ТипВзаимодействия,
			СообщениеBase64,
			ТипСообщения,
			РегистрационныйНомерФСС,
			ИнформацияОПрограмме.ИДКонфигурации,
			ИнформацияОПрограмме.ВерсияКонфигурации,
			ТекстПараметровТипаВзаимодействия,
			НастройкиВызова.ИдентификаторСообщенияСЭДО);
	КонецЕсли;
	
	Возврат Текст;
	
КонецФункции

&НаСервереБезКонтекста
Функция ТекстЗапросаПолученияКоличестваСообщенийСЭДОФСС(
			РегистрационныйНомерФСС,
			ДатаСообщений = Неопределено,
			Настройки = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ТекстЗапросаПолученияКоличестваСообщенийСЭДОФСС(
		РегистрационныйНомерФСС,
		ДатаСообщений,
		Настройки);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СтрокаИзДанныхПоАдресу(Адрес)
	
	Данные = ПолучитьИзВременногоХранилища(Адрес);
	Если ТипЗнч(Данные) = Тип("ДвоичныеДанные") Тогда
		Чтение = Новый ЧтениеДанных(Данные);
		Результат = Чтение.ПрочитатьСимволы();
		Чтение.Закрыть();
	Иначе
		Результат = Данные;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция)
	
	Результат = ЭлектронныйДокументооборотСФССКлиентСервер.ПараметрыСохраненияЗапросаСЭДОФСС(
			Организация, Операция);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Процедура СохранитьДанныеЗапросаСЭДОФСС(ПараметрыСохранения)
	
	ЭлектронныйДокументооборотСФСС.СохранитьДанныеЗапросаСЭДО(ПараметрыСохранения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция РегистрационныйНомерФССОрганизации(Организация)
	
	СвойстваОрганизацииДляОбмена = СвойстваОрганизацииДляОбмена(Организация);
	Возврат СвойстваОрганизацииДляОбмена.РегистрационныйНомерФСС;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПараметрыПакетногоПолученияИзСЭДО()
	
	Результат = Новый Структура;
	Результат.Вставить("ИспользоватьПакетноеПолучение", Ложь);
	Результат.Вставить("КоличествоСообщенийВПакете", 1);
	
	ДанныеСобытия = ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступилоРасширенная(
		"Пакетное получение СЭДО");
	Если ДанныеСобытия.СобытиеНаступило Тогда
		Результат.ИспользоватьПакетноеПолучение = Истина;
		ГодОкончания = Год(ДанныеСобытия.ДатаОкончания);
		Год3000 = 3000;
		Если ГодОкончания > Год3000 Тогда
			Результат.КоличествоСообщенийВПакете = ГодОкончания - Год3000;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКоличествоСообщенийВПакетномЗапросеСЭДО()
	
	ПараметрыПолучения = ПараметрыПакетногоПолученияИзСЭДО();
	Если ПараметрыПолучения.ИспользоватьПакетноеПолучение Тогда
		Возврат ПараметрыПолучения.КоличествоСообщенийВПакете;
	Иначе
		Возврат 0;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#Область АдресПодписанногоSOAPОперацииСЭДОФСС

&НаКлиенте
Асинх Процедура АдресПодписанногоSOAPОперацииСЭДОФСС(
		ВыполняемоеОповещение,
		ВыгрузкаЗапросаОперацииСЭДО,
		ЗашифроватьSOAP = Истина,
		ВозвращатьАдресПодписанногоЗапроса = Ложь,
		ЗадаватьВопросОТестовомСервере = Истина,
		СвойстваОбменаПоСЭДОЧерезОператора = Неопределено) Экспорт
	
	Организация = ВыгрузкаЗапросаОперацииСЭДО.Организация;
	
	ОрганизацияИспользуетОбменСФСС 						= Ложь;
	ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи 	= Ложь;
	НастроенОбменВУниверсальномФормате 					= Ложь;
	УчетнаяЗаписьДокументооборота 						= Неопределено;
	
	ПолучитьСвойстваОрганизацииДляФССНаСервере(
		Организация,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,
		УчетнаяЗаписьДокументооборота);
		
	Если НЕ ОрганизацияИспользуетОбменСФСС Тогда
		ТекстПредупреждения = НСтр("ru = 'По организации %1 не включена возможность электронного документооборота с ФСС.'");
		ТекстПредупреждения = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(ТекстПредупреждения, Истина);
		ТекстПредупреждения = СтрШаблон(ТекстПредупреждения, Организация);
		Если НЕ НастроенОбменВУниверсальномФормате Тогда
			ПараметрыОбменаСФСС = ПараметрыОбменаСФСС(Организация);
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.
				ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение(ПараметрыОбменаСФСС);
		Иначе
			Ждать ПредупреждениеАсинх(ТекстПредупреждения);
		КонецЕсли;
		Результат = РезультатПолучитьДанныеСЭДОФССПослеПодписиSOAP();
		Результат.ОписаниеОшибки = ТекстПредупреждения;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ВыполнятьАвтонастройку = НастроенОбменВУниверсальномФормате И ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ВыгрузкаЗапросаОперацииСЭДО", 		ВыгрузкаЗапросаОперацииСЭДО);
	ДополнительныеПараметры.Вставить("ЗашифроватьSOAP", 					ЗашифроватьSOAP);
	ДополнительныеПараметры.Вставить("ВозвращатьАдресПодписанногоЗапроса", 	ВозвращатьАдресПодписанногоЗапроса);
	ДополнительныеПараметры.Вставить("АдресПодписанногоЗапросаSOAP", 		Неопределено);
	ДополнительныеПараметры.Вставить("ЗадаватьВопросОТестовомСервере", 		ЗадаватьВопросОТестовомСервере);
	ДополнительныеПараметры.Вставить("ВыполнятьАвтонастройку", 				ВыполнятьАвтонастройку);
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоСЭДОЧерезОператора",	СвойстваОбменаПоСЭДОЧерезОператора);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если ВыполнятьАвтонастройку Тогда
		АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗаписьДокументооборота, , Оповещение, , , Истина);
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"АдресПодписанногоSOAPОперацииСЭДОФССПослеПроверкиЛицензииИСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	Если ВходящийКонтекст.ВыполнятьАвтонастройку Тогда
		
		Организация = ВходящийКонтекст.ВыгрузкаЗапросаОперацииСЭДО.Организация;
		
		ПроверитьСертификатИЛицензию(
			ОписаниеОповещения, 
			Неопределено, 
			Организация, 
			"ФСС");
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПроверкиЛицензииИСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ВыгрузкаЗапросаОперацииСЭДО 	= ДополнительныеПараметры.ВыгрузкаЗапросаОперацииСЭДО;
	ЗашифроватьSOAP 				= ДополнительныеПараметры.ЗашифроватьSOAP;
	ЗадаватьВопросОТестовомСервере 	= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	
	ПространствоИменЗапроса = "http://www.fss.ru/integration/types/sedo/v01";
	ДобавляемыеАтрибутыЗапроса = "xmlns:ds=""http://www.w3.org/2000/09/xmldsig#"""
		+ " xmlns:v01=""" + ПространствоИменЗапроса + """"
		+ " xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"""
		+ " xmlns:wsse=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-secext-1.0.xsd"""
		+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""";
	ДобавляемыеАтрибутыПодписи = "xmlns=""http://www.w3.org/2000/09/xmldsig#""";
	
	СкорректированнаяВыгрузкаЗапроса = СкорректироватьВыгрузкуЗапросаОперацииДляSOAPСЭДОФСС(
		ВыгрузкаЗапросаОперацииСЭДО.ТекстЗапроса,
		ПространствоИменЗапроса);
	
	Если ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора = Неопределено Тогда
		ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора =
			СвойстваОбменаПоСЭДОЧерезОператора(ВыгрузкаЗапросаОперацииСЭДО.Организация);
	КонецЕсли;
	
	НастройкиВызова = Новый Структура;
	Если ВыгрузкаЗапросаОперацииСЭДО.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		НастройкиВызова.Вставить("СодержимоеПодписанногоЗапросаSOAP",
			ВыгрузкаЗапросаОперацииСЭДО.СодержимоеПодписанногоЗапросаSOAP);
	КонецЕсли;
	РезультатИнициализации = АдресПодписанногоSOAPОперацииСЭДОФССИнициализацияНаСервере(
		ВыгрузкаЗапросаОперацииСЭДО.Организация,
		СкорректированнаяВыгрузкаЗапроса,
		ДобавляемыеАтрибутыЗапроса,
		ВыгрузкаЗапросаОперацииСЭДО.РегистрационныйНомерФСС,
		ЗашифроватьSOAP,
		ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора,
		НастройкиВызова);
	
	Если РезультатИнициализации = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗапроса", 			ДобавляемыеАтрибутыЗапроса);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 			ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("СкорректированнаяВыгрузкаЗапроса", 	СкорректированнаяВыгрузкаЗапроса);
	ДополнительныеПараметры.Вставить("РезультатИнициализации", 				РезультатИнициализации);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеПредупрежденияПриТестовомРежиме",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если РезультатИнициализации.ТестовыйСерверФСССЭДО И ЗадаватьВопросОТестовомСервере Тогда
		ТекстВопроса = НСтр("ru = 'Внимание! Включен режим тестирования СЭДО!
			|
			|В режиме тестирования обмен выполняется через тестовый шлюз СЭДО ФСС.
			|Данные не соответствуют действительности и не засчитываются ФСС.
			|
			|Продолжить обмен?'");
		ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, КодВозвратаДиалога.Да);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПредупрежденияПриТестовомРежиме(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыгрузкаЗапросаОперацииСЭДО 		= ДополнительныеПараметры.ВыгрузкаЗапросаОперацииСЭДО;
	ЗашифроватьSOAP 					= ДополнительныеПараметры.ЗашифроватьSOAP;
	ДобавляемыеАтрибутыЗапроса 			= ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи 			= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	СкорректированнаяВыгрузкаЗапроса 	= ДополнительныеПараметры.СкорректированнаяВыгрузкаЗапроса;
	РезультатИнициализации 				= ДополнительныеПараметры.РезультатИнициализации;
	ВозвращатьАдресПодписанногоЗапроса 	= ДополнительныеПараметры.ВозвращатьАдресПодписанногоЗапроса;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		РезультатДляВозврата = Новый Структура;
		РезультатДляВозврата.Вставить("ОтмененоПользователем", Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатДляВозврата);
		Возврат;
	КонецЕсли;
	
	ЗапросОперацииСЭДО64 = РезультатИнициализации.ЗапросОперацииСЭДО64;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РезультатИнициализации);
	УчетнаяЗапись = РезультатИнициализации.УчетнаяЗапись;
	
	СертификатСтрахователяОтпечаток = РезультатИнициализации.СертификатСтрахователяОтпечаток;
	СертификатСтрахователя 			= РезультатИнициализации.СертификатСтрахователя;
	СертификатФСССЭДООтпечаток 		= РезультатИнициализации.СертификатФСССЭДООтпечаток;
	СертификатФСССЭДО 				= РезультатИнициализации.СертификатФСССЭДО;
	МЧД 							= РезультатИнициализации.МЧД;
	ОбменПоСЭДОНапрямую 			= СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	ИдентификаторАбонентаСЭДО 		= СвойстваОбменаПоСЭДОЧерезОператора.ИдентификаторАбонентаСЭДО;
	
	Если ДополнительныеПараметры.ВыгрузкаЗапросаОперацииСЭДО.Свойство("ОтпечатокСертификатаПодписанта") Тогда
		СертификатСтрахователяОтпечаток = ДополнительныеПараметры.ВыгрузкаЗапросаОперацииСЭДО.ОтпечатокСертификатаПодписанта;
		СертификатСтрахователя = Неопределено;
	КонецЕсли;
	
	СодержимоеСообщения = ?(ВыгрузкаЗапросаОперацииСЭДО.Свойство("СодержимоеСообщения"),
		ВыгрузкаЗапросаОперацииСЭДО.СодержимоеСообщения, Неопределено);
	ИдентификаторРодителя = ?(ВыгрузкаЗапросаОперацииСЭДО.Свойство("ИдентификаторРодителя"),
		ВыгрузкаЗапросаОперацииСЭДО.ИдентификаторРодителя, Неопределено);
	
	// Заново создаем структуру параметров.
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", 			ВыгрузкаЗапросаОперацииСЭДО.РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ВыгрузкаЗапроса", 					СкорректированнаяВыгрузкаЗапроса);
	ДополнительныеПараметры.Вставить("СертификатФСССЭДООтпечаток", 			СертификатФСССЭДООтпечаток);
	ДополнительныеПараметры.Вставить("СертификатФСССЭДО", 					СертификатФСССЭДО);
	ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
	ДополнительныеПараметры.Вставить("ОбменПоСЭДОНапрямую", 				ОбменПоСЭДОНапрямую);
	ДополнительныеПараметры.Вставить("ИдентификаторАбонентаСЭДО", 			ИдентификаторАбонентаСЭДО);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ЗашифроватьSOAP", 					ЗашифроватьSOAP);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗапроса", 			ДобавляемыеАтрибутыЗапроса);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 			ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("ЗапросОперацииСЭДО64", 				ЗапросОперацииСЭДО64);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", 						УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("АдресПодписанногоЗапросаSOAP", 		Неопределено);
	ДополнительныеПараметры.Вставить("ВозвращатьАдресПодписанногоЗапроса", 	ВозвращатьАдресПодписанногоЗапроса);
	ДополнительныеПараметры.Вставить("СодержимоеСообщения", 				СодержимоеСообщения);
	ДополнительныеПараметры.Вставить("ИдентификаторРодителя", 				ИдентификаторРодителя);
	Если ВыгрузкаЗапросаОперацииСЭДО.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		ДополнительныеПараметры.Вставить("СодержимоеПодписанногоЗапросаSOAP",
			ВыгрузкаЗапросаОперацииСЭДО.СодержимоеПодписанногоЗапросаSOAP);
	КонецЕсли;
	Если ВыгрузкаЗапросаОперацииСЭДО.Свойство("ОтпечатокСертификатаПодписанта") Тогда
		ДополнительныеПараметры.Вставить("ОтпечатокСертификатаПодписанта",
			ВыгрузкаЗапросаОперацииСЭДО.ОтпечатокСертификатаПодписанта);
	КонецЕсли;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеПоискаСертификатаСтрахователя",
		ЭтотОбъект, ДополнительныеПараметры);
	Если ДополнительныеПараметры.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, Ложь, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	ИначеЕсли НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) И СертификатСтрахователя <> Неопределено Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, Истина, СертификатСтрахователя);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатСтрахователяОтпечаток, "MY");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПоискаСертификатаСтрахователя(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатФСССЭДООтпечаток = ДополнительныеПараметры.СертификатФСССЭДООтпечаток;
	СертификатФСССЭДО = ДополнительныеПараметры.СертификатФСССЭДО;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХранененияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", СертификатСтрахователя);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеПоискаСертификатаФСС",
		ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХранененияКлюча) Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("Выполнено", Истина);
		РезультатВыполнения.Вставить("МенеджерКриптографии", Неопределено);
		РезультатВыполнения.Вставить("СертификатНайден", ЗашифроватьSOAP);
		РезультатВыполнения.Вставить("СвойстваСертификата", СертификатФСССЭДО);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатФСССЭДООтпечаток, "AddressBook");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПоискаСертификатаФСС(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя 	= ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP 		= ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХраненияКлюча 		= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	УчетнаяЗапись 			= ДополнительныеПараметры.УчетнаяЗапись;
	ОбменПоСЭДОНапрямую 	= ДополнительныеПараметры.ОбменПоСЭДОНапрямую;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатФСССЭДО = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	СертификатыОбмена = Новый СписокЗначений();
	Если НЕ ДополнительныеПараметры.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		СертификатыОбмена.Добавить(СертификатСтрахователя, НСтр("ru = 'Сертификат страхователя'"));
	КонецЕсли;
	Если ЗашифроватьSOAP И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДТолькоПодписание") <> Истина Тогда
		Если ОбменПоСЭДОНапрямую Тогда
			СертификатыОбмена.Добавить(СертификатФСССЭДО, НСтр("ru = 'Сертификат шифрования ЭЛН, ПОВЭД, ССВ, СЭДО'"));
		Иначе
			СертификатыОбмена.Добавить(СертификатФСССЭДО, НСтр("ru = 'Сертификат сервера документооборота учетной записи'"));
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ СертификатыГодятсяДляПодписанияОтчетностиФСС(СертификатыОбмена) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("СертификатФСССЭДО", СертификатФСССЭДО);
	
	Если ДополнительныеПараметры.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		// подписание уже выполнено
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
		
		СодержимоеПодписанногоЗапросаSOAP = ДополнительныеПараметры.СодержимоеПодписанногоЗапросаSOAP;
		
		ПодписанноеСообщение64 = "";
		ПрефиксСодержимого = "<message>";
		ПозицияСодержимого = СтрНайти(СодержимоеПодписанногоЗапросаSOAP, ПрефиксСодержимого);
		Если ПозицияСодержимого <> 0 Тогда
			ПозицияСодержимого = ПозицияСодержимого + СтрДлина(ПрефиксСодержимого);
			ПодписанноеСообщение64 = Сред(СодержимоеПодписанногоЗапросаSOAP, ПозицияСодержимого);
			ПозицияЗавершения = СтрНайти(ПодписанноеСообщение64, "</message>");
			Если ПозицияЗавершения <> 0 Тогда
				ПодписанноеСообщение64 = Лев(ПодписанноеСообщение64, ПозицияЗавершения - 1);
			КонецЕсли;
			ПодписанноеСообщение64 = СтрЗаменить(ПодписанноеСообщение64, Символы.ПС, "");
			ПодписанноеСообщение64 = СтрЗаменить(ПодписанноеСообщение64, Символы.ВК, "");
		КонецЕсли;
		ДополнительныеПараметры.Вставить("ПодписанноеСообщение64", ПодписанноеСообщение64);
		
		ДанныеПодписанногоЗапросаSOAP = ПолучитьДвоичныеДанныеИзСтроки(СодержимоеПодписанногоЗапросаSOAP, "utf-8");
		
		Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			СодержимоеПодписанногоЗапросаSOAP64 = Base64Строка(ДанныеПодписанногоЗапросаSOAP);
			СодержимоеПодписанногоЗапросаSOAP64 = СтрЗаменить(СодержимоеПодписанногоЗапросаSOAP64, Символы.ПС, "");
			СодержимоеПодписанногоЗапросаSOAP64 = СтрЗаменить(СодержимоеПодписанногоЗапросаSOAP64, Символы.ВК, "");
			РезультатОперации = СодержимоеПодписанногоЗапросаSOAP64;
		Иначе
			РезультатОперации = ПоместитьВоВременноеХранилище(ДанныеПодписанногоЗапросаSOAP, Новый УникальныйИдентификатор);
		КонецЕсли;
		
		АдресПодписанногоSOAPОперацииСЭДОФССПослеПолученияАдресаЗапроса(РезультатОперации, ДополнительныеПараметры);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеЭкспортаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатСтрахователя);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеЭкспортаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча 		= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЗапросОперацииСЭДО64 	= ДополнительныеПараметры.ЗапросОперацииСЭДО64;
	СодержимоеСообщения 	= ДополнительныеПараметры.СодержимоеСообщения;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПолучателя64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("СертификатПолучателя64", СертификатПолучателя64);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеЗаписиФайлаСообщения",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если СодержимоеСообщения <> Неопределено Тогда
		ДанныеСообщения = ПолучитьДвоичныеДанныеИзСтроки(СодержимоеСообщения, "utf-8");
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			АдресСообщения = ПоместитьВоВременноеХранилище(ДанныеСообщения, Новый УникальныйИдентификатор);
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, АдресСообщения);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		Иначе
			СодержимоеСообщенияВBase64 = Base64Строка(ДанныеСообщения);
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СодержимоеСообщенияВBase64);
		КонецЕсли;
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеЗаписиФайлаСообщения(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя 	= ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	СодержимоеСообщения 	= ДополнительныеПараметры.СодержимоеСообщения;
	ИдентификаторРодителя 	= ДополнительныеПараметры.ИдентификаторРодителя;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлСообщения = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлСообщения", ФайлСообщения);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеПодписиСообщения",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если СодержимоеСообщения <> Неопределено Тогда
		НастройкиПодписи = Новый Структура;
		НастройкиПодписи.Вставить("ЭтоИсключающаяКанонизация", 						Истина);
		НастройкиПодписи.Вставить("ДополнитьПространстваИменУзлаПодписи", 			Истина);
		НастройкиПодписи.Вставить("ТипКаноническогоXML", 							"КаноническийXML1_1");
		НастройкиПодписи.Вставить("ИспользоватьПреобразованиеККаноническомуXML", 	Истина);
		НастройкиПодписи.Вставить("КанонизацияБезКомментариев", 					Истина);
		НастройкиПодписи.Вставить("ПрефиксПодписи", 								"ds");
		НастройкиПодписи.Вставить("ДополнительноеПространствоИмен", 				"http://www.fss.ru/integration/types/common/v01");
		НастройкиПодписи.Вставить("ДобавитьШтампВремени", 							Истина);
		НастройкиПодписи.Вставить("ИдентификаторРодителя", 							ИдентификаторРодителя);
		ПодписатьФайлXMLDSig(
			СертификатСтрахователя,
			ФайлСообщения,
			Оповещение,,,
			НастройкиПодписи);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПодписиСообщения(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча 		= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СодержимоеСообщения 	= ДополнительныеПараметры.СодержимоеСообщения;
	ФайлСообщения 			= ДополнительныеПараметры.ФайлСообщения;
	ЛокальнаяПодпись 		= ЭтоЛокальнаяПодпись(МестоХраненияКлюча);
	
	Если ЗначениеЗаполнено(ФайлСообщения) Тогда
		Если НЕ ЛокальнаяПодпись Тогда
			УдалитьИзВременногоХранилища(ФайлСообщения);
		Иначе
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлСообщения);
		КонецЕсли;
	КонецЕсли;
	
	Если ПолноеИмяПодписанногоФайла = Неопределено И СодержимоеСообщения <> Неопределено Тогда
		ОписаниеОшибки = НСтр("ru = 'Не удалось подписать файл СЭДО.'");
		ПараметрыВывода = ЭлектронныйДокументооборотСФССКлиентСервер.НовыеПараметрыВывестиОшибку(Истина);
		ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(ОписаниеОшибки, ПараметрыВывода);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлПодписанногоСообщения", ПолноеИмяПодписанногоФайла);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеЧтенияФайлаСообщения",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если СодержимоеСообщения = Неопределено Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	ИначеЕсли НЕ ЛокальнаяПодпись Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеЧтенияФайлаСообщения(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча 			= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЗапросОперацииСЭДО64 		= ДополнительныеПараметры.ЗапросОперацииСЭДО64;
	СодержимоеСообщения 		= ДополнительныеПараметры.СодержимоеСообщения;
	ФайлПодписанногоСообщения 	= ДополнительныеПараметры.ФайлПодписанногоСообщения;
	ЛокальнаяПодпись 			= ЭтоЛокальнаяПодпись(МестоХраненияКлюча);
	
	Если ЛокальнаяПодпись Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлПодписанногоСообщения);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ПодписанноеСообщение64 = Результат.СтрокаBase64;
	
	Если СодержимоеСообщения <> Неопределено Тогда
		Если НЕ ЛокальнаяПодпись Тогда
			ПодставитьСообщениеВТекстЗапросаОперацииСЭДОФСС(
				ЗапросОперацииСЭДО64,
				ДополнительныеПараметры.ВыгрузкаЗапроса,
				ПодписанноеСообщение64);
			
		Иначе
			ДанныеЗапросаОперации = Base64Значение(ЗапросОперацииСЭДО64);
			ТекстЗапросаОперации = ПолучитьСтрокуИзДвоичныхДанных(ДанныеЗапросаОперации, "utf-8");
			
			ПодписанноеСообщение64 = СтрЗаменить(ПодписанноеСообщение64, Символы.ВК, "");
			ПодписанноеСообщение64 = СтрЗаменить(ПодписанноеСообщение64, Символы.ПС, "");
			ТекстЗапросаОперации = СтрЗаменить(ТекстЗапросаОперации, "%Message%", ПодписанноеСообщение64);
			ДополнительныеПараметры.ВыгрузкаЗапроса = СтрЗаменить(ДополнительныеПараметры.ВыгрузкаЗапроса,
				"%Message%", ПодписанноеСообщение64);
			
			ДанныеЗапросаОперации = ПолучитьДвоичныеДанныеИзСтроки(ТекстЗапросаОперации, "utf-8");
			ЗапросОперацииСЭДО64 = Base64Строка(ДанныеЗапросаОперации);
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ПодписанноеСообщение64",
		?(СодержимоеСообщения = Неопределено, "", ПодписанноеСообщение64));
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеЗаписиФайла",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если НЕ ЛокальнаяПодпись Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ЗапросОперацииСЭДО64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ЗапросОперацииСЭДО64);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПодставитьСообщениеВТекстЗапросаОперацииСЭДОФСС(
		АдресЗапросаОперации,
		ВыгрузкаЗапроса,
		ПодписанноеСообщение64)
	
	ДанныеЗапросаОперации = ПолучитьИзВременногоХранилища(АдресЗапросаОперации);
	УдалитьИзВременногоХранилища(АдресЗапросаОперации);
	ТекстЗапросаОперации = ПолучитьСтрокуИзДвоичныхДанных(ДанныеЗапросаОперации, "utf-8");
	
	ДанныеПодписанногоСообщения = ПолучитьИзВременногоХранилища(ПодписанноеСообщение64);
	УдалитьИзВременногоХранилища(ПодписанноеСообщение64);
	ПодписанноеСообщение64 = Base64Строка(ДанныеПодписанногоСообщения);
	ПодписанноеСообщение64 = СтрЗаменить(ПодписанноеСообщение64, Символы.ВК, "");
	ПодписанноеСообщение64 = СтрЗаменить(ПодписанноеСообщение64, Символы.ПС, "");
	ТекстЗапросаОперации = СтрЗаменить(ТекстЗапросаОперации, "%Message%", ПодписанноеСообщение64);
	ВыгрузкаЗапроса = СтрЗаменить(ВыгрузкаЗапроса, "%Message%", ПодписанноеСообщение64);
	
	ДанныеЗапросаОперации = ПолучитьДвоичныеДанныеИзСтроки(ТекстЗапросаОперации, "utf-8");
	АдресЗапросаОперации = ПоместитьВоВременноеХранилище(ДанныеЗапросаОперации, Новый УникальныйИдентификатор);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеЗаписиФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗапроса = ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлЗапросаОперацииСЭДО = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлЗапросаОперацииСЭДО", ФайлЗапросаОперацииСЭДО);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеПодписания", ЭтотОбъект, ДополнительныеПараметры);
	
	ДобавляемыеАтрибутыПодписиИЗаписи = ДобавляемыеАтрибутыПодписи
		+ ?(ДобавляемыеАтрибутыПодписи <> "" И ДобавляемыеАтрибутыЗапроса <> "", " ", "")
		+ ДобавляемыеАтрибутыЗапроса;
	ИдентификаторПодписываемыхДанных = "REGNO_" + РегистрационныйНомерФСС;
	
	НастройкиПодписи = Новый Структура;
	НастройкиПодписи.Вставить("Трансформация1_1", 						Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 				Ложь);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 				Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 						Неопределено);
	НастройкиПодписи.Вставить("НормализацияОтключена", 					Истина);
	НастройкиПодписи.Вставить("ДанныеВнутриПодписи", 					Истина);
	НастройкиПодписи.Вставить("ИдентификаторПодписываемыхДанных", 		ИдентификаторПодписываемыхДанных);
	НастройкиПодписи.Вставить("ДобавляемыеАтрибуты", 					ДобавляемыеАтрибутыПодписиИЗаписи);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОСертификате", 		Ложь);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОбОбъекте", 			Истина);
	НастройкиПодписи.Вставить("ДополнительныеТрансформации", 			Неопределено);
	НастройкиПодписи.Вставить("ДополнительныеОбратныеТрансформации", 	Неопределено);
	НастройкиПодписи.Вставить("ЭтоИсключающаяКанонизация",				Истина);
	НастройкиПодписи.Вставить("ИспользуетсяУзелТрансформации",			Истина);
	НастройкиПодписи.Вставить("ТипКаноническогоXML",					?(ДополнительныеПараметры.ОбменПоСЭДОНапрямую,
																			"ИсключающийКаноническийXMLСКомментариями",
																			"ИсключающийКаноническийXML"));
	НастройкиПодписи.Вставить("ИспользоватьПреобразованиеККаноническомуXML",			Истина);
	
	ПодписатьФайлXMLDSig(
		СертификатСтрахователя,
		ФайлЗапросаОперацииСЭДО,
		Оповещение,,,
		НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПодписания(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФайлЗапросаОперацииСЭДО = ДополнительныеПараметры.ФайлЗапросаОперацииСЭДО;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьИзВременногоХранилища(ФайлЗапросаОперацииСЭДО);
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлЗапросаОперацииСЭДО);
	КонецЕсли;
	
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ОписаниеОшибки = НСтр("ru = 'Не удалось подписать файл запроса СЭДО.'");
		ПараметрыВывода = ЭлектронныйДокументооборотСФССКлиентСервер.НовыеПараметрыВывестиОшибку(Истина);
		ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(ОписаниеОшибки, ПараметрыВывода);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлПодписиЗапросаОперацииСЭДО", ПолноеИмяПодписанногоФайла);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеЧтенияФайла",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеЧтенияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	РегистрационныйНомерФСС = ДополнительныеПараметры.РегистрационныйНомерФСС;
	ВыгрузкаЗапроса = ДополнительныеПараметры.ВыгрузкаЗапроса;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗашифроватьSOAP = ДополнительныеПараметры.ЗашифроватьSOAP;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыЗапроса = ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи = ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	СертификатПолучателя64 = ДополнительныеПараметры.СертификатПолучателя64;
	ФайлПодписиЗапросаОперацииСЭДО = ДополнительныеПараметры.ФайлПодписиЗапросаОперацииСЭДО;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлПодписиЗапросаОперацииСЭДО);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлПодписиЗапросаОперацииСЭДО64 = Результат.СтрокаBase64;
	
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыЗапроса", 	ДобавляемыеАтрибутыЗапроса);
	НастройкиОперации.Вставить("ДобавляемыеАтрибутыПодписи", 	ДобавляемыеАтрибутыПодписи);
	НастройкиОперации.Вставить("ВозвращатьТекстВBase64", 		ЗашифроватьSOAP);
	НастройкиОперации.Вставить("МЧД", 							ДополнительныеПараметры.МЧД);
	НастройкиОперации.Вставить("ОбменПоСЭДОНапрямую", 			ДополнительныеПараметры.ОбменПоСЭДОНапрямую);
	НастройкиОперации.Вставить("ИдентификаторАбонентаСЭДО", 	ДополнительныеПараметры.ИдентификаторАбонентаСЭДО);
	РезультатОперации = АдресSOAPЗапросОперацииСЭДОФССПоПодписиНаСервере(
		РегистрационныйНомерФСС,
		ВыгрузкаЗапроса,
		ФайлПодписиЗапросаОперацииСЭДО64,
		СертификатПолучателя64,
		НастройкиОперации);
	
	АдресПодписанногоSOAPОперацииСЭДОФССПослеПолученияАдресаЗапроса(РезультатОперации, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеЗаписиФайлаПодписанногоSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя 				= ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФСССЭДО 					= ДополнительныеПараметры.СертификатФСССЭДО;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресПодписанногоЗапросаSOAP 		= ДополнительныеПараметры.АдресПодписанногоЗапросаSOAP;
	ВозвращатьАдресПодписанногоЗапроса 	= ДополнительныеПараметры.ВозвращатьАдресПодписанногоЗапроса;
	ПодписанноеСообщение64 				= ДополнительныеПараметры.ПодписанноеСообщение64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлПодписанногоSOAP = Результат.ИмяФайла;
	
	ШаблонЗапросаSOAP =
		"<SOAP-ENV:Envelope xmlns:SOAP-ENV=""http://schemas.xmlsoap.org/soap/envelope/"">
		|	<SOAP-ENV:Header/>
		|	<SOAP-ENV:Body>
		|%1
		|	</SOAP-ENV:Body>
		|</SOAP-ENV:Envelope>";
	
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ШаблонЗапросаSOAP", 								ШаблонЗапросаSOAP);
	НастройкиОперации.Вставить("ПоместитьВУзелX509CertificateСертификатПолучателя", Истина);
	НастройкиОперации.Вставить("АдресПодписанногоЗапросаSOAP", 						АдресПодписанногоЗапросаSOAP);
	НастройкиОперации.Вставить("ВозвращатьАдресПодписанногоЗапроса", 				ВозвращатьАдресПодписанногоЗапроса);
	НастройкиОперации.Вставить("СценарийФСС", 										Истина);
	НастройкиОперации.Вставить("ПодписанноеСообщение64", 							ПодписанноеСообщение64);
	
	Если ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДТолькоПодписание") = Истина Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("АдресЗашифрованногоЗапросаSOAP", 	Неопределено);
		РезультатВыполнения.Вставить("АдресПодписаногоЗапросаSOAP", 	АдресПодписанногоЗапросаSOAP);
		РезультатВыполнения.Вставить("ПодписанноеСообщение64", 			ПодписанноеСообщение64);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	Иначе
		Если ДополнительныеПараметры.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
			СертификатСтрахователя = СертификатФСССЭДО;
		КонецЕсли;
		ЗашифроватьФайлXML(
			СертификатФСССЭДО,
			СертификатСтрахователя,
			ФайлПодписанногоSOAP,
			ВыполняемоеОповещение,,
			Истина,
			НастройкиОперации);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСЭДОФССПослеПолученияАдресаЗапроса(РезультатОперации, ДополнительныеПараметры)
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если ЭтоАдресВременногоХранилища(РезультатОперации) Тогда
		ДополнительныеПараметры.АдресПодписанногоЗапросаSOAP = РезультатОперации;
	Иначе
		ДополнительныеПараметры.АдресПодписанногоЗапросаSOAP =
			ПоместитьВоВременноеХранилище(Base64Значение(РезультатОперации),
				Новый УникальныйИдентификатор);
	КонецЕсли;
	
	Если НЕ ДополнительныеПараметры.ЗашифроватьSOAP Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, РезультатОперации);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСЭДОФССПослеЗаписиФайлаПодписанногоSOAP",
			ЭтотОбъект, ДополнительныеПараметры);
		
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
				Истина, Неопределено, РезультатОперации);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, РезультатОперации);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ПолучитьДанныеСЭДОФСС

&НаКлиенте
Функция РезультатПолучитьДанныеСЭДОФССПослеПодписиSOAP()
	
	Результат = Новый Структура;
	Результат.Вставить("АдресПодписаногоЗапросаSOAP", "");
	Результат.Вставить("АдресЗашифрованногоЗапросаSOAP", "");
	Результат.Вставить("ПодписанноеСообщение64", "");
	Результат.Вставить("ОписаниеОшибки", "");
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПараметрыЗапросаПолучитьДанныеСЭДОФСС()
	
	Результат = Новый Структура;
	Результат.Вставить("Организация", Неопределено);
	Результат.Вставить("Операция", "");
	Результат.Вставить("ТекстЗапроса", "");
	Результат.Вставить("ИдентификаторыСообщений", Новый Массив);
	Результат.Вставить("РезультатФоновойЗагрузки", Неопределено);
	Результат.Вставить("ТипыСообщений", Новый Массив);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ПолучитьДанныеСЭДОФСС(
		ВыполняемоеОповещение,
		ЗапросПолученияДанных,
		ЗадаватьВопросОТестовомСервере = Истина,
		ТипВзаимодействия = Неопределено,
		ОтображатьСостояние = Истина,
		СвойстваОбменаПоСЭДОЧерезОператора = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат = РезультатПолучитьДанныеСЭДОФСС();
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	Операция = ЗапросПолученияДанных.Операция;
	Организация = ЗапросПолученияДанных.Организация;
	РегистрационныйНомерФСС = РегистрационныйНомерФССОрганизации(Организация);
	
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		Результат.ОписаниеОшибки = НСтр("ru='Не указана организация.'");
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Если ПустаяСтрока(РегистрационныйНомерФСС) Тогда
		Шаблон = НСтр("ru='Не указан регистрационный номер ФСС организации %1.'");
		Результат.ОписаниеОшибки = СтрШаблон(Шаблон, Организация);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	РезультатПроверкиРегистрационногоНомераФСС = ПроверитьДлинуРегистрационныйНомерФСС(
		Результат,
		ТипВзаимодействия,
		Организация);
	Если НЕ РезультатПроверкиРегистрационногоНомераФСС Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 		Организация);
	ДополнительныеПараметры.Вставить("Операция", 				Операция);
	ДополнительныеПараметры.Вставить("Результат", 				Результат);
	ДополнительныеПараметры.Вставить("ОшибкаОтправкиНаСервер", 	Ложь);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 		ТипВзаимодействия);
	ДополнительныеПараметры.Вставить("ПодписанноеСообщение64", 	"");
	ДополнительныеПараметры.Вставить("РезультатФоновойЗагрузки", Неопределено);
	ДополнительныеПараметры.Вставить("ОтображатьСостояние", 	ОтображатьСостояние);
	ДополнительныеПараметры.Вставить("ИдентификаторыСообщений", ЗапросПолученияДанных.ИдентификаторыСообщений);
	
	Если ТипЗнч(ВыполняемоеОповещение.ДополнительныеПараметры) = Тип("Структура")
		И ВыполняемоеОповещение.ДополнительныеПараметры.Свойство("ТипСообщенияОтвета") Тогда
		ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",
			ВыполняемоеОповещение.ДополнительныеПараметры.ТипСообщенияОтвета);
	КонецЕсли;
	
	Если СвойстваОбменаПоСЭДОЧерезОператора = Неопределено Тогда
		СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	КонецЕсли;
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоСЭДОЧерезОператора", СвойстваОбменаПоСЭДОЧерезОператора);
	
	РезультатФоновойЗагрузки = Неопределено;
	Если ЗапросПолученияДанных.Свойство("РезультатФоновойЗагрузки", РезультатФоновойЗагрузки)
		И ЗначениеЗаполнено(РезультатФоновойЗагрузки) Тогда
		// Данные были получены в фоновом задании, требуется расшифровка.
		ДополнительныеПараметры.РезультатФоновойЗагрузки = РезультатФоновойЗагрузки;
		АдресЗашифрованногоОтветаSOAP = РезультатФоновойЗагрузки.АдресЗашифрованногоОтветаSOAP;
		РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(Организация);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
		
		ТекстОтвета = ПолучитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			ДанныеОтвета = ПолучитьДвоичныеДанныеИзСтроки(ТекстОтвета, "windows-1251");
			АдресЗашифрованногоОтветаSOAP = ПоместитьВоВременноеХранилище(ДанныеОтвета, Новый УникальныйИдентификатор);;
		КонецЕсли;
		ЗаполнитьЗначенияСвойств(Результат, РезультатФоновойЗагрузки);
		Результат.АдресПодписаногоЗапросаSOAP = РезультатФоновойЗагрузки.АдресПодписаногоЗапросаSOAP;
		Результат.ОтветПолученФоновымЗаданием = Истина;
		Расшифровать =  Найти(ТекстОтвета, "<xenc:EncryptedData") > 0;
		Оповещение = Новый ОписаниеОповещения("ПолучитьДанныеСЭДОФССПослеПроверкиПодписиSOAP",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПроверитьПодписьSOAPОтветаЭЛНВФСС(
			Оповещение,
			Организация,
			АдресЗашифрованногоОтветаSOAP,
			Расшифровать, , ,
			Истина);
		Возврат;
	КонецЕсли;
	
	ПодписатьSOAP = Истина;
	ЗашифроватьSOAP = Истина;
	
	Если Операция = ОперацияПолученияСпискаВходящихСЭДОФСС() Тогда
		ПодписатьSOAP = Ложь;
		ЗашифроватьSOAP = Ложь;
	КонецЕсли;
	
	ВыгрузкаЗапросаОперацииСЭДО = ОбщегоНазначенияКлиентСервер.
		СкопироватьСтруктуру(ЗапросПолученияДанных);
	ВыгрузкаЗапросаОперацииСЭДО.Вставить("РегистрационныйНомерФСС", РегистрационныйНомерФСС);
	Если ВыгрузкаЗапросаОперацииСЭДО.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		ДополнительныеПараметры.Вставить("СодержимоеПодписанногоЗапросаSOAP",
			ВыгрузкаЗапросаОперацииСЭДО.СодержимоеПодписанногоЗапросаSOAP);
	КонецЕсли;
	
	ДополнительныеПараметрыОповещения = ВыполняемоеОповещение.ДополнительныеПараметры;
	Если ТипЗнч(ДополнительныеПараметрыОповещения) = Тип("Структура")
		И ДополнительныеПараметрыОповещения.Свойство("ОтображатьСостояние") Тогда
		
		ОтображатьСостояние = ДополнительныеПараметрыОповещения.ОтображатьСостояние;
		
	КонецЕсли;
	
	Если ОтображатьСостояние Тогда
		Состояние(НСтр("ru = 'СЭДО СФР'"), , НСтр("ru='Формирование запроса для сервера...'"));
	КонецЕсли;
	
	Если ПодписатьSOAP Тогда
		
		// Подписание запроса.
		Оповещение = Новый ОписаниеОповещения("ПолучитьДанныеСЭДОФССПослеПодписиSOAP",
			ЭтотОбъект,
			ДополнительныеПараметры);
		АдресПодписанногоSOAPОперацииСЭДОФСС(
			Оповещение,
			ВыгрузкаЗапросаОперацииСЭДО,
			ЗашифроватьSOAP,
			Истина,
			ЗадаватьВопросОТестовомСервере,
			СвойстваОбменаПоСЭДОЧерезОператора);
		Возврат;
		
	Иначе
		
		АдресПодписаного = ПоместитьВоВременноеХранилище(
			ВыгрузкаЗапросаОперацииСЭДО.ТекстЗапроса,
			Новый УникальныйИдентификатор);
			
		Результат = РезультатПолучитьДанныеСЭДОФССПослеПодписиSOAP();
		Результат.АдресПодписаногоЗапросаSOAP = АдресПодписаного;
		Результат.АдресЗашифрованногоЗапросаSOAP = АдресПодписаного;
		ПолучитьДанныеСЭДОФССПослеПодписиSOAP(Результат, ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеСЭДОФССПослеПодписиSOAP(
	РезультатПодписиШифрования,
	ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОрганизацияСсылка 					= ДополнительныеПараметры.ОрганизацияСсылка;
	Операция 							= ДополнительныеПараметры.Операция;
	Результат 							= ДополнительныеПараметры.Результат;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	ИдентификаторыСообщений 			= ДополнительныеПараметры.ИдентификаторыСообщений;
	
	Если ДополнительныеПараметры.Свойство("ОтображатьСостояние") Тогда
		ОтображатьСостояние		= ДополнительныеПараметры.ОтображатьСостояние;
	Иначе
		ОтображатьСостояние		= Истина;
	КонецЕсли;
	
	ОписаниеОшибки = "";
	Если ЗначениеЗаполнено(РезультатПодписиШифрования)
		И РезультатПодписиШифрования.Свойство("ОписаниеОшибки") Тогда
		ОписаниеОшибки = РезультатПодписиШифрования.ОписаниеОшибки;
	КонецЕсли;
	
	ОтмененоПользователем = ЗначениеЗаполнено(РезультатПодписиШифрования)
		И РезультатПодписиШифрования.Свойство("ОтмененоПользователем")
		И РезультатПодписиШифрования.ОтмененоПользователем;
	
	Если НЕ ЗначениеЗаполнено(РезультатПодписиШифрования)
		ИЛИ ОтмененоПользователем
		ИЛИ НЕ ПустаяСтрока(ОписаниеОшибки) Тогда
			Если ОтмененоПользователем Тогда
				Результат.ОписаниеОшибки = НСтр("ru='Операция была отменена пользователем.'");
				Результат.ОтмененоПользователем = Истина;
			ИначеЕсли НЕ ПустаяСтрока(ОписаниеОшибки) Тогда
				Результат.ОписаниеОшибки = ОписаниеОшибки;
			Иначе
				Результат.ОписаниеОшибки =
					НСтр("ru = 'Произошла ошибка при выполнении криптоопераций над запросом к серверу СЭДО. Подробности см. в журнале регистрации.'");
			КонецЕсли;
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
	КонецЕсли;
	
	ПодписанноеСообщение64 = ?(РезультатПодписиШифрования.Свойство("ПодписанноеСообщение64"),
		РезультатПодписиШифрования.ПодписанноеСообщение64, "");
	ДополнительныеПараметры.Вставить("ПодписанноеСообщение64", ПодписанноеСообщение64);
	
	Если ОтображатьСостояние Тогда
		Состояние(НСтр("ru = 'СЭДО СФР'"), , НСтр("ru='Получение данных с сервера...'"));
	КонецЕсли;
	
	// Отправка запроса.
	МЧДТолькоПодписание = (ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДТолькоПодписание") = Истина);
	Если МЧДТолькоПодписание Тогда
		РезультатОтправки = Новый Структура;
		РезультатОтправки.Вставить("АдресЗашифрованногоОтветаSOAP", 				Неопределено);
		РезультатОтправки.Вставить("АдресЗашифрованногоОтветаSOAPДвоичныеДанные", 	Неопределено);
		РезультатОтправки.Вставить("АдресРесурса", 									"");
		РезультатОтправки.Вставить("АдресСервера", 									"");
		РезультатОтправки.Вставить("КодСостояния", 									0);
		РезультатОтправки.Вставить("ОшибкаСоединения", 								Ложь);
		РезультатОтправки.Вставить("Выполнено", 									Истина);
		РезультатОтправки.Вставить("ТребуетсяРасшифровкаОтвета", 					Ложь);
		
	Иначе
		РезультатОтправки = ОтправитьЗапросСервисуСЭДОФССНаСервере(
			ОрганизацияСсылка,
			Операция,
			РезультатПодписиШифрования.АдресЗашифрованногоЗапросаSOAP,
			СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
	КонецЕсли;
	
	АдресПодписаногоЗапросаSOAP = РезультатПодписиШифрования.АдресПодписаногоЗапросаSOAP;
	АдресЗашифрованногоОтветаSOAP = РезультатОтправки.АдресЗашифрованногоОтветаSOAP;
	АдресЗашифрованногоОтветаSOAPДвоичныеДанные = РезультатОтправки.АдресЗашифрованногоОтветаSOAPДвоичныеДанные;
	
	Результат.АдресПодписаногоЗапросаSOAP = АдресПодписаногоЗапросаSOAP;
	Результат.АдресРасшифрованногоОтветаSOAP = АдресЗашифрованногоОтветаSOAP;
	Результат.АдресРесурса = РезультатОтправки.АдресРесурса;
	Результат.АдресСервера = РезультатОтправки.АдресСервера;
	Результат.КодСостояния = РезультатОтправки.КодСостояния;
	Результат.АдресЗашифрованногоЗапросаSOAP = РезультатПодписиШифрования.АдресЗашифрованногоЗапросаSOAP;
	Результат.ОшибкаСоединения = РезультатОтправки.ОшибкаСоединения;
	
	Если НЕ РезультатОтправки.Выполнено Тогда
		
		Если РезультатОтправки.ТребуетсяРасшифровкаОтвета Тогда
			
			// Необходимо расшифровать ответ сервера.
			ДополнительныеПараметры.ОшибкаОтправкиНаСервер = Истина;
			
		Иначе
			
			Если НЕ РезультатОтправки.ОшибкаСоединения Тогда
				
				// Разбираем текст ошибки.
				РезультатРазбора = РазобратьОшибкуСервераСЭДОФСС(
					ПолучитьИзВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP));
				Если РезультатРазбора.Выполнено Тогда
					ОписаниеОшибки = РезультатРазбора.ОписаниеОшибки;
				Иначе
					ОписаниеОшибки = РезультатОтправки.ОписаниеОшибки;
				КонецЕсли;
				
				УдалитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAPДвоичныеДанные);
				
			Иначе
				
				ОписаниеОшибки = РезультатОтправки.ОписаниеОшибки;
				
			КонецЕсли;
			
			// Сохраняем данные запроса в случае ошибки.
			ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(ОрганизацияСсылка, Операция);
			ПараметрыЗапроса.ОписаниеОшибки 		= ОписаниеОшибки;
			ПараметрыЗапроса.АдресЗапросаSOAP 		= АдресПодписаногоЗапросаSOAP;
			Если НЕ РезультатОтправки.ОшибкаСоединения
				ИЛИ ЭтоАдресВременногоХранилища(АдресЗашифрованногоОтветаSOAP) Тогда
				ПараметрыЗапроса.АдресОтветаSOAP = АдресЗашифрованногоОтветаSOAP;
			КонецЕсли;
			ПараметрыЗапроса.АдресРесурса 			= Результат.АдресРесурса;
			ПараметрыЗапроса.АдресСервера 			= Результат.АдресСервера;
			ПараметрыЗапроса.КодСостояния 			= Результат.КодСостояния;
			ПараметрыЗапроса.ТипВзаимодействияСЭДО 	= ТипВзаимодействия;
			ПараметрыЗапроса.УдалятьИзХранилища 	= Ложь;
			ОбменЧерезОператора 	= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
			ПараметрыЗапроса.ОбменЧерезОператора 	= ОбменЧерезОператора;
			Если Операция = ОперацияПолученияСообщенияСЭДОФСС() Тогда
				ПараметрыЗапроса.ИдентификаторФСС = ИдентификаторыСообщений[0];
			КонецЕсли;
			
			Если НЕ Операция = ОперацияОтправкиСообщенияСЭДОФСС() Тогда
				// Данные запроса сохраняются в другом методе.
				СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
			КонецЕсли;
			
			Результат.ОписаниеОшибки = ОписаниеОшибки;
			
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		АдресЗашифрованногоОтветаSOAP = РезультатОтправки.АдресЗашифрованногоОтветаSOAPДвоичныеДанные;
	Иначе
		АдресЗашифрованногоОтветаSOAP = РезультатОтправки.АдресЗашифрованногоОтветаSOAP;
	КонецЕсли;
	
	Расшифровать = Истина;
	Если НЕ РезультатОтправки.ТребуетсяРасшифровкаОтвета Тогда
		Если Операция = ОперацияОтправкиСообщенияСЭДОФСС()
			ИЛИ Операция = ОперацияПолученияСпискаВходящихСЭДОФСС() Тогда
				Расшифровать = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьДанныеСЭДОФССПослеПроверкиПодписиSOAP",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если МЧДТолькоПодписание Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПодписьВалидна", 					Истина);
		РезультатВыполнения.Вставить("АдресРасшифрованногоОтветаSOAP", 	Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		НастройкиВызова = Новый Структура;
		Если ДополнительныеПараметры.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
			НастройкиВызова.Вставить("СодержимоеПодписанногоЗапросаSOAP",
				ДополнительныеПараметры.СодержимоеПодписанногоЗапросаSOAP);
		КонецЕсли;
		
		ПроверитьПодписьSOAPОтветаЭЛНВФСС(
			Оповещение,
			ОрганизацияСсылка,
			АдресЗашифрованногоОтветаSOAP,
			Расшифровать,,,
			Истина,
			НастройкиВызова);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеСЭДОФССПослеПроверкиПодписиSOAP(РезультатПроверкиПодписи, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	Результат 					= ДополнительныеПараметры.Результат;
	ОшибкаОтправкиНаСервер 		= ДополнительныеПараметры.ОшибкаОтправкиНаСервер;
	ОрганизацияСсылка 			= ДополнительныеПараметры.ОрганизацияСсылка;
	Операция 					= ДополнительныеПараметры.Операция;
	ТипВзаимодействия 			= ДополнительныеПараметры.ТипВзаимодействия;
	РезультатФоновойЗагрузки 	= ДополнительныеПараметры.РезультатФоновойЗагрузки;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	Если НЕ ЗначениеЗаполнено(РезультатПроверкиПодписи)
		ИЛИ (ТипЗнч(РезультатПроверкиПодписи) = Тип("Структура")
			И РезультатПроверкиПодписи.Свойство("Выполнено")
			И НЕ РезультатПроверкиПодписи.Выполнено) Тогда
			
		Если ЗначениеЗаполнено(РезультатПроверкиПодписи)
			И РезультатПроверкиПодписи.Свойство("ОписаниеОшибки") Тогда
			Результат.ОписаниеОшибки = РезультатПроверкиПодписи.ОписаниеОшибки;
		Иначе
			Результат.ОписаниеОшибки = НСтр("ru='Не удалось проверить подпись ответа сервера СЭДО.'");
		КонецЕсли;
		
		// Удаляем из регистра фоновой загрузки данные о сообщении при возникновении ошибки с криптографией.
		Если ЗначениеЗаполнено(РезультатФоновойЗагрузки) Тогда
			УбратьЗапросИзОчередиФоновогоЗадания(РезультатФоновойЗагрузки);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
		
	КонецЕсли;
	
	Если НЕ РезультатПроверкиПодписи.ПодписьВалидна Тогда
		// Выводим сообщение, но продолжаем обмен (данные-то мы получили).
		ТекстОшибки = НСтр("ru = 'Подпись СЭДО СФР недействительна.'");
		Если РезультатПроверкиПодписи.Свойство("ОписаниеОшибки") Тогда
			ТекстОшибки = ТекстОшибки + Символы.ПС + РезультатПроверкиПодписи.ОписаниеОшибки;
		КонецЕсли;
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных СЭДО СФР'"),
			"Предупреждение",
			ТекстОшибки,,
			Истина);
		Результат.ОшибкаПроверкиПодписиЗаписана = Истина;
	КонецЕсли;
	
	Результат.АдресРасшифрованногоОтветаSOAP = РезультатПроверкиПодписи.АдресРасшифрованногоОтветаSOAP;
	
	Если ОшибкаОтправкиНаСервер Тогда
		
		// Ранее возникла ошибка при отправке на сервер.
		РезультатРазбора = РазобратьОшибкуСервераСЭДОФСС(
			ПолучитьИзВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP));
		Если РезультатРазбора.Выполнено Тогда
			ОписаниеОшибки = РезультатРазбора.ОписаниеОшибки;
		Иначе
			ОписаниеОшибки = НСтр("ru='Ошибка отправки запроса на сервер СЭДО.'");
		КонецЕсли;
		
		// Сохраняем данные запроса в случае ошибки.
		ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(ОрганизацияСсылка, Операция);
		ПараметрыЗапроса.АдресЗапросаSOAP 		= Результат.АдресПодписаногоЗапросаSOAP;
		ПараметрыЗапроса.АдресОтветаSOAP 		= Результат.АдресРасшифрованногоОтветаSOAP;
		ПараметрыЗапроса.АдресРесурса 			= Результат.АдресРесурса;
		ПараметрыЗапроса.АдресСервера 			= Результат.АдресСервера;
		ПараметрыЗапроса.КодСостояния 			= Результат.КодСостояния;
		ПараметрыЗапроса.ОписаниеОшибки 		= ОписаниеОшибки;
		ПараметрыЗапроса.ТипВзаимодействияСЭДО 	= ТипВзаимодействия;
		ПараметрыЗапроса.УдалятьИзХранилища 	= Ложь;
		ПараметрыЗапроса.ОбменЧерезОператора 	= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
		СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
		
		Результат.ОписаниеОшибки = ОписаниеОшибки;
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
		
	КонецЕсли;
	
	Результат.Вставить("ПодписанноеСообщение64", ДополнительныеПараметры.ПодписанноеСообщение64);
	
	Результат.ПодписьВалидна = РезультатПроверкиПодписи.ПодписьВалидна;
	Результат.Выполнено = Истина;
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Функция РезультатПолучитьДанныеСЭДОФСС()
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("АдресРасшифрованногоОтветаSOAP", Неопределено);
	Результат.Вставить("АдресПодписаногоЗапросаSOAP", Неопределено);
	Результат.Вставить("ОшибкаПроверкиПодписиЗаписана", Ложь);
	Результат.Вставить("ПодписьВалидна", Неопределено);
	Результат.Вставить("АдресРесурса", "");
	Результат.Вставить("АдресСервера", "");
	Результат.Вставить("КодСостояния", 0);
	Результат.Вставить("ОтмененоПользователем", Ложь);
	Результат.Вставить("АдресЗашифрованногоЗапросаSOAP", "");
	Результат.Вставить("ОшибкаСоединения", Ложь);
	Результат.Вставить("ОтветПолученФоновымЗаданием", Ложь);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ОтправитьСообщениеСЭДОФСС

&НаКлиенте
Процедура ОтправитьСообщениеСЭДОФСС(
	ПараметрыСообщения,
	ЗадаватьВопросОТестовомСервере = Истина,
	ОтображатьСостояние = Истина,
	ВыводитьДиалогСостоянияОтправки = Истина,
	СвойстваОбменаПоСЭДОЧерезОператора = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", 			Ложь);
	Результат.Вставить("ОписаниеОшибки", 		"");
	Результат.Вставить("ИдентификаторЗапроса", 	Неопределено);
	Результат.Вставить("ИдентификаторПакета", 	Неопределено);
	Результат.Вставить("ОбменЧерезОператора", 	Неопределено);
	
	ПараметрыСообщения.ТипСообщения = ЭлектронныйДокументооборотСФССКлиентСервер.ТипСообщенияСЭДОВСтроку(
		ПараметрыСообщения.ТипСообщения);
	
	Организация = ПараметрыСообщения.Организация;
	Операция = ОперацияОтправкиСообщенияСЭДОФСС();
	НастройкиВызова = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	ЭтоПользовательскийЗапрос = ПараметрыСообщения.ЭтоПользовательскийЗапрос;
	
	Если ПараметрыСообщения.Свойство("РегистрационныйНомерФСС") И ПараметрыСообщения.РегистрационныйНомерФСС <> "" Тогда
		РегистрационныйНомерФСС = Строка(ПараметрыСообщения.РегистрационныйНомерФСС);
		ЗаполнитьЗначенияСвойств(НастройкиВызова, ПараметрыСообщения);
		Если ПараметрыСообщения.Свойство("СвойстваДляОбмена") И ПараметрыСообщения.СвойстваДляОбмена <> Неопределено Тогда
			ЗаполнитьЗначенияСвойств(НастройкиВызова, ПараметрыСообщения.СвойстваДляОбмена);
		КонецЕсли;
		
	Иначе
		НастройкиВызова.ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСЭДО(
			ПараметрыСообщения.ТипСообщения);
		ЭтоТипВзаимодействияОрганизацияСЭДО = (НастройкиВызова.ТипВзаимодействия =
			ДокументооборотСФССКлиентСервер.ТипВзаимодействияОрганизацияСЭДО());
		ПолучитьОГРН 			= ЭтоТипВзаимодействияОрганизацияСЭДО;
		ПолучитьИННКППиСНИЛС 	= ЭтоТипВзаимодействияОрганизацияСЭДО;
		СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
			Организация,,
			ПолучитьОГРН,,
			ПолучитьИННКППиСНИЛС);
		РегистрационныйНомерФСС = СвойстваОрганизации.РегистрационныйНомерФСС;
		РезультатПроверкиРегистрационногоНомераФСС = ПроверитьДлинуРегистрационныйНомерФСС(
			Результат,
			НастройкиВызова.ТипВзаимодействия,
			Организация);
		Если НЕ РезультатПроверкиРегистрационногоНомераФСС Тогда
			ВыполнитьОбработкуОповещения(ПараметрыСообщения.ОповещениеОбратногоВызова, Результат);
			Возврат;
		КонецЕсли;
		Если ЭтоТипВзаимодействияОрганизацияСЭДО Тогда
			ЗаполнитьЗначенияСвойств(НастройкиВызова, СвойстваОрганизации);
		КонецЕсли;
	КонецЕсли;
	
	ТипыСообщений = ДокументооборотСФССКлиентСервер.ТипыСообщенийСЭДО(Истина);
	ЭтоПодписываемаяВыгрузка = (ПараметрыСообщения.ТипСообщения =
			ТипыСообщений.ТипСообщенияСозданиеМашиночитаемойДоверенностиФСС
		ИЛИ ПараметрыСообщения.ТипСообщения = ТипыСообщений.ТипСообщенияОтзывМашиночитаемойДоверенностиФСС
		ИЛИ ПараметрыСообщения.ТипСообщения = ТипыСообщений.ТипСообщенияЗапросСпискаМашиночитаемыхДоверенностейФСС
		ИЛИ ПараметрыСообщения.ТипСообщения = ТипыСообщений.ТипСообщенияЗапросМашиночитаемойДоверенностиФСС
		ИЛИ ПараметрыСообщения.ТипСообщения =
			ТипыСообщений.ТипСообщенияЗапросПолномочийУполномоченногоМашиночитаемойДоверенностиФСС);
	
	Если СвойстваОбменаПоСЭДОЧерезОператора = Неопределено Тогда
		СброситьБезПолучения = Истина;
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, СброситьБезПолучения);
		СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	КонецЕсли;
	ИдентификаторСообщенияСЭДО = ?(СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую, "",
		Строка(Новый УникальныйИдентификатор));
	НастройкиВызова.ОбменПоСЭДОНапрямую = СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	НастройкиВызова.ИдентификаторСообщенияСЭДО = ИдентификаторСообщенияСЭДО;
	Результат.ОбменЧерезОператора = НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	
	ЗапросДляПолученияДанных = ПараметрыЗапросаПолучитьДанныеСЭДОФСС();
	ЗапросДляПолученияДанных.Организация = Организация;
	ЗапросДляПолученияДанных.Операция = Операция;
	ЗапросДляПолученияДанных.ТекстЗапроса =
		ТекстЗапросаОтправкиСообщенияСЭДОФСС(
			РегистрационныйНомерФСС,
			ПараметрыСообщения.ТипСообщения,
			?(ЭтоПодписываемаяВыгрузка, Неопределено, ПараметрыСообщения.СодержимоеСообщения),
			НастройкиВызова);
	Если ЭтоПодписываемаяВыгрузка Тогда
		ЗапросДляПолученияДанных.Вставить("СодержимоеСообщения", ПараметрыСообщения.СодержимоеСообщения);
		ИдентификаторРодителя = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыСообщения,
			"ИдентификаторРодителя", "");
		ЗапросДляПолученияДанных.Вставить("ИдентификаторРодителя", ИдентификаторРодителя);
	КонецЕсли;
	ЗапросДляПолученияДанных.ИдентификаторыСообщений = ЗначениеВКоллекцию(ИдентификаторСообщенияСЭДО);
	ЗапросДляПолученияДанных.ТипыСообщений = ЗначениеВКоллекцию(ПараметрыСообщения.ТипСообщения);
	Если ПараметрыСообщения.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
		ЗапросДляПолученияДанных.Вставить("СодержимоеПодписанногоЗапросаSOAP",
			ПараметрыСообщения.СодержимоеПодписанногоЗапросаSOAP);
	КонецЕсли;
	Если ПараметрыСообщения.Свойство("ОтпечатокСертификатаПодписанта") Тогда
		ЗапросДляПолученияДанных.Вставить("ОтпечатокСертификатаПодписанта",
			ПараметрыСообщения.ОтпечатокСертификатаПодписанта);
	КонецЕсли;
		
	НастройкиЗаписиОтправки = ?(ПараметрыСообщения.Свойство("НастройкиЗаписиОтправки"),
		ПараметрыСообщения.НастройкиЗаписиОтправки, Неопределено);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 	ПараметрыСообщения.ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Результат", 					Результат);
	ДополнительныеПараметры.Вставить("ОписаниеОшибки", 				ПараметрыСообщения.ОписаниеОшибки);
	ДополнительныеПараметры.Вставить("Операция", 					Операция);
	ДополнительныеПараметры.Вставить("Организация", 				Организация);
	ДополнительныеПараметры.Вставить("ТипСообщения", 				ПараметрыСообщения.ТипСообщения);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 			НастройкиВызова.ТипВзаимодействия);
	ДополнительныеПараметры.Вставить("НастройкиЗаписиОтправки", 	НастройкиЗаписиОтправки);
	ДополнительныеПараметры.Вставить("РезультатОтправкиЗапроса", 				Неопределено);
	ДополнительныеПараметры.Вставить("АдресЗашифрованногоЗапросаSOAP", 			"");
	ДополнительныеПараметры.Вставить("АдресПодписаногоЗапросаSOAP",				"");
	ДополнительныеПараметры.Вставить("ВыводитьДиалогСостоянияОтправки", 		ВыводитьДиалогСостоянияОтправки);
	ДополнительныеПараметры.Вставить("ФормаДлительнойОперации",					Неопределено);
	ДополнительныеПараметры.Вставить("МаксимальноеКоличестовоПопытокОтправки", 	10);
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоСЭДОЧерезОператора", 		СвойстваОбменаПоСЭДОЧерезОператора);
	ДополнительныеПараметры.Вставить("ИдентификаторСообщенияСЭДО", 				ИдентификаторСообщенияСЭДО);
	ДополнительныеПараметры.Вставить("ЭтоПользовательскийЗапрос",				ЭтоПользовательскийЗапрос);
	
	Если ВыводитьДиалогСостоянияОтправки Тогда
		ПараметрыОжидания = Новый Структура();
		ПараметрыОжидания.Вставить("РежимДиалогаПовторить", Ложь);
		ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
			"Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.ОтправкаСообщенияСЭДО",
			ПараметрыОжидания, , , , , ,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ОтправитьСообщениеСЭДОФССПослеЗапроса", ЭтотОбъект, ДополнительныеПараметры);
	ПолучитьДанныеСЭДОФСС(
		Оповещение,
		ЗапросДляПолученияДанных,
		ЗадаватьВопросОТестовомСервере,
		НастройкиВызова.ТипВзаимодействия,
		ОтображатьСостояние,
		СвойстваОбменаПоСЭДОЧерезОператора);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьСообщениеСЭДОФССПослеЗапроса(РезультатОтправки, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	ШаблонОписанияОшибки 				= ДополнительныеПараметры.ОписаниеОшибки;
	Организация 						= ДополнительныеПараметры.Организация;
	Операция 							= ДополнительныеПараметры.Операция;
	ТипСообщения 						= ДополнительныеПараметры.ТипСообщения;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	НастройкиЗаписиОтправки 			= ДополнительныеПараметры.НастройкиЗаписиОтправки;
	ВыводитьДиалогСостоянияОтправки 	= ДополнительныеПараметры.ВыводитьДиалогСостоянияОтправки;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	ИдентификаторСообщенияСЭДО 			= ДополнительныеПараметры.ИдентификаторСообщенияСЭДО;
	ЭтоПользовательскийЗапрос			= ДополнительныеПараметры.ЭтоПользовательскийЗапрос;
	
	ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция);
	ПараметрыЗапроса.АдресЗапросаSOAP 			= РезультатОтправки.АдресПодписаногоЗапросаSOAP;
	ПараметрыЗапроса.АдресОтветаSOAP 			= РезультатОтправки.АдресРасшифрованногоОтветаSOAP;
	ПараметрыЗапроса.АдресРесурса 				= РезультатОтправки.АдресРесурса;
	ПараметрыЗапроса.АдресСервера 				= РезультатОтправки.АдресСервера;
	ПараметрыЗапроса.КодСостояния 				= РезультатОтправки.КодСостояния;
	ПараметрыЗапроса.ТипСообщенияСЭДО 			= ТипСообщения;
	ПараметрыЗапроса.ТипВзаимодействияСЭДО 		= ТипВзаимодействия;
	ПараметрыЗапроса.УдалятьИзХранилища 		= Истина;
	ПараметрыЗапроса.ОбменЧерезОператора 		= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	ПараметрыЗапроса.ЭтоПользовательскийЗапрос	= ЭтоПользовательскийЗапрос;
	
	ПодписанныйЗапросSOAP64 = "";
	Если РезультатОтправки.Выполнено Тогда
		МЧДТолькоПодписание = (ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДТолькоПодписание") = Истина);
		
		// Отправка была выполнена успешно.
		АдресРезультата = РезультатОтправки.АдресРасшифрованногоОтветаSOAP;
		Если МЧДТолькоПодписание Тогда
			ИдентификаторОтвета = Строка(Новый УникальныйИдентификатор);
			РезультатРазбора = Новый Структура;
			РезультатРазбора.Вставить("Выполнено", Истина);
			РезультатРазбора.Вставить("Идентификатор", ИдентификаторОтвета);
			
		Иначе
			ТекстОтвета = ПолучитьИзВременногоХранилища(АдресРезультата);
			РезультатРазбора = РазобратьОтветСЭДОФСССИдентификатором(ТекстОтвета);
		КонецЕсли;
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			// Ошибка разбора ответа.
			Шаблон = НСтр("ru='При отправке сообщения в СЭДО произошла ошибка:
			|%1'");
			ОписаниеОшибки = СтрШаблон(Шаблон, РезультатРазбора.ОписаниеОшибки);
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Отправка данных в СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки,,
				Истина);
				
			ПараметрыЗапроса.ОписаниеОшибки = ОписаниеОшибки;
			РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
		Иначе
			// Ответ успешно разобран.
			Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
				ПараметрыЗапроса.ИдентификаторПакетаФСС = РезультатРазбора.Идентификатор;
				РезультатРазбора.Идентификатор = ИдентификаторСообщенияСЭДО;
			КонецЕсли;
			РезультатОперации.ИдентификаторЗапроса = РезультатРазбора.Идентификатор;
			РезультатОперации.ИдентификаторПакета = ПараметрыЗапроса.ИдентификаторПакетаФСС;
			ПараметрыЗапроса.ИдентификаторФСС = РезультатРазбора.Идентификатор;
		КонецЕсли;
		
		Если РезультатРазбора.Выполнено И НастройкиЗаписиОтправки <> Неопределено И НЕ МЧДТолькоПодписание Тогда
			КороткоеИмяФайлаПакета = НастройкиЗаписиОтправки.КороткоеИмяФайлаПакета;
			Если СтрНайти(КороткоеИмяФайлаПакета, "%1") <> 0 Тогда
				КороткоеИмяФайлаПакета = СтрШаблон(
					КороткоеИмяФайлаПакета,
					РезультатОперации.ИдентификаторЗапроса);
			КонецЕсли;
			
			ЭтоАдресВременногоХранилища = Истина;
			ОтправкаСсылка = ДобавитьОтправкуФССНаСервере(
				ЭтоАдресВременногоХранилища,
				КороткоеИмяФайлаПакета,
				ПараметрыЗапроса.АдресЗапросаSOAP,
				РезультатОперации.ИдентификаторЗапроса,
				НастройкиЗаписиОтправки);
			РезультатОперации.Вставить("ОтправкаСсылка", ОтправкаСсылка);
			
		ИначеЕсли РезультатРазбора.Выполнено И МЧДТолькоПодписание Тогда
			ДанныеПодписаногоЗапросаSOAP = ПолучитьИзВременногоХранилища(РезультатОтправки.АдресПодписаногоЗапросаSOAP);
			ПодписанныйЗапросSOAP64 = Base64Строка(ДанныеПодписаногоЗапросаSOAP);
		КонецЕсли;
		
		// Сохраняем данные запроса в журнал запросов.
		СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
			Возврат;
		КонецЕсли;
		
	Иначе
		
		// При отправке возникла ошибка.
		ПараметрыЗапроса.ОписаниеОшибки = РезультатОтправки.ОписаниеОшибки;
		ПараметрыЗапроса.УдалятьИзХранилища = Ложь;
		// Сохраняем данные запроса в журнал запросов.
		СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
		
		Если ПриВыполненииЗапросаВозниклаОшибкиСоединения(ПараметрыЗапроса)
			И ВыводитьДиалогСостоянияОтправки
			И СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			// Возникала ошибка соединения с сервером СЭДО.
			ДополнительныеПараметры.АдресЗашифрованногоЗапросаSOAP = РезультатОтправки.АдресЗашифрованногоЗапросаSOAP;
			ДополнительныеПараметры.АдресПодписаногоЗапросаSOAP = РезультатОтправки.АдресПодписаногоЗапросаSOAP;
			ДополнительныеПараметры.РезультатОтправкиЗапроса = РезультатОтправки;
			ОтправитьСообщениеСЭДОВФоне(ДополнительныеПараметры);
			Возврат;
		КонецЕсли;
		
		// Закрываем диалог отправки.
		Если ВыводитьДиалогСостоянияОтправки Тогда
			ЗакрытьФормуДлительнойОперации(ДополнительныеПараметры.ФормаДлительнойОперации);
		КонецЕсли;
		
		ОписаниеОшибки = СтрШаблон(ШаблонОписанияОшибки, РезультатОтправки.ОписаниеОшибки);
		РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
		Возврат;
		
	КонецЕсли;
	
	// Отправка была выполнена успешно.
	Если ВыводитьДиалогСостоянияОтправки Тогда
		ЗакрытьФормуДлительнойОперации(ДополнительныеПараметры.ФормаДлительнойОперации);
	КонецЕсли;
	
	ПодписанноеСообщение64 = ?(РезультатОтправки.Свойство("ПодписанноеСообщение64"), РезультатОтправки.ПодписанноеСообщение64, "");
	РезультатОперации.Вставить("ПодписанноеСообщение64", 			ПодписанноеСообщение64);
	РезультатОперации.Вставить("ПодписанныйЗапросSOAP64", 			ПодписанныйЗапросSOAP64);
	
	РезультатОперации.Выполнено = Истина;
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
	
КонецПроцедуры

#КонецОбласти

#Область ОтправитьСообщениеСЭДОВФоне

&НаКлиенте
Процедура ОтправитьСообщениеСЭДОВФоне(ДополнительныеПараметры)
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация 						= ДополнительныеПараметры.Организация;
	Операция 							= ДополнительныеПараметры.Операция;
	АдресЗашифрованногоЗапросаSOAP 		= ДополнительныеПараметры.АдресЗашифрованногоЗапросаSOAP;
	АдресПодписаногоЗапросаSOAP			= ДополнительныеПараметры.АдресПодписаногоЗапросаSOAP;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	ТипСообщения 						= ДополнительныеПараметры.ТипСообщения;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	МаксимальноеКоличестовоПопытокОтправки = ДополнительныеПараметры.МаксимальноеКоличестовоПопытокОтправки;
	
	Если НЕ ДополнительныеПараметры.Свойство("НомерПопытки")
		ИЛИ (ДополнительныеПараметры.Свойство("НомерПопытки")
			И ДополнительныеПараметры.НомерПопытки = 0) Тогда
			
		Если НЕ ДополнительныеПараметры.Свойство("НомерПопытки") Тогда
			ДополнительныеПараметры.Вставить("НомерПопытки", 1);
			ДополнительныеПараметры.Вставить("ВсегоПопыток", 1);
		Иначе
			ПараметрыОжидания = Новый Структура();
			ПараметрыОжидания.Вставить("РежимДиалогаПовторить", Ложь);
			ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
				"Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.ОтправкаСообщенияСЭДО",
				ПараметрыОжидания, , , , , ,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		КонецЕсли;
		
		ТекстСообщения = НСтр("ru='Скорее всего, сервер СФР перегружен.'");
		Шаблон = НСтр("ru='Попытка отправки %1 из %2...'");
		ТекстПопытка = СтрШаблон(Шаблон, 2, МаксимальноеКоличестовоПопытокОтправки);
		ТекстСообщения = СтроковыеФункцииКлиент.ФорматированнаяСтрока("<b>" + ТекстПопытка + "</b>" + Символы.ПС + ТекстСообщения);
		ЭлементДекорации = ДополнительныеПараметры.ФормаДлительнойОперации.Элементы.ДекорацияПоясняющийТекстДлительнойОперации;
		ЭлементДекорации.Заголовок = ТекстСообщения;
		ДополнительныеПараметры.ФормаДлительнойОперации.Элементы.ФормаЗакрыть.Видимость = Истина;
		
	КонецЕсли;
	
	ДополнительныеПараметры.НомерПопытки = ДополнительныеПараметры.НомерПопытки + 1;
	ДополнительныеПараметры.ВсегоПопыток = ДополнительныеПараметры.ВсегоПопыток + 1;
	
	НомерПопытки = ДополнительныеПараметры.НомерПопытки;
	ВсегоПопыток = ДополнительныеПараметры.ВсегоПопыток;
	
	ТекстСообщения = НСтр("ru='Скорее всего, сервер СФР перегружен.'");
	Шаблон = НСтр("ru='Попытка отправки %1 из %2...'");
	ТекстПопытка = СтрШаблон(Шаблон, НомерПопытки, МаксимальноеКоличестовоПопытокОтправки);
	ТекстСообщения = СтроковыеФункцииКлиент.ФорматированнаяСтрока("<b>" + ТекстПопытка + "</b>" + Символы.ПС + ТекстСообщения);
	ЭлементДекорации = ДополнительныеПараметры.ФормаДлительнойОперации.Элементы.ДекорацияПоясняющийТекстДлительнойОперации;
	ЭлементДекорации.Заголовок = ТекстСообщения;
	ДополнительныеПараметры.ФормаДлительнойОперации.ОбновитьОтображениеДанных();
	
	ДлительнаяОперация = ДлительнаяОперацияОтправкиЗапросаСЭДО(Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP,
		СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
		
	Оповещение = Новый ОписаниеОповещения("ОтправитьСообщениеСЭДОВФонеПослеОтправкиЗапроса", ЭтотОбъект, ДополнительныеПараметры);
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
	ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, Оповещение, ПараметрыОжидания);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьСообщениеСЭДОВФонеПослеОтправкиЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация 						= ДополнительныеПараметры.Организация;
	Операция 							= ДополнительныеПараметры.Операция;
	АдресЗашифрованногоЗапросаSOAP 		= ДополнительныеПараметры.АдресЗашифрованногоЗапросаSOAP;
	АдресПодписаногоЗапросаSOAP			= ДополнительныеПараметры.АдресПодписаногоЗапросаSOAP;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	ТипСообщения 						= ДополнительныеПараметры.ТипСообщения;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	НомерПопытки 						= ДополнительныеПараметры.НомерПопытки;
	ВсегоПопыток 						= ДополнительныеПараметры.ВсегоПопыток;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	МаксимальноеКоличестовоПопытокОтправки = ДополнительныеПараметры.МаксимальноеКоличестовоПопытокОтправки;
	
	Если Результат.Статус = "Выполнено" Тогда
		НовыйРезультатОтправки = ПолучитьИзВременногоХранилища(Результат.АдресРезультата);
		Если НЕ ПустаяСтрока(НовыйРезультатОтправки.ТекстЗашифрованногоОтветаSOAP) Тогда
			Адрес = ПоместитьВоВременноеХранилище(НовыйРезультатОтправки.ТекстЗашифрованногоОтветаSOAP,
				Новый УникальныйИдентификатор);
		Иначе
			Адрес = "";
		КонецЕсли;
		НовыйРезультатОтправки.АдресЗашифрованногоОтветаSOAP = Адрес;
		Если ЗначениеЗаполнено(НовыйРезультатОтправки.ДвоичныеДанныеЗашифрованногоОтветаSOAP) Тогда
			Адрес = ПоместитьВоВременноеХранилище(НовыйРезультатОтправки.ДвоичныеДанныеЗашифрованногоОтветаSOAP,
				Новый УникальныйИдентификатор);
		Иначе
			Адрес = "";
		КонецЕсли;
		НовыйРезультатОтправки.АдресЗашифрованногоОтветаSOAPДвоичныеДанные = Адрес;
	Иначе
		ЗакрытьФормуДлительнойОперации(ДополнительныеПараметры.ФормаДлительнойОперации);
		РезультатОперации.ОписаниеОшибки = НСтр("ru='Ошибка выполнения фоновой отправки на сервер СЭДО СФР.'");;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
		Возврат;
	КонецЕсли;

	ФормаОткрыта = ТипЗнч(ДополнительныеПараметры.ФормаДлительнойОперации) = ТипФормыКлиентскогоПриложения()
		И ДополнительныеПараметры.ФормаДлительнойОперации.Открыта();
	Если НЕ ФормаОткрыта Тогда
		// Форма закрыта, пользователь нажал отмену.
		РезультатОперации.ОписаниеОшибки = НовыйРезультатОтправки.ОписаниеОшибки;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
		Возврат;
	КонецЕсли;
		
	ДополнительныеПараметры.РезультатОтправкиЗапроса = НовыйРезультатОтправки;
	
	ДополнительныеПараметры.ФормаДлительнойОперации.ОбновитьОтображениеДанных();
	
	// Сохраняем данные запроса.
	ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция);
	ПараметрыЗапроса.АдресЗапросаSOAP 		= АдресПодписаногоЗапросаSOAP;
	ПараметрыЗапроса.АдресОтветаSOAP 		= НовыйРезультатОтправки.АдресЗашифрованногоОтветаSOAP;
	ПараметрыЗапроса.АдресРесурса 			= НовыйРезультатОтправки.АдресРесурса;
	ПараметрыЗапроса.АдресСервера 			= НовыйРезультатОтправки.АдресСервера;
	ПараметрыЗапроса.КодСостояния 			= НовыйРезультатОтправки.КодСостояния;
	ПараметрыЗапроса.ТипСообщенияСЭДО 		= ТипСообщения;
	ПараметрыЗапроса.ТипВзаимодействияСЭДО 	= ТипВзаимодействия;
	ПараметрыЗапроса.УдалятьИзХранилища 	= Ложь;
	ПараметрыЗапроса.ОбменЧерезОператора 	= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	ПараметрыЗапроса.ОписаниеОшибки 		= НовыйРезультатОтправки.ОписаниеОшибки;
	
	Если ПриВыполненииЗапросаВозниклаОшибкиСоединения(НовыйРезультатОтправки)
		И МаксимальноеКоличестовоПопытокОтправки > НомерПопытки Тогда
		// Возникла ошибка соединения, но максимальное количество попыток не превышено.
		ДополнительныеПараметры.РезультатОтправкиЗапроса = НовыйРезультатОтправки;
		ОтправитьСообщениеСЭДОВФоне(ДополнительныеПараметры);
		СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
		Возврат;
	КонецЕсли;
	
	Если НовыйРезультатОтправки.Выполнено Тогда
		
		// Был получен ответ сервера СЭДО, запускаем расшифровку ответа.
		ЗакрытьФормуДлительнойОперации(ДополнительныеПараметры.ФормаДлительнойОперации);
		
		РезультатФоновойЗагрузки = РезультатПолучитьДанныеСЭДОФСС();
		РезультатФоновойЗагрузки.АдресЗашифрованногоЗапросаSOAP = АдресЗашифрованногоЗапросаSOAP;
		АдресЗашифрованногоОтветаSOAP = НовыйРезультатОтправки.АдресЗашифрованногоОтветаSOAP; 
		ЗаполнитьЗначенияСвойств(РезультатФоновойЗагрузки, НовыйРезультатОтправки);
		РезультатФоновойЗагрузки.Вставить("АдресЗашифрованногоОтветаSOAP", АдресЗашифрованногоОтветаSOAP);
		РезультатФоновойЗагрузки.АдресПодписаногоЗапросаSOAP = АдресПодписаногоЗапросаSOAP;
		
		ЗапросДляПолученияДанных = ПараметрыЗапросаПолучитьДанныеСЭДОФСС();
		ЗапросДляПолученияДанных.Организация = Организация;
		ЗапросДляПолученияДанных.Операция = Операция;
		ЗапросДляПолученияДанных.РезультатФоновойЗагрузки = РезультатФоновойЗагрузки;
		
		Оповещение = Новый ОписаниеОповещения("ОтправитьСообщениеСЭДОФССПослеЗапроса", ЭтотОбъект, ДополнительныеПараметры);
		ПолучитьДанныеСЭДОФСС(
			Оповещение,
			ЗапросДляПолученияДанных,
			Ложь,
			ТипВзаимодействия,
			Истина,
			СвойстваОбменаПоСЭДОЧерезОператора);
		Возврат;
		
	КонецЕсли;
	
	// Не удалось отпавить сообщение после максимального количества попыток.
	СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
	ЗакрытьФормуДлительнойОперации(ДополнительныеПараметры.ФормаДлительнойОперации);
	
	// Открывается новая форма с кнопкой повтора отправки.
	ПараметрыОжидания = Новый Структура();
	ТекстСообщенияЖирным = НСтр("ru='К сожалению, не удалось отправить документ из-за недоступности сервера СФР.'");
	ТекстСообщения = НСтр("ru='Вы можете повторить отправку сейчас или вернуться к ней позже.'")
		+ Символы.ПС + НСтр("ru='Как правило, сервер СФР менее загружен с 20:00 по 08:00 (МСК).'");
	ТекстСообщения = СтроковыеФункцииКлиент.ФорматированнаяСтрока(
		"<b>" + ТекстСообщенияЖирным + "</b>" + Символы.ПС
		+ Символы.ПС + ТекстСообщения);
	ПараметрыОжидания.Вставить("ТекстСообщения", ТекстСообщения);
	ПараметрыОжидания.Вставить("РежимДиалогаПовторить", Истина);
	Оповещение = Новый ОписаниеОповещения("ОтправитьСообщениеСЭДОВФонеПослеДиалогаПовторить", ЭтотОбъект, ДополнительныеПараметры);
	ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
		"Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.ОтправкаСообщенияСЭДО",
		ПараметрыОжидания, , , , ,Оповещение, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	ЭлементДекорации = ДополнительныеПараметры.ФормаДлительнойОперации.Элементы.ДекорацияПоясняющийТекстДлительнойОперации;
	ЭлементДекорации.Заголовок = ТекстСообщения;
	ДополнительныеПараметры.ФормаДлительнойОперации.ОбновитьОтображениеДанных();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьСообщениеСЭДОВФонеПослеДиалогаПовторить(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 		= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОтправки 				= ДополнительныеПараметры.РезультатОтправкиЗапроса;
	РезультатОперации 				= ДополнительныеПараметры.Результат;
	
	Если Результат = НСтр("ru='Повторить'") Тогда
		ДополнительныеПараметры.НомерПопытки = 0;
		ОтправитьСообщениеСЭДОВФоне(ДополнительныеПараметры);
		Возврат;
	КонецЕсли;

	РезультатОперации.ОписаниеОшибки = РезультатОтправки.ОписаниеОшибки;
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ДлительнаяОперацияОтправкиЗапросаСЭДО(
		Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP,
		ОбменПоСЭДОНапрямую = Неопределено)
	
	ТекстЗашифрованногоЗапросаSOAP = ПолучитьИзВременногоХранилища(АдресЗашифрованногоЗапросаSOAP);
	
	ПараметрыПроцедуры 	= Новый Структура;
	ПараметрыПроцедуры.Вставить("Организация", 						Организация);
	ПараметрыПроцедуры.Вставить("Операция", 						Операция);
	ПараметрыПроцедуры.Вставить("ТекстЗашифрованногоЗапросаSOAP", 	ТекстЗашифрованногоЗапросаSOAP);
	ПараметрыПроцедуры.Вставить("ОбменПоСЭДОНапрямую", 				ОбменПоСЭДОНапрямую);
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(Новый УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Отправка запроса к серверу СЭДО'");
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	
	Возврат ДлительныеОперации.ВыполнитьВФоне(
		"Обработки.ДокументооборотСКонтролирующимиОрганами.ОтправитьЗапросСервисуСЭДОФССФоновое",
		ПараметрыПроцедуры,
		ПараметрыВыполнения);
	
КонецФункции

#КонецОбласти

#Область ПолучитьВходящиеСообщенияСЭДОФСС

&НаКлиенте
Процедура ПолучитьВходящиеСообщенияСЭДОФСС(
		ОповещениеОбратногоВызова,
		Организации,
		ЗадаватьВопросОТестовомСервере = Истина,
		ПоказыватьДлительнуюОперацию = Истина,
		ТипВзаимодействия = Неопределено,
		ТипСообщенияОтвета = Неопределено,
		ОтображатьСостояние = Истина) Экспорт
		
	Если НЕ ТипЗнч(Организации) = Тип("Массив") Тогда
		Организации = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Организации);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеСообщенийПоОрганизациям", Новый Соответствие);
	Результат.Вставить("БылиОшибки", Ложь);
	Результат.Вставить("ОшибкиПоОрганизациям", Новый Соответствие);
	Результат.Вставить("РезультатыПоОрганизациям", Новый Соответствие);
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 		ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Результат", 						Результат);
	ДополнительныеПараметры.Вставить("Организации", 					Организации);
	ДополнительныеПараметры.Вставить("ИндексОрганизации", 				0);
	ДополнительныеПараметры.Вставить("ФормаДлительнойОперации", 		Неопределено);
	ДополнительныеПараметры.Вставить("ЗадаватьВопросОТестовомСервере", 	ЗадаватьВопросОТестовомСервере);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 				ТипВзаимодействия);
	ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",				ТипСообщенияОтвета);
	ДополнительныеПараметры.Вставить("ОтображатьСостояние",				ОтображатьСостояние);
	
	Если ПоказыватьДлительнуюОперацию Тогда
		ПараметрыОжидания = Новый Структура();
		ПараметрыОжидания.Вставить("ТекстСообщения",
			НСтр("ru = 'Получение входящих сообщений СЭДО СФР'"));
		ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
			"ОбщаяФорма.ДлительнаяОперация", ПараметрыОжидания);
	КонецЕсли;
	
	СброситьБезПолучения = Истина;
	СвойстваОбменаПоСЭДОЧерезОператора(Организации, СброситьБезПолучения);
	
	ПолучитьВходящиеСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьВходящиеСообщенияСЭДОФССЦикл(ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 			= ДополнительныеПараметры.Результат;
	ИндексОрганизации 			= ДополнительныеПараметры.ИндексОрганизации;
	Организации 				= ДополнительныеПараметры.Организации;
	Результат 					= ДополнительныеПараметры.Результат;
	ФормаДлительнойОперации 	= ДополнительныеПараметры.ФормаДлительнойОперации;
	ТипВзаимодействия 			= ДополнительныеПараметры.ТипВзаимодействия;
	
	Если ИндексОрганизации >= Организации.Количество() Тогда
		// Закроем окно заставки длительной операции, если она была открыта.
		ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
		// Анализ ошибок и вывод диалога о повторном автоматическом получении в случае необходимости.
		АнализОшибокПолученияСообщений(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Организация = Организации[ИндексОрганизации];
	
	Если НЕ ФормаДлительнойОперации = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Получение входящих сообщений СЭДО по организации: '") + Организация
			+ Символы.ПС + НСтр("ru = 'Пожалуйста, подождите...'");
		ЭлементДекорации = ФормаДлительнойОперации.Элементы.ДекорацияПоясняющийТекстДлительнойОперации;
		ЭлементДекорации.Заголовок = ТекстСообщения;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьВходящиеСообщенияСЭДОФССЦиклПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	РезультатОперацииПроверки = Новый Структура;
	РезультатОперацииПроверки.Вставить("Выполнено", Ложь);
	РезультатОперацииПроверки.Вставить("ОписаниеОшибки", "");
	РезультатОперацииПроверки.Вставить("ИдентификаторЗапроса", Неопределено);
	
	РезультатПроверкиРегистрационногоНомераФСС = ПроверитьДлинуРегистрационныйНомерФСС(
			РезультатОперацииПроверки,
			ТипВзаимодействия,
			Организация);
	Если НЕ РезультатПроверкиРегистрационногоНомераФСС Тогда
		Результат.БылиОшибки = Истина;
		Ошибки = Новый Массив;
		Ошибки.Добавить(РезультатОперацииПроверки.ОписаниеОшибки);
		ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(РезультатОперацииПроверки.ОписаниеОшибки);
		Результат.ОшибкиПоОрганизациям.Вставить(Организация, Ошибки);
		ДополнительныеПараметры.ИндексОрганизации = ИндексОрганизации + 1;
		ПолучитьВходящиеСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
		Возврат;
	КонецЕсли;
	
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = Ложь;
	
	Если ОповещениеОбратногоВызова.ИмяПроцедуры = "ОбновитьРезультатКонкретнойОтправкиФССПослеОбменаСЭДО" Тогда
		МетаданныеВходящихСообщенийСЭДОФСС(
			Оповещение,
			Организация,,,
			ТипВзаимодействия,
			СброситьСвойстваОбменаПоСЭДОЧерезОператора);
	Иначе
		МетаданныеВходящихСообщенийСЭДОФССЗаПериод(
			Оповещение,
			Организация,,
			ТипВзаимодействия,
			СброситьСвойстваОбменаПоСЭДОЧерезОператора);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьВходящиеСообщенияСЭДОФССЦиклПослеЗапроса(
	РезультатВходящие,
	ДополнительныеПараметры) Экспорт
	
	Результат 						= ДополнительныеПараметры.Результат;
	ОповещениеОбратногоВызова 		= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексОрганизации 				= ДополнительныеПараметры.ИндексОрганизации;
	Организации 					= ДополнительныеПараметры.Организации;
	ФормаДлительнойОперации 		= ДополнительныеПараметры.ФормаДлительнойОперации;
	ЗадаватьВопросОТестовомСервере 	= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	ТипВзаимодействия 				= ДополнительныеПараметры.ТипВзаимодействия;
	
	Организация = Организации[ИндексОрганизации];
	
	Если НЕ РезультатВходящие.Выполнено Тогда
		Результат.БылиОшибки = Истина;
		Ошибки = Новый Массив;
		Ошибки.Добавить(РезультатВходящие.ОписаниеОшибки);
		Результат.ОшибкиПоОрганизациям.Вставить(Организация, Ошибки);
	КонецЕсли;
	
	НовыеСообщения = НовыеСообщенияСЭДОФСС(Организация,, ТипВзаимодействия);
	Идентификаторы = Новый Массив;
	ТипыВзаимодействий = Новый Массив;
	ТипСообщенияОтвета = Неопределено;
	Если ДополнительныеПараметры.Свойство("ТипСообщенияОтвета") Тогда
		ТипСообщенияОтвета = ДополнительныеПараметры.ТипСообщенияОтвета;
	КонецЕсли;
	Для каждого Сообщение Из НовыеСообщения Цикл
		Если ТипСообщенияОтвета = Неопределено Тогда
			Идентификаторы.Добавить(Сообщение.Идентификатор);
			ТипыВзаимодействий.Добавить(Сообщение.ТипВзаимодействия);
		Иначе
			Если ТипСообщенияОтвета = Сообщение.Тип Тогда
				Идентификаторы.Добавить(Сообщение.Идентификатор);
				ТипыВзаимодействий.Добавить(Сообщение.ТипВзаимодействия);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если НовыеСообщения.Количество() > 0 Тогда
		ФормаДлительнойОтправкиОткрыта = ДлительнаяОтправкаКлиент.ФормаДлительнойОтправкиЕстьСредиОткрытых(Истина);
		Если ФормаДлительнойОтправкиОткрыта Тогда
			АктивнаяФорма = ФормаДлительноеДействиеИзОткрытых();
			Если НЕ АктивнаяФорма = Неопределено И НЕ АктивнаяФорма.ЭтоРасшифровка Тогда
				// Вызываем смену этапа если это не расшифровка входящих.
				// При смене этапа во время расшифровки сбивается счетчик процентов.
				ДополнительныеПараметрыОповещения = Новый Структура();
				ДополнительныеПараметрыОповещения.Вставить("ИмяСобытия", 	"ПолучитьВходящиеСообщенияСЭДОФССЦиклПослеЗапроса");
				ДополнительныеПараметрыОповещения.Вставить("Орган", 		ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
				ДополнительныеПараметрыОповещения.Вставить("Отправка", 		ПредопределенноеЗначение("Справочник.ОтправкиФСС.ПустаяСсылка"));
				ДополнительныеПараметрыОповещения.Вставить("УчетнаяЗапись", Организация);
				ДополнительныеПараметрыОповещения.Вставить("ЕстьНовыеФСС",	Истина);
				Оповестить("Смена этапа обмена", ДополнительныеПараметрыОповещения);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = Ложь;
	
	// Получаем содержимое входящих сообщений по организации.
	Оповещение = Новый ОписаниеОповещения("ПолучитьВходящиеСообщенияСЭДОФССЦиклПослеПолученияСообщений",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПолучитьСообщенияСЭДОФСС(
		Оповещение,
		Организация,
		Идентификаторы,
		Ложь,
		ЗадаватьВопросОТестовомСервере,
		ТипыВзаимодействий,
		СброситьСвойстваОбменаПоСЭДОЧерезОператора);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьВходящиеСообщенияСЭДОФССЦиклПослеПолученияСообщений(
	РезультатПолучитьСообщения,
	ДополнительныеПараметры) Экспорт
	
	Результат						= ДополнительныеПараметры.Результат;
	ОповещениеОбратногоВызова		= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексОрганизации				= ДополнительныеПараметры.ИндексОрганизации;
	Организации						= ДополнительныеПараметры.Организации;
	ДанныеСообщенийПоОрганизациям	= Результат.ДанныеСообщенийПоОрганизациям;
	ФормаДлительнойОперации			= ДополнительныеПараметры.ФормаДлительнойОперации;
	РезультатыПоОрганизациям		= Результат.РезультатыПоОрганизациям;
	
	Организация = Организации[ИндексОрганизации];
	
	Если РезультатПолучитьСообщения.БылиОшибки Тогда
		Результат.БылиОшибки = Истина;
		Результат.ОшибкиПоОрганизациям.Вставить(Организация, РезультатПолучитьСообщения.Ошибки);
		Для каждого Ошибка из РезультатПолучитьСообщения.Ошибки Цикл
			ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(Ошибка);
		КонецЦикла;
	КонецЕсли;
	
	РезультатыПоОрганизациям.Вставить(Организация, РезультатПолучитьСообщения.РезультатыПолучения);
	ДанныеСообщенийПоОрганизациям.Вставить(Организация, РезультатПолучитьСообщения.ДанныеСообщений);
	
	Если НЕ РезультатПолучитьСообщения.ПолучитьПовторно Тогда
		ИндексОрганизации = ИндексОрганизации + 1;
	КонецЕсли;
	
	ДополнительныеПараметры.ИндексОрганизации = ИндексОрганизации;
	
	ПолучитьВходящиеСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьВходящиеСообщенияСЭДОФССПоОрганизациямПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	Организация					= ДополнительныеПараметры.Организация;
	КоличествоНеРасшифрованных	= ДополнительныеПараметры.КоличествоНеРасшифрованных;
	КоличествоРасшифрованных	= ДополнительныеПараметры.КоличествоРасшифрованных;
	
	КоличествоРасшифрованныхСЭДО = 0;
	КоличествоНеРасшифрованныхСЭДО = 0;
	Для каждого КлючЗначение Из Результат.ДанныеСообщенийПоОрганизациям Цикл
		КоличествоРасшифрованныхСЭДО = КоличествоРасшифрованныхСЭДО + КлючЗначение.Значение.Количество();
	КонецЦикла;
	
	ДополнительныеПараметры.КоличествоРасшифрованных = КоличествоРасшифрованных + КоличествоРасшифрованныхСЭДО - 1;
	
	Если Результат.БылиОшибки Тогда
		КоличествоЗагружаемых = 0;
		Для каждого КлючЗначение Из Результат.РезультатыПоОрганизациям Цикл
			Для каждого Запрос Из КлючЗначение.Значение Цикл
				КоличествоЗагружаемых = КоличествоЗагружаемых + Запрос.Идентификаторы.Количество();
			КонецЦикла;
		КонецЦикла;
		КоличествоНеРасшифрованныхСЭДО = КоличествоЗагружаемых - КоличествоРасшифрованныхСЭДО;
		ДополнительныеПараметры.КоличествоНеРасшифрованных = КоличествоНеРасшифрованных
			+ КоличествоНеРасшифрованныхСЭДО;
		
		Для каждого КлючЗначение Из Результат.ОшибкиПоОрганизациям Цикл
			Для каждого Ошибка из КлючЗначение.Значение Цикл
				ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(Ошибка);
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	Отбор = Новый Структура("Организация", Организация);
	РезультатОтбора = ТаблицаРасшифровкиПоОрганизациям.НайтиСтроки(Отбор);
	Если НЕ РезультатОтбора.Количество() = 0 Тогда 
		РезультатОтбора[0].КоличествоРасшифрованных = РезультатОтбора[0].КоличествоРасшифрованных
			+ КоличествоРасшифрованныхСЭДО - 1;
		РезультатОтбора[0].КоличествоНеРасшифрованных = РезультатОтбора[0].КоличествоНеРасшифрованных
			+ КоличествоНеРасшифрованныхСЭДО;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"РасшифроватьТребующиеРасшифровкуЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Функция ФормаДлительноеДействиеИзОткрытых() Экспорт
	
	Окна = ПолучитьОкна();
	
	Если Окна = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	Для Каждого ОткрытоеОкно Из Окна Цикл
		
		Если ТипЗнч(ОткрытоеОкно) <> Тип("ОкноКлиентскогоПриложения") Тогда
			Продолжить;
		КонецЕсли;
		
		ОткрытыеФормы = ОткрытоеОкно.Содержимое;
		
		Если ОткрытыеФормы.Количество() <> 1 Тогда
			Продолжить;
		КонецЕсли;
		
		ОткрытаяФорма = ОткрытыеФормы[0];
		ИмяОткрытойФормы      = ОткрытаяФорма.ИмяФормы;
		Открыта       = ОткрытаяФорма.Открыта();
		КраткоеИмя    = ДлительнаяОтправкаКлиентСервер.ИмяФормыПоПолномуИмени(ИмяОткрытойФормы);
		
		ЭтоФормаДлительноеДействие = КраткоеИмя = "ДлительноеДействие";
		
		Если ЭтоФормаДлительноеДействие И Открыта Тогда
			
			Возврат ОткрытаяФорма;
			
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИдентификаторыДляПовторнойЗагрузкиИзСЭДО()
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	ЗапросыДляПовторнойОтправкиСЭДО.Идентификатор КАК Идентификатор
	               |ИЗ
	               |	РегистрСведений.ЗапросыДляПовторнойОтправкиСЭДО КАК ЗапросыДляПовторнойОтправкиСЭДО";
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = Запрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	
	Идентификаторы = Новый Массив;
	Если НЕ РезультатЗапроса.Пустой() Тогда
		Идентификаторы = РезультатЗапроса.Выгрузить().ВыгрузитьКолонку("Идентификатор");
	КонецЕсли;
	
	Возврат Идентификаторы;
	
КонецФункции

&НаКлиенте
Функция АнализОшибокПолученияСообщений(ОповещениеОбратногоВызова, РезультатПолучения)
	
	// Вызов переопределяемого метода.
	ЭлектронныйДокументооборотСФССКлиентПереопределяемый.ПослеПолученияСообщенийСЭДО(
			РезультатПолучения);
	
	Если НЕ РезультатПолучения.БылиОшибки Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения);
		Возврат Истина;
	КонецЕсли;
	
	ИдентификаторыДляПовторнойЗагрузки = ИдентификаторыДляПовторнойЗагрузкиИзСЭДО();
	
	ЗапросыСОшибкойОтправки = Новый Массив;
	БылиОшибкиПриПолучении = Ложь;
	СчетчикВсего = 0;
	СчетчикОшибок = 0;
	КоличествоНовыхИдентификаторов = 0;
	Для каждого КлючИЗначение Из РезультатПолучения.РезультатыПоОрганизациям Цикл
		Организация = КлючИЗначение.Ключ;
		СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
		Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			// Для операторной схемы механизм фоновой загрузки не используется.
			Продолжить;
		КонецЕсли;
		Для каждого Результат Из КлючИЗначение.Значение Цикл 
			ДанныеЗапроса = Неопределено;
			Если НЕ Результат.Свойство("ДанныеЗапроса", ДанныеЗапроса) Тогда
				Продолжить;
			КонецЕсли;
			Если ПриВыполненииЗапросаВозниклаОшибкиСоединения(ДанныеЗапроса) Тогда
				БылиОшибкиПриПолучении = Истина;
				Для каждого Идентификатор Из ДанныеЗапроса.ИдентификаторыСообщений Цикл
					Если ИдентификаторыДляПовторнойЗагрузки.Найти(Идентификатор) = Неопределено Тогда
						КоличествоНовыхИдентификаторов = КоличествоНовыхИдентификаторов + 1;
					КонецЕсли;
				КонецЦикла;
				ЗапросыСОшибкойОтправки.Добавить(Результат.ДанныеЗапроса);
				СчетчикОшибок = СчетчикОшибок + 1;
			КонецЕсли;
			СчетчикВсего = СчетчикВсего + 1;
		КонецЦикла;
	КонецЦикла;
	
	Если БылиОшибкиПриПолучении Тогда

		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Запросы", ЗапросыСОшибкойОтправки);
		ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", ОповещениеОбратногоВызова);
		ДополнительныеПараметры.Вставить("РезультатПолучения", РезультатПолучения);
		ДополнительныеПараметры.Вставить("БылВыведенДиалогВключенияЗадания", Ложь);
		
		Если НЕ ВключеноЗаданиеФоновогоОбменаСЭДО()
			ИЛИ КоличествоНовыхИдентификаторов > 0 Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"АнализОшибокПолученияСообщенийПослеВопроса",
				ЭтотОбъект,
				ДополнительныеПараметры);
				
			ДополнительныеПараметры.БылВыведенДиалогВключенияЗадания = Истина;
			
			ПараметрыФормы = Новый Структура;
			ПараметрыФормы.Вставить("СчетчикВсего", СчетчикВсего);
			ПараметрыФормы.Вставить("СчетчикОшибок", СчетчикОшибок);
			ОткрытьФорму(
				"Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.ПодключениеЗаданияПолученияСообщенийСЭДО",
				ПараметрыФормы, , , , ,
				ОписаниеОповещения,
				РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
			Возврат Истина;
			
		Иначе
			
			АнализОшибокПолученияСообщенийПослеВопроса(Истина, ДополнительныеПараметры); 
			Возврат Истина;
			
		КонецЕсли;
		
	КонецЕсли;
		
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения);

КонецФункции

&НаСервереБезКонтекста
Функция ВключеноЗаданиеФоновогоОбменаСЭДО()
	
	Возврат ЭлектронныйДокументооборотСФСС.ВключеноЗаданиеФоновогоОбменаСЭДО();
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьДляДанныеФоновогоЗаданияЗагрузкиИзСЭДО(ЗапросыСОшибкойВыполнения)
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	
	УстановитьПривилегированныйРежим(Истина);
	
	НачатьТранзакцию();
	
	Счетчик = 0;
	Для каждого Запрос Из ЗапросыСОшибкойВыполнения Цикл
		Запись = РегистрыСведений.ЗапросыДляПовторнойОтправкиСЭДО.СоздатьМенеджерЗаписи();
		Запись.Организация = Запрос.Организация;
		Идентификатор = Запрос.ИдентификаторыСообщений[0];
		Запись.Идентификатор = Идентификатор;
		Запись.Прочитать();
		ЗаполнитьЗначенияСвойств(Запись, Запрос);
		Если НЕ ЗначениеЗаполнено(Запись.Идентификатор) Тогда
			Счетчик = Счетчик + 1;
		КонецЕсли;
		Запись.Идентификатор = Идентификатор;
		Если НЕ ЭтоАдресВременногоХранилища(Запрос.АдресЗашифрованногоЗапросаSOAP) Тогда
			Шаблон = НСтр("ru='Не заполнен адрес зашифрованного запроса. Код ошибки %1, текст ошибки: %2'");
			ТекстОшибки = СтрШаблон(Шаблон, Запрос.КодСостояния, Запрос.ТекстОшибки);
			ЗаписатьСобытиеСЭДОВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстОшибки);
			Продолжить;
		КонецЕсли;
		ТекстЗапроса = ПолучитьИзВременногоХранилища(Запрос.АдресЗашифрованногоЗапросаSOAP);
		Запись.ДанныеЗапроса = Новый ХранилищеЗначения(ТекстЗапроса, Новый СжатиеДанных(9));
		ТекстЗапроса = СтрокаИзДанныхПоАдресу(Запрос.АдресПодписаногоЗапросаSOAP);
		Запись.ДанныеПодписаногоЗапроса = Новый ХранилищеЗначения(ТекстЗапроса, Новый СжатиеДанных(9));
		Если НЕ ЗначениеЗаполнено(Запись.Дата) Тогда
			Запись.Дата = ТекущаяДатаНаСервере();
		КонецЕсли;
		Запись.ДатаПопыткиПодключения = ТекущаяДатаНаСервере();
		Запись.ОписаниеОшибки = Запрос.ТекстОшибки;
		Попытка
			Запись.Записать();
		Исключение
			ОтменитьТранзакцию();
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			Шаблон = НСтр("ru='При записи данных регистра ЗапросыДляПовторнойОтправкиСЭДО возникла ошибка:'")
				+ Символы.ПС + "%1";
			ТекстОшибки = СтрШаблон(Шаблон, ТекстОшибки);
			ЗаписатьСобытиеСЭДОВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстОшибки);
			Результат.ОписаниеОшибки = НСтр("ru='При добавлении записи в регистр ""Запросы для повторной отправки в СЭДО"" возникла ошибка.'");
			Возврат Результат;
		КонецПопытки;
		
	КонецЦикла;
	
	ЗафиксироватьТранзакцию();
	
	УстановитьПривилегированныйРежим(Ложь);
	
	ЭлектронныйДокументооборотСФСС.ВключитьФоновыйОбменСЭДО();
	
	ПараметрыВывода = ЭлектронныйДокументооборотСФССКлиентСервер.НовыеПараметрыВывестиОшибку(Истина);
	ВыведенСчетчик = Ложь;
	Если Счетчик > 0 Тогда 
		ТекстКоличестваСообщений = ПолучитьСклоненияСтрокиПоЧислу("сообщение", Счетчик, "",
			"ЧС=Количественное", "ПД=Именительный");
		Шаблон = НСтр("ru='К очереди фоновой загрузки СЭДО добавлено %1.'");
		ТекстСообщения = СтрШаблон(Шаблон, ТекстКоличестваСообщений[0]);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(ТекстСообщения, ПараметрыВывода);
		ВыведенСчетчик = Истина;
	КонецЕсли;
	
	Статистика = ЭлектронныйДокументооборотСФСС.КоличествоЗапросовВОчередиАвтоматическойЗагрузки();
	Если Статистика.КоличествоЗапросов > 0
		И НЕ Статистика.КоличествоЗапросов = Счетчик Тогда
		ОбщееКоличество = ПолучитьСклоненияСтрокиПоЧислу("сообщение", Статистика.КоличествоЗапросов, "",
			"ЧС=Количественное", "ПД=Именительный");
		Если ВыведенСчетчик Тогда
			Шаблон = НСтр("ru='Всего в очереди фоновой загрузки %1.'");
		Иначе
			Шаблон = НСтр("ru='Включена фоновая загрузка с сервера СЭДО, в очереди %1.'");
		КонецЕсли;
		ТекстСообщения = СтрШаблон(Шаблон, ОбщееКоличество[0]);
		ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(ТекстСообщения, ПараметрыВывода);
	КонецЕсли;
	
	Результат.Выполнено = Истина;
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ЗаписатьСобытиеСЭДОВЖурнал(Знач Уровень, ТекстСобытия, Данные = Неопределено)
	
	Если ТипЗнч(Уровень) = Тип("Строка") Тогда
		Уровень = УровеньСобытияПоПредставлению(Уровень);
	КонецЕсли;
	
	КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
	Событие = НСтр("ru='Обмен с СЭДО СФР'", КодОсновногоЯзыка);
	ЗаписьЖурналаРегистрации(Событие, Уровень, , Данные, ТекстСобытия);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция УровеньСобытияПоПредставлению(ПредставлениеУровня)
	
	Если ПредставлениеУровня = "Информация" Тогда
		Возврат УровеньЖурналаРегистрации.Информация;
	ИначеЕсли ПредставлениеУровня = "Ошибка" Тогда
		Возврат УровеньЖурналаРегистрации.Ошибка;
	ИначеЕсли ПредставлениеУровня = "Предупреждение" Тогда
		Возврат УровеньЖурналаРегистрации.Предупреждение; 
	ИначеЕсли ПредставлениеУровня = "Примечание" Тогда
		Возврат УровеньЖурналаРегистрации.Примечание;
	Иначе
		Возврат УровеньЖурналаРегистрации.Информация;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Функция НовыйЗапросКСерверуСЭДО()
	
	Результат = Новый Структура;
	Результат.Вставить("ИдентификаторыСообщений", Новый Массив);
	Результат.Вставить("КодСостояния", 0);
	Результат.Вставить("ТекстОшибки", "");
	Результат.Вставить("АдресЗашифрованногоЗапросаSOAP", "");
	Результат.Вставить("АдресПодписаногоЗапросаSOAP", "");
	Результат.Вставить("АдресРасшифрованногоОтветаSOAP", "");
	Результат.Вставить("Операция", "");
	Результат.Вставить("ТипВзаимодействия", 0);
	Результат.Вставить("Организация", Неопределено);
	Результат.Вставить("АдресСервера", "");
	Результат.Вставить("АдресРесурса", "");
	Результат.Вставить("ОшибкаСоединения", Ложь); 
	Результат.Вставить("ТипСообщения", 0); 
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПриВыполненииЗапросаВозниклаОшибкиСоединения(ДанныеЗапроса)
	
	Возврат ЭлектронныйДокументооборотСФСС.ПриВыполненииЗапросаВозниклаОшибкаСоединения(ДанныеЗапроса);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьНаличиеОтветаВРегистреФоновыхЗапросов(Идентификатор)
	
	Результат = Новый Структура;
	Результат.Вставить("БылПолученОтветСервера", Ложь);
	// Поля являются подмножеством полей РезультатПолучитьДанныеСЭДОФСС().
	Результат.Вставить("АдресЗашифрованногоОтветаSOAP", "");
	Результат.Вставить("АдресРесурса", "");
	Результат.Вставить("АдресСервера", "");
	Результат.Вставить("КодСостояния", 0);
	Результат.Вставить("АдресПодписаногоЗапросаSOAP", "");
	Результат.Вставить("Операция", "");
	Результат.Вставить("Идентификатор", Идентификатор);
	Результат.Вставить("ИдентификаторыСообщений", Новый Массив);
	Результат.Вставить("Организация", Неопределено);
	Результат.ИдентификаторыСообщений.Добавить(Идентификатор);
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ
	               |	ЗапросыДляПовторнойОтправкиСЭДО.Идентификатор КАК Идентификатор,
	               |	ЗапросыДляПовторнойОтправкиСЭДО.Организация КАК Организация,
	               |	ЗапросыДляПовторнойОтправкиСЭДО.АдресРесурса КАК АдресРесурса,
	               |	ЗапросыДляПовторнойОтправкиСЭДО.АдресСервера КАК АдресСервера,
	               |	ЗапросыДляПовторнойОтправкиСЭДО.КодСостояния КАК КодСостояния,
	               |	ЗапросыДляПовторнойОтправкиСЭДО.Операция КАК Операция
	               |ИЗ
	               |	РегистрСведений.ЗапросыДляПовторнойОтправкиСЭДО КАК ЗапросыДляПовторнойОтправкиСЭДО
	               |ГДЕ
	               |	ЗапросыДляПовторнойОтправкиСЭДО.БылПолученОтветСервера
	               |	И ЗапросыДляПовторнойОтправкиСЭДО.Идентификатор = &Идентификатор";
	
	Запрос.УстановитьПараметр("Идентификатор", Идентификатор);
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Результат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Выборка.Следующий();
	
	Запись = РегистрыСведений.ЗапросыДляПовторнойОтправкиСЭДО.СоздатьМенеджерЗаписи();
	Запись.Организация = Выборка.Организация;
	Запись.Идентификатор = Выборка.Идентификатор;
	Запись.Прочитать(); 
	
	Результат.БылПолученОтветСервера = Истина;
	ТекстОтвета = Запись.ДанныеОтвета.Получить();
	Результат.АдресЗашифрованногоОтветаSOAP = ПоместитьВоВременноеХранилище(ТекстОтвета, Новый УникальныйИдентификатор);
	ТекстЗапроса = Запись.ДанныеПодписаногоЗапроса.Получить();
	Результат.АдресПодписаногоЗапросаSOAP = ПоместитьВоВременноеХранилище(ТекстЗапроса, Новый УникальныйИдентификатор);
	
	ЗаполнитьЗначенияСвойств(Результат, Выборка);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура АнализОшибокПолученияСообщенийПослеВопроса(ВключитьЗадание, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатПолучения 					= ДополнительныеПараметры.РезультатПолучения;
	БылВыведенДиалогВключенияЗадания 	= ДополнительныеПараметры.БылВыведенДиалогВключенияЗадания;
	
	Если НЕ ВключитьЗадание = Неопределено И ВключитьЗадание Тогда
		
		Запросы = ДополнительныеПараметры.Запросы;
		Результат = ДобавитьДляДанныеФоновогоЗаданияЗагрузкиИзСЭДО(Запросы);
		Если НЕ Результат.Выполнено Тогда
			ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
		КонецЕсли;
		
	КонецЕсли; 
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения);
	
КонецПроцедуры

#КонецОбласти

#Область ПолучитьСообщенияСЭДОФСС

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОФСС(
		ОповещениеОбратногоВызова,
		Организация,
		Идентификаторы,
		ОткрыватьФормуДлительнойОперации = Истина,
		ЗадаватьВопросОТестовомСервере = Истина,
		ТипыВзаимодействий = Неопределено,
		СброситьСвойстваОбменаПоСЭДОЧерезОператора = Истина,
		ФормаДлительнойОперации = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеСообщений", Новый Массив);
	Результат.Вставить("БылиОшибки", Ложь);
	Результат.Вставить("Ошибки", Новый Массив);
	Результат.Вставить("РезультатыПолучения", Новый Массив);
	Результат.Вставить("ОперацияБылаОтмененаПользователем", Ложь);
	// Повторное получение вызывается в случае если будет получено сообщение 332 типа.
	Результат.Вставить("ПолучитьПовторно", Ложь);
	
	ДополнительныеПараметры = ОповещениеОбратногоВызова.ДополнительныеПараметры;
	
	ОтображатьСостояние = ?(НЕ ДополнительныеПараметры = Неопределено И ДополнительныеПараметры.Свойство("ОтображатьСостояние"),
		ДополнительныеПараметры.ОтображатьСостояние, Истина);
	КоличествоСообщенийВПакетномЗапросе = ПолучитьКоличествоСообщенийВПакетномЗапросеСЭДО();
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Результат", 							Результат);
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 			ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Идентификаторы", 						Идентификаторы);
	ДополнительныеПараметры.Вставить("ИндексСообщения", 					0);
	ДополнительныеПараметры.Вставить("Организация", 						Организация);
	ДополнительныеПараметры.Вставить("ФормаДлительнойОперации", 			ФормаДлительнойОперации);
	ДополнительныеПараметры.Вставить("ЗадаватьВопросОТестовомСервере", 		ЗадаватьВопросОТестовомСервере);
	ДополнительныеПараметры.Вставить("ТипыВзаимодействий", 					ТипыВзаимодействий);
	ДополнительныеПараметры.Вставить("ОтображатьСостояние", 				ОтображатьСостояние);
	ДополнительныеПараметры.Вставить("СведенияОДатахПолученияСообщений", 	Неопределено);
	ДополнительныеПараметры.Вставить("КоличествоСообщенийВПакетномЗапросе", КоличествоСообщенийВПакетномЗапросе);
	
	// Открываем форму длительной операции.
	ПараметрыОжидания = Новый Структура();
	ПараметрыОжидания.Вставить("ТекстСообщения", НСтр("ru = 'Получение входящих сообщений СЭДО СФР.'"));
	Если ОткрыватьФормуДлительнойОперации Тогда
		ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
			"ОбщаяФорма.ДлительнаяОперация", ПараметрыОжидания);
	КонецЕсли;
	
	Если СброситьСвойстваОбменаПоСЭДОЧерезОператора Тогда
		СброситьБезПолучения = Истина;
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, СброситьБезПолучения);
	КонецЕсли;
	
	ПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 		= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексСообщения 				= ДополнительныеПараметры.ИндексСообщения;
	Организация 					= ДополнительныеПараметры.Организация;
	Результат 						= ДополнительныеПараметры.Результат;
	Идентификаторы 					= ДополнительныеПараметры.Идентификаторы;
	ФормаДлительнойОперации 		= ДополнительныеПараметры.ФормаДлительнойОперации;
	ЗадаватьВопросОТестовомСервере 	= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	ТипыВзаимодействий 				= ДополнительныеПараметры.ТипыВзаимодействий;
	
	Если ОбменПрерванПользователем Или НЕ ФормаДлительнойОперацииОткрыта(ФормаДлительнойОперации) Тогда
		// Пользователь нажал Отмена в форме загрузки.
		Результат.ОперацияБылаОтмененаПользователем = Истина;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Если ИндексСообщения % 10 = 0 Тогда
		// для предотвращения переполнения стека
		ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ДополнительныеПараметры,
			"БРО.ВходящийКонтекстПолучитьСообщенияСЭДОФСС");
		ПодключитьОбработчикОжидания("Подключаемый_СбросСтекаПолучитьСообщенияСЭДОФССЦикл", 3, Истина);
	Иначе
		СбросСтекаПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СбросСтекаПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры)
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексСообщения 					= ДополнительныеПараметры.ИндексСообщения;
	Организация 						= ДополнительныеПараметры.Организация;
	Результат 							= ДополнительныеПараметры.Результат;
	Идентификаторы 						= ДополнительныеПараметры.Идентификаторы;
	ФормаДлительнойОперации 			= ДополнительныеПараметры.ФормаДлительнойОперации;
	ЗадаватьВопросОТестовомСервере 		= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	ТипыВзаимодействий 					= ДополнительныеПараметры.ТипыВзаимодействий;
	КоличествоСообщенийВПакетномЗапросе = ДополнительныеПараметры.КоличествоСообщенийВПакетномЗапросе;
	
	Если ОбменПрерванПользователем Или ИндексСообщения >= Идентификаторы.Количество() Тогда
		// Закроем окно заставки длительной операции, если она была открыта.
		ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		// Оповещение для форм.
		ИмяСобытия = ЭлектронныйДокументооборотСФССКлиент.ИмяСобытияОповещенияЗавершенияПолученияСообщенийИзСЭДО();
		Оповестить(Имясобытия, Результат);
		Возврат;
	КонецЕсли;
	
	ТипВзаимодействия = ?(ЗначениеЗаполнено(ТипыВзаимодействий) И ИндексСообщения < ТипыВзаимодействий.Количество(),
		ТипыВзаимодействий[ИндексСообщения], Неопределено);
	
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = Ложь;
	
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	ОбменПоСЭДОНапрямую = СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	
	Если КоличествоСообщенийВПакетномЗапросе <= 1
		ИЛИ ОбменПоСЭДОНапрямую
		ИЛИ Идентификаторы.Количество() = 1 Тогда
		// Получение единичного сообщения.
		Идентификатор = Идентификаторы[ИндексСообщения];
		Оповещение = Новый ОписаниеОповещения("ПолучитьСообщенияСЭДОФССЦиклПослеПолученияСообщения",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПолучитьСообщениеСЭДОФСС(
			Оповещение,
			Организация,
			Идентификатор,
			ЗадаватьВопросОТестовомСервере,
			ТипВзаимодействия,
			СброситьСвойстваОбменаПоСЭДОЧерезОператора);
	Иначе
		// Пакетное получение сообщений.
		ИдентификаторыДляПакетногоЗапроса = Новый Массив;
		КоличествоСообщений = 1;
		Для Индекс = ИндексСообщения По Идентификаторы.ВГраница() Цикл
			ИдентификаторыДляПакетногоЗапроса.Добавить(Идентификаторы[Индекс]);
			Если КоличествоСообщений >= КоличествоСообщенийВПакетномЗапросе Тогда
				Прервать;
			КонецЕсли;
			КоличествоСообщений = КоличествоСообщений + 1;
		КонецЦикла;
		Оповещение = Новый ОписаниеОповещения("ПолучитьСообщенияСЭДОФССПакетноЦиклПослеПолученияСообщений",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПараметрыПолучения = НовыеПараметрыПолучитьСообщенияСЭДОПакетно();
		ПараметрыПолучения.ЗадаватьВопросОТестовомСервере = ЗадаватьВопросОТестовомСервере;
		ПараметрыПолучения.ТипВзаимодействия = ТипВзаимодействия;
		ПараметрыПолучения.СброситьСвойстваОбменаПоСЭДОЧерезОператора = СброситьСвойстваОбменаПоСЭДОЧерезОператора;
		ПолучитьСообщенияСЭДОПакетно(
			Оповещение,
			Организация,
			ИдентификаторыДляПакетногоЗапроса,
			ПараметрыПолучения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_СбросСтекаПолучитьСообщенияСЭДОФССЦикл() Экспорт
	
	ВходящийКонтекст = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ВходящийКонтекстПолучитьСообщенияСЭДОФСС");
	
	СбросСтекаПолучитьСообщенияСЭДОФССЦикл(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОФССЦиклПослеПолученияСообщения(РезультатПолученияСообщения, ДополнительныеПараметры) Экспорт
	
	Результат 					= ДополнительныеПараметры.Результат;
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексСообщения 			= ДополнительныеПараметры.ИндексСообщения;
	ФормаДлительнойОперации 	= ДополнительныеПараметры.ФормаДлительнойОперации;
	Идентификаторы 				= ДополнительныеПараметры.Идентификаторы;
	Организация 				= ДополнительныеПараметры.Организация;
	Идентификатор				= Идентификаторы[ИндексСообщения];
	
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	
	Если НЕ РезультатПолученияСообщения.Выполнено Тогда
		Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую
			И ДополнительныеПараметры.СведенияОДатахПолученияСообщений = Неопределено Тогда
			
			ДополнительныеПараметры.СведенияОДатахПолученияСообщений = ПолучитьДатыПолученияСообщений(
				Организация,,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
		КонецЕсли;
		СведенияОДатахПолученияСообщений = ДополнительныеПараметры.СведенияОДатахПолученияСообщений;
		СообщенияДоСЭДОЧерезОператораПолучены = СведенияОДатахПолученияСообщений <> Неопределено
			И СведенияОДатахПолученияСообщений.ОбменЧерезОператора = Истина;
		
		Если СообщенияДоСЭДОЧерезОператораПолучены ИЛИ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			Результат.БылиОшибки = Истина;
			Результат.Ошибки.Добавить(РезультатПолученияСообщения.ОписаниеОшибки);
		КонецЕсли;
		
		ЭтоКритическаяОшибкаОбменаЧерезОператора = ЭтоКритическаяОшибкаОбменаЧерезОператора(
			СвойстваОбменаПоСЭДОЧерезОператора, РезультатПолученияСообщения);
		ЭтоОшибкаСоединенияОбменаЧерезОператора = РезультатПолученияСообщения.ОшибкаСоединения
			И НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
		Если РезультатПолученияСообщения.ОтмененоПользователем
			ИЛИ ЭтоОшибкаСоединенияОбменаЧерезОператора
			ИЛИ ЭтоКритическаяОшибкаОбменаЧерезОператора Тогда
			// Прерываем получение сообщений если пользователь отменил операцию
			// или в случае ошибки обмена через оператора.
			ИндексСообщения = Идентификаторы.Количество();
		КонецЕсли;
		
	Иначе
		Тип332 = ДокументооборотСФССКлиентСервер.ТипыСообщенийСЭДО(Истина).ТипСообщенияОтветНаЗапросИсторическихДанных;
		Для каждого Сообщение Из РезультатПолученияСообщения.ДанныеСообщений Цикл
			Результат.ДанныеСообщений.Добавить(Сообщение);
			Если Строка(Сообщение.Тип) = Тип332
				И НЕ Результат.ПолучитьПовторно Тогда
				Результат.ПолучитьПовторно = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Результат.РезультатыПолучения.Добавить(РезультатПолученияСообщения);
	
	ИндексСообщения = ИндексСообщения + 1;
	ДополнительныеПараметры.ИндексСообщения = ИндексСообщения;
	
	// После получения первого сообщения для последующих вопрос не задаем.
	ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере = Ложь;
	
	ПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Функция ЭтоКритическаяОшибкаОбменаЧерезОператора(СвойстваОбменаПоСЭДОЧерезОператора, РезультатПолученияСообщения)
	
	КодыСостояния = Новый Массив;
	КодыСостояния.Добавить(400);
	КодыСостояния.Добавить(503);
	КодыСостояния.Добавить(504);
	Результат = НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую
		И РезультатПолученияСообщения.Свойство("ДанныеЗапроса")
		И НЕ КодыСостояния.Найти(РезультатПолученияСообщения.ДанныеЗапроса.КодСостояния) = Неопределено;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ПолучитьСообщенияСЭДОПакетно

&НаКлиенте
Функция НовыеПараметрыПолучитьСообщенияСЭДОПакетно()
	
	Результат = Новый Структура;
	Результат.Вставить("ЗадаватьВопросОТестовомСервере", Ложь);
	Результат.Вставить("ТипВзаимодействия", Неопределено);
	Результат.Вставить("СброситьСвойстваОбменаПоСЭДОЧерезОператора", Истина);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОПакетно(
		ОповещениеОбратногоВызова,
		Организация,
		Идентификаторы,
		ПараметрыПолучения) Экспорт
		
	ЗадаватьВопросОТестовомСервере = ПараметрыПолучения.ЗадаватьВопросОТестовомСервере;
	ТипВзаимодействия = ПараметрыПолучения.ТипВзаимодействия;
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = ПараметрыПолучения.СброситьСвойстваОбменаПоСЭДОЧерезОператора;
	
	Результат = НовыйРезультатПолученияСообщенийСЭДО();
	
	Если Идентификаторы.Количество() = 0 Тогда
		Результат.ОписаниеОшибки = НСтр("ru='Не заполнен список идентификаторов сообщений СЭДО.'");
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	Операция = ОперацияПолученияСообщенияСЭДОФСС();
	
	ОтображатьСостояние = Истина;
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ОтображатьСостояние") Тогда
		ОтображатьСостояние = ОповещениеОбратногоВызова.ДополнительныеПараметры.ОтображатьСостояние;
	КонецЕсли;
	
	Настройки = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Настройки.ТипВзаимодействия = ТипВзаимодействия;
	ЭтоТипВзаимодействияОрганизацияСЭДО =
		(ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияОрганизацияСЭДО());
	ПолучитьОГРН 			= ЭтоТипВзаимодействияОрганизацияСЭДО;
	ПолучитьИННКППиСНИЛС 	= ЭтоТипВзаимодействияОрганизацияСЭДО;
	СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
		Организация,,
		ПолучитьОГРН,,
		ПолучитьИННКППиСНИЛС);
	РегистрационныйНомерФСС = СвойстваОрганизации.РегистрационныйНомерФСС;
	Если ЭтоТипВзаимодействияОрганизацияСЭДО Тогда
		ЗаполнитьЗначенияСвойств(Настройки, СвойстваОрганизации);
	КонецЕсли;
	
	Если СброситьСвойстваОбменаПоСЭДОЧерезОператора Тогда
		СброситьБезПолучения = Истина;
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, СброситьБезПолучения);
	КонецЕсли;
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	
	Настройки.ОбменПоСЭДОНапрямую = СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	
	ЗапросДляПолученияДанных = ПараметрыЗапросаПолучитьДанныеСЭДОФСС();
	ЗапросДляПолученияДанных.Организация = Организация;
	ЗапросДляПолученияДанных.Операция = Операция;
	ЗапросДляПолученияДанных.ТекстЗапроса =
		ТекстЗапросаПолученияСообщенийПакетноСЭДОФСС(
			РегистрационныйНомерФСС,
			Идентификаторы,
			Настройки);
	ЗапросДляПолученияДанных.РезультатФоновойЗагрузки = Новый Массив;
	ЗапросДляПолученияДанных.ИдентификаторыСообщений = Идентификаторы;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 	ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Результат", 					Результат);
	ДополнительныеПараметры.Вставить("Организация", 				Организация);
	ДополнительныеПараметры.Вставить("Операция", 					Операция);
	ДополнительныеПараметры.Вставить("ИдентификаторыСообщений", 	Идентификаторы);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 			ТипВзаимодействия);
	ДополнительныеПараметры.Вставить("ДанныеБылиПолученыВФоне", 	Ложь);
	ДополнительныеПараметры.Вставить("ОтображатьСостояние", 		ОтображатьСостояние);
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоСЭДОЧерезОператора",
		СвойстваОбменаПоСЭДОЧерезОператора);
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьСообщенияСЭДОПакетноПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПолучитьДанныеСЭДОФСС(
		Оповещение,
		ЗапросДляПолученияДанных,
		ЗадаватьВопросОТестовомСервере,
		ТипВзаимодействия,
		ОтображатьСостояние)
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОПакетноПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	Организация 						= ДополнительныеПараметры.Организация;
	Операция 							= ДополнительныеПараметры.Операция;
	ИдентификаторыСообщений				= ДополнительныеПараметры.ИдентификаторыСообщений;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	ДанныеЗапроса 						= РезультатОперации.ДанныеЗапроса;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	// Данные запроса для повтора в случае ошибки.
	Для каждого Идентификатор из ИдентификаторыСообщений Цикл
		ДанныеЗапроса.ИдентификаторыСообщений.Добавить(Идентификатор);
	КонецЦикла;
	ДанныеЗапроса.ТипВзаимодействия 				= ТипВзаимодействия;
	ДанныеЗапроса.Операция 							= Операция;
	ДанныеЗапроса.Организация 						= Организация;
	ДанныеЗапроса.АдресСервера 						= Результат.АдресСервера;
	ДанныеЗапроса.АдресРесурса 						= Результат.АдресРесурса;
	ДанныеЗапроса.КодСостояния 						= Результат.КодСостояния;
	ДанныеЗапроса.ТекстОшибки 						= Результат.ОписаниеОшибки;
	ДанныеЗапроса.АдресЗашифрованногоЗапросаSOAP 	= Результат.АдресЗашифрованногоЗапросаSOAP;
	ДанныеЗапроса.ОшибкаСоединения 					= Результат.ОшибкаСоединения;
	ДанныеЗапроса.АдресПодписаногоЗапросаSOAP 		= Результат.АдресПодписаногоЗапросаSOAP;
	ДанныеЗапроса.АдресРасшифрованногоОтветаSOAP 	= Результат.АдресРасшифрованногоОтветаSOAP;
	
	// Данные запроса для сохранения в регистр журнала запросов.
	ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция);
	ПараметрыЗапроса.АдресЗапросаSOAP 			= Результат.АдресПодписаногоЗапросаSOAP;
	ПараметрыЗапроса.АдресОтветаSOAP 			= Результат.АдресРасшифрованногоОтветаSOAP;
	ПараметрыЗапроса.АдресРесурса 				= Результат.АдресРесурса;
	ПараметрыЗапроса.АдресСервера 				= Результат.АдресСервера;
	ПараметрыЗапроса.КодСостояния 				= Результат.КодСостояния;
	ПараметрыЗапроса.ТипВзаимодействияСЭДО 		= ТипВзаимодействия;
	ПараметрыЗапроса.УдалятьИзХранилища 		= Истина;
	ПараметрыЗапроса.ОбменЧерезОператора 		= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	ПараметрыЗапроса.ПакетныйЗапросСообщений 	= Истина;
	
	РезультатОперации.ОшибкаСоединения = Результат.ОшибкаСоединения;
	
	Если Результат.Выполнено Тогда
		АдресРезультата = Результат.АдресРасшифрованногоОтветаSOAP;
		ТекстОтвета = ПолучитьИзВременногоХранилища(АдресРезультата);
		
		// Разбор ответа.
		Попытка
			РезультатРазбора = РазобратьОтветПолучитьСообщенияСЭДОПакетно(ТекстОтвета, , ПараметрыЗапроса.Идентификатор);
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			КраткоеПредставлениеОшибки = ОбработкаОшибок.КраткоеПредставлениеОшибки(ИнформацияОбОшибке);
			ПодробноеПредставлениеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			Шаблон = НСтр("ru='При разборе данных сообщений возникла ошибка:'") + Символы.ПС
				+ "%1";
			РезультатОперации.ОписаниеОшибки = СтрШаблон(Шаблон, КраткоеПредставлениеОшибки);
			Шаблон = НСтр("ru='При разборе данных сообщений возникла ошибка. Текст сообщения:'") + Символы.ПС
				+ "%1" + Символы.ПС + "%2";
			ТекстОшибки = СтрШаблон(Шаблон,
				ТекстОтвета, ПодробноеПредставлениеОшибки);
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщений СЭДО ФСС'"),
				"Ошибка",
				ТекстОшибки, ,
				Истина);
			Если ИдентификаторыСообщений.Количество() > 0 Тогда
				ПараметрыЗапроса.ИдентификаторФСС = ИдентификаторыСообщений[0];
			КонецЕсли;
			ПараметрыЗапроса.ЭтоПользовательскийЗапрос = Истина;
			ПараметрыЗапроса.ОписаниеОшибки = РезультатОперации.ОписаниеОшибки;
			ПараметрыЗапроса.ДанныеПолученныхПакетноСообщений =
				ДанныеПолученныхПакетноСообщенийСЭДО(
					Организация,
					РезультатРазбора,
					РезультатОперации);
			СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
			Возврат;
		КонецПопытки;
		
		ИдентификаторыНеНайденных = РезультатРазбора.ИдентификаторыНеНайденных;
		РезультатОперации.СообщенияНеНайдены = ИдентификаторыНеНайденных.Количество() > 0;
		РезультатОперации.ИдентификаторыНеНайденных = ИдентификаторыНеНайденных;
		РезультатОперации.ОшибкиПоИдентификаторам = РезультатРазбора.ОшибкиПоИдентификаторам;
		
		Если РезультатРазбора.ДанныеСообщений.Количество() > 0 Тогда
			ДанныеСообщения = РезультатРазбора.ДанныеСообщений[0];
			ПараметрыЗапроса.ИдентификаторФСС = ДанныеСообщения.Идентификатор;
			ТипыСообщений = Новый Массив;
			Для каждого ДанныеСообщения из РезультатРазбора.ДанныеСообщений Цикл
				ТипыСообщений.Добавить(ДанныеСообщения.Тип);
			КонецЦикла;
			ПараметрыЗапроса.ТипСообщенияСЭДО = СтрСоединить(ТипыСообщений, ",");
		КонецЕсли;
		
		ПараметрыЗапроса.ДанныеПолученныхПакетноСообщений =
			ДанныеПолученныхПакетноСообщенийСЭДО(
				Организация,
				РезультатРазбора,
				РезультатОперации);
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			
			// Получен список ошибок.
			Если ЗначениеЗаполнено(ИдентификаторыСообщений)
				И ИдентификаторыСообщений.Количество() > 0 Тогда
				Шаблон = ?(СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую,
					НСтр("ru = 'При получении сообщений с сервера СЭДО ФСС произошли ошибки'"),
					НСтр("ru = 'При получении сообщений с сервера оператора СЭДО ФСС произошли ошибки'"));
			КонецЕсли;
			Шаблон = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(Шаблон, Истина);
			Шаблон = Шаблон + ":
				|%1";
			ОписаниеОшибки = СтрСоединить(РезультатРазбора.Ошибки, Символы.ПС);
			ОписаниеОшибки = СтрШаблон(Шаблон, ОписаниеОшибки);
			РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
			// Записываем ошибку в журнал регистрации.
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщений СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки,,
				Истина);
			ПараметрыЗапроса.ЭтоПользовательскийЗапрос = Истина;
			ПараметрыЗапроса.ОписаниеОшибки = ОписаниеОшибки;
		Иначе
			// Сообщение было успешно разобрано.
			ДанныеСообщений = РезультатРазбора.ДанныеСообщений;
			РезультатОперации.Вставить("ДанныеСообщений", РезультатРазбора.ДанныеСообщений);
			РезультатСохранения = СохранитьДанныеВходящихСообщенийСЭДОФСС(
				Организация,
				ДанныеСообщений,
				ТипВзаимодействия,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
			
			Если РезультатСохранения.БылиОшибкиОбработки Тогда
				// Сохраняем данные запроса в журнал запросов.
				СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
				// Удаляем запрос по сообщению из очереди фонового получения.
				УбратьЗапросИзОчередиФоновогоЗадания(ДанныеЗапроса);
				РезультатОперации.ОписаниеОшибки = СтрСоединить(РезультатСохранения.ОшибкиОбработки, Символы.ПС);
				ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
				Возврат;
			КонецЕсли;
			РезультатОперации.Выполнено = Истина;
		КонецЕсли;
		
		// Сохраняем данные запроса в журнал запросов.
		СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
		
	Иначе
		Шаблон = НСтр("ru='Не удалось получить сообщения СЭДО СФР.
			|%1'");
		РезультатОперации.ОписаниеОшибки = СтрШаблон(Шаблон, Результат.ОписаниеОшибки);
		РезультатОперации.ОтмененоПользователем = Результат.ОтмененоПользователем;
	КонецЕсли;
	
	Если НЕ ПриВыполненииЗапросаВозниклаОшибкиСоединения(Результат) Тогда
		// Удаляем запрос по сообщению из очереди фонового получения.
		УбратьЗапросИзОчередиФоновогоЗадания(ДанныеЗапроса);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОФССПакетноЦиклПослеПолученияСообщений(
	РезультатПолученияСообщений,
	ДополнительныеПараметры) Экспорт
	
	Результат 					= ДополнительныеПараметры.Результат;
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексСообщения 			= ДополнительныеПараметры.ИндексСообщения;
	ФормаДлительнойОперации 	= ДополнительныеПараметры.ФормаДлительнойОперации;
	Идентификаторы 				= ДополнительныеПараметры.Идентификаторы;
	Организация 				= ДополнительныеПараметры.Организация;
	Идентификатор				= Идентификаторы[ИндексСообщения];
	
	КоличествоСообщенийВПакетномЗапросе = ДополнительныеПараметры.КоличествоСообщенийВПакетномЗапросе;
	
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	
	Если НЕ РезультатПолученияСообщений.Выполнено Тогда
		Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую
			И ДополнительныеПараметры.СведенияОДатахПолученияСообщений = Неопределено Тогда
			
			ДополнительныеПараметры.СведенияОДатахПолученияСообщений = ПолучитьДатыПолученияСообщений(
				Организация,,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
		КонецЕсли;
		СведенияОДатахПолученияСообщений = ДополнительныеПараметры.СведенияОДатахПолученияСообщений;
		СообщенияДоСЭДОЧерезОператораПолучены = СведенияОДатахПолученияСообщений <> Неопределено
			И СведенияОДатахПолученияСообщений.ОбменЧерезОператора = Истина;
		
		Если СообщенияДоСЭДОЧерезОператораПолучены ИЛИ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			Результат.БылиОшибки = Истина;
			Результат.Ошибки.Добавить(РезультатПолученияСообщений.ОписаниеОшибки);
		КонецЕсли;
		
		ЭтоКритическаяОшибкаОбменаЧерезОператора = ЭтоКритическаяОшибкаОбменаЧерезОператора(
			СвойстваОбменаПоСЭДОЧерезОператора, РезультатПолученияСообщений);
		ЭтоОшибкаСоединенияОбменаЧерезОператора = РезультатПолученияСообщений.ОшибкаСоединения
			И НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
		Если РезультатПолученияСообщений.ОтмененоПользователем
			ИЛИ ЭтоОшибкаСоединенияОбменаЧерезОператора
			ИЛИ ЭтоКритическаяОшибкаОбменаЧерезОператора Тогда
			// Прерываем получение сообщений если пользователь отменил операцию
			// или в случае ошибки обмена через оператора.
			ИндексСообщения = Идентификаторы.Количество();
		КонецЕсли;
		
	Иначе
		Тип332 = ДокументооборотСФССКлиентСервер.ТипыСообщенийСЭДО(Истина).ТипСообщенияОтветНаЗапросИсторическихДанных;
		Для каждого ДанныеСообщения из РезультатПолученияСообщений.ДанныеСообщений Цикл
			Результат.ДанныеСообщений.Добавить(ДанныеСообщения);
			Если Строка(ДанныеСообщения.Тип) = Тип332
				И НЕ Результат.ПолучитьПовторно Тогда
				Результат.ПолучитьПовторно = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Результат.РезультатыПолучения.Добавить(РезультатПолученияСообщений);
	
	КоличествоСообщенийВОтвете = РезультатПолученияСообщений.ДанныеСообщений.Количество()
		+ РезультатПолученияСообщений.ОшибкиПоИдентификаторам.Количество();
	Если КоличествоСообщенийВОтвете > 0 Тогда
		ИндексСообщения = ИндексСообщения + КоличествоСообщенийВОтвете;
	Иначе
		ИндексСообщения = ИндексСообщения + КоличествоСообщенийВПакетномЗапросе;
	КонецЕсли;

	ДополнительныеПараметры.ИндексСообщения = ИндексСообщения;
	
	// После получения первого сообщения для последующих вопрос не задаем.
	ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере = Ложь;
	
	ПаузаМеждуЗапросами = 0;
	// Пауза перед следующим запросом.
	Если ИндексСообщения < Идентификаторы.ВГраница() Тогда
		Если КоличествоСообщенийВПакетномЗапросе < 10 Тогда
			ПаузаМеждуЗапросами = 0.1;
		Иначе
			ПаузаМеждуЗапросами = 1;
		КонецЕсли;
		ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ДополнительныеПараметры,
			"БРО.ПолучитьСообщенияСЭДОПакетно");
		ПодключитьОбработчикОжидания("Подключаемый_ПолучитьСообщенияСЭДОПакетно", ПаузаМеждуЗапросами, Истина);
	Иначе
		ПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПолучитьСообщенияСЭДОПакетно()

	ДополнительныеПараметры = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ПолучитьСообщенияСЭДОПакетно");
	ПолучитьСообщенияСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Функция ТекстЗапросаПолученияСообщенийПакетноСЭДОФСС(
		РегистрационныйНомерФСС,
		ИдентификаторыСообщений,
		Настройки = Неопределено) Экспорт
	
	НастройкиВызова = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Если Настройки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, Настройки);
	КонецЕсли;
	
	Если НастройкиВызова.ТипВзаимодействия = Неопределено Тогда
		НастройкиВызова.ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	ИнформацияОПрограмме = ИнформацияОПрограмме();
	
	ТекстПараметровТипаВзаимодействия = ДокументооборотСФССКлиентСервер.ТекстПараметровТипаВзаимодействияСЭДО(
		РегистрационныйНомерФСС, НастройкиВызова);
		
	УзелИдентификаторыСообщений = "";
	
	Для каждого Идентификатор из ИдентификаторыСообщений Цикл
		Шаблон = "
			|            <uuid>%1</uuid>";
		УзелИдентификаторыСообщений = УзелИдентификаторыСообщений + СтрШаблон(Шаблон, Идентификатор);
	КонецЦикла;
	
	Шаблон = "
		|   <soapenv:Body xmlns:soapenv=""http://schemas.xmlsoap.org/soap/envelope/"""
		+ " xmlns:v01=""http://www.fss.ru/integration/types/sedo/v01"""
		+ " xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd"""
		+ " wsu:Id=""REGNO_%2"">
		|      <v01:getMessageRequest>
		|         <systemInfo>
		|            <specVersion>1.0</specVersion>
		|            <software>%4</software>
		|            <softwareVersion>%5</softwareVersion>
		|         </systemInfo>
		|         <interactionType>%1</interactionType>
		|         <uuidList>%3
		|         </uuidList>%6
		|      </v01:getMessageRequest>
		|   </soapenv:Body>";
	
	Текст = СтрШаблон(Шаблон,
		НастройкиВызова.ТипВзаимодействия,
		РегистрационныйНомерФСС,
		УзелИдентификаторыСообщений,
		ИнформацияОПрограмме.ИДКонфигурации,
		ИнформацияОПрограмме.ВерсияКонфигурации,
		ТекстПараметровТипаВзаимодействия);
	
	Возврат Текст;
	
КонецФункции

&НаКлиенте
Функция НовыйРезультатПолученияСообщенийСЭДО()
	
	Результат = Новый Структура;
	Результат.Вставить("Идентификаторы", Новый Массив);
	Результат.Вставить("ДанныеСообщений", Новый Массив);
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("ОтмененоПользователем", Ложь);
	Запрос = НовыйЗапросКСерверуСЭДО();
	Результат.Вставить("ДанныеЗапроса", Запрос);
	Результат.Вставить("ОшибкаСоединения", Ложь);
	Результат.Вставить("СообщенияНеНайдены", Ложь);
	Результат.Вставить("ИдентификаторыНеНайденных", Новый Массив);
	Результат.Вставить("ОшибкиПоИдентификаторам", Новый Соответствие);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьОтветПолучитьСообщенияСЭДОПакетно(СодержимоеОтвета, ДатаСообщений = Неопределено, ИдентификаторЗапроса = "")
	
	ДанныеСообщений = Новый Массив;
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("Ошибки", Новый Массив);
	Результат.Вставить("КонвертSOAP", СодержимоеОтвета);
	Результат.Вставить("КодыОшибок", Новый Массив);
	Результат.Вставить("ИдентификаторыНеНайденных", Новый Массив);
	Результат.Вставить("ОшибкиПоИдентификаторам", Новый Соответствие);
	Результат.Вставить("ДанныеСообщений", ДанныеСообщений);
	
	Чтение = Новый ЧтениеXML;
	Попытка
		Чтение.УстановитьСтроку(СодержимоеОтвета);
		ДанныеОтвета = ФабрикаXDTO.ПрочитатьXML(Чтение);
	Исключение
		Чтение.Закрыть();
		ОписаниеОшибки = НСтр("ru='Ошибка разбора ответа сервера с данными сообщения СЭДО.'");
		Результат.Ошибки.Добавить(ОписаниеОшибки);
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ПодробноеПредставлениеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
		ТекстОшибки = НСтр("ru='Ошибка разбора ответа сервера СЭДО:'") 
			+ Символы.ПС + ПодробноеПредставлениеОшибки
			+ Символы.ПС + НСтр("ru='Содержимое ответа:'") + Символы.ПС + СодержимоеОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
		Возврат Результат;
	КонецПопытки;
	Чтение.Закрыть();
	
	// Пример ответа сервера оператора (ответ всегда пакетный, даже при обычном запросе).
	//	<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//	<soap:Body xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//		<v01:sedoMessageResponse xmlns:v01="http://www.fss.ru/integration/types/sedo/v01">
	//			<packageResult>
	//				<sedoMessageList>
	//					<sedoMessage>
	//						<uuid>1882a16b-e21b-4817-8aef-446bee0ab92e</uuid>
	//						<messageType>111</messageType>
	//						<recipientId>3101011870</recipientId>
	//						<needConfirm>false</needConfirm>
	//						<expiryDate>2099-01-01</expiryDate>
	//						<message>PD94bWwgdmVyc..</message>
	//					</sedoMessage>
	//				</sedoMessageList>
	//				<errorList/>
	//			</packageResult>
	//		</v01:sedoMessageResponse>
	//	</soap:Body>
	//</soap:Envelope>
	// 
	// Сообщение с ошибкой от оператора (ответ всегда пакетный, даже при обычном запросе)
	// <soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//	<soap:Body xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
	//		<v01:sedoMessageResponse xmlns:v01="http://www.fss.ru/integration/types/sedo/v01">
	//			<packageResult>
	//				<sedoMessageList/>
	//				<errorList>
	//					<errorPackage>
	//						<uuid>1976dea6-790c-458d-8658-7b338bd8ecfd</uuid>
	//						<error>
	//							<com:code xmlns:com="http://www.fss.ru/integration/types/common/v01">E_SEDO_3010</com:code>
	//							<com:message xmlns:com="http://www.fss.ru/integration/types/common/v01">Указанное сообщение не найдено</com:message>
	//						</error>
	//					</errorPackage>
	//				</errorList>
	//			</packageResult>
	//		</v01:sedoMessageResponse>
	//	</soap:Body>
	//</soap:Envelope>
	
	Ошибка = НСтр("ru='Не удалось разобрать ответ сервера СЭДО с данными сообщения. Подробности см. в журнале регистрации.'");
	ЕстьУзелСообщения = ЕстьСвойство(ДанныеОтвета.Body, "sedoMessageResponse");
	Если НЕ ЕстьУзелСообщения Тогда
		// Определяем тело сообщения для записи в журнал.
		ТелоОтвета = ИзвлечьТелоОтветаИзКонтейнераСЭДО(СодержимоеОтвета);
		ТекстОшибки = НСтр("ru='С сервера СЭДО СФР получен ответ с ошибками:'") + Символы.ПС + ТелоОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
		Ошибка = ИзвлечьТекстОшибкиИзТелаОтветаСЭДО(ДанныеОтвета.Body);
		Результат.Ошибки.Добавить(Ошибка);
		Возврат Результат;
	КонецЕсли;
	
	УзелДанных = ДанныеОтвета.Body.sedoMessageResponse;
	УзлыОшибокИлиОшибокПакета = Неопределено;
	Если УзелДанных.Свойства().Получить("errorList") = Неопределено Тогда
		Если УзелДанных.Свойства().Получить("packageResult") <> Неопределено Тогда
			УзелПакета = УзелДанных.packageResult;
			Если УзелПакета.Свойства().Получить("errorList") <> Неопределено Тогда
				УзелОшибок = УзелПакета.errorList;
				Если УзелОшибок.Свойства().Получить("errorPackage") <> Неопределено Тогда
					УзлыОшибокИлиОшибокПакета = УзелОшибок.errorPackage;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		УзлыОшибокИлиОшибокПакета = УзелДанных.errorList;
	КонецЕсли;
	
	КодОшибкиСообщениеНеНайдено = "E_SEDO_3010";
	Если УзлыОшибокИлиОшибокПакета <> Неопределено Тогда
		
		// Возвращен список ошибок.
		Для каждого УзелОшибокИлиОшибокПакета Из ЗначениеВКоллекцию(УзлыОшибокИлиОшибокПакета) Цикл
			ИдентификаторСообщения = "";
			ОшибкиВУзле = Новый Массив;
			Если НЕ УзелОшибокИлиОшибокПакета.Свойства().Получить("uuid") = Неопределено Тогда
				ИдентификаторСообщения = УзелОшибокИлиОшибокПакета.uuid;
			КонецЕсли;
			
			Если УзелОшибокИлиОшибокПакета.Свойства().Получить("error") = Неопределено Тогда
				УзелОшибок = УзелОшибокИлиОшибокПакета;
			Иначе
				УзелОшибок = УзелОшибокИлиОшибокПакета.error;
			КонецЕсли;
			
			Если УзелОшибок.Свойства().Получить("message") <> Неопределено Тогда
				Для каждого Ошибка Из ЗначениеВКоллекцию(УзелОшибок.message) Цикл
					Результат.Ошибки.Добавить(Ошибка);
					ОшибкиВУзле.Добавить(Ошибка);
				КонецЦикла;
			КонецЕсли;
			Если ЕстьСвойство(УзелОшибок, "code") Тогда
				Для каждого Код Из ЗначениеВКоллекцию(УзелОшибок.code) Цикл
					Результат.КодыОшибок.Добавить(Код);
					Если Код = КодОшибкиСообщениеНеНайдено Тогда
						Результат.ИдентификаторыНеНайденных.Добавить(ИдентификаторСообщения);
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
			Если ЗначениеЗаполнено(ИдентификаторСообщения) Тогда
				ТекстОшибки = СтрСоединить(ОшибкиВУзле, Символы.ПС);
				Результат.ОшибкиПоИдентификаторам.Вставить(ИдентификаторСообщения, ТекстОшибки);
			КонецЕсли;
		КонецЦикла;
		
		ТелоОтвета = ИзвлечьТелоОтветаИзКонтейнераСЭДО(СодержимоеОтвета);
		ТекстОшибки = НСтр("ru='С сервера СЭДО СФР получен ответ с ошибками:'") + Символы.ПС + ТелоОтвета;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщений СЭДО СФР'",
			КодОсновногоЯзыка);
		ЗаписьЖурналаРегистрации(
			ИмяСобытия,
			УровеньЖурналаРегистрации.Ошибка, , ,
			ТекстОшибки);
	КонецЕсли;
	
	Если НЕ ЕстьСвойство(УзелДанных, "result") Тогда
		Если ЕстьСвойство(УзелДанных, "packageResult")
			И ЕстьСвойство(УзелДанных.packageResult, "sedoMessageList") Тогда
			УзелДанныхСообщений = УзелДанных.packageResult.sedoMessageList.sedoMessage;
			ПакетноеПолучение = Истина;
		Иначе
			Возврат Результат;
		КонецЕсли;
	Иначе
		УзелДанныхСообщений = УзелДанных.result;
	КонецЕсли;
	
	Для каждого УзелДанныеСообщения из ЗначениеВКоллекцию(УзелДанныхСообщений) Цикл
		
		ДанныеСообщения = ЭлектронныйДокументооборотСФССКлиентСервер.НовыеДанныеСообщенияСЭДО();
		РазобратьУзелСДаннымиСообщенияСЭДО(УзелДанныеСообщения, ДанныеСообщения, ДатаСообщений, ИдентификаторЗапроса);
		ДанныеСообщений.Добавить(ДанныеСообщения);
		
	КонецЦикла;
	
	Результат.Выполнено = Истина;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеПолученныхПакетноСообщенийСЭДО(
	Организация,
	РезультатРазбора,
	РезультатОперации)
	
	ТаблицаПакетныхСообщений = Новый ТаблицаЗначений;
	ТаблицаПакетныхСообщений.Колонки.Добавить("УникальныйИдентификатор");
	ТаблицаПакетныхСообщений.Колонки.Добавить("ОписаниеОшибкиСообщения");
	ТаблицаПакетныхСообщений.Колонки.Добавить("ТипСообщения");
	
	Для каждого ДанныеСообщения Из РезультатРазбора.ДанныеСообщений Цикл
		НоваяСтрока = ТаблицаПакетныхСообщений.Добавить();
		НоваяСтрока.УникальныйИдентификатор = ДанныеСообщения.Идентификатор;
		НоваяСтрока.ТипСообщения = ДанныеСообщения.Тип;
	КонецЦикла;
	
	Для каждого КлючЗначение Из РезультатРазбора.ОшибкиПоИдентификаторам Цикл
		ИдентификаторСообщения = КлючЗначение.Ключ;
		НоваяСтрока = ТаблицаПакетныхСообщений.Добавить();
		НоваяСтрока.УникальныйИдентификатор = ИдентификаторСообщения;
		НоваяСтрока.ОписаниеОшибкиСообщения = КлючЗначение.Значение;
	КонецЦикла;
	
	Для каждого ИдентификаторСообщения Из РезультатРазбора.ИдентификаторыНеНайденных Цикл
		ОбработатьОшибкуСообщениеСЭДОНеНайдено(Организация, ИдентификаторСообщения);
	КонецЦикла;
	
	ДанныеПолученныхПакетноСообщений = Новый ХранилищеЗначения(ТаблицаПакетныхСообщений);
	
	Возврат ДанныеПолученныхПакетноСообщений;
	
КонецФункции

#КонецОбласти

#Область ПолучитьСообщениеСЭДОФСС

&НаКлиенте
Процедура ПолучитьСообщениеСЭДОФСС(
		ОповещениеОбратногоВызова,
		Организация,
		ИдентификаторСообщения,
		ЗадаватьВопросОТестовомСервере,
		ТипВзаимодействия = Неопределено,
		СброситьСвойстваОбменаПоСЭДОЧерезОператора = Истина) Экспорт
		
	Результат = НовыйРезультатПолученияСообщенийСЭДО();
	ИдентификаторыСообщений = ЗначениеВКоллекцию(ИдентификаторСообщения);
	Результат.Идентификаторы = ИдентификаторыСообщений;
	
	Если ПустаяСтрока(ИдентификаторСообщения) Тогда
		Результат.ОписаниеОшибки = НСтр("ru='Не заполнен идентификатор сообщения СЭДО.'");
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	Операция = ОперацияПолученияСообщенияСЭДОФСС();
	
	ОтображатьСостояние = Истина;
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ОтображатьСостояние") Тогда
		ОтображатьСостояние = ОповещениеОбратногоВызова.ДополнительныеПараметры.ОтображатьСостояние;
	КонецЕсли;
	
	Настройки = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Настройки.ТипВзаимодействия = ТипВзаимодействия;
	ЭтоТипВзаимодействияОрганизацияСЭДО =
		(ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияОрганизацияСЭДО());
	ПолучитьОГРН 			= ЭтоТипВзаимодействияОрганизацияСЭДО;
	ПолучитьИННКППиСНИЛС 	= ЭтоТипВзаимодействияОрганизацияСЭДО;
	СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
		Организация,,
		ПолучитьОГРН,,
		ПолучитьИННКППиСНИЛС);
	РегистрационныйНомерФСС = СвойстваОрганизации.РегистрационныйНомерФСС;
	Если ЭтоТипВзаимодействияОрганизацияСЭДО Тогда
		ЗаполнитьЗначенияСвойств(Настройки, СвойстваОрганизации);
	КонецЕсли;
	
	Если СброситьСвойстваОбменаПоСЭДОЧерезОператора Тогда
		СброситьБезПолучения = Истина;
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, СброситьБезПолучения);
	КонецЕсли;
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	
	Настройки.ОбменПоСЭДОНапрямую = СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	
	ЗапросДляПолученияДанных = ПараметрыЗапросаПолучитьДанныеСЭДОФСС();
	ЗапросДляПолученияДанных.Организация = Организация;
	ЗапросДляПолученияДанных.Операция = Операция;
	ЗапросДляПолученияДанных.ТекстЗапроса =
		ТекстЗапросаПолученияСообщенияСЭДОФСС(
			РегистрационныйНомерФСС,
			ИдентификаторСообщения,
			Настройки);
	ЗапросДляПолученияДанных.РезультатФоновойЗагрузки = Неопределено;
	ЗапросДляПолученияДанных.ИдентификаторыСообщений = ИдентификаторыСообщений;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 	ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Результат", 					Результат);
	ДополнительныеПараметры.Вставить("Организация", 				Организация);
	ДополнительныеПараметры.Вставить("Операция", 					Операция);
	ДополнительныеПараметры.Вставить("ИдентификаторСообщения", 		ИдентификаторСообщения);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 			ТипВзаимодействия);
	ДополнительныеПараметры.Вставить("ДанныеБылиПолученыВФоне", 	Ложь);
	ДополнительныеПараметры.Вставить("ОтображатьСостояние", 		ОтображатьСостояние);
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоСЭДОЧерезОператора",
		СвойстваОбменаПоСЭДОЧерезОператора);
	
	РезультатФоновойЗагрузки = ПроверитьНаличиеОтветаВРегистреФоновыхЗапросов(ИдентификаторСообщения);
	Если РезультатФоновойЗагрузки.БылПолученОтветСервера Тогда
		ЗапросДляПолученияДанных.РезультатФоновойЗагрузки = РезультатФоновойЗагрузки;
		ДополнительныеПараметры.ДанныеБылиПолученыВФоне = Истина;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьСообщениеСЭДОФССПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПолучитьДанныеСЭДОФСС(
		Оповещение,
		ЗапросДляПолученияДанных,
		ЗадаватьВопросОТестовомСервере,
		ТипВзаимодействия,
		ОтображатьСостояние)

КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщениеСЭДОФССПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 			= ДополнительныеПараметры.Результат;
	Организация 				= ДополнительныеПараметры.Организация;
	Операция 					= ДополнительныеПараметры.Операция;
	ИдентификаторСообщения 		= ДополнительныеПараметры.ИдентификаторСообщения;
	ТипВзаимодействия 			= ДополнительныеПараметры.ТипВзаимодействия;
	ДанныеЗапроса 				= РезультатОперации.ДанныеЗапроса;
	СвойстваОбменаПоСЭДОЧерезОператора = ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	// Данные запроса для повтора в случае ошибки.
	ДанныеЗапроса.ИдентификаторыСообщений.Добавить(ИдентификаторСообщения);
	ДанныеЗапроса.ТипВзаимодействия 				= ТипВзаимодействия;
	ДанныеЗапроса.Операция 							= Операция;
	ДанныеЗапроса.Организация 						= Организация;
	ДанныеЗапроса.АдресСервера 						= Результат.АдресСервера;
	ДанныеЗапроса.АдресРесурса 						= Результат.АдресРесурса;
	ДанныеЗапроса.КодСостояния 						= Результат.КодСостояния;
	ДанныеЗапроса.ТекстОшибки 						= Результат.ОписаниеОшибки;
	ДанныеЗапроса.АдресЗашифрованногоЗапросаSOAP 	= Результат.АдресЗашифрованногоЗапросаSOAP;
	ДанныеЗапроса.ОшибкаСоединения 					= Результат.ОшибкаСоединения;
	ДанныеЗапроса.АдресПодписаногоЗапросаSOAP 		= Результат.АдресПодписаногоЗапросаSOAP;
	ДанныеЗапроса.АдресРасшифрованногоОтветаSOAP 	= Результат.АдресРасшифрованногоОтветаSOAP;
	
	// Данные запроса для сохранения в регистр журнала запросов.
	ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция);
	ПараметрыЗапроса.АдресЗапросаSOAP 		= Результат.АдресПодписаногоЗапросаSOAP;
	ПараметрыЗапроса.АдресОтветаSOAP 		= Результат.АдресРасшифрованногоОтветаSOAP;
	ПараметрыЗапроса.АдресРесурса 			= Результат.АдресРесурса;
	ПараметрыЗапроса.АдресСервера 			= Результат.АдресСервера;
	ПараметрыЗапроса.КодСостояния 			= Результат.КодСостояния;
	ПараметрыЗапроса.ТипВзаимодействияСЭДО 	= ТипВзаимодействия;
	ПараметрыЗапроса.УдалятьИзХранилища 	= Истина;
	ПараметрыЗапроса.ОбменЧерезОператора 	= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	
	РезультатОперации.ОшибкаСоединения = Результат.ОшибкаСоединения;
	
	Если Результат.Выполнено Тогда
		АдресРезультата = Результат.АдресРасшифрованногоОтветаSOAP;
		ТекстОтвета = ПолучитьИзВременногоХранилища(АдресРезультата);
		
		// Разбор ответа.
		Попытка
			РезультатРазбора = РазобратьОтветПолучитьСообщениеСЭДОФСС(ТекстОтвета, , ПараметрыЗапроса.Идентификатор);
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			КраткоеПредставлениеОшибки = ОбработкаОшибок.КраткоеПредставлениеОшибки(ИнформацияОбОшибке);
			ПодробноеПредставлениеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			Шаблон = НСтр("ru='При разборе данных сообщения %1 возникла ошибка:'") + Символы.ПС
				+ "%2";
			РезультатОперации.ОписаниеОшибки = СтрШаблон(Шаблон,
				ИдентификаторСообщения, КраткоеПредставлениеОшибки);
			Шаблон = НСтр("ru='При разборе данных сообщения %1 возникла ошибка. Текст сообщения:'") + Символы.ПС
				+ "%2" + Символы.ПС + "%3"; 
			ОписаниеОшибки = СтрШаблон(Шаблон,
				ИдентификаторСообщения, ТекстОтвета, ПодробноеПредставлениеОшибки);
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки, ,
				Истина);
			ПараметрыЗапроса.ОписаниеОшибки = РезультатОперации.ОписаниеОшибки;
			ПараметрыЗапроса.ИдентификаторФСС = ИдентификаторСообщения;
			СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
			Возврат;
		КонецПопытки;
		
		ПараметрыЗапроса.ТипСообщенияСЭДО = РезультатРазбора.ДанныеСообщения.Тип;
		Если ЗначениеЗаполнено(РезультатРазбора.ДанныеСообщения.Идентификатор) Тогда
			ПараметрыЗапроса.ИдентификаторФСС = РезультатРазбора.ДанныеСообщения.Идентификатор;
		Иначе
			ПараметрыЗапроса.ИдентификаторФСС = ИдентификаторСообщения;
		КонецЕсли;
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			Если РезультатРазбора.СообщениеНеНайдено Тогда
				ОбработатьОшибкуСообщениеСЭДОНеНайдено(Организация, ИдентификаторСообщения);
				РезультатОперации.СообщенияНеНайдены = Истина;
				РезультатОперации.ИдентификаторыНеНайденных.Добавить(ИдентификаторСообщения);
			КонецЕсли;
			
			ТекстОшибки = СтрСоединить(РезультатРазбора.Ошибки, Символы.ПС);
			РезультатОперации.ОшибкиПоИдентификаторам.Вставить(ИдентификаторСообщения, ТекстОшибки);
			
			// Получен список ошибок.
			Шаблон = ?(СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую,
				НСтр("ru = 'В тексте сообщения с идентификатором %1 с сервера СЭДО ФСС получена ошибка'"),
				НСтр("ru = 'В тексте сообщения с идентификатором %1 с сервера оператора СЭДО ФСС получена ошибка'"));
			Шаблон = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(Шаблон, Истина);
			Шаблон = Шаблон + ":
				|%2";
			ОписаниеОшибки = СтрШаблон(Шаблон, ИдентификаторСообщения, ТекстОшибки);
			РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
			// Записываем ошибку в журнал регистрации.
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение сообщения СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки,,
				Истина);
			ПараметрыЗапроса.ОписаниеОшибки = ОписаниеОшибки;
		Иначе
			// Сообщение было успешно разобрано.
			ДанныеСообщения = РезультатРазбора.ДанныеСообщения;
			ДанныеСообщения.ИдентификаторЗапроса = ПараметрыЗапроса.Идентификатор;
			РезультатОперации.ДанныеСообщений = ЗначениеВКоллекцию(ДанныеСообщения);
			РезультатСохранения = СохранитьДанныеВходящихСообщенийСЭДОФСС(
				Организация,
				ДанныеСообщения,
				ТипВзаимодействия,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
				
			Если РезультатСохранения.БылиОшибкиОбработки Тогда
				// Сохраняем данные запроса в журнал запросов.
				СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
				// Удаляем запрос по сообщению из очереди фонового получения.
				УбратьЗапросИзОчередиФоновогоЗадания(ДанныеЗапроса);
				РезультатОперации.ОписаниеОшибки = СтрСоединить(РезультатСохранения.ОшибкиОбработки, Символы.ПС);
				ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
				Возврат;
			КонецЕсли;
			РезультатОперации.Выполнено = Истина;
		КонецЕсли;
		
		// Сохраняем данные запроса в журнал запросов.
		СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
		
	Иначе
		Шаблон = НСтр("ru='Не удалось получить сообщение СЭДО СФР.
			|%1'");
		РезультатОперации.ОписаниеОшибки = СтрШаблон(Шаблон, Результат.ОписаниеОшибки);
		РезультатОперации.ОтмененоПользователем = Результат.ОтмененоПользователем;
	КонецЕсли;
	
	Если НЕ ПриВыполненииЗапросаВозниклаОшибкиСоединения(Результат) Тогда
		// Удаляем запрос по сообщению из очереди фонового получения.
		УбратьЗапросИзОчередиФоновогоЗадания(ДанныеЗапроса);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбработатьОшибкуСообщениеСЭДОНеНайдено(Организация, ИдентификаторСообщения)
	
	ТипСтрока = НЕ ЭлектронныйДокументооборотСФСС.ТипСообщенияЧислоВРегистреВходящихСообщенийСЭДО();
	Типы = ДокументооборотСФССКлиентСервер.ТипыСообщенийСЭДО(ТипСтрока);
	
	Запись = РегистрыСведений.ВходящиеСообщенияСЭДОФСС.СоздатьМенеджерЗаписи();
	Запись.Организация = Организация;
	Запись.Идентификатор = ИдентификаторСообщения;
	Запись.Прочитать();
	Тип332 = Типы.ТипСообщенияОтветНаЗапросИсторическихДанных;
	Если Запись.Тип = Тип332 Тогда
		// Снимает отметку Новое для сообщений 332 типа, так как они удаляются
		// с сервера через неделю после создания.
		Запись.Новое = Ложь;
		Попытка
			Запись.Записать();
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			Шаблон = НСтр("ru='При записи данных регистра ВходящиеСообщенияСЭДОФСС по сообщению %1 возникла ошибка :'")
				+ Символы.ПС + "%2";
			ТекстОшибки = СтрШаблон(Шаблон, ИдентификаторСообщения, ТекстОшибки);
			ЗаписатьСобытиеСЭДОВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстОшибки);
		КонецПопытки;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура УбратьЗапросИзОчередиФоновогоЗадания(ДанныеЗапроса)
	
	УстановитьПривилегированныйРежим(Истина);
	Запись = РегистрыСведений.ЗапросыДляПовторнойОтправкиСЭДО.СоздатьМенеджерЗаписи();
	Запись.Организация = ДанныеЗапроса.Организация;
	Запись.Идентификатор = ДанныеЗапроса.ИдентификаторыСообщений[0];
	Запись.Прочитать();
	Если ЗначениеЗаполнено(Запись.Идентификатор) Тогда
		Попытка
			Запись.Удалить();
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			Шаблон = НСтр("ru='При записи данных регистра ""Запросы для повторной отправки СЭДО"" возникла ошибка:'")
				+ Символы.ПС + "%1";
			ТекстОшибки = СтрШаблон(Шаблон, ТекстОшибки);
			ЗаписатьСобытиеСЭДОВЖурнал(УровеньЖурналаРегистрации.Ошибка, ТекстОшибки);
			ТекстОшибки = НСтр("ru='При удалении записи регистра ""Запросы для повторной отправки СЭДО"" возникла ошибка.'");
			ПараметрыВывода = ЭлектронныйДокументооборотСФССКлиентСервер.НовыеПараметрыВывестиОшибку(Истина);
			ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(ТекстОшибки, ПараметрыВывода);
		КонецПопытки;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ПолучитьСообщенияСЭДОЗаПериод

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериод(ОповещениеОбратногоВызова,
		Организации,
		ДатаНачала,
		ДатаОкончания,
		ПараметрыПолучения) Экспорт
		
	РезультатПолучения = Новый Структура;
	РезультатПолучения.Вставить("ДанныеСообщений", 						Новый Массив);
	РезультатПолучения.Вставить("БылиОшибки", 							Ложь);
	РезультатПолучения.Вставить("Ошибки", 								Новый Массив);
	РезультатПолучения.Вставить("РезультатыПолучения", 					Новый Массив);
	РезультатПолучения.Вставить("ОперацияБылаОтмененаПользователем", 	Ложь);
	РезультатПолучения.Вставить("ОшибкиПоОрганизациям", Новый Соответствие);
	
	// Формируем даты периода.
	ДатыПериода = Новый Массив;
	ТекущаяДата = НачалоДня(ДатаНачала);
	Если ДатаНачала > ДатаОкончания Тогда
		РезультатПолучения.БылиОшибки = Истина;
		ТекстОшибки = НСтр("ru='Дата начала периода больше чем дата окончания периода получения сообщений.'");
		РезультатПолучения.Ошибки.Добавить(ТекстОшибки);
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения);
		Возврат;
	КонецЕсли;
	ВремяНачалоДня = Дата(1, 1, 1, 0, 0, 0);
	ВремяКонцаДня = Дата(1, 1, 1, 23, 59, 59);
	Сутки = 3600 * 24;
	ДатаОкончанияКонецДня = КонецДня(ДатаОкончания);
	Пока ТекущаяДата < ДатаОкончанияКонецДня Цикл
		ДатаПериода = ЭлектронныйДокументооборотСФССКлиентСервер.ПолучитьСтрокуДатВСтруктуре(
			ТекущаяДата, ВремяНачалоДня, ВремяКонцаДня);
		ДатыПериода.Добавить(ДатаПериода);
		ТекущаяДата = ТекущаяДата + Сутки;
	КонецЦикла;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 			ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Организации", 						Организации);
	ДополнительныеПараметры.Вставить("ДатаНачала", 							ДатаНачала);
	ДополнительныеПараметры.Вставить("ДатаОкончания", 						ДатаОкончания);
	ДополнительныеПараметры.Вставить("ОткрыватьФормуДлительнойОперации", 	ПараметрыПолучения.ОткрыватьФормуДлительнойОперации);
	ДополнительныеПараметры.Вставить("ЗадаватьВопросОТестовомСервере", 		ПараметрыПолучения.ЗадаватьВопросОТестовомСервере);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 					ПараметрыПолучения.ТипВзаимодействия);
		ДополнительныеПараметры.Вставить("ПринудительноПовторноПолучитьСообщения",
		ПараметрыПолучения.ПринудительноПовторноПолучитьСообщения);
	ДополнительныеПараметры.Вставить("РезультатПолучения", 					РезультатПолучения);
	ДополнительныеПараметры.Вставить("СброситьСвойстваОбменаПоСЭДОЧерезОператора",
		ПараметрыПолучения.СброситьСвойстваОбменаПоСЭДОЧерезОператора);
	ДополнительныеПараметры.Вставить("ФормаДлительнойОперации", 			Неопределено);
	ДополнительныеПараметры.Вставить("ДатыПериода", 						ДатыПериода);
	ДополнительныеПараметры.Вставить("ИндексТекущейОрганизации",			0);
	ДополнительныеПараметры.Вставить("ТекущаяОрганизация",					Неопределено);
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоОрганизациям",		Новый Соответствие);
	ДополнительныеПараметры.Вставить("ДанныеСообщений332",	Новый Массив);
	ДополнительныеПараметры.Вставить("ИндексТекущегоСообщения", 			0);
	ДополнительныеПараметры.Вставить("ИдентификаторыЗагруженныхСообщений332",Новый Массив);
	ДополнительныеПараметры.Вставить("ИдентификаторыВходящихПоОрганизациям", Новый Соответствие);
	ДополнительныеПараметры.Вставить("ИнтервалОжиданияСообщения332", 		5);
	ДополнительныеПараметры.Вставить("ДатаНачалаОжидания", 					Неопределено);
	ДополнительныеПараметры.Вставить("ПериодОбработчикаОжидания", 			5);
	
	// Открываем форму длительной операции.
	ПараметрыОжидания = Новый Структура();
	ПараметрыОжидания.Вставить("ТекстСообщения", НСтр("ru = 'Получение сообщений СЭДО за период.'"));
	Если ПараметрыПолучения.ОткрыватьФормуДлительнойОперации Тогда
		ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
			"ОбщаяФорма.ДлительнаяОперация", ПараметрыОжидания);
	КонецЕсли;
	
	ПолучитьСообщенияСЭДОЗаПериодЗапросИдентификторовЦикл(ДополнительныеПараметры);
	
КонецПроцедуры 

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗапросИдентификторовЦикл(ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	ИндексТекущейОрганизации = ДополнительныеПараметры.ИндексТекущейОрганизации;
	Организации = ДополнительныеПараметры.Организации;
	ДатыПериода = ДополнительныеПараметры.ДатыПериода;
	ЗадаватьВопросОТестовомСервере = ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = ДополнительныеПараметры.СброситьСвойстваОбменаПоСЭДОЧерезОператора;
	СвойстваОбменаПоОрганизациям = ДополнительныеПараметры.СвойстваОбменаПоОрганизациям;
	ФормаДлительнойОперации = ДополнительныеПараметры.ФормаДлительнойОперации;
	ДанныеСообщений332 = ДополнительныеПараметры.ДанныеСообщений332;
	
	Если ИндексТекущейОрганизации > Организации.Количество() - 1 Тогда
		Если ДанныеСообщений332.Количество() = 0 Тогда
			РезультатПолучения.БылиОшибки = Истина;
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения); 
			ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
		Иначе
			ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332(ДополнительныеПараметры);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ФормаДлительнойОперацииОткрыта(ФормаДлительнойОперации) Тогда
		// Пользователь нажал Отмена в форме загрузки.
		РезультатПолучения.ОперацияБылаОтмененаПользователем = Истина;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения); 
		Возврат;
	КонецЕсли;
	
	Организация = Организации[ИндексТекущейОрганизации];
	ДополнительныеПараметры.ТекущаяОрганизация = Организация;
	
	Если СброситьСвойстваОбменаПоСЭДОЧерезОператора Тогда
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, Истина);
	КонецЕсли;
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	СвойстваОбменаПоОрганизациям[Организация] = СвойстваОбменаПоСЭДОЧерезОператора;
	
	Результат = РезультатМетаданныеВходящихСообщенийСЭДОФСС();
	
	ПараметрыПолучения = ПараметрыМетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(Организация, ДатыПериода);
	ПараметрыПолучения.ЗадаватьВопросОТестовомСервере = ЗадаватьВопросОТестовомСервере;
	ПараметрыПолучения.Результат = Результат;
	ПараметрыПолучения.СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора;
	Оповещение = Новый ОписаниеОповещения(
		"ПолучитьСообщенияСЭДОЗаПериодЗапросИдентификаторовЦиклПослеОтправки330", ЭтотОбъект, ДополнительныеПараметры);
	ПараметрыПолучения.ОповещениеОбратногоВызова = Оповещение;
	ПараметрыПолучения.ЭтоПользовательскийЗапрос = Истина;
	
	МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(ПараметрыПолучения);
	// Отключаем вопрос о тестовом сервере.
	ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере = Ложь;
	
КонецПроцедуры 

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗапросИдентификаторовЦиклПослеОтправки330(Результат, ДополнительныеПараметры) Экспорт
	
	СвойстваОбменаПоОрганизациям = ДополнительныеПараметры.СвойстваОбменаПоОрганизациям;
	ОшибкиПоОрганизациям = ДополнительныеПараметры.РезультатПолучения.ОшибкиПоОрганизациям;
	ТекущаяОрганизация = ДополнительныеПараметры.ТекущаяОрганизация;
	ИндексТекущейОрганизации = ДополнительныеПараметры.ИндексТекущейОрганизации;
	ДанныеСообщений332 = ДополнительныеПараметры.ДанныеСообщений332;
	ИдентификаторыЗагруженныхСообщений332 = ДополнительныеПараметры.ИдентификаторыЗагруженныхСообщений332;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	ПринудительноПовторноПолучитьСообщения = ДополнительныеПараметры.ПринудительноПовторноПолучитьСообщения;
	ИдентификаторыВходящихПоОрганизациям = ДополнительныеПараметры.ИдентификаторыВходящихПоОрганизациям;
	
	Если НЕ Результат.Выполнено Тогда
		Ошибки = ОшибкиПоОрганизациям[ТекущаяОрганизация];
		Если Ошибки = Неопределено Тогда
			Ошибки = Новый Массив;
		КонецЕсли;
		Ошибки.Добавить(Результат.ОписаниеОшибки);
		ОшибкиПоОрганизациям[ТекущаяОрганизация] = Ошибки;
		РезультатПолучения.Ошибки.Добавить(Результат.ОписаниеОшибки);
		РезультатПолучения.БылиОшибки = Истина;
	Иначе
		СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоОрганизациям[ТекущаяОрганизация];
		Сообщение = Новый Структура();
		Сообщение.Вставить("Организация", ТекущаяОрганизация);
		Сообщение.Вставить("ИдентификаторСообщения", Результат.ИдентификаторСообщения);
		ДанныеСообщений332.Добавить(Сообщение);
		Если СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			// При прямом обмене сообщение 332 обычно загружается сразу.
			Если НЕ Результат.Сообщение332НеНайдено Тогда
				ИдентификаторыЗагруженныхСообщений332.Добавить(Результат.ИдентификаторСообщения);
				Если ПринудительноПовторноПолучитьСообщения Тогда
					ИдентификаторыВходящихПоОрганизациям[ТекущаяОрганизация] = Результат.ИдентификаторыСообщенийИз332;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры.ИндексТекущейОрганизации = ИндексТекущейОрганизации + 1;
	ПолучитьСообщенияСЭДОЗаПериодЗапросИдентификторовЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332(ДополнительныеПараметры) Экспорт
	
	ИнтервалОжиданияСообщения332 = ДополнительныеПараметры.ИнтервалОжиданияСообщения332;
	СвойстваОбменаПоОрганизациям = ДополнительныеПараметры.СвойстваОбменаПоОрганизациям;
	ПериодОбработчикаОжидания = ДополнительныеПараметры.ПериодОбработчикаОжидания;
	
	ЕстьОбменЧерезОператора = Ложь;
	Для каждого КлючЗначение Из СвойстваОбменаПоОрганизациям Цикл
		СвойстваОбменаПоСЭДОЧерезОператора = КлючЗначение.Значение;
		Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			ЕстьОбменЧерезОператора = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	// Делаем паузу перед следующей попыткой скачать 332 сообщения (список сообщений в ответ на тип 330).
	ДополнительныеПараметры.ДатаНачалаОжидания = ТекущаяДата();
	ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ДополнительныеПараметры,
		"БРО.ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений");
	ПодключитьОбработчикОжидания("Подключаемый_ПолучитьСообщенияСЭДОЗаПериод", ПериодОбработчикаОжидания, Истина);
	
	ТекстСообщения = НСтр("ru = 'СЭДО СФР'");
	Шаблон = НСтр("ru='Пауза %1 секунд перед скачиванием списка сообщений.'");
	Пояснение = СтрШаблон(Шаблон, ИнтервалОжиданияСообщения332);
	Пояснение = Пояснение + " " + НСтр("ru='Загрузку можно прервать, данные будут загружены позднее автоматически...'");
	Состояние(ТекстСообщения, , Пояснение);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332Цикл(ДополнительныеПараметры) Экспорт
	
	Сообщения332 = ДополнительныеПараметры.ДанныеСообщений332;
	ИндексТекущегоСообщения = ДополнительныеПараметры.ИндексТекущегоСообщения;
	ЗадаватьВопросОТестовомСервере = ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	ТипВзаимодействия = ДополнительныеПараметры.ТипВзаимодействия;
	ИдентификаторыЗагруженныхСообщений332 = ДополнительныеПараметры.ИдентификаторыЗагруженныхСообщений332;
	ФормаДлительнойОперации = ДополнительныеПараметры.ФормаДлительнойОперации;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	
	Если ИндексТекущегоСообщения > Сообщения332.Количество() - 1 Тогда
		Если ИдентификаторыЗагруженныхСообщений332.Количество() = Сообщения332.Количество() Тогда
			// Были загружены все сообщения 332.
			ДополнительныеПараметры.ИндексТекущейОрганизации = 0;
			ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщенийЦикл(ДополнительныеПараметры);
		Иначе
			// Включаем обработчик ожидания и делаем следующую попытку загружить сообщения 332.
			ДополнительныеПараметры.ИндексТекущегоСообщения = 0;
			ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332(ДополнительныеПараметры);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ФормаДлительнойОперацииОткрыта(ФормаДлительнойОперации) Тогда
		// Пользователь нажал Отмена в форме загрузки.
		РезультатПолучения.ОперацияБылаОтмененаПользователем = Истина;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения); 
		Возврат;
	КонецЕсли;
	
	Сообщение = Сообщения332[ИндексТекущегоСообщения];
	ДополнительныеПараметры.ТекущаяОрганизация = Сообщение.Организация;
	Если НЕ ИдентификаторыЗагруженныхСообщений332.Найти(Сообщение.ИдентификаторСообщения) = Неопределено Тогда
		// Сообщение 332 уже было загружено.
		ДополнительныеПараметры.ИндексТекущегоСообщения = ИндексТекущегоСообщения + 1;
		ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332Цикл(ДополнительныеПараметры);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332ПослеЗапроса", ЭтотОбъект, ДополнительныеПараметры);
	ПолучитьСообщениеСЭДОФСС(Оповещение,
		Сообщение.Организация,
		Сообщение.ИдентификаторСообщения,
		ЗадаватьВопросОТестовомСервере,
		ТипВзаимодействия,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332ПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ИдентификаторыЗагруженныхСообщений332 = ДополнительныеПараметры.ИдентификаторыЗагруженныхСообщений332;
	ИндексТекущегоСообщения = ДополнительныеПараметры.ИндексТекущегоСообщения;
	Сообщения332 = ДополнительныеПараметры.ДанныеСообщений332;
	ОшибкиПоОрганизациям = ДополнительныеПараметры.РезультатПолучения.ОшибкиПоОрганизациям;
	ТекущаяОрганизация = ДополнительныеПараметры.ТекущаяОрганизация;
	ПринудительноПовторноПолучитьСообщения = ДополнительныеПараметры.ПринудительноПовторноПолучитьСообщения;
	ИдентификаторыВходящихПоОрганизациям = ДополнительныеПараметры.ИдентификаторыВходящихПоОрганизациям;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	СвойстваОбменаПоОрганизациям = ДополнительныеПараметры.СвойстваОбменаПоОрганизациям;
	ИнтервалОжиданияСообщения332 = ДополнительныеПараметры.ИнтервалОжиданияСообщения332;
	
	СвойстваОбменаПоОрганизации = СвойстваОбменаПоОрганизациям[ТекущаяОрганизация];
	Если НЕ Результат.Выполнено Тогда
		Ошибки = ОшибкиПоОрганизациям[ТекущаяОрганизация];
		Если Ошибки = Неопределено Тогда
			Ошибки = Новый Массив;
		КонецЕсли;
		Если СвойстваОбменаПоОрганизации.ОбменПоСЭДОНапрямую Тогда
			ТекстОшибки = НСтр("ru='Не был загружен ответ сервера СЭДО со списком сообщений. Через %1 секунд будет выполнена ещё одна попытка загрузки...'");
		Иначе
			ТекстОшибки = НСтр("ru='Не был загружен ответ оператора со списком сообщений, возможно сообщения ещё не подготовлены. Через %1 секунд будет выполнена ещё одна попытка загрузки...'");
		КонецЕсли;
		ТекстОшибки = СтрШаблон(ТекстОшибки, ИнтервалОжиданияСообщения332);
		
		РезультатПолучения.Ошибки.Добавить(ТекстОшибки);
		Ошибки.Добавить(Результат.ОписаниеОшибки);
		ОшибкиПоОрганизациям[ТекущаяОрганизация] = Ошибки;
	Иначе
		Сообщение = Сообщения332[ИндексТекущегоСообщения];
		ИдентификаторыЗагруженныхСообщений332.Добавить(Сообщение.ИдентификаторСообщения);
		Если ПринудительноПовторноПолучитьСообщения Тогда
			РезультатРазбора = РазобратьОтветВходящиеСообщенияСЭДОФСС(
				Результат.ДанныеСообщений[0].ТекстСообщения, Неопределено, СвойстваОбменаПоОрганизации);
			ДанныеСообщений = РезультатРазбора.ДанныеСообщений;
			Идентификаторы = Новый Массив;
			Для каждого Данные Из ДанныеСообщений Цикл
				Идентификаторы.Добавить(Данные.Идентификатор);
			КонецЦикла;
			ИдентификаторыВходящихПоОрганизациям[ТекущаяОрганизация] = Идентификаторы;
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры.ИндексТекущегоСообщения = ИндексТекущегоСообщения + 1;
	ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332Цикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщенийЦикл(ДополнительныеПараметры) Экспорт
	
	// Загрузка сообщений.
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	ИндексТекущейОрганизации = ДополнительныеПараметры.ИндексТекущейОрганизации;
	Организации = ДополнительныеПараметры.Организации;
	ЗадаватьВопросОТестовомСервере = ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = ДополнительныеПараметры.СброситьСвойстваОбменаПоСЭДОЧерезОператора;
	СвойстваОбменаПоОрганизациям = ДополнительныеПараметры.СвойстваОбменаПоОрганизациям; 
	ФормаДлительнойОперации = ДополнительныеПараметры.ФормаДлительнойОперации; 
	ТипВзаимодействия = ДополнительныеПараметры.ТипВзаимодействия;
	ПринудительноПовторноПолучитьСообщения = ДополнительныеПараметры.ПринудительноПовторноПолучитьСообщения;
	ИдентификаторыВходящихПоОрганизациям = ДополнительныеПараметры.ИдентификаторыВходящихПоОрганизациям;
	ДатаНачала = НачалоДня(ДополнительныеПараметры.ДатаНачала);
	
	Если ИндексТекущейОрганизации > Организации.Количество() - 1 Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения); 
		ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
		Возврат;
	КонецЕсли;
	
	Организация = Организации[ИндексТекущейОрганизации];
	ДополнительныеПараметры.ТекущаяОрганизация = Организация;
	
	ТипыВзаимодействий = Неопределено;
	Если ПринудительноПовторноПолучитьСообщения Тогда
		// Загружаемс все сообщения, которые пришли в 332.
		Идентификаторы = ИдентификаторыВходящихПоОрганизациям[Организация];
	Иначе
		НовыеСообщения = НовыеСообщенияСЭДОФСС(Организация, , ТипВзаимодействия, ДатаНачала);
		Если НовыеСообщения.Количество() = 0 Тогда
			ДополнительныеПараметры.ИндексТекущейОрганизации = ИндексТекущейОрганизации + 1;
			ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщенийЦикл(ДополнительныеПараметры);
			Возврат;
		КонецЕсли;
		
		Идентификаторы = Новый Массив;
		ТипыВзаимодействий = Новый Массив;
		
		Для каждого Сообщение Из НовыеСообщения Цикл
			Идентификаторы.Добавить(Сообщение.Идентификатор);
			ТипыВзаимодействий.Добавить(Сообщение.ТипВзаимодействия);
		КонецЦикла;
	КонецЕсли;
	
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = Ложь;
	
	// Получаем содержимое входящих сообщений по организации.
	Оповещение = Новый ОписаниеОповещения("ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщенийЦиклПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПолучитьСообщенияСЭДОФСС(
		Оповещение,
		Организация,
		Идентификаторы,
		Ложь,
		ЗадаватьВопросОТестовомСервере,
		ТипыВзаимодействий,
		СброситьСвойстваОбменаПоСЭДОЧерезОператора,
		ФормаДлительнойОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщенийЦиклПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОшибкиПоОрганизациям = ДополнительныеПараметры.РезультатПолучения.ОшибкиПоОрганизациям;
	ТекущаяОрганизация = ДополнительныеПараметры.ТекущаяОрганизация;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	
	Если Результат.БылиОшибки Тогда
		РезультатПолучения.БылиОшибки = Истина;
		Ошибки = ОшибкиПоОрганизациям[ТекущаяОрганизация];
		Если Ошибки = Неопределено Тогда
			Ошибки = Новый Массив;
		КонецЕсли;
		Для каждого Ошибка Из Результат.Ошибки Цикл
			Ошибки.Добавить(Ошибка);
			РезультатПолучения.Ошибки.Добавить(Ошибка);
		КонецЦикла;
		ОшибкиПоОрганизациям[ТекущаяОрганизация] = Ошибки;
	КонецЕсли;
	
	// Извлекаем результаты.
	Для каждого Данные Из Результат.ДанныеСообщений Цикл
		РезультатПолучения.ДанныеСообщений.Добавить(Данные);
	КонецЦикла;
	Для каждого ДанныеРезультата Из Результат.РезультатыПолучения Цикл
		РезультатПолучения.РезультатыПолучения.Добавить(ДанныеРезультата);
	КонецЦикла;
	
	ИндексТекущейОрганизации = ДополнительныеПараметры.ИндексТекущейОрганизации;
	
	ДополнительныеПараметры.ИндексТекущейОрганизации = ИндексТекущейОрганизации + 1;
	ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщенийЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПолучитьСообщенияСЭДОЗаПериод()

	ДополнительныеПараметры = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений");
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ФормаДлительнойОперации = ДополнительныеПараметры.ФормаДлительнойОперации;
	ДатаНачалаОжидания = ДополнительныеПараметры.ДатаНачалаОжидания;
	ПериодОбработчикаОжидания = ДополнительныеПараметры.ПериодОбработчикаОжидания;
	ИнтервалОжиданияСообщения332 = ДополнительныеПараметры.ИнтервалОжиданияСообщения332;
	РезультатПолучения = ДополнительныеПараметры.РезультатПолучения;
	
	Если НЕ ФормаДлительнойОперацииОткрыта(ФормаДлительнойОперации) Тогда
		// Пользователь нажал Отмена в форме загрузки.
		РезультатПолучения.ОперацияБылаОтмененаПользователем = Истина;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатПолучения);
		Возврат;
	КонецЕсли;
	
	Если ТекущаяДата() - ДатаНачалаОжидания >= ИнтервалОжиданияСообщения332 Тогда
		// Заданный интервал закончился.
		МаксимальныйИнтервалОжидания = 60;
		ИнтервалОжиданияСообщения332 = ИнтервалОжиданияСообщения332 * 2;
		Если ИнтервалОжиданияСообщения332 > МаксимальныйИнтервалОжидания Тогда
			ИнтервалОжиданияСообщения332 = МаксимальныйИнтервалОжидания;
		КонецЕсли;
		ДополнительныеПараметры.ИнтервалОжиданияСообщения332 = ИнтервалОжиданияСообщения332;
		// Продолжаем получение.
		ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений332Цикл(ДополнительныеПараметры);
	Иначе
		// Продолжаем ожидание.
		ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ДополнительныеПараметры,
			"БРО.ПолучитьСообщенияСЭДОЗаПериодЗагрузкаСообщений");
		ПодключитьОбработчикОжидания("Подключаемый_ПолучитьСообщенияСЭДОЗаПериод", ПериодОбработчикаОжидания, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция РезультатМетаданныеВходящихСообщенийСЭДОФСС()
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеСообщений", Новый Массив);
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	Результат.Вставить("АдресЗашифрованногоЗапросаSOAP", "");
	Результат.Вставить("ИдентификаторСообщения", "");
	Результат.Вставить("ИдентификаторыСообщенийИз332", Новый Массив);
	Результат.Вставить("Сообщение332НеНайдено", Ложь);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область МетаданныеВходящихСообщенийОрганизацийСЭДОФСС

&НаКлиенте
Процедура МетаданныеВходящихСообщенийОрганизацийСЭДОФСС(ОповещениеОбратногоВызова,
		Организации,
		СообщенияСДаты = Неопределено,
		СообщенияПоДату = Неопределено,
		ТипВзаимодействия = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ДанныеСообщенийПоОрганизациям", Новый Соответствие);
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 	ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Результат", 					Результат);
	ДополнительныеПараметры.Вставить("Организации", 				Организации);
	ДополнительныеПараметры.Вставить("ИндексОрганизации", 			0);
	ДополнительныеПараметры.Вставить("СообщенияСДаты", 				СообщенияСДаты);
	ДополнительныеПараметры.Вставить("СообщенияПоДату", 			Неопределено);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 			ТипВзаимодействия);
	
	МетаданныеВходящихСообщенийОрганизацийСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийОрганизацийСЭДОФССЦикл(ДополнительныеПараметры)
	
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 			= ДополнительныеПараметры.Результат;
	ИндексОрганизации 			= ДополнительныеПараметры.ИндексОрганизации;
	Организации 				= ДополнительныеПараметры.Организации;
	Результат 					= ДополнительныеПараметры.Результат;
	СообщенияСДаты 				= ДополнительныеПараметры.СообщенияСДаты;
	ТипВзаимодействия 			= ДополнительныеПараметры.ТипВзаимодействия;
	
	Если ИндексОрганизации >= Организации.Количество() Тогда
		Результат.Выполнено = Истина;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Организация = Организации[ИндексОрганизации];
	
	Оповещение = Новый ОписаниеОповещения("МетаданныеВходящихСообщенийОрганизацийСЭДОФССЦиклПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	МетаданныеВходящихСообщенийСЭДОФСС(
		Оповещение,
		Организация,
		СообщенияСДаты,,
		ТипВзаимодействия);
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийОрганизацийСЭДОФССЦиклПослеЗапроса(РезультатПолучитьДанныеСообщений, ДополнительныеПараметры) Экспорт
	
	Результат = ДополнительныеПараметры.Результат;
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексОрганизации = ДополнительныеПараметры.ИндексОрганизации;
	Организации = ДополнительныеПараметры.Организации;
	ДанныеСообщенийПоОрганизациям = Результат.ДанныеСообщенийПоОрганизациям;
	
	Если НЕ РезультатПолучитьДанныеСообщений.Выполнено Тогда
		Результат.ОписаниеОшибки = РезультатПолучитьДанныеСообщений.ОписаниеОшибки;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Организация = Организации[ИндексОрганизации];
	ДанныеСообщенийПоОрганизациям.Вставить(Организация, РезультатПолучитьДанныеСообщений.ДанныеСообщений);
	
	ИндексОрганизации = ИндексОрганизации + 1;
	ДополнительныеПараметры.ИндексОрганизации = ИндексОрганизации;
	
	МетаданныеВходящихСообщенийОрганизацийСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

#КонецОбласти

#Область МетаданныеВходящихСообщенийСЭДОФСС

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФСС(
		ОповещениеОбратногоВызова,
		Организация,
		ДатаСообщений = Неопределено,
		НеПроверятьПодписьФСС = Ложь,
		ТипВзаимодействия = Неопределено,
		СброситьСвойстваОбменаПоСЭДОЧерезОператора = Истина) Экспорт
	
	Результат = РезультатМетаданныеВходящихСообщенийСЭДОФСС();
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	ОтображатьСостояние = Истина;
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ОтображатьСостояние") Тогда
		ОтображатьСостояние = ОповещениеОбратногоВызова.ДополнительныеПараметры.ОтображатьСостояние;
	КонецЕсли;
	
	ЗадаватьВопросОТестовомСервере = Истина;
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ЗадаватьВопросОТестовомСервере") Тогда
		ЗадаватьВопросОТестовомСервере = 
			ОповещениеОбратногоВызова.ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	КонецЕсли;
	
	Настройки = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Настройки.ТипВзаимодействия = ТипВзаимодействия;
	ЭтоТипВзаимодействияОрганизацияСЭДО =
		(ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияОрганизацияСЭДО());
	ПолучитьОГРН 			= ЭтоТипВзаимодействияОрганизацияСЭДО;
	ПолучитьИННКППиСНИЛС 	= ЭтоТипВзаимодействияОрганизацияСЭДО;
	СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
		Организация,,
		ПолучитьОГРН,,
		ПолучитьИННКППиСНИЛС);
	РегистрационныйНомерФСС = СвойстваОрганизации.РегистрационныйНомерФСС;
	Если ЭтоТипВзаимодействияОрганизацияСЭДО Тогда
		ЗаполнитьЗначенияСвойств(Настройки, СвойстваОрганизации);
	КонецЕсли;
	
	Если СброситьСвойстваОбменаПоСЭДОЧерезОператора Тогда
		СброситьБезПолучения = Истина;
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, СброситьБезПолучения);
	КонецЕсли;
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	Настройки.ОбменПоСЭДОНапрямую 		= СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	Настройки.ИдентификаторАбонентаСЭДО = СвойстваОбменаПоСЭДОЧерезОператора.ИдентификаторАбонентаСЭДО;
	
	ТекстЗапроса = ТекстЗапросаПолученияКоличестваСообщенийСЭДОФСС(
		РегистрационныйНомерФСС,
		ДатаСообщений,
		Настройки);
	
	Операция = ОперацияПолученияСпискаВходящихСЭДОФСС();
	ЗапросДляПолученияДанных = ПараметрыЗапросаПолучитьДанныеСЭДОФСС();
	ЗапросДляПолученияДанных.Организация =Организация;
	ЗапросДляПолученияДанных.Операция = Операция;
	ЗапросДляПолученияДанных.ТекстЗапроса = ТекстЗапроса;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", 	ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Результат", 					Результат);
	ДополнительныеПараметры.Вставить("Организация", 				Организация);
	ДополнительныеПараметры.Вставить("Операция", 					Операция);
	ДополнительныеПараметры.Вставить("ТекстЗапроса", 				ТекстЗапроса);
	ДополнительныеПараметры.Вставить("ДатаСообщений", 				ДатаСообщений);
	ДополнительныеПараметры.Вставить("ТипВзаимодействия", 			ТипВзаимодействия);
	ДополнительныеПараметры.Вставить("ОтображатьСостояние", 		ОтображатьСостояние);
	ДополнительныеПараметры.Вставить("СвойстваОбменаПоСЭДОЧерезОператора", СвойстваОбменаПоСЭДОЧерезОператора);
	
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ТипСообщенияОтвета") Тогда
		ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",
			ОповещениеОбратногоВызова.ДополнительныеПараметры.ТипСообщенияОтвета);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("МетаданныеВходящихСообщенийСЭДОФССПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПолучитьДанныеСЭДОФСС(
		Оповещение,
		ЗапросДляПолученияДанных,
		ЗадаватьВопросОТестовомСервере,
		ТипВзаимодействия,
		ОтображатьСостояние,
		СвойстваОбменаПоСЭДОЧерезОператора);
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 			= ДополнительныеПараметры.Результат;
	Организация 				= ДополнительныеПараметры.Организация;
	Операция 					= ДополнительныеПараметры.Операция;
	ТекстЗапроса 				= ДополнительныеПараметры.ТекстЗапроса;
	ДатаСообщений 				= ДополнительныеПараметры.ДатаСообщений;
	ТипВзаимодействия 			= ДополнительныеПараметры.ТипВзаимодействия;
	СвойстваОбменаПоСЭДОЧерезОператора = ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	Если Результат.Выполнено Тогда
		
		// Сообщения были успешно получены.
		ТекстОтвета = ПолучитьИзВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP);
		
		// Разбор ответа сервера.
		РезультатРазбора = РазобратьОтветВходящиеСообщенияСЭДОФСС(ТекстОтвета, ДатаСообщений, СвойстваОбменаПоСЭДОЧерезОператора);
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			
			Шаблон = НСтр("ru='При получении данных сообщений ФСС произошла ошибка:
			|%1'");
			ОписаниеОшибки = СтрШаблон(Шаблон, РезультатРазбора.ОписаниеОшибки);
			РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки,,
				Истина);
				
			// В случае ошибки разбора сохраняем запрос в журнал запросов.
			ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция);
			ПараметрыЗапроса.ОписаниеОшибки = ОписаниеОшибки;
			ПараметрыЗапроса.АдресЗапросаSOAP = Результат.АдресПодписаногоЗапросаSOAP;
			ПараметрыЗапроса.АдресОтветаSOAP = Результат.АдресРасшифрованногоОтветаSOAP;
			ПараметрыЗапроса.АдресРесурса = Результат.АдресРесурса;
			ПараметрыЗапроса.АдресСервера = Результат.АдресСервера;
			ПараметрыЗапроса.КодСостояния = Результат.КодСостояния;
			ПараметрыЗапроса.ТипВзаимодействияСЭДО = ТипВзаимодействия;
			ПараметрыЗапроса.ОбменЧерезОператора 	= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
			СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
			
		Иначе
			
			ДанныеСообщений = РезультатРазбора.ДанныеСообщений;
			РезультатОперации.ДанныеСообщений = ДанныеСообщений;
			// Сохранение данных о сообщениях в БД.
			РезультатСохранения = СохранитьДанныеВходящихСообщенийСЭДОФСС(
				Организация,
				ДанныеСообщений,
				ТипВзаимодействия,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
			Если РезультатСохранения.БылиОшибкиОбработки Тогда
				РезультатОперации.ОписаниеОшибки = СтрСоединить(РезультатСохранения.ОшибкиОбработки, Символы.ПС);
				ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
				Возврат;
			КонецЕсли;
			
			ДанныеШироковещательныхСообщений = РезультатРазбора.ДанныеШироковещательныхСообщений;
			СохранитьДанныеРассылокСЭДОФСС(
				Организация,
				ДанныеШироковещательныхСообщений,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
			
			РезультатОперации.Выполнено = Истина;
			
		КонецЕсли;
		
		Если ЭтоАдресВременногоХранилища(Результат.АдресПодписаногоЗапросаSOAP) Тогда
			УдалитьИзВременногоХранилища(Результат.АдресПодписаногоЗапросаSOAP);
		КонецЕсли;
		
		Если ЭтоАдресВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP) Тогда
			УдалитьИзВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP);
		КонецЕсли;
		
	Иначе
		
		Если ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			// Для прямого обмена выполняем дополнительную обработку результата получения.
			ОбработатьОшибкуПриПолученииМетаданныхВходящихСЭДО(Результат, РезультатОперации, ДополнительныеПараметры);
		Иначе
			РезультатОперации.ОписаниеОшибки = Результат.ОписаниеОшибки;
		КонецЕсли;
	
	КонецЕсли;
		
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция МетаданныеВходящихСообщенийСЭДОАктуальны(Организация, ОбменПоСЭДОНапрямую = Неопределено)
	
	ДатаПоследнегоСеансаОбмена = ДатаПоследнегоСеансаОбменаСЭДО(Организация, ОбменПоСЭДОНапрямую);
	ТекущееВремяПоМоскве = ЭлектронныйДокументооборотСФСС.МосковскоеВремя();
	ПериодАктуальностиСпискаВходящих = 24 * 3600;
	
	Возврат ТекущееВремяПоМоскве - ДатаПоследнегоСеансаОбмена < ПериодАктуальностиСпискаВходящих;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДатаПоследнегоСеансаОбменаСЭДО(Организация, ОбменПоСЭДОНапрямую = Неопределено)
	
	ОбменЧерезОператора = Неопределено;
	Если НЕ ОбменПоСЭДОНапрямую = Неопределено Тогда
		ОбменЧерезОператора = НЕ ОбменПоСЭДОНапрямую;
	КонецЕсли; 
	
	Возврат ЭлектронныйДокументооборотСФСС.ДатаСобытияСЭДО(
		Организация, Перечисления.СобытияОбменаСЭДО.ПолучениеСпискаСообщений, ОбменЧерезОператора);
	
КонецФункции

&НаКлиенте
Функция ОбработатьОшибкуПриПолученииМетаданныхВходящихСЭДО(РезультатОтправкиЗапроса, РезультатПолученияВходящих, ДополнительныеПараметры)
	
	Организация 						= ДополнительныеПараметры.Организация;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	БылаОшибкаСоединения = ПриВыполненииЗапросаВозниклаОшибкиСоединения(РезультатОтправкиЗапроса);
	Если НЕ БылаОшибкаСоединения
		ИЛИ (БылаОшибкаСоединения
			И НЕ МетаданныеВходящихСообщенийСЭДОАктуальны(Организация, СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую))Тогда
		// Список входящих не получался дольше, чем заданный период.
		Если БылаОшибкаСоединения Тогда
			Шаблон = ?(СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую,
				НСтр("ru = 'Похоже, сервер СЭДО ФСС перегружен. Попробуйте повторить получение сообщений через некоторое время.'"),
				НСтр("ru = 'Похоже, сервер оператора СЭДО ФСС перегружен. Попробуйте повторить получение сообщений через некоторое время.'"));
			Шаблон = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(Шаблон, Истина);
			Шаблон = Шаблон + Символы.ПС + НСтр("ru='Подробнее:'") + Символы.ПС
			+ "Не удалось получить список входящих сообщений СЭДО." + Символы.ПС
			+ "%1" + Символы.ПС
			+ "Список входящих также будет загружаться в фоновом режиме, после получения будет предложено их расшифровать.";
		Иначе
			Шаблон = ?(СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую,
				НСтр("ru = 'Не удалось получить список входящих сообщений СЭДО СФР.'"),
				НСтр("ru = 'Не удалось получить список входящих сообщений оператора СЭДО СФР.'"))
			+ НСтр("ru='Подробнее:'") + Символы.ПС
			+ "%1" + Символы.ПС
			+ НСтр("ru='Список входящих также будет загружаться в фоновом режиме, после получения будет предложено их расшифровать.'");
		КонецЕсли;
		РезультатПолученияВходящих.ОписаниеОшибки = СтрШаблон(Шаблон, РезультатОтправкиЗапроса.ОписаниеОшибки);
		Если РезультатПолученияВходящих.Свойство("АдресЗашифрованногоЗапросаSOAP") Тогда
			РезультатПолученияВходящих.АдресЗашифрованногоЗапросаSOAP = РезультатОтправкиЗапроса.АдресЗашифрованногоЗапросаSOAP;
		КонецЕсли;
	Иначе
		РезультатПолученияВходящих.Выполнено = Истина;
	КонецЕсли;
		
	Возврат Истина;
	
КонецФункции

#Область МетаданныеВходящихСообщенийСЭДОФССЗаПериод

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗаПериод(
	ОповещениеОбратногоВызова,
	Организация,
	НеПроверятьПодписьФСС = Ложь,
	ТипВзаимодействия = Неопределено,
	СброситьСвойстваОбменаПоСЭДОЧерезОператора = Истина) Экспорт
	
	Результат = РезультатМетаданныеВходящихСообщенийСЭДОФСС();
	
	Если ОбменПрерванПользователем Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	
	Если СброситьСвойстваОбменаПоСЭДОЧерезОператора Тогда
		СброситьБезПолучения = Истина;
		СвойстваОбменаПоСЭДОЧерезОператора(Организация, СброситьБезПолучения);
	КонецЕсли;
	СвойстваОбменаПоСЭДОЧерезОператора = СвойстваОбменаПоСЭДОЧерезОператора(Организация);
	
	НуженАнализВходящих = Ложь;
	Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
		КоличествоЗапросовИсторическихСообщений = ПолучитьКоличествоЗапросовИсторическихСообщений(
			Организация, Ложь);
		НуженАнализВходящих = КоличествоЗапросовИсторическихСообщений = 0;
	КонецЕсли;
	
	ТекущаяДата = Неопределено;
	СведенияОДатахПолученияСообщений = ПолучитьДатыПолученияСообщений(
		Организация,
		ТекущаяДата,
		СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую,
		НуженАнализВходящих);
	ДатыПолученияСообщений = СведенияОДатахПолученияСообщений.МассивДат;
	
	Если ДатыПолученияСообщений.Количество() = 0 Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ОтображатьСостояние = Истина;
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ОтображатьСостояние") Тогда
		ОтображатьСостояние = ОповещениеОбратногоВызова.ДополнительныеПараметры.ОтображатьСостояние;
	КонецЕсли;
	
	ЗадаватьВопросОТестовомСервере = Истина;
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ЗадаватьВопросОТестовомСервере") Тогда
		ЗадаватьВопросОТестовомСервере = 
			ОповещениеОбратногоВызова.ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	КонецЕсли;
	
	ДополнительныеПараметры = ПараметрыМетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(Организация, ДатыПолученияСообщений);
	ДополнительныеПараметры.ОповещениеОбратногоВызова 			= ОповещениеОбратногоВызова;
	ДополнительныеПараметры.Результат 							= Результат;
	ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере 		= ЗадаватьВопросОТестовомСервере;
	ДополнительныеПараметры.ТипВзаимодействия 					= ТипВзаимодействия;
	ДополнительныеПараметры.ТекущаяДата 						= Дата(1, 1, 1);
	ДополнительныеПараметры.ОтображатьСостояние 				= ОтображатьСостояние;
	ДополнительныеПараметры.ОшибкаПриЗапросеМетаданных 			= Ложь;
	ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора 	= СвойстваОбменаПоСЭДОЧерезОператора;
	
	ПолучениеАктуальныхСообщений = ?(
		ДатыПолученияСообщений.Количество() > 0 И НЕ ЗначениеЗаполнено(ДатыПолученияСообщений[0].Дата),
		Истина,
		Ложь);
	ДополнительныеПараметры.ПредварительноПолучитьАктуальные = ПолучениеАктуальныхСообщений;
	
	СообщенияДоСЭДОЧерезОператораПолучены = (СведенияОДатахПолученияСообщений.ОбменЧерезОператора = Истина);
	ДополнительныеПараметры.СообщенияДоСЭДОЧерезОператораПолучены = СообщенияДоСЭДОЧерезОператораПолучены;
	
	Если СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую
		ИЛИ СообщенияДоСЭДОЧерезОператораПолучены Тогда
			
		МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(ДополнительныеПараметры);
		
	Иначе
		Если КоличествоЗапросовИсторическихСообщений = 0 Тогда
			Если НЕ СведенияОДатахПолученияСообщений.Найдено Тогда
				// оператор не может прислать старые сообщения до подписки страхователя при первой отправке,
				// кроме как в результате явного запроса исторических сообщений
				КоличествоДнейЗагрузкиИсторииЧерезОператора = 14;
				МассивДат = Новый Массив;
				ДатаПолученияСообщения = ТекущаяДата - КоличествоДнейЗагрузкиИсторииЧерезОператора * 24 * 60 * 60;
				Для НомерДня = 0 По КоличествоДнейЗагрузкиИсторииЧерезОператора Цикл
					ВремяНачала = ?(НомерДня = 0, ДатаПолученияСообщения, НачалоДня(ДатаПолученияСообщения));
					ВремяКонца = ?(НомерДня = КоличествоДнейЗагрузкиИсторииЧерезОператора,
						ДатаПолученияСообщения, КонецДня(ДатаПолученияСообщения));
					СтрокаДат = ЭлектронныйДокументооборотСФССКлиентСервер.ПолучитьСтрокуДатВСтруктуре(
						ДатаПолученияСообщения, ВремяНачала, ВремяКонца);
					МассивДат.Добавить(СтрокаДат);
					ДатаПолученияСообщения = ДатаПолученияСообщения + 24 * 60 * 60;
				КонецЦикла;
				ДополнительныеПараметры.ДатыПолученияСообщений = МассивДат;
			КонецЕсли;
			МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(ДополнительныеПараметры);
			
		Иначе
			
			// Сообщение на подписку страхователя к оператору было отправлено.
 			РезультатПолучения = МетаданныеВходящихСообщенийСЭДОЗаПериодБезКриптографии(
				Организация, ДатыПолученияСообщений, Истина, СвойстваОбменаПоСЭДОЧерезОператора);
			ЗаполнитьЗначенияСвойств(Результат, РезультатПолучения);
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
			Возврат;
			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗаПериодЦикл(ДополнительныеПараметры)
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация 						= ДополнительныеПараметры.Организация;
	Результат 							= ДополнительныеПараметры.Результат;
	ДатыПолученияСообщений				= ДополнительныеПараметры.ДатыПолученияСообщений;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	ЗадаватьВопросОТестовомСервере		= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	ТекущаяДата							= ДополнительныеПараметры.ТекущаяДата;
	ОшибкаПриЗапросеМетаданных			= ДополнительныеПараметры.ОшибкаПриЗапросеМетаданных;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	НеСохранятьДатуЗагрузкиИдентификаторов = ДополнительныеПараметры.НеСохранятьДатуЗагрузкиИдентификаторов;
	
	ОтображатьСостояние = ?(ДополнительныеПараметры.Свойство("ОтображатьСостояние"),
		ДополнительныеПараметры.ОтображатьСостояние,
		Истина);
		
	Если ОбменПрерванПользователем Или ОшибкаПриЗапросеМетаданных ИЛИ ДатыПолученияСообщений.Количество() = 0 Тогда
		Если ЗначениеЗаполнено(ТекущаяДата)
			И НЕ НеСохранятьДатуЗагрузкиИдентификаторов Тогда
			СохранитьДатуСеансаОбменаСЭДОФСС(
				Организация,
				ТекущаяДата,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
		КонецЕсли;
		
		Если СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			Результат.Выполнено = Истина;
		Иначе
			Результат.Выполнено = НЕ ОшибкаПриЗапросеМетаданных;
		КонецЕсли;
		
		Если ТипЗнч(ОповещениеОбратногоВызова.ДополнительныеПараметры) = Тип("Структура") Тогда
			ОповещениеОбратногоВызова.ДополнительныеПараметры.Вставить("ТекущаяДата", ТекущаяДата);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ДатаСообщения = Неопределено;
	Если ДатыПолученияСообщений.Количество() > 0 Тогда
		ДатаСообщения = ДатыПолученияСообщений[0];
		ДатыПолученияСообщений.Удалить(0);
		Если НЕ ЗначениеЗаполнено(ДатаСообщения.Дата) Тогда
			ДатаСообщения = Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	ДатаПолученияСообщений = ЭлектронныйДокументооборотСФССКлиентСервер.ПолучитьДатуПолученияСообщений(
		ДатаСообщения);
	
	Настройки = ДокументооборотСФССКлиентСервер.НастройкиТекстовЗапросовСЭДОФСС();
	Настройки.ТипВзаимодействия = ТипВзаимодействия;
	ЭтоТипВзаимодействияОрганизацияСЭДО =
		(ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияОрганизацияСЭДО());
	ПолучитьОГРН 			= ЭтоТипВзаимодействияОрганизацияСЭДО;
	ПолучитьИННКППиСНИЛС 	= ЭтоТипВзаимодействияОрганизацияСЭДО;
	СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
		Организация,,
		ПолучитьОГРН,,
		ПолучитьИННКППиСНИЛС);
	РегистрационныйНомерФСС = СвойстваОрганизации.РегистрационныйНомерФСС;
	Если ЭтоТипВзаимодействияОрганизацияСЭДО Тогда
		ЗаполнитьЗначенияСвойств(Настройки, СвойстваОрганизации);
	КонецЕсли;
	
	Настройки.ОбменПоСЭДОНапрямую 		= СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
	Настройки.ИдентификаторАбонентаСЭДО = СвойстваОбменаПоСЭДОЧерезОператора.ИдентификаторАбонентаСЭДО;
	
	ТекстЗапроса = ТекстЗапросаПолученияКоличестваСообщенийСЭДОФСС(
		РегистрационныйНомерФСС,
		ДатаСообщения,
		Настройки);
	
	Операция = ОперацияПолученияСпискаВходящихСЭДОФСС();
	ЗапросДляПолученияДанных = ПараметрыЗапросаПолучитьДанныеСЭДОФСС();
	ЗапросДляПолученияДанных.Организация = Организация;
	ЗапросДляПолученияДанных.Операция = Операция;
	ЗапросДляПолученияДанных.ТекстЗапроса = ТекстЗапроса;
	
	ДополнительныеПараметры.Операция = Операция;
	ДополнительныеПараметры.ТекстЗапроса = ТекстЗапроса;
	ДополнительныеПараметры.ДатаСообщений = ДатаПолученияСообщений;
	
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ТипСообщенияОтвета") Тогда
		ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",
			ОповещениеОбратногоВызова.ДополнительныеПараметры.ТипСообщенияОтвета);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("МетаданныеВходящихСообщенийСЭДОФССЗаПериодПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПолучитьДанныеСЭДОФСС(
		Оповещение,
		ЗапросДляПолученияДанных,
		ЗадаватьВопросОТестовомСервере,
		ТипВзаимодействия,
		ОтображатьСостояние,
		СвойстваОбменаПоСЭДОЧерезОператора);
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗаПериодПослеЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 	= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 			= ДополнительныеПараметры.Результат;
	Организация 				= ДополнительныеПараметры.Организация;
	Операция 					= ДополнительныеПараметры.Операция;
	ТекстЗапроса 				= ДополнительныеПараметры.ТекстЗапроса;
	ДатыПолученияСообщений		= ДополнительныеПараметры.ДатыПолученияСообщений;
	ДатаСообщений				= ДополнительныеПараметры.ДатаСообщений;
	ТипВзаимодействия 			= ДополнительныеПараметры.ТипВзаимодействия;
	СвойстваОбменаПоСЭДОЧерезОператора = ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	
	Если Результат.Выполнено Тогда
		
		// Сообщения были успешно получены.
		ТекстОтвета = ПолучитьИзВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP);
		
		// Разбор ответа сервера.
		РезультатРазбора = РазобратьОтветВходящиеСообщенияСЭДОФСС(ТекстОтвета, ДатаСообщений, СвойстваОбменаПоСЭДОЧерезОператора);
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			
			Шаблон = НСтр("ru='При получении данных сообщений ФСС произошла ошибка:
			|%1'");
			ОписаниеОшибки = СтрШаблон(Шаблон, РезультатРазбора.ОписаниеОшибки);
			РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки,,
				Истина);
				
			// В случае ошибки разбора сохраняем запрос в журнал запросов.
			ПараметрыЗапроса = ПараметрыСохраненияЗапросаСЭДОФСС(Организация, Операция);
			ПараметрыЗапроса.ОписаниеОшибки = ОписаниеОшибки;
			ПараметрыЗапроса.АдресЗапросаSOAP = Результат.АдресПодписаногоЗапросаSOAP;
			ПараметрыЗапроса.АдресОтветаSOAP = Результат.АдресРасшифрованногоОтветаSOAP;
			ПараметрыЗапроса.АдресРесурса = Результат.АдресРесурса;
			ПараметрыЗапроса.АдресСервера = Результат.АдресСервера;
			ПараметрыЗапроса.КодСостояния = Результат.КодСостояния;
			ПараметрыЗапроса.ТипВзаимодействияСЭДО = ТипВзаимодействия;
			ПараметрыЗапроса.ОбменЧерезОператора 	= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
			СохранитьДанныеЗапросаСЭДОФСС(ПараметрыЗапроса);
			
		Иначе
			
			ДанныеСообщений = РезультатРазбора.ДанныеСообщений;
			Для каждого ЭлементДанных из ДанныеСообщений Цикл
				РезультатОперации.ДанныеСообщений.Добавить(ЭлементДанных);
			КонецЦикла;
			// Сохранение данных о сообщениях в БД.
			РезультатСохранения = СохранитьДанныеВходящихСообщенийСЭДОФСС(
				Организация,
				ДанныеСообщений,
				ТипВзаимодействия,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
			Если РезультатСохранения.БылиОшибкиОбработки Тогда
				РезультатОперации.ОписаниеОшибки = СтрСоединить(РезультатСохранения.ОшибкиОбработки, Символы.ПС);
				ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
				Возврат;
			КонецЕсли;
			
			ДанныеШироковещательныхСообщений = РезультатРазбора.ДанныеШироковещательныхСообщений;
			СохранитьДанныеРассылокСЭДОФСС(
				Организация,
				ДанныеШироковещательныхСообщений,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
			
			РезультатОперации.Выполнено = Истина;
			
		КонецЕсли;
		
		ДополнительныеПараметры.ТекущаяДата = ДатаСообщений;
		
		Если ЭтоАдресВременногоХранилища(Результат.АдресПодписаногоЗапросаSOAP) Тогда
			УдалитьИзВременногоХранилища(Результат.АдресПодписаногоЗапросаSOAP);
		КонецЕсли;
		
		Если ЭтоАдресВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP) Тогда
			УдалитьИзВременногоХранилища(Результат.АдресРасшифрованногоОтветаSOAP);
		КонецЕсли;
		
	Иначе
		
		// При получении возникла ошибка.
		Если ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			// Для прямого обмена выполняем дополнительную обработку результата получения.
			ОбработатьОшибкуПриПолученииМетаданныхВходящихСЭДО(Результат, РезультатОперации, ДополнительныеПараметры);
		Иначе
			РезультатОперации.ОписаниеОшибки = Результат.ОписаниеОшибки;
		КонецЕсли;
		
		Если НЕ РезультатОперации.Выполнено Тогда
			ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(РезультатОперации.ОписаниеОшибки);
		КонецЕсли;
		
		ДополнительныеПараметры.ОшибкаПриЗапросеМетаданных = Истина;
		
	КонецЕсли;
	
	МетаданныеВходящихСообщенийСЭДОФССЗаПериодЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

#КонецОбласти

#Область МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений

&НаКлиенте
Функция ПараметрыМетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(Организация, ДатыПолученияСообщений)
	
	Результат = Новый Структура;
	
	ПустаяДата = Дата(1, 1, 1);
	РезультатПолучения = РезультатМетаданныеВходящихСообщенийСЭДОФСС();
	
	Результат.Вставить("ОповещениеОбратногоВызова",				Неопределено);
	Результат.Вставить("Результат", 							РезультатПолучения);
	Результат.Вставить("Организация", 							Организация);
	Результат.Вставить("ДатыПолученияСообщений", 				ДатыПолученияСообщений);
	Результат.Вставить("ДатаСообщений",							ПустаяДата);
	Результат.Вставить("ЗадаватьВопросОТестовомСервере",		Ложь);
	Результат.Вставить("ТипВзаимодействия", 					2);
	Результат.Вставить("ТекущаяДата", 							ПустаяДата);
	Результат.Вставить("ОтображатьСостояние", 					Ложь);
	Результат.Вставить("ОшибкаПриЗапросеМетаданных",			Ложь);
	Результат.Вставить("СвойстваОбменаПоСЭДОЧерезОператора",	Неопределено);
	Результат.Вставить("ЭтоПользовательскийЗапрос",				Ложь);
	Результат.Вставить("ТипСообщенияОтвета", 					"");
	Результат.Вставить("ПредварительноПолучитьАктуальные", 		Ложь);
	Результат.Вставить("СообщенияДоСЭДОЧерезОператораПолучены", Ложь);
	Результат.Вставить("Операция", 								"");
	Результат.Вставить("ТекстЗапроса", 							"");
	Результат.Вставить("НеСохранятьДатуЗагрузкиИдентификаторов", Ложь);
	
	Возврат Результат;

КонецФункции

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(
	ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация 						= ДополнительныеПараметры.Организация;
	Результат 							= ДополнительныеПараметры.Результат;
	ДатыПолученияСообщений				= ДополнительныеПараметры.ДатыПолученияСообщений;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	ЗадаватьВопросОТестовомСервере 		= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	ТекущаяДата 						= ДополнительныеПараметры.ТекущаяДата;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	ЭтоПользовательскийЗапрос = Ложь;
	Если ДополнительныеПараметры.Свойство("ЭтоПользовательскийЗапрос") Тогда
		ЭтоПользовательскийЗапрос		= ДополнительныеПараметры.ЭтоПользовательскийЗапрос;
	КонецЕсли;
	
	ОтображатьСостояние = ?(ДополнительныеПараметры.Свойство("ОтображатьСостояние"),
		ДополнительныеПараметры.ОтображатьСостояние,
		Истина);
		
	Если ДополнительныеПараметры.ПредварительноПолучитьАктуальные Тогда
		
		// Создаем копию полей.
		ДополнительныеПараметры.Вставить("ДатыПолученияСообщенийКопия",
			ДатыПолученияСообщений);
		ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызоваКопия",
			ОповещениеОбратногоВызова);
			
		// Создаем новый массив дат с пустой датой.
		ДатыПолученияСообщенийНовый = Новый Массив;
		ДатаИВремя = ЭлектронныйДокументооборотСФССКлиентСервер.ПолучитьСтрокуДатВСтруктуре(
			Неопределено);
		ДатыПолученияСообщенийНовый.Добавить(ДатаИВремя);
		ДополнительныеПараметры.Вставить("ДатыПолученияСообщений",
			ДатыПолученияСообщенийНовый);
			
		// Новое оповещение.
		ОповещениеОбратногоВызоваНовое = Новый ОписаниеОповещения(
			"МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщенийПослеПолученияАктуальных",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ДополнительныеПараметры.ОповещениеОбратногоВызова = ОповещениеОбратногоВызоваНовое;
		
		// Получаем актуальные сообщения.
		МетаданныеВходящихСообщенийСЭДОФССЗаПериодЦикл(ДополнительныеПараметры);
		Возврат;
		
	КонецЕсли;
	
	ДатаСообщения = Неопределено;
	Если ДатыПолученияСообщений.Количество() > 0 Тогда
		ДатаСообщения = ДатыПолученияСообщений[ДатыПолученияСообщений.Количество() - 1];
		Если НЕ ЗначениеЗаполнено(ДатаСообщения.Дата) Тогда
			ДатаСообщения = Неопределено;
		КонецЕсли;
	Иначе
		Если ТипЗнч(ОповещениеОбратногоВызова.ДополнительныеПараметры) = Тип("Структура") Тогда
			ОповещениеОбратногоВызова.ДополнительныеПараметры.Вставить("ТекущаяДата", ТекущаяДата);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	// Если обмен через оператора и дней немного, то запрашиваем идентификаторы по дням.
	КоличествоДатКоторыеПолучаемЗапросамиПоДням = 2;
	Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую
		И ДатыПолученияСообщений.Количество() <= КоличествоДатКоторыеПолучаемЗапросамиПоДням Тогда
		РезультатПолучения = МетаданныеВходящихСообщенийСЭДОЗаПериодБезКриптографии(
			Организация, ДатыПолученияСообщений, Ложь, СвойстваОбменаПоСЭДОЧерезОператора);
		ЗаполнитьЗначенияСвойств(Результат, РезультатПолучения);
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ПараметрыОтправкаСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ПараметрыОтправкаСообщения.ЭтоПользовательскийЗапрос = ЭтоПользовательскийЗапрос;
	ПараметрыОтправкаСообщения.Вставить("РегистрационныйНомерФСС",	"");
	ПараметрыОтправкаСообщения.Вставить("Организация",				Организация);
	ПараметрыОтправкаСообщения.Вставить("ТипВзаимодействия", 		Неопределено);
	ПараметрыОтправкаСообщения.Вставить("СвойстваДляОбмена", 		Неопределено);
	ПараметрыОтправкаСообщения.Вставить("НастройкиЗаписиОтправки", 	Неопределено);
	ПараметрыОтправкаСообщения.Вставить("ДатыСообщений", 			ДатыПолученияСообщений);
	
	ЗаполнитьПараметрыОтправкиПоОрганизации(
		ПараметрыОтправкаСообщения,
		Организация);
	Если ПараметрыОтправкаСообщения.СодержимоеСообщения = "" Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ПараметрыОтправкаСообщения.ОписаниеОшибки =
		НСтр("ru='Не удалось получить список архивных сообщений.'")
		+ Символы.ПС + "%1";
		
	Операция = "GetMessage";
	ДополнительныеПараметры.Вставить("Операция", 							Операция);
	ДополнительныеПараметры.Вставить("ТекстЗапроса", 						"");
	ДополнительныеПараметры.ДатаСообщений =
		?(ТипЗнч(ДатаСообщения) = Тип("Структура"), ДатаСообщения.Дата, Неопределено);
	
	Если ОповещениеОбратногоВызова.ДополнительныеПараметры.Свойство("ТипСообщенияОтвета") Тогда
		ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",
			ОповещениеОбратногоВызова.ДополнительныеПараметры.ТипСообщенияОтвета);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщенийПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПараметрыОтправкаСообщения.ОповещениеОбратногоВызова = Оповещение;
	ОтправитьСообщениеСЭДОФСС(
		ПараметрыОтправкаСообщения,
		ЗадаватьВопросОТестовомСервере,
		ОтображатьСостояние,
		Ложь,
		СвойстваОбменаПоСЭДОЧерезОператора);
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщенийПослеЗапроса(
	Результат,
	ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	Организация 						= ДополнительныеПараметры.Организация;
	Операция 							= ДополнительныеПараметры.Операция;
	ТекстЗапроса 						= ДополнительныеПараметры.ТекстЗапроса;
	ДатыПолученияСообщений				= ДополнительныеПараметры.ДатыПолученияСообщений;
	ТекущаяДата 						= ДополнительныеПараметры.ТекущаяДата;
	ДатаСообщений						= ДополнительныеПараметры.ДатаСообщений;
	ТипВзаимодействия 					= ДополнительныеПараметры.ТипВзаимодействия;
	ЗадаватьВопросОТестовомСервере		= ДополнительныеПараметры.ЗадаватьВопросОТестовомСервере;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	ЭтоПользовательскийЗапрос			= ДополнительныеПараметры.ЭтоПользовательскийЗапрос;
	
	ДатыПолученияСообщений.Очистить();
	
	Если Результат.Выполнено Тогда
		// Запоминаем идентификатор 332 сообщения, которое в дальнейшем будет загружаться.
		ИдентификаторСообщения = Результат.ИдентификаторЗапроса;
		РезультатОперации.Вставить("ИдентификаторСообщения", ИдентификаторСообщения);
		Если НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую Тогда
			// Для операторной схемы ответное сообщение приходит с задержкой.
			РезультатОперации.Выполнено = Истина;
			Если ТипЗнч(ОповещениеОбратногоВызова.ДополнительныеПараметры) = Тип("Структура") Тогда
				ОповещениеОбратногоВызова.ДополнительныеПараметры.Вставить("ТекущаяДата", ТекущаяДата);
			КонецЕсли;
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
			Возврат;
		КонецЕсли;
		
		// Загрузка ответного сообщения 332.
		Оповещение = Новый ОписаниеОповещения(
			"МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщенийПослеПолучения",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПолучитьСообщениеСЭДОФСС(Оповещение,
			Организация,
			ИдентификаторСообщения,
			Ложь);
	Иначе
		// При отправке сообщения 330 возникла ошибка.
		Если НЕ МетаданныеВходящихСообщенийСЭДОАктуальны(Организация, СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую)
			ИЛИ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую
			И НЕ ЭтоПользовательскийЗапрос Тогда
			Шаблон = НСтр("ru='Не удалось отправить запрос исторических сообщений СЭДО СФР.
				|%1
				|Возможно, сервер перегружен. Попробуйте повторить получение сообщений через некоторое время.'");
			РезультатОперации.ОписаниеОшибки = СтрШаблон(Шаблон, Результат.ОписаниеОшибки);
			ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
		Иначе
			РезультатОперации.ОписаниеОшибки = Результат.ОписаниеОшибки;
		КонецЕсли;
		// Отключаем сохранение даты получения списка входящих сообщений.
		ДополнительныеПараметры.ТекущаяДата = Дата(1, 1, 1);
		МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщенийПослеПолучения(
	Результат,
	ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова 			= ДополнительныеПараметры.ОповещениеОбратногоВызова;
	РезультатОперации 					= ДополнительныеПараметры.Результат;
	Организация 						= ДополнительныеПараметры.Организация;
	Операция 							= ДополнительныеПараметры.Операция;
	ТекстЗапроса 						= ДополнительныеПараметры.ТекстЗапроса;
	СвойстваОбменаПоСЭДОЧерезОператора 	= ДополнительныеПараметры.СвойстваОбменаПоСЭДОЧерезОператора;
	ЭтоПользовательскийЗапрос			= ДополнительныеПараметры.ЭтоПользовательскийЗапрос;
	
	Если Результат.Выполнено Тогда
		
		// Разбор содержимого сообщения 332.
		СодержимоеСообщенияОтвета = Результат.ДанныеСообщений[0].ТекстСообщения;
		РезультатРазбора = РазобратьОтветВходящиеСообщенияСЭДОФСС(СодержимоеСообщенияОтвета, Неопределено, СвойстваОбменаПоСЭДОЧерезОператора);
		
		Если НЕ РезультатРазбора.Выполнено Тогда
			
			Шаблон = НСтр("ru='При получении данных сообщений из СЭДО произошла ошибка:
				|%1'");
			ОписаниеОшибки = СтрШаблон(Шаблон, РезультатРазбора.ОписаниеОшибки);
			РезультатОперации.ОписаниеОшибки = ОписаниеОшибки;
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных СЭДО ФСС'"),
				"Ошибка",
				ОписаниеОшибки,,
				Истина);
				
		Иначе
			
			ДанныеСообщений = РезультатРазбора.ДанныеСообщений;
			РезультатОперации.ДанныеСообщений = ДанныеСообщений;
			// Сохранение данных о сообщениях в БД.
			РезультатСохранения = СохранитьДанныеВходящихСообщенийСЭДОФСС(
				Организация,
				ДанныеСообщений, ,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
			Если РезультатСохранения.БылиОшибкиОбработки Тогда
				РезультатОперации.ОписаниеОшибки = СтрСоединить(РезультатСохранения.ОшибкиОбработки, Символы.ПС);
				ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, РезультатОперации);
				Возврат;
			КонецЕсли;
			Если НЕ РезультатОперации.Свойство("ИдентификаторыСообщенийИз332") Тогда
				РезультатОперации.Вставить("ИдентификаторыСообщенийИз332", Новый Массив);
			КонецЕсли;
			Для каждого Сообщение Из ДанныеСообщений Цикл
				РезультатОперации.ИдентификаторыСообщенийИз332.Добавить(Сообщение.Идентификатор);
			КонецЦикла;
			РезультатОперации.Выполнено = Истина;
			
		КонецЕсли;
		
	Иначе
		
		// При получении возникла ошибка.
		Если НЕ МетаданныеВходящихСообщенийСЭДОАктуальны(Организация, СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую)
			И НЕ ЭтоПользовательскийЗапрос Тогда
			Шаблон = ?(СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую,
				НСтр("ru='Не удалось получить список входящих сообщений СЭДО СФР.
				|%1
				|Возможно, сервер перегружен. Попробуйте повторить получение сообщений через некоторое время.'"),
				НСтр("ru='Не удалось получить список входящих сообщений СЭДО СФР.
				|%1
				|Возможно, сервер оператора перегружен. Попробуйте повторить получение сообщений через некоторое время.'"));
			РезультатОперации.ОписаниеОшибки = СтрШаблон(Шаблон, Результат.ОписаниеОшибки);
		Иначе
			РезультатОперации.ОписаниеОшибки = Результат.ОписаниеОшибки;
		КонецЕсли;
		// Отключаем сохранение даты получения списка входящих сообщений.
		ДополнительныеПараметры.ТекущаяДата = Дата(1, 1, 1);
		
		Если Результат.СообщенияНеНайдены Тогда
			// Ситуация возникает если сообщение 332 не успело сформироваться на стороне сервера.
			РезультатОперации.Сообщение332НеНайдено = Истина;
			РезультатОперации.Выполнено = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(
		ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщенийПослеПолученияАктуальных(
		Результат, ДополнительныеПараметры) Экспорт
		
	ДополнительныеПараметры.ДатыПолученияСообщений = ДополнительныеПараметры.ДатыПолученияСообщенийКопия;
	ДополнительныеПараметры.ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызоваКопия;
	ДополнительныеПараметры.ПредварительноПолучитьАктуальные = Ложь;
	МетаданныеВходящихСообщенийСЭДОФССЗапросИсторическихСообщений(ДополнительныеПараметры);
			
КонецПроцедуры

#КонецОбласти

#Область ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФСС

&НаКлиенте
Процедура ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФСС(
		ОповещениеОбратногоВызова,
		СотрудникиОрганизаций,
		Операция,
		ОткрыватьФормуДлительнойОперации = Истина) Экспорт
		
	Организации = Новый Массив;
	Для каждого КлючЗначение Из СотрудникиОрганизаций Цикл
		Организации.Добавить(КлючЗначение.Ключ);
	КонецЦикла;
	
	Результат = Новый Структура;
	Результат.Вставить("БылиОшибки", Ложь);
	Результат.Вставить("Ошибки", Новый Массив);
	Результат.Вставить("РезультатыОперации", Новый Соответствие);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("СотрудникиОрганизаций", СотрудникиОрганизаций);
	ДополнительныеПараметры.Вставить("Организации", Организации);
	ДополнительныеПараметры.Вставить("ИндексТекущейОрганизации", 0);
	ДополнительныеПараметры.Вставить("Результат", Результат);
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Операция", Операция);
	ДополнительныеПараметры.Вставить("ФормаДлительнойОперации", Неопределено);
	
	Если Операция = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.
			ВидОперацииСЭДОФССПодписать() Тогда
		ОписаниеОперации = НСтр("ru='Подписка сотрудников на уведомления по ЭЛН.'");
	ИначеЕсли Операция = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.
			ВидОперацииСЭДОФССОтписать() Тогда
		ОписаниеОперации = НСтр("ru='Отключение подписки сотрудников от уведомлений по ЭЛН.'");
	Иначе
		Шаблон = НСтр("ru='Изменение подписки сотрудников на уведомление по ЭЛН. Неизвестная операция %1.'");
		ВызватьИсключение СтрШаблон(Шаблон, Операция);
	КонецЕсли;
	
	// Открываем форму длительной операции.
	ПараметрыОжидания = Новый Структура();
	ПараметрыОжидания.Вставить("ТекстСообщения", ОписаниеОперации);
	Если ОткрыватьФормуДлительнойОперации Тогда
		ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
			"ОбщаяФорма.ДлительнаяОперация", ПараметрыОжидания);
	КонецЕсли;
	
	ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФССЦикл(
		ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	ИндексТекущейОрганизации = ДополнительныеПараметры.ИндексТекущейОрганизации;
	Организации = ДополнительныеПараметры.Организации;
	Результат = ДополнительныеПараметры.Результат;
	СотрудникиОрганизаций = ДополнительныеПараметры.СотрудникиОрганизаций;
	Операция = ДополнительныеПараметры.Операция;
	ФормаДлительнойОперации = ДополнительныеПараметры.ФормаДлительнойОперации;
	
	Если ИндексТекущейОрганизации >= Организации.Количество() Тогда
		ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФССПослеЗапроса",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	Организация = Организации[ИндексТекущейОрганизации];
	СписокСНИЛС = СотрудникиОрганизаций.Получить(Организация);
	
	ТипСобытия = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.
		ТипСобытияСЭДОФССИзменениеСостоянияЭЛН();
	
	Если Операция = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.
			ВидОперацииСЭДОФССПодписать() Тогда
		ПодписатьСписокСНИЛССЭДОФСС(Оповещение, Организация, СписокСНИЛС, ТипСобытия);
	ИначеЕсли Операция = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.
			ВидОперацииСЭДОФССОтписать() Тогда
		ОтписатьСписокСНИЛССЭДОФСС(Оповещение, Организация, СписокСНИЛС, ТипСобытия);
	Иначе
		Шаблон = НСтр("ru='Изменение подписки сотрудников на уведомление по ЭЛН. Неизвестная операция %1.'");
		ВызватьИсключение СтрШаблон(Шаблон, Операция);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФССПослеЗапроса(
		РезультатОперации,
		ДополнительныеПараметры) Экспорт
		
	ИндексТекущейОрганизации = ДополнительныеПараметры.ИндексТекущейОрганизации;
	Организации = ДополнительныеПараметры.Организации;
	Результат = ДополнительныеПараметры.Результат;
		
	Если НЕ РезультатОперации.Выполнено Тогда
		Результат.БылиОшибки = Истина;
		Результат.Ошибки.Добавить(РезультатОперации.ОписаниеОшибки);
	КонецЕсли;
	
	Организация = Организации[ИндексТекущейОрганизации];
		
	Результат.РезультатыОперации.Вставить(Организация, РезультатОперации);
	ДополнительныеПараметры.ИндексТекущейОрганизации = ИндексТекущейОрганизации + 1;
	
	ПодписатьОтписатьСотрудниковОрганизацийНаУведомленияЭЛНСЭДОФССЦикл(ДополнительныеПараметры);
	
КонецПроцедуры

#КонецОбласти

#Область ПодписатьСписокСНИЛССЭДОФСС

&НаКлиенте
Процедура ПодписатьСписокСНИЛССЭДОФСС(ОповещениеОбратногоВызова,
		Организация,
		СписокСНИЛС,
		ТипСобытия) Экспорт
		
	ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(
		"ПодписатьСписокСНИЛССЭДОФСС",
		"СписокСНИЛС",
		СписокСНИЛС,
		Тип("Массив"));
		
	РегистрационныйНомерФСС = РегистрационныйНомерФССОрганизации(Организация);
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ПараметрыСообщения.ТипСообщения = 3;
	ПараметрыСообщения.СодержимоеСообщения =
		СодержимоеСообщенияПодпискиРаботниковНаУведомлениеСтрахователяОбИзмененииСтатусаЭЛНВМО(
			РегистрационныйНомерФСС,
			СписокСНИЛС,
			ТипСобытия);
	ПараметрыСообщения.Организация = Организация;
	ПараметрыСообщения.ОповещениеОбратногоВызова = ОповещениеОбратногоВызова;
	ПараметрыСообщения.ОписаниеОшибки =
		НСтр("ru='Не удалось подписать организацию на уведомления ФСС об изменении состояния ЭЛН для "
		+ ПросклонятьКоличествоЭлементовВСписке(СписокСНИЛС, "сотрудник") + ".
		|%1'");
		
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ПараметрыСообщения", ПараметрыСообщения);
	
	СтраховательПодписан = СтраховательПодписанНаУведомленияПоЭЛН(Организация);
	Если НЕ СтраховательПодписан Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ПодписатьСписокСНИЛССЭДОФССПослеПодпискиСтрахователя",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПодписатьСтрахователяНаУведомленияПоЭЛН(Оповещение, Организация);
		Возврат;
	КонецЕсли;
	
	РезультатПодписки = РезультатОтправитьСообщениеСЭДОФСС();
	РезультатПодписки.Выполнено = Истина;
	ПодписатьСписокСНИЛССЭДОФССПослеПодпискиСтрахователя(РезультатПодписки, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСписокСНИЛССЭДОФССПослеПодпискиСтрахователя(РезультатПодпискиСтрахователя, ДополнительныеПараметры) Экспорт
	
	ПараметрыСообщения = ДополнительныеПараметры.ПараметрыСообщения;
	ОповещениеОбратногоВызова = ПараметрыСообщения.ОповещениеОбратногоВызова;
	Организация = ПараметрыСообщения.Организация;
	
	Если НЕ РезультатПодпискиСтрахователя.Выполнено Тогда
		Результат = РезультатОтправитьСообщениеСЭДОФСС();
		Результат.ОписаниеОшибки = РезультатПодпискиСтрахователя.ОписаниеОшибки;
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ОтписатьСписокСНИЛССЭДОФСС(ОповещениеОбратногоВызова,
		Организация,
		СписокСНИЛС,
		ТипСобытия) Экспорт
		
	РегистрационныйНомерФСС = РегистрационныйНомерФССОрганизации(Организация);
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ПараметрыСообщения.ТипСообщения = 4;
	ПараметрыСообщения.СодержимоеСообщения =
		СодержимоеСообщенияОтпискиРаботниковНаУведомлениеСтрахователяОбИзмененииСтатусаЭЛНВМО(
			РегистрационныйНомерФСС,
			СписокСНИЛС,
			ТипСобытия);
	ПараметрыСообщения.Организация = Организация;
	ПараметрыСообщения.ОповещениеОбратногоВызова = ОповещениеОбратногоВызова;

	ПараметрыСообщения.ОписаниеОшибки =
		НСтр("ru='Не удалось отключить подписку организации на уведомления ФСС об изменении состояний ЭЛН для "
			+ ПросклонятьКоличествоЭлементовВСписке(СписокСНИЛС, "сотрудник") + ".
			|%1'");
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения);
	
КонецПроцедуры

#КонецОбласти

#Область ПодписатьСтрахователяНаУведомленияПоЭЛН

&НаКлиенте
Процедура ПодписатьСтрахователяНаУведомленияПоЭЛН(ОповещениеОбратногоВызова, Организация) Экспорт
	
	РегистрационныйНомерФСС = РегистрационныйНомерФССОрганизации(Организация);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Организация", Организация);
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ПараметрыСообщения.ТипСообщения = 7;
	ПараметрыСообщения.СодержимоеСообщения =
		СодержимоеСообщенияПодпискиСтрахователяНаОповещенияОбИзмененииСтатусаЭЛНВМО(РегистрационныйНомерФСС);
	ПараметрыСообщения.Организация = Организация;
	ПараметрыСообщения.ОписаниеОшибки =
		НСтр("ru='Не удалось подписать организацию на оповещения об изменении состояний ЭЛН сотрудников.
		|%1'");
	
	Оповещение = Новый ОписаниеОповещения(
		"ПодписатьСтрахователяНаУведомленияПоЭЛНПослеПодписки",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПараметрыСообщения.ОповещениеОбратногоВызова = Оповещение;
	
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСтрахователяНаУведомленияПоЭЛНПослеПодписки(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация = ДополнительныеПараметры.Организация;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
	
КонецПроцедуры

#КонецОбласти

#Область ОтписатьСтрахователяОтУведомленийПоЭЛН

&НаКлиенте
Процедура ОтписатьСтрахователяОтУведомленийПоЭЛН(ОповещениеОбратногоВызова, Организация) Экспорт
	
	РегистрационныйНомерФСС = РегистрационныйНомерФССОрганизации(Организация);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Организация", Организация);
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ПараметрыСообщения.ТипСообщения = 8;
	ПараметрыСообщения.СодержимоеСообщения =
		СодержимоеСообщенияОтписыванияСтрахователяОтОповещенийОбИзмененииСтатусаЭЛНВМО(РегистрационныйНомерФСС);
	ПараметрыСообщения.Организация = Организация;
	ПараметрыСообщения.ОписаниеОшибки =
		НСтр("ru='Не удалось отписать организацию от оповещений об изменении состояний ЭЛН сотрудников.
		|%1'");
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтписатьСтрахователяОтУведомленийПоЭЛНПослеОтписки",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПараметрыСообщения.ОповещениеОбратногоВызова = Оповещение;
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтписатьСтрахователяОтУведомленийПоЭЛНПослеОтписки(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация = ДополнительныеПараметры.Организация;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
	
КонецПроцедуры

#КонецОбласти

#Область ПодтверждениеПрочтенияСообщенийСЭДОФСС

&НаКлиенте
Процедура ПодтверждениеПрочтенияСообщенийСЭДОФСС(ОповещениеОбратногоВызова,
		Организация,
		ИдентификаторыСообщений,
		ПослеОтправкиПолучитьВходящие,
		ОткрыватьФормуДлительнойОперации = Истина) Экспорт
		
	Для каждого Идентификатор Из ИдентификаторыСообщений Цикл
		Если НЕ ЗначениеЗаполнено(Идентификатор) Тогда
			Результат = РезультатОтправитьСообщениеСЭДОФСС();
			Результат.ОписаниеОшибки = НСтр("ru='Не указан идентификатор исходного сообщения.'");
			ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
			Возврат;
		КонецЕсли;
	КонецЦикла;
	
	РегистрационныйНомерФСС = РегистрационныйНомерФССОрганизации(Организация);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОбратногоВызова", ОповещениеОбратногоВызова);
	ДополнительныеПараметры.Вставить("Организация", Организация);
	ДополнительныеПараметры.Вставить("ИдентификаторыСообщений", ИдентификаторыСообщений);
	ДополнительныеПараметры.Вставить("ФормаДлительнойОперации", Неопределено);
	ДополнительныеПараметры.Вставить("ПослеОтправкиПолучитьВходящие", ПослеОтправкиПолучитьВходящие);
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ПараметрыСообщения.ТипСообщения = 6;
	ПараметрыСообщения.СодержимоеСообщения =
		СодержимоеСообщенияПодтверждениеПрочтенияСообщений(ИдентификаторыСообщений);
	ПараметрыСообщения.Организация = Организация;
	ПараметрыСообщения.ОписаниеОшибки =
		НСтр("ru='Не удалось отправить подтверждение о получении сообщения в СЭДО СФР.
		|%1'");
	
	Оповещение = Новый ОписаниеОповещения(
		"ПодтверждениеПрочтенияСообщенияПослеОтправки",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПараметрыСообщения.ОповещениеОбратногоВызова = Оповещение;
	
	// Открываем форму длительной операции.
	ПараметрыОжидания = Новый Структура();
	ПараметрыОжидания.Вставить("ТекстСообщения", НСтр("ru = 'Отправка подтверждений о получении сообщений СЭДО СФР.'"));
	Если ОткрыватьФормуДлительнойОперации Тогда
		ДополнительныеПараметры.ФормаДлительнойОперации = ОткрытьФорму(
			"ОбщаяФорма.ДлительнаяОперация", ПараметрыОжидания);
	КонецЕсли;
	
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения, , , Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодтверждениеПрочтенияСообщенияПослеОтправки(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	Организация = ДополнительныеПараметры.Организация;
	ИдентификаторыСообщений = ДополнительныеПараметры.ИдентификаторыСообщений;
	ФормаДлительнойОперации = ДополнительныеПараметры.ФормаДлительнойОперации;
	ПослеОтправкиПолучитьВходящие = ДополнительныеПараметры.ПослеОтправкиПолучитьВходящие;
	
	// Закроем окно заставки длительной операции, если она была открыта.
	ЗакрытьФормуДлительнойОперации(ФормаДлительнойОперации);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
		Возврат;
	КонецЕсли;
	
	СохранитьДатуОтправкиПодтвержденияОПрочтении(Организация, ИдентификаторыСообщений);
	
	Если ПослеОтправкиПолучитьВходящие Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ПодтверждениеПрочтенияСообщенияПослеПолученияВходящих",
			ЭтотОбъект,
			ДополнительныеПараметры);
 		ПолучитьВходящиеСообщенияСЭДОФСС(
			Оповещение,
			Организация);
		Возврат;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодтверждениеПрочтенияСообщенияПослеПолученияВходящих(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОбратногоВызова = ДополнительныеПараметры.ОповещениеОбратногоВызова;
	
	ВыполнитьОбработкуОповещения(ОповещениеОбратногоВызова, Результат);
	
КонецПроцедуры

#КонецОбласти

#Область СфомироватьОтветНаОбращениеСФРКСтрахователю

&НаСервереБезКонтекста
Функция ОтветыНаОбращениеСФРКСтрахователю(СсылкаНаОбращение)
	
	Возврат ЭлектронныйДокументооборотСФСС.ОтветыНаОбращениеСФРКСтрахователю(СсылкаНаОбращение);
	
КонецФункции

&НаСервереБезКонтекста
Функция СфомироватьОтветНаОбращениеСФРНаСервере(Ссылка)
	
	Возврат ЭлектронныйДокументооборотСФСС.СфомироватьОтветНаОбращениеСФРКСтрахователю(Ссылка);
	
КонецФункции

&НаКлиенте
Асинх Процедура СфомироватьОтветНаОбращениеСФРКСтрахователю(Ссылка) Экспорт
	
	Ответы = ОтветыНаОбращениеСФРКСтрахователю(Ссылка);
	
	// ищем существующий ответ
	Если Ответы.Количество() > 0 Тогда
		ТекстВопроса = НСтр("ru = 'На данное обращение уже создан ответ. Вы уверены, что хотите создать новый?'");
		Ответ = Ждать ВопросАсинх(ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		Если НЕ Ответ = КодВозвратаДиалога.Да Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	СформированныйОтвет = СфомироватьОтветНаОбращениеСФРНаСервере(Ссылка);
	
	ПустаяСсылкаНаОтвет = ПредопределенноеЗначение("Документ.ОтветСтрахователяНаОбращениеСФР.ПустаяСсылка");
	Если СформированныйОтвет = ПустаяСсылкаНаОтвет Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура("Ключ", СформированныйОтвет);
	ОткрытьФорму(
		"Документ.ОтветСтрахователяНаОбращениеСФР.Форма.ФормаДокумента",
		ПараметрыФормы);
	
КонецПроцедуры
	
#КонецОбласти

#Область ОбменЛисткамиНетрудоспособностиСФСС

&НаКлиенте
Процедура ПолучитьДанныеЭЛНИзФСС(ВыполняемоеОповещение, ЗапросДляПолученияЭЛН) Экспорт
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 		ЗапросДляПолученияЭЛН.Организация);
	Оповещение = Новый ОписаниеОповещения("ПолучитьДанныеЭЛНИзФССПослеПодписиSOAP", ЭтотОбъект, ДополнительныеПараметры);
	
	ВыгрузкаЗапросаОперацииСЭЛН = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ЗапросДляПолученияЭЛН);
	ВыгрузкаЗапросаОперацииСЭЛН.Вставить("ТекстЗапроса", ВыгрузкаЗапросаОперацииСЭЛН.ТекстXML);
	ВыгрузкаЗапросаОперацииСЭЛН.Удалить("ТекстXML");
	
	// Подписание запроса.
	АдресПодписанногоSOAPОперацииСЭЛНВФСС(
		Оповещение,
		ВыгрузкаЗапросаОперацииСЭЛН);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеЭЛНИзФССПослеПодписиSOAP(АдресЗашифрованногоЗапросаSOAP, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОрганизацияСсылка 		= ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если НЕ ЗначениеЗаполнено(АдресЗашифрованногоЗапросаSOAP) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Состояние(НСтр("ru = 'Получение данных ЭЛН из ФСС...'"));
	
	// Отправка запроса.
	РезультатОтправки = ОтправитьЗапросСервисуФССЭЛННаСервере(
		ОрганизацияСсылка,
		"getPrivateLNData",
		АдресЗашифрованногоЗапросаSOAP);
	Если РезультатОтправки.АдресЗашифрованногоОтветаSOAP = Неопределено Тогда
		Если СтрНайти(РезультатОтправки.ТекстОшибки, "зашифровано на ключе отличном от ключа уполномоченного лица") > 0 Тогда
			ИнструкцияСсылка = Новый ФорматированнаяСтрока(
				НСтр("ru = 'Инструкция по установке и настройке сертификатов обмена с ФСС.'"),,,,
				"https://its.1c.ru/bmk/elreps/fss_cert");
			ТекстСообщения = Новый ФорматированнаяСтрока(
				НСтр("ru = 'Ошибка расшифровки на сервере ФСС: выбран некорректный сертификата ФСС ЭЛН, ПОВЭД, СЭДО в настройках обмена с ФСС.'") + "
							|
							|",
				ИнструкцияСсылка);
			ПоказатьПредупреждение(, ТекстСообщения);
			
		Иначе
			ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатОтправки.ТекстОшибки);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьДанныеЭЛНИзФССПослеПроверкиПодписиSOAP", ЭтотОбъект, ДополнительныеПараметры);
	ПроверитьПодписьSOAPОтветаЭЛНВФСС(
		Оповещение,
		ОрганизацияСсылка,
		РезультатОтправки.АдресЗашифрованногоОтветаSOAP);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеЭЛНИзФССПослеПроверкиПодписиSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если НЕ Результат.ПодписьВалидна Тогда
		// Выводим сообщение, но продолжаем обмен (данные-то мы получили).
		ТекстОшибки = НСтр("ru = 'Подпись ЭЛН недействительна.'");
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Получение данных ЭЛН из ФСС'"),
			"Предупреждение",
			ТекстОшибки,,
			Истина);
	КонецЕсли;
	Результат.Вставить("ОшибкаПроверкиПодписиЗаписана", НЕ Результат.ПодписьВалидна);
	Результат.ПодписьВалидна = Истина;
	
	Состояние(НСтр("ru = 'Получение данных ЭЛН из ФСС...'"));
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьВыгрузкуРеестраДанныхЭЛНВСервисФСС(ВыполняемоеОповещение, ВыгрузкаРеестраДанныхЭЛН) Экспорт
	
	ОтправкаБезСохраненияПротокола = ВыгрузкаРеестраДанныхЭЛН.Свойство("ОтправкаБезСохраненияПротокола")
		И ВыгрузкаРеестраДанныхЭЛН.ОтправкаБезСохраненияПротокола = Истина;
	
	Если ОтправкаБезСохраненияПротокола Тогда
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 		ВыгрузкаРеестраДанныхЭЛН.Организация);
		Оповещение = Новый ОписаниеОповещения("ОтправитьВыгрузкуРеестраДанныхЭЛНВСервисФССПослеПодписиSOAP",
			ЭтотОбъект, ДополнительныеПараметры);
		
		// Подписание реестра.
		АдресПодписанногоSOAPЗагрузкиЭЛНВФСС(
			Оповещение,
			ВыгрузкаРеестраДанныхЭЛН.Организация,
			ВыгрузкаРеестраДанныхЭЛН.РегистрационныйНомерФСС,
			ВыгрузкаРеестраДанныхЭЛН.ТекстXML);
		
	Иначе
		ФормаРеестраЭЛН = ?(ВыгрузкаРеестраДанныхЭЛН.Свойство("Форма"),
			ВыгрузкаРеестраДанныхЭЛН.Форма, ВыполняемоеОповещение.Модуль);
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС(
			ФормаРеестраЭЛН,,,
			ВыгрузкаРеестраДанныхЭЛН.Организация,
			ВыгрузкаРеестраДанныхЭЛН,
			ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьВыгрузкуРеестраДанныхЭЛНВСервисФССПослеПодписиSOAP(АдресЗашифрованногоЗапросаSOAP, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОрганизацияСсылка 		= ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если НЕ ЗначениеЗаполнено(АдресЗашифрованногоЗапросаSOAP) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Состояние(НСтр("ru = 'Отправка реестра ЭЛН в ФСС...'"));
	
	// Отправка реестра.
	РезультатОтправки = ОтправитьЗапросСервисуФССЭЛННаСервере(
		ОрганизацияСсылка,
		"prParseReestrFileRequest",
		АдресЗашифрованногоЗапросаSOAP);
	Если РезультатОтправки.АдресЗашифрованногоОтветаSOAP = Неопределено Тогда
		Если СтрНайти(РезультатОтправки.ТекстОшибки, "зашифровано на ключе отличном от ключа уполномоченного лица") > 0 Тогда
			ИнструкцияСсылка = Новый ФорматированнаяСтрока(
				НСтр("ru = 'Инструкция по установке и настройке сертификатов обмена с ФСС.'"),,,,
				"https://its.1c.ru/bmk/elreps/fss_cert");
			ТекстСообщения = Новый ФорматированнаяСтрока(
				НСтр("ru = 'Ошибка расшифровки на сервере ФСС: выбран некорректный сертификата ФСС ЭЛН, ПОВЭД, СЭДО в настройках обмена с ФСС.'") + "
							|
							|",
				ИнструкцияСсылка);
			ПоказатьПредупреждение(, ТекстСообщения);
			
		Иначе
			ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатОтправки.ТекстОшибки);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ОтправитьВыгрузкуРеестраДанныхЭЛНВСервисФССПослеПроверкиПодписиSOAP",
		ЭтотОбъект, ДополнительныеПараметры);
	ПроверитьПодписьSOAPОтветаЭЛНВФСС(
		Оповещение,
		ОрганизацияСсылка,
		РезультатОтправки.АдресЗашифрованногоОтветаSOAP);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьВыгрузкуРеестраДанныхЭЛНВСервисФССПослеПроверкиПодписиSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если НЕ Результат.ПодписьВалидна Тогда
		// Выводим сообщение, но продолжаем обмен (данные-то мы получили).
		ТекстОшибки = НСтр("ru = 'Подпись результатов отправки недействительна.'");
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Отправка реестра ЭЛН в ФСС'"),
			"Предупреждение",
			ТекстОшибки,,
			Истина);
	КонецЕсли;
	Результат.Вставить("ОшибкаПроверкиПодписиЗаписана", НЕ Результат.ПодписьВалидна);
	Результат.ПодписьВалидна = Истина;
	
	Состояние(НСтр("ru = 'Отправка реестра ЭЛН в ФСС...'"));
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправитьЗапросСервисуФССЭЛННаСервере(
		ЗНАЧ Организация,
		ЗНАЧ Операция,
		ЗНАЧ АдресЗашифрованногоЗапросаSOAP)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьЗапросСервисуФССЭЛН(
		Организация,
		Операция,
		АдресЗашифрованногоЗапросаSOAP);
	
КонецФункции

&НаСервереБезКонтекста
Функция ИспользоватьТестовыйСерверФССЭЛН(ОрганизацияСсылка)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ИспользоватьТестовыйСерверФССЭЛН(ОрганизацияСсылка);
	
КонецФункции

#КонецОбласти

#Область ОтправкаПодтвержденияВидаДеятельностиФСС

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФСС(
		ВыполняемоеОповещение,
		ВыгрузкаЗапросаОперации,
		НастройкиОперации = Неопределено) Экспорт
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("ЗашифроватьSOAP", 					Истина);
	НастройкиВызова.Вставить("ЭтоИсключающаяКанонизация", 			Истина);
	НастройкиВызова.Вставить("ПрефиксИдентификатораОГРН", 			"");
	НастройкиВызова.Вставить("ПроверкиИАвтонастройкаВыполнены", 	Ложь);
	НастройкиВызова.Вставить("СертификатСтрахователя", 				Неопределено);
	НастройкиВызова.Вставить("СертификатФСС", 						Неопределено);
	НастройкиВызова.Вставить("МЧД", 								"");
	НастройкиВызова.Вставить("ОтправкаСсылка", 						Неопределено);
	НастройкиВызова.Вставить("СертификатШифрованияОтветаВПодписи", 	Ложь);
	
	Если НастройкиОперации <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, НастройкиОперации);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(НастройкиОперации);
	Иначе
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Ложь);
	КонецЕсли;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НастройкиВызова);
	
	Если НЕ НастройкиВызова.ПроверкиИАвтонастройкаВыполнены Тогда
		ОбъектСсылка = ?(ЗначениеЗаполнено(ВыгрузкаЗапросаОперации.Организация),
			ВыгрузкаЗапросаОперации.Организация, НастройкиВызова.ОтправкаСсылка);
		Свойства = ПолучитьСвойстваОбъектаДляФСС(ОбъектСсылка);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Свойства);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НастройкиВызова);
		
		ВыгрузкаЗапросаОперации.Организация 				= Свойства.ОрганизацияСсылка;
		ОрганизацияИспользуетОбменСФСС 						= Свойства.ОрганизацияИспользуетОбменСФСС;
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи 	= Свойства.ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи;
		НастроенОбменВУниверсальномФормате 					= Свойства.НастроенОбменВУниверсальномФормате;
		УчетнаяЗаписьДокументооборота 						= Свойства.УчетнаяЗаписьДокументооборота;
		
		Если НЕ ОрганизацияИспользуетОбменСФСС Тогда
			Если НастроенОбменВУниверсальномФормате Тогда
				ТекстСообщения = НСтр("ru = 'Для организации не включена возможность отправки в ФСС'");
				ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Иначе
				ДокументооборотСКОКлиент.ПоказатьРекламуАкцииКадровыеРешения(Свойства.ОрганизацияСсылка);
			КонецЕсли;
			
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ВыполнятьАвтонастройку = 
		НЕ НастройкиВызова.ПроверкиИАвтонастройкаВыполнены 
		И НастроенОбменВУниверсальномФормате
		И ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи;
		
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ВыгрузкаЗапросаОперации", ВыгрузкаЗапросаОперации);
	ДополнительныеПараметры.Вставить("НастройкиВызова", 		НастройкиВызова);
	ДополнительныеПараметры.Вставить("ВыполнятьАвтонастройку", 	ВыполнятьАвтонастройку);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если ВыполнятьАвтонастройку Тогда
		
		АвтоматическаяНастройкаУчетнойЗаписи(
			УчетнаяЗаписьДокументооборота,,
			Оповещение,,,
			Истина);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПроверкиЛицензииИСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	Если ВходящийКонтекст.ВыполнятьАвтонастройку Тогда
		
		Организация = ВходящийКонтекст.ВыгрузкаЗапросаОперации.Организация;
		
		ПроверитьСертификатИЛицензию(
			ОписаниеОповещения, 
			Неопределено, 
			Организация, 
			"ФСС");
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСвойстваОбъектаДляФСС(ОбъектСсылка)
	
	Если ТипЗнч(ОбъектСсылка) = Тип("СправочникСсылка.ОтправкиФСС") Тогда
		ОрганизацияСсылка = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОбъектСсылка, "Организация");
	Иначе
		ОрганизацияСсылка = ОбъектСсылка;
	КонецЕсли;
	
	//РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ОрганизацияСсылка);
	
	ОрганизацияИспользуетОбменСФСС 		= Ложь;
	ОрганизацияИспользуетАвтонастройку 	= Ложь;
	НастроенОбменВУниверсальномФормате 	= Ложь;
	УчетнаяЗаписьДокументооборота 		= Неопределено;
	
	ПолучитьСвойстваОрганизацииДляФССНаСервере(
		ОрганизацияСсылка,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройку,
		УчетнаяЗаписьДокументооборота);
	
	Результат = Новый Структура;
	Результат.Вставить("ОрганизацияСсылка", 								ОрганизацияСсылка);
	Результат.Вставить("ОрганизацияИспользуетОбменСФСС", 					ОрганизацияИспользуетОбменСФСС);
	Результат.Вставить("ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи", ОрганизацияИспользуетАвтонастройку);
	Результат.Вставить("НастроенОбменВУниверсальномФормате", 				НастроенОбменВУниверсальномФормате);
	Результат.Вставить("УчетнаяЗаписьДокументооборота", 					УчетнаяЗаписьДокументооборота);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПроверкиЛицензииИСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ВыгрузкаЗапросаОперации = ДополнительныеПараметры.ВыгрузкаЗапросаОперации;
	НастройкиВызова 		= ДополнительныеПараметры.НастройкиВызова;
	МестоХраненияКлюча		= КонтекстМоделиХраненияКлюча(НастройкиВызова);
	
	ИнформацияОСертификатахФСС = ИнформацияОСертификатахФССНаСервере(
		НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
		Истина);
	
	ДополнительныеПараметры.Вставить("ИнформацияОСертификатахФСС", ИнформацияОСертификатахФСС);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеУстановкиСертификатаФССЭЛН",
		ЭтотОбъект, ДополнительныеПараметры);
	
	УстанавливаемыеСертификаты = Новый Массив;
	
	СертификатКорневогоУЦ = Новый Структура;
	СертификатКорневогоУЦ.Вставить("Отпечаток", ИнформацияОСертификатахФСС.КорневойСертификатУЦГОСТ2012Отпечаток);
	СертификатКорневогоУЦ.Вставить("Данные", ИнформацияОСертификатахФСС.КорневойСертификатУЦГОСТ2012Данные);
	СертификатКорневогоУЦ.Вставить("Хранилище", "ROOT");
	УстанавливаемыеСертификаты.Добавить(СертификатКорневогоУЦ);
	
	СертификатУЦ = Новый Структура;
	СертификатУЦ.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФСССертификатУЦГОСТ2012Отпечаток);
	СертификатУЦ.Вставить("Данные", ИнформацияОСертификатахФСС.ФСССертификатУЦГОСТ2012Данные);
	СертификатУЦ.Вставить("Хранилище", "CA");
	УстанавливаемыеСертификаты.Добавить(СертификатУЦ);
	
	СертификатФССЭЛН = Новый Структура;
	СертификатФССЭЛН.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Отпечаток);
	СертификатФССЭЛН.Вставить("Данные", ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Данные);
	СертификатФССЭЛН.Вставить("Хранилище", "AddressBook");
	УстанавливаемыеСертификаты.Добавить(СертификатФССЭЛН);
	
	УстановитьСертификаты(
		Оповещение,
		УстанавливаемыеСертификаты,
		МестоХраненияКлюча);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеУстановкиСертификатаФССЭЛН(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыгрузкаЗапросаОперации 	= ДополнительныеПараметры.ВыгрузкаЗапросаОперации;
	НастройкиВызова 			= ДополнительныеПараметры.НастройкиВызова;
	ИнформацияОСертификатахФСС 	= ДополнительныеПараметры.ИнформацияОСертификатахФСС;
	МестоХраненияКлюча			= КонтекстМоделиХраненияКлюча(НастройкиВызова);
	
	ПространствоИменЗапроса = "http://ru/fss/ws/gtw/openservice";
	ПространствоИменXMLDSig = "http://www.w3.org/2000/09/xmldsig#";
	ДобавляемыйАтрибутКонверта = "xmlns:s=""http://schemas.xmlsoap.org/soap/envelope/""";
	ДобавляемыйАтрибутXMLDSig = "xmlns:ds=""" + ПространствоИменXMLDSig + """";
	ДобавляемыйАтрибутOASIS =
		"xmlns:wsu=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-utility-1.0.xsd""";
	ДобавляемыйАтрибутБезопасностьOASIS =
		"xmlns:wsse=""http://docs.oasis-open.org/wss/2004/01/oasis-200401-wss-wssecurity-secext-1.0.xsd""";
	ДобавляемыеАтрибутыПодписи = "xmlns=""" + ПространствоИменXMLDSig + """";
	Если НастройкиВызова.ЭтоИсключающаяКанонизация Тогда
		ДобавляемыеАтрибутыЗапроса = ДобавляемыйАтрибутКонверта
			+ " " + ДобавляемыйАтрибутOASIS;
		ДобавляемыеАтрибутыКонверта = ДобавляемыйАтрибутКонверта
			+ " " + ДобавляемыйАтрибутXMLDSig
			+ " " + ДобавляемыйАтрибутБезопасностьOASIS
			+ " " + ДобавляемыйАтрибутOASIS;
		ДобавляемыеАтрибутыБезопасности = ДобавляемыйАтрибутБезопасностьOASIS;
		ДобавляемыеАтрибутыПодписиИЗаписи = ДобавляемыеАтрибутыПодписи;
	Иначе
		ДобавляемыеАтрибутыЗапроса = ДобавляемыйАтрибутXMLDSig
			+ " xmlns:fil=""" + ПространствоИменЗапроса + """"
			+ " " + ДобавляемыйАтрибутКонверта
			+ " " + ДобавляемыйАтрибутБезопасностьOASIS
			+ " " + ДобавляемыйАтрибутOASIS;
		ДобавляемыеАтрибутыКонверта = ДобавляемыеАтрибутыЗапроса;
		ДобавляемыеАтрибутыБезопасности = "";
		ДобавляемыеАтрибутыПодписиИЗаписи = ДобавляемыеАтрибутыПодписи
			+ ?(ДобавляемыеАтрибутыПодписи <> "" И ДобавляемыеАтрибутыЗапроса <> "", " ", "")
			+ ДобавляемыеАтрибутыЗапроса;
	КонецЕсли;
	
	Если Лев(ВыгрузкаЗапросаОперации.ТекстЗапроса, 39) = "<?xml version=""1.0"" encoding=""utf-8""?>" + Символы.ПС Тогда
		ВыгрузкаЗапросаОперации.ТекстЗапроса = Сред(ВыгрузкаЗапросаОперации.ТекстЗапроса, 40);
		ВыгрузкаЗапросаОперации.ТекстЗапроса = СокрЛ(ВыгрузкаЗапросаОперации.ТекстЗапроса);
	КонецЕсли;
	ВыгрузкаЗапросаОперации.ТекстЗапроса = УдалитьПустоеПространствоМеждуУзлами(ВыгрузкаЗапросаОперации.ТекстЗапроса);
	СкорректированнаяВыгрузкаЗапроса = СкорректироватьВыгрузкуЗапросаОперацииДляSOAP(
		ВыгрузкаЗапросаОперации.ТекстЗапроса,
		ПространствоИменЗапроса);
	СкорректированнаяВыгрузкаЗапросаОперации = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ВыгрузкаЗапросаОперации);
	СкорректированнаяВыгрузкаЗапросаОперации.ТекстЗапроса = СкорректированнаяВыгрузкаЗапроса;
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	РезультатИнициализации = АдресПодписанногоSOAPОперацииСОКВЭДВФССИнициализацияНаСервере(
		ВыгрузкаЗапросаОперации.Организация,
		СкорректированнаяВыгрузкаЗапросаОперации,
		ДобавляемыеАтрибутыЗапроса,
		НастройкиВызова.ЗашифроватьSOAP,
		МестоХраненияКлюча,
		НЕ СертификатыИзвестны,
		НастройкиВызова.ЭтоИсключающаяКанонизация);
	
	Если РезультатИнициализации = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ЗапросОперации64 = РезультатИнициализации.ЗапросОперации64;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РезультатИнициализации);
	
	Если СертификатыИзвестны Тогда
		СертификатСтрахователя 		= НастройкиВызова.СертификатСтрахователя;
		СертификатФССЭЛНОтпечаток 	= Неопределено;
		СертификатФССЭЛН 			= НастройкиВызова.СертификатФСС;
		МЧД 						= НастройкиВызова.МЧД;
		
	Иначе
		СертификатСтрахователяОтпечаток = РезультатИнициализации.СертификатСтрахователяОтпечаток;
		СертификатСтрахователя 			= РезультатИнициализации.СертификатСтрахователя;
		СертификатФССЭЛНОтпечаток 		= РезультатИнициализации.СертификатФССЭЛНОтпечаток;
		СертификатФССЭЛН 				= РезультатИнициализации.СертификатФССЭЛН;
		МЧД 							= РезультатИнициализации.МЧД;
	КонецЕсли;
	
	ЗаменитьСертификатФССЭЛННаГОСТ2012(
		ИнформацияОСертификатахФСС,
		МестоХраненияКлюча,
		СертификатФССЭЛНОтпечаток,
		СертификатФССЭЛН);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		МассивАдресовДанныхСертификатов = Новый Массив;
		Для каждого ИнформацияОСертификатеФСС Из ИнформацияОСертификатахФСС Цикл
			Если Прав(ИнформацияОСертификатеФСС.Ключ, 6) = "Данные" И ИнформацияОСертификатеФСС.Значение <> Неопределено Тогда
				МассивАдресовДанныхСертификатов.Добавить(ИнформацияОСертификатеФСС.Значение);
			КонецЕсли;
		КонецЦикла;
		УдалитьАдресаИзВременногоХранилища(МассивАдресовДанныхСертификатов);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", 			РезультатИнициализации.РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ОГРН", 								РезультатИнициализации.ОГРН);
	ДополнительныеПараметры.Вставить("Организация", 						ВыгрузкаЗапросаОперации.Организация);
	ДополнительныеПараметры.Вставить("ВыгрузкаЗапроса", 					СкорректированнаяВыгрузкаЗапроса);
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛНОтпечаток", 			СертификатФССЭЛНОтпечаток);
	ДополнительныеПараметры.Вставить("СертификатФССЭЛН", 					СертификатФССЭЛН);
	ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("НастройкиВызова", 					НастройкиВызова);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыЗапроса", 			ДобавляемыеАтрибутыЗапроса);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыКонверта", 		ДобавляемыеАтрибутыКонверта);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыБезопасности", 	ДобавляемыеАтрибутыБезопасности);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписи", 			ДобавляемыеАтрибутыПодписи);
	ДополнительныеПараметры.Вставить("ДобавляемыеАтрибутыПодписиИЗаписи", 	ДобавляемыеАтрибутыПодписиИЗаписи);
	ДополнительныеПараметры.Вставить("ЗапросОперации64", 					ЗапросОперации64);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПоискаСертификатаСтрахователя",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если СертификатыИзвестны ИЛИ НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, Истина, СертификатСтрахователя);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатСтрахователяОтпечаток, "MY");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаменитьСертификатФССЭЛННаГОСТ2012(
		ИнформацияОСертификатахФСС,
		МестоХраненияКлюча,
		СертификатФССЭЛНОтпечаток,
		СертификатФССЭЛН)
	
	Если СертификатФССЭЛНОтпечаток = ИнформацияОСертификатахФСС.ФССЭЛНСертификатОтпечаток Тогда
		СертификатФССЭЛНОтпечаток = ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Отпечаток;
		
	ИначеЕсли СертификатФССЭЛНОтпечаток = ИнформацияОСертификатахФСС.ФССЭЛНТестовыйСертификатОтпечаток Тогда
		СертификатФССЭЛНОтпечаток = ИнформацияОСертификатахФСС.ФССЭЛНТестовыйСертификатГОСТ2012Отпечаток;
	КонецЕсли;
	
	Если СертификатФССЭЛН <> Неопределено
		И СертификатФССЭЛН.Отпечаток = ИнформацияОСертификатахФСС.ФССЭЛНСертификатОтпечаток Тогда
		
		СертификатФССЭЛН = Новый Структура;
		СертификатФССЭЛН.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Отпечаток);
		СертификатФССЭЛН.Вставить("ДействителенС", '20181008120728');
		СертификатФССЭЛН.Вставить("ДействителенПо", '20191008121728');
		СертификатФССЭЛН.Вставить("СерийныйНомер", "00 c4 75 0d 5b 11 e4 db 80 e8 11 f4 ca b7 4f a0 1e");
		СертификатФССЭЛН.Вставить("Поставщик", "C=RU,CN=ФГБУ НИИ «Восход»,E=uc@voskhod.ru,L=г. Москва,O=ФГБУ НИИ «Восход»,"
			+ "OID1_2_643_100_1=1167746289248,OID1_2_643_3_131_1_1=007729498813,OID1_2_840_113549_1_9_1=uc@voskhod.ru,"
			+ "OID2_5_4_10=ФГБУ НИИ «Восход»,OID2_5_4_3=ФГБУ НИИ «Восход»,OID2_5_4_6=RU,OID2_5_4_7=г. Москва,"
			+ "OID2_5_4_8=77 Москва,OID2_5_4_9=улица Удальцова, дом 85,ST=77 Москва,Street=улица Удальцова, дом 85");
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			ДанныеСертификата = ПолучитьИзВременногоХранилища(ИнформацияОСертификатахФСС.ФССЭЛНСертификатГОСТ2012Данные);
			СертификатФССЭЛН.Вставить("Сертификат", ДанныеСертификата);
		Иначе
			СертификатФССЭЛН.Вставить("Хранилище", "ADDRESSBOOK");
		КонецЕсли;
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФССЭЛН);
		
	ИначеЕсли СертификатФССЭЛН <> Неопределено
		И СертификатФССЭЛН.Отпечаток = ИнформацияОСертификатахФСС.ФССЭЛНТестовыйСертификатОтпечаток Тогда
		
		СертификатФССЭЛН = Новый Структура;
		СертификатФССЭЛН.Вставить("Отпечаток", ИнформацияОСертификатахФСС.ФССЭЛНТестовыйСертификатГОСТ2012Отпечаток);
		СертификатФССЭЛН.Вставить("ДействителенС", '20181008120558');
		СертификатФССЭЛН.Вставить("ДействителенПо", '20191008121558');
		СертификатФССЭЛН.Вставить("СерийныйНомер", "00 c4 75 0d 5b 11 e4 db 80 e8 11 f3 ca e9 fc 45 e5");
		СертификатФССЭЛН.Вставить("Поставщик", "C=RU,CN=ФГБУ НИИ «Восход»,E=uc@voskhod.ru,L=г. Москва,"
			+ "O=ФГБУ НИИ «Восход»,OID1_2_643_100_1=1167746289248,OID1_2_643_3_131_1_1=007729498813,"
			+ "OID1_2_840_113549_1_9_1=uc@voskhod.ru,OID2_5_4_10=ФГБУ НИИ «Восход»,OID2_5_4_3=ФГБУ НИИ «Восход»,"
			+ "OID2_5_4_6=RU,OID2_5_4_7=г. Москва,OID2_5_4_8=77 Москва,OID2_5_4_9=улица Удальцова, дом 85,ST=77 Москва,"
			+ "Street=улица Удальцова, дом 85");
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			ДанныеСертификата = ПолучитьИзВременногоХранилища(ИнформацияОСертификатахФСС.ФССЭЛНТестовыйСертификатГОСТ2012Данные);
			СертификатФССЭЛН.Вставить("Сертификат", ДанныеСертификата);
		Иначе
			СертификатФССЭЛН.Вставить("Хранилище", "ADDRESSBOOK");
		КонецЕсли;
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФССЭЛН);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция УдалитьПустоеПространствоМеждуУзлами(ТекстXML)
	
	МассивРезультатов = Новый Массив;
	
	ПозицияНачала = 1;
	ДлинаТекстаXML = СтрДлина(ТекстXML);
	Для ПозицияСимвола = 1 По ДлинаТекстаXML Цикл
		СимволXML = Сред(ТекстXML, ПозицияСимвола, 1);
		Если СимволXML = "<" Тогда
			ТекстМеждуУзлами = Сред(ТекстXML, ПозицияНачала, ПозицияСимвола - ПозицияНачала);
			ТекстМеждуУзлами = СокрЛП(ТекстМеждуУзлами);
			МассивРезультатов.Добавить(ТекстМеждуУзлами);
			ПозицияНачала = ПозицияСимвола;
			
		ИначеЕсли СимволXML = ">" Тогда
			ТекстУзла = Сред(ТекстXML, ПозицияНачала, ПозицияСимвола - ПозицияНачала + 1);
			МассивРезультатов.Добавить(ТекстУзла);
			ПозицияНачала = ПозицияСимвола + 1;
		КонецЕсли;
	КонецЦикла;
	
	ТекстМеждуУзлами = Сред(ТекстXML, ПозицияНачала, ДлинаТекстаXML - ПозицияНачала + 1);
	ТекстМеждуУзлами = СокрЛП(ТекстМеждуУзлами);
	МассивРезультатов.Добавить(ТекстМеждуУзлами);
	
	Результат = СтрСоединить(МассивРезультатов);
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресПодписанногоSOAPОперацииСОКВЭДВФССИнициализацияНаСервере(
		Знач ОрганизацияСсылка,
		Знач ВыгрузкаЗапросаОперации,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач ПолучитьСертификатФССЭЛН = Ложь,
		Знач МестоХраненияКлюча = Неопределено,
		Знач ВозвращатьСертификаты = Истина,
		Знач ЭтоИсключающаяКанонизация = Ложь)
	
	СвойстваОрганизации = СвойстваОрганизацииДляОбмена(
		ОрганизацияСсылка,
		Ложь,
		НЕ ЗначениеЗаполнено(ВыгрузкаЗапросаОперации.ОГРН));
	
	Результат = Новый Структура;
	Результат.Вставить("РегистрационныйНомерФСС", 	Неопределено);
	Результат.Вставить("ОГРН", 						Неопределено);
	
	Результат.РегистрационныйНомерФСС = ?(СвойстваОрганизации.Свойство("РегистрационныйНомерФСС"),
		СвойстваОрганизации.РегистрационныйНомерФСС, ВыгрузкаЗапросаОперации.РегистрационныйНомерФСС);
	Результат.ОГРН = ?(СвойстваОрганизации.Свойство("ОГРН"),
		СвойстваОрганизации.ОГРН, ВыгрузкаЗапросаОперации.ОГРН);
	
	ЗапросОперации64 = ЗапросОперацииСОКВЭДВФССДляПодписиSOAP64НаСервере(
		ВыгрузкаЗапросаОперации.ТекстЗапроса,
		ДобавляемыеАтрибутыЗапроса,
		Результат,
		ЭтоИсключающаяКанонизация);
	Если ЗапросОперации64 = Неопределено Тогда
		Ошибка = НСтр("ru = 'Подтверждение вида деятельности не заполнено'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
		Возврат Неопределено;
	КонецЕсли;
	
	Если МестоХраненияКлюча = Неопределено ИЛИ ВозвращатьСертификаты Тогда
		РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	КонецЕсли;
	Если ВозвращатьСертификаты Тогда
		ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	КонецЕсли;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
		ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		ДвоичныеДанныеЗапросаОперации = Base64Значение(ЗапросОперации64);
		ЗапросОперации64 = ПоместитьВоВременноеХранилище(ДвоичныеДанныеЗапросаОперации, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	Если ВозвращатьСертификаты Тогда
		ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка);
		Если ОтпечаткиСертификатов = Неопределено ИЛИ НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток) Тогда
			Ошибка = НСтр("ru = 'Не выбран сертификат страхователя в настройках обмена с ФСС для 1С-Отчетности'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Возврат Неопределено;
		КонецЕсли;
		Если ПолучитьСертификатФССЭЛН И НЕ ЗначениеЗаполнено(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток) Тогда
			Ошибка = НСтр("ru = 'Не выбран сертификат ФСС ЭЛН в настройках обмена с ФСС для 1С-Отчетности'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Возврат Неопределено;
		КонецЕсли;
		
		СертификатСтрахователя = Неопределено;
		СертификатФССЭЛН = Неопределено;
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
			ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
			Иначе
				СертификатСтрахователя = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY");
			КонецЕсли;
			Если СертификатСтрахователя = Неопределено Тогда
				Ошибка = НСтр("ru = 'Не найден сертификат страхователя'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
				Возврат Неопределено;
			КонецЕсли;
			
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
			СертификатСтрахователя.Вставить("КлючевойКонтейнер", ИдентификаторДокументооборота);
			
			Если ПолучитьСертификатФССЭЛН Тогда
				Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
					СертификатФССЭЛН = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
				Иначе
					СертификатФССЭЛН = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
				КонецЕсли;
				Если СертификатФССЭЛН = Неопределено Тогда
					Ошибка = НСтр("ru = 'Не найден сертификат ФСС ЭЛН'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
					Возврат Неопределено;
				КонецЕсли;
				ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФССЭЛН);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Результат.Вставить("ЗапросОперации64", 						ЗапросОперации64);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Если ВозвращатьСертификаты Тогда
		Результат.Вставить("СертификатСтрахователяОтпечаток", 	ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
		Результат.Вставить("СертификатСтрахователя", 			СертификатСтрахователя);
		Результат.Вставить("СертификатФССЭЛНОтпечаток", 		ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток);
		Результат.Вставить("СертификатФССЭЛН", 					СертификатФССЭЛН);
		Результат.Вставить("ТестовыйСерверФССЭЛН", 				ОтпечаткиСертификатов.ТестовыйСерверФССЭЛН);
		Результат.Вставить("МЧД", 								ОтпечаткиСертификатов.МЧД);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойстваОрганизацииДляОбмена(
		ОрганизацияСсылка,
		ПолучитьРегистрационныйНомерФСС = Истина,
		ПолучитьОГРН = Ложь,
		ПолучитьКодПодчиненностиФСС = Ложь,
		ПолучитьИННКППиСНИЛС = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Результат = КонтекстЭДО.СвойстваОрганизацииДляОбмена(
		ОрганизацияСсылка,
		ПолучитьРегистрационныйНомерФСС,
		ПолучитьОГРН,
		ПолучитьКодПодчиненностиФСС,
		ПолучитьИННКППиСНИЛС);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПоискаСертификатаСтрахователя(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатФССЭЛНОтпечаток 			= ДополнительныеПараметры.СертификатФССЭЛНОтпечаток;
	СертификатФССЭЛН 					= ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиВызова 					= ДополнительныеПараметры.НастройкиВызова;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	СертификатСтрахователя = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", СертификатСтрахователя);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПоискаСертификатаФССЭЛН",
		ЭтотОбъект, ДополнительныеПараметры);
	Если СертификатыИзвестны ИЛИ НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата",
			Истина, Неопределено, НастройкиВызова.ЗашифроватьSOAP, СертификатФССЭЛН);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СертификатФССЭЛНОтпечаток, "AddressBook");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПоискаСертификатаФССЭЛН(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя 				= ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиВызова 					= ДополнительныеПараметры.НастройкиВызова;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатыИзвестны = НастройкиВызова.СертификатСтрахователя <> Неопределено
		ИЛИ НастройкиВызова.СертификатФСС <> Неопределено;
	
	СертификатФССЭЛН = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатСтрахователя, НСтр("ru = 'Сертификат страхователя'"));
	Если НастройкиВызова.ЗашифроватьSOAP Тогда
		СертификатыОбмена.Добавить(СертификатФССЭЛН, НСтр("ru = 'Сертификат ФСС ЭЛН'"));
	КонецЕсли;
	
	Если НЕ СертификатыИзвестны И НЕ СертификатыГодятсяДляПодписанияОтчетностиФСС(СертификатыОбмена) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("СертификатФССЭЛН", СертификатФССЭЛН);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЭкспортаСертификата",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СертификатСтрахователя);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЭкспортаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЗапросОперации64 					= ДополнительныеПараметры.ЗапросОперации64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатПолучателя64 = Результат.СтрокаBase64;
	
	ДополнительныеПараметры.Вставить("СертификатПолучателя64", СертификатПолучателя64);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЗаписиФайла",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла",
			Истина, Неопределено, ЗапросОперации64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ЗапросОперации64);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЗаписиФайла(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя 				= ДополнительныеПараметры.СертификатСтрахователя;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиВызова 					= ДополнительныеПараметры.НастройкиВызова;
	ДобавляемыеАтрибутыЗапроса 			= ДополнительныеПараметры.ДобавляемыеАтрибутыЗапроса;
	ДобавляемыеАтрибутыПодписи 			= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	ДобавляемыеАтрибутыПодписиИЗаписи 	= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписиИЗаписи;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОрганизацияСсылка					= ДополнительныеПараметры.Организация;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлЗапросаОперации = Результат.ИмяФайла;
	
	ДополнительныеПараметры.Вставить("ФайлЗапросаОперации", ФайлЗапросаОперации);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПодписания", ЭтотОбъект, ДополнительныеПараметры);
	
	ИдентификаторПодписываемыхДанных = ?(ЗначениеЗаполнено(ДополнительныеПараметры.ОГРН),
		"OGRN_" + ДополнительныеПараметры.ОГРН, "body");
	
	НастройкиПодписи = Новый Структура;
	НастройкиПодписи.Вставить("Трансформация1_1", 						Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 				Ложь);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 				Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 						Неопределено);
	НастройкиПодписи.Вставить("НормализацияОтключена", 					Истина);
	НастройкиПодписи.Вставить("ДанныеВнутриПодписи", 					Истина);
	НастройкиПодписи.Вставить("ИдентификаторПодписываемыхДанных", 		ИдентификаторПодписываемыхДанных);
	НастройкиПодписи.Вставить("ДобавляемыеАтрибуты", 					ДобавляемыеАтрибутыПодписиИЗаписи);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОСертификате", 		Истина);
	НастройкиПодписи.Вставить("НеВключатьИнформациюОбОбъекте", 			Истина);
	НастройкиПодписи.Вставить("ДополнительныеТрансформации", 			Неопределено);
	НастройкиПодписи.Вставить("ДополнительныеОбратныеТрансформации", 	Неопределено);
	НастройкиПодписи.Вставить("ИспользуетсяУзелТрансформации", 			Истина);
	НастройкиПодписи.Вставить("ЭтоИсключающаяКанонизация", 				НастройкиВызова.ЭтоИсключающаяКанонизация);
	НастройкиПодписи.Вставить("Применение", 							"ОтправкиФСС");
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ПодтверждениеВидаДеятельности", ОрганизацияСсылка); 
		ПодготовитьОблачнуюПодпись("Подтверждение вида деятельности.", ".xml", ТребуетПодтверждениеОперации, НастройкиПодписи);
	КонецЕсли;
	
	ПодписатьФайлXMLDSig(
		СертификатСтрахователя,
		ФайлЗапросаОперации,
		Оповещение,,,
		НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеПодписания(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФайлЗапросаОперации 				= ДополнительныеПараметры.ФайлЗапросаОперации;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьИзВременногоХранилища(ФайлЗапросаОперации);
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлЗапросаОперации);
	КонецЕсли;
	
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось подписать подтверждение вида деятельности'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлПодписиЗапросаОперации", ПолноеИмяПодписанногоФайла);
	Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЧтенияФайла",
		ЭтотОбъект, ДополнительныеПараметры);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64",
			Истина, Неопределено, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЧтенияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	Организация 						= ДополнительныеПараметры.Организация;
	ВыгрузкаЗапроса 					= ДополнительныеПараметры.ВыгрузкаЗапроса;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиВызова 					= ДополнительныеПараметры.НастройкиВызова;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ДобавляемыеАтрибутыКонверта 		= ДополнительныеПараметры.ДобавляемыеАтрибутыКонверта;
	ДобавляемыеАтрибутыБезопасности 	= ДополнительныеПараметры.ДобавляемыеАтрибутыБезопасности;
	ДобавляемыеАтрибутыПодписи 			= ДополнительныеПараметры.ДобавляемыеАтрибутыПодписи;
	СертификатПолучателя64 				= ДополнительныеПараметры.СертификатПолучателя64;
	ФайлПодписиЗапросаОперации 			= ДополнительныеПараметры.ФайлПодписиЗапросаОперации;
	
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлПодписиЗапросаОперации);
	КонецЕсли;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ФайлПодписиЗапросаОперации64 = Результат.СтрокаBase64;
	
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("РегистрационныйНомерФСС", 				ДополнительныеПараметры.РегистрационныйНомерФСС);
	НастройкиОперации.Вставить("ОГРН", 									ДополнительныеПараметры.ОГРН);
	НастройкиОперации.Вставить("Организация", 							Организация);
	НастройкиОперации.Вставить("ВозвращатьТекстВBase64", 				НастройкиВызова.ЗашифроватьSOAP);
	НастройкиОперации.Вставить("ЭтоИсключающаяКанонизация", 			НастройкиВызова.ЭтоИсключающаяКанонизация);
	НастройкиОперации.Вставить("ПрефиксИдентификатораОГРН", 			НастройкиВызова.ПрефиксИдентификатораОГРН);
	НастройкиОперации.Вставить("СертификатШифрованияОтветаВПодписи", 	НастройкиВызова.СертификатШифрованияОтветаВПодписи);
	НастройкиОперации.Вставить("МЧД", 									ДополнительныеПараметры.МЧД);
	РезультатОперации = АдресSOAPЗапросОперацииСОКВЭДВФССПоПодписиНаСервере(
		ВыгрузкаЗапроса,
		ФайлПодписиЗапросаОперации64,
		СертификатПолучателя64,
		ДобавляемыеАтрибутыКонверта,
		ДобавляемыеАтрибутыБезопасности,
		ДобавляемыеАтрибутыПодписи,
		НастройкиОперации);
	
	Если НЕ НастройкиВызова.ЗашифроватьSOAP Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОперации);
		
	Иначе
		ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", РезультатОперации);
		Оповещение = Новый ОписаниеОповещения("АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЗаписиФайлаПодписанногоSOAP",
			ЭтотОбъект, ДополнительныеПараметры);
			
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, РезультатОперации);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, РезультатОперации);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПодписанногоSOAPОперацииСОКВЭДВФССПослеЗаписиФайлаПодписанногоSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатСтрахователя 	= ДополнительныеПараметры.СертификатСтрахователя;
	НастройкиВызова 		= ДополнительныеПараметры.НастройкиВызова;
	СертификатФССЭЛН 		= ДополнительныеПараметры.СертификатФССЭЛН;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ПодписанныйSOAP64 		= ДополнительныеПараметры.ПодписанныйSOAP64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(ВыполняемоеОповещение.ДополнительныеПараметры) = Тип("Структура") Тогда
		ВыполняемоеОповещение.ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", ПодписанныйSOAP64);
	КонецЕсли;
	
	ФайлПодписанногоSOAP = Результат.ИмяФайла;
	
	ШаблонЗапросаSOAP =
		"<SOAP-ENV:Envelope xmlns:SOAP-ENV=""http://schemas.xmlsoap.org/soap/envelope/"">
		|	<SOAP-ENV:Header/>
		|	<SOAP-ENV:Body>
		|%1
		|	</SOAP-ENV:Body>
		|</SOAP-ENV:Envelope>";
	НастройкиОперации = Новый Структура;
	НастройкиОперации.Вставить("ШаблонЗапросаSOAP", 	ШаблонЗапросаSOAP);
	НастройкиОперации.Вставить("ЭтоСодержимоеOASIS", 	Истина);
	НастройкиОперации.Вставить("Применение", 			"ОтправкиФСС");
	НастройкиОперации.Вставить("ПоместитьВУзелX509CertificateСертификатПолучателя",
		НастройкиВызова.СертификатШифрованияОтветаВПодписи);
	НастройкиОперации.Вставить("СценарийФСС", Истина);
	
	ЗашифроватьФайлXML(
		СертификатФССЭЛН,
		СертификатСтрахователя,
		ФайлПодписанногоSOAP,
		ВыполняемоеОповещение,,
		Истина,
		НастройкиОперации);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗапросОперацииСОКВЭДВФССДляПодписиSOAP64НаСервере(
		Знач ВыгрузкаЗапросаОперации,
		Знач ДобавляемыеАтрибутыЗапроса,
		Знач СвойстваОрганизации,
		Знач ЭтоИсключающаяКанонизация = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ЗапросОперацииСОКВЭДВФССДляПодписиSOAP64(
		ВыгрузкаЗапросаОперации,
		ДобавляемыеАтрибутыЗапроса,
		СвойстваОрганизации,
		ЭтоИсключающаяКанонизация);
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресSOAPЗапросОперацииСОКВЭДВФССПоПодписиНаСервере(
		Знач ВыгрузкаЗапроса,
		Знач ПодписьЗапросаЭЛН64,
		Знач СертификатПолучателя64,
		Знач ДобавляемыеАтрибутыКонверта,
		Знач ДобавляемыеАтрибутыБезопасности,
		Знач ДобавляемыеАтрибутыПодписи,
		Знач НастройкиОперации = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.АдресSOAPЗапросОперацииСОКВЭДВФССПоПодписи(
		ВыгрузкаЗапроса,
		ПодписьЗапросаЭЛН64,
		СертификатПолучателя64,
		ДобавляемыеАтрибутыКонверта,
		ДобавляемыеАтрибутыБезопасности,
		ДобавляемыеАтрибутыПодписи,
		НастройкиОперации);
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция ОпределитьОрганПФРОрганизации(Организация, КодОрганаПФР = Неопределено, РегНомерПФР = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьОрганПФРОрганизации(Организация, КодОрганаПФР, РегНомерПФР);
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗаписатьФайлВыгрузкиВоВременноеХранилище(
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		КодировкаФайла,
		УникальныйИдентификатор,
		УдалитьBOM = Ложь)
	
	// сохраняем выгрузку во временный файл
	ИмяВремФайла = ПолучитьИмяВременногоФайла();
	ОбъектЗаписьТекста = Новый ЗаписьТекста(ИмяВремФайла, КодировкаФайла);
	ОбъектЗаписьТекста.Записать(ТекстВыгрузки);
	ОбъектЗаписьТекста.Закрыть();
	
	ДанныеВыгрузки = Новый ДвоичныеДанные(ИмяВремФайла);
	// удалим из файла в кодировке "UTF-8" сигнатуру BOM
	Если нрег(КодировкаФайла) = "utf-8" И УдалитьBOM Тогда
		ТекстВыгрузки64 = Base64Строка(ДанныеВыгрузки);
		ТекстВыгрузки64 = Сред(ТекстВыгрузки64, 5);
		ДанныеВыгрузки = Base64Значение(ТекстВыгрузки64);
	КонецЕсли;
	
	// помещаем файл во временное хранилище
	АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(ДанныеВыгрузки, УникальныйИдентификатор);
	
	// удаляем временный файл
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВремФайла);
	
	//
	Возврат АдресВоВременномХранилище;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьРегламентированныйОтчетПФРИзВнешнихЭлектронныхПредставлений(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьРегламентированныйОтчетПФРИзВнешнихЭлектронныхПредставлений(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьЗаявленияПоЭлДокументооборотуСПФР(Заявление)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьЗаявленияПоЭлДокументооборотуСПФР(Заявление);
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьУведомлениеОПредоставленииПолномочийПредставителю(Уведомление)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьУведомлениеОПредоставленииПолномочийПредставителю(Уведомление);
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьУведомлениеОПрекращенииПолномочийПредставителя(Уведомление)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьУведомлениеОПрекращенииПолномочийПредставителя(Уведомление);
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетПФР(Контекст, ВыполняемоеОповещение)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура,
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Отчет = Контекст.Документ;
	ИмяМетаданныхДокумента = Контекст.ТипОтправляемогоДокумента;
	Если ИмяМетаданныхДокумента = "РегламентированныйОтчет" Тогда
		
		Результат = Новый Массив;
		
		ЭтоКомплектыРСВ1 = (Контекст.ИсточникОтчета = "РегламентированныйОтчетРСВ1" И ТипЗнч(Контекст.ДатаНачалаПериодаОтчета) = Тип("Дата") И Контекст.ДатаНачалаПериодаОтчета >= '20140101000000');
		
		// особая обработка для РСВ-1 - форма должна отправляться из АДВ-6-2
		Если Контекст.ИсточникОтчета = "РегламентированныйОтчетРСВ1" И ЭтоПервичныйОтчет(Отчет) И НЕ ЭтоКомплектыРСВ1 Тогда
			ТекстВопроса = "В соответствии с требованиями Пенсионного фонда Российской Федерации представление файлов отчетности
				|в электронной форме по страховым взносам и персонифицированному учету с 1 апреля 2011 года,
				|включая отчетность за первый квартал 2011 года, в обязательном порядке осуществляется одновременно и
				|одним отправлением.
				|
				|Используйте форму документа ""Опись сведений АДВ-6-2"" для совместной отправки.
				|
				|Продолжить действие БЕЗ ПРИВЯЗКИ к отчетности по персонифицированному учету (не рекомендуется)?";
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("Контекст", Контекст);
			ДополнительныеПараметры.Вставить("Отчет", Отчет);
			ДополнительныеПараметры.Вставить("Результат", Результат);
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("ЭтоКомплектыРСВ1", ЭтоКомплектыРСВ1);
			ОписаниеОповещения = Новый ОписаниеОповещения("ВопросПродолжитьВыгрузкуРеглОтчетаПФРЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет, "Внимание!");
		Иначе
			ВыгрузитьРегламентированныйОтчетПФРПродолжение(Контекст, Отчет, Результат, ВыполняемоеОповещение, ЭтоКомплектыРСВ1)
		КонецЕсли;
		
	
	ИначеЕсли ИмяМетаданныхДокумента = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
		
		Результат = ВыгрузитьРегламентированныйОтчетПФРИзВнешнихЭлектронныхПредставлений(Контекст);
		ВсегоВАрхиве = 0;
		Для Каждого Запись Из Результат Цикл
			Если Запись.ВАрхиве Тогда
				ВсегоВАрхиве = ВсегоВАрхиве + 1;
			КонецЕсли;
		КонецЦикла;
		
		Если ВсегоВАрхиве > 0 Тогда //Данные одного или более файлов отсутствуют
			
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("Контекст", Контекст);
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		
			ОписаниеОповещения = Новый ОписаниеОповещения("ВопросПродолжитьВыгрузкуРеглОтчетаПФРНеудача", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьУведомлениеАрхивныхФайлов(ОписаниеОповещения, 24, 2, Истина);			
			
			Возврат;
			
		КонецЕсли;
	
		Контекст.Вставить("РезультатВыгрузки", Результат);
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
		
	ИначеЕсли ИмяМетаданныхДокумента = "ЗаявленияПоЭлДокументооборотуСПФР" Тогда
		
		Контекст.Вставить("РезультатВыгрузки", ВыгрузитьЗаявленияПоЭлДокументооборотуСПФР(Отчет));
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
		
	ИначеЕсли ИмяМетаданныхДокумента = "УведомлениеОПредоставленииПолномочийПредставителю" Тогда
		
		Контекст.Вставить("РезультатВыгрузки", ВыгрузитьУведомлениеОПредоставленииПолномочийПредставителю(Отчет));
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		Если НЕ Контекст.РезультатВыгрузки = Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
		КонецЕсли;
		
	ИначеЕсли ИмяМетаданныхДокумента = "УведомлениеОПрекращенииПолномочийПредставителя" Тогда
		
		Контекст.Вставить("РезультатВыгрузки", ВыгрузитьУведомлениеОПрекращенииПолномочийПредставителя(Отчет));
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
		
	Иначе
		
		Результат = Новый Массив;
		
		СвойстваФайлаВыгрузки = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ВыгрузитьДокументСервер(
			Отчет,
			Контекст.УникальныйИдентификатор);
		Если НЕ ЗначениеЗаполнено(СвойстваФайлаВыгрузки) Тогда
			РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
			
			Возврат;
			
		КонецЕсли;
		
		// заполняем результат
		Если ТипЗнч(СвойстваФайлаВыгрузки) = Тип("Структура") Тогда
			ТекстВыгрузки = СвойстваФайлаВыгрузки.АдресФайлаВыгрузки;
			ИмяФайлаВыгрузки = СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки;
			ТипФайлаОтчетности = СвойстваФайлаВыгрузки.ТипФайлаВыгрузки;
			ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес", Отчет, ТипПоИдентификаторуТипаФайлаВыгрузки(ТипФайлаОтчетности), СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки, СвойстваФайлаВыгрузки.АдресФайлаВыгрузки);
			Результат.Добавить(ОписательОтчета);
		Иначе
			Для Инд = 0 По СвойстваФайлаВыгрузки.Количество() - 1 Цикл
				Результат.Добавить(Новый Структура("Документ, Тип, Имя, Адрес", СвойстваФайлаВыгрузки[Инд].Ссылка,
																					ТипПоИдентификаторуТипаФайлаВыгрузки(СвойстваФайлаВыгрузки[Инд].ТипФайлаВыгрузки),
																					СвойстваФайлаВыгрузки[Инд].ИмяФайлаВыгрузки,
																					СвойстваФайлаВыгрузки[Инд].АдресФайлаВыгрузки));
			КонецЦикла;
		КонецЕсли;
		
		Контекст.Вставить("РезультатВыгрузки", Результат);
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);

	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВопросПродолжитьВыгрузкуРеглОтчетаПФРНеудача(Выбор, ДопПараметры) Экспорт
	
	РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", ДопПараметры.Контекст, Ложь);
	ВыполнитьОбработкуОповещения(ДопПараметры.ВыполняемоеОповещение, РезультатВыгрузки);
			
КонецПроцедуры

&НаКлиенте
Процедура ВопросПродолжитьВыгрузкуРеглОтчетаПФРЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Контекст = ДополнительныеПараметры.Контекст;
	Отчет = ДополнительныеПараметры.Отчет;
	Результат = ДополнительныеПараметры.Результат;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЭтоКомплектыРСВ1 = ДополнительныеПараметры.ЭтоКомплектыРСВ1;
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
	Иначе
		ВыгрузитьРегламентированныйОтчетПФРПродолжение(Контекст, Отчет, Результат, ВыполняемоеОповещение, ЭтоКомплектыРСВ1)
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетПФРПродолжение(Контекст, Отчет, Результат, ВыполняемоеОповещение, ЭтоКомплектыРСВ1)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура,
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// выгружаем отчет
	ТекстВыгрузки = "";
	ИмяФайлаВыгрузки = "";
	КодировкаФайлаВыгрузки = Неопределено;
	ЭлементыВыгрузки = Неопределено;
	РезультатВыгрузки = СдачаОтчетностиЧерезСервисСпецоператораКлиент.ВыгрузитьРегламентированныйОтчет(Отчет, Ложь, ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайлаВыгрузки, , ЭлементыВыгрузки, Контекст.УникальныйИдентификатор, ЭтоКомплектыРСВ1);
	Если НЕ РезультатВыгрузки Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = '%1Отправка отчета прервана.'"), ИнформацияОбОшибке().Описание + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		РезультатВыгрузкиПФР = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузкиПФР);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекстВыгрузки) И ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") И ИмяФайлаВыгрузки.Количество() = 1
		И ТипЗнч(ЭлементыВыгрузки) = Тип("Массив") И ЭлементыВыгрузки.Количество() > 0 И ТипЗнч(ЭлементыВыгрузки[0]) = Тип("Структура") Тогда
		
		АдресВоВременномХранилище = ЭлементыВыгрузки[0].АдресФайлаВыгрузки;
		
		ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес", Отчет, СтруктураСсылочныхДанных.Перечисления_ТипыФайловОтчетностиПФР_ПачкаРСВ, ИмяФайлаВыгрузки[0], АдресВоВременномХранилище);
		Результат.Добавить(ОписательОтчета);
		
	ИначеЕсли ЭтоКомплектыРСВ1 Тогда
		
		МассивТекстовВыгрузки = ?(ЗначениеЗаполнено(ТекстВыгрузки), ТекстВыгрузки, ЭлементыВыгрузки);
		МассивИменФайловВыгрузки = ИмяФайлаВыгрузки;
		
		Для ИндексВыгрузки = 0 По МассивТекстовВыгрузки.ВГраница() Цикл
			ТекстВыгрузки = МассивТекстовВыгрузки[ИндексВыгрузки];
			ИмяФайлаВыгрузки = МассивИменФайловВыгрузки[ИндексВыгрузки];
			
			// пишем файл во временное хранилище
			КодировкаФайла = "windows-1251";
			АдресВоВременномХранилище = ЗаписатьФайлВыгрузкиВоВременноеХранилище(ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайла, Контекст.УникальныйИдентификатор);
			
			// заполняем результат
			ОпределитьТипПоСодержимому = (ИндексВыгрузки <> 0);
			ТипыФайловОтчетностиПФР = ?(НЕ ОпределитьТипПоСодержимому, СтруктураСсылочныхДанных.Перечисления_ТипыФайловОтчетностиПФР_ПачкаРСВ, Неопределено);
			ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес, ОпределитьТипПоСодержимому", Отчет, ТипыФайловОтчетностиПФР, ИмяФайлаВыгрузки, АдресВоВременномХранилище, ОпределитьТипПоСодержимому);
			Результат.Добавить(ОписательОтчета);
		КонецЦикла;
		
	Иначе
		
		// пишем файл во временное хранилище
		КодировкаФайла = "windows-1251";
		АдресВоВременномХранилище = ЗаписатьФайлВыгрузкиВоВременноеХранилище(ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайла, Контекст.УникальныйИдентификатор);
		
		// заполняем результат
		ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес", Отчет, СтруктураСсылочныхДанных.Перечисления_ТипыФайловОтчетностиПФР_ПачкаРСВ, ИмяФайлаВыгрузки, АдресВоВременномХранилище);
		Результат.Добавить(ОписательОтчета);
		
	КонецЕсли;
	
	Контекст.Вставить("РезультатВыгрузки", Результат);
	РезультатВыгрузкиПФР = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузкиПФР);
	
	
КонецПроцедуры

&НаКлиенте
Процедура Отправка_СформироватьПакет(ОповещениеОЗавершении, ТранспортноеСообщение, КонтролирующийОрган, ПараметрыОперации = Неопределено)
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется формирование пакета для отправки..."); // ФНС, ПФР, ФСГС, СОС, ЦБ
	
	// готовим пакет
	Если КонтролирующийОрган = "ФНС" Тогда
		СформироватьПакетФНС(ОповещениеОЗавершении, ТранспортноеСообщение,, ПараметрыОперации);
	ИначеЕсли КонтролирующийОрган = "ПФР" Тогда
		СформироватьПакетПФР(ОповещениеОЗавершении, ТранспортноеСообщение,, ПараметрыОперации);
	ИначеЕсли КонтролирующийОрган = "ФСГС" Тогда
		СформироватьПакетФСГС(ОповещениеОЗавершении, ТранспортноеСообщение,, ПараметрыОперации);
	ИначеЕсли КонтролирующийОрган = "СОС" Тогда
		СформироватьПакетСОС(ОповещениеОЗавершении, ТранспортноеСообщение,, ПараметрыОперации);
	ИначеЕсли КонтролирующийОрган = "ЦБ" Тогда
		СформироватьПакетЦБ(ОповещениеОЗавершении, ТранспортноеСообщение,, ПараметрыОперации);
	КонецЕсли;
	
КонецПроцедуры

#Область ОтправкаРегламентированногоОтчетаВПФР

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФР(Док, ВыполняемоеОповещение) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Док",                   Док);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФРПослеПодтвержденияПовторнойОтправки",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ПодтвердитьПовторнуюОтправку(Док, "ПФР", ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФРПослеПодтвержденияПовторнойОтправки(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	
	// готовимся к выгрузке
	Если НЕ ОтправкаРегламентированногоОтчетаВПФР_ПередВыгрузкойФайла(КонтекстОтправки, Док, УникальныйИдентификатор) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаРегламентированногоОтчетаВПФР_ПослеПроверкиЗаявления",
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Ложь); //+
		Иначе
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР

		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Ложь); //+
		Возврат;
		
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	
	// выгружаем отчет и доверенность, при необходимости
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОтправкаРегламентированногоОтчетаВПФРПослеВыгрузки", ЭтотОбъект, ДополнительныеПараметры);
	ВыгрузитьРегламентированныйОтчетПФР(КонтекстОтправки, ОписаниеОповещения)// клиент
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФРПослеВыгрузки(Результат, ВходящийКонтекст) Экспорт
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
			
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	РезультатВыгрузки     = Результат.РезультатВыгрузки;
	КонтекстОтправки      = Результат.Контекст; // Обновленный по результатам выгрузки контекст.
	
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Если НЕ РезультатВыгрузки Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;

	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФРПослеПроверкиСоответствияОрганизации",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	РезультатВыгрузкиОтчетности = КонтекстОтправки.РезультатВыгрузки;
	
	Если КонтекстОтправки.ТипОтправляемогоДокумента = "ЭлектронныеПредставленияРегламентированныхОтчетов"
		И РезультатВыгрузкиОтчетности.Количество() > 0 Тогда
		
		РегНомерСФРИлиПФРФайла = ДокументооборотСКОКлиентСервер.ИзвлечьРегистрационныйНомерСФРИлиПФРИзИмениФайла(
			РезультатВыгрузкиОтчетности[0].Имя);
		РегНомерСФР = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(КонтекстОтправки, "РегНомерСФР");
		Если РегНомерСФРИлиПФРФайла <> Неопределено И РегНомерСФРИлиПФРФайла <> КонтекстОтправки.РегНомерПФР
			И РегНомерСФРИлиПФРФайла <> РегНомерСФР Тогда
			
			Если СтрДлина(СокрЛП(РегНомерСФРИлиПФРФайла)) = 10 И (ЗначениеЗаполнено(РегНомерСФР)
				ИЛИ НЕ ЗначениеЗаполнено(КонтекстОтправки.РегНомерПФР)) Тогда
				
				ТекстВопроса = НСтр("ru = 'Регистрационный номер СФР в имени файла отчета ""%1"" отличается
										  |от регистрационного номера СФР в свойствах организации ""%2"".'");
				ТекстВопроса = СтрШаблон(ТекстВопроса, РегНомерСФРИлиПФРФайла, РегНомерСФР);
				
			Иначе
				ТекстВопроса = НСтр("ru = 'Регистрационный номер ПФР в имени файла отчета ""%1"" отличается
										  |от регистрационного номера ПФР в свойствах организации ""%2"".'");
				
				ЭтоОтправкаВСФР = ДокументооборотСКОКлиентСервер.ЭтоОтправкаВСФР(
					КонтекстОтправки.Документ,,, ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР"));
				ТекстВопроса = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(ТекстВопроса, Истина,, НЕ ЭтоОтправкаВСФР);
				ТекстВопроса = СтрШаблон(ТекстВопроса, РегНомерСФРИлиПФРФайла, КонтекстОтправки.РегНомерПФР);
			КонецЕсли;
			
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(, Истина) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстВопроса);
				ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Нет);
				Возврат;
			Иначе
				
				ТекстВопроса = ТекстВопроса + Символы.ПС + НСтр("ru = 'Продолжить отправку?'");
				ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);
				Возврат;
		КонецЕсли;
	КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФРПослеПроверкиСоответствияОрганизации(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	КонтекстОтправки      = ВходящийКонтекст.КонтекстОтправки;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
		
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФРПослеПодтвержденияДляПеречня",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	// проверяем наличие перечня льготных профессий
	ПрефиксИмениФайлаПеречняЛьготныхПрофессий = "PERECH-";
	ДлинаПрефиксаИмениФайлаПеречняЛьготныхПрофессий = СтрДлина(ПрефиксИмениФайлаПеречняЛьготныхПрофессий);
	РезультатВыгрузкиОтчетности = КонтекстОтправки.РезультатВыгрузки;
	
	Для каждого СтрРезультатВыгрузкиОтчетности Из РезультатВыгрузкиОтчетности Цикл
		ПрефиксИмениОтчета = Лев(СтрРезультатВыгрузкиОтчетности.Имя, ДлинаПрефиксаИмениФайлаПеречняЛьготныхПрофессий);
		
		Если ВРег(ПрефиксИмениОтчета) = ПрефиксИмениФайлаПеречняЛьготныхПрофессий
			И (СтрРезультатВыгрузкиОтчетности.Тип <>
			ПредопределенноеЗначение("Перечисление.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР")
			ИЛИ РезультатВыгрузкиОтчетности.Количество() = 1) Тогда
			
			ТекстВопроса = НСтр("ru = 'Не рекомендуется отправка перечня льготных профессий в качестве основного документа.
									  |Выполнить отправку перечня льготных профессий можно в разделе ""Письма"" 1С-Отчетности, приложив файл к письму в ПФР.
									  |
									  |Продолжить отправку (отчет может быть не принят ПФР)?'");
			ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);
			Возврат;
		КонецЕсли;
	КонецЦикла;
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФРПослеПодтвержденияДляПеречня(Результат, ВходящийКонтекст) Экспорт
	
	Документ = ВходящийКонтекст.КонтекстОтправки.Документ;
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		Возврат;
		
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФР_ПослеПроверкиЗаключенногоСоглашенияСПФР", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	Организация = ВходящийКонтекст.КонтекстОтправки.Организация;
	ПроверитьЧтоЗаключеноСоглашениеСПФР(Документ, Организация, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФР_ПослеПроверкиЗаключенногоСоглашенияСПФР(Результат, ВходящийКонтекст) Экспорт
	
	Документ = ВходящийКонтекст.КонтекстОтправки.Документ;
	
	Если Результат <> Истина Тогда
		
		ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФРПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ВходящийКонтекст);
		
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Документ", Документ);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", ОписаниеОповещения);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Документ;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 7;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;	
		
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ПФР
		Возврат;
	КонецЕсли;
		
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаРегламентированногоОтчетаВПФРПослеВыгрузки",  0.1, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаРегламентированногоОтчетаВПФРПослеВыгрузки() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	Документ 					= КонтекстДлительнойОперации.Документ;
	ОписаниеОповещения 			= КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи..."); // ПФР
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Документ, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФРПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФР_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.КонтекстОтправки.Документ, ВходящийКонтекст.КонтекстОтправки.Организация);
	
КонецПроцедуры

&НаСервере
Функция ОтчетОтправляетсяПоЭДОК(Отчет) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтчетОтправляетсяПоЭДОК(Отчет);
	
КонецФункции

&НаКлиенте
Процедура ПроверитьЧтоЗаключеноСоглашениеСПФР(Отчет, Организация, ОписаниеЗавершения) Экспорт
	
	Состояние = СостояниеЭДОсПФР(Организация);
	
	НужноПредупредить = 
		ОтчетОтправляетсяПоЭДОК(Отчет)
		И НужноОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(Состояние, Истина)
		И НЕ ТипЗнч(Отчет) = Тип("ДокументСсылка.ЗаявленияПоЭлДокументооборотуСПФР");
		
	Если НужноПредупредить Тогда
		
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ОписаниеЗавершения)
			И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина) = 0 Тогда
			
			Ошибка = ДлительнаяОтправкаКлиентСервер.СообщениеНеПодключенаКЭДОПФР();
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
			
		Иначе
			ОткрытьФормуПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиОтправки(
				Состояние, 
				Организация,
				ОписаниеЗавершения,
				Отчет);
		КонецЕсли;
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ОписаниеЗавершения, Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФР_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура,
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	КонтекстОтправки      = ВходящийКонтекст.КонтекстОтправки;
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРегламентированногоОтчетаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР

		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", 	ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", 		КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВПФРПослеФормированияПакета", ЭтотОбъект, Контекст);
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ПФР");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправкаРегламентированногоОтчетаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВПФР_ПередВыгрузкойФайла(Контекст, Документ, УникальныйИдентификатор)
	
	ВыводитьСообщения = Ложь;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ТекстСообщения = "";
	Результат = КонтекстЭДОСервер.ОтправкаРегламентированногоОтчетаВПФР_ПередВыгрузкойФайла(Контекст, Документ, УникальныйИдентификатор, ТекстСообщения);
	
	Если ЗначениеЗаполнено(ТекстСообщения) И ВыводитьСообщения Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФР_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ОтправкаРегламентированногоОтчетаВПФР(Параметры_Док, Параметры_ВыполняемоеОповещение);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура СформироватьПечатныеДокументыПоНалоговойИлиБухгалтерскойОтчетности(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	Если Настройки.Свойство("ФорматДокументооборота") Тогда
		ФорматДокументооборота = Настройки.ФорматДокументооборота;
	КонецЕсли;
	
	ВсеВАрхиве = Неопределено;
	Если ФорматДокументооборота = СтруктураСсылочныхДанных["Перечисления_ФорматыДокументооборотаСФНС_Приказ534"] Тогда 
		ПолучитьФайлыЦиклаОбмена534(ЦиклОбмена, Настройки, ВсеВАрхиве);
	Иначе
		ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве);
	КонецЕсли;
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	// выводим электронное представление отчета (141 и 534)
	Если Настройки.ПечататьДокумент Тогда
		НастройкаПечататьДокумент(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// 534
	
	// выводим подтверждение даты отправки
	Если Настройки.ПечататьПодтверждениеДатыОтправки Тогда		
		ПечатныеДокументы_ПечататьПодтверждениеДатыОтправки_НалогБухОтчетность(Результат, НижнийКолонтитул);			
	КонецЕсли;
	
	// выводим результат приема
	Если Настройки.ПечататьРезультатПриема Тогда		
		ПечатныеДокументы_ПечататьРезультатПриема_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим результат обработки
	Если Настройки.ПечататьРезультатОбработки Тогда		
		ПечатныеДокументы_ПечататьРезультатОбработки_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим извещения о получении
	Если Настройки.ПечататьИзвещенияОПолучении Тогда
		ПечатныеДокументы_ПечататьИзвещенияОПолучении(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// 141
	
	// выводим подтверждение отправки
	Если Настройки.ПечататьПодтверждениеОтправки Тогда		
		ПечатныеДокументы_ПечататьПодтверждениеОтправки_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим протокол
	Если Настройки.ПечататьПротокол Тогда		
		ПечатныеДокументы_ПечататьПротокол_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим уведомление об уточнении
	Если Настройки.ПечататьУведомлениеОбУточнении Тогда		
		ПечатныеДокументы_ПечататьУведомлениеОбУточнении_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим протокол приема 2-НДФЛ
	Если Настройки.ПечататьПротоколПриема2НДФЛ Тогда		
		ПечатныеДокументы_ПечататьПротоколПриема2НДФЛ_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим реестр сведений 2-НДФЛ
	Если Настройки.ПечататьРеестрСведений2НДФЛ Тогда		
		ПечатныеДокументы_ПечататьРеестрСведений2НДФЛ_НалогБухОтчетность(Результат, НижнийКолонтитул);		
	КонецЕсли;
	
	// выводим отчет о состоянии
	Если Настройки.ПечататьСостояние Тогда		
		ПечатныеДокументы_ПечататьСостояние_НалогБухОтчетность(Результат, ЦиклОбмена)		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПечатныеДокументыПоЗаявлениям(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена534(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	// выводим электронное представление отчета
	Если Настройки.ПечататьДокумент Тогда
		ПечатныеДокументы_ПечататьДокумент_ПоЗаявлениям(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим подтверждение даты отправки
	Если Настройки.ПечататьПодтверждениеДатыОтправки Тогда
		ПечатныеДокументы_ПечататьПодтверждениеДатыОтправки_ПоЗаявлениям(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим результат приема
	Если Настройки.ПечататьРезультатПриема Тогда
		ПечатныеДокументы_ПечататьРезультатПриема_ПоЗаявлениям(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим результат обработки
	Если Настройки.ПечататьРезультатОбработки Тогда
		ПечатныеДокументы_ПечататьРезультатОбработки_ПоЗаявлениям(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим сообщение об отзыве
	Если Настройки.ПечататьСообщениеОбОтзыве Тогда
		ПечатныеДокументы_ПечататьСообщениеОбОтзыве_ПоЗаявлениям(Результат, НижнийКолонтитул);
	КонецЕсли;

	// выводим сообщение о несоответствиях
	Если Настройки.ПечататьСообщениеОНесоответствиях Тогда
		ПечатныеДокументы_ПечататьСообщениеОНесоответствиях_ПоЗаявлениям(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим отчет о состоянии
	Если Настройки.ПечататьСостояние Тогда
		ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
		Если ТаблДок <> Неопределено Тогда
			Результат.Добавить(ТаблДок, "Отчет о состоянии");
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);

КонецПроцедуры

&НаКлиенте
Процедура СформироватьПечатныеДокументыПоФорме2НДФЛ(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена534(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	// выводим электронное представление 2-НДФЛ
	Если Настройки.ПечататьДокумент Тогда
		ПечатныеДокументы_ПечататьДокумент_2НДФЛ(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим подтверждение даты отправки
	Если Настройки.ПечататьПодтверждениеДатыОтправки Тогда
		ПечатныеДокументы_ПечататьПодтверждениеДатыОтправки_2НДФЛ(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим результат приема
	Если Настройки.ПечататьРезультатПриема Тогда
		ПечатныеДокументы_ПечататьРезультатПриема_2НДФЛ(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	Если Настройки.ПечататьИзвещенияОПолучении Тогда
		ПечатныеДокументы_ПечататьИзвещенияОПолучении(Результат, НижнийКолонтитул);
	КонецЕсли;
	
	// выводим отчет о состоянии
	Если Настройки.ПечататьСостояние Тогда
		ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
		Если ТаблДок <> Неопределено Тогда
			Результат.Добавить(ТаблДок, "Отчет о состоянии");
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПечатныеДокументыПоОтчетностиПФР(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	// выводим электронное представление отчета
	Если Настройки.ПечататьДокумент Тогда
		ПечатныеДокументы_ПечататьДокумент_ОтчетПФР(Результат, НижнийКолонтитул, ЦиклОбмена);
	КонецЕсли;
	
	// выводим подтверждение получения
	Если Настройки.ПечататьИзвещениеОбОтказеПФР Тогда
		ПечатныеДокументы_ПечататьИзвещениеОбОтказеПФР_ОтчетПФР(Результат, НижнийКолонтитул, ЦиклОбмена);
	КонецЕсли;
	
	// выводим протокол
	Если Настройки.ПечататьПротокол Тогда
		ПечатныеДокументы_ПечататьПротокол_ОтчетПФР(Результат, НижнийКолонтитул, ЦиклОбмена);
	КонецЕсли;
	
	// выводим отчет о состоянии
	Если Настройки.ПечататьСостояние Тогда
		ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
		Если ТаблДок <> Неопределено Тогда
			Результат.Добавить(ТаблДок, "Отчет о состоянии");
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПечатныеДокументыПоОтчетностиФСГС(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	// выводим электронное представление отчета
	Если Настройки.ПечататьДокумент Тогда
		ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетностиФСГС"));
		Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл
			//Кодировка = ?(НРег(Прав(ФайлОтчетности.ИмяФайла, 4)) = ".xml", ?(ВРег(Лев(ФайлОтчетности.ИмяФайла, 6)) = "NO_BUH" ИЛИ ВРег(Лев(ФайлОтчетности.ИмяФайла, 6)) = "NO_BOU", "windows-1251", "utf-8"), "cp866");
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл,, Истина);	// Определение по имени заменено на авто
			ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеБланка("Электронное представление", ФайлОтчетности.ИмяФайла, Тело, НижнийКолонтитул);
			ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;
	
	// выводим подтверждение даты отправки
	Если Настройки.ПечататьПодтверждениеОтправки Тогда
		ПодтвержденияДатыОтправки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеДатыОтправкиФСГС"));
		Для Каждого ПодтверждениеДатыОтправки Из ПодтвержденияДатыОтправки Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ПодтверждениеДатыОтправки.Файл,, Истина);	// Фиксированная "windows-1251" заменена на авто
			ПолучитьЭЦП(Файлы.Индекс(ПодтверждениеДатыОтправки));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(ПодтверждениеДатыОтправки.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправкиФСГС("Подтверждение даты отправки", ПодтверждениеДатыОтправки.ИмяФайла, Тело, ЦиклОбмена);
			ПараметрыПечати.Отправка = ПодтверждениеДатыОтправки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;
	
	// выводим протокол
	Если Настройки.ПечататьПротокол Тогда
		Протоколы = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПротоколФСГС"));
		Для Каждого СтрПротокол Из Протоколы Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПротокол.Файл,, Истина);
			ПолучитьЭЦП(Файлы.Индекс(СтрПротокол));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПротокол.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеПротоколаФСГС("Протокол входного контроля", СтрПротокол.ИмяФайла, Тело, ЦиклОбмена);
			ПараметрыПечати.Отправка = СтрПротокол.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;
	
	// выводим подтверждение получения (ФСГС)
	Если Настройки.ПросмотрПодтвержденияПолученияФСГС Тогда
		Протоколы = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеПолученияФСГС"));
		Для Каждого СтрПротокол Из Протоколы Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПротокол.Файл,, Истина);
			ПолучитьЭЦП(Файлы.Индекс(СтрПротокол));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПротокол.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументПодтверждениеПолученияФСГС(Тело, ЦиклОбмена);
			ПараметрыПечати.Отправка = СтрПротокол.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;	
	
	// выводим отчет о состоянии
	Если Настройки.ПечататьСостояние Тогда
		ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
		Если ТаблДок <> Неопределено Тогда
			Результат.Добавить(ТаблДок, "Отчет о состоянии");
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
КонецПроцедуры

// Эта и следущие процедуры и ф-ции Перенесены из Модуля
// для использования формой "Предварительный просмотр печатных форм"
&НаКлиенте
Процедура СформироватьПечатныеДокументы(ЦиклОбмена, Настройки, ВыполняемоеОповещение) Экспорт
	
	Результат = Новый СписокЗначений;
	
	ТипЦиклаОбмена = ПолучитьРеквизитНаСервере(ЦиклОбмена, "Тип");
	Если ТипЦиклаОбмена.Пустая() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	Если Настройки.ПечататьСостояние Тогда
		
		ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
		Если ТаблДок <> Неопределено Тогда
			Результат.Добавить(ТаблДок, "Отчет о состоянии");
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_Заявление"] Тогда
		
		СформироватьПечатныеДокументыПоЗаявлениям(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_Форма2НДФЛ"] Тогда 
		
		СформироватьПечатныеДокументыПоФорме2НДФЛ(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_ОтчетностьПФР"] Тогда 
		
		 СформироватьПечатныеДокументыПоОтчетностиПФР(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		 
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_ИОС"] Тогда
		 
		СформироватьПечатныеДокументыИОС(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_НеформализованнаяПерепискаПФРИсходящие"]
		ИЛИ ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_НеформализованнаяПерепискаПФРВходящие"] Тогда
		
		СформироватьПечатныеДокументыПисьмаПФР(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
		
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_ОтчетностьФСГС"]
		ИЛИ ТипЦиклаОбмена = ПредопределенноеЗначение("Перечисление.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС") Тогда
		 
		СформироватьПечатныеДокументыПоОтчетностиФСГС(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		
	ИначеЕсли ТипЦиклаОбмена = СтруктураСсылочныхДанных["Перечисления_ТипыЦикловОбмена_ОтчетностьЦБ"] Тогда
		 
		СформироватьПечатныеДокументыПоОтчетностиЦБ(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		
	ИначеЕсли ПечатьВозможнаДляЦиклаОбмена(ТипЦиклаОбмена) Тогда
		
		СформироватьПечатныеДокументыПоНалоговойИлиБухгалтерскойОтчетности(ЦиклОбмена, Настройки, ВыполняемоеОповещение);
		
	ИначеЕсли НЕ ПечатьВозможнаДляЦиклаОбмена(ТипЦиклаОбмена) Тогда
		
		// выводим отчет о состоянии
		Если Настройки.ПечататьСостояние Тогда
			ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
			Если ТаблДок <> Неопределено Тогда
				Результат.Добавить(ТаблДок, "Отчет о состоянии");
			КонецЕсли;
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция печатьПредставлениеЦиклаОбмена(ЦиклОбмена, ДляКолонтитула = Истина) Экспорт
	
	ТипЦиклаОбмена = ЦиклОбмена.Тип;
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
		Результат = ?(ЦиклОбмена.ВидДокумента = 0, "Первичный", "Корректирующий (корр. №" + ЦиклОбмена.ВидДокумента + ")") + " отчет вида """ + ЦиклОбмена.ВидОтчета + """"
					+ ?(ДляКолонтитула, Символы.ПС, " ")
					+ "по организации """ + ЦиклОбмена.Организация
					+ """ за " + ПредставлениеПериода(НачалоДня(ЦиклОбмена.ДатаНачалаПериода), КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина")
					+ " в ИФНС " + ЦиклОбмена.ВнешняяОрганизация;
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		Результат = "2-НДФЛ"
					+ ?(ДляКолонтитула, Символы.ПС, " ")
					+ "по организации """ + ЦиклОбмена.Организация
					+ """ за " + ПредставлениеПериода(НачалоДня(ЦиклОбмена.ДатаНачалаПериода), КонецДня(ЦиклОбмена.ДатаОкончанияПериода), "ФП=Истина");
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана Тогда
		Результат = "Неформализованный документ налогового органа";
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика Тогда
		Предмет = ЦиклОбмена.Предмет;
		СвойстваДокумента = ПолучитьСвойстваНеформализованногоДокумента(Предмет);
		Если СвойстваДокумента <> Неопределено Тогда
			Результат = Строка(СвойстваДокумента.Тип);
		Иначе
			Результат = "Документ";
		КонецЕсли;
		Результат = Результат + " """ + СвойстваДокумента.ИмяФайла
					+ """ от организации """ + ЦиклОбмена.Организация
					+ """ в ИФНС " + ЦиклОбмена.ВнешняяОрганизация;
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
		Результат = "Запрос """ + ЦиклОбмена.ВидУслуги
					+ """ по организации """ + ЦиклОбмена.Организация
					+ """ за " + Формат(ЦиклОбмена.ДатаНачалаПериода, "ДФ='dd.MM.yyyy'") + " - " + Формат(ЦиклОбмена.ДатаОкончанияПериода, "ДФ='dd.MM.yyyy'");
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ИОН Тогда
		Результат = "Запрос """ + ЦиклОбмена.ВидУслуги
					+ """ по организации """ + ЦиклОбмена.Организация
					+ """ за " + Формат(ЦиклОбмена.ДатаНачалаПериода, "ДФ='dd.MM.yyyy'") + " - " + Формат(ЦиклОбмена.ДатаОкончанияПериода, "ДФ='dd.MM.yyyy'");
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ПисьмоНО Тогда
		Результат = "Вход. письмо от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Рассылка Тогда
		Результат = "Вход. рассылка от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая Тогда
		Результат = "Вход. групповая рассылка от НО " + ЦиклОбмена.ВнешняяОрганизация + " от " + ЦиклОбмена.ДатаСоздания;
	Иначе
		Результат = ПредставлениеЦиклаОбмена(ЦиклОбмена);
	КонецЕсли;
	Возврат Результат;
	
КонецФункции

&НаСервере
Процедура ПолучитьФайлыЦиклаОбмена534(ЦиклОбмена, Настройки, ВсеВАрхиве = Ложь)
	
	ВсеВАрхиве = Ложь;
	Файлы.Очистить();
	
	// выполняем запрос, сохраняем файлы во временный каталог и формируем таблицу файлов
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	ТаблицаЗапроса = КонтекстЭДОСервер.ПолучитьТаблицуФайловЦиклаОбмена534(ЦиклОбмена, Настройки);
	
	ТранспортныеСообщенияСПрисоединеннымиФайлами = Новый Массив;
	Для каждого СтрокаТаблицыЗапроса Из ТаблицаЗапроса Цикл
		Если ТранспортныеСообщенияСПрисоединеннымиФайлами.Найти(СтрокаТаблицыЗапроса.ТранспортноеСообщение) = Неопределено
			И СтрокаТаблицыЗапроса.Данные.Получить() = Неопределено Тогда
			
			ТранспортныеСообщенияСПрисоединеннымиФайлами.Добавить(СтрокаТаблицыЗапроса.ТранспортноеСообщение);
		КонецЕсли;
	КонецЦикла;
	
	ТаблицаФайлов = Неопределено;
	Если ТранспортныеСообщенияСПрисоединеннымиФайлами.Количество() > 0 Тогда
		ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			ТранспортныеСообщенияСПрисоединеннымиФайлами,
			"ТранспортноеСообщениеПрисоединенныеФайлы",
			,
			"ВладелецФайла, ИсходноеИмяФайла");
		
		Для каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
			СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
		КонецЦикла;
	КонецЕсли;
	
	ВсегоВАрхиве = 0;
	Для каждого СтрокаТаблицыЗапроса Из ТаблицаЗапроса Цикл
		Если СтрокаТаблицыЗапроса.ВАрхиве Тогда
			ВсегоВАрхиве = ВсегоВАрхиве + 1;
			Продолжить;
		КонецЕсли;
		ДвоичныеДанныеФайла = СтрокаТаблицыЗапроса.Данные.Получить();
		Если ДвоичныеДанныеФайла = Неопределено Тогда
			ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицыЗапроса.ИмяФайла);
			
			ПараметрыОтбора = Новый Структура("ВладелецФайла, ИсходноеИмяФайла",
				СтрокаТаблицыЗапроса.ТранспортноеСообщение, ИсходноеИмяФайла);
			СтрокиТаблицы = ТаблицаФайлов.НайтиСтроки(ПараметрыОтбора);
			Если СтрокиТаблицы.Количество() > 0 Тогда
				ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокиТаблицы[0].Ссылка);
			КонецЕсли;
		КонецЕсли;
		
		Если ДвоичныеДанныеФайла <> Неопределено Тогда
			ВременныйФайл = Base64Строка(ДвоичныеДанныеФайла);
			Если ВременныйФайл <> Неопределено Тогда
				НовСтр = Файлы.Добавить();
				ЗаполнитьЗначенияСвойств(НовСтр, СтрокаТаблицыЗапроса);
				НовСтр.Файл = ВременныйФайл;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если (ВсегоВАрхиве > 0) И (Файлы.Количество() = 0) Тогда
		ВсеВАрхиве = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве = Ложь)
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	
	ВсеВАрхиве = Ложь;
	Файлы.Очистить();
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ТаблицаЗапроса = КонтекстЭДОСервер.ПолучитьТаблицуФайловЦиклаОбмена(ЦиклОбмена, Настройки);
	
	ТранспортныеСообщенияСПрисоединеннымиФайлами = Новый Массив;
	ТаблицаФайлов = Неопределено;
	ВсегоВАрхиве = 0;
	Если ТаблицаЗапроса <> Неопределено Тогда 
		Для Каждого СтрокаТаблицыЗапроса Из ТаблицаЗапроса Цикл
			Если ТранспортныеСообщенияСПрисоединеннымиФайлами.Найти(СтрокаТаблицыЗапроса.ТранспортноеСообщение) = Неопределено
				И СтрокаТаблицыЗапроса.Данные.Получить() = Неопределено Тогда
				
				ТранспортныеСообщенияСПрисоединеннымиФайлами.Добавить(СтрокаТаблицыЗапроса.ТранспортноеСообщение);
			КонецЕсли;
		КонецЦикла;
		
		Если ТранспортныеСообщенияСПрисоединеннымиФайлами.Количество() > 0 Тогда
			ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
				ТранспортныеСообщенияСПрисоединеннымиФайлами,
				"ТранспортноеСообщениеПрисоединенныеФайлы",
				,
				"ВладелецФайла, ИсходноеИмяФайла");
			
			Для Каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
				СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
			КонецЦикла;
		КонецЕсли;
		
		Для Каждого СтрокаТаблицыЗапроса Из ТаблицаЗапроса Цикл
			Если СтрокаТаблицыЗапроса.ВАрхиве Тогда
				ВсегоВАрхиве = ВсегоВАрхиве + 1;
				Продолжить;
			КонецЕсли;
			
			ДвоичныеДанныеФайла = СтрокаТаблицыЗапроса.Данные.Получить();
			Если ДвоичныеДанныеФайла = Неопределено Тогда
				ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицыЗапроса.ИмяФайла);
				
				ПараметрыОтбора = Новый Структура("ВладелецФайла, ИсходноеИмяФайла",
					СтрокаТаблицыЗапроса.ТранспортноеСообщение, ИсходноеИмяФайла);
				СтрокиТаблицы = ТаблицаФайлов.НайтиСтроки(ПараметрыОтбора);
				Если СтрокиТаблицы.Количество() > 0 Тогда
					ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокиТаблицы[0].Ссылка);
				КонецЕсли;
			КонецЕсли;
			
			Если ДвоичныеДанныеФайла <> Неопределено Тогда
				ВременныйФайл = Base64Строка(ДвоичныеДанныеФайла);
				Если ВременныйФайл <> Неопределено Тогда
					НовСтр = Файлы.Добавить();
					ЗаполнитьЗначенияСвойств(НовСтр, СтрокаТаблицыЗапроса);
					НовСтр.Файл = ВременныйФайл;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если (ВсегоВАрхиве > 0) И (Файлы.Количество() = 0) Тогда
		ВсеВАрхиве = Истина;
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ПолучитьЭЦП(ИсходныйФайлИД)
	
	ИсходныйФайл = Файлы.Получить(ИсходныйФайлИД);
	
	// инициализируем таблицу возможных подписей и их свойств
	КартаДокументовИПодписей = ИнициализироватьКартуДокументовИПодписей();
	
	ТаблицаПодписей.Очистить();
	ПодписиАбонента.Очистить();
	ПодписиКонтрагента.Очистить();
	
	// находим строку, соответствующую типу подписанного файла, во вспомогательной таблице
	СтрОпределениеТипа = КартаДокументовИПодписей.Найти(ИсходныйФайл.ИдТипа, "ИдТипа");
	Если СтрОпределениеТипа = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// определяем подписи сначала абонента, а затем сторонней организации
	Если Файлы.НайтиСтроки(Новый Структура("ИмяПодписанногоФайла", "")).Количество() <> Файлы.Количество() Тогда 
		ПодписиАбонентаМассив = Файлы.НайтиСтроки(Новый Структура("ИдТипа, ИмяПодписанногоФайла", СтрОпределениеТипа.ИдПодписиНалогоплательщика, ИсходныйФайл.ИмяФайла));
		ПодписиКонтрагентаМассив = Файлы.НайтиСтроки(Новый Структура("ИдТипа, ИмяПодписанногоФайла", СтрОпределениеТипа.ИдПодписиКонтрагента, ИсходныйФайл.ИмяФайла));
		Если ПодписиКонтрагентаМассив.Количество() = 0 Тогда
			ПодписиКонтрагентаМассив = Файлы.НайтиСтроки(Новый Структура("ИдТипа", СтрОпределениеТипа.ИдПодписиКонтрагента));
		КонецЕсли;
	Иначе
		
		ПодписиАбонентаМассив = Файлы.НайтиСтроки(Новый Структура("ИдТипа", СтрОпределениеТипа.ИдПодписиНалогоплательщика));
		Если ПодписиАбонентаМассив.Количество() = 0 И ЗначениеЗаполнено(СтрОпределениеТипа.ИдПодписиНалогоплательщика2) Тогда
			ПодписиАбонентаМассив = Файлы.НайтиСтроки(Новый Структура("ИдТипа", СтрОпределениеТипа.ИдПодписиНалогоплательщика2));
		КонецЕсли;
		
		ПодписиКонтрагентаМассив = Файлы.НайтиСтроки(Новый Структура("ИдТипа", СтрОпределениеТипа.ИдПодписиКонтрагента));
		
	КонецЕсли;
	
	// "конвертируем" значение для передачи
	Для Каждого ПодписьАбонента Из ПодписиАбонентаМассив Цикл 
		НоваяСтрока = ПодписиАбонента.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, ПодписьАбонента);
	КонецЦикла;
		
	Для Каждого ПодписьКонтрагента Из ПодписиКонтрагентаМассив Цикл
		НоваяСтрока = ПодписиКонтрагента.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, ПодписьКонтрагента);
	КонецЦикла;
	
	ПоместитьПодписиВТаблицу();
	
КонецПроцедуры

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеБланка(
		Заголовок,
		ИмяФайла,
		Тело,
		НижнийКолонтитул,
		ЦиклОбмена = Неопределено,
		ТипыСообщений = Неопределено)
	
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
	ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
	
	ТаблДок.Вывести(Секция_Разделитель);
	
	Если ЦиклОбмена = Неопределено ИЛИ ТипыСообщений = Неопределено Тогда
		СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);
		
	Иначе
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		КонтекстЭДОСервер.ДобавитьШтампПодписиПодДокументом(
			ЦиклОбмена.Предмет,
			ТипыСообщений,
			ТаблДок,
			2,
			Ложь);
	КонецЕсли;
	
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправки(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияДатыОтправки(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	// если текст уведомления разобрать удалось, то выводим основную информацию из него
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ПодтверждениеДатыОтправки");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеДатыОтправки");
		
		ОбластьМакета.Параметры.Отправитель = СвойстваИзПодтверждения.ДанныеФайлаОтправки.ПредставлениеОтпр;
		ОбластьМакета.Параметры.СОС = СвойстваИзПодтверждения.ПодтвердившаяОрганизация;
		ОбластьМакета.Параметры.Дата = СвойстваИзПодтверждения.ДанныеФайлаОтправки.ДатаОтправки;
		ОбластьМакета.Параметры.Время = СтрЗаменить(СвойстваИзПодтверждения.ДанныеФайлаОтправки.ВремяОтправки,".",":");
		ОбластьМакета.Параметры.НаименованиеФайла = ВРег(СвойстваИзПодтверждения.ИмяФайла);
		ОбластьМакета.Параметры.ЭлектронныйАдресОтправителя = СвойстваИзПодтверждения.ДанныеФайлаОтправки.EmailОтправитель;
		ОбластьМакета.Параметры.ЭлектронныйАдресПолучателя = СвойстваИзПодтверждения.ДанныеФайлаОтправки.EmailПолучатель;
		ОбластьМакета.Параметры.КодНалоговогоОргана = СвойстваИзПодтверждения.ДанныеФайлаОтправки.КодНО;
		
		ТабДокумент.Вывести(ОбластьМакета);//ПодтверждениеДатыОтправки
		
		// выводим информацию о подписях
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
	Иначе
		
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправкиПредставление(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияДатыОтправкиПредставление(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	// если текст уведомления разобрать удалось, то выводим основную информацию из него
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ПодтверждениеДатыОтправкиПредставление");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеДатыОтправкиПредставление");
		
		ОбластьМакета.Параметры.Отправитель = СвойстваИзПодтверждения.ДанныеФайлаОтправки.ПредставлениеОтпр;
		ОбластьМакета.Параметры.СОС = СвойстваИзПодтверждения.ПодтвердившаяОрганизация;
		ОбластьМакета.Параметры.Дата = СвойстваИзПодтверждения.ДанныеФайлаОтправки.ДатаОтправки;
		ОбластьМакета.Параметры.Время = СтрЗаменить(СвойстваИзПодтверждения.ДанныеФайлаОтправки.ВремяОтправки,".",":");
		ОбластьМакета.Параметры.НаименованиеФайла = ВРег(СвойстваИзПодтверждения.ИмяФайла);
		ОбластьМакета.Параметры.ЭлектронныйАдресОтправителя = СвойстваИзПодтверждения.ДанныеФайлаОтправки.EmailОтправитель;
		ОбластьМакета.Параметры.ЭлектронныйАдресПолучателя = СвойстваИзПодтверждения.ДанныеФайлаОтправки.EmailПолучатель;
		ОбластьМакета.Параметры.НаименованиеИКодНалоговогоОргана = СвойстваИзПодтверждения.ДанныеФайлаОтправки.НаимНО + ", " + НСтр("ru = 'код'") + " " + СвойстваИзПодтверждения.ДанныеФайлаОтправки.КодНО;
		
		ТабДокумент.Вывести(ОбластьМакета);//ПодтверждениеДатыОтправкиПредставление
		
		// выводим информацию о подписях
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
	Иначе
		
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеКвитанцииОПриеме(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзКвитанции = ПолучитьОсновныеСвойстваИзКвитанцииОПриеме(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзКвитанции) Тогда
		
		Бланк = ПолучитьМакетОбработки("КвитанцияОПриеме");
		ТабДокумент.АвтоМасштаб = Истина;
		ОсновнаяЧастьБланка = Бланк.ПолучитьОбласть("КвитанцияОПриеме");
		ЗаполнитьЗначенияСвойств(ОсновнаяЧастьБланка.Параметры, СвойстваИзКвитанции);
		ТабДокумент.Вывести(ОсновнаяЧастьБланка);
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Бланк, ТаблицаПодписей);
		
	Иначе
		
		// если разобрать не удалось - выводим его в виде текста
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
	
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеУведомленияОбУточнении(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// пытаемся разобрать уведомление
	СвойстваИзУведомления = ПолучитьОсновныеСвойстваИзУведомления(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзУведомления) Тогда
		Макет = ПолучитьМакетОбработки("УведомлениеОбУточнении");
		
		ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбУточненииШапка");
		ОбластьМакета.Параметры.КодНалоговогоОргана = СвойстваИзУведомления.ОбщиеСведения.КодНо;
		ОбластьМакета.Параметры.НаименованиеФайла = ВРег(СвойстваИзУведомления.ОбщиеСведения.ИмяОбрабФайла);
		ОбластьМакета.Параметры.Налогоплательщик = СвойстваИзУведомления.ПредставлениеНП;
		ОбластьМакета.Параметры.Отправитель = СвойстваИзУведомления.ПредставлениеОтпр;
		
		ОбластьМакета.Параметры.НаименованиеДекларации = СформироватьТекстовуюИнформациюОДекларации(СвойстваИзУведомления.ОбщиеСведения);
		
		// выводим информацию о основной ошибке
		Если СвойстваИзУведомления.ОбщиеСведения.Свойство("КодОшОб") Тогда
			КодОшибки = СвойстваИзУведомления.ОбщиеСведения.КодОшОб;
		Иначе
			КодОшибки = "-";
		КонецЕсли;
		
		ТекстОшибки = "Код: " + КодОшибки + " Описание:" + СвойстваИзУведомления.ОбщиеСведения.ТекстОшОб;
		
		ОбластьМакета.Параметры.ОшибкаОбщая = ТекстОшибки;
		
		ТабДокумент.Вывести(ОбластьМакета); // УведомлениеОбУточненииШапка
		
		// выводим информацию о ошибках в файлах
		ИмяФайла = "";
		ВыводимыйПоКаждомуФайлуТекст = "";
		
		Если СвойстваИзУведомления.Ошибки.Количество() > 0 Тогда
			
			Для Каждого Ошибка Из СвойстваИзУведомления.Ошибки Цикл
				
				
				Если ПустаяСтрока(Ошибка.КодОшибки) тогда Ошибка.КодОшибки = "-" КонецЕсли;
				Если ПустаяСтрока(Ошибка.Описание) тогда Ошибка.Описание = "-" КонецЕсли;
				
				ТекстОшибки = "Код: " + Ошибка.КодОшибки + " Описание:" + Ошибка.Описание;
						
				Если Ошибка.ИмяФайла = ИмяФайла Тогда
					
					ВыводимыйПоКаждомуФайлуТекст = ВыводимыйПоКаждомуФайлуТекст + Символы.ПС + ТекстОшибки;
					
				Иначе
					
					Если НЕ ПустаяСтрока(ВыводимыйПоКаждомуФайлуТекст) Тогда
						ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбУточненииОшибки");
						ОбластьМакета.Параметры.ОшибкаФайл = ВыводимыйПоКаждомуФайлуТекст;
						ТабДокумент.Вывести(ОбластьМакета); 
					КонецЕсли;
					
					ИмяФайла = Ошибка.ИмяФайла;
					ВыводимыйПоКаждомуФайлуТекст = "В файле " + ИмяФайла + " обнаружены ошибки" + Символы.ПС + ТекстОшибки;
				КонецЕсли;
				
			КонецЦикла;
			
			Если НЕ ПустаяСтрока(ВыводимыйПоКаждомуФайлуТекст) Тогда
					ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбУточненииОшибки");
					ОбластьМакета.Параметры.ОшибкаФайл = ВыводимыйПоКаждомуФайлуТекст;
					ТабДокумент.Вывести(ОбластьМакета); 
			КонецЕсли;
			
		КонецЕсли;
		
		// выводим информацию о рекомендациях
		Если СвойстваИзУведомления.Рекомендации.Количество() > 0 Тогда
			
			ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбУточненииРекомендацииШапка");
			ТабДокумент.Вывести(ОбластьМакета); 
			
			Для Каждого Рекомендация Из СвойстваИзУведомления.Рекомендации Цикл
				ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбУточненииРекомендации");
				ОбластьМакета.Параметры.Рекомендация = Рекомендация;
				ТабДокумент.Вывести(ОбластьМакета); 
			КонецЦикла;
			
		КонецЕсли;
		
		Если СвойстваИзУведомления.ТаблицаПереченьСведений.Количество() > 0 Тогда
			ОбластьМакета = Макет.ПолучитьОбласть("ОбщаяИнформацияОПеречнеСведений");
			ТабДокумент.Вывести(ОбластьМакета);
		КонецЕсли;
		
		ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбУточненииПодвал");
		НаименованиеНалоговогоОргана = "";
		Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
		                      |	НалоговыеОрганы.Наименование
		                      |ИЗ
		                      |	Справочник.НалоговыеОрганы КАК НалоговыеОрганы
		                      |ГДЕ
		                      |	НалоговыеОрганы.Код = &КодНалоговогоОргана");
		Запрос.УстановитьПараметр("КодНалоговогоОргана", СвойстваИзУведомления.ОбщиеСведения.КодНо);
		Выборка = Запрос.Выполнить().Выбрать();
		Если Выборка.Следующий() Тогда
			НаименованиеНалоговогоОргана = Выборка.Наименование;
		КонецЕсли;
		Если ЗначениеЗаполнено(НаименованиеНалоговогоОргана) Тогда
			ОбластьМакета.Параметры.НаименованиеИКодНалоговогоОргана = НаименованиеНалоговогоОргана + ", " + НСтр("ru = 'код'") + " " + СвойстваИзУведомления.ОбщиеСведения.КодНо;
		Иначе
			ОбластьМакета.Параметры.НаименованиеИКодНалоговогоОргана = НСтр("ru = 'ФНС'") + " " + СвойстваИзУведомления.ОбщиеСведения.КодНо;
		КонецЕсли;
		
		ТабДокумент.Вывести(ОбластьМакета); // УведомлениеОбУточненииПодвал
		
		НапечататьПереченьСведенийУведомления(ТабДокумент, Макет, СвойстваИзУведомления);
		
		НапечататьДоходыИНалогиФЛУведомления(ТабДокумент, Макет, СвойстваИзУведомления);
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	Иначе
		
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, Подписи)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, Подписи);
	
КонецФункции

&НаСервере
Функция СформироватьТекстовуюИнформациюОДекларации(СведенияОДекларации)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьТекстовуюИнформациюОДекларации(СведенияОДекларации); 
		
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеИзвещенияОВводе(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзИзвещения = ПолучитьОсновныеСвойстваИзИзвещенияОВводе(Тело);
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзИзвещения) Тогда
		
		ТабДокумент = ТабДокументИзвещенияОВводе(СвойстваИзИзвещения);
		
	Иначе
		
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция ТабДокументИзвещенияОВводе(СвойстваИзИзвещения)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТабДокументИзвещенияОВводе(СвойстваИзИзвещения, ТаблицаПодписей);
	
КонецФункции

Функция ПолучитьОсновныеСвойстваИзИзвещенияОВводе(СтрУведомление) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОсновныеСвойстваИзИзвещенияОВводе(СтрУведомление);
	
КонецФункции

&НаСервере
Функция ВывестиОтчетОСостоянии(ЦиклОбмена)
	
	Если ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР Тогда
		Возврат ВывестиОтчетОСостоянииПФР(ЦиклОбмена);
	ИначеЕсли ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС Тогда
		Возврат ВывестиОтчетОСостоянииФСГС(ЦиклОбмена);
	Иначе
		Возврат ВывестиОтчетОСостоянииФНС(ЦиклОбмена);
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ВывестиОтчетОСостоянииПФР(ЦиклОбмена)
	
	ТипЦиклаОбмена = ЦиклОбмена.Тип;
	
	СоответствиеТипаСообщенияКраткомуПредставлению = ИнициализироватьСоответствиеТСКП();
	
	ПредусмотренныеТипыСообщений = Новый ТаблицаЗначений;
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипСообщения");
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипЦиклаОбмена");
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР, Перечисления.ТипыЦикловОбмена.ОтчетностьПФР);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР, Перечисления.ТипыЦикловОбмена.ОтчетностьПФР);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПротоколПФР, Перечисления.ТипыЦикловОбмена.ОтчетностьПФР);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР, Перечисления.ТипыЦикловОбмена.ОтчетностьПФР);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР, Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР, Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР, Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР, Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРВходящие);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР, Перечисления.ТипыЦикловОбмена.РассылкаПФР);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР, Перечисления.ТипыЦикловОбмена.ИОС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР, Перечисления.ТипыЦикловОбмена.ИОС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР, Перечисления.ТипыЦикловОбмена.ИОС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР, Перечисления.ТипыЦикловОбмена.ИОС);
	
	// получаем шаблон бланка
	ШаблонМакета = ПолучитьМакетОбработки("БланкПечатиОтчетаОСостоянииПФР");
	
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	ТаблДок.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
	ЗаполнитьЗначенияСвойств(ТаблДок.НижнийКолонтитул, ШаблонМакета.НижнийКолонтитул);
	ЗаполнитьЗначенияСвойств(ТаблДок.ВерхнийКолонтитул, ШаблонМакета.ВерхнийКолонтитул);
	// устанавливать колонтитулы
	
	// получаем области-строки
	ОблШапка = ШаблонМакета.ПолучитьОбласть("Шапка");
	ОблСтрока = ШаблонМакета.ПолучитьОбласть("Строка");
	ОблПодвалСообщения = ШаблонМакета.ПолучитьОбласть("ПодвалСообщения");
	ОблШапкаКритическиеОшибки = ШаблонМакета.ПолучитьОбласть("ШапкаУведомленияОбОшибках");
	ОблСтрокаКритическаяОшибка = ШаблонМакета.ПолучитьОбласть("СтрокаУведомлениеОбОшибке");
	ОблКритическиеОшибкиОтсутствуют = ШаблонМакета.ПолучитьОбласть("УведомленияОбОшибкахОтсутствуют");
	ОблПодвал = ШаблонМакета.ПолучитьОбласть("Подвал");
	
	// инициализируем значения шапки
	ОблШапка.Области.Идентификатор.Текст = ЦиклОбмена.Идентификатор;
	ОблШапка.Области.Организация.Текст = ЦиклОбмена.Организация;
	ОблШапка.Области.ВнешняяОрганизация.Текст = ЦиклОбмена.ВнешняяОрганизация;
	
	// формируем строку с предметом
	СтрПредмет = "";
	ПредметыПереписки = ПолучитьПредметыЦиклаОбмена(ЦиклОбмена);
	Для Каждого ПредметПереписки Из ПредметыПереписки Цикл
		Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОтчетностьПФР Тогда
			Если ЗначениеЗаполнено(ПредметПереписки) Тогда
				СтрПредмет = СтрПредмет + Символы.ПС + ПредставлениеОтчетаПФР(ПредметПереписки);
			КонецЕсли;
		ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ИОС Тогда
			СтрПредмет = СтрПредмет + Символы.ПС + Строка(ПредметПереписки);
		Иначе
			Если ЗначениеЗаполнено(ПредметПереписки) Тогда
				СтрПредмет = СтрПредмет + Символы.ПС + ПредставлениеПредметаПФР(ПредметПереписки);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	Если СтрПредмет = "" Тогда
		СтрПредмет = "<Отсутствует>";
	Иначе
		СтрПредмет = СокрЛП(СтрПредмет);
	КонецЕсли;
	
	// присваиваем текстовое представление предмета
	ОблШапка.Области.ПредметПереписки.Текст = СтрПредмет;
	
	ОблШапка.Области.Создан.Текст = Формат(ЦиклОбмена.ДатаСоздания, "ДЛФ=DDT");
	ОблШапка.Области.Завершен.Текст = ?(НЕ ЗначениеЗаполнено(ЦиклОбмена.ДатаЗакрытия) ИЛИ ЦиклОбмена.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияЦиклаОбмена(), "<не завершен>", Формат(ЦиклОбмена.ДатаЗакрытия, "ДЛФ=DDT"));
	ОблШапка.Области.ПоследнееСообщение.Текст = Формат(ЦиклОбмена.ДатаПоследнегоСообщения, "ДЛФ=DDT");
	
	ТаблДок.Вывести(ОблШапка);
	
	// выбираем информацию по сообщениям цикла обмена
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортныеСообщения.Тип КАК Тип,
						  |	ТранспортныеСообщения.ДатаТранспорта,
						  |	ТранспортныеСообщения.Статус КАК СтатусСообщения,
						  |	ТранспортныеСообщения.ПометкаУдаления,
						  |	ТранспортныеСообщения.ПротоколСОшибкой КАК ПротоколСОшибкой,
						  |	ТранспортныеСообщения.Ссылка КАК Ссылка,
						  |	ТранспортныеСообщения.Тема КАК Тема,
						  |	НерасшифрованныеВложения.КоличествоСообщений КАК КоличествоНерасшифрованных,
						  |	РасшифрованныеВложения.КоличествоСообщений КАК КоличествоРасшифрованных,
						  |	ТранспортныеСообщения.Дата КАК Дата
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |		ГДЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение) КАК НерасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = НерасшифрованныеВложения.ТранспортноеСообщение
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |		ГДЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение) КАК РасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = РасшифрованныеВложения.ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортныеСообщения.ЦиклОбмена = &ЦиклОбмена
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортныеСообщения.ЦиклОбмена,
						  |	ТранспортныеСообщения.Тип.Порядок,
						  |	Дата");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	ТаблицаСообщений = Запрос.Выполнить().Выгрузить();
	
	ПоследнийТипСообщения = Неопределено;
	
	Для ИндТипСообщения = 1 По ПредусмотренныеТипыСообщений.Количество() Цикл
		
		ТекСтр = ПредусмотренныеТипыСообщений[ИндТипСообщения - 1];
		Если ТекСтр.ТипЦиклаОбмена <> ТипЦиклаОбмена Тогда
			Продолжить;
		КонецЕсли;
		
		ТекТипСообщения = ТекСтр.ТипСообщения;
		СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТекТипСообщения));
		
		Если СообщенияТекТипа.Количество() = 0 Тогда
			
			ОблСтрока.Области.ТипСообщения.Текст = СоответствиеТипаСообщенияКраткомуПредставлению[ТекТипСообщения];
			ОблСтрока.Области.СтатусСообщения.Текст = "Отсутствует";
			ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , Ложь);
			
			ТаблДок.Вывести(ОблСтрока);
			
		Иначе
			
			Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
				
				ТипСообщения = СтрСообщение.Тип;
				ДатаТранспортаСообщения = СтрСообщение.ДатаТранспорта;
				СтатусСтрСообщения = СтрСообщение.СтатусСообщения;
				ПометкаУдаленияСообщения = СтрСообщение.ПометкаУдаления;
				ЭтоПротоколСОшибкой = СтрСообщение.ПротоколСОшибкой;
				Сообщение = СтрСообщение.Ссылка;
				ЧислоКонтейнеровСообщения = СтрСообщение.КоличествоНерасшифрованных;
				ЧислоЭлементовСодержимого = СтрСообщение.КоличествоРасшифрованных;
				ДатаСообщения = СтрСообщение.Дата;
				
				ПредставлениеТипа = СоответствиеТипаСообщенияКраткомуПредставлению[ТипСообщения];
				Если ПредставлениеТипа = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				// заполняем ТипСообщения
				Если ПоследнийТипСообщения <> ТипСообщения Тогда
					ОблСтрока.Области.ТипСообщения.Текст = ПредставлениеТипа;
				Иначе
					ОблСтрока.Области.ТипСообщения.Текст = "";
				КонецЕсли;
				ПоследнийТипСообщения = ТипСообщения;
				
				// заполняем строку статуса
				Если СтатусСтрСообщения = Перечисления.СтатусыПисем.Полученное Тогда
					ТекстСтатуса = "От " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Принято " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
					Если ЧислоЭлементовСодержимого = 0 Тогда
						ТекстСтатуса = ТекстСтатуса + " Не расшифровано.";
					КонецЕсли;
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Исходящее Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Не отправлено.";
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Отправленное Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Отправлено " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
				Иначе
					Продолжить;
				КонецЕсли;
				
				Если ЭтоПротоколСОшибкой Тогда
					ТекстСтатуса = ТекстСтатуса + Символы.ПС + "Содержит сведения об ошибках!";
				КонецЕсли;
				
				ОблСтрока.Области.СтатусСообщения.Текст = ТекстСтатуса;
				ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , ПометкаУдаленияСообщения);
				
				ТаблДок.Вывести(ОблСтрока);
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвалСообщения);
	
	// вывод информации об ошибках
	ТипыОшибок = Новый Массив;
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР);
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	Для Каждого ТипОшибки Из ТипыОшибок Цикл
		СообщенияОшибки = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТипОшибки));
		Если СообщенияОшибки.Количество() > 0 Тогда
			ТаблДок.Вывести(ОблШапкаКритическиеОшибки);
			Для Каждого СообщениеОшибка Из СообщенияОшибки Цикл
				ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибке.Текст = СокрЛП(СообщениеОшибка.Тема);
				ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибкеДата.Текст = Формат(СообщениеОшибка.ДатаТранспорта, "ДЛФ=DDT");
				ТаблДок.Вывести(ОблСтрокаКритическаяОшибка);
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвал);
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция ВывестиОтчетОСостоянииФНС(ЦиклОбмена)
	
	ФорматДокументооборота = ЦиклОбмена.ФорматДокументооборота;
	Если ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534 Тогда
		Возврат ВывестиОтчетОСостоянииФНС534(ЦиклОбмена);
	Иначе
		Возврат ВывестиОтчетОСостоянииФНС141(ЦиклОбмена);
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ВывестиОтчетОСостоянииФНС141(ЦиклОбмена)
	
	ТипЦиклаОбмена = ЦиклОбмена.Тип;
	
	СоответствиеТипаСообщенияКраткомуПредставлению = ИнициализироватьСоответствиеТСКП();
	
	ПредусмотренныеТипыСообщений = Новый ТаблицаЗначений;
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипСообщения");
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипЦиклаОбмена");
	
	// получаем шаблон бланка
	ШаблонМакета = ПолучитьМакетОбработки("БланкПечатиОтчетаОСостоянииФНС");
	
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	ТаблДок.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
	ЗаполнитьЗначенияСвойств(ТаблДок.НижнийКолонтитул, ШаблонМакета.НижнийКолонтитул);
	ЗаполнитьЗначенияСвойств(ТаблДок.ВерхнийКолонтитул, ШаблонМакета.ВерхнийКолонтитул);
	// устанавливать колонтитулы
	
	// получаем области-строки
	ОблШапка = ШаблонМакета.ПолучитьОбласть("Шапка");
	ОблСтрока = ШаблонМакета.ПолучитьОбласть("Строка");
	ОблПодвалСообщения = ШаблонМакета.ПолучитьОбласть("ПодвалСообщения");
	ОблШапкаКритическиеОшибки = ШаблонМакета.ПолучитьОбласть("ШапкаКритическиеОшибки");
	ОблСтрокаКритическаяОшибка = ШаблонМакета.ПолучитьОбласть("СтрокаКритическаяОшибка");
	ОблКритическиеОшибкиОтсутствуют = ШаблонМакета.ПолучитьОбласть("КритическиеОшибкиОтсутствуют");
	ОблПодвал = ШаблонМакета.ПолучитьОбласть("Подвал");
	
	// инициализируем значения шапки
	ОблШапка.Области.Идентификатор.Текст = ЦиклОбмена.Идентификатор;
	ОблШапка.Области.Организация.Текст = ЦиклОбмена.Организация;
	ОблШапка.Области.ВнешняяОрганизация.Текст = ЦиклОбмена.ВнешняяОрганизация;
	
	// формируем строку с предметом
	Предмет = ЦиклОбмена.Предмет;
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность Тогда
	
		Если ТипЗнч(Предмет) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
			СтрПредмет = "Отчет " + РегламентированнаяОтчетностьКлиентСервер.ПредставлениеДокументаРеглОтч(Предмет);
		ИначеЕсли ЗначениеЗаполнено(Предмет) Тогда
			СтрПредмет = "Внешние отчет:" + Символы.ПС + Строка(Предмет);
		Иначе
			СтрПредмет = "<Отсутствует>";
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалогоплательщика Тогда
		
		СтрПредмет = Строка(Предмет);
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НеформализованныеДокументыНалоговогоОргана Тогда
		
		Если ЗначениеЗаполнено(Предмет) тогда
			СтрПредмет = Строка(Предмет);
		Иначе
			СтрПредмет = "<Предмет будет создан в момент расшифровки первичного сообщения, содержащего документ налогового органа>";
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
		
		СтрПредмет = Строка(Предмет);
		
	КонецЕсли;
		
	ОблШапка.Области.ПредметПереписки.Текст = СтрПредмет;
	
	ОблШапка.Области.Создан.Текст = Формат(ЦиклОбмена.ДатаСоздания, "ДЛФ=DDT");
	ОблШапка.Области.Завершен.Текст = ?(НЕ ЗначениеЗаполнено(ЦиклОбмена.ДатаЗакрытия) ИЛИ ЦиклОбмена.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияЦиклаОбмена(), "<не завершен>", Формат(ЦиклОбмена.ДатаЗакрытия, "ДЛФ=DDT"));
	ОблШапка.Области.ПоследнееСообщение.Текст = Формат(ЦиклОбмена.ДатаПоследнегоСообщения, "ДЛФ=DDT");
	
	ТаблДок.Вывести(ОблШапка);
	
	// выбираем информацию по сообщениям цикла обмена
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортныеСообщения.Тип КАК Тип,
						  |	ТранспортныеСообщения.ДатаТранспорта,
						  |	ТранспортныеСообщения.Статус КАК СтатусСообщения,
						  |	ТранспортныеСообщения.ПометкаУдаления,
						  |	ТранспортныеСообщения.ПротоколСОшибкой КАК ПротоколСОшибкой,
						  |	ТранспортныеСообщения.Ссылка КАК Ссылка,
						  |	ТранспортныеСообщения.Тема КАК Тема,
						  |	НерасшифрованныеВложения.КоличествоСообщений КАК КоличествоНерасшифрованных,
						  |	РасшифрованныеВложения.КоличествоСообщений КАК КоличествоРасшифрованных,
						  |	ТранспортныеСообщения.Дата КАК Дата
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |		ГДЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение) КАК НерасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = НерасшифрованныеВложения.ТранспортноеСообщение
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |		ГДЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение) КАК РасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = РасшифрованныеВложения.ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортныеСообщения.ЦиклОбмена = &ЦиклОбмена
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортныеСообщения.ЦиклОбмена,
						  |	ТранспортныеСообщения.Тип.Порядок,
						  |	Дата");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	ТаблицаСообщений = Запрос.Выполнить().Выгрузить();
	
	ПоследнийТипСообщения = Неопределено;
	
	Для ИндТипСообщения = 1 По ПредусмотренныеТипыСообщений.Количество() Цикл
		
		ТекСтр = ПредусмотренныеТипыСообщений[ИндТипСообщения - 1];
		Если ТекСтр.ТипЦиклаОбмена <> ТипЦиклаОбмена Тогда
			Продолжить;
		КонецЕсли;
		
		ТекТипСообщения = ТекСтр.ТипСообщения;
		СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТекТипСообщения));
		
		Если СообщенияТекТипа.Количество() = 0 Тогда
			
			ПредставлениеТипа = СоответствиеТипаСообщенияКраткомуПредставлению[ТекТипСообщения];
			ОблСтрока.Области.ТипСообщения.Текст = ПредставлениеТипа;
			ОблСтрока.Области.СтатусСообщения.Текст = "Отсутствует";
			ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , Ложь);
			ТаблДок.Вывести(ОблСтрока);
			
		Иначе
			
			Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
				
				ТипСообщения = СтрСообщение.Тип;
				ДатаТранспортаСообщения = СтрСообщение.ДатаТранспорта;
				СтатусСтрСообщения = СтрСообщение.СтатусСообщения;
				ПометкаУдаленияСообщения = СтрСообщение.ПометкаУдаления;
				ЭтоПротоколСОшибкой = СтрСообщение.ПротоколСОшибкой;
				Сообщение = СтрСообщение.Ссылка;
				ЧислоКонтейнеровСообщения = СтрСообщение.КоличествоНерасшифрованных;
				ЧислоЭлементовСодержимого = СтрСообщение.КоличествоРасшифрованных;
				ДатаСообщения = СтрСообщение.Дата;
				
				ПредставлениеТипа = СоответствиеТипаСообщенияКраткомуПредставлению[ТипСообщения];
				Если ПредставлениеТипа = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				// заполняем ТипСообщения
				Если ПоследнийТипСообщения <> ТипСообщения Тогда
					ОблСтрока.Области.ТипСообщения.Текст = ПредставлениеТипа;
				Иначе
					ОблСтрока.Области.ТипСообщения.Текст = "";
				КонецЕсли;
				ПоследнийТипСообщения = ТипСообщения;
				
				// заполняем строку статуса
				Если СтатусСтрСообщения = Перечисления.СтатусыПисем.Полученное Тогда
					ТекстСтатуса = "От " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Принято " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
					Если ЧислоЭлементовСодержимого = 0 Тогда
						ТекстСтатуса = ТекстСтатуса + " Не расшифровано.";
					КонецЕсли;
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Исходящее Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Не отправлено.";
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Отправленное Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Отправлено " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
				Иначе
					Продолжить;
				КонецЕсли;
				
				Если ЭтоПротоколСОшибкой Тогда
					Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ЗапросыНаИнформационноеОбслуживание Тогда
						ТекстСтатуса = ТекстСтатуса + Символы.ПС + "Содержит отказ в обслуживании!";
					Иначе
						ТекстСтатуса = ТекстСтатуса + Символы.ПС + "Содержит сведения об ошибках!";
					КонецЕсли;
				КонецЕсли;
				
				ОблСтрока.Области.СтатусСообщения.Текст = ТекстСтатуса;
				ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , ПометкаУдаленияСообщения);
				
				ТаблДок.Вывести(ОблСтрока);
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвалСообщения);
	
	// вывод критических ошибок
	КритическиеОшибки = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка));
	Если КритическиеОшибки.Количество() > 0 Тогда
		ТаблДок.Вывести(ОблШапкаКритическиеОшибки);
		Для Каждого КритическаяОшибка Из КритическиеОшибки Цикл
			ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибке.Текст = СокрЛП(КритическаяОшибка.Тема);
			ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибкеДата.Текст = Формат(КритическаяОшибка.ДатаТранспорта, "ДЛФ=DDT");
			ТаблДок.Вывести(ОблСтрокаКритическаяОшибка);
		КонецЦикла;
	КонецЕсли;
	
	ТаблДок.Вывести(ОблПодвал);
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьМакетОбработки(ИмяМакета)  Экспорт
	
	ОбработкаЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат ОбработкаЭДО.ПолучитьМакет(ИмяМакета);
	
КонецФункции

&НаСервере
Функция ВывестиОтчетОСостоянииФНС534(ЦиклОбмена)
	
	ТипЦиклаОбмена = ЦиклОбмена.Тип;
	
	СоответствиеТипаСообщенияКраткомуПредставлению = ИнициализироватьСоответствиеТСКП();
	
	ПредусмотренныеТипыСообщений = Новый ТаблицаЗначений;
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипСообщения");
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипЦиклаОбмена");
	
	// 534
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ДекларацияНП, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность);
	
	// 534 ДРП НО
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ДокументНО, 					Перечисления.ТипыЦикловОбмена.Документ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП, 			Перечисления.ТипыЦикловОбмена.Документ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП, 	Перечисления.ТипыЦикловОбмена.Документ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО , 	Перечисления.ТипыЦикловОбмена.Документ);
	
	//534 Заявление
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП, Перечисления.ТипыЦикловОбмена.Заявление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО, Перечисления.ТипыЦикловОбмена.Заявление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, Перечисления.ТипыЦикловОбмена.Заявление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО, Перечисления.ТипыЦикловОбмена.Заявление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО, Перечисления.ТипыЦикловОбмена.Заявление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, Перечисления.ТипыЦикловОбмена.Заявление);
	
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Заявление Тогда
		РезультатыОбработкиЗаявленияВСтаромФормате = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО);
		Если РезультатыОбработкиЗаявленияВСтаромФормате.Количество() = 0 Тогда
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО, Перечисления.ТипыЦикловОбмена.Заявление);
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, Перечисления.ТипыЦикловОбмена.Заявление);	
		Иначе
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО, Перечисления.ТипыЦикловОбмена.Заявление);
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП, Перечисления.ТипыЦикловОбмена.Заявление);
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО, Перечисления.ТипыЦикловОбмена.Заявление);
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП, Перечисления.ТипыЦикловОбмена.Заявление);
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО, Перечисления.ТипыЦикловОбмена.Заявление);
			ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП, Перечисления.ТипыЦикловОбмена.Заявление);	
		КонецЕсли;
	КонецЕсли;
	
	// 534 2-НДФЛ
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП, Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО, Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО, Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО, Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, Перечисления.ТипыЦикловОбмена.Форма2НДФЛ);
	
	// 534 НФД
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ОбращениеНП, Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО, Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО, Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО, Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, Перечисления.ТипыЦикловОбмена.ОбращениеНП);
	
	// 534 Представление
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП, Перечисления.ТипыЦикловОбмена.Представление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО, Перечисления.ТипыЦикловОбмена.Представление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, Перечисления.ТипыЦикловОбмена.Представление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО, Перечисления.ТипыЦикловОбмена.Представление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО, Перечисления.ТипыЦикловОбмена.Представление);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, Перечисления.ТипыЦикловОбмена.Представление);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоНО, Перечисления.ТипыЦикловОбмена.ПисьмоНО);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП, Перечисления.ТипыЦикловОбмена.ПисьмоНО);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РассылкаНО, Перечисления.ТипыЦикловОбмена.РассылкаГрупповая);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП, Перечисления.ТипыЦикловОбмена.РассылкаГрупповая);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РассылкаНО, Перечисления.ТипыЦикловОбмена.Рассылка);
	
	// 534 ИОН
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ЗапросНП, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО, Перечисления.ТипыЦикловОбмена.ИОН);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, Перечисления.ТипыЦикловОбмена.ИОН);
	
	// запрос на выписку ЕГРЮЛ/ЕГРИП
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП, Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП, Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП);
	
	// получаем шаблон бланка
	ШаблонМакета = ПолучитьМакетОбработки("БланкПечатиОтчетаОСостоянииФНС534");
	
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	ТаблДок.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
	ЗаполнитьЗначенияСвойств(ТаблДок.НижнийКолонтитул, ШаблонМакета.НижнийКолонтитул);
	ЗаполнитьЗначенияСвойств(ТаблДок.ВерхнийКолонтитул, ШаблонМакета.ВерхнийКолонтитул);
	// устанавливать колонтитулы
	
	// получаем области-строки
	ОблШапка = ШаблонМакета.ПолучитьОбласть("Шапка");
	ОблСтрока = ШаблонМакета.ПолучитьОбласть("Строка");
	ОблПодвалСообщения = ШаблонМакета.ПолучитьОбласть("ПодвалСообщения");
	ОблШапкаКритическиеОшибки = ШаблонМакета.ПолучитьОбласть("ШапкаКритическиеОшибки");
	ОблСтрокаКритическаяОшибка = ШаблонМакета.ПолучитьОбласть("СтрокаКритическаяОшибка");
	ОблКритическиеОшибкиОтсутствуют = ШаблонМакета.ПолучитьОбласть("КритическиеОшибкиОтсутствуют");
	ОблПодвал = ШаблонМакета.ПолучитьОбласть("Подвал");
	
	// инициализируем значения шапки
	ОблШапка.Области.Идентификатор.Текст = ЦиклОбмена.Идентификатор;
	ОблШапка.Области.Организация.Текст = ЦиклОбмена.Организация;
	ОблШапка.Области.ВнешняяОрганизация.Текст = ЦиклОбмена.ВнешняяОрганизация;
	
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда
		ОблШапка.Области.СубъектыПерепискиТип.Текст = "Оператор электронного документооборота:";
	КонецЕсли;
	
	// формируем строку с предметом
	Предмет = ЦиклОбмена.Предмет;
	Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность 
	ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Заявление Тогда
	
		Если ТипЗнч(Предмет) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
			СтрПредмет = "Отчет " + РегламентированнаяОтчетностьКлиентСервер.ПредставлениеДокументаРеглОтч(Предмет);
		ИначеЕсли ЗначениеЗаполнено(Предмет) Тогда
			СтрПредмет = "Внешние отчет:" + Символы.ПС + Строка(Предмет);
		Иначе
			СтрПредмет = "<Отсутствует>";
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Форма2НДФЛ Тогда
		
		СтрПредмет = Строка(Предмет);
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОбращениеНП
	ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Представление Тогда
		
		СтрПредмет = Строка(Предмет);
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ПисьмоНО
	ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Рассылка
	ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.РассылкаГрупповая Тогда

		
		Если ЗначениеЗаполнено(Предмет) тогда
			СтрПредмет = Строка(Предмет);
		Иначе
			СтрПредмет = "<Предмет будет создан в момент расшифровки первичного сообщения, содержащего документ налогового органа>";
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Документ Тогда
		
		МассивПредметов = ПолучитьПредметыЦиклаОбмена(ЦиклОбмена);
		КоличествоПредметов = МассивПредметов.Количество();
		Если КоличествоПредметов = 0 Тогда
			СтрПредмет = "<Предмет будет создан в момент расшифровки первичного сообщения, содержащего документ налогового органа>";
		Иначе
			СтрПредмет = "";
			Для каждого ЭлПредмет Из МассивПредметов Цикл
				СтрПредмет = СтрПредмет + Строка(ЭлПредмет) + Символы.ПС;
			КонецЦикла;
			СтрПредмет = Лев(СтрПредмет, СтрДлина(СтрПредмет) - 1);
		КонецЕсли;
		
	ИначеЕсли ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ИОН
		ИЛИ ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП Тогда
		
		СтрПредмет = Строка(Предмет);
		
	КонецЕсли;
		
	ОблШапка.Области.ПредметПереписки.Текст = СтрПредмет;
	
	ОблШапка.Области.Создан.Текст = Формат(ЦиклОбмена.ДатаСоздания, "ДЛФ=DDT");
	ОблШапка.Области.Завершен.Текст = ?(НЕ ЗначениеЗаполнено(ЦиклОбмена.ДатаЗакрытия) ИЛИ ЦиклОбмена.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияЦиклаОбмена(), "<не завершен>", Формат(ЦиклОбмена.ДатаЗакрытия, "ДЛФ=DDT"));
	ОблШапка.Области.ПоследнееСообщение.Текст = Формат(ЦиклОбмена.ДатаПоследнегоСообщения, "ДЛФ=DDT");
	
	ТаблДок.Вывести(ОблШапка);
	
	// выбираем информацию по сообщениям цикла обмена
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортныеСообщения.Тип КАК Тип,
						  |	ТранспортныеСообщения.ДатаТранспорта,
						  |	ТранспортныеСообщения.Статус КАК СтатусСообщения,
						  |	ТранспортныеСообщения.ПометкаУдаления,
						  |	ТранспортныеСообщения.ПротоколСОшибкой КАК ПротоколСОшибкой,
						  |	ТранспортныеСообщения.Ссылка КАК Ссылка,
						  |	ТранспортныеСообщения.Тема КАК Тема,
						  |	НерасшифрованныеВложения.КоличествоСообщений КАК КоличествоНерасшифрованных,
						  |	РасшифрованныеВложения.КоличествоСообщений КАК КоличествоРасшифрованных,
						  |	ТранспортныеСообщения.Дата КАК Дата
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |		ГДЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение) КАК НерасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = НерасшифрованныеВложения.ТранспортноеСообщение
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |		ГДЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение) КАК РасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = РасшифрованныеВложения.ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортныеСообщения.ЦиклОбмена = &ЦиклОбмена
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортныеСообщения.ЦиклОбмена,
						  |	ТранспортныеСообщения.Тип.Порядок,
						  |	Дата");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	ТаблицаСообщений = Запрос.Выполнить().Выгрузить();
	
	ПоследнийТипСообщения = Неопределено;
	
	Для ИндТипСообщения = 1 По ПредусмотренныеТипыСообщений.Количество() Цикл
		
		// получаем очередной тип сообщения и проверяем, относится ли он к типу текущего цикла обмена
		ТекСтр = ПредусмотренныеТипыСообщений[ИндТипСообщения - 1];
		Если ТекСтр.ТипЦиклаОбмена <> ТипЦиклаОбмена Тогда
			Продолжить;
		КонецЕсли;
		
		// находим сообщения текущего типа у нашего цикла обмена
		ТекТипСообщения = ТекСтр.ТипСообщения;
		СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТекТипСообщения));
		
		// если сообщение текущего типа отсутствует, то так и выведем
		Если СообщенияТекТипа.Количество() = 0 Тогда
			
			// формируем представление типа
			ПредставлениеТипа = СоответствиеТипаСообщенияКраткомуПредставлению[ТекТипСообщения];
			
			ОблСтрока.Области.ТипСообщения.Текст = ПредставлениеТипа;
			ОблСтрока.Области.СтатусСообщения.Текст = "Отсутствует";
			ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , Ложь);
			ТаблДок.Вывести(ОблСтрока);
			
		Иначе
			
			// если сообщения текущего типа присутствуют, то выведем информацию по каждому из них
			Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
				
				ТипСообщения = СтрСообщение.Тип;
				ДатаТранспортаСообщения = СтрСообщение.ДатаТранспорта;
				СтатусСтрСообщения = СтрСообщение.СтатусСообщения;
				ПометкаУдаленияСообщения = СтрСообщение.ПометкаУдаления;
				ЭтоПротоколСОшибкой = СтрСообщение.ПротоколСОшибкой;
				ЧислоЭлементовСодержимого = СтрСообщение.КоличествоРасшифрованных;
				ДатаСообщения = СтрСообщение.Дата;
				
				ПредставлениеТипа = СоответствиеТипаСообщенияКраткомуПредставлению[ТипСообщения];
				Если ПредставлениеТипа = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				// заполняем ТипСообщения
				Если ПоследнийТипСообщения <> ТипСообщения Тогда
					ОблСтрока.Области.ТипСообщения.Текст = ПредставлениеТипа;
				Иначе
					ОблСтрока.Области.ТипСообщения.Текст = "";
				КонецЕсли;
				ПоследнийТипСообщения = ТипСообщения;
				
				// заполняем строку статуса
				Если СтатусСтрСообщения = Перечисления.СтатусыПисем.Полученное Тогда
					ТекстСтатуса = "От " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Принято " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
					Если ЧислоЭлементовСодержимого = 0 Тогда
						ТекстСтатуса = ТекстСтатуса + " Не расшифровано.";
					КонецЕсли;
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Исходящее Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Не отправлено.";
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Отправленное Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Отправлено " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
				Иначе
					Продолжить;
				КонецЕсли;
				
				Если ЭтоПротоколСОшибкой Тогда
					Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ИОН Тогда
						ТекстСтатуса = ТекстСтатуса + Символы.ПС + "Содержит отказ в обслуживании!";
					Иначе
						ТекстСтатуса = ТекстСтатуса + Символы.ПС + "Содержит сведения об ошибках!";
					КонецЕсли;
				КонецЕсли;
				
				ОблСтрока.Области.СтатусСообщения.Текст = ТекстСтатуса;
				ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , ПометкаУдаленияСообщения);
				
				ТаблДок.Вывести(ОблСтрока);
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвалСообщения);
	
	// вывод информации об ошибках
	ТипыОшибок = Новый Массив;
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС);
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	Для Каждого ТипОшибки Из ТипыОшибок Цикл
		СообщенияОшибки = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТипОшибки));
		Если СообщенияОшибки.Количество() > 0 Тогда
			ТаблДок.Вывести(ОблШапкаКритическиеОшибки);
			Для Каждого СообщениеОшибка Из СообщенияОшибки Цикл
				ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибке.Текст = СокрЛП(СообщениеОшибка.Тема);
				ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибкеДата.Текст = Формат(СообщениеОшибка.ДатаТранспорта, "ДЛФ=DDT");
				ТаблДок.Вывести(ОблСтрокаКритическаяОшибка);
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвал);
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеПредметаПФР(Предмет)
	
	Если ТипЗнч(Предмет) = Тип("СправочникСсылка.МакетыПенсионныхДел")
		ИЛИ ТипЗнч(Предмет) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		Возврат СокрЛП(Предмет.Наименование) + " от " + Формат(Предмет.ДатаОтправки, "ДЛФ=DDT");	
	Иначе
		Если Предмет.Статус = Перечисления.СтатусыПисем.Полученное Тогда
			Возврат "Входящее письмо """ + СокрЛП(Предмет.Наименование) + """ от " + Формат(Предмет.ДатаОтправки, "ДЛФ=DDT");
		Иначе
			Возврат "Исходящее письмо """ + СокрЛП(Предмет.Наименование) + """ от " + Формат(Предмет.ДатаСообщения, "ДЛФ=DDT");
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеОтчетаПФР(ОтчетПФР)
	
	Если ТипЗнч(ОтчетПФР) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		Возврат РегламентированнаяОтчетностьКлиентСервер.ПредставлениеДокументаРеглОтч(ОтчетПФР);
	Иначе
		Возврат "" + ОтчетПФР;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, ТипСообщения, ТипЦиклаОбмена)
	
	НовСтр = ПредусмотренныеТипыСообщений.Добавить();
	НовСтр.ТипСообщения = ТипСообщения;
	НовСтр.ТипЦиклаОбмена = ТипЦиклаОбмена;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьОсновныеСвойстваИзУведомления(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Рекомендации = Новый Массив;
	
	ТаблицаОшибок = Новый ТаблицаЗначений;
	ТаблицаОшибок.Колонки.Добавить("ИмяФайла");
	ТаблицаОшибок.Колонки.Добавить("КодОшибки");
	ТаблицаОшибок.Колонки.Добавить("Описание");
	
	ТаблицаПереченьСведений = Новый ТаблицаЗначений;
	ТаблицаПереченьСведений.Колонки.Добавить("СНИЛС");
	ТаблицаПереченьСведений.Колонки.Добавить("ИНН");
	ТаблицаПереченьСведений.Колонки.Добавить("ФИО");
	ТаблицаПереченьСведений.Колонки.Добавить("ТекстОшибки");
	
	ТаблицаДоходыИНалогиФЛ = Новый ТаблицаЗначений;
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("НомерСправки");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("НомерКорректировки");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("ОшибочныйЭлемент");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("ФИОФизическогоЛица");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("ТекстОшибкиВДоходахИНалогахФЛ");
	
	ПереченьОснований = Новый Массив;
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML уведомления об уточнении::%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);   
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	ВерсияФормата = УзелФайл.Строки.Найти("ВерсФорм", "Имя");
	
	Если НЕ ЗначениеЗаполнено(ВерсияФормата) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен атрибут ""ВерсФорм"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ОбщегоНазначенияКлиентСервер.СравнитьВерсии(ВерсияФормата.Значение + ".0.0", "5.05.0.0") < 0 Тогда
		ТекстСообщения = НСтр("ru = 'Устаревшая версия формата XML уведомления, версия формата должна быть равна 5.05 или выше.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСведУвед = УзелДокумент.Строки.Найти("СведУвед", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСведУвед) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""СведУвед"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	//Рекомендации
	УзлыРекНал = УзелСведУвед.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "РекНал", "Э"));
	Для Каждого УзелРекНал Из УзлыРекНал Цикл
		Значение = СокрЛП(УзелРекНал.Значение);
		Если НЕ ПустаяСтрока(Значение) Тогда
			Рекомендации.Добавить(СокрЛП(УзелРекНал.Значение));
		КонецЕсли;
	КонецЦикла;
	
	//Ошибки конкретных файлов
	УзлыВыявлНарФайл = УзелСведУвед.Строки.НайтиСтроки(Новый Структура("Имя", "ВыявлОшФайл"));
	Для Каждого УзелВыявНарФайл Из УзлыВыявлНарФайл Цикл
		
		ИмяФайла = УзелВыявНарФайл.Строки.Найти("ИмяОбрабФайла", "Имя").Значение;
		УзлыПеречВыявНар = УзелВыявНарФайл.Строки.НайтиСтроки(Новый Структура("Имя", "СвПоОшибке"));
		
		Для Каждого УзелПеречВыявНар Из УзлыПеречВыявНар Цикл
			
			УзелКодОш = УзелПеречВыявНар.Строки.Найти("КодОшибки", "Имя");
			УзелТекстОш = УзелПеречВыявНар.Строки.Найти("ТекстОш", "Имя");
			
			Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш) Тогда
				
				КодОшибки = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "");
				ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "");
				
				НовСтр = ТаблицаОшибок.Добавить();
				НовСтр.КодОшибки = КодОшибки;
				НовСтр.Описание = ТекстОшибки;
				НовСтр.ИмяФайла = ИмяФайла;
			КонецЕсли;
			
		КонецЦикла;
		
		ПолучитьСвойстваУзлаСвИдФЛ(УзелВыявНарФайл, ТаблицаПереченьСведений);
		
		ПолучитьСвойстваУзлаСвДохНалФЛ(УзелВыявНарФайл, ТаблицаДоходыИНалогиФЛ);
		
	КонецЦикла;
	
	ТаблицаПереченьОснований = ТаблицаПереченьСведений.Скопировать();
	ТаблицаПереченьОснований.Свернуть("ТекстОшибки");
	ПереченьОснований = ТаблицаПереченьСведений.ВыгрузитьКолонку("ТекстОшибки");
	
	// Сведения об отправителе отчетности
	ПредставлениеОтпр = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвОтпр", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	// Сведения о налогоплательщике
	ПредставлениеНП = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвНП", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("НПФЛ", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("НПЮЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	УзлыОбщСвУвед = УзелСведУвед.Строки.НайтиСтроки(Новый Структура("Имя", "ОбщСвУвед"));
	Если УзлыОбщСвУвед.Количество() > 0 Тогда
		УзелОбщСвУвед = УзлыОбщСвУвед[0];
		Для Каждого УзелОбщСвед Из УзелОбщСвУвед.Строки Цикл
			ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
		КонецЦикла;
	КонецЕсли;
	
	// Налоговый орган
	ОбщиеСведенияНалоговыйОрган = УзелДокумент.Строки.Найти("КодНО", "Имя").Значение;
	ОбщиеСведения.Вставить("КодНО", ОбщиеСведенияНалоговыйОрган);
	
	СвойстваУведомления = Новый Структура();
	СвойстваУведомления.Вставить("ОбщиеСведения", ОбщиеСведения);
	СвойстваУведомления.Вставить("ПредставлениеОтпр", ПредставлениеОтпр);
	СвойстваУведомления.Вставить("ПредставлениеНП", ПредставлениеНП);
	СвойстваУведомления.Вставить("Ошибки", ТаблицаОшибок);
	СвойстваУведомления.Вставить("Рекомендации", Рекомендации);
	СвойстваУведомления.Вставить("ТаблицаПереченьСведений",   	ТаблицаПереченьСведений);
	СвойстваУведомления.Вставить("ПереченьОснований",   		ПереченьОснований);
	СвойстваУведомления.Вставить("ТаблицаДоходыИНалогиФЛ",   	ТаблицаДоходыИНалогиФЛ);
	 
	Возврат СвойстваУведомления;
	
КонецФункции

&НаСервереБезКонтекста
Функция печатьЗагрузитьСтрокуXMLВДеревоЗначений(СтрокаXML, ОписаниеОшибки = Неопределено, ЧтениеXML = Неопределено, Знач ТекУзел = Неопределено)
	
	ПерваяИтерация = (ТекУзел = Неопределено);
	Если ПерваяИтерация Тогда
		ТекУзел = печатьСоздатьДеревоСтруктурыXML();
		Попытка
			ЧтениеXML = Новый ЧтениеXML;
			ЧтениеXML.УстановитьСтроку(СтрокаXML);
		Исключение
			ОписаниеОшибки = "Ошибка разбора XML: " + ИнформацияОбОшибке().Описание + ".";
			Возврат Неопределено;
		КонецПопытки;
	КонецЕсли;
	
	Попытка
		Пока ЧтениеXML.Прочитать() Цикл
			ТипУзла = ЧтениеXML.ТипУзла;
			Если ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				НовУзел = ТекУзел.Строки.Добавить();
				НовУзел.Имя = ЧтениеXML.Имя;
				НовУзел.Тип = "Э";
				НовУзел.Значение = ЧтениеXML.Значение;
				Пока ЧтениеXML.ПрочитатьАтрибут() Цикл
					НовАтрибут = НовУзел.Строки.Добавить();
					НовАтрибут.Имя = ЧтениеXML.Имя;
					НовАтрибут.Тип = "А";
					НовАтрибут.Значение = ЧтениеXML.Значение;
				КонецЦикла;
				печатьЗагрузитьСтрокуXMLВДеревоЗначений(СтрокаXML, ОписаниеОшибки, ЧтениеXML, НовУзел);
			ИначеЕсли ТипУзла = ТипУзлаXML.КонецЭлемента Тогда
				Прервать;
			ИначеЕсли ТипУзла = ТипУзлаXML.Текст И ТипЗнч(ТекУзел) = Тип("СтрокаДереваЗначений") И ТекУзел.Тип = "Э" Тогда
				ТекУзел.Значение = ЧтениеXML.Значение;
			КонецЕсли;
		КонецЦикла;
	Исключение
		ОписаниеОшибки = "Ошибка разбора XML: " + ИнформацияОбОшибке().Описание + ".";
		Возврат Неопределено;
	КонецПопытки;
	
	Если ПерваяИтерация Тогда
		Возврат ТекУзел;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция печатьСоздатьДеревоСтруктурыXML()
	
	ДеревоСтруктуры = Новый ДеревоЗначений;
	ДеревоСтруктуры.Колонки.Добавить("Имя");
	ДеревоСтруктуры.Колонки.Добавить("Тип");
	ДеревоСтруктуры.Колонки.Добавить("Значение");
	Возврат ДеревоСтруктуры;
	
КонецФункции

&НаСервере
Функция ПолучитьОсновныеСвойстваИзКвитанцииОПриеме(СтрКвитанция)
	
	Если НЕ ЗначениеЗаполнено(СтрКвитанция) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СвПолДок       = "";
	ДатаПрин       = "";
	НаимВидДок     = "";
	ИмяПринятФайла = "";
	ДатаНапр       = "";
	ВремНапр       = "";
	СвОтпрДок      = "";
	НОПром         = "";
	СвАдр          = "";
	ДатаПост       = "";
	РегНом         = "";
	ФИОДолж        = "";

	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрКвитанция, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка разбора XML квитанции о приеме:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ПолучитьУзел(ДеревоXML,"Файл");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = ПолучитьУзел(УзелФайл, "Документ");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СведенияИзТэгаСвКвит = ПолучитьСведенияИзТэгаСвКвитКвитанцииОПриеме(УзелДокумент);
	Если СведенияИзТэгаСвКвит = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		ДатаПрин       = СведенияИзТэгаСвКвит.ДатаПрин;
		НаимВидДок     = СведенияИзТэгаСвКвит.НаимВидДок;
		ДатаНапр       = СведенияИзТэгаСвКвит.ДатаНапр;
		ВремНапр       = СведенияИзТэгаСвКвит.ВремНапр;
		ДатаПост       = СведенияИзТэгаСвКвит.ДатаПост;
		РегНом         = СведенияИзТэгаСвКвит.РегНом;
		ИмяПринятФайла = СведенияИзТэгаСвКвит.ИмяПринятФайла;
	КонецЕсли;
	
	СвПолДок 	= ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, "СвПолДок");
	СвОтпрДок 	= ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, "СвОтпрДок");
	СвАдр 		= ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, "СвАдр");
	ФИОДолж 	= ПолучитьФИОДолжИзТэгаФИОДолж(УзелДокумент);
		
	СвойстваКвитанции = Новый Структура();
	СвойстваКвитанции.Вставить("СвПолДок", СвПолДок);
	СвойстваКвитанции.Вставить("ДатаПрин", ДатаПрин);
	СвойстваКвитанции.Вставить("НаимВидДок", НаимВидДок);
	СвойстваКвитанции.Вставить("ДатаНапр", ДатаНапр);
	СвойстваКвитанции.Вставить("ВремНапр", ВремНапр);
	СвойстваКвитанции.Вставить("ДатаПост", ДатаПост);
	СвойстваКвитанции.Вставить("РегНом", РегНом);
	СвойстваКвитанции.Вставить("ИмяПринятФайла", ИмяПринятФайла);
	СвойстваКвитанции.Вставить("СвОтпрДок", СвОтпрДок);
	СвойстваКвитанции.Вставить("СвАдр", СвАдр);
	СвойстваКвитанции.Вставить("ФИОДолж", ФИОДолж);
	
	Возврат СвойстваКвитанции;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПредставлениеНП(ЮрФизЛицо,ДанныеНП)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьПредставлениеНП(ЮрФизЛицо,ДанныеНП);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзПодтвержденияДатыОтправки(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка чтения XML подтверждения даты отправки:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелОргПодт = УзелДокумент.Строки.Найти("ОргПодт", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелОргПодт) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: не обнаружен узел ""ОргПодт"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения об организации, подтвердившей дату отправки
	УзелСпецОперат = УзелОргПодт.Строки.Найти("СпецОперат", "Имя");
	УзелНО = УзелОргПодт.Строки.Найти("НО", "Имя");
	ОрганизацияСтр = "";
	
	Если ЗначениеЗаполнено(УзелСпецОперат) Тогда
		НаимОрг = УзелСпецОперат.Строки.Найти("НаимОрг", "Имя").Значение;
		ИденСОС = УзелСпецОперат.Строки.Найти("ИденСОС", "Имя").Значение;
		ОрганизацияСтр = СокрЛП(НаимОрг);
	ИначеЕсли ЗначениеЗаполнено(УзелНО) Тогда
		УзелКодНО = УзелНО.Строки.Найти("КодНО", "Имя");
		УзелНаимНО = УзелНО.Строки.Найти("НаимНО", "Имя");
		Если ЗначениеЗаполнено(УзелКодНО) И ЗначениеЗаполнено(УзелКодНО.Значение) Тогда
			ОрганизацияСтр = СокрЛП(УзелКодНО.Значение);
			Если ЗначениеЗаполнено(УзелНаимНО) И ЗначениеЗаполнено(УзелНаимНО.Значение) Тогда
				ОрганизацияСтр = ОрганизацияСтр + " (" + СокрЛП(УзелНаимНО.Значение) + ")";
			КонецЕсли;
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: отсутствуют сведения об организации, подтвердившей дату отправки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения подтверждения
	УзелСведПодтв = УзелДокумент.Строки.Найти("СведПодтв", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСведПодтв) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: отсутствуют сведения подтверждения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения об имени поступившего файла
	УзелИмяПостФайла = УзелСведПодтв.Строки.Найти("ИмяПостФайла", "Имя", Истина);
	Если ЗначениеЗаполнено(УзелИмяПостФайла) Тогда
		ИмяПоступившегоФайла = СокрЛП(УзелИмяПостФайла.Значение);
	КонецЕсли;
	
	// получаем сведения о дате и времени поступившего файла
	УзелДатаОтпр = УзелСведПодтв.Строки.Найти("ДатаОтпр", "Имя");
	УзелВремяОтпр = УзелСведПодтв.Строки.Найти("ВремяОтпр", "Имя");
	Если ЗначениеЗаполнено(УзелДатаОтпр) Тогда
		ДатаОтправки = СокрЛП(УзелДатаОтпр.Значение);
	КонецЕсли;
	Если ЗначениеЗаполнено(УзелВремяОтпр) Тогда
		ДатаОтправки = СокрЛП(ДатаОтправки + " " + СокрЛП(УзелВремяОтпр.Значение));
	КонецЕсли;
	
	// Сведения об отправителе отчетности
	ПредставлениеОтпр = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвОтпр", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	EmailОтправитель = ДанныеОтправителя.Строки.Найти("E-mail", "Имя").Значение;
	
	// Сведения об получателе отчетности
	ДанныеПолучателя = УзелДокумент.Строки.Найти("СвПолДок", "Имя");
	
	EmailПолучатель = ДанныеПолучателя.Строки.Найти("E-mail", "Имя").Значение;
	
	СведПолучателяНО = ДанныеПолучателя.Строки.Найти("ОтпрНО", "Имя");
	
	КодНО = СведПолучателяНО.Строки.Найти("КодНО", "Имя").Значение;
	НаимНО = СведПолучателяНО.Строки.Найти("НаимНО", "Имя").Значение;
	
	ДанныеФайлаОтправки = Новый Структура("ДатаОтправки,ВремяОтправки,EmailОтправитель,EmailПолучатель,КодНО,ПредставлениеОтпр,НаимНО",
	СокрЛП(УзелДатаОтпр.Значение),СокрЛП(УзелВремяОтпр.Значение),EmailОтправитель,EmailПолучатель,КодНО,ПредставлениеОтпр,НаимНО);
	
	Возврат Новый Структура("ПодтвердившаяОрганизация, ДатаВремяОтправки, ИмяФайла, ДанныеФайлаОтправки", ОрганизацияСтр, ДатаОтправки, ИмяПоступившегоФайла, ДанныеФайлаОтправки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзПодтвержденияДатыОтправкиПредставление(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка чтения XML подтверждения даты отправки:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелОргПодт = УзелДокумент.Строки.Найти("ОргПодт", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелОргПодт) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: не обнаружен узел ""ОргПодт"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения об организации, подтвердившей дату отправки
	УзелСпецОперат = УзелОргПодт.Строки.Найти("СпецОперат", "Имя");
	УзелНО = УзелОргПодт.Строки.Найти("НО", "Имя");
	ОрганизацияСтр = "";
	
	Если ЗначениеЗаполнено(УзелСпецОперат) Тогда
		НаимОрг = УзелСпецОперат.Строки.Найти("НаимОрг", "Имя").Значение;
		ИденСОС = УзелСпецОперат.Строки.Найти("ИденСОС", "Имя").Значение;
		ОрганизацияСтр = СокрЛП(НаимОрг);
	ИначеЕсли ЗначениеЗаполнено(УзелНО) Тогда
		УзелКодНО = УзелНО.Строки.Найти("КодНО", "Имя");
		УзелНаимНО = УзелНО.Строки.Найти("НаимНО", "Имя");
		Если ЗначениеЗаполнено(УзелКодНО) И ЗначениеЗаполнено(УзелКодНО.Значение) Тогда
			ОрганизацияСтр = СокрЛП(УзелКодНО.Значение);
			Если ЗначениеЗаполнено(УзелНаимНО) И ЗначениеЗаполнено(УзелНаимНО.Значение) Тогда
				ОрганизацияСтр = ОрганизацияСтр + " (" + СокрЛП(УзелНаимНО.Значение) + ")";
			КонецЕсли;
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: отсутствуют сведения об организации, подтвердившей дату отправки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения подтверждения
	УзелСведПодтв = УзелДокумент.Строки.Найти("СведПодтв", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСведПодтв) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения: отсутствуют сведения подтверждения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения об имени поступившего файла
	УзлыИмяПостФайла = УзелСведПодтв.Строки.НайтиСтроки(Новый Структура("Имя", "ИмяПостФайла"), Истина);
	Если ЗначениеЗаполнено(УзлыИмяПостФайла) Тогда
		ИменаПоступившихФайлов = Новый Массив;
		Для Каждого УзелИмяПостФайла Из УзлыИмяПостФайла Цикл
			ИменаПоступившихФайлов.Добавить(СокрЛП(УзелИмяПостФайла.Значение));
		КонецЦикла;
		
		ИмяПоступившегоФайла = СтрСоединить(ИменаПоступившихФайлов, ", ");
	КонецЕсли;
	
	// получаем сведения о дате и времени поступившего файла
	УзелДатаОтпр = УзелСведПодтв.Строки.Найти("ДатаОтпр", "Имя");
	УзелВремяОтпр = УзелСведПодтв.Строки.Найти("ВремяОтпр", "Имя");
	Если ЗначениеЗаполнено(УзелДатаОтпр) Тогда
		ДатаОтправки = СокрЛП(УзелДатаОтпр.Значение);
	КонецЕсли;
	Если ЗначениеЗаполнено(УзелВремяОтпр) Тогда
		ДатаОтправки = СокрЛП(ДатаОтправки + " " + СокрЛП(УзелВремяОтпр.Значение));
	КонецЕсли;
	
	// Сведения об отправителе отчетности
	ПредставлениеОтпр = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвОтпр", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	EmailОтправитель = ДанныеОтправителя.Строки.Найти("E-mail", "Имя").Значение;
	
	// Сведения об получателе отчетности
	ДанныеПолучателя = УзелДокумент.Строки.Найти("СвПолДок", "Имя");
	
	EmailПолучатель = ДанныеПолучателя.Строки.Найти("E-mail", "Имя").Значение;
	
	СведПолучателяНО = ДанныеПолучателя.Строки.Найти("ОтпрНО", "Имя");
	
	КодНО = СведПолучателяНО.Строки.Найти("КодНО", "Имя").Значение;
	НаимНО = СведПолучателяНО.Строки.Найти("НаимНО", "Имя").Значение;
	
	ДанныеФайлаОтправки = Новый Структура("ДатаОтправки,ВремяОтправки,EmailОтправитель,EmailПолучатель,КодНО,ПредставлениеОтпр,НаимНО",
	СокрЛП(УзелДатаОтпр.Значение),СокрЛП(УзелВремяОтпр.Значение),EmailОтправитель,EmailПолучатель,КодНО,ПредставлениеОтпр,НаимНО);
	
	Возврат Новый Структура("ПодтвердившаяОрганизация, ДатаВремяОтправки, ИмяФайла, ДанныеФайлаОтправки", ОрганизацияСтр, ДатаОтправки, ИмяПоступившегоФайла, ДанныеФайлаОтправки);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Процедура ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, Код, Имя)
	
	НовСтр = ПоляОбщихСведений.Добавить();
	НовСтр.Код = Код;
	НовСтр.Имя = Имя;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ВывестиТекстПострочно(ТаблДок, МакетСтрока, Текст, ИмяОбласти = "Тело")
	
	Для Инд = 1 По СтрЧислоСтрок(Текст) Цикл
		МакетСтрока.Области[ИмяОбласти].Текст = СтрПолучитьСтроку(Текст, Инд);
		ТаблДок.Вывести(МакетСтрока);
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСвойстваНеформализованногоДокумента(Предмет)
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
						  |	ВложенияНеформализованныхДокументов.ИмяФайла,
						  |	ВложенияНеформализованныхДокументов.Тип
						  |ИЗ
						  |	РегистрСведений.ВложенияНеформализованныхДокументов КАК ВложенияНеформализованныхДокументов
						  |ГДЕ
						  |	ВложенияНеформализованныхДокументов.НеформализованныйДокумент = &НеформализованныйДокумент");
	Запрос.УстановитьПараметр("НеформализованныйДокумент", Предмет);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() И ЗначениеЗаполнено(Выборка.ИмяФайла) Тогда
		Возврат Новый Структура("ИмяФайла, Тип", Выборка.ИмяФайла, Выборка.Тип);
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ИнициализироватьКартуДокументовИПодписей()
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ИнициализироватьКартуДокументовИПодписей();
	
КонецФункции

&НаКлиенте
Функция СформироватьИмяФайла(ИсходноеИмяФайла) Экспорт
	
	ИмяФайла = СтрЗаменить(ИсходноеИмяФайла, "/", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "\", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, ":", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "*", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "?", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, """","_");
	ИмяФайла = СтрЗаменить(ИмяФайла, "<", "_");
	ИмяФайла = СтрЗаменить(ИмяФайла, ">", "_");
	Возврат СтрЗаменить(ИмяФайла, "|", "_");
	
КонецФункции

&НаСервереБезКонтекста
Функция ИнициализироватьСоответствиеТСКП()
	
	СоответствиеТСКП = Новый Соответствие;
	
	// ФНС 534
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ДекларацияНП, "Отчет");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДекларацияНО, "Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеНП, "Извещение на подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДекларацияНО, "Извещение о получении отчета");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДекларацияНО, "Результат приема");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНП, "Извещение о получении результата приема");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДекларацияНО, "Результат обработки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиНП, "Извещение о получении результата обработки");
	
	// ФНС 534 2-НДФЛ
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.Форма2НДФЛНП, "Форма 2-НДФЛ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеФорма2НДФЛНО, "Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеФорма2НДФЛНО, "Извещение о получении формы 2-НДФЛ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаФорма2НДФЛНО, "Результат приема");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФНС, "Уведомление об ошибке");
	
	// ФНС 534 НФД
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ОбращениеНП, "Письмо");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеОбращениеНО, "Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбращениеНО, "Извещение о получении письма");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаОбращениеНО, "Результат приема");
	
	// ФНС Представление
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП, "Представление");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПредставлениеНО, "Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПредставлениеНО, "Извещение о получении представления");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаПредставлениеНО, "Результат приема");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоНО, "Письмо");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоНП, "Извещение о получении письма");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаНО, "Рассылка");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРассылкаНП, "Извещение о получении рассылки");
	
	// ФНС 534 ИОН
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНП, "Запрос");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗапросНО, "Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗапросНО, "Извещение о получении запроса");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗапросНО, "Результат приема");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО, "Результат обработки");
	
	// ПФР
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР, "Отчет");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР, "Подтверждение получения");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР, "Протокол");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР, "Квитанция на протокол");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР, "Исходящее письмо");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееКвитанцияПФР, "Квитанция на исходящее письмо");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееПФР, "Входящее письмо");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР, "Квитанция на входящее письмо");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР, "Запрос");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияЗапросаПФР, "Подтверждение получения");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросПФР, "Ответ на запрос");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР, "Квитанция на ответ на запрос");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеПФР, "Уведомление об ошибке");
	
	// ФСГС
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС, "Отчет");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС, "Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС, "Извещение о получении подтверждения даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС, "Извещение о получении отчетности");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС, "Протокол");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС, "Извещение о получении протокола");

	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС, "Исходящее письмо");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС, "Извещение о получении письма его получателем");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС, "Входящее письмо");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС, "Рассылка");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС, "Рассылка шаблонов");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС, "Извещение о получении письма его получателем");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС, "Уведомление об ошибке");
	//
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка, "Критическая ошибка");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПустаяСсылка(), "Неизвестный тип");
	
	// ФНС Заявление
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗаявлениеНП,						"Отчет");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеЗаявлениеНО,			"Подтверждение даты отправки");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеЗаявлениеНО,				"Извещение о получении отчета");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаЗаявлениеНО,		"Результат приема");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеНО, 	"Результат обработки заявления");
	
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеРФНО, 	"Результат обработки в РФ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиРФНП,	"Извещение о получении результата обработки в РФ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.СообщениеОбОтзывеЗаявлениеРФНО,	"Сообщение об отзыве заявления в РФ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеОбОтзывеЗаявлениеРФНП,	"Извещение о получении сообщения об отзыве заявления в РФ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗаявлениеТСНО,	"Результат обработки заявления в ТС");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатОбработкиТСНП,	"Извещение о получении результата обработки заявления в ТС");
	
	//ФНС Документ
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ДокументНО, 						"Документ");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеДокументНП, 				"Извещение о получении документа");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП,			"Результат приема документа");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ИзвещениеРезультатПриемаНО, 		"Извещение о получении результата приема документа");
	
	// запрос на выписку ЕГРЮЛ/ЕГРИП
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП,					"Запрос на выписку");
	СоответствиеТСКП.Вставить(Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП,	"Результат обработки запроса");

	Возврат СоответствиеТСКП;

КонецФункции

&НаСервереБезКонтекста
Функция УчетнаяЗаписьНастроенаВерно(Знач УчетнаяЗапись, УпрощеннаяПроверкаСертификатов = Истина) Экспорт
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		УчетнаяЗапись = УчетнаяЗаписьОрганизации(УчетнаяЗапись);
	КонецЕсли;
	
	Если УчетнаяЗапись = Неопределено ИЛИ УчетнаяЗапись = Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка() Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// определяем результирующую структуру
	СвойстваУчетнойЗаписи = Новый Структура("Наименование, АдресЭлектроннойПочты, ОбменНапрямую, ОбменНапрямуюСФСГС, "
		+ "ИмяПользователяPOP3, ПарольPOP3, СерверPOP3, ИмяПользователяSMTP, ПарольSMTP, СерверSMTP, ТребуетсяSMTPАутентификация, "
		+ "НалоговыйОрган, СерверДокументооборота, СертификатРуководителя, СертификатГлавногоБухгалтера, СертификатДляШифрования, "
		+ "НалоговыйОрганАдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками, НалоговыйОрганСертификат, "
		+ "НалоговыйОрганКод, НалоговыйОрганНаименование, НалоговыйОрганПредставление, "
		+ "СерверДокументооборотаПредставление, СерверДокументооборотаСертификат, "
		+ "СерверДокументооборотаАдресЭлектроннойПочтыФНС, СерверДокументооборотаАдресЭлектроннойПочтыПФР, "
		+ "СерверДокументооборотаАдресЭлектроннойПочтыФСГС, "
		+ "ПредназначенаДляДокументооборотаСФНС, ПредназначенаДляДокументооборотаСПФР, "
		+ "ПредназначенаДляДокументооборотаСФСГС, ПредназначенаДляДокументооборотаСЦБ, "
		+ "ИдентификаторСпецоператора, ИдентификаторАбонента, ЭтоМультиРежим");
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникОбъект.УчетныеЗаписиДокументооборота") Тогда
		
		// заполняем свойства из объекта
		ЗаполнитьЗначенияСвойств(СвойстваУчетнойЗаписи, УчетнаяЗапись);
		
		ЗапросРеквизитыПоСсылкам = Новый Запрос("ВЫБРАТЬ
		                                        |	"""" КАК ИД,
		                                        |	"""" КАК Код,
		                                        |	"""" КАК Наименование,
		                                        |	"""" КАК Представление,
		                                        |	"""" КАК Сертификат,
		                                        |	"""" КАК Адрес1,
		                                        |	"""" КАК Адрес2,
		                                        |	"""" КАК Адрес3");
		
		Если ЗначениеЗаполнено(УчетнаяЗапись.НалоговыйОрган) Тогда
			
			ЗапросРеквизитыПоСсылкам.Текст = ЗапросРеквизитыПоСсылкам.Текст + "
			                                        |
			                                        |ОБЪЕДИНИТЬ ВСЕ
			                                        |
			                                        |ВЫБРАТЬ
			                                        |	&ИДНалоговыйОрган,
			                                        |	НалоговыеОрганы.Код,
			                                        |	НалоговыеОрганы.Наименование,
			                                        |	НалоговыеОрганы.Представление,
			                                        |	"""",
			                                        |	НалоговыеОрганы.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками,
			                                        |	"""",
			                                        |	""""
			                                        |ИЗ
			                                        |	Справочник.НалоговыеОрганы КАК НалоговыеОрганы
			                                        |ГДЕ
			                                        |	НалоговыеОрганы.Ссылка = &НалоговыйОрган";
			ЗапросРеквизитыПоСсылкам.УстановитьПараметр("ИДНалоговыйОрган", "НалоговыйОрган");
			ЗапросРеквизитыПоСсылкам.УстановитьПараметр("НалоговыйОрган", УчетнаяЗапись.НалоговыйОрган);
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(УчетнаяЗапись.СерверДокументооборота) Тогда
			
			ЗапросРеквизитыПоСсылкам.Текст = ЗапросРеквизитыПоСсылкам.Текст + "
			                                        |
			                                        |ОБЪЕДИНИТЬ ВСЕ
			                                        |
			                                        |ВЫБРАТЬ
			                                        |	&ИДСерверДокументооборота,
			                                        |	"""",
			                                        |	"""",
			                                        |	СерверыДокументооборота.Представление,
			                                        |	СерверыДокументооборота.Сертификат,
			                                        |	СерверыДокументооборота.АдресЭлектроннойПочтыФНС,
			                                        |	СерверыДокументооборота.АдресЭлектроннойПочтыПФР,
			                                        |	СерверыДокументооборота.АдресЭлектроннойПочтыФСГС
			                                        |ИЗ
			                                        |	Справочник.СерверыДокументооборота КАК СерверыДокументооборота
			                                        |ГДЕ
			                                        |	СерверыДокументооборота.Ссылка = &СерверДокументооборота";
			ЗапросРеквизитыПоСсылкам.УстановитьПараметр("ИДСерверДокументооборота", "СерверДокументооборота");
			ЗапросРеквизитыПоСсылкам.УстановитьПараметр("СерверДокументооборота", УчетнаяЗапись.СерверДокументооборота);
			
		КонецЕсли;
		
		Выборка = ЗапросРеквизитыПоСсылкам.Выполнить().Выбрать();
		Пока Выборка.Следующий() Цикл
			
			ИД = Выборка.ИД;
			Если ИД = "НалоговыйОрган" Тогда
				СвойстваУчетнойЗаписи.НалоговыйОрганКод = Выборка.Код;
				СвойстваУчетнойЗаписи.НалоговыйОрганНаименование = Выборка.Наименование;
				СвойстваУчетнойЗаписи.НалоговыйОрганПредставление = Выборка.Представление;
				СвойстваУчетнойЗаписи.НалоговыйОрганАдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками = Выборка.Адрес1;
			ИначеЕсли ИД = "СерверДокументооборота" Тогда
				СвойстваУчетнойЗаписи.СерверДокументооборотаПредставление = Выборка.Представление;
				СвойстваУчетнойЗаписи.СерверДокументооборотаСертификат = Выборка.Сертификат;
				СвойстваУчетнойЗаписи.СерверДокументооборотаАдресЭлектроннойПочтыФНС = Выборка.Адрес1;
				СвойстваУчетнойЗаписи.СерверДокументооборотаАдресЭлектроннойПочтыПФР = Выборка.Адрес2;
				СвойстваУчетнойЗаписи.СерверДокументооборотаАдресЭлектроннойПочтыФСГС = Выборка.Адрес3;
			КонецЕсли;
			
		КонецЦикла;
		
	Иначе
		
		Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
		                      |	УчетныеЗаписиДокументооборота.Наименование КАК Наименование,
		                      |	УчетныеЗаписиДокументооборота.ОбменНапрямую КАК ОбменНапрямую,
		                      |	УчетныеЗаписиДокументооборота.ОбменНапрямуюСФСГС КАК ОбменНапрямуюСФСГС,
		                      |	УчетныеЗаписиДокументооборота.ИмяПользователяPOP3 КАК ИмяПользователяPOP3,
		                      |	УчетныеЗаписиДокументооборота.ПарольPOP3 КАК ПарольPOP3,
		                      |	УчетныеЗаписиДокументооборота.СерверPOP3 КАК СерверPOP3,
		                      |	УчетныеЗаписиДокументооборота.ИмяПользователяSMTP КАК ИмяПользователяSMTP,
		                      |	УчетныеЗаписиДокументооборота.ПарольSMTP КАК ПарольSMTP,
		                      |	УчетныеЗаписиДокументооборота.СерверSMTP КАК СерверSMTP,
		                      |	УчетныеЗаписиДокументооборота.ТребуетсяSMTPАутентификация КАК ТребуетсяSMTPАутентификация,
		                      |	УчетныеЗаписиДокументооборота.АдресЭлектроннойПочты КАК АдресЭлектроннойПочты,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган КАК НалоговыйОрган,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота КАК СерверДокументооборота,
		                      |	УчетныеЗаписиДокументооборота.СертификатРуководителя КАК СертификатРуководителя,
		                      |	УчетныеЗаписиДокументооборота.СертификатГлавногоБухгалтера КАК СертификатГлавногоБухгалтера,
		                      |	УчетныеЗаписиДокументооборота.СертификатДляШифрования КАК СертификатДляШифрования,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФНС КАК ПредназначенаДляДокументооборотаСФНС,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСПФР КАК ПредназначенаДляДокументооборотаСПФР,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФСГС КАК ПредназначенаДляДокументооборотаСФСГС,
		                      |	УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСЦБ КАК ПредназначенаДляДокументооборотаСЦБ,
		                      |	УчетныеЗаписиДокументооборота.ИдентификаторАбонента КАК ИдентификаторАбонента,
		                      |	УчетныеЗаписиДокументооборота.ИдентификаторСпецоператора КАК ИдентификаторСпецоператора,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Представление КАК НалоговыйОрганПредставление,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками КАК НалоговыйОрганАдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Код КАК НалоговыйОрганКод,
		                      |	УчетныеЗаписиДокументооборота.НалоговыйОрган.Наименование КАК НалоговыйОрганНаименование,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота.Представление КАК СерверДокументооборотаПредставление,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота.АдресЭлектроннойПочтыФНС КАК СерверДокументооборотаАдресЭлектроннойПочтыФНС,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота.АдресЭлектроннойПочтыПФР КАК СерверДокументооборотаАдресЭлектроннойПочтыПФР,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота.АдресЭлектроннойПочтыФСГС КАК СерверДокументооборотаАдресЭлектроннойПочтыФСГС,
		                      |	УчетныеЗаписиДокументооборота.СерверДокументооборота.Сертификат КАК СерверДокументооборотаСертификат,
		                      |	УчетныеЗаписиДокументооборота.ЭтоМультиРежим КАК ЭтоМультиРежим
		                      |ИЗ
		                      |	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
		                      |ГДЕ
		                      |	УчетныеЗаписиДокументооборота.Ссылка = &Ссылка");
		Запрос.УстановитьПараметр("Ссылка", УчетнаяЗапись);
		РезультатЗапроса = Запрос.Выполнить();
		Если РезультатЗапроса.Пустой() Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не удалось извлечь из информационной базы информацию об учетной записи %1!'"), Символ(34) + УчетнаяЗапись + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		Выборка = РезультатЗапроса.Выбрать();
		Выборка.Следующий();
		
		ЗаполнитьЗначенияСвойств(СвойстваУчетнойЗаписи, Выборка);
		
	КонецЕсли;
	
	ПризнакОшибки = Ложь;
	
	// проверяем адрес электронной почты учетной записи
	Если ПустаяСтрока(СвойстваУчетнойЗаписи.АдресЭлектроннойПочты) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Для учетной записи %1 не задан адрес электронной почты!'"),
			Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	// проверяем параметры доступа к учетной записи на сервере документооборота
	Если ПустаяСтрока(СвойстваУчетнойЗаписи.ИмяПользователяPOP3) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Для учетной записи %1 не задано имя пользователя для доступа по протоколу POP3!'"),
			Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если ПустаяСтрока(СвойстваУчетнойЗаписи.ПарольPOP3) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Для учетной записи %1 не задан пароль для доступа по протоколу POP3!'"),
			Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если ПустаяСтрока(СвойстваУчетнойЗаписи.СерверPOP3) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Для учетной записи %1 не задан адрес сервера POP3!'"),
			Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если СвойстваУчетнойЗаписи.ТребуетсяSMTPАутентификация Тогда
	
		Если ПустаяСтрока(СвойстваУчетнойЗаписи.ИмяПользователяSMTP) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для учетной записи %1 требуется SMTP-аутентификация, но не задано имя пользователя для доступа по протоколу SMTP!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
		Если ПустаяСтрока(СвойстваУчетнойЗаписи.ПарольSMTP) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для учетной записи %1 требуется SMTP-аутентификация, но не задан пароль для доступа по протоколу SMTP!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	// проверяем свойства налогового органа
	Если СвойстваУчетнойЗаписи.ПредназначенаДляДокументооборотаСФНС Тогда
		
		Если НЕ ЗначениеЗаполнено(СвойстваУчетнойЗаписи.ИдентификаторАбонента) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для учетной записи %1 не задан идентификатор абонента!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
		Если СвойстваУчетнойЗаписи.НалоговыйОрган.Пустая() Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для учетной записи %1 не задан налоговый орган!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
			
		Иначе
			
			Если ПустаяСтрока(СвойстваУчетнойЗаписи.НалоговыйОрганКод) Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Для налогового органа %1, сопоставленного учетной записи %2, не задан код!'"),
					Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.НалоговыйОрганПредставление)+ Символ(34),
					Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ПризнакОшибки = Истина;
			КонецЕсли;
			
			Если СвойстваУчетнойЗаписи.ОбменНапрямую И ПустаяСтрока(СвойстваУчетнойЗаписи.НалоговыйОрганАдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками) Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Для налогового органа %1, сопоставленного учетной записи %2, не задан адрес электронной почты для целей обмена!'"),
					Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.НалоговыйОрганПредставление)+ Символ(34),
					Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ПризнакОшибки = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// проверяем свойства сервера документооборота
	Если СвойстваУчетнойЗаписи.СерверДокументооборота.Пустая() Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Для учетной записи %1 не задан сервер документооборота!'"),
			Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	Иначе
		
		Если СвойстваУчетнойЗаписи.ПредназначенаДляДокументооборотаСФНС И ПустаяСтрока(СвойстваУчетнойЗаписи.СерверДокументооборотаСертификат) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для сервера документооборота %1, сопоставленного учетной записи %2, не задан цифровой сертификат!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.СерверДокументооборотаПредставление)+ Символ(34),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
		Если СвойстваУчетнойЗаписи.ПредназначенаДляДокументооборотаСФНС И ПустаяСтрока(СвойстваУчетнойЗаписи.СерверДокументооборотаАдресЭлектроннойПочтыФНС) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для сервера документооборота %1, сопоставленного учетной записи %2, не задан адрес электронной почты для целей обмена с ФНС!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.СерверДокументооборотаПредставление)+ Символ(34),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
		Если СвойстваУчетнойЗаписи.ПредназначенаДляДокументооборотаСПФР И ПустаяСтрока(СвойстваУчетнойЗаписи.СерверДокументооборотаАдресЭлектроннойПочтыПФР) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для сервера документооборота %1, сопоставленного учетной записи %2, не задан адрес электронной почты для целей обмена с ПФР!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.СерверДокументооборотаПредставление)+ Символ(34),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
		Если СвойстваУчетнойЗаписи.ПредназначенаДляДокументооборотаСФСГС И ПустаяСтрока(СвойстваУчетнойЗаписи.СерверДокументооборотаАдресЭлектроннойПочтыФСГС) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для сервера документооборота %1, сопоставленного учетной записи %2, не задан адрес электронной почты для целей обмена с Росстатом!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.СерверДокументооборотаПредставление)+ Символ(34),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	Если СвойстваУчетнойЗаписи.ЭтоМультиРежим <> Истина Тогда
		Если ПустаяСтрока(СвойстваУчетнойЗаписи.СертификатРуководителя) Тогда //+
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для учетной записи %1 не задан сертификат руководителя!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
		
		Если ПустаяСтрока(СвойстваУчетнойЗаписи.СертификатДляШифрования) Тогда //+
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для учетной записи %1 не задан сертификат для целей шифрования!'"),
				Символ(34) + СокрЛП(СвойстваУчетнойЗаписи.Наименование) + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Возврат (НЕ ПризнакОшибки);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ОтправитьСообщенияПоУчетнойЗаписи(
	Знач УчетнаяЗапись,
	Знач ПочтовыйПрофиль = Неопределено,
	ТихийРежим = Ложь,
	ТребуетсяПроверкаУчетнойЗаписи = Ложь,
	ТребуетсяАвтонастройка = Истина) Экспорт
	
	Если УчетнаяЗапись = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не задана учетная запись для получения сообщений!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат;
	КонецЕсли;
		
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		
		ссылкаУчетнаяЗапись = УчетнаяЗаписьОрганизации(УчетнаяЗапись);
		Если ссылкаУчетнаяЗапись.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для организации %1 не предусмотрена возможность использования встроенного механизма информационного взаимодействия с ФНС, ПФР и Росстатом по каналам связи.'"),
				Символ(34) + ссылкаУчетнаяЗапись + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат;
		КонецЕсли;
		
		Организация = УчетнаяЗапись;
		УчетнаяЗапись = ссылкаУчетнаяЗапись;
		
	Иначе
		Организация = Неопределено;
	КонецЕсли;
		
	Если ТребуетсяПроверкаУчетнойЗаписи И НЕ УчетнаяЗаписьНастроенаВерно(УчетнаяЗапись) Тогда
		Возврат;
	КонецЕсли;
	
	КомпонентаПочтыСоздаетсяВПроцедуре = (ПочтовыйПрофиль = Неопределено);
	Если КомпонентаПочтыСоздаетсяВПроцедуре Тогда
		ПочтовыйПрофиль = ИнициализироватьСвойстваПочты(УчетнаяЗапись, , Истина);
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	ДокументТранспортноеСообщение.Ссылка
	                      |ИЗ
	                      |	Документ.ТранспортноеСообщение КАК ДокументТранспортноеСообщение
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
	                      |		ПО (ТранспортныеКонтейнеры.ТранспортноеСообщение = ДокументТранспортноеСообщение.Ссылка)
	                      |ГДЕ
	                      |	ДокументТранспортноеСообщение.Статус = &Статус
	                      |	И ДокументТранспортноеСообщение.ПометкаУдаления = &ПометкаУдаления
	                      |	И ДокументТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
	                      |	И ДокументТранспортноеСообщение.Тип В(&ТипыАвтоотправляемыхСообщений)
	                      |	И ДокументТранспортноеСообщение.ЦиклОбмена.ДатаЗакрытия = &ДатаЗакрытия
	                      |	И ДокументТранспортноеСообщение.ЦиклОбмена.ПометкаУдаления = &ПометкаУдаления
	                      |
	                      |УПОРЯДОЧИТЬ ПО
	                      |	ДокументТранспортноеСообщение.ЦиклОбмена.ДатаСоздания,
	                      |	ДокументТранспортноеСообщение.Тип.Порядок");
	
	ТипыСообщений = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ТипыАвтоотправляемыхСообщенийПоУчетнойЗаписи(УчетнаяЗапись);
	
	Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыПисем.Исходящее);
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ДатаЗакрытия", ПолучитьПустуюДатуЗавершенияЦиклаОбмена());
	Запрос.УстановитьПараметр("ТипыАвтоотправляемыхСообщений", ТипыСообщений);
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	КоличествоОтправленных = 0;
	КоличествоНеОтправленных = 0;

	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Пока Выборка.Следующий() Цикл
		
		Если КонтекстЭДОСервер.ОтправитьТранспортноеСообщение(Выборка.Ссылка, ПочтовыйПрофиль, ТихийРежим) Тогда
			КоличествоОтправленных = КоличествоОтправленных + 1;
		Иначе
			КоличествоНеОтправленных = КоличествоНеОтправленных + 1;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ВидЦиклаОбменаПоСообщению(Сообщение)
	
	Возврат Сообщение.ЦиклОбмена.Вид;
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакет(ОповещениеОЗавершении, Сообщение, Оповещать = Ложь) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ПараметрыОперации = Новый Структура;

	ВидЦиклаОбмена = ВидЦиклаОбменаПоСообщению(Сообщение);
	Если ВидЦиклаОбмена = СтруктураСсылочныхДанных.Перечисления_ВидыЦикловОбмена_ЦиклОбменаСПФР Тогда
		РежимПодтвержденияОблачнойПодписи("СформироватьПакетПФР", Сообщение, ПараметрыОперации);
		СформироватьПакетПФР(ОповещениеОЗавершении, Сообщение, Оповещать, ПараметрыОперации);
	ИначеЕсли ВидЦиклаОбмена = СтруктураСсылочныхДанных.Перечисления_ВидыЦикловОбмена_ЦиклОбменаСФСГС Тогда
		РежимПодтвержденияОблачнойПодписи("СформироватьПакетФСГС", Сообщение, ПараметрыОперации);
		СформироватьПакетФСГС(ОповещениеОЗавершении, Сообщение, Оповещать, ПараметрыОперации);
	ИначеЕсли ВидЦиклаОбмена = СтруктураСсылочныхДанных.Перечисления_ВидыЦикловОбмена_ЦиклОбменаСФНС Тогда
		РежимПодтвержденияОблачнойПодписи("СформироватьПакетФНС", Сообщение, ПараметрыОперации);
		СформироватьПакетФНС(ОповещениеОЗавершении, Сообщение, Оповещать, ПараметрыОперации);
	ИначеЕсли ВидЦиклаОбмена = СтруктураСсылочныхДанных.Перечисления_ВидыЦикловОбмена_ЦиклОбменаССОС Тогда
		РежимПодтвержденияОблачнойПодписи("СформироватьПакетСОС", Сообщение, ПараметрыОперации);
		СформироватьПакетСОС(ОповещениеОЗавершении, Сообщение, Оповещать, ПараметрыОперации);
	Иначе
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСообщенияКШифрованию(УчетнаяЗапись) Экспорт
	
	Результат = Новый Массив;
	
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		
		ссылкаУчетнаяЗапись = УчетнаяЗаписьОрганизации(УчетнаяЗапись);
		Если ссылкаУчетнаяЗапись.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для организации %1 не предусмотрена возможность использования встроенного механизма защищенного документооборота с ФНС, ПФР и Росстатом по каналам связи.'"),
				Символ(34) + ссылкаУчетнаяЗапись + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		Организация = УчетнаяЗапись;
		УчетнаяЗапись = ссылкаУчетнаяЗапись;
		
	Иначе
		Организация = Неопределено;
	КонецЕсли;
	
	ТипыСообщений = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ТипыАвтоотправляемыхСообщенийПоУчетнойЗаписи(УчетнаяЗапись);
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	                      |	ДокументТранспортноеСообщение.Ссылка,
	                      |	ДокументТранспортноеСообщение.Тип
	                      |ИЗ
	                      |	Документ.ТранспортноеСообщение КАК ДокументТранспортноеСообщение
	                      |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
	                      |		ПО (ТранспортныеКонтейнеры.ТранспортноеСообщение = ДокументТранспортноеСообщение.Ссылка)
	                      |		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
	                      |		ПО (СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение = ДокументТранспортноеСообщение.Ссылка)
	                      |ГДЕ
	                      |	ДокументТранспортноеСообщение.Статус = &Статус
	                      |	И ДокументТранспортноеСообщение.ПометкаУдаления = &ПометкаУдаления
	                      |	И ДокументТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
	                      |	И ДокументТранспортноеСообщение.Тип В(&ТипыАвтоотправляемыхСообщений)
	                      |	И ДокументТранспортноеСообщение.ЦиклОбмена.ДатаЗакрытия = &ДатаЗакрытия
	                      |	И ДокументТранспортноеСообщение.ЦиклОбмена.ПометкаУдаления = &ПометкаУдаления
	                      |	И (ЕСТЬNULL(СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение, ИСТИНА) <> ИСТИНА ИЛИ ДокументТранспортноеСообщение.Тип В (&ТипыПринудительноОтправляемыеСообщения))
	                      |	И ЕСТЬNULL(ТранспортныеКонтейнеры.ТранспортноеСообщение, ИСТИНА) = ИСТИНА
	                      |
	                      |УПОРЯДОЧИТЬ ПО
	                      |	ДокументТранспортноеСообщение.ЦиклОбмена.ДатаСоздания,
	                      |	ДокументТранспортноеСообщение.Тип.Порядок");
	
	Запрос.УстановитьПараметр("Статус", Перечисления.СтатусыПисем.Исходящее);
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ДатаЗакрытия", ПолучитьПустуюДатуЗавершенияЦиклаОбмена());
	Запрос.УстановитьПараметр("ТипыАвтоотправляемыхСообщений", ТипыСообщений);
	
	// у протокола ПФР может не быть содержимого, но его все равно надо отправлять
	ПринудительноОтправляемыеСообщения = Новый Массив;
	Права = Мультирежим.ПраваНаУчетнуюЗапись(УчетнаяЗапись);
	Если Права.ЭтоМультиРежим И Права.СдаватьВПФР ИЛИ НЕ Права.ЭтоМультиРежим Тогда
		ПринудительноОтправляемыеСообщения.Добавить(Перечисления.ТипыТранспортныхСообщений.ПротоколКвитанцияПФР);
		ПринудительноОтправляемыеСообщения.Добавить(Перечисления.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР);
	КонецЕсли;
	Запрос.УстановитьПараметр("ТипыПринудительноОтправляемыеСообщения", ПринудительноОтправляемыеСообщения);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Результат.Добавить(Выборка.Ссылка);
	КонецЦикла;
	
	Возврат ?(ЗначениеЗаполнено(Результат), Результат, Неопределено);
	
КонецФункции

&НаКлиенте
Процедура ЗашифроватьСообщенияПоУчетнойЗаписи(ВыполняемоеОповещение, Знач УчетнаяЗапись, ТихийРежим = Ложь) Экспорт
	
	Если УчетнаяЗапись = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не задана учетная запись для подготовки сообщений!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	СообщенияКШифрованию = ПолучитьСообщенияКШифрованию(УчетнаяЗапись);
	Если СообщенияКШифрованию = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;

	Контекст = Новый Структура;
	Контекст.Вставить("СообщенияКШифрованию", СообщенияКШифрованию);
	Контекст.Вставить("ТекущееСообщение", 0);
	Контекст.Вставить("КоличествоЗашифрованных", 0);
	Контекст.Вставить("КоличествоНеЗашифрованных", 0);
	Контекст.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	
	ЗашифроватьСледующееСообщениеПоУчетнойЗаписи(Контекст);

КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьСледующееСообщениеПоУчетнойЗаписи(ВходящийКонтекст)
	
	Если ВходящийКонтекст.ТекущееСообщение < ВходящийКонтекст.СообщенияКШифрованию.Количество() Тогда
		
		СообщениеКШифрованию = ВходящийКонтекст.СообщенияКШифрованию[ВходящийКонтекст.ТекущееСообщение];
		
		Оповещение = Новый ОписаниеОповещения(
			"ЗашифроватьСледующееСообщениеПоУчетнойЗаписиПослеФормированияСообщения", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		СформироватьПакет(Оповещение, СообщениеКШифрованию);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ЗашифроватьСледующееСообщениеПоУчетнойЗаписи() Экспорт
	
	ВходящийКонтекст = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ВходящийКонтекстЗашифроватьСледующееСообщениеПоУчетнойЗаписи");
	
	ЗашифроватьСледующееСообщениеПоУчетнойЗаписи(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗашифроватьСледующееСообщениеПоУчетнойЗаписиПослеФормированияСообщения(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Истина Тогда
		ВходящийКонтекст.КоличествоЗашифрованных = ВходящийКонтекст.КоличествоЗашифрованных + 1;
	Иначе
		ВходящийКонтекст.КоличествоНеЗашифрованных = ВходящийКонтекст.КоличествоНеЗашифрованных + 1;
	КонецЕсли;
	ВходящийКонтекст.ТекущееСообщение = ВходящийКонтекст.ТекущееСообщение + 1;
	
	Если ВходящийКонтекст.ТекущееСообщение % 10 = 0 Тогда
		// для предотвращения переполнения стека
		ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ВходящийКонтекст,
			"БРО.ВходящийКонтекстЗашифроватьСледующееСообщениеПоУчетнойЗаписи");
		ПодключитьОбработчикОжидания("Подключаемый_ЗашифроватьСледующееСообщениеПоУчетнойЗаписи", 0.1, Истина);
	Иначе
		ЗашифроватьСледующееСообщениеПоУчетнойЗаписи(ВходящийКонтекст);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоУчетнойЗаписи(
		ВыполняемоеОповещение,
		парамУчетнаяЗапись,
		СписокПолученныхТС = Неопределено) Экспорт
		
	ТихийРежим = Ложь;
		
	// проверяем, включена ли возможность документооборота
	Если ТипЗнч(парамУчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		ссылкаУчетнаяЗапись = УчетнаяЗаписьОрганизации(парамУчетнаяЗапись);
		Если НЕ ЗначениеЗаполнено(ссылкаУчетнаяЗапись) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Для организации %1 не предусмотрена возможность информационного взаимодействия с ФНС, ПФР и Росстатом по каналам связи.'"),
				Символ(34) + парамУчетнаяЗапись + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
			Возврат;
		КонецЕсли;
		УчетнаяЗапись = ссылкаУчетнаяЗапись;
	Иначе
		УчетнаяЗапись = парамУчетнаяЗапись;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
		"АвтоматическаяНастройкаУчетнойЗаписи", 
		,
		, 
		УчетнаяЗапись);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("УчетнаяЗапись",         УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ТихийРежим",            ТихийРежим);
	ДополнительныеПараметры.Вставить("СписокПолученныхТС",    СписокПолученныхТС);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоУчетнойЗаписиПослеАвтоматическойНастройкиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);

	АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗапись, ,ОписаниеОповещения, , , ТихийРежим);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоУчетнойЗаписиПослеАвтоматическойНастройкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	УчетнаяЗапись         = ДополнительныеПараметры.УчетнаяЗапись;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ТихийРежим            = ДополнительныеПараметры.ТихийРежим;
	СписокПолученныхТС    = ДополнительныеПараметры.СписокПолученныхТС;
	
	Если НЕ УчетнаяЗаписьНастроенаВерно(УчетнаяЗапись) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписи", 
			,
			, 
			УчетнаяЗапись);
		
		// получаем сообщения
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ТихийРежим",            ТихийРежим);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись",         УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);

		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОсуществитьОбменПоУчетнойЗаписиЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
			
		ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписи(
			ОписаниеОповещения,
			УчетнаяЗапись,
			,
			ТихийРежим,
			,
			Ложь,
			Ложь,
			СписокПолученныхТС);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоУчетнойЗаписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ТихийРежим = ДополнительныеПараметры.ТихийРежим;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоУчетнойЗаписиПослеШифрования", 
		ЭтотОбъект, 
		ДополнительныеПараметры); 
	
	// шифруем сообщения по учетной записи
	Если НЕ ТихийРежим Тогда
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ЗашифроватьСообщенияПоУчетнойЗаписи", 
			, 
			, 
			УчетнаяЗапись);
		
		ЗашифроватьСообщенияПоУчетнойЗаписи(ОписаниеОповещения, УчетнаяЗапись, ТихийРежим);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоУчетнойЗаписиПослеШифрования(Результат, ДополнительныеПараметры) Экспорт
	
	ТихийРежим 				= ДополнительныеПараметры.ТихийРежим;
	УчетнаяЗапись 			= ДополнительныеПараметры.УчетнаяЗапись;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
		"ОтправитьСообщенияПоУчетнойЗаписи", 
		, 
		, 
		УчетнаяЗапись);
	
	// отправляем сообщения
	ОтправитьСообщенияПоУчетнойЗаписи(УчетнаяЗапись, , ТихийРежим, Ложь, Ложь);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменСЭДОПоОрганизации(
		Форма,
		Организация,
		ВыполняемоеОповещение,
		КонтролирующийОрган = Неопределено,
		СсылкаНаОтправляемыйОбъект = Неопределено,
		ТипСообщенияОтвета = Неопределено) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация",                Организация);
	ДополнительныеПараметры.Вставить("Форма",                      Форма);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",      ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("КонтролирующийОрган",        КонтролирующийОрган);
	ДополнительныеПараметры.Вставить("СсылкаНаОтправляемыйОбъект", СсылкаНаОтправляемыйОбъект);
	ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",         ТипСообщенияОтвета);
	
	Права = МультирежимВызовСервера.ПраваНаУчетнуюЗапись(УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("Права", Права);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоОрганизацииСЭДОСФР",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииСЭДОСФР(Результат, ДополнительныеПараметры) Экспорт
	
	Организация					= ДополнительныеПараметры.Организация;
	КонтролирующийОрган			= ДополнительныеПараметры.КонтролирующийОрган;
	ИмяФормыВызова				= ДополнительныеПараметры.Форма.ИмяФормы;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоОрганизацииСЭДОСФРЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ТекущиеУчетныеЗаписиСтрахователяФСС = ПолучитьТекущиеУчетныеЗаписиСтрахователяФСС();
	ОрганизацияПодключенаКФСС = ТекущиеУчетныеЗаписиСтрахователяФСС.Найти(Организация);
	
	Если Не ОбменПрерванПользователем И ИмяФормыВызова = "ОбщаяФорма.РегламентированнаяОтчетность"
		И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "ФСС")
		И НЕ ОрганизацияПодключенаКФСС = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ИзменитьПараметрыДлительнойОтправки(
			"ТекущаяОрганизация",
			Организация);
		ПолучитьВходящиеСообщенияСЭДОФСС(
			ОписаниеОповещения,
			Организация,
			Ложь,
			Ложь);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииСЭДОСФРЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьЗначениеДобавитьОтдельнуюКнопкуОбменаДляСЭДО()

	Возврат
		ЭлектронныйДокументооборотСФСС.ПолучитьЗначениеДобавитьОтдельнуюКнопкуОбменаДляСЭДО();
	
КонецФункции

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизации(
		Форма,
		Организация,
		ВыполняемоеОповещение,
		КонтролирующийОрган = Неопределено,
		СсылкаНаОтправляемыйОбъект = Неопределено,
		ТипСообщенияОтвета = Неопределено) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация",                Организация);
	ДополнительныеПараметры.Вставить("Форма",                      Форма);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",      ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("КонтролирующийОрган",        КонтролирующийОрган);
	ДополнительныеПараметры.Вставить("СсылкаНаОтправляемыйОбъект", СсылкаНаОтправляемыйОбъект);
	ДополнительныеПараметры.Вставить("ТипСообщенияОтвета",         ТипСообщенияОтвета);
	
	Права = МультирежимВызовСервера.ПраваНаУчетнуюЗапись(УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("Права", Права);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоОрганизацииФСС",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	Если Не ОбменПрерванПользователем И ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись)
		И ЗначениеЗаполнено(УчетнаяЗапись) И 
		(КонтролирующийОрган = Неопределено 
		ИЛИ КонтролирующийОрган = "ФНС" 
		ИЛИ КонтролирующийОрган = "ПФР" 
		ИЛИ КонтролирующийОрган = "ФСГС") Тогда
		
		ОсуществитьОбменПоУчетнойЗаписи(
			ОписаниеОповещения,
			УчетнаяЗапись);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииФСС(
	Результат,
	ДополнительныеПараметры) Экспорт
	
	Организация 				= ДополнительныеПараметры.Организация;
	КонтролирующийОрган 		= ДополнительныеПараметры.КонтролирующийОрган;
	СсылкаНаОтправляемыйОбъект 	= ДополнительныеПараметры.СсылкаНаОтправляемыйОбъект;
	ИмяФормыВызова				= ДополнительныеПараметры.Форма.ИмяФормы;
	
	Права = ДополнительныеПараметры.Права;
	ЕстьДоступ = Права.ЭтоМультиРежим И Права.СдаватьВФСС ИЛИ НЕ Права.ЭтоМультиРежим;
	
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииФСС",
			,
			,
			Организация);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоОрганизацииФССПолучитьВходящиеСообщенияСЭДОФСС",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	// ФСС
	Если Не ОбменПрерванПользователем И ЕстьДоступ 
		И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "ФСС") Тогда
		Если СсылкаНаОтправляемыйОбъект = Неопределено Тогда
			// Выполняем отправку для нескольких ссылок
			Отправки = НезавершенныеПоследниеОтправкиФССПоОрганизации(Организация);
		Иначе
			// Выполняем отправку только для одной ссылки 
			Отправки = Новый Массив;
			Отправка = ПолучитьПоследнююОтправкуОтчетаВФСС(СсылкаНаОтправляемыйОбъект);
			Если Отправка <> Неопределено Тогда 
				Отправки.Добавить(Отправка);
			КонецЕсли;
		КонецЕсли;
		
		Если Отправки = Неопределено Или Отправки.Количество() = 0 Тогда
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		Иначе
			ОбновитьРезультатНезавершенныхПоследнихОтправокФССПоОрганизации(Отправки, 0, ОписаниеОповещения);
		КонецЕсли;
	Иначе
		// После ФСС выполняем обмен по РПН
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииФССПолучитьВходящиеСообщенияСЭДОФСС(Результат, ДополнительныеПараметры) Экспорт
	
	Организация					= ДополнительныеПараметры.Организация;
	КонтролирующийОрган			= ДополнительныеПараметры.КонтролирующийОрган;
	ИмяФормыВызова				= ДополнительныеПараметры.Форма.ИмяФормы;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОсуществитьОбменПоОрганизацииРПН",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ТекущиеУчетныеЗаписиСтрахователяФСС = ПолучитьТекущиеУчетныеЗаписиСтрахователяФСС();
	ОрганизацияПодключенаКФСС = ТекущиеУчетныеЗаписиСтрахователяФСС.Найти(Организация);
	
	// Выполняем обмен с ФСС только если не включена дополнительная кнопка обмена.
	ДобавитьОтдельнуюКнопкуОбменаДляСЭДО = ПолучитьЗначениеДобавитьОтдельнуюКнопкуОбменаДляСЭДО();
	Если Не ОбменПрерванПользователем И ИмяФормыВызова = "ОбщаяФорма.РегламентированнаяОтчетность"
		И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "ФСС")
		И НЕ ОрганизацияПодключенаКФСС = Неопределено
		И НЕ ДобавитьОтдельнуюКнопкуОбменаДляСЭДО Тогда
		ДлительнаяОтправкаКлиентСервер.ИзменитьПараметрыДлительнойОтправки(
			"ТекущаяОрганизация",
			Организация);
		ПолучитьВходящиеСообщенияСЭДОФСС(
			ОписаниеОповещения,
			Организация,
			Ложь,
			Ложь, , ,
			Ложь);
	Иначе
		// После ФСС выполняем обмен по РПН
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокФССПоОрганизации(Отправки, НомерОбъекта, ВыполняемоеОповещение)
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииФСС", 
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"), 
			Отправка);
			
		ДополнительныеПараметры = Новый Структура("Отправки, НомерОбъекта, ВыполняемоеОповещение", Отправки, НомерОбъекта, ВыполняемоеОповещение);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатНезавершенныхПоследнихОтправокФССПоОрганизацииЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка) Тогда
			ИспользоватьКеширование = НомерОбъекта > 0;
			ОбновитьРезультатКонкретнойОтправкиФСС(
				Отправка,
				Истина,
				ОписаниеОповещения,
				ИспользоватьКеширование,
				НомерОбъекта);
			
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокФССПоОрганизацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Отправки = ДополнительныеПараметры.Отправки;
	НомерОбъекта = ДополнительныеПараметры.НомерОбъекта;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокФССПоОрганизации(Отправки, НомерОбъекта + 1, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииФСРАР(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 						= ДополнительныеПараметры.Форма;
	Организация 				= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	КонтролирующийОрган 		= ДополнительныеПараметры.КонтролирующийОрган;
	СсылкаНаОтправляемыйОбъект 	= ДополнительныеПараметры.СсылкаНаОтправляемыйОбъект;
	
	Права = ДополнительныеПараметры.Права;
	ЕстьДоступ = Права.ЭтоМультиРежим И Права.ПодатьЗаявкуНаСертификатДляФСРАР ИЛИ НЕ Права.ЭтоМультиРежим;
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОсуществитьОбменПоОрганизацииБанкРоссии", ЭтотОбъект, ДополнительныеПараметры);
	
	// ФСРАР
	Если Не ОбменПрерванПользователем И ЕстьДоступ 
		И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "ФСРАР") Тогда

		Если СсылкаНаОтправляемыйОбъект = Неопределено Тогда
			// Выполняем отправку для нескольких ссылок
			Отправки = НезавершенныеПоследниеОтправкиФСРАРПоОрганизации(Организация);
		Иначе
			// Выполняем отправку только для одной ссылки 
			Отправки = Новый Массив;
			Отправка = ПолучитьПоследнююОтправкуОтчетаВФСРАР(СсылкаНаОтправляемыйОбъект);
			Если Отправка <> Неопределено Тогда 
				Отправки.Добавить(Отправка);
			КонецЕсли;
		КонецЕсли;
		
		Если Отправки = Неопределено Или Отправки.Количество() = 0 Тогда
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
			
		Иначе
			ЭтоВызовИзСписка = (СсылкаНаОтправляемыйОбъект = Неопределено);
			ОбновитьРезультатНезавершенныхПоследнихОтправокФСРАРПоОрганизации(Форма, Отправки, 0, ЭтоВызовИзСписка, ОписаниеОповещения);
		КонецЕсли;
	Иначе
		// После ФСРАР возвращаемся в самое первое место вызова функции отправки 
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокФСРАРПоОрганизации(Форма, Отправки, НомерОбъекта, ЭтоВызовИзСписка, ВыполняемоеОповещение)
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииФСРАР", 
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР"), 
			Отправка);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Форма", 					Форма);
		ДополнительныеПараметры.Вставить("Отправки", 				Отправки);
		ДополнительныеПараметры.Вставить("НомерОбъекта", 			НомерОбъекта);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка", 		ЭтоВызовИзСписка);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатНезавершенныхПоследнихОтправокФСРАРПоОрганизацииЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка) Тогда
			ОбновитьРезультатКонкретнойОтправкиФСРАР(Форма, Отправка, ОписаниеОповещения,, ЭтоВызовИзСписка);
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокФСРАРПоОрганизацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 					= ДополнительныеПараметры.Форма;
	Отправки 				= ДополнительныеПараметры.Отправки;
	НомерОбъекта 			= ДополнительныеПараметры.НомерОбъекта;
	ЭтоВызовИзСписка 		= ДополнительныеПараметры.ЭтоВызовИзСписка;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокФСРАРПоОрганизации(Форма, Отправки, НомерОбъекта + 1, ЭтоВызовИзСписка, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииРПН(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 						= ДополнительныеПараметры.Форма;
	Организация 				= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	КонтролирующийОрган 		= ДополнительныеПараметры.КонтролирующийОрган;
	СсылкаНаОтправляемыйОбъект 	= ДополнительныеПараметры.СсылкаНаОтправляемыйОбъект;
	
	Права = ДополнительныеПараметры.Права;
	ЕстьДоступ = Права.ЭтоМультиРежим И Права.ПодатьЗаявкуНаПодключениеРПН ИЛИ НЕ Права.ЭтоМультиРежим;
	
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииФСС",
			,
			,
			Организация);
	
	Если НЕ Результат = Неопределено И Результат.ОшибкиПоОрганизациям.Количество() > 0 Тогда
		ДлительнаяОтправкаКлиентСервер.ИзменитьПараметрыДлительнойОтправки("ТекущаяОрганизация", Организация);
		Для каждого Ошибка из Результат.ОшибкиПоОрганизациям[Организация] Цикл
			ЭлектронныйДокументооборотСФССКлиентСервер.ВывестиОшибку(Ошибка);
		КонецЦикла;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОсуществитьОбменПоОрганизацииФТС", ЭтотОбъект, ДополнительныеПараметры);
	
	// РПН
	Если Не ОбменПрерванПользователем И ЕстьДоступ 
		И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "РПН") Тогда

		ИспользоватьОбмен = Неопределено;
		Если СсылкаНаОтправляемыйОбъект = Неопределено Тогда
			// Выполняем отправку для нескольких ссылок
			Отправки = НезавершенныеПоследниеОтправкиРПНПоОрганизации(Организация, ИспользоватьОбмен);
		Иначе
			// Выполняем отправку только для одной ссылки 
			Отправки = Новый Массив;
			Отправка = ПолучитьПоследнююОтправкуОтчетаВРПН(СсылкаНаОтправляемыйОбъект);
			Если Отправка <> Неопределено Тогда 
				Отправки.Добавить(Отправка);
			КонецЕсли;
		КонецЕсли;
		
		ЭтоВызовИзСписка = (СсылкаНаОтправляемыйОбъект = Неопределено);
		ОрганизацияПолученияУведомлений = ?(ИспользоватьОбмен = Истина, Организация, Неопределено);
		ОбновитьРезультатНезавершенныхПоследнихОтправокРПНПоОрганизации(
			Форма,
			Отправки,
			0,
			ЭтоВызовИзСписка,
			ОписаниеОповещения,
			ОрганизацияПолученияУведомлений);
		
	Иначе
		// После РПН выполняем обмен по ФТС
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

#Область БанкРоссии

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииБанкРоссии(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 						= ДополнительныеПараметры.Форма;
	Организация 				= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение 		= Новый ОписаниеОповещения("ОсуществитьОбменПоОрганизацииРЭЦ", ЭтотОбъект, ДополнительныеПараметры);
	КонтролирующийОрган 		= ДополнительныеПараметры.КонтролирующийОрган;
	
	Если Не ОбменПрерванПользователем И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "УдалитьБанкРоссии") Тогда
		Если ДокументооборотСКОКлиентСервер.ПодсистемаУдалитьЦБСуществует() Тогда
			МодульУдалитьДокументооборотСБанкомРоссииКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("УдалитьДокументооборотСБанкомРоссииКлиент");
			Отправки = МодульУдалитьДокументооборотСБанкомРоссииКлиент.ПолучитьНеЗавершенныеОтправки(Организация);
			ОбновитьРезультатНезавершенныхПоследнихОтправокБанкРоссииПоОрганизации(Форма, Отправки, 0, ВыполняемоеОповещение);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);	
		КонецЕсли
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокБанкРоссииПоОрганизации(Форма, Отправки, НомерОбъекта, ВыполняемоеОповещение)
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииБанкРоссии", 
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.УдалитьБанкРоссии"), 
			Отправка);
		
		ДополнительныеПараметры = Новый Структура("Форма, Отправки, НомерОбъекта, ВыполняемоеОповещение", Форма, Отправки, НомерОбъекта, ВыполняемоеОповещение);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатНезавершенныхПоследнихОтправокБанкРоссииПоОрганизацииЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка) Тогда
			Если ДокументооборотСКОКлиентСервер.ПодсистемаУдалитьЦБСуществует() Тогда
				МодульУдалитьДокументооборотСБанкомРоссииКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("УдалитьДокументооборотСБанкомРоссииКлиент");
				МодульУдалитьДокументооборотСБанкомРоссииКлиент.ПолучитьСтатусОтчета(ОписаниеОповещения, Отправка, Форма);
			КонецЕсли;
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокБанкРоссииПоОрганизацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	Отправки = ДополнительныеПараметры.Отправки;
	НомерОбъекта = ДополнительныеПараметры.НомерОбъекта;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокБанкРоссииПоОрганизации(Форма, Отправки, НомерОбъекта + 1, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииРЭЦ(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 					= ДополнительныеПараметры.Форма;
	Организация 			= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	КонтролирующийОрган 	= ДополнительныеПараметры.КонтролирующийОрган;
	ОписаниеОповещения 		= Новый ОписаниеОповещения("ОсуществитьОбменПоОрганизацииМЧДРР", ЭтотОбъект, ДополнительныеПараметры);
	
	Если Не ОбменПрерванПользователем И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "РЭЦ") Тогда
		Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ПроверкаВРЭЦ") Тогда
			МодульДокументооборотСРЭЦКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСРЭЦКлиент");
			Отправки = МодульДокументооборотСРЭЦКлиент.ПолучитьНеЗавершенныеОтправки(Организация);
			ОбновитьРезультатНезавершенныхПоследнихОтправокРЭЦПоОрганизации(Форма, Организация, Отправки, 0, ОписаниеОповещения);
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);	
		КонецЕсли
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокРЭЦПоОрганизации(Форма, Организация, Отправки, НомерОбъекта, ВыполняемоеОповещение)

	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииРЭЦ", 
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РЭЦ"), 
			Отправка);
		
		ДополнительныеПараметры = Новый Структура("Форма, Организация, Отправки, НомерОбъекта, ВыполняемоеОповещение", Форма, Организация, Отправки, НомерОбъекта, ВыполняемоеОповещение);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатНезавершенныхПоследнихОтправокРЭЦПоОрганизацииЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка) Тогда
			МодульДокументооборотСРЭЦКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСРЭЦКлиент");
			МодульДокументооборотСРЭЦКлиент.ПолучитьСтатусОтчета(ОписаниеОповещения, Организация, Отправка, Форма);
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокРЭЦПоОрганизацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	Организация = ДополнительныеПараметры.Организация;
	Отправки = ДополнительныеПараметры.Отправки;
	НомерОбъекта = ДополнительныеПараметры.НомерОбъекта;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокРЭЦПоОрганизации(Форма, Организация, Отправки, НомерОбъекта + 1, ВыполняемоеОповещение);
	
КонецПроцедуры


&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииМЧДРР(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 					= ДополнительныеПараметры.Форма;
	Организация 			= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение	= ДополнительныеПараметры.ВыполняемоеОповещение;
	КонтролирующийОрган 	= ДополнительныеПараметры.КонтролирующийОрган;
	ОписаниеОповещения 		= Новый ОписаниеОповещения("ОсуществитьОбменПоРегистрацииЮЛ", ЭтотОбъект, ДополнительныеПараметры);
	
	Если Не ОбменПрерванПользователем И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "ЦПРР") Тогда
		Отправки = ПолучитьНезавершенныеОтправкиМЧДРР(Организация);
		ОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРРПоОрганизации(Форма, Организация, Отправки, 0, ОписаниеОповещения);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРРПоОрганизации(
		Форма,
		Организация,
		Отправки,
		НомерОбъекта,
		ВыполняемоеОповещение)
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииМЧДРР",
			"ЦПРР",
			Отправка.Ссылка);
		
		ДополнительныеПараметры = Новый Структура("Форма, Организация, Отправки, НомерОбъекта, ВыполняемоеОповещение",
			Форма, Организация, Отправки, НомерОбъекта, ВыполняемоеОповещение);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРРПоОрганизацииЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка.Ссылка) Тогда
			Если Отправка.РежимыИспользования = "ФТС" Тогда
				ОбновитьМашиночитаемуюДоверенностьФТС(
					ОписаниеОповещения,
					Организация,,,
					Отправка.Ссылка);
					
			Иначе
				ДокументооборотСКОКлиент.ОбновитьСостояниеМЧДРР(ОписаниеОповещения, Отправка.Ссылка);
			КонецЕсли;
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРРПоОрганизацииЗавершение(
		Результат,
		ДополнительныеПараметры) Экспорт
	
	Если ДополнительныеПараметры.НомерОбъекта % 10 = 0 Тогда
		// для предотвращения переполнения стека
		ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ДополнительныеПараметры,
			"БРО.ВходящийКонтекстОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРР");
		ПодключитьОбработчикОжидания("Подключаемый_СбросСтекаОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРР",
			0.1, Истина);
		
	Иначе
		ОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРРПоОрганизации(
			ДополнительныеПараметры.Форма,
			ДополнительныеПараметры.Организация,
			ДополнительныеПараметры.Отправки,
			ДополнительныеПараметры.НомерОбъекта + 1,
			ДополнительныеПараметры.ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_СбросСтекаОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРР() Экспорт
	
	ДополнительныеПараметры = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ВходящийКонтекстОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРР");
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокМЧДРРПоОрганизации(
		ДополнительныеПараметры.Форма,
		ДополнительныеПараметры.Организация,
		ДополнительныеПараметры.Отправки,
		ДополнительныеПараметры.НомерОбъекта + 1,
		ДополнительныеПараметры.ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьНезавершенныеОтправкиМЧДРР(Организация) Экспорт
	
	Результат = Новый Массив;
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	МашиночитаемыеДоверенностиРаспределенныйРеестр.Ссылка КАК Ссылка,
		|	МашиночитаемыеДоверенностиРаспределенныйРеестр.РежимыИспользования КАК РежимыИспользования
		|ИЗ
		|	Справочник.МашиночитаемыеДоверенностиРаспределенныйРеестр КАК МашиночитаемыеДоверенностиРаспределенныйРеестр
		|ГДЕ
		|	НЕ МашиночитаемыеДоверенностиРаспределенныйРеестр.ПометкаУдаления
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Организация = &Организация
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыМашиночитаемойДоверенностиКО.ВРаботе)
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыМашиночитаемойДоверенностиКО.ОтказВРегистрации)
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыМашиночитаемойДоверенностиКО.ОшибкаРегистрации)
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыМашиночитаемойДоверенностиКО.ИстекСрокДействия)
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыМашиночитаемойДоверенностиКО.Зарегистрировано)
		|	И МашиночитаемыеДоверенностиРаспределенныйРеестр.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыМашиночитаемойДоверенностиКО.Отозвано)
		|УПОРЯДОЧИТЬ ПО
		|	МашиночитаемыеДоверенностиРаспределенныйРеестр.РежимыИспользования");
	
	Запрос.Параметры.Вставить("Организация", Организация);
	
	ТаблицаОтправок = Запрос.Выполнить().Выгрузить();
	Для каждого СтрокаТаблицы Из ТаблицаОтправок Цикл
		СтрокаРезультата = Новый Структура;
		СтрокаРезультата.Вставить("Ссылка", 				СтрокаТаблицы.Ссылка);
		СтрокаРезультата.Вставить("РежимыИспользования", 	СтрокаТаблицы.РежимыИспользования);
		Результат.Добавить(СтрокаРезультата);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции


&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляБанкаРоссии(Результат, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	Если ДополнительныеПараметры.Свойство("ОтчетСсылка") Тогда
		ОтчетаСсылка = ДополнительныеПараметры.ОтчетСсылка;
	Иначе
		ОтчетСсылка = ДополнительныеПараметры.СсылкаНаОтчет;
	КонецЕсли;
	ОрганизацияОтчета = ДополнительныеПараметры.ОрганизацияОтчета;
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетаСсылка);
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	
	Если ЭтоРегламентированныйОтчет Тогда
		
		Если ЭтоОтправкаИзФормыОтчетность Тогда
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляБанкРоссииРегламентированныйОтчет(ОрганизацияОтчета, ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность, ЭтоРегламентированныйОтчет);
		Иначе
			// отправляем только из записанной формы
			Если Форма.Модифицированность Тогда
				ДополнительныеПараметры = Новый Структура("ОрганизацияОтчета, ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность, ЭтоРегламентированныйОтчет", ОрганизацияОтчета, ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность, ЭтоРегламентированныйОтчет);
				ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляБанкаРоссии", ЭтотОбъект, ДополнительныеПараметры);
				Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
			Иначе
				ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляБанкРоссииРегламентированныйОтчет(ОрганизацияОтчета, ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность, ЭтоРегламентированныйОтчет);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляБанкРоссииРегламентированныйОтчет(ОрганизацияОтчета, ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность, ЭтоРегламентированныйОтчет)
	
	Если ДокументооборотСКОКлиентСервер.ПодсистемаУдалитьЦБСуществует() Тогда
		МодульУдалитьДокументооборотСБанкомРоссииКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("УдалитьДокументооборотСБанкомРоссииКлиент");
		Если ЭтоОтправкаИзФормыОтчетность Тогда
			МодульУдалитьДокументооборотСБанкомРоссииКлиент.ОтправитьОтчет(ОрганизацияОтчета, ОтчетСсылка, Форма);
		Иначе
			МодульУдалитьДокументооборотСБанкомРоссииКлиент.ОтправитьОтчет(
				Форма.СтруктураРеквизитовФормы.Организация,
				Форма.СтруктураРеквизитовФормы.мСохраненныйДок, Форма);
		КонецЕсли;	
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область РегистрацияЮЛ

&НаКлиенте
Процедура ОсуществитьОбменПоРегистрацииЮЛ(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 					= ДополнительныеПараметры.Форма;
	Организация 			= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение	= ДополнительныеПараметры.ВыполняемоеОповещение;
	КонтролирующийОрган 	= ДополнительныеПараметры.КонтролирующийОрган;
	СсылкаЗаявления			= ДополнительныеПараметры.СсылкаНаОтправляемыйОбъект;
	
	Права = ДополнительныеПараметры.Права;
	ЕстьДоступ = Права.ЭтоМультиРежим И Права.СдаватьВФНС ИЛИ НЕ Права.ЭтоМультиРежим;
	
	Если Не ОбменПрерванПользователем И ЕстьДоступ И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "РегистрацияЮЛФНС")
		И ОбщегоНазначенияКлиент.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ГосударственнаяРегистрацияВФНС") Тогда
		МодульДокументооборотРегистрацияВФНССлужебныйКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотРегистрацияВФНССлужебныйКлиент");
		ОповещениеЗавершения = Новый ОписаниеОповещения("ОсуществитьОбменПоРегистрацииЮЛЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(СсылкаЗаявления) Тогда
			МодульДокументооборотРегистрацияВФНССлужебныйКлиент.ОбновитьСостояниеЗаявленияНаРегистрациюЮЛ(ОповещениеЗавершения, СсылкаЗаявления);
		Иначе
			МодульДокументооборотРегистрацияВФНССлужебныйКлиент.ОбновитьСостояниеЗаявленийНаРегистрациюЮЛ(ОповещениеЗавершения, Организация);
		КонецЕсли;
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоРегистрацииЮЛЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	СсылкаЗаявления			= ДополнительныеПараметры.СсылкаНаОтправляемыйОбъект;
	ВыполняемоеОповещение	= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка обмена с сервисом ФНС.'") + Символы.ПС + Символы.ПС + Результат.Ошибка;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьПакетДокументовВФНСЗавершение(РезультатОтправки, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	ОтправкаСсылка = Неопределено;
	Если РезультатОтправки <> Неопределено Тогда
		ОтправкаСсылка = РезультатОтправки.ОтправкаСсылка;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОтправкаСсылка) Тогда
		Оповестить("Завершение обновления");
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатОтправки);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокРПНПоОрганизации(
		Форма,
		Отправки,
		НомерОбъекта,
		ЭтоВызовИзСписка,
		ВыполняемоеОповещение,
		ОрганизацияПолученияУведомлений = Неопределено)
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		Если ОрганизацияПолученияУведомлений = Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
			
		Иначе
			ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
				"ОсуществитьОбменПоОрганизацииРПН",
				ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН"));
			ПолучитьУведомленияРПН(ВыполняемоеОповещение, ОрганизацияПолученияУведомлений);
		КонецЕсли;
		
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииРПН",
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН"),
			Отправка);
			
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Форма", 							Форма);
		ДополнительныеПараметры.Вставить("Отправки", 						Отправки);
		ДополнительныеПараметры.Вставить("НомерОбъекта", 					НомерОбъекта);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка", 				ЭтоВызовИзСписка);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ОрганизацияПолученияУведомлений", ОрганизацияПолученияУведомлений);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбновитьРезультатНезавершенныхПоследнихОтправокРПНПоОрганизацииЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка) Тогда
			ОбновитьРезультатКонкретнойОтправкиРПН(Форма, Отправка, ОписаниеОповещения,, ЭтоВызовИзСписка);
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокРПНПоОрганизацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 							= ДополнительныеПараметры.Форма;
	Отправки 						= ДополнительныеПараметры.Отправки;
	НомерОбъекта 					= ДополнительныеПараметры.НомерОбъекта;
	ЭтоВызовИзСписка 				= ДополнительныеПараметры.ЭтоВызовИзСписка;
	ВыполняемоеОповещение 			= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОрганизацияПолученияУведомлений = ДополнительныеПараметры.ОрганизацияПолученияУведомлений;
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокРПНПоОрганизации(
		Форма,
		Отправки,
		НомерОбъекта + 1,
		ЭтоВызовИзСписка,
		ВыполняемоеОповещение,
		ОрганизацияПолученияУведомлений);
	
КонецПроцедуры

&НаКлиенте
Процедура ОсуществитьОбменПоОрганизацииФТС(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 						= ДополнительныеПараметры.Форма;
	Организация 				= ДополнительныеПараметры.Организация;
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	КонтролирующийОрган 		= ДополнительныеПараметры.КонтролирующийОрган;
	СсылкаНаОтправляемыйОбъект 	= ДополнительныеПараметры.СсылкаНаОтправляемыйОбъект;
	
	Права = ДополнительныеПараметры.Права;
	ЕстьДоступ = Права.ЭтоМультиРежим И Права.ПодатьЗаявкуНаПодключениеФТС ИЛИ НЕ Права.ЭтоМультиРежим;
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОсуществитьОбменПоОрганизацииФСРАР", ЭтотОбъект, ДополнительныеПараметры);
	
	// ФТС
	Если Не ОбменПрерванПользователем И ЕстьДоступ И (КонтролирующийОрган = Неопределено ИЛИ КонтролирующийОрган = "ФТС") Тогда
		Если СсылкаНаОтправляемыйОбъект = Неопределено Тогда
			// Выполняем отправку для нескольких ссылок
			Отправки = НезавершенныеПоследниеОтправкиФТСПоОрганизации(Организация);
		Иначе
			// Выполняем отправку только для одной ссылки 
			Отправки = Новый Массив;
			Отправка = ПолучитьПоследнююОтправкуОтчетаВФТС(СсылкаНаОтправляемыйОбъект);
			Если Отправка <> Неопределено Тогда 
				Отправки.Добавить(Отправка);
			КонецЕсли;
		КонецЕсли;
		
		Если Отправки = Неопределено Или Отправки.Количество() = 0 Тогда
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
			
		Иначе
			ЭтоВызовИзСписка = (СсылкаНаОтправляемыйОбъект = Неопределено);
			ОбновитьРезультатНезавершенныхПоследнихОтправокФТСПоОрганизации(Форма, Отправки, 0, ЭтоВызовИзСписка, ОписаниеОповещения);
		КонецЕсли;
	Иначе
		// После ФТС выполняем обмен по ФСРАР
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокФТСПоОрганизации(Форма, Отправки, НомерОбъекта, ЭтоВызовИзСписка, ВыполняемоеОповещение)

	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > Отправки.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	Иначе
		Отправка = Отправки[НомерОбъекта];
		
		ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
			"ОсуществитьОбменПоОрганизацииФТС", 
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС"),
			Отправка);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Форма", 					Форма);
		ДополнительныеПараметры.Вставить("Отправки", 				Отправки);
		ДополнительныеПараметры.Вставить("НомерОбъекта", 			НомерОбъекта);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка", 		ЭтоВызовИзСписка);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатНезавершенныхПоследнихОтправокФТСПоОрганизацииЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ЗначениеЗаполнено(Отправка) Тогда
			ОбновитьРезультатКонкретнойОтправкиФТС(Форма, Отправка, ОписаниеОповещения,, ЭтоВызовИзСписка);
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатНезавершенныхПоследнихОтправокФТСПоОрганизацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 					= ДополнительныеПараметры.Форма;
	Отправки 				= ДополнительныеПараметры.Отправки;
	НомерОбъекта 			= ДополнительныеПараметры.НомерОбъекта;
	ЭтоВызовИзСписка 		= ДополнительныеПараметры.ЭтоВызовИзСписка;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОбновитьРезультатНезавершенныхПоследнихОтправокФТСПоОрганизации(Форма, Отправки, НомерОбъекта + 1, ЭтоВызовИзСписка, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервере
Функция НезавершенныеПоследниеОтправкиФССПоОрганизации(Организация)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НезавершенныеПоследниеОтправкиФССПоОрганизации(Организация);

КонецФункции

&НаСервере
Функция НезавершенныеПоследниеОтправкиФСРАРПоОрганизации(Организация)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НезавершенныеПоследниеОтправкиФСРАРПоОрганизации(Организация);

КонецФункции

&НаСервере
Функция НезавершенныеПоследниеОтправкиРПНПоОрганизации(Организация, ПолучитьУведомления = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НезавершенныеПоследниеОтправкиРПНПоОрганизации(Организация, ПолучитьУведомления);
	
КонецФункции

&НаСервере
Функция НезавершенныеПоследниеОтправкиФТСПоОрганизации(Организация)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.НезавершенныеПоследниеОтправкиФТСПоОрганизации(Организация);
	
КонецФункции

&НаКлиенте
Процедура ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписи(
		ВыполняемоеОповещение,
		Знач УчетнаяЗапись,
		Почта = Неопределено,
		ТихийРежим = Ложь,
		РасшифровыватьПринудительно = Ложь,
		ТребуетсяПроверкаУчетнойЗаписи = Истина,
		ТребуетсяАвтонастройка = Истина,
		СписокПолученныхТС = Неопределено) Экспорт
		
	Если ОбменПрерванПользователем Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
		
	// проверяем учетную запись
	Если ТребуетсяПроверкаУчетнойЗаписи И НЕ УчетнаяЗаписьНастроенаВерно(УчетнаяЗапись) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	// проверяем, задана ли учетная запись
	Если УчетнаяЗапись = Неопределено Тогда
		Если НЕ ТихийРежим Тогда
			ТекстСообщения = НСтр("ru = 'Не задана учетная запись для получения сообщений!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	// проверяем, включена ли возможность документооборота
	Если ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации") Тогда
		
		ссылкаУчетнаяЗапись = УчетнаяЗаписьОрганизации(УчетнаяЗапись);
		Если НЕ ЗначениеЗаполнено(ссылкаУчетнаяЗапись) Тогда
			Если НЕ ТихийРежим Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Для организации %1 не предусмотрена возможность информационного взаимодействия с ФНС, ПФР и Росстатом по каналам связи.'"),
					Символ(34) + УчетнаяЗапись + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			КонецЕсли;
			ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
			Возврат;
		КонецЕсли;
		
		Организация = УчетнаяЗапись;
		УчетнаяЗапись = ссылкаУчетнаяЗапись;
		
	Иначе
		Организация = Неопределено;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("УчетнаяЗапись",               УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("Почта",                       Почта);
	ДополнительныеПараметры.Вставить("ТихийРежим",                  ТихийРежим);
	ДополнительныеПараметры.Вставить("ТребуетсяАвтонастройка",      ТребуетсяАвтонастройка);
	ДополнительныеПараметры.Вставить("РасшифровыватьПринудительно", РасшифровыватьПринудительно);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",       ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("СписокПолученныхТС",          СписокПолученныхТС);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеАвтонастройкиУчетнойЗаписиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	// вызываем автонастройку при необходимости
	Если ТребуетсяАвтонастройка Тогда
		АвтоматическаяНастройкаУчетнойЗаписи(
			УчетнаяЗапись,
			Организация,
			ОписаниеОповещения,
			,
			,
			ТихийРежим);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ЗапуститьЗаданиеЗаменыКодовФСГС()
	
	ПараметрыЗапуска = Новый Массив;
	ПараметрыЗапуска.Добавить(Истина);
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(ЭтотОбъект.УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = "Замена кодов ТОГС";
	
	Возврат ДлительныеОперации.ВыполнитьВФоне(
		"ЭлектронныйДокументооборотСКонтролирующимиОрганами.ФоноваяЗаменаВОрганизацияхИПодразделенияхКодОрганаФСГС",
		ПараметрыЗапуска,
		ПараметрыВыполнения);
	
КонецФункции
	
&НаКлиенте
Процедура ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеАвтонастройкиУчетнойЗаписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ДлительнаяОперация = ЗапуститьЗаданиеЗаменыКодовФСГС();
	ДополнительныеПараметры.Вставить("РезультатАвтонастройки", Результат);
	
	ОповещениеОЗавершении = Новый ОписаниеОповещения(
		"ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеЗаменыКодовФСГС",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
	ПараметрыОжидания.Интервал = 1;
	ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);	
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеЗаменыКодовФСГС(Результат, ДополнительныеПараметры) Экспорт
	
	Результат = ДополнительныеПараметры.РезультатАвтонастройки;
	ДополнительныеПараметры.Удалить("РезультатАвтонастройки");
	
	ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеАвтонастройкиУчетнойЗаписиПродолжение(Результат, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеАвтонастройкиУчетнойЗаписиПродолжение(Результат, ДополнительныеПараметры) Экспорт
	
	УчетнаяЗапись               = ДополнительныеПараметры.УчетнаяЗапись;
	Почта                       = ДополнительныеПараметры.Почта;
	ТихийРежим                  = ДополнительныеПараметры.ТихийРежим;
	ТребуетсяАвтонастройка      = ДополнительныеПараметры.ТребуетсяАвтонастройка;
	РасшифровыватьПринудительно = ДополнительныеПараметры.РасшифровыватьПринудительно;
	ВыполняемоеОповещение       = ДополнительныеПараметры.ВыполняемоеОповещение;
	СписокПолученныхТС          = ДополнительныеПараметры.СписокПолученныхТС;
	
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
		"ПолучитьСообщенияПоУчетнойЗаписи", 
		, 
		, 
		УчетнаяЗапись);
		
	Если ОбменПрерванПользователем Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
		
	// получаем сообщения с сервера
	ВходящиеСообщения = Новый Массив;
	Если НЕ ПолучитьСообщенияПоУчетнойЗаписи(УчетнаяЗапись, ТихийРежим, ВходящиеСообщения) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ДополнитьСписокПолученныхТранспортныхСообщений(ВходящиеСообщения, СписокПолученныхТС);
	
	ОбращениеККлючуРазрешено = РасшифровыватьПринудительно ИЛИ НЕ ТихийРежим;
		
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаОбмена(
		"РасшифроватьСообщенияПоУчетнойЗаписи", 
		, 
		, 
		УчетнаяЗапись);
		
	// расшифровываем сообщения по учетной записи
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВходящиеСообщения",     ВходящиеСообщения);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеРасшифровкиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	РасшифроватьСообщенияПоУчетнойЗаписи(
		УчетнаяЗапись,
		ТихийРежим,
		ОбращениеККлючуРазрешено,
		ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИРасшифроватьСообщенияПоУчетнойЗаписиПослеРасшифровкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ДополнитьСписокПолученныхТранспортныхСообщений(ВходящиеСообщения, СписокПолученныхТС)
	
	Если СписокПолученныхТС <> Неопределено Тогда
	
		Для Каждого ВходящееСообщение Из ВходящиеСообщения Цикл
			
			Если СписокПолученныхТС.НайтиПоЗначению(ВходящееСообщение) = Неопределено Тогда 
				
				СписокПолученныхТС.Добавить(ВходящееСообщение);
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОпределитьТипСообщения(Сообщение, ВложенияЕсть = Истина) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьТипСообщения(Сообщение, ВложенияЕсть);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьНалоговыйОрганПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, ОпределитьСерверДокументооборотаПриНеобходимости = Истина) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	НалоговыеОрганы.Ссылка
	                      |ИЗ
	                      |	Справочник.НалоговыеОрганы КАК НалоговыеОрганы
	                      |ГДЕ
	                      |	НалоговыеОрганы.АдресЭлектроннойПочтыДляЦелейДокументооборотаСНалогоплательщиками = &АдресЭлектроннойПочты");
	Запрос.УстановитьПараметр("АдресЭлектроннойПочты", АдресЭлектроннойПочты);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
	Если ОпределитьСерверДокументооборотаПриНеобходимости Тогда
		Возврат ОпределитьСерверДокументооборотаПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, Ложь);
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьСерверДокументооборотаПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, ОпределитьНалоговыйОрганПриНеобходимости = Истина) Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	СерверыДокументооборота.Ссылка
	                      |ИЗ
	                      |	Справочник.СерверыДокументооборота КАК СерверыДокументооборота
	                      |ГДЕ
	                      |	СерверыДокументооборота.АдресЭлектроннойПочтыФНС = &АдресЭлектроннойПочты
	                      |	ИЛИ СерверыДокументооборота.АдресЭлектроннойПочтыПФР = &АдресЭлектроннойПочты
	                      |	ИЛИ СерверыДокументооборота.АдресЭлектроннойПочтыФСГС = &АдресЭлектроннойПочты");
	Запрос.УстановитьПараметр("АдресЭлектроннойПочты", АдресЭлектроннойПочты);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Возврат Выборка.Ссылка;
	КонецЕсли;
	
	Если ОпределитьНалоговыйОрганПриНеобходимости Тогда
		Возврат ОпределитьНалоговыйОрганПоАдресуЭлектроннойПочты(АдресЭлектроннойПочты, Ложь);
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции
 
&НаСервереБезКонтекста
Процедура РаспознатьТранспортноеСообщение(парамОбъект, Записывать = Истина) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.РаспознатьТранспортноеСообщение(парамОбъект, Записывать);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ТранспортноеСообщениеОбрабатывается(ТранспортноеСообщение) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТранспортноеСообщениеОбрабатывается(ТранспортноеСообщение);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСообщенияПоУчетнойЗаписи(Знач УчетнаяЗапись, ТихийРежим = Ложь, СообщенияРезультат = Неопределено)

	ДокументооборотСКО.НачатьСеансДокументооборотаСКО(Перечисления.ИнициаторыСеансовСвязиСКонтролирующимиОрганами.Пользователь, УчетнаяЗапись);
	
	Попытка
		СообщенияРезультат = ДокументооборотСКО.ПолучитьСообщения(УчетнаяЗапись);
	Исключение
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		ДокументооборотСКО.ЗакончитьСеансДокументооборотаСКО(УчетнаяЗапись, Истина);
		Возврат Ложь;
	КонецПопытки;
		   
	ДокументооборотСКО.ЗакончитьСеансДокументооборотаСКО(УчетнаяЗапись, Истина);
	
	Возврат Истина;
	
КонецФункции

&НаСервере
Функция ПолучитьТребующиеРасшифровкиСообщения(Знач УчетнаяЗапись)
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ТранспортноеСообщение.Ссылка КАК Ссылка,
		|	ТранспортноеСообщение.Тип КАК Тип,
		|	ТранспортноеСообщение.УчетнаяЗапись КАК УчетнаяЗапись,
		|	ТранспортноеСообщение.Отправитель КАК Отправитель,
		|	ТранспортноеСообщение.Получатель КАК Получатель
		|ПОМЕСТИТЬ Сообщения
		|ИЗ
		|	Документ.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ГДЕ
		|	НЕ ТранспортноеСообщение.ПометкаУдаления
		|	И ТранспортноеСообщение.УчетнаяЗапись = &УчетнаяЗапись
		|	И ТранспортноеСообщение.ДатаТранспорта > &ТриМесяцаНазад
		|	И НЕ ТранспортноеСообщение.ЦиклОбмена.ПометкаУдаления
		|	И ТранспортноеСообщение.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыПисем.Полученное)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЦиклыОбмена.Ссылка КАК Ссылка,
		|	ЦиклыОбмена.ДатаСоздания КАК ДатаСоздания,
		|	ЦиклыОбмена.Вид КАК Вид
		|ПОМЕСТИТЬ Циклы
		|ИЗ
		|	Справочник.ЦиклыОбмена КАК ЦиклыОбмена
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Сообщения КАК Сообщения
		|		ПО (Сообщения.Ссылка.ЦиклОбмена = ЦиклыОбмена.Ссылка)
		|ГДЕ
		|	НЕ ЦиклыОбмена.ПометкаУдаления
		|	И ЦиклыОбмена.УчетнаяЗапись = &УчетнаяЗапись
		|	И ЦиклыОбмена.ДатаСоздания > &ТриМесяцаНазад
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение
		|ПОМЕСТИТЬ Исключенные
		|ИЗ
		|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Сообщения КАК Сообщения
		|		ПО (Сообщения.Ссылка = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ИсключенныеСообщения.ТранспортноеСообщение
		|ИЗ
		|	РегистрСведений.ИсключенныеИзОбработкиТранспортныеСообщения КАК ИсключенныеСообщения
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Сообщения КАК Сообщения
		|		ПО (Сообщения.Ссылка = ИсключенныеСообщения.ТранспортноеСообщение)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	Сообщения.Ссылка КАК ТранспортноеСообщение,
		|	Сообщения.Тип КАК Тип,
		|	Циклы.ДатаСоздания КАК ДатаСоздания,
		|	ЛОЖЬ КАК Удалить,
		|	УчетныеЗаписи.ЭтоМультиРежим КАК ЭтоМультиРежим,
		|	УчетныеЗаписи.Ссылка КАК УчетнаяЗапись,
		|	ВЫБОР
		|		КОГДА ТИПЗНАЧЕНИЯ(Сообщения.Отправитель) = ТИП(Справочник.НалоговыеОрганы)
		|				ИЛИ ТИПЗНАЧЕНИЯ(Сообщения.Получатель) = ТИП(Справочник.НалоговыеОрганы)
		|				ИЛИ Циклы.Вид = ЗНАЧЕНИЕ(Перечисление.ВидыЦикловОбмена.ЦиклОбменаСФНС)
		|			ТОГДА ЗНАЧЕНИЕ(Перечисление.ТипыКонтролирующихОрганов.ФНС)
		|		КОГДА ТИПЗНАЧЕНИЯ(Сообщения.Отправитель) = ТИП(Справочник.ОрганыФСГС)
		|				ИЛИ ТИПЗНАЧЕНИЯ(Сообщения.Получатель) = ТИП(Справочник.ОрганыФСГС)
		|				ИЛИ Циклы.Вид = ЗНАЧЕНИЕ(Перечисление.ВидыЦикловОбмена.ЦиклОбменаСФСГС)
		|			ТОГДА ЗНАЧЕНИЕ(Перечисление.ТипыКонтролирующихОрганов.ФСГС)
		|		КОГДА ТИПЗНАЧЕНИЯ(Сообщения.Отправитель) = ТИП(Справочник.ОрганыПФР)
		|				ИЛИ ТИПЗНАЧЕНИЯ(Сообщения.Получатель) = ТИП(Справочник.ОрганыПФР)
		|				ИЛИ Циклы.Вид = ЗНАЧЕНИЕ(Перечисление.ВидыЦикловОбмена.ЦиклОбменаСПФР)
		|			ТОГДА ЗНАЧЕНИЕ(Перечисление.ТипыКонтролирующихОрганов.ПФР)
		|		КОГДА Сообщения.Тип В (&ТипыТранспортныхСообщенийЦБ)
		|			ТОГДА ЗНАЧЕНИЕ(Перечисление.ТипыКонтролирующихОрганов.ЦБ)
		|	КОНЕЦ КАК Направление,
		|	УчетныеЗаписи.ЭтоМультиРежим
		|		И Сообщения.Тип В (&ТипыСообщенийИнициированныхФНС) КАК ТолькоДляШифровальщика
		|ИЗ
		|	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Сообщения КАК Сообщения
		|		ПО (Сообщения.Ссылка = ТранспортныеКонтейнеры.ТранспортноеСообщение)
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Циклы КАК Циклы
		|		ПО (Сообщения.Ссылка.ЦиклОбмена = Циклы.Ссылка)
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписи
		|		ПО (Сообщения.УчетнаяЗапись = УчетныеЗаписи.Ссылка)
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ВладельцыОбъектов1СОтчетности КАК ВладельцыОбъектов1СОтчетности
		|		ПО (Циклы.Ссылка = ВладельцыОбъектов1СОтчетности.Объект)
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ВладельцыОбъектов1СОтчетности КАК ВладельцыОбъектов1СОтчетностиТС
		|		ПО (Сообщения.Ссылка = ВладельцыОбъектов1СОтчетностиТС.Объект
		|			И ВладельцыОбъектов1СОтчетности.Владелец ЕСТЬ NULL)
		|ГДЕ
		|	УчетныеЗаписи.Ссылка = &УчетнаяЗапись
		|	И НЕ ТранспортныеКонтейнеры.ТранспортноеСообщение В
		|				(ВЫБРАТЬ
		|					Исключенные.ТранспортноеСообщение
		|				ИЗ
		|					Исключенные)
		|	И (НЕ УчетныеЗаписи.ЭтоМультиРежим
		|	ИЛИ (ВладельцыОбъектов1СОтчетности.Владелец = &ТекущийПользователь
		|			ИЛИ ВладельцыОбъектов1СОтчетности.Владелец ЕСТЬ NULL)
		|		И (ВладельцыОбъектов1СОтчетностиТС.Владелец = &ТекущийПользователь
		|			ИЛИ ВладельцыОбъектов1СОтчетностиТС.Владелец ЕСТЬ NULL))
		|
		|УПОРЯДОЧИТЬ ПО
		|	Сообщения.Ссылка,
		|	Циклы.ДатаСоздания,
		|	Сообщения.Тип.Порядок");
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	УчетнаяЗапись = ?(
		ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.Организации"),
		УчетнаяЗаписьОрганизации(УчетнаяЗапись), 
		УчетнаяЗапись);
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	
	Запрос.УстановитьПараметр("УчетнаяЗапись", 					УчетнаяЗапись);
	Запрос.УстановитьПараметр("ТриМесяцаНазад", 				ТекущаяДатаСеанса() - 90*24*60*60);
	Запрос.УстановитьПараметр("ТипыТранспортныхСообщенийЦБ", 	КонтекстЭДОСервер.ТипыТранспортныхСообщенийЦБ());
	Запрос.УстановитьПараметр("ТипыСообщенийИнициированныхФНС", КонтекстЭДОСервер.ТипыСообщенийИнициированныхФНС());
	Запрос.УстановитьПараметр("УчетнаяЗапись", 					УчетнаяЗапись);
	Запрос.УстановитьПараметр("ТекущийПользователь", 			ТекущийПользователь);
	
	Таблица = Запрос.Выполнить().Выгрузить();
	
	Для каждого Строка Из Таблица Цикл
	
		ИсключитьТранспортноеСообщениеИзСпискаСообщенийПолученныхАвтообменом(Строка.ТранспортноеСообщение);
		
		Если Не ТранспортноеСообщениеОбрабатывается(Строка.ТранспортноеСообщение) Тогда
			Строка.Удалить = Истина;
			Продолжить;
		КонецЕсли;
	
	КонецЦикла;
	
	Мультирежим.СообщенияДляРасшифровки_ПроверитьПраваНаУчетнуюЗапись(Таблица, УчетнаяЗапись, Ложь);
	КонтекстЭДОСервер.СообщенияДляРасшифровки_УдалитьЛишниеСтроки(Таблица);
	
	Результат = Новый Массив;
	Для каждого Строка Из Таблица Цикл
		
		Результат.Добавить(Новый Структура("ТранспортноеСообщение, Тип", Строка.ТранспортноеСообщение, Строка.Тип));
		
	КонецЦикла;
	
	Мультирежим.ЗаписатьДатуПросмотраНерасшифрованных(УчетнаяЗапись);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ИсключитьТранспортноеСообщениеИзСпискаСообщенийПолученныхАвтообменом(ТранспортноеСообщение)

	НачатьТранзакцию();
	Попытка
		НовыйНабор = РегистрыСведений.СвойстваТранспортныхСообщений.СоздатьНаборЗаписей();
		НовыйНабор.Отбор.ТранспортноеСообщение.Установить(ТранспортноеСообщение);
		НовыйНабор.Записать();
		
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
	КонецПопытки;
		
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьСообщенияПоУчетнойЗаписи(Знач УчетнаяЗапись, ТихийРежим = Ложь, ОбращениеККлючуРазрешено = Истина, ВыполняемоеОповещение = Неопределено)
	
	ТребующиеРасшифровкиСообщения = ПолучитьТребующиеРасшифровкиСообщения(УчетнаяЗапись); // сервер
	Если ОбменПрерванПользователем Или ТребующиеРасшифровкиСообщения.Количество() = 0 Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки = Новый Массив;
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки.Добавить(СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_КритическаяОшибка);
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки.Добавить(СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеПФР);
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки.Добавить(СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеФНС);
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки.Добавить(СтруктураСсылочныхДанных.Перечисления_ТипыТранспортныхСообщений_УведомлениеОбОшибкеФСГС);
	
	КоличествоРасшифрованных = 0;
	КоличествоНеРасшифрованных = 0;
	НомерОбъекта = 0;
	
	РасшифроватьТребующиеРасшифровкиСообщения(ВыполняемоеОповещение, КоличествоНеРасшифрованных, КоличествоРасшифрованных, НомерОбъекта, ОбращениеККлючуРазрешено, ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки, ТихийРежим, ТребующиеРасшифровкиСообщения);
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьТребующиеРасшифровкиСообщения(
	ВыполняемоеОповещение,
	КоличествоНеРасшифрованных,
	КоличествоРасшифрованных,
	НомерОбъекта,
	ОбращениеККлючуРазрешено,
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки,
	ТихийРежим,
	ТребующиеРасшифровкиСообщения)
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > ТребующиеРасшифровкиСообщения.Количество() Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		
	Иначе
		ВходящийКонтекст = Новый Структура;
		ВходящийКонтекст.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
		ВходящийКонтекст.Вставить("КоличествоНеРасшифрованных", 	КоличествоНеРасшифрованных);
		ВходящийКонтекст.Вставить("КоличествоРасшифрованных", 		КоличествоРасшифрованных);
		ВходящийКонтекст.Вставить("НомерОбъекта", 					НомерОбъекта);
		ВходящийКонтекст.Вставить("ОбращениеККлючуРазрешено", 		ОбращениеККлючуРазрешено);
		ВходящийКонтекст.Вставить("ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки",
			ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки);
		ВходящийКонтекст.Вставить("ТихийРежим", 					ТихийРежим);
		ВходящийКонтекст.Вставить("ТребующиеРасшифровкиСообщения", 	ТребующиеРасшифровкиСообщения);
		
		Если НомерОбъекта % 10 = 0 Тогда
			// для предотвращения переполнения стека
			ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ВходящийКонтекст,
				"БРО.ВходящийКонтекстасшифроватьТребующееРасшифровкиСообщение");
			ПодключитьОбработчикОжидания("Подключаемый_РасшифроватьТребующееРасшифровкиСообщение", 0.1, Истина);
		Иначе
			РасшифроватьТребующееРасшифровкиСообщение(ВходящийКонтекст);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьТребующееРасшифровкиСообщение(ВходящийКонтекст)
	
	ВыполняемоеОповещение 			= ВходящийКонтекст.ВыполняемоеОповещение;
	КоличествоНеРасшифрованных 		= ВходящийКонтекст.КоличествоНеРасшифрованных;
	КоличествоРасшифрованных 		= ВходящийКонтекст.КоличествоРасшифрованных;
	НомерОбъекта 					= ВходящийКонтекст.НомерОбъекта;
	ОбращениеККлючуРазрешено 		= ВходящийКонтекст.ОбращениеККлючуРазрешено;
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки =
		ВходящийКонтекст.ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки;
	ТихийРежим 						= ВходящийКонтекст.ТихийРежим;
	ТребующиеРасшифровкиСообщения 	= ВходящийКонтекст.ТребующиеРасшифровкиСообщения;
	
	ТребующееРасшифровкиСообщение = ТребующиеРасшифровкиСообщения[НомерОбъекта];
	// если обращение к ключу запрещено, то распаковываем только незашифрованные контейнеры
	Если НЕ ОбращениеККлючуРазрешено
		И ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки.Найти(ТребующееРасшифровкиСообщение.Тип) = Неопределено Тогда
		
		РасшифроватьТребующиеРасшифровкиСообщения(
			ВыполняемоеОповещение, 
			КоличествоНеРасшифрованных, 
			КоличествоРасшифрованных, 
			НомерОбъекта + 1, 
			ОбращениеККлючуРазрешено, 
			ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки, 
			ТихийРежим, 
			ТребующиеРасшифровкиСообщения);
			
		Возврат;
	КонецЕсли;
	
	// фиксируем успех или неудачу
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",                                ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("КоличествоНеРасшифрованных",                           КоличествоНеРасшифрованных);
	ДополнительныеПараметры.Вставить("КоличествоРасшифрованных",                             КоличествоРасшифрованных);
	ДополнительныеПараметры.Вставить("НомерОбъекта",                                         НомерОбъекта);
	ДополнительныеПараметры.Вставить("ОбращениеККлючуРазрешено",                             ОбращениеККлючуРазрешено);
	ДополнительныеПараметры.Вставить("ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки", ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки);
	ДополнительныеПараметры.Вставить("ТихийРежим",                                           ТихийРежим);
	ДополнительныеПараметры.Вставить("ТребующиеРасшифровкиСообщения",                        ТребующиеРасшифровкиСообщения);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("РасшифроватьТребующиеРасшифровкиСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	РасшифроватьТранспортноеСообщение(ТребующееРасшифровкиСообщение.ТранспортноеСообщение, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_РасшифроватьТребующееРасшифровкиСообщение() Экспорт
	
	ВходящийКонтекст = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ВходящийКонтекстасшифроватьТребующееРасшифровкиСообщение");
	
	РасшифроватьТребующееРасшифровкиСообщение(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьТребующиеРасшифровкиСообщенияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	КоличествоНеРасшифрованных = ДополнительныеПараметры.КоличествоНеРасшифрованных;
	КоличествоРасшифрованных = ДополнительныеПараметры.КоличествоРасшифрованных;
	НомерОбъекта = ДополнительныеПараметры.НомерОбъекта;
	ОбращениеККлючуРазрешено = ДополнительныеПараметры.ОбращениеККлючуРазрешено;
	ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки = ДополнительныеПараметры.ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки;
	ТихийРежим = ДополнительныеПараметры.ТихийРежим;
	ТребующиеРасшифровкиСообщения = ДополнительныеПараметры.ТребующиеРасшифровкиСообщения;
	
	Если Результат Тогда
		КоличествоРасшифрованных = КоличествоРасшифрованных + 1;
	Иначе
		КоличествоНеРасшифрованных = КоличествоНеРасшифрованных + 1;
	КонецЕсли;
	
	РасшифроватьТребующиеРасшифровкиСообщения(ВыполняемоеОповещение, КоличествоНеРасшифрованных, КоличествоРасшифрованных, НомерОбъекта + 1, ОбращениеККлючуРазрешено, ТипыСообщенийНеТребующихОбращенияККлючуДляРаспаковки, ТихийРежим, ТребующиеРасшифровкиСообщения);
	
КонецПроцедуры


#Область ОтправкаНеформализованногоДокументаВФНС

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФНС(Док, Организация, ВыполняемоеОповещение, ОтобразитьРезультат = Истина) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Док) Тогда
		Возврат;
	КонецЕсли;
	
	Контекст = Новый Структура("Док, Организация, ВыполняемоеОповещение", Док, Организация, ВыполняемоеОповещение);
	Контекст.Вставить("ОтобразитьРезультат", ОтобразитьРезультат);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФНСПослеАвтонастройкиУчетнойЗаписи", 
		ЭтотОбъект, 
		Контекст);
		
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док", Док);
	КонтекстДлительнойОперации.Вставить("Организация", Организация);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", ОписаниеОповещения);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка письма в ФНС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаНеформализованногоДокументаВФНС",  0.1, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаНеформализованногоДокументаВФНС() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Док = КонтекстДлительнойОперации.Док;
	ОписаниеОповещения = КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док,ОписаниеОповещения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаНеформализованногоДокументаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Док = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, указан ли получатель
	ПолучательДокумента = Док.Получатель;
	Если НЕ ЗначениеЗаполнено(ПолучательДокумента) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель письма.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем на макс. объем вложений 100Мб
	Приложения = ПолучитьВложенияНеформализованногоДокумента(Док, , Истина);
	ОбщиеРазмер = Приложения.Итог("Размер");
	Если ОбщиеРазмер > 100 *1024*1024 Тогда // общий размер не может превышать 100Мб
		ТекстСообщения = НСтр("ru = 'Общий размер вложений отправляемого сообщения превышает 100 Мб.
				|К сожалению, форматом запрещена отправка сообщений с вложениями общим размером более 100 Мб.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ДвоичныеДанныеФайлаПисьма = Неопределено;
	ИмяФайлаПисьма = Неопределено;
	РезультатФормированияФайлаПисьма = СформироватьФайлПисьма(Док, ДвоичныеДанныеФайлаПисьма, ИмяФайлаПисьма);
	Если НЕ РезультатФормированияФайлаПисьма Тогда
		Возврат Ложь;
	КонецЕсли;
	
	КороткоеИмяФайлаПисьма = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа
	КодНалоговогоОргана = СокрЛП(Док.Получатель.Код);
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		
		// формируем файл доверенности
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(ЭлРегистрацияВИФНС, КороткоеИмяФайлаДоверенности, Организация);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
		
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.Предмет = Док;
	ЦиклОбмена.Идентификатор = Док.Идентификатор;
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = Док.Получатель;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ОбращениеНП;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ОбращениеНП;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = Док.Получатель;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем письмо
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Обращение, ДвоичныеДанныеФайлаПисьма, ИмяФайлаПисьма, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка письма прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем приложения к письму
	Для Каждого СтрПриложение Из Приложения Цикл
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, СтрПриложение.Данные, СтрПриложение.ИмяФайла, СтрПриложение.Размер, , , , , , СгенерироватьUUID(), ТипСодержимогоПоИмениФайла(СтрПриложение.ИмяФайла)) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, ФайлДоверенности, КороткоеИмяФайлаДоверенности, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Док, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФНС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	Параметры_ОтобразитьРезультат 	= ВходящийКонтекст.ОтобразитьРезультат;
	
	ОтправкаНеформализованногоДокументаВФНС(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение, Параметры_ОтобразитьРезультат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФНСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ОтобразитьРезультат = ВходящийКонтекст.ОтобразитьРезультат;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись изменений...");
	
	// отражаем изменения в базе
	Если НЕ ОтправкаНеформализованногоДокументаВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаНеформализованногоДокументаВФНС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
			
		Иначе
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Док);

			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС

		КонецЕсли;
		
		Возврат;
		
	КонецЕсли;
	
	Если НЕ ДокументооборотСКОВызовСервера.ОрганизацияПодключенаКИнспекции(Док)
		ИЛИ НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", 	ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", 		КонтекстОтправки);
	Контекст.Вставить("ОтобразитьРезультат", 	ОтобразитьРезультат);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФНС_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		Контекст);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(Док, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФНС_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФНС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(
		ОписаниеОповещения, 
		ВходящийКонтекст.КонтекстОтправки.Документ, 
		ВходящийКонтекст.КонтекстОтправки.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФНС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФНСПослеФормированияПакета", 
		ЭтотОбъект,
		ВходящийКонтекст);
		
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	Отправка_СформироватьПакет(Оповещение, ВходящийКонтекст.КонтекстОтправки.ТранспортноеСообщение, "ФНС");

КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФНСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	Если ВходящийКонтекст.ОтобразитьРезультат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // Письмо в ФНС
	Иначе
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, КонтекстОтправки.Документ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция ЗаписатьУзелДереваВXML(СтрокаДерева, ПотокXML)
	
	Если ТипЗнч(СтрокаДерева) = Тип("ДеревоЗначений") Тогда
		ПотокXML.ЗаписатьНачалоЭлемента("К_2_180_06_001_001");
		ПотокXML.ЗаписатьАтрибут("xmlns:xsi", "http://www.w3.org/2001/XMLSchema-instance");
		ПотокXML.ЗаписатьАтрибут("xsi:noNamespaceSchemaLocation", "NP_NO_2_180_06_001_001.xsd");
		Для каждого Стр Из СтрокаДерева.Строки Цикл
			ЗаписатьУзелДереваВXML(Стр, ПотокXML);
		КонецЦикла;
		ПотокXML.ЗаписатьКонецЭлемента();
	Иначе
		Если СтрокаДерева.Тип = "А" ИЛИ СтрокаДерева.Тип = "A" Тогда 
			ПотокXML.ЗаписатьАтрибут(СтрокаДерева.Код, Строка(СтрокаДерева.Значение));
		Иначе
			ПотокXML.ЗаписатьНачалоЭлемента(СтрокаДерева.Код);
			Для каждого Лист из СтрокаДерева.Строки Цикл
				ЗаписатьУзелДереваВXML(Лист, ПотокXML);
			КонецЦикла;
			ПотокXML.ЗаписатьТекст(Строка(СтрокаДерева.Значение));
			ПотокXML.ЗаписатьКонецЭлемента();
		КонецЕсли;
	КонецЕсли;

КонецФункции

&НаСервереБезКонтекста
Функция ЗаписатьДеревоВXML(Дерево);
	
	ПотокXML = РегламентированнаяОтчетность.СоздатьНовыйПотокXML();
	
	Для Каждого Стр Из Дерево.Строки Цикл
		ЗаписатьУзелДереваВXML(Стр, ПотокXML);
	КонецЦикла;
	
	ТекстДляЗаписи = ПотокXML.Закрыть();
	ТекстДляЗаписи = "<?xml version=""1.0"" encoding=""windows-1251""?>" + Сред(ТекстДляЗаписи, СтрНайти(ТекстДляЗаписи, Символы.ПС));
	Возврат ТекстДляЗаписи;
	
КонецФункции
      
&НаСервереБезКонтекста
Функция СформироватьПараметрыПисьма(Письмо)
	
	СведенияОбОрганизации = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Письмо.Организация);
	
	ЭтоПБОЮЛ = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Письмо.Организация);
	СведенияОбОрганизации.Вставить("ЭтоПБОЮЛ", ЭтоПБОЮЛ);
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа
	КодНалоговогоОргана = СокрЛП(Письмо.Получатель.Код);
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(
		Письмо.Организация,
		КодНалоговогоОргана,,
		НЕ ЭтоПБОЮЛ,
		СведенияОбОрганизации);
		
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
		
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		ДоверенностьПредставителя = Доверенность;
		// признак подписания представителем
		СведенияОбОрганизации.Вставить("ПрПодп", 2);
		СведенияДоверенности = ПолучитьСведенияДоверенности(ЭлРегистрацияВИФНС, Письмо.Организация);
		Если СведенияДоверенности = Неопределено Тогда
			Возврат Неопределено;
		КонецЕсли;
		СведенияОбОрганизации.Вставить("Доверенность", СведенияДоверенности);
	Иначе
		// признак подписания налогоплательщиком
		СведенияОбОрганизации.Вставить("ПрПодп", 1);
	КонецЕсли;
	
	Если ЭтоПБОЮЛ Тогда
		ФИОИП = ФИОИндивидуальногоПредпринимателя(Письмо.Организация, СведенияОбОрганизации);
		Если ФИОИП = Неопределено Тогда
			СведенияОбОрганизации.Вставить("ИПФамилия", "");
			СведенияОбОрганизации.Вставить("ИПИмя", "");
			СведенияОбОрганизации.Вставить("ИПОтчество", "");
		Иначе
			СведенияОбОрганизации.Вставить("ИПФамилия", ФИОИП.Фамилия);
			СведенияОбОрганизации.Вставить("ИПИмя", ФИОИП.Имя);
			СведенияОбОрганизации.Вставить("ИПОтчество", ФИОИП.Отчество);
		КонецЕсли;
	Иначе
		Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
			СведенияОбОрганизации.Вставить("КППЮЛ", СокрЛП(ЭлРегистрацияВИФНС.КПП));
		Иначе
			СведенияОбОрганизации.Вставить("КППЮЛ", СокрЛП(СведенияОбОрганизации.КППЮЛ));
		КонецЕсли;
	КонецЕсли;
	
	Возврат СведенияОбОрганизации;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьЗаполнениеПисьма(Письмо, Параметры)
	
	СтатусПроверки = Истина;
	
	Если Параметры = Неопределено Тогда
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Письмо.Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не задана организация!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	ИначеЕсли НЕ ЗначениеЗаполнено(Письмо.Получатель) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	КонецЕсли;
	
	// проверка сведений об отправителе
	Если Параметры.ПрПодп = 1 Тогда // отправитель - налогоплательщик
		Если Параметры.ЭтоПБОЮЛ Тогда
			Если НЕ ЗначениеЗаполнено(Параметры.ИННФЛ) Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан ИНН индивидуального предпринимателя!'"));
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЕсли;
	Иначе
		СвПредст = Параметры.Доверенность.СвУпПред.СвПред;
		Если НЕ СвПредст.Свойство("СвОрг") Тогда
			Если НЕ СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННФЛ = СвПредст.СведФизЛ.ИННФЛ;
			Иначе
				ИННФЛ = ?(Параметры.ЭтоПБОЮЛ, Параметры.ИННФЛ, Параметры.ИННЮЛ);
			КонецЕсли;
			Если НЕ ЗначениеЗаполнено(ИННФЛ) Тогда
				Если НЕ СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации ИЛИ Параметры.ЭтоПБОЮЛ Тогда
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан ИНН физлица-представителя!'"));
				Иначе
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан ИНН организации физлица-представителя!'"));
				КонецЕсли;
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// проверяем сведения об организации
	Если НЕ Параметры.ЭтоПБОЮЛ Тогда
	
		Если ПустаяСтрока(Параметры.АдрЮр) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан адрес организации (индивидуального предпринимателя)!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ОргРайон) И НЕ ЗначениеЗаполнено(Параметры.ОргГород) И НЕ ЗначениеЗаполнено(Параметры.ОргНПункт)
			И НЕ ЗначениеЗаполнено(Параметры.ОргУлица) И НЕ ЗначениеЗаполнено(Параметры.ОргДом) И НЕ ЗначениеЗаполнено(Параметры.ОргКорпус)
			И НЕ ЗначениеЗаполнено(Параметры.ОргКвартира) И СтрЧислоВхождений(Параметры.АдрЮР, ",") <> 9 И СтрЧислоВхождений(Параметры.АдрЮР, ",") <> 12 Тогда
			ТекстСообщения = НСтр("ru = 'Адрес организации (индивидуального предпринимателя) должен содержать в себе 9 или 12 запятых!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если Параметры.ОргКодСубъект = "" Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить код региона, указанного в адресе организации (индивидуального предпринимателя).
										|Проверьте правильность указания региона!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.НаимЮЛПол) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано полное наименование организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИННЮЛ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан ИНН организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(Параметры.ИННЮЛ, Ложь) Тогда
			ТекстСообщения = НСтр("ru = 'Некорректно задан ИНН организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.КППЮЛ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан КПП организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли СтрДлина(Параметры.КППЮЛ) <> 9 Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан КПП организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	Иначе
		
		Если ПустаяСтрока(Параметры.АдрМЖ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан адрес места жительства индивидуального предпринимателя!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.РайонМЖ) И НЕ ЗначениеЗаполнено(Параметры.ГородМЖ) И НЕ ЗначениеЗаполнено(Параметры.НПунктМЖ)
			И НЕ ЗначениеЗаполнено(Параметры.УлицаМЖ) И НЕ ЗначениеЗаполнено(Параметры.ДомМЖ) И НЕ ЗначениеЗаполнено(Параметры.КорпусМЖ)
			И НЕ ЗначениеЗаполнено(Параметры.КвартираМЖ) И СтрЧислоВхождений(Параметры.АдрМЖ, ",") <> 9 И СтрЧислоВхождений(Параметры.АдрМЖ, ",") <> 12 Тогда
			ТекстСообщения = НСтр("ru = 'Адрес места жительства индивидуального предпринимателя должен содержать в себе 9 или 12 запятых!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если Параметры.ОргКодСубъект = "" Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить код региона, указанного в адресе места жительства индивидуального предпринимателя.
										|Проверьте правильность указания региона!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИННФЛ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан ИНН индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(Параметры.ИННФЛ, Истина) Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан ИНН индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИПФамилия) Тогда
			ТекстСообщения = НСтр("ru = 'Не задана фамилия индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИПИмя) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано имя индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	// если представитель объявлен, то проверяем сведения о нем
	Если Параметры.ПрПодп = 2 Тогда
		
		СведенияДоверенности = Параметры.Доверенность;
		СведФизЛ = СведенияДоверенности.СвУпПред.СвПред.СведФизЛ;
		ФИОПодписанта = ДокументооборотСКОВызовСервера.УточнитьФИОПредставителя(
			СведФизЛ.ФИО,
			СведенияДоверенности.ДанныеПредставителя);
		Если НЕ ЗначениеЗаполнено(ФИОПодписанта) Тогда
			ТекстСообщения = НСтр("ru = 'В сведениях о доверенности не задано ФИО уполномоченного лица представителя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ ЗначениеЗаполнено(ФИОПодписанта.Фамилия) Тогда
			ТекстСообщения = НСтр("ru = 'В сведениях о доверенности не задана фамилия уполномоченного лица представителя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ ЗначениеЗаполнено(ФИОПодписанта.Имя) Тогда
			ТекстСообщения = НСтр("ru = 'В сведениях о доверенности не задано имя уполномоченного лица представителя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	Иначе
		
		ФИОПодписанта = РегламентированнаяОтчетность.РазложитьФИО(Параметры.ФИОРук);
		Если НЕ ЗначениеЗаполнено(ФИОПодписанта.Фамилия) Тогда
			ТекстСообщения = НСтр("ru = 'Не задана фамилия руководителя организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ ЗначениеЗаполнено(ФИОПодписанта.Имя) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано имя руководителя организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	
	Возврат СтатусПроверки;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьФайлПисьма(Письмо, ДвоичныеДанныеФайлаПисьма, ИмяФайлаПисьма)
	
	// собираем параметры письма
	ПараметрыПисьма = СформироватьПараметрыПисьма(Письмо);
	Если НЕ ПроверитьЗаполнениеПисьма(Письмо, ПараметрыПисьма) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать пакет с письмом!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем идентификатор отправителя
	Если ПараметрыПисьма.ПрПодп = 2 Тогда
		СвПред = ПараметрыПисьма.Доверенность.СвУпПред.СвПред;
		Если СвПред.Свойство("СвОрг") Тогда
			ИННКПП = СокрЛП(СвПред.СвОрг.ИННЮЛ) + СокрЛП(СвПред.СвОрг.КПП);
		Иначе
			Если НЕ ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННКПП = СокрЛП(СвПред.СведФизЛ.ИННФЛ);
			ИначеЕсли ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации
				И ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.Свойство("ЯвляетсяСотрудникомОрганизацииПоМЧД")
				И ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизацииПоМЧД = Ложь
				И ДокументооборотСКОКлиентСервер.ПараметрыОтправкиПоМЧДФНС("ИзменятьФайлВыгрузкиДляФизическогоЛица") Тогда
				ИННКПП = ?(ПараметрыПисьма.Доверенность.СвПредст.ЯвляетсяФЛ, СокрЛП(ПараметрыПисьма.Доверенность.СвПредст.ИННФЛ),
					СокрЛП(ПараметрыПисьма.Доверенность.СвПредст.ИННЮЛ) + СокрЛП(ПараметрыПисьма.КППЮЛ));
			Иначе
				ИННКПП = ?(ПараметрыПисьма.ЭтоПБОЮЛ, СокрЛП(ПараметрыПисьма.ИННФЛ), СокрЛП(ПараметрыПисьма.ИННЮЛ) + СокрЛП(ПараметрыПисьма.КППЮЛ));
			КонецЕсли;
		КонецЕсли;
	Иначе
		ИННКПП = ?(ПараметрыПисьма.ЭтоПБОЮЛ, СокрЛП(ПараметрыПисьма.ИННФЛ), СокрЛП(ПараметрыПисьма.ИННЮЛ) + СокрЛП(ПараметрыПисьма.КППЮЛ));
	КонецЕсли;
	
	ЭтоКрупнейший    = ДокументооборотСКО.ЭтоКрупнейший(Письмо.Организация);
	КодНОКрупнейшего = ДокументооборотСКО.КодНОКрупнейшего(Письмо.Организация);

	ИмяФайлаБезРасширения = "IU_OBRNP_"
							 + ?(ЭтоКрупнейший И ЗначениеЗаполнено(КодНОКрупнейшего), СокрЛП(КодНОКрупнейшего), СокрЛП(Письмо.Получатель.Код))
							 + "_"
							 + СокрЛП(Письмо.Получатель.Код)
							 + "_"
							 + ИННКПП
							 + "_"
							 + Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd")
							 + "_"
							 + Строка(Новый УникальныйИдентификатор);
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	УзелФайл = ДобавитьУзелXML(Дерево, "Файл");
	ДобавитьУзелXML(УзелФайл, "ИдФайл", Истина, XMLСтрока(ИмяФайлаБезРасширения));
	ДобавитьУзелXML(УзелФайл, "ВерсПрог", Истина, Лев(XMLСтрока(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы()), 40));
	ДобавитьУзелXML(УзелФайл, "ВерсФорм", Истина, XMLСтрока("5.04"));
	УзелДокумент = ДобавитьУзелXML(УзелФайл, "Документ");
	
		ДобавитьУзелXML(УзелДокумент, "КНД", Истина, XMLСтрока("1166102"));
		ДобавитьУзелXML(УзелДокумент, "ДатаДок", Истина, XMLСтрока(Формат(ТекущаяДатаСеанса(), "ДФ=dd.MM.yyyy")));
		
		УзелСвНП = ДобавитьУзелXML(УзелДокумент, "СвНП");
		Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
			
			УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "НПЮЛ");
			
				ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(ПараметрыПисьма.НаимЮЛПол), 1000));
				ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(ПараметрыПисьма.ИННЮЛ));
				ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(ПараметрыПисьма.КППЮЛ));
				
		Иначе
			
			УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "НПФЛ");
			
				ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННФЛ)), Истина);
				УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
				
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
					
		КонецЕсли;
		
		УзелСвНП = ДобавитьУзелXML(УзелДокумент, "СвОтпр");
		Если ПараметрыПисьма.ПрПодп = 1 Тогда // отправитель - налогоплательщик
			Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
				УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрЮЛ");
					ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(ПараметрыПисьма.НаимЮЛПол), 1000));
					ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(ПараметрыПисьма.ИННЮЛ));
					ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(ПараметрыПисьма.КППЮЛ));
			Иначе
				УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрФЛ");
					ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННФЛ)), Истина);
					УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
						ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
						ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
						ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
			КонецЕсли;
		Иначе
			СвДоверенность = ПараметрыПисьма.Доверенность;
			СвПредст = СвДоверенность.СвУпПред.СвПред;
			Если СвПредст.Свойство("СвОрг") Тогда // представитель - ЮЛ
				УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрЮЛ");
					ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(СокрЛП(СвПредст.СвОрг.НаимОрг)), 1000));
					ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(СвПредст.СвОрг.ИННЮЛ));
					ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(СвПредст.СвОрг.КПП));
			Иначе
				
				Если СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
					Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
						УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрЮЛ");
						ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(ПараметрыПисьма.НаимЮЛПол), 1000));
						ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(ПараметрыПисьма.ИННЮЛ));
						ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(ПараметрыПисьма.КППЮЛ));	
					Иначе
						УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрФЛ");
						ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННФЛ)), Истина);
						УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
						ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
						ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
						ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
					КонецЕсли;
				Иначе	
					УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрФЛ");
					ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(СвПредст.СведФизЛ.ИННФЛ)), Истина);
					ФИОПредставителя = СвПредст.СведФизЛ.ФИО;
					УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Фамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Имя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Отчество)), 60), Истина);
				КонецЕсли;

			КонецЕсли;
		КонецЕсли;
		
		УзелПодписант = ДобавитьУзелXML(УзелДокумент, "Подписант");
			ДобавитьУзелXML(УзелПодписант, "ПрПодп", Истина, ПараметрыПисьма.ПрПодп);
			УзелФИО = ДобавитьУзелXML(УзелПодписант, "ФИО");
			Если ПараметрыПисьма.ПрПодп = 1 Тогда
				Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
					ФИОПодписанта = РегламентированнаяОтчетность.РазложитьФИО(ПараметрыПисьма.ФИОРук);
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Фамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Имя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Отчество)), 60), Истина);
				Иначе
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
				КонецЕсли;
			Иначе
				ФИОПодписанта = ДокументооборотСКОВызовСервера.УточнитьФИОПредставителя(
					ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ФИО,
					ПараметрыПисьма.Доверенность.ДанныеПредставителя);
				ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Фамилия)), 60));
				ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Имя)), 60));
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Отчество)), 60), Истина);
			КонецЕсли;
		
		УзелОбращИнф = ДобавитьУзелXML(УзелДокумент, "ОбращИнф");
		
			ДобавитьУзелXML(УзелОбращИнф, "ИФНС", Истина, XMLСтрока(СокрЛП(Письмо.Получатель.Код)));
			ДобавитьУзелXML(УзелОбращИнф, "ОбращТекст", , СокрЛП(Письмо.Содержание), , Истина);
			УзелПрил = ДобавитьУзелXML(УзелОбращИнф, "Прил");
			
				Вложения = ПолучитьВложенияНеформализованногоДокумента(Письмо);
				ДобавитьУзелXML(УзелПрил, "КолФайл", Истина, XMLСтрока(Вложения.Количество()));
				Для Каждого Вложение Из Вложения Цикл
					ДобавитьУзелXML(УзелПрил, "НаимПрил", , XMLСтрока(СокрЛП(Вложение.ИмяФайла)));
				КонецЦикла;
		
	// сохранение дерева XML в файл и двоичные данные
	ТекстВыгрузкиXML = ЗаписатьДеревоВXML(Дерево);
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	ЗаписьТекста = Новый ЗаписьТекста(ВремФайл, "windows-1251");
	ЗаписьТекста.Записать(ТекстВыгрузкиXML);
	ЗаписьТекста.Закрыть();
	
	ДвоичныеДанныеФайлаПисьма = Новый ДвоичныеДанные(ВремФайл);
	УдалитьФайлы(ВремФайл);
	
	ИмяФайлаПисьма = ИмяФайлаБезРасширения + ".xml";
	
	Возврат Истина;
	
КонецФункции

#Область ОтправкаНеформализованногоДокументаВПФР

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФР(Док, Организация, ВыполняемоеОповещение, ОтобразитьРезультат = Истина) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Док) Тогда
		Возврат;
	КонецЕсли;
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док", Док);
	КонтекстДлительнойОперации.Вставить("Организация", Организация);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	КонтекстДлительнойОперации.Вставить("ОтобразитьРезультат", ОтобразитьРезультат);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВПФР_ПослеПроверкиЗаключенногоСоглашенияСПФР", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ПроверитьЧтоЗаключеноСоглашениеСПФР(Док, Организация, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФР_ПослеПроверкиЗаключенногоСоглашенияСПФР(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> Истина Тогда
		
		ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВПФР_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(Док, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФР_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка письма в ПФР
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаНеформализованногоДокументаВПФР",  0.1, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаНеформализованногоДокументаВПФР() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();

	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВПФРПослеАвтонастройкиУчетнойЗаписи", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(КонтекстДлительнойОперации.Док, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФР_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	Параметры_ОтобразитьРезультат	= ВходящийКонтекст.ОтобразитьРезультат;
	
	ОтправкаНеформализованногоДокументаВПФР(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение, Параметры_ОтобразитьРезультат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФРПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВПФР_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.Док, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФР_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ОтобразитьРезультат = ВходящийКонтекст.ОтобразитьРезультат;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// отражаем изменения в базе
	Если НЕ ОтправкаНеформализованногоДокументаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаНеформализованногоДокументаВПФР_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
				
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Док);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		КонецЕсли;
	
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Формирование пакета...");
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОтобразитьРезультат", ОтобразитьРезультат);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВПФРПослеФормированияПакета", ЭтотОбъект, Контекст);
		
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ПФР");

КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВПФРПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		Возврат;
	КонецЕсли;
	
	Если ВходящийКонтекст.ОтобразитьРезультат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // Письмо в ПФР
	Иначе
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, КонтекстОтправки.Документ);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаНеформализованногоДокументаВПФР_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Док = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ПФР
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСПФР") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, указан ли получатель
	ПолучательДокумента = Док.Получатель;
	Если НЕ ЗначениеЗаполнено(ПолучательДокумента) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель письма.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Приложения = ПолучитьВложенияНеформализованногоДокумента(Док, , Истина);
	
	// получаем содержание письма
	ТекстСодержания = Док.Содержание;
	
	// проверяем, не пустое ли письмо
	Если НЕ ЗначениеЗаполнено(ТекстСодержания) И Приложения.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'В письме не задано содержание и отсутствуют вложения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ИмяФайлаСодержимого = ВыгрузитьТекстВФайл(ТекстСодержания);
	КороткоеИмяФайлаСодержимого = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР;
	ЦиклОбмена.Предмет = Док;
	ЦиклОбмена.Идентификатор = Док.Идентификатор;
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = Док.Получатель;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = Док.Получатель;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем письмо
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо, ИмяФайлаСодержимого, КороткоеИмяФайлаСодержимого, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.Текст1251) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка письма прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем приложения к письму
	Для Каждого СтрПриложение Из Приложения Цикл
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение, СтрПриложение.Данные, СтрПриложение.ИмяФайла, СтрПриложение.Размер, , , , , , СгенерироватьUUID(), ТипСодержимогоПоИмениФайла(СтрПриложение.ИмяФайла, Истина)) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Док, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ТипТранспортногоСообщения", ТранспортноеСообщение.Тип);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура ПоказатьПротоколОнлайнПроверки(
		Отчет, 
		ИмяФайлаВыгрузки, 
		ТекстВыгрузки, 
		ПротоколПроверки, 
		ИмяФайлаПротокола, 
		РезультатПроверкиВУниверсальномФормате = Неопределено, 
		ПрограммаПроверки = "", 
		ПоказФормаУспешнойПроверкиРазрешен = Ложь, 
		ВыполняемоеОповещение = Неопределено) Экспорт
	
	СтруктураПараметров = Новый Структура("Отчет, ИмяФайлаВыгрузки, ТекстВыгрузки, Протокол, ИмяФайлаПротокола, ПрограммаПроверки");
	СтруктураПараметров.Отчет = Отчет;
	Если ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") Тогда
		СтруктураПараметров.ИмяФайлаВыгрузки = ?(ИмяФайлаВыгрузки.Количество() > 0, ИмяФайлаВыгрузки[0], "");
	Иначе
		СтруктураПараметров.ИмяФайлаВыгрузки = ИмяФайлаВыгрузки;
	КонецЕсли;
	Если ТипЗнч(ТекстВыгрузки) = Тип("Массив") Тогда
		СтруктураПараметров.ТекстВыгрузки = ?(ТекстВыгрузки.Количество() > 0, ТекстВыгрузки[0], "");
	Иначе
		СтруктураПараметров.ТекстВыгрузки = ТекстВыгрузки;
	КонецЕсли;
	СтруктураПараметров.Протокол = ПротоколПроверки;
	СтруктураПараметров.ИмяФайлаПротокола = ИмяФайлаПротокола;
	СтруктураПараметров.ПрограммаПроверки = ПрограммаПроверки;
	
	Если (РезультатПроверкиВУниверсальномФормате <> 0 И РезультатПроверкиВУниверсальномФормате <> "0") ИЛИ НЕ ПоказФормаУспешнойПроверкиРазрешен Тогда

		СтруктураПараметров.Вставить("РезультатПроверкиВУниверсальномФормате", Строка(РезультатПроверкиВУниверсальномФормате));

		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОРезультатеПроверкиВИнтернете(Ложь, СтруктураПараметров);
		Иначе
			ПоказатьРезультатОнлайнПроверкиПоПараметрам(СтруктураПараметров, ВыполняемоеОповещение);
		КонецЕсли;
	Иначе

		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОРезультатеПроверкиВИнтернете(Истина, СтруктураПараметров);
		Иначе
			ПоказатьРезультатУспешнойОнлайнПроверкиПоПараметрам(СтруктураПараметров, ВыполняемоеОповещение);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьРезультатУспешнойОнлайнПроверкиПоПараметрам(СтруктураПараметров, ВыполняемоеОповещение = Неопределено) Экспорт
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.РезультатУспешнойОнлайнПроверки", 
		СтруктураПараметров,
		,
		,
		,
		,
		ВыполняемоеОповещение, 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);	
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьРезультатОнлайнПроверкиПоПараметрам(СтруктураПараметров, ВыполняемоеОповещение = Неопределено) Экспорт
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.РезультатОнлайнПроверки", 
		СтруктураПараметров,
		,
		,
		,
		,
		ВыполняемоеОповещение, 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);	
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ОНЛАЙН-ПРОВЕРКА РЕГЛАМЕНТИРОВАННОЙ ОТЧЕТНОСТИ
//

&НаСервереБезКонтекста
Функция ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПодготовкаИПроверкаНаСервере(Контекст)
	
	Если ТипЗнч(Контекст.Документ) = Тип("Массив") Тогда
		ОсновнойОтчет = Контекст.Документ[0];
	Иначе
		ОсновнойОтчет = Контекст.Документ;
	КонецЕсли;
	
	МетаданныеОбъекта = Метаданные.НайтиПоТипу(ТипЗнч(ОсновнойОтчет));
	Если МетаданныеОбъекта = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Неизвестный тип документа!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ИмяТипаОтчета = МетаданныеОбъекта.Имя;
	
	Если ИмяТипаОтчета = "РегламентированныйОтчет" Тогда
		ИсточникОтчета = ОсновнойОтчет.ИсточникОтчета;
	КонецЕсли;
	
	ОрганизацияОтчета = ОрганизацияОтчетаДляОнлайнПроверки(Контекст.Документ);
	УчетнаяЗаписьОрганизации = УчетнаяЗаписьОрганизации(ОрганизацияОтчета);
	
	ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Неопределено;
	Если УчетнаяЗаписьОрганизации <> Неопределено И УчетнаяЗаписьОрганизации <> Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка() Тогда
		ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(УчетнаяЗаписьОрганизации.СпецоператорСвязи, "ОнлайнПроверкаПризнак");
		Если ЗначениеЗаполнено(ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате) Тогда
			ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Булево(ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате);
		КонецЕсли;
	КонецЕсли;
	
	ОтправкаПерсональныхДанныхРазрешена = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ОтправкаПерсональныхДанныхРазрешена");
	
	ЭтоНДС = (ТипЗнч(ОсновнойОтчет) = Тип("ДокументСсылка.РегламентированныйОтчет") И ИсточникОтчета = "РегламентированныйОтчетНДС"
		ИЛИ ТипЗнч(ОсновнойОтчет) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") И ОсновнойОтчет.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НДС);
	
	БольшеНеПоказыватьПредупреждениеПриложенияНДСНаПроверкуНеОтправляются = Ложь;
	Если ЭтоНДС Тогда
		КлючОбъектаПредупрежденияНДС = ?(ТипЗнч(ОсновнойОтчет) = Тип("ДокументСсылка.РегламентированныйОтчет"),
			"ПредупреждениеПриложенияНДСНаПроверкуНеОтправляются",
			"ПредупреждениеПриложенияНДСНаПроверкуНеОтправляютсяДляЭлектронныхПредставлений");
		КлючНастроекПредупрежденияНДС = "БольшеНеПоказывать";
		
		БольшеНеПоказыватьПредупреждениеПриложенияНДСНаПроверкуНеОтправляются = ОбщегоНазначения.ХранилищеОбщихНастроекЗагрузить(
			КлючОбъектаПредупрежденияНДС,
			КлючНастроекПредупрежденияНДС,
			Ложь);
	КонецЕсли;
	
	Контекст.Вставить("Организация", ОрганизацияОтчета);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗаписьОрганизации);
	Контекст.Вставить("ТипОтправляемогоДокумента", ИмяТипаОтчета);
	Контекст.Вставить("ИсточникОтчета", ИсточникОтчета);
	Контекст.Вставить("ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате", ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате);
	Контекст.Вставить("ИнформацияОСервереОнлайнПроверки", Неопределено);
	Контекст.Вставить("ОтправкаПерсональныхДанныхРазрешена", ОтправкаПерсональныхДанныхРазрешена);
	Контекст.Вставить("ЭтоНДС", ЭтоНДС);
	Контекст.Вставить("БольшеНеПоказыватьПредупреждениеПриложенияНДСНаПроверкуНеОтправляются", БольшеНеПоказыватьПредупреждениеПриложенияНДСНаПроверкуНеОтправляются);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоместитьВнешнееЭлектронноеПредставлениеВоВременноеХранилище(Контекст, ДляОнлайнПроверки = Ложь)
	
	ЭлПредставления = ИзвлечьЭлектронныеПредставленияИзХранилища(Контекст.Документ);
	Если ЭлПредставления = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ДляОнлайнПроверки Тогда
		ИндексЭлПредставления = ЭлПредставления.Количество() - 1;
		Пока ИндексЭлПредставления >= 0 Цикл
			Если ЭлПредставления[ИндексЭлПредставления].ТипФайлаОтчетности = Перечисления.ТипыФайловБухОтчетности.АудиторскоеЗаключение
			ИЛИ ЭлПредставления[ИндексЭлПредставления].ТипФайлаОтчетности = Перечисления.ТипыФайловБухОтчетности.ЗаявлениеСоюзаСельхозПроизводителей
			ИЛИ ЭлПредставления[ИндексЭлПредставления].ТипФайлаОтчетности = Перечисления.ТипыФайловБухОтчетности.ПояснительнаяЗаписка 
			ИЛИ ЭлПредставления[ИндексЭлПредставления].ТипФайлаОтчетности = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.СведенияПриложениеПФР Тогда
				ЭлПредставления.Удалить(ИндексЭлПредставления);
			КонецЕсли;
			ИндексЭлПредставления = ИндексЭлПредставления - 1;
		КонецЦикла;
	КонецЕсли;
	
	ВсегоВАрхиве = 0;
	Для Каждого ЗагруженныйОтчет Из ЭлПредставления Цикл
		Если ЗагруженныйОтчет.ВАрхиве Тогда
			ВсегоВАрхиве = ВсегоВАрхиве + 1;
		КонецЕсли;
	КонецЦикла;
	
	Если ВсегоВАрхиве > 0 Тогда
		ТекстСообщения = НСтр("ru = 'Файл загруженного отчета был ранее перемещен в архив. Извлечение содержимого невозможно!'");
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Если ЭлПредставления.Количество() = 1 Тогда
		
		ЭлПредставление = ЭлПредставления[0];
		ИмяФайлаВыгрузки = ЭлПредставление.ИмяФайла;
		ДанныеВыгрузки = ЭлПредставление.Данные;
		
		АдресФайлаВыгрузки = ПоместитьВоВременноеХранилище(ДанныеВыгрузки, Контекст.УникальныйИдентификатор);
		
		Контекст.Вставить("АдресФайлаВыгрузки", АдресФайлаВыгрузки);
		Контекст.Вставить("ИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
		
	Иначе
		
		АдресФайлаВыгрузки = Новый Массив;
		ИмяФайлаВыгрузки = Новый Массив;
		КодировкаФайла = Новый Массив;
		ТипФайлаВыгрузки = Новый Массив;
		ДокументыМассив = Новый Массив;
		
		Для Каждого ЭлПредставление Из ЭлПредставления Цикл
			
			АдресФайлаВыгрузки.Добавить(ПоместитьВоВременноеХранилище(ЭлПредставление.Данные, Контекст.УникальныйИдентификатор));
			ИмяФайлаВыгрузки.Добавить(ЭлПредставление.ИмяФайла);
			КодировкаФайла.Добавить("windows-1251");
			ТипФайлаВыгрузки.Добавить(ЭлПредставление.ТипФайлаОтчетности);
			ДокументыМассив.Добавить(Контекст.Документ);
			
		КонецЦикла;
		
		Контекст.Вставить("АдресФайлаВыгрузки", АдресФайлаВыгрузки);
		Контекст.Вставить("ИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
		Контекст.Вставить("КодировкаФайла", КодировкаФайла);
		Контекст.Вставить("ТипФайлаВыгрузки", ТипФайлаВыгрузки);
		Контекст.Вставить("Документы", ДокументыМассив);
		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ВыгрузитьОтчет(
	ОповещениеОЗавершении,
	Контекст,
	Форма = Неопределено,
	ДополнительныеДанные = Неопределено)
	
	ЭтоГрупповаяПроверка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки();

	ТекстВыгрузкиИлиАдрес = "";
	ИмяФайлаВыгрузки = "";
	КодировкаФайла = Неопределено;
	ТипФайлаВыгрузки = "";
	ДокументыВыгрузки = Неопределено;
	
	Отчет = Контекст.Документ;
	ТипПараметра = ТипЗнч(Отчет);
	
	Если Контекст.КонтролирующийОрган = "ФСС" Тогда
		
		ЭтоРеестрСведений = ДокументооборотСФССКлиентСервер.ЭтоРеестрСведенийНаВыплатуПособийФСС(Отчет);
		ЭтоРеестрЭЛН = ДокументооборотСФССКлиентСервер.ЭтоРеестрДанныхЭЛНЗаполняемыхРаботодателем(Отчет);
		ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам =
			ДокументооборотСФССКлиентСервер.ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(Отчет);
		
		Если ТипПараметра = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипПараметра = Тип("Неопределено") Тогда
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
			Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(Отчет, Ложь, , ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, КодировкаФайла) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				Иначе
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				КонецЕсли;

				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(ТекстВыгрузкиИлиАдрес) = Тип("Строка")
				И СтрНайти(ТекстВыгрузкиИлиАдрес, "<OKVEDConfirmationRequest") <> 0 Тогда
				
				// приложение файла поддерживается только при выгрузке подтверждения вида деятельности для СЭДО
				ПозицияПриложения = СтрНайти(ТекстВыгрузкиИлиАдрес, "<noteFile");
				Если ПозицияПриложения <> 0 Тогда
					ОкончаниеПриложения = "</noteFile>";
					ПозицияОкончанияПриложения = СтрНайти(ТекстВыгрузкиИлиАдрес, ОкончаниеПриложения);
					Если ПозицияОкончанияПриложения <> 0 Тогда
						ПозицияОкончанияПриложения = ПозицияОкончанияПриложения + СтрДлина(ОкончаниеПриложения);
						ТекстВыгрузкиИлиАдрес = Лев(ТекстВыгрузкиИлиАдрес, ПозицияПриложения - 1)
							+ Сред(ТекстВыгрузкиИлиАдрес, ПозицияОкончанияПриложения);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
			
		ИначеЕсли ЭтоРеестрСведений ИЛИ ЭтоРеестрЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
			КодировкаФайла = Неопределено;
			ПолучитьСвойстваФайлаВыгрузкиНаСервере(
				Отчет,
				ТекстВыгрузкиИлиАдрес,
				ИмяФайлаВыгрузки,
				КодировкаФайла);
			Если НЕ ЗначениеЗаполнено(КодировкаФайла) Тогда
				КодировкаФайла = "windows-1251";
			КонецЕсли;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = "МашиночитаемыеДоверенностиФСС" Тогда
			
			ОпределятьКодНалоговогоОрганаПолучателя = Истина;
			ФорматДоверенностиПоПриказуФНС 			= Истина;
			ПараметрыВозврата = Новый Структура;
			ПараметрыВозврата.Вставить("ВозвращатьАдресВоВременномХранилище", 	Истина);
			ПараметрыВозврата.Вставить("ВозвращатьИмяФайлаСРасширением", 		Истина);
			РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьМЧДФСС(
				Отчет,,,
				ПараметрыВозврата);
			
			Если НЕ ЗначениеЗаполнено(РезультатыВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(РезультатыВыгрузки) = Тип("Массив") Тогда
				РезультатыВыгрузки = РезультатыВыгрузки[0];
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес 	= РезультатыВыгрузки.Адрес;
			ИмяФайлаВыгрузки 		= РезультатыВыгрузки.Имя;
			КодировкаФайла 			= "utf-8";
			ТипФайлаВыгрузки 		= Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки 		= Отчет;
			
		ИначеЕсли ТипПараметра = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
			Если Отчет = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда

				Если ЭтоГрупповаяПроверка Тогда
					ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;

				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			КодировкаФайла = "windows-1251";
			ОпределеннаяКодировка = Неопределено;
			
			ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(Отчет, ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, ОпределеннаяКодировка);
			
			Если НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) ИЛИ НЕ ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда

				Если ЭтоГрупповаяПроверка Тогда
					ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;

				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось определить тип документа!'");
			
			Если ЭтоГрупповаяПроверка Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
			Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			КонецЕсли;
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
			Возврат;
		КонецЕсли;
		
	ИначеЕсли Контекст.КонтролирующийОрган = "ФСРАР" Тогда
		
		Если ТипПараметра = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипПараметра = Тип("Неопределено") Тогда
		
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
			
			Если Контекст.Свойство("АдресГотовыхДанных") И Контекст.АдресГотовыхДанных <> Неопределено Тогда
				ТекстВыгрузкиИлиАдрес = ПолучитьИзВременногоХранилищаЕслиСтрока(Контекст.АдресГотовыхДанных);
				Если НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) Тогда
					ТекстВыгрузкиИлиАдрес = Контекст.АдресГотовыхДанных;
				КонецЕсли;
				ИмяФайлаВыгрузки = Контекст.ИмяФайлаГотовыхДанных;
				КодировкаФайла = "windows-1251";
			Иначе
				Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(Отчет, Ложь, Ложь, ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, КодировкаФайла) Тогда
					ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
					
					Если ЭтоГрупповаяПроверка Тогда
						ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
					Иначе
					ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
					КонецЕсли;

					ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
					Возврат;
				КонецЕсли;
			КонецЕсли;
			
		Иначе // электронное представление
			Если Отчет = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
				Если ЭтоГрупповаяПроверка Тогда
					ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			КодировкаФайла = "windows-1251";
			ОпределеннаяКодировка = Неопределено;
			
			ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(Отчет, ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, ОпределеннаяКодировка);
			
			Если НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) ИЛИ НЕ ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
		
		КонецЕсли;
		
	ИначеЕсли Контекст.КонтролирующийОрган = "РПН" Тогда
		
		Если ТипПараметра = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипПараметра = Тип("Неопределено") Тогда
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
			Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(Отчет, Ложь, Ложь, ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, КодировкаФайла) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(ТекстВыгрузкиИлиАдрес) = Тип("Массив") Тогда
				АдресОсновногоФайлаВыгрузки = Неопределено;
				Для каждого ЭлементВыгрузки Из ТекстВыгрузкиИлиАдрес Цикл
					Если ТипЗнч(ЭлементВыгрузки) = Тип("Структура")
						И ЭлементВыгрузки.Свойство("АдресФайлаВыгрузки")
						И ЭлементВыгрузки.Свойство("ИмяФайлаВыгрузки") Тогда
						
						РасширениеВыгрузки = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
							ЭлементВыгрузки.ИмяФайлаВыгрузки).Расширение;
						Если нрег(РасширениеВыгрузки) = ".xml" Тогда
							АдресОсновногоФайлаВыгрузки = ЭлементВыгрузки.АдресФайлаВыгрузки;
							ИмяФайлаВыгрузки = ЭлементВыгрузки.ИмяФайлаВыгрузки;
							Прервать;
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
				Если АдресОсновногоФайлаВыгрузки = Неопределено И ТекстВыгрузкиИлиАдрес.Количество() > 0
					И ТипЗнч(ТекстВыгрузкиИлиАдрес[0]) = Тип("Структура")
					И ТекстВыгрузкиИлиАдрес[0].Свойство("АдресФайлаВыгрузки")
					И ТекстВыгрузкиИлиАдрес[0].Свойство("ИмяФайлаВыгрузки") Тогда
					
					АдресОсновногоФайлаВыгрузки = ТекстВыгрузкиИлиАдрес[0].АдресФайлаВыгрузки;
					ИмяФайлаВыгрузки = ТекстВыгрузкиИлиАдрес[0].ИмяФайлаВыгрузки;
				КонецЕсли;
				Если АдресОсновногоФайлаВыгрузки <> Неопределено Тогда
					ТекстВыгрузкиИлиАдрес = АдресОсновногоФайлаВыгрузки;
				КонецЕсли;
			КонецЕсли;
			
		Иначе // электронное представление
			Если Отчет = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			КодировкаФайла = "utf-8";
			ОпределеннаяКодировка = Неопределено;
			
			ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
				Отчет,
				ТекстВыгрузкиИлиАдрес,
				ИмяФайлаВыгрузки,
				ОпределеннаяКодировка,
				"utf-8");
			
			Если НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) ИЛИ НЕ ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
		КонецЕсли;
		
	ИначеЕсли Контекст.КонтролирующийОрган = "ФТС" Тогда
		
		Если ТипПараметра = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипПараметра = Тип("Неопределено") Тогда
			
			РезультатВыгрузкиИзЗаявленияОВвозеТоваров = ПопытатьсяВыгрузитьСтатформуИзЗаявленияОВвозеТоваров(
				ТекстВыгрузкиИлиАдрес,
				ИмяФайлаВыгрузки,
				Отчет);
				
			Если РезультатВыгрузкиИзЗаявленияОВвозеТоваров = Неопределено Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			ИначеЕсли РезультатВыгрузкиИзЗаявленияОВвозеТоваров Тогда
				КодировкаФайла = "utf-8";
			КонецЕсли;
			
			Если НЕ РезультатВыгрузкиИзЗаявленияОВвозеТоваров Тогда
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
				Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(Отчет, Ложь, Ложь, ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, КодировкаФайла) Тогда
					ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					Если ЭтоГрупповаяПроверка Тогда
						ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
					КонецЕсли;
					ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
					Возврат;
				КонецЕсли;
			КонецЕсли;
			
		Иначе // электронное представление
			Если Отчет = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			КодировкаФайла = "utf-8";
			ОпределеннаяКодировка = Неопределено;
			
			ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
				Отчет,
				ТекстВыгрузкиИлиАдрес,
				ИмяФайлаВыгрузки,
				ОпределеннаяКодировка,
				"utf-8");
			
			Если НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) ИЛИ НЕ ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
		
		КонецЕсли;
		
	ИначеЕсли Контекст.КонтролирующийОрган = "ЦБ" Тогда
		
		ТекстСообщения = НСтр("ru = 'Проверка в интернете не поддерживается'");
		Если ЭтоГрупповаяПроверка Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
		
	Иначе
		
		Если ТипПараметра = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
			
			// если на входе электронное представление, то получим его из ИБ
			Если НЕ ПоместитьВнешнееЭлектронноеПредставлениеВоВременноеХранилище(Контекст, Истина) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Контекст);
			Возврат;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = "УведомлениеОПолучателеДокументов" Тогда
			
			Если ДополнительныеДанные = Неопределено Тогда
				СертификатыДолжностныхЛиц = СертификатыДолжностныхЛицПолучателяДокументов(Отчет);
				
				Если СертификатыДолжностныхЛиц <> Неопределено И ЭтоЛокальнаяПодпись(СертификатыДолжностныхЛиц)
				И ЗначениеЗаполнено(СертификатыДолжностныхЛиц.ОтпечатокСертификатаДляШифрования) Тогда
					СвойстваСертификатаДляШифроания = Новый Структура("Отпечаток", СертификатыДолжностныхЛиц.ОтпечатокСертификатаДляШифрования);
					
					ДополнительныеПараметры = Новый Структура("ОповещениеОЗавершении, Контекст, Форма", ОповещениеОЗавершении, Контекст, Форма);
					Оповещение = Новый ОписаниеОповещения(
						"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ВыгрузитьОтчетПослеПоискаСертификатаДляШифрования", ЭтотОбъект, ДополнительныеПараметры);
					КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, СвойстваСертификатаДляШифроания,, Ложь);
					Возврат;
				КонецЕсли;
			КонецЕсли;
			
			ВладелецСертификатаОтветственногоЗаШифрование = ?(ДополнительныеДанные <> Неопределено,
				ДополнительныеДанные.ВладелецСертификатаОтветственногоЗаШифрование, Неопределено);
			
			ФайлУведомленияОПолучателеДокументов = СформироватьФайлУведомленияОПолучателеДокументов(
				Отчет,
				ВладелецСертификатаОтветственногоЗаШифрование,
				Контекст.УникальныйИдентификатор);
			
			Если ФайлУведомленияОПолучателеДокументов = Неопределено Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес = ФайлУведомленияОПолучателеДокументов.АдресФайлаУведомления;
			ИмяФайлаВыгрузки = ФайлУведомленияОПолучателеДокументов.ИмяФайлаУведомления;
			КодировкаФайла = "windows-1251";
			ТипФайлаВыгрузки = Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки = Отчет;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = "МашиночитаемыеДоверенностиФНС"
			ИЛИ Контекст.ТипОтправляемогоДокумента = "ЗаявленияОбОтзывеМЧДФНС" Тогда
			
			ОпределятьКодНалоговогоОрганаПолучателя = Истина;
			ФорматДоверенностиПоПриказуФНС 			= Истина;
			ПараметрыВозврата = Новый Структура;
			ПараметрыВозврата.Вставить("ВозвращатьАдресВоВременномХранилище", 	Истина);
			ПараметрыВозврата.Вставить("ВозвращатьИмяФайлаСРасширением", 		Истина);
			ПараметрыВозврата.Вставить("ВозвращатьПриложеннуюПодпись", 			Истина);
			Если Контекст.ТипОтправляемогоДокумента = "МашиночитаемыеДоверенностиФНС" Тогда
				РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьМЧД(
					Отчет,,,
					ОпределятьКодНалоговогоОрганаПолучателя,
					ФорматДоверенностиПоПриказуФНС,
					Истина,
					ПараметрыВозврата);
			Иначе
				РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьЗаявлениеОбОтзывеМЧДФНС(
					Отчет,,,
					ОпределятьКодНалоговогоОрганаПолучателя,
					ФорматДоверенностиПоПриказуФНС,
					Истина,
					ПараметрыВозврата);
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(РезультатыВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(РезультатыВыгрузки) = Тип("Массив") Тогда
				РезультатыВыгрузки = РезультатыВыгрузки[0];
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес 	= РезультатыВыгрузки.Адрес;
			ИмяФайлаВыгрузки 		= РезультатыВыгрузки.Имя;
			КодировкаФайла 			= "windows-1251";
			ТипФайлаВыгрузки 		= Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки 		= Отчет;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = "МашиночитаемыеДоверенностиРаспределенныйРеестр" Тогда
			
			ОпределятьКодНалоговогоОрганаПолучателя = Истина;
			ФорматДоверенностиПоПриказуФНС 			= Истина;
			ПараметрыВозврата = Новый Структура;
			ПараметрыВозврата.Вставить("ВозвращатьАдресВоВременномХранилище", Истина);
			РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьМЧДРР(
				Отчет,,,
				ОпределятьКодНалоговогоОрганаПолучателя,
				ФорматДоверенностиПоПриказуФНС,
				Истина,
				ПараметрыВозврата);
			
			Если НЕ ЗначениеЗаполнено(РезультатыВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(РезультатыВыгрузки) = Тип("Массив") Тогда
				РезультатыВыгрузки = РезультатыВыгрузки[0];
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес 	= РезультатыВыгрузки.Адрес;
			ИмяФайлаВыгрузки 		= РезультатыВыгрузки.Имя;
			КодировкаФайла 			= "windows-1251";
			ТипФайлаВыгрузки 		= Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки 		= Отчет;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = "МашиночитаемыеДоверенности"
			И ДокументооборотСКОКлиентСервер.ПодсистемаЦПРРМЧДСуществует() Тогда
			
			РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьМЧДЕдиногоФормата(Отчет);
			
			Если НЕ ЗначениеЗаполнено(РезультатыВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(РезультатыВыгрузки) = Тип("Массив") Тогда
				РезультатыВыгрузки = РезультатыВыгрузки[0];
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес 	= РезультатыВыгрузки.Адрес;
			ИмяФайлаВыгрузки 		= РезультатыВыгрузки.Имя;
			КодировкаФайла 			= "utf-8";
			ТипФайлаВыгрузки 		= Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки 		= Отчет;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = "УведомлениеОПредоставленииПолномочийПредставителю"
			ИЛИ Контекст.ТипОтправляемогоДокумента = "УведомлениеОПрекращенииПолномочийПредставителя" Тогда
			
			Если Контекст.ТипОтправляемогоДокумента = "УведомлениеОПредоставленииПолномочийПредставителю" Тогда
				РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьУведомлениеОПредоставленииПолномочийПредставителю(
					Отчет);
			Иначе
				РезультатыВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьУведомлениеОПрекращенииПолномочийПредставителя(
					Отчет);
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(РезультатыВыгрузки) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если ТипЗнч(РезультатыВыгрузки) = Тип("Массив") Тогда
				РезультатыВыгрузки = РезультатыВыгрузки[0];
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес 	= РезультатыВыгрузки.Адрес;
			ИмяФайлаВыгрузки 		= РезультатыВыгрузки.Имя;
			КодировкаФайла 			= "utf-8";
			ТипФайлаВыгрузки 		= Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки 		= Отчет;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("УведомлениеОКонтролируемыхСделках")
			ИЛИ Контекст.ТипОтправляемогоДокумента = "УведомлениеОСпецрежимахНалогообложения" Тогда
			
			МассивВыгруженныхУведомлений = Неопределено;
			Если НЕ ВыгрузитьУведомлениеФНС(Отчет, МассивВыгруженныхУведомлений) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить файл выгрузки уведомления'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес = Новый Массив;
			ИмяФайлаВыгрузки = Новый Массив;
			КодировкаФайла = Новый Массив;
			ТипФайлаВыгрузки = Новый Массив;
			ДокументыВыгрузки = Новый Массив;
			
			Для Каждого РезультатВыгрузкиУведомления Из МассивВыгруженныхУведомлений Цикл
				
				ДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(РезультатВыгрузкиУведомления.АдресФайлаВыгрузки);
				АдресФайлаВыгрузки = ПоместитьВоВременноеХранилище(ДанныеФайлаВыгрузки, Контекст.УникальныйИдентификатор);
				
				ТекстВыгрузкиИлиАдрес.Добавить(АдресФайлаВыгрузки);
				ИмяФайлаВыгрузки.Добавить(РезультатВыгрузкиУведомления.ИмяФайлаВыгрузки);
				КодировкаФайла.Добавить("windows-1251");
				ТипФайлаВыгрузки.Добавить(Контекст.ТипОтправляемогоДокумента);
				ДокументыВыгрузки.Добавить(Отчет);
				
			КонецЦикла;
			
			Если ТекстВыгрузкиИлиАдрес.Количество() >= 1 Тогда
				ТекстВыгрузкиИлиАдрес = ТекстВыгрузкиИлиАдрес[0];
				ИмяФайлаВыгрузки = ИмяФайлаВыгрузки[0];
				КодировкаФайла = КодировкаФайла[0];
				ТипФайлаВыгрузки = ТипФайлаВыгрузки[0];
				ДокументыВыгрузки = ДокументыВыгрузки[0];
			КонецЕсли;
			
		ИначеЕсли Контекст.ТипОтправляемогоДокумента = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("ЖурналУчетаСчетовФактурДляПередачиВЭлектронномВиде") Тогда
			
			РезультатВыгрузкиЖурнала = ПолучитьДанныеФайлаВыгрузкиЖурналаСчетовФактур(Отчет, Контекст.УникальныйИдентификатор);
			Если РезультатВыгрузкиЖурнала = Неопределено ИЛИ НЕ ЗначениеЗаполнено(РезультатВыгрузкиЖурнала.ИдФайл) ИЛИ НЕ ЗначениеЗаполнено(РезультатВыгрузкиЖурнала.АдресВременногоХранилища) Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить файл выгрузки журнала'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			ТекстВыгрузкиИлиАдрес = РезультатВыгрузкиЖурнала.АдресВременногоХранилища;
			ИмяФайлаВыгрузки = РезультатВыгрузкиЖурнала.ИдФайл;
			КодировкаФайла = "windows-1251";
			ТипФайлаВыгрузки = Контекст.ТипОтправляемогоДокумента;
			ДокументыВыгрузки = Отчет;
			
		ИначеЕсли ДокументооборотСКОВызовСервера.ЭтоРегламентированныйОтчетРеестрНДС(Отчет) Тогда
			
			ДанныеФайлаРеестраНДС = ПолучитьДанныеФайлаРеестраНДС(Отчет, Форма);
			
			ТекстВыгрузкиИлиАдрес = Новый Массив;
			ИмяФайлаВыгрузки = Новый Массив;
			КодировкаФайла = Новый Массив;
			ТипФайлаВыгрузки = Новый Массив;
			ДокументыВыгрузки = Новый Массив;
			
			Если ДанныеФайлаРеестраНДС.Количество() = 0 Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			Иначе
				
			Для Каждого ДанныеФайла Из ДанныеФайлаРеестраНДС Цикл
				
				ТекстВыгрузкиИлиАдрес.Добавить(ДанныеФайла.Адрес);
				ИмяФайлаВыгрузки.Добавить(ДанныеФайла.Имя);
				КодировкаФайла.Добавить(ДанныеФайла.Кодировка);
				ТипФайлаВыгрузки.Добавить(Контекст.ТипОтправляемогоДокумента);
				ДокументыВыгрузки.Добавить(Отчет);
				
			КонецЦикла;
			
			КонецЕсли;
			
		ИначеЕсли ДокументооборотСКОВызовСервера.ЭтоРегламентированныйОтчетРеестрАкцизы(Отчет) Тогда
			
			ДанныеФайлаРеестраАкцизов = ПолучитьДанныеФайлаРеестраНДС(Отчет, Форма);
			
			ТекстВыгрузкиИлиАдрес = Новый Массив;
			ИмяФайлаВыгрузки = Новый Массив;
			КодировкаФайла = Новый Массив;
			ТипФайлаВыгрузки = Новый Массив;
			ДокументыВыгрузки = Новый Массив;
			
			Если ДанныеФайлаРеестраАкцизов.Количество() = 0 Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			Иначе
			
			Для Каждого ДанныеФайла Из ДанныеФайлаРеестраАкцизов Цикл
				
				ТекстВыгрузкиИлиАдрес.Добавить(ДанныеФайла.Адрес);
				ИмяФайлаВыгрузки.Добавить(ДанныеФайла.Имя);
				КодировкаФайла.Добавить(ДанныеФайла.Кодировка);
				ТипФайлаВыгрузки.Добавить(Контекст.ТипОтправляемогоДокумента);
				ДокументыВыгрузки.Добавить(Отчет);
				
			КонецЦикла;
			
			КонецЕсли;
			
		Иначе
			
			ВозвращатьИменаИТекстыФайловВыгрузкиВВидеМассивов = (ТипЗнч(Отчет) = Тип("ДокументСсылка.РегламентированныйОтчет")
				И ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(Отчет, "ИсточникОтчета") = "РегламентированныйОтчетРСВ1");
			
			// выгружаем
			РезультатВыгрузки = СдачаОтчетностиЧерезСервисСпецоператораКлиент.ВыгрузитьРегламентированныйОтчет(
				Отчет, 
				Ложь, 
				ТекстВыгрузкиИлиАдрес, 
				ИмяФайлаВыгрузки, 
				КодировкаФайла, 
				ТипФайлаВыгрузки, 
				ДокументыВыгрузки, 
				Контекст.УникальныйИдентификатор,
				ВозвращатьИменаИТекстыФайловВыгрузкиВВидеМассивов);

				
			Если НЕ РезультатВыгрузки Тогда
				ТекстСообщения = НСтр("ru = 'Не удалось выгрузить отчет!'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Если ЭтоГрупповаяПроверка Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
				Возврат;
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) И ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") И ИмяФайлаВыгрузки.Количество() = 1
				И ТипЗнч(ДокументыВыгрузки) = Тип("Массив") И ДокументыВыгрузки.Количество() > 0 И ТипЗнч(ДокументыВыгрузки[0]) = Тип("Структура") Тогда
				ТекстВыгрузкиИлиАдрес = ДокументыВыгрузки[0].АдресФайлаВыгрузки;
				ИмяФайлаВыгрузки = ИмяФайлаВыгрузки[0];
				ТипФайлаВыгрузки = ДокументыВыгрузки[0].ТипФайлаВыгрузки;
				ДокументыВыгрузки = Отчет;
			ИначеЕсли НЕ ЗначениеЗаполнено(ТекстВыгрузкиИлиАдрес) И ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") И ТипЗнч(ДокументыВыгрузки) = Тип("Массив")
				И ТипЗнч(ДокументыВыгрузки[0]) = Тип("Строка") Тогда
				ТекстВыгрузкиИлиАдрес = Новый Массив;
				Для ИндексДокументаВыгрузки = 0 По ДокументыВыгрузки.Количество() - 1 Цикл
					АдресФайлаВыгрузки = ЗаписатьФайлВыгрузкиВоВременноеХранилище(ДокументыВыгрузки[ИндексДокументаВыгрузки], ИмяФайлаВыгрузки[ИндексДокументаВыгрузки],
						КодировкаФайла, Контекст.УникальныйИдентификатор);
					ТекстВыгрузкиИлиАдрес.Добавить(АдресФайлаВыгрузки);
					ДокументыВыгрузки[ИндексДокументаВыгрузки] = Отчет;
				КонецЦикла;
			КонецЕсли;
			
			Если (ТипЗнч(ТекстВыгрузкиИлиАдрес) = Тип("Массив") ИЛИ ТипЗнч(ТекстВыгрузкиИлиАдрес) = Тип("ФиксированныйМассив"))
				И (ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") ИЛИ ТипЗнч(ИмяФайлаВыгрузки) = Тип("ФиксированныйМассив"))
				И (ТипЗнч(КодировкаФайла) = Тип("Массив") ИЛИ ТипЗнч(КодировкаФайла) = Тип("ФиксированныйМассив"))
				И (ТипЗнч(ТипФайлаВыгрузки) = Тип("Массив") ИЛИ ТипЗнч(ТипФайлаВыгрузки) = Тип("ФиксированныйМассив"))
				И (ТипЗнч(ДокументыВыгрузки) = Тип("Массив") ИЛИ ТипЗнч(ДокументыВыгрузки) = Тип("ФиксированныйМассив")) Тогда
				
				Для каждого ТипФайлаВыгрузкиОтчета Из ТипФайлаВыгрузки Цикл
					Если ТипФайлаВыгрузкиОтчета = "ДокументацияПоКонтролируемымСделкам" Тогда
						ТекстВыгрузкиИлиАдрес = ТекстВыгрузкиИлиАдрес[0];
						ИмяФайлаВыгрузки = ИмяФайлаВыгрузки[0];
						КодировкаФайла = КодировкаФайла[0];
						ТипФайлаВыгрузки = ТипФайлаВыгрузки[0];
						ДокументыВыгрузки = ДокументыВыгрузки[0];
						Прервать;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ТипЗнч(ТекстВыгрузкиИлиАдрес) = Тип("Строка") Тогда
		Если НЕ ЭтоАдресВременногоХранилища(ТекстВыгрузкиИлиАдрес) Тогда
			АдресФайлаВыгрузки = ЗаписатьФайлВыгрузкиВоВременноеХранилище(ТекстВыгрузкиИлиАдрес, ИмяФайлаВыгрузки, КодировкаФайла, Контекст.УникальныйИдентификатор);
		Иначе
			АдресФайлаВыгрузки = ТекстВыгрузкиИлиАдрес;
		КонецЕсли;
	Иначе
		АдресФайлаВыгрузки = ТекстВыгрузкиИлиАдрес;
	КонецЕсли;
	
	Контекст.Вставить("АдресФайлаВыгрузки", АдресФайлаВыгрузки);
	Контекст.Вставить("ИмяФайлаВыгрузки", 	ИмяФайлаВыгрузки);
	Контекст.Вставить("КодировкаФайла", 	КодировкаФайла);
	Контекст.Вставить("ТипФайлаВыгрузки", 	ТипФайлаВыгрузки);
	Контекст.Вставить("Документы", 			ДокументыВыгрузки);
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Контекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ВыгрузитьОтчетПослеПоискаСертификатаДляШифрования(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОЗавершении 	= ДополнительныеПараметры.ОповещениеОЗавершении;
	Контекст 				= ДополнительныеПараметры.Контекст;
	Форма 					= ДополнительныеПараметры.Форма;
	
	ВладелецСертификатаОтветственногоЗаШифрование = ?(Результат.Выполнено И Результат.СертификатНайден,
		Результат.СвойстваСертификата.Владелец, Неопределено);
	ДополнительныеДанные = Новый Структура("ВладелецСертификатаОтветственногоЗаШифрование", ВладелецСертификатаОтветственногоЗаШифрование);
	
	ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ВыгрузитьОтчет(ОповещениеОЗавершении, Контекст, Форма, ДополнительныеДанные);
	
КонецПроцедуры

&НаКлиенте
Функция ШаблонИнформацииОСервереОнлайнПроверки() Экспорт
	
	ИнформацияОСервереОнлайнПроверки = Новый Структура();
	ИнформацияОСервереОнлайнПроверки.Вставить("Наименование",  "");
	ИнформацияОСервереОнлайнПроверки.Вставить("ТипРеализации", "");
	ИнформацияОСервереОнлайнПроверки.Вставить("Ресурс",        Неопределено);
	ИнформацияОСервереОнлайнПроверки.Вставить("Отпечаток",     "");
	
	Возврат ИнформацияОСервереОнлайнПроверки;
	
КонецФункции

&НаКлиенте
Функция РезультатАвтонастройкиИзКэша(Организация) Экспорт
	
	ФормаГрупповойПроверки = ДлительнаяОтправкаКлиент.ФормаГрупповойОтправкиИзОткрытых();
	
	РезультатАвтонастройки = Неопределено;
	
	Если ФормаГрупповойПроверки <> Неопределено Тогда
		РезультатыАвтонастройкиОрганизации = ФормаГрупповойПроверки.РезультатыАвтонастройки.Получить(Организация);
		Если РезультатыАвтонастройкиОрганизации <> Неопределено Тогда
			РезультатАвтонастройки  = РезультатыАвтонастройкиОрганизации.РезультатАвтонастройки;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат РезультатАвтонастройки;
	
КонецФункции

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_Проверка(
		Контекст,
		Форма,
		ВыполняемоеОповещение)
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Истина Тогда
		
		Контекст.ИнформацияОСервереОнлайнПроверки = ШаблонИнформацииОСервереОнлайнПроверки();
		
		ОсновнойОтчет = Контекст.Документ;
		Если ТипЗнч(Контекст.Документ) = Тип("Массив") Тогда
			ОсновнойОтчет = Контекст.Документ[0];
		КонецЕсли;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Контекст",              Контекст);
		
		РезультатАвтонастройки = РезультатАвтонастройкиИзКэша(Контекст.Организация);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПроверкаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		Если РезультатАвтонастройки = Неопределено Тогда
			
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(
			ОсновнойОтчет,
			ОписаниеОповещения,
			Контекст.ИнформацияОСервереОнлайнПроверки);
		
	Иначе
		
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатАвтонастройки);

		КонецЕсли;

	Иначе
		
		РезультатПроверки 		= Неопределено;
		ПовторениеНеобходимо 	= Истина;
		КоличествоПовторений 	= 0;
		
		ВыполнитьОнлайнПроверкуОтчета(ВыполняемоеОповещение, Контекст, КоличествоПовторений);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПроверкаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	
	Если ЗначениеЗаполнено(Результат)
		И Результат.Свойство("СтруктураДляИнформацииОСервереОнлайнПроверки") Тогда
		
		Контекст.ИнформацияОСервереОнлайнПроверки = Результат.СтруктураДляИнформацииОСервереОнлайнПроверки;
		
		РезультатПроверки = Неопределено;
		ПовторениеНеобходимо = Истина;
		КоличествоПовторений = 0;
		
		ВыполнитьОнлайнПроверкуОтчета(ВыполняемоеОповещение, Контекст, КоличествоПовторений);
	
	Иначе
		ТекстПредупреждения = НСтр("ru = 'Учетная запись документооборота не поддерживает проверку в интернете'");
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
		Иначе
			ПоказатьПредупреждение(, ТекстПредупреждения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОнлайнПроверкуОтчета(ВыполняемоеОповещение, Контекст, КоличествоПовторений)
	
	// вызываем онлайн-проверку
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, КоличествоПовторений", ВыполняемоеОповещение, КоличествоПовторений);
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыполнитьОнлайнПроверкуОтчета_ОнлайнПроверкаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОнлайнПроверкаОтчета(Контекст, ОписаниеОповещения); // клиент
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОнлайнПроверкуОтчета_ОнлайнПроверкаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	КоличествоПовторений  = ДополнительныеПараметры.КоличествоПовторений;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст              = Результат.Контекст;
	РезультатПроверки     = Результат.РезультатПроверки;
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыполнитьОнлайнПроверкуОтчетаЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
	Если НЕ РезультатПроверки Тогда
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии();
		Иначе
			ПоказатьПредупреждение(ОписаниеОповещения, НСтр("ru = 'Не удалось проверить отчет!'"));
		КонецЕсли;
	Иначе
		
		РезультатПроверки = Контекст.Результат;
		КоличествоПроверокДляУниверсальногоФормата = ?(РезультатПроверки.Свойство("КоличествоПроверокДляУниверсальногоФормата"), РезультатПроверки.КоличествоПроверокДляУниверсальногоФормата, 0);
		КоличествоПротоколовДляУниверсальногоФормата = ?(РезультатПроверки.Свойство("КоличествоПротоколовДляУниверсальногоФормата"), РезультатПроверки.КоличествоПротоколовДляУниверсальногоФормата, 0);
		
		// анализируем код результата, для универсального формата возможно получение протокола и при успешном результате проверки
		КодРезультатаПроверки = РезультатПроверки.КодРезультата;
		Если КодРезультатаПроверки = 0 И КоличествоПротоколовДляУниверсальногоФормата = 0 И КоличествоПроверокДляУниверсальногоФормата <= 1 Тогда
			
			Если РезультатПроверки.Свойство("Протокол") И ЗначениеЗаполнено(РезультатПроверки.Протокол) Тогда
				ПоказатьПротоколОнлайнПроверки(
					Контекст.Документ,
					Контекст.ИмяФайлаВыгрузки,
					Контекст.АдресФайлаВыгрузки,
					?(РезультатПроверки.Свойство("Протокол"), РезультатПроверки.Протокол, ""),
					?(РезультатПроверки.Свойство("ИмяФайлаПротокола"), РезультатПроверки.ИмяФайлаПротокола, ""),
					КодРезультатаПроверки,
					?(РезультатПроверки.Свойство("ПрограммаПроверки"), РезультатПроверки.ПрограммаПроверки, ""),
					Истина,
					ВыполняемоеОповещение); // клиент
				
			Иначе
				ПерваяПрограммаПроверки = ?(РезультатПроверки.Свойство("ПерваяПрограммаПроверки"), РезультатПроверки.ПерваяПрограммаПроверки, "");
				
				ПоказатьПротоколОнлайнПроверки(
					Контекст.Документ,
					"",
					"",
					"",
					"",
					0,
					ПерваяПрограммаПроверки,
					Истина,
					ВыполняемоеОповещение); // клиент
			КонецЕсли;
			
		ИначеЕсли КодРезультатаПроверки = 1 И КоличествоПротоколовДляУниверсальногоФормата = 0 Тогда
			ДанныеСертификата = Неопределено;
			Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Истина ИЛИ НЕ РезультатПроверки.Свойство("Сертификат", ДанныеСертификата) ИЛИ КоличествоПовторений > 1 Тогда

				ТекстПредупреждения = НСтр("ru = 'Не удалось проверить отчетность на сервере!'") + "
					|
					|" + СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ответ сервера: %1'"), РезультатПроверки.ПредставлениеРезультата);

				Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
				Иначе
					ПоказатьПредупреждение(ОписаниеОповещения, ТекстПредупреждения);
				КонецЕсли;
			Иначе
				КоличествоПовторений = КоличествоПовторений + 1;
				ДополнительныеПараметры.Вставить("КоличествоПовторений", 	КоличествоПовторений);
				ДополнительныеПараметры.Вставить("Контекст", 				Контекст);
				МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Контекст.УчетнаяЗапись);
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"ВыполнитьОнлайнПроверкуОтчета_ПослеРегистрацииСертификата", 
					ЭтотОбъект, 
					ДополнительныеПараметры);
				
				ЗарегистрироватьСертификатСервераОнлайнПроверки(
					МестоХраненияКлюча,
					ДанныеСертификата,
					ОписаниеОповещения);
				
			КонецЕсли;
			
		ИначеЕсли (КодРезультатаПроверки = 6 И Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина)
			ИЛИ (Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Истина И (ТипЗнч(Контекст.ИмяФайлаВыгрузки) = Тип("Массив") ИЛИ КоличествоПроверокДляУниверсальногоФормата > 1)) Тогда
			Если ЗначениеЗаполнено(РезультатПроверки.РезультатыПоДокументам) Тогда
				ДополнитьРезультатыГрупповойОнлайнПроверки(Контекст, РезультатПроверки);
				
				ПоказатьРезультатыГрупповойОнлайнПроверки(
					РезультатПроверки.РезультатыПоДокументам,
					Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате,
					ТипЗнч(Контекст.ИмяФайлаВыгрузки) <> Тип("Массив"),
					ВыполняемоеОповещение);
				
			Иначе
				ТекстПредупреждения = НСтр("ru = 'Отчетность не прошла проверку! К сожалению, детальные результаты недоступны.'");
				Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
				Иначе
					ПоказатьПредупреждение(ОписаниеОповещения, ТекстПредупреждения);
				КонецЕсли;
			КонецЕсли;
			
		ИначеЕсли (КодРезультатаПроверки <> 4 И Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина)
			ИЛИ (КодРезультатаПроверки <> 3 И КодРезультатаПроверки <> 4 И Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Истина И КоличествоПротоколовДляУниверсальногоФормата = 0) Тогда
			Если ЗначениеЗаполнено(РезультатПроверки.ПредставлениеРезультата) Тогда

				ТекстПредупреждения = НСтр("ru = 'Не удалось проверить отчетность на сервере!'") + "
					|
					|" + СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ответ сервера: %1'"), РезультатПроверки.ПредставлениеРезультата);
				Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
				Иначе
					ПоказатьПредупреждение(ОписаниеОповещения, ТекстПредупреждения);
				КонецЕсли;
			Иначе
				ТекстПредупреждения = НСтр("ru = 'Во время проверки файла выгрузки на сервере произошла неизвестная ошибка!'");
				Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
			Иначе
					ПоказатьПредупреждение(ОписаниеОповещения, ТекстПредупреждения);
				КонецЕсли;
			КонецЕсли;
			
		Иначе
			Если (РезультатПроверки.Свойство("Протокол") И ЗначениеЗаполнено(РезультатПроверки.Протокол)) ИЛИ КоличествоПротоколовДляУниверсальногоФормата > 0 Тогда
				ПоказатьПротоколОнлайнПроверки(Контекст.Документ, Контекст.ИмяФайлаВыгрузки, Контекст.АдресФайлаВыгрузки, ?(РезультатПроверки.Свойство("Протокол"), РезультатПроверки.Протокол, ""), ?(РезультатПроверки.Свойство("ИмяФайлаПротокола"), РезультатПроверки.ИмяФайлаПротокола, ""),
					?(Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Истина, КодРезультатаПроверки, Неопределено),
					?(РезультатПроверки.Свойство("ПрограммаПроверки"), РезультатПроверки.ПрограммаПроверки, ""), Истина, ВыполняемоеОповещение); // клиент
			Иначе
				ТекстПредупреждения = НСтр("ru = 'Отчет не прошел проверку! К сожалению, расширенный протокол недоступен.'");
				Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
					ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
				Иначе
					ПоказатьПредупреждение(ОписаниеОповещения, ТекстПредупреждения);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОнлайнПроверкуОтчета_ПослеРегистрацииСертификата(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	Если Результат.Выполнено Тогда
		
		КоличествоПовторений 	= ВходящийКонтекст.КоличествоПовторений;
		Контекст 				= ВходящийКонтекст.Контекст;
		
		ВыполнитьОнлайнПроверкуОтчета(ВыполняемоеОповещение, Контекст, КоличествоПовторений);
	Иначе
		ПоказатьПредупреждение(ВыполняемоеОповещение, НСтр("ru = 'Не удалось зарегистрировать новый сертификат сервера онлайн-проверки!
			|Продолжение невозможно.'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОнлайнПроверкуОтчетаЗавершение(ВыполняемоеОповещение) Экспорт
	
	// Используем переходник, так как в ОписанииОповещения у ПоказатьПредупреждение 1 параметр, 
	// а в переданном ВыполняемоеОповещение два параметра.
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки(ВходящийКонтекст);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки(ДополнительныеПараметры) Экспорт
	
	Если ДополнительныеПараметры.Свойство("СсылкаНаОтчет") Тогда
		СсылкаНаОтчет = ДополнительныеПараметры.СсылкаНаОтчет;
	Иначе
		СсылкаНаОтчет = ДополнительныеПараметры.Ссылка;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(СсылкаНаОтчет, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПослеНастройкиЗаявления(Результат, ДополнительныеПараметры) Экспорт
	
	Если ДополнительныеПараметры.Свойство("СсылкаНаОтчет") Тогда
		СсылкаНаОтчет = ДополнительныеПараметры.СсылкаНаОтчет;
	Иначе
		СсылкаНаОтчет = ДополнительныеПараметры.Ссылка;
	КонецЕсли;
		
	КонтролирующийОрган 			= ДополнительныеПараметры.КонтролирующийОрган;
	Форма 							= ДополнительныеПараметры.Форма;
	СледуетОбновлятьПанельОтправки 	= ДополнительныеПараметры.СледуетОбновлятьПанельОтправки;	
	
	Контекст = Новый Структура;
	Контекст.Вставить("Документ", 				 СсылкаНаОтчет);
	Контекст.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	Контекст.Вставить("КонтролирующийОрган", 	 КонтролирующийОрган);
	Контекст.Вставить("СледуетОбновитьПанельОтправки", Ложь);
	
	Если ДополнительныеПараметры.Свойство("АдресГотовыхДанных") Тогда 
		АдресГотовыхДанных 	  = ДополнительныеПараметры.АдресГотовыхДанных;
		ИмяФайлаГотовыхДанных = ДополнительныеПараметры.ИмяФайлаГотовыхДанных;
	Иначе
		АдресГотовыхДанных 		= Неопределено;
		ИмяФайлаГотовыхДанных 	= Неопределено;
	КонецЕсли;
	
	Контекст.Вставить("АдресГотовыхДанных", 	АдресГотовыхДанных);
	Контекст.Вставить("ИмяФайлаГотовыхДанных", 	ИмяФайлаГотовыхДанных);
	
	// заполняем реквизиты контекста
	Если НЕ ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПодготовкаИПроверкаНаСервере(Контекст) Тогда

		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ТекстПредупреждения = НСтр("ru = 'Проверка в Интернете недоступна для этого документа'");
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
		КонецЕсли;

		Возврат;

	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Контекст.УчетнаяЗапись) Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ПослеПроверкиЗаявления", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		
		ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(СсылкаНаОтчет, ОписаниеОповещения); 
		
		Возврат;
		
	КонецЕсли;
	
	ЭтоГрупповаяПроверка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки();

	// получаем разрешение на отправку персонральных данных
	Если ЭтоГрупповаяПроверка Тогда

		ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеРазрешенияНаОтправку(
			Контекст, 
			Форма, 
			КонтролирующийОрган, 
			СледуетОбновлятьПанельОтправки);

	ИначеЕсли Контекст.ОтправкаПерсональныхДанныхРазрешена = Неопределено 
		ИЛИ НЕ Контекст.ОтправкаПерсональныхДанныхРазрешена Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Контекст", 						Контекст);
		ДополнительныеПараметры.Вставить("Форма", 							Форма);
		ДополнительныеПараметры.Вставить("КонтролирующийОрган", 			КонтролирующийОрган);
		ДополнительныеПараметры.Вставить("СледуетОбновлятьПанельОтправки", 	СледуетОбновлятьПанельОтправки);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредупреждениеОбОтправкеПерсональныхДанных",,,,,, ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца) ;
		
	Иначе
		ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеРазрешенияНаОтправку(Контекст, Форма, КонтролирующийОрган, СледуетОбновлятьПанельОтправки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиЗавершение(ОтправкаПерсональныхДанныхРазрешена, ДополнительныеПараметры) Экспорт
	
	Контекст 							= ДополнительныеПараметры.Контекст;
	Форма 								= ДополнительныеПараметры.Форма;
	КонтролирующийОрган 				= ДополнительныеПараметры.КонтролирующийОрган;
	СледуетОбновлятьПанельОтправки		= ДополнительныеПараметры.СледуетОбновлятьПанельОтправки;	
	
	Если ОтправкаПерсональныхДанныхРазрешена = КодВозвратаДиалога.ОК Тогда
		
		ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеРазрешенияНаОтправку(
			Контекст, 
			Форма, 
			КонтролирующийОрган, 
			СледуетОбновлятьПанельОтправки);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеРазрешенияНаОтправку(Контекст, Форма, КонтролирующийОрган, СледуетОбновлятьПанельОтправки)
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Форма", 							Форма);
	ДополнительныеПараметры.Вставить("КонтролирующийОрган", 			КонтролирующийОрган);
	ДополнительныеПараметры.Вставить("СледуетОбновлятьПанельОтправки", 	СледуетОбновлятьПанельОтправки);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеВыгрузкиОтчета",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	// выгружаем отчет
	ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_ВыгрузитьОтчет(ОписаниеОповещения, Контекст, Форма);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеВыгрузкиОтчета(Контекст, ДополнительныеПараметры) Экспорт
	
	Форма 							= ДополнительныеПараметры.Форма;
	КонтролирующийОрган 			= ДополнительныеПараметры.КонтролирующийОрган;
	СледуетОбновлятьПанельОтправки 	= ДополнительныеПараметры.СледуетОбновлятьПанельОтправки;
	
	Если Контекст = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЭтоНДССПриложениями = Ложь;
	Если Контекст.ЭтоНДС Тогда
		Если ТипЗнч(Контекст.Документ) = Тип("ДокументСсылка.РегламентированныйОтчет")
			И ТипЗнч(Контекст.Документы) = Тип("Массив") И Контекст.Документы.Количество() >= 1 Тогда
			
			ЭтоНДССПриложениями = Истина;
			
		ИначеЕсли ТипЗнч(Контекст.Документ) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов")
			И ТипЗнч(Контекст.ИмяФайлаВыгрузки) = Тип("Массив") Тогда
			
			Для каждого ИмяФайлаВыгрузки Из Контекст.ИмяФайлаВыгрузки Цикл
				Если СтрНайти(ИмяФайлаВыгрузки, "NO_NDS_") <> 1 Тогда
					ЭтоНДССПриложениями = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	ЭтоГрупповаяПроверка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки();
	
	ДополнительныеПараметры = Новый Структура(); 
	ДополнительныеПараметры.Вставить("Форма", 							Форма);
	ДополнительныеПараметры.Вставить("КонтролирующийОрган", 			КонтролирующийОрган);
	ДополнительныеПараметры.Вставить("СледуетОбновлятьПанельОтправки", 	СледуетОбновлятьПанельОтправки);
	ДополнительныеПараметры.Вставить("Контекст", 						Контекст);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеПредупрежденияНДС",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если ЭтоГрупповаяПроверка Тогда
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	ИначеЕсли ЭтоНДССПриложениями И НЕ Контекст.БольшеНеПоказыватьПредупреждениеПриложенияНДСНаПроверкуНеОтправляются Тогда
		ТекстПредупреждения = ?(ТипЗнч(Контекст.Документ) = Тип("ДокументСсылка.РегламентированныйОтчет"),
			НСтр("ru = 'Обратите внимание, информация по счетам-фактурам проверяется отдельно, при нажатии кнопки ""Заполнить"".'"),
			НСтр("ru = 'Обратите внимание, информация по счетам-фактурам на проверку не отправляется.'"));
		
		СтруктураПараметров = Новый Структура();
		СтруктураПараметров.Вставить("ТекстПредупреждения", 		ТекстПредупреждения);
		СтруктураПараметров.Вставить("ЗаголовокФормы", 				НСтр("ru = 'Предупреждение'"));
		СтруктураПараметров.Вставить("ОднаКнопка", 					Истина);
		
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредупреждениеСФлагомБольшеНеПоказывать",
			СтруктураПараметров,
			Форма,
			,
			,
			,
			ОписаниеОповещения,
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеПредупрежденияНДС(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 		= ДополнительныеПараметры.Форма;
	Контекст 	= ДополнительныеПараметры.Контекст;
	
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		СохранитьБольшеНеПоказыватьПредупреждениеНДС(Контекст.Документ, Результат.БольшеНеПоказывать);
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеРазрешенияНаОтправкуЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ПроверитьОтчетСИспользованиемСервисаОнлайнПроверки_Проверка(
		Контекст,
		Форма,
		ОписаниеОповещения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьБольшеНеПоказыватьПредупреждениеНДС(Отчет, БольшеНеПоказывать)
	
	КлючОбъектаПредупрежденияНДС = ?(ТипЗнч(Отчет) = Тип("ДокументСсылка.РегламентированныйОтчет"),
		"ПредупреждениеПриложенияНДСНаПроверкуНеОтправляются",
		"ПредупреждениеПриложенияНДСНаПроверкуНеОтправляютсяДляЭлектронныхПредставлений");
	КлючНастроекПредупрежденияНДС = "БольшеНеПоказывать";
	
	ОбщегоНазначения.ХранилищеОбщихНастроекСохранить(
		КлючОбъектаПредупрежденияНДС,
		КлючНастроекПредупрежденияНДС,
		БольшеНеПоказывать);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьОтчетСИспользованиемСервисаОнлайнПроверкиПослеРазрешенияНаОтправкуЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	КонтролирующийОрган = ДополнительныеПараметры.КонтролирующийОрган;
	СледуетОбновлятьПанельОтправки = ДополнительныеПараметры.СледуетОбновлятьПанельОтправки;
	Контекст = ДополнительныеПараметры.Контекст;
	// перерисовываем панель отправки, если необходимо
	Если СледуетОбновлятьПанельОтправки И Контекст.СледуетОбновитьПанельОтправки Тогда
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ОбновитьПанельСостоянияОтправкиВРегламентированномОтчете(Форма, КонтролирующийОрган);
	КонецЕсли;	
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ХранилищеПоПредставлению(Знач Хранилище)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ХранилищеПоПредставлению(Хранилище);
	
КонецФункции
	
&НаСервереБезКонтекста
Функция ЗарегистрироватьСертификатВСистемеНаСервере(АдресДанныхСертификата, Хранилище, ОтпечатокСертификата = Неопределено)
	
	ЛогическоеХранилище = ХранилищеПоПредставлению(Хранилище);
	ДвоичныеДанныеСертификата = ПолучитьИзВременногоХранилища(АдресДанныхСертификата);
	
	ХранилищеСертификатов.Добавить(ДвоичныеДанныеСертификата, ЛогическоеХранилище);
	СвойстваСертификата = СервисКриптографии.ПолучитьСвойстваСертификата(ДвоичныеДанныеСертификата);
	
	ОтпечатокСертификата = ?(СвойстваСертификата <> Неопределено, СвойстваСертификата.Отпечаток, Неопределено);
	Возврат (СвойстваСертификата <> Неопределено);
	
КонецФункции

&НаКлиенте
Функция ЗарегистрироватьСертификатСервераОнлайнПроверки(
		МестоХраненияКлюча,
		ИмяФайлаСертификата,
		ВыполняемоеОповещение,
		ОтпечатокСертификата = Неопределено)
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ИмяФайлаСертификата", 				ИмяФайлаСертификата);
	ДополнительныеПараметры.Вставить("ОтпечатокСертификата", 				ОтпечатокСертификата);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча);
	
	// регистрируем сертификат в хранилище AddressBook
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ЗарегистрироватьСертификатСервераОнлайнПроверкиПослеРегистрации", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		ОтпечатокСертификата = Неопределено;

		РезультатРегистрации = ЗарегистрироватьСертификатВСистемеНаСервере(ИмяФайлаСертификата, "AddressBook", ОтпечатокСертификата);
		
		СвойстваСертификата = Новый Структура("Отпечаток", ОтпечатокСертификата);
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("Выполнено", РезультатРегистрации);
		РезультатВыполнения.Вставить("СвойстваСертификата", СвойстваСертификата);
		
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВыполнения);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ОтпечатокСертификата = Неопределено;

		РезультатРегистрации = ЗарегистрироватьСертификатВХранилищеПолучателей(ИмяФайлаСертификата, "AddressBook", ОтпечатокСертификата);
		
		СвойстваСертификата = Новый Структура("Отпечаток", ОтпечатокСертификата);
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("Выполнено", РезультатРегистрации);
		РезультатВыполнения.Вставить("СвойстваСертификата", СвойстваСертификата);
		
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВыполнения);
		
	Иначе
		
		КриптографияЭДКОКлиент.ИмпортироватьСертификат(ОписаниеОповещения, ИмяФайлаСертификата, "AddressBook");
		
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ЗарегистрироватьСертификатСервераОнлайнПроверкиПослеРегистрации(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ВходящийКонтекст.Вставить("Отпечаток", Результат.СвойстваСертификата.Отпечаток);
		ИмяФайлаСертификата = ВходящийКонтекст.ИмяФайлаСертификата;
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗарегистрироватьСертификатСервераОнлайнПроверкиПолучениняДанныхСертификата", 
			ЭтотОбъект, 
			ВходящийКонтекст); 
	
		// запоминаем
		Если НЕ ЭтоЛокальнаяПодпись(ВходящийКонтекст) Тогда
			
			РезультатВыполнения = Новый Структура();
			РезультатВыполнения.Вставить("Выполнено", 		Истина);
			РезультатВыполнения.Вставить("СтрокаBase64", 	ИмяФайлаСертификата);
			
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВыполнения);
			
		Иначе
				
				ОперацииСФайламиЭДКОКлиент.ФайлВBase64(ОписаниеОповещения, ИмяФайлаСертификата);
				
		КонецЕсли;
		
	Иначе
		
		ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
		
		ТекстСообщения = НСтр("ru = 'Не удалось зарегистрировать сертификат сервера онлайн-проверки в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("Выполнено", Ложь);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатСервераОнлайнПроверкиПолучениняДанныхСертификата(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	Если Результат.Выполнено Тогда
		
		ОтпечатокСертификата 	= ВходящийКонтекст.Отпечаток;
		ДанныеСертификата 		= Результат.СтрокаBase64;
					
		Если ЗапомнитьСведенияОСертификатеОнлайнПроверкиВКонстантах(ОтпечатокСертификата, ДанныеСертификата) Тогда
			
			РезультатВыполнения = Новый Структура();
			РезультатВыполнения.Вставить("Выполнено", Истина);
			РезультатВыполнения.Вставить("Отпечаток", ОтпечатокСертификата);
		
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Иначе
			
			РезультатВыполнения = Новый Структура();
			РезультатВыполнения.Вставить("Выполнено", Ложь);
			
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
			
	КонецЕсли;
	
	Иначе
	
		ТекстСообщения = НСтр("ru = 'Не удалось зарегистрировать сертификат сервера онлайн-проверки в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("Выполнено", Ложь);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗапомнитьСведенияОСертификатеОнлайнПроверкиВКонстантах(ОтпечатокСертификата, ДанныеСертификата)
	
	ТипЗнчДанныеСертификата = ТипЗнч(ДанныеСертификата);
	Если ТипЗнчДанныеСертификата = Тип("Строка") Тогда
		Если ЭтоАдресВременногоХранилища(ДанныеСертификата) Тогда
			ДанныеСертификата = ПолучитьИзВременногоХранилища(ДанныеСертификата);
		Иначе // строка Base64
			ДанныеСертификата = ДвоичныеДанныеИзСтроки64НаСервере(ДанныеСертификата);
		КонецЕсли;
		ДанныеХранилище = Новый ХранилищеЗначения(ДанныеСертификата);
	ИначеЕсли ТипЗнчДанныеСертификата = Тип("ДвоичныеДанные") Тогда
		ДанныеХранилище = Новый ХранилищеЗначения(ДанныеСертификата);
	Иначе
		ДанныеХранилище = ДанныеСертификата;
	КонецЕсли;
	
	КонстантыНабор = Константы.СоздатьНабор("ДокументооборотСКонтролирующимиОрганами_ОтпечатокСертификатаОнлайнПроверки, ДокументооборотСКонтролирующимиОрганами_СертификатСервераОнлайнПроверки");
	КонстантыНабор.ДокументооборотСКонтролирующимиОрганами_ОтпечатокСертификатаОнлайнПроверки = ОтпечатокСертификата;
	КонстантыНабор.ДокументооборотСКонтролирующимиОрганами_СертификатСервераОнлайнПроверки = ДанныеХранилище;
	Попытка
		КонстантыНабор.Записать();
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось зарегистрировать сертификат сервера онлайн-проверки в информационной базе:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИзИБСертификатСервераОнлайнПроверки() Экспорт
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	Константы.ДокументооборотСКонтролирующимиОрганами_ОтпечатокСертификатаОнлайнПроверки КАК Отпечаток,
						  |	Константы.ДокументооборотСКонтролирующимиОрганами_СертификатСервераОнлайнПроверки КАК Сертификат
						  |ИЗ
						  |	Константы КАК Константы");
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Отпечаток = Выборка.Отпечаток;
		Сертификат = Выборка.Сертификат;
		Если ЗначениеЗаполнено(Отпечаток) И ЗначениеЗаполнено(Сертификат) Тогда
			Возврат Новый Структура("Отпечаток, Сертификат", Отпечаток, Сертификат);
		Иначе
			Возврат Неопределено;
		КонецЕсли;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьЗапросДляОбработкиНаСерверОнлайнПроверки(ИмяФайлаЗапроса, ПолучитьСертификат = Ложь)
	
	АдресСервераОнлайнПроверки = "https://onlinefc.taxcom.ru";
	РесурсНаСервере = "/online_fc/online_fc.dll" + ?(ПолучитьСертификат, "?IncludeCertificate", "");
	
	// устанавливаем соединение с сервером
	ОписаниеОшибки = "";
	Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(АдресСервераОнлайнПроверки, ОписаниеОшибки);
	Если Соединение = Неопределено Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Не удалось установить соединение с сервером онлайн-проверки:
                  |%1'"), ОписаниеОшибки);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// посылаем запрос
	ФайлРезультата = ПолучитьИмяВременногоФайла();
	Попытка
		Соединение.ОтправитьДляОбработки(ИмяФайлаЗапроса, РесурсНаСервере, ФайлРезультата);
		Соединение = Неопределено;
		ПовторятьСоединение = Ложь;
	Исключение
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Неопределено;
	КонецПопытки;
	
	Возврат ФайлРезультата;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьЗапросДляОбработкиНаСерверОнлайнПроверкиВУниверсальномФормате(ИмяФайлаЗапроса, ПараметрыРесурса, ПовторятьСоединение)
	
	Сервис = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбменССерверомСоздатьСервис( , , , ПараметрыРесурса, Ложь);
	Если Сервис = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	СтрокаBase64 = ПрочитатьBase64ИзФайла(ИмяФайлаЗапроса);
	
	Попытка
		XDTOРезультат = Сервис.Test(СтрокаBase64);
	Исключение
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
		ТекстОшибки = ДокументооборотСКОВызовСервера.ДобавитьТекстОшибкиСертификатаКА(ТекстОшибки);
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка в интернете'"),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
		Возврат Неопределено;
	КонецПопытки;
	ПовторятьСоединение = Ложь;
	
	ФайлРезультата = ПолучитьИмяВременногоФайла();
	Текст = Новый ЗаписьТекста(ФайлРезультата, "windows-1251");
	Текст.ЗаписатьСтроку(XDTOРезультат);
	Текст.Закрыть();
	
	Возврат ФайлРезультата;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИОтправитьЗапросДляПолученияСертификатаНаСерверОнлайнПроверки(Контекст)
	
	// формируем запрос
	ИмяФайлаЗапроса = СформироватьЗапросКСерверуОнлайнПроверки(Контекст.УчетнаяЗапись);
	Если ИмяФайлаЗапроса = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// отправляем запрос на сервер
	ИмяФайлаРезультата = ОтправитьЗапросДляОбработкиНаСерверОнлайнПроверки(ИмяФайлаЗапроса, Истина);
	Если ИмяФайлаРезультата = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ИмяФайлаРезультата,
		"ИмяФайлаРезультата",
		"СформироватьИОтправитьЗапросДляПолученияСертификатаНаСерверОнлайнПроверки");	
	
	// помещаем результат во временное хранилище
	//@skip-check 1c-fresh-audit
	АдресФайлаРезультатаПроверки = ПоместитьВоВременноеХранилище(
		Новый ДвоичныеДанные(ИмяФайлаРезультата), Контекст.УникальныйИдентификатор);
	Контекст.Вставить("АдресФайлаРезультатаПроверки", АдресФайлаРезультатаПроверки);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверки(Контекст, ВыполняемоеОповещение)
	
	УчетнаяЗаписьОрганизации = Контекст.УчетнаяЗапись;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(УчетнаяЗаписьОрганизации);
	
	// инициализируем "константы"
	АдресСервераОнлайнПроверки = "https://onlinefc.taxcom.ru";
	РесурсНаСервере = "/online_fc/online_fc.dll?IncludeCertificate";
	
	// 
	Если НЕ СформироватьИОтправитьЗапросДляПолученияСертификатаНаСерверОнлайнПроверки(Контекст) Тогда // сервер
		РезультатВыполнения = Новый Структура("СвойстваСертификатаПолучателя, Контекст", Неопределено, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	// разбираем полученный результат
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ВыполняемоеОповещение);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверкиПослеРазбораРезультатаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОнлайнПроверкаОтчета_РазобратьРезультатОтСервераОнлайнПроверки(Контекст, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверкиПослеРазбораРезультатаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение               = ДополнительныеПараметры.ВыполняемоеОповещение;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Контекст                            = Результат.Контекст;
	
	Если НЕ Результат.РезультатПроверки Тогда
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("СвойстваСертификатаПолучателя", Неопределено);
		РезультатВыполнения.Вставить("Контекст",                      Контекст);

		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
		
	КонецЕсли;
	
	РезультатОтСервера = Контекст.Результат;
	Если РезультатОтСервера = Неопределено Тогда
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("СвойстваСертификатаПолучателя", Неопределено);
		РезультатВыполнения.Вставить("Контекст",                      Контекст);

		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	ИначеЕсли РезультатОтСервера.КодРезультата <> 5 Тогда
		
		ПредставлениеРезультата = РезультатОтСервера.ПредставлениеРезультата;
		Если ПредставлениеРезультата <> Неопределено Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не удалось получить сертификат сервера онлайн-проверки: %1'"), ПредставлениеРезультата);
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось получить сертификат сервера онлайн-проверки.'");
		КонецЕсли;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("СвойстваСертификатаПолучателя", Неопределено);
		РезультатВыполнения.Вставить("Контекст",                      Контекст);

		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	ИначеЕсли НЕ РезультатОтСервера.Свойство("КодРезультата") ИЛИ НЕ РезультатОтСервера.Свойство("Сертификат") Тогда
		
		ТекстСообщения = НСтр("ru = 'Не удалось получить сертификат сервера онлайн-проверки: некорректный ответ сервера.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("СвойстваСертификатаПолучателя", Неопределено);
		РезультатВыполнения.Вставить("Контекст",                      Контекст);

		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
		
	КонецЕсли;
	
	// регистрируем в ИБ и системе
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ОтпечатокСертификата = Неопределено;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверкиПослеРегистрацииСертификата",
		ЭтотОбъект, 
		ДополнительныеПараметры); 
		
	ЗарегистрироватьСертификатСервераОнлайнПроверки(
		МестоХраненияКлюча,
		РезультатОтСервера.Сертификат,
		ОписаниеОповещения,
		ОтпечатокСертификата);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверкиПослеРегистрацииСертификата(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	Контекст = ВходящийКонтекст.Контекст;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка регистрации в системном хранилище сертификата сервера онлайн-проверки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("СвойстваСертификатаПолучателя, Контекст", Неопределено, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ОтпечатокСертификата = Результат.Отпечаток;
	
	// получаем свойства зарегистрированного в системе сертификата, проверяем сертификат получателя
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст, ОтпечатокСертификата", ВыполняемоеОповещение, Контекст, ОтпечатокСертификата);
	Оповещение = Новый ОписаниеОповещения("ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверкиПослеПоискаСертификата", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СвойстваСертификатаПолучателя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечатокСертификата, "AddressBook");
		Иначе
			СвойстваСертификатаПолучателя = ПолучитьСвойстваОблачногоСертификата(ОтпечатокСертификата, "AddressBook", Истина);
		КонецЕсли;
		Если СвойстваСертификатаПолучателя <> Неопределено Тогда
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификатаПолучателя);
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				РезультатПроверки = ПроверитьСертификатНаСервере(СвойстваСертификатаПолучателя, МестоХраненияКлюча);
			Иначе
				РезультатПроверки = СвойстваСертификатаПолучателя.Валиден;
			КонецЕсли;
			СвойстваСертификатаПолучателя.Вставить("Валиден", РезультатПроверки = Истина);
		КонецЕсли;
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата", Истина, Неопределено, СвойстваСертификатаПолучателя <> Неопределено, СвойстваСертификатаПолучателя);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, ОтпечатокСертификата, "AddressBook", Истина, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверкиПослеПоискаСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	ОтпечатокСертификата = ДополнительныеПараметры.ОтпечатокСертификата;
	
	Если НЕ Результат.Выполнено ИЛИ НЕ Результат.СертификатНайден Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка регистрации в системном хранилище сертификата сервера онлайн-проверки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("СвойстваСертификатаПолучателя, Контекст", Неопределено, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
		
	ИначеЕсли НЕ Результат.СвойстваСертификата.Валиден Тогда
		РезультатВыполнения = Новый Структура("СвойстваСертификатаПолучателя, Контекст", Неопределено, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	СвойстваСертификатаПолучателя = Результат.СвойстваСертификата;
	
	Контекст.Вставить("СертификатСервераОнлайнПроверки", ОтпечатокСертификата);
	Контекст.Вставить("СвойстваСертификатаСервераОнлайнПроверки", СвойстваСертификатаПолучателя);
	
	РезультатВыполнения = Новый Структура("СвойстваСертификатаПолучателя, Контекст", СвойстваСертификатаПолучателя, Контекст);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОнлайнПроверкаОтчета_ПодготовкаНаСервере(Контекст)
	
	// различные проверки свойств учетной записи
	УчетнаяЗаписьОрганизации = Контекст.УчетнаяЗапись;
	Если УчетнаяЗаписьОрганизации.ОбменНапрямую Тогда
		ТекстСообщения = НСтр("ru = 'В настройках учетной записи отключен признак обмена через оператора электронного документооборота!
									|Возможность проверки в Интернете доступна только абонентам операторов электронного документооборота ""Калуга Астрал"" и ""Такском"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ УчетнаяЗаписьОрганизации.ИспользоватьСервисОнлайнПроверкиОтчетов Тогда
		ТекстСообщения = НСтр("ru = 'В свойствах учетной записи организации отключена возможность использования сервиса онлайн-проверки отчетности!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// получаем сертификат отправителя
	Сертификат = Мультирежим.СертификатПользователя(УчетнаяЗаписьОрганизации, "Отпечаток");
	Если НЕ ЗначениеЗаполнено(Сертификат.Отпечаток) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Сертификат.ТекстОшибки);
		Возврат Ложь;
	КонецЕсли;
	
	// получаем сертификат сервера онлайн-проверки
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате = Истина Тогда
		Если НЕ ЗначениеЗаполнено(Контекст.ИнформацияОСервереОнлайнПроверки.Отпечаток) Тогда
			ТекстСообщения = НСтр("ru = 'Сертификат сервера онлайн-проверки не получен.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		ИначеЕсли Контекст.ИнформацияОСервереОнлайнПроверки.ТипРеализации <> "SOAP" ИЛИ Контекст.ИнформацияОСервереОнлайнПроверки.Ресурс = Неопределено ИЛИ НЕ ЗначениеЗаполнено(Контекст.ИнформацияОСервереОнлайнПроверки.Ресурс.Определение) Тогда
			ТекстСообщения = НСтр("ru = 'Адрес сервера онлайн-проверки не получен.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		СертификатСервераОнлайнПроверки = Новый Структура("Отпечаток, Сертификат", Контекст.ИнформацияОСервереОнлайнПроверки.Отпечаток, "");
		
	Иначе
		СертификатСервераОнлайнПроверки = ПолучитьИзИБСертификатСервераОнлайнПроверки();
	КонецЕсли;
	
	// создаем временный каталог, куда поместим файл выгрузки
	ВременныйКаталог = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	Если ТипЗнч(Контекст.ИмяФайлаВыгрузки) = Тип("Строка") Тогда
		
		РегламентированнаяОтчетностьКлиентСервер.ПроверитьИмяФайла(Контекст.ИмяФайлаВыгрузки);
		ИмяВременногоФайлаВыгрузки = ВременныйКаталог + Контекст.ИмяФайлаВыгрузки;
		
		// выгружаем файл выгрузки во временный каталог
		ДвоичныеДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(Контекст.АдресФайлаВыгрузки);
		
		Если ДвоичныеДанныеФайлаВыгрузки = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось получить данные отчета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		Иначе
		ДвоичныеДанныеФайлаВыгрузки.Записать(ИмяВременногоФайлаВыгрузки);
		КонецЕсли;
		
	Иначе
		
		ИменаВременныхФайловВыгрузки = Новый Массив;
		Инд = 0;
		Пока Инд < Контекст.ИмяФайлаВыгрузки.Количество() Цикл
			
			ИмяФайлаВыгрузки = Контекст.ИмяФайлаВыгрузки[Инд];
			
			Если ТипЗнч(Контекст.Документ) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов")
				И Контекст.Документ.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НДС
				И СтрНайти(ИмяФайлаВыгрузки, "NO_NDS_") <> 1 Тогда
				
				Контекст.ИмяФайлаВыгрузки.Удалить(Инд);
				
				Если ТипЗнч(Контекст.АдресФайлаВыгрузки) = Тип("Массив") Тогда
					УдалитьИзВременногоХранилища(Контекст.АдресФайлаВыгрузки[Инд]);
					Контекст.АдресФайлаВыгрузки.Удалить(Инд);
				КонецЕсли;
				
				Если ТипЗнч(Контекст.Документы) = Тип("Массив") Тогда
					ДанныеДокумента = Контекст.Документы[Инд];
					Если ТипЗнч(ДанныеДокумента) = Тип("Структура")
						И ЭтоАдресВременногоХранилища(ДанныеДокумента.АдресФайлаВыгрузки) Тогда
						
						УдалитьИзВременногоХранилища(ДанныеДокумента.АдресФайлаВыгрузки);
					КонецЕсли;
					Контекст.Документы.Удалить(Инд);
				КонецЕсли;
				
				Продолжить;
			КонецЕсли;
			
			РегламентированнаяОтчетностьКлиентСервер.ПроверитьИмяФайла(ИмяФайлаВыгрузки);
			ИмяВременногоФайлаВыгрузки = ВременныйКаталог + ИмяФайлаВыгрузки;
			
			// выгружаем файл выгрузки во временный каталог
			Если ТипЗнч(Контекст.АдресФайлаВыгрузки) = Тип("Массив") Тогда
				АдресФайлаВыгрузки = Контекст.АдресФайлаВыгрузки[Инд];
				ДвоичныеДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(АдресФайлаВыгрузки);
				ДвоичныеДанныеФайлаВыгрузки.Записать(ИмяВременногоФайлаВыгрузки);
			ИначеЕсли ТипЗнч(Контекст.Документы) = Тип("Массив") Тогда
				
				ДанныеДокумента = Контекст.Документы[Инд];
				Если ТипЗнч(ДанныеДокумента) = Тип("Структура") Тогда
					АдресФайлаВыгрузки = ДанныеДокумента.АдресФайлаВыгрузки;
					
					Если ЭтоАдресВременногоХранилища(АдресФайлаВыгрузки) Тогда
						ДвДанные = ПолучитьИзВременногоХранилища(АдресФайлаВыгрузки);
						ДвДанные.Записать(ИмяВременногоФайлаВыгрузки);
					Иначе
						ТекстСообщения = НСтр("ru = 'Файл выгрузки не найден.'");
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
						Возврат Ложь;
					КонецЕсли;
					
				Иначе
					ТекстВыгрузки = Контекст.Документы[Инд];
					
					//@skip-check 1c-fresh-audit
					ОбъектЗаписьТекста = Новый ЗаписьТекста(
						ИмяВременногоФайлаВыгрузки, Контекст.КодировкаФайла);
					ОбъектЗаписьТекста.Записать(ТекстВыгрузки);
					ОбъектЗаписьТекста.Закрыть();
				КонецЕсли;
				
			КонецЕсли;
			
			ИменаВременныхФайловВыгрузки.Добавить(ИмяВременногоФайлаВыгрузки);
			Инд = Инд + 1;
			
		КонецЦикла;
		
		ИмяВременногоФайлаВыгрузки = ИменаВременныхФайловВыгрузки;
		
	КонецЕсли;
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		
		// архивируем файл выгрузки
		ИмяФайлаАрхива = ПолучитьИмяВременногоФайла();
		РезультатАрхивирования = ЗаархивироватьФайл(ИмяВременногоФайлаВыгрузки, ИмяФайлаАрхива);
		МассивОписанийИДанных = ОбщегоНазначенияЭДКО.МассивОписанийИДанныхZipФайла(ИмяФайлаАрхива);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаАрхива);
		ИмяФайлаАрхива = ПолучитьИмяВременногоФайла();
		ОбщегоНазначенияЭДКО.СформироватьZip(МассивОписанийИДанных, ИмяФайлаАрхива, "cp866");
		
		Если РезультатАрхивирования Тогда
			// сохраняем во временное хранилище
			АдресАрхиваФайлаВыгрузки = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ИмяФайлаАрхива), Контекст.УникальныйИдентификатор);
		КонецЕсли;
		
	Иначе
		
		Если ТипЗнч(ИмяВременногоФайлаВыгрузки) = Тип("Строка") Тогда
			ИменаВременныхФайловВыгрузки = Новый Массив;
			ИменаВременныхФайловВыгрузки.Добавить(ИмяВременногоФайлаВыгрузки);
		Иначе
			ИменаВременныхФайловВыгрузки = ИмяВременногоФайлаВыгрузки;
		КонецЕсли;
		
		РезультатАрхивирования = Истина;
		АдресаАрхивовФайловВыгрузки = Новый Массив;
		
		Для Каждого ИмяВременногоФайлаВыгрузки Из ИменаВременныхФайловВыгрузки Цикл
			// архивируем файл выгрузки
			ИмяФайлаАрхива = ПолучитьИмяВременногоФайла();
			РезультатАрхивирования = ЗаархивироватьФайл(ИмяВременногоФайлаВыгрузки, ИмяФайлаАрхива);
			Если НЕ РезультатАрхивирования Тогда
				Прервать;
			КонецЕсли;
			
			// сохраняем во временное хранилище
			АдресАрхиваФайлаВыгрузки = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ИмяФайлаАрхива), Контекст.УникальныйИдентификатор);
			АдресаАрхивовФайловВыгрузки.Добавить(АдресАрхиваФайлаВыгрузки);
		КонецЦикла;
		
		АдресАрхиваФайлаВыгрузки = АдресаАрхивовФайловВыгрузки;
		
	КонецЕсли;
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
	Если НЕ РезультатАрхивирования Тогда
		Возврат Ложь;
	КонецЕсли;
	
	//
	Контекст.Вставить("СертификатОтправителя", Сертификат.Отпечаток);
	Контекст.Вставить("СертификатСервераОнлайнПроверки", СертификатСервераОнлайнПроверки);
	Контекст.Вставить("АдресАрхиваФайлаВыгрузки", АдресАрхиваФайлаВыгрузки);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрование(Контекст, ВыполняемоеОповещение)
	
	УчетнаяЗапись = Контекст.УчетнаяЗапись;
	СертификатОтправителя = Контекст.СертификатОтправителя;
	СертификатПолучателя = Контекст.СертификатСервераОнлайнПроверки;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	///////////////////////////////////////////////////////////////////////////////
	// РАБОТА С СЕРТИФИКАТОМ ОТПРАВИТЕЛЯ
	
	// получаем свойства сертификата
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст, УчетнаяЗапись, СертификатПолучателя, ИдентификаторДокументооборота", ВыполняемоеОповещение, Контекст, УчетнаяЗапись, СертификатПолучателя, ИдентификаторДокументооборота);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПоискаСертификатаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	НовыйСертификат = Новый Структура("Отпечаток", СертификатОтправителя);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, НовыйСертификат);
	КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, НовыйСертификат, Истина, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПоискаСертификатаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	
	Если НЕ Результат.Выполнено ИЛИ НЕ Результат.СертификатНайден Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось получить свойства сертификата учетной записи %1 для шифрования.
						|Вероятно, сертификат не зарегистрирован в системном хранилище.'"), Символ(34) + УчетнаяЗапись + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	ИначеЕсли НЕ Результат.СвойстваСертификата.Валиден Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Сертификат для шифрования учетной записи %1 не прошел проверку валидности.'"),
			Символ(34) + УчетнаяЗапись + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	СвойстваСертификатаОтправителя = Результат.СвойстваСертификата;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификатаОтправителя);
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст, УчетнаяЗапись, СертификатПолучателя, ИдентификаторДокументооборота", ВыполняемоеОповещение, Контекст, УчетнаяЗапись, СертификатПолучателя, ИдентификаторДокументооборота);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеЭкспортаСертификатаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, СвойстваСертификатаОтправителя, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеЭкспортаСертификатаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось экспортировать в файл сертификат отправителя для шифрования!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	ДанныеСертификатаОтправителя = ?(Результат.Выполнено, Результат.СтрокаBase64, Неопределено);
	
	///////////////////////////////////////////////////////////////////////////////
	// РАБОТА С СЕРТИФИКАТОМ ПОЛУЧАТЕЛЯ
	
	Если НЕ ЗначениеЗаполнено(СертификатПолучателя) Тогда
		// определяем сертификат получателя
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
		ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
		Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияИзИнтернетаСертификатаСервераОнлайнПроверкиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ПолучитьИзИнтернетЗарегистрироватьИПроверитьСертификатСервераОнлайнПроверки(Контекст, Оповещение);
		
	Иначе
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатПолучателя);
		
		// получаем свойства сертификата
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Контекст", Контекст);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
		ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
		ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения(
			"ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияСвойствСертификатаПолучателяЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, СертификатПолучателя,, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияСвойствСертификатаПолучателяЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	СвойстваСертификатаПолучателя = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если СвойстваСертификатаПолучателя <> Неопределено Тогда
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Контекст", Контекст);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
		ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
		ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
		ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

		Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияСвойствСертификатаПолучателяИИмпортаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СертификатПолучателя.Сертификат) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка получения свойств сертификата сервера онлайн-проверки!
									|Вероятно, сертификат не зарегистрирован в системе.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	СертификатBase64 = ХранилищеЗначенияВВидеСтроки(СертификатПолучателя.Сертификат);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеЗаписиФайлаСертификатаПолучателяЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда	
		ИмяФайлаСертификата = ПоместитьВоВременноеХранилищеДанныеСКлиента(СертификатBase64, Контекст.УникальныйИдентификатор);
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ИмяФайлаСертификата);
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
	Иначе
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СертификатBase64, , Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеЗаписиФайлаСертификатаПолучателяЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	СвойстваСертификатаПолучателя = ДополнительныеПараметры.СвойстваСертификатаПолучателя;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
	КонецЕсли;
	
	ИмяФайлаСертификата = ?(Результат.Выполнено, Результат.ИмяФайла, Неопределено);

	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
	ДополнительныеПараметры.Вставить("ИмяФайлаСертификата", ИмяФайлаСертификата);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеИмпортаСертификатаПолучателяЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		РегистрацияСертификатаУспешна = ЗарегистрироватьСертификатВСистемеНаСервере(ИмяФайлаСертификата, "AddressBook", СертификатПолучателя.Отпечаток) И ЗначениеЗаполнено(СертификатПолучателя.Отпечаток);
		Если РегистрацияСертификатаУспешна Тогда
			СвойстваСертификатаПолучателя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СертификатПолучателя.Отпечаток, "AddressBook");
		КонецЕсли;
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СвойстваСертификата", РегистрацияСертификатаУспешна И ЗначениеЗаполнено(СвойстваСертификатаПолучателя), Неопределено, СвойстваСертификатаПолучателя);
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		РегистрацияСертификатаУспешна = ЗарегистрироватьСертификатВХранилищеПолучателей(ИмяФайлаСертификата, "AddressBook", СертификатПолучателя.Отпечаток) И ЗначениеЗаполнено(СертификатПолучателя.Отпечаток);
		Если РегистрацияСертификатаУспешна Тогда
			СвойстваСертификатаПолучателя = ПолучитьСвойстваОблачногоСертификата(СертификатПолучателя.Отпечаток, "AddressBook");
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификатаПолучателя);
		КонецЕсли;
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СвойстваСертификата", РегистрацияСертификатаУспешна И ЗначениеЗаполнено(СвойстваСертификатаПолучателя), Неопределено, СвойстваСертификатаПолучателя);
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	Иначе
		КриптографияЭДКОКлиент.ИмпортироватьСертификат(Оповещение, ИмяФайлаСертификата, "AddressBook", Ложь);
	КонецЕсли
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеИмпортаСертификатаПолучателяЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	СвойстваСертификатаПолучателя = ДополнительныеПараметры.СвойстваСертификатаПолучателя;
	ИмяФайлаСертификата = ДополнительныеПараметры.ИмяФайлаСертификата;
	
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось зарегистрировать сертификат сервера онлайн-проверки в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	СертификатПолучателя.Отпечаток = Результат.СвойстваСертификата.Отпечаток;
	СвойстваСертификатаПолучателя = Результат.СвойстваСертификата;

	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ИмяФайлаСертификата);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияСвойствСертификатаПолучателяИИмпортаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияИзИнтернетаСертификатаСервераОнлайнПроверкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	
	СвойстваСертификатаПолучателя = Результат.СвойстваСертификатаПолучателя;
	Контекст = Результат.Контекст;
	
	Если НЕ ЗначениеЗаполнено(СвойстваСертификатаПолучателя) Тогда
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	СертификатПолучателя = Новый Структура("Отпечаток, Сертификат", СвойстваСертификатаПолучателя.Отпечаток, Неопределено);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияСвойствСертификатаПолучателяИИмпортаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеПолученияСвойствСертификатаПолучателяИИмпортаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	СвойстваСертификатаПолучателя = ДополнительныеПараметры.СвойстваСертификатаПолучателя;
	
	///////////////////////////////////////////////////////////////////////////////
	// ШИФРОВАНИЕ АРХИВА С ФАЙЛОМ ВЫГРУЗКИ
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		
		// формируем зашифрованный контейнер с файлом выгрузки
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификатаПолучателя);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Контекст", Контекст);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
		ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
		ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
		ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеЗашифрованияВыгрузкиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		СертификатыЗашифрования = Новый Массив;
		СертификатыЗашифрования.Добавить(СвойстваСертификатаПолучателя);
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(Оповещение, Контекст.АдресАрхиваФайлаВыгрузки, СертификатыЗашифрования, , Ложь);
		Возврат;
		
	КонецЕсли;
	
	// при использовании универсального формата
	АдресаЗашифрованныхФайлов = Новый Массив();
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
	ДополнительныеПараметры.Вставить("АдресаЗашифрованныхФайлов", АдресаЗашифрованныхФайлов);
	ДополнительныеПараметры.Вставить("ИндексАрхива", 0);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеЗашифрованиеВУниверсальномФорматеВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеПослеЗашифрованияВыгрузкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	СвойстваСертификатаПолучателя = ДополнительныеПараметры.СвойстваСертификатаПолучателя;
	
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать зашифрованный пакет для отправки на сервер онлайн-проверки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	АдресЗашифрованногоФайла = Результат.ЗашифрованныйФайл;
	
	Контекст.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	Контекст.Вставить("СертификатПолучателя", СертификатПолучателя.Отпечаток);
	Контекст.Вставить("АдресЗашифрованногоФайла", АдресЗашифрованногоФайла);
	
	РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеЗашифрованиеВУниверсальномФорматеВЦиклеЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	СертификатПолучателя = ДополнительныеПараметры.СертификатПолучателя;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	ДанныеСертификатаОтправителя = ДополнительныеПараметры.ДанныеСертификатаОтправителя;
	СвойстваСертификатаПолучателя = ДополнительныеПараметры.СвойстваСертификатаПолучателя;
	АдресаЗашифрованныхФайлов = ДополнительныеПараметры.АдресаЗашифрованныхФайлов;
	ИндексАрхива = ДополнительныеПараметры.ИндексАрхива;
	
	Если ИндексАрхива > 0 Тогда
		Если НЕ Результат.Выполнено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось сформировать зашифрованный пакет для отправки на сервер онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
			Возврат;
		КонецЕсли;
		
		АдресЗашифрованногоФайла = Результат.ЗашифрованныйФайл;
		АдресаЗашифрованныхФайлов.Добавить(АдресЗашифрованногоФайла);
	КонецЕсли;
	
	Если ИндексАрхива < Контекст.АдресАрхиваФайлаВыгрузки.Количество() Тогда
		АдресАрхива = Контекст.АдресАрхиваФайлаВыгрузки[ИндексАрхива];
		ИндексАрхива = ИндексАрхива + 1;
		
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификатаПолучателя);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Контекст", Контекст);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("СертификатПолучателя", СертификатПолучателя);
		ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
		ДополнительныеПараметры.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
		ДополнительныеПараметры.Вставить("СвойстваСертификатаПолучателя", СвойстваСертификатаПолучателя);
		ДополнительныеПараметры.Вставить("АдресаЗашифрованныхФайлов", АдресаЗашифрованныхФайлов);
		ДополнительныеПараметры.Вставить("ИндексАрхива", ИндексАрхива);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрованиеЗашифрованиеВУниверсальномФорматеВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		СертификатыЗашифрования = Новый Массив;
		СертификатыЗашифрования.Добавить(СвойстваСертификатаПолучателя);
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(Оповещение, АдресАрхива, СертификатыЗашифрования, , Ложь);
		Возврат;
	КонецЕсли;
	
	АдресЗашифрованногоФайла = АдресаЗашифрованныхФайлов;
	
	Контекст.Вставить("ДанныеСертификатаОтправителя", ДанныеСертификатаОтправителя);
	Контекст.Вставить("СертификатПолучателя", СертификатПолучателя.Отпечаток);
	Контекст.Вставить("АдресЗашифрованногоФайла", АдресЗашифрованногоФайла);
	
	РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОнлайнПроверкаОтчета_СформироватьИОтправитьЗапрос(Контекст, ПовторятьСоединение)
	
	// формируем запрос
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		ИмяФайлаЗапроса = СформироватьЗапросКСерверуОнлайнПроверки(Контекст.УчетнаяЗапись, Контекст.ДанныеСертификатаОтправителя, Контекст.АдресЗашифрованногоФайла);
		
	Иначе
		Если ТипЗнч(Контекст.ИмяФайлаВыгрузки) = Тип("Строка") Тогда
			ИменаФайловВыгрузки = Новый Массив();
			ИменаФайловВыгрузки.Добавить(Контекст.ИмяФайлаВыгрузки);
		Иначе
			ИменаФайловВыгрузки = Контекст.ИмяФайлаВыгрузки;
		КонецЕсли;
		ИмяФайлаЗапроса = СформироватьЗапросКСерверуОнлайнПроверкиВУниверсальномФормате(Контекст.УчетнаяЗапись, Контекст.Документ, Контекст.КонтролирующийОрган, Контекст.ДанныеСертификатаОтправителя, ИменаФайловВыгрузки, Контекст.АдресЗашифрованногоФайла);
	КонецЕсли;
	
	Если ИмяФайлаЗапроса = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		ФайлРезультатаПроверки = ОтправитьЗапросДляОбработкиНаСерверОнлайнПроверки(ИмяФайлаЗапроса);
		ПовторятьСоединение = Ложь;
	Иначе
		ФайлРезультатаПроверки = ОтправитьЗапросДляОбработкиНаСерверОнлайнПроверкиВУниверсальномФормате(ИмяФайлаЗапроса, Контекст.ИнформацияОСервереОнлайнПроверки.Ресурс, ПовторятьСоединение);
	КонецЕсли;
	Если ФайлРезультатаПроверки = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ФайлРезультатаПроверки, "ФайлРезультатаПроверки", "ОнлайнПроверкаОтчета_СформироватьИОтправитьЗапрос");	
		
	// анализируем результат
	//@skip-check 1c-fresh-audit
	ОбъектФайл = Новый Файл(ФайлРезультатаПроверки);
	Если НЕ ОбъектФайл.Существует() Тогда
		ТекстСообщения = НСтр("ru = 'На запрос о проверке отчета не получен ответ от сервера.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// помещаем во временное хранилище
	//@skip-check 1c-fresh-audit
	АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(
		Новый ДвоичныеДанные(ФайлРезультатаПроверки), Контекст.УникальныйИдентификатор);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРезультатаПроверки);
	
	// запоминаем в контексте адрес во временном хранилище
	Контекст.Вставить("АдресФайлаРезультатаПроверки", АдресВоВременномХранилище);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОнлайнПроверкаОтчета(Контекст, ВыполняемоеОповещение)
	
	// подготовительные действия и проверки на сервере
	Если НЕ ОнлайнПроверкаОтчета_ПодготовкаНаСервере(Контекст) Тогда // сервер
		Результат = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// устанавливаем и проверяем сертификаты сервера онлайн-проверки
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОнлайнПроверкаОтчетаПослеУстановкиИПроверкиСертификатов", ЭтотОбъект, ДополнительныеПараметры);
	ОнлайнПроверкаОтчета_УстановкаИПроверкаСертификатовИШифрование(Контекст, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчетаПослеУстановкиИПроверкиСертификатов(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = Результат.Контекст;
	Если Не Результат.РезультатПроверки Тогда
		
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии();
		Иначе
			РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("Контекст, ВыполняемоеОповещение", Контекст, ВыполняемоеОповещение);
	ОписаниеОповещения = Новый ОписаниеОповещения("ОнлайнПроверкаОтчетаВЦикле", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчетаВЦикле(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат <> Истина Тогда
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии();
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Контекст = ДополнительныеПараметры.Контекст;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	// формируем запрос и отправляем
	ПовторятьСоединение = Истина;
	Если НЕ ОнлайнПроверкаОтчета_СформироватьИОтправитьЗапрос(Контекст, ПовторятьСоединение) Тогда // сервер
		Если ПовторятьСоединение Тогда
			ОписаниеОповещения = Новый ОписаниеОповещения("ОнлайнПроверкаОтчетаВЦикле", ЭтотОбъект, ДополнительныеПараметры);
			
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
				НСтр("ru = 'Не удалось обратиться к серверу онлайн-проверки'"), 
				ОписаниеОповещения);
				
			Возврат;
			
		Иначе

			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии();
			КонецЕсли;

			Результат = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	// разбираем результат
	ОнлайнПроверкаОтчета_РазобратьРезультатОтСервераОнлайнПроверки(Контекст, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция РазобратьРезультатОтСервераОнлайнПроверки_НаСервере(Контекст)
	
	ИменаФайловЗашифрованныхКонтейнеров = ?(Контекст.Свойство("АдресЗашифрованногоФайла"),
		Контекст.АдресЗашифрованногоФайла, Неопределено);
	Если ТипЗнч(ИменаФайловЗашифрованныхКонтейнеров) = Тип("Массив") Тогда
		Для ИндексФайла = 0 По ИменаФайловЗашифрованныхКонтейнеров.Количество() - 1 Цикл
			ИмяФайлаЗашифрованногоКонтейнера = ИменаФайловЗашифрованныхКонтейнеров[ИндексФайла];
			Если ЭтоАдресВременногоХранилища(ИмяФайлаЗашифрованногоКонтейнера) Тогда
				УдалитьИзВременногоХранилища(ИмяФайлаЗашифрованногоКонтейнера);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	СтекЭлементов = Новый Массив;
	
	// загружаем файл XML в дерево
	ОписаниеОшибки = "";
	ДеревоПроверки = ЗагрузитьXMLВДеревоЗначений(Контекст.АдресФайлаРезультатаПроверки, , ОписаниеОшибки);
	Если ДеревоПроверки = Неопределено Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки:%1'"), Символы.ПС + ОписаниеОшибки);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если ДеревоПроверки.Строки.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		
		СоответствиеКодаВозвратаПредставлению = Новый Соответствие;
		СоответствиеКодаВозвратаПредставлению.Вставить(-1, НСтр("ru = 'Произошла неизвестная ошибка.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(0, НСтр("ru = 'Проверка прошла успешно.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(1, НСтр("ru = 'Не удалось расшифровать полученные данные.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(2, НСтр("ru = 'Проверка файлов данного типа не поддерживается.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(3, НСтр("ru = 'Проверяемый файл поврежден или не соответствует требуемому формату.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(4, НСтр("ru = 'Проверка не прошла.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(5, НСтр("ru = 'Проверка не была выполнена.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(6, НСтр("ru = 'Один или несколько документов не прошли проверку.'"));
		
		// позиционируемся на элементе верхнего уровня
		УзелReport = ДеревоПроверки.Строки.Найти("Report", "Имя");
		Если УзелReport = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		// позиционируемся на узле результата
		УзелResult = УзелReport.Строки.Найти("Result", "Имя");
		Если УзелResult = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		// ищем узлы Successed и ExtCode
		УзелSucceeded = УзелResult.Строки.Найти("Succeeded", "Имя");
		УзелExtCode = УзелResult.Строки.Найти("ExtCode", "Имя");
		Если УзелSucceeded = Неопределено ИЛИ УзелExtCode = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		ПризнакУспешнойПроверки = XMLЗначениеВнутр(Тип("Булево"), УзелSucceeded.Значение);
		Если ПризнакУспешнойПроверки = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки: некорректный признак успешного завершения проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		КодЗавершения = XMLЗначениеВнутр(Тип("Число"), УзелExtCode.Значение);
		Если КодЗавершения = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки: некорректный код завершения проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		Результат = Новый Структура;
		
		// загружаем из XML таблицу с детальной информацией о проверках каждого документа
		УзелDocuments = УзелReport.Строки.Найти("Documents", "Имя");
		Если УзелDocuments <> Неопределено Тогда
			
			ТаблицаРезультатовПоДокументам = Новый ТаблицаЗначений;
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ИмяФайлаОтчета");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ИсходноеИмяФайлаПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ИмяФайлаПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ТипПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("РезультатПроверки");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("РезультатПроверкиСтр");
			
			УзлыDocument = УзелDocuments.Строки.НайтиСтроки(Новый Структура("Имя", "Document"));
			Для Каждого УзелDocument Из УзлыDocument Цикл
				АтрибутыУзлаDocument = ПолучитьАтрибутыУзла(УзелDocument);
				
				НовСтрТаблицыРезультатовПоДокументам = ТаблицаРезультатовПоДокументам.Добавить();
				НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаОтчета = АтрибутыУзлаDocument.report;
				Если АтрибутыУзлаDocument.Свойство("protocol") Тогда
					НовСтрТаблицыРезультатовПоДокументам.ИсходноеИмяФайлаПротокола = АтрибутыУзлаDocument.protocol;
				КонецЕсли;
				Если АтрибутыУзлаDocument.Свойство("file") Тогда
					НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаПротокола = АтрибутыУзлаDocument.file;
				КонецЕсли;
				Если АтрибутыУзлаDocument.Свойство("type") Тогда
					НовСтрТаблицыРезультатовПоДокументам.ТипПротокола = АтрибутыУзлаDocument.type;
				КонецЕсли;
				
				УзелResult = УзелDocument.Строки.Найти("Result", "Имя");
				Если УзелResult <> Неопределено Тогда
					АтрибутыУзлаResult = ПолучитьАтрибутыУзла(УзелResult);
					
					НовСтрТаблицыРезультатовПоДокументам.РезультатПроверки = XMLЗначениеВнутр(Тип("Булево"), АтрибутыУзлаResult.Succeeded);
					Если АтрибутыУзлаResult.Свойство("ExtCode") Тогда
						
						КодРезультатаПроверки = XMLЗначениеВнутр(Тип("Число"), АтрибутыУзлаResult.ExtCode);
						НовСтрТаблицыРезультатовПоДокументам.РезультатПроверки = КодРезультатаПроверки;
						
						ПредставлениеРезультата = СоответствиеКодаВозвратаПредставлению[КодРезультатаПроверки];
						НовСтрТаблицыРезультатовПоДокументам.РезультатПроверкиСтр = ПредставлениеРезультата;
						
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЦикла;
			
			РезультатыПоДокументам = ПоместитьВоВременноеХранилище(ТаблицаРезультатовПоДокументам, Контекст.УникальныйИдентификатор);
			
			Результат.Вставить("РезультатыПоДокументам", РезультатыПоДокументам);
			
		КонецЕсли;
		
		// добавляем сертификат в результат, если он есть
		УзелCertificate = УзелReport.Строки.Найти("Certificate", "Имя");
		Если УзелCertificate <> Неопределено Тогда
			Результат.Вставить("Certificate", XMLЗначениеВнутр(Тип("Строка"), УзелCertificate.Значение));
		КонецЕсли;
		
		// добавляем протокол в результат, если он есть
		УзелProtocol = УзелReport.Строки.Найти("Protocol", "Имя");
		Если УзелProtocol <> Неопределено Тогда
			Результат.Вставить("Protocol", XMLЗначениеВнутр(Тип("Строка"), УзелProtocol.Значение));
		КонецЕсли;
		
		ПредставлениеРезультата = СоответствиеКодаВозвратаПредставлению[КодЗавершения];
		
	Иначе
		
		// разбор результатов в универсальном формате
		
		СоответствиеКодаВозвратаПредставлению = Новый Соответствие;
		СоответствиеКодаВозвратаПредставлению.Вставить(-1, НСтр("ru = 'Произошла неизвестная ошибка.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(0, НСтр("ru = 'Проверка прошла успешно.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(1, НСтр("ru = 'Не удалось расшифровать полученные данные.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(2, НСтр("ru = 'Проверяемый документ поврежден или не соответствует требуемому формату.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(3, НСтр("ru = 'Проверка не прошла.'"));
		СоответствиеКодаВозвратаПредставлению.Вставить(4, НСтр("ru = 'Проверка прошла успешно, но есть предупреждения.'"));
		
		// позиционируемся на элементе верхнего уровня
		УзелОтвет = ДеревоПроверки.Строки.Найти("Ответ", "Имя");
		Если УзелОтвет = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		ПредставлениеРезультата = УзелОтвет.Значение;
		
		// позиционируемся на узле результата
		УзелОбщийРезультатПроверки = УзелОтвет.Строки.Найти("ОбщийРезультатПроверки", "Имя");
		Если УзелОбщийРезультатПроверки = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		АтрибутыУзлаОбщийРезультатПроверки = ПолучитьАтрибутыУзла(УзелОбщийРезультатПроверки);
		
		// проверяем наличие атрибута кода
		Если НЕ АтрибутыУзлаОбщийРезультатПроверки.Свойство("Код") Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		
		КодЗавершения = XMLЗначениеВнутр(Тип("Число"), АтрибутыУзлаОбщийРезультатПроверки.Код);
		Если КодЗавершения = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки: некорректный код завершения проверки.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Неопределено;
		КонецЕсли;
		ПризнакУспешнойПроверки = (КодЗавершения = 0);
		Если НЕ ЗначениеЗаполнено(ПредставлениеРезультата) Тогда
			ПредставлениеРезультата = СоответствиеКодаВозвратаПредставлению[КодЗавершения];
		КонецЕсли;
		
		Результат = Новый Структура;
		
		// загружаем из XML таблицу с информацией о программах проверки
		ТаблицаПрограммПроверки = Неопределено;
		УзлыПрограммаПроверки = УзелОтвет.Строки.НайтиСтроки(Новый Структура("Имя", "ПрограммаПроверки"));
		Если УзлыПрограммаПроверки.Количество() > 0 Тогда
			
			ТаблицаПрограммПроверки = Новый ТаблицаЗначений;
			ТаблицаПрограммПроверки.Колонки.Добавить("КодПрограммыПроверки");
			ТаблицаПрограммПроверки.Колонки.Добавить("Наименование");
			ТаблицаПрограммПроверки.Колонки.Добавить("Версия");
			ТаблицаПрограммПроверки.Колонки.Добавить("ДатаВремяОбновления");
			ТаблицаПрограммПроверки.Колонки.Добавить("ПредставлениеНаименования");
			
			ПерваяПрограммаПроверки = "";
			Для Каждого УзелПрограммаПроверки Из УзлыПрограммаПроверки Цикл
				АтрибутыУзлаПрограммаПроверки = ПолучитьАтрибутыУзла(УзелПрограммаПроверки);
				
				НовСтрТаблицыПрограммПроверки = ТаблицаПрограммПроверки.Добавить();
				НовСтрТаблицыПрограммПроверки.КодПрограммыПроверки = АтрибутыУзлаПрограммаПроверки.Код;
				Если АтрибутыУзлаПрограммаПроверки.Свойство("Наименование") Тогда
					НовСтрТаблицыПрограммПроверки.Наименование = АтрибутыУзлаПрограммаПроверки.Наименование;
				КонецЕсли;
				Если АтрибутыУзлаПрограммаПроверки.Свойство("Версия") Тогда
					НовСтрТаблицыПрограммПроверки.Версия = АтрибутыУзлаПрограммаПроверки.Версия;
				КонецЕсли;
				Если АтрибутыУзлаПрограммаПроверки.Свойство("ДатаВремяОбновления") Тогда
					НовСтрТаблицыПрограммПроверки.ДатаВремяОбновления = XMLЗначениеВнутр(Тип("Дата"), АтрибутыУзлаПрограммаПроверки.ДатаВремяОбновления);
				КонецЕсли;
				НовСтрТаблицыПрограммПроверки.ПредставлениеНаименования = Строка(НовСтрТаблицыПрограммПроверки.Наименование)
					+ ?(ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.Наименование) И ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.Версия), " ", "")
					+ ?(ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.Версия), НСтр("ru = 'версия'") + " " + НовСтрТаблицыПрограммПроверки.Версия, "")
					+ ?((ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.Наименование) ИЛИ ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.Версия)) И ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.ДатаВремяОбновления), " ", "")
					+ ?(ЗначениеЗаполнено(НовСтрТаблицыПрограммПроверки.ДатаВремяОбновления), НСтр("ru = 'от'") + " " + Формат(НовСтрТаблицыПрограммПроверки.ДатаВремяОбновления, "ДЛФ=Д"), "");
				Если НЕ ЗначениеЗаполнено(ПерваяПрограммаПроверки) Тогда
					ПерваяПрограммаПроверки = НовСтрТаблицыПрограммПроверки.ПредставлениеНаименования;
				КонецЕсли;
			КонецЦикла;
			
			ПрограммыПроверки = ПоместитьВоВременноеХранилище(ТаблицаПрограммПроверки, Контекст.УникальныйИдентификатор);
			
			Результат.Вставить("ПрограммыПроверки", ПрограммыПроверки);
			Результат.Вставить("ПерваяПрограммаПроверки", ПерваяПрограммаПроверки);
			
		КонецЕсли;
		
		// загружаем из XML таблицу с детальной информацией о проверках каждого документа
		ПризнакиУпаковкиПротоколов = Новый Соответствие;
		УзлыДокумент = УзелОтвет.Строки.НайтиСтроки(Новый Структура("Имя", "Документ"));
		Если УзлыДокумент.Количество() > 0 Тогда
			
			ТаблицаРезультатовПоДокументам = Новый ТаблицаЗначений;
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ИмяФайлаОтчета");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ИсходноеИмяФайлаПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ИмяФайлаПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ТипПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("РезультатПроверки");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("РезультатПроверкиСтр");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("КодПрограммыПроверки");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ПрограммаПроверки");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("ПризнакУпаковкиПротокола");
			ТаблицаРезультатовПоДокументам.Колонки.Добавить("РезультатыПроверок");
			
			Для Каждого УзелДокумент Из УзлыДокумент Цикл
				АтрибутыУзлаДокумент = ПолучитьАтрибутыУзла(УзелДокумент);
				
				НовСтрТаблицыРезультатовПоДокументам = ТаблицаРезультатовПоДокументам.Добавить();
				НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаОтчета = АтрибутыУзлаДокумент.ИмяФайлаВыгрузки;
				РегламентированнаяОтчетностьКлиентСервер.ПроверитьИмяФайла(НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаОтчета);
				
				//@skip-check 1c-fresh-audit
				ФайлОтчета = Новый Файл(НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаОтчета);
				
				Если АтрибутыУзлаДокумент.Свойство("ИмяФайлаПротоколаПроверки") Тогда
					НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаПротокола = АтрибутыУзлаДокумент.ИмяФайлаПротоколаПроверки;
				КонецЕсли;
				
				Если АтрибутыУзлаДокумент.Свойство("ФорматПротоколаПроверки") Тогда
					НовСтрТаблицыРезультатовПоДокументам.ТипПротокола = АтрибутыУзлаДокумент.ФорматПротоколаПроверки;
					НовСтрТаблицыРезультатовПоДокументам.ИсходноеИмяФайлаПротокола = ФайлОтчета.ИмяБезРасширения + "-LOG." + НовСтрТаблицыРезультатовПоДокументам.ТипПротокола;
				КонецЕсли;
				
				Если АтрибутыУзлаДокумент.Свойство("ПризнакУпаковкиПротокола") Тогда
					НовСтрТаблицыРезультатовПоДокументам.ПризнакУпаковкиПротокола = АтрибутыУзлаДокумент.ПризнакУпаковкиПротокола;
				КонецЕсли;
				
				Если НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаПротокола <> Неопределено И НовСтрТаблицыРезультатовПоДокументам.ПризнакУпаковкиПротокола <> Неопределено Тогда
					ПризнакиУпаковкиПротоколов[нрег(СокрЛП(НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаПротокола))] = НовСтрТаблицыРезультатовПоДокументам.ПризнакУпаковкиПротокола;
				КонецЕсли;
				
				// ищем наименее удачный результат проверки
				ТаблицаРезультатовПроверок = Новый ТаблицаЗначений;
				ТаблицаРезультатовПроверок.Колонки.Добавить("ИсходноеИмяФайлаПротокола");
				ТаблицаРезультатовПроверок.Колонки.Добавить("ИмяФайлаПротокола");
				ТаблицаРезультатовПроверок.Колонки.Добавить("ТипПротокола");
				ТаблицаРезультатовПроверок.Колонки.Добавить("РезультатПроверки");
				ТаблицаРезультатовПроверок.Колонки.Добавить("РезультатПроверкиСтр");
				ТаблицаРезультатовПроверок.Колонки.Добавить("КодПрограммыПроверки");
				ТаблицаРезультатовПроверок.Колонки.Добавить("ПрограммаПроверки");
				ТаблицаРезультатовПроверок.Колонки.Добавить("ПризнакУпаковкиПротокола");
				
				УзлыРезультатПроверки = УзелДокумент.Строки.НайтиСтроки(Новый Структура("Имя", "РезультатПроверки"));
				
				ИсходноеИмяФайлаПротоколаНаименееУспешнойПроверки = Неопределено;
				ИмяФайлаПротоколаНаименееУспешнойПроверки = Неопределено;
				ФорматПротоколаНаименееУспешнойПроверки = Неопределено;
				РезультатНаименееУспешнойПроверки = Неопределено;
				РезультатНаименееУспешнойПроверкиСтр = Неопределено;
				КодПрограммыНаименееУспешнойПроверки = Неопределено;
				ПрограммаНаименееУспешнойПроверки = Неопределено;
				ПризнакУпаковкиПротоколаНаименееУспешнойПроверки = Неопределено;
				
				ИндексПротоколаПроверки = 0;
				НомерПротоколаПроверки = "";
				Для Каждого УзелРезультатПроверки Из УзлыРезультатПроверки Цикл
					АтрибутыУзлаРезультатПроверки = ПолучитьАтрибутыУзла(УзелРезультатПроверки);
					
					НовСтрТаблицыРезультатовПроверок = ТаблицаРезультатовПроверок.Добавить();
					
					Если АтрибутыУзлаРезультатПроверки.Свойство("ИмяФайлаПротоколаПроверки") Тогда
						НовСтрТаблицыРезультатовПроверок.ИмяФайлаПротокола = АтрибутыУзлаРезультатПроверки.ИмяФайлаПротоколаПроверки;
					КонецЕсли;
					Если НЕ ЗначениеЗаполнено(НовСтрТаблицыРезультатовПроверок.ИмяФайлаПротокола) Тогда
						НовСтрТаблицыРезультатовПроверок.ИмяФайлаПротокола = НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаПротокола;
					КонецЕсли;
					
					Если АтрибутыУзлаРезультатПроверки.Свойство("ФорматПротоколаПроверки") Тогда
						НовСтрТаблицыРезультатовПроверок.ТипПротокола = АтрибутыУзлаРезультатПроверки.ФорматПротоколаПроверки;
						НовСтрТаблицыРезультатовПроверок.ИсходноеИмяФайлаПротокола = ФайлОтчета.ИмяБезРасширения + "-LOG" + НомерПротоколаПроверки + "." + НовСтрТаблицыРезультатовПроверок.ТипПротокола;
					КонецЕсли;
					Если НЕ ЗначениеЗаполнено(НовСтрТаблицыРезультатовПроверок.ТипПротокола) Тогда
						НовСтрТаблицыРезультатовПроверок.ТипПротокола = НовСтрТаблицыРезультатовПоДокументам.ТипПротокола;
						НовСтрТаблицыРезультатовПроверок.ИсходноеИмяФайлаПротокола = НовСтрТаблицыРезультатовПоДокументам.ИсходноеИмяФайлаПротокола;
					КонецЕсли;
					
					НовСтрТаблицыРезультатовПроверок.РезультатПроверки = XMLЗначениеВнутр(Тип("Число"), АтрибутыУзлаРезультатПроверки.Код);
					
					НовСтрТаблицыРезультатовПроверок.РезультатПроверкиСтр = УзелРезультатПроверки.Значение;
					Если НЕ ЗначениеЗаполнено(НовСтрТаблицыРезультатовПроверок.РезультатПроверкиСтр) Тогда
						НовСтрТаблицыРезультатовПроверок.РезультатПроверкиСтр = СоответствиеКодаВозвратаПредставлению[НовСтрТаблицыРезультатовПроверок.РезультатПроверки];
					КонецЕсли;
					
					Если АтрибутыУзлаРезультатПроверки.Свойство("КодПрограммыПроверки") Тогда
						НовСтрТаблицыРезультатовПроверок.КодПрограммыПроверки = АтрибутыУзлаРезультатПроверки.КодПрограммыПроверки;
						Если ЗначениеЗаполнено(НовСтрТаблицыРезультатовПроверок.КодПрограммыПроверки) И ТаблицаПрограммПроверки <> Неопределено Тогда
							СтрТаблицыПрограммПроверки = ТаблицаПрограммПроверки.Найти(НовСтрТаблицыРезультатовПроверок.КодПрограммыПроверки, "КодПрограммыПроверки");
							Если СтрТаблицыПрограммПроверки <> Неопределено Тогда
								НовСтрТаблицыРезультатовПроверок.ПрограммаПроверки = СтрТаблицыПрограммПроверки.ПредставлениеНаименования
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
					
					Если АтрибутыУзлаРезультатПроверки.Свойство("ПризнакУпаковкиПротокола") Тогда
						НовСтрТаблицыРезультатовПроверок.ПризнакУпаковкиПротокола = АтрибутыУзлаРезультатПроверки.ПризнакУпаковкиПротокола;
					КонецЕсли;
					Если НЕ ЗначениеЗаполнено(НовСтрТаблицыРезультатовПроверок.ПризнакУпаковкиПротокола) Тогда
						НовСтрТаблицыРезультатовПроверок.ПризнакУпаковкиПротокола = НовСтрТаблицыРезультатовПоДокументам.ПризнакУпаковкиПротокола;
					КонецЕсли;
					
					Если НовСтрТаблицыРезультатовПроверок.ИмяФайлаПротокола <> Неопределено И НовСтрТаблицыРезультатовПроверок.ПризнакУпаковкиПротокола <> Неопределено Тогда
						ПризнакиУпаковкиПротоколов[нрег(СокрЛП(НовСтрТаблицыРезультатовПроверок.ИмяФайлаПротокола))] = НовСтрТаблицыРезультатовПроверок.ПризнакУпаковкиПротокола;
					КонецЕсли;
					
					Если РезультатНаименееУспешнойПроверки = Неопределено ИЛИ (НовСтрТаблицыРезультатовПроверок.РезультатПроверки <> 0
							И (РезультатНаименееУспешнойПроверки = 0 ИЛИ НовСтрТаблицыРезультатовПроверок.РезультатПроверки < РезультатНаименееУспешнойПроверки)) Тогда
						ИсходноеИмяФайлаПротоколаНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.ИсходноеИмяФайлаПротокола;
						ИмяФайлаПротоколаНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.ИмяФайлаПротокола;
						ФорматПротоколаНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.ТипПротокола;
						РезультатНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.РезультатПроверки;
						РезультатНаименееУспешнойПроверкиСтр = НовСтрТаблицыРезультатовПроверок.РезультатПроверкиСтр;
						КодПрограммыНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.КодПрограммыПроверки;
						ПрограммаНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.ПрограммаПроверки;
						ПризнакУпаковкиПротоколаНаименееУспешнойПроверки = НовСтрТаблицыРезультатовПроверок.ПризнакУпаковкиПротокола;
					КонецЕсли;
					
					ИндексПротоколаПроверки = ИндексПротоколаПроверки + 1;
					НомерПротоколаПроверки = Формат(ИндексПротоколаПроверки + 1, "ЧН=0; ЧГ=");
				КонецЦикла;
				
				НовСтрТаблицыРезультатовПоДокументам.РезультатыПроверок = ПоместитьВоВременноеХранилище(ТаблицаРезультатовПроверок, Контекст.УникальныйИдентификатор);
				
				Если ЗначениеЗаполнено(ИсходноеИмяФайлаПротоколаНаименееУспешнойПроверки) Тогда
					НовСтрТаблицыРезультатовПоДокументам.ИсходноеИмяФайлаПротокола = ИсходноеИмяФайлаПротоколаНаименееУспешнойПроверки;
				КонецЕсли;
				Если ЗначениеЗаполнено(ИмяФайлаПротоколаНаименееУспешнойПроверки) Тогда
					НовСтрТаблицыРезультатовПоДокументам.ИмяФайлаПротокола = ИмяФайлаПротоколаНаименееУспешнойПроверки;
				КонецЕсли;
				Если ЗначениеЗаполнено(ФорматПротоколаНаименееУспешнойПроверки) Тогда
					НовСтрТаблицыРезультатовПоДокументам.ТипПротокола = ФорматПротоколаНаименееУспешнойПроверки;
				КонецЕсли;
				Если ЗначениеЗаполнено(ПризнакУпаковкиПротоколаНаименееУспешнойПроверки) Тогда
					НовСтрТаблицыРезультатовПоДокументам.ПризнакУпаковкиПротокола = ПризнакУпаковкиПротоколаНаименееУспешнойПроверки;
				КонецЕсли;
				НовСтрТаблицыРезультатовПоДокументам.РезультатПроверки = РезультатНаименееУспешнойПроверки;
				НовСтрТаблицыРезультатовПоДокументам.РезультатПроверкиСтр = РезультатНаименееУспешнойПроверкиСтр;
				НовСтрТаблицыРезультатовПоДокументам.КодПрограммыПроверки = КодПрограммыНаименееУспешнойПроверки;
				НовСтрТаблицыРезультатовПоДокументам.ПрограммаПроверки = ПрограммаНаименееУспешнойПроверки;
				
			КонецЦикла;
			
			РезультатыПоДокументам = ПоместитьВоВременноеХранилище(ТаблицаРезультатовПоДокументам, Контекст.УникальныйИдентификатор);
			
			Результат.Вставить("РезультатыПоДокументам", РезультатыПоДокументам);
			
		КонецЕсли;
		
		// добавляем имеющиеся протоколы в результат
		УзлыПротокол = УзелОтвет.Строки.НайтиСтроки(Новый Структура("Имя", "Протокол"));
		
		КоличествоПротоколов = УзлыПротокол.Количество();
		Результат.Вставить("КоличествоПротоколов", КоличествоПротоколов);
		
		Для ИндексПротокола = 0 По КоличествоПротоколов - 1 Цикл
			УзелПротокол = УзлыПротокол[ИндексПротокола];
			НомерПротоколаСтр = Формат(ИндексПротокола + 1, "ЧН=0; ЧГ=");
			
			ПротоколBase64 = XMLЗначениеВнутр(Тип("Строка"), УзелПротокол.Значение);
			Результат.Вставить("Протокол" + НомерПротоколаСтр, ПротоколBase64);
			
			АтрибутыУзлаПротокол = ПолучитьАтрибутыУзла(УзелПротокол);
			ИмяФайлаПротокола = АтрибутыУзлаПротокол.ИмяФайлаПротоколаПроверки;
			Результат.Вставить("ИмяФайлаПротокола" + НомерПротоколаСтр, ИмяФайлаПротокола);
			
			Результат.Вставить("ПризнакУпаковкиПротокола" + НомерПротоколаСтр, ПризнакиУпаковкиПротоколов[нрег(СокрЛП(ИмяФайлаПротокола))]);
		КонецЦикла;
		
	КонецЕсли;
	
	// инициализируем структуру-результат
	Результат.Вставить("Результат", ПризнакУспешнойПроверки);
	Результат.Вставить("КодРезультата", КодЗавершения);
	Результат.Вставить("ПредставлениеРезультата", ПредставлениеРезультата);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоместитьВоВременноеХранилищеЕслиСуществует(ИмяФайла, Адрес, ПоместитьКакДвоичныеДанные = Ложь)
	
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ИмяФайла, "ИмяФайла", "ПоместитьВоВременноеХранилищеЕслиСуществует");	
		
	//@skip-check 1c-fresh-audit
	ОбъектФайл = Новый Файл(ИмяФайла);
	Если НЕ ОбъектФайл.Существует() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДанныеФайла = ИмяФайла;
	Если ПоместитьКакДвоичныеДанные Тогда
		//@skip-check 1c-fresh-audit
		ДанныеФайла = Новый ДвоичныеДанные(ИмяФайла);
	КонецЕсли;
	Возврат ПоместитьВоВременноеХранилище(ДанныеФайла, Адрес);
	
КонецФункции

&НаСервереБезКонтекста
Функция РазобратьРезультатОтСервераОнлайнПроверки_РаспаковатьРасшифрованныйРезультатНаСервере(Контекст)
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		
		// сохраняем протокол во временный файл
		ФайлАрхиваСПротоколом64 = Контекст.ФайлАрхиваСПротоколом64;
		ДвоичныеДанныеФайлаАрхиваСПротоколом = ДвоичныеДанныеИзСтроки64НаСервере(ФайлАрхиваСПротоколом64);
		ИмяФайлаАрхиваСПротоколом = ПолучитьИмяВременногоФайла("zip");
		ДвоичныеДанныеФайлаАрхиваСПротоколом.Записать(ИмяФайлаАрхиваСПротоколом);
		
		// 
		КаталогПротоколов = Разархивировать(ИмяФайлаАрхиваСПротоколом);
		ФайлыПротоколов = ФайлыКаталога(КаталогПротоколов);
		Если НЕ ЗначениеЗаполнено(ФайлыПротоколов) Тогда
			ТекстСообщения = НСтр("ru = 'Ошибка разбора ответа от сервера онлайн-проверки отчетности.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
	Иначе
		
		// разбор результатов при универсальном формате
		
		ФайлыПротоколов = Новый Массив();
		Для ИндексПротокола = 0 ПО Контекст.Результат.КоличествоПротоколов - 1 Цикл
			НомерПротокола = Формат(ИндексПротокола + 1, "ЧН=0; ЧГ=");
			
			ФайлАрхиваСПротоколом64 = Неопределено;
			Если НЕ Контекст.Свойство("ФайлBase64АрхиваСПротоколом" + НомерПротокола, ФайлАрхиваСПротоколом64) Тогда
				ТекстСообщения = НСтр("ru = 'Ошибка разбора ответа от сервера онлайн-проверки отчетности.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
			
			ИмяФайлаПротокола = Неопределено;
			Контекст.Результат.Свойство("ИмяФайлаПротокола" + НомерПротокола, ИмяФайлаПротокола);
			
			ПризнакУпаковкиПротокола = Неопределено;
			Контекст.Результат.Свойство("ПризнакУпаковкиПротокола" + НомерПротокола, ПризнакУпаковкиПротокола);
			
			Если ЗначениеЗаполнено(ФайлАрхиваСПротоколом64) Тогда
				Если ПризнакУпаковкиПротокола = Истина Тогда
					// сохраняем протокол во временный файл
					ДвоичныеДанныеФайлаАрхиваСПротоколом = ДвоичныеДанныеИзСтроки64НаСервере(ФайлАрхиваСПротоколом64);
					ИмяФайлаАрхиваСПротоколом = ПолучитьИмяВременногоФайла("zip");
					ДвоичныеДанныеФайлаАрхиваСПротоколом.Записать(ИмяФайлаАрхиваСПротоколом);
					
					//
					КаталогПротоколов = Разархивировать(ИмяФайлаАрхиваСПротоколом);
					ФайлыПротокола = ФайлыКаталога(КаталогПротоколов);
					Если НЕ ЗначениеЗаполнено(ФайлыПротокола) Тогда
						ТекстСообщения = НСтр("ru = 'Ошибка разбора ответа от сервера онлайн-проверки отчетности.'");
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
						Возврат Ложь;
					КонецЕсли;
					
					Для Каждого ФайлПротокола Из ФайлыПротокола Цикл
						ФайлыПротоколов.Добавить(ФайлПротокола);
					КонецЦикла;
					
				Иначе
					КаталогРазмещения = ПолучитьИмяВременногоФайла();
					КаталогРазмещения = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогРазмещения);
					
					СоздатьКаталог(КаталогРазмещения);
					
					ДвоичныеДанныеФайлаАрхиваСПротоколом = ДвоичныеДанныеИзСтроки64НаСервере(ФайлАрхиваСПротоколом64);
					// Проверка файла на недопустимые символы.
					РегламентированнаяОтчетностьКлиентСервер.ПроверитьИмяФайла(ИмяФайлаПротокола);
					//@skip-check 1c-fresh-audit
					ДвоичныеДанныеФайлаАрхиваСПротоколом.Записать(КаталогРазмещения + ИмяФайлаПротокола);
					
					//@skip-check 1c-fresh-audit
					ФайлПротокола = Новый Файл(КаталогРазмещения + ИмяФайлаПротокола);
					ФайлыПротоколов.Добавить(ФайлПротокола);
				КонецЕсли;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	// если протокол один, то определяем свойства протокола в структуре результата
	// для совместимости с ранней реализацией
	Если ФайлыПротоколов.Количество() = 1 Тогда
		
		ФайлПротокола = ФайлыПротоколов[0];
		РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
			ФайлПротокола.ПолноеИмя, "ФайлПротокола.ПолноеИмя",
			"РазобратьРезультатОтСервераОнлайнПроверки_РаспаковатьРасшифрованныйРезультатНаСервере");		
		//@skip-check 1c-fresh-audit
		АдресФайлаПротокола = ПоместитьВоВременноеХранилище(
			Новый ДвоичныеДанные(ФайлПротокола.ПолноеИмя), Контекст.УникальныйИдентификатор);
		
		// определяем короткое имя файла протокола
		Контекст.Результат.Вставить("ИмяФайлаПротокола", ФайлПротокола.Имя);
		
		// добавляем протокол в результат
		Контекст.Результат.Вставить("Протокол", АдресФайлаПротокола);
		
	КонецЕсли;
	
	// формируем полные пути к файлам протоколов в соотв. колонке таблицы РезультатыПоДокументам
	АдресТаблицыДокументов = Неопределено;
	Если Контекст.Результат.Свойство("РезультатыПоДокументам", АдресТаблицыДокументов) Тогда
		КоличествоПроверокДляУниверсальногоФормата = 0;
		КоличествоПротоколовДляУниверсальногоФормата = 0;
		
		ТаблицаДокументов = ПолучитьИзВременногоХранилища(АдресТаблицыДокументов);
		
		Для Каждого СтрРезультат Из ТаблицаДокументов Цикл
			Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
				
				Если ЗначениеЗаполнено(СтрРезультат.ИмяФайлаПротокола) Тогда
					СтрРезультат.ИмяФайлаПротокола = ПоместитьВоВременноеХранилищеЕслиСуществует(КаталогПротоколов + СтрРезультат.ИмяФайлаПротокола, Контекст.УникальныйИдентификатор, Истина);
				КонецЕсли;
				
			Иначе
				
				// разбор результатов при универсальном формате
				
				ИмяФайлаОсновногоПротокола = Строка(СтрРезультат.ИмяФайлаПротокола);
				ФайлПротоколаНайден = Ложь;
				Если ЗначениеЗаполнено(СтрРезультат.ИмяФайлаПротокола) Тогда
					Для Каждого ФайлПротокола Из ФайлыПротоколов Цикл
						Если СокрЛП(ВРег(ФайлПротокола.Имя)) = СокрЛП(ВРег(СтрРезультат.ИмяФайлаПротокола)) Тогда
							СтрРезультат.ИмяФайлаПротокола = ПоместитьВоВременноеХранилищеЕслиСуществует(ФайлПротокола.ПолноеИмя, Контекст.УникальныйИдентификатор, Истина);
							КоличествоПроверокДляУниверсальногоФормата = КоличествоПроверокДляУниверсальногоФормата + 1;
							КоличествоПротоколовДляУниверсальногоФормата = КоличествоПротоколовДляУниверсальногоФормата + 1;
							ФайлПротоколаНайден = Истина;
							Прервать;
						КонецЕсли;
					КонецЦикла;
					Если НЕ ФайлПротоколаНайден Тогда
						СтрРезультат.ИмяФайлаПротокола = Неопределено;
					КонецЕсли;
				КонецЕсли;
				
				ТаблицаРезультатовПроверок = Неопределено;
				Если СтрРезультат.РезультатыПроверок <> Неопределено Тогда
					ТаблицаРезультатовПроверок = ПолучитьИзВременногоХранилища(СтрРезультат.РезультатыПроверок);
					
					Для Каждого РезультатПроверки Из ТаблицаРезультатовПроверок Цикл
						
						Если ЗначениеЗаполнено(РезультатПроверки.ИмяФайлаПротокола) Тогда
							Если СокрЛП(ВРег(РезультатПроверки.ИмяФайлаПротокола)) = СокрЛП(ВРег(ИмяФайлаОсновногоПротокола)) Тогда
								РезультатПроверки.ИмяФайлаПротокола = СтрРезультат.ИмяФайлаПротокола;
							Иначе
								ФайлПротоколаНайден = Ложь;
								Для Каждого ФайлПротокола Из ФайлыПротоколов Цикл
									Если СокрЛП(ВРег(ФайлПротокола.Имя)) = СокрЛП(ВРег(РезультатПроверки.ИмяФайлаПротокола)) Тогда
										РезультатПроверки.ИмяФайлаПротокола = ПоместитьВоВременноеХранилищеЕслиСуществует(ФайлПротокола.ПолноеИмя, Контекст.УникальныйИдентификатор, Истина);
										КоличествоПротоколовДляУниверсальногоФормата = КоличествоПротоколовДляУниверсальногоФормата + 1;
										ФайлПротоколаНайден = Истина;
										Прервать;
									КонецЕсли;
								КонецЦикла;
								Если НЕ ФайлПротоколаНайден Тогда
									РезультатПроверки.ИмяФайлаПротокола = Неопределено;
								КонецЕсли;
								КоличествоПроверокДляУниверсальногоФормата = КоличествоПроверокДляУниверсальногоФормата + ТаблицаРезультатовПроверок.Количество();
							КонецЕсли;
						Иначе
							КоличествоПроверокДляУниверсальногоФормата = КоличествоПроверокДляУниверсальногоФормата + ТаблицаРезультатовПроверок.Количество();
						КонецЕсли;
						
					КонецЦикла;
					
					ПоместитьВоВременноеХранилище(ТаблицаРезультатовПроверок, СтрРезультат.РезультатыПроверок);
				КонецЕсли;
				
				// информация для диалога показа результатов по одиночному отчету
				Если ТаблицаДокументов.Количество() = 1 И КоличествоПротоколовДляУниверсальногоФормата >= 1 Тогда
					Контекст.Результат.Вставить("ИмяФайлаПротокола", СтрРезультат.ИсходноеИмяФайлаПротокола);
					Контекст.Результат.Вставить("Протокол", СтрРезультат.ИмяФайлаПротокола);
					Контекст.Результат.Вставить("ПрограммаПроверки", СтрРезультат.ПрограммаПроверки);
				КонецЕсли;
				
			КонецЕсли;
		КонецЦикла;
		
		ПоместитьВоВременноеХранилище(ТаблицаДокументов, АдресТаблицыДокументов);
		
		Контекст.Результат.Вставить("КоличествоПроверокДляУниверсальногоФормата", КоличествоПроверокДляУниверсальногоФормата);
		Контекст.Результат.Вставить("КоличествоПротоколовДляУниверсальногоФормата", КоличествоПротоколовДляУниверсальногоФормата);
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиенте(Контекст, ВыполняемоеОповещение)
	
	Результат = Контекст.Результат;
	КодЗавершения = Результат.КодРезультата;
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(Контекст.УчетнаяЗапись);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	Если Контекст.ПоддержкаСервисаОнлайнПроверкиВУниверсальномФормате <> Истина Тогда
		
		// получаем протокол
		ПротоколBase64 = Неопределено;
		Если (КодЗавершения = 0 ИЛИ КодЗавершения = 4 ИЛИ КодЗавершения = 6)
			И Результат.Свойство("Protocol", ПротоколBase64) Тогда // получаем строку Base64 зашифрованного протокола
			
			ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст", ВыполняемоеОповещение, Контекст);
			КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеЗаписиВФайлПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
				ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				ПротоколBase64Адрес = ПоместитьВоВременноеХранилище(ПротоколBase64, ЭтотОбъект.УникальныйИдентификатор);
				ДополнительныеПараметры.Вставить("ПротоколBase64Адрес", ПротоколBase64Адрес);
				
				ПротоколДДАдрес = ДеКодироватьИзBase64СодержимоеНаСервере(ПротоколBase64Адрес, ЭтотОбъект.УникальныйИдентификатор);
				ДополнительныеПараметры.Вставить("ПротоколДДАдрес", ПротоколДДАдрес);
				
				РезультатВыполнения = Новый Структура("Выполнено, ИмяФайла", Истина, ПротоколДДАдрес);
				ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
					Возврат;
				КонецЕсли;
				
				ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПротоколBase64, , Ложь);
			
		ИначеЕсли КодЗавершения = 1 ИЛИ КодЗавершения = 5 Тогда
			
			// получаем строку Base64 сертификата
			СертификатBase64 = Неопределено;
			Если НЕ Результат.Свойство("Certificate", СертификатBase64) Тогда
				Если КодЗавершения = 1 Тогда
					РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
					ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
					Возврат;
				ИначеЕсли КодЗавершения = 5 Тогда
					ТекстСообщения = НСтр("ru = 'Некорректный формат ответа сервера онлайн-проверки: в ответе на запрос о сертификате сервера отсутствуют данные сертификата.'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
					ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
					Возврат;
				КонецЕсли;
			КонецЕсли;
			
			// пишем файл сертификата на клиент
			ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст", ВыполняемоеОповещение, Контекст);
			Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеЗаписиФайлаСертификатаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
				ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				ИмяФайлаСертификата = ПоместитьВоВременноеХранилищеДанныеСКлиента(СертификатBase64, Контекст.УникальныйИдентификатор);
				РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ИмяФайлаСертификата);
				ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
				Возврат;
			Иначе
				ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СертификатBase64, , Ложь);
			КонецЕсли;
			
		Иначе
			РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		КонецЕсли;
		
	Иначе
		
		// разбор результатов при универсальном формате
		
		КоличествоПротоколов = 0;
		Если НЕ (Результат.Свойство("КоличествоПротоколов", КоличествоПротоколов) И КоличествоПротоколов > 0) Тогда
			РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
			Возврат;
		КонецЕсли;
		
		// преобразовываем протокол в двоичные данные
		СвойстваСертификатаОтправителя = Неопределено;
		РежимСервисаИнициализацияВыполнена = Ложь;
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Контекст", Контекст);
		ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
		ДополнительныеПараметры.Вставить("РежимСервисаИнициализацияВыполнена", РежимСервисаИнициализацияВыполнена);
		ДополнительныеПараметры.Вставить("СвойстваСертификатаОтправителя", СвойстваСертификатаОтправителя);
		ДополнительныеПараметры.Вставить("КоличествоПротоколов", КоличествоПротоколов);
		ДополнительныеПараметры.Вставить("НомерПротокола", 1);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение);
		
	КонецЕсли;
	
КонецПроцедуры


&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеЗаписиВФайлПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаКонтейнераПротокола = Результат.ИмяФайла;
	
	// расшифровываем
	//ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст, ИмяФайлаКонтейнераПротокола", ВыполняемоеОповещение, Контекст, ИмяФайлаКонтейнераПротокола);
	
	ДополнительныеПараметры.Вставить("ИмяФайлаКонтейнераПротокола", ИмяФайлаКонтейнераПротокола);
	Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеРасшифрованияПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	СертификатРасшифрования = Новый Структура("Отпечаток", Контекст.СертификатОтправителя);
	КонтекстМоделиХраненияКлюча(ДополнительныеПараметры, СертификатРасшифрования);
	
	КриптографияЭДКОКлиент.РасшифроватьPKCS7(Оповещение, ИмяФайлаКонтейнераПротокола, , СертификатРасшифрования, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеРасшифрованияПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	ИмяФайлаКонтейнераПротокола = ДополнительныеПараметры.ИмяФайлаКонтейнераПротокола;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	// если не удалось распаковать, то прерываемся
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка разбора ответа от сервера онлайн-проверки отчетности: не удалось расшифровать протокол.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаАрхиваСПротоколом = Результат.РасшифрованныйФайл;
	
	// удаляем файл контейнера
	ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ИмяФайлаКонтейнераПротокола);
	
	Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеЧтенияФайлаПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ФайлАрхиваСПротоколом64Адрес = КодироватьВBase64СодержимоеНаСервере(Результат.РасшифрованныйФайл, ЭтотОбъект.УникальныйИдентификатор);
		ФайлАрхиваСПротоколом64 = ПолучитьИзВременногоХранилища(ФайлАрхиваСПротоколом64Адрес);
		
		РезультатВыполнения = Новый Структура("Выполнено, СтрокаBase64", Истина, ФайлАрхиваСПротоколом64);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ИмяФайлаАрхиваСПротоколом);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеЧтенияФайлаПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	
	Если НЕ Результат.Выполнено Тогда
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ФайлАрхиваСПротоколом64 = Результат.СтрокаBase64;
	
	Контекст.Вставить("ФайлАрхиваСПротоколом64", ФайлАрхиваСПротоколом64);
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Контекст", ВыполняемоеОповещение, Контекст);
	Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеПолученияПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеПолученияПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	
	// распаковываем на сервере
	Если НЕ РазобратьРезультатОтСервераОнлайнПроверки_РаспаковатьРасшифрованныйРезультатНаСервере(Контекст) Тогда
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеПослеЗаписиФайлаСертификатаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
	КонецЕсли;
	
	ИмяФайлаСертификата = ?(Результат.Выполнено, Результат.ИмяФайла, Неопределено);
	
	// добавляем сертификат в результат
	Контекст.Результат.Вставить("Сертификат", ИмяФайлаСертификата);
	
	РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеЗавершение(РезультатВЦикле, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	РежимСервисаИнициализацияВыполнена = ДополнительныеПараметры.РежимСервисаИнициализацияВыполнена;
	СвойстваСертификатаОтправителя = ДополнительныеПараметры.СвойстваСертификатаОтправителя;
	КоличествоПротоколов = ДополнительныеПараметры.КоличествоПротоколов;
	НомерПротокола = ДополнительныеПараметры.НомерПротокола;
	
	Результат = Контекст.Результат;
	
	Пока НомерПротокола <= КоличествоПротоколов Цикл
		НомерПротоколаСтр = Формат(НомерПротокола, "ЧН=0; ЧГ=");
		НомерПротокола = НомерПротокола + 1;
		
		ПротоколBase64 = Неопределено;
		
		Если НЕ Результат.Свойство("Протокол" + НомерПротоколаСтр, ПротоколBase64) Тогда
			РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
			Возврат;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ПротоколBase64) Тогда
			ФайлАрхиваСПротоколом64 = ПротоколBase64;
			
			Контекст.Вставить("ФайлBase64АрхиваСПротоколом" + НомерПротоколаСтр, ФайлАрхиваСПротоколом64);
			
		Иначе
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("Контекст", Контекст);
			ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
			ДополнительныеПараметры.Вставить("РежимСервисаИнициализацияВыполнена", РежимСервисаИнициализацияВыполнена);
			ДополнительныеПараметры.Вставить("СвойстваСертификатаОтправителя", СвойстваСертификатаОтправителя);
			ДополнительныеПараметры.Вставить("КоличествоПротоколов", КоличествоПротоколов);
			ДополнительныеПараметры.Вставить("НомерПротоколаСтр", НомерПротоколаСтр);
			ДополнительныеПараметры.Вставить("НомерПротокола", НомерПротокола);
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			
			Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеПослеЗаписиВФайлПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			
			Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
				
				АдресКонтейнераПротокола = ЗагрузитьДанныеНаСервер(ПротоколBase64, Контекст.УникальныйИдентификатор,, Истина,, Истина); //Входящий Base64 извлечем преобразуем в ДД
				Результат = Новый Структура("Выполнено, ИмяФайла", Истина, АдресКонтейнераПротокола);
				ВыполнитьОбработкуОповещения(Оповещение, Результат);
				
			Иначе
				ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПротоколBase64, , Ложь);
			КонецЕсли;
			Возврат;
			//КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	// распаковываем на сервере
	Если НЕ РазобратьРезультатОтСервераОнлайнПроверки_РаспаковатьРасшифрованныйРезультатНаСервере(Контекст) Тогда
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Истина, Контекст);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеПослеЗаписиВФайлПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	РежимСервисаИнициализацияВыполнена = ДополнительныеПараметры.РежимСервисаИнициализацияВыполнена;
	СвойстваСертификатаОтправителя = ДополнительныеПараметры.СвойстваСертификатаОтправителя;
	КоличествоПротоколов = ДополнительныеПараметры.КоличествоПротоколов;
	НомерПротоколаСтр = ДополнительныеПараметры.НомерПротоколаСтр;
	НомерПротокола = ДополнительныеПараметры.НомерПротокола;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаКонтейнераПротокола = Результат.ИмяФайла;
	
	// расшифровываем
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("РежимСервисаИнициализацияВыполнена", РежимСервисаИнициализацияВыполнена);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаОтправителя", СвойстваСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("КоличествоПротоколов", КоличествоПротоколов);
	ДополнительныеПараметры.Вставить("НомерПротоколаСтр", НомерПротоколаСтр);
	ДополнительныеПараметры.Вставить("НомерПротокола", НомерПротокола);
	ДополнительныеПараметры.Вставить("ИмяФайлаКонтейнераПротокола", ИмяФайлаКонтейнераПротокола);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеПослеРасшифрованияПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	СертификатРасшифрования = Новый Структура("Отпечаток", Контекст.СертификатОтправителя);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатРасшифрования);
	КриптографияЭДКОКлиент.РасшифроватьPKCS7(Оповещение, ИмяФайлаКонтейнераПротокола, , СертификатРасшифрования, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеПослеРасшифрованияПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	РежимСервисаИнициализацияВыполнена = ДополнительныеПараметры.РежимСервисаИнициализацияВыполнена;
	СвойстваСертификатаОтправителя = ДополнительныеПараметры.СвойстваСертификатаОтправителя;
	КоличествоПротоколов = ДополнительныеПараметры.КоличествоПротоколов;
	НомерПротоколаСтр = ДополнительныеПараметры.НомерПротоколаСтр;
	НомерПротокола = ДополнительныеПараметры.НомерПротокола;
	ИмяФайлаКонтейнераПротокола = ДополнительныеПараметры.ИмяФайлаКонтейнераПротокола;
	
	// если не удалось распаковать, то прерываемся
	Если НЕ Результат.Выполнено Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка разбора ответа от сервера онлайн-проверки отчетности: не удалось расшифровать протокол.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаАрхиваСПротоколом = Результат.РасшифрованныйФайл;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		УдалитьИзВременногоХранилища(ИмяФайлаКонтейнераПротокола);
		ИмяФайлаКонтейнераПротокола = "";
	Иначе
	// удаляем файл контейнера
	ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ИмяФайлаКонтейнераПротокола);
	КонецЕсли;
	
	// сохраняем в Контекст в Base64
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("РежимСервисаИнициализацияВыполнена", РежимСервисаИнициализацияВыполнена);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаОтправителя", СвойстваСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("КоличествоПротоколов", КоличествоПротоколов);
	ДополнительныеПараметры.Вставить("НомерПротоколаСтр", НомерПротоколаСтр);
	ДополнительныеПараметры.Вставить("НомерПротокола", НомерПротокола);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

	Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеПослеЧтенияФайлаПротоколаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ЗакодироватьВBase64ЗначениеВременногоХранилища(ИмяФайлаАрхиваСПротоколом, Ложь);		
		СтрокаBase64 = ПолучитьИзВременногоХранилища(ИмяФайлаАрхиваСПротоколом);
		УдалитьИзВременногоХранилища(ИмяФайлаАрхиваСПротоколом);
		Результат = Новый Структура("Выполнено, СтрокаBase64", Истина, СтрокаBase64);
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ИмяФайлаАрхиваСПротоколом);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеПослеЧтенияФайлаПротоколаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Контекст = ДополнительныеПараметры.Контекст;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИдентификаторДокументооборота = ДополнительныеПараметры.ИдентификаторДокументооборота;
	РежимСервисаИнициализацияВыполнена = ДополнительныеПараметры.РежимСервисаИнициализацияВыполнена;
	СвойстваСертификатаОтправителя = ДополнительныеПараметры.СвойстваСертификатаОтправителя;
	КоличествоПротоколов = ДополнительныеПараметры.КоличествоПротоколов;
	НомерПротоколаСтр = ДополнительныеПараметры.НомерПротоколаСтр;
	НомерПротокола = ДополнительныеПараметры.НомерПротокола;
	
	Если НЕ Результат.Выполнено Тогда
		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ФайлАрхиваСПротоколом64 = Результат.СтрокаBase64;
	
	Контекст.Вставить("ФайлBase64АрхиваСПротоколом" + НомерПротоколаСтр, ФайлАрхиваСПротоколом64);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Контекст", Контекст);
	ДополнительныеПараметры.Вставить("ИдентификаторДокументооборота", ИдентификаторДокументооборота);
	ДополнительныеПараметры.Вставить("РежимСервисаИнициализацияВыполнена", РежимСервисаИнициализацияВыполнена);
	ДополнительныеПараметры.Вставить("СвойстваСертификатаОтправителя", СвойстваСертификатаОтправителя);
	ДополнительныеПараметры.Вставить("КоличествоПротоколов", КоличествоПротоколов);
	ДополнительныеПараметры.Вставить("НомерПротокола", НомерПротокола);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Оповещение = Новый ОписаниеОповещения("РазобратьРезультатОтСервераОнлайнПроверки_НаКлиентеВЦиклеВУниверсальномФорматеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОнлайнПроверкаОтчета_РазобратьРезультатОтСервераОнлайнПроверки(Контекст, ВыполняемоеОповещение)
	
	Результат = РазобратьРезультатОтСервераОнлайнПроверки_НаСервере(Контекст); // сервер
	Если Результат = Неопределено Тогда

		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии();
		КонецЕсли;

		РезультатВыполнения = Новый Структура("РезультатПроверки, Контекст", Ложь, Контекст);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Контекст.Вставить("Результат", Результат);
	
	РазобратьРезультатОтСервераОнлайнПроверки_НаКлиенте(Контекст, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ФайлыКаталога(Каталог)
	
	Возврат НайтиФайлы(Каталог, ПолучитьМаскуВсеФайлы());
	
КонецФункции

&НаСервереБезКонтекста
Функция Разархивировать(ИмяФайлаАрхива)
	
	// создаем каталог распаковки
	КаталогРаспаковки = ПолучитьИмяВременногоФайла();
	КаталогРаспаковки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогРаспаковки);
	
	СоздатьКаталог(КаталогРаспаковки);
	
	// распаковываем все содержимое
	Попытка
		ОбъектЧтение = Новый ЧтениеZipФайла(ИмяФайлаАрхива);
		ОбъектЧтение.ИзвлечьВсе(КаталогРаспаковки);
		ОбъектЧтение.Закрыть();
		Возврат КаталогРаспаковки;
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка распаковки пакета:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьЗапросКСерверуОнлайнПроверки(УчетнаяЗапись, ДанныеСертификата = Неопределено, ИмяФайлаЗашифрованногоКонтейнера = Неопределено)
	
	АдресОтправителя = СокрЛП(УчетнаяЗапись.АдресЭлектроннойПочты);
	Если НЕ ЗначениеЗаполнено(АдресОтправителя) Тогда
		ТекстСообщения = НСтр("ru = 'В свойствах учетной записи не указан адрес электронной почты.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// инициализируем объект для записи XML
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ОбъектЗапись = Новый ЗаписьXML;
	ПараметрыЗаписиXML = Новый ПараметрыЗаписиXML("windows-1251", , Ложь);
	ОбъектЗапись.ОткрытьФайл(ИмяФайлаЗапроса, ПараметрыЗаписиXML);
	ОбъектЗапись.ЗаписатьОбъявлениеXML();
	
	// выводим корневой элемент Request
	ОбъектЗапись.ЗаписатьНачалоЭлемента("Request");
	
	// выводим элемент ClientInfo
	ОбъектЗапись.ЗаписатьНачалоЭлемента("ClientInfo");
	ОбъектЗапись.ЗаписатьАтрибут("email", АдресОтправителя);
	ОбъектЗапись.ЗаписатьКонецЭлемента();
	
	Если ДанныеСертификата <> Неопределено Тогда
		
		ДанныеСертификатаБезПереводовСтрок = СтрЗаменить(ДанныеСертификата, Символы.ВК, "");
		ДанныеСертификатаБезПереводовСтрок = СтрЗаменить(ДанныеСертификатаБезПереводовСтрок, Символы.ПС, "");
		
		// выводим элемент Certificate
		ОбъектЗапись.ЗаписатьНачалоЭлемента("Certificate");
		ОбъектЗапись.ЗаписатьТекст(ДанныеСертификатаБезПереводовСтрок);
		ОбъектЗапись.ЗаписатьКонецЭлемента();
		
	КонецЕсли;
	
	Если ИмяФайлаЗашифрованногоКонтейнера <> Неопределено Тогда
		
		Если ЭтоАдресВременногоХранилища(ИмяФайлаЗашифрованногоКонтейнера) Тогда
			ИмяФайлаДляЧтения = ПолучитьИмяВременногоФайла();
			ПолучитьИзВременногоХранилища(ИмяФайлаЗашифрованногоКонтейнера).Записать(ИмяФайлаДляЧтения);
		Иначе
			ИмяФайлаДляЧтения = ИмяФайлаЗашифрованногоКонтейнера;
		КонецЕсли;
		
		// кодируем в Base64 зашифрованный контейнер
		ФайлЗашифрованногоКонтейнераBase64 = ПрочитатьBase64ИзФайла(ИмяФайлаДляЧтения);
		
		// выводим элемент Data
		ОбъектЗапись.ЗаписатьНачалоЭлемента("Data");
		ОбъектЗапись.ЗаписатьТекст(ФайлЗашифрованногоКонтейнераBase64);
		ОбъектЗапись.ЗаписатьКонецЭлемента();
		
	КонецЕсли;
	
	// записываем конец для корневого элемента
	ОбъектЗапись.ЗаписатьКонецЭлемента();
	
	ОбъектЗапись.Закрыть();
	
	Возврат ИмяФайлаЗапроса;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьЗапросКСерверуОнлайнПроверкиВУниверсальномФормате(УчетнаяЗапись, Документ, КонтролирующийОрган, ДанныеСертификата, ИменаФайловВыгрузки, ИменаФайловЗашифрованныхКонтейнеров)
	
	// инициализируем объект для записи XML
	ИмяФайлаЗапроса = ПолучитьИмяВременногоФайла();
	ОбъектЗапись = Новый ЗаписьXML;
	ПараметрыЗаписиXML = Новый ПараметрыЗаписиXML("windows-1251", , Ложь);
	ОбъектЗапись.ОткрытьФайл(ИмяФайлаЗапроса, ПараметрыЗаписиXML);
	ОбъектЗапись.ЗаписатьОбъявлениеXML();
	
	// выводим корневой элемент Запрос
	ОбъектЗапись.ЗаписатьНачалоЭлемента("Запрос");
	ОбъектЗапись.ЗаписатьАтрибут("ВерсФорм", "1.0");
	ОбъектЗапись.ЗаписатьАтрибут("ДатаВремяФормирования", XMLСтрока(ТекущаяДатаСеанса()));
	ОбъектЗапись.ЗаписатьАтрибут("ВерсПрог", РегламентированнаяОтчетность.НазваниеИВерсияПрограммы());
	
	// выводим элемент УчетнаяЗапись
	ИдентификаторАбонента = нрег(Сред(УчетнаяЗапись.ИдентификаторАбонента, СтрДлина(УчетнаяЗапись.ИдентификаторСпецоператора) + 1));
	ОбъектЗапись.ЗаписатьНачалоЭлемента("УчетнаяЗапись");
	ОбъектЗапись.ЗаписатьТекст(ИдентификаторАбонента);
	ОбъектЗапись.ЗаписатьКонецЭлемента();
	
	// выводим элемент ТипПолучателя
	ОбъектЗапись.ЗаписатьНачалоЭлемента("ТипПолучателя");
	ОбъектЗапись.ЗаписатьТекст(КонтролирующийОрган);
	ОбъектЗапись.ЗаписатьКонецЭлемента();
	
	// выводим элемент Сертификат
	ОбъектЗапись.ЗаписатьНачалоЭлемента("Сертификат");
	ОбъектЗапись.ЗаписатьТекст(ДанныеСертификата);
	ОбъектЗапись.ЗаписатьКонецЭлемента();
	
	Для ИндексФайла = 0 По ИменаФайловЗашифрованныхКонтейнеров.Количество() - 1 Цикл
		
		Если ТипЗнч(Документ) = Тип("Массив") Тогда
			Отчет = Документ[ИндексФайла];
		Иначе
			Отчет = Документ;
		КонецЕсли;
		МетаданныеОбъекта = Метаданные.НайтиПоТипу(ТипЗнч(Отчет));
		ИсточникОтчета = "";
		Если МетаданныеОбъекта <> Неопределено Тогда
			ИмяТипаОтчета = МетаданныеОбъекта.Имя;
			Если ИмяТипаОтчета = "РегламентированныйОтчет" Тогда
				ИсточникОтчета = Отчет.ИсточникОтчета;
			Иначе
				ИсточникОтчета = ИмяТипаОтчета;
			КонецЕсли;
		КонецЕсли;
		
		// выводим элемент Документ
		ОбъектЗапись.ЗаписатьНачалоЭлемента("Документ");
		ОбъектЗапись.ЗаписатьАтрибут("ИмяФайлаВыгрузки", ИменаФайловВыгрузки[ИндексФайла]);
		Если ЗначениеЗаполнено(ИсточникОтчета) Тогда
			ОбъектЗапись.ЗаписатьАтрибут("ТипДокумента", ИсточникОтчета);
		КонецЕсли;
		ОбъектЗапись.ЗаписатьКонецЭлемента();
		
	КонецЦикла;
	
	Для ИндексФайла = 0 По ИменаФайловЗашифрованныхКонтейнеров.Количество() - 1 Цикл
		
		ИмяФайлаЗашифрованногоКонтейнера = ИменаФайловЗашифрованныхКонтейнеров[ИндексФайла];
		Если ЭтоАдресВременногоХранилища(ИмяФайлаЗашифрованногоКонтейнера) Тогда
			ИмяФайлаДляЧтения = ПолучитьИмяВременногоФайла();
			ПолучитьИзВременногоХранилища(ИмяФайлаЗашифрованногоКонтейнера).Записать(ИмяФайлаДляЧтения);
		Иначе
			ИмяФайлаДляЧтения = ИмяФайлаЗашифрованногоКонтейнера;
		КонецЕсли;
		
		// кодируем в Base64 зашифрованный контейнер
		ФайлЗашифрованногоКонтейнераBase64 = ПрочитатьBase64ИзФайла(ИмяФайлаДляЧтения);
		
		// выводим элемент Данные
		ОбъектЗапись.ЗаписатьНачалоЭлемента("Данные");
		ОбъектЗапись.ЗаписатьАтрибут("ИмяФайлаВыгрузки", ИменаФайловВыгрузки[ИндексФайла]);
		ОбъектЗапись.ЗаписатьТекст(ФайлЗашифрованногоКонтейнераBase64);
		ОбъектЗапись.ЗаписатьКонецЭлемента();
		
	КонецЦикла;
	
	// записываем конец для корневого элемента
	ОбъектЗапись.ЗаписатьКонецЭлемента();
	
	ОбъектЗапись.Закрыть();
	
	Возврат ИмяФайлаЗапроса;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗаархивироватьФайл(ИмяИсходногоФайла, ИмяФайлаАрхива = Неопределено)
	
	Если ТипЗнч(ИмяИсходногоФайла) = Тип("Массив") Тогда
		МассивФайлов = ИмяИсходногоФайла;
	Иначе
		МассивФайлов = Новый Массив;
		МассивФайлов.Добавить(ИмяИсходногоФайла);
	КонецЕсли;
	
	Попытка
		ОбъектЗапись = Новый ЗаписьZipФайла(ИмяФайлаАрхива, , , , УровеньСжатияZIP.Максимальный);
		Для Каждого ЭлФайл Из МассивФайлов Цикл
			ОбъектЗапись.Добавить(ЭлФайл);
		КонецЦикла;
		ОбъектЗапись.Записать();
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка записи архива:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	Конецпопытки;
	
	Возврат Истина;
	
КонецФункции

//
// ОНЛАЙН-ПРОВЕРКА РЕГЛАМЕНТИРОВАННОЙ ОТЧЕТНОСТИ
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// ФУНКЦИИ ВЫГРУЗКИ АРХИВА ДОКУМЕНТООБОРОТА
//

&НаКлиенте
Процедура ПолучениеПеремещенныхФайлов(Контейнеры, Организация, ВариантЗапуска, Владелец, ОбработкаЗавершения = Неопределено) Экспорт
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Контейнеры", Контейнеры);
	ПараметрыФормы.Вставить("Организация", Организация);
	ПараметрыФормы.Вставить("РежимФормы", ВариантЗапуска);
	
	ФормаРезультат = ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ТранспортныеКонтейнерыВАрхиве", 
		ПараметрыФормы,
		Владелец, , , ,
		ОбработкаЗавершения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбмена(ВыгружаемыеЦиклыОбмена, ВыгружатьКонтейнеры = Истина, ВыгружатьСодержимое = Ложь, Путь = Неопределено, ДополнятьПуть = Истина) Экспорт
	
	Если ТипЗнч(ВыгружаемыеЦиклыОбмена) <> Тип("Массив") Тогда
		ЦиклыОбмена = Новый Массив;
		ЦиклыОбмена.Добавить(ВыгружаемыеЦиклыОбмена);
	Иначе
		ЦиклыОбмена = ВыгружаемыеЦиклыОбмена;
	КонецЕсли;
	
	// заданы ли циклы обмена
	Если НЕ ЗначениеЗаполнено(ЦиклыОбмена) Тогда
		ПоказатьПредупреждение(, "Не выбраны циклы обмена для выгрузки!");
		Возврат;
	КонецЕсли;
			
	ВАрхиве = 0;
	Перемещено = 0;
	ОбщееКоличествоФайлов = ПолучитьОбщееКоличествоВыгружаемыхФайлов(ЦиклыОбмена, ВыгружатьКонтейнеры, ВыгружатьСодержимое, ВАрхиве, Перемещено);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ЦиклыОбмена", 					ЦиклыОбмена);
	ДополнительныеПараметры.Вставить("ОбщееКоличествоФайлов", 			ОбщееКоличествоФайлов);
	ДополнительныеПараметры.Вставить("Путь", 							Путь);
	ДополнительныеПараметры.Вставить("ВыгружатьКонтейнеры", 			ВыгружатьКонтейнеры);
	ДополнительныеПараметры.Вставить("ВыгружатьСодержимое", 			ВыгружатьСодержимое);
	ДополнительныеПараметры.Вставить("ДополнятьПуть", 					ДополнятьПуть);
	
	Если ОбщееКоличествоФайлов = 0 Тогда
		ПоказатьПредупреждение(, "Нет информации для выгрузки!");
		Возврат;
	КонецЕсли;
	
	Если Перемещено > 0 Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьЦиклыОбменаЗавершениеВыбора", ЭтотОбъект, ДополнительныеПараметры);
		ПолучениеПеремещенныхФайлов(ЦиклыОбмена, Неопределено, ?(Перемещено >= ОбщееКоличествоФайлов, "Выгрузка", "ВыгрузкаЧастично"), Неопределено, ОписаниеОповещения);
	
	ИначеЕсли ВАрхиве >= ОбщееКоличествоФайлов Тогда
		ПоказатьПредупреждение(, "Все файлы были перемещены в архив. Выгрузка не возможна!");
		
	ИначеЕсли ВАрхиве > 0 Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьЦиклыОбменаЗавершениеВыбора", ЭтотОбъект, ДополнительныеПараметры);
		ПоказатьВопрос(ОписаниеОповещения, "Файлы частично были перемещены в архив. Продолжить выгрузку без них?", РежимДиалогаВопрос.ДаНет);
		
	Иначе
		ВыгрузитьЦиклыОбменаПродолжение(ДополнительныеПараметры);
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаЗавершениеВыбора(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		ВыгрузитьЦиклыОбменаПродолжение(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаПродолжение(ДополнительныеПараметры)
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыгрузитьЦиклыОбменаЗавершение", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ТекстСообщения = ОперацииСФайламиЭДКОКлиент.ТекстСообщенияДляНеобязательнойУстановкиРасширенияРаботыСФайлами();
	ФайловаяСистемаКлиент.ПодключитьРасширениеДляРаботыСФайлами(ОписаниеОповещения, ТекстСообщения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаЗавершение(ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами, ДополнительныеПараметры) Экспорт
	
	Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
		ВыгрузитьЦиклыОбменаСРасширениемРаботыСФайлами(ДополнительныеПараметры);
	Иначе
		ВыгрузитьЦиклыОбменаБезРасширенияРаботыСФайлами(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьВыгружаемыеФайлы(ЦиклыОбмена, ВыгружатьКонтейнеры, ВыгружатьСодержимое)
	
	Запрос = Новый Запрос;
	Запросы = Новый Массив;
	
	// В запросе отсуствует ПометкаУдаления = Ложь так как в истории отправки
	// отображаются помеченные на уделение этапы. Для них выгрузка 
	// тоже должна работать.
	Если ВыгружатьСодержимое Тогда
		Запросы.Добавить("ВЫБРАТЬ //$Разрешенные
						|	СодержимоеТранспортныхКонтейнеров.ИмяФайла,
						|	СодержимоеТранспортныхКонтейнеров.Данные,
						|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение,
						|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Тип,
						|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена,
						|	""Содержимое"" КАК ИДСодержимое
						|ИЗ
						|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						|
						|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						|			ПО 		(СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
						|				И	(СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
						|				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
						|
						|ГДЕ
						|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						|	И (СостояниеОбъектов.Архивный ЕСТЬ NULL Или СостояниеОбъектов.Архивный <> Истина)
						|	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Статус В (&Статусы)");
	КонецЕсли;
	
	Если ВыгружатьКонтейнеры Тогда
		Запросы.Добавить("ВЫБРАТЬ //$Разрешенные
						|	ТранспортныеКонтейнеры.ИмяФайла,
						|	ТранспортныеКонтейнеры.Данные,
						|	ТранспортныеКонтейнеры.ТранспортноеСообщение,
						|	ТранспортныеКонтейнеры.ТранспортноеСообщение.Тип,
						|	ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена,
						|	""Контейнер"" КАК ИДКонтейнеры
						|ИЗ
						|	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						|
						|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						|			ПО 		(СостояниеОбъектов.Объект = ТранспортныеКонтейнеры.ТранспортноеСообщение)
						|				И	(СостояниеОбъектов.ИмяФайла = ТранспортныеКонтейнеры.ИмяФайла)
						|				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ТранспортныеКонтейнеры))
						|
						|ГДЕ
						|	ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						|	И (СостояниеОбъектов.Архивный ЕСТЬ NULL Или СостояниеОбъектов.Архивный <> Истина)
						|	И ТранспортныеКонтейнеры.ТранспортноеСообщение.Статус В (&Статусы)");
	КонецЕсли;
	
	СтрОбъединитьВсе = "
						|
						|ОБЪЕДИНИТЬ ВСЕ
						|
						|";
	
	Для Инд = 1 По Запросы.Количество() Цикл
		ТекстЗапрос = СтрЗаменить(Запросы[Инд - 1], "//$Разрешенные", ?(Инд = 1, "РАЗРЕШЕННЫЕ", ""));
		Запрос.Текст = Запрос.Текст + ?(Инд = 1, "", СтрОбъединитьВсе) + ТекстЗапрос;
	КонецЦикла;
	
	Статусы = Новый Массив;
	Статусы.Добавить(Перечисления.СтатусыПисем.Отправленное);
	Статусы.Добавить(Перечисления.СтатусыПисем.Полученное);
	Запрос.УстановитьПараметр("Статусы", Статусы);
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклыОбмена);
	Выборка = Запрос.Выполнить().Выгрузить();
	
	ТранспортныеСообщенияСПрисоединеннымиФайлами = Новый Массив;
	Для каждого ЭлементВыборки Из Выборка Цикл
		Если ТранспортныеСообщенияСПрисоединеннымиФайлами.Найти(ЭлементВыборки.ТранспортноеСообщение) = Неопределено
			И ЭлементВыборки.Данные.Получить() = Неопределено Тогда
			
			ТранспортныеСообщенияСПрисоединеннымиФайлами.Добавить(ЭлементВыборки.ТранспортноеСообщение);
		КонецЕсли;
	КонецЦикла;
	
	ТаблицаФайлов = Неопределено;
	Если ТранспортныеСообщенияСПрисоединеннымиФайлами.Количество() > 0 Тогда
		ИменаСправочников = ?(ВыгружатьСодержимое, "ТранспортноеСообщениеПрисоединенныеФайлы", "")
			+ ?(ВыгружатьСодержимое И ВыгружатьКонтейнеры, ", ", "")
			+ ?(ВыгружатьКонтейнеры, "ТранспортноеСообщениеКонтейнерыПрисоединенныеФайлы", "");
		ТаблицаФайлов = ОбщегоНазначенияЭДКО.ПрикрепленныеФайлыКОбъектуИзСправочника(
			ТранспортныеСообщенияСПрисоединеннымиФайлами,
			ИменаСправочников,,
			"ВладелецФайла, ИсходноеИмяФайла");
		
		Для каждого СтрокаТаблицы Из ТаблицаФайлов Цикл
			СтрокаТаблицы.ИсходноеИмяФайла = ВРЕГ(СтрокаТаблицы.ИсходноеИмяФайла);
		КонецЦикла;
	КонецЕсли;
	
	МассивВыборки = Новый Массив;
	Для каждого ЭлементВыборки Из Выборка Цикл
		ЭлементМассива = Новый Структура;
		
		ДанныеТранспортногоКонтейнера = ЭлементВыборки.Данные.Получить();
		Если ДанныеТранспортногоКонтейнера = Неопределено Тогда
			ИсходноеИмяФайла = ВРЕГ(ЭлементВыборки.ИмяФайла);
			
			МассивДанныхТранспортногоКонтейнера = Новый Массив;
			ИндексФайла = 0;
			Пока Истина Цикл
				ИндексФайлаСтрокой = ?(ИндексФайла = 0, "", Формат(ИндексФайла, "ЧДЦ=; ЧН=; ЧГ="));
				
				ПараметрыОтбора = Новый Структура("ВладелецФайла, ИсходноеИмяФайла",
					ЭлементВыборки.ТранспортноеСообщение, ИсходноеИмяФайла + ИндексФайлаСтрокой);
				СтрокиТаблицы = ТаблицаФайлов.НайтиСтроки(ПараметрыОтбора);
				Если СтрокиТаблицы.Количество() > 0 Тогда
					ДанныеЧастиТранспортногоКонтейнера = РаботаСФайлами.ДвоичныеДанныеФайла(СтрокиТаблицы[0].Ссылка);
					МассивДанныхТранспортногоКонтейнера.Добавить(ДанныеЧастиТранспортногоКонтейнера);
					
				ИначеЕсли ИндексФайла > 0 Тогда
					Прервать;
				КонецЕсли;
				
				Если Выборка.Колонки.Найти("ИДСодержимое") <> Неопределено И ЭлементВыборки.ИДСодержимое <> "Контейнер" Тогда
					Прервать;
				КонецЕсли;
				ИндексФайла = ИндексФайла + 1;
			КонецЦикла;
			
			Если МассивДанныхТранспортногоКонтейнера.Количество() <> 0 Тогда
				Если МассивДанныхТранспортногоКонтейнера.Количество() = 1 Тогда
					ДанныеТранспортногоКонтейнера = МассивДанныхТранспортногоКонтейнера[0];
				Иначе
					ДанныеТранспортногоКонтейнера = СоединитьДвоичныеДанные(МассивДанныхТранспортногоКонтейнера);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ДанныеТранспортногоКонтейнера) Тогда
			// В "битом" регистре могут попадаться пустые данные. Такие данные не нужны.
			Продолжить;
		КонецЕсли;
		
		Для Каждого КолонкаВыборки Из Выборка.Колонки Цикл 
			
			ИмяКолонки = КолонкаВыборки.Имя;
			Если ИмяКолонки = "Данные" Тогда
				ЭлементМассива.Вставить("Адрес", ПоместитьВоВременноеХранилище(ДанныеТранспортногоКонтейнера, Новый УникальныйИдентификатор));
			Иначе
				ЗначениеЭлемента = ЭлементВыборки[ИмяКолонки];
				ЭлементМассива.Вставить(ИмяКолонки, ЗначениеЭлемента);
			КонецЕсли;
			
		КонецЦикла;
		МассивВыборки.Добавить(ЭлементМассива);
	КонецЦикла;
	
	Возврат МассивВыборки;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОбщееКоличествоВыгружаемыхФайлов(ЦиклыОбмена, ВыгружатьКонтейнеры, ВыгружатьСодержимое, ИзНихВАрхиве = 0, Перемещено = 0)
	
	Запрос = Новый Запрос;
	Запросы = Новый Массив;
	
	// В запросе отсуствует ПометкаУдаления = Ложь, 
	// так как в истории отправки показываем помеченные на удаление
	// этапы, для них выгрузка тоже долджна работать.
	Если ВыгружатьСодержимое Тогда
		Запросы.Добавить("ВЫБРАТЬ //$Разрешенные
						|	Количество(ИСТИНА)
						|	, ЕстьNULL(Сумма(
						|	ВЫБОР
						|		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА 0
						|		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА 0
						|	ИНАЧЕ
						|		1
						|	КОНЕЦ
						|	), 0) ВАрхиве,
						|	0 Перемещено
						|ИЗ
						|	РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						|
						|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						|			ПО 		(СостояниеОбъектов.Объект = СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение)
						|				И	(СостояниеОбъектов.ИмяФайла = СодержимоеТранспортныхКонтейнеров.ИмяФайла)
						|				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.СодержимоеТранспортныхКонтейнеров))
						|
						|ГДЕ
						|	СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						|	И СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.Статус В (&Статусы)");
	КонецЕсли;
	
	Если ВыгружатьКонтейнеры Тогда
		Запросы.Добавить("ВЫБРАТЬ //$Разрешенные
						|	Количество(ИСТИНА)
						|	, ЕстьNULL(Сумма(
						|	ВЫБОР
						|		КОГДА СостояниеОбъектов.Архивный ЕСТЬ NULL ТОГДА 0
						|		КОГДА СостояниеОбъектов.Архивный = Ложь ТОГДА 0
						|	ИНАЧЕ
						|		1
						|	КОНЕЦ
						|	), 0) ВАрхиве,
						|	ЕСТЬNULL(СУММА(ВЫБОР КОГДА ЕСТЬNULL(СостояниеАрхивированияФайловДОСКонтролирующимиОрганами.Архивный, ЛОЖЬ) = ИСТИНА ТОГДА 1 ИНАЧЕ 0 КОНЕЦ), 0) Перемещено
						|ИЗ
						|	РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						|
						|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПризнакиАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеОбъектов
						|			ПО 		(СостояниеОбъектов.Объект = ТранспортныеКонтейнеры.ТранспортноеСообщение)
						|				И	(СостояниеОбъектов.ИмяФайла = ТранспортныеКонтейнеры.ИмяФайла)
						|				И	(СостояниеОбъектов.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ТранспортныеКонтейнеры))
						|
						|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СостояниеАрхивированияФайловДОСКонтролирующимиОрганами КАК СостояниеАрхивированияФайловДОСКонтролирующимиОрганами
						|			ПО 		(СостояниеАрхивированияФайловДОСКонтролирующимиОрганами.Объект = ТранспортныеКонтейнеры.ТранспортноеСообщение)
						|				И	(СостояниеАрхивированияФайловДОСКонтролирующимиОрганами.ИмяФайла = ТранспортныеКонтейнеры.ИмяФайла)
						|				И	(СостояниеАрхивированияФайловДОСКонтролирующимиОрганами.Владелец = Значение(Перечисление.ВидыАрхивируемыхМетаданныхДО.ТранспортныеКонтейнеры))
						|
						|ГДЕ
						|	ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						|	И ТранспортныеКонтейнеры.ТранспортноеСообщение.Статус В (&Статусы)");
	КонецЕсли;
	
	СтрОбъединитьВсе = "
						|
						|ОБЪЕДИНИТЬ ВСЕ
						|
						|";
	
	Для Инд = 1 По Запросы.Количество() Цикл
		ТекстЗапрос = СтрЗаменить(Запросы[Инд - 1], "//$Разрешенные", ?(Инд = 1, "РАЗРЕШЕННЫЕ", ""));
		Запрос.Текст = Запрос.Текст + ?(Инд = 1, "", СтрОбъединитьВсе) + ТекстЗапрос;
	КонецЦикла;
	
	Статусы = Новый Массив;
	Статусы.Добавить(Перечисления.СтатусыПисем.Отправленное);
	Статусы.Добавить(Перечисления.СтатусыПисем.Полученное);
	Запрос.УстановитьПараметр("Статусы", Статусы);
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклыОбмена);
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	
	ИзНихВАрхиве = 0;
	Перемещено = 0;
	Результат = 0;
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Результат = Результат + ?(ЗначениеЗаполнено(Выборка.Получить(0)), Выборка.Получить(0), 0);
		ИзНихВАрхиве = ИзНихВАрхиве + ?(ЗначениеЗаполнено(Выборка.ВАрхиве), Выборка.ВАрхиве, 0);
		Перемещено = Перемещено + ?(ЗначениеЗаполнено(Выборка.Перемещено), Выборка.Перемещено, 0);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПредставленияЦикловОбмена(ЦиклыОбмена)
	
	ПредставленияЦикловОбмена = Новый Соответствие;
	
	Запрос = Новый Запрос("ВЫБРАТЬ
						  |	ЦиклыОбмена.Ссылка,
						  |	ЦиклыОбмена.Организация,
						  |	ЦиклыОбмена.ВнешняяОрганизация,
						  |	ЦиклыОбмена.ДатаСоздания,
						  |	ЦиклыОбмена.Тип,
						  |	ЦиклыОбмена.ВидОтчета,
						  |	ЦиклыОбмена.ДатаНачалаПериода,
						  |	ЦиклыОбмена.ДатаОкончанияПериода,
						  |	ЦиклыОбмена.ВидДокумента,
						  |	ЦиклыОбмена.ВидУслуги,
						  |	ЦиклыОбмена.ФорматОтвета,
						  |	ЦиклыОбмена.Предмет
						  |ИЗ
						  |	Справочник.ЦиклыОбмена КАК ЦиклыОбмена
						  |ГДЕ
						  |	ЦиклыОбмена.Ссылка В(&Ссылка)");
	Запрос.УстановитьПараметр("Ссылка", ЦиклыОбмена);
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		ПредставленияЦикловОбмена.Вставить(Выборка.Ссылка, ПредставлениеЦиклаОбмена(Выборка));
		
	КонецЦикла;
	
	Возврат ПредставленияЦикловОбмена;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеЦиклаОбмена(ЦиклОбмена) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПредставлениеЦиклаОбмена(ЦиклОбмена);
	
КонецФункции

//
// ФУНКЦИИ ВЫГРУЗКИ АРХИВА ДОКУМЕНТООБОРОТА
////////////////////////////////////////////////////////////////////////////////

&НаСервере
Процедура СформироватьТаблицуОтветовНаЗапросИОН(Запрос)
	
	ТаблицаОтветовНаЗапросИОН = ПолучитьОтветыНаЗапросИОН(Запрос, Истина);
	ТаблицаОтветовНаЗапросИОН.Колонки.Добавить("Адрес");
	Для Каждого СтрокаОтвета Из ТаблицаОтветовНаЗапросИОН Цикл
		Если ЗначениеЗаполнено(СтрокаОтвета.Данные) Тогда 
			ГУИД = Новый УникальныйИдентификатор;
			СтрокаОтвета.Адрес = ПоместитьВоВременноеХранилище(СтрокаОтвета.Данные.Получить(), ГУИД);
		КонецЕсли;
	КонецЦикла;
	
	ОтветыНаЗапросыИОН.Загрузить(ТаблицаОтветовНаЗапросИОН);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОН(Запрос) Экспорт
	
	// получаем все документы к показу
	СформироватьТаблицуОтветовНаЗапросИОН(Запрос);
	
	// определяем документ, который показываем
	КоличествоОтветов = ОтветыНаЗапросыИОН.Количество();
	Если КоличествоОтветов = 0 Тогда
		Возврат;
	ИначеЕсли КоличествоОтветов > 1 Тогда
		СписокВыбора = Новый СписокЗначений;
		Для Каждого СтрокаОтвета Из ОтветыНаЗапросыИОН Цикл 
			СписокВыбора.Добавить(СтрокаОтвета.ИмяФайла);
		КонецЦикла;
		СписокВыбора.ЗаполнитьПометки(Истина);
		ДополнительныеПараметры = Новый Структура("Запрос", Запрос);
		ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьОтветНаЗапросИОНЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		СписокВыбора.ПоказатьОтметкуЭлементов(ОписаниеОповещения);
	Иначе
		ПоказатьОтветНаЗапросИОННаКлиенте(Запрос);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОНЗавершение(СписокВыбора, ДополнительныеПараметры) Экспорт
	
	Запрос = ДополнительныеПараметры.Запрос;
	
	Если СписокВыбора = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// удаляем не помеченные строки из таблицы ответов
	Для Каждого СтрСписокВыбора Из СписокВыбора Цикл
		Если НЕ СтрСписокВыбора.Пометка Тогда
			СтрОтветНаЗапросИОН = ОтветыНаЗапросыИОН.НайтиСтроки(Новый Структура("ИмяФайла", СтрСписокВыбора.Значение));
			ОтветыНаЗапросыИОН.Удалить(СтрОтветНаЗапросИОН[0]);
		КонецЕсли;
	КонецЦикла;
	
	ПоказатьОтветНаЗапросИОННаКлиенте(Запрос);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОННаКлиенте(Запрос)
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОтметитьКакПрочтенное(Запрос);
	
	ОтветНаЗапросИОН = ОтветыНаЗапросыИОН[0];
	
	Если ОтветНаЗапросИОН.ВАрхиве Тогда
		
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3);

		Возврат;
	КонецЕсли;
	
	ЭтоPDF = ЭтоPDFФайл(ОтветНаЗапросИОН.Адрес, ОтветНаЗапросИОН.ИмяФайла);
	
	Если ОтветНаЗапросИОН.ТипСодержимого = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_xml
		И ТребуетсяОсобыйСпособОтображения(ОтветНаЗапросИОН.ИмяФайла, ОтветНаЗапросИОН.ТипСодержимого)
		И НЕ ЭтоPDF Тогда
		
		ПоказатьКакТабличныйДокумент(Запрос, ОтветНаЗапросИОН.ИмяФайла, ОтветНаЗапросИОН.Адрес);
		
	ИначеЕсли ОтветНаЗапросИОН.ТипСодержимого = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_xml
		И НЕ ЭтоВебКлиент() 
		И НЕ ОбщегоНазначенияКлиент.ЭтоLinuxКлиент() 
		И НЕ ОбщегоНазначенияКлиент.ЭтоMacOSКлиент()
		И НЕ ЭтоPDF Тогда
		
		ПоказатьHTML(ОтветНаЗапросИОН.ИмяФайла, ОтветНаЗапросИОН.Адрес, Ложь, Истина);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОН.Адрес, ОтветНаЗапросИОН.ИмяФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьФайлЗапросаИОН(Запрос, ДвоичныеДанныеФайлаЗапроса, ИмяФайлаЗапроса, УникальныйИдентификатор = Неопределено)
	
	ЭтоФормат5_07  = ДокументооборотСКОНаВремяВызоваПовтИсп.ЭтоФорматЗапросаИОН5_07();
	ЭтоПриложение2Или4 = Документы.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика.ЭтоПриложение2Или4СправкиОНаличииСальдо(Запрос);

	// собираем параметры письма
	ПараметрыПисьма = СформироватьПараметрыЗапроса(Запрос);
	Если НЕ ПроверитьЗаполнениеЗапроса535(Запрос, ПараметрыПисьма) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось сформировать запрос на информационное обслуживание!'"));
		Возврат Ложь;
	КонецЕсли;
	
	// формируем идентификатор отправителя
	Если ПараметрыПисьма.ПрПодп = 2 Тогда
		СвПред = ПараметрыПисьма.Доверенность.СвУпПред.СвПред;
		Если СвПред.Свойство("СвОрг") Тогда
			ИННКПП = СокрЛП(СвПред.СвОрг.ИННЮЛ) + СокрЛП(СвПред.СвОрг.КПП);
		Иначе
			Если НЕ ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННКПП = СокрЛП(СвПред.СведФизЛ.ИННФЛ);
			ИначеЕсли ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации
				И ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.Свойство("ЯвляетсяСотрудникомОрганизацииПоМЧД")
				И ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизацииПоМЧД = Ложь
				И ДокументооборотСКОКлиентСервер.ПараметрыОтправкиПоМЧДФНС("ИзменятьФайлВыгрузкиДляФизическогоЛица") Тогда
				ИННКПП = ?(ПараметрыПисьма.Доверенность.СвПредст.ЯвляетсяФЛ, СокрЛП(ПараметрыПисьма.Доверенность.СвПредст.ИННФЛ),
					СокрЛП(ПараметрыПисьма.Доверенность.СвПредст.ИННЮЛ) + СокрЛП(ПараметрыПисьма.КППЮЛ));
			Иначе
				ИННКПП = ?(ПараметрыПисьма.ЭтоПБОЮЛ, СокрЛП(ПараметрыПисьма.ИННФЛ), СокрЛП(ПараметрыПисьма.ИННЮЛ) + СокрЛП(ПараметрыПисьма.КППЮЛ));
			КонецЕсли;
		КонецЕсли;
	Иначе
		ИННКПП = ?(ПараметрыПисьма.ЭтоПБОЮЛ, СокрЛП(ПараметрыПисьма.ИННФЛ), СокрЛП(ПараметрыПисьма.ИННЮЛ) + СокрЛП(ПараметрыПисьма.КППЮЛ));
	КонецЕсли;
	
	// формируем код получателя
	Если НЕ ЗначениеЗаполнено(Запрос.Получатель) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось определить получателя запроса!'"));
		Возврат Ложь;	
	Иначе
		КодИФНСПолучателя = СокрЛП(Запрос.Получатель.Код);
		КодНалоговогоОргана = КодИФНСПолучателя;
	КонецЕсли;
	
	// инициализируем служебные соответствия
	ВидыУслугИОН = Перечисления.ВидыУслугПриИОН;
	
	СоответствиеВидаУслугиКоду = Новый Соответствие;
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.ПредставлениеПеречняБухгалтерскойИНалоговойОтчетности, "3");
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.ПредставлениеАктовСверкиРасчетов, "4");
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате, "5");
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.СправкаОПринадлежностиСумм, "6");
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.СправкаОНаличииСальдо, "7");
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.СведенияОбОтрицательномСальдоЕНС, "8");
	СоответствиеВидаУслугиКоду.Вставить(ВидыУслугИОН.СправкаОПринадлежностиСуммАгрегированныеДанные, "9");
	
	СоответствиеФорматовОтветовИдентификатору = Новый Соответствие;
	СоответствиеФорматовОтветовИдентификатору.Вставить(Перечисления.ФорматОтветаНаЗапросИОН.RTF, "1");
	СоответствиеФорматовОтветовИдентификатору.Вставить(Перечисления.ФорматОтветаНаЗапросИОН.XML, "2");
	СоответствиеФорматовОтветовИдентификатору.Вставить(Перечисления.ФорматОтветаНаЗапросИОН.XLS, "3");
	СоответствиеФорматовОтветовИдентификатору.Вставить(Перечисления.ФорматОтветаНаЗапросИОН.PDF, "4");
	
	ЭтоКрупнейший    = ДокументооборотСКО.ЭтоКрупнейший(Запрос.Организация);
	КодНОКрупнейшего = ДокументооборотСКО.КодНОКрупнейшего(Запрос.Организация);

	ИмяФайлаБезРасширения = "IU_ZAPR_"
							 + ?(ЭтоКрупнейший И ЗначениеЗаполнено(КодНОКрупнейшего), СокрЛП(КодНОКрупнейшего), СокрЛП(КодНалоговогоОргана))
							 + "_"
							 + СокрЛП(КодИФНСПолучателя)
							 + "_"
							 + ИННКПП
							 + "_"
							 + Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd")
							 + "_"
							 + Строка(Новый УникальныйИдентификатор);
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	УзелФайл = ДобавитьУзелXML(Дерево, "Файл");
	ДобавитьУзелXML(УзелФайл, "ИдФайл", Истина, XMLСтрока(ИмяФайлаБезРасширения));
	ДобавитьУзелXML(УзелФайл, "ВерсПрог", Истина, Лев(XMLСтрока(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы()), 40));
	
	Если ЭтоФормат5_07 Тогда
		ДобавитьУзелXML(УзелФайл, "ВерсФорм", Истина, XMLСтрока("5.07"));
	Иначе
	ДобавитьУзелXML(УзелФайл, "ВерсФорм", Истина, XMLСтрока("5.06"));
	КонецЕсли;
	
	УзелДокумент = ДобавитьУзелXML(УзелФайл, "Документ");
	
		ДобавитьУзелXML(УзелДокумент, "КНД", Истина, XMLСтрока("1166101"));
		ДобавитьУзелXML(УзелДокумент, "ДатаДок", Истина, XMLСтрока(Формат(ТекущаяДатаСеанса(), "ДФ=dd.MM.yyyy")));
		УзелСвНП = ДобавитьУзелXML(УзелДокумент, "СвНП");
			
		Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
			
			УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "НПЮЛ");
			
				ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.НаимЮЛПол)), 1000));
				ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННЮЛ)));
				ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.КППЮЛ)));
				
		Иначе
			
			УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "НПФЛ");
			
				ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННФЛ)), Истина);
				УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
				
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
								
		КонецЕсли;
		
		УзелСвНП = ДобавитьУзелXML(УзелДокумент, "СвОтпр");
		Если ПараметрыПисьма.ПрПодп = 1 Тогда // отправитель - налогоплательщик
			Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
				УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрЮЛ");
					ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.НаимЮЛПол)), 1000));
					ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННЮЛ)));
					ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.КППЮЛ)));
			Иначе
				УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрФЛ");
					ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННФЛ)), Истина);
					УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
						ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
						ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
						ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
			КонецЕсли;
		Иначе
			СвДоверенность = ПараметрыПисьма.Доверенность;
			СвПредст = СвДоверенность.СвУпПред.СвПред;
			Если СвПредст.Свойство("СвОрг") Тогда // представитель - ЮЛ
				УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрЮЛ");
				ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(СокрЛП(СвПредст.СвОрг.НаимОрг)), 1000));
					ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(СокрЛП(СвПредст.СвОрг.ИННЮЛ)));
					ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(СокрЛП(СвПредст.СвОрг.КПП)));
			Иначе
				Если СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
					Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
						УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрЮЛ");
						ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.НаимЮЛПол)), 1000));
						ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННЮЛ)));
						ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.КППЮЛ)));	
					Иначе
						УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрФЛ");
						ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыПисьма.ИННФЛ)), Истина);
						УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
						ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
						ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
						ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
					КонецЕсли;
				Иначе	
					УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "СвОтпрФЛ");
					ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(СвПредст.СведФизЛ.ИННФЛ)), Истина);
					ФИОПредставителя = СвПредст.СведФизЛ.ФИО;
					УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Фамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Имя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Отчество)), 60), Истина);
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		УзелПодписант = ДобавитьУзелXML(УзелДокумент, "Подписант");
		
			ДобавитьУзелXML(УзелПодписант, "ПрПодп", Истина, ПараметрыПисьма.ПрПодп);
			УзелФИО = ДобавитьУзелXML(УзелПодписант, "ФИО");
			Если ПараметрыПисьма.ПрПодп = 1 Тогда
				Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ Тогда
					ФИОПодписанта = РегламентированнаяОтчетность.РазложитьФИО(ПараметрыПисьма.ФИОРук);
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Фамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Имя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Отчество)), 60), Истина);
				Иначе
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПФамилия)), 60));
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПИмя)), 60));
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыПисьма.ИПОтчество)), 60), Истина);
				КонецЕсли;
			Иначе
				ФИОПодписанта = ДокументооборотСКОВызовСервера.УточнитьФИОПредставителя(
					ПараметрыПисьма.Доверенность.СвУпПред.СвПред.СведФизЛ.ФИО,
					ПараметрыПисьма.Доверенность.ДанныеПредставителя);
				ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Фамилия)), 60));
				ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Имя)), 60));
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Отчество)), 60), Истина);

				Если ЭтоФормат5_07 Тогда
					УзелСвПред = ДобавитьУзелXML(УзелПодписант, "СвПред");
						НаимДок = ПараметрыПисьма.Доверенность.НаимДок;
						ДобавитьУзелXML(УзелСвПред, "НаимДок", Истина, Лев(XMLСтрока(СокрЛП(НаимДок)), 120));
				КонецЕсли;

			КонецЕсли;
		
		УзелЗапрИнфУсл = ДобавитьУзелXML(УзелДокумент, "ЗапрИнфУсл");
		
			ДобавитьУзелXML(УзелЗапрИнфУсл, "ИФНС", Истина, XMLСтрока(КодИФНСПолучателя));
			ДобавитьУзелXML(УзелЗапрИнфУсл, "КодЗапр", Истина, СоответствиеВидаУслугиКоду[Запрос.ВидУслуги]);
			ДобавитьУзелXML(УзелЗапрИнфУсл, "ФормОтв", Истина, СоответствиеФорматовОтветовИдентификатору[Запрос.ФорматОтвета]);
			
			ПериодОбязателен = ДокументооборотСКОКлиентСервер.ОбязателенПериодВЗапросеИОН(Запрос);
			
			Если ПериодОбязателен Тогда
				УзелПериод = ДобавитьУзелXML(УзелЗапрИнфУсл, "Период");
					ДобавитьУзелXML(УзелПериод, "ДатаНач", Истина, XMLСтрока(Формат(Запрос.ДатаНачалаПериода, "ДФ=dd.MM.yyyy")));
					ДобавитьУзелXML(УзелПериод, "ДатаКон", Истина, XMLСтрока(Формат(Запрос.ДатаОкончанияПериода, "ДФ=dd.MM.yyyy")));
			КонецЕсли;
			
			Если Запрос.ВидУслуги = Перечисления.ВидыУслугПриИОН.СправкаОНаличииСальдо
				И (Запрос.ДеталСвед ИЛИ Запрос.ИнфПрНач ИЛИ Запрос.РасчСуммПени ИЛИ Запрос.ДеталНачСумм) Тогда
				
				УзелДеталСпр = ДобавитьУзелXML(УзелЗапрИнфУсл, "ДеталСпр");
					ДобавитьУзелXML(УзелДеталСпр, "ДеталСвед", Истина, ?(Запрос.ДеталСвед, "1","0"));
					ДобавитьУзелXML(УзелДеталСпр, "ИнфПрНач", Истина, ?(Запрос.ИнфПрНач, "1","0"));
					ДобавитьУзелXML(УзелДеталСпр, "РасчСуммПени", Истина, ?(Запрос.РасчСуммПени, "1","0"));
					
					УзелДеталНачСумм = ДобавитьУзелXML(УзелДеталСпр, "ДеталНачСумм", Истина, ?(Запрос.ДеталНачСумм, "1","0"));
					Если Запрос.ДеталНачСумм Тогда
						УзелСвДеталСум = ДобавитьУзелXML(УзелДеталСпр, "СвДеталСум");
						ДобавитьУзелXML(УзелСвДеталСум, "ПрПредДетал", Истина, Строка(Запрос.ПрПредДетал));
						ДобавитьУзелXML(УзелСвДеталСум, "Квартал", Истина, Строка(Квартал(Запрос.ДатаНачалаПериода)));
						ДобавитьУзелXML(УзелСвДеталСум, "Год", Истина, Формат(Год(Запрос.ДатаНачалаПериода), "ЧГ=0; ДФ=yyyy"));
					КонецЕсли;
				
			КонецЕсли;
			
			ЭтоАкт = Запрос.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеАктовСверкиРасчетов;
			Если ЭтоАкт ИЛИ ЭтоПриложение2Или4 Тогда
				УзелЗапрКБК = ДобавитьУзелXML(УзелЗапрИнфУсл, "ЗапрКБК");
				
				Если Запрос.КоличествоНалогов = Перечисления.КоличествоЭлементовДляСверкиИОН.Все Тогда
					ДобавитьУзелXML(УзелЗапрКБК, "ПрКБК", Истина, "1");
				Иначе
					ДобавитьУзелXML(УзелЗапрКБК, "ПрКБК", Истина, "2");
					УзелПеречКБК = ДобавитьУзелXML(УзелЗапрКБК, "ПеречКБК");
					Для Каждого Стр Из Запрос.ЗапрашиваемыеНалоги Цикл
						ДобавитьУзелXML(УзелПеречКБК, "КБК", , XMLСтрока(Стр.КБК));
					КонецЦикла;
				КонецЕсли;
			КонецЕсли;
			
			Если НЕ ПараметрыПисьма.ЭтоПБОЮЛ И (ЭтоАкт ИЛИ ЭтоПриложение2Или4) Тогда
				
				УзелЗапрКПП = ДобавитьУзелXML(УзелЗапрИнфУсл, "ЗапрКПП");
				
				Если Запрос.КоличествоКПП = Перечисления.КоличествоЭлементовДляСверкиИОН.Все Тогда
					ДобавитьУзелXML(УзелЗапрКПП, "ПрКПП", Истина, "1");
				Иначе
					ДобавитьУзелXML(УзелЗапрКПП, "ПрКПП", Истина, "2");
					УзелПеречКПП = ДобавитьУзелXML(УзелЗапрКПП, "ПеречКПП");
					Для Каждого Стр Из Запрос.ПереченьКПП Цикл
						ДобавитьУзелXML(УзелПеречКПП, "КПП", , XMLСтрока(Стр.КПП));
					КонецЦикла;
				КонецЕсли;
				
			КонецЕсли;
			
			Если ЭтоПриложение2Или4 Тогда
				
				УзелЗапрОКТМО = ДобавитьУзелXML(УзелЗапрИнфУсл, "ЗапрОКТМО");
				
				Если Запрос.КоличествоОКТМО = Перечисления.КоличествоЭлементовДляСверкиИОН.Все Тогда
					ДобавитьУзелXML(УзелЗапрОКТМО, "ПрОКТМО", Истина, "1");
				Иначе
					ДобавитьУзелXML(УзелЗапрОКТМО, "ПрОКТМО", Истина, "2");
					УзелПеречОКТМО = ДобавитьУзелXML(УзелЗапрОКТМО, "ПеречОКТМО");
					Для Каждого Стр Из Запрос.ПереченьОКТМО Цикл
						ДобавитьУзелXML(УзелПеречОКТМО, "ОКТМО", , XMLСтрока(Стр.ОКТМО));
					КонецЦикла;
				КонецЕсли;
				
			КонецЕсли;
		
	// сохранение дерева XML в файл и двоичные данные
	ТекстВыгрузкиXML = ЗаписатьДеревоВXML(Дерево);
	
	// записываем XML запроса во временный файл
	ВремФайл = ПолучитьИмяВременногоФайла();
	Если НЕ ЗаписатьXMLВФайл(ТекстВыгрузкиXML, ВремФайл) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось записать текст запроса во временный файл!'"));
		Возврат Ложь;
	КонецЕсли;
	
	// считываем двоичные данные файла запроса, после чего удаляем файл
	//@skip-check 1c-fresh-audit
	ДвоичныеДанныеФайлаЗапроса = Новый ДвоичныеДанные(ВремФайл);
	ДвоичныеДанныеФайлаЗапроса = ПоместитьВоВременноеХранилище(
		ДвоичныеДанныеФайлаЗапроса, УникальныйИдентификатор);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
	
	// формируем имя файла запроса
	ИмяФайлаЗапроса = ИмяФайлаБезРасширения + ".xml";
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция Квартал(Дата) Экспорт
	
	Возврат Цел((Месяц(Дата) - 1) / 3) + 1;

КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьЗаполнениеЗапроса535(ДокЗапрос, Параметры)
	
	СтатусПроверки = Истина;
	
	Если Параметры = Неопределено Тогда
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДокЗапрос.Организация) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задана организация!'"));
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДокЗапрос.Получатель)  Тогда 
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан получатель!'"));
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	КонецЕсли;
	
	// проверка сведений об отправителе
	Если Параметры.ПрПодп = 1 Тогда // отправитель - налогоплательщик
		Если Параметры.ЭтоПБОЮЛ Тогда
			Если НЕ ЗначениеЗаполнено(Параметры.ИННФЛ) Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан ИНН индивидуального предпринимателя!'"));
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЕсли;
	Иначе
		СвПредст = Параметры.Доверенность.СвУпПред.СвПред;
		Если НЕ СвПредст.Свойство("СвОрг") Тогда
			Если НЕ СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННФЛ = СвПредст.СведФизЛ.ИННФЛ;
			Иначе
				ИННФЛ = ?(Параметры.ЭтоПБОЮЛ, Параметры.ИННФЛ, Параметры.ИННЮЛ);
			КонецЕсли;
			Если НЕ ЗначениеЗаполнено(ИННФЛ) Тогда
				Если НЕ СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации ИЛИ Параметры.ЭтоПБОЮЛ Тогда
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан ИНН физлица-представителя!'"));
				Иначе
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан ИНН организации физлица-представителя!'"));
				КонецЕсли;
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
		
	Если НЕ ЗначениеЗаполнено(Параметры.КодНО) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан налоговый орган!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если СтрДлина(Параметры.КодНО) <> 4 ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(Параметры.КодНО) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Код налогового органа должен состоять из четырех цифр!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	ЭтоСправка = ДокЗапрос.ВидУслуги = Перечисления.ВидыУслугПриИОН.СправкаОНаличииСальдо;

	Если НЕ ЗначениеЗаполнено(ДокЗапрос.ДатаНачалаПериода) И НЕ ЭтоСправка Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задана дата начала периода!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДокЗапрос.ДатаОкончанияПериода) И НЕ ЭтоСправка Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задана дата окончания периода!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если ДокЗапрос.ДатаНачалаПериода > ДокЗапрос.ДатаОкончанияПериода Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Дата начала периода не может быть больше даты окончания!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДокЗапрос.ФорматОтвета) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Выберите один из типов файла ответа!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если Параметры.ЭтоПБОЮЛ Тогда
		Если ПустаяСтрока(Параметры.ИПФамилия) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось определить фамилию индивидуального предпринимателя!'"));
			СтатусПроверки = Ложь;
		КонецЕсли;
		Если ПустаяСтрока(Параметры.ИПИмя) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось определить имя индивидуального предпринимателя!'"));
			СтатусПроверки = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ДокЗапрос.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПустаяСсылка() Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не указан вид сверки!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Возврат СтатусПроверки;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ СОХРАНЕНИЯ ЗАПРОСА В ФАЙЛ НА ДИСК

&НаКлиенте
Процедура ВыгрузитьЗапросИОНВФайл(ДокЗапрос, УникальныйИдентификатор = Неопределено) Экспорт
	Перем Запрос, ИмяФайлаЗапроса;
	
	Если СформироватьФайлЗапросаИОН(ДокЗапрос, Запрос, ИмяФайлаЗапроса, УникальныйИдентификатор) Тогда
		ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(Новый Структура("Адрес, Имя", Запрос, ИмяФайлаЗапроса));
	КонецЕсли;
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ФОРМИРОВАНИЯ ЗАПРОСА НА ИНФОРМАЦИОННОЕ ОБСЛУЖИВАНИЕ

&НаСервереБезКонтекста
Функция СформироватьПараметрыЗапроса(Запрос)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПараметрыЗапроса(Запрос);
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////

#Область ОтправкаЗапросаНаИнформационноеОбслуживание

&НаКлиенте
Процедура ОтправкаЗапросаНаИнформационноеОбслуживание(Док, Организация, ВыполняемоеОповещение) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Док) Тогда
		Возврат;
	КонецЕсли;
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док",                   Док);
	КонтекстДлительнойОперации.Вставить("Организация",           Организация);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаИнформационноеОбслуживание_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(Док, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаИнформационноеОбслуживание_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка сверки в ФНС
		Возврат;
	КонецЕсли;
		
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаЗапросаНаИнформационноеОбслуживание",  0.1, Истина);

КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаЗапросаНаИнформационноеОбслуживание() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Док 		= КонтекстДлительнойОперации.Док;
	Контекст 	= ОбщегоНазначенияКлиент.СкопироватьРекурсивно(КонтекстДлительнойОперации);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаИнформационноеОбслуживаниеПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		Контекст);
		
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаИнформационноеОбслуживание_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ОтправкаЗапросаНаИнформационноеОбслуживание(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаИнформационноеОбслуживаниеПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаИнформационноеОбслуживание_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.Док, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаИнформационноеОбслуживание_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");

	// отражаем изменения в базе
	Если НЕ ОтправкаЗапросаНаИнформационноеОбслуживание_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаЗапросаНаИнформационноеОбслуживание_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док);//+
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Док);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		КонецЕсли;
		
		Возврат;
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док);//+
		Возврат;
		
	КонецЕсли;
	
	ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(Док, "ВидУслуги, ТипСверки");
	ВидУслуги = ЗначенияРеквизитов.ВидУслуги;
	ТипСверки = ЗначенияРеквизитов.ТипСверки;
	
	Если ВидУслуги <> ПредопределенноеЗначение("Перечисление.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате")
		И ТипСверки <> ПредопределенноеЗначение("Перечисление.ТипыСверокИОН.ВЦеломПоОрганизации") Тогда
		// У данного вида сверки указывается получатель 0000, поэтому по нему проверять не нужно.
		Если НЕ ДокументооборотСКОВызовСервера.ОрганизацияПодключенаКИнспекции(Док)
			ИЛИ НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаИнформационноеОбслуживаниеПослеФормированияПакета", ЭтотОбъект, Контекст);
		
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС");
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаИнформационноеОбслуживаниеПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФНС
	
КонецПроцедуры

&НаСервереБезКонтекста

&НаСервереБезКонтекста
Функция ОтправкаЗапросаНаИнформационноеОбслуживание_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Док = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// находим налоговый орган по коду
	КодНалоговогоОрганаКонечногоПолучателяДокумента = СокрЛП(Док.Получатель.Код);
	КодНалоговогоОргана = КодНалоговогоОрганаКонечногоПолучателяДокумента;
	
	Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) 
		И (Док.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате
		ИЛИ Док.ТипСверки = Перечисления.ТипыСверокИОН.ВЦеломПоОрганизации) Тогда
		
		ЭтоКрупнейший    = ДокументооборотСКО.ЭтоКрупнейший(Док.Организация);
		КодНОКрупнейшего = ДокументооборотСКО.КодНОКрупнейшего(Док.Организация);
		КодНалоговогоОрганаКонечногоПолучателяДокумента = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Док.Организация, , "КодНО").КодНО);
		Если ЭтоКрупнейший И ЗначениеЗаполнено(КодНОКрупнейшего) Тогда
			КодНалоговогоОргана = СокрЛП(КодНОКрупнейшего);
		Иначе
			КодНалоговогоОргана = КодНалоговогоОрганаКонечногоПолучателяДокумента;
		КонецЕсли;
		
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан налоговый орган'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	НалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНалоговогоОргана);
	Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не найден налоговый орган с кодом %1 в справочнике ""Налоговые органы""!'"), КодНалоговогоОргана);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, указан ли получатель
	ПолучательДокумента = Док.Получатель;
	Если НЕ ЗначениеЗаполнено(ПолучательДокумента) 
		И Док.ВидУслуги <> Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате
		И Док.ТипСверки <> Перечисления.ТипыСверокИОН.ВЦеломПоОрганизации Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель письма.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ДвоичныеДанныеФайлаЗапроса = Неопределено;
	ИмяФайлаЗапроса = Неопределено;
	РезультатФормированияФайлаЗапроса = СформироватьФайлЗапросаИОН(Док, ДвоичныеДанныеФайлаЗапроса, ИмяФайлаЗапроса);
	Если НЕ РезультатФормированияФайлаЗапроса Тогда
		Возврат Ложь;
	КонецЕсли;
	
	КороткоеИмяФайлаПисьма = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа
	ЭлРегистрацияВИФНС = Док.Получатель;
	Если НЕ ЗначениеЗаполнено(ЭлРегистрацияВИФНС) 
		И (Док.ВидУслуги = Перечисления.ВидыУслугПриИОН.ПредставлениеСправкиОбИсполненииОбязанностейПоУплате
		ИЛИ Док.ТипСверки = Перечисления.ТипыСверокИОН.ВЦеломПоОрганизации) Тогда
		
		ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Док.Организация, КодНалоговогоОрганаКонечногоПолучателяДокумента);
		КодНалоговогоОрганаКонечногоПолучателя = КодНалоговогоОрганаКонечногоПолучателяДокумента;
		
	Иначе
		
		КодНалоговогоОрганаКонечногоПолучателя = Неопределено; // определять по регистрации в ИФНС
		
	КонецЕсли;
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		
		// формируем файл доверенности
		ЭтоФормат5_07  = ДокументооборотСКОНаВремяВызоваПовтИсп.ЭтоФорматЗапросаИОН5_07();
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(ЭлРегистрацияВИФНС, КороткоеИмяФайлаДоверенности, Организация, КодНалоговогоОрганаКонечногоПолучателя,,ЭтоФормат5_07);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
		
		//подменять имя файла при отправке через уполномоченного представителя не требуется
		
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.Предмет = Док;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = НалоговыйОрган;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОН;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.ДатаНачалаПериода = Док.ДатаНачалаПериода;
	ЦиклОбмена.ДатаОкончанияПериода = Док.ДатаОкончанияПериода;
	ЦиклОбмена.ВидУслуги = Док.ВидУслуги;
	ЦиклОбмена.ФорматОтвета = Док.ФорматОтвета;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ЗапросНП;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = НалоговыйОрган;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем запрос
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Запрос, ДвоичныеДанныеФайлаЗапроса, ИмяФайлаЗапроса, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, ФайлДоверенности, КороткоеИмяФайлаДоверенности, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Док, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция УдалитьУзлыКромеАвтонастройки(ДеревоНастроекАдрес, ВыбранныйАдрес);
	
	Если ЭтоАдресВременногоХранилища(ДеревоНастроекАдрес) Тогда 
		ДеревоНастроек = ПолучитьИзВременногоХранилища(ДеревоНастроекАдрес);
	КонецЕсли;
	
	СтрокиСАдресамиУчетныхЗаписей = ДеревоНастроек.Строки.НайтиСтроки(Новый Структура("Параметр", "emailabsos"), Истина);

	// Получаем ИскомыйУзелAbsos
	ИскомыйУзелAbsos = Неопределено;
	Для Каждого СтрокаСАдресомУчетнойЗаписи Из СтрокиСАдресамиУчетныхЗаписей Цикл
		Если СтрокаСАдресомУчетнойЗаписи.Значение = ВыбранныйАдрес Тогда 
			ИскомыйУзелAbsos = СтрокаСАдресомУчетнойЗаписи.Родитель;
		КонецЕсли;
	КонецЦикла;
	
	// Удаляем лишние строки
	Для Каждого СтрокаСАдресомУчетнойЗаписи Из СтрокиСАдресамиУчетныхЗаписей Цикл
		УзелAbsos = СтрокаСАдресомУчетнойЗаписи.Родитель;
		Если СтрокаСАдресомУчетнойЗаписи.Родитель <> ИскомыйУзелAbsos Тогда
			УзелAbsos.Родитель.Строки.Удалить(УзелAbsos);
		КонецЕсли;
	КонецЦикла;
	
	ПоместитьВоВременноеХранилище(ДеревоНастроек, ДеревоНастроекАдрес);
	
КонецФункции


&НаСервереБезКонтекста
Функция КоличествоСтрокСАдресамиУчетныхЗаписей(ДеревоНастроекАдрес)
	
	Если ЭтоАдресВременногоХранилища(ДеревоНастроекАдрес) Тогда 
		ДеревоНастроек = ПолучитьИзВременногоХранилища(ДеревоНастроекАдрес);
	КонецЕсли;
	// находим все узлы с адресами учетных записей абонента
	СтрокиСАдресамиУчетныхЗаписей = ДеревоНастроек.Строки.НайтиСтроки(Новый Структура("Параметр", "emailabsos"), Истина);
	Возврат СтрокиСАдресамиУчетныхЗаписей.Количество();
	
КонецФункции

// ищем среди узлов адрес настраиваемой учетной записи
&НаСервереБезКонтекста
Функция НайтиАдресНастраиваемойУчетнойЗаписи(ДеревоНастроекАдрес, АдресУчетнойЗаписи)
	
	Если ЭтоАдресВременногоХранилища(ДеревоНастроекАдрес) Тогда 
		ДеревоНастроек = ПолучитьИзВременногоХранилища(ДеревоНастроекАдрес);
	КонецЕсли;
	СтрокиСАдресамиУчетныхЗаписей = ДеревоНастроек.Строки.НайтиСтроки(Новый Структура("Параметр", "emailabsos"), Истина);
	ИскомыйУзелAbsos = Неопределено;
	Для Каждого СтрокаСАдресомУчетнойЗаписи Из СтрокиСАдресамиУчетныхЗаписей Цикл
		Если СтрокаСАдресомУчетнойЗаписи.Значение = АдресУчетнойЗаписи Тогда
			ИскомыйУзелAbsos = СтрокаСАдресомУчетнойЗаписи.Родитель;
		КонецЕсли;
	КонецЦикла;
	
	// если узел не найден, то предложим пользователю выбрать из нескольких узлов
	Если ИскомыйУзелAbsos = Неопределено Тогда
		// формируем список выбора
		СписокВыбора = Новый СписокЗначений;
		Для Каждого СтрокаСАдресомУчетнойЗаписи Из СтрокиСАдресамиУчетныхЗаписей Цикл
			СписокВыбора.Добавить(СтрокаСАдресомУчетнойЗаписи.Значение);
		КонецЦикла;
		Возврат СписокВыбора;
	Иначе
		УдалитьУзлыКромеАвтонастройки(ДеревоНастроекАдрес, АдресУчетнойЗаписи);
		Возврат АдресУчетнойЗаписи;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ОтфильтроватьДеревоНастроекПоУчетнойЗаписи(ДеревоНастроекАдрес, АдресУчетнойЗаписи, ВыполняемоеОповещение)
	
	Если КоличествоСтрокСАдресамиУчетныхЗаписей(ДеревоНастроекАдрес) < 2 Тогда
		Результат = Новый Структура("ДеревоНастроекАдрес, РезультатФильтрации", ДеревоНастроекАдрес, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// ищем среди узлов адрес настраиваемой учетной записи
	ИскомыйУзелAbsos = НайтиАдресНастраиваемойУчетнойЗаписи(ДеревоНастроекАдрес, АдресУчетнойЗаписи);
	
	// если узел не найден, то предложим пользователю выбрать из нескольких узлов
	Если ТипЗнч(ИскомыйУзелAbsos) = Тип("СписокЗначений") Тогда
		
		СписокВыбора = ИскомыйУзелAbsos;
		// предлагаем выбрать
		ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ДеревоНастроекАдрес", ВыполняемоеОповещение, ДеревоНастроекАдрес);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОтфильтроватьДеревоНастроекПоУчетнойЗаписиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		СписокВыбора.ПоказатьВыборЭлемента(ОписаниеОповещения, "Выберите одну из учетных записей");
	Иначе
		Результат = Новый Структура("ДеревоНастроекАдрес, АдресУчетнойЗаписи, РезультатФильтрации", ДеревоНастроекАдрес, АдресУчетнойЗаписи, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтфильтроватьДеревоНастроекПоУчетнойЗаписиЗавершение(РезультатВыбора, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ДеревоНастроекАдрес = ДополнительныеПараметры.ДеревоНастроекАдрес;
	
	Если РезультатВыбора = Неопределено Тогда
		Результат = Новый Структура("ДеревоНастроекАдрес, РезультатФильтрации", ДеревоНастроекАдрес, Ложь);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	Иначе
		// удаляем все узлы absos, кроме того, по которому будем автонастраивать
		УдалитьУзлыКромеАвтонастройки(ДеревоНастроекАдрес, РезультатВыбора.Значение);
		
		Результат = Новый Структура("ДеревоНастроекАдрес, РезультатФильтрации", ДеревоНастроекАдрес, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьТаблицуИсправленийНаОсновеФайлаНастроек(
		ФайлНастроек,
		УведомлятьТолькоОбОшибках,
		ВыполняемоеОповещение,
		УчетнаяЗапись = Неопределено,
		Организация = Неопределено) Экспорт
	
	Сообщения = СоздатьТаблицуСообщений(Истина);
	ФормаПрогресса = Неопределено;
	
	Попытка
		АдресФайлаНастроек = ФайлНастроек;
		ДеревоНастроек = ВосстановитьXMLВДерево(АдресФайлаНастроек, Сообщения);
		РезультатЗаполнения = Истина;
	Исключение
		РезультатЗаполнения = Ложь;
	КонецПопытки;
	Если НЕ РезультатЗаполнения ИЛИ НЕ ЗначениеЗаполнено(ДеревоНастроек) ИЛИ ДеревоНастроек.КоличествоСтрок = 0 Тогда
		ВывестиСообщения(Неопределено, Сообщения, УведомлятьТолькоОбОшибках);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	// определяем адрес электронной почты настраиваемой учетной записи
	Если УчетнаяЗапись = Неопределено Тогда
		АдресУчетнойЗаписи = "";
	Иначе
		АдресУчетнойЗаписи = СокрЛП(ПолучитьРеквизитНаСервере(УчетнаяЗапись, "АдресЭлектроннойПочты"));
	КонецЕсли;
	
	// оставляем только тот узел дерева, который соответствует настраиваемой учетке
	// если настраиваемую учетку определить не удалось, то предлагаем пользователю выбрать
	ДеревоНастроек = ДеревоНастроек.АдресДереваЗначений;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Сообщения", 					Сообщения);
	ДополнительныеПараметры.Вставить("УведомлятьТолькоОбОшибках", 	УведомлятьТолькоОбОшибках);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", 				УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("Организация", 				Организация);
	ОписаниеОповещения = Новый ОписаниеОповещения("СформироватьТаблицуИсправленийНаОсновеФайлаНастроекПослеФильтрацииДереваНастроекПоУчетнойЗаписиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОтфильтроватьДеревоНастроекПоУчетнойЗаписи(ДеревоНастроек, АдресУчетнойЗаписи, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьТаблицуИсправленийНаОсновеФайлаНастроекПослеФильтрацииДереваНастроекПоУчетнойЗаписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	Сообщения 					= ДополнительныеПараметры.Сообщения;
	УведомлятьТолькоОбОшибках 	= ДополнительныеПараметры.УведомлятьТолькоОбОшибках;
	УчетнаяЗапись 				= ДополнительныеПараметры.УчетнаяЗапись;
	Организация 				= ДополнительныеПараметры.Организация;
	
	ДеревоНастроек = Результат.ДеревоНастроекАдрес;
	РезультатФильтрации = Результат.РезультатФильтрации;
	
	Если НЕ РезультатФильтрации Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	// формируем "плоский список" настроек
	НастройкиРОКИ = СформироватьСписокНастроекРОКИПоДеревуНастроекXML(ДеревоНастроек, Сообщения);
	Если НЕ ЗначениеЗаполнено(НастройкиРОКИ) Тогда
		ВывестиСообщения(Неопределено, Сообщения, УведомлятьТолькоОбОшибках);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	НастройкиРОКИ.Вставить("СертификатБезопасности", ПолучитьОтпечатокСертификатаБезопасностиРОКИ());
	
	// получаем настройки учетной записи
	НастройкиУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
	НастройкиУчетнойЗаписи.Вставить("УчетнаяЗапись", 	УчетнаяЗапись);
	НастройкиУчетнойЗаписи.Вставить("Организация", 		Организация);
	
	// формируем таблицу исправлений
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, Сообщения, УведомлятьТолькоОбОшибках", ВыполняемоеОповещение, Сообщения, УведомлятьТолькоОбОшибках);
	Оповещение = Новый ОписаниеОповещения("СформироватьТаблицуИсправленийНаОсновеФайлаНастроекПослеПолученияТаблицыНеобходимыхИсправленийЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ПолучитьТаблицуНеобходимыхИсправлений(НастройкиУчетнойЗаписи, НастройкиРОКИ, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьТаблицуИсправленийНаОсновеФайлаНастроекПослеПолученияТаблицыНеобходимыхИсправленийЗавершение(ТаблицаИсправлений, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Сообщения = ДополнительныеПараметры.Сообщения;
	УведомлятьТолькоОбОшибках = ДополнительныеПараметры.УведомлятьТолькоОбОшибках;
	
	// выводим сообщения ,если они есть, и закрываем форму прогресса
	ВывестиСообщения(Неопределено, Сообщения, УведомлятьТолькоОбОшибках);
	
	// возвращаем таблицу с командами настройки
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ТаблицаИсправлений);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьКомментарийМакета()
	
	Возврат ДокументооборотСКО.ПолучитьОбработкуЭДО().Метаданные().Макеты.СертификатБезопасностиРОКИ.Комментарий;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьОтпечатокСертификатаБезопасностиРОКИ()
	
	// получаем отпечаток сертификата безопасности
	КомментарийМакета = ПолучитьКомментарийМакета();
	ВхождениеДвоеточия = СтрНайти(КомментарийМакета, ":");
	Если ВхождениеДвоеточия = 0 Тогда
		ОтпечатокСертификатаБезопасностиРОКИ = СокрЛП(КомментарийМакета);
	Иначе
		ОтпечатокСертификатаБезопасностиРОКИ = СокрЛП(Сред(КомментарийМакета, ВхождениеДвоеточия + 1));
	КонецЕсли;
	Возврат ОтпечатокСертификатаБезопасностиРОКИ;
	
КонецФункции

&НаКлиенте
Функция СертификатЗарегистрированВХранилище(Отпечаток, Хранилище, МассивСертификатов) Экспорт
	
	Результат = Неопределено;
	Результат = МассивСертификатов.Найти(нрег(СокрЛП(Отпечаток)));
	Возврат Результат <> Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ВывестиСообщения(УчетнаяЗапись, Знач Сообщения, УведомлятьТолькоОбОшибках)
	
	Если ЭтоАдресВременногоХранилища(Сообщения) Тогда
		Сообщения = ПолучитьИзВременногоХранилища(Сообщения);
	КонецЕсли;
	
	Если Сообщения.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	ПредставлениеУчетнаяЗапись = ?(ТипЗнч(УчетнаяЗапись) = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота"), " """ + УчетнаяЗапись + """", "");
	
	Если Сообщения.Найти(Истина, "Ошибка") <> Неопределено Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось произвести автоматическую настройку параметров учетной записи %1:'"),
			ПредставлениеУчетнаяЗапись);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	Иначе
		Если НЕ УведомлятьТолькоОбОшибках Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Автоматическая настройка параметров учетной записи %1 успешно завершена:'"),
				ПредставлениеУчетнаяЗапись);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
	КонецЕсли;
	
	Для Инд = 1 По Сообщения.Количество() Цикл
		ТекСтр = Сообщения[Инд - 1];
		Если УведомлятьТолькоОбОшибках И НЕ ТекСтр.Ошибка Тогда
			Продолжить;
		КонецЕсли;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекСтр.Сообщение);
	КонецЦикла;
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// Работа с XML

&НаСервере
Функция ВосстановитьXMLВДерево(ИмяФайла, Сообщения, Узел = Неопределено, ЧтениеXML = Неопределено)
	
	Если Узел = Неопределено Тогда
		
		ВерхнийУровень = Истина;
		Узел = Новый ДеревоЗначений;
		Узел.Колонки.Добавить("Параметр");
		Узел.Колонки.Добавить("Значение");
		
		ТекстXML = ПрочитатьТекстИзФайла(ИмяФайла, , Истина);
		
		ЧтениеXML = Новый ЧтениеXML;
		ЧтениеXML.УстановитьСтроку(ТекстXML);
		
	Иначе
		ВерхнийУровень = Ложь;
	КонецЕсли;
	
	Попытка
		Пока ЧтениеXML.Прочитать() Цикл
			ТипУзла = ЧтениеXML.ТипУзла;
			Если ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				НовСтр = Узел.Строки.Добавить();
				НовСтр.Параметр = нрег(ЧтениеXML.Имя);
				ВосстановитьXMLВДерево(ИмяФайла, Сообщения, НовСтр, ЧтениеXML);
			ИначеЕсли ТипУзла = ТипУзлаXML.КонецЭлемента Тогда
				Возврат Неопределено;
			ИначеЕсли ТипУзла = ТипУзлаXML.Текст Тогда
				Узел.Значение = ЧтениеXML.Значение;
			КонецЕсли;
		КонецЦикла;
	Исключение
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректный формат файла настроек!'") + Символы.ПС + ИнформацияОбОшибке().Описание);
	КонецПопытки;
	
	Если ВерхнийУровень Тогда
		ЧтениеXML.Закрыть();
		КоличествоСтрок = Узел.Строки.Количество();
		АдресУзла = ПоместитьВоВременноеХранилище(Узел, Новый УникальныйИдентификатор);
		ВозвращаемоеЗначение = Новый Структура("АдресДереваЗначений, КоличествоСтрок", АдресУзла, КоличествоСтрок);
		Узел = Неопределено;
		ЧтениеXML = Неопределено;
		Возврат ВозвращаемоеЗначение;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция НайтиПодчиненныйУзел(Родитель, ИмяПодчиненногоУзла)
	
	Возврат Родитель.Строки.Найти(нрег(ИмяПодчиненногоУзла), "Параметр");
	
КонецФункции

&НаСервереБезКонтекста
Функция НайтиПодчиненныеУзлы(Родитель, ИмяПодчиненногоУзла)
	
	Возврат Родитель.Строки.НайтиСтроки(Новый Структура("Параметр", нрег(ИмяПодчиненногоУзла)));
	
КонецФункции

&НаСервереБезКонтекста
Функция ПреобразоватьXMLКЗначению(Тип, СтрокаXML)
	
	Если ТипЗнч(Тип) = Тип("Строка") Тогда
		ЗаданныйТип = Тип(Тип);
	Иначе
		ЗаданныйТип = Тип;
	КонецЕсли;
	
	Если ЗаданныйТип = Тип("Дата") Тогда
		Возврат ДатаПоСтроке(СтрокаXML);
	Иначе
		Попытка
			Возврат XMLЗначение(ЗаданныйТип, СтрокаXML);
		Исключение
			Возврат РегламентированнаяОтчетностьКлиентСервер.ПустоеЗначениеТипа(ЗаданныйТип);
		КонецПопытки;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСвойстваИФНСИзИБ(КодыИФНС)
	
	// создаем массив-результат
	СведенияОбИФНС = Новый Массив;
	
	// получаем из ИБ
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	ИФНСы.Код,
	                      |	ИФНСы.Наименование,
	                      |	ИФНСы.Сертификаты.(
	                      |		Сертификат
	                      |	)
	                      |ИЗ
	                      |	Справочник.НалоговыеОрганы КАК ИФНСы
	                      |ГДЕ
	                      |	ИФНСы.Код В(&Код)");
	Запрос.УстановитьПараметр("Код", КодыИФНС);
	Выборка = Запрос.Выполнить().Выбрать();
	
	// формируем результат
	Пока Выборка.Следующий() Цикл
		
		СертификатыИФНС = Новый Массив;
		ВыборкаСертификаты = Выборка.Сертификаты.Выбрать();
		Пока ВыборкаСертификаты.Следующий() Цикл
			СертификатыИФНС.Добавить(СокрЛП(ВыборкаСертификаты.Сертификат));
		КонецЦикла;
		
		СведенияОбИФНС.Добавить(Новый Структура("Код, Наименование, Сертификаты",
								СокрЛП(Выборка.Код), СокрЛП(Выборка.Наименование), СертификатыИФНС));
		
	КонецЦикла;
	
	Возврат СведенияОбИФНС;
	
КонецФункции

&НаСервереБезКонтекста
Функция СравнитьМассивыБезУчетаПорядкаЭлементов(Массив1, Массив2)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СравнитьМассивыБезУчетаПорядкаЭлементов(Массив1, Массив2);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСвойстваОтделенийПФРИзИБ(КодыОтделенийПФР)
	
	// создаем массив-результат
	СведенияОбОтделениях = Новый Массив;
	
	// получаем из ИБ
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	ОрганыПФР.Код,
	                      |	ОрганыПФР.Наименование,
	                      |	ОрганыПФР.Сертификаты.(
	                      |		Сертификат
	                      |	),
	                      |	ОрганыПФР.СертификатыЭДОК.(
	                      |		Сертификат
	                      |	),
	                      |	ОрганыПФР.СертификатыЕЦП.(
	                      |		Сертификат
	                      |	)
	                      |ИЗ
	                      |	Справочник.ОрганыПФР КАК ОрганыПФР
	                      |ГДЕ
	                      |	ОрганыПФР.Код В(&Код)");
	Запрос.УстановитьПараметр("Код", КодыОтделенийПФР);
	Выборка = Запрос.Выполнить().Выбрать();
	
	// формируем результат
	Пока Выборка.Следующий() Цикл
		
		СертификатыОрганаПФР = Новый Массив;
		ВыборкаСертификаты = Выборка.Сертификаты.Выбрать();
		Пока ВыборкаСертификаты.Следующий() Цикл
			СертификатыОрганаПФР.Добавить(СокрЛП(ВыборкаСертификаты.Сертификат));
		КонецЦикла;
		СертификатыОрганаПФРЭДОК = Новый Массив;
		ВыборкаСертификатыЭДОК = Выборка.СертификатыЭДОК.Выбрать();
		Пока ВыборкаСертификатыЭДОК.Следующий() Цикл
			СертификатыОрганаПФРЭДОК.Добавить(СокрЛП(ВыборкаСертификатыЭДОК.Сертификат));
		КонецЦикла;
		СертификатыОрганаПФРЕЦП = Новый Массив;
		ВыборкаСертификатыЕЦП = Выборка.СертификатыЕЦП.Выбрать();
		Пока ВыборкаСертификатыЕЦП.Следующий() Цикл
			СертификатыОрганаПФРЕЦП.Добавить(СокрЛП(ВыборкаСертификатыЕЦП.Сертификат));
		КонецЦикла;
		СведенияОбОтделениях.Добавить(Новый Структура("Код, Наименование, Сертификаты, СертификатыЭДОК, СертификатыЕЦП",
			СокрЛП(Выборка.Код), СокрЛП(Выборка.Наименование), СертификатыОрганаПФР, СертификатыОрганаПФРЭДОК,
			СертификатыОрганаПФРЕЦП));
	КонецЦикла;
	
	Возврат СведенияОбОтделениях;
	
КонецФункции

&НаКлиенте
Процедура ПолучитьТаблицуНеобходимыхИсправлений(НастройкиИБ, НастройкиРОКИ, ВыполняемоеОповещение)
	
	Исправления = Новый Массив;
	
	Если НастройкиИБ.ПредназначенаДляДокументооборотаСФНС <> НастройкиРОКИ.ПредназначенаДляДокументооборотаСФНС Тогда
		ОпределитьИзменение(Исправления, "ИзменитьПризнакДокументооборотаСФНС", НастройкиИБ.ПредназначенаДляДокументооборотаСФНС, НастройкиРОКИ.ПредназначенаДляДокументооборотаСФНС, "Изменен признак возможности документооборота с ФНС по учетной записи");
	КонецЕсли;
	
	Если НастройкиИБ.ПредназначенаДляДокументооборотаСПФР <> НастройкиРОКИ.ПредназначенаДляДокументооборотаСПФР Тогда
		ОпределитьИзменение(Исправления, "ИзменитьПризнакДокументооборотаСПФР", НастройкиИБ.ПредназначенаДляДокументооборотаСПФР, НастройкиРОКИ.ПредназначенаДляДокументооборотаСПФР, "Изменен признак возможности документооборота с СФР (бывш. ПФР) по учетной записи");
	КонецЕсли;
	
	Если НастройкиИБ.ПредназначенаДляДокументооборотаСФСГС <> НастройкиРОКИ.ПредназначенаДляДокументооборотаСФСГС Тогда
		ОпределитьИзменение(Исправления, "ИзменитьПризнакДокументооборотаСФСГС", НастройкиИБ.ПредназначенаДляДокументооборотаСФСГС, НастройкиРОКИ.ПредназначенаДляДокументооборотаСФСГС, "Изменен признак возможности документооборота с Росстатом по учетной записи");
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ИспользуетсяЕЦППФР") Тогда
		Если нрег(НастройкиИБ.ИспользуетсяЕЦППФР) <> нрег(НастройкиРОКИ.ИспользуетсяЕЦППФР) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьИспользуетсяЕЦППФР", НастройкиИБ.ИспользуетсяЕЦППФР, НастройкиРОКИ.ИспользуетсяЕЦППФР, "В настройках учетной записи изменен признак возможности отправки в ЕЦП СФР (бывш. ПФР)");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ТипыДокументовЕЦППФР") Тогда
		Если нрег(НастройкиИБ.ТипыДокументовЕЦППФР) <> нрег(НастройкиРОКИ.ТипыДокументовЕЦППФР) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьТипыДокументовЕЦППФР", НастройкиИБ.ТипыДокументовЕЦППФР, НастройкиРОКИ.ТипыДокументовЕЦППФР, "В настройках учетной записи изменены типы документов, отправляемых в ЕЦП СФР (бывш. ПФР)");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ОператорРегНомерПФР") Тогда
		Если нрег(НастройкиИБ.ОператорРегНомерПФР) <> нрег(НастройкиРОКИ.ОператорРегНомерПФР) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьОператорРегНомерПФР", НастройкиИБ.ОператорРегНомерПФР, НастройкиРОКИ.ОператорРегНомерПФР, "В настройках учетной записи изменен регистрационный номер СФР (бывш. ПФР) оператора");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ОператорНаименованиеПолное") Тогда
		Если нрег(НастройкиИБ.ОператорНаименованиеПолное) <> нрег(НастройкиРОКИ.ОператорНаименованиеПолное) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьОператорНаименованиеПолное", НастройкиИБ.ОператорНаименованиеПолное, НастройкиРОКИ.ОператорНаименованиеПолное, "В настройках учетной записи изменено полное наименование оператора");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ОператорНаименованиеКраткое") Тогда
		Если нрег(НастройкиИБ.ОператорНаименованиеКраткое) <> нрег(НастройкиРОКИ.ОператорНаименованиеКраткое) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьОператорНаименованиеКраткое", НастройкиИБ.ОператорНаименованиеКраткое, НастройкиРОКИ.ОператорНаименованиеКраткое, "В настройках учетной записи изменено краткое наименование оператора");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ОператорИНН") Тогда
		Если нрег(НастройкиИБ.ОператорИНН) <> нрег(НастройкиРОКИ.ОператорИНН) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьОператорИНН", НастройкиИБ.ОператорИНН, НастройкиРОКИ.ОператорИНН, "В настройках учетной записи изменен ИНН оператора");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ОператорКПП") Тогда
		Если нрег(НастройкиИБ.ОператорКПП) <> нрег(НастройкиРОКИ.ОператорКПП) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьОператорКПП", НастройкиИБ.ОператорКПП, НастройкиРОКИ.ОператорКПП, "В настройках учетной записи изменен КПП оператора");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("АдресЭлектроннойПочты") Тогда
		Если нрег(НастройкиИБ.АдресЭлектроннойПочты) <> нрег(НастройкиРОКИ.АдресЭлектроннойПочты) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьАдресЭлектроннойПочты", НастройкиИБ.АдресЭлектроннойПочты, НастройкиРОКИ.АдресЭлектроннойПочты, "В настройках учетной записи изменен адрес электронной почты");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ИдентификаторАбонента") Тогда
		Если СокрЛП(НастройкиИБ.ИдентификаторАбонента) <> СокрЛП(НастройкиРОКИ.ИдентификаторАбонента) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьИдентификаторАбонента", НастройкиИБ.ИдентификаторАбонента, НастройкиРОКИ.ИдентификаторАбонента, "В настройках учетной записи изменен идентификатор абонента");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ИдентификаторСпецоператора") Тогда
		Если СокрЛП(НастройкиИБ.ИдентификаторСпецоператора) <> СокрЛП(НастройкиРОКИ.ИдентификаторСпецоператора) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьИдентификаторСпецоператора", НастройкиИБ.ИдентификаторСпецоператора, НастройкиРОКИ.ИдентификаторСпецоператора, "В настройках учетной записи изменен идентификатор оператора электронного документооборота");
		КонецЕсли;
	КонецЕсли;
	
	// свойства сервера документооборота
	Если НастройкиРОКИ.Свойство("АдресСпецоператора") Тогда
		Если нрег(НастройкиИБ.АдресЭлектроннойПочтыПочтовогоСервера) <> нрег(НастройкиРОКИ.АдресСпецоператора) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьАдресПочтовогоСервера", НастройкиИБ.АдресЭлектроннойПочтыПочтовогоСервера, НастройкиРОКИ.АдресСпецоператора, "В настройках сервера документооборота изменен адрес электронной почты для документооборота с ФНС");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("АдресСпецоператораПФР") Тогда
		Если нрег(НастройкиИБ.АдресЭлектроннойПочтыПочтовогоСервераПФР) <> нрег(НастройкиРОКИ.АдресСпецоператораПФР) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьАдресПочтовогоСервераПФР", НастройкиИБ.АдресЭлектроннойПочтыПочтовогоСервераПФР, НастройкиРОКИ.АдресСпецоператораПФР, "В настройках сервера документооборота изменен адрес электронной почты для документооборота с СФР (бывш. ПФР)");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("АдресСпецоператораФСГС") Тогда
		Если нрег(НастройкиИБ.АдресЭлектроннойПочтыПочтовогоСервераФСГС) <> нрег(НастройкиРОКИ.АдресСпецоператораФСГС) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьАдресПочтовогоСервераФСГС", НастройкиИБ.АдресЭлектроннойПочтыПочтовогоСервераФСГС, НастройкиРОКИ.АдресСпецоператораФСГС, "В настройках сервера документооборота изменен адрес электронной почты для документооборота с Росстатом");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("СертификатСОС") Тогда
		Если нрег(НастройкиИБ.СертификатПочтовогоСервера) <> нрег(НастройкиРОКИ.СертификатСОС.Отпечаток) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьСертификатПочтовогоСервера", нрег(НастройкиИБ.СертификатПочтовогоСервера), НастройкиРОКИ.СертификатСОС, "В настройках сервера документооборота изменен сертификат");
		КонецЕсли;
	КонецЕсли;
	
	// настройки доступа по POP3
	Если НастройкиРОКИ.Свойство("СерверPOP3") Тогда
		Если нрег(НастройкиИБ.СерверPOP3) <> нрег(НастройкиРОКИ.СерверPOP3) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьСерверPOP3", НастройкиИБ.СерверPOP3, НастройкиРОКИ.СерверPOP3, "В настройках учетной записи изменен адрес сервера POP3");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ПортPOP3") Тогда
		Если нрег(НастройкиИБ.ПортPOP3) <> нрег(НастройкиРОКИ.ПортPOP3) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьПортPOP3", НастройкиИБ.ПортPOP3, НастройкиРОКИ.ПортPOP3, "В настройках учетной записи изменен порт сервера POP3");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ИмяПользователяPOP3") Тогда
		Если нрег(НастройкиИБ.ИмяПользователяPOP3) <> нрег(НастройкиРОКИ.ИмяПользователяPOP3) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьИмяПользователяPOP3", НастройкиИБ.ИмяПользователяPOP3, НастройкиРОКИ.ИмяПользователяPOP3, "В настройках учетной записи изменено имя пользователя POP3");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ПарольPOP3") Тогда
		Если нрег(НастройкиИБ.ПарольPOP3) <> нрег(НастройкиРОКИ.ПарольPOP3) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьПарольPOP3", НастройкиИБ.ПарольPOP3, НастройкиРОКИ.ПарольPOP3, "В настройках учетной записи изменен пароль POP3");
		КонецЕсли;
	КонецЕсли;
	
	// настройки доступа по SMTP
	Если НастройкиРОКИ.Свойство("СерверSMTP") Тогда
		Если нрег(НастройкиИБ.СерверSMTP) <> нрег(НастройкиРОКИ.СерверSMTP) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьСерверSMTP", НастройкиИБ.СерверSMTP, НастройкиРОКИ.СерверSMTP, "В настройках учетной записи изменен адрес сервера SMTP");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиРОКИ.Свойство("ПортSMTP") Тогда
		Если нрег(НастройкиИБ.ПортSMTP) <> нрег(НастройкиРОКИ.ПортSMTP) Тогда
			ОпределитьИзменение(Исправления, "ИзменитьПортSMTP", НастройкиИБ.ПортSMTP, НастройкиРОКИ.ПортSMTP, "В настройках учетной записи изменен порт сервера SMTP");
		КонецЕсли;
	КонецЕсли;
	
	Если НастройкиИБ.ТребуетсяSMTPАутентификация Тогда // считается, что аутентификация на сервере SMTP не требуется никогда
		ОпределитьИзменение(Исправления, "ИзменитьФлагТребуетсяSMTPАутентификация", Истина, Ложь, "В настройках учетной записи изменен признак необходимости аутентификации на сервере SMTP");
	КонецЕсли;
	
	// проверяем необходимость обновления информации о сертификатах ответственных лиц организации
	Если НастройкиРОКИ.Свойство("Сертификаты") И ТипЗнч(НастройкиРОКИ.Сертификаты) = Тип("Массив") Тогда
		
		АбонентскиеСертификаты = НастройкиРОКИ.Сертификаты;
		
		// проверяем сертификат руководителя
		Если НЕ ЗначениеЗаполнено(НастройкиИБ.СертификатРуководителя) Тогда //+
			
			// сначала ищем сертификат руководителя
			СертификатыРуководителя = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 1);
			
			// если не найден - сертификат ответственного за шифрование
			Если СертификатыРуководителя.Количество() = 0 Тогда
				СертификатыРуководителя = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 3);
			КонецЕсли;
			
			// если сертификат ответственного не найден, то ищем сертификат главного бухгалтера
			Если СертификатыРуководителя.Количество() = 0 Тогда
				СертификатыРуководителя = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 2);
			КонецЕсли;
			
			// если и сертификат главного бухгалтера не найден, то берем первый в списке
			Если СертификатыРуководителя.Количество() = 0 Тогда
				Если АбонентскиеСертификаты.Количество() > 0 Тогда
					СертификатыРуководителя = Новый Массив;
					СертификатыРуководителя.Добавить(АбонентскиеСертификаты[0]);
				КонецЕсли;
			КонецЕсли;
			
			Если СертификатыРуководителя.Количество() > 0 Тогда
				ОпределитьИзменение(Исправления,
									"ИзменитьСертификатРуководителя",
									нрег(НастройкиИБ.СертификатРуководителя),//+
									СертификатыРуководителя[0], 
									"В настройках учетной записи изменен сертификат руководителя");
			КонецЕсли;
			
		Иначе
			СертификатИБВСписке = Ложь;
			
			// сначала ищем сертификат руководителя
			СертификатыРуководителя = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 1);
			
			// если не найден - сертификат ответственного за шифрование
			Если СертификатыРуководителя.Количество() = 0 Тогда
				СертификатыРуководителя = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 3);
			КонецЕсли;
			
			// если сертификат ответственного не найден, то ищем сертификат главного бухгалтера
			Если СертификатыРуководителя.Количество() = 0 Тогда
				СертификатыРуководителя = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 2);
			КонецЕсли;
			
			// если и сертификат главного бухгалтера не найден, то берем первый в списке
			Если СертификатыРуководителя.Количество() = 0 Тогда
				Если АбонентскиеСертификаты.Количество() > 0 Тогда
					СертификатыРуководителя = Новый Массив;
					СертификатыРуководителя.Добавить(АбонентскиеСертификаты[0]);
				КонецЕсли;
			КонецЕсли;
			
			Для Каждого Сертификат Из СертификатыРуководителя Цикл
				Если нрег(Сертификат.Отпечаток) = нрег(НастройкиИБ.СертификатРуководителя) Тогда //+
					СертификатИБВСписке = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если НЕ СертификатИБВСписке Тогда
				Если СертификатыРуководителя.Количество() = 0 Тогда
					НовоеЗначение = Новый Структура("Отпечаток");
				Иначе
					НовоеЗначение = СертификатыРуководителя[0];
				КонецЕсли;
				ОпределитьИзменение(Исправления, "ИзменитьСертификатРуководителя", нрег(НастройкиИБ.СертификатРуководителя), НовоеЗначение, "В настройках учетной записи изменен сертификат руководителя"); //+
			КонецЕсли;
			
		КонецЕсли;
		
		// проверяем сертификат главного бухгалтера
		Если НЕ ЗначениеЗаполнено(НастройкиИБ.СертификатГлавногоБухгалтера) Тогда
			СертификатыБухгалтера = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 2);
			Если СертификатыБухгалтера.Количество() > 0 Тогда
				ОпределитьИзменение(Исправления,
									"ИзменитьСертификатГлавногоБухгалтера",
									нрег(НастройкиИБ.СертификатГлавногоБухгалтера),
									СертификатыБухгалтера[0],
									"В настройках учетной записи изменен сертификат главного бухгалтера");
			КонецЕсли;
		Иначе
			СертификатИБВСписке = Ложь;
			СертификатыБухгалтера = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 2);
			Для Каждого СертификатБухгалтера Из СертификатыБухгалтера Цикл
				Если нрег(СертификатБухгалтера.Отпечаток) = нрег(НастройкиИБ.СертификатГлавногоБухгалтера) Тогда
					СертификатИБВСписке = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если НЕ СертификатИБВСписке Тогда
				Если СертификатыБухгалтера.Количество() = 0 Тогда
					НовоеЗначение = Новый Структура("Отпечаток");
				Иначе
					НовоеЗначение = СертификатыБухгалтера[0];
				КонецЕсли;
				ОпределитьИзменение(Исправления, "ИзменитьСертификатГлавногоБухгалтера", нрег(НастройкиИБ.СертификатГлавногоБухгалтера), НовоеЗначение, "В настройках учетной записи изменен сертификат главного бухгалтера");
			КонецЕсли;
		КонецЕсли;
		
		// проверяем сертификат ответственного за шифрование
		Если НЕ ЗначениеЗаполнено(НастройкиИБ.СертификатДляШифрования) Тогда
			СертификатыДляШифрования = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ПризнакШифрования", Истина);
			Если СертификатыДляШифрования.Количество() = 0 Тогда
				СертификатыДляШифрования = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 3);
			КонецЕсли;
			Если СертификатыДляШифрования.Количество() > 0 Тогда
				ОпределитьИзменение(Исправления, "ИзменитьСертификатДляШифрования", нрег(НастройкиИБ.СертификатДляШифрования), СертификатыДляШифрования[0], "В настройках учетной записи изменен сертификат для шифрования");
			КонецЕсли;
		Иначе
			СертификатИБВСписке = Ложь;
			СертификатыДляШифрования = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ПризнакШифрования", Истина);
			Если СертификатыДляШифрования.Количество() = 0 Тогда
				СертификатыДляШифрования = НайтиСтрокиМассиваСтруктур(АбонентскиеСертификаты, "ТипПодписи", 3);
			КонецЕсли;
			Для Каждого СертификатДляШифрования Из СертификатыДляШифрования Цикл
				Если нрег(СертификатДляШифрования.Отпечаток) = нрег(НастройкиИБ.СертификатДляШифрования) Тогда
					СертификатИБВСписке = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
			Если НЕ СертификатИБВСписке Тогда
				ОпределитьИзменение(Исправления, "ИзменитьСертификатДляШифрования", нрег(НастройкиИБ.СертификатДляШифрования), СертификатыДляШифрования[0], "В настройках учетной записи изменен сертификат для шифрования");
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// проверка ИФНС
	ПроверкаИФНС = НастройкиРОКИ.Свойство("ИФНСы") И ТипЗнч(НастройкиРОКИ.ИФНСы) = Тип("Массив");
	Если ПроверкаИФНС Тогда
		
		// проверка основной ИФНС
		ИФНСы = НастройкиРОКИ.ИФНСы;
		Если ИФНСы.Количество() = 1 Тогда
			ОсновнаяИФНС = ИФНСы[0];
		Иначе
			ОсновнаяИФНС = НайтиСтрокуВМассивеСтруктурКлиент(ИФНСы, Истина,"ИФНСПоУмолчанию");
		КонецЕсли;
		Если НастройкиИБ.КодНалоговогоОргана <> ОсновнаяИФНС.Код Тогда
			ОпределитьИзменение(Исправления, "ИзменитьОсновнуюИФНС", нрег(НастройкиИБ.КодНалоговогоОргана), ОсновнаяИФНС, "В настройках учетной записи изменен налоговый орган");
		КонецЕсли;
		
		// формируем массив кодов ИФНС, задействованных по данным РОКИ
		МассивКодовИФНС = ВыгрузитьКолонкуИзМассиваСтруктур(ИФНСы, "Код");
		
		// получаем из базы информацию об этих отделениях ПФР
		СведенияИзИБ = ПолучитьСвойстваИФНСИзИБ(МассивКодовИФНС);
		
		// создание кэша зарегистрированных в хранилище сертификатов
		
		ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ", ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ);
		Оповещение = Новый ОписаниеОповещения("ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовAddressBookЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		ДополнительныеПараметрыМетода = Новый Структура;
		ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
		ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
		
		КриптографияЭДКОКлиент.ПолучитьСертификаты(
			Оповещение, Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "AddressBook", Истина), ДополнительныеПараметрыМетода);
		Возврат;
		
	КонецЕсли;
	
	Результат = Новый Структура("Выполнено, МенеджерКриптографии", Ложь, Неопределено);
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ, МассивСертификатовAddressBook, МассивСертификатовCA", ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ, Неопределено, Неопределено);
	Оповещение = Новый ОписаниеОповещения("ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовROOTЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовAddressBookЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиИБ = ДополнительныеПараметры.НастройкиИБ;
	НастройкиРОКИ = ДополнительныеПараметры.НастройкиРОКИ;
	Исправления = ДополнительныеПараметры.Исправления;
	ПроверкаИФНС = ДополнительныеПараметры.ПроверкаИФНС;
	ИФНСы = ДополнительныеПараметры.ИФНСы;
	ОсновнаяИФНС = ДополнительныеПараметры.ОсновнаяИФНС;
	СведенияИзИБ = ДополнительныеПараметры.СведенияИзИБ;
	
	МассивСертификатовAddressBook = Новый Массив;
	Если Результат.Выполнено Тогда
		Для каждого СертификатХранилища Из Результат.Сертификаты Цикл
			МассивСертификатовAddressBook.Добавить(нрег(СокрЛП(СертификатХранилища.Отпечаток)));
		КонецЦикла;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ, МассивСертификатовAddressBook", ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ, МассивСертификатовAddressBook);
	Оповещение = Новый ОписаниеОповещения("ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовCAЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ДополнительныеПараметрыМетода = Новый Структура;
	ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
	ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
	
	КриптографияЭДКОКлиент.ПолучитьСертификаты(
		Оповещение, Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "CA", Истина), ДополнительныеПараметрыМетода);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовCAЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиИБ = ДополнительныеПараметры.НастройкиИБ;
	НастройкиРОКИ = ДополнительныеПараметры.НастройкиРОКИ;
	Исправления = ДополнительныеПараметры.Исправления;
	ПроверкаИФНС = ДополнительныеПараметры.ПроверкаИФНС;
	ИФНСы = ДополнительныеПараметры.ИФНСы;
	ОсновнаяИФНС = ДополнительныеПараметры.ОсновнаяИФНС;
	СведенияИзИБ = ДополнительныеПараметры.СведенияИзИБ;
	МассивСертификатовAddressBook = ДополнительныеПараметры.МассивСертификатовAddressBook;
	
	МассивСертификатовCA = Новый Массив;
	Если Результат.Выполнено Тогда
		Для каждого СертификатХранилища Из Результат.Сертификаты Цикл
			МассивСертификатовCA.Добавить(нрег(СокрЛП(СертификатХранилища.Отпечаток)));
		КонецЦикла;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ, МассивСертификатовAddressBook, МассивСертификатовCA", ВыполняемоеОповещение, НастройкиИБ, НастройкиРОКИ, Исправления, ПроверкаИФНС, ИФНСы, ОсновнаяИФНС, СведенияИзИБ, МассивСертификатовAddressBook, МассивСертификатовCA);
	Оповещение = Новый ОписаниеОповещения("ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовROOTЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ДополнительныеПараметрыМетода = Новый Структура;
	ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
	ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
	
	КриптографияЭДКОКлиент.ПолучитьСертификаты(
		Оповещение, Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "ROOT", Истина), ДополнительныеПараметрыМетода);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьТаблицуНеобходимыхИсправленийПослеПолученияСертификатовROOTЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	НастройкиИБ = ДополнительныеПараметры.НастройкиИБ;
	НастройкиРОКИ = ДополнительныеПараметры.НастройкиРОКИ;
	Исправления = ДополнительныеПараметры.Исправления;
	ПроверкаИФНС = ДополнительныеПараметры.ПроверкаИФНС;
	ИФНСы = ДополнительныеПараметры.ИФНСы;
	ОсновнаяИФНС = ДополнительныеПараметры.ОсновнаяИФНС;
	СведенияИзИБ = ДополнительныеПараметры.СведенияИзИБ;
	МассивСертификатовAddressBook = ДополнительныеПараметры.МассивСертификатовAddressBook;
	МассивСертификатовCA = ДополнительныеПараметры.МассивСертификатовCA;
	
	Если ПроверкаИФНС Тогда
		
		МассивСертификатовROOT = Новый Массив;
		Если Результат.Выполнено Тогда
			Для каждого СертификатХранилища Из Результат.Сертификаты Цикл
				МассивСертификатовROOT.Добавить(нрег(СокрЛП(СертификатХранилища.Отпечаток)));
			КонецЦикла;
		КонецЕсли;
		
		// проверка всех ИФНС
		Для Каждого СтрИФНС Из ИФНСы Цикл
		
			// ищем ИФНС по коду
			КодИФНС = СокрЛП(СтрИФНС.Код);
			ЗаписьИФНС = НайтиСтрокуВМассивеСтруктурКлиент(СведенияИзИБ, КодИФНС, "Код");
			Если ЗаписьИФНС = Неопределено Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьИФНС", , КодИФНС, "Налоговый орган с кодом " + КодИФНС + " не определен в информационной базе", СтрИФНС.Наименование);
			КонецЕсли;
			
			// проверяем на необходимость изменения списка сертификатов инспекции ФНС в ИБ
			СертификатыИФНСсРОКИ = ВыгрузитьКолонкуИзМассиваСтруктур(СтрИФНС.Сертификаты, "Отпечаток");
			СертификатыИФНСИзИБ = ?(ЗаписьИФНС = Неопределено, Неопределено, ЗаписьИФНС.Сертификаты);
			Если НЕ СравнитьМассивыБезУчетаПорядкаЭлементов(СертификатыИФНСсРОКИ, СертификатыИФНСИзИБ) Тогда
				ОпределитьИзменение(Исправления, "ИзменитьСертификатИФНС", СертификатыИФНСИзИБ, СтрИФНС.Сертификаты, "Сертификат налогового органа с кодом " + КодИФНС + " изменен", КодИФНС);
			КонецЕсли;
			
			// проверяем на необходимость добавления сертификатов ИФНС в список в ИБ и регистрации в системе
			Для Каждого СтрСертификатИФНС Из СтрИФНС.Сертификаты Цикл
				
				// проверяем на необходимость регистрации в системе
				Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатИФНС.Отпечаток, "AddressBook", МассивСертификатовAddressBook) Тогда
					ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатНалоговогоОргана", , СтрСертификатИФНС, "Сертификат налогового органа с кодом " + КодИФНС + " не зарегистрирован в системе", КодИФНС);
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЕсли;
	
	
	// проверка отделений ПФР
	ОтделенияПФР = Неопределено;
	Если НастройкиРОКИ.Свойство("ОтделенияПФР", ОтделенияПФР) Тогда
		
		// формируем массив кодов отделений ПФР, задействованных по данным РОКИ
		МассивКодовОтделенийПФР = ВыгрузитьКолонкуИзМассиваСтруктур(ОтделенияПФР, "Код");
		
		// получаем из базы информацию об этих отделениях ПФР
		СведенияИзИБ = ПолучитьСвойстваОтделенийПФРИзИБ(МассивКодовОтделенийПФР);
		
		// перебираем отделения ПФР с РОКИ и определяем необходимость создания записи в справочнике отделений ПФР,
		// обновления списка сертификатов и регистрации их в системе
		Для Каждого СтрОтделениеПФР Из ОтделенияПФР Цикл
			
			// ищем отделение ПФР по коду
			КодОрганаПФР = СокрЛП(СтрОтделениеПФР.Код);
			ЗаписьОрганПФР = НайтиСтрокуВМассивеСтруктурКлиент(СведенияИзИБ, КодОрганаПФР, "Код");
			Если ЗаписьОрганПФР = Неопределено Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьОрганПФР", , КодОрганаПФР, "Орган СФР (бывш. ПФР) с кодом " + КодОрганаПФР + " не определен в информационной базе", СтрОтделениеПФР.Наименование);
			КонецЕсли;
			
			// проверяем на необходимость изменения списка сертификатов органа ПФР в ИБ
			СертификатыОрганаПФРсРОКИ = ВыгрузитьКолонкуИзМассиваСтруктур(СтрОтделениеПФР.Сертификаты, "Отпечаток");
			СертификатыОрганаПФРИзИБ = ?(ЗаписьОрганПФР = Неопределено, Неопределено, ЗаписьОрганПФР.Сертификаты);
			Если НЕ СравнитьМассивыБезУчетаПорядкаЭлементов(СертификатыОрганаПФРсРОКИ, СертификатыОрганаПФРИзИБ) Тогда
				ОпределитьИзменение(Исправления, "ИзменитьСертификатОрганаПФР", СертификатыОрганаПФРИзИБ, СтрОтделениеПФР.Сертификаты, "Сертификат органа СФР (бывш. ПФР) с кодом " + КодОрганаПФР + " изменен", КодОрганаПФР);
			КонецЕсли;
			
			// проверяем на необходимость изменения списка сертификатов ПФР для ЭДОК в ИБ
			СертификатыОрганаПФРЭДОКсРОКИ = ВыгрузитьКолонкуИзМассиваСтруктур(СтрОтделениеПФР.СертификатыЭДОК, "Отпечаток");
			СертификатыОрганаПФРЭДОКИзИБ = ?(ЗаписьОрганПФР = Неопределено, Неопределено, ЗаписьОрганПФР.СертификатыЭДОК);
			Если НЕ СравнитьМассивыБезУчетаПорядкаЭлементов(СертификатыОрганаПФРЭДОКсРОКИ, СертификатыОрганаПФРЭДОКИзИБ) Тогда
				ОпределитьИзменение(
					Исправления,
					"ИзменитьСертификатОрганаПФРЭДОК",
					СертификатыОрганаПФРЭДОКИзИБ,
					СтрОтделениеПФР.СертификатыЭДОК,
					НСтр("ru = 'Сертификат СФР (бывш. ПФР) для ЭДОК изменен'"),
					КодОрганаПФР);
			КонецЕсли;
			
			// проверяем на необходимость изменения списка сертификатов ПФР для ЕЦП в ИБ
			СертификатыОрганаПФРЕЦПсРОКИ = ВыгрузитьКолонкуИзМассиваСтруктур(СтрОтделениеПФР.СертификатыЕЦП, "Отпечаток");
			СертификатыОрганаПФРЕЦПИзИБ = ?(ЗаписьОрганПФР = Неопределено, Неопределено, ЗаписьОрганПФР.СертификатыЕЦП);
			Если НЕ СравнитьМассивыБезУчетаПорядкаЭлементов(СертификатыОрганаПФРЕЦПсРОКИ, СертификатыОрганаПФРЕЦПИзИБ) Тогда
				ОпределитьИзменение(
					Исправления,
					"ИзменитьСертификатОрганаПФРЕЦП",
					СертификатыОрганаПФРЕЦПИзИБ,
					СтрОтделениеПФР.СертификатыЕЦП,
					НСтр("ru = 'Сертификат СФР (бывш. ПФР) для ЕЦП изменен'"),
					КодОрганаПФР);
			КонецЕсли;
			
			// проверяем на необходимость добавления сертификатов отделения ПФР в список в ИБ и регистрации в системе
			Для Каждого СтрСертификатОтделения Из СтрОтделениеПФР.Сертификаты Цикл
				
				// проверяем на необходимость регистрации в системе
				Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатОтделения.Отпечаток, "AddressBook", МассивСертификатовAddressBook) Тогда
					ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатОрганаПФР", , СтрСертификатОтделения, "Сертификат органа СФР (бывш. ПФР) с кодом " + КодОрганаПФР + " не зарегистрирован в системе", КодОрганаПФР);
				КонецЕсли;
				
			КонецЦикла;
			
			// проверяем на необходимость добавления сертификатов ПФР для ЭДОК в список в ИБ и регистрации в системе
			Для каждого СтрСертификатОтделения Из СтрОтделениеПФР.СертификатыЭДОК Цикл
				
				// проверяем на необходимость регистрации в системе
				Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатОтделения.Отпечаток, "AddressBook",
					МассивСертификатовAddressBook) Тогда
					
					ОпределитьИзменение(
						Исправления,
						"ЗарегистрироватьСертификатОрганаПФРЭДОК",,
						СтрСертификатОтделения,
						НСтр("ru = 'Сертификат СФР (бывш. ПФР) для ЭДОК не зарегистрирован в системе'"),
						КодОрганаПФР);
				КонецЕсли;
				
			КонецЦикла;
			
			// проверяем на необходимость добавления сертификатов ПФР для ЕЦП в список в ИБ и регистрации в системе
			Для каждого СтрСертификатОтделения Из СтрОтделениеПФР.СертификатыЕЦП Цикл
				
				// проверяем на необходимость регистрации в системе
				Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатОтделения.Отпечаток, "AddressBook",
					МассивСертификатовAddressBook) Тогда
					
					ОпределитьИзменение(
						Исправления,
						"ЗарегистрироватьСертификатОрганаПФРЕЦП",,
						СтрСертификатОтделения,
						НСтр("ru = 'Сертификат СФР (бывш. ПФР) для ЕЦП не зарегистрирован в системе'"),
						КодОрганаПФР);
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЕсли;
	
	// проверка отделений ФСГС
	ОтделенияФСГС = Неопределено;
	Если НастройкиРОКИ.Свойство("ОтделенияФСГС", ОтделенияФСГС) Тогда
		
		// формируем массив кодов отделений ФСГС, задействованных по данным РОКИ
		МассивКодовОтделенийФСГС = ВыгрузитьКолонкуИзМассиваСтруктур(ОтделенияФСГС, "Код");
		
		// получаем из базы информацию об этих отделениях ФСГС
		СведенияИзИБ = ПолучитьСвойстваОтделенийФСГСИзИБ(МассивКодовОтделенийФСГС);
		
		// перебираем отделения ФСГС с РОКИ и определяем необходимость создания записи в справочнике отделений ФСГС,
		// обновления списка сертификатов и регистрации их в системе
		Для Каждого СтрОтделениеФСГС Из ОтделенияФСГС Цикл
			
			// ищем отделение ФСГС по коду
			КодОрганаФСГС = СокрЛП(СтрОтделениеФСГС.Код);
			ЗаписьОрганФСГС = НайтиСтрокуВМассивеСтруктурКлиент(СведенияИзИБ, КодОрганаФСГС, "Код");
			Если ЗаписьОрганФСГС = Неопределено Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьОрганФСГС", , КодОрганаФСГС, "Орган ФСГС с кодом " + КодОрганаФСГС + " не определен в информационной базе", СтрОтделениеФСГС.Наименование);
			КонецЕсли;
			
			// проверяем на необходимость изменения списка сертификатов органа ФСГС в ИБ
			СертификатыОрганаФСГСсРОКИ = ВыгрузитьКолонкуИзМассиваСтруктур(СтрОтделениеФСГС.Сертификаты, "Отпечаток");
			СертификатыОрганаФСГСИзИБ = ?(ЗаписьОрганФСГС = Неопределено, Неопределено, ЗаписьОрганФСГС.Сертификаты);
			Если НЕ СравнитьМассивыБезУчетаПорядкаЭлементов(СертификатыОрганаФСГСсРОКИ, СертификатыОрганаФСГСИзИБ) Тогда
				ОпределитьИзменение(Исправления, "ИзменитьСертификатОрганаФСГС", СертификатыОрганаФСГСИзИБ, СтрОтделениеФСГС.Сертификаты, "Сертификат органа ФСГС с кодом " + КодОрганаФСГС + " изменен", КодОрганаФСГС);
			КонецЕсли;
			
			// проверяем на необходимость добавления сертификатов отделения ФСГС в список в ИБ и регистрации в системе
			Для Каждого СтрСертификатОтделения Из СтрОтделениеФСГС.Сертификаты Цикл
				
				// проверяем на необходимость регистрации в системе
				Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатОтделения.Отпечаток, "AddressBook", МассивСертификатовAddressBook) Тогда
					ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатОрганаФСГС", , СтрСертификатОтделения, "Сертификат органа ФСГС с кодом " + КодОрганаФСГС + " не зарегистрирован в системе", КодОрганаФСГС);
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЦикла;
		
	КонецЕсли;
	
	// уточнение организации для настроек ФСС, ФСРАР, РПН, ФТС
	НастройкиИБОрганизация = НастройкиИБ.Организация;
	Если (НастройкиРОКИ.Свойство("ФССИспользуется") И НастройкиРОКИ.ФССИспользуется
		ИЛИ НастройкиРОКИ.Свойство("ФСРАРИспользуется") И НастройкиРОКИ.ФСРАРИспользуется
		ИЛИ НастройкиРОКИ.Свойство("РПНИспользуется") И НастройкиРОКИ.РПНИспользуется
		ИЛИ НастройкиРОКИ.Свойство("ФТСИспользуется") И НастройкиРОКИ.ФТСИспользуется) Тогда
		
		Если ТипЗнч(НастройкиИБОрганизация) = Тип("Структура") И НастройкиИБОрганизация.Свойство("ИНН")
			И НастройкиИБОрганизация.Свойство("КПП") Тогда
			
			НастройкиИБОрганизация = ОпределитьОрганизациюПоИННиКППНаСервере(НастройкиИБОрганизация.ИНН,
				НастройкиИБОрганизация.КПП, Истина);
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(НастройкиИБОрганизация) И ЗначениеЗаполнено(НастройкиИБ.УчетнаяЗапись) Тогда
			НастройкиИБОрганизация = ОпределитьОрганизациюПоУчетнойЗаписи(НастройкиИБ.УчетнаяЗапись);
		КонецЕсли;
	КонецЕсли;
	
	// проверка настроек ФСС
	Если НастройкиРОКИ.Свойство("ФССИспользуется") И НастройкиРОКИ.ФССИспользуется
		И ЗначениеЗаполнено(НастройкиИБОрганизация) Тогда
		
		// получаем из базы информацию настройках обмена с ФСС
		СведенияИзИБ = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(НастройкиИБОрганизация);
		СертификатФССИзИБ = ?(СведенияИзИБ = Неопределено, Неопределено, СведенияИзИБ.СертификатФССОтпечаток);
		СертификатФССЭЛНИзИБ = ?(СведенияИзИБ = Неопределено, Неопределено, СведенияИзИБ.СертификатФССЭЛНОтпечаток);
		ТестовыйСерверФССЭЛНИзИБ = ?(СведенияИзИБ = Неопределено, Неопределено, СведенияИзИБ.ТестовыйСерверФССЭЛН);
		СертификатФССсРОКИ = ?(НастройкиРОКИ.Свойство("СертификатФСС"), НастройкиРОКИ.СертификатФСС, Неопределено);
		СертификатФССЭЛНсРОКИ = ?(НастройкиРОКИ.Свойство("СертификатФССЭЛН"), НастройкиРОКИ.СертификатФССЭЛН, Неопределено);
		
		Если СведенияИзИБ = Неопределено Тогда
			ОпределитьИзменение(
				Исправления,
				"ЗарегистрироватьНаправлениеФСС",,,
				НСтр("ru = 'Обмен с СФР (бывш. ФСС) не включен в информационной базе'"));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СертификатФССсРОКИ) И СертификатФССсРОКИ <> СертификатФССИзИБ Тогда
			ОпределитьИзменение(
				Исправления,
				"ИзменитьСертификатФСС",
				СертификатФССИзИБ,
				СертификатФССсРОКИ,
				НСтр("ru = 'Сертификат СФР (бывш. ФСС) изменен'"));
			
			Если НЕ СертификатЗарегистрированВХранилище(СертификатФССсРОКИ.Отпечаток, "AddressBook",
				МассивСертификатовAddressBook) Тогда
				
				ОпределитьИзменение(
					Исправления,
					"ЗарегистрироватьСертификатФСС",,
					СертификатФССсРОКИ,
					НСтр("ru = 'Сертификат СФР (бывш. ФСС) не зарегистрирован в системе'"));
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СертификатФССЭЛНсРОКИ) И СертификатФССЭЛНсРОКИ <> СертификатФССЭЛНИзИБ
			И (ТестовыйСерверФССЭЛНИзИБ <> Истина ИЛИ НЕ ЗначениеЗаполнено(СертификатФССЭЛНИзИБ)) Тогда
			ОпределитьИзменение(
				Исправления,
				"ИзменитьСертификатФССЭЛН",
				СертификатФССЭЛНИзИБ,
				СертификатФССЭЛНсРОКИ,
				НСтр("ru = 'Сертификат СФР (бывш. ФСС) ЭЛН, ПОВЭД, ССВ, СЭДО изменен'"));
			
			Если НЕ СертификатЗарегистрированВХранилище(СертификатФССЭЛНсРОКИ.Отпечаток, "AddressBook",
				МассивСертификатовAddressBook) Тогда
				
				ОпределитьИзменение(
					Исправления,
					"ЗарегистрироватьСертификатФССЭЛН",,
					СертификатФССЭЛНсРОКИ,
					НСтр("ru = 'Сертификат СФР (бывш. ФСС) ЭЛН, ПОВЭД, ССВ, СЭДО не зарегистрирован в системе'"));
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// проверка настроек ФСРАР
	Если НастройкиРОКИ.Свойство("ФСРАРИспользуется") И НастройкиРОКИ.ФСРАРИспользуется
		И ЗначениеЗаполнено(НастройкиИБОрганизация) Тогда
		
		// получаем из базы информацию настройках обмена с ФСРАР
		СведенияИзИБ = НастройкиФСРАРНаСервере(НастройкиИБОрганизация);
		КодРегионаФСРАРИзИБ = ?(СведенияИзИБ.ИспользоватьОбмен, СведенияИзИБ.КодРегиона, Неопределено);
		СертификатФСРАРИзИБ = ?(СведенияИзИБ.ИспользоватьОбмен, СведенияИзИБ.СертификатФСРАРОтпечаток, Неопределено);
		СертификатФСРАРСубъектаРФИзИБ = ?(СведенияИзИБ.ИспользоватьОбмен, СведенияИзИБ.СертификатСубъектаРФОтпечаток, Неопределено);
		КодРегионаФСРАРсРОКИ = ?(НастройкиРОКИ.Свойство("КодРегионаФСРАР"), НастройкиРОКИ.КодРегионаФСРАР, Неопределено);
		СертификатФСРАРсРОКИ = ?(НастройкиРОКИ.Свойство("СертификатФСРАР"), НастройкиРОКИ.СертификатФСРАР, Неопределено);
		СертификатФСРАРСубъектаРФсРОКИ = ?(НастройкиРОКИ.Свойство("СертификатФСРАРСубъектаРФ"), НастройкиРОКИ.СертификатФСРАРСубъектаРФ, Неопределено);
		
		Если НЕ СведенияИзИБ.ИспользоватьОбмен Тогда
			ОпределитьИзменение(
				Исправления,
				"ЗарегистрироватьНаправлениеФСРАР",,,
				НСтр("ru = 'Обмен с Росалкогольтабакконтролем не включен в информационной базе'"));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(КодРегионаФСРАРсРОКИ) И КодРегионаФСРАРсРОКИ <> КодРегионаФСРАРИзИБ Тогда
			ОпределитьИзменение(
				Исправления,
				"ИзменитьКодРегионаФСРАР",
				КодРегионаФСРАРИзИБ,
				КодРегионаФСРАРсРОКИ,
				НСтр("ru = 'Код региона Росалкогольтабакконтроля изменен'"));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СертификатФСРАРсРОКИ) И СертификатФСРАРсРОКИ <> СертификатФСРАРИзИБ Тогда
			ОпределитьИзменение(
				Исправления,
				"ИзменитьСертификатФСРАР",
				СертификатФСРАРИзИБ,
				СертификатФСРАРсРОКИ,
				НСтр("ru = 'Сертификат Росалкогольтабакконтроля изменен'"));
			
			Если НЕ СертификатЗарегистрированВХранилище(СертификатФСРАРсРОКИ.Отпечаток, "AddressBook",
				МассивСертификатовAddressBook) Тогда
				
				ОпределитьИзменение(
					Исправления,
					"ЗарегистрироватьСертификатФСРАР",,
					СертификатФСРАРсРОКИ,
					НСтр("ru = 'Сертификат Росалкогольтабакконтроля не зарегистрирован в системе'"));
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СертификатФСРАРСубъектаРФсРОКИ)
			И СертификатФСРАРСубъектаРФсРОКИ <> СертификатФСРАРСубъектаРФИзИБ Тогда
			
			ОпределитьИзменение(
				Исправления,
				"ИзменитьСертификатФСРАРСубъектаРФ",
				СертификатФСРАРСубъектаРФИзИБ,
				СертификатФСРАРСубъектаРФсРОКИ,
				НСтр("ru = 'Сертификат субъекта РФ Росалкогольтабакконтроля изменен'"));
			
			Если НЕ СертификатЗарегистрированВХранилище(СертификатФСРАРСубъектаРФсРОКИ.Отпечаток, "AddressBook",
				МассивСертификатовAddressBook) Тогда
				
				ОпределитьИзменение(
					Исправления,
					"ЗарегистрироватьСертификатФСРАРСубъектаРФ",,
					СертификатФСРАРСубъектаРФсРОКИ,
					НСтр("ru = 'Сертификат субъекта РФ Росалкогольтабакконтроля не зарегистрирован в системе'"));
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// проверка настроек РПН
	Если НастройкиРОКИ.Свойство("РПНИспользуется") И НастройкиРОКИ.РПНИспользуется
		И ЗначениеЗаполнено(НастройкиИБОрганизация) Тогда
		
		// получаем из базы информацию настройках обмена с РПН
		СведенияИзИБ = НастройкиРПННаСервере(НастройкиИБОрганизация);
		СертификатРПНИзИБ = ?(СведенияИзИБ.ИспользоватьОбмен, СведенияИзИБ.СертификатРПНОтпечаток, Неопределено);
		СертификатРПНсРОКИ = ?(НастройкиРОКИ.Свойство("СертификатРПН"), НастройкиРОКИ.СертификатРПН, Неопределено);
		
		Если НЕ СведенияИзИБ.ИспользоватьОбмен Тогда
			ОпределитьИзменение(
				Исправления,
				"ЗарегистрироватьНаправлениеРПН",,,
				НСтр("ru = 'Обмен с Росприроднадзором не включен в информационной базе'"));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СертификатРПНсРОКИ) И СертификатРПНсРОКИ <> СертификатРПНИзИБ Тогда
			ОпределитьИзменение(
				Исправления,
				"ИзменитьСертификатРПН",
				СертификатРПНИзИБ,
				СертификатРПНсРОКИ,
				НСтр("ru = 'Сертификат Росприроднадзора изменен'"));
			
			Если НЕ СертификатЗарегистрированВХранилище(СертификатРПНсРОКИ.Отпечаток, "AddressBook",
				МассивСертификатовAddressBook) Тогда
				
				ОпределитьИзменение(
					Исправления,
					"ЗарегистрироватьСертификатРПН",,
					СертификатРПНсРОКИ,
					НСтр("ru = 'Сертификат Росприроднадзора не зарегистрирован в системе'"));
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	// проверка настроек ФТС
	Если НастройкиРОКИ.Свойство("ФТСИспользуется") И НастройкиРОКИ.ФТСИспользуется
		И ЗначениеЗаполнено(НастройкиИБОрганизация) Тогда
		
		// получаем из базы информацию настройках обмена с ФТС
		СведенияИзИБ = НастройкиФТСНаСервере(НастройкиИБОрганизация);
		
		Если НЕ СведенияИзИБ.ИспользоватьОбмен Тогда
			ОпределитьИзменение(
				Исправления,
				"ЗарегистрироватьНаправлениеФТС",,,
				НСтр("ru = 'Обмен с ФТС не включен в информационной базе'"));
		КонецЕсли;
		
	КонецЕсли;
	
	// проверка на необходимость регистрации сертификата СОС
	Если НастройкиРОКИ.Свойство("СертификатСОС") И НЕ СертификатЗарегистрированВХранилище(НастройкиРОКИ.СертификатСОС.Отпечаток, "AddressBook", МассивСертификатовAddressBook) Тогда
		ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатСОС", , НастройкиРОКИ.СертификатСОС, "Сертификат оператора электронного документооборота не зарегистрирован в системе");
	КонецЕсли;
	
	// проверка на необходимость регистрации корневых сертификатов ГНИВЦ
	СертификатыКорневыеГНИВЦ = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКорневыеГНИВЦ", СертификатыКорневыеГНИВЦ) Тогда
		Для каждого СтрСертификатКорневойГНИВЦ Из СертификатыКорневыеГНИВЦ Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатКорневойГНИВЦ.Отпечаток, "ROOT", МассивСертификатовROOT) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКорневойГНИВЦ", , СтрСертификатКорневойГНИВЦ, "Корневой сертификат ГНИВЦ не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// проверка на необходимость регистрации корневых сертификатов Такском
	СертификатыКорневыеТакском = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКорневыеТакском", СертификатыКорневыеТакском) Тогда
		Для каждого СтрСертификатКорневойТакском Из СертификатыКорневыеТакском Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатКорневойТакском.Отпечаток, "ROOT", МассивСертификатовROOT) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКорневойТакском", , СтрСертификатКорневойТакском, "Корневой сертификат оператора электронного документооборота не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// проверка на необходимость регистрации кросс-сертификатов Такском
	СертификатыКРОСС = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКРОСС", СертификатыКРОСС) Тогда
		Для каждого СтрСертификатКРОСС Из СертификатыКРОСС Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрСертификатКРОСС.Отпечаток, "CA", МассивСертификатовCA) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКроссГНИВЦ", , СтрСертификатКРОСС, "Кросс-сертификат электронного документооборота не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	
	// проверка не необходимость регистрации корневых сертификатов ПФР
	СертификатыКорневыеПФР = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКорневыеПФР", СертификатыКорневыеПФР) Тогда
		Для Каждого СтрКорневойСертификатПФР Из СертификатыКорневыеПФР Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрКорневойСертификатПФР.Отпечаток, "ROOT", МассивСертификатовROOT) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКорневойПФР", , СтрКорневойСертификатПФР, "Корневой сертификат, используемый при обмене с СФР (бывш. ПФР), не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	
	// проверка не необходимость регистрации кросс-сертификатов ПФР
	СертификатыКроссПФР = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКРОССПФР", СертификатыКроссПФР) Тогда
		Для Каждого СтрКроссСертификатПФР Из СертификатыКроссПФР Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрКроссСертификатПФР.Отпечаток, "CA", МассивСертификатовCA) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКРОССПФР", , СтрКроссСертификатПФР, "Кросс-сертификат, используемый при обмене с СФР (бывш. ПФР), не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	
	// проверка не необходимость регистрации корневых сертификатов ФСГС
	СертификатыКорневыеФСГС = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКорневыеФСГС", СертификатыКорневыеФСГС) Тогда
		Для Каждого СтрКорневойСертификатФСГС Из СертификатыКорневыеФСГС Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрКорневойСертификатФСГС.Отпечаток, "ROOT", МассивСертификатовROOT) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКорневойФСГС", , СтрКорневойСертификатФСГС, "Корневой сертификат, используемый при обмене с Росстатом, не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// проверка не необходимость регистрации кросс-сертификатов ФСГС
	СертификатыКроссФСГС = Неопределено;
	Если НастройкиРОКИ.Свойство("СертификатыКРОССФСГС", СертификатыКроссФСГС) Тогда
		Для Каждого СтрКроссСертификатФСГС Из СертификатыКроссФСГС Цикл
			Если НЕ СертификатЗарегистрированВХранилище(СтрКроссСертификатФСГС.Отпечаток, "CA", МассивСертификатовCA) Тогда
				ОпределитьИзменение(Исправления, "ЗарегистрироватьСертификатКРОССФСГС", , СтрКроссСертификатФСГС, "Кросс-сертификат, используемый при обмене с Росстатом, не зарегистрирован в системе");
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// возвращаем таблицу-результат
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Исправления);
	
КонецПроцедуры

&НаСервере
Функция СформироватьСписокНастроекРОКИПоДеревуНастроекXML(ДеревоНастроекАдрес, Сообщения)
	
	ДеревоНастроек = ПолучитьИзВременногоХранилища(ДеревоНастроекАдрес);
	Результат = Новый Структура;
	
	// позиционируемся на сведениях об абоненте
	УзелRegistrationFile = НайтиПодчиненныйУзел(ДеревоНастроек, "RegistrationFile");
	Если УзелRegistrationFile = Неопределено Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректная структура файла настроек!'"));
		Возврат Результат;
	КонецЕсли;
	
	УзелRegistration = НайтиПодчиненныйУзел(УзелRegistrationFile, "Registration");
	Если УзелRegistration = Неопределено Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректная структура файла настроек!'"));
		Возврат Результат;
	КонецЕсли;
	
	УзелOperator = НайтиПодчиненныйУзел(УзелRegistration, "Operator");
	Если УзелOperator <> Неопределено Тогда
		УзелOperatorPFRID = НайтиПодчиненныйУзел(УзелOperator, "OperatorPFRID");
		ОператорРегНомерПФР = СокрЛП(УзелOperatorPFRID.Значение);
		Результат.Вставить("ОператорРегНомерПФР", ОператорРегНомерПФР);
		УзелOperatorINN = НайтиПодчиненныйУзел(УзелOperator, "OperatorINN");
		ОператорИНН = СокрЛП(УзелOperatorINN.Значение);
		Результат.Вставить("ОператорИНН", ОператорИНН);
		УзелOperatorKPP = НайтиПодчиненныйУзел(УзелOperator, "OperatorKPP");
		ОператорКПП = СокрЛП(УзелOperatorKPP.Значение);
		Результат.Вставить("ОператорКПП", ОператорКПП);
		УзелOperatorName = НайтиПодчиненныйУзел(УзелOperator, "OperatorName");
		ОператорНаименованиеПолное = СокрЛП(УзелOperatorName.Значение);
		Результат.Вставить("ОператорНаименованиеПолное", ОператорНаименованиеПолное);
		УзелOperatorShortName = НайтиПодчиненныйУзел(УзелOperator, "OperatorShortName");
		ОператорНаименованиеКраткое = СокрЛП(УзелOperatorShortName.Значение);
		Результат.Вставить("ОператорНаименованиеКраткое", ОператорНаименованиеКраткое);
	КонецЕсли;
	
	УзелAbonent = НайтиПодчиненныйУзел(УзелRegistration, "Abonent");
	Если УзелAbonent = Неопределено Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректная структура файла настроек!'"));
		Возврат Результат;
	КонецЕсли;
	
	// заполняем пароль
	УзелIdentifier = НайтиПодчиненныйУзел(УзелAbonent, "Identifier");
	Если УзелIdentifier <> Неопределено Тогда
		ИдентификаторАбонента = СокрЛП(УзелIdentifier.Значение);
		Результат.Вставить("ИдентификаторАбонента", ИдентификаторАбонента);
		Результат.Вставить("ИдентификаторСпецоператора", Лев(ИдентификаторАбонента, 3));
	КонецЕсли;
	
	УзелAbsos = НайтиПодчиненныйУзел(УзелAbonent, "AbSos");
	Если УзелAbsos = Неопределено Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректная структура файла настроек!'"));
		Возврат Результат;
	КонецЕсли;
	
	
	// заполняем параметры сервера POP3
	ПараметрыСервераPOP3 = РазложитьСтрокуАдресаСервера(НайтиПодчиненныйУзел(УзелAbsos, "AbSOS_POP3"));
	Если ПараметрыСервераPOP3 <> Неопределено Тогда
		Результат.Вставить("СерверPOP3", ПараметрыСервераPOP3.Сервер);
		Результат.Вставить("ПортPOP3", ПараметрыСервераPOP3.Порт);
	КонецЕсли;
	
	// заполняем параметры сервера SMTP
	ПараметрыСервераSMTP = РазложитьСтрокуАдресаСервера(НайтиПодчиненныйУзел(УзелAbsos, "AbSOS_SMTP"));
	Если ПараметрыСервераSMTP <> Неопределено Тогда
		Результат.Вставить("СерверSMTP", ПараметрыСервераSMTP.Сервер);
		Результат.Вставить("ПортSMTP", ПараметрыСервераSMTP.Порт);
	КонецЕсли;
	
	// заполняем адрес электронной почты и имя пользователя
	УзелEmailAbSOS = НайтиПодчиненныйУзел(УзелAbsos, "EmailAbSOS");
	Если УзелEmailAbSOS <> Неопределено Тогда
		Результат.Вставить("АдресЭлектроннойПочты", СокрЛП(УзелEmailAbSOS.Значение));
		ИмяПользователя = ВыделитьИмяПользователяИзEmail(УзелEmailAbSOS);
		Если ЗначениеЗаполнено(ИмяПользователя) Тогда
			Результат.Вставить("ИмяПользователяPOP3", СокрЛП(ИмяПользователя));
		КонецЕсли;
	КонецЕсли;
	
	// заполняем пароль
	УзелAbSOSPSW = НайтиПодчиненныйУзел(УзелAbsos, "AbSOSPSW");
	Если УзелAbSOSPSW <> Неопределено Тогда
		Результат.Вставить("ПарольPOP3", СокрЛП(УзелAbSOSPSW.Значение));
	КонецЕсли;
	
	УзелEmailSosSOS = НайтиПодчиненныйУзел(УзелAbsos, "EmailSosSOS");
	Если УзелEmailSosSOS <> Неопределено Тогда
		Результат.Вставить("АдресСпецоператора", СокрЛП(УзелEmailSosSOS.Значение));
	КонецЕсли;
	
	
	// заполняем признак сдачи напрямую
	УзелPR_GPR = НайтиПодчиненныйУзел(УзелAbsos, "PR_GPR");
	Если УзелPR_GPR <> Неопределено Тогда
		Результат.Вставить("ПризнакСдачиНапрямую", ПреобразоватьXMLКЗначению("Булево", УзелPR_GPR.Значение));
	КонецЕсли;
	
	
	// заполняем сведения о сертификатах абонента
	СертификатыАбонента = Новый Массив;
	УзелCertificates = НайтиПодчиненныйУзел(УзелAbsos, "Certificates");
	Если УзелCertificates <> Неопределено Тогда
		УзлыCertificate_Ab = НайтиПодчиненныеУзлы(УзелCertificates, "Certificate_Ab");
		Для Каждого УзелCertificate_Ab Из УзлыCertificate_Ab Цикл
			СертификатыАбонента.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_Ab));
		КонецЦикла;
	КонецЕсли;
	Результат.Вставить("Сертификаты", СертификатыАбонента);
	
	
	// заполняем сведения об ИФНС
	ДатаСеанса = ТекущаяДатаСеанса();
	ИФНСы = Новый Массив;
	УзлыIFNS = НайтиПодчиненныеУзлы(УзелAbsos, "IFNS");
	Для Каждого УзелIFNS Из УзлыIFNS Цикл
		
		СтрИФНС_Код = Неопределено;
		СтрИФНС_Наименование = Неопределено;
		СтрИФНС_Сертификаты = Неопределено;
		СтрИФНС_ИФНСПоУмолчанию = Неопределено;
		СтрИФНС_СерверДокументооборота = Неопределено;
		СтрИФНС_СертификатПочтовогоСервера = Неопределено;
		
		УзелIFNS_CODE = НайтиПодчиненныйУзел(УзелIFNS, "IFNS_CODE");
		Если УзелIFNS_CODE <> Неопределено Тогда
			СтрИФНС_Код = СокрЛП(УзелIFNS_CODE.Значение);
		КонецЕсли;
		
		УзелIFNS_name = НайтиПодчиненныйУзел(УзелIFNS, "IFNS_name");
		Если УзелIFNS_name <> Неопределено Тогда
			СтрИФНС_Наименование = СокрЛП(УзелIFNS_name.Значение);
		КонецЕсли;
		
		СертификатыИФНС = Новый Массив;
		УзлыCertificate_IFNS = НайтиПодчиненныеУзлы(УзелIFNS, "Certificate_IFNS");
		Для Каждого УзелCertificate_IFNS Из УзлыCertificate_IFNS Цикл
			СертификатыИФНС.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_IFNS));
		КонецЦикла;
		Для НомерСертификатаСКонца = 1 По УзлыCertificate_IFNS.Количество() Цикл
			ИндексСертификата = УзлыCertificate_IFNS.Количество() - НомерСертификатаСКонца;
			Если (ДатаСеанса < СертификатыИФНС[ИндексСертификата].ДействителенС
				ИЛИ ДатаСеанса > СертификатыИФНС[ИндексСертификата].ДействителенПо)
				И СертификатыИФНС.Количество() > 1 Тогда
				СертификатыИФНС.Удалить(ИндексСертификата);
			КонецЕсли;
		КонецЦикла;
		СтрИФНС_Сертификаты = СертификатыИФНС;
		
		УзелDuty_EMail = НайтиПодчиненныйУзел(УзелIFNS, "Duty_EMail");
		Если УзелDuty_EMail <> Неопределено Тогда
			СтрИФНС_ИФНСПоУмолчанию = ПреобразоватьXMLКЗначению("Булево", УзелDuty_EMail.Значение);
		КонецЕсли;
		
		УзелPostserver = НайтиПодчиненныйУзел(УзелIFNS, "Postserver");
		Если УзелPostserver <> Неопределено Тогда
			УзелEmailPOST = НайтиПодчиненныйУзел(УзелPostserver, "EmailPOST");
			Если УзелEmailPOST <> Неопределено Тогда
				СтрИФНС_СерверДокументооборота = СокрЛП(УзелEmailPOST.Значение);
			КонецЕсли;
			УзелCertificate_Post = НайтиПодчиненныйУзел(УзелPostserver, "Certificate_Post");
			Если УзелCertificate_Post <> Неопределено Тогда
				СтрИФНС_СертификатПочтовогоСервера = СвойстваСертификатаПоУзлу(УзелCertificate_Post);
			КонецЕсли;
		КонецЕсли;
		
		ИФНСы.Добавить(Новый Структура("Код, Наименование, Сертификаты, ИФНСПоУмолчанию, СерверДокументооборота, СертификатПочтовогоСервера", СтрИФНС_Код,
			СтрИФНС_Наименование,
			СтрИФНС_Сертификаты,
			СтрИФНС_ИФНСПоУмолчанию,
			СтрИФНС_СерверДокументооборота,
			СтрИФНС_СертификатПочтовогоСервера));
		
	КонецЦикла;
	Результат.Вставить("ИФНСы", ИФНСы);
	
	
	// заполняем сведения об отделениях СФР (бывш. ПФР)
	КодыОтделенийПФР = Новый Массив;
	УзелPFR = НайтиПодчиненныйУзел(УзелAbsos, "PFR");
	Если ЗначениеЗаполнено(УзелPFR) Тогда
		
		// заполняем адрес оператора электронного документооборота для СФР (бывш. ПФР)
		УзелEmailSosPFR = НайтиПодчиненныйУзел(УзелPFR, "EmailSosPFR");
		Если УзелEmailSosPFR <> Неопределено Тогда
			Результат.Вставить("АдресСпецоператораПФР", СокрЛП(УзелEmailSosPFR.Значение));
		КонецЕсли;
		
		// заполняем таблицу отделений СФР (бывш. ПФР)
		ОтделенияПФР = Новый Массив;
		
		УзелDirections = НайтиПодчиненныйУзел(УзелPFR, "Directions");
		Если ЗначениеЗаполнено(УзелDirections) Тогда
			
			УзлыDirection = НайтиПодчиненныеУзлы(УзелDirections, "Direction");
			Для Каждого УзелDirection Из УзлыDirection Цикл
				
				НовоеОтделениеПФР_Код = Неопределено;
				НовоеОтделениеПФР_Наименование = Неопределено;
				НовоеОтделениеПФР_Сертификаты = Неопределено;
				
				УзелPFRYprCode = НайтиПодчиненныйУзел(УзелDirection, "PFRYprCode");
				Если УзелPFRYprCode <> Неопределено Тогда
					НовоеОтделениеПФР_Код = СокрЛП(УзелPFRYprCode.Значение);
				КонецЕсли;
				
				УзелPFRYprName = НайтиПодчиненныйУзел(УзелDirection, "PFRYprName");
				Если УзелPFRYprName <> Неопределено Тогда
					НовоеОтделениеПФР_Наименование = СокрЛП(УзелPFRYprName.Значение);
				КонецЕсли;
				
				СертификатыОтделенияПФР = Новый Массив;
				УзелPFRCerts = НайтиПодчиненныйУзел(УзелDirection, "PFRCerts");
				Если ЗначениеЗаполнено(УзелPFRCerts) Тогда
					
					УзлыCertificate_PFR = НайтиПодчиненныеУзлы(УзелPFRCerts, "Certificate_PFR");
					Для Каждого УзелCertificate_PFR Из УзлыCertificate_PFR Цикл
						СертификатыОтделенияПФР.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_PFR));
					КонецЦикла;
					Для НомерСертификатаСКонца = 1 По УзлыCertificate_PFR.Количество() Цикл
						ИндексСертификата = УзлыCertificate_PFR.Количество() - НомерСертификатаСКонца;
						Если (ДатаСеанса < СертификатыОтделенияПФР[ИндексСертификата].ДействителенС
							ИЛИ ДатаСеанса > СертификатыОтделенияПФР[ИндексСертификата].ДействителенПо)
							И СертификатыОтделенияПФР.Количество() > 1 Тогда
							СертификатыОтделенияПФР.Удалить(ИндексСертификата);
						КонецЕсли;
					КонецЦикла;
					
					НовоеОтделениеПФР_Сертификаты = СертификатыОтделенияПФР;
					
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(НовоеОтделениеПФР_Код) Тогда
					НовоеОтделениеПФР_Код = "ЕЦП";
				КонецЕсли;
				КодыОтделенийПФР.Добавить(НовоеОтделениеПФР_Код);
				ОтделенияПФР.Добавить(Новый Структура("Код, Наименование, Сертификаты, СертификатыЭДОК, СертификатыЕЦП",
					НовоеОтделениеПФР_Код, НовоеОтделениеПФР_Наименование, НовоеОтделениеПФР_Сертификаты, Новый Массив,
						Новый Массив));
				
			КонецЦикла;
			
		КонецЕсли;
		
		Результат.Вставить("ОтделенияПФР", ОтделенияПФР);
		
	КонецЕсли;
	
	// заполняем сведения о СФР (бывш. ПФР) ЭДОК
	УзелPFREDOK = НайтиПодчиненныйУзел(УзелAbsos, "PFREDOK");
	Если ЗначениеЗаполнено(УзелPFREDOK) Тогда
		
		// заполняем адрес оператора электронного документооборота для СФР (бывш. ПФР)
		Если НЕ Результат.Свойство("АдресСпецоператораПФР") ИЛИ НЕ ЗначениеЗаполнено(Результат.АдресСпецоператораПФР) Тогда
			УзелEmailSosPFR = НайтиПодчиненныйУзел(УзелPFREDOK, "EmailSosPFR");
			Если УзелEmailSosPFR <> Неопределено Тогда
				Результат.Вставить("АдресСпецоператораПФР", СокрЛП(УзелEmailSosPFR.Значение));
			КонецЕсли;
		КонецЕсли;
		
		// заполняем таблицу отделений СФР (бывш. ПФР) ЭДОК
		ОтделенияПФРЭДОК = Новый Массив;
		ЕдиныеСертификатыПФРЭДОК = Новый Массив;
		
		УзелDirections = НайтиПодчиненныйУзел(УзелPFREDOK, "Directions");
		Если ЗначениеЗаполнено(УзелDirections) Тогда
			
			УзлыDirection = НайтиПодчиненныеУзлы(УзелDirections, "Direction");
			Для Каждого УзелDirection Из УзлыDirection Цикл
				
				НовоеОтделениеПФР_Код = Неопределено;
				НовоеОтделениеПФР_Наименование = Неопределено;
				НовоеОтделениеПФР_Сертификаты = Неопределено;
				
				УзелPFRYprCode = НайтиПодчиненныйУзел(УзелDirection, "PFRYprCode");
				Если УзелPFRYprCode <> Неопределено Тогда
					НовоеОтделениеПФР_Код = СокрЛП(УзелPFRYprCode.Значение);
				КонецЕсли;
				
				УзелPFRYprName = НайтиПодчиненныйУзел(УзелDirection, "PFRYprName");
				Если УзелPFRYprName <> Неопределено Тогда
					НовоеОтделениеПФР_Наименование = СокрЛП(УзелPFRYprName.Значение);
				КонецЕсли;
				
				СертификатыОтделенияПФР = Новый Массив;
				УзелPFRCerts = НайтиПодчиненныйУзел(УзелDirection, "PFRCerts");
				Если ЗначениеЗаполнено(УзелPFRCerts) Тогда
					
					УзлыCertificate_PFR = НайтиПодчиненныеУзлы(УзелPFRCerts, "Certificate_PFR");
					Для Каждого УзелCertificate_PFR Из УзлыCertificate_PFR Цикл
						СертификатыОтделенияПФР.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_PFR));
					КонецЦикла;
					Для НомерСертификатаСКонца = 1 По УзлыCertificate_PFR.Количество() Цикл
						ИндексСертификата = УзлыCertificate_PFR.Количество() - НомерСертификатаСКонца;
						Если (ДатаСеанса < СертификатыОтделенияПФР[ИндексСертификата].ДействителенС
							ИЛИ ДатаСеанса > СертификатыОтделенияПФР[ИндексСертификата].ДействителенПо)
							И СертификатыОтделенияПФР.Количество() > 1 Тогда
							СертификатыОтделенияПФР.Удалить(ИндексСертификата);
						КонецЕсли;
					КонецЦикла;
					
					НовоеОтделениеПФР_Сертификаты = СертификатыОтделенияПФР;
					
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(НовоеОтделениеПФР_Код) Тогда
					НовоеОтделениеПФР_Код = "ЕЦП";
				КонецЕсли;
				ОтделенияПФРЭДОК.Добавить(Новый Структура("Код, Наименование, Сертификаты, СертификатыЭДОК, СертификатыЕЦП",
					НовоеОтделениеПФР_Код, НовоеОтделениеПФР_Наименование, Новый Массив, НовоеОтделениеПФР_Сертификаты,
						Новый Массив));
				Если НовоеОтделениеПФР_Код = "ЕЦП" Тогда
					ЕдиныеСертификатыПФРЭДОК = НовоеОтделениеПФР_Сертификаты;
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
		Если НЕ Результат.Свойство("ОтделенияПФР") ИЛИ Результат.ОтделенияПФР.Количество() = 0 Тогда
			Результат.Вставить("ОтделенияПФР", ОтделенияПФРЭДОК);
			
		Иначе
			Для каждого ОтделениеПФРЭДОК Из ОтделенияПФРЭДОК Цикл
				ИндексОтделенияПФР = КодыОтделенийПФР.Найти(ОтделениеПФРЭДОК.Код);
				Если ИндексОтделенияПФР <> Неопределено Тогда
					Результат.ОтделенияПФР[ИндексОтделенияПФР].СертификатыЭДОК = ОтделениеПФРЭДОК.СертификатыЭДОК;
					ОтделениеПФРЭДОК.СертификатыЭДОК = Неопределено;
				КонецЕсли;
			КонецЦикла;
			
			Для каждого ОтделениеПФРЭДОК Из ОтделенияПФРЭДОК Цикл
				Если ОтделениеПФРЭДОК.СертификатыЭДОК <> Неопределено Тогда
					НовоеОтделениеПФР = ОбщегоНазначения.СкопироватьРекурсивно(ОтделениеПФРЭДОК);
					Результат.ОтделенияПФР.Добавить(НовоеОтделениеПФР);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Для каждого ОтделениеПФР Из Результат.ОтделенияПФР Цикл
			Если ОтделениеПФР.СертификатыЭДОК.Количество() = 0 Тогда
				ОтделениеПФР.СертификатыЭДОК = ЕдиныеСертификатыПФРЭДОК;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	// заполняем сведения о СФР (бывш. ПФР) ЕЦП
	Результат.Вставить("ИспользуетсяЕЦППФР", Ложь);
	Результат.Вставить("ТипыДокументовЕЦППФР", "");
	УзелPFRETSP = НайтиПодчиненныйУзел(УзелAbsos, "PFRETSP");
	Если ЗначениеЗаполнено(УзелPFRETSP) Тогда
		
		// заполняем адрес оператора электронного документооборота для СФР (бывш. ПФР)
		Если НЕ Результат.Свойство("АдресСпецоператораПФР") ИЛИ НЕ ЗначениеЗаполнено(Результат.АдресСпецоператораПФР) Тогда
			УзелEmailSosPFR = НайтиПодчиненныйУзел(УзелPFRETSP, "EmailSosPFR");
			Если УзелEmailSosPFR <> Неопределено Тогда
				Результат.Вставить("АдресСпецоператораПФР", СокрЛП(УзелEmailSosPFR.Значение));
			КонецЕсли;
		КонецЕсли;
		
		// заполняем типы документов оператора электронного документооборота для СФР (бывш. ПФР) ЕЦП
		Результат.Вставить("ИспользуетсяЕЦППФР", Истина);
		Если НЕ Результат.Свойство("ТипыДокументовЕЦППФР") ИЛИ НЕ ЗначениеЗаполнено(Результат.ТипыДокументовЕЦППФР) Тогда
			УзелDocTypes = НайтиПодчиненныйУзел(УзелPFRETSP, "DocTypes");
			Если УзелDocTypes <> Неопределено Тогда
				Результат.Вставить("ТипыДокументовЕЦППФР", СокрЛП(УзелDocTypes.Значение));
			КонецЕсли;
		КонецЕсли;
		
		// заполняем таблицу отделений СФР (бывш. ПФР) ЕЦП
		ОтделенияПФРЕЦП = Новый Массив;
		ЕдиныеСертификатыПФРЕЦП = Новый Массив;
		
		УзелDirections = НайтиПодчиненныйУзел(УзелPFRETSP, "Directions");
		Если ЗначениеЗаполнено(УзелDirections) Тогда
			
			УзлыDirection = НайтиПодчиненныеУзлы(УзелDirections, "Direction");
			Для Каждого УзелDirection Из УзлыDirection Цикл
				
				НовоеОтделениеПФР_Код = Неопределено;
				НовоеОтделениеПФР_Наименование = Неопределено;
				НовоеОтделениеПФР_Сертификаты = Неопределено;
				
				УзелPFRYprCode = НайтиПодчиненныйУзел(УзелDirection, "PFRYprCode");
				Если УзелPFRYprCode <> Неопределено Тогда
					НовоеОтделениеПФР_Код = СокрЛП(УзелPFRYprCode.Значение);
				КонецЕсли;
				
				УзелPFRYprName = НайтиПодчиненныйУзел(УзелDirection, "PFRYprName");
				Если УзелPFRYprName <> Неопределено Тогда
					НовоеОтделениеПФР_Наименование = СокрЛП(УзелPFRYprName.Значение);
				КонецЕсли;
				
				СертификатыОтделенияПФР = Новый Массив;
				УзелPFRCerts = НайтиПодчиненныйУзел(УзелDirection, "PFRCerts");
				Если ЗначениеЗаполнено(УзелPFRCerts) Тогда
					
					УзлыCertificate_PFR = НайтиПодчиненныеУзлы(УзелPFRCerts, "Certificate_PFR");
					Для Каждого УзелCertificate_PFR Из УзлыCertificate_PFR Цикл
						СертификатыОтделенияПФР.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_PFR));
					КонецЦикла;
					Для НомерСертификатаСКонца = 1 По УзлыCertificate_PFR.Количество() Цикл
						ИндексСертификата = УзлыCertificate_PFR.Количество() - НомерСертификатаСКонца;
						Если (ДатаСеанса < СертификатыОтделенияПФР[ИндексСертификата].ДействителенС
							ИЛИ ДатаСеанса > СертификатыОтделенияПФР[ИндексСертификата].ДействителенПо)
							И СертификатыОтделенияПФР.Количество() > 1 Тогда
							СертификатыОтделенияПФР.Удалить(ИндексСертификата);
						КонецЕсли;
					КонецЦикла;
					
					НовоеОтделениеПФР_Сертификаты = СертификатыОтделенияПФР;
					
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(НовоеОтделениеПФР_Код) Тогда
					НовоеОтделениеПФР_Код = "ЕЦП";
				КонецЕсли;
				ОтделенияПФРЕЦП.Добавить(Новый Структура("Код, Наименование, Сертификаты, СертификатыЭДОК, СертификатыЕЦП",
					НовоеОтделениеПФР_Код, НовоеОтделениеПФР_Наименование, Новый Массив, Новый Массив,
						НовоеОтделениеПФР_Сертификаты));
				Если НовоеОтделениеПФР_Код = "ЕЦП" Тогда
					ЕдиныеСертификатыПФРЕЦП = НовоеОтделениеПФР_Сертификаты;
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
		Если НЕ Результат.Свойство("ОтделенияПФР") ИЛИ Результат.ОтделенияПФР.Количество() = 0 Тогда
			Результат.Вставить("ОтделенияПФР", ОтделенияПФРЕЦП);
			
		Иначе
			Для каждого ОтделениеПФРЕЦП Из ОтделенияПФРЕЦП Цикл
				ИндексОтделенияПФР = КодыОтделенийПФР.Найти(ОтделениеПФРЕЦП.Код);
				Если ИндексОтделенияПФР <> Неопределено Тогда
					Результат.ОтделенияПФР[ИндексОтделенияПФР].СертификатыЕЦП = ОтделениеПФРЕЦП.СертификатыЕЦП;
					ОтделениеПФРЕЦП.СертификатыЕЦП = Неопределено;
				КонецЕсли;
			КонецЦикла;
			
			Для каждого ОтделениеПФРЕЦП Из ОтделенияПФРЕЦП Цикл
				Если ОтделениеПФРЕЦП.СертификатыЕЦП <> Неопределено Тогда
					НовоеОтделениеПФР = ОбщегоНазначения.СкопироватьРекурсивно(ОтделениеПФРЕЦП);
					Результат.ОтделенияПФР.Добавить(НовоеОтделениеПФР);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Для каждого ОтделениеПФР Из Результат.ОтделенияПФР Цикл
			Если ОтделениеПФР.СертификатыЕЦП.Количество() = 0 Тогда
				ОтделениеПФР.СертификатыЕЦП = ЕдиныеСертификатыПФРЕЦП;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	// заполняем сведения об отделениях ФСГС
	УзелFSGS = НайтиПодчиненныйУзел(УзелAbsos, "FSGS");
	Если ЗначениеЗаполнено(УзелFSGS) Тогда
		
		// заполняем адрес оператора электронного документооборота для ФСГС
		УзелEmailSosFSGS = НайтиПодчиненныйУзел(УзелFSGS, "EmailSosFSGS");
		Если УзелEmailSosFSGS <> Неопределено Тогда
			Результат.Вставить("АдресСпецоператораФСГС", СокрЛП(УзелEmailSosFSGS.Значение));
		КонецЕсли;
		
		// заполняем таблицу отделений ФСГС
		ОтделенияФСГС = Новый Массив;
		
		УзелDirections = НайтиПодчиненныйУзел(УзелFSGS, "Directions");
		Если ЗначениеЗаполнено(УзелDirections) Тогда
			
			УзлыDirection = НайтиПодчиненныеУзлы(УзелDirections, "Direction");
			Для Каждого УзелDirection Из УзлыDirection Цикл
				
				НовоеОтделениеФСГС_Код = Неопределено;
				НовоеОтделениеФСГС_Наименование = Неопределено;
				НовоеОтделениеФСГС_Сертификаты = Неопределено;
				
				УзелFSGSOrgCode = НайтиПодчиненныйУзел(УзелDirection, "FSGSOrgCode");
				Если УзелFSGSOrgCode <> Неопределено Тогда
					НовоеОтделениеФСГС_Код = СокрЛП(УзелFSGSOrgCode.Значение);
				КонецЕсли;
				
				УзелFSGSOrgName = НайтиПодчиненныйУзел(УзелDirection, "FSGSOrgName");
				Если УзелFSGSOrgName <> Неопределено Тогда
					НовоеОтделениеФСГС_Наименование = СокрЛП(УзелFSGSOrgName.Значение);
				КонецЕсли;
				
				УзелFSGSCerts = НайтиПодчиненныйУзел(УзелDirection, "FSGSCerts");
				Если ЗначениеЗаполнено(УзелFSGSCerts) Тогда
					
					СертификатыОтделенияФСГС = Новый Массив;
					
					УзлыCertificate_FSGS = НайтиПодчиненныеУзлы(УзелFSGSCerts, "Certificate_FSGS");
					Для Каждого УзелCertificate_FSGS Из УзлыCertificate_FSGS Цикл
						СертификатыОтделенияФСГС.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_FSGS));
					КонецЦикла;
					Для НомерСертификатаСКонца = 1 По УзлыCertificate_FSGS.Количество() Цикл
						ИндексСертификата = УзлыCertificate_FSGS.Количество() - НомерСертификатаСКонца;
						Если (ДатаСеанса < СертификатыОтделенияФСГС[ИндексСертификата].ДействителенС
							ИЛИ ДатаСеанса > СертификатыОтделенияФСГС[ИндексСертификата].ДействителенПо)
							И СертификатыОтделенияФСГС.Количество() > 1 Тогда
							СертификатыОтделенияФСГС.Удалить(ИндексСертификата);
						КонецЕсли;
					КонецЦикла;
					
					НовоеОтделениеФСГС_Сертификаты = СертификатыОтделенияФСГС;
					
				КонецЕсли;
				
				ОтделенияФСГС.Добавить(Новый Структура("Код, Наименование, Сертификаты",
					НовоеОтделениеФСГС_Код, НовоеОтделениеФСГС_Наименование, НовоеОтделениеФСГС_Сертификаты));
				
			КонецЦикла;
			
		КонецЕсли;
		
		Результат.Вставить("ОтделенияФСГС", ОтделенияФСГС);
		
	КонецЕсли;
	
	// заполняем сведения о настройках ФСС
	УзелFSS = НайтиПодчиненныйУзел(УзелAbsos, "FSS");
	Результат.Вставить("ФССИспользуется", ЗначениеЗаполнено(УзелFSS));
	Если ЗначениеЗаполнено(УзелFSS) Тогда
		
		// заполняем регистрационный номер ФСС
		УзелRNS_FSS = НайтиПодчиненныйУзел(УзелFSS, "RNS_FSS");
		Если ЗначениеЗаполнено(УзелRNS_FSS) Тогда
			Результат.Вставить("РегистрационныйНомерФСС", СокрЛП(УзелRNS_FSS.Значение));
		КонецЕсли;
		
		СертификатыФСС = Новый Массив;
		
		УзлыCert_4FSS = НайтиПодчиненныеУзлы(УзелFSS, "Cert_4FSS");
		Для каждого УзелCert_4FSS Из УзлыCert_4FSS Цикл
			СертификатыФСС.Добавить(СвойстваСертификатаПоУзлу(УзелCert_4FSS));
		КонецЦикла;
		Для НомерСертификатаСКонца = 1 По УзлыCert_4FSS.Количество() Цикл
			ИндексСертификата = УзлыCert_4FSS.Количество() - НомерСертификатаСКонца;
			Если (ДатаСеанса < СертификатыФСС[ИндексСертификата].ДействителенС
				ИЛИ ДатаСеанса > СертификатыФСС[ИндексСертификата].ДействителенПо)
				И СертификатыФСС.Количество() > 1 Тогда
				СертификатыФСС.Удалить(ИндексСертификата);
			КонецЕсли;
		КонецЦикла;
		
		Если СертификатыФСС.Количество() > 0 Тогда
			Результат.Вставить("СертификатФСС", СертификатыФСС[0]);
		КонецЕсли;
		
		СертификатыФССЭЛН = Новый Массив;
		
		УзлыCert_ELN = НайтиПодчиненныеУзлы(УзелFSS, "Cert_ELN");
		Для каждого УзелCert_ELN Из УзлыCert_ELN Цикл
			СертификатыФССЭЛН.Добавить(СвойстваСертификатаПоУзлу(УзелCert_ELN));
		КонецЦикла;
		УзлыCert_POVED = НайтиПодчиненныеУзлы(УзелFSS, "Cert_POVED");
		Для каждого УзелCert_POVED Из УзлыCert_POVED Цикл
			СертификатыФССЭЛН.Добавить(СвойстваСертификатаПоУзлу(УзелCert_POVED));
		КонецЦикла;
		Для НомерСертификатаСКонца = 1 По УзлыCert_ELN.Количество() + УзлыCert_POVED.Количество() Цикл
			ИндексСертификата = УзлыCert_ELN.Количество() + УзлыCert_POVED.Количество() - НомерСертификатаСКонца;
			Если (ДатаСеанса < СертификатыФССЭЛН[ИндексСертификата].ДействителенС
				ИЛИ ДатаСеанса > СертификатыФССЭЛН[ИндексСертификата].ДействителенПо)
				И СертификатыФССЭЛН.Количество() > 1 Тогда
				СертификатыФССЭЛН.Удалить(ИндексСертификата);
			КонецЕсли;
		КонецЦикла;
		
		Если СертификатыФССЭЛН.Количество() > 0 Тогда
			Результат.Вставить("СертификатФССЭЛН", СертификатыФССЭЛН[0]);
		КонецЕсли;
		
	КонецЕсли;
	
	// заполняем сведения о настройках ФСРАР
	УзелFSRAR = НайтиПодчиненныйУзел(УзелAbsos, "FSRAR");
	Результат.Вставить("ФСРАРИспользуется", ЗначениеЗаполнено(УзелFSRAR));
	Если ЗначениеЗаполнено(УзелFSRAR) Тогда
		
		// заполняем код региона для ФСРАР
		УзелRegion = НайтиПодчиненныйУзел(УзелFSRAR, "Region");
		Если ЗначениеЗаполнено(УзелRegion) Тогда
			Результат.Вставить("КодРегионаФСРАР", СокрЛП(УзелRegion.Значение));
		КонецЕсли;
		
		СертификатыФСРАР = Новый Массив;
		
		УзлыCert_RAR = НайтиПодчиненныеУзлы(УзелFSRAR, "Cert_RAR");
		Для каждого УзелCert_RAR Из УзлыCert_RAR Цикл
			СертификатыФСРАР.Добавить(СвойстваСертификатаПоУзлу(УзелCert_RAR));
		КонецЦикла;
		Для НомерСертификатаСКонца = 1 По УзлыCert_RAR.Количество() Цикл
			ИндексСертификата = УзлыCert_RAR.Количество() - НомерСертификатаСКонца;
			Если (ДатаСеанса < СертификатыФСРАР[ИндексСертификата].ДействителенС
				ИЛИ ДатаСеанса > СертификатыФСРАР[ИндексСертификата].ДействителенПо)
				И СертификатыФСРАР.Количество() > 1 Тогда
				СертификатыФСРАР.Удалить(ИндексСертификата);
			КонецЕсли;
		КонецЦикла;
		
		Если СертификатыФСРАР.Количество() > 0 Тогда
			Результат.Вставить("СертификатФСРАР", СертификатыФСРАР[0]);
		КонецЕсли;
		
		СертификатыФСРАРСубъектовРФ = Новый Массив;
		
		УзлыCert_Region = НайтиПодчиненныеУзлы(УзелFSRAR, "Cert_Region");
		Для каждого УзелCert_Region Из УзлыCert_Region Цикл
			СертификатыФСРАРСубъектовРФ.Добавить(СвойстваСертификатаПоУзлу(УзелCert_Region));
		КонецЦикла;
		Для НомерСертификатаСКонца = 1 По УзлыCert_Region.Количество() Цикл
			ИндексСертификата = УзлыCert_Region.Количество() - НомерСертификатаСКонца;
			Если (ДатаСеанса < СертификатыФСРАРСубъектовРФ[ИндексСертификата].ДействителенС
				ИЛИ ДатаСеанса > СертификатыФСРАРСубъектовРФ[ИндексСертификата].ДействителенПо)
				И СертификатыФСРАРСубъектовРФ.Количество() > 1 Тогда
				СертификатыФСРАРСубъектовРФ.Удалить(ИндексСертификата);
			КонецЕсли;
		КонецЦикла;
		
		Если СертификатыФСРАРСубъектовРФ.Количество() > 0 Тогда
			Результат.Вставить("СертификатФСРАРСубъектаРФ", СертификатыФСРАРСубъектовРФ[0]);
		КонецЕсли;
		
	КонецЕсли;
	
	// заполняем сведения о настройках РПН
	УзелRPN = НайтиПодчиненныйУзел(УзелAbsos, "RPN");
	Результат.Вставить("РПНИспользуется", ЗначениеЗаполнено(УзелRPN));
	Если ЗначениеЗаполнено(УзелRPN) Тогда
		
		СертификатыРПН = Новый Массив;
		
		УзлыCert_RPN = НайтиПодчиненныеУзлы(УзелRPN, "Cert_RPN");
		Для каждого УзелCert_RPN Из УзлыCert_RPN Цикл
			СертификатыРПН.Добавить(СвойстваСертификатаПоУзлу(УзелCert_RPN));
		КонецЦикла;
		Для НомерСертификатаСКонца = 1 По УзлыCert_RPN.Количество() Цикл
			ИндексСертификата = УзлыCert_RPN.Количество() - НомерСертификатаСКонца;
			Если (ДатаСеанса < СертификатыРПН[ИндексСертификата].ДействителенС
				ИЛИ ДатаСеанса > СертификатыРПН[ИндексСертификата].ДействителенПо)
				И СертификатыРПН.Количество() > 1 Тогда
				СертификатыРПН.Удалить(ИндексСертификата);
			КонецЕсли;
		КонецЦикла;
		
		Если СертификатыРПН.Количество() > 0 Тогда
			Результат.Вставить("СертификатРПН", СертификатыРПН[0]);
		КонецЕсли;
		
	КонецЕсли;
	
	// заполняем сведения о настройках ФТС
	УзелFTS = НайтиПодчиненныйУзел(УзелAbsos, "FTS");
	Результат.Вставить("ФТСИспользуется", ЗначениеЗаполнено(УзелFTS));
	
	// заполняем родительские сертификаты
	УзелCert_CA = НайтиПодчиненныйУзел(УзелRegistration, "Cert_CA");
	Если УзелCert_CA <> Неопределено Тогда
		
		// узлы с корневыми сертификатами ГНИВЦ
		СертификатыКорневыеГНИВЦ = Новый Массив;
		УзлыCert_ROOT_GNIVC = НайтиПодчиненныеУзлы(УзелCert_CA, "Cert_ROOT_GNIVC");
		Для Каждого УзелCert_ROOT_GNIVC Из УзлыCert_ROOT_GNIVC Цикл
			НовыйСертификатКорневойГНИВЦ = СвойстваСертификатаПоУзлу(УзелCert_ROOT_GNIVC);
			СертификатыКорневыеГНИВЦ.Добавить(НовыйСертификатКорневойГНИВЦ);
		КонецЦикла;
		Результат.Вставить("СертификатыКорневыеГНИВЦ", СертификатыКорневыеГНИВЦ);
		
		// узлы с корневыми сертификатами СОС
		СертификатыКорневыеТакском = Новый Массив;
		УзлыCert_ROOT_SOS = НайтиПодчиненныеУзлы(УзелCert_CA, "Cert_ROOT_SOS");
		Для Каждого УзелCert_ROOT_SOS Из УзлыCert_ROOT_SOS Цикл
			НовыйСертификатКорневойТакском = СвойстваСертификатаПоУзлу(УзелCert_ROOT_SOS);
			СертификатыКорневыеТакском.Добавить(НовыйСертификатКорневойТакском);
		КонецЦикла;
		Результат.Вставить("СертификатыКорневыеТакском", СертификатыКорневыеТакском);
		
		// узлы с сертификатами СОС
		УзлыCert_SOS = НайтиПодчиненныеУзлы(УзелCert_CA, "Cert_SOS");
		Для Каждого УзелCert_SOS Из УзлыCert_SOS Цикл
			Результат.Вставить("СертификатСОС", СвойстваСертификатаПоУзлу(УзелCert_SOS));
		КонецЦикла;
		
		// узлы с кросс-сертификатами
		СертификатыКРОСС = Новый Массив;
		УзлыCert_CROSS = НайтиПодчиненныеУзлы(УзелCert_CA, "Cert_CROSS");
		Для Каждого УзелCert_CROSS Из УзлыCert_CROSS Цикл
			НовыйСертификатКРОСС = СвойстваСертификатаПоУзлу(УзелCert_CROSS);
			СертификатыКРОСС.Добавить(НовыйСертификатКРОСС);
		КонецЦикла;
		Результат.Вставить("СертификатыКРОСС", СертификатыКРОСС);
		
	КонецЕсли;
	
	
	// заполняем сертификаты ПФР (их может не быть, если обмен с ПФР не задействован по учетной записи)
	УзелCertPFR = НайтиПодчиненныйУзел(УзелRegistration, "Cert_PFR");
	Если УзелCertPFR <> Неопределено Тогда
		
		// заполняем таблицу корневых сертификатов ПФР
		КорневыеСертификатыПФР = Новый Массив;
		УзелRoot = НайтиПодчиненныйУзел(УзелCertPFR, "Root");
		Если УзелRoot <> Неопределено Тогда
			УзлыCertificate_PFR = НайтиПодчиненныеУзлы(УзелRoot, "Certificate_PFR");
			Для Каждого УзелCertificate_PFR Из УзлыCertificate_PFR Цикл
				КорневыеСертификатыПФР.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_PFR));
			КонецЦикла;
		КонецЕсли;
		Результат.Вставить("СертификатыКорневыеПФР", КорневыеСертификатыПФР);
		
		// заполняем таблицу кросс-сертификатов ПФР
		КроссСертификатыПФР = Новый Массив;
		УзелCROSS = НайтиПодчиненныйУзел(УзелCertPFR, "CROSS");
		Если УзелCROSS <> Неопределено Тогда
			УзлыCertificate_PFR = НайтиПодчиненныеУзлы(УзелCROSS, "Certificate_PFR");
			Для Каждого УзелCertificate_PFR Из УзлыCertificate_PFR Цикл
				КроссСертификатыПФР.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_PFR));
			КонецЦикла;
		КонецЕсли;
		Результат.Вставить("СертификатыКРОССПФР", КроссСертификатыПФР);
		
	КонецЕсли;
	
	// заполняем сертификаты ФСГС (их может не быть, если обмен с ФСГС не задействован по учетной записи)
	УзелCertFSGS = НайтиПодчиненныйУзел(УзелRegistration, "Cert_FSGS");
	Если УзелCertFSGS <> Неопределено Тогда
		
		// заполняем таблицу корневых сертификатов ФСГС
		КорневыеСертификатыФСГС = Новый Массив;
		УзелRoot = НайтиПодчиненныйУзел(УзелCertFSGS, "Root");
		Если УзелRoot <> Неопределено Тогда
			УзлыCertificate_FSGS = НайтиПодчиненныеУзлы(УзелRoot, "Certificate_FSGS");
			Для Каждого УзелCertificate_FSGS Из УзлыCertificate_FSGS Цикл
				КорневыеСертификатыФСГС.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_FSGS));
			КонецЦикла;
		КонецЕсли;
		Результат.Вставить("СертификатыКорневыеФСГС", КорневыеСертификатыФСГС);
		
		// заполняем таблицу кросс-сертификатов ФСГС
		КроссСертификатыФСГС = Новый Массив;
		УзелCROSS = НайтиПодчиненныйУзел(УзелCertFSGS, "CROSS");
		Если УзелCROSS <> Неопределено Тогда
			УзлыCertificate_FSGS = НайтиПодчиненныеУзлы(УзелCROSS, "Certificate_FSGS");
			Для Каждого УзелCertificate_FSGS Из УзлыCertificate_FSGS Цикл
				КроссСертификатыФСГС.Добавить(СвойстваСертификатаПоУзлу(УзелCertificate_FSGS));
			КонецЦикла;
		КонецЕсли;
		Результат.Вставить("СертификатыКРОССФСГС", КроссСертификатыФСГС);
		
	КонецЕсли;
	
	// инициализируем признаки обмена с ФНС, ПФР и Росстат
	Результат.Вставить("ПредназначенаДляДокументооборотаСФНС", УзлыIFNS.Количество() > 0);
	Результат.Вставить("ПредназначенаДляДокументооборотаСПФР", ЗначениеЗаполнено(УзелPFR) ИЛИ ЗначениеЗаполнено(УзелPFREDOK) ИЛИ ЗначениеЗаполнено(УзелPFRETSP));
	Результат.Вставить("ПредназначенаДляДокументооборотаСФСГС", ЗначениеЗаполнено(УзелFSGS));
	
	// возвращаем результат
	Возврат Результат;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Работа с криптографией

&НаСервереБезКонтекста
Функция ПолучитьДанныеСтрокой64(ДвДанные) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьДанныеСтрокой64(ДвДанные);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСтроку64ИзХранилища(ДанныеИзХранилища) Экспорт 
	
	Если ТипЗнч(ДанныеИзХранилища) = Тип("ХранилищеЗначения") Тогда 
		Возврат ПолучитьДанныеСтрокой64(ДанныеИзХранилища.Получить());
	Иначе 
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьАдресСтрокой64(АдресДанных) Экспорт
	
	ДвДанные = ПолучитьИзВременногоХранилища(АдресДанных);
	Возврат ПолучитьДанныеСтрокой64(ДвДанные);
	
КонецФункции

&НаСервере
Функция ПолучитьЗакэшированныйФайлНастроек(
		УчетнаяЗапись,
		ПараметрыСоединения,
		ДатаОбновленияФайлаНаСервере,
		Расшифрован,
		Сообщения)
	
	Если ТипЗнч(УчетнаяЗапись) <> Тип("СправочникСсылка.УчетныеЗаписиДокументооборота") Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
	                      |	ФайлыСведенийРОКИ.Данные,
	                      |	ФайлыСведенийРОКИ.Расшифрован,
	                      |	ФайлыСведенийРОКИ.ДатаФайла
	                      |ИЗ
	                      |	РегистрСведений.ФайлыСведенийРОКИ КАК ФайлыСведенийРОКИ
	                      |ГДЕ
	                      |	ФайлыСведенийРОКИ.УчетнаяЗапись = &УчетнаяЗапись
	                      |	И ФайлыСведенийРОКИ.ИмяРесурса = &ИмяРесурса");
	Если ЗначениеЗаполнено(ДатаОбновленияФайлаНаСервере) Тогда
		Запрос.Текст = Запрос.Текст + "
						  |	И ФайлыСведенийРОКИ.ДатаФайла = &ДатаФайла";
		Запрос.УстановитьПараметр("ДатаФайла", ДатаОбновленияФайлаНаСервере);
	Иначе
		Запрос.Текст = Запрос.Текст + "
						  |УПОРЯДОЧИТЬ ПО
						  |	ФайлыСведенийРОКИ.ДатаФайла УБЫВ";
	КонецЕсли;
	Запрос.УстановитьПараметр("УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр("ИмяРесурса", ПараметрыСоединения.ИмяРесурсаСНастройками);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Данные = Выборка.Данные;
		Если ЗначениеЗаполнено(Данные) Тогда
			ДатаОбновленияФайлаНаСервере = Выборка.ДатаФайла;
			Расшифрован = Выборка.Расшифрован;
			ФайлНастроек = ПолучитьИмяВременногоФайла();
			Попытка
				Данные.Получить().Записать(ФайлНастроек);
				Возврат ФайлНастроек;
			Исключение
				ДобавитьСообщение(Сообщения, НСтр("ru = 'Ошибка разбора файла с информацией о настройках учетной записи:'") + Символы.ПС + ИнформацияОбОшибке().Описание);
				Соединение = Неопределено;
				Возврат Неопределено;
			КонецПопытки;
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Работа с сервером

&НаСервере
Функция ПолучитьФайлССервера(Соединение, ИмяФайла, ИнформацияОбОшибке = Неопределено, ВызванаССервера = Ложь)
	
	//@skip-check 1c-fresh-audit
	ВремФайл = ПолучитьИмяВременногоФайла(РасширениеФайла(ИмяФайла));
	
	Попытка
		ПараметрыЗапроса = Новый HTTPЗапрос(ИмяФайла);
		ОтветСервера = Соединение.Получить(ПараметрыЗапроса);
		Если ВызванаССервера Тогда
			//@skip-check 1c-fresh-audit
			ОтветСервера.ПолучитьТелоКакДвоичныеДанные().Записать(ВремФайл);
			Возврат ВремФайл;
		Иначе
			Возврат ПрочитатьBase64ИзФайла(
				//@skip-check 1c-fresh-audit
				ОтветСервера.ПолучитьТелоКакДвоичныеДанные().Записать(ВремФайл));
		КонецЕсли;
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

&НаСервере
Функция ПолучитьДатуОбновленияФайлаНастроекНаСервере(Соединение, ПараметрыСоединения, Сообщения)
	
	// получаем ресурс dt
	ИнформацияОбОшибке = Неопределено;
	ИмяФайлаДТ = ПолучитьФайлССервера(Соединение, ПараметрыСоединения.ИмяРесурсаДТ, ИнформацияОбОшибке, Истина);
	Если ИмяФайлаДТ = Неопределено Тогда
		ДобавитьСообщение(Сообщения,
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'На сервере ""Такском"" не найдена информация о настройках учетной записи (отсутствует файл %1)!%2'"),
			ПараметрыСоединения.ИмяРесурсаДТ,
			Символы.ПС + ИнформацияОбОшибке.Описание));
		Соединение = Неопределено;
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем значение даты из файла dt
	ЗначениеИзФайлаДТ = ПолучитьДатуИзФайлаДТ(ИмяФайлаДТ);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяФайлаДТ);
	Если ЗначениеИзФайлаДТ = Неопределено Тогда
		ДобавитьСообщение(Сообщения, НСтр("ru = 'Некорректный файл с информацией о дате обновления настроек на сервере ""Такском""!'"));
		Соединение = Неопределено;
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ЗначениеИзФайлаДТ;
	
КонецФункции

&НаСервере
Функция ПолучитьДатуИзФайлаДТ(ФайлДТ)
	
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ФайлДТ, "ФайлДТ", "ПолучитьДатуИзФайлаДТ");	
		
	Попытка
		//@skip-check 1c-fresh-audit
		ЧтениеТекста = Новый ЧтениеТекста(ФайлДТ, "windows-1251");
		СтрокаДТ = СокрЛП(ЧтениеТекста.ПрочитатьСтроку());
		ЧтениеТекста.Закрыть();
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	ВхождениеПробела = СтрНайти(СтрокаДТ, " ");
	Если ВхождениеПробела = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаДТ = СокрЛП(Лев(СтрокаДТ, ВхождениеПробела - 1));
	ВремяДТ = СокрЛП(Сред(СтрокаДТ, ВхождениеПробела + 1));
	
	ВхождениеДефиса = СтрНайти(ДатаДТ, "-");
	Если ВхождениеДефиса = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ГодДТ = СокрЛП(Лев(ДатаДТ, ВхождениеДефиса - 1));
	ДатаДТ = Сред(ДатаДТ, ВхождениеДефиса + 1);
	ВхождениеДефиса = СтрНайти(ДатаДТ, "-");
	Если ВхождениеДефиса = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	МесяцДТ = СокрЛП(Лев(ДатаДТ, ВхождениеДефиса - 1));
	ДеньМесяцаДТ = СокрЛП(Сред(ДатаДТ, ВхождениеДефиса + 1));
	
	ВхождениеДвоеточия = СтрНайти(ВремяДТ, ":");
	Если ВхождениеДвоеточия = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	ЧасДТ = СокрЛП(Лев(ВремяДТ, ВхождениеДвоеточия - 1));
	МинутаДТ = сокрЛП(Сред(ВремяДТ, ВхождениеДвоеточия + 1));
	
	Если ПустаяСтрока(ГодДТ)
	ИЛИ ПустаяСтрока(МесяцДТ)
	ИЛИ ПустаяСтрока(ДеньМесяцаДТ)
	ИЛИ ПустаяСтрока(ЧасДТ)
	ИЛИ ПустаяСтрока(МинутаДТ)
	ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ГодДТ)
	ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(МесяцДТ)
	ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ДеньМесяцаДТ)
	ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(ЧасДТ)
	ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(МинутаДТ) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		Возврат Дата(Число(ГодДТ), Число(МесяцДТ), Число(ДеньМесяцаДТ), Число(ЧасДТ), Число(МинутаДТ), 0);
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Вспомогательные процедуры и функции разбора XML с настройками

&НаКлиенте
Функция ПолучитьСтруктуруИсправлений()
	
	Исправления = Новый Структура("Настройка, Представление, СтароеЗначение, НовоеЗначение, ДопПараметр");
	Возврат Исправления;
	
КонецФункции

&НаКлиенте
Процедура ОпределитьИзменение(ТаблицаИзменений, Настройка, СтароеЗначение, НовоеЗначение, Представление, ДополнительныйПараметр = Неопределено)
	
	НовСтр = ПолучитьСтруктуруИсправлений();
	НовСтр.Настройка = Настройка;
	НовСтр.Представление = Представление;
	НовСтр.СтароеЗначение = СтароеЗначение;
	НовСтр.НовоеЗначение = НовоеЗначение;
	НовСтр.ДопПараметр = ДополнительныйПараметр;
	ТаблицаИзменений.Добавить(НовСтр);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция РазложитьСтрокуАдресаСервера(ИсхУзел)
	
	Результат = Новый Структура("Сервер, Порт", "", 0);
	
	Если НЕ ЗначениеЗаполнено(ИсхУзел) Тогда
		Возврат Результат;
	КонецЕсли;
	
	ИсхСтрока = ИсхУзел.Значение;
	
	ВхождениеДвоеточия = СтрНайти(ИсхСтрока, ":");
	Если ВхождениеДвоеточия = 0 Тогда
		Результат.Сервер = СокрЛП(ИсхСтрока);
	Иначе
		Сервер = СокрЛП(Лев(ИсхСтрока, ВхождениеДвоеточия - 1));
		Порт = СокрЛП(Сред(ИсхСтрока, ВхождениеДвоеточия + 1));
		Результат.Сервер = СокрЛП(Сервер);
		Если СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(Порт) Тогда
			Результат.Порт = Число(Порт);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыделитьИмяПользователяИзEmail(ИсхУзел)
	
	Если НЕ ЗначениеЗаполнено(ИсхУзел) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ИсхСтрока = ИсхУзел.Значение;
	
	ВхождениеРазделителя = СтрНайти(ИсхСтрока, "@");
	Если ВхождениеРазделителя = 0 Тогда
		Возврат "";
	Иначе
		Возврат СокрЛП(Лев(ИсхСтрока, ВхождениеРазделителя - 1));
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойстваСертификатаПоУзлу(Узел, ЧтоЗаполнять = Неопределено)
	
	Если ЧтоЗаполнять = Неопределено Тогда
		ЧтоЗаполнять = Новый Структура("ИмяВладельца, ДолжностьВладельца, ДействителенС, ДействителенПо,"
										+ " Отпечаток, Сертификат, ПризнакШифрования, ТипПодписи");
	КонецЕсли;
	
	УзелSubject_CN				= НайтиПодчиненныйУзел(Узел, "Subject_CN");
	УзелSubject_OU				= НайтиПодчиненныйУзел(Узел, "Subject_OU");
	УзелValidity_Period_with	= НайтиПодчиненныйУзел(Узел, "Validity_Period_with");
	УзелValidity_Period_on		= НайтиПодчиненныйУзел(Узел, "Validity_Period_on");
	УзелThumbprint				= НайтиПодчиненныйУзел(Узел, "Thumbprint");
	УзелCert					= НайтиПодчиненныйУзел(Узел, "Cert");
	УзелCRYPT_FLAG				= НайтиПодчиненныйУзел(Узел, "CRYPT_FLAG");
	УзелSIGN_TYPE				= НайтиПодчиненныйУзел(Узел, "SIGN_TYPE");
	
	Если ЗначениеЗаполнено(УзелSubject_CN) Тогда
		ЧтоЗаполнять.ИмяВладельца = СокрЛП(УзелSubject_CN.Значение);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелSubject_OU) Тогда
		ЧтоЗаполнять.ДолжностьВладельца = СокрЛП(УзелSubject_OU.Значение);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелValidity_Period_with) Тогда
		ЧтоЗаполнять.ДействителенС = ПреобразоватьXMLКЗначению("Дата", УзелValidity_Period_with.Значение);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелValidity_Period_on) Тогда
		ЧтоЗаполнять.ДействителенПо = ПреобразоватьXMLКЗначению("Дата", УзелValidity_Period_on.Значение);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелThumbprint) Тогда
		ЧтоЗаполнять.Отпечаток = нрег(СокрЛП(УзелThumbprint.Значение));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелCert) Тогда
		ЧтоЗаполнять.Сертификат = СокрЛП(УзелCert.Значение);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелCRYPT_FLAG) Тогда
		ЧтоЗаполнять.ПризнакШифрования = ПреобразоватьXMLКЗначению("Булево", УзелCRYPT_FLAG.Значение);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УзелSIGN_TYPE) Тогда
		ЧтоЗаполнять.ТипПодписи = ПреобразоватьXMLКЗначению("Число", УзелSIGN_TYPE.Значение);
	КонецЕсли;
	
	Возврат ЧтоЗаполнять;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
// Возвращает копию переданной структуры данных
Функция СкопироватьСтруктуруДанных(КоллекцияИсходная) Экспорт
	
	ТипЗначения = ТипЗнч(КоллекцияИсходная);
	Если ТипЗначения = Тип("Структура") Тогда
		КоллекцияКопия=Новый Структура;
		Для Каждого Элемент Из КоллекцияИсходная Цикл
			КоллекцияКопия.Вставить(Элемент.Ключ,СкопироватьСтруктуруДанных(Элемент.Значение));
		КонецЦикла;
		Возврат КоллекцияКопия;
	ИначеЕсли ТипЗначения=Тип("Массив") Тогда
		КоллекцияКопия = Новый Массив;
		Для Каждого Элемент Из КоллекцияИсходная Цикл
			КоллекцияКопия.Добавить(СкопироватьСтруктуруДанных(Элемент));
		КонецЦикла;
		Возврат КоллекцияКопия;
	ИначеЕсли ТипЗначения=Тип("Соответствие") Тогда
		КоллекцияКопия = Новый Соответствие;
		Для Каждого Элемент Из КоллекцияИсходная Цикл
			КоллекцияКопия.Вставить(Элемент.Ключ, СкопироватьСтруктуруДанных(Элемент.Значение));
		КонецЦикла;
		Возврат КоллекцияКопия;
	Иначе
		Возврат КоллекцияИсходная
	КонецЕсли;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция СкопироватьМассив(ИсходныйМассив) 
	
	МассивКопия = Новый Массив;
	Для Каждого Элемент Из ИсходныйМассив Цикл
		КопияЭлемента = СкопироватьСтруктуруДанных(Элемент);
		МассивКопия.Добавить(КопияЭлемента);
	КонецЦикла;
	
	Возврат МассивКопия;
	
КонецФункции 

&НаКлиентеНаСервереБезКонтекста
Функция НайтиСтрокиМассиваСтруктур(Знач МассивСтруктур, КлючПоиска, ЗначениеПоиска) Экспорт
	
	МассивДляПоиска = СкопироватьМассив(МассивСтруктур);
	Счетчик = 0;
	КоличествоЭлементов = МассивДляПоиска.Количество();
	Пока Счетчик < КоличествоЭлементов Цикл
		Если МассивДляПоиска[Счетчик].Свойство(КлючПоиска)
			И МассивДляПоиска[Счетчик][КлючПоиска] = ЗначениеПоиска Тогда
			Счетчик = Счетчик + 1;
		Иначе
			МассивДляПоиска.Удалить(Счетчик);
			КоличествоЭлементов = КоличествоЭлементов - 1;
		КонецЕсли;
	КонецЦикла;
	Возврат МассивДляПоиска;
	
КонецФункции

&НаКлиенте
Функция НайтиСтрокуВМассивеСтруктурКлиент(Знач МассивСтруктур, ЗначениеПоиска, ИмяКолонки) Экспорт
	
	Для Каждого ЭлементМассива Из МассивСтруктур Цикл
		Если ЭлементМассива.Свойство(ИмяКолонки) И ЭлементМассива[ИмяКолонки] = ЗначениеПоиска Тогда
			Возврат ЭлементМассива;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция НайтиСтрокуВМассивеСтруктур(Знач МассивСтруктур, ЗначениеПоиска, ИмяКолонки) Экспорт
	
	Для Каждого ЭлементМассива Из МассивСтруктур Цикл
		Если ЭлементМассива.Свойство(ИмяКолонки) И ЭлементМассива[ИмяКолонки] = ЗначениеПоиска Тогда
			Возврат ЭлементМассива;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ВыгрузитьКолонкуИзМассиваСтруктур(Знач МассивСтруктур, ИмяКолонки)
	
	Результат = Новый Массив;
	Если МассивСтруктур <> Неопределено Тогда
		Для Каждого ЭлементМассива Из МассивСтруктур Цикл
			Если ЭлементМассива.Свойство(ИмяКолонки) И ЭлементМассива[ИмяКолонки] <> Неопределено Тогда 
				Результат.Добавить(ЭлементМассива[ИмяКолонки]);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПолучитьПустуюДатуЗавершенияЦиклаОбменаКлиент() Экспорт
	
	Возврат '39991231235959';
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ОНЛАЙН-ОБНОВЛЕНИЯ МОДУЛЯ


&НаСервереБезКонтекста
Функция ИДТекущейКонфигурацииСоответствуетШаблону(ИДТекущейКонфигурации, ИДКонфигурации)
	
	Возврат ВРЕГ(СокрЛП(ИДТекущейКонфигурации)) = ВРЕГ(СокрЛП(ИДКонфигурации));
	
КонецФункции


&НаСервереБезКонтекста
Функция ТекущаяКонфигурацияСоответствуетУзлу(УзелКонфигурация, ИнформацияОПрограмме)
	
	РезультатПоискаАтрибута = УзелКонфигурация.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "ИД", "А"));
	Если РезультатПоискаАтрибута.Количество() = 0 Тогда
		Возврат Ложь;
	Иначе
		ИДКонфигурацииУзла = СокрЛП(РезультатПоискаАтрибута[0].Значение);
		Если ИДТекущейКонфигурацииСоответствуетШаблону(ИнформацияОПрограмме.ИмяМетаданныхКонфигурации, ИДКонфигурацииУзла)
		ИЛИ ИДТекущейКонфигурацииСоответствуетШаблону(ИнформацияОПрограмме.ИДКонфигурации, ИДКонфигурацииУзла) Тогда
			Возврат Истина;
		Иначе
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ТекущаяПлатформаСоответствуетУзлу(УзелПлатформа, ВерсияТекущейПлатформы)
	
	РезультатПоискаАтрибута = УзелПлатформа.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "Верс", "А"));
	Если РезультатПоискаАтрибута.Количество() = 0 Тогда
		Возврат Ложь;
	Иначе
		ШаблонВерсии = СокрЛП(РезультатПоискаАтрибута[0].Значение);
		Возврат ВерсияТекущейПлатформыСоответствуетШаблону(ВерсияТекущейПлатформы, ШаблонВерсии);
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ВерсияТекущейПлатформыСоответствуетШаблону(ВерсияТекущейПлатформы, ШаблонВерсии)
	
	МассивЧастейШаблона = РегламентированнаяОтчетностьКлиентСервер.РазобратьСтрокуВМассивПоРазделителю(ШаблонВерсии);
	МассивЧастейТекущейВерсии = РегламентированнаяОтчетностьКлиентСервер.РазобратьСтрокуВМассивПоРазделителю(ВерсияТекущейПлатформы);
	
	Для Инд = 0 По МассивЧастейШаблона.Количество() - 1 Цикл
		Если МассивЧастейТекущейВерсии.Количество() <= Инд Тогда
			Прервать;
		КонецЕсли;
		ТекущаяЧастьШаблона = МассивЧастейШаблона[Инд];
		ТекущаяЧастьТекущейВерсии = МассивЧастейТекущейВерсии[Инд];
		Если НЕ ЧастьВерсииСоответствуетЧастиШаблонаВерсии(ТекущаяЧастьТекущейВерсии, ТекущаяЧастьШаблона) Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЧастьВерсииСоответствуетЧастиШаблонаВерсии(Знач ЧастьТекущейВерсии, Знач ЧастьШаблона)
	
	ЧастьТекущейВерсии = СокрЛП(ЧастьТекущейВерсии);
	ЧастьШаблона = СокрЛП(ЧастьШаблона);
	
	Если Лев(ЧастьШаблона, 1) <> "[" ИЛИ Прав(ЧастьШаблона, 1) <> "]" Тогда
		Возврат (ЧастьШаблона = ЧастьТекущейВерсии);
	КонецЕсли;
	
	ЧастьШаблона = СокрЛП(Сред(ЧастьШаблона, 2, СтрДлина(ЧастьШаблона) - 2));
	СоставляющиеЧастиШаблона = РегламентированнаяОтчетностьКлиентСервер.РазобратьСтрокуВМассивПоРазделителю(ЧастьШаблона, ",");
	Если СоставляющиеЧастиШаблона.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	ИДПеременной = "X";
	Для Каждого Составляющее Из СоставляющиеЧастиШаблона Цикл
		Если ЗначениеЗаполнено(Составляющее) Тогда
			ТекстСоставляющего = СтрЗаменить(Составляющее, ИДПеременной, ЧастьТекущейВерсии);
			РезультатВычисления = Неопределено;
			Попытка
				РезультатВычисления = ОбщегоНазначения.ВычислитьВБезопасномРежиме(ТекстСоставляющего);
			Исключение
				ИнформацияОбОшибке = ИнформацияОбОшибке();
				ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
				ЗаписьЖурналаРегистрации(
					НСтр("ru = 'Электронный документооборот с контролирующими органами. Свойства внешнего модуля'"),
					УровеньЖурналаРегистрации.Ошибка,,,
					ТекстОшибки);
			КонецПопытки;
			
			Если ТипЗнч(РезультатВычисления) <> Тип("Булево") ИЛИ НЕ РезультатВычисления Тогда
				Возврат Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЭлектронныйДокументооборотИспользуетсяКлиент(УчитыватьФССФСРАРРПНиФТС = Ложь)
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	КОЛИЧЕСТВО(Организации.Ссылка) КАК Ссылка
						  |ИЗ
						  |	Справочник.Организации КАК Организации
						  |ГДЕ
						  |	Организации.ВидОбменаСКонтролирующимиОрганами = &ВидОбменаСКонтролирующимиОрганами
						  |	И Организации.УчетнаяЗаписьОбмена <> &ПустаяУчетнаяЗаписьОбмена
						  |	И Организации.ПометкаУдаления = &ПометкаУдаления
						  |	И Организации.УчетнаяЗаписьОбмена.ПометкаУдаления = &ПометкаУдаления");
	Запрос.УстановитьПараметр("ВидОбменаСКонтролирующимиОрганами", Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате);
	Запрос.УстановитьПараметр("ПустаяУчетнаяЗаписьОбмена", Справочники.УчетныеЗаписиДокументооборота.ПустаяСсылка());
	Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
	
	Результат = Неопределено;
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
	Исключение
		Результат = Ложь;
	КонецПопытки;
	
	Если Результат = Неопределено Тогда
		Если Выборка.Следующий() Тогда
			Результат = ЗначениеЗаполнено(Выборка.Получить(0));
			
		Иначе
			Результат = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ Результат И УчитыватьФССФСРАРРПНиФТС Тогда
		Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
							  |	КОЛИЧЕСТВО(НастройкиОбменаФСС.ИспользоватьОбмен) КАК ИспользоватьОбмен
							  |ИЗ
							  |	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаФСС
							  |ГДЕ
							  |	НастройкиОбменаФСС.ИспользоватьОбмен = &ИспользоватьОбмен
							  |	И НастройкиОбменаФСС.Организация <> &ПустаяОрганизация
							  |	И НастройкиОбменаФСС.Организация.ПометкаУдаления = &ПометкаУдаления
							  |	И (НастройкиОбменаФСС.Пользователь ЕСТЬ NULL
							  |	ИЛИ НастройкиОбменаФСС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))");
		Запрос.УстановитьПараметр("ИспользоватьОбмен", Истина);
		Запрос.УстановитьПараметр("ПустаяОрганизация", Справочники.Организации.ПустаяСсылка());
		Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
		
		Результат = Неопределено;
		Попытка
			Выборка = Запрос.Выполнить().Выбрать();
		Исключение
			Результат = Ложь;
		КонецПопытки;
		
		Если Результат = Неопределено Тогда
			Если Выборка.Следующий() Тогда
				Результат = ЗначениеЗаполнено(Выборка.Получить(0));
				
			Иначе
				Результат = Ложь;
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ Результат Тогда
			Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
								  |	КОЛИЧЕСТВО(НастройкиОбменаФСРАР.ИспользоватьОбмен) КАК ИспользоватьОбмен
								  |ИЗ
								  |	РегистрСведений.НастройкиОбменаФСРАР КАК НастройкиОбменаФСРАР
								  |ГДЕ
								  |	НастройкиОбменаФСРАР.ИспользоватьОбмен = &ИспользоватьОбмен
								  |	И НастройкиОбменаФСРАР.Организация <> &ПустаяОрганизация
								  |	И НастройкиОбменаФСРАР.Организация.ПометкаУдаления = &ПометкаУдаления
								  |	И (НастройкиОбменаФСРАР.Пользователь ЕСТЬ NULL
								  |	ИЛИ НастройкиОбменаФСРАР.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))");
			Запрос.УстановитьПараметр("ИспользоватьОбмен", Истина);
			Запрос.УстановитьПараметр("ПустаяОрганизация", Справочники.Организации.ПустаяСсылка());
			Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
			
			Результат = Неопределено;
			Попытка
				Выборка = Запрос.Выполнить().Выбрать();
			Исключение
				Результат = Ложь;
			КонецПопытки;
			
			Если Результат = Неопределено Тогда
				Если Выборка.Следующий() Тогда
					Результат = ЗначениеЗаполнено(Выборка.Получить(0));
					
				Иначе
					Результат = Ложь;
				КонецЕсли;
			КонецЕсли;
			
			Если НЕ Результат Тогда
				Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
									  |	КОЛИЧЕСТВО(НастройкиОбменаРПН.ИспользоватьОбмен) КАК ИспользоватьОбмен
									  |ИЗ
									  |	РегистрСведений.НастройкиОбменаРПН КАК НастройкиОбменаРПН
									  |ГДЕ
									  |	НастройкиОбменаРПН.ИспользоватьОбмен = &ИспользоватьОбмен
									  |	И НастройкиОбменаРПН.Организация <> &ПустаяОрганизация
									  |	И НастройкиОбменаРПН.Организация.ПометкаУдаления = &ПометкаУдаления
									  |	И (НастройкиОбменаРПН.Пользователь ЕСТЬ NULL
									  |	ИЛИ НастройкиОбменаРПН.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))");
				Запрос.УстановитьПараметр("ИспользоватьОбмен", Истина);
				Запрос.УстановитьПараметр("ПустаяОрганизация", Справочники.Организации.ПустаяСсылка());
				Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
				
				Результат = Неопределено;
				Попытка
					Выборка = Запрос.Выполнить().Выбрать();
				Исключение
					Результат = Ложь;
				КонецПопытки;
				
				Если Результат = Неопределено Тогда
					Если Выборка.Следующий() Тогда
						Результат = ЗначениеЗаполнено(Выборка.Получить(0));
						
					Иначе
						Результат = Ложь;
					КонецЕсли;
				КонецЕсли;
				
				Если НЕ Результат Тогда
					Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
										  |	КОЛИЧЕСТВО(НастройкиОбменаФТС.ИспользоватьОбмен) КАК ИспользоватьОбмен
										  |ИЗ
										  |	РегистрСведений.НастройкиОбменаФТС КАК НастройкиОбменаФТС
										  |ГДЕ
										  |	НастройкиОбменаФТС.ИспользоватьОбмен = &ИспользоватьОбмен
										  |	И НастройкиОбменаФТС.Организация <> &ПустаяОрганизация
										  |	И НастройкиОбменаФТС.Организация.ПометкаУдаления = &ПометкаУдаления
										  |	И (НастройкиОбменаФТС.Пользователь ЕСТЬ NULL
										  |	ИЛИ НастройкиОбменаФТС.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))");
					Запрос.УстановитьПараметр("ИспользоватьОбмен", Истина);
					Запрос.УстановитьПараметр("ПустаяОрганизация", Справочники.Организации.ПустаяСсылка());
					Запрос.УстановитьПараметр("ПометкаУдаления", Ложь);
					
					Результат = Неопределено;
					Попытка
						Выборка = Запрос.Выполнить().Выбрать();
					Исключение
						Результат = Ложь;
					КонецПопытки;
					
					Если Результат = Неопределено Тогда
						Если Выборка.Следующий() Тогда
							Результат = ЗначениеЗаполнено(Выборка.Получить(0));
							
						Иначе
							Результат = Ложь;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИнтервалПроверкиНаличияОбновлений()
	
	Возврат 1*60*60; // Раз в час
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПараметрыИнтернета() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьПараметрыИнтернета();
	
КонецФункции

&НаСервереБезКонтекста
Функция РазрешитьОбновлениеМодуля(ОбновитьСейчас = Ложь)
	
	// В режиме сервисов модуль обновлять запрещено
	Если ОбщегоНазначения.РазделениеВключено() Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// если обмен не используется, то и обновлять ничего не надо
	Если НЕ ЭлектронныйДокументооборотИспользуетсяКлиент(Истина) И НЕ ОбновитьСейчас Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ОбновлениеРазрешеноПользователем = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_РазрешитьОнлайнОбновление");
	Если ОбновлениеРазрешеноПользователем = Неопределено Тогда
		ОбновлениеРазрешеноПользователем = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСНалоговымиОрганами_РазрешитьОнлайнОбновление"); // прежнее имя параметра
	КонецЕсли;
	
	// Если требуется обновить принудительно, то разрешаем обновление
	Если ОбновитьСейчас Тогда
		ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_ДатаПредыдущейПроверкиНаличияОбновлений", , ТекущаяДатаСеанса());
		Возврат ОбновлениеРазрешеноПользователем;
	КонецЕсли;
	
	// если обновление разрешено или еще не определено,
	// необходимо проверить на временной интервал
	Если ОбновлениеРазрешеноПользователем = Неопределено ИЛИ ОбновлениеРазрешеноПользователем Тогда 
		сохрДатаПредыдущейПроверкиНаличияОбновлений = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ДатаПредыдущейПроверкиНаличияОбновлений");
		Если сохрДатаПредыдущейПроверкиНаличияОбновлений = Неопределено Тогда
			ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_ДатаПредыдущейПроверкиНаличияОбновлений", , ТекущаяДатаСеанса());
			Возврат ОбновлениеРазрешеноПользователем;
		ИначеЕсли ТекущаяДатаСеанса() < сохрДатаПредыдущейПроверкиНаличияОбновлений + ИнтервалПроверкиНаличияОбновлений() Тогда 
			Возврат Ложь;
		Иначе
			ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_ДатаПредыдущейПроверкиНаличияОбновлений", , ТекущаяДатаСеанса());
		КонецЕсли;
	КонецЕсли;
	
	Возврат ОбновлениеРазрешеноПользователем;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ПолучитьИмяФайлаКартыРелизов(КлючевойФайл)
	
	// считываем содержимое ключевого файла
	ТекстКлючевогоФайла = ПрочитатьТекстИзФайла(КлючевойФайл);
	
	// выделяем имя файла-карты
	СтрРазделитель = "=";
	ВхождениеРазделителя = СтрНайти(ТекстКлючевогоФайла, СтрРазделитель);
	Если НЕ ЗначениеЗаполнено(ВхождениеРазделителя) Тогда
		Возврат Неопределено;
	Иначе
		Возврат СокрЛП(Сред(ТекстКлючевогоФайла, ВхождениеРазделителя + 1));
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция РаспаковатьФайл(ФайлАрхива, ОписаниеОшибки = Неопределено)
	
	// создаем временный каталог
	ВременныйКаталог = ПолучитьИмяВременногоФайла();
	СоздатьКаталог(ВременныйКаталог);
	
	// определяем временный файл
	ФайлРезультат = ПолучитьИмяВременногоФайла("xml");
	
	// распаковываем во временный каталог
	Попытка
		ЧтениеЗИП = Новый ЧтениеZipФайла(ФайлАрхива);
		ПервыйЭлементАрхива = ЧтениеЗИП.Элементы.Получить(0);
		ЧтениеЗИП.Извлечь(ПервыйЭлементАрхива, ВременныйКаталог, РежимВосстановленияПутейФайловZIP.НеВосстанавливать);
		ЧтениеЗИП.Закрыть();
	Исключение
		ОписаниеОшибки = ИнформацияОбОшибке().Описание;
		Возврат Неопределено;
	КонецПопытки;
	
	// ищем файл в каталоге распаковки
	РезультатПоиска = НайтиФайлы(ВременныйКаталог, ПолучитьМаскуВсеФайлы());
	Если РезультатПоиска.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// переименовываем файл из архива
	ПервыйФайл = РезультатПоиска.Получить(0);
	// Файлы создаются внутри функции.
	//@skip-check 1c-fresh-audit
	ПереместитьФайл(ПервыйФайл.ПолноеИмя, ФайлРезультат);
	
	// удаляем каталог распаковки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
	
	Возврат ФайлРезультат;
	
КонецФункции

&НаСервере
Функция РаспаковатьСохранитьСправочникВКэше(ФайлКартаРелизовАрхив, ИмяФайлаКартыРелизовБезРасширения, ШапкаОшибки, СвойстваМодуляДляТекущегоРелиза)
	
	// распаковываем
	ОписаниеОшибки = "";
	ФайлКартаРелизов = РаспаковатьФайл(ФайлКартаРелизовАрхив, ОписаниеОшибки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлКартаРелизовАрхив);
	
	Если НЕ ЗначениеЗаполнено(ФайлКартаРелизов) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1%2.'"),
			ШапкаОшибки + Символы.ПС + Символы.ПС,
			Символы.ПС + ОписаниеОшибки);
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем справочник в дерево
	ТекстФайлаСправочника = "";
	ОписаниеОшибки = "";
	
	ДеревоКартыРелизов = ЗагрузитьXMLВДеревоЗначений(ФайлКартаРелизов, ТекстФайлаСправочника, ОписаниеОшибки);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлКартаРелизов);
	
	Если НЕ ЗначениеЗаполнено(ДеревоКартыРелизов) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1%2.'"),
			ШапкаОшибки + Символы.ПС + Символы.ПС,
			Символы.ПС + ОписаниеОшибки);
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// сохранение в кеше удалено
	
	// получаем справочник из кэша
	Если ДеревоКартыРелизов = Неопределено Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
	
	// определяем последнюю доступную версию модуля документооборота для текущего релиза
	СвойстваМодуляДляТекущегоРелиза = ОпределитьСвойстваВнешнегоМодуляДляТекущегоРелиза(ДеревоКартыРелизов);
	Если НЕ ЗначениеЗаполнено(СвойстваМодуляДляТекущегоРелиза) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// сравниваем ее с той, которая хранится в ИБ
	ВерсияМодуляВИнтернете 	= СтрЗаменить(СвойстваМодуляДляТекущегоРелиза.Версия, Символы.НПП, "");
	ВерсияМодуляВБазе 		= СтрЗаменить(ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ВерсияВнешнегоМодуляДокументооборота(), Символы.НПП, "");
	
	Если ВерсияМодуляВИнтернете = ВерсияМодуляВБазе Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции //РаспаковатьСохранитьСправочникВКэше

&НаСервере
Функция ПолучитьСохранитьОбновление(ПараметрыСервераИнтернета, СвойстваМодуляДляТекущегоРелиза) Экспорт
	
	ШапкаОшибки = НСтр("ru = 'Ошибка обновления модуля защищенного документооборота:'");
	
	// пытаемся установить соединение с сервером
	ОписаниеОшибки = "";
	Соединение = ДокументооборотСКО.УстановитьСоединениеССерверомИнтернета(ПараметрыСервераИнтернета.URLСервера, ОписаниеОшибки);
	Если Соединение = Неопределено Тогда
		
		ТекстСообщения = ШапкаОшибки + Символы.ПС + ОписаниеОшибки;
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		
		Возврат Ложь;
		
	КонецЕсли;
	
	// пытаемся получить ключевой файл с сервера
	КлючевойФайл = ПолучитьИмяВременногоФайла();
	Попытка
		Соединение.Получить(ПараметрыСервераИнтернета.КаталогФайловНаСервере
							+ ПараметрыСервераИнтернета.ИмяКлючевогоФайлаНаСервере,
							КлючевойФайл);
	Исключение
							
		ПодробнаяИнформацияОбОшибке = ОбщегоНазначенияЭДКОКлиентСервер.ПолучитьИнформациюОбОшибке(ИнформацияОбОшибке());
		ТекстСообщения = ШапкаОшибки + Символы.ПС + КраткоеПредставлениеОшибки(ПодробнаяИнформацияОбОшибке);
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		
		Возврат Ложь;
	КонецПопытки;
	
	// извлекаем имя файла-карты релизов из ключевого файла
	ИмяФайлаКартыРелизовБезРасширения = ПолучитьИмяФайлаКартыРелизов(КлючевойФайл);
	УдалитьФайлы(КлючевойФайл);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаКартыРелизовБезРасширения) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// получение версии справочника внешних модулей, закэшированного в ИБ - удалено
	
	ПолучитьФайлСправочникаВнешнихМодулей = Истина;
	
	// при необходимости получаем файл справочника внешних модулей с сервера и кэшируем его в ИБ
	ДеревоКартыРелизов = Неопределено;
	
	Если ПолучитьФайлСправочникаВнешнихМодулей Тогда
		
		ИмяФайлаКартыРелизовНаСервере = "/" + ИмяФайлаКартыРелизовБезРасширения + ".zip";
		ФайлКартаРелизовАрхив = ПолучитьИмяВременногоФайла("zip");
		СвойстваМодуляДляТекущегоРелиза = Неопределено;
		
		// получаем с сервера
		Попытка
			Соединение.Получить(ПараметрыСервераИнтернета.КаталогФайловНаСервере + ИмяФайлаКартыРелизовНаСервере, ФайлКартаРелизовАрхив);
		Исключение
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1Ошибка доступа к серверу обновлений модуля документооборота: %2.'"),
				ШапкаОшибки + Символы.ПС + Символы.ПС,
				Символы.ПС + ИнформацияОбОшибке().Описание);
			КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
				УровеньЖурналаРегистрации.Ошибка,,,
				ТекстСообщения);
			Возврат Ложь;
		КонецПопытки;
		
		УдалосьРаспаковать = РаспаковатьСохранитьСправочникВКэше(
			ФайлКартаРелизовАрхив, 
			ИмяФайлаКартыРелизовБезРасширения, 
			ШапкаОшибки, 
			СвойстваМодуляДляТекущегоРелиза);
			
		Если НЕ УдалосьРаспаковать Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ОбновитьМодульДокументооборотаСФНСПриНеобходимости(ОбновитьСейчас = Ложь, ДополнительныеПараметры = Неопределено) Экспорт
	
	РазрешитьОнлайнОбновлениеМодуля = РазрешитьОбновлениеМодуля(ОбновитьСейчас);
	
	Если РазрешитьОнлайнОбновлениеМодуля = Ложь Тогда
		ПослеПолученияКонтекстаЭДО(ДополнительныеПараметры, Ложь);
	ИначеЕсли РазрешитьОнлайнОбновлениеМодуля = Неопределено Тогда
		// Если пользователь раньше не давал согласия на обновление модуля,
		// то спросим его асинхронно через одну минуту,
		// чтобы "надоедливые" окна не вылезали в момент открытия формы 1С-Отчетность или любого другого окна.
		ПослеПолученияКонтекстаЭДО(ДополнительныеПараметры, Ложь);
		ПодключитьОбработчикОжидания("Подключаемый_ОткрытьФормуПодтвержденияВыходаВИнтернетДляОбновленияМодуля", 60, Истина);
	Иначе
		ОбновитьМодульДокументооборотаСФНС(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОткрытьФормуПодтвержденияВыходаВИнтернетДляОбновленияМодуля()
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОткрытьФормуПодтвержденияВыходаВИнтернетДляОбновленияМодуляЗавершение", ЭтотОбъект);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПодтверждениеВыходаВИнтернетДляЦелейОбновленияМодуля",,,,,,ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПодтвержденияВыходаВИнтернетДляОбновленияМодуляЗавершение(РезультатЗапросаРазрешения, ДополнительныеПараметры) Экспорт
	
	Если РезультатЗапросаРазрешения = Истина Тогда
		ОбновитьМодульДокументооборотаСФНС();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеПолученияКонтекстаЭДО(ДополнительныеПараметры, МодульОбновлен)
	
	Если ДополнительныеПараметры = Неопределено Тогда
		// Параметров не будет в том случае, если сюда пришли из формы
		// получения согласия на обновление модуля документооборота.
		// В этом случае даже при обновлении модуля, контекст не обновляем,
		// используем старый.
		Возврат;
	КонецЕсли;
	
	Если ДополнительныеПараметры.Свойство("ВыполняемоеОповещение") Тогда
		ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	Иначе
		ВыполняемоеОповещение = Неопределено;
	КонецЕсли;
	
	Если МодульОбновлен Тогда
		
		ДокументооборотСКОКлиент.ОбновитьИПолучитьКонтекстЭДО(ВыполняемоеОповещение);
		
	ИначеЕсли ВыполняемоеОповещение <> Неопределено Тогда
		
		ФормаРезультат = ДополнительныеПараметры.ФормаРезультат;
		
		СтруктураРезультата = Новый Структура;
		СтруктураРезультата.Вставить("ТекстОшибки", "");
		СтруктураРезультата.Вставить("КонтекстЭДО", ФормаРезультат);
		ПараметрыПриложения.Вставить("РегламентированнаяОтчетность.КонтекстЭДО", ФормаРезультат);
		
		ДокументооборотСКОКлиент.ОбновитьПутьВК();
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, СтруктураРезультата);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьМодульДокументооборотаСФНС(ВходящийКонтекст = Неопределено) Экспорт

	ЭтоОбновлениеИзМастера = ТипЗнч(ВходящийКонтекст) = Тип("Структура")
		И ВходящийКонтекст.Свойство("ЭтоОбновлениеИзМастера")
		И ВходящийКонтекст.ЭтоОбновлениеИзМастера;
		
	МоментОткрытияФормыПодождите 	= ТекущаяДатаНаСервере();
	СвойстваМодуляДляТекущегоРелиза = Неопределено;
	КаталогФайловНаСервере 			= Неопределено;
	ПараметрыСервераИнтернета 		= ПолучитьПараметрыИнтернета();
	
	Если ПолучитьСохранитьОбновление(ПараметрыСервераИнтернета, СвойстваМодуляДляТекущегоРелиза) = Ложь Тогда 
		
		Если ВходящийКонтекст <> Неопределено Тогда
			ПослеПолученияКонтекстаЭДО(ВходящийКонтекст, Ложь);
		КонецЕсли;
		
	Иначе
		
		ПараметрыДлительногоОбмена = Новый Структура();
		ПараметрыДлительногоОбмена.Вставить("ЭтоОбновлениеИзМастера", ЭтоОбновлениеИзМастера);
		
		ДлительнаяОтправкаКлиент.ОткрытьФормуОжиданияЗагрузкиМодуля(ПараметрыДлительногоОбмена, ЭтотОбъект); // обновление модуля
		
		Если ВходящийКонтекст <> Неопределено Тогда
			ВходящийКонтекст.Вставить("Версия", СвойстваМодуляДляТекущегоРелиза.Версия);
		Иначе
			ВходящийКонтекст = Новый Структура("Версия", СвойстваМодуляДляТекущегоРелиза.Версия);
		КонецЕсли;
		
		АдресФайлаНаВебСервере = 
			"http://"
			+ ПараметрыСервераИнтернета.URLСервера
			+ ПараметрыСервераИнтернета.КаталогФайловНаСервере
			+ "/" 
			+ СвойстваМодуляДляТекущегоРелиза.Имя 
			+ ".zip";
		
		РезультатСкачивания = СкачатьФайлВоВременноеХранилище(АдресФайлаНаВебСервере);
		
		Если РезультатСкачивания.Статус Тогда
		
			Результат = ЗакачатьИВключитьПризнакИспользования(РезультатСкачивания.Путь, ВходящийКонтекст.Версия);
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуОжиданияЗагрузкиМодуля(ЭтоОбновлениеИзМастера);
			
			Если ВходящийКонтекст <> Неопределено Тогда
				ПослеПолученияКонтекстаЭДО(ВходящийКонтекст, Результат);
			КонецЕсли;
			
		Иначе
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуОжиданияЗагрузкиМодуля(ЭтоОбновлениеИзМастера);
			
			Если ВходящийКонтекст <> Неопределено Тогда
				ПослеПолученияКонтекстаЭДО(ВходящийКонтекст, Ложь);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ЗакачатьИВключитьПризнакИспользования(АдресФайла, ВерсияВнешнегоМодуля)
	
	ШапкаОшибки = НСтр("ru = 'Ошибка обновления модуля защищенного документооборота:'");
	
	ДвДанныеАрхива 	= ПолучитьИзВременногоХранилища(АдресФайла);
	АрхивМодуля 	= ПолучитьИмяВременногоФайла();
	
	Попытка
		ДвДанныеАрхива.Записать(АрхивМодуля);
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1%2.'"),
																				ШапкаОшибки + Символы.ПС + Символы.ПС,
																				ИнформацияОбОшибке().Описание);
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Если НЕ КонтекстЭДОСервер.ИзменениеСвойствМодуляДокументооборотаВозможно() Тогда
		ТекстСообщения = НСтр("ru='В Интернете доступно обновление модуля документооборота,
									|но у Вас недостаточно прав для регистрации обновленного модуля в информационной базе.
									|Обратитесь к администратору информационной базы.'");
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// распаковываем модуль
	ОписаниеОшибки = "";
	ФайлМодуля = РаспаковатьФайл(АрхивМодуля, ОписаниеОшибки);
	УдалитьФайлы(АрхивМодуля);
	Если НЕ ЗначениеЗаполнено(ФайлМодуля) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1%2.'"),
																				ШапкаОшибки + Символы.ПС + Символы.ПС,
																				ОписаниеОшибки);
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// регистрируем модуль в ИБ
	РезультатРегистрации = КонтекстЭДОСервер.ПрименитьСвойстваМодуляДокументооборота(Истина, ФайлМодуля, ОписаниеОшибки, ВерсияВнешнегоМодуля);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлМодуля);
	
	Если НЕ РезультатРегистрации Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='%1%2.'"),
																				ШапкаОшибки + Символы.ПС + Символы.ПС,
																				ОписаниеОшибки);
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Обновление модуля защищенного документооборота'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаСервере
Функция ОпределитьСвойстваВнешнегоМодуляДляТекущегоРелиза(ДеревоКартыРелизов)
	
	ИнформацияОПрограмме = ИнформацияОПрограмме();
	
	Для Каждого Стр1 Из ДеревоКартыРелизов.Строки Цикл // уровень узла Файл
		Для Каждого Стр2 Из Стр1.Строки Цикл // уровень узла Платформа
			Если Стр2.Тип = "Э" И ТекущаяПлатформаСоответствуетУзлу(Стр2, ИнформацияОПрограмме.ВерсияПлатформы) Тогда
				Для Каждого Стр3 Из Стр2.Строки Цикл // уровень узла Конфигурация
					Если Стр3.Тип = "Э" И ТекущаяКонфигурацияСоответствуетУзлу(Стр3, ИнформацияОПрограмме) Тогда
						Для Каждого Стр4 Из Стр3.Строки Цикл // уровень узла Релиз
							Если Стр4.Тип = "Э" Тогда
								
								РезультатПоискаАтрибутаСВерсиейРелиза = Стр4.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "Верс", "А"));
								Если РезультатПоискаАтрибутаСВерсиейРелиза.Количество() = 0 Тогда
									Продолжить;
								КонецЕсли;
								ВерсияРелиза = СокрЛП(РезультатПоискаАтрибутаСВерсиейРелиза[0].Значение);
								
								Если ВерсияРелиза <> ИнформацияОПрограмме.ВерсияКонфигурации Тогда
									Продолжить;
								КонецЕсли;
								
								Для Каждого Стр5 Из Стр4.Строки Цикл
									Если Стр5.Тип = "Э" И Стр5.Имя = "Мод" Тогда
										УзелВерсия = Стр5.Строки.Найти("Верс", "Имя");
										УзелИмя = Стр5.Строки.Найти("Имя", "Имя");
										УзелДата = Стр5.Строки.Найти("Дата", "Имя");
										Если УзелВерсия <> Неопределено И УзелИмя <> Неопределено Тогда
											Результат = Новый Структура("Версия, Имя", СокрЛП(УзелВерсия.Значение), СокрЛП(УзелИмя.Значение));
											Если ЗначениеЗаполнено(УзелДата) И ЗначениеЗаполнено(УзелДата.Значение) Тогда
												Результат.Вставить("Дата", УзелДата.Значение);
											КонецЕсли;
											Возврат Результат;
										КонецЕсли;
									КонецЕсли;
								КонецЦикла;
								
							КонецЕсли;
						КонецЦикла;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИнформацияОПрограмме()
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ИнформацияОПрограмме();
	
КонецФункции

//Документообороты Заявление и Документ

&НаСервереБезКонтекста
Функция ДатаИзСтроки(СтрДата) Экспорт
	Если СтрДата = "" Тогда
		ВозвращаемаяДата = Дата(1, 1, 1);
	Иначе
		МассивПодстрок = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(СтрДата, ".");
		Если Число(МассивПодстрок[0]) = 0 Тогда
			МассивПодстрок[0] = "1";
		КонецЕсли;
		Если Число(МассивПодстрок[1]) = 0 Тогда
			МассивПодстрок[1] = "1";
		КонецЕсли;
		Если Число(МассивПодстрок[2]) = 0 Тогда
			МассивПодстрок[2] = "1";
		КонецЕсли;
		ВозвращаемаяДата = Дата(МассивПодстрок[2], МассивПодстрок[1], МассивПодстрок[0]);
	КонецЕсли;
	
	Возврат ВозвращаемаяДата;
	
КонецФункции

// Функция возвращает значение строки дерева значений из колонки "Имя"
&НаСервереБезКонтекста
Функция ИзвлечьДанное(СтрокаДерева, Имя) 
	СтрокаДанных = СтрокаДерева.Строки.Найти(Имя, "Имя", Истина);
	Если СтрокаДанных = Неопределено Тогда
		Возврат "";
	Иначе
		Возврат СтрокаДанных.Значение;
	КонецЕсли;
КонецФункции

&НаСервереБезКонтекста
Функция ПрерватьФормированиеРезультатаПриема(ТранспортноеСообщение)
	
	ПолученоПозже5февраля2025 = ТребованияФНСВызовСервера.ПолученоПозже5февраля2025ПоТС(ТранспортноеСообщение);
	ОписьСодержитТребования   = ТребованияФНСВызовСервера.ОписьСодержитТребованияПоВложению(ТранспортноеСообщение) = Истина;
	
	Если НЕ ПолученоПозже5февраля2025 Тогда
		Возврат ОписьСодержитТребования;
	КонецЕсли;
	
	ЭДООбязательно = ТребованияФНСВызовСервера.ЭДООбязательноПоТС(ТранспортноеСообщение);
	
	// Блокируем авто отправку подтверждения для уведомлений, если не понятно про обязательность ЭДО,
	// поскольку если ЭДО обязательно, то подтверждение отправлять не нужно.
	
	// Так же блокируем авто отправку подтверждения для уведомлений, если ЭДО обязательно,
	// поскольку в этом случае подтверждение отправлять не нужно.
	
	ЗаблокироватьДляУведомлений = НЕ ОписьСодержитТребования И (ЭДООбязательно = Неопределено ИЛИ ЭДООбязательно = Истина);
	
	Результат = ОписьСодержитТребования ИЛИ ЗаблокироватьДляУведомлений;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура СоздатьРезультатПриемаНаКлиентеСУчетомВопроса(
		ОповещениеОЗавершении, 
		ОснованиеСсылка, 
		ПоложительныйПрием = Истина, 
		ТихийРежим = Ложь, 
		УчитыватьПризнакАвтоподтверждения = Ложь,
		НужноПересоздать = Ложь) Экспорт
	
	Результат = ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(ОснованиеСсылка);
	
	Если УчитыватьПризнакАвтоподтверждения И НЕ Результат.Автоподтверждение Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ТипСообщения = Результат.Тип;
	
	Если ТипСообщения <> ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ДокументНО") Тогда
		// формируем пакет, если не сформирован
		Если ОповещениеОЗавершении.ДополнительныеПараметры.Свойство("Подтверждение")
			И ЗначениеЗаполнено(ОповещениеОЗавершении.ДополнительныеПараметры.Подтверждение)
			И НЕ СообщениеЗашифровано(ОповещениеОЗавершении.ДополнительныеПараметры.Подтверждение) Тогда
			СформироватьПакет(ОповещениеОЗавершении, ОповещениеОЗавершении.ДополнительныеПараметры.Подтверждение, Истина);
			Возврат;
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	Если УчитыватьПризнакАвтоподтверждения И ПрерватьФормированиеРезультатаПриема(ОснованиеСсылка) = Истина Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	Подключено = НаправлениеОтправкиСообщенияПодключено(ОповещениеОЗавершении, ОснованиеСсылка, ТихийРежим);
	Если НЕ Подключено Тогда
		Возврат;
	КонецЕсли;
	
	//создаем результат приема
	УжеСуществует = Ложь;
	СообщениеРезультатПриема = СоздатьРезультатПриемаНаСервереСУчетомПризнака(
		ОснованиеСсылка, 
		ПоложительныйПрием, 
		УчитыватьПризнакАвтоподтверждения, 
		УжеСуществует,
		НужноПересоздать);
		
	Если УжеСуществует Тогда
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, СообщениеРезультатПриема);
		
	ИначеЕсли СообщениеРезультатПриема = Неопределено Тогда
		
		Результат = РезультатПодтверждения();
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось создать результат приема сообщения %1!'"),
							Символ(34) + ПредставлениеТранспортногоСообщения(ОснованиеСсылка) + Символ(34));
		Если НЕ ТихийРежим Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		Результат.ТекстОшибки = ТекстСообщения;
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
		Возврат;		
	Иначе
		Контекст = Новый Структура;
		Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
		Контекст.Вставить("СообщениеРезультатПриема", СообщениеРезультатПриема);
		
		Оповещение = Новый ОписаниеОповещения(
			"СоздатьРезультатПриемаНаКлиентеСУчетомВопросаПослеПодписания", ЭтотОбъект, Контекст);
		КонтекстОтправки = Новый Структура;
		РеквизитыСообщения = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначенияРеквизитовОбъекта(СообщениеРезультатПриема, "Тип, УчетнаяЗапись");
		ПодписатьСообщение(
			"ФНС", Оповещение, КонтекстОтправки, СообщениеРезультатПриема, 
			РеквизитыСообщения.Тип, "Шифрование", , РеквизитыСообщения.УчетнаяЗапись, ОснованиеСсылка);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьРезультатПриемаНаКлиентеСУчетомВопросаПослеПодписания(Результат, ВходящийКонтекст) Экспорт

	ВыполнитьОбработкуОповещения(
		ВходящийКонтекст.ОповещениеОЗавершении,
		?(Результат = Истина, ВходящийКонтекст.СообщениеРезультатПриема, Неопределено));
	
КонецПроцедуры
	

&НаКлиенте
Функция РезультатПодтверждения()

	Результат = Новый Структура();
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ТекстОшибки", "");
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СоздатьРезультатПриемаНаСервереСУчетомПризнака(
		ОснованиеСсылка, 
		ПоложительныйПрием = Истина, 
		УчитыватьПризнакАвтоподтверждения = Истина, 
		УжеСуществует = Ложь,
		НужноПересоздать = Ложь)
	
	Если ОснованиеСсылка.Тип <> Перечисления.ТипыТранспортныхСообщений.ДокументНО Тогда
		Возврат Неопределено;
	КонецЕсли;

	Если УчитыватьПризнакАвтоподтверждения Тогда
		Результат = ПризнакАвтоматическогоФормированияПодтвержденийИТипСообщения(ОснованиеСсылка);
		Если НЕ Результат.Автоподтверждение Тогда
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	Если НужноПересоздать Тогда
		СуществующееСообщение = ПолучитьРезультатПриемаВходящейОписи(ОснованиеСсылка.ЦиклОбмена.Предмет);
		НовоеСообщение = СуществующееСообщение.ПолучитьОбъект();
	Иначе
		НовоеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	КонецЕсли;
	
	Если ПоложительныйПрием = Истина Тогда
		//формируем квитанцию о приеме
		НовоеСообщение.Тема = "ФНС:1.0-квитанцияОПриеме-РезультатПриемаДокументНП";
		НовоеСообщение.ПротоколСОшибкой = Ложь;
	ИначеЕсли ПоложительныйПрием = Ложь Тогда
		//формируем уведомление об отказе
		НовоеСообщение.Тема = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаДокументНП";
		НовоеСообщение.ПротоколСОшибкой = Истина;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		
		НовоеСообщение.Заполнить(ОснованиеСсылка);
		НовоеСообщение.Записать();
		
	Исключение
		
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке);
		
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Запись результата приема'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,
			,
			,
			ИнформацияОбОшибке);
		
		Возврат Неопределено;
		
	КонецПопытки;
	
	Если НужноПересоздать Тогда
		
		УдалитьВложенияРезультатаПриема(СуществующееСообщение);
		
	Иначе
		
		УжеСуществует = НовоеСообщение.УжеСуществует = Истина;
		Если УжеСуществует И НовоеСообщение.СуществующееСообщение <> Неопределено Тогда
			Возврат НовоеСообщение.СуществующееСообщение;
		КонецЕсли;
		
	КонецЕсли;
	
	Если НовоеСообщение.ОшибкаЗаполнения <> Истина Тогда
		Если СформироватьВложениеРезультатПриема(НовоеСообщение.Ссылка) Тогда
			Возврат НовоеСообщение.Ссылка;
		КонецЕсли;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции 

&НаСервереБезКонтекста
Функция СформироватьВложениеРезультатПриема(ТранспортноеСообщение)
	
	ТемаСообщения = ТранспортноеСообщение.Тема;
	
	Если ТемаСообщения = "ФНС:1.0-квитанцияОПриеме-РезультатПриемаДокументНП" Тогда
		Возврат СформироватьВложениеКвитанцияОПриемеДокумент(ТранспортноеСообщение);
	ИначеЕсли ТемаСообщения = "ФНС:1.0-уведомлениеОбОтказе-РезультатПриемаДокументНП" Тогда
		Возврат СформироватьВложениеУведомлениеОбОтказеДокумент(ТранспортноеСообщение);
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ЗаполнитьУзелОбщимиСведениямиОбУведомлении(Узел, ТранспортноеСообщение, ДИВы)
	
	КНД = КНДТребования(ТранспортноеСообщение.Основание.ЦиклОбмена);
	ДобавитьУзелXML(Узел, "КНД", Истина, КНД);
	ДобавитьУзелXML(Узел, "НаимВидДок", Истина, "Формат сообщений о документах направляемых налогоплательщику (представителю) или в налоговый орган в отношениях, регулируемых законодательством о налогах и сборах");
	
	ДатаНапр = Формат(ТранспортноеСообщение.Основание.Дата, "ДФ=dd.MM.yyyy");
	ДобавитьУзелXML(Узел, "ДатаНапр", Истина, ДатаНапр);
	
	ВремНапр = Формат(ТранспортноеСообщение.Основание.Дата, "ДФ=ЧЧ.мм.сс");
	ДобавитьУзелXML(Узел, "ВремНапр", Истина, ВремНапр);
	
	ДатаПост = Формат(ТранспортноеСообщение.Основание.ДатаТранспорта, "ДФ=dd.MM.yyyy");
	ДобавитьУзелXML(Узел, "ДатаПост", Истина, ДатаПост);

	Для Каждого СтрДИВ Из ДИВы Цикл
		ДобавитьУзелXML(Узел, "ИмяПринятФайла", Ложь, ИмяФайлаБезРасширения(СтрДИВ.ИмяФайла));
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ЗаполнитьУзелСведениямиОбУчастникеЭДОДокумент(Узел, Сведения)
	
	ТипУчастникаЭДО = Сведения.ТипУчастника;
	Если ТипУчастникаЭДО = "Организация" Тогда
		
		Если Сведения.ЭтоФизЛицо Тогда
			
			УзелФЛ = ДобавитьУзелXML(Узел, "ОтпрФЛ");
			ДобавитьУзелXML(УзелФЛ, "ИННФЛ", Истина, Сведения.ИНН);
			
			УзелФИО = ДобавитьУзелXML(УзелФЛ, "ФИО");
			ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Сведения.Фамилия);
			ДобавитьУзелXML(УзелФИО, "Имя", Истина, Сведения.Имя);
			Если ЗначениеЗаполнено(Сведения.Отчество) Тогда
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Сведения.Отчество);
			КонецЕсли;
			
		Иначе
			
			УзелЮЛ = ДобавитьУзелXML(Узел, "ОтпрЮЛ");
			ДобавитьУзелXML(УзелЮЛ, "НаимОрг", Истина, Сведения.Наименование);
			ДобавитьУзелXML(УзелЮЛ, "ИННЮЛ", Истина, Сведения.ИНН);
			ДобавитьУзелXML(УзелЮЛ, "КПП", Истина, Сведения.КПП);
			
		КонецЕсли;
		
	ИначеЕсли ТипУчастникаЭДО = "НалоговыйОрган" Тогда
		
		УзелНО = ДобавитьУзелXML(Узел, "ОтпрНО");
		ДобавитьУзелXML(УзелНО, "КодНО", Истина, Сведения.Код);
		ДобавитьУзелXML(УзелНО, "НаимНО", Истина, Сведения.Наименование);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ЗаполнитьУзелСведениямиОКвитанции(Узел, ТранспортноеСообщение, ДИВы)
	
	ДатаПрин = Формат(ТекущаяДатаСеанса(), "ДФ=dd.MM.yyyy");
	ДобавитьУзелXML(Узел, "ДатаПрин", Истина, ДатаПрин);
	
	КНД = КНДТребования(ТранспортноеСообщение.Основание.ЦиклОбмена);
	ДобавитьУзелXML(Узел, "КНД", Истина, КНД);
	ДобавитьУзелXML(Узел, "НаимВидДок", Истина, "Формат сообщений о документах направляемых налогоплательщику (представителю) или в налоговый орган в отношениях, регулируемых законодательством о налогах и сборах");

	ДатаНапр = Формат(ТранспортноеСообщение.Основание.Дата, "ДФ=dd.MM.yyyy");
	ДобавитьУзелXML(Узел, "ДатаНапр", Истина, ДатаНапр);
	
	ВремНапр = Формат(ТранспортноеСообщение.Основание.Дата, "ДФ=ЧЧ.мм.сс");
	ДобавитьУзелXML(Узел, "ВремНапр", Истина, ВремНапр);
	
	ДатаПост = Формат(ТранспортноеСообщение.Основание.ДатаТранспорта, "ДФ=dd.MM.yyyy");
	ДобавитьУзелXML(Узел, "ДатаПост", Истина, ДатаПост);

	Для Каждого СтрДИВ Из ДИВы Цикл
		ДобавитьУзелXML(Узел, "ИмяПринятФайла", Ложь, ИмяФайлаБезРасширения(СтрДИВ.ИмяФайла));
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция КНДТребования(ЦиклОбмена)
	
	СообщенияЦикла = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,Ложь);
	// Получаем транспортное сообщение, содержащее требование.
	// Оно обязательно должно быть, иначе бы мы сейчас не подтверждали его получение.
	СтрДокументНО = СообщенияЦикла.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ДокументНО));
	
	Если СтрДокументНО.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПервичноеТранспортноеСообщение = СтрДокументНО[0];
	
	// Ищем файл с описанием передаваемого документа TR_INFSOOB
	// см. ПРИКАЗ от 9 ноября 2010 г. N ММВ-7-6/535@
	ИмяФайла = "TR_INFSOOB.xml";
	ВложенияПервичногоСообщения = ПолучитьВложенияТранспортногоСообщения(ПервичноеТранспортноеСообщение, Истина, , ИмяФайла);
	ВложениеПервичногоСообщения = ВложенияПервичногоСообщения[0];

	// записываем вложение во временный файл
	ФайлВложение = ПолучитьИмяВременногоФайла("xml");
	Попытка
		ВложениеПервичногоСообщения.Данные.Получить().Записать(ФайлВложение);
	Исключение
		Возврат Неопределено;
	КонецПопытки;
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ФайлВложение);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлВложение);
	Если ДеревоXML = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Результат = Новый Структура;
	
	Узел = ДеревоXML.Строки.Найти("КНДДок", "Имя", Истина);
	Если Узел = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		Возврат Узел.Значение;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLКвитанцииОПриемеДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаКвитанции)
	
	// выделяем имя файла без расширения из короткого имени файла квитанции
	РасширениеФайлаКвитанции = РасширениеФайла(ИмяФайлаКвитанции);
	ДлинаРасширенияИмениФайлаКвитанции = СтрДлина(РасширениеФайлаКвитанции);
	ИмяФайлаКвитанцииБезРасширения = Лев(ИмяФайлаКвитанции, СтрДлина(ИмяФайлаКвитанции) - ДлинаРасширенияИмениФайлаКвитанции - 1);
			  
	// определяем дату получения основания
	ДатаПолученияОснования = ТранспортноеСообщение.Основание.ДатаТранспорта;
	
	// получаем сведения о получателе из входящего требования
	РеквизитыПолучателя = ПолучитьРеквизитыПолучателяИзВходящегоТребования(ТранспортноеСообщение.Основание);
	Если РеквизитыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения о налогоплательщике из входящего требования
	РеквизитыНалогоплательщика = ПолучитьРеквизитыНалогоплательщикаИзВходящегоТребования(ТранспортноеСообщение.Основание);
	Если РеквизитыНалогоплательщика = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты налогоплательщика из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем XML-дерево квитанции
	ДеревоXML = СоздатьДеревоXML();
	
	// иниицализируем атрибуты корневого элемента
	ДобавитьУзелXML(ДеревоXML, "ИдФайл", Истина, ИмяФайлаКвитанцииБезРасширения);
	ДобавитьУзелXML(ДеревоXML, "ВерсПрог", Истина, Лев(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40));
	ДобавитьУзелXML(ДеревоXML, "ВерсФорм", Истина, "5.03");
	
	// создаем узел Документ
	УзелДокумент = ДобавитьУзелXML(ДеревоXML, "Документ");
	ДобавитьУзелXML(УзелДокумент, "КНД", Истина, "1167001");
	
	// добавляем сведения о получателе
	УзелСвПолДок = ДобавитьУзелXML(УзелДокумент, "СвПолДок");
	Если (РеквизитыПолучателя.НаимОрг <> "" ИЛИ РеквизитыНалогоплательщика.НаимОрг <> "")
	И ЗначениеЗаполнено(РеквизитыПолучателя.ИННЮЛ) 
	И ЗначениеЗаполнено(РеквизитыПолучателя.КПП) Тогда
		//ЮЛ
		
		НаимОрг = ?(РеквизитыПолучателя.НаимОрг = "", РеквизитыНалогоплательщика.НаимОрг, РеквизитыПолучателя.НаимОрг);
		
		УзелЮЛ = ДобавитьУзелXML(УзелСвПолДок, "ОтпрЮЛ");
		ДобавитьУзелXML(УзелЮЛ, "НаимОрг", 	Истина, НаимОрг);
		ДобавитьУзелXML(УзелЮЛ, "ИННЮЛ", 	Истина, РеквизитыПолучателя.ИННЮЛ);
		ДобавитьУзелXML(УзелЮЛ, "КПП", 		Истина, РеквизитыПолучателя.КПП);
		
    ИначеЕсли ЗначениеЗаполнено(РеквизитыПолучателя.ИННФЛ)
	И ЗначениеЗаполнено(РеквизитыПолучателя.Фамилия) 
	И ЗначениеЗаполнено(РеквизитыПолучателя.Имя) Тогда
    	//ФЛ
		УзелФЛ = ДобавитьУзелXML(УзелСвПолДок, "ОтпрФЛ");
		ДобавитьУзелXML(УзелФЛ, "ИННФЛ", 	Истина, РеквизитыПолучателя.ИННФЛ);
		УзелФИО = ДобавитьУзелXML(УзелФЛ, "ФИО");
		ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, РеквизитыПолучателя.Фамилия);
		ДобавитьУзелXML(УзелФИО, "Имя", Истина, РеквизитыПолучателя.Имя);
		Если ЗначениеЗаполнено(РеквизитыПолучателя.Отчество) Тогда
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, РеквизитыПолучателя.Отчество);
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// добавляем сведения о квитанции
	УзелСвКвит = ДобавитьУзелXML(УзелДокумент, "СвКвит");
	ЗаполнитьУзелСведениямиОКвитанции(УзелСвКвит, ТранспортноеСообщение, ДИВы);
	
	// добавляем сведения о промежуточном НО
	ИдентификаторыПолучателя = ПолучитьИдентификаторыПолучателя(ТранспортноеСообщение.ЦиклОбмена);
	Если ИдентификаторыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;

	// получаем сведения об отправителе
	КодНО	= ИдентификаторыПолучателя.КонечныйНалоговыйОрган;
	ОтпрНО 	= Справочники.НалоговыеОрганы.НайтиПоКоду(КодНО);
	Если НЕ ЗначениеЗаполнено(ОтпрНО) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	СведенияОбОтправителе = СформироватьСведенияОбУчастникеДокументооборота(ОтпрНО, ТранспортноеСообщение);
	Если НЕ ЗначениеЗаполнено(СведенияОбОтправителе) Тогда
		Возврат Неопределено;
	КонецЕсли;

	// добавляем сведения об отправителе
	УзелСвОтпрДок = ДобавитьУзелXML(УзелДокумент, "СвОтпрДок");
	ЗаполнитьУзелСведениямиОбУчастникеЭДОДокумент(УзелСвОтпрДок, СведенияОбОтправителе);
	
	КодНОПром = ИдентификаторыПолучателя.ПромежуточныйНалоговыйОрган;
	Если ЗначениеЗаполнено(КодНОПром) Тогда
		НОПром = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНОПром);
		Если ЗначениеЗаполнено(НОПром) Тогда
			УзелНОПром = ДобавитьУзелXML(УзелДокумент, "НОПром");	
			ДобавитьУзелXML(УзелНОПром, "КодНО", Истина, КодНОПром);
			ДобавитьУзелXML(УзелНОПром, "НаимНО", Истина, НОПром.Наименование);
		КонецЕсли;
	КонецЕсли;
	
	// добавляем сведения о налогоплательщике
	УзелСвАдр = ДобавитьУзелXML(УзелДокумент, "СвАдр");
	Если РеквизитыНалогоплательщика.НаимОрг <> ""
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.ИННЮЛ) 
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.КПП) Тогда
		//налогоплательщик ЮЛ
		УзелЮЛ = ДобавитьУзелXML(УзелСвАдр, "ЮЛ");
		ДобавитьУзелXML(УзелЮЛ, "НаимОрг", 	Истина, РеквизитыНалогоплательщика.НаимОрг);
		ДобавитьУзелXML(УзелЮЛ, "ИННЮЛ", 	Истина, РеквизитыНалогоплательщика.ИННЮЛ);
		ДобавитьУзелXML(УзелЮЛ, "КПП", 		Истина, РеквизитыНалогоплательщика.КПП);
    ИначеЕсли ЗначениеЗаполнено(РеквизитыНалогоплательщика.ИННФЛ)
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.Фамилия) 
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.Имя) Тогда
    	//налогоплательщик ФЛ
		УзелФЛ = ДобавитьУзелXML(УзелСвАдр, "ФЛ");
		ДобавитьУзелXML(УзелФЛ, "ИННФЛ", 	Истина, РеквизитыНалогоплательщика.ИННФЛ);
		УзелФИО = ДобавитьУзелXML(УзелФЛ, "ФИО");
		ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, РеквизитыНалогоплательщика.Фамилия);
		ДобавитьУзелXML(УзелФИО, "Имя", Истина, РеквизитыНалогоплательщика.Имя);
		Если ЗначениеЗаполнено(РеквизитыНалогоплательщика.Отчество) Тогда
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, РеквизитыНалогоплательщика.Отчество);
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты налогоплательщика из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// добавляем узел ФИОДолж
	ФИОДолж = ПолучитьФИОДолж(ТранспортноеСообщение);
	Если ФИОДолж = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты налогоплательщика из настроек организации'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	УзелФИОДолж = ДобавитьУзелXML(УзелДокумент, "ФИОДолж");
	ДобавитьУзелXML(УзелФИОДолж, "Фамилия", Истина, ФИОДолж.Фамилия);
	ДобавитьУзелXML(УзелФИОДолж, "Имя", Истина, ФИОДолж.Имя);
	Если ЗначениеЗаполнено(ФИОДолж.Отчество) Тогда
		ДобавитьУзелXML(УзелФИОДолж, "Отчество", Истина, ФИОДолж.Отчество);
	КонецЕсли;
	
	// формируем XML-строку на основе дерева
	ТекстXML = ВыгрузитьДеревоВXML_ПФР(ДеревоXML, "Файл");
	
	Возврат ТекстXML;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLУведомленияОбОтказеДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаУведомления, ТаблицаОшибок)
	
	// выделяем имя файла без расширения из короткого имени файла уведомления
	РасширениеФайлаУведомления = РасширениеФайла(ИмяФайлаУведомления);
	ДлинаРасширенияИмениФайлаУведомления = СтрДлина(РасширениеФайлаУведомления);
	ИмяФайлаУведомленияБезРасширения = Лев(ИмяФайлаУведомления, СтрДлина(ИмяФайлаУведомления) - ДлинаРасширенияИмениФайлаУведомления - 1);
			  
	// определяем дату получения основания
	ДатаПолученияОснования = ТранспортноеСообщение.Основание.ДатаТранспорта;
	
	// получаем сведения о получателе из входящего требования
	РеквизитыПолучателя = ПолучитьРеквизитыПолучателяИзВходящегоТребования(ТранспортноеСообщение.Основание);
	Если РеквизитыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения о налогоплательщике из входящего требования
	РеквизитыНалогоплательщика = ПолучитьРеквизитыНалогоплательщикаИзВходящегоТребования(ТранспортноеСообщение.Основание);
	Если РеквизитыНалогоплательщика = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты налогоплательщика из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем XML-дерево уведомления
	ДеревоXML = СоздатьДеревоXML();
	
	// иниицализируем атрибуты корневого элемента
	ДобавитьУзелXML(ДеревоXML, "ИдФайл", Истина, ИмяФайлаУведомленияБезРасширения);
	ДобавитьУзелXML(ДеревоXML, "ВерсПрог", Истина, Лев(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40));
	ДобавитьУзелXML(ДеревоXML, "ВерсФорм", Истина, "5.02");
	
	// создаем узел Документ
	УзелДокумент = ДобавитьУзелXML(ДеревоXML, "Документ");
	ДобавитьУзелXML(УзелДокумент, "КНД", Истина, "1167003");
	
	// добавляем сведения о получателе
	УзелСвПолДок = ДобавитьУзелXML(УзелДокумент, "СвПолДок");
	Если (РеквизитыПолучателя.НаимОрг <> "" ИЛИ РеквизитыНалогоплательщика.НаимОрг <> "")
	И ЗначениеЗаполнено(РеквизитыПолучателя.ИННЮЛ) 
	И ЗначениеЗаполнено(РеквизитыПолучателя.КПП) Тогда
		//ЮЛ
		УзелЮЛ = ДобавитьУзелXML(УзелСвПолДок, "ПолЮЛ");
		
		НаимОрг = ?(РеквизитыПолучателя.НаимОрг = "", РеквизитыНалогоплательщика.НаимОрг, РеквизитыПолучателя.НаимОрг);
		
		ДобавитьУзелXML(УзелЮЛ, "НаимОрг", 	Истина, НаимОрг);
		ДобавитьУзелXML(УзелЮЛ, "ИННЮЛ", 	Истина, РеквизитыПолучателя.ИННЮЛ);
		ДобавитьУзелXML(УзелЮЛ, "КПП", 		Истина, РеквизитыПолучателя.КПП);
	ИначеЕсли ЗначениеЗаполнено(РеквизитыПолучателя.ИННФЛ)
	И ЗначениеЗаполнено(РеквизитыПолучателя.Фамилия) 
	И ЗначениеЗаполнено(РеквизитыПолучателя.Имя) Тогда
		//ФЛ
		УзелФЛ = ДобавитьУзелXML(УзелСвПолДок, "ПолФЛ");
		ДобавитьУзелXML(УзелФЛ, "ИННФЛ", 	Истина, РеквизитыПолучателя.ИННФЛ);
		УзелФИО = ДобавитьУзелXML(УзелФЛ, "ФИО");
		ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, РеквизитыПолучателя.Фамилия);
		ДобавитьУзелXML(УзелФИО, "Имя", Истина, РеквизитыПолучателя.Имя);
		Если ЗначениеЗаполнено(РеквизитыПолучателя.Отчество) Тогда
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, РеквизитыПолучателя.Отчество);
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// добавляем сведения о промежуточном НО
	ИдентификаторыПолучателя = ПолучитьИдентификаторыПолучателя(ТранспортноеСообщение.ЦиклОбмена);
	Если ИдентификаторыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;

	// получаем сведения об отправителе
	КодНО	= ИдентификаторыПолучателя.КонечныйНалоговыйОрган;
	ОтпрНО 	= Справочники.НалоговыеОрганы.НайтиПоКоду(КодНО);
	Если НЕ ЗначениеЗаполнено(ОтпрНО) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	СведенияОбОтправителе = СформироватьСведенияОбУчастникеДокументооборота(ОтпрНО, ТранспортноеСообщение);
	Если НЕ ЗначениеЗаполнено(СведенияОбОтправителе) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// добавляем сведения об отправителе
	УзелСвОтпрДок = ДобавитьУзелXML(УзелДокумент, "СвОтпрДок");
	ЗаполнитьУзелСведениямиОбУчастникеЭДОДокумент(УзелСвОтпрДок, СведенияОбОтправителе);
	
	// добавляем сведения о промежуточном НО
	КодНОПром = ИдентификаторыПолучателя.ПромежуточныйНалоговыйОрган;
	Если ЗначениеЗаполнено(КодНОПром) Тогда
		НОПром = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНОПром);
		Если ЗначениеЗаполнено(НОПром) Тогда
			УзелНОПром = ДобавитьУзелXML(УзелДокумент, "НОПром");	
			ДобавитьУзелXML(УзелНОПром, "КодНО", Истина, КодНОПром);
			ДобавитьУзелXML(УзелНОПром, "НаимНО", Истина, НОПром.Наименование);
		КонецЕсли;
	КонецЕсли;
	
	// добавляем сведения о налогоплательщике
	УзелСвАдр = ДобавитьУзелXML(УзелДокумент, "СвАдр");
	Если РеквизитыНалогоплательщика.НаимОрг <> ""
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.ИННЮЛ) 
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.КПП) Тогда
		//налогоплательщик ЮЛ
		УзелЮЛ = ДобавитьУзелXML(УзелСвАдр, "ЮЛ");
		ДобавитьУзелXML(УзелЮЛ, "НаимОрг", 	Истина, РеквизитыНалогоплательщика.НаимОрг);
		ДобавитьУзелXML(УзелЮЛ, "ИННЮЛ", 	Истина, РеквизитыНалогоплательщика.ИННЮЛ);
		ДобавитьУзелXML(УзелЮЛ, "КПП", 		Истина, РеквизитыНалогоплательщика.КПП);
    ИначеЕсли ЗначениеЗаполнено(РеквизитыНалогоплательщика.ИННФЛ)
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.Фамилия) 
	И ЗначениеЗаполнено(РеквизитыНалогоплательщика.Имя) Тогда
    	//налогоплательщик ФЛ
		УзелФЛ = ДобавитьУзелXML(УзелСвАдр, "ФЛ");
		ДобавитьУзелXML(УзелФЛ, "ИННФЛ", 	Истина, РеквизитыНалогоплательщика.ИННФЛ);
		УзелФИО = ДобавитьУзелXML(УзелФЛ, "ФИО");
		ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, РеквизитыНалогоплательщика.Фамилия);
		ДобавитьУзелXML(УзелФИО, "Имя", Истина, РеквизитыНалогоплательщика.Имя);
		Если ЗначениеЗаполнено(РеквизитыНалогоплательщика.Отчество) Тогда
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, РеквизитыНалогоплательщика.Отчество);
		КонецЕсли;
	Иначе
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты налогоплательщика из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// добавляем сведения об уведомлении
	УзелСведУведОтказ = ДобавитьУзелXML(УзелДокумент, "СведУведОтказ");
	
	// добавляем общие сведения об уведомлении
	УзелОбщСвУвед = ДобавитьУзелXML(УзелСведУведОтказ, "ОбщСвУвед");
	ЗаполнитьУзелОбщимиСведениямиОбУведомлении(УзелОбщСвУвед, ТранспортноеСообщение, ДИВы);
	
	// добавляем сведения о выявленных нарушениях

	Для каждого Ошибка Из ТаблицаОшибок Цикл
		КодОшибки = Ошибка.КодОшибки;
		ТекстОшибки = Ошибка.ТекстОшибки;
		УзелПеречВыявНар = ДобавитьУзелXML(УзелСведУведОтказ, "ПеречВыявНар");
		ДобавитьУзелXML(УзелПеречВыявНар, "КодОш", Ложь, КодОшибки);
		ДобавитьУзелXML(УзелПеречВыявНар, "ТекстОш", Ложь, ТекстОшибки, Ложь, Истина);
	КонецЦикла;
	
	// добавляем узел ФИОДолж
	ФИОДолж = ПолучитьФИОДолж(ТранспортноеСообщение);
	Если ФИОДолж = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты налогоплательщика из настроек организации'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	УзелФИОДолж = ДобавитьУзелXML(УзелДокумент, "ФИОДолж");
	ДобавитьУзелXML(УзелФИОДолж, "Фамилия", Истина, ФИОДолж.Фамилия);
	ДобавитьУзелXML(УзелФИОДолж, "Имя", Истина, ФИОДолж.Имя);
	Если ЗначениеЗаполнено(ФИОДолж.Отчество) Тогда
		ДобавитьУзелXML(УзелФИОДолж, "Отчество", Истина, ФИОДолж.Отчество);
	КонецЕсли;
	
	// формируем XML-строку на основе дерева
	ТекстXML = ВыгрузитьДеревоВXML_ПФР(ДеревоXML, "Файл");
	
	Возврат ТекстXML;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьВложениеКвитанцияОПриемеДокумент(ТранспортноеСообщение) Экспорт
	
	// проверяем, не сформирована ли уже квитанция о приеме
	ИмеющиесяКвитанции = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
	Если ИмеющиесяКвитанции.Количество() > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	ТипыДИВ = Новый Массив;
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
	
	ДИВы = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Ложь, ТипыДИВ);
	
	Если ДИВы.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать уведомление об отказе: отсутствует исходный документ в сообщении-основании.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ДИВы.Колонки.Добавить("ПодписьBase64");
	
	// формируем имя файла квитанции
	UUIDКвитанции = СгенерироватьUUID();
	ИмяФайлаКвитанции = СформироватьИмяФайлаКвитанции(ТранспортноеСообщение, ДИВы, UUIDКвитанции);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаКвитанции) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать квитанцию о приеме документа: не удалось сформировать имя файла квитанции.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;

	// формируем XML квитанции о приеме документа
	ТекстКвитанции = СформироватьXMLКвитанцииОПриемеДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаКвитанции);
	Если НЕ ЗначениеЗаполнено(ТекстКвитанции) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// пишем XML во временный файл в кодировке windows-1251
	ФайлКвитанции = ВыгрузитьТекстВФайл(ТекстКвитанции);
	Если НЕ ЗначениеЗаполнено(ФайлКвитанции) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл извещения в ИБ
	ТипСодержимогоТранспортногоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент;
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
		ТранспортноеСообщение,
		ТипСодержимогоТранспортногоКонтейнера,
		ФайлКвитанции,
		ИмяФайлаКвитанции,,,,,,,
		UUIDКвитанции,
		Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		
		Возврат Ложь;
	КонецЕсли;
	
	// удаляем временный файл с квитанцией
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлКвитанции);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьВложениеУведомлениеОбОтказеДокумент(ТранспортноеСообщение) Экспорт
	
	// проверяем, не сформировано ли уже уведомление об отказе
	ИмеющиесяУведомления = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент);
	Если ИмеющиесяУведомления.Количество() > 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	ТипыДИВ = Новый Массив;
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);
	ТипыДИВ.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение);
	
	ДИВы = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение.Основание, Ложь, ТипыДИВ);
	
	Если ДИВы.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать уведомление об отказе: отсутствует исходный документ в сообщении-основании.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ДИВы.Колонки.Добавить("ПодписьBase64");
	
	// формируем имя файла уведомления
	UUIDУведомления = СгенерироватьUUID();
	ИмяФайлаУведомления = СформироватьИмяФайлаУведомленияОбОтказеДокумент(ТранспортноеСообщение, ДИВы, UUIDУведомления);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаУведомления) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось создать уведомление об отказе: не удалось сформировать имя файла уведомления.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ТаблицаОшибок = ПолучитьТаблицуОшибокОписиВходящихДокументов();
	Если ТаблицаОшибок = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// формируем XML уведомления о приеме документа
	ТекстУведомления = СформироватьXMLУведомленияОбОтказеДокумента(ТранспортноеСообщение, ДИВы, ИмяФайлаУведомления, ТаблицаОшибок);
	Если НЕ ЗначениеЗаполнено(ТекстУведомления) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// пишем XML во временный файл в кодировке windows-1251
	ФайлУведомления = ВыгрузитьТекстВФайл(ТекстУведомления);
	Если НЕ ЗначениеЗаполнено(ФайлУведомления) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл извещения в ИБ
	ТипСодержимогоТранспортногоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент;
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
		ТранспортноеСообщение,
		ТипСодержимогоТранспортногоКонтейнера,
		ФайлУведомления,
		ИмяФайлаУведомления,,,,,,,
		UUIDУведомления,
		Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		
		Возврат Ложь;
	КонецЕсли;
	
	// удаляем временный файл с уведомлением
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлУведомления);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИмяФайлаКвитанции(ТранспортноеСообщение, ДИВы, UUID)
	
	Префикс = "KV";	
	
	// выделяем префикс из имени основного ДИВ основания
	ОсновнойДИВ = ДИВы.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ))[0];
	ИмяФайлаДИВ = ОсновнойДИВ.ИмяФайла;
	
	ПрефиксОснования = ВыделитьПрефиксИзИмениФайлаОснования(ИмяФайлаДИВ);
	Если НЕ ЗначениеЗаполнено(ПрефиксОснования) Тогда
		// если префикс выделить не удалось, то пишем хоть какой-нибудь (это лучше, чем не отправлять извещение вообще)
		ПрефиксОснования = "NO_PREF";
	КонецЕсли;
	
	ИдентификаторыПолучателя = ПолучитьИдентификаторыПолучателя(ТранспортноеСообщение.ЦиклОбмена);
	Если ИдентификаторыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	ИдентификаторПолучателя 		 = ИдентификаторыПолучателя.ПромежуточныйНалоговыйОрган;
	ИдентификаторКонечногоПолучателя = ИдентификаторыПолучателя.КонечныйНалоговыйОрган;
	
	// формируем идентификатор отправителя
	РеквизитыПолучателя = ПолучитьРеквизитыПолучателяИзВходящегоТребования(ТранспортноеСообщение.Основание);
	Если РеквизитыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;

	Если ЗначениеЗаполнено(РеквизитыПолучателя.ИННЮЛ) 
	И ЗначениеЗаполнено(РеквизитыПолучателя.КПП) Тогда
		//налогоплательщик ЮЛ
		ИдентификаторОтправителя = РеквизитыПолучателя.ИННЮЛ + РеквизитыПолучателя.КПП;
    ИначеЕсли ЗначениеЗаполнено(РеквизитыПолучателя.ИННФЛ) Тогда
    	//налогоплательщик ФЛ
		ИдентификаторОтправителя = РеквизитыПолучателя.ИННФЛ;
	Иначе
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// формируем строку с текущей датой
	GGGGMMDD = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd");
	
	// 
	ИдентификаторФайла = UUID;
	
	//
	РасширениеФайла = "xml";
	
	// собираем имя файла квитанции из составляющих
	ИмяФайлаквитанции = Префикс + "_" + ПрефиксОснования + "_" + ИдентификаторПолучателя + "_" + ИдентификаторКонечногоПолучателя + "_" + ИдентификаторОтправителя + "_" + GGGGMMDD + "_" + ИдентификаторФайла + "." + РасширениеФайла;
	Возврат ИмяФайлаКвитанции;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИдентификаторыПолучателя(ЦиклОбмена)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьИдентификаторыПолучателя(ЦиклОбмена);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИмяФайлаУведомленияОбОтказеДокумент(ТранспортноеСообщение, ДИВы, UUID)
	
	Префикс = "UO";	
	
	// выделяем префикс из имени основного ДИВ основания
	ОсновнойДИВ = ДИВы.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ))[0];
	ИмяФайлаДИВ = ОсновнойДИВ.ИмяФайла;
	
	ПрефиксОснования = ВыделитьПрефиксИзИмениФайлаОснования(ИмяФайлаДИВ);
	Если НЕ ЗначениеЗаполнено(ПрефиксОснования) Тогда
		// если префикс выделить не удалось, то пишем хоть какой-нибудь (это лучше, чем не отправлять извещение вообще)
		ПрефиксОснования = "NO_PREF";
	КонецЕсли;
	
	ИдентификаторыПолучателя = ПолучитьИдентификаторыПолучателя(ТранспортноеСообщение.ЦиклОбмена);
	Если ИдентификаторыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	ИдентификаторПолучателя 		 = ИдентификаторыПолучателя.ПромежуточныйНалоговыйОрган;
	ИдентификаторКонечногоПолучателя = ИдентификаторыПолучателя.КонечныйНалоговыйОрган;
	
	// формируем идентификатор отправителя
	РеквизитыПолучателя = ПолучитьРеквизитыПолучателяИзВходящегоТребования(ТранспортноеСообщение.Основание);
	Если РеквизитыПолучателя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;

	Если ЗначениеЗаполнено(РеквизитыПолучателя.ИННЮЛ) 
	И ЗначениеЗаполнено(РеквизитыПолучателя.КПП) Тогда
		//налогоплательщик ЮЛ
		ИдентификаторОтправителя = РеквизитыПолучателя.ИННЮЛ + РеквизитыПолучателя.КПП;
    ИначеЕсли ЗначениеЗаполнено(РеквизитыПолучателя.ИННФЛ) Тогда
    	//налогоплательщик ФЛ
		ИдентификаторОтправителя = РеквизитыПолучателя.ИННФЛ;
	Иначе
		ТекстСообщения = НСтр("ru = 'Не удалось извлечь реквизиты получателя из входящего документа налогового органа'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	    Возврат Неопределено;
	КонецЕсли;
	
	// формируем строку с текущей датой
	GGGGMMDD = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd");
	
	// 
	ИдентификаторФайла = UUID;
	
	//
	РасширениеФайла = "xml";
	
	// собираем имя файла уведомления из составляющих
	ИмяФайлаУведомления = Префикс + "_" + ПрефиксОснования + "_" + ИдентификаторПолучателя + "_" + ИдентификаторКонечногоПолучателя + "_" + ИдентификаторОтправителя + "_" + GGGGMMDD + "_" + ИдентификаторФайла + "." + РасширениеФайла;
	Возврат ИмяФайлаУведомления;
	
КонецФункции
       
&НаСервереБезКонтекста
Функция ПолучитьФИОДолж(ТранспортноеСообщение)
	
	Организация = ТранспортноеСообщение.Отправитель;
	флПБОЮЛ = НЕ РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(Организация);
	КодНО = ТранспортноеСообщение.Основание.Отправитель.Код;
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНО);
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		// получим ФИО уполномоченного представителя из доверенности
		
		Возврат ПолучитьФИОДоверенности(Доверенность, Перечисления.СубъектыДоверенностиНалогоплательщика.ПредставительФЛ);
	Иначе
		// получим ФИО руководителя организации / индивидуального предпринимателя
		Если флПБОЮЛ Тогда
			Возврат РегламентированнаяОтчетность.РазложитьФИО(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "ФИО").ФИО);
		Иначе
			Возврат РегламентированнаяОтчетность.РазложитьФИО(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "ФИОРук").ФИОРук);
		КонецЕсли;
	КонецЕсли;
	
КонецФункции 

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеКвитанцииОПриемеЗаявление(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// формируем таблицу с объявлениями полей узла с общими сведениями
	ПоляОбщихСведений = Новый ТаблицаЗначений;
	ПоляОбщихСведений.Колонки.Добавить("Код");
	ПоляОбщихСведений.Колонки.Добавить("Имя");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИдФайл", "Идентификатор файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаВремяНапр", "Дата и время направления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "КНД", "Код формы по КНД");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НаимВидДок", "Наименование вида документа");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерДокНП", "Номер заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаДокНП", "Дата заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаПост", "Дата поступления отчета");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаПрин", "Дата принятия отчета");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "РегНом", "Рег. номер заявления в налоговом органе");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРег", "Дата регистрации заявления");
	
	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	// выводим секцию заголовка
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	// выводим разделитель
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	// пытаемся разобрать уведомление
	СвойстваИзКвитанции = ПолучитьОсновныеСвойстваИзКвитанцииОПриемеЗаявление(Тело);
	
	// если разобрать не удалось - выводим его в виде текста
	Если НЕ ЗначениеЗаполнено(СвойстваИзКвитанции) Тогда
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
		
		// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Иначе
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		
		Если СвойстваИзКвитанции.Ошибки.Количество() > 0 Тогда
			
			// выводим "шапку" таблицы ошибок
			Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
			Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Ошибки:";
			ТаблДок.Вывести(Секция_ЗаголовокГруппы);
			
			// получаем область для вывода единицы информации уведомления
			Для Каждого Ошибка Из СвойстваИзКвитанции.Ошибки Цикл
				ВывестиТекстПострочно(ТаблДок, Секция_Тело, "Ошибка " + Ошибка.КодОшибки + ": " + Ошибка.Описание);
			КонецЦикла;
			
			// выводим пустую строку - разделитель
			Секция_Тело.Области.Тело.Текст = "";
			ТаблДок.Вывести(Секция_Тело);
			
		КонецЕсли;
		
		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Общие сведения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим общие сведения поэлементно
		ОбщиеСведенияСообщения = СвойстваИзКвитанции.ОбщиеСведения;
		Для Каждого ПолеОбщихСведений Из ПоляОбщихСведений Цикл
			Если ОбщиеСведенияСообщения.Свойство(ПолеОбщихСведений.Код) Тогда
				ЗначениеПоля = ОбщиеСведенияСообщения[ПолеОбщихСведений.Код];
				Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
					Секция_Тело.Области.Тело.Текст = ПолеОбщихСведений.Имя + ": " + СокрЛП(ЗначениеПоля);
					ТаблДок.Вывести(Секция_Тело);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Разделитель);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);
	
	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеСообщенияОПростОтметки(Заголовок, ИмяФайла, Тело, НижнийКолонтитул) Экспорт

	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("СообщениеОПроставленииОтметкиНалоговогоОргана");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	СвойстваИзКвитанции = ПолучитьОсновныеСвойстваИзСообщенияОПростОтметки(Тело);
	ОбщиеСведения = СвойстваИзКвитанции.ОбщиеСведения;
	
	// выводим секцию заголовка
	Секция_Тело = Бланк.ПолучитьОбласть("СообщениеОПроставленииОтметки");
	Секция_Тело.Параметры.Заполнить(ОбщиеСведения.СвОтметка);
	
	Секция_Тело.Параметры.ОтправительЗаявления = ОбщиеСведения.ОтпрНО.НаимНО + ", " + ОбщиеСведения.ОтпрНО.КодНО;
	Если ОбщиеСведения.Свойство("НОПром") Тогда
		Секция_Тело.Параметры.НалоговыйОрган = ОбщиеСведения.НОПром.НаимНО + ", " + ОбщиеСведения.НОПром.КодНО;
	Иначе
		Секция_Тело.Параметры.НалоговыйОрган = ОбщиеСведения.ОтпрНО.НаимНО + ", " + ОбщиеСведения.ОтпрНО.КодНО;
	КонецЕсли;
	
	ДолжНО = ОбщиеСведения.ДолжНО;
	Секция_Тело.Параметры.ДолжностьЧин = 
		?(ДолжНО.Свойство("Долж"), ДолжНО.Долж, "") + " " 
		+ ?(ДолжНО.Свойство("Чин"), ДолжНО.Чин, "");
	Секция_Тело.Параметры.ДолжностьЧин = СокрЛП(Секция_Тело.Параметры.ДолжностьЧин);
	
	ФИОДолж = ОбщиеСведения.ФИОДолж;
	Секция_Тело.Параметры.ФИО = 
		СокрЛП(ФИОДолж.Фамилия + " "
		+ ФИОДолж.Имя + " "
		+ ?(ФИОДолж.Свойство("Отчество"), ФИОДолж.Отчество, ""));
		
	Секция_Тело.Параметры.Заполнить(ОбщиеСведения.ДолжНО); // Тлф, если есть
		
	// ИП и ЮЛ
	Если ОбщиеСведения.Свойство("ФЛ") Тогда
		
		ФЛ  = ОбщиеСведения.ФЛ;
		ФИО = ОбщиеСведения.ФИО;
		
		Секция_Тело.Параметры.Налогоплательщик = 
			СокрЛП(ФИО.Фамилия + " "
			+ ФИО.Имя + " "
			+ ?(ФИО.Свойство("Отчество"), ФИО.Отчество, "")) + ", "
			+ ФЛ.ИННФЛ;

	Иначе
		
		ЮЛ  = ОбщиеСведения.ЮЛ;
		
		Секция_Тело.Параметры.Налогоплательщик =
			?(ЮЛ.Свойство("НаимОрг"), ЮЛ.НаимОрг + ", ", "")
			+ ЮЛ.ИННЮЛ
			+ ?(ЮЛ.Свойство("КПП"), "/" + ЮЛ.КПП, "");
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Тело);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);
	
	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеСообщенияОбОтзыве(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// формируем таблицу с объявлениями полей узла с общими сведениями
	ПоляОбщихСведений = Новый ТаблицаЗначений;
	ПоляОбщихСведений.Колонки.Добавить("Код");
	ПоляОбщихСведений.Колонки.Добавить("Имя");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИдФайл", "Идентификатор файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерДокНП", "Номер заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаДокНП", "Дата заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "РегНомДок", "Рег. номер заявления в налоговом органе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегЭл", "Дата регистрации в НО в электронном виде");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегБум", "Дата регистрации в НО на бумажном носителе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаОснОтз", "Дата служебной записки - основания отзыва заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "КодСтранПрод1", "Код страны продавца");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ПричЗам", "Причина замены заявления");
	
	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	// выводим секцию заголовка
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	// выводим разделитель
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	// пытаемся разобрать уведомление
	СвойстваИзИзвещения = ПолучитьОсновныеСвойстваИзСообщенияОбОтзыве(Тело);
	
	// если разобрать не удалось - выводим его в виде текста
	Если НЕ ЗначениеЗаполнено(СвойстваИзИзвещения) Тогда
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
		
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Иначе
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		
		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Общие сведения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим общие сведения поэлементно
		ОбщиеСведенияИзвещения = СвойстваИзИзвещения.ОбщиеСведения;
		Для Каждого ПолеОбщихСведений Из ПоляОбщихСведений Цикл
			Если ОбщиеСведенияИзвещения.Свойство(ПолеОбщихСведений.Код) Тогда
				ЗначениеПоля = ОбщиеСведенияИзвещения[ПолеОбщихСведений.Код];
				Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
					Секция_Тело.Области.Тело.Текст = ПолеОбщихСведений.Имя + ": " + СокрЛП(ЗначениеПоля);
					ТаблДок.Вывести(Секция_Тело);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Разделитель);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);
	
	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеСообщенияОНесоответствиях(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// формируем таблицу с объявлениями полей узла с общими сведениями
	ПоляОбщихСведений = Новый ТаблицаЗначений;
	ПоляОбщихСведений.Колонки.Добавить("Код");
	ПоляОбщихСведений.Колонки.Добавить("Имя");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИдФайл", "Идентификатор файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерДокНП", "Номер заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаДокНП", "Дата заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "РегНомДок", "Рег. номер заявления в налоговом органе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегЭл", "Дата регистрации в НО в электронном виде");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегБум", "Дата регистрации в НО на бумажном носителе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "КодСтранПрод1", "Код страны продавца");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИмяФЛК", "Имя файла протокола ФЛК");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаФЛК", "Дата протокола ФЛК");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаИД", "Дата ошибок идентификации");
	
	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	// выводим секцию заголовка
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	// выводим разделитель
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	// пытаемся разобрать уведомление
	СвойстваИзИзвещения = ПолучитьОсновныеСвойстваИзСообщенияОНесоответствиях(Тело);
	
	// если разобрать не удалось - выводим его в виде текста
	Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
	Если НЕ ЗначениеЗаполнено(СвойстваИзИзвещения) Тогда
		
		ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
		
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Иначе
		
		Если СвойстваИзИзвещения.Ошибки.Количество() > 0 Тогда
			// выводим "шапку" таблицы ошибок
			Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
			Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Ошибки ФЛК:";
			ТаблДок.Вывести(Секция_ЗаголовокГруппы);
			
			// получаем область для вывода единицы информации уведомления
			Для Каждого Ошибка Из СвойстваИзИзвещения.Ошибки Цикл
				ВывестиТекстПострочно(ТаблДок, Секция_Тело, "Ошибка " + Ошибка.КодОшибки + ": " + Ошибка.Описание);
			КонецЦикла;
			
			// выводим пустую строку - разделитель
			Секция_Тело.Области.Тело.Текст = "";
			ТаблДок.Вывести(Секция_Тело);
		КонецЕсли;		
		
		Если СвойстваИзИзвещения.ОшибкиИД.Количество() > 0 Тогда
			// выводим "шапку" таблицы ошибок идентификации
			Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
			Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Ошибки идентификации:";
			ТаблДок.Вывести(Секция_ЗаголовокГруппы);
			
			// получаем область для вывода единицы информации 
			Для Каждого Ошибка Из СвойстваИзИзвещения.ОшибкиИД Цикл
				ВывестиТекстПострочно(ТаблДок, Секция_Тело, "Продавец с ИНН (УНП, ИИН) " + Ошибка.ИНН + ": " + Ошибка.Комментарий);
			КонецЦикла;
			
			// выводим пустую строку - разделитель
			Секция_Тело.Области.Тело.Текст = "";
			ТаблДок.Вывести(Секция_Тело);
		КонецЕсли;	
		
		
		
		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Общие сведения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим общие сведения поэлементно
		ОбщиеСведенияИзвещения = СвойстваИзИзвещения.ОбщиеСведения;
		Для Каждого ПолеОбщихСведений Из ПоляОбщихСведений Цикл
			Если ОбщиеСведенияИзвещения.Свойство(ПолеОбщихСведений.Код) Тогда
				ЗначениеПоля = ОбщиеСведенияИзвещения[ПолеОбщихСведений.Код];
				Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
					Секция_Тело.Области.Тело.Текст = ПолеОбщихСведений.Имя + ": " + СокрЛП(ЗначениеПоля);
					ТаблДок.Вывести(Секция_Тело);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Разделитель);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);
	
	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеСообщенияОПроверке(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// формируем таблицу с объявлениями полей узла с общими сведениями
	ПоляОбщихСведений = Новый ТаблицаЗначений;
	ПоляОбщихСведений.Колонки.Добавить("Код");
	ПоляОбщихСведений.Колонки.Добавить("Имя");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИдФайл", "Идентификатор файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "КНД", "Код формы по КНД");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерДокНП", "Номер заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаДокНП", "Дата заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "РегНомДок", "Рег. номер заявления в налоговом органе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегЭл", "Дата регистрации в НО в электронном виде");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегБум", "Дата регистрации в НО на бумажном носителе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерУведОткз", "Номер уведомления об отказе в проставлении отметки");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаУведОткз", "Дата уведомления об отказе в проставлении отметки");
	
	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	// выводим секцию заголовка
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	// выводим разделитель
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	// пытаемся разобрать уведомление
	СвойстваИзИзвещения = ПолучитьОсновныеСвойстваИзСообщенияОПроверке(Тело);
	
	// если разобрать не удалось - выводим его в виде текста
	Если НЕ ЗначениеЗаполнено(СвойстваИзИзвещения) Тогда
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
		
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Иначе
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		
		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Результат проверки:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим отметку
		Секция_Тело.Области.Тело.Текст = СокрЛП(СвойстваИзИзвещения.Отметка);
		ТаблДок.Вывести(Секция_Тело);
		
		// выводим пустую строку - разделитель
		Секция_Тело.Области.Тело.Текст = "";
		ТаблДок.Вывести(Секция_Тело);

		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Общие сведения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим общие сведения поэлементно
		ОбщиеСведенияИзвещения = СвойстваИзИзвещения.ОбщиеСведения;
		Для Каждого ПолеОбщихСведений Из ПоляОбщихСведений Цикл
			Если ОбщиеСведенияИзвещения.Свойство(ПолеОбщихСведений.Код) Тогда
				ЗначениеПоля = ОбщиеСведенияИзвещения[ПолеОбщихСведений.Код];
				Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
					Секция_Тело.Области.Тело.Текст = ПолеОбщихСведений.Имя + ": " + СокрЛП(ЗначениеПоля);
					ТаблДок.Вывести(Секция_Тело);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Разделитель);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);

	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзКвитанцииОПриемеЗаявление(СтрКвитанция)
	
	Если НЕ ЗначениеЗаполнено(СтрКвитанция) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТаблицаОшибок = Новый ТаблицаЗначений;
	ТаблицаОшибок.Колонки.Добавить("КодОшибки");
	ТаблицаОшибок.Колонки.Добавить("Описание");

	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрКвитанция, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвКвит = УзелДокумент.Строки.Найти("СвКвит", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвКвит) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""СвКвит"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с перечнем ошибок
	УзлыОшибки = УзелСвКвит.Строки.НайтиСтроки(Новый Структура("Имя", "Ошибки"));
	Для Каждого УзелОшибки Из УзлыОшибки Цикл
		
		УзелКодОшиб = УзелОшибки.Строки.Найти("КодОшиб", "Имя");
		УзелТекстОшиб = УзелОшибки.Строки.Найти("ТекстОшиб", "Имя");
		
		КодОшибки = ?(ЗначениеЗаполнено(УзелКодОшиб), СокрЛП(УзелКодОшиб.Значение), "");
		ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОшиб), СокрЛП(УзелТекстОшиб.Значение), "");
		
		НовСтр = ТаблицаОшибок.Добавить();
		НовСтр.КодОшибки = КодОшибки;
		НовСтр.Описание = ТекстОшибки;
		
	КонецЦикла;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	Для Каждого УзелОбщСвед Из УзелСвКвит.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	Если ОбщиеСведения.Свойство("ДатаНапр") И ОбщиеСведения.Свойство("ВремНапр") Тогда
		ОбщиеСведения.Вставить("ДатаВремяНапр", ОбщиеСведения.ДатаНапр + " " + ОбщиеСведения.ВремНапр);
	КонецЕсли;
	
	Возврат Новый Структура("Ошибки, ОбщиеСведения", ТаблицаОшибок, ОбщиеСведения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзСообщенияОПростОтметки(СтрКвитанция)
	
	Если НЕ ЗначениеЗаполнено(СтрКвитанция) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ОбщиеСведения = Новый Структура;
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрКвитанция, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Для Каждого УзелСведений Из УзелФайл.Строки Цикл
		ИмяУзла = УзелСведений.Имя;
		Если ИмяУзла = "ИдФайл" ИЛИ ИмяУзла = "ВерсПрог" ИЛИ ИмяУзла = "ВерсФорм" Тогда
			ОбщиеСведения.Вставить(УзелСведений.Имя, СокрЛП(УзелСведений.Значение));
		КонецЕсли;
	КонецЦикла;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Для Каждого УзелОбщСвед Из УзелДокумент.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	ПолучитьСведенияУзла(УзелДокумент, "ОтпрЮЛ",    ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ОтпрФЛ",    ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "СвОтметка", ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ОтпрНО",    ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "НОПром",    ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ЮЛ", 		ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ФЛ", 		ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ФИОДолж", 	ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ФИО", 		ОбщиеСведения);
	ПолучитьСведенияУзла(УзелДокумент, "ДолжНО", 	ОбщиеСведения);
	
	Возврат Новый Структура("ОбщиеСведения", ОбщиеСведения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзСообщенияОбОтзыве(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвОтзыв = УзелДокумент.Строки.Найти("СвОтзыв", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвОтзыв) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""СвОтзыв"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	Для Каждого УзелОбщСвед Из УзелСвОтзыв.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	// разбираем сведения об отметке
	УзелПричЗам = УзелСвОтзыв.Строки.Найти("ПричЗам", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелПричЗам) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""ПричЗам"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Отказ = Истина;
		Возврат Неопределено;
	Иначе
		КодПричЗам = СокрЛП(УзелПричЗам.Значение);
		Если КодПричЗам = "001" Тогда
			ПричЗам = "По инициативе налогоплательщика";
		ИначеЕсли КодПричЗам = "002" Тогда
			ПричЗам = "По инициативе налогового органа";
		Иначе
			ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""ПричЗам"".'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Отказ = Истина;
			Возврат Неопределено;	
		КонецЕсли;
		ОбщиеСведения.Вставить("ПричЗам", ПричЗам);
	КонецЕсли;
	
	Возврат Новый Структура("ОбщиеСведения", ОбщиеСведения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзСообщенияОНесоответствиях(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Ошибки = Новый ТаблицаЗначений;
	Ошибки.Колонки.Добавить("КодОшибки");
	Ошибки.Колонки.Добавить("Описание");
	
	ОшибкиИД = Новый ТаблицаЗначений;
	ОшибкиИД.Колонки.Добавить("ИНН");
	ОшибкиИД.Колонки.Добавить("Комментарий");

	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвНесоответ = УзелДокумент.Строки.Найти("СвНесоответ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвНесоответ) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""СвНесоответ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем ошибки ФЛК
	УзлыОшибкиФЛК = УзелСвНесоответ.Строки.НайтиСтроки(Новый Структура("Имя", "ОшибкиФЛК"));
	Для Каждого УзелОшибкиФЛК Из УзлыОшибкиФЛК Цикл
		УзелКодОш = УзелОшибкиФЛК.Строки.Найти("КодОшиб", "Имя");
		УзелТекстОш = УзелОшибкиФЛК.Строки.Найти("ТекстОшиб", "Имя");
		Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш) Тогда
			
			КодОшибки = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "");
			ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "");
			
			НовСтр = Ошибки.Добавить();
			НовСтр.КодОшибки = КодОшибки;
			НовСтр.Описание = ТекстОшибки;
			
		КонецЕсли;
	КонецЦикла;
	
	// анализируем ошибки идентификации
	УзлыОшибкиИД = УзелСвНесоответ.Строки.НайтиСтроки(Новый Структура("Имя", "ОшибкиИД"));
	Для Каждого УзелОшибкиИД Из УзлыОшибкиИД Цикл
		УзелИдНомПрод1 = УзелОшибкиИД.Строки.Найти("ИдНомПрод1", "Имя");
		УзелКоммент = УзелОшибкиИД.Строки.Найти("Коммент", "Имя");
		Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш) Тогда
			
			ИНН = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "");
			Комментарий = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "");
			
			НовСтр = ОшибкиИД.Добавить();
			НовСтр.ИНН = ИНН;
			НовСтр.Комментарий = Комментарий;
			
		КонецЕсли;
	КонецЦикла;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	Для Каждого УзелОбщСвед Из УзелСвНесоответ.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	Возврат Новый Структура("Ошибки, ОшибкиИД, ОбщиеСведения", Ошибки, ОшибкиИД, ОбщиеСведения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзСообщенияОПроверке(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвПроверка = УзелДокумент.Строки.Найти("СвПроверка", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвПроверка) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""СвПроверка"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	Для Каждого УзелОбщСвед Из УзелСвПроверка.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	// разбираем сведения об отметке
	УзелОтметка = УзелСвПроверка.Строки.Найти("Отметка", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелОтметка) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Отметка"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Отказ = Истина;
		Возврат Неопределено;
	Иначе
		КодОтметка = СокрЛП(УзелОтметка.Значение);
		Если КодОтметка = "1" Тогда
			Отметка = "Проставлена отметка об уплате косвенных налогов.";
		ИначеЕсли КодОтметка = "2" Тогда
			Отметка = "Проставлена отметка об освобождении от налогобложения.";
		ИначеЕсли КодОтметка = "3" Тогда
			Отметка = "Принято решение об отказе в проставлении отметки";
		Иначе
			ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: некорректное значение узла ""Отметка"".'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Отказ = Истина;
			Возврат Неопределено;	
		КонецЕсли;
	КонецЕсли;
	
	Возврат Новый Структура("Отметка, ОбщиеСведения", Отметка, ОбщиеСведения);
	
КонецФункции

&НаСервереБезКонтекста
Процедура УстановитьУчетнуюЗаписьОрганизации(Организация,УчетнаяЗапись) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
		Объект 									 = Организация.ПолучитьОбъект();
		Объект.УчетнаяЗаписьОбмена 				 = УчетнаяЗапись;
		Объект.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате;
		
		Объект.Записать();
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновление(ПараметрыФункции, ВыполняемоеОповещение, ПовторятьСоединение = Истина) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	
	//в клиент-серверном режиме необходимы промежуточные переменные
	ВыводитьСообщения = НЕ (ПараметрыФункции.ТихийРежим 
		ИЛИ ПараметрыФункции.РежимАвтонастройки И НЕ ПараметрыФункции.ИгнорироватьРежимАвтонастройки
		ИЛИ ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности);
	
	// получаем и разбираем данные с сервера СОС
	ПараметрыРегистрацияПолучитьДанныеССервера = ПараметрыРегистрацияПолучитьДанныеССервера();
	ЗаполнитьЗначенияСвойств(ПараметрыРегистрацияПолучитьДанныеССервера, ПараметрыФункции);
	ПараметрыРегистрацияПолучитьДанныеССервера.ФайлИзКэша = Ложь;
	ПараметрыРегистрацияПолучитьДанныеССервера.ВыводитьСообщения = ВыводитьСообщения;
	ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес = ЭтаФорма.УникальныйИдентификатор;
	ПараметрыРегистрацияПолучитьДанныеССервера.ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ЭтаФорма.УникальныйИдентификатор;
	ПараметрыРегистрацияПолучитьДанныеССервера.СсылкаНаЗаявление = ПараметрыФункции.СсылкаНаЗаявление;
	
	ДополнительныеПараметры = Новый Структура("ПараметрыФункции, ВыполняемоеОповещение, ВыводитьСообщения, ПараметрыРегистрацияПолучитьДанныеССервера, ПовторятьСоединение", ПараметрыФункции, ВыполняемоеОповещение, ВыводитьСообщения, ПараметрыРегистрацияПолучитьДанныеССервера, ПовторятьСоединение);
	ОбработатьОбновлениеВЦикле(Истина, ДополнительныеПараметры);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЭтоСообщениеЗашифрованоНаОблачныйСертификат(Адрес, СерийныйНомерТекущегоПользователя = "")
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоСообщениеЗашифрованоНаОблачныйСертификат(Адрес, СерийныйНомерТекущегоПользователя);
	
КонецФункции

&НаКлиенте
Процедура ОбработатьОбновлениеВЦикле(Результат, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОбработатьОбновление(), которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ПараметрыФункции      = ДополнительныеПараметры.ПараметрыФункции;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Результат <> Истина Тогда
		
		РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Ложь);
		РезультатОбновления.Вставить("ОшибкаПолученияДанныхССервера");
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
		
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОбработатьОбновлениеВЦиклеПослеУстановкиКорневогоСертификатаВР",
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	УстановитьСертификатыВРПриНеобходимости(ОписаниеОповещения);
		
КонецПроцедуры

&НаКлиенте
Функция ИзвлечьОписаниеПричиныОшибки(Результат)
	
	Перем ОписаниеОшибки;
	
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		Если Результат.Свойство("ОписаниеОшибки", ОписаниеОшибки) Тогда
			Возврат ОписаниеОшибки;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

&НаКлиенте
Процедура ОбработатьОбновлениеВЦиклеПослеПолученияВременногоКаталогаЗавершение(РезультатОперацииСФайлами, ДополнительныеПараметры) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОбработатьОбновление(), которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ДополнительныеПараметры.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	Результат = ДополнительныеПараметры.Результат;
	ПараметрыАвторизации = ДополнительныеПараметры.ПараметрыАвторизации;
	ФайлИзКэша = ДополнительныеПараметры.ФайлИзКэша;
	
	Если НЕ РезультатОперацииСФайлами.Выполнено Тогда
		РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Ложь);
		РезультатОбновления.Вставить("ОшибкаСозданияКаталога", ИзвлечьОписаниеПричиныОшибки(РезультатОперацииСФайлами));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
		Возврат;
	КонецЕсли;
	
	ВременныйКаталог = РезультатОперацииСФайлами.ИмяКаталога;
	
		// Среди пришедших в рег файле отпечатков ищем тот, который пришел в ответе.
	// Если отпечатки совпадают, то устанавливаем сертификат, иначе пропускаем установку сертифката

	ЭтоВторичноеЗаявление = ПараметрыФункции.ЭтоВторичноеЗаявление;
	ОтпечатокИзОтвета     = ПараметрыФункции.ОтпечатокСертификатаИзОтвета;
	
	Если ЗначениеЗаполнено(ОтпечатокИзОтвета) 
		И ЭтоВторичноеЗаявление Тогда
		
		УстанавливатьСертификат = ДокументооборотСКОКлиентСервер.УстанавливатьСертификат(
			ОтпечатокИзОтвета, 
			Сертификаты);
		
	Иначе
		УстанавливатьСертификат = Истина;
	КонецЕсли;
	
	// устанавливаем сертификаты
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ОтпечатокСертификата", ОтпечатокИзОтвета);
	ДополнительныеПараметры.Вставить("ДанныеУзлаКонфиденциальнаяИнформацияАдрес", ДанныеУзлаКонфиденциальнаяИнформацияАдрес);
	ДополнительныеПараметры.Вставить("ВременныйКаталог", ВременныйКаталог);
	ДополнительныеПараметры.Вставить("ВыводитьСообщения", ВыводитьСообщения);
	ДополнительныеПараметры.Вставить("ПараметрыРегистрацияПолучитьДанныеССервера", ПараметрыРегистрацияПолучитьДанныеССервера);
	ДополнительныеПараметры.Вставить("ПараметрыФункции", ПараметрыФункции);
	ДополнительныеПараметры.Вставить("Результат", Результат);
	ДополнительныеПараметры.Вставить("УстанавливатьСертификат", УстанавливатьСертификат);
	ДополнительныеПараметры.Вставить("ПараметрыАвторизации", ПараметрыАвторизации);
	ДополнительныеПараметры.Вставить("ФайлИзКэша", ФайлИзКэша);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОбработатьОбновлениеВЦиклеПослеУстановкиСертификатовПроверка", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	Если УстанавливатьСертификат Тогда
		
		ПараметрыУстановки = ПараметрыРегистрацияУстановитьСертификаты();
		ЗаполнитьЗначенияСвойств(ПараметрыУстановки, ПараметрыФункции);
		ПараметрыУстановки.ВременныйКаталог = ВременныйКаталог;

		РегистрацияУстановитьСертификаты(ПараметрыУстановки, Оповещение);
		
	Иначе
		РезультатРегистрацияУстановитьСертификаты = Новый Структура("РезультатВыполнения, ПараметрыФункции", Истина, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатРегистрацияУстановитьСертификаты);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеВЦиклеПослеУстановкиСертификатовПроверка(РезультатРегистрацияУстановитьСертификаты, ДополнительныеПараметры) Экспорт
	
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	МестоХраненияКлючаНастроек = КонтекстМоделиХраненияКлюча(ПараметрыРегистрацияПолучитьДанныеССервера);
	
	РезультатУстановкиСертификатов = РезультатРегистрацияУстановитьСертификаты.РезультатВыполнения;
	ПараметрыРегистрацияУстановитьСертификаты = РезультатРегистрацияУстановитьСертификаты.ПараметрыФункции;
	
	// попытка установки сертификатов сервиса DSS, в случае неудачной попытки в локальном варианте
	Если НЕ РезультатУстановкиСертификатов И УстанавливатьСертификат
		И ЭтоЛокальнаяПодпись(МестоХраненияКлюча)
		И ЭтоОблачнаяПодпись(МестоХраненияКлючаНастроек) Тогда
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлючаНастроек, ПараметрыФункции);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлючаНастроек, ПараметрыРегистрацияУстановитьСертификаты);
		Оповещение = Новый ОписаниеОповещения("ОбработатьОбновлениеВЦиклеПослеУстановкиСертификатовЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		РегистрацияУстановитьСертификаты(ПараметрыРегистрацияУстановитьСертификаты, Оповещение);
	Иначе
		ОбработатьОбновлениеВЦиклеПослеУстановкиСертификатовЗавершение(РезультатРегистрацияУстановитьСертификаты, ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеВЦиклеПослеУстановкиСертификатовЗавершение(РезультатРегистрацияУстановитьСертификаты, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОбработатьОбновление(), которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ОтпечатокСертификата = ДополнительныеПараметры.ОтпечатокСертификата;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ДополнительныеПараметры.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	Результат = ДополнительныеПараметры.Результат;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	ПараметрыАвторизации = ДополнительныеПараметры.ПараметрыАвторизации;
	ФайлИзКэша = ДополнительныеПараметры.ФайлИзКэша;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	МестоХраненияКлючаНастроек = КонтекстМоделиХраненияКлюча(ПараметрыРегистрацияПолучитьДанныеССервера);
	
	РезультатУстановкиСертификатов = РезультатРегистрацияУстановитьСертификаты.РезультатВыполнения;
	ПараметрыРегистрацияУстановитьСертификаты = РезультатРегистрацияУстановитьСертификаты.ПараметрыФункции;
	
	Если ПараметрыРегистрацияУстановитьСертификаты <> Неопределено 
		И ПараметрыРегистрацияУстановитьСертификаты.Свойство("МенеджерКриптографии") Тогда
		ПараметрыФункции.Вставить("МенеджерКриптографии", ПараметрыРегистрацияУстановитьСертификаты.МенеджерКриптографии);	
	КонецЕсли;
	
	Если УстанавливатьСертификат Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыФункции, ПараметрыРегистрацияУстановитьСертификаты);
		ОтпечатокСертификата = ПараметрыРегистрацияУстановитьСертификаты.ОтпечатокСертификата;
	КонецЕсли;
	
	Если СтрНайти(ПараметрыРегистрацияПолучитьДанныеССервера.ДействиеОблачнойПодписи, "АктивироватьПароль") > 0 Тогда
		МодульСервисКриптографииDSSКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиентСервер");
		ПарольПоУмолчанию = МодульСервисКриптографииDSSКлиентСервер.ОбъектПароля("", 4);
		КриптографияЭДКОКлиентСервер.ПараметрыМестаХраненияКлюча(МестоХраненияКлюча, "Пароль", ПарольПоУмолчанию);
	КонецЕсли;	
	
	// Расшифровка данных, хранящихся в зашифрованной области рег. файла
	РезультатРасшифровки 		= Истина;
	АдресВоВременномХранилище 	= Неопределено;
	
	Если РезультатУстановкиСертификатов И Не ФайлИзКэша Тогда
		
		ВыполнятьРасшифровку = ПараметрыФункции.ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки;
		
		// Расшифровка
		ДополнительныеПараметры = Новый Структура("ОтпечатокСертификата, ДанныеУзлаКонфиденциальнаяИнформацияАдрес, АдресВоВременномХранилище, ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, ПараметрыРегистрацияПолучитьДанныеССервера, ПараметрыФункции, Результат, РезультатРасшифровки, РезультатУстановкиСертификатов, УстанавливатьСертификат, ФайлИзКэша", ОтпечатокСертификата, ДанныеУзлаКонфиденциальнаяИнформацияАдрес, АдресВоВременномХранилище, ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, ПараметрыРегистрацияПолучитьДанныеССервера, ПараметрыФункции, Результат, РезультатРасшифровки, РезультатУстановкиСертификатов, УстанавливатьСертификат, ФайлИзКэша);
		ДополнительныеПараметры.Вставить("ТрансляцияОшибокУстановкиСертификатов", РезультатРегистрацияУстановитьСертификаты);
		
		Оповещение = Новый ОписаниеОповещения("ОбработатьОбновлениеВЦиклеПослеРасшифрованияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Если ВыполнятьРасшифровку Тогда
			
			Если НЕ ЗначениеЗаполнено(ОтпечатокСертификата) Тогда
				
				Если ПараметрыФункции.Свойство("СсылкаНаЗаявление") 
					И ЗначениеЗаполнено(ПараметрыФункции.СсылкаНаЗаявление) Тогда
					
					ОтпечатокСертификата = ОтпечатокСертификатаИзЗаявления(ПараметрыФункции.СсылкаНаЗаявление);
					
				КонецЕсли;
					
			КонецЕсли;
			
			РегистрацияРасшифроватьДанные(
				МестоХраненияКлюча,
				МестоХраненияКлючаНастроек,
				ОтпечатокСертификата,
				ПараметрыФункции.ИдентификаторДокументооборота,
				ДанныеУзлаКонфиденциальнаяИнформацияАдрес,
				ВыводитьСообщения,
				Оповещение);
		Иначе
			РезультатВыполнения = Новый Структура("РезультатРасшифровки, АдресВоВременномХранилище, МестоХраненияКлюча", Ложь, АдресВоВременномХранилище, МестоХраненияКлюча);
			РезультатВыполнения.Вставить("ОшибкаПропущенаРасшифровка");
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		КонецЕсли;
		
	Иначе
		ОбработатьОбновлениеПослеКриптографииЗавершение(
			АдресВоВременномХранилище, 
			ВременныйКаталог, 
			ВыводитьСообщения, 
			ВыполняемоеОповещение, 
			ПараметрыРегистрацияПолучитьДанныеССервера, 
			ПараметрыФункции, 
			Результат, 
			РезультатРасшифровки, 
			РезультатУстановкиСертификатов, 
			УстанавливатьСертификат, 
			ФайлИзКэша,
			РезультатРегистрацияУстановитьСертификаты,
			Новый Структура
		);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ОтпечатокСертификатаИзЗаявления(СсылкаНаЗаявление)
	
	ОтпечатокСертификата = "";
	
	ИспользуетсяСуществующий = СсылкаНаЗаявление.СпособПолученияСертификата = Перечисления.СпособПолученияСертификата.ИспользоватьСуществующий;
	
	Если ИспользуетсяСуществующий Тогда

		Сертификат = ОбработкаЗаявленийАбонентаВызовСервера.СертификатИзЗаявления(СсылкаНаЗаявление);
		ОтпечатокСертификата = Сертификат.Отпечаток;
		
	КонецЕсли;
	
	Если ТипЗнч(ОтпечатокСертификата) = Тип("ДвоичныеДанные") Тогда
		ОтпечатокСертификата = СтрЗаменить(Строка(ОтпечатокСертификата), " ", "");
	КонецЕсли;
		
	
	Возврат ОтпечатокСертификата;
					
КонецФункции

&НаКлиенте
Процедура ОбработатьОбновлениеВЦиклеПослеРасшифрованияЗавершение(РезультатВыполнения, ДополнительныеПараметры) Экспорт
	
	ОтпечатокСертификата = ДополнительныеПараметры.ОтпечатокСертификата;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ДополнительныеПараметры.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	Результат = ДополнительныеПараметры.Результат;
	РезультатРасшифровки = ДополнительныеПараметры.РезультатРасшифровки;
	РезультатУстановкиСертификатов = ДополнительныеПараметры.РезультатУстановкиСертификатов;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	ФайлИзКэша = ДополнительныеПараметры.ФайлИзКэша;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	
	РезультатРасшифровки = РезультатВыполнения.РезультатРасшифровки;
	АдресВоВременномХранилище = РезультатВыполнения.АдресВоВременномХранилище;
	Если РезультатРасшифровки = Истина Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РезультатВыполнения);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ПараметрыФункции);
	КонецЕсли;
	
	ТрансляцияОшибокУстановкиСертификатов = ДополнительныеПараметры.ТрансляцияОшибокУстановкиСертификатов;
	ТрансляцияОшибокРасшифровки = РезультатВыполнения;
				
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) 
		И РезультатРасшифровки 
		И (ЗначениеЗаполнено(ПараметрыРегистрацияПолучитьДанныеССервера.ДействиеОблачнойПодписи) 
		ИЛИ УстанавливатьСертификат) Тогда
		ОбработатьОбновлениеАктивироватьОблачнуюПодпись(АдресВоВременномХранилище, ПараметрыРегистрацияПолучитьДанныеССервера.ДействиеОблачнойПодписи, ДополнительныеПараметры);  
		
	Иначе	
		ОбработатьОбновлениеПослеКриптографииЗавершение(
			АдресВоВременномХранилище, 
			ВременныйКаталог, 
			ВыводитьСообщения, 
			ВыполняемоеОповещение, 
			ПараметрыРегистрацияПолучитьДанныеССервера, 
			ПараметрыФункции, 
			Результат, 
			РезультатРасшифровки, 
			РезультатУстановкиСертификатов, 
			УстанавливатьСертификат, 
			ФайлИзКэша,
			ТрансляцияОшибокУстановкиСертификатов,
			ТрансляцияОшибокРасшифровки
		);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеПослеКриптографииЗавершение(
		АдресВоВременномХранилище, 
		ВременныйКаталог, 
		ВыводитьСообщения, 
		ВыполняемоеОповещение,  
		ПараметрыРегистрацияПолучитьДанныеССервера, 
		ПараметрыФункции, 
		Результат, 
		РезультатРасшифровки, 
		РезультатУстановкиСертификатов, 
		УстанавливатьСертификат, 
		ФайлИзКэша,
		ТрансляцияОшибокУстановкиСертификатов = Неопределено,
		ТрансляцияОшибокРасшифровки = Неопределено)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// Обновление информации в справочниках
	Если РезультатУстановкиСертификатов И РезультатРасшифровки Тогда
		
		ПараметрыОбновления = ПараметрыРегистрацияОбновитьСправочники();
		ЗаполнитьЗначенияСвойств(ПараметрыОбновления, ПараметрыФункции);
		
		ПараметрыОбновления.ВременныйФайлОбновленийАдрес 	= ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес;
		ПараметрыОбновления.АдресВоВременномХранилище 		= АдресВоВременномХранилище;
		ПараметрыОбновления.ФайлИзКэша 						= ФайлИзКэша;
		
		РезультатОбновленияСправочников = РегистрацияОбновитьСправочники(ПараметрыОбновления);
		ЗаполнитьЗначенияСвойств(ПараметрыФункции, ПараметрыОбновления);
		
		ЭтоНоваяУчетнаяЗапись     = ПараметрыОбновления.ЭтоНоваяУчетнаяЗапись;
		СтруктураАвтонастройкиФСС = ПараметрыОбновления.СтруктураАвтонастройкиФСС;
		СтруктураАвтонастройкиРПН = ПараметрыОбновления.СтруктураАвтонастройкиРПН;
		СтруктураАвтонастройкиФТС = ПараметрыОбновления.СтруктураАвтонастройкиФТС;
		
	КонецЕсли;
	
	// Сохранение дополнительных сведений по учетной записи в регистр
	ПараметрыСертификата = Неопределено;
	Если (РезультатУстановкиСертификатов ИЛИ НЕ УстанавливатьСертификат) Тогда
		
		ПараметрыУчетнойЗаписи = ?(ПараметрыФункции.УчетнаяЗапись = Неопределено, НоваяУчетнаяЗапись, ПараметрыФункции.УчетнаяЗапись);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ВременныйКаталог", ВременныйКаталог);
		ДополнительныеПараметры.Вставить("ВыводитьСообщения", ВыводитьСообщения);
		ДополнительныеПараметры.Вставить("ПараметрыРегистрацияПолучитьДанныеССервера", ПараметрыРегистрацияПолучитьДанныеССервера);
		ДополнительныеПараметры.Вставить("ПараметрыФункции", ПараметрыФункции);
		ДополнительныеПараметры.Вставить("Результат", Результат);
		ДополнительныеПараметры.Вставить("РезультатРасшифровки", РезультатРасшифровки);
		ДополнительныеПараметры.Вставить("РезультатУстановкиСертификатов", РезультатУстановкиСертификатов);
		ДополнительныеПараметры.Вставить("УстанавливатьСертификат", УстанавливатьСертификат);
		ДополнительныеПараметры.Вставить("ПараметрыУчетнойЗаписи", ПараметрыУчетнойЗаписи);
		ДополнительныеПараметры.Вставить("РезультатОбновленияСправочников", РезультатОбновленияСправочников);
		ДополнительныеПараметры.Вставить("СтруктураАвтонастройкиФСС", СтруктураАвтонастройкиФСС);
		ДополнительныеПараметры.Вставить("СтруктураАвтонастройкиРПН", СтруктураАвтонастройкиРПН);
		ДополнительныеПараметры.Вставить("СтруктураАвтонастройкиФТС", СтруктураАвтонастройкиФТС);
		ДополнительныеПараметры.Вставить("ЭтоНоваяУчетнаяЗапись", ЭтоНоваяУчетнаяЗапись);
		
		Оповещение = Новый ОписаниеОповещения("ОбработатьОбновлениеПослеПоискаСертификатаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		Попытка
			
			Сертификат = МультирежимВызовСервера.СертификатПользователя(ПараметрыУчетнойЗаписи);
			Если Сертификат.Найден Тогда
				
				Если ЭтоПодписьСервиса(Сертификат.МестоХраненияКлюча) Тогда
					ПараметрыСертификата = ПолучитьСвойстваСертификатаСубъектаНаСервере(Сертификат.Отпечаток);
					ЗаполнитьМестоХраненияКлюча(Сертификат.МестоХраненияКлюча, ПараметрыСертификата);
				ИначеЕсли ЭтоОблачнаяПодпись(Сертификат.МестоХраненияКлюча) Тогда
					ПараметрыСертификата = ПолучитьСвойстваОблачногоСертификата(Сертификат.Отпечаток, "MY");
					ЗаполнитьМестоХраненияКлюча(Сертификат.МестоХраненияКлюча, ПараметрыСертификата);
				Иначе
					
					МенеджерКриптографии = ?(
						ПараметрыФункции.Свойство("МенеджерКриптографии"), 
						ПараметрыФункции.МенеджерКриптографии, 
						Неопределено);
						
					КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, Сертификат.Отпечаток, "MY", , Ложь, МенеджерКриптографии);
					Возврат;
					
				КонецЕсли;
			КонецЕсли;
			
		Исключение
			ПараметрыСертификата = Неопределено;
		КонецПопытки;
		
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден, СвойстваСертификата", Истина, Неопределено, ПараметрыСертификата <> Неопределено, ПараметрыСертификата);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ВременныйКаталог);
		
		РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Результат);
		Если РезультатУстановкиСертификатов = Ложь И ТипЗнч(ТрансляцияОшибокУстановкиСертификатов) = Тип("Структура") Тогда
			Для Каждого Свойство Из ТрансляцияОшибокУстановкиСертификатов Цикл
				Если Лев(Свойство.Ключ, 6) = "Ошибка" Тогда
					РезультатОбновления.Вставить(Свойство.Ключ, Свойство.Значение);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		Если РезультатРасшифровки = Ложь И ТипЗнч(ТрансляцияОшибокРасшифровки) = Тип("Структура") Тогда
			Для Каждого Свойство Из ТрансляцияОшибокРасшифровки Цикл
				Если Лев(Свойство.Ключ, 6) = "Ошибка" Тогда
					РезультатОбновления.Вставить(Свойство.Ключ, Свойство.Значение);
				КонецЕсли;
			КонецЦикла;
			Если ТрансляцияОшибокРасшифровки.Количество() = 0 Тогда
				РезультатОбновления.Вставить("ОшибкаПропущенаРасшифровка");
			КонецЕсли;
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеПослеПоискаСертификатаЗавершение(РезультатВыполнения, ДополнительныеПараметры) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	Результат = ДополнительныеПараметры.Результат;
	РезультатРасшифровки = ДополнительныеПараметры.РезультатРасшифровки;
	РезультатУстановкиСертификатов = ДополнительныеПараметры.РезультатУстановкиСертификатов;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	ПараметрыУчетнойЗаписи = ДополнительныеПараметры.ПараметрыУчетнойЗаписи;
	РезультатОбновленияСправочников = ДополнительныеПараметры.РезультатОбновленияСправочников;
	СтруктураАвтонастройкиФСС = ДополнительныеПараметры.СтруктураАвтонастройкиФСС;
	СтруктураАвтонастройкиРПН = ДополнительныеПараметры.СтруктураАвтонастройкиРПН;
	СтруктураАвтонастройкиФТС = ДополнительныеПараметры.СтруктураАвтонастройкиФТС;
	ЭтоНоваяУчетнаяЗапись = ДополнительныеПараметры.ЭтоНоваяУчетнаяЗапись;
	
	ПараметрыСертификата = ?(РезультатВыполнения.Выполнено И РезультатВыполнения.СертификатНайден, РезультатВыполнения.СвойстваСертификата, Неопределено);
	
	Если (РезультатУстановкиСертификатов ИЛИ НЕ УстанавливатьСертификат) Тогда
		
		ПараметрыСохранения 								= ДокументооборотСКОКлиентСервер.ПараметрыСохранитьДополнительныеРеквизитыУчетнойЗаписи();
		ПараметрыСохранения.ВременныйФайлОбновленийАдрес 	= ПараметрыРегистрацияПолучитьДанныеССервера.ВременныйФайлОбновленийАдрес;
		ПараметрыСохранения.УчетнаяЗаписьОрганизации 		= ПараметрыУчетнойЗаписи;
		ПараметрыСохранения.ПараметрыСертификата 			= ПараметрыСертификата;
		
		ЗаполнитьЗначенияСвойств(ПараметрыСохранения, ПараметрыФункции);
		
		СохранитьДополнительныеРеквизитыУчетнойЗаписи(ПараметрыСохранения);
		ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности = ПараметрыСохранения.ТекстОшибокДляМастераПодключенияК1СОтчетности;
	КонецЕсли;
	
	Если (РезультатУстановкиСертификатов ИЛИ НЕ УстанавливатьСертификат) И РезультатРасшифровки И РезультатОбновленияСправочников Тогда
		
		// производим настройку параметров ФСС
		Если СтруктураАвтонастройкиФСС <> Неопределено И ПараметрыФункции.Организация <> Неопределено Тогда
			
			ФлагАвтозаполнения = СтруктураАвтонастройкиФСС.ФлагАвтозаполнения;
			Если СтруктураАвтонастройкиФСС.ФлагСообщения И ВыводитьСообщения Тогда
				Режим = РежимДиалогаВопрос.ДаНет;
				Текст = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Произвести автоматическую настройку параметров отправки в ФСС для организации %1?'"), ПараметрыФункции.Организация);
				ДополнительныеПараметры = Новый Структура("ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, НоваяУчетнаяЗапись, ПараметрыФункции, СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, СтруктураАвтонастройкиФСС, ЭтоНоваяУчетнаяЗапись", ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, НоваяУчетнаяЗапись, ПараметрыФункции, СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, СтруктураАвтонастройкиФСС, ЭтоНоваяУчетнаяЗапись);
				ОписаниеОповещения = Новый ОписаниеОповещения("ОбработатьОбновлениеНастройкаФССЗавершение", ЭтотОбъект, ДополнительныеПараметры);
				ПоказатьВопрос(ОписаниеОповещения, Текст, Режим, 0, КодВозвратаДиалога.Нет, "Предупреждение");
				
			Иначе
				
				Если ФлагАвтозаполнения Тогда
					ОбновитьРегистрыСведенийНастройкиОбменаФСС(
						ПараметрыФункции.Организация,
						СтруктураАвтонастройкиФСС.ОтпечатокСертификата,
						СтруктураАвтонастройкиФСС.СертификатФССОтпечаток,
						СтруктураАвтонастройкиФСС.СертификатФССЭЛНОтпечаток,
						ВыводитьСообщения);
				КонецЕсли;
				
				ОбработатьОбновлениеНастройкаРПН(СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, ПараметрыФункции, ВыводитьСообщения, ЭтоНоваяУчетнаяЗапись, НоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение);
				
			КонецЕсли;
			
		Иначе
			
			ОбработатьОбновлениеНастройкаРПН(СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, ПараметрыФункции, ВыводитьСообщения, ЭтоНоваяУчетнаяЗапись, НоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение);
			
		КонецЕсли;
		
	Иначе
		
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ВременныйКаталог);
		
		РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Результат);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеНастройкаФССЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ОбъектНоваяУчетнаяЗапись = ДополнительныеПараметры.НоваяУчетнаяЗапись;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	СтруктураАвтонастройкиРПН = ДополнительныеПараметры.СтруктураАвтонастройкиРПН;
	СтруктураАвтонастройкиФТС = ДополнительныеПараметры.СтруктураАвтонастройкиФТС;
	СтруктураАвтонастройкиФСС = ДополнительныеПараметры.СтруктураАвтонастройкиФСС;
	ЭтоНоваяУчетнаяЗапись = ДополнительныеПараметры.ЭтоНоваяУчетнаяЗапись;
	
	ФлагАвтозаполнения = СтруктураАвтонастройкиФСС.ФлагАвтозаполнения;
	Если Ответ = КодВозвратаДиалога.Нет Тогда
		ФлагАвтозаполнения = Ложь;
	КонецЕсли;
	
	Если ФлагАвтозаполнения Тогда
		ОбновитьРегистрыСведенийНастройкиОбменаФСС(
			ПараметрыФункции.Организация,
			СтруктураАвтонастройкиФСС.ОтпечатокСертификата,
			СтруктураАвтонастройкиФСС.СертификатФССОтпечаток,
			СтруктураАвтонастройкиФСС.СертификатФССЭЛНОтпечаток,
			ВыводитьСообщения);
	КонецЕсли;
	
	ОбработатьОбновлениеНастройкаРПН(СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, ПараметрыФункции, ВыводитьСообщения, ЭтоНоваяУчетнаяЗапись, ОбъектНоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеНастройкаРПН(
		СтруктураАвтонастройкиРПН, 
		СтруктураАвтонастройкиФТС, 
		ПараметрыФункции, 
		ВыводитьСообщения, 
		ЭтоНоваяУчетнаяЗапись, 
		НоваяУчетнаяЗапись, 
		ВременныйКаталог, 
		ВыполняемоеОповещение)
	
	// производим настройку параметров РПН
	Если СтруктураАвтонастройкиРПН <> Неопределено И ПараметрыФункции.Организация <> Неопределено Тогда
		
		ФлагАвтозаполнения = СтруктураАвтонастройкиРПН.ФлагАвтозаполнения;
		Если СтруктураАвтонастройкиРПН.ФлагСообщения И ВыводитьСообщения Тогда
			
			Режим = РежимДиалогаВопрос.ДаНет;
			Текст = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Произвести автоматическую настройку параметров отправки в Росприроднадзора для организации %1?'"), ПараметрыФункции.Организация);
			
			ДополнительныеПараметры = Новый Структура("ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, НоваяУчетнаяЗапись, ПараметрыФункции, СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, ЭтоНоваяУчетнаяЗапись", ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, НоваяУчетнаяЗапись, ПараметрыФункции, СтруктураАвтонастройкиРПН, СтруктураАвтонастройкиФТС, ЭтоНоваяУчетнаяЗапись);
			ОписаниеОповещения = Новый ОписаниеОповещения("ОбработатьОбновлениеНастройкаРПНЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьВопрос(ОписаниеОповещения, Текст, Режим, 0, КодВозвратаДиалога.Нет, "Предупреждение");
			
		Иначе
			
			Если ФлагАвтозаполнения Тогда
				ОбновитьРегистрыСведенийНастройкиОбменаРПН(
					ПараметрыФункции.Организация, 
					СтруктураАвтонастройкиРПН.ОтпечатокСертификата,
					СтруктураАвтонастройкиРПН.СертификатРПНОтпечаток, 
					ВыводитьСообщения, 
					СтруктураАвтонастройкиРПН.EMail, 
					СтруктураАвтонастройкиРПН.Пароль);
			КонецЕсли;
			
			ОбработатьОбновлениеНастройкаФТС(
				СтруктураАвтонастройкиФТС, 
				ПараметрыФункции, 
				ВыводитьСообщения, 
				ЭтоНоваяУчетнаяЗапись, 
				НоваяУчетнаяЗапись, 
				ВременныйКаталог, 
				ВыполняемоеОповещение);
			
		КонецЕсли;
		
	Иначе
		
		ОбработатьОбновлениеНастройкаФТС(
			СтруктураАвтонастройкиФТС, 
			ПараметрыФункции, 
			ВыводитьСообщения, 
			ЭтоНоваяУчетнаяЗапись, 
			НоваяУчетнаяЗапись, 
			ВременныйКаталог, 
			ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеНастройкаРПНЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ОбъектНоваяУчетнаяЗапись = ДополнительныеПараметры.НоваяУчетнаяЗапись;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	СтруктураАвтонастройкиРПН = ДополнительныеПараметры.СтруктураАвтонастройкиРПН;
	СтруктураАвтонастройкиФТС = ДополнительныеПараметры.СтруктураАвтонастройкиФТС;
	ЭтоНоваяУчетнаяЗапись = ДополнительныеПараметры.ЭтоНоваяУчетнаяЗапись;
	
	ФлагАвтозаполнения = СтруктураАвтонастройкиРПН.ФлагАвтозаполнения;
	Если Ответ = КодВозвратаДиалога.Нет Тогда
		ФлагАвтозаполнения = Ложь;
	КонецЕсли;
	
	Если ФлагАвтозаполнения Тогда
		ОбновитьРегистрыСведенийНастройкиОбменаРПН(
			ПараметрыФункции.Организация, СтруктураАвтонастройкиРПН.ОтпечатокСертификата,
			СтруктураАвтонастройкиРПН.СертификатРПНОтпечаток, ВыводитьСообщения, СтруктураАвтонастройкиРПН.EMail, СтруктураАвтонастройкиРПН.Пароль);
	КонецЕсли;
	
	ОбработатьОбновлениеНастройкаФТС(
		СтруктураАвтонастройкиФТС, 
		ПараметрыФункции, 
		ВыводитьСообщения, 
		ЭтоНоваяУчетнаяЗапись, 
		ОбъектНоваяУчетнаяЗапись, 
		ВременныйКаталог, 
		ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеНастройкаФТС(
		СтруктураАвтонастройкиФТС, 
		ПараметрыФункции, 
		ВыводитьСообщения, 
		ЭтоНоваяУчетнаяЗапись, 
		НоваяУчетнаяЗапись, 
		ВременныйКаталог, 
		ВыполняемоеОповещение)
	
	// производим настройку параметров ФТС
	Если СтруктураАвтонастройкиФТС <> Неопределено И ПараметрыФункции.Организация <> Неопределено Тогда
		
		ФлагАвтозаполнения = СтруктураАвтонастройкиФТС.ФлагАвтозаполнения;
		Если СтруктураАвтонастройкиФТС.ФлагСообщения И ВыводитьСообщения Тогда
			
			Режим = РежимДиалогаВопрос.ДаНет;
			Текст = СтрШаблон(НСтр("ru = 'Произвести автоматическую настройку параметров отправки в ФТС для организации %1?'"), ПараметрыФункции.Организация);
			
			ДополнительныеПараметры = Новый Структура("ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, НоваяУчетнаяЗапись, ПараметрыФункции, СтруктураАвтонастройкиФТС, ЭтоНоваяУчетнаяЗапись", ВременныйКаталог, ВыводитьСообщения, ВыполняемоеОповещение, НоваяУчетнаяЗапись, ПараметрыФункции, СтруктураАвтонастройкиФТС, ЭтоНоваяУчетнаяЗапись);
			ОписаниеОповещения = Новый ОписаниеОповещения("ОбработатьОбновлениеНастройкаФТСЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьВопрос(ОписаниеОповещения, Текст, Режим, 0, КодВозвратаДиалога.Нет, "Предупреждение");
			
		Иначе
			
			Если ФлагАвтозаполнения Тогда
				ОбновитьРегистрыСведенийНастройкиОбменаФТС(
					ПараметрыФункции.Организация,
					СтруктураАвтонастройкиФТС.ОтпечатокСертификата,
					ВыводитьСообщения,
					СтруктураАвтонастройкиФТС.Логин,
					СтруктураАвтонастройкиФТС.Пароль,
					СтруктураАвтонастройкиФТС.МЧД);
			КонецЕсли;
			
			ОбработатьОбновлениеВернутьРезультат(ЭтоНоваяУчетнаяЗапись, ПараметрыФункции, НоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение);
			
		КонецЕсли;
		
	Иначе
		
		ОбработатьОбновлениеВернутьРезультат(ЭтоНоваяУчетнаяЗапись, ПараметрыФункции, НоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеНастройкаФТСЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ОбъектНоваяУчетнаяЗапись = ДополнительныеПараметры.НоваяУчетнаяЗапись;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	СтруктураАвтонастройкиФТС = ДополнительныеПараметры.СтруктураАвтонастройкиФТС;
	ЭтоНоваяУчетнаяЗапись = ДополнительныеПараметры.ЭтоНоваяУчетнаяЗапись;
	
	ФлагАвтозаполнения = СтруктураАвтонастройкиФТС.ФлагАвтозаполнения;
	Если Ответ = КодВозвратаДиалога.Нет Тогда
		ФлагАвтозаполнения = Ложь;
	КонецЕсли;
	
	Если ФлагАвтозаполнения Тогда
		ОбновитьРегистрыСведенийНастройкиОбменаФТС(
			ПараметрыФункции.Организация,
			СтруктураАвтонастройкиФТС.ОтпечатокСертификата,
			ВыводитьСообщения,
			СтруктураАвтонастройкиФТС.Логин,
			СтруктураАвтонастройкиФТС.Пароль,
			СтруктураАвтонастройкиФТС.МЧД);
	КонецЕсли;
	
	ОбработатьОбновлениеВернутьРезультат(ЭтоНоваяУчетнаяЗапись, ПараметрыФункции, ОбъектНоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеВернутьРезультат(ЭтоНоваяУчетнаяЗапись, ПараметрыФункции, НоваяУчетнаяЗапись, ВременныйКаталог, ВыполняемоеОповещение)
	
	// при необходимости, показываем форму установки прав доступа для учетной записи
	Если ЭтоНоваяУчетнаяЗапись 
		И (ПараметрыФункции.ПутьКонтейнерЗакрытогоКлюча <> Неопределено ИЛИ ПараметрыФункции.ВызовИзМастераАвтонастройки) 
		И НЕ ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности Тогда
		
		ПараметрыФормы = Новый Структура("НоваяУчетнаяЗапись, Организация");
		ПараметрыФормы.НоваяУчетнаяЗапись 	= НоваяУчетнаяЗапись;
		ПараметрыФормы.Организация 			= ПараметрыФункции.Организация;
		
		ДополнительныеПараметры = Новый Структура(
			"ВременныйКаталог, ПараметрыФункции, ВыполняемоеОповещение", 
			ВременныйКаталог, 
			ПараметрыФункции, 
			ВыполняемоеОповещение);
			
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбработатьОбновлениеВернутьРезультатЗавершение", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ФормаУстановкиПравДоступа", 
			ПараметрыФормы,
			,
			,
			,
			,
			ОписаниеОповещения,
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

	Иначе
		
		Результат = Истина;
		
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ВременныйКаталог);
		
		РезультатОбновления = Новый Структура(
			"ПараметрыФункции, РезультатОбновления", 
			ПараметрыФункции, 
			Результат);
			
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);

	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеВернутьРезультатЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ВременныйКаталог);
	
	РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Истина);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
	
КонецПроцедуры

&НаКлиенте
Функция РегистрацияПолучитьДанныеССервераКлиент(
		ПараметрыФункции,
		ПовторятьСоединение = Ложь,
		ОбновленыПодписиСертификатовДокументооборота = Ложь) Экспорт
	
	Возврат РегистрацияПолучитьДанныеССервера(
		ПараметрыФункции,
		ПовторятьСоединение,
		ОбновленыПодписиСертификатовДокументооборота);
	
КонецФункции

&НаСервере
Функция РегистрацияПолучитьДанныеССервера(
		ПараметрыФункции,
		ПовторятьСоединение = Ложь,
		ОбновленыПодписиСертификатовДокументооборота = Ложь)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РегистрацияПолучитьДанныеССервера(
		ПараметрыФункции,
		ПовторятьСоединение,
		Сертификаты,
		ОбновленыПодписиСертификатовДокументооборота);
	
КонецФункции

&НаСервере
Функция РегистрацияОбновитьСправочники(ПараметрыФункции)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	ПараметрыФункции.НоваяУчетнаяЗапись = НоваяУчетнаяЗапись;
	
	Результат = КонтекстЭДОСервер.РегистрацияОбновитьСправочники(ПараметрыФункции);
		
	Если Результат Тогда
		Если ЗначениеЗаполнено(ПараметрыФункции.НоваяУчетнаяЗапись) Тогда
			НоваяУчетнаяЗапись = ПараметрыФункции.НоваяУчетнаяЗапись;
		КонецЕсли;
		
		ДокументDOM = РегФайлКА.ДокументDOMПоАдресу(ПараметрыФункции.ВременныйФайлОбновленийАдрес);
		КонтекстЭДОСервер.ОбновитьВсеСертификатыВУчетнойЗаписиДО(ДокументDOM, Сертификаты, ПараметрыФункции);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура РегистрацияУстановитьСертификаты(ПараметрыФункции, ВыполняемоеОповещение)
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		
		РезультатВыполнения = РегистрацияУстановитьСертификатыНаСервере(ПараметрыФункции.ОтпечатокСертификата);
		Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", РезультатВыполнения, ПараметрыФункции);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		
		УстановитьСертификатыПолучателей(ПараметрыФункции, ВыполняемоеОповещение);
		
	Иначе
		ПараметрыЦикла = Новый Структура;
		ПараметрыЦикла.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ПараметрыЦикла.Вставить("ПараметрыФункции", ПараметрыФункции);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("УстановитьСертификатыЛокально", ЭтотОбъект, ПараметрыЦикла);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияУстановитьСертификатыПослеПоискаСертификатовЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	
	Если НЕ Результат.Выполнено Тогда
		Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", Ложь, ПараметрыФункции);
		Результат.Вставить("ОшибкаПоискаСертификатовПередУстановкой", ИзвлечьОписаниеПричиныОшибки(Результат));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	МассивСертификатов = Результат.Сертификаты;
	
	ВременныйКаталог = ПараметрыФункции.ВременныйКаталог;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, МассивСертификатов, НомерСертификата", ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, МассивСертификатов, 0);
	Оповещение = Новый ОписаниеОповещения("РегистрацияУстановитьСертификатыЗаписьСертификатовВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	РезультатЗаписиСертификатаВФайл = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, Неопределено);
	ВыполнитьОбработкуОповещения(Оповещение, РезультатЗаписиСертификатаВФайл);
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияУстановитьСертификатыЗаписьСертификатовВЦиклеЗавершение(РезультатЗаписиСертификатаВФайл, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	МассивСертификатов = ДополнительныеПараметры.МассивСертификатов;
	НомерСертификата = ДополнительныеПараметры.НомерСертификата;
	
	Если НЕ РезультатЗаписиСертификатаВФайл.Выполнено Тогда
		Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", Ложь, ПараметрыФункции);
		Результат.Вставить("ОшибкаЗаписиСертификатаВФайл", ИзвлечьОписаниеПричиныОшибки(РезультатЗаписиСертификатаВФайл));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Пока НомерСертификата < Сертификаты.Количество() Цикл
		
		Сертификат = Сертификаты[НомерСертификата];
		НомерСертификата = НомерСертификата + 1;
		
		Если Сертификат.ЭтоЧужойСертификат Тогда
			Продолжить;
		КонецЕсли;
		
		СертификатНайден = Ложь;
		Для каждого НайденныйСертификат Из МассивСертификатов Цикл
			Если нрег(СокрЛП(НайденныйСертификат.Отпечаток)) = нрег(СокрЛП(Сертификат.Отпечаток)) Тогда
				СертификатНайден = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
			
		Если НЕ СертификатНайден Тогда
			Сертификат.ТребуетсяУстановить = Истина;
			ПутьКФайлуСерта = ВременныйКаталог + Сертификат.Отпечаток + ".cer";
			ТелоСертификата = Сертификат.Тело;
			Если СтрНайти(ТелоСертификата, "BEGIN CERTIFICATE") = 0 Тогда
				ТелоСертификата = "-----BEGIN CERTIFICATE-----" + символы.ПС
								+ ТелоСертификата + символы.ПС
								+ "-----END CERTIFICATE-----";
			КонецЕсли;
			
			ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, МассивСертификатов, НомерСертификата", ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, МассивСертификатов, НомерСертификата);
			Оповещение = Новый ОписаниеОповещения("РегистрацияУстановитьСертификатыЗаписьСертификатовВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ОперацииСФайламиЭДКОКлиент.ТекстВФайл(Оповещение, ТелоСертификата, ПутьКФайлуСерта);
			Возврат;
		КонецЕсли;
		
	КонецЦикла;
	
	ВыводитьСообщения = ПараметрыФункции.РежимАвтонастройки И НЕ ПараметрыФункции.ТихийРежим;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, ВыводитьСообщения, НомерСертификата", ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, ВыводитьСообщения, 0);
	Оповещение = Новый ОписаниеОповещения("РегистрацияУстановитьСертификатыУстановкаСертификатовВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	РезультатУстановкиСертификата = Новый Структура("Выполнено, МенеджерКриптографии", Истина, Неопределено);
	ВыполнитьОбработкуОповещения(Оповещение, РезультатУстановкиСертификата);
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияУстановитьСертификатыУстановкаСертификатовВЦиклеЗавершение(РезультатУстановкиСертификата, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыФункции      = ДополнительныеПараметры.ПараметрыФункции;
	ВременныйКаталог      = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения     = ДополнительныеПараметры.ВыводитьСообщения;
	НомерСертификата      = ДополнительныеПараметры.НомерСертификата;
	
	УстановкаСертификатСАвтоопределениемКриптопровайдера =
		?(ДополнительныеПараметры.Свойство("УстановкаСертификатСАвтоопределениемКриптопровайдера"),
		ДополнительныеПараметры.УстановкаСертификатСАвтоопределениемКриптопровайдера, Ложь);
	ДополнительныеПараметры.Удалить("УстановкаСертификатСАвтоопределениемКриптопровайдера");
	
	Если НЕ РезультатУстановкиСертификата.Выполнено Тогда
		
		Текст = НСтр("ru = 'Не удалось установить личный сертификат (%1)!'");
		Текст = СтрШаблон(Текст, ДополнительныеПараметры.Сертификат.Комментарий);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
			Текст,
			ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности,
			ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности,
			ВыводитьСообщения);
		
		Если УстановкаСертификатСАвтоопределениемКриптопровайдера Тогда
			
			Результат = Новый Структура();
			Результат.Вставить("РезультатВыполнения",               Ложь);
			Результат.Вставить("ПараметрыФункции",                  ПараметрыФункции);
			Результат.Вставить("ОшибкаУстановкиЛичногоСертификата", ИзвлечьОписаниеПричиныОшибки(РезультатУстановкиСертификата));
			
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			
		Иначе
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"УстановитьСертификатВКонтейнерИХранилище_ПослеСозданияМенеджераКриптографии",
				ЭтотОбъект,
				ДополнительныеПараметры);
			РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии", Ложь, Неопределено);
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВыполнения);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Пока НомерСертификата < Сертификаты.Количество() Цикл
		
		Сертификат       = Сертификаты[НомерСертификата];
		НомерСертификата = НомерСертификата + 1;
		
		Если Сертификат.ТребуетсяУстановить Тогда
		
			Если Сертификат.Хранилище = "MY" Тогда
				Если ПараметрыФункции.ПутьКонтейнерЗакрытогоКлюча <> Неопределено И Сертификат.ТипПолучателя = "" Тогда
					
					Если ЗначениеЗаполнено(ПараметрыФункции.ПутьКонтейнерЗакрытогоКлюча) Тогда
						
						ДополнительныеПараметры = Новый Структура();
						ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
						ДополнительныеПараметры.Вставить("ПараметрыФункции",      ПараметрыФункции);
						ДополнительныеПараметры.Вставить("ВременныйКаталог",      ВременныйКаталог);
						ДополнительныеПараметры.Вставить("ВыводитьСообщения",     ВыводитьСообщения);
						ДополнительныеПараметры.Вставить("НомерСертификата",      НомерСертификата);
						ДополнительныеПараметры.Вставить("Сертификат",            Сертификат);

						УстановитьСертификатВКонтейнерИХранилище(ДополнительныеПараметры);
							
						Возврат;
						
					ИначеЕсли ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило(
						"Ош.настр.пустой контейнер") Тогда
						
						ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
							НСтр("ru = 'Не удалось определить путь к контейнеру закрытого ключа!'"),
							ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности,
							ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности,
							ВыводитьСообщения);
							
						Результат = Новый Структура();
						Результат.Вставить("РезультатВыполнения", Ложь);
						Результат.Вставить("ПараметрыФункции",   ПараметрыФункции);
						Результат.Вставить("ОшибкаОпределенияПутиКонтейнера");
						
						ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
						Возврат;
						
					КонецЕсли;
					
				КонецЕсли;
				
			Иначе
				
				ДополнительныеПараметры = Новый Структура();
				ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
				ДополнительныеПараметры.Вставить("ПараметрыФункции",      ПараметрыФункции);
				ДополнительныеПараметры.Вставить("ВременныйКаталог",      ВременныйКаталог);
				ДополнительныеПараметры.Вставить("ВыводитьСообщения",     ВыводитьСообщения);
				ДополнительныеПараметры.Вставить("НомерСертификата",      НомерСертификата);
				ДополнительныеПараметры.Вставить("Сертификат",            Сертификат);
				ДополнительныеПараметры.Вставить("СчетчикСертификата",    1);

				Оповещение = Новый ОписаниеОповещения(
					"РегистрацияУстановитьСертификатыИмпортСертификатовВЦиклеЗавершение", 
					ЭтотОбъект, 
					ДополнительныеПараметры);
					
				РезультатИмпортаСертификата = Новый Структура("Выполнено, МенеджерКриптографии", Ложь, РезультатУстановкиСертификата.МенеджерКриптографии);
				
				ВыполнитьОбработкуОповещения(Оповещение, РезультатИмпортаСертификата);
				Возврат;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	ПараметрыФункции.Вставить("МенеджерКриптографии", РезультатУстановкиСертификата.МенеджерКриптографии);
	Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", Истина, ПараметрыФункции);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияУстановитьСертификатыИмпортСертификатовВЦиклеЗавершение(РезультатИмпортаСертификата, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	НомерСертификата = ДополнительныеПараметры.НомерСертификата;
	
	Если НЕ ДополнительныеПараметры.Свойство("Сертификат") Тогда
		
		ТекстОшибки = НСтр("ru = 'При автонастройке учетной записи произошла ошибка. Попробуйте повторить операцию. Подробнее в журнале регистраций'");
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(ТекстОшибки,
			ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности,
			ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности,
			ВыводитьСообщения);
					
		Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", Ложь, ПараметрыФункции);
		Результат.Вставить("ОшибкаУстановкиСертификатаВХранилище", ТекстОшибки);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Сертификат = ДополнительныеПараметры.Сертификат;
	СчетчикСертификата = ДополнительныеПараметры.СчетчикСертификата;
	
	РезультатВыполнения = РезультатИмпортаСертификата.Выполнено;
	Пока СчетчикСертификата < 10 Цикл
		Если РезультатВыполнения Тогда
			Сертификат.Отпечаток = РезультатИмпортаСертификата.СвойстваСертификата.Отпечаток;
			Прервать;
		КонецЕсли;
		
		СчетчикСертификата = СчетчикСертификата + 1;
		
		ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, ВыводитьСообщения, НомерСертификата, Сертификат, СчетчикСертификата", ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, ВыводитьСообщения, НомерСертификата, Сертификат, СчетчикСертификата);
		Оповещение = Новый ОписаниеОповещения("РегистрацияУстановитьСертификатыИмпортСертификатовВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.ИмпортироватьСертификат(
			Оповещение,
			ВременныйКаталог + Сертификат.Отпечаток + ".cer",
			Сертификат.Хранилище,
			Ложь,
			?(РезультатВыполнения, РезультатИмпортаСертификата.МенеджерКриптографии, Неопределено));
		Возврат;
	КонецЦикла;
	
	Если ВыводитьСообщения Тогда
		Если НЕ РезультатВыполнения Тогда
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось установить сертификат с отпечатком %1 в хранилище %2'"), Сертификат.Отпечаток, Сертификат.Хранилище),
					ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности,
					ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности,
					ВыводитьСообщения);
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ РезультатВыполнения Тогда
		Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", Ложь, ПараметрыФункции);
		Результат.Вставить("ОшибкаУстановкиСертификатаВХранилище", ИзвлечьОписаниеПричиныОшибки(РезультатИмпортаСертификата));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, ВыводитьСообщения, НомерСертификата", ВыполняемоеОповещение, ПараметрыФункции, ВременныйКаталог, ВыводитьСообщения, НомерСертификата);
	Оповещение = Новый ОписаниеОповещения("РегистрацияУстановитьСертификатыУстановкаСертификатовВЦиклеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	РезультатУстановкиСертификата = Новый Структура("Выполнено, МенеджерКриптографии", Истина, РезультатИмпортаСертификата.МенеджерКриптографии);
	ВыполнитьОбработкуОповещения(Оповещение, РезультатУстановкиСертификата);
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияРасшифроватьДанные(
		МестоХраненияКлюча,
		МестоХраненияКлючаНастроек,
		ОтпечатокСертификата,
		ИдентификаторДокументооборота,
		ДанныеУзлаКонфиденциальнаяИнформацияАдрес,
		ВыводитьСообщения,
		ВыполняемоеОповещение)
	
	ПараметрыОперации = Новый Структура;
	
	Сертификат = Новый Структура("Отпечаток", ОтпечатокСертификата);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Сертификат);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Сертификат.Вставить("КлючевойКонтейнер", ИдентификаторДокументооборота);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",                     ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ДанныеУзлаКонфиденциальнаяИнформацияАдрес", ДанныеУзлаКонфиденциальнаяИнформацияАдрес);
	ДополнительныеПараметры.Вставить("ДанныеДляРасшифровки", 					  ПолучитьИзВременногоХранилища(ДанныеУзлаКонфиденциальнаяИнформацияАдрес));
	ДополнительныеПараметры.Вставить("ВыводитьСообщения",                         ВыводитьСообщения);
	ДополнительныеПараметры.Вставить("ПараметрыОперации",						  ПараметрыОперации);
	ДополнительныеПараметры.Вставить("Сертификат",					  			  Сертификат);
	ДополнительныеПараметры.Вставить("МестоХраненияКлюча",			  			  МестоХраненияКлюча);
	ДополнительныеПараметры.Вставить("МестоХраненияКлючаНастроек",	  			  МестоХраненияКлючаНастроек);

	Если НЕ ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		Оповещение = Новый ОписаниеОповещения(
			"РегистрацияРасшифроватьДанныеОблачнаяПодпись", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		КриптографияЭДКОКлиент.РасшифроватьPKCS7(Оповещение, ДанныеУзлаКонфиденциальнаяИнформацияАдрес, , Сертификат, НЕ ЭтоОблачнаяПодпись(МестоХраненияКлючаНастроек));
	Иначе
		РегистрацияРасшифроватьДанныеПроверитьОблачнуюПодпись(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияРасшифроватьДанныеПослеРасшифрования(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ДополнительныеПараметры.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	АдресВоВременномХранилище = ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	Если Результат.Выполнено Тогда
		Если ТипЗнч(Результат.РасшифрованныйФайл) = Тип("ДвоичныеДанные") Тогда
			АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(Результат.РасшифрованныйФайл, Новый УникальныйИдентификатор);
		Иначе
			АдресВоВременномХранилище = Результат.РасшифрованныйФайл;
		КонецЕсли;
	Иначе
		Если ВыводитьСообщения Тогда
			
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Ошибка криптографии. Невозможно расшифровать файл обновлений:
                      |%1'"), 
				Результат.ОписаниеОшибки);
					  
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		РезультатВыполнения = Новый Структура("РезультатРасшифровки, АдресВоВременномХранилище, МестоХраненияКлюча", Ложь, Неопределено, МестоХраненияКлюча);
		РезультатВыполнения.Вставить("ОписаниеОшибки", Результат.ОписаниеОшибки);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения = Новый Структура("РезультатРасшифровки, АдресВоВременномХранилище, МестоХраненияКлюча", Истина, АдресВоВременномХранилище, МестоХраненияКлюча);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаСервере
Функция ПолучитьПараметрСпецоператораКлиент(Знач Спецоператор, ПараметрыСтруктура, Макет = Неопределено) Экспорт
	
	КонтекстЭДОСервер      = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ПараметрыСпецоператора = КонтекстЭДОСервер.ПолучитьПараметрыОператораСвязи(Спецоператор);
	ЗаполнитьЗначенияСвойств(ПараметрыСтруктура, ПараметрыСпецоператора);
	Возврат ПараметрыСтруктура;
	
КонецФункции

#Область ОтправкаЗаявлениеНаПодключение

&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеПоказатьСообщениеОНевозможностиБезбумажногоПродления(ОповещениеОЗавершении, ОписаниеОшибки) Экспорт
	
	Контекст = Новый Структура("ОписаниеОповещения", ОповещениеОЗавершении);
	Оповещение = Новый ОписаниеОповещения("СформироватьИОтправитьПослеОтветаНаВопросОНеобходимостиОбычногоПродления", ЭтотОбъект, Контекст);
	ТекстВопроса = НСтр("ru = 'Продление с использованием электронной подписи невозможно по причине:
                         |%1
                         |
                         |Отправить заявление без подписания?'");
	ТекстВопроса = СтрШаблон(ТекстВопроса, ОписаниеОшибки); 
	ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьПослеОтветаНаВопросОНеобходимостиОбычногоПродления(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = КодВозвратаДиалога.Да Тогда
		ИзменитьПризнакБезбумажногоПродления(ВходящийКонтекст.ОписаниеОповещения.ДополнительныеПараметры.ДокументЗаявление.Ссылка);
		ВходящийКонтекст.ОписаниеОповещения.ДополнительныеПараметры.Вставить("ПодписатьМетодомБРО", Ложь);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОписаниеОповещения, Истина);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОписаниеОповещения, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеПослеОтветаНаВопросЭПВОблаке(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Истина Тогда
		СформироватьИОтправитьЗаявлениеВМоделиСервисов(
			ВходящийКонтекст.ДокументЗаявление, ВходящийКонтекст.ВыполняемоеОповещение);
	Иначе
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ПериодДействияСертификатаКорректный(ДокументЗаявление, ИмяПоляСертификата) Экспорт
	
	Сертификат  = СертификатИзЗаявления(ДокументЗаявление, ИмяПоляСертификата);
	ТекущаяДата = ТекущаяДата();
	
	Даты          = ДатыСертификата(Сертификат);
	МестнаяДатаС  = Даты.ДатаС;
	МестнаяДатаПо = Даты.ДатаПо;
	
	Если МестнаяДатаПо = Неопределено
		ИЛИ МестнаяДатаС = Неопределено Тогда
		
		Возврат Ложь;
		
	ИначеЕсли ТекущаяДата > МестнаяДатаПо Тогда
		
		Возврат Ложь;
		
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура СравнитьРеквизитыСертификатов(Форма, ИмяПоляСертификата, ОбработчикЗавершения) Экспорт
	
	// Сначала проверка периода, потом реквизитов
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОбработчикЗавершения", ОбработчикЗавершения);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СравнитьРеквизитыСертификатов_ПослеПолученияОтвета", 
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ДокументЗаявление = Форма.ДокументЗаявление;
	Если НЕ ПериодДействияСертификатаКорректный(ДокументЗаявление, ИмяПоляСертификата) Тогда
		
		Кнопки = Новый СписокЗначений;
		Кнопки.Добавить("Вернуться к заявлению для исправления", НСтр("ru = 'ОК'"));
		
		ТекстВопроса = НСтр("ru = 'Подписание заявления выбранным сертификатом невозможно по причине:
                             |Срок действия сертификата истек.
                             |
                             |Вернуться к заявлению для исправления?'");
		
		ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, Кнопки);
		
		Возврат;
	КонецЕсли;
	
	ЗаполнитьТаблицуСравненияРеквизитов(Форма, ИмяПоляСертификата);
		
	Если ЕстьРазличающиесяРеквизиты(Форма) Тогда
		
		АдресТаблицыСравненияРеквизитов = Форма.АдресТаблицыСравненияРеквизитов();
		СертификатДляПодписания         = СертификатИзЗаявления(ДокументЗаявление, ИмяПоляСертификата);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("СертификатДляПодписания",          СертификатДляПодписания);
		ДополнительныеПараметры.Вставить("СпособПолученияСертификата",       ДокументЗаявление.СпособПолученияСертификата);
		ДополнительныеПараметры.Вставить("АдресТаблицыСравненияРеквизитов",  АдресТаблицыСравненияРеквизитов);
		ДополнительныеПараметры.Вставить("ЭтоУдаленноеПереизданиеСертификата", ДокументЗаявление.ЭтоУдаленноеПереизданиеСертификата);
		
		ДополнительныеПараметры.Вставить("ТипЗаявления", ДокументЗаявление.ТипЗаявления);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.Мастер_СравнениеРеквизитовСертификата", 
			ДополнительныеПараметры,
			,
			,
			,
			,
			ОписаниеОповещения);
		
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Выполнено", Истина);
		
		ВыполнитьОбработкуОповещения(ОбработчикЗавершения, ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ЕстьРазличающиесяРеквизиты(Форма)
	
	Отбор = Новый Структура();
	Отбор.Вставить("Различается", Истина);
	 
	Возврат Форма.ТаблицаСравненияРеквизитов.НайтиСтроки(Отбор).Количество() > 0;
	
КонецФункции

&НаСервере
Функция СертификатИзЗаявления(ДокументЗаявление, ИмяПоляСертификата)
	
	Возврат ДокументЗаявление.Ссылка[ИмяПоляСертификата].Получить();
	
КонецФункции

&НаКлиенте
Функция ЗаполнитьТаблицуСравненияРеквизитов(Форма, ИмяПоляСертификата)
	
	ДокументЗаявление        = Форма.ДокументЗаявление;
	ЭтоВторичное             = ДокументЗаявление.ТипЗаявления = ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение");
	ИспользоватьСуществующий = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(ДокументЗаявление);
	ЭтоСертификатДругогоУЦ   = ДокументЗаявление.ЭтоСертификатДругогоУЦ;
	ЭтоЮридическоеЛицо       = ДокументЗаявление.ТипОрганизации;
	
	ЭтоСертификатФизЛица = Форма.ВладелецЭЦПЭтоФизЛицо 
		ИЛИ ИспользоватьСуществующий И ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоСертификатФизЛица(Форма.ВключаемыйСертификат);
	
	СравниватьРеквизитыОрганизации = НЕ ЭтоСертификатФизЛица;
	
	// Сертификат, который только планируем издать
	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаКлиент.ПараметрыНовогоСертификата(ДокументЗаявление);
	// Сертификат, которым будем подписывать
	СертификатДляПодписания = СертификатИзЗаявления(ДокументЗаявление, ИмяПоляСертификата);
	
	Таблица = Форма.ТаблицаСравненияРеквизитов;
	Таблица.Очистить();
	
	// Заголовок
	НоваяСтрока = Таблица.Добавить();
	НоваяСтрока.СтороннийСертификат = НСтр("ru = 'В сертификате'");
	НоваяСтрока.НовыйСертификат 	= НСтр("ru = 'В заявлении'");
	НоваяСтрока.ЭтоЗаголовок		= Истина;
	
	// ИНН
	ВладелецВСертификатеДляПодписания = ДокументооборотСКОКлиентСервер.ВладелецСертификат(СертификатДляПодписания);
	
	ИННыИзНовогоСертификата       = ДокументооборотСКОКлиентСервер.ИННыИзСертификата(РеквизитыЗаявления);
	ИННыИзСертификатДляПодписания = ДокументооборотСКОКлиентСервер.ИННыИзСертификата(ВладелецВСертификатеДляПодписания);
	
	Если ЭтоЮридическоеЛицо И СравниватьРеквизитыОрганизации Тогда
		ДобавитьСтрокуВТаблицуСравненияПоЗначениям(
			Таблица,
			НСтр("ru = 'ИНН'"),
			ИННыИзНовогоСертификата.ЮЛ,
			ИННыИзСертификатДляПодписания.ЮЛ);
	КонецЕсли;

	// ОГРН
	Если СравниватьРеквизитыОрганизации
		И НЕ Форма.ЭтоНотариусАдвокатИлиГКФХ Тогда 
		
		Если ЭтоЮридическоеЛицо Тогда
			Представление = НСтр("ru = 'ОГРН'");
			Имя1 = "1.2.643.100.1";
			Имя2 = "OGRN";
		Иначе
			Представление = НСтр("ru = 'ОГРНИП'");
			Имя1 = "1.2.643.100.5";
			Имя2 = "OGRNIP";
		КонецЕсли;
		
		ДобавитьСтрокуВТаблицуСравнения(
			Таблица,
			Представление,
			РеквизитыЗаявления,
			Имя1,
			СертификатДляПодписания,
			Имя2);

	КонецЕсли;
	
	// Адрес
	СравниватьАдрес = 
		НЕ ЭтоСертификатДругогоУЦ 
		И НЕ Форма.ВладелецЭЦПЭтоФизЛицо;
		
	Если СравниватьАдрес Тогда
		
		ДобавитьСтрокуВТаблицуСравнения(
			Таблица,
			НСтр("ru = 'Страна'"),
			РеквизитыЗаявления,
			"2.5.4.6",
			СертификатДляПодписания,
			"C");

	КонецЕсли;
	
	// При удаленном переиздании адрес раздрешено менять
	СравниватьРегион = СравниватьАдрес И ЭтоЮридическоеЛицо И НЕ (ЭтоВторичное И Форма.ЭтоУдаленноеПереизданиеСертификата);
	Если СравниватьРегион Тогда
		
		ДобавитьСтрокуВТаблицуСравнения(
			Таблица,
			НСтр("ru = 'Регион'"),
			РеквизитыЗаявления,
			"2.5.4.8",
			СертификатДляПодписания,
			"ST");
			
	КонецЕсли;
	
	// Фамилия
	ДобавитьСтрокуВТаблицуСравнения(
		Таблица,
		НСтр("ru = 'Фамилия владелеца'"),
		РеквизитыЗаявления,
		"2.5.4.4",
		СертификатДляПодписания,
		"SN");
		
	// Имя Отчество
	ДобавитьСтрокуВТаблицуСравнения(
		Таблица,
		НСтр("ru = 'Имя Отчество'"),
		РеквизитыЗаявления,
		"2.5.4.42",
		СертификатДляПодписания,
		"GN");
		
	// ИНН Владельца
	Если ЗначениеЗаполнено(ИННыИзСертификатДляПодписания.ФЛ) Тогда
		ДобавитьСтрокуВТаблицуСравненияПоЗначениям(
			Таблица,
			НСтр("ru = 'ИНН владельца сертификата'"),
			ИННыИзНовогоСертификата.ФЛ,
			ИННыИзСертификатДляПодписания.ФЛ);
	КонецЕсли;
	
	// СНИЛС
	ДобавитьСтрокуВТаблицуСравнения(
		Таблица,
		НСтр("ru = 'СНИЛС'"),
		РеквизитыЗаявления,
		"1.2.643.100.3",
		СертификатДляПодписания,
		"SNILS");
	
КонецФункции

&НаКлиенте
Функция РеквизитСертификата(Сертификат, Реквизит)
	
	Если Сертификат.Свойство(Реквизит) Тогда
		Значение = Сертификат[Реквизит];
	Иначе
		Значение = "";
	КонецЕсли;
	
	Возврат Значение;
	
КонецФункции

&НаКлиенте
Процедура СравнитьРеквизитыСертификатов_ПослеПолученияОтвета(Результат, ВходящийКонтекст) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Выполнено", Ложь);
	ДополнительныеПараметры.Вставить("Действие",  Результат);
		
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОбработчикЗавершения, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ВозможноБезбумажноеПродление(ОповещениеОЗавершении, ВладелецСертификата) Экспорт
	
	Отпечаток = НРег(СтрЗаменить(ВладелецСертификата.Сертификат.Отпечаток, " ", ""));
	Контекст = Новый Структура("ВладелецСертификата, ОповещениеОЗавершении", ВладелецСертификата, ОповещениеОЗавершении);
	Оповещение = Новый ОписаниеОповещения("ВозможноБезбумажноеПродлениеПослеПоискаСертификатовВОблачномХранилище", ЭтотОбъект, Контекст);
	НовыйСертификат = Новый Структура("Отпечаток", Отпечаток);
	КонтекстМоделиХраненияКлюча(Истина, НовыйСертификат);
	КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, НовыйСертификат, Ложь, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВозможноБезбумажноеПродлениеПослеПоискаСертификатовВОблачномХранилище(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		Если Результат.СертификатНайден Тогда
			ВходящийКонтекст.Вставить("Сертификат", Результат.СвойстваСертификата.Сертификат);
			ВозможноБезбумажноеПродлениеПослеПолученияСертификата(ВходящийКонтекст);
			Возврат;	
		КонецЕсли;
	КонецЕсли;
	
	Отпечаток = НРег(СтрЗаменить(ВходящийКонтекст.ВладелецСертификата.Сертификат.Отпечаток, " ", ""));
	Оповещение = Новый ОписаниеОповещения("ВозможноБезбумажноеПродлениеПослеПоискаСертификатов", ЭтотОбъект, ВходящийКонтекст);
	НовыйСертификат = Новый Структура("Отпечаток", Отпечаток);
	КонтекстМоделиХраненияКлюча(Ложь, НовыйСертификат);
	КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, НовыйСертификат, Ложь, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВозможноБезбумажноеПродлениеПослеПоискаСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		Если Результат.СертификатНайден Тогда
			ВходящийКонтекст.Вставить("Сертификат", Результат.СвойстваСертификата);
			Оповещение = Новый ОписаниеОповещения("ВозможноБезбумажноеПродлениеПослеЭкспортаСертификата", ЭтотОбъект, ВходящийКонтекст);
			КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(Оповещение, ВходящийКонтекст.ВладелецСертификата.Сертификат);
		Иначе
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Новый Структура("Выполнено,ОписаниеОшибки", Ложь, "Сертификат не найден"));
		КонецЕсли;
	Иначе 
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Результат); 
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВозможноБезбумажноеПродлениеПослеЭкспортаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		ВходящийКонтекст.Вставить("Сертификат", Base64Значение(Результат.СтрокаBase64));
		ВозможноБезбумажноеПродлениеПослеПолученияСертификата(ВходящийКонтекст);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВозможноБезбумажноеПродлениеПослеПолученияСертификата(ВходящийКонтекст)
	
	БезбумажноеПродлениеВозможно = Истина;
	Причины = Новый Массив;
	СвойстваСертификата = ПолучитьСвойстваСертификатаДляПроверкиБезбумажногоПродленияНаСервере(ВходящийКонтекст.Сертификат);
	
	ТекущаяДата = ОбщегоНазначенияКлиент.ДатаСеанса();
	
	Если СвойстваСертификата.ДатаОкончания < ТекущаяДата Тогда
		БезбумажноеПродлениеВозможно = Ложь;
		Причины.Добавить(НСтр("ru = 'Срок действия сертификата истек'"));
	КонецЕсли;
	
	СНИЛС = СтрЗаменить(СтрЗаменить(ВходящийКонтекст.ВладелецСертификата.СНИЛС, " ", ""), "-", "");
	Если СНИЛС <> СвойстваСертификата.СНИЛС Тогда
		БезбумажноеПродлениеВозможно = Ложь;
		
		Причины.Добавить(НСтр("ru = 'СНИЛС владельца сертификата отличается от значения указанного в сертификате'"));
	КонецЕсли;
	
	Если ВходящийКонтекст.ВладелецСертификата.ИНН <> СвойстваСертификата.ИНН Тогда
		БезбумажноеПродлениеВозможно = Ложь;
		
		Причины.Добавить(НСтр("ru = 'ИНН отличается от значения указанного в сертификате'"));
	КонецЕсли;
	
	ФИО = ВРег(СтрЗаменить(СтрЗаменить(СтрЗаменить(ВходящийКонтекст.ВладелецСертификата.ФИО, "-", ""), " ", ""), "_", ""));
	ФИОИсходный = ВРег(СтрЗаменить(СтрЗаменить(СтрЗаменить(СвойстваСертификата.ФИО, "-", ""), " ", ""), "_", ""));
	Если ФИО <> ФИОИсходный Тогда
		БезбумажноеПродлениеВозможно = Ложь;
		
		Причины.Добавить(НСтр("ru = 'ФИО владельца сертификата отличается от ФИО указанного в сертификате'"));
	КонецЕсли;
	
	РезультатВыполнения = Новый Структура("Выполнено", Истина);
	РезультатВыполнения.Вставить("БезбумажноеПродлениеВозможно", БезбумажноеПродлениеВозможно);
	Если Не БезбумажноеПродлениеВозможно Тогда
		РезультатВыполнения.Вставить("Причина", СтрСоединить(Причины, Символы.ПС));	
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСвойстваСертификатаДляПроверкиБезбумажногоПродленияНаСервере(ФайлСертификата) Экспорт
	
	Сертификат = Новый СертификатКриптографии(ФайлСертификата);
	
	Субъект = Сертификат.Субъект;
	ИНН     = ДокументооборотСКОКлиентСервер.ИННИзСертификата(Субъект);
	
	Свойства = Новый Структура;	
	Если НЕ Субъект.Свойство("OID2_5_4_4") // Фамилия
		ИЛИ НЕ Субъект.Свойство("OID2_5_4_42") // Имя[ Отчетство]
		ИЛИ НЕ Субъект.Свойство("OID1_2_643_100_3") // СНИЛС	
		ИЛИ ИНН = "" Тогда
		Возврат Свойства;	
	КонецЕсли;
	
	ФИО = СокрЛП(Субъект["OID2_5_4_4"]) + " " + СокрЛП(Субъект["OID2_5_4_42"]);
	
	Свойства.Вставить("ФИО", ФИО);
	Свойства.Вставить("СНИЛС", Субъект["OID1_2_643_100_3"]);
	Свойства.Вставить("ИНН", ИНН);
	Свойства.Вставить("ДатаНачала", Сертификат.ДатаНачала);
	Свойства.Вставить("ДатаОкончания", Сертификат.ДатаОкончания);
	Свойства.Вставить("Издатель", Сертификат.Издатель.O);
	
	Возврат Свойства;
	
КонецФункции

&НаКлиенте
Функция ПараметрыПроцедурыСформироватьИОтправитьЗаявление() Экспорт
	
	Контекст = Новый Структура;
	Контекст.Вставить("ДокументЗаявление", 							Неопределено);
	Контекст.Вставить("ИдентификаторАбонента", 						"");
	Контекст.Вставить("ВызовИзМастераПодключенияК1СОтчетности", 	Ложь);
	Контекст.Вставить("ВыполняемоеОповещение", 						Неопределено);
	Контекст.Вставить("ФормироватьЗакрытыйКлючИЗапросНаСертификат", Истина);
	
	// Следующие параметры определяются внутри процедуры СформироватьИОтправитьЗаявление
	
	Контекст.Вставить("ПодписатьМетодомБРО",				Ложь);
	Контекст.Вставить("ПодписатьПлатформой", 				Ложь);
	Контекст.Вставить("ЭтоЗаявлениеНаИзменение", 			Ложь);
	Контекст.Вставить("ЭтоРучноеЗаявление", 				Ложь);
	Контекст.Вставить("СтороннийСертификат", 				Неопределено);
	Контекст.Вставить("ЭтоЗаявлениеНаПодключение", 			Ложь);
	Контекст.Вставить("АдресТекстаОтправленногоЗаявления", 	Неопределено);
	Контекст.Вставить("ПрисоединенныйФайлЗаявления", 		Неопределено);
	Контекст.Вставить("ФормаВладелец", 						Неопределено);
	Контекст.Вставить("OIDЛицензииКриптоПро", 				"");
	
	// Будут вычеслены в ходе отправки
	Контекст.Вставить("АдресПодписиПодЗаявлением", 	Неопределено);
	Контекст.Вставить("СканыКЗаявлению", 			Новый Массив);
	Контекст.Вставить("ЭтоНулевка", 				Ложь);
	
	Возврат Контекст;
	
КонецФункции

// Описание параметров находится в процедуре ПараметрыПроцедурыСформироватьИОтправитьЗаявление
&НаКлиенте
Процедура СформироватьИОтправитьЗаявление(Контекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СформироватьИОтправитьЗаявлениеПослеУстановкиКорневогоСертификатаВР",
		ЭтотОбъект, 
		Контекст);
		
	УстановитьСертификатыВРПриНеобходимости(ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеВМоделиСервисов(ДокументЗаявление, ВыполняемоеОповещение)
	
	// Реквизиты формы, чтобы потом работать с ними в подключаемой процедуре
	ОписаниеОповещенияПослеОтправкиЗаявления = ВыполняемоеОповещение;
	ОтправляемоеЗаявление 					 = ДокументЗаявление.Ссылка;
	
	АлгоритмКонтейнераКлючей = ЗаявлениеОпределитьАлгоритмДляСозданияКонтейнераКлючей(ДокументЗаявление);
	Если АлгоритмКонтейнераКлючей = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'При получении алгоритма генерации ключей возникла ошибка. Сервис временно недоступен.'");
		РезультатВыполнения = Новый Структура("ТекстОшибки", ТекстОшибки);
		ВыполнитьОбработкуОповещения(ОписаниеОповещенияПослеОтправкиЗаявления, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	НачатьОтправкуОблачногоЗаявленияВБазуЗаявок(ДокументЗаявление.Ссылка, АлгоритмКонтейнераКлючей);
	ПодключитьОбработчикОжидания("Подключаемый_ПроверитьСостояниеОтправкиОблачногоЗаявленияВБазуЗаявок", 1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьЗаявление_ПослеПолученияВременногоКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено Тогда
	
		ВременныйКаталог = Результат.ИмяКаталога;
		ДополнительныеПараметры.Вставить("ВременныйКаталог", ВременныйКаталог);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("СформироватьИОтправитьЗаявление_ПослеФормированияКлюча", ЭтотОбъект, ДополнительныеПараметры);
		
		Если ДополнительныеПараметры.ФормироватьЗакрытыйКлючИЗапросНаСертификат Тогда
			
			// Подменяем оповещение, чтобы не сразу вернуться в заявление, а обработать результат
			ВходящийКонтекст = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ДополнительныеПараметры);
			ВходящийКонтекст.ВыполняемоеОповещение = ОписаниеОповещения;
			
			Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
				ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификат(ВходящийКонтекст);
			Иначе
				ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнаяПодпись(ВходящийКонтекст);
			КонецЕсли;	
				
		Иначе
			
			ДополнительныеПараметры.Вставить("ТекстЗапросаНаСертификат", 	"");
			ДополнительныеПараметры.Вставить("ПродолжитьОтправкуЗаявления", Истина);
			ДополнительныеПараметры.Вставить("ТекстОшибки", 				"");
			ДополнительныеПараметры.Вставить("ПутьКонтейнерЗакрытогоКлюча", ДополнительныеПараметры.ПутьКонтейнерЗакрытогоКлюча);
			
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, ДополнительныеПараметры);
		КонецЕсли;
	Иначе
		Результат = Новый Структура("ТекстОшибки", Результат.ОписаниеОшибки);
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьЗаявление_ПослеФормированияКлюча(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.ПродолжитьОтправкуЗаявления Тогда
		
		ДокументЗаявление 			= ДополнительныеПараметры.ДокументЗаявление;
		ИдентификаторАбонента 		= ДополнительныеПараметры.ИдентификаторАбонента;
		ТекстЗапросаНаСертификат 	= Результат.ТекстЗапросаНаСертификат;
		АлгоритмКонтейнераКлючей 	= Результат.АлгоритмКонтейнераКлючей;
		
		ТекстЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.АдресЗаявленияПо1СОтчетности(
			ДокументЗаявление, 
			ТекстЗапросаНаСертификат,
			ИдентификаторАбонента,
			АлгоритмКонтейнераКлючей);
			
		АдресЗаявления = ТекстЗаявления.АдресЗаявления;
			
		ДополнительныеПараметры.Вставить("ПрисоединенныйФайлЗаявления", ТекстЗаявления.ПрисоединенныйФайл);
			
		УпакованныеФайлы = ОперацииСФайламиЭДКОВызовСервера.УпаковатьФайлы(
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Новый Структура("Адрес,Имя", АдресЗаявления, "file")), 
			УникальныйИдентификатор);

		ДополнительныеПараметры.Вставить("АдресАрхивированногоЗаявленияНаРегистрацию", 	УпакованныеФайлы);
		ДополнительныеПараметры.Вставить("АдресТекстаОтправленногоЗаявления", 			АдресЗаявления);
		
		// Копируем из результата
		ДополнительныеПараметры.Вставить("ПродолжитьОтправкуЗаявления", 					Результат.ПродолжитьОтправкуЗаявления);
		ДополнительныеПараметры.Вставить("ПутьКонтейнерЗакрытогоКлюча", 					Результат.ПутьКонтейнерЗакрытогоКлюча);
		ДополнительныеПараметры.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", 	Результат.ТекстОшибки);
		
		СформироватьИОтправить_ПодписатьЗаявление(ДополнительныеПараметры);
		
	Иначе
		
		ДополнительныеПараметры.Вставить("ПродолжитьОтправкуЗаявления", 					Ложь);
		ДополнительныеПараметры.Вставить("АдресТекстаОтправленногоЗаявления", 				"");
		ДополнительныеПараметры.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности", 	Результат.ТекстОшибки);
		
		Результат = Новый Структура("Выполнено", Истина);
		
		СформироватьИОтправитьФайлыЗаявления(Результат, ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаявлениеСформироватьИОтправитьПакет(ПараметрыФункции)
	
	УстановитьПривилегированныйРежим(Истина);
	
	ВременныйКаталог = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	ПараметрыФункции.Вставить("ИмяФайлаЗаявлениеНаРегистрацию", 	СгенерироватьИмяФайлаДокументаВСоставеПакета());
	ПараметрыФункции.Вставить("ИмяФайлаПодписьПодЗаявлением", 		СгенерироватьИмяФайлаДокументаВСоставеПакета());
	ПараметрыФункции.Вставить("ИмяФайлаОписаниеПакета", 			"packageDescription.xml");
	ПараметрыФункции.Вставить("ИдентификаторАбонента", 				Сред(ПараметрыФункции.ДокументЗаявление.УчетнаяЗапись.ИдентификаторАбонента, 4));
	ПараметрыФункции.Вставить("ВременныйКаталог", 					ВременныйКаталог);
	ПараметрыФункции.Вставить("ИдентификаторДокументооборота",  	ПараметрыФункции.ДокументЗаявление.ИдентификаторДокументооборота);
	ПараметрыФункции.Вставить("СпецОператорСвязиДокументооборота", 	ПараметрыФункции.ДокументЗаявление.СпецОператорСвязи);
	
	ЗаявлениеСформироватьОписаниеПакета(ПараметрыФункции);
	
	ФайлыПакета = Новый Массив;
	
	// Заявление
	ОписанияФайлаЗаявления = Новый Структура();
	ОписанияФайлаЗаявления.Вставить("Адрес", ПараметрыФункции.АдресАрхивированногоЗаявленияНаРегистрацию);
	ОписанияФайлаЗаявления.Вставить("Имя", 	 ПараметрыФункции.ИмяФайлаЗаявлениеНаРегистрацию);
	ФайлыПакета.Добавить(ОписанияФайлаЗаявления);
	
	Если ПараметрыФункции.Свойство("АдресПодписиПодЗаявлением") И ЭтоАдресВременногоХранилища(ПараметрыФункции.АдресПодписиПодЗаявлением) Тогда
		
		// Подпись заявления
		ОписанияФайлаПодписиЗаявления = Новый Структура();
		ОписанияФайлаПодписиЗаявления.Вставить("Адрес", ПараметрыФункции.АдресПодписиПодЗаявлением);
		ОписанияФайлаПодписиЗаявления.Вставить("Имя", 	ПараметрыФункции.ИмяФайлаПодписьПодЗаявлением);
		
		ФайлыПакета.Добавить(ОписанияФайлаПодписиЗаявления);
		
	КонецЕсли;
	
	Если ПараметрыФункции.Свойство("СканыКЗаявлению") Тогда
		
		СканыКЗаявлению = ПараметрыФункции.СканыКЗаявлению;
		Для каждого Скан Из СканыКЗаявлению Цикл
			
			// Скан
			ОписанияСкана = Новый Структура();
			ОписанияСкана.Вставить("Адрес", Скан.АдресДанных);
			ОписанияСкана.Вставить("Имя", 	Скан.ИмяФайлаДанных);
			
			ФайлыПакета.Добавить(ОписанияСкана);
			
			Если ЗначениеЗаполнено(Скан.АдресПодписи) И ЭтоАдресВременногоХранилища(Скан.АдресПодписи) Тогда
				
				// Подпись скана
				ОписанияПодписиСкана = Новый Структура();
				ОписанияПодписиСкана.Вставить("Адрес",  Скан.АдресПодписи);
				ОписанияПодписиСкана.Вставить("Имя", 	Скан.ИмяФайлаПодписи);

				ФайлыПакета.Добавить(ОписанияПодписиСкана);
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьИмяФайла(ПараметрыФункции.ИмяФайлаОписаниеПакета);
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ПараметрыФункции.ВременныйКаталог,
		"ПараметрыФункции.ВременныйКаталог",
		"ЗаявлениеСформироватьИОтправитьПакет");		
	ПутьКФайлуОписания 		= ПараметрыФункции.ВременныйКаталог + ПараметрыФункции.ИмяФайлаОписаниеПакета;
	//@skip-check 1c-fresh-audit
	ДвДанныеФайлаОписания 	= Новый ДвоичныеДанные(ПутьКФайлуОписания);
	АдресФайлаОписания 		= ПоместитьВоВременноеХранилище(ДвДанныеФайлаОписания);
	
	// Описание архива
	ОписанияФайлаОписания = Новый Структура();
	ОписанияФайлаОписания.Вставить("Адрес", АдресФайлаОписания);
	ОписанияФайлаОписания.Вставить("Имя", 	ПараметрыФункции.ИмяФайлаОписаниеПакета);
		
	ФайлыПакета.Добавить(ОписанияФайлаОписания);
	
	Пакет = ОперацииСФайламиЭДКОВызовСервера.УпаковатьФайлы(ФайлыПакета);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
	
	ПрисоединитьФайлЗаявления(ПараметрыФункции.ДокументЗаявление, Пакет, "Пакет_с_заявлением", "zip");
	
	ОбменССерверомОтправитьЗаявление(
		Пакет, 
		ПараметрыФункции.ДокументЗаявление,
		ПараметрыФункции.Статус, 
		ПараметрыФункции.ДатаОтправки, 
		ПараметрыФункции.СтатусКомментарий,
		ПараметрыФункции.СостояниеОтправкиСообщения,
		ПараметрыФункции.ВызовИзМастераПодключения, 
		ПараметрыФункции.ТекстОшибокДляМастераПодключенияК1СОтчетности, 
		ПараметрыФункции.ПовторятьСоединение);
		
КонецПроцедуры
	
&НаСервереБезКонтекста
Функция ЗаявлениеСформироватьОписаниеПакета(ПараметрыФункции)
	
	ДокументОписания = Новый ЗаписьXML;
	ИмяФайла = ПараметрыФункции.ВременныйКаталог + ПараметрыФункции.ИмяФайлаОписаниеПакета;
	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ИмяФайла, "ИмяФайла", "ЗаявлениеСформироватьОписаниеПакета");		
	//@skip-check 1c-fresh-audit
	ДокументОписания.ОткрытьФайл(
		ИмяФайла, "windows-1251");
	ДокументОписания.ЗаписатьОбъявлениеXML();
	
	ДокументОписания.ЗаписатьНачалоЭлемента("пакет");
	
	Если ПараметрыФункции.ПодписатьПлатформой Тогда
		ДокументОписания.ЗаписатьАтрибут("версияФормата", "1С:1.2");
	ИначеЕсли ПараметрыФункции.ЭтоЗаявлениеНаИзменение Тогда
		ДокументОписания.ЗаписатьАтрибут("версияФормата", "1С:1.1");
	Иначе
		ДокументОписания.ЗаписатьАтрибут("версияФормата", "1С:1.0");
	КонецЕсли;
	
	ДокументОписания.ЗаписатьАтрибут("версПрог", РегламентированнаяОтчетность.ВерсияПрограммы());
	ДокументОписания.ЗаписатьАтрибут("типДокументооборота", "РегистрацияАбонента");
	ДокументОписания.ЗаписатьАтрибут("типТранзакции", "Регистрация");
	ДокументОписания.ЗаписатьАтрибут("идентификаторДокументооборота", ПараметрыФункции.ИдентификаторДокументооборота);
	
	ДокументОписания.ЗаписатьНачалоЭлемента("отправитель");
	ДокументОписания.ЗаписатьАтрибут("типСубъекта", "абонент");
	Если ПараметрыФункции.ЭтоЗаявлениеНаИзменение Тогда
		ДокументОписания.ЗаписатьАтрибут("идентификаторСубъекта", ПараметрыФункции.ИдентификаторАбонента);
	КонецЕсли;
	ДокументОписания.ЗаписатьКонецЭлемента();
	
	ДокументОписания.ЗаписатьНачалоЭлемента("получатель");
	СпецОператорСвязи = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(ПараметрыФункции.СпецОператорСвязиДокументооборота,"ВебСервисИдентификаторСубъекта");
	ДокументОписания.ЗаписатьАтрибут("идентификаторСубъекта", СпецОператорСвязи);
	ДокументОписания.ЗаписатьАтрибут("типСубъекта", "спецоператор");
	ДокументОписания.ЗаписатьКонецЭлемента();
	
	// документ-заявление
	ДокументОписания.ЗаписатьНачалоЭлемента("документ");
	ДокументОписания.ЗаписатьАтрибут("идентификаторДокумента", ОбщегоНазначенияЭДКОКлиентСервер.НовыйИдентификатор());
	ДокументОписания.ЗаписатьАтрибут("типДокумента", "Заявление");
	ДокументОписания.ЗаписатьАтрибут("типСодержимого", "xml");
	ДокументОписания.ЗаписатьАтрибут("сжат", "true");
	ДокументОписания.ЗаписатьАтрибут("зашифрован", "true");
	
	ДокументОписания.ЗаписатьНачалоЭлемента("содержимое");
	ДокументОписания.ЗаписатьАтрибут("имяФайла", ПараметрыФункции.ИмяФайлаЗаявлениеНаРегистрацию);
	ДокументОписания.ЗаписатьКонецЭлемента();
	
	Если ПараметрыФункции.ПодписатьМетодомБРО ИЛИ ПараметрыФункции.ПодписатьПлатформой Тогда
		ДокументОписания.ЗаписатьНачалоЭлемента("подпись");
		ДокументОписания.ЗаписатьАтрибут("имяФайла", ПараметрыФункции.ИмяФайлаПодписьПодЗаявлением);
		ДокументОписания.ЗаписатьКонецЭлемента();	
	КонецЕсли;
	ДокументОписания.ЗаписатьКонецЭлемента();
	
	ЗаявлениеСформироватьОписаниеПакета_ДобавитьСканы(ДокументОписания, ПараметрыФункции);
	
	// пакет
	ДокументОписания.ЗаписатьКонецЭлемента();
	
	ДокументОписания.Закрыть();
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗаявлениеСформироватьОписаниеПакета_ДобавитьСканы(ДокументОписания, ПараметрыФункции)
	
	Если ПараметрыФункции.Свойство("СканыКЗаявлению") 
		И (ПараметрыФункции.ЭтоЗаявлениеНаИзменение ИЛИ ПараметрыФункции.ЭтоЗаявлениеНаПодключение) Тогда
		
		СканыКЗаявлению = ПараметрыФункции.СканыКЗаявлению;
		Для каждого Скан Из СканыКЗаявлению Цикл
			
			ДокументОписания.ЗаписатьНачалоЭлемента("документ");
			ДокументОписания.ЗаписатьАтрибут("идентификаторДокумента", Скан.идентификаторДокумента);
			
			ДокументОписания.ЗаписатьАтрибут("типДокумента", Скан.ВидДокумента);
			
			Если Скан.Свойство("идентификаторДокументаРодителя")
				И ЗначениеЗаполнено(Скан.идентификаторДокументаРодителя) Тогда
				ДокументОписания.ЗаписатьАтрибут("идентификаторМЧД", Скан.идентификаторДокументаРодителя);
			КонецЕсли;
			
			ДокументОписания.ЗаписатьАтрибут("типСодержимого", Нрег(Скан.Расширение));
			
			ДокументОписания.ЗаписатьАтрибут("сжат", "true");
			ДокументОписания.ЗаписатьАтрибут("зашифрован", "true");
			
			ДокументОписания.ЗаписатьНачалоЭлемента("содержимое");
			ДокументОписания.ЗаписатьАтрибут("имяФайла", Скан.ИмяФайлаДанных);
			ДокументОписания.ЗаписатьКонецЭлемента();
			
			Если ЗначениеЗаполнено(Скан.АдресПодписи) И ЭтоАдресВременногоХранилища(Скан.АдресПодписи) Тогда
				ДокументОписания.ЗаписатьНачалоЭлемента("подпись");
				ДокументОписания.ЗаписатьАтрибут("имяФайла", Скан.ИмяФайлаПодписи);
				ДокументОписания.ЗаписатьКонецЭлемента();
			КонецЕсли;
			
			ДокументОписания.ЗаписатьКонецЭлемента();
			
		КонецЦикла; 
		
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ОбменССерверомОтправитьЗаявление(
		Знач Файл, 
		ДокументЗаявление, 
		Статус, 
		ДатаОтправки, 
		СтатусКомментарий,
		СостояниеОтправкиСообщения,
		ВызовИзМастераПодключенияК1СОтчетности, 
		ТекстОшибокДляМастераПодключенияК1СОтчетности, 
		ПовторятьСоединение = Ложь)
		
	СпецоператорСвязи = ДокументЗаявление.СпецоператорСвязи;
	
	Сервис = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбменССерверомСоздатьСервис(
		СпецоператорСвязи, 
		ВызовИзМастераПодключенияК1СОтчетности, 
		ТекстОшибокДляМастераПодключенияК1СОтчетности, , 
		Ложь);
	
	Если Сервис = Неопределено Тогда
		СостояниеОтправкиСообщения = "Не удалось отправить сообщение";
		Возврат;
	КонецЕсли;
	
	Если ЭтоАдресВременногоХранилища(Файл) Тогда
		СтрокаBase64 = Base64Строка(ПолучитьИзВременногоХранилища(Файл));
	Иначе
		РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
			Файл, "Файл", "ОбменССерверомОтправитьЗаявление");			
		//@skip-check 1c-fresh-audit
		СтрокаBase64 = Base64Строка(Новый ДвоичныеДанные(Файл));
	КонецЕсли;
	
	ЭтоВторичноеЗаявление = ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоЗаявлениеНаИзменение(ДокументЗаявление);
	
	Если НЕ ЗначениеЗаполнено(ДокументЗаявление.НомерОсновнойПоставки1с) Тогда
		
		ТикетАутентификации = ТикетАутентификацииНаПорталеПоддержки();
		Тикет = ТикетАутентификации.Тикет;
		
		Если НЕ ЗначениеЗаполнено(Тикет)
			И НЕ ЭтоВторичноеЗаявление Тогда
			
			ТекстОшибки = НСтр("ru = 'ТикетАутентификацииНаПорталеПоддержки.КодОшибки:'") + ТикетАутентификации.КодОшибки;
			
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
				ТекстОшибки, 
				ВызовИзМастераПодключенияК1СОтчетности, 
				ТекстОшибокДляМастераПодключенияК1СОтчетности, 
				НЕ ВызовИзМастераПодключенияК1СОтчетности);
				
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Попытка
		Если ЗначениеЗаполнено(Тикет) Тогда  
			XDTOРезультат = Сервис.SendPacketWithTicket(СтрокаBase64, Тикет);
		Иначе
			XDTOРезультат = Сервис.SendPacket(СтрокаBase64);
		КонецЕсли;
	Исключение
		
		ТекстОшибки = НСтр("ru = 'Не удалось обратиться к серверу оператора ""Калуга Астрал"". '") + Символы.ПС + КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
		СостояниеОтправкиСообщения = ДокументооборотСКОВызовСервера.ДобавитьТекстОшибкиСертификатаКА(ТекстОшибки);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
			СостояниеОтправкиСообщения, 
			ВызовИзМастераПодключенияК1СОтчетности, 
			ТекстОшибокДляМастераПодключенияК1СОтчетности, 
			НЕ ВызовИзМастераПодключенияК1СОтчетности);
			
		КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		
		ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Не удалось обратиться к серверу оператора ""Калуга Астрал"" при отправке заявления.'", КодОсновногоЯзыка),
			УровеньЖурналаРегистрации.Ошибка,
			,
			,
			ИнформацияОбОшибке);
			
		// Пробуем запасным способом проверить статус заявления, чтобы заявления не задублировались на сервере КА
		КонтрольСостояния = ОбработкаЗаявленийАбонентаВызовСервера.ОбменССерверомПолучитьОтвет(
			ДокументЗаявление, 
			ВызовИзМастераПодключенияК1СОтчетности);
			
		Если КонтрольСостояния.Выполнено Тогда
			СостояниеОтправкиСообщения = "Заявление отправлено в обработку на сервер";
			Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
			СтатусКомментарий = КонтрольСостояния.СтатусКомментарий;
		КонецЕсли;
			
		Возврат;
		
	КонецПопытки;
	
	ПовторятьСоединение = Ложь;
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(XDTOРезультат);
	ПостроительДОМ = Новый ПостроительDOM();
	ДОМ = ПостроительДОМ.Прочитать(ЧтениеXML);
	УзелDOM = ДОМ.ПолучитьЭлементыПоИмени("code");
	кодРезультата = УзелDOM[0].ТекстовоеСодержимое;
	ДатаОтправки = ТекущаяДатаСеанса();
	
	Если КодРезультата = "0" Тогда
		СостояниеОтправкиСообщения = "Заявление отправлено в обработку на сервер";
		Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
		СтатусКомментарий = "";
	ИначеЕсли КодРезультата = "1000" Тогда
		СостояниеОтправкиСообщения = "Заявление отправлено в автоматическую обработку";
		Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено;
		СтатусКомментарий = "";
	Иначе
		
		СообщениеОшибки = "";
		
		УзелDOM = ДОМ.ПолучитьЭлементыПоИмени("errorMessage");
		
		Если УзелDOM.Количество() > 0 Тогда
			СообщениеОшибки = УзелDOM[0].ТекстовоеСодержимое;
			СостояниеОтправкиСообщения = "При обработке заявления на сервере возникла ошибка:
			|"+ Символы.пс + сообщениеОшибки;
		КонецЕсли;
		
		СтатусКомментарий = сообщениеОшибки;
		Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено;
	КонецЕсли;
	ЧтениеXML.Закрыть();
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ТикетАутентификацииНаПорталеПоддержки() Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	ВладелецТикета = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ВладелецТикета();
	
	МодульИнтернетПоддержкаПользователей = ЭлектронныйДокументооборотСКонтролирующимиОрганами.МодульИнтернетПоддержкаПользователей();
	
	Если МодульИнтернетПоддержкаПользователей = Неопределено Тогда
		Возврат Неопределено;
	Иначе
		
		РезультатПолученияТикета = МодульИнтернетПоддержкаПользователей.ТикетАутентификацииНаПорталеПоддержки(ВладелецТикета);
		
		Возврат РезультатПолученияТикета;
		
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Функция ТекстДляПревышеноКоличествоПопытокПолученияТикета() Экспорт

	Текст = Новый ФорматированнаяСтрока(
		Символы.ПС,
		НСтр("ru = 'Что делать?'"), Символы.ПС,
		НСтр("ru = '1. Повторите попытку позже.'"), Символы.ПС,
		НСтр("ru = '2. При необходимости '"),
		Новый ФорматированнаяСтрока(НСтр("ru = 'восстановите'"),,,, "восстановить пароль Портала ИТС"),
		НСтр("ru = ' пароль и '"),
		Новый ФорматированнаяСтрока(НСтр("ru = 'повторите'"),,,, "задать логин и пароль от ИТС"),
		НСтр("ru = ' попытку авторизации.'"));

	Возврат Текст;

КонецФункции

&НаКлиенте
Процедура ОбработкаНавигационнойСсылкиИТС(НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Если НавигационнаяСсылкаФорматированнойСтроки = "задать логин и пароль от ИТС" Тогда
		 УказатьЛогинИПарольИТС();
	ИначеЕсли НавигационнаяСсылкаФорматированнойСтроки = "перейти на Портал ИТС" Тогда
		 ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПерейтиНаПортал();
	ИначеЕсли НавигационнаяСсылкаФорматированнойСтроки = "восстановить пароль Портала ИТС" Тогда
		МодульИнтернетПоддержкаПользователейКлиент = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.МодульИнтернетПоддержкаПользователейКлиент();
		МодульИнтернетПоддержкаПользователейКлиент.ОткрытьСтраницуВосстановленияПароля();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УказатьЛогинИПарольИТС() Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(); 
	
	МодульИнтернетПоддержкаПользователейКлиент = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.МодульИнтернетПоддержкаПользователейКлиент();
	МодульИнтернетПоддержкаПользователейКлиент.ПодключитьИнтернетПоддержкуПользователей(
		ОписаниеОповещения,
		ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьФайлыЗаявления(Результат, ДополнительныеПараметры) Экспорт
	
	ТекстОшибки 				= ДополнительныеПараметры.ТекстОшибокДляМастераПодключенияК1СОтчетности;
	СостояниеОтправкиСообщения 	= "";

	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Выполнено = Истина Тогда
		
		ДокументЗаявление = ДополнительныеПараметры.ДокументЗаявление;
		
		Если ДополнительныеПараметры.ПродолжитьОтправкуЗаявления Тогда
			
			ПараметрыОтправки = ПараметрыЗаявлениеСформироватьИОтправитьПакет(ДополнительныеПараметры);
			ЗаявлениеСформироватьИОтправитьПакет(ПараметрыОтправки);
			
			КопируемыеСвойства = "Статус, ДатаОтправки, СтатусКомментарий";
			ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, ПараметрыОтправки, КопируемыеСвойства);
			
			Если ПараметрыОтправки.Статус <> ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено") Тогда
				
				ПовторятьСоединение 		= ПараметрыОтправки.ПовторятьСоединение;
				СостояниеОтправкиСообщения 	= ПараметрыОтправки.СостояниеОтправкиСообщения;
				ТекстОшибки					= ПараметрыОтправки.ТекстОшибокДляМастераПодключенияК1СОтчетности;
				
				СообщитьОбОшибкеОтправкиЗаявления(
					СостояниеОтправкиСообщения, 
					ПовторятьСоединение,
					ТекстОшибки,
					ДополнительныеПараметры);
					
			КонецЕсли;
			
		КонецЕсли;
		
		ОбновитьДанныеЗаявленияПослеОтправки(ДополнительныеПараметры);
			
		ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(,ДополнительныеПараметры.ВременныйКаталог);
		
		Если ДокументЗаявление.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено") Тогда
			ОбработкаЗаявленийАбонентаКлиент.ВключитьАвтоматическуюПроверкуСтатуса(ДокументЗаявление.Ссылка);
		ИначеЕсли ДокументЗаявление.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено") Тогда
			ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(
				ДокументЗаявление.Ссылка, 
				Новый Структура("НастройкаЗавершена", Истина));
		КонецЕсли;
		
	КонецЕсли;
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		
		СтруктураРезультата = Новый Структура();
		СтруктураРезультата.Вставить("ТекстОшибки", 		ТекстОшибки);
		СтруктураРезультата.Вставить("СостояниеОтправки", 	СостояниеОтправкиСообщения);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, СтруктураРезультата);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ПараметрыЗаявлениеСформироватьИОтправитьПакет(ДополнительныеПараметры)
	
	ПараметрыОтправки 	= ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ДополнительныеПараметры);
	ПараметрыОтправки.Вставить("ВызовИзМастераПодключения", ДополнительныеПараметры.ВызовИзМастераПодключенияК1СОтчетности);
	
	// Удаляем те свойства, которые нельзя передавать на сервер
	ПараметрыОтправки.Удалить("ВыполняемоеОповещение");
	ПараметрыОтправки.Удалить("ФормаВладелец");
	
	ПараметрыОтправки.Вставить("СостояниеОтправкиСообщения", 	"");
	ПараметрыОтправки.Вставить("ПовторятьСоединение", 			Истина);

	Возврат ПараметрыОтправки;
	
КонецФункции

&НаКлиенте
Процедура ЗаявлениеЗашифроватьПослеПолученияСвойствСертификатаОператораСвязи(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено И Результат.СертификатНайден Тогда
		
		ДополнительныеПараметры.Вставить("СвойстваСертификатаОператораСвязи", Результат.СвойстваСертификата);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеШифрованияФайлаЗаявления", ЭтотОбъект, ДополнительныеПараметры);
		
		ПараметрыОперации = Новый Структура;
		ПараметрыОперации.Вставить("ОбработкаОшибок", Ложь);
		
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
			ОписаниеОповещения, 
			ДополнительныеПараметры.АдресАрхивированногоЗаявленияНаРегистрацию, 
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Результат.СвойстваСертификата),,
			ДополнительныеПараметры.ВыводитьСообщения,,, 
			ПараметрыОперации);

	ИначеЕсли Результат.Выполнено И НЕ Результат.СертификатНайден Тогда
		Результат.Вставить("ТекстОшибки", НСтр("ru = 'Не удалось получить свойства сертификата оператора связи'"));
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
		
	Иначе
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправить_ПодписатьЗаявление(ПараметрыФункции)
	
	ДокументЗаявление = ПараметрыФункции.ДокументЗаявление;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	ОрганизацияСсылка = ДокументЗаявление.Организация;
	
	Если ПараметрыФункции.ПодписатьПлатформой Тогда
		
		ПодписатьЗаявлениеСтороннимСертификатом(ПараметрыФункции);
		
	ИначеЕсли ПараметрыФункции.ПодписатьМетодомБРО Тогда
		
		Сертификат = ПолучитьСертификатИзЗаявленияНаСервере(ДокументЗаявление.Ссылка);
		МестоХраненияСертификата = КриптографияЭДКОКлиентСервер.КонтекстМоделиХраненияКлюча(Сертификат);
		
		ПараметрыОперации = Неопределено;
		Если ЭтоОблачнаяПодпись(МестоХраненияСертификата) Тогда
			ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ЗаявлениеНаПодключение", ОрганизацияСсылка); 
			ПараметрыОперации = ПодготовитьОблачнуюПодпись("Заявление на подключение к 1С-Отчетности", "xml", ТребуетПодтверждениеОперации);
		КонецЕсли;
	
		ПараметрыФункции.Вставить("Сертификат", Сертификат);
			
		// Нельзя объединять оповещения, так как параметры оповещения различные
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьИОтправить_ПослеПодписанияЗаявления", 
			ЭтотОбъект, 
			ПараметрыФункции);
			
		АдресЗаявления  = ПараметрыФункции.АдресТекстаОтправленногоЗаявления;
		АдресРезультата = ПоместитьВоВременноеХранилище(, Новый УникальныйИдентификатор);
		
		КриптографияЭДКОКлиент.ПодписатьPKCS7(
			Оповещение, 
			ПараметрыФункции.Сертификат, 
			АдресЗаявления, 
			Ложь, 
			АдресРезультата,,, 
			Истина, 
			ПараметрыОперации);
			
	Иначе
		
		// Подписания не будет, но сканы все равно надо приложить
		СканыКЗаявлению = СформироватьИОтправить_ПолучитьПрисоединенныеФайлы(ПараметрыФункции);
		Если СканыКЗаявлению.Количество() > 0 Тогда
			ПараметрыФункции.Вставить("СканыКЗаявлению", СканыКЗаявлению);
		КонецЕсли;
		
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СформироватьИОтправитьЗаявлениеВМоделиСервисов(ДокументЗаявление, ПараметрыФункции.ВыполняемоеОповещение);
		Иначе
			ЗаявлениеЗашифровать(ПараметрыФункции);
		КонецЕсли;
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьЗаявлениеСтороннимСертификатом(ПараметрыФункции)
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПодписатьЗаявлениеСтороннимСертификатом_Завершение", 
		ЭтотОбъект, 
		ПараметрыФункции);
		
	ДокументЗаявление = ПараметрыФункции.ДокументЗаявление;
		
	АдресЗаявления          = ПараметрыФункции.АдресТекстаОтправленногоЗаявления;
	ДвДанныеДляПодписания   = ПолучитьИзВременногоХранилища(АдресЗаявления);
	ТипКриптопровайдера     = ДокументЗаявление.ТипКриптопровайдера;
	СертификатДляПодписания = ОбработкаЗаявленийАбонентаВызовСервера.СертификатИзЗаявления(ДокументЗаявление);
	
	ОбработкаЗаявленийАбонентаКлиент.ПодписатьЗаявлениеСтороннимСертификатом(
		ЭтотОбъект, 
		ОписаниеОповещения, 
		ДвДанныеДляПодписания, 
		СертификатДляПодписания,
		ТипКриптопровайдера);
		
		
КонецПроцедуры

&НаСервере
Процедура ДобавитьПодписьКФайлуЗаявления(АдресПодписи, ИмяФайлаИлиПрисоединенныйФайл, ДокументЗаявление)
	
	Если ТипЗнч(ИмяФайлаИлиПрисоединенныйФайл) = Тип("Строка") Тогда
		
		Для Каждого СтрокаТаблицы Из ДокументЗаявление.ЭлектронныеДокументы Цикл
			Если СтрокаТаблицы.Документ = ИмяФайлаИлиПрисоединенныйФайл Тогда
				ПрисоединенныйФайл = СтрокаТаблицы.Файл;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ПрисоединенныйФайл = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
	Иначе
		
		ПрисоединенныйФайл = ИмяФайлаИлиПрисоединенныйФайл;
		
	КонецЕсли;
	
	Если ДокументЗаявление.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное Тогда
		ДанныеСертификата = ДокументЗаявление.РеквизитыСертификата.Получить();
	Иначе
		ДанныеСертификата = ДокументЗаявление.Сертификат.Получить();
	КонецЕсли;
	
	СвойстваПодписи = Новый Структура;
	СвойстваПодписи.Вставить("Подпись", 	 ПолучитьИзВременногоХранилища(АдресПодписи));
	СвойстваПодписи.Вставить("Сертификат", 	 ДанныеСертификата.Сертификат);
	СвойстваПодписи.Вставить("ПодписьВерна", Истина);
	СвойстваПодписи.Вставить("ДатаПроверкиПодписи", ТекущаяДата());
	СвойстваПодписи.Вставить("КомуВыданСертификат", "");
	
	РаботаСФайлами.ДобавитьПодписьКФайлу(ПрисоединенныйФайл, СвойстваПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправить_ПослеПодписанияЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Если ВходящийКонтекст.ПодписатьПлатформой Тогда
		МестоХраненияСертификата = КриптографияЭДКОКлиентСервер.КонтекстМоделиХраненияКлюча(Ложь);
	Иначе
		Сертификат = ВходящийКонтекст.Сертификат;
		МестоХраненияСертификата = КриптографияЭДКОКлиентСервер.КонтекстМоделиХраненияКлюча(Сертификат);
	КонецЕсли;
		
	// Из-за того, что подписание выполняется методом компоненты или платформы, то результат возвращается разный
	Если ЭтоОблачнаяПодпись(МестоХраненияСертификата) Тогда
		Выполнено = Результат.Выполнено;
	Иначе
		Выполнено = ВыполненоПодписаниеФайлаЗаявления(Результат, ВходящийКонтекст);
	КонецЕсли;
	
	Если Выполнено Тогда
		
		ДокументЗаявление = ВходящийКонтекст.ДокументЗаявление;
		Адрес = АдресФайлаПодписиФайлаЗаявления(Результат, ВходящийКонтекст);
		ВходящийКонтекст.Вставить("АдресПодписиПодЗаявлением", Адрес);
		
		ДобавитьПодписьКФайлуЗаявления(
			Адрес, 
			ВходящийКонтекст.ПрисоединенныйФайлЗаявления, 
			ДокументЗаявление.Ссылка);
		
		СканыКЗаявлению = СформироватьИОтправить_ПолучитьПрисоединенныеФайлы(ВходящийКонтекст);
		ВходящийКонтекст.Вставить("СканыКЗаявлению", 		СканыКЗаявлению);
		ВходящийКонтекст.Вставить("ИндексТекущегоСкана",	0);
		
		Если ЭтоОблачнаяПодпись(МестоХраненияСертификата) И СканыКЗаявлению.Количество() > 1 И ВключитьПакетнуюПодпись Тогда
			СформироватьИОтправить_ПодписатьСканыКЗаявлениюПакетно(ВходящийКонтекст);
		Иначе	
			СформироватьИОтправить_ПодписатьСканыКЗаявлениюВЦикле(ВходящийКонтекст);
		КонецЕсли;
		   
	Иначе
		
		СообщитьОбОшибкеПриПодписанииЗаявления(ВходящийКонтекст, Результат.ОписаниеОшибки);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СообщитьОбОшибкеПриПодписанииЗаявления(ВходящийКонтекст, ОписаниеОшибки)
	
	ИспользоватьСуществующий = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(ВходящийКонтекст.ДокументЗаявление);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	
	ТолькоПодписание = Ложь;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
		И НЕ ВходящийКонтекст.ЭтоЗаявлениеНаПодключение
		И ВходящийКонтекст.ПодписатьМетодомБРО Тогда
		СвойствоОблачнойПодписи = ОбработкаЗаявленийАбонентаКлиентСервер.СведенияОблачнойПодписиЗаявления(ВходящийКонтекст.ДокументЗаявление);
		ТолькоПодписание = СвойствоОблачнойПодписи.ТребуетИзменения;
	КонецЕсли;
	
	// Вывод ошибки
	Если ВходящийКонтекст.ПодписатьМетодомБРО И ВходящийКонтекст.ЭтоЗаявлениеНаИзменение И НЕ ТолькоПодписание Тогда
		// Заявление на изменение
		Оповещение = Новый ОписаниеОповещения("СформироватьИОтправитьЗаявлениеПослеОтветаНаВопрос", ЭтотОбъект, ВходящийКонтекст);
		СформироватьИОтправитьЗаявлениеПоказатьСообщениеОНевозможностиБезбумажногоПродления(Оповещение, ОписаниеОшибки);
	Иначе
		// Первичное заявление
		СформироватьИОтправитьЗаявление_СообщитьОНевозможностиЭлектронногоПодписания(ВходящийКонтекст, ОписаниеОшибки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьЗаявление_СообщитьОНевозможностиЭлектронногоПодписания(ВходящийКонтекст, ТекстОшибки = "") Экспорт
	
	Если ВходящийКонтекст.СтороннийСертификат = Неопределено Тогда 
		ПредставлениеСертификата = "";
	Иначе
		ПредставлениеСертификата = Символы.ПС + ВходящийКонтекст.СтороннийСертификат.ПредставлениеСертификата;
	КонецЕсли;
	
	ТекстОшибки = НСтр("ru = 'Не удалось выполнить подписание сертификатом'") 
					+ ПредставлениеСертификата
					+ НСтр("ru = ' по причине:'")
					+ Символы.Таб + ТекстОшибки;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекстОшибки", ТекстОшибки);
	 
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправить_ПодписатьСканыКЗаявлениюВЦикле(ВходящийКонтекст) Экспорт
	
	ИндексТекущегоСкана = ВходящийКонтекст.ИндексТекущегоСкана;
	СканыКЗаявлению 	= ВходящийКонтекст.СканыКЗаявлению;
	ДокументЗаявление	= ВходящийКонтекст.ДокументЗаявление;
	МестоХраненияКлюча	= КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	ОрганизацияСсылка   = ДокументЗаявление.Организация;
	
	Если ИндексТекущегоСкана = СканыКЗаявлению.Количество() Тогда
		
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СформироватьИОтправитьЗаявлениеВМоделиСервисов(ДокументЗаявление, ВходящийКонтекст.ВыполняемоеОповещение);
		Иначе
			ЗаявлениеЗашифровать(ВходящийКонтекст);
		КонецЕсли;
	
	Иначе
		
		ТекущийСкан = СканыКЗаявлению[ИндексТекущегоСкана];
		
		Если ВходящийКонтекст.ПодписатьМетодомБРО Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"СформироватьИОтправить_ПослеПодписанияСледующегоСкана", 
				ЭтотОбъект, 
				ВходящийКонтекст); 
				
			АдресРезультата = ПоместитьВоВременноеХранилище(, Новый УникальныйИдентификатор);
				
			ПараметрыОперации = Неопределено;
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
				ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ЗаявлениеНаПодключениеСканы", ОрганизацияСсылка); 
				ПараметрыОперации = ПодготовитьОблачнуюПодпись(ТекущийСкан.Наименование, ТекущийСкан.Расширение, ТребуетПодтверждениеОперации);
			КонецЕсли;
			
			КриптографияЭДКОКлиент.ПодписатьPKCS7(
				ОписаниеОповещения, 
				ВходящийКонтекст.Сертификат, 
				ТекущийСкан.АдресДанных, 
				ДокументЗаявление.ЭтоУдаленноеПереизданиеСертификата, 
				АдресРезультата,
				Ложь,
				,
				ВходящийКонтекст.ПодписатьПлатформой,
				ПараметрыОперации);
				
		Иначе

			ПодписатьСканыСтороннимСертификатом(ВходящийКонтекст);
			
		КонецЕсли;
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправить_ПослеПодписанияСледующегоСкана(Результат, ВходящийКонтекст) Экспорт

	Выполнено = ВыполненоПодписаниеФайлаЗаявления(Результат, ВходящийКонтекст);
	
	Если Выполнено Тогда
		
		ИндексТекущегоСкана = ВходящийКонтекст.ИндексТекущегоСкана;
		СканыКЗаявлению 	= ВходящийКонтекст.СканыКЗаявлению;

		ТекущийСкан = СканыКЗаявлению[ИндексТекущегоСкана];
		ТекущийСкан.АдресПодписи = АдресФайлаПодписиФайлаЗаявления(Результат, ВходящийКонтекст);
		
		ДобавитьПодписьКФайлуЗаявления(
			ТекущийСкан.АдресПодписи, 
			ТекущийСкан.ПрисоединенныйФайл, 
			ВходящийКонтекст.ДокументЗаявление.Ссылка);
		
		ВходящийКонтекст.ИндексТекущегоСкана = ВходящийКонтекст.ИндексТекущегоСкана + 1;
		
		СформироватьИОтправить_ПодписатьСканыКЗаявлениюВЦикле(ВходящийКонтекст);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Результат);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеПослеОтветаНаВопрос(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Истина Тогда
		
		ДокументЗаявление = ВходящийКонтекст.ДокументЗаявление;
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
		
		ИзменитьПризнакБезбумажногоПродления(ДокументЗаявление.Ссылка);
		
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СформироватьИОтправитьЗаявлениеВМоделиСервисов(ДокументЗаявление, ВходящийКонтекст.ВыполняемоеОповещение);
		Иначе
			ВходящийКонтекст.Вставить("ПодписатьМетодомБРО", Ложь);
			ЗаявлениеЗашифровать(ВходящийКонтекст);
		КонецЕсли;
		
	Иначе
		
		Если ВходящийКонтекст.Свойство("ВыполняемоеОповещение") Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("Выполнено",   Ложь);
			ДополнительныеПараметры.Вставить("ТекстОшибки", НСтр("ru = 'Пользователь отказался от продления без использования электронной подписи'"));
			 
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, ДополнительныеПараметры);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИзменитьПризнакБезбумажногоПродления(ДокументЗаявление)
	
	ДокументОбъект = ДокументЗаявление.ПолучитьОбъект();
	ДокументОбъект.ПодписатьЭП = Ложь;
	ДокументОбъект.ТребуетсяВстречаСПартнером = Истина;
	ДокументОбъект.Записать();
	
КонецПроцедуры

&НаКлиенте
Функция СформироватьИОтправить_ПолучитьПрисоединенныеФайлы(ВходящийКонтекст)
	
	ФайлыЗаявления = Новый Массив;
	
	ДокументЗаявление 	= ВходящийКонтекст.ДокументЗаявление;
	ВидыДокументов 		= ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоВходящемуКонтексту(ВходящийКонтекст);
	
	ИдентификаторыМЧД = ОбработкаЗаявленийАбонентаКлиентСервер.СоответствиеИДиМЧД(ДокументЗаявление);
	
	Для Каждого ЭлектронныйДокумент Из ДокументЗаявление.ЭлектронныеДокументы Цикл
		
		ЕстьОшибка = 
			НЕ (ЗначениеЗаполнено(ЭлектронныйДокумент.Документ)
			И ВидыДокументов.Свойство(ЭлектронныйДокумент.Документ))
			И НЕ ЭтоДокументыДляДЛУЦ(ДокументЗаявление, ЭлектронныйДокумент.Документ);
		
		Если ЕстьОшибка Тогда
			Продолжить;
		КонецЕсли;
		
		ДанныеФайла = РаботаСФайламиКлиент.ДанныеФайла(ЭлектронныйДокумент.Файл, УникальныйИдентификатор, Истина);
		
		ВидДокумента = ОбработкаЗаявленийАбонентаКлиентСервер.ВидДокументаЗаявленияСкорректированный(ЭлектронныйДокумент);
		
		НовыйСкан = ШаблонФайлаЗаявления(ДокументЗаявление.ПодписатьЭП);
		НовыйСкан.Вставить("ВидДокумента", 		 ВидДокумента);
		НовыйСкан.Вставить("Наименование", 		 ДанныеФайла.Наименование);
		НовыйСкан.Вставить("АдресДанных", 		 ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
		НовыйСкан.Вставить("Расширение", 		 ДанныеФайла.Расширение);
		НовыйСкан.Вставить("ПрисоединенныйФайл", ЭлектронныйДокумент.Файл);
		
		СформироватьИОтправить_ДополнитьДаннымиМЧД(
			НовыйСкан, 
			ЭлектронныйДокумент, 
			ИдентификаторыМЧД);
		
		ФайлыЗаявления.Добавить(НовыйСкан);
		
	КонецЦикла;
	
	Возврат ФайлыЗаявления;
	
КонецФункции

&НаКлиенте
Функция ЭтоДокументыДляДЛУЦ(ДокументЗаявление, Документ)
	
	Возврат ДокументЗаявление.ЭтоУдаленноеПереизданиеСертификата
		И (Документ = "ЗаявлениеНаИзготовлениеСертификата" ИЛИ Документ = "ЗаявлениеНаОтзывСертификата");
	
КонецФункции

&НаКлиенте
Процедура СформироватьИОтправить_ДополнитьДаннымиМЧД(
		НовыйСкан, 
		ЭлектронныйДокумент,
		ИдентификаторыМЧД)
	
	Если ЗначениеЗаполнено(ЭлектронныйДокумент.МЧД) Тогда
		
		ИД_МЧД = ИдентификаторыМЧД.Получить(ЭлектронныйДокумент.МЧД);
		
		Если ЭлектронныйДокумент.ЭтоМЧД Тогда
			НовыйСкан.Вставить("идентификаторДокумента", ИД_МЧД);
		КонецЕсли;
		
		Если ЭлектронныйДокумент.ЭтоПодписьКМЧД Тогда
			НовыйСкан.Вставить("идентификаторДокументаРодителя", ИД_МЧД);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция ШаблонФайлаЗаявления(ПодписатьЭП)
	
	Файл = Новый Структура;
	Файл.Вставить("ВидДокумента", 		 "");
	Файл.Вставить("Наименование", 		 "");
	Файл.Вставить("АдресДанных", 		 "");
	Файл.Вставить("ИмяФайлаДанных", 	 СгенерироватьИмяФайлаДокументаВСоставеПакета());
	Файл.Вставить("Расширение", 		 "");
	Файл.Вставить("ПрисоединенныйФайл",  Неопределено);
	Файл.Вставить("идентификаторДокумента", ОбщегоНазначенияЭДКОКлиентСервер.НовыйИдентификатор());
	Файл.Вставить("идентификаторДокументаРодителя", "");
	
	Файл.Вставить("АдресПодписи", "");
	Если ПодписатьЭП Тогда
		Файл.Вставить("ИмяФайлаПодписи", СгенерироватьИмяФайлаДокументаВСоставеПакета());
	Иначе
		Файл.Вставить("ИмяФайлаПодписи", "");
	КонецЕсли;
		
	Возврат Файл;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСертификатИзЗаявленияНаСервере(ДокументЗаявление)
	
	Если ДокументЗаявление.ТипЗаявления = Перечисления.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное Тогда
		Возврат ДокументЗаявление.РеквизитыСертификата.Получить();
	Иначе
		Возврат ДокументЗаявление.Сертификат.Получить();
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ПрисоединитьФайлЗаявления(Заявление, АдресФайлаВоВременномХранилище, ИмяДокумента, РасширениеБезТочки = "xml")
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПрисоединитьФайлЗаявления(Заявление, АдресФайлаВоВременномХранилище, ИмяДокумента, РасширениеБезТочки);

КонецФункции

&НаКлиенте
Процедура ЗаявлениеЗашифроватьПослеШифрованияФайлаЗаявления(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Если Результат.Выполнено Тогда
		
		Если ДополнительныеПараметры.Свойство("СканыКЗаявлению")
			И (ДополнительныеПараметры.ЭтоЗаявлениеНаИзменение ИЛИ ДополнительныеПараметры.ЭтоЗаявлениеНаПодключение) Тогда
			
			ДополнительныеПараметры.Вставить("ИндексТекущегоСкана", 0);
			ЗаявлениеЗашифровать_ЗашифроватьСканыКЗаявлениюВЦикле(ДополнительныеПараметры);
				
		Иначе
			СформироватьИОтправитьФайлыЗаявления(Результат, ДополнительныеПараметры);
		КонецЕсли;
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		НастройкиПользователя = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
		Если ТипЗнч(НастройкиПользователя) <> Тип("Структура") Тогда
			Результат = Новый Структура("ТекстОшибки", НСтр("ru = 'Не удалось определить параметры для шифрования.'"));
		КонецЕсли;	
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
		
	Иначе
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеЗашифровать_ЗашифроватьСканыКЗаявлениюВЦикле(ВходящийКонтекст) Экспорт
	
	ИндексТекущегоСкана = ВходящийКонтекст.ИндексТекущегоСкана;
	СканыКЗаявлению 	= ВходящийКонтекст.СканыКЗаявлению;
	
	Если ИндексТекущегоСкана = СканыКЗаявлению.Количество() Тогда
		
		Результат = Новый Структура("Выполнено", Истина);
		СформироватьИОтправитьФайлыЗаявления(Результат, ВходящийКонтекст);
	
	Иначе
		
		ТекущийСкан = СканыКЗаявлению[ИндексТекущегоСкана];
		
		АдресРезультата = ПоместитьВоВременноеХранилище(, Новый УникальныйИдентификатор);
			
		ОписаниеОповещения = Новый ОписаниеОповещения("ЗаявлениеЗашифровать_ПослеШифрованияСледующегоСкана", ЭтотОбъект, ВходящийКонтекст);
		
		ОписаниеСкана    = Новый Структура("Адрес,Имя", ТекущийСкан.АдресДанных, "file");
		ЗначениеВМассиве = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОписаниеСкана);
		
		ТекущийСкан.АдресДанных = ОперацииСФайламиЭДКОВызовСервера.УпаковатьФайлы(
			ЗначениеВМассиве, 
			ТекущийСкан.АдресДанных);
			
		МассивСертификатов = Новый Массив;
		МассивСертификатов.Добавить(ВходящийКонтекст.СвойстваСертификатаОператораСвязи);

		// Шифруем файл заявления.
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
			ОписаниеОповещения, 
			ТекущийСкан.АдресДанных, 
			МассивСертификатов,
			ТекущийСкан.АдресДанных,
			ВходящийКонтекст.ВыводитьСообщения);

	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеЗашифровать_ПослеШифрованияСледующегоСкана(Результат, ВходящийКонтекст) Экспорт

	Если Результат.Выполнено Тогда
		
		ВходящийКонтекст.ИндексТекущегоСкана = ВходящийКонтекст.ИндексТекущегоСкана + 1;
		ЗаявлениеЗашифровать_ЗашифроватьСканыКЗаявлениюВЦикле(ВходящийКонтекст);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Результат);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция РеквизитСертификатаДляПодписания(Сертификат2, КлючСертификатаДляПодписания)
	
	ВладелецСертификат = ДокументооборотСКОКлиентСервер.ВладелецСертификат(Сертификат2);
	Возврат РеквизитСертификата(ВладелецСертификат, КлючСертификатаДляПодписания);
	
КонецФункции

&НаКлиенте
Функция РеквизитНовогоСертификата(Сертификат1, КлючНовогоСертификат)
	
	Если Сертификат1.НайтиПоЗначению(КлючНовогоСертификат) <> Неопределено Тогда
		Возврат Сертификат1.НайтиПоЗначению(КлючНовогоСертификат).Представление;
	Иначе
		Возврат "";
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Функция ДобавитьСтрокуВТаблицуСравнения(
		Таблица,
		Представление,
		Сертификат1,
		Имя1,
		Сертификат2,
		Имя2)
		
	Значение1 = РеквизитНовогоСертификата(Сертификат1, Имя1);
	Значение2 = РеквизитСертификатаДляПодписания(Сертификат2, Имя2);
		
	НоваяСтрока = ДобавитьСтрокуВТаблицуСравненияПоЗначениям(
		Таблица,
		Представление,
		Значение1,
		Значение2);
	
	Возврат НоваяСтрока;
	
КонецФункции

&НаКлиенте
Функция ДобавитьСтрокуВТаблицуСравненияПоЗначениям(
		Таблица,
		Представление,
		Значение1,
		Значение2)
	
	НоваяСтрока = Таблица.Добавить();
	
	НоваяСтрока.Наименование        = Представление;
	НоваяСтрока.НовыйСертификат     = Значение1;
	НоваяСтрока.СтороннийСертификат = Значение2;
	
	Совпадают = ДокументооборотСКОКлиентСервер.ЗначенияСовпадают(
		НоваяСтрока.НовыйСертификат, 
		НоваяСтрока.СтороннийСертификат);
	
	НоваяСтрока.Различается = НЕ Совпадают;
	
	Возврат НоваяСтрока;
	
КонецФункции

&НаКлиенте
Процедура СформироватьИОтправитьЗаявление_ПослеПолученияНастроекШифрования(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ДокументЗаявление = ВходящийКонтекст.ДокументЗаявление;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	МодельХраненияКлюча = КриптографияЭДКОКлиентСервер.МодельХраненияЗакрытогоКлюча(МестоХраненияКлюча);
	НастройкиПользователя = ПодготовитьДанныеСервераШифрования(РезультатВыполнения, 
							ОбработкаЗаявленийАбонентаКлиентСервер.ПолучитьПараметрПодключения(ДокументЗаявление, "ИдентификаторСервера"));
	
	Если НастройкиПользователя <> Неопределено Тогда
		МестоХраненияКлюча = КриптографияЭДКОКлиентСервер.СвойстваМестаХраненияКлюча(МодельХраненияКлюча, НастройкиПользователя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ВходящийКонтекст);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СформироватьИОтправитьЗаявление_ПослеПолученияВременногоКаталога", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		ОперацииСФайламиЭДКОКлиент.СоздатьКаталогНаКлиенте(ОписаниеОповещения);
	Иначе
		Результат = Новый Структура("ТекстОшибки", НСтр("ru = 'Не удалось определить параметры для шифрования.'"));
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Результат);
	КонецЕсли;	

КонецПроцедуры	

&НаСервереБезКонтекста
Функция ПодготовитьДанныеСервераШифрования(РезультатВыполнения, ОблачнаяПодписьИдентификаторСервера)
	
	Результат = Неопределено;
	
	Если НЕ КриптографияЭДКО.ИспользованиеОблачнойПодписиВозможно() Тогда
		Возврат Результат;
	КонецЕсли;
	
	ЛогинШифрования = "UserEncryption"; // значение по-умолчанию
	ПарольШифрования = "password"; // значение по-умолчанию

	ИщемИдентификатор = ОблачнаяПодписьИдентификаторСервера;
	МассивСерверов = Неопределено;
	НашлиСервер = Неопределено;
	
	Если РезультатВыполнения = Неопределено Тогда
		ПараметрыФункции = Новый Структура();
		ПараметрыФункции.Вставить("СпецоператорСвязи", Перечисления.СпецоператорыСвязи.КалугаАстрал); 
		ПараметрыФункции.Вставить("ВнутреннийИдентификатор", ИщемИдентификатор); 
		МассивСерверов = ДокументооборотСКО.ПолучитьОбработкуЭДО().ОбменССерверомСписокСерверовОблачнойПодписи(ПараметрыФункции);
	ИначеЕсли РезультатВыполнения.Статус = "Выполнено" Тогда
		МассивСерверов = ПолучитьИзВременногоХранилища(РезультатВыполнения.АдресРезультата);
	КонецЕсли;
	
	Если МассивСерверов <> Неопределено Тогда
		Для Каждого СтрокаМассива Из МассивСерверов Цикл
			Если СтрокаМассива.ВнутреннийИдентификатор = ИщемИдентификатор Тогда
				Если ЗначениеЗаполнено(СтрокаМассива.Ссылка) Тогда
					НашлиСервер = СтрокаМассива.Ссылка;
				Иначе
					НашлиСервер = СтрокаМассива;
					ЛогинШифрования = НашлиСервер.ПользовательШифрования;
					ПарольШифрования = НашлиСервер.ПарольПользовательШифрования;
					Прервать;
				КонецЕсли;	
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если НашлиСервер <> Неопределено Тогда
		НастройкиПользователя = Неопределено;
		
		МодульСервисКриптографииDSSПодтверждениеСервер = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSSПодтверждениеСервер");
		МодульСервисКриптографииDSSКлиентСервер = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSSКлиентСервер");
		ИмяПеречисления = "СпособыАвторизацииDSS";
		СпособыАвторизацииDSS = Перечисления[ИмяПеречисления];
		
		МодульСервисКриптографииDSSПодтверждениеСервер.ДополнитьНастройкиПользователя(НастройкиПользователя, НашлиСервер, ЛогинШифрования);
		НастройкиПользователя.Пароль = МодульСервисКриптографииDSSКлиентСервер.ОбъектПароля(ПарольШифрования);
		НастройкиПользователя.ПервичнаяАутентификация = СпособыАвторизацииDSS.Первичный_УчетныеДанные;
		
		Результат = НастройкиПользователя;
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура СформироватьИОтправить_ПодписатьСканыКЗаявлениюПакетно(ВходящийКонтекст) Экспорт
	
	Сертификат = ВходящийКонтекст.Сертификат;
	СканыКЗаявлению = ВходящийКонтекст.СканыКЗаявлению;
	ДокументЗаявление = ВходящийКонтекст.ДокументЗаявление;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	МестоХраненияКлюча	= КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	
	ПараметрыЦикла = НовыЙ Структура();
	ПараметрыЦикла.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ПараметрыЦикла.Вставить("ДокументЗаявление", ДокументЗаявление);
	ПараметрыЦикла.Вставить("СканыКЗаявлению", СканыКЗаявлению);
	ПараметрыЦикла.Вставить("ПараметрыФункции", ВходящийКонтекст);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
			"СформироватьИОтправить_ПодписатьСканыКЗаявлениюПакетноПослеВыполнения", ЭтотОбъект, ПараметрыЦикла);
	
	МассивДокументов = Новый Массив;
	Для Счетчик = 0 По СканыКЗаявлению.ВГраница() Цикл
		ТекущийСкан = СканыКЗаявлению[Счетчик];
		НовыйДокументПакетнойПодписи(
			МассивДокументов,
			ТекущийСкан.АдресДанных,
			ТекущийСкан.Наименование,
			ТекущийСкан.Расширение,
			Счетчик);
	КонецЦикла;

	ПараметрыОперации = Новый Структура("ПодтверждатьОперацию", Ложь);
	ПакетнаяПодписьОблачнойПодписи(ОписаниеОповещения, МестоХраненияКлюча, МассивДокументов, Сертификат, ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьИОтправить_ПодписатьСканыКЗаявлениюПакетноПослеВыполнения(РезультатВыполнения, ВходящийКонтекст) Экспорт

	СканыКЗаявлению = ВходящийКонтекст.СканыКЗаявлению;
	ДокументЗаявление = ВходящийКонтекст.ДокументЗаявление;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ПараметрыФункции = ВходящийКонтекст.ПараметрыФункции;
	
	Если РезультатВыполнения.Выполнено Тогда
		Для Счетчик = 0 По РезультатВыполнения.Результат.ВГраница() Цикл
			ТекущийСкан = СканыКЗаявлению[Счетчик];
			Подпись = РезультатВыполнения.Результат[Счетчик];
			ДобавитьПодписьКФайлуЗаявления(Подпись, ТекущийСкан.ПрисоединенныйФайл, ВходящийКонтекст.ДокументЗаявление.Ссылка);
		КонецЦикла;
		
		ЗаявлениеЗашифровать(ПараметрыФункции);
		
	Иначе
		ОтрицательныйОтвет = Новый Структура;
		ОтрицательныйОтвет.Вставить("Выполнено", Ложь);
		ОтрицательныйОтвет.Вставить("ОписаниеОшибки", РезультатВыполнения.Ошибка);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ОтрицательныйОтвет);
		
	КонецЕсли;
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПодписатьЗаявлениеСтороннимСертификатом_Завершение(Результат, ПараметрыФункции) Экспорт
	
	Если Результат.Выполнено Тогда
		
		СформироватьИОтправить_ПослеПодписанияЗаявления(
			Результат.ПодписанноеСообщение, 
			ПараметрыФункции);
			
	Иначе
		
		СформироватьИОтправитьЗаявление_СообщитьОНевозможностиЭлектронногоПодписания(
			ПараметрыФункции, 
			Результат.ТекстОшибки);
			
	КонецЕсли;
			
КонецПроцедуры

&НаКлиенте
Функция ВыполненоПодписаниеФайлаЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	// Из-за того, что подписание выполняется методом компоненты или платформы, то результат возвращается разный
	ВыполненоПодписаниеПлатформой  = ВходящийКонтекст.ПодписатьПлатформой;
	ВыполненоПодписаниеМетодомБРО = ВходящийКонтекст.ПодписатьМетодомБРО И Результат.Выполнено;
	
	Выполнено = ВыполненоПодписаниеПлатформой ИЛИ ВыполненоПодписаниеМетодомБРО;
	
	Возврат Выполнено;
	
КонецФункции

&НаКлиенте
Функция АдресФайлаПодписиФайлаЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	
	// Из-за того, что подписание выполняется методом компоненты или платформы, то результат возвращается разный
	Если ВходящийКонтекст.ПодписатьМетодомБРО Тогда
		Адрес = Результат.ФайлПодписи;
	Иначе
		Адрес = ПоместитьВоВременноеХранилище(Результат, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	Возврат Адрес;
	
КонецФункции

&НаКлиенте
Процедура ПодписатьСканыСтороннимСертификатом(ПараметрыФункции)
		
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПодписатьСканыСтороннимСертификатом_Завершение", 
		ЭтотОбъект, 
		ПараметрыФункции);
		
	ТекущийСкан = ПараметрыФункции.СканыКЗаявлению[ПараметрыФункции.ИндексТекущегоСкана];
				
	ДокументЗаявление       = ПараметрыФункции.ДокументЗаявление;
	ДвДанныеДляПодписания   = ПолучитьИзВременногоХранилища(ТекущийСкан.АдресДанных);
	ТипКриптопровайдера     = ДокументЗаявление.ТипКриптопровайдера;
	СертификатДляПодписания = ОбработкаЗаявленийАбонентаВызовСервера.СертификатИзЗаявления(ДокументЗаявление);
			
	ОбработкаЗаявленийАбонентаКлиент.ПодписатьЗаявлениеСтороннимСертификатом(
		ЭтотОбъект, 
		ОписаниеОповещения, 
		ДвДанныеДляПодписания, 
		СертификатДляПодписания,
		ТипКриптопровайдера);
		
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСканыСтороннимСертификатом_Завершение(Результат, ПараметрыФункции) Экспорт
	
	Если Результат.Выполнено Тогда
			
		СформироватьИОтправить_ПослеПодписанияСледующегоСкана(
			Результат.ПодписанноеСообщение, 
			ПараметрыФункции);
			
	Иначе
		
		СформироватьИОтправитьЗаявление_СообщитьОНевозможностиЭлектронногоПодписания(
			ПараметрыФункции, 
			Результат.ТекстОшибки);
			
	КонецЕсли;
	
КонецПроцедуры
	
&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеПослеУстановкиКорневогоСертификатаВР(Результат, Контекст) Экспорт
	
	ДокументЗаявление 		  = Контекст.ДокументЗаявление;
	ЭтоЗаявлениеНаПодключение = ДокументЗаявление.ЭтоУпрощенноеЗаявление;
	ЭтоЗаявлениеНаИзменение   = НЕ ЭтоЗаявлениеНаПодключение;
	ИспользоватьСуществующий  = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(ДокументЗаявление);
	
	УчетнаяЗапись = ОбработкаЗаявленийАбонентаКлиентСервер.ПолучитьПараметрПодключения(
		ДокументЗаявление, 
		"УчетнаяЗапись");
		
	МестоХраненияКлюча = КриптографияЭДКОКлиентСервер.ОпределитьМестоХраненияКлюча(
		ДокументЗаявление.МодельХраненияЗакрытогоКлюча,
		УчетнаяЗапись);
	
	СертификатДляПодписания = ОбработкаЗаявленийАбонентаВызовСервера.СертификатИзЗаявления(ДокументЗаявление);
	
	ПодписатьПлатформой = 
		ДокументЗаявление.ПодписатьЭП 
		И ЭтоЛокальнаяПодпись(СертификатДляПодписания)
		И ИспользоватьСуществующий
		И ДокументЗаявление.ЭтоСертификатДругогоУЦ;
	
	ПодписатьМетодомБРО = 
		ДокументЗаявление.ПодписатьЭП
		И НЕ ПодписатьПлатформой;
		
	Контекст.Вставить("ПодписатьМетодомБРО", 			ПодписатьМетодомБРО);
	Контекст.Вставить("ПодписатьПлатформой", 			ПодписатьПлатформой);
	Контекст.Вставить("АлгоритмКонтейнераКлючей", 		Неопределено);
	Контекст.Вставить("ЭтоЗаявлениеНаПодключение", 		ЭтоЗаявлениеНаПодключение);
	Контекст.Вставить("ЭтоЗаявлениеНаИзменение", 		ЭтоЗаявлениеНаИзменение);
	Контекст.Вставить("ВыводитьСообщения", 				Ложь);
	Контекст.Вставить("Статус", 						ДокументЗаявление.Статус);
	Контекст.Вставить("ДатаОтправки", 					ДокументЗаявление.ДатаОтправкиЗаявления);
	Контекст.Вставить("СтатусКомментарий", 				ДокументЗаявление.СтатусКомментарий);
	Контекст.Вставить("ПутьКонтейнерЗакрытогоКлюча",	ДокументЗаявление.ПутьКонтейнерЗакрытогоКлюча);
	Контекст.Вставить("ТекстОшибокДляМастераПодключенияК1СОтчетности",  "");
	Контекст.Вставить("ТекстЗапросаНаСертификат", 		"");
	Контекст.Вставить("ИспользоватьСуществующий", 		ИспользоватьСуществующий);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Контекст);

	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		СформироватьИОтправить_ПодписатьЗаявление(Контекст);
		
	ИначеЕсли ЭтоОблачнаяПодпись(Контекст) 
		И ОбработкаЗаявленийАбонентаКлиентСервер.ПолучитьПараметрПодключения(ДокументЗаявление, "СоздатьНовуюУчетнуюЗапись") Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СформироватьИОтправитьЗаявление_ПослеПолученияНастроекШифрования", 
			ЭтотОбъект, 
			Контекст);
			
		ОткрытьНастройкиОблачнойПодписиФоновымЗаданием(ОписаниеОповещения, Контекст);

	Иначе
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СформироватьИОтправитьЗаявление_ПослеПолученияВременногоКаталога", 
			ЭтотОбъект, 
			Контекст);
			
		ОперацииСФайламиЭДКОКлиент.СоздатьКаталогНаКлиенте(ОписаниеОповещения);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ЗаявлениеОпределитьАлгоритмДляСозданияКонтейнераКлючей(ДанныеОрганизации) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Возврат КриптографияЭДКОКлиентСервер.АлгоритмПоУмолчанию();
	
КонецФункции

&НаСервере
Функция ДатыСертификата(Сертификат)

	// На клиенте их надо привести к местному времени
	
	ДатаС  = Дата(1,1,1);
	ДатаПо = Дата(1,1,1);
	
	Если Сертификат.Свойство("ДействителенС") Тогда
		// Получено нашей компонентой
		ДатаС = Сертификат.ДействителенС;
	ИначеЕсли Сертификат.Свойство("ДатаНачала") Тогда
		// Получено компонентой платформы
		ДатаС = Сертификат.ДатаНачала;
	КонецЕсли;
		
	Если Сертификат.Свойство("ДействителенПо") Тогда
		// Получено нашей компонентой
		ДатаПо = Сертификат.ДействителенПо;
	ИначеЕсли Сертификат.Свойство("ДатаОкончания") Тогда
		// Получено компонентой платформы
		ДатаПо = Сертификат.ДатаОкончания;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ДатаС",  ДатаС);
	ДополнительныеПараметры.Вставить("ДатаПо", ДатаПо);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

#КонецОбласти

&НаСервере
Функция СкачатьФайлВоВременноеХранилище(Знач URL, ПараметрыПолучения = Неопределено, Знач ЗаписыватьОшибку = Истина) Экспорт
	
	Возврат ПолучениеФайловИзИнтернета.СкачатьФайлВоВременноеХранилище(URL, ПараметрыПолучения, ЗаписыватьОшибку);
	
КонецФункции

&НаКлиенте
Процедура УстановитьСертификатВКонтейнерИХранилище(ДополнительныеПараметры)
	
	Сертификат            = ДополнительныеПараметры.Сертификат;
	ПараметрыФункции      = ДополнительныеПараметры.ПараметрыФункции;
	ВыводитьСообщения     = ДополнительныеПараметры.ВыводитьСообщения;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ДанныеСертификата = Новый Структура("Сертификат", Base64Значение(Сертификат.Тело));
	МассивИзОдного = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ДанныеСертификата);
	ИнформацияОСертификате = КриптографияЭДКОСлужебныйВызовСервера.ИзвлечьИнформациюОКриптопровайдереПоСертификату(
		МассивИзОдного,
		Истина);
		
	Если ИнформацияОСертификате.Количество() > 0 Тогда
		СвойстваКриптопровайдера = ИнформацияОСертификате[0].Криптопровайдер;
		
	Иначе
		СвойстваКриптопровайдера = Неопределено;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"УстановитьСертификатВКонтейнерИХранилище_ПослеСозданияМенеджераКриптографии",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если СвойстваКриптопровайдера = Неопределено Тогда
		РезультатВыполнения = Новый Структура("Выполнено, МенеджерКриптографии", Ложь, Неопределено);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВыполнения);
	Иначе
		КриптографияЭДКОКлиент.СоздатьМенеджерКриптографии(ОписаниеОповещения, Ложь, СвойстваКриптопровайдера, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьСертификатВКонтейнерИХранилище_ПослеСозданияМенеджераКриптографии(Результат, ВходящийКонтекст) Экспорт
	
	Сертификат            = ВходящийКонтекст.Сертификат;
	ПараметрыФункции      = ВходящийКонтекст.ПараметрыФункции;
	ВременныйКаталог      = ВходящийКонтекст.ВременныйКаталог;
	ВыводитьСообщения     = ВходящийКонтекст.ВыводитьСообщения;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ВходящийКонтекст.Вставить("УстановкаСертификатСАвтоопределениемКриптопровайдера", НЕ Результат.Выполнено);
	
	Оповещение = Новый ОписаниеОповещения(
		"РегистрацияУстановитьСертификатыУстановкаСертификатовВЦиклеЗавершение", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	КриптографияЭДКОКлиент.УстановитьСертификатВКонтейнерИХранилище(
		Оповещение, 
		ВременныйКаталог + Сертификат.Отпечаток + ".cer", 
		ПараметрыФункции.ПутьКонтейнерЗакрытогоКлюча, 
		ВыводитьСообщения И НЕ ПараметрыФункции.ВызовИзМастераПодключенияК1СОтчетности,
		?(Результат.Выполнено, Результат.МенеджерКриптографии, Неопределено));
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьДокумент_ПоЗаявлениям(Результат, НижнийКолонтитул)
	
	ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетности"));
	Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл 
		Кодировка = ?(нрег(Прав(ФайлОтчетности.ИмяФайла, 4)) = ".xml", "windows-1251", "cp866");
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл, Кодировка);
		ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеБланка(Представление, ФайлОтчетности.ИмяФайла, Тело, НижнийКолонтитул);
		ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПодтверждениеДатыОтправки_ПоЗаявлениям(Результат, НижнийКолонтитул)
	
	СтрПодтвержденияДатыОтправки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеДатыОтправки"));
	Для Каждого СтрПодтверждениеДатыОтправки Из СтрПодтвержденияДатыОтправки Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПодтверждениеДатыОтправки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрПодтверждениеДатыОтправки));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПодтверждениеДатыОтправки.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправки(Представление, СтрПодтверждениеДатыОтправки.ИмяФайла, Тело, НижнийКолонтитул);
		ПараметрыПечати.Отправка = СтрПодтверждениеДатыОтправки.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьРезультатПриема_ПоЗаявлениям(Результат, НижнийКолонтитул)
	
	СтрРезультатыПриема = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатПриема"));
	Для Каждого СтрРезультатПриема Из СтрРезультатыПриема Цикл 
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатПриема.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатПриема));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатПриема.ТранспортноеСообщение);
		
		Если СтрРезультатПриема.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтказеДокумент"] Тогда 
			
			ТабДок = СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриемеНалоговойДекларацииЗаявление(Представление, СтрРезультатПриема.ИмяФайла, Тело, НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТабДок;
			ПараметрыПечати.Отправка = СтрРезультатПриема.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		Иначе
			
			ТаблДок = СформироватьТабличныйДокументНаОсновеКвитанцииОПриемеЗаявление(Представление, СтрРезультатПриема.ИмяФайла, Тело, НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТаблДок;
			ПараметрыПечати.Отправка = СтрРезультатПриема.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьРезультатОбработки_ПоЗаявлениям(Результат, НижнийКолонтитул)
	
	СтрРезультатыОбработки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатОбработки"));
	Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработки Цикл
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатОбработки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатОбработки));
		
		Если СтрРезультатОбработки.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОПроверке"] Тогда 
			
			Представление = "Сообщение о проверке";
			
			ТаблДок = СформироватьТабличныйДокументнаОсновеСообщенияОПроверке(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТаблДок;
			ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		ИначеЕсли СтрРезультатОбработки.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОПростОтметки"] Тогда 
			
			Представление = "Сообщение о проставлении отметки";
			
			ТаблДок = СформироватьТабличныйДокументНаОсновеСообщенияОПростОтметки(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТаблДок;
			ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
        ИначеЕсли СтрРезультатОбработки.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбОтказеОтметки"] Тогда 
			
			Представление = "Уведомление об отказе в проставлении отметки";
			
			ТаблДок = СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриемеНалоговойДекларацииОтметки(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТаблДок;
			ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЕсли;
	КонецЦикла;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьСообщениеОбОтзыве_ПоЗаявлениям(Результат, НижнийКолонтитул)
	
	 СтрРезультатыОбработки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "СообщениеОбОтзыве"));
	Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработки Цикл
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатОбработки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатОбработки));

		Если СтрРезультатОбработки.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОбОтзывеЗаявления"] Тогда 
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатОбработки.ТранспортноеСообщение);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументнаОсновеСообщенияОбОтзыве(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
			ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьСообщениеОНесоответствиях_ПоЗаявлениям(Результат, НижнийКолонтитул)
	
	СтрРезультатыОбработки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "СообщениеОНесоответствиях"));
	Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработки Цикл
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатОбработки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатОбработки));
		
		Если СтрРезультатОбработки.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_СообщениеОНесоответствиях"] Тогда 
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатОбработки.ТранспортноеСообщение);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументнаОсновеСообщенияОНесоответствиях(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
			ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьДокумент_2НДФЛ(Результат, НижнийКолонтитул)
	
	СтрРезультатыОбработки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "СообщениеОНесоответствиях"));
	Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработки Цикл
		
		ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетности"));
		Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл
			Кодировка = ?(нрег(Прав(ФайлОтчетности.ИмяФайла, 4)) = ".xml", "windows-1251", "cp866");
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл, Кодировка);
			ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатОбработки.ТранспортноеСообщение);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеБланка(Представление, ФайлОтчетности.ИмяФайла, Тело, НижнийКолонтитул);
			ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПодтверждениеДатыОтправки_2НДФЛ(Результат, НижнийКолонтитул)
	
	СтрПодтвержденияДатыОтправки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеДатыОтправки"));
	Для Каждого СтрПодтверждениеДатыОтправки ИЗ СтрПодтвержденияДатыОтправки Цикл
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПодтверждениеДатыОтправки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрПодтверждениеДатыОтправки));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПодтверждениеДатыОтправки.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправки(
			Представление, 
			СтрПодтверждениеДатыОтправки.ИмяФайла, 
			Тело, 
			НижнийКолонтитул);
			
		ПараметрыПечати.Отправка = СтрПодтверждениеДатыОтправки.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьРезультатПриема_2НДФЛ(Результат, НижнийКолонтитул)
	
	СтрРезультатыПриема = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатПриема"));
	Для Каждого СтрРезультатПриема Из СтрРезультатыПриема Цикл
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатПриема.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатПриема));
		
		Если СтрРезультатПриема.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Реестр2НДФЛ"] Тогда
			
			Представление = "Реестр сведений 2-НДФЛ";
			
			ТаблДок = СформироватьТабличныйДокументНаОсновеБланка(
				Представление, 
				СтрРезультатПриема.ИмяФайла, 
				Тело, 
				НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТаблДок;
			ПараметрыПечати.Отправка = СтрРезультатПриема.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		ИначеЕсли СтрРезультатПриема.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Протокол2НДФЛ"] Тогда 
			
			Представление = "Протокол приема 2-НДФЛ";
			
			ТаблДок = СформироватьТабличныйДокументНаОсновеБланка(
				Представление, 
				СтрРезультатПриема.ИмяФайла, 
				Тело, 
				НижнийКолонтитул);
			
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = ТаблДок;
			ПараметрыПечати.Отправка = СтрРезультатПриема.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция ПредставлениеТранспортногоСообщнияПоТипу(ТранспортноеСообщение) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПредставлениеТранспортногоСообщнияПоТипу(ТранспортноеСообщение);
	
КонецФункции

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьДокумент_ОтчетПФР(Результат, НижнийКолонтитул, ЦиклОбмена)
	
	ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетностиПФР"));
	Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл
		
		Кодировка = ?(нрег(Прав(ФайлОтчетности.ИмяФайла, 4)) = ".xml", "windows-1251", "cp866");
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл, Кодировка);
		ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
		
		ТипыСообщений = Новый Массив;
		ТипыСообщений.Добавить(
			ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР"));
		ТипыСообщений.Добавить(ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПротоколПФР"));
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок = СформироватьТабличныйДокументНаОсновеБланка(
			Представление,
			ФайлОтчетности.ИмяФайла,
			Тело,
			НижнийКолонтитул,
			ЦиклОбмена,
			ТипыСообщений);
		ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьИзвещениеОбОтказеПФР_ОтчетПФР(Результат, НижнийКолонтитул, ЦиклОбмена)
	
	ПодтвержденияПолученияПФР = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеПолученияПФР"));
	Для Каждого ПодтверждениеПолученияПФР Из ПодтвержденияПолученияПФР Цикл
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ПодтверждениеПолученияПФР.Файл, , Истина);
		ПолучитьЭЦП(Файлы.Индекс(ПодтверждениеПолученияПФР));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(ПодтверждениеПолученияПФР.ТранспортноеСообщение);
		
		ТаблДок = СформироватьТабличныйДокументПФРНаОсновеПодтвержденияОПолучении(Представление, ПодтверждениеПолученияПФР.ИмяФайла, Тело, ЦиклОбмена);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТаблДок;
		ПараметрыПечати.Отправка = ПодтверждениеПолученияПФР.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПротокол_ОтчетПФР(Результат, НижнийКолонтитул, ЦиклОбмена)
	
	Протоколы = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПротоколПФР"));
	Для Каждого СтрПротокол Из Протоколы Цикл
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПротокол.Файл, , Истина);
		ПолучитьЭЦП(Файлы.Индекс(СтрПротокол));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПротокол.ТранспортноеСообщение);
		
		ТаблДок = СформироватьТабличныйДокументПФРНаОсновеПротокола(
			Представление,
			СтрПротокол.ИмяФайла,
			Тело,
			ЦиклОбмена,
			СтрПротокол.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТаблДок;
		ПараметрыПечати.Отправка = СтрПротокол.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция СформироватьТабличныйДокументОтветНаЗапросИОС(Тело, ЦиклОбмена)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзОтветаНаЗапросИОС(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ОтветНаЗапросПФР");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеШапка");
		ОбластьМакета.Параметры.Наименование 		  = ЦиклОбмена.Организация;
		ОбластьМакета.Параметры.ДатаОтправки 		  = СвойстваИзПодтверждения.ДатаВремяОтправки;
		ОбластьМакета.Параметры.ОрганПФР 			  = ЦиклОбмена.ВнешняяОрганизация;
		ОбластьМакета.Параметры.ЯвляетсяПоложительным = СвойстваИзПодтверждения.ЯвляетсяПоложительным;
		
		ТабДокумент.Вывести(ОбластьМакета); // ПодтверждениеШапка
		
		Для Каждого СтрокаИмяФайла Из СвойстваИзПодтверждения.Приложения Цикл
			
			ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеИмяФайла");
			ОбластьМакета.Параметры.ИмяФайла = СтрокаИмяФайла;
			ТабДокумент.Вывести(ОбластьМакета); //ПодтверждениеИмяФайла  
			
		КонецЦикла;
		
		ОбластьМакета = Макет.ПолучитьОбласть("Подвал");
		ТабДокумент.Вывести(ОбластьМакета); // ПодтверждениеПодвал
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументПодтверждениеПолученияПисьмаПФР(Тело, ЦиклОбмена)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияПолученияПисьмаПФР(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ПодтверждениеПолученияПисьмаПФР");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеШапка");
		ОбластьМакета.Параметры.Наименование 		   = ЦиклОбмена.Организация;
		ОбластьМакета.Параметры.ДатаПолучения 		   = СвойстваИзПодтверждения.ДатаПолучения;
		ОбластьМакета.Параметры.ОрганПФР 			   = ЦиклОбмена.ВнешняяОрганизация;
		
		ТабДокумент.Вывести(ОбластьМакета);
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументПодтверждениеПолученияЗапросаПФР(Тело, ЦиклОбмена)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияПолученияЗапросаПФР(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ПодтверждениеПолученияЗапросаПФР");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеШапка");
		ОбластьМакета.Параметры.Наименование 		   = ЦиклОбмена.Организация;
		ОбластьМакета.Параметры.ДатаПолучения 		   = СвойстваИзПодтверждения.ДатаПолучения;
		ОбластьМакета.Параметры.ОрганПФР 			   = ЦиклОбмена.ВнешняяОрганизация;
		ОбластьМакета.Параметры.ИдентификаторДокумента = СвойстваИзПодтверждения.ИдентификаторДокумента;
		
		ТабДокумент.Вывести(ОбластьМакета);
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзПодтвержденияПолученияПисьмаПФР(Тело)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// загружаем XML из строки в дерево
	ДеревоXML = КонтекстЭДОСервер.ЗагрузитьСтрокуXMLВДеревоЗначений(Тело);
	Если ДеревоXML = Неопределено Тогда
		Отказ = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем дату-время получения
	УзлыДатаВремяПолучения = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "датаВремяПолучения", "Э"), Истина);
	Если УзлыДатаВремяПолучения.Количество() = 0 Тогда
		ДатаПолучения = "";
	Иначе
		ДатаПолучения = Формат(ДатаВремяИзСтрокиXML(УзлыДатаВремяПолучения[0].Значение), "ДЛФ=DDT; ДП=-");
	КонецЕсли;

	Возврат Новый Структура("ДатаПолучения", ДатаПолучения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзПодтвержденияПолученияЗапросаПФР(Тело)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// загружаем XML из строки в дерево
	ДеревоXML = КонтекстЭДОСервер.ЗагрузитьСтрокуXMLВДеревоЗначений(Тело);
	Если ДеревоXML = Неопределено Тогда
		Отказ = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем дату-время получения
	УзлыДатаВремяПолучения = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "датаВремяПолучения", "Э"), Истина);
	Если УзлыДатаВремяПолучения.Количество() = 0 Тогда
		ДатаПолучения = "";
	Иначе
		ДатаПолучения = Формат(ДатаВремяИзСтрокиXML(УзлыДатаВремяПолучения[0].Значение), "ДЛФ=DDT; ДП=-");
	КонецЕсли;
	
	// определяем идентификатор документа с запросом
	УзлыИдентификаторДокумента = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "идентификаторДокумента", "Э"), Истина);
	Если УзлыИдентификаторДокумента.Количество() = 0 Тогда
		ИдентификаторДокумента = "";
	Иначе
		ИдентификаторДокумента = УзлыИдентификаторДокумента[0].Значение;
	КонецЕсли;

	Возврат Новый Структура("ДатаПолучения, ИдентификаторДокумента", ДатаПолучения, ИдентификаторДокумента);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзОтветаНаЗапросИОС(Тело)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// загружаем XML из строки в дерево
	ДеревоXML = КонтекстЭДОСервер.ЗагрузитьСтрокуXMLВДеревоЗначений(Тело);
	Если ДеревоXML = Неопределено Тогда
		Отказ = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем дату-время получения
	УзлыДатаВремяОтправки = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "датаВремяОтправки", "Э"), Истина);
	Если УзлыДатаВремяОтправки.Количество() = 0 Тогда
		ДатаВремяОтправки = "";
	Иначе
		ДатаВремяОтправки = Формат(ДатаВремяИзСтрокиXML(УзлыДатаВремяОтправки[0].Значение), "ДЛФ=DDT; ДП=-");
	КонецЕсли;
	
	// получаем признак того, что протокол положительный
	УзлыЗапросОбработанУспешно = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "запросОбработанУспешно", "Э"), Истина);
	Если УзлыЗапросОбработанУспешно.Количество() = 0 Тогда
		ЯвляетсяПоложительным = "";
	Иначе
		ЗначениеСтр = УзлыЗапросОбработанУспешно[0].Значение;
		ЗначениеБулево = XMLЗначение(Тип("Булево"), ЗначениеСтр);
		ЯвляетсяПоложительным = ?(ЗначениеБулево, "Да", "Нет");
	КонецЕсли;
	
	// находим узел "протокол"
	Приложения = Новый Массив;
	УзелОтвет  = ДеревоXML.Строки.Найти("ответ", "Имя");
	Если УзелОтвет <> Неопределено Тогда
	
		// находим информацию о приложениях в XML
		УзлыСписокПриложений = УзелОтвет.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "списокПриложений", "Э"));
		Если УзлыСписокПриложений.Количество() > 0 Тогда
			
			УзелСписокПриложений = УзлыСписокПриложений[0];
			
			// перебираем приложения
			УзлыПриложение = УзелСписокПриложений.Строки.НайтиСтроки(Новый Структура("Имя", "приложение"));
			Если УзлыПриложение.Количество() > 0 Тогда
				
				Для Каждого УзелПриложение Из УзлыПриложение Цикл
					
					// находим подчиненный узел имяФайла
					УзелИмяФайла = УзелПриложение.Строки.Найти("имяФайла");
					Если УзелИмяФайла <> Неопределено Тогда
						Приложения.Добавить(УзелИмяФайла.Значение);
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Новый Структура("ЯвляетсяПоложительным, Приложения, ДатаВремяОтправки", ЯвляетсяПоложительным, Приложения, ДатаВремяОтправки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДатаВремяИзСтрокиXML(ЗначениеСтр)
	
	Попытка
		Возврат XMLЗначение(Тип("Дата"), ЗначениеСтр);
	Исключение
		Возврат '00010101';
	КонецПопытки;
	
КонецФункции

&НаКлиенте
Процедура СформироватьПечатныеДокументыИОС(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	
	Если Настройки.ПечататьОтветНаЗапросИОС Тогда
		ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ОтветНаЗапросИОС"));
		Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл);
			
			ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументОтветНаЗапросИОС(Тело, ЦиклОбмена);
			ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;
	
	Если Настройки.ПечататьПодтвержденияПолученияЗапросаПФР Тогда
		ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеПолученияЗапросаПФР"));
		Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл);
			
			ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументПодтверждениеПолученияЗапросаПФР(Тело, ЦиклОбмена);
			ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;
		
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПечатныеДокументыПисьмаПФР(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	
	Если Настройки.ПодтверждениеПолученияПисьмаПФР Тогда
		ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеПолученияПисьмаПФР"));
		Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл);
			
			ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументПодтверждениеПолученияПисьмаПФР(Тело, ЦиклОбмена);
			ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
			
		КонецЦикла;
	КонецЕсли;	
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
КонецПроцедуры

&НаСервере
Функция СформироватьТабличныйДокументПодтверждениеПолученияФСГС(Тело, ЦиклОбмена)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияПолученияФСГС(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ПодтвержденияПолученияФСГС");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ФСГСПодтверждение");
		ОбластьМакета.Параметры.Заполнить(СвойстваИзПодтверждения);
		
		ТабДокумент.Вывести(ОбластьМакета);
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзПодтвержденияПолученияФСГС(Тело)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// загружаем XML из строки в дерево
	ДеревоXML = КонтекстЭДОСервер.ЗагрузитьСтрокуXMLВДеревоЗначений(Тело);
	Если ДеревоXML = Неопределено Тогда
		Отказ = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
	УзелИзвещение = ДеревоXML.Строки.Найти("извещение", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелИзвещение) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML извещения: не обнаружен узел ""Извещение"".'");
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		Отказ = Истина;
		Возврат  Неопределено;
	КонецЕсли;
	
	УзелОтправитель = УзелИзвещение.Строки.Найти("отправитель", "Имя", Истина);
	Если ЗначениеЗаполнено(УзелОтправитель) Тогда
		ИдентификаторОтправителя = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелОтправитель, "идентификатор");
		НатуральныйИдентификаторОтправителя  = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелОтправитель, "натуральныйИдентификатор");
		ТипОтправителя = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелОтправитель, "тип");
		НазваниеОтправителя = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелОтправитель, "название");
	Иначе
		ИдентификаторОтправителя = "";
		НатуральныйИдентификаторОтправителя  = "";
		ТипОтправителя = "";
		НазваниеОтправителя = "";
	КонецЕсли;
	
	УзелПолучатель = УзелИзвещение.Строки.Найти("получатель", "Имя", Истина);
	Если ЗначениеЗаполнено(УзелПолучатель) Тогда
		ИдентификаторПолучатель = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелПолучатель, "идентификатор");
		НатуральныйИдентификаторПолучатель  = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелПолучатель, "натуральныйИдентификатор");
		ТипПолучатель = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелПолучатель, "тип");
		НазваниеПолучатель = ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелПолучатель, "название");
	Иначе
		ИдентификаторПолучатель = "";
		НатуральныйИдентификаторПолучатель  = "";
		ТипПолучатель = "";
		НазваниеПолучатель = "";
	КонецЕсли;	

	УзелДокументы = УзелИзвещение.Строки.Найти("документы", "Имя", Истина);
	Если НЕ ЗначениеЗаполнено(УзелДокументы) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML извещения: не обнаружен узел ""Документы"".'");
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		Отказ = Истина;
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем идентификаторы поступивших файлов
	ИдентификаторыДокументов = "";
	Для каждого УзелДокумент Из УзелДокументы.Строки Цикл
		ИдентификаторДокумента = УзелДокумент.Строки.Найти("идентификаторДокумента", "Имя");
		Если ЗначениеЗаполнено(ИдентификаторДокумента) Тогда
			ИдентификаторыДокументов = ИдентификаторыДокументов + ?(ПустаяСтрока(ИдентификаторыДокументов), "", ",") + СокрЛП(ИдентификаторДокумента.Значение);
		КонецЕсли;
	КонецЦикла;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ИдентификаторыДокументов", 	ИдентификаторыДокументов);
	ДополнительныеПараметры.Вставить("ИдентификаторОтправителя", 	ИдентификаторОтправителя);
	ДополнительныеПараметры.Вставить("НатуральныйИдентификаторОтправителя", НатуральныйИдентификаторОтправителя);
	ДополнительныеПараметры.Вставить("ТипОтправителя", 				ТипОтправителя);
	ДополнительныеПараметры.Вставить("НазваниеОтправителя", 		НазваниеОтправителя);
	ДополнительныеПараметры.Вставить("ИдентификаторПолучатель", 	ИдентификаторПолучатель);
	ДополнительныеПараметры.Вставить("НатуральныйИдентификаторПолучатель", НатуральныйИдентификаторПолучатель);
	ДополнительныеПараметры.Вставить("ТипПолучатель", 				ТипПолучатель);
	ДополнительныеПараметры.Вставить("НазваниеПолучатель", 			НазваниеПолучатель);

	Возврат ДополнительныеПараметры;

КонецФункции

&НаСервереБезКонтекста
Функция ЗначениУзлаИзПодтвержденияПолученияФСГС(УзелРодитель, ИмяДочернегоУзла)
	
	Строка = УзелРодитель.Строки.Найти(ИмяДочернегоУзла, "Имя");
	Если Строка = Неопределено Тогда
		Результат = "";
	Иначе
		Результат = Строка.Значение;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция ПечатьВозможнаДляЦиклаОбмена(Тип) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПечатьВозможнаДляЦиклаОбмена(Тип); 
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСведенияУзла(УзелРодитель, ИмяУзла, ОбщиеСведения)
	
	УзелСвОтметка = УзелРодитель.Строки.Найти(ИмяУзла, "Имя", Истина);
	Если НЕ ЗначениеЗаполнено(УзелСвОтметка) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ДополнительныеПараметры = Новый Структура();
	  
	Для Каждого УзелОбщСвед Из УзелСвОтметка.Строки Цикл
		ДополнительныеПараметры.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	ОбщиеСведения.Вставить(ИмяУзла, ДополнительныеПараметры);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(ИсходноеЗначение, ЗначениеДляОтправки = Неопределено) Экспорт
	
	Возврат ДокументооборотСКО.ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(ИсходноеЗначение, ЗначениеДляОтправки, Неопределено);
	
КонецФункции

&НаКлиенте
Процедура ОбработатьОбновлениеВЦиклеПослеУстановкиКорневогоСертификатаВР(Результат, ДополнительныеПараметры) Экспорт
	
	ПараметрыФункции      = ДополнительныеПараметры.ПараметрыФункции;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ВыводитьСообщения                          = ДополнительныеПараметры.ВыводитьСообщения;
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПовторятьСоединение                        = ДополнительныеПараметры.ПовторятьСоединение;
	
	ТихийРежим = Неопределено;
	ИнформацияОСервереОнлайнПроверки = Неопределено;
	ДополнительныеПараметры.ПараметрыФункции.Свойство("ТихийРежим", ТихийРежим);
	ДополнительныеПараметры.ПараметрыФункции.Свойство("СтруктураДляИнформацииОСервереОнлайнПроверки", ИнформацияОСервереОнлайнПроверки);
	
	ТихийРежим = ?(ТихийРежим = Неопределено, Ложь, ТихийРежим);
	
	ОбновленыПодписиСертификатовДокументооборота = Ложь;
	Результат = РегистрацияПолучитьДанныеССервера(
		ПараметрыРегистрацияПолучитьДанныеССервера,
		ПовторятьСоединение,
		ОбновленыПодписиСертификатовДокументооборота);
		
	Если ОбновленыПодписиСертификатовДокументооборота Тогда
		ПараметрыПриложения.Вставить("КриптографияЭДКО.ПодписиСертификатовДокументооборота", Неопределено);
	КонецЕсли;
	Если НЕ Результат И ТихийРежим И ИнформацияОСервереОнлайнПроверки = Неопределено Тогда //Не работать при отправке на онлайн проверку
		
		РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Ложь);
		РезультатОбновления.Вставить("ОшибкаПолученияДанныхССервера");
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
		
		Возврат;
		
	ИначеЕсли НЕ Результат И ПовторятьСоединение Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбработатьОбновлениеВЦикле",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу оператора ""Калуга Астрал""'"),
			ОписаниеОповещения);
		
		Возврат;
	КонецЕсли;
	
	ПараметрыАвторизации = Новый Структура;
	Если ЗначениеЗаполнено(ПараметрыФункции.УчетнаяЗапись) Тогда
		ПараметрыАвторизации.Вставить("УчетнаяЗапись", ПараметрыФункции.УчетнаяЗапись);
	Иначе
		ПараметрыАвторизации.Вставить("ИдентификаторДокументооборота", ПараметрыФункции.ИдентификаторДокументооборота);
		ПараметрыАвторизации.Вставить("ТелефонМобильныйДляАвторизации", ПараметрыФункции.ТелефонМобильныйДляАвторизации);
	КонецЕсли;
	
	// служебные переменные
	ЭтоНоваяУчетнаяЗапись = Истина;
	
	СтруктураАвтонастройкиФСС = Неопределено;
	СтруктураАвтонастройкиРПН = Неопределено;
	СтруктураАвтонастройкиФТС = Неопределено;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	ЗаполнитьЗначенияСвойств(ПараметрыФункции, ПараметрыРегистрацияПолучитьДанныеССервера);
	
	ФайлИзКэша = ПараметрыРегистрацияПолучитьДанныеССервера.ФайлИзКэша;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ПараметрыРегистрацияПолучитьДанныеССервера.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
		
	// если не получилось, то прерываемся
	Если НЕ Результат Тогда
		РезультатОбновления = Новый Структура("ПараметрыФункции, РезультатОбновления", ПараметрыФункции, Ложь);
		РезультатОбновления.Вставить("ОшибкаПолученияДанныхССервера");
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатОбновления);
		Возврат;
	Иначе
		Результат = Ложь;
	КонецЕсли;
	
	СерийныйНомер = СерийныйНомерЛичногоСертификата();
	
	ПодписьСервиса  = ЭтоСообщениеЗашифрованоНаОблачныйСертификат(ДанныеУзлаКонфиденциальнаяИнформацияАдрес, СерийныйНомер);
	ОблачнаяПодпись = КриптографияЭДКОСлужебныйВызовСервера.ЭтоСообщениеЗашифрованоОблачнойПодписью(
		ДанныеУзлаКонфиденциальнаяИнформацияАдрес,
		СерийныйНомер);
	
	Если ПодписьСервиса <> Неопределено И ПодписьСервиса Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Истина);
	ИначеЕсли ОблачнаяПодпись <> Неопределено Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ОблачнаяПодпись);
	ИначеЕсли НЕ ФайлИзКэша Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Ложь);
	КонецЕсли;
	
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ПараметрыФункции);

	// создаем на клиенте временный каталог, который необходим для хранения промежуточных файлов
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",                      ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ДанныеУзлаКонфиденциальнаяИнформацияАдрес",  ДанныеУзлаКонфиденциальнаяИнформацияАдрес);
	ДополнительныеПараметры.Вставить("ВыводитьСообщения",                          ВыводитьСообщения);
	ДополнительныеПараметры.Вставить("ПараметрыРегистрацияПолучитьДанныеССервера", ПараметрыРегистрацияПолучитьДанныеССервера);
	ДополнительныеПараметры.Вставить("ПараметрыФункции",                           ПараметрыФункции);
	ДополнительныеПараметры.Вставить("Результат",                                  Результат);
	ДополнительныеПараметры.Вставить("ПараметрыАвторизации",                       ПараметрыАвторизации);
	ДополнительныеПараметры.Вставить("ФайлИзКэша",                                 ФайлИзКэша);
	
	Оповещение = Новый ОписаниеОповещения("ОбработатьОбновлениеВЦиклеПослеПолученияВременногоКаталогаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ОперацииСФайламиЭДКОКлиент.СоздатьКаталогНаКлиенте(Оповещение);
	Иначе
		РезультатОперацииСФайлами = Новый Структура("Выполнено, ДвоичныеДанные, ИмяКаталога", Истина, Неопределено, Неопределено);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатОперацииСФайлами);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПечатныеДокументыПоОтчетностиЦБ(ЦиклОбмена, Настройки, ВыполняемоеОповещение)
	
	ВсеВАрхиве = Неопределено;
	ПолучитьФайлыЦиклаОбмена(ЦиклОбмена, Настройки, ВсеВАрхиве);
	
	Если ВсеВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3, Истина);
		Возврат;
	КонецЕсли;
	
	Результат = Новый СписокЗначений;
	НижнийКолонтитул = печатьПредставлениеЦиклаОбмена(ЦиклОбмена);
	
	// выводим электронное представление отчета
	Если Настройки.ПечататьДокумент Тогда
		ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетностиЦБ"));
		Для каждого ФайлОтчетности Из ФайлыОтчетности Цикл
			Если нрег(ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ФайлОтчетности.ИмяФайла).Расширение) = ".xml" Тогда
				Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл,, Истина);
				ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
				
				Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
				ПараметрыПечати = КонструкторПечатиПротокола();
				ПараметрыПечати.ТабДок = СформироватьТабличныйДокументНаОсновеБланка(
					НСтр("ru = 'Электронное представление'"),
					ФайлОтчетности.ИмяФайла,
					Тело,
					НижнийКолонтитул);
				ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
				
				Результат.Добавить(ПараметрыПечати, Представление);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	// выводим результат загрузки
	Если Настройки.ПечататьПодтверждениеОтправки Тогда
		РезультатыЗагрузки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатЗагрузкиЦБ"));
		РезультатыЗагрузкиПриложения = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатЗагрузкиПриложениеЦБ"));
		Для каждого РезультатЗагрузки Из РезультатыЗагрузки Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(РезультатЗагрузки.Файл,, Истина);
			ТелоПриложения = "";
			Для каждого РезультатЗагрузкиПриложение Из РезультатыЗагрузкиПриложения Цикл
				Если РезультатЗагрузкиПриложение.ТранспортноеСообщение = РезультатЗагрузки.ТранспортноеСообщение Тогда
					ТелоПриложения = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(РезультатЗагрузкиПриложение.Файл,, Истина);
				КонецЕсли;
			КонецЦикла;
			ПолучитьЭЦП(Файлы.Индекс(РезультатЗагрузки));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(РезультатЗагрузки.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок = СформироватьТабличныйДокументНаОсновеКвитанцииЦБ(
				НСтр("ru = 'Результат загрузки'"),
				РезультатЗагрузки.ИмяФайла,
				Тело,
				ЦиклОбмена,
				РезультатЗагрузки.ТранспортноеСообщение,
				ТелоПриложения);
			ПараметрыПечати.Отправка = РезультатЗагрузки.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
		КонецЦикла;
	КонецЕсли;
	
	// выводим результат приема
	Если Настройки.ПечататьРезультатПриема Тогда
		РезультатыПриема = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатПриемаДокументЦБ"));
		РезультатыПриемаПриложения = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатПриемаПриложениеЦБ"));
		Для каждого РезультатПриема Из РезультатыПриема Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(РезультатПриема.Файл,, Истина);
			ТелоПриложения = "";
			Для каждого РезультатПриемаПриложения Из РезультатыПриемаПриложения Цикл
				Если РезультатПриемаПриложения.ТранспортноеСообщение = РезультатПриема.ТранспортноеСообщение Тогда
					ТелоПриложения = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(РезультатПриемаПриложения.Файл,, Истина);
				КонецЕсли;
			КонецЦикла;
			ПолучитьЭЦП(Файлы.Индекс(РезультатПриема));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(РезультатПриема.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок = СформироватьТабличныйДокументНаОсновеКвитанцииЦБ(
				НСтр("ru = 'Результат приема'"),
				РезультатПриема.ИмяФайла,
				Тело,
				ЦиклОбмена,
				РезультатПриема.ТранспортноеСообщение,
				ТелоПриложения);
			ПараметрыПечати.Отправка = РезультатПриема.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
		КонецЦикла;
	КонецЕсли;
	
	// выводим результат обработки
	Если Настройки.ПечататьПротокол Тогда
		Протоколы = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатОбработкиДокументЦБ"));
		Для каждого СтрПротокол Из Протоколы Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПротокол.Файл,, Истина);
			ПолучитьЭЦП(Файлы.Индекс(СтрПротокол));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПротокол.ТранспортноеСообщение);
			ПараметрыПечати = КонструкторПечатиПротокола();
			ПараметрыПечати.ТабДок = СформироватьТабличныйДокументНаОсновеРезультатаОбработкиЦБ(
				НСтр("ru = 'Результат обработки'"),
				СтрПротокол.ИмяФайла,
				Тело,
				ЦиклОбмена,
				СтрПротокол.ТранспортноеСообщение);
			ПараметрыПечати.Отправка = СтрПротокол.ТранспортноеСообщение;
			
			Результат.Добавить(ПараметрыПечати, Представление);
		КонецЦикла;
	КонецЕсли;
	
	// выводим отчет о состоянии
	Если Настройки.ПечататьСостояние Тогда
		ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
		Если ТаблДок <> Неопределено Тогда
			Результат.Добавить(ТаблДок, НСтр("ru = 'Отчет о состоянии'"));
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

Функция ЭтоPDFФайл(АдресФайла, ИмяФайла)
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ПолучитьИзВременногоХранилища(АдресФайла).Записать(ИмяВременногоФайла);
	
	Текст = НовыйЧтениеТекстаНаСервере(ИмяВременногоФайла);
	Строка = Текст.ПрочитатьСтроку();
	Текст.Закрыть();
	
	ЭтоPDF = СтрНайти(ВРег(Строка), "PDF") > 0;
	
	Если ЭтоPDF Тогда
		
		Части = СтрРазделить(ИмяФайла, ".");
		Части[Части.Количество() - 1] = "pdf";
		
		ИмяФайла = СтрСоединить(Части, ".");
		
	КонецЕсли;
	
	Возврат ЭтоPDF;
	
КонецФункции

&НаСервереБезКонтекста
Функция ТекстСообщения(Адрес)

	Возврат Справочники.ДокументыРеализацииПолномочийНалоговыхОрганов.ТекстСообщения(Адрес);

КонецФункции

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетИДоверенностьДляОтправкиПослеПредупреждения2НДФЛ(
		ДополнительныеПараметры) Экспорт
	
	Контекст = ДополнительныеПараметры.Контекст;
	
	ЧастиИмениФайла = СтрРазделить(Контекст.ИмяФайлаВыгрузки, "_");
	КодНалоговогоОргана = ?(ЧастиИмениФайла.Количество() >= 4, ЧастиИмениФайла[3], Неопределено);
	
	Если ЭтоАдресВременногоХранилища(Контекст.ТекстВыгрузки) Тогда
		АдресВыгрузки = Контекст.ТекстВыгрузки;
	Иначе
		ДанныеВыгрузки = ПолучитьДвоичныеДанныеИзСтроки(Контекст.ТекстВыгрузки, "windows-1251");
		АдресВыгрузки = ПоместитьВоВременноеХранилище(ДанныеВыгрузки, Новый УникальныйИдентификатор)
	КонецЕсли;
	
	Вложения = Новый СписокЗначений;
	Вложения.Добавить(АдресВыгрузки, Контекст.ИмяФайлаВыгрузки);
	
	ИнтерфейсыВзаимодействияБРОКлиент.СоздатьПисьмо(
		ЭтаФорма,
		Контекст.Организация,
		"ФНС",
		КодНалоговогоОргана,,
		Вложения);
	
КонецПроцедуры

&НаКлиенте
Функция СерийныйНомерЛичногоСертификата()
	
	Для каждого Сертификат Из Сертификаты Цикл
		Если ЗначениеЗаполнено(Сертификат.СерийныйНомер) Тогда
			Возврат Сертификат.СерийныйНомер;
		КонецЕсли;
	
	КонецЦикла;

	Возврат "";

КонецФункции

&НаКлиенте
Процедура ПоказатьОтветыНаОбращениеСФРКСтрахователю(Обращение) Экспорт
	
	Ответы = ОтветыНаОбращениеСФРКСтрахователю(Обращение);
	
	Если Ответы.Количество() = 0 Тогда
		
		ТекстСообщения = НСтр("ru = 'Ответы отсутствуют'");
		ПоказатьПредупреждение(, ТекстСообщения);;
		Возврат;
		
	КонецЕсли;
		
	Если Ответы.Количество() = 1 Тогда
		
		ПоказатьЗначение(, Ответы[0])
		
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Ответы", Ответы);
	
		ОткрытьФорму(
			"Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.ОтветыНаОбращенияСФР",
			ДополнительныеПараметры,,
			Новый УникальныйИдентификатор);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция СлужебныеДанныеОблачнойПодписи(АдресШифрованногоБлока, УчетнаяЗапись, ОблачныйСертификат)
	
	МодульЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Результат = МодульЭДО.СлужебныеДанныеОблачнойПодписи(АдресШифрованногоБлока, УчетнаяЗапись, ОблачныйСертификат);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПечатнаяФормаРаспискиОблачнойПодписи(ОблачныйСертификат) Экспорт
	
	Результат = Новый ТабличныйДокумент;
	ПолучитьОбластьМакетаСертификат(Неопределено, Результат, Неопределено, Неопределено, ОблачныйСертификат, Неопределено);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПечатнаяФормаПодключенияОблачнойПодписи(ПараметрыЦикла) Экспорт
	
	Результат = Новый ТабличныйДокумент;
	МакетПечати = Обработки.ДокументооборотСКонтролирующимиОрганами.ПолучитьМакет("ПодключениеУчетнойЗаписиОблачнойПодписи");
	ТекстЗаголовка = НСтр("ru = 'Данные учетной записи электронной подписи для подключения к сервису и подтверждения операций.'");
	
	МакетЗаголовка = МакетПечати.ПолучитьОбласть("Заголовок");
	МакетЗаголовка.Параметры.ТекстЗаголовка = ТекстЗаголовка;
	ЗаполнитьЗначенияСвойств(МакетЗаголовка.Параметры, ПараметрыЦикла);
	Результат.Вывести(МакетЗаголовка);
	
	Если ПараметрыЦикла.Свойство("Пароль") Тогда
		МакетШапки = МакетПечати.ПолучитьОбласть("Логин");
		ЗаполнитьЗначенияСвойств(МакетШапки.Параметры, ПараметрыЦикла);
		Результат.Вывести(МакетШапки);
	КонецЕсли;
	
	Если ПараметрыЦикла.Свойство("ДанныеКлюча") Тогда
		МакетШапки = МакетПечати.ПолучитьОбласть("Ключ");
		КартинкаКода = МакетШапки.Рисунки.ВекторАутентификации;
		КартинкаКода.Линия = Новый Линия(ТипЛинииРисункаТабличногоДокумента.НетЛинии);
		КартинкаКода.ЦветФона = Новый Цвет();
		КартинкаКода.Картинка = Новый Картинка(ПараметрыЦикла.ДанныеКлюча);
		Результат.Вывести(МакетШапки);
	КонецЕсли;
	
	Если ПараметрыЦикла.Свойство("Инструкция") Тогда
		МакетШапки = МакетПечати.ПолучитьОбласть("Инструкция");
		ЗаполнитьЗначенияСвойств(МакетШапки.Параметры, ПараметрыЦикла);
		Результат.Вывести(МакетШапки);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодпись(АдресВоВременномХранилище, ДействиеОблачнойПодписи, ДополнительныеПараметры)
	
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	ОтпечатокСертификата = ДополнительныеПараметры.ОтпечатокСертификата;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	УчетнаяЗапись = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
	ДанныеОблачнойПодписи = СлужебныеДанныеОблачнойПодписи(АдресВоВременномХранилище, УчетнаяЗапись, ПараметрыРегистрацияПолучитьДанныеССервера.ОблачныйСертификат);
	Успешно = ДанныеОблачнойПодписи.Выполнено;
	ВсеДействия = СтрРазделить(ДействиеОблачнойПодписи, ",", Ложь);
	Если ВсеДействия.Количество() = 0 Тогда
		Успешно = Истина;
	КонецЕсли;
	
	ПараметрыЦикла = Новый Структура;
	ПараметрыЦикла.Вставить("АдресВоВременномХранилище", АдресВоВременномХранилище);
	ПараметрыЦикла.Вставить("ДополнительныеПараметры", ДополнительныеПараметры);
	
	Если НЕ Успешно Тогда
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						НСтр("ru = 'Некорректный конфиденциальный блок'"));

	Иначе
		ВсеДействия = СтрРазделить(ДействиеОблачнойПодписи, ",", Ложь);
		
		ДействиеАктивироватьПароль = ВсеДействия.Найти("АктивироватьПароль") <> Неопределено;
		ДействиеАктивироватьКлючПриложения = ВсеДействия.Найти("АктивироватьКлючПриложенияИПолучитьКодАктивации") <> Неопределено;
		ДействиеПовторныйКлючПриложения = ВсеДействия.Найти("КлючПриложения") <> Неопределено;
		ДействиеОтправитьРасписку = ВсеДействия.Найти("ОтправитьРасписку") <> Неопределено И ЗначениеЗаполнено(ПараметрыФункции.ЗаявлениеПодключения);
		ДействиеПолучитьКодАктивации = ВсеДействия.Найти("ПолучитьКодАктивации") <> Неопределено;
		ДействиеОбновитьСертификаты = ВсеДействия.Найти("ОбновитьСписокСертификатов") <> Неопределено;
		
		ПараметрыЦикла.Вставить("ЗаявлениеПодключения", ПараметрыФункции.ЗаявлениеПодключения);
		ПараметрыЦикла.Вставить("ОблачныйСертификат", ПараметрыРегистрацияПолучитьДанныеССервера.ОблачныйСертификат);
		ПараметрыЦикла.Вставить("ОтпечатокСертификата", ДанныеОблачнойПодписи.ОтпечатокСертификата);
		ПараметрыЦикла.Вставить("УчетнаяЗапись", УчетнаяЗапись);
		ПараметрыЦикла.Вставить("Логин", ДанныеОблачнойПодписи.Логин);
		ПараметрыЦикла.Вставить("Сервер", ДанныеОблачнойПодписи.Владелец);
		ПараметрыЦикла.Вставить("Пароль", ДанныеОблачнойПодписи.Пароль);
		ПараметрыЦикла.Вставить("ДанныеКлюча", ДанныеОблачнойПодписи.КлючПриложения);
		ПараметрыЦикла.Вставить("ПодписьПриложения", ДанныеОблачнойПодписи.ПодписьПриложения);
		ПараметрыЦикла.Вставить("ПодписьПароля", ДанныеОблачнойПодписи.ПодписьПароля);
		ПараметрыЦикла.Вставить("ПодписьКодаАктивации", ДанныеОблачнойПодписи.ПодписьКодаАктивации);
		ПараметрыЦикла.Вставить("ИдентификаторСубъекта", ДанныеОблачнойПодписи.ИдентификаторСубъекта);
		ПараметрыЦикла.Вставить("ДействиеАктивироватьПароль", ДействиеАктивироватьПароль);
		ПараметрыЦикла.Вставить("ДействиеАктивироватьКлючПриложения", ДействиеАктивироватьКлючПриложения);
		ПараметрыЦикла.Вставить("ДействиеПовторныйКлючПриложения", ДействиеПовторныйКлючПриложения);
		ПараметрыЦикла.Вставить("ДействиеПолучитьКодАктивации", ДействиеПолучитьКодАктивации);
		ПараметрыЦикла.Вставить("ДействиеОтправитьРасписку", ДействиеОтправитьРасписку);
		ПараметрыЦикла.Вставить("ДействиеОбновитьСертификаты", ДействиеОбновитьСертификаты);
		ПараметрыЦикла.Вставить("АдресРасписки", ПоместитьВоВременноеХранилище(Неопределено, УникальныйИдентификатор));
		ПараметрыЦикла.Вставить("ИдентификаторАбонента", ПараметрыФункции.ИдентификаторАбонента);
		ПараметрыЦикла.Вставить("ОтправитьЭПРасписки", Ложь);
		ПараметрыЦикла.Вставить("УстанавливатьСертификат", УстанавливатьСертификат);
		ПараметрыЦикла.Вставить("ВнутреннийИдентификатор", ДанныеОблачнойПодписи.ВнутреннийИдентификатор);
		ПараметрыЦикла.Вставить("ВыводитьСообщения", ВыводитьСообщения);
		
		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПодтверждения", ЭтотОбъект, ПараметрыЦикла);
		ФормаПечатногоДокумента = Неопределено;
		ЗаголовокПодтверждения = "";
		ОписаниеДействия = "";
		ПараметрыОперации = Новый Структура;
							
		Если ПараметрыЦикла.ДействиеАктивироватьПароль И ПараметрыЦикла.ДействиеАктивироватьКлючПриложения Тогда
			ПараметрыДляПечати = Новый Структура("Сервер, Логин, Пароль, ДанныеКлюча, Инструкция");
			ЗаполнитьЗначенияСвойств(ПараметрыДляПечати, ПараметрыЦикла);
			ПараметрыДляПечати.Инструкция = НСтр("ru = 'Необходимо завершить настройки для подключения электронной подписи в программе и мобильного приложения'") + ":" + Символы.ПС 
								+ НСтр("ru = ' - сохраните полученный пароль для подключения'") + Символы.ПС
								+ НСтр("ru = ' - отсканируйте в мобильном приложении полученный QR-код'") + Символы.ПС
								+ НСтр("ru = ' - подтвердите получение пароля и QR-кода'") + Символы.ПС
								+ НСтр("ru = ' - введите в мобильном приложении высланный в SMS код активации'") + Символы.ПС
								+ НСтр("ru = ' - подключитесь к электронной подписи в программе с новым паролем'") + Символы.ПС
								+ НСтр("ru = ' - с помощью мобильного приложения выполните операцию контрольной подписи'");
			ФормаПечатногоДокумента = ПечатнаяФормаПодключенияОблачнойПодписи(ПараметрыДляПечати);
			ЗаголовокПодтверждения = НСтр("ru = 'Подтверждаю получение пароля и QR-кода'");
			ПараметрыОперации.Вставить("ПовторнаяОтправкаКода", Истина);
			ПараметрыОперации.Вставить("ИдентификаторСубъекта", ПараметрыЦикла.ИдентификаторСубъекта);
			ПараметрыОперации.Вставить("ПодписьКодаАктивации", ПараметрыЦикла.ПодписьКодаАктивации);
			
		ИначеЕсли ПараметрыЦикла.ДействиеАктивироватьПароль Тогда
			ПараметрыДляПечати = Новый Структура("Сервер, Логин, Пароль, Инструкция");
			ЗаполнитьЗначенияСвойств(ПараметрыДляПечати, ПараметрыЦикла);
			ПараметрыДляПечати.Инструкция = НСтр("ru = 'Необходимо завершить настройки для подключения к электронной подписи'") + ":" + Символы.ПС 
								+ НСтр("ru = ' - сохраните полученный пароль для подключения'") + Символы.ПС
								+ НСтр("ru = ' - подтвердите получение пароля'") + Символы.ПС
								+ НСтр("ru = ' - с помощью мобильного приложения выполните операцию контрольной подписи'") + Символы.ПС
								+ НСтр("ru = ' - подключитесь к электронной подписи в программе с новым паролем'");
			ФормаПечатногоДокумента = ПечатнаяФормаПодключенияОблачнойПодписи(ПараметрыДляПечати);
			ЗаголовокПодтверждения = НСтр("ru = 'Подтверждаю получение пароля'");
			
		ИначеЕсли ПараметрыЦикла.ДействиеАктивироватьКлючПриложения Тогда
			ПараметрыДляПечати = Новый Структура("Сервер, Логин, ДанныеКлюча, Инструкция");
			ЗаполнитьЗначенияСвойств(ПараметрыДляПечати, ПараметрыЦикла);
			ПараметрыДляПечати.Инструкция = НСтр("ru = 'Необходимо завершить настройки для подключения мобильного приложения'") + ":" + Символы.ПС 
								+ НСтр("ru = ' - отсканируйте в мобильном приложении полученный QR-код'") + Символы.ПС
								+ НСтр("ru = ' - подтвердите получение QR-кода'") + Символы.ПС
								+ НСтр("ru = ' - введите в мобильном приложении высланный в SMS код активации, 
								|   который придет после продолжения'");
			ФормаПечатногоДокумента = ПечатнаяФормаПодключенияОблачнойПодписи(ПараметрыДляПечати);
			ЗаголовокПодтверждения = НСтр("ru = 'Подтверждаю получение QR-кода'");
			ПараметрыОперации.Вставить("ПовторнаяОтправкаКода", Истина);
			ПараметрыОперации.Вставить("ИдентификаторСубъекта", ПараметрыЦикла.ИдентификаторСубъекта);
			ПараметрыОперации.Вставить("ПодписьКодаАктивации", ПараметрыЦикла.ПодписьКодаАктивации);
			
		ИначеЕсли ПараметрыЦикла.ДействиеПовторныйКлючПриложения Тогда
			ПараметрыДляПечати = Новый Структура("Сервер, Логин, ДанныеКлюча, Инструкция");
			ЗаполнитьЗначенияСвойств(ПараметрыДляПечати, ПараметрыЦикла);
			ПараметрыДляПечати.Инструкция = НСтр("ru = 'Необходимо повторно настроить подключение мобильного приложения'") + ":" + Символы.ПС 
								+ НСтр("ru = ' - отсканируйте в мобильном приложении полученный QR-код'") + Символы.ПС
								+ НСтр("ru = ' - подтвердите получение QR-кода'") + Символы.ПС
								+ НСтр("ru = ' - введите в мобильном приложении высланный ранее в SMS код активации'");
			ФормаПечатногоДокумента = ПечатнаяФормаПодключенияОблачнойПодписи(ПараметрыДляПечати);
			ЗаголовокПодтверждения = НСтр("ru = 'Подтверждаю получение QR-кода'");
			ПараметрыОперации.Вставить("ПовторнаяОтправкаКода", Истина);
			ПараметрыОперации.Вставить("ИдентификаторСубъекта", ПараметрыЦикла.ИдентификаторСубъекта);
			ПараметрыОперации.Вставить("ПодписьКодаАктивации", ПараметрыЦикла.ПодписьКодаАктивации);
								
		КонецЕсли;
		
		Если ФормаПечатногоДокумента <> Неопределено Тогда
			ПолучитьПодтверждениеПользователя(ОписаниеСледующее,
						НСтр("ru = 'Параметры подключения к электронной подписи'"),
						"",
						ФормаПечатногоДокумента, 
						ЗаголовокПодтверждения,
						ПараметрыОперации);
		Иначе
			ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПодтверждения(КодВозвратаДиалога.ОК, ПараметрыЦикла);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры	

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПодтверждения(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	Если РезультатВыполнения = КодВозвратаДиалога.ОК Тогда
		ОповещениеОЗавершении = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьАктивироватьКод", ЭтотОбъект, ПараметрыЦикла);
		
		Если ПараметрыЦикла.ДействиеАктивироватьКлючПриложения Тогда
			ДлительнаяОперация = АктивироватьКлючПриложенияУчетнойЗаписиОблачнойПодписиНаСервере(ПараметрыЦикла.ПодписьПриложения, ПараметрыЦикла.ИдентификаторСубъекта);
		
			ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
			ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
			
			ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
		Иначе
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Новый Структура("Выполнено", Истина));
		КонецЕсли;
		
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						НСтр("ru = 'Отказ от получения нового ключа в мобильном приложении.'"));
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьАктивироватьКод(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	Успешно = Ложь;
	Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыполнения, "Статус", "") = "Выполнено" Тогда
		РезультатВыполнения = ПолучитьИзВременногоХранилища(РезультатВыполнения.АдресРезультата);
		Успешно = РезультатВыполнения.Выполнено И РезультатВыполнения.Результат;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыполнения, "Выполнено", Ложь) = Истина Тогда
		Успешно = Истина;
	КонецЕсли;
	
	Если Успешно Тогда
		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьАктивироватьПароль", ЭтотОбъект, ПараметрыЦикла);
		Если ПараметрыЦикла.ДействиеПолучитьКодАктивации Тогда
			ДлительнаяОперация = ОбменССерверомОтправитьКодАктивацииКлючаОблачнойПодписиНаСервере(ПараметрыЦикла.ПодписьКодаАктивации, ПараметрыЦикла.ИдентификаторСубъекта);
								
			ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
			ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;

			ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОписаниеСледующее, ПараметрыОжидания);
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеСледующее, Новый Структура("Выполнено", Истина));
		КонецЕсли;
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						НСтр("ru = 'Ошибка при активации нового ключа мобильного приложения'"));
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьАктивироватьПароль(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	ВыводитьСообщения = ПараметрыЦикла.ВыводитьСообщения;
	Успешно = Ложь;
	Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыполнения, "Статус", "") = "Выполнено" Тогда
		РезультатВыполнения = ПолучитьИзВременногоХранилища(РезультатВыполнения.АдресРезультата);
		Успешно = РезультатВыполнения.Выполнено;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыполнения, "Выполнено", Ложь) = Истина Тогда
		Успешно = Истина;
	КонецЕсли;
	
	Если Успешно Тогда

		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПодписи", ЭтотОбъект, ПараметрыЦикла);
		
		Если ПараметрыЦикла.ДействиеАктивироватьПароль Тогда
			СвойстваСертификата = Новый Структура("Отпечаток", ПараметрыЦикла.ОтпечатокСертификата);
			
			МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
			МодульСервисКриптографииDSSКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиентСервер");
			
			ДанныеДляПодписи = ПолучитьДвоичныеДанныеИзСтроки(ПараметрыЦикла.ПодписьПароля);
			СвойстваПодписи = МодульСервисКриптографииDSSКлиентСервер.ПолучитьСвойствоПодписиCMS(Истина, Ложь);
			МодульСервисКриптографииDSSКлиентСервер.ПолучитьИнформациюДокументаДляПодписи(СвойстваПодписи, НСтр("ru = 'Контрольная подпись'"), "txt");
			
			ПараметрыОперации = Новый Структура;
			ПараметрыОперации.Вставить("ПодтверждатьПринудительно", Истина);
			ПараметрыОперации.Вставить("ОбработкаОшибок", ВыводитьСообщения);

			МодульСервисКриптографииDSSКлиент.Подписать(ОписаниеСледующее, ПараметрыЦикла.УчетнаяЗапись, ДанныеДляПодписи, СвойстваПодписи, СвойстваСертификата, ПараметрыОперации);
			
		Иначе
			ОбработатьОбновлениеАктивироватьОблачнуюПодписьУстановитьСертификат(Новый Структура("Выполнено, Ошибка", Истина, ""), ПараметрыЦикла);
		КонецЕсли;
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						НСтр("ru = 'Ошибка отправки кода авторизации'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПодписи(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	ВыводитьСообщения = ПараметрыЦикла.ВыводитьСообщения;
	Если РезультатВыполнения.Выполнено Тогда
		ОповещениеОЗавершении = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПодтвержениеПароля", ЭтотОбъект, ПараметрыЦикла);
		ДлительнаяОперация = АктивироватьУчетнуюЗаписьОблачнойПодписиНаСервере(
					РезультатВыполнения.Результат, 
					ПараметрыЦикла.ПодписьПароля,
					ПараметрыЦикла.ИдентификаторСубъекта);
		
		ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
		ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
		
		ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
							
	Иначе
		ТекстОшибки = НСтр("ru = 'Ошибка при формировании контрольной подписи'");
		Если ВыводитьСообщения Тогда
			ТекстОшибки = ТекстОшибки + Символы.ПС + Символы.ПС
						+ РезультатВыполнения.Ошибка;
		КонецЕсли;
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла, Ложь, ТекстОшибки);
	КонецЕсли;	
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПередПодтвержениемПароля(УчетнаяЗаписьОблачнойПодписи)
	
	МодульСервисКриптографииDSSКлиентСервер = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSSКлиентСервер");
	МодульСервисКриптографииDSSСлужебный = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSSСлужебный");
	ИдентификаторУчетнойЗаписи = УчетнаяЗаписьОблачнойПодписи.УникальныйИдентификатор();
		
	НовоеЗначение = МодульСервисКриптографииDSSКлиентСервер.ОбъектПароля("", 3);
	МодульСервисКриптографииDSSСлужебный.СохранитьЗакрытыеДанные(СокрЛП(ИдентификаторУчетнойЗаписи), НовоеЗначение, "Пароль");

КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПодтвержениеПароля(ОтветСервиса, ПараметрыЦикла) Экспорт
	
	УчетнаяЗаписьОблачнойПодписи = ПараметрыЦикла.УчетнаяЗапись;
	Успешно = Ложь;
	
	Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ОтветСервиса, "Статус", "") = "Выполнено" Тогда
		РезультатВыполнения = ПолучитьИзВременногоХранилища(ОтветСервиса.АдресРезультата);
		Успешно = РезультатВыполнения.Выполнено И РезультатВыполнения.Результат;
	КонецЕсли;
	
	Если Успешно Тогда
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьПередПодтвержениемПароля(УчетнаяЗаписьОблачнойПодписи);
		МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
		МодульСервисКриптографииDSSСлужебныйВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSСлужебныйВызовСервера");
		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПроверки", ЭтотОбъект, ПараметрыЦикла);
		ПараметрыОперации = Новый Структура();
		ПараметрыОперации.Вставить("Принудительно", Истина);
		ПараметрыОперации.Вставить("ОжидатьВыполнения", Истина);
		ПараметрыОперации.Вставить("ПояснениеОперации", НСтр("ru = 'Подключитесь к сервису электронной подписи в программе с помощью пароля, полученного ранее.'"));
		МодульСервисКриптографииDSSСлужебныйВызовСервера.СброситьПараметрыСеансаУчетнойЗаписи(УчетнаяЗаписьОблачнойПодписи);
		МодульСервисКриптографииDSSКлиент.ПроверкаАутентификацииПользователя(ОписаниеСледующее, УчетнаяЗаписьОблачнойПодписи, ПараметрыОперации);
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Успешно,
						НСтр("ru = 'Ошибка при активировании учетной записи'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПроверки(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	ВыводитьСообщения = ПараметрыЦикла.ВыводитьСообщения;
	
	Если РезультатВыполнения.Выполнено Тогда
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьУстановитьСертификат(РезультатВыполнения, ПараметрыЦикла);
	Иначе
		ТекстОшибки = НСтр("ru = 'Ошибка при проверке подключения'");
		Если ВыводитьСообщения Тогда
			ТекстОшибки = ТекстОшибки + Символы.ПС + Символы.ПС
						+ РезультатВыполнения.Ошибка;
		КонецЕсли;
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						РезультатВыполнения.Выполнено,
						ТекстОшибки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьУстановитьСертификат(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	ОтпечатокСертификата = ПараметрыЦикла.ОтпечатокСертификата;
	УстанавливатьСертификат = ПараметрыЦикла.УстанавливатьСертификат;
	ВнутреннийИдентификатор = ПараметрыЦикла.ВнутреннийИдентификатор;
	
	Если РезультатВыполнения.Выполнено Тогда
		Если УстанавливатьСертификат И НЕ ЗначениеЗаполнено(ВнутреннийИдентификатор) Тогда
			ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПоискаСертификата", ЭтотОбъект, ПараметрыЦикла);
			МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
			МодульСервисКриптографииDSSКлиент.НайтиСертификат(ОписаниеСледующее, Новый Структура("Отпечаток", ОтпечатокСертификата)); 
		Иначе
			ОбработатьОбновлениеАктивироватьОблачнуюПодписьОтправитьРасписку(РезультатВыполнения, ПараметрыЦикла); 
		КонецЕсли;
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						РезультатВыполнения.Выполнено,
						НСтр("ru = 'Ошибка установки сертификата'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеПоискаСертификата(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	УчетнаяЗаписьОблачнойПодписи = ПараметрыЦикла.УчетнаяЗапись;
	ОблачныйСертификат = Base64Значение(ПараметрыЦикла.ОблачныйСертификат);
	ОтпечатокСертификата = ПараметрыЦикла.ОтпечатокСертификата;
	
	Если НЕ РезультатВыполнения.Выполнено Тогда
		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьОтправитьРасписку", ЭтотОбъект, ПараметрыЦикла);
		МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
		МодульСервисКриптографииDSSКлиент.УстановитьСертификат(ОписаниеСледующее, УчетнаяЗаписьОблачнойПодписи, ОблачныйСертификат); 
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьОтправитьРасписку(РезультатВыполнения, ПараметрыЦикла); 
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьОтправитьРасписку(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	Если РезультатВыполнения.Выполнено 
		И ПараметрыЦикла.ДействиеОтправитьРасписку
		И ЗначениеЗаполнено(ПараметрыЦикла.ЗаявлениеПодключения) Тогда
		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПередОтправкойРасписки", ЭтотОбъект, ПараметрыЦикла);
		
		СписокФайлов = Новый Массив;
		СписокФайлов.Добавить(Новый Структура("Идентификатор, Представление, АдресФайла", "Расписка", "Выбрать файл", ПараметрыЦикла.АдресРасписки));
		
		ПараметрыОперации = Новый Структура("ОтправкаЭП", Ложь);
		ПараметрыОперации.Вставить("СписокФайлов", СписокФайлов);
		
		ФормаПечатногоДокумента = ПечатнаяФормаРаспискиОблачнойПодписи(ПараметрыЦикла.ОблачныйСертификат);
		ОписаниеДействия = НСтр("ru = 'Необходимо отправить расписку в получении сертификата:'") + Символы.ПС 
							+ НСтр("ru = ' 1. Распечатайте сертификат, подпишите вручную и отсканируйте его.'") + Символы.ПС
							+ НСтр("ru = ' 2. При отправке нужно будет приложить файл с отсканированной распиской: <Расписка>'");
							
		Если ПараметрыОперации.ОтправкаЭП Тогда
			ОписаниеДействия = ОписаниеДействия + Символы.ПС + НСтр("ru = ' 3. Или отправьте ЭП для полученного сертификата.'");
		КонецЕсли;					
		
		ПолучитьПодтверждениеРасписки(ОписаниеСледующее,
					НСтр("ru = 'Расписка при получении сертификата'"),
					ОписаниеДействия,
					ФормаПечатногоДокумента, 
					"",
					ПараметрыОперации);
		
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						РезультатВыполнения.Выполнено,
						РезультатВыполнения.Ошибка);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПередОтправкойРасписки(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
	ВыводитьСообщения = ПараметрыЦикла.ВыводитьСообщения;
	
	Если ТипЗнч(РезультатВыполнения) = Тип("Структура") Тогда
		КомандаВыбора = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыполнения, "Команда", "");
	Иначе
		КомандаВыбора = РезультатВыполнения;
	КонецЕсли;
	
	Если КомандаВыбора = "ОтправитьБумажнуюРасписку" Тогда
		РезультатВызова = Новый Структура();
		РезультатВызова.Вставить("ИмяФайла", РезультатВыполнения.СписокФайлов[0].Имя);
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьОбработкаРасписки(РезультатВызова, ПараметрыЦикла);
		
	ИначеЕсли КомандаВыбора = "ОтправитьЭПРасписки" Тогда
		ОписаниеСледующее = Новый ОписаниеОповещения("ОбработатьОбновлениеАктивироватьОблачнуюПодписьПолучениеФайлаРасписки", ЭтотОбъект, ПараметрыЦикла);
		ПараметрыЦикла.ОтправитьЭПРасписки = Истина;
		СвойстваСертификата = Новый Структура("Отпечаток", ПараметрыЦикла.ОтпечатокСертификата);
		
		МодульСервисКриптографииDSSКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиентСервер");
		
		ДанныеДляПодписи = Base64Значение(ПараметрыЦикла.ОблачныйСертификат);
		СвойстваПодписи = МодульСервисКриптографииDSSКлиентСервер.ПолучитьСвойствоПодписиCMS(Истина, Ложь);
		МодульСервисКриптографииDSSКлиентСервер.ПолучитьИнформациюДокументаДляПодписи(СвойстваПодписи, НСтр("ru = 'Подпись в получении сертификата'"), "bin");
		
		ПараметрыОперации = Новый Структура;
		ПараметрыОперации.Вставить("ПодтверждатьПринудительно", Истина);
		ПараметрыОперации.Вставить("ОбработкаОшибок", ВыводитьСообщения);

		МодульСервисКриптографииDSSКлиент.Подписать(ОписаниеСледующее, ПараметрыЦикла.УчетнаяЗапись, ДанныеДляПодписи, СвойстваПодписи, СвойстваСертификата, ПараметрыОперации);
		
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						НСтр("ru = 'Отказ от отправки расписки'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПолучениеФайлаРасписки(РезультатВызова, ПараметрыЦикла) Экспорт
	
	Успешно = Ложь;
	Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВызова, "Статус", "") = "Выполнено" Тогда
		РезультатВыполнения = ПолучитьИзВременногоХранилища(РезультатВызова.АдресРезультата);
		Успешно = РезультатВыполнения.Выполнено;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВызова, "Выполнено", Ложь) = Истина Тогда
		РезультатВыполнения = РезультатВызова;
		Успешно = Истина;
	КонецЕсли;
	
	Если Успешно Тогда
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьОбработкаРасписки(РезультатВыполнения.Результат, ПараметрыЦикла);
	Иначе
		ТекстОшибки = НСтр("ru = 'Не указан файл расписки'");
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						ТекстОшибки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьОбработкаРасписки(Результат, ПараметрыЦикла)
	
	ТекстОшибки = "";
	Если ПараметрыЦикла.ОтправитьЭПРасписки Тогда
		РезультатОтправки = ОтправитьРаспискуНаСервере(
						ПараметрыЦикла.ЗаявлениеПодключения,
						ПараметрыЦикла.ИдентификаторАбонента,
						ПоместитьВоВременноеХранилище(Base64Значение(ПараметрыЦикла.ОблачныйСертификат)),
						Base64Значение(ПараметрыЦикла.ОблачныйСертификат),
						Результат);
	Иначе
		РезультатОтправки = ОтправитьРаспискуНаСервере(
						ПараметрыЦикла.ЗаявлениеПодключения,
						ПараметрыЦикла.ИдентификаторАбонента,
						ПараметрыЦикла.АдресРасписки,
						Результат.ИмяФайла);
	КонецЕсли;
	
	Если НЕ РезультатОтправки.Выполнено Тогда
		ТекстОшибки = РезультатОтправки.ОписаниеОшибки;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ТекстОшибки) Тогда
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла,
						Ложь,
						ТекстОшибки);
	Иначе
		ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла, Истина);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьРезультат(ПараметрыЦикла, Результат, ОписаниеОшибки = "") Экспорт
	
	ВыводитьСообщения = ПараметрыЦикла.ВыводитьСообщения;
	
	РезультатВыполнения = Новый Структура("РезультатРасшифровки, АдресВоВременномХранилище", Результат, ПараметрыЦикла.АдресВоВременномХранилище);
	Если НЕ Результат Тогда
		РезультатВыполнения.Вставить("ОшибкаАктивированияУчетнойЗаписиОблачнойПодписи", ОписаниеОшибки);
	КонецЕсли;
	
	ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеВыполнения(РезультатВыполнения, ПараметрыЦикла.ДополнительныеПараметры);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправитьРаспискуНаСервере(ЗаявлениеПодключения, ИдентификаторАбонента, Расписка, ОблачныйСертификатИмяФайла = Неопределено, ПодписьРасписки = Неопределено)
	
	Если НЕ ЗначениеЗаполнено(ЗаявлениеПодключения) Тогда
		Возврат Новый Структура("Выполнено", Истина);
	КонецЕсли;
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов", ЗаявлениеПодключения);
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", Неопределено);
	Если ПодписьРасписки = Неопределено Тогда
		СвойстваФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ОблачныйСертификатИмяФайла);
		ПараметрыФайла.Вставить("ИмяБезРасширения", СвойстваФайла.ИмяБезРасширения);
		ПараметрыФайла.Вставить("РасширениеБезТочки", Сред(СвойстваФайла.Расширение, 2));
	Иначе
		ПараметрыФайла.Вставить("ИмяБезРасширения", "Расписка");
		ПараметрыФайла.Вставить("РасширениеБезТочки", "cer");
	КонецЕсли;
	
	ПрисоединенныйФайл = РаботаСФайлами.ДобавитьФайл(ПараметрыФайла, Расписка);
	
	Если ПодписьРасписки <> Неопределено Тогда
		СвойстваПодписи = Новый Структура;
		СвойстваПодписи.Вставить("Подпись", ПодписьРасписки);
		СвойстваПодписи.Вставить("Сертификат", Base64Значение(ОблачныйСертификатИмяФайла));
		СвойстваПодписи.Вставить("ПодписьВерна", Истина);
		СвойстваПодписи.Вставить("ДатаПроверкиПодписи", ТекущаяДатаСеанса());
		СвойстваПодписи.Вставить("КомуВыданСертификат", "");
		
		РаботаСФайлами.ДобавитьПодписьКФайлу(ПрисоединенныйФайл, СвойстваПодписи);
	КонецЕсли;
	
	ДокументЗаявление = ЗаявлениеПодключения.ПолучитьОбъект();
	СтрокаТаблицы = Неопределено;
	Для Каждого ЭлектронныйДокумент Из ДокументЗаявление.ЭлектронныеДокументы Цикл
		Если ЭлектронныйДокумент.Документ = "Расписка" Тогда
			СтрокаТаблицы = ЭлектронныйДокумент;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокаТаблицы = Неопределено Тогда
		СтрокаТаблицы = ДокументЗаявление.ЭлектронныеДокументы.Добавить();
	КонецЕсли;
	
	СтрокаТаблицы.Документ = "Расписка";
	СтрокаТаблицы.Файл = ПрисоединенныйФайл;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправитьРаспискуНаСервере(ДокументЗаявление, ИдентификаторАбонента);
	
КонецФункции

&НаКлиенте
Процедура ОбработатьОбновлениеАктивироватьОблачнуюПодписьПослеВыполнения(РезультатВыполнения, ДополнительныеПараметры) Экспорт
	
	ОтпечатокСертификата = ДополнительныеПараметры.ОтпечатокСертификата;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ДополнительныеПараметры.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	ВременныйКаталог = ДополнительныеПараметры.ВременныйКаталог;
	ВыводитьСообщения = ДополнительныеПараметры.ВыводитьСообщения;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыРегистрацияПолучитьДанныеССервера = ДополнительныеПараметры.ПараметрыРегистрацияПолучитьДанныеССервера;
	ПараметрыФункции = ДополнительныеПараметры.ПараметрыФункции;
	Результат = ДополнительныеПараметры.Результат;
	РезультатРасшифровки = ДополнительныеПараметры.РезультатРасшифровки;
	РезультатУстановкиСертификатов = ДополнительныеПараметры.РезультатУстановкиСертификатов И РезультатВыполнения.РезультатРасшифровки;
	УстанавливатьСертификат = ДополнительныеПараметры.УстанавливатьСертификат;
	ФайлИзКэша = ДополнительныеПараметры.ФайлИзКэша;
	
	РезультатРасшифровки = РезультатВыполнения.РезультатРасшифровки;
	АдресВоВременномХранилище = РезультатВыполнения.АдресВоВременномХранилище;
	
	ТрансляцияОшибокУстановкиСертификатов = ДополнительныеПараметры.ТрансляцияОшибокУстановкиСертификатов;
	ТрансляцияОшибокРасшифровки = РезультатВыполнения;
	
	ОбработатьОбновлениеПослеКриптографииЗавершение(
		АдресВоВременномХранилище, 
		ВременныйКаталог, 
		ВыводитьСообщения, 
		ВыполняемоеОповещение, 
		ПараметрыРегистрацияПолучитьДанныеССервера, 
		ПараметрыФункции, 
		Результат, 
		РезультатРасшифровки, 
		РезультатУстановкиСертификатов, 
		УстанавливатьСертификат, 
		ФайлИзКэша,
		ТрансляцияОшибокУстановкиСертификатов,
		ТрансляцияОшибокРасшифровки
	);
		
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьФормуПодтверждения(ПараметрыФормы, ОповещениеСледующее, РежимОткрытияОкна) Экспорт
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПодтверждениеПользователяОблачнойПодписи", ПараметрыФормы, , Новый УникальныйИдентификатор(), , , ОповещениеСледующее, РежимОткрытияОкна);
	
КонецПроцедуры

&НаКлиенте
// Формирует форму с текстом описания и получения подтверждения действия от пользователя
//
// Параметры:
//  ОповещениеОЗавершении 	- ОписаниеОповещения - указывает процедуру для получения результата
//     Результат 			- Произвольный - действие пользователя
//	Заголовок				- Строка - заголовок формы
//	ОписаниеПодтверждения	- Строка - заголовок "галочки", если пустая, то поле не отображается
//	ТекстВопроса			- Строка, ФорматированнаяСтрока - содержание описания
//	ПечДокумент				- ТабличныйДокумент - отображается табличный документ, вместо надписи
//	ПараметрыОперации		- Структура
//		* СписокКоманд		- СписокЗначений - список кнопок размещаемых на форме
//		* ТаймАут			- Число - время ожидания в секундах
//		* Редактирование	- Булево - имеет смысл, если передан табличный документ
//		* РежимОткрытияОкна - РежимОткрытияОкнаФормы - режим открытия формы
//	
Процедура ПолучитьПодтверждениеПользователя(
				ОповещениеОЗавершении,
				Заголовок,
				ТекстВопроса = "", 
				ПечДокумент = Неопределено,
				ОписаниеПодтверждения = "",
				ПараметрыОперации = Неопределено) Экспорт
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("Заголовок", Заголовок);
	ПараметрыФормы.Вставить("ЗаголовокПодтверждения", ОписаниеПодтверждения);
	ПараметрыФормы.Вставить("ТекстВопроса", ТекстВопроса);
	
	Если ПечДокумент <> Неопределено Тогда
		ПараметрыФормы.Вставить("ТабДок", ПечДокумент);
		ПараметрыФормы.Вставить("Редактирование", Истина);
	КонецЕсли;
	
	СписокКоманд = Новый СписокЗначений;
	СписокКоманд.Добавить("КодВозвратаДиалога.ОК", НСтр("ru = 'Продолжить'"), Истина);
	СписокКоманд.Добавить("КодВозвратаДиалога.Отмена", НСтр("ru = 'Отмена'"));
	
	ПараметрыФормы.Вставить("СписокКоманд", СписокКоманд);
	
	РежимОткрытияОкна = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыОперации, "РежимОткрытияОкна", РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ПараметрыФормы, ПараметрыОперации, Истина);
	КонецЕсли;	

	ПараметрыЦикла = Новый Структура();
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ОповещениеСледующее = Новый ОписаниеОповещения("ПолучитьПодтверждениеПользователяЗавершение", ЭтотОбъект, ПараметрыЦикла);
	
	ПолучитьФормуПодтверждения(ПараметрыФормы, ОповещениеСледующее, РежимОткрытияОкна);
	
КонецПроцедуры

&НаКлиенте
// Формирует форму с текстом описания и получения подтверждения действия от пользователя
//
// Параметры:
//  ОповещениеОЗавершении 	- ОписаниеОповещения - указывает процедуру для получения результата
//     Результат 			- Произвольный - действие пользователя
//	Заголовок				- Строка - заголовок формы
//	ОписаниеПодтверждения	- Строка - заголовок "галочки", если пустая, то поле не отображается
//	ТекстВопроса			- Строка, ФорматированнаяСтрока - содержание описания
//	ПечДокумент				- ТабличныйДокумент - отображается табличный документ, вместо надписи
//	ПараметрыОперации		- Структура
//		* СписокКоманд		- СписокЗначений - список кнопок размещаемых на форме
//		* ТаймАут			- Число - время ожидания в секундах
//		* Редактирование	- Булево - имеет смысл, если передан табличный документ
//		* РежимОткрытияОкна - РежимОткрытияОкнаФормы - режим открытия формы
//	
Процедура ПолучитьПодтверждениеРасписки(
				ОповещениеОЗавершении,
				Заголовок,
				ТекстВопроса = "", 
				ПечДокумент = Неопределено,
				ОписаниеПодтверждения = "",
				ПараметрыОперации = Неопределено) Экспорт
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("Заголовок", Заголовок);
	ПараметрыФормы.Вставить("ЗаголовокПодтверждения", ОписаниеПодтверждения);
	ПараметрыФормы.Вставить("ТекстВопроса", ТекстВопроса);
	ПараметрыФормы.Вставить("ШиринаТаблицы", 95);
	
	РежимОткрытияОкна = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыОперации, "РежимОткрытияОкна", РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	ОтправкаЭП = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыОперации, "ОтправкаЭП", Ложь);
	
	Если ПечДокумент <> Неопределено Тогда
		ПараметрыФормы.Вставить("ТабДок", ПечДокумент);
		ПараметрыФормы.Вставить("Редактирование", Истина);
	КонецЕсли;
	
	СписокКоманд = Новый СписокЗначений;
	СписокКоманд.Добавить("ОтправитьБумажнуюРасписку", НСтр("ru = 'Отправить скан-копию расписки'"), Истина);
	Если ОтправкаЭП Тогда
		СписокКоманд.Добавить("ОтправитьЭПРасписки", НСтр("ru = 'Отправить с эл. подписью'"));
	КонецЕсли;
	
	ПараметрыФормы.Вставить("СписокКоманд", СписокКоманд);
	
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ПараметрыФормы, ПараметрыОперации, Истина);
	КонецЕсли;	

	ПараметрыЦикла = Новый Структура();
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ОповещениеСледующее = Новый ОписаниеОповещения("ПолучитьПодтверждениеПользователяЗавершение", ЭтотОбъект, ПараметрыЦикла);
	
	ПолучитьФормуПодтверждения(ПараметрыФормы, ОповещениеСледующее, РежимОткрытияОкна);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьПодтверждениеПользователяЗавершение(РезультатВыбора, ПараметрыЦикла) Экспорт
	
	Если РезультатВыбора = Неопределено Тогда
		Результат = КодВозвратаДиалога.Отмена;
	Иначе
		Результат = РезультатВыбора;
	КонецЕсли;
	
	Если ПараметрыЦикла.ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ПараметрыЦикла.ОповещениеОЗавершении, Результат);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияРасшифроватьДанныеПроверитьОблачнуюПодпись(ВходящийКонтекст)
	
	МестоХраненияКлюча = ВходящийКонтекст.МестоХраненияКлюча;
	УчетнаяЗаписьОблачнойПодписи = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
	
	Если ЗначениеЗаполнено(УчетнаяЗаписьОблачнойПодписи) Тогда
		ПараметрыОперации = Новый Структура;
		ПарольПользователя = КриптографияЭДКОКлиентСервер.ПараметрыМестаХраненияКлюча(МестоХраненияКлюча, "Пароль");
		Если ПарольПользователя <> Неопределено Тогда
			ПараметрыОперации.Вставить("ПарольПользователя", ПарольПользователя);
			ПараметрыОперации.Вставить("ПоказатьДиалог", Ложь);
		КонецЕсли;
		ОписаниеСледующее = Новый ОписаниеОповещения("РегистрацияРасшифроватьДанныеПроверитьОблачнуюПодписьЗавершение", ЭтотОбъект, ВходящийКонтекст);
		МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
		МодульСервисКриптографииDSSКлиент.ОбновитьПолитикуПользователя(ОписаниеСледующее, УчетнаяЗаписьОблачнойПодписи, ПараметрыОперации);
	Иначе
		РегистрацияРасшифроватьДанныеПроверитьОблачнуюПодписьЗавершение(Новый Структура("Выполнено", Истина), ВходящийКонтекст);
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияРасшифроватьДанныеПроверитьОблачнуюПодписьЗавершение(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	МестоХраненияКлюча = ВходящийКонтекст.МестоХраненияКлюча;
	ДанныеДляРасшифровки = ВходящийКонтекст.ДанныеДляРасшифровки;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ВходящийКонтекст.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	ПараметрыОперации = ВходящийКонтекст.ПараметрыОперации;
	
	МодульСервисКриптографииDSSASNКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSASNКлиентСервер");
	СвойстваЗашифрования = МодульСервисКриптографииDSSASNКлиентСервер.ПолучитьСертификатыШифрования(ДанныеДляРасшифровки);
	
	ТекущийСертификат = Неопределено;
	Если СвойстваЗашифрования.Количество() > 0 Тогда
		ТекущийСертификат = Новый Структура;
		ТекущийСертификат.Вставить("СерийныйНомер", СвойстваЗашифрования[0].СерийныйНомер);
		ТекущийСертификат.Вставить("Поставщик", СвойстваЗашифрования[0].ОбщееИмя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ТекущийСертификат);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"РегистрацияРасшифроватьДанныеОблачнаяПодпись", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	КриптографияЭДКОКлиент.РасшифроватьPKCS7(Оповещение, ДанныеУзлаКонфиденциальнаяИнформацияАдрес, , ТекущийСертификат, Ложь, , ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияРасшифроватьДанныеОблачнаяПодпись(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ПараметрыОперации = ВходящийКонтекст.ПараметрыОперации;
	МестоХраненияКлюча = ВходящийКонтекст.МестоХраненияКлюча;
	МестоХраненияКлючаНастроек = ВходящийКонтекст.МестоХраненияКлючаНастроек;
	ЭтоОблачнаяПодпись = ЭтоОблачнаяПодпись(МестоХраненияКлюча);
	УчетнаяЗаписьОблачнойПодписи = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
	
	Если НЕ РезультатВыполнения.Выполнено И НЕ ЭтоОблачнаяПодпись И ЭтоОблачнаяПодпись(МестоХраненияКлючаНастроек) Тогда
		ВходящийКонтекст.МестоХраненияКлюча = МестоХраненияКлючаНастроек;
		РегистрацияРасшифроватьДанныеПроверитьОблачнуюПодпись(ВходящийКонтекст);
	ИначеЕсли РезультатВыполнения.Выполнено ИЛИ НЕ ЭтоОблачнаяПодпись Тогда
		РегистрацияРасшифроватьДанныеПослеРасшифрования(РезультатВыполнения, ВходящийКонтекст);
	Иначе
		МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
		МодульСервисКриптографииDSSКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиентСервер");
		
		Если МодульСервисКриптографииDSSКлиентСервер.ЭтоОшибкаОтказа(РезультатВыполнения.ОписаниеОшибки) Тогда
			РегистрацияРасшифроватьДанныеПослеРасшифрования(РезультатВыполнения, ВходящийКонтекст);
		Иначе	
			ПараметрыОперации = Неопределено;
			ПарольПользователя = КриптографияЭДКОКлиентСервер.ПараметрыМестаХраненияКлюча(МестоХраненияКлюча, "Пароль");
			Если ПарольПользователя <> Неопределено Тогда
				ПараметрыОперации = Новый Структура;
				ПараметрыОперации.Вставить("ПарольПользователя", ПарольПользователя);
				ПараметрыОперации.Вставить("ПоказатьДиалог", Ложь);
			КонецЕсли;
			
			Оповещение = Новый ОписаниеОповещения(
				"РегистрацияРасшифроватьДанныеОблачнаяПодписьПослеОбновления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			МодульСервисКриптографииDSSКлиент.ОбновитьДанныеСертификатов(Оповещение, УчетнаяЗаписьОблачнойПодписи, ПараметрыОперации);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РегистрацияРасшифроватьДанныеОблачнаяПодписьПослеОбновления(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ПараметрыОперации = ВходящийКонтекст.ПараметрыОперации;
	Сертификат = ВходящийКонтекст.Сертификат;
	ДанныеДляРасшифровки = ВходящийКонтекст.ДанныеДляРасшифровки;
	ДанныеУзлаКонфиденциальнаяИнформацияАдрес = ВходящийКонтекст.ДанныеУзлаКонфиденциальнаяИнформацияАдрес;
	
	Если НЕ РезультатВыполнения.Выполнено Тогда
		Ответ = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, РезультатВыполнения.Ошибка);
		РегистрацияРасшифроватьДанныеПослеРасшифрования(Ответ, ВходящийКонтекст);
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"РегистрацияРасшифроватьДанныеПослеРасшифрования", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		КриптографияЭДКОКлиент.РасшифроватьPKCS7(Оповещение, ДанныеДляРасшифровки, ДанныеУзлаКонфиденциальнаяИнформацияАдрес, Сертификат, Ложь, , ПараметрыОперации);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификат(ДополнительныеПараметры)
	
	Если ПустаяСтрока(ДополнительныеПараметры.ПутьКонтейнерЗакрытогоКлюча) Тогда
		
		Если НЕ ДополнительныеПараметры.ВызовИзМастераПодключенияК1СОтчетности Тогда 
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗаявлениеСформироватьЗакрытыйКлючПослеИнструкцииЗавершение", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
				
			ПоказатьИнструкциюСозданияКонтейнера(ДополнительныеПараметры.ДокументЗаявление.ТипКриптопровайдера, ОписаниеОповещения);
			
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПродолжение(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПродолжение(ДополнительныеПараметры)
	
	АлгоритмКонтейнераКлючей = ЗаявлениеОпределитьАлгоритмДляСозданияКонтейнераКлючей(ДополнительныеПараметры.ДокументЗаявление);
	Если АлгоритмКонтейнераКлючей = Неопределено Тогда
		СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(ДополнительныеПараметры,
			НСтр("ru = 'Не удалось получить алгоритм генерации ключа.'"));
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("АлгоритмКонтейнераКлючей", АлгоритмКонтейнераКлючей);
	ДополнительныеПараметры.Вставить("НапомнитьОбновитьКриптопровайдер",
		АлгоритмКонтейнераКлючей = "GOST R 34.10-2012-256" ИЛИ АлгоритмКонтейнераКлючей = "GOST R 34.10-2012-512");
	
	Оповещение = Новый ОписаниеОповещения(
		"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеПолученияКриптопровайдеров", ЭтотОбъект, ДополнительныеПараметры);
	
	Если ДополнительныеПараметры.НапомнитьОбновитьКриптопровайдер Тогда
		ДополнительныеПараметры.Вставить("НапомнитьОбновитьКриптопровайдер", Истина);
		
		КриптографияЭДКОКлиент.ПолучитьКриптопровайдеры(Оповещение, Ложь, Ложь);
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено", Ложь);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеПолученияКриптопровайдеров(Результат, ДополнительныеПараметры) Экспорт
	
	Оповещение = Новый ОписаниеОповещения(
		"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеПредупрежденияОбОбновленииКриптопровайдера",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если Результат.Выполнено И ДополнительныеПараметры.НапомнитьОбновитьКриптопровайдер Тогда
		ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 	Оповещение);
		ДополнительныеПараметры.Вставить("Криптопровайдеры", 		Результат.Криптопровайдеры);
		ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимости(ДополнительныеПараметры);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеПредупрежденияОбОбновленииКриптопровайдера(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат Тогда
		СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(ДополнительныеПараметры);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ВыводитьИнформациюОбОшибках", НЕ ДополнительныеПараметры.ВызовИзМастераПодключенияК1СОтчетности);
	ДополнительныеПараметры.Вставить("УникальноеИмяКонтейнера", 	ДополнительныеПараметры.ПутьКонтейнерЗакрытогоКлюча);
	ДополнительныеПараметры.Вставить("МенеджерКриптографииДляЗапросаНаСертификат", 	Неопределено);
	
	Если ЗначениеЗаполнено(ДополнительныеПараметры.ПутьКонтейнерЗакрытогоКлюча) Тогда
		
		Результат = Новый Структура();
		Результат.Вставить("Выполнено", 				Истина);
		Результат.Вставить("Алгоритм", 					ДополнительныеПараметры.АлгоритмКонтейнераКлючей);
		Результат.Вставить("УникальноеИмяКонтейнера", 	ДополнительныеПараметры.ПутьКонтейнерЗакрытогоКлюча);
		
		ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера(Результат, ДополнительныеПараметры);
		
	Иначе
		
		ПутьКонтейнерЗакрытогоКлюча = КриптографияЭДКОКлиент.СгенерироватьИмяКонтейнера(ДополнительныеПараметры.ДокументЗаявление.КраткоеНаименование);
		
		Оповещение = Новый ОписаниеОповещения(
			"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияМенеджера", 
			ЭтотОбъект, 
			ДополнительныеПараметры,
			"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера_ОбработчикОшибки", 
			ЭтотОбъект);
		
		ЭтоLinux = ОбщегоНазначенияЭДКОКлиент.ЭтоКриптопровайдерLinux();
		ПутьМодуляКриптографии = ?(ЭтоLinux,
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПутьМодуляКриптографии(), "");
		ПоддерживаемыеКриптопровайдеры = КриптографияЭДКОКлиентСервер.ПоддерживаемыеКриптопровайдеры(
			ДополнительныеПараметры.АлгоритмКонтейнераКлючей,
			ЭтоLinux,
			ДополнительныеПараметры.ДокументЗаявление.ТипКриптопровайдера,
			ПутьМодуляКриптографии);
		ПараметрыМодуляКриптографии = ?(ПоддерживаемыеКриптопровайдеры.Количество() >= 1,
			ПоддерживаемыеКриптопровайдеры[0], Неопределено);
		
		КриптографияЭДКОКлиент.СоздатьМенеджерКриптографии(Оповещение, Ложь, ПараметрыМодуляКриптографии, Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияМенеджера(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ВходящийКонтекст.АлгоритмКонтейнераКлючей = Результат.Алгоритм;
		
		ПутьКонтейнерЗакрытогоКлюча = КриптографияЭДКОКлиент.СгенерироватьИмяКонтейнера(ВходящийКонтекст.ДокументЗаявление.КраткоеНаименование);
		ВходящийКонтекст.Вставить("МенеджерКриптографииДляЗапросаНаСертификат", Результат.МенеджерКриптографии);
		
		ЭкспортируемыйКлюч = НЕ ОбязателенТокен(ВходящийКонтекст);
		
		Если ВходящийКонтекст.ДокументЗаявление.ВключитьЛицензиюКриптоПро Тогда
			
			Оповещение = Новый ОписаниеОповещения("ЗаявлениеСоздатьКонтейнерИЗапросНаСертификатПослеЗавершения", ЭтотОбъект, ВходящийКонтекст);
			СтрокаПередачиДанных = ОбработкаЗаявленийАбонентаКлиент.ЗаявлениеСформироватьЗапросНаСертификатКвалифицированный(ВходящийКонтекст);
			
			КриптографияЭДКОКлиент.СоздатьКонтейнерИЗапросНаСертификат(
				Оповещение, 
				СтрокаПередачиДанных,
				ПутьКонтейнерЗакрытогоКлюча,
				ВходящийКонтекст.ВыводитьСообщения,
				Результат.МенеджерКриптографии,
				ВходящийКонтекст.АлгоритмКонтейнераКлючей,
				ЭкспортируемыйКлюч);
				
		Иначе
				
			ВходящийКонтекст.Вставить("МенеджерКриптографииДляЗапросаНаСертификат",  Результат.МенеджерКриптографии);
			
			Оповещение = Новый ОписаниеОповещения(
				"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера", 
				ЭтотОбъект, 
				ВходящийКонтекст,
				"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера_ОбработчикОшибки", 
				ЭтотОбъект);
				
			КриптографияЭДКОКлиент.СоздатьКонтейнерКлючей(
				Оповещение, 
				ПутьКонтейнерЗакрытогоКлюча,
				НЕ ВходящийКонтекст.ВызовИзМастераПодключенияК1СОтчетности,
				Результат.МенеджерКриптографии,
				ВходящийКонтекст.АлгоритмКонтейнераКлючей,
				ЭкспортируемыйКлюч);
				
		КонецЕсли;
			
	Иначе
		СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(ВходящийКонтекст);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСоздатьКонтейнерИЗапросНаСертификатПослеЗавершения(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера(Результат, ВходящийКонтекст);
		
	Иначе
		
		СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(
			ВходящийКонтекст, 
			Результат.ОписаниеОшибки);
				
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера_ОбработчикОшибки(ИнформацияОбОшибке, СтандартнаяОбработка, ВходящийКонтекст) Экспорт
	
	СтандартнаяОбработка = Ложь;
	ПодробноеПредставление = КраткоеПредставлениеОшибки(ИнформацияОбОшибке);
	
	СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(
		ВходящийКонтекст, 
		ПодробноеПредставление);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера_ОшибкаСозданияКлюча(
		ВходящийКонтекст,
		ТекстОшибкиПриСозданииКлюча = Неопределено) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ТекстОшибки = ?(ТекстОшибкиПриСозданииКлюча = Неопределено,
		НСтр("ru = 'Криптографическая ошибка: не удалось создать контейнер закрытого ключа.'"), ТекстОшибкиПриСозданииКлюча);
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
		ТекстОшибки, 
		ВходящийКонтекст.ВызовИзМастераПодключенияК1СОтчетности, 
		ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности);
		
	Оповестить(НСтр("ru = 'Упрощенное заявление. Ошибка создание закрытого ключа'"), ТекстОшибки, ВходящийКонтекст.ДокументЗаявление);
		
	СтруктураРезультата = Новый Структура("ПродолжитьОтправкуЗаявления, ТекстОшибки, ПутьКонтейнерЗакрытогоКлюча", 
											Ложь, ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности, "");
											
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, СтруктураРезультата);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		СохраненКорректно = КонтейнерЗакрытогоКлючаСохраненКорректно(
			Результат.УникальноеИмяКонтейнера, 
			ВходящийКонтекст);
		
		Если СохраненКорректно Тогда
			
			АлгоритмКонтейнераКлючей = Результат.Алгоритм;
			
			ВходящийКонтекст.УникальноеИмяКонтейнера = Результат.УникальноеИмяКонтейнера;
			ВходящийКонтекст.АлгоритмКонтейнераКлючей = АлгоритмКонтейнераКлючей;
			
			ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(
				ВходящийКонтекст, 
				"БРО.КонтекстОтправкиЗаявления");
			
			Оповестить(НСтр("ru = 'Упрощенное заявление. Успешное создание закрытого ключа'"), ,ВходящийКонтекст.ДокументЗаявление);
			
			// Чтобы в форме отправки крутился бублик
			ПодключитьОбработчикОжидания("Подключаемый_ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера", 0.1, Истина);
			
		Иначе
			
			Шаблон = НСтр("ru = 'При получении сертификата в %1 закрытый ключ должен быть сохранен на токен.'");
			Текст = СтрШаблон(Шаблон, Строка(ВходящийКонтекст.ДокументЗаявление.УдостоверяющийЦентр));

			СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(ВходящийКонтекст, Текст);
			Возврат;
			
		КонецЕсли;
		
	Иначе
		
		СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(ВходящийКонтекст);
		Возврат;
		
	КонецЕсли;
	
КонецПроцедуры
 
&НаКлиенте
Процедура Подключаемый_ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияКонтейнера()
	
	ВходящийКонтекст     = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации("БРО.КонтекстОтправкиЗаявления");
	СтрокаПередачиДанных = ОбработкаЗаявленийАбонентаКлиент.ЗаявлениеСформироватьЗапросНаСертификатКвалифицированный(ВходящийКонтекст);
	
	Оповещение = Новый ОписаниеОповещения(
		"ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияЗапросаНаСертификат", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	КриптографияЭДКОКлиент.СоздатьЗапросНаСертификат(
		Оповещение, 
		СтрокаПередачиДанных,
		ВходящийКонтекст.УникальноеИмяКонтейнера,
		ВходящийКонтекст.ВыводитьСообщения,
		ВходящийКонтекст.МенеджерКриптографииДляЗапросаНаСертификат,
		ВходящийКонтекст.АлгоритмКонтейнераКлючей);
		
	ВходящийКонтекст.Удалить("МенеджерКриптографииДляЗапросаНаСертификат");
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПослеСозданияЗапросаНаСертификат(Результат, ВходящийКонтекст) Экспорт
	
	СтруктураРезультата = Новый Структура();
	СтруктураРезультата.Вставить("ПродолжитьОтправкуЗаявления", Результат.Выполнено);
	СтруктураРезультата.Вставить("ТекстОшибки",                 ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности);
	СтруктураРезультата.Вставить("ПутьКонтейнерЗакрытогоКлюча", ВходящийКонтекст.УникальноеИмяКонтейнера);
	
	Если Результат.Выполнено Тогда
		
		СтруктураРезультата.Вставить("ТекстЗапросаНаСертификат", Результат.ЗапросНаСертификат);
		СтруктураРезультата.Вставить("АлгоритмКонтейнераКлючей", ВходящийКонтекст.АлгоритмКонтейнераКлючей);
		
	Иначе
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
			НСтр("ru = 'Криптографическая ошибка: не удалось сформировать запрос на сертификат для контролирующих органов.'"), 
			ВходящийКонтекст.ВызовИзМастераПодключенияК1СОтчетности, 
			ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности);
			
		СтруктураРезультата.Вставить("ТекстЗапросаНаСертификат", "");
		СтруктураРезультата.Вставить("ТекстОшибки", Результат.ОписаниеОшибки);
				
	КонецЕсли;
		
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, СтруктураРезультата);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючПослеИнструкцииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
		ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатПродолжение(ДополнительныеПараметры);
			
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьИнструкциюСозданияКонтейнера(ТипКриптопровайдера, ВыполняемоеОповещение = Неопределено) Экспорт
	
	Если ТипКриптопровайдера <> ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet") 
		И ТипКриптопровайдера <> ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro") Тогда
	
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		
		Возврат;
	КонецЕсли;
	
	Если ТипКриптопровайдера = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet") Тогда
		
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("МакетИнструкцииДляПоказа","ИнструкцияVipNetCSPСозданиеКонтейнера");
		ПараметрыФормы.Вставить("Заголовок", "Инструкция по созданию контейнера закрытого ключа для криптопровайдера ViPNet CSP");
		
	Иначе
		
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("МакетИнструкцииДляПоказа","ИнструкцияКриптоПроСозданиеКонтейнера");
		ПараметрыФормы.Вставить("Заголовок", "Инструкция по созданию контейнера закрытого ключа для криптопровайдера КриптоПро CSP");
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПоказатьИнструкциюСозданияКонтейнераЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ФормаОтображенияИнструкцииHTML",
		ПараметрыФормы, 
		, 
		, 
		, 
		, 
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьИнструкциюСозданияКонтейнераЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Ложь);
	Иначе
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеЗашифровать(ДополнительныеПараметры)
	
	ДокументЗаявление = ДополнительныеПараметры.ДокументЗаявление;	
	МестоХраненияКлюча 	= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	// Проверяем, устрановлени ли сертификат оператора электронного документооборота в хранилище сертификатов
	ХранилищеСертификата 	= "AddressBook";
	Отпечаток 				= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(ДокументЗаявление.СпецоператорСвязи, "СертификатБезопасности_Отпечаток");
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеПроверкиРегистрацииСертификата", ЭтотОбъект, ДополнительныеПараметры);
	Если ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(ОписаниеОповещения, Отпечаток, ХранилищеСертификата);
	Иначе
		Сертификат = Новый Структура();
		Сертификат.Вставить("Отпечаток", Отпечаток);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Сертификат);
		КриптографияЭДКОКлиент.НайтиСертификат(ОписаниеОповещения, Сертификат);
	КонецЕсли;	
	
КонецПроцедуры

&НаСервере
Процедура НачатьОтправкуОблачногоЗаявленияВБазуЗаявок(ДокументЗаявление, АлгоритмКонтейнераКлючей)

	АдресЗаданияПоОтправкеОблачногоЗаявления = ПоместитьВоВременноеХранилище("", Новый УникальныйИдентификатор);
	
	ДополнительныеПараметры = Новый Массив();
	ДополнительныеПараметры.Добавить(АдресЗаданияПоОтправкеОблачногоЗаявления);
	ДополнительныеПараметры.Добавить(ДокументЗаявление);
	ДополнительныеПараметры.Добавить(АлгоритмКонтейнераКлючей);
	
	ФоновыеЗадания.Выполнить("ЭлектронныйДокументооборотСКонтролирующимиОрганами.НачатьОтправкуОблачногоЗаявленияВБазуЗаявок", ДополнительныеПараметры);

КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПроверитьСостояниеОтправкиОблачногоЗаявленияВБазуЗаявок()
	
	Если ЭтоАдресВременногоХранилища(АдресЗаданияПоОтправкеОблачногоЗаявления) Тогда
		
		Результат = ПолучитьИзВременногоХранилища(АдресЗаданияПоОтправкеОблачногоЗаявления);
		
		Если ЗначениеЗаполнено(Результат) Тогда
			
			ОтключитьОбработчикОжидания("Подключаемый_ПроверитьСостояниеОтправкиОблачногоЗаявленияВБазуЗаявок");
			
			Если Результат.Выполнено Тогда
				
				ДанныеЗаявления = Результат.ДанныеЗаявления;
				
				// Копирование новых свойств в открытую форму заявления.
				ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(ОтправляемоеЗаявление, ДанныеЗаявления);
				
				Если ДанныеЗаявления.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено") Тогда
					ОбработкаЗаявленийАбонентаКлиент.ВключитьАвтоматическуюПроверкуСтатуса(ОтправляемоеЗаявление);
					ТекстОшибки = "";
				Иначе
					ТекстОшибки = НСтр("ru = 'При обработке заявления на сервере возникла ошибка. Сервис временно недоступен.'");
				КонецЕсли;
				
				Результат = Новый Структура("ТекстОшибки", ТекстОшибки);
				ВыполнитьОбработкуОповещения(ОписаниеОповещенияПослеОтправкиЗаявления, Результат);
				
			Иначе
				
				Результат.Вставить("ТекстОшибки", Результат.ОписаниеОшибки);
				ВыполнитьОбработкуОповещения(ОписаниеОповещенияПослеОтправкиЗаявления, Результат); // Результат содержит текст ошибки
				
			КонецЕсли;
			
		Иначе
			
			ПодключитьОбработчикОжидания("Подключаемый_ПроверитьСостояниеОтправкиОблачногоЗаявленияВБазуЗаявок", 1, Истина);
			
		КонецЕсли;
		
	Иначе
		
		ПодключитьОбработчикОжидания("Подключаемый_ПроверитьСостояниеОтправкиОблачногоЗаявленияВБазуЗаявок", 1, Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСтраницуДляСкачивания(Версия) Экспорт
	
	Если Версия = СтруктураСсылочныхДанных.Перечисления_ТипыКриптоПровайдеров_VipNet Тогда
		
		АдресСтраницы = "http://infotecs.ru/downloads/product_full.php?id_product=2096";
		
	Иначе
		
		АдресСтраницы = "http://cryptopro.ru/";
		
	КонецЕсли;
	
	ПерейтиПоНавигационнойСсылке(АдресСтраницы);
	
КонецПроцедуры

&НаКлиенте
Функция ПолучитьПредставлениеКриптопровайдера(Элемент,ВыборКриптопровайдера)
	
	Возврат Элемент.СписокВыбора[ВыборКриптопровайдера-1].Представление;
	
КонецФункции

&НаКлиенте
Процедура ПолеВыбораКриптопровайдераПриИзменении(Элемент,ТаблицаЗначенийКС,КартинкаВидимость,ВыборКриптопровайдера,КриптопровайдерРекомендация, ВыполняемоеОповещение = Неопределено) Экспорт
	
	ПолеВыбораКриптопровайдера = ПолучитьПредставлениеКриптопровайдера(Элемент,ВыборКриптопровайдера);
	
	Режим = РежимДиалогаВопрос.ДаНет;
	
	ТекстВипнет = "Перейти на сайт разработчика с которого можно бесплатно скачать и установить криптопровайдер ?
	|
	|Внимание! Если будет произведено скачивание и установка, рекомендуется перезагрузить компьютер и запустить мастер заново.";
	
	ТекстКриптоПро = "Перейти на сайт разработчика на котором можно ознакомиться с информацией о способах приобретения криптопровайдера ?
	|
	|Внимание! Если будет произведено приобретение и установка, рекомендуется перезагрузить компьютер и запустить мастер заново.";
	
	Для каждого Строка из ТаблицаЗначенийКС Цикл
		
		Если Строка.ОтображаемыйТекст = ПолеВыбораКриптопровайдера Тогда
			Если Строка.УстановленОС Тогда
				
			ИначеЕсли  Строка.Статус = "(требуется установка)" Тогда
				
				ДополнительныеПараметры = Новый Структура("ТаблицаЗначенийКС, КриптопровайдерРекомендация, ВыборКриптопровайдера, КартинкаВидимость, ВыполняемоеОповещение", ТаблицаЗначенийКС, КриптопровайдерРекомендация, ВыборКриптопровайдера, КартинкаВидимость, ВыполняемоеОповещение);
				ОписаниеОповещения = Новый ОписаниеОповещения("ПолеВыбораКриптопровайдераПриИзмененииВопросВипнетЗавершение", ЭтотОбъект, ДополнительныеПараметры);
				ПоказатьВопрос(ОписаниеОповещения, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1'"),ТекстВипнет), Режим, 0, КодВозвратаДиалога.Нет);
				Возврат;				
			Иначе
				
				ДополнительныеПараметры = Новый Структура("ТаблицаЗначенийКС, КриптопровайдерРекомендация, ВыборКриптопровайдера, КартинкаВидимость, ВыполняемоеОповещение", ТаблицаЗначенийКС, КриптопровайдерРекомендация, ВыборКриптопровайдера, КартинкаВидимость, ВыполняемоеОповещение);
				ОписаниеОповещения = Новый ОписаниеОповещения("ПолеВыбораКриптопровайдераПриИзмененииВопросКриптоПроЗавершение", ЭтотОбъект, ДополнительныеПараметры);
				ПоказатьВопрос(ОписаниеОповещения, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1'"),ТекстКриптоПро), Режим, 0, КодВозвратаДиалога.Нет);
				Возврат;				
			КонецЕсли;
			
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ВыработатьРекомендацииПоКриптопровайдеру(ТаблицаЗначенийКС,КриптопровайдерРекомендация,ВыборКриптопровайдера,КартинкаВидимость,ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолеВыбораКриптопровайдераПриИзмененииВопросВипнетЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ТаблицаЗначенийКС = ДополнительныеПараметры.ТаблицаЗначенийКС;
	КриптопровайдерРекомендация = ДополнительныеПараметры.КриптопровайдерРекомендация;
	ВыборКриптопровайдера = ДополнительныеПараметры.ВыборКриптопровайдера;
	КартинкаВидимость = ДополнительныеПараметры.КартинкаВидимость;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		ОткрытьСтраницуДляСкачивания(СтруктураСсылочныхДанных.Перечисления_ТипыКриптоПровайдеров_VipNet);
	КонецЕсли;
	
	ВыработатьРекомендацииПоКриптопровайдеру(ТаблицаЗначенийКС,КриптопровайдерРекомендация,ВыборКриптопровайдера,КартинкаВидимость,ВыполняемоеОповещение);
		
КонецПроцедуры

&НаКлиенте
Процедура ПолеВыбораКриптопровайдераПриИзмененииВопросКриптоПроЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ТаблицаЗначенийКС = ДополнительныеПараметры.ТаблицаЗначенийКС;
	КриптопровайдерРекомендация = ДополнительныеПараметры.КриптопровайдерРекомендация;
	ВыборКриптопровайдера = ДополнительныеПараметры.ВыборКриптопровайдера;
	КартинкаВидимость = ДополнительныеПараметры.КартинкаВидимость;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		ОткрытьСтраницуДляСкачивания(СтруктураСсылочныхДанных.Перечисления_ТипыКриптоПровайдеров_CryptoPro);
	КонецЕсли;
	
	ВыработатьРекомендацииПоКриптопровайдеру(ТаблицаЗначенийКС,КриптопровайдерРекомендация,ВыборКриптопровайдера,КартинкаВидимость,ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьИмяТипКриптопровайдера() Экспорт
	Возврат Новый Структура("Имя,Тип",Константы.ДокументооборотСКонтролирующимиОрганами_ИмяКриптопровайдера.Получить(), Константы.ДокументооборотСКонтролирующимиОрганами_ТипКриптопровайдера.Получить());
КонецФункции

&НаКлиенте
Процедура ВыработатьРекомендацииПоКриптопровайдеру(ТаблицаЗначенийКС,КриптопровайдерРекомендация,ВыборКриптопровайдера,КартинкаВидимость,ВыполняемоеОповещение = Неопределено) Экспорт
	
	Инфотекс  = ТаблицаЗначенийКС.Получить(0).УстановленОС;
	КриптоПро = ТаблицаЗначенийКС.Получить(1).УстановленОС;
	
	Если Инфотекс и КриптоПро Тогда
		
		КартинкаВидимость = Истина;
		
		КриптопровайдерРекомендация = "Установка криптопровайдера не требуется. В системе уже установлено два криптопровайдера.
		|
		|Внимание, возможны ошибки в работе, вызванные конфликтом криптопровайдеров. Типичное проявление - не расшифровываются входящие документы.
		|
		|При появлении ошибок рекомендуется обратиться к системному администратору, и оставить только один криптопровайдер (рекомендуется CryptoPro).";
		
	ИначеЕсли КриптоПро Тогда
		
		Если ВыборКриптопровайдера = 1 Тогда
			
			КартинкаВидимость = Ложь;
			
			КриптопровайдерРекомендация = "В системе уже установлен CryptoPro CSP. 
			|
			|Но для использования выбран Инфотекс ViPNet CSP.
			|
			|Измените выбор либо скачайте и установите Инфотекс ViPNet CSP.";
			
			КартинкаВидимость = Истина;
			
		Иначе
			
			КартинкаВидимость = Ложь;
			
			КриптопровайдерРекомендация = "Установка криптопровайдера не требуется. 
			|
			|В системе уже установлен криптопровайдер CryptoPro CSP.";
			
		КонецЕсли;
		
	ИначеЕсли Инфотекс Тогда
		
		Если ВыборКриптопровайдера = 1 Тогда
			
			КартинкаВидимость = Ложь;
			
			КриптопровайдерРекомендация = "Установка криптопровайдера не требуется. 
			|
			|В системе уже установлен криптопровайдер Инфотекс ViPNet CSP.";
		Иначе
			
			КриптопровайдерРекомендация = "В системе уже установлен Инфотекс ViPNet CSP.
			|
			|Но для использования выбран CryptoPro CSP.
			|
			|Измените выбор либо приобретите и установите CryptoPro CSP.";
			
			КартинкаВидимость = Истина;
			
		КонецЕсли;
	Иначе
		
		КартинкаВидимость = Истина;
		
		КриптопровайдерРекомендация = "Требуется скачивание и установка криптопровайдера.
		|
		|Рекомендован бесплатный Инфотекс ViPNet CSP.
		|
		|После скачивания и установки криптопровайдера рекомендуется перезагрузить компьютер и запустить мастер заново.";
		
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		СтруктураВозврата = Новый Структура("КартинкаВидимость, КриптопровайдерРекомендация", КартинкаВидимость, КриптопровайдерРекомендация);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, СтруктураВозврата);	
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура НовоеНаправление(Получатели, ТипПолучателя, КодПолучателя, КПП = "", Наименование = "") Экспорт
	
	Отбор = Новый Структура("ТипПолучателя, КодПолучателя, КПП",ТипПолучателя,КодПолучателя,КПП);
	
	Если Получатели.НайтиСтроки(Отбор).Количество() <> 0 Тогда 
		Возврат;
	КонецЕсли;
	
	НоваяСтрокаНаправления = Получатели.Добавить();
	НоваяСтрокаНаправления.ТипПолучателя = ТипПолучателя;
	НоваяСтрокаНаправления.КодПолучателя = КодПолучателя;
	
	Если ЗначениеЗаполнено(КПП) Тогда
		НоваяСтрокаНаправления.КПП = КПП;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Наименование) Тогда
		НоваяСтрокаНаправления.Наименование = Наименование;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ПроверитьКПП(КПП) Экспорт
	
	Возврат ДокументооборотСКОКлиентСервер.ПроверитьКПП(КПП);
	
КонецФункции

&НаКлиенте
Функция ПроверитьЦифровойКодЗаданнойДлины(Параметр, Длина, ПропускатьПустой=Ложь) Экспорт
	
	Возврат ДокументооборотСКОКлиентСервер.ПроверитьЦифровойКодЗаданнойДлины(Параметр, Длина, ПропускатьПустой);

КонецФункции

&НаКлиенте
Функция ПроверитьРегистрационныйНомерПФР(Параметр, ПропускатьПустой = Ложь, ПропускатьРегНомерСФР = Ложь) Экспорт
	
	Возврат ДокументооборотСКОКлиентСервер.ПроверитьРегистрационныйНомерПФР(Параметр, ПропускатьПустой,
		ПропускатьРегНомерСФР);
	
КонецФункции

&НаКлиенте
Функция ПроверитьНомерОрганаПФР(Знач Номер)
	Результат = Ложь;
	Номер = СокрЛП(Номер);
	Если СтрДлина(Номер)= 7 тогда
		Результат =  ПроверитьЦифровойКодЗаданнойДлины(Лев(Номер,3),3) И
		ПроверитьЦифровойКодЗаданнойДлины(Прав(Номер,3),3) И
		Сред(Номер,4,1)="-";
	КонецЕсли;
	Возврат Результат;
КонецФункции

&НаКлиенте
Процедура ВосстановитьНаправленияПоУмолчанию(Получатели, ДанныеОрганизации) Экспорт 
	
	ВосстановитьНаправленияПоУмолчаниюПФР(Получатели, ДанныеОрганизации);
	ВосстановитьНаправленияПоУмолчаниюФСС(Получатели, ДанныеОрганизации);
	ВосстановитьНаправленияПоУмолчаниюФСГС(Получатели, ДанныеОрганизации);
	ВосстановитьНаправленияПоУмолчаниюФНС(Получатели, ДанныеОрганизации);
	
КонецПроцедуры

&НаКлиенте
Процедура ВосстановитьНаправленияПоУмолчаниюФНС(Получатели, ДанныеОрганизации) Экспорт 
	
	КодПолучателяФНС 	= ДанныеОрганизации.КодНО;
	ЭтоЮрЛицо 			= ДанныеОрганизации.ТипОрганизации;
	
	Если СтрДлина(СокрЛП(КодПолучателяФНС)) = 4 
		И (ЭтоЮрЛицо И СтрДлина(ДанныеОрганизации.КППЮЛ) <> 0 ИЛИ НЕ ЭтоЮрЛицо) 
		И НЕ Прав(СокрЛП(КодПолучателяФНС),2) = "00" Тогда
		
		НовоеНаправление(
			Получатели,
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС"),
			КодПолучателяФНС,
			ДанныеОрганизации.КППЮЛ);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВосстановитьНаправленияПоУмолчаниюФСГС(Получатели, ДанныеОрганизации, Спецоператор = Неопределено, Знач КодРегиона = Неопределено) Экспорт
	
	// ФСГС
	Если НЕ ПустаяСтрока(ДанныеОрганизации.КодОрганаФСГС) 
		И КодОрганаФСГССоответствуетТребованиямКлиент(ДанныеОрганизации.КодОрганаФСГС) Тогда
		
		НовоеНаправление(
			Получатели,
			ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС"),
			ДанныеОрганизации.КодОрганаФСГС);
			
	Иначе
		
		// Определяем направление по региону.
		Если НЕ ЗначениеЗаполнено(КодРегиона) Тогда
			Если ДанныеОрганизации.ТипОрганизации Тогда
				АдресЮридический = ДанныеОрганизации.АдрЮР;
			Иначе
				АдресЮридический = ДанныеОрганизации.АдрМЖ;
			КонецЕсли;
			
			АдресЮридический = РегламентированнаяОтчетностьВызовСервера.ЗаменитьТекстРегионаНаКодРегиона(АдресЮридический);
			
			КодРегиона = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(АдресЮридический).Регион;
		КонецЕсли;
		
		ТОГС = ТОГСПоРегиону(КодРегиона, Спецоператор);
		
		Если ЗначениеЗаполнено(ТОГС) Тогда
			
			НовоеНаправление(
				Получатели,
				ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС"),
				ТОГС);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ТОГСПоРегиону(КодРегиона, Спецоператор)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТОГСПоРегиону(КодРегиона, Спецоператор);
			
КонецФункции

&НаСервере
Функция НаименованиеТОГСаПоКоду(КодТОГСа, Спецоператор) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НаименованиеТОГСаПоКоду(КодТОГСа, Спецоператор);
	
КонецФункции

&НаКлиенте
Процедура ВосстановитьНаправленияПоУмолчаниюФСС(Получатели, ДанныеОрганизации) 
	
	// ФСС
	КодПодчФСС = "";

	Если ДанныеОрганизации.Свойство("КодПодчиненностиФСС",КодПодчФСС) Тогда
		КодПодчФСС = ДанныеОрганизации.КодПодчиненностиФСС;
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ДанныеОрганизации.РеквизитДопКодФСС) Тогда
		ДопКодФСС = ДанныеОрганизации.РеквизитДопКодФСС;
	ИначеЕсли ДанныеОрганизации.Свойство("ОбъектДополнительныйКодФСС") И НЕ ПустаяСтрока(ДанныеОрганизации.ОбъектДополнительныйКодФСС) Тогда 
		ДопКодФСС = ДанныеОрганизации.ОбъектДополнительныйКодФСС;
	КонецЕсли; 

	КодПодразделенияФССПоУмолчанию = ПолучитьКодПодразделенияФССПоУмолчанию(
		КодПодчФСС,
		ДопКодФСС,
		ДанныеОрганизации.РегистрационныйНомерФСС);
		
	ЕстьНаправлениеФСС = НЕ ПустаяСтрока(КодПодразделенияФССПоУмолчанию);
	
	Если ЕстьНаправлениеФСС Тогда
		НовоеНаправление(Получатели,СтруктураСсылочныхДанных.Перечисления_ТипыКонтролирующихОрганов_ФСС,"",);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВосстановитьНаправленияПоУмолчаниюПФР(Получатели, ДанныеОрганизации)
	
	// ПФР
	Если ПустаяСтрока(ДанныеОрганизации.КодОрганаПФР) ИЛИ СтрДлина(ДанныеОрганизации.КодОрганаПФР) < 7 Тогда
		КодПолучателяПФР = Лев(ДанныеОрганизации.КодОрганаПФР,7); 
	Иначе
		КодПолучателяПФР = ДанныеОрганизации.КодОрганаПФР;
	КонецЕсли;
	
	Если ПроверитьНомерОрганаПФР(КодПолучателяПФР) Тогда
		Если СтрДлина(СокрЛП(ДанныеОрганизации.РегНомПФР))= 14 Тогда
			
			НовоеНаправление(
				Получатели,
				ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР"),
				КодПолучателяПФР);
				
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция НаправлениеУникально(Получатели, Действие, НовыеЗначенияПолучателя, ПредыдущиеЗначенияПолучателя = Неопределено) Экспорт
	
	ФНС	= ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС");
	ПФР 	= ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР");
	ФСГС 	= ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС");
	ФСС 	= ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС");
	
	Отбор = Новый Структура();
	Отбор.Вставить("ТипПолучателя", НовыеЗначенияПолучателя.ТипПолучателя);

	Если НовыеЗначенияПолучателя.ТипПолучателя = ФНС Тогда
	
		Отбор.Вставить("КодПолучателя", НовыеЗначенияПолучателя.КодПолучателя);
		Отбор.Вставить("КПП", 			НовыеЗначенияПолучателя.КПП);
		
	ИначеЕсли НовыеЗначенияПолучателя.ТипПолучателя = ФСГС Тогда
		
		Отбор.Вставить("КодПолучателя", НовыеЗначенияПолучателя.КодПолучателя);
		
	КонецЕсли;
		
	СредиПолучателейУжеЕстьТакой = Получатели.НайтиСтроки(Отбор).Количество() > 0;
	
	ОтказатьВДобавлениие 	= СредиПолучателейУжеЕстьТакой И Действие = "Добавить";
	
	ОтказатьВРедактировании = СредиПолучателейУжеЕстьТакой И Действие = "Редактировать"
		И (ПредыдущиеЗначенияПолучателя.ТипПолучателя <> НовыеЗначенияПолучателя.ТипПолучателя
		ИЛИ ПредыдущиеЗначенияПолучателя.КодПолучателя <> НовыеЗначенияПолучателя.КодПолучателя
		ИЛИ ПредыдущиеЗначенияПолучателя.КПП <> НовыеЗначенияПолучателя.КПП);
	
	Если ОтказатьВДобавлениие ИЛИ ОтказатьВРедактировании Тогда
		
		Если НовыеЗначенияПолучателя.ТипПолучателя = ФНС Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Получатель с таким кодом налоговой уже добавлен.'"));
		ИначеЕсли НовыеЗначенияПолучателя.ТипПолучателя = ФСС Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Направление сдачи в ФСС уже добавлено.'"));
		ИначеЕсли НовыеЗначенияПолучателя.ТипПолучателя = ПФР Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Направление сдачи в ПФР уже добавлено.'"));
		ИначеЕсли НовыеЗначенияПолучателя.ТипПолучателя = ФСГС Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Направление сдачи в Росстат уже добавлено.'"));
		КонецЕсли;
				
		Возврат Ложь;
			
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуВводаНаправления(
		ФормаВладелец,
		ОписаниеОповещения,
		ДополнительныеПараметры) Экспорт 
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ФормаВводаНаправлений",
		ДополнительныеПараметры,
		ФормаВладелец,
		,
		,
		,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

&НаКлиенте
Функция ТекущаяСтрокаВыбрана(Таблица,ТекСтрока,Действие) Экспорт
	СтрокаНЕВыбрана = Ложь;
	
	Если Таблица.Количество() > 0 И ТекСтрока <> Неопределено Тогда
		Возврат Истина;
	ИначеЕсли Таблица.Количество() > 0 И ТекСтрока = Неопределено Тогда
		СтрокаНЕВыбрана = Истина;
	ИначеЕсли Таблица.Количество() = 0 И ТекСтрока = Неопределено Тогда
		СтрокаНЕВыбрана = Истина;
	КонецЕсли;
	
	Если СтрокаНЕВыбрана Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не выбрана строка для %1'"),Действие), ,"Таблица");
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции
        
&НаСервереБезКонтекста
Процедура ПодготовитьДанныеАдреса(СтруктураАдресаРегион,ПредставлениеАдрес)
	СтруктураАдресаРегион = РегламентированнаяОтчетностьВызовСервера.ПолучитьНазваниеРегионаПоКоду(СтруктураАдресаРегион);
	ПредставлениеАдрес = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеАдресаВФормате9Запятых(ПредставлениеАдрес,Истина);
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьАдрес(ДополнительныеПараметры) Экспорт
	
	Адрес             = ДополнительныеПараметры.Адрес;
	АдресИмя          = ДополнительныеПараметры.АдресИмя;
	Элемент           = ДополнительныеПараметры.Элемент;
	Оповещение        = ДополнительныеПараметры.Оповещение;
	ПринудительноФИАС = ДополнительныеПараметры.ПринудительноФИАС;
	ТолькоПросмотр    = ДополнительныеПараметры.ТолькоПросмотр;

	ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(Адрес);
	
	АдресИмяПредставление = ?(АдресИмя = "АдресЮридический","Адрес юридический","Адрес фактический");
	
	ВидАдреса = ?(АдресИмя = "АдресЮридический", 
		ПредопределенноеЗначение("Справочник.ВидыКонтактнойИнформации.ЮрАдресОрганизации"), 
		ПредопределенноеЗначение("Справочник.ВидыКонтактнойИнформации.ФактАдресОрганизации"));
	
	Если ЭтоАдресПоФИАСу ИЛИ ПринудительноФИАС Тогда
		
		ПараметрыФормы = УправлениеКонтактнойИнформациейКлиент.ПараметрыФормыКонтактнойИнформации(ВидАдреса, Адрес);
		ПараметрыФормы.Вставить("ТолькоПросмотр", ТолькоПросмотр);
		
		УправлениеКонтактнойИнформациейКлиент.ОткрытьФормуКонтактнойИнформации(ПараметрыФормы, , Оповещение);
			
	Иначе
		
		ЗначенияПолей   = Новый СписокЗначений;
		СтруктураАдреса = РегламентированнаяОтчетностьКлиентСервер.РазложитьАдрес(Адрес);
		
		ПредставлениеАдрес = Адрес;
		СтруктураАдресаРегион = СтруктураАдреса.Регион;
		ПодготовитьДанныеАдреса(СтруктураАдресаРегион, ПредставлениеАдрес);

		Если НЕ СтруктураАдреса = Неопределено Тогда
			
			КодСтраны = "";
			НазваниеСтраны = "";
			СтруктураАдреса.Свойство("Страна", КодСтраны);
			
			Если ЗначениеЗаполнено(КодСтраны) Тогда
				НазваниеСтраны = ОпределитьНазваниеСтраны(КодСтраны);
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(НазваниеСтраны) Тогда
				НазваниеСтраны = "РОССИЯ";
			КонецЕсли;
			
			ЗначенияПолей.Добавить(НазваниеСтраны,"Страна");
			ЗначенияПолей.Добавить(СтруктураАдреса.Индекс,"Индекс");
			ЗначенияПолей.Добавить(СтруктураАдресаРегион,"Регион");
			ЗначенияПолей.Добавить(СтруктураАдреса.Район,"Район");
			ЗначенияПолей.Добавить(СтруктураАдреса.Город,"Город");
			ЗначенияПолей.Добавить(СтруктураАдреса.НаселенныйПункт,"НаселенныйПункт");
			ЗначенияПолей.Добавить(СтруктураАдреса.Улица,"Улица");
			ЗначенияПолей.Добавить(СтруктураАдреса.Дом,"Дом");
			ЗначенияПолей.Добавить(СтруктураАдреса.Корпус,"Корпус");
			ЗначенияПолей.Добавить(СтруктураАдреса.Квартира,"Квартира");
			ЗначенияПолей.Добавить(СтруктураАдреса.ТипДома,"ТипДома");
			ЗначенияПолей.Добавить(СтруктураАдреса.ТипКвартиры,"ТипКвартиры");
			ЗначенияПолей.Добавить(СтруктураАдреса.ТипКорпуса,"ТипКорпуса");
			ЗначенияПолей.Добавить(АдресИмяПредставление,"НаименованиеВида");
			
		КонецЕсли;
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Заголовок",				АдресИмяПредставление);
		ПараметрыФормы.Вставить("ЗначенияПолей",			ЗначенияПолей);
		ПараметрыФормы.Вставить("Представление",			ПредставлениеАдрес);
		ПараметрыФормы.Вставить("ВидКонтактнойИнформации",	ВидАдреса);
		ПараметрыФормы.Вставить("ТолькоПросмотр", 			ТолькоПросмотр);
		
		УправлениеКонтактнойИнформациейКлиент.ОткрытьФормуКонтактнойИнформации(ПараметрыФормы, , Оповещение);

	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПредставлениеАдресаИзДанныхОрганизации(Адрес) Экспорт

	Возврат ОбработкаЗаявленийАбонента.ПредставлениеАдресаИзДанныхОрганизации(Адрес);

КонецФункции

&НаСервереБезКонтекста
Функция СкорректироватьАдресПоМуниципальному(АдресXML, АдресJSON) Экспорт

	Возврат ОбработкаЗаявленийАбонента.СкорректироватьАдресПоМуниципальному(АдресXML, АдресJSON);

КонецФункции

&НаКлиенте
Функция РедактироватьАдресКонвертацияРезультата(Результат, ПринудительноФИАС = Ложь) Экспорт
	
	НовыйАдрес = Новый Структура();
	НовыйАдрес.Вставить("Модифицированность",Ложь);
	
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		
		ЭтоАдресПоФИАСу = УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВXML(Результат.КонтактнаяИнформация);

		Если ЭтоАдресПоФИАСу ИЛИ ПринудительноФИАС Тогда
			
			АдресXML  = Результат.КонтактнаяИнформация;
			АдресJSON = Результат.Значение;
			
 			Адрес = СкорректироватьАдресПоМуниципальному(АдресXML, АдресJSON);
			
			АдресПредставление = ПредставлениеАдресаИзДанныхОрганизации(Адрес);
			
			НовыйАдрес.Вставить("Модифицированность",	Истина);
			НовыйАдрес.Вставить("Адрес", 				Адрес);
			НовыйАдрес.Вставить("АдресПредставление",	АдресПредставление);
			
		Иначе
			
			РоссийскийАдрес = Новый Соответствие;
			
			РоссийскийАдрес.Вставить("Индекс","");
			РоссийскийАдрес.Вставить("Регион","");
			РоссийскийАдрес.Вставить("Район","");
			РоссийскийАдрес.Вставить("Город","");
			РоссийскийАдрес.Вставить("НаселенныйПункт","");
			РоссийскийАдрес.Вставить("Улица","");
			РоссийскийАдрес.Вставить("Дом","");
			РоссийскийАдрес.Вставить("Корпус","");
			РоссийскийАдрес.Вставить("Квартира","");
			РоссийскийАдрес.Вставить("ТипДома", "");
			РоссийскийАдрес.Вставить("ТипКорпуса", "");
			РоссийскийАдрес.Вставить("ТипКвартиры", "");
			
			РегламентированнаяОтчетностьВызовСервера.СформироватьАдрес(Результат.КонтактнаяИнформация, РоссийскийАдрес);
			
			Адрес = "643,"
			+ РоссийскийАдрес["Индекс"] + ","
			+ РоссийскийАдрес["Регион"] + ","
			+ РоссийскийАдрес["Район"] + ","
			+ РоссийскийАдрес["Город"] + ","
			+ РоссийскийАдрес["НаселенныйПункт"] + ","
			+ РоссийскийАдрес["Улица"] + ","
			+ РоссийскийАдрес["Дом"] + ","
			+ РоссийскийАдрес["Корпус"] + ","
			+ РоссийскийАдрес["Квартира"] + ","
			+ РоссийскийАдрес["ТипДома"] + ","
			+ РоссийскийАдрес["ТипКорпуса"] + ","
			+ РоссийскийАдрес["ТипКвартиры"];
			
			Если Адрес = "643,,,,,,,,," Тогда
				Адрес = "";
			КонецЕсли;
			
			АдресПредставление = РегламентированнаяОтчетностьКлиентСервер.ПредставлениеАдресаВФормате9Запятых(Адрес);
			Адрес = РегламентированнаяОтчетностьВызовСервера.ЗаменитьТекстРегионаНаКодРегиона(Адрес);
			
			НовыйАдрес.Вставить("Модифицированность",	Истина);
			НовыйАдрес.Вставить("Адрес", 				Адрес);
			НовыйАдрес.Вставить("АдресПредставление",	АдресПредставление);
			
		КонецЕсли;

	КонецЕсли;
	
	Возврат НовыйАдрес;
	
КонецФункции

&НаКлиенте
Функция ПолучитьВидИсполнителя(ДанныеОрганизации,Организация) Экспорт
	
	ВыборБухгалтера		= 0;
	ВыборДиректора		= 0;
	ВыборИсполнителя	= 0;
	
	ФИОБух = НЕ ПустаяСтрока(ДанныеОрганизации.ФИОБух);
	ФИОРук = НЕ ПустаяСтрока(ДанныеОрганизации.ФИОРук);
	
	Если ФИОБух Тогда
		ВыборБухгалтера		= 1;
		ВыборДиректора		= 0;
		ВыборИсполнителя	= 0;
	КонецЕсли;
	
	Если ФИОРук Тогда
		ВыборБухгалтера		= 0;
		ВыборДиректора		= 1;
		ВыборИсполнителя	= 0;
	КонецЕсли;
	
	Возврат Новый Структура("ВыборБухгалтера,ВыборДиректора,ВыборИсполнителя,ФИОБух,ФИОРук",ВыборБухгалтера,ВыборДиректора,ВыборИсполнителя,НЕ ФИОБух,НЕ ФИОРук);
	
КонецФункции

&НаСервереБезКонтекста 
Функция ПроверитьТребованиеУчетнойЗаписи(Организация) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ЗаявлениеАбонентаСпецоператораСвязи.УчетнаяЗапись КАК УчетнаяЗапись
	|ИЗ
	|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
	|ГДЕ
	|	ЗаявлениеАбонентаСпецоператораСвязи.Организация = &Организация
	|	И ЗаявлениеАбонентаСпецоператораСвязи.УчетнаяЗапись <> ЗНАЧЕНИЕ(Справочник.УчетныеЗаписиДокументооборота.ПустаяСсылка)";
	
	Запрос.УстановитьПараметр("Организация", Организация);
	
	Попытка
		
		Результат = Запрос.Выполнить().Выгрузить();
		
		Если Результат.Количество() = 0 Тогда
			Возврат Истина;
		Иначе
			Возврат Ложь;
		КонецЕсли;
		
	Исключение Возврат Истина; КонецПопытки;
	
КонецФункции

&НаКлиенте
Функция ПолучитьСпецоператорыСвязи(СпецоператорыСвязи = Неопределено,Макет = Неопределено) Экспорт
	
	Если СпецоператорыСвязи = Неопределено Тогда
		СпецоператорыСвязи = ПолучитьСпецоператоровСвязи();
	КонецЕсли;
	
	Массив = Новый Массив();
	
	Если СпецоператорыСвязи <> Неопределено Тогда
		
		Для Каждого СпецоператорСвязи Из СпецоператорыСвязи Цикл 
			
			СтруктураПараметра = Новый Структура("ВебСервисПризнак");
			
			Значение = ПолучитьПараметрСпецоператораКлиент(Спецоператорсвязи,СтруктураПараметра,Макет).ВебСервисПризнак;
			
			Если Значение <> Неопределено И Значение = "Истина" Тогда
				Массив.Добавить(Спецоператорсвязи.Ссылка);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Массив
	
КонецФункции

&НаКлиенте
Процедура ОткрытьСтраницуУЦНажатие(Спецоператор) Экспорт 
	Если НЕ ЗначениеЗаполнено(Спецоператор) Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Сначала укажите удостоверяющий центр.'"));
	Иначе
		СтруктураПараметра = Новый Структура("АдресВебСтраницы");
		АдресСтраницы = ПолучитьПараметрСпецоператораКлиент(Спецоператор,СтруктураПараметра).АдресВебСтраницы;
		ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(АдресСтраницы);
	КонецЕсли;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбновитьРегистрыСведенийНастройкиОбменаФСС(
		Организация,
		ОтпечатокСертификата,
		СертификатФССОтпечаток,
		СертификатФССЭЛНОтпечаток,
		ВыводитьСообщения)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ОбновитьРегистрыСведенийНастройкиОбменаФСС(
		Организация,
		ОтпечатокСертификата,
		СертификатФССОтпечаток,
		СертификатФССЭЛНОтпечаток,
		ВыводитьСообщения);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСпецоператоровСвязи()
	
	Запрос = Новый Запрос;
	Массив = Новый Массив();
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	СпецоператорыСвязи.Ссылка
	|ИЗ
	|	Перечисление.СпецоператорыСвязи КАК СпецоператорыСвязи";
	
	Попытка
		Выборка = Запрос.Выполнить().Выбрать();
		Пока Выборка.Следующий() Цикл
			
			СпецоператорыСвязи = Выборка.Ссылка;
			ИмяСпецоператора = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьИмяЗначенияПеречисленияСпецоператорыСвязиПоСинониму(СпецоператорыСвязи);
			Массив.Добавить(Новый Структура("Ссылка,Имя", СпецоператорыСвязи, ИмяСпецоператора));
		КонецЦикла;
	Исключение
		Массив = Неопределено;
	КонецПопытки;
	
	Возврат Массив;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьДопКодФСС(ДопКодФСС,Реквизит,ДоступностьВидимость,КодФСС,ТолькоПросмотр = Ложь) Экспорт
	
	Реквизит.Видимость = ДоступностьВидимость;
	
	Реквизит.ТолькоПросмотр = ?(ТолькоПросмотр ИЛИ НЕ ПустаяСтрока(ДопКодФСС), Истина, Ложь);
	
	Если ДоступностьВидимость И ПустаяСтрока(ДопКодФСС) И НЕ ПустаяСтрока(КодФСС) Тогда
		ОтметкаНезаполненного = Истина;
	Иначе
		ОтметкаНезаполненного = Ложь;
	КонецЕсли;
	
	Реквизит.АвтоОтметкаНезаполненного = ОтметкаНезаполненного;
	Реквизит.ОтметкаНезаполненного = ОтметкаНезаполненного; 

КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьНомерОсновнойПоставки1с() Экспорт
	
	НомерОсновнойПоставки1с = "";
	
	Возврат НомерОсновнойПоставки1с;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ЗаполнитьДанныеОрганизации(СтруктураРеквизитов) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ЗаполнитьДанныеОрганизации(СтруктураРеквизитов);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ЗаполнитьДанныеСпецоператораСвязи(СтруктураРеквизитов) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ЗаполнитьДанныеСпецоператораСвязи(СтруктураРеквизитов);

КонецПроцедуры

&НаСервереБезКонтекста
Функция ДополнитьДанныеОрганизацииДаннымиПоОтветственнымЛицам(СтруктураРеквизитов) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ДополнитьДанныеОрганизацииДаннымиПоОтветственнымЛицам(СтруктураРеквизитов);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьСериюДокумента(ВидДокумента,СерияДокумента, ТихийРежим = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПроверитьСериюДокумента(ВидДокумента,СерияДокумента, ТихийРежим);
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЗАПРОСА НА ВЫПИСКУ ИЗ ЕДИНОГО ГОСУДАРСТВЕННОГО РЕЕСТРА
//

&НаСервереБезКонтекста
Функция ПолучитьОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(парамЗапрос, СДанными = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(парамЗапрос, СДанными);
	
КонецФункции

Функция ПолучитьРезультатОбработкиЗапроса(РезультатОбработки, ДокументОбъект) Экспорт
	
	РезультатОбработкиЗапроса = РезультатОбработки;
	
	Если РезультатОбработки = "По Вашему запросу данных не найдено" Тогда
		РезультатОбработкиЗапроса = "По запрошенному " + ПолучитьВидПараметраЗапроса(ДокументОбъект) + " информация в реестрах отсутствует."
	КонецЕсли;
	
	Возврат РезультатОбработкиЗапроса;
	
КонецФункции


////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ И ФУНКЦИИ ОТПРАВКИ И ПОЛУЧЕНИЯ ОТВЕТА НА
// ЗАПРОС НА ВЫПИСКУ ИЗ ЕДИНОГО ГОСУДАРСТВЕННОГО РЕЕСТРА
//

#Область ОтправкаЗапросаНаВыпискуИзЕГР

&НаКлиенте
Процедура ОтправкаЗапросаНаВыпискуИзЕГР(Док, Организация, ВыполняемоеОповещение) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Док) Тогда
		Возврат;
	КонецЕсли;
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док",                   Док);
	КонтекстДлительнойОперации.Вставить("Организация",           Организация);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаВыпискуИзЕГР_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(Док, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаЗапросаНаВыпискуИзЕГР_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаВыпискуИзЕГР_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);

	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ВходящийКонтекст.Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка выписки из ЕГРЮЛ
		Возврат;
	КонецЕсли;
		
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаЗапросаНаВыпискуИзЕГР",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаЗапросаНаВыпискуИзЕГР() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	Док 	 = КонтекстДлительнойОперации.Док;
	Контекст = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(КонтекстДлительнойОперации);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");

	ОписаниеОповещения = Новый ОписаниеОповещения("ОтправкаЗапросаНаВыпискуИзЕГРПослеАвтонастройкиУчетнойЗаписи", ЭтотОбъект, Контекст);
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док,ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаВыпискуИзЕГР_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ОтправкаЗапросаНаВыпискуИзЕГР(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаЗапросаНаВыпискуИзЕГРПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	// отражаем изменения в базе
	Если НЕ ОтправкаЗапросаНаВыпискуИзЕГР_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаЗапросаНаВыпискуИзЕГР_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			Возврат;
			
		Иначе
		
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Док);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЕГРЮЛ
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки",      КонтекстОтправки);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаВыпискуИзЕГР_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		Контекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.Док, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаВыпискуИзЕГР_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;	
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаЗапросаНаВыпискуИзЕГРПослеФормированияПакета", ЭтотОбъект, ВходящийКонтекст);
		
	Отправка_СформироватьПакет(Оповещение, ВходящийКонтекст.КонтекстОтправки.ТранспортноеСообщение, "СОС");

КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаНаВыпискуИзЕГРПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЕГРЮЛ
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЕГРЮЛ
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ЕГРЮЛ
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаЗапросаНаВыпискуИзЕГР_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Док = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем учетную запись документооборота
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем признак поддержки отправки запроса для оператора электронного документооборота
	СпецоператорСвязи = УчетнаяЗапись.СпецоператорСвязи;
	
	Если СпецоператорСвязи = Неопределено ИЛИ СпецоператорСвязи = Перечисления.СпецоператорыСвязи.ПустаяСсылка() Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'Для учетной записи %1 не указан оператор электронного документооборота.
						|Отправка прервана!'"), Символ(34) + УчетнаяЗапись + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Если СпецоператорСвязи <> Перечисления.СпецоператорыСвязи.КалугаАстрал
		И СпецоператорСвязи <> Перечисления.СпецоператорыСвязи.Форус Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'Оператор электронного документооборота %1 не поддерживает отправку запроса на выписку из ЕГРЮЛ\ЕГРИП.
						|Отправка прервана!'"), Символ(34) + СпецоператорСвязи + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ДвоичныеДанныеФайлаЗапроса = Неопределено;
	РезультатФормированияФайлаЗапроса = СформироватьФайлЗапросаНаВыпискуИзЕдиногоГосударственногоРеестра(Док, ДвоичныеДанныеФайлаЗапроса, УчетнаяЗапись);
	Если НЕ РезультатФормированияФайлаЗапроса Тогда
		Возврат Ложь;
	КонецЕсли;
	
	КороткоеИмяФайлаПисьма = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбмена.Наименование = "Запрос на выписку из ЕГРЮЛ/ЕГРИП по " + ПолучитьВидПараметраЗапроса(Док) + " " + Док.ПараметрЗапроса + " от " + Док.Организация + ".";
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаССОС;
	ЦиклОбмена.Предмет = Док;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = УчетнаяЗапись.СерверДокументооборота;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.ДатаНачалаПериода = ТекущаяДатаСеанса();
	ЦиклОбмена.ДатаОкончанияПериода = ДобавитьМесяц(ЦиклОбмена.ДатаНачалаПериода, 1);
	ЦиклОбмена.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияЦиклаОбмена();
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ЗапросНаВыпискуЕРГЮЛ_ЕГРИП;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = УчетнаяЗапись.СерверДокументооборота;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Отправка прервана.'"));
			
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Отправка прервана.'"));
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем запрос
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросНаВыпискуИзЕГРЮЛ_ЕГРИП, ДвоичныеДанныеФайлаЗапроса, "Запрос.xml", , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
		
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Док, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция СформироватьФайлЗапросаНаВыпискуИзЕдиногоГосударственногоРеестра(Запрос, ДвоичныеДанныеФайлаЗапроса, УчетнаяЗапись)
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(ВремФайл,"windows-1251");
	ЗаписьXML.ЗаписатьОбъявлениеXML();
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("Запрос");
	ЗаписьXML.ЗаписатьАтрибут("ВерсФорм","1.0");
	ЗаписьXML.ЗаписатьАтрибут("ДатаВремяФормирования", XMLСтрока(ТекущаяДатаСеанса()));
	ЗаписьXML.ЗаписатьАтрибут("ВерсПрог", РегламентированнаяОтчетность.НазваниеИВерсияПрограммы());
	ОбработкаЗаявленийАбонента.ЗаписатьУзелXML(ЗаписьXML,"ИдентификаторАбонента", XMLСтрока(УчетнаяЗапись.ИдентификаторАбонента));
	
	ЗаписьXML.ЗаписатьНачалоЭлемента("ЗапрашиваемаяИнформация");
	
	Если Запрос.ВидПараметраЗапроса = Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ОГРН Тогда
		ЗаписьXML.ЗаписатьАтрибут("ОГРН", XMLСтрока(Запрос.ПараметрЗапроса));
	ИначеЕсли Запрос.ВидПараметраЗапроса = Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ИНН Тогда
		ЗаписьXML.ЗаписатьАтрибут("ИНН", XMLСтрока(Запрос.ПараметрЗапроса));
	КонецЕсли;
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.Закрыть();
	
	// считываем двоичные данные файла запроса, после чего удаляем файл
	ДвоичныеДанныеФайлаЗапроса = Новый ДвоичныеДанные(ВремФайл);
	УдалитьФайлы(ВремФайл);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетСОС_ЗаключительныеДействияНаСервере(Контекст)
	
	ДеревоДокументов = ПолучитьИзВременногоХранилища(Контекст.ДеревоДокументов);
	
	//////////////////////////////////////////////////////////////////////////////
	// ФОРМИРОВАНИЕ ФАЙЛА ОПИСАНИЯ ПАКЕТА
	
	// формируем
	ФайлОписаниеПакета = СформироватьОписаниеПакетаСОС(ДеревоДокументов,
														Контекст.UUIDПакета,
														Контекст.КодТипаДокументооборота,
														Контекст.НаименованиеТипаДокументооборота,
														Контекст.КодТипаТранзакции,
														Контекст.НаименованиеТипаТранзакции,
														Контекст.КодОтправителя,
														Контекст.КодСпецоператора,
														Контекст.КодПолучателя,
														Контекст.ТипПолучателя,
														Контекст.УникальныйИдентификатор);
	Если ФайлОписаниеПакета = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	//////////////////////////////////////////////////////////////////////////////
	// СОЗДАНИЕ АРХИВА ПАКЕТА
	
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	КороткоеИмяФайлаАрхива = "SOS_" +
							Контекст.КодОтправителя + "_" +
							Контекст.КодПолучателя + "_" +
							СгенерироватьUUID() + "_" + 
							Контекст.КодТипаДокументооборота + "_" +
							Контекст.КодТипаТранзакции + "_" +
							Контекст.КодТипаОсновногоДокумента +
							".zip";
	ПолноеИмяФайлаАрхива = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайлаАрхива;
	
	ЗаписьZIP = Новый ЗаписьZipФайла(ПолноеИмяФайлаАрхива, , , МетодСжатияZIP.Копирование); // архив пакета создается без сжатия

	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Для Каждого СтрДокумент Из ДеревоДокументов.Строки Цикл
		
		ПолноеИмяФайлаПакета = КонтекстЭДОСервер.ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(
			КаталогФайловОтчетности, СтрДокумент);
		Если НЕ ЗначениеЗаполнено(ПолноеИмяФайлаПакета) Тогда
			Возврат Ложь;
		КонецЕсли;
		ЗаписьZIP.Добавить(ПолноеИмяФайлаПакета);
		
		Для Каждого СтрДокумент2 Из СтрДокумент.Строки Цикл
			ПолноеИмяФайлаПакета = КонтекстЭДОСервер.ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(
				КаталогФайловОтчетности, СтрДокумент2);
			Если НЕ ЗначениеЗаполнено(ПолноеИмяФайлаПакета) Тогда
				Возврат Ложь;
			КонецЕсли;
			ЗаписьZIP.Добавить(ПолноеИмяФайлаПакета);
		КонецЦикла;
		
	КонецЦикла;
	
	// добавляем описание пакета
	ПолноеИмяФайлаОписанияПакета = КаталогФайловОтчетности + "packageDescription.xml";
	ПолучитьИзВременногоХранилища(ФайлОписаниеПакета).Записать(ПолноеИмяФайлаОписанияПакета);
	ЗаписьZIP.Добавить(ПолноеИмяФайлаОписанияПакета);
	
	ЗаписьZIP.Записать();
	
	//////////////////////////////////////////////////////////////////////////////
	// ДОБАВЛЕНИЕ ПАКЕТА В РЕГИСТР КОНТЕЙНЕРОВ
	
	ДобавитьТранспортныйКонтейнер(Контекст.ТранспортноеСообщение, ПолноеИмяФайлаАрхива, КороткоеИмяФайлаАрхива);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеПакетаСОС(ОписаниеДокументов, UUIDПакета = Неопределено, КодТипаДокументоооборота = "01", ТипДокументооборота = "Декларация", КодТипаТранзакции = "01", ТипТранзакции = "ДекларацияНП", КодОтправителя = "", КодСОС = Неопределено, КодПолучателя = "", ТипПолучателя = "налоговыйОрган", ИдентификаторДляВременногоХранилища = Неопределено) Экспорт
	
	Если UUIDПакета = Неопределено Тогда
		UUIDПакета = СгенерироватьUUID();
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "ТрансИнф"
	УзелТрансИнф = ДобавитьУзелXML(Дерево, "ТрансИнф");
	ДобавитьУзелXML(УзелТрансИнф, "версияФормата",							Истина, "СОС:1.0");
	ДобавитьУзелXML(УзелТрансИнф, "кодТипаДокументооборота",				Истина, КодТипаДокументоооборота);
	ДобавитьУзелXML(УзелТрансИнф, "типДокументооборота",					Истина, ТипДокументооборота);
	ДобавитьУзелXML(УзелТрансИнф, "кодТипаТранзакции",						Истина, КодТипаТранзакции);
	ДобавитьУзелXML(УзелТрансИнф, "типТранзакции",							Истина, ТипТранзакции);
	ДобавитьУзелXML(УзелТрансИнф, "идентификаторДокументооборота", 			Истина, UUIDПакета);
	ДобавитьУзелXML(УзелТрансИнф, "ВерсПрог", 								Истина, СокрЛП(Лев(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40)));
	
	// добавляем узел "отправитель"
	УзелОтправитель = ДобавитьУзелXML(УзелТрансИнф, "отправитель");
	ДобавитьУзелXML(УзелОтправитель, "идентификаторСубъекта",				Истина, КодОтправителя);
	ДобавитьУзелXML(УзелОтправитель, "типСубъекта",							Истина, "абонент");
	
	// добавляем узел "спецоператор"
	Если КодСОС <> Неопределено Тогда
		УзелСпецоператор = ДобавитьУзелXML(УзелТрансИнф, "спецоператор");
		ДобавитьУзелXML(УзелСпецоператор, "идентификаторСубъекта",			Истина, КодСОС);
		ДобавитьУзелXML(УзелСпецоператор, "типСубъекта",					Истина, "спецоператор");
	КонецЕсли;
	
	// добавляем узел "получатель"
	УзелПолучатель = ДобавитьУзелXML(УзелТрансИнф, "получатель");
	ДобавитьУзелXML(УзелПолучатель, "идентификаторСубъекта",				Истина, КодПолучателя);
	ДобавитьУзелXML(УзелПолучатель, "типСубъекта",							Истина, ТипПолучателя);

	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// добавляем узлы "Документ"
	Для Каждого СтрДокумент Из ОписаниеДокументов.Строки Цикл
		
		// добавляем очередной узел "Документ"
		УзелДокумент = ДобавитьУзелXML(УзелТрансИнф, "документ");
		ДобавитьУзелXML(УзелДокумент, "кодТипаДокумента",				Истина, КонтекстЭДОСервер.КодПоТипуДокументаПакета(СтрДокумент.ТипДокумента, КодТипаДокументоооборота));
		ДобавитьУзелXML(УзелДокумент, "типДокумента",					Истина, КонтекстЭДОСервер.ИдентификаторПоТипуДокументаПакета(СтрДокумент.ТипДокумента));
		ДобавитьУзелXML(УзелДокумент, "типСодержимого",					Истина, СтрДокумент.ТипСодержимого);
		ДобавитьУзелXML(УзелДокумент, "сжат",							Истина, XMLСтрока(СтрДокумент.Сжат = Истина));
		ДобавитьУзелXML(УзелДокумент, "зашифрован",						Истина, XMLСтрока(СтрДокумент.Зашифрован = Истина));
		ДобавитьУзелXML(УзелДокумент, "идентификаторДокумента",			Истина, СтрДокумент.ИдентификаторДокумента);
		ДобавитьУзелXML(УзелДокумент, "исходноеИмяФайла",				Истина, СтрДокумент.ИмяФайлаДокумента);
		
		// добавляем узел "Содержимое"
		Если ЗначениеЗаполнено(СтрДокумент.ИмяФайлаАрхива) Тогда
			УзелСодержимое = ДобавитьУзелXML(УзелДокумент, "содержимое");
			ДобавитьУзелXML(УзелСодержимое, "имяФайла",					Истина, СтрДокумент.ИмяФайлаАрхива);
		КонецЕсли;
		
		// добавляем подписи под документом
		Для Каждого СтрПодпись Из СтрДокумент.Строки Цикл
			
			// добавляем очередной узел "Подпись"
			УзелПодпись = ДобавитьУзелXML(УзелДокумент, "подпись");
			ДобавитьУзелXML(УзелПодпись, "имяФайла",					Истина, СтрПодпись.ИмяФайлаДокумента);
			ДобавитьУзелXML(УзелПодпись, "роль",						Истина, "абонент");
			
		КонецЦикла;
		
	КонецЦикла;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево);
	
	// записываем дерево в файл
	ПолноеИмяФайла = ПолучитьИмяВременногоФайла();
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайла);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// записываем двоичные данные файла во временное хранилище
	АдресВХранилище = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайла), ИдентификаторДляВременногоХранилища);
	
	Возврат АдресВХранилище;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИП(Запрос) Экспорт
	
	// получаем все документы к показу
	СформироватьТаблицуОтветовНаЗапросВыпискиЕГРЮЛ_ЕГРИП(Запрос);
	
	// определяем документ, который показываем
	КоличествоОтветов = ОтветыНаЗапросыВыпискиЕГРЮЛ_ЕГРИП.Количество();
	Если КоличествоОтветов = 0 Тогда
		Возврат;
	ИначеЕсли КоличествоОтветов > 1 Тогда
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОтметитьКакПрочтенное(Запрос);
		
		СписокВыбора = Новый СписокЗначений;
		Для Каждого СтрокаОтвета Из ОтветыНаЗапросыВыпискиЕГРЮЛ_ЕГРИП Цикл 
			Если СтрокаОтвета.ВАрхиве Тогда
				ПредставлениеЗначения = СтрокаОтвета.ИмяФайла + " [перемещено в архив]";
			Иначе
				ПредставлениеЗначения = СтрокаОтвета.ИмяФайла;
			КонецЕсли;
			СписокВыбора.Добавить(СтрокаОтвета.ИмяФайла, ПредставлениеЗначения);
		КонецЦикла;
		
		ДополнительныеПараметры = Новый Структура("СписокВыбора", СписокВыбора);
		ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИПЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		СписокВыбора.ПоказатьВыборЭлемента(ОписаниеОповещения);
		
	Иначе
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОтметитьКакПрочтенное(Запрос);
		
		ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИПНаКлиенте(0);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИПЗавершение(ВыбранныйЭлемент, ДополнительныеПараметры) Экспорт
	
	Если ВыбранныйЭлемент = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СписокВыбора = ДополнительныеПараметры.СписокВыбора;
	
	ИндексВыбора = СписокВыбора.Индекс(ВыбранныйЭлемент);
	
	ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИПНаКлиенте(ИндексВыбора);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИПНаКлиенте(ИндексВыбора)
	
	ОтветНаЗапросЕГРЮЛ = ОтветыНаЗапросыВыпискиЕГРЮЛ_ЕГРИП[ИндексВыбора];
	
	Если ОтветНаЗапросЕГРЮЛ.ВАрхиве Тогда
		ПоказатьУведомлениеАрхивныхФайлов(, 0, 3, Истина);
		Возврат;
	КонецЕсли;
	
	ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросЕГРЮЛ.Адрес, ОтветНаЗапросЕГРЮЛ.ИмяФайла);
	
КонецПроцедуры

&НаСервере
Процедура СформироватьТаблицуОтветовНаЗапросВыпискиЕГРЮЛ_ЕГРИП(Запрос)
	
	ТаблицаОтветовНаЗапросЕГРЮЛ_ЕГРИП = ПолучитьОтветыНаЗапросыВыпискиИзЕГРЮЛ_ЕГРИП(Запрос, Истина);
	ТаблицаОтветовНаЗапросЕГРЮЛ_ЕГРИП.Колонки.Добавить("Адрес");
	Для Каждого СтрокаОтвета Из ТаблицаОтветовНаЗапросЕГРЮЛ_ЕГРИП Цикл
		Если ЗначениеЗаполнено(СтрокаОтвета.Данные) Тогда 
			ГУИД = Новый УникальныйИдентификатор;
			СтрокаОтвета.Адрес = ПоместитьВоВременноеХранилище(СтрокаОтвета.Данные.Получить(), ГУИД);
		КонецЕсли;
	КонецЦикла;
	
	ОтветыНаЗапросыВыпискиЕГРЮЛ_ЕГРИП.Загрузить(ТаблицаОтветовНаЗапросЕГРЮЛ_ЕГРИП);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьВидПараметраЗапроса(Документ) Экспорт
	
	ВидПараметраЗапроса = "";
	
	Если НЕ ЗначениеЗаполнено(Документ) Тогда
		Возврат ВидПараметраЗапроса;
	КонецЕсли;
	
	Если Документ.ВидПараметраЗапроса = Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ИНН Тогда
		ВидПараметраЗапроса= "ИНН";
	ИначеЕсли Документ.ВидПараметраЗапроса = Перечисления.ВидыПараметровЗапросаНаВыпискуИзЕГРЮЛ_ЕГРИП.ОГРН Тогда
		ВидПараметраЗапроса = "ОГРН";
	КонецЕсли;
	
	Возврат ВидПараметраЗапроса;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДополнитьРезультатыГрупповойОнлайнПроверки(Контекст, СвойстваРезультата)
	
	// извлекаем таблицы исходных файлов и результатов
	Если ТипЗнч(Контекст.АдресФайлаВыгрузки) = Тип("Массив") Тогда
		АдресаФайлаВыгрузки = Контекст.АдресФайлаВыгрузки;
	Иначе
		АдресаФайлаВыгрузки = Новый Массив;
		АдресаФайлаВыгрузки.Добавить(Контекст.АдресФайлаВыгрузки);
	КонецЕсли;
	
	Если ТипЗнч(Контекст.ИмяФайлаВыгрузки) = Тип("Массив") Тогда
		ИменаФайлаВыгрузки = Контекст.ИмяФайлаВыгрузки;
	Иначе
		ИменаФайлаВыгрузки = Новый Массив;
		ИменаФайлаВыгрузки.Добавить(Контекст.ИмяФайлаВыгрузки);
	КонецЕсли;
	
	Если Контекст.Свойство("ТипФайлаВыгрузки") И ТипЗнч(Контекст.ТипФайлаВыгрузки) = Тип("Массив") Тогда
		ТипФайлаВыгрузки = Контекст.ТипФайлаВыгрузки;
	Иначе
		ТипФайлаВыгрузки = Новый Массив;
		Если Контекст.Свойство("ТипФайлаВыгрузки") Тогда
			ТипФайлаВыгрузки.Добавить(Контекст.ТипФайлаВыгрузки);
		ИначеЕсли Контекст.Свойство("ТипОтправляемогоДокумента") Тогда
			ТипФайлаВыгрузки.Добавить(Контекст.ТипОтправляемогоДокумента);
		Иначе
			ТипФайлаВыгрузки.Добавить("");
		КонецЕсли;
	КонецЕсли;
	
	СсылкиНаДокументы = ?(Контекст.Свойство("Документы") И Контекст.Документы <> Неопределено, Контекст.Документы, Контекст.Документ);
	Если ТипЗнч(СсылкиНаДокументы) <> Тип("Массив") Тогда
		МассивСсылокНаДокументы = Новый Массив;
		МассивСсылокНаДокументы.Добавить(СсылкиНаДокументы);
		СсылкиНаДокументы = МассивСсылокНаДокументы;
	КонецЕсли;
	
	АдресТаблицыРезультата = СвойстваРезультата.РезультатыПоДокументам;
	ТаблицаРезультата = ПолучитьИзВременногоХранилища(АдресТаблицыРезультата);
	
	// определяем дополнительные колонки в таблице результатов
	ТаблицаРезультата.Колонки.Добавить("ТипФайлаОтчета");
	ТаблицаРезультата.Колонки.Добавить("Документ");
	ТаблицаРезультата.Колонки.Добавить("ПолноеИмяФайлаВыгрузки");
	
	// заполняем новые колонки таблицы результатов
	Для Каждого СтрРезультат Из ТаблицаРезультата Цикл
		ИмяФайлаОтчета = СтрРезультат.ИмяФайлаОтчета;
		ИндФайла = ИменаФайлаВыгрузки.Найти(ИмяФайлаОтчета);
		Если ИндФайла <> Неопределено Тогда
			СтрРезультат.ТипФайлаОтчета = ТипПоИдентификаторуТипаФайлаВыгрузки(ТипФайлаВыгрузки[ИндФайла]);
			СтрРезультат.Документ = СсылкиНаДокументы[ИндФайла];
			СтрРезультат.ПолноеИмяФайлаВыгрузки = АдресаФайлаВыгрузки[ИндФайла]; // СвойстваВыгрузки.КаталогВыгрузки + СтрФайлОтчета.Имя);
		КонецЕсли;
	КонецЦикла;
	
	СвойстваРезультата.Вставить("РезультатыПоДокументам", ПоместитьВоВременноеХранилище(ТаблицаРезультата, Контекст.УникальныйИдентификатор));
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЭтоУспешныйРезультатГрупповойОнлайнПроверки(ДополнительныеПараметры)
	
	Таблица = ПолучитьИзВременногоХранилища(ДополнительныеПараметры.АдресТаблицыРезультатов);
	Для каждого Строка Из Таблица Цикл
		
		ЭтоОшибка = Строка.РезультатПроверки <> 0 И Строка.РезультатПроверки <> "0";
		Если ЭтоОшибка Тогда
			 Возврат Ложь;
		КонецЕсли;
	
	КонецЦикла; 
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьРезультатыГрупповойОнлайнПроверки(
		ТаблицаРезультата,
		ПоддержкаВУниверсальномФормате,
		РежимПоказаДляОдиночногоДокументаРазрешен,
		ВыполняемоеОповещение)
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("АдресТаблицыРезультатов",                   ТаблицаРезультата);
	ДополнительныеПараметры.Вставить("ПоддержкаВУниверсальномФормате",            ПоддержкаВУниверсальномФормате);
	ДополнительныеПараметры.Вставить("РежимПоказаДляОдиночногоДокументаРазрешен", РежимПоказаДляОдиночногоДокументаРазрешен);
	 
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
		
		Успешно = ЭтоУспешныйРезультатГрупповойОнлайнПроверки(ДополнительныеПараметры);
		
		ДополнительныеПараметры.Вставить("ЭтоГрупповойРезультат", Истина);
		ДлительнаяОтправкаКлиент.ОповеститьОРезультатеПроверкиВИнтернете(Успешно, ДополнительныеПараметры);
		
	Иначе
		
		ПоказатьРезультатыГрупповойОнлайнПроверкиПоПараметрам(ДополнительныеПараметры, ВыполняемоеОповещение);

	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПоказатьРезультатыГрупповойОнлайнПроверкиПоПараметрам(ВходящийКонтекст, ВыполняемоеОповещение = Неопределено) Экспорт
		
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.РезультатГрупповойОнлайнПроверки", 
		ВходящийКонтекст,
		,
		,
		,
		,
		ВыполняемоеОповещение,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

&НаСервереБезКонтекста
Функция ТипПоИдентификаторуТипаФайлаВыгрузки(ИдентификаторТипаФайлаВыгрузки)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТипПоИдентификаторуТипаФайлаВыгрузки(ИдентификаторТипаФайлаВыгрузки);
	
КонецФункции

#Область Автообмен

// Получаем настройки с учетом текущего пользователя.
&НаСервереБезКонтекста
Функция ПолучитьТекущиеУчетныеЗаписиСтрахователяФСС()
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьТекущиеУчетныеЗаписиСтрахователяФСС();
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИОбработатьВходящие(ИдентификаторФормы, ПараметрыПоказа = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьИОбработатьВходящие(ИдентификаторФормы, ПараметрыПоказа);
	
КонецФункции

&НаСервереБезКонтекста
Функция ТребующиеВниманияРасшифровкиИНовые()
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТребующиеВниманияРасшифровкиИНовые();
	
КонецФункции

&НаКлиенте
Процедура ПолучитьИОбработатьВходящиеКлиент(ТихаяОбработкаПоУмолчанию = Ложь) Экспорт

	Если ДлительнаяОтправкаКлиент.ФормаДлительноеДействиеОткрыта() Тогда
		Возврат;
	КонецЕсли;
	
	ОбменПрерванПользователем = Ложь;
	
	ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
		НСтр("ru = 'Электронный документооборот с контролирующими органами. Обмен'"),,
		НСтр("ru = 'Запуск получения и обработки входящих сообщений'"));
	
	ПараметрыПоказа = ПараметрыПоказаСообщенийТребующихРасшифровки(ТихаяОбработкаПоУмолчанию);
	Если НЕ ТихаяОбработкаПоУмолчанию И НЕ ПараметрыПоказа.ПоказДляТекущегоПользователя
		И НЕ ПараметрыПоказа.ПоказДляДругихПользователей Тогда
		ПараметрыПриложения["БРО.РазрешенПоказДляДругихПользователей"] = Истина;
		Возврат;
	КонецЕсли;
	
	ВходящиеСообщения = ПолучитьИОбработатьВходящие(УникальныйИдентификатор, ПараметрыПоказа);
	Если НЕ ТихаяОбработкаПоУмолчанию Тогда
		Если ПараметрыПриложения["БРО.РазрешенПоказДляДругихПользователей"] = Истина Тогда
			Если ПараметрыПоказа.ПоказДляДругихПользователей Тогда
				ПараметрыПриложения["БРО.РазрешенПоказДляДругихПользователей"] = Ложь; // для чередования
			КонецЕсли;
		Иначе
			ПараметрыПриложения["БРО.РазрешенПоказДляДругихПользователей"] = Истина; // поскольку уже проверили для себя
		КонецЕсли;
	КонецЕсли;
	Если ВходящиеСообщения = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// оповещаем открытые формы
	Если ВходящиеСообщения.НовыеСообщения.Количество() > 0 Тогда
		Оповестить("Заполнение списка полученных сообщений", ВходящиеСообщения.НовыеСообщения);
	КонецЕсли;
	
	// выводим диалог
	Если ВходящиеСообщения.ТребующиеРасшифровки.Количество() > 0 Тогда
		ВыполнитьРасшифровкуПриНеобходимости(
			ВходящиеСообщения.ТребующиеРасшифровки,
			ПараметрыПоказа,
			ТихаяОбработкаПоУмолчанию);
	ИначеЕсли ВходящиеСообщения.ТребуетсяПоказПользователюПослеАвторасшифровки Тогда
		ПоказатьСообщенияПослеАвторасшифровки(ВходящиеСообщения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ФормаРасшифровкиСообщенийОткрыта()
	
	Окна = ПолучитьОкна();
	
	Если Окна = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ИмяФормыСообщенийТребующихРасшифровки = ПутьКОбъекту + ".Форма.ПоказСообщенийТребующихРасшифровки";
	ИмяФормыСообщенийДляДругихПользователей = ПутьКОбъекту + ".Форма.ПоказСообщенийДляДругихПользователей";
	ИмяФормыОшибкиОтправки 		= ПутьКОбъекту + ".Форма.ОшибкиОтправки";
	ИмяФормыРезультатОбмена 	= ПутьКОбъекту + ".Форма.РезультатОбмена";
	ИмяФормыДлительноеДействие 	= ПутьКОбъекту + ".Форма.ДлительноеДействие";
	
	// Для формы, открытой в режиме "РежимОткрытияОкнаФормы.БлокироватьОкноВладельца",
	// не выполняется поиск по уникальному идентификатору, поэтому приходится искать самим.
	Для Каждого ОткрытоеОкно Из Окна Цикл
		Если ТипЗнч(ОткрытоеОкно) = Тип("ОкноКлиентскогоПриложения") 
			И ОткрытоеОкно.Содержимое.Количество() = 1
			И (ОткрытоеОкно.Содержимое[0].ИмяФормы = ИмяФормыСообщенийТребующихРасшифровки
			ИЛИ ОткрытоеОкно.Содержимое[0].ИмяФормы = ИмяФормыСообщенийДляДругихПользователей
			ИЛИ ОткрытоеОкно.Содержимое[0].ИмяФормы = ИмяФормыОшибкиОтправки
			ИЛИ ОткрытоеОкно.Содержимое[0].ИмяФормы = ИмяФормыРезультатОбмена
			ИЛИ ОткрытоеОкно.Содержимое[0].ИмяФормы = ИмяФормыДлительноеДействие) Тогда
			
			Возврат ОткрытоеОкно.Содержимое[0].Открыта();
			
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

&НаКлиенте
Процедура НачатьДлительнуюОперациюПоРасшифровкеСообщений(Результат, ВходящийКонтекст) Экспорт
	
	ПараметрыПоказа = ВходящийКонтекст.ПараметрыПоказа;
	
	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("НикогдаНеПоказыватьДляДругихПользователей") Тогда
		ПараметрыПоказа = Результат;
		Результат = ?(Результат.НикогдаНеПоказыватьДляДругихПользователей, Ложь, Неопределено);
	КонецЕсли;
	
	Если Результат = Ложь Тогда
		// Сюда попадем, если форму расшифровки закрыли по "Больше не показывать".
		Если ПараметрыПоказа.НикогдаНеПоказыватьДляДругихПользователей
			И ПараметрыПриложения["БРО.БольшеНеПоказыватьСообщенияТребующиеРасшифровки"] = Истина Тогда
			ДокументооборотСКОКлиент.ПодключениеОбработчикаОжиданияАвтообмена(Ложь);
		Иначе
			ДокументооборотСКОКлиент.ПодключениеОбработчикаОжиданияАвтообмена(Истина);
		КонецЕсли;
	ИначеЕсли Результат = Неопределено Тогда
		// Сюда попадем, если форму расшифровки закрыли по крестику.
		ДокументооборотСКОКлиент.ПодключениеОбработчикаОжиданияАвтообмена(Истина);
	КонецЕсли;
	
	ПолучитьДопПараметрыДляРасшифровкиСообщений(Результат);
	Если Результат = Ложь ИЛИ Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	ПараметрыПоказа.Удалить("Пользователи");
	ПараметрыПоказа.Удалить("СвойстваПользователей");
	ПараметрыПоказа.Удалить("СообщенийПоКатегориям");
	
	// производим расшифровку
	КоличествоРасшифрованных   = 0;
	КоличествоНеРасшифрованных = 0;
	НомерОбъекта               = 0;
	Организация                = Неопределено;
	ВсегоСообщений             = СообщенияТребующиеРасшифровки.Количество();
	
	ПараметрыРасшифровки = Новый Структура;
	ПараметрыРасшифровки.Вставить("КоличествоРасшифрованных", 	КоличествоРасшифрованных);
	ПараметрыРасшифровки.Вставить("КоличествоНеРасшифрованных", КоличествоНеРасшифрованных);
	ПараметрыРасшифровки.Вставить("НомерОбъекта", 				НомерОбъекта);
	ПараметрыРасшифровки.Вставить("Организация", 				Организация);
	ПараметрыРасшифровки.Вставить("ВсегоСообщений", 			ВсегоСообщений);
	ПараметрыРасшифровки.Вставить("ТекущаяОрганизация", 		Неопределено);
	ПараметрыРасшифровки.Вставить("ПараметрыПоказа", 			ПараметрыПоказа);
	
	ПараметрыДлительногоОбмена = ДлительнаяОтправкаКлиент.ПараметрыДлительногоОбмена();
	ПараметрыДлительногоОбмена.ЭтоРасшифровка 			= Истина;
	ПараметрыДлительногоОбмена.Организации 				= СообщенияТребующиеРасшифровки[НомерОбъекта].Организация;
	ПараметрыДлительногоОбмена.ОбщееКоличествоЭтапов 	= ВсегоСообщений;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительногоОбмена(ПараметрыДлительногоОбмена) Тогда // Расшифровка.
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_РасшифроватьТребующиеРасшифровку", 0.1, Истина);
	
КонецПроцедуры

&НаСервере
Процедура ПолучитьДопПараметрыДляРасшифровкиСообщений(Результат)
	
	Мультирежим.ЗаписатьДатуПросмотраНерасшифрованных();
	Если Результат = Ложь ИЛИ Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СообщенияТребующиеРасшифровки.Загрузить(ПолучитьИзВременногоХранилища(Результат.АдресТребующиеРасшифровки));
	ТаблицаРасшифровкиПоОрганизациям.Загрузить(ПолучитьИзВременногоХранилища(Результат.АдресТаблицаРасшифровкиПоОрганизациям));
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщенияПослеАвторасшифровки(ВходящиеСообщения)

	ПараметрыДлительногоОбмена = ДлительнаяОтправкаКлиент.ПараметрыДлительногоОбмена();
	ПараметрыДлительногоОбмена.ЭтоРасшифровка     = Истина;
	ПараметрыДлительногоОбмена.ЭтоАвторасшифровка = Истина;
	ПараметрыДлительногоОбмена.АдресДереваНовыхСобытий = ВходящиеСообщения.АдресДереваНовыхСобытий;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительногоОбмена(ПараметрыДлительногоОбмена) Тогда // Расшифровка.
		Возврат;
	КонецЕсли;
	
	ОтобразитьКонечныйРезультатРасшифровки(Неопределено, 0, 0, 0);

КонецПроцедуры

&НаСервере
Функция ФоноваяОперацияПоРасшифровкеСообщенийПараметры(АдресТребующиеРасшифровки)
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Расшифровка ТС для подписи в модели сервиса'");
	ПараметрыВыполнения.ОжидатьЗавершение = Истина;
	
	ТребующиеРасшифровки = ПолучитьИзВременногоХранилища(АдресТребующиеРасшифровки);
	МассивТС = Новый Массив;
	
	Для Каждого СтрокаМассива Из ТребующиеРасшифровки Цикл
		МассивТС.Добавить(СтрокаМассива.ТранспортноеСообщение);
	КонецЦикла;	
	
	Возврат ДлительныеОперации.ВыполнитьВФоне(
		"ЭлектронныйДокументооборотСКонтролирующимиОрганами.РасшифроватьТранспортныеСообщенияВФоне", 
		МассивТС, 
		ПараметрыВыполнения);

КонецФункции

&НаКлиенте
Процедура ФоноваяОперацияПоРасшифровкеСообщений(АдресТребующиеРасшифровки, ПараметрыЦикла) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ФоноваяОперацияПоРасшифровкеСообщений_Результат", 
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент,
		ПараметрыЦикла); 
		
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
	ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
	ПараметрыОжидания.Интервал = 1;
	
	ДлительнаяОперация = ФоноваяОперацияПоРасшифровкеСообщенийПараметры(АдресТребующиеРасшифровки);
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОписаниеОповещения, ПараметрыОжидания);
	
КонецПроцедуры

&НаКлиенте
Процедура ФоноваяОперацияПоРасшифровкеСообщений_Результат(ДлительнаяОперация, ВходящийКонтекст) Экспорт
	
	ВсегоРасшифровано = 0;
	
	ВходящийКонтекст.Удалить("КонтекстЭДО");
	
	Если ДлительнаяОперация <> Неопределено И
		(ДлительнаяОперация.Статус = "Выполнено") Тогда
		РезультатРасшифровки = ПолучитьИзВременногоХранилища(ДлительнаяОперация.АдресРезультата);
		Если РезультатРасшифровки.Выполнено Тогда
			ВсегоРасшифровано = РезультатРасшифровки.ВсегоРасшифровано;
		КонецЕсли;
	КонецЕсли;
	
	Если ВсегоРасшифровано > 0 Тогда
		ПараметрыДлительногоОбмена = ДлительнаяОтправкаКлиент.ПараметрыДлительногоОбмена();
		ПараметрыДлительногоОбмена.ЭтоРасшифровка 			= Истина;
		ПараметрыДлительногоОбмена.ОбщееКоличествоЭтапов 	= ВсегоРасшифровано;
		ПараметрыДлительногоОбмена.ЭтоАвторасшифровка 		= Истина;
		
		ДлительнаяОтправкаКлиент.ПоказатьФормуДлительногоОбмена(ПараметрыДлительногоОбмена);
		
		ОтобразитьКонечныйРезультатРасшифровки(
			Неопределено,
			ВсегоРасшифровано,
			0,
			ВсегоРасшифровано);
			
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"НачатьДлительнуюОперациюПоРасшифровкеСообщений",
			ЭтотОбъект,
			ВходящийКонтекст);
		
		ПараметрыПоказа = ВходящийКонтекст.ПараметрыПоказа;
		Если ВходящийКонтекст.ТихаяОбработкаПоУмолчанию Тогда
			Результат = СобратьСообщенияДляРасшифровкиНаКлиенте(ВходящийКонтекст.ИтогиПоСообщениям, ПараметрыПоказа);
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
			
		ИначеЕсли ПараметрыПоказа <> Неопределено И ПараметрыПоказа.ПоказДляДругихПользователей Тогда
			ОткрытьФорму(
				ПутьКОбъекту + ".Форма.ПоказСообщенийДляДругихПользователей",
				ВходящийКонтекст,,,,,
				ОписаниеОповещения);
			
		Иначе
			ОткрытьФорму(
				ПутьКОбъекту + ".Форма.ПоказСообщенийТребующихРасшифровки",
				ВходящийКонтекст,,,,,
				ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСообщенияДляРасшифровки(МассивСообщений, ПараметрыПоказа = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Результат = КонтекстЭДОСервер.ПолучитьСообщенияДляРасшифровки(МассивСообщений, ПараметрыПоказа);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СобратьСообщенияДляРасшифровкиНаКлиенте(ИтогиПоСообщениям, ПараметрыПоказа = Неопределено) Экспорт
	
	Результат = СобратьСообщенияДляРасшифровки(ИтогиПоСообщениям, ПараметрыПоказа);
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СобратьСообщенияДляРасшифровки(ИтогиПоСообщениям, ПараметрыПоказа = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Результат = КонтекстЭДОСервер.СобратьСообщенияДляРасшифровки(ИтогиПоСообщениям, ПараметрыПоказа);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ВыполнитьРасшифровкуПриНеобходимости(
		ТребующиеРасшифровки,
		ПараметрыПоказа = Неопределено,
		ТихаяОбработкаПоУмолчанию = Ложь) Экспорт
	
	Если ФормаРасшифровкиСообщенийОткрыта()
		ИЛИ ДлительнаяОтправкаКлиентСервер.ФормаДлительнойОтправкиОткрыта() Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ДетальнаяИнформацияОРасшифровке = ПолучитьСообщенияДляРасшифровки(ТребующиеРасшифровки, ПараметрыПоказа);
	ИнтерактивныйРежим = Истина;
	
	ПараметрыОперации = Новый Структура;
	ПараметрыОперации.Вставить("Сообщения", 		ТребующиеРасшифровки);
	ПараметрыОперации.Вставить("ИтогиПоСообщениям", ДетальнаяИнформацияОРасшифровке.ИтогиПоСообщениям);
	ПараметрыОперации.Вставить("ПараметрыПоказа", 	ПараметрыПоказа);
	
	Если ДетальнаяИнформацияОРасшифровке.Отказ Тогда
		ИнтерактивныйРежим = Ложь;
		
	ИначеЕсли ДетальнаяИнформацияОРасшифровке.ОтключеноПодтверждение И НЕ ПараметрыПоказа.ВсеСообщения Тогда
		Результат = СобратьСообщенияДляРасшифровки(ДетальнаяИнформацияОРасшифровке.ИтогиПоСообщениям, ПараметрыПоказа);
		Если Результат <> Неопределено Тогда
			ПараметрыОперации.Вставить("КонтекстЭДО", ЭтотОбъект);
			ПараметрыОперации.Вставить("ТихаяОбработкаПоУмолчанию", ТихаяОбработкаПоУмолчанию);
			ФоноваяОперацияПоРасшифровкеСообщений(Результат.АдресТребующиеРасшифровки, ПараметрыОперации);
			ИнтерактивныйРежим = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ИнтерактивныйРежим Тогда
		ВходящийКонтекст = Новый Структура("ПараметрыПоказа", ПараметрыПоказа);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"НачатьДлительнуюОперациюПоРасшифровкеСообщений",
			ЭтотОбъект,
			ВходящийКонтекст);
			
		Если ТихаяОбработкаПоУмолчанию Тогда
			Результат = СобратьСообщенияДляРасшифровкиНаКлиенте(ПараметрыОперации.ИтогиПоСообщениям, ПараметрыПоказа);
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
			
		ИначеЕсли ПараметрыПоказа <> Неопределено И ПараметрыПоказа.ПоказДляДругихПользователей Тогда
			ОткрытьФорму(
				ПутьКОбъекту + ".Форма.ПоказСообщенийДляДругихПользователей",
				ПараметрыОперации,,,,,
				ОписаниеОповещения);
			
		Иначе
			ОткрытьФорму(
				ПутьКОбъекту + ".Форма.ПоказСообщенийТребующихРасшифровки",
				ПараметрыОперации,,,,,
				ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
	Возврат НЕ ДетальнаяИнформацияОРасшифровке.Отказ;
	
КонецФункции

&НаКлиенте
Функция ПараметрыПоказаСообщенийТребующихРасшифровки(ТихаяОбработкаПоУмолчанию = Ложь) Экспорт
	
	Результат =
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПараметрыПоказаСообщенийТребующихРасшифровки();
	
	Результат.ПоказДляТекущегоПользователя = ТихаяОбработкаПоУмолчанию
		ИЛИ ПараметрыПриложения["БРО.БольшеНеПоказыватьСообщенияТребующиеРасшифровки"] <> Истина;
	Результат.ПоказДляДругихПользователей = НЕ ТихаяОбработкаПоУмолчанию
		И ПараметрыПриложения["БРО.РазрешенПоказДляДругихПользователей"] = Истина;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область РасшифровкаПослеАвтоообмена

&НаКлиенте
Процедура Подключаемый_РасшифроватьТребующиеРасшифровку() Экспорт
	
	Организация 					= ПараметрыРасшифровки.Организация;
	ВсегоСообщений 					= ПараметрыРасшифровки.ВсегоСообщений;
	КоличествоНеРасшифрованных 		= ПараметрыРасшифровки.КоличествоНеРасшифрованных;
	КоличествоРасшифрованных 		= ПараметрыРасшифровки.КоличествоРасшифрованных;
	НомерОбъекта 					= ПараметрыРасшифровки.НомерОбъекта;
	ПараметрыПоказа 				= ПараметрыРасшифровки.ПараметрыПоказа;
	
	Если ОбменПрерванПользователем Или НомерОбъекта + 1 > СообщенияТребующиеРасшифровки.Количество() Тогда
		
		ОтобразитьКонечныйРезультатРасшифровки(
			Организация,
			ВсегоСообщений,
			КоличествоНеРасшифрованных,
			КоличествоРасшифрованных);
		
	Иначе
		
		Сообщение = СообщенияТребующиеРасшифровки[НомерОбъекта];
		
		Организация = Сообщение.Организация;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Организация", 				Организация);
		ДополнительныеПараметры.Вставить("Сообщение", 					Сообщение);
		ДополнительныеПараметры.Вставить("ВсегоСообщений", 				ВсегоСообщений);
		ДополнительныеПараметры.Вставить("КоличествоНеРасшифрованных", 	КоличествоНеРасшифрованных);
		ДополнительныеПараметры.Вставить("КоличествоРасшифрованных", 	КоличествоРасшифрованных);
		ДополнительныеПараметры.Вставить("НомерОбъекта", 				НомерОбъекта);
		ДополнительныеПараметры.Вставить("ПараметрыПоказа", 			ПараметрыПоказа);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"РасшифроватьТребующиеРасшифровкуЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		Если НЕ ЗначениеЗаполнено(Сообщение.ТранспортноеСообщение) Тогда
			// пустая ссылка транспортного сообщения означает признак сообщения СЭДО
			РасшифроватьСообщенияСЭДО(
				ОписаниеОповещения,
				Организация,
				НомерОбъекта,
				ДополнительныеПараметры);
		Иначе
			Сообщения = ТребующиеВниманияРасшифровкиИНовые();
			Если Сообщения.ТребующиеРасшифровки.Найти(Сообщение.ТранспортноеСообщение) = Неопределено Тогда
				ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
			Иначе
				РасшифроватьТранспортноеСообщение(Сообщение.ТранспортноеСообщение, ОписаниеОповещения, ПараметрыПоказа);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифроватьТребующиеРасшифровкуЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	ВсегоСообщений 				= ВходящийКонтекст.ВсегоСообщений;
	КоличествоНеРасшифрованных 	= ВходящийКонтекст.КоличествоНеРасшифрованных;
	КоличествоРасшифрованных 	= ВходящийКонтекст.КоличествоРасшифрованных;
	НомерОбъекта 				= ВходящийКонтекст.НомерОбъекта;
	Сообщение 					= ВходящийКонтекст.Сообщение;
	Организация 				= ВходящийКонтекст.Организация;
	ПараметрыПоказа 			= ВходящийКонтекст.ПараметрыПоказа;
	
	Если Результат Тогда
		
		ПодтвердитьРасшифровку(Сообщение.ТранспортноеСообщение);
		КоличествоРасшифрованных = КоличествоРасшифрованных + 1;
		
		НараститьКоличествоРасшифрованныхИНерасшифрованных(Организация, Истина);

	Иначе
		КоличествоНеРасшифрованных = КоличествоНеРасшифрованных + 1;
		
		НараститьКоличествоРасшифрованныхИНерасшифрованных(Организация, , Истина);
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("КоличествоПройденныхЭтапов", 	КоличествоРасшифрованных + КоличествоНеРасшифрованных);
	ДополнительныеПараметры.Вставить("ОбщееКоличествоЭтапов", 		ВсегоСообщений);
	ДополнительныеПараметры.Вставить("ТекущаяОрганизация", 			Организация);
		
	ДлительнаяОтправкаКлиент.ОповеститьОСменеЭтапаРасшифровки(ДополнительныеПараметры);
	
	ПараметрыРасшифровки = Новый Структура;
	ПараметрыРасшифровки.Вставить("КоличествоРасшифрованных", 	КоличествоРасшифрованных);
	ПараметрыРасшифровки.Вставить("КоличествоНеРасшифрованных", КоличествоНеРасшифрованных);
	ПараметрыРасшифровки.Вставить("НомерОбъекта", 				НомерОбъекта + 1);
	ПараметрыРасшифровки.Вставить("Организация", 				Организация);
	ПараметрыРасшифровки.Вставить("ВсегоСообщений", 			ВсегоСообщений);
	ПараметрыРасшифровки.Вставить("ПараметрыПоказа", 			ПараметрыПоказа);
	
	ПодключитьОбработчикОжидания("Подключаемый_РасшифроватьТребующиеРасшифровку", 0.1, Истина);
	
КонецПроцедуры
	
&НаКлиенте
Процедура НараститьКоличествоРасшифрованныхИНерасшифрованных(
		Организация = Неопределено,
		НараститьКоличествоРасшифрованных = Ложь, 
		НараститьКоличествоНеРасшифрованных = Ложь)
	
	Отбор = Новый Структура("Организация", Организация);
	РезультатОтбора = ТаблицаРасшифровкиПоОрганизациям.НайтиСтроки(Отбор);
	Если РезультатОтбора.Количество() = 0 Тогда 
		Возврат;
	КонецЕсли;
	
	Если НараститьКоличествоРасшифрованных Тогда
		РезультатОтбора[0].КоличествоРасшифрованных = РезультатОтбора[0].КоличествоРасшифрованных + 1;
	КонецЕсли;
	
	Если НараститьКоличествоНеРасшифрованных Тогда
		РезультатОтбора[0].КоличествоНеРасшифрованных = РезультатОтбора[0].КоличествоНеРасшифрованных + 1;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ПодтвердитьРасшифровку(Сообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ПодтвердитьРасшифровку(Сообщение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтобразитьКонечныйРезультатРасшифровки(
	Организация,
	ВсегоСообщений,
	КоличествоНеРасшифрованных,
	КоличествоРасшифрованных)
	
	ДокументооборотСКОКлиент.ПодключениеОбработчикаОжиданияАвтообмена(Истина);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("КоличествоНеРасшифрованных", 	КоличествоНеРасшифрованных);
	ДополнительныеПараметры.Вставить("КоличествоРасшифрованных", 	КоличествоРасшифрованных);
	ДополнительныеПараметры.Вставить("ВсегоСообщений", 				ВсегоСообщений);
	
	ДлительнаяОтправкаКлиент.ОповеститьОЗавершенииРасшифровки(ДополнительныеПараметры);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ПоказатьВложениеФСГС(СтрВложение, ВыполняемоеОповещение = Неопределено) Экспорт
	
	Если СтрВложение.Свойство("ЭтоHTML") И СтрВложение.ЭтоHTML Тогда
		ПоказатьHTML(СтрВложение.ИмяФайла, СтрВложение.Данные);
	ИначеЕсли СтрВложение.Свойство("ЭтоXML") И СтрВложение.ЭтоXML
		И НЕ ЭтоВебКлиент() И НЕ ОбщегоНазначенияКлиент.ЭтоLinuxКлиент() И НЕ ОбщегоНазначенияКлиент.ЭтоMacOSКлиент() Тогда
		
		ПоказатьHTML(СтрВложение.ИмяФайла, СтрВложение.Данные, , Истина);
	ИначеЕсли СтрВложение.Свойство("ЭтоTXT") И СтрВложение.ЭтоTXT Тогда
		ПоказатьТекстИзСтроки(СтрВложение.ТекстВложения, СтрВложение.ИмяФайла);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(СтрВложение.Адрес, СтрВложение.ИмяФайла);
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено ТОгда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьОписаниеСведенийФСГС(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// извлекаем ключевые параметры
	КлючевыеПараметрыФайлаВыгрузки = ОпределитьКлючевыеПараметрыФайлаВыгрузкиФСГС(Сообщение, Вложения);
	Если НЕ ЗначениеЗаполнено(КлючевыеПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "форма" - описание формы
	УзелФорма = ДобавитьУзелXML(Дерево, "форма");
	// добавляем узел "название" - название формы
	УзелНазвание = ДобавитьУзелXML(УзелФорма, "название",, КлючевыеПараметрыФайлаВыгрузки["название"]);
	// добавляем узел "идентификатор" - идентификатор формы
	УзелИдентификатор = ДобавитьУзелXML(УзелФорма, "идентификатор",, КлючевыеПараметрыФайлаВыгрузки["идентификатор"]);
	
	// добавляем узел "год" - год периода, за который предоставляется отчет
	УзелГод = ДобавитьУзелXML(Дерево, "год",, КлючевыеПараметрыФайлаВыгрузки["год"]);
	
	// добавляем узел "типПериода" - тип периода, принимает значения "месяц", "квартал", "год"
	УзелТипПериода = ДобавитьУзелXML(Дерево, "типПериода",, КлючевыеПараметрыФайлаВыгрузки["типПериода"]);
	
	// добавляем узел "номерПериода" - порядковый номер периода в году
	УзелНомерПериода = ДобавитьУзелXML(Дерево, "номерПериода",, КлючевыеПараметрыФайлаВыгрузки["номерПериода"]);
	
	// добавляем узел "номерОтчета" - порядковый номер отчета в указанном периоде
	УзелНомерОтчета = ДобавитьУзелXML(Дерево, "номерОтчета",, КлючевыеПараметрыФайлаВыгрузки["номерОтчета"]);
	
	// добавляем узел "видОтчета" - вид отчета, принимает значения "статистический", "бухгалтерский"
	УзелВидОтчета = ДобавитьУзелXML(Дерево, "видОтчета",, КлючевыеПараметрыФайлаВыгрузки["видОтчета"]);
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описаниеОтчета");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьКлючевыеПараметрыФайлаВыгрузкиФСГС(Сообщение, Вложения)
	
	// ищем файл выгрузки
	СтрФайлВыгрузки = Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС, "Тип");
	Если СтрФайлВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось обнаружить файл выгрузки среди вложений сообщения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	ФайлыВыгрузки = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, , СтрФайлВыгрузки.ИмяФайла);
	
	// сохраняем файл выгрузки во временный файл
	ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
	Попытка
		ФайлыВыгрузки[0].Данные.Получить().Записать(ИмяВременногоФайлаВыгрузки);
	Исключение
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось сохранить файл выгрузки во временный каталог.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецПопытки;
	
	// разбираем содержимое файла выгрузки
	ПараметрыФайлаВыгрузки = СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведенийФСГС(ИмяВременногоФайлаВыгрузки);
	Если НЕ ЗначениеЗаполнено(ПараметрыФайлаВыгрузки) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ЦиклОбмена = Сообщение.ЦиклОбмена;
	
	// определяем наименование отчета
	ВидОтчета = ЦиклОбмена.ВидОтчета;
	Если ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СтатистикаПрочиеФормы
	   И ТипЗнч(ЦиклОбмена.Предмет) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		НазваниеФормы = ЦиклОбмена.Предмет.НаименованиеОтчета;
	Иначе
		НазваниеФормы = ?(ПустаяСтрока(ВидОтчета.Описание), СокрЛП(ВидОтчета.Наименование), СокрЛП(ВидОтчета.Описание));
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(НазваниеФормы) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить название представляемой формы.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	ЭтоБухгалтерскаяОтчетность = (ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СтатистикаБухОтчетность);
	
	// ищем идентификатор формы
	Если ЭтоБухгалтерскаяОтчетность Тогда
		ИдентификаторФормы = ПараметрыФайлаВыгрузки["КНД"];
	Иначе
		ИдентификаторФормы = ПараметрыФайлаВыгрузки["code"];
	КонецЕсли;
	
	Если ИдентификаторФормы = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить идентификатор формы по файлу выгрузки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем год
	Год = Формат(Год(ЦиклОбмена.ДатаНачалаПериода), "ЧГ=");
	Если НЕ ЗначениеЗаполнено(Год) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить отчетный год.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	Если ЭтоБухгалтерскаяОтчетность Тогда
		ГодИзПараметров = ПараметрыФайлаВыгрузки["ОтчетГод"];
	Иначе
		ГодИзПараметров = ПараметрыФайлаВыгрузки["year"];
	КонецЕсли;
	
	Если Год <> ГодИзПараметров Тогда
		ТекстСообщения = НСтр("ru = 'Год периода отчета не совпадает с годом в параметрах файла выгрузки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	// определяем тип периода - "месяц", "квартал", "год" и др..
	Если ЭтоБухгалтерскаяОтчетность Тогда
		ТипПериода = "год";
	Иначе
		ТипПериода = "месяц";
		КодТипаПериода = Прав(ИдентификаторФормы, 3);
		Если КодТипаПериода = "001" Тогда
			ТипПериода = "год";
		ИначеЕсли КодТипаПериода = "002" Тогда
			ТипПериода = "полугодие";
		ИначеЕсли КодТипаПериода = "004" Тогда
			ТипПериода = "квартал";
		КонецЕсли; 
	КонецЕсли;
	
	// определяем номер периода в году
	Если ЭтоБухгалтерскаяОтчетность Тогда
		НомерПериода = "1"; // годовая
	Иначе
		НомерПериода = ПараметрыФайлаВыгрузки["period"];
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(НомерПериода) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать описание сведений: не удалось определить порядковый номер периода в году.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// определяем порядковый номер отчета в указанном периоде
	НомерОтчета = 1;
	Попытка
		НомерОтчета = 1 + ЦиклОбмена.Предмет.Вид;
	Исключение КонецПопытки;
	
	Если ЭтоБухгалтерскаяОтчетность Тогда
		ВидОтчетаСтр = "бухгалтерский";
	Иначе
		ВидОтчетаСтр = "статистический";
	КонецЕсли;
	
	// удаляем временный файл выгрузки
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайлаВыгрузки);
	
	Возврат Новый Структура("название,      идентификатор,      год, типПериода, номерПериода, номерОтчета, видОтчета",
							 НазваниеФормы, ИдентификаторФормы, Год, ТипПериода, НомерПериода, НомерОтчета, ВидОтчетаСтр);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведенийФСГС(ФайлВыгрузки)

	РегламентированнаяОтчетностьКлиентСервер.ПроверитьПолноеИмяВременногоФайла(
		ФайлВыгрузки,
		"ФайлВыгрузки",
		"СформироватьСтруктуруПараметровФайлаВыгрузкиДляОписанияСведенийФСГС");	
	
	ПоказателиВыгрузки = Новый Соответствие;
	ОбъектЧтениеXML = Новый ЧтениеXML;
	Попытка
		//@skip-check 1c-fresh-audit
		ОбъектЧтениеXML.ОткрытьФайл(ФайлВыгрузки);
		ОбъектЧтениеXML.ИгнорироватьПробелы = Ложь;
		Пока ОбъектЧтениеXML.Прочитать() Цикл
			Если ОбъектЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента Тогда
				Пока ОбъектЧтениеXML.ПрочитатьАтрибут() Цикл
					Если ПоказателиВыгрузки[ОбъектЧтениеXML.Имя] = Неопределено Тогда
						ПоказателиВыгрузки.Вставить(СтрЗаменить(ОбъектЧтениеXML.Имя, "-", "_"), ОбъектЧтениеXML.Значение);
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;	
		ОбъектЧтениеXML.Закрыть();
		ПоказателиВыгрузки.Вставить("ЭтоXML", Истина);
	Исключение
		Текст = Новый ЧтениеТекста;
		Текст.Открыть(ФайлВыгрузки, "cp866");
		ТекСтр = Текст.ПрочитатьСтроку();
		Пока ТекСтр <> Неопределено Цикл
			ТекСтр = СокрЛП(ТекСтр);
			ВхождениеДвоеточия = СтрНайти(ТекСтр, ":");
			ИмяПоказателя = СокрЛП(Лев(ТекСтр, ВхождениеДвоеточия - 1));
			ЗначениеПоказателя = СокрЛП(Сред(ТекСтр, ВхождениеДвоеточия + 1));
			Если ВхождениеДвоеточия <> 0 Тогда
				Если ПоказателиВыгрузки[ИмяПоказателя] = Неопределено Тогда
					ПоказателиВыгрузки.Вставить(ИмяПоказателя, ЗначениеПоказателя);
				КонецЕсли;
			КонецЕсли;
			ТекСтр = Текст.ПрочитатьСтроку();
		КонецЦикла;
		Текст.Закрыть();
		ПоказателиВыгрузки.Вставить("ЭтоXML", Ложь);
	КонецПопытки;
	Возврат ПоказателиВыгрузки;

КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеСведенийДляСообщенияФСГС(Контекст)
	
	Сообщение = Контекст.ТранспортноеСообщение;

	ТипыВложений = Новый Массив;
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС);
	Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
	
	Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС, "Тип") <> Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	// генерируем файл
	КороткоеИмяФайла = Неопределено;
	ФайлОписаниеСведений = СформироватьОписаниеСведенийФСГС(Сообщение, Вложения, КороткоеИмяФайла);
	Если ФайлОписаниеСведений = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл в содержимое
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
		Сообщение,
		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС,
		ФайлОписаниеСведений,
		КороткоеИмяФайла,,,,,,,
		СгенерироватьUUID(),
		Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетФСГС_СформироватьОписаниеДокументовПакета(Контекст)
	
	//
	Сообщение = Контекст.ТранспортноеСообщение;
	
	// определяем структуру с отложенными командами
	ОтложенныеКоманды = Новый СписокЗначений;
	
	// создаем вспомогательное дерево описания документов пакета
	ДеревоДокументов = СоздатьДеревоОписанияДокументовПакета();
	
	// создаем временный каталог для выгрузки файлов
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();

	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	//
	Если Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС Тогда
		
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить документ во временный каталог для подписания.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания сначала отчетами и описанием сведений
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС
			И ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеФайлаОтчетностиФСГС Тогда
				Продолжить;
			КонецЕсли;
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = КонтекстЭДОСервер.ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			ТипДокумента = ВложениеДИВ.Тип;
			
			Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС Тогда
				// шифруем файл
				ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
				// определяем документ в дереве описания
				СтрДокумент = КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, ТипДокумента, "xml", ВложениеДИВ.Идентификатор, Истина, Истина, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			Иначе
				// определяем документ в дереве описания
				СтрДокумент = КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, ТипДокумента, "xml", ВложениеДИВ.Идентификатор, Истина, Ложь, , КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			КонецЕсли;
		
		КонецЦикла;
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС Тогда
		
		// выгружаем в него все файлы сообщения
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить файлы пакета во временный каталог.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// извлекаем из сообщения-основания информацию о вложениях и заполняем дерево описания сначала ДИВами
		ДИВыОснования = ПолучитьВложенияТранспортногоСообщения(Сообщение.Основание, , , ВложенияДИВ.ВыгрузитьКолонку("ЭЦПИмяПодписанногоФайла"));
		Для Каждого СтрДИВОснование Из ДИВыОснования Цикл
			КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, СтрДИВОснование.Тип, КонтекстЭДОСервер.ТипСодержимогоПоОписаниюДИВ(СтрДИВОснование, Истина), СтрДИВОснование.Идентификатор, Истина, Ложь, СтрДИВОснование.ИмяФайла, , ВложениеДИВ.Адрес);
		КонецЦикла;
		
	ИначеЕсли Сообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС Тогда
		
		// выгружаем в него все файлы сообщения
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить файлы пакета во временный каталог.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания сначала отчетами и описанием сведений
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС Тогда
				Продолжить;
			КонецЕсли;
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = КонтекстЭДОСервер.ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			// определяем документ в дереве описания
			СтрДокумент = КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, "xml", ВложениеДИВ.Идентификатор, Истина, Ложь, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
		
		КонецЦикла;
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС Тогда
		
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить документ во временный каталог для подписания.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания ДИВ'ами
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись Тогда
				Продолжить;
			КонецЕсли;
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = КонтекстЭДОСервер.ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС Тогда
				
				// шифруем файл
				ОтложенныеКоманды.Добавить(ВложениеДИВ.Адрес, "Зашифровать");
				
				// добавляем в дерево описания
				Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС Тогда
					ТипСодержимого = "plain1251";
				ИначеЕсли ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС Тогда
					ТипСодержимого = ОпределитьТипСодержимогоПриложенияКПисьму(ВложениеДИВ);
				КонецЕсли;
				СтрДокумент = КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, ТипСодержимого, ВложениеДИВ.Идентификатор, Истина, Истина, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
				
			Иначе
				// определяем документ в дереве описания
				СтрДокумент = КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, "xml", ВложениеДИВ.Идентификатор, Истина, Ложь, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
			КонецЕсли;
			
		КонецЦикла;
		
	ИначеЕсли Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
		
		// выгружаем в него все файлы сообщения
		ТипыСодержимого = Новый Массив;
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС);
		ТипыСодержимого.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		ВложенияДИВ = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, ТипыСодержимого);
		ВложенияДИВ.Колонки.Добавить("Адрес");
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			Попытка
				ВложениеДИВ.Адрес = ПоместитьВоВременноеХранилище(ВложениеДИВ.Данные.Получить(), Контекст.УникальныйИдентификатор);
			Исключение
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				ТекстСообщения = НСтр("ru = 'Ошибка формирования пакета: не удалось сохранить файлы пакета во временный каталог.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецПопытки;
		КонецЦикла;
		
		// заполняем дерево описания сначала отчетами и описанием сведений
		Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
			
			Если ВложениеДИВ.Тип <> Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ИзвещениеОПолученииДокументаФСГС Тогда
				Продолжить;
			КонецЕсли;
			
			// пакуем файл
			КороткоеИмяФайлаАрхива = КонтекстЭДОСервер.ЗаархивироватьФайлПакета(ВложениеДИВ, "file", Контекст.УникальныйИдентификатор);
			Если КороткоеИмяФайлаАрхива = Неопределено Тогда
				ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
				Возврат Ложь;
			КонецЕсли;
			
			// определяем документ в дереве описания
			СтрДокумент = КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ДеревоДокументов, ВложениеДИВ.Тип, "xml", ВложениеДИВ.Идентификатор, Истина, Ложь, ВложениеДИВ.ИмяФайла, КороткоеИмяФайлаАрхива, ВложениеДИВ.Адрес);
		
		КонецЦикла;
		
	КонецЕсли;
	
	// дополняем дерево описания информацией об ЭЦП
	Для Каждого ВложениеДИВ Из ВложенияДИВ Цикл
		Если ВложениеДИВ.Тип = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись Тогда
			ПодписанныйДокумент = ДеревоДокументов.Строки.Найти(ВложениеДИВ.ЭЦПИмяПодписанногоФайла, "ИмяФайлаДокумента");
			Если ПодписанныйДокумент <> Неопределено Тогда
				КонтекстЭДОСервер.ОпределитьДокументВДеревеОписания(ПодписанныйДокумент, , , , , , ВложениеДИВ.ИмяФайла, , ВложениеДИВ.Адрес);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	МЧД = "";
	ДанныеДоверенности = Неопределено;
	Если Контекст.ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС Тогда
		ПользовательМЧД = ?(Мультирежим.ЭтоМультиРежим(Контекст.УчетнаяЗапись),
			Пользователи.ТекущийПользователь(), Справочники.Пользователи.ПустаяСсылка());
		ТаблицаМЧД = ДокументооборотСКОВызовСервера.МЧДОбмена(Контекст.СсылкаОтправитель, "ФСГС", ПользовательМЧД);
		Если ТаблицаМЧД.Количество() <> 0 И ЗначениеЗаполнено(ТаблицаМЧД[0].Доверенность) Тогда
			ИмяТипаСправочникаМашиночитаемыеДоверенности = "МашиночитаемыеДоверенности";
			ПараметрыМЧДЕдиногоФормата = ДокументооборотСКОВызовСервера.ПараметрыМЧДЕдиногоФормата();
			ЭтоНомерДоверенности = (ТипЗнч(ТаблицаМЧД[0].Доверенность) = Тип("Строка"));
			ЕстьРеквизитНомерДоверенности = НЕ ЭтоНомерДоверенности
				И ДокументооборотСКОКлиентСервер.ПодсистемаЦПРРМЧДСуществует()
				И Метаданные.Справочники[ИмяТипаСправочникаМашиночитаемыеДоверенности].Реквизиты.Найти(
				ПараметрыМЧДЕдиногоФормата.РеквизитНомерДоверенности) <> Неопределено;
			Если (ЕстьРеквизитНомерДоверенности ИЛИ ЭтоНомерДоверенности)
				И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Исп.МЧД для Росстата", Истина) Тогда
				
				Если ЭтоНомерДоверенности Тогда
					Если ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Расш.МЧД для Росстата",
						'20250501') Тогда
						
						ТекстСообщения = НСтр("ru = 'Для отправки отчетности представителем требуется выбрать машиночитаемую доверенность для Росстата, поддержка указания только номера доверенности прекращена'");
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
						Возврат Ложь;
						
					Иначе
						МЧД = ТаблицаМЧД[0].Доверенность;
					КонецЕсли;
					
				Иначе
					РеквизитыМЧДЕдиногоФормата = ПараметрыМЧДЕдиногоФормата.РеквизитНомерДоверенности;
					ЕстьРеквизитДатаВыдачи = Метаданные.Справочники[ИмяТипаСправочникаМашиночитаемыеДоверенности].Реквизиты.Найти(
						ПараметрыМЧДЕдиногоФормата.РеквизитДатаВыдачи) <> Неопределено;
					Если ЕстьРеквизитДатаВыдачи Тогда
						РеквизитыМЧДЕдиногоФормата = РеквизитыМЧДЕдиногоФормата + ", " + ПараметрыМЧДЕдиногоФормата.РеквизитДатаВыдачи;
					КонецЕсли;
					ЕстьРеквизитДатаОкончания = Метаданные.Справочники[ИмяТипаСправочникаМашиночитаемыеДоверенности].Реквизиты.Найти(
						ПараметрыМЧДЕдиногоФормата.РеквизитДатаОкончания) <> Неопределено;
					Если ЕстьРеквизитДатаОкончания Тогда
						РеквизитыМЧДЕдиногоФормата = РеквизитыМЧДЕдиногоФормата + ", "
							+ ПараметрыМЧДЕдиногоФормата.РеквизитДатаОкончания;
					КонецЕсли;
					
					СвойстваМЧДЕдиногоФормата = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ТаблицаМЧД[0].Доверенность,
						РеквизитыМЧДЕдиногоФормата);
					
					Если ЕстьРеквизитДатаВыдачи ИЛИ ЕстьРеквизитДатаОкончания Тогда
						ДатаСеанса = ТекущаяДатаСеанса();
						Если ЕстьРеквизитДатаОкончания
							И СвойстваМЧДЕдиногоФормата[ПараметрыМЧДЕдиногоФормата.РеквизитДатаОкончания] < ДатаСеанса Тогда
							
							ТекстСообщения = НСтр("ru = 'Истек срок действия доверенности'");
							ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
							Возврат Ложь;
							
						ИначеЕсли ЕстьРеквизитДатаВыдачи
							И СвойстваМЧДЕдиногоФормата[ПараметрыМЧДЕдиногоФормата.РеквизитДатаВыдачи] > ДатаСеанса Тогда
							
							ТекстСообщения = НСтр("ru = 'Дата начала действия доверенности еще не наступила'");
							ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
							Возврат Ложь;
						КонецЕсли;
					КонецЕсли;
					
					ТехническийСтатус =
						ДокументооборотСКОВызовСервера.СвойстваМЧДЕдиногоФормата(ТаблицаМЧД[0].Доверенность).ТехническийСтатус;
					Если ТехническийСтатус <> Неопределено И ТехническийСтатус =
						ДокументооборотСКОВызовСервера.ТехническиеСтатусыМЧДЕдиногоФормата("Отменена") Тогда
						
						ТекстСообщения = НСтр("ru = 'Доверенность отозвана'");
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
						Возврат Ложь;
					КонецЕсли;
					
					МЧД = СвойстваМЧДЕдиногоФормата[ПараметрыМЧДЕдиногоФормата.РеквизитНомерДоверенности];
					
					Если ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Расш.МЧД для Росстата",
						'20250501') Тогда
						
						ДанныеДоверенности = Справочники.МашиночитаемыеДоверенностиРаспределенныйРеестр.ДанныеМЧДЕдиногоФормата(
							ТаблицаМЧД[0].Доверенность);
						Если ДанныеДоверенности = Неопределено Тогда
							Возврат Ложь;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
			
		Иначе
			Если КриптографияЭДКОКлиентСервер.ТипСубъектаСертификата(Контекст.ОбъектСертификатРуководителя.Владелец,
				Контекст.СсылкаОтправитель) = "ФЛ" Тогда
				
				ТипОрганизации = ДокументооборотСКОВызовСервера.ТипОрганизации(Контекст.СсылкаОтправитель);
				
				Если ТипОрганизации.ЭтоИностраннаяОрганизация Тогда
					ЭтоОрганизацияОбязательнойМЧДРосстата =
						ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Обяз.МЧД ин.для Росстата");
				ИначеЕсли ТипОрганизации.ЭтоИП Тогда
					ЭтоОрганизацияОбязательнойМЧДРосстата =
						ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Обяз.МЧД ИП для Росстата", '20250801');
				ИначеЕсли ТипОрганизации.ЭтоФилиал Тогда
					ЭтоОрганизацияОбязательнойМЧДРосстата =
						ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Обяз.МЧД фил.для Росстата", '20250501');
				Иначе
					ЭтоОрганизацияОбязательнойМЧДРосстата =
						ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Обяз.МЧД для Росстата", '20250601');
				КонецЕсли;
				
				Если ЭтоОрганизацияОбязательнойМЧДРосстата Тогда
					ТекстСообщения =
						НСтр("ru = 'В связи с вступившими в силу изменениями, для отправки отчета укажите машиночитаемую доверенность для Росстата в настройках обмена с контролирующими органами.'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					КонтекстОснования = Новый ФиксированнаяСтруктура("АдресСсылки", "НастройкиМЧДДляФСГС");
					ТекстСообщения = НСтр("ru = 'Настройка машиночитаемых доверенностей для Росстата'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
						ТекстСообщения,,,,,
						КонтекстЭДОСервер.НовыйКонтекстОбработки(КонтекстОснования));
					Возврат Ложь;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// формируем файл описания пакета
	ФайлОписаниеПакета = СформироватьОписаниеПакетаФСГС(ДеревоДокументов,
														Контекст.UUIDПакета,
														Контекст.ТипДокументооборота,
														Контекст.ТипТранзакции,
														Контекст.КодОтправителя,
														Контекст.КодПолучателя,
														Контекст.КодСистемыОтправителя,
														Контекст.УникальныйИдентификатор,
														?(ДанныеДоверенности = Неопределено, МЧД, ДанныеДоверенности));
	Если ФайлОписаниеПакета = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Контекст.Вставить("ОтложенныеКоманды", ОтложенныеКоманды);
	Контекст.Вставить("ДеревоДокументов", ПоместитьВоВременноеХранилище(ДеревоДокументов, Контекст.УникальныйИдентификатор));
	Контекст.Вставить("ФайлОписаниеПакета", ФайлОписаниеПакета);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетФСГС_ЗаключительныеДействияНаСервере(Контекст)
	
	// получаем дерево документов
	ДеревоДокументов = ПолучитьИзВременногоХранилища(Контекст.ДеревоДокументов);
	
	// создаем каталог, в который будем помещать пакуемые файлы
	КаталогФайловОтчетности = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
	
	// создаем файл пакета
	КодИлиРегионПолучателя = РазделитьКодФСГС(Контекст.КодПолучателя).идентификаторСубъекта;
	КороткоеИмяФайлаПакета = Контекст.ПрефиксИмениФайлаКонтейнера + ?(ПустаяСтрока(Контекст.ПрефиксИмениФайлаКонтейнера), "", ".") + "STAT_" +
							ВРег(Контекст.КодОтправителя) + "_" +
							ВРег(КодИлиРегионПолучателя) + "_" +
							СгенерироватьUUID() + "_" + 
							Контекст.КодТипаДокументооборота + "_" +
							Контекст.КодТипаТранзакции + 
							".zip";

	ПолноеИмяФайлаПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайлаПакета;
	
	// создаем архив
	ЗаписьZIP = Новый ЗаписьZipФайла(ПолноеИмяФайлаПакета); // для ФСГС сжатие включено

	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// добавляем файла из дерева описания пакета
	Для Каждого СтрДокумент Из ДеревоДокументов.Строки Цикл
		ПолноеИмяДобавляемогоФайла = КонтекстЭДОСервер.ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(
			КаталогФайловОтчетности, СтрДокумент);
		Если НЕ ЗначениеЗаполнено(ПолноеИмяДобавляемогоФайла) Тогда
			Возврат Ложь;
		КонецЕсли;
		ЗаписьZIP.Добавить(ПолноеИмяДобавляемогоФайла);
		Для Каждого СтрДокумент2 Из СтрДокумент.Строки Цикл
			ПолноеИмяДобавляемогоФайла = КонтекстЭДОСервер.ВыгрузитьФайлПакетаИзВременногоХранилищаВКаталог(
				КаталогФайловОтчетности, СтрДокумент2);
			Если НЕ ЗначениеЗаполнено(ПолноеИмяДобавляемогоФайла) Тогда
				Возврат Ложь;
			КонецЕсли;
			ЗаписьZIP.Добавить(ПолноеИмяДобавляемогоФайла);
		КонецЦикла;
	КонецЦикла;
	
	// добавляем само описание пакета
	ПолноеИмяФайлаОписанияПакета = КаталогФайловОтчетности + "packageDescription.xml";
	ПолучитьИзВременногоХранилища(Контекст.ФайлОписаниеПакета).Записать(ПолноеИмяФайлаОписанияПакета);
	ЗаписьZIP.Добавить(ПолноеИмяФайлаОписанияПакета);
	
	// записываем архив
	ЗаписьZIP.Записать();
	
	// добавляем архив в регистр
	ДобавитьТранспортныйКонтейнер(Контекст.ТранспортноеСообщение, ПолноеИмяФайлаПакета, КороткоеИмяФайлаПакета);
	
	// удаляем временный каталог
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(КаталогФайловОтчетности);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОКПОСтатОтчета(Сообщение)
	КодОтправителя = "";
	ИспользуетсяМеханизмНесколькихНаправлений = Ложь;
	Попытка
		РегламентированнаяОтчетностьПереопределяемый.ИспользуетсяОтправкаВРосстатПоРазнымНаправлениям(ИспользуетсяМеханизмНесколькихНаправлений);
		Если ИспользуетсяМеханизмНесколькихНаправлений Тогда 
			РеглОтчет = Сообщение.ЦиклОбмена.Предмет;
			ДанныеОтчета = РеглОтчет.ДанныеОтчета.Получить();
			Если ДанныеОтчета.Свойство("КодПодразделенияФСГС_8_14", КодОтправителя) Тогда 
				Если Не ЗначениеЗаполнено(СокрЛП(КодОтправителя)) Тогда
					КодОтправителя = ПолучитьКодОКПООрганизации(Сообщение);
				КонецЕсли;
				Возврат КодОтправителя;
			КонецЕсли;
			
			ОбособленноеПодразделение = ДанныеОтчета.ОбособленноеПодразделение;
			РегламентированнаяОтчетностьПереопределяемый.ПолучитьКодОКПОПодразделения(ОбособленноеПодразделение, КодОтправителя);
			Если Не ЗначениеЗаполнено(СокрЛП(КодОтправителя)) Тогда
				КодОтправителя = ПолучитьКодОКПООрганизации(Сообщение);
			КонецЕсли;
		Иначе
			КодОтправителя = ПолучитьКодОКПООрганизации(Сообщение);
		КонецЕсли;
	Исключение
		КодОтправителя = ПолучитьКодОКПООрганизации(Сообщение);
	КонецПопытки;
	
	Возврат КодОтправителя;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКодОКПООрганизации(Сообщение)
	ИмяРеквКодПоОКПО = "КодПоОКПО";
	Если Метаданные.Справочники.Организации.Реквизиты.Найти("КодОКПО") <> Неопределено Тогда
		ИмяРеквКодПоОКПО = "КодОКПО";
	КонецЕсли;
	Возврат СокрЛП(Сообщение.Отправитель[ИмяРеквКодПоОКПО]);
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетФСГС_ОбщиеПроверкиПередФормированием(Контекст)
	
	Сообщение = Контекст.ТранспортноеСообщение;
	
	// проверка на то, что сообщение исходящее
	СтатусТранспортногоСообщения = Сообщение.Статус;
	Если СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Сохраненное
	И СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Исходящее
	И СтатусТранспортногоСообщения <> Перечисления.СтатусыПисем.Отправленное Тогда
		ТекстСообщения = НСтр("ru = 'Невозможно формирование пакета по входящему сообщению!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ПризнакОшибки = Ложь;
	
	ТипСообщения = Сообщение.Тип;
		
	СсылкаОтправитель = Сообщение.Отправитель;
	СсылкаПолучатель = ОпределитьПолучателяТранспортногоКонтейнера(Сообщение);
	УчетнаяЗапись = Сообщение.УчетнаяЗапись;
	ЦиклОбмена = Сообщение.ЦиклОбмена;
	СообщениеОснование = Сообщение.Основание;
	
	Если НЕ ЗначениеЗаполнено(ТипСообщения) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: тип транспортного сообщения не определен!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ПризнакОшибки = Истина;
	КонецЕсли;
				
	Если НЕ ЗначениеЗаполнено(СсылкаОтправитель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задан отправитель!'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					ПризнакОшибки = Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СсылкаПолучатель) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задан получатель!'");
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
						ПризнакОшибки = Истина;
					КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: не задана учетная запись!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	ИначеЕсли НЕ УчетнаяЗапись.ПредназначенаДляДокументооборотаСФСГС Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: учетная запись не предназначена для электронного документооборота с Росстат!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
				КонецЕсли;
				
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		ТекстСообщения = НСтр("ru = 'Ошибка шифрования транспортного сообщения: цикл обмена не задан!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ПризнакОшибки = Истина;
	КонецЕсли;
			
	// определяем сертификат получателя
	Если ТипЗнч(СсылкаПолучатель) <> Тип("СправочникСсылка.ОрганыФСГС") Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Получатель <%1> не является органом ФСГС.'"), СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);		
		Возврат Ложь;	
	КонецЕсли;
	
	СертификатПолучателя = ПолучитьСертификатСубъекта(СсылкаПолучатель);
	Если СертификатПолучателя.Количество() = 0 Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В свойствах территориального органа ФСГС %1 не задан ни один сертификат.'"), СсылкаПолучатель);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// определяем сертификаты отправителя
	Сертификат = Мультирежим.СертификатПользователя(УчетнаяЗапись, "Отпечаток");
	Если НЕ Сертификат.Найден Тогда
		ТекстСообщения = Сертификат.ТекстОшибки;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// получаем и проверяем код отправителя
	КодОтправителя = ПолучитьОКПОСтатОтчета(Сообщение);
	Если ПустаяСтрока(КодОтправителя) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан код ОКПО организации-отправителя.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ КодОКПОСоответствуетТребованиям(КодОтправителя) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректно задан код ОКПО организации-отправителя.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// получаем и проверяем код получателя
	КодПолучателя = СокрЛП(СсылкаПолучатель.Код);
	Если ПустаяСтрока(СтрЗаменить(СокрЛП(КодПолучателя), "-", "")) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан код территориального органа ФСГС.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ КодОрганаФСГССоответствуетТребованиям(КодПолучателя) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректно задан код территориального органа ФСГС.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
			КонецЕсли;
			
	Если ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС Тогда
		ПрефиксИмениФайлаКонтейнера = "";
		ТипДокументооборота = "отчетСтат";
		КодТипаДокументооборота = "4";
		ТипТранзакции = "отчет";
		КодТипаТранзакции = "1";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС Тогда
		ПрефиксИмениФайлаКонтейнера = "";
		ТипДокументооборота = "отчетСтат";
		КодТипаДокументооборота = "4";
		ТипТранзакции = "подтверждениеКвитанция";
		КодТипаТранзакции = "6";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС Тогда
		ПрефиксИмениФайлаКонтейнера = "";
		ТипДокументооборота = "отчетСтат";
		КодТипаДокументооборота = "4";
		ТипТранзакции = "протоколИзвещение";
		КодТипаТранзакции = "4";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС Тогда
		ПрефиксИмениФайлаКонтейнера = "stat_unformal";
		ТипДокументооборота = "письмоРеспондент";
		КодТипаДокументооборота = "1";
		ТипТранзакции = "письмо";
		КодТипаТранзакции = "1";
	ИначеЕсли ТипСообщения = Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС Тогда
		ПрефиксИмениФайлаКонтейнера = "stat_unformal";
		ТипДокументооборота = "письмоОрганФСГС";
		КодТипаДокументооборота = "2";
		ТипТранзакции = "извещение";
		КодТипаТранзакции = "3";
		КонецЕсли;
		
	КодСистемыОтправителя = УчетнаяЗапись.ИдентификаторСистемыОтправителяФСГС;
	
	Если ПустаяСтрока(КодСистемыОтправителя) Тогда
		Если УчетнаяЗапись.СпецОператорСвязи = Перечисления.СпецоператорыСвязи.Такском Тогда
			КодСистемыОтправителя = "taxcom";
		ИначеЕсли УчетнаяЗапись.СпецОператорСвязи = Перечисления.СпецоператорыСвязи.КалугаАстрал Тогда
			КодСистемыОтправителя = "astralreport";
		ИначеЕсли УчетнаяЗапись.СпецОператорСвязи = Перечисления.СпецоператорыСвязи.Форус Тогда
			КодСистемыОтправителя = "astralreport";
		Иначе
			КодСистемыОтправителя = "1c";
		КонецЕсли;
	КонецЕсли;
	
	КодОтправителя = КодСистемыОтправителя + "." + КодОтправителя;

	UUIDПакета = СокрЛП(ЦиклОбмена.Идентификатор);
	
	// запоминаем в контексте
	Контекст.Вставить("ТипСообщения", ТипСообщения);
	Контекст.Вставить("СсылкаОтправитель", СсылкаОтправитель);
	Контекст.Вставить("СсылкаПолучатель", СсылкаПолучатель);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена);
	Контекст.Вставить("СертификатРуководителя", СокрЛП(Сертификат.Отпечаток)); //+
	Контекст.Вставить("СертификатОтветственногоЗаШифрование", СокрЛП(Сертификат.Отпечаток)); //+
	Контекст.Вставить("СертификатПолучателя", СертификатПолучателя);
	
	Контекст.Вставить("ТипДокументооборота", ТипДокументооборота);
	Контекст.Вставить("КодТипаДокументооборота", КодТипаДокументооборота);
	Контекст.Вставить("ТипТранзакции", ТипТранзакции);
	Контекст.Вставить("КодТипаТранзакции", КодТипаТранзакции);
	
	Контекст.Вставить("КодОтправителя", КодОтправителя);
	Контекст.Вставить("КодПолучателя", КодПолучателя);
	Контекст.Вставить("КодСистемыОтправителя", КодСистемыОтправителя);
	Контекст.Вставить("UUIDПакета", UUIDПакета);
	
	Контекст.Вставить("ПрефиксИмениФайлаКонтейнера", ПрефиксИмениФайлаКонтейнера);
	
	Контекст.Вставить("СообщениеОснование", СообщениеОснование);
	
	Возврат НЕ ПризнакОшибки;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазделитьКодФСГС(Знач КодФСГС)
	
	ЧистыйКодФСГС = СокрЛП(КодФСГС);
	Коды = Новый Структура("идентификаторСубъекта, идентификаторПодразделения");
	Если СтрДлина(ЧистыйКодФСГС) = 5 Тогда
		Коды.идентификаторСубъекта = ЧистыйКодФСГС;
		Коды.идентификаторПодразделения = "";
	Иначе
		Коды.идентификаторСубъекта = Лев(ЧистыйКодФСГС, 2);
		Коды.идентификаторПодразделения = Сред(ЧистыйКодФСГС, 4);
	КонецЕсли;
	
	Возврат Коды;
	
КонецФункции
	
&НаСервереБезКонтекста
Функция СформироватьОписаниеПакетаФСГС(
		ОписаниеДокументов,
		UUIDПакета = Неопределено,
		ТипДокументооборота = "отчетСтат",
		ТипТранзакции = "отчет",
		КодОтправителя = "",
		КодПолучателя = "",
		Знач КодСистемыОтправителя = Неопределено,
		ИдентификаторДляВременногоХранилища = Неопределено,
		МЧД = "") Экспорт
	
	Если UUIDПакета = Неопределено Тогда
		UUIDПакета = СгенерироватьUUID();
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(КодСистемыОтправителя) Тогда
		КодСистемыОтправителя = "1C";
	КонецЕсли;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "пакет"
	УзелПакет = ДобавитьУзелXML(Дерево, "пакет");
	ДобавитьУзелXML(УзелПакет, "версияФормата",							Истина, "Стат:1.0");
	ДобавитьУзелXML(УзелПакет, "типДокументооборота",					Истина, ТипДокументооборота);
	ДобавитьУзелXML(УзелПакет, "идентификаторДокументооборота", 		Истина, UUIDПакета);
	ДобавитьУзелXML(УзелПакет, "типТранзакции",							Истина, ТипТранзакции);
	
	// добавляем узел "отправитель"
	УзелОтправитель = ДобавитьУзелXML(УзелПакет, "отправитель");
	ДобавитьУзелXML(УзелОтправитель, "идентификаторСубъекта",			Истина, СокрЛП(КодОтправителя));
	ДобавитьУзелXML(УзелОтправитель, "типСубъекта",						Истина, "респондент");
	
	// добавляем узел "системаОтправителя"
	УзелСистемаОтправителя = ДобавитьУзелXML(УзелПакет, "системаОтправителя");
	ДобавитьУзелXML(УзелСистемаОтправителя, "идентификаторСубъекта",	Истина, СокрЛП(КодСистемыОтправителя));
	ДобавитьУзелXML(УзелСистемаОтправителя, "типСубъекта",				Истина, "оператор");
	
	// для новых необходимо выделение подразделения отдельным кодом
	КодыСубъектаПодразделения = РазделитьКодФСГС(КодПолучателя);
	
	// добавляем узел "Получатель"
	УзелПолучатель = ДобавитьУзелXML(УзелПакет, "получатель");
	ДобавитьУзелXML(УзелПолучатель, "идентификаторСубъекта",			Истина, КодыСубъектаПодразделения.идентификаторСубъекта);
	Если ЗначениеЗаполнено(КодыСубъектаПодразделения.идентификаторПодразделения) Тогда
		ДобавитьУзелXML(УзелПолучатель, "идентификаторПодразделения",			Истина, КодыСубъектаПодразделения.идентификаторПодразделения);
	КонецЕсли;
	ДобавитьУзелXML(УзелПолучатель, "типСубъекта",						Истина, "органФСГС");
	
	Если ЗначениеЗаполнено(МЧД) Тогда
		ДанныеДоверенности = ?(ТипЗнч(МЧД) = Тип("Структура") ИЛИ ТипЗнч(МЧД) = Тип("ФиксированнаяСтруктура"),
			МЧД, Неопределено);
		РегистрационныйНомерДоверенности = ?(ДанныеДоверенности = Неопределено,
			МЧД, ДанныеДоверенности.РегистрационныйНомерДоверенности);
		ИННДоверителя = ?(ДанныеДоверенности = Неопределено, "", ?(ЗначениеЗаполнено(ДанныеДоверенности.ДоверительЮЛ_ИНН),
			ДанныеДоверенности.ДоверительЮЛ_ИНН, ДанныеДоверенности.ДоверительФЛ_ИНН));
		ИННПредставителя = ?(ДанныеДоверенности = Неопределено, "", ?(ЗначениеЗаполнено(ДанныеДоверенности.ПредставительФЛ_ИНН),
			ДанныеДоверенности.ПредставительФЛ_ИНН, ДанныеДоверенности.ПредставительЮЛ_ИНН));
		РегистрационныйНомерРНД = ?(ДанныеДоверенности = Неопределено, "", ДанныеДоверенности.РегистрационныйНомерРНД);
		
		УзелРасширения = ДобавитьУзелXML(УзелПакет, "расширения");
		УзелДоверенность = ДобавитьУзелXML(УзелРасширения, "доверенность");
		ДобавитьУзелXML(УзелДоверенность, "идентификаторМЧД", Истина, РегистрационныйНомерДоверенности);
		Если ДанныеДоверенности <> Неопределено Тогда
			ДобавитьУзелXML(УзелДоверенность, "ИННдоверителя", Истина, ИННДоверителя);
			ДобавитьУзелXML(УзелДоверенность, "ИННпредставителя", Истина, ИННПредставителя);
			Если ЗначениеЗаполнено(РегистрационныйНомерРНД) Тогда
				ДобавитьУзелXML(УзелДоверенность, "идентификаторМЧДНотариус", Истина, РегистрационныйНомерРНД);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// Создадим контекст ЭДО один раз до начала цикла обхода, чтобы не тратить время на каждом шаге.
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// добавляем узлы "Документ"
	Для Каждого СтрДокумент Из ОписаниеДокументов.Строки Цикл
		
		// добавляем очередной узел "Документ"
		УзелДокумент = ДобавитьУзелXML(УзелПакет, "документ");
		ДобавитьУзелXML(УзелДокумент, "типДокумента",					Истина, КонтекстЭДОСервер.ИдентификаторПоТипуДокументаПакета(СтрДокумент.ТипДокумента));
		ДобавитьУзелXML(УзелДокумент, "типСодержимого",					Истина, СтрДокумент.ТипСодержимого);
		ДобавитьУзелXML(УзелДокумент, "сжат",							Истина, XMLСтрока(СтрДокумент.Сжат = Истина));
		ДобавитьУзелXML(УзелДокумент, "зашифрован",						Истина, XMLСтрока(СтрДокумент.Зашифрован = Истина));
		ДобавитьУзелXML(УзелДокумент, "идентификаторДокумента",			Истина, СтрДокумент.ИдентификаторДокумента);
		
		// добавляем атрибут "ИсходноеИмяФайла"
		Если ЗначениеЗаполнено(СтрДокумент.ИмяФайлаДокумента) Тогда
			ДобавитьУзелXML(УзелДокумент, "исходноеИмяФайла",			Истина, СтрДокумент.ИмяФайлаДокумента);
		КонецЕсли;
			
		// добавляем узел "Содержимое"
		Если ЗначениеЗаполнено(СтрДокумент.ИмяФайлаАрхива) Тогда
			УзелСодержимое = ДобавитьУзелXML(УзелДокумент, "содержимое");
			ДобавитьУзелXML(УзелСодержимое, "имяФайла",					Истина, СтрДокумент.ИмяФайлаАрхива);
		КонецЕсли;
		
		// добавляем подписи под документом
		Для Каждого СтрПодпись Из СтрДокумент.Строки Цикл
			
			// добавляем очередной узел "Подпись"
			УзелПодпись = ДобавитьУзелXML(УзелДокумент, "подпись");
			ДобавитьУзелXML(УзелПодпись, "имяФайла",					Истина, СтрПодпись.ИмяФайлаДокумента);
			ДобавитьУзелXML(УзелПодпись, "роль",						Истина, "респондент");
			
		КонецЦикла;
		
	КонецЦикла;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево);
	
	// записываем дерево в файл
	ПолноеИмяФайла = ПолучитьИмяВременногоФайла();
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайла);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// записываем двоичные данные файла во временное хранилище
	АдресВХранилище = ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ПолноеИмяФайла), ИдентификаторДляВременногоХранилища);
	
	Возврат АдресВХранилище;
	
КонецФункции

&НаСервереБезКонтекста
Функция КодОрганаФСГССоответствуетТребованиям(КодОрганаФСГС) Экспорт
	
	ЧистыйКодОрганаФСГС = СокрЛП(КодОрганаФСГС);
	КодНСИ = Лев(ЧистыйКодОрганаФСГС, 3);
	Возврат (СтрДлина(ЧистыйКодОрганаФСГС) >= 4) И (СтрокаСоответствуетШаблону(КодНСИ, "№№-"));
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеПисьмаДляСообщенияФСГС(Контекст)
	
	Сообщение = Контекст.ТранспортноеСообщение;
	
	ТипыВложений = Новый Массив;
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС);
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС);
	Вложения = ПолучитьВложенияТранспортногоСообщения(Сообщение, , ТипыВложений);
	
	// проверяем, не сформировано ли ранее описание письма
	Если Вложения.Найти(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС, "Тип") <> Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	// получаем строки вложений ЭЦП с данными
	ТипыВложений.Добавить(Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	ВложенияЭЦП = ПолучитьВложенияТранспортногоСообщения(Сообщение, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
	
	// добавляем строки описания ЭЦП к ранее полученным строкам описаний
	Вложения.Колонки.Добавить("Данные");
	Для Каждого СтрВложениеЭЦП Из ВложенияЭЦП Цикл
		НовСтр = Вложения.Добавить();
		ЗаполнитьЗначенияСвойств(НовСтр, СтрВложениеЭЦП);
	КонецЦикла;
	
	// генерируем файл
	КороткоеИмяФайла = Неопределено;
	ФайлОписаниеПисьма = СформироватьОписаниеПисьмаФСГС(Сообщение, Вложения, КороткоеИмяФайла);
	Если ФайлОписаниеПисьма = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// загружаем файл в содержимое
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
		Сообщение,
		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ОписаниеПисьмаФСГС,
		ФайлОписаниеПисьма,
		КороткоеИмяФайла,,,,,,,
		СгенерироватьUUID(),
		Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьОписаниеПисьмаФСГС(Сообщение, Вложения, КороткоеИмяФайла = Неопределено)
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	// добавляем узел "тема"
	УзелТема = ДобавитьУзелXML(Дерево, "тема", , XMLСтрока(СокрЛП(Сообщение.ЦиклОбмена.Предмет.Наименование)));
	
	// добавляем узел "ответНа"
	ИдентификаторОснования = Сообщение.ЦиклОбмена.Предмет.ИдентификаторОснования;
	Если ЗначениеЗаполнено(ИдентификаторОснования) Тогда
		УзелОтветНаПисьмо = ДобавитьУзелXML(Дерево, "ответНа", , XMLСтрока(ИдентификаторОснования));
	КонецЕсли;
	
	// формируем строку XML на основе дерева
	СтрокаXML = ВыгрузитьДеревоВXML_ПФР(Дерево, "описание");
	
	// записываем строку в файл
	КороткоеИмяФайла = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	ПолноеИмяФайлаДокументаВСоставеПакета = ПолучитьКаталогВременныхФайлов() + КороткоеИмяФайла;
	РезультатЗаписи = ЗаписатьXMLВФайл(СтрокаXML, ПолноеИмяФайлаДокументаВСоставеПакета);
	Если РезультатЗаписи = Ложь Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат ПолноеИмяФайлаДокументаВСоставеПакета;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИзвещениеОПолученииФСГС(ТранспортноеСообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьИзвещениеОПолученииФСГС(ТранспортноеСообщение);
	
КонецФункции
 
&НаСервереБезКонтекста
Функция КодОКПОСоответствуетТребованиям(КодОКПО)
	
	ВремКодОКПО = СокрЛП(КодОКПО);
	Если СтрДлина(ВремКодОКПО) = 14 Тогда
		Если НЕ СтрокаСоответствуетШаблону(ВремКодОКПО, "№№№№№№№№№№№№№№") Тогда 
			Возврат Ложь;
		КонецЕсли;
		ВремКодОКПО = Лев(ВремКодОКПО, 8);
	КонецЕсли;
	СтрКодОКПО = Прав("00000" + ВремКодОКПО, ?(СтрДлина(ВремКодОКПО) <= 8, 8, 10));
	
	Если НЕ СтрокаСоответствуетШаблону(СтрКодОКПО, "№№№№№№№№") И НЕ СтрокаСоответствуетШаблону(СтрКодОКПО, "№№№№№№№№№№") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	КонтрольнаяЦифра = Число(Прав(СтрКодОКПО, 1));
	ДлинаКодаРасчет = СтрДлина(СтрКодОКПО) - 1;
	СтрКодОКПОРасчет = Лев(СтрКодОКПО, ДлинаКодаРасчет);
	
	СдвигВесов = 0; КонтрольноеЧисло = 0;
	Для НомРасч = 1 По 2 Цикл
		СуммаПроизведений = 0;
		Для НомПоз = 1 По ДлинаКодаРасчет Цикл
			ВесРазряда = (НомПоз + СдвигВесов) % 10;
			Если ВесРазряда = 0 Тогда
				ВесРазряда = 10;
			КонецЕсли;
			ЦифраКодаОКПО = Число(Сред(СтрКодОКПОРасчет, НомПоз, 1));
			СуммаПроизведений = СуммаПроизведений + ЦифраКодаОКПО * ВесРазряда;
		КонецЦикла; 
		КонтрольноеЧисло = СуммаПроизведений % 11;
		Если КонтрольноеЧисло = 10 Тогда
			Если НомРасч = 1 Тогда
				СдвигВесов = 2;
				Продолжить;
			Иначе
				КонтрольноеЧисло = 0;
			КонецЕсли;	
		КонецЕсли;
		Прервать;
	КонецЦикла;
	
	Возврат Истина; //проверка отключена (КонтрольнаяЦифра = КонтрольноеЧисло);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьОрганФСГСОрганизации(Организация) Экспорт
	
	КодОрганаФСГС = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодОрганаФСГС").КодОрганаФСГС);
	Если НЕ ЗначениеЗаполнено(КодОрганаФСГС) Тогда
		Возврат Неопределено;
	Иначе
		Возврат Справочники.ОрганыФСГС.НайтиПоКоду(КодОрганаФСГС);
	КонецЕсли;
	
КонецФункции

#Область ОтправкаРегламентированногоОтчетаВФСГС

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФСГС(Док, ВыполняемоеОповещение) Экспорт
	
	Контекст = Новый Структура();
	Контекст.Вставить("Док",                   Док);
	Контекст.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФСГСПослеПодтвержденияПовторнойОтправки",
		ЭтотОбъект,
		Контекст);
	
	ПодтвердитьПовторнуюОтправку(Док, "ФСГС", ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФСГСПослеПодтвержденияПовторнойОтправки(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	// готовимся к выгрузке
	Если НЕ ОтправкаРегламентированногоОтчетаВФСГС_ПередВыгрузкойФайла(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Ложь); //+
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Ложь); //+
		Возврат;
		
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	
	// выгружаем отчет и доверенность, при необходимости
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("КонтекстОтправки", 		КонтекстОтправки);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФСГСПослеВыгрузки",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ВыгрузитьРегламентированныйОтчетФСГС(КонтекстОтправки, ОписаниеОповещения)// клиент
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФСГСПослеВыгрузки(Результат, ВходящийКонтекст) Экспорт
	
	Документ = ВходящийКонтекст.КонтекстОтправки.Документ;
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();

	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	РезультатВыгрузки     = Результат.РезультатВыгрузки;
	КонтекстОтправки      = Результат.Контекст;
	
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Если НЕ РезультатВыгрузки Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		Возврат;
		
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОтправкаРегламентированногоОтчетаВФСГСПослеАвтонастройкиУчетнойЗаписи", 
		ЭтотОбъект, 
		ВходящийКонтекст);

	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 	ОписаниеОповещения);
	КонтекстДлительнойОперации.Вставить("Документ", 			Документ);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Документ;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 6;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда //отправка отчета в Росстат
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаРегламентированногоОтчетаВФСГСПослеВыгрузки",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаРегламентированногоОтчетаВФСГСПослеВыгрузки() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Документ = КонтекстДлительнойОперации.Документ;
	ОписаниеОповещения 	= КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи..."); // ФСГС

	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Документ, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФСГСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФСГС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.КонтекстОтправки.Документ, ВходящийКонтекст.КонтекстОтправки.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФСГС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	КонтекстОтправки      = ВходящийКонтекст.КонтекстОтправки;
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРегламентированногоОтчетаВФСГС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", 	ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", 		КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВФСГСПослеФормированияПакета", ЭтотОбъект, Контекст);
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФСГС");

КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетФСГС(Контекст, ВыполняемоеОповещение)
	
	Результат = Новый Массив;
	
	Отчет = Контекст.Документ;
	ИмяМетаданныхДокумента = Контекст.ТипОтправляемогоДокумента;
	Если ИмяМетаданныхДокумента = "РегламентированныйОтчет" Тогда
		
		// выгружаем отчет
		ТекстВыгрузки = "";
		ИмяФайлаВыгрузки = "";
		КодировкаФайлаВыгрузки = Неопределено;
		РезультатВыгрузки = СдачаОтчетностиЧерезСервисСпецоператораКлиент.ВыгрузитьРегламентированныйОтчет(Отчет, Ложь, ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайлаВыгрузки, , , Контекст.УникальныйИдентификатор);
		Если НЕ РезультатВыгрузки Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = '%1Отправка отчета прервана.'"), ИнформацияОбОшибке().Описание + Символы.ПС);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			
			РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
			Возврат;
		КонецЕсли;
		
		// формируем имя файла выгрузки
		ОбъектФайл = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаВыгрузки);
		Если Нрег(ОбъектФайл.Расширение) = ".xml" И (СтрНайти(ОбъектФайл.Имя, "NO_BUH") = 0 ИЛИ СтрНайти(ОбъектФайл.Имя, "NO_BOUPR") = 0 ИЛИ СтрНайти(ОбъектФайл.Имя, "NO_BOUCH") = 0) Тогда
			ИмяФайлаВыгрузки = ОбъектФайл.ИмяБезРасширения + "_" + СгенерироватьUUID() + ОбъектФайл.Расширение;
		Иначе
			ИмяФайлаВыгрузки = ОбъектФайл.Имя;
		КонецЕсли; 
		

		// пишем файл во временное хранилище
		КодировкаФайла = "utf-8";
		Если КодировкаФайлаВыгрузки <> Неопределено Тогда
			КодировкаФайла = КодировкаФайлаВыгрузки;
		КонецЕсли;
		АдресВоВременномХранилище = ЗаписатьФайлВыгрузкиВоВременноеХранилище(ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайла, Контекст.УникальныйИдентификатор);
		
		// заполняем результат
		ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес", Отчет, Неопределено, ИмяФайлаВыгрузки, АдресВоВременномХранилище);
		Результат.Добавить(ОписательОтчета);
		
	Иначе
		
		// выгружаем
		ТекстВыгрузки = "";
		ИмяФайлаВыгрузки = "";
		КодировкаФайла = "utf-8";
		Если НЕ ИзвлечьЭлектронноеПредставлениеФСГСИзХранилища(Контекст, ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайла) Тогда
			РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
			Возврат;
		КонецЕсли;
	
		// пишем файл во временное хранилище		
		АдресВоВременномХранилище = ЗаписатьФайлВыгрузкиВоВременноеХранилище(ТекстВыгрузки, ИмяФайлаВыгрузки, КодировкаФайла, Контекст.УникальныйИдентификатор);
		
		// заполняем результат
		ОписательОтчета = Новый Структура("Документ, Тип, Имя, Адрес", Отчет, Неопределено, ИмяФайлаВыгрузки, АдресВоВременномХранилище);
		Результат.Добавить(ОписательОтчета);
		
	КонецЕсли;
	
	Контекст.Вставить("РезультатВыгрузки", Результат);
	РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВФСГСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт 

	Документ = ВходящийКонтекст.КонтекстОтправки.Документ;
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС

		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(ВходящийКонтекст.КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСГС.
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьОрганФСГСПоРеглОтчету(ДокОснование)
	
	Если ТипЗнч(ДокОснование) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		
		ОрганФСГС = ДокОснование.Получатель;
		
	Иначе
		
		Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокОснование, "Организация");
		ОрганФСГС   = ОпределитьОрганФСГСОрганизации(Организация);
		ИспользуетсяМеханизмНесколькихНаправлений = Ложь;
		Попытка
			РегламентированнаяОтчетностьПереопределяемый.ИспользуетсяОтправкаВРосстатПоРазнымНаправлениям(ИспользуетсяМеханизмНесколькихНаправлений);
			Если ИспользуетсяМеханизмНесколькихНаправлений Тогда 
				КодФСГС = РегламентированнаяОтчетность.ПолучитьКодОрганаФСГССтатотчета(ДокОснование);
				Если ЗначениеЗаполнено(КодФСГС) Тогда
					ОрганФСГС = Справочники.ОрганыФСГС.НайтиПоКоду(КодФСГС);
				КонецЕсли;
			КонецЕсли;
		Исключение
			КодОсновногоЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
			ИнформацияОбОшибке = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ЗаписьЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Определение кода органа ФСГС'", КодОсновногоЯзыка),
				УровеньЖурналаРегистрации.Ошибка,
				,
				,
				ИнформацияОбОшибке);
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат ОрганФСГС;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВФСГС_ПередВыгрузкойФайла(Контекст)
	
	Если ТипЗнч(Контекст.Документ) = Тип("Массив") Тогда
		ОсновнойДок = Контекст.Документ[0];
	Иначе
		ОсновнойДок = Контекст.Документ;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОсновнойДок) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОсновнойДок, "Организация");
	Если Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с Росстатом
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФСГС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	ТипЗнчДок = ТипЗнч(ОсновнойДок);
	МетаданныеОтчета = Метаданные.НайтиПоТипу(ТипЗнчДок);
	Если МетаданныеОтчета = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Неизвестный тип отчета.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ИмяМетаданныхОтчета = МетаданныеОтчета.Имя;
	Если ИмяМетаданныхОтчета = "РегламентированныйОтчет" Тогда
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОсновнойДок, "ДатаНачала, ДатаОкончания, ИсточникОтчета");
		ДатаНачалаПериодаОтчета = ЗначенияРеквизитов.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ЗначенияРеквизитов.ДатаОкончания;
		ВидРегламентированногоОтчетаСтр = ЗначенияРеквизитов.ИсточникОтчета;
	Иначе //электронное представление
		ЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОсновнойДок, "ДатаНачала, ДатаОкончания, ВидОтчета");
		ДатаНачалаПериодаОтчета = ЗначенияРеквизитов.ДатаНачала;
		ДатаОкончанияПериодаОтчета = ЗначенияРеквизитов.ДатаОкончания;
		Если ТипЗнч(ЗначенияРеквизитов.ВидОтчета) = Тип("СправочникСсылка.РегламентированныеОтчеты") Тогда
			ВидРегламентированногоОтчетаСтр = ЗначенияРеквизитов.ВидОтчета.ИсточникОтчета;
		Иначе
			ВидРегламентированногоОтчетаСтр = ЗначенияРеквизитов.ВидОтчета.Источник;
		КонецЕсли;
	КонецЕсли;
	
	ОрганФСГС = ПолучитьОрганФСГСПоРеглОтчету(ОсновнойДок);
	Если ОрганФСГС = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'В свойствах организации не задан код территориального органа ФСГС.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли НЕ ЗначениеЗаполнено(ОрганФСГС) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти в справочнике ""Органы ФСГС"" получателя отчетности.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС;
	ТипТранспортногоСообщения = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС;
	ТипСодержимогоКонтейнера = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС;
	
	Если ИмяМетаданныхОтчета = "РегламентированныйОтчет" Тогда
		ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.НайтиПоРеквизиту("Источник", ВидРегламентированногоОтчетаСтр);
		Если ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.ПустаяСсылка()
			И СтрНачинаетсяС(ВидРегламентированногоОтчетаСтр, "EF_") Тогда
			ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.ОтчетФСГСДляВыпускаПатчей;
		КонецЕсли;
		Если ВидОтправляемогоОтчета = Справочники.ВидыОтправляемыхДокументов.ПустаяСсылка() Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В справочнике ""Виды отправляемых документов"" не найден отчет вида %1!'"), ВидРегламентированногоОтчетаСтр);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	Иначе
		ВидОтправляемогоОтчета = ЗначенияРеквизитов.ВидОтчета;
	КонецЕсли;
	
	Контекст.Вставить("Организация",                  Организация);
	Контекст.Вставить("ДатаНачалаПериодаОтчета",      ДатаНачалаПериодаОтчета);
	Контекст.Вставить("ДатаОкончанияПериодаОтчета",   ДатаОкончанияПериодаОтчета);
	Контекст.Вставить("ТипОтправляемогоДокумента",    ИмяМетаданныхОтчета);
	Контекст.Вставить("ТипЦиклаОбмена",               ТипЦиклаОбмена);
	Контекст.Вставить("ТипТранспортногоСообщения",    ТипТранспортногоСообщения);
	Контекст.Вставить("ТипСодержимогоКонтейнера",     ТипСодержимогоКонтейнера);
	Контекст.Вставить("УчетнаяЗапись",                УчетнаяЗапись);
	Контекст.Вставить("ОрганФСГС",                    ОрганФСГС);
	Контекст.Вставить("ВидРегламентированногоОтчета", ВидОтправляемогоОтчета);
	Контекст.Вставить("ИсточникОтчета",               ВидРегламентированногоОтчетаСтр);
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВФСГС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Отчет = Контекст.Документ;
	Организация = Контекст.Организация;
	ТипЦиклаОбмена = Контекст.ТипЦиклаОбмена;
	УчетнаяЗапись = Контекст.УчетнаяЗапись;
	ОрганФСГС = Контекст.ОрганФСГС;
	ТипТранспортногоСообщения = Контекст.ТипТранспортногоСообщения;
	ВидРегламентированногоОтчета = Контекст.ВидРегламентированногоОтчета;
	РезультатВыгрузкиОтчетности = Контекст.РезультатВыгрузки;
	
	// формируем массив предметов
	МассивПредметов = Новый Массив;
	Для Инд = 0 По РезультатВыгрузкиОтчетности.Количество() - 1 Цикл
		МассивПредметов.Добавить(РезультатВыгрузкиОтчетности[Инд].Документ);
	КонецЦикла;
	Если МассивПредметов.Найти(Отчет) = Неопределено Тогда
		МассивПредметов.Вставить(0, Отчет);
	КонецЕсли;
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.ВидОтчета = ВидРегламентированногоОтчета;
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ДатаНачалаПериода = Контекст.ДатаНачалаПериодаОтчета;
	ЦиклОбмена.ДатаОкончанияПериода = Контекст.ДатаОкончанияПериодаОтчета;
	ЦиклОбмена.ВнешняяОрганизация = ОрганФСГС;
	ЦиклОбмена.Тип = ТипЦиклаОбмена;
	
	ЦиклОбмена.Предмет = МассивПредметов[0];
	Для Инд = 1 По МассивПредметов.Количество() - 1 Цикл
		НовСтр = ЦиклОбмена.ДополнительныеПредметы.Добавить();
		НовСтр.Предмет = МассивПредметов[Инд];
	КонецЦикла;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = ТипТранспортногоСообщения;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = ОрганФСГС;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
		
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем ДИВы
	Для Каждого СтрРезультатВыгрузкиОтчетности Из РезультатВыгрузкиОтчетности Цикл
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка,
														Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиФСГС,
														СтрРезультатВыгрузкиОтчетности.Адрес,
														СтрРезультатВыгрузкиОтчетности.Имя,
														,
														,
														,
														,
														,
														СтрРезультатВыгрузкиОтчетности.Тип,
														СгенерироватьUUID(),
														Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// пишем цикл обмена только (!) после записи ДИВ, потому что при записи цикла
	// используется информация из связанных ДИВ
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка отчета прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправкиФСГС(Заголовок, ИмяФайла, Тело, ЦиклОбмена)
	
	// пытаемся разобрать уведомление
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияДатыОтправкиФСГС(ЦиклОбмена, Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		
		Макет = ПолучитьМакетОбработки("ФСГСПодтверждениеДатыОтправки");
		
		ОбластьМакета = Макет.ПолучитьОбласть("ФСГСПодтверждение");
		ОбластьМакета.Параметры.ДатаВремяОтправления = СвойстваИзПодтверждения.ДатаВремяОтправки;
		ОбластьМакета.Параметры.ИмяФайла = СвойстваИзПодтверждения.ИменаФайлов;
		ОбластьМакета.Параметры.СпецОператорСвязи = СвойстваИзПодтверждения.ПодтвердившаяОрганизация;
		ОбластьМакета.Параметры.НаименованиеОтправителя = СвойстваИзПодтверждения.НазваниеОтправителя;
		ОбластьМакета.Параметры.ТипОтправителя = СвойстваИзПодтверждения.ТипОтправителя;
		ОбластьМакета.Параметры.КодОтправителя = СвойстваИзПодтверждения.ОКПООтправителя;
		ОбластьМакета.Параметры.НаименованиеПолучателя = СвойстваИзПодтверждения.НазваниеПолучателя;
		ОбластьМакета.Параметры.ТипПолучателя = СвойстваИзПодтверждения.ТипПолучателя;
		ОбластьМакета.Параметры.КодПолучателя = СвойстваИзПодтверждения.КодПолучателя;
		
		ТабДокумент.Вывести(ОбластьМакета); // ФСГСПодтверждение
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция ВывестиОтчетОСостоянииФСГС(ЦиклОбмена)
	
	ТипЦиклаОбмена = ЦиклОбмена.Тип;
	
	СоответствиеТипаСообщенияКраткомуПредставлению = ИнициализироватьСоответствиеТСКП();
	
	ПредусмотренныеТипыСообщений = Новый ТаблицаЗначений;
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипСообщения");
	ПредусмотренныеТипыСообщений.Колонки.Добавить("ТипЦиклаОбмена");
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС, Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС, Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС, Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеОПолученииОтчетностиФСГС, Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПротоколВходногоКонтроляОтчетностиФСГС, Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПротоколВходногоКонтроляОтчетностиФСГС, Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС, Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПодтверждениеДатыОтправкиФСГС, Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПодтверждениеДатыОтправкиФСГС, Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоИсходящееФСГС, Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ПисьмоВходящееФСГС, Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС, Перечисления.ТипыЦикловОбмена.ИндивидуальноеИнформированиеФСГС);
	
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РассылкаФСГС, Перечисления.ТипыЦикловОбмена.РассылкаФСГС);
	ДобавитьСтрокуВТаблицуТиповСообщений(ПредусмотренныеТипыСообщений, Перечисления.ТипыТранспортныхСообщений.РассылкаШаблоновФСГС, Перечисления.ТипыЦикловОбмена.РассылкаШаблоновФСГС);
	
	// получаем шаблон бланка
	ШаблонМакета = ПолучитьМакетОбработки("БланкПечатиОтчетаОСостоянииФСГС");
	
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	ТаблДок.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
	ЗаполнитьЗначенияСвойств(ТаблДок.НижнийКолонтитул, ШаблонМакета.НижнийКолонтитул);
	ЗаполнитьЗначенияСвойств(ТаблДок.ВерхнийКолонтитул, ШаблонМакета.ВерхнийКолонтитул);
	// устанавливать колонтитулы
	
	// получаем области-строки
	ОблШапка = ШаблонМакета.ПолучитьОбласть("Шапка");
	ОблСтрока = ШаблонМакета.ПолучитьОбласть("Строка");
	ОблПодвалСообщения = ШаблонМакета.ПолучитьОбласть("ПодвалСообщения");
	ОблШапкаКритическиеОшибки = ШаблонМакета.ПолучитьОбласть("ШапкаУведомленияОбОшибках");
	ОблСтрокаКритическаяОшибка = ШаблонМакета.ПолучитьОбласть("СтрокаУведомлениеОбОшибке");
	ОблКритическиеОшибкиОтсутствуют = ШаблонМакета.ПолучитьОбласть("УведомленияОбОшибкахОтсутствуют");
	ОблПодвал = ШаблонМакета.ПолучитьОбласть("Подвал");
	
	// инициализируем значения шапки
	ОблШапка.Области.Идентификатор.Текст = ЦиклОбмена.Идентификатор;
	ОблШапка.Области.Организация.Текст = ЦиклОбмена.Организация;
	ОблШапка.Области.ВнешняяОрганизация.Текст = ЦиклОбмена.ВнешняяОрганизация;
	
	// формируем строку с предметом
	СтрПредмет = "";
	ПредметыПереписки = ПолучитьПредметыЦиклаОбмена(ЦиклОбмена);
	Для Каждого ПредметПереписки Из ПредметыПереписки Цикл
		Если ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.ОтчетностьФСГС Тогда
			Если ЗначениеЗаполнено(ПредметПереписки) Тогда
				СтрПредмет = СтрПредмет + Символы.ПС + ПредставлениеОтчетаФСГС(ПредметПереписки);
			КонецЕсли;
		Иначе
			Если ЗначениеЗаполнено(ПредметПереписки) Тогда
				СтрПредмет = СтрПредмет + Символы.ПС + ПредставлениеПредметаФСГС(ПредметПереписки);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	Если СтрПредмет = "" Тогда
		СтрПредмет = "<Отсутствует>";
	Иначе
		СтрПредмет = СокрЛП(СтрПредмет);
	КонецЕсли;
	
	// присваиваем текстовое представление предмета
	ОблШапка.Области.ПредметПереписки.Текст = СтрПредмет;
	
	ОблШапка.Области.Создан.Текст = Формат(ЦиклОбмена.ДатаСоздания, "ДЛФ=DDT");
	ОблШапка.Области.Завершен.Текст = ?(НЕ ЗначениеЗаполнено(ЦиклОбмена.ДатаЗакрытия) ИЛИ ЦиклОбмена.ДатаЗакрытия = ПолучитьПустуюДатуЗавершенияЦиклаОбмена(), "<не завершен>", Формат(ЦиклОбмена.ДатаЗакрытия, "ДЛФ=DDT"));
	ОблШапка.Области.ПоследнееСообщение.Текст = Формат(ЦиклОбмена.ДатаПоследнегоСообщения, "ДЛФ=DDT");
	
	ТаблДок.Вывести(ОблШапка);
	
	// выбираем информацию по сообщениям цикла обмена
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортныеСообщения.Тип КАК Тип,
						  |	ТранспортныеСообщения.ДатаТранспорта,
						  |	ТранспортныеСообщения.Статус КАК СтатусСообщения,
						  |	ТранспортныеСообщения.ПометкаУдаления,
						  |	ТранспортныеСообщения.ПротоколСОшибкой КАК ПротоколСОшибкой,
						  |	ТранспортныеСообщения.Ссылка КАК Ссылка,
						  |	ТранспортныеСообщения.Тема КАК Тема,
						  |	НерасшифрованныеВложения.КоличествоСообщений КАК КоличествоНерасшифрованных,
						  |	РасшифрованныеВложения.КоличествоСообщений КАК КоличествоРасшифрованных,
						  |	ТранспортныеСообщения.Дата КАК Дата
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.ТранспортныеКонтейнеры КАК ТранспортныеКонтейнеры
						  |		ГДЕ
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение.ЦиклОбмена = &ЦиклОбмена
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			ТранспортныеКонтейнеры.ТранспортноеСообщение) КАК НерасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = НерасшифрованныеВложения.ТранспортноеСообщение
						  |		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ РАЗЛИЧНЫЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение КАК ТранспортноеСообщение,
						  |			КОЛИЧЕСТВО(ИСТИНА) КАК КоличествоСообщений
						  |		ИЗ
						  |			РегистрСведений.СодержимоеТранспортныхКонтейнеров КАК СодержимоеТранспортныхКонтейнеров
						  |		ГДЕ
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение.ЦиклОбмена В(&ЦиклОбмена)
						  |		
						  |		СГРУППИРОВАТЬ ПО
						  |			СодержимоеТранспортныхКонтейнеров.ТранспортноеСообщение) КАК РасшифрованныеВложения
						  |		ПО ТранспортныеСообщения.Ссылка = РасшифрованныеВложения.ТранспортноеСообщение
						  |ГДЕ
						  |	ТранспортныеСообщения.ЦиклОбмена = &ЦиклОбмена
						  |
						  |УПОРЯДОЧИТЬ ПО
						  |	ТранспортныеСообщения.ЦиклОбмена,
						  |	ТранспортныеСообщения.Тип.Порядок,
						  |	Дата");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	ТаблицаСообщений = Запрос.Выполнить().Выгрузить();
	
	ПоследнийТипСообщения = Неопределено;
	
	Для ИндТипСообщения = 1 По ПредусмотренныеТипыСообщений.Количество() Цикл
		
		ТекСтр = ПредусмотренныеТипыСообщений[ИндТипСообщения - 1];
		Если ТекСтр.ТипЦиклаОбмена <> ТипЦиклаОбмена Тогда
			Продолжить;
		КонецЕсли;
		
		ТекТипСообщения = ТекСтр.ТипСообщения;
		СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТекТипСообщения));
		
		Если СообщенияТекТипа.Количество() = 0 Тогда
			
			ОблСтрока.Области.ТипСообщения.Текст = СоответствиеТипаСообщенияКраткомуПредставлению[ТекТипСообщения];
			ОблСтрока.Области.СтатусСообщения.Текст = "Отсутствует";
			ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , Ложь);
			
			ТаблДок.Вывести(ОблСтрока);
			
		Иначе
			
			Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
				
				ТипСообщения = СтрСообщение.Тип;
				ДатаТранспортаСообщения = СтрСообщение.ДатаТранспорта;
				СтатусСтрСообщения = СтрСообщение.СтатусСообщения;
				ПометкаУдаленияСообщения = СтрСообщение.ПометкаУдаления;
				ЭтоПротоколСОшибкой = СтрСообщение.ПротоколСОшибкой;
				Сообщение = СтрСообщение.Ссылка;
				ЧислоКонтейнеровСообщения = СтрСообщение.КоличествоНерасшифрованных;
				ЧислоЭлементовСодержимого = СтрСообщение.КоличествоРасшифрованных;
				ДатаСообщения = СтрСообщение.Дата;
				
				ПредставлениеТипа = СоответствиеТипаСообщенияКраткомуПредставлению[ТипСообщения];
				Если ПредставлениеТипа = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				// заполняем ТипСообщения
				Если ПоследнийТипСообщения <> ТипСообщения Тогда
					ОблСтрока.Области.ТипСообщения.Текст = ПредставлениеТипа;
				Иначе
					ОблСтрока.Области.ТипСообщения.Текст = "";
				КонецЕсли;
				ПоследнийТипСообщения = ТипСообщения;
				
				// заполняем строку статуса
				Если СтатусСтрСообщения = Перечисления.СтатусыПисем.Полученное Тогда
					ТекстСтатуса = "От " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Принято " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
					Если ЧислоЭлементовСодержимого = 0 Тогда
						ТекстСтатуса = ТекстСтатуса + " Не расшифровано.";
					КонецЕсли;
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Исходящее Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Не отправлено.";
				ИначеЕсли СтатусСтрСообщения = Перечисления.СтатусыПисем.Отправленное Тогда
					ТекстСтатуса = "Создано " + Формат(ДатаСообщения, "ДЛФ=DDT") + ". Отправлено " + Формат(ДатаТранспортаСообщения, "ДЛФ=DDT") + ".";
				Иначе
					Продолжить;
				КонецЕсли;
				
				Если ЭтоПротоколСОшибкой Тогда
					ТекстСтатуса = ТекстСтатуса + Символы.ПС + "Содержит сведения об ошибках!";
				КонецЕсли;
				
				ОблСтрока.Области.СтатусСообщения.Текст = ТекстСтатуса;
				ОблСтрока.Области.СтатусСообщения.Шрифт = Новый Шрифт(ОблСтрока.Области.СтатусСообщения.Шрифт, , , , , , ПометкаУдаленияСообщения);
				
				ТаблДок.Вывести(ОблСтрока);
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвалСообщения);
	
	// вывод информации об ошибках
	ТипыОшибок = Новый Массив;
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.УведомлениеОбОшибкеФСГС);
	ТипыОшибок.Добавить(Перечисления.ТипыТранспортныхСообщений.КритическаяОшибка);
	Для Каждого ТипОшибки Из ТипыОшибок Цикл
		СообщенияОшибки = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", ТипОшибки));
		Если СообщенияОшибки.Количество() > 0 Тогда
			ТаблДок.Вывести(ОблШапкаКритическиеОшибки);
			Для Каждого СообщениеОшибка Из СообщенияОшибки Цикл
				ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибке.Текст = СокрЛП(СообщениеОшибка.Тема);
				ОблСтрокаКритическаяОшибка.Области.УведомлениеОбОшибкеДата.Текст = Формат(СообщениеОшибка.ДатаТранспорта, "ДЛФ=DDT");
				ТаблДок.Вывести(ОблСтрокаКритическаяОшибка);
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	ТаблДок.Вывести(ОблПодвал);
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеПредметаФСГС(Предмет)
	
	Если Предмет.Статус = Перечисления.СтатусыПисем.Полученное Тогда
		Возврат "Входящее письмо от Росстата""" + СокрЛП(Предмет.Наименование) + """ от " + Формат(Предмет.ДатаОтправки, "ДЛФ=DDT");
	Иначе
		Возврат "Исходящее письмо респондента""" + СокрЛП(Предмет.Наименование) + """ от " + Формат(Предмет.ДатаСообщения, "ДЛФ=DDT");
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеОтчетаФСГС(ОтчетФСГС)
	
	Если ТипЗнч(ОтчетФСГС) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		Возврат РегламентированнаяОтчетностьКлиентСервер.ПредставлениеДокументаРеглОтч(ОтчетФСГС);
	Иначе
		Возврат "" + ОтчетФСГС;
	КонецЕсли;
	
КонецФункции

Функция ПолучитьОсновныеСвойстваИзПодтвержденияДатыОтправкиФСГС(ЦиклОбмена, СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка чтения XML подтверждения даты отправки:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелПодтверждениеОператора = ДеревоXML.Строки.Найти("подтверждениеОператора", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелПодтверждениеОператора) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения даты отправки: не обнаружен узел ""ПодтверждениеОператора"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелПосылка = УзелПодтверждениеОператора.Строки.Найти("посылка", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелПосылка) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения даты отправки: не обнаружен узел ""Посылка"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелОтправитель = УзелПосылка.Строки.Найти("отправитель", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелОтправитель) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения даты отправки: не обнаружен узел ""Отправитель"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокументы = УзелПосылка.Строки.Найти("документы", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокументы) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML подтверждения даты отправки: не обнаружен узел ""Документы"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения о дате и времени поступившего файла
	УзелДатаВремяОтправки = УзелПодтверждениеОператора.Строки.Найти("датаВремяОтправки", "Имя");
	Если ЗначениеЗаполнено(УзелДатаВремяОтправки) Тогда
		ДатаВремяОтправки = СокрЛП(XMLЗначение(Тип("Дата"), УзелДатаВремяОтправки.Значение));
		ДатаОтправки = СокрЛП(ДатаВремяОтправки);
	КонецЕсли;
	
	//ищем данные отправителя
	УзелПосылка = УзелПодтверждениеОператора.Строки.Найти("посылка", "Имя");
	УзелОтправитель = УзелПосылка.Строки.Найти("отправитель", "Имя");
	
	// сведения о подтвердившей организации
	УзелИдентификаторОтправителя = УзелОтправитель.Строки.Найти("идентификатор", "Имя");
	Если ЗначениеЗаполнено(УзелИдентификаторОтправителя) Тогда
		ИдентификаторОтправителя = СокрЛП(УзелИдентификаторОтправителя.Значение);
	Иначе
		ИдентификаторОтправителя = "";
	КонецЕсли;
	ИдентификаторСпецоператора = Лев(ИдентификаторОтправителя, СтрНайти(ИдентификаторОтправителя, ".") - 1);
	ОрганизацияСтр = "Оператор электронного документооборота" + ?(ПустаяСтрока(ИдентификаторСпецоператора), "", " (" + ИдентификаторСпецоператора + ")");
	
	УзелТипОтправителя = УзелОтправитель.Строки.Найти("тип", "Имя");
	Если ЗначениеЗаполнено(УзелТипОтправителя) Тогда
		ТипОтправителя = СокрЛП(УзелТипОтправителя.Значение);
	КонецЕсли;
	
	УзелНазваниеОтправителя = УзелОтправитель.Строки.Найти("название", "Имя");
	Если ЗначениеЗаполнено(УзелНазваниеОтправителя) Тогда
		НазваниеОтправителя = СокрЛП(УзелНазваниеОтправителя.Значение);
	КонецЕсли;
	
	УзелОКПООтправителя = УзелОтправитель.Строки.Найти("натуральныйИдентификатор", "Имя");
	Если ЗначениеЗаполнено(УзелОКПООтправителя) Тогда
		ОКПООтправителя = СокрЛП(УзелОКПООтправителя.Значение);
	КонецЕсли;
	
	//данные получателя
	УзелПолучатель = УзелПосылка.Строки.Найти("получатель", "Имя");
	
	УзелТипПолучателя = УзелПолучатель.Строки.Найти("тип", "Имя");
	Если ЗначениеЗаполнено(УзелТипПолучателя) Тогда
		ТипПолучателя = СокрЛП(УзелТипПолучателя.Значение);
	КонецЕсли;
	
	УзелНазваниеПолучателя = УзелПолучатель.Строки.Найти("название", "Имя");
	Если ЗначениеЗаполнено(УзелНазваниеПолучателя) Тогда
		НазваниеПолучателя = СокрЛП(УзелНазваниеПолучателя.Значение);
	КонецЕсли;
	
	УзелКодПолучателя = УзелПолучатель.Строки.Найти("натуральныйИдентификатор", "Имя");
	Если ЗначениеЗаполнено(УзелКодПолучателя) Тогда
		КодПолучателя = СокрЛП(УзелКодПолучателя.Значение);
	КонецЕсли;
	
	// извлекаем имена поступивших файлов
	ИмяПоступившегоФайла = "";
	УзелИмяФайла = УзелПосылка.Строки.Найти("исходноеИмяФайла", "Имя");
	Если ЗначениеЗаполнено(УзелИмяФайла) Тогда
		ИмяПоступившегоФайла = СокрЛП(УзелИмяФайла.Значение);
	КонецЕсли;
	
	Возврат Новый Структура("ПодтвердившаяОрганизация, ДатаВремяОтправки, ИменаФайлов, ТипОтправителя, НазваниеОтправителя, ОКПООтправителя, ТипПолучателя, НазваниеПолучателя, КодПолучателя", ОрганизацияСтр, ДатаОтправки, ИмяПоступившегоФайла, ТипОтправителя, НазваниеОтправителя, ОКПООтправителя, ТипПолучателя, НазваниеПолучателя, КодПолучателя);
	
КонецФункции

#Область ОтправкаНеформализованногоДокументаВФСГС

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФСГС(Док, Организация, ВыполняемоеОповещение, ОтобразитьРезультат = Истина) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Док) Тогда
		Возврат;
	КонецЕсли;
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док", 						Док);
	КонтекстДлительнойОперации.Вставить("Организация", 				Организация);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	КонтекстДлительнойОперации.Вставить("ОтобразитьРезультат", 		ОтобразитьРезультат);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФСГС_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(Док, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФСГС_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт

	Док = ВходящийКонтекст.Док;

	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка письма в ФСГС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаНеформализованногоДокументаВФСГС",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаНеформализованногоДокументаВФСГС() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();


	
	Док = КонтекстДлительнойОперации.Док;
	
	ДополнительныеПараметры = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(КонтекстДлительнойОперации);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФСГСПослеАвтонастройкиУчетнойЗаписи", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док,ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФСГС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	Параметры_ОтобразитьРезультат	= ВходящийКонтекст.ОтобразитьРезультат;
	
	ОтправкаНеформализованногоДокументаВФСГС(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение, Параметры_ОтобразитьРезультат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФСГСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФСГС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.Док, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФСГС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ОтобразитьРезультат = ВходящийКонтекст.ОтобразитьРезультат;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// отражаем изменения в базе
	Если НЕ ОтправкаНеформализованногоДокументаВФСГС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаНеформализованногоДокументаВФСГС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Док);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОтобразитьРезультат", ОтобразитьРезультат);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаНеформализованногоДокументаВФСГСПослеФормированияПакета", ЭтотОбъект, Контекст);
		
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
		
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФСГС");

КонецПроцедуры

&НаКлиенте
Процедура ОтправкаНеформализованногоДокументаВФСГСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСГС
		Возврат;
	КонецЕсли;
	
	Если ВходящийКонтекст.ОтобразитьРезультат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСГС
	Иначе
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, КонтекстОтправки.Документ);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаНеформализованногоДокументаВФСГС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Док = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с Росстатом
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФСГС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, указан ли получатель
	ПолучательДокумента = Док.Получатель;
	Если НЕ ЗначениеЗаполнено(ПолучательДокумента) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель письма.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Приложения = ПолучитьВложенияНеформализованногоДокумента(Док, , Истина);
	
	// получаем содержание письма
	ТекстСодержания = Док.Содержание;
	
	// проверяем, не пустое ли письмо
	Если НЕ ЗначениеЗаполнено(ТекстСодержания) И Приложения.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'В письме не задано содержание и отсутствуют вложения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ИмяФайлаСодержимого = ВыгрузитьТекстВФайл(ТекстСодержания);
	КороткоеИмяФайлаСодержимого = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФСГС;
	ЦиклОбмена.Предмет = Док;
	ЦиклОбмена.Идентификатор = Док.Идентификатор;
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = Док.Получатель;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ПисьменноеОбращениеВФСГС;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = Док.Получатель;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФСГС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем письмо
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоФСГС, ИмяФайлаСодержимого, КороткоеИмяФайлаСодержимого, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.Текст1251) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка письма прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем приложения к письму
	Для Каждого СтрПриложение Из Приложения Цикл
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПриложениеПисьмаФСГС, СтрПриложение.Данные, СтрПриложение.ИмяФайла, СтрПриложение.Размер, , , , , , СгенерироватьUUID(), ТипСодержимогоПоИмениФайла(СтрПриложение.ИмяФайла, Истина)) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Док, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ТипТранспортногоСообщения", ТранспортноеСообщение.Тип);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция ПолучитьСвойстваОтделенийФСГСИзИБ(КодыОтделенийФСГС)
	
	СведенияОбОтделениях = Новый Массив;
	
	// получаем из ИБ
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	ОрганыФСГС.Код,
	                      |	ОрганыФСГС.Наименование,
	                      |	ОрганыФСГС.Сертификаты.(
	                      |		Сертификат
	                      |	)
	                      |ИЗ
	                      |	Справочник.ОрганыФСГС КАК ОрганыФСГС
	                      |ГДЕ
	                      |	ОрганыФСГС.Код В(&Код)");
	Запрос.УстановитьПараметр("Код", КодыОтделенийФСГС);
	Выборка = Запрос.Выполнить().Выбрать();
	
	// формируем результат
	Пока Выборка.Следующий() Цикл
		
		СертификатыОрганаФСГС = Новый Массив;
		ВыборкаСертификаты = Выборка.Сертификаты.Выбрать();
		Пока ВыборкаСертификаты.Следующий() Цикл
			СертификатыОрганаФСГС.Добавить(СокрЛП(ВыборкаСертификаты.Сертификат));
		КонецЦикла;
		СведенияОбОтделениях.Добавить(Новый Структура("Код, Наименование, Сертификаты",
														СокрЛП(Выборка.Код), СокрЛП(Выборка.Наименование), СертификатыОрганаФСГС));
	КонецЦикла;
	
	Возврат СведенияОбОтделениях;
	
КонецФункции

&НаСервереБезКонтекста
Функция РезультатыПриемаДекларацийБезПрикрепленныхПодписей(ЦиклыОбмена)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РезультатыПриемаДекларацийБезПрикрепленныхПодписей(ЦиклыОбмена);
	
КонецФункции

&НаКлиенте
Процедура ПроверитьПрикрепленныеПодписиИВыгрузитьПакетыПоФНС(ЦиклОбмена) Экспорт
		
	СообщенияДляРасшифровки = РезультатыПриемаДекларацийБезПрикрепленныхПодписей(ЦиклОбмена);
	КоличествоСообщений = СообщенияДляРасшифровки.Количество();

	Если КоличествоСообщений = 0 Тогда
		ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНС(ЦиклОбмена);
		Возврат;
	КонецЕсли; 

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НПП", 0);
	ДополнительныеПараметры.Вставить("КоличествоСообщений", КоличествоСообщений);
	ДополнительныеПараметры.Вставить("ЦиклОбмена", ЦиклОбмена);
	ДополнительныеПараметры.Вставить("СообщенияДляРасшифровки", СообщенияДляРасшифровки);
	
	РасшифроватьСообщенияБезПодписей(Истина, ДополнительныеПараметры); 

КонецПроцедуры 

&НаКлиенте
Процедура РасшифроватьСообщенияБезПодписей(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат <> Истина Тогда
		Возврат;	
	КонецЕсли; 
	
	НПП = ДополнительныеПараметры.НПП;
	КоличествоСообщений = ДополнительныеПараметры.КоличествоСообщений;
	ЦиклОбмена = ДополнительныеПараметры.ЦиклОбмена;
	СообщенияДляРасшифровки = ДополнительныеПараметры.СообщенияДляРасшифровки;
	
	Если НПП = КоличествоСообщений Тогда
		ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНС(ЦиклОбмена);
		Возврат;
	КонецЕсли;
	
	СообщениеДляРасшифровки = СообщенияДляРасшифровки[НПП];
	ДополнительныеПараметры.НПП = НПП + 1;
	
	ОписаниеОповещения = Новый ОписаниеОповещения("РасшифроватьСообщенияБезПодписей", ЭтотОбъект, ДополнительныеПараметры);
	
	ПараметрыОперации = Новый Структура("РасшифровкаОдного", Истина);
	РасшифроватьТранспортноеСообщение(СообщениеДляРасшифровки, ОписаниеОповещения, , ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНС(ВыгружаемыеЦиклыОбмена, Путь = Неопределено, ДополнятьПуть = Истина) Экспорт
	
	Если ТипЗнч(ВыгружаемыеЦиклыОбмена) <> Тип("Массив") Тогда
		ЦиклыОбмена = Новый Массив;
		ЦиклыОбмена.Добавить(ВыгружаемыеЦиклыОбмена);
	Иначе
		ЦиклыОбмена = ВыгружаемыеЦиклыОбмена;
	КонецЕсли;
	
	// заданы ли циклы обмена
	Если НЕ ЗначениеЗаполнено(ЦиклыОбмена) Тогда
		ПоказатьПредупреждение(, "Не выбраны циклы обмена для выгрузки!");
		Возврат;
	КонецЕсли;
	
	ОбщееКоличествоФайлов = ПолучитьОбщееКоличествоВыгружаемыхПакетовПоДокументооборотамСдачиОтчетностиВФНС(ЦиклыОбмена);
	Если ОбщееКоличествоФайлов = 0 Тогда
		ПоказатьПредупреждение(, "Нет информации для выгрузки!");
		Возврат;
	КонецЕсли;
	
	ВсегоОбъектов = 0;
	ВсегоВАрхиве = 0;
	ВсегоОшибок = 0;
	
	СтатистикаПоЦиклам = ПолучитьВыгружаемыеПакетыПоДокументооборотамСдачиОтчетностиВФНС(ЦиклыОбмена, УникальныйИдентификатор, Истина);
	Для Каждого ЦО Из ЦиклыОбмена Цикл
		РезультатЦО = СтатистикаПоЦиклам.Получить(ЦО);
		
		Если ТипЗнч(РезультатЦО) = Тип("Строка") Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(РезультатЦО);
			ВсегоОшибок = ВсегоОшибок + 1;
			Продолжить;
		КонецЕсли;
			
		ВсегоОбъектов = ВсегоОбъектов + РезультатЦО.Всего;
		ВсегоВАрхиве = ВсегоВАрхиве + РезультатЦО.ВАрхиве;
	КонецЦикла;	
	Если ВсегоВАрхиве > 0 Тогда
		ПоказатьПредупреждение(, "Часть файлов перемещена в архив. Не могу продолжить выгрузку!");
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ЦиклыОбмена, Путь, ДополнятьПуть, ОбщееКоличествоФайлов", ЦиклыОбмена, Путь, ДополнятьПуть, ОбщееКоличествоФайлов);
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНСЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ТекстСообщения = ОперацииСФайламиЭДКОКлиент.ТекстСообщенияДляНеобязательнойУстановкиРасширенияРаботыСФайлами();
	ФайловаяСистемаКлиент.ПодключитьРасширениеДляРаботыСФайлами(ОписаниеОповещения, ТекстСообщения, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНСЗавершение(ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами, ДополнительныеПараметры) Экспорт
	
	ДополнительныеПараметры.Вставить("ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами", ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами);
	
	Путь = ДополнительныеПараметры.Путь;
	
	// заполнен ли путь
	Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
		
		Если НЕ ЗначениеЗаполнено(Путь) Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНСЗавершениеПослеПолученияКаталога", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
			Диалог.Заголовок = "Укажите каталог";
			Диалог.ПроверятьСуществованиеФайла = Истина;
			Диалог.Показать(ОписаниеОповещения);
		Иначе
			ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНСЗавершениеПослеПолученияКаталога(Неопределено, ДополнительныеПараметры);
		КонецЕсли;
		
	Иначе
		
		ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНСЗавершениеПослеПолученияКаталога(Неопределено, ДополнительныеПараметры)

	КонецЕсли;
	
	
КонецПроцедуры

&НаКлиенте
Функция ПолучитьОбщееКоличествоВыгружаемыхПакетовПоДокументооборотамСдачиОтчетностиВФНС(ЦиклыОбмена)
	
	Возврат ЦиклыОбмена.Количество();
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьВыгружаемыеПакетыПоДокументооборотамСдачиОтчетностиВФНС(ЦиклыОбмена, УникальныйИдентификатор, ТолькоОценка = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьВыгружаемыеПакетыПоДокументооборотамСдачиОтчетностиВФНС(ЦиклыОбмена, УникальныйИдентификатор, ТолькоОценка);
	
КонецФункции

&НаКлиенте
Функция ПолучитьКодПодразделенияФССПоУмолчанию(КодПодчиненности,ДопРегНомерФСС,РегНомерФСС) Экспорт
	
	КодФСС = "";
	
	Если ПроверитьЦифровойКодЗаданнойДлины(КодПодчиненности, 5) Тогда
		КодФСС = КодПодчиненности;
	ИначеЕсли ПроверитьЦифровойКодЗаданнойДлины(ДопРегНомерФСС, 10) Тогда
		КодФСС = ДопРегНомерФСС;
	ИначеЕсли ПроверитьЦифровойКодЗаданнойДлины(РегНомерФСС, 10)Тогда
		КодФСС = РегНомерФСС;
	КонецЕсли;
	
	Возврат Лев(КодФСС,4);
КонецФункции

&НаКлиенте
Функция КодОрганаФСГССоответствуетТребованиямКлиент(КодОрганаФСГС) Экспорт 
	
	Возврат СтрокаСоответствуетШаблону(СокрЛП(КодОрганаФСГС), "№№-№№");
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьТаблицуОшибокОписиВходящихДокументов()
	
	ТаблицаОшибок = Новый ТаблицаЗначений;
	ТаблицаОшибок.Колонки.Добавить("КодОшибки");
	ТаблицаОшибок.Колонки.Добавить("ТекстОшибки");
	
	НоваяСтрока = ТаблицаОшибок.Добавить();
	НоваяСтрока.КодОшибки = "0300100001";
	НоваяСтрока.ТекстОшибки = "Формат файла не определен";
	
	Возврат ТаблицаОшибок;
	
КонецФункции	

&НаСервереБезКонтекста
Функция ПроверитьЗаполнениеПараметровПриОтправкеПредставления(Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	СтатусПроверки = Истина;
	// проверяем сведения об организации
	Если НЕ Параметры.ЭтоПБОЮЛ Тогда
	
		Если НЕ ЗначениеЗаполнено(Параметры.НаимЮЛПол) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано полное наименование организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИННЮЛ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан ИНН организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(Параметры.ИННЮЛ, Ложь) Тогда
			ТекстСообщения = НСтр("ru = 'Некорректно задан ИНН организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.КППЮЛ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан КПП организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли СтрДлина(Параметры.КППЮЛ) <> 9 Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан КПП организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	Иначе
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИННФЛ) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан ИНН индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(Параметры.ИННФЛ, Истина) Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан ИНН индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИПФамилия) Тогда
			ТекстСообщения = НСтр("ru = 'Не задана фамилия индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Параметры.ИПИмя) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано имя индивидуального предпринимателя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	// если представитель объявлен, то проверяем сведения о нем
	Если Параметры.ПрПодп = 2 Тогда
		
		СведенияДоверенности = Параметры.Доверенность;
		СведФизЛ = СведенияДоверенности.СвУпПред.СвПред.СведФизЛ;
		ФИОПодписанта = ДокументооборотСКОВызовСервера.УточнитьФИОПредставителя(
			СведФизЛ.ФИО,
			СведенияДоверенности.ДанныеПредставителя);
		Если НЕ ЗначениеЗаполнено(ФИОПодписанта) Тогда
			ТекстСообщения = НСтр("ru = 'В сведениях о доверенности не задано ФИО уполномоченного лица представителя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ ЗначениеЗаполнено(ФИОПодписанта.Фамилия) Тогда
			ТекстСообщения = НСтр("ru = 'В сведениях о доверенности не задана фамилия уполномоченного лица представителя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ ЗначениеЗаполнено(ФИОПодписанта.Имя) Тогда
			ТекстСообщения = НСтр("ru = 'В сведениях о доверенности не задано имя уполномоченного лица представителя.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	Иначе
		
		ФИОПодписанта = РегламентированнаяОтчетность.РазложитьФИО(Параметры.ФИОРук);
		Если НЕ ЗначениеЗаполнено(ФИОПодписанта.Фамилия) Тогда
			ТекстСообщения = НСтр("ru = 'Не задана фамилия руководителя организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		ИначеЕсли НЕ ЗначениеЗаполнено(ФИОПодписанта.Имя) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано имя руководителя организации.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат СтатусПроверки;
	
КонецФункции

&НаСервере
Функция ПроверитьЗаполнениеОписиИсходящихДокументовВФНС(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	ОписьСсылка = Контекст.Документ;
	ЭтоПакетСДопДокументами  = ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка);

	ТЧПредставляемыеДокументы = ОписьСсылка.ПредставляемыеДокументы;
	
	Справочники.ШаблоныПоясненийДляФНС.УстановитьТекстВФорматированныйДокумент(
		ОписьСсылка, 
		ТекстовоеПояснение, 
		"ТекстовоеПояснение");
		
	ЕстьПояснение = Справочники.ШаблоныПоясненийДляФНС.ЗаполненТекстФорматированногоДокумента(ТекстовоеПояснение);
		
	// проверяем заполненность табличной части ПредставляемыеДокументы
	Если ТЧПредставляемыеДокументы.Количество() = 0 И НЕ ЕстьПояснение Тогда
		ТекстСообщения = НСтр("ru = 'Не выбраны документы для отправки.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ОшибокНет = Истина;
	
	ТипСкан = Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде");
	
	Для каждого СтрокаТЧ Из ТЧПредставляемыеДокументы Цикл
		
		Если ТипЗнч(СтрокаТЧ.СсылкаНаОбъект) <> ТипСкан Тогда
			Продолжить;
		КонецЕсли;
			
		Если ЭтоПакетСДопДокументами Тогда
			
			Если НЕ ЗначениеЗаполнено(СтрокаТЧ.СсылкаНаОбъект.КВД) Тогда
			
				ПредставлениеДокумента = Строка(СтрокаТЧ.СсылкаНаОбъект);
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'У документа %1 не заполнен вид документа'"), Символ(34) + ПредставлениеДокумента + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОшибокНет = Ложь;
				
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(СтрокаТЧ.СсылкаНаОбъект.СведенияДокумента) Тогда
			
				ПредставлениеДокумента = Строка(СтрокаТЧ.СсылкаНаОбъект);
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'У документа %1 не заполнено наименование'"), Символ(34) + ПредставлениеДокумента + Символ(34));
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОшибокНет = Ложь;
				
			КонецЕсли;
				
		КонецЕсли;
		
		Результат = Справочники.СканированныеДокументыДляПередачиВЭлектронномВиде.ПроверитьPDFВЗаписанномСканированномДокументе(
			СтрокаТЧ.СсылкаНаОбъект);
		Если НЕ Результат Тогда
			ОшибокНет = Ложь;
		КонецЕсли;
		
	КонецЦикла;
	
	МаксКолвоФайлов = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.МаксимальнойКоличествоФайловОписи();
	ДопустимоеКоличествоФайлов = 
		МаксКолвоФайлов 
		- КонтекстЭДОСервер.КоличествоФайловОписиИсходящихДокументовПриОтправке(ОписьСсылка)
		- ?(ЕстьПояснение, 1, 0);
	
	Если ДопустимоеКоличествоФайлов < 0 Тогда
		
		ТекстСообщения = "Допустимое количество файлов превышено на " +(-ДопустимоеКоличествоФайлов) + ". Разделите опись на несколько частей перед отправкой";
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОшибокНет = Ложь;
		
	КонецЕсли; 
	
	РазмерПояснения = Справочники.ШаблоныПоясненийДляФНС.РазмерТекстовогоПоясненияПоСсылке(
		ОписьСсылка,
		ТекстовоеПояснение, 
		"ТекстовоеПояснение");
	
	//проверка на размер в мегабайтах (не более 72)
	Размер = 
		КонтекстЭДОСервер.ОриентировочныйРазмерОписиИсходящихДокументовПриОтправке(ОписьСсылка) 
		+ ?(ЕстьПояснение, РазмерПояснения, 0);
		
	РазмерВМегабайтах = Окр(Размер/ 1024 / 1024, 2, РежимОкругления.Окр15как20);
	
	ДопустимоеКоличествоМегабайт = 72 - РазмерВМегабайтах;	
	
	Если ДопустимоеКоличествоМегабайт < 0 Тогда
		
		ТекстСообщения = "Допустимый размер описи превышен на " +(-ДопустимоеКоличествоМегабайт) + " Мб. Разделите опись на несколько частей перед отправкой";
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОшибокНет = Ложь;
		
	КонецЕсли;
	
	Возврат ОшибокНет;
	
КонецФункции

#Область ОтправкаОписиИсходящихДокументовВФНС

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС(
		ОписьСсылка,
		Организация,
		УникальныйИдентификаторФормы,
		ВыполняемоеОповещение,
		ВыполнятьАвтонастройку = Истина) Экспорт
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("ОписьСсылка",                  ОписьСсылка);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение",        ВыполняемоеОповещение);
	КонтекстДлительнойОперации.Вставить("УникальныйИдентификаторФормы", УникальныйИдентификаторФормы);
	КонтекстДлительнойОперации.Вставить("Организация",                  Организация);
	КонтекстДлительнойОперации.Вставить("ВыполнятьАвтонастройку",       ВыполнятьАвтонастройку);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаОписиИсходящихДокументовВФНС_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ОписьСсылка, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт

	ОписьСсылка = ВходящийКонтекст.ОписьСсылка;
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ОписьСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка ответа на требование документов
		Возврат;
	КонецЕсли;

	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаОписиИсходящихДокументовВФНС",  0.1, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаОписиИсходящихДокументовВФНС() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ОписьСсылка 			= КонтекстДлительнойОперации.ОписьСсылка;
	ВыполнятьАвтонастройку 	= КонтекстДлительнойОперации.ВыполнятьАвтонастройку;
	
	ДополнительныеПараметры = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(КонтекстДлительнойОперации);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаОписиИсходящихДокументовВФНСПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");
	
	Если ВыполнятьАвтонастройку Тогда
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОписьСсылка,ОписаниеОповещения);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложение(ВыполняемоеОповещение, Контекст)
	
	ТребующиеПодписания = Контекст.ТребующиеПодписания;
	УчетнаяЗапись = Контекст.УчетнаяЗапись;
	ТранспортноеСообщение = Контекст.ТранспортноеСообщение;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Контекст);
	
	Отпечаток = ПолучитьОтпечаток(УчетнаяЗапись, "Руководитель",);
	
	Сертификат = Новый Структура(
		"Отпечаток, КлючевойКонтейнер",
		Отпечаток, РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота);
		
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Сертификат);
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) И ТребующиеПодписания.Количество() > 1 И ВключитьПакетнуюПодпись Тогда
		ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПакетно(ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, Сертификат);
	Иначе	
		НомерФайлаТребующегоПодписания = 0;
		ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПоследовательно(ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, НомерФайлаТребующегоПодписания, Сертификат);
	КонецЕсли;	
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПоследовательно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		НомерФайлаТребующегоПодписания,
		СертификатРуководителя,
		МенеджерКриптографии = Неопределено,
		Алгоритм = Неопределено)
		
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатРуководителя);
	
	Если НомерФайлаТребующегоПодписания <= ТребующиеПодписания.ВГраница() Тогда
		
		ДанныеФайла = ТребующиеПодписания[НомерФайлаТребующегоПодписания];
		
		АлгоритмПодписи = Алгоритм;
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
			ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ОписьИсходящихДокументовВФНС", ТранспортноеСообщение); 
			ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеФайла.ИмяФайла);
			АлгоритмПодписи = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
		КонецЕсли;
		
		ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, НомерФайлаТребующегоПодписания, СертификатРуководителя", 
			ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, НомерФайлаТребующегоПодписания, СертификатРуководителя);
			
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПоследовательноПослеПодписанияФайла", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.ПодписатьPKCS7(
			ОписаниеОповещения,
			СертификатРуководителя,
			ДанныеФайла.АдресВременногоХранилища,
			Ложь,,,
			МенеджерКриптографии,,
			АлгоритмПодписи);
		
	Иначе
		
		//Все подписи добавлены
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПоследовательноПослеПодписанияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Ложь);	
		Возврат;
	КонецЕсли;
	
	ТранспортноеСообщение 			= ДополнительныеПараметры.ТранспортноеСообщение;
	ТребующиеПодписания 			= ДополнительныеПараметры.ТребующиеПодписания;
	НомерФайлаТребующегоПодписания 	= ДополнительныеПараметры.НомерФайлаТребующегоПодписания;
	Сертификат			 			= ДополнительныеПараметры.СертификатРуководителя;
	МестоХраненияКлюча				= КонтекстМоделиХраненияКлюча(Сертификат);
	
	ДанныеФайла = ТребующиеПодписания[НомерФайлаТребующегоПодписания];
	
	КороткоеИмяФайлаПодписи = ДанныеФайла.ИдФайлЭП;
	
	// добавляем подпись как приложение
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
			ТранспортноеСообщение, 
			СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение, 
			Результат.ФайлПодписи, 
			КороткоеИмяФайлаПодписи, 
			,
			,
			,
			,
			,
			,
			СгенерироватьUUID(), 
			ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn")) Тогда
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания подписи для документа %1'."), ДанныеФайла.ИмяФайла);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Ложь);	
		Возврат;
		
	КонецЕсли;

	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
	
		МенеджерКриптографии = Неопределено;
		
	Иначе
			
		МенеджерКриптографии = Результат.МенеджерКриптографии;
		
	КонецЕсли;
	
	Алгоритм = ?(МенеджерКриптографии <> Неопределено, Результат.Алгоритм, Неопределено);
	
	ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПоследовательно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		НомерФайлаТребующегоПодписания + 1,
		Сертификат,
		МенеджерКриптографии,
		Алгоритм);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ОтправкаОписиИсходящихДокументовВФНС(
		ВходящийКонтекст.ОписьСсылка,
		ВходящийКонтекст.Организация,
		ВходящийКонтекст.УникальныйИдентификаторФормы,
		ВходящийКонтекст.ВыполняемоеОповещение,
		Ложь);
		
КонецПроцедуры
		
&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаОписиИсходящихДокументовВФНС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ОписьСсылка, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	ОписьСсылка = ВходящийКонтекст.ОписьСсылка;
	УникальныйИдентификаторФормы = ВходящийКонтекст.УникальныйИдентификаторФормы;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ОтправкаОбъектаЗапрещена = ДокументооборотСКОВызовСервера.ОтправкаОбъектаЗапрещенаНаТарифеПромоЕНС(ОписьСсылка);
	Если ОтправкаОбъектаЗапрещена Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНевозможностиОтправкиНаТарифе(ОписьСсылка);
		Возврат;
	КонецЕсли;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", ОписьСсылка);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификаторФормы);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// отражаем изменения в базе
	Если НЕ ОтправкаОписиИсходящихДокументовВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаОписиИсходящихДокументовВФНС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ОписьСсылка, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОписьСсылка, ВыполняемоеОповещение, Ложь);//+
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если НЕ ДокументооборотСКОВызовСервера.ОрганизацияПодключенаКИнспекции(ОписьСсылка)
		ИЛИ НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ОписьСсылка) Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОписьСсылка, ВыполняемоеОповещение, Ложь); //+
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Добавление подписей...");
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаОписиИсходящихДокументовВФНСПослеДобавлениеПодписейКакПриложение", ЭтотОбъект, Контекст);
		
	// добавляем подписи для каждого из документов из массива ТребующиеПодписания	
	ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложение(Оповещение, КонтекстОтправки)
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНСПослеДобавлениеПодписейКакПриложение(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Формирование пакета...");
	
	// формируем пакет
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаОписиИсходящихДокументовВФНСПослеФормированияПакета", ЭтотОбъект, ВходящийКонтекст);
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыОперации = Новый Структура("ТребуетПодтверждениОперации", КонтекстОтправки.ТребующиеПодписания.Количество() = 0);
	КонецЕсли;
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС", ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 	  Ложь);
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС
		Возврат;
	КонецЕсли;
	
	ПеренестиТекстовоеПояснениеВДеревоДокументовПослеОтправки(КонтекстОтправки.Документ);
	
	Оповестить("Запись_ОписиИсходящихДокументовВНалоговыеОрганы", , КонтекстОтправки.Документ);
	
	ДополнительныеПараметры.Вставить("РезультатОтправки", Истина);
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФНС
	
КонецПроцедуры	

&НаСервереБезКонтекста
Функция ТекстОшибкиОтправкиОписи(ОписьСсылка)
	
	ЭтоПакетСДопДокументами = ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка);
	Если ЭтоПакетСДопДокументами Тогда
		ТекстСообщения = НСтр("ru = 'Отправка %1 прервана.'");
		ТекстСообщения = СтрШаблон(ТекстСообщения, ДокументооборотСКОКлиентСервер.ПредставлениеПакетаСДопДокументамиВРодительномПадеже(Истина));
	Иначе
		ТекстСообщения = НСтр("ru = 'Отправка ответа на требование прервана.'");
	КонецЕсли;
	
	Возврат ТекстСообщения;
	
КонецФункции

&НаСервере
Функция ОтправкаОписиИсходящихДокументовВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	// проверяем заполненность табличной части ПредставляемыеДокументы
	Если НЕ ОтправкаОписиИсходящихДокументовВФНС_Проверки(Контекст) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ОписьСсылка    = Контекст.Документ;
	Организация    = ОписьСсылка.Организация;
	УчетнаяЗапись  = УчетнаяЗаписьОрганизации(Организация);
	НалоговыйОрган = ОписьСсылка.НалоговыйОрган;
	
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	// ищем РегистрацияВИФНС по организации и коду налогового органа
	КодНалоговогоОргана = СокрЛП(НалоговыйОрган.Код);
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		
		// формируем файл доверенности
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(
			ЭлРегистрацияВИФНС,
			КороткоеИмяФайлаДоверенности,
			Организация,,,
			Истина);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось выгрузить файл доверенности.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
		
		//подменять имя файла при отправке через уполномоченного представителя не требуется
		
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
	
	ЭтоФормат_ON_DOCNPNO_1_886_00_05_02 = ТребованияФНСКлиентСервер.ЭтоФормат_ON_DOCNPNO_1_886_00_05_02();
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.Предмет = ОписьСсылка;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = НалоговыйОрган;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = НалоговыйОрган;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	РезультатВыгрузкиОписи = Неопределено;
	Если НЕ ВыгрузитьОписьИсходящихДокументов(Контекст, ТранспортноеСообщение.Ссылка, РезультатВыгрузкиОписи) Тогда
		ТекстСообщения = ТекстОшибкиОтправкиОписи(ОписьСсылка);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли; 
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	//добавляем файл Представление
	ДанныеФайлаПредставление = РезультатВыгрузкиОписи.ДанныеФайлаПредставление;
	ИмяФайлаПредставления = ДанныеФайлаПредставление.ИмяФайла;
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(ДанныеФайлаПредставление.АдресВременногоХранилища);
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление, ДвоичныеДанныеФайла, ИмяФайлаПредставления, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ТекстСообщения = ТекстОшибкиОтправкиОписи(ОписьСсылка);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	ТребующиеПодписания = Новый Массив;
	
	// добавляем файлы выгрузки
	Для Каждого ТекущийЭД Из РезультатВыгрузкиОписи.ТаблицаДокФорм Цикл
		
		МассивЭлементовЭД = Новый Массив;
		
		//заполнение массива МассивЭлементовЭД (возможное количество количество элементов: 1, 2, 4)
		СвойстваЭлементаЭД = Новый Структура("ИмяФайла, АдресВременногоХранилища, ТипСодержимого");
		СвойстваЭлементаЭД.ИмяФайла 				= ТекущийЭД.ИмяФайлаДанных;
		СвойстваЭлементаЭД.АдресВременногоХранилища = ТекущийЭД.АдресФайлаДанных;
		СвойстваЭлементаЭД.ТипСодержимого 			= Перечисления.ТипыСодержимогоФайлов.xml;
		
		МассивЭлементовЭД.Добавить(СвойстваЭлементаЭД);
		
		Если ЗначениеЗаполнено(ТекущийЭД.ИмяФайлаПодписи) И ЗначениеЗаполнено(ТекущийЭД.АдресФайлаПодписи) Тогда
			
			ИмяФайла = ИсправленнноеИмяФайлаЭПФайлаОписиИсходящихДокументов(
				ТекущийЭД.ИмяФайлаПодписи, 
				ТекущийЭД.ИмяФайлаДанных);
			
			СвойстваЭлементаЭД = Новый Структура("ИмяФайла, АдресВременногоХранилища, ТипСодержимого");
			СвойстваЭлементаЭД.ИмяФайла 				= ИмяФайла;
			СвойстваЭлементаЭД.АдресВременногоХранилища = ТекущийЭД.АдресФайлаПодписи;
			СвойстваЭлементаЭД.ТипСодержимого 			= Перечисления.ТипыСодержимогоФайлов.sgn;
			
			МассивЭлементовЭД.Добавить(СвойстваЭлементаЭД);
		Иначе
			// Ветка для отправки книг и журнала учета СФ.
			ДанныеФайла = Новый Структура;
			ДанныеФайла.Вставить("ИмяФайла", 	ТекущийЭД.ИмяФайлаДанных);
			ДанныеФайла.Вставить("ИдФайлЭП",  	ТекущийЭД.ИмяФайлаПодписи);
			ДанныеФайла.Вставить("АдресВременногоХранилища", ТекущийЭД.АдресФайлаДанных);
			
			ТребующиеПодписания.Добавить(ДанныеФайла);
		КонецЕсли;

		Если ЗначениеЗаполнено(ТекущийЭД.ПодтверждениеИмяФайлаДанных) Тогда
			
			СвойстваЭлементаЭД = Новый Структура("ИмяФайла, АдресВременногоХранилища, ТипСодержимого");
			СвойстваЭлементаЭД.ИмяФайла 				= ТекущийЭД.ПодтверждениеИмяФайлаДанных;
			СвойстваЭлементаЭД.АдресВременногоХранилища = ТекущийЭД.ПодтверждениеАдресФайлаДанных;
			СвойстваЭлементаЭД.ТипСодержимого 			= Перечисления.ТипыСодержимогоФайлов.xml;
			
			МассивЭлементовЭД.Добавить(СвойстваЭлементаЭД);
			
			ИмяФайла = ИсправленнноеИмяФайлаЭПФайлаОписиИсходящихДокументов(
				ТекущийЭД.ПодтверждениеИмяФайлаПодписи, 
				ТекущийЭД.ПодтверждениеИмяФайлаДанных);
			
			СвойстваЭлементаЭД = Новый Структура("ИмяФайла, АдресВременногоХранилища, ТипСодержимого");
			СвойстваЭлементаЭД.ИмяФайла 				= ИмяФайла;
			СвойстваЭлементаЭД.АдресВременногоХранилища = ТекущийЭД.ПодтверждениеАдресФайлаПодписи;
			СвойстваЭлементаЭД.ТипСодержимого 			= Перечисления.ТипыСодержимогоФайлов.sgn;
			
			МассивЭлементовЭД.Добавить(СвойстваЭлементаЭД);
		КонецЕсли;
		
		//разбор МассивЭлементовЭД 
		Для каждого СвойстваЭлементаЭД Из МассивЭлементовЭД Цикл
			
			ИмяФайла 		= СвойстваЭлементаЭД.ИмяФайла;
			АдресФайла 		= СвойстваЭлементаЭД.АдресВременногоХранилища;
			ТипСодержимого	= СвойстваЭлементаЭД.ТипСодержимого;
			
			Если ЭтоАдресВременногоХранилища(АдресФайла) Тогда
				ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(АдресФайла);
			Иначе
				ТекстСообщения = НСтр("ru = 'Не удалось получить файл %1 документа %2. 
                                       |Замените или удалите поврежденный документ и повторите отправку'");
				ТекстСообщения = СтрШаблон(ТекстСообщения, СвойстваЭлементаЭД.ИмяФайла, ТекущийЭД.ПредставлениеДокумента);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
				Возврат Ложь;
			КонецЕсли;
			
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, ДвоичныеДанныеФайла, ИмяФайла, , , , , , , СгенерироватьUUID(), ТипСодержимого) Тогда
				ТекстСообщения = ТекстОшибкиОтправкиОписи(ОписьСсылка);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
				Возврат Ложь;
			КонецЕсли;	
		КонецЦикла;

	КонецЦикла;
	
	// добавляем файлы сканов документов и ЭП
	Для Каждого ТекущийСкан Из РезультатВыгрузкиОписи.ТаблицаНеДокФорм Цикл
		
		Для каждого ДанныеФайла Из ТекущийСкан.МассивФайлов Цикл
			
			//добавляем файл 
			ИмяФайла = ДанныеФайла.ИмяФайла;
			ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(ДанныеФайла.АдресВременногоХранилища);
			
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, ДвоичныеДанныеФайла, ИмяФайла, , , , , , , СгенерироватьUUID(), ТипСодержимогоПоИмениФайла(ИмяФайла)) Тогда
				ТекстСообщения = ТекстОшибкиОтправкиОписи(ОписьСсылка);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
				Возврат Ложь;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ТекущийСкан.ИдФайлЭП) 
				И НЕ ЗначениеЗаполнено(ТекущийСкан.АдресЭП) Тогда
				
				ФайлНаПодписание = Новый Структура;
				ФайлНаПодписание.Вставить("ИмяФайла", 	ИмяФайла);
				ФайлНаПодписание.Вставить("ИдФайлЭП",  	ТекущийСкан.ИдФайлЭП);
				ФайлНаПодписание.Вставить("АдресВременногоХранилища", ДанныеФайла.АдресВременногоХранилища);
				
				ТребующиеПодписания.Добавить(ФайлНаПодписание);
				
			КонецЕсли;

			
		КонецЦикла;
		
		Если ЗначениеЗаполнено(ТекущийСкан.ИдФайлЭП) И ЗначениеЗаполнено(ТекущийСкан.АдресЭП) Тогда
			
			//добавляем файл 
			ИмяФайла = ТекущийСкан.ИдФайлЭП;
			ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(ТекущийСкан.АдресЭП);
			
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, ДвоичныеДанныеФайла, ИмяФайла, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.sgn) Тогда
				ТекстСообщения = ТекстОшибкиОтправкиОписи(ОписьСсылка);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ОтменитьТранзакцию();
				Возврат Ложь;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, ФайлДоверенности, КороткоеИмяФайлаДоверенности, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
		
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(ОписьСсылка, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТребующиеПодписания", ТребующиеПодписания);
	
	Возврат Истина;
	
КонецФункции

&НаСервере
Функция ОтправкаОписиИсходящихДокументовВФНС_Проверки(Контекст)
	
	ОписьСсылка = Контекст.Документ;
	
	// проверяем заполненность табличной части ПредставляемыеДокументы
	Если НЕ ПроверитьЗаполнениеОписиИсходящихДокументовВФНС(Контекст) Тогда
		ТекстСообщения = ТекстОшибкиОтправкиОписи(ОписьСсылка);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли механизм обмена с ФНС для организации, указанной в описи
	Организация = ОписьСсылка.Организация;
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, указан ли получатель
	НалоговыйОрган = ОписьСсылка.НалоговыйОрган;
	Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
		ТекстСообщения = НСтр("ru = 'Не указан получатель - налоговый орган.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПакетно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		СертификатРуководителя)
		
	ПараметрыЦикла = НовыЙ Структура();
	ПараметрыЦикла.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ПараметрыЦикла.Вставить("ТребующиеПодписания", ТребующиеПодписания);
	ПараметрыЦикла.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПакетноПослеВыполнения", ЭтотОбъект, ПараметрыЦикла);
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СертификатРуководителя);
	
	МассивДокументов = Новый Массив;
	Для Счетчик = 0 По ТребующиеПодписания.ВГраница() Цикл
		ДанныеФайла = ТребующиеПодписания[Счетчик];
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеФайла.ИмяФайла);
		НовыйДокументПакетнойПодписи(
			МассивДокументов,
			ДанныеФайла.АдресВременногоХранилища,
			ОписаниеФайла.Имя,
			ОписаниеФайла.Расширение,
			Счетчик);
	КонецЦикла;

	ПакетнаяПодписьОблачнойПодписи(ОписаниеОповещения, МестоХраненияКлюча, МассивДокументов, СертификатРуководителя);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОписиИсходящихДокументовВФНС_ДобавлениеПодписейКакПриложениеПакетноПослеВыполнения(
		РезультатВыполнения, ВходящийКонтекст) Экспорт
		
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ТребующиеПодписания = ВходящийКонтекст.ТребующиеПодписания;
	ТранспортноеСообщение = ВходящийКонтекст.ТранспортноеСообщение;
	Результат = РезультатВыполнения.Выполнено;
		
	Если Результат Тогда
		
		Для Счетчик = 0 По РезультатВыполнения.Результат.ВГраница() Цикл
			Подпись = РезультатВыполнения.Результат[Счетчик];
			ДанныеФайла = ТребующиеПодписания[Счетчик];
			КороткоеИмяФайлаПодписи = ДанныеФайла.ИдФайлЭП;
			
			// добавляем подпись как приложение
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
					ТранспортноеСообщение, 
					СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение, 
					Подпись, 
					КороткоеИмяФайлаПодписи, 
					,
					,
					,
					,
					,
					,
					СгенерироватьUUID(), 
					ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn")) Тогда
				
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания подписи для документа %1'."), ДанныеФайла.ИмяФайла);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				
				Результат = Ложь;
				Прервать;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Результат);
	
КонецПроцедуры

#КонецОбласти

#Область ОтправкаЖурналаСчетовФактур

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт

	ОтправкаЖурналаСчетовФактурВФНС(
		ВходящийКонтекст.ДокументСсылка,
		ВходящийКонтекст.Организация,
		ВходящийКонтекст.УникальныйИдентификаторФормы,
		ВходящийКонтекст.ВыполняемоеОповещение);
		
КонецПроцедуры
		
&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС(
		ДокументСсылка,
		Организация,
		УникальныйИдентификаторФормы,
		ВыполняемоеОповещение) Экспорт
		
	ВыполнятьАвтонастройку = Истина;

	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", ДокументСсылка);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификаторФормы);
	
	ОтправкаОбъектаЗапрещена = ДокументооборотСКОВызовСервера.ОтправкаОбъектаЗапрещенаНаТарифеПромоЕНС(ДокументСсылка);
	Если ОтправкаОбъектаЗапрещена Тогда
		ДокументооборотСКОКлиент.СообщитьОНевозможностиОтправкиНаТарифе(ДокументСсылка);
		Возврат;
	КонецЕсли;
	
	// отражаем изменения в базе
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	Если НЕ ОтправкаЖурналаСчетовФактурВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ДокументСсылка", 					ДокументСсылка);
			ДополнительныеПараметры.Вставить("Организация",			 			Организация);
			ДополнительныеПараметры.Вставить("УникальныйИдентификаторФормы", 	УникальныйИдентификаторФормы);
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаЖурналаСчетовФактурВФНС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ДокументСсылка, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ДокументСсылка, ВыполняемоеОповещение, Ложь);//+
		Иначе
		
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);

			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Журнал
			
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ДокументСсылка) Тогда
		
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ДокументСсылка, ВыполняемоеОповещение, Ложь);//+
		Возврат;
		
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ДокументСсылка",               ДокументСсылка);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении",        ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("УникальныйИдентификаторФормы", УникальныйИдентификаторФормы);
	ДополнительныеПараметры.Вставить("Организация",                  Организация);
	ДополнительныеПараметры.Вставить("КонтекстОтправки",             КонтекстОтправки);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЖурналаСчетовФактурВФНСПослеАвтонастройкиУчетнойЗаписи", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 		ОписаниеОповещения);
	КонтекстДлительнойОперации.Вставить("ДокументСсылка", 			ДокументСсылка);
	КонтекстДлительнойОперации.Вставить("ВыполнятьАвтонастройку", 	ВыполнятьАвтонастройку);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ДокументСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка журнала в ФНС
		Возврат;
	КонецЕсли;

	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаЖурналаСчетовФактурВФНС",  0.1, Истина);
		
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаЖурналаСчетовФактурВФНС() Экспорт
		
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ДокументСсылка 			= КонтекстДлительнойОперации.ДокументСсылка;
	ОписаниеОповещения 		= КонтекстДлительнойОперации.ОписаниеОповещения;
	ВыполнятьАвтонастройку 	= КонтекстДлительнойОперации.ВыполнятьАвтонастройку;
	
	Если ВыполнятьАвтонастройку Тогда
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ДокументСсылка,ОписаниеОповещения);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЖурналаСчетовФактурВФНС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ДокументСсылка, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаЖурналаСчетовФактурВФНСПослеДобавлениеПодписейКакПриложение", ЭтотОбъект, ВходящийКонтекст);
		
	// добавляем подписи для каждого из документов из массива ТребующиеПодписания	
	ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложение(Оповещение, ВходящийКонтекст.КонтекстОтправки)
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаЖурналаСчетовФактурВФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	ДокументСсылка 	= Контекст.Документ;
	
	// проверяем, включен ли механизм обмена с ФНС для организации, указанной в журнале
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	//требуется сформировать файл доверенности 
	//и определить налоговый орган (для записи в реквизит цикла обмена)
	//для этого определяем ИФНС через переопределяемый модуль
	КодНалоговогоОргана = СведенияПоОтправляемымОбъектам(ДокументСсылка).КодКонтролирующегоОргана;
	Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
	Если НЕ ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	НалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНалоговогоОргана);
	Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'Не найден налоговый орган с кодом %1  в справочнике налоговых органов""!.'"), Символ(34) + КодНалоговогоОргана + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	//теперь формируем файл доверенности
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(
			ЭлРегистрацияВИФНС,
			КороткоеИмяФайлаДоверенности,
			Организация,,,
			Истина);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось выгрузить файл доверенности.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
		
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.Предмет = ДокументСсылка;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = НалоговыйОрган;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление;
	ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ЖурналУчетаСчетовФактур;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = НалоговыйОрган;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	РезультатВыгрузкиЖурнала = Неопределено;
	Если НЕ ВыгрузитьЖурналСчетовФактурДляОтправкиВФНС(Контекст, РезультатВыгрузкиЖурнала, ЭлРегистрацияВИФНС) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка журнала прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли; 
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	//добавляем файл Представление
	ДанныеФайлаПредставление = РезультатВыгрузкиЖурнала.ДанныеФайлаПредставление;
	ИмяФайлаПредставления = ДанныеФайлаПредставление.ИмяФайла;
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(ДанныеФайлаПредставление.АдресВременногоХранилища);
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление, ДвоичныеДанныеФайла, ИмяФайлаПредставления, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка журнала прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	ИдФайл 				= РезультатВыгрузкиЖурнала.ИдФайл;
	ИдФайлЭП 			= РезультатВыгрузкиЖурнала.ИдФайлЭП;
	АдресФайлаДанных 	= РезультатВыгрузкиЖурнала.АдресВременногоХранилища;
	
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(АдресФайлаДанных);
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, ДвоичныеДанныеФайла, ИдФайл, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка журнала прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	ТребующиеПодписания = Новый Массив;
	
	ДанныеФайла = Новый Структура;
	ДанныеФайла.Вставить("ИмяФайла", 					ИдФайл);
	ДанныеФайла.Вставить("ИдФайлЭП", 					ИдФайлЭП);
	ДанныеФайла.Вставить("АдресВременногоХранилища", 	АдресФайлаДанных);
	
	ТребующиеПодписания.Добавить(ДанныеФайла);
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, ФайлДоверенности, КороткоеИмяФайлаДоверенности, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
		
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(ДокументСсылка, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТребующиеПодписания", ТребующиеПодписания);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложение(ВыполняемоеОповещение, Контекст)
	
	ТребующиеПодписания     = Контекст.ТребующиеПодписания;
	УчетнаяЗапись           = Контекст.УчетнаяЗапись;
	ТранспортноеСообщение   = Контекст.ТранспортноеСообщение;
	РеквизитыУчетнойЗаписи  = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись);
	МестоХраненияКлюча		= КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	Отпечаток               = ПолучитьОтпечаток(УчетнаяЗапись, "Руководитель",);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Контекст);

	Сертификат = Новый Структура();
	Сертификат.Вставить("Отпечаток",         Отпечаток);
	Сертификат.Вставить("КлючевойКонтейнер", РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Сертификат);
	
	НомерФайлаТребующегоПодписания = 0;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) И ТребующиеПодписания.Количество() > 1 И ВключитьПакетнуюПодпись Тогда
		ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПакетно(
			ВыполняемоеОповещение,
			ТранспортноеСообщение,
			ТребующиеПодписания,
			Сертификат);
	Иначе	
		ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПоследовательно(
			ВыполняемоеОповещение,
			ТранспортноеСообщение,
			ТребующиеПодписания,
			НомерФайлаТребующегоПодписания,
			Сертификат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПоследовательно(
	ВыполняемоеОповещение,
	ТранспортноеСообщение,
	ТребующиеПодписания,
	НомерФайлаТребующегоПодписания,
	Сертификат,
	МенеджерКриптографии = Неопределено,
	Алгоритм = Неопределено)
	
	Если НомерФайлаТребующегоПодписания <= ТребующиеПодписания.ВГраница() Тогда
		
		ДанныеФайла = ТребующиеПодписания[НомерФайлаТребующегоПодписания];
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",          ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ТранспортноеСообщение",          ТранспортноеСообщение);
		ДополнительныеПараметры.Вставить("ТребующиеПодписания",            ТребующиеПодписания);
		ДополнительныеПараметры.Вставить("НомерФайлаТребующегоПодписания", НомерФайлаТребующегоПодписания);
		ДополнительныеПараметры.Вставить("СертификатРуководителя",         Сертификат);
			
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПоследовательноПослеПодписанияФайла",
			ЭтотОбъект,
			ДополнительныеПараметры);
			
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Сертификат);
		АлгоритмПодписи = Алгоритм;
		
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
			ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеФайла.ИмяФайла);
			ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ЖурналСчетовФактурВФНС", ТранспортноеСообщение); 
			АлгоритмПодписи = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
		КонецЕсли;
		
		КриптографияЭДКОКлиент.ПодписатьPKCS7(
			ОписаниеОповещения,
			Сертификат,
			ДанныеФайла.АдресВременногоХранилища,
			Ложь,,,
			МенеджерКриптографии,,
			АлгоритмПодписи);
		
	Иначе
		
		//Все подписи добавлены
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПоследовательноПослеПодписанияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Ложь);	
		Возврат;
	КонецЕсли;
	
	ТранспортноеСообщение 			= ДополнительныеПараметры.ТранспортноеСообщение;
	ТребующиеПодписания 			= ДополнительныеПараметры.ТребующиеПодписания;
	НомерФайлаТребующегоПодписания 	= ДополнительныеПараметры.НомерФайлаТребующегоПодписания;
	Сертификат 						= ДополнительныеПараметры.СертификатРуководителя;
	МестоХраненияКлюча				= КонтекстМоделиХраненияКлюча(Сертификат);
	
	ДанныеФайла = ТребующиеПодписания[НомерФайлаТребующегоПодписания];
	
	КороткоеИмяФайлаПодписи = ДанныеФайла.ИдФайлЭП;
	
	// добавляем подпись как приложение
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение, Результат.ФайлПодписи, КороткоеИмяФайлаПодписи, , , , , , , СгенерироватьUUID(), ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn")) Тогда
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания подписи для документа %1'."), ДанныеФайла.ИмяФайла);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Ложь);	
		Возврат;
		
	КонецЕсли;

	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
	
		МенеджерКриптографии = Неопределено;
		
	Иначе
			
		МенеджерКриптографии = Результат.МенеджерКриптографии;
		
	КонецЕсли;
	
	Алгоритм = ?(МенеджерКриптографии <> Неопределено, Результат.Алгоритм, Неопределено);
	
	ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПоследовательно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		НомерФайлаТребующегоПодписания + 1,
		Сертификат,
		МенеджерКриптографии,
		Алгоритм);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНСПослеДобавлениеПодписейКакПриложение(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Журнал
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаЖурналаСчетовФактурВФНСПослеФормированияПакета", ЭтотОбъект, ВходящийКонтекст);
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыОперации = Новый Структура("ТребуетПодтверждениОперации", Ложь);
	КонецЕсли;	
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС", ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Журнал
		
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Журнал;

		Возврат;
	КонецЕсли;
	
	Оповестить("Запись_ЖурналУчетаСчетовФактурДляПередачиВЭлектронномВиде", КонтекстОтправки.Документ);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // Журнал.
	
КонецПроцедуры	

//используется для независимой (от требования о представлении документов) отправки журнала СФ
&НаСервереБезКонтекста
Функция ВыгрузитьЖурналСчетовФактурДляОтправкиВФНС(Контекст, ДанныеФайловВыгрузки, ЭлРегистрацияВИФНС)
	
	УникальныйИдентификаторФормы = Контекст.УникальныйИдентификатор;
	ДокументСсылка = Контекст.Документ;
	
	ДанныеФайловВыгрузки = ПолучитьДанныеФайлаВыгрузкиЖурналаСчетовФактур(ДокументСсылка, УникальныйИдентификаторФормы);	
	Если ДанныеФайловВыгрузки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось получить файл выгрузки журнала.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;	
	
	Организация 			= ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "Организация");
	КодНалоговогоОргана 	= ЭлРегистрацияВИФНС.Код;
	ПараметрыОписи 			= СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана);
	
	Если НЕ ПроверитьЗаполнениеПараметровПриОтправкеПредставления(ПараметрыОписи) Тогда
		Возврат Ложь;
	КонецЕсли;
	ПараметрыОписи.Вставить("Журнал", "ЖУРНАЛ");
	
	ПараметрыОписи.Вставить("ЭтоЖурналСФ", Истина);
	
	// формируем XML представления и имя файла представления
	ИмяФайлаПредставления = Неопределено;
	КолФайл = 2;
	ТекстПредставления = СформироватьXMLПредставления(
		Организация, 
		ДанныеФайловВыгрузки, 
		ПараметрыОписи, 
		ИмяФайлаПредставления, 
		КолФайл);
		
	Если НЕ ЗначениеЗаполнено(ТекстПредставления) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать XML файл описи исходящих документов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	КодировкаТекстаДанных = "windows-1251";
	ДвоичныеДанныеФайла = ПолучитьДвоичныеДанныеИзСтроки(ТекстПредставления, КодировкаТекстаДанных);
	СсылкаНаДвоичныеДанныеФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, УникальныйИдентификаторФормы);
	
	ДанныеФайлаПредставление = Новый Структура;
	ДанныеФайлаПредставление.Вставить("ИмяФайла", ИмяФайлаПредставления);
	ДанныеФайлаПредставление.Вставить("АдресВременногоХранилища", СсылкаНаДвоичныеДанныеФайла);
	
	
	ДанныеФайловВыгрузки.Вставить("ДанныеФайлаПредставление", ДанныеФайлаПредставление);
	
	Возврат Истина;

КонецФункции

//используется для независимой (от требования о представлении документов) отправки журнала СФ
&НаСервереБезКонтекста
Функция ПолучитьДанныеФайлаВыгрузкиЖурналаСчетовФактур(ДокументСсылка, УникальныйИдентификаторФормы)
	
	// инициализируем структуру-результат
	Результат = Новый Структура;
	Если ТипЗнч(ДокументСсылка) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда

		ВсеФайлы = ИзвлечьЭлектронныеПредставленияИзХранилища(ДокументСсылка, 1);
		
		Если ВсеФайлы <> Неопределено И ВсеФайлы.Количество() > 0 Тогда
			Выборка = ВсеФайлы[0];			
			Если Выборка.ВАрхиве Тогда
				ДанныеПредставления = Base64Значение("");
			Иначе
				ДанныеПредставления = Выборка.Данные;
			КонецЕсли;			
		Иначе
			Выборка = Новый Структура("ИмяФайла", "");
			ДанныеПредставления = Неопределено;
		КонецЕсли;
		
		Результат.Вставить("ИдФайл", Выборка.ИмяФайла);
		Результат.Вставить("АдресВременногоХранилища", ПоместитьВоВременноеХранилище(ДанныеПредставления, Новый УникальныйИдентификатор));

	Иначе
		МассивНДС 	= Новый Массив;
		МассивНДС.Добавить(ДокументСсылка);
		СоответствиеНДС 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьФайлыВыгрузкиНДС(МассивНДС, Новый УникальныйИдентификатор);
		МассивФайлов = СоответствиеНДС.Получить(ДокументСсылка);
		
		Если НЕ ЗначениеЗаполнено(МассивФайлов) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		ДанныеФайла = МассивФайлов[0];
		
		Результат.Вставить("ИдФайл", 					ДанныеФайла.ИмяФайла);
		Результат.Вставить("АдресВременногоХранилища", 	ДанныеФайла.АдресВременногоХранилища);
	КонецЕсли;	
	
	ИдФайлЭП = ИмяФайлаЭПФайлаОписиИсходящихДокументов(Результат.ИдФайл);
	Результат.Вставить("ИдФайлЭП", ИдФайлЭП);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПакетно(
	ВыполняемоеОповещение,
	ТранспортноеСообщение,
	ТребующиеПодписания,
	Сертификат,
	Алгоритм = Неопределено)
	
	ПараметрыЦикла = НовыЙ Структура();
	ПараметрыЦикла.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ПараметрыЦикла.Вставить("ТребующиеПодписания", ТребующиеПодписания);
	ПараметрыЦикла.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПакетноПослеВыполнения", ЭтотОбъект, ПараметрыЦикла);
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Сертификат);
	
	МассивДокументов = Новый Массив;
	Для Счетчик = 0 По ТребующиеПодписания.ВГраница() Цикл
		ДанныеФайла = ТребующиеПодписания[Счетчик];
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеФайла.ИмяФайла);
		НовыйДокументПакетнойПодписи(
			МассивДокументов,
			ДанныеФайла.АдресВременногоХранилища,
			ОписаниеФайла.Имя,
			ОписаниеФайла.Расширение,
			Счетчик);
	КонецЦикла;

	ПакетнаяПодписьОблачнойПодписи(ОписаниеОповещения, МестоХраненияКлюча, МассивДокументов, Сертификат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЖурналаСчетовФактурВФНС_ДобавлениеПодписейКакПриложениеПакетноПослеВыполнения(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ТребующиеПодписания = ВходящийКонтекст.ТребующиеПодписания;
	ТранспортноеСообщение = ВходящийКонтекст.ТранспортноеСообщение;
	
	Результат = РезультатВыполнения.Выполнено;
	
	Если Результат Тогда
		
		Для Счетчик = 0 По РезультатВыполнения.Результат.ВГраница() Цикл
			ДанныеФайла = ТребующиеПодписания[Счетчик];
			Подпись = РезультатВыполнения.Результат[Счетчик];
			КороткоеИмяФайлаПодписи = ДанныеФайла.ИдФайлЭП;
			
			// добавляем подпись как приложение
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение, Подпись, КороткоеИмяФайлаПодписи, , , , , , , СгенерироватьUUID(), ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn")) Тогда
				
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания подписи для документа %1'."), ДанныеФайла.ИмяФайла);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				
				Результат = Ложь;
				Прервать;
				
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;	
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Результат);	
	
КонецПроцедуры

#КонецОбласти


//выгрузка доверенности для ДО Представление
&НаСервереБезКонтекста
Функция ВыгрузитьДоверенностьПредставление(
		РегистрацияВИФНС,
		КороткоеИмяФайлаДоверенности = Неопределено,
		Организация = Неопределено,
		КодНалоговогоОрганаКонечногоПолучателя = Неопределено,
		ИгнорироватьПризнакКрупнейшегоНалогоплательщика = Ложь,
		ИсключеноПриложениеДоверенности = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьДоверенностьПредставление(
		РегистрацияВИФНС,
		КороткоеИмяФайлаДоверенности,
		Организация,
		КодНалоговогоОрганаКонечногоПолучателя,
		ИгнорироватьПризнакКрупнейшегоНалогоплательщика,
		ИсключеноПриложениеДоверенности);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьИдентификаторОтправителяПолучателя(ТранспортноеСообщение)
	
	// формируем идентификатор получателя
	ИдентификаторПолучателя = СформироватьИдентификаторПолучателя(ТранспортноеСообщение);
	
	// формируем идентификатор отправителя
	ИдентификаторОтправителя = СформироватьИдентификаторОтправителя(ТранспортноеСообщение);
	
	// собираем имя файла представления из составляющих
	ИдентификаторОтправителяПолучателя = ИдентификаторОтправителя + "_" + ИдентификаторПолучателя;
	
	Возврат ИдентификаторОтправителяПолучателя;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана);
	
КонецФункции

&НаСервере
Функция ПолучитьДанныеПоФайламВыгрузкиОписиИсходящихДокументов(ОписьСсылка, ИдентификаторОтправителяПолучателя, УникальныйИдентификаторФормы)
	
	ТаблицаНеДокФорм = ШаблонТаблицыНеДокФорм();
	ТаблицаДокФорм 	 = ШаблонТаблицыДокФорм();

	ТипСкан = Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде");
	
	// СоответствиеФайлыПринадлежащиеОписи: ключ - имя файла, значение - адрес во временном хранилище
	СоответствиеФайлыПринадлежащиеОписи	= ПолучитьФайлыПринадлежащиеОписи(ОписьСсылка, УникальныйИдентификаторФормы);
	
	Для каждого СтрокаПредставляемыйДокумент Из ОписьСсылка.ПредставляемыеДокументы Цикл
	
		Если ТипЗнч(СтрокаПредставляемыйДокумент.СсылкаНаОбъект) = ТипСкан Тогда
	
			ДобавитьСтрокуТаблицыНеДокФорм(
				ИдентификаторОтправителяПолучателя, 
				УникальныйИдентификаторФормы,
				СтрокаПредставляемыйДокумент,
				ТаблицаНеДокФорм,
				ОписьСсылка);

		Иначе

			ДобавитьСтрокуТаблицыДокФорм(
				СтрокаПредставляемыйДокумент,
				СоответствиеФайлыПринадлежащиеОписи,
				ТаблицаДокФорм,
				ОписьСсылка);
	
		КонецЕсли;
	
	КонецЦикла;

	ДобавитьТекстовоеПояснение(
		ИдентификаторОтправителяПолучателя, 
		УникальныйИдентификаторФормы,
		ТаблицаНеДокФорм,
		ОписьСсылка);

	Результат = Новый Структура;
	Результат.Вставить("ТаблицаНеДокФорм", 	ТаблицаНеДокФорм);
	Результат.Вставить("ТаблицаДокФорм", 	ТаблицаДокФорм);

	Возврат Результат;
		
КонецФункции

&НаСервереБезКонтекста
Функция ШаблонТаблицыДокФорм()
			
	ТаблицаДокФорм = Новый ТаблицаЗначений;
	ТаблицаДокФорм.Колонки.Добавить("ПорНом");
	ТаблицаДокФорм.Колонки.Добавить("ВидДокумента");

	// Сведения по документу 
	ТаблицаДокФорм.Колонки.Добавить("КНД");
			
	ТаблицаДокФорм.Колонки.Добавить("ИмяФайлаДанных");
	ТаблицаДокФорм.Колонки.Добавить("АдресФайлаДанных");

	ТаблицаДокФорм.Колонки.Добавить("ИмяФайлаПодписи");
	ТаблицаДокФорм.Колонки.Добавить("АдресФайлаПодписи");
			
	// Сведения по документу-основанию
	ТаблицаДокФорм.Колонки.Добавить("СведенияДокументаОснования");

	ТаблицаДокФорм.Колонки.Добавить("ПодтверждениеКНД");
				
	// Сведения по документу-подтвержению
	ТаблицаДокФорм.Колонки.Добавить("ПодтверждениеИмяФайлаДанных");
	ТаблицаДокФорм.Колонки.Добавить("ПодтверждениеАдресФайлаДанных");
				
	ТаблицаДокФорм.Колонки.Добавить("ПодтверждениеИмяФайлаПодписи");
	ТаблицаДокФорм.Колонки.Добавить("ПодтверждениеАдресФайлаПодписи");
				
	ТаблицаДокФорм.Колонки.Добавить("ПредставлениеДокумента");
				
	ТаблицаДокФорм.Колонки.Добавить("ЭтоПакетСДопДокументами");
				
	Возврат ТаблицаДокФорм;
				
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьСтрокуТаблицыДокФорм(
	СтрокаПредставляемыйДокумент,
	СоответствиеФайлыПринадлежащиеОписи,
	ТаблицаДокФорм,
	ОписьСсылка)
	
НовСтр = ТаблицаДокФорм.Добавить();  
	
	НовСтр.ПорНом = ?(СтрокаПредставляемыйДокумент.ЯвляетсяИнформацией, "2.", "1.") 
	+ Формат(СтрокаПредставляемыйДокумент.ПорядковыйНомер, "ЧЦ=2; ЧН=; ЧВН=");
	
	НовСтр.ВидДокумента = СтрокаПредставляемыйДокумент.ВидДокумента;
	
	НовСтр.КНД = СтрокаПредставляемыйДокумент.КНД;
	
	СведенияДокументаОснования 	= СтрокаПредставляемыйДокумент.СведенияДокументаОснования;
	НомерДокументаОснования 	= СтрокаПредставляемыйДокумент.НомерДокументаОснования;
	ДатаДокументаОснования 		= СтрокаПредставляемыйДокумент.ДатаДокументаОснования;
	
	Если ЗначениеЗаполнено(СведенияДокументаОснования) Тогда
		НовСтр.СведенияДокументаОснования = СведенияДокументаОснования;
	ИначеЕсли ЗначениеЗаполнено(НомерДокументаОснования) ИЛИ ЗначениеЗаполнено(ДатаДокументаОснования) Тогда
		НовСтр.СведенияДокументаОснования = НомерДокументаОснования + " от " + Формат(ДатаДокументаОснования, "ДФ=dd.MM.yyyy");
	КонецЕсли;
	
	НовСтр.ИмяФайлаДанных 	= СтрокаПредставляемыйДокумент.ИмяФайлаДанных;
	НовСтр.АдресФайлаДанных = СоответствиеФайлыПринадлежащиеОписи[СтрокаПредставляемыйДокумент.ИмяФайлаДанных];
	
	Если ЗначениеЗаполнено(СтрокаПредставляемыйДокумент.ИмяФайлаПодписи) Тогда
		
		НовСтр.ИмяФайлаПодписи 		= ИсправленнноеИмяФайлаЭПФайлаОписиИсходящихДокументов(
			СтрокаПредставляемыйДокумент.ИмяФайлаПодписи,
			СтрокаПредставляемыйДокумент.ИмяФайлаДанных);
			
		НовСтр.АдресФайлаПодписи	= СоответствиеФайлыПринадлежащиеОписи[СтрокаПредставляемыйДокумент.ИмяФайлаПодписи];
		
	Иначе
		// При отправке журнал учета СФ и книг не имеют файла подписи.
		// Адрес с данными подписи будет сформирован позже.
		НовСтр.ИмяФайлаПодписи 		= ИмяФайлаЭПФайлаОписиИсходящихДокументов(СтрокаПредставляемыйДокумент.ИмяФайлаДанных);
	КонецЕсли;
	
	НовСтр.ПодтверждениеКНД = СтрокаПредставляемыйДокумент.ПодтверждениеКНД; 
	
	Если ЗначениеЗаполнено(СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаДанных) Тогда
		НовСтр.ПодтверждениеИмяФайлаДанных 		= СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаДанных;
		НовСтр.ПодтверждениеАдресФайлаДанных 	= СоответствиеФайлыПринадлежащиеОписи[СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаДанных];
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаПодписи) Тогда
		
		НовСтр.ПодтверждениеИмяФайлаПодписи 	= ИсправленнноеИмяФайлаЭПФайлаОписиИсходящихДокументов(
			СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаПодписи,
			СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаДанных);
		НовСтр.ПодтверждениеАдресФайлаПодписи	= СоответствиеФайлыПринадлежащиеОписи[СтрокаПредставляемыйДокумент.ПодтверждениеИмяФайлаПодписи];
		
	КонецЕсли;
	
	// Представление нужно для вывода текста предупреждений
	Если ЗначениеЗаполнено(СтрокаПредставляемыйДокумент.СсылкаНаОбъект) Тогда
		НовСтр.ПредставлениеДокумента	= Строка(СтрокаПредставляемыйДокумент.СсылкаНаОбъект);
	Иначе
		НовСтр.ПредставлениеДокумента	= Строка(СтрокаПредставляемыйДокумент.ВидДокумента);
	КонецЕсли;
	
	НовСтр.ЭтоПакетСДопДокументами = ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Функция КодДокДляДокСкана(ОписьСсылка, ДокСкан)
	
	ЭтоПакетСДопДокументами = ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка);
	
	Если НЕ ЭтоПакетСДопДокументами Тогда
		// Для ответа на требование документов задан жестко
		КодДок = "9998";
	ИначеЕсли ЗначениеЗаполнено(ДокСкан.КВД) Тогда
		КодДок = ДокСкан.КВД;
	Иначе
		// Иной документ
		// Из ПРИКАЗа от 27 октября 2016 г. N ММВ-7-6/583@
		КодДок = "9999";
	КонецЕсли;
	
	Возврат КодДок;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОбщееКоличествоОтправляемыхФайловОписиИсходящихДокументов(ДанныеФайловВыгрузки)
	
	КоличествоФайлов = 0;
	
	ТаблицаНеДокФорм = ДанныеФайловВыгрузки.ТаблицаНеДокФорм;
	Для каждого СтрокаТаблицыДокСканов Из ТаблицаНеДокФорм Цикл
		
		КоличествоФайлов = КоличествоФайлов + СтрокаТаблицыДокСканов.МассивФайлов.Количество();
		
		ЕстьФайлЭП = ЗначениеЗаполнено(СтрокаТаблицыДокСканов.ИдФайлЭП);
		Если ЕстьФайлЭП Тогда
			КоличествоФайлов = КоличествоФайлов + 1;
		КонецЕсли;
		
	КонецЦикла; 
	
	ТаблицаДокФорм = ДанныеФайловВыгрузки.ТаблицаДокФорм;
	Для каждого СтрокаТаблицыДокФорм Из ТаблицаДокФорм Цикл
		Если ЗначениеЗаполнено(СтрокаТаблицыДокФорм.ПодтверждениеИмяФайлаДанных) Тогда
			КоличествоФайлов = КоличествоФайлов + 4;
		Иначе
			КоличествоФайлов = КоличествоФайлов + 2;
		КонецЕсли;
	КонецЦикла;
	
	Возврат КоличествоФайлов;
	
КонецФункции

&НаСервере
Функция ВыгрузитьОписьИсходящихДокументов(Контекст, ТранспортноеСообщение, ДанныеФайловВыгрузки)
	
	УникальныйИдентификаторФормы = Контекст.УникальныйИдентификатор;
	ОписьСсылка = Контекст.Документ;
	
	ИдентификаторОтправителяПолучателя = СформироватьИдентификаторОтправителяПолучателя(ТранспортноеСообщение);
	
	ДанныеФайловВыгрузки = ПолучитьДанныеПоФайламВыгрузкиОписиИсходящихДокументов(ОписьСсылка, ИдентификаторОтправителяПолучателя, УникальныйИдентификаторФормы);	
	Если ДанныеФайловВыгрузки = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;	
	
	Организация 		= ОписьСсылка.Организация;
	КодНалоговогоОргана = СокрЛП(ОписьСсылка.НалоговыйОрган.Код);
	ЭлРегистрацияВИФНС 	= РегистрацияВИФНСПоОрганизацииИКодуНО(ОписьСсылка.Организация, КодНалоговогоОргана);
	
	ПараметрыОписи = СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана);
	
	Если НЕ ПроверитьЗаполнениеПараметровПриОтправкеПредставления(ПараметрыОписи) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка) Тогда 
		
		ИмяФайла = ИмяФайлаПервичногоСообщения(ОписьСсылка.Основание);
		
		Если ИмяФайла = Неопределено Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить имя файла документа, для которого отправляется %1'");
			ТекстСообщения = СтрШаблон(ТекстСообщения, ДокументооборотСКОКлиентСервер.ПредставлениеПакетаСДопДокументами(Истина));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		ИдФайлОсн = ИмяФайлаБезРасширения(ИмяФайла);
		
	Иначе
		
		ВложенияСДанными = ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(ОписьСсылка.Основание);
		Если ВложенияСДанными.Количество() = 0 Тогда
			ИдФайлОсн = ИмяФайлаТребованияИзОписания(ОписьСсылка.Основание);
		Иначе
			ИдФайлОсн = ИмяФайлаБезРасширения(ВложенияСДанными[0].ИмяФайла);
		КонецЕсли;
		
		Если ИдФайлОсн = "" Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить имя файла требования. Возможно у требования отсутствуют вложения'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	ПараметрыОписи.Вставить("ИдФайлОсн", ИдФайлОсн);
	
	ПараметрыОписи.Вставить("ЭтоОтветНаТребованиеДокументов", Истина);
	
	// формируем имя файла представления
	ИмяФайлаПредставления = Неопределено;
	
	//при подсчете количества вложений не учитываем 6 служебных файла и служебные подписи вложений (делим пополам)
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КолФайл = ПолучитьОбщееКоличествоОтправляемыхФайловОписиИсходящихДокументов(ДанныеФайловВыгрузки); 
	
	// формируем XML представления
	ТекстПредставления = СформироватьXMLПредставления(Организация, ДанныеФайловВыгрузки, ПараметрыОписи, ИмяФайлаПредставления, КолФайл);
	Если НЕ ЗначениеЗаполнено(ТекстПредставления) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать XML файл описи исходящих документов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	КодировкаТекстаДанных = "windows-1251";
	ДвоичныеДанныеФайла = ПолучитьДвоичныеДанныеИзСтроки(ТекстПредставления, КодировкаТекстаДанных);
	СсылкаНаДвоичныеДанныеФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, УникальныйИдентификаторФормы);
	
	ДанныеФайлаПредставление = Новый Структура;
	ДанныеФайлаПредставление.Вставить("ИмяФайла", ИмяФайлаПредставления);
	ДанныеФайлаПредставление.Вставить("АдресВременногоХранилища", СсылкаНаДвоичныеДанныеФайла);
	
	ДанныеФайловВыгрузки.Вставить("ДанныеФайлаПредставление", ДанныеФайлаПредставление);
	
	Возврат Истина;

КонецФункции

&НаСервереБезКонтекста
Функция ИмяФайлаПервичногоСообщения(Ссылка)
	
	ЦиклОбмена = ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Ссылка);
	
	Если НЕ ЗначениеЗаполнено(ЦиклОбмена) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	СообщенияЦикла = КонтекстЭДОСервер.ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,Ложь,Истина);
	
	Если СообщенияЦикла.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПервичноеСообщение = Неопределено;
	
	ТипыПервичныхСообщений = ДокументооборотСКОВызовСервера.ТипПервичныхСообщений();
	Для каждого Сообщение Из СообщенияЦикла Цикл
		Для каждого ТипПервичныхСообщений Из ТипыПервичныхСообщений Цикл
			Если Сообщение.Тип = ТипПервичныхСообщений Тогда
				ПервичноеСообщение = Сообщение;
				Прервать;
			КонецЕсли;
		КонецЦикла; 
	КонецЦикла; 

	Если ПервичноеСообщение = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ФильтрПоТипу = ДокументооборотСКОВызовСервера.ТипыСодержимогоТранспортногоКонтейнераПервичныхСообщений();
	
	ТаблицаСРезультатом = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(
		ПервичноеСообщение,
		Истина, 
		ФильтрПоТипу,
		,
		Истина);
		
	ИмяФайла = Неопределено;
	
	Для Каждого СтрокаРезультата Из ТаблицаСРезультатом Цикл
		
		Если ТипЗнч(Ссылка) = Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов") Тогда
			
			ИмяФайла = ПолучитьИмяВременногоФайла();
			Данные   = СтрокаРезультата.Данные.Получить();
			Данные.Записать(ИмяФайла);
			
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
			Свойства = КонтекстЭДОСервер.ПолучитьСвойстваОписиВходящихДокументов(ПервичноеСообщение.Ссылка, ИмяФайла);
			
			Если Свойства.ТаблицаДокументов.Количество() = 0 Тогда
				Возврат Неопределено;
			КонецЕсли;
			
			НайденДокумент = Ложь;
			
			// Для требований и уведомлений ищем название файла ровно для этого требования, а не для всей описи
			Для каждого Документ Из Свойства.ТаблицаДокументов Цикл
				
				Если Ссылка.ДатаДокумента = Документ.РеквизитыДокумента.ДатаДокумента
					И Ссылка.НомерДокумента = Документ.РеквизитыДокумента.НомерДокумента Тогда
					
					Для каждого Вложение Из Документ.СписокИменФайловВложений Цикл
						ИмяФайла = Вложение.Значение;
						НайденДокумент = Истина;
						Прервать;
					КонецЦикла; 
					
				КонецЕсли;
				
				Если НайденДокумент Тогда
					Прервать;
				КонецЕсли;
				
			КонецЦикла;
			
			Если НЕ НайденДокумент Тогда
				ИмяФайла = СтрокаРезультата.ИмяФайла;
			КонецЕсли;
			
		Иначе
			ИмяФайла = СтрокаРезультата.ИмяФайла;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ИмяФайла;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьФайлыПринадлежащиеОписи(ОписьСсылка, УникальныйИдентификаторФормы, СписокИменФайлов = Неопределено) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьФайлыПринадлежащиеОписи(ОписьСсылка, УникальныйИдентификаторФормы, СписокИменФайлов);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLПредставления(
		Организация,
		ДанныеФайловВыгрузки, 
		ПараметрыОписи, 
		ИмяФайлаПредставления, 
		КолФайл)
	
	// создаем XML-дерево
	ДеревоXML = СоздатьДеревоXML();
	
	СформироватьXMLПредставления_ИдФайл(ДеревоXML, ПараметрыОписи, ИмяФайлаПредставления, Организация);
	
	ДобавитьУзелXML(ДеревоXML, "ВерсПрог", Истина, Лев(РегламентированнаяОтчетность.НазваниеИВерсияПрограммы(), 40));
	ДобавитьУзелXML(ДеревоXML, "ВерсФорм", Истина, "5.03");
	
	// создаем узел Документ
	УзелДокумент = ДобавитьУзелXML(ДеревоXML, "Документ");
	ДобавитьУзелXML(УзелДокумент, "КНД", Истина, "1184002");
	ДобавитьУзелXML(УзелДокумент, "ДатаДок", Истина, XMLСтрока(Формат(ТекущаяДатаСеанса(), "ДФ=dd.MM.yyyy")));

	СформироватьXMLПредставления_СвОтпрДок(УзелДокумент, ПараметрыОписи);
	СформироватьXMLПредставления_СвПолДок(УзелДокумент, ПараметрыОписи);
	СформироватьXMLПредставления_СвНП(УзелДокумент, ПараметрыОписи);
	СформироватьXMLПредставления_Подписант(УзелДокумент, ПараметрыОписи);
	
	Если НЕ СформироватьXMLПредставления_ДокПредстНО(УзелДокумент, ПараметрыОписи, ДанныеФайловВыгрузки, КолФайл) Тогда
		Возврат "";
	КонецЕсли;
	
	// формируем XML-строку на основе дерева
	ТекстXML = ВыгрузитьДеревоВXML_ПФР(ДеревоXML, "Файл");
	
	Возврат ТекстXML;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLПредставления_ИдФайл(ДеревоXML, ПараметрыОписи, ИмяФайлаПредставления, Организация)
	
	// формируем идентификатор отправителя
	Если ПараметрыОписи.ПрПодп = 2 Тогда
		СвПред = ПараметрыОписи.Доверенность.СвУпПред.СвПред;
		Если СвПред.Свойство("СвОрг") Тогда
			ИННКПП = СокрЛП(СвПред.СвОрг.ИННЮЛ) + СокрЛП(СвПред.СвОрг.КПП);
		Иначе
			Если НЕ ПараметрыОписи.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННКПП = СокрЛП(СвПред.СведФизЛ.ИННФЛ);
			ИначеЕсли ПараметрыОписи.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации
				И ПараметрыОписи.Доверенность.СвУпПред.СвПред.СведФизЛ.Свойство("ЯвляетсяСотрудникомОрганизацииПоМЧД")
				И ПараметрыОписи.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизацииПоМЧД = Ложь
				И ДокументооборотСКОКлиентСервер.ПараметрыОтправкиПоМЧДФНС("ИзменятьФайлВыгрузкиДляФизическогоЛица") Тогда
				ИННКПП = ?(ПараметрыОписи.Доверенность.СвПредст.ЯвляетсяФЛ, СокрЛП(ПараметрыОписи.Доверенность.СвПредст.ИННФЛ),
					СокрЛП(ПараметрыОписи.Доверенность.СвПредст.ИННЮЛ) + СокрЛП(ПараметрыОписи.КППЮЛ));
			Иначе
				ИННКПП = ?(ПараметрыОписи.ЭтоПБОЮЛ, СокрЛП(ПараметрыОписи.ИННФЛ), СокрЛП(ПараметрыОписи.ИННЮЛ) + СокрЛП(ПараметрыОписи.КППЮЛ));
			КонецЕсли;
		КонецЕсли;
	Иначе
		ИННКПП = ?(ПараметрыОписи.ЭтоПБОЮЛ, СокрЛП(ПараметрыОписи.ИННФЛ), СокрЛП(ПараметрыОписи.ИННЮЛ) + СокрЛП(ПараметрыОписи.КППЮЛ));
	КонецЕсли;
	
	ЭтоКрупнейший    = ДокументооборотСКО.ЭтоКрупнейший(Организация);
	КодНОКрупнейшего = ДокументооборотСКО.КодНОКрупнейшего(Организация);
	
	ИмяФайлаБезРасширения = "ON_DOCNPNO_"
							 + ?(ЭтоКрупнейший И ЗначениеЗаполнено(КодНОКрупнейшего), СокрЛП(КодНОКрупнейшего), ПараметрыОписи.КодНО)
							 + "_"
							 + ПараметрыОписи.КодНО
							 + "_"
							 + ИННКПП
							 + "_"
							 + Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd")
							 + "_"
							 + Строка(Новый УникальныйИдентификатор);

	ИмяФайлаБезРасширения = ВРЕГ(ИмяФайлаБезРасширения);
	
	ИмяФайлаПредставления = ИмяФайлаБезРасширения + ".xml";
	
	// иниицализируем атрибуты корневого элемента
	ДобавитьУзелXML(ДеревоXML, "ИдФайл", Истина, ИмяФайлаБезРасширения);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLПредставления_ДокПредстНО(УзелДокумент, ПараметрыОписи, ДанныеФайловВыгрузки, КолФайл)
	
	 // формируем основную часть файла
	Если ПараметрыОписи.Свойство("ЭтоОтветНаТребованиеДокументов") Тогда
		
		Если НЕ ДобавитьВДеревоДокументыРеализацииПолномочий(УзелДокумент, ДанныеФайловВыгрузки, ПараметрыОписи, КолФайл) Тогда
			Возврат Ложь;
		Конецесли;
		
	ИначеЕсли ПараметрыОписи.Свойство("ЭтоОтветНаТребованиеПоНДС") Тогда
		
		ИдФайлОсн 	= ПараметрыОписи.ИмяФайлТребПояс;
		ИдФайл 		= ДанныеФайловВыгрузки.ИдФайл;
		ИдФайлЭП	= ДанныеФайловВыгрузки.ИдФайлЭП;
		
		ДобавитьУзелДокФорм_ДокПредстНО(УзелДокумент, "1160200", ИдФайлОсн, КолФайл, ИдФайл, ИдФайлЭП);
		
	ИначеЕсли ПараметрыОписи.Свойство("ЭтоЖурналСФ") Тогда
		
		ИдФайлОсн 	= "ЖУРНАЛ";
		ИдФайл 		= ДанныеФайловВыгрузки.ИдФайл;
		ИдФайлЭП 	= ДанныеФайловВыгрузки.ИдФайлЭП;
		
		ДобавитьУзелДокФорм_ДокПредстНО(УзелДокумент, "1115104", ИдФайлОсн, КолФайл, ИдФайл, ИдФайлЭП);
		
	Иначе
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось определить содержимое описи.'"),);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			
		Возврат Ложь;
		
	КонецЕсли;
	
	Возврат Истина;

КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLПредставления_Подписант(УзелДокумент, ПараметрыОписи)
			
	УзелПодписант = ДобавитьУзелXML(УзелДокумент, "Подписант");
	
	ЭтоИП = ПараметрыОписи.ЭтоПБОЮЛ;
	Если ЭтоИП Тогда
		ПрПодп = ПараметрыОписи.ПрПодп;
	Иначе
		ПрПодп = ПараметрыОписи.ПрПодп + 2;
	КонецЕсли;
	ДобавитьУзелXML(УзелПодписант, "ПрПодп", Истина, ПрПодп);
		
	УзелФИО = ДобавитьУзелXML(УзелПодписант, "ФИО");
	Если ПараметрыОписи.ПрПодп = 1 Тогда
		Если НЕ ПараметрыОписи.ЭтоПБОЮЛ Тогда
			ФИОПодписанта = РегламентированнаяОтчетность.РазложитьФИО(ПараметрыОписи.ФИОРук);
			ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Фамилия)), 60));
			ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Имя)), 60));
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Отчество)), 60), Истина);
		Иначе
			ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПФамилия)), 60));
			ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПИмя)), 60));
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПОтчество)), 60), Истина);
		КонецЕсли;
	Иначе
		ФИОПодписанта = ДокументооборотСКОВызовСервера.УточнитьФИОПредставителя(
			ПараметрыОписи.Доверенность.СвУпПред.СвПред.СведФизЛ.ФИО,
			ПараметрыОписи.Доверенность.ДанныеПредставителя);
		ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Фамилия)), 60));
		ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Имя)), 60));
		ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПодписанта.Отчество)), 60), Истина);
		
		УзелСвПред = ДобавитьУзелXML(УзелПодписант, "СвПред");
		
		СвДоверенность = ПараметрыОписи.Доверенность;
		УчСвДов = СвДоверенность.УчСвДов;
		
		ДобавитьУзелXML(УзелСвПред, "НаимДок", Истина, СвДоверенность.ДанныеПредставителя.ДокументПредставителя);
	КонецЕсли;
			
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLПредставления_СвНП(УзелДокумент, ПараметрыОписи)
		
	УзелСвНП = ДобавитьУзелXML(УзелДокумент, "СвНП");
	Если НЕ ПараметрыОписи.ЭтоПБОЮЛ Тогда
		
		УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "НПЮЛ");
		
		ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(ПараметрыОписи.НаимЮЛПол), 1000));
		ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, XMLСтрока(ПараметрыОписи.ИННЮЛ));
		ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, XMLСтрока(ПараметрыОписи.КППЮЛ));
		
	Иначе
		
		УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "НПФЛ");
		
		ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыОписи.ИННФЛ)), Истина);
		УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
		
		ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПФамилия)), 60));
		ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПИмя)), 60));
		ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПОтчество)), 60), Истина);
		
	КонецЕсли;
		
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьXMLПредставления_СвПолДок(УзелДокумент, ПараметрыОписи)
		
	УзелСвПолуч = ДобавитьУзелXML(УзелДокумент, "СвПолДок");
	УзелОтпрНО	= ДобавитьУзелXML(УзелСвПолуч, "ОтпрНО");
	ДобавитьУзелXML(УзелОтпрНО, "КодНО", Истина, ПараметрыОписи.КодНО);
	НаименованиеНО = НаименованиеНОПОКоду(ПараметрыОписи.КодНО);
	ДобавитьУзелXML(УзелОтпрНО, "НаимНО", Истина, НаименованиеНО);
		
КонецФункции
		
&НаСервереБезКонтекста
Функция СформироватьXMLПредставления_СвОтпрДок(УзелДокумент, ПараметрыОписи)
	
	// добавляем сведения об отправителе
	УзелСвОтпр = ДобавитьУзелXML(УзелДокумент, "СвОтпрДок");

	Если ПараметрыОписи.ПрПодп = 1 Тогда // отправитель - налогоплательщик
		
		Если НЕ ПараметрыОписи.ЭтоПБОЮЛ Тогда
			УзелОтпрЮЛ = ДобавитьУзелXML(УзелСвОтпр, "ОтпрЮЛ");
			ДобавитьУзелXML(УзелОтпрЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(ПараметрыОписи.НаимЮЛПол), 1000));
			ДобавитьУзелXML(УзелОтпрЮЛ, "ИННЮЛ", Истина, XMLСтрока(ПараметрыОписи.ИННЮЛ));
			ДобавитьУзелXML(УзелОтпрЮЛ, "КПП", Истина, XMLСтрока(ПараметрыОписи.КППЮЛ));
		Иначе
			УзелОтпрФЛ = ДобавитьУзелXML(УзелСвОтпр, "ОтпрФЛ");
			ДобавитьУзелXML(УзелОтпрФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыОписи.ИННФЛ)), Истина);
			УзелФИО = ДобавитьУзелXML(УзелОтпрФЛ, "ФИО");
			ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПФамилия)), 60));
			ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПИмя)), 60));
			ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПОтчество)), 60), Истина);
		КонецЕсли;
		
	Иначе // отправитель - представитель
		
		СвДоверенность = ПараметрыОписи.Доверенность;
		СвПредст = СвДоверенность.СвУпПред.СвПред;
		
		Если СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
			Если НЕ ПараметрыОписи.ЭтоПБОЮЛ Тогда
				УзелОтпрЮЛ = ДобавитьУзелXML(УзелСвОтпр, "ОтпрЮЛ");
				ДобавитьУзелXML(УзелОтпрЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(ПараметрыОписи.НаимЮЛПол), 1000));
				ДобавитьУзелXML(УзелОтпрЮЛ, "ИННЮЛ", Истина, XMLСтрока(ПараметрыОписи.ИННЮЛ));
				ДобавитьУзелXML(УзелОтпрЮЛ, "КПП", Истина, XMLСтрока(ПараметрыОписи.КППЮЛ));
			Иначе
				УзелОтпрФЛ = ДобавитьУзелXML(УзелСвОтпр, "ОтпрФЛ");
				ДобавитьУзелXML(УзелОтпрФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(ПараметрыОписи.ИННФЛ)), Истина);
				УзелФИО = ДобавитьУзелXML(УзелОтпрФЛ, "ФИО");
				ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПФамилия)), 60));
				ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПИмя)), 60));
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ПараметрыОписи.ИПОтчество)), 60), Истина);
			КонецЕсли;
		Иначе	
			Если СвПредст.Свойство("СвОрг") Тогда // представитель - ЮЛ
				УзелОтпрЮЛ = ДобавитьУзелXML(УзелСвОтпр, "ОтпрЮЛ");
				ДобавитьУзелXML(УзелОтпрЮЛ, "НаимОрг", Истина, Лев(XMLСтрока(СокрЛП(СвПредст.СвОрг.НаимОрг)), 1000));
				ДобавитьУзелXML(УзелОтпрЮЛ, "ИННЮЛ", Истина, XMLСтрока(СвПредст.СвОрг.ИННЮЛ));
				ДобавитьУзелXML(УзелОтпрЮЛ, "КПП", Истина, XMLСтрока(СвПредст.СвОрг.КПП));
			Иначе
				УзелОтпрФЛ = ДобавитьУзелXML(УзелСвОтпр, "ОтпрФЛ");
				ДобавитьУзелXML(УзелОтпрФЛ, "ИННФЛ", Истина, XMLСтрока(СокрЛП(СвПредст.СведФизЛ.ИННФЛ)), Истина);
				ФИОПредставителя = СвПредст.СведФизЛ.ФИО;
				УзелФИО = ДобавитьУзелXML(УзелОтпрФЛ, "ФИО");
				ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Фамилия)), 60));
				ДобавитьУзелXML(УзелФИО, "Имя", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Имя)), 60));
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, Лев(XMLСтрока(СокрЛП(ФИОПредставителя.Отчество)), 60), Истина);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция НаименованиеНОПОКоду(КодНО)

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ ПЕРВЫЕ 1
		|	РегистрацииВНалоговомОргане.Наименование КАК Наименование
		|ИЗ
		|	Справочник.РегистрацииВНалоговомОргане КАК РегистрацииВНалоговомОргане
		|ГДЕ
		|	РегистрацииВНалоговомОргане.Код = &Код";

	Запрос.УстановитьПараметр("Код", КодНО);

	РезультатЗапроса = Запрос.Выполнить();

	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();

	Наименование = "";
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Наименование = ВыборкаДетальныеЗаписи.Наименование;
	КонецЦикла;
		
	Если Наименование = "" Тогда
		Наименование = НСтр("ru = 'ИФНС '") + Строка(КодНО);
	КонецЕсли;

	Возврат Наименование;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСоответствиеКНДПоВидуДокумента()
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Виды = КонтекстЭДОСервер.ВидыПредставляемыхДокументов();
	
	Соответствие = Новый Соответствие;
	Для каждого Вид Из Виды Цикл
		Если ЗначениеЗаполнено(Вид.КНД) Тогда
			Соответствие.Вставить(Вид.Значение, Вид.КНД);
		КонецЕсли;
	КонецЦикла; 
	
	Возврат Соответствие;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(СсылкаДокумент, ИменаВложений = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(СсылкаДокумент, ИменаВложений);
	
КонецФункции


&НаСервереБезКонтекста
Функция УдалитьФайлыДокументовРеализацииПолномочийНалоговыхОргановПоВладельцу(ВладелецСсылка, МассивИменФайлов) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.УдалитьФайлыДокументовРеализацииПолномочийНалоговыхОргановПоВладельцу(ВладелецСсылка, МассивИменФайлов);
	
КонецФункции
///////////////////////////////////////////////////////////////////////////////
// Представление уведомлений о котролируемых сделках в налоговые органы
&НаСервереБезКонтекста
Функция ВыгрузитьУведомлениеФНС(УведомлениеСсылка, ДанныеФайловВыгрузки)
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДанныеФайловВыгрузки = Новый Структура("ДанныеФайлаПредставление");
	
	// выгружаем файлы уведомления
	
	// создаем объект
	УведомлениеОбъект = УведомлениеСсылка.ПолучитьОбъект();
	
	// формируем текст выгрузки
	Если ТипЗнч(УведомлениеОбъект) = Тип("ДокументОбъект.УведомлениеОСпецрежимахНалогообложения") Тогда
		ИдентификаторПолучателя = ДлительнаяОтправкаКлиентСервер.ЗначенияПараметровДлительнойОтправки()["ИдентификаторПолучателя"];
		МассивВыгруженныхУведомлений = УведомлениеОбъект.ВыгрузитьДокумент(ИдентификаторПолучателя);
	Иначе
		МассивВыгруженныхУведомлений = УведомлениеОбъект.ВыгрузитьДокументСРазделениемНаФайлы();
	КонецЕсли;
	
	Если МассивВыгруженныхУведомлений = Неопределено Тогда
		Возврат Ложь;
	Иначе
		ДанныеФайловВыгрузки = МассивВыгруженныхУведомлений;
	КонецЕсли;

	Возврат Истина;

КонецФункции

// Представление уведомлений в налоговые органы
///////////////////////////////////////////////////////////////////////////////


&НаКлиенте
Процедура ВыгрузитьЗапросИОСВФайл(ДокЗапрос, УникальныйИдентификатор = Неопределено) Экспорт
	Перем Запрос, ИмяФайлаЗапроса;
	
	Если СформироватьФайлЗапросаИОС(ДокЗапрос, Запрос, ИмяФайлаЗапроса, УникальныйИдентификатор) Тогда
		ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(Новый Структура("Адрес, Имя", Запрос, ИмяФайлаЗапроса));
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОпределитьТипПриложенияКОтветуНаЗапросПФР(Данные)
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	
	Если ТипЗнч(Данные) = Тип("ДвоичныеДанные") Тогда
		Данные.Записать(ВремФайл);
	ИначеЕсли ЭтоАдресВременногоХранилища(Данные) Тогда
		ПолучитьИзВременногоХранилища(Данные).Записать(ВремФайл);
	Иначе
		Данные.Получить().Записать(ВремФайл);
	КонецЕсли;
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ВремФайл);
	Если ДеревоXML = Неопределено Тогда
		Возврат 0;
	ИначеЕсли ДеревоXML.Строки.Найти("справка", "Имя") <> Неопределено ИЛИ ДеревоXML.Строки.Найти("справкаОСостоянииРасчетов", "Имя") <> Неопределено Тогда
		Возврат 1;
	ИначеЕсли ДеревоXML.Строки.Найти("сверка", "Имя") <> Неопределено Тогда
		Возврат 2;
	Иначе
		Возврат 0;
	КонецЕсли;
	
КонецФункции

#Область ОтправкаЗапросаИОС

&НаКлиенте
Процедура ОтправкаЗапросаИОС(Док, Организация, ВыполняемоеОповещение) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Док) Тогда
		Возврат;
	КонецЕсли;
	
	ВидУслуги = ПолучитьРеквизитНаСервере(Док, "ВидУслуги");
	Если ВидУслуги = ПредопределенноеЗначение("Перечисление.ВидыУслугПриИОС.СправкаОСостоянииРасчетов") Тогда
		 ТекстОшибки = НСтр("ru = 'Отправка запроса на сверку невозможна по причине:
                            |ПФР более не поддерживает запрос справок о состоянии расчетов.'");
		 ПоказатьПредупреждение(, ТекстОшибки);
		 Возврат;
	КонецЕсли;
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док",                   Док);
	КонтекстДлительнойОперации.Вставить("Организация",           Организация);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаИОС_ПослеПроверкиЗаключенногоСоглашенияСПФР", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ПроверитьЧтоЗаключеноСоглашениеСПФР(Док, Организация, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаЗапросаИОС_ПослеПроверкиЗаключенногоСоглашенияСПФР(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> Истина Тогда
		
		ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР
		
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаИОС_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ВходящийКонтекст.Док, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаЗапросаИОС_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;

	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка сверки в ПФР
		Возврат;
	КонецЕсли;
		
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаЗапросаИОС", 0.1, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаЗапросаИОС() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Контекст = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(КонтекстДлительнойОперации);
	Док      = Контекст.Док;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаИОСПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		Контекст);
		
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаИОС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Док;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ОтправкаЗапросаИОС(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение);

КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаЗапросаИОСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаЗапросаИОС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.Док, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаИОС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	Док = ВходящийКонтекст.Док;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", Док);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");

	// отражаем изменения в базе
	Если НЕ ОтправкаЗапросаИОС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаЗапросаИОС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		Иначе
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Док);
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР, ИОС
			
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ВыполняемоеОповещение, Док); //+
		Возврат;
		
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаЗапросаИОСПослеФормированияПакета", ЭтотОбъект, Контекст);
		
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ПФР");
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаЗапросаИОСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР, ИОС
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР, ИОС
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ПФР, ИОС
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция ОбъектЗаполненКорректно(Ссылка) Экспорт
	
	Возврат Ссылка.ПолучитьОбъект().ПроверитьЗаполнение();
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаЗапросаИОС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Док = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Док) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Док, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ПФР
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСПФР") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, указан ли получатель
	ПолучательДокумента = Док.Получатель;
	Если НЕ ЗначениеЗаполнено(ПолучательДокумента) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель запроса.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ДвоичныеДанныеФайлаЗапроса = Неопределено;
	ИмяФайлаЗапроса = Неопределено;
	РезультатФормированияФайлаЗапроса = СформироватьФайлЗапросаИОС(Док, ДвоичныеДанныеФайлаЗапроса, ИмяФайлаЗапроса);
	Если НЕ РезультатФормированияФайлаЗапроса Тогда
		Возврат Ложь;
	КонецЕсли;
	
	КороткоеИмяФайлаПисьма = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР;
	ЦиклОбмена.Предмет = Док;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = ПолучательДокумента;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.ИОС;
	ЦиклОбмена.ДатаНачалаПериода = Док.Дата;
	ЦиклОбмена.ДатаОкончанияПериода = Док.Дата;
	ЦиклОбмена.ВидУслуги = Док.ВидУслуги;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = ПолучательДокумента;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
		
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем запрос
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЗапросИОС, ДвоичныеДанныеФайлаЗапроса, ИмяФайлаЗапроса, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ТекстСообщения = НСтр("ru = 'Отправка запроса прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Док, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции


&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОС(Запрос, ВыполняемоеОповещение = Неопределено) Экспорт
		
	// получаем все документы к показу
	СформироватьТаблицуОтветовНаЗапросИОС(Запрос);
	
	// определяем документ, который показываем
	КоличествоОтветов = ОтветыНаЗапросыИОС.Количество();
	
	Если КоличествоОтветов = 0 Тогда
		
		ТекстПредупреждения = НСтр("ru = 'В пакете с ответом от ПФР отсутствует запрошенный документ.
                                    |Для получения корректного ответа попробуйте отправить запрос в виде письма.'");
		ПоказатьПредупреждение(,ТекстПредупреждения);
		
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь)
		КонецЕсли;
	ИначеЕсли КоличествоОтветов > 1 Тогда
		СписокВыбора = Новый СписокЗначений;
		Для Каждого СтрокаОтвета Из ОтветыНаЗапросыИОС Цикл
			СписокВыбора.Добавить(СтрокаОтвета.ИмяФайла);
		КонецЦикла;
		ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, СписокВыбора, Запрос", ВыполняемоеОповещение, СписокВыбора, Запрос);
		ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьОтветНаЗапросИОСЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		СписокВыбора.ПоказатьВыборЭлемента(ОписаниеОповещения);
	Иначе
		ПоказатьОтветНаЗапросИОСНаКлиенте(Запрос, ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОСНаКлиенте(Запрос, ВыполняемоеОповещение)
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОтметитьКакПрочтенное(Запрос);
	
	ОтветНаЗапросИОС	= ОтветыНаЗапросыИОС[0];
	ЭтоВебКлиент 		= ЭтоВебКлиент();
	
	Если ОтветНаЗапросИОС.ВАрхиве Тогда
		
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3);

		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина)
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	// перебираем ответы
	ОтветОтрицательный = (ОтветыНаЗапросыИОС.НайтиСтроки(Новый Структура("ОтветОтрицательный", Истина)).Количество() > 0);
	Если ОтветОтрицательный Тогда
		Если ЭтоВебКлиент Тогда
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла);
		Иначе			
			Если ОтветНаЗапросИОС.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст1251 Тогда
				
				ПоказатьТекстИзСтроки(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла, "windows-1251");
				
			ИначеЕсли ОтветНаЗапросИОС.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст866 Тогда
				
				ПоказатьТекстИзСтроки(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла, "cp866");
				
			Иначе
				
				ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла);
				
			КонецЕсли;
		КонецЕсли;
	Иначе
		ТипОтветаНаЗапрос = ОпределитьТипПриложенияКОтветуНаЗапросПФР(ОтветНаЗапросИОС.Адрес);
		Если ТипОтветаНаЗапрос = 1 Тогда
			ПоказатьСправкуОСостоянииРасчетовИОС(Запрос);
		ИначеЕсли ТипОтветаНаЗапрос = 2 Тогда
			ПоказатьСверкуФИОиСНИЛС(Запрос);
		Иначе
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла);
		КонецЕсли;
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина)
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросСправкиОРасчетах(Запрос, ВыполняемоеОповещение = Неопределено) Экспорт
	
	ВходящийФайл = ПолучитьСсылкуНаФайл(Запрос);
	ДанныеФайла = РаботаСФайламиСлужебныйВызовСервера.ДанныеФайлаДляОткрытия(ВходящийФайл,
		Неопределено, УникальныйИдентификатор, Неопределено);
	РаботаСФайламиКлиент.ОткрытьФайл(ДанныеФайла, Ложь);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСсылкуНаФайл(Ссылка)
	
	Возврат Ссылка.ВходящийФайл;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьОтветНаЗапросСправкиОРасчетахЗавершение(РезультатВыбора, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СписокВыбора = ДополнительныеПараметры.СписокВыбора;
	Запрос = ДополнительныеПараметры.Запрос;
	
	Если РезультатВыбора = Неопределено Тогда
		
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		КонецЕсли;
		
	Иначе
		
		// удаляем не помеченные строки из таблицы ответов
		Для Каждого СтрСписокВыбора Из СписокВыбора Цикл
			Если СтрСписокВыбора.Значение <> РезультатВыбора.Значение Тогда
				СтрОтветНаЗапросИОС = ОтветыНаЗапросыИОС.НайтиСтроки(Новый Структура("ИмяФайла", СтрСписокВыбора.Значение));
				ОтветыНаЗапросыИОС.Удалить(СтрОтветНаЗапросИОС[0]);
			КонецЕсли;
		КонецЦикла;
		
		ПоказатьОтветНаЗапросСправкиОРасчетахНаКлиенте(Запрос, ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросСправкиОРасчетахНаКлиенте(Запрос, ВыполняемоеОповещение)
	
	//ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОтметитьКакПрочтенное(Запрос);
	
	ОтветНаЗапросИОС	= ОтветыНаЗапросыИОС[0];
	ЭтоВебКлиент 		= ЭтоВебКлиент();
	
	Если ОтветНаЗапросИОС.ВАрхиве Тогда
		
		ПоказатьУведомлениеАрхивныхФайлов(, 17, 3);

		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина)
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	// перебираем ответы
	ОтветОтрицательный = (ОтветыНаЗапросыИОС.НайтиСтроки(Новый Структура("ОтветОтрицательный", Истина)).Количество() > 0);
	Если ОтветОтрицательный Тогда
		Если ЭтоВебКлиент Тогда
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла);
		Иначе			
			Если ОтветНаЗапросИОС.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст1251 Тогда
				
				ПоказатьТекстИзСтроки(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла, "windows-1251");
				
			ИначеЕсли ОтветНаЗапросИОС.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст866 Тогда
				
				ПоказатьТекстИзСтроки(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла, "cp866");
				
			Иначе
				
				ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла);
				
			КонецЕсли;
		КонецЕсли;
	Иначе
		ТипОтветаНаЗапрос = ОпределитьТипПриложенияКОтветуНаЗапросПФР(ОтветНаЗапросИОС.Адрес);
		Если ТипОтветаНаЗапрос = 1 Тогда
			ПоказатьСправкуОСостоянииРасчетовИОС(Запрос);
		ИначеЕсли ТипОтветаНаЗапрос = 2 Тогда
			ПоказатьСверкуФИОиСНИЛС(Запрос);
		Иначе
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ОтветНаЗапросИОС.Адрес, ОтветНаЗапросИОС.ИмяФайла);
		КонецЕсли;
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина)
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПриложениеКОтветуНаЗапросПФР(СтрВложение, ВыполняемоеОповещение = Неопределено) Экспорт
	
	Если СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_xml Тогда
		ТипПриложения = ОпределитьТипПриложенияКОтветуНаЗапросПФР(СтрВложение.Данные);
		Если ТипПриложения = 1 Тогда // справка о состоянии расчетов
			ПоказатьСправкуОСостоянииРасчетовИОС(СтрВложение.ТранспортноеСообщение);
		ИначеЕсли ТипПриложения = 2 Тогда // сверка ФИО-СНИЛС
			ПоказатьСверкуФИОиСНИЛС(СтрВложение.ТранспортноеСообщение);
		Иначе
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(СтрВложение.Адрес, СтрВложение.ИмяФайла);
		КонецЕсли;
	ИначеЕсли ЭтоВебКлиент() ИЛИ ОбщегоНазначенияКлиент.ЭтоLinuxКлиент() ИЛИ ОбщегоНазначенияКлиент.ЭтоMacOSКлиент() Тогда
		
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(СтрВложение.Адрес, СтрВложение.ИмяФайла);
		
	ИначеЕсли СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_html Тогда
		
		ПоказатьHTML(СтрВложение.ИмяФайла, СтрВложение.Данные, Ложь);
		
	ИначеЕсли СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст1251 Тогда
		
		ПоказатьТекстИзСтроки(СтрВложение.Данные, СтрВложение.ИмяФайла, "windows-1251");
		
	ИначеЕсли СтрВложение.ТипСодержимогоФайла = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_Текст866 Тогда
		
		ПоказатьТекстИзСтроки(СтрВложение.Данные, СтрВложение.ИмяФайла, "cp866");
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(СтрВложение.Адрес, СтрВложение.ИмяФайла);
	КонецЕсли;
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ЭтоВебКлиент() Экспорт
	
	ЭтоВебКлиент = Ложь;
	#Если ВебКлиент Тогда
		ЭтоВебКлиент = Истина; 
	#КонецЕсли
	
	Возврат ЭтоВебКлиент;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьСверкуФИОиСНИЛС(Запрос) Экспорт
	
	СтруктураПараметров = Новый Структура("Основание", Запрос);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СверкаФИОиСНИЛС", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСправкуОСостоянииРасчетовИОС(Запрос) Экспорт
	
	СтруктураПараметров = Новый Структура("Основание", Запрос);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СправкаОСостоянииРасчетовИОС", СтруктураПараметров);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьОтветыНаЗапросИОС(парамЗапрос, СДанными = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОтветыНаЗапросИОС(парамЗапрос, СДанными);

КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьЗаполнениеЗапросаИОС(ДокЗапрос, Параметры)
	
	СтатусПроверки = Истина;
	
	Если НЕ ЗначениеЗаполнено(ДокЗапрос.Организация) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задана организация!'"));
		СтатусПроверки = Ложь;
	ИначеЕсли НЕ ЗначениеЗаполнено(ДокЗапрос.Получатель) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан получатель!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДокЗапрос.Организация) И НЕ ЗначениеЗаполнено(Параметры.РегНомПФР) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан регистрационный номер организации в справочнике!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	Если ЗначениеЗаполнено(ДокЗапрос.Организация) И НЕ ЗначениеЗаполнено(Параметры.КодОрганаПФР) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не задан код территориального органа ПФР в справочнике!'"));
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если НЕ СтатусПроверки Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем на заполненность вид услуги
	ВидУслуги = ДокЗапрос.ВидУслуги;
	Если НЕ ЗначениеЗаполнено(ВидУслуги) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не выбран вид услуги!'"));
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	ИначеЕсли ВидУслуги = Перечисления.ВидыУслугПриИОС.СверкаФИОиСНИЛС Тогда
		
		Если ДокЗапрос.ЗастрахованныеЛица.Количество() = 0 Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не заданы застрахованные лица, по которым производится запрос на сверку!'"));
			СтатусПроверки = Ложь;
			Возврат СтатусПроверки;
		Иначе
			
			Для Каждого СтрЗастрахованноеЛицо Из ДокЗапрос.ЗастрахованныеЛица Цикл
				Если НЕ ЗначениеЗаполнено(СтрЗастрахованноеЛицо.Фамилия) Тогда
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'В строке %1 не задана фамилия застрахованного лица.'"), СтрЗастрахованноеЛицо.НомерСтроки));
					СтатусПроверки = Ложь;
				КонецЕсли;
				Если НЕ ЗначениеЗаполнено(СтрЗастрахованноеЛицо.Имя) Тогда
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'В строке %1 не задано имя застрахованного лица.'"), СтрЗастрахованноеЛицо.НомерСтроки));
					СтатусПроверки = Ложь;
				КонецЕсли;
			КонецЦикла;
			
			Если НЕ СтатусПроверки Тогда
				Возврат СтатусПроверки;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат СтатусПроверки;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПараметрыОтправкиВПФР(Организация)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПараметрыОтправкиВПФР(Организация);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПодтверждениеПолученияЗапросаПФРДляСообщения(Сообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПодтверждениеПолученияЗапросаПФРДляСообщения(Сообщение);
	
КонецФункции

&НаСервере
Процедура СформироватьТаблицуОтветовНаЗапросИОС(Запрос)
	
	ТаблицаОтветовНаЗапросИОC = ПолучитьОтветыНаЗапросИОС(Запрос, Истина);
	ТаблицаОтветовНаЗапросИОC.Колонки.Добавить("Адрес");
	Для Каждого СтрокаОтвета Из ТаблицаОтветовНаЗапросИОC Цикл
		Если ЗначениеЗаполнено(СтрокаОтвета.Данные) Тогда 
			ГУИД = Новый УникальныйИдентификатор;
			СтрокаОтвета.Адрес = ПоместитьВоВременноеХранилище(СтрокаОтвета.Данные.Получить(), ГУИД);
		КонецЕсли;
	КонецЦикла;
	
	ОтветыНаЗапросыИОС.Загрузить(ТаблицаОтветовНаЗапросИОC);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьФайлЗапросаИОС(Запрос, ДвоичныеДанныеФайлаЗапроса, ИмяФайлаЗапроса, УникальныйИдентификатор = Неопределено)
	
	// собираем параметры письма
	ПараметрыЗапроса = СформироватьПараметрыОтправкиВПФР(Запрос.Организация);
	Если НЕ ПроверитьЗаполнениеЗапросаИОС(Запрос, ПараметрыЗапроса) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось сформировать запрос на информационное обслуживание!'"));
		Возврат Ложь;
	КонецЕсли;
	
	// инициализируем служебные соответствия
	СоответствиеВидаУслугиКоду = Новый Соответствие;
	СоответствиеВидаУслугиКоду.Вставить(Перечисления.ВидыУслугПриИОС.СправкаОСостоянииРасчетов, "1");
	СоответствиеВидаУслугиКоду.Вставить(Перечисления.ВидыУслугПриИОС.СверкаФИОиСНИЛС, "2");
	
	ИмяФайлаБезРасширения = "PFR-REQ-ORG-" + СокрЛП(ПараметрыЗапроса.РегНомПФР) + "-" + СгенерироватьUUID();
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	УзелЗапрос = ДобавитьУзелXML(Дерево, "запрос");
	
	Если Запрос.ВидУслуги = Перечисления.ВидыУслугПриИОС.СправкаОСостоянииРасчетов Тогда
		
		ДобавитьУзелXML(УзелЗапрос, "версияФормата", Истина, XMLСтрока("01"));
		
		ДобавитьУзелXML(УзелЗапрос, "кодУПФР", , СокрЛП(ПараметрыЗапроса.КодОрганаПФР));
		ДобавитьУзелXML(УзелЗапрос, "регистрационныйНомерПлательщика", , СокрЛП(ПараметрыЗапроса.РегНомПФР));
		ДобавитьУзелXML(УзелЗапрос, "кодОтвета", , "1");
		ДобавитьУзелXML(УзелЗапрос, "наДату", , Формат(Запрос.НаДату, "ДФ=yyyy-MM-dd"));
		
	ИначеЕсли Запрос.ВидУслуги = Перечисления.ВидыУслугПриИОС.СверкаФИОиСНИЛС Тогда
		
		ДобавитьУзелXML(УзелЗапрос, "кодУПФР", , СокрЛП(ПараметрыЗапроса.КодОрганаПФР));
		ДобавитьУзелXML(УзелЗапрос, "регистрационныйНомерПлательщика", , СокрЛП(ПараметрыЗапроса.РегНомПФР));
		ДобавитьУзелXML(УзелЗапрос, "кодОтвета", , "2");
		
		УзелСписокЗастрахованныхЛиц = ДобавитьУзелXML(УзелЗапрос, "списокЗастрахованныхЛиц");
		
		Для Каждого Стр Из Запрос.ЗастрахованныеЛица Цикл
			
			УзелЗастрахованноеЛицо = ДобавитьУзелXML(УзелСписокЗастрахованныхЛиц, "застрахованноеЛицо");
			
			УзелФИО = ДобавитьУзелXML(УзелЗастрахованноеЛицо, "ФИО");
			ДобавитьУзелXML(УзелФИО, "Фамилия", , ВРЕГ(СокрЛП(Стр.Фамилия)));
			ДобавитьУзелXML(УзелФИО, "Имя", , ВРЕГ(СокрЛП(Стр.Имя)));
			ДобавитьУзелXML(УзелФИО, "Отчество", , ВРЕГ(СокрЛП(Стр.Отчество)));
			
			ДобавитьУзелXML(УзелЗастрахованноеЛицо, "страховойНомер", , СокрЛП(Стр.СтраховойНомер));
			
		КонецЦикла;
		
	КонецЕсли;
	
	// сохранение дерева XML в файл и двоичные данные
	ТекстВыгрузкиXML = ЗаписатьДеревоВXML(Дерево);
	
	// записываем XML запроса во временный файл
	ВремФайл = ПолучитьИмяВременногоФайла();
	Если НЕ ЗаписатьXMLВФайл(ТекстВыгрузкиXML, ВремФайл) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось записать текст запроса во временный файл!'"));
		Возврат Ложь;
	КонецЕсли;
	
	// считываем двоичные данные файла запроса, после чего удаляем файл
	ДвоичныеДанныеФайлаЗапроса = Новый ДвоичныеДанные(ВремФайл);
	ДвоичныеДанныеФайлаЗапроса = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайлаЗапроса, УникальныйИдентификатор);
	УдалитьФайлы(ВремФайл);
	
	// формируем имя файла запроса
	ИмяФайлаЗапроса = ИмяФайлаБезРасширения + ".xml";
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзПодтвержденияОПолученииПФР(СтрПодтверждение)
	
	Если НЕ ЗначениеЗаполнено(СтрПодтверждение) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТаблицаИменФайлов = Новый ТаблицаЗначений;
	ТаблицаИменФайлов.Колонки.Добавить("ИмяФайла");
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрПодтверждение, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML подтверждения о получении:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("подтверждениеПолучения", "Имя");
	УзелУОД = Неопределено;
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		УзелФайл = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ЭДПФР", "Э");
		Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
			УзелФайл = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ЭДСФР", "Э");
		КонецЕсли;
		Если ЗначениеЗаполнено(УзелФайл) Тогда
			УзелУОД = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "УОД", "Э");
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(УзелУОД) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку("Некорректная структура XML протокола: не обнаружен узел ""УОД"" или ""подтверждениеПолучения"".");
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	ОбщиеСведения = Новый Структура;
	
	Если ЗначениеЗаполнено(УзелУОД) Тогда
		ОбщиеСведения.Вставить("ЭтоУОД", Истина);
		
		УзелОператор = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУОД,,, "Оператор", "Э");
		УзелНаименованиеКраткоеОператора = Неопределено;
		УзелРегНомерОператора = Неопределено;
		Если ЗначениеЗаполнено(УзелОператор) Тогда
			УзелНаименованиеКраткоеОператора = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелОператор,,,
				"НаименованиеКраткое", "Э");
			УзелРегНомерОператора = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелОператор,,, "РегНомер", "Э");
		КонецЕсли;
		
		НаименованиеОператора = ?(ЗначениеЗаполнено(УзелНаименованиеКраткоеОператора),
			СокрЛП(УзелНаименованиеКраткоеОператора.Значение), "");
		ОбщиеСведения.Вставить("НаименованиеОператора", НаименованиеОператора);
		
		РегистрационныйНомерОператора = ?(ЗначениеЗаполнено(УзелРегНомерОператора),
			СокрЛП(УзелРегНомерОператора.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомерОператора", РегистрационныйНомерОператора);
		
		УзелСтрахователь = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУОД,,, "Страхователь", "Э");
		УзелНаименованиеКраткое = Неопределено;
		УзелФамилия = Неопределено;
		УзелИмя = Неопределено;
		УзелОтчество = Неопределено;
		УзелРегНомер = Неопределено;
		УзелИНН = Неопределено;
		УзелКПП = Неопределено;
		Если ЗначениеЗаполнено(УзелСтрахователь) Тогда
			УзелНаименованиеКраткое = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(
				УзелСтрахователь,,, "НаименованиеКраткое", "Э");
			УзелФИО = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "ФИО", "Э");
			Если УзелФИО <> Неопределено Тогда
				УзелФамилия = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФИО,,, "Фамилия", "Э");
				УзелИмя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФИО,,, "Имя", "Э");
				УзелОтчество = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФИО,,, "Отчество", "Э");
			КонецЕсли;
			УзелРегНомер = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "РегНомер", "Э");
			УзелИНН = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "ИНН", "Э");
			УзелКПП = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "КПП", "Э");
		КонецЕсли;
		
		НаименованиеОрганизации = ?(ЗначениеЗаполнено(УзелНаименованиеКраткое),
			СокрЛП(УзелНаименованиеКраткое.Значение), "");
		ОбщиеСведения.Вставить("НаименованиеОрганизации", НаименованиеОрганизации);
		
		Фамилия = ?(ЗначениеЗаполнено(УзелФамилия), СокрЛП(УзелФамилия.Значение), "");
		ОбщиеСведения.Вставить("Фамилия", Фамилия);
		
		Имя = ?(ЗначениеЗаполнено(УзелИмя), СокрЛП(УзелИмя.Значение), "");
		ОбщиеСведения.Вставить("Имя", Имя);
		
		Отчество = ?(ЗначениеЗаполнено(УзелОтчество), СокрЛП(УзелОтчество.Значение), "");
		ОбщиеСведения.Вставить("Отчество", Отчество);
		
		РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегНомер), СокрЛП(УзелРегНомер.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
		
		ИНН = ?(ЗначениеЗаполнено(УзелИНН), СокрЛП(УзелИНН.Значение), "");
		ОбщиеСведения.Вставить("ИНН", ИНН);
		
		КПП = ?(ЗначениеЗаполнено(УзелКПП), СокрЛП(УзелКПП.Значение), "");
		ОбщиеСведения.Вставить("КПП", КПП);
		
		УзелДоставленныйПакет = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУОД,,, "ДоставленныйПакет", "Э");
		УзелИдентификатор = Неопределено;
		УзелДатаВремя = Неопределено;
		УзелКанал = Неопределено;
		Если ЗначениеЗаполнено(УзелДоставленныйПакет) Тогда
			УзелИдентификатор = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДоставленныйПакет,,, "Идентификатор", "Э");
			УзелДатаВремя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДоставленныйПакет,,, "ДатаВремя", "Э");
			УзелКанал = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДоставленныйПакет,,, "Канал", "Э");
		КонецЕсли;
		
		ИдентификаторДоставленногоПакета = ?(ЗначениеЗаполнено(УзелИдентификатор), СокрЛП(УзелИдентификатор.Значение), "");
		ОбщиеСведения.Вставить("ИдентификаторДоставленногоПакета", ИдентификаторДоставленногоПакета);
		
		ДатаВремяДоставкиПакета = ?(ЗначениеЗаполнено(УзелДатаВремя),
			XMLЗначение(Тип("Дата"), УзелДатаВремя.Значение), Неопределено);
		ОбщиеСведения.Вставить("ДатаВремяДоставкиПакета", ДатаВремяДоставкиПакета);
		ДатаВремяДоставкиПакетаСтрокой = Формат(ДатаВремяДоставкиПакета, "ДЛФ=ДДВ");
		ОбщиеСведения.Вставить("ДатаВремяДоставкиПакетаСтрокой", ДатаВремяДоставкиПакетаСтрокой);
		
		КаналПередачи = ?(ЗначениеЗаполнено(УзелКанал), СокрЛП(УзелКанал.Значение), "");
		ОбщиеСведения.Вставить("КаналПередачи", КаналПередачи);
		КаналПередачиСтрокой = ?(ВРег(КаналПередачи) = "EDOK", НСтр("ru = 'Электронный документооборот'"),
			?(ВРег(КаналПередачи) = "ES.KS", НСтр("ru = 'Кабинет страхователя'"),
			?(ВРег(КаналПередачи) = "POST", НСтр("ru = 'Бумажная почта'"),
			?(ВРег(КаналПередачи) = "PERS", НСтр("ru = 'Лично'"), СокрЛП(КаналПередачи)))));
		ОбщиеСведения.Вставить("КаналПередачиСтрокой", КаналПередачиСтрокой);
		
		УзелСлужебнаяИнформация = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "СлужебнаяИнформация", "Э");
		УзелGUID = Неопределено;
		УзелДатаВремя = Неопределено;
		Если ЗначениеЗаполнено(УзелСлужебнаяИнформация) Тогда
			УзелGUID = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "GUID", "Э");
			УзелДатаВремя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "ДатаВремя", "Э");
		КонецЕсли;
		
		ИдентификаторКвитанции = ?(ЗначениеЗаполнено(УзелGUID), СокрЛП(УзелGUID.Значение), "");
		ОбщиеСведения.Вставить("ИдентификаторКвитанции", ИдентификаторКвитанции);
		
		ДатаОтправки = ?(ЗначениеЗаполнено(УзелДатаВремя), XMLЗначение(Тип("Дата"), УзелДатаВремя.Значение), "");
		ОбщиеСведения.Вставить("ДатаОтправки", ДатаОтправки);
		ДатаОтправкиСтрокой = Формат(ДатаОтправки, "ДЛФ=ДДВ");
		ОбщиеСведения.Вставить("ДатаОтправкиСтрокой", ДатаОтправкиСтрокой);
		
	Иначе
		УзелРегНомер = УзелФайл.Строки.Найти("регистрационныйНомерОрганизации", "Имя");
		РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегНомер), СокрЛП(УзелРегНомер.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
		
		УзелДатаПолучения = УзелФайл.Строки.Найти("датаВремяПолучения", "Имя");
		ДатаПолучения = ?(ЗначениеЗаполнено(УзелДатаПолучения),
			XMLЗначение(Тип("Дата"), УзелДатаПолучения.Значение), Неопределено);
		ДатаПолученияСтрокой = Формат(ДатаПолучения, "ДЛФ=DDT");
		ОбщиеСведения.Вставить("ДатаПолучения", ДатаПолученияСтрокой);
		ОбщиеСведения.Вставить("ДатаВремяДоставкиПакета", ДатаПолучения);
		ДатаВремяДоставкиПакетаСтрокой = Формат(ДатаПолучения, "ДЛФ=ДДВ");
		ОбщиеСведения.Вставить("ДатаВремяДоставкиПакетаСтрокой", ДатаВремяДоставкиПакетаСтрокой);
		
		УзлыОписаниеПачек = УзелФайл.Строки.НайтиСтроки(Новый Структура("Имя", "описаниеПачек"));
		Для Каждого УзелОписаниеПачек Из УзлыОписаниеПачек Цикл
			УзлыИменФайлов = УзелОписаниеПачек.Строки.НайтиСтроки(Новый Структура("Имя", "пачка"));
			Для Каждого УзлыИмен Из УзлыИменФайлов Цикл
				
				УзелИмяФайла = УзлыИмен.Строки.Найти("имяФайла", "Имя");
				ИмяФайла = ?(ЗначениеЗаполнено(УзелИмяФайла), СокрЛП(УзелИмяФайла.Значение), "");
				НовСтр = ТаблицаИменФайлов.Добавить();
				НовСтр.ИмяФайла = ИмяФайла;
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Новый Структура("ИмяФайла, ОбщиеСведения", ТаблицаИменФайлов, ОбщиеСведения);
	
КонецФункции

&НаСервере
Функция ПолучитьОсновныеСвойстваИзПротоколаПФР(
		СтрПротокол,
		ЦиклОбмена = Неопределено,
		ТранспортноеСообщение = Неопределено)
	
	Если НЕ ЗначениеЗаполнено(СтрПротокол) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТаблицаИменФайлов = Новый ТаблицаЗначений;
	ТаблицаИменФайлов.Колонки.Добавить("ИмяФайла");
	
	ТаблицаИменВложений = Новый ТаблицаЗначений;
	ТаблицаИменВложений.Колонки.Добавить("ИмяВложения");
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрПротокол, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML подтверждения о получении:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("протокол", "Имя");
	УзелУОРР = Неопределено;
	УзелУОНД = Неопределено;
	УзелАДИ_РЕГ = Неопределено;
	УзелАДИ_8 = Неопределено;
	УзелАВП_ПУ = Неопределено;
	УзелРПОП_ПУ = Неопределено;
	УзелРОПОП_ПУ = Неопределено;
	УзелТУФС_ПУ = Неопределено;
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		УзелФайл = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ЭДПФР", "Э");
		Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
			УзелФайл = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ЭДСФР", "Э");
		КонецЕсли;
		Если ЗначениеЗаполнено(УзелФайл) Тогда
			УзелУППО = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "УПП", "Э"); // УППО, УУОН-ПУ
			Если НЕ ЗначениеЗаполнено(УзелУППО) Тогда
				УзелУОРР = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "УОРР", "Э");
				Если НЕ ЗначениеЗаполнено(УзелУОРР) Тогда
					УзелУОНД = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "УОНД", "Э");
					Если НЕ ЗначениеЗаполнено(УзелУОНД) Тогда
						УзелАДИ_РЕГ = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "АДИ-РЕГ", "Э");
						Если НЕ ЗначениеЗаполнено(УзелАДИ_РЕГ) Тогда
							УзелАДИ_8 = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "АДИ-8", "Э");
							Если НЕ ЗначениеЗаполнено(УзелАДИ_8) Тогда
								УзелАВП_ПУ = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "АВП-ПУ", "Э");
								Если НЕ ЗначениеЗаполнено(УзелАВП_ПУ) Тогда
									УзелРПОП_ПУ = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "РПОП-ПУ", "Э");
									Если НЕ ЗначениеЗаполнено(УзелРПОП_ПУ) Тогда
										УзелРОПОП_ПУ = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "РОПОП-ПУ", "Э");
										Если НЕ ЗначениеЗаполнено(УзелРОПОП_ПУ) Тогда
											УзелТУФС_ПУ = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "ТУФС-ПУ", "Э");
										КонецЕсли;
									КонецЕсли;
								КонецЕсли;
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(УзелУППО) И НЕ ЗначениеЗаполнено(УзелУОРР) И НЕ ЗначениеЗаполнено(УзелУОНД)
			И НЕ ЗначениеЗаполнено(УзелАДИ_РЕГ) И НЕ ЗначениеЗаполнено(УзелАДИ_8) И НЕ ЗначениеЗаполнено(УзелАВП_ПУ)
			И НЕ ЗначениеЗаполнено(УзелРПОП_ПУ) И НЕ ЗначениеЗаполнено(УзелРОПОП_ПУ) И НЕ ЗначениеЗаполнено(УзелТУФС_ПУ) Тогда
			
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку("Некорректная структура XML протокола: не обнаружен узел ""УПП"", ""УОРР"", ""УОНД"", ""АДИ-РЕГ"", ""АДИ-8"" или ""протокол"".");
			Возврат Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	ОбщиеСведения = Новый Структура;
	
	Если ЗначениеЗаполнено(УзелУППО) ИЛИ ЗначениеЗаполнено(УзелАДИ_РЕГ) ИЛИ ЗначениеЗаполнено(УзелАДИ_8)
		ИЛИ ЗначениеЗаполнено(УзелАВП_ПУ) ИЛИ ЗначениеЗаполнено(УзелРПОП_ПУ) ИЛИ ЗначениеЗаполнено(УзелРОПОП_ПУ)
		ИЛИ ЗначениеЗаполнено(УзелТУФС_ПУ) Тогда
		
		УзелСлужебнаяИнформация = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "СлужебнаяИнформация", "Э");
		УзелДатаВремя = Неопределено;
		Если ЗначениеЗаполнено(УзелСлужебнаяИнформация) Тогда
			УзелДатаВремя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "ДатаВремя", "Э");
		КонецЕсли;
		
		ДатаОтправки = ?(ЗначениеЗаполнено(УзелДатаВремя), XMLЗначение(Тип("Дата"), УзелДатаВремя.Значение), "");
		ОбщиеСведения.Вставить("ДатаОтправки", ДатаОтправки);
		
		ДатаОтправкиСтрокой = Формат(ДатаОтправки, "ДЛФ=ДДВ");
		ОбщиеСведения.Вставить("ДатаОтправкиСтрокой", ДатаОтправкиСтрокой);
		
		УзелКонтрагент = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "Контрагент", "Э");
		УзелРегНомер = Неопределено;
		Если ЗначениеЗаполнено(УзелКонтрагент) Тогда
			УзелРегНомер = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелКонтрагент,,, "РегистрационныйНомер", "Э");
		КонецЕсли;
		
		РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегНомер), СокрЛП(УзелРегНомер.Значение), "");
		Если НЕ ЗначениеЗаполнено(РегистрационныйНомер) И ЗначениеЗаполнено(ЦиклОбмена) Тогда
			ОрганизацияСсылка = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЦиклОбмена, "Организация");
			Если ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
				РегистрационныйНомер = КонтекстЭДОСервер.СформироватьПараметрыОтправкиВПФР(ОрганизацияСсылка).РегНомПФР;
			КонецЕсли;
		КонецЕсли;
		ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
		
		Если ЗначениеЗаполнено(УзелУППО) Тогда
			УзелПроверяемыйДокумент = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУППО,,, "ПроверяемыйДокумент", "Э");
			Если ЗначениеЗаполнено(УзелПроверяемыйДокумент) Тогда
				УзлыФайл = Новый Массив;
				КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелПроверяемыйДокумент,, УзлыФайл, "Файл", "Э");
				
				Для каждого УзелФайл Из УзлыФайл Цикл
					УзлыИмяФайла = УзелФайл.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "ИмяФайла", "А"));
					Если УзлыИмяФайла.Количество() <> 0 Тогда
						НовСтр = ТаблицаИменФайлов.Добавить();
						НовСтр.ИмяФайла = УзлыИмяФайла[0].Значение;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТранспортноеСообщение) Тогда
			Вложения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеПФР);
			
			Для каждого Вложение Из Вложения Цикл
				НовСтр = ТаблицаИменВложений.Добавить();
				НовСтр.ИмяВложения = Вложение.ИмяФайла;
			КонецЦикла;
		КонецЕсли;
		
		ОбщиеСведения.Вставить("ЯвляетсяПоложительным", Истина);
		ОбщиеСведения.Вставить("СданЧастично", Ложь);
		Если ЗначениеЗаполнено(УзелУППО) Тогда
			УзлыУППКодРезультата = Новый Массив;
			КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУППО,, УзлыУППКодРезультата, "КодРезультата", "Э");
			Для каждого УзелУППКодРезультата Из УзлыУППКодРезультата Цикл
				ЗначениеКодаРезультата = СокрЛП(УзелУППКодРезультата.Значение);
				Если ЗначениеКодаРезультата = "30" ИЛИ ЗначениеКодаРезультата = "40" Тогда
					ОбщиеСведения.СданЧастично = Истина;
				ИначеЕсли ЗначениеКодаРезультата = "50" Тогда
					ОбщиеСведения.СданЧастично = Ложь;
					ОбщиеСведения.ЯвляетсяПоложительным = Ложь;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		ИначеЕсли НЕ ЗначениеЗаполнено(УзелАДИ_РЕГ) Тогда
			ОбщиеСведения.ЯвляетсяПоложительным = Ложь;
		КонецЕсли;
		
		ДатаВремяДоставкиПакета = Неопределено;
		ДатаВремяДоставкиПакетаСтрокой = "";
		Если ЗначениеЗаполнено(ЦиклОбмена) Тогда
			УведомленияОДоставке = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,
				Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР);
			Если УведомленияОДоставке.Количество() > 0 Тогда
				ВложенияУведомленияОДоставке = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(УведомленияОДоставке[0],
					Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПодтверждениеПолученияОтчетностиПФР,, Истина);
				Для каждого ВложениеУведомленияОДоставке Из ВложенияУведомленияОДоставке Цикл
					ДвоичныеДанныеВложения = ВложениеУведомленияОДоставке.Данные.Получить();
					Если ДвоичныеДанныеВложения <> Неопределено Тогда
						ТекстXMLВложения = ПрочитатьТекстИзФайла(ВложениеУведомленияОДоставке.Адрес,, Истина);
						СвойстваУведомленияОДоставке = ПолучитьОсновныеСвойстваИзПодтвержденияОПолученииПФР(ТекстXMLВложения);
						Если СвойстваУведомленияОДоставке <> Неопределено И (ДатаВремяДоставкиПакета = Неопределено
							ИЛИ СвойстваУведомленияОДоставке.ОбщиеСведения.ДатаВремяДоставкиПакетаСтрокой < ДатаВремяДоставкиПакета) Тогда
							
							ДатаВремяДоставкиПакета = СвойстваУведомленияОДоставке.ОбщиеСведения.ДатаВремяДоставкиПакетаСтрокой;
							ДатаВремяДоставкиПакетаСтрокой = СвойстваУведомленияОДоставке.ОбщиеСведения.ДатаВремяДоставкиПакетаСтрокой;
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
		ОбщиеСведения.Вставить("ДатаПолучения", ДатаВремяДоставкиПакетаСтрокой);
		
	ИначеЕсли ЗначениеЗаполнено(УзелУОРР) Тогда
		ОбщиеСведения.Вставить("ЭтоУОРР", Истина);
		
		УзелСтрахователь = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУОРР,,, "Страхователь", "Э");
		УзелНаименованиеКраткое = Неопределено;
		УзелРегНомер = Неопределено;
		Если ЗначениеЗаполнено(УзелСтрахователь) Тогда
			УзелНаименованиеКраткое = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,,
				"НаименованиеКраткое", "Э");
			УзелРегНомер = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "РегНомер", "Э");
		КонецЕсли;
		
		НаименованиеОрганизации = ?(ЗначениеЗаполнено(УзелНаименованиеКраткое),
			СокрЛП(УзелНаименованиеКраткое.Значение), "");
		ОбщиеСведения.Вставить("НаименованиеОрганизации", НаименованиеОрганизации);
		
		НачалоНаименования = НаименованиеОрганизации;
		ОкончаниеНаименования = "";
		МаксимальнаяДлинаНачала = 60;
		Если СтрДлина(НаименованиеОрганизации) > МаксимальнаяДлинаНачала Тогда
			Для ИндексСКонца = 0 По МаксимальнаяДлинаНачала - 14 Цикл
				ПозицияСНачала = МаксимальнаяДлинаНачала + 1 - ИндексСКонца;
				Если Сред(НачалоНаименования, ПозицияСНачала, 1) = " " Тогда
					НачалоНаименования = Лев(НаименованиеОрганизации, ПозицияСНачала - 1);
					НачалоНаименования = СокрП(НачалоНаименования);
					ОкончаниеНаименования = Сред(НаименованиеОрганизации, ПозицияСНачала + 1);
					ОкончаниеНаименования = СокрЛ(ОкончаниеНаименования);
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		ОбщиеСведения.Вставить("НачалоНаименования", НачалоНаименования);
		ОбщиеСведения.Вставить("ОкончаниеНаименования", ОкончаниеНаименования);
		
		РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегНомер), СокрЛП(УзелРегНомер.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
		
		УзелДокумент = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУОРР,,, "Документ", "Э");
		УзелНаименованиеДокумента = Неопределено;
		УзелРегистрационныйНомерОбращения = Неопределено;
		УзелДатаРегистрацииОбращения = Неопределено;
		Если ЗначениеЗаполнено(УзелДокумент) Тогда
			УзелНаименованиеДокумента = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,,
				"НаименованиеДокумента", "Э");
			УзелРегистрационныйНомерОбращения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,,
				"РегистрационныйНомерОбращения", "Э");
			УзелДатаРегистрацииОбращения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,,
				"ДатаРегистрацииОбращения", "Э");
		КонецЕсли;
		
		НаименованиеДокумента = ?(ЗначениеЗаполнено(УзелНаименованиеДокумента),
			СокрЛП(УзелНаименованиеДокумента.Значение), "");
		ОбщиеСведения.Вставить("НаименованиеДокумента", НаименованиеДокумента);
		
		РегистрационныйНомерОбращения = ?(ЗначениеЗаполнено(УзелРегистрационныйНомерОбращения),
			СокрЛП(УзелРегистрационныйНомерОбращения.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомерОбращения", РегистрационныйНомерОбращения);
		
		ДатаРегистрацииОбращения = ?(ЗначениеЗаполнено(УзелДатаРегистрацииОбращения),
			XMLЗначение(Тип("Дата"), УзелДатаРегистрацииОбращения.Значение), Неопределено);
		ОбщиеСведения.Вставить("ДатаРегистрацииОбращения", ДатаРегистрацииОбращения);
		ДатаРегистрацииОбращенияСтрокой = Формат(ДатаРегистрацииОбращения, "ДЛФ=ДД");
		ОбщиеСведения.Вставить("ДатаРегистрацииОбращенияСтрокой", ДатаРегистрацииОбращенияСтрокой);
		ДеньРегистрацииОбращенияСтрокой = Формат(ДатаРегистрацииОбращения, "ДФ=д");
		ОбщиеСведения.Вставить("ДеньРегистрацииОбращенияСтрокой", ДеньРегистрацииОбращенияСтрокой);
		МесяцРегистрацииОбращенияСтрокой = МесяцДатыСтрокой(ДатаРегистрацииОбращения);
		ОбщиеСведения.Вставить("МесяцРегистрацииОбращенияСтрокой", МесяцРегистрацииОбращенияСтрокой);
		ГодРегистрацииОбращенияСтрокой = Формат(ДатаРегистрацииОбращения, "ДФ=гггг");
		ОбщиеСведения.Вставить("ГодРегистрацииОбращенияСтрокой", ГодРегистрацииОбращенияСтрокой);
		
		УзелСведенияОРезультатеРассмотрения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелУОРР,,,
			"СведенияОРезультатеРассмотрения", "Э");
		УзелРезультатРассмотрения = Неопределено;
		УзелКодПричиныОтказа = Неопределено;
		УзелПричинаОтказа = Неопределено;
		Если ЗначениеЗаполнено(УзелСведенияОРезультатеРассмотрения) Тогда
			УзелРезультатРассмотрения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСведенияОРезультатеРассмотрения,,,
				"РезультатРассмотрения", "Э");
			УзелКодПричиныОтказа = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСведенияОРезультатеРассмотрения,,,
				"КодПричиныОтказа", "Э");
			УзелПричинаОтказа = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСведенияОРезультатеРассмотрения,,,
				"ПричинаОтказа", "Э");
		КонецЕсли;
		
		РезультатРассмотрения = ?(ЗначениеЗаполнено(УзелРезультатРассмотрения),
			XMLЗначение(Тип("Число"), УзелРезультатРассмотрения.Значение), "");
		ОбщиеСведения.Вставить("РезультатРассмотрения", РезультатРассмотрения);
		
		КодПричиныОтказа = ?(ЗначениеЗаполнено(УзелКодПричиныОтказа), СокрЛП(УзелКодПричиныОтказа.Значение), "");
		ОбщиеСведения.Вставить("КодПричиныОтказа", КодПричиныОтказа);
		
		ПричинаОтказа = ?(ЗначениеЗаполнено(УзелПричинаОтказа), СокрЛП(УзелПричинаОтказа.Значение), "");
		ОбщиеСведения.Вставить("ПричинаОтказа", ПричинаОтказа);
		
		УзелСлужебнаяИнформация = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "СлужебнаяИнформация", "Э");
		УзелGUID = Неопределено;
		УзелДатаВремя = Неопределено;
		Если ЗначениеЗаполнено(УзелСлужебнаяИнформация) Тогда
			УзелGUID = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "GUID", "Э");
			УзелДатаВремя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "ДатаВремя", "Э");
		КонецЕсли;
		
		ИдентификаторКвитанции = ?(ЗначениеЗаполнено(УзелGUID), СокрЛП(УзелGUID.Значение), "");
		ОбщиеСведения.Вставить("ИдентификаторКвитанции", ИдентификаторКвитанции);
		
		ДатаОтправки = ?(ЗначениеЗаполнено(УзелДатаВремя), XMLЗначение(Тип("Дата"), УзелДатаВремя.Значение), "");
		ОбщиеСведения.Вставить("ДатаОтправки", ДатаОтправки);
		
		ДеньОтправкиСтрокой = Формат(ДатаОтправки, "ДФ=д");
		ОбщиеСведения.Вставить("ДеньОтправкиСтрокой", ДеньОтправкиСтрокой);
		МесяцОтправкиСтрокой = МесяцДатыСтрокой(ДатаОтправки);
		ОбщиеСведения.Вставить("МесяцОтправкиСтрокой", МесяцОтправкиСтрокой);
		ГодОтправкиСтрокой = Формат(ДатаОтправки, "ДФ=гггг");
		ОбщиеСведения.Вставить("ГодОтправкиСтрокой", ГодОтправкиСтрокой);
		
	ИначеЕсли ЗначениеЗаполнено(УзелУОНД) Тогда
		ОбщиеСведения.Вставить("ЭтоУОНД", Истина);
		
		УзелСлужебнаяИнформация = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "СлужебнаяИнформация", "Э");
		УзелДатаВремя = Неопределено;
		Если ЗначениеЗаполнено(УзелСлужебнаяИнформация) Тогда
			УзелДатаВремя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСлужебнаяИнформация,,, "ДатаВремя", "Э");
		КонецЕсли;
		
		ДатаОтправки = ?(ЗначениеЗаполнено(УзелДатаВремя), XMLЗначение(Тип("Дата"), УзелДатаВремя.Значение), "");
		ОбщиеСведения.Вставить("ДатаОтправки", ДатаОтправки);
		
		ДатаОтправкиСтрокой = Формат(ДатаОтправки, "ДЛФ=ДДВ");
		ОбщиеСведения.Вставить("ДатаОтправкиСтрокой", ДатаОтправкиСтрокой);
		
		ДеньОтправкиСтрокой = Формат(ДатаОтправки, "ДФ=д");
		ОбщиеСведения.Вставить("ДеньОтправкиСтрокой", ДеньОтправкиСтрокой);
		МесяцОтправкиСтрокой = МесяцДатыСтрокой(ДатаОтправки);
		ОбщиеСведения.Вставить("МесяцОтправкиСтрокой", МесяцОтправкиСтрокой);
		ГодОтправкиСтрокой = Формат(ДатаОтправки, "ДФ=гггг");
		ОбщиеСведения.Вставить("ГодОтправкиСтрокой", ГодОтправкиСтрокой);
		
		УзелСтрахователь = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "Страхователь", "Э");
		УзелНаименованиеКраткое = Неопределено;
		УзелФИО = Неопределено;
		УзелРегНомер = Неопределено;
		Если ЗначениеЗаполнено(УзелСтрахователь) Тогда
			УзелНаименованиеКраткое = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,,
				"НаименованиеКраткое", "Э");
			УзелФИО = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "ФИО", "Э");
			УзелРегНомер = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелСтрахователь,,, "РегНомер", "Э");
		КонецЕсли;
		
		НаименованиеОрганизации = ?(ЗначениеЗаполнено(УзелНаименованиеКраткое),
			СокрЛП(УзелНаименованиеКраткое.Значение), "");
		ОбщиеСведения.Вставить("НаименованиеОрганизации", НаименованиеОрганизации);
		
		РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегНомер), СокрЛП(УзелРегНомер.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
		
		УзелФамилия = Неопределено;
		УзелИмя = Неопределено;
		УзелОтчество = Неопределено;
		Если ЗначениеЗаполнено(УзелФИО) Тогда
			УзелФамилия = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФИО,,, "Фамилия", "Э");
			УзелИмя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФИО,,, "Имя", "Э");
			УзелОтчество = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФИО,,, "Отчество", "Э");
		КонецЕсли;
		
		ФамилияСтрахователя = ?(ЗначениеЗаполнено(УзелФамилия), СокрЛП(УзелФамилия.Значение), "");
		ОбщиеСведения.Вставить("Фамилия", ФамилияСтрахователя);
		ИмяСтрахователя = ?(ЗначениеЗаполнено(УзелИмя), СокрЛП(УзелИмя.Значение), "");
		ОбщиеСведения.Вставить("Имя", ИмяСтрахователя);
		ОтчествоСтрахователя = ?(ЗначениеЗаполнено(УзелОтчество), СокрЛП(УзелОтчество.Значение), "");
		ОбщиеСведения.Вставить("Отчество", ОтчествоСтрахователя);
		
		ФИОСтрахователя = ФамилияСтрахователя + ?(ЗначениеЗаполнено(ФамилияСтрахователя)
			И ЗначениеЗаполнено(ИмяСтрахователя), " ", "") + ИмяСтрахователя + ?((ЗначениеЗаполнено(ФамилияСтрахователя)
			ИЛИ ЗначениеЗаполнено(ИмяСтрахователя)) И ЗначениеЗаполнено(ОтчествоСтрахователя), " ", "") + ОтчествоСтрахователя;
		НаименованиеОрганизацииИлиФИОСтрахователя = ?(ЗначениеЗаполнено(УзелНаименованиеКраткое), НаименованиеОрганизации,
			ФИОСтрахователя);
		
		НачалоНаименования = НаименованиеОрганизацииИлиФИОСтрахователя;
		ОкончаниеНаименования = "";
		МаксимальнаяДлинаНачала = 60;
		Если СтрДлина(НаименованиеОрганизацииИлиФИОСтрахователя) > МаксимальнаяДлинаНачала Тогда
			Для ИндексСКонца = 0 По МаксимальнаяДлинаНачала - 14 Цикл
				ПозицияСНачала = МаксимальнаяДлинаНачала + 1 - ИндексСКонца;
				Если Сред(НачалоНаименования, ПозицияСНачала, 1) = " " Тогда
					НачалоНаименования = Лев(НаименованиеОрганизацииИлиФИОСтрахователя, ПозицияСНачала - 1);
					НачалоНаименования = СокрП(НачалоНаименования);
					ОкончаниеНаименования = Сред(НаименованиеОрганизацииИлиФИОСтрахователя, ПозицияСНачала + 1);
					ОкончаниеНаименования = СокрЛ(ОкончаниеНаименования);
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		ОбщиеСведения.Вставить("НачалоНаименования", НачалоНаименования);
		ОбщиеСведения.Вставить("ОкончаниеНаименования", ОкончаниеНаименования);
		
		УзелДокумент = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "Документ", "Э");
		УзелНаименованиеДокумента = Неопределено;
		УзелКодДокумента = Неопределено;
		УзелРегистрационныйНомерОбращения = Неопределено;
		УзелВОтветНа = Неопределено;
		Если ЗначениеЗаполнено(УзелСтрахователь) Тогда
			УзелНаименованиеДокумента = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,,
				"Наименование", "Э");
			УзелКодДокумента = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,, "Код", "Э");
			УзелРегистрационныйНомерОбращения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,,
				"РегНомерОбращ", "Э");
			УзелВОтветНа = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелДокумент,,, "ВОтветНа", "Э");
		КонецЕсли;
		
		НаименованиеДокумента = ?(ЗначениеЗаполнено(УзелНаименованиеДокумента),
			СокрЛП(УзелНаименованиеДокумента.Значение), "");
		ОбщиеСведения.Вставить("НаименованиеДокумента", НаименованиеДокумента);
		
		КодДокумента = ?(ЗначениеЗаполнено(УзелКодДокумента), СокрЛП(УзелКодДокумента.Значение), "");
		ОбщиеСведения.Вставить("КодДокумента", КодДокумента);
		
		РегистрационныйНомерОбращения = ?(ЗначениеЗаполнено(УзелРегистрационныйНомерОбращения),
			СокрЛП(УзелРегистрационныйНомерОбращения.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомерОбращения", РегистрационныйНомерОбращения);
		
		ВОтветНа = ?(ЗначениеЗаполнено(УзелВОтветНа), СокрЛП(УзелВОтветНа.Значение), "");
		ОбщиеСведения.Вставить("ВОтветНа", ВОтветНа);
		
		УзелПричина = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "Причина", "Э");
		Причина = ?(ЗначениеЗаполнено(УзелПричина), СокрЛП(УзелПричина.Значение), "");
		ОбщиеСведения.Вставить("Причина", Причина);
		
		УзелКомментарий = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(УзелФайл,,, "Комментарий", "Э");
		Комментарий = ?(ЗначениеЗаполнено(УзелКомментарий), СокрЛП(УзелКомментарий.Значение), "");
		ОбщиеСведения.Вставить("Комментарий", Комментарий);
		
	Иначе
		УзелЯвляетсяПоложительным = УзелФайл.Строки.Найти("являетсяПоложительным", "Имя");
		ЯвляетсяПоложительным = ?(ЗначениеЗаполнено(УзелЯвляетсяПоложительным),
			XMLЗначение(Тип("Булево"),УзелЯвляетсяПоложительным.Значение), "");
		ОбщиеСведения.Вставить("ЯвляетсяПоложительным", ЯвляетсяПоложительным);
		
		УзелДатаОтправки = УзелФайл.Строки.Найти("датаВремяОтправки", "Имя");
		ДатаОтправки = ?(ЗначениеЗаполнено(УзелДатаОтправки),
			Формат(XMLЗначение(Тип("Дата"), УзелДатаОтправки.Значение), "ДЛФ=ДДВ"), "");
		ОбщиеСведения.Вставить("ДатаОтправки", ДатаОтправки);
		
		УзелПодтверждение = УзелФайл.Строки.Найти("подтверждениеПолучения", "Имя");
		
		УзелРегНомер = УзелПодтверждение.Строки.Найти("регистрационныйНомерОрганизации", "Имя");
		РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегНомер), СокрЛП(УзелРегНомер.Значение), "");
		ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
		
		УзелДатаПолучения = УзелПодтверждение.Строки.Найти("датаВремяПолучения", "Имя");
		ДатаПолучения = ?(ЗначениеЗаполнено(УзелДатаПолучения),
			Формат(XMLЗначение(Тип("Дата"), УзелДатаПолучения.Значение), "ДЛФ=ДДВ"), "");
		ОбщиеСведения.Вставить("ДатаПолучения", ДатаПолучения);	
		
		УзлыОписаниеПачек = УзелПодтверждение.Строки.НайтиСтроки(Новый Структура("Имя", "описаниеПачек"));
		Для Каждого УзелОписаниеПачек Из УзлыОписаниеПачек Цикл
			УзлыИменФайлов = УзелОписаниеПачек.Строки.НайтиСтроки(Новый Структура("Имя", "пачка"));
			Для Каждого УзлыИмен Из УзлыИменФайлов Цикл
				
				УзелИмяФайла = УзлыИмен.Строки.Найти("имяФайла", "Имя");
				ИмяФайла = ?(ЗначениеЗаполнено(УзелИмяФайла), СокрЛП(УзелИмяФайла.Значение), "");
				НовСтр = ТаблицаИменФайлов.Добавить();
				НовСтр.ИмяФайла = ИмяФайла;
			КонецЦикла;
		КонецЦикла;
		
		УзлыОписаниеПриложений = УзелФайл.Строки.НайтиСтроки(Новый Структура("Имя", "описаниеПриложений"));
		Для Каждого УзелОписаниеПриложений Из УзлыОписаниеПриложений Цикл
			УзлыИменПриложений = УзелОписаниеПриложений.Строки.НайтиСтроки(Новый Структура("Имя", "приложение"));
			Для Каждого УзлыИмен Из УзлыИменПриложений Цикл
				УзелИмяФайла = УзлыИмен.Строки.Найти("имяФайла", "Имя");
				ИмяФайла = ?(ЗначениеЗаполнено(УзелИмяФайла), СокрЛП(УзелИмяФайла.Значение), "");
				НовСтр = ТаблицаИменВложений.Добавить();
				НовСтр.ИмяВложения = ИмяФайла;
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	Возврат Новый Структура("ИмяФайла, ОбщиеСведения, Вложения", ТаблицаИменФайлов, ОбщиеСведения, ТаблицаИменВложений);
	
КонецФункции

&НаСервереБезКонтекста
Функция МесяцДатыСтрокой(МесяцИлиДата)
	
	Если НЕ ЗначениеЗаполнено(МесяцИлиДата) Тогда
		Возврат "";
	КонецЕсли;
	
	МесяцЧислом = ?(ТипЗнч(МесяцИлиДата) = Тип("Дата"), Месяц(МесяцИлиДата), МесяцИлиДата);
	
	Если МесяцЧислом = 1 Тогда
		Возврат НСтр("ru = 'января'");
	ИначеЕсли МесяцЧислом = 2 Тогда
		Возврат НСтр("ru = 'февраля'");
	ИначеЕсли МесяцЧислом = 3 Тогда
		Возврат НСтр("ru = 'марта'");
	ИначеЕсли МесяцЧислом = 4 Тогда
		Возврат НСтр("ru = 'апреля'");
	ИначеЕсли МесяцЧислом = 5 Тогда
		Возврат НСтр("ru = 'мая'");
	ИначеЕсли МесяцЧислом = 6 Тогда
		Возврат НСтр("ru = 'июня'");
	ИначеЕсли МесяцЧислом = 7 Тогда
		Возврат НСтр("ru = 'июля'");
	ИначеЕсли МесяцЧислом = 8 Тогда
		Возврат НСтр("ru = 'августа'");
	ИначеЕсли МесяцЧислом = 9 Тогда
		Возврат НСтр("ru = 'сентября'");
	ИначеЕсли МесяцЧислом = 10 Тогда
		Возврат НСтр("ru = 'октября'");
	ИначеЕсли МесяцЧислом = 11 Тогда
		Возврат НСтр("ru = 'ноября'");
	ИначеЕсли МесяцЧислом = 12 Тогда
		Возврат НСтр("ru = 'декабря'");
	Иначе
		Возврат ""
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваПФРИзЦиклаОбмена(ЦиклОбмена)
	
	ДатыСообщений = Новый Структура;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортныеСообщения.Тип КАК Тип,
						  |	ТранспортныеСообщения.ДатаТранспорта,
						  |	ТранспортныеСообщения.Дата КАК Дата
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
						  |ГДЕ
						  |	ТранспортныеСообщения.ЦиклОбмена = &ЦиклОбмена
						  |");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	ТаблицаСообщений = Запрос.Выполнить().Выгрузить();
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР));
	ДатаОтправленоСтрахователем = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаОтправленоСтрахователем = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=DDT");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаОтправленоСтрахователем", ДатаОтправленоСтрахователем);
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР));
	ДатаПодтверждения = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаПодтверждения = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=DDT");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаПодтверждения", ДатаПодтверждения);
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПротоколПФР));
	ДатаПротокола = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаПротокола = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=DDT");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаПротокола", ДатаПротокола);
	
	возврат ДатыСообщений;
КонецФункции

// - Формирование печатной формы протокола Росстат
&НаСервере
Функция СформироватьТабличныйДокументНаОсновеПротоколаФСГС(Заголовок, ИмяФайла, Тело, ЦиклОбмена)
	
	ТекстПротокола = Тело;
	
	// загружаем XML из строки в дерево
	ДеревоXML = ЗагрузитьСтрокуXMLВДеревоЗначений(Тело);
	
	Если ДеревоXML <> Неопределено Тогда
		
		УзелПротокол = ДеревоXML.Строки.Найти("protocol", "Имя");
		
		Если УзелПротокол <> Неопределено Тогда
			
			УзелСтатусПротокола = УзелПротокол.Строки.Найти("status", "Имя");
			СтатусПротокола = "";
			Если УзелСтатусПротокола <> Неопределено Тогда
				СтатусПротокола = XMLЗначение(Тип("Строка"), УзелСтатусПротокола.Значение);
			КонецЕсли;
			УзелСообщениеПротокола = УзелПротокол.Строки.Найти("msg", "Имя");
			СообщениеПротокола = "";
			Если УзелСообщениеПротокола <> Неопределено Тогда
				СообщениеПротокола = XMLЗначение(Тип("Строка"), УзелСообщениеПротокола.Значение);
			КонецЕсли;
			
			УзелТитульный = УзелПротокол.Строки.Найти("title", "Имя");
			Если УзелТитульный <> Неопределено Тогда
				
				ЗначенияЭлементов = Новый Соответствие;
				УзлыЭлементы = УзелТитульный.Строки.НайтиСтроки(Новый Структура("Имя, Тип", "item", "Э"));
				Для каждого УзелЭлемент Из УзлыЭлементы Цикл
					УзелИмяЭлемента = УзелЭлемент.Строки.Найти("name", "Имя");
					Если УзелИмяЭлемента <> Неопределено И ЗначениеЗаполнено(УзелИмяЭлемента.Значение) Тогда
						УзелЗначениеЭлемента = УзелЭлемент.Строки.Найти("value", "Имя");
						ЗначенияЭлементов.Вставить(УзелИмяЭлемента.Значение, ?(УзелЗначениеЭлемента <> Неопределено, XMLЗначение(Тип("Строка"), УзелЗначениеЭлемента.Значение), Неопределено));
					КонецЕсли;
				КонецЦикла;
				
				ЗначенияУведомлений = Новый Массив;
				УзелГруппа = УзелПротокол.Строки.Найти("group", "Имя");
				ЗаголовокГруппы = "";
				Если УзелГруппа <> Неопределено Тогда
					Для каждого УзелУведомление Из УзелГруппа.Строки Цикл
						СообщениеЭлемента = СокрЛП(XMLЗначение(Тип("Строка"), УзелУведомление.Значение));
						Если УзелУведомление.Тип <> "Э" Тогда
							Если УзелУведомление.Имя = "msg" Тогда
								ЗаголовокГруппы = СообщениеЭлемента;
							КонецЕсли;
							Продолжить;
						КонецЕсли;
						УзелСообщение = УзелУведомление.Строки.Найти("msg", "Имя");
						Если УзелСообщение <> Неопределено И ЗначениеЗаполнено(УзелСообщение.Значение) Тогда
							СообщениеЭлемента = " - " + СокрЛП(XMLЗначение(Тип("Строка"), УзелСообщение.Значение));
						КонецЕсли;
						ЗначенияУведомлений.Добавить(СообщениеЭлемента);
					КонецЦикла;
				КонецЕсли;
				
				КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
				ОтчетСдан = КонтекстЭДОСервер.ПротоколФСГСОтчетСданПоТексту(СтатусПротокола, СообщениеПротокола);
				
				// формируем текст протокола
				Если ОтчетСдан И НРег(СтатусПротокола) = "ok" Тогда
					ТекстПротокола = "ОТЧЕТ ПРИНЯТ УСПЕШНО." + Символы.ПС + Символы.ПС + "Уведомление о при~ме в обработку." + Символы.ПС + Символы.ПС;
				ИначеЕсли ОтчетСдан Тогда
					ТекстПротокола = "ОТЧЕТ ПРИНЯТ УСПЕШНО." + Символы.ПС + Символы.ПС + СообщениеПротокола + Символы.ПС + Символы.ПС;
				Иначе
					ТекстПротокола = СообщениеПротокола + Символы.ПС + Символы.ПС;
				КонецЕсли;
				
				Если ЗначенияЭлементов["dt_load"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Дата при~ма отч~та: " + СокрЛП(ЗначенияЭлементов["dt_load"]) + Символы.ПС;
				КонецЕсли;
				Если ЗначенияЭлементов["obj"] <> Неопределено Тогда
					Если СокрЛП(ЗначенияЭлементов["ko"]) = СокрЛП(ЗначенияЭлементов["obj"]) Тогда
						ИдентификаторРеспондента = СокрЛП(ЗначенияЭлементов["obj"]);
					Иначе
						ИдентификаторРеспондента = СокрЛП(Строка(ЗначенияЭлементов["ko"]) + " " + ЗначенияЭлементов["obj"]);
					КонецЕсли;
					ТекстПротокола = ТекстПротокола + "Идентификатор респондента: " + ИдентификаторРеспондента + Символы.ПС;
				КонецЕсли;
				Если ЗначенияЭлементов["file"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Имя файла с отч~том: " + СокрЛП(ЗначенияЭлементов["file"]) + Символы.ПС;
				КонецЕсли;
				Если ЗначенияЭлементов["dt_send"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Дата передачи файла: " + СокрЛП(ЗначенияЭлементов["dt_send"]) + Символы.ПС;
				КонецЕсли;
				
				ТекстПротокола = ТекстПротокола + Символы.ПС;
				
				Если ЗначенияЭлементов["form_name"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Форма отч~та: " + СокрЛП(ЗначенияЭлементов["form_name"]) + Символы.ПС;
				КонецЕсли;
				Если ЗначенияЭлементов["form_code"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Код формы отч~та: " + СокрЛП(ЗначенияЭлементов["form_code"]) + Символы.ПС;
				КонецЕсли;
				Если ЗначенияЭлементов["year"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Год отч~та: " + СокрЛП(ЗначенияЭлементов["year"]) + Символы.ПС;
				КонецЕсли;
				Если ЗначенияЭлементов["period"] <> Неопределено Тогда
					ТекстПротокола = ТекстПротокола + "Код периода отч~та: " + СокрЛП(ЗначенияЭлементов["period"]) + Символы.ПС;
				КонецЕсли;
				
				// выводим уведомления, для разъяснения причины отказа
				Если ЗначенияУведомлений.Количество() > 0 Тогда
					ТекстПротокола = ТекстПротокола + Символы.ПС + ЗаголовокГруппы + ":" + Символы.ПС;
					Для каждого Уведомление Из ЗначенияУведомлений Цикл
						ТекстПротокола = ТекстПротокола + Уведомление + Символы.ПС;
					КонецЦикла;
				КонецЕсли;
				
				ТекстПротокола = СтрЗаменить(СтрЗаменить(ТекстПротокола, "~", Символ(1105)), "&quot;", """");
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Макет = ПолучитьМакетОбработки("ФСГСПротокол");
	
	ОбластьМакета = Макет.ПолучитьОбласть("ПротоколШапка");
	ТабДокумент.Вывести(ОбластьМакета); // ПротоколШапка
	
	Для Инд = 1 По СтрЧислоСтрок(ТекстПротокола) Цикл
		ОбластьМакета = Макет.ПолучитьОбласть("ТекстПротокола");
		ОбластьМакета.Параметры.ТекстПротокола = СтрПолучитьСтроку(ТекстПротокола, Инд);
		ТабДокумент.Вывести(ОбластьМакета);
	КонецЦикла;
	
	СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
	
	Возврат ТабДокумент;
	
КонецФункции

// - Формирование печатной формы ПФР подтверждения о получении
&НаСервере
Функция СформироватьТабличныйДокументПФРНаОсновеПодтвержденияОПолучении(Заголовок, ИмяФайла, Тело, ЦиклОбмена)
	
	СвойстваИзПодтверждения = ПолучитьОсновныеСвойстваИзПодтвержденияОПолученииПФР(Тело);
	ДатыСообщений = ПолучитьОсновныеСвойстваПФРИзЦиклаОбмена(ЦиклОбмена);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПодтверждения) Тогда
		Если СвойстваИзПодтверждения.ОбщиеСведения.Свойство("ЭтоУОД") И СвойстваИзПодтверждения.ОбщиеСведения.ЭтоУОД Тогда
			Макет = ПолучитьМакетОбработки("ПФРУведомлениеОДоставке");
			
			ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОДоставке");
			ОбластьМакета.Параметры.РегистрационныйНомерОператора =
				СвойстваИзПодтверждения.ОбщиеСведения.РегистрационныйНомерОператора;
			ОбластьМакета.Параметры.НаименованиеОператора = СвойстваИзПодтверждения.ОбщиеСведения.НаименованиеОператора;
			ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзПодтверждения.ОбщиеСведения.РегистрационныйНомер;
			ОбластьМакета.Параметры.Наименование =
				?(ЗначениеЗаполнено(СвойстваИзПодтверждения.ОбщиеСведения.НаименованиеОрганизации),
				СвойстваИзПодтверждения.ОбщиеСведения.НаименованиеОрганизации,
				СокрЛП(СвойстваИзПодтверждения.ОбщиеСведения.Фамилия + " " + СвойстваИзПодтверждения.ОбщиеСведения.Имя
				+ " " + СвойстваИзПодтверждения.ОбщиеСведения.Отчество));
			ОбластьМакета.Параметры.ИНН = СвойстваИзПодтверждения.ОбщиеСведения.ИНН;
			ОбластьМакета.Параметры.КПП = СвойстваИзПодтверждения.ОбщиеСведения.КПП;
			ОбластьМакета.Параметры.ИдентификаторДоставленногоПакета =
				СвойстваИзПодтверждения.ОбщиеСведения.ИдентификаторДоставленногоПакета;
			ОбластьМакета.Параметры.ДатаВремяДоставкиПакета =
				СвойстваИзПодтверждения.ОбщиеСведения.ДатаВремяДоставкиПакетаСтрокой;
			ОбластьМакета.Параметры.КаналПередачи = СвойстваИзПодтверждения.ОбщиеСведения.КаналПередачиСтрокой;
			
			ТабДокумент.Вывести(ОбластьМакета); // УведомлениеОДоставке
			
			ТипыСообщений = Новый Массив;
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ПодтверждениеПолученияОтчетностиПФР);
			
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
			КонтекстЭДОСервер.ДобавитьШтампПодписиПодДокументом(
				ЦиклОбмена.Предмет,
				ТипыСообщений,
				ТабДокумент,
				2,
				Ложь);
			
		Иначе
			Макет = ПолучитьМакетОбработки("ПФРКвитанция");
			
			ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеШапка");
			ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзПодтверждения.ОбщиеСведения.РегистрационныйНомер;
			ОбластьМакета.Параметры.Наименование = ЦиклОбмена.Организация;
			ОбластьМакета.Параметры.ДатаОтправки = ДатыСообщений.ДатаОтправленоСтрахователем;
			ОбластьМакета.Параметры.ДатаПолучения = СвойстваИзПодтверждения.ОбщиеСведения.ДатаПолучения;
			ОбластьМакета.Параметры.ДатаПодтверждения = ДатыСообщений.ДатаПодтверждения;
			ОбластьМакета.Параметры.ОрганПФР = ЦиклОбмена.ВнешняяОрганизация;
			
			ТабДокумент.Вывести(ОбластьМакета); // ПодтверждениеШапка
			
			Для Каждого СтрокаИмяФайла Из СвойстваИзПодтверждения.ИмяФайла Цикл
				
				ОбластьМакета = Макет.ПолучитьОбласть("ПодтверждениеИмяФайла");
				ОбластьМакета.Параметры.ИмяФайла = СтрокаИмяФайла.ИмяФайла;
				ТабДокумент.Вывести(ОбластьМакета); //ПодтверждениеИмяФайла  
				
			КонецЦикла;
			
			ОбластьМакета = Макет.ПолучитьОбласть("Подвал");
			ТабДокумент.Вывести(ОбластьМакета); // ПодтверждениеПодвал
			
			СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
			
		КонецЕсли;
		
		ТабДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
		ТабДокумент.АвтоМасштаб = Истина;
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

// - Формирование печатной формы ПФР протокол

&НаСервере
Функция СформироватьТабличныйДокументПФРНаОсновеПротокола(
		Заголовок,
		ИмяФайла,
		Тело,
		ЦиклОбмена,
		ТранспортноеСообщение = Неопределено)
	
	СвойстваИзПротокола = ПолучитьОсновныеСвойстваИзПротоколаПФР(Тело, ЦиклОбмена, ТранспортноеСообщение);
	ДатыСообщений = ПолучитьОсновныеСвойстваПФРИзЦиклаОбмена(ЦиклОбмена);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	Если ЗначениеЗаполнено(СвойстваИзПротокола) Тогда
		Если СвойстваИзПротокола.ОбщиеСведения.Свойство("ЭтоУОРР") И СвойстваИзПротокола.ОбщиеСведения.ЭтоУОРР Тогда
			Макет = ПолучитьМакетОбработки("ПФРУведомлениеОРезультатеРассмотрения");
			
			ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОРезультатеРассмотрения");
			ОбластьМакета.Параметры.Наименование = СвойстваИзПротокола.ОбщиеСведения.НачалоНаименования;
			ОбластьМакета.Параметры.ОкончаниеНаименования = СвойстваИзПротокола.ОбщиеСведения.ОкончаниеНаименования;
			ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзПротокола.ОбщиеСведения.РегистрационныйНомер;
			ОбластьМакета.Параметры.НаименованиеДокумента = СвойстваИзПротокола.ОбщиеСведения.НаименованиеДокумента;
			ОбластьМакета.Параметры.РегистрационныйНомерОбращения =
				СвойстваИзПротокола.ОбщиеСведения.РегистрационныйНомерОбращения;
			ДатаРегистрацииОбращенияБезБуквыГода = Строка(СвойстваИзПротокола.ОбщиеСведения.ДеньРегистрацииОбращенияСтрокой)
				+ " " + Строка(СвойстваИзПротокола.ОбщиеСведения.МесяцРегистрацииОбращенияСтрокой)
				+ " " + Строка(СвойстваИзПротокола.ОбщиеСведения.ГодРегистрацииОбращенияСтрокой);
			ОбластьМакета.Параметры.ДатаРегистрацииОбращения = СокрЛП(ДатаРегистрацииОбращенияБезБуквыГода);
			ОбластьМакета.Параметры.РезультатРассмотренияУдовлетворено =
				?(СвойстваИзПротокола.ОбщиеСведения.РезультатРассмотрения = 1, "V", "");
			ОбластьМакета.Параметры.РезультатРассмотренияОтказВУдовлетворении = 
				?(СвойстваИзПротокола.ОбщиеСведения.РезультатРассмотрения = 2, "V", "");
			ОбластьМакета.Параметры.КодПричиныОтказа = СвойстваИзПротокола.ОбщиеСведения.КодПричиныОтказа;
			ОбластьМакета.Параметры.ПричинаОтказа = СвойстваИзПротокола.ОбщиеСведения.ПричинаОтказа;
			ОбластьМакета.Параметры.ДеньДатыИВремени = СвойстваИзПротокола.ОбщиеСведения.ДеньОтправкиСтрокой;
			ОбластьМакета.Параметры.МесяцДатыИВремени = СвойстваИзПротокола.ОбщиеСведения.МесяцОтправкиСтрокой;
			ОбластьМакета.Параметры.ГодДатыИВремени = СвойстваИзПротокола.ОбщиеСведения.ГодОтправкиСтрокой;
			
			ТабДокумент.Вывести(ОбластьМакета); // УведомлениеОРезультатеРассмотрения
			
			ТипыСообщений = Новый Массив;
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
			
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
			КонтекстЭДОСервер.ДобавитьШтампПодписиПодДокументом(
				ЦиклОбмена.Предмет,
				ТипыСообщений,
				ТабДокумент,
				2,
				Ложь);
			
		ИначеЕсли СвойстваИзПротокола.ОбщиеСведения.Свойство("ЭтоУОНД") И СвойстваИзПротокола.ОбщиеСведения.ЭтоУОНД Тогда
			Макет = ПолучитьМакетОбработки("ПФРУведомлениеОНевозможностиДоставки");
			
			ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОНевозможностиДоставки");
			ОбластьМакета.Параметры.Наименование = СвойстваИзПротокола.ОбщиеСведения.НачалоНаименования;
			ОбластьМакета.Параметры.ОкончаниеНаименования = СвойстваИзПротокола.ОбщиеСведения.ОкончаниеНаименования;
			ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзПротокола.ОбщиеСведения.РегистрационныйНомер;
			ОбластьМакета.Параметры.НаименованиеДокумента = СвойстваИзПротокола.ОбщиеСведения.НаименованиеДокумента;
			ОбластьМакета.Параметры.КодДокумента = СвойстваИзПротокола.ОбщиеСведения.КодДокумента;
			ОбластьМакета.Параметры.ВОтветНа = СвойстваИзПротокола.ОбщиеСведения.ВОтветНа;
			ОбластьМакета.Параметры.РегистрационныйНомерОбращения =
				СвойстваИзПротокола.ОбщиеСведения.РегистрационныйНомерОбращения;
			ОбластьМакета.Параметры.Причина = СвойстваИзПротокола.ОбщиеСведения.Причина;
			ОбластьМакета.Параметры.Комментарий = СвойстваИзПротокола.ОбщиеСведения.Комментарий;
			ОбластьМакета.Параметры.ДеньДатыИВремени = СвойстваИзПротокола.ОбщиеСведения.ДеньОтправкиСтрокой;
			ОбластьМакета.Параметры.МесяцДатыИВремени = СвойстваИзПротокола.ОбщиеСведения.МесяцОтправкиСтрокой;
			ОбластьМакета.Параметры.ГодДатыИВремени = СвойстваИзПротокола.ОбщиеСведения.ГодОтправкиСтрокой;
			
			ТабДокумент.Вывести(ОбластьМакета); // УведомлениеОНевозможностиДоставки
			
			ТипыСообщений = Новый Массив;
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
			
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
			КонтекстЭДОСервер.ДобавитьШтампПодписиПодДокументом(
				ЦиклОбмена.Предмет,
				ТипыСообщений,
				ТабДокумент,
				2,
				Ложь);
			
		Иначе
			ЭтоОтправкаВСФР = ДокументооборотСКОКлиентСервер.ЭтоОтправкаВСФР(
				ЦиклОбмена.Предмет,,
				ЦиклОбмена.ДатаСоздания,
				ЦиклОбмена.Вид,
				ЦиклОбмена.Тип);
			
			Если ЭтоОтправкаВСФР Тогда
				Макет = ПолучитьМакетОбработки("СФРПротокол");
			Иначе
				Макет = ПолучитьМакетОбработки("ПФРПротокол");
			КонецЕсли;
			
			ОбластьМакета = Макет.ПолучитьОбласть("ПротоколШапка");
			ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзПротокола.ОбщиеСведения.РегистрационныйНомер;
			ОбластьМакета.Параметры.Наименование = ЦиклОбмена.Организация;
			ОбластьМакета.Параметры.ДатаОтправки = ДатыСообщений.ДатаОтправленоСтрахователем;
			ОбластьМакета.Параметры.ДатаПолучения = СвойстваИзПротокола.ОбщиеСведения.ДатаПолучения;
			ОбластьМакета.Параметры.ДатаПодтверждения = ДатыСообщений.ДатаПодтверждения;
			ОбластьМакета.Параметры.ДатаПротокола = ДатыСообщений.ДатаПротокола;
			
			ОбластьМакета.Параметры.ОрганПФР = ЦиклОбмена.ВнешняяОрганизация;
			
			ТабДокумент.Вывести(ОбластьМакета); // ПротоколШапка
			
			ОбластьМакета = Макет.ПолучитьОбласть("ПротоколПачки");
			ТабДокумент.Вывести(ОбластьМакета); // ПротоколПачки
			
			Для Каждого СтрокаИмяФайла Из СвойстваИзПротокола.ИмяФайла Цикл
				
				ОбластьМакета = Макет.ПолучитьОбласть("ПротоколИмяПачек");
				ОбластьМакета.Параметры.ИмяФайла = СтрокаИмяФайла.ИмяФайла;
				ТабДокумент.Вывести(ОбластьМакета); //ПротоколИмяПачек
				
			КонецЦикла;
			
			ОбластьМакета = Макет.ПолучитьОбласть("ПротоколРезультат");
			Если СвойстваИзПротокола.ОбщиеСведения.ЯвляетсяПоложительным Тогда
				Если СвойстваИзПротокола.ОбщиеСведения.Свойство("СданЧастично") И СвойстваИзПротокола.ОбщиеСведения.СданЧастично Тогда
					РешениеПФР = "ПРИНЯТО К ОБРАБОТКЕ ЧАСТИЧНО";
				Иначе
					РешениеПФР = "ПРИНЯТО К ОБРАБОТКЕ";
				КонецЕсли;
			Иначе 
				РешениеПФР = "НЕ ПРИНЯТО К ОБРАБОТКЕ";
			КонецЕсли;
			ОбластьМакета.Параметры.РешениеПФР = РешениеПФР;
			ТабДокумент.Вывести(ОбластьМакета); // ПротоколРезультат
			
			Для Каждого СтрокаИмяФайла Из СвойстваИзПротокола.Вложения Цикл
				
				ОбластьМакета = Макет.ПолучитьОбласть("ПротоколПриложения");
				ОбластьМакета.Параметры.ИмяПриложения = СтрокаИмяФайла.ИмяВложения;
				ТабДокумент.Вывести(ОбластьМакета); //ПротоколПриложения
				
			КонецЦикла;
			
			ОбластьМакета = Макет.ПолучитьОбласть("Подвал");
			ТабДокумент.Вывести(ОбластьМакета); // ПротоколПодвал
			
			СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
			
			ТипыСообщений = Новый Массив;
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР);
			ТипыСообщений.Добавить(Перечисления.ТипыТранспортныхСообщений.ПротоколПФР);
			
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
			КонтекстЭДОСервер.ДобавитьШтампПодписиПодДокументом(
				ЦиклОбмена.Предмет,
				ТипыСообщений,
				ТабДокумент,
				2,
				Ложь);
		КонецЕсли;
		
		ТабДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
		ТабДокумент.АвтоМасштаб = Истина;
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// ДОКУМЕНТООБОРОТ С ФСС

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправки(Форма, ОтправкаСсылка, ВыполняемоеОповещение, ПараметрыОбновленияОтправки = Неопределено) Экспорт
	
	Если ТипЗнч(ОтправкаСсылка) = Тип("СправочникСсылка.ОтправкиФСС") Тогда
		ОбновитьРезультатКонкретнойОтправкиФСС(ОтправкаСсылка, Истина, ВыполняемоеОповещение);
		
	ИначеЕсли ТипЗнч(ОтправкаСсылка) = Тип("СправочникСсылка.ОтправкиФСРАР") Тогда
		ОбновитьРезультатКонкретнойОтправкиФСРАР(Форма, ОтправкаСсылка, ВыполняемоеОповещение, ПараметрыОбновленияОтправки);
		
	ИначеЕсли ТипЗнч(ОтправкаСсылка) = Тип("СправочникСсылка.ОтправкиРПН") Тогда
		ОбновитьРезультатКонкретнойОтправкиРПН(Форма, ОтправкаСсылка, ВыполняемоеОповещение, ПараметрыОбновленияОтправки);
		
	ИначеЕсли ТипЗнч(ОтправкаСсылка) = Тип("СправочникСсылка.ОтправкиФТС") Тогда
		Если ПараметрыОбновленияОтправки.Свойство("ЭтоДоверенностьФТС")
			И ПараметрыОбновленияОтправки.ЭтоДоверенностьФТС Тогда
			
			ОбновитьМашиночитаемуюДоверенностьФТС(
				ВыполняемоеОповещение,
				ПараметрыОбновленияОтправки.Организация,,,
				ОтправкаСсылка);
			
		Иначе
			ОбновитьРезультатКонкретнойОтправкиФТС(Форма, ОтправкаСсылка, ВыполняемоеОповещение, ПараметрыОбновленияОтправки);
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(ОтправкаСсылка) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиРаспределенныйРеестр") Тогда
		ДокументооборотСКОКлиент.ОбновитьСостояниеМЧДРР(ВыполняемоеОповещение, ОтправкаСсылка);
		
	ИначеЕсли ТипЗнч(ПараметрыОбновленияОтправки) = Тип("Структура") И ПараметрыОбновленияОтправки.Свойство("ЭтоСЭДО")
		И ПараметрыОбновленияОтправки.ЭтоСЭДО Тогда
		
		Организации = ?(ПараметрыОбновленияОтправки.Свойство("Организация")
			И ЗначениеЗаполнено(ПараметрыОбновленияОтправки.Организация),
			ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ПараметрыОбновленияОтправки.Организация),
			Неопределено);
		ТипВзаимодействия = ?(ПараметрыОбновленияОтправки.Свойство("ТипВзаимодействия"),
			ПараметрыОбновленияОтправки.ТипВзаимодействия, Неопределено);
		ПолучитьВходящиеСообщенияСЭДОФСС(
			ВыполняемоеОповещение,
			Организации,,,
			ТипВзаимодействия);
		
	ИначеЕсли ТипЗнч(ПараметрыОбновленияОтправки) = Тип("Структура") И ПараметрыОбновленияОтправки.Свойство("ЭтоЗагрузкаДоверенности")
		И ПараметрыОбновленияОтправки.ЭтоЗагрузкаДоверенности Тогда
		
		ДокументооборотСКОКлиент.ПолучитьДанныеМЧДРРПолучениеПолныхДанных(ПараметрыОбновленияОтправки.Ключ);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСС(
		ОтправкаСсылка,
		ТихийРежим,
		ВыполняемоеОповещение,
		ИспользоватьКеширование = Ложь,
		НомерОбъекта = Неопределено) Экспорт
	
	ИнформацияПоОтправке = ИнформацияПоОтправкеФСС(ОтправкаСсылка);
	
	ВыполнитьОбменСЭДО = ОбменСЭДОФССВремяИстекло(
		ИнформацияПоОтправке.ОрганизацияСсылка,
		ИнформацияПоОтправке.ОтправкаЧерезСЭДО,
		ИспользоватьКеширование,,
		ИнформацияПоОтправке.ТипВзаимодействия);
	
	Если ИнформацияПоОтправке.ОтправкаЧерезСЭДО И ЗначениеЗаполнено(ИнформацияПоОтправке.ОрганизацияСсылка) Тогда
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтправкаСсылка", 			ОтправкаСсылка);
		ДополнительныеПараметры.Вставить("ТихийРежим", 				ТихийРежим);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ИспользоватьКеширование", ИспользоватьКеширование);
		ДополнительныеПараметры.Вставить("ИнформацияПоОтправке", 	ИнформацияПоОтправке);
		
		ДополнитьСтруктуруПараметровПолученияСообщений(
			ДополнительныеПараметры,
			ВыполняемоеОповещение.ДополнительныеПараметры,
			ОтправкаСсылка);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбновитьРезультатКонкретнойОтправкиФССПослеОбменаСЭДО",
			ЭтотОбъект,
			ДополнительныеПараметры);
		Если ВыполнитьОбменСЭДО Тогда
			МассивОрганизаций = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ИнформацияПоОтправке.ОрганизацияСсылка);
			
			ПолучитьВходящиеСообщенияСЭДОФСС(
				ОписаниеОповещения,
				МассивОрганизаций,
				ТихийРежим,
				Ложь,
				ИнформацияПоОтправке.ТипВзаимодействия,
				ДополнительныеПараметры.ТипСообщенияОтвета);
			
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ТихийРежим Тогда
		Если ДокументооборотСКОКлиент.ИспользуетсяРежимТестирования() Тогда
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("ОтправкаСсылка", 			ОтправкаСсылка);
			ДополнительныеПараметры.Вставить("ИнформацияПоОтправке", 	ИнформацияПоОтправке);
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("НомерОбъекта", 			НомерОбъекта);
			ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФССЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПродолжитьОтправкуНаТестовыйСерверФСС(ОписаниеОповещения)
		Иначе
			ОбновитьРезультатКонкретнойОтправкиФССНаКлиенте(
				ОтправкаСсылка,
				ИнформацияПоОтправке,
				ВыполняемоеОповещение,
				НомерОбъекта);
		КонецЕсли;
	Иначе
		ОбновитьРезультатКонкретнойОтправкиФССНаКлиенте(
			ОтправкаСсылка,
			ИнформацияПоОтправке,
			ВыполняемоеОповещение,
			НомерОбъекта);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ОбменСЭДОФССВремяИстекло(
		ОрганизацияСсылка,
		ОтправкаЧерезСЭДО,
		ИспользоватьКеширование = Ложь,
		ПринудительноОбновитьКеш = Ложь,
		ТипВзаимодействия = Неопределено)
	
	Результат = Истина;
	
	Если ТипВзаимодействия = Неопределено Тогда
		ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСтраховательСЭДО();
	КонецЕсли;
	СтрокаТипаВзаимодействия = Формат(ТипВзаимодействия, "ЧДЦ=; ЧН=; ЧГ=");
	
	Если НЕ ИспользоватьКеширование И ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		ДатыПолученияВходящихСообщенийСЭДОФСС = ПараметрыПриложения["БРО.ДатыПолученияВходящихСообщенийСЭДОФСС"
			+ СтрокаТипаВзаимодействия];
		Если ДатыПолученияВходящихСообщенийСЭДОФСС = Неопределено Тогда
			ДатыПолученияВходящихСообщенийСЭДОФСС = Новый Соответствие;
		КонецЕсли;
		Если ЗначениеЗаполнено(ДатыПолученияВходящихСообщенийСЭДОФСС[ОрганизацияСсылка]) Тогда
			ДатыПолученияВходящихСообщенийСЭДОФСС.Вставить(ОрганизацияСсылка, Неопределено);
			ПараметрыПриложения["БРО.ДатыПолученияВходящихСообщенийСЭДОФСС" + СтрокаТипаВзаимодействия] =
				ДатыПолученияВходящихСообщенийСЭДОФСС;
		КонецЕсли;
	КонецЕсли;
	
	Если ОтправкаЧерезСЭДО И ЗначениеЗаполнено(ОрганизацияСсылка) Тогда
		ДатыПолученияВходящихСообщенийСЭДОФСС = ПараметрыПриложения["БРО.ДатыПолученияВходящихСообщенийСЭДОФСС"
			+ СтрокаТипаВзаимодействия];
		Если ДатыПолученияВходящихСообщенийСЭДОФСС = Неопределено Тогда
			ДатыПолученияВходящихСообщенийСЭДОФСС = Новый Соответствие;
		КонецЕсли;
		ДатаПолучения = ДатыПолученияВходящихСообщенийСЭДОФСС[ОрганизацияСсылка];
		ДатаСеанса = ОбщегоНазначенияКлиент.ДатаСеанса();
		
		Если ЗначениеЗаполнено(ДатаПолучения) И ДатаСеанса < ДатаПолучения + 10 * 60 И ДатаСеанса >= ДатаПолучения Тогда
			Результат = Ложь;
		КонецЕсли;
		
		Если Результат ИЛИ ПринудительноОбновитьКеш Тогда
			ДатыПолученияВходящихСообщенийСЭДОФСС.Вставить(ОрганизацияСсылка, ДатаСеанса);
			ПараметрыПриложения["БРО.ДатыПолученияВходящихСообщенийСЭДОФСС" + СтрокаТипаВзаимодействия] =
				ДатыПолученияВходящихСообщенийСЭДОФСС;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССПослеОбменаСЭДО(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат <> Неопределено И Результат.Свойство("ОшибкиПоОрганизациям") Тогда
		Для каждого ОшибкиПоОрганизации Из Результат.ОшибкиПоОрганизациям Цикл
			Если ЗначениеЗаполнено(ОшибкиПоОрганизации.Значение) Тогда
				ТекстОшибки = Строка(ОшибкиПоОрганизации.Ключ) + ": "
					+ СтрСоединить(ОшибкиПоОрганизации.Значение, Символы.ВК + Символы.ПС);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	ОтправкаСсылка 			= ДополнительныеПараметры.ОтправкаСсылка;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ИнформацияПоОтправке 	= ДополнительныеПараметры.ИнформацияПоОтправке;
	
	ПараметрыПротокола = Новый Структура;
	ПараметрыПротокола.Вставить("ТолькоПолучитьОтправкуСЭДО", Истина);
	РезультатСохранения = СохранитьПротоколПриОперацииСОКВЭДИлиПолучитьОтправкуСЭДОНаСервере(
		ОтправкаСсылка,
		Неопределено,
		Неопределено,
		ПараметрыПротокола);
	
	ПринудительноОбновитьКеш = Истина;
	ОбменСЭДОФССВремяИстекло(
		ИнформацияПоОтправке.ОрганизацияСсылка,
		Истина,
		Истина,
		ПринудительноОбновитьКеш,
		ИнформацияПоОтправке.ТипВзаимодействия);
	
	Результат = Неопределено;
	Если РезультатСохранения <> Неопределено И РезультатСохранения.Рассматривается Тогда
		Результат = Новый Структура("Рассматривается", Истина);
	КонецЕсли;
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ОтправкаСсылка 			= ДополнительныеПараметры.ОтправкаСсылка;
	ИнформацияПоОтправке 	= ДополнительныеПараметры.ИнформацияПоОтправке;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	НомерОбъекта 			= ДополнительныеПараметры.НомерОбъекта;
	Если Результат Тогда
		ОбновитьРезультатКонкретнойОтправкиФССНаКлиенте(
			ОтправкаСсылка,
			ИнформацияПоОтправке,
			ВыполняемоеОповещение,
			НомерОбъекта);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССНаКлиенте(
		ОтправкаСсылка,
		ИнформацияПоОтправке,
		ВыполняемоеОповещение,
		НомерОбъекта = Неопределено)
	
	ПолученРезультатОтСервераФСС = Ложь;
	ПовторятьСоединение = Истина;
	
	ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(
		ПовторятьСоединение,
		ПолученРезультатОтСервераФСС,
		ОтправкаСсылка,
		ИнформацияПоОтправке,
		ВыполняемоеОповещение,,
		НомерОбъекта);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ИнформацияПоОтправкеФСС(ОтправкаСсылка, ИспользоватьКеширование = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ИнформацияПоОтправкеФСС(ОтправкаСсылка, ИспользоватьКеширование);
	
КонецФункции

// Процедура с рекурсивным вызовом заменяет цикл (+ завершение из настройки доступа к интернету)
&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(
		ПовторятьСоединение,
		ПолученРезультатОтСервераФСС,
		ОтправкаСсылка,
		ИнформацияПоОтправке,
		ВыполняемоеОповещение,
		НомерПопытки = 1,
		НомерОбъекта = Неопределено)
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ПовторятьСоединение", 			ПовторятьСоединение);
	ДополнительныеПараметры.Вставить("ПолученРезультатОтСервераФСС", 	ПолученРезультатОтСервераФСС);
	ДополнительныеПараметры.Вставить("ОтправкаСсылка", 					ОтправкаСсылка);
	ДополнительныеПараметры.Вставить("ИнформацияПоОтправке", 			ИнформацияПоОтправке);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("НомерПопытки", 					НомерПопытки);
	ДополнительныеПараметры.Вставить("НомерОбъекта", 					НомерОбъекта);
	
	Если НомерОбъекта <> Неопределено И НомерПопытки = 1 И НомерОбъекта % 10 = 0
		ИЛИ НомерПопытки % 10 = 0 Тогда
		
		// для предотвращения переполнения стека
		ДлительнаяОтправкаКлиент.ЗапомнитьКонтекстДлительнойОперации(ДополнительныеПараметры,
			"БРО.ВходящийКонтекстОбновитьРезультатКонкретнойОтправкиФСС");
		ПодключитьОбработчикОжидания("Подключаемый_СбросСтекаОбновитьРезультатКонкретнойОтправкиФССВЦикле", 0.1, Истина);
	Иначе
		СбросСтекаОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СбросСтекаОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(ДополнительныеПараметры)
	
	ПовторятьСоединение 			= ДополнительныеПараметры.ПовторятьСоединение;
	ИнформацияПоОтправке 			= ДополнительныеПараметры.ИнформацияПоОтправке;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦиклеПослеПодписанияИЗашифрования",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ЭтоДанныеЭЛН = (ИнформацияПоОтправке.ТипДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ИнформацияПоОтправке.ТипДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ИнформацияПоОтправке.ВидОтчета =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ИнформацияПоОтправке.ТипДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ИнформацияПоОтправке.ВидОтчета =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	
	Если ЭтоДанныеЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		Возврат;
	КонецЕсли;
	
	Если ПовторятьСоединение И ЭтоПодтверждениеВидаДеятельности Тогда
		// значение загружено только для подтверждения вида деятельности
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ИнформацияПоОтправке);
		
		ИнформацияДляЗапроса = ИнформацияДляЗапросаОперацииСОКВЭДВФСС(ИнформацияПоОтправке.ИдентификаторОтправкиНаСервере);
		ТекстЗапросаРезультатов = СтрШаблон(
			"<getResultByIdRequest xmlns=""http://ws.fss.ru/services/okvedconfirm"""
			+ " xmlns:xs=""http://www.w3.org/2001/XMLSchema"" xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance"">"
				+ "<systemInfo xmlns="""">"
					+ "<specVersion>1.0</specVersion>"
					+ "<software>%1</software>"
					+ "<softwareVersion>%2</softwareVersion>"
				+ "</systemInfo>"
				+ "<requestId xmlns="""">%3</requestId>"
			+ "</getResultByIdRequest>",
			ИнформацияДляЗапроса.НазваниеПрограммыВHTML,
			ИнформацияДляЗапроса.ВерсияПрограммыВHTML,
			ИнформацияДляЗапроса.ИдентификаторОтправкиВHTML);
		
		ВыгрузкаЗапросаОперации = Новый Структура;
		ВыгрузкаЗапросаОперации.Вставить("Организация", 			ИнформацияПоОтправке.ОрганизацияСсылка);
		ВыгрузкаЗапросаОперации.Вставить("РегистрационныйНомерФСС", Неопределено);
		ВыгрузкаЗапросаОперации.Вставить("ОГРН", 					Неопределено);
		ВыгрузкаЗапросаОперации.Вставить("ТекстЗапроса", 			ТекстЗапросаРезультатов);
		
		НастройкиОперации = Новый Структура;
		НастройкиОперации.Вставить("ПроверкиИАвтонастройкаВыполнены", 		Ложь);
		НастройкиОперации.Вставить("СертификатСтрахователя", 				Неопределено);
		НастройкиОперации.Вставить("СертификатФСС", 						Неопределено);
		НастройкиОперации.Вставить("МЧД", 									"");
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НастройкиОперации);
		
		АдресПодписанногоSOAPОперацииСОКВЭДВФСС(
			ОписаниеОповещения,
			ВыгрузкаЗапросаОперации,
			НастройкиОперации);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_СбросСтекаОбновитьРезультатКонкретнойОтправкиФССВЦикле() Экспорт
	
	ВходящийКонтекст = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации(
		"БРО.ВходящийКонтекстОбновитьРезультатКонкретнойОтправкиФСС");
	
	СбросСтекаОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦиклеПослеПодписанияИЗашифрования(
		АдресЗашифрованногоЗапросаSOAP,
		ДополнительныеПараметры) Экспорт
	
	ПовторятьСоединение 			= ДополнительныеПараметры.ПовторятьСоединение;
	ПолученРезультатОтСервераФСС 	= ДополнительныеПараметры.ПолученРезультатОтСервераФСС;
	ОтправкаСсылка 					= ДополнительныеПараметры.ОтправкаСсылка;
	ИнформацияПоОтправке 			= ДополнительныеПараметры.ИнформацияПоОтправке;
	ВыполняемоеОповещение 			= ДополнительныеПараметры.ВыполняемоеОповещение;
	НомерПопытки 					= ДополнительныеПараметры.НомерПопытки;
	
	ЭтоДанныеЭЛН = (ИнформацияПоОтправке.ТипДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ИнформацияПоОтправке.ТипДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ИнформацияПоОтправке.ВидОтчета =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ИнформацияПоОтправке.ТипДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ИнформацияПоОтправке.ВидОтчета =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	
	Если ПовторятьСоединение И ЭтоПодтверждениеВидаДеятельности И АдресЗашифрованногоЗапросаSOAP = Неопределено
		ИЛИ ЭтоДанныеЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		
		Если НЕ ЭтоДанныеЭЛН И НЕ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось зашифровать запрос результатов'"));
		КонецЕсли;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	НастройкиССервера = Неопределено;
	Если ПовторятьСоединение Тогда
		
		//при успешном обращении на сервер ФСС отправка обновится в ИБ
		АдресЗашифрованногоОтветаSOAP = Неопределено;
		ОбновитьРезультатКонкретнойОтправкиФСССНаСервере(
			ОтправкаСсылка,
			ПолученРезультатОтСервераФСС,
			НастройкиССервера,
			АдресЗашифрованногоЗапросаSOAP,
			АдресЗашифрованногоОтветаSOAP);
		
		Если НЕ ПолученРезультатОтСервераФСС И НастройкиССервера <> Неопределено Тогда
			// возможно, проблемы с доступом в интернет
			ДополнительныеПараметры.ПолученРезультатОтСервераФСС = ПолученРезультатОтСервераФСС;
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦиклеЗавершение",
				ЭтотОбъект,
				ДополнительныеПараметры);
			
			Если НастройкиССервера.РазделениеВключено Тогда
				ПовторитьПопытку = (НомерПопытки < 5);
				Если НЕ ПовторитьПопытку Тогда
					ТекстСообщения = СтрШаблон(
						НСтр("ru = 'Не удалось обратиться к серверу ФСС РФ (http://%1). Возможно, сервер перегружен. Попробуйте через некоторое время использовать кнопку ""Обновить"" в отчете %2.'"),
						НастройкиССервера.АдресСервера,
						НастройкиССервера.ПредставлениеОтчетаОшибки);
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				КонецЕсли;
				ВыполнитьОбработкуОповещения(ОписаниеОповещения, ПовторитьПопытку);
				
			Иначе
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Не удалось обратиться к серверу ФСС РФ (http://%1)'"),
					НастройкиССервера.АдресСервера);
					
				ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
					ТекстСообщения,
					ОписаниеОповещения);
			КонецЕсли;
			Возврат;
			
		Иначе
			ПовторятьСоединение = Ложь;
			
			Если ЭтоПодтверждениеВидаДеятельности И АдресЗашифрованногоОтветаSOAP <> Неопределено Тогда
				ДополнительныеПараметры.Вставить("АдресЗашифрованногоОтветаSOAP", АдресЗашифрованногоОтветаSOAP);
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦиклеПослеРасшифрованияИПроверкиПодписи",
					ЭтотОбъект, ДополнительныеПараметры);
				
				ЗаписыватьОшибкиВЖурналРегистрации 	= Истина;
				ПроверитьПодписьSOAPОтветаЭЛНВФСС(
					ОписаниеОповещения,
					ИнформацияПоОтправке.ОрганизацияСсылка,
					АдресЗашифрованногоОтветаSOAP,,
					ЗаписыватьОшибкиВЖурналРегистрации,
					?(ЭтоПодтверждениеВидаДеятельности, "ПодтверждениеВидаДеятельности", ""));
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(
			ПовторятьСоединение,
			ПолученРезультатОтСервераФСС,
			ОтправкаСсылка,
			ИнформацияПоОтправке,
			ВыполняемоеОповещение);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ИнформацияДляЗапросаОперацииСОКВЭДВФСС(ИдентификаторОтправки)
	
	Результат = Новый Структура;
	Результат.Вставить("ВерсияПрограммыВHTML", 			"");
	Результат.Вставить("НазваниеПрограммыВHTML", 		"");
	Результат.Вставить("ИдентификаторОтправкиВHTML", 	"");
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Результат.ВерсияПрограммыВHTML 			= РегламентированнаяОтчетность.ВерсияПрограммы();
	Результат.ВерсияПрограммыВHTML 			= Лев(Результат.ВерсияПрограммыВHTML, 20);
	Результат.ВерсияПрограммыВHTML 			= КонтекстЭДО.ТекстВHTML(Результат.ВерсияПрограммыВHTML);
	Результат.НазваниеПрограммыВHTML 		= РегламентированнаяОтчетность.НазваниеПрограммы();
	Результат.НазваниеПрограммыВHTML 		= Лев(Результат.НазваниеПрограммыВHTML, 50);
	Результат.НазваниеПрограммыВHTML 		= КонтекстЭДО.ТекстВHTML(Результат.НазваниеПрограммыВHTML);
	Результат.ИдентификаторОтправкиВHTML 	= КонтекстЭДО.ТекстВHTML(ИдентификаторОтправки);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦиклеПослеРасшифрованияИПроверкиПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	ОтправкаСсылка 					= ДополнительныеПараметры.ОтправкаСсылка;
	ИнформацияПоОтправке 			= ДополнительныеПараметры.ИнформацияПоОтправке;
	ВыполняемоеОповещение 			= ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресЗашифрованногоОтветаSOAP 	= ДополнительныеПараметры.АдресЗашифрованногоОтветаSOAP;
	
	Если Результат = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при расшифровке протокола ФСС.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		УдалитьИзВременногоХранилища(АдресЗашифрованногоОтветаSOAP);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	Если НЕ Результат.ПодписьВалидна Тогда
		// Записываем сообщение, но продолжаем обмен (данные-то мы получили).
		ТекстОшибки = НСтр("ru = 'Подпись протокола недействительна.'");
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Подтверждение вида деятельности ФСС'"),
			"Ошибка",
			ТекстОшибки,,
			Истина);
	КонецЕсли;
	
	ПараметрыПротокола = Новый Структура;
	ПараметрыПротокола.Вставить("АдресРасшифрованногоОтветаSOAP", 	Результат.АдресРасшифрованногоОтветаSOAP);
	ПараметрыПротокола.Вставить("АдресЗашифрованногоОтветаSOAP", 	АдресЗашифрованногоОтветаSOAP);
	ПараметрыПротокола.Вставить("ИдентификаторОтправкиНаСервере", 	ИнформацияПоОтправке.ИдентификаторОтправкиНаСервере);
	РезультатСохранения = СохранитьПротоколПриОперацииСОКВЭДИлиПолучитьОтправкуСЭДОНаСервере(
		ОтправкаСсылка,
		ИнформацияПоОтправке.ОтчетСсылка,
		ИнформацияПоОтправке.ОрганизацияСсылка,
		ПараметрыПротокола);
	
	Результат = Неопределено;
	Если РезультатСохранения.Рассматривается Тогда
		Результат = Новый Структура("Рассматривается", Истина);
	КонецЕсли;
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦиклеЗавершение(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ПолученРезультатОтСервераФСС 	= ДополнительныеПараметры.ПолученРезультатОтСервераФСС;
	ОтправкаСсылка 					= ДополнительныеПараметры.ОтправкаСсылка;
	ИнформацияПоОтправке 			= ДополнительныеПараметры.ИнформацияПоОтправке;
	ВыполняемоеОповещение 			= ДополнительныеПараметры.ВыполняемоеОповещение;
	НомерПопытки 					= ДополнительныеПараметры.НомерПопытки;
	
	ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	Если ПовторятьСоединение Тогда
		ОбновитьРезультатКонкретнойОтправкиФССНаКлиентеОтправкаВЦикле(
			ПовторятьСоединение,
			ПолученРезультатОтСервераФСС,
			ОтправкаСсылка,
			ИнформацияПоОтправке,
			ВыполняемоеОповещение,
			НомерПопытки + 1);
	Иначе
			
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСС. Нет доступа в Интернет.
		
	КонецЕсли;
			
КонецПроцедуры
// Процедура с рекурсивным вызовом заменяет цикл (+ завершение из настройки доступа к интернету)

&НаКлиенте
Процедура ОткрытьПротоколОтправкиФСС(Форма) Экспорт
	
	ОтчетСсылка = ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	
	Если ОтчетСсылка = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСС(ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСертификатыИзНастроекОрганизацииДляФСС(
		МестоХраненияКлюча,
		ОрганизацияСсылка,
		ВыполняемоеОповещение,
		ПолучатьСертификатФССЭЛН = Ложь)
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура,
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОтпечаткиСертификатов = ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка);
	
	Если ОтпечаткиСертификатов = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		СертификатСтрахователя = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток);
		СертификатФСС = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(?(ПолучатьСертификатФССЭЛН,
			ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток, ОтпечаткиСертификатов.СертификатФССОтпечаток));
		Результат = Новый Структура("СертификатСтрахователя, СертификатФСС, МЧД",
			СертификатСтрахователя, СертификатФСС, ОтпечаткиСертификатов.МЧД);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		СертификатСтрахователя = ПолучитьСвойстваОблачногоСертификата(ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY");
		СертификатФСС = ПолучитьСвойстваОблачногоСертификата(?(ПолучатьСертификатФССЭЛН,
			ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток, ОтпечаткиСертификатов.СертификатФССОтпечаток));
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФСС);
		Результат = Новый Структура("СертификатСтрахователя, СертификатФСС, МЧД",
			СертификатСтрахователя, СертификатФСС, ОтпечаткиСертификатов.МЧД);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	Иначе
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ОтпечаткиСертификатов", 		ОтпечаткиСертификатов);
		ДополнительныеПараметры.Вставить("ПолучатьСертификатФССЭЛН", 	ПолучатьСертификатФССЭЛН);
		Оповещение = Новый ОписаниеОповещения("ПолучитьСертификатыИзНастроекОрганизацииДляФССПослеПоискаСертификатаСтрахователяЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, ОтпечаткиСертификатов.СертификатСтрахователяОтпечаток, "MY", , Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСертификатыИзНастроекОрганизацииДляФССПослеПоискаСертификатаСтрахователяЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	ОтпечаткиСертификатов 		= ДополнительныеПараметры.ОтпечаткиСертификатов;
	ПолучатьСертификатФССЭЛН 	= ДополнительныеПараметры.ПолучатьСертификатФССЭЛН;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, СертификатСтрахователя, МЧД",
		ВыполняемоеОповещение, СертификатСтрахователя, ОтпечаткиСертификатов.МЧД);
	Оповещение = Новый ОписаниеОповещения("ПолучитьСертификатыИзНастроекОрганизацииДляФССПослеПоискаСертификатаФССЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(
		Оповещение,
		?(ПолучатьСертификатФССЭЛН, ОтпечаткиСертификатов.СертификатФССЭЛНОтпечаток, ОтпечаткиСертификатов.СертификатФССОтпечаток),
		"AddressBook",,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьСертификатыИзНастроекОрганизацииДляФССПослеПоискаСертификатаФССЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатСтрахователя = ДополнительныеПараметры.СертификатСтрахователя;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатФСС = ?(Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	Результат = Новый Структура("СертификатСтрахователя, СертификатФСС, МЧД",
		СертификатСтрахователя, СертификатФСС, ДополнительныеПараметры.МЧД);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СертификатыГодятсяДляПодписанияОтчетностиФСС(Сертификаты)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СертификатыГодятсяДляПодписанияОтчетностиФСС(Сертификаты);
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеПолученияСертификатовЗавершение(ПараметрыФормированияПакета, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура,
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета 					= ДополнительныеПараметры.ОрганизацияОтчета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	Идентификатор 						= ДополнительныеПараметры.Идентификатор;
	ТекстФайлаВыгрузки 					= ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки 					= ДополнительныеПараметры.ИмяФайлаВыгрузки;
	РасширениеСТочкой 					= ДополнительныеПараметры.РасширениеСТочкой;
	РегистрационныйНомерФСС 			= ДополнительныеПараметры.РегистрационныйНомерФСС;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	
	Если ПараметрыФормированияПакета = Неопределено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	СертификатСтрахователя 	= ПараметрыФормированияПакета.СертификатСтрахователя;
	СертификатФСС 			= ПараметрыФормированияПакета.СертификатФСС;
	МЧД 					= ПараметрыФормированияПакета.МЧД;
	Если ВыполняемоеОповещение <> Неопределено
		И ТипЗнч(ВыполняемоеОповещение.ДополнительныеПараметры) = Тип("Структура") Тогда
		
		ВыполняемоеОповещение.ДополнительныеПараметры.Вставить("МЧД", МЧД);
	КонецЕсли;
	
	//проверяем сертификаты
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатСтрахователя, "Сертификат страхователя");
	СертификатыОбмена.Добавить(СертификатФСС, "Сертификат ФСС");
	
	Если НЕ СертификатыГодятсяДляПодписанияОтчетностиФСС(СертификатыОбмена) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Сертификаты не подходят для подписания.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если СертификатСтрахователя = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить сертификат.'"));
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСтрахователя);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 					ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 					ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("РасширениеСТочкой", 					РасширениеСТочкой);
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", 			РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 			ТипОтправляемогоДокумента);
	ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 			ВидОтправляемогоДокумента);
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	ДополнительныеПараметры.Вставить("СертификатФСС", 						СертификатФСС);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 					ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("АдресДанныхВыгрузки", 				Неопределено);
	ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	ЭтоПодписьИШифрованиеSOAP = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоПодписьИШифрованиеSOAP Тогда

		ДополнительныеПараметры.Вставить("ВременныйКаталог", "");
		ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
		
		Если НЕ ЭтоПодписьИШифрованиеSOAP Тогда
			Адрес = ЗагрузитьДанныеНаСервер(ТекстФайлаВыгрузки, Форма.УникальныйИдентификатор,, Истина);
			ДополнительныеПараметры.Вставить("АдресДанныхВыгрузки", Адрес);
		КонецЕсли;
		
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеЗаписиФайлаЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено", Истина));
		Возврат;
	КонецЕсли;
	
	//Получим имя временного файла
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВФССПослеПолученияВременногоКаталогаЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ОперацииСФайламиЭДКОКлиент.КаталогВременныхФайловНаКлиенте(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеПолученияВременногоКаталогаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	ТекстФайлаВыгрузки 		= ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки 		= ДополнительныеПараметры.ИмяФайлаВыгрузки;
	РасширениеСТочкой 		= ДополнительныеПараметры.РасширениеСТочкой;
	СертификатСтрахователя 	= ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФСС 			= ДополнительныеПараметры.СертификатФСС;
	ОповещениеОЗавершении 	= ДополнительныеПараметры.ОповещениеОЗавершении;
	
	Если НЕ Результат.Выполнено Тогда
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ВременныйКаталог = Результат.ИмяКаталога;
	
	ПолноеИмяФайлаВыгрузки = ВременныйКаталог + ИмяФайлаВыгрузки;
	
	ДополнительныеПараметры.Вставить("ВременныйКаталог", ВременныйКаталог);
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", ПолноеИмяФайлаВыгрузки);
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеЗаписиФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.ТекстВФайл(Оповещение, ТекстФайлаВыгрузки, ПолноеИмяФайлаВыгрузки);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеЗаписиФайлаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ТекстФайлаВыгрузки 					= ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ВременныйКаталог 					= ДополнительныеПараметры.ВременныйКаталог;
	ПолноеИмяФайлаВыгрузки 				= ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	РасширениеСТочкой 					= ДополнительныеПараметры.РасширениеСТочкой;
	РегистрационныйНомерФСС 			= ДополнительныеПараметры.РегистрационныйНомерФСС;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	СертификатСтрахователя 				= ДополнительныеПараметры.СертификатСтрахователя;
	СертификатФСС 						= ДополнительныеПараметры.СертификатФСС;
	ОрганизацияОтчета 					= ДополнительныеПараметры.ОрганизацияОтчета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	АдресДанныхВыгрузки 				= ДополнительныеПараметры.АдресДанныхВыгрузки;
	МЧД 								= ДополнительныеПараметры.МЧД;
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	
	ПараметрыФорматаЭЛН = ?(ДополнительныеПараметры.Свойство("ПараметрыФорматаЭЛН"),
		ДополнительныеПараметры.ПараметрыФорматаЭЛН, Неопределено);
	
	Если НЕ Результат.Выполнено Тогда
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	
	// подписываем и шифруем
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Подписание и шифрование файла выгрузки ...'")); // ФСС
	КороткоеИмяЗашифрованногоПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
		ПолноеИмяФайлаВыгрузки).ИмяБезРасширения
		+ ?((ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам)
		И нрег(РасширениеСТочкой) = ".xml",
		"_" + Строка(Новый УникальныйИдентификатор), "") + РасширениеСТочкой;
	ЗашифрованныйПакет = ВременныйКаталог + КороткоеИмяЗашифрованногоПакета;
	
	СертификатыЗашифрования = Новый Массив;
	СертификатыЗашифрования.Добавить(СертификатФСС);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 			ТипОтправляемогоДокумента);
	ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 			ВидОтправляемогоДокумента);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("ЗашифрованныйПакет", 					ЗашифрованныйПакет);
	ДополнительныеПараметры.Вставить("СертификатыЗашифрования", 			СертификатыЗашифрования);
	ДополнительныеПараметры.Вставить("ФайлПодписи", 						"");
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", 				ПолноеИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
	ДополнительныеПараметры.Вставить("АдресДанныхВыгрузки", 				АдресДанныхВыгрузки);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка",	 				ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Если ЭтоДанныеЭЛН Тогда
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеПодписанияИЗашифрованияЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
		
		НастройкиОперации = Новый Структура;
		НастройкиОперации.Вставить("ПроверкиИАвтонастройкаВыполнены", 		Истина);
		НастройкиОперации.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
		НастройкиОперации.Вставить("СертификатФСС", 						СертификатФСС);
		НастройкиОперации.Вставить("МЧД", 									ДополнительныеПараметры.МЧД);
		НастройкиОперации.Вставить("ПараметрыФорматаЭЛН", 					ПараметрыФорматаЭЛН);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НастройкиОперации);
		
		АдресПодписанногоSOAPЗагрузкиЭЛНВФСС(
			Оповещение,
			ОрганизацияОтчета,
			РегистрационныйНомерФСС,
			ТекстФайлаВыгрузки,,
			НастройкиОперации);
		
	ИначеЕсли ЭтоПодтверждениеВидаДеятельности ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеПодписанияИЗашифрованияЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ВыгрузкаЗапросаОперации = Новый Структура;
		ВыгрузкаЗапросаОперации.Вставить("Организация", 			ОрганизацияОтчета);
		ВыгрузкаЗапросаОперации.Вставить("РегистрационныйНомерФСС", Неопределено);
		ВыгрузкаЗапросаОперации.Вставить("ОГРН", 					Неопределено);
		ВыгрузкаЗапросаОперации.Вставить("ТекстЗапроса", 			ТекстФайлаВыгрузки);
		
		НастройкиОперации = Новый Структура;
		НастройкиОперации.Вставить("ПрефиксИдентификатораОГРН", 			"");
		НастройкиОперации.Вставить("ПроверкиИАвтонастройкаВыполнены", 		Истина);
		НастройкиОперации.Вставить("СертификатСтрахователя", 				СертификатСтрахователя);
		НастройкиОперации.Вставить("СертификатФСС", 						СертификатФСС);
		НастройкиОперации.Вставить("МЧД", 									ДополнительныеПараметры.МЧД);
		НастройкиОперации.Вставить("СертификатШифрованияОтветаВПодписи", 	Ложь);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НастройкиОперации);
		
		Если ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
			НастройкиОперации.ПрефиксИдентификатораОГРН 			= "http://eln.fss.ru/actor/recipient/";
			НастройкиОперации.СертификатШифрованияОтветаВПодписи 	= Истина;
		КонецЕсли;
		
		АдресПодписанногоSOAPОперацииСОКВЭДВФСС(
			Оповещение,
			ВыгрузкаЗапросаОперации,
			НастройкиОперации);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеПолученияИмениФайлаПакета",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Неопределено)

	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеПолученияИмениФайлаПакета(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ТипОтправляемогоДокумента = ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ПолноеИмяФайлаВыгрузки = ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	ОрганизацияСсылка = ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ПакетДляОтправкиВФСС", ОрганизацияСсылка); 
		ПараметрыОперации = ПодготовитьОблачнуюПодпись(ПолноеИмяФайлаВыгрузки, "xml", ТребуетПодтверждениеОперации);
	Иначе
		ПараметрыОперации = "*";
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеПодписанияЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ПодписатьPKCS7(
		Оповещение,
		ДополнительныеПараметры.СертификатСтрахователя,
		?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча), ДополнительныеПараметры.АдресДанныхВыгрузки,
			ПолноеИмяФайлаВыгрузки),
		Истина,,
		Ложь,,,
		ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеПодписанияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ЗашифрованныйПакет 					= ДополнительныеПараметры.ЗашифрованныйПакет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыЗашифрования 			= ДополнительныеПараметры.СертификатыЗашифрования;
	
	Если НЕ Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеВопросаПриОшибкеПодписи",
			ЭтотОбъект, ДополнительныеПараметры);
		
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
			
		Иначе
			ТипКриптопровайдераВладельца = ?(Результат.Свойство("ТипКриптопровайдераВладельца"),
				Результат.ТипКриптопровайдераВладельца, Неопределено);
			ПоказатьВопросПриОшибкеКриптооперации(
				Оповещение,
				"ПодписатьPKCS7",
				Результат.ОписаниеОшибки,
				ТипКриптопровайдераВладельца);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ФайлПодписи = Результат.ФайлПодписи;
	
	ДополнительныеПараметры.Вставить("ФайлПодписи", ФайлПодписи);
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеПодписанияИЗашифрованияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
		Оповещение,
		ФайлПодписи,
		СертификатыЗашифрования,
		?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча), Неопределено, ЗашифрованныйПакет));
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеВопросаПриОшибкеПодписи(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Повторить Тогда
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФССПослеПолученияИмениФайлаПакета",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
		
	Иначе
		Если Результат = КодВозвратаДиалога.ОК Тогда
			// после показа подробной информации об ошибке подписи
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий(); //ФСС.
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось подписать файл выгрузки!'"));
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		КонецЕсли;
		
		Если ДополнительныеПараметры.ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ОповещениеОЗавершении);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФССПослеПодписанияИЗашифрованияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	ЗашифрованныйПакет 					= ДополнительныеПараметры.ЗашифрованныйПакет;
	ФайлПодписи 						= ДополнительныеПараметры.ФайлПодписи;
	
	ПодписанныйSOAP64 = ?(ДополнительныеПараметры.Свойство("ПодписанныйSOAP64"),
		ДополнительныеПараметры.ПодписанныйSOAP64, Неопределено);
	
	Если ЗначениеЗаполнено(ФайлПодписи) Тогда
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			Если ФайлПодписи <> ПодписанныйSOAP64 И (ТипЗнч(Результат) <> Тип("Структура")
				ИЛИ НЕ Результат.Свойство("ЗашифрованныйФайл") ИЛИ ФайлПодписи <> Результат.ЗашифрованныйФайл) Тогда
				УдалитьИзВременногоХранилища(ФайлПодписи);
			КонецЕсли;
		Иначе
			ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ФайлПодписи);
		КонецЕсли;
	КонецЕсли;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	ЭтоПодписьИШифрованиеSOAP = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	ОперацияВыполнена = ?(ЭтоПодписьИШифрованиеSOAP, ЗначениеЗаполнено(Результат), Результат.Выполнено);
	Если НЕ ОперацияВыполнена Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось зашифровать файл выгрузки или не настроена возможность электронного документооборота с ФСС!'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоПодписьИШифрованиеSOAP Тогда

		ИмяФайлаЗашифрованногоПакета = ?(ЭтоПодписьИШифрованиеSOAP, Результат, Результат.ЗашифрованныйФайл);
		
		ИмяФайлаВыгрузки = ЗашифрованныйПакет;
		
		ЗашифрованныйПакет = Новый Структура;
		ЗашифрованныйПакет.Вставить("ИмяФайлаВыгрузки", 			ИмяФайлаВыгрузки);
		ЗашифрованныйПакет.Вставить("ИмяФайлаЗашифрованногоПакета", ИмяФайлаЗашифрованногоПакета);
		ЗашифрованныйПакет.Вставить("ПодписанныйSOAP64", 			ПодписанныйSOAP64);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ЗашифрованныйПакет);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция КодПодчиненности(ХранилищеДанныхОтчета)
	
	Код = "";
	ДанныеОтчета = ХранилищеДанныхОтчета.Получить();
	Если ДанныеОтчета.ПоказателиОтчета.Свойство("ПолеТабличногоДокументаТитульный") Тогда
		ТитульныйЛист = ДанныеОтчета.ПоказателиОтчета.ПолеТабличногоДокументаТитульный;
	Иначе
		ТитульныйЛист = ДанныеОтчета.ПоказателиОтчета.ПолеТабличногоДокументаПриложение1;
	КонецЕсли;
	Если ТитульныйЛист.Свойство("КодПодчиненности") Тогда
		Код = ТитульныйЛист.КодПодчиненности;
	ИначеЕсли ТитульныйЛист.Свойство("КодПодчиненности_1") Тогда
		Код = ТитульныйЛист.КодПодчиненности_1
			+ ТитульныйЛист.КодПодчиненности_2
			+ ТитульныйЛист.КодПодчиненности_3
			+ ТитульныйЛист.КодПодчиненности_4
			+ ТитульныйЛист.КодПодчиненности_5;
	КонецЕсли;
	
	Возврат Код;
	
КонецФункции

&НаСервереБезКонтекста
Функция КодРегионаВФорме(ХранилищеДанныхОтчета)
	
	Код = Неопределено;
	ДанныеОтчета = ХранилищеДанныхОтчета.ДанныеОтчета.Получить();
	ТитульныйЛист = ДанныеОтчета.ПоказателиОтчета.ПолеТабличногоДокументаТитульный;
	Если ТитульныйЛист.Свойство("КодРегионаВФорме") Тогда
		Код = ТитульныйЛист.КодРегионаВФорме;
	КонецЕсли;
	
	Возврат Код;
	
КонецФункции

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт

	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС(
		ВходящийКонтекст.Форма,
		ВходящийКонтекст.ЭтоОтправкаИзФормыОтчетность,
		ВходящийКонтекст.ОтчетСсылка,
		ВходящийКонтекст.ОрганизацияОтчета,
		ВходящийКонтекст.РезультатВыгрузки,
		ВходящийКонтекст.ОповещениеОЗавершении);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС(
		Форма = Неопределено,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь,
		Знач ОтчетСсылка = Неопределено,
		Знач ОрганизацияОтчета = Неопределено,
		Знач РезультатВыгрузки = Неопределено,
		Знач ОповещениеОЗавершении = Неопределено) Экспорт
		
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияОтчета 	= ДокументооборотСФССКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
		ОтчетСсылка 		= ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ИнформацияДляОтправки = ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПодготовкаНаСервере(ОрганизацияОтчета,
		ОтчетСсылка);
	ОбъектПоУмолчаниюРеестрЭЛНСуществует = ИнформацияДляОтправки.ОбъектПоУмолчаниюРеестрЭЛНСуществует;
	ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует =
		ИнформацияДляОтправки.ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует;
	
	МЧДОтпечатокРуководителя = ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДОтпечатокРуководителя");
	Если ЗначениеЗаполнено(МЧДОтпечатокРуководителя) Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Ложь);
	Иначе
		РеквизитыУчетнойЗаписи = ИнформацияДляОтправки.РеквизитыУчетнойЗаписи;
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	КонецЕсли;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	ЭтоРегламентированныйОтчет 	= (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") 
		ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	ЭтоРеестрСведений 			= ДокументооборотСФССКлиентСервер.ЭтоРеестрСведенийНаВыплатуПособийФСС(ОтчетСсылка);
	ЭтоРеестрЭЛН 				= ДокументооборотСФССКлиентСервер.ЭтоРеестрДанныхЭЛНЗаполняемыхРаботодателем(ОтчетСсылка,
		ОбъектПоУмолчаниюРеестрЭЛНСуществует);
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам =
		ДокументооборотСФССКлиентСервер.ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(ОтчетСсылка,
		ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует);
	ЭтоЭлектронноеПредставление = (ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов"));
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Форма", 								Форма);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 		ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 					ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("РезультатВыгрузки", 					РезультатВыгрузки);
	ДополнительныеПараметры.Вставить("ЭтоДанныеЭЛН", 						ЭтоРеестрЭЛН);
	ДополнительныеПараметры.Вставить("ЭтоПодтверждениеВидаДеятельности", 	Ложь);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Если ЭтоРегламентированныйОтчет Тогда
		
		// отправляем только из записанной формы
		Если НЕ ЭтоОтправкаИзФормыОтчетность И Форма.Модифицированность Тогда
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеСохраненияРегОтчета",
				ЭтотОбъект,
				ДополнительныеПараметры);
				
			Форма.СохранитьНаКлиенте(, ОписаниеОповещения);

		Иначе

			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПолучениеСвойствРегОтчета(ДополнительныеПараметры);
			
		КонецЕсли;
		
	ИначеЕсли ЭтоРеестрСведений ИЛИ ЭтоРеестрЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		
		Если НЕ ЗначениеЗаполнено(ОтчетСсылка) Тогда
			ПоказатьПредупреждение(, "Перед отправкой реестр сведений требуется записать.");
			Если ОповещениеОЗавершении <> Неопределено Тогда
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
		КодОрганаФСС = Лев(ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьКодПодчиненностиРеестраСведенийНаВыплатуПособийФСС(ОтчетСсылка), 4);
		ДатыПериода = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДатыПериодаДокумента(ОтчетСсылка);
		
		ПоляДляЗаписиОтправки = Новый Структура();
		ПоляДляЗаписиОтправки.Вставить("КодОрганаФСС",         КодОрганаФСС);
		ПоляДляЗаписиОтправки.Вставить("ДатаНачалаПериода",    ДатыПериода.ДатаНачала);
		ПоляДляЗаписиОтправки.Вставить("ДатаОкончанияПериода", ДатыПериода.ДатаОкончания);
		ПоляДляЗаписиОтправки.Вставить("Версия",               0);
		
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПроверкиПодключения(ДополнительныеПараметры);
		
		
	ИначеЕсли ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.МашиночитаемыеДоверенностиФСС") Тогда
		
		ЗначенияРеквизитов = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначенияРеквизитовОбъекта(ОтчетСсылка,
			"ДоверительФЛ_РегистрационныйНомерФСС, ДатаВыдачи, ДатаОкончания");
		
		КодОрганаФСС = Лев(ЗначенияРеквизитов.ДоверительФЛ_РегистрационныйНомерФСС, 4);
		
		ПоляДляЗаписиОтправки = Новый Структура();
		ПоляДляЗаписиОтправки.Вставить("КодОрганаФСС", 			КодОрганаФСС);
		ПоляДляЗаписиОтправки.Вставить("ДатаНачалаПериода", 	ЗначенияРеквизитов.ДатаВыдачи);
		ПоляДляЗаписиОтправки.Вставить("ДатаОкончанияПериода", 	ЗначенияРеквизитов.ДатаОкончания);
		ПоляДляЗаписиОтправки.Вставить("Версия", 				0);
		
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПроверкиПодключения(ДополнительныеПараметры);
		
	ИначеЕсли ЭтоЭлектронноеПредставление Тогда
		
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			Если Форма.Модифицированность Тогда
				ПоказатьПредупреждение(, НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
				Если ОповещениеОЗавершении <> Неопределено Тогда
					ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
				КонецЕсли;
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено 
			ИЛИ ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			
			Если ОповещениеОЗавершении <> Неопределено Тогда
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
		ПоляДляЗаписиОтправки = ПоляНаСервереДляЗаписиОтправкиВФССНаСервере(ОтчетСсылка);
		
		ДополнительныеПараметры.ЭтоДанныеЭЛН = (ПоляДляЗаписиОтправки.ТипДокумента =
			ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
		ДополнительныеПараметры.ЭтоПодтверждениеВидаДеятельности = ПоляДляЗаписиОтправки.ТипДокумента =
			ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
			И ПоляДляЗаписиОтправки.ВидОтчета =
			ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности");
		ПоляДляЗаписиОтправки.Удалить("ВидОтчета");
		ПоляДляЗаписиОтправки.Удалить("ТипДокумента");
		
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПроверкиПодключения(ДополнительныеПараметры);
		
	Иначе
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить тип документа.'"));
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПодготовкаНаСервере(ОрганизацияОтчета, ОтчетСсылка)
	
	Если НЕ ЗначениеЗаполнено(ОрганизацияОтчета)
		И ТипЗнч(ОтчетСсылка) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиФСС") Тогда
		
		ОрганизацияОтчета = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОтчетСсылка, "Организация");
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияОтчета);
	ОбъектПоУмолчаниюРеестрЭЛНСуществует =
		Метаданные.Документы.Найти("РеестрДанныхЭЛНЗаполняемыхРаботодателем") <> Неопределено;
	ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует =
		Метаданные.Документы.Найти("РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам") <> Неопределено;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Результат = Новый Структура;
	Результат.Вставить("РеквизитыУчетнойЗаписи", РеквизитыУчетнойЗаписи);
	Результат.Вставить("ОбъектПоУмолчаниюРеестрЭЛНСуществует", ОбъектПоУмолчаниюРеестрЭЛНСуществует);
	Результат.Вставить("ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует",
		ОбъектПоУмолчаниюРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникамСуществует);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПроверкиПодключения(ДополнительныеПараметры) Экспорт
	
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета 					= ДополнительныеПараметры.ОрганизацияОтчета;
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	ЭтоДанныеЭЛН 						= ДополнительныеПараметры.ЭтоДанныеЭЛН;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	
	ОрганизацияИспользуетОбменСФСС                    = Ложь;
	ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи = Ложь;
	НастроенОбменВУниверсальномФормате                = Ложь;	
	
	ПараметрыФорматов = ?(ЭтоДанныеЭЛН, Новый Структура("ПараметрыФорматаЭЛН", Неопределено), Неопределено);
	ПолучитьСвойстваОрганизацииДляФССНаСервере(
		ОрганизацияОтчета,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,,
		ПараметрыФорматов);
	ДополнительныеПараметры.Вставить("ПараметрыФорматаЭЛН", ?(ПараметрыФорматов = Неопределено, Неопределено,
		ПараметрыФорматов.ПараметрыФорматаЭЛН));
	
	Если НЕ ОрганизацияИспользуетОбменСФСС Тогда
		Если НастроенОбменВУниверсальномФормате Тогда
			
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка);//+
			Если ОповещениеОЗавершении <> Неопределено Тогда
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			КонецЕсли;
			
		Иначе
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ОтчетСсылка, ОписаниеОповещения);

		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если НастроенОбменВУниверсальномФормате 
		И НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ОтчетСсылка) Тогда
		
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка);//+
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;			
		Возврат;
	КонецЕсли;
	
	Если НастроенОбменВУниверсальномФормате И ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеАвтонастройки", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОтчетСсылка,ОписаниеОповещения);
		
	Иначе
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеАвтонастройки(Истина, ДополнительныеПараметры);
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеАвтонастройки(Результат, ДополнительныеПараметры) Экспорт
	
	ОрганизацияОтчета 	= ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоДанныеЭЛН 		= ДополнительныеПараметры.ЭтоДанныеЭЛН;
	ЭтоПодтверждениеВидаДеятельности 	= ДополнительныеПараметры.ЭтоПодтверждениеВидаДеятельности;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ДополнительныеПараметры.ОтчетСсылка);
	
	ПоказатьПредупреждениеОбОтправкеНаТестовыйСерверФСС =
		ДокументооборотСФССКлиентСервер.ТипВзаимодействияСЭДО(ТипЗнчСсылкаНаОтчет, -1) = -1
		И ПоказатьПредупреждениеОбОтправкеНаТестовыйСерверФССПослеАвтонастройки(
		ОрганизацияОтчета,
		ЭтоДанныеЭЛН,
		ЭтоПодтверждениеВидаДеятельности);
		
	Если ПоказатьПредупреждениеОбОтправкеНаТестовыйСерверФСС Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеВопросаОТестовомРежиме",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ПродолжитьОтправкуНаТестовыйСерверФСС(ОписаниеОповещения, Истина);
		
	Иначе
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеСохраненияИПроверок(ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеВопросаОТестовомРежиме(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат Тогда
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеСохраненияИПроверок(ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеСохраненияРегОтчета(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 		= ДополнительныеПараметры.Форма;
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	
	// если отчет не был записан ранее, то получим ссылку на записанный отчет
	Если ОтчетСсылка = Неопределено Тогда
		
		ОтчетСсылка = ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
		ДополнительныеПараметры.ОтчетСсылка = ОтчетСсылка;
		
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПолучениеСвойствРегОтчета(ДополнительныеПараметры);

КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПолучениеСвойствРегОтчета(ДополнительныеПараметры)
	
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	
	Если ОтчетСсылка = Неопределено Тогда
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если ЭтоОтправкаИзФормыОтчетность Тогда
		ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка,
			"ДатаНачала, ДатаОкончания, Вид, ДанныеОтчета, ИсточникОтчета");
		
		КодПодчиненности = КодПодчиненности(ЗначенияРеквизитов.ДанныеОтчета);
		
		ПоляДляЗаписиОтправки = Новый Структура("КодОрганаФСС, ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
			Лев(КодПодчиненности, 4),
			НачалоДня(ЗначенияРеквизитов.ДатаНачала),
			КонецДня(ЗначенияРеквизитов.ДатаОкончания),
			ЗначенияРеквизитов.Вид);
		
		Если ЗначенияРеквизитов.ИсточникОтчета = "РегламентированныйОтчетПодтверждениеВидаДеятельности" Тогда
			ДополнительныеПараметры.ЭтоПодтверждениеВидаДеятельности = Истина;
		КонецЕсли;
		
	Иначе
		СтруктураРеквизитовФормы = Форма.СтруктураРеквизитовФормы;
		
		Если РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(СтруктураРеквизитовФормы, "мВариант") Тогда
			Версия = СтруктураРеквизитовФормы.мВариант;
		Иначе
			Версия = 0;
		КонецЕсли;
		
		Если РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(Форма, "СтруктураДанныхТитульный") Тогда
			ДанныеТитульный = Форма.СтруктураДанныхТитульный;
		Иначе
			ДанныеТитульный = Форма.СтруктураДанныхПриложение1;
		КонецЕсли;
		
		Если РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(ДанныеТитульный, "КодПодчиненности") Тогда
			КодОрганаФСС = Лев(ДанныеТитульный.КодПодчиненности, 4);
		ИначеЕсли РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(ДанныеТитульный, "КодПодчиненности_1") 
				И РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(ДанныеТитульный, "КодПодчиненности_2") 
				И РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(ДанныеТитульный, "КодПодчиненности_3") 
				И РегламентированнаяОтчетностьКлиентСервер.СвойствоОпределено(ДанныеТитульный, "КодПодчиненности_4") Тогда
			
			КодОрганаФСС = ДанныеТитульный.КодПодчиненности_1  
				+ ДанныеТитульный.КодПодчиненности_2  
				+ ДанныеТитульный.КодПодчиненности_3  
				+ ДанныеТитульный.КодПодчиненности_4; 
		Иначе
			ПоказатьПредупреждение(, "Не удалось определить код органа ФСС при отправке!");
			Если ОповещениеОЗавершении <> Неопределено Тогда
				ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
		ПоляДляЗаписиОтправки = Новый Структура("КодОрганаФСС, ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
			КодОрганаФСС,
			СтруктураРеквизитовФормы.мДатаНачалаПериодаОтчета,
			СтруктураРеквизитовФормы.мДатаКонцаПериодаОтчета,
			Версия);
		
		Если СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетПодтверждениеВидаДеятельности") <> 0 Тогда
			ДополнительныеПараметры.ЭтоПодтверждениеВидаДеятельности = Истина;
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПроверкиПодключения(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеСохраненияИПроверок(ДополнительныеПараметры)
	
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ОтчетСсылка, ОписаниеОповещения);

КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт	
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);

	ПроверитьСертификатИЛицензию(
		ОписаниеОповещения, 
		ВходящийКонтекст.ОтчетСсылка, 
		ВходящийКонтекст.ОрганизацияОтчета, 
		"ФСС");
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Форма 		= ВходящийКонтекст.Форма;
	ОтчетСсылка = ВходящийКонтекст.ОтчетСсылка;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтверждения", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПодтверждениеОтправкиОтчета(
		Форма, 
		ОписаниеОповещения, 
		ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтверждения(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.ОК Тогда
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияЗавершение", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	
	ПодтвердитьПовторнуюОтправку(ОтчетСсылка, "ФСС", ОписаниеОповещения);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОтправкаРегламентированногоОтчетаВФССПослеПодтвержденияЗавершение(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Форма 							= ДополнительныеПараметры.Форма;
	ОтчетСсылка 					= ДополнительныеПараметры.ОтчетСсылка;
	ЭтоОтправкаИзФормыОтчетность 	= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОповещениеОЗавершении 			= ДополнительныеПараметры.ОповещениеОЗавершении;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ИнформацияОбОтправляемомДокументе = ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОтчетСсылка,
		Истина);
	Если ИнформацияОбОтправляемомДокументе = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить вид отправляемого документа.'"));
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	ТипОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента = ИнформацияОбОтправляемомДокументе.ВидОтправляемогоДокумента;
	
	ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 	ТипОтправляемогоДокумента);
	ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 	ВидОтправляемогоДокумента);
	
	ЭтоПодтверждениеВидаДеятельности = ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияПриложений",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если ЭтоПодтверждениеВидаДеятельности
		И ТипЗнч(ОтчетСсылка) <> Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ОписаниеОповещения) Тогда
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
		Иначе
			ТекстВопроса = НСтр("ru = 'Прием приложения 3 в электронном виде не поддерживается ФСС.
									  |Будут отправлены приложения 1 и 2.
									  |
									  |Продолжить?'");
			ПоказатьВопрос(
				ОписаниеОповещения,
				ТекстВопроса,
				РежимДиалогаВопрос.ДаНет,,
				КодВозвратаДиалога.Да);
		КонецЕсли;
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияПриложений(Результат, ДополнительныеПараметры) Экспорт
	
	Форма 								= ДополнительныеПараметры.Форма;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОрганизацияОтчета 					= ДополнительныеПараметры.ОрганизацияОтчета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	РезультатВыгрузки 					= ДополнительныеПараметры.РезультатВыгрузки;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	
	РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВФСС(
		ОтчетСсылка,
		Форма,
		ЭтоОтправкаИзФормыОтчетность,
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		РезультатВыгрузки,
		ОрганизацияОтчета);
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	Если РезультатВыгрузки = Неопределено Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось выгрузить отчет.'"));
		Возврат;
		
	КонецЕсли;
	
	Если РезультатВыгрузки.ОтправкаЧерезСЭДО Тогда
		ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 	РезультатВыгрузки.ИмяФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 	РезультатВыгрузки.ТекстФайлаВыгрузки);
		ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
		ПараметрыСообщения.СодержимоеСообщения = РезультатВыгрузки.ТекстФайлаВыгрузки;
		ПараметрыСообщения.Вставить("ИдентификаторРодителя", РезультатВыгрузки.ИдентификаторРодителя);
		ПараметрыСообщения.Организация = ОрганизацияОтчета;
		
		ПараметрыОтправкиВидаДокумента =
			ДокументооборотСФССКлиентСервер.ПараметрыОтправкиВидаДокументаЧерезСЭДО(ВидОтправляемогоДокумента, Ложь, Ложь);
		ПараметрыСообщения.ТипСообщения = ПараметрыОтправкиВидаДокумента.ТипСообщения;
		ПараметрыСообщения.ОписаниеОшибки = ПараметрыОтправкиВидаДокумента.ОписаниеОшибки;
		Если РезультатВыгрузки.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
			ПараметрыСообщения.Вставить("СодержимоеПодписанногоЗапросаSOAP",
				РезультатВыгрузки.СодержимоеПодписанногоЗапросаSOAP);
		КонецЕсли;
		Если РезультатВыгрузки.Свойство("ОтпечатокСертификатаПодписанта") Тогда
			ПараметрыСообщения.Вставить("ОтпечатокСертификатаПодписанта", РезультатВыгрузки.ОтпечатокСертификатаПодписанта);
		КонецЕсли;
		
		ПараметрыСообщения.ОповещениеОбратногоВызова = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеОтправкиЧерезСЭДО",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		МЧДСкрытьДлительнуюОтправку =
			(ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДСкрытьДлительнуюОтправку") = Истина);
		ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения,,, НЕ МЧДСкрытьДлительнуюОтправку);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 		РезультатВыгрузки.ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("РасширениеСТочкой", 		РезультатВыгрузки.РасширениеСТочкой);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 		РезультатВыгрузки.ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет", 	ТипЗнч(ОтчетСсылка));
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", РезультатВыгрузки.РегистрационныйНомерФСС); // реестры ЭЛН
	
	Если ЗначениеЗаполнено(РезультатВыгрузки.РегистрационныйНомерФСС)
		И НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ПоляДляЗаписиОтправки.КодОрганаФСС) Тогда
		
		ДополнительныеПараметры.ПоляДляЗаписиОтправки.КодОрганаФСС = Лев(РезультатВыгрузки.РегистрационныйНомерФСС, 4);
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеИнициализации",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 	ОписаниеОповещения);
	КонтекстДлительнойОперации.Вставить("ОрганизацияОтчета", 	ОрганизацияОтчета);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, КонтекстДлительнойОперации);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ОтчетСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФСС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияЗавершение",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеОтправкиЧерезСЭДО(Результат, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка 				= ДополнительныеПараметры.ОтчетСсылка;
	ОповещениеОЗавершении 		= ДополнительныеПараметры.ОповещениеОЗавершении;
	ВидОтправляемогоДокумента 	= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	МестоХраненияКлюча 			= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Результат.ОписаниеОшибки);
		
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ПодписанныйПакет = ?(Результат.Свойство("ПодписанноеСообщение64")
		И ЗначениеЗаполнено(Результат.ПодписанноеСообщение64), Результат.ПодписанноеСообщение64, Неопределено);
	Если ЗначениеЗаполнено(ПодписанныйПакет) И
		НЕ КриптографияЭДКОКлиентСервер.ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		ДанныеПодписанногоСообщения = Base64Значение(ПодписанныйПакет);
		ПодписанныйПакет = ПоместитьВоВременноеХранилище(ДанныеПодписанногоСообщения, Новый УникальныйИдентификатор);
	КонецЕсли;
	
	ОбменПоСЭДОНапрямую = СвойстваОбменаПоСЭДОЧерезОператора(
		ДополнительныеПараметры.ОрганизацияОтчета).ОбменПоСЭДОНапрямую;
	
	Если ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДТолькоПодписание") = Истина Тогда
		ПодписанныйЗапросSOAP64 = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Результат, "ПодписанныйЗапросSOAP64");
		ЗаписатьПодписьМЧДФСС(
			ДополнительныеПараметры.ОтчетСсылка,
			ПодписанныйЗапросSOAP64);
		НоваяОтправкаСсылка = Неопределено;
		
	Иначе
		НастройкиОтправки = Новый Структура;
		НастройкиОтправки.Вставить("ОтчетСсылка", 				ДополнительныеПараметры.ОтчетСсылка);
		НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 	ДополнительныеПараметры.ПоляДляЗаписиОтправки);
		НастройкиОтправки.Вставить("ЭтоТекстФайлаПакета", 		Истина);
		НастройкиОтправки.Вставить("ПодписанныйПакет", 			ПодписанныйПакет);
		НастройкиОтправки.Вставить("ОбменЧерезОператора", 		НЕ ОбменПоСЭДОНапрямую);
		НоваяОтправкаСсылка = ДобавитьОтправкуФССНаСервере(
			НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
			ДополнительныеПараметры.ИмяФайлаВыгрузки,
			ДополнительныеПараметры.ТекстФайлаВыгрузки,
			Результат.ИдентификаторЗапроса,
			НастройкиОтправки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПодписанныйПакет) И
		НЕ КриптографияЭДКОКлиентСервер.ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		УдалитьИзВременногоХранилища(ПодписанныйПакет);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(НоваяОтправкаСсылка) Тогда
		ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
		ПараметрыДлительнойОтправки.ОтчетСсылка 				= ОтчетСсылка;
		ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 1;
		ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ДополнительныеПараметры;
		
		Если ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФСС
			ПараметрыАвтозапроса = Новый Структура();
			ПараметрыАвтозапроса.Вставить("Ключ", НоваяОтправкаСсылка);
			
			ЭтоПромежуточныйПротокол = (ВидОтправляемогоДокумента =
				ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", 		ПараметрыАвтозапроса);
			ДополнительныеПараметры.Вставить("ЭтоПромежуточныйПротокол", 	ЭтоПромежуточныйПротокол);
			
			ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСС.
		КонецЕсли;
	КонецЕсли;
	
	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ВыгрузитьОтчетДляОтправкиВФСС(
		ОтчетСсылка,
		Форма,
		ЭтоОтправкаИзФормыОтчетность,
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		РезультатВыгрузки = Неопределено,
		ОрганизацияОтчета = Неопределено)
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура,
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ТекстФайлаВыгрузки		= "";
	ИмяФайлаВыгрузки		= "";
	ИдентификаторРодителя 	= "";
	РегистрационныйНомерФСС = Неопределено;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	ЭтоРеестрСведений = ДокументооборотСФССКлиентСервер.ЭтоРеестрСведенийНаВыплатуПособийФСС(ОтчетСсылка);
	ЭтоЭлектронноеПредставление = (ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов"));
	
	Если ЭтоРегламентированныйОтчет  Тогда
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
		Иначе
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
		КонецЕсли;

		Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(ОтчетСсылка, Ложь, , ТекстФайлаВыгрузки, ИмяФайлаВыгрузки) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				НСтр("ru = 'Не удалось выгрузить отчет.'"));
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		
	ИначеЕсли ЭтоЭлектронноеПредставление Тогда
		
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
			ОтчетСсылка,
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,,
			?(ЭтоПодтверждениеВидаДеятельности ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам ИЛИ ЭтоДанныеЭЛН,
				"utf-8", Неопределено));
		Если ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
			НачалоТекстаВыгрузки = Лев(ТекстФайлаВыгрузки, 100);
			НачалоТекстаВыгрузки = нрег(НачалоТекстаВыгрузки);
			Если СтрНайти(НачалоТекстаВыгрузки, "encoding=""windows-1251""") > 0 Тогда
				ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
					ОтчетСсылка,
					ТекстФайлаВыгрузки,
					ИмяФайлаВыгрузки,,
					"windows-1251");
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ЭтоРеестрСведений ИЛИ ЭтоДанныеЭЛН ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		
		Если РезультатВыгрузки <> Неопределено Тогда
			ТекстФайлаВыгрузки 		= РезультатВыгрузки.ТекстXML;
			ИмяФайлаВыгрузки 		= РезультатВыгрузки.ИмяБезРасширения + ".xml";
			СвойстваФайлаВыгрузки 	= Новый Структура("РегистрационныйНомерФСС", РезультатВыгрузки.РегистрационныйНомерФСС);
			
		Иначе
			СвойстваФайлаВыгрузки = Неопределено;
			ПолучитьСвойстваФайлаВыгрузкиНаСервере(
				ОтчетСсылка,
				ТекстФайлаВыгрузки,
				ИмяФайлаВыгрузки,,
				СвойстваФайлаВыгрузки);
		КонецЕсли;
		
		Если ЭтоДанныеЭЛН И СвойстваФайлаВыгрузки <> Неопределено
			И СвойстваФайлаВыгрузки.Свойство("РегистрационныйНомерФСС") Тогда
			
			РегистрационныйНомерФСС = СвойстваФайлаВыгрузки.РегистрационныйНомерФСС;
		КонецЕсли;
		
	ИначеЕсли ТипЗнчСсылкаНаОтчет = Тип("СправочникСсылка.МашиночитаемыеДоверенностиФСС") Тогда
		ПараметрыВозврата = Новый Структура;
		ПараметрыВозврата.Вставить("ВозвращатьИмяФайлаСРасширением", Истина);
		РезультатВыгрузки = ДокументооборотСКОВызовСервера.ВыгрузитьМЧДФСС(
			ОтчетСсылка,,
			Ложь,
			ПараметрыВозврата);
		ТекстФайлаВыгрузки 		= РезультатВыгрузки.Содержимое;
		ИмяФайлаВыгрузки 		= РезультатВыгрузки.ИмяФайла;
		ИдентификаторРодителя 	= РезультатВыгрузки.ИдентификаторРодителя;
		
	Иначе
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить тип документа.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
		Возврат Неопределено;
	КонецЕсли;
	
	// замена символов, приводящих к ошибки проверки подписи на сервере ФСС
	Если ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам
		ИЛИ ДокументооборотСФССКлиентСервер.ТипВзаимодействияСЭДО(ВидОтправляемогоДокумента, -1) <> -1 Тогда
		
		ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, Символ(61630), Символ(8211)); // замена длинного дефиса
		ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, Символ(8194), " "); // замена короткого пробела
	КонецЕсли;
	
	РезультатИнициализации = ВыгрузитьОтчетДляОтправкиВФССИнициализацияНаСервере(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		ИмяФайлаВыгрузки,
		ТекстФайлаВыгрузки,
		ОрганизацияОтчета);
	
	Если ЗначениеЗаполнено(РезультатИнициализации.ТекстОшибки) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(РезультатИнициализации.ТекстОшибки);
		Возврат Неопределено;
	КонецЕсли;
	
	РасширениеФайла = РезультатИнициализации.РасширениеФайла;
	Если РезультатИнициализации.ТекстФайлаВыгрузки <> Неопределено Тогда
		ТекстФайлаВыгрузки = РезультатИнициализации.ТекстФайлаВыгрузки;
	КонецЕсли;
	
	Если ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам И НЕ РезультатИнициализации.ОтправкаЧерезСЭДО Тогда
		ТекстПрефикса = "<submitCovidWorkerRegistrRequest ";
		ТекстПостфикса = "</submitCovidWorkerRegistrRequest>";
		ТекстПрефикса2023 = "<submit2023WorkerRegistrRequest ";
		ТекстПостфикса2023 = "</submit2023WorkerRegistrRequest>";
		ТекстПрефиксаГосслужащим = "<PaymentsFederalCivilServantsRequest ";
		ТекстПостфиксаГосслужащим = "</PaymentsFederalCivilServantsRequest>";
		
		Для НомерРеестра = 1 По 2 Цикл
			ПозицияПрефикса = СтрНайти(ТекстФайлаВыгрузки, ТекстПрефикса);
			Если ПозицияПрефикса > 0 Тогда
				ТекстФайлаВыгрузки = Сред(ТекстФайлаВыгрузки, ПозицияПрефикса);
				ПозицияПостфикса = СтрНайти(ТекстФайлаВыгрузки, ТекстПостфикса);
				Если ПозицияПостфикса > 0 Тогда
					ДлинаПостфикса = СтрДлина(ТекстПостфикса);
					ТекстФайлаВыгрузки = Лев(ТекстФайлаВыгрузки, ПозицияПостфикса + ДлинаПостфикса - 1);
				КонецЕсли;
				
				Прервать;
				
			ИначеЕсли НомерРеестра = 1 Тогда
				Если СтрНайти(ТекстФайлаВыгрузки, ТекстПрефиксаГосслужащим) = 0 Тогда
					ТекстПрефикса = ТекстПрефикса2023;
					ТекстПостфикса = ТекстПостфикса2023;
				Иначе
					ТекстПрефикса = ТекстПрефиксаГосслужащим;
					ТекстПостфикса = ТекстПостфиксаГосслужащим;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, ТекстПрефикса + " ", ТекстПрефикса);
		
		ПространствоИменОтчета = "xmlns=""http://www.fss.ru/integration/types/pvso/quarantine/covidworkerregistr/v01""";
		Если СтрНайти(ТекстФайлаВыгрузки, ПространствоИменОтчета) = 0 Тогда
			ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, ТекстПрефикса, ТекстПрефикса + ПространствоИменОтчета + " ");
		КонецЕсли;
	КонецЕсли;
	
	РасширениеСТочкой = "";
	Если РасширениеФайла = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить тип документа.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
		Возврат Неопределено;
	Иначе
		РасширениеСТочкой = "." + РасширениеФайла;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось сформировать файл выгрузки.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
		Возврат Неопределено;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не определено имя файла выгрузки.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
		Возврат Неопределено;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 		ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 		ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("РасширениеСТочкой", 		РасширениеСТочкой);
	ДополнительныеПараметры.Вставить("РегистрационныйНомерФСС", РегистрационныйНомерФСС);
	ДополнительныеПараметры.Вставить("ОтправкаЧерезСЭДО",		РезультатИнициализации.ОтправкаЧерезСЭДО);
	ДополнительныеПараметры.Вставить("ИдентификаторРодителя",	ИдентификаторРодителя);
	Если ТипЗнч(РезультатВыгрузки) = Тип("Структура") Тогда
		Если РезультатВыгрузки.Свойство("СодержимоеПодписанногоЗапросаSOAP") Тогда
			ДополнительныеПараметры.Вставить("СодержимоеПодписанногоЗапросаSOAP",
				РезультатВыгрузки.СодержимоеПодписанногоЗапросаSOAP);
		КонецЕсли;
		Если РезультатВыгрузки.Свойство("ОтпечатокСертификатаПодписанта") Тогда
			ДополнительныеПараметры.Вставить("ОтпечатокСертификатаПодписанта",
				РезультатВыгрузки.ОтпечатокСертификатаПодписанта);
		КонецЕсли;
	КонецЕсли;
	Возврат ДополнительныеПараметры;
	
КонецФункции

&НаКлиенте
Процедура ОтозватьМашиночитаемуюДоверенностьФСС(
		ОповещениеОЗавершении = Неопределено,
		ОтчетСсылка = Неопределено,
		Организация = Неопределено,
		НомерДоверенности = "") Экспорт
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 	ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 			ОтчетСсылка);
	ДополнительныеПараметры.Вставить("Организация", 			Организация);
	ДополнительныеПараметры.Вставить("НомерДоверенности", 		НомерДоверенности);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОтозватьМашиночитаемуюДоверенностьФССПослеВвода",
		ЭтотОбъект, ДополнительныеПараметры);
	ОткрытьФорму(
		"Справочник.МашиночитаемыеДоверенностиФСС.Форма.ФормаОтзыва",,
		ЭтаФорма,,,,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтозватьМашиночитаемуюДоверенностьФССПослеВвода(СтруктураДанных, ДополнительныеПараметры) Экспорт
	
	Если СтруктураДанных = Неопределено Тогда
		Если ДополнительныеПараметры.ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ОповещениеОЗавершении, Ложь);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ПараметрыСообщенияОтзыва = ДокументооборотСКОВызовСервера.ПараметрыОтзываМашиночитаемойДоверенностиФСС(
		ДополнительныеПараметры.ОтчетСсылка,
		ДополнительныеПараметры.Организация,
		ДополнительныеПараметры.НомерДоверенности,
		СтруктураДанных.Причина,
		СтруктураДанных.ОтозватьЦепочку);
	
	ДополнительныеПараметры.Вставить("ДатаНачалаОтправки", ПараметрыСообщенияОтзыва.ДатаСеансаНаСервере);
	
	ПараметрыСообщенияОтзыва.Удалить("ДатаСеансаНаСервере");
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ЗаполнитьЗначенияСвойств(ПараметрыСообщения, ПараметрыСообщенияОтзыва);
	ПараметрыСообщения.СодержимоеСообщения = ПараметрыСообщенияОтзыва.СодержимоеИлиАдресСообщения;
	ПараметрыСообщения.ОповещениеОбратногоВызова = Новый ОписаниеОповещения(
		"ОтозватьМашиночитаемуюДоверенностьФССПослеОтправки", ЭтотОбъект, ДополнительныеПараметры);
	
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения, , , Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтозватьМашиночитаемуюДоверенностьФССПослеОтправки(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОЗавершении = ДополнительныеПараметры.ОповещениеОЗавершении;
	
	Если НЕ Результат.Выполнено Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(Результат.ОписаниеОшибки);
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	СведенияОбОтчете = Новый Структура;
	СведенияОбОтчете.Вставить("ВидКонтролирующегоОргана",
		ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
	СведенияОбОтчете.Вставить("Наименование", НСтр("ru = 'Отзыв машиночитаемой доверенности (ФСС)'"));
	СведенияОбОтчете.Вставить("Организация", ДополнительныеПараметры.Организация);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("Организация", 				ДополнительныеПараметры.Организация);
	КонтекстДлительнойОперации.Вставить("СсылкаНаОтчет", 			ДополнительныеПараметры.ОтчетСсылка);
	КонтекстДлительнойОперации.Вставить("НомерОтчета", 				ДополнительныеПараметры.НомерДоверенности);
	КонтекстДлительнойОперации.Вставить("ДатаНачалаОтправки", 		ДополнительныеПараметры.ДатаНачалаОтправки);
	КонтекстДлительнойОперации.Вставить("ОповещениеОЗавершении", 	ДополнительныеПараметры.ОповещениеОЗавершении);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Неопределено;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	ПараметрыДлительнойОтправки.Вставить("СведенияОбОтчете", СведенияОбОтчете);
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФСС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтозватьМашиночитаемуюДоверенностьФССПослеОтправки", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтозватьМашиночитаемуюДоверенностьФССПослеОтправки() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ВидОтчета = ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ОтзывМашиночитаемойДоверенностиФСС");
	ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСЭДО(ВидОтчета);
	
	ПараметрыАвтозапроса = Новый Структура();
	ПараметрыАвтозапроса.Вставить("Ключ", 				Неопределено);
	ПараметрыАвтозапроса.Вставить("ЭтоСЭДО", 			Истина);
	ПараметрыАвтозапроса.Вставить("ТипВзаимодействия", 	ТипВзаимодействия);
	ПараметрыАвтозапроса.Вставить("Организация", 		КонтекстДлительнойОперации.Организация);
	ПараметрыАвтозапроса.Вставить("ВидОтчета", 			ВидОтчета);
	ПараметрыАвтозапроса.Вставить("СсылкаНаОтчет", 		КонтекстДлительнойОперации.СсылкаНаОтчет);
	ПараметрыАвтозапроса.Вставить("НомерОтчета", 		КонтекстДлительнойОперации.НомерОтчета);
	ПараметрыАвтозапроса.Вставить("ДатаНачалаОтправки", КонтекстДлительнойОперации.ДатаНачалаОтправки);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
	ДополнительныеПараметры.Вставить("ЭтоЗапросРезультатаОперации", Истина);
	ДополнительныеПараметры.Вставить("ВидКонтролирующегоОргана",
		ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры);
	
	Если КонтекстДлительнойОперации.ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(КонтекстДлительнойОперации.ОповещениеОЗавершении, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьМашиночитаемуюДоверенностьФСС() Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗагрузитьМашиночитаемуюДоверенностьФССПослеВвода", ЭтотОбъект);
	ОткрытьФорму(
		"Справочник.МашиночитаемыеДоверенностиФСС.Форма.ФормаЗагрузки",,
		ЭтаФорма,,,,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьМашиночитаемуюДоверенностьФССПослеВвода(
		СтруктураДанных,
		ДополнительныеПараметры) Экспорт
	
	Если СтруктураДанных = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыСообщенияЗапроса = ДокументооборотСКОВызовСервера.ПараметрыЗапросаМашиночитаемойДоверенностиФСС(
		СтруктураДанных.Организация,
		СтруктураДанных.НомерДоверенности);
		
	Оповестить("ЗагрузкаМЧДФСС", СтруктураДанных);
	
	Если ПараметрыСообщенияЗапроса.Свойство("ЭтоСведенияОбОтчетеОперации")
		И ПараметрыСообщенияЗапроса.ЭтоСведенияОбОтчетеОперации
		И ТипЗнч(ПараметрыСообщенияЗапроса.ОтчетСсылка) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиФСС") Тогда
		
		СсылкаСуществующейДоверенности = ПараметрыСообщенияЗапроса.ОтчетСсылка;
		ПоказатьЗначение(, СсылкаСуществующейДоверенности);
		Возврат;
	КонецЕсли;
	
	СтруктураДанных.Вставить("ДатаНачалаОтправки", ПараметрыСообщенияЗапроса.ДатаСеансаНаСервере);
	ПараметрыСообщенияЗапроса.Удалить("ДатаСеансаНаСервере");
	
	ПараметрыСообщения = ПараметрыОтправитьСообщениеСЭДОФСС();
	ЗаполнитьЗначенияСвойств(ПараметрыСообщения, ПараметрыСообщенияЗапроса);
	ПараметрыСообщения.СодержимоеСообщения = ПараметрыСообщенияЗапроса.СодержимоеИлиАдресСообщения;
	ПараметрыСообщения.ОповещениеОбратногоВызова = Новый ОписаниеОповещения(
		"ЗагрузитьМашиночитаемуюДоверенностьФССПослеОтправки", ЭтотОбъект, СтруктураДанных);
	
	ОтправитьСообщениеСЭДОФСС(ПараметрыСообщения, , , Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьМашиночитаемуюДоверенностьФССПослеОтправки(
		Результат,
		ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(Результат.ОписаниеОшибки);
		Возврат;
	КонецЕсли;
	
	СведенияОбОтчете = Новый Структура;
	СведенияОбОтчете.Вставить("ВидКонтролирующегоОргана",
		ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
	СведенияОбОтчете.Вставить("Наименование", НСтр("ru = 'Запрос машиночитаемой доверенности (ФСС)'"));
	СведенияОбОтчете.Вставить("Организация", ДополнительныеПараметры.Организация);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("Организация", 			ДополнительныеПараметры.Организация);
	КонтекстДлительнойОперации.Вставить("НомерОтчета", 			ДополнительныеПараметры.НомерДоверенности);
	КонтекстДлительнойОперации.Вставить("ДатаНачалаОтправки", 	ДополнительныеПараметры.ДатаНачалаОтправки);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Неопределено;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	ПараметрыДлительнойОтправки.Вставить("СведенияОбОтчете", СведенияОбОтчете);
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФСС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ЗагрузитьМашиночитаемуюДоверенностьФССПослеОтправки", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ЗагрузитьМашиночитаемуюДоверенностьФССПослеОтправки() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ВидОтчета = ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ЗапросМашиночитаемойДоверенностиФСС");
	ТипВзаимодействия = ДокументооборотСФССКлиентСервер.ТипВзаимодействияСЭДО(ВидОтчета);
	
	ПараметрыАвтозапроса = Новый Структура();
	ПараметрыАвтозапроса.Вставить("Ключ", 				Неопределено);
	ПараметрыАвтозапроса.Вставить("ЭтоСЭДО", 			Истина);
	ПараметрыАвтозапроса.Вставить("ТипВзаимодействия", 	ТипВзаимодействия);
	ПараметрыАвтозапроса.Вставить("Организация", 		КонтекстДлительнойОперации.Организация);
	ПараметрыАвтозапроса.Вставить("ВидОтчета", 			ВидОтчета);
	ПараметрыАвтозапроса.Вставить("НомерОтчета", 		КонтекстДлительнойОперации.НомерОтчета);
	ПараметрыАвтозапроса.Вставить("ДатаНачалаОтправки", КонтекстДлительнойОперации.ДатаНачалаОтправки);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
	ДополнительныеПараметры.Вставить("ЭтоЗапросРезультатаОперации", Истина);
	ДополнительныеПараметры.Вставить("ВидКонтролирующегоОргана",
		ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПоказатьПредупреждениеОбОтправкеНаТестовыйСерверФССПослеАвтонастройки(
		ОрганизацияОтчета,
		ЭтоДанныеЭЛН,
		ЭтоПодтверждениеВидаДеятельности)
	
	Если ЭтоПодтверждениеВидаДеятельности
		И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.ПОВЭД через СЭДО", Истина) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат ?(ЭтоДанныеЭЛН, ИспользоватьТестовыйСерверФССЭЛН(ОрганизацияОтчета),
		ДокументооборотСКОВызовСервера.ИспользуетсяРежимТестирования())
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьОтчетДляОтправкиВФССИнициализацияНаСервере(
	ТипОтправляемогоДокумента,
	ВидОтправляемогоДокумента,
	ИмяФайлаВыгрузки = Неопределено,
	ТекстФайлаВыгрузки = Неопределено,
	ОрганизацияОтчета = Неопределено)
	
	ЭтоРеестрСтимулирующихВыплат2023 = (ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам")
		И ЗначениеЗаполнено(ТекстФайлаВыгрузки) И СтрНайти(ТекстФайлаВыгрузки, "<submit2023WorkerRegistrRequest ") > 0);
	ЭтоРеестрСтимулирующихВыплатГосслужащим = (ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам")
		И ЗначениеЗаполнено(ТекстФайлаВыгрузки) И СтрНайти(ТекстФайлаВыгрузки, "<PaymentsFederalCivilServantsRequest ") > 0);
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("ЭтоРеестрСтимулирующихВыплат2023", 		ЭтоРеестрСтимулирующихВыплат2023);
	НастройкиВызова.Вставить("ЭтоРеестрСтимулирующихВыплатГосслужащим", ЭтоРеестрСтимулирующихВыплатГосслужащим);
	
	ОтправкаЧерезСЭДО = ДокументооборотСФССКлиентСервер.ПараметрыОтправкиВидаДокументаЧерезСЭДО(
		ВидОтправляемогоДокумента,,,,
		НастройкиВызова).ОтправкаЧерезСЭДО;
	
	РасширениеФайла = ПолучитьРасширениеФайлаФССПоТипуОтправляемогоДокумента(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		ОтправкаЧерезСЭДО);
	
	ЭтоПодтверждениеВидаДеятельности =
		(ТипОтправляемогоДокумента = Перечисления.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС
		И ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности);
	
	ТекстОшибки = "";
	ДанныеФайлаВыгрузки = ТекстФайлаВыгрузки;
	Если ТекстФайлаВыгрузки <> Неопределено И ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС") Тогда
		
		ДанныеФайлаВыгрузки = ДанныеРеестраЭЛННаСервере(ТекстФайлаВыгрузки);
		
	ИначеЕсли ТипОтправляемогоДокумента =
	ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.РеестрСведенийФСС") Тогда
		
		Если СтрНайти(ИмяФайлаВыгрузки, "\") <> 0 ИЛИ СтрНайти(ИмяФайлаВыгрузки, "/") <> 0 Тогда
			ТекстОшибки = СтрШаблон(
				НСтр("ru = 'Некорректное имя файла выгрузки ""%1"", проверьте языковые настройки системы.'"),
				ИмяФайлаВыгрузки);
			
		Иначе
			СвойстваОрганизации = СвойстваОрганизацииДляОбмена(ОрганизацияОтчета, Истина, Ложь);
			Пока Истина Цикл
				Если СтрНайти(ИмяФайлаВыгрузки, "_" + СвойстваОрганизации.РегистрационныйНомерФСС + "_") <> 0
					И (СтрДлина(СвойстваОрганизации.РегистрационныйНомерФСС) <> 10
					ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(СвойстваОрганизации.РегистрационныйНомерФСС)) Тогда
					
					ТекстОшибки = СтрШаблон(?(СвойстваОрганизации.ЭтоДополнительныйКодФСС,
						НСтр("ru = 'Дополнительный код ФСС в свойствах организации должен состоять из 10 цифр, текущее значение ""%1"".'"),
						НСтр("ru = 'Регистрационный номер ФСС в свойствах организации должен состоять из 10 цифр, текущее значение ""%1"".'")),
						СвойстваОрганизации.РегистрационныйНомерФСС);
					Прервать;
				КонецЕсли;
				
				Если СвойстваОрганизации.РегистрационныйНомерФСС = СокрЛП(СвойстваОрганизации.РегистрационныйНомерФСС) Тогда
					Прервать;
				КонецЕсли;
				СвойстваОрганизации.РегистрационныйНомерФСС = СокрЛП(СвойстваОрганизации.РегистрационныйНомерФСС);
			КонецЦикла;
		КонецЕсли;
		
	ИначеЕсли ЭтоПодтверждениеВидаДеятельности Тогда
		ПозицияПрефикса = СтрНайти(ТекстФайлаВыгрузки, "<OKVEDConfirmationRequest ");
		Если ПозицияПрефикса > 0 Тогда
			ТекстФайлаВыгрузки = Сред(ТекстФайлаВыгрузки, ПозицияПрефикса);
			ТекстПостфикса = "</OKVEDConfirmationRequest>";
			ПозицияПостфикса = СтрНайти(ТекстФайлаВыгрузки, ТекстПостфикса);
			Если ПозицияПостфикса > 0 Тогда
				ДлинаПостфикса = СтрДлина(ТекстПостфикса);
				ТекстФайлаВыгрузки = Лев(ТекстФайлаВыгрузки, ПозицияПостфикса + ДлинаПостфикса - 1);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ОтправкаЧерезСЭДО Тогда
		ДанныеФайлаВыгрузки = ДокументооборотСФССКлиентСервер.ПараметрыОтправкиВидаДокументаЧерезСЭДО(
			ВидОтправляемогоДокумента, Ложь, Ложь, ТекстФайлаВыгрузки).ТекстВыгрузки;
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("РасширениеФайла", 		РасширениеФайла);
	Результат.Вставить("ТекстФайлаВыгрузки", 	ДанныеФайлаВыгрузки);
	Результат.Вставить("ТекстОшибки", 			ТекстОшибки);
	Результат.Вставить("ОтправкаЧерезСЭДО", 	ОтправкаЧерезСЭДО);
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьОтправкуФССНаСервере(
		Знач ЭтоЭлектроннаяПодписьВМоделиСервиса,
		Знач КороткоеИмяФайлаПакета,
		Знач СтрокаBase64ФайлаПакета,
		Знач Идентификатор,
		Знач НастройкиОтправки) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ДобавитьОтправкуФСС(
		ЭтоЭлектроннаяПодписьВМоделиСервиса,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		Идентификатор,
		НастройкиОтправки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеРеестраЭЛННаСервере(ВыгрузкаXML)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ДанныеРеестраЭЛН(ВыгрузкаXML);
	
КонецФункции

&НаКлиенте
Функция ВыгрузитьОтчетДляОтправкиВРПН(ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность) Экспорт

	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСРПНКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ТекстФайлаВыгрузки 	= "";
	ИмяФайлаВыгрузки 	= "";
	Приложения = Новый Массив;
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	Кодировка 			= "utf-8"; // кодировка, в которую нужно сконвертировать текст выгрузки перед подписью
	
	Если ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено") Тогда
		
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
		Иначе
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
		КонецЕсли;
		
		Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(ОтчетСсылка, Ложь, Ложь, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		
		Если ТипЗнч(ТекстФайлаВыгрузки) = Тип("Массив") Тогда
			АдресОсновногоФайлаВыгрузки = Неопределено;
			Для каждого ЭлементВыгрузки Из ТекстФайлаВыгрузки Цикл
				Если ТипЗнч(ЭлементВыгрузки) = Тип("Структура")
					И ЭлементВыгрузки.Свойство("АдресФайлаВыгрузки")
					И ЭлементВыгрузки.Свойство("ИмяФайлаВыгрузки") Тогда
					
					РасширениеВыгрузки = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
						ЭлементВыгрузки.ИмяФайлаВыгрузки).Расширение;
					Если нрег(РасширениеВыгрузки) = ".xml" И АдресОсновногоФайлаВыгрузки = Неопределено Тогда
						АдресОсновногоФайлаВыгрузки = ЭлементВыгрузки.АдресФайлаВыгрузки;
						ИмяФайлаВыгрузки = ЭлементВыгрузки.ИмяФайлаВыгрузки;
						
					Иначе
						Приложения.Добавить(ЭлементВыгрузки);
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			Если АдресОсновногоФайлаВыгрузки = Неопределено И ТекстФайлаВыгрузки.Количество() > 0
				И ТипЗнч(ТекстФайлаВыгрузки[0]) = Тип("Структура")
				И ТекстФайлаВыгрузки[0].Свойство("АдресФайлаВыгрузки")
				И ТекстФайлаВыгрузки[0].Свойство("ИмяФайлаВыгрузки") Тогда
				
				АдресОсновногоФайлаВыгрузки = ТекстФайлаВыгрузки[0].АдресФайлаВыгрузки;
				ИмяФайлаВыгрузки = ТекстФайлаВыгрузки[0].ИмяФайлаВыгрузки;
				Приложения.Удалить(0);
			КонецЕсли;
			Если АдресОсновногоФайлаВыгрузки <> Неопределено Тогда
				ТекстФайлаВыгрузки = ПрочитатьТекстИзФайла(АдресОсновногоФайлаВыгрузки, "utf-8");
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения") Тогда
		МассивВыгруженныхУведомлений = Неопределено;
		Если НЕ ВыгрузитьУведомлениеФНС(ОтчетСсылка, МассивВыгруженныхУведомлений) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		ТекстФайлаВыгрузки = ПрочитатьТекстИзФайла(МассивВыгруженныхУведомлений[0].АдресФайлаВыгрузки, "utf-8");
		ИмяФайлаВыгрузки = МассивВыгруженныхУведомлений[0].ИмяФайлаВыгрузки;
		
	Иначе // электронное представление
		Если ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
			ОтчетСсылка,
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,,
			"utf-8");
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 	ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 	ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("Кодировка", 			Кодировка);
	
	Возврат ДополнительныеПараметры;

КонецФункции

&НаКлиенте
Функция ВыгрузитьОтчетДляОтправкиВФСРАР(
	ОтчетСсылка,
	Форма,
	ЭтоОтправкаИзФормыОтчетность,
	АдресГотовыхДанных = Неопределено,
	ИмяФайлаГотовыхДанных = Неопределено) Экспорт
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ТекстФайлаВыгрузки 	= "";
	ИмяФайлаВыгрузки 	= ""; 
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Если ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено") Тогда
		
		Если АдресГотовыхДанных <> Неопределено Тогда
			ПривестиКСтроке = (РегламентированнаяОтчетностьКлиент.ВебБраузер() = "MSIE");
			ТекстФайлаВыгрузки = ПолучитьИзВременногоХранилищаЕслиСтрока(АдресГотовыхДанных, ПривестиКСтроке);
			ИмяФайлаВыгрузки = ИмяФайлаГотовыхДанных;
			
		Иначе
			Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда 
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
			Иначе
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
			КонецЕсли;
			
			Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(ОтчетСсылка, Ложь, Ложь, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
		
	Иначе // электронное представление
		Если ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(ОтчетСсылка, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 	ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 	ИмяФайлаВыгрузки);
	
	Возврат ДополнительныеПараметры;

КонецФункции

&НаКлиенте
Функция ВыгрузитьОтчетДляОтправкиВФТС(ОтчетСсылка, Форма, ЭтоОтправкаИзФормыОтчетность) Экспорт

	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ТекстФайлаВыгрузки 	= "";
	ИмяФайлаВыгрузки 	= ""; 
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Если ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено") Тогда
		
		РезультатВыгрузкиИзЗаявленияОВвозеТоваров = ПопытатьсяВыгрузитьСтатформуИзЗаявленияОВвозеТоваров(
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,
			ОтчетСсылка);

		Если РезультатВыгрузкиИзЗаявленияОВвозеТоваров = Неопределено Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		
		Если НЕ РезультатВыгрузкиИзЗаявленияОВвозеТоваров Тогда
			Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
			Иначе
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
			КонецЕсли;
			
			Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(ОтчетСсылка, Ложь, Ложь, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
				Возврат Неопределено;
			КонецЕсли;
		КонецЕсли;
		
	Иначе // электронное представление
		Если ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
			Возврат Неопределено;
		КонецЕсли;
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
			ОтчетСсылка,
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,,
			"utf-8");
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 	ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 	ИмяФайлаВыгрузки);
	
	Возврат ДополнительныеПараметры;

КонецФункции

&НаКлиенте
Процедура Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПодтвержденияЗавершение() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ОписаниеОповещения 					= КонтекстДлительнойОперации.ОписаниеОповещения;
	ОрганизацияОтчета 					= КонтекстДлительнойОперации.ОрганизацияОтчета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(КонтекстДлительнойОперации);

	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеИнициализации(Результат, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОтправкаРегламентированногоОтчетаВФСС_ПослеИнициализации(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОрганизацияСсылка 				= ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка 					= ДополнительныеПараметры.ОтчетСсылка;
	Форма 							= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 	= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОповещениеОЗавершении 			= ДополнительныеПараметры.ОповещениеОЗавершении;
	ТипОтправляемогоДокумента 		= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 		= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСФССКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	Если ОтчетСсылка = Неопределено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияСсылка = ДокументооборотСФССКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	УчетнаяЗапись 						= РеквизитыУчетнойЗаписи.Ссылка;
	Идентификатор 						= РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	ВыполняемоеОповещение = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеВыгрузкиПакетаЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	// запрашиваем параметры формирования пакета
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Идентификатор",         Идентификатор);
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВФССПослеПолученияСертификатовЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	ПолучатьСертификатФССЭЛН = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	ПолучитьСертификатыИзНастроекОрганизацииДляФСС(
		МестоХраненияКлюча,
		ОрганизацияСсылка,
		Оповещение,
		ПолучатьСертификатФССЭЛН);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеВыгрузкиПакетаЗавершение(РезультатВыгрузки, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОтправкаРегламентированногоОтчетаВФСС_ПослеВыгрузкиПакетаЗавершение(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОрганизацияСсылка 					= ДополнительныеПараметры.ОрганизацияОтчета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	МЧД 								= ?(ДополнительныеПараметры.Свойство("МЧД"), ДополнительныеПараметры.МЧД, "");
	
	Если НЕ ЗначениеЗаполнено(РезультатВыгрузки) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	ЭтоПодписьИШифрованиеSOAP = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоПодписьИШифрованиеSOAP Тогда
		
		СтрокаBase64ФайлаПакета = РезультатВыгрузки.ИмяФайлаЗашифрованногоПакета;
		КороткоеИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(РезультатВыгрузки.ИмяФайлаВыгрузки).Имя;
		
		Результат = Новый Структура();
		Результат.Вставить("Выполнено",      Истина);
		Результат.Вставить("ДвоичныеДанные", Неопределено);
		Результат.Вставить("СтрокаBase64",   СтрокаBase64ФайлаПакета);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета", 				КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 					ОрганизацияСсылка);
		ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", 				ПоляДляЗаписиОтправки);
		ДополнительныеПараметры.Вставить("Форма", 								Форма);
		ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 		ЭтоОтправкаИзФормыОтчетность);
		ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 			ТипОтправляемогоДокумента);
		ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 			ВидОтправляемогоДокумента);
		ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", 					РезультатВыгрузки.ПодписанныйSOAP64);
		ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеЧтенияФайлаВыгрузкиПакетаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
	
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	Иначе
		
		КороткоеИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(РезультатВыгрузки).Имя;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета", 				КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 					ОрганизацияСсылка);
		ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", 				ПоляДляЗаписиОтправки);
		ДополнительныеПараметры.Вставить("Форма", 								Форма);
		ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 		ЭтоОтправкаИзФормыОтчетность);
		ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 			ТипОтправляемогоДокумента);
		ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 			ВидОтправляемогоДокумента);
		ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", 					Неопределено);
		ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеЧтенияФайлаВыгрузкиПакетаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, РезультатВыгрузки);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеЧтенияФайлаВыгрузкиПакетаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОтправкаРегламентированногоОтчетаВФСС_ПослеВыгрузкиПакетаЗавершение(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КороткоеИмяФайлаПакета 				= ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	ОрганизацияСсылка 					= ДополнительныеПараметры.ОрганизацияСсылка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	ПодписанныйSOAP64 					= ДополнительныеПараметры.ПодписанныйSOAP64;
	МЧД 								= ДополнительныеПараметры.МЧД;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаПакета = Результат.СтрокаBase64;
	
	ИнформацияДляОтправкиВФСС = СформироватьФайлОтправкиВФССНаСервере(
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
		Форма.УникальныйИдентификатор,
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента);
	СтрокаBase64ФайлаПакета 	= ИнформацияДляОтправкиВФСС.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки 	= ИнформацияДляОтправкиВФСС.СтрокаBase64ФайлаОтправки;
	
	Если СтрокаBase64ФайлаОтправки = Неопределено Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось сформировать файл отправки.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
		
	КонецЕсли;
	
	// определим адрес сервера ФСС
	
	АдресСервера = СерверПриемаОтчетностиФСС(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		ОрганизацияСсылка);
	
	Если АдресСервера = Неопределено Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось определить тип документа.'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСС.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
		
	КонецЕсли;
	
	// отправляем
	
	НастройкиОтправки = Новый Структура;
	НастройкиОтправки.Вставить("ОрганизацияСсылка", 			ОрганизацияСсылка);
	НастройкиОтправки.Вставить("ОтчетСсылка", 					ОтчетСсылка);
	НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 		ПоляДляЗаписиОтправки);
	НастройкиОтправки.Вставить("Форма", 						Форма);
	НастройкиОтправки.Вставить("ЭтоОтправкаИзФормыОтчетность", 	ЭтоОтправкаИзФормыОтчетность);
	НастройкиОтправки.Вставить("ТипОтправляемогоДокумента", 	ТипОтправляемогоДокумента);
	НастройкиОтправки.Вставить("ВидОтправляемогоДокумента", 	ВидОтправляемогоДокумента);
	НастройкиОтправки.Вставить("ПодписанныйSOAP64", 			ПодписанныйSOAP64);
	НастройкиОтправки.Вставить("МЧД", 							МЧД);
	НастройкиОтправки.Вставить("ОповещениеОЗавершении", 		ОповещениеОЗавершении);
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССОтправкаВЦикле(
		МестоХраненияКлюча,
		АдресСервера,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		НастройкиОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервере(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		ЭтоВызовИзСписка)
	
	ПодписанныйПакетДляАвторизации = "";
	АвторизацияПодготовлена = Истина;
	
	Если НЕ АвторизацияПроизведена Тогда
		
		СлучайныйКлюч                      = "";
		Данные64CAPTCHA                    = "";
		ПараметрыСоединения.ЗначениеCookie = "";
		
		ПодписанныйПакетДляАвторизацииТребуется = (СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы") 
			ИЛИ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале);
		
		Если ПодписанныйПакетДляАвторизацииТребуется Тогда
			
			ПодписываемыеДанные = ПолучитьПодписываемыеДанныеФСРАРНаСервере(
				ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
				СлучайныйКлюч,
				Данные64CAPTCHA,
				ОтправкаСсылка,
				ЭтоРегламентированныйОтчет,
				ПараметрыОтправки,
				ПараметрыСоединения,
				,
				ЭтоВызовИзСписка);
			
			ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
				
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПодписываемыеДанные) Тогда
			
			Если СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы") ИЛИ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале Тогда
				ЗначениеCAPTCHA = "";
				Если Данные64CAPTCHA <> "" Тогда
					
					ДополнительныеПараметры = Новый Структура;
					ДополнительныеПараметры.Вставить("Форма",                                                              Форма);
					ДополнительныеПараметры.Вставить("ОтправкаСсылка",                                                     ОтправкаСсылка);
					ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",                                              ВыполняемоеОповещение);
					ДополнительныеПараметры.Вставить("ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал",   ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал);
					ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",                                            ФормаДлительнаяОперация);
					ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",                                        СертификатыИПараметрыОбмена);
					ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                                               СхемаСдачиОтчетности);
					ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизации",                                     ПодписанныйПакетДляАвторизации);
					ДополнительныеПараметры.Вставить("Крипт",                                                              Крипт);
					ДополнительныеПараметры.Вставить("ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64",   ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64);
					ДополнительныеПараметры.Вставить("ПараметрыСоединения",                                                ПараметрыСоединения);
					ДополнительныеПараметры.Вставить("ПараметрыОтправки",                                                  ПараметрыОтправки);
					ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",                                        ПараметрыФормированияПакета);
					ДополнительныеПараметры.Вставить("УчетнаяЗапись",                                                      УчетнаяЗапись);
					ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",                                         ЭтоРегламентированныйОтчет);
					ДополнительныеПараметры.Вставить("ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале", ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале);
					ДополнительныеПараметры.Вставить("СлучайныйКлюч",                                                      СлучайныйКлюч);
					ДополнительныеПараметры.Вставить("ПодписываемыеДанные",                                                ПодписываемыеДанные);
					ДополнительныеПараметры.Вставить("АвторизацияПроизведена",                                             АвторизацияПроизведена);
					ДополнительныеПараметры.Вставить("АвторизацияПодготовлена",                                            АвторизацияПодготовлена);
					ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",                                                   ЭтоВызовИзСписка);
					ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизацииТребуется",                            ПодписанныйПакетДляАвторизацииТребуется);
					ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
					
					ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервереЗавершение", ЭтотОбъект, ДополнительныеПараметры); 
					
					ВызватьФормуВводаCAPTCHA(Данные64CAPTCHA, , ОписаниеОповещения);
					Возврат;
				КонецЕсли;
				
				ДополнительныеПараметры = Новый Структура;
				ДополнительныеПараметры.Вставить("Форма",                                                              Форма);
				ДополнительныеПараметры.Вставить("ОтправкаСсылка",                                                     ОтправкаСсылка);
				ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",                                              ВыполняемоеОповещение);
				ДополнительныеПараметры.Вставить("ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал",   ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал);
				ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",                                            ФормаДлительнаяОперация);
				ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",                                        СертификатыИПараметрыОбмена);
				ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                                               СхемаСдачиОтчетности);
				ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

				ДополнительныеПараметры.Вставить("Крипт",                                                              Крипт);
				ДополнительныеПараметры.Вставить("ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64",   ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64);
				ДополнительныеПараметры.Вставить("ПараметрыСоединения",                                                ПараметрыСоединения);
				ДополнительныеПараметры.Вставить("ПараметрыОтправки",                                                  ПараметрыОтправки);
				ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",                                        ПараметрыФормированияПакета);
				ДополнительныеПараметры.Вставить("УчетнаяЗапись",                                                      УчетнаяЗапись);
				ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",                                         ЭтоРегламентированныйОтчет);
				ДополнительныеПараметры.Вставить("ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале", ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале);
				ДополнительныеПараметры.Вставить("АвторизацияПроизведена",                                             АвторизацияПроизведена);
				ДополнительныеПараметры.Вставить("АвторизацияПодготовлена",                                            АвторизацияПодготовлена);
				ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",                                                   ЭтоВызовИзСписка);
				ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизацииТребуется",                            ПодписанныйПакетДляАвторизацииТребуется);
				
				Оповещение = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервереПослеФормированияПодписанногоПакетаДляПортала", ЭтотОбъект, ДополнительныеПараметры);
				
				СформироватьПодписанныйПакетДляПорталаФСРАР(
					МестоХраненияКлюча,
					УчетнаяЗапись,
					ПодписываемыеДанные,
					ПараметрыФормированияПакета.СертификатАбонента,
					Оповещение,
					Крипт,
					СлучайныйКлюч,
					ЗначениеCAPTCHA);
				Возврат;
				
			КонецЕсли;
			
		КонецЕсли;
		
		Если ПодписанныйПакетДляАвторизацииТребуется И НЕ ЗначениеЗаполнено(ПодписанныйПакетДляАвторизации) Тогда
			АвторизацияПодготовлена = Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПодготовкиАвторизации(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		ПодписанныйПакетДляАвторизации,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		АвторизацияПодготовлена,
		ЭтоВызовИзСписка);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССОтправкаВЦикле(
		МестоХраненияКлюча,
		АдресСервера,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		НастройкиОтправки,
		НомерПопытки = 1)
	
	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура ОтправкаРегламентированногоОтчетаВФСС_ПослеВыгрузкиПакетаЗавершение(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ОрганизацияСсылка 				= НастройкиОтправки.ОрганизацияСсылка;
	ОтчетСсылка 					= НастройкиОтправки.ОтчетСсылка;
	ПоляДляЗаписиОтправки 			= НастройкиОтправки.ПоляДляЗаписиОтправки;
	Форма 							= НастройкиОтправки.Форма;
	ЭтоОтправкаИзФормыОтчетность 	= НастройкиОтправки.ЭтоОтправкаИзФормыОтчетность;
	ТипОтправляемогоДокумента 		= НастройкиОтправки.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 		= НастройкиОтправки.ВидОтправляемогоДокумента;
	ПодписанныйSOAP64 				= НастройкиОтправки.ПодписанныйSOAP64;
	МЧД 							= НастройкиОтправки.МЧД;
	ОповещениеОЗавершении 			= НастройкиОтправки.ОповещениеОЗавершении;
	
	ЭтоДанныеЭЛН = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ДанныеЭЛНФСС"));
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	ЭтоПодписьИШифрованиеSOAP = ЭтоДанныеЭЛН ИЛИ ЭтоПодтверждениеВидаДеятельности
		ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам;
	
	ПовторятьСоединение = Истина;
	ПараметрыОтправкиНаСервере = Новый Структура;
	ПараметрыОтправкиНаСервере.Вставить("АдресСервера", 				АдресСервера);
	ПараметрыОтправкиНаСервере.Вставить("РазделениеВключено", 			Неопределено);
	ПараметрыОтправкиНаСервере.Вставить("ПоляДляЗаписиОтправки", 		ПоляДляЗаписиОтправки);
	ПараметрыОтправкиНаСервере.Вставить("ТипОтправляемогоДокумента", 	ТипОтправляемогоДокумента);
	ПараметрыОтправкиНаСервере.Вставить("ВидОтправляемогоДокумента", 	ВидОтправляемогоДокумента);
	ПараметрыОтправкиНаСервере.Вставить("ПодписанныйSOAP64", 			ПодписанныйSOAP64);
	ПараметрыОтправкиНаСервере.Вставить("МЧД", 							МЧД);
	НоваяОтправкаСсылка = ОтправитьВФССНаСервере(
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		ОтчетСсылка,
		ПовторятьСоединение,
		ПараметрыОтправкиНаСервере);
	
	Если ЗначениеЗаполнено(НоваяОтправкаСсылка) Тогда
		Если ЭтоПодписьИШифрованиеSOAP Тогда
			АдресЗашифрованногоОтветаSOAP = НоваяОтправкаСсылка;
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("АдресСервера", 						ПараметрыОтправкиНаСервере.АдресСервера);
			ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета", 				КороткоеИмяФайлаПакета);
			ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки", 			СтрокаBase64ФайлаОтправки);
			ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета", 			СтрокаBase64ФайлаПакета);
			ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 					ОрганизацияСсылка);
			ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
			ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", 				ПараметрыОтправкиНаСервере.ПоляДляЗаписиОтправки);
			ДополнительныеПараметры.Вставить("Форма", 								Форма);
			ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 		ЭтоОтправкаИзФормыОтчетность);
			ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 			ТипОтправляемогоДокумента);
			ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 			ВидОтправляемогоДокумента);
			ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", 					ПодписанныйSOAP64);
			ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
			ДополнительныеПараметры.Вставить("НомерПопытки", 						НомерПопытки);
			ДополнительныеПараметры.Вставить("АдресЗашифрованногоОтветаSOAP", 		АдресЗашифрованногоОтветаSOAP);
			КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеОтправкиSOAP",
				ЭтотОбъект, ДополнительныеПараметры);
			
			ЗаписыватьОшибкиВЖурналРегистрации 	= Истина;

			ПроверитьПодписьSOAPОтветаЭЛНВФСС(
				ОписаниеОповещения,
				ОрганизацияСсылка,
				АдресЗашифрованногоОтветаSOAP,,
				ЗаписыватьОшибкиВЖурналРегистрации,
				?(ЭтоПодтверждениеВидаДеятельности, "ПодтверждениеВидаДеятельности",
					?(ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам,
					"РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам", "")));
			
		Иначе
			ПараметрыАвтозапроса = Новый Структура();
			ПараметрыАвтозапроса.Вставить("Ключ", НоваяОтправкаСсылка);
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
		
			ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСС.
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли ПовторятьСоединение Тогда
		
		// не получено файла результата при отправке пакета. Возможно, проблемы с доступом в интернет
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("АдресСервера", 						ПараметрыОтправкиНаСервере.АдресСервера);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета", 				КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки", 			СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета", 			СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 					ОрганизацияСсылка);
		ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", 				ПараметрыОтправкиНаСервере.ПоляДляЗаписиОтправки);
		ДополнительныеПараметры.Вставить("Форма", 								Форма);
		ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 		ЭтоОтправкаИзФормыОтчетность);
		ДополнительныеПараметры.Вставить("ТипОтправляемогоДокумента", 			ТипОтправляемогоДокумента);
		ДополнительныеПараметры.Вставить("ВидОтправляемогоДокумента", 			ВидОтправляемогоДокумента);
		ДополнительныеПараметры.Вставить("ПодписанныйSOAP64", 					ПодписанныйSOAP64);
		ДополнительныеПараметры.Вставить("МЧД", 								МЧД);
		ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 				ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("НомерПопытки", 						НомерПопытки);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПоказаОшибкиДоступаВИнтернет",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		Если ПараметрыОтправкиНаСервере.РазделениеВключено Тогда
			ПовторитьПопытку = (НомерПопытки < 5);
			Если НЕ ПовторитьПопытку Тогда
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Не удалось обратиться к серверу ФСС РФ (http://%1). Возможно, сервер перегружен. Попробуйте повторить отправку через некоторое время.'"),
					ПараметрыОтправкиНаСервере.АдресСервера);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			КонецЕсли;
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, ПовторитьПопытку);
			
		Иначе
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Не удалось обратиться к серверу ФСС РФ (http://%1)'"),
				ПараметрыОтправкиНаСервере.АдресСервера);
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(ТекстСообщения, ОписаниеОповещения);
		КонецЕсли;
		Возврат;
		
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСС. Нет доступа в Интернет.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеОтправкиSOAP(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	КороткоеИмяФайлаПакета 				= ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	СтрокаBase64ФайлаПакета 			= ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	ОрганизацияСсылка 					= ДополнительныеПараметры.ОрганизацияСсылка;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	ПодписанныйSOAP64 					= ДополнительныеПараметры.ПодписанныйSOAP64;
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	АдресЗашифрованногоОтветаSOAP 		= ДополнительныеПараметры.АдресЗашифрованногоОтветаSOAP;
	
	ЭтоПодтверждениеВидаДеятельности = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.ПодтверждениеВидаДеятельности"));
	ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам = (ТипОтправляемогоДокумента =
		ПредопределенноеЗначение("Перечисление.ТипыОтправляемыхДокументов.ПрочаяОтчетностьФСС")
		И ВидОтправляемогоДокумента =
		ПредопределенноеЗначение("Справочник.ВидыОтправляемыхДокументов.РеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам"));
	
	Если Результат = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Ошибка при расшифровке результата отправки в ФСС.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();// ФCC.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ Результат.ПодписьВалидна Тогда
		// Записываем сообщение, но продолжаем обмен (данные-то мы получили).
		ТекстОшибки = НСтр("ru = 'Подпись результатов отправки недействительна.'");
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами.'") + " " + ?(ЭтоПодтверждениеВидаДеятельности,
				НСтр("ru = 'Подтверждение вида деятельности ФСС'"), ?(ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам,
				НСтр("ru = 'Реестр стимулирующих выплат медицинским и социальным работникам'"), НСтр("ru = 'Реестр ЭЛН'"))),
			"Ошибка",
			ТекстОшибки,,
			Истина);
	КонецЕсли;
	
	ПараметрыРезультата = Новый Структура;
	ПараметрыРезультата.Вставить("АдресРасшифрованногоОтветаSOAP", 			Результат.АдресРасшифрованногоОтветаSOAP);
	ПараметрыРезультата.Вставить("АдресЗашифрованногоОтветаSOAP", 			АдресЗашифрованногоОтветаSOAP);
	ПараметрыРезультата.Вставить("ПоляДляЗаписиОтправки", 					ПоляДляЗаписиОтправки);
	ПараметрыРезультата.Вставить("СохранятьАдресРасшифрованногоОтветаSOAP", ОповещениеОЗавершении <> Неопределено);
	ПараметрыРезультата.Вставить("ПодписанныйSOAP64", 						ПодписанныйSOAP64);
	
	Если ЭтоПодтверждениеВидаДеятельности Тогда
		РезультатОтправки = ОбработатьРезультатОтправкиПриОперацииСОКВЭДНаСервере(
			ОтчетСсылка,
			ОрганизацияСсылка,
			МестоХраненияКлюча,
			КороткоеИмяФайлаПакета,
			СтрокаBase64ФайлаПакета,
			ПараметрыРезультата);
			
	ИначеЕсли ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам Тогда
		РезультатОтправки = ОбработатьРезультатОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникамНаСервере(
			ОтчетСсылка,
			ОрганизацияСсылка,
			МестоХраненияКлюча,
			КороткоеИмяФайлаПакета,
			СтрокаBase64ФайлаПакета,
			ПараметрыРезультата);
		
	Иначе
		РезультатОтправки = ОбработатьРезультатОтправкиРеестраЭЛННаСервере(
			ОтчетСсылка,
			ОрганизацияСсылка,
			МестоХраненияКлюча,
			КороткоеИмяФайлаПакета,
			СтрокаBase64ФайлаПакета,
			ПараметрыРезультата);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(РезультатОтправки.НоваяОтправкаСсылка) Тогда
		Если ЭтоПодтверждениеВидаДеятельности И РезультатОтправки.СтатусОтправки =
			ПредопределенноеЗначение("Перечисление.СтатусыОтправки.Отправлен") Тогда
			
			ПараметрыАвтозапроса = Новый Структура();
			ПараметрыАвтозапроса.Вставить("Ключ", РезультатОтправки.НоваяОтправкаСсылка);
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
			ДополнительныеПараметры.Вставить("ЭтоПромежуточныйПротокол", Истина);
			
			ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСС.
			
		Иначе
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий(); // ФСС.
			
			// Отобразить появление новой записи в журнале обмена
			ПараметрыОповещения = Новый Структура();
			ПараметрыОповещения.Вставить("Ссылка", 		ОтчетСсылка);
			ПараметрыОповещения.Вставить("Организация", ОрганизацияСсылка);
			
			Оповестить("Запись_ОтправкиФСС", ПараметрыОповещения, РезультатОтправки.НоваяОтправкаСсылка);
			
			Если Форма <> Неопределено И НЕ ЭтоОтправкаИзФормыОтчетность Тогда
				ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ОбновитьПанельСостоянияОтправкиВРегламентированномОтчете(
					Форма,
					"ФСС");
			КонецЕсли;
			
			Если ЭтоПодтверждениеВидаДеятельности ИЛИ ЭтоРеестрСтимулирующихВыплатМедицинскимИСоциальнымРаботникам
				ИЛИ ТипЗнч(ОтчетСсылка) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
				
				ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСС(РезультатОтправки.НоваяОтправкаСсылка);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССПослеПоказаОшибкиДоступаВИнтернет(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	АдресСервера 						= ДополнительныеПараметры.АдресСервера;
	КороткоеИмяФайлаПакета 				= ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	СтрокаBase64ФайлаОтправки 			= ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	СтрокаBase64ФайлаПакета 			= ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	ОрганизацияСсылка 					= ДополнительныеПараметры.ОрганизацияСсылка;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	Форма 								= ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТипОтправляемогоДокумента 			= ДополнительныеПараметры.ТипОтправляемогоДокумента;
	ВидОтправляемогоДокумента 			= ДополнительныеПараметры.ВидОтправляемогоДокумента;
	ПодписанныйSOAP64 					= ДополнительныеПараметры.ПодписанныйSOAP64;
	ОповещениеОЗавершении 				= ДополнительныеПараметры.ОповещениеОЗавершении;
	МЧД 								= ДополнительныеПараметры.МЧД;
	НомерПопытки 						= ДополнительныеПараметры.НомерПопытки;
	
	ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	Если ПовторятьСоединение Тогда
		НастройкиОтправки = Новый Структура;
		НастройкиОтправки.Вставить("ОрганизацияСсылка", 			ОрганизацияСсылка);
		НастройкиОтправки.Вставить("ОтчетСсылка", 					ОтчетСсылка);
		НастройкиОтправки.Вставить("ПоляДляЗаписиОтправки", 		ПоляДляЗаписиОтправки);
		НастройкиОтправки.Вставить("Форма", 						Форма);
		НастройкиОтправки.Вставить("ЭтоОтправкаИзФормыОтчетность", 	ЭтоОтправкаИзФормыОтчетность);
		НастройкиОтправки.Вставить("ТипОтправляемогоДокумента", 	ТипОтправляемогоДокумента);
		НастройкиОтправки.Вставить("ВидОтправляемогоДокумента", 	ВидОтправляемогоДокумента);
		НастройкиОтправки.Вставить("ПодписанныйSOAP64", 			ПодписанныйSOAP64);
		НастройкиОтправки.Вставить("МЧД", 							МЧД);
		НастройкиОтправки.Вставить("ОповещениеОЗавершении", 		ОповещениеОЗавершении);
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФССОтправкаВЦикле(
			МестоХраненияКлюча,
			АдресСервера,
			КороткоеИмяФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			СтрокаBase64ФайлаПакета,
			НастройкиОтправки,
			НомерПопытки + 1);
		
	Иначе
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСС. Нет доступа в Интернет.
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПротоколОбработкиПоСсылкеИсточника(КонтролирующийОрган, ИсточникСсылка) Экспорт
	
	Если КонтролирующийОрган = "ФСС" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС") Тогда
		ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСС(ИсточникСсылка);
		
	ИначеЕсли КонтролирующийОрган = "ФСРАР" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР") Тогда
		ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСРАР(ИсточникСсылка);
		
	ИначеЕсли КонтролирующийОрган = "РПН" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН") Тогда
		ПоказатьПротоколОбработкиПоСсылкеИсточникаДляРПН(ИсточникСсылка);
		
	ИначеЕсли КонтролирующийОрган = "ФТС" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС") Тогда
		ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФТС(ИсточникСсылка);
		
	ИначеЕсли КонтролирующийОрган = "УдалитьБанкРоссии" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.УдалитьБанкРоссии") Тогда
		МодульУдалитьДокументооборотСБанкомРоссииКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("УдалитьДокументооборотСБанкомРоссииКлиент");
		МодульУдалитьДокументооборотСБанкомРоссииКлиент.ПоказатьПротокол(ИсточникСсылка);
		
	ИначеЕсли КонтролирующийОрган = "Минобороны" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.Минобороны") Тогда
		МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
		МодульДокументооборотСМинобороныКлиент.ПоказатьПротокол(ИсточникСсылка);
		
	ИначеЕсли КонтролирующийОрган = "РЭЦ" ИЛИ КонтролирующийОрган = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РЭЦ") Тогда
		МодульДокументооборотСРЭЦКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСРЭЦКлиент");
		МодульДокументооборотСРЭЦКлиент.ПоказатьПротокол(ИсточникСсылка);
		
	Иначе
		ПоказатьДокументыЦикловОбмена(ИсточникСсылка, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСС(ИсточникСсылка, ПечатьВозможна = Истина) Экспорт
	
	ВАрхиве = ОбъектВАрхиве(ИсточникСсылка, "Протокол");
	Если ВАрхиве Тогда 
		ПоказатьУведомлениеАрхивныхФайлов(, 16, 0, Истина);
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ИсточникСсылка",      ИсточникСсылка);
	ПараметрыФормы.Вставить("ПечатьВозможна",      ПечатьВозможна);
	ПараметрыФормы.Вставить("КонтролирующийОрган", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
		
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПротоколОбработкиHTML", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура РазобратьСоставляющую(СтрСоставляющая, Результат)
	
	// расщепляем текущую составляющую и добавляем в структуру Результат
	Для ВнутрИнд = 1 По СтрДлина(СтрСоставляющая) Цикл
		Если Сред(СтрСоставляющая, ВнутрИнд, 1) = "=" Тогда
			ДоРазделителя = СокрЛП(Лев(СтрСоставляющая, ВнутрИнд - 1));
			ПослеРазделителя = СокрЛП(Сред(СтрСоставляющая, ВнутрИнд + 1));
			Если НЕ ПустаяСтрока(ДоРазделителя) И НЕ ПустаяСтрока(ПослеРазделителя) Тогда
				Результат.Вставить(ДоРазделителя, ПослеРазделителя);
			КонецЕсли;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Функция РазобратьСтрокуСубъекта(СтрокаСубъект) Экспорт
	
	Результат = Новый Соответствие;
	ПоследнийРазделитель = 0;
	
	ДлинаСтрокиСубъект = СтрДлина(СтрокаСубъект);
	Для Инд = 1 По ДлинаСтрокиСубъект Цикл
		ТекСимвол = Сред(СтрокаСубъект, Инд, 1);
		Если ТекСимвол = "," Тогда
			ТекСоставляющая = Сред(СтрокаСубъект, ПоследнийРазделитель + 1, Инд - (ПоследнийРазделитель + 1));
			ПоследнийРазделитель = Инд;
			РазобратьСоставляющую(ТекСоставляющая, Результат);
		КонецЕсли;
	КонецЦикла;
	
	Если ПоследнийРазделитель <> ДлинаСтрокиСубъект Тогда
		РазобратьСоставляющую(СокрЛП(Сред(СтрокаСубъект, ПоследнийРазделитель + 1)), Результат);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьФайлОтправкиВФССНаСервере(
		КороткоеИмяФайлаВыгрузки,
		СтрокаBase64ФайлаВыгрузки,
		ЭтоАдреса = Истина,
		Адрес = Неопределено,
		ТипОтправляемогоДокумента = Неопределено,
		ВидОтправляемогоДокумента = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СформироватьФайлОтправкиВФССССервера(
		КороткоеИмяФайлаВыгрузки,
		СтрокаBase64ФайлаВыгрузки,
		ЭтоАдреса,
		Адрес,
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ОбновитьРезультатКонкретнойОтправкиФСССНаСервере(
		ОтправкаСсылка,
		ПолученРезультатОтСервераФСС,
		НастройкиССервера,
		АдресЗашифрованногоЗапросаSOAP = Неопределено,
		АдресЗашифрованногоОтветаSOAP = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДО.ОбновитьРезультатКонкретнойОтправкиФССССервера(
		ОтправкаСсылка,
		ПолученРезультатОтСервераФСС,
		НастройкиССервера,
		АдресЗашифрованногоЗапросаSOAP,
		АдресЗашифрованногоОтветаSOAP);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СохранитьПротоколПриОперацииСОКВЭДИлиПолучитьОтправкуСЭДОНаСервере(
		ОтправкаСсылка,
		ОтчетСсылка,
		ОрганизацияСсылка,
		ПараметрыПротокола)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СохранитьПротоколПриОперацииСОКВЭДИлиПолучитьОтправкуСЭДО(
		ОтправкаСсылка,
		ОтчетСсылка,
		ОрганизацияСсылка,
		ПараметрыПротокола);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВФССНаСервере(
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		ОтчетСсылка,
		ПовторятьСоединение,
		ПараметрыОтправкиНаСервере)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьВФССССервера(
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		ОтчетСсылка,
		ПовторятьСоединение,
		ПараметрыОтправкиНаСервере);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбработатьРезультатОтправкиРеестраЭЛННаСервере(
		ОтчетСсылка,
		ОрганизацияСсылка,
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбработатьРезультатОтправкиРеестраЭЛН(
		ОтчетСсылка,
		ОрганизацияСсылка,
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбработатьРезультатОтправкиПриОперацииСОКВЭДНаСервере(
		ОтчетСсылка,
		ОрганизацияСсылка,
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбработатьРезультатОтправкиПриОперацииСОКВЭД(
		ОтчетСсылка,
		ОрганизацияСсылка,
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбработатьРезультатОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникамНаСервере(
		ОтчетСсылка,
		ОрганизацияСсылка,
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбработатьРезультатОтправкиРеестраСтимулирующихВыплатМедицинскимИСоциальнымРаботникам(
		ОтчетСсылка,
		ОрганизацияСсылка,
		МестоХраненияКлюча,
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ПараметрыРезультата);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФССНаСервере(ОрганизацияСсылка) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьОтпечаткиСертификатовИзНастроекОрганизацииДляФСС(ОрганизацияСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ПолучитьСвойстваОрганизацииДляФССНаСервере(
		Организация,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,
		УчетнаяЗаписьДокументооборота = Неопределено,
		ПараметрыФорматов = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДО.ПолучитьСвойстваОрганизацииДляФСС(
		Организация,
		НастроенОбменВУниверсальномФормате,
		ОрганизацияИспользуетОбменСФСС,
		ОрганизацияИспользуетАвтонастройкуПоУчетнойЗаписи,
		УчетнаяЗаписьДокументооборота,
		ПараметрыФорматов);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
		Владелец,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		ОпределеннаяКодировка = Неопределено,
		ПреобразоватьИзКодировки = Неопределено,
		Приложения = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДО.ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодировать(
		Владелец,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		ОпределеннаяКодировка,
		ПреобразоватьИзКодировки);
	Если ТипЗнч(Приложения) = Тип("Массив") Тогда
		ПриложенияКОтчету = ПолучитьПриложенияКОтчету(Владелец);
		ОбщегоНазначенияКлиентСервер.ДополнитьМассив(Приложения, ПриложенияКОтчету);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ПолучитьСвойстваФайлаВыгрузкиНаСервере(
		ОбъектСсылка,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		КодировкаФайлаВыгрузки = Неопределено,
		СвойстваФайлаВыгрузки = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДО.ПолучитьСвойстваФайлаВыгрузки(
		ОбъектСсылка,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		КодировкаФайлаВыгрузки,
		СвойстваФайлаВыгрузки);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПоляНаСервереДляЗаписиОтправкиВФССНаСервере(ОбъектСсылка) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПоляНаСервереДляЗаписиОтправкиВФСС(ОбъектСсылка);
	
КонецФункции

&НаКлиенте
Процедура ПоказатьОтветыНаТребованияФСС(Ответы) Экспорт
	
	Если Ответы.Количество() > 1 Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Ответы", Ответы);

		ОткрытьФорму(ПутьКОбъекту + ".Форма.ОтветыНаТребованияФСС", ДополнительныеПараметры,, Новый УникальныйИдентификатор);
		
	ИначеЕсли Ответы.Количество() = 1 Тогда
		
		ПоказатьЗначение(, Ответы[0]);
		
	Иначе
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Ответы отсутствуют'"));
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДополнитьСтруктуруПараметровПолученияСообщений(СтруктураСвойств, ДополнительныеПараметры, ОтправкаСсылка)
	
	Если ДополнительныеПараметры = Неопределено Тогда
		ТипСообщенияОтвета = Неопределено;
	Иначе
		ДополнительныеПараметрыОповещения = ДополнительныеПараметры.ВыполняемоеОповещение.ДополнительныеПараметры;
		
		ТипСообщенияОтвета = ?(ДополнительныеПараметрыОповещения.Свойство("ТипСообщенияОтвета"),
			ДополнительныеПараметрыОповещения.ТипСообщенияОтвета, Неопределено);
	КонецЕсли;
	
	СтруктураСвойств.Вставить("ТипСообщенияОтвета", ТипСообщенияОтвета);
	
КонецПроцедуры

///////////////////////////////////////////////////////////////////////////////
// Заявление абонента оператора электронного документооборот

&НаКлиенте
Процедура ОткрытьФСРАРСтраницуУЦНажатие(Спецоператор) Экспорт 
	Если Спецоператор = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Сначала укажите удостоверяющий центр!'"));
	Иначе
		СтруктураПараметра = Новый Структура("ФСРАРАдрес");
		АдресСтраницы = ПолучитьПараметрСпецоператораКлиент(Спецоператор,СтруктураПараметра).ФСРАРАдрес;
		ПерейтиПоНавигационнойСсылке(АдресСтраницы);
	КонецЕсли;
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// ДОКУМЕНТООБОРОТ С ФСРАР

// ОтправкаСсылка ссылается на загруженный объект
&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАР(Форма, ОтправкаСсылка, ВыполняемоеОповещение = Неопределено, ПараметрыОбновленияОтправки = Неопределено, ЭтоВызовИзСписка = Ложь) Экспорт
	
	ФедеральныйПортал = Неопределено;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Форма",                Форма);
	ДополнительныеПараметры.Вставить("ОтправкаСсылка",       ОтправкаСсылка);
	ДополнительныеПараметры.Вставить("ФедеральныйПортал",    ФедеральныйПортал);
	ДополнительныеПараметры.Вставить("ЗначениеCookie",       ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.ЗначениеCookie, ""));
	ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",     ЭтоВызовИзСписка);
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеПолученияСертификатовИПараметровОбменаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	СертификатыИПараметрыОбменаОрганизацииДляФСРАР(
		Оповещение,
		Неопределено,
		ФедеральныйПортал,
		Неопределено,
		Истина,
		Истина,
		ОтправкаСсылка,
		Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеПолученияСертификатовИПараметровОбменаЗавершение(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение               = ДополнительныеПараметры.ВыполняемоеОповещение;
	Форма                               = ДополнительныеПараметры.Форма;
	ОрганизацияСсылка                   = СертификатыИПараметрыОбмена.ОрганизацияИзОтправки;
	РеквизитыУчетнойЗаписи              = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	ИдентификаторДокументооборота       = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	ТелефонМобильныйДляАвторизации      = РеквизитыУчетнойЗаписи.ТелефонМобильныйДляАвторизации;
	
	УчетнаяЗапись = Новый Структура("ТелефонМобильныйДляАвторизации, ИдентификаторДокументооборота", ТелефонМобильныйДляАвторизации, ИдентификаторДокументооборота);
	
	ПараметрыФормированияПакета = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	СхемаСдачиОтчетности        = СертификатыИПараметрыОбмена.СвойстваОтправки.СхемаСдачиОтчетности;
	
	Если НЕ ЗначениеЗаполнено(СхемаСдачиОтчетности) Тогда
		СхемаСдачиОтчетности = СертификатыИПараметрыОбмена.СхемаСдачиОтчетности;
	КонецЕсли;
	
	Если НЕ ПараметрыФормированияПакета.ИспользоватьОбмен Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Использование документооборота с Росалкогольтабакконтролем не включено для организации!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФСРАРПослеИнициализации", ЭтотОбъект, ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПослеИнициализации(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение               = ДополнительныеПараметры.ВыполняемоеОповещение;
	Форма                               = ДополнительныеПараметры.Форма;
	ОтправкаСсылка                      = ДополнительныеПараметры.ОтправкаСсылка;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	ЗначениеCookie                      = ДополнительныеПараметры.ЗначениеCookie;
	ЭтоВызовИзСписка                    = ДополнительныеПараметры.ЭтоВызовИзСписка;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	Крипт                               = Неопределено;
	ОрганизацияСсылка                   = СертификатыИПараметрыОбмена.ОрганизацияИзОтправки;
	
	ПараметрыФормированияПакета = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	ИнформацияОРегионе          = СертификатыИПараметрыОбмена.ИнформацияОРегионе;
	ЭтоРегламентированныйОтчет  = СертификатыИПараметрыОбмена.ЭтоРегламентированныйОтчет;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	КодИНаименованиеРегиона = ПараметрыФормированияПакета.КодРегиона + ?(ЗначениеЗаполнено(ИнформацияОРегионе.НаименованиеРегиона), " - ", "") + ИнформацияОРегионе.НаименованиеРегиона;
	ФормаДлительнаяОперация = Неопределено;
	
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64 = Неопределено;
	
	АвторизацияПроизведена = ЗначениеЗаполнено(ЗначениеCookie);
	
	ПараметрыОтправки = Новый Структура();
	ПараметрыОтправки.Вставить("Организация",                  ОрганизацияСсылка);
	ПараметрыОтправки.Вставить("ИНН",                          Неопределено);
	ПараметрыОтправки.Вставить("ФедеральныйПортал",            ФедеральныйПортал);
	ПараметрыОтправки.Вставить("КодРегиона",                   ПараметрыФормированияПакета.КодРегиона);
	ПараметрыОтправки.Вставить("КодИНаименованиеРегиона",      КодИНаименованиеРегиона);
	ПараметрыОтправки.Вставить("СсылкаНаПорталОнлайнПроверки", ИнформацияОРегионе.СсылкаНаПортал);
	ПараметрыОтправки.Вставить("СхемаСдачиОтчетности",         СхемаСдачиОтчетности);
	ПараметрыОтправки.Вставить("ПоляДляЗаписиОтправки",        Неопределено);

	ПараметрыСоединения = Новый Структура();
	ПараметрыСоединения.Вставить("Пароль",                         ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ);
	ПараметрыСоединения.Вставить("СохранитьПароль",                ЗначениеЗаполнено(ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ));
	ПараметрыСоединения.Вставить("СохраненныйПароль",              ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ);
	ПараметрыСоединения.Вставить("СоединениеHTTP",                 Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie",                 ЗначениеCookie);
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиНаСервере", "");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером",         "");
	ПараметрыСоединения.Вставить("ПовторятьСоединение",            Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля",          Истина);
	ПараметрыСоединения.Вставить("ТребуетсяРегистрация",           Неопределено);
	ПараметрыСоединения.Вставить("ТребуетсяПовторнаяАвторизация",  Неопределено);
	ПараметрыСоединения.Вставить("СообщенияПользователю",          Новый Массив);
	
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале = ((СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы")
		ИЛИ СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы"))
		И СертификатыИПараметрыОбмена.СвойстваОтправки.ЗаверенныйПакетОтправлен = Истина);
		
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал = ((СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы")
		ИЛИ СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы"))
		И НЕ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале И СертификатыИПараметрыОбмена.СвойстваОтправки.СтатусОтправкиНаРегиональныйСервер = ПредопределенноеЗначение("Перечисление.СтатусыОтправки.Сдан"));
	
	ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервере(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		ЭтоВызовИзСписка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервереПослеФормированияПодписанногоПакетаДляПортала(ПодписанныйПакетДляАвторизации, ДополнительныеПараметры) Экспорт
	
	Форма                                                              = ДополнительныеПараметры.Форма;
	ОтправкаСсылка                                                     = ДополнительныеПараметры.ОтправкаСсылка;
	ВыполняемоеОповещение                                              = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал   = ДополнительныеПараметры.ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал;
	МестоХраненияКлюча												   = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФормаДлительнаяОперация                                            = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СертификатыИПараметрыОбмена                                        = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	СхемаСдачиОтчетности                                               = ДополнительныеПараметры.СхемаСдачиОтчетности;
	Крипт                                                              = ДополнительныеПараметры.Крипт;
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64   = ДополнительныеПараметры.ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64;
	ПараметрыСоединения                                                = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                                                  = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыФормированияПакета                                        = ДополнительныеПараметры.ПараметрыФормированияПакета;
	УчетнаяЗапись                                                      = ДополнительныеПараметры.УчетнаяЗапись;
	ЭтоРегламентированныйОтчет                                         = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале = ДополнительныеПараметры.ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале;
	АвторизацияПроизведена                                             = ДополнительныеПараметры.АвторизацияПроизведена;
	АвторизацияПодготовлена                                            = ДополнительныеПараметры.АвторизацияПодготовлена;
	ЭтоВызовИзСписка                                                   = ДополнительныеПараметры.ЭтоВызовИзСписка;
	ПодписанныйПакетДляАвторизацииТребуется                            = ДополнительныеПараметры.ПодписанныйПакетДляАвторизацииТребуется;
	
	Если ПодписанныйПакетДляАвторизацииТребуется И НЕ ЗначениеЗаполнено(ПодписанныйПакетДляАвторизации) Тогда
		АвторизацияПодготовлена = Ложь;
	КонецЕсли;
	
	ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПодготовкиАвторизации(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		ПодписанныйПакетДляАвторизации,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		АвторизацияПодготовлена,
		ЭтоВызовИзСписка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервереЗавершение(ЗначениеCAPTCHA, ДополнительныеПараметры) Экспорт
	
	Форма                                                              = ДополнительныеПараметры.Форма;
	ОтправкаСсылка                                                     = ДополнительныеПараметры.ОтправкаСсылка;
	ВыполняемоеОповещение                                              = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал   = ДополнительныеПараметры.ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал;
	МестоХраненияКлюча												   = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФормаДлительнаяОперация                                            = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СертификатыИПараметрыОбмена                                        = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	СхемаСдачиОтчетности                                               = ДополнительныеПараметры.СхемаСдачиОтчетности;
	ПодписанныйПакетДляАвторизации                                     = ДополнительныеПараметры.ПодписанныйПакетДляАвторизации;
	Крипт                                                              = ДополнительныеПараметры.Крипт;
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64   = ДополнительныеПараметры.ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64;
	ПараметрыСоединения                                                = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                                                  = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыФормированияПакета                                        = ДополнительныеПараметры.ПараметрыФормированияПакета;
	УчетнаяЗапись                                                      = ДополнительныеПараметры.УчетнаяЗапись;
	ЭтоРегламентированныйОтчет                                         = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале = ДополнительныеПараметры.ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале;
	СлучайныйКлюч                                                      = ДополнительныеПараметры.СлучайныйКлюч;
	ПодписываемыеДанные                                                = ДополнительныеПараметры.ПодписываемыеДанные;
	АвторизацияПроизведена                                             = ДополнительныеПараметры.АвторизацияПроизведена;
	АвторизацияПодготовлена                                            = ДополнительныеПараметры.АвторизацияПодготовлена;
	ЭтоВызовИзСписка                                                   = ДополнительныеПараметры.ЭтоВызовИзСписка;
	ПодписанныйПакетДляАвторизацииТребуется                            = ДополнительныеПараметры.ПодписанныйПакетДляАвторизацииТребуется;
	
	Оповещение = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервереПослеФормированияПодписанногоПакетаДляПорталаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	СформироватьПодписанныйПакетДляПорталаФСРАР(
		МестоХраненияКлюча,
		УчетнаяЗапись,
		ПодписываемыеДанные,
		ПараметрыФормированияПакета.СертификатАбонента,
		Оповещение,
		Крипт,
		СлучайныйКлюч,
		ЗначениеCAPTCHA);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервереПослеФормированияПодписанногоПакетаДляПорталаЗавершение(ПодписанныйПакетДляАвторизации, ДополнительныеПараметры) Экспорт
	
	Форма                                                              = ДополнительныеПараметры.Форма;
	ОтправкаСсылка                                                     = ДополнительныеПараметры.ОтправкаСсылка;
	ВыполняемоеОповещение                                              = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал   = ДополнительныеПараметры.ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал;
	МестоХраненияКлюча												   = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФормаДлительнаяОперация                                            = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СертификатыИПараметрыОбмена                                        = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	СхемаСдачиОтчетности                                               = ДополнительныеПараметры.СхемаСдачиОтчетности;
	Крипт                                                              = ДополнительныеПараметры.Крипт;
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64   = ДополнительныеПараметры.ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64;
	ПараметрыСоединения                                                = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                                                  = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыФормированияПакета                                        = ДополнительныеПараметры.ПараметрыФормированияПакета;
	УчетнаяЗапись                                                      = ДополнительныеПараметры.УчетнаяЗапись;
	ЭтоРегламентированныйОтчет                                         = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале = ДополнительныеПараметры.ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале;
	АвторизацияПроизведена                                             = ДополнительныеПараметры.АвторизацияПроизведена;
	АвторизацияПодготовлена                                            = ДополнительныеПараметры.АвторизацияПодготовлена;
	ЭтоВызовИзСписка                                                   = ДополнительныеПараметры.ЭтоВызовИзСписка;
	ПодписанныйПакетДляАвторизацииТребуется                            = ДополнительныеПараметры.ПодписанныйПакетДляАвторизацииТребуется;
	
	Если ПодписанныйПакетДляАвторизацииТребуется И НЕ ЗначениеЗаполнено(ПодписанныйПакетДляАвторизации) Тогда
		АвторизацияПодготовлена = Ложь;
	КонецЕсли;
	
	ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПодготовкиАвторизации(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		ПодписанныйПакетДляАвторизации,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		АвторизацияПодготовлена,
		ЭтоВызовИзСписка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПодготовкиАвторизации(
	Форма,
	ОтправкаСсылка,
	ВыполняемоеОповещение,
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
	МестоХраненияКлюча,
	ФормаДлительнаяОперация,
	СертификатыИПараметрыОбмена,
	СхемаСдачиОтчетности,
	ПодписанныйПакетДляАвторизации,
	Крипт,
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
	ПараметрыСоединения,
	ПараметрыОтправки,
	ПараметрыФормированияПакета,
	УчетнаяЗапись,
	ЭтоРегламентированныйОтчет,
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
	АвторизацияПроизведена,
	АвторизацияПодготовлена,
	ЭтоВызовИзСписка)
	
	Если АвторизацияПодготовлена Тогда
		
		ПерваяПопыткаАвторизации = Истина;
		ОперацияУспешна = Ложь;
		ПараметрыСоединения.ПовторятьЗапросПароля = Истина;
		
		ОбновитьРезультатКонкретнойОтправкиФСРАРВводПароля(
			ОперацияУспешна,
			ПерваяПопыткаАвторизации,
			Форма,
			ОтправкаСсылка,
			ВыполняемоеОповещение,
			ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
			МестоХраненияКлюча,
			ФормаДлительнаяОперация,
			СертификатыИПараметрыОбмена,
			СхемаСдачиОтчетности,
			Крипт,
			ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ПараметрыФормированияПакета,
			УчетнаяЗапись,
			ЭтоРегламентированныйОтчет,
			ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
			ПодписанныйПакетДляАвторизации,
			АвторизацияПроизведена,
			ЭтоВызовИзСписка);
		Возврат;
		
	КонецЕсли;
	
	ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПослеВводаПароля(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		ЭтоВызовИзСписка);
		
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРВводПароля(
	ОперацияУспешна,
	ПерваяПопыткаАвторизации,
	Форма,
	ОтправкаСсылка,
	ВыполняемоеОповещение,
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
	МестоХраненияКлюча,
	ФормаДлительнаяОперация,
	СертификатыИПараметрыОбмена,
	СхемаСдачиОтчетности,
	Крипт,
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
	ПараметрыСоединения,
	ПараметрыОтправки,
	ПараметрыФормированияПакета,
	УчетнаяЗапись,
	ЭтоРегламентированныйОтчет,
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
	ПодписанныйПакетДляАвторизации,
	АвторизацияПроизведена,
	ЭтоВызовИзСписка);
	
	// при успешном обращении на сервер ФСРАР отправка обновится в ИБ
	ОперацияУспешна = ОбновитьРезультатКонкретнойОтправкиФСРАРСНаСервере(
		ОтправкаСсылка,
		Истина,
		ПодписанныйПакетДляАвторизации,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ЭтоВызовИзСписка);
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);

	ПерваяПопыткаАвторизации = Ложь;
	
	Если НЕ ОперацияУспешна И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
		
		ОбновитьРезультатКонкретнойОтправкиФСРАРВводПароля(
			ОперацияУспешна,
			ПерваяПопыткаАвторизации,
			Форма,
			ОтправкаСсылка,
			ВыполняемоеОповещение,
			ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
			МестоХраненияКлюча,
			ФормаДлительнаяОперация,
			СертификатыИПараметрыОбмена,
			СхемаСдачиОтчетности,
			Крипт,
			ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ПараметрыФормированияПакета,
			УчетнаяЗапись,
			ЭтоРегламентированныйОтчет,
			ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
			ПодписанныйПакетДляАвторизации,
			АвторизацияПроизведена,
			ЭтоВызовИзСписка);
		Возврат;
		
	КонецЕсли;
	
	ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПослеВводаПароля(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		ЭтоВызовИзСписка);
		
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПослеВводаПароля(
		Форма,
		ОтправкаСсылка,
		ВыполняемоеОповещение,
		ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
		МестоХраненияКлюча,
		ФормаДлительнаяОперация,
		СертификатыИПараметрыОбмена,
		СхемаСдачиОтчетности,
		Крипт,
		ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ПараметрыФормированияПакета,
		УчетнаяЗапись,
		ЭтоРегламентированныйОтчет,
		ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
		АвторизацияПроизведена,
		ЭтоВызовИзСписка)

	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// возможно, проблемы с доступом в интернет
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Форма",                                                              Форма);
		ДополнительныеПараметры.Вставить("ОтправкаСсылка",                                                     ОтправкаСсылка);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",                                              ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал",   ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",                                            ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",                                        СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                                               СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("Крипт",                                                              Крипт);
		ДополнительныеПараметры.Вставить("ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64",   ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                                                ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                                                  ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",                                        ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись",                                                      УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",                                         ЭтоРегламентированныйОтчет);
		ДополнительныеПараметры.Вставить("ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале", ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале);
		ДополнительныеПараметры.Вставить("АвторизацияПроизведена",                                             АвторизацияПроизведена);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",                                                   ЭтоВызовИзСписка);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПослеВводаПароляЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		ТекстСообщения = НСтр("ru = 'Не удалось обратиться к серверу Росалкогольтабакконтроля'");
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			ТекстСообщения,
			ОписаниеОповещения);
			
		Возврат;
		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФСРАРПослеПослеВводаПароляЗавершение(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	Форма                                                              = ДополнительныеПараметры.Форма;
	ОтправкаСсылка                                                     = ДополнительныеПараметры.ОтправкаСсылка;
	ВыполняемоеОповещение                                              = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал   = ДополнительныеПараметры.ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал;
	МестоХраненияКлюча												   = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФормаДлительнаяОперация                                            = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СертификатыИПараметрыОбмена                                        = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	СхемаСдачиОтчетности                                               = ДополнительныеПараметры.СхемаСдачиОтчетности;
	Крипт                                                              = ДополнительныеПараметры.Крипт;
	ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64   = ДополнительныеПараметры.ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64;
	ПараметрыСоединения                                                = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                                                  = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыФормированияПакета                                        = ДополнительныеПараметры.ПараметрыФормированияПакета;
	УчетнаяЗапись                                                      = ДополнительныеПараметры.УчетнаяЗапись;
	ЭтоРегламентированныйОтчет                                         = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале = ДополнительныеПараметры.ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале;
	АвторизацияПроизведена                                             = ДополнительныеПараметры.АвторизацияПроизведена;
	ЭтоВызовИзСписка                                                   = ДополнительныеПараметры.ЭтоВызовИзСписка;
	
	ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// Рекурсивный вызов по условию (замена цикла)	
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ОбновитьРезультатКонкретнойОтправкиФСРАРПодписаниеНаРегиональномСервере(
			Форма,
			ОтправкаСсылка,
			ВыполняемоеОповещение,
			ПолученыРезультатыРегиональногоПередОтправкойНаФедеральныйПортал,
			МестоХраненияКлюча,
			ФормаДлительнаяОперация,
			СертификатыИПараметрыОбмена,
			СхемаСдачиОтчетности,
			Крипт,
			ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ПараметрыФормированияПакета,
			УчетнаяЗапись,
			ЭтоРегламентированныйОтчет,
			ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
			АвторизацияПроизведена,
			ЭтоВызовИзСписка);
			
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		
	КонецЕсли;
																	
КонецПроцедуры
// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаКлиенте
Процедура ОткрытьПротоколОтправкиФСРАР(Форма) Экспорт
	
	ОтчетСсылка = ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	
	Если ОтчетСсылка = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСРАР(ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляФСРАР(
		ВыполняемоеОповещение,
		ОрганизацияСсылка,
		ФедеральныйПортал,
		КодРегиона,
		ПолучитьОсновныеСвойстваПоследнейОтправки,
		ПолучитьИнформациюОРегионе = Ложь,
		ОтправкаСсылка = Неопределено,
		ОтчетСсылка = Неопределено,
		ОпределитьНастроенностьОбменаВУниверсальномФормате = Ложь)
	
	РезультатВыполнения = Новый Структура();
	РезультатВыполнения.Вставить("ПараметрыФормированияПакета",        Неопределено);
	РезультатВыполнения.Вставить("ИнформацияОРегионе",                 Неопределено);
	РезультатВыполнения.Вставить("СхемаСдачиОтчетности",               Неопределено);
	РезультатВыполнения.Вставить("ОрганизацияИзОтправки",              Неопределено);
	РезультатВыполнения.Вставить("СвойстваОтправки",                   Неопределено);
	РезультатВыполнения.Вставить("Крипт",                              Неопределено);
	РезультатВыполнения.Вставить("НастроенОбменВУниверсальномФормате", Неопределено);
	РезультатВыполнения.Вставить("ПравоИзмененияУчетнойЗаписи",        Неопределено);
	РезультатВыполнения.Вставить("ЭтоРегламентированныйОтчет",         Неопределено);
	
	СвойстваОбмена = СвойстваОбменаОрганизацииНаСервере(
		ОрганизацияСсылка,
		ФедеральныйПортал = Неопределено,
		ОпределитьНастроенностьОбменаВУниверсальномФормате,
		ПолучитьОсновныеСвойстваПоследнейОтправки,
		ПолучитьИнформациюОРегионе,
		ОтправкаСсылка,
		ОтчетСсылка);
		
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СвойстваОбмена);
	Если СвойстваОбмена.ОтправкаЧерезФедеральныйПортал <> Неопределено Тогда
		ФедеральныйПортал = СвойстваОбмена.ОтправкаЧерезФедеральныйПортал;
		ВыполняемоеОповещение.ДополнительныеПараметры.Вставить("ФедеральныйПортал", ФедеральныйПортал);
	КонецЕсли;
	
	РезультатВыполнения.ИнформацияОРегионе                 = СвойстваОбмена.ИнформацияОРегионе;
	РезультатВыполнения.ОрганизацияИзОтправки              = СвойстваОбмена.ОрганизацияИзОтправки;
	РезультатВыполнения.СвойстваОтправки                   = СвойстваОбмена.СвойстваОтправки;
	РезультатВыполнения.НастроенОбменВУниверсальномФормате = СвойстваОбмена.НастроенОбменВУниверсальномФормате;
	РезультатВыполнения.ПравоИзмененияУчетнойЗаписи        = СвойстваОбмена.ПравоИзмененияУчетнойЗаписи;
	РезультатВыполнения.ЭтоРегламентированныйОтчет         = СвойстваОбмена.ЭтоРегламентированныйОтчет;

	РезультатВыполнения.ПараметрыФормированияПакета = Новый Структура();
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("ИспользоватьОбмен",         Ложь);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("СертификатАбонента",        Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("СертификатСубъектаРФ",      Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("СертификатФСРАР",           Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("КодРегиона",                "");
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("ИспользоватьАвтонастройку", Ложь);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("ПарольПорталаСубъектаРФ",   "");
	
	Если СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьОбмен = СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен;
		РезультатВыполнения.ПараметрыФормированияПакета.КодРегиона = ?(ЗначениеЗаполнено(КодРегиона), КодРегиона, СвойстваОбмена.НастройкиОбмена.КодРегиона);
		РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьАвтонастройку = СвойстваОбмена.НастройкиОбмена.ИспользоватьАвтонастройку;
		РезультатВыполнения.ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ = СвойстваОбмена.НастройкиОбмена.ПарольПорталаСубъектаРФ;
	КонецЕсли;
	
	РезультатВыполнения.СхемаСдачиОтчетности = СхемаСдачиОтчетностиФСРАР(
		ФедеральныйПортал,
		СвойстваОбмена.ИнформацияОРегионе.ТипПортала,
		РезультатВыполнения.ПараметрыФормированияПакета.КодРегиона);
	
	Если НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток);
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатСубъектаРФ = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СвойстваОбмена.НастройкиОбмена.СертификатСубъектаРФОтпечаток);
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатФСРАР = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СвойстваОбмена.НастройкиОбмена.СертификатФСРАРОтпечаток);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ПолучитьСвойстваОблачногоСертификата(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток, "MY");
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатСубъектаРФ = ПолучитьСвойстваОблачногоСертификата(СвойстваОбмена.НастройкиОбмена.СертификатСубъектаРФОтпечаток);
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатФСРАР = ПолучитьСвойстваОблачногоСертификата(СвойстваОбмена.НастройкиОбмена.СертификатФСРАРОтпечаток);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, РезультатВыполнения.ПараметрыФормированияПакета.СертификатСубъектаРФ);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, РезультатВыполнения.ПараметрыФормированияПакета.СертификатФСРАР);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	Иначе
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("СвойстваОбмена",       СвойстваОбмена);
		ДополнительныеПараметры.Вставить("РезультатВыполнения",  РезультатВыполнения);
		
		Оповещение = Новый ОписаниеОповещения(
			"СертификатыИПараметрыОбменаОрганизацииДляФСРАРПослеПолученияСвойствСертификатаАбонентаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(
			Оповещение,
			СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток,
			"MY",
			,
			Ложь);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляФСРАРПослеПолученияСвойствСертификатаАбонентаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СвойстваОбмена = ДополнительныеПараметры.СвойстваОбмена;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, СвойстваОбмена, РезультатВыполнения", ВыполняемоеОповещение, СвойстваОбмена, РезультатВыполнения);
	Оповещение = Новый ОписаниеОповещения("СертификатыИПараметрыОбменаОрганизацииДляФСРАРПослеПолученияСвойствСертификатаСубъектаРФЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СвойстваОбмена.НастройкиОбмена.СертификатСубъектаРФОтпечаток, "AddressBook", , Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляФСРАРПослеПолученияСвойствСертификатаСубъектаРФЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СвойстваОбмена = ДополнительныеПараметры.СвойстваОбмена;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатСубъектаРФ = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, РезультатВыполнения", ВыполняемоеОповещение, РезультатВыполнения);
	Оповещение = Новый ОписаниеОповещения("СертификатыИПараметрыОбменаОрганизацииДляФСРАРПослеПолученияСвойствСертификатаФСРАРЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СвойстваОбмена.НастройкиОбмена.СертификатФСРАРОтпечаток, "AddressBook", , Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляФСРАРПослеПолученияСвойствСертификатаФСРАРЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатФСРАР = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Функция СертификатыГодятсяДляПодписанияОтчетностиФСРАР(Сертификаты)
	
	ТекДата = ТекущаяДатаНаСервере();
	ПризнакОшибки = Ложь;
	
	Для Каждого Сертификат Из Сертификаты Цикл
		
		Серт	= Сертификат.Значение;
		Предст	= Сертификат.Представление;
		Если Серт = Неопределено Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: сертификат не выбран в настройках сдачи отчетности в Росалкогольтабакконтроль!'"), Предст));
			Продолжить;
		КонецЕсли;
		
		Если ТекДата < Серт.ДействителенС Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: период действия сертификата еще не наступил (%2)!'"), Предст, Строка(Серт.ДействителенС)));
		КонецЕсли;
		
		Если ТекДата > Серт.ДействителенПо Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: период действия сертификата истек (%2)!'"), Предст, Строка(Серт.ДействителенПо)));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат (НЕ ПризнакОшибки);
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАР(
		Форма,
		ВыполняемоеОповещение,
		ФедеральныйПортал = Ложь,
		ТекстФайлаВыгрузки = Неопределено,
		ИмяФайлаВыгрузки = Неопределено,
		КодРегионаВФормеПриОтправке = Неопределено,
		СертификатыИПараметрыОбмена = Неопределено,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь,
		Знач ОтчетСсылка = Неопределено,
		Знач ОрганизацияСсылка = Неопределено,
		Знач АдресГотовыхДанных = Неопределено,
		Знач ИмяФайлаГотовыхДанных = Неопределено) Экспорт
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	Если ОтчетСсылка = Неопределено Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Если ТекстФайлаВыгрузки = Неопределено И ИмяФайлаВыгрузки = Неопределено Тогда
		РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВФСРАР(
			ОтчетСсылка,
			Форма,
			ЭтоОтправкаИзФормыОтчетность,
			АдресГотовыхДанных,
			ИмяФайлаГотовыхДанных);
		
		Если РезультатВыгрузки = Неопределено Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				НСтр("ru = 'Не удалось выгрузить отчет.'"));
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
		
		ТекстФайлаВыгрузки = РезультатВыгрузки.ТекстФайлаВыгрузки;
		ИмяФайлаВыгрузки   = РезультатВыгрузки.ИмяФайлаВыгрузки;
	КонецЕсли;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияСсылка = ДокументооборотСФСРАРКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи              = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	УчетнаяЗапись                       = РеквизитыУчетнойЗаписи.Ссылка;
	Идентификатор                       = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",               ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Идентификатор",                       Идентификатор);
	ДополнительныеПараметры.Вставить("КодРегионаВФормеПриОтправке",         КодРегионаВФормеПриОтправке);
	ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка",                   ОрганизацияСсылка);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет",                 ТипЗнчСсылкаНаОтчет);
	ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
	ДополнительныеПараметры.Вставить("Форма",                               Форма);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность",        ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки",        			ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки",        			ИмяФайлаВыгрузки);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Если АдресГотовыхДанных <> Неопределено Тогда
		ДополнительныеПараметры.Вставить("АдресГотовыхДанных", 				АдресГотовыхДанных);
		ДополнительныеПараметры.Вставить("ИмяФайлаГотовыхДанных", 			ИмяФайлаГотовыхДанных);
	КонецЕсли;

	ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеИнициализации", ЭтотОбъект, ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеИнициализации(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение       = ДополнительныеПараметры.ВыполняемоеОповещение;
	КодРегионаВФормеПриОтправке = ДополнительныеПараметры.КодРегионаВФормеПриОтправке;
	ОрганизацияСсылка           = ДополнительныеПараметры.ОрганизацияСсылка;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФедеральныйПортал           = ДополнительныеПараметры.ФедеральныйПортал;
	
	Если НЕ Результат Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	Иначе
		ВызовИзОтправки = (КодРегионаВФормеПриОтправке <> Неопределено);
		
		ДополнительныеПараметры.Вставить("ВызовИзОтправки", ВызовИзОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"ВыгрузитьПакетДляОтправкиВФСРАРПослеПолученияСертификатовИПараметровОбмена",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		СертификатыИПараметрыОбменаОрганизацииДляФСРАР(
			Оповещение,
			ОрганизацияСсылка,
			ФедеральныйПортал,
			Неопределено,
			Ложь,
			НЕ ФедеральныйПортал И ВызовИзОтправки,
			,
			,
			НЕ ВызовИзОтправки);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеПолученияСертификатовИПараметровОбмена(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт
	
	ВызовИзОтправки = ДополнительныеПараметры.ВызовИзОтправки;
	ОтчетСсылка 	= ДополнительныеПараметры.ОтчетСсылка;
	
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВФСРАРПослеНастройкиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если НЕ ВызовИзОтправки 
		И СертификатыИПараметрыОбмена.НастроенОбменВУниверсальномФормате 
		И СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИспользоватьАвтонастройку 
		И СертификатыИПараметрыОбмена.ПравоИзмененияУчетнойЗаписи Тогда
		
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОтчетСсылка,ОписаниеОповещения);
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеНастройкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВызовИзОтправки                     = ДополнительныеПараметры.ВызовИзОтправки;
	ВыполняемоеОповещение               = ДополнительныеПараметры.ВыполняемоеОповещение;
	Идентификатор                       = ДополнительныеПараметры.Идентификатор;
	КодРегионаВФормеПриОтправке         = ДополнительныеПараметры.КодРегионаВФормеПриОтправке;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ТипЗнчСсылкаНаОтчет                 = ДополнительныеПараметры.ТипЗнчСсылкаНаОтчет;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТекстФайлаВыгрузки        			= ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки        			= ДополнительныеПараметры.ИмяФайлаВыгрузки;
	
	Если СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента = Неопределено ИЛИ
		СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатСубъектаРФ = Неопределено ИЛИ 
		СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатФСРАР = Неопределено Тогда
		
		Ошибка = НСтр("ru = 'Использование документооборота с Росалкогольтабакконтролем не включено для организации!'");
		
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	// запрашиваем параметры формирования пакета и параметры обмена при вызове из отправки
	Если НЕ СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИспользоватьОбмен Тогда
		
		Ошибка = НСтр("ru = 'Использование документооборота с Росалкогольтабакконтролем не включено для организации!'");
		
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ФедеральныйПортал И ВызовИзОтправки Тогда
		
		Если НЕ ЗначениеЗаполнено(СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.КодРегиона) Тогда
			
			Ошибка = НСтр("ru = 'В параметрах отправки отчетности в Росалкогольтабакконтроль не задан код региона'");
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
			Иначе
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
				Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
		Если СертификатыИПараметрыОбмена.СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ОбщийРегиональныйИФедеральныйСервер")
			И СертификатыИПараметрыОбмена.СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы")
			И СертификатыИПараметрыОбмена.СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы") Тогда
			
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
				Ошибка = НСтр("ru = 'Программа не поддерживает автоматическую отправку отчета в вашем регионе.'");
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
			Иначе
				ОткрытьФорму(ПутьКОбъекту + ".Форма.ОтправкаВручнуюВФСРАР",,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
				Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(КодРегионаВФормеПриОтправке) И КодРегионаВФормеПриОтправке <> СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.КодРегиона Тогда
			
			Ошибка = НСтр("ru = 'Код региона в отчете отличается от кода региона в параметрах отправки отчетности в Росалкогольтабакконтроль'");
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
			Иначе
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
				Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			КонецЕсли;
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	СертификатАбонента   = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента;
	СертификатСубъектаРФ = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатСубъектаРФ;
	СертификатФСРАР      = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатФСРАР;
	
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатАбонента);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатСубъектаРФ);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатФСРАР);
	
	// В том числе, портал ФСРАР принимает сертификаты физических лиц без ОГРНИП в качестве сертификатов ИП по ИНН
	ЭтоФизическоеЛицо = (КриптографияЭДКОКлиентСервер.ТипСубъектаСертификата(
		СертификатАбонента.Владелец, ДополнительныеПараметры.ОрганизацияСсылка) = "ФЛ");
	ЭтоВыгрузкаВXML = ЭтоФизическоеЛицо И НЕ ВызовИзОтправки
		И НЕ ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры);
	
	//проверяем сертификаты
	Если НЕ ЭтоВыгрузкаВXML Тогда
		СертификатыОбмена = Новый СписокЗначений();
		СертификатыОбмена.Добавить(СертификатАбонента, НСтр("ru = 'Сертификат абонента'"));
		Если НЕ ЭтоФизическоеЛицо Тогда
			Если НЕ ФедеральныйПортал Тогда
				СертификатыОбмена.Добавить(СертификатСубъектаРФ, НСтр("ru = 'Сертификат субъекта РФ'"));
			КонецЕсли;
			СертификатыОбмена.Добавить(СертификатФСРАР, НСтр("ru = 'Сертификат ФСРАР'"));
		КонецЕсли;
		
		Если НЕ СертификатыГодятсяДляПодписанияОтчетностиФСРАР(СертификатыОбмена) Тогда
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ЭтоКрупнаяДекларация = НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки)
			И ДополнительныеПараметры.Свойство("АдресГотовыхДанных") И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено;
		
		// для среднего размера деклараций в хранилище, если передан адрес, содержится текст, а не двоичные данные
		Если НЕ ЭтоКрупнаяДекларация Тогда
			
			Если ДополнительныеПараметры.Свойство("АдресГотовыхДанных") И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено Тогда
				УдалитьИзВременногоХранилища(ДополнительныеПараметры.АдресГотовыхДанных);
			КонецЕсли;
			
			//Текст > Двоичные Данные > Подписывание > Кодирование в Base64 > Упаковка в zip > Шифрование 
			ПолноеИмяФайлаНаСервере = "";
			Адрес = ЗагрузитьДанныеНаСервер(ТекстФайлаВыгрузки, Форма.УникальныйИдентификатор, ПолноеИмяФайлаНаСервере, Истина, Ложь);
			ТекстФайлаВыгрузки = "";
			ДополнительныеПараметры.Вставить("АдресГотовыхДанных", Адрес);
			ДополнительныеПараметры.Вставить("ПолноеИмяФайлаНаСервере", ПолноеИмяФайлаНаСервере);
			
		КонецЕсли;
	
		ДополнительныеПараметры.Вставить("ВременныйКаталог", "");
		ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
	КонецЕсли;
	
	Если ЭтоВыгрузкаВXML Тогда
		ДополнительныеПараметры.Вставить("МестоХраненияКлюча", МестоХраненияКлюча);
		ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", ТекстФайлаВыгрузки);
		
		СсылкаНаМашиночитаемуюДоверенность = Новый ФорматированнаяСтрока(
			НСтр("ru = 'машиночитаемая доверенность (единый формат)'"),,,,
			"https://fsrar.gov.ru/egais/mchd");
		СсылкаНаИнструкцию = Новый ФорматированнаяСтрока(НСтр("ru = 'ЕГАИС УТМ'"),,,, "https://its.1c.ru/db/egais");
		ТекстСообщения = Новый ФорматированнаяСтрока(
			НСтр("ru = 'В случае использования сертификата физического лица выгрузка для портала ФСРАР не используется. Алкогольные декларации подаются только через ЕГАИС в формате выгрузки с расширением "".XML"".'") + "
					   |
					   |" + НСтр("ru = 'В'") + " ",
			СсылкаНаИнструкцию,
			" " + НСтр("ru = 'должна быть загружена'") + " ",
			СсылкаНаМашиночитаемуюДоверенность,
			" " + НСтр("ru = 'на представителя с сертификатом физического лица.'"));
		КнопкиВопроса = Новый СписокЗначений;
		КнопкиВопроса.Добавить(КодВозвратаДиалога.ОК, НСтр("ru = 'Выгрузить в формате XML'"));
		КнопкиВопроса.Добавить(КодВозвратаДиалога.Отмена);
		Оповещение = Новый ОписаниеОповещения(
			"ВыгрузитьПакетДляОтправкиВФСРАРПослеВопросаОФорматеXML",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПоказатьВопрос(
			Оповещение,
			ТекстСообщения,
			КнопкиВопроса,,
			КодВозвратаДиалога.ОК);
		Возврат;
	КонецЕсли;
	
	Если ЭтоФизическоеЛицо Тогда
		ЭтоКрупнаяДекларация = НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки)
			И ДополнительныеПараметры.Свойство("АдресГотовыхДанных")
			И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено;
		НастройкиОтправки = Новый Структура;
		НастройкиОтправки.Вставить("ФедеральныйПортал", ФедеральныйПортал);
		НастройкиОтправки.Вставить("КодРегиона", 		КодРегионаВФормеПриОтправке);
		
		ОтправитьВЕГАИС(
			ВыполняемоеОповещение,
			ОтчетСсылка,
			ДополнительныеПараметры.ОрганизацияСсылка,
			ИмяФайлаВыгрузки,
			?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоКрупнаяДекларация, ДополнительныеПараметры.АдресГотовыхДанных,
				Неопределено),
			?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоКрупнаяДекларация, "", ТекстФайлаВыгрузки),
			НастройкиОтправки);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("СертификатАбонента", СертификатАбонента);
	ДополнительныеПараметры.Вставить("СертификатСубъектаРФ", СертификатСубъектаРФ);
	ДополнительныеПараметры.Вставить("СертификатФСРАР", СертификатФСРАР);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("ФедеральныйПортал", ФедеральныйПортал);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеЗаписиФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено", Истина));		
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеПолученияВременногоКаталогаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.КаталогВременныхФайловНаКлиенте(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеВопросаОФорматеXML(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.ОК Тогда
		ЭтоКрупнаяДекларация = НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ТекстФайлаВыгрузки)
			И ДополнительныеПараметры.Свойство("АдресГотовыхДанных")
			И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено;
		
		Если ЭтоЛокальнаяПодпись(ДополнительныеПараметры.МестоХраненияКлюча) И НЕ ЭтоКрупнаяДекларация Тогда
			ДанныеФайлаВыгрузки = ПолучитьДвоичныеДанныеИзСтроки(ДополнительныеПараметры.ТекстФайлаВыгрузки, "windows-1251");
			АдресФайлаВыгрузки = ПоместитьВоВременноеХранилище(ДанныеФайлаВыгрузки, УникальныйИдентификатор);
		Иначе
			АдресФайлаВыгрузки = ДополнительныеПараметры.АдресГотовыхДанных;
		КонецЕсли;
		
		СохраняемыйФайл = Новый Структура("Имя, Адрес", ДополнительныеПараметры.ИмяФайлаВыгрузки, АдресФайлаВыгрузки);
		ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(СохраняемыйФайл);
	КонецЕсли;
	
	Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена",
		Неопределено, ДополнительныеПараметры.СертификатыИПараметрыОбмена);
	ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеПолученияВременногоКаталогаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ТекстФайлаВыгрузки = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	СертификатСубъектаРФ = ДополнительныеПараметры.СертификатСубъектаРФ;
	СертификатФСРАР = ДополнительныеПараметры.СертификатФСРАР;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФедеральныйПортал = ДополнительныеПараметры.ФедеральныйПортал;
	
	Если НЕ Результат.Выполнено Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ВременныйКаталог = Результат.ИмяКаталога;
	
	ПолноеИмяФайлаВыгрузки = ВременныйКаталог + ИмяФайлаВыгрузки;
	
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", ПолноеИмяФайлаВыгрузки);
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеЗаписиФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ЭтоКрупнаяДекларация = НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки)
		И ДополнительныеПараметры.Свойство("АдресГотовыхДанных") И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено;
	
	Если ЭтоКрупнаяДекларация Тогда
		
		РезультатВыполнения = Новый Структура("Выполнено", Истина);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.ТекстВФайл(Оповещение, ТекстФайлаВыгрузки, ПолноеИмяФайлаВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеЗаписиФайлаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ТекстФайлаВыгрузки = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ПолноеИмяФайлаВыгрузки = ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	СертификатСубъектаРФ = ДополнительныеПараметры.СертификатСубъектаРФ;
	СертификатФСРАР = ДополнительныеПараметры.СертификатФСРАР;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФедеральныйПортал = ДополнительныеПараметры.ФедеральныйПортал;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОрганизацияСсылка = ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если НЕ Результат.Выполнено Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// подписываем
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Подписание файла выгрузки ...'")); // ФСРАР
	
	Если СертификатАбонента = Неопределено Тогда
		Ошибка = НСтр("ru = 'Не удалось определить сертификат.'");
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеПодписанияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ЭтоКрупнаяДекларация = НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки)
		И ДополнительныеПараметры.Свойство("АдресГотовыхДанных") И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено;
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ПолноеИмяФайлаВыгрузки);
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ПакетДляОтправкиВФСРАРЗавершение", ОрганизацияСсылка); 
		ПараметрыОперации = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
	КонецЕсли;
	
	КриптографияЭДКОКлиент.ПодписатьPKCS7(
		Оповещение,
		СертификатАбонента,
		?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоКрупнаяДекларация, ДополнительныеПараметры.АдресГотовыхДанных, ПолноеИмяФайлаВыгрузки),
		Истина,
		?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоКрупнаяДекларация, Неопределено, ПолноеИмяФайлаВыгрузки + ".sig"),
		Ложь, , , ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеПодписанияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ТекстФайлаВыгрузки = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ПолноеИмяФайлаВыгрузки = ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	СертификатСубъектаРФ = ДополнительныеПараметры.СертификатСубъектаРФ;
	СертификатФСРАР = ДополнительныеПараметры.СертификатФСРАР;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФедеральныйПортал = ДополнительныеПараметры.ФедеральныйПортал;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		Ошибка = НСтр("ru = 'Не удалось подписать файл выгрузки!'");
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	// упаковываем
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Упаковка файла выгрузки ...'")); // ФСРАР
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеЧтенияПодписанногоФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ЭтоКрупнаяДекларация = НЕ ЗначениеЗаполнено(ТекстФайлаВыгрузки)
		И ДополнительныеПараметры.Свойство("АдресГотовыхДанных") И ДополнительныеПараметры.АдресГотовыхДанных <> Неопределено;
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) ИЛИ ЭтоКрупнаяДекларация Тогда
		
		Если Результат.ФайлПодписи <> ДополнительныеПараметры.АдресГотовыхДанных Тогда
			УдалитьИзВременногоХранилища(ДополнительныеПараметры.АдресГотовыхДанных);
			ДополнительныеПараметры.АдресГотовыхДанных = Результат.ФайлПодписи;
		КонецЕсли;
		
		РезультатВыполнения = Новый Структура("Выполнено, СтрокаBase64", Истина, ДополнительныеПараметры.АдресГотовыхДанных);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяФайлаВыгрузки + ".sig");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеЧтенияПодписанногоФайлаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ПолноеИмяФайлаВыгрузки = ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	СертификатСубъектаРФ = ДополнительныеПараметры.СертификатСубъектаРФ;
	СертификатФСРАР = ДополнительныеПараметры.СертификатФСРАР;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФедеральныйПортал = ДополнительныеПараметры.ФедеральныйПортал;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ПодписаннойВыгрузки = Результат.СтрокаBase64;
	
	АдресАрхиваВыгрузки = ЗаписатьZipФайлПакетаНаСервере(СтрокаBase64ПодписаннойВыгрузки, ИмяФайлаВыгрузки + ".sig");
	Если АдресАрхиваВыгрузки = Неопределено Тогда
		Ошибка = НСтр("ru = 'Не удалось упаковать файл выгрузки!'");
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("АдресАрхиваВыгрузки", АдресАрхиваВыгрузки);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
						
		Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеЗаписиZipФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		РезультатВыполнения = Новый Структура("Выполнено", Истина);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
				
		Возврат;
	КонецЕсли;
	
	ДанныеАрхиваВыгрузки = ПолучитьИзВременногоХранилища(АдресАрхиваВыгрузки);
	СтрокаBase64АрхиваВыгрузки = Base64Строка(ДанныеАрхиваВыгрузки);
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеЗаписиZipФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, СтрокаBase64АрхиваВыгрузки, ПолноеИмяФайлаВыгрузки + ".sig.zip");
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеЗаписиZipФайлаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолноеИмяФайлаВыгрузки = ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	СертификатСубъектаРФ = ДополнительныеПараметры.СертификатСубъектаРФ;
	СертификатФСРАР = ДополнительныеПараметры.СертификатФСРАР;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФедеральныйПортал = ДополнительныеПараметры.ФедеральныйПортал;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	АдресАрхиваВыгрузки = ДополнительныеПараметры.АдресАрхиваВыгрузки;
		
	Если НЕ Результат.Выполнено Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// шифруем
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Шифрование файла выгрузки ...'"));
	ИмяФайлаЗашифрованногоПакета = ПолноеИмяФайлаВыгрузки + ".sig.zip.enc";
	
	ДополнительныеПараметры.Вставить("ИмяФайлаЗашифрованногоПакета", ИмяФайлаЗашифрованногоПакета);
	
	СертификатыЗашифрования = Новый Массив;
	Если НЕ ФедеральныйПортал Тогда
		СертификатыЗашифрования.Добавить(СертификатСубъектаРФ);
	КонецЕсли;
	СертификатыЗашифрования.Добавить(СертификатФСРАР);
	
	ИмяДляШифрования = ПолноеИмяФайлаВыгрузки + ".sig.zip";
	//ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ИмяФайлаЗашифрованногоПакета, СертификатыИПараметрыОбмена", ВыполняемоеОповещение, ИмяФайлаЗашифрованногоПакета, СертификатыИПараметрыОбмена);
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВФСРАРПослеЗашифрованияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ИмяДляШифрования = АдресАрхиваВыгрузки;
		ИмяФайлаЗашифрованногоПакета = ПоместитьВоВременноеХранилище(Неопределено, УникальныйИдентификатор);
	КонецЕсли;
	
	КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
		Оповещение,
		ИмяДляШифрования,
		СертификатыЗашифрования,
		ИмяФайлаЗашифрованногоПакета,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВФСРАРПослеЗашифрованияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаЗашифрованногоПакета = ДополнительныеПараметры.ИмяФайлаЗашифрованногоПакета;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		Ошибка = НСтр("ru = 'Не удалось зашифровать файл выгрузки!'");
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ИмяФайлаЗашифрованногоПакета = Новый Структура;
		ИмяФайлаЗашифрованногоПакета.Вставить("ИмяФайлаЗашифрованногоПакета", Результат.ЗашифрованныйФайл);
		ИмяФайлаЗашифрованногоПакета.Вставить("ИмяФайлаВыгрузки", ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки);
	КонецЕсли;	
	
	Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", ИмяФайлаЗашифрованногоПакета, СертификатыИПараметрыОбмена);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

#Область ОбменСЕГАИС

&НаКлиенте
Процедура ОтправитьВЕГАИС(
		ВыполняемоеОповещение,
		ОтчетСсылка,
		ОрганизацияСсылка,
		ИмяФайлаВыгрузки,
		АдресГотовыхДанных,
		ТекстФайлаВыгрузки,
		НастройкиОтправки)
	
	СвойстваФайлаВыгрузки = Новый Структура;
	СвойстваФайлаВыгрузки.Вставить("ИмяФайлаВыгрузки", 		ИмяФайлаВыгрузки);
	СвойстваФайлаВыгрузки.Вставить("АдресГотовыхДанных", 	АдресГотовыхДанных);
	СвойстваФайлаВыгрузки.Вставить("ТекстФайлаВыгрузки", 	ТекстФайлаВыгрузки);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 			ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 		ОрганизацияСсылка);
	ДополнительныеПараметры.Вставить("СвойстваФайлаВыгрузки", 	СвойстваФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("НастройкиОтправки", 		НастройкиОтправки);
	ДополнительныеПараметры.Вставить("ТекстОшибки", 			"");
	ДополнительныеПараметры.Вставить("НастройкиОбмена", 		Неопределено);
	
	НастройкиОбмена = НастройкиОбменаФСРАРЕГАИС(ОрганизацияСсылка);
	Если ЗначениеЗаполнено(НастройкиОбмена.АдресУТМ) ИЛИ ЗначениеЗаполнено(НастройкиОбмена.ПортУТМ)
		ИЛИ ЗначениеЗаполнено(НастройкиОбмена.Таймаут) ИЛИ НастройкиОбмена.ОбменНаСервере = Истина Тогда
		
		Оповещение = Новый ОписаниеОповещения("ОтправитьВЕГАИСПослеНастройки", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, НастройкиОбмена);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения("ОтправитьВЕГАИСВызовНастройки", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция НастройкиОбменаФСРАРЕГАИС(Организация)
	
	Результат = Новый Структура;
	Результат.Вставить("АдресУТМ", 			"");
	Результат.Вставить("ПортУТМ", 			0);
	Результат.Вставить("Таймаут", 			0);
	Результат.Вставить("ОбменНаСервере", 	Ложь);
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	НастройкиОбменаФСРАР.АдресУТМ КАК АдресУТМ,
		|	НастройкиОбменаФСРАР.ПортУТМ КАК ПортУТМ,
		|	НастройкиОбменаФСРАР.ТаймаутУТМ КАК ТаймаутУТМ,
		|	НастройкиОбменаФСРАР.ОбменСУТМНаСервере КАК ОбменСУТМНаСервере
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСРАР КАК НастройкиОбменаФСРАР
		|ГДЕ
		|	НастройкиОбменаФСРАР.Организация = &ОрганизацияСсылка
		|	И (НастройкиОбменаФСРАР.Пользователь ЕСТЬ NULL
		|	ИЛИ НастройкиОбменаФСРАР.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))");
	
	Запрос.УстановитьПараметр("ОрганизацияСсылка", Организация);
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		Результат.АдресУТМ 			= Выборка.АдресУТМ;
		Результат.ПортУТМ 			= Выборка.ПортУТМ;
		Результат.Таймаут 			= Выборка.ТаймаутУТМ;
		Результат.ОбменНаСервере 	= Выборка.ОбменСУТМНаСервере;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОтправитьВЕГАИСВызовНастройки(Результат, ДополнительныеПараметры) Экспорт
	
	Оповещение = Новый ОписаниеОповещения("ОтправитьВЕГАИСПослеНастройки", ЭтотОбъект, ДополнительныеПараметры);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ОрганизацияСсылка", 	ДополнительныеПараметры.ОрганизацияСсылка);
	ПараметрыФормы.Вставить("ТекстОшибки", 			ДополнительныеПараметры.ТекстОшибки);
	ПараметрыФормы.Вставить("НастройкиОбмена", 		ДополнительныеПараметры.НастройкиОбмена);
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.НастройкиОбменаЕГАИС",
		ПараметрыФормы,,,,,
		Оповещение,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьВЕГАИСПослеНастройки(НастройкиОбменаИлиРезультатДиалогаОбОшибкеДоступа,
		ДополнительныеПараметры) Экспорт
	
	Если ТипЗнч(НастройкиОбменаИлиРезультатДиалогаОбОшибкеДоступа) = Тип("Булево") Тогда
		РезультатДиалогаОбОшибкеДоступа = НастройкиОбменаИлиРезультатДиалогаОбОшибкеДоступа;
		НастройкиОбмена = ДополнительныеПараметры.НастройкиОбмена;
	Иначе
		РезультатДиалогаОбОшибкеДоступа = Истина;
		НастройкиОбмена = НастройкиОбменаИлиРезультатДиалогаОбОшибкеДоступа;
		ДополнительныеПараметры.Вставить("НастройкиОбмена", НастройкиОбмена);
	КонецЕсли;
	
	Если НастройкиОбмена = Неопределено ИЛИ НЕ РезультатДиалогаОбОшибкеДоступа Тогда
		НастройкиОтправки = ДополнительныеПараметры.НастройкиОтправки;
		
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("РезультатВыгрузки", 	Неопределено);
		РезультатВыполнения.Вставить("ОтправленоВЕГАИС", 	Ложь);
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Если НастройкиОбмена.ОбменНаСервере Тогда
		СвойстваОтвета = ОтправитьВЕГАИСОтправкаНаСервере(
			ДополнительныеПараметры.ОтчетСсылка,
			ДополнительныеПараметры.ОрганизацияСсылка,
			НастройкиОбмена,
			ДополнительныеПараметры.СвойстваФайлаВыгрузки);
		
	Иначе
		СвойстваОтвета = ОтправитьВЕГАИСОтправкаНаКлиенте(
			ДополнительныеПараметры.ОтчетСсылка,
			ДополнительныеПараметры.ОрганизацияСсылка,
			НастройкиОбмена,
			ДополнительныеПараметры.СвойстваФайлаВыгрузки);
	КонецЕсли;
	
	Если СвойстваОтвета.КодСостояния = 200 И НЕ ЗначениеЗаполнено(СвойстваОтвета.ТекстОшибки) Тогда
		НастройкиОтправки = ДополнительныеПараметры.НастройкиОтправки;
		
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("РезультатВыгрузки", 	Неопределено);
		РезультатВыполнения.Вставить("ОтправленоВЕГАИС", 	Истина);
		РезультатВыполнения.Вставить("НоваяОтправкаСсылка", СвойстваОтвета.ОтправкаСсылка);
		РезультатВыполнения.Вставить("ФедеральныйПортал", 	НастройкиОтправки.ФедеральныйПортал);
		РезультатВыполнения.Вставить("КодРегиона", 			НастройкиОтправки.КодРегиона);
		
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СвойстваОтвета.ТекстОшибки) И СвойстваОтвета.КодСостояния = "ПовторитьНастройкуОбмена" Тогда
		ДополнительныеПараметры.Вставить("ТекстОшибки", СвойстваОтвета.ТекстОшибки);
		Оповещение = Новый ОписаниеОповещения("ОтправитьВЕГАИСВызовНастройки", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение);
		Возврат;
	КонецЕсли;
	
	ПовторятьСоединение = ЗначениеЗаполнено(СвойстваОтвета.ТекстОшибки)
		И СвойстваОтвета.КодСостояния <> "ВывестиТекстОшибки";
	
	Если ПовторятьСоединение Тогда
		ПортУТМСтрокой = Формат(НастройкиОбмена.ПортУТМ, "ЧДЦ=; ЧН=; ЧГ=");
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Не удалось обратиться к серверу ЕГАИС (%1)'"),
			НастройкиОбмена.АдресУТМ + ":" + ПортУТМСтрокой);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ОтправитьВЕГАИСПослеНастройки", ЭтотОбъект, ДополнительныеПараметры);
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			ТекстСообщения,
			ОписаниеОповещения);
		
	Иначе
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(?(СвойстваОтвета.КодСостояния = "ВывестиТекстОшибки",
			СвойстваОтвета.ТекстОшибки, НСтр("ru = 'Не удалось отправить отчет в ЕГАИС'")));
		
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("РезультатВыгрузки", 	Неопределено);
		РезультатВыполнения.Вставить("ОтправленоВЕГАИС", 	Ложь);
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправитьВЕГАИСПодгтовкаЗапроса(
		ТипОперации,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправитьВЕГАИСПодгтовкаЗапроса(
		ТипОперации,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВЕГАИСОбработкаОтвета(
		ТипОперации,
		ОтчетСсылка,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки = Неопределено,
		ТекстОтвета = "")
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправитьВЕГАИСОбработкаОтвета(
		ТипОперации,
		ОтчетСсылка,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки,
		ТекстОтвета);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВЕГАИСОтправкаНаСервере(
		ОтчетСсылка,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки)
	
	СвойстваЗапроса = ОтправитьВЕГАИСПодгтовкаЗапроса(
		"СлужебнаяИнформацияОбОрганизации",
		ОрганизацияСсылка,
		НастройкиОбмена);
	
	СвойстваОтвета = ДокументооборотСКОКлиентСервер.ОтправитьHTTPЗапрос(
		СвойстваЗапроса.ТранспортныйМодуль,
		СвойстваЗапроса.ПараметрыВызова);
	
	РезультатИлиСвойстваЗапроса = ОтправитьВЕГАИСОбработкаИЗапрос(
		СвойстваОтвета,
		ОтчетСсылка,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки);
	
	Если НЕ РезультатИлиСвойстваЗапроса.Свойство("СвойстваЗапроса") Тогда
		Возврат РезультатИлиСвойстваЗапроса;
	КонецЕсли;
		
	СвойстваЗапроса = РезультатИлиСвойстваЗапроса.СвойстваЗапроса;
	
	СвойстваОтвета = ДокументооборотСКОКлиентСервер.ОтправитьHTTPЗапрос(
		СвойстваЗапроса.ТранспортныйМодуль,
		СвойстваЗапроса.ПараметрыВызова);
	
	ОтправкаСсылка = Неопределено;
	Если СвойстваОтвета.КодСостояния = 200 И НЕ ЗначениеЗаполнено(СвойстваОтвета.ТекстОшибки) Тогда
		ОтправкаСсылка = ОтправитьВЕГАИСОбработкаОтвета(
			"ОтправкаДекларации",
			ОтчетСсылка,
			ОрганизацияСсылка,
			НастройкиОбмена,
			СвойстваФайлаВыгрузки);
	КонецЕсли;
	СвойстваОтвета.Вставить("ОтправкаСсылка", ОтправкаСсылка);
	
	Возврат СвойстваОтвета;
	
КонецФункции

&НаКлиенте
Функция ОтправитьВЕГАИСОтправкаНаКлиенте(
		ОтчетСсылка,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки)
	
	#Если ВебКлиент Тогда
		Возврат Новый Структура("КодСостояния, ТекстОшибки, ОтправкаСсылка", "ВывестиТекстОшибки",
			НСтр("ru = 'Обмен с ЕГАИС из веб-клиента недоступен. Используйте тонкий клиент'"), Неопределено);
	#Иначе
		СвойстваЗапроса = ОтправитьВЕГАИСПодгтовкаЗапроса(
			"СлужебнаяИнформацияОбОрганизации",
			ОрганизацияСсылка,
			НастройкиОбмена);
		
		СвойстваОтвета = ДокументооборотСКОКлиентСервер.ОтправитьHTTPЗапрос(
			СвойстваЗапроса.ТранспортныйМодуль,
			СвойстваЗапроса.ПараметрыВызова);
		
		РезультатИлиСвойстваЗапроса = ОтправитьВЕГАИСОбработкаИЗапрос(
			СвойстваОтвета,
			ОтчетСсылка,
			ОрганизацияСсылка,
			НастройкиОбмена,
			СвойстваФайлаВыгрузки);
		
		Если НЕ РезультатИлиСвойстваЗапроса.Свойство("СвойстваЗапроса") Тогда
			Возврат РезультатИлиСвойстваЗапроса;
		КонецЕсли;
		
		СвойстваЗапроса = РезультатИлиСвойстваЗапроса.СвойстваЗапроса;
		
		СвойстваОтвета = ДокументооборотСКОКлиентСервер.ОтправитьHTTPЗапрос(
			СвойстваЗапроса.ТранспортныйМодуль,
			СвойстваЗапроса.ПараметрыВызова);
		
		ОтправкаСсылка = Неопределено;
		Если СвойстваОтвета.КодСостояния = 200 И НЕ ЗначениеЗаполнено(СвойстваОтвета.ТекстОшибки) Тогда
			ОтправкаСсылка = ОтправитьВЕГАИСОбработкаОтвета(
				"ОтправкаДекларации",
				ОтчетСсылка,
				ОрганизацияСсылка,
				НастройкиОбмена,
				СвойстваФайлаВыгрузки);
		КонецЕсли;
		СвойстваОтвета.Вставить("ОтправкаСсылка", ОтправкаСсылка);
		
		Возврат СвойстваОтвета;
	#КонецЕсли
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВЕГАИСОбработкаИЗапрос(
		СвойстваОтвета,
		ОтчетСсылка,
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки)
	
	Если СвойстваОтвета.КодСостояния = 200 И НЕ ЗначениеЗаполнено(СвойстваОтвета.ТекстОшибки) Тогда
		ТекстОшибки = ОтправитьВЕГАИСОбработкаОтвета(
			"СлужебнаяИнформацияОбОрганизации",
			ОтчетСсылка,
			ОрганизацияСсылка,
			НастройкиОбмена,,
			СвойстваОтвета.ТекстОтвета);
		
		Если ЗначениеЗаполнено(ТекстОшибки) Тогда
			Возврат Новый Структура("КодСостояния, ТекстОшибки, ОтправкаСсылка", "ПовторитьНастройкуОбмена",
				ТекстОшибки, Неопределено);
		КонецЕсли;
	КонецЕсли;
	
	СвойстваЗапроса = ОтправитьВЕГАИСПодгтовкаЗапроса(
		"ОтправкаДекларации",
		ОрганизацияСсылка,
		НастройкиОбмена,
		СвойстваФайлаВыгрузки);
	
	Возврат Новый Структура("СвойстваЗапроса", СвойстваЗапроса);
	
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
		
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР(
		ВходящийКонтекст.Форма, 
		ВходящийКонтекст.КодРегионаВФорме,
		ВходящийКонтекст.ЭтоОтправкаИзФормыОтчетность, 
		ВходящийКонтекст.ОтчетСсылка, 
		ВходящийКонтекст.ОрганизацияОтчета, 
		ВходящийКонтекст.АдресГотовыхДанных, 
		ВходящийКонтекст.ИмяФайлаГотовыхДанных);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР(
		Форма, 
		КодРегионаВФорме = Неопределено,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь, 
		Знач ОтчетСсылка = Неопределено, 
		Знач ОрганизацияОтчета = Неопределено, 
		Знач АдресГотовыхДанных = Неопределено, 
		Знач ИмяФайлаГотовыхДанных = Неопределено) Экспорт
		
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияОтчета 	= ДокументооборотСФСРАРКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
		ОтчетСсылка 		= ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Форма", 						 Форма);
	ДополнительныеПараметры.Вставить("КодРегионаВФорме", 			 КодРегионаВФорме);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 				 ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 			 ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("АдресГотовыхДанных", 			 АдресГотовыхДанных);
	ДополнительныеПараметры.Вставить("ИмяФайлаГотовыхДанных", 		 ИмяФайлаГотовыхДанных);
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	
	Если ЭтоРегламентированныйОтчет Тогда
		
		Если ЭтоОтправкаИзФормыОтчетность Тогда
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеСохранения(Истина, ДополнительныеПараметры);
		Иначе
			// отправляем только из записанной формы
			Если Форма.Модифицированность Тогда
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеСохранения",
					ЭтотОбъект,
					ДополнительныеПараметры);
				
				Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
			Иначе
				ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеСохранения(Истина, ДополнительныеПараметры);
			КонецЕсли;
		КонецЕсли;
	Иначе // электронное представление
		
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			Если Форма.Модифицированность Тогда
				ПоказатьПредупреждение(,НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено 
			ИЛИ ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			Возврат;
		КонецЕсли;
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеСохранения(Истина, ДополнительныеПараметры);
		
	КонецЕсли;

КонецПроцедуры
	
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеСохранения(Результат, ДополнительныеПараметры) Экспорт
	
	Форма              = ДополнительныеПараметры.Форма;
	ОтчетСсылка        = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета  = ДополнительныеПараметры.ОрганизацияОтчета;
	
	Если НЕ ЗначениеЗаполнено(ОтчетСсылка) Тогда
		ОтчетСсылка = ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
		ДополнительныеПараметры.ОтчетСсылка = ОтчетСсылка;
	КонецЕсли;
	
	СвойстваОбмена                      = СвойстваОбменаОрганизацииНаСервере(ОрганизацияОтчета, Ложь, Истина, Ложь);
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(СвойстваОбмена);
	УчетнаяЗапись                       = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияОтчета);
	
	Если НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		Если НЕ СвойстваОбмена.НастроенОбменВУниверсальномФормате Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАР_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ОтчетСсылка, ОписаниеОповещения); //+
			
		Иначе
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка); //+
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если СвойстваОбмена.НастроенОбменВУниверсальномФормате 
		И НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ОтчетСсылка) Тогда
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка); //+
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет",                 ТипЗнч(ОтчетСсылка));

	ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеНастройкиУчетнойПолитикиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если СвойстваОбмена.НастроенОбменВУниверсальномФормате 
		И СвойстваОбмена.НастройкиОбмена.ИспользоватьАвтонастройку 
		И СвойстваОбмена.ПравоИзмененияУчетнойЗаписи Тогда
		
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОтчетСсылка,ОписаниеОповещения);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеНастройкиУчетнойПолитикиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма                               = ДополнительныеПараметры.Форма;
	КодРегионаВФорме                    = ДополнительныеПараметры.КодРегионаВФорме;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ТипЗнчСсылкаНаОтчет                 = ДополнительныеПараметры.ТипЗнчСсылкаНаОтчет;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	АдресГотовыхДанных                  = ДополнительныеПараметры.АдресГотовыхДанных;
	ИмяФайлаГотовыхДанных               = ДополнительныеПараметры.ИмяФайлаГотовыхДанных;
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	
	Если ЭтоРегламентированныйОтчет Тогда
		
		Если ЭтоОтправкаИзФормыОтчетность Тогда
			
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРРегламентированныйОтчет(
				КодРегионаВФорме,
				ОрганизацияОтчета,
				ОтчетСсылка,
				УчетнаяЗапись,
				Форма,
				ЭтоОтправкаИзФормыОтчетность,
				ЭтоРегламентированныйОтчет,
				МестоХраненияКлюча,
				АдресГотовыхДанных,
				ИмяФайлаГотовыхДанных);
				
		Иначе
			// отправляем только из записанной формы
			Если Форма.Модифицированность Тогда
				
				ДополнительныеПараметры.Вставить("УчетнаяЗапись",              УчетнаяЗапись);
				ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет", ЭтоРегламентированныйОтчет);
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРЗавершение",
					ЭтотОбъект,
					ДополнительныеПараметры);
				
				Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
				
			Иначе
				
				ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРРегламентированныйОтчет(
					КодРегионаВФорме,
					ОрганизацияОтчета,
					ОтчетСсылка,
					УчетнаяЗапись,
					Форма,
					ЭтоОтправкаИзФормыОтчетность,
					ЭтоРегламентированныйОтчет,
					МестоХраненияКлюча,
					АдресГотовыхДанных,
					ИмяФайлаГотовыхДанных);
				
			КонецЕсли;
		КонецЕсли;
	Иначе // электронное представление
		
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			Если Форма.Модифицированность Тогда
				ПоказатьПредупреждение(,НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено 
			ИЛИ ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			Возврат;
		КонецЕсли;
		
		ПоляДляЗаписиОтправки 	= ПоляДляЗаписиОтправкиВФСРАРНаСервере(ОтчетСсылка);
		ФедеральныйПортал 		= ПоляДляЗаписиОтправки.ФедеральныйПортал;
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения(
			КодРегионаВФорме,
			ОрганизацияОтчета,
			ОтчетСсылка,
			ПоляДляЗаписиОтправки,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоОтправкаИзФормыОтчетность,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			АдресГотовыхДанных,
			ИмяФайлаГотовыхДанных);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	КодРегионаВФорме                    = ДополнительныеПараметры.КодРегионаВФорме;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	АдресГотовыхДанных = ДополнительныеПараметры.АдресГотовыхДанных;
	ИмяФайлаГотовыхДанных = ДополнительныеПараметры.ИмяФайлаГотовыхДанных;
	
	// если отчет не был записан ранее, то получим ссылку на записанный отчет
	Если ОтчетСсылка = Неопределено Тогда
		ОтчетСсылка = ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРРегламентированныйОтчет(
		КодРегионаВФорме,
		ОрганизацияОтчета,
		ОтчетСсылка,
		УчетнаяЗапись,
		Форма,
		ЭтоОтправкаИзФормыОтчетность,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		АдресГотовыхДанных,
		ИмяФайлаГотовыхДанных);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРРегламентированныйОтчет(
	КодРегионаВФорме,
	ОрганизацияОтчета,
	ОтчетСсылка,
	УчетнаяЗапись,
	Форма,
	ЭтоОтправкаИзФормыОтчетность,
	ЭтоРегламентированныйОтчет,
	МестоХраненияКлюча,
	АдресГотовыхДанных = Неопределено,
	ИмяФайлаГотовыхДанных = Неопределено)
	
	Если ОтчетСсылка = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ФедеральныйПортал = ОтчетПодаетсяЧерезФедеральныйПорталНаСервере(ОтчетСсылка);
	
	Если ЭтоОтправкаИзФормыОтчетность Тогда
		
		ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ДатаНачала, ДатаОкончания, Вид, ДанныеОтчета");
		
		ПоляДляЗаписиОтправки = Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия, ФедеральныйПортал",
			НачалоДня(ЗначенияРеквизитов.ДатаНачала),
			КонецДня(ЗначенияРеквизитов.ДатаОкончания),
			ЗначенияРеквизитов.Вид,
			ФедеральныйПортал);
			
	Иначе
			
		ПоляДляЗаписиОтправки = Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия, ФедеральныйПортал",
			Форма.СтруктураРеквизитовФормы.мДатаНачалаПериодаОтчета,
			Форма.СтруктураРеквизитовФормы.мДатаКонцаПериодаОтчета,
			Форма.СтруктураРеквизитовФормы.ВидДокумента * Форма.НомерКорректировки,
			ФедеральныйПортал);
		
	КонецЕсли;
	
	Если ЭтоОтправкаИзФормыОтчетность Тогда
		ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ДанныеОтчета");
		КодРегионаВФорме = КодРегионаВФорме(ЗначенияРеквизитов);
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения(
		КодРегионаВФорме,
		ОрганизацияОтчета,
		ОтчетСсылка,
		ПоляДляЗаписиОтправки,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоОтправкаИзФормыОтчетность,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		АдресГотовыхДанных,
		ИмяФайлаГотовыхДанных);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения(
	КодРегионаВФорме,
	ОрганизацияОтчета,
	ОтчетСсылка,
	ПоляДляЗаписиОтправки,
	УчетнаяЗапись,
	ФедеральныйПортал,
	Форма,
	ЭтоОтправкаИзФормыОтчетность,
	ЭтоРегламентированныйОтчет,
	МестоХраненияКлюча,
	АдресГотовыхДанных = Неопределено,
	ИмяФайлаГотовыхДанных = Неопределено)
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("КодРегионаВФорме",                    КодРегионаВФорме);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета",                   ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки",               ПоляДляЗаписиОтправки);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
	ДополнительныеПараметры.Вставить("Форма",                               Форма);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность",        ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
	ДополнительныеПараметры.Вставить("АдресГотовыхДанных",                  АдресГотовыхДанных);
	ДополнительныеПараметры.Вставить("ИмяФайлаГотовыхДанных",               ИмяФайлаГотовыхДанных);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ОтчетСсылка, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ОтчетСсылка, ВходящийКонтекст.ОрганизацияОтчета, "ФСРАР");
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеСохранения_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;	
	
	Форма 		= ВходящийКонтекст.Форма;
	ОтчетСсылка = ВходящийКонтекст.ОтчетСсылка;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПодтверждения",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	ПодтверждениеОтправкиОтчета(
		Форма, 
		ОписаниеОповещения,
		ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПодтверждения(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат <> КодВозвратаДиалога.ОК Тогда
		Возврат;
	КонецЕсли;
	
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	
	// проверяем, отправлялся ли отчет
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПодтвержденияЗавершение",
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ПодтвердитьПовторнуюОтправку(ОтчетСсылка, "ФСРАР", ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПодтвержденияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	КодРегионаВФорме             = ДополнительныеПараметры.КодРегионаВФорме;
	ОрганизацияОтчета            = ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	ФедеральныйПортал            = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                        = ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	АдресГотовыхДанных           = ДополнительныеПараметры.АдресГотовыхДанных;
	ИмяФайлаГотовыхДанных        = ДополнительныеПараметры.ИмяФайлаГотовыхДанных;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВФСРАР(
		ОтчетСсылка,
		Форма,
		ЭтоОтправкаИзФормыОтчетность,
		АдресГотовыхДанных,
		ИмяФайлаГотовыхДанных);
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	Если РезультатВыгрузки = Неопределено Тогда
		
		Ошибка = НСтр("ru = 'Не удалось выгрузить отчет.'");

		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	СертификатыИПараметрыОбмена = Неопределено;

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПроверкиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("Форма", 						Форма);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 			ОписаниеОповещения);
	КонтекстДлительнойОперации.Вставить("РезультатВыгрузки", 			РезультатВыгрузки);
	КонтекстДлительнойОперации.Вставить("ФедеральныйПортал", 			ФедеральныйПортал);
	КонтекстДлительнойОперации.Вставить("КодРегионаВФорме", 			КодРегионаВФорме);
	КонтекстДлительнойОперации.Вставить("СертификатыИПараметрыОбмена", 	СертификатыИПараметрыОбмена);
	КонтекстДлительнойОперации.Вставить("ЭтоОтправкаИзФормыОтчетность", ЭтоОтправкаИзФормыОтчетность);
	КонтекстДлительнойОперации.Вставить("ОтчетСсылка", 					ОтчетСсылка);
	КонтекстДлительнойОперации.Вставить("ОрганизацияОтчета", 			ОрганизацияОтчета);
	КонтекстДлительнойОперации.Вставить("АдресГотовыхДанных", 			АдресГотовыхДанных);
	КонтекстДлительнойОперации.Вставить("ИмяФайлаГотовыхДанных", 		ИмяФайлаГотовыхДанных);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ОтчетСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 5;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФСРАР
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПодтвержденияЗавершение",  0.1, Истина);
	
КонецПроцедуры
	
&НаКлиенте
Процедура Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПодтвержденияЗавершение() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Форма 							= КонтекстДлительнойОперации.Форма;
	ОписаниеОповещения 				= КонтекстДлительнойОперации.ОписаниеОповещения;
	РезультатВыгрузки 				= КонтекстДлительнойОперации.РезультатВыгрузки;
	ФедеральныйПортал 				= КонтекстДлительнойОперации.ФедеральныйПортал;
	КодРегионаВФорме 				= КонтекстДлительнойОперации.КодРегионаВФорме;
	СертификатыИПараметрыОбмена 	= КонтекстДлительнойОперации.СертификатыИПараметрыОбмена;
	ЭтоОтправкаИзФормыОтчетность 	= КонтекстДлительнойОперации.ЭтоОтправкаИзФормыОтчетность;
	ОтчетСсылка 					= КонтекстДлительнойОперации.ОтчетСсылка;
	ОрганизацияОтчета 				= КонтекстДлительнойОперации.ОрганизацияОтчета;
	АдресГотовыхДанных 				= КонтекстДлительнойОперации.АдресГотовыхДанных;
	ИмяФайлаГотовыхДанных 			= КонтекстДлительнойОперации.ИмяФайлаГотовыхДанных;
	
	ВыгрузитьПакетДляОтправкиВФСРАР(
		Форма,
		ОписаниеОповещения,
		ФедеральныйПортал,
		РезультатВыгрузки.ТекстФайлаВыгрузки,
		РезультатВыгрузки.ИмяФайлаВыгрузки,
		Строка(КодРегионаВФорме),
		СертификатыИПараметрыОбмена,
		ЭтоОтправкаИзФормыОтчетность,
		ОтчетСсылка,
		ОрганизацияОтчета,
		АдресГотовыхДанных,
		ИмяФайлаГотовыхДанных);
	
КонецПроцедуры	

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПроверкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	
	Если НЕ ЗначениеЗаполнено(Результат.РезультатВыгрузки) Тогда
		Если Результат.Свойство("ОтправленоВЕГАИС") И Результат.ОтправленоВЕГАИС Тогда
			ПараметрыАвтозапроса = Новый Структура();
			ПараметрыАвтозапроса.Вставить("Ключ", 				Результат.НоваяОтправкаСсылка);
			ПараметрыАвтозапроса.Вставить("ФедеральныйПортал", 	Результат.ФедеральныйПортал);
			ПараметрыАвтозапроса.Вставить("КодРегиона", 		Результат.КодРегиона);
			ПараметрыАвтозапроса.Вставить("ЗначениеCookie", 	"");
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
			
			ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСРАР.
			
		Иначе
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", Результат.СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("РезультатВыгрузки", 			Результат.РезультатВыгрузки);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеИнициализацииЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеИнициализацииЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	КодРегионаВФорме                    = ДополнительныеПараметры.КодРегионаВФорме;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки               = ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		Возврат;
	КонецЕсли;
	
	ПараметрыФормированияПакета = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	ИнформацияОРегионе          = СертификатыИПараметрыОбмена.ИнформацияОРегионе;
	СхемаСдачиОтчетности        = СертификатыИПараметрыОбмена.СхемаСдачиОтчетности;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		СтрокаBase64ФайлаПакета = РезультатВыгрузки.ИмяФайлаЗашифрованногоПакета;
		КороткоеИмяФайлаПакета = РезультатВыгрузки.ИмяФайлаВыгрузки + ".sig.zip.enc";
		
		Результат = Новый Структура();
		Результат.Вставить("Выполнено",      Истина);
		Результат.Вставить("ДвоичныеДанные", Неопределено);
		Результат.Вставить("СтрокаBase64",   СтрокаBase64ФайлаПакета);
		
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета", ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("ИнформацияОРегионе",          ИнформацияОРегионе);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",        СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",      КороткоеИмяФайлаПакета);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеЧтенияФайлаВыгрузкиПакетаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	Иначе
		КороткоеИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(РезультатВыгрузки).Имя;
		
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета", ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("ИнформацияОРегионе",          ИнформацияОРегионе);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",        СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",      КороткоеИмяФайлаПакета);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеЧтенияФайлаВыгрузкиПакетаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, РезультатВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеЧтенияФайлаВыгрузкиПакетаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	КодРегионаВФорме                    = ДополнительныеПараметры.КодРегионаВФорме;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки               = ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаПакета = Результат.СтрокаBase64;
	
	СтрокаBase64ФайлаОтправки = СформироватьФайлОтправкиВФСРАРНаСервере(
		КороткоеИмяФайлаПакета,
		СхемаСдачиОтчетности,
		СтрокаBase64ФайлаПакета,
		// замена на перечисление МодельРаботыСКлючам
		НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
		Форма.УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Отправка отчета ...'")); // ФСРАР
	
	Крипт = СертификатыИПараметрыОбмена.Крипт;
	КодИНаименованиеРегиона = ПараметрыФормированияПакета.КодРегиона + ?(ЗначениеЗаполнено(ИнформацияОРегионе.НаименованиеРегиона), " - ", "") + ИнформацияОРегионе.НаименованиеРегиона;
	ФормаДлительнаяОперация = Неопределено;
	
	ПараметрыОтправки = Новый Структура();
	ПараметрыОтправки.Вставить("Организация",                  ОрганизацияОтчета);
	ПараметрыОтправки.Вставить("ИНН",                          Неопределено);
	ПараметрыОтправки.Вставить("ФедеральныйПортал",            ФедеральныйПортал);
	ПараметрыОтправки.Вставить("КодРегиона",                   ПараметрыФормированияПакета.КодРегиона);
	ПараметрыОтправки.Вставить("КодИНаименованиеРегиона",      КодИНаименованиеРегиона);
	ПараметрыОтправки.Вставить("СсылкаНаПорталОнлайнПроверки", ИнформацияОРегионе.СсылкаНаПортал);
	ПараметрыОтправки.Вставить("СхемаСдачиОтчетности",         СхемаСдачиОтчетности);
	ПараметрыОтправки.Вставить("ПоляДляЗаписиОтправки",        ПоляДляЗаписиОтправки);
	
	ПараметрыСоединения = Новый Структура();
	ПараметрыСоединения.Вставить("Пароль",                         ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ);
	ПараметрыСоединения.Вставить("СохранитьПароль",                ЗначениеЗаполнено(ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ));
	ПараметрыСоединения.Вставить("СохраненныйПароль",              ПараметрыФормированияПакета.ПарольПорталаСубъектаРФ);
	ПараметрыСоединения.Вставить("СоединениеHTTP",                 Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie",                 "");
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиНаСервере", "");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером",         "");
	ПараметрыСоединения.Вставить("ПовторятьСоединение",            Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля",          Истина);
	ПараметрыСоединения.Вставить("ТребуетсяРегистрация",           Ложь);
	ПараметрыСоединения.Вставить("ТребуетсяПовторнаяАвторизация",  Ложь);
	ПараметрыСоединения.Вставить("СообщенияПользователю",          Новый Массив);
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизации(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки);
	
КонецПроцедуры

// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (Цикл)
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизации(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки)
	
	СлучайныйКлюч                      = "";
	Данные64CAPTCHA                    = "";
	ПараметрыСоединения.ЗначениеCookie = "";
	ПодписываемыеДанные                = Неопределено;
	ПодписанныйПакетДляАвторизации     = "";
	
	Если СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы") Тогда
		
		ПодписываемыеДанные = ПолучитьПодписываемыеДанныеФСРАРНаСервере(
			Ложь,
			СлучайныйКлюч,
			Данные64CAPTCHA,
			Неопределено,
			ЭтоРегламентированныйОтчет,
			ПараметрыОтправки,
			ПараметрыСоединения);

		ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
			
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПодписываемыеДанные) Тогда
		
		Если СхемаСдачиОтчетности <> ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы") Тогда
			
			ЗначениеCAPTCHA = "";
			Если Данные64CAPTCHA <> "" Тогда
				
				ДополнительныеПараметры = Новый Структура;
				ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
				ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
				ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
				ДополнительныеПараметры.Вставить("Форма",                               Форма);
				ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
				ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
				ДополнительныеПараметры.Вставить("ПодписываемыеДанные",                 ПодписываемыеДанные);
				ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизации",      ПодписанныйПакетДляАвторизации);
				ДополнительныеПараметры.Вставить("Крипт",                               Крипт);
				ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
				ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
				ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
				ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
				ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
				ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
				ДополнительныеПараметры.Вставить("СлучайныйКлюч",                       СлучайныйКлюч);
				ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
				ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
				ДополнительныеПараметры.Вставить("РезультатВыгрузки",                   РезультатВыгрузки);
				ДополнительныеПараметры.Вставить("ИнформацияОРегионе",                  ИнформацияОРегионе);
				ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
				ДополнительныеПараметры.Вставить("КодИНаименованиеРегиона",             КодИНаименованиеРегиона);
				ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
				
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизацииЗавершение",
					ЭтотОбъект,
					ДополнительныеПараметры);
				
				ВызватьФормуВводаCAPTCHA(Данные64CAPTCHA, , ОписаниеОповещения);
				Возврат;
			КонецЕсли;
			
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
			ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
			ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
			ДополнительныеПараметры.Вставить("Форма",                               Форма);
			ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
			ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
			ДополнительныеПараметры.Вставить("Крипт",                               Крипт);
			ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
			ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
			ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
			ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
			ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
			ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
			ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
			ДополнительныеПараметры.Вставить("РезультатВыгрузки",                   РезультатВыгрузки);
			ДополнительныеПараметры.Вставить("ИнформацияОРегионе",                  ИнформацияОРегионе);
			ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
			ДополнительныеПараметры.Вставить("КодИНаименованиеРегиона",             КодИНаименованиеРегиона);
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			
			Оповещение = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизацииПослеФормированияПодписанногоПакетаДляПортала",
				ЭтотОбъект,
				ДополнительныеПараметры);
			
			СформироватьПодписанныйПакетДляПорталаФСРАР(
				МестоХраненияКлюча,
				УчетнаяЗапись,
				ПодписываемыеДанные,
				ПараметрыФормированияПакета.СертификатАбонента,
				Оповещение,
				Крипт,
				СлучайныйКлюч,
				ЗначениеCAPTCHA);
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПолученияПодписанногоПакетаДляАвторизации(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизацииПослеФормированияПодписанногоПакетаДляПортала(ПодписанныйПакетДляАвторизации, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПолученияПодписанногоПакетаДляАвторизации(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизацииЗавершение(ЗначениеCAPTCHA, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПодписанныйПакетДляАвторизации      = ДополнительныеПараметры.ПодписанныйПакетДляАвторизации;
	ПодписываемыеДанные                 = ДополнительныеПараметры.ПодписываемыеДанные;
	СлучайныйКлюч                       = ДополнительныеПараметры.СлучайныйКлюч;
	
	Оповещение = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизацииПослеФормированияПодписанногоПакетаДляПорталаЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	СформироватьПодписанныйПакетДляПорталаФСРАР(
		МестоХраненияКлюча,
		УчетнаяЗапись,
		ПодписываемыеДанные,
		ПараметрыФормированияПакета.СертификатАбонента,
		Оповещение,
		Крипт,
		СлучайныйКлюч,
		ЗначениеCAPTCHA);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизацииПослеФормированияПодписанногоПакетаДляПорталаЗавершение(
	ПодписанныйПакетДляАвторизации,
	ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПолученияПодписанногоПакетаДляАвторизации(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеПолученияПодписанногоПакетаДляАвторизации(
	ОтчетСсылка,
	УчетнаяЗапись,
	ФедеральныйПортал,
	Форма,
	ЭтоРегламентированныйОтчет,
	МестоХраненияКлюча,
	СертификатыИПараметрыОбмена,
	ПараметрыСоединения,
	РезультатВыгрузки,
	ПараметрыФормированияПакета,
	ИнформацияОРегионе,
	СхемаСдачиОтчетности,
	КороткоеИмяФайлаПакета,
	Крипт,
	СтрокаBase64ФайлаПакета,
	СтрокаBase64ФайлаОтправки,
	КодИНаименованиеРегиона,
	ФормаДлительнаяОперация,
	ПараметрыОтправки,
	ПодписанныйПакетДляАвторизации)
	
	РезультатОтправки = Неопределено;																										
																											
	Если ЗначениеЗаполнено(ПодписанныйПакетДляАвторизации) 
		ИЛИ СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы") Тогда
		
		ПерваяПопыткаАвторизации = Истина;
		ПараметрыСоединения.ПовторятьЗапросПароля = Истина;
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРВводПароля(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки,
			ПодписанныйПакетДляАвторизации,
			РезультатОтправки,
			ПерваяПопыткаАвторизации);
			
		Возврат;			
																			
	КонецЕсли;

	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеОтправки(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации,
		РезультатОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРВводПароля(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации,
		РезультатОтправки,
		ПерваяПопыткаАвторизации)
																				
	Данные64CAPTCHA = "";
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаПароля(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Данные64CAPTCHA,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки,
			ПодписанныйПакетДляАвторизации,
			РезультатОтправки,
			ПерваяПопыткаАвторизации);
	
КонецПроцедуры	

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаПароля(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Данные64CAPTCHA,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации,
		РезультатОтправки,
		ПерваяПопыткаАвторизации)
	
	РезультатОтправки = ОтправитьВФСРАРНаСервере(
		МестоХраненияКлюча,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		ПодписанныйПакетДляАвторизации,
		КороткоеИмяФайлаПакета,
		ОтчетСсылка,
		ПараметрыОтправки,
		ПараметрыСоединения,
		Данные64CAPTCHA);

	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	Если РезультатОтправки = Неопределено И ЗначениеЗаполнено(Данные64CAPTCHA) Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("РезультатВыгрузки",                   РезультатВыгрузки);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("ИнформацияОРегионе",                  ИнформацияОРегионе);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Крипт",                               Крипт);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("КодИНаименованиеРегиона",             КодИНаименованиеРегиона);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизации",      ПодписанныйПакетДляАвторизации);
		ДополнительныеПараметры.Вставить("РезультатОтправки",                   РезультатОтправки);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации",            ПерваяПопыткаАвторизации);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаПароляЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ВызватьФормуВводаCAPTCHA(Данные64CAPTCHA, НСтр("ru = 'Для отправки отчета просим ввести защитный код.'"), ОписаниеОповещения);
		
		Возврат;
		
	ИначеЕсли РезультатОтправки = Неопределено И ПараметрыСоединения.ТребуетсяПовторнаяАвторизация = Истина Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизации(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки);
		
		Возврат;
		
	КонецЕсли;
	
	Если РезультатОтправки = Неопределено И ПараметрыСоединения.ТребуетсяРегистрация = Истина Тогда
		
		СлучайныйКлюч   = "";
		Данные64CAPTCHA = "";
		
		ПодписываемыеДанные = ПолучитьПодписываемыеДанныеФСРАРНаСервере(
			Ложь,
			СлучайныйКлюч,
			Данные64CAPTCHA,
			Неопределено,
			ЭтоРегламентированныйОтчет,
			ПараметрыОтправки,
			ПараметрыСоединения,
			Истина);
		
		ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);

		Если ПараметрыОтправки.ФедеральныйПортал 
			ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы")
			ИЛИ ПараметрыОтправки.СхемаСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы") Тогда
			АдресСервера = "service.fsrar.ru";
		Иначе
			АдресСервера = "service.alcolicenziat.ru";
		КонецЕсли;
		
		АдресСервера = ПолучитьЗаменяемоеЗначениеТестовогоСервера1С(АдресСервера);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("РезультатВыгрузки",                   РезультатВыгрузки);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("ИнформацияОРегионе",                  ИнформацияОРегионе);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Крипт",                               Крипт);
		ДополнительныеПараметры.Вставить("СлучайныйКлюч",                       СлучайныйКлюч);
		ДополнительныеПараметры.Вставить("ПодписываемыеДанные",                 ПодписываемыеДанные);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("КодИНаименованиеРегиона",             КодИНаименованиеРегиона);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизации",      ПодписанныйПакетДляАвторизации);
		ДополнительныеПараметры.Вставить("РезультатОтправки",                   РезультатОтправки);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации",            ПерваяПопыткаАвторизации);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаРегистрационнойИнформацииДляПорталаФСРАР",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ВызватьФормуРегистрацииНаПорталеФСРАР("", "https://" + АдресСервера, Оповещение);
		
		Возврат;
	КонецЕсли;
	
	ПерваяПопыткаАвторизации = Ложь;
	
	// Рекурсивный вызов по условию (замена цикла)
	Если РезультатОтправки = Неопределено И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
			
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРВводПароля(
				ОтчетСсылка,
				УчетнаяЗапись,
				ФедеральныйПортал,
				Форма,
				ЭтоРегламентированныйОтчет,
				МестоХраненияКлюча,
				СертификатыИПараметрыОбмена,
				ПараметрыСоединения,
				РезультатВыгрузки,
				ПараметрыФормированияПакета,
				ИнформацияОРегионе,
				СхемаСдачиОтчетности,
				КороткоеИмяФайлаПакета,
				Крипт,
				СтрокаBase64ФайлаПакета,
				СтрокаBase64ФайлаОтправки,
				КодИНаименованиеРегиона,
				ФормаДлительнаяОперация,
				ПараметрыОтправки,
				ПодписанныйПакетДляАвторизации,
				РезультатОтправки,
				ПерваяПопыткаАвторизации);
				
		Возврат;
		
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);

	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеОтправки(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки,
			ПодписанныйПакетДляАвторизации,
			РезультатОтправки);
																												
КонецПроцедуры
																		
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаПароляЗавершение(ЗначениеCAPTCHA, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПодписанныйПакетДляАвторизации      = ДополнительныеПараметры.ПодписанныйПакетДляАвторизации;
	РезультатОтправки                   = ДополнительныеПараметры.РезультатОтправки;
	ПерваяПопыткаАвторизации            = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	
	Если НЕ ЗначениеЗаполнено(ЗначениеCAPTCHA) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		СтрокаBase64ФайлаПакета = РезультатВыгрузки.ИмяФайлаЗашифрованногоПакета;
		КороткоеИмяФайлаПакета = РезультатВыгрузки.ИмяФайлаВыгрузки + ".sig.zip.enc";
		
		Результат = Новый Структура();
		Результат.Вставить("Выполнено",      Истина);
		Результат.Вставить("ДвоичныеДанные", Неопределено);
		Результат.Вставить("СтрокаBase64",   СтрокаBase64ФайлаПакета);
		
		ДополнительныеПараметры.Вставить("ЗначениеCAPTCHA", 		ЗначениеCAPTCHA);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета", 	КороткоеИмяФайлаПакета);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеЧтенияФайлаВыгрузкиПакетаИВводаПароляЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	Иначе
		КороткоеИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(РезультатВыгрузки).Имя;
		
		ДополнительныеПараметры.Вставить("ЗначениеCAPTCHA", 		ЗначениеCAPTCHA);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета", 	КороткоеИмяФайлаПакета);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеЧтенияФайлаВыгрузкиПакетаИВводаПароляЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, РезультатВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеЧтенияФайлаВыгрузкиПакетаИВводаПароляЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	// отправка после получения значения CAPTCHA для отправки
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПодписанныйПакетДляАвторизации      = ДополнительныеПараметры.ПодписанныйПакетДляАвторизации;
	РезультатОтправки                   = ДополнительныеПараметры.РезультатОтправки;
	ПерваяПопыткаАвторизации            = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	ЗначениеCAPTCHA                     = ДополнительныеПараметры.ЗначениеCAPTCHA;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаПакета = Результат.СтрокаBase64;
	
	СтрокаBase64ФайлаОтправки = СформироватьФайлОтправкиВФСРАРНаСервере(
		КороткоеИмяФайлаПакета,
		СхемаСдачиОтчетности,
		СтрокаBase64ФайлаПакета,
		НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
		Форма.УникальныйИдентификатор,
		ЗначениеCAPTCHA);
	
	Данные64CAPTCHA = "";
	Крипт = СертификатыИПараметрыОбмена.Крипт;
	
	ПараметрыСоединения.ТребуетсяРегистрация          = Неопределено;
	ПараметрыСоединения.ТребуетсяПовторнаяАвторизация = Неопределено;
	
	РезультатОтправки = ОтправитьВФСРАРНаСервере(
		МестоХраненияКлюча,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		"",
		КороткоеИмяФайлаПакета,
		ОтчетСсылка,
		ПараметрыОтправки,
		ПараметрыСоединения,
		Данные64CAPTCHA);
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);

	ПерваяПопыткаАвторизации = Ложь;
	
	// Рекурсивный вызов по условию (замена цикла)
	Если РезультатОтправки = Неопределено И ЗначениеЗаполнено(Данные64CAPTCHA) Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("РезультатВыгрузки",                   РезультатВыгрузки);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("ИнформацияОРегионе",                  ИнформацияОРегионе);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Крипт",                               Крипт);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("КодИНаименованиеРегиона",             КодИНаименованиеРегиона);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПодписанныйПакетДляАвторизации",      ПодписанныйПакетДляАвторизации);
		ДополнительныеПараметры.Вставить("РезультатОтправки",                   РезультатОтправки);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации",            ПерваяПопыткаАвторизации);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаПароляЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ВызватьФормуВводаCAPTCHA(Данные64CAPTCHA, НСтр("ru = 'Для отправки отчета просим ввести защитный код.'"), ОписаниеОповещения);
		
		Возврат;
		
	КонецЕсли;
	
	Если РезультатОтправки = Неопределено И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
			
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРВводПароля(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки,
			ПодписанныйПакетДляАвторизации,
			РезультатОтправки,
			ПерваяПопыткаАвторизации);
					
		Возврат;
		
	КонецЕсли;

	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеОтправки(
		ОтчетСсылка,
		УчетнаяЗапись,
		ФедеральныйПортал,
		Форма,
		ЭтоРегламентированныйОтчет,
		МестоХраненияКлюча,
		СертификатыИПараметрыОбмена,
		ПараметрыСоединения,
		РезультатВыгрузки,
		ПараметрыФормированияПакета,
		ИнформацияОРегионе,
		СхемаСдачиОтчетности,
		КороткоеИмяФайлаПакета,
		Крипт,
		СтрокаBase64ФайлаПакета,
		СтрокаBase64ФайлаОтправки,
		КодИНаименованиеРегиона,
		ФормаДлительнаяОперация,
		ПараметрыОтправки,
		ПодписанныйПакетДляАвторизации,
		РезультатОтправки);
																					
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеВводаРегистрационнойИнформацииДляПорталаФСРАР(РезультатФормы, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СлучайныйКлюч                       = ДополнительныеПараметры.СлучайныйКлюч;
	ПодписываемыеДанные                 = ДополнительныеПараметры.ПодписываемыеДанные;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПодписанныйПакетДляАвторизации      = ДополнительныеПараметры.ПодписанныйПакетДляАвторизации;
	РезультатОтправки                   = ДополнительныеПараметры.РезультатОтправки;
	ПерваяПопыткаАвторизации            = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	
	Если РезультатФормы = Неопределено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		Возврат;
	КонецЕсли;
	
	EMail = РезультатФормы.EMail;
	
	Оповещение = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеФормированияПодписанногоПакетаДляПортала",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	СформироватьПодписанныйПакетДляПорталаФСРАР(
		МестоХраненияКлюча,
		УчетнаяЗапись,
		ПодписываемыеДанные,
		ПараметрыФормированияПакета.СертификатАбонента,
		Оповещение,
		Крипт,
		СлучайныйКлюч,
		"",
		Истина,
		EMail);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРОтправкаПослеФормированияПодписанногоПакетаДляПортала(ПодписанныйПакетДляРегистрации, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПодписанныйПакетДляАвторизации      = ДополнительныеПараметры.ПодписанныйПакетДляАвторизации;
	РезультатОтправки                   = ДополнительныеПараметры.РезультатОтправки;
	ПерваяПопыткаАвторизации            = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
		
	РезультатРегистрации = ЗарегистрироватьсяВФСРАРНаСервере(
		ПодписанныйПакетДляРегистрации,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ЭтоРегламентированныйОтчет);
		
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);

	Если РезультатРегистрации Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизации(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки);
			
	Иначе
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеОтправки(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки,
			ПодписанныйПакетДляАвторизации,
			РезультатОтправки);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПослеОтправки(
	ОтчетСсылка,
	УчетнаяЗапись,
	ФедеральныйПортал,
	Форма,
	ЭтоРегламентированныйОтчет,
	МестоХраненияКлюча,
	СертификатыИПараметрыОбмена,
	ПараметрыСоединения,
	РезультатВыгрузки,
	ПараметрыФормированияПакета,
	ИнформацияОРегионе,
	СхемаСдачиОтчетности,
	КороткоеИмяФайлаПакета,
	Крипт,
	СтрокаBase64ФайлаПакета,
	СтрокаBase64ФайлаОтправки,
	КодИНаименованиеРегиона,
	ФормаДлительнаяОперация,
	ПараметрыОтправки,
	ПодписанныйПакетДляАвторизации,
	РезультатОтправки)

	Если РезультатОтправки <> Неопределено Тогда
		Если РезультатОтправки.УспешноОбработана = Ложь Тогда
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
			
		Иначе
			
			ПараметрыАвтозапроса = Новый Структура();
			ПараметрыАвтозапроса.Вставить("Ключ",              РезультатОтправки.НоваяОтправкаСсылка);
			ПараметрыАвтозапроса.Вставить("ФедеральныйПортал", ПараметрыОтправки.ФедеральныйПортал);
			ПараметрыАвтозапроса.Вставить("КодРегиона",        ПараметрыОтправки.КодРегиона);
			ПараметрыАвтозапроса.Вставить("ЗначениеCookie",    ПараметрыСоединения.ЗначениеCookie);
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
			
			ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФСРАР.
			
		КонецЕсли;
		
	ИначеЕсли ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// не получено файла результата при отправке пакета. Возможно, проблемы с доступом в интернет
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("УчетнаяЗапись",                       УчетнаяЗапись);
		ДополнительныеПараметры.Вставить("ФедеральныйПортал",                   ФедеральныйПортал);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("ЭтоРегламентированныйОтчет",          ЭтоРегламентированныйОтчет);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("РезультатВыгрузки",                   РезультатВыгрузки);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("ИнформацияОРегионе",                  ИнформацияОРегионе);
		ДополнительныеПараметры.Вставить("СхемаСдачиОтчетности",                СхемаСдачиОтчетности);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Крипт",                               Крипт);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("КодИНаименованиеРегиона",             КодИНаименованиеРегиона);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВФСРАРПослеПоказаОшибкиДоступаВИнтернет", ЭтотОбъект, ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу Росалкогольтабакконтроля'"),
			ОписаниеОповещения);
			
		Возврат;
		
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();//ФСРАР.
		
	КонецЕсли;
			
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВФСРАРПослеПоказаОшибкиДоступаВИнтернет(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	УчетнаяЗапись                       = ДополнительныеПараметры.УчетнаяЗапись;
	ФедеральныйПортал                   = ДополнительныеПараметры.ФедеральныйПортал;
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоРегламентированныйОтчет          = ДополнительныеПараметры.ЭтоРегламентированныйОтчет;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	РезультатВыгрузки                   = ДополнительныеПараметры.РезультатВыгрузки;
	ПараметрыФормированияПакета         = ДополнительныеПараметры.ПараметрыФормированияПакета;
	ИнформацияОРегионе                  = ДополнительныеПараметры.ИнформацияОРегионе;
	СхемаСдачиОтчетности                = ДополнительныеПараметры.СхемаСдачиОтчетности;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Крипт                               = ДополнительныеПараметры.Крипт;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	КодИНаименованиеРегиона             = ДополнительныеПараметры.КодИНаименованиеРегиона;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
															
    ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// Рекурсивный вызов по условию (замена цикла)
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФСРАРПолучитьПодписанныйПакетДляАвторизации(
			ОтчетСсылка,
			УчетнаяЗапись,
			ФедеральныйПортал,
			Форма,
			ЭтоРегламентированныйОтчет,
			МестоХраненияКлюча,
			СертификатыИПараметрыОбмена,
			ПараметрыСоединения,
			РезультатВыгрузки,
			ПараметрыФормированияПакета,
			ИнформацияОРегионе,
			СхемаСдачиОтчетности,
			КороткоеИмяФайлаПакета,
			Крипт,
			СтрокаBase64ФайлаПакета,
			СтрокаBase64ФайлаОтправки,
			КодИНаименованиеРегиона,
			ФормаДлительнаяОперация,
			ПараметрыОтправки);
			
	Иначе
			
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФСРАР. Нет доступа в Интернет.
		
	КонецЕсли;
	
КонецПроцедуры
// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаСервере
Функция ЗначенияРеквизитовОбъекта(Ссылка, Знач Реквизиты) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, Реквизиты);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПодписанныйПакетДляПорталаФСРАР(МестоХраненияКлюча, УчетнаяЗапись, ПодписываемыеДанные, СертификатАбонента, ВыполняемоеОповещение, Крипт = Неопределено, СлучайныйКлюч = "", ЗначениеCAPTCHA = "", ДляРегистрации = Ложь, Email = "")
	
	Если СертификатАбонента = Неопределено Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Не удалось определить сертификат!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
			
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ДополнительныеПараметры.Вставить("ПодписываемыеДанные", ПодписываемыеДанные);
	ДополнительныеПараметры.Вставить("СертификатАбонента", СертификатАбонента);
	ДополнительныеПараметры.Вставить("Крипт", Крипт);
	ДополнительныеПараметры.Вставить("СлучайныйКлюч", СлучайныйКлюч);
	ДополнительныеПараметры.Вставить("ЗначениеCAPTCHA", ЗначениеCAPTCHA);
	ДополнительныеПараметры.Вставить("ДляРегистрации", ДляРегистрации);
	ДополнительныеПараметры.Вставить("Email", Email);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		ПодписываемыеДанныеАдрес = ПоместитьВоВременноеХранилище(ПодписываемыеДанные, ЭтотОбъект.УникальныйИдентификатор);
		ДополнительныеПараметры.Вставить("ПодписываемыеДанныеАдрес", ПодписываемыеДанныеАдрес);
		
		ПодписываемыеДанныеВUnicodeBase64Адрес = ПреобразоватьПодписываемыеДанныеФСРАРНаСервере(ПодписываемыеДанныеАдрес, ЭтотОбъект.УникальныйИдентификатор);
		ДополнительныеПараметры.Вставить("ПодписываемыеДанныеВUnicodeBase64Адрес", ПодписываемыеДанныеВUnicodeBase64Адрес);
		
		ДополнительныеПараметры.Вставить("ВременныйКаталог", "");
		ДополнительныеПараметры.Вставить("ИмяФайлаПодписываемыхДанныхВUnicode", "");
	
		Оповещение = Новый ОписаниеОповещения("СформироватьПодписанныйПакетДляПорталаФСРАРПослеЗаписиФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено", Истина));
		Возврат;
		
	КонецЕсли;
	
	ПодписываемыеДанныеВUnicodeBase64 = СтрокаСимволовПервойПоловиныASCIIВUnicodeBase64НаСервере(ПодписываемыеДанные);
	ДополнительныеПараметры.Вставить("ПодписываемыеДанныеВUnicodeBase64", ПодписываемыеДанныеВUnicodeBase64);
	
	Оповещение = Новый ОписаниеОповещения("СформироватьПодписанныйПакетДляПорталаФСРАРПослеПолученияВременногоКаталогаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.КаталогВременныхФайловНаКлиенте(Оповещение);
			
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПодписанныйПакетДляПорталаФСРАРПослеПолученияВременногоКаталогаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ПодписываемыеДанныеВUnicodeBase64 = ДополнительныеПараметры.ПодписываемыеДанныеВUnicodeBase64;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВременныйКаталог = Результат.ИмяКаталога;
	ИмяФайлаПодписываемыхДанныхВUnicode = ВременныйКаталог + Строка(Новый УникальныйИдентификатор) + ".dat";
	
	ДополнительныеПараметры.Вставить("ВременныйКаталог", ВременныйКаталог);
	ДополнительныеПараметры.Вставить("ИмяФайлаПодписываемыхДанныхВUnicode", ИмяФайлаПодписываемыхДанныхВUnicode);

	Оповещение = Новый ОписаниеОповещения("СформироватьПодписанныйПакетДляПорталаФСРАРПослеЗаписиФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПодписываемыеДанныеВUnicodeBase64, ИмяФайлаПодписываемыхДанныхВUnicode);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПодписанныйПакетДляПорталаФСРАРПослеЗаписиФайлаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог 					= ДополнительныеПараметры.ВременныйКаталог;
	Крипт 								= ДополнительныеПараметры.Крипт;
	ИмяФайлаПодписываемыхДанныхВUnicode = ДополнительныеПараметры.ИмяФайлаПодписываемыхДанныхВUnicode;
	СертификатАбонента					= ДополнительныеПараметры.СертификатАбонента;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	УчетнаяЗапись						= ДополнительныеПараметры.УчетнаяЗапись;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаПодписи = ВременныйКаталог + Строка(Новый УникальныйИдентификатор) + ".sig";
	ДополнительныеПараметры.Вставить("ИмяФайлаПодписи", ИмяФайлаПодписи);

	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ИмяФайлаПодписи = ПоместитьВоВременноеХранилище(Неопределено, ЭтотОбъект.УникальныйИдентификатор);
		ИмяФайлаПодписываемыхДанныхВUnicode = ДополнительныеПараметры.ПодписываемыеДанныеВUnicodeBase64Адрес;
		
		ДополнительныеПараметры.Вставить("ИмяФайлаПодписиАдрес", ИмяФайлаПодписи);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатАбонента);
	КонецЕсли;

	Оповещение = Новый ОписаниеОповещения("СформироватьПодписанныйПакетДляПорталаФСРАРПослеПодписанияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаПодписываемыхДанныхВUnicode);
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ПакетДляОтправкиВФСРАР", УчетнаяЗапись); 
		ПараметрыОперации = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
	КонецЕсли;
	
	КриптографияЭДКОКлиент.ПодписатьPKCS7(Оповещение, СертификатАбонента, ИмяФайлаПодписываемыхДанныхВUnicode, Истина, ИмяФайлаПодписи, Ложь, , , ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПодписанныйПакетДляПорталаФСРАРПослеПодписанияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		Ошибка = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Не удалось подписать информацию для авторизации:
				|%1'"), ИнформацияОбОшибке().Описание);

		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ДополнительныеПараметры) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(Ошибка);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Ошибка);
			ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	ИмяФайлаПодписи = ДополнительныеПараметры.ИмяФайлаПодписи;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Оповещение = Новый ОписаниеОповещения("СформироватьПодписанныйПакетДляПорталаФСРАРПослеПолученияЗакодированнойПодписиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		ИмяФайлаПодписиBase64Адрес = КодироватьВBase64СодержимоеНаСервере(ДополнительныеПараметры.ИмяФайлаПодписиАдрес, ЭтотОбъект.УникальныйИдентификатор);
		ДополнительныеПараметры.Вставить("ИмяФайлаПодписиBase64Адрес", ИмяФайлаПодписиBase64Адрес);
		СтрокаBase64 = ПолучитьИзВременногоХранилища(ИмяФайлаПодписиBase64Адрес);		
		ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено, СтрокаBase64", Истина, СтрокаBase64));
		Возврат;
		
	КонецЕсли;
	
	ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ИмяФайлаПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПодписанныйПакетДляПорталаФСРАРПослеПолученияЗакодированнойПодписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПодписываемыеДанные = ДополнительныеПараметры.ПодписываемыеДанные;
	СертификатАбонента = ДополнительныеПараметры.СертификатАбонента;
	Крипт = ДополнительныеПараметры.Крипт;
	СлучайныйКлюч = ДополнительныеПараметры.СлучайныйКлюч;
	ЗначениеCAPTCHA = ДополнительныеПараметры.ЗначениеCAPTCHA;
	ДляРегистрации = ДополнительныеПараметры.ДляРегистрации;
	Email = ДополнительныеПараметры.Email;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ЗакодированнаяПодпись = Результат.СтрокаBase64;
	
	ЗакодированнаяПодписьДляСсылки = ЗакодированныйUTF8ПараметрСсылки(ЗакодированнаяПодпись, "BASE64");
	
	ВладелецДляСсылки = "";
	EmailДляСсылки = "";
	Если ДляРегистрации Тогда
		СвойстваВладельца = РазобратьСтрокуСубъекта(СертификатАбонента.Владелец);
		Для каждого СвойствоВладельца Из СвойстваВладельца Цикл
			ИмяСвойства = СвойствоВладельца.Ключ;
			ЗначениеСвойства = СвойствоВладельца.Значение;
			Если СтрНайти(ЗначениеСвойства, """") > 0 Тогда
				ЗначениеСвойства = """" + СтрЗаменить(ЗначениеСвойства, """", """""") + """";
			КонецЕсли;
			ЗначениеСвойства = СтрЗаменить(ЗначениеСвойства, "\", "\\");
			ЗначениеСвойства = СтрЗаменить(ЗначениеСвойства, """", "\""");
			ВладелецДляСсылки = ВладелецДляСсылки + ?(ВладелецДляСсылки <> "", ",", "") + """" + ИмяСвойства + """:""" + ЗначениеСвойства + """";
		КонецЦикла;
		ВладелецДляСсылки = ЗакодированныйUTF8ПараметрСсылки("{" + ВладелецДляСсылки + "}");
		
		EmailДляСсылки = ЗакодированныйUTF8ПараметрСсылки(Email);
	КонецЕсли;
	
	Результат = "user_sign=" + ЗакодированнаяПодписьДляСсылки + ?(НЕ ДляРегистрации, "&user_type=user", "") + "&thumbprint=" + ВРег(СертификатАбонента.Отпечаток)
		+ ?(НЕ ДляРегистрации, "", "&subject=" + ВладелецДляСсылки) + "&csrf=" + СлучайныйКлюч + ?(НЕ ДляРегистрации, "", "&email=" + EmailДляСсылки)
		+ ?(ЗначениеCAPTCHA <> "", "&captcha_code=" + ЗначениеCAPTCHA, "");
	
	ВыполняемоеОповещение.ДополнительныеПараметры.Вставить("Крипт", Крипт);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура ВызватьФормуРегистрацииНаПорталеФСРАР(EMail, Портал, ВыполняемоеОповещение)
	
	ПараметрыФормы = Новый Структура("EMail, Портал", EMail, Портал);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.РегистрацияНаПорталеФСРАР", ПараметрыФормы,,,,, ВыполняемоеОповещение, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ВызватьФормуВводаCAPTCHA(Данные64CAPTCHA, ПричинаЗапросаКода = "", ВыполняемоеОповещение = Неопределено)
	
	ПараметрыФормы = Новый Структура("Данные64КартинкиСКодом, ПричинаЗапросаКода", Данные64CAPTCHA, ПричинаЗапросаКода);
	ОписаниеОповещения = Новый ОписаниеОповещения("ВызватьФормуВводаCAPTCHAЗавершение", ЭтотОбъект, ВыполняемоеОповещение);	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ВводCAPTCHA", ПараметрыФормы,,,,, ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ВызватьФормуВводаCAPTCHAЗавершение(РезультатФормы, ВыполняемоеОповещение) Экспорт
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Строка(РезультатФормы));
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСРАР(ИсточникСсылка, ПечатьВозможна = Истина) Экспорт
	
	ВАрхиве = ОбъектВАрхиве(ИсточникСсылка, "Протокол");
	Если ВАрхиве Тогда 
		ПоказатьУведомлениеАрхивныхФайлов(, 16, 0, Истина);
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ИсточникСсылка",      ИсточникСсылка);
	ПараметрыФормы.Вставить("ПечатьВозможна",      ПечатьВозможна);
	ПараметрыФормы.Вставить("КонтролирующийОрган", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР"));
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПротоколОбработкиHTML", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлНаКлиенте(ИмяВременногоФайла, ВыполняемоеОповещение) Экспорт
	
	Если ИмяВременногоФайла = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	#Если ВебКлиент Тогда
		Если ТипЗнч(ИмяВременногоФайла) = Тип("Структура") Тогда // ЭП в облаке
			ПолучитьФайл(ИмяВременногоФайла.ИмяФайлаЗашифрованногоПакета, ИмяВременногоФайла.ИмяФайлаВыгрузки, Истина);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
		Иначе
			ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ИмяВременногоФайла", ВыполняемоеОповещение, ИмяВременногоФайла);
			ОписаниеОповещения = Новый ОписаниеОповещения("СохранитьФайлНаКлиентеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ТекстСообщения = ОперацииСФайламиЭДКОКлиент.ТекстСообщенияДляНеобязательнойУстановкиРасширенияРаботыСФайлами();
			ФайловаяСистемаКлиент.ПодключитьРасширениеДляРаботыСФайлами(ОписаниеОповещения, ТекстСообщения, Истина); 
		КонецЕсли;
		
	#Иначе
		СохранитьФайлНаКлиентеНеВеб(ИмяВременногоФайла, ВыполняемоеОповещение);
	#КонецЕсли
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлНаКлиентеНеВеб(ИмяВременногоФайла, ВыполняемоеОповещение) Экспорт
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
	Диалог.Заголовок = НСтр("ru = 'Укажите каталог'");
	Диалог.ПроверятьСуществованиеФайла = Истина;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ИмяВременногоФайла", ИмяВременногоФайла);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СохранитьФайлНаКлиентеНеВебПослеВыбораКаталога", 
		ЭтотОбъект, 
		ДополнительныеПараметры); 
	
	Диалог.Показать(ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлНаКлиентеНеВебПослеВыбораКаталога(ВыбранныеФайлы, ВходящийКонтекст) Экспорт

	ИмяВременногоФайла		= ВходящийКонтекст.ИмяВременногоФайла;
	ВыполняемоеОповещение	= ВходящийКонтекст.ВыполняемоеОповещение;
	
	Если ВыбранныеФайлы = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
		Возврат;
	КонецЕсли;
	
	Путь = ВыбранныеФайлы[0];
	Путь = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(Путь);
	
	Если ТипЗнч(ИмяВременногоФайла) = Тип("Структура") Тогда // ЭП в облаке
		ИмяФайлаВыгрузки = ИмяВременногоФайла.ИмяФайлаВыгрузки;
		ПолучитьИзВременногоХранилища(ИмяВременногоФайла.ИмяФайлаЗашифрованногоПакета).Записать(Путь + ИмяФайлаВыгрузки);// Двоичные данные, лежащие во временном хранилище
		ПоказатьПредупреждение(, НСтр("ru = 'Выгрузка завершена.'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	Иначе
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СохранитьФайлНаКлиентеНеВебПослеПеремещенияФайла", 
			ЭтотОбъект, 
			ВходящийКонтекст);
		
		ПолноеИмяКонечногоФайла = Путь + ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяВременногоФайла).Имя;
		НачатьПеремещениеФайла(ОписаниеОповещения, ИмяВременногоФайла, ПолноеИмяКонечногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлНаКлиентеНеВебПослеПеремещенияФайла(ПеремещаемыйФайл, ВходящийКонтекст) Экспорт

	ПоказатьПредупреждение(, НСтр("ru = 'Выгрузка завершена.'"));
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлНаКлиентеЗавершение(ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяВременногоФайла = ДополнительныеПараметры.ИмяВременногоФайла;
	
	Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
		ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ИмяВременногоФайла", ВыполняемоеОповещение, ИмяВременногоФайла);
		Оповещение = Новый ОписаниеОповещения("СохранитьФайлНаКлиентеПослеЧтенияФайлаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ИмяВременногоФайла);
		Возврат;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлНаКлиентеПослеЧтенияФайлаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяВременногоФайла = ДополнительныеПараметры.ИмяВременногоФайла;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ВременногоФайла = Результат.СтрокаBase64;
	
	ПолучитьФайл(ПоместитьФайлВоВременноеХранилищеНаСервере(СтрокаBase64ВременногоФайла),
		ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяВременногоФайла).Имя, Истина);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Функция ЗакодированныйUTF8ПараметрСсылки(ПараметрСсылки, ФорматДанных = "")
	
	Если ВРег(ФорматДанных) = "BASE64" Тогда
		
		Результат = СтрЗаменить(ПараметрСсылки, "+", "%2B");
		Результат = СтрЗаменить(Результат, "/", "%2F");
		Результат = СтрЗаменить(Результат, "=", "%3D");
		Результат = СтрЗаменить(Результат, Символы.ВК, "%0D");
		Результат = СтрЗаменить(Результат, Символы.ПС, "%0A");
		Возврат Результат;
		
	КонецЕсли;
	
	ТаблицаШестнадцатеричныхПредставлений
		= "000102030405060708090A0B0C0D0E0F101112131415161718191A1B1C1D1E1F"
		+ "202122232425262728292A2B2C2D2E2F303132333435363738393A3B3C3D3E3F"
		+ "404142434445464748494A4B4C4D4E4F505152535455565758595A5B5C5D5E5F"
		+ "606162636465666768696A6B6C6D6E6F707172737475767778797A7B7C7D7E7F"
		+ "808182838485868788898A8B8C8D8E8F909192939495969798999A9B9C9D9E9F"
		+ "A0A1A2A3A4A5A6A7A8A9AAABACADAEAFB0B1B2B3B4B5B6B7B8B9BABBBCBDBEBF"
		+ "C0C1C2C3C4C5C6C7C8C9CACBCCCDCECFD0D1D2D3D4D5D6D7D8D9DADBDCDDDEDF"
		+ "E0E1E2E3E4E5E6E7E8E9EAEBECEDEEEFF0F1F2F3F4F5F6F7F8F9FAFBFCFDFEFF";
	
	Результат = "";
	
	Для ИндексСимвола = 1 По СтрДлина(ПараметрСсылки) Цикл
		
		СимволПараметраСсылки = Сред(ПараметрСсылки, ИндексСимвола, 1);
		КодСимволаПараметраСсылки = КодСимвола(СимволПараметраСсылки);
		Если (КодСимволаПараметраСсылки >= КодСимвола("0") И КодСимволаПараметраСсылки <= КодСимвола("9"))
			ИЛИ (КодСимволаПараметраСсылки >= КодСимвола("A") И КодСимволаПараметраСсылки <= КодСимвола("Z"))
			ИЛИ (КодСимволаПараметраСсылки >= КодСимвола("a") И КодСимволаПараметраСсылки <= КодСимвола("z"))
			ИЛИ СимволПараметраСсылки = "_" ИЛИ СимволПараметраСсылки = "*" ИЛИ СимволПараметраСсылки = "-"
			ИЛИ СимволПараметраСсылки = "." Тогда
			
			Результат = Результат + СимволПараметраСсылки
			
		ИначеЕсли СимволПараметраСсылки = " " Тогда
			
			Результат = Результат + "+"
			
		Иначе
			
			Код = КодСимвола(Сред(ПараметрСсылки, ИндексСимвола, 1));
			
			// представить в формате UTF8
			Если Код <= 255 Тогда
				
				Результат = Результат + "%" + Сред(ТаблицаШестнадцатеричныхПредставлений, Код * 2 + 1, 2);
				
			ИначеЕсли Код <= 2047 Тогда
				
				МладшаяЧастьКода = Код % 64;
				СтаршаяЧастьКода = (Код - МладшаяЧастьКода) / 64;
				МладшаяЧастьКода = 128 + МладшаяЧастьКода;
				СтаршаяЧастьКода = 192 + СтаршаяЧастьКода;
				Результат = Результат + "%" + Сред(ТаблицаШестнадцатеричныхПредставлений, СтаршаяЧастьКода * 2 + 1, 2)
					+ "%" + Сред(ТаблицаШестнадцатеричныхПредставлений, МладшаяЧастьКода * 2 + 1, 2);
				
			Иначе
				
				МладшаяЧастьКода = Код % 64;
				СтаршаяЧастьКода = (Код - МладшаяЧастьКода) / 64;
				СредняяЧастьКода = СтаршаяЧастьКода % 64;
				СтаршаяЧастьКода = (СтаршаяЧастьКода - СредняяЧастьКода) / 64;
				МладшаяЧастьКода = 128 + МладшаяЧастьКода;
				СредняяЧастьКода = 128 + СредняяЧастьКода;
				СтаршаяЧастьКода = 224 + СтаршаяЧастьКода;
				Результат = Результат + "%" + Сред(ТаблицаШестнадцатеричныхПредставлений, СтаршаяЧастьКода * 2 + 1, 2)
					+ "%" + Сред(ТаблицаШестнадцатеричныхПредставлений, СредняяЧастьКода * 2 + 1, 2)
					+ "%" + Сред(ТаблицаШестнадцатеричныхПредставлений, МладшаяЧастьКода * 2 + 1, 2);
				
			КонецЕсли;
			
		КонецЕсли;
			
	КонецЦикла;
		
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СхемаСдачиОтчетностиФСРАР(ФедеральныйПортал, ТипПортала, КодРегиона) Экспорт
	
	Если ФедеральныйПортал Тогда
		Результат = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ФедеральныйСервер");
		
	ИначеЕсли ТипПортала = "Общий" Тогда
		Результат = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ОбщийРегиональныйИФедеральныйСервер");
		
	ИначеЕсли ТипПортала = "Типовой" Тогда
		Результат = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы");
		
	ИначеЕсли ТипПортала = "СвояРеализация" И КодРегиона = "77" Тогда
		Результат = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.МосковскийРегиональныйИФедеральныйСерверы");
		
	Иначе
		Результат = ПредопределенноеЗначение("Перечисление.СхемыСдачиОтчетностиФСРАР.СдачаВручную");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьФайлОтправкиВФСРАРНаСервере(КороткоеИмяФайлаВыгрузки, СхемаСдачиОтчетности, СтрокаBase64ФайлаВыгрузки, ЭтоАдреса = Истина, Адрес = Неопределено, ЗначениеCAPTCHA = "")
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СформироватьФайлОтправкиВФСРАРССервера(КороткоеИмяФайлаВыгрузки, СхемаСдачиОтчетности, СтрокаBase64ФайлаВыгрузки, ЭтоАдреса, Адрес, ЗначениеCAPTCHA);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбновитьРезультатКонкретнойОтправкиФСРАРСНаСервере(Знач ОтправкаСсылка, Знач ОтправкаОбъектЗагружен, Знач ПодписанныйПакетДляАвторизации, Знач ПараметрыОтправки, ПараметрыСоединения, ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64 = Неопределено, Знач ВызовИзАвтозапроса = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбновитьРезультатКонкретнойОтправкиФСРАРССервера(ОтправкаСсылка, ОтправкаОбъектЗагружен, ПодписанныйПакетДляАвторизации, ПараметрыОтправки, ПараметрыСоединения, ПодписанныйРегиональнымДляОтправкиНаФедеральныйСерверПакетBase64, ВызовИзАвтозапроса);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВФСРАРНаСервере(МестоХраненияКлюча, Знач СтрокаBase64ФайлаОтправки, Знач СтрокаBase64ФайлаПакета, Знач ПодписанныйПакетДляАвторизации, Знач КороткоеИмяФайлаПакета, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA = "")
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьВФСРАРССервера(МестоХраненияКлюча, СтрокаBase64ФайлаОтправки, СтрокаBase64ФайлаПакета, ПодписанныйПакетДляАвторизации, КороткоеИмяФайлаПакета, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения, Данные64CAPTCHA);
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗарегистрироватьсяВФСРАРНаСервере(Знач ПодписанныйПакетДляРегистрации, Знач ПараметрыОтправки, ПараметрыСоединения, ОтправкаРегламентированногоОтчета = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ЗарегистрироватьсяВФСРАРССервера(ПодписанныйПакетДляРегистрации, ПараметрыОтправки, ПараметрыСоединения, ОтправкаРегламентированногоОтчета);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоляДляЗаписиОтправкиВФСРАРНаСервере(ОбъектСсылка)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПоляНаСервереДляЗаписиОтправкиВФСРАР(ОбъектСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПодписываемыеДанныеФСРАРНаСервере(
	Знач ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале,
	СлучайныйКлюч,
	Данные64CAPTCHA,
	Знач ОтправкаСсылка,
	Знач ЭтоРегламентированныйОтчет,
	Знач ПараметрыОтправки,
	ПараметрыСоединения,
	ДляРегистрации = Ложь,
	ЭтоВызовИзСписка = Ложь)
	
	ВызовИзОтправки = (ОтправкаСсылка = Неопределено); // при отправке декларации ссылки на отправку в ИБ еще нет
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Если ПараметрыОтправки.СхемаСдачиОтчетности <> Перечисления.СхемыСдачиОтчетностиФСРАР.ТиповойРегиональныйИФедеральныйСерверы ИЛИ ПолучениеРезультатовОтправкиПослеРегиональногоНаФедеральномПортале Тогда
		Результат = КонтекстЭДО.ПолучитьПодписываемыеДанныеПорталаФСРАРССервера(СлучайныйКлюч, Данные64CAPTCHA, ПараметрыОтправки, ПараметрыСоединения, ВызовИзОтправки, ДляРегистрации);
		
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
	Если Результат = Неопределено И ПараметрыСоединения.ОписаниеОшибкиСервером <> НСтр("ru = 'Выполнение операции не поддерживается'") Тогда
		
		Если ВызовИзОтправки Тогда
			ТекстСообщения = НСтр("ru = 'Зашифрованный пакет не удалось отправить на сервер Росалкогольтабакконтроля.'") + " " + ?(ЭтоРегламентированныйОтчет,
				НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Выгрузка"" - ""Выгрузить для портала ФСРАР"".'"), НСтр("ru = 'Для выгрузки отчета в файл используйте меню ""Еще"" - ""Выгрузить"".'"))
				+ " " + НСтр("ru = 'С информацией о порядке представления отчета ознакомьтесь на сайте http://service.fsrar.ru'");
			
		Иначе
			ТекстСообщения = НСтр("ru = 'Не удалось получить результаты отправки с сервера Росалкогольтабакконтроля.'");
			
			Если ЭтоВызовИзСписка Тогда
				ПредставлениеОтчетаОтправки = КонтекстЭДО.ПредставлениеОтчета(ОтправкаСсылка.ОтчетСсылка);
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = '%1 Возможно, сервер перегружен. Попробуйте через некоторое время использовать кнопку ""Обновить"" в отчете %2.'"),
					ТекстСообщения,
					ПредставлениеОтчетаОтправки);
			КонецЕсли;
		КонецЕсли;
		
		Если ПараметрыСоединения.СообщенияПользователю <> Неопределено Тогда
			ПараметрыСоединения.СообщенияПользователю.Добавить(ТекстСообщения);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтчетПодаетсяЧерезФедеральныйПорталНаСервере(ОтчетСсылка, ОтправкаСсылка = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтчетПодаетсяЧерезФедеральныйПортал(ОтчетСсылка, ОтправкаСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Функция НастройкиФСРАРНаСервере(Знач ОрганизацияСсылка, Знач ПолучитьПараметрыОнлайнПроверки = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.НастройкиФСРАР(ОрганизацияСсылка, ПолучитьПараметрыОнлайнПроверки);
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойстваОбменаОрганизацииНаСервере(Знач ОрганизацияСсылка, Знач ОпределитьОтправкуЧерезФедеральныйПорталДляОтчета, Знач ОпределитьНастроенностьОбменаВУниверсальномФормате, Знач ПолучитьОсновныеСвойстваПоследнейОтправки, Знач ПолучитьИнформациюОРегионе = Ложь, Знач ОтправкаСсылка = Неопределено, ОтчетСсылка = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СвойстваОбменаОрганизации(ОрганизацияСсылка, ОпределитьОтправкуЧерезФедеральныйПорталДляОтчета, ОпределитьНастроенностьОбменаВУниверсальномФормате, ПолучитьОсновныеСвойстваПоследнейОтправки, ПолучитьИнформациюОРегионе, ОтправкаСсылка, ОтчетСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Функция ЗаписатьZipФайлПакетаНаСервере(Знач СтрокаBase64, Знач ИмяАрхивируемогоФайла)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ЗаписатьZipФайлПакета(СтрокаBase64, ИмяАрхивируемогоФайла);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоместитьФайлВоВременноеХранилищеНаСервере(СтрокаBase64Файла)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПоместитьФайлВоВременноеХранилищеНаСервере(СтрокаBase64Файла);
	
КонецФункции

&НаСервереБезКонтекста
Функция СтрокаСимволовПервойПоловиныASCIIВUnicodeBase64НаСервере(СтрокаСимволовПервойПоловиныASCII)
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ЗаписьТекста = Новый ЗаписьТекста(ИмяВременногоФайла, "windows-1251");
	Для ПозицияСимвола = 1 По СтрДлина(СтрокаСимволовПервойПоловиныASCII) Цикл
		ЗаписьТекста.Записать(Сред(СтрокаСимволовПервойПоловиныASCII, ПозицияСимвола, 1));
		ЗаписьТекста.Записать(Символ(0));
	КонецЦикла;
	ЗаписьТекста.Закрыть();
		
	ДвоичныеДанныеФайла = Новый ДвоичныеДанные(ИмяВременногоФайла);
	Результат = Base64Строка(ДвоичныеДанныеФайла);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
		
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПреобразоватьПодписываемыеДанныеФСРАРНаСервере(ПодписываемыеДанныеАдрес, УникальныйИдентификаторФормы); 
	
	Значение = ПолучитьИзВременногоХранилища(ПодписываемыеДанныеАдрес);
	Результат = СтрокаСимволовПервойПоловиныASCIIВUnicodeBase64НаСервере(Значение);	
	Результат = Base64Значение(Результат);
	
	Возврат ПоместитьВоВременноеХранилище(Результат, УникальныйИдентификаторФормы);
	
КонецФункции

&НаСервереБезКонтекста
Функция КодироватьВBase64СодержимоеНаСервере(АдресДанных, УникальныйИдентификаторФормы)
	
	ДД = ПолучитьИзВременногоХранилища(АдресДанных);
	Результат = Base64Строка(ДД);
	Возврат ПоместитьВоВременноеХранилище(Результат, УникальныйИдентификаторФормы);	
	
КонецФункции

&НаСервереБезКонтекста
Функция ДеКодироватьИзBase64СодержимоеНаСервере(АдресДанных, УникальныйИдентификаторФормы)
	
	Б64 = ПолучитьИзВременногоХранилища(АдресДанных);
	ДД = Base64Значение(Б64);
	
	Возврат ПоместитьВоВременноеХранилище(ДД, УникальныйИдентификаторФормы);	
	
КонецФункции

////////////////////////////////////////////////////////////////////////////////

&НаСервереБезКонтекста
Функция ОпределитьНазваниеСтраны(Страна)
	
	Возврат Справочники.СтраныМира.НайтиПоКоду(Страна).Наименование;
	
КонецФункции

&НаКлиенте
Процедура НапечататьДокумент(ПечДокумент, Заголовок = "", Редактирование = Ложь) Экспорт
	
	Если ПечДокумент = Неопределено тогда
		Возврат;
	КонецЕсли;
	
	Если ПечДокумент.МасштабПечати = 100 Тогда
		ПечДокумент.АвтоМасштаб = Истина;
	КонецЕсли;
	ПечДокумент.ОтображатьЗаголовки = Ложь;
	ПечДокумент.ОтображатьСетку = Ложь;
	ПечДокумент.Защита = Ложь;
	ПечДокумент.ТолькоПросмотр = Истина;
	
	СтруктураПараметров = Новый Структура();
	СтруктураПараметров.Вставить("Заголовок", Заголовок);
	СтруктураПараметров.Вставить("ТабДок",    ПечДокумент);
	
	Если Редактирование Тогда
		СтруктураПараметров.Вставить("Редактирование", Истина);
	КонецЕсли;

	ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаПростойПечати", СтруктураПараметров,, Новый УникальныйИдентификатор());
	
КонецПроцедуры

&НаСервере
Функция ПодготовитьМакет(Знач Объект, Параметры, КлючПараметровПечати, ИмяМакета) Экспорт
	
	ТабДокумент = Новый ТабличныйДокумент;

	ТабДокумент.КлючПараметровПечати = КлючПараметровПечати;
	
	Макет = ПолучитьМакетОбработки(ИмяМакета);
	
	Если ИмяМакета = "ПечатьСоглашение" Тогда
		
		ПолучитьОбластьМакетаСоглашение(Объект, ТабДокумент, Макет,Параметры);

	ИначеЕсли ИмяМакета = "ПечатьСертификат" Тогда
		
		ПолучитьОбластьМакетаСертификат(Объект, ТабДокумент,Макет, Параметры.Сертификат, Параметры.ТекстСертификата, Параметры.Заголовок);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ПолучитьОбластьМакетаСоглашение(Объект, ТабДокумент, Макет,ПараметрыСпецОператора) Экспорт
	
	// Здесь наименование УЦ приходит в уже нужном виде из документа или макета
	ОбластьМакета = Макет.ПолучитьОбласть("Соглашение");
	ЗаполнитьЗначенияСвойств(ОбластьМакета.Параметры, ПараметрыСпецОператора);
	ТабДокумент.Вывести(ОбластьМакета);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучательЗаявления(Заявление) Экспорт
	
	Возврат ДанныеУЦ(Заявление).ПолучательЗаявленияВУЦ;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеУЦ(Заявление) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ДанныеУЦ(Заявление);
	
КонецФункции

&НаСервереБезКонтекста
Процедура ПолучитьОбластьМакетаСертификат(Объект, ТабДокумент, Макет, Сертификат, ТекстСертификата, ЗаголовокОтчета)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	КонтекстЭДОСервер.ПечатьСертификатаНаСервере(ТабДокумент, Base64Значение(ТекстСертификата));
	
КонецПроцедуры

&НаСервере
Функция ЗаявлениеУжеСформировано(Организация) Экспорт
	
	ПоЭтойОрганизацииУжеЕстьЗаявления = ложь;

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Ссылка
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|ГДЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.ПометкаУдаления = ЛОЖЬ
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Организация = &Организация
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Статус <> ЗНАЧЕНИЕ(Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено)";

	Запрос.УстановитьПараметр("Организация", Организация);

	РезультатЗапроса = Запрос.Выполнить();

	Если не РезультатЗапроса.Пустой() Тогда
		ПоЭтойОрганизацииУжеЕстьЗаявления = истина;
	КонецЕсли;
	
	Возврат ПоЭтойОрганизацииУжеЕстьЗаявления;
	
КонецФункции	

&НаКлиенте
Процедура ОткрытьСписокЗаявлений(Организация) Экспорт
	
	ЗначениеОтбора  	= Новый Структура("Организация", Организация);
	СтруктураПараметров = Новый Структура("Отбор", ЗначениеОтбора);
	
	ОткрытьФорму("Документ.ЗаявлениеАбонентаСпецоператораСвязи.ФормаСписка", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьИнструкциюПоПодключениюК1СОтчетности() Экспорт
	
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку("http://its.1c.ru/bmk/elreps/manual");
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьВидеоИнструкциюПоПодключениюК1СОтчетности() Экспорт
	
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку("http://its.1c.ru/bmk/elreps/video");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьРеквизит(Объект, ИмяРеквизита)
	Возврат Объект[ИмяРеквизита];
КонецФункции


&НаКлиенте
Процедура ПечатьСоглашенияобОказанииУслуг(Знач Документ) Экспорт
	
	СпецоператорСвязи = ПолучитьРеквизит(Документ, "СпецоператорСвязи");
	Если НЕ ЗначениеЗаполнено(СпецоператорСвязи) тогда
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не выбран Удостоверяющий центр в документе %1.'"), Строка(Документ));
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
		
	иначе
		
		ТабДок = ТабДокСоглашения(Документ);
		НапечататьДокумент(ТабДок, "Соглашение об оказании услуг");
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ПодменитьДанныеУЦ(Заявление, ПараметрыСпецОператора)
	
	ДанныеУЦ = ДанныеУЦ(Заявление);
	
	Если ПараметрыСпецОператора.Свойство("НаименованиеУЦ") Тогда
		ПараметрыСпецОператора.НаименованиеУЦ = ДанныеУЦ.НаименованиеУЦ;
	КонецЕсли;
	
	Если ПараметрыСпецОператора.Свойство("АдресРегламентУЦ") Тогда
		ПараметрыСпецОператора.АдресРегламентУЦ = ДанныеУЦ.РегламентУЦ;
	КонецЕсли;
	
	Если ПараметрыСпецОператора.Свойство("ЗаявлениеПолучатель") Тогда
		ПараметрыСпецОператора.ЗаявлениеПолучатель = ДанныеУЦ.ПолучательЗаявленияВУЦ;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ТабДокСоглашения(Знач Заявление) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ПараметрыСпецОператора = КонтекстЭДОСервер.ПолучитьПараметрыОператораСвязи(Заявление.СпецоператорСвязи);
	
	ПодменитьДанныеУЦ(Заявление, ПараметрыСпецОператора);
	
	Возврат ПодготовитьМакет(Заявление, ПараметрыСпецОператора, "ПАРАМЕТРЫ_ПЕЧАТИ_ПечатьСоглашение","ПечатьСоглашение");
	
КонецФункции

&НаКлиенте
Функция HTMLДокСоглашения(Заявление) Экспорт
	
	СтруктураДанныхСпецоператорыСвязи = Новый Структура;
	ЗаполнитьДанныеСпецоператораСвязи(СтруктураДанныхСпецоператорыСвязи);
	
	ТекстДляЗамены = Строка(СтруктураДанныхСпецоператорыСвязи.ТекстМакетаСоглашение); // ПечатьСоглашениеHTML
	
	СтруктураПараметра = Новый Структура("НаименованиеУЦ,АдресРегламентУЦ,СетьДоверенныхУЦ,СуммаОтветственностиУЦ,НаименованиеПОРаспространяемогоУЦ");
	
	СпецоператорСвязи = ЗначенияРеквизитовОбъекта(Заявление, "СпецоператорСвязи").СпецоператорСвязи;
	
	ПараметрыСпецоператора = ПолучитьПараметрСпецоператораКлиент(
		СпецоператорСвязи,
		СтруктураПараметра,
		СтруктураДанныхСпецоператорыСвязи.Макет);
		
	ПодменитьДанныеУЦ(Заявление, ПараметрыСпецОператора);
	
	ТекстДляЗамены = СтрЗаменить(ТекстДляЗамены,"#НаименованиеУЦ#",ПараметрыСпецоператора.НаименованиеУЦ);
	ТекстДляЗамены = СтрЗаменить(ТекстДляЗамены,"#АдресРегламентУЦ#",ПараметрыСпецоператора.АдресРегламентУЦ);
	ТекстДляЗамены = СтрЗаменить(ТекстДляЗамены,"#НаименованиеПОРаспространяемогоУЦ#",ПараметрыСпецоператора.НаименованиеПОРаспространяемогоУЦ);
	
	Возврат ТекстДляЗамены;
	
КонецФункции

&НаКлиенте
Процедура ПечатьЛицензииОбИспользованииПО(Знач Заявление) Экспорт
	
	ТабДок = ТабДокЛицензии(Заявление);
	ЗаголовокФормы = "Лицензия";
		
	НапечататьДокумент(ТабДок, ЗаголовокФормы);
	
КонецПроцедуры

&НаСервере
Функция ТабДокЛицензии(Заявление) Экспорт
	
	Спецоператор   = Заявление.СпецоператорСвязи;
	НаименованиеПО = ДокументооборотСКОПовтИсп.ПолучитьПараметрСпецоператора(Спецоператор, "НаименованиеПОРаспространяемогоУЦ");
	
	Макет  = ПолучитьМакетОбработки("ПечатьЛицензия");
	ТабДок = Новый ТабличныйДокумент;
	ТабДок.КлючПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ПечатьЛицензия";

	ОбластьМакета = Макет.ПолучитьОбласть("Лицензия");
	
	Если Заявление.Статус = Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено Тогда
		// По просьбе Калуги-Астрал
		ОбластьМакета.Параметры.Фирма = "";
	Иначе
		ОбластьМакета.Параметры.Фирма = Заявление.КраткоеНаименование;
	КонецЕсли;
	
	ОбластьМакета.Параметры.НаименованиеПОРаспространяемогоУЦ = НаименованиеПО;
	
	УчетнаяЗапись = Заявление.УчетнаяЗапись;
	Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		ОбластьМакета.Параметры.ДатаВыдачи = Формат(УчетнаяЗапись.ЛицензияДатаНачала, "ДЛФ=D");
		
		Число = РазницаДатВМесяцах(УчетнаяЗапись.ЛицензияДатаОкончания, УчетнаяЗапись.ЛицензияДатаНачала);
		ОбластьМакета.Параметры.Число = Число;
		
	Иначе
		ОбластьМакета.Параметры.Число = 12;
	КонецЕсли;
		
	ТабДок.Вывести(ОбластьМакета);
	
	Возврат ТабДок;
	
КонецФункции

&НаСервере
Функция РазницаДатВМесяцах(ДатаБольшая, ДатаМеньшая) Экспорт
	
	Годы    = Год(ДатаБольшая) - Год(ДатаМеньшая);
	Месяцы  = Месяц(ДатаБольшая) - Месяц(ДатаМеньшая);
	
	Разность = Месяцы + Годы * 12;
	
	Возврат Разность;
	
КонецФункции 

&НаКлиенте
Процедура ПечатьСертификатаПользователя(Знач Документ) Экспорт
	
	Если ОбработкаЗаявленийАбонентаВызовСервера.ЭтоРуководитель(Документ) Тогда
		
		Текст = НСтр("ru = 'Печать сертификата для руководителей не требуется'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(Текст);
		Возврат;
		
	ИначеЕсли ОбработкаЗаявленийАбонентаВызовСервера.ЭтоБюджетополучатель(Документ) Тогда
		
		Текст = НСтр("ru = 'Печать сертификата для бюджетополучателей не требуется'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(Текст);
		Возврат;
		
	КонецЕсли;
	
	УчетнаяЗапись = ПолучитьУчетнуюЗапись(Документ);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(УчетнаяЗапись);
		
	Если ПолучитьРеквизит(Документ, "Статус")<> СтруктураСсылочныхДанных.Перечисления_СтатусыЗаявленияАбонентаСпецоператораСвязи_Одобрено Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Сертификат можно распечатать только после создания учетной записи.'"));
		Возврат;
	КонецЕсли;
	
	Сертификат = МультирежимВызовСервера.СертификатПользователя(УчетнаяЗапись);
	Если НЕ Сертификат.Найден Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(Сертификат.ТекстОшибки);
		Возврат;
	КонецЕсли;
	
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Сертификат);
	
	СертификатыПользователя = Новый Массив;
	СертификатыПользователя.Добавить(Сертификат);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Документ", 		Документ);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", 	УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("Сертификат", 		Сертификат);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПечатьСертификатаПослеПолученияСвойствСертификата", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	КриптографияЭДКОКлиент.НайтиСертификаты(ОписаниеОповещения, СертификатыПользователя);
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьСертификатаПослеПолученияСвойствСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено И Результат.Сертификаты.Количество() > 0 Тогда
	
		ВходящийКонтекст.Вставить("ДанныеСертификата", Результат.Сертификаты[0]);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПечатьСертификатаПослеПолученияТекстаСертификата", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(
			ОписаниеОповещения, 
			ВходящийКонтекст.Сертификат);
			
	Иначе
		ПоказатьПредупреждение(, НСтр("ru = 'Сертификат не найден'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьСертификатаПослеПолученияТекстаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ТекстСертификата 	= Результат.СтрокаBase64;
		ДанныеСертификата 	= ВходящийКонтекст.ДанныеСертификата;
		Документ			= ВходящийКонтекст.Документ;
		
		СтруктураПараметров = Новый Структура("Сертификат, ТекстСертификата, Заголовок", ДанныеСертификата, ТекстСертификата, "Сведения о сертификате");
		
		Макет = ПодготовитьМакет(Документ, СтруктураПараметров, "ПАРАМЕТРЫ_ПЕЧАТИ_ПечатьСертификат","ПечатьСертификат");
		НапечататьДокумент(Макет, "Сертификат пользователя");
		
	КонецЕсли;
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПечатьПакетаДокументов(Знач Документ, ВыполняемоеОповещение = Неопределено) Экспорт
	
	СпецоператорСвязи = ПолучитьРеквизит(Документ, "СпецоператорСвязи");	
	Если НЕ ЗначениеЗаполнено(СпецоператорСвязи) тогда
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
	Иначе
		
		СтруктураДокумента = ПолучитьКлючУникальности(Документ);          
		
		ПараметрыФормы = Новый Структура("СтруктураДокумента", СтруктураДокумента);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Документ",              Документ);
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПечатьПакетаДокументовЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ФормаПечатьПакетаДокументовДляПодключения", 
			ПараметрыФормы, 
			,
			, 
			, 
			, 
			ОписаниеОповещения, 
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьПакетаДокументовЗавершение(СписокПечати, ДополнительныеПараметры) Экспорт
	
	Документ = ДополнительныеПараметры.Документ;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если СписокПечати <> Неопределено Тогда
		НапечататьПакетДокументов(Документ, СписокПечати);
	КонецЕсли;
	Если ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере 
Функция ПолучитьКлючУникальности(Документ)
	
	Возврат Новый Структура("Статус,УчетнаяЗапись,Организация",Документ.Статус,Документ.УчетнаяЗапись,Документ.Организация );
	
КонецФункции	

&НаКлиенте
Процедура НапечататьПакетДокументов(Объект,СписокДокументов)
	
	Если СписокДокументов.ПечататьЗаявление Тогда
		НапечататьЗаявлениеПо1СОтчетности(Объект);
	КонецЕсли;
	
	Если СписокДокументов.ПечататьСоглашение Тогда
		ПечатьСоглашенияобОказанииУслуг(Объект);
	КонецЕсли;
	
	Если СписокДокументов.ПечататьЛицензия Тогда
		ПечатьЛицензииОбИспользованииПО(Объект);
	КонецЕсли;
	
	Если СписокДокументов.ПечататьСертификат Тогда
		ПечатьСертификатаПользователя(Объект);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ПолучитьКодПолучателяКонтролирующегоОргана(ДанныеОрганизации, ТипПолучателя) Экспорт

	КодПолучателя = ""; 
	
	Если ТипПолучателя = СтруктураСсылочныхДанных.Перечисления_ТипыКонтролирующихОрганов_ПФР Тогда
		
		Если ПустаяСтрока(ДанныеОрганизации.КодОрганаПФР) ИЛИ СтрДлина(ДанныеОрганизации.КодОрганаПФР) < 7 Тогда
			КодПолучателя = Лев(ДанныеОрганизации.РегНомПФР,7);
		Иначе
			КодПолучателя = ДанныеОрганизации.КодОрганаПФР;
		КонецЕсли;
		
	Иначе
		
		КодПодчФСС = "";
		Если ДанныеОрганизации.Свойство("КодПодчиненностиФСС",КодПодчФСС) Тогда
			КодПодчФСС = ДанныеОрганизации.КодПодчиненностиФСС;
		КонецЕсли;
		
		КодПолучателя = ПолучитьКодПодразделенияФССПоУмолчанию(КодПодчФСС,ДанныеОрганизации.ОбъектДополнительныйКодФСС,ДанныеОрганизации.РегистрационныйНомерФСС);
		
	КонецЕсли;
	
	Возврат КодПолучателя;	

КонецФункции

&НаКлиенте
Функция ПолучитьКодТОГСа(КодРегиона, Спецоператор, КодТОГСа = "") Экспорт

	Отделения = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ОтделенияРосстатаРегиона(
		КодРегиона,
		Спецоператор);
	
	Если Отделения.Количество() > 0 Тогда
		Возврат Отделения[0].КодТОГС;
	Иначе
		Возврат КодТОГСа;
		КонецЕсли;
			
КонецФункции	

&НаСервереБезКонтекста
Функция ИзвлечьЭлектронныеПредставленияВХранилище(Владелец, УникальныйИдентификатор = Неопределено) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.ИзвлечьЭлектронныеПредставленияВХранилище(Владелец, УникальныйИдентификатор);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИменаФайловЭлектронныхПредставленийВХранилище(Владелец) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.ПолучитьИменаФайловЭлектронныхПредставленийВХранилище(Владелец)
	
КонецФункции

&НаСервереБезКонтекста
Функция АдресХранилищаФайлаЭлектронногоПредставления(Владелец, ИмяФайла, УникальныйИдентификатор = Неопределено) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.АдресХранилищаФайлаЭлектронногоПредставления(Владелец, ИмяФайла, УникальныйИдентификатор);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеОдногоФайлаЭлектронногоПредставления(Владелец, ИмяФайла) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.ДанныеОдногоФайлаЭлектронногоПредставления(Владелец, ИмяФайла);
	
КонецФункции

&НаСервереБезКонтекста
Функция ИзвлечьИменаФайловИзХранилища(Владелец) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.ИзвлечьИменаФайловИзХранилища(Владелец);
			
КонецФункции

&НаСервереБезКонтекста
Функция КоличествоФайловВКомплекте(Владелец) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.КоличествоФайловВКомплекте(Владелец);
	
КонецФункции

&НаСервереБезКонтекста
Функция РазмерФайловВКомплекте(Владелец) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.РазмерФайловВКомплекте(Владелец);
	
КонецФункции



///////////////////////////////////////////////////////////////////////////////
// Представление уведомлений в налоговые органы

#Область ОтправкаУведомлениеФНС

&НаСервереБезКонтекста
Функция ОтправкаУведомлениеФНС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст, РезультатВыгрузкиУведомления, ПриложенияУведомления)
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	УведомлениеСсылка = Контекст.Документ;
	
	ЭтоУведомлениеОКонтролируемыхСделках 	= Ложь;
	ЭтоИсходящееУведомлениеФНС 				= Ложь;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(УведомлениеСсылка);
	ИмяДокументаУведомлениеОКонтролируемыхСделках 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("УведомлениеОКонтролируемыхСделках");
	ИмяДокументаИсходящееУведомлениеФНС 			= "УведомлениеОСпецрежимахНалогообложения";
	
	Если ИмяДокументаУведомлениеОКонтролируемыхСделках <> Неопределено И ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка." + ИмяДокументаУведомлениеОКонтролируемыхСделках) Тогда
		ЭтоУведомлениеОКонтролируемыхСделках = Истина;
	ИначеЕсли ИмяДокументаИсходящееУведомлениеФНС <> Неопределено И ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка." + ИмяДокументаИсходящееУведомлениеФНС) Тогда
		ЭтоИсходящееУведомлениеФНС = Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УведомлениеСсылка, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	КонецЕсли;

	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
	КонецЕсли;
		
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	//Определяем Регистрацию ИФНС и Налоговый орган
	Если ЭтоУведомлениеОКонтролируемыхСделках Тогда
		// это уведомление о контролируемых сделках
		
		// ищем РегистрацияВИФНС по организации и коду налогового органа
		КодНалоговогоОргана = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(Организация, , "КодНО").КодНО);
		Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		НалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНалоговогоОргана);
		Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не найден налоговый орган с кодом %1  в справочнике налоговых органов""!.'"), Символ(34) + КодНалоговогоОргана + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
		
	ИначеЕсли ЭтоИсходящееУведомлениеФНС Тогда
		// это исходящее уведомление ФНС
		
		Если ТипЗнч(УведомлениеСсылка) = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения") Тогда
			ЭлРегистрацияВИФНС = УведомлениеСсылка.РегистрацияВИФНС;
		Иначе 
			ЭлРегистрацияВИФНС = Неопределено;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		КодНалоговогоОргана = ЭлРегистрацияВИФНС.Код;
		Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		НалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНалоговогоОргана);
		Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не найден налоговый орган с кодом %1  в справочнике налоговых органов""!.'"), Символ(34) + КодНалоговогоОргана + Символ(34));
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЭтоУведомлениеОКонтролируемыхСделках Тогда
		ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.УведомлениеОКонтролируемыхСделках;
	ИначеЕсли ЭтоИсходящееУведомлениеФНС Тогда
		ВидОтправляемогоДокумента =
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьВидОтправляемогоДокументаПоСсылке(
			УведомлениеСсылка);
	Иначе
		ВидОтправляемогоДокумента = Неопределено;
	КонецЕсли;
	
	ДанныеФайлаПредставление = РезультатВыгрузкиУведомления;
	ИмяФайлаПредставления = РезультатВыгрузкиУведомления.ИмяФайлаВыгрузки;
	АдресФайлаПредставления = РезультатВыгрузкиУведомления.АдресФайлаВыгрузки;
	
	ДанныеРегистрации = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС);
	ДоверенностьПредставителя = ДанныеРегистрации.Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(ДоверенностьПредставителя) Тогда
		
		Если ВидОтправляемогоДокумента = Справочники.ВидыОтправляемыхДокументов.ЗапретПредставленияУполномоченнымиЛицами Тогда
			ТекстСообщения =
				НСтр("ru = 'Отправка по доверенности уведомления о запрете/отмене запрета представления деклараций по НДС уполномоченными лицами не предусмотрена законодательством'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;
		
		// формируем файл доверенности
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(
			ЭлРегистрацияВИФНС,
			КороткоеИмяФайлаДоверенности,
			Организация,,,
			Истина);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
		
		// получаем реквизиты доверенности
		// если представитель ЮЛ или [ФЛ и не сотрудник организации], то подменяем имя файла
		ИмяТипаСправочникаМашиночитаемыеДоверенности = ?(ДокументооборотСКОКлиентСервер.ПодсистемаЦПРРМЧДСуществует(),
			"МашиночитаемыеДоверенности", "");
		ЯвляетсяСотрудникомОрганизацииПоМЧД = Неопределено;
		Если ТипЗнч(ДоверенностьПредставителя) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиФНС")
			ИЛИ ТипЗнч(ДоверенностьПредставителя) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиРаспределенныйРеестр")
			ИЛИ ЗначениеЗаполнено(ИмяТипаСправочникаМашиночитаемыеДоверенности) И ТипЗнч(ДоверенностьПредставителя) =
				Тип("СправочникСсылка." + ИмяТипаСправочникаМашиночитаемыеДоверенности) Тогда
			
			РеквизитыДоверенности = ДокументооборотСКОВызовСервера.ЗначенияРеквизитовМЧД(ДоверенностьПредставителя,
				"ДоверительЮЛ_ИНН, ДоверительЮЛ_КПП, ДоверительФЛ_ИНН, Представитель, ПредставительЮЛ_ИНН, ПредставительЮЛ_КПП, "
				+ "ПредставительФЛ_ИНН, ПредставительФЛ_ОГРН, ФИО, ВерсияФормата, КоличествоПредставителей, Представители, "
				+ "ПредставительЯвляетсяСотрудником");
			Если РеквизитыДоверенности = Неопределено Тогда
				Возврат Ложь;
			КонецЕсли;
			ЭтоВерсия5_02 = ЗначениеЗаполнено(РеквизитыДоверенности.ВерсияФормата)
				И ОбщегоНазначенияКлиентСервер.СравнитьВерсии(РеквизитыДоверенности.ВерсияФормата + ".0.0", "5.02.0.0") >= 0;
			ПредставительЗаданСтрокойФИО = (ЗначениеЗаполнено(РеквизитыДоверенности.Представитель)
				И НЕ ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительЮЛ_ИНН)
				И ТипЗнч(РеквизитыДоверенности.Представитель) = Тип("Строка"));
			Если ПредставительЗаданСтрокойФИО И НЕ ЗначениеЗаполнено(ИмяТипаСправочникаМашиночитаемыеДоверенности)
				ИЛИ ТипЗнч(ДоверенностьПредставителя) <>
				Тип("СправочникСсылка." + ИмяТипаСправочникаМашиночитаемыеДоверенности) Тогда
				СтрокиФИО = РеквизитыДоверенности.ФИО.Выгрузить().НайтиСтроки(Новый Структура("Владелец",
					Перечисления.СубъектыДоверенностиНалогоплательщика.ПредставительФЛ));
				ПредставительЗаданСтрокойФИО = ?(СтрокиФИО.Количество() <> 0,
					РеквизитыДоверенности.Представитель = ДокументооборотСКОКлиентСервер.ПолучитьПредставлениеФИО(СтрокиФИО[0]),
					Ложь); // сотрудник ИП, если "ПредставительЯвляетсяСотрудником" не поддерживается
			КонецЕсли;
			
			ТекстСообщения = ДокументооборотСКОВызовСервера.УточнитьРеквизитыПредставителяМЧДФНС(РеквизитыДоверенности,
				ДоверенностьПредставителя, ДанныеРегистрации);
			Если ЗначениеЗаполнено(ТекстСообщения) Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
			КонецЕсли;
			
			ЯвляетсяСотрудникомОрганизацииПоМЧД =
				?(ТипЗнч(ДоверенностьПредставителя) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиРаспределенныйРеестр")
						ИЛИ ЭтоВерсия5_02, РеквизитыДоверенности.ПредставительЯвляетсяСотрудником
						И НЕ ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительЮЛ_ИНН), Ложь)
					ИЛИ ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительЮЛ_ИНН)
						И ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительЮЛ_КПП)
						И РеквизитыДоверенности.ДоверительЮЛ_ИНН = РеквизитыДоверенности.ПредставительЮЛ_ИНН
						И РеквизитыДоверенности.ДоверительЮЛ_КПП = РеквизитыДоверенности.ПредставительЮЛ_КПП
					ИЛИ ПредставительЗаданСтрокойФИО;
			РеквизитыДоверенности = Новый Структура("ПредставительЯвляетсяЮЛ, ПредставительЯвляетсяСотрудником, "
				+ "ДоверительЮЛ_ИНН, ДоверительЮЛ_КПП, ДоверительФЛ_ИНН, "
				+ "ПредставительЮЛ_ИНН, ПредставительЮЛ_КПП, ПредставительФЛ_ИНН",
				ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительЮЛ_ИНН),
				ЯвляетсяСотрудникомОрганизацииПоМЧД
					ИЛИ НЕ ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительЮЛ_ИНН)
						И НЕ ЗначениеЗаполнено(РеквизитыДоверенности.ПредставительФЛ_ОГРН),
				РеквизитыДоверенности.ДоверительЮЛ_ИНН,
				РеквизитыДоверенности.ДоверительЮЛ_КПП,
				РеквизитыДоверенности.ДоверительФЛ_ИНН,
				РеквизитыДоверенности.ПредставительЮЛ_ИНН,
				РеквизитыДоверенности.ПредставительЮЛ_КПП,
				РеквизитыДоверенности.ПредставительФЛ_ИНН);
			
		Иначе
			РеквизитыДоверенности = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ДоверенностьПредставителя,
				"ПредставительЯвляетсяЮЛ, ПредставительЯвляетсяСотрудником, ПредставительЮЛ_ИНН, ПредставительЮЛ_КПП, "
				+ "ПредставительФЛ_ИНН");
		КонецЕсли;
		
		Если РеквизитыДоверенности.ПредставительЯвляетсяЮЛ
			ИЛИ (НЕ РеквизитыДоверенности.ПредставительЯвляетсяЮЛ
				И (НЕ РеквизитыДоверенности.ПредставительЯвляетсяСотрудником
					ИЛИ (ЯвляетсяСотрудникомОрганизацииПоМЧД = Ложь
						И ДокументооборотСКОКлиентСервер.ПараметрыОтправкиПоМЧДФНС("ИзменятьФайлВыгрузкиДляФизическогоЛица")))) Тогда
			
			ИспользоватьРеквизитыДоверителя = НЕ РеквизитыДоверенности.ПредставительЯвляетсяЮЛ
				И РеквизитыДоверенности.ПредставительЯвляетсяСотрудником
				И ЯвляетсяСотрудникомОрганизацииПоМЧД = Ложь;
			
			Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) Тогда
				КодНалоговогоОргана = СокрЛП(РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(
					Организация,, "КодНО").КодНО);
			КонецЕсли;
			НастройкиВызова = Новый Структура;
			НастройкиВызова.Вставить("РеквизитыДоверенности", 			РеквизитыДоверенности);
			НастройкиВызова.Вставить("ДанныеРегистрации", 				ДанныеРегистрации);
			НастройкиВызова.Вставить("ИспользоватьРеквизитыДоверителя", ИспользоватьРеквизитыДоверителя);
			КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
			Если НЕ КонтекстЭДОСервер.ИзменитьФайлВыгрузкиПриОтправкеЧерезУполномоченногоПредставителя(
				АдресФайлаПредставления,
				ИмяФайлаПредставления,
				ДоверенностьПредставителя,
				КодНалоговогоОргана,,,
				НастройкиВызова) Тогда
				Возврат Ложь;
			КонецЕсли;
		КонецЕсли;
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
	
	НачатьТранзакцию();
		
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.Предмет = УведомлениеСсылка;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = НалоговыйОрган;
	
	ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Представление;
	
	//определяем вид отправляемого докумнта
	Если ЭтоУведомлениеОКонтролируемыхСделках ИЛИ ЭтоИсходящееУведомлениеФНС Тогда
		ЦиклОбмена.ВидОтчета = ВидОтправляемогоДокумента;
		
		Если ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.УведомлениеОсвобождениеНалогНаПрибыльСколково
			И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.ув.осв.приб.Сколк.01")
			ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.УведомлениеОсвобождениеНДССколково
			И ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.ув.осв.НДС Сколк.01")
			ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.ИсчисленныеСуммыНалогов
			И НЕ ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.ув.об исчис.сум.н.12")
			ИЛИ ЦиклОбмена.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.СоблюдениеУсловийПониженныхСтавок
			И НЕ ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.собл.усл.пон.став.12") Тогда
			
			ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.НалоговаяИлиБухгалтерскаяОтчетность;
			ЦиклОбмена.ДатаНачалаПериода = НачалоДня(УведомлениеСсылка.Дата);
			ЦиклОбмена.ДатаОкончанияПериода = НачалоДня(УведомлениеСсылка.Дата);
		КонецЕсли;
	КонецЕсли;
	
	ЦиклОбмена.Тип = ТипЦиклаОбмена;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = ?(ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Представление,
		Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП,
		Перечисления.ТипыТранспортныхСообщений.ДекларацияНП);
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = НалоговыйОрган;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	//добавляем файл Представление
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(АдресФайлаПредставления);
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ДвоичныеДанныеФайла.Записать(ИмяВременногоФайла);
	Если ИнтерфейсыВзаимодействияБРОВызовСервера.СведенияДляБакета(, ИмяФайлаПредставления).Выполнено Тогда
		ТекстСведений = ИнтерфейсыВзаимодействияБРОВызовСервера.СведенияДляБакета(ИмяВременногоФайла,
			ИмяФайлаПредставления).ТекстСведений;
	Иначе
		ТекстСведений = "";
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
		ТранспортноеСообщение.Ссылка,
		?(ТипЦиклаОбмена = Перечисления.ТипыЦикловОбмена.Представление,
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление,
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетности),
		ДвоичныеДанныеФайла,
		ИмяФайлаПредставления,,,,,,,
		СгенерироватьUUID(),
		Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем приложения к уведомлению
	Для каждого ПриложениеУведомления Из ПриложенияУведомления Цикл
		ДвоичныеДанныеПриложения = ПолучитьИзВременногоХранилища(ПриложениеУведомления.АдресФайлаВыгрузки);
		РезультатДобавления = ДобавитьСодержимоеТранспортногоКонтейнера(
			ТранспортноеСообщение.Ссылка,
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение,
			ДвоичныеДанныеПриложения,
			ПриложениеУведомления.ИмяФайлаВыгрузки,
			ДвоичныеДанныеПриложения.Размер(),,,,,,
			СгенерироватьUUID(),
			ТипСодержимогоПоИмениФайла(ПриложениеУведомления.ИмяФайлаВыгрузки));
		
		Если НЕ РезультатДобавления Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, ФайлДоверенности, КороткоеИмяФайлаДоверенности, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
		
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(УведомлениеСсылка, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТекстСведений", ТекстСведений);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаУведомлениеФНС(УведомлениеСсылка, ОрганизацияОтчета, УникальныйИдентификаторФормы, ВыполняемоеОповещение) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	
	МассивВыгруженныхУведомлений = Неопределено;
	Если НЕ ВыгрузитьУведомлениеФНС(УведомлениеСсылка, МассивВыгруженныхУведомлений) Тогда
		
		ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
		
		ТекстСообщения = НСтр("ru = 'Отправка уведомления прервана. Не удалось выгрузить файл выгрузки уведомления'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Уведомления.
		
		Возврат;
	КонецЕсли;
	
	ПриложенияВыгруженныхУведомлений = Новый Массив;
	НомерУведомления = 0;
	ПриложенияРазрешены = Ложь;
	ЛюбыеXMLПриложенияРазрешены = Ложь;
	Пока НомерУведомления < МассивВыгруженныхУведомлений.Количество() Цикл
		ВыгруженноеУведомление = МассивВыгруженныхУведомлений[НомерУведомления];
		ИмяФайлаПриведенное = ВРЕГ(ВыгруженноеУведомление.ИмяФайлаВыгрузки);
		Если ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 3) = "PR_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 5) = "0250_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1125332_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1110304_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1125315_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1110305_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1125336_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1125337_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1125338_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1110313_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1150140_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 8) = "1110395_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 5) = "0300_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 5) = "9996_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 12) = "ON_ZVNALMON_"
			ИЛИ ПриложенияРазрешены И Лев(ИмяФайлаПриведенное, 10) = "NO_MFSOAZ_"
			ИЛИ ПриложенияРазрешены И ЛюбыеXMLПриложенияРазрешены И Прав(ИмяФайлаПриведенное, 4) = ".XML" Тогда
				ДвоичныеДанныеВыгрузки = ПолучитьИзВременногоХранилища(ВыгруженноеУведомление.АдресФайлаВыгрузки);
				ВыгруженноеУведомление.АдресФайлаВыгрузки = ПоместитьВоВременноеХранилище(ДвоичныеДанныеВыгрузки, Новый УникальныйИдентификатор);
				ПриложенияВыгруженныхУведомлений[НомерУведомления - 1].Добавить(ВыгруженноеУведомление);
				МассивВыгруженныхУведомлений.Удалить(НомерУведомления);
		Иначе
			Если Лев(ИмяФайлаПриведенное, 11) = "ON_UVNPDUS_"
				ИЛИ Лев(ИмяФайлаПриведенное, 8) = "NP_GALB_"
				ИЛИ Лев(ИмяФайлаПриведенное, 16) = "ON_UCHPOLNALMON_"
				ИЛИ Лев(ИмяФайлаПриведенное, 13) = "ON_RIVNALMON_"
				ИЛИ Лев(ИмяФайлаПриведенное, 15) = "ON_SRSVKNALMON_"
				ИЛИ Лев(ИмяФайлаПриведенное, 11) = "ON_ZAPMMNO_"
				ИЛИ Лев(ИмяФайлаПриведенное, 14) = "ON_UVSOGLMMNO_"
				ИЛИ Лев(ИмяФайлаПриведенное, 12) = "ON_RAZNMMNO_"
				ИЛИ Лев(ИмяФайлаПриведенное, 13) = "ON_UVVPRMMNO_"
				ИЛИ Лев(ИмяФайлаПриведенное, 12) = "ON_IPDNOISO_"
				ИЛИ Лев(ИмяФайлаПриведенное, 12) = "ON_ZAKLSZPK_"
				ИЛИ Лев(ИмяФайлаПриведенное, 12) = "ON_UVSDELOP_"
				ИЛИ Лев(ИмяФайлаПриведенное, 12) = "UT_DOKUVKSD_"
				ИЛИ Лев(ИмяФайлаПриведенное, 16) = "NO_UVNPOKODOKNO_"
				ИЛИ Лев(ИмяФайлаПриведенное, 14) = "NO_UVEDAUDZAK_"
				ИЛИ Лев(ИмяФайлаПриведенное, 14) = "NO_ZVPSODOKNO_" Тогда
					ПриложенияРазрешены = Истина;
					Если Лев(ИмяФайлаПриведенное, 11) = "ON_ZAPMMNO_" Тогда
						ЛюбыеXMLПриложенияРазрешены = Истина;
					КонецЕсли;
			КонецЕсли;
			ПриложенияВыгруженныхУведомлений.Добавить(Новый Массив);
			НомерУведомления = НомерУведомления + 1;
		КонецЕсли;
	КонецЦикла;
	
	Если МассивВыгруженныхУведомлений.Количество() >= 1
		И ВРег(Лев(МассивВыгруженныхУведомлений[0].ИмяФайлаВыгрузки, 16)) = "SR_ISCHTRZEMNAL_"
		И СтрНайти(МассивВыгруженныхУведомлений[0].ТестВыгрузки, "ПрСообщ=""2""") > 0
		И НЕ ОнлайнСервисыРегламентированнойОтчетностиВызовСервера.СобытиеНаступило("Отпр.пер.сооб.нал.пр.2", '20230101') Тогда
		
		ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
		
		ТекстСообщения = НСтр("ru = 'Запрос сообщения об исчисленном налоге на имущество в электронном виде предусматривается с 01.01.2023'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Уведомления.
		
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("УведомлениеСсылка",					УведомлениеСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета",					ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("УникальныйИдентификаторФормы",		УникальныйИдентификаторФормы);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("МассивВыгруженныхУведомлений",		МассивВыгруженныхУведомлений);
	ДополнительныеПараметры.Вставить("ПриложенияВыгруженныхУведомлений",	ПриложенияВыгруженныхУведомлений);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОтправкаУведомлениеФНСПослеАвтонастройкиУчетнойЗаписи", ЭтотОбъект, ДополнительныеПараметры);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("УведомлениеСсылка", 	УведомлениеСсылка);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 	ОписаниеОповещения);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= УведомлениеСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 4;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка уведомления в ФНС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаУведомлениеФНС",  0.1, Истина);
		
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаУведомлениеФНС() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	УведомлениеСсылка 	= КонтекстДлительнойОперации.УведомлениеСсылка;
	ОписаниеОповещения 	= КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(УведомлениеСсылка,ОписаниеОповещения);
		
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаУведомлениеФНСПослеАвтонастройкиУчетнойЗаписи(Результат, ДополнительныеПараметры) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаУведомлениеФНС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ДополнительныеПараметры.УведомлениеСсылка, ДополнительныеПараметры.ОрганизацияОтчета);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаУведомлениеФНС_ПослеПроверкиСертификатаИЛицензии(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	УведомлениеСсылка 					= ДополнительныеПараметры.УведомлениеСсылка;
	УникальныйИдентификаторФормы 		= ДополнительныеПараметры.УникальныйИдентификаторФормы;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	МассивВыгруженныхУведомлений 		= ДополнительныеПараметры.МассивВыгруженныхУведомлений;
	ПриложенияВыгруженныхУведомлений 	= ДополнительныеПараметры.ПриложенияВыгруженныхУведомлений;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", УведомлениеСсылка);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификаторФормы);
	
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", 				ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", 					КонтекстОтправки);
	Контекст.Вставить("МассивВыгруженныхУведомлений", 		МассивВыгруженныхУведомлений);
	Контекст.Вставить("ПриложенияВыгруженныхУведомлений", 	ПриложенияВыгруженныхУведомлений);
	Контекст.Вставить("ТекущееУведомление", 			0);
	
	ОтправкаУведомлениеФНСПоследовательнаяОтправка(Контекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаУведомлениеФНСПоследовательнаяОтправка(ВходящийКонтекст)
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если ВходящийКонтекст.ТекущееУведомление < ВходящийКонтекст.МассивВыгруженныхУведомлений.Количество() Тогда
		
		ВыгруженноеУведомление 	= ВходящийКонтекст.МассивВыгруженныхУведомлений[ВходящийКонтекст.ТекущееУведомление];
		ПриложенияУведомления 	= ВходящийКонтекст.ПриложенияВыгруженныхУведомлений[ВходящийКонтекст.ТекущееУведомление];
		
		// отражаем изменения в базе
		Если НЕ ОтправкаУведомлениеФНС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки, ВыгруженноеУведомление, ПриложенияУведомления) Тогда // сервер
			
			Если КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
				ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
				ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(КонтекстОтправки.Документ, ВходящийКонтекст.ОповещениеОЗавершении, Ложь);//+
			Иначе
				ДополнительныеПараметры = Новый Структура();
				ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
				ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
				
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Уведомления.
			КонецЕсли;
			
			Возврат;
			
		ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(КонтекстОтправки.Документ) Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(КонтекстОтправки.Документ, ВходящийКонтекст.ОповещениеОЗавершении, Ложь);//+
			Возврат;
			
		КонецЕсли;
		
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		Оповещение = Новый ОписаниеОповещения(
			"ОтправкаУведомлениеФНСПослеФормированияПакета", ЭтотОбъект, ВходящийКонтекст);
		
		ТекстСведений = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(КонтекстОтправки, "ТекстСведений");
		ПараметрыОперации = Новый Структура("ТекстСведений", ТекстСведений);
		
		Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС", ПараметрыОперации);
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // Уведомления.
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаУведомлениеФНСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Уведомления.
		
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Уведомления.
		
		Возврат;
	КонецЕсли;
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	ВходящийКонтекст.ТекущееУведомление = ВходящийКонтекст.ТекущееУведомление + 1;
	
	ОтправкаУведомлениеФНСПоследовательнаяОтправка(ВходящийКонтекст);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Функция ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках(КороткоеИмяФайла) Экспорт
	Возврат ВРег(Лев(КороткоеИмяФайла, 10)) = "UT_UVKNRSD";
КонецФункции

&НаКлиенте
Функция МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках() Экспорт
	Возврат 1024 * 1024 * 100;
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьУчетнуюЗапись(Объект)
	
	Возврат Объект.УчетнаяЗапись;	
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(Отпечаток, ЛогическоеХранилище = Неопределено, ВыполнятьПроверку = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(Отпечаток, ЛогическоеХранилище, ВыполнятьПроверку);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСвойстваСертификатаСубъектаНаСервере(Отпечаток, Хранилище = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСвойстваСертификатаСубъектаНаСервере(Отпечаток, Хранилище);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьСертификатНаСервере(Сертификат, МестоХраненияКлюча = Неопределено)
	
	Сертификат = КриптографияЭДКО.НайтиСертификат(Сертификат, Истина);
	Возврат Сертификат.Валиден;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСвойстваОблачногоСертификата(Отпечаток, ЛогическоеХранилище = Неопределено, ВыполнятьПроверку = Ложь) Экспорт
	
	СертификатПоиска  = Новый Структура("Отпечаток", Отпечаток);
	Сертификат = КриптографияЭДКО.НайтиОблачныйСертификат(СертификатПоиска, ВыполнятьПроверку, ЛогическоеХранилище = "MY");
	Возврат Сертификат;
	
КонецФункции

&НаКлиенте
Функция ПараметрыАвтонастройкаПоКодуПользователяИСпецоператору() Экспорт
	
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить("ИдентификаторАбонента");
	ПараметрыФункции.Вставить("СпецОператорСвязи");
	ПараметрыФункции.Вставить("ПутьКонтейнерЗакрытогоКлюча");
	ПараметрыФункции.Вставить("Организация");
	ПараметрыФункции.Вставить("УчетнаяЗапись", Неопределено);
	ПараметрыФункции.Вставить("ТихийРежим", Ложь);
	ПараметрыФункции.Вставить("РежимАвтонастройки", Ложь);
	ПараметрыФункции.Вставить("ИдентификаторДокументооборота", "");
	ПараметрыФункции.Вставить("ТелефонМобильныйДляАвторизации", "");
	
	КонтекстМоделиХраненияКлюча(Ложь, ПараметрыФункции);
		
	Возврат ПараметрыФункции;

КонецФункции

&НаКлиенте
Функция ПараметрыОбработатьОбновление() Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ПараметрыФункции = ДокументооборотСКОКлиентСервер.ПараметрыОбработатьОбновление();
	
	КонтекстМоделиХраненияКлюча(Ложь, ПараметрыФункции);
	
	Возврат ПараметрыФункции;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПараметрыРегистрацияПолучитьДанныеССервера()

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПараметрыРегистрацияПолучитьДанныеССервера();
	
КонецФункции

&НаСервереБезКонтекста
Функция ПараметрыРегистрацияОбновитьСправочники()
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПараметрыРегистрацияОбновитьСправочники();
	
КонецФункции

&НаСервереБезКонтекста
Функция ПараметрыРегистрацияУстановитьСертификаты()

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПараметрыРегистрацияУстановитьСертификаты();
	
КонецФункции

&НаСервере
Функция РегистрацияУстановитьСертификатыНаСервере(ОтпечатокСертификата)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РегистрацияУстановитьСертификаты(ОтпечатокСертификата, Сертификаты);
	
КонецФункции

// Функция написана для соблюдения стандарта Работа в разных часовых поясах
//
&НаСервереБезКонтекста
Функция ТекущаяДатаНаСервере() Экспорт

	Возврат ТекущаяДатаСеанса();

КонецФункции

&НаКлиенте
Процедура ПродолжитьОтправкуНаТестовыйСерверФСС(ВыполняемоеОповещение, ИгнорироватьРежимТестирования = Ложь) Экспорт
	
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВыполняемоеОповещение) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	Иначе
		
		ПродолжитьОтправкуНаТестовыйСерверФСС = Ложь;
		
		Если ИгнорироватьРежимТестирования ИЛИ ДокументооборотСКОКлиент.ИспользуетсяРежимТестирования() Тогда
			ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ВыполняемоеОповещение);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПродолжитьОтправкуНаТестовыйСерверФССЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ТекстВопроса = НСтр("ru = 'Внимание! Включен режим тестирования!
				|
				|В режиме тестирования отчетность отправляется через тестовый СФР (бывш. ФСС).
				|Сдача отчетности засчитана не будет.
				|
				|Продолжить отправку?'");
			ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ПродолжитьОтправкуНаТестовыйСерверФСС);
		КонецЕсли;
	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьОтправкуНаТестовыйСерверФССЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	ПродолжитьОтправкуНаТестовыйСерверФСС = (Ответ = КодВозвратаДиалога.Да);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ПродолжитьОтправкуНаТестовыйСерверФСС);
	
КонецПроцедуры

&НаСервере
Функция СерверПриемаОтчетностиФСС(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента = Неопределено,
		Организация = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СерверПриемаОтчетностиФСС(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		Организация);
	
КонецФункции

&НаКлиенте
Процедура ЕстьДанныеДляФормированияВторичногоЗаявления(
		ВыполняемоеОповещение,
		УчетнаяЗапись,
		ТекстОшибокДляМастераПодключенияК1СОтчетности = "") Экспорт
	
	// Вторичное заявление не может быть создано, если в регистре ДополнительныеРеквизитыУчетнойЗаписи нет ни одной записи
	// Если в регистре нет ни одной записи - пытаемся заполнить регистр из рег. файла, а потом повторно проверяем
	
	Если ЕстьЗаписьВРегистреДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись) Тогда
		Результат = Новый Структура("ЕстьДанные, ТекстОшибок", Истина, ТекстОшибокДляМастераПодключенияК1СОтчетности);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// Принудительно выполняем автонастройку учетной записи
	ВызовИзМастераПодключенияК1СОтчетности 					= Истина; 
	ТекстОшибокДляМастераПодключенияК1СОтчетности 			= "";
	ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки = Ложь;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("УчетнаяЗапись",        УчетнаяЗапись);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ЕстьДанныеДляФормированияВторичногоЗаявленияЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	АвтоматическаяНастройкаУчетнойЗаписи(
		УчетнаяЗапись,
		,
		ОписаниеОповещения,
		,
		Истина,
		Ложь,
		,
		ВызовИзМастераПодключенияК1СОтчетности,
		ТекстОшибокДляМастераПодключенияК1СОтчетности,
		,
		ВыполнятьРасшифровкуЗашифрованнойЧастиФайлаАвтонастроки);
		
КонецПроцедуры

&НаКлиенте
Процедура ЕстьДанныеДляФормированияВторичногоЗаявленияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	УчетнаяЗапись = ДополнительныеПараметры.УчетнаяЗапись;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Результат.Свойство("ТекстОшибок") Тогда
		ТекстОшибокДляМастераПодключенияК1СОтчетности = Результат.ТекстОшибок;
	Иначе
		ТекстОшибокДляМастераПодключенияК1СОтчетности = "";
	КонецЕсли;
	
	// Повторная проверка
	Если ЕстьЗаписьВРегистреДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись) Тогда
		РезультатПроверки = Новый Структура("ЕстьДанные, ТекстОшибок", Истина, ТекстОшибокДляМастераПодключенияК1СОтчетности);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатПроверки);
	Иначе
		РезультатПроверки = Новый Структура("ЕстьДанные, ТекстОшибок", Ложь, ТекстОшибокДляМастераПодключенияК1СОтчетности);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатПроверки);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЕстьЗаписьВРегистреДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись) Экспорт
	
	НаборЗаписейРегистра = РегистрыСведений.ДополнительныеРеквизитыУчетнойЗаписи.СоздатьНаборЗаписей();
	НаборЗаписейРегистра.Отбор.УчетнаяЗапись.Установить(УчетнаяЗапись);
	НаборЗаписейРегистра.Прочитать(); 
	
	Если НаборЗаписейРегистра.Количество() > 0 Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции
	
&НаКлиенте
Процедура КодРосстата(КодРосстата, Спецоператор, ВыполняемоеОповещение) Экспорт
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ТекущийКодТОГС",КодРосстата);
	ПараметрыФормы.Вставить("Спецоператор",Спецоператор);
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.СписокТОГС",
		ПараметрыФормы,
		,
		,
		,
		,
		ВыполняемоеОповещение,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры		
		
&НаСервереБезКонтекста
Функция ОрганизацияФизЛицаИзДокументаЗаявления(ФизЛицо) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация,
		|	МАКСИМУМ(ЗаявлениеАбонентаСпецоператораСвязи.Дата) КАК Дата
		|ИЗ
		|	Документ.ЗаявлениеАбонентаСпецоператораСвязи КАК ЗаявлениеАбонентаСпецоператораСвязи
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
		|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ПользователиУчетныхЗаписейДокументооборота КАК ПользователиУчетныхЗаписейДокументооборота
		|			ПО УчетныеЗаписиДокументооборота.Ссылка = ПользователиУчетныхЗаписейДокументооборота.УчетнаяЗапись
		|		ПО ЗаявлениеАбонентаСпецоператораСвязи.УчетнаяЗапись = УчетныеЗаписиДокументооборота.Ссылка
		|ГДЕ
		|	НЕ УчетныеЗаписиДокументооборота.ПометкаУдаления
		|	И ПользователиУчетныхЗаписейДокументооборота.Пользователь = &Пользователь
		|	И ЗаявлениеАбонентаСпецоператораСвязи.Статус = &Статус
		|	И ЗаявлениеАбонентаСпецоператораСвязи.ВладелецЭЦП = &ВладелецЭЦП
		|
		|СГРУППИРОВАТЬ ПО
		|	ЗаявлениеАбонентаСпецоператораСвязи.Организация";

	Запрос.УстановитьПараметр("Пользователь", 	Пользователи.ТекущийПользователь());
	Запрос.УстановитьПараметр("Статус", 		Перечисления.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено);
	Запрос.УстановитьПараметр("ВладелецЭЦП", 	ФизЛицо);

	Результат = Запрос.Выполнить().Выгрузить();
	МассивОрганизаций = Результат.ВыгрузитьКолонку("Организация"); 
	
	Возврат МассивОрганизаций;
	
КонецФункции

&НаСервере
Функция ПоддерживаетсяВторичноеЗаявление(Организация) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ПоддерживаетсяВторичноеЗаявление = КонтекстЭДОСервер.ПоддерживаетсяВторичноеЗаявление(Организация);
	
	Возврат ПоддерживаетсяВторичноеЗаявление;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьФормуПредупрежденияОбИзменившихсяНастройкахПодключения(Организация, ФормаВладельца) Экспорт
	
	// Проверка наличия учетной записи
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) ИЛИ НЕ ЗначениеЗаполнено(Организация) Тогда
		Возврат;
	КонецЕсли;
	
	// Если форма мастера открыта, то не выводим предупреждения
	ПрерватьОткрытие = 
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ФормаОткрыта("ФормаПредупрежденияОСменеЗначенияПараметраПодключения")
		ИЛИ ОбработкаЗаявленийАбонентаКлиент.ФормаОповещенияИлиМастераУжеОткрыта();
		
	Если ПрерватьОткрытие Тогда
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация", Организация);
	ДополнительныеПараметры.Вставить("ФормаВладельца", ФормаВладельца);
			
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПоказатьФормуПредупрежденияОбИзменившихсяНастройкахПодключения_ПослеПолученияСертификата", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ПолучитьДанныеЛокальногоСертификатаОрганизации(Организация, ОписаниеОповещения);
	
КонецПроцедуры

&НаСервере
Функция ПолучитьТипВладельцаЭЦПСервер(Тип) Экспорт
	
	Если Тип = 1 Тогда
		ТипИсполнителя = СтруктураСсылочныхДанных.Перечисления_ТипыВладельцевЭЦП_Руководитель;
	ИначеЕсли Тип = 2 Тогда
		ТипИсполнителя = СтруктураСсылочныхДанных.Перечисления_ТипыВладельцевЭЦП_ГлавныйБухгалтер;
	Иначе
		ТипИсполнителя =СтруктураСсылочныхДанных.Перечисления_ТипыВладельцевЭЦП_ДругойСотрудник;
	КонецЕсли;
	
	Возврат ТипИсполнителя;
	
КонецФункции

&НаСервере
Процедура СохранитьДополнительныеРеквизитыУчетнойЗаписи(ПараметрыФункции)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.СохранитьДополнительныеРеквизитыУчетнойЗаписи(ПараметрыФункции);
			
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЭтоПервичныйОтчет(Отчет)
	
	Возврат Отчет.Вид = 0;
	
КонецФункции

&НаКлиенте
Функция ВыбранКорректныйФайл(Знач ПолныйПуть, Знач ПолноеИмяФайлаБезПути) Экспорт
	
	ПолныйПуть = Врег(ПолныйПуть);
	ПолноеИмяФайлаБезПути = Врег(ПолноеИмяФайлаБезПути);
	
	ДлинаПолногоИмениФайлаБезПути = СтрДлина(ПолноеИмяФайлаБезПути);
	Возврат Прав(ПолныйПуть,ДлинаПолногоИмениФайлаБезПути) = ПолноеИмяФайлаБезПути;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИзХранилищаЗначений(Хранилище) Экспорт
	
	Результат = Хранилище.Получить();
	Возврат Результат;
	
КонецФункции

// Используется в случае, если клиент не поддерживает расширение работы с файлами
&НаСервереБезКонтекста
Функция СвойстваФайла(Знач АдресДанных, Знач ПолноеИмя) Экспорт
	
	// Остальные свойства
	Файл = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ПолноеИмя);
	
	СтруктураСвойств = Новый Структура("АдресДанных, Имя, ИмяБезРасширения, ПолноеИмя, Путь, Размер, Расширение");
	ЗаполнитьЗначенияСвойств(СтруктураСвойств, Файл);
	СтруктураСвойств.АдресДанных = АдресДанных;
	СтруктураСвойств.Размер      = ПолучитьИзВременногоХранилища(АдресДанных).Размер();
	
	Возврат СтруктураСвойств;
	
КонецФункции


////////////////////////////////////////////////////////////////////////////////
// ДОКУМЕНТООБОРОТ С РПН

// ОтправкаСсылка ссылается на загруженный объект
&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиРПН(Форма, ОтправкаСсылка, ВыполняемоеОповещение, ПараметрыОбновленияОтправки = Неопределено, ЭтоВызовИзСписка = Ложь) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Форма",                Форма);
	ДополнительныеПараметры.Вставить("ОтправкаСсылка",       ОтправкаСсылка);
	ДополнительныеПараметры.Вставить("ЗначениеCookie",       ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.ЗначениеCookie, ""));
	ДополнительныеПараметры.Вставить("EMail",                ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.EMail, ""));
	ДополнительныеПараметры.Вставить("Пароль",               ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.Пароль, ""));
	ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",     ЭтоВызовИзСписка);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОбновитьРезультатКонкретнойОтправкиРПНПослеПолученияСертификатовИПараметровОбмена",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	СертификатыИПараметрыОбменаОрганизацииДляРПН(
		Оповещение,
		Неопределено,
		Истина,
		ОтправкаСсылка,
		Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиРПНПослеПолученияСертификатовИПараметровОбмена(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = 	ДополнительныеПараметры.ВыполняемоеОповещение;
	Форма = 					ДополнительныеПараметры.Форма;
	ОтправкаСсылка = 			ДополнительныеПараметры.ОтправкаСсылка;
	ЗначениеCookie = 			ДополнительныеПараметры.ЗначениеCookie;
	EMail = 					ДополнительныеПараметры.EMail;
	Пароль = 					ДополнительныеПараметры.Пароль;
	ЭтоВызовИзСписка = 			ДополнительныеПараметры.ЭтоВызовИзСписка;
	
	Крипт = Неопределено;
	ОрганизацияСсылка = СертификатыИПараметрыОбмена.ОрганизацияИзОтправки;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	ТелефонМобильныйДляАвторизации = РеквизитыУчетнойЗаписи.ТелефонМобильныйДляАвторизации;
	
	УчетнаяЗапись = Новый Структура("ТелефонМобильныйДляАвторизации, ИдентификаторДокументооборота", ТелефонМобильныйДляАвторизации, ИдентификаторДокументооборота);
	
	ПараметрыФормированияПакета = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	ЭтоРегламентированныйОтчет = СертификатыИПараметрыОбмена.ЭтоРегламентированныйОтчет;
	
	Если НЕ ПараметрыФормированияПакета.ИспользоватьОбмен Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Использование документооборота с Росприроднадзором не включено для организации!'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	ФормаДлительнаяОперация = Неопределено;
	
	ПараметрEMail = ?(ЗначениеЗаполнено(EMail), EMail, ПараметрыФормированияПакета.EMail);
	ПараметрПароль = ?(ЗначениеЗаполнено(Пароль), Пароль, ПараметрыФормированияПакета.Пароль);
	
	АвторизацияПроизведена = ЗначениеЗаполнено(ЗначениеCookie);
	ПараметрыОтправки = Новый Структура();
	ПараметрыОтправки.Вставить("Организация",             ОрганизацияСсылка);
	ПараметрыОтправки.Вставить("АдресСервера",            СерверПриемаОтчетностиРПН(Истина, ОрганизацияСсылка));
	ПараметрыОтправки.Вставить("ПоляДляЗаписиОтправки",   Неопределено);
	ПараметрыОтправки.Вставить("ЭтоНеподписываемыйОтчет", СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет);
	ПараметрыОтправки.Вставить("БумажнаяПодачаОтчета",    СертификатыИПараметрыОбмена.БумажнаяПодачаОтчета);
	
	ПараметрыСоединения = Новый Структура();
	ПараметрыСоединения.Вставить("EMail",                          ПараметрEMail);
	ПараметрыСоединения.Вставить("Пароль",                         ПараметрПароль);
	ПараметрыСоединения.Вставить("СохранитьПароль",                ЗначениеЗаполнено(ПараметрыФормированияПакета.Пароль));
	ПараметрыСоединения.Вставить("СохраненныйEmail",               ПараметрыФормированияПакета.EMail);
	ПараметрыСоединения.Вставить("СохраненныйПароль",              ПараметрыФормированияПакета.Пароль);
	ПараметрыСоединения.Вставить("СоединениеHTTP",                 Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie",                 ЗначениеCookie);
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиНаСервере", "");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером",         "");
	ПараметрыСоединения.Вставить("ПовторятьСоединение",            Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля",          СертификатыИПараметрыОбмена.СвойстваОтправки.ИдентификаторОтправкиОператору = 0);
	ПараметрыСоединения.Вставить("СообщенияПользователю",          Новый Массив);
	ПараметрыСоединения.Вставить("ОбменНапрямую",                  ПараметрыФормированияПакета.ОбменНапрямую);
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиОператору", Неопределено);
	
	ПерваяПопыткаАвторизации = Истина;
	ОперацияУспешна = Ложь;
	
	ОбновитьРезультатКонкретнойОтправкиРПНВводПароля(
		ВыполняемоеОповещение,
		ПараметрыСоединения,
		ПараметрыОтправки,
		ФормаДлительнаяОперация,
		АвторизацияПроизведена,
		ОтправкаСсылка,
		ПерваяПопыткаАвторизации,
		СертификатыИПараметрыОбмена,
		ЭтоВызовИзСписка);
	
КонецПроцедуры

// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (Цикл)
&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиРПНВводПароля(
	ВыполняемоеОповещение,
	ПараметрыСоединения,
	ПараметрыОтправки,
	ФормаДлительнаяОперация,
	АвторизацияПроизведена,
	ОтправкаСсылка,
	ПерваяПопыткаАвторизации,
	СертификатыИПараметрыОбмена,
	ЭтоВызовИзСписка)
	
	Если СертификатыИПараметрыОбмена.СвойстваОтправки.ИдентификаторОтправкиОператору = 0 
		И (НЕ ЗначениеЗаполнено(ПараметрыСоединения.EMail) ИЛИ НЕ ЗначениеЗаполнено(ПараметрыСоединения.Пароль) ИЛИ НЕ ПерваяПопыткаАвторизации) Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения", 			ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки", 				ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация", 		ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("АвторизацияПроизведена", 			АвторизацияПроизведена);
		ДополнительныеПараметры.Вставить("ОтправкаСсылка", 					ОтправкаСсылка);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации", 		ПерваяПопыткаАвторизации);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", 	СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка", 				ЭтоВызовИзСписка);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиРПНВводПароляЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		ВызватьФормуВводаПароляПорталаРПН(
			"https://" + ПараметрыОтправки.АдресСервера + "/",
			ПараметрыСоединения.EMail,
			ПараметрыСоединения.Пароль,
			ПараметрыСоединения.СохранитьПароль,
			ОписаниеОповещения);
		
		Возврат;
	КонецЕсли;
	
	// при успешном обращении на сервер РПН отправка обновится в ИБ
	ОперацияУспешна = ОбновитьРезультатКонкретнойОтправкиРПННаСервере(
		ОтправкаСсылка,
		Истина,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ЭтоВызовИзСписка);
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ПерваяПопыткаАвторизации = Ложь;
	
	// рекурсивный вызов - замена цикла
	Если НЕ ОперацияУспешна И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
		
		ОбновитьРезультатКонкретнойОтправкиРПНВводПароля(
			ВыполняемоеОповещение,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ФормаДлительнаяОперация,
			АвторизацияПроизведена,
			ОтправкаСсылка,
			ПерваяПопыткаАвторизации,
			СертификатыИПараметрыОбмена,
			ЭтоВызовИзСписка);
		
	Иначе
		
		ОбновитьРезультатКонкретнойОтправкиРПНПослеОтправки(
			ВыполняемоеОповещение,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ФормаДлительнаяОперация,
			АвторизацияПроизведена,
			ОтправкаСсылка,
			ПерваяПопыткаАвторизации,
			СертификатыИПараметрыОбмена,
			ЭтоВызовИзСписка);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиРПНВводПароляЗавершение(РезультатВводаПароля, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение       = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыСоединения         = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки           = ДополнительныеПараметры.ПараметрыОтправки;
	ФормаДлительнаяОперация     = ДополнительныеПараметры.ФормаДлительнаяОперация;
	АвторизацияПроизведена      = ДополнительныеПараметры.АвторизацияПроизведена;
	ОтправкаСсылка              = ДополнительныеПараметры.ОтправкаСсылка;
	ПерваяПопыткаАвторизации    = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ЭтоВызовИзСписка            = ДополнительныеПараметры.ЭтоВызовИзСписка;
	
	Если РезультатВводаПароля = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	Иначе
		
		ПараметрыСоединения.EMail           = РезультатВводаПароля.EMail;
		ПараметрыСоединения.Пароль          = РезультатВводаПароля.Пароль;
		ПараметрыСоединения.СохранитьПароль = РезультатВводаПароля.СохранитьПароль;
		
		// при успешном обращении на сервер РПН отправка обновится в ИБ
		ОперацияУспешна = ОбновитьРезультатКонкретнойОтправкиРПННаСервере(
			ОтправкаСсылка,
			Истина,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ЭтоВызовИзСписка);
		
		ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
		
		ПерваяПопыткаАвторизации = Ложь;
		
		// рекурсивный вызов - замена цикла
		Если НЕ ОперацияУспешна И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
			
			ОбновитьРезультатКонкретнойОтправкиРПНВводПароля(
				ВыполняемоеОповещение,
				ПараметрыСоединения,
				ПараметрыОтправки,
				ФормаДлительнаяОперация,
				АвторизацияПроизведена,
				ОтправкаСсылка,
				ПерваяПопыткаАвторизации,
				СертификатыИПараметрыОбмена,
				ЭтоВызовИзСписка);
		Иначе
			
			ОбновитьРезультатКонкретнойОтправкиРПНПослеОтправки(
				ВыполняемоеОповещение,
				ПараметрыСоединения,
				ПараметрыОтправки,
				ФормаДлительнаяОперация,
				АвторизацияПроизведена,
				ОтправкаСсылка,
				ПерваяПопыткаАвторизации,
				СертификатыИПараметрыОбмена,
				ЭтоВызовИзСписка);
			
		КонецЕсли;
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиРПНПослеОтправки(
	ВыполняемоеОповещение,
	ПараметрыСоединения,
	ПараметрыОтправки,
	ФормаДлительнаяОперация,
	АвторизацияПроизведена,
	ОтправкаСсылка,
	ПерваяПопыткаАвторизации,
	СертификатыИПараметрыОбмена,
	ЭтоВызовИзСписка)
	
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// возможно, проблемы с доступом в интернет
		Если НЕ АвторизацияПроизведена Тогда
			ПараметрыСоединения.ЗначениеCookie = "";
		КонецЕсли;
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",       ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",         ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",           ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",     ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("АвторизацияПроизведена",      АвторизацияПроизведена);
		ДополнительныеПараметры.Вставить("ОтправкаСсылка",              ОтправкаСсылка);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации",    ПерваяПопыткаАвторизации);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",            ЭтоВызовИзСписка);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбновитьРезультатКонкретнойОтправкиРПНПослеОтправкиЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу Росприроднадзора'"),
			ОписаниеОповещения);
		
		Возврат;
		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиРПНПослеОтправкиЗавершение(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение                   = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыСоединения                     = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                       = ДополнительныеПараметры.ПараметрыОтправки;
	ФормаДлительнаяОперация                 = ДополнительныеПараметры.ФормаДлительнаяОперация;
	АвторизацияПроизведена                  = ДополнительныеПараметры.АвторизацияПроизведена;
	ОтправкаСсылка                          = ДополнительныеПараметры.ОтправкаСсылка;
	ПерваяПопыткаАвторизации                = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	СертификатыИПараметрыОбмена             = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ЭтоВызовИзСписка                        = ДополнительныеПараметры.ЭтоВызовИзСписка;
	ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// рекурсивный вызов - замена цикла
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ОбновитьРезультатКонкретнойОтправкиРПНВводПароля(
			ВыполняемоеОповещение,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ФормаДлительнаяОперация,
			АвторизацияПроизведена,
			ОтправкаСсылка,
			ПерваяПопыткаАвторизации,
			СертификатыИПараметрыОбмена,
			ЭтоВызовИзСписка);
		
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);	
		
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьПротоколОтправкиРПН(Форма) Экспорт
	
	ОтчетСсылка = ДокументооборотСРПНКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	
	Если ОтчетСсылка = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПоказатьПротоколОбработкиПоСсылкеИсточникаДляРПН(ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляРПН(
		ВыполняемоеОповещение,
		ОрганизацияСсылка,
		ПолучитьОсновныеСвойстваПоследнейОтправки,
		ОтправкаСсылка = Неопределено,
		ОтчетСсылка = Неопределено,
		ОпределитьНастроенностьОбменаВУниверсальномФормате = Ложь,
		СвойстваПоследнейОтправки = Неопределено)
	
	РезультатВыполнения = Новый Структура;
	РезультатВыполнения.Вставить("ПараметрыФормированияПакета", 		Неопределено);
	РезультатВыполнения.Вставить("ОрганизацияИзОтправки", 				Неопределено);
	РезультатВыполнения.Вставить("СвойстваОтправки", 					Неопределено);
	РезультатВыполнения.Вставить("Крипт", 								Неопределено);
	РезультатВыполнения.Вставить("НастроенОбменВУниверсальномФормате", 	Неопределено);
	РезультатВыполнения.Вставить("ПравоИзмененияУчетнойЗаписи", 		Неопределено);
	РезультатВыполнения.Вставить("ЭтоРегламентированныйОтчет", 			Неопределено);
	РезультатВыполнения.Вставить("ЭтоРегламентированноеУведомление", 	Неопределено);
	РезультатВыполнения.Вставить("ЭтоНеподписываемыйОтчет", 			Неопределено);
	РезультатВыполнения.Вставить("БумажнаяПодачаОтчета", 				Неопределено);
	
	СвойстваОбмена = СвойстваОбменаОрганизацииДляРПННаСервере(ОрганизацияСсылка, ОпределитьНастроенностьОбменаВУниверсальномФормате, ПолучитьОсновныеСвойстваПоследнейОтправки, ОтправкаСсылка, ОтчетСсылка, , СвойстваПоследнейОтправки);
	РезультатВыполнения.Вставить("ОрганизацияСсылка", ОрганизацияСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СвойстваОбмена);
	
	РезультатВыполнения.ОрганизацияИзОтправки = СвойстваОбмена.ОрганизацияИзОтправки;
	
	РезультатВыполнения.СвойстваОтправки = СвойстваОбмена.СвойстваОтправки;
	
	РезультатВыполнения.НастроенОбменВУниверсальномФормате = СвойстваОбмена.НастроенОбменВУниверсальномФормате;
	РезультатВыполнения.ПравоИзмененияУчетнойЗаписи = СвойстваОбмена.ПравоИзмененияУчетнойЗаписи;
	РезультатВыполнения.ЭтоРегламентированныйОтчет = СвойстваОбмена.ЭтоРегламентированныйОтчет;
	РезультатВыполнения.ЭтоРегламентированноеУведомление = СвойстваОбмена.ЭтоРегламентированноеУведомление;
	РезультатВыполнения.ЭтоНеподписываемыйОтчет = СвойстваОбмена.ЭтоНеподписываемыйОтчет;
	РезультатВыполнения.БумажнаяПодачаОтчета = СвойстваОбмена.БумажнаяПодачаОтчета;
	
	РезультатВыполнения.ПараметрыФормированияПакета = Новый Структура("ИспользоватьОбмен, СертификатАбонента, СертификатРПН, EMail, Пароль, ИспользоватьАвтонастройку, ОбменНапрямую", Ложь, Неопределено, Неопределено, Неопределено, Неопределено, Ложь, Ложь);
	Если НЕ СвойстваОбмена.ЭтоНеподписываемыйОтчет И НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьОбмен = СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен;
	РезультатВыполнения.ПараметрыФормированияПакета.EMail = СвойстваОбмена.НастройкиОбмена.EMail;
	РезультатВыполнения.ПараметрыФормированияПакета.Пароль = СвойстваОбмена.НастройкиОбмена.Пароль;
	РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьАвтонастройку = СвойстваОбмена.НастройкиОбмена.ИспользоватьАвтонастройку;
	РезультатВыполнения.ПараметрыФормированияПакета.ОбменНапрямую = СвойстваОбмена.НастройкиОбмена.ОбменНапрямую;
	
	Если СвойстваОбмена.ЭтоНеподписываемыйОтчет Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = Неопределено;
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатРПН = Неопределено;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток);
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатРПН = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СвойстваОбмена.НастройкиОбмена.СертификатРПНОтпечаток);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ПолучитьСвойстваОблачногоСертификата(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток, "MY");
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатРПН = ПолучитьСвойстваОблачногоСертификата(СвойстваОбмена.НастройкиОбмена.СертификатРПНОтпечаток);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, РезультатВыполнения.ПараметрыФормированияПакета.СертификатРПН);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		
	Иначе
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("СвойстваОбмена",        СвойстваОбмена);
		ДополнительныеПараметры.Вставить("РезультатВыполнения",   РезультатВыполнения);
		
		Оповещение = Новый ОписаниеОповещения(
			"СертификатыИПараметрыОбменаОрганизацииДляРПНПослеПолученияСвойствСертификатаАбонентаЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		Если ЗначениеЗаполнено(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток) Тогда
			КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток, "MY", , Ложь);
		Иначе
			РезультатПоиска = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден", Истина, Неопределено, Ложь);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатПоиска);
		КонецЕсли;
		
	КонецЕсли;
	
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляРПНПослеПолученияСвойствСертификатаАбонентаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СвойстваОбмена = ДополнительныеПараметры.СвойстваОбмена;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, РезультатВыполнения", ВыполняемоеОповещение, РезультатВыполнения);
	Оповещение = Новый ОписаниеОповещения("СертификатыИПараметрыОбменаОрганизацииДляРПНПослеПолученияСвойствСертификатаРПНЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если ЗначениеЗаполнено(СвойстваОбмена.НастройкиОбмена.СертификатРПНОтпечаток) Тогда
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СвойстваОбмена.НастройкиОбмена.СертификатРПНОтпечаток, "AddressBook", , Ложь);
	Иначе
		РезультатПоиска = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден", Истина, Неопределено, Ложь);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатПоиска);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляРПНПослеПолученияСвойствСертификатаРПНЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатРПН = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Функция СертификатыГодятсяДляПодписанияОтчетностиРПН(Сертификаты)
	
	ТекДата = ТекущаяДатаНаСервере();
	ПризнакОшибки = Ложь;
	
	Для Каждого Сертификат Из Сертификаты Цикл
		
		Серт	= Сертификат.Значение;
		Предст	= Сертификат.Представление;
		Если Серт = Неопределено Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: сертификат не выбран в настройках сдачи отчетности в Росприроднадзор!'"), Предст));
			Продолжить;
		КонецЕсли;
		
		Если ТекДата < Серт.ДействителенС Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: период действия сертификата еще не наступил (%2)!'"), Предст, Строка(Серт.ДействителенС)));
		КонецЕсли;
		
		Если ТекДата > Серт.ДействителенПо Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1: период действия сертификата истек (%2)!'"), Предст, Строка(Серт.ДействителенПо)));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат (НЕ ПризнакОшибки);
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПН(
		Форма,
		ВыполняемоеОповещение,
		ВыполнятьАвтонастройку = Истина,
		ТекстФайлаВыгрузки = Неопределено,
		ИмяФайлаВыгрузки = Неопределено,
		Кодировка = Неопределено,
		СертификатыИПараметрыОбмена = Неопределено,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь,
		Знач ОтчетСсылка = Неопределено,
		Знач ОрганизацияСсылка = Неопределено) Экспорт
		
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСРПНКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	Если ОтчетСсылка = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияСсылка = ДокументооборотСРПНКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	УчетнаяЗапись = РеквизитыУчетнойЗаписи.Ссылка;
	Идентификатор = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ВыполняемоеОповещение);	
	
	ДополнительныеПараметры.Вставить("Форма", Форма);
	ДополнительныеПараметры.Вставить("ВыполнятьАвтонастройку", ВыполнятьАвтонастройку);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", ОтчетСсылка);
	ДополнительныеПараметры.Вставить("Идентификатор", Идентификатор);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", ОрганизацияСсылка);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

	ВыгрузитьПакетДляОтправкиВРПНПослеИнициализации(Истина, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеИнициализации(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
	КонецЕсли;
	
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияСсылка = ДополнительныеПараметры.ОрганизацияСсылка;
	
	ОпределитьНастроенностьОбменаВУниверсальномФормате = ДополнительныеПараметры.ВыполнятьАвтонастройку;
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВРПНПослеПолученияСертификатовИПараметровОбменаЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	СертификатыИПараметрыОбменаОрганизацииДляРПН(
		Оповещение,
		ОрганизацияСсылка,
		Ложь,
		Неопределено,
		ОтчетСсылка,
		ОпределитьНастроенностьОбменаВУниверсальномФормате);
		
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеПолученияСертификатовИПараметровОбменаЗавершение(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт
	
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
	
	Если ДополнительныеПараметры.ВыполнятьАвтонастройку И 
			СертификатыИПараметрыОбмена.НастроенОбменВУниверсальномФормате И 
			СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИспользоватьАвтонастройку И 
			СертификатыИПараметрыОбмена.ПравоИзмененияУчетнойЗаписи Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ДополнительныеПараметры.ОтчетСсылка, ОписаниеОповещения);
		Возврат;
		
	КонецЕсли;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	ВыгрузитьПакетДляОтправкиВРПНПослеАвтонастройкиУчетнойЗаписи(
		ДополнительныеПараметры.Форма, 
		ДополнительныеПараметры.ВыполняемоеОповещение, 
		ДополнительныеПараметры.СертификатыИПараметрыОбмена, 
		ДополнительныеПараметры.ОтчетСсылка, 
		МестоХраненияКлюча, 
		ДополнительныеПараметры.Идентификатор, 
		ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность,
		ДополнительныеПараметры.ОрганизацияСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Идентификатор = ДополнительныеПараметры.Идентификатор;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	ВыгрузитьПакетДляОтправкиВРПНПослеАвтонастройкиУчетнойЗаписи(Форма, ВыполняемоеОповещение, СертификатыИПараметрыОбмена, ОтчетСсылка, МестоХраненияКлюча, Идентификатор, ЭтоОтправкаИзФормыОтчетность);	
		
КонецПроцедуры
		
&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеАвтонастройкиУчетнойЗаписи(Форма, ВыполняемоеОповещение, СертификатыИПараметрыОбмена, ОтчетСсылка, МестоХраненияКлюча, 
		Идентификатор, Знач ЭтоОтправкаИзФормыОтчетность = Ложь, ОрганизацияСсылка = Неопределено)
	
	// запрашиваем параметры формирования пакета и параметры обмена при вызове из отправки
	Если НЕ СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИспользоватьОбмен И НЕ СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Использование документооборота с Росприроднадзором не включено для организации!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатАбонента = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента;
	Если СертификатАбонента <> Неопределено И НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		СертификатАбонента.Вставить("КлючевойКонтейнер", Идентификатор);
	КонецЕсли;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатАбонента);
	
	СертификатРПН = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатРПН;
	Если СертификатАбонента <> Неопределено И НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатРПН);
	КонецЕсли;
	
	//проверяем сертификаты
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатАбонента, НСтр("ru = 'Сертификат абонента'"));
	
	Если НЕ СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет 
		И НЕ СертификатыГодятсяДляПодписанияОтчетностиРПН(СертификатыОбмена) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	// формируем файл выгрузки
	
	ТекстФайлаВыгрузки 	= "";
	ИмяФайлаВыгрузки 	= "";
	Приложения 			= Новый Массив;
	Кодировка 			= "utf-8"; // кодировка, в которую нужно сконвертировать текст выгрузки перед подписью
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Если ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет")
	ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено") Тогда
	
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
		Иначе
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
		КонецЕсли;
	
		Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(ОтчетСсылка, Ложь, Ложь, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки) Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		
		Если ТипЗнч(ТекстФайлаВыгрузки) = Тип("Массив") Тогда
			АдресОсновногоФайлаВыгрузки = Неопределено;
			Для каждого ЭлементВыгрузки Из ТекстФайлаВыгрузки Цикл
				Если ТипЗнч(ЭлементВыгрузки) = Тип("Структура")
					И ЭлементВыгрузки.Свойство("АдресФайлаВыгрузки")
					И ЭлементВыгрузки.Свойство("ИмяФайлаВыгрузки") Тогда
					
					РасширениеВыгрузки = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
						ЭлементВыгрузки.ИмяФайлаВыгрузки).Расширение;
					Если нрег(РасширениеВыгрузки) = ".xml" И АдресОсновногоФайлаВыгрузки = Неопределено Тогда
						АдресОсновногоФайлаВыгрузки = ЭлементВыгрузки.АдресФайлаВыгрузки;
						ИмяФайлаВыгрузки = ЭлементВыгрузки.ИмяФайлаВыгрузки;
						
					Иначе
						Приложения.Добавить(ЭлементВыгрузки);
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			Если АдресОсновногоФайлаВыгрузки = Неопределено И ТекстФайлаВыгрузки.Количество() > 0
				И ТипЗнч(ТекстФайлаВыгрузки[0]) = Тип("Структура")
				И ТекстФайлаВыгрузки[0].Свойство("АдресФайлаВыгрузки")
				И ТекстФайлаВыгрузки[0].Свойство("ИмяФайлаВыгрузки") Тогда
				
				АдресОсновногоФайлаВыгрузки = ТекстФайлаВыгрузки[0].АдресФайлаВыгрузки;
				ИмяФайлаВыгрузки = ТекстФайлаВыгрузки[0].ИмяФайлаВыгрузки;
				Приложения.Удалить(0);
			КонецЕсли;
			Если АдресОсновногоФайлаВыгрузки <> Неопределено Тогда
				ТекстФайлаВыгрузки = ПрочитатьТекстИзФайла(АдресОсновногоФайлаВыгрузки, "utf-8");
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения") Тогда
		МассивВыгруженныхУведомлений = Неопределено;
		Если НЕ ВыгрузитьУведомлениеФНС(ОтчетСсылка, МассивВыгруженныхУведомлений) Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		ТекстФайлаВыгрузки = ПрочитатьТекстИзФайла(МассивВыгруженныхУведомлений[0].АдресФайлаВыгрузки, "utf-8");
		ИмяФайлаВыгрузки = МассивВыгруженныхУведомлений[0].ИмяФайлаВыгрузки;
		
	Иначе // электронное представление
		Если ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		
		ОпределеннаяКодировка = Неопределено;
		
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
			ОтчетСсылка,
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,
			ОпределеннаяКодировка,
			"utf-8",
			Приложения);
		
		Если ОпределеннаяКодировка <> Неопределено Тогда
			//Кодировка = ОпределеннаяКодировка;
		КонецЕсли;		
	КонецЕсли;
	
	Префикс = нрег(Лев(ИмяФайлаВыгрузки, 15));
	Если СтрНайти(Префикс, "рсч_") > 0
		ИЛИ СтрНайти(Префикс, "мсп_") > 0
		ИЛИ СтрНайти(Префикс, "2тпо_") > 0
		ИЛИ СтрНайти(Префикс, "онв_") > 0
		ИЛИ СтрНайти(Префикс, "2тпр_") > 0
		ИЛИ СтрНайти(Префикс, "2тпа_") > 0 Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Данный формат отчета устарел и не принимается Росприроднадзором. Используйте актуальную форму отчета'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыФункции = Новый Структура;
		ПараметрыФункции.Вставить("ТекстФайлаВыгрузки",      ТекстФайлаВыгрузки);
		ПараметрыФункции.Вставить("Сертификаты",             СертификатыОбмена);
		ПараметрыФункции.Вставить("Идентификатор",           Идентификатор);
		ПараметрыФункции.Вставить("Адрес",                   Форма.УникальныйИдентификатор);
		ПараметрыФункции.Вставить("ИмяФайлаВыгрузки",        ИмяФайлаВыгрузки);
		ПараметрыФункции.Вставить("Кодировка",               Кодировка);
		ПараметрыФункции.Вставить("ЭтоНеподписываемыйОтчет", СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет);
		
		// подготовка выгрузки пакета, без выполнения подписи для подписываемых отчетов
		АдресРезультатаВыгрузки = ЗагрузитьДанныеНаСервер(ТекстФайлаВыгрузки, Форма.УникальныйИдентификатор,, Истина);
	
		Кодировка = Неопределено;
		
		Если СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет ИЛИ АдресРезультатаВыгрузки = Неопределено Тогда
			Если АдресРезультатаВыгрузки <> Неопределено Тогда
				РезультатВыгрузки = Новый Структура("ИмяФайлаЗашифрованногоПакета, ИмяФайлаВыгрузки", АдресРезультатаВыгрузки, ИмяФайлаВыгрузки);
				СтруктураВозвратаЭтоЭлектроннаяПодпись = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", РезультатВыгрузки, СертификатыИПараметрыОбмена);
			Иначе
				СтруктураВозвратаЭтоЭлектроннаяПодпись = Неопределено;
			КонецЕсли;
			
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, СтруктураВозвратаЭтоЭлектроннаяПодпись);
			
		Иначе
			ФайлВыгрузки = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаВыгрузки);
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("ВременныйКаталог", 					Неопределено);
			ДополнительныеПараметры.Вставить("ПолноеИмяПодписанногоФайла", 			Неопределено);
			ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", 				АдресРезультатаВыгрузки);
			ДополнительныеПараметры.Вставить("СертификатАбонента", 					СертификатАбонента);
			ДополнительныеПараметры.Вставить("СертификатРПН", 						СертификатРПН);
			ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", 		СертификатыИПараметрыОбмена);
			ДополнительныеПараметры.Вставить("ФайлВыгрузки", 						ФайлВыгрузки);
			ДополнительныеПараметры.Вставить("Кодировка", 							Кодировка);
			ДополнительныеПараметры.Вставить("Идентификатор", 						Идентификатор);
			ДополнительныеПараметры.Вставить("Приложения", 							Приложения);
			ДополнительныеПараметры.Вставить("ОрганизацияСсылка",					ОрганизацияСсылка);
			ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНПослеЗаписиФайлаВыгрузки", ЭтотОбъект, ДополнительныеПараметры);
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, АдресРезультатаВыгрузки);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		КонецЕсли;
		
		Возврат;
		
	КонецЕсли;
	
	// получим имя временного файла
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 					ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 					ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("СертификатАбонента", 					СертификатАбонента);
	ДополнительныеПараметры.Вставить("СертификатРПН", 						СертификатРПН);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", 		СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("Кодировка", 							Кодировка);
	ДополнительныеПараметры.Вставить("Идентификатор", 						Идентификатор);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка",					ОрганизацияСсылка);
	ДополнительныеПараметры.Вставить("Приложения", 							Приложения);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНПослеПолученияВременногоКаталога", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.КаталогВременныхФайловНаКлиенте(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеПолученияВременногоКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки 					= ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ТекстФайлаВыгрузки 					= ДополнительныеПараметры.ТекстФайлаВыгрузки;
	СертификатАбонента 					= ДополнительныеПараметры.СертификатАбонента;
	СертификатРПН 						= ДополнительныеПараметры.СертификатРПН;
	СертификатыИПараметрыОбмена 		= ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	Кодировка 							= ДополнительныеПараметры.Кодировка;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Идентификатор 						= ДополнительныеПараметры.Идентификатор;
	Приложения 							= ДополнительныеПараметры.Приложения;
	ОрганизацияСсылка					= ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВременныйКаталог = Результат.ИмяКаталога;
	
	ПолноеИмяФайлаВыгрузки = ВременныйКаталог + ИмяФайлаВыгрузки;
	ФайлВыгрузки = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаВыгрузки);
	ПолноеИмяПодписанногоФайла = ВременныйКаталог + ФайлВыгрузки.ИмяБезРасширения + ".sgn";
	
	// при необходимости конвертируем текст выгрузки в нужную кодировку
	ТекстФайлаВыгрузки64 = Неопределено;
	Если СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет И ЗначениеЗаполнено(Кодировка) Тогда
		ТекстФайлаВыгрузки64 = ТекстВКодировке64(ТекстФайлаВыгрузки, Кодировка);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ВременныйКаталог", 					ВременныйКаталог);
	ДополнительныеПараметры.Вставить("ПолноеИмяПодписанногоФайла", 			ПолноеИмяПодписанногоФайла);
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", 				ПолноеИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("СертификатАбонента", 					СертификатАбонента);
	ДополнительныеПараметры.Вставить("СертификатРПН", 						СертификатРПН);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", 		СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("ФайлВыгрузки", 						ФайлВыгрузки);
	ДополнительныеПараметры.Вставить("Кодировка", 							Кодировка);
	ДополнительныеПараметры.Вставить("Идентификатор", 						Идентификатор);
	ДополнительныеПараметры.Вставить("Приложения", 							Приложения);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка", 					ОрганизацияСсылка);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНПослеЗаписиФайлаВыгрузки", ЭтотОбъект, ДополнительныеПараметры);
	Если ЗначениеЗаполнено(ТекстФайлаВыгрузки64) Тогда
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ТекстФайлаВыгрузки64, ПолноеИмяФайлаВыгрузки);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ТекстВФайл(Оповещение, ТекстФайлаВыгрузки, ПолноеИмяФайлаВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеЗаписиФайлаВыгрузки(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ВременныйКаталог 					= ДополнительныеПараметры.ВременныйКаталог;
	ПолноеИмяПодписанногоФайла 			= ДополнительныеПараметры.ПолноеИмяПодписанногоФайла;
	ПолноеИмяФайлаВыгрузки 				= ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатАбонента 					= ДополнительныеПараметры.СертификатАбонента;
	СертификатРПН 						= ДополнительныеПараметры.СертификатРПН;
	СертификатыИПараметрыОбмена 		= ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФайлВыгрузки 						= ДополнительныеПараметры.ФайлВыгрузки;
	Кодировка 							= ДополнительныеПараметры.Кодировка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Идентификатор 						= ДополнительныеПараметры.Идентификатор;
	Приложения 							= ДополнительныеПараметры.Приложения;
	ОрганизацияСсылка					= ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет Тогда
		РезультатВыгрузки = ПолноеИмяФайлаВыгрузки;
		
		ВозвращаемаяСтруктура = Новый Структура;
		ВозвращаемаяСтруктура.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
		ВозвращаемаяСтруктура.Вставить("РезультатВыгрузки", РезультатВыгрузки);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ВозвращаемаяСтруктура);
		Возврат;
	КонецЕсли;
	
	// подписываем
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Подписание файла выгрузки ...'")); // РПН
	
	Если СертификатАбонента = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось определить сертификат!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВременныйКаталог", 					ВременныйКаталог);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 				ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ПолноеИмяПодписанногоФайла", 			ПолноеИмяПодписанногоФайла);
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", 				ПолноеИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("СертификатАбонента", 					СертификатАбонента);
	ДополнительныеПараметры.Вставить("СертификатРПН", 						СертификатРПН);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", 		СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("ФайлВыгрузки", 						ФайлВыгрузки);
	ДополнительныеПараметры.Вставить("Идентификатор", 						Идентификатор);
	ДополнительныеПараметры.Вставить("Приложения", 							Приложения);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	НастройкиПодписи = Новый Структура;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ПолноеИмяФайлаВыгрузки);
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ПакетДляОтправкиВРПН", ОрганизацияСсылка); 
		ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации, НастройкиПодписи);
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНПослеПодписанияФайлаXMLDSigЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ПодписатьФайлXMLDSig(СертификатАбонента, ПолноеИмяФайлаВыгрузки, Оповещение, ПолноеИмяПодписанногоФайла, Кодировка, НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеПодписанияФайлаXMLDSigЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог 					= ДополнительныеПараметры.ВременныйКаталог;
	ВыполняемоеОповещение 				= ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолноеИмяПодписанногоФайла 			= ДополнительныеПараметры.ПолноеИмяПодписанногоФайла;
	ПолноеИмяФайлаВыгрузки 				= ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатАбонента 					= ДополнительныеПараметры.СертификатАбонента;
	СертификатРПН 						= ДополнительныеПараметры.СертификатРПН;
	СертификатыИПараметрыОбмена 		= ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ФайлВыгрузки 						= ДополнительныеПараметры.ФайлВыгрузки;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Идентификатор 						= ДополнительныеПараметры.Идентификатор;
	Приложения 							= ДополнительныеПараметры.Приложения;
	
	Если Результат = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось подписать файл выгрузки!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ИспользуетсяШифрование = Ложь;
	
	// шифруем
	Если ИспользуетсяШифрование Тогда
		ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Шифрование файла выгрузки ...'")); // РПН
		ИмяФайлаПакета = ФайлВыгрузки.ИмяБезРасширения + ".xms";
	Иначе
		ИмяФайлаПакета = ФайлВыгрузки.Имя;
	КонецЕсли;
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ПолноеИмяПодписанногоФайла = Результат;
		РезультатВыгрузки = Неопределено;
	ИначеЕсли ИспользуетсяШифрование Тогда
		РезультатВыгрузки = ВременныйКаталог + ИмяФайлаПакета;
	Иначе
		РезультатВыгрузки = ПолноеИмяПодписанногоФайла;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВременныйКаталог", 			ВременныйКаталог);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ПолноеИмяПодписанногоФайла", 	ПолноеИмяПодписанногоФайла);
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаВыгрузки", 		ПолноеИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("РезультатВыгрузки", 			РезультатВыгрузки);
	ДополнительныеПараметры.Вставить("ФайлВыгрузки", 				ФайлВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаПакета", 				ИмяФайлаПакета);
	ДополнительныеПараметры.Вставить("Приложения", 					Приложения);
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНПослеЗашифрованияЗавершение",
		ЭтотОбъект, ДополнительныеПараметры);

	
	Если ИспользуетсяШифрование Тогда
		СертификатыШифрования = Новый Массив;
		СертификатыШифрования.Добавить(СертификатРПН);
		
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
			Оповещение,
			ПолноеИмяПодписанногоФайла,
			СертификатыШифрования,
			РезультатВыгрузки,
			Ложь,,
			?(НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча), Идентификатор, Неопределено),
			"*");
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ЗашифрованныйФайл", Истина, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеЗашифрованияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВременныйКаталог 			= ДополнительныеПараметры.ВременныйКаталог;
	ВыполняемоеОповещение 		= ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолноеИмяПодписанногоФайла 	= ДополнительныеПараметры.ПолноеИмяПодписанногоФайла;
	ПолноеИмяФайлаВыгрузки 		= ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	РезультатВыгрузки 			= ДополнительныеПараметры.РезультатВыгрузки;
	ФайлВыгрузки 				= ДополнительныеПараметры.ФайлВыгрузки;
	ИмяФайлаПакета 				= ДополнительныеПараметры.ИмяФайлаПакета;
	Приложения 					= ДополнительныеПараметры.Приложения;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось зашифровать файл выгрузки!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если РезультатВыгрузки = Неопределено Тогда // ЭП в облаке
		РезультатВыгрузки = Новый Структура("ИмяФайлаЗашифрованногоПакета, ИмяФайлаВыгрузки", Результат.ЗашифрованныйФайл, ИмяФайлаПакета);
	КонецЕсли;
	
	ВозвращаемаяСтруктура = Новый Структура;
	ВозвращаемаяСтруктура.Вставить("СертификатыИПараметрыОбмена", 	СертификатыИПараметрыОбмена);
	ВозвращаемаяСтруктура.Вставить("РезультатВыгрузки", 			РезультатВыгрузки);
	ВозвращаемаяСтруктура.Вставить("ИмяФайлаВыгрузки", 				ФайлВыгрузки.Имя);
	ВозвращаемаяСтруктура.Вставить("Приложения", 					Приложения);
	ВозвращаемаяСтруктура.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВРПНПослеЧтенияФайлаПакета",
		ЭтотОбъект, ВозвращаемаяСтруктура);
	Если ТипЗнч(РезультатВыгрузки) = Тип("Структура") Тогда
		РезультатВыполнения = Новый Структура("Выполнено, СтрокаBase64", Истина, РезультатВыгрузки);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ВозвращаемаяСтруктура.РезультатВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВРПНПослеЧтенияФайлаПакета(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось сохранить подписанный файл выгрузки!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаПакета = Результат.СтрокаBase64;
	
	ВозвращаемаяСтруктура = ДополнительныеПараметры;
	ВозвращаемаяСтруктура.Удалить("ВыполняемоеОповещение");
	АдресФайлаПакета = АдресПакетОтправкиДляРПН(
		ВозвращаемаяСтруктура.ИмяФайлаВыгрузки,
		СтрокаBase64ФайлаПакета,
		ВозвращаемаяСтруктура.Приложения);
	ВозвращаемаяСтруктура.Удалить("Приложения");
	Если АдресФайлаПакета = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось упаковать файл выгрузки!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	ИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
		ВозвращаемаяСтруктура.ИмяФайлаВыгрузки).ИмяБезРасширения + ".zip";
	
	ВозвращаемаяСтруктура.РезультатВыгрузки = Новый Структура("ИмяФайлаЗашифрованногоПакета, ИмяФайлаВыгрузки",
		АдресФайлаПакета, ИмяФайлаПакета);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ВозвращаемаяСтруктура);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция АдресПакетОтправкиДляРПН(ИмяФайлаВыгрузки, РезультатВыгрузки, Приложения)
	
	Если ТипЗнч(РезультатВыгрузки) = Тип("Структура") Тогда
		КороткоеИмяЗагружаемогоФайла = РезультатВыгрузки.ИмяФайлаВыгрузки;
		ДвоичныеДанныеЗагружаемогоФайла = ПолучитьИзВременногоХранилища(РезультатВыгрузки.ИмяФайлаЗашифрованногоПакета);
		
	Иначе
		КороткоеИмяЗагружаемогоФайла = ИмяФайлаВыгрузки;
		ДвоичныеДанныеЗагружаемогоФайла = Base64Значение(РезультатВыгрузки);
	КонецЕсли;
	
	ВременныйКаталог = "";
	ПолноеИмяФайлаПакета = "";
	Попытка
		ВременныйКаталог = ОперацииСФайламиЭДКО.СоздатьВременныйКаталог();
		РегламентированнаяОтчетностьКлиентСервер.ПроверитьИмяФайла(КороткоеИмяЗагружаемогоФайла);
		//@skip-check 1c-fresh-audit
		ДвоичныеДанныеЗагружаемогоФайла.Записать(ВременныйКаталог + КороткоеИмяЗагружаемогоФайла);
		Для каждого ПриложениеВыгрузки Из Приложения Цикл
			ДвоичныеДанныеПриложения = ПолучитьИзВременногоХранилища(ПриложениеВыгрузки.АдресФайлаВыгрузки);
			ДвоичныеДанныеПриложения.Записать(ВременныйКаталог + ПриложениеВыгрузки.ИмяФайлаВыгрузки);
		КонецЦикла;
		
		ПолноеИмяФайлаПакета = ПолучитьИмяВременногоФайла();
		ОбъектЗапись = Новый ЗаписьZipФайла(ПолноеИмяФайлаПакета);
		ОбъектЗапись.Добавить(ВременныйКаталог + "*.*");
		ОбъектЗапись.Записать();
		
		ДвоичныеДанныеПакета = Новый ДвоичныеДанные(ПолноеИмяФайлаПакета);
	Исключение
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаПакета);
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		ЗаписьЖурналаРегистрации(
			НСтр("ru = 'Электронный документооборот с контролирующими органами. Выгрузка пакета отправки для Росприроднадзора'", ОбщегоНазначения.КодОсновногоЯзыка()),
			УровеньЖурналаРегистрации.Ошибка,,,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке));
		Возврат Неопределено;
	КонецПопытки;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВременныйКаталог);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ПолноеИмяФайлаПакета);
	
	Для каждого ПриложениеВыгрузки Из Приложения Цикл
		УдалитьИзВременногоХранилища(ПриложениеВыгрузки.АдресФайлаВыгрузки);
	КонецЦикла;
	
	Возврат ПоместитьВоВременноеХранилище(ДвоичныеДанныеПакета, Новый УникальныйИдентификатор);
	
КонецФункции

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
		
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН(
		ВходящийКонтекст.Форма,
		ВходящийКонтекст.ЭтоОтправкаИзФормыОтчетность,
		ВходящийКонтекст.ОтчетСсылка,
		ВходящийКонтекст.ОрганизацияОтчета);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН(
		Форма,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь,
		Знач ОтчетСсылка = Неопределено,
		Знач ОрганизацияОтчета = Неопределено) Экспорт
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияОтчета 	= ДокументооборотСРПНКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
		ОтчетСсылка 		= ДокументооборотСРПНКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Форма", 							Форма);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 	ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 					ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 				ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет", 			ТипЗнчСсылкаНаОтчет);
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") 
		ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	ЭтоРегламентированноеУведомление = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения"));
		
	Если ЭтоРегламентированныйОтчет ИЛИ ЭтоРегламентированноеУведомление Тогда
		
		// отправляем только из записанной формы
		Если Форма.Модифицированность Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН_ПослеСохранения",
				ЭтотОбъект,
				ДополнительныеПараметры);
			
			Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
			
		Иначе
			
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН_ПослеСохранения(Истина, ДополнительныеПараметры);
			
		КонецЕсли;
		
	Иначе // электронное представление
		
		Если Форма.Модифицированность Тогда
			ПоказатьПредупреждение(,НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
		Иначе
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН_ПослеСохранения(Истина, ДополнительныеПараметры);
		КонецЕсли;
		
	КонецЕсли;
		
КонецПроцедуры
		
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН_ПослеСохранения(Результат, ДополнительныеПараметры) Экспорт
	
	Форма                        = ДополнительныеПараметры.Форма;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета            = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	Если НЕ ЗначениеЗаполнено(ОтчетСсылка) Тогда
		ОтчетСсылка = ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
		ДополнительныеПараметры.ОтчетСсылка = ОтчетСсылка;
	КонецЕсли;
	
	ВидОтчетаФормыЭлектронногоПредставления = ?(
		Форма.ИмяФормы = "Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.Форма.ФормаЭлемента", 
		Форма.Объект.ВидОтчета, 
		Неопределено);
	
	ПараметрыПолученияИнформацииОбОтчете = Новый Структура();
	ПараметрыПолученияИнформацииОбОтчете.Вставить("ОтключитьОпределениеНеподписываемыхОтчетов", Ложь);
	ПараметрыПолученияИнформацииОбОтчете.Вставить("ИмяФормы",                                   Форма.ИмяФормы);
	ПараметрыПолученияИнформацииОбОтчете.Вставить("ВидОтчетаФормыЭлектронногоПредставления",    ВидОтчетаФормыЭлектронногоПредставления);

	
	СвойстваОбмена = СвойстваОбменаОрганизацииДляРПННаСервере(
		ОрганизацияОтчета,
		Истина,
		Ложь,
		,
		ОтчетСсылка,
		ПараметрыПолученияИнформацииОбОтчете);
		

	ДополнительныеПараметры.Вставить("СвойстваОбмена",                      СвойстваОбмена);
	КонтекстМоделиХраненияКлюча(СвойстваОбмена, ДополнительныеПараметры);
	
	Если НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен И НЕ СвойстваОбмена.ЭтоНеподписываемыйОтчет Тогда
		
		Если НЕ СвойстваОбмена.НастроенОбменВУниверсальномФормате Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПН_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ОтчетСсылка, ОписаниеОповещения);
			
		Иначе
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка);//+
		КонецЕсли;
		
		Возврат;
		
	КонецЕсли;
	
	Если СвойстваОбмена.НастроенОбменВУниверсальномФормате 
		И НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ОтчетСсылка) Тогда
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка);//+
		Возврат;
	КонецЕсли;
		
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПроверкиПодключения(ДополнительныеПараметры);

		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПроверкиПодключения(ДополнительныеПараметры)
	
	Если ДокументооборотСКОКлиент.ИспользуетсяРежимТестирования() Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПроверкиПодключенияЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ПродолжитьОтправкуНаТестовыйСерверРПН(ОписаниеОповещения);
		
	Иначе
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияОтправкиНаТС(ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПроверкиПодключенияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат Тогда
		Возврат;
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияОтправкиНаТС(ДополнительныеПараметры);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияОтправкиНаТС(ДополнительныеПараметры)
	
	Форма 			= ДополнительныеПараметры.Форма;
	СвойстваОбмена 	= ДополнительныеПараметры.СвойстваОбмена;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;

	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет", ТипЗнч(ОтчетСсылка));
	
	Если СвойстваОбмена.НастроенОбменВУниверсальномФормате 
		И СвойстваОбмена.НастройкиОбмена.ИспользоватьАвтонастройку 
		И СвойстваОбмена.ПравоИзмененияУчетнойЗаписи Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНАвтонастройкаУчетнойЗаписиЗавершение",
			ЭтотОбъект,
			ДополнительныеПараметры);
			
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОтчетСсылка, ОписаниеОповещения);
		
	Иначе
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеАвтонастройкиУчетнойЗаписи(ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНАвтонастройкаУчетнойЗаписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеАвтонастройкиУчетнойЗаписи(ДополнительныеПараметры);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеАвтонастройкиУчетнойЗаписи(ДополнительныеПараметры)
	
	ТипЗнчСсылкаНаОтчет          = ДополнительныеПараметры.ТипЗнчСсылкаНаОтчет;
	Форма                        = ДополнительныеПараметры.Форма;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	ЭтоРегламентированноеУведомление = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения"));
	
	Если ЭтоРегламентированныйОтчет ИЛИ ЭтоРегламентированноеУведомление Тогда
		
		// отправляем только из записанной формы
		Если Форма.Модифицированность Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеСохраненияОтчета",
				ЭтотОбъект,
				ДополнительныеПараметры);
			
			Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
			
		Иначе
			
			Если ОтчетСсылка = Неопределено Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
				Возврат;
			КонецЕсли;
			
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеЗаписиОтчета(ДополнительныеПараметры);
			
		КонецЕсли;
		
	Иначе // электронное представление
		
		Если Форма.Модифицированность Тогда
			ПоказатьПредупреждение(,НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
		Иначе
			
			Если ОтчетСсылка = Неопределено ИЛИ ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
				Возврат;
			КонецЕсли;
			
			ПоляДляЗаписиОтправки = ПоляДляЗаписиОтправкиВРПННаСервере(ОтчетСсылка);
			
			ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки(ДополнительныеПараметры);

		КонецЕсли;
		
	КонецЕсли;
	
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеСохраненияОтчета(Результат, ДополнительныеПараметры) Экспорт
	
	Форма                               = ДополнительныеПараметры.Форма;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	// если не удалось сохранить - возврат
	Если Результат = Ложь Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
		Возврат;
	КонецЕсли;
	
	// если отчет не был записан ранее, то получим ссылку на записанный отчет
	Если НЕ ЗначениеЗаполнено(ОтчетСсылка) Тогда
		
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			ОтчетСсылка = ДокументооборотСРПНКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
			ДополнительныеПараметры.ОтчетСсылка = ОтчетСсылка;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
			Возврат;
		КонецЕсли;
	
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеЗаписиОтчета(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеЗаписиОтчета(ДополнительныеПараметры)
	
	ТипЗнчСсылкаНаОтчет          = ДополнительныеПараметры.ТипЗнчСсылкаНаОтчет;
	Форма                        = ДополнительныеПараметры.Форма;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	ЭтоРегламентированноеУведомление = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.УведомлениеОСпецрежимахНалогообложения"));
	
	ПоляДляЗаписиОтправки = Новый Структура();
	
	Если ЭтоОтправкаИзФормыОтчетность Тогда
		
		Если НЕ ЭтоРегламентированноеУведомление Тогда
			ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ДатаНачала, ДатаОкончания, Вид");
			
			ПоляДляЗаписиОтправки.Вставить("ДатаНачалаПериода",    НачалоДня(ЗначенияРеквизитов.ДатаНачала));
			ПоляДляЗаписиОтправки.Вставить("ДатаОкончанияПериода", КонецДня(ЗначенияРеквизитов.ДатаОкончания));
			ПоляДляЗаписиОтправки.Вставить("Версия",               ЗначенияРеквизитов.Вид);
			
		Иначе
			ЗначениеРеквизитаДата = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(ОтчетСсылка, "Дата");
			
			ПоляДляЗаписиОтправки.Вставить("ДатаНачалаПериода",    НачалоГода(ЗначениеРеквизитаДата));
			ПоляДляЗаписиОтправки.Вставить("ДатаОкончанияПериода", КонецГода(ЗначениеРеквизитаДата));
			ПоляДляЗаписиОтправки.Вставить("Версия",               0);
		КонецЕсли;
		
	Иначе
		
		Если НЕ ЭтоРегламентированноеУведомление Тогда
			ПоляДляЗаписиОтправки.Вставить("ДатаНачалаПериода",    Форма.СтруктураРеквизитовФормы.мДатаНачалаПериодаОтчета);
			ПоляДляЗаписиОтправки.Вставить("ДатаОкончанияПериода", Форма.СтруктураРеквизитовФормы.мДатаКонцаПериодаОтчета);
			ПоляДляЗаписиОтправки.Вставить("Версия",               Форма.СтруктураРеквизитовФормы.ВидДокумента * Форма.НомерКорректировки);
			
		Иначе
			ПоляДляЗаписиОтправки.Вставить("ДатаНачалаПериода",    НачалоГода(Форма.Объект.Дата));
			ПоляДляЗаписиОтправки.Вставить("ДатаОкончанияПериода", КонецГода(Форма.Объект.Дата));
			ПоляДляЗаписиОтправки.Вставить("Версия",               0);
		КонецЕсли;
		
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", ПоляДляЗаписиОтправки);
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки(ДополнительныеПараметры)
	
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ОтчетСсылка, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт	
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ОтчетСсылка, ВходящийКонтекст.ОрганизацияОтчета, "РПН");
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправки_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;	
	
	Форма       = ВходящийКонтекст.Форма;
	ОтчетСсылка = ВходящийКонтекст.ОтчетСсылка;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправкиЗавершение",
		ЭтотОбъект,
		ВходящийКонтекст);
		
	ПодтверждениеОтправкиОтчета(
		Форма, 
		ОписаниеОповещения,
		ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодготовкиДанныхДляЗаписиОтправкиЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.ОК Тогда
		Возврат;
	КонецЕсли;
	
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ПодтвердитьПовторнуюОтправку(ОтчетСсылка, "РПН", ОписаниеОповещения);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ЭтоОтправкаИзФормыОтчетность 		= ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	Форма								= ДополнительныеПараметры.Форма;
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВРПН(
		ОтчетСсылка, 
		Форма, 
		ЭтоОтправкаИзФормыОтчетность);
		
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();

	Если РезультатВыгрузки = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось выгрузить отчет.'"));
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 	РезультатВыгрузки.ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 	РезультатВыгрузки.ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("Кодировка", 			РезультатВыгрузки.Кодировка);
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет", ТипЗнч(ОтчетСсылка));
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеИнициализации",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 					ОписаниеОповещения);
	КонтекстДлительнойОперации.Вставить("ОрганизацияОтчета", 					ОрганизацияОтчета);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, КонтекстДлительнойОперации);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ОтчетСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в РПН
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияЗавершение",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеПодтвержденияЗавершение() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ОрганизацияОтчета 					= КонтекстДлительнойОперации.ОрганизацияОтчета;
	ОписаниеОповещения 					= КонтекстДлительнойОперации.ОписаниеОповещения;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(КонтекстДлительнойОперации);
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);

	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеИнициализации(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
		Возврат;
	КонецЕсли;
		
	СертификатыИПараметрыОбмена = Неопределено;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеВыгрузкиПакета",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	Форма                               = ДополнительныеПараметры.Форма;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХранненияКлюча 				= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТекстФайлаВыгрузки                  = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки                    = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	Кодировка                           = ДополнительныеПараметры.Кодировка;

	ВыгрузитьПакетДляОтправкиВРПН(
		Форма,
		ОписаниеОповещения,
		Ложь,
		ТекстФайлаВыгрузки,
		ИмяФайлаВыгрузки,
		Кодировка,
		СертификатыИПараметрыОбмена,
		ЭтоОтправкаИзФормыОтчетность,
		ОтчетСсылка,
		ОрганизацияОтчета);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеВыгрузкиПакета(Результат, ДополнительныеПараметры) Экспорт	
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
		Возврат;
	КонецЕсли;
	
	СертификатыИПараметрыОбмена         = Результат.СертификатыИПараметрыОбмена;
	Форма                               = ДополнительныеПараметры.Форма;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПоляДляЗаписиОтправки               = ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	ПараметрыФормированияПакета         = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	
	Если ТипЗнч(Результат.РезультатВыгрузки) = Тип("Структура") ИЛИ НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		СтрокаBase64ФайлаПакета = Результат.РезультатВыгрузки.ИмяФайлаЗашифрованногоПакета;
		КороткоеИмяФайлаПакета  = Результат.РезультатВыгрузки.ИмяФайлаВыгрузки;
		
		Результат = Новый Структура();
		Результат.Вставить("Выполнено",      Истина);
		Результат.Вставить("ДвоичныеДанные", Неопределено);
		Результат.Вставить("СтрокаBase64",   СтрокаBase64ФайлаПакета);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ОрганизацияОтчета",                   ОрганизацияОтчета);
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки",               ПоляДляЗаписиОтправки);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

		Оповещение = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеЧтенияФайлаПакета",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	Иначе
		
		ИспользоватьШифрование = Ложь;
		
		Крипт = СертификатыИПараметрыОбмена.Крипт;
		
		КороткоеИмяФайлаПакета = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
			?(ИспользоватьШифрование, Результат.РезультатВыгрузки, Результат.ИмяФайлаВыгрузки)).Имя;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ОрганизацияОтчета",                   ОрганизацияОтчета);
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки",               ПоляДляЗаписиОтправки);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета",         ПараметрыФормированияПакета);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеЧтенияФайлаПакета", ЭтотОбъект, ДополнительныеПараметры);
		
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, Результат.РезультатВыгрузки);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеЧтенияФайлаПакета(Результат, ДополнительныеПараметры) Экспорт
	
	ОрганизацияОтчета 					= ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	СертификатыИПараметрыОбмена 		= ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыФормированияПакета 		= ДополнительныеПараметры.ПараметрыФормированияПакета;
	КороткоеИмяФайлаПакета 				= ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Форма 								= ДополнительныеПараметры.Форма;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаПакета = Результат.СтрокаBase64;
	
	СтрокаBase64ФайлаОтправки = СформироватьФайлОтправкиВРПННаСервере(
		КороткоеИмяФайлаПакета,
		СтрокаBase64ФайлаПакета,
		ЭтоАдресВременногоХранилища(СтрокаBase64ФайлаПакета) ИЛИ НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча),
		Форма.УникальныйИдентификатор);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Отправка отчета ...'")); // РПН
	
	ФормаДлительнаяОперация = Неопределено;
	
	ПараметрыОтправки = Новый Структура();
	ПараметрыОтправки.Вставить("Организация", 				ОрганизацияОтчета);
	ПараметрыОтправки.Вставить("АдресСервера", 				СерверПриемаОтчетностиРПН(Истина, ОрганизацияОтчета));
	ПараметрыОтправки.Вставить("ПоляДляЗаписиОтправки", 	ПоляДляЗаписиОтправки);
	ПараметрыОтправки.Вставить("ЭтоНеподписываемыйОтчет", 	СертификатыИПараметрыОбмена.ЭтоНеподписываемыйОтчет);
	ПараметрыОтправки.Вставить("БумажнаяПодачаОтчета", 		СертификатыИПараметрыОбмена.БумажнаяПодачаОтчета);
	
	ПараметрыСоединения = Новый Структура();
	ПараметрыСоединения.Вставить("EMail", 							ПараметрыФормированияПакета.EMail);
	ПараметрыСоединения.Вставить("Пароль", 							ПараметрыФормированияПакета.Пароль);
	ПараметрыСоединения.Вставить("СохранитьПароль", 				ЗначениеЗаполнено(ПараметрыФормированияПакета.Пароль));
	ПараметрыСоединения.Вставить("СохраненныйEmail", 				ПараметрыФормированияПакета.EMail);
	ПараметрыСоединения.Вставить("СохраненныйПароль", 				ПараметрыФормированияПакета.Пароль);
	ПараметрыСоединения.Вставить("СоединениеHTTP", 					Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie", 					"");
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиНаСервере", 	"");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером", 			"");
	ПараметрыСоединения.Вставить("ПовторятьСоединение", 			Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля", 			ПараметрыФормированияПакета.ОбменНапрямую);
	ПараметрыСоединения.Вставить("СообщенияПользователю", 			Новый Массив);
	ПараметрыСоединения.Вставить("ОбменНапрямую", 					ПараметрыФормированияПакета.ОбменНапрямую);
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиОператору", 	Неопределено);
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНОтправкаВЦикле(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		КороткоеИмяФайлаПакета,
		Форма);
	
КонецПроцедуры

// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (Цикл) 
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНОтправкаВЦикле(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		КороткоеИмяФайлаПакета,
		Форма)
		
	РезультатОтправки = Неопределено;
	ПараметрыСоединения.ЗначениеCookie = "";
	
	ПерваяПопыткаАвторизации = Истина;
	Если ПараметрыСоединения.ОбменНапрямую Тогда
		ПараметрыСоединения.ПовторятьЗапросПароля = Истина;
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНВводПароля(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		КороткоеИмяФайлаПакета,
		РезультатОтправки,
		ПерваяПопыткаАвторизации,
		Форма);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНВводПароля(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		КороткоеИмяФайлаПакета,
		РезультатОтправки,
		ПерваяПопыткаАвторизации,
		Форма)
	
	Если ПараметрыСоединения.ОбменНапрямую 
		И (НЕ ЗначениеЗаполнено(ПараметрыСоединения.EMail) ИЛИ НЕ ЗначениеЗаполнено(ПараметрыСоединения.Пароль) ИЛИ НЕ ПерваяПопыткаАвторизации) Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("РезультатОтправки",                   РезультатОтправки);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации",            ПерваяПопыткаАвторизации);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНВводПароляЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		ВызватьФормуВводаПароляПорталаРПН(
			"https://" + ПараметрыОтправки.АдресСервера + "/",
			ПараметрыСоединения.EMail,
			ПараметрыСоединения.Пароль,
			ПараметрыСоединения.СохранитьПароль,
			ОписаниеОповещения);
		
		Возврат;
	КонецЕсли;
	
	РезультатОтправки = ОтправитьВРПННаСервере(
		МестоХраненияКлюча,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		КороткоеИмяФайлаПакета,
		ОтчетСсылка,
		ПараметрыОтправки,
		ПараметрыСоединения);
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ПерваяПопыткаАвторизации = Ложь;
	
	// рекурсивный вызов - замена цикла
	Если РезультатОтправки = Неопределено И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНВводПароля(
			ОтчетСсылка,
			МестоХраненияКлюча,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ФормаДлительнаяОперация,
			СтрокаBase64ФайлаОтправки,
			СтрокаBase64ФайлаПакета,
			КороткоеИмяФайлаПакета,
			РезультатОтправки,
			ПерваяПопыткаАвторизации,
			Форма);
			
	Иначе
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеОтправки(
			ОтчетСсылка,
			МестоХраненияКлюча,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ФормаДлительнаяОперация,
			СтрокаBase64ФайлаОтправки,
			СтрокаBase64ФайлаПакета,
			КороткоеИмяФайлаПакета,
			РезультатОтправки,
			Форма);
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНВводПароляЗавершение(РезультатВводаПароля, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	РезультатОтправки                   = ДополнительныеПараметры.РезультатОтправки;
	ПерваяПопыткаАвторизации            = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	Форма                               = ДополнительныеПараметры.Форма;
	
	Если РезультатВводаПароля = Неопределено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН
		Возврат;
	Иначе
		
		ПараметрыСоединения.EMail           = РезультатВводаПароля.EMail;
		ПараметрыСоединения.Пароль          = РезультатВводаПароля.Пароль;
		ПараметрыСоединения.СохранитьПароль = РезультатВводаПароля.СохранитьПароль;
		
		РезультатОтправки = ОтправитьВРПННаСервере(
			МестоХраненияКлюча,
			СтрокаBase64ФайлаОтправки,
			СтрокаBase64ФайлаПакета,
			КороткоеИмяФайлаПакета,
			ОтчетСсылка,
			ПараметрыОтправки,
			ПараметрыСоединения);
		
		ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
			
		ПерваяПопыткаАвторизации = Ложь;
		
		Если РезультатОтправки = Неопределено И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
			
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНВводПароля(
				ОтчетСсылка,
				МестоХраненияКлюча,
				ПараметрыОтправки,
				ПараметрыСоединения,
				ФормаДлительнаяОперация,
				СтрокаBase64ФайлаОтправки,
				СтрокаBase64ФайлаПакета,
				КороткоеИмяФайлаПакета,
				РезультатОтправки,
				ПерваяПопыткаАвторизации,
				Форма);
				
		Иначе
			
			ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеОтправки(
				ОтчетСсылка,
				МестоХраненияКлюча, 
				ПараметрыОтправки,
				ПараметрыСоединения,
				ФормаДлительнаяОперация,
				СтрокаBase64ФайлаОтправки,
				СтрокаBase64ФайлаПакета,
				КороткоеИмяФайлаПакета,
				РезультатОтправки,
				Форма);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНПослеОтправки(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаПакета,
		КороткоеИмяФайлаПакета,
		РезультатОтправки,
		Форма)
	
	Если РезультатОтправки <> Неопределено Тогда
		
		ПараметрыАвтозапроса = Новый Структура();
		ПараметрыАвтозапроса.Вставить("Ключ",           РезультатОтправки);
		ПараметрыАвтозапроса.Вставить("ЗначениеCookie", ПараметрыСоединения.ЗначениеCookie);
		ПараметрыАвтозапроса.Вставить("EMail",          ПараметрыСоединения.EMail);
		ПараметрыАвтозапроса.Вставить("Пароль",         ПараметрыСоединения.Пароль);

		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
			
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // РПН.
		
	ИначеЕсли ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// не получено файла результата при отправке пакета. Возможно, проблемы с доступом в интернет
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПакета",             СтрокаBase64ФайлаПакета);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаПакета",              КороткоеИмяФайлаПакета);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВРПНПослеПоказаОшибкиДоступаВИнтернет", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу Росприроднадзора'"), 
			ОписаниеОповещения);
			
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // РПН.
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВРПНПослеПоказаОшибкиДоступаВИнтернет(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	СтрокаBase64ФайлаПакета             = ДополнительныеПараметры.СтрокаBase64ФайлаПакета;
	КороткоеИмяФайлаПакета              = ДополнительныеПараметры.КороткоеИмяФайлаПакета;
	Форма                               = ДополнительныеПараметры.Форма;
	
	ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// рекурсивный вызов - замена цикла
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляРПНОтправкаВЦикле(
			ОтчетСсылка,
			МестоХраненияКлюча,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ФормаДлительнаяОперация,
			СтрокаBase64ФайлаОтправки,
			СтрокаBase64ФайлаПакета,
			КороткоеИмяФайлаПакета,
			Форма);
			
	Иначе
			
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // РПН. Нет доступа в Интернет.
		
	КонецЕсли;
	
КонецПроцедуры
// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаКлиенте
Процедура ВызватьФормуВводаПароляПорталаРПН(Портал, EMail, Пароль, СохранитьПароль, ВыполняемоеОповещение = Неопределено)
	
	ПараметрыФормы = Новый Структура("Портал, EMail, Пароль, СохранитьПароль", Портал, EMail, Пароль, СохранитьПароль);
	ОписаниеОповещения = Новый ОписаниеОповещения("ВызватьФормуВводаПароляПорталаРПНЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ВводПароляРПН", ПараметрыФормы,,,,, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ВызватьФормуВводаПароляПорталаРПНЗавершение(РезультатФормы, ВыполняемоеОповещение) Экспорт
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПротоколОбработкиПоСсылкеИсточникаДляРПН(ИсточникСсылка, ПечатьВозможна = Истина) Экспорт
	
	ВАрхиве = ОбъектВАрхиве(ИсточникСсылка, "Протокол");
	Если ВАрхиве Тогда 
		ПоказатьУведомлениеАрхивныхФайлов(, 16, 0, Истина);
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ИсточникСсылка",      ИсточникСсылка);
	ПараметрыФормы.Вставить("ПечатьВозможна",      ПечатьВозможна);
	ПараметрыФормы.Вставить("КонтролирующийОрган", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН"));
		
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПротоколОбработкиHTML", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьОтправкуНаТестовыйСерверРПН(ВыполняемоеОповещение) Экспорт
	
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВыполняемоеОповещение) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);

	Иначе
		
		ПродолжитьОтправкуНаТестовыйСерверРПН = Ложь;
		
		Если ДокументооборотСКОКлиент.ИспользуетсяРежимТестирования() Тогда
			ОписаниеОповещения = Новый ОписаниеОповещения("ПродолжитьОтправкуНаТестовыйСерверРПНЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
			ПоказатьВопрос(ОписаниеОповещения, НСтр("ru = 'Внимание! Включен режим тестирования!
				|
				|В режиме тестирования отчетность отправляется через тестовый портал Росприроднадзора.
				|Сдача отчетности засчитана не будет.
				|
				|Продолжить отправку?'"), РежимДиалогаВопрос.ДаНет);
		Иначе
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ПродолжитьОтправкуНаТестовыйСерверРПН);
		КонецЕсли;
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ПродолжитьОтправкуНаТестовыйСерверРПНЗавершение(Ответ, ВыполняемоеОповещение) Экспорт
	
	ПродолжитьОтправкуНаТестовыйСерверРПН = (Ответ = КодВозвратаДиалога.Да);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ПродолжитьОтправкуНаТестовыйСерверРПН);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбновитьРегистрыСведенийНастройкиОбменаРПН(Организация, ОтпечатокСертификата, СертификатРПНОтпечаток, ВыводитьСообщения, EMail, Пароль)
	
	Попытка
		
		ОбменРПН = Мультирежим.МенеджерЗаписиНастроекОбмена(
			Организация,
			ОтпечатокСертификата,
			"РПН",
			"НастройкиОбменаРПН",
			"СертификатАбонентаОтпечаток");

		ОбменРПН.СертификатРПНОтпечаток = СертификатРПНОтпечаток;
		ОбменРПН.ИспользоватьАвтонастройку = Истина;
		ОбменРПН.EMail = EMail;
		ОбменРПН.Пароль = Пароль;
		ОбменРПН.Записать();
		
		Если ВыводитьСообщения Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				НСтр("ru = 'Обновлены параметры отправки отчетности в Росприроднадзор.'"));
		КонецЕсли;
		
	Исключение
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось обновить параметры отправки отчетности в Росприроднадзор по причине:
                  |%1'"), КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецПопытки;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьФайлОтправкиВРПННаСервере(КороткоеИмяФайлаВыгрузки, СтрокаBase64ФайлаВыгрузки, ЭтоАдреса = Истина, Адрес = Неопределено)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СформироватьФайлОтправкиВРПНССервера(КороткоеИмяФайлаВыгрузки, СтрокаBase64ФайлаВыгрузки, ЭтоАдреса, Адрес);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбновитьРезультатКонкретнойОтправкиРПННаСервере(Знач ОтправкаСсылка, Знач ОтправкаОбъектЗагружен, Знач ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбновитьРезультатКонкретнойОтправкиРПНССервера(ОтправкаСсылка, ОтправкаОбъектЗагружен, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВРПННаСервере(МестоХраненияКлюча, Знач СтрокаBase64ФайлаОтправки, Знач СтрокаBase64ФайлаПакета, Знач КороткоеИмяФайлаПакета, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьВРПНССервера(МестоХраненияКлюча, СтрокаBase64ФайлаОтправки, СтрокаBase64ФайлаПакета, КороткоеИмяФайлаПакета, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоляДляЗаписиОтправкиВРПННаСервере(ОбъектСсылка)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПоляНаСервереДляЗаписиОтправкиВРПН(ОбъектСсылка);
	
КонецФункции

&НаСервере
Функция СерверПриемаОтчетностиРПН(ОбменНапрямую = Ложь, Организация = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СерверПриемаОтчетностиРПН(ОбменНапрямую, Организация);
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойстваОбменаОрганизацииДляРПННаСервере(Знач ОрганизацияСсылка, Знач ОпределитьНастроенностьОбменаВУниверсальномФормате, Знач ПолучитьОсновныеСвойстваПоследнейОтправки, Знач ОтправкаСсылка = Неопределено, ОтчетСсылка = Неопределено, ПараметрыПолученияИнформацииОбОтчете = Неопределено, СвойстваПоследнейОтправки = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СвойстваОбменаОрганизацииДляРПН(ОрганизацияСсылка, ОпределитьНастроенностьОбменаВУниверсальномФормате, ПолучитьОсновныеСвойстваПоследнейОтправки, ОтправкаСсылка, ОтчетСсылка, ПараметрыПолученияИнформацииОбОтчете, СвойстваПоследнейОтправки);
	
КонецФункции

&НаСервереБезКонтекста
Функция НастройкиРПННаСервере(Знач ОрганизацияСсылка, Знач ПолучитьПараметрыОнлайнПроверки = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.НастройкиРПН(ОрганизацияСсылка, ПолучитьПараметрыОнлайнПроверки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ТекстВКодировке64(Текст, Кодировка)
	
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
	ЗаписьТекста = Новый ЗаписьТекста(ИмяВременногоФайла, Кодировка);
	ЗаписьТекста.Записать(Текст);
	ЗаписьТекста.Закрыть();
	
	ДвоичныеДанные = Новый ДвоичныеДанные(ИмяВременногоФайла);
	Результат = Base64Строка(ДвоичныеДанные);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсФСС(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	Если ЗначениеЗаполнено(Организация) Тогда 
		ПараметрыФормы = Новый Структура("ОрганизацияСсылка", Организация);
		ОткрытьФорму("РегистрСведений.НастройкиОбменаФСС.ФормаЗаписи", ПараметрыФормы,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	Иначе
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("ВидНастроек", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС"));
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаНастроекДокументооборотаОрганизаций", ПараметрыОткрытия,,Новый УникальныйИдентификатор,Окно);
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсФСРАР(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	Если ЗначениеЗаполнено(Организация) Тогда
		ПараметрыФормы = Новый Структура("ОрганизацияСсылка", Организация);
		ОткрытьФорму("РегистрСведений.НастройкиОбменаФСРАР.ФормаЗаписи", ПараметрыФормы,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);	    		
	Иначе
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("ВидНастроек", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР"));
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаНастроекДокументооборотаОрганизаций", ПараметрыОткрытия,,Новый УникальныйИдентификатор,Окно);
	КонецЕсли;	
		
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсРПН(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	Если ЗначениеЗаполнено(Организация) Тогда
		ПараметрыФормы = Новый Структура("ОрганизацияСсылка", Организация);
		ОткрытьФорму("РегистрСведений.НастройкиОбменаРПН.ФормаЗаписи", ПараметрыФормы,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	Иначе
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("ВидНастроек", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН"));
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаНастроекДокументооборотаОрганизаций", ПараметрыОткрытия,,Новый УникальныйИдентификатор,Окно);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсФТС(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	Если ЗначениеЗаполнено(Организация) Тогда
		ПараметрыФормы = Новый Структура("ОрганизацияСсылка", Организация);
		ОткрытьФорму("РегистрСведений.НастройкиОбменаФТС.ФормаЗаписи", ПараметрыФормы,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	Иначе
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("ВидНастроек", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС"));
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаНастроекДокументооборотаОрганизаций", ПараметрыОткрытия,,Новый УникальныйИдентификатор,Окно);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсРЭЦ(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	ПутьКНастройкамРЭЦ = "РегистрСведений.НастройкиОбменаРЭЦ";
	Если ЗначениеЗаполнено(Организация) Тогда
		ПараметрыФормы = Новый Структура("ОрганизацияСсылка", Организация);
		ОткрытьФорму(ПутьКНастройкамРЭЦ + ".ФормаЗаписи", ПараметрыФормы,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	Иначе
		ОткрытьФорму(ПутьКНастройкамРЭЦ + ".ФормаСписка",,, Новый УникальныйИдентификатор, Окно);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОбщихНастроекЭДО(Окно = Неопределено) Экспорт 
		
	ОткрытьФорму(ПутьКОбъекту + ".Форма.Настройки",,,, Окно);
		
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуСпискаЗаявленийНаПодключение(Организация = Неопределено) Экспорт
	
	// Если Организация известна, то открываем список заявлений только по этой организации
	Если ЗначениеЗаполнено(Организация) Тогда
		ДополнительныеПараметры = Новый Структура("Организация", Организация);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.Документ_ЗаявлениеАбонентаСпецоператораСвязи_ФормаСписка", ДополнительныеПараметры);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.Документ_ЗаявлениеАбонентаСпецоператораСвязи_ФормаСписка");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуНастроекОбмена(Организация = Неопределено) Экспорт
	
	// Если организация известна, то открываем настройки обмена только по этой организации
	Если ЗначениеЗаполнено(Организация) Тогда
		ДополнительныеПараметры = Новый Структура("Организация", Организация);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.НастройкиОбмена", ДополнительныеПараметры,,Организация);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.СписокНастроекОбмена");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуРасширенныхНастроек(Организация) Экспорт
	
	ДополнительныеПараметры = Новый Структура("Организация", Организация);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.РасширенныеНастройки", ДополнительныеПараметры);
	
КонецПроцедуры


&НаКлиенте
Процедура ОткрытьФормуПодключенияИлиНастройкиНаправленияСдачиОтчетности(
		ЭлементНастроекОбмена,
		ТипКонтролирующегоОргана,
		ТекущаяДатаССервера = Неопределено,
		ОповещениеОЗавершении = Неопределено) Экспорт
	
	ПравоЧтенияУчетнойЗаписиОбмена 		= ЭлементНастроекОбмена.ПравоЧтенияУчетнойЗаписиОбмена;
	СпецоператорСвязи 					= ЭлементНастроекОбмена.СпецоператорСвязи;
	СвойстваСертификатаРуководителя 	= ЭлементНастроекОбмена.СвойстваСертификатаРуководителя;
	
	ЕстьУчетнаяЗапись = ЭлементНастроекОбмена.ВидОбменаСКонтролирующимиОрганами =
		ПредопределенноеЗначение("Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате")
		И ЗначениеЗаполнено(ЭлементНастроекОбмена.УчетнаяЗаписьОбмена);
	
	Если ЕстьУчетнаяЗапись И ЭлементНастроекОбмена.ПравоЧтенияУчетнойЗаписиОбмена Тогда
		ЗаявлениеНаИзменениеДоступно =
			ЗначениеЗаполнено(ЭлементНастроекОбмена.СпецоператорСвязи)
			И ЭлементНастроекОбмена.СпецоператорСвязи <> ПредопределенноеЗначение("Перечисление.СпецоператорыСвязи.Такском")
			И ЭлементНастроекОбмена.СпецоператорСвязи <> ПредопределенноеЗначение("Перечисление.СпецоператорыСвязи.Прочие");
	Иначе
		ЗаявлениеНаИзменениеДоступно = Ложь;
	КонецЕсли;
	
	Если ЕстьУчетнаяЗапись И ЭлементНастроекОбмена.ПравоЧтенияУчетнойЗаписиОбмена
		И ЭлементНастроекОбмена.СвойстваСертификатаРуководителя <> Неопределено Тогда
		
		ТекДата = ?(ТекущаяДатаССервера <> Неопределено, ТекущаяДатаССервера, ТекущаяДатаНаСервере());
		СертификатДействителен = ТекДата >= СвойстваСертификатаРуководителя.ДействителенС
			И ТекДата <= СвойстваСертификатаРуководителя.ДействителенПо;
	Иначе
		СертификатДействителен = Ложь;
	КонецЕсли;
	
	НастроитьПодключенноеНаправление = Ложь;
	НаправлениеПодключено = Ложь;
	Если ЗаявлениеНаИзменениеДоступно Тогда
		Если ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.УчетнаяЗаписьПредназначенаДляДокументооборотаСФНС <> ЭлементНастроекОбмена.СдаватьВФНС;
			НаправлениеПодключено = ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСФНС;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.УчетнаяЗаписьПредназначенаДляДокументооборотаСПФР <> ЭлементНастроекОбмена.СдаватьВПФР;
			НаправлениеПодключено = ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСПФР;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.УчетнаяЗаписьПредназначенаДляДокументооборотаСФСГС <> ЭлементНастроекОбмена.СдаватьВФСГС;
			НаправлениеПодключено = ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСФСГС;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ЦБ") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.УчетнаяЗаписьПредназначенаДляДокументооборотаСЦБ <> ЭлементНастроекОбмена.СдаватьВЦБ;
			НаправлениеПодключено = ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСЦБ;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.ОбщиеНастройкиОбменаФСС_ИспользоватьОбмен <> ЭлементНастроекОбмена.СдаватьВФСС;
			НаправлениеПодключено = ЭлементНастроекОбмена.НастройкиОбменаФСС_ИспользоватьОбмен;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.ОбщиеНастройкиОбменаФСРАР_ИспользоватьОбмен <> ЭлементНастроекОбмена.СдаватьВФСРАР;
			НаправлениеПодключено = ЭлементНастроекОбмена.НастройкиОбменаФСРАР_ИспользоватьОбмен;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.ОбщиеНастройкиОбменаРПН_ИспользоватьОбмен <> ЭлементНастроекОбмена.СдаватьВРПН;
			НаправлениеПодключено = ЭлементНастроекОбмена.НастройкиОбменаРПН_ИспользоватьОбмен;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС") Тогда
			НастроитьПодключенноеНаправление =
				ЭлементНастроекОбмена.ОбщиеНастройкиОбменаФТС_ИспользоватьОбмен <> ЭлементНастроекОбмена.СдаватьВФТС;
			НаправлениеПодключено = ЭлементНастроекОбмена.НастройкиОбменаФТС_ИспользоватьОбмен;
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.Минобороны") Тогда
			НастроитьПодключенноеНаправление = Истина;
			НаправлениеПодключено = ЭлементНастроекОбмена.НастройкиОбменаМинобороны_ИспользоватьОбмен;
		КонецЕсли;
	КонецЕсли;
	
	ЭтоФНСПФРИлиРосстатИлиЦБ = (ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС")
		ИЛИ ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР")
		ИЛИ ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС")
		ИЛИ ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ЦБ"));
	
	Если ЗаявлениеНаИзменениеДоступно И НЕ НастроитьПодключенноеНаправление Тогда
		ДополнительныеПараметры = Новый Структура("ЭлементНастроекОбмена", ЭлементНастроекОбмена);
		
		Оповещение = Новый ОписаниеОповещения("ОткрытьФормуПодключенияИлиНастройкиНаправленияСдачиОтчетностиПослеФормы",
			ЭтотОбъект, ДополнительныеПараметры);
		
		Если НаправлениеПодключено И ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР") Тогда
			
			СостояниеЭДОсПФР = ЭлементНастроекОбмена.СостояниеЭДОсПФР;
			
			ОткрытьПредложениеОтправитьЗаявление = НужноОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(СостояниеЭДОсПФР, Ложь)
				И НЕ СостояниеЭДОсПФР.НаОтключение.ОрганизацияОтключена;
			
			Если ОткрытьПредложениеОтправитьЗаявление Тогда
				
				ОткрытьФормуПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиОтправки(
					СостояниеЭДОсПФР,
					ЭлементНастроекОбмена.Организация,
					ОповещениеОЗавершении);
				
			Иначе
				
				ПараметрыОтключенияНаправления= Новый Структура;
				ПараметрыОтключенияНаправления.Вставить("НастроитьМЧД", Истина);
				ОткрытьФормуОтключенияНаправленияСдачиОтчетности(
					Оповещение,
					ЭлементНастроекОбмена.Организация,
					ТипКонтролирующегоОргана,
					ПараметрыОтключенияНаправления);
				
			КонецЕсли;
			
		ИначеЕсли НаправлениеПодключено
			И ТипКонтролирующегоОргана <> ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС")
			И ТипКонтролирующегоОргана <> ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС") Тогда
			
			ОткрытьФормуОтключенияНаправленияСдачиОтчетности(
				Оповещение,
				ЭлементНастроекОбмена.Организация,
				ТипКонтролирующегоОргана);
			
		Иначе

			Если ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС") Тогда
				Получатели = Новый СписокЗначений;
				Для каждого КодФНС Из ЭлементНастроекОбмена.КодыФНС Цикл
					Получатели.Добавить(КодФНС.Код, КодФНС.КПП);
				КонецЦикла;
				
			ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС") Тогда
				Получатели = Новый СписокЗначений;
				Для каждого КодФСГС Из ЭлементНастроекОбмена.КодыФСГС Цикл
					Получатели.Добавить(КодФСГС);
				КонецЦикла;
				
			Иначе
				Получатели = Неопределено;
			КонецЕсли;
			
			ПараметрыПодключенияНаправления = Новый Структура;
			ПараметрыПодключенияНаправления.Вставить("НаправлениеПодключено", НаправлениеПодключено);
			Если ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС") Тогда
				ПараметрыПодключенияНаправления.Вставить("НастроитьМЧД", Истина);
			КонецЕсли;
			ОткрытьФормуПодключенияНаправленияСдачиОтчетности(
				Оповещение,
				ЭлементНастроекОбмена.Организация,
				ЭлементНастроекОбмена.СпецоператорСвязи,
				ТипКонтролирующегоОргана,
				Получатели,
				НЕ СертификатДействителен,
				ПараметрыПодключенияНаправления);
		КонецЕсли;
		
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		КонецЕсли;
		
		Возврат;
		
	Иначе
		Если ЭтоФНСПФРИлиРосстатИлиЦБ Тогда
			
			Если ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ПФР") Тогда
				
				СостояниеЭДОсПФР = ЭлементНастроекОбмена.СостояниеЭДОсПФР;
				
				ОткрытьПредложениеОтправитьЗаявление = НужноОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(СостояниеЭДОсПФР, Ложь);
				
				Если ОткрытьПредложениеОтправитьЗаявление Тогда
					
					ОткрытьФормуПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиОтправки(
						СостояниеЭДОсПФР, 
						ЭлементНастроекОбмена.Организация,
						ОповещениеОЗавершении);
					
				Иначе
					
					ОткрытьФормуНастройкиЭДОсФНСиПФРиРосстатомиЦБ(
						ЭлементНастроекОбмена.Организация,
						ЭтотОбъект,,,
						ТипКонтролирующегоОргана);
					
				КонецЕсли;
			Иначе
				ОткрытьФормуНастройкиЭДОсФНСиПФРиРосстатомиЦБ(
					ЭлементНастроекОбмена.Организация,
					ЭтотОбъект,,,
					ТипКонтролирующегоОргана);
			КонецЕсли;
			
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС") Тогда
			ОткрытьФормуНастройкиЭДОсФСС(ЭлементНастроекОбмена.Организация, ЭтотОбъект);
			
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР") Тогда
			ОткрытьФормуНастройкиЭДОсФСРАР(ЭлементНастроекОбмена.Организация, ЭтотОбъект);
			
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН") Тогда
			ОткрытьФормуНастройкиЭДОсРПН(ЭлементНастроекОбмена.Организация, ЭтотОбъект);
			
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС") Тогда
			ОткрытьФормуНастройкиЭДОсФТС(ЭлементНастроекОбмена.Организация, ЭтотОбъект);
			
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.Минобороны") Тогда
			ОткрытьФормуНастройкиЭДОсМинобороны(ЭлементНастроекОбмена.Организация, ЭтотОбъект);
			
		КонецЕсли;
		
		Если ОповещениеОЗавершении <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПодключенияИлиНастройкиНаправленияСдачиОтчетностиПослеФормы(Результат, ДополнительныеПараметры) Экспорт
	
	ЭлементНастроекОбмена = ДополнительныеПараметры.ЭлементНастроекОбмена;
	
	Если ТипЗнч(Результат) = Тип("ПеречислениеСсылка.ТипыКонтролирующихОрганов") Тогда
		ФлагиВМастер = Новый Структура;
		ФлагиВМастер.Вставить("ПодключаемоеНаправление", 					Результат);
		ФлагиВМастер.Вставить("ПродлитьСертификат", 						Ложь);
		ФлагиВМастер.Вставить("ИзменитьРеквизитыПодключенияК1СОтчетности", 	Ложь);
		ФлагиВМастер.Вставить("ПродлитьЛицензиюНа1СОтчетность", 			Ложь);
		
		АктивироватьФлагиМастера = Новый Структура;
		АктивироватьФлагиМастера.Вставить("ФлагиВМастер", ФлагиВМастер);
		
		ПараметрыОткрытияФормы = Новый Структура;
		ПараметрыОткрытияФормы.Вставить("Организация", ЭлементНастроекОбмена.Организация);
		ПараметрыОткрытияФормы.Вставить("АктивироватьФлагиМастера", АктивироватьФлагиМастера);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
			ЭлементНастроекОбмена.Организация,
			ЭтотОбъект,,
			ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
			ПараметрыОткрытияФормы);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПодключенияНаправленияСдачиОтчетности(
		ОповещениеОЗавершении,
		Организация,
		Спецоператор,
		ТипПолучателя,
		СписокЗначенийПолучатели,
		СертификатНедоступенИлиИстек = Ложь,
		ПараметрыПодключенияНаправления = Неопределено,
		ОтправкаЗаявленияЗапрещена = Ложь) Экспорт
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Организация", 					Организация);
	ДополнительныеПараметры.Вставить("Спецоператор", 					Спецоператор);
	ДополнительныеПараметры.Вставить("ТипПолучателя", 					ТипПолучателя);
	ДополнительныеПараметры.Вставить("Получатели", 						СписокЗначенийПолучатели);
	ДополнительныеПараметры.Вставить("СертификатНедоступенИлиИстек", 	СертификатНедоступенИлиИстек);
	ДополнительныеПараметры.Вставить("ПараметрыПодключенияНаправления", ПараметрыПодключенияНаправления);
	ДополнительныеПараметры.Вставить("ОтправкаЗаявленияЗапрещена", 		ОтправкаЗаявленияЗапрещена);
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ПодключениеНаправленияСдачиОтчетности",
		ДополнительныеПараметры,,,,,
		ОповещениеОЗавершении,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтключенияНаправленияСдачиОтчетности(
		ОповещениеОЗавершении,
		Организация,
		ТипПолучателя,
		ПараметрыОтключенияНаправления = Неопределено,
		ОтправкаЗаявленияЗапрещена = Ложь) Экспорт
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Организация", 					Организация);
	ДополнительныеПараметры.Вставить("ТипПолучателя", 					ТипПолучателя);
	ДополнительныеПараметры.Вставить("ПараметрыОтключенияНаправления", 	ПараметрыОтключенияНаправления);
	ДополнительныеПараметры.Вставить("ОтправкаЗаявленияЗапрещена", 		ОтправкаЗаявленияЗапрещена);
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ОтключениеНаправленияСдачиОтчетности",
		ДополнительныеПараметры,,,,,
		ОповещениеОЗавершении,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуСпискаПользователей(
		ОповещениеОЗавершении,
		СписокЗначенийПользователи,
		ТолькоПросмотр = Ложь) Экспорт
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Пользователи", 	СписокЗначенийПользователи);
	ДополнительныеПараметры.Вставить("ТолькоПросмотр", 	ТолькоПросмотр);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СписокПользователей",
		ДополнительныеПараметры,,,,,
		ОповещениеОЗавершении,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура УправлениеОтображениемОрганизации(Форма, Организация) Экспорт
	
	Если Форма.Элементы.Найти("Организация") <> Неопределено Тогда
		Форма.Элементы.Организация.Видимость = НЕ ЗначениеЗаполнено(Организация);
	КонецЕсли;
	
	// Определяем надпись для организации 
	Если Форма.Элементы.Найти("НадписьОрганизация") <> Неопределено Тогда
		Если ЗначениеЗаполнено(Организация) Тогда
			Форма.Элементы.НадписьОрганизация.Заголовок = НСтр("ru = 'Организация: '") + КраткоеНаименованиеОрганизации(Организация);
			Форма.Элементы.НадписьОрганизация.Ширина = 0;
		Иначе
			Форма.Элементы.НадписьОрганизация.Заголовок = НСтр("ru = 'Организация:'");
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция КраткоеНаименованиеОрганизации(Организация)
	
	Если Метаданные.Справочники.Организации.Реквизиты.Найти("НаименованиеСокращенное") = Неопределено Тогда
		КраткоеНаименование = СокрЛП(Организация.Наименование);
	Иначе
		КраткоеНаименование = СокрЛП(Организация.НаименованиеСокращенное);
	КонецЕсли;
		
	Возврат КраткоеНаименование;
	
КонецФункции

&НаСервере
Функция ПолучитьСтатусОтправкиОбъекта(Док) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСтатусОтправкиОбъекта(Док);

КонецФункции

&НаКлиенте
Процедура СоздатьПисьмоОтвет(Ссылка) Экспорт
	
	// ищем существующий ответ
	Если ОтветНаПисьмоСуществует(Ссылка) Тогда
		ТекстВопроса = НСтр("ru = 'На данное письмо уже создан ответ. Вы уверены, что хотите создать новый?'");
		
		ОбработчикВопроса = Новый ОписаниеОповещения("СоздатьПисьмоОтветВопросЗавершение", ЭтотОбъект, Ссылка);
		ПоказатьВопрос(ОбработчикВопроса, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	Иначе
		СоздатьПисьмоОтветВопросЗавершение(Неопределено, Ссылка);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПисьмоОтветВопросЗавершение(Ответ, Ссылка) Экспорт
	
	Если Ответ <> Неопределено И Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	// если это ретроконверсия, то обработка особая
	ЭтоРетроконверсия = ПолучитьРеквизит(Ссылка, "Ретроконверсия");
	Если ЭтоРетроконверсия Тогда
		// показываем форму параметров ответа на ретроконверсию
		СтруктураПараметров = Новый Структура("Основание");
		СтруктураПараметров.Основание = Ссылка;
		ОписаниеОповещения = Новый ОписаниеОповещения("СоздатьПисьмоОтветВопросЗавершениеРетроконверсияЗавершение", ЭтотОбъект, Ссылка); 
		ОткрытьФорму(ПутьКОбъекту + ".Форма.Ретроконверсия", СтруктураПараметров,,,,, ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		Возврат;
		
	Иначе
		ОтветНаПисьмо = СоздатьОтветНаПисьмо(Ссылка);		
	КонецЕсли;
	
	Если ОтветНаПисьмо <> Неопределено Тогда
		СтруктураПараметров = Новый Структура("Ключ", ОтветНаПисьмо);
		ОткрытьФорму("Справочник.ПерепискаСКонтролирующимиОрганами.ФормаОбъекта", СтруктураПараметров);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПисьмоОтветВопросЗавершениеРетроконверсияЗавершение(НастройкиОтвета, Ссылка) Экспорт
	
	Если НастройкиОтвета = КодВозвратаДиалога.Отмена Тогда
		Возврат;
	КонецЕсли;
	
	ОтветНаПисьмо = СоздатьОтветНаПисьмо(Ссылка, НастройкиОтвета);
	
	Если ОтветНаПисьмо <> Неопределено Тогда
		СтруктураПараметров = Новый Структура("Ключ", ОтветНаПисьмо);
		ОткрытьФорму("Справочник.ПерепискаСКонтролирующимиОрганами.ФормаОбъекта", СтруктураПараметров);
	КонецЕсли;
		
КонецПроцедуры

// Печать электронного представления

&НаСервереБезКонтекста
Функция ПечатьИзЭлектронногоПредставленияНеПоддерживается(ЭлектронноеПредставление, ВидОтчета, ЭтоВебКлиент, ВыводЧерезВебСервис, АдресПараметровПроксиВоВременномХранилище, СвойстваОтчета)
	
	РеквизитыЭлектронногоПредставления = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ЭлектронноеПредставление, "ВидОтчета, ТипПолучателя, Получатель");
	ВидОтчета = РеквизитыЭлектронногоПредставления.ВидОтчета;
	ТипПолучателя = РеквизитыЭлектронногоПредставления.ТипПолучателя;
	Получатель = РеквизитыЭлектронногоПредставления.Получатель;
	ВыводЧерезВебСервис = Ложь;
	АдресПараметровПроксиВоВременномХранилище = "";
	НаименованиеОтчета = "";
	НаименованиеИФНС = Неопределено;
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ПечатьНеПоддерживается = КонтекстЭДО.ПечатьИзЭлектронногоПредставленияНеПоддерживается(ВидОтчета);
	
	Если НЕ ПечатьНеПоддерживается И ЗначениеЗаполнено(ВидОтчета) Тогда
		ВыводЧерезВебСервис = РегламентированнаяОтчетностьВызовСервера.ПроверитьВозможностьВыводаМашиночитаемойФормыЧерезВебСервис(АдресПараметровПроксиВоВременномХранилище, , ЭтоВебКлиент);
		
		Если ВыводЧерезВебСервис Тогда
			НаименованиеОтчета = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВидОтчета, "Источник");
			НаименованиеОтчета = ?(ЗначениеЗаполнено(НаименованиеОтчета), НаименованиеОтчета, "РегламентированныйОтчет" + Строка(ВидОтчета));
			Если Метаданные.Отчеты.Найти(НаименованиеОтчета) = Неопределено Тогда
				ВыводЧерезВебСервис = Ложь;
				НаименованиеОтчета = "";
				
			Иначе
				НаименованиеОтчета = "Отчет." + НаименованиеОтчета + ".Форма.ОсновнаяФорма";
				
				Если ТипПолучателя = Перечисления.ТипыКонтролирующихОрганов.ФНС И ЗначениеЗаполнено(Получатель) И ТипЗнч(Получатель) = Тип("СправочникСсылка.НалоговыеОрганы") Тогда
					НаименованиеИФНС = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Получатель, "Наименование");
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	СвойстваОтчета = Новый Структура("ТипПолучателя, НаименованиеОтчета, НаименованиеИФНС", ТипПолучателя, НаименованиеОтчета, НаименованиеИФНС);
	
	Возврат ПечатьНеПоддерживается;
	
КонецФункции

&НаКлиенте
Процедура ПечатьЭлектронногоПредставления(ЭлектронноеПредставление, АдресДанных = Неопределено, 
		Знач ПользовательскоеНаименованиеОтчета = Неопределено, ИмяФайла = "") Экспорт
		
	ЭтоВебКлиент = ЭтоВебКлиент();
	ВидОтчета = Неопределено;
	ВыводЧерезВебСервис = Ложь;
	АдресПараметровПроксиВоВременномХранилище = "";
	СвойстваОтчета = Неопределено;
	
	ПечатьНеПоддерживается = ПечатьИзЭлектронногоПредставленияНеПоддерживается(ЭлектронноеПредставление, ВидОтчета, ЭтоВебКлиент, ВыводЧерезВебСервис, АдресПараметровПроксиВоВременномХранилище, СвойстваОтчета);
	Если ПечатьНеПоддерживается Тогда
		СписокКнопок = Новый СписокЗначений;
		СписокКнопок.Добавить("ОК");
		СписокКнопок.Добавить("Подробнее", НСтр("ru = 'Подробнее...'"));
		
		ТекстВопроса = НСтр("ru = 'Для ""%1"" возможность печати не предусмотрена'");
		ТекстВопроса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстВопроса, ВидОтчета);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПечатьЭлектронногоПредставленияПослеВыбораОтветаНаВопрос", ЭтотОбъект);
		ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, СписокКнопок);
		Возврат;
	КонецЕсли;
	
	Если Не ВыводЧерезВебСервис И НЕ БраузерПоддерживаетРаботуСКомпонентойПечатиМашиночитаемыхБланков() Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(АдресДанных) ИЛИ (ВыводЧерезВебСервис И НЕ ЗначениеЗаполнено(ИмяФайла)) Тогда
		АдресДанных = АдресДанныхЭлектронногоПредставления(ЭлектронноеПредставление, ИмяФайла);
	КонецЕсли;
	
	Если АдресДанных = Неопределено Тогда
		ПоказатьПредупреждение(, "Электронное представление не определено!");
		Возврат;
	КонецЕсли;
	
	Если ВыводЧерезВебСервис Тогда
		Если ВРег(Прав(ИмяФайла, 4)) <> ".TXT" Тогда
			КодировкаТекстаВыгрузки = "windows-1251";
			Если (СвойстваОтчета.ТипПолучателя = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСГС") И (НЕ ВРег(Лев(ИмяФайла, 6)) = "NO_BUH") И (НЕ ВРег(Лев(ИмяФайла, 6)) = "NO_BOU"))
			ИЛИ СвойстваОтчета.ТипПолучателя = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН") ИЛИ СвойстваОтчета.ТипПолучателя = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС") Тогда
				КодировкаТекстаВыгрузки = "utf-8";
			КонецЕсли;
		Иначе
			КодировкаТекстаВыгрузки = "cp866";
		КонецЕсли;
		
		ФайлВыгрузкиВоВременномХранилище = Новый Структура;
		ФайлВыгрузкиВоВременномХранилище.Вставить("АдресФайлаВыгрузкиВоВременномХранилище", АдресДанных);
		ФайлВыгрузкиВоВременномХранилище.Вставить("ИмяФайлаВыгрузки", ИмяФайла);
		ФайлВыгрузкиВоВременномХранилище.Вставить("КодировкаФайлаВыгрузки", КодировкаТекстаВыгрузки);
		
		ПараметрыСформированногоОтчета = Новый Структура;
		Если РегламентированнаяОтчетностьВызовСервера.ВывестиМашиночитаемуюФормуЧерезВебСервис(СвойстваОтчета.НаименованиеОтчета, СвойстваОтчета.НаименованиеИФНС, ФайлВыгрузкиВоВременномХранилище, ПараметрыСформированногоОтчета, АдресПараметровПроксиВоВременномХранилище) Тогда
			РасширениеФайлаОтчета = "pdf";
			ИмяФайлаВыгрузки = Неопределено;
			Если ФайлВыгрузкиВоВременномХранилище.Свойство("ИмяФайлаВыгрузки", ИмяФайлаВыгрузки) И ЗначениеЗаполнено(ИмяФайлаВыгрузки) Тогда
				ПозицияТочки = СтрНайти(ИмяФайлаВыгрузки, ".");
				ИмяФайлаОтчета = ?(ПозицияТочки = 0, ИмяФайлаВыгрузки, Лев(ИмяФайлаВыгрузки, ПозицияТочки - 1)) + "." + РасширениеФайлаОтчета;
			Иначе
				ИмяФайлаОтчета = "" + Новый УникальныйИдентификатор() + "." + РасширениеФайлаОтчета;
			КонецЕсли;
			
			// Дополним список параметров для получения отчета
			ПараметрыСформированногоОтчета.Вставить("АдресПараметровПроксиВоВременномХранилище", АдресПараметровПроксиВоВременномХранилище);
			
			// Откроем специальную форму длительной операции. Из нее с возрастающим периодом
			// производится вызов серверной процедуры для проверки готовности отчета на веб-сервисе.
			ПараметрыФормы = Новый Структура;
			ПараметрыФормы.Вставить("ПараметрыВызоваЭкспортнойПроцедуры", ПараметрыСформированногоОтчета);
			ПараметрыФормы.Вставить("ИмяФайлаОтчета", ИмяФайлаОтчета);
			
			РегламентированнаяОтчетностьКлиент.ПолучитьОбщуюФормуПоИмени("ДлительнаяОперацияПриПолученииМЧБ", ПараметрыФормы, ЭтаФорма).Открыть();
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	// Сохранение в файл.
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ЭлектронноеПредставление",	ЭлектронноеПредставление);
	ДополнительныеПараметры.Вставить("ВидОтчета", 					ВидОтчета);
	ДополнительныеПараметры.Вставить("АдресДанных", 				АдресДанных);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПечатьЭлектронногоПредставленияПослеСохраненияВФайл", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ОперацииСФайламиЭДКОКлиент.ДанныеССервераВФайл(ОписаниеОповещения, АдресДанных, ".bin");
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьЭлектронногоПредставленияПослеВыбораОтветаНаВопрос(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = "Подробнее" Тогда
		ОткрытьФорму("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.Форма.СписокОтчетовПоддерживающихПечать")	
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ПечатьЭлектронногоПредставленияПослеУстановкиКомпоненты(ТаксДокПринт, ВремКаталог, ВремФайл)
	
	Если ТаксДокПринт <> Неопределено Тогда
		Попытка
			ТаксДокПринт.PrintTiffFolder = ВремКаталог;
			ТаксДокПринт.PreviewFile1C(ВремФайл, 0);
			Результат = Истина;
		Исключение
			ОбработатьОшибкуМодуляПечати();
			ТаксДокПринт = Неопределено;
			Результат = Ложь;
		КонецПопытки;
		
	Иначе
		
		#Если ВебКлиент Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения("ПечатьЭлектронногоПредставленияПослеУстановкиКомпонентыЗавершение", ЭтотОбъект);
			ПоказатьВопрос(ОписаниеОповещения, НСтр("ru='Формирование бланка формы прервано. Не удалось подключить вспомогательную
								 |компоненту для формирования бланков форм с двухмерным штрихкодом PDF417.
								 |Возможно, браузер не настроен в соответствии с особенностями настройки веб-браузера.
								 |Открыть справочную информацию с описанием особенностей настроек веб-браузера?'"), РежимДиалогаВопрос.ДаНет);
								
		#КонецЕсли
		
	КонецЕсли;
	
	ТаксДокПринт = Неопределено;
	
	// чистим временные каталоги.
	ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ВремФайл);

КонецПроцедуры

#Если ВебКлиент Тогда
	
&НаКлиенте	
Процедура ПечатьЭлектронногоПредставленияПослеУстановкиКомпонентыЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		
		ОткрытьСправку("v8help://1cv8/FeatureIE");
		
	КонецЕсли;
	
КонецПроцедуры

#КонецЕсли

&НаКлиенте
Процедура ПечатьЭлектронногоПредставленияВопросУстановитьКомпонентуЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	ТаксДокПринт = ДополнительныеПараметры.ТаксДокПринт;
	ВремКаталог = ДополнительныеПараметры.ВремКаталог;
	ВремФайл = ДополнительныеПараметры.ВремФайл;
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Результат = Ложь;
	Иначе
		// если не удалось создать объект и пользователь не против, то запускаем установку компоненты
		Если НЕ РегламентированнаяОтчетностьКлиент.УстановитьКомпонентуПечатиМашиночитаемыхФорм(Истина, ДистрибутивКомпонентыПечатиМашиночитаемыхФорм, ВремКаталог) Тогда
			ПоказатьПредупреждение(,НСтр("ru='Формирование машиночитаемой формы прервано: в системе не установлена вспомогательная компонента.'"));
			Результат = Ложь;
		Иначе
			ТаксДокПринт = РегламентированнаяОтчетностьКлиент.СоздатьОбъектДляФормированияМашиночитаемойФормы();
			КомпонентаУжеУстанавливалась = Истина;
		КонецЕсли;
	КонецЕсли;
	
	ПечатьЭлектронногоПредставленияПослеУстановкиКомпоненты(ТаксДокПринт, ВремКаталог, ВремФайл);
	
КонецПроцедуры

&НаСервере
Функция СформироватьСтруктуруПараметровФайлаВыгрузки(АдресДанных)
	
	ФайлВыгрузки = ПолучитьИмяВременногоФайла();
	ПолучитьИзВременногоХранилища(АдресДанных).Записать(ФайлВыгрузки);
	
	ПоказателиВыгрузки = Новый Соответствие;
	ОбъектЧтениеXML = Новый ЧтениеXML;
	Попытка
		ОбъектЧтениеXML.ОткрытьФайл(ФайлВыгрузки);
		ОбъектЧтениеXML.ИгнорироватьПробелы = Ложь;
		Пока ОбъектЧтениеXML.Прочитать() Цикл
			Если ОбъектЧтениеXML.ТипУзла = ТипУзлаXML.НачалоЭлемента И ОбъектЧтениеXML.Имя <> "СодПерСвед" Тогда
				Пока ОбъектЧтениеXML.ПрочитатьАтрибут() Цикл
					ПоказателиВыгрузки.Вставить(ОбъектЧтениеXML.Имя, ОбъектЧтениеXML.Значение);
				КонецЦикла;
			КонецЕсли;
		КонецЦикла;	
		ОбъектЧтениеXML.Закрыть();
		ПоказателиВыгрузки.Вставить("ЭтоXML", Истина);
	Исключение
		ОбъектЧтениеXML.Закрыть();
		Текст = Новый ЧтениеТекста;
		Текст.Открыть(ФайлВыгрузки, "cp866");
		ТекСтр = Текст.ПрочитатьСтроку();
		Пока ТекСтр <> Неопределено Цикл
			ТекСтр = СокрЛП(ТекСтр);
			ВхождениеДвоеточия = СтрНайти(ТекСтр, ":");
			Если ВхождениеДвоеточия <> 0 Тогда
				ПоказателиВыгрузки.Вставить(СокрЛП(Лев(ТекСтр, ВхождениеДвоеточия - 1)), СокрЛП(Сред(ТекСтр, ВхождениеДвоеточия + 1)));
			КонецЕсли;
			ТекСтр = Текст.ПрочитатьСтроку();
		КонецЦикла;
		Текст.Закрыть();
		Текст = Неопределено;
		ПоказателиВыгрузки.Вставить("ЭтоXML", Ложь);
	КонецПопытки;
	
	УдалитьФайлы(ФайлВыгрузки);
	
	Возврат ПоказателиВыгрузки;

КонецФункции

&НаСервереБезКонтекста
Функция АдресДанныхЭлектронногоПредставления(Ссылка, ИмяФайла = "")
	
	АдресДанных = Неопределено;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	СвойстваФайлов = КонтекстЭДОСервер.ИзвлечьЭлектронныеПредставленияВХранилище(Ссылка, Неопределено);
	Если СвойстваФайлов <> Неопределено Тогда 
		Если Ссылка.ВидОтчета = Справочники.ВидыОтправляемыхДокументов.НДС Тогда
			Для каждого СвойствоФайла Из СвойстваФайлов Цикл
				Если СтрНайти(СвойствоФайла.ИмяФайла, "NO_NDS_") Тогда	
					АдресДанных = СвойствоФайла.Данные;
					ИмяФайла = СвойствоФайла.ИмяФайла;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		Иначе
			Если СвойстваФайлов.Количество() > 0 Тогда
				АдресДанных = СвойстваФайлов[0].Данные;
				ИмяФайла = СвойстваФайлов[0].ИмяФайла;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ПоместитьВоВременноеХранилище(АдресДанных, Новый УникальныйИдентификатор);
	
КонецФункции

&НаСервере
Процедура СоздатьОтчетЗаданногоВида(Объект, СтруктураПараметров, ИспользоватьКомпоненту, ВозниклаОшибка = Ложь);
	
	ТаблицаШаблонов.Очистить();
	
	ИсточникОтчета = ?(ТипЗнч(Объект.ВидОтчета) = Тип("СправочникСсылка.ВидыОтправляемыхДокументов"), Объект.ВидОтчета.Источник, Объект.ВидОтчета.ИсточникОтчета);
	Если ИсточникОтчета <> Неопределено Тогда
		ОбъектОтчет = РеглОтчеты(ИсточникОтчета, Истина);
	КонецЕсли;
	
	КаталогВремФайлов = КаталогВременныхФайлов();
	КаталогВремФайлов = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВремФайлов);
	ВремКаталог = КаталогВремФайлов + Строка(Новый УникальныйИдентификатор);
	ВремКаталог = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ВремКаталог);
	СоздатьКаталог(ВремКаталог);
	
	ВерсияФорматаВыгрузки = ?(СтруктураПараметров["ЭтоXML"], СтруктураПараметров["ВерсФорм"], СтруктураПараметров["ВерФОтч"]);
	
	// выгружаем во временный каталог шаблоны печати, соответствующие сформированному файлу выгрузки
	ВыгрузкаУспешна = ВыгрузитьШаблоныПечатиВКаталог(
		ОбъектОтчет, 
		ВремКаталог, 
		СтруктураПараметров["КНД"], 
		ВерсияФорматаВыгрузки, 
		ДистрибутивКомпонентыПечатиМашиночитаемыхФорм);
	
	Если НЕ ВыгрузкаУспешна Тогда
		ВозниклаОшибка = Истина;
		Возврат;
	КонецЕсли;
	
	// поместить файлы шаблонов печати в хранилище
	ФайлыВыгрузки = НайтиФайлы(ВремКаталог, "*");
	Для Каждого ФайлВыгрузки Из ФайлыВыгрузки Цикл
		НоваяСтрока = ТаблицаШаблонов.Добавить();
		НоваяСтрока.Имя = ФайлВыгрузки.Имя;
		ПолноеИмяФайла = ФайлВыгрузки.ПолноеИмя;
		Если ИспользоватьКомпоненту Тогда 
			//@skip-check 1c-fresh-audit
			ФайлОбработкиBase64 = Base64Строка(Новый ДвоичныеДанные(ПолноеИмяФайла));
			СтрокаЗамены = Символ(13) + Символ(10);
			ФайлОбработкиBase64 = СтрЗаменить(ФайлОбработкиBase64, СтрокаЗамены, "");
			
			ГУИД = Новый УникальныйИдентификатор;
			НоваяСтрока.АдресСодержимого = ПоместитьВоВременноеХранилище(ФайлОбработкиBase64, ГУИД);
		Иначе 
			ГУИД = Новый УникальныйИдентификатор;
			//@skip-check 1c-fresh-audit
			НоваяСтрока.АдресСодержимого = ПоместитьВоВременноеХранилище(
				Новый ДвоичныеДанные(ПолноеИмяФайла), ГУИД);
		КонецЕсли;
	КонецЦикла;
	
	// обязательно удалить временный каталог
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремКаталог);
		
КонецПроцедуры

&НаСервере
Функция ВыгрузитьШаблоныПечатиВКаталог(
		Объект,
		ВремКаталог,
		Знач КНД,
		Знач ВерсияФорматаВыгрузки,
		ДистрибутивКомпонентыПечатиМашиночитаемыхФорм)
	
	ТочныеПараметрыШаблонаЗаданы = (ЗначениеЗаполнено(КНД) И ЗначениеЗаполнено(ВерсияФорматаВыгрузки));
	
	// если точные параметры заданы, то сначала пытаемся извлечь внутренний шаблон
	КоличествоВыгруженныхШаблонов = 0;
	Если ТочныеПараметрыШаблонаЗаданы Тогда
		ИспользуемыйШаблон = Неопределено;
		Пока СтрДлина(ВерсияФорматаВыгрузки) < 7 Цикл
			ВерсияФорматаВыгрузки = ВерсияФорматаВыгрузки + "0";
		КонецЦикла;
		ПрефиксИмениШаблона = СокрЛП(КНД + "_" + ВерсияФорматаВыгрузки);
		ПрефиксИмениМакетаШаблона = "МБ_" + СтрЗаменить(ПрефиксИмениШаблона, ".", "_");
	Иначе
		ПрефиксИмениМакетаШаблона = "МБ_";
	КонецЕсли;
	
	ШаблоныМашиночитаемыхФорм = ПолучитьШаблоныМашиночитаемыхФорм(Объект, ПрефиксИмениМакетаШаблона, КоличествоВыгруженныхШаблонов, ТочныеПараметрыШаблонаЗаданы, ПрефиксИмениШаблона, ДистрибутивКомпонентыПечатиМашиночитаемыхФорм);//ДистрибутивКомпонентыПечатиМашиночитаемыхФорм);
	
	Если ШаблоныМашиночитаемыхФорм.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти необходимый шаблон машиночитаемой формы'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	Иначе
		Для Каждого Элемент Из ШаблоныМашиночитаемыхФорм Цикл
			
			Попытка
				
				Элемент.Значение.Записать(ВремКаталог + Элемент.Представление);
				
			Исключение
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось выгрузить шаблон машиночитаемой формы ""%1"" во временный каталог!%2'"), Элемент.Представление, Символы.ПС + ОписаниеОшибки());
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Ложь;
				
			КонецПопытки;
			
		КонецЦикла;
	КонецЕсли;
		
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОбработатьОшибкуМодуляПечати()
	
	ОписаниеОшибки = СокрЛП(ОписаниеОшибки());
	КодОшибки = Сред(ОписаниеОшибки, СтрНайти(ОписаниеОшибки, "0x"), 10);
	
	ТекстПредупреждения = "";
	Если КодОшибки = "0x80040004" ИЛИ КодОшибки = "0x80040005" Тогда
		ТекстПредупреждения = "Не удалось установить параметр печати!";
	ИначеЕсли КодОшибки = "0x80040001" Тогда
		ТекстПредупреждения = "При попытке печати в компоненте печати возникла неизвестная ошибка!";
	ИначеЕсли КодОшибки = "0x80040032" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось найти файл выгрузки отчета!";
	ИначеЕсли КодОшибки = "0x80040033" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось получить доступ к файлу выгрузки отчета!";
	ИначеЕсли КодОшибки = "0x8004000A" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось распознать формат файла выгрузки!";
	ИначеЕсли КодОшибки = "0x8004000B" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось распознать формат файла выгрузки!";
	ИначеЕсли КодОшибки = "0x8004000F" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось распознать версию формата выгрузки из текста выгрузки!";
	ИначеЕсли КодОшибки = "0x80040010" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось распознать форму по КНД в файле выгрузки!";
	ИначеЕсли КодОшибки = "0x80040014" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось сформировать машиночитаемую форму по причине некорректного файла выгрузки!";
	ИначеЕсли КодОшибки = "0x8004003C" Тогда
		ТекстПредупреждения = "Отсутствует шаблон для формирования машиночитаемой формы!";
	ИначеЕсли КодОшибки = "0x80040046" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось найти каталог с шаблонами для печати!";
	ИначеЕсли КодОшибки = "0x80050035" Тогда
		ТекстПредупреждения = "Не найдена одна из вспомогательных библиотек, используемых компонентой печати (GNIVCPDOC.dll)!";
	ИначеЕсли КодОшибки = "0x800501C5" Тогда
		ТекстПредупреждения = "Одна из вспомогательных библиотек, используемых компонентой печати (GNIVCPDOC.dll), подменена или устарела!";
	ИначеЕсли КодОшибки = "0x80050030" Тогда
		ТекстПредупреждения = "Одна из вспомогательных библиотек, используемых компонентой печати (GNIVCPDOC.dll), повреждена!";
	ИначеЕсли КодОшибки = "0x80040064" Тогда // печать прервана
		Возврат;
	ИначеЕсли КодОшибки = "0x80040001" Тогда
		ТекстПредупреждения = "Компоненте печати не удалось найти каталог с шаблонами для печати!";
	Иначе
		ТекстПредупреждения = ОписаниеОшибки;
	КонецЕсли;

	Если ТекстПредупреждения = "" Тогда
		ПоказатьПредупреждение(,НСтр("ru = 'При формировании машиночитаемой формы возникла неизвестная ошибка'"));
	Иначе
		ПоказатьПредупреждение(,ТекстПредупреждения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция РеглОтчеты(Наим, НеЗапоминатьВКэше = Ложь) Экспорт
		
	Перем ВременныйФайл;
	Перем ПараметрыОтчета;
	
	НайденныйЭлемент = Справочники.РегламентированныеОтчеты.НайтиПоРеквизиту("ИсточникОтчета", Наим);
	Если НайденныйЭлемент = Справочники.РегламентированныеОтчеты.ПустаяСсылка() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если Метаданные.Документы.Найти(Наим) <> Неопределено Тогда
		
		Возврат Документы[Наим];
		
	ИначеЕсли Метаданные.Отчеты.Найти(Наим) <> Неопределено Тогда
		
		Возврат Отчеты[Наим];
		
	Иначе
		
		Возврат Неопределено;
		
	КонецЕсли; 
	
	Возврат Неопределено;
			
КонецФункции

&НаСервере
Функция ПолучитьШаблоныМашиночитаемыхФорм(Знач ИмяОтчета, ПрефиксИмениМакетаШаблона, КоличествоВыгруженныхШаблонов, ТочныеПараметрыШаблонаЗаданы, ПрефиксИмениШаблона, ДистрибутивКомпонентыПечатиМашиночитаемыхФорм) Экспорт
	
	ШаблоныМашиночитаемыхФорм = Новый СписокЗначений;
	
	Если СтрНайти(ИмяОтчета, "ОтчетМенеджер") > 0 Тогда
		ИмяОтчета = Сред(ИмяОтчета, СтрДлина("ОтчетМенеджер.") + 1);
	Иначе
		ИмяОтчета = Сред(Лев(ИмяОтчета, СтрНайти(ИмяОтчета, ".Форма.") - 1), 7);
	КонецЕсли;
	
	ОбъектМетаданных = Метаданные.Отчеты.Найти(ИмяОтчета);
	
	Если ОбъектМетаданных <> Неопределено Тогда
		Попытка
			МакетыОтчета = ОбъектМетаданных.Макеты;
		
			Для Каждого МакетОтчета Из МакетыОтчета Цикл
				Если МакетОтчета.ТипМакета = Метаданные.СвойстваОбъектов.ТипМакета.ДвоичныеДанные
				И Лев(нрег(МакетОтчета.Имя), СтрДлина(ПрефиксИмениМакетаШаблона)) = нрег(ПрефиксИмениМакетаШаблона) Тогда
					СинонимМакета = СокрЛП(МакетОтчета.Синоним);
					ВхождениеПробела = СтрНайти(СинонимМакета, " ");
					ИмяФайлаШаблона = ?(ВхождениеПробела = 0, СинонимМакета, Лев(СинонимМакета, СтрДлина(ВхождениеПробела - 1)));
					Попытка
						ШаблоныМашиночитаемыхФорм.Добавить(Отчеты[ОбъектМетаданных.Имя].Создать().ПолучитьМакет(МакетОтчета.Имя), ИмяФайлаШаблона);
						КоличествоВыгруженныхШаблонов = КоличествоВыгруженныхШаблонов + 1;
					Исключение
						ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось получить шаблон машиночитаемой формы ""%1""!%2'"), ИмяФайлаШаблона, Символы.ПС + ОписаниеОшибки());
						ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					КонецПопытки;
				КонецЕсли;
			КонецЦикла;
			
		Исключение
		КонецПопытки;
	КонецЕсли;
	// выгружаем внешние шаблоны
	Если ТочныеПараметрыШаблонаЗаданы Тогда
		РезультатЗапроса = ВыполнитьЗапросКРегиструШаблоновПечатиМашиночитаемыхФорм(ПрефиксИмениШаблона + "%");
		Если КоличествоВыгруженныхШаблонов = 0 И РезультатЗапроса.Пустой() Тогда
			РезультатЗапроса = ВыполнитьЗапросКРегиструШаблоновПечатиМашиночитаемыхФорм();
		КонецЕсли;
	Иначе
		РезультатЗапроса = ВыполнитьЗапросКРегиструШаблоновПечатиМашиночитаемыхФорм();
	КонецЕсли;
	
	Если РезультатЗапроса.Пустой() Тогда
		
		Если КоличествоВыгруженныхШаблонов = 0 Тогда
			ТекстСообщения  = НСтр("ru = 'В регистре сведений ""Шаблоны печати машиночитаемых форм"" отсутствует необходимый шаблон.
	                                |Скачайте необходимые шаблоны с сайта http://www.gnivc.ru/ или любого другого сайта и загрузите их в регистр сведений ""Шаблоны печати машиночитаемых форм""'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		
	Иначе
		Выборка = РезультатЗапроса.Выбрать();
		Пока Выборка.Следующий() Цикл
			Попытка
				ШаблоныМашиночитаемыхФорм.Добавить(Выборка.Шаблон.Получить(), Выборка.ИмяФайлаШаблона);
			Исключение
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Не удалось получить шаблон машиночитаемой формы ""%1""!%2'"), Выборка.ИмяФайлаШаблона, Символы.ПС + ОписаниеОшибки());
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			КонецПопытки;
		КонецЦикла;
	КонецЕсли;
		
	РегламентированнаяОтчетностьВызовСервера.ПолучитьДистрибутивКомпонентыПечатиМашиночитаемыхФорм(ДистрибутивКомпонентыПечатиМашиночитаемыхФорм);
	
	Возврат ШаблоныМашиночитаемыхФорм;
	
КонецФункции // ПолучитьШаблоныМашиночитаемыхФорм()

&НаСервере
Функция ВыполнитьЗапросКРегиструШаблоновПечатиМашиночитаемыхФорм(ИмяФайлаШаблона = Неопределено)
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	ШаблоныПечатиМашиночитаемыхФорм.ИмяФайлаШаблона,
	                      |	ШаблоныПечатиМашиночитаемыхФорм.Шаблон
	                      |ИЗ
	                      |	РегистрСведений.ШаблоныПечатиМашиночитаемыхФорм КАК ШаблоныПечатиМашиночитаемыхФорм");
	Если ИмяФайлаШаблона <> Неопределено Тогда
		Запрос.Текст = Запрос.Текст + "
						  |ГДЕ
						  |	ШаблоныПечатиМашиночитаемыхФорм.ИмяФайлаШаблона ПОДОБНО &ИмяФайлаШаблона
						  |УПОРЯДОЧИТЬ ПО
						  |	ШаблоныПечатиМашиночитаемыхФорм.ИмяФайлаШаблона УБЫВ";
		Запрос.УстановитьПараметр("ИмяФайлаШаблона", ИмяФайлаШаблона);
	КонецЕсли;
	Возврат Запрос.Выполнить();
	
КонецФункции

&НаКлиенте
Процедура ВыгрузкаЭлектронногоПредставления(ЭлектронноеПредставление) Экспорт
	
	Если ЗначениеЗаполнено(ЭлектронноеПредставление) Тогда
		
		КонтекстПредставления = Новый Структура("Документ", ЭлектронноеПредставление);
		КонтекстПредставления.Вставить("УникальныйИдентификатор", ЭтотОбъект.УникальныйИдентификатор);
		Результат = ВыгрузитьРегламентированныйОтчетПФРИзВнешнихЭлектронныхПредставлений(КонтекстПредставления);
		ВсегоВАрхиве = 0;
		Для Каждого Запись Из Результат Цикл
			Если Запись.ВАрхиве Тогда
				ВсегоВАрхиве = ВсегоВАрхиве + 1;
			КонецЕсли;
		КонецЦикла;
		
		Если ВсегоВАрхиве > 0 Тогда //Данные одного или более файлов отсутствуют
			ПоказатьУведомлениеАрхивныхФайлов(, 24, 0, Истина);			
			Возврат;
		КонецЕсли;
		
		Если НЕ ПолучитьДанныеЭлектронногоПредставленияНаСервере(ЭлектронноеПредставление) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Данные для выгрузки отсутствуют'"));
			Возврат;
		КонецЕсли;
		
		Если ВыборкаСодержимого.Количество() = 0 Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Данные для выгрузки отсутствуют'"));
			Возврат;
		КонецЕсли;
		
		МассивИменФайлов = Новый Массив;
		МассивОписанийПолучаемыеФайлы = Новый Массив;
		Для Каждого Контейнер Из ВыборкаСодержимого Цикл 
			МассивИменФайлов.Добавить(Контейнер.КороткоеИмяФайла);
			ОписаниеФайла = Новый ОписаниеПередаваемогоФайла(Контейнер.КороткоеИмяФайла, Контейнер.АдресСодержимого); 
			МассивОписанийПолучаемыеФайлы.Добавить(ОписаниеФайла);
		КонецЦикла;
		
		ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(МассивОписанийПолучаемыеФайлы);
		
	Иначе
		
		ПоказатьПредупреждение(, НСтр("ru = 'Выберите файлы, которые следует сохранить на диск, и повторите попытку'"));
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьДанныеЭлектронногоПредставленияНаСервере(ЭлектронноеПредставление)
	
	ОчиститьТаблицуИХранилище();
	
	НаборЗаписей = РегистрыСведений.ХранилищеЭлектронныхПредставленийРегламентированныхОтчетов.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ЭлектронноеПредставление.Установить(ЭлектронноеПредставление);
	НаборЗаписей.Прочитать();

	Для каждого Запись Из НаборЗаписей Цикл
	
		НоваяСтрока 					= ВыборкаСодержимого.Добавить();
		ГУИД 							= Новый УникальныйИдентификатор;
		НоваяСтрока.КороткоеИмяФайла 	= Запись.ИмяФайла;
	    НоваяСтрока.АдресСодержимого 	= ПоместитьВоВременноеХранилище(Запись.Данные.Получить(), ГУИД);
	
	КонецЦикла; 
	
	Возврат Истина;
	
КонецФункции

&НаСервере
Процедура ОчиститьТаблицуИХранилище()
	
	Если ВыборкаСодержимого.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Выборка Из ВыборкаСодержимого Цикл 
		УдалитьИзВременногоХранилища(Выборка.АдресСодержимого);
	КонецЦикла;
	
	ВыборкаСодержимого.Очистить();
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьПротоколОтправкиФСС(ПоследняяОтправка) Экспорт
	
	ИдентификаторОтправкиНаСервере = "";
	Адрес = ПолучитьАдресФайлаПротоколаФСС(ПоследняяОтправка, ИдентификаторОтправкиНаСервере);
	ПолучитьФайл(Адрес, НСтр("ru = 'Протокол'") + "_" + ИдентификаторОтправкиНаСервере + ".html");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьАдресФайлаПротоколаФСС(ОтправкаСсылка, ИдентификаторОтправкиНаСервере)
	
	
	ИдентификаторОтправкиНаСервере = ОтправкаСсылка.ИдентификаторОтправкиНаСервере;
	
	СтрПротокол = ОтправкаСсылка.Протокол.Получить();
	ВременныйФайл = ПолучитьИмяВременногоФайла();
	Текст = Новый ЗаписьТекста(ВременныйФайл, КодировкаТекста.UTF8);
	Текст.Записать(СтрПротокол);
	Текст.Закрыть();
	
	Возврат ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ВременныйФайл));
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПоследнююОтправкуОтчетаВФСС(Ссылка) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьПоследнююОтправкуОтчетаВФСС(Ссылка);

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПоследнююОтправкуОтчетаВФСРАР(Ссылка) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьПоследнююОтправкуОтчетаВФСРАР(Ссылка);

КонецФункции

&НаКлиенте
Процедура ПолучитьПротоколОтправкиФСРАР(ПоследняяОтправка) Экспорт
	
	ИмяФайлаПакетаДоТочки = "";
	Адрес = ПолучитьАдресФайлаПротоколаФСРАР(ПоследняяОтправка, ИмяФайлаПакетаДоТочки);
	
	ПозицияТочки = СтрНайти(ИмяФайлаПакетаДоТочки, ".");
	Если ПозицияТочки > 0 Тогда
		ИмяФайлаПакетаДоТочки = Лев(ИмяФайлаПакетаДоТочки, ПозицияТочки - 1);
	КонецЕсли;
	
	ПолучитьФайл(Адрес, НСтр("ru = 'Протокол'") + ИмяФайлаПакетаДоТочки + ".html");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьАдресФайлаПротоколаФСРАР(ОтправкаСсылка, ИмяФайлаПакетаДоТочки)
	
	ИмяФайлаПакетаДоТочки = ОтправкаСсылка.ИмяФайлаПакета;
	
	СтрПротокол = ОтправкаСсылка.Протокол.Получить();
	ВременныйФайл = ПолучитьИмяВременногоФайла();
	Текст = Новый ЗаписьТекста(ВременныйФайл, КодировкаТекста.UTF8);
	Текст.Записать(СтрПротокол);
	Текст.Закрыть();
	
	Возврат ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ВременныйФайл));
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПоследнююОтправкуОтчетаВРПН(Ссылка) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьПоследнююОтправкуОтчетаВРПН(Ссылка);

КонецФункции

&НаКлиенте
Процедура ПолучитьПротоколОтправкиРПН(ПоследняяОтправка) Экспорт
	
	ИмяФайлаПакетаДоТочки = "";
	Адрес = ПолучитьАдресФайлаПротоколаРПН(ПоследняяОтправка, ИмяФайлаПакетаДоТочки);
	
	ПозицияТочки = СтрНайти(ИмяФайлаПакетаДоТочки, ".");
	Если ПозицияТочки > 0 Тогда
		ИмяФайлаПакетаДоТочки = Лев(ИмяФайлаПакетаДоТочки, ПозицияТочки - 1);
	КонецЕсли;
	
	ПолучитьФайл(Адрес, НСтр("ru = 'Протокол'") + ИмяФайлаПакетаДоТочки + ".html");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьАдресФайлаПротоколаРПН(ОтправкаСсылка, ИмяФайлаПакетаДоТочки)
	
	ИмяФайлаПакетаДоТочки = ОтправкаСсылка.ИмяФайлаПакета;
	
	СтрПротокол = ОтправкаСсылка.Протокол.Получить();
	ВременныйФайл = ПолучитьИмяВременногоФайла();
	Текст = Новый ЗаписьТекста(ВременныйФайл, КодировкаТекста.UTF8);
	Текст.Записать(СтрПротокол);
	Текст.Закрыть();
	
	Возврат ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ВременныйФайл));
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПоследнююОтправкуОтчетаВФТС(Ссылка, ИдентификаторОтправкиНаСервере = "") Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьПоследнююОтправкуОтчетаВФТС(Ссылка, ИдентификаторОтправкиНаСервере);

КонецФункции

&НаКлиенте
Процедура ПолучитьПротоколОтправкиФТС(ПоследняяОтправка) Экспорт
	
	ИмяФайлаВыгрузкиДоТочки = "";
	Адрес = ПолучитьАдресФайлаПротоколаФТС(ПоследняяОтправка, ИмяФайлаВыгрузкиДоТочки);
	
	ПозицияТочки = СтрНайти(ИмяФайлаВыгрузкиДоТочки, ".");
	Если ПозицияТочки > 0 Тогда
		ИмяФайлаВыгрузкиДоТочки = Лев(ИмяФайлаВыгрузкиДоТочки, ПозицияТочки - 1);
	КонецЕсли;
	
	ПолучитьФайл(Адрес, НСтр("ru = 'Протокол'") + ИмяФайлаВыгрузкиДоТочки + ".html");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьАдресФайлаПротоколаФТС(ОтправкаСсылка, ИмяФайлаВыгрузкиДоТочки)
	
	ИмяФайлаВыгрузкиДоТочки = ОтправкаСсылка.ИмяФайлаВыгрузки;
	
	СтрПротокол = ОтправкаСсылка.Протокол.Получить();
	ВременныйФайл = ПолучитьИмяВременногоФайла();
	Текст = Новый ЗаписьТекста(ВременныйФайл, КодировкаТекста.UTF8);
	Текст.Записать(СтрПротокол);
	Текст.Закрыть();
	
	Возврат ПоместитьВоВременноеХранилище(Новый ДвоичныеДанные(ВременныйФайл));
	
КонецФункции

&НаКлиенте
Процедура ПоказатьФормуСтатусовОтправкиИзСписка(Элемент) Экспорт
		
	ТекущиеДанные = Элемент.ТекущиеДанные;
	Если ТекущиеДанные <> Неопределено Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Ссылка");
		ДополнительныеПараметры.Вставить("Наименование");
		ДополнительныеПараметры.Вставить("Организация");
		ДополнительныеПараметры.Вставить("ВидКонтролирующегоОргана");
		ДополнительныеПараметры.Вставить("КодКонтролирующегоОргана");
		ДополнительныеПараметры.Вставить("ПредставлениеКонтролирующегоОргана");
		ДополнительныеПараметры.Вставить("ПредставлениеПериода");
		
		// Определяем изменяемый документ
		Если ТекущиеДанные.Свойство("РегламентированныйОтчет") Тогда
			Ссылка = ТекущиеДанные.РегламентированныйОтчет;
		Иначе
			Ссылка = ТекущиеДанные.Ссылка;
		КонецЕсли;
		ДополнительныеПараметры.Вставить("Ссылка", Ссылка);
		
		Если ДокументооборотСКОВызовСервера.ЭтоРежимОблачной1СО() Тогда
			МодульКалендарьОтчетностиКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("КалендарьОтчетностиКлиент");
			РезультатПоказа = МодульКалендарьОтчетностиКлиент.ПоказатьФормуСтатусовОтправкиИзСпискаДляВыплатИПособийСЭДО(Ссылка);
			Если РезультатПоказа Тогда
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		Если ТекущиеДанные.Свойство("НаименованиеОтчета") Тогда
			Наименование = ТекущиеДанные.НаименованиеОтчета;
		Иначе
			Наименование = ТекущиеДанные.Наименование;
		КонецЕсли;
		ДополнительныеПараметры.Вставить("Наименование", Наименование);
		
		ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, ТекущиеДанные);
		
		Если ТекущиеДанные.Свойство("Отправка") Тогда
			ДополнительныеПараметры.Вставить("Отправка", ТекущиеДанные.Отправка);
		КонецЕсли;
		
		Если ТекущиеДанные.Свойство("СтраницаЖурнала") Тогда
			ДополнительныеПараметры.Вставить("СтраницаЖурнала", ТекущиеДанные.СтраницаЖурнала);
		КонецЕсли;
		
		// Проверяем, сдается ли отчет в контролирующие органы 
		ОтправляетсяВКонтролирующийОрган = Истина;
		Если ТекущиеДанные.Свойство("НеОтправляетсяВКонтролирующийОрган") Тогда
			ОтправляетсяВКонтролирующийОрган = НЕ ТекущиеДанные.НеОтправляетсяВКонтролирующийОрган;
		Иначе
			ОтправляетсяВКонтролирующийОрган = Истина;
		КонецЕсли;
		
		Если НЕ ОтправляетсяВКонтролирующийОрган И
			НЕ ДополнительныеПараметры.ВидКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.Минобороны") Тогда
			ТекстПредупреждения = НСтр("ru = '""%1"" не сдается в электронном виде'");
			ТекстПредупреждения = СтрЗаменить(ТекстПредупреждения, "%1", Наименование);
			ПоказатьПредупреждение(,ТекстПредупреждения);
			Возврат;
		КонецЕсли;
		
		// Проверяем, был ли отправлен отчет
		СостояниеСдачиОтчетностиВСписке = ТекущиеДанные.СостояниеСдачиОтчетности;
		Если СостояниеСдачиОтчетностиВСписке = ПредопределенноеЗначение("Перечисление.СостояниеСдачиОтчетности.ДокументооборотНеНачат") Тогда
			
			ШаблонТекста = "отчета";
			
			Если ТипЗнч(Ссылка) = Тип("ДокументСсылка.ОтветСтрахователяНаТребованиеПроверкиФСС") Тогда
				ШаблонТекста = "документа";
			КонецЕсли;
			
			ТекстПредупреждения = НСтр("ru = 'Просмотр подробной информации о состоянии %1
				|возможен только после его отправки'");
			ТекстПредупреждения = СтрШаблон(ТекстПредупреждения, ШаблонТекста);
			ПоказатьПредупреждение(,ТекстПредупреждения);
			Возврат;
		КонецЕсли;
		
		// Показываем форму предупреждения
		ПоказатьФормуСтатусовОтправки(Ссылка, ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьФормуСтатусовОтправки(Ссылка, ДополнительныеПараметры = Неопределено) Экспорт
		
	Если ДополнительныеПараметры = Неопределено Тогда
		ДополнительныеПараметры = Новый Структура;
	КонецЕсли;
		
	ДополнительныеПараметры.Вставить("Ссылка", Ссылка);
	
	Если ТипЗнч(Ссылка) = Тип("ДокументСсылка.ВходящийДокументСЭДОФСС") Тогда
		ДополнительныеПараметры.Вставить("Ключ", Ссылка);
		ОткрытьФорму("Документ.ВходящийДокументСЭДОФСС.Форма.ФормаДокумента", 	ДополнительныеПараметры);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаСостоянияОтправки", 	ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьРезультатПриемаВходящейОписи(ВходящаяОписьСсылка)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// получаем список сообщений
	ПоследнийЦиклОбмена 		= ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(ВходящаяОписьСсылка);
	СообщениеОснование			= КонтекстЭДОСервер.ПолучитьСообщениеЦиклаОбмена(ПоследнийЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.ДокументНО);
	СообщениеРезультатПриема 	= КонтекстЭДОСервер.ПолучитьСообщениеЦиклаОбмена(ПоследнийЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументНП, СообщениеОснование);
	Если СообщениеРезультатПриема = Документы.ТранспортноеСообщение.ПустаяСсылка() Тогда
		Возврат Неопределено;
	Иначе
		Возврат СообщениеРезультатПриема;
	КонецЕсли;

КонецФункции  

&НаСервереБезКонтекста
Функция ЭтоТребованиеОПредставленииДокументов(ДокументОписи) Экспорт
	
	Возврат ДокументОписи.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДанныеТребованияСФР(Требование)
	
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(
		Требование, "Организация, ИдентификаторСообщения");
	
КонецФункции
	
&НаКлиенте
Процедура СоздатьРезультатПриемаВходящейОписиИнтерактивноПоДокументуОписи(
		ДокументОписи, 
		ПоложительныйРезультат, 
		ДействиеПослеПодтвержденияПриема, 
		ТихийРежим = Ложь) Экспорт
		
	Если ТипЗнч(ДокументОписи) = Тип("ДокументСсылка.ВходящийДокументСЭДОФСС") Тогда
		Данные = ДанныеТребованияСФР(ДокументОписи);
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Требование", 			ДокументОписи);
		ДополнительныеПараметры.Вставить("ДействиеПослеПодтвержденияПриема", ДействиеПослеПодтвержденияПриема);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СоздатьРезультатПриемаВходящейОписиИнтерактивно_ПослеПодтвержденияСЭДО", ЭтотОбъект, ДополнительныеПараметры);
		ЭлектронныйДокументооборотСФССКлиент.ОтправитьПодтверждениеОПолученииСообщенийСЭДО(
			ОписаниеОповещения, Данные.Организация, Данные.ИдентификаторСообщения, Ложь);
		Возврат;
	КонецЕсли;
			
	//Определяем ссылку на опись входящих сообщений
	ОписьВходящихДокументов = ДокументооборотСКОВызовСервера.ПолучитьОписьВходящихДокументовПоТребованию(ДокументОписи);
	
	//Проверяем наличие уже созданного результата приема
	РезультатПриема  = ПолучитьРезультатПриемаВходящейОписи(ОписьВходящихДокументов);
	НужноПересоздать = НужноСформироватьСвежуюКвитанциюОПриеме(РезультатПриема);
	
	Если РезультатПриема = Неопределено ИЛИ НужноПересоздать Тогда
		//Результат приема еще не создавали
		
		//Подготовим описание оповещения для последующего диалога
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ОписьВходящихДокументов", 			ОписьВходящихДокументов);
		ДополнительныеПараметры.Вставить("ПоложительныйРезультат", 				ПоложительныйРезультат);
		ДополнительныеПараметры.Вставить("ДействиеПослеПодтвержденияПриема", 	ДействиеПослеПодтвержденияПриема);
		ДополнительныеПараметры.Вставить("НужноПересоздать", 					НужноПересоздать);
		ДополнительныеПараметры.Вставить("ТихийРежим", 							ТихийРежим);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("СоздатьРезультатПриемаВходящейОписиИнтерактивно_ПослеПодтверждения", ЭтотОбъект, ДополнительныеПараметры);
		
		Если ТихийРежим Тогда
			
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
			
		Иначе
		
			//Определяем количество требований в описи входящих сообщений
			КоличествоДокументов = ПолучитьКоличествоДокументовПоОписиВходящихДокументов(ОписьВходящихДокументов);
			МассивТребований = ТребованияФНСВызовСервера.ТребованияВОписи(ОписьВходящихДокументов);
			Если КоличествоДокументов < 2 Тогда
				//Задаем вопрос, ответ передаем в СоздатьРезультатПриемаВходящейОписиИнтерактивно_ПослеПодтверждения
				Если ПоложительныйРезультат Тогда
					ТекстВопроса = "Подтвердить прием """ + ДокументОписи + """ (подтверждение будет отправлено в ФНС)?";
		            ТекстКнопки = "Подтвердить прием";
				Иначе
					ТекстВопроса = "Отказать в приеме """ + ДокументОписи + """ (отказ будет отправлен в ФНС)?";	
					ТекстКнопки = "Отказать в приеме";
				КонецЕсли;
				
				СписокКнопок = Новый СписокЗначений;
				СписокКнопок.Добавить(КодВозвратаДиалога.Да, ТекстКнопки);
				СписокКнопок.Добавить(КодВозвратаДиалога.Отмена, "Отмена");
				
				ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, СписокКнопок, , КодВозвратаДиалога.Да);
			Иначе
				ПараметрыФормы = Новый Структура;
				ПараметрыФормы.Вставить("ОписьВходящихДокументов", ОписьВходящихДокументов);
				ПараметрыФормы.Вставить("ПоложительныйРезультатПриема", ПоложительныйРезультат);
				
				ОткрытьФорму("Справочник.ДокументыРеализацииПолномочийНалоговыхОрганов.Форма.ФормаОтправкиРезультатаПриема", ПараметрыФормы, , , , , ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
			КонецЕсли;
			
		КонецЕсли;
		
	Иначе
		
		//Результат приема уже создавали
		//Откроем его
		Статус = ПолучитьРеквизит(РезультатПриема, "Статус");
		Если Статус = ПредопределенноеЗначение("Перечисление.СтатусыПисем.Отправленное") Тогда
			
			Если ТихийРежим Тогда
				//Результат приема уже создавали
				ВыполнитьОбработкуОповещения(ДействиеПослеПодтвержденияПриема, Истина);
			Иначе
				ПоказатьЗначение(, РезультатПриема);
			КонецЕсли;
		Иначе
			
			ДополнительныеПараметры = Новый Структура("ДействиеПослеПодтвержденияПриема", ДействиеПослеПодтвержденияПриема);
			
			СоздатьРезультатПриемаВходящейОписиИнтерактивно_СформироватьПакетФНС(
				РезультатПриема, 
				ДополнительныеПараметры);
					
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

&НаСервере
Функция НужноСформироватьСвежуюКвитанциюОПриеме(ТранспортноеСообщение)
	
	Если НЕ ЗначениеЗаполнено(ТранспортноеСообщение)
		ИЛИ НЕ ЗначениеЗаполнено(ТранспортноеСообщение.ЦиклОбмена) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если ТипЗнч(ТранспортноеСообщение.ЦиклОбмена.Предмет) <> Тип("СправочникСсылка.ОписиВходящихДокументовИзНалоговыхОрганов") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ИмяКвитанции = ИмяВложенияТранспортногоСообщения(
		ТранспортноеСообщение, 
		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		
	Если ИмяКвитанции = "" Тогда
		Возврат Истина;
	КонецЕсли;

	ЧастиИмени    = СтрРазделить(ИмяКвитанции, "_");
	ДатаСтрокой   = ЧастиИмени[5];
	ДатаКвитанции = ОбщегоНазначенияКлиентСервер.СтрокаВДату(ДатаСтрокой);
	
	НужноСформироватьСвежую = НачалоДня(ДатаКвитанции) <> НачалоДня(ТекущаяДатаСеанса());
	
	Возврат НужноСформироватьСвежую;

КонецФункции

&НаСервереБезКонтекста
Функция УдалитьВложенияРезультатаПриема(ТранспортноеСообщение)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	ИмяКвитанции = ИмяВложенияТранспортногоСообщения(
		ТранспортноеСообщение, 
		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.КвитанцияОПриемеДокумент);
		
	Если ИмяКвитанции <> "" Тогда
		КонтекстЭДОСервер.УдалитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, ИмяКвитанции);;
	КонецЕсли;

	// Удаляем подпись
	ИмяПодписи = ИмяВложенияТранспортногоСообщения(
		ТранспортноеСообщение, 
		Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ЭлектронноЦифроваяПодпись);
		
	Если ИмяПодписи <> "" Тогда	
		КонтекстЭДОСервер.УдалитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, ИмяПодписи);
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура СоздатьРезультатПриемаВходящейОписиИнтерактивно_ПослеПодтверждения(РезультатВопроса, ВходящийКонтекст) Экспорт
	 
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		//Было выбрано создать результат приема
		
		ПоложительныйРезультат = ВходящийКонтекст.ПоложительныйРезультат;
		ОписьВходящихДокументов = ВходящийКонтекст.ОписьВходящихДокументов;
		
	    ДокументНО = ПолучитьСообщениеДокументНОПоОписиВходящихДокументов(ОписьВходящихДокументов);
		Если ДокументНО <> Неопределено Тогда
			
			ВходящийКонтекст.Вставить("ЭтоОтправкаПодтвержденияИзПанелиОтправки", Истина);
			//создаем и подписываем сообщение
			Оповещение = Новый ОписаниеОповещения(
				"СоздатьРезультатПриемаВходящейОписиИнтерактивно_СформироватьПакетФНС", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			СоздатьРезультатПриемаНаКлиентеСУчетомВопроса(
				Оповещение, 
				ДокументНО, 
				ПоложительныйРезультат, 
				ВходящийКонтекст.ТихийРежим,
				,
				ВходящийКонтекст.НужноПересоздать);
				
		КонецЕсли;
	КонецЕсли;   
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьРезультатПриемаВходящейОписиИнтерактивно_СформироватьПакетФНС(РезультатПриема, ВходящийКонтекст) Экспорт
	
	ВозниклаОшибка = 
		РезультатПриема = Неопределено 
		ИЛИ РезультатПриема = Ложь 
		ИЛИ ТипЗнч(РезультатПриема) = Тип("Структура");
		
	Если ВозниклаОшибка Тогда
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ДействиеПослеПодтвержденияПриема, РезультатПриема);
		
	Иначе
		
		ВходящийКонтекст.Вставить("РезультатПриема", РезультатПриема);
		Оповещение = Новый ОписаниеОповещения(
			"СоздатьРезультатПриемаВходящейОписиИнтерактивноПоДокументуОписиЗавершениеПослеФормированияПакетаФНС", ЭтотОбъект, ВходящийКонтекст);
		СформироватьПакетФНС(Оповещение, РезультатПриема);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьРезультатПриемаВходящейОписиИнтерактивноПоДокументуОписиЗавершениеПослеФормированияПакетаФНС(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Ложь Тогда
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ДействиеПослеПодтвержденияПриема, Ложь);
	Иначе
		
		//отправляем сообщение
		РезультатОтправки = ОтправитьТранспортноеСообщение(ВходящийКонтекст.РезультатПриема);
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ДействиеПослеПодтвержденияПриема, РезультатОтправки);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СохранитьДатуПодтвержденияТребованияСФР(Требование)
	
	ТребованиеОбъект = Требование.ПолучитьОбъект();
	ТребованиеОбъект.ДатаОтправкиПодтверждения = ТекущаяДатаНаСервере();
	ТребованиеОбъект.Записать();
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура СоздатьРезультатПриемаВходящейОписиИнтерактивно_ПослеПодтвержденияСЭДО(Результат, ДополнительныеПараметры) Экспорт
	
	ДействиеПослеПодтвержденияПриема = ДополнительныеПараметры.ДействиеПослеПодтвержденияПриема;
	Если Результат.Выполнено Тогда
		СохранитьДатуПодтвержденияТребованияСФР(ДополнительныеПараметры.Требование);
		ВыполнитьОбработкуОповещения(ДействиеПослеПодтвержденияПриема, Истина);
	Иначе
		ВыполнитьОбработкуОповещения(ДействиеПослеПодтвержденияПриема, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСообщениеДокументНОПоОписиВходящихДокументов(ОписьВходящихДокументов)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// Получим список сообщений
	ПоследнийЦиклОбмена 		= ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(ОписьВходящихДокументов);
	СообщениеДокументНО			= КонтекстЭДОСервер.ПолучитьСообщениеЦиклаОбмена(ПоследнийЦиклОбмена, Перечисления.ТипыТранспортныхСообщений.ДокументНО);
	Если СообщениеДокументНО 	= Документы.ТранспортноеСообщение.ПустаяСсылка() Тогда
		Возврат Неопределено;
	Иначе
		Возврат СообщениеДокументНО;
	КонецЕсли;

КонецФункции

&НаКлиенте
Процедура НажатиеНаКнопкуПоказатьОтветыПоТребованию(ТребованиеСсылка, ВключаяНевозможностьПредоставленияДокументов = Истина) Экспорт
	
	Если ТипЗнч(ТребованиеСсылка) = Тип("ДокументСсылка.ВходящийДокументСЭДОФСС") Тогда
		
		ОтветыНаТребованиеСФР = ОтветыНаТребованиеФСС(ТребованиеСсылка);
		КоличествоОтветов = ОтветыНаТребованиеСФР.Количество();
		
		Если КоличествоОтветов = 0 Тогда
			
			ТекстСообщения = НСтр("ru = 'Ответы на требование СФР отсутствуют'");
			ПоказатьПредупреждение(, ТекстСообщения);
			Возврат;
			
		КонецЕсли;
		
		Если КоличествоОтветов > 1 Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("Основание", ТребованиеСсылка);
			
			ОткрытьФорму(
				"Обработка.ДокументооборотСКонтролирующимиОрганами.Форма.ОтветыНаТребованияФСС",
				ДополнительныеПараметры,,
				Новый УникальныйИдентификатор);
			
		Иначе
			
			ПоказатьЗначение(, ОтветыНаТребованиеСФР[0]);
			
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(ТребованиеСсылка) = Тип("ДокументСсылка.ОбращениеСФРКСтрахователю") Тогда
		
		Ответы = ОтветыНаОбращениеСФРКСтрахователю(ТребованиеСсылка);
		Если Ответы.Количество() = 0 Тогда
			
			СфомироватьОтветНаОбращениеСФРКСтрахователю(ТребованиеСсылка);
			
		Иначе
			
			ПоказатьОтветыНаОбращениеСФРКСтрахователю(ТребованиеСсылка);
			
		КонецЕсли;
		
	Иначе
		МассивОтветов = ОтветыНаТребование(ТребованиеСсылка, ВключаяНевозможностьПредоставленияДокументов);
		
		Если МассивОтветов.Количество() = 0 Тогда
			
			// Создание ответа
			СоздатьОтветНаТребованиеФНС(ТребованиеСсылка);
			
		Иначе
			
			ПоказатьОтветыНаТребованияФНС(МассивОтветов);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НажатиеНаКнопкуПоказатьОтветыПоТребованиюФСС(ТребованиеСсылка) Экспорт
	
	МассивОтветов = ОтветыНаТребованиеФСС(ТребованиеСсылка);
	
	Если МассивОтветов.Количество() > 0 Тогда
	
		ПоказатьОтветыНаТребованияФСС(МассивОтветов);
	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьНеотправленныеИзвещенияОПриеме(ОтчетСсылка, Организация, КонтролирующийОрган, ВыполняемоеОповещение) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ПолучатьОшибкиОтправки", Истина);
	
	НеотправленныеИзвещения = ТекущееСостояниеОтправки(ОтчетСсылка, КонтролирующийОрган, ДополнительныеПараметры).НеотправленныеИзвещения;

	Контекст = Новый Структура;
	Контекст.Вставить("ОтчетСсылка", ОтчетСсылка);
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("КонтролирующийОрган", КонтролирующийОрган);
	Контекст.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	Оповещение = Новый ОписаниеОповещения(
		"СоздатьПодтверждениеПриНеобходимостиПослеВыполнения", ЭтотОбъект, Контекст);
		
	// Если есть сообщения, не имеющие подтверждения, то создаем соотвествующие подтверждения
	Если НеотправленныеИзвещения.МассивСообщенийНеИмеющихПодтверждений.Количество() > 0 Тогда
		
		Контекст = Новый Структура;
		Контекст.Вставить("МассивСообщенийНеИмеющихПодтверждений", НеотправленныеИзвещения.МассивСообщенийНеИмеющихПодтверждений);
		Контекст.Вставить("ОповещениеОЗавершении", 	Оповещение);
		Контекст.Вставить("ТекущееСообщение", 		0);
		Контекст.Вставить("ЭтоОтправкаПодтвержденияИзПанелиОтправки", Истина);
		
		СоздатьПодтверждениеПриНеобходимостиПоследовательно(Контекст);
		Возврат;
	КонецЕсли;
	
	СоздатьПодтверждениеПриНеобходимостиПослеВыполнения(Истина, Контекст);	
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждениеПриНеобходимостиПоследовательно(ВходящийКонтекст)
	
	Если ВходящийКонтекст.ТекущееСообщение < ВходящийКонтекст.МассивСообщенийНеИмеющихПодтверждений.Количество() Тогда
		
		Оповещение = Новый ОписаниеОповещения(
			"СоздатьПодтверждениеПриНеобходимостиПоследовательноПослеВыполнения", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		Сообщение = ВходящийКонтекст.МассивСообщенийНеИмеющихПодтверждений[ВходящийКонтекст.ТекущееСообщение];
		
		СоздатьПодтверждениеПриНеобходимости(Оповещение, Сообщение, Истина);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждениеПриНеобходимостиПоследовательноПослеВыполнения(Результат, ВходящийКонтекст) Экспорт
	
	ВходящийКонтекст.ТекущееСообщение = ВходящийКонтекст.ТекущееСообщение + 1;
	СоздатьПодтверждениеПриНеобходимостиПоследовательно(ВходящийКонтекст);

КонецПроцедуры

&НаКлиенте
Процедура СоздатьПодтверждениеПриНеобходимостиПослеВыполнения(Результат, ВходящийКонтекст) Экспорт
	
	// Теперь у нас должны остаться только неотправленные сообщения
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ПолучатьОшибкиОтправки", Истина);
	
	НеотправленныеИзвещения = ТекущееСостояниеОтправки(ВходящийКонтекст.ОтчетСсылка, ВходящийКонтекст.КонтролирующийОрган, ДополнительныеПараметры).НеотправленныеИзвещения;
	МассивНеотправленныхПодтверждений = НеотправленныеИзвещения.МассивНеотправленныхПодтверждений;

	Если МассивНеотправленныхПодтверждений.Количество() > 0 Тогда
		
		УчетнаяЗапись = УчетнаяЗаписьОрганизации(ВходящийКонтекст.Организация); 
		ДополнительныеПараметры = Новый Структура("МассивНеотправленныхПодтверждений, ВыполняемоеОповещение, УчетнаяЗапись, Организация", 
			МассивНеотправленныхПодтверждений, ВходящийКонтекст.ВыполняемоеОповещение, УчетнаяЗапись, ВходящийКонтекст.Организация);
		ОписаниеОповещения = Новый ОписаниеОповещения("ОтправитьНеотправленныеИзвещенияОПриемеПослеАвтонастройкиУчетнойЗаписи", ЭтотОбъект, ДополнительныеПараметры);
		АвтоматическаяНастройкаУчетнойЗаписи(УчетнаяЗапись, ВходящийКонтекст.Организация, ОписаниеОповещения);
	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьНеотправленныеИзвещенияОПриемеПослеАвтонастройкиУчетнойЗаписи(Результат, ДополнительныеПараметры) Экспорт
	
	МассивНеотправленныхПодтверждений = ДополнительныеПараметры.МассивНеотправленныхПодтверждений;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;

	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВыполняемоеОповещение);
	Оповещение = Новый ОписаниеОповещения(
		"ОтправитьНеотправленныеИзвещенияОПриемеПослеОтправки", ЭтотОбъект, Контекст);
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", Оповещение);
	Контекст.Вставить("КоличествоОтправленных", 0);
	Контекст.Вставить("КоличествоНеОтправленных", 0);
	Контекст.Вставить("МассивНеотправленныхПодтверждений", МассивНеотправленныхПодтверждений);
	Контекст.Вставить("ТекущееПодтверждение", 0);
	
	ОтправитьНеотправленныеИзвещенияПоследовательно(Контекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьНеотправленныеИзвещенияПоследовательно(ВходящийКонтекст)
	
	Если ВходящийКонтекст.ТекущееПодтверждение < ВходящийКонтекст.МассивНеотправленныхПодтверждений.Количество() Тогда
		Сообщение = ВходящийКонтекст.МассивНеотправленныхПодтверждений[ВходящийКонтекст.ТекущееПодтверждение];
		
		// формируем пакет, если не сформирован
		Если НЕ СообщениеЗашифровано(Сообщение) Тогда
			Оповещение = Новый ОписаниеОповещения(
				"ОтправитьНеотправленныеИзвещенияПоследовательноПослеВыполнения", ЭтотОбъект, ВходящийКонтекст);
			СформироватьПакет(Оповещение, Сообщение, Истина);
			Возврат;
		КонецЕсли;
		ОтправитьНеотправленныеИзвещенияПоследовательноПослеВыполнения(Истина, ВходящийКонтекст);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьНеотправленныеИзвещенияПоследовательноПослеВыполнения(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Истина Тогда
		Сообщение = ВходящийКонтекст.МассивНеотправленныхПодтверждений[ВходящийКонтекст.ТекущееПодтверждение];
		
		// Отправляем сообщение
		Если ОтправитьТранспортноеСообщение(Сообщение) Тогда
			ВходящийКонтекст.КоличествоОтправленных = ВходящийКонтекст.КоличествоОтправленных + 1;
		Иначе
			ВходящийКонтекст.КоличествоНеОтправленных = ВходящийКонтекст.КоличествоНеОтправленных + 1;
		КонецЕсли;
		
		ВходящийКонтекст.ТекущееПодтверждение = ВходящийКонтекст.ТекущееПодтверждение + 1;	
		ОтправитьНеотправленныеИзвещенияПоследовательно(ВходящийКонтекст);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьНеотправленныеИзвещенияОПриемеПослеОтправки(Результат, ВходящийКонтекст) Экспорт
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ТекущееСостояниеОтправки(Знач Ссылка, Знач КонтролирующийОрган = "ФНС", Знач ДополнительныеПараметры = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТекущееСостояниеОтправки(Ссылка, КонтролирующийОрган, ДополнительныеПараметры);
	
КонецФункции

&НаКлиенте
Процедура ПоказатьПодтверждениеПолученияПФР(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СообщениеВладелецФайла = ТранспортноеСообщение;
	
	Результат = ПолучитьАдресВложения(СообщениеВладелецФайла, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(СообщениеВладелецФайла) Тогда
		СтруктураПараметров.Вставить("Сообщение", СообщениеВладелецФайла);
	КонецЕсли;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда
		СтруктураПараметров.Вставить("ПодтверждениеПолучения", Адрес);
	КонецЕсли;
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", ПечатьВозможна);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрПодтвержденияПолученияПФР", СтруктураПараметров, );
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСоответствиеТиповСодержимого() Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСоответствиеТиповСодержимого();
	
КонецФункции

&НаСервере
Функция ПолучитьАдресВложения(СообщениеВладелецФайла, ИмяФайлаДокумента)
	
	Результат = Новый Структура;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Вложения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(СообщениеВладелецФайла, Истина, , ИмяФайлаДокумента);
	
	Если Вложения.Количество() = 0 Тогда
		Результат.Вставить("Предупреждение", "Документ не найден в информационной базе.");
		Возврат Результат;
	КонецЕсли;
	
	ГУИД = Новый УникальныйИдентификатор;
	Адрес = ПоместитьВоВременноеХранилище(Вложения[0].Данные.Получить(), ГУИД);
	Результат.Вставить("Адрес", Адрес);
	Возврат Результат;

КонецФункции

&НаКлиенте
Процедура ПоказатьПротоколПФР(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СообщениеВладелецФайла = ТранспортноеСообщение;
	
	Результат = ПолучитьАдресВложения(СообщениеВладелецФайла, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(СообщениеВладелецФайла) Тогда
		СтруктураПараметров.Вставить("СообщениеПротокол", СообщениеВладелецФайла);
	КонецЕсли;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда 
		СтруктураПараметров.Вставить("Протокол", Адрес);
	КонецЕсли;
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", ПечатьВозможна);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрПротоколаПФР", СтруктураПараметров, );
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПодтверждениеПолученияПисьмаПФР(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента) Экспорт
	
	Результат = ПолучитьАдресВложения(ТранспортноеСообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда 
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда 
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда 
		СтруктураПараметров.Вставить("ПодтверждениеПолучения", Адрес);
	КонецЕсли;
	
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", Истина);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрПодтвержденияПолученияПисьмаПФР", СтруктураПараметров, );
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОСИзФормыТранспортногоСообщения(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента) Экспорт
	
	Результат = ПолучитьАдресВложения(ТранспортноеСообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда 
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(ТранспортноеСообщение) Тогда 
		СтруктураПараметров.Вставить("СообщениеПротокол", ТранспортноеСообщение);
	КонецЕсли;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда 
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда 
		СтруктураПараметров.Вставить("Протокол", Адрес);
	КонецЕсли;
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", Истина);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрОтветаНаЗапросИОС", СтруктураПараметров, );
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПодтверждениеПолученияЗапросаПФР(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента) Экспорт
	
	Результат = ПолучитьАдресВложения(ТранспортноеСообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда 
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда 
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда 
		СтруктураПараметров.Вставить("ПодтверждениеПолучения", Адрес);
	КонецЕсли;
	
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", Истина);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрПодтвержденияПолученияЗапросаПФР", СтруктураПараметров, );
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеОбОтказе(ЦиклОбмена, ТранспортноеСообщение, ТипТранспортногоСообщения, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаПодтверждения, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	
	Если ТипТранспортногоСообщения = СтруктураСсылочныхДанных["Перечисления_ТипыТранспортныхСообщений_РезультатПриемаЗапросНО"] 
		ИЛИ ТипТранспортногоСообщения = СтруктураСсылочныхДанных["Перечисления_ТипыТранспортныхСообщений_РезультатПриемаОбращениеНО"] Тогда
		ОткрытьФорму(ПутьКОбъекту + ".Форма.УведомлениеОбОтказеОбращениеЗапрос", СтруктураПараметров);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.УведомлениеОбОтказе", СтруктураПараметров);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьКвитанциюОПриеме(ЦиклОбмена, ТранспортноеСообщение, ТипТранспортногоСообщения, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаКвитанции, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	
	Если ТипТранспортногоСообщения = СтруктураСсылочныхДанных["Перечисления_ТипыТранспортныхСообщений_РезультатПриемаЗапросНО"] Тогда
		ОткрытьФорму(ПутьКОбъекту + ".Форма.КвитанцияОПриемеЗапрос", СтруктураПараметров);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.КвитанцияОПриеме", СтруктураПараметров);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьИзвещениеОВводе(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаИзвещения, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ИзвещениеОВводе", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеОбУточнении504(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаУведомления, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.УведомлениеОбУточнении505", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьИзвещениеОПолучении(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаИзвещения, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ИзвещениеОПолучении", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьКвитанцияОПриемеЗаявление(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.КвитанцияОПриемеЗаявление", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОПроставленииОтметки(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОПроставленииОтметки", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеОбОтказеОтметки(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.УведомлениеОбОтказеОтметки", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОПроверке(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОПроверке", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОбОтзывеЗаявления(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОбОтзывеЗаявления", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОНесоответствиях(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОНесоответствиях", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеОбОтказеДокумент(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.УведомлениеОбОтказеДокумент", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьКвитанцияОПриемеДокумент(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.КвитанцияОПриемеДокумент", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПодтверждениеДатыПолучения(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ИмяФайлаПодтверждения, ЦиклОбмена, ПечатьВозможна", ТранспортноеСообщение, ИмяФайлаДокумента, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПодтверждениеДатыПолучения", СтруктураПараметров);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПодтверждениеПолученияФСГС(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	Результат = ПолучитьАдресВложения(ТранспортноеСообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда 
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(ТранспортноеСообщение) Тогда 
		СтруктураПараметров.Вставить("Сообщение", ТранспортноеСообщение);
	КонецЕсли;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда 
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда 
		СтруктураПараметров.Вставить("ПодтверждениеПолучения", Адрес);
	КонецЕсли;
	
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", Истина);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрПодтвержденияПолученияФСГС", СтруктураПараметров, );

КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПротоколФСГС(ЦиклОбмена, ТранспортноеСообщение, ИмяФайлаДокумента, ПечатьВозможна = Ложь) Экспорт
	
	Результат = ПолучитьАдресВложения(ТранспортноеСообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда
		ПоказатьПредупреждение(,Результат.Предупреждение);
		Возврат;
	Иначе 
		Адрес = Результат.Адрес;
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	Если ЗначениеЗаполнено(ТранспортноеСообщение) Тогда
		СтруктураПараметров.Вставить("СообщениеПротокол", ТранспортноеСообщение);
	КонецЕсли;
	Если ЗначениеЗаполнено(ИмяФайлаДокумента) Тогда 
		СтруктураПараметров.Вставить("ИмяФайла", ИмяФайлаДокумента);
	КонецЕсли;
	Если ЗначениеЗаполнено(Адрес) Тогда 
		СтруктураПараметров.Вставить("Протокол", Адрес);
	КонецЕсли;
	СтруктураПараметров.Вставить("ЦиклОбмена", ЦиклОбмена);
	СтруктураПараметров.Вставить("ПечатьВозможна", ПечатьВозможна);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрПротоколаФСГС", СтруктураПараметров, );
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьРасширениеФайлаФССПоТипуОтправляемогоДокумента(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента = Неопределено,
		ОтправкаЧерезСЭДО = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьРасширениеФайлаФССПоТипуОтправляемогоДокумента(
		ТипОтправляемогоДокумента,
		ВидОтправляемогоДокумента,
		ОтправкаЧерезСЭДО);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОбъектСсылка,
		ПолучитьВидОтправляемогоДокумента = Ложь) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьТипВидаОтправляемогоДокументаПоСсылкеНаОбъектОтправкиДляФСС(
		ОбъектСсылка,
		ПолучитьВидОтправляемогоДокумента);
	
КонецФункции

&НаКлиенте
Процедура ПоказатьПротоколПриложениеПФР(Сообщение, ИмяФайлаДокумента, ВыполняемоеОповещение = Неопределено) Экспорт
	
	Результат = ПолучитьСтрокуВложенияНаСервере(Сообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда 
		ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьПредупреждениеЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
		ПоказатьПредупреждение(ОписаниеОповещения,Результат.Предупреждение);
	Иначе
		ПоказатьПриложениеПФР(Сообщение ,Результат, ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеЗавершение(ВыполняемоеОповещение) Экспорт
	
	Если ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьСтрокуВложенияНаСервере(СообщениеПротокол, ИмяФайлаДокумента) Экспорт
	
	Результат = Новый Структура;
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Вложения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(СообщениеПротокол, Истина, , ИмяФайлаДокумента, Истина);
	Если Вложения.Количество() = 0 Тогда
		Результат.Вставить("Предупреждение", "Приложение не найдено.");
		Возврат Результат;
	КонецЕсли;
	
	Вложение = Вложения[0];
	Для Каждого Колонка Из Вложения.Колонки Цикл
		Если ТипЗнч(Вложение[Колонка.Имя]) = Тип("ХранилищеЗначения") Тогда 
			Результат.Вставить(Колонка.Имя, Вложение[Колонка.Имя].Получить());
		Иначе
			Результат.Вставить(Колонка.Имя, Вложение[Колонка.Имя]);
		КонецЕсли;
	КонецЦикла;
	
	Если Результат.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.html Тогда 
		Результат.Вставить("ЭтоHTML", Истина);
	ИначеЕсли Результат.ТипСодержимогоФайла = Перечисления.ТипыСодержимогоФайлов.Текст1251 Тогда 
		Результат.Вставить("ЭтоTXT", Истина);
	КонецЕсли;
	
	Возврат Результат;
КонецФункции

&НаКлиенте
Процедура ПоказатьПротоколВложениеФСГС(Сообщение, ИмяФайлаДокумента, ВыполняемоеОповещение)
	
	Результат = ПолучитьСтрокуВложенияНаСервере(Сообщение, ИмяФайлаДокумента);
	Если Результат.Свойство("Предупреждение") Тогда 
		ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьПредупреждениеЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
		ПоказатьПредупреждение(ОписаниеОповещения,Результат.Предупреждение);
	Иначе
		ПоказатьВложениеФСГС(Результат, ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПодтверждениеДатыОтправки(Сообщение, ЦиклОбмена, ПечатьВозможна = Ложь) Экспорт
	
	СтруктураПараметров = Новый Структура("Сообщение, ЦиклОбмена, ПечатьВозможна", Сообщение, ЦиклОбмена, ПечатьВозможна);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПодтверждениеДатыОтправки", СтруктураПараметров);
	
КонецПроцедуры

&НаСервере
Функция ВложенияТранспортногоСообщения(ТранспортноеСообщение)
	
	// Мы не можем на клиент передать таблицу значений, поэтому передаем массив структур
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ВложенияТранспортногоСообщения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение);
	
	ДанныеВложений = Новый Массив;
	Для каждого Вложение Из ВложенияТранспортногоСообщения Цикл
	
		ЗначенияПолейВложений = Новый Структура;
		ЗначенияПолейВложений.Вставить("ИмяФайла", 				Вложение.ИмяФайла); 
		ЗначенияПолейВложений.Вставить("Тип", 					Вложение.Тип);
		ЗначенияПолейВложений.Вставить("ТранспортноеСообщение", Вложение.ТранспортноеСообщение);
		ЗначенияПолейВложений.Вставить("ТипСодержимогоФайла",   Вложение.ТипСодержимогоФайла);
		ЗначенияПолейВложений.Вставить("ВАрхиве",   			Вложение.ВАрхиве);
		
		ДанныеВложений.Добавить(ЗначенияПолейВложений);
	
	КонецЦикла;
	
	Возврат ДанныеВложений;
	
КонецФункции

&НаСервере
Функция ПротоколЕсть(Отправка)
	
	Если Отправка.Протокол = Неопределено Тогда
		Возврат Ложь;
	Иначе 
		Возврат ЗначениеЗаполнено(Отправка.Протокол.Получить());
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция ПечатьПротоколаВозможна(ЦиклОбмена, ТранспортноеСообщение, РезультатНастройки)
	
	ПечатьВозможна = Ложь;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ПечатьВозможнаДляЦиклаОбмена = КонтекстЭДОСервер.ПечатьВозможнаДляЦиклаОбмена(ЦиклОбмена.Тип);
	
	Если ПечатьВозможнаДляЦиклаОбмена Тогда
	
		КонтекстЭДОСервер  = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		РезультатНастройки = КонтекстЭДОСервер.ПротоколыДляПечати(ЦиклОбмена, ТранспортноеСообщение);
		
		// Определяем, есть ли протоколы для печати
		ЕстьПротоколыДляПечати = Ложь;
		Если РезультатНастройки <> Неопределено Тогда 
			Для каждого ВыбранныйПротокол Из РезультатНастройки Цикл
				Если ВыбранныйПротокол.Значение = Истина Тогда
					ПечатьВозможна = Истина;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ПечатьВозможна;
	
КонецФункции

&НаКлиенте
Функция ТипКонтролирующегоОргана(Знач КонтролирующийОрган = "ФНС") Экспорт
	
	Результат = КонтролирующийОрган;
	
	Если ТипЗнч(КонтролирующийОрган) = Тип("Строка") Тогда
		Результат = ТипКонтролирующегоОрганаНаСервере(КонтролирующийОрган);
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции

&НаСервере
Функция ТипКонтролирующегоОрганаНаСервере(Знач КонтролирующийОрган = "ФНС") Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Результат = КонтекстЭДОСервер.ТипКонтролирующегоОргана(КонтролирующийОрган);
	
	Возврат Результат;
		
КонецФункции

&НаСервере
Функция ПолучитьСсылкуНаВходящийФайл(Ссылка)
	
	Если ЗначениеЗаполнено(Ссылка.ВходящийФайлСПодписьюФонда) Тогда
		Возврат Ссылка.ВходящийФайлСПодписьюФонда;
	Иначе
		Возврат Ссылка.ВходящийФайл;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьПротокол(
		Знач Ссылка,
		Знач ВидКонтролирующегоОргана,
		Знач Протокол = Неопределено,
		Знач Организация = Неопределено) Экспорт
	
	Если ТипЗнч(Ссылка) <> Тип("СправочникСсылка.ДокументыРеализацииПолномочийНалоговыхОрганов")
		И ТипЗнч(Ссылка) <> Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами") Тогда
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОтметитьКакПрочтенное(Ссылка);
	КонецЕсли;
	
	ТипКонтролирующегоОргана = ТипКонтролирующегоОргана(ВидКонтролирующегоОргана);
	
	Если ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗаявлениеАбонентаСпецоператораСвязи") Тогда
		
		СтатусЗаявления = ПолучитьРеквизитНаСервере(Ссылка, "Статус");
		
		Если ЗначениеЗаполнено(Протокол) Тогда
			
			// В качестве протокола - адрес текста отправленного заявления
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(Протокол, "Заявление.xml");
			
		ИначеЕсли СтатусЗаявления = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено") Тогда
			
			// Для отправленного открываем XML-текст заявления
			Протокол = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.АдресТекстаОтправленногоЗаявления(Ссылка);
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(Протокол, "Заявление.xml");
			
		ИначеЕсли СтатусЗаявления = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено")
			ИЛИ СтатусЗаявления = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено") Тогда
			
			// Если заявление уже обработано оператором, то открываем комментарий от оператора электронного документооборота
			СтатусКомментарий = ПолучитьРеквизитНаСервере(Ссылка, "СтатусКомментарий");
			ПоказатьПредупреждение(, СтатусКомментарий);
			
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(Ссылка) = Тип("ДокументСсылка.ЗаявлениеНаФормированиеСправкиОРасчетах") Тогда
		
		Если ТипЗнч(Протокол) = Тип("СправочникСсылка.ОтправкиФСС") Тогда
			Если ПротоколЕсть(Протокол) Тогда
				ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСС(Протокол);
			КонецЕсли;
		Иначе
			ВходящийФайл = ПолучитьСсылкуНаВходящийФайл(Ссылка);
			ДанныеФайла = РаботаСФайламиСлужебныйВызовСервера.ДанныеФайлаДляОткрытия(ВходящийФайл,
				Неопределено, УникальныйИдентификатор, Неопределено);
			РаботаСФайламиКлиент.ОткрытьФайл(ДанныеФайла, Ложь);
		КонецЕсли;
		
	Иначе
		
		// Если протокол пустой, то получаем последний протокол из последней отправки/цикла обмена
		Если Протокол = Неопределено Тогда
			// Получение текущего состояния
			ТекущееСостояние = ТекущееСостояниеОтправки(Ссылка, ТипКонтролирующегоОргана);
			ТекущийЭтап = ТекущееСостояние.ТекущийЭтапОтправки;
			Если ТекущийЭтап <> Неопределено Тогда
				Протокол = ТекущийЭтап.Протокол;
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Протокол) Тогда
			Возврат;
		КонецЕсли;
		
		// ФСС
		Если ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСС") Тогда
			
			Если НЕ ЗначениеЗаполнено(Протокол) Тогда
				Протокол = ПолучитьПоследнююОтправкуОтчетаВФСС(Ссылка);
			КонецЕсли;
			
			Если ЗначениеЗаполнено(Протокол) И ПротоколЕсть(Протокол) Тогда
				ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСС(Протокол);
			КонецЕсли;
			
		// ФСРАР
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФСРАР") Тогда
			
			Если НЕ ЗначениеЗаполнено(Протокол) Тогда
				Протокол = ПолучитьПоследнююОтправкуОтчетаВФСРАР(Ссылка);
			КонецЕсли;

			Если ЗначениеЗаполнено(Протокол)И ПротоколЕсть(Протокол) Тогда
				ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФСРАР(Протокол);
			КонецЕсли;
			
		// РПН
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РПН") Тогда
			
			Если НЕ ЗначениеЗаполнено(Протокол) Тогда
				Протокол = ПолучитьПоследнююОтправкуОтчетаВРПН(Ссылка);
			КонецЕсли;
				
			Если ЗначениеЗаполнено(Протокол) И ПротоколЕсть(Протокол) Тогда
				ПоказатьПротоколОбработкиПоСсылкеИсточникаДляРПН(Протокол);
			КонецЕсли;
			
		// ФТС
	ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС")
		ИЛИ ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ЦПРР") Тогда
			
			Если ТипЗнч(Протокол) = Тип("Строка") Тогда
				ПерейтиПоНавигационнойСсылке(Протокол);
			Иначе
				
				Если НЕ ЗначениеЗаполнено(Протокол) Тогда
					Протокол = ПолучитьПоследнююОтправкуОтчетаВФТС(Ссылка);
				КонецЕсли;
				
				Если ЗначениеЗаполнено(Протокол) И ПротоколЕсть(Протокол) Тогда
					ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФТС(Протокол);
				КонецЕсли;
				
			КонецЕсли;
			
		// Банк России
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.УдалитьБанкРоссии") Тогда
			
			Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.УдалитьСдачаОтчетностиВБанкРоссии") Тогда
				МодульУдалитьДокументооборотСБанкомРоссииКлиент =
					ОбщегоНазначенияКлиент.ОбщийМодуль("УдалитьДокументооборотСБанкомРоссииКлиент");
				Если НЕ ЗначениеЗаполнено(Протокол) Тогда
					Протокол = МодульУдалитьДокументооборотСБанкомРоссииКлиент.ПолучитьПоследнююОтправкуОтчета(Ссылка);
				КонецЕсли;
					
				Если ЗначениеЗаполнено(Протокол) И ПротоколЕсть(Протокол) Тогда
					МодульУдалитьДокументооборотСБанкомРоссииКлиент.ПоказатьПротокол(Протокол);
				КонецЕсли;
			КонецЕсли;
			
		// Минобороны
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.Минобороны") Тогда
			
			Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВМинобороны") Тогда
				МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
				
				Если НЕ ЗначениеЗаполнено(Протокол) Тогда
					Протокол = МодульДокументооборотСМинобороныКлиент.ПолучитьПоследнююОтправкуОтчета(Ссылка);
				КонецЕсли;
					
				Если ЗначениеЗаполнено(Протокол) И ПротоколЕсть(Протокол) Тогда
					МодульДокументооборотСМинобороныКлиент.ПоказатьПротокол(Протокол);
				КонецЕсли;
			КонецЕсли;
			
		// РЭЦ
		ИначеЕсли ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РЭЦ") Тогда
			
			МодульДокументооборотСРЭЦКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСРЭЦКлиент");
			Если ЗначениеЗаполнено(Протокол) Тогда 
				ПоследняяОтправка = Протокол;
			Иначе
				ПоследняяОтправка = МодульДокументооборотСРЭЦКлиент.ПолучитьПоследнююОтправкуОтчета(Ссылка);
			КонецЕсли;			
			МодульДокументооборотСРЭЦКлиент.ПоказатьПротокол(ПоследняяОтправка);

		Иначе
			
			// Остальные контролирующие органы
			
			// Определяем протокол и цикл обмена.
			Если ТипЗнч(Протокол) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда
				ЦиклОбмена = ПолучитьРеквизит(Протокол, "ЦиклОбмена");
			ИначеЕсли ТипЗнч(Протокол) = Тип("СправочникСсылка.ЦиклыОбмена") Тогда
				
				ЦиклОбмена = Протокол;
				
				ДополнительныеПараметры = Новый Структура();
				ДополнительныеПараметры.Вставить("ПолучатьДаты", 			Истина);
				ДополнительныеПараметры.Вставить("ПолучатьОшибкиОтправки", 	Истина);
				ДополнительныеПараметры.Вставить("Отправка", 				ЦиклОбмена);
				
				ТекущееСостояние = ТекущееСостояниеОтправки(
					Ссылка, 
					ВидКонтролирующегоОргана,
					ДополнительныеПараметры);
					
				Если ТекущееСостояние = Неопределено Тогда
					Возврат;
				КонецЕсли;
				
				ТекущийЭтапОтправки = ТекущееСостояние.ТекущийЭтапОтправки;
				
				Если ТекущийЭтапОтправки = Неопределено Тогда
					Возврат;
				КонецЕсли;
				
				Протокол = ТекущийЭтапОтправки.Протокол;
				
			КонецЕсли;
			
			РезультатНастройки = Неопределено;
			
			Если ЗначениеЗаполнено(Протокол) И ЗначениеЗаполнено(ЦиклОбмена) Тогда
				
				Вложения     = ВложенияТранспортногоСообщения(Протокол);
				ЕстьВложения = Вложения.Количество() > 0;
				ТипПротокола = ПолучитьРеквизит(Протокол, "Тип");
				
				Если ТипПротокола = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.РезультатОбработкиЗапросНО") И ЕстьВложения Тогда
					
					ПоказатьОтветНаЗапросИОН(Ссылка);
					
				ИначеЕсли ТипПротокола = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ОтветНаЗапросПФР") И ЕстьВложения Тогда
					
					ПоказатьОтветНаЗапросИОС(Ссылка);
					
				ИначеЕсли ТипПротокола = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.РезультатОбработкиЗапросаНаВыпискуЕРГЮЛ_ЕГРИП") И ЕстьВложения Тогда
					
					ПоказатьОтветНаЗапросВыпискиЕГРЮЛ_ЕГРИП(Ссылка);
					
				ИначеЕсли ПечатьПротоколаВозможна(ЦиклОбмена, Протокол, РезультатНастройки) Тогда
					
					ПоказатьДокументыЦикловОбмена(ЦиклОбмена, Истина, Протокол, РезультатНастройки);
					
				Иначе
					
					СоответствиеТиповСодержимого   = ПолучитьСоответствиеТиповСодержимого();
					
					Если ЕстьВложения Тогда
						ПоказатьВложениеТранспортногоСообщения(Вложения, 0, Протокол, ЦиклОбмена, СоответствиеТиповСодержимого, Ложь);
					Иначе
						// Сначала расшифровываем, потом показываем протокол
						ДополнительныеПараметры = Новый Структура;
						ДополнительныеПараметры.Вставить("Протокол", 		Протокол);
						ДополнительныеПараметры.Вставить("ЦиклОбмена", 		ЦиклОбмена);
						ДополнительныеПараметры.Вставить("СсылкаНаОтчет", 	Ссылка);
						ДополнительныеПараметры.Вставить("Организация", 	Организация);
						ОписаниеОповещения 		= Новый ОписаниеОповещения("ОткрытьВложениеПротоколаПослеРасшифровки", ЭтотОбъект, ДополнительныеПараметры);
						РасшифроватьТранспортноеСообщение(Протокол, ОписаниеОповещения);

					КонецЕсли;
					
				КонецЕсли;
					
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьПротоколПоСсылке(
		Знач Ссылка,
		Знач ВидКонтролирующегоОргана,
		Знач НавигационнаяСсылка,
		Знач Организация = Неопределено) Экспорт
		
	ТипКонтролирующегоОргана = ТипКонтролирующегоОргана(ВидКонтролирующегоОргана);
	
	// Регистрация ЮЛ
	Если ТипКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.РегистрацияЮЛФНС") Тогда
		Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ГосударственнаяРегистрацияВФНС") Тогда
			МодульДокументооборотРегистрацияВФНССлужебныйКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотРегистрацияВФНССлужебныйКлиент");
			МодульДокументооборотРегистрацияВФНССлужебныйКлиент.ОткрытьФайлПоСсылке(Ссылка, НавигационнаяСсылка, УникальныйИдентификатор);
		КонецЕсли;
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьВложениеПротоколаПослеРасшифровки(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Ложь Тогда
		Возврат;
	КонецЕсли;
	
	Протокол 		= ДополнительныеПараметры.Протокол;
	ЦиклОбмена 		= ДополнительныеПараметры.ЦиклОбмена;
	СсылкаНаОтчет 	= ДополнительныеПараметры.СсылкаНаОтчет;
	Организация 	= ДополнительныеПараметры.Организация;
	
	Оповестить("Расшифровка вложения протокола для открытия", Протокол, ЦиклОбмена);
	
	Если ЗначениеЗаполнено(СсылкаНаОтчет) И ЗначениеЗаполнено(Организация) Тогда
		ПараметрыОповещения = Новый Структура;
		ПараметрыОповещения.Вставить("Ссылка", 		СсылкаНаОтчет);
		ПараметрыОповещения.Вставить("Организация", Организация);
		Оповестить("Актуализация состояния отправки", ПараметрыОповещения, ЦиклОбмена);
	КонецЕсли;
	
	СоответствиеТиповСодержимого 	= ПолучитьСоответствиеТиповСодержимого();
	ВложенияТранспортногоСообщения 	= ВложенияТранспортногоСообщения(Протокол);

	РезультатНастройки = Неопределено;
	
	Если ПечатьПротоколаВозможна(ЦиклОбмена, Протокол, РезультатНастройки) Тогда
		ПоказатьДокументыЦикловОбмена(ЦиклОбмена, Истина, Протокол, РезультатНастройки);
	Иначе
		Если ВложенияТранспортногоСообщения.Количество() > 0 Тогда
			ПоказатьВложениеТранспортногоСообщения(ВложенияТранспортногоСообщения, 0, Протокол, ЦиклОбмена, СоответствиеТиповСодержимого, Ложь);
		Иначе
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось расшифровать протокол'"));
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры 

&НаКлиенте
Процедура ПоказатьВложениеТранспортногоСообщения(ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена = Истина);
	
	Если НомерОбъекта + 1 <= ВложенияТранспортногоСообщения.Количество() Тогда
		
		Вложение = ВложенияТранспортногоСообщения[НомерОбъекта];
		
		Если Вложение.ВАрхиве Тогда
			ПоказатьУведомлениеАрхивныхФайлов(, 9, 3, Истина);
			Возврат;
		КонецЕсли;
		
		ТипДокумента 				= СоответствиеТиповСодержимого.Получить(Вложение.Тип);
		ТипТранспортногоСообщения 	= ПолучитьРеквизитНаСервере(Протокол, "Тип");
		
		Если ТипДокумента = "ФайлОтчетностиПФР" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ПодтверждениеПолученияОтчетностиПФР" Тогда
			ПоказатьПодтверждениеПолученияПФР(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "СведенияПриложениеПФР" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьПротоколПриложениеПФР(Протокол, Вложение.ИмяФайла, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ПротоколПФР" Тогда
			ПоказатьПротоколПФР(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "ПротоколПриложениеПФР" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьПротоколПриложениеПФР(Протокол, Вложение.ИмяФайла, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ФайлОписанияСведенийПФР" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ЭлектронноЦифроваяПодпись" Тогда
			// пропускаем
			
			// неформализованные ПФР
		ИначеЕсли ТипДокумента = "Письмо" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ПисьмоПриложение" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьПротоколПриложениеПФР(Протокол, Вложение.ИмяФайла, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ПодтверждениеПолученияПисьма" Тогда
			ПоказатьПодтверждениеПолученияПисьмаПФР(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла);
		ИначеЕсли ТипДокумента = "ОписаниеПисьма" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ТранспортнаяИнформация" Тогда
			// пропускаем
			
			// запросы ПФР
		ИначеЕсли ТипДокумента = "ЗапросИОС" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ФайлОписанияЗапросаПФР" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ОтветНаЗапросПФР" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			Предмет = ПолучитьРеквизитНаСервере(ПоследнийЦиклОбмена, "Предмет");
			ПоказатьОтветНаЗапросИОС(Предмет, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ОтветПриложениеНаЗапросПФР" Тогда
			// Если есть приложение, то оно откроется в ветке ОтветНаЗапросПФР
		ИначеЕсли ТипДокумента = "ПодтверждениеПолученияЗапросПФР" Тогда
			ПоказатьПодтверждениеПолученияЗапросаПФР(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла);
			// отчетность ФНС
		ИначеЕсли ТипДокумента = "ФайлОтчетности" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "Форма2НДФЛ" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "Доверенность" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ОписаниеСведений" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ОписаниеСведений2НДФЛ" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ПодтверждениеДатыОтправки" Тогда
			ПоказатьПодтверждениеДатыОтправки(Протокол, ПоследнийЦиклОбмена, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказе" Тогда
			ПоказатьУведомлениеОбОтказе(ПоследнийЦиклОбмена, Протокол, ТипТранспортногоСообщения, Вложение.ИмяФайла);
		ИначеЕсли ТипДокумента = "КвитанцияОПриеме" Тогда
			ПоказатьКвитанциюОПриеме(ПоследнийЦиклОбмена, Протокол, ТипТранспортногоСообщения, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "ИзвещениеОВводе" Тогда
			ПоказатьИзвещениеОВводе(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОбУточнении" Тогда
			ПоказатьУведомлениеОбУточнении504(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "ИзвещениеОПолучении" Тогда
			ПоказатьИзвещениеОПолучении(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
			
			//Заявление
		ИначеЕсли ТипДокумента = "КвитанцияОПриемеЗаявления" Тогда
			ПоказатьКвитанцияОПриемеЗаявление(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "СообщениеОПростОтметки" Тогда
			ПоказатьСообщениеОПроставленииОтметки(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказеОтметки" Тогда
			ПоказатьУведомлениеОбОтказеОтметки(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "СообщениеОПроверке" Тогда
			ПоказатьСообщениеОПроверке(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "СообщениеОбОтзывеЗаявления" Тогда
			ПоказатьСообщениеОбОтзывеЗаявления(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "СообщениеОНесоответствиях" Тогда
			ПоказатьСообщениеОНесоответствиях(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "Заявление" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ОписаниеСведенийЗаявление" Тогда
			// пропускаем
			
			//Документ
		ИначеЕсли ТипДокумента = "Документ" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ОписаниеДокумент" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказеДокумент" Тогда
			ПоказатьУведомлениеОбОтказеДокумент(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "КвитанцияОПриемеДокумент" Тогда
			ПоказатьКвитанцияОПриемеДокумент(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла);
			
			// 2-НДФЛ
		ИначеЕсли ТипДокумента = "Протокол2НДФЛ" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "Реестр2НДФЛ" Тогда
			// пропускаем
			
			// обращение
		ИначеЕсли ТипДокумента = "Обращение" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "Приложение" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьПротоколПриложениеПФР(Протокол, Вложение.ИмяФайла, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ПодтверждениеДатыПолучения" Тогда
			ПоказатьПодтверждениеДатыПолучения(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "ОписаниеСведенийОбращениеПисьмоРассылка" Тогда
			// пропускаем
			
			// представление
		ИначеЕсли ТипДокумента = "Представление" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ДоверенностьПредставление" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ОписаниеПредставление" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ПодтверждениеДатыОтправкиПредставление" Тогда
			ПоказатьПодтверждениеДатыОтправки(Протокол, ПоследнийЦиклОбмена, ПечатьВозможнаДляЦиклаОбмена);
			
			// входящие неформализованные
		ИначеЕсли ТипДокумента = "Рассылка" Тогда
			// пропускаем
			
			// запросы
		ИначеЕсли ТипДокумента = "Запрос" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "Ответ" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьПротоколПриложениеПФР(Протокол, Вложение.ИмяФайла, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ОписаниеСведенийЗапрос" Тогда
			// пропускаем
			
			// отчетность в Росстат
		ИначеЕсли ТипДокумента = "ФайлОтчетностиФСГС" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ИзвещениеОПолученииДокументаФСГС" Тогда
			ПоказатьПодтверждениеПолученияФСГС(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла);
		ИначеЕсли ТипДокумента = "ПодтверждениеОператораФСГС" Тогда
			ПоказатьПодтверждениеДатыПолучения(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОПриемеВОбработкуОтчетаФСГС" Тогда
			ПоказатьПротоколФСГС(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОбУточненииОтчетаФСГС" Тогда
			ПоказатьПротоколФСГС(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОНесоответствииФорматуОтчетаФСГС" Тогда
			ПоказатьПротоколФСГС(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОбОтказеОтчетаФСГС" Тогда
			ПоказатьПротоколФСГС(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "УведомлениеОбОтклоненииОтчетаФСГС" Тогда
			ПоказатьПротоколФСГС(ПоследнийЦиклОбмена, Протокол, Вложение.ИмяФайла, ПечатьВозможнаДляЦиклаОбмена);
		ИначеЕсли ТипДокумента = "ОписаниеФайлаОтчетностиФСГС" Тогда
			// пропускаем
			
			// переписка с Росстатом
		ИначеЕсли ТипДокумента = "ПисьмоФСГС" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "РассылкаФСГС" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "РассылкаШаблоновФСГС" Тогда
			// пропускаем
		ИначеЕсли ТипДокумента = "ПриложениеПисьмаФСГС" Тогда
			ДополнительныеПараметры = Новый Структура("ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена", ВложенияТранспортногоСообщения, НомерОбъекта, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьВложениеТранспортногоСообщенияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьПротоколВложениеФСГС(Протокол, Вложение.ИмяФайла, ОписаниеОповещения);
			Возврат;
		ИначеЕсли ТипДокумента = "ОписаниеПисьмаФСГС" Тогда
			// пропускаем
			
		КонецЕсли;
		
		ПоказатьВложениеТранспортногоСообщения(ВложенияТранспортногоСообщения, НомерОбъекта + 1, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПоказатьВложениеТранспортногоСообщенияЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВложенияТранспортногоСообщения = ДополнительныеПараметры.ВложенияТранспортногоСообщения;
	НомерОбъекта = ДополнительныеПараметры.НомерОбъекта;
	Протокол = ДополнительныеПараметры.Протокол;
	ПоследнийЦиклОбмена = ДополнительныеПараметры.ПоследнийЦиклОбмена;
	СоответствиеТиповСодержимого = ДополнительныеПараметры.СоответствиеТиповСодержимого;
	ПечатьВозможнаДляЦиклаОбмена = ДополнительныеПараметры.ПечатьВозможнаДляЦиклаОбмена;
	
	ПоказатьВложениеТранспортногоСообщения(ВложенияТранспортногоСообщения, НомерОбъекта + 1, Протокол, ПоследнийЦиклОбмена, СоответствиеТиповСодержимого, ПечатьВозможнаДляЦиклаОбмена);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОрганизацияОтчетаДляОнлайнПроверки(Документ)
	
	Если ТипЗнч(Документ) = Тип("Массив") Тогда
		ОсновнойОтчет = Документ[0];
	Иначе
		ОсновнойОтчет = Документ;
	КонецЕсли;
	
	Возврат ДокументооборотСКОВызовСервера.ОрганизацияПоОбъектуОтправки(ОсновнойОтчет);
	
КонецФункции

#Область ЗагрузкаЭлектронныхПредставлений

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаНачало(Адрес, ФайлыДляИмпорта = "", ДополнительныеПараметры = Неопределено) Экспорт
	
	Если ДополнительныеПараметры = Неопределено Тогда
		ДополнительныеПараметры = Новый Структура();
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("ФайлыДляИмпорта", ФайлыДляИмпорта);
	ДополнительныеПараметры.Вставить("Адрес", 			Адрес);
		
	ОписаниеОповещения = Новый ОписаниеОповещения("ПолучениеФайловДляИмпортаПослеПолученияКонтекста", ЭтотОбъект, ДополнительныеПараметры);
	ДокументооборотСКОКлиент.ПолучитьКонтекстЭДО(ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаПослеПолученияКонтекста(Результат, ДополнительныеПараметры) Экспорт

	КонтекстЭДОКлиент = Результат.КонтекстЭДО;
	
	ДополнительныеПараметры.Вставить("КонтекстЭДОКлиент", КонтекстЭДОКлиент);
	ОписаниеОповещения = Новый ОписаниеОповещения("ПолучениеФайловДляИмпортаПоддерживаетсяРасширениеРаботыСФайламиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	ТекстСообщения = ОперацииСФайламиЭДКОКлиент.ТекстСообщенияДляНеобязательнойУстановкиРасширенияРаботыСФайлами();
	ФайловаяСистемаКлиент.ПодключитьРасширениеДляРаботыСФайлами(ОписаниеОповещения, ТекстСообщения, Истина); 
	
КонецПроцедуры

&НаКлиенте
Функция ПолучитьПодготовленныйДиалогВыбораФайла()
	
	ДиалогВыбора = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	ДиалогВыбора.Заголовок = НСтр("ru = 'Выберите файл(ы) для импорта'");
	ДиалогВыбора.МножественныйВыбор = Истина;
	ДиалогВыбора.ПроверятьСуществованиеФайла = Истина;
	ДиалогВыбора.Фильтр = "Все файлы (*.*)|*.*";
	
	Возврат ДиалогВыбора;
	
КонецФункции

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаПоддерживаетсяРасширениеРаботыСФайламиЗавершение(ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами, ДополнительныеПараметры) Экспорт
	
	ДополнительныеПараметры.Вставить("ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами", ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами);
	
	ФайлыБылиВыбраны = Ложь;
	Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
		
		Если НЕ ДополнительныеПараметры.Свойство("ФайлыДляИмпорта") 
			ИЛИ НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ФайлыДляИмпорта) Тогда
			
			ДиалогВыбора = ПолучитьПодготовленныйДиалогВыбораФайла();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПолучениеФайловДляИмпортаПослеВыбораФайлов", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДиалогВыбора.Показать(ОписаниеОповещения);
			
		Иначе
			ФайлыБылиВыбраны = Истина;
			ПолучениеФайловДляИмпортаПослеВыбораФайлов(ДополнительныеПараметры.ФайлыДляИмпорта, ДополнительныеПараметры)
		КонецЕсли;
		
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения("ПолучениеФайловДляИмпортаПослеПомещенияФайла", ЭтотОбъект, ДополнительныеПараметры);		
		НачатьПомещениеФайла(ОписаниеОповещения,,, Истина, ДополнительныеПараметры.Адрес);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаПослеПомещенияФайла(ФайлыБылиВыбраны, АдресДанных, ВыбранноеИмяФайла, ДополнительныеПараметры) Экспорт
	
	ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами = ДополнительныеПараметры.ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами;
	ФайлыДляИмпорта = ДополнительныеПараметры.ФайлыДляИмпорта;
	
	Если ФайлыБылиВыбраны Тогда
		
		ОсновнойФайл = Неопределено;
		
		Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
			Если Не ЗначениеЗаполнено(ФайлыДляИмпорта) Тогда
				ВыбранныеФайлы = ДополнительныеПараметры.ВыбранныеФайлы;
			Иначе
				ВыбранныеФайлы = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ФайлыДляИмпорта, Символы.ПС);
			КонецЕсли;
			Если ВыбранныеФайлы.Количество() > 0 Тогда
				ОсновнойФайл = ВыбранныеФайлы[0];
			КонецЕсли;
		Иначе
			ВыбранныеФайлы = Новый Массив;
			ВыбранныеФайлы.Добавить(ВыбранноеИмяФайла);
			ОсновнойФайл = ВыбранноеИмяФайла;
		КонецЕсли;
		
		Если ОсновнойФайл = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
		ДополнительныеПараметры.Вставить("ВыбранныеФайлы", 	ВыбранныеФайлы);
		ДополнительныеПараметры.Вставить("ОсновнойФайл", 	ОсновнойФайл);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПолучениеФайловДляИмпортаПослеОпределенияСвойствОсновногоФайла", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
			ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, ОсновнойФайл);
		Иначе
			СвойстваФайла = СвойстваФайла(АдресДанных, ВыбранноеИмяФайла);
			Результат = Новый Структура();
			Результат.Вставить("Выполнено", 	Истина);
			Результат.Вставить("СвойстваФайла", СвойстваФайла);
			
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаЗавершение(ФайлПолучен, ВходящийКонтекст) Экспорт
	
	Если ФайлПолучен Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ФайлыДляИмпорта", 									ВходящийКонтекст.ФайлыДляИмпорта);
		ДополнительныеПараметры.Вставить("ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках", 	ВходящийКонтекст.ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках);
		ДополнительныеПараметры.Вставить("ВыполнитьРазделениеФайла", 							ВходящийКонтекст.ВыполнитьРазделениеФайла);
		ДополнительныеПараметры.Вставить("ВыполнятьПроверкуКомплектаУведомлений", 				ВходящийКонтекст.ВыполнятьПроверкуКомплектаУведомлений);

		Если ВходящийКонтекст.Свойство("ВыполняемоеОповещение") Тогда
			
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, ДополнительныеПараметры);
			
		Иначе
			// Открыть форму электронного представления
			ОткрытьФорму("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.Форма.ФормаЭлемента", ДополнительныеПараметры);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОнлайнПроверка

&НаКлиенте
Процедура ПредупредитьОбОграничениях(ЭтоОтчет, Наименование, Знач ТекстПредупреждения)
	
	// Переводим первый символ в нижний регистр
	ТекстПредупреждения = НРег(Лев(ТекстПредупреждения, 1)) + Сред(ТекстПредупреждения, 2);
	
	ТекстПредупреждения = НСтр("ru = 'Для %1 ""%2"" '") + ТекстПредупреждения;
	
	ВидОбъекта = "";
	Если ЭтоОтчет Тогда
		ВидОбъекта = НСтр("ru = 'отчета'");
	Иначе
		ВидОбъекта = НСтр("ru = 'уведомления'"); 
	КонецЕсли;
	
	ТекстПредупреждения = СтрЗаменить(ТекстПредупреждения, "%1", ВидОбъекта);
	ТекстПредупреждения = СтрЗаменить(ТекстПредупреждения, "%2", Наименование);
	
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки() Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачномДействии(ТекстПредупреждения);
	Иначе
		ПоказатьПредупреждение(,ТекстПредупреждения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьПараметрыДляОнлайнПроверки(ВидКонтролирующегоОргана, ПроверяемыйОбъект)
	
	КонтролирующийОрган = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ИмяПеречисления(ВидКонтролирующегоОргана);
	НеОтправляетсяВКонтролирующийОрган = Не ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбъектОтправляетсяВКонтролирующиеОрганы(ПроверяемыйОбъект);

	Возврат Новый Структура("КонтролирующийОрган, НеОтправляетсяВКонтролирующийОрган", КонтролирующийОрган, НеОтправляетсяВКонтролирующийОрган);
	
КонецФункции

&НаКлиенте
Процедура ПроверитьВИнтернетеНачало_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ПроверитьВИнтернетеНачало(
		ВходящийКонтекст.ПроверяемыйОбъект, 
		ВходящийКонтекст.Представление, 
		ВходящийКонтекст.ЭтоОтчет, 
		ВходящийКонтекст.Организация, 
		ВходящийКонтекст.ВидКонтролирующегоОргана, 
		ВходящийКонтекст.АдресГотовыхДанных, 
		ВходящийКонтекст.ИмяФайлаГотовыхДанных);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьВИнтернетеНачало(
		ПроверяемыйОбъект, 
		Представление, 
		ЭтоОтчет, 
		Организация, 
		ВидКонтролирующегоОргана, 
		АдресГотовыхДанных = Неопределено, 
		ИмяФайлаГотовыхДанных = Неопределено) Экспорт
	
	// Переопределяем проверку в Интернете
	ВыполнитьСтандартнуюПроверкуВИнтернете = Истина;
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПроверитьВИнтернетеПереопределяемый(ПроверяемыйОбъект, ВыполнитьСтандартнуюПроверкуВИнтернете);
	
	// Если проверка была выполнена в переопределяемой процедуре, то стандартную проверку не выполняем
	Если ВыполнитьСтандартнуюПроверкуВИнтернете Тогда
		
		ПараметрыДляОнлайнПроверки = ПолучитьПараметрыДляОнлайнПроверки(ВидКонтролирующегоОргана, ПроверяемыйОбъект);
		
		Если ПараметрыДляОнлайнПроверки.НеОтправляетсяВКонтролирующийОрган Тогда
			
			ПредупредитьОбОграничениях(ЭтоОтчет, Представление, НСтр("ru = 'Проверка в интернете не предусмотрена'"));
			
		ИначеЕсли НЕ ЗначениеЗаполнено(УчетнаяЗаписьОрганизации(Организация)) Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ПроверяемыйОбъект", 		 ПроверяемыйОбъект);
			ДополнительныеПараметры.Вставить("Представление", 			 Представление);
			ДополнительныеПараметры.Вставить("ЭтоОтчет", 				 ЭтоОтчет);
			ДополнительныеПараметры.Вставить("Организация", 			 Организация);
			ДополнительныеПараметры.Вставить("ВидКонтролирующегоОргана", ВидКонтролирующегоОргана);
			ДополнительныеПараметры.Вставить("АдресГотовыхДанных", 		 АдресГотовыхДанных);
			ДополнительныеПараметры.Вставить("ИмяФайлаГотовыхДанных", 	 ИмяФайлаГотовыхДанных);
			  
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПроверитьВИнтернетеНачало_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ПроверяемыйОбъект, ОписаниеОповещения);
			
		ИначеЕсли НЕ ОнлайнПроверкаДоступна(Организация, ПараметрыДляОнлайнПроверки.КонтролирующийОрган) Тогда
			ПредупредитьОбОграничениях(ЭтоОтчет, Представление, НСтр("ru = 'Проверка в интернете не предусмотрена'"));
		Иначе
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("КонтролирующийОрган",		ПараметрыДляОнлайнПроверки.КонтролирующийОрган);
			ДополнительныеПараметры.Вставить("ПроверяемыйОбъект", 		ПроверяемыйОбъект);
			ДополнительныеПараметры.Вставить("АдресГотовыхДанных", 		АдресГотовыхДанных);
			ДополнительныеПараметры.Вставить("ИмяФайлаГотовыхДанных", 	ИмяФайлаГотовыхДанных);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПроверитьВИнтернетеЗавершение", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			Если ТипЗнч(ПроверяемыйОбъект) = Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
				
				РегламентированнаяОтчетностьКлиент.ПередВыгрузкойРегламентированногоОтчета(ОписаниеОповещения, ПроверяемыйОбъект);
				Возврат;
				
			Иначе
				
				ВыполнитьОбработкуОповещения(ОписаниеОповещения, Ложь);
				Возврат;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьВИнтернетеЗавершение(Отказ, ДополнительныеПараметры) Экспорт
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	АдресГотовыхДанных = Неопределено;
	ИмяФайлаГотовыхДанных = Неопределено;
	Если ДополнительныеПараметры.Свойство("АдресГотовыхДанных") Тогда
		АдресГотовыхДанных = ДополнительныеПараметры.АдресГотовыхДанных;
		ИмяФайлаГотовыхДанных = ДополнительныеПараметры.ИмяФайлаГотовыхДанных;
	КонецЕсли;
		
	РегламентированнаяОтчетностьКлиент.ПроверитьВИнтернетеПоСсылке(ДополнительныеПараметры.ПроверяемыйОбъект, ДополнительныеПараметры.КонтролирующийОрган, АдресГотовыхДанных, ИмяФайлаГотовыхДанных);
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция ОнлайнПроверкаДоступна(Организация, КонтролирующийОрган)
	
	Если КонтролирующийОрган = "ФСС" Тогда
		ПараметрыПрорисовкиКнопокОтправки = ДокументооборотСФССКлиентСервер.ПараметрыПрорисовкиКнопокОтправки(Организация);
	ИначеЕсли КонтролирующийОрган = "ФСРАР" Тогда
		ПараметрыПрорисовкиКнопокОтправки = ДокументооборотСФСРАРКлиентСервер.ПараметрыПрорисовкиКнопокОтправки(Организация);
	ИначеЕсли КонтролирующийОрган = "РПН" Тогда
		ПараметрыПрорисовкиКнопокОтправки = ДокументооборотСРПНКлиентСервер.ПараметрыПрорисовкиКнопокОтправки(Организация);
	ИначеЕсли КонтролирующийОрган = "ФТС" Тогда
		ПараметрыПрорисовкиКнопокОтправки = ДокументооборотСФТСКлиентСервер.ПараметрыПрорисовкиКнопокОтправки(Организация);
	Иначе
		ПараметрыПрорисовкиКнопокОтправки = ДокументооборотСКОВызовСервера.ПараметрыПрорисовкиКнопокОтправки(Организация, КонтролирующийОрган);
	КонецЕсли;
	
	Если ПараметрыПрорисовкиКнопокОтправки.Свойство("ПроверитьВИнтернете") Тогда
		Возврат ПараметрыПрорисовкиКнопокОтправки.ПроверитьВИнтернете;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

&НаСервереБезКонтекста
Функция КнопкаОбновленияВЗаявленииНаПодключениеДоступна(Ссылка) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.КнопкаОбновленияВЗаявленииНаПодключениеДоступна(Ссылка);
	
КонецФункции

&НаКлиенте
Функция ОбновлениеМодуляДокументооборотаНаИсправительныйДоступно(ВерсияМодуляДокументооборотаСИсправлениемОшибки, СвойстваМодуляДляТекущегоРелиза = Неопределено) Экспорт
	
	// Если не известен номер модуля с исправлением, то не понятно, на что нужно обновлять
	Если НЕ ЗначениеЗаполнено(ВерсияМодуляДокументооборотаСИсправлениемОшибки) Тогда 
		Возврат Ложь;
	КонецЕсли;
	
	КаталогФайловНаСервере = Неопределено;

	ПараметрыСервераИнтернета = ПолучитьПараметрыИнтернета();
	
	// Если в Интернете нет более свежей версии, чем сейчас в базе, то принудительно обновлять модуль не имеет смысла
	Если ПолучитьСохранитьОбновление(ПараметрыСервераИнтернета, СвойстваМодуляДляТекущегоРелиза) = Ложь Тогда
		Возврат Ложь;
	КонецЕсли;
	
	СвойстваМодуляДляТекущегоРелиза.Дата = ДатаПоСтроке(СвойстваМодуляДляТекущегоРелиза.Дата);
	
	ВерсияТекущегоМодуляВИнтернете = ЧислоИзСтроки(СвойстваМодуляДляТекущегоРелиза.Версия);
	Если ВерсияТекущегоМодуляВИнтернете = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ВерсияМодуляСИсправлениемОшибки = ЧислоИзСтроки(ВерсияМодуляДокументооборотаСИсправлениемОшибки);
	Если ВерсияМодуляСИсправлениемОшибки = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;	
	
	// Если модуль, выложенный в интернете не содержит исправление ошибки, из-за которой возникала блокировка отправки отчета, 
	// то принудительно обновлять модуль не имеет смысла
	Если ВерсияТекущегоМодуляВИнтернете < ВерсияМодуляСИсправлениемОшибки Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// Можно обновлять модуль если
	// 1. В Интернете выложен модуль более свежий чем в базе
	// 2. Этот модуль содержит исправление ошибки, из-за которой возникала блокировка отправки отчета
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Функция ЧислоИзСтроки(Знач Текст)
	
	Текст = СтрЗаменить(Текст, Символы.НПП, "");
	
	Попытка
		Число = Число(Текст);
	Исключение
		Число = Неопределено;
	КонецПопытки;
	
	Возврат Число;

КонецФункции

&НаКлиенте
Процедура СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателя(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки.УчетнаяЗапись);
	
	Если ТипЗнч(КонтекстОтправки.СертификатПолучателя) <> Тип("Массив") Тогда
		Отпечатки = Новый Массив;
		Отпечатки.Добавить(КонтекстОтправки.СертификатПолучателя);
	Иначе
		Отпечатки = КонтекстОтправки.СертификатПолучателя;
	КонецЕсли;
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("Отпечатки", Отпечатки);
	Контекст.Вставить("МестоХраненияКлюча", МестоХраненияКлюча);
	
	Оповещение = Новый ОписаниеОповещения("СформироватьПакетПФР_ПослеУстановкиСертификатаЕЦП", ЭтотОбъект, Контекст);
	Если КонтекстОтправки.ЭтоЕЦП И КонтекстОтправки.СертификатШифрованияЕЦП <> Неопределено Тогда
		УстанавливаемыеСертификаты = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(КонтекстОтправки.СертификатШифрованияЕЦП);
		УстановитьСертификаты(
			Оповещение,
			УстанавливаемыеСертификаты,
			МестоХраненияКлюча);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(Выполнено, НайденныеСертификаты, НенайденныеСертификаты, СсылкаПолучатель)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Выполнено, НайденныеСертификаты, НенайденныеСертификаты, СсылкаПолучатель);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	НайденныеСертификаты   = ?(Результат.Свойство("Сертификаты"), Результат.Сертификаты, Неопределено);
	НенайденныеСертификаты = ?(Результат.Свойство("НенайденныеСертификаты"), Результат.НенайденныеСертификаты, Неопределено);
	СсылкаПолучатель       = ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель;
	
	Если Не СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(Результат.Выполнено, НайденныеСертификаты, НенайденныеСертификаты, СсылкаПолучатель) Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить("ОбъектСертификатПолучателя", НайденныеСертификаты);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетПФР_ПодготовитьСертификатыДляПоиска(Знач УчетнаяЗапись, Знач Сертификат, Знач СертификатОтветственногоЗаШифрование)

 	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
 	Возврат КонтекстЭДОСервер.СформироватьПакетПФР_ПодготовитьСертификатыДляПоиска(
 		УчетнаяЗапись,
 		Сертификат,
 		СертификатОтветственногоЗаШифрование);

КонецФункции

&НаКлиенте
Процедура СформироватьПакетПФР_ПроверкиСертификатов(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если КонтекстОтправки.Свойство("ОбъектСертификатОтветственногоЗаШифрование") Тогда // возможна ситуация, когда сертификаты уже получены и проверены
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Истина);
		Возврат;
	КонецЕсли;
	
	СертификатыДляПоиска = СформироватьПакетПФР_ПодготовитьСертификатыДляПоиска(
		КонтекстОтправки.УчетнаяЗапись,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если СертификатыДляПоиска = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
			
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("ВыполнятьПроверку", Истина);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	ОбъектыСертификатов = СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Результат.Сертификаты,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
		
	Если ОбъектыСертификатов = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
		
	///////////////////////////////////////////////////////////////////////////////
	КонтекстОтправки = ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки;
	Для каждого ОбъектСертификат Из ОбъектыСертификатов Цикл
		КонтекстОтправки.Вставить(ОбъектСертификат.Ключ, ОбъектСертификат.Значение);
	КонецЦикла;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(Знач Сертификаты, Знач СертификатРуководителя, Знач СертификатОтветственногоЗаШифрование)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетПФР_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Сертификаты, СертификатРуководителя, СертификатОтветственногоЗаШифрование);

КонецФункции

&НаКлиенте
Процедура СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура СформироватьПакет_ОтработатьОтложенныеКоманды(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстМоделиХраненияКлюча(КонтекстОтправки.УчетнаяЗапись, КонтекстОтправки);
	
	СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиентеПоследовательно(ОповещениеОЗавершении, КонтекстОтправки, КонтекстОтправки.ОтложенныеКоманды, 0);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиентеПоследовательно(ОповещениеОЗавершении, КонтекстОтправки, ОтложенныеКоманды, ТекущаяКоманда)

	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура СформироватьПакет_ОтработатьОтложенныеКомандыПоследовательно(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	Если ТекущаяКоманда < ОтложенныеКоманды.Количество() Тогда
		Контекст = Новый Структура;
		Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
		Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		Контекст.Вставить("ОтложенныеКоманды", ОтложенныеКоманды);
		Контекст.Вставить("ТекущаяКоманда", ТекущаяКоманда);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиентеПоследовательноПослеВыполненияКоманды", ЭтотОбъект, Контекст);
		Если ОтложенныеКоманды[ТекущаяКоманда].Представление = "Зашифровать" Тогда
			ПараметрыОперации = Неопределено;
			Если КонтекстОтправки.Свойство("АлгоритмШифрования") И ЗначениеЗаполнено(КонтекстОтправки.АлгоритмШифрования) Тогда
				ПараметрыОперации = Новый Структура;
				ПараметрыОперации.Вставить("АлгоритмШифрования", КонтекстОтправки.АлгоритмШифрования);
				ПараметрыОперации.Вставить("АлгоритмШифрованияОбязателен",
					КонтекстОтправки.Свойство("АлгоритмШифрованияОбязателен") И КонтекстОтправки.АлгоритмШифрованияОбязателен);
				ПараметрыОперации.Вставить("СобиратьСведенияОШифровании",
					КонтекстОтправки.Свойство("СобиратьСведенияОШифровании") И КонтекстОтправки.СобиратьСведенияОШифровании);
			КонецЕсли;
			
			ОбъектСертификат = КонтекстОтправки.ОбъектСертификатПолучателя;
			
			Если КонтекстОтправки.Свойство("ОбъектСертификатСервераДокументооборота")
				И ЗначениеЗаполнено(КонтекстОтправки.ОбъектСертификатСервераДокументооборота)
				И КонтекстОтправки.Свойство("СвойстваОтложенныхКоманд") Тогда
				
				ИдентификаторОтложеннойКоманды = ОтложенныеКоманды[ТекущаяКоманда].ПолучитьИдентификатор();
				СвойстваОтложеннойКоманды = КонтекстОтправки.СвойстваОтложенныхКоманд[ИдентификаторОтложеннойКоманды];
				Если СвойстваОтложеннойКоманды <> Неопределено И СвойстваОтложеннойКоманды.ДляСервераДокументооборота Тогда
					СформироватьПакет_ЗашифроватьНаСертификатахПолучателей(
						Оповещение,
						КонтекстОтправки.ОбъектСертификатПолучателя,
						КонтекстОтправки.ОбъектСертификатСервераДокументооборота,
						ОтложенныеКоманды,
						ТекущаяКоманда,
						МестоХраненияКлюча,
						ПараметрыОперации);
					Возврат;
					
				ИначеЕсли СвойстваОтложеннойКоманды <> Неопределено И ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(
					СвойстваОтложеннойКоманды, "ТолькоДляСервераДокументооборота", Истина) Тогда
					
					ОбъектСертификат = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
						КонтекстОтправки.ОбъектСертификатСервераДокументооборота);
				КонецЕсли;
			КонецЕсли;
			
			КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
				Оповещение,
				ОтложенныеКоманды[ТекущаяКоманда].Значение,
				ОбъектСертификат,,,,
				МестоХраненияКлюча,,
				ПараметрыОперации);
			
		ИначеЕсли ОтложенныеКоманды[ТекущаяКоманда].Представление = "ПодписатьФайлОписанияПакета" Тогда
			Если КонтекстОтправки.Свойство("ФорматДокументооборота") И КонтекстОтправки.ФорматДокументооборота =
				ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
				
				НастройкиПодписи = Новый Структура;
				НастройкиПодписи.Вставить("ДополнитьПространстваИменУзлаПодписи", 	Истина);
				НастройкиПодписи.Вставить("ПодстрокаУзлаДляИнформацииОПодписи", 	"ЭДСФР");
				НастройкиПодписи.Вставить("ИмяСобытияДляИнформацииОПодписи", 		"ЕЦП добавлять SignInfo");
				
				Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
					ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ЗаявлениеВПФР", КонтекстОтправки.УчетнаяЗапись); 
					ПодготовитьОблачнуюПодпись("Сведения ПФР", "xml", ТребуетПодтверждениеОперации, НастройкиПодписи);
				КонецЕсли;
				
				ПодписатьФайлXMLDSig(
					КонтекстОтправки.ОбъектСертификатРуководителя,
					ОтложенныеКоманды[ТекущаяКоманда].Значение,
					Оповещение,,,
					НастройкиПодписи);
				
			Иначе
				ДанныеДляПодписи = ОтложенныеКоманды[ТекущаяКоманда].Значение;
				ПараметрыОперации = Неопределено;
				Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
					ТребуетПодтверждениеОперации = СвойствоСтруктуры(КонтекстОтправки, "ТребуетПодтверждениеОперации", Неопределено);
					ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи(КонтекстОтправки.ТипСообщения, КонтекстОтправки.УчетнаяЗапись, КонтекстОтправки, ТребуетПодтверждениеОперации);
					Если ЭтоАдресВременногоХранилища(ДанныеДляПодписи) Тогда
						ПараметрыОперации = ПодготовитьОблачнуюПодпись(Неопределено, , ТребуетПодтверждениеОперации);
					ИначеЕсли ТипЗнч(ДанныеДляПодписи) = Тип("Строка") Тогда
						ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеДляПодписи);
						ПараметрыОперации = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
					Иначе
						ПараметрыОперации = ПодготовитьОблачнуюПодпись(Неопределено, , ТребуетПодтверждениеОперации);
					КонецЕсли;
					ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, КонтекстОтправки.ОбъектСертификатРуководителя);
				КонецЕсли;
				
				КриптографияЭДКОКлиент.ПодписатьPKCS7(
					Оповещение, КонтекстОтправки.ОбъектСертификатРуководителя, ДанныеДляПодписи, Ложь,
					ПоместитьВоВременноеХранилище(Неопределено, КонтекстОтправки.УникальныйИдентификатор), , , , ПараметрыОперации);
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		ВыполнитьОбработкуОповещения(
			ОповещениеОЗавершении, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакет_ЗашифроватьНаСертификатахПолучателей(
		ОповещениеОЗавершении,
		ОбъектСертификатПолучателя,
		ОбъектСертификатСервераДокументооборота,
		ОтложенныеКоманды,
		ТекущаяКоманда,
		МодельРаботыСКлючами = Неопределено,
		ПараметрыОперации = Неопределено)
	
	АдресКопии = СкопироватьВоВременномХранилище(ОтложенныеКоманды[ТекущаяКоманда].Значение, Новый УникальныйИдентификатор);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 					ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("ОбъектСертификатСервераДокументооборота", ОбъектСертификатСервераДокументооборота);
	ДополнительныеПараметры.Вставить("ОтложенныеКоманды", 						ОтложенныеКоманды);
	ДополнительныеПараметры.Вставить("ТекущаяКоманда", 							ТекущаяКоманда);
	ДополнительныеПараметры.Вставить("АдресКопии", 								АдресКопии);
	ДополнительныеПараметры.Вставить("МодельРаботыСКлючами", 					МодельРаботыСКлючами);
	ДополнительныеПараметры.Вставить("ПараметрыОперации", 						ПараметрыОперации);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакет_ЗашифроватьНаСертификатахПолучателейПослеЗашифрования", ЭтотОбъект, ДополнительныеПараметры);
	
	КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
		Оповещение,
		ОтложенныеКоманды[ТекущаяКоманда].Значение,
		ОбъектСертификатПолучателя,,,,
		МодельРаботыСКлючами,,
		ДополнительныеПараметры.ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакет_ЗашифроватьНаСертификатахПолучателейПослеЗашифрования(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОЗавершении 					= ДополнительныеПараметры.ОповещениеОЗавершении;
	ОбъектСертификатСервераДокументооборота = ДополнительныеПараметры.ОбъектСертификатСервераДокументооборота;
	АдресКопии 								= ДополнительныеПараметры.АдресКопии;
	МодельРаботыСКлючами					= ДополнительныеПараметры.МодельРаботыСКлючами;
	
	Если Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакет_ЗашифроватьНаСертификатахПолучателейПослеЗашифрованияКопии", ЭтотОбъект, ДополнительныеПараметры);
		
		МассивОбъектовСертификатСервераДокументооборота = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			ОбъектСертификатСервераДокументооборота);
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
			Оповещение,
			АдресКопии,
			МассивОбъектовСертификатСервераДокументооборота,,,,
			МодельРаботыСКлючами,,
			ДополнительныеПараметры.ПараметрыОперации);
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, Результат.ОписаниеОшибки);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакет_ЗашифроватьНаСертификатахПолучателейПослеЗашифрованияКопии(Результат, ДополнительныеПараметры) Экспорт
	
	ОповещениеОЗавершении 	= ДополнительныеПараметры.ОповещениеОЗавершении;
	ОтложенныеКоманды 		= ДополнительныеПараметры.ОтложенныеКоманды;
	ТекущаяКоманда 			= ДополнительныеПараметры.ТекущаяКоманда;
	АдресКопии 				= ДополнительныеПараметры.АдресКопии;
	
	Если Результат.Выполнено Тогда
		СкопироватьВоВременномХранилище(
			ОтложенныеКоманды[ТекущаяКоманда].Значение,
			ОтложенныеКоманды[ТекущаяКоманда].Значение,
			"FD2WshXWT3yZW18vlRZXnA==",
			АдресКопии);
		
		РезультатВыполнения = Новый Структура("Выполнено, ЗашифрованныйФайл", Истина, ОтложенныеКоманды[ТекущаяКоманда].Значение);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатВыполнения);
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, Результат.ОписаниеОшибки);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СкопироватьВоВременномХранилище(
		АдресДанных,
		АдресРезультата = Неопределено,
		Разделитель64 = Неопределено,
		АдресДобавляемыхДанных = Неопределено)
	
	Данные = ПолучитьИзВременногоХранилища(АдресДанных);
	Если НЕ ЗначениеЗаполнено(АдресДобавляемыхДанных) Тогда
		Возврат ПоместитьВоВременноеХранилище(Данные, АдресРезультата);
	КонецЕсли;
	
	МассивИменФайлов = Новый Массив;
	
	ФайлДанных = ПолучитьИмяВременногоФайла();
	Данные.Записать(ФайлДанных);
	МассивИменФайлов.Добавить(ФайлДанных);
	
	Если ЗначениеЗаполнено(Разделитель64) Тогда
		ФайлРазделителя = ПолучитьИмяВременногоФайла();
		ДанныеРазделителя = Base64Значение(Разделитель64);
		ДанныеРазделителя.Записать(ФайлРазделителя);
		МассивИменФайлов.Добавить(ФайлРазделителя);
	КонецЕсли;
	
	ФайлДобавляемыхДанных = ПолучитьИмяВременногоФайла();
	ДобавляемыеДанные = ПолучитьИзВременногоХранилища(АдресДобавляемыхДанных);
	ДобавляемыеДанные.Записать(ФайлДобавляемыхДанных);
	МассивИменФайлов.Добавить(ФайлДобавляемыхДанных);
	
	УдалитьИзВременногоХранилища(АдресДобавляемыхДанных);
	
	ФайлРезультата = ПолучитьИмяВременногоФайла();
	ОбъединитьФайлы(МассивИменФайлов, ФайлРезультата);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлДанных);
	Если ЗначениеЗаполнено(Разделитель64) Тогда
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРазделителя);
	КонецЕсли;
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлДобавляемыхДанных);
	
	ДанныеРезультата = Новый ДвоичныеДанные(ФайлРезультата);
	Результат = ПоместитьВоВременноеХранилище(ДанныеРезультата, АдресРезультата);
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ФайлРезультата);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиентеПоследовательноПослеВыполненияКоманды(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	Если ВходящийКонтекст.ОтложенныеКоманды[ВходящийКонтекст.ТекущаяКоманда].Представление = "ПодписатьФайлОписанияПакета"
		И КонтекстОтправки.Свойство("ФорматДокументооборота") И КонтекстОтправки.ФорматДокументооборота =
		ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
		
		Результат = Новый Структура("Выполнено, ФайлПодписи, ОписаниеОшибки", Результат <> Неопределено, Результат,
			?(Результат = Неопределено, НСтр("ru = 'Не удалось подписать в формате XMLDSig'"), ""));
	КонецЕсли;
	
	Если Результат.Выполнено Тогда
		Если ВходящийКонтекст.ОтложенныеКоманды[ВходящийКонтекст.ТекущаяКоманда].Представление = "ПодписатьФайлОписанияПакета" Тогда	
			ВходящийКонтекст.КонтекстОтправки.Вставить("ФайлПодписиПодФайломОписанияПакета", Результат.ФайлПодписи);
		КонецЕсли;
		СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиентеПоследовательно(
			ВходящийКонтекст.ОповещениеОЗавершении, ВходящийКонтекст.КонтекстОтправки, ВходящийКонтекст.ОтложенныеКоманды, ВходящийКонтекст.ТекущаяКоманда + 1);
	Иначе
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
	КонецЕсли;
	
КонецПроцедуры

#Область СформироватьПакетПФР

&НаКлиенте
Процедура СформироватьПакетПФР(ОповещениеОЗавершении, ТранспортноеСообщение, Оповещать = Ложь, ПараметрыОперации = Неопределено) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	 
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Оповещать);
	
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(КонтекстОтправки, ПараметрыОперации, Истина);
	КонецЕсли;	
	
	// общие проверки
	Если НЕ СформироватьПакетПФР_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда // сервер
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	Если ТипЗнч(ОповещениеОЗавершении.ДополнительныеПараметры) = Тип("Структура")
		И ОповещениеОЗавершении.ДополнительныеПараметры.Свойство("КонтекстОтправки")
		И ТипЗнч(ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки) = Тип("Структура") Тогда
		ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить("ЭтоЕЦП", КонтекстОтправки.ЭтоЕЦП);
	КонецЕсли;
	
	// получение и проверки сертификатов
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеПроверкиИПолученияСертификатовПолучателя", ЭтотОбъект, Контекст);
		
	СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателя(Оповещение, КонтекстОтправки); // клиент
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеПроверкиИПолученияСертификатовПолучателя(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Результат Тогда
		// проверки на клиенте (сертификаты)
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетПФРПослеПроверкиСертификатов", ЭтотОбъект, ВходящийКонтекст);
			
		СформироватьПакетПФР_ПроверкиСертификатов(Оповещение, КонтекстОтправки);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеПроверкиСертификатов(Результат, ВходящийКонтекст) Экспорт	
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если НЕ Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеВыгрузкиСертификатаРуководителя",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	Если КонтекстОтправки.Свойство("ФорматДокументооборота")
		И КонтекстОтправки.ФорматДокументооборота =
			ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
		
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(
			ОписаниеОповещения,
			КонтекстОтправки.ОбъектСертификатРуководителя,
			Ложь);
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, СтрокаBase64", Истина, "");
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеВыгрузкиСертификатаРуководителя(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеПроверкиСФРИспользованиеКлюча",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	Если Результат.Выполнено И ЗначениеЗаполнено(Результат.СтрокаBase64) Тогда
		ДанныеСертификата = Base64Значение(Результат.СтрокаBase64);
		
		Если ДокументооборотСКОКлиентСервер.СФРИспользованиеКлючаСоответствуетТребованиям(
			ДанныеСертификата, КонтекстОтправки.ОбъектСертификатРуководителя.ДействителенПо) = Ложь Тогда
			
			ПараметрыОрганизации = Новый Структура;
			ПараметрыОрганизации.Вставить("Организация", КонтекстОтправки.СсылкаОтправитель);
			ПараметрыОрганизации.Вставить("Отпечаток", КонтекстОтправки.СертификатРуководителя);
			Организации = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ПараметрыОрганизации);
			
			ПараметрыФормыПредупреждения = Новый Структура();
			ПараметрыФормыПредупреждения.Вставить("Организации", Организации);
			ПараметрыФормыПредупреждения.Вставить("ВызовПриОтправке", Истина);
			ПараметрыФормыПредупреждения.Вставить("СкрытьНастройкуНапоминаний", Истина);
			
			Если ОткрытьФорму(
				ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеСФРИспользованиеКлючаПоОрганизации",
				ПараметрыФормыПредупреждения,,,,,
				ОписаниеОповещения) = Неопределено Тогда
				
				ВыполнитьОбработкуОповещения(ОписаниеОповещения, "ПродолжитьОтправку");
			КонецЕсли;
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, "ПродолжитьОтправку");
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеПроверкиСФРИспользованиеКлюча(
		Результат,
		ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	Если Результат <> "ПродолжитьОтправку" Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// формируем файл описания сведений, если это первичное сообщение с отчетностью
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьПФР")
		ИЛИ КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееЗапросПФР") Тогда
		
		Если НЕ СформироватьОписаниеСведенийДляСообщения(КонтекстОтправки) Тогда
			СообщениеОбОшибке = НСтр("ru = 'Формирование пакета прервано.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СообщениеОбОшибке);
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ЭтоМакетПД = ЭтоМакетПД(КонтекстОтправки.ЦиклОбмена);
	ЭтоЗаявление = ЭтоЗаявлениеОНазначенииПенсии(КонтекстОтправки.ЦиклОбмена);
	Если ЭтоМакетПД ИЛИ ЭтоЗаявление Тогда
		
		Настройки = ПолучитьПараметрыОтправкиМакетовПенсионныхДелИЗаявления(КонтекстОтправки.ЦиклОбмена);
		
		Если Настройки = Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		КонтекстОтправки.Вставить("Настройки", Настройки);
	КонецЕсли;
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
	Контекст.Вставить("ПервыйВызов", Истина);
	Контекст.Вставить("СформироватьПакетМакетаПДИЗаявление", ЭтоМакетПД ИЛИ ЭтоЗаявление);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеПодписания", ЭтотОбъект, Контекст);
	СформироватьПакетПФР_Подписание(Оповещение, КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеПодписания(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
		
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
			
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыОперации = Новый Структура("ТребуетПодтверждениОперации", Ложь);
	КонецЕсли;
			
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР")
		И ВходящийКонтекст.ПервыйВызов Тогда
		
		Если ВходящийКонтекст.СформироватьПакетМакетаПДИЗаявление Тогда
			Если КонтекстОтправки.Настройки.Вложение.ТребуетсяАрхивирование 
				И НЕ СформироватьПакетМакетаПенсионногоДелаИЗаявления(КонтекстОтправки) Тогда
				ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
			КонецЕсли;
			
			ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
			ВходящийКонтекст.Вставить("ПервыйВызов", ВходящийКонтекст.ПервыйВызов);
			ВходящийКонтекст.Вставить("СформироватьПакетМакетаПДИЗаявление", Ложь);
			
			Оповещение = Новый ОписаниеОповещения(
				"СформироватьПакетПФРПослеПодписания", ЭтотОбъект, ВходящийКонтекст);
			СформироватьПакетПФР_Подписание(Оповещение, ВходящийКонтекст.КонтекстОтправки, ПараметрыОперации);
			Возврат;
		КонецЕсли;
		
		// формируем файл описания письма, если его нет
		Если НЕ СформироватьОписаниеПисьмаДляСообщения(КонтекстОтправки) Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		ВходящийКонтекст.Вставить("ПервыйВызов", Ложь);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьТранспортнуюИнформациюДляСообщенияПослеВыполнения", ЭтотОбъект, ВходящийКонтекст);
		// формируем файл с транспортной информацией, если его нет
		СформироватьТранспортнуюИнформациюДляСообщения(Оповещение, КонтекстОтправки);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПисьмоВходящееКвитанцияПФР")
		И ВходящийКонтекст.ПервыйВызов Тогда
		// формируем подтверждение получения
		Если НЕ СформироватьПодтверждениеПолученияДляСообщения(КонтекстОтправки) Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		ВходящийКонтекст.Вставить("ПервыйВызов", Ложь);
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетПФРПослеПодписания", ЭтотОбъект, ВходящийКонтекст);
		СформироватьПакетПФР_Подписание(Оповещение, ВходящийКонтекст.КонтекстОтправки, ПараметрыОперации);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ОтветНаЗапросКвитанцияПФР")
		И ВходящийКонтекст.ПервыйВызов Тогда
		// формируем подтверждение получения
		Если НЕ СформироватьПодтверждениеПолученияЗапросаПФРДляСообщения(КонтекстОтправки.ТранспортноеСообщение) Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		ВходящийКонтекст.Вставить("ПервыйВызов", Ложь);
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетПФРПослеПодписания", ЭтотОбъект, ВходящийКонтекст);
		СформироватьПакетПФР_Подписание(Оповещение, ВходящийКонтекст.КонтекстОтправки, ПараметрыОперации);
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеВыгрузкиСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	СертификатШифрования = КонтекстОтправки.ОбъектСертификатРуководителя;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатШифрования);
	
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(ОписаниеОповещения, СертификатШифрования, Ложь);
		
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеВыгрузкиСертификата(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	ИндексСертификатаПолучателя = ?(КонтекстОтправки.Свойство("ИндексСертификатаПолучателя"),
		КонтекстОтправки.ИндексСертификатаПолучателя, -1) + 1;
	ДвДанныеСертификатаПолучателя = ?(КонтекстОтправки.Свойство("ДвДанныеСертификатаПолучателя"),
		КонтекстОтправки.ДвДанныеСертификатаПолучателя, Новый Массив);
	
	ДвДанныеСертификата = Неопределено;
	Если Результат.Выполнено Тогда
		ДвДанныеСертификата = Base64Значение(Результат.СтрокаBase64);
		
		Если ИндексСертификатаПолучателя = 0 Тогда
			КонтекстОтправки.Вставить("ДвДанныеСертификатаРуководителя", ДвДанныеСертификата);
			ДвДанныеСертификатаПолучателя.Очистить();
		КонецЕсли;
	КонецЕсли;
	
	Если ИндексСертификатаПолучателя <> 0 Тогда
		ДвДанныеСертификатаПолучателя.Добавить(ДвДанныеСертификата);
	КонецЕсли;
	
	Если КонтекстОтправки.Свойство("ФорматДокументооборота")
		И КонтекстОтправки.ФорматДокументооборота = ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК")
		И ИндексСертификатаПолучателя < КонтекстОтправки.ОбъектСертификатПолучателя.Количество() Тогда
		
		КонтекстОтправки.Вставить("ИндексСертификатаПолучателя", 	ИндексСертификатаПолучателя);
		КонтекстОтправки.Вставить("ДвДанныеСертификатаПолучателя", 	ДвДанныеСертификатаПолучателя);
		
		СертификатШифрования = КонтекстОтправки.ОбъектСертификатПолучателя[ИндексСертификатаПолучателя];
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатШифрования);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СформироватьПакетПФРПослеВыгрузкиСертификата",
			ЭтотОбъект,
			ВходящийКонтекст);
		
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(
			ОписаниеОповещения, СертификатШифрования, Ложь);
		Возврат;
		
	ИначеЕсли ИндексСертификатаПолучателя > 0 Тогда
		КонтекстОтправки.Удалить("ИндексСертификатаПолучателя");
	КонецЕсли;
	
	// формируем дерево описания документов пакета (сервер)
	Если НЕ СформироватьПакетПФР_СформироватьОписаниеДокументовПакета(КонтекстОтправки) Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	РежимПодтвержденияОблачнойПодписи("СформироватьПакетПФР", КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки);

	// отрабатываем отложенные команды на клиенте (шифрование)
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеВыполненияОтложенныхКомандНаКлиенте", ЭтотОбъект, ВходящийКонтекст);
	СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(Оповещение, КонтекстОтправки);
		
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ЗарегистрироватьСертификатВСистемеНачало(ОбработкаОповещения, МестоХраненияКлюча, ФайлСертификата, 
		Хранилище, ОтпечатокСертификата = Неопределено) Экспорт
		
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение", ОбработкаОповещения);
	
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		РезультатРегистрации = ЗарегистрироватьСертификатВСистемеНаСервере(ФайлСертификата, Хранилище, ОтпечатокСертификата);
		Результат = Новый Структура("Выполнено, Отпечаток", РезультатРегистрации, ОтпечатокСертификата);
		ВыполнитьОбработкуОповещения(ОбработкаОповещения, Результат);
		
	ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		Если ЭтоАдресВременногоХранилища(ФайлСертификата) 
			ИЛИ ТипЗнч(ФайлСертификата) = Тип("ДвоичныеДанные") Тогда
			ДанныеСертификата = ФайлСертификата;
		Иначе
			ДанныеСертификата = СтрЗаменить(ФайлСертификата, "-----BEGIN CERTIFICATE-----" + Символы.ПС, "");
			ДанныеСертификата = СтрЗаменить(ДанныеСертификата, "-----END CERTIFICATE-----" + Символы.ПС, "");
			ДанныеСертификата = Base64Значение(ДанныеСертификата);
		КонецЕсли;	
		РезультатРегистрации = ЗарегистрироватьСертификатВХранилищеПолучателей(ДанныеСертификата, Хранилище, ОтпечатокСертификата);
		Результат = Новый Структура("Выполнено, Отпечаток", РезультатРегистрации, ОтпечатокСертификата);
		ВыполнитьОбработкуОповещения(ОбработкаОповещения, Результат);
		
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения("ЗарегистрироватьСертификатВСистемеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		КриптографияЭДКОКлиент.ИмпортироватьСертификат(ОписаниеОповещения, ФайлСертификата, Хранилище);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗарегистрироватьСертификатВСистемеЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено Тогда
		РезультатРегистрации = Новый Структура("Выполнено, Отпечаток", Истина, Результат.СвойстваСертификата.Отпечаток);
	Иначе
		РезультатРегистрации = Новый Структура("Выполнено, Отпечаток", Ложь, Неопределено);
	КонецЕсли;
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатРегистрации);
	
КонецПроцедуры

&НаКлиенте
Процедура СообщитьОбОшибкеОтправкиЗаявления(
	СостояниеОтправкиСообщения, 
	ПовторятьСоединение,
	ТекстОшибки,
	ДополнительныеПараметры)
	
	Если НЕ ПустаяСтрока(СостояниеОтправкиСообщения) И ПовторятьСоединение Тогда
		
		Результат = Новый Структура("ТекстОшибки", ТекстОшибки);
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
			
	ИначеЕсли НЕ ПустаяСтрока(СостояниеОтправкиСообщения) Тогда
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
			СостояниеОтправкиСообщения, 
			ДополнительныеПараметры.ВызовИзМастераПодключенияК1СОтчетности, 
			ТекстОшибки);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьДанныеЗаявленияПослеОтправки(ДополнительныеПараметры)
	
	ДокументЗаявление 	= ДополнительныеПараметры.ДокументЗаявление;
	Статус 				= ДополнительныеПараметры.Статус;
	ДатаОтправки 		= ДополнительныеПараметры.ДатаОтправки;
	СтатусКомментарий 	= ДополнительныеПараметры.СтатусКомментарий;
	
	ПутьКонтейнерЗакрытогоКлюча = ДополнительныеПараметры.ПутьКонтейнерЗакрытогоКлюча;
	
	Если ПутьКонтейнерЗакрытогоКлюча <> ДокументЗаявление.ПутьКонтейнерЗакрытогоКлюча
		ИЛИ	Статус <> ДокументЗаявление.Статус
		ИЛИ СтатусКомментарий <> ДокументЗаявление.СтатусКомментарий Тогда
		
		ДокументЗаявление.ПутьКонтейнерЗакрытогоКлюча 	= ПутьКонтейнерЗакрытогоКлюча;
		ДокументЗаявление.Статус 					  	= Статус;
		ДокументЗаявление.ДатаОтправкиЗаявления 	  	= ДатаОтправки;
		ДокументЗаявление.СтатусКомментарий 		  	= СтатусКомментарий;
		
		Если ДополнительныеПараметры.Свойство("АдресТекстаОтправленногоЗаявления")
			И ЭтоАдресВременногоХранилища(ДополнительныеПараметры.АдресТекстаОтправленногоЗаявления) Тогда
			АдресЗаявления = ДополнительныеПараметры.АдресТекстаОтправленногоЗаявления;
		Иначе
			АдресЗаявления = Неопределено;
		КонецЕсли;
		
		РеквизитыДокументаДляЗаписи = Новый Структура;
		РеквизитыДокументаДляЗаписи.Вставить("ПутьКонтейнерЗакрытогоКлюча", ПутьКонтейнерЗакрытогоКлюча);
		РеквизитыДокументаДляЗаписи.Вставить("Статус",						Статус);
		РеквизитыДокументаДляЗаписи.Вставить("ДатаОтправкиЗаявления",		ДатаОтправки);
		РеквизитыДокументаДляЗаписи.Вставить("СтатусКомментарий", 			СтатусКомментарий);
		РеквизитыДокументаДляЗаписи.Вставить("АдресЗаявления", 				АдресЗаявления);
		
		ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(ДокументЗаявление.Ссылка, РеквизитыДокументаДляЗаписи);
	
	КонецЕсли;
	
КонецПроцедуры
	
&НаКлиенте
Процедура ЗаявлениеЗашифроватьПослеПроверкиРегистрацииСертификата(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ХранилищеСертификата = "AddressBook";
		ОтпечатокСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(ДополнительныеПараметры.ДокументЗаявление.СпецоператорСвязи, "СертификатБезопасности_Отпечаток");
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
		
		Если НЕ Результат.СертификатНайден Тогда
			
			ТекстСертификата = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(ДополнительныеПараметры.ДокументЗаявление.СпецоператорСвязи, "СертификатБезопасности");
			
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
				ХранилищеСертификата 	= "AddressBook";
				ОписаниеОповещения = Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеРегистрацииСертификата", ЭтотОбъект, ДополнительныеПараметры);
				ЗарегистрироватьСертификатВСистемеНачало(ОписаниеОповещения, МестоХраненияКлюча, ТекстСертификата, ХранилищеСертификата, ОтпечатокСертификата);
			Иначе
				ОписаниеОповещения 		= Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеСохраненияСертификатаВФайл", ЭтотОбъект, ДополнительныеПараметры);
				ПутьКФайлуСертификата 	= ДополнительныеПараметры.ВременныйКаталог + ОтпечатокСертификата + ".cer";
				ОперацииСФайламиЭДКОКлиент.ТекстВФайл(ОписаниеОповещения, ТекстСертификата, ПутьКФайлуСертификата);
			КонецЕсли;
		Иначе
			ОписаниеОповещения 		= Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеРегистрацииСертификата", ЭтотОбъект, ДополнительныеПараметры);
			РезультатРегистрации 	= Новый Структура("Выполнено, Отпечаток", Истина, Результат.СвойстваСертификата.Отпечаток);
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатРегистрации);
		КонецЕсли;
		
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения("СформироватьИОтправитьФайлыЗаявления", ЭтотОбъект, ДополнительныеПараметры);
		Результат = Новый Структура("Выполнено", Ложь);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеЗашифроватьПослеСохраненияСертификатаВФайл(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено Тогда
		
		// Если сертификат оператора электронного документооборота не зарегистрирован, то регистрируем его
		ХранилищеСертификата 	= "AddressBook";
		ОтпечатокСертификата 	= ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьПараметрСпецоператора(ДополнительныеПараметры.ДокументЗаявление.СпецоператорСвязи, "СертификатБезопасности_Отпечаток");
		ПутьКФайлуСертификата 	= ДополнительныеПараметры.ВременныйКаталог + ОтпечатокСертификата + ".cer";
		МестоХраненияКлюча		= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеРегистрацииСертификата", ЭтотОбъект, ДополнительныеПараметры);
		ЗарегистрироватьСертификатВСистемеНачало(ОписаниеОповещения, МестоХраненияКлюча, ПутьКФайлуСертификата, ХранилищеСертификата, ОтпечатокСертификата);
		
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения("СформироватьИОтправитьФайлыЗаявления", ЭтотОбъект, ДополнительныеПараметры);
		Результат = Новый Структура("Выполнено", Ложь);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеЗашифроватьПослеРегистрацииСертификата(Результат, ДополнительныеПараметры) Экспорт
	// Действия после регистрации сертификата оператора связи
	Если Результат.Выполнено Тогда
		
		ХранилищеСертификата 	= "AddressBook";
		ДокументЗаявление 		= ДополнительныеПараметры.ДокументЗаявление;
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ЗаявлениеЗашифроватьПослеПолученияСвойствСертификатаОператораСвязи", ЭтотОбъект, ДополнительныеПараметры);
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			
			// В модели сервиса получаем сертификат синхронно
			Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
				РезультатаПолученияСертификата = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(Результат.Отпечаток, ХранилищеСертификата);
			Иначе
				РезультатаПолученияСертификата = ПолучитьСвойстваОблачногоСертификата(Результат.Отпечаток, ХранилищеСертификата);
			КонецЕсли;
			ПараметрыПродолженияРаботы = Новый Структура;
			Если РезультатаПолученияСертификата = Неопределено Тогда
				ПараметрыПродолженияРаботы.Вставить("Выполнено", Ложь);
			Иначе
				ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, РезультатаПолученияСертификата);
				ПараметрыПродолженияРаботы.Вставить("Выполнено", Истина);
				ПараметрыПродолженияРаботы.Вставить("СертификатНайден", Истина);
				ПараметрыПродолженияРаботы.Вставить("СвойстваСертификата", РезультатаПолученияСертификата);
			КонецЕсли;
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, ПараметрыПродолженияРаботы);
			
		Иначе
			КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(ОписаниеОповещения, Результат.Отпечаток, ХранилищеСертификата);
		КонецЕсли;

	Иначе
		Результат = Новый Структура("Выполнено", Ложь);
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры


#Область СформироватьПакетСОС

&НаКлиенте
Процедура СформироватьПакетСОС(ОповещениеОЗавершении, ТранспортноеСообщение, Оповещать = Ложь, ПараметрыОперации = Неопределено) Экспорт
	
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Оповещать);
	
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(КонтекстОтправки, ПараметрыОперации, Истина);
	КонецЕсли;	
	
	// общие проверки
	Если НЕ СформироватьПакетФНС_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда // сервер
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// получение и проверки сертификатов
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетСОСПослеПроверкиИПолученияСертификатовПолучателя", ЭтотОбъект, Контекст);
		
	СформироватьПакетФНС_ПолучитьИПроверитьСертификатыПолучателя(Оповещение, КонтекстОтправки); // клиент
	
КонецПроцедуры 

&НаКлиенте
Процедура СформироватьПакетСОСПослеПроверкиИПолученияСертификатовПолучателя(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Результат Тогда
		Если НЕ СформироватьПакетФНС_ПроверкиНаСервере(КонтекстОтправки) Тогда // универсальная
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		// формируем дерево описания документов пакета (сервер)
		Если НЕ СформироватьПакетФНС_СформироватьДеревоОписанияДокументовПакета(КонтекстОтправки) Тогда // универсальная
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;

		РежимПодтвержденияОблачнойПодписи("ПакетСОС", КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки);
	
		// отрабатываем отложенные команды на клиенте (шифрование)
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетСОСПослеВыполненияОтложенныхКомандНаКлиенте", ЭтотОбъект, ВходящийКонтекст);
		СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(Оповещение, КонтекстОтправки);

	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетСОСПослеВыполненияОтложенныхКомандНаКлиенте(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);	
		Возврат;
	КонецЕсли;
	
	// формируем файл описания пакета, создаем архив и помещаем в ИБ
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если НЕ СформироватьПакетСОС_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.Оповещать Тогда
		Оповестить("Изменение сообщения", Новый Структура("Сообщение, ЦиклОбмена, Предмет",
														КонтекстОтправки.ТранспортноеСообщение,
														КонтекстОтправки.ЦиклОбмена));
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры	

&НаСервереБезКонтекста
Функция ПодготовитьПараметрыПодписатьСообщение(Знач КонтролирующийОрган, 
                                               МестоХраненияКлюча,
                                               Знач Сообщение,
                                               Знач ТипСообщения,
                                               Знач СертификатПодписанта, 
                                               Знач УчетнаяЗапись,
                                               Знач СообщениеОснование)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПодготовитьПараметрыПодписатьСообщение(
				КонтролирующийОрган,
				МестоХраненияКлюча,
				Сообщение,
				ТипСообщения,
				СертификатПодписанта,
				УчетнаяЗапись,
				СообщениеОснование);

КонецФункции

&НаКлиенте
Процедура ПодписатьСообщение(
		КонтролирующийОрган,
		ОповещениеОЗавершении,
		КонтекстОтправки,
		Сообщение,
		ТипСообщения,
		СертификатПодписанта = Неопределено,
		ИмяСгенерированногоФайлаЭЦП = Неопределено,
		УчетнаяЗапись = Неопределено,
		СообщениеОснование = Неопределено) Экспорт
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(УчетнаяЗапись, КонтекстОтправки);
	
	ЦиклОбмена = ?(КонтекстОтправки.Свойство("ЦиклОбмена"), КонтекстОтправки.ЦиклОбмена, Неопределено);
	
	ФорматДокументооборота = ?(КонтекстОтправки.Свойство("ФорматДокументооборота"),
		КонтекстОтправки.ФорматДокументооборота, Неопределено);
	СообщениеОснованиеИПараметры = Новый Структура;
	СообщениеОснованиеИПараметры.Вставить("СообщениеОснование", 			СообщениеОснование);
	СообщениеОснованиеИПараметры.Вставить("ЦиклОбмена", 					ЦиклОбмена);
	СообщениеОснованиеИПараметры.Вставить("ФорматДокументооборота", 		ФорматДокументооборота);
	СообщениеОснованиеИПараметры.Вставить("ПолучитьФорматДокументооборота", КонтролирующийОрган = "ПФР");
	
	ПараметрыФункции = ПодготовитьПараметрыПодписатьСообщение(
		КонтролирующийОрган,
		МестоХраненияКлюча,
		Сообщение,
		ТипСообщения,
		СертификатПодписанта,
		УчетнаяЗапись,
		СообщениеОснованиеИПараметры);
	
	Если СообщениеОснованиеИПараметры.ПолучитьФорматДокументооборота Тогда
		КонтекстОтправки.Вставить("ФорматДокументооборота", ПараметрыФункции.ФорматДокументооборота);
	КонецЕсли;
	
	ТребуетПодтверждениеОперации = СвойствоСтруктуры(КонтекстОтправки, "ТребуетПодтверждениОперации", Неопределено);
	РежимПодтвержденияОблачнойПодписи(ТипСообщения, Сообщение, КонтекстОтправки, ТребуетПодтверждениеОперации);
	
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, КонтекстОтправки);
	ПараметрыФункции.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Оповещение = Новый ОписаниеОповещения("ПодписатьСообщениеПослеВыполнения", ЭтотОбъект, Контекст);	
	
	ПодписатьСообщениеНаКлиенте(Оповещение, ПараметрыФункции);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПодготовитьДокументыДляПодписания(СообщениеДокументыКоторогоПодписываются, Сообщение, СертификатыПодписантов, ТипыДИВ, ВременныйКаталог = "")
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПодготовитьДокументыДляПодписания(
		СообщениеДокументыКоторогоПодписываются,
		Сообщение,
		СертификатыПодписантов,
		ТипыДИВ,
		ВременныйКаталог);
	
КонецФункции

&НаКлиенте
Процедура ПодписатьСообщениеПослеСозданияКаталога(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ПараметрыФункции = ВходящийКонтекст.ПараметрыФункции;
	ВременныйКаталог = Результат.ИмяКаталога;
	
	ДокументыДляПодписания = ПодготовитьДокументыДляПодписания(
		ПараметрыФункции.СообщениеДокументыКоторогоПодписываются,
		ПараметрыФункции.Сообщение,
		ПараметрыФункции.СертификатыПодписантов,
		ПараметрыФункции.ТипыДИВ,
		ВременныйКаталог);
		
	КонтекстОтправки = ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	ПакетнаяПодпись = Ложь;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
		И ПараметрыФункции.СертификатыПодписантов.Количество() = 1
		И ДокументыДляПодписания.Количество() > 1 Тогда
		ЭДОК = СвойствоСтруктуры(КонтекстОтправки, "ФорматДокументооборота") = ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК");
		ВсеТипы = Новый Соответствие;
		ВсеСертификаты = Новый Соответствие;
		Для Каждого СтрокаМассива Из ДокументыДляПодписания Цикл
			РасширениеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(СтрокаМассива.ИмяФайла).Расширение;
			ВсеТипы.Вставить(НРег(РасширениеФайла), Истина);
		КонецЦикла;
		
		ПакетнаяПодпись = НЕ (ВсеТипы[".xml"] <> Неопределено И ЭДОК);
		
	КонецЕсли;
	
	Если ПакетнаяПодпись И ВключитьПакетнуюПодпись Тогда
		ВходящийКонтекст.Вставить("ДокументыДляПодписывания", ДокументыДляПодписания);
		
		Оповещение = Новый ОписаниеОповещения("ПодписатьСообщенияПакетноПослеВыполнения", ЭтотОбъект, ВходящийКонтекст);
		МассивДокументов = Новый Массив;
		Для Счетчик = 0 По ДокументыДляПодписания.ВГраница() Цикл
			СтрокаМассива = ДокументыДляПодписания[Счетчик];
			ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(СтрокаМассива.ИмяФайла);
			НовыйДокументПакетнойПодписи(
				МассивДокументов,
				СтрокаМассива.Данные,
				ОписаниеФайла.Имя,
				ОписаниеФайла.Расширение,
				Счетчик);
		КонецЦикла;

		ПараметрыОперации = Новый Структура("ПодтверждатьОперацию", КонтекстОтправки.ТребуетПодтверждениеОперации);
		ПакетнаяПодписьОблачнойПодписи(Оповещение, МестоХраненияКлюча, МассивДокументов, ПараметрыФункции.СертификатыПодписантов[0], ПараметрыОперации);
		
	Иначе	
		ПодписатьСообщенияПоследовательно(ВходящийКонтекст, ДокументыДляПодписания, 0, ВременныйКаталог);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСообщенияПоследовательно(ВходящийКонтекст, ДокументыДляПодписывания, ТекущийДокумент, ВременныйКаталог)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если ТекущийДокумент <= ДокументыДляПодписывания.ВГраница() Тогда
		
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки);
		
		ЭтоОписьИходящихДокументов = ЭтоПодписаниеОписи(ВходящийКонтекст);
		
		ДокументДляПодписывания = ДокументыДляПодписывания[ТекущийДокумент];

		Контекст = ВходящийКонтекст;
		Контекст.Вставить("ДокументыДляПодписывания", ДокументыДляПодписывания);
		Контекст.Вставить("ТекущийДокумент", ТекущийДокумент);
		Контекст.Вставить("ВременныйКаталог", ВременныйКаталог);
		Если Не Контекст.Свойство("МенеджерКриптографии") Тогда
			Контекст.Вставить("МенеджерКриптографии", Неопределено);
		КонецЕсли;
		Если Не Контекст.Свойство("ДвоичныеДанные") Тогда
			Контекст.Вставить("ДвоичныеДанные", Неопределено);
		КонецЕсли;
		
		Оповещение = Новый ОписаниеОповещения(
			"ПодписатьСообщенияПоследовательноПослеСохраненияДанныхВФайл", ЭтотОбъект, Контекст);
		
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
			ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча)
			ИЛИ ЭтоОписьИходящихДокументов Тогда
			ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено, ДвоичныеДанные", Истина, Неопределено));
		Иначе		
			ОперацииСФайламиЭДКОКлиент.ДанныеССервераВФайл(
				Оповещение, ДокументДляПодписывания.Данные, ДокументДляПодписывания.ПолноеИмяФайла, Истина, Контекст.ДвоичныеДанные);
		КонецЕсли
	Иначе
		Если ВременныйКаталог <> "" Тогда //Не ЭП в облаке
		    ОперацииСФайламиЭДКОКлиент.УдалитьФайлыНаКлиенте(, ВременныйКаталог);
		КонецЕсли;
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении,
			Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСообщенияПоследовательноПослеСохраненияДанныхВФайл(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ПараметрыФункции = ВходящийКонтекст.ПараметрыФункции;
		КонтекстОтправки = ПараметрыФункции.КонтекстОтправки;
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
		ЭтоОписьИходящихДокументов = ЭтоПодписаниеОписи(ВходящийКонтекст);
		
		ДокументДляПодписывания = ВходящийКонтекст.ДокументыДляПодписывания[ВходящийКонтекст.ТекущийДокумент];
		
		ВходящийКонтекст.Вставить("ДвоичныеДанные", Результат.ДвоичныеДанные);
		Если КонтекстОтправки.Свойство("ФорматДокументооборота") И КонтекстОтправки.ФорматДокументооборота =
			ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
			
			РасширениеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайла).Расширение;
			ЭтоПодписьXMLDSig = (нрег(РасширениеФайла) = ".xml");
		Иначе
			ЭтоПодписьXMLDSig = Ложь;
		КонецЕсли;
		ВходящийКонтекст.Вставить("ЭтоПодписьXMLDSig", ЭтоПодписьXMLDSig);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПодписатьСообщенияПоследовательноПослеПодписания", ЭтотОбъект, ВходящийКонтекст);
		
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
			ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча)
			ИЛИ ЭтоОписьИходящихДокументов Тогда
			Данные = ДокументДляПодписывания.Данные;
			Подпись = Неопределено;
		Иначе
			Данные = Результат.ИмяФайла;
			Подпись = ВходящийКонтекст.ВременныйКаталог
				+ ?(КонтекстОтправки.Свойство("ФорматДокументооборота") И КонтекстОтправки.ФорматДокументооборота =
				ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК"),
				".|.sig", "|.bin");
		КонецЕсли;
		
		ТребуетПодтверждениеОперации = СвойствоСтруктуры(КонтекстОтправки, "ТребуетПодтверждениеОперации", Ложь);
		
		Если ЭтоПодписьXMLDSig Тогда
			Кодировка = Неопределено; // не требуется перекодировать, отчет уже в "utf-8"
			Если КонтекстОтправки.Свойство("ТипДокументооборота") И КонтекстОтправки.ТипДокументооборота = "СведенияПФР" Тогда
				
				ПрефиксИмениФайла = Лев(ДокументДляПодписывания.ИмяФайла, 4);
				ПрефиксИмениФайла = ВРег(ПрефиксИмениФайла);
				
				Если ПрефиксИмениФайла = "PFR-" Тогда
					Кодировка = "windows-1251";
				КонецЕсли; // иначе ПрефиксИмениФайла = "ПФР_"
			КонецЕсли;
			
			НастройкиПодписи = Новый Структура;
			НастройкиПодписи.Вставить("ДополнитьПространстваИменУзлаПодписи", 	Истина);
			НастройкиПодписи.Вставить("ПодстрокаУзлаДляИнформацииОПодписи", 	"ЭДСФР");
			НастройкиПодписи.Вставить("ИмяСобытияДляИнформацииОПодписи", 		"ЕЦП добавлять SignInfo");
			
			Если ЗначениеЗаполнено(ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки.МЧД) Тогда
				НастройкиПодписи.Вставить("ЭтоИсключающаяКанонизация", 						Истина);
				НастройкиПодписи.Вставить("ТипКаноническогоXML", 							"КаноническийXML1_1");
				НастройкиПодписи.Вставить("ИспользоватьПреобразованиеККаноническомуXML", 	Истина);
				НастройкиПодписи.Вставить("КанонизацияБезКомментариев", 					Истина);
				НастройкиПодписи.Вставить("ПрефиксПодписи", 								"ds");
				НастройкиПодписи.Вставить("ДополнительноеПространствоИмен", 				"http://www.fss.ru/integration/types/common/v01");
				НастройкиПодписи.Вставить("ДобавитьШтампВремени", 							Истина);
				НастройкиПодписи.Вставить("ТолькоСвойстваБезШтампаВремени", 				Истина);
				НастройкиПодписи.Вставить("ИдентификаторРодителя", 							ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки.МЧД);
				НастройкиПодписи.Вставить("ИсключитьИдентификаторСсылкиРодителя", 			Истина);
			КонецЕсли;
			
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
				ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайла);
				ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации, НастройкиПодписи);
			КонецЕсли;
			
			ПодписатьФайлXMLDSig(
				ДокументДляПодписывания.Сертификат,
				Данные,
				Оповещение,
				Подпись,
				Кодировка,
				НастройкиПодписи);
				
		Иначе
			ПараметрыОперации = Неопределено;
			Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
				ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайла);
				ПараметрыОперации = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
			КонецЕсли;	
			
			КриптографияЭДКОКлиент.ПодписатьPKCS7(
				Оповещение, ДокументДляПодписывания.Сертификат, Данные, 
				Ложь, Подпись, Истина, ВходящийКонтекст.МенеджерКриптографии, , ПараметрыОперации);
				
		КонецЕсли;
		
	Иначе
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка выгрузки документа
                 |%1
                 |во временный файл для подписания.'"), ВходящийКонтекст.ДокументыДляПодписывания[ВходящийКонтекст.ТекущийДокумент].ИмяФайла);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСообщенияПоследовательноПослеПодписания(Результат, ВходящийКонтекст) Экспорт
	
	Если ВходящийКонтекст.ЭтоПодписьXMLDSig Тогда
		Результат = Новый Структура("Выполнено, ФайлПодписи, ОписаниеОшибки", Результат <> Неопределено, Результат,
			?(Результат = Неопределено, НСтр("ru = 'Не удалось подписать в формате XMLDSig'"), ""));
	КонецЕсли;
	
	Если Результат.Выполнено Тогда
		
		КонтекстОтправки = ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки;
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
		ЭтоОписьИходящихДокументов = ЭтоПодписаниеОписи(ВходящийКонтекст);
		ЭтоОтчетностьЦБ = ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки.Свойство("ТипСообщения")
			И ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки.ТипСообщения =
			ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьЦБ");
		
		Оповещение = Новый ОписаниеОповещения(
			"ПодписатьСообщенияПоследовательноПослеЧтенияДанныхИзФайла", ЭтотОбъект, ВходящийКонтекст);
		
		Если ЭтоОтчетностьЦБ Тогда
			ИмяФайлаПодписи = ВходящийКонтекст.ДокументыДляПодписывания[ВходящийКонтекст.ТекущийДокумент].ИмяФайла;
			ИмяФайлаПодписи = ?(нрег(Прав(ИмяФайлаПодписи, 4)) = ".enc", Лев(ИмяФайлаПодписи, СтрДлина(ИмяФайлаПодписи) - 4),
				ИмяФайлаПодписи) + ".2.sig";
			
		Иначе
			ИмяФайлаПодписи = Результат.ФайлПодписи;
		КонецЕсли;
		ВходящийКонтекст.ДокументыДляПодписывания[ВходящийКонтекст.ТекущийДокумент].Вставить("ИмяФайлаПодписи", ИмяФайлаПодписи);
		
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
			ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча)
			ИЛИ ЭтоОписьИходящихДокументов Тогда
			Если КонтекстОтправки.Свойство("ФорматДокументооборота") И КонтекстОтправки.ФорматДокументооборота =
				ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
				ИмяФайлаПодписи = "." + СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент(".sig");
			Иначе
				ИмяФайлаПодписи = СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент();
			КонецЕсли;
			ВходящийКонтекст.ДокументыДляПодписывания[ВходящийКонтекст.ТекущийДокумент].ИмяФайлаПодписи = ИмяФайлаПодписи;
					
			ДД = ПолучитьИзВременногоХранилища(Результат.ФайлПодписи);
			СтрокаBase64 = Base64Строка(ДД);
			ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено, СтрокаBase64", Истина, СтрокаBase64));
		Иначе			
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, Результат.ФайлПодписи, Истина, ВходящийКонтекст.ДвоичныеДанные);
		КонецЕсли;
	Иначе
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка подписания документа: %1.
					   |По причине: %2'"), 
			ПредставлениеТранспортногоСообщения(ВходящийКонтекст.ПараметрыФункции.Сообщение), 
			Результат.ОписаниеОшибки);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении, Ложь);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ЭтоПодписаниеОписи(ВходящийКонтекст)
	
	Если ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки.Свойство("ТипСообщения") Тогда
		ЭтоОписьИходящихДокументов = ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПредставлениеНП");
	Иначе
		ЭтоОписьИходящихДокументов = Ложь;
	КонецЕсли;
	
	Возврат ЭтоОписьИходящихДокументов;
	
КонецФункции

&НаКлиенте
Процедура ПодписатьСообщенияПоследовательноПослеЧтенияДанныхИзФайла(Результат, ВходящийКонтекст) Экспорт
	
	ДокументДляПодписывания = ВходящийКонтекст.ДокументыДляПодписывания[ВходящийКонтекст.ТекущийДокумент];
	Если Результат.Выполнено Тогда
		// сохраняем в регистр
		Если ВходящийКонтекст.ЭтоПодписьXMLDSig Тогда
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ВходящийКонтекст.ПараметрыФункции.Сообщение,
				СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ФайлОтчетностиПФР,
				Новый Структура("Строка64", Результат.СтрокаBase64),
				ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайла).Имя,,,,,,
				ПредопределенноеЗначение("Перечисление.ТипыФайловОтчетностиПФР.ПачкаИС"),
				СгенерироватьUUID(),
				ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.xml")) Тогда
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Ошибка добавления содержимого транспортного контейнера ""%1"".'"),
					ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайла).Имя);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ВыполнитьОбработкуОповещения(
					ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
			КонецЕсли;
			
		Иначе
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ВходящийКонтекст.ПараметрыФункции.Сообщение,
				СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ЭлектронноЦифроваяПодпись,
				Новый Структура("Строка64", Результат.СтрокаBase64),
				ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайлаПодписи).Имя,
				,
				ДокументДляПодписывания.ИмяФайла,
				ДокументДляПодписывания.Сертификат.Отпечаток,
				СтруктураСсылочныхДанных.Перечисления_СтатусыПроверкиЭЦП_Валидна,
				Истина) Тогда
				
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Ошибка добавления содержимого транспортного контейнера ""%1"".'"),
					ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайлаПодписи).Имя);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ВыполнитьОбработкуОповещения(
					ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
			КонецЕсли;
			
			Если Лев(ДокументДляПодписывания.ИмяФайла, 9) = "ON_DOVEL_" Тогда
				СтруктураИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДокументДляПодписывания.ИмяФайла);
				РасширениеДокументаДляПодписывания = СтруктураИмениФайла.Расширение;
				Если нрег(РасширениеДокументаДляПодписывания) = ".xml" Тогда
					ЕстьПриложениеСПодписью = Ложь;
					Для каждого ЭлементИзДокументовДляПодписывания Из ВходящийКонтекст.ДокументыДляПодписывания Цикл
						РасширениеЭлементаИзДокументовДляПодписывания = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(
							ЭлементИзДокументовДляПодписывания.ИмяФайла).Расширение;
						Если нрег(РасширениеЭлементаИзДокументовДляПодписывания) = ".sgn" Тогда
							ЕстьПриложениеСПодписью = Истина;
							Прервать;
						КонецЕсли;
					КонецЦикла;
					
					Если НЕ ЕстьПриложениеСПодписью Тогда
						ТипСодержимогоФайлаПриложения = ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn");
						ИмяФайлаПриложения = СтруктураИмениФайла.ИмяБезРасширения + "_SGN_"
							+ нрег(Строка(Новый УникальныйИдентификатор())) + ".sgn";
						Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
							ВходящийКонтекст.ПараметрыФункции.Сообщение,
							СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение,
							Новый Структура("Строка64", Результат.СтрокаBase64),
							ИмяФайлаПриложения,,,,,,,
							СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент(""),
							ТипСодержимогоФайлаПриложения) Тогда
							
							ТекстСообщения = СтрШаблон(
								НСтр("ru = 'Ошибка добавления содержимого транспортного контейнера ""%1"".'"),
								ИмяФайлаПриложения);
							ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
							ВыполнитьОбработкуОповещения(
								ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
							Возврат;
						КонецЕсли;
						ПриложениеДляПодписания = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ДокументДляПодписывания);
						ДанныеПриложения = Base64Значение(Результат.СтрокаBase64);
						ПриложениеДляПодписания.Данные = ПоместитьВоВременноеХранилище(ДанныеПриложения, Новый УникальныйИдентификатор);
						ПриложениеДляПодписания.ИмяФайла = ИмяФайлаПриложения;
						ПриложениеДляПодписания.ИмяФайлаПодписи = СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент();
						ПриложениеДляПодписания.ПолноеИмяФайла = ВходящийКонтекст.ВременныйКаталог + ИмяФайлаПриложения;
						ВходящийКонтекст.ДокументыДляПодписывания.Добавить(ПриложениеДляПодписания);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		ПодписатьСообщенияПоследовательно(ВходящийКонтекст, ВходящийКонтекст.ДокументыДляПодписывания, ВходящийКонтекст.ТекущийДокумент + 1, ВходящийКонтекст.ВременныйКаталог);
	Иначе
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка подписания документа
                  |%1.'"), ДокументДляПодписывания.ИмяФайла);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении, Ложь);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьТранспортнуюИнформациюДляСообщения(ОповещениеОЗавершении, КонтекстОтправки)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если ТипЗнч(КонтекстОтправки.ОбъектСертификатПолучателя) = Тип("Массив") Тогда
		СвойстваСертификатаПолучателя = КонтекстОтправки.ОбъектСертификатПолучателя[0];
	Иначе
		СвойстваСертификатаПолучателя = КонтекстОтправки.ОбъектСертификатПолучателя;
	КонецЕсли;
	Если ТипЗнч(СвойстваСертификатаПолучателя) = Тип("ФиксированнаяСтруктура") Тогда
		СвойстваСертификатаПолучателя = Новый Структура(СвойстваСертификатаПолучателя);
	КонецЕсли;
	
	КонтекстМоделиХраненияКлюча(КонтекстОтправки.УчетнаяЗапись, СвойстваСертификатаПолучателя);
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьТранспортнуюИнформациюДляСообщенияПослеЭкспортаСертификатаВBase64", ЭтотОбъект, Контекст);
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(
		Оповещение, СвойстваСертификатаПолучателя);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьТранспортнуюИнформациюДляСообщенияПослеЭкспортаСертификатаВBase64(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		СформироватьТранспортнуюИнформациюИСохранитьВИБ(ВходящийКонтекст.КонтекстОтправки.ТранспортноеСообщение, Результат.СтрокаBase64);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФРПослеВыполненияОтложенныхКомандНаКлиенте(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть аналогичная процедура СформироватьПакетПФРПослеВыполненияОтложенныхКоманд(),
	// которая используется при отправке отчетов из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// формируем файл описания пакета, создаем архив и помещаем в ИБ
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется запись состояния отправки в базу данных..."); // ПФР
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если НЕ СформироватьПакетПФР_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.Оповещать Тогда
		Оповестить("Изменение сообщения", Новый Структура("Сообщение, ЦиклОбмена, Предмет",
														КонтекстОтправки.ТранспортноеСообщение,
														КонтекстОтправки.ЦиклОбмена));
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПоследовательноОтложенно()
	ВыполнитьРасшифровкуСообщенийПоследовательно(КонтекстОтложеннойОперации);
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПоследовательно(ВходящийКонтекст)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
		
	Если ВходящийКонтекст.ТекущийДокумент < ДокументыПакета.Количество() Тогда
		ЭлементПакета = ДокументыПакета[ВходящийКонтекст.ТекущийДокумент];
		
		Оповещение = Новый ОписаниеОповещения(
			"ВыполнитьРасшифровкуСообщенийПоследовательноПослеРасшифровки", ЭтотОбъект, ВходящийКонтекст);
		Если ВходящийКонтекст.ТипСообщения = "ФНС_534" Тогда 
			АдресФайла = ?(ЗначениеЗаполнено(ЭлементПакета.АдресФайла), ЭлементПакета.АдресФайла, ЭлементПакета.АдресФайлаПодписи);
			АлгоритмФайла = ?(ЗначениеЗаполнено(ЭлементПакета.АдресФайла), ЭлементПакета.АлгоритмФайла, ЭлементПакета.АлгоритмФайлаПодписи);
		Иначе
			АдресФайла = ЭлементПакета.АдресФайла;
			АлгоритмФайла = ЭлементПакета.АлгоритмФайла;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(АдресФайла) Тогда
			Если ЭлементПакета.ДокументЗашифрован Тогда
				ПараметрыОперации = Новый Структура("АлгоритмШифрованияОбязателен", ВходящийКонтекст.АлгоритмШифрованияОбязателен);
				КриптографияЭДКОКлиент.РасшифроватьPKCS7(
					Оповещение,
					АдресФайла,,
					ВходящийКонтекст.СертификатПолучателя,,,
					АлгоритмФайла,
					ПараметрыОперации);
				Возврат;
			КонецЕсли;
		Иначе
			ВходящийКонтекст.ТекущийДокумент = ВходящийКонтекст.ТекущийДокумент + 1;
			ВыполнитьРасшифровкуСообщенийПоследовательно(ВходящийКонтекст);
			Возврат;
		КонецЕсли;
		
		ВыполнитьРасшифровкуСообщенийПоследовательноПослеРасшифровки(Новый Структура("Выполнено", Истина), ВходящийКонтекст);
		
	Иначе
		Если ВходящийКонтекст.ТипСообщения = "ПФР" И (НЕ ВходящийКонтекст.Свойство("ФорматДокументооборота")
			ИЛИ ВходящийКонтекст.ФорматДокументооборота <> ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК")) Тогда
			// разбираем дополнительные описания с целью восстановить исходные имена файлов
			ВосстановитьИсходныеИменаФайлов(ВходящийКонтекст.СсылкаНаОбъект);
		КонецЕсли;
		
		// помещаем файлы в регистр содержимого, повторно распознаем получателя после расшифровки содержимого
		Если НЕ ПоместитьФайлыВРегистрСодержимого(ВходящийКонтекст.СсылкаНаОбъект) Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		ЗафиксироватьПопыткуРасшифровки(ВходящийКонтекст.СсылкаНаОбъект, Истина);
		СвойстваОбъекта = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначенияРеквизитовОбъекта(
			ВходящийКонтекст.СсылкаНаОбъект, "ЦиклОбмена.Предмет, ЦиклОбмена.Организация");
		ОбновитьСостояниеЭДОсПФРПриЗаписиТранспортногоСообщения(СвойстваОбъекта.ЦиклОбменаПредмет,
			?(ВходящийКонтекст.ТипСообщения = "ПФР", ВходящийКонтекст.СсылкаНаОбъект, Неопределено));
		
		// особая обработка в случае неформализованных документов налогового органа, документов реализации своих полномочий
		// уведомлений об уточнении и протоколов приема 2-НДФЛ
		РезультатОбработки = ОбработатьНеформализованныеДокументы(ВходящийКонтекст.СсылкаНаОбъект);
		ВходящийКонтекст.Вставить("РезультатОбработкиНеформализованныхДокументов", РезультатОбработки);
		Если НЕ РезультатОбработки.Выполнено Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		Если РезультатОбработки.ИзменилсяСтатусМЧДФНС = Истина Тогда
			ПараметрыПриложения["ДокументооборотСКОКлиент.ИзменилсяСтатусМЧДФНС"] = Истина;
		КонецЕсли;
		
		ОтправкаОтчетностиВИнтерфакс = "РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОтправкаОтчетностиВИнтерфакс";
		Если ОбщегоНазначенияКлиент.ПодсистемаСуществует(ОтправкаОтчетностиВИнтерфакс) Тогда
			МодульОтправкаРегОтчетовВСПАРККлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ОтправкаРегОтчетовВСПАРККлиент");
			МодульОтправкаРегОтчетовВСПАРККлиент.ОтправитьОтчетВСПАРКПриНеобходимостиПоТранспортномуСообщению(ВходящийКонтекст.СсылкаНаОбъект);
		КонецЕсли;
		
		Попытка
			Источник = Новый Структура();
			Источник.Вставить("Ссылка", СвойстваОбъекта.ЦиклОбменаПредмет);
			Источник.Вставить("Организация", СвойстваОбъекта.ЦиклОбменаОрганизация);
			Источник.Вставить("МетодВызова", "РасшифроватьТранспортноеСообщение");
			Оповестить("Запись_РегламентированныйОтчет", Источник);
		Исключение
		КонецПопытки;
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ОтправитьУПУППослеМЧДФНС",
			ЭтотОбъект, ВходящийКонтекст);
			
		Если (ТипЗнч(СвойстваОбъекта.ЦиклОбменаПредмет) = Тип("СправочникСсылка.МашиночитаемыеДоверенностиФНС")
			ИЛИ ТипЗнч(СвойстваОбъекта.ЦиклОбменаПредмет) = Тип("СправочникСсылка.ЗаявленияОбОтзывеМЧДФНС"))
			И НЕ ЭтоОбменИзСтарогоЖурналаОбмена(ВходящийКонтекст) Тогда
			
			ДокументооборотСКОКлиент.НастроитьМЧДФНС(ОписаниеОповещения);
			
		Иначе
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьУПУППослеМЧДФНС(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ДействияПослеОбмена",
		ЭтотОбъект, ВходящийКонтекст);
			
	Если НЕ ЭтоОбменИзСтарогоЖурналаОбмена(ВходящийКонтекст)
		И ВходящийКонтекст.РезультатОбработкиНеформализованныхДокументов.ИзменилсяСтатусЗПЭД Тогда
		
		ОтправитьУПУППослеОбмена(ОписаниеОповещения);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ЭтоОбменИзСтарогоЖурналаОбмена(ВходящийКонтекст)
	
	Возврат ДокументооборотСКОКЛиент.ЕстьИменаПроцедурВОповещенияхОЗавершении(
		"ОсуществитьОбменПоУчетнойЗаписи", 
		ВходящийКонтекст.ОповещениеОЗавершении);
	
КонецФункции

&НаКлиенте
Процедура ДействияПослеОбмена(Результат, ВходящийКонтекст) Экспорт
	
	// Если при создании подтверждения из панели отправки отчета или формы этапов отправки
	// оказалось, что сообщение, на которое создается подтверждение, не расшифровано,
	// то мы пытаемся расшифровать его принудительно и теперь нужно попытаться повторно создать на него подтверждение
	// в процедуре СоздатьПодтверждениеПослеРасшифровки.
	// Если оно было расшифровано, то создаем на него подтверждение.
	
	ДополнительныеПараметры = ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры;
	ЭтоОтправкаПодтвержденияИзПанелиОтправки = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ЭтоОтправкаПодтвержденияИзПанелиОтправки");
	
	Если ЭтоОтправкаПодтвержденияИзПанелиОтправки Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ВыполнитьРасшифровкуСообщенийПослеСозданияПодтверждения", ЭтотОбъект, ВходящийКонтекст);
			
		СоздатьПодтверждениеПриНеобходимости(Оповещение, ВходящийКонтекст.СсылкаНаОбъект)
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПоследовательноПослеНастройкиМЧДФНС(Результат, ВходящийКонтекст) Экспорт
	
	// Если при создании подтверждения из панели отправки отчета или формы этапов отправки
	// оказалось, что сообщение, на которое создается подтверждение, не расшифровано,
	// то мы пытаемся расшифровать его принудительно и теперь нужно попытаться повторно создать на него подтверждение
	// в процедуре СоздатьПодтверждениеПослеРасшифровки.
	// Если оно было расшифровано, то создаем на него подтверждение.
	
	ДополнительныеПараметры = ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры;
	ЭтоОтправкаПодтвержденияИзПанелиОтправки = ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ЭтоОтправкаПодтвержденияИзПанелиОтправки");
	
	Если ЭтоОтправкаПодтвержденияИзПанелиОтправки Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ВыполнитьРасшифровкуСообщенийПослеСозданияПодтверждения", ЭтотОбъект, ВходящийКонтекст);
			
		СоздатьПодтверждениеПриНеобходимости(Оповещение, ВходящийКонтекст.СсылкаНаОбъект)
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПослеСозданияПодтверждения(Результат, ВходящийКонтекст) Экспорт
	
	ВходящийКонтекст.Вставить("Подтверждение", Результат);
	Оповещение = Новый ОписаниеОповещения(
			"ВыполнитьРасшифровкуСообщенийПослеСозданияРезультатПриема", ЭтотОбъект, ВходящийКонтекст);
			
	СоздатьРезультатПриемаНаКлиентеСУчетомВопроса(Оповещение, ВходящийКонтекст.СсылкаНаОбъект, Истина, , Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПослеСозданияРезультатПриема(Результат, ВходящийКонтекст) Экспорт
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПоследовательноПослеРасшифровки(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПослеЗавершенияРасшифровки(Результат, ВходящийКонтекст);
	
	Если Результат.Выполнено Тогда
		Если ВходящийКонтекст.ТекущийДокумент >= ДокументыПакета.Количество() Тогда
			ТекстОшибки = СтрШаблон(
				НСтр("ru = 'Документ с индексом %1 отсутствует в пакете (количество документов пакета: %2).'"),
				Строка(ВходящийКонтекст.ТекущийДокумент),
				Строка(ДокументыПакета.Количество()));
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Электронный документооборот с контролирующими органами. Расшифровка'"),
				"Предупреждение",
				ТекстОшибки,,
				Истина);
			Возврат;
		КонецЕсли;
		
		ЭлементПакета = ДокументыПакета[ВходящийКонтекст.ТекущийДокумент];
		Если ВходящийКонтекст.ТипСообщения = "ФНС_534" Тогда 
			АдресФайла = ?(ЗначениеЗаполнено(ЭлементПакета.АдресФайла), ЭлементПакета.АдресФайла, ЭлементПакета.АдресФайлаПодписи);
		Иначе
			АдресФайла = ЭлементПакета.АдресФайла;
		КонецЕсли;
		Если ЭлементПакета.ДокументСжат Тогда		
			Если Не ЗначениеЗаполнено(АдресФайла) Тогда
				ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
			Иначе
				ЭтоФайлПодписи = ВходящийКонтекст.ТипСообщения = "ФНС_534" И НЕ ЗначениеЗаполнено(ЭлементПакета.АдресФайла);
				ТипАрхива = ?(ВходящийКонтекст.ТипСообщения = "ПФР",
					?(ЗначениеЗаполнено(ВходящийКонтекст.ФорматДокументооборота),
					?(ВходящийКонтекст.ФорматДокументооборота = 
					ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК"), "gzip", "zip"),
					Неопределено), "zip");
				АдресФайла = РазархивироватьФайлИзВременногоХранилища(АдресФайла, НЕ ЭтоФайлПодписи, ТипАрхива);
				Если ВходящийКонтекст.ТипСообщения = "ПФР" И ТипАрхива = "gzip"
					И НЕ ЗначениеЗаполнено(ВходящийКонтекст.ФорматДокументооборота) Тогда
					
					ВходящийКонтекст.ФорматДокументооборота =
						ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК");
				КонецЕсли;
				
				Если АдресФайла <> Неопределено ИЛИ НЕ ЭтоФайлПодписи Тогда
					ЭлементПакета.АдресФайла = АдресФайла;
					Если ВходящийКонтекст.ТипСообщения = "ФНС_534" Тогда
						Если ЗначениеЗаполнено(ЭлементПакета.АдресФайла) Тогда
							ЭлементПакета.АдресФайла = АдресФайла;
						Иначе
							ЭлементПакета.АдресФайлаПодписи = АдресФайла;
						КонецЕсли;
					Иначе
						ЭлементПакета.АдресФайла = АдресФайла;
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Если ВходящийКонтекст.ТипСообщения = "ФСГС" Тогда 
			Если ЭлементПакета.ТипДокумента = ПредопределенноеЗначение("Перечисление.ТипыСодержимогоТранспортногоКонтейнера.ШаблонФСГС") Тогда
				Попытка
					ПараметрыШаблона = Новый Структура("АдресФайла, ИмяФайла", ЭлементПакета.АдресФайла, ЭлементПакета.ИмяФайла);
					ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ПриПолученииНовогоШаблонаФСГС(ПараметрыШаблона);
				Исключение
				КонецПопытки;
			КонецЕсли;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ЭлементПакета.АдресФайлаПодписи) ИЛИ ВходящийКонтекст.ТипСообщения = "ПФР"
			И ВходящийКонтекст.ФорматДокументооборота =
			ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
			
			МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст.СертификатПолучателя);
			
			Оповещение = Новый ОписаниеОповещения(
				"ВыполнитьРасшифровкуСообщенийПоследовательноПослеПроверкиПодписи", ЭтотОбъект, ВходящийКонтекст);
			Если ЗначениеЗаполнено(ЭлементПакета.АдресФайла) Тогда
				Если ВходящийКонтекст.ТипСообщения = "ПФР" И ВходящийКонтекст.ФорматДокументооборота =
					ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК") Тогда
					
					Результат = Новый Структура("Выполнено, ПодписьВалидна, Подписанты", Истина, Неопределено, Неопределено);
					ВыполнитьОбработкуОповещения(Оповещение, Результат);
					
				Иначе
					КриптографияЭДКОКлиент.ПроверитьПодписьPKCS7(
						Оповещение,
						МестоХраненияКлюча,
						ЭлементПакета.АдресФайлаПодписи,
						ЭлементПакета.АдресФайла,,
						Ложь,,
						ЭлементПакета.АлгоритмФайлаПодписи);
				КонецЕсли;
			Иначе
				Результат = Новый Структура("Выполнено, ПодписьВалидна, Подписанты", Истина, Неопределено, Неопределено);
				ВыполнитьОбработкуОповещения(Оповещение, Результат);
			КонецЕсли;
			Возврат;
		КонецЕсли;
	Иначе
		
		Если ВходящийКонтекст.ПараметрыПоказа <> Неопределено И ВходящийКонтекст.ПараметрыПоказа.ПоказДляДругихПользователей
			ИЛИ ТранспортноеСообщениеЗашифрованоДляТекущегоПользователя(ВходящийКонтекст.СсылкаНаОбъект) Тогда
			
			ТекстСообщения = НСтр("ru = 'Ошибка расшифровки файла документа при распаковке пакета.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				ТекстСообщения,,,,,
				НовыйКонтекстРасшифровки(ВходящийКонтекст.СсылкаНаОбъект, ВходящийКонтекст.ПараметрыПоказа));
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ВходящийКонтекст.ТекущийДокумент = ВходящийКонтекст.ТекущийДокумент + 1;
	ВыполнитьРасшифровкуСообщенийПоследовательно(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьРасшифровкуСообщенийПоследовательноПослеПроверкиПодписи(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если НЕ Результат.Выполнено ИЛИ Результат.ПодписьВалидна = Ложь Тогда
		ИмяСобытия = НСтр("ru = 'Электронный документооборот с контролирующими органами. Проверка подписи'");
		ТекстСообщения = НСтр("ru = 'Ошибка проверки подписи файла документа при распаковке пакета ""%1"" (тип сообщения: ""%2"", "
			+ "тип документа: ""%3"", имя файла: ""%4"", идентификатор: ""%5"", в модели сервиса: %6). %7'");
		
		Если Результат.Свойство("ОписаниеОшибки") Тогда
			ТекстОшибки = Результат.ОписаниеОшибки;
		Иначе
			ТекстОшибки = НСтр("ru = 'Подпись невалидна.'");
		КонецЕсли;
		
		Если Результат.Свойство("Подписанты") И Результат.Подписанты <> Неопределено И Результат.Подписанты.Количество() > 0 Тогда
			ТекстИнформацииОСертификате = НСтр("ru = 'Проверка сертификатом поставщика ""%1"" с серийным номером ""%2"".'");
			ТекстОшибки = ТекстОшибки + " " + СтрШаблон(
				ТекстИнформацииОСертификате,
				Результат.Подписанты[0].Поставщик,
				Результат.Подписанты[0].СерийныйНомер);
		КонецЕсли;
		
		ТекстСообщения = СтрШаблон(
			ТекстСообщения,
			Строка(ВходящийКонтекст.СсылкаНаОбъект),
			ВходящийКонтекст.ТипСообщения,
			Строка(ДокументыПакета[ВходящийКонтекст.ТекущийДокумент].ТипДокумента),
			ДокументыПакета[ВходящийКонтекст.ТекущийДокумент].ИмяФайла,
			ДокументыПакета[ВходящийКонтекст.ТекущийДокумент].Идентификатор,
			Строка(НЕ ЭтоЛокальнаяПодпись(ВходящийКонтекст.СертификатПолучателя)),
			ТекстОшибки);
		
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(ИмяСобытия, "Предупреждение", ТекстСообщения,, Истина);
	КонецЕсли;
	
	ЭлементПакета = ДокументыПакета[ВходящийКонтекст.ТекущийДокумент];
	ЭлементПакета.СтатусПроверкиПодписи = ?(Результат.Выполнено И Результат.ПодписьВалидна = Неопределено,
		ПредопределенноеЗначение("Перечисление.СтатусыПроверкиЭЦП.ПустаяСсылка"),
		?(Результат.Выполнено И Результат.ПодписьВалидна,
		ПредопределенноеЗначение("Перечисление.СтатусыПроверкиЭЦП.Валидна"),
		ПредопределенноеЗначение("Перечисление.СтатусыПроверкиЭЦП.НеВалидна")));
	Если Результат.Свойство("Подписанты") И Результат.Подписанты <> Неопределено
		И Результат.Подписанты.Количество() > 0 И Результат.Подписанты[0].Свойство("Отпечаток") Тогда
		ЭлементПакета.ОтпечатокСертификатаПодписи = Результат.Подписанты[0].Отпечаток;
	Иначе
		ЭлементПакета.ОтпечатокСертификатаПодписи = "";
	КонецЕсли;
	
	ВходящийКонтекст.ТекущийДокумент = ВходящийКонтекст.ТекущийДокумент + 1;
	ВыполнитьРасшифровкуСообщенийПоследовательно(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВПФРПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт 

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	Документ = ВходящийКонтекст.КонтекстОтправки.Документ;
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);

		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР.
		
		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(ВходящийКонтекст.КонтекстОтправки) Тогда // сервер

		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ПФР.
		
		Возврат;
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ПФР.
		
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаСРасширениемРаботыСФайлами(ДополнительныеПараметры) Экспорт
	
	Путь = ДополнительныеПараметры.Путь;
	
	// Выбор каталога для сохранения файлов цикла обмена
	Если ЗначениеЗаполнено(Путь) Тогда
		
		ВыбранныеФайлы = Новый Массив;
		ВыбранныеФайлы.Вставить(Путь);
		
		ВыгрузитьЦиклыОбменаПослеВыбораКаталога(ВыбранныеФайлы, ДополнительныеПараметры);
		
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыгрузитьЦиклыОбменаПослеВыбораКаталога", 
			ЭтотОбъект, 
			ДополнительныеПараметры); 
		
		Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
		Диалог.Заголовок = "Укажите каталог";
		Диалог.ПроверятьСуществованиеФайла = Истина;
		Диалог.Показать(ОписаниеОповещения);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаПослеВыбораКаталога(ВыбранныеФайлы, ДополнительныеПараметры) Экспорт
	
	Если ВыбранныеФайлы = Неопределено ИЛИ ВыбранныеФайлы.Количество() = 0 Тогда
		Возврат;
	Иначе
		Путь = ВыбранныеФайлы[0];
		ДополнительныеПараметры.Вставить("Путь", Путь);
	КонецЕсли;
	
	ЦиклыОбмена 				= ДополнительныеПараметры.ЦиклыОбмена;
	ОбщееЧислоЦикловОбмена 		= ЦиклыОбмена.Количество();
	ПредставленияЦикловОбмена 	= ПолучитьПредставленияЦикловОбмена(ЦиклыОбмена);
	
	ДополнительныеПараметры.Вставить("СоответствиеСообщенияКаталогу", 	Новый Соответствие);
	ДополнительныеПараметры.Вставить("СоответствиеТипаСообщенияКраткомуПредставлению", 	ИнициализироватьСоответствиеТСКП());
	ДополнительныеПараметры.Вставить("ОбщееЧислоЦикловОбмена", 			ОбщееЧислоЦикловОбмена);
	ДополнительныеПараметры.Вставить("ПредставленияЦикловОбмена", 		ПредставленияЦикловОбмена);
	ДополнительныеПараметры.Вставить("ЧислоВыгруженныхЦикловОбмена", 	0);
	ДополнительныеПараметры.Вставить("ЧислоВыгруженныхФайлов", 			0);
	ДополнительныеПараметры.Вставить("МассивОписанийПолучаемыеФайлы", 	Новый Массив);
	
	ВыгрузитьЦиклыОбменаПоКаждомуЦиклуОбмена(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаПоКаждомуЦиклуОбмена(ДополнительныеПараметры)
	
	ОбщееЧислоЦикловОбмена			= ДополнительныеПараметры.ОбщееЧислоЦикловОбмена;
	ЦиклыОбмена 					= ДополнительныеПараметры.ЦиклыОбмена;
	Путь 							= ДополнительныеПараметры.Путь;
	ОбщееКоличествоФайлов 			= ДополнительныеПараметры.ОбщееКоличествоФайлов;
	ЧислоВыгруженныхЦикловОбмена	= ДополнительныеПараметры.ЧислоВыгруженныхЦикловОбмена; 
	МассивОписанийПолучаемыеФайлы	= ДополнительныеПараметры.МассивОписанийПолучаемыеФайлы;
	
	Если ЧислоВыгруженныхЦикловОбмена < ОбщееЧислоЦикловОбмена Тогда
		
		// формируем текущую порцию циклов обмена
		ПорцияЦиклов = Новый Массив;
		РазмерПорции = 10;
		Для Инд = ЧислоВыгруженныхЦикловОбмена + 1 По Мин(ЧислоВыгруженныхЦикловОбмена + РазмерПорции, ОбщееЧислоЦикловОбмена) Цикл
			ПорцияЦиклов.Добавить(ЦиклыОбмена[Инд - 1]);
		КонецЦикла;
		ЧислоВыгруженныхЦикловОбмена = ЧислоВыгруженныхЦикловОбмена + РазмерПорции;
		ДополнительныеПараметры.Вставить("ЧислоВыгруженныхЦикловОбмена", ЧислоВыгруженныхЦикловОбмена);
		
		// получаем выгружаемые файлы
		ВыгружатьКонтейнеры 	= ДополнительныеПараметры.ВыгружатьКонтейнеры;
		ВыгружатьСодержимое 	= ДополнительныеПараметры.ВыгружатьСодержимое;

		ТранспортныеСообщения = ПолучитьВыгружаемыеФайлы(ПорцияЦиклов, ВыгружатьКонтейнеры, ВыгружатьСодержимое);
		
		ОбщееЧислоТранспортныхСообщений = ТранспортныеСообщения.Количество();
		
		// для каждого из выгружаемых файлов...
		ДополнительныеПараметры.Вставить("МассивОписанийПолучаемыеФайлы", 			Новый Массив);
		ДополнительныеПараметры.Вставить("ЧислоВыгруженныхТранспортныхСообщений", 	0);
		ДополнительныеПараметры.Вставить("ОбщееЧислоТранспортныхСообщений", 		ОбщееЧислоТранспортныхСообщений);
		ДополнительныеПараметры.Вставить("ТранспортныеСообщения", 					ТранспортныеСообщения);
			
		ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбмена(ДополнительныеПараметры);
		
	Иначе
		
		ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(МассивОписанийПолучаемыеФайлы);

	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбмена(ДополнительныеПараметры)
		
	Путь 											= ДополнительныеПараметры.Путь;
	СоответствиеСообщенияКаталогу					= ДополнительныеПараметры.СоответствиеСообщенияКаталогу;
	СоответствиеТипаСообщенияКраткомуПредставлению	= ДополнительныеПараметры.СоответствиеТипаСообщенияКраткомуПредставлению;
	ЧислоВыгруженныхЦикловОбмена					= ДополнительныеПараметры.ЧислоВыгруженныхЦикловОбмена;
	МассивОписанийПолучаемыеФайлы					= ДополнительныеПараметры.МассивОписанийПолучаемыеФайлы;
	ОбщееЧислоТранспортныхСообщений					= ДополнительныеПараметры.ОбщееЧислоТранспортныхСообщений;
	ЧислоВыгруженныхТранспортныхСообщений			= ДополнительныеПараметры.ЧислоВыгруженныхТранспортныхСообщений;
	ПредставленияЦикловОбмена 						= ДополнительныеПараметры.ПредставленияЦикловОбмена;
	ТранспортныеСообщения 							= ДополнительныеПараметры.ТранспортныеСообщения;
	
	Если ЧислоВыгруженныхТранспортныхСообщений <= ОбщееЧислоТранспортныхСообщений - 1 Тогда
		
		ОбработкаПрерыванияПользователя();
		
		СодержимоеТранспортногоСообщения = ТранспортныеСообщения[ЧислоВыгруженныхТранспортныхСообщений];
		ДополнительныеПараметры.Вставить("СодержимоеТранспортногоСообщения", СодержимоеТранспортногоСообщения);
		
		ЦиклОбмена 			= СодержимоеТранспортногоСообщения.ТранспортноеСообщениеЦиклОбмена;
		Сообщение 			= СодержимоеТранспортногоСообщения.ТранспортноеСообщение;
		ТипСообщения 		= СодержимоеТранспортногоСообщения.ТранспортноеСообщениеТип;
		
		ПредставлениеЦиклаОбмена 			= ПредставленияЦикловОбмена[ЦиклОбмена];
		КраткоеПредставлениеТипаСообщения 	= СоответствиеТипаСообщенияКраткомуПредставлению[ТипСообщения];
		ДополнительныеПараметры.Вставить("КраткоеПредставлениеТипаСообщения",	КраткоеПредставлениеТипаСообщения);
		ДополнительныеПараметры.Вставить("ПредставлениеЦиклаОбмена", 			ПредставлениеЦиклаОбмена);
		
		Если НЕ ЗначениеЗаполнено(КраткоеПредставлениеТипаСообщения) Тогда
			КраткоеПредставлениеТипаСообщения = ТипСообщения;
		КонецЕсли;
		
		// если сообщение уже выгружалось, то используем каталог предыдущей выгрузки
		КаталогВыгрузки = СоответствиеСообщенияКаталогу[Сообщение];
		Если КаталогВыгрузки = Неопределено Тогда
			
			КаталогВыгрузки = Путь;
			КаталогВыгрузки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВыгрузки);
			
			// дополняем путь каталогом цикла обмена
			ДополнятьПуть = ДополнительныеПараметры.ДополнятьПуть;
			Если ДополнятьПуть Тогда
				КаталогВыгрузки = КаталогВыгрузки + СокрЛП(СформироватьИмяФайла(ПредставлениеЦиклаОбмена));
				КаталогВыгрузки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВыгрузки);
			КонецЕсли;
			
			ПолныйКаталогВыгрузки = КаталогВыгрузки + КраткоеПредставлениеТипаСообщения;
			ПолныйКаталогВыгрузки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(ПолныйКаталогВыгрузки);
			
			// дополняем путь каталогом типа сообщения
			ДополнительныеПараметры.Вставить("НомерКаталога", 			1);
			ДополнительныеПараметры.Вставить("КаталогВыгрузки", 		КаталогВыгрузки);
			ДополнительныеПараметры.Вставить("ПолныйКаталогВыгрузки", 	ПолныйКаталогВыгрузки);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПроверкиСуществованияКаталогаВыгрузки", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
				
			ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, ПолныйКаталогВыгрузки);
			
		Иначе
			
			ДополнительныеПараметры.Вставить("КаталогВыгрузки", КаталогВыгрузки);
			ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПодбораИмениКаталогаВыгрузки(ДополнительныеПараметры);
			
		КонецЕсли;
		
	Иначе
		
		ВыгрузитьЦиклыОбменаПоКаждомуЦиклуОбмена(ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПроверкиСуществованияКаталогаВыгрузки(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено Тогда
		
		Если Результат.СвойстваФайла.Существует Тогда
			
			// Если каталог уже существует, то пытаемся создать новый каталог путем прибавления 1 к счетчику: (1)
			НомерКаталога 						= ДополнительныеПараметры.НомерКаталога;
			ПолныйКаталогВыгрузки 				= ДополнительныеПараметры.ПолныйКаталогВыгрузки;
			КаталогВыгрузки 					= ДополнительныеПараметры.КаталогВыгрузки;
			КраткоеПредставлениеТипаСообщения 	= ДополнительныеПараметры.КраткоеПредставлениеТипаСообщения;
			
			НомерКаталога 		  = НомерКаталога + 1;
			ПолныйКаталогВыгрузки = КаталогВыгрузки + КраткоеПредставлениеТипаСообщения + " (" + Формат(НомерКаталога, "ЧГ=") + ")\";
			
			ДополнительныеПараметры.Вставить("НомерКаталога", 			НомерКаталога);
			ДополнительныеПараметры.Вставить("ПолныйКаталогВыгрузки", 	ПолныйКаталогВыгрузки);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПроверкиСуществованияКаталогаВыгрузки", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
				
			ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, ПолныйКаталогВыгрузки);
			
		Иначе
			
			КаталогВыгрузки = ДополнительныеПараметры.ПолныйКаталогВыгрузки;
			ДополнительныеПараметры.КаталогВыгрузки = КаталогВыгрузки;
			
			// запоминаем путь сообщения
			СодержимоеТранспортногоСообщения 	= ДополнительныеПараметры.СодержимоеТранспортногоСообщения;
			Сообщение 							= СодержимоеТранспортногоСообщения.ТранспортноеСообщение;
			СоответствиеСообщенияКаталогу		= ДополнительныеПараметры.СоответствиеСообщенияКаталогу;
			
			СоответствиеСообщенияКаталогу.Вставить(Сообщение, КаталогВыгрузки);
			ДополнительныеПараметры.Вставить("СоответствиеСообщенияКаталогу", СоответствиеСообщенияКаталогу);
			
			ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПодбораИмениКаталогаВыгрузки(ДополнительныеПараметры);
			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПодбораИмениКаталогаВыгрузки(ДополнительныеПараметры)
	
	ОбщееЧислоТранспортныхСообщений			= ДополнительныеПараметры.ОбщееЧислоТранспортныхСообщений;
	ЧислоВыгруженныхТранспортныхСообщений	= ДополнительныеПараметры.ЧислоВыгруженныхТранспортныхСообщений;
	СоответствиеСообщенияКаталогу			= ДополнительныеПараметры.СоответствиеСообщенияКаталогу;
	СодержимоеТранспортногоСообщения		= ДополнительныеПараметры.СодержимоеТранспортногоСообщения;
	КаталогВыгрузки 						= ДополнительныеПараметры.КаталогВыгрузки;
	КраткоеПредставлениеТипаСообщения		= ДополнительныеПараметры.КраткоеПредставлениеТипаСообщения;
	ПредставлениеЦиклаОбмена				= ДополнительныеПараметры.ПредставлениеЦиклаОбмена;
		
	// Содержимое выгружаем в подкаталог "Содержимое" каталога сообщения
	Если СодержимоеТранспортногоСообщения.Свойство("ИДСодержимое") И СодержимоеТранспортногоСообщения.ИДСодержимое = "Содержимое" 
		ИЛИ СодержимоеТранспортногоСообщения.Свойство("ИДКонтейнеры") И СодержимоеТранспортногоСообщения.ИДКонтейнеры = "Контейнеры" Тогда
		КаталогВыгрузки = КаталогВыгрузки + "Содержимое\";
	КонецЕсли;
	
	// прорисовываем состояние в форме прогресса
	// используем состояние
	ЧислоВыгруженныхТранспортныхСообщений = ЧислоВыгруженныхТранспортныхСообщений + 1;
	Состояние(ПредставлениеЦиклаОбмена, ЧислоВыгруженныхТранспортныхСообщений / ОбщееЧислоТранспортныхСообщений * 100, КраткоеПредставлениеТипаСообщения);
	
	ДополнительныеПараметры.Вставить("ЧислоВыгруженныхТранспортныхСообщений", 	ЧислоВыгруженныхТранспортныхСообщений);
	ДополнительныеПараметры.Вставить("КаталогВыгрузки", 						КаталогВыгрузки);
	
	// Проверяем существование каталога. Если не существует - то создаем.
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПроверкиСуществованияКаталога", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, КаталогВыгрузки);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеПроверкиСуществованияКаталога(Результат, ДополнительныеПараметры) Экспорт
		
	Если Результат.Выполнено Тогда 
		
		КаталогВыгрузки = ДополнительныеПараметры.КаталогВыгрузки;
		Если Результат.СвойстваФайла.Существует Тогда
			
			ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеСозданияКаталога(КаталогВыгрузки, ДополнительныеПараметры);
		
		Иначе
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеСозданияКаталога", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
				
			// если каталог выгрузки не существует, то пытаемся создать
			НачатьСозданиеКаталога(ОписаниеОповещения, КаталогВыгрузки);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбменаПослеСозданияКаталога(КаталогВыгрузки, ДополнительныеПараметры) Экспорт
	
	Если ЗначениеЗаполнено(КаталогВыгрузки) Тогда
		
		Если Прав(КаталогВыгрузки, 1) <> ПолучитьРазделительПути() Тогда
			КаталогВыгрузки = КаталогВыгрузки + ПолучитьРазделительПути();
		КонецЕсли;
		
		СодержимоеТранспортногоСообщения = ДополнительныеПараметры.СодержимоеТранспортногоСообщения;
		ДополнительныеПараметры.Вставить("КаталогВыгрузки", КаталогВыгрузки);
			
		// если в каталоге выгрузки уже присутствует файл с тем же именем,
		// то добавляем в конце имени выгружаемого файла счетчик
		Инд = 1;
		ПолноеИмяФайлаБезПрефикса = КаталогВыгрузки + СформироватьИмяФайла(СодержимоеТранспортногоСообщения.ИмяФайла);
		ПолноеИмяФайла = ПолноеИмяФайлаБезПрефикса;
		
		// определяем вхождение последней точки
		ПоследнееВхожденияТочки = 0;
		ДлинаПолногоИмениФайла = СтрДлина(ПолноеИмяФайлаБезПрефикса);
		Для Инд = 1 По ДлинаПолногоИмениФайла Цикл
			ТекИнд = ДлинаПолногоИмениФайла - Инд + 1;
			Если Сред(ПолноеИмяФайлаБезПрефикса, ТекИнд, 1) = "." Тогда
				ПоследнееВхожденияТочки = ТекИнд;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		ДополнительныеПараметры.Вставить("ПолноеИмяФайла", 				ПолноеИмяФайла);
		ДополнительныеПараметры.Вставить("ПолноеИмяФайлаБезПрефикса", 	ПолноеИмяФайлаБезПрефикса);
		ДополнительныеПараметры.Вставить("ПоследнееВхожденияТочки", 	ПоследнееВхожденияТочки);
		ДополнительныеПараметры.Вставить("Инд", 						1);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыгрузитьТранспортноеСообщениеПослеПроверкиСуществованияПутиКФайлу", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, ПолноеИмяФайла);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьТранспортноеСообщениеПослеПроверкиСуществованияПутиКФайлу(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат.Выполнено Тогда
		
		МассивОписанийПолучаемыеФайлы			= ДополнительныеПараметры.МассивОписанийПолучаемыеФайлы;
		ОбщееЧислоТранспортныхСообщений			= ДополнительныеПараметры.ОбщееЧислоТранспортныхСообщений;
		ЧислоВыгруженныхТранспортныхСообщений	= ДополнительныеПараметры.ЧислоВыгруженныхТранспортныхСообщений;
		ТранспортныеСообщения 					= ДополнительныеПараметры.ТранспортныеСообщения;
		ПолноеИмяФайлаБезПрефикса 				= ДополнительныеПараметры.ПолноеИмяФайлаБезПрефикса;
		ПолноеИмяФайла 							= ДополнительныеПараметры.ПолноеИмяФайла;
		ПоследнееВхожденияТочки 				= ДополнительныеПараметры.ПоследнееВхожденияТочки;
		СодержимоеТранспортногоСообщения 		= ДополнительныеПараметры.СодержимоеТранспортногоСообщения;
		Инд 									= ДополнительныеПараметры.Инд;
		
		Если Результат.СвойстваФайла.Существует Тогда
		
			Инд = Инд + 1;
			Если ПоследнееВхожденияТочки = 0 Тогда
				ПолноеИмяФайла = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = '%1 (%2)'"), 
					ПолноеИмяФайлаБезПрефикса,
					Формат(Инд, "ЧГ="));
			Иначе
				ПолноеИмяФайла = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = '%1 (%2) %3'"), 	
					Лев(ПолноеИмяФайлаБезПрефикса, ПоследнееВхожденияТочки - 1),
					Формат(Инд, "ЧГ="),
					Сред(ПолноеИмяФайлаБезПрефикса, ПоследнееВхожденияТочки)); 
			КонецЕсли;
			
			ДополнительныеПараметры.Инд 			= Инд;
			ДополнительныеПараметры.ПолноеИмяФайла 	= ПолноеИмяФайла;
			
			// Проверяем существование заново
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ВыгрузитьТранспортноеСообщениеПослеПроверкиСуществованияПутиКФайлу", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
				
			ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, ПолноеИмяФайла);
		
		Иначе
		
			// Адрес данных и сформированное имя файла помещается в массив описаний
			
			АдресВоВременномХранилище 	= СодержимоеТранспортногоСообщения.Адрес;
			ОписаниеФайла 				= Новый ОписаниеПередаваемогоФайла(ПолноеИмяФайла, АдресВоВременномХранилище); 
			
			МассивОписанийПолучаемыеФайлы.Добавить(ОписаниеФайла);
			ДополнительныеПараметры.Вставить("МассивОписанийПолучаемыеФайлы", МассивОписанийПолучаемыеФайлы);
			
			ВыгрузитьТранспортноеСообщениеПоКаждомуЦиклуОбмена(ДополнительныеПараметры);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьЦиклыОбменаБезРасширенияРаботыСФайлами(ДополнительныеПараметры) Экспорт
	
	ЦиклыОбмена 			= ДополнительныеПараметры.ЦиклыОбмена;
	ВыгружатьКонтейнеры 	= ДополнительныеПараметры.ВыгружатьКонтейнеры;
	ВыгружатьСодержимое 	= ДополнительныеПараметры.ВыгружатьСодержимое;
	
	// количество циклов обмена, по которым за один раз считывается информация из ИБ
	РазмерПорции = 10;
	
	// формируем представления циклов обмена
	ПредставленияЦикловОбмена = ПолучитьПредставленияЦикловОбмена(ЦиклыОбмена);
	
	ЧислоВыгруженныхФайлов = 0;
	ЧислоВыгруженныхЦикловОбмена = 0;
	
	МассивОписанийПолучаемыеФайлы = Новый Массив;
	
	ОбщееЧислоЦикловОбмена = ЦиклыОбмена.Количество();
	Пока ЧислоВыгруженныхЦикловОбмена < ОбщееЧислоЦикловОбмена Цикл
		
		// формируем текущую порцию циклов обмена
		ПорцияЦиклов = Новый Массив;
		Для Инд = ЧислоВыгруженныхЦикловОбмена + 1 По Мин(ЧислоВыгруженныхЦикловОбмена + РазмерПорции, ОбщееЧислоЦикловОбмена) Цикл
			ПорцияЦиклов.Добавить(ЦиклыОбмена[Инд - 1]);
		КонецЦикла;
		ЧислоВыгруженныхЦикловОбмена = ЧислоВыгруженныхЦикловОбмена + РазмерПорции;
		
		// получаем выгружаемые файлы
		ВыборкаМассив 	= ПолучитьВыгружаемыеФайлы(ПорцияЦиклов, ВыгружатьКонтейнеры, ВыгружатьСодержимое);
		ОбщееЧисло 		= ВыборкаМассив.Количество();
		
		Для СчетчикЦикла = 0 По ОбщееЧисло - 1 Цикл 
			
			ОбработкаПрерыванияПользователя();
			Выборка 		= ВыборкаМассив[СчетчикЦикла];
			ПолноеИмяФайла 	= СформироватьИмяФайла(Выборка.ИмяФайла);
			
			//заполняем МассивОписанийПолучаемыеФайлы
			АдресВоВременномХранилище = Выборка.Адрес;
			ОписаниеФайла = Новый ОписаниеПередаваемогоФайла(ПолноеИмяФайла, АдресВоВременномХранилище); 
			МассивОписанийПолучаемыеФайлы.Добавить(ОписаниеФайла);
			
		КонецЦикла;
		
	КонецЦикла;
	
	ПолучитьФайлыБезРасширенияРаботыСФайлами(МассивОписанийПолучаемыеФайлы);
	ПоказатьПредупреждение(, "Выгрузка завершена.");
	
КонецПроцедуры

#Область СформироватьПакетФСГС

&НаКлиенте
Процедура СформироватьПакетФСГС(ОповещениеОЗавершении, ТранспортноеСообщение, Оповещать = Ложь, ПараметрыОперации = Неопределено) Экспорт
	
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Оповещать);
	
	// общие проверки
	Если НЕ СформироватьПакетФСГС_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда // сервер
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстМоделиХраненияКлюча(КонтекстОтправки.УчетнаяЗапись, КонтекстОтправки);
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(КонтекстОтправки, ПараметрыОперации, Истина);
	КонецЕсли;	
	
	// получение и проверки сертификатов
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФСГСПослеПроверкиИПолученияСертификатовПолучателя", ЭтотОбъект, Контекст);
		
	СформироватьПакетФСГС_ПолучитьИПроверитьСертификатыПолучателя(Оповещение, КонтекстОтправки); // клиент

КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГС_ПолучитьИПроверитьСертификатыПолучателя(ОповещениеОЗавершении, КонтекстОтправки)
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	Если ТипЗнч(КонтекстОтправки.СертификатПолучателя) <> Тип("Массив") Тогда
		Отпечатки = Новый Массив;
		Отпечатки.Добавить(КонтекстОтправки.СертификатПолучателя);
	Иначе
		Отпечатки = КонтекстОтправки.СертификатПолучателя;
	КонецЕсли;
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("Отпечатки", Отпечатки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФСГС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	СертификатыДляПоиска = Новый Массив;
	Для Каждого Отпечаток Из Отпечатки Цикл
		НовыйСертификат = Новый Структура("Отпечаток", Отпечаток);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, НовыйСертификат);
		СертификатыДляПоиска.Добавить(НовыйСертификат);	
	КонецЦикла;
	
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГС_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат.Выполнено Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось определить цифровой сертификат получателя
                  |%1
                  |для шифрования сообщения!'"), ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если Результат.НенайденныеСертификаты.Количество() > 0 Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не удалось определить цифровой сертификат получателя
                  |%1
                  |для шифрования сообщения!'"), ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
		КонецЕсли;
	
	Если ТипЗнч(ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель) = Тип("СправочникСсылка.ОрганыФСГС") Тогда
		Для Каждого ОбъектСертификатОрганаФСГС Из Результат.Сертификаты Цикл				
			РезультатПроверкиСертификатаОрганаФСГС = ПроверитьСертификатПоПериоду(ОбъектСертификатОрганаФСГС);
			Если РезультатПроверкиСертификатаОрганаФСГС <> Истина Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Ошибка проверки сертификата органа ФСГС %1 с отпечатком %2.'"),
					ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель, 
					ОбъектСертификатОрганаФСГС.Отпечаток);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
	КонецЕсли;
		КонецЦикла;
	
			// проверяем на наличие хотя бы одного сертификата
		Если Результат.Сертификаты.Количество() = 0 Тогда 
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'В свойствах органа ФСГС %1 не задан ни один сертификат.'"), ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;		
	КонецЕсли;
	
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить("ОбъектСертификатПолучателя", Результат.Сертификаты);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГСПослеПроверкиИПолученияСертификатовПолучателя(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Результат Тогда
		// проверки на клиенте (сертификаты)
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетФСГСПослеПроверкиСертификатов", ЭтотОбъект, ВходящийКонтекст);
			
		СформироватьПакетФСГС_ПроверкиСертификатов(Оповещение, КонтекстОтправки);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГС_ПроверкиСертификатов(ОповещениеОЗавершении, КонтекстОтправки)
	
	Если КонтекстОтправки.Свойство("ОбъектСертификатОтветственногоЗаШифрование") Тогда // возможна ситуация, когда сертификаты уже получены и проверены
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Истина);
		Возврат;
	КонецЕсли;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	СертификатыДляПоиска = Новый Массив;
	
	ОтпечатокСертификатРуководителя = СокрЛП(КонтекстОтправки.СертификатРуководителя);
	ОтпечатокСертификатОтветственногоЗаШифрование = СокрЛП(КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если Не ЗначениеЗаполнено(ОтпечатокСертификатРуководителя) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В настройках учетной записи %1 не задан сертификат руководителя.'"), КонтекстОтправки.УчетнаяЗапись);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	Иначе
		НовыйСертификат = Новый Структура("Отпечаток", ОтпечатокСертификатРуководителя);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НовыйСертификат);
		СертификатыДляПоиска.Добавить(НовыйСертификат);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ОтпечатокСертификатОтветственногоЗаШифрование) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'В настройках учетной записи %1 не задан сертификат ответственного за шифрование.'"), КонтекстОтправки.УчетнаяЗапись);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	ИначеЕсли ОтпечатокСертификатОтветственногоЗаШифрование <> ОтпечатокСертификатРуководителя Тогда
		НовыйСертификат = Новый Структура("Отпечаток", ОтпечатокСертификатОтветственногоЗаШифрование);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, НовыйСертификат);
		СертификатыДляПоиска.Добавить(НовыйСертификат);
	КонецЕсли;
	
			
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("ВыполнятьПроверку", Истина);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФСГС_ПроверкиСертификатовПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГС_ПроверкиСертификатовПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	ОтпечатокСертификатРуководителя = СокрЛП(КонтекстОтправки.СертификатРуководителя);
	ОтпечатокСертификатОтветственногоЗаШифрование = СокрЛП(КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	ОбъектСертификатРуководителя = Неопределено;
	ОбъектСертификатОтветственногоЗаШифрование = Неопределено;
	
	Для Каждого НайденныйСертификат Из Результат.Сертификаты Цикл
		Если ОтпечатокСертификатРуководителя = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатРуководителя = НайденныйСертификат;
		КонецЕсли;
		Если ОтпечатокСертификатОтветственногоЗаШифрование = НайденныйСертификат.Отпечаток Тогда
			ОбъектСертификатОтветственногоЗаШифрование = НайденныйСертификат;
	КонецЕсли;
	КонецЦикла;
	
	Если ОбъектСертификатРуководителя = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат руководителя в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если Не ОбъектСертификатРуководителя.Валиден Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если ОтпечатокСертификатОтветственногоЗаШифрование = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось найти сертификат ответственного за шифрование в системном хранилище сертификатов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если Не ОбъектСертификатОтветственногоЗаШифрование.Валиден Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	///////////////////////////////////////////////////////////////////////////////
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить("ОбъектСертификатРуководителя", ОбъектСертификатРуководителя);
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить("ОбъектСертификатОтветственногоЗаШифрование", ОбъектСертификатОтветственногоЗаШифрование);
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры
	
&НаКлиенте
Процедура СформироватьПакетФСГСПослеПроверкиСертификатов(Результат, ВходящийКонтекст) Экспорт	
			
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	// формируем файл описания сведений, если это первичное сообщение с отчетностью
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьФСГС") Тогда
		Если НЕ СформироватьОписаниеСведенийДляСообщенияФСГС(КонтекстОтправки) Тогда
			ТекстСообщения = НСтр("ru = 'Формирование пакета прервано.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
	Контекст.Вставить("ПервыйВызов", Истина);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФСГСПослеПодписания", ЭтотОбъект, Контекст);
	СформироватьПакетФСГС_Подписание(Оповещение, КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГС_Подписание(ОповещениеОЗавершении, КонтекстОтправки)
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется подписание пакета с документами..."); // ФСГС
		
	// генерируем подписи
	ПодписатьСообщение("ФСГС", ОповещениеОЗавершении, КонтекстОтправки,
		КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки.ТипСообщения,
		,, КонтекстОтправки.УчетнаяЗапись, КонтекстОтправки.СообщениеОснование);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГСПослеПодписания(Результат, ВходящийКонтекст) Экспорт
		
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;

	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПисьмоИсходящееФСГС") Тогда
	
		// формируем файл описания письма, если его нет
		Если НЕ СформироватьОписаниеПисьмаДляСообщенияФСГС(КонтекстОтправки) Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ИзвещениеПисьмоВходящееФСГС")
		И ВходящийКонтекст.ПервыйВызов Тогда
		// формируем подтверждение получения
		Если НЕ СформироватьИзвещениеОПолученииФСГС(КонтекстОтправки.ТранспортноеСообщение) Тогда
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		ВходящийКонтекст.Вставить("ПервыйВызов", Ложь);
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетФСГСПослеПодписания", ЭтотОбъект, ВходящийКонтекст);
		СформироватьПакетФСГС_Подписание(Оповещение, ВходящийКонтекст.КонтекстОтправки);
	КонецЕсли;
	
	// формируем дерево описания документов пакета (сервер)
	Если НЕ СформироватьПакетФСГС_СформироватьОписаниеДокументовПакета(КонтекстОтправки) Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	РежимПодтвержденияОблачнойПодписи("СформироватьПакетФСГС", КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки);
	
	// отрабатываем отложенные команды на клиенте (шифрование)
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетФСГСПослеВыполненияОтложенныхКомандНаКлиенте", ЭтотОбъект, ВходящийКонтекст);
	СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(Оповещение, КонтекстОтправки);
		
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетФСГСПослеВыполненияОтложенныхКомандНаКлиенте(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// формируем файл описания пакета, создаем архив и помещаем в ИБ
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется запись состояния отправки в базу данных..."); // ФСГС
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если НЕ СформироватьПакетФСГС_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.Оповещать Тогда
		Оповестить("Изменение сообщения", Новый Структура("Сообщение, ЦиклОбмена, Предмет",
														КонтекстОтправки.ТранспортноеСообщение,
														КонтекстОтправки.ЦиклОбмена));
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура ВыгрузитьПакетыПоДокументооборотамСдачиОтчетностиВФНСЗавершениеПослеПолученияКаталога(ВыбранныеФайлы, ДополнительныеПараметры) Экспорт
	
	ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами = ДополнительныеПараметры.ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами;
	Путь = ДополнительныеПараметры.Путь;
	
	Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами 
		И НЕ ЗначениеЗаполнено(Путь)
		И ВыбранныеФайлы = Неопределено Тогда
		
		// Путь сохранения не выбран
		Возврат;
		
	ИначеЕсли ВыбранныеФайлы <> Неопределено Тогда
		
		Путь = ВыбранныеФайлы[0] + 
			?(Прав(ВыбранныеФайлы[0], 1) = ПолучитьРазделительПути(), "", ПолучитьРазделительПути());
		ДополнительныеПараметры.Вставить("Путь", Путь);
		
	КонецЕсли;
	
	ЦиклыОбмена 			= ДополнительныеПараметры.ЦиклыОбмена;
	ОбщееКоличествоФайлов 	= ДополнительныеПараметры.ОбщееКоличествоФайлов;
	
	// количество циклов обмена, по которым за один раз считывается информация из ИБ
	РазмерПорции = 10;
	СоответствиеСообщенияКаталогу = Новый Соответствие;
	СоответствиеТипаСообщенияКраткомуПредставлению = ИнициализироватьСоответствиеТСКП();
	
	// формируем представления циклов обмена
	ПредставленияЦикловОбмена = ПолучитьПредставленияЦикловОбмена(ЦиклыОбмена);
	
	ЧислоВыгруженныхФайлов = 0;
	ЧислоВыгруженныхЦикловОбмена = 0;
	
	МассивОписанийПолучаемыеФайлы = Новый Массив;
	
	ОбщееЧислоЦикловОбмена = ЦиклыОбмена.Количество();
	Пока ЧислоВыгруженныхЦикловОбмена < ОбщееЧислоЦикловОбмена Цикл
		
		// формируем текущую порцию циклов обмена
		ПорцияЦиклов = Новый Массив;
		Для Инд = ЧислоВыгруженныхЦикловОбмена + 1 По Мин(ЧислоВыгруженныхЦикловОбмена + РазмерПорции, ОбщееЧислоЦикловОбмена) Цикл
			ПорцияЦиклов.Добавить(ЦиклыОбмена[Инд - 1]);
		КонецЦикла;
		ЧислоВыгруженныхЦикловОбмена = ЧислоВыгруженныхЦикловОбмена + РазмерПорции;
		
		// получаем выгружаемые файлы
		
		Пакеты = ПолучитьВыгружаемыеПакетыПоДокументооборотамСдачиОтчетностиВФНС(ПорцияЦиклов, УникальныйИдентификатор);
		
		// для каждого из выгружаемых файлов...
		Для Каждого СтрПакет Из Пакеты Цикл
			
			ОбработкаПрерыванияПользователя();
			
			СвойстваПакета = СтрПакет.Значение;
			Если ТипЗнч(СвойстваПакета) = Тип("Строка") Тогда
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СвойстваПакета);
				Продолжить;
			КонецЕсли;
			
			ИмяФайлаПакета = СвойстваПакета.ИмяФайла;
			АдресВоВременномХранилище = СвойстваПакета.Адрес;
			
			ЦиклОбмена = СтрПакет.Ключ;
			ПредставлениеЦиклаОбмена = ПредставленияЦикловОбмена[ЦиклОбмена];
			
			Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
				
				КаталогВыгрузки = Путь;
				КаталогВыгрузки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВыгрузки);
				КаталогВыгрузки = КаталогВыгрузки + СокрЛП(СформироватьИмяФайла(ПредставлениеЦиклаОбмена));
				КаталогВыгрузки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВыгрузки);
				
				// прорисовываем состояние в форме прогресса
				ЧислоВыгруженныхФайлов = ЧислоВыгруженныхФайлов + 1;
				Состояние(ПредставлениеЦиклаОбмена, ЧислоВыгруженныхФайлов / ОбщееКоличествоФайлов * 100);
								
				//заполняем МассивОписанийПолучаемыеФайлы
				ПолноеИмяФайла = КаталогВыгрузки + ИмяФайлаПакета;
			Иначе
				ПолноеИмяФайла = ИмяФайлаПакета;
			КонецЕсли;
			
			ОписаниеФайла = Новый ОписаниеПередаваемогоФайла(ПолноеИмяФайла, АдресВоВременномХранилище);
			МассивОписанийПолучаемыеФайлы.Добавить(ОписаниеФайла);
		
		КонецЦикла;
		
	КонецЦикла;
	
	ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(МассивОписанийПолучаемыеФайлы);
	
КонецПроцедуры

&НаКлиенте
Функция БраузерПоддерживаетРаботуСКомпонентойПечатиМашиночитаемыхБланков() Экспорт

	#Если ВебКлиент Тогда
		
		ВебБраузер = РегламентированнаяОтчетностьКлиент.ВебБраузер();
		
		Если ЗначениеЗаполнено(ВебБраузер)
		   И НЕ ВебБраузер = "MSIE" Тогда
		   
		    ПоказатьПредупреждение(, НСтр("ru='Внимание! Данная функция в Веб-клиенте поддерживается только в браузере Internet Explorer.'"));
			Возврат Ложь;
			
		КонецЕсли;
		
	#КонецЕсли
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ПолучитьФайлыБезРасширенияРаботыСФайлами(МассивОписаний) Экспорт
	
	Если МассивОписаний <> Неопределено Тогда
		Для каждого ОписаниеПолучаемогоФайла Из МассивОписаний Цикл
			ПолучитьФайл(ОписаниеПолучаемогоФайла.Хранение, ОписаниеПолучаемогоФайла.Имя, Истина);
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьЭлектронногоПредставленияПослеСохраненияВФайл(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ПоказатьПредупреждение(,"Не удалось выгрузить электронное представление отчета во временный файл!" + Символы.ПС + Символы.ПС + ОписаниеОшибки());
		Возврат;
	КонецЕсли;
	
	ВремФайл = Результат.ИмяФайла;
	
	СтруктураИмениФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ВремФайл);
	ВремКаталог 		= СтруктураИмениФайла.Путь;
	
	ЭлектронноеПредставление 	= ВходящийКонтекст.ЭлектронноеПредставление;
	ВидОтчета 					= ВходящийКонтекст.ВидОтчета;
	АдресДанных					= ВходящийКонтекст.АдресДанных;
		
	// извлекаем параметры сформированного файла выгрузки
	СтруктураПараметров = СформироватьСтруктуруПараметровФайлаВыгрузки(АдресДанных);
	
	// пытаемся создать отчет заданного вида
	Если ЗначениеЗаполнено(ВидОтчета) Тогда
		#Если ВебКлиент Тогда 
			ИспользоватьКомпоненту = Истина;
		#Иначе
			ИспользоватьКомпоненту = Ложь;
		#КонецЕсли
		ВозниклаОшибка 	= Ложь;
		СоздатьОтчетЗаданногоВида(ЭлектронноеПредставление, СтруктураПараметров, ИспользоватьКомпоненту, ВозниклаОшибка);
		Если ВозниклаОшибка Тогда
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТекущийНомерШаблона", 	0);
	ДополнительныеПараметры.Вставить("ВремКаталог", 			ВремКаталог);
	ДополнительныеПараметры.Вставить("ВремФайл", 				ВремФайл);
	 
	ПечатьЭлектронногоПредставленияСохранениеШаблоновОтчета(Неопределено, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьЭлектронногоПредставленияСохранениеШаблоновОтчета(Результат, ВходящийКонтекст) Экспорт
	
	ТекущийНомерШаблона 	= ВходящийКонтекст.ТекущийНомерШаблона;
	ВремКаталог 			= ВходящийКонтекст.ВремКаталог;
	ВремФайл 				= ВходящийКонтекст.ВремФайл;
	
	Если ТекущийНомерШаблона > ТаблицаШаблонов.Количество() - 1 Тогда
		
		// пытаемся создать объект для формирования машиночитаемой формы
		ТаксДокПринт = Неопределено;
		ТаксДокПринт = РегламентированнаяОтчетностьКлиент.СоздатьОбъектДляФормированияМашиночитаемойФормы();
		// если не удалось создать объект, то спрашиваем у пользователя, хочет ли он установить компоненту
		Если ТаксДокПринт = Неопределено Тогда
			
			СписокКнопок = Новый СписокЗначений;
			СписокКнопок.Добавить(КодВозвратаДиалога.Да, НСтр("ru='Установить'"));
			СписокКнопок.Добавить(КодВозвратаДиалога.Нет, НСтр("ru='Отмена'"));
			
			ДополнительныеПараметры = Новый Структура("ТаксДокПринт, ВремКаталог, ВремФайл", ТаксДокПринт, ВремКаталог, ВремФайл);
			ОписаниеОповещения = Новый ОписаниеОповещения("ПечатьЭлектронногоПредставленияВопросУстановитьКомпонентуЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьВопрос(ОписаниеОповещения, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru='Для формирования машиночитаемой формы необходимо установить%1библиотечный модуль печати двухмерного штрихкода PDF417.'"), Символы.ПС), СписокКнопок);
						
			Возврат;
			
		КонецЕсли;
		
		ПечатьЭлектронногоПредставленияПослеУстановкиКомпоненты(ТаксДокПринт, ВремКаталог, ВремФайл);
		
	Иначе
		
		ДанныеВыгрузки = ТаблицаШаблонов[ТекущийНомерШаблона];
		ПолноеИмяФайла = ВремКаталог + ДанныеВыгрузки.Имя;
		
		ТекущийНомерШаблона = ТекущийНомерШаблона + 1;
		ВходящийКонтекст.Вставить("ТекущийНомерШаблона", ТекущийНомерШаблона);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПечатьЭлектронногоПредставленияСохранениеШаблоновОтчета", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		ОперацииСФайламиЭДКОКлиент.ДанныеССервераВФайл(ОписаниеОповещения, ДанныеВыгрузки.АдресСодержимого, ПолноеИмяФайла);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаПослеВыбораФайлов(ВыбранныеФайлы, ДополнительныеПараметры) Экспорт
	
	ФайлыБылиВыбраны = Ложь;
	Если ВыбранныеФайлы <> Неопределено Тогда
		
		ФайлыБылиВыбраны = Истина;
		ДополнительныеПараметры.Вставить("ВыбранныеФайлы", ВыбранныеФайлы);
		ПолучениеФайловДляИмпортаПослеПомещенияФайла(ФайлыБылиВыбраны, "", "", ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловДляИмпортаПослеОпределенияСвойствОсновногоФайла(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		Возврат;
	КонецЕсли;
	
	ОсновнойФайлНаДиске = Результат.СвойстваФайла;
	ДополнительныеПараметры.Вставить("ОсновнойФайлНаДиске", ОсновнойФайлНаДиске);
	
	ВыбранныеФайлы 		= ДополнительныеПараметры.ВыбранныеФайлы;
	КонтекстЭДОКлиент 	= ДополнительныеПараметры.КонтекстЭДОКлиент;
	
	ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках = КонтекстЭДОКлиент.ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках(ОсновнойФайлНаДиске.Имя);
	ДополнительныеПараметры.Вставить("ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках", ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках);
	
	Если ВыбранныеФайлы.Количество() = 1 Тогда
		
		МаксимальныйРазмер = МаксимальныйРазмерФайлаУведомленияОКонтролируемыхСделках();
		
		Если ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках
			И ОсновнойФайлНаДиске.Размер > МаксимальныйРазмер Тогда
			
			РазмерВМб = Цел(МаксимальныйРазмер/1024/1024);
			ТекстСообщения = НСтр("ru='Согласно требованиям ФНС файл формата 5.01 больше %2 Мб должен быть разбит на файлы формата 5.02.%1Загрузить файл с разбивкой?'");
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "%1", Символы.ПС);
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "%2", Строка(РазмерВМб));
			
			Кнопки = Новый СписокЗначений;
			Кнопки.Добавить("Да", "Да");
			Кнопки.Добавить("Нет", "Нет");
			
			ОписаниеОповещения = Новый ОписаниеОповещения("ПолучениеФайловПослеВопросаОРазбиенииФайла", ЭтотОбъект, ДополнительныеПараметры);
			
			ПоказатьВопрос(ОписаниеОповещения, ТекстСообщения, Кнопки);
		Иначе
			ПолучениеФайловПослеВопросаОРазбиенииФайла("Нет", ДополнительныеПараметры);
		КонецЕсли;
	Иначе
		ПолучениеФайловПослеВопросаОРазбиенииФайла("Нет", ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловПослеВопросаОРазбиенииФайла(Ответ, ДополнительныеПараметры) Экспорт
	
	КонтекстЭДОКлиент = ДополнительныеПараметры.КонтекстЭДОКлиент;
	
	Если Ответ = "Да" Тогда
		ДополнительныеПараметры.Вставить("ВыполнитьРазделениеФайла", 				Истина);
		ДополнительныеПараметры.Вставить("ВыполнятьПроверкуКомплектаУведомлений", 	Ложь);
	Иначе
		ДополнительныеПараметры.Вставить("ВыполнитьРазделениеФайла", 				Ложь);
		Если ДополнительныеПараметры.ЭтоФайлВыгрузкиУведомленияОКонтролируемыхСделках Тогда
			ДополнительныеПараметры.Вставить("ВыполнятьПроверкуКомплектаУведомлений", Истина);
		Иначе
			ДополнительныеПараметры.Вставить("ВыполнятьПроверкуКомплектаУведомлений", Ложь);
		КонецЕсли;
		РезультатИмпорта = Ложь;
	КонецЕсли;

	ОсновнойФайлНаДиске 									= ДополнительныеПараметры.ОсновнойФайлНаДиске;
	ВыбранныеФайлы 											= ДополнительныеПараметры.ВыбранныеФайлы;
	ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами 	= ДополнительныеПараметры.ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами;
	
	Если ВыбранныеФайлы = Неопределено Тогда
		ПолучениеФайловДляИмпортаЗавершение(Ложь, ДополнительныеПараметры);
		Возврат;
	КонецЕсли;

	// составляем массив с объектами Файл
	Если ПоддерживаетсяИспользованиеРасширенияРаботыСФайлами Тогда
		МассивФайлов = Новый Массив;
		Для Каждого ЭлФайл Из ВыбранныеФайлы Цикл
			Файл = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ЭлФайл);
			МассивФайлов.Добавить(Новый Структура("Имя, ПолноеИмя, Расширение, АдресДанных", Файл.Имя, Файл.ПолноеИмя, Файл.Расширение));
		КонецЦикла;
		
		ПомещаемыеФайлы = Новый Массив;
		Для Каждого ЭлФайл Из МассивФайлов Цикл 
			ОписаниеФайла = Новый ОписаниеПередаваемогоФайла(ЭлФайл.ПолноеИмя); 
			ПомещаемыеФайлы.Добавить(ОписаниеФайла);
		КонецЦикла;
		
		ДополнительныеПараметры.Вставить("МассивФайлов", МассивФайлов);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПолучениеФайловПослеПомещенияФайлов", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		
		НачатьПомещениеФайлов(ОписаниеОповещения, ПомещаемыеФайлы, Ложь, Новый УникальныйИдентификатор);
		
	Иначе
		МассивФайлов = Новый Массив;
		МассивФайлов.Добавить(Новый Структура("Имя, ПолноеИмя, Расширение, АдресДанных", 
			ОсновнойФайлНаДиске.Имя, ОсновнойФайлНаДиске.ПолноеИмя, ОсновнойФайлНаДиске.Расширение, ОсновнойФайлНаДиске.АдресДанных));
		РезультатИмпорта = Истина;
		
		ДополнительныеПараметры.Вставить("ФайлыДляИмпорта", МассивФайлов);
		ПолучениеФайловДляИмпортаЗавершение(РезультатИмпорта, ДополнительныеПараметры);

	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучениеФайловПослеПомещенияФайлов(ПомещенныеФайлы, ДополнительныеПараметры) Экспорт
	
	Если ПомещенныеФайлы = Неопределено Тогда
		РезультатИмпорта = Ложь;
	Иначе
		
		МассивФайлов = ДополнительныеПараметры.МассивФайлов;
		
		Для каждого ЭлФайл Из МассивФайлов Цикл
			Для каждого ОписаниеПереданногоФайла Из ПомещенныеФайлы Цикл
				Если ОписаниеПереданногоФайла.ПолноеИмя = ЭлФайл.ПолноеИмя Тогда
					ЭлФайл.АдресДанных = ОписаниеПереданногоФайла.Хранение;
					Прервать;
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
		
		РезультатИмпорта = Истина;
	КонецЕсли;
		
	ДополнительныеПараметры.Вставить("ФайлыДляИмпорта", МассивФайлов);
	ПолучениеФайловДляИмпортаЗавершение(РезультатИмпорта, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПФР_Подписание(ОповещениеОЗавершении, КонтекстОтправки, ПараметрыОперации = Неопределено)

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется подписание пакета с документами..."); // ПФР
		
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(КонтекстОтправки, ПараметрыОперации, Истина);
	КонецЕсли;	
	
	// генерируем подписи
	ПодписатьСообщение(
		"ПФР", 
		ОповещениеОЗавершении, 
		КонтекстОтправки,
		КонтекстОтправки.ТранспортноеСообщение, 
		КонтекстОтправки.ТипСообщения,
		,
		,
		КонтекстОтправки.УчетнаяЗапись, 
		КонтекстОтправки.СообщениеОснование);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьТранспортнуюИнформациюДляСообщенияПослеВыполнения(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	Если Не Результат Тогда
		ТекстСообщения = НСтр("ru = 'Формирование пакета прервано.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыОперации = Новый Структура("ТребуетПодтверждениОперации", Ложь);
	КонецЕсли;

	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетПФРПослеПодписания", ЭтотОбъект, ВходящийКонтекст);
	СформироватьПакетПФР_Подписание(Оповещение, КонтекстОтправки, ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодписатьСообщенияПакетноПослеВыполнения(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	Если РезультатВыполнения.Выполнено Тогда
		
		КонтекстОтправки = ВходящийКонтекст.ПараметрыФункции.КонтекстОтправки;
		ЭДОК = СвойствоСтруктуры(КонтекстОтправки, "ФорматДокументооборота") = ПредопределенноеЗначение("Перечисление.ФорматыДокументооборотаСПФР.ЭДОК");

		Оповещение = Новый ОписаниеОповещения(
			"ПодписатьСообщенияПоследовательноПослеЧтенияДанныхИзФайла", ЭтотОбъект, ВходящийКонтекст);
		
		Для Счетчик = 0 По РезультатВыполнения.Результат.ВГраница() Цикл
			Подпись = РезультатВыполнения.Результат[Счетчик];
			Если ЭДОК Тогда
				ИмяФайлаПодписи = "." + СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент(".sig");
			Иначе
				ИмяФайлаПодписи = СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент();
			КонецЕсли;
			
			ДанныеПодписи = ПолучитьИзВременногоХранилища(Подпись);
			ДанныеПодписи = Base64Строка(ДанныеПодписи);
			ВходящийКонтекст.ДокументыДляПодписывания[Счетчик].Вставить("ИмяФайлаПодписи", ИмяФайлаПодписи);	
			ВходящийКонтекст.ДокументыДляПодписывания[Счетчик].Вставить("ДанныеПодписи", ДанныеПодписи);	
		КонецЦикла;	
		
		Для Каждого СтрокаМассива Из ВходящийКонтекст.ДокументыДляПодписывания Цикл
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
				ВходящийКонтекст.ПараметрыФункции.Сообщение,
				СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_ЭлектронноЦифроваяПодпись,
				Новый Структура("Строка64", СтрокаМассива.ДанныеПодписи),
				ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(СтрокаМассива.ИмяФайлаПодписи).Имя,,
				СтрокаМассива.ИмяФайла,
				СтрокаМассива.Сертификат.Отпечаток,
				СтруктураСсылочныхДанных.Перечисления_СтатусыПроверкиЭЦП_Валидна,
				Истина) Тогда
				
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Ошибка добавления содержимого транспортного контейнера ""%1""'"),
					СтрокаМассива.ИмяФайла);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
				Возврат;
			КонецЕсли;
		КонецЦикла;
		
		ВыполнитьОбработкуОповещения(
			ВходящийКонтекст.ОповещениеОЗавершении,
			Истина);
		
	Иначе
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Ошибка подписания документа: %1.
					   |По причине: %2'"), 
			ПредставлениеТранспортногоСообщения(ВходящийКонтекст.ПараметрыФункции.Сообщение), 
			РезультатВыполнения.Ошибка);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ТранспортноеСообщениеЗашифрованоДляТекущегоПользователя(ТранспортноеСообщение)
	
	МассивВладельцев = Мультирежим.ВладельцыТранспортногоСообщения(ТранспортноеСообщение);
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	
	Возврат МассивВладельцев.Количество() = 0 ИЛИ МассивВладельцев.Найти(ТекущийПользователь) <> Неопределено;
	
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура ПодписатьСообщениеНаКлиенте(ОповещениеОЗавершении, ПараметрыФункции)
	
	// создаем временный каталог для выгрузки файлов
	Контекст = Новый Структура;
	Контекст.Вставить("ПараметрыФункции", ПараметрыФункции);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"ПодписатьСообщениеПослеСозданияКаталога", ЭтотОбъект, Контекст);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции.КонтекстОтправки);
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча)
		ИЛИ ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено, ИмяКаталога", Истина, ""));
	Иначе
		ОперацииСФайламиЭДКОКлиент.СоздатьКаталогНаКлиенте(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФайлАвтонастройки(УчетнаяЗапись) Экспорт

	Если УчетнаяЗапись <> Неопределено Тогда
		
		Адрес = АдресДанныхФайлаАвтонастройки(УчетнаяЗапись);
		Если Адрес = Неопределено Тогда
			
			ТекстСообщения = НСтр("ru = 'В регистре сведений ""Кэш файлов с ресурсов операторов ЭДО"" отсутствует файл настроек по данной учетной записи.
                                   |Попробуйте выполнить автонастройку и повторить сохранение файла.'");
			ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
			
		Иначе
			
			СохраняемыйФайл = Новый ОписаниеПередаваемогоФайла(Строка(УчетнаяЗапись), Адрес); 
			ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(СохраняемыйФайл);
			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция АдресДанныхФайлаАвтонастройки(УчетнаяЗапись) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.АдресДанныхФайлаАвтонастройки(УчетнаяЗапись);
	
КонецФункции

#Область ПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ

&НаКлиенте
Процедура ПредупредитьОбОшибкеВСтатусахОтправки2НДФЛ() Экспорт
	
	ПредупредитьОНеобходимостиДублированияОтправок();
	
	ПредупредитьОбОшибкеВСтатусахОтправкиПФР();
	
	РезультатАнализа = ТребуетсяПредупредитьОбОшибкеВСтатусахОтправки2НДФЛ();
	
	Если РезультатАнализа.ТребуетсяПредупредить Тогда
		ПоказатьПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ(РезультатАнализа.АдресОтчетов);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ(Адрес) Экспорт
	
	ПараметрыОткрытияФормы = Новый Структура("Адрес", Адрес);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредупреждениеОНекорректныхСтатусахОтправки2НДФЛ", ПараметрыОткрытияФормы);
	
КонецПроцедуры

&НаСервере
Функция ТребуетсяПредупредитьОбОшибкеВСтатусахОтправки2НДФЛ() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТребуетсяПредупредитьОбОшибкеВСтатусахОтправки2НДФЛ();
	
КонецФункции

#КонецОбласти

#Область ПредупреждениеОНеобходимостиДублированияОтправок

&НаКлиенте
Процедура ПредупредитьОНеобходимостиДублированияОтправок() Экспорт
	
	РезультатАнализа = ТребуетсяПредупредитьОНеобходимостиДублированияОтправок();
	
	Если РезультатАнализа.ТребуетсяПредупредить Тогда
		ПоказатьПредупреждениеОНеобходимостиДублированияОтправок(РезультатАнализа.АдресОтчетов);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОНеобходимостиДублированияОтправок(Адрес) Экспорт
	
	ПараметрыОткрытияФормы = Новый Структура("Адрес", Адрес);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредупреждениеОНеобходимостиДублированияОтправок", ПараметрыОткрытияФормы);
	
КонецПроцедуры

&НаСервере
Функция ТребуетсяПредупредитьОНеобходимостиДублированияОтправок() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТребуетсяПредупредитьОНеобходимостиДублированияОтправок();
	
КонецФункции

#КонецОбласти

#Область ЛичныеКабинеты

&НаКлиенте
Процедура ПерейтиПоСсылкеВИнтернетЭксплорере(АдресСсылки) Экспорт
	
	СтрокаОС = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СтрокаОС();
	
	Если СтрНайти(СтрокаОС, "Windows") = 0 Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Для работы с сервисом требуется операционная система семейства Windows'"));
		Возврат;
		
	КонецЕсли;
	
	#Если ВебКлиент Тогда
		//это веб клиент
		ВебБраузер = РегламентированнаяОтчетностьКлиент.ВебБраузер();
		Если ВебБраузер = "MSIE" Тогда
			//это IE
			ПерейтиПоНавигационнойСсылке(АдресСсылки);
		Иначе
			//это не IE
			ПерейтиПоСсылкеВИнтернетЭксплоререНасильно(АдресСсылки);
		КонецЕсли;
	#Иначе
		//это тонкий клиент
		ПерейтиПоСсылкеВИнтернетЭксплоререНасильно(АдресСсылки);
	#КонецЕсли
	
КонецПроцедуры

&НаКлиенте
Процедура ПерейтиПоСсылкеВИнтернетЭксплоререНасильно(АдресСсылки)
	
	ОперацииСФайламиЭДКОКлиент.ЗапуститьПриложениеНаКлиенте(,
		"rundll32 shell32.dll, ShellExec_RunDLL iexplore " + АдресСсылки,,,
		АдресСсылки);
	
КонецПроцедуры

#КонецОбласти

///////////////////////////////////////////////////////////////////////////////
// Получение требований и представление документов в налоговые органы

&НаСервереБезКонтекста
Функция ПолучитьСтруктуруРазмеровСканДокументов(СканДокументы) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьСтруктуруРазмеровСканДокументов(СканДокументы);

КонецФункции

// Помещает присоединенные файлы объектов ИБ, 
// являющихся источниками для заполнения реквизитов сканированных документов, 
// представляемых по требованию ФНС, во временное хранилище и возвращает их свойства.
// Следует возвращать свойства всех файлов следующих типов: JPEG, TIFF, PNG, PDF.
//
// Параметры 
//	ИдентификаторФормыВладельца	- УникальныйИдентификатор, уникальный идентификатор формы, 
//		во временное хранилище которой требуется поместить данные присоединенных файлов.
//	ФайлыИсточников				- Соответствие, соответствие переданных ссылок на источники и массива структур 
//		Ключ 		- ссылка на источник
//		Значение 	- Массив, массив структур (начальное значение: пустой массив)
//		(каждый элемент массива -  структура свойств одного файла)
//
//		Поля структуры:
//			Имя			- Строка, короткое имя файла с расширением
//			Размер		- Число, размер файла в байтах
//			АдресДанных	- Строка, адрес временного хранилища
//
&НаСервереБезКонтекста
Процедура ПолучитьИзображенияПрисоединенныхФайловИсточников(ФайлыИсточников, ИдентификаторФормыВладельца) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ПолучитьИзображенияПрисоединенныхФайловИсточников(ФайлыИсточников, ИдентификаторФормыВладельца);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура УдалитьПрисоединенныеФайлыСканированныеДокументыДляПередачиВЭлектронномВиде(МассивФайлов) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.УдалитьПрисоединенныеФайлыСканированныеДокументыДляПередачиВЭлектронномВиде(МассивФайлов);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись);
		
КонецФункции

&НаКлиенте
Функция НайденыЗапрещенныеСимволы(АнализируемаяСтрока, НаименованиеРеквизита, Поле, ТихийРежим = Ложь) Экспорт
	
	Возврат ДокументооборотСКОКлиентСервер.НайденыЗапрещенныеСимволы(
		АнализируемаяСтрока, 
		НаименованиеРеквизита, 
		Поле, 
		ТихийРежим);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьТаблицуПрисоединенныхФайловСканДокумента(СканированныйДокументСсылка)

	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
	                      |	СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.Ссылка,
						  |	СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.ИмяФайла
	                      |ИЗ
	                      |	Справочник.СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы КАК СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы
	                      |ГДЕ
	                      |	СканированныеДокументыДляПередачиВЭлектронномВидеПрисоединенныеФайлы.ВладелецФайла = &ВладелецФайла");
	Запрос.УстановитьПараметр("ВладелецФайла", СканированныйДокументСсылка);
	Результат = Запрос.Выполнить();
	Выборка = Результат.Выбрать();
	
	ТаблицаФайлов = Новый ТаблицаЗначений;
	ТаблицаФайлов.Колонки.Добавить("ИмяФайла");
	ТаблицаФайлов.Колонки.Добавить("Данные");
	ТаблицаФайлов.Колонки.Добавить("Ссылка");
	
	Пока Выборка.Следующий() Цикл
		
		Данные = РаботаСФайлами.ДвоичныеДанныеФайла(Выборка.Ссылка);
		
		НоваяСтрока = ТаблицаФайлов.Добавить();
		НоваяСтрока.ИмяФайла 	= Выборка.ИмяФайла;
		НоваяСтрока.Данные		= Данные;
		НоваяСтрока.Ссылка		= Выборка.Ссылка;

	КонецЦикла;

	Возврат ТаблицаФайлов;

КонецФункции

&НаКлиенте
Функция ТребуетсяОсобыйСпособОтображения(ИмяФайла, ТипСодержимого)
	
	Если СтрНайти(ИмяФайла, "IU_SPISOB") Тогда
		Возврат Истина;
	ИначеЕсли (СтрНайти(ИмяФайла, "IU_AKTSVP_") 
		ИЛИ СтрНайти(ИмяФайла, "IU_VO_") 
		ИЛИ СтрНайти(ИмяФайла, "IU_SPKR") 
		ИЛИ СтрНайти(ИмяФайла, "IU_PN_")
		ИЛИ СтрНайти(ИмяФайла, "IU_SPKRO")
		ИЛИ СтрНайти(ИмяФайла, "IU_SPPRINSUMENP")
		ИЛИ СтрНайти(ИмяФайла, "IU_SPPRINDENSRENP")
		ИЛИ СтрНайти(ИмяФайла, "IU_SVISOB"))
		И ТипСодержимого = СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоФайлов_xml Тогда
		
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьКакТабличныйДокумент(ЗапросИОН, ИмяФайла, АдресФайла) Экспорт
	
	Если ВРег(ОбщегоНазначенияКлиентСервер.ПолучитьРасширениеИмениФайла(ИмяФайла)) = "XML" Тогда
		Если СтрНайти(ИмяФайла, "IU_SPPRINSUMENP")
			ИЛИ СтрНайти(ИмяФайла, "IU_SPPRINDENSRENP")
			ИЛИ СтрНайти(ИмяФайла, "IU_SVISOB") Тогда
			
			ТекстXML  = ТекстСообщения(АдресФайла);
			Результат = ДокументооборотСКОКлиент.ПредставлениеСверкиСФНС(ЗапросИОН, ТекстXML);
			Результат = ДобавитьПодписьВСправкуИОН(ЗапросИОН, Результат);
			
			Если СтрНайти(ИмяФайла, "IU_SPPRINSUMENP") Тогда
				НапечататьДокумент(Результат, НСтр("ru = 'Справка о принадлежности сумм'"));
			ИначеЕсли СтрНайти(ИмяФайла, "IU_SPPRINDENSRENP") Тогда
				НапечататьДокумент(Результат, НСтр("ru = 'Справка о принадлежности сумм (агрегированные данные)'"));
			ИначеЕсли СтрНайти(ИмяФайла, "IU_SVISOB") Тогда
				НапечататьДокумент(Результат, НСтр("ru = 'Сведения об отрицательном сальдо ЕНС'"));
			КонецЕсли;
			
		ИначеЕсли СтрНайти(ИмяФайла, "IU_SPISOB") Тогда
			
			Если ВерсияФормата(АдресФайла) = "5.04" Тогда
				ТекстXML  = ТекстСообщения(АдресФайла);
				Результат = ДокументооборотСКОКлиент.ПредставлениеСверкиСФНС(ЗапросИОН, ТекстXML);
				Результат = ДобавитьПодписьВСправкуИОН(ЗапросИОН, Результат);
			Иначе
				Результат = СформироватьСправкуОбИсполненииНалогоплательщикомОбязанностиПоУплатеНалогов(ЗапросИОН, АдресФайла);
			КонецЕсли;
			НапечататьДокумент(Результат, НСтр("ru = 'Справка об исполнении обязанности по уплате налогов'"));
			
		ИначеЕсли СтрНайти(ИмяФайла, "IU_AKTSVP_") Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ИмяФайла",   ИмяФайла);
			ДополнительныеПараметры.Вставить("АдресФайла", АдресФайла);
			ДополнительныеПараметры.Вставить("ЗапросИОН",  ЗапросИОН);
			
			ЭтоНоваяВерсия = 
				ВерсияФормата(АдресФайла) = "5.03"
				ИЛИ ВерсияФормата(АдресФайла) = "5.04";
			
			Если ЭтоНоваяВерсия Тогда
				ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаАктаСверкиСФНС5_03", ДополнительныеПараметры);
			Иначе
				ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаАктаСверкиСФНС", ДополнительныеПараметры);
			КонецЕсли;
			
		ИначеЕсли СтрНайти(ИмяФайла, "IU_VO_") Тогда
			
			Результат = СформироватьВыпискуОпераций(ЗапросИОН, АдресФайла);
			НапечататьДокумент(Результат, НСтр("ru = 'Выписка операций по расчетам с бюджетом'"));
			
		ИначеЕсли СтрНайти(ИмяФайла, "IU_SPKRO") Тогда
			
			Результат = СформироватьСправкаОНаличииСальдо(ЗапросИОН, АдресФайла);
			НапечататьДокумент(Результат, НСтр("ru = 'Справка о наличии сальдо'"));

		ИначеЕсли СтрНайти(ИмяФайла, "IU_SPKR") Тогда
			
			Результат = СформироватьСправкуОСостоянииРасчетовСФНС(ЗапросИОН, АдресФайла);
			НапечататьДокумент(Результат, НСтр("ru = 'Справка о состоянии расчетов с ФНС'"));
			
		ИначеЕсли СтрНайти(ИмяФайла, "IU_PN_") Тогда
			
			Результат = СформироватьСписокПредставленнойОтчетности(ЗапросИОН, АдресФайла);
			НапечататьДокумент(Результат, НСтр("ru = 'Список представленной отчетности'"));
			
		КонецЕсли;
		
	Иначе
		// Обход ошибки платформы, когда указано, что файл xml, но приложен pdf файл
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(АдресФайла, ИмяФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьСправкаОНаличииСальдо(ЗапросИОН, АдресФайла) Экспорт
	
	Возврат Документы.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика.СправкаОНаличииСальдо(ЗапросИОН, АдресФайла);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьСписокПредставленнойОтчетности(ЗапросИОН, АдресФайла) Экспорт
	
	// Получаем файл xml
	ИмяФайла = ПолучитьИмяВременногоФайла("xml");
	ПолучитьИзВременногоХранилища(АдресФайла).Записать(ИмяФайла);
	
	// Дессериализуем в XDTO
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайла);
	Справка = ФабрикаXDTO.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	ЭтоВерсия5_04 = ВерсияФормата(АдресФайла) = "5.04";
	
	Документ  = Справка.Документ;
	Подписант = Документ.Подписант;
	КодНО     = Подписант.КодНО;
	
	Если НЕ ЭтоВерсия5_04 Тогда
		НаимНО = Подписант.НаимНО;
	КонецЕсли;
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	Если ЭтоВерсия5_04 Тогда
		МакетСправки = ПолучитьМакетОбработки("СписокПредставленнойОтчетности5_04");
		ПеречНБО = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Документ.ПеречНБО);
	Иначе
		МакетСправки = ПолучитьМакетОбработки("СписокПредставленнойОтчетности5_03");
		ПеречНБО = СписокИзXDTO(Документ, "ПеречНБО");
	КонецЕсли;
	
	Количество = ПеречНБО.Количество();
	Счетчик    = 0;
	Для каждого СтрокаПеречНБО Из ПеречНБО Цикл
		
		Счетчик = Счетчик + 1;
		
		ОбластьШапка = МакетСправки.ПолучитьОбласть("Шапка");
		
		Если ЭтоВерсия5_04 Тогда
			ОбластьШапка.Параметры.Заполнить(Документ);
			ОбластьШапка.Параметры.КодНО = КодНО;
			СвНП = Документ.СвНП;
		Иначе
			ОбластьШапка.Параметры.Заполнить(СтрокаПеречНБО);
			ОбластьШапка.Параметры.НаимНО = НаимНО;
			СвНП = СтрокаПеречНБО.СвНП;
		КонецЕсли;
		
		ДобавитьВСверкуПолучателя(ОбластьШапка, СвНП);
		
		Если НЕ ЭтоВерсия5_04 Тогда
			ОбластьШапка.Параметры.Адрес = ПредставлениеАдрРФСтрокой(СвНП.АдрРФ);
		КонецЕсли;
		ТабличныйДокумент.Вывести(ОбластьШапка);
		
		ДокумНБО = СписокИзXDTO(СтрокаПеречНБО, "ДокумНБО");
		
		Если ДокумНБО = Неопределено Тогда
			
			ОбластьТЧ = МакетСправки.ПолучитьОбласть("ТЧ");
			ТабличныйДокумент.Вывести(ОбластьТЧ);
			
		Иначе
			
			Для каждого СтрДокумНБО Из ДокумНБО Цикл
				ОбластьТЧ = МакетСправки.ПолучитьОбласть("ТЧ");
				ОбластьТЧ.Параметры.Заполнить(СтрДокумНБО);
				
				// Чтобы формат выводился корректно
				СумДеклар = СвойствоXDTO(СтрДокумНБО, "СумДеклар");
				Если СумДеклар <> "" Тогда
					ОбластьТЧ.Параметры.СумДеклар = Число(СумДеклар);
				КонецЕсли;
				
				СумКРБ = СвойствоXDTO(СтрДокумНБО, "СумКРБ");
				Если СумКРБ <> "" Тогда
					ОбластьТЧ.Параметры.СумКРБ = Число(СумКРБ);
				КонецЕсли;
				
				ОбластьТЧ.Параметры.Отчет = НазваниеОтчетаПоКНД(СтрДокумНБО.КНД);
				
				Если ЭтоВерсия5_04 Тогда
					Если СтрДокумНБО.СпособПредДок = "0" Тогда
						ОбластьТЧ.Параметры.СпособПредДок = НСтр("ru = 'документ не направляется/не представляется'");
					ИначеЕсли СтрДокумНБО.СпособПредДок = "1" Тогда
						ОбластьТЧ.Параметры.СпособПредДок = НСтр("ru = 'документ представляется'");
					ИначеЕсли СтрДокумНБО.СпособПредДок = "2" Тогда
						ОбластьТЧ.Параметры.СпособПредДок = НСтр("ru = 'документ направляется'");
					ИначеЕсли СтрДокумНБО.СпособПредДок = "3" Тогда
						ОбластьТЧ.Параметры.СпособПредДок = НСтр("ru = 'возможно, как направление, так и представление документа'");
					КонецЕсли;
					
					Если СтрДокумНБО.СостоянДок = "1" Тогда
						ОбластьТЧ.Параметры.СостоянДок = НСтр("ru = 'зарегистрирован'");
					ИначеЕсли СтрДокумНБО.СостоянДок = "2" Тогда
						ОбластьТЧ.Параметры.СостоянДок = НСтр("ru = 'введен в БД АИС'");
					ИначеЕсли СтрДокумНБО.СостоянДок = "3" Тогда
						ОбластьТЧ.Параметры.СостоянДок = НСтр("ru = 'проведено начисление в карточку «Расчеты с бюджетом» по сведениям налогоплательщика'");
					КонецЕсли;
					
				Иначе
					ДобавитьОКАТО_ОКТМО(ОбластьТЧ, СтрДокумНБО);
				КонецЕсли;
				
				ТабличныйДокумент.Вывести(ОбластьТЧ);
			КонецЦикла;
			
			ОбластьПодпись = МакетСправки.ПолучитьОбласть("Подвал");
			
			Если НЕ ЭтоВерсия5_04 Тогда
				ОбластьПодпись.Параметры.НаименованиеКодФНС = НаимНО + ", " +КодНО;
				СформироватьСписокПредставленнойОтчетности_ДобавитьНижнююТаблицу(ОбластьПодпись, СтрокаПеречНБО);
			КонецЕсли;
			
			ТабличныйДокумент.Вывести(ОбластьПодпись);
			
		КонецЕсли;
		
		Если НЕ ЭтоВерсия5_04 И Счетчик <> Количество Тогда
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		
	КонецЦикла;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, ТабличныйДокумент, 3, Ложь);
	
	ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	ТабличныйДокумент.НижнийКолонтитул.ТекстВЦентре = "[&НомерСтраницы]";
	ТабличныйДокумент.НижнийКолонтитул.Выводить = Истина;
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция НазваниеОтчетаПоКНД(КНД) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	МакетКНД = КонтекстЭДОСервер.ПолучитьМакет("ОписаниеФормДляИмпорта");
	ИдентификаторОтчета = "";
	Для Инд = 1 По МакетКНД.ВысотаТаблицы Цикл
		_КНД = СокрЛП(МакетКНД.Область(Инд, 1, Инд, 1).Текст);
		
		Если _КНД = КНД Тогда
			ИдентификаторОтчета = СокрЛП(МакетКНД.Область(Инд, 2, Инд, 2).Текст);
		КонецЕсли;
			
	КонецЦикла;
	
	Если ИдентификаторОтчета = "" Тогда
		Возврат КНД;
	Иначе
		МакетСписокДокументов = Справочники.ВидыОтправляемыхДокументов.ПолучитьМакет("ОписаниеВидовОтправляемыхДокументов");
		Для Ном = 1 По МакетСписокДокументов.ВысотаТаблицы Цикл
			_ИдентификаторОтчета = СокрЛП(МакетСписокДокументов.Область(Ном, 1, Ном, 1).Текст);
			
			Если _ИдентификаторОтчета = ИдентификаторОтчета Тогда
				НаименованиеОтчета = СокрЛП(МакетСписокДокументов.Область(Ном, 2, Ном, 2).Текст);
				Возврат НаименованиеОтчета;
			КонецЕсли;
			
		КонецЦикла;
	КонецЕсли;
	
	Возврат КНД;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьСправкуОСостоянииРасчетовСФНС(ЗапросИОН, АдресФайла) Экспорт
	
	// Получаем файл xml
	ИмяФайла = ПолучитьИмяВременногоФайла("xml");
	ПолучитьИзВременногоХранилища(АдресФайла).Записать(ИмяФайла);
	
	// Дессериализуем в XDTO
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайла);
	Справка = ФабрикаXDTO.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	КНД = Справка.Документ.КНД;
	ЭтоКНД1160082 = КНД = "1160082";
	
	Документ  = Справка.Документ;
	Подписант = Документ.Подписант;
	
	// Только в КНД 1160082
	ЕстьКодНОСпр = Подписант.Свойства().Получить("КодНОСпр") <> Неопределено;
	Если ЕстьКодНОСпр Тогда
		КодНО = Подписант.КодНОСпр;
	Иначе
		КодНО = Подписант.КодНО;
	КонецЕсли;
		
	НаимНО = Подписант.НаимНО;
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	Если ЭтоКНД1160082 Тогда
		МакетСправки = ПолучитьМакетОбработки("СправкаОСостоянииРасчетовКНД1160082");
	Иначе
		МакетСправки = ПолучитьМакетОбработки("СправкаОСостоянииРасчетов");
	КонецЕсли;
	
	СвРасч = СписокИзXDTO(Документ, "СвРасч");
	
	Количество = СвРасч.Количество();
	Счетчик    = 0;
	Для каждого СтрокаСвРасч Из СвРасч Цикл
		Счетчик = Счетчик + 1;
		
		ОбластьШапка = МакетСправки.ПолучитьОбласть("Шапка");
		ОбластьШапка.Параметры.Заполнить(СтрокаСвРасч);
		
		СвНП = СтрокаСвРасч.СвНП;
		
		ДобавитьВСверкуПолучателя(ОбластьШапка, СвНП);
		
		// В КНД 1160082 отсутствует
		Если НЕ ЭтоКНД1160082 Тогда
			ОбластьШапка.Параметры.Адрес = ПредставлениеАдрРФСтрокой(СвНП.АдрРФ);
		КонецЕсли;
		
		ТабличныйДокумент.Вывести(ОбластьШапка);
		
		СвРасчВид = СписокИзXDTO(СтрокаСвРасч, "СвРасчВид");
		
		Если СвРасчВид = Неопределено Тогда
			
			ОбластьТЧ = МакетСправки.ПолучитьОбласть("ТЧ");
			ТабличныйДокумент.Вывести(ОбластьТЧ);
			
		Иначе
			
			Для каждого СтрСвРасчВид Из СвРасчВид Цикл
				ОбластьТЧ = МакетСправки.ПолучитьОбласть("ТЧ");
				ОбластьТЧ.Параметры.Заполнить(СтрСвРасчВид);
				
				// Только в КНД 1160082
				ЕстьКодНО = СтрСвРасчВид.СостСум.Свойства().Получить("КодНО") <> Неопределено;
				Если ЕстьКодНО Тогда
					ОбластьТЧ.Параметры.Заполнить(СтрСвРасчВид.СостСум);
				КонецЕсли;
				
				ДобавитьОКАТО_ОКТМО(ОбластьТЧ, СтрСвРасчВид.КодОбр);
				
				ОбластьТЧ.Параметры.Заполнить(СтрСвРасчВид.СостСум.СостРасч);
				
				ТабличныйДокумент.Вывести(ОбластьТЧ);
			КонецЦикла;
			
		КонецЕсли;
		
		ОбластьПодпись = МакетСправки.ПолучитьОбласть("Подвал");
		ОбластьПодпись.Параметры.НаименованиеКодФНС = НаимНО + ", " +КодНО;
		ТабличныйДокумент.Вывести(ОбластьПодпись);
		
		Если Счетчик <> Количество Тогда
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		
	КонецЦикла;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, ТабличныйДокумент, 3, Ложь);
	
	ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	ТабличныйДокумент.НижнийКолонтитул.ТекстВЦентре = "[&НомерСтраницы]";
	ТабличныйДокумент.НижнийКолонтитул.Выводить = Истина;
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьВыпискуОпераций(ЗапросИОН, АдресФайла) Экспорт

	// Получаем файл xml
	ИмяФайла = ПолучитьИмяВременногоФайла("xml");
	ПолучитьИзВременногоХранилища(АдресФайла).Записать(ИмяФайла);
	
	// Дессериализуем в XDTO
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайла);
	Справка = ФабрикаXDTO.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	Документ  = Справка.Документ;
	Подписант = Документ.Подписант;
	КодНО     = Подписант.КодНО;
	НаимНО    = Подписант.НаимНО;
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	МакетСправки = ПолучитьМакетОбработки("ВыпискаОпераций");
	
	ВыпОперРСБ = СписокИзXDTO(Документ, "ВыпОперРСБ");
	
	Количество = ВыпОперРСБ.Количество();
	Счетчик    = 0;
	Для каждого СтрокаВыпОперРСБ Из ВыпОперРСБ Цикл
		
		Счетчик = Счетчик + 1;
		
		ОбластьШапка = МакетСправки.ПолучитьОбласть("Шапка");
		ОбластьШапка.Параметры.Заполнить(СтрокаВыпОперРСБ);
		ОбластьШапка.Параметры.НаимНО = НаимНО;
		
		СвНП = СтрокаВыпОперРСБ.СвНП;
		
		ДобавитьВСверкуПолучателя(ОбластьШапка, СвНП);
		
		СтатусНП = СвойствоXDTO(СвНП, "СтатусНП");
		Если СтатусНП = "01" Тогда
			СтатусНП = НСтр("ru = 'Юридическое лицо'");
		ИначеЕсли СтатусНП = "02" Тогда
			СтатусНП = НСтр("ru = 'Налоговый агент'");
		ИначеЕсли СтатусНП = "09" Тогда
			СтатусНП = НСтр("ru = 'Индивидуальный предприниматель'");
		ИначеЕсли СтатусНП = "10" Тогда
			СтатусНП = НСтр("ru = 'Частный нотариус'");
		ИначеЕсли СтатусНП = "11" Тогда
			СтатусНП = НСтр("ru = 'Адвокат, учредивший адвокатский кабинет'");
		ИначеЕсли СтатусНП = "12" Тогда
			СтатусНП = НСтр("ru = 'Глава крестьянского (фермерского) хозяйства'");
		ИначеЕсли СтатусНП = "13" Тогда
			СтатусНП = НСтр("ru = 'Иное физическое лицо'");
		ИначеЕсли СтатусНП = "14" Тогда
			СтатусНП = НСтр("ru = 'Налогоплательщик, производящий выплаты физическим лицам (пп. 1 п. 1 ст. 235 НК РФ)'");
		КонецЕсли;
		
		ОбластьШапка.Параметры.СтатусНП = СтатусНП;
		
		ДобавитьОКАТО_ОКТМО(ОбластьШапка, СтрокаВыпОперРСБ);
			
		ОбластьШапка.Параметры.Адрес = ПредставлениеАдрРФСтрокой(СвНП.АдрРФ);
		
		ТабличныйДокумент.Вывести(ОбластьШапка);
		
		ЗапОперРСБ = СписокИзXDTO(СтрокаВыпОперРСБ, "ЗапОперРСБ");
		Если ЗапОперРСБ = Неопределено Тогда
			ОбластьТЧ = МакетСправки.ПолучитьОбласть("ТЧ");
			ТабличныйДокумент.Вывести(ОбластьТЧ);
		Иначе
			
			Для каждого СтрокаЗапОперРСБ Из ЗапОперРСБ Цикл
				
				ОбластьТЧ = МакетСправки.ПолучитьОбласть("ТЧ");
				ОбластьТЧ.Параметры.Заполнить(СтрокаЗапОперРСБ);
				
				ДокОтч = СвойствоXDTO(СтрокаЗапОперРСБ, "ДокОтч"); 
				Если ДокОтч <> "" Тогда
					ОбластьТЧ.Параметры.Заполнить(ДокОтч);
				КонецЕсли;
				
				Сумма = СвойствоXDTO(СтрокаЗапОперРСБ, "Сумма"); 
				Если Сумма <> "" Тогда
					ОбластьТЧ.Параметры.Заполнить(Сумма);
				КонецЕсли;
				
				ПНЗ = СвойствоXDTO(СтрокаЗапОперРСБ, "ПНЗ"); 
				Если ПНЗ <> "" Тогда
					ОбластьТЧ.Параметры.Заполнить(ПНЗ);
				КонецЕсли;
				
				ТабличныйДокумент.Вывести(ОбластьТЧ);
				
			КонецЦикла;
			
			ОбластьКонецТЧ = МакетСправки.ПолучитьОбласть("КонецТЧ");
			ТабличныйДокумент.Вывести(ОбластьКонецТЧ);
			
		КонецЕсли;
		
		ОбластьПодпись = МакетСправки.ПолучитьОбласть("Подпись");
		ОбластьПодпись.Параметры.НаименованиеКодФНС = НаимНО + ", " +КодНО;
		ТабличныйДокумент.Вывести(ОбластьПодпись);
		
		Если Счетчик <> Количество Тогда
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		
	КонецЦикла;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, ТабличныйДокумент, 3, Ложь);
	
	ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	ТабличныйДокумент.НижнийКолонтитул.ТекстВЦентре = "[&НомерСтраницы]";
	ТабличныйДокумент.НижнийКолонтитул.Выводить = Истина;
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДобавитьОКАТО_ОКТМО(Шапка, Узел)
	
	// ОКАТО, ОКТМО
	ОКАТО = СвойствоXDTO(Узел, "ОКАТО");
	ОКТМО = СвойствоXDTO(Узел, "ОКТМО");
	
	Если ЗначениеЗаполнено(ОКАТО) Тогда
		Шапка.Параметры.ОКАТО_ОКТМО = ОКАТО;
	Иначе
		Шапка.Параметры.ОКАТО_ОКТМО = ОКТМО;
	КонецЕсли;

КонецПроцедуры

&НаСервереБезКонтекста
Процедура ДобавитьВСверкуПолучателя(Шапка, Узел)
	
	ДобавитьВСверкуПолучателя_ЮЛ(Шапка, Узел);
	ДобавитьВСверкуПолучателя_ИП_ФЛ(Шапка, Узел);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ДобавитьВСверкуПолучателя_ИП_ФЛ(Шапка, Узел)
	
	ПолеИП = "НПИП";
	ПолеФЛ = "НПФЛ";
	
	ЕстьПолеИП = Узел.Свойства().Получить(ПолеИП) <> Неопределено;
	ЕстьПолеФЛ = Узел.Свойства().Получить(ПолеФЛ) <> Неопределено;
	
	Если ЕстьПолеИП ИЛИ ЕстьПолеФЛ Тогда
		
		Если ЕстьПолеИП Тогда
			НПФЛ = Узел[ПолеИП];
		ИначеЕсли ЕстьПолеФЛ Тогда
			НПФЛ = Узел[ПолеФЛ];
		КонецЕсли;
		
		Шапка.Параметры.ИНН = НПФЛ.ИННФЛ;
		Шапка.Параметры.НаименованиеОрганизации = ФИОИзТега(НПФЛ);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ДобавитьВСверкуПолучателя_ЮЛ(Шапка, Узел)
	
	// ИНН, КПП, Наименование организации
	ПолеЮЛ = "НПЮЛ"; 
	
	ЕстьПолеЮЛ = Узел.Свойства().Получить(ПолеЮЛ) <> Неопределено;
	Если ЕстьПолеЮЛ Тогда
		
		НПЮЛ = Узел[ПолеЮЛ];
		
		Шапка.Параметры.ИНН = НПЮЛ.ИННЮЛ;
		Шапка.Параметры.КПП = НПЮЛ.КПП;
		Шапка.Параметры.НаименованиеОрганизации = НПЮЛ.НаимОрг;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ФИОИзТега(Узел) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ФИОИзТега(Узел);
	
КонецФункции

&НаСервереБезКонтекста
Функция СписокИзXDTO(Узел, Поле) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СписокИзXDTO(Узел, Поле); 
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойствоXDTO(Узел, Поле) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СвойствоXDTO(Узел, Поле);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьСправкуОбИсполненииНалогоплательщикомОбязанностиПоУплатеНалогов(ЗапросИОН, АдресФайла)
	
	// Получаем файл xml
	ИмяФайла = ПолучитьИмяВременногоФайла("xml");
	ПолучитьИзВременногоХранилища(АдресФайла).Записать(ИмяФайла);
	
	// Дессериализуем в XDTO
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.ОткрытьФайл(ИмяФайла);	
	Справка = ФабрикаXDTO.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	// Получаем параметры из файла	
	ПараметрыЗаполнения = Новый Структура;

	Если Справка.Свойства().Получить("Документ") = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если Справка.Документ.Свойства().Получить("СвНП") = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если Справка.Документ.Свойства().Получить("СвПолуч") <> Неопределено Тогда
		СвПолуч = Справка.Документ.СвПолуч;
		Если СвПолуч.Свойства().Получить("ПолучЮЛ") <> Неопределено Тогда
			ПараметрыЗаполнения.Вставить("КПП", СвПолуч.ПолучЮЛ.КПП);
		Иначе
			ПараметрыЗаполнения.Вставить("КПП", "");
		КонецЕсли;
	КонецЕсли;
	
	ФИОИнспектора = "";
	Если Справка.Документ.Свойства().Получить("Подписант") <> Неопределено Тогда
		Подписант = Справка.Документ.Подписант;
		Если Подписант.Свойства().Получить("ФИО") <> Неопределено Тогда
			
			ФИО = Подписант.ФИО;
			
			ФИОСтруктура = Новый Структура();
			Если ФИО.Свойства().Получить("Фамилия") <> Неопределено Тогда
				ФИОСтруктура.Вставить("Фамилия", ФИО.Фамилия);
			Иначе
				ФИОСтруктура.Вставить("Фамилия", "");
			КонецЕсли;
			
			Если ФИО.Свойства().Получить("Имя") <> Неопределено Тогда
				ФИОСтруктура.Вставить("Имя", ФИО.Фамилия);
			Иначе
				ФИОСтруктура.Вставить("Имя", "");
			КонецЕсли;
			
			Если ФИО.Свойства().Получить("Отчество") <> Неопределено Тогда
				ФИОСтруктура.Вставить("Отчество", ФИО.Отчество);
			Иначе
				ФИОСтруктура.Вставить("Отчество", "");
			КонецЕсли;
			
			ФИОИнспектора = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(ФИОСтруктура);
			
		КонецЕсли;
	КонецЕсли;
	ПараметрыЗаполнения.Вставить("ФИОИнспектора", ФИОИнспектора);
	
	Налогоплательщик = Справка.Документ.СвНП;
	Если Налогоплательщик.Свойства().Получить("НПЮЛ") <> Неопределено Тогда
		ПараметрыЗаполнения.Вставить("ИНН", Налогоплательщик.НПЮЛ.ИННЮЛ);
		ПараметрыЗаполнения.Вставить("Налогоплательщик", Налогоплательщик.НПЮЛ.НаимОрг);	
	Иначе
		ПараметрыЗаполнения.Вставить("ИНН", Налогоплательщик.НПФЛ.ИННФЛ);
		ФИО = Налогоплательщик.НПФЛ.ФИО.Фамилия + " " + Налогоплательщик.НПФЛ.ФИО.Имя;
		Если Налогоплательщик.НПФЛ.ФИО.Свойства().Получить("Отчество") <> Неопределено Тогда
			ФИО = ФИО + " " + Налогоплательщик.НПФЛ.ФИО.Отчество;
		КонецЕсли;
		ПараметрыЗаполнения.Вставить("Налогоплательщик", ФИО);
	КонецЕсли;
	
	Если Налогоплательщик.Свойства().Получить("АдрРФ") <> Неопределено Тогда
		ПараметрыЗаполнения.Вставить("Адрес", ПредставлениеАдрРФСтрокой(Налогоплательщик.АдрРФ));
	КонецЕсли;
	Подписант = Справка.Документ.Подписант;
	ПараметрыЗаполнения.Вставить("НалоговыйОрган", Подписант.НаимНО + ", " + Подписант.КодНО);

	Обязанности = Справка.Документ.СпрИспОбяз;
	
	ПараметрыЗаполнения.Вставить("НомерСправки", Обязанности.НомСпр);
	ПараметрыЗаполнения.Вставить("ПоСостояниюНа", Дата(Прав(Обязанности.ДатаСостСв, 4), Сред(Обязанности.ДатаСостСв, 4, 2), Лев(Обязанности.ДатаСостСв, 2)));
	ПараметрыЗаполнения.Вставить("НаличиеОбязанностей", Булево(Число(Обязанности.ПрНеиспОбяз)));
	
	ПараметрыЗаполнения.Вставить("СписокИФНС", Новый Массив);
	Если Обязанности.Свойства().Получить("ПерНО") <> Неопределено Тогда
		Если ТипЗнч(Обязанности.ПерНО.КодИФНС) = Тип("СписокXDTO") Тогда
			Для Каждого КодИФНС Из Обязанности.ПерНО.КодИФНС Цикл
				ПараметрыЗаполнения.СписокИФНС.Добавить(КодИФНС);
			КонецЦикла;
		Иначе
			ПараметрыЗаполнения.СписокИФНС.Добавить(Обязанности.ПерНО.КодИФНС);
		КонецЕсли;
	КонецЕсли;
	
	Если ПараметрыЗаполнения.НаличиеОбязанностей И ПараметрыЗаполнения.СписокИФНС.Количество() > 0 Тогда
		ПараметрыЗаполнения.Вставить("Приложение", "Приложение: на 1 листах.***");
	КонецЕсли;
	
	
	// Формируем табличный документ
	ТабличныйДокумент = Новый ТабличныйДокумент;
	
	МакетСправки = ПолучитьМакетОбработки("СправкаОбИсполненииНалогоплательщикомОбязанностиПоУплатеНалогов");
	
	// Основная часть справки
	СправкаОбласть = МакетСправки.ПолучитьОбласть("Справка");

	СправкаОбласть.Параметры.Заполнить(ПараметрыЗаполнения);
	СправкаОбласть.Параметры.ПоСостояниюНа = Формат(ПараметрыЗаполнения.ПоСостояниюНа, "ДФ='dd MMMM yyyy ""г.""'");
	СправкаОбласть.Параметры.НаличиеОбязанностей = Формат(ПараметрыЗаполнения.НаличиеОбязанностей, "БЛ='НЕ ИМЕЕТ'; БИ=ИМЕЕТ");
	
	ТабличныйДокумент.Вывести(СправкаОбласть);
	
	СноскаОбласть = МакетСправки.ПолучитьОбласть("Сноска");
	ТабличныйДокумент.Вывести(СноскаОбласть);
	
	// Приложение к справке
	Если ПараметрыЗаполнения.НаличиеОбязанностей И ПараметрыЗаполнения.СписокИФНС.Количество() > 0 Тогда
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
		ОбластьШапкаПриложения = МакетСправки.ПолучитьОбласть("ШапкаПриложения");
		ОбластьШапкаПриложения.Параметры.Заполнить(ПараметрыЗаполнения);
		ТабличныйДокумент.Вывести(ОбластьШапкаПриложения);
		
		ОбластьСтрокаПриложения = МакетСправки.ПолучитьОбласть("СтрокаПриложения");
		ОбластьПустаяСтрокаПриложения = МакетСправки.ПолучитьОбласть("СтрокаПриложенияПустая");
		ОбластьПодвалПриложения = МакетСправки.ПолучитьОбласть("ПодвалПриложения");
		
		ПорядковыйНомер = 1;
		Для Каждого КодИФНС Из ПараметрыЗаполнения.СписокИФНС Цикл
			ОбластьСтрокаПриложения.Параметры.НомерПоПорядку = Строка(ПорядковыйНомер)
				+ ?(ПорядковыйНомер = ПараметрыЗаполнения.СписокИФНС.Количество(), "**", "") + ".";
			ОбластьСтрокаПриложения.Параметры.Испекция = КодИФНС;
			Если ПорядковыйНомер = ПараметрыЗаполнения.СписокИФНС.Количество() Тогда
				ОбластьСтрокаПриложения.Параметры.ПереносСтроки = ".";
			Иначе
				ОбластьСтрокаПриложения.Параметры.ПереносСтроки = ";";
			КонецЕсли;
			Если ПорядковыйНомер = 1 Тогда
				ОбластьСтрокаПриложения.Параметры.ПереносСтроки = ОбластьСтрокаПриложения.Параметры.ПереносСтроки + "*";
			КонецЕсли;
			
			ПорядковыйНомер = ПорядковыйНомер + 1;
			
			ТабличныйДокумент.Вывести(ОбластьСтрокаПриложения);
		КонецЦикла;
		Для Индекс = ПорядковыйНомер По 24 Цикл
			ТабличныйДокумент.Вывести(ОбластьПустаяСтрокаПриложения);			
		КонецЦикла;
		
		ТабличныйДокумент.Вывести(ОбластьПодвалПриложения);
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, ТабличныйДокумент, 2, Истина);
	
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПредставлениеАдрРФСтрокой(Знач Адрес)
    
    АдресСтрокой = "";
    АдресМассив = Новый Массив;
    Если Адрес.Свойства().Получить("Индекс") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Индекс);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("КодРегион") <> Неопределено Тогда
        Регион = "";
        МакетРегионы = ПолучитьМакетОбработки("КодыРегионов353приказ");	
        Для Инд = 1 По МакетРегионы.ВысотаТаблицы Цикл
            ТекРегион = МакетРегионы.Область(Инд, 1, Инд, 1).Текст;		
            Если ТекРегион = Адрес.КодРегион Тогда
                Регион = СокрЛП(МакетРегионы.Область(Инд, 2, Инд, 2).Текст);
            КонецЕсли;	
		КонецЦикла;
		АдресМассив.Добавить(Регион);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("Район") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Район);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("Город") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Город);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("НаселПункт") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.НаселПункт);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("Улица") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Улица);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("Дом") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Дом);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("Корпус") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Корпус);
    КонецЕсли;
    
    Если Адрес.Свойства().Получить("Кварт") <> Неопределено Тогда
        АдресМассив.Добавить(Адрес.Кварт);
	КонецЕсли;
	
	Возврат СтрСоединить(АдресМассив, ", ");

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКоличествоДокументовПоОписиВходящихДокументов(ОписьВходящихДокументовСсылка) Экспорт
	
	МассивТребований = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ 
		|	Количество(ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.СсылкаНаОбъект) КАК КоличествоДокументов
		|ИЗ
		|	Справочник.ОписиВходящихДокументовИзНалоговыхОрганов.ВходящиеДокументы КАК ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы
		|ГДЕ
		|	ОписиВходящихДокументовИзНалоговыхОргановВходящиеДокументы.Ссылка = &ОписьВходящихДокументовСсылка";

	Запрос.УстановитьПараметр("ОписьВходящихДокументовСсылка", ОписьВходящихДокументовСсылка);
	
	Результат = Запрос.Выполнить();

	Выборка = Результат.Выбрать();

	Если Выборка.Следующий() Тогда
	
		Возврат Выборка.КоличествоДокументов;	
		
	Иначе
		
		Возврат 0;	
	
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция НоваяОписьВходящихДокументовИзНалоговыхОрганов(
		СвойстваОписиВходящихДокументов,
		ВложенияПриложение,
		ТранспортноеСообщение) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НоваяОписьВходящихДокументовИзНалоговыхОрганов(
		СвойстваОписиВходящихДокументов,
		ВложенияПриложение,
		ТранспортноеСообщение);
			
КонецФункции

#Область ОтправкаОтветаНаТребованиеКДекларацииНДС

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС(
		ДокументСсылка,
		Организация,
		УникальныйИдентификаторФормы,
		ВыполняемоеОповещение) Экспорт
		
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("ДокументСсылка",               ДокументСсылка);
	КонтекстДлительнойОперации.Вставить("ВыполняемоеОповещение",        ВыполняемоеОповещение);
	КонтекстДлительнойОперации.Вставить("УникальныйИдентификаторФормы", УникальныйИдентификаторФормы);
	КонтекстДлительнойОперации.Вставить("Организация",                  Организация);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		КонтекстДлительнойОперации);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ДокументСсылка, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт

	ДокументСсылка = ВходящийКонтекст.ДокументСсылка;	
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ДокументСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 6;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= ВходящийКонтекст;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка ответа на требование НДС
		Возврат;
	КонецЕсли;
		
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС",  0.1, Истина); 
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС() Экспорт
		
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	ДокументСсылка = КонтекстДлительнойОперации.ДокументСсылка;
	
	ДополнительныеПараметры = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(КонтекстДлительнойОперации);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи...");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДСПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ДокументСсылка,ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
		
	ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС(
		ВходящийКонтекст.ДокументСсылка,
		ВходящийКонтекст.Организация,
		ВходящийКонтекст.УникальныйИдентификаторФормы,
		ВходящийКонтекст.ВыполняемоеОповещение);

КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДСПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ДокументСсылка, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	ДокументСсылка 				 = ВходящийКонтекст.ДокументСсылка;
	УникальныйИдентификаторФормы = ВходящийКонтекст.УникальныйИдентификаторФормы;
	ВыполняемоеОповещение 		 = ВходящийКонтекст.ВыполняемоеОповещение;
	
	ОтправкаОбъектаЗапрещена = ДокументооборотСКОВызовСервера.ОтправкаОбъектаЗапрещенаНаТарифеПромоЕНС(ДокументСсылка);
	Если ОтправкаОбъектаЗапрещена Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНевозможностиОтправкиНаТарифе(ДокументСсылка);
		Возврат;
	КонецЕсли;	
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", ДокументСсылка);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификаторФормы);
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// отражаем изменения в базе
	Если НЕ ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ДокументСсылка, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ДокументСсылка, ВыполняемоеОповещение, Ложь);//+
			
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС, ответ на требование
		КонецЕсли;
		
		Возврат;
	КонецЕсли;
	
	Если НЕ ДокументооборотСКОВызовСервера.ОрганизацияПодключенаКИнспекции(ДокументСсылка)
		ИЛИ НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ДокументСсылка) Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ДокументСсылка, ВыполняемоеОповещение, Ложь);//+
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", 	ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", 		КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДСПослеДобавлениеПодписейКакПриложение", ЭтотОбъект, Контекст);
		
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
		
	// добавляем подписи для каждого из документов из массива ТребующиеПодписания	
	ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложение(Оповещение, КонтекстОтправки)
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	ДокументСсылка 	= Контекст.Документ;
	
	// проверяем, включен ли механизм обмена с ФНС для организации, указанной в ответе на требование
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	//требуется сформировать файл доверенности 
	//и определить налоговый орган (для записи в реквизит цикла обмена)
	//для этого определяем ИФНС (там содержатся данные по доверенности)

	// проверяем, указан ли получатель
	НалоговыйОрган = ДокументСсылка.НалоговыйОрган;
	Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
		ТекстСообщения = НСтр("ru = 'Не указан получатель - налоговый орган.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	КодНалоговогоОргана = СокрЛП(НалоговыйОрган.Код);
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
	Если НЕ ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	//теперь формируем файл доверенности
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(Доверенность) Тогда
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(
			ЭлРегистрацияВИФНС,
			КороткоеИмяФайлаДоверенности,
			Организация,,,
			Истина);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось выгрузить файл доверенности.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
		
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.Предмет = ДокументСсылка;
	ЦиклОбмена.Идентификатор = СгенерироватьUUID();
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = НалоговыйОрган;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.Представление;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = НалоговыйОрган;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	РезультатВыгрузкиОбъектаОтправки = Неопределено;
	Если НЕ ВыгрузитьПоясненияКДекларацииПоНДС(Контекст, РезультатВыгрузкиОбъектаОтправки, ЭлРегистрацияВИФНС) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли; 
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
 	//добавляем файл Представление
	ДанныеФайлаПредставление = РезультатВыгрузкиОбъектаОтправки.ДанныеФайлаПредставление;
	ИмяФайлаПредставления = ДанныеФайлаПредставление.ИмяФайла;
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(ДанныеФайлаПредставление.АдресВременногоХранилища);
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление, ДвоичныеДанныеФайла, ИмяФайлаПредставления, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.xml) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	ТребующиеПодписания = Новый Массив;
	
	ДанныеФайлаВыгрузкиОбъектаОтправки = РезультатВыгрузкиОбъектаОтправки.ДанныеФайлаВыгрузкиОбъектаОтправки;
	
	ИдФайл		 		= ДанныеФайлаВыгрузкиОбъектаОтправки.ИдФайл;
	ИдФайлЭП			= ДанныеФайлаВыгрузкиОбъектаОтправки.ИдФайлЭП;
	АдресФайлаДанных 	= ДанныеФайлаВыгрузкиОбъектаОтправки.АдресВременногоХранилища;
	
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(АдресФайлаДанных);
	
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
			ТранспортноеСообщение.Ссылка, 
			Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение, 
			ДвоичныеДанныеФайла, ИдФайл, 
			, , , , , , 
			СгенерироватьUUID(), 
			Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			
		ТекстСообщения = НСтр("ru = 'Отправка прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	ДанныеФайла = Новый Структура;
	ДанныеФайла.Вставить("ИмяФайла", 				 ИдФайл);
	ДанныеФайла.Вставить("ИдФайлЭП", 				 ИдФайлЭП);
	ДанныеФайла.Вставить("АдресВременногоХранилища", АдресФайлаДанных);
	
	ТребующиеПодписания.Добавить(ДанныеФайла);
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
				ТранспортноеСообщение.Ссылка, 
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, 
				ФайлДоверенности, 
				КороткоеИмяФайлаДоверенности, 
				, , , , , , 
				СгенерироватьUUID(), 
				Перечисления.ТипыСодержимогоФайлов.xml) Тогда
				
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
		
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(ДокументСсылка, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	Контекст.Вставить("ТребующиеПодписания", ТребующиеПодписания);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложение(ВыполняемоеОповещение, Контекст)
	
	ТребующиеПодписания = Контекст.ТребующиеПодписания;
	УчетнаяЗапись = Контекст.УчетнаяЗапись;
	ТранспортноеСообщение = Контекст.ТранспортноеСообщение;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗапись);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Контекст);
	
	ОтпечатокСертификатаРуководителя = ПолучитьОтпечаток(УчетнаяЗапись, "Руководитель",);
	СертификатРуководителя = Новый Структура("Отпечаток, КлючевойКонтейнер",
	ОтпечатокСертификатаРуководителя, РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, СертификатРуководителя);
	
	НомерФайлаТребующегоПодписания = 0;
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) И ТребующиеПодписания.Количество() > 1 И ВключитьПакетнуюПодпись Тогда
		ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПакетно(ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, СертификатРуководителя);
	Иначе	
		ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПоследовательно(ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, НомерФайлаТребующегоПодписания, СертификатРуководителя);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПоследовательно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		НомерФайлаТребующегоПодписания,
		Сертификат,
		МенеджерКриптографии = Неопределено,
		Алгоритм = Неопределено)
		
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Сертификат);
	
	Если НомерФайлаТребующегоПодписания <= ТребующиеПодписания.ВГраница() Тогда
		
		ДанныеФайла = ТребующиеПодписания[НомерФайлаТребующегоПодписания];
		
		ДополнительныеПараметры = Новый Структура(
			"ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, НомерФайлаТребующегоПодписания, СертификатРуководителя", 
			ВыполняемоеОповещение, ТранспортноеСообщение, ТребующиеПодписания, НомерФайлаТребующегоПодписания, Сертификат);
			
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПоследовательноПослеПодписанияФайла", ЭтотОбъект, ДополнительныеПараметры);
		
		АлгоритмПодписи = Алгоритм;
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Сертификат);
		Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
			ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеФайла.ИмяФайла);
			ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ОтветНаТребованиеПоясненийКДекларацииНДС", ТранспортноеСообщение); 
			АлгоритмПодписи = ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации);
		КонецЕсли;

		КриптографияЭДКОКлиент.ПодписатьPKCS7(
			ОписаниеОповещения,
			Сертификат,
			ДанныеФайла.АдресВременногоХранилища,
			Ложь,,,
			МенеджерКриптографии,,
			АлгоритмПодписи);
		
	Иначе
		
		//Все подписи добавлены
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПоследовательноПослеПодписанияФайла(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Ложь);	
		Возврат;
	КонецЕсли;
	
	ТранспортноеСообщение 			= ДополнительныеПараметры.ТранспортноеСообщение;
	ТребующиеПодписания 			= ДополнительныеПараметры.ТребующиеПодписания;
	НомерФайлаТребующегоПодписания 	= ДополнительныеПараметры.НомерФайлаТребующегоПодписания;
	Сертификат			 			= ДополнительныеПараметры.СертификатРуководителя;
	
	ДанныеФайла = ТребующиеПодписания[НомерФайлаТребующегоПодписания];
	
	КороткоеИмяФайлаПодписи = ДанныеФайла.ИдФайлЭП;
	
	// добавляем подпись как приложение
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение, Результат.ФайлПодписи, КороткоеИмяФайлаПодписи, , , , , , , СгенерироватьUUID(), ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn")) Тогда
		
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания подписи для документа %1'."), ДанныеФайла.ИмяФайла);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Ложь);	
		Возврат;
		
	КонецЕсли;

	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Сертификат);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
	
		МенеджерКриптографии = Неопределено;
		
	Иначе
			
		МенеджерКриптографии = Результат.МенеджерКриптографии;
		
	КонецЕсли;
	
	Алгоритм = ?(МенеджерКриптографии <> Неопределено, Результат.Алгоритм, Неопределено);
	
	ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПоследовательно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		НомерФайлаТребующегоПодписания + 1,
		Сертификат,
		МенеджерКриптографии,
		Алгоритм);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДСПослеДобавлениеПодписейКакПриложение(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки);
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС, ответ на требование
		
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Запись данных...");
	
	// формируем пакет
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДСПослеФормированияПакета", ЭтотОбъект, ВходящийКонтекст);
	
	ПараметрыОперации = Неопределено;
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ПараметрыОперации = Новый Структура("ТребуетПодтверждениОперации", Ложь);
	КонецЕсли;	
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС", ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
	Если Не Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС, ответ на требование
		Возврат;
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Отправка...");
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФНС, ответ на требование
		Возврат;
	КонецЕсли;
	
	ОчиститьПустыеСтрокиПоясненияКДекларацииПоНДС(КонтекстОтправки.Документ);
	
	Оповестить("Запись_ПоясненияКДекларацииПоНДС", , КонтекстОтправки.Документ);
	
	ДополнительныеПараметры.Вставить("РезультатОтправки", Истина);
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФНС, ответ на требование
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОчиститьПустыеСтрокиПоясненияКДекларацииПоНДС(Пояснение)
	
	Документы.ПоясненияКДекларацииПоНДС.ОчиститьПустыеСтроки(Пояснение);

КонецПроцедуры

&НаСервереБезКонтекста
Функция ВыгрузитьПоясненияКДекларацииПоНДС(Контекст, РезультатВыгрузкиОбъектаОтправки, ЭлРегистрацияВИФНС)
	
	РезультатВыгрузкиОбъектаОтправки = Новый Структура;
	
	УникальныйИдентификаторФормы = Контекст.УникальныйИдентификатор;
	ДокументСсылка = Контекст.Документ;
	
	ДанныеФайлаВыгрузкиОбъектаОтправки = ПолучитьФайлВыгрузкиОтветаНаТребованиеПояснений(ДокументСсылка, УникальныйИдентификаторФормы);	
	Если ДанныеФайлаВыгрузкиОбъектаОтправки = Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось получить файл выгрузки пояснений.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;	
	
	РезультатВыгрузкиОбъектаОтправки.Вставить("ДанныеФайлаВыгрузкиОбъектаОтправки", ДанныеФайлаВыгрузкиОбъектаОтправки);
	
	Организация 			= ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "Организация");
	КодНалоговогоОргана 	= ЭлРегистрацияВИФНС.Код;
	ПараметрыОписи 			= СформироватьПараметрыОписиИсходящихДокументов(Организация, ЭлРегистрацияВИФНС, КодНалоговогоОргана);
	
	Если НЕ ПроверитьЗаполнениеПараметровПриОтправкеПредставления(ПараметрыОписи) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	// определяем ИмяФайлТребПояс
	ИмяФайлаТребованияПояснений = ПолучитьИмяФайлаТребованияПояснений(ДокументСсылка.Требование);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаТребованияПояснений) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось получить имя файла требования пояснений.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;	
	ПараметрыОписи.Вставить("ИмяФайлТребПояс", ИмяФайлаБезРасширения(ИмяФайлаТребованияПояснений));
	
	ПараметрыОписи.Вставить("ЭтоОтветНаТребованиеПоНДС", Истина);
	
	// определяем ИдФайлНалОтч
	ИмяФайлаДекларацииНДС = ПолучитьИмяФайлаДекларацииНДС(ДокументСсылка);
	Если НЕ ЗначениеЗаполнено(ИмяФайлаДекларацииНДС) Тогда
		ТекстСообщения = НСтр("ru = 'Не указана декларация по НДС, для которой отправляется пояснение.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	ПараметрыОписи.Вставить("ИдФайлНалОтч", ИмяФайлаБезРасширения(ИмяФайлаДекларацииНДС));

	// формируем XML представления и имя файла представления
	ИмяФайлаПредставления = Неопределено;
	КолФайл = 2;
	ТекстПредставления = СформироватьXMLПредставления(
		Организация, 
		ДанныеФайлаВыгрузкиОбъектаОтправки, 
		ПараметрыОписи, 
		ИмяФайлаПредставления, 
		КолФайл);
		
	Если НЕ ЗначениеЗаполнено(ТекстПредставления) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось сформировать XML файл описи исходящих документов.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	КодировкаТекстаДанных = "windows-1251";
	ДвоичныеДанныеФайла = ПолучитьДвоичныеДанныеИзСтроки(ТекстПредставления, КодировкаТекстаДанных);
	СсылкаНаДвоичныеДанныеФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла, УникальныйИдентификаторФормы);
	
	ДанныеФайлаПредставление = Новый Структура;
	ДанныеФайлаПредставление.Вставить("ИмяФайла", ИмяФайлаПредставления);
	ДанныеФайлаПредставление.Вставить("АдресВременногоХранилища", СсылкаНаДвоичныеДанныеФайла);
	
	
	РезультатВыгрузкиОбъектаОтправки.Вставить("ДанныеФайлаПредставление", ДанныеФайлаПредставление);
	
	Возврат Истина;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьФайлВыгрузкиОтветаНаТребованиеПояснений(ДокументСсылка, УникальныйИдентификаторФормы)
	
	Возврат Документы.ПоясненияКДекларацииПоНДС.ПолучитьФайлВыгрузки(ДокументСсылка, УникальныйИдентификаторФормы);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИмяФайлаТребованияПояснений(Требование)

	КонтекстЭДОСервер 		= ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ПриложенияТребования 	= КонтекстЭДОСервер.ПолучитьФайлыДокументовРеализацииПолномочийНалоговыхОрганов(Требование);
	
	// Первая попытка - ищем pdf-файл
	// Среди всех приложений требования ищем pdf-файл.
	Для каждого ПриложениеТребования Из ПриложенияТребования Цикл
		
		СвойстваФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ПриложениеТребования.ИмяФайла);
		
		Если ВРЕГ(СвойстваФайла.Расширение) <> ВРЕГ(".xml") Тогда
			Возврат СвойстваФайла.Имя;
		КонецЕсли;
	
	КонецЦикла;
	
	// Вторая попытка - ищем xml-файл
	Для каждого ПриложениеТребования Из ПриложенияТребования Цикл
		
		Если ЛЕВ(ПриложениеТребования.ИмяФайла, 8) = "1165050_" Тогда
			Возврат СвойстваФайла.Имя;
		КонецЕсли;
	
	КонецЦикла;
	
	Возврат Неопределено;

КонецФункции 

&НаСервереБезКонтекста
Функция ПолучитьИмяФайлаДекларацииНДС(Пояснение)

	Декларация = Пояснение.Декларация;
	
	Если ЗначениеЗаполнено(Декларация)
		И ТипЗнч(Декларация) = Тип("Строка") Тогда
		
		Возврат Декларация;
		
	Иначе
		
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
		Если ЗначениеЗаполнено(Декларация) Тогда
			Возврат КонтекстЭДОСервер.ИдФайлаДекларацииНДС(Декларация);
		Иначе
			Возврат Неопределено;
		КонецЕсли;
			
	КонецЕсли;

КонецФункции 

&НаКлиенте
Процедура ПоказатьФормуВыбораТребованияФНС(
		ОписаниеОповещения, 
		Организация = Неопределено, 
		Знач ВидДокумента = Неопределено,
		Требования = Неопределено) Экспорт
	
	ВидыДокумента = Новый Массив;
	Если ВидДокумента = Неопределено Тогда  
		ВидыДокумента.Добавить(ПредопределенноеЗначение("Перечисление.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов"));
		ВидыДокумента.Добавить(ПредопределенноеЗначение("Перечисление.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС"));
	Иначе
		ВидыДокумента.Добавить(ВидДокумента);
	Конецесли;	
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВидыДокумента", 	ВидыДокумента);
	ДополнительныеПараметры.Вставить("Организация", 	Организация);
	ДополнительныеПараметры.Вставить("Требования", 		Требования);
	
	ОткрытьФорму("Справочник.ДокументыРеализацииПолномочийНалоговыхОрганов.Форма.ФормаВыбораТребования", 
		ДополнительныеПараметры,,,,,ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры
	
&НаКлиенте
Процедура СоздатьОтветНаТребованиеСВыборомТребования(
		ОписьВходящихДокументов = Неопределено, // Если указано, то список требований берется из описи
		Организация 			= Неопределено, // Если указана, то в список фильтруется по ней
		ВходящийКонтекст 		= Неопределено) Экспорт
	
	// Если требование одно, то форму выбора не показываем
	Если ОписьВходящихДокументов = Неопределено Тогда
		Требования = Неопределено;
	Иначе
		Требования = ТребованияФНСВызовСервера.ТребованияВОписи(ОписьВходящихДокументов);
		Если Требования.Количество() = 1 Тогда
			СоздатьОтветНаТребованиеФНС(Требования[0], ВходящийКонтекст);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СоздатьОтветНаТребованиеФНС", 
		ЭтотОбъект,
		ВходящийКонтекст);
	
	ПоказатьФормуВыбораТребованияФНС(ОписаниеОповещения, Организация, , Требования);
	
КонецПроцедуры
	
&НаКлиенте
Процедура СоздатьОтветНаТребованиеФНС(Требование, Знач ДополнительныеПараметры = Неопределено) Экспорт
	
	ВидТребования = Неопределено;
	Если Требование <> Неопределено Тогда
		
		ВидТребования = ЗначенияРеквизитовОбъекта(Требование, "ВидДокумента")["ВидДокумента"];
		
		Если ВидТребования = ПредопределенноеЗначение("Перечисление.ВидыНалоговыхДокументов.ТребованиеОПредставленииДокументов") Тогда
			
			СоздатьОтветНаТребованиеДокументовСВыборомВидаОтвета(Требование);
			
		ИначеЕсли ВидТребования = ПредопределенноеЗначение("Перечисление.ВидыНалоговыхДокументов.ТребованиеОПредставленииПоясненийКДекларацииНДС") Тогда
			// Создание ответа в виде
			// - Пояснения к декларации по НДС (для НДС)
			// - Письма (для других отчетов)
			СоздатьПоясненийКДекларацииНДССВыборомВидаПояснения(Требование, ДополнительныеПараметры);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ДополнительныеПараметры <> Неопределено
		И ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ВыполняемоеОповещение")
		И ДополнительныеПараметры.ВыполняемоеОповещение <> Неопределено Тогда
		
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, ВидТребования);
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Функция СоздатьПоясненийКДекларацииНДССВыборомВидаПояснения(Требование, ВходящийКонтекст = Неопределено) Экспорт
	
	ВТребованииЕстьXMLФайл = ЕстьТребованияКРазделам8_12(Требование);
	
	ЕстьРеестрИмущество  = ДокументооборотСКОВызовСервера.ЕстьУведомление("РеестрЛьготИмущество");
	ЕстьРеестрНДС        = ДокументооборотСКОВызовСервера.ЕстьУведомление("РеестрДокументовПодтверждающихЛьготы");

	СписокВыбора  = Новый СписокЗначений;
	СписокВыбора.Добавить(1, "Пояснение к декларации по НДС");
	СписокВыбора.Добавить(3, ОтветПакетом());
	
	Если ЕстьРеестрНДС Тогда
		СписокВыбора.Добавить(7, "Реестр документов, подтверждающих льготы по НДС");
	КонецЕсли;
	
	Если НЕ ВТребованииЕстьXMLФайл И ЕстьРеестрИмущество Тогда
		СписокВыбора.Добавить(8, "Реестр документов, подтверждающих льготы по налогу на имущество");
	КонецЕсли;
	
	ВыбратьВидОтвета(СписокВыбора, Требование);
			
КонецФункции

&НаКлиенте
Процедура СоздатьПоясненияКДекларацииПоНДС(Требование) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	
	ЭлементыОтбора = Новый Структура;
	ЭлементыОтбора.Вставить("ОснованиеСсылка", Требование);
	
	ДополнительныеПараметры.Вставить("ЗначенияЗаполнения", ЭлементыОтбора);
	
	ОткрытьФорму("Документ.ПоясненияКДекларацииПоНДС.ФормаОбъекта", ДополнительныеПараметры, , Новый УникальныйИдентификатор);
	
КонецПроцедуры

&НаСервере
Функция ЕстьТребованияКРазделам8_12(ДокументыРеализацииПолномочий)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЕстьТребованияКРазделам8_12(ДокументыРеализацииПолномочий);
	
КонецФункции

&НаКлиенте
Процедура СоздатьОтветНаДокументРеализацииПолномочий(ДокументыРеализацииПолномочийСсылка, ДополнительныеПараметры = Неопределено) Экспорт
	
	ЭтоТребованиеФНС = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоТребованиеФНС(ДокументыРеализацииПолномочийСсылка);
	Если ЭтоТребованиеФНС Тогда		
		// Создание ответа на
		// - требование документов
		// - требование по Декларации по НДС
		СоздатьОтветНаТребованиеФНС(ДокументыРеализацииПолномочийСсылка, ДополнительныеПараметры);
	Иначе
		// Создание письма на 
		// - уведомления
		СоздатьПисьмоВОтветНаДокументРеализацииПолномочийСВыборомВарианта(ДокументыРеализацииПолномочийСсылка);

	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПисьмоВОтветНаДокументРеализацииПолномочий(Требование) Экспорт
	
	Значения = ЗначенияРеквизитовОбъекта(Требование, "НомерДокумента, ДатаДокумента");
	
	ЗначенияЗаполнения = Новый Структура;
	ЗначенияЗаполнения.Вставить("ОснованиеСсылка", Требование);
	ЗначенияЗаполнения.Вставить("Дата",  Значения.ДатаДокумента);
	ЗначенияЗаполнения.Вставить("Номер", Значения.НомерДокумента);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
		
	ОткрытьФорму("Справочник.ПерепискаСКонтролирующимиОрганами.ФормаОбъекта", ДополнительныеПараметры,,Новый УникальныйИдентификатор);
		
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтветыНаТребованияФНС(Ответы) Экспорт
	
	Если Ответы.Количество() > 1 Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Ответы", Ответы);

		ОткрытьФорму(ПутьКОбъекту + ".Форма.ОтветыНаТребованияФНС", ДополнительныеПараметры,, Новый УникальныйИдентификатор);
		
	ИначеЕсли Ответы.Количество() = 1 Тогда
		
		ПоказатьЗначение(, Ответы[0]);
		
	Иначе
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Ответы отсутствуют'"));
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьОтправленныеОтветыНаТребованияФНС(Требование) Экспорт
	
	ПоказатьОтветыНаТребованияФНС(ОтправленныеОтветыНаТребованияФНС(Требование));
	
КонецПроцедуры

&НаСервере
Функция ОтправленныеОтветыНаТребованияФНС(Требование) Экспорт
	
	Возврат Справочники.ДокументыРеализацииПолномочийНалоговыхОрганов.ОтправленныеОтветыНаТребованиеПоясненийКДекларацииНДС(Требование);
	
КонецФункции

&НаСервере
Функция ОтветыНаТребование(ТребованиеСсылка, ВключаяНевозможностьПредоставленияДокументов = Истина) Экспорт
	
	Возврат ТребованияФНС.ОтветыНаТребование(ТребованиеСсылка, , ВключаяНевозможностьПредоставленияДокументов);
	
КонецФункции

&НаСервере
Функция ОтветыНаТребованиеФСС(ТребованиеСсылка) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтветыНаТребованиеФСС(ТребованиеСсылка);
	
КонецФункции

&НаКлиенте
Процедура ПодготовитьКорректирующуюДекларациюПоНДСИзПояснения(Пояснение) Экспорт
	
	ПараметрыОткрытия = Новый Структура();
	ПараметрыОткрытия.Вставить("Раздел", ПредопределенноеЗначение("Перечисление.СтраницыЖурналаОтчетность.Отчеты"));
	
	ОткрытьФорму("ОбщаяФорма.РегламентированнаяОтчетность", ПараметрыОткрытия, , "1С-Отчетность", );
	
	Оповестить("Открытие формы 1С-Отчетность", ПараметрыОткрытия);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьРеквизитыНалогоплательщикаИзВходящегоТребования(ТранспортноеСообщениеВходящегоТребования)
	
	// извлекаем вложения расшифрованного входящего сообщения
	Вложения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщениеВходящегоТребования.Ссылка, Истина);
	ВложенияДокумент = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ));
	Если ВложенияДокумент.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ВложениеДокумент = ВложенияДокумент[0];
	
	// считываем документ
	ВремФайлДокумент = ПолучитьИмяВременногоФайла();
	ВложениеДокумент.Данные.Получить().Записать(ВремФайлДокумент);
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ВремФайлДокумент);
	
	УзлыСвНП = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя", "СвНП"), Истина);
	Если УзлыСвНП.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвНП = УзлыСвНП[0];
	
	Результат = Новый Структура();
	Результат.Вставить("НаимОрг", 	ИзвлечьДанное(УзелСвНП, "НаимОрг"));
	Результат.Вставить("ИННЮЛ", 	ИзвлечьДанное(УзелСвНП, "ИННЮЛ"));
	Результат.Вставить("КПП", 		ИзвлечьДанное(УзелСвНП, "КПП"));
	Результат.Вставить("ИННФЛ", 	ИзвлечьДанное(УзелСвНП, "ИННФЛ"));
	Результат.Вставить("Фамилия", 	ИзвлечьДанное(УзелСвНП, "Фамилия"));
	Результат.Вставить("Имя", 		ИзвлечьДанное(УзелСвНП, "Имя"));
	Результат.Вставить("Отчество",	ИзвлечьДанное(УзелСвНП, "Отчество"));
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлДокумент);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьРеквизитыПолучателяИзВходящегоТребования(ТранспортноеСообщениеВходящегоТребования)
	
	// извлекаем вложения расшифрованного входящего сообщения
	Вложения = ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщениеВходящегоТребования.Ссылка, Истина);
	ВложенияДокумент = Вложения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ));
	Если ВложенияДокумент.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ВложениеДокумент = ВложенияДокумент[0];
	
	// считываем документ
	ВремФайлДокумент = ПолучитьИмяВременногоФайла();
	ВложениеДокумент.Данные.Получить().Записать(ВремФайлДокумент);
	
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(ВремФайлДокумент);
	
	КНДТребования = ИзвлечьДанное(ДеревоXML, "КНД");
	
	УзлыСвНП = ДеревоXML.Строки.НайтиСтроки(Новый Структура("Имя", "СвПолДок"), Истина);
	
	Если УзлыСвНП.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвНП = УзлыСвНП[0];
	
	Результат = Новый Структура();
	Результат.Вставить("НаимОрг", 	ИзвлечьДанное(УзелСвНП, "НаимОрг"));
	Результат.Вставить("ИННЮЛ", 	ИзвлечьДанное(УзелСвНП, "ИННЮЛ"));
	Результат.Вставить("КПП", 		ИзвлечьДанное(УзелСвНП, "КПП"));
	Результат.Вставить("ИННФЛ", 	ИзвлечьДанное(УзелСвНП, "ИННФЛ"));
	Результат.Вставить("Фамилия", 	ИзвлечьДанное(УзелСвНП, "Фамилия"));
	Результат.Вставить("Имя", 		ИзвлечьДанное(УзелСвНП, "Имя"));
	Результат.Вставить("Отчество",	ИзвлечьДанное(УзелСвНП, "Отчество"));
	
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайлДокумент);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура СоздатьОтветНаТребованиеДокументовСВыборомВидаОтвета(Требование, ВходящийКонтекст = Неопределено) Экспорт
	
	// Предлагаем меню с выбором из 2х вариантов
	СписокВыбора  = Новый СписокЗначений;
	СписокВыбора.Добавить(4, "Ответ на требование документов (информации)");
	СписокВыбора.Добавить(5, "Уведомление о невозможности ответить в срок");
	
	ВыбратьВидОтвета(СписокВыбора, Требование);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПисьмоОтветСВыборомВидаОтвета(Ссылка, ВходящийКонтекст = Неопределено) Экспорт
	
	// Предлагаем меню с выбором из 2х вариантов
	СписокВыбора  = Новый СписокЗначений;
	СписокВыбора.Добавить(6, "Письмо");
	СписокВыбора.Добавить(3, "Отправка истребуемых документов");
	
	ВыбратьВидОтвета(СписокВыбора, Ссылка);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПакетСДопДокуменами(ОснованиеПакета) Экспорт
	
	Организация = СведенияПоОтправляемымОбъектам(ОснованиеПакета).Организация;
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("Организация", Организация);
	
	Если ОснованиеПакета <> Неопределено Тогда
		
		Если НЕ ОснованиеПакетаВыбраноКорректно(ОснованиеПакета) Тогда
			Возврат;
		КонецЕсли;
		
		ПараметрыФормы.Вставить("ОснованиеПакета", ОснованиеПакета);
	КонецЕсли;
	
	ОткрытьФорму("Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ФормаОбъекта", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьОтветНаТребованиеДокументов(Требование) Экспорт
	
	ЭлементыОтбора = Новый Структура;
	ЭлементыОтбора.Вставить("ОснованиеСсылка", Требование);
	ПараметрыФормы = Новый Структура("ЗначенияЗаполнения", ЭлементыОтбора);
	ОткрытьФорму("Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ФормаОбъекта", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораВидаОтветаНаТребование(ВидОтвета, ДополнительныеПараметры) Экспорт
	
	Если ВидОтвета <> Неопределено Тогда
		
		Ссылка = ДополнительныеПараметры.Ссылка;
	
		Если ВидОтвета.Значение = 1 Тогда
			СоздатьПоясненияКДекларацииПоНДС(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 2 Тогда
			СоздатьПисьмоВОтветНаДокументРеализацииПолномочий(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 3 Тогда
			СоздатьПакетСДопДокуменами(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 4 Тогда
			СоздатьОтветНаТребованиеДокументов(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 5 Тогда
			ПодготвоитьУведомлениеОНевозможностиОтветитьВСрок(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 6 Тогда
			СоздатьПисьмоОтвет(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 7 Тогда
			ПодготовитьРеестрДокументов(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 8 Тогда
			ПодготовитьРеестрДокументовИмущество(Ссылка);
		ИначеЕсли ВидОтвета.Значение = 9 Тогда
			ПодготвоитьУведомлениеПоТребованию(Ссылка, "СогласиеСМотивированнымМнением");
		ИначеЕсли ВидОтвета.Значение = 10 Тогда
			ПодготвоитьУведомлениеПоТребованию(Ссылка, "РазногласиеСМотивированнымМнением");
		ИначеЕсли ВидОтвета.Значение = 11 Тогда
			ПодготвоитьУведомлениеПоТребованию(Ссылка, "ВзаимосогласительнаяПроцедура");
		ИначеЕсли ВидОтвета.Значение = 12 Тогда
			
			УведомлениеОСпецрежимахНалогообложенияКлиент.СоздатьПояснениеКИзвещениюОбИсчисленныхНалогах(
				ЭтотОбъект, 
				Ссылка);
			
		ИначеЕсли ВидОтвета.Значение = 13 Тогда
			ПодготвоитьУведомлениеПоТребованию(Ссылка, "НевозможностьПредоставленияИнформации");
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ЭтоРешениеПоЖалобе(Требование)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоРешениеПоЖалобе(Требование);

КонецФункции

&НаСервере
Функция ЭтоМотивированноеМнение(Требование)

	Возврат Требование.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.МотивированноеМнение;

КонецФункции

&НаСервере
Функция ЭтоИсчислениеНалога(Требование)

	Виды = Перечисления.ВидыНалоговыхДокументов;
	
	Возврат Требование.ВидДокумента = Виды.СообщениеОбИсчисленнойСуммеТранспортногоНалога
		ИЛИ Требование.ВидДокумента = Виды.СообщениеОбИсчисленнойСуммеТранспортногоИмущественногоЗемельногоНалогов
		ИЛИ Требование.ВидДокумента = Виды.СообщениеОбИсчисленнойСуммеЗемельногоНалога;

КонецФункции

&НаСервере
Функция ЭтоЗапросОПредставленииДокументовИнформации(Требование)
	
	Возврат Требование.ВидДокумента = Перечисления.ВидыНалоговыхДокументов.ЗапросОПредставленииДокументовИнформации;
	
КонецФункции

&НаКлиенте
Функция ОтветПакетом()

	Возврат НСтр("ru = 'Пакет с текстовым пояснением и доп. документами'");

КонецФункции

&НаКлиенте
Функция ОтветПисьмом()

	Возврат НСтр("ru = 'Письмо (не рекомендуется)'");

КонецФункции

&НаКлиенте
Процедура СоздатьПисьмоВОтветНаДокументРеализацииПолномочийСВыборомВарианта(Требование) Экспорт
	
	Пакет  = ОтветПакетом();
	Письмо = ОтветПисьмом();
	
	Если ТребуетсяОтправкаПакетаСДопДокументами(Требование) Тогда 
		
		СписокВыбора  = Новый СписокЗначений;
		СписокВыбора.Добавить(3, Пакет);
		СписокВыбора.Добавить(2, Письмо);
		
		ВыбратьВидОтвета(СписокВыбора, Требование);
		
	ИначеЕсли ЭтоРешениеПоЖалобе(Требование) Тогда

		ЗначенияЗаполнения = Новый Структура;
		ЗначенияЗаполнения.Вставить("ОснованиеСсылка", Требование);

		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);

		ОткрытьФорму("Справочник.ПерепискаСКонтролирующимиОрганами.ФормаОбъекта", ДополнительныеПараметры,,Новый УникальныйИдентификатор);
		
	ИначеЕсли ЭтоМотивированноеМнение(Требование) Тогда
		
		СписокВыбора  = Новый СписокЗначений;
		
		ЕстьУведомление = ДокументооборотСКОВызовСервера.ЕстьУведомление("СогласиеСМотивированнымМнением");
		Если ЕстьУведомление Тогда
			СписокВыбора.Добавить(9,  "Согласие с мотивированным мнением налогового органа");
		КонецЕсли;
		
		ЕстьУведомление = ДокументооборотСКОВызовСервера.ЕстьУведомление("РазногласиеСМотивированнымМнением");
		Если ЕстьУведомление Тогда
			СписокВыбора.Добавить(10, "Разногласия на мотивированное мнение налогового органа");
		КонецЕсли;
			
		ЕстьУведомление = ДокументооборотСКОВызовСервера.ЕстьУведомление("ВзаимосогласительнаяПроцедура");
		Если ЕстьУведомление Тогда
			СписокВыбора.Добавить(11, "Уведомление о согласии (несогласии) с мотивированным мнением налогового органа по результатам взаимосогласительной процедуры");
		КонецЕсли;
		
		СписокВыбора.Добавить(3, Пакет);
		СписокВыбора.Добавить(2, Письмо);
		
		ВыбратьВидОтвета(СписокВыбора, Требование);
		
	ИначеЕсли ЭтоИсчислениеНалога(Требование) Тогда
		
		СписокВыбора  = Новый СписокЗначений;
		СписокВыбора.Добавить(12, "Пояснение в связи с сообщением об исчисленных суммах налога");
		СписокВыбора.Добавить(3, Пакет);
		
		ВыбратьВидОтвета(СписокВыбора, Требование);
		
	ИначеЕсли ЭтоЗапросОПредставленииДокументовИнформации(Требование) Тогда
		
		СписокВыбора  = Новый СписокЗначений;
		СписокВыбора.Добавить(3, Пакет);
		СписокВыбора.Добавить(13, "Уведомление о невозможности представления документов (информации) в электронной форме");
		
		ВыбратьВидОтвета(СписокВыбора, Требование);
		
	Иначе
		
		СоздатьОтветНаТребованиеДокументов(Требование);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьВидОтвета(СписокВыбора, Ссылка) Экспорт
	
	ДополнительныеПараметры = Новый Структура("Ссылка", Ссылка);
	
	Оповещение = Новый ОписаниеОповещения(
		"ПослеВыбораВидаОтветаНаТребование", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	СписокВыбора.ПоказатьВыборЭлемента(Оповещение, НСтр("ru = 'Выберите вид ответа'"));
		
КонецПроцедуры

&НаКлиенте
Процедура ПодготвоитьУведомлениеОНевозможностиОтветитьВСрок(Требование) Экспорт
	
	ПодготвоитьУведомлениеПоТребованию(Требование, "НевозможностьПредоставленияДокументов");
	
КонецПроцедуры

&НаКлиенте
Процедура ПодготвоитьУведомлениеПоТребованию(Требование, ИмяВида) Экспорт
	
	ВидУведомления = ПредопределенноеЗначение("Перечисление.ВидыУведомленийОСпецрежимахНалогообложения." + ИмяВида);
	Организация = СведенияПоОтправляемымОбъектам(Требование).Организация;
	
	ПараметрыУведомления = Новый Структура;
	ПараметрыУведомления.Вставить("Организация", 	Организация);
	ПараметрыУведомления.Вставить("ВидУведомления", ВидУведомления);
	ПараметрыУведомления.Вставить("Данные", 		Требование);
	
	ОткрытьФорму("Документ.УведомлениеОСпецрежимахНалогообложения.ФормаОбъекта",  ПараметрыУведомления);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодготовитьРеестрДокументов(Требование) Экспорт
	
	Организация = СведенияПоОтправляемымОбъектам(Требование).Организация;
	
	ПараметрыУведомления = Новый Структура;
	ПараметрыУведомления.Вставить("Организация", 	Организация);
	ПараметрыУведомления.Вставить("ВидУведомления", ПредопределенноеЗначение("Перечисление.ВидыУведомленийОСпецрежимахНалогообложения.РеестрДокументовПодтверждающихЛьготы"));
	ПараметрыУведомления.Вставить("Данные", 		Требование);
	
	ОткрытьФорму("Документ.УведомлениеОСпецрежимахНалогообложения.ФормаОбъекта",  ПараметрыУведомления);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодготовитьРеестрДокументовИмущество(Требование) Экспорт
	
	Организация = СведенияПоОтправляемымОбъектам(Требование).Организация;
	
	ПараметрыУведомления = Новый Структура;
	ПараметрыУведомления.Вставить("Организация", 	Организация);
	ПараметрыУведомления.Вставить("ВидУведомления", ПредопределенноеЗначение("Перечисление.ВидыУведомленийОСпецрежимахНалогообложения.РеестрЛьготИмущество"));
	ПараметрыУведомления.Вставить("Данные", 		Требование);
	
	ОткрытьФорму("Документ.УведомлениеОСпецрежимахНалогообложения.ФормаОбъекта",  ПараметрыУведомления);
	
КонецПроцедуры

&НаСервере
Функция ТребуетсяОтправкаПакетаСДопДокументами(Ссылка) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТребуетсяОтправкаПакетаСДопДокументами(Ссылка);
	
КонецФункции

&НаКлиенте
Процедура СоздатьИсходящуюОписьПоОснованию(ОснованиеСсылка) Экспорт
	
	ЭлементыОтбора = Новый Структура;
	ЭлементыОтбора.Вставить("ОснованиеСсылка", ОснованиеСсылка);
	ПараметрыФормы = Новый Структура("ЗначенияЗаполнения", ЭлементыОтбора);
	ОткрытьФорму("Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.ФормаОбъекта", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьПоясненийКДекларацииНДССВыборомВидаПоясненияПослеВыбора(ВидОтвета, ДополнительныеПараметры) Экспорт
	
	Если ВидОтвета <> Неопределено Тогда
	
		Если ВидОтвета.Значение = 1 Тогда
			СоздатьПоясненияКДекларацииПоНДС(ДополнительныеПараметры.ДокументРеализацииПолномочий);
		ИначеЕсли ВидОтвета.Значение = 2 Тогда
			СоздатьПисьмоВОтветНаДокументРеализацииПолномочий(ДополнительныеПараметры.ДокументРеализацииПолномочий);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПакетно(
		ВыполняемоеОповещение,
		ТранспортноеСообщение,
		ТребующиеПодписания,
		Сертификат,
		МенеджерКриптографии = Неопределено)
		
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Сертификат);
	
	ПараметрыЦикла = НовыЙ Структура();
	ПараметрыЦикла.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ПараметрыЦикла.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	ПараметрыЦикла.Вставить("ТребующиеПодписания", ТребующиеПодписания);
	ПараметрыЦикла.Вставить("СертификатРуководителя", Сертификат);
	ПараметрыЦикла.Вставить("МенеджерКриптографии", МенеджерКриптографии);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПакетноПослеВыполнения", ЭтотОбъект, ПараметрыЦикла);
	
	МассивДокументов = Новый Массив;
	Для Счетчик = 0 По ТребующиеПодписания.ВГраница() Цикл
		ДанныеФайла = ТребующиеПодписания[Счетчик];
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ДанныеФайла.ИмяФайла);
		НовыйДокументПакетнойПодписи(
			МассивДокументов,
			ДанныеФайла.АдресВременногоХранилища,
			ОписаниеФайла.Имя,
			ОписаниеФайла.Расширение,
			Счетчик);
	КонецЦикла;

	ПакетнаяПодписьОблачнойПодписи(ОписаниеОповещения, МестоХраненияКлюча, МассивДокументов, Сертификат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаОтветаНаТребованиеПоясненийКДекларацииНДС_ДобавлениеПодписейКакПриложениеПакетноПослеВыполнения(
		РезультатВыполнения, 
		ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ТранспортноеСообщение = ВходящийКонтекст.ТранспортноеСообщение;
	ТребующиеПодписания = ВходящийКонтекст.ТребующиеПодписания;
	Результат = РезультатВыполнения.Выполнено;
	
	Если Результат Тогда
		
		Для Счетчик = 0 По РезультатВыполнения.Результат.ВГраница() Цикл
			Подпись = РезультатВыполнения.Результат[Счетчик];
			ДанныеФайла = ТребующиеПодписания[Счетчик];
			КороткоеИмяФайлаПодписи = ДанныеФайла.ИдФайлЭП;
			Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение, 
					СтруктураСсылочныхДанных.Перечисления_ТипыСодержимогоТранспортногоКонтейнера_Приложение, 
					Подпись, 
					КороткоеИмяФайлаПодписи, , , , , , , 
					СгенерироватьUUID(), 
					ПредопределенноеЗначение("Перечисление.ТипыСодержимогоФайлов.sgn")) Тогда
				
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка создания подписи для документа %1'."), ДанныеФайла.ИмяФайла);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				
				Результат = Ложь;
				Прервать;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение,	Результат);
	
КонецПроцедуры

#КонецОбласти

&НаСервереБезКонтекста
Функция НовыйЧтениеТекстаНаСервере(ИмяФайла, Кодировка = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НовыйЧтениеТекстаНаСервере(ИмяФайла, Кодировка);
	
КонецФункции

#Область ОбменСФТС

////////////////////////////////////////////////////////////////////////////////
// ДОКУМЕНТООБОРОТ С ФТС

// ОтправкаСсылка ссылается на загруженный объект
&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФТС(Форма, ОтправкаСсылка, ВыполняемоеОповещение, ПараметрыОбновленияОтправки = Неопределено, ЭтоВызовИзСписка = Ложь) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Форма",                Форма);
	ДополнительныеПараметры.Вставить("ОтправкаСсылка",       ОтправкаСсылка);
	ДополнительныеПараметры.Вставить("ЗначениеCookie",       ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.ЗначениеCookie, ""));
	ДополнительныеПараметры.Вставить("Логин",                ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.Логин, ""));
	ДополнительныеПараметры.Вставить("Пароль",               ?(ПараметрыОбновленияОтправки <> Неопределено, ПараметрыОбновленияОтправки.Пароль, ""));
	ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",     ЭтоВызовИзСписка);
	Оповещение = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФТСПослеПолученияСертификатовИПараметровОбмена", ЭтотОбъект, ДополнительныеПараметры);
	
	СертификатыИПараметрыОбменаОрганизацииДляФТС(
		Оповещение,
		Неопределено,
		Истина,
		ОтправкаСсылка,
		Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФТСПослеПолученияСертификатовИПараметровОбмена(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	Форма 					= ДополнительныеПараметры.Форма;
	ОтправкаСсылка 			= ДополнительныеПараметры.ОтправкаСсылка;
	ЗначениеCookie 			= ДополнительныеПараметры.ЗначениеCookie;
	Логин 					= ДополнительныеПараметры.Логин;
	Пароль 					= ДополнительныеПараметры.Пароль;
	ЭтоВызовИзСписка 		= ДополнительныеПараметры.ЭтоВызовИзСписка;
	
	Крипт = Неопределено;
	ОрганизацияСсылка = СертификатыИПараметрыОбмена.ОрганизацияИзОтправки;
	
	РеквизитыУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	ИдентификаторДокументооборота = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	ТелефонМобильныйДляАвторизации = РеквизитыУчетнойЗаписи.ТелефонМобильныйДляАвторизации;
	
	УчетнаяЗапись = Новый Структура("ТелефонМобильныйДляАвторизации, ИдентификаторДокументооборота", ТелефонМобильныйДляАвторизации, ИдентификаторДокументооборота);
	
	ПараметрыФормированияПакета = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	ЭтоРегламентированныйОтчет = СертификатыИПараметрыОбмена.ЭтоРегламентированныйОтчет;
	
	Если НЕ ПараметрыФормированияПакета.ИспользоватьОбмен Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Использование документооборота с Федеральной таможенной службой не включено для организации!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	ФормаДлительнаяОперация = Неопределено;
	
	ПараметрЛогин = ?(ЗначениеЗаполнено(Логин), Логин, ПараметрыФормированияПакета.Логин);
	ПараметрПароль = ?(ЗначениеЗаполнено(Пароль), Пароль, ПараметрыФормированияПакета.Пароль);
	
	АвторизацияПроизведена = ЗначениеЗаполнено(ЗначениеCookie);
	ИнформацияОСервереФТС = СерверПриемаОтчетностиФТС();
	
	ПараметрыОтправки = Новый Структура;
	ПараметрыОтправки.Вставить("Организация", 								ОрганизацияСсылка);
	ПараметрыОтправки.Вставить("АдресСервера", 								ИнформацияОСервереФТС.АдресСервера);
	ПараметрыОтправки.Вставить("ВерсияФормированияВыгрузки", 				ИнформацияОСервереФТС.ВерсияФормированияВыгрузки);
	ПараметрыОтправки.Вставить("ВерсияОтправкиПортала", 					ИнформацияОСервереФТС.ВерсияОтправкиПортала);
	ПараметрыОтправки.Вставить("ПоляДляЗаписиОтправки", 					Неопределено);
	ПараметрыОтправки.Вставить("ИдентификаторПредыдущейОтправкиНаСервере", 	"");
	
	ПараметрыСоединения = Новый Структура;
	ПараметрыСоединения.Вставить("Логин", 										ПараметрЛогин);
	ПараметрыСоединения.Вставить("Пароль", 										ПараметрПароль);
	ПараметрыСоединения.Вставить("СохранитьПароль", 							ЗначениеЗаполнено(ПараметрыФормированияПакета.Пароль));
	ПараметрыСоединения.Вставить("СохраненныйЛогин", 							ПараметрыФормированияПакета.Логин);
	ПараметрыСоединения.Вставить("СохраненныйПароль", 							ПараметрыФормированияПакета.Пароль);
	ПараметрыСоединения.Вставить("СоединениеHTTP", 								Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie", 								"");
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиНаСервере", 				"");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером", 						"");
	ПараметрыСоединения.Вставить("ОписаниеОшибокПроверки", 						"");
	ПараметрыСоединения.Вставить("ПовторятьСоединение", 						Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля", 						Истина);
	ПараметрыСоединения.Вставить("ОрганизацияПодтверждена", 					Неопределено);
	ПараметрыСоединения.Вставить("ОтправленЗапросНаПодтверждениеОрганизации", 	Ложь);
	ПараметрыСоединения.Вставить("СообщенияПользователю", 						Новый Массив);
	ПараметрыСоединения.Вставить("ИдентификаторВыполненияОтправки", 			"");
	ПараметрыСоединения.Вставить("ПодписываемоеСодержимоеВыгрузки64", 			"");
	ПараметрыСоединения.Вставить("ПодписываемыйЗапрос64", 						"");
	ПараметрыСоединения.Вставить("ТекущийАдресСервера", 						ИнформацияОСервереФТС.АдресСервера);
	ПараметрыСоединения.Вставить("ВерсияПортала", 								ИнформацияОСервереФТС.ВерсияПортала);
	ПараметрыСоединения.Вставить("ТекстОтветаАвторизации", 						"");
	
	ПараметрыСоединения.ЗначениеCookie = "";
	
	ПерваяПопыткаАвторизации = Истина;
	ОперацияУспешна = Ложь;
	ПараметрыСоединения.ПовторятьЗапросПароля = Истина;
	
	ОбновитьРезультатКонкретнойОтправкиФТСВводПароля(
		ВыполняемоеОповещение, 
		ПараметрыСоединения, 
		ПараметрыОтправки, 
		ФормаДлительнаяОперация,
		АвторизацияПроизведена,
		ОтправкаСсылка, 
		ПерваяПопыткаАвторизации,
		ЭтоВызовИзСписка);
	
КонецПроцедуры

// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (Цикл)
&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФТСВводПароля(
	ВыполняемоеОповещение,
	ПараметрыСоединения,
	ПараметрыОтправки,
	ФормаДлительнаяОперация,
	АвторизацияПроизведена,
	ОтправкаСсылка,
	ПерваяПопыткаАвторизации,
	ЭтоВызовИзСписка)
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.Логин) 
		ИЛИ НЕ ЗначениеЗаполнено(ПараметрыСоединения.Пароль) 
		ИЛИ НЕ ПерваяПопыткаАвторизации Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",    ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",      ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",        ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",  ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("АвторизацияПроизведена",   АвторизацияПроизведена);
		ДополнительныеПараметры.Вставить("ОтправкаСсылка",           ОтправкаСсылка);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации", ПерваяПопыткаАвторизации);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",         ЭтоВызовИзСписка);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьРезультатКонкретнойОтправкиФТСВводПароляЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		ВызватьФормуВводаПароляПорталаФТС(
			"https://" + ПараметрыОтправки.АдресСервера + "/",
			ПараметрыСоединения.Логин,
			ПараметрыСоединения.Пароль,
			ПараметрыСоединения.СохранитьПароль,
			ОписаниеОповещения);
		
		Возврат;
	КонецЕсли;
	
	// при успешном обращении на сервер ФТС отправка обновится в ИБ
	ОперацияУспешна = ОбновитьРезультатКонкретнойОтправкиФТСНаСервере(
		ОтправкаСсылка,
		Истина,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ЭтоВызовИзСписка);
		
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ПерваяПопыткаАвторизации = Ложь;
	
	// рекурсивный вызов - замена цикла
	Если НЕ ОперацияУспешна И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
		
		ОбновитьРезультатКонкретнойОтправкиФТСВводПароля(
			ВыполняемоеОповещение,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ФормаДлительнаяОперация,
			АвторизацияПроизведена,
			ОтправкаСсылка,
			ПерваяПопыткаАвторизации,
			ЭтоВызовИзСписка);
		
	Иначе
		
		ОбновитьРезультатКонкретнойОтправкиФТСПослеОтправки(
			ВыполняемоеОповещение,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ФормаДлительнаяОперация,
			АвторизацияПроизведена,
			ОтправкаСсылка,
			ПерваяПопыткаАвторизации,
			ЭтоВызовИзСписка);
		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФТСВводПароляЗавершение(РезультатВводаПароля, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение    = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыСоединения      = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки        = ДополнительныеПараметры.ПараметрыОтправки;
	ФормаДлительнаяОперация  = ДополнительныеПараметры.ФормаДлительнаяОперация;
	АвторизацияПроизведена   = ДополнительныеПараметры.АвторизацияПроизведена;
	ОтправкаСсылка           = ДополнительныеПараметры.ОтправкаСсылка;
	ПерваяПопыткаАвторизации = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	ЭтоВызовИзСписка         = ДополнительныеПараметры.ЭтоВызовИзСписка;
	
	Если РезультатВводаПароля = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	Иначе
		
		ПараметрыСоединения.Логин           = РезультатВводаПароля.Логин;
		ПараметрыСоединения.Пароль          = РезультатВводаПароля.Пароль;
		ПараметрыСоединения.СохранитьПароль = РезультатВводаПароля.СохранитьПароль;
		
		// при успешном обращении на сервер ФТС отправка обновится в ИБ
		ОперацияУспешна = ОбновитьРезультатКонкретнойОтправкиФТСНаСервере(
			ОтправкаСсылка,
			Истина,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ЭтоВызовИзСписка);
		
		ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
		
		ПерваяПопыткаАвторизации = Ложь;
		
		// рекурсивный вызов - замена цикла
		Если НЕ ОперацияУспешна И ПараметрыСоединения.ПовторятьЗапросПароля Тогда
			
			ОбновитьРезультатКонкретнойОтправкиФТСВводПароля(
				ВыполняемоеОповещение,
				ПараметрыСоединения,
				ПараметрыОтправки,
				ФормаДлительнаяОперация,
				АвторизацияПроизведена,
				ОтправкаСсылка,
				ПерваяПопыткаАвторизации,
				ЭтоВызовИзСписка);
			
		Иначе
			
			ОбновитьРезультатКонкретнойОтправкиФТСПослеОтправки(
				ВыполняемоеОповещение,
				ПараметрыСоединения,
				ПараметрыОтправки,
				ФормаДлительнаяОперация,
				АвторизацияПроизведена,
				ОтправкаСсылка,
				ПерваяПопыткаАвторизации,
				ЭтоВызовИзСписка);
				
		КонецЕсли;
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФТСПослеОтправки(
	ВыполняемоеОповещение,
	ПараметрыСоединения,
	ПараметрыОтправки,
	ФормаДлительнаяОперация,
	АвторизацияПроизведена,
	ОтправкаСсылка,
	ПерваяПопыткаАвторизации,
	ЭтоВызовИзСписка)
	
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// возможно, проблемы с доступом в интернет
		Если НЕ АвторизацияПроизведена Тогда
			ПараметрыСоединения.ЗначениеCookie = "";
		КонецЕсли;
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",    ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",      ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",        ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",  ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("АвторизацияПроизведена",   АвторизацияПроизведена);
		ДополнительныеПараметры.Вставить("ОтправкаСсылка",           ОтправкаСсылка);
		ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации", ПерваяПопыткаАвторизации);
		ДополнительныеПараметры.Вставить("ЭтоВызовИзСписка",         ЭтоВызовИзСписка);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОбновитьРезультатКонкретнойОтправкиФТСПослеОтправкиЗавершение", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу Федеральной таможенной службы'"), 
			ОписаниеОповещения);
			
		Возврат;
		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьРезультатКонкретнойОтправкиФТСПослеОтправкиЗавершение(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение                   = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПараметрыСоединения                     = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                       = ДополнительныеПараметры.ПараметрыОтправки;
	ФормаДлительнаяОперация                 = ДополнительныеПараметры.ФормаДлительнаяОперация;
	АвторизацияПроизведена                  = ДополнительныеПараметры.АвторизацияПроизведена;
	ОтправкаСсылка                          = ДополнительныеПараметры.ОтправкаСсылка;
	ПерваяПопыткаАвторизации                = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	ЭтоВызовИзСписка                           = ДополнительныеПараметры.ЭтоВызовИзСписка;
	ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// рекурсивный вызов - замена цикла
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ОбновитьРезультатКонкретнойОтправкиФТСВводПароля(
			ВыполняемоеОповещение,
			ПараметрыСоединения,
			ПараметрыОтправки,
			ФормаДлительнаяОперация,
			АвторизацияПроизведена,
			ОтправкаСсылка,
			ПерваяПопыткаАвторизации,
			ЭтоВызовИзСписка);
		
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);	
		
	КонецЕсли;	
	
КонецПроцедуры
// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаКлиенте
Процедура ОткрытьПротоколОтправкиФТС(Форма) Экспорт
	
	ОтчетСсылка = ДокументооборотСФТСКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	
	Если ОтчетСсылка = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФТС(ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляФТС(
		ВыполняемоеОповещение,
		ОрганизацияСсылка,
		ПолучитьОсновныеСвойстваПоследнейОтправки,
		ОтправкаСсылка = Неопределено,
		ОтчетСсылка = Неопределено,
		ОпределитьНастроенностьОбменаВУниверсальномФормате = Ложь)
	
	РезультатВыполнения = Новый Структура("ПараметрыФормированияПакета, ОрганизацияИзОтправки, СвойстваОтправки, Крипт, НастроенОбменВУниверсальномФормате, ПравоИзмененияУчетнойЗаписи, ЭтоРегламентированныйОтчет", Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено);
	
	СвойстваОбмена = СвойстваОбменаОрганизацииДляФТСНаСервере(ОрганизацияСсылка, ОпределитьНастроенностьОбменаВУниверсальномФормате, ПолучитьОсновныеСвойстваПоследнейОтправки, ОтправкаСсылка, ОтчетСсылка);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(СвойстваОбмена);
	
	РезультатВыполнения.ОрганизацияИзОтправки = СвойстваОбмена.ОрганизацияИзОтправки;
	
	РезультатВыполнения.СвойстваОтправки = СвойстваОбмена.СвойстваОтправки;
	
	РезультатВыполнения.НастроенОбменВУниверсальномФормате = СвойстваОбмена.НастроенОбменВУниверсальномФормате;
	РезультатВыполнения.ПравоИзмененияУчетнойЗаписи = СвойстваОбмена.ПравоИзмененияУчетнойЗаписи;
	РезультатВыполнения.ЭтоРегламентированныйОтчет = СвойстваОбмена.ЭтоРегламентированныйОтчет;
	
	РезультатВыполнения.ПараметрыФормированияПакета = Новый Структура;
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("ИспользоватьОбмен", 			Ложь);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("СертификатАбонента", 			Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("МЧД", 						Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("ИННДоверителя", 				"");
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("Логин", 						Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("Пароль", 						Неопределено);
	РезультатВыполнения.ПараметрыФормированияПакета.Вставить("ИспользоватьАвтонастройку", 	Ложь);
	Если НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьОбмен = СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен;
	РезультатВыполнения.ПараметрыФормированияПакета.МЧД = СвойстваОбмена.НастройкиОбмена.МЧД;
	РезультатВыполнения.ПараметрыФормированияПакета.ИННДоверителя = СвойстваОбмена.НастройкиОбмена.ИННДоверителя;
	РезультатВыполнения.ПараметрыФормированияПакета.Логин = СвойстваОбмена.НастройкиОбмена.Логин;
	РезультатВыполнения.ПараметрыФормированияПакета.Пароль = СвойстваОбмена.НастройкиОбмена.Пароль;
	РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьАвтонастройку = СвойстваОбмена.НастройкиОбмена.ИспользоватьАвтонастройку;
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СвойстваСертификата = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток);
		Иначе
			СвойстваСертификата = ПолучитьСвойстваОблачногоСертификата(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток, "MY");
		КонецЕсли;
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификата);
		РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = СвойстваСертификата;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, СвойстваОбмена, РезультатВыполнения", ВыполняемоеОповещение, СвойстваОбмена, РезультатВыполнения);
	Оповещение = Новый ОписаниеОповещения("СертификатыИПараметрыОбменаОрганизацииДляФТСПослеПолученияСвойствСертификатаАбонентаЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	Если ЗначениеЗаполнено(СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток) Тогда
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(Оповещение, СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток, "MY", , Ложь);
	Иначе
		РезультатПоиска = Новый Структура("Выполнено, МенеджерКриптографии, СертификатНайден", Истина, Неопределено, Ложь);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатПоиска);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляФТСПослеПолученияСвойствСертификатаАбонентаЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СвойстваОбмена = ДополнительныеПараметры.СвойстваОбмена;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ?(Результат.Выполнено И Результат.СертификатНайден, Результат.СвойстваСертификата, Неопределено);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Функция СертификатыГодятсяДляПодписанияОтчетностиФТС(Сертификаты)
	
	ТекДата = ТекущаяДатаНаСервере();
	ПризнакОшибки = Ложь;
	
	Для Каждого Сертификат Из Сертификаты Цикл
		
		Серт	= Сертификат.Значение;
		Предст	= Сертификат.Представление;
		Если Серт = Неопределено Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтрШаблон(НСтр("ru = '%1: сертификат не выбран в настройках сдачи отчетности в Федеральную таможенную службу!'"), Предст));
			Продолжить;
		КонецЕсли;
		
		Если ТекДата < Серт.ДействителенС Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтрШаблон(НСтр("ru = '%1: период действия сертификата еще не наступил (%2)!'"), Предст, Строка(Серт.ДействителенС)));
		КонецЕсли;
		
		Если ТекДата > Серт.ДействителенПо Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(СтрШаблон(НСтр("ru = '%1: период действия сертификата истек (%2)!'"), Предст, Строка(Серт.ДействителенПо)));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат (НЕ ПризнакОшибки);
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьДляОтправкиВФТС(
		Форма,
		ВыполняемоеОповещение,
		ТекстФайлаВыгрузки,
		ИмяФайлаВыгрузки,
		СертификатыИПараметрыОбмена = Неопределено,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь,
		Знач ОтчетСсылка = Неопределено,
		Знач ОрганизацияСсылка = Неопределено)
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОтчетСсылка = ДокументооборотСФТСКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	Если ОтчетСсылка = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияСсылка = ДокументооборотСФТСКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
	КонецЕсли;
	
	РеквизитыУчетнойЗаписи              = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	УчетнаяЗапись                       = РеквизитыУчетнойЗаписи.Ссылка;
	Идентификатор                       = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",               ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Форма",                               Форма);
	ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
	ДополнительныеПараметры.Вставить("Идентификатор",                       Идентификатор);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность",        ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки",        			ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки",        			ИмяФайлаВыгрузки);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьДляОтправкиВФТСПослеПолученияСертификатовИПараметровОбменаЗавершение", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	СертификатыИПараметрыОбменаОрганизацииДляФТС(
		Оповещение,
		ОрганизацияСсылка,
		Ложь,
		Неопределено,
		ОтчетСсылка,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьДляОтправкиВФТСПослеПолученияСертификатовИПараметровОбменаЗавершение(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение               = ДополнительныеПараметры.ВыполняемоеОповещение;
	Форма                               = ДополнительныеПараметры.Форма;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	Идентификатор                       = ДополнительныеПараметры.Идентификатор;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТекстФайлаВыгрузки                  = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки                    = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	
	// запрашиваем параметры формирования пакета и параметры обмена при вызове из отправки
	Если НЕ СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИспользоватьОбмен Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Использование документооборота с Федеральной таможенной службой не включено для организации!'"));
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	СертификатАбонента = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента;
	Если СертификатАбонента <> Неопределено И НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		СертификатАбонента.Вставить("КлючевойКонтейнер", Идентификатор);
	КонецЕсли;
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СертификатАбонента);
	
	//проверяем сертификаты
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатАбонента, НСтр("ru = 'Сертификат абонента'"));
	
	Если НЕ СертификатыГодятсяДляПодписанияОтчетностиФТС(СертификатыОбмена) Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	// формируем файл выгрузки

	КодировкаФайла = Неопределено;
	ТекстФайлаВыгрузки = "";
	ИмяФайлаВыгрузки = "";
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Если ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет")
		ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено") Тогда
		
		РезультатВыгрузкиИзЗаявленияОВвозеТоваров = ПопытатьсяВыгрузитьСтатформуИзЗаявленияОВвозеТоваров(
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,
			ОтчетСсылка);

		Если РезультатВыгрузкиИзЗаявленияОВвозеТоваров = Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		
		Если НЕ РезультатВыгрузкиИзЗаявленияОВвозеТоваров Тогда
			Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента", , Форма);
			Иначе
				ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
			КонецЕсли;
			
			Если НЕ ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(ОтчетСсылка, Ложь, Ложь, ТекстФайлаВыгрузки, ИмяФайлаВыгрузки) Тогда
				ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
	Иначе // электронное представление
		Если ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
			Возврат;
		КонецЕсли;
		
		ОпределеннаяКодировка = Неопределено;
		
		ИзвлечьЭлектронноеПредставлениеИзХранилищаИПерекодироватьНаСервере(
			ОтчетСсылка,
			ТекстФайлаВыгрузки,
			ИмяФайлаВыгрузки,
			ОпределеннаяКодировка,
			"utf-8");
		
		Если ОпределеннаяКодировка <> Неопределено Тогда
			//КодировкаФайла = ОпределеннаяКодировка;
		КонецЕсли;
	КонецЕсли;
	
	ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, Символы.ВК + Символы.ПС, " "); // обход ошибки платформы при канонизации XML
	ТекстФайлаВыгрузки = СтрЗаменить(ТекстФайлаВыгрузки, Символы.ПС, " "); // обход ошибки платформы при канонизации XML
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда

		Адрес = ЗагрузитьДанныеНаСервер(ТекстФайлаВыгрузки, Форма.УникальныйИдентификатор,, Истина, Ложь,, Истина);
		
		СтруктураВозвратаЭтоЭлектроннаяПодпись = Новый Структура("РезультатВыгрузки, ИмяФайлаВыгрузки", Адрес, ИмяФайлаВыгрузки);
			//ЗакодироватьВBase64ЗначениеВременногоХранилища(Адрес);
			
		Если СтруктураВозвратаЭтоЭлектроннаяПодпись <> Неопределено Тогда
			СтруктураВозвратаЭтоЭлектроннаяПодпись.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
		КонецЕсли;
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, СтруктураВозвратаЭтоЭлектроннаяПодпись);
		Возврат;
		
	КонецЕсли;
	
	// получим имя временного файла
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ИмяФайлаВыгрузки, ТекстФайлаВыгрузки, СертификатыИПараметрыОбмена", ВыполняемоеОповещение, ИмяФайлаВыгрузки, ТекстФайлаВыгрузки, СертификатыИПараметрыОбмена);
	ДополнительныеПараметры.Вставить("КодировкаФайла", КодировкаФайла);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьДляОтправкиВФТСПослеПолученияВременногоКаталога", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.КаталогВременныхФайловНаКлиенте(Оповещение);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПопытатьсяПолучитьЗаявлениеОВвозеТоваровИзСтатформы(ОтчетСсылка, ДокументЗаявлениеОВвозеТоваров = Неопределено, ИмяДокументаЗаявлениеОВвозеТоваров = Неопределено) Экспорт
	
	РеквизитыОтчета = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ВыбраннаяФорма, ДанныеОтчета");
	Если РеквизитыОтчета.ВыбраннаяФорма <> "ФормаОтчетаПоЗаявлениюОВвозеТоваров" Тогда
		ДокументЗаявлениеОВвозеТоваров = Неопределено;
		ИмяДокументаЗаявлениеОВвозеТоваров = Неопределено;
		Возврат Ложь;
	КонецЕсли;
	
	ИмяДокументаЗаявлениеОВвозеТоваров = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСерверПереопределяемый.ИмяОбъектаМетаданных("ЗаявлениеОВвозеТоваров");
	Если ИмяДокументаЗаявлениеОВвозеТоваров = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Заявление о ввозе товаров не поддерживается в этой конфигурации, выгрузка статистической формы учета перемещения товаров по заявлению невозможна!'"));
		ДокументЗаявлениеОВвозеТоваров = Неопределено;
		Возврат Неопределено;
	КонецЕсли;
	
	ДокументЗаявлениеОВвозеТоваров = РеквизитыОтчета.ДанныеОтчета.Получить().ЗаявлениеОВвозеТоваров;
	Если НЕ ЗначениеЗаполнено(ДокументЗаявлениеОВвозеТоваров) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Заявление о ввозе товаров не выбрано, выгрузка статистической формы учета перемещения товаров по заявлению невозможна!'"));
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПопытатьсяВыгрузитьСтатформуИзЗаявленияОВвозеТоваров(
		ТекстФайлаВыгрузки,
		ИмяФайлаВыгрузки,
		ОтчетСсылка = Неопределено,
		ВернутьАдрес = Ложь,
		ТолькоПроверка = Ложь,
		ДокументЗаявлениеОВвозеТоваров = Неопределено,
		ИмяДокументаЗаявлениеОВвозеТоваров = Неопределено) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ДокументЗаявлениеОВвозеТоваров) Тогда
		
		РезультатПолученияЗаявленияОВвозеТоваров = ПопытатьсяПолучитьЗаявлениеОВвозеТоваровИзСтатформы(
			ОтчетСсылка,
			ДокументЗаявлениеОВвозеТоваров,
			ИмяДокументаЗаявлениеОВвозеТоваров);
		
		Если РезультатПолученияЗаявленияОВвозеТоваров <> Истина Тогда
			ТекстФайлаВыгрузки = Неопределено;
			ИмяФайлаВыгрузки = Неопределено;
			Возврат РезультатПолученияЗаявленияОВвозеТоваров;
		КонецЕсли;
	КонецЕсли;
	
	РезультатВыгрузки = Документы[ИмяДокументаЗаявлениеОВвозеТоваров].ВыгрузитьУчетПеремещенияТоваров(Новый УникальныйИдентификатор(), ДокументЗаявлениеОВвозеТоваров, ТолькоПроверка);
	Если РезультатВыгрузки = Неопределено ИЛИ РезультатВыгрузки.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если НЕ ТолькоПроверка Тогда
		Если НЕ ВернутьАдрес Тогда
			ИмяВременногоФайлаВыгрузки = ПолучитьИмяВременногоФайла();
			ДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(РезультатВыгрузки[0].АдресФайлаВыгрузки);
			ДанныеФайлаВыгрузки.Записать(ИмяВременногоФайлаВыгрузки);
			ОбъектЧтениеТекста = Новый ЧтениеТекста(ИмяВременногоФайлаВыгрузки, "utf-8");
			ТекстФайлаВыгрузки = ОбъектЧтениеТекста.Прочитать();
			ОбъектЧтениеТекста.Закрыть();
			
		Иначе
			ТекстФайлаВыгрузки = РезультатВыгрузки[0].АдресФайлаВыгрузки;
		КонецЕсли;
		
	Иначе
		ТекстФайлаВыгрузки = Неопределено;
	КонецЕсли;
	
	ИмяФайлаВыгрузки = РезультатВыгрузки[0].ИмяФайлаВыгрузки;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьДляОтправкиВФТСПослеПолученияВременногоКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ТекстФайлаВыгрузки = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВременныйКаталог = Результат.ИмяКаталога;
	
	ПолноеИмяФайлаВыгрузки = ВременныйКаталог + ИмяФайлаВыгрузки;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПолноеИмяФайлаВыгрузки, СертификатыИПараметрыОбмена", ВыполняемоеОповещение, ПолноеИмяФайлаВыгрузки, СертификатыИПараметрыОбмена);
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьДляОтправкиВФТСПослеЗаписиФайлаВыгрузки", ЭтотОбъект, ДополнительныеПараметры);
	ОперацииСФайламиЭДКОКлиент.ТекстВФайл(Оповещение, ТекстФайлаВыгрузки, ПолноеИмяФайлаВыгрузки);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьДляОтправкиВФТСПослеЗаписиФайлаВыгрузки(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	ПолноеИмяФайлаВыгрузки = ДополнительныеПараметры.ПолноеИмяФайлаВыгрузки;
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВозвращаемаяСтруктура = Новый Структура;
	ВозвращаемаяСтруктура.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
	ВозвращаемаяСтруктура.Вставить("РезультатВыгрузки", ПолноеИмяФайлаВыгрузки);
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ВозвращаемаяСтруктура);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
		
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС(
		ВходящийКонтекст.Форма,
		ВходящийКонтекст.ЭтоОтправкаИзФормыОтчетность,
		ВходящийКонтекст.ОтчетСсылка,
		ВходящийКонтекст.ОрганизацияОтчета);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС(
		Форма, 
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь, 
		Знач ОтчетСсылка = Неопределено, 
		Знач ОрганизацияОтчета = Неопределено) Экспорт
	
	Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
		ОрганизацияОтчета 	= ДокументооборотСФТСКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
		ОтчетСсылка 		= ДокументооборотСФТСКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	КонецЕсли;
	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Форма", 						 Форма);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 				 ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 			 ОрганизацияОтчета);
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	
	Если ЭтоРегламентированныйОтчет Тогда
		
		// отправляем только из записанной формы
		Если Форма.Модифицированность Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС_ПослеСохранения", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
				
			Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
			Возврат;
		КонецЕсли;
		
	Иначе // электронное представление
		
		Если Форма.Модифицированность Тогда
			ПоказатьПредупреждение(,НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
			Возврат;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено ИЛИ ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС_ПослеСохранения(Истина, ДополнительныеПараметры);
	
КонецПроцедуры	
	
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС_ПослеСохранения(Результат, ДополнительныеПараметры) Экспорт
	
	Форма                               = ДополнительныеПараметры.Форма;
	ЭтоОтправкаИзФормыОтчетность        = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета                   = ДополнительныеПараметры.ОрганизацияОтчета;
	
	Если НЕ ЗначениеЗаполнено(ОтчетСсылка) Тогда
		ОтчетСсылка = ДокументооборотСФСРАРКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
		ДополнительныеПараметры.ОтчетСсылка = ОтчетСсылка;
	КонецЕсли;
	
	СвойстваОбмена = СвойстваОбменаОрганизацииДляФТСНаСервере(
		ОрганизацияОтчета, 
		Истина, 
		Ложь, 
		,
		ОтчетСсылка);
	
	Если НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		Если НЕ СвойстваОбмена.НастроенОбменВУниверсальномФормате Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ДополнительныеПараметры);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ОтчетСсылка, ОписаниеОповещения);
			
		Иначе
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если СвойстваОбмена.НастроенОбменВУниверсальномФормате 
		И НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ОтчетСсылка) Тогда
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ОтчетСсылка);//+
		Возврат;
	КонецЕсли;
	
	МестоХраненияКлюча  = КонтекстМоделиХраненияКлюча(СвойстваОбмена);
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	Если СвойстваОбмена.НастроенОбменВУниверсальномФормате 
		И СвойстваОбмена.НастройкиОбмена.ИспользоватьАвтонастройку 
		И СвойстваОбмена.ПравоИзмененияУчетнойЗаписи Тогда

		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет", 				ТипЗнчСсылкаНаОтчет);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСАвтонастройкаУчетнойЗаписиЗавершение", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
		
		ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ОтчетСсылка,ОписаниеОповещения);
		
		Возврат;
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеАвтонастройкиУчетнойЗаписи(
		Форма, 
		ОтчетСсылка, 
		МестоХраненияКлюча, 
		ТипЗнчСсылкаНаОтчет, 
		ОрганизацияОтчета, 
		ЭтоОтправкаИзФормыОтчетность);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСАвтонастройкаУчетнойЗаписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ТипЗнчСсылкаНаОтчет = ДополнительныеПараметры.ТипЗнчСсылкаНаОтчет;
	ОрганизацияОтчета = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеАвтонастройкиУчетнойЗаписи(Форма, ОтчетСсылка, МестоХраненияКлюча, ТипЗнчСсылкаНаОтчет, ОрганизацияОтчета, ЭтоОтправкаИзФормыОтчетность);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеАвтонастройкиУчетнойЗаписи(Форма, ОтчетСсылка, МестоХраненияКлюча, ТипЗнчСсылкаНаОтчет, 
	ОрганизацияОтчета, Знач ЭтоОтправкаИзФормыОтчетность = Ложь)
	
	ЭтоРегламентированныйОтчет = (ТипЗнчСсылкаНаОтчет = Тип("ДокументСсылка.РегламентированныйОтчет") ИЛИ ТипЗнчСсылкаНаОтчет = Тип("Неопределено"));
	
	Если ЭтоРегламентированныйОтчет Тогда
		
		// отправляем только из записанной формы
		Если Форма.Модифицированность Тогда
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("Форма", Форма);
			ДополнительныеПараметры.Вставить("ОтчетСсылка", ОтчетСсылка);
			ДополнительныеПараметры.Вставить("ОрганизацияОтчета", ОрганизацияОтчета);
			ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", ЭтоОтправкаИзФормыОтчетность);
			КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			
			ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеАвтонастройкиУчетнойЗаписиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			Форма.СохранитьНаКлиенте(, ОписаниеОповещения);
			Возврат;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
		Если ЭтоОтправкаИзФормыОтчетность Тогда
			
			ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ДатаНачала, ДатаОкончания, Вид");
		
			ПоляДляЗаписиОтправки = Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
				НачалоДня(ЗначенияРеквизитов.ДатаНачала),
				КонецДня(ЗначенияРеквизитов.ДатаОкончания),
				ЗначенияРеквизитов.Вид);
				
		Иначе
		
			ПоляДляЗаписиОтправки = Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
				Форма.СтруктураРеквизитовФормы.мДатаНачалаПериодаОтчета,
				Форма.СтруктураРеквизитовФормы.мДатаКонцаПериодаОтчета,
				Форма.СтруктураРеквизитовФормы.ВидДокумента * Форма.НомерКорректировки);
				
		КонецЕсли;
		
	Иначе // электронное представление
		
		Если Форма.Модифицированность Тогда
			ПоказатьПредупреждение(,НСтр("ru = 'Необходимо записать отчет перед отправкой.'"));
			Возврат;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено ИЛИ ОтчетСсылка = ПредопределенноеЗначение("Справочник.ЭлектронныеПредставленияРегламентированныхОтчетов.ПустаяСсылка") Тогда
			Возврат;
		КонецЕсли;
		
		ПоляДляЗаписиОтправки = ПоляДляЗаписиОтправкиВФТСНаСервере(ОтчетСсылка);
		
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения(
		Форма, 
		ОтчетСсылка, 
		МестоХраненияКлюча, 
		ПоляДляЗаписиОтправки, 
		ОрганизацияОтчета, 
		ЭтоОтправкаИзФормыОтчетность);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеАвтонастройкиУчетнойЗаписиЗавершение(Результат, ВходящийКонтекст) Экспорт

	Форма 								= ВходящийКонтекст.Форма;
	ОтчетСсылка 						= ВходящийКонтекст.ОтчетСсылка;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	ОрганизацияОтчета 					= ВходящийКонтекст.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность 		= ВходящийКонтекст.ЭтоОтправкаИзФормыОтчетность;
	
	// если не удалось сохранить - возврат
	Если Результат = Ложь Тогда
		Возврат;
	КонецЕсли;
	
	// если отчет не был записан ранее, то получим ссылку на записанный отчет
	Если ОтчетСсылка = Неопределено Тогда
		
		Если НЕ ЭтоОтправкаИзФормыОтчетность Тогда
			ОтчетСсылка = ДокументооборотСФТСКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
			ВходящийКонтекст.ОтчетСсылка = ОтчетСсылка;
		КонецЕсли;
		
		Если ОтчетСсылка = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЭтоОтправкаИзФормыОтчетность Тогда
		
		ЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ОтчетСсылка, "ДатаНачала, ДатаОкончания, Вид");
		
		ПоляДляЗаписиОтправки = Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
			НачалоДня(ЗначенияРеквизитов.ДатаНачала),
			КонецДня(ЗначенияРеквизитов.ДатаОкончания),
			ЗначенияРеквизитов.Вид);
		
	Иначе
		
		ПоляДляЗаписиОтправки = Новый Структура("ДатаНачалаПериода, ДатаОкончанияПериода, Версия",
			Форма.СтруктураРеквизитовФормы.мДатаНачалаПериодаОтчета,
			Форма.СтруктураРеквизитовФормы.мДатаКонцаПериодаОтчета,
			Форма.СтруктураРеквизитовФормы.ВидДокумента * Форма.НомерКорректировки);
			
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения(
		Форма, 
		ОтчетСсылка, 
		МестоХраненияКлюча, 
		ПоляДляЗаписиОтправки, 
		ОрганизацияОтчета, 
		ЭтоОтправкаИзФормыОтчетность);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения(
		Форма, 
		ОтчетСсылка, 
		МестоХраненияКлюча, 
		ПоляДляЗаписиОтправки, 
		ОрганизацияОтчета,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь)
	
	// запрашиваем подтверждение
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Форма", 								Форма);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", 				ПоляДляЗаписиОтправки);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 					ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 		ЭтоОтправкаИзФормыОтчетность);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(ОтчетСсылка, ОписаниеОповещения);
	
КонецПроцедуры	

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ОтчетСсылка, ВходящийКонтекст.ОрганизацияОтчета, "ФТС");
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохранения_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	Форма 		= ВходящийКонтекст.Форма;
	ОтчетСсылка = ВходящийКонтекст.ОтчетСсылка;
	
	ПараметрыФормы = Новый Структура("СсылкаНаОтчет", ОтчетСсылка);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохраненияЗавершение", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	ПодтверждениеОтправкиОтчета(
		Форма, 
		ОписаниеОповещения, 
		ОтчетСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеСохраненияЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Форма = ДополнительныеПараметры.Форма;
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПоляДляЗаписиОтправки = ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	ОрганизацияОтчета = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	
	Если Ответ <> КодВозвратаДиалога.ОК Тогда
		Возврат;
	КонецЕсли;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодтверждения(Форма, ОтчетСсылка, МестоХраненияКлюча, ПоляДляЗаписиОтправки, ОрганизацияОтчета, ЭтоОтправкаИзФормыОтчетность);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодтверждения(Форма, ОтчетСсылка, МестоХраненияКлюча, ПоляДляЗаписиОтправки, ОрганизацияОтчета,
		Знач ЭтоОтправкаИзФормыОтчетность = Ложь)
	
	// проверяем, отправлялся ли отчет
	РезультатПроверки = ПроверитьПередОтправкойВФТСНаСервере(ОтчетСсылка);
	ИдентификаторНеПринятойОтправкиНаСервере = "";
	Если НЕ РезультатПроверки.ОтправкаРазрешена Тогда
		Если ЗначениеЗаполнено(РезультатПроверки.ТекстВопроса) Тогда
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("Форма",                                    Форма);
			ДополнительныеПараметры.Вставить("ОтчетСсылка",                              ОтчетСсылка);
			ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки",                    ПоляДляЗаписиОтправки);
			ДополнительныеПараметры.Вставить("ОрганизацияОтчета",                        ОрганизацияОтчета);
			ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность",             ЭтоОтправкаИзФормыОтчетность);
			ДополнительныеПараметры.Вставить("ИдентификаторНеПринятойОтправкиНаСервере", РезультатПроверки.ИдентификаторНеПринятойОтправкиНаСервере);
			ДополнительныеПараметры.Вставить("ВозможноАннулированиеСОтправкойНового",    РезультатПроверки.КнопкиВопроса <> Неопределено);
			КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
			
			ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодтвержденияЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			Если РезультатПроверки.КнопкиВопроса <> Неопределено Тогда
				Для каждого КнопкаВопроса Из РезультатПроверки.КнопкиВопроса Цикл
					КнопкаВопроса.Значение = ?(КнопкаВопроса.Значение = "Да", 	КодВозвратаДиалога.Да,
											 ?(КнопкаВопроса.Значение = "Нет", 	КодВозвратаДиалога.Нет, КнопкаВопроса.Значение));
				КонецЦикла;
			КонецЕсли;
			ПоказатьВопрос(
				ОписаниеОповещения,
				РезультатПроверки.ТекстВопроса,
				?(РезультатПроверки.КнопкиВопроса = Неопределено, РежимДиалогаВопрос.ДаНет, РезультатПроверки.КнопкиВопроса),,
				КодВозвратаДиалога.Нет);
		КонецЕсли;
		
	Иначе
		РежимыОтправки = Новый Структура;
		РежимыОтправки.Вставить("ИдентификаторНеПринятойОтправкиНаСервере", ИдентификаторНеПринятойОтправкиНаСервере);
		РежимыОтправки.Вставить("ЭтоАннулированиеСОтправкойНового", 		Ложь);
		РежимыОтправки.Вставить("ТекстПричиныАннулирования", 				"");
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПроверки(
			Форма,
			ОтчетСсылка,
			МестоХраненияКлюча,
			ПоляДляЗаписиОтправки,
			ОрганизацияОтчета,
			ЭтоОтправкаИзФормыОтчетность,
			РежимыОтправки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодтвержденияЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Форма                                    = ДополнительныеПараметры.Форма;
	ОтчетСсылка                              = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча						 = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПоляДляЗаписиОтправки                    = ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	ОрганизацияОтчета                        = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность             = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ИдентификаторНеПринятойОтправкиНаСервере = ДополнительныеПараметры.ИдентификаторНеПринятойОтправкиНаСервере;
	ВозможноАннулированиеСОтправкойНового    = ДополнительныеПараметры.ВозможноАннулированиеСОтправкойНового;
	
	ЭтоАннулированиеСОтправкойНового = (Ответ = "АннулироватьОтправитьНовый");
	
	Если Ответ <> КодВозвратаДиалога.Да И НЕ ЭтоАннулированиеСОтправкойНового Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтоАннулированиеСОтправкойНового Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеВводаЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
		ПоказатьВводСтроки(
			ОписаниеОповещения,,
			НСтр("ru = 'Введите причину аннулирования'"),
			1500,
			Истина);
		Возврат;
		
	ИначеЕсли ВозможноАннулированиеСОтправкойНового Тогда
		ИдентификаторНеПринятойОтправкиНаСервере = ""; // отправка отдельного отчета, без аннулирования
	КонецЕсли;
	
	РежимыОтправки = Новый Структура;
	РежимыОтправки.Вставить("ИдентификаторНеПринятойОтправкиНаСервере", ИдентификаторНеПринятойОтправкиНаСервере);
	РежимыОтправки.Вставить("ЭтоАннулированиеСОтправкойНового", 		Ложь);
	РежимыОтправки.Вставить("ТекстПричиныАннулирования", 				"");
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПроверки(
		Форма,
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПоляДляЗаписиОтправки,
		ОрганизацияОтчета,
		ЭтоОтправкаИзФормыОтчетность,
		РежимыОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеВводаЗавершение(ТекстОтвета, ДополнительныеПараметры) Экспорт
	
	Форма                                    = ДополнительныеПараметры.Форма;
	ОтчетСсылка                              = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча						 = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПоляДляЗаписиОтправки                    = ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	ОрганизацияОтчета                        = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность             = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ИдентификаторНеПринятойОтправкиНаСервере = ДополнительныеПараметры.ИдентификаторНеПринятойОтправкиНаСервере;
	ВозможноАннулированиеСОтправкойНового    = ДополнительныеПараметры.ВозможноАннулированиеСОтправкойНового;
	
	Если ТекстОтвета = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекстОтвета) Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодтвержденияЗавершение",
			ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, "АннулироватьОтправитьНовый");
		Возврат;
	КонецЕсли;
	
	РежимыОтправки = Новый Структура;
	РежимыОтправки.Вставить("ИдентификаторНеПринятойОтправкиНаСервере", ИдентификаторНеПринятойОтправкиНаСервере);
	РежимыОтправки.Вставить("ЭтоАннулированиеСОтправкойНового", 		Истина);
	РежимыОтправки.Вставить("ТекстПричиныАннулирования", 				ТекстОтвета);
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПроверки(
		Форма,
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПоляДляЗаписиОтправки,
		ОрганизацияОтчета,
		ЭтоОтправкаИзФормыОтчетность,
		РежимыОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПроверки(
	Форма,
	ОтчетСсылка,
	МестоХраненияКлюча,
	ПоляДляЗаписиОтправки,
	ОрганизацияОтчета,
	ЭтоОтправкаИзФормыОтчетность,
	РежимыОтправки)
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВФТС(
		ОтчетСсылка, 
		Форма, 
		ЭтоОтправкаИзФормыОтчетность);
		
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
		
	Если РезультатВыгрузки = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось выгрузить отчет.'"));
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Форма", 									Форма);
	ДополнительныеПараметры.Вставить("ОтчетСсылка", 							ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ПоляДляЗаписиОтправки", 					ПоляДляЗаписиОтправки);
	ДополнительныеПараметры.Вставить("ОрганизацияОтчета", 						ОрганизацияОтчета);
	ДополнительныеПараметры.Вставить("ЭтоОтправкаИзФормыОтчетность", 			ЭтоОтправкаИзФормыОтчетность);
	ДополнительныеПараметры.Вставить("РежимыОтправки", 							РежимыОтправки);
	ДополнительныеПараметры.Вставить("ТекстФайлаВыгрузки", 						РезультатВыгрузки.ТекстФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки", 						РезультатВыгрузки.ИмяФайлаВыгрузки);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеИнициализации", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("ОрганизацияОтчета", 					ОрганизацияОтчета);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 					ОписаниеОповещения);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, КонтекстДлительнойОперации);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ОтчетСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ФТС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПроверки",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПроверки() Экспорт
	
	КонтекстДлительнойОперации 	= ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	ОписаниеОповещения 			= КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеИнициализации(Результат, ДополнительныеПараметры) Экспорт
	
	Форма                        = ДополнительныеПараметры.Форма;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	ОрганизацияОтчета            = ДополнительныеПараметры.ОрганизацияОтчета;
	ЭтоОтправкаИзФормыОтчетность = ДополнительныеПараметры.ЭтоОтправкаИзФормыОтчетность;
	ТекстФайлаВыгрузки           = ДополнительныеПараметры.ТекстФайлаВыгрузки;
	ИмяФайлаВыгрузки             = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	СертификатыИПараметрыОбмена = Неопределено;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеВыгрузкиПакета",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Формирование файлов для отправки..."); // ФТС
		
	ВыгрузитьДляОтправкиВФТС(
		Форма,
		ОписаниеОповещения,
		ТекстФайлаВыгрузки,
		ИмяФайлаВыгрузки,
		СертификатыИПараметрыОбмена,
		ЭтоОтправкаИзФормыОтчетность,
		ОтчетСсылка,
		ОрганизацияОтчета);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеВыгрузкиПакета(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	СертификатыИПараметрыОбмена = Результат.СертификатыИПараметрыОбмена;

	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПараметрыФормированияПакета = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета;
	
	ДополнительныеПараметры.Вставить("ПараметрыФормированияПакета", ПараметрыФормированияПакета);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
	
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		СтрокаBase64ФайлаВыгрузки = Результат.РезультатВыгрузки;

		КороткоеИмяФайлаВыгрузки = Результат.ИмяФайлаВыгрузки;
		
		Результат = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64", Истина, Неопределено, СтрокаBase64ФайлаВыгрузки);
		
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки", КороткоеИмяФайлаВыгрузки);
		
		Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЧтенияФайлаВыгрузки", ЭтотОбъект, ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Результат);
		
	Иначе
		Крипт = СертификатыИПараметрыОбмена.Крипт;
		КороткоеИмяФайлаВыгрузки = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(Результат.РезультатВыгрузки).Имя;
		
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки", КороткоеИмяФайлаВыгрузки);

		Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЧтенияФайлаВыгрузки", ЭтотОбъект, ДополнительныеПараметры);
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, Результат.РезультатВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЧтенияФайлаВыгрузки(Результат, ДополнительныеПараметры) Экспорт
	
	ОрганизацияОтчета 					= ДополнительныеПараметры.ОрганизацияОтчета;
	ОтчетСсылка 						= ДополнительныеПараметры.ОтчетСсылка;
	ПоляДляЗаписиОтправки 				= ДополнительныеПараметры.ПоляДляЗаписиОтправки;
	СертификатыИПараметрыОбмена 		= ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ПараметрыФормированияПакета 		= ДополнительныеПараметры.ПараметрыФормированияПакета;
	КороткоеИмяФайлаВыгрузки 			= ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	Форма 								= ДополнительныеПараметры.Форма;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	РежимыОтправки 						= ДополнительныеПараметры.РежимыОтправки;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаВыгрузки = Результат.СтрокаBase64;
	
	ФормаДлительнаяОперация = Неопределено;
	ИнформацияОСервереФТС = СерверПриемаОтчетностиФТС();
	
	ПараметрыОтправки = Новый Структура();
	ПараметрыОтправки.Вставить("Организация", 					ОрганизацияОтчета);
	ПараметрыОтправки.Вставить("АдресСервера", 					ИнформацияОСервереФТС.АдресСервера);
	ПараметрыОтправки.Вставить("ВерсияФормированияВыгрузки", 	ИнформацияОСервереФТС.ВерсияФормированияВыгрузки);
	ПараметрыОтправки.Вставить("ВерсияОтправкиПортала", 		ИнформацияОСервереФТС.ВерсияОтправкиПортала);
	ПараметрыОтправки.Вставить("ПоляДляЗаписиОтправки", 		ПоляДляЗаписиОтправки);
	ПараметрыОтправки.Вставить("РежимыОтправки", 				РежимыОтправки);
	
	ПараметрыСоединения = Новый Структура();
	ПараметрыСоединения.Вставить("Логин", 										ПараметрыФормированияПакета.Логин);
	ПараметрыСоединения.Вставить("Пароль", 										ПараметрыФормированияПакета.Пароль);
	ПараметрыСоединения.Вставить("СохранитьПароль", 							ЗначениеЗаполнено(ПараметрыФормированияПакета.Пароль));
	ПараметрыСоединения.Вставить("СохраненныйЛогин", 							ПараметрыФормированияПакета.Логин);
	ПараметрыСоединения.Вставить("СохраненныйПароль", 							ПараметрыФормированияПакета.Пароль);
	ПараметрыСоединения.Вставить("СоединениеHTTP", 								Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie", 								"");
	ПараметрыСоединения.Вставить("ИдентификаторОтправкиНаСервере", 				"");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером", 						"");
	ПараметрыСоединения.Вставить("ОписаниеОшибокПроверки", 						"");
	ПараметрыСоединения.Вставить("ПовторятьСоединение", 						Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля", 						Истина);
	ПараметрыСоединения.Вставить("ОрганизацияПодтверждена", 					Неопределено);
	ПараметрыСоединения.Вставить("ОтправленЗапросНаПодтверждениеОрганизации", 	Ложь);
	ПараметрыСоединения.Вставить("СообщенияПользователю", 						Новый Массив);
	ПараметрыСоединения.Вставить("ИдентификаторВыполненияОтправки", 			"");
	ПараметрыСоединения.Вставить("ПодписываемоеСодержимоеВыгрузки64", 			"");
	ПараметрыСоединения.Вставить("ПодписываемыйЗапрос64", 						"");
	ПараметрыСоединения.Вставить("ТекущийАдресСервера", 						ИнформацияОСервереФТС.АдресСервера);
	ПараметрыСоединения.Вставить("ВерсияПортала", 								ИнформацияОСервереФТС.ВерсияПортала);
	ПараметрыСоединения.Вставить("ТекстОтветаАвторизации", 						"");
	ПараметрыСоединения.Вставить("СертификатАбонента", 							ПараметрыФормированияПакета.СертификатАбонента);
	
	КонвертироватьВКодировку = "utf-8";
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Передача файлов на сервер..."); // ФТС
	
	СтрокаBase64ФайлаОтправки = СформироватьФайлОтправкиВФТСНаСервере(
		КороткоеИмяФайлаВыгрузки, 
		СтрокаBase64ФайлаВыгрузки, 
		НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча), 
		Форма.УникальныйИдентификатор, 
		КонвертироватьВКодировку, 
		ПараметрыОтправки);
		
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСОтправкаВЦикле(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаВыгрузки,
		КороткоеИмяФайлаВыгрузки,
		Форма,
		СертификатыИПараметрыОбмена);
	
КонецПроцедуры

// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (Цикл) 
&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСОтправкаВЦикле(
	ОтчетСсылка,
	МестоХраненияКлюча,
	ПараметрыОтправки,
	ПараметрыСоединения,
	ФормаДлительнаяОперация,
	СтрокаBase64ФайлаОтправки,
	СтрокаBase64ФайлаВыгрузки,
	КороткоеИмяФайлаВыгрузки,
	Форма,
	СертификатыИПараметрыОбмена)
	
	РезультатОтправки                         = Неопределено;
	ПараметрыСоединения.ЗначениеCookie        = "";
	ПерваяПопыткаАвторизации                  = Истина;
	ПараметрыСоединения.ПовторятьЗапросПароля = Истина;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСВводПароля(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаВыгрузки,
		КороткоеИмяФайлаВыгрузки,
		РезультатОтправки,
		ПерваяПопыткаАвторизации,
		Форма,
		СертификатыИПараметрыОбмена);
		
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСВводПароля(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаОтправки,
		СтрокаBase64ФайлаВыгрузки,
		КороткоеИмяФайлаВыгрузки,
		РезультатОтправки,
		ПерваяПопыткаАвторизации,
		Форма,
		СертификатыИПараметрыОбмена)
		
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
	ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
	ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
	ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
	ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаВыгрузки",           СтрокаBase64ФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки",            КороткоеИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("РезультатОтправки",                   РезультатОтправки);
	ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации",            ПерваяПопыткаАвторизации);
	ДополнительныеПараметры.Вставить("Форма",                               Форма);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСВводПароляЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	
	Если НЕ ЗначениеЗаполнено(ПараметрыСоединения.Логин) 
		ИЛИ НЕ ЗначениеЗаполнено(ПараметрыСоединения.Пароль) 
		ИЛИ НЕ ПерваяПопыткаАвторизации Тогда
		
		ВызватьФормуВводаПароляПорталаФТС(
			"https://" + ПараметрыОтправки.АдресСервера + "/",
			ПараметрыСоединения.Логин,
			ПараметрыСоединения.Пароль,
			ПараметрыСоединения.СохранитьПароль,
			ОписаниеОповещения);
		
	Иначе
		
		РезультатВводаПароля = Новый Структура();
		РезультатВводаПароля.Вставить("Логин",           ПараметрыСоединения.Логин);
		РезультатВводаПароля.Вставить("Пароль",          ПараметрыСоединения.Пароль);
		РезультатВводаПароля.Вставить("СохранитьПароль", ПараметрыСоединения.СохранитьПароль);
		
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, РезультатВводаПароля);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСВводПароляЗавершение(РезультатВводаПароля, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	СтрокаBase64ФайлаВыгрузки           = ДополнительныеПараметры.СтрокаBase64ФайлаВыгрузки;
	КороткоеИмяФайлаВыгрузки            = ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	РезультатОтправки                   = ДополнительныеПараметры.РезультатОтправки;
	ПерваяПопыткаАвторизации            = ДополнительныеПараметры.ПерваяПопыткаАвторизации;
	Форма                               = ДополнительныеПараметры.Форма;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	
	Если РезультатВводаПароля = Неопределено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	ПараметрыСоединения.Логин           = РезультатВводаПароля.Логин;
	ПараметрыСоединения.Пароль          = РезультатВводаПароля.Пароль;
	ПараметрыСоединения.СохранитьПароль = РезультатВводаПароля.СохранитьПароль;
	
	РезультатОтправки = ОтправитьВФТСНаСервере(
		МестоХраненияКлюча,
		СтрокаBase64ФайлаОтправки,
		КороткоеИмяФайлаВыгрузки,
		ОтчетСсылка,
		ПараметрыОтправки,
		ПараметрыСоединения);
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ПерваяПопыткаАвторизации = Ложь;
	
	// рекурсивный вызов - замена цикла
	
	ОтправитьЗапросНаПодтверждениеОрганизации = (НЕ РезультатОтправки И ПараметрыСоединения.ОрганизацияПодтверждена <> Неопределено
		И НЕ ПараметрыСоединения.ОтправленЗапросНаПодтверждениеОрганизации);
	Если ОтправитьЗапросНаПодтверждениеОрганизации Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка", ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки", ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения", ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация", ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаВыгрузки", СтрокаBase64ФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки", КороткоеИмяФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("Форма", Форма);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", СертификатыИПараметрыОбмена);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЗаписиПодписываемогоЗапроса", ЭтотОбъект, ДополнительныеПараметры);
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ПараметрыСоединения.ПодписываемыйЗапрос64);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПараметрыСоединения.ПодписываемыйЗапрос64);
		КонецЕсли;
		
		Если ПараметрыСоединения.ОрганизацияПодтверждена = Ложь Тогда
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	Если РезультатОтправки Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаВыгрузки",           СтрокаBase64ФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки",            КороткоеИмяФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЗаписиПодписываемойВыгрузкиЗавершение", ЭтотОбъект, ДополнительныеПараметры);
		
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			
			РезультатВыполнения = Новый Структура();
			РезультатВыполнения.Вставить("Выполнено",      Истина);
			РезультатВыполнения.Вставить("ДвоичныеДанные", Неопределено);
			РезультатВыполнения.Вставить("ИмяФайла",       ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64);
			
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
			
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПараметрыСоединения.ПодписываемоеСодержимоеВыгрузки64);
		КонецЕсли;
		
	ИначеЕсли ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибокПроверки) Тогда
		
		СтруктураПараметров = Новый Структура();
		СтруктураПараметров.Вставить("ИмяФайла",              "");
		СтруктураПараметров.Вставить("Содержимое",            ПараметрыСоединения.ОписаниеОшибокПроверки);
		СтруктураПараметров.Вставить("Кодировка",             Неопределено);
		СтруктураПараметров.Вставить("ПереноситьПоСловам",    Истина);
		СтруктураПараметров.Вставить("ТекстЗаголовка",        НСтр("ru = 'Протокол ошибок'"));
		СтруктураПараметров.Вставить("ПечатьРазрешена",       Истина);
		СтруктураПараметров.Вставить("ВысотаФормы",           15);
		СтруктураПараметров.Вставить("КлючУникальностиФормы", "ОписаниеОшибокПроверкиФТС");
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий(); // ФТС.
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрТекст", СтруктураПараметров);
		
		
	ИначеЕсли ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// не получено файла результата при отправке пакета. Возможно, проблемы с доступом в интернет
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаОтправки",           СтрокаBase64ФайлаОтправки);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаВыгрузки",           СтрокаBase64ФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки",            КороткоеИмяФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВФТСПослеПоказаОшибкиДоступаВИнтернет", ЭтотОбъект, ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу Федеральной таможенной службы'"), 
			ОписаниеОповещения);
			
	ИначеЕсли НЕ ОтправитьЗапросНаПодтверждениеОрганизации Тогда
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЗаписиПодписываемогоЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ИмяФайлаВыгрузки = ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	ОрганизацияОтчета = ДополнительныеПараметры.ПараметрыОтправки.Организация;
	
	ИмяФайлаПодписываемогоЗапроса = Результат.ИмяФайла;
	
	НастройкиПодписи = Новый Структура();
	НастройкиПодписи.Вставить("Трансформация1_1", 			Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 	Истина);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 	Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 			Неопределено);
	НастройкиПодписи.Вставить("СсылкаНаМЧД", 				СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.МЧД);
	НастройкиПодписи.Вставить("ИННДоверителя", 				СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИННДоверителя);
	Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодписанияФайлаЗапросаXMLDSig", ЭтотОбъект, ДополнительныеПараметры);
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаВыгрузки);
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ОтправкаВФТС", ОрганизацияОтчета); 
		ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, ТребуетПодтверждениеОперации, НастройкиПодписи);
	КонецЕсли;
	
	ПодписатьФайлXMLDSig(СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента, ИмяФайлаПодписываемогоЗапроса, Оповещение, , , НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодписанияФайлаЗапросаXMLDSig(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось подписать файл запроса подтверждения организации!'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСЧтенияФайлаПодписиЗапроса", ЭтотОбъект, ДополнительныеПараметры);
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, СтрокаBase64", Истина, Неопределено, ПолноеИмяПодписанногоФайла);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСЧтенияФайлаПодписиЗапроса(Результат, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПараметрыОтправки = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыСоединения = ДополнительныеПараметры.ПараметрыСоединения;
	ФормаДлительнаяОперация = ДополнительныеПараметры.ФормаДлительнаяОперация;
	Форма = ДополнительныеПараметры.Форма;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	ВыводитьОшибки = (ПараметрыСоединения.ОрганизацияПодтверждена = Ложь)
		ИЛИ (НЕ ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибокПроверки) И НЕ ПараметрыСоединения.ПовторятьСоединение);
	
	СтрокаBase64ФайлаПодписанногоЗапроса = Результат.СтрокаBase64;
	
	РезультатОтправки = ОтправитьПодписанныйЗапросВФТСНаСервере(
		МестоХраненияКлюча,
		СтрокаBase64ФайлаПодписанногоЗапроса,
		ОтчетСсылка,
		ПараметрыОтправки,
		ПараметрыСоединения);
		
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	Если РезультатОтправки Тогда
		Если ПараметрыСоединения.ОрганизацияПодтверждена = Истина Тогда
			ТекстСообщения = НСтр("ru = 'Отправлен запрос обновления сведений об ЭП организации на портале ФТС, обработка запроса обычно происходит в течение 10 минут.'") + ?(ВыводитьОшибки, "
										|" + НСтр("ru = 'Отчет не отправлен, повторите попытку отправки позже.'"), "");
		Иначе
			ТекстСообщения = СтрШаблон(
				НСтр("ru = 'Внимание! Отправлен запрос на подтверждение ЭП организации (проверка в ЕСИА), обработка запроса обычно происходит в течение 10 минут.
						   |Отчет не отправлен, повторите попытку отправки позже, после подтверждения организации порталом ФТС.
						   |Если вы ожидаете подтверждения организации более 2 часов и отправка не удается, обратитесь в техническую поддержку на портале ФТС %1'"),
				"https://" + ПараметрыОтправки.АдресСервера);
		КонецЕсли;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
	ИначеЕсли ВыводитьОшибки И ПараметрыСоединения.ОрганизацияПодтверждена = Истина
		И СтрНайти(ПараметрыСоединения.ОписаниеОшибкиСервером, "Запрос на добавление организации уже создан, ожидайте решения") = 0 Тогда
		
		ТекстСообщения = НСтр("ru = 'Ошибка отправки запроса обновления сведений о сертификате ЭП организации на портале ФТС.'")
			+ ?(ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибкиСервером), " " + ПараметрыСоединения.ОписаниеОшибкиСервером, "");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Если ВыводитьОшибки Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправитьПодписанныйЗапросВФТСНаСервере(МестоХраненияКлюча, Знач СтрокаBase64ФайлаПодписанногоЗапроса, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьПодписанныйЗапросВФТСССервера(МестоХраненияКлюча, СтрокаBase64ФайлаПодписанногоЗапроса, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
КонецФункции

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВФТСПослеПоказаОшибкиДоступаВИнтернет(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СтрокаBase64ФайлаОтправки           = ДополнительныеПараметры.СтрокаBase64ФайлаОтправки;
	СтрокаBase64ФайлаВыгрузки           = ДополнительныеПараметры.СтрокаBase64ФайлаВыгрузки;
	КороткоеИмяФайлаВыгрузки            = ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	Форма                               = ДополнительныеПараметры.Форма;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	
	ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// рекурсивный вызов - замена цикла
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСОтправкаВЦикле(
			ОтчетСсылка,
			МестоХраненияКлюча,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ФормаДлительнаяОперация,
			СтрокаBase64ФайлаОтправки,
			СтрокаBase64ФайлаВыгрузки,
			КороткоеИмяФайлаВыгрузки,
			Форма,
			СертификатыИПараметрыОбмена);
		
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФТС. Нет доступа в Интернет.
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЗаписиПодписываемойВыгрузкиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатыИПараметрыОбмена = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	ИмяФайлаВыгрузки = ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ОрганизацияОтчета = ДополнительныеПараметры.ПараметрыОтправки.Организация;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		Возврат;
	КонецЕсли;
	
	ИмяФайлаПодписываемойВыгрузки = Результат.ИмяФайла;
	
	НастройкиПодписи = Новый Структура();
	НастройкиПодписи.Вставить("Трансформация1_1", 			Истина);
	НастройкиПодписи.Вставить("ВключатьХэшСертификата", 	Истина);
	НастройкиПодписи.Вставить("НеВключатьЗаголовокXML", 	Истина);
	НастройкиПодписи.Вставить("ЗаменаУзлаОбъекта", 			Неопределено);
	НастройкиПодписи.Вставить("ПереданХешВместоОбъекта", 	Истина);
	НастройкиПодписи.Вставить("СсылкаНаМЧД", 				СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.МЧД);
	НастройкиПодписи.Вставить("ИННДоверителя", 				СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИННДоверителя);
	
	Оповещение = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодписанияФайлаXMLDSigЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(ИмяФайлаВыгрузки);
		ТребуетПодтверждениеОперации = РежимПодтвержденияОблачнойПодписи("ОтправкаВФТС", ОрганизацияОтчета); 
		ПодготовитьОблачнуюПодпись(ОписаниеФайла.Имя, ОписаниеФайла.Расширение, Истина, НастройкиПодписи);
	КонецЕсли;
	
	ПодписатьФайлXMLDSig(
		СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента,
		ИмяФайлаПодписываемойВыгрузки,
		Оповещение,
		,
		,
		НастройкиПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПодписанияФайлаXMLDSigЗавершение(ПолноеИмяПодписанногоФайла, ДополнительныеПараметры) Экспорт
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	
	Если ПолноеИмяПодписанногоФайла = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось подписать файл выгрузки!'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС. 
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСЧтенияФайлаПодписиЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		
		РезультатВыполнения = Новый Структура();
		РезультатВыполнения.Вставить("Выполнено",      Истина);
		РезультатВыполнения.Вставить("ДвоичныеДанные", Неопределено);
		РезультатВыполнения.Вставить("СтрокаBase64",   ПолноеИмяПодписанногоФайла);

		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	Иначе
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(Оповещение, ПолноеИмяПодписанногоФайла);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСЧтенияФайлаПодписиЗавершение(Результат, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча 					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	КороткоеИмяФайлаВыгрузки            = ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	Форма                               = ДополнительныеПараметры.Форма;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	
	Если НЕ Результат.Выполнено Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС. 
		Возврат;
	КонецЕсли;
	
	СтрокаBase64ФайлаПодписи = Результат.СтрокаBase64;
	
	ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСОтправкаПодписиВЦикле(
		ОтчетСсылка,
		МестоХраненияКлюча,
		ПараметрыОтправки,
		ПараметрыСоединения,
		ФормаДлительнаяОперация,
		СтрокаBase64ФайлаПодписи,
		КороткоеИмяФайлаВыгрузки,
		Форма,
		СертификатыИПараметрыОбмена);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСОтправкаПодписиВЦикле(
	ОтчетСсылка,
	МестоХраненияКлюча,
	ПараметрыОтправки,
	ПараметрыСоединения,
	ФормаДлительнаяОперация,
	СтрокаBase64ФайлаПодписи,
	КороткоеИмяФайлаВыгрузки,
	Форма,
	СертификатыИПараметрыОбмена)

	РезультатОтправки = ОтправитьПодписьВФТСНаСервере(
		МестоХраненияКлюча,
		СтрокаBase64ФайлаПодписи,
		КороткоеИмяФайлаВыгрузки,
		ОтчетСсылка,
		ПараметрыОтправки,
		ПараметрыСоединения);
		
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(ПараметрыСоединения.СообщенияПользователю);
	
	ОтправитьЗапросНаПодтверждениеОрганизации = (РезультатОтправки = Неопределено И ПараметрыСоединения.ОрганизацияПодтверждена <> Неопределено
		И НЕ ПараметрыСоединения.ОтправленЗапросНаПодтверждениеОрганизации);
	Если ОтправитьЗапросНаПодтверждениеОрганизации Тогда
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка", 						ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки", 					ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения", 				ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация", 			ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПодписи", 			СтрокаBase64ФайлаПодписи);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки", 			КороткоеИмяФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("Форма", 								Форма);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена", 		СертификатыИПараметрыОбмена);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		Оповещение = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеЗаписиПодписываемогоЗапроса", ЭтотОбъект, ДополнительныеПараметры);
		Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
			РезультатВыполнения = Новый Структура("Выполнено, ДвоичныеДанные, ИмяФайла", Истина, Неопределено, ПараметрыСоединения.ПодписываемыйЗапрос64);
			ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		Иначе
			ОперацииСФайламиЭДКОКлиент.Base64ВФайл(Оповещение, ПараметрыСоединения.ПодписываемыйЗапрос64);
		КонецЕсли;
		
		Если ПараметрыСоединения.ОрганизацияПодтверждена = Ложь Тогда
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	Если РезультатОтправки <> Неопределено Тогда
		
		ПараметрыАвтозапроса = Новый Структура();
		ПараметрыАвтозапроса.Вставить("Ключ",           РезультатОтправки);
		ПараметрыАвтозапроса.Вставить("ЗначениеCookie", ПараметрыСоединения.ЗначениеCookie);
		ПараметрыАвтозапроса.Вставить("Логин",          ПараметрыСоединения.Логин);
		ПараметрыАвтозапроса.Вставить("Пароль",         ПараметрыСоединения.Пароль);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ПараметрыАвтозапроса", ПараметрыАвтозапроса);
		
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ФТС.
		
	ИначеЕсли ЗначениеЗаполнено(ПараметрыСоединения.ОписаниеОшибокПроверки) Тогда
		
		СтруктураПараметров = Новый Структура();
		СтруктураПараметров.Вставить("ИмяФайла",              "");
		СтруктураПараметров.Вставить("Содержимое",            ПараметрыСоединения.ОписаниеОшибокПроверки);
		СтруктураПараметров.Вставить("Кодировка",             Неопределено);
		СтруктураПараметров.Вставить("ПереноситьПоСловам",    Истина);
		СтруктураПараметров.Вставить("ТекстЗаголовка",        НСтр("ru = 'Протокол ошибок'"));
		СтруктураПараметров.Вставить("ПечатьРазрешена",       Истина);
		СтруктураПараметров.Вставить("ВысотаФормы",           15);
		СтруктураПараметров.Вставить("КлючУникальностиФормы", "ОписаниеОшибокПроверкиФТС");
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий(); // ФТС.
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ПросмотрТекст", СтруктураПараметров);
		
	ИначеЕсли ПараметрыСоединения.ПовторятьСоединение Тогда
		
		// не получено файла результата при отправке пакета. Возможно, проблемы с доступом в интернет
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
		ДополнительныеПараметры.Вставить("ПараметрыОтправки",                   ПараметрыОтправки);
		ДополнительныеПараметры.Вставить("ПараметрыСоединения",                 ПараметрыСоединения);
		ДополнительныеПараметры.Вставить("ФормаДлительнаяОперация",             ФормаДлительнаяОперация);
		ДополнительныеПараметры.Вставить("СтрокаBase64ФайлаПодписи",            СтрокаBase64ФайлаПодписи);
		ДополнительныеПараметры.Вставить("КороткоеИмяФайлаВыгрузки",            КороткоеИмяФайлаВыгрузки);
		ДополнительныеПараметры.Вставить("Форма",                               Форма);
		ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         СертификатыИПараметрыОбмена);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПоказаОшибкиДоступаВИнтернет", ЭтотОбъект, ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			НСтр("ru = 'Не удалось обратиться к серверу Федеральной таможенной службы'"),
			ОписаниеОповещения);
			
	ИначеЕсли НЕ ОтправитьЗапросНаПодтверждениеОрганизации Тогда
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(); // ФТС.
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСПослеПоказаОшибкиДоступаВИнтернет(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ОтчетСсылка                         = ДополнительныеПараметры.ОтчетСсылка;
	МестоХраненияКлюча					= КонтекстМоделиХраненияКлюча(ДополнительныеПараметры);
	ПараметрыОтправки                   = ДополнительныеПараметры.ПараметрыОтправки;
	ПараметрыСоединения                 = ДополнительныеПараметры.ПараметрыСоединения;
	ФормаДлительнаяОперация             = ДополнительныеПараметры.ФормаДлительнаяОперация;
	СтрокаBase64ФайлаПодписи            = ДополнительныеПараметры.СтрокаBase64ФайлаПодписи;
	КороткоеИмяФайлаВыгрузки            = ДополнительныеПараметры.КороткоеИмяФайлаВыгрузки;
	Форма                               = ДополнительныеПараметры.Форма;
	СертификатыИПараметрыОбмена         = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	
	ПараметрыСоединения.ПовторятьСоединение = (РезультатДиалогаОбОшибкеДоступа = Истина);
	
	// рекурсивный вызов - замена цикла
	Если ПараметрыСоединения.ПовторятьСоединение Тогда
		
		ПриНажатииНаКнопкуОтправкиВКонтролирующийОрганДляФТСОтправкаПодписиВЦикле(
			ОтчетСсылка,
			МестоХраненияКлюча,
			ПараметрыОтправки,
			ПараметрыСоединения,
			ФормаДлительнаяОперация,
			СтрокаBase64ФайлаПодписи,
			КороткоеИмяФайлаВыгрузки,
			Форма,
			СертификатыИПараметрыОбмена);
			
	Иначе
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НетДоступаВИнтернет", Истина);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ФТС. Нет доступа в Интернет.	
		
	КонецЕсли;
	
КонецПроцедуры
// Цикл Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаКлиенте
Процедура ВызватьФормуВводаПароляПорталаФТС(Портал, Логин, Пароль, СохранитьПароль, ВыполняемоеОповещение = Неопределено)
	
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(, Истина)
		И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина) = 0 Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(НСтр("ru = 'Не указаны параметры доступа к порталу Федеральной таможенной службы'"));
	Иначе
		ПараметрыФормы = Новый Структура("Портал, Логин, Пароль, СохранитьПароль", Портал, Логин, Пароль, СохранитьПароль);
		ОписаниеОповещения = Новый ОписаниеОповещения("ВызватьФормуВводаПароляПорталаФТСЗавершение", ЭтотОбъект, ВыполняемоеОповещение);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ВводПароляФТС", ПараметрыФормы,,,,, ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВызватьФормуВводаПароляПорталаФТСЗавершение(РезультатФормы, ВыполняемоеОповещение) Экспорт
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПротоколОбработкиПоСсылкеИсточникаДляФТС(ИсточникСсылка, ПечатьВозможна = Истина) Экспорт
	
	ВАрхиве = ОбъектВАрхиве(ИсточникСсылка, "Протокол");
	Если ВАрхиве Тогда 
		ПоказатьУведомлениеАрхивныхФайлов(, 16, 0, Истина);
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("ИсточникСсылка",      ИсточникСсылка);
	ПараметрыФормы.Вставить("ПечатьВозможна",      ПечатьВозможна);
	ПараметрыФормы.Вставить("КонтролирующийОрган", ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФТС"));
		
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПротоколОбработкиHTML", ПараметрыФормы);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбновитьРегистрыСведенийНастройкиОбменаФТС(
		Организация,
		ОтпечатокСертификата,
		ВыводитьСообщения,
		Логин,
		Пароль,
		МЧД = Неопределено)
	
	Попытка
	
		ОбменФТС = Мультирежим.МенеджерЗаписиНастроекОбмена(
			Организация,
			ОтпечатокСертификата,
			"ФТС",
			"НастройкиОбменаФТС",
			"СертификатАбонентаОтпечаток");
		
		ОбменФТС.ИспользоватьАвтонастройку 	= Истина;
		ОбменФТС.Доверенность 				= МЧД;
		ОбменФТС.Логин 						= Логин;
		ОбменФТС.Пароль 					= Пароль;
		ОбменФТС.Записать();
		Если ВыводитьСообщения Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				НСтр("ru = 'Обновлены параметры отправки отчетности в Федеральную таможенную службу.'"));
		КонецЕсли;
		
	Исключение
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Не удалось обновить параметры отправки отчетности в Федеральную таможенную службу по причине:
                  |%1'"), КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецПопытки;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьФайлОтправкиВФТСНаСервере(
		КороткоеИмяФайлаВыгрузки, 
		СтрокаBase64ФайлаВыгрузки, 
		ЭтоАдреса, 
		Адрес, 
		Кодировка, 
		ПараметрыОтправки)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СформироватьФайлОтправкиВФТСССервера(
		КороткоеИмяФайлаВыгрузки, 
		СтрокаBase64ФайлаВыгрузки, 
		ЭтоАдреса, 
		Адрес, 
		Кодировка, 
		ПараметрыОтправки);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбновитьРезультатКонкретнойОтправкиФТСНаСервере(Знач ОтправкаСсылка, Знач ОтправкаОбъектЗагружен, Знач ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбновитьРезультатКонкретнойОтправкиФТСССервера(ОтправкаСсылка, ОтправкаОбъектЗагружен, ПараметрыОтправки, ПараметрыСоединения, ЭтоВызовИзСписка);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьПередОтправкойВФТСНаСервере(Отчет)
	
	Результат = Новый Структура;
	Результат.Вставить("ОтправкаРазрешена", 						Истина);
	Результат.Вставить("ТекстВопроса", 								"");
	Результат.Вставить("КнопкиВопроса", 							Неопределено);
	Результат.Вставить("ИдентификаторНеПринятойОтправкиНаСервере", 	"");
	
	ИдентификаторОтправкиНаСервере = "";
	ТекущаяОтправка = ПолучитьПоследнююОтправкуОтчетаВФТС(Отчет, ИдентификаторОтправкиНаСервере);
	Если ЗначениеЗаполнено(ТекущаяОтправка) Тогда
		СтатусОтправки = ТекущаяОтправка.СтатусОтправки;
		Результат.ИдентификаторНеПринятойОтправкиНаСервере = ?(СтатусОтправки = Перечисления.СтатусыОтправки.НеПринят
			ИЛИ СтатусОтправки = Перечисления.СтатусыОтправки.Сдан, ИдентификаторОтправкиНаСервере, "");
		
		Если СтатусОтправки = Перечисления.СтатусыОтправки.Отправлен Тогда
			Результат.ОтправкаРазрешена = Ложь;
			Результат.ТекстВопроса = НСтр("ru = 'Отчет уже отправлен, результат отправки не получен.
												|Вы действительно хотите отправить отчет повторно?
												|
												|Обратите внимание, для аннулирования отправленного ранее отчета и подачи исправленного вместо него,
												|нужно дождаться сдачи отчета и обновить состояние, затем воспользоваться кнопкой отправки.'");
			Возврат Результат;
			
		ИначеЕсли СтатусОтправки = Перечисления.СтатусыОтправки.Сдан Тогда
			Результат.ОтправкаРазрешена = Ложь;
			Результат.ТекстВопроса = НСтр("ru = 'Отчет уже отправлен и успешно сдан.
												|
												|Для аннулирования сданного отчета и подачи исправленного вместо него, нажмите ""Аннулировать с отправкой нового"".
												|Если хотите отправить как новый отчет, нажмите ""Отправить повторно"".'");
			КнопкиВопроса = Новый СписокЗначений;
			КнопкиВопроса.Добавить("АннулироватьОтправитьНовый", 	НСтр("ru = 'Аннулировать с отправкой нового'"));
			КнопкиВопроса.Добавить("Да", 							НСтр("ru = 'Отправить повторно'"));
			КнопкиВопроса.Добавить("Нет", 							НСтр("ru = 'Отмена'"));
			Результат.КнопкиВопроса = КнопкиВопроса;
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьВФТСНаСервере(МестоХраненияКлюча, Знач СтрокаBase64ФайлаОтправки, Знач КороткоеИмяФайлаВыгрузки, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьВФТСССервера(МестоХраненияКлюча, СтрокаBase64ФайлаОтправки, КороткоеИмяФайлаВыгрузки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправитьПодписьВФТСНаСервере(МестоХраненияКлюча, Знач СтрокаBase64ФайлаПодписи, Знач КороткоеИмяФайлаВыгрузки, Знач ОтчетСсылка, Знач ПараметрыОтправки, ПараметрыСоединения)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОтправитьПодписьВФТСССервера(МестоХраненияКлюча, СтрокаBase64ФайлаПодписи, КороткоеИмяФайлаВыгрузки, ОтчетСсылка, ПараметрыОтправки, ПараметрыСоединения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПоляДляЗаписиОтправкиВФТСНаСервере(ОбъектСсылка)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПоляНаСервереДляЗаписиОтправкиВФТС(ОбъектСсылка);
	
КонецФункции

&НаСервере
Функция СерверПриемаОтчетностиФТС() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СерверПриемаОтчетностиФТС();
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойстваОбменаОрганизацииДляФТСНаСервере(Знач ОрганизацияСсылка, Знач ОпределитьНастроенностьОбменаВУниверсальномФормате, Знач ПолучитьОсновныеСвойстваПоследнейОтправки, Знач ОтправкаСсылка = Неопределено, ОтчетСсылка = Неопределено) Экспорт
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.СвойстваОбменаОрганизацииДляФТС(ОрганизацияСсылка, ОпределитьНастроенностьОбменаВУниверсальномФормате, ПолучитьОсновныеСвойстваПоследнейОтправки, ОтправкаСсылка, ОтчетСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Функция НастройкиФТСНаСервере(Знач ОрганизацияСсылка, Знач ПолучитьПараметрыОнлайнПроверки = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.НастройкиФТС(ОрганизацияСсылка, ПолучитьПараметрыОнлайнПроверки);
	
КонецФункции

#Область ТаможенноеДекларирование

// ОтправкаСсылка ссылается на загруженный объект
&НаКлиенте
Процедура ПолучитьПроцедурыДекларированияФТС(
		ВыполняемоеОповещение,
		Организация,
		РежимПолучения = "ВыпущенныеДТ",
		Отборы = Неопределено,
		РежимыРаботы = Неопределено) Экспорт
	
	РежимыРаботыВызова = Новый Структура;
	РежимыРаботыВызова.Вставить("ТолькоПолучитьНастройки", 						Ложь);
	РежимыРаботыВызова.Вставить("ПовторятьЗапросПароляПриОшибке", 				Истина);
	РежимыРаботыВызова.Вставить("ПоказыватьДиалогПриОшибкеДоступаВИнтернет", 	Истина);
	Если РежимыРаботы <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(РежимыРаботыВызова, РежимыРаботы);
	КонецЕсли;
	
	ПерваяПопыткаАвторизации 	= Истина;
	АвторизацияПроизведена 		= Ложь;
	ИнформацияОСервереФТС 		= СерверПриемаОтчетностиФТС();
	НастройкиФТС 				= НастройкиФТСНаСервере(Организация);
	
	ПараметрыОтправки = Новый Структура;
	ПараметрыОтправки.Вставить("Организация", 					Организация);
	ПараметрыОтправки.Вставить("АдресСервера", 					ИнформацияОСервереФТС.АдресСервера);
	ПараметрыОтправки.Вставить("ВерсияФормированияВыгрузки", 	ИнформацияОСервереФТС.ВерсияФормированияВыгрузки);
	ПараметрыОтправки.Вставить("ВерсияОтправкиПортала", 		ИнформацияОСервереФТС.ВерсияОтправкиПортала);
	ПараметрыОтправки.Вставить("РежимПолучения", 				РежимПолучения);
	ПараметрыОтправки.Вставить("Отборы", 						Отборы);
	
	ПараметрыСоединения = Новый Структура;
	ПараметрыСоединения.Вставить("Логин", 					НастройкиФТС.Логин);
	ПараметрыСоединения.Вставить("Пароль", 					НастройкиФТС.Пароль);
	ПараметрыСоединения.Вставить("СохранитьПароль", 		ЗначениеЗаполнено(НастройкиФТС.Пароль));
	ПараметрыСоединения.Вставить("СохраненныйЛогин", 		НастройкиФТС.Логин);
	ПараметрыСоединения.Вставить("СохраненныйПароль", 		НастройкиФТС.Пароль);
	ПараметрыСоединения.Вставить("СоединениеHTTP", 			Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie", 			"");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером", 	"");
	ПараметрыСоединения.Вставить("ОписаниеОшибокПроверки", 	"");
	ПараметрыСоединения.Вставить("ПовторятьСоединение", 	Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля", 	Истина);
	ПараметрыСоединения.Вставить("СообщенияПользователю", 	Новый Массив);
	ПараметрыСоединения.Вставить("ТекущийАдресСервера", 	ИнформацияОСервереФТС.АдресСервера);
	ПараметрыСоединения.Вставить("ВерсияПортала", 			ИнформацияОСервереФТС.ВерсияПортала);
	ПараметрыСоединения.Вставить("ТекстОтветаАвторизации", 	"");
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ПараметрыСоединения", 			ПараметрыСоединения);
	ДополнительныеПараметры.Вставить("ПараметрыОтправки", 				ПараметрыОтправки);
	ДополнительныеПараметры.Вставить("АвторизацияПроизведена", 			АвторизацияПроизведена);
	ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации", 		ПерваяПопыткаАвторизации);
	ДополнительныеПараметры.Вставить("РежимыРаботы", 					РежимыРаботыВызова);
	ДополнительныеПараметры.Вставить("ОповещениеОПолученииНастроек", 	Неопределено);
	
	Оповещение = Новый ОписаниеОповещения("ПолучитьПроцедурыДекларированияФТСПослеПолученияНастроек",
		ЭтотОбъект, ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(Оповещение, Истина);
	
КонецПроцедуры

// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (Цикл)
&НаКлиенте
Процедура ПолучитьПроцедурыДекларированияФТСПослеПолученияНастроек(РезультатДиалогаОбОшибкеДоступа,
		ДополнительныеПараметры) Экспорт
	
	Если РезультатДиалогаОбОшибкеДоступа <> Истина Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ПараметрыСоединения.Логин)
		ИЛИ НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ПараметрыСоединения.Пароль)
		ИЛИ НЕ ДополнительныеПараметры.ПерваяПопыткаАвторизации Тогда
		
		ДополнительныеПараметры.ПерваяПопыткаАвторизации = Истина;
		
		Оповещение = Новый ОписаниеОповещения("ПолучитьПроцедурыДекларированияФТСПослеВводаПароля",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ВызватьФормуВводаПароляПорталаФТС(
			"https://" + ДополнительныеПараметры.ПараметрыОтправки.АдресСервера + "/",
			ДополнительныеПараметры.ПараметрыСоединения.Логин,
			ДополнительныеПараметры.ПараметрыСоединения.Пароль,
			ДополнительныеПараметры.ПараметрыСоединения.СохранитьПароль,
			Оповещение);
		
		Возврат;
	КонецЕсли;
	
	Если ДополнительныеПараметры.РежимыРаботы.ТолькоПолучитьНастройки Тогда
		ВыполняемоеОповещение = ?(ДополнительныеПараметры.ОповещениеОПолученииНастроек = Неопределено,
			ДополнительныеПараметры.ВыполняемоеОповещение, ДополнительныеПараметры.ОповещениеОПолученииНастроек);
		ДополнительныеПараметры.ВыполняемоеОповещение 			= Неопределено;
		ДополнительныеПараметры.ОповещениеОПолученииНастроек 	= Неопределено;
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ДополнительныеПараметры);
		Возврат;
	КонецЕсли;
	
	// при успешном обращении на сервер ФТС отправка обновится в ИБ
	ПроцедурыДекларирования = ПолучитьПроцедурыДекларированияФТСНаСервере(ДополнительныеПараметры.ПараметрыОтправки,
		ДополнительныеПараметры.ПараметрыСоединения);
	
	ОбработатьРезультатыПолученияПроцедурДекларированияФТС(
		ДополнительныеПараметры.ВыполняемоеОповещение,
		ПроцедурыДекларирования,
		ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьРезультатыПолученияПроцедурДекларированияФТС(
		ВыполняемоеОповещение,
		ПроцедурыДекларирования,
		ДополнительныеПараметрыПолучения,
		ОповещениеОПолученииНастроек = Неопределено) Экспорт
	
	ДополнительныеПараметрыПолучения.ВыполняемоеОповещение 			= ВыполняемоеОповещение;
	ДополнительныеПараметрыПолучения.ОповещениеОПолученииНастроек 	= ОповещениеОПолученииНастроек;
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(
		ДополнительныеПараметрыПолучения.ПараметрыСоединения.СообщенияПользователю);
	
	ДополнительныеПараметрыПолучения.ПерваяПопыткаАвторизации = Ложь;
	
	// рекурсивный вызов - замена цикла
	Если ПроцедурыДекларирования = Неопределено
		И ДополнительныеПараметрыПолучения.ПараметрыСоединения.ПовторятьЗапросПароля Тогда
		
		Если ДополнительныеПараметрыПолучения.РежимыРаботы.ПовторятьЗапросПароляПриОшибке Тогда
			Оповещение = Новый ОписаниеОповещения("ПолучитьПроцедурыДекларированияФТСПослеПолученияНастроек",
				ЭтотОбъект, ДополнительныеПараметрыПолучения);
			
			ВыполнитьОбработкуОповещения(Оповещение, Истина);
			Возврат;
		КонецЕсли;
		
	ИначеЕсли ДополнительныеПараметрыПолучения.ПараметрыСоединения.ПовторятьСоединение Тогда
		// возможно, проблемы с доступом в интернет
		Если НЕ ДополнительныеПараметрыПолучения.АвторизацияПроизведена Тогда
			ДополнительныеПараметрыПолучения.ПараметрыСоединения.ЗначениеCookie = "";
		КонецЕсли;
		
		Если ДополнительныеПараметрыПолучения.РежимыРаботы.ПоказыватьДиалогПриОшибкеДоступаВИнтернет Тогда
			Оповещение = Новый ОписаниеОповещения("ПолучитьПроцедурыДекларированияФТСПослеПолученияНастроек",
				ЭтотОбъект, ДополнительныеПараметрыПолучения);
			
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
				НСтр("ru = 'Не удалось обратиться к серверу Федеральной таможенной службы'"),
				Оповещение);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ДополнительныеПараметрыПолучения.ВыполняемоеОповещение, ПроцедурыДекларирования);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьПроцедурыДекларированияФТСПослеВводаПароля(РезультатВводаПароля, ДополнительныеПараметры) Экспорт
	
	Если РезультатВводаПароля = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		
	Иначе
		ДополнительныеПараметры.ПараметрыСоединения.Логин 			= РезультатВводаПароля.Логин;
		ДополнительныеПараметры.ПараметрыСоединения.Пароль 			= РезультатВводаПароля.Пароль;
		ДополнительныеПараметры.ПараметрыСоединения.СохранитьПароль = РезультатВводаПароля.СохранитьПароль;
		
		Оповещение = Новый ОписаниеОповещения("ПолучитьПроцедурыДекларированияФТСПослеПолученияНастроек",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ВыполнитьОбработкуОповещения(Оповещение, Истина);
	КонецЕсли;
	
КонецПроцедуры
// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаСервереБезКонтекста
Функция ПолучитьПроцедурыДекларированияФТСНаСервере(Знач ПараметрыОтправки, ПараметрыСоединения)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ПолучитьПроцедурыДекларированияФТСССервера(ПараметрыОтправки, ПараметрыСоединения);
	
КонецФункции

#КонецОбласти

#Область МашиночитаемыеДоверенностиФТС

&НаКлиенте
Процедура ОтправитьМашиночитаемуюДоверенностьФТС(
		ВыполняемоеОповещение,
		Организация,
		КороткоеИмяФайлаВыгрузки,
		АдресВыгрузки,
		АдресПодписи,
		ОтчетСсылка = Неопределено) Экспорт
	
	ПараметрыОбработки = Новый Структура;
	ПараметрыОбработки.Вставить("КороткоеИмяФайлаВыгрузки", КороткоеИмяФайлаВыгрузки);
	ПараметрыОбработки.Вставить("АдресВыгрузки", 			АдресВыгрузки);
	ПараметрыОбработки.Вставить("АдресПодписи", 			АдресПодписи);
	ПараметрыОбработки.Вставить("ОтчетСсылка", 				ОтчетСсылка);
	
	ОбработатьНаПорталеФТС(
		ВыполняемоеОповещение,
		Организация,
		"ОтправитьМашиночитаемуюДоверенностьФТС",
		ПараметрыОбработки);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьМашиночитаемуюДоверенностьФТС(
		ВыполняемоеОповещение,
		Организация,
		ИдентификаторОтправкиНаСервере = "",
		ОтчетСсылка = Неопределено,
		ОтправкаСсылка = Неопределено) Экспорт
	
	ПараметрыОбработки = Новый Структура;
	ПараметрыОбработки.Вставить("ИдентификаторОтправкиНаСервере", 	ИдентификаторОтправкиНаСервере);
	ПараметрыОбработки.Вставить("ОтчетСсылка", 						ОтчетСсылка);
	ПараметрыОбработки.Вставить("ОтправкаСсылка", 					ОтправкаСсылка);
	
	ОбработатьНаПорталеФТС(
		ВыполняемоеОповещение,
		Организация,
		"ОбновитьМашиночитаемуюДоверенностьФТС",
		ПараметрыОбработки);
	
КонецПроцедуры

// ОтправкаСсылка ссылается на загруженный объект
&НаКлиенте
Процедура ОбработатьНаПорталеФТС(
		ВыполняемоеОповещение,
		Организация,
		ТипОперации,
		ПараметрыОбработки = Неопределено) Экспорт
	
	РежимыРаботыВызова = Новый Структура;
	РежимыРаботыВызова.Вставить("ПовторятьЗапросПароляПриОшибке", 				Истина);
	РежимыРаботыВызова.Вставить("ПоказыватьДиалогПриОшибкеДоступаВИнтернет", 	Истина);
	
	ПерваяПопыткаАвторизации 	= Истина;
	АвторизацияПроизведена 		= Ложь;
	ИнформацияОСервереФТС 		= СерверПриемаОтчетностиФТС();
	НастройкиФТС 				= НастройкиФТСНаСервере(Организация);
	
	ПараметрыОтправки = Новый Структура;
	ПараметрыОтправки.Вставить("Организация", 						Организация);
	ПараметрыОтправки.Вставить("АдресСервера", 						ИнформацияОСервереФТС.АдресСервера);
	ПараметрыОтправки.Вставить("ВерсияФормированияВыгрузки", 		ИнформацияОСервереФТС.ВерсияФормированияВыгрузки);
	ПараметрыОтправки.Вставить("ВерсияОтправкиПортала", 			ИнформацияОСервереФТС.ВерсияОтправкиПортала);
	ПараметрыОтправки.Вставить("ВерсияФормированияДоверенности", 	ИнформацияОСервереФТС.ВерсияФормированияДоверенности);
	ПараметрыОтправки.Вставить("ВерсияОтправкиДоверенности", 		ИнформацияОСервереФТС.ВерсияОтправкиДоверенности);
	ПараметрыОтправки.Вставить("ТипОперации", 						ТипОперации);
	Если ПараметрыОбработки <> Неопределено Тогда
		Для каждого ПараметрОбработки Из ПараметрыОбработки Цикл
			ПараметрыОтправки.Вставить(ПараметрОбработки.Ключ, ПараметрОбработки.Значение);
		КонецЦикла;
	КонецЕсли;
	
	ПараметрыСоединения = Новый Структура;
	ПараметрыСоединения.Вставить("Логин", 					НастройкиФТС.Логин);
	ПараметрыСоединения.Вставить("Пароль", 					НастройкиФТС.Пароль);
	ПараметрыСоединения.Вставить("СохранитьПароль", 		ЗначениеЗаполнено(НастройкиФТС.Пароль));
	ПараметрыСоединения.Вставить("СохраненныйЛогин", 		НастройкиФТС.Логин);
	ПараметрыСоединения.Вставить("СохраненныйПароль", 		НастройкиФТС.Пароль);
	ПараметрыСоединения.Вставить("СоединениеHTTP", 			Неопределено);
	ПараметрыСоединения.Вставить("ЗначениеCookie", 			"");
	ПараметрыСоединения.Вставить("ОписаниеОшибкиСервером", 	"");
	ПараметрыСоединения.Вставить("ОписаниеОшибокПроверки", 	"");
	ПараметрыСоединения.Вставить("ПовторятьСоединение", 	Истина);
	ПараметрыСоединения.Вставить("ПовторятьЗапросПароля", 	Истина);
	ПараметрыСоединения.Вставить("СообщенияПользователю", 	Новый Массив);
	ПараметрыСоединения.Вставить("ТекущийАдресСервера", 	ИнформацияОСервереФТС.АдресСервера);
	ПараметрыСоединения.Вставить("ВерсияПортала", 			ИнформацияОСервереФТС.ВерсияПортала);
	ПараметрыСоединения.Вставить("ТекстОтветаАвторизации", 	"");
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 			ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ПараметрыСоединения", 			ПараметрыСоединения);
	ДополнительныеПараметры.Вставить("ПараметрыОтправки", 				ПараметрыОтправки);
	ДополнительныеПараметры.Вставить("АвторизацияПроизведена", 			АвторизацияПроизведена);
	ДополнительныеПараметры.Вставить("ПерваяПопыткаАвторизации", 		ПерваяПопыткаАвторизации);
	ДополнительныеПараметры.Вставить("РежимыРаботы", 					РежимыРаботыВызова);
	
	Оповещение = Новый ОписаниеОповещения("ОбработатьНаПорталеФТСПослеПолученияНастроек",
		ЭтотОбъект, ДополнительныеПараметры);
	
	ВыполнитьОбработкуОповещения(Оповещение, Истина);
	
КонецПроцедуры

// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (Цикл)
&НаКлиенте
Процедура ОбработатьНаПорталеФТСПослеПолученияНастроек(РезультатДиалогаОбОшибкеДоступа,
		ДополнительныеПараметры) Экспорт
	
	Если РезультатДиалогаОбОшибкеДоступа <> Истина Тогда
		ДлительнаяОтправкаКлиент.ОповеститьОНевозможностиОбновитьСтатусОтчета();
		Если ДополнительныеПараметры.ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ПараметрыСоединения.Логин)
		ИЛИ НЕ ЗначениеЗаполнено(ДополнительныеПараметры.ПараметрыСоединения.Пароль)
		ИЛИ НЕ ДополнительныеПараметры.ПерваяПопыткаАвторизации Тогда
		
		ДополнительныеПараметры.ПерваяПопыткаАвторизации = Истина;
		
		Оповещение = Новый ОписаниеОповещения("ОбработатьНаПорталеФТСПослеВводаПароля",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ВызватьФормуВводаПароляПорталаФТС(
			"https://" + ДополнительныеПараметры.ПараметрыОтправки.АдресСервера + "/",
			ДополнительныеПараметры.ПараметрыСоединения.Логин,
			ДополнительныеПараметры.ПараметрыСоединения.Пароль,
			ДополнительныеПараметры.ПараметрыСоединения.СохранитьПароль,
			Оповещение);
		
		Возврат;
	КонецЕсли;
	
	// при успешном обращении на сервер ФТС отправка обновится в ИБ
	РезультатОтправки = ОбработатьНаПорталеФТСНаСервере(ДополнительныеПараметры.ПараметрыОтправки,
		ДополнительныеПараметры.ПараметрыСоединения);
	Если РезультатОтправки <> Неопределено Тогда
		РезультатОтправки.Вставить("ЗначениеCookie", 	ДополнительныеПараметры.ПараметрыСоединения.ЗначениеCookie);
		РезультатОтправки.Вставить("Логин", 			ДополнительныеПараметры.ПараметрыСоединения.Логин);
		РезультатОтправки.Вставить("Пароль", 			ДополнительныеПараметры.ПараметрыСоединения.Пароль);
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ВывестиСообщенияССервераНаКлиенте(
		ДополнительныеПараметры.ПараметрыСоединения.СообщенияПользователю);
	
	ДополнительныеПараметры.ПерваяПопыткаАвторизации = Ложь;
	
	// рекурсивный вызов - замена цикла
	Если РезультатОтправки = Неопределено
		И ДополнительныеПараметры.ПараметрыСоединения.ПовторятьЗапросПароля Тогда
		
		Если ДополнительныеПараметры.РежимыРаботы.ПовторятьЗапросПароляПриОшибке Тогда
			Оповещение = Новый ОписаниеОповещения("ОбработатьНаПорталеФТСПослеПолученияНастроек",
				ЭтотОбъект, ДополнительныеПараметры);
			
			ВыполнитьОбработкуОповещения(Оповещение, Истина);
			Возврат;
		КонецЕсли;
		
	ИначеЕсли ДополнительныеПараметры.ПараметрыСоединения.ПовторятьСоединение Тогда
		// возможно, проблемы с доступом в интернет
		Если НЕ ДополнительныеПараметры.АвторизацияПроизведена Тогда
			ДополнительныеПараметры.ПараметрыСоединения.ЗначениеCookie = "";
		КонецЕсли;
		
		Если ДополнительныеПараметры.РежимыРаботы.ПоказыватьДиалогПриОшибкеДоступаВИнтернет Тогда
			Оповещение = Новый ОписаниеОповещения("ОбработатьНаПорталеФТСПослеПолученияНастроек",
				ЭтотОбъект, ДополнительныеПараметры);
			
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
				НСтр("ru = 'Не удалось обратиться к серверу Федеральной таможенной службы'"),
				Оповещение);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Если ДополнительныеПараметры.ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, РезультатОтправки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьНаПорталеФТСПослеВводаПароля(РезультатВводаПароля, ДополнительныеПараметры) Экспорт
	
	Если РезультатВводаПароля = Неопределено Тогда
		Если ДополнительныеПараметры.ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		
	Иначе
		ДополнительныеПараметры.ПараметрыСоединения.Логин 			= РезультатВводаПароля.Логин;
		ДополнительныеПараметры.ПараметрыСоединения.Пароль 			= РезультатВводаПароля.Пароль;
		ДополнительныеПараметры.ПараметрыСоединения.СохранитьПароль = РезультатВводаПароля.СохранитьПароль;
		
		Оповещение = Новый ОписаниеОповещения("ОбработатьНаПорталеФТСПослеПолученияНастроек",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ВыполнитьОбработкуОповещения(Оповещение, Истина);
	КонецЕсли;
	
КонецПроцедуры
// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаСервереБезКонтекста
Функция ОбработатьНаПорталеФТСНаСервере(Знач ПараметрыОтправки, ПараметрыСоединения)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДО.ОбработатьНаПорталеФТСССервера(ПараметрыОтправки, ПараметрыСоединения);
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область ОтправкаРеестраНДС

#КонецОбласти

#Область ОтправкаРеестраНДС

// используется также для отправки реестров по акцизам
&НаКлиенте
Процедура ОтправкаРеестраНДС(ВыполняемоеОповещение) Экспорт
	
	ДополнительныеПараметры = ВыполняемоеОповещение.ДополнительныеПараметры;
	ДокументСсылка 	 		= ДополнительныеПараметры.СсылкаНаОтчет;
	Идентификатор 			= ДополнительныеПараметры.Форма.УникальныйИдентификатор;
	Организация             = ДополнительныеПараметры.ОрганизацияОтчета;
	
	// Копируем только нужные параметры и дополняем своими.
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("УникальныйИдентификатор", 	Идентификатор);
	КонтекстОтправки.Вставить("Документ", 				 	ДокументСсылка);
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	
	ДанныеФайловВыгрузки = ПолучитьДанныеФайлаРеестраНДС(ДокументСсылка, ДополнительныеПараметры.Форма);
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	Если ДанныеФайловВыгрузки.Количество() <> 2
		И (ДанныеФайловВыгрузки.Количество() <> 1 ИЛИ Лев(ДанныеФайловВыгрузки[0].Имя, 9) <> "ON_REESTR") Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Реестр.

		Возврат;
	КонецЕсли;

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ДанныеФайловВыгрузки", 	ДанныеФайловВыгрузки);
	ДополнительныеПараметры.Вставить("Организация", 	        Организация);
	ДополнительныеПараметры.Вставить("ДокументСсылка", 	        ДокументСсылка);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРеестраНДСПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("ДокументСсылка", 		ДокументСсылка);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", 	ОписаниеОповещения);

	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= ДокументСсылка;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;

	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка реестра в ФНС
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаРеестраНДС",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаРеестраНДС() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	ДокументСсылка 		= КонтекстДлительнойОперации.ДокументСсылка;
	ОписаниеОповещения 	= КонтекстДлительнойОперации.ОписаниеОповещения;
		
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(ДокументСсылка, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРеестраНДС_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ОтправкаРеестраНДС(ВходящийКонтекст.ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРеестраНДСПослеАвтонастройкиУчетнойЗаписи(Результат, Знач ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРеестраНДС_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.ДокументСсылка, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРеестраНДС_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	ВыполняемоеОповещение	= ВходящийКонтекст.ВыполняемоеОповещение;
	ДополнительныеПараметры = ВыполняемоеОповещение.ДополнительныеПараметры;
	ДокументСсылка 	 		= ДополнительныеПараметры.СсылкаНаОтчет;
	Идентификатор 			= ДополнительныеПараметры.Форма.УникальныйИдентификатор;
	ДанныеФайловВыгрузки	= ВходящийКонтекст.ДанныеФайловВыгрузки;
	
	ОтправкаОбъектаЗапрещена = ДокументооборотСКОВызовСервера.ОтправкаОбъектаЗапрещенаНаТарифеПромоЕНС(ДокументСсылка);
	Если ОтправкаОбъектаЗапрещена Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНевозможностиОтправкиНаТарифе(ДокументСсылка);
		Возврат;
	КонецЕсли;
	
	// Копируем только нужные параметры и дополняем своими.
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("УникальныйИдентификатор", 	Идентификатор);
	КонтекстОтправки.Вставить("Документ", 				 	ДокументСсылка);
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРеестраНДС_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки, ДанныеФайловВыгрузки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаРеестраНДС_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст); 
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ДокументСсылка, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ДокументСсылка, ВыполняемоеОповещение, Ложь); //+
			
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Реестр.
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ДокументСсылка) Тогда
			
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ДокументСсылка, ВыполняемоеОповещение, Ложь); //+
		Возврат;
			
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", 	ВыполняемоеОповещение);
	Контекст.Вставить("КонтекстОтправки", 		КонтекстОтправки);
	
	// формируем пакет
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРеестраНДСПослеФормированияПакета", ЭтотОбъект, Контекст);
		
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ФНС");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаРеестраНДС_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст, ДанныеФайловВыгрузки)
	
	ДокументСсылка = Контекст.Документ;
	
	// проверяем, включен ли механизм обмена с ФНС для организации, указанной в журнале
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ФНС
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСФНС") <> Истина Тогда
		
	    // Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	//требуется сформировать файл доверенности 
	//и определить налоговый орган (для записи в реквизит цикла обмена)
	//для этого определяем ИФНС через переопределяемый модуль
	КодНалоговогоОргана = СведенияПоОтправляемымОбъектам(ДокументСсылка).КодКонтролирующегоОргана;
	Если НЕ ЗначениеЗаполнено(КодНалоговогоОргана) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	ЭлРегистрацияВИФНС = РегистрацияВИФНСПоОрганизацииИКодуНО(Организация, КодНалоговогоОргана);
	Если НЕ ЗначениеЗаполнено(ЭлРегистрацияВИФНС) Тогда
		ТекстСообщения = НСтр("ru = 'Не удалось определить налоговый орган, в который представляется отчетность!'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	НалоговыйОрган = Справочники.НалоговыеОрганы.НайтиПоКоду(КодНалоговогоОргана);
	Если НЕ ЗначениеЗаполнено(НалоговыйОрган) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'Не найден налоговый орган с кодом %1  в справочнике налоговых органов""!.'"), Символ(34) + КодНалоговогоОргана + Символ(34));
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	//теперь формируем файл доверенности
	ФайлДоверенности = Неопределено;
	КороткоеИмяФайлаДоверенности = Неопределено;
	
	Доверенность = РегистрацияВНОСервер.ДанныеРегистрации(ЭлРегистрацияВИФНС).Доверенность;
	
	Если ЗначениеЗаполнено(ЭлРегистрацияВИФНС) И ЗначениеЗаполнено(Доверенность) Тогда
		ФайлДоверенности = ВыгрузитьДоверенностьПредставление(
			ЭлРегистрацияВИФНС,
			КороткоеИмяФайлаДоверенности,
			Организация,,,
			Истина);
		Если НЕ ЗначениеЗаполнено(ФайлДоверенности) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось выгрузить файл доверенности.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		ИначеЕсли ФайлДоверенности = "ИсключеноПриложениеДоверенности" Тогда
			ФайлДоверенности = Неопределено;
		КонецЕсли;
	ИначеЕсли РегистрацияВНОСервер.НетОбязательнойДоверенности(ЭлРегистрацияВИФНС) Тогда
		
		Возврат Ложь;
		
	КонецЕсли;
		
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка 				= Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид 					= Перечисления.ВидыЦикловОбмена.ЦиклОбменаСФНС;
	ЦиклОбмена.ФорматДокументооборота = Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЦиклОбмена.Предмет 				= ДокументСсылка;
	ЦиклОбмена.Идентификатор 		= СгенерироватьUUID();
	ЦиклОбмена.Организация 			= Организация;
	ЦиклОбмена.УчетнаяЗапись 		= УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация 	= НалоговыйОрган;
	ЦиклОбмена.Тип 					= Перечисления.ТипыЦикловОбмена.Представление;
	ЦиклОбмена.ВидОтчета 			= ЭлектронныйДокументооборотСКонтролирующимиОрганами.ВидОтправляемогоДокументаРеестраНДСПоСсылке(ДокументСсылка);
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена 	= ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип 			= Перечисления.ТипыТранспортныхСообщений.ПредставлениеНП;
	ТранспортноеСообщение.Отправитель 	= Организация;
	ТранспортноеСообщение.Получатель 	= НалоговыйОрган;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус 		= Перечисления.СтатусыПисем.Исходящее;
	
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ФНС) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
		
	Для каждого ФайлВыгрузки Из ДанныеФайловВыгрузки Цикл
		
		Если Найти(ФайлВыгрузки.Имя, ".2_") > 0 Тогда
			ТипСодержимого = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Приложение;
		Иначе
			ТипСодержимого = Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Представление;
		КонецЕсли;
	
		//добавляем файл Представление
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
				ТранспортноеСообщение.Ссылка, 
				ТипСодержимого, 
				ФайлВыгрузки.Адрес, 
				ФайлВыгрузки.Имя, 
				, 
				, 
				, 
				, 
				, 
				, 
				СгенерироватьUUID(), 
				Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			
			ТекстСообщения = НСтр("ru = 'Отправка реестра прервана.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			ОтменитьТранзакцию();
			Возврат Ложь;
			
		КонецЕсли;
		
	КонецЦикла;
	
	// добавляем доверенность, если она есть
	Если ЗначениеЗаполнено(ФайлДоверенности) Тогда
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(
				ТранспортноеСообщение.Ссылка, 
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ДоверенностьПредставление, 
				ФайлДоверенности, 
				КороткоеИмяФайлаДоверенности, 
				, 
				,
				,
				,
				, 
				, 
				СгенерироватьUUID(), 
				Перечисления.ТипыСодержимогоФайлов.xml) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЕсли;
		
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ИнформацияОбОшибке().Описание);
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(ДокументСсылка, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаРеестраНДСПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки 	= ВходящийКонтекст.КонтекстОтправки;
	Документ 			= КонтекстОтправки.Документ;
	
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Реестр.
		
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Реестр.

		Возврат;
	КонецЕсли;
	
	Оповестить("Запись_РегламентированныйОтчетРеестрНДСПриложение", КонтекстОтправки.Документ);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // Реестр.
	
КонецПроцедуры

// используется для независимой (от требования о представлении документов) отправки журнала СФ,
// используется также для получения данных реестров акцизов
&НаКлиенте
Функция ПолучитьДанныеФайлаРеестраНДС(ДокументСсылка, Форма)
	
	// инициализируем структуру-результат
	МассивВыгруженныхФайлов = Новый Массив;
	Если ТипЗнч(ДокументСсылка) = Тип("СправочникСсылка.ЭлектронныеПредставленияРегламентированныхОтчетов") Тогда
		
		МассивВыгруженныхФайлов = ПолучитьДанныеФайлаЭлектронногоПредставленияРеестраНДС(ДокументСсылка, Форма.УникальныйИдентификатор);
		
	Иначе
		
		// выгружаем отчет
		МассивДанныхВыгрузки		= "";
		МассивИменФайловВыгрузки 	= "";
		КодировкаФайлаВыгрузки 		= Неопределено;
		
		Если Форма <> Неопределено
			И ТипЗнч(Форма) = Тип("ФормаКлиентскогоПриложения")
			И (СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрНДСПриложение") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрНДСДекларации") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрНДС_КНД") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрАкцизыПриложение") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрАкцизыВычетыДенатурированныйЭтиловыйСпирт") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрАкцизыВычетыВиноматериалыВиногрИФруктСусло") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрАкцизыВычетыВиноград") > 0
			ИЛИ СтрНайти(Форма.ИмяФормы, "РегламентированныйОтчетРеестрАкцизы_КНД") > 0) Тогда
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента",,Форма);
		Иначе
			ФормаВыгрузкиРеглОтчета = ПолучитьФорму("Документ.ВыгрузкаРегламентированныхОтчетов.Форма.ФормаДокумента");
		КонецЕсли;
		
		РезультатВыгрузки = ФормаВыгрузкиРеглОтчета.ВыгрузитьОтчет(
			ДокументСсылка, 
			Ложь, 
			, 
			МассивДанныхВыгрузки, 
			МассивИменФайловВыгрузки, 
			КодировкаФайлаВыгрузки, 
			Истина);
			
		Если НЕ РезультатВыгрузки Тогда
			Возврат МассивВыгруженныхФайлов;
		КонецЕсли;
		
		МодульДокументооборотСРЭЦВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСРЭЦВызовСервера");
		МодульДокументооборотСРЭЦВызовСервера.ОбработатьМассивДанныхВыгрузки(ДокументСсылка, МассивДанныхВыгрузки);
		
		Для каждого ДанныеВыгрузки Из МассивДанныхВыгрузки Цикл
		
			ДанныеФайла = Новый Структура;
			ДанныеФайла.Вставить("Имя", 		ДанныеВыгрузки.ИмяФайлаВыгрузки);
			ДанныеФайла.Вставить("Адрес", 		ДанныеВыгрузки.АдресФайлаВыгрузки);
			ДанныеФайла.Вставить("Кодировка", 	КодировкаФайлаВыгрузки);
			
			МассивВыгруженныхФайлов.Добавить(ДанныеФайла);
		
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат МассивВыгруженныхФайлов;
	
КонецФункции

// используется также для получения данных электиронных представлений реестров по акцизам
&НаСервереБезКонтекста
Функция ПолучитьДанныеФайлаЭлектронногоПредставленияРеестраНДС(ДокументСсылка, УникальныйИдентификаторФормы)
	
	МассивВыгруженныхФайлов = Новый Массив;
	
	Таб = ИзвлечьЭлектронныеПредставленияИзХранилища(ДокументСсылка, 2);
	
	Для Каждого Выборка Из Таб Цикл
		
		Если Выборка.ВАрхиве Тогда
			ДанныеПредставления = Base64Значение("");
		Иначе
			ДанныеПредставления = Выборка.Данные;
		КонецЕсли;
		
		ДанныеФайла = Новый Структура;
		ДанныеФайла.Вставить("Имя", 	Выборка.ИмяФайла);		
		ДанныеФайла.Вставить("Адрес", 	ПоместитьВоВременноеХранилище(ДанныеПредставления, Новый УникальныйИдентификатор));
		
		МассивВыгруженныхФайлов.Добавить(ДанныеФайла);
		
	КонецЦикла;
	
	Возврат МассивВыгруженныхФайлов;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВДеревоДокументыРеализацииПолномочий(УзелДокумент, ДанныеФайловВыгрузки, ПараметрыОписи, КолФайл)
	
	УзелДокНапрНО 	= ДобавитьУзелXML(УзелДокумент, "ДокПредстНО");
	
	ДобавитьУзелXML(УзелДокНапрНО, "КолФайл", Истина, Формат(КолФайл, "ЧГ=0"));
	ДобавитьУзелXML(УзелДокНапрНО, "ИдФайлОсн", Ложь, ПараметрыОписи.ИдФайлОсн);
	
	Если НЕ ДобавитьВФайлВыгрузкиДокФорм(УзелДокНапрНО, ДанныеФайловВыгрузки) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если НЕ ДобавитьФайлВыгрузкиНеДокФорм(УзелДокНапрНО, ДанныеФайловВыгрузки) Тогда
		Возврат Ложь;
	КонецЕсли;
		
	Возврат Истина;
		
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВФайлВыгрузкиДокФорм(УзелДокНапрНО, ДанныеФайловВыгрузки)
			
	КНДПоВидуДокумента = ПолучитьСоответствиеКНДПоВидуДокумента();
			
	Для каждого СведенияЭлектронногоДокумента Из ДанныеФайловВыгрузки.ТаблицаДокФорм Цикл
			
		ОбщиеСвойстваЭД   = Новый Структура();
		МассивЭлементовЭД = Новый Массив;
		
		ОбщиеСвойстваЭД.Вставить("ПорНомДок", СведенияЭлектронногоДокумента.ПорНом);
		
		ОбщиеСвойстваЭД.Вставить("ЭтоПакетСДопДокументами", СведенияЭлектронногоДокумента.ЭтоПакетСДопДокументами);
	
		ДокФорм_ОпределитьСведенияДокумента(СведенияЭлектронногоДокумента, МассивЭлементовЭД, КНДПоВидуДокумента);
		
		Если НЕ ДокФорм_ОпределитьСведенияДокументаПодтверждения(СведенияЭлектронногоДокумента, МассивЭлементовЭД) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		Если НЕ ДокФорм_ОпределитьСведенияДокументаОснования(СведенияЭлектронногоДокумента, ОбщиеСвойстваЭД) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		ДокФорм_ДобавитьВсеСведенияВОпись(УзелДокНапрНО, МассивЭлементовЭД, ОбщиеСвойстваЭД);
		
	КонецЦикла;
		
	Возврат Истина;
		
КонецФункции

&НаСервереБезКонтекста
Функция ДокФорм_ДобавитьВсеСведенияВОпись(УзелДокНапрНО, МассивЭлементовЭД, ОбщиеСвойстваЭД)

	//заполнение файла xml на основе ОбщиеСвойстваЭД и МассивЭлементовЭД 
	Для каждого СвойстваЭлементаЭД Из МассивЭлементовЭД Цикл
		
		УзелДокФорм	= ДобавитьУзелXML(УзелДокНапрНО, "ДокФорм");

		Если НЕ ОбщиеСвойстваЭД.ЭтоПакетСДопДокументами Тогда 
			ДобавитьУзелXML(УзелДокФорм, "ПорНомДок", 	Истина, ОбщиеСвойстваЭД.ПорНомДок);
		КонецЕсли;
		
		Если ТребованияФНСКлиентСервер.ЭтоФормат_ON_DOCNPNO_1_886_00_05_02() Тогда
			Если СвойстваЭлементаЭД.Свойство("ТипФайл") Тогда
				ДобавитьУзелXML(УзелДокФорм, "ТипФайл", Истина, СвойстваЭлементаЭД.ТипФайл);
			Иначе
				ДобавитьУзелXML(УзелДокФорм, "ТипФайл", Истина, 1);
			КонецЕсли;
		КонецЕсли;
		
		ДобавитьУзелXML(УзелДокФорм, "ИдФайл", 		Истина, СвойстваЭлементаЭД.ИдФайл);

		Если ОбщиеСвойстваЭД.Свойство("СвДокОсн") И ЗначениеЗаполнено(ОбщиеСвойстваЭД.СвДокОсн) Тогда
			ДобавитьУзелXML(УзелДокФорм, "СвДокОсн", Ложь, ОбщиеСвойстваЭД.СвДокОсн);
		Конецесли;

		ДобавитьУзелXML(УзелДокФорм, "ИдФайлЭП", 	Ложь, СвойстваЭлементаЭД.ИдФайлЭП);
		ДобавитьУзелXML(УзелДокФорм, "КНД_Док", 	Ложь, СвойстваЭлементаЭД.КНД_Док);

	КонецЦикла;
			
КонецФункции

&НаСервереБезКонтекста
Функция ДокФорм_ОпределитьСведенияДокумента(СведенияЭлектронногоДокумента, МассивЭлементовЭД, КНДПоВидуДокумента)

	//заполнение массива МассивЭлементовЭД
	СвойстваЭлементаЭД = Новый Структура("КНД_Док, ИдФайл, ИдФайлЭП, ТипФайл");
				
	ИдФайл = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(СведенияЭлектронногоДокумента.ИмяФайлаДанных).ИмяБезРасширения;
				
	СвойстваЭлементаЭД.ИдФайл		= ИдФайл;
	СвойстваЭлементаЭД.ИдФайлЭП 	= СведенияЭлектронногоДокумента.ИмяФайлаПодписи;
	
	Если ЗначениеЗаполнено(СведенияЭлектронногоДокумента.КНД) Тогда
		СвойстваЭлементаЭД.КНД_Док = СведенияЭлектронногоДокумента.КНД;
	Иначе
		СвойстваЭлементаЭД.КНД_Док = КНДПоВидуДокумента[СведенияЭлектронногоДокумента.ВидДокумента];
	КонецЕсли;
	
	ДокФорм_ОпределитьТипФайл(СведенияЭлектронногоДокумента.ИмяФайлаДанных, СвойстваЭлементаЭД);
				
	МассивЭлементовЭД.Добавить(СвойстваЭлементаЭД);
				
КонецФункции

&НаСервереБезКонтекста
Функция ДокФорм_ОпределитьСведенияДокументаОснования(СведенияЭлектронногоДокумента, ОбщиеСвойстваЭД)
				
	Если СведенияЭлектронногоДокумента.ВидДокумента = Перечисления.ВидыПредставляемыхДокументов.АктПриемкиСдачиРабот Тогда
				
		Если ЗначениеЗаполнено(СведенияЭлектронногоДокумента.СведенияДокументаОснования) Тогда
			ОбщиеСвойстваЭД.Вставить("СвДокОсн", СведенияЭлектронногоДокумента.СведенияДокументаОснования);
		КонецЕсли;
				
	КонецЕсли;
			
	Возврат Истина;
		
КонецФункции

&НаСервереБезКонтекста
Функция ДокФорм_ОпределитьСведенияДокументаПодтверждения(СведенияЭлектронногоДокумента, МассивЭлементовЭД)
		
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Виды = КонтекстЭДОСервер.ВидыДокументовЭДОСПодтверждением();
	
	Если ЗначениеЗаполнено(СведенияЭлектронногоДокумента.ПодтверждениеИмяФайлаДанных) Тогда

		СвойстваЭлементаЭД = Новый Структура("КНД_Док, ИдФайл, ИдФайлЭП");
		ИмяФайлаВыгрузки = Сред(СведенияЭлектронногоДокумента.ПодтверждениеИмяФайлаДанных, 1, СтрДлина(СведенияЭлектронногоДокумента.ПодтверждениеИмяФайлаДанных) - 4); //без расширения и точки
		СвойстваЭлементаЭД.ИдФайл		= ИмяФайлаВыгрузки;
		СвойстваЭлементаЭД.ИдФайлЭП 	= СведенияЭлектронногоДокумента.ПодтверждениеИмяФайлаПодписи;
		
		ДокФорм_ОпределитьТипФайл(СведенияЭлектронногоДокумента.ПодтверждениеИмяФайлаДанных, СвойстваЭлементаЭД);
		
		Если ЗначениеЗаполнено(СведенияЭлектронногоДокумента.ПодтверждениеКНД) Тогда
			СвойстваЭлементаЭД.КНД_Док = СведенияЭлектронногоДокумента.ПодтверждениеКНД;
			МассивЭлементовЭД.Добавить(СвойстваЭлементаЭД);
		Иначе
			ТекстСообщения = НСтр("ru = 'Отсутствует КНД электронного документа-подтверждения для '") + СведенияЭлектронногоДокумента.ПредставлениеДокумента;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			Возврат Ложь;
		КонецЕсли;

	ИначеЕсли Виды.Найти(СведенияЭлектронногоДокумента.ВидДокумента) <> Неопределено Тогда
		ТекстСообщения = НСтр("ru = 'Отсутствует электронный документ-подтверждение у '") + СведенияЭлектронногоДокумента.ПредставлениеДокумента;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДобавитьУзелДокФорм_ДокПредстНО(УзелДокумент, КНД_Док, ИдФайлОсн, КолФайл, Знач ИдФайл, Знач ИдФайлЭП)
	
	УзелДокНапрНО 	= ДобавитьУзелXML(УзелДокумент, "ДокПредстНО");
	
	ДобавитьУзелXML(УзелДокНапрНО, "КолФайл", Истина, КолФайл);
	
	Если ИдФайлОсн 	= "ЖУРНАЛ" Тогда
		ДобавитьУзелXML(УзелДокНапрНО, "Журнал", Ложь, ИдФайлОсн);
	Иначе
		ДобавитьУзелXML(УзелДокНапрНО, "ИдФайлОсн", Ложь, ИдФайлОсн);
	КонецЕсли;
	
	УзелДокФорм	= ДобавитьУзелXML(УзелДокНапрНО, "ДокФорм");
	
	ДобавитьУзелXML(УзелДокФорм, "ТипФайл", Истина, 1);
	
	ИдФайл = Сред(ИдФайл, 1, СтрДлина(ИдФайл) - 4); //без расширения и точки
	ДобавитьУзелXML(УзелДокФорм, "ИдФайл", Истина, ИдФайл);
	ДобавитьУзелXML(УзелДокФорм, "ИдФайлЭП", Ложь, ИдФайлЭП);
	ДобавитьУзелXML(УзелДокФорм, "КНД_Док", Ложь, КНД_Док);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ДокФорм_ОпределитьТипФайл(ИмяФайлаДанных, СвойстваЭлементаЭД)

	Расширение = РасширениеФайла(ИмяФайлаДанных);
	Если ВРег(Расширение) = "XML" Тогда
		СвойстваЭлементаЭД.Вставить("ТипФайл", 1);
	Иначе
		// 2 - PDF (стандарт PDF/A-3)
		СвойстваЭлементаЭД.Вставить("ТипФайл", 2);
	КонецЕсли;
				
КонецПроцедуры

&НаСервереБезКонтекста
Функция ДобавитьФайлВыгрузкиНеДокФорм(УзелДокНапрНО, ДанныеФайловВыгрузки)
	
	// Пишем ДокСкан
	Для каждого Строка Из ДанныеФайловВыгрузки.ТаблицаНеДокФорм Цикл
		
		Результат = ДобавитьВыгрузкуДокСкан(УзелДокНапрНО, Строка);
		
		Если Не Результат Тогда
			Возврат Ложь;
		КонецЕсли;

	КонецЦикла;
	
	// Пишем ДокPDF
	Для каждого Строка Из ДанныеФайловВыгрузки.ТаблицаНеДокФорм Цикл
		
		Результат = ДобавитьВыгрузкуДокPDF(УзелДокНапрНО, Строка);
		
		Если Не Результат Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЦикла;
	
	// Пишем ДокТабл
	Для Каждого Строка Из ДанныеФайловВыгрузки.ТаблицаНеДокФорм Цикл
		
		Результат = ДобавитьВыгрузкуДокТабл(УзелДокНапрНО, Строка);
		
		Если Не Результат Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЦикла;
		
	Возврат Истина;
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВыгрузкуДокСкан(УзелДокНапрНО, Строка)
	
	Результат = ПодготовитьМассивФайловНеДокФорм(УзелДокНапрНО, Строка, Ложь);
	Если НЕ Результат.Выполнено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Результат.МассивФайлов.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	Узел = ДобавитьУзелXML(УзелДокНапрНО, "ДокСкан");
	ДобавитьВыгрузкуНеДокФорм(Узел, Строка, Ложь);
	
	Для каждого ДанныеФайла Из Результат.МассивФайлов Цикл
		ДобавитьУзелXML(Узел, "ИмяФайл", Ложь, ДанныеФайла.ИмяФайла);
	КонецЦикла;
	
	Возврат Истина;

КонецФункции

&НаСервереБезКонтекста
Функция ПодготовитьМассивФайловНеДокФорм(УзелДокНапрНО, Строка, ЭтоДобавлениеДокPDF = Ложь, ЭтоДобавлениеДокТабл = Ложь)
	
	Если Строка.МассивФайлов.Количество() = 0 Тогда
	
		ТекстСообщения = СтрШаблон(НСтр("ru='У сканированного документа %1 отсутствуют прикрепленные файлы!'"),
			Строка.Документ);
			
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		Возврат ДокументооборотСКОКлиентСервер.НеВыполнено();
	
	КонецЕсли;
	
	ДокPDF = Перечисления.ВидыPDF.ДокPDF;
	
	МассивФайлов = Новый Массив;
	
	Для Каждого Файл Из Строка.МассивФайлов Цикл
		
		ЭтоДокPDF  = Файл.ВидPDF = ДокPDF;
		ЭтоДокТабл = ВРег(Файл.Расширение) = "ODS";
		
		// Тип "ДокСкан" для файлов в форматах "JPG, TIF, PNG" или для файлов в формате "PNG" с видом "Скан-копия в формате PDF" (ДокСкан)
		// Тип "ДокPDF" для файлов в формате "PNG" с видом "Неотсканированные PDF-файлы" (ДокPDF)
		// Тип "ДокТабл" для файлов в формате "ODS"
		Если ЭтоДобавлениеДокPDF Тогда
			
			Если ЭтоДокPDF Тогда
				МассивФайлов.Добавить(Файл);
			КонецЕсли;
			
		ИначеЕсли ЭтоДобавлениеДокТабл Тогда
			
			Если ЭтоДокТабл Тогда
				МассивФайлов.Добавить(Файл);
			КонецЕсли;
					
		ИначеЕсли Не ЭтоДокPDF
			И Не ЭтоДокТабл Тогда
			
			МассивФайлов.Добавить(Файл);
			
		КонецЕсли;
	
	КонецЦикла;
	
	Результат = ДокументооборотСКОКлиентСервер.Выполнено();
	Результат.Вставить("МассивФайлов", МассивФайлов);
	
	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВыгрузкуДокPDF(УзелДокНапрНО, Строка)
	
	Результат = ПодготовитьМассивФайловНеДокФорм(УзелДокНапрНО, Строка, Истина);
	Если НЕ Результат.Выполнено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Результат.МассивФайлов.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;

	Для каждого ДанныеФайла Из Результат.МассивФайлов Цикл
		
		ТипЭП = ?(ДанныеФайла.ЕстьВстроеннаяПодпись, "2", "1");
		
		Узел = ДобавитьУзелXML(УзелДокНапрНО, "ДокPDF");
		ДобавитьВыгрузкуНеДокФорм(Узел, Строка, Истина, ТипЭП);
		ДобавитьУзелXML(Узел, "ИмяФайл", Ложь, ДанныеФайла.ИмяФайла);
		
	КонецЦикла;
	
	Возврат Истина;

КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВыгрузкуНеДокФорм(Узел, Строка, ЭтоДобавлениеДокPDF, ТипЭП = "1")
	
	ЗапрашиваемыеРеквизиты = "Наименование, ВидДокумента, НомерОснования, ДатаОснования, СведенияДокумента, СведенияДокументаОснования";
	
	Если ЗначениеЗаполнено(Строка.Документ) Тогда
	РеквизитыДокумента = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Строка.Документ, ЗапрашиваемыеРеквизиты);
	Иначе
		РеквизитыДокумента = Новый Структура(ЗапрашиваемыеРеквизиты);
		РеквизитыДокумента.Наименование = НСтр("ru = 'Пояснение'");
	КонецЕсли;
			
	ДобавитьУзелXML(Узел, "КодДок", Истина, Строка.КодДок);
			
	Если НЕ Строка.ЭтоПакетСДопДокументами Тогда
		ДобавитьУзелXML(Узел, "ПорНомДок", Истина, Строка.ПорНом);
	КонецЕсли;
			
	НаимДок = "";
	Если ЗначениеЗаполнено(РеквизитыДокумента.СведенияДокумента) Тогда
		НаимДок = РеквизитыДокумента.СведенияДокумента;
	Иначе
		// Для обратной совместимости при добавлении сканов по старому формату 
		НаимДок = РеквизитыДокумента.Наименование;
	КонецЕсли;
	
	ДобавитьУзелXML(Узел, "НаимДок", Истина, НаимДок);
	
	Если ЭтоДобавлениеДокPDF Тогда
		ДобавитьУзелXML(Узел, "ТипЭП", Истина, ТипЭП);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Строка.ИдФайлЭП) Тогда
		ДобавитьУзелXML(Узел, "ИдФайлЭП", Истина, Строка.ИдФайлЭП);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(РеквизитыДокумента.СведенияДокументаОснования) Тогда
		СвДокОсн = РеквизитыДокумента.СведенияДокументаОснования;
		ДобавитьУзелXML(Узел, "СвДокОсн", Ложь, СвДокОсн);
	ИначеЕсли ЗначениеЗаполнено(РеквизитыДокумента.НомерОснования) Тогда
		// Для обратной совместимости при добавлении сканов по старому формату
		СвДокОсн = РеквизитыДокумента.НомерОснования + " от " + Формат(РеквизитыДокумента.ДатаОснования, "ДФ=dd.MM.yyyy");
		ДобавитьУзелXML(Узел, "СвДокОсн", Ложь, СвДокОсн);
	КонецЕсли;

КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьВыгрузкуДокТабл(УзелДокНапрНО, Строка)
	
	Результат = ПодготовитьМассивФайловНеДокФорм(УзелДокНапрНО, Строка, Ложь, Истина);
	
	Если Не Результат.Выполнено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Для Каждого ДанныеФайла Из Результат.МассивФайлов Цикл
		
		Узел = ДобавитьУзелXML(УзелДокНапрНО, "ДокТабл");
		
		ДобавитьВыгрузкуНеДокФорм(Узел, Строка, Ложь);
		
		ДобавитьУзелXML(Узел, "ИмяФайл", Ложь, ДанныеФайла.ИмяФайла);
		
	КонецЦикла;
	
	Возврат Истина;
			
КонецФункции

#КонецОбласти

#Область ОтправкаМакетовПенсионныхДел

&НаСервереБезКонтекста
Функция СформироватьПакетМакетаПенсионногоДелаИЗаявления(КонтекстОтправки)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетМакетаПенсионногоДелаИЗаявления(КонтекстОтправки);
	
КонецФункции

&НаКлиенте
Процедура ОтправкаМакетаПенсионногоДелаИЗаявления(Документ, Организация, ОповещениеОЗавершении) Экспорт
	
	Если НЕ ОбъектЗаполненКорректно(Документ) Тогда
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ОповещениеОЗавершении) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке();
		КонецЕсли;
		Возврат;
	КонецЕсли;

	Контекст = Новый Структура();
	Контекст.Вставить("Документ",              Документ);
	Контекст.Вставить("Организация",           Организация);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаМакетаПенсионногоДела_ПослеНастройкиЗаявления", 
		ЭтотОбъект, 
		Контекст);
		
	ЗавершитьНастройкуЗаявленияНаИзменение(Документ, ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОтправкаМакетаПенсионногоДела_ПослеНастройкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Документ = ВходящийКонтекст.Документ;
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаМакетаПенсионногоДелаПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ВходящийКонтекст);
		
	КонтекстДлительнойОперации = Новый Структура;
	КонтекстДлительнойОперации.Вставить("Документ", 	Документ);
	КонтекстДлительнойОперации.Вставить("Оповещение", 	Оповещение);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Документ;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 3;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка макета в ПФР 
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаМакетаПенсионногоДела",  0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаМакетаПенсионногоДела() Экспорт
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи..."); // ПФР
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Документ 	= КонтекстДлительнойОперации.Документ;
	Оповещение 	= КонтекстДлительнойОперации.Оповещение;
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Документ, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаМакетаПенсионногоДела_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Док 					= ВходящийКонтекст.Документ;
	Параметры_Организация 			= ВходящийКонтекст.Организация;
	Параметры_ВыполняемоеОповещение = ВходящийКонтекст.ОповещениеОЗавершении;
	
	ОтправкаМакетаПенсионногоДелаИЗаявления(Параметры_Док, Параметры_Организация, Параметры_ВыполняемоеОповещение);

КонецПроцедуры	
	
&НаКлиенте
Процедура ОтправкаМакетаПенсионногоДелаПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаМакетаПенсионногоДела_ПослеПроверкиСертификатаИЛицензии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.Документ, ВходящийКонтекст.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаМакетаПенсионногоДела_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("Документ", ВходящийКонтекст.Документ);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	
	// отражаем изменения в базе
	Если НЕ ОтправкаМакетаПенсионногоДела_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаМакетаПенсионногоДела_ПослеПроверкиЗаявления", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(ВходящийКонтекст.Документ, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			
			ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ВходящийКонтекст.Документ, ВходящийКонтекст.ОповещениеОЗавершении, ВходящийКонтекст.Документ);//+
			
		Иначе
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		ВходящийКонтекст.Документ);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Макеты пенсионных дел.
		КонецЕсли;

		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(ВходящийКонтекст.Документ) Тогда
			
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(ВходящийКонтекст.Документ, ВходящийКонтекст.ОповещениеОЗавершении, ВходящийКонтекст.Документ);//+
		Возврат;

	КонецЕсли;

	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаМакетаПенсионногоДелаПослеФормированияПакета", ЭтотОбъект, Контекст);
		
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется формирование пакета для отправки..."); // ПФР
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ПФР");

КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьВложенияЗаявленияОНазначенииПенсии(Документ) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Ссылка", Документ);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ЗаявлениеОНазначенииПенсииЭлектронныеДокументы.Ссылка КАК Ссылка,
	|	ЗаявлениеОНазначенииПенсииЭлектронныеДокументы.Документ КАК Документ,
	|	ЗаявлениеОНазначенииПенсииЭлектронныеДокументы.Файл КАК Файл
	|ИЗ
	|	Справочник.ЗаявлениеОНазначенииПенсии.ЭлектронныеДокументы КАК ЗаявлениеОНазначенииПенсииЭлектронныеДокументы
	|ГДЕ
	|	ЗаявлениеОНазначенииПенсииЭлектронныеДокументы.Ссылка = &Ссылка";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Таблица = Новый ТаблицаЗначений;
	Таблица.Колонки.Добавить("ИмяФайла", Новый ОписаниеТипов("Строка"));
	Таблица.Колонки.Добавить("Размер", Новый ОписаниеТипов("Число"));
	Таблица.Колонки.Добавить("Тип");
	Таблица.Колонки.Добавить("Данные");
	
	Пока Выборка.Следующий() Цикл
		
		ДополнительныеПараметры = РаботаСФайламиКлиентСервер.ПараметрыДанныхФайла();
		ДанныеФайла = РаботаСФайлами.ДанныеФайла(Выборка.Файл, ДополнительныеПараметры);
		НоваяСтрока = Таблица.Добавить();
		ИмяФайла    = СтрШаблон("%1.%2", ДанныеФайла.Наименование, ДанныеФайла.Расширение);
		НоваяСтрока.ИмяФайла = СтрЗаменить(НРег(ОбщегоНазначенияЭДКОКлиентСервер.ЗаменитьНечитаемыеСимволы(ИмяФайла)), " ", "_");
		НоваяСтрока.Размер = ДанныеФайла.Размер;
		НоваяСтрока.Данные = ДанныеФайла.СсылкаНаДвоичныеДанныеФайла;
		
	КонецЦикла;
	
	Возврат Таблица;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьВложенияМакетаПенсионногоДела(Документ) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Ссылка", Документ);
	Запрос.Текст =
	"ВЫБРАТЬ
	|	МакетыПенсионныхДелЭлектронныеДокументы.Документ,
	|	МакетыПенсионныхДелЭлектронныеДокументы.Страница1,
	|	МакетыПенсионныхДелЭлектронныеДокументы.Страница2,
	|	МакетыПенсионныхДелЭлектронныеДокументы.Размер,
	|	МакетыПенсионныхДелЭлектронныеДокументы.Файл,
	|	МакетыПенсионныхДелЭлектронныеДокументы.Файл.Расширение КАК ФайлРасширение
	|ИЗ
	|	Справочник.МакетыПенсионныхДел.ЭлектронныеДокументы КАК МакетыПенсионныхДелЭлектронныеДокументы
	|ГДЕ
	|	МакетыПенсионныхДелЭлектронныеДокументы.Ссылка = &Ссылка";
	
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Таблица = Новый ТаблицаЗначений;
	Таблица.Колонки.Добавить("ИмяФайла", Новый ОписаниеТипов("Строка"));
	Таблица.Колонки.Добавить("Размер", Новый ОписаниеТипов("Число"));
	Таблица.Колонки.Добавить("Тип");
	Таблица.Колонки.Добавить("Данные");
	
	Пока Выборка.Следующий() Цикл
		ДополнительныеПараметры = РаботаСФайламиКлиентСервер.ПараметрыДанныхФайла();
		ДанныеФайла = РаботаСФайлами.ДанныеФайла(Выборка.Файл, ДополнительныеПараметры);
		
		НоваяСтрока = Таблица.Добавить();
		Если ЗначениеЗаполнено(Выборка.Страница1) И ЗначениеЗаполнено(Выборка.Страница2) Тогда
			ИмяФайла = СтрШаблон("%1_страница_%3-%4.%2", Выборка.Документ, ДанныеФайла.Расширение, Выборка.Страница1, Выборка.Страница2);
		ИначеЕсли ЗначениеЗаполнено(Выборка.Страница1) ИЛИ ЗначениеЗаполнено(Выборка.Страница2) Тогда
			Страница = ?(ЗначениеЗаполнено(Выборка.Страница1), Выборка.Страница1, Выборка.Страница2);
			ИмяФайла = СтрШаблон("%1_страница_%3.%2", Выборка.Документ, ДанныеФайла.Расширение, Страница);
		Иначе
			ИмяФайла = СтрШаблон("%1.%2", Выборка.Документ, ДанныеФайла.Расширение);
		КонецЕсли;
		
		НоваяСтрока.ИмяФайла = СтрЗаменить(НРег(ОбщегоНазначенияЭДКОКлиентСервер.ЗаменитьНечитаемыеСимволы(ИмяФайла)), " ", "_");
		НоваяСтрока.Размер = ДанныеФайла.Размер;
		НоваяСтрока.Данные = ДанныеФайла.СсылкаНаДвоичныеДанныеФайла;
	КонецЦикла;
	
	Возврат Таблица;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаМакетаПенсионногоДела_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	Документ = Контекст.Документ;
	Если НЕ ЗначениеЗаполнено(Документ) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбран документ для отправки'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// проверяем, включен ли обмен по организации
	Организация = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Документ, "Организация");
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не выбрана организация-отправитель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	ИначеЕсли Организация.ВидОбменаСКонтролирующимиОрганами <> Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);

		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, предназначена ли эта учетная запись для документооборота с ПФР
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("ОтсутствуетУчетнаяЗапись", Истина);
		Возврат Ложь;
		
	ИначеЕсли ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗапись, "ПредназначенаДляДокументооборотаСПФР") <> Истина Тогда
		
		// Текст ошибки будет выведен в виде формы
		Контекст.Вставить("НаправлениеНеПодключено", Истина);
		Возврат Ложь;
		
	КонецЕсли;
	
	// проверяем, указан ли получатель
	ПолучательДокумента = Документ.Получатель;
	Если НЕ ЗначениеЗаполнено(ПолучательДокумента) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель письма.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
		
	Если ТипЗнч(Документ) = Тип("СправочникСсылка.ЗаявлениеОНазначенииПенсии") Тогда
		Приложения = ПолучитьВложенияЗаявленияОНазначенииПенсии(Документ);
	Иначе
		Приложения = ПолучитьВложенияМакетаПенсионногоДела(Документ);
	КонецЕсли;
	
	// получаем содержание письма
	ТекстСодержания = Документ.Содержание;
	
	// проверяем, не пустое ли письмо
	Если НЕ ЗначениеЗаполнено(ТекстСодержания) И Приложения.Количество() = 0 Тогда
		ТекстСообщения = НСтр("ru = 'В письме не задано содержание и отсутствуют вложения.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Ложь;
	КонецЕсли;
	
	// формируем файл с содержимым
	ИмяФайлаСодержимого = ВыгрузитьТекстВФайл(ТекстСодержания);
	КороткоеИмяФайлаСодержимого = СгенерироватьИмяФайлаДокументаВСоставеПакета();
	
	НачатьТранзакцию();
	
	// формируем цикл обмена
	ЦиклОбмена = Справочники.ЦиклыОбмена.СоздатьЭлемент();
	ЦиклОбменаСсылка = Справочники.ЦиклыОбмена.ПолучитьСсылку();
	ЦиклОбмена.УстановитьСсылкуНового(ЦиклОбменаСсылка);
	ЦиклОбмена.Вид = Перечисления.ВидыЦикловОбмена.ЦиклОбменаСПФР;
	ЦиклОбмена.Предмет = Документ;
	ЦиклОбмена.Идентификатор = Документ.Идентификатор;
	ЦиклОбмена.Организация = Организация;
	ЦиклОбмена.УчетнаяЗапись = УчетнаяЗапись;
	ЦиклОбмена.ВнешняяОрганизация = Документ.Получатель;
	ЦиклОбмена.Тип = Перечисления.ТипыЦикловОбмена.НеформализованнаяПерепискаПФРИсходящие;
	
	// формируем первичное транспортное сообщение
	ТранспортноеСообщение = Документы.ТранспортноеСообщение.СоздатьДокумент();
	ТранспортноеСообщение.ЦиклОбмена = ЦиклОбменаСсылка;
	ТранспортноеСообщение.Тип = Перечисления.ТипыТранспортныхСообщений.ПисьмоИсходящееПФР;
	ТранспортноеСообщение.Отправитель = Организация;
	ТранспортноеСообщение.Получатель = Документ.Получатель;
	ТранспортноеСообщение.УчетнаяЗапись = УчетнаяЗапись;
	ТранспортноеСообщение.Статус = Перечисления.СтатусыПисем.Исходящее;
	Попытка
		ТранспортноеСообщение.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		Возврат Ложь;
	КонецПопытки;
	
	Если НЕ Мультирежим.ЗаписатьВладельцевОбъекта1СОтчетности(
		ЦиклОбменаСсылка,
		УчетнаяЗапись,
		Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
		
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// ДОБАВЛЯЕМ ВЛОЖЕНИЯ
	
	// добавляем письмо
	Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Письмо, ИмяФайлаСодержимого, КороткоеИмяФайлаСодержимого, , , , , , , СгенерироватьUUID(), Перечисления.ТипыСодержимогоФайлов.Текст1251) Тогда
		ТекстСообщения = НСтр("ru = 'Отправка письма прервана.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ОтменитьТранзакцию();
		Возврат Ложь;
	КонецЕсли;
	
	// добавляем приложения к письму
	Для Каждого СтрПриложение Из Приложения Цикл
		Если НЕ ДобавитьСодержимоеТранспортногоКонтейнера(ТранспортноеСообщение.Ссылка, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПисьмоПриложение, СтрПриложение.Данные, СтрПриложение.ИмяФайла, СтрПриложение.Размер, , , , , , СгенерироватьUUID(), ТипСодержимогоПоИмениФайла(СтрПриложение.ИмяФайла, Истина)) Тогда
			ОтменитьТранзакцию();
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	// записываем цикл обмена
	Попытка
		ЦиклОбмена.Записать();
	Исключение
		ОтменитьТранзакцию();
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		Возврат Ложь;
	КонецПопытки;
	
	// изменяем статус отправки
	ЗаписатьСтатусОтправкиОбъекта(Документ, Перечисления.СтатусыОтправки.ВКонверте);
	
	ЗафиксироватьТранзакцию();
	
	Контекст.Вставить("Организация", Организация);
	Контекст.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	Контекст.Вставить("ТранспортноеСообщение", ТранспортноеСообщение.Ссылка);
	Контекст.Вставить("ТипТранспортногоСообщения", ТранспортноеСообщение.Тип);
	Контекст.Вставить("ЦиклОбмена", ЦиклОбмена.Ссылка);
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ОтправкаМакетаПенсионногоДелаПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Не Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Макеты пенсионных дел.

		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // Макеты пенсионных дел.
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		КонтекстОтправки.Документ);
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // Макеты пенсионных дел.
	
КонецПроцедуры
	 	
&НаСервереБезКонтекста
Функция ПолучитьПараметрыОтправкиМакетовПенсионныхДелИЗаявления(ЦиклОбмена)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьПараметрыОтправкиМакетовПенсионныхДелИЗаявления(ЦиклОбмена);
	
КонецФункции

&НаСервереБезКонтекста
Функция ЭтоМакетПД(ЦиклОбмена)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоМакетПД(ЦиклОбмена);
		
КонецФункции

&НаСервереБезКонтекста
Функция ЭтоЗаявлениеОНазначенииПенсии(ЦиклОбмена)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоЗаявлениеОНазначенииПенсии(ЦиклОбмена);
		
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура СоздатьЗапросНаСверку(Организация, ВидУслуги) Экспорт
	
	ЗначенияЗаполнения = Новый Структура();
	ЗначенияЗаполнения.Вставить("ВидУслуги", 	ВидУслуги);
	ЗначенияЗаполнения.Вставить("Организация", 	Организация);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
	
	Если ТипЗнч(ВидУслуги) = Тип("ПеречислениеСсылка.ВидыУслугПриИОН") Тогда
		// Это запрос ИОН
		ОткрытьФорму("Документ.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика.Форма.ФормаДокумента", ПараметрыФормы);
	Иначе
		// Это запрос ИОС
		ОткрытьФорму("Документ.ЗапросНаИнформационноеОбслуживаниеСтрахователя.Форма.ФормаДокумента", ПараметрыФормы);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ВыбратьКодОрганаТОГС(ДополнительныеПараметры) Экспорт
	
	ПутьКФорме = ПутьКОбъекту + ".Форма.СписокТОГС";
	
	Если ДополнительныеПараметры.Свойство("ОписаниеОповещения") Тогда
		ОписаниеОповещения = ДополнительныеПараметры.ОписаниеОповещения;
		// Нельзя сохранять оповещение иначе форма будет заблокирована.
		ДополнительныеПараметры.Удалить("ОписаниеОповещения");
	Иначе
		ОписаниеОповещения = Неопределено;
	КонецЕсли;
	
	Если ДополнительныеПараметры.Свойство("Элемент") Тогда
		Элемент = ДополнительныеПараметры.Элемент;
		ДополнительныеПараметры.Удалить("Элемент");
	Иначе
		Элемент = Неопределено;
	КонецЕсли;
	
	ОткрытьФорму(
		ПутьКФорме,
		ДополнительныеПараметры,
		Элемент,
		,
		,
		,
		ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуНаправления(
		Форма,
		Действие,
		ИмяТаблицыФормы,
		ДанныеОрганизации,
		ТолькоФНС,
		АдресПолучателей,
		ВыполняемоеОповещение = Неопределено) Экспорт
	
	ТаблицаФормы = Форма.Элементы[ИмяТаблицыФормы];
	Если ТаблицаФормы.ТекущиеДанные <> Неопределено
		И Действие = "Редактировать" Тогда
		ТипПолучателя 	= ТаблицаФормы.ТекущиеДанные.ТипПолучателя;
		КодПолучателя 	= ТаблицаФормы.ТекущиеДанные.КодПолучателя;
		КПП 			= ТаблицаФормы.ТекущиеДанные.КПП;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура("Действие, ДанныеОрганизации, ТипПолучателя, КодПолучателя, КПП");
	ПараметрыФормы.Вставить("Действие", 			Действие);
	ПараметрыФормы.Вставить("ДанныеОрганизации", 	ДанныеОрганизации);
	ПараметрыФормы.Вставить("ТолькоФНС", 			ТолькоФНС);
	ПараметрыФормы.Вставить("ТипПолучателя", 		ТипПолучателя);
	ПараметрыФормы.Вставить("КодПолучателя", 		КодПолучателя);
	ПараметрыФормы.Вставить("КПП", 					КПП);
	ПараметрыФормы.Вставить("АдресПолучателей", 	АдресПолучателей);
	ПараметрыФормы.Вставить("ИмяТаблицыФормы", 		ИмяТаблицыФормы);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Действие", 				Действие);
	ДополнительныеПараметры.Вставить("ИмяТаблицыФормы", 		ИмяТаблицыФормы);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьФормуНаправленияЗавершение", 
		Форма, 
		ДополнительныеПараметры);
	
	ОткрытьФормуВводаНаправления(
		Форма,
		ОписаниеОповещения,
		ПараметрыФормы);
		
КонецПроцедуры
	
&НаКлиенте
Процедура ОткрытьФормуНаправленияЗавершение(Форма, РедактируемаяТаблица, Результат, ВходящийКонтекст) Экспорт
	
	Действие 		= ВходящийКонтекст.Действие;
	ИмяТаблицыФормы = ВходящийКонтекст.ИмяТаблицыФормы;
	ТаблицаФормы 	= Форма.Элементы[ИмяТаблицыФормы];
	
	Если ЗначениеЗаполнено(Результат) Тогда
		
		Если Действие = "Добавить" Тогда
			
			НоваяСтрока = РедактируемаяТаблица.Добавить();
			НоваяСтрока.КодПолучателя 	= Результат.КодПолучателя;
			НоваяСтрока.КПП 			= Результат.КПП;
			НоваяСтрока.ТипПолучателя	= Результат.ТипПолучателя;
			
			ТаблицаФормы.Обновить();
			
		ИначеЕсли Действие = "Редактировать" Тогда
			
			ИдентификаторСтроки 		= ТаблицаФормы.ТекущаяСтрока;
			ТекущаяСтрока 				= РедактируемаяТаблица.НайтиПоИдентификатору(ИдентификаторСтроки);
			
			ТекущаяСтрока.КодПолучателя = Результат.КодПолучателя;
			ТекущаяСтрока.КПП 			= Результат.КПП;
			ТекущаяСтрока.ТипПолучателя	= Результат.ТипПолучателя;
			
			ТаблицаФормы.Обновить();
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ВходящийКонтекст.ВыполняемоеОповещение <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура УдалитьНаправление(
		Форма,
		РедактируемаяТаблица,
		ИмяТаблицыФормы,
		ИмяКнопки) Экспорт
	
	ТекущаяСтрока = Форма.Элементы[ИмяТаблицыФормы].ТекущаяСтрока;
	
	ТекущаяСтрокаВыбрана = ТекущаяСтрокаВыбрана(
		РедактируемаяТаблица,
		ТекущаяСтрока,
		"удаления");
	
	Если ТекущаяСтрокаВыбрана Тогда
			
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ТекущаяСтрока", 		ТекущаяСтрока);
		ДополнительныеПараметры.Вставить("ИмяТаблицыФормы", 	ИмяТаблицыФормы);
		ДополнительныеПараметры.Вставить("ИмяКнопки", 			ИмяКнопки);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"УдалитьНаправлениеЗавершение", 
			Форма, 
			ДополнительныеПараметры);
		
		ТекстВопроса = НСтр("ru = 'Удалить выбранное направление?'");
		
		ПоказатьВопрос(
			ОписаниеОповещения, 
			ТекстВопроса, 
			РежимДиалогаВопрос.ДаНет,
			,
			КодВозвратаДиалога.Да);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УдалитьНаправлениеЗавершение(Форма, РедактируемаяТаблица, РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	ТекущаяСтрока 		= ДополнительныеПараметры.ТекущаяСтрока;
	ИмяТаблицыФормы		= ДополнительныеПараметры.ИмяТаблицыФормы;
	ИмяКнопки 			= ДополнительныеПараметры.ИмяКнопки;
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		
		ЭлементКоллекции 		= РедактируемаяТаблица.НайтиПоИдентификатору(ТекущаяСтрока);
		ИндексЭлементаКоллекции = РедактируемаяТаблица.Индекс(ЭлементКоллекции);
		РедактируемаяТаблица.Удалить(ИндексЭлементаКоллекции);
		
		Форма.Элементы[ИмяКнопки].Доступность = РедактируемаяТаблица.Количество() <> 0;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НастройкаПечататьДокументЦиклаОбмена(Результат, НижнийКолонтитул, ЦиклОбмена) Экспорт
	
	ПредметыОбмена = ПолучитьПредметыЦиклаОбмена(ЦиклОбмена);
	
	Если ПредметыОбмена.Количество() = 0
		ИЛИ ТипЗнч(ПредметыОбмена[0]) <> Тип("ДокументСсылка.РегламентированныйОтчет") Тогда
		
		НастройкаПечататьДокумент(Результат, НижнийКолонтитул);
		
		Возврат;
	КонецЕсли;
	
	ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетности"));
	Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл 
		
		ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
		
		// Формируем табличный документ, содержащий только штампы подписей.
		ТабДокЭЦП = СформироватьТабличныйДокументНаОсновеБланка("Печатная форма", ФайлОтчетности.ИмяФайла, "", "");
		ПакетДокументов = РегламентированнаяОтчетностьКлиент.СформироватьПечатныйБланкРегламентированногоОтчетаСЭЦП(
			ПредметыОбмена[0], ТабДокЭЦП, УникальныйИдентификатор);
		
		Если ТипЗнч(ПакетДокументов) <> Тип("Структура") Тогда
			НастройкаПечататьДокумент(Результат, НижнийКолонтитул);
			Возврат;
		КонецЕсли;
		
		//РегламентированнаяОтчетностьКлиент.ПоказатьПечатныйБланкРегламентированногоОтчетаPDFcЭЦП(ПредметыОбмена[0], ТабДокЭЦП);
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
		
		Для Каждого ЭлементСостава Из ПакетДокументов.Состав Цикл
			Если ЭтоАдресВременногоХранилища(ЭлементСостава.Данные) Тогда
				
				ПараметрыПечати = КонструкторПечатиПротокола();
				ПараметрыПечати.ТабДок = ПолучитьИзВременногоХранилища(ЭлементСостава.Данные);
				ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
				
				Результат.Добавить(ПараметрыПечати, Представление + "." + Символы.ПС + ЭлементСостава.Наименование);
				
			КонецЕсли;
		КонецЦикла;
		
		Прервать; // используем ЭЦП первого файла отчетности.
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура НастройкаПечататьДокумент(Результат, НижнийКолонтитул)

	ФайлыОтчетности = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ФайлОтчетности"));
	Для Каждого ФайлОтчетности Из ФайлыОтчетности Цикл 
		
		//	Кодировка = ?(нрег(Прав(ФайлОтчетности.ИмяФайла, 4)) = ".xml", "windows-1251", "cp866");
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(ФайлОтчетности.Файл,, Истина);	// Фиксированная Кодировка заменена на авто
		ПолучитьЭЦП(Файлы.Индекс(ФайлОтчетности));
		
		Представление   = ПредставлениеТранспортногоСообщнияПоТипу(ФайлОтчетности.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок = СформироватьТабличныйДокументНаОсновеБланка(
			"Электронное представление отчета", 
			ФайлОтчетности.ИмяФайла, 
			Тело, 
			НижнийКолонтитул);
			
		ПараметрыПечати.Отправка = ФайлОтчетности.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
			
	КонецЦикла;

КонецПроцедуры

&НаКлиенте
Функция КонструкторПечатиПротокола()
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ТабДок",   Новый ТабличныйДокумент);
	ДополнительныеПараметры.Вставить("Отправка", Неопределено);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

&НаСервереБезКонтекста
Функция КНДФайла(XMLФайл)
	
	Если НЕ ЗначениеЗаполнено(XMLФайл) Тогда
		Возврат Неопределено;
	КонецЕсли;

	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , XMLФайл, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелКНД = УзелДокумент.Строки.Найти("КНД", "Имя");
	Если ЗначениеЗаполнено(УзелКНД) Тогда
		Возврат УзелКНД.Значение;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриемеНалоговойДекларации(Заголовок, ИмяФайла, Тело, НижнийКолонтитул) Экспорт
	
	СвойстваИзУведомления = ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриемеНалоговойДекларации(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	ТабДокумент.АвтоМасштаб = Истина;
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзУведомления) Тогда
		Макет = ПолучитьМакетОбработки("УведомлениеОбОтказе");
		ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбОтказеШапка");
		ОбластьМакета.Параметры.НалоговыйОрган = СвойстваИзУведомления.ОбщиеСведенияНалоговыйОрган.НаимНО +" (код "+СвойстваИзУведомления.ОбщиеСведенияНалоговыйОрган.КодНО+")" ;
		ОбластьМакета.Параметры.ИмяФайла = ВРег(СвойстваИзУведомления.ОбщиеСведения.ИмяОбрабФайла);
		ОбластьМакета.Параметры.Налогоплательщик = СвойстваИзУведомления.ПредставлениеНП;
		ОбластьМакета.Параметры.Отправитель = СвойстваИзУведомления.ПредставлениеОтпр;
		ОбластьМакета.Параметры.Дата = СвойстваИзУведомления.ОбщиеСведения.ДатаПредст;
		ОбластьМакета.Параметры.Время = СтрЗаменить(СвойстваИзУведомления.ОбщиеСведения.ВремПредст,".",":");
		ОбластьМакета.Параметры.ДатаПриема = СвойстваИзУведомления.ОбщиеСведения.ДатаПрием;
		ОбластьМакета.Параметры.НаименованиеДекларации = СформироватьТекстовуюИнформациюОДекларации(СвойстваИзУведомления.ОбщиеСведения);
		
		ТабДокумент.Вывести(ОбластьМакета); // УведомлениеОбОтказеШапка
		
		Для Каждого Ошибка Из СвойстваИзУведомления.ТаблицаОшибокОбщийПеречень Цикл
			ОбластьМакета = Макет.ПолучитьОбласть("ОшибкиОбщие");
			ТекстОшибки = "Код: " + Ошибка.КодОшибки + " Описание:" + Ошибка.Описание;
			ОбластьМакета.Параметры.ОшибкаОбщая = ТекстОшибки;
			ТабДокумент.Вывести(ОбластьМакета);
		КонецЦикла;
		
		Если СвойстваИзУведомления.ТаблицаОшибокПоФайлам.Количество()>0 Тогда
			
			ОбластьМакета = Макет.ПолучитьОбласть("ОшибкиФайлШапка");
			ТабДокумент.Вывести(ОбластьМакета);
			
			ИмяФайла = "";
			ВыводимыйПоКаждомуФайлуТекст = "";
			
			Для Каждого Ошибка Из СвойстваИзУведомления.ТаблицаОшибокПоФайлам Цикл
				
				ТекстОшибки = 
					"Код: " + ?(ПустаяСтрока(Ошибка.КодОшибки),"-",Ошибка.КодОшибки) + Символы.ПС + 
					"Описание: " + ?(ПустаяСтрока(Ошибка.Описание),"-",Ошибка.Описание) + 
					?(ПустаяСтрока(Ошибка.ИдОш),"", Символы.ПС + "Доп. сведения: " + Строка(Ошибка.ИдОш)) + Символы.ПС;
								
				Если Ошибка.ИмяФайла = ИмяФайла Тогда
					
					ВыводимыйПоКаждомуФайлуТекст = ВыводимыйПоКаждомуФайлуТекст + Символы.ПС + ТекстОшибки;
					
				Иначе
					
					Если НЕ ПустаяСтрока(ВыводимыйПоКаждомуФайлуТекст) Тогда
						ОбластьМакета = Макет.ПолучитьОбласть("ОшибкиФайл");
						ОбластьМакета.Параметры.ОшибкаФайл = ВыводимыйПоКаждомуФайлуТекст;
						ТабДокумент.Вывести(ОбластьМакета);
					КонецЕсли;
					
					ИмяФайла = Ошибка.ИмяФайла;
					ВыводимыйПоКаждомуФайлуТекст = "В файле " + ИмяФайла + " обнаружены ошибки:" + Символы.ПС + Символы.ПС + ТекстОшибки;
				КонецЕсли;
				
			КонецЦикла;
			
			Если НЕ ПустаяСтрока(ВыводимыйПоКаждомуФайлуТекст) Тогда
				ОбластьМакета = Макет.ПолучитьОбласть("ОшибкиФайл");
				ОбластьМакета.Параметры.ОшибкаФайл = ВыводимыйПоКаждомуФайлуТекст;
				ТабДокумент.Вывести(ОбластьМакета); 
			КонецЕсли;
			
		КонецЕсли;
		
		ОбластьМакета = Макет.ПолучитьОбласть("ВторойБлокОшибок");
		Если СвойстваИзУведомления.ТаблицаОшибокОбщийПеречень.Количество() > 0
			ИЛИ СвойстваИзУведомления.ТаблицаОшибокПоФайлам.Количество() > 0 Тогда
			ОбластьМакета.Параметры.НеПринимается = "V";
		КонецЕсли;
		ТабДокумент.Вывести(ОбластьМакета);
		
		УказанПереченьСведений = СвойстваИзУведомления.ПереченьОснований.Количество() > 0;
		
		Если УказанПереченьСведений Тогда
			Для каждого Основание Из СвойстваИзУведомления.ПереченьОснований Цикл
				ОбластьМакета = Макет.ПолучитьОбласть("ПереченьОснований");
				ОбластьМакета.Параметры.ТекстОшибки = Основание;
				ТабДокумент.Вывести(ОбластьМакета);
			КонецЦикла;
		Иначе
			ОбластьМакета = Макет.ПолучитьОбласть("ПереченьОснований");
			ОбластьМакета.Параметры.ТекстОшибки = "-";
			ТабДокумент.Вывести(ОбластьМакета);
		КонецЕсли;
		
		ОбластьМакета = Макет.ПолучитьОбласть("УведомлениеОбОтказеПодвал");
		
		Если УказанПереченьСведений Тогда
			ОбластьМакета.Параметры.СчитаетсяНепредставленным = "V";
		КонецЕсли;
		
		ТабДокумент.Вывести(ОбластьМакета);
		
		// Перечень сведений к уведомлению о том, что расчёт считается непредставленным
		НапечататьПереченьСведенийУведомления(ТабДокумент, Макет, СвойстваИзУведомления);
		
		НапечататьДоходыИНалогиФЛУведомления(ТабДокумент, Макет, СвойстваИзУведомления);
		
		// выводим информацию о подписях
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
	Иначе
		// если разобрать не удалось - выводим его в виде текста
		
		// получаем бланк отчета из макета
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Процедура НапечататьПереченьСведенийУведомления(ТабДокумент, Макет, СвойстваИзУведомления)
	
	// Перечень сведений к уведомлению о том, что расчёт считается непредставленным
	Если СвойстваИзУведомления.ТаблицаПереченьСведений.Количество() > 0 Тогда
		
		ТабДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
		ОбластьМакета = Макет.ПолучитьОбласть("ШапкаПеречняСведений");
		ТабДокумент.Вывести(ОбластьМакета);
		
		Для каждого СтрокаПеречня Из СвойстваИзУведомления.ТаблицаПереченьСведений Цикл
			ОбластьМакета = Макет.ПолучитьОбласть("СтрокаСведений");
			ЗаполнитьЗначенияСвойств(ОбластьМакета.Параметры, СтрокаПеречня); 
			ТабДокумент.Вывести(ОбластьМакета);
		КонецЦикла; 
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура НапечататьДоходыИНалогиФЛУведомления(ТабДокумент, Макет, СвойстваИзУведомления)
	
	// Перечень ошибок сведений о доходах и суммах налога физического лица уведомления
	Если СвойстваИзУведомления.ТаблицаДоходыИНалогиФЛ.Количество() > 0 Тогда
		
		ТабДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
		ОбластьМакета = Макет.ПолучитьОбласть("ШапкаДоходыИНалогиФЛ");
		ТабДокумент.Вывести(ОбластьМакета);
		
		Для каждого СтрокаДоходыИНалогиФЛ Из СвойстваИзУведомления.ТаблицаДоходыИНалогиФЛ Цикл
			ОбластьМакета = Макет.ПолучитьОбласть("СтрокаДоходыИНалогиФЛ");
			ЗаполнитьЗначенияСвойств(ОбластьМакета.Параметры, СтрокаДоходыИНалогиФЛ); 
			ТабДокумент.Вывести(ОбластьМакета);
		КонецЦикла; 
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриеме(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзУведомления = ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриеме(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзУведомления) Тогда
		
		Бланк 		= ПолучитьМакетОбработки("УведомлениеОбОтказеВПриеме");
		ТабДокумент.АвтоМасштаб = Истина;
		ОсновнаяЧастьБланка = Бланк.ПолучитьОбласть("Уведомление");
		ЗаполнитьЗначенияСвойств(ОсновнаяЧастьБланка.Параметры, СвойстваИзУведомления);
		ТабДокумент.Вывести(ОсновнаяЧастьБланка);
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Бланк, ТаблицаПодписей);
		
	Иначе
		// если разобрать не удалось - выводим его в виде текста
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеКвитанцииОПриемеНалоговойДекларации(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзУведомления = ПолучитьОсновныеСвойстваИзКвитанцииОПриемеНалоговойДекларации(Тело);
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзУведомления) Тогда
		
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		ТабДокумент = КонтекстЭДОСервер.ТабДокументКвитанцияОПриеме(СвойстваИзУведомления, ТаблицаПодписей); 
		
	Иначе
		
		// если разобрать не удалось - выводим его в виде текста
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
	
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСведенияИзТэгаСвКвитКвитанцииОПриеме(УзелДокумент)
	
	ДатаПрин       = "";
	НаимВидДок     = "";
	ДатаНапр       = "";
	ВремНапр       = "";
	ДатаПост       = "";
	РегНом         = ""; // необязательный.
	ИмяПринятФайла = ""; // множественный тэг.
	
	УзелСвКвит = ПолучитьУзел(УзелДокумент, "СвКвит");
	Если НЕ ЗначениеЗаполнено(УзелСвКвит) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДатаПрин = ПолучитьУзел(УзелСвКвит, "ДатаПрин");
	Если НЕ ЗначениеЗаполнено(УзелДатаПрин) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаПрин = УзелДатаПрин.Значение;
	
	УзелНаимВидДок = ПолучитьУзел(УзелСвКвит, "НаимВидДок");
	Если НЕ ЗначениеЗаполнено(УзелНаимВидДок) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НаимВидДок = УзелНаимВидДок.Значение;
	
	УзелДатаНапр = ПолучитьУзел(УзелСвКвит, "ДатаНапр");
	Если НЕ ЗначениеЗаполнено(УзелДатаНапр) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаНапр = УзелДатаНапр.Значение;
	
	УзелВремНапр = ПолучитьУзел(УзелСвКвит, "ВремНапр");
	Если НЕ ЗначениеЗаполнено(УзелВремНапр) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ВремНапр = УзелВремНапр.Значение;
	
	УзелДатаПост = ПолучитьУзел(УзелСвКвит, "ДатаПост");
	Если НЕ ЗначениеЗаполнено(УзелДатаПост) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаПост = УзелДатаПост.Значение;
	
	УзелРегНом = ПолучитьУзел(УзелСвКвит, "РегНом", Ложь); // Необязательный.
	Если ЗначениеЗаполнено(УзелРегНом) Тогда
		РегНом = УзелРегНом.Значение;
	КонецЕсли;
	
	ИменаПринятФайла = Новый Массив; // Множественный.
	СтрокиИмяПринятФайла = УзелСвКвит.Строки.НайтиСтроки(Новый Структура("Имя", "ИмяПринятФайла"));
	Для Каждого СтрокаИмяПринятФайла Из СтрокиИмяПринятФайла Цикл
		ИменаПринятФайла.Добавить(СтрокаИмяПринятФайла.Значение);
	КонецЦикла;
	ИмяПринятФайла = СтрСоединить(ИменаПринятФайла, Символы.ПС);
	
	СвойстваКвитанции = Новый Структура();
	СвойстваКвитанции.Вставить("ДатаПрин",       ДатаПрин);
	СвойстваКвитанции.Вставить("НаимВидДок",     НаимВидДок);
	СвойстваКвитанции.Вставить("ДатаНапр",       ДатаНапр);
	СвойстваКвитанции.Вставить("ВремНапр",       ВремНапр);
	СвойстваКвитанции.Вставить("ДатаПост",       ДатаПост);
	СвойстваКвитанции.Вставить("РегНом",         РегНом);
	СвойстваКвитанции.Вставить("ИмяПринятФайла", ИмяПринятФайла);
	
	Возврат СвойстваКвитанции;
	
КонецФункции

&НаСервере
Функция ПолучитьОсновныеСвойстваИзКвитанцииОПриемеНалоговойДекларации(СтрКвитанция)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьОсновныеСвойстваИзКвитанцииОПриемеНалоговойДекларации(СтрКвитанция);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриемеНалоговойДекларации(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;

	ТаблицаОшибокОбщийПеречень = Новый ТаблицаЗначений;
	ТаблицаОшибокОбщийПеречень.Колонки.Добавить("КодОшибки");
	ТаблицаОшибокОбщийПеречень.Колонки.Добавить("Описание");
	
	ТаблицаОшибокПоФайлам = Новый ТаблицаЗначений;
	ТаблицаОшибокПоФайлам.Колонки.Добавить("ИмяФайла");
	ТаблицаОшибокПоФайлам.Колонки.Добавить("КодОшибки");
	ТаблицаОшибокПоФайлам.Колонки.Добавить("Описание");
	ТаблицаОшибокПоФайлам.Колонки.Добавить("ИдОш");
	
	ТаблицаПереченьСведений = Новый ТаблицаЗначений;
	ТаблицаПереченьСведений.Колонки.Добавить("СНИЛС");
	ТаблицаПереченьСведений.Колонки.Добавить("ИНН");
	ТаблицаПереченьСведений.Колонки.Добавить("ФИО");
	ТаблицаПереченьСведений.Колонки.Добавить("ТекстОшибки");
	
	ТаблицаДоходыИНалогиФЛ = Новый ТаблицаЗначений;
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("НомерСправки");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("НомерКорректировки");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("ОшибочныйЭлемент");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("ФИОФизическогоЛица");
	ТаблицаДоходыИНалогиФЛ.Колонки.Добавить("ТекстОшибкиВДоходахИНалогахФЛ");
	
	ПереченьОснований = Новый Массив;
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML уведомления об отказе:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСведУведУточ = УзелДокумент.Строки.Найти("СведУведУточ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСведУведУточ) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""СведУведУточ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с перечнем выявленных нарушений
	УзлыВыявлНарФайл = УзелСведУведУточ.Строки.НайтиСтроки(Новый Структура("Имя", "ВыявлНарФайл"));
	Для Каждого УзелВыявНарФайл Из УзлыВыявлНарФайл Цикл
		
		УзлыПеречВыявНар = УзелВыявНарФайл.Строки.НайтиСтроки(Новый Структура("Имя", "ПеречВыявНар"));
		ИмяФайла = УзелВыявНарФайл.Строки.Найти("ИмяОбрабФайла", "Имя").Значение;

		Для Каждого УзелПеречВыявНар Из УзлыПеречВыявНар Цикл
			
			УзелКодОш = УзелПеречВыявНар.Строки.Найти("КодОш", "Имя");
			УзелТекстОш = УзелПеречВыявНар.Строки.Найти("ТекстОш", "Имя");
			УзелИдОш = УзелПеречВыявНар.Строки.Найти("ИдОш", "Имя");
			
			Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш) ИЛИ ЗначениеЗаполнено(УзелИдОш) Тогда
				
				КодОшибки = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "-");
				ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "-");
				ИдОш = ?(ЗначениеЗаполнено(УзелИдОш), СокрЛП(УзелИдОш.Значение), "-");
				
				НовСтр = ТаблицаОшибокПоФайлам.Добавить();
				НовСтр.КодОшибки = КодОшибки;
				НовСтр.Описание  = ТекстОшибки;
				НовСтр.ИмяФайла  = ИмяФайла;
				НовСтр.ИдОш 	 = ИдОш;
				
			КонецЕсли;
			
		КонецЦикла;
		
		ПолучитьСвойстваУзлаСвИдФЛ(УзелВыявНарФайл, ТаблицаПереченьСведений);
		
		ПолучитьСвойстваУзлаСвДохНалФЛ(УзелВыявНарФайл, ТаблицаДоходыИНалогиФЛ);
		
	КонецЦикла;
	
	ТаблицаПереченьОснований = ТаблицаПереченьСведений.Скопировать();
	ТаблицаПереченьОснований.Свернуть("ТекстОшибки");
	ПереченьОснований = ТаблицаПереченьСведений.ВыгрузитьКолонку("ТекстОшибки");
	
	//ошибки
	УзлыПеречВыявНар = УзелСведУведУточ.Строки.НайтиСтроки(Новый Структура("Имя", "ПеречВыявНар"));
	Для Каждого УзелПеречВыявНар Из УзлыПеречВыявНар Цикл
		
		УзелКодОш = УзелПеречВыявНар.Строки.Найти("КодОш", "Имя");
		УзелТекстОш = УзелПеречВыявНар.Строки.Найти("ТекстОш", "Имя");
					
		Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш)Тогда
			
			КодОшибки = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "-");
			ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "-");
			
			Если ТаблицаОшибокОбщийПеречень.Найти(ТекстОшибки,"Описание") = Неопределено Тогда
				
				НовСтр = ТаблицаОшибокОбщийПеречень.Добавить();
				НовСтр.КодОшибки = КодОшибки;
				НовСтр.Описание = ТекстОшибки;
				
			КонецЕсли;
		КонецЕсли;
		
	КонецЦикла;
	
	// Налоговый орган
	ОбщиеСведенияНалоговыйОрган = Новый Структура;
	Если УзелДокумент.Строки.Количество() > 0 Тогда
		Для Каждого УзелНО Из УзелДокумент.Строки Цикл
			ОбщиеСведенияНалоговыйОрган.Вставить(УзелНО.Имя, СокрЛП(УзелНО.Значение));
		КонецЦикла;
	КонецЕсли;
	
	// Сведения об отправителе отчетности
	ПредставлениеОтпр = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвОтпр", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеОтпр = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	// Сведения о налогоплательщике
	ПредставлениеНП = "";
	ДанныеОтправителя = УзелДокумент.Строки.Найти("СвНП", "Имя");
	ОтпрФЛ = ДанныеОтправителя.Строки.Найти("НПФЛ", "Имя");
	ОтпрЮЛ = ДанныеОтправителя.Строки.Найти("НПЮЛ", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		ПредставлениеНП = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	УзлыОбщСвУвед = УзелСведУведУточ.Строки.НайтиСтроки(Новый Структура("Имя", "ОбщСвУвед"));
	Если УзлыОбщСвУвед.Количество() > 0 Тогда
		УзелОбщСвУвед = УзлыОбщСвУвед[0];
		Для Каждого УзелОбщСвед Из УзелОбщСвУвед.Строки Цикл
			
			Если ОбщиеСведения.Свойство(УзелОбщСвед.Имя) Тогда
				
				// Если несколько значений с одним ключем, объединяем их.
				// Может быть у узла ИмяОбрабФайла
				ПредыдущееЗначение 	= Строка(ОбщиеСведения[УзелОбщСвед.Имя]);
				НовоеЗначение 		= СокрЛП(Строка(УзелОбщСвед.Значение));
				
				ОбщиеСведения.Вставить(УзелОбщСвед.Имя, ПредыдущееЗначение + ", " + НовоеЗначение);
				
			Иначе
				
				ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
				
			КонецЕсли;
			
		КонецЦикла;
	КонецЕсли;
	
	СвойстваУведомления = Новый Структура();
	СвойстваУведомления.Вставить("ТаблицаОшибокОбщийПеречень",  ТаблицаОшибокОбщийПеречень);
	СвойстваУведомления.Вставить("ТаблицаОшибокПоФайлам",       ТаблицаОшибокПоФайлам);
	СвойстваУведомления.Вставить("ТаблицаПереченьСведений",   	ТаблицаПереченьСведений);
	СвойстваУведомления.Вставить("ПереченьОснований",   		ПереченьОснований);
	СвойстваУведомления.Вставить("ОбщиеСведения",               ОбщиеСведения);
	СвойстваУведомления.Вставить("ОбщиеСведенияНалоговыйОрган", ОбщиеСведенияНалоговыйОрган);
	СвойстваУведомления.Вставить("ПредставлениеНП",             ПредставлениеНП);
	СвойстваУведомления.Вставить("ПредставлениеОтпр",           ПредставлениеОтпр);
	СвойстваУведомления.Вставить("ТаблицаДоходыИНалогиФЛ", 		ТаблицаДоходыИНалогиФЛ);
	
	Возврат СвойстваУведомления;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ПолучитьСвойстваУзлаСвИдФЛ(УзелВыявНарФайл, ТаблицаПереченьСведений)
	
	УзлыСвИдФЛ = УзелВыявНарФайл.Строки.НайтиСтроки(Новый Структура("Имя", "СвИдФЛ"));
	Для Каждого УзелСвИдФЛ Из УзлыСвИдФЛ Цикл
		УзелДанИдФЛ_НП = УзелСвИдФЛ.Строки[0].Строки;
		
		СНИЛС = УзелДанИдФЛ_НП.Найти("СНИЛС").Значение; // обязательный
		
		УзелИННФЛ = УзелДанИдФЛ_НП.Найти("ИННФЛ"); // необязательный
		Если УзелИННФЛ = Неопределено Тогда
			ИННФЛ = "";
		Иначе
			ИННФЛ = УзелИННФЛ.Значение;
		КонецЕсли;
		
		УзелФИО = УзелДанИдФЛ_НП.Найти("ФИО").Строки;
		
		УзелФамилия = УзелФИО.Найти("Фамилия");
		Фамилия = ?(УзелФамилия = Неопределено, "", УзелФамилия.Значение);
		УзелИмя = УзелФИО.Найти("Имя");
		Имя = ?(УзелИмя = Неопределено, "", УзелИмя.Значение);
		УзелОтчество = УзелФИО.Найти("Отчество");
		Отчество = ?(УзелОтчество = Неопределено, "", УзелОтчество.Значение);
		ФамилияИмя = СокрЛП(Фамилия + " " + Имя);
		ФамилияИмяОтчество = СокрЛП(ФамилияИмя + " " + Отчество);
		
		УзлыЗначОшЭл = УзелДанИдФЛ_НП.НайтиСтроки(Новый Структура("Имя", "ЗначОшЭл"));
		Если УзлыЗначОшЭл.Количество() = 0 Тогда
			УзелЗначОшЭл = Новый Структура("Значение", "");
			УзлыЗначОшЭл = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(УзелЗначОшЭл);
		КонецЕсли;
		Для каждого УзелЗначОшЭл Из УзлыЗначОшЭл Цикл
			НоваяСтрока = ТаблицаПереченьСведений.Добавить();
			НоваяСтрока.СНИЛС 		= СНИЛС;
			НоваяСтрока.ИНН 		= ИННФЛ;
			НоваяСтрока.ФИО 		= ФамилияИмяОтчество;
			НоваяСтрока.ТекстОшибки = УзелЗначОшЭл.Значение;
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ПолучитьСвойстваУзлаСвДохНалФЛ(УзелВыявНарФайл, ТаблицаПереченьСведений)
	
	УзлыСвДохНалФЛ = УзелВыявНарФайл.Строки.НайтиСтроки(Новый Структура("Имя", "СвДохНалФЛ"));
	Для Каждого УзелСвДохНалФЛ Из УзлыСвДохНалФЛ Цикл
		НомСпр = УзелСвДохНалФЛ.Строки.Найти("НомСпр").Значение; // обязательный
		
		НомКор = УзелСвДохНалФЛ.Строки.Найти("НомКор").Значение; // обязательный
		
		КодРекв = УзелСвДохНалФЛ.Строки.Найти("КодРекв").Значение; // обязательный
		
		УзелФИО = УзелСвДохНалФЛ.Строки.Найти("ФИО");
		
		Фамилия = УзелФИО.Строки.Найти("Фамилия").Значение;
		Имя 	= УзелФИО.Строки.Найти("Имя").Значение;
		
		УзелОтчество = УзелФИО.Строки.Найти("Отчество"); // необязательный
		Если УзелОтчество = Неопределено Тогда 
			Отчество = "";
		Иначе
			Отчество = УзелОтчество.Значение;
		КонецЕсли;
		
		УзлыОпОш = УзелСвДохНалФЛ.Строки.НайтиСтроки(Новый Структура("Имя", "ОпОш"));
		Если УзлыОпОш.Количество() = 0 Тогда
			УзелОпОш = Новый Структура("Значение", "");
			УзлыОпОш = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(УзелОпОш);
		КонецЕсли;
		Для каждого УзелОпОш Из УзлыОпОш Цикл
			НоваяСтрока = ТаблицаПереченьСведений.Добавить();
			НоваяСтрока.НомерСправки 					= НомСпр;
			НоваяСтрока.НомерКорректировки 				= НомКор;
			НоваяСтрока.ОшибочныйЭлемент 				= КодРекв;
			НоваяСтрока.ФИОФизическогоЛица 				= Фамилия + " " + Имя + " " + Отчество;
			НоваяСтрока.ТекстОшибкиВДоходахИНалогахФЛ 	= СокрЛП(УзелОпОш.Значение);
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриеме(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Получатель    = "";
	Отправитель   = "";
	НОПром        = "";
	ДатаНапр      = "";
	ВремНапр      = "";
	ДатаПост      = "";
	ИмяОбрабФайла = "";
	ПричиныОтказа = "";
	ФИОДолж       = "";

	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML уведомления об отказе:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ПолучитьУзел(ДеревоXML, "Файл");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = ПолучитьУзел(УзелФайл, "Документ");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПричиныОтказаИИмяФайла 	= ПолучитьПричинаОтказаИИмяФайлаИзУведомленияОбОтказеВПриеме(УзелДокумент);
	Если ЗначениеЗаполнено(ПричиныОтказаИИмяФайла) Тогда
		ПричиныОтказа 	= ПричиныОтказаИИмяФайла.ПричинаОтказа;
		ИмяОбрабФайла 	= ПричиныОтказаИИмяФайла.ИмяОбрабФайла;
	КонецЕсли;
	
	Получатель 		= ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, "СвПолДок");
	Отправитель 	= ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, "СвОтпрДок");
	НОПром 			= ПолучитьПромежуточныйНОИзУведомленияОбОтказеВПриеме(УзелДокумент);
	
	ДатаНапрИПост = ПолучитьДатуНапрИПостИзУведомленияОбОтказеВПриеме(УзелДокумент);
	Если ЗначениеЗаполнено(ДатаНапрИПост) Тогда
		ДатаНапр = ДатаНапрИПост.ДатаНапр;
		ВремНапр = ДатаНапрИПост.ВремНапр;
		ДатаПост = ДатаНапрИПост.ДатаПост;
	КонецЕсли;
	
	ФИОДолж = ПолучитьФИОДолжИзТэгаФИОДолж(УзелДокумент);
	
	СвойстваУведомления = Новый Структура();
	СвойстваУведомления.Вставить("Получатель",  	Получатель);
	СвойстваУведомления.Вставить("Отправитель", 	Отправитель);
	СвойстваУведомления.Вставить("НОПром", 			НОПром);
	СвойстваУведомления.Вставить("ДатаНапр", 		ДатаНапр);
	СвойстваУведомления.Вставить("ВремНапр", 		ВремНапр);
	СвойстваУведомления.Вставить("ДатаПост", 		ДатаПост);
	СвойстваУведомления.Вставить("ПричиныОтказа", 	ПричиныОтказа);
	СвойстваУведомления.Вставить("ИмяОбрабФайла", 	ИмяОбрабФайла);
	СвойстваУведомления.Вставить("ФИОДолж", 		ФИОДолж);
	
	Возврат СвойстваУведомления;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПромежуточныйНОИзУведомленияОбОтказеВПриеме(УзелДокумент)
	
	НОПром = "";
	
	УзелНОПром = ПолучитьУзел(УзелДокумент, "НОПром", Ложь);
	Если НЕ ЗначениеЗаполнено(УзелНОПром) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелПолНО = ПолучитьУзел(УзелНОПром, "ПолНО", Ложь);
	Если НЕ ЗначениеЗаполнено(УзелПолНО) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	НОПром = ПолучитьПредставлениеНалоговогоОрганаИзXML(УзелПолНО);
	
	Возврат НОПром;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьФИОДолжИзТэгаФИОДолж(УзелДокумент)
	
	ФИОДолж = "";
	
	УзелФИОДолж = ПолучитьУзел(УзелДокумент, "ФИОДолж");
	Если НЕ ЗначениеЗаполнено(УзелФИОДолж) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелФамилия 	= ПолучитьУзел(УзелФИОДолж, "Фамилия");
	УзелИмя 		= ПолучитьУзел(УзелФИОДолж, "Имя");
	УзелОтчество	= ПолучитьУзел(УзелФИОДолж, "Отчество", Ложь);
	
	ФИО = Новый Структура();
	ФИО.Вставить("Фамилия", 	СокрЛП(УзелФамилия.Значение));
	ФИО.Вставить("Имя", 		СокрЛП(УзелИмя.Значение));
	
	Если ЗначениеЗаполнено(УзелОтчество) Тогда
		ФИО.Вставить("Отчество", СокрЛП(УзелОтчество.Значение));
	Иначе
		ФИО.Вставить("Отчество", "");
	КонецЕсли;
		
	ФИОДолж = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(ФИО);
	
	Возврат ФИОДолж;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПричинаОтказаИИмяФайлаИзУведомленияОбОтказеВПриеме(УзелДокумент)
	
	ПричинаОтказа = "";
	ИмяОбрабФайла = "";
	
	УзелСведУведОтказ = ПолучитьУзел(УзелДокумент, "СведУведОтказ");
	Если НЕ ЗначениеЗаполнено(УзелСведУведОтказ) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелОбщСвУвед = ПолучитьУзел(УзелСведУведОтказ, "ОбщСвУвед");
	Если НЕ ЗначениеЗаполнено(УзелОбщСвУвед) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// Имя файла
	ИменаПринятФайла = Новый Массив;
	СтрокиИмяПринятФайла = УзелОбщСвУвед.Строки.НайтиСтроки(Новый Структура("Имя", "ИмяПринятФайла"));
	Для Каждого СтрокаИмяПринятФайла Из СтрокиИмяПринятФайла Цикл
		ИменаПринятФайла.Добавить(СтрокаИмяПринятФайла.Значение);
	КонецЦикла;
	ИмяОбрабФайла = СтрСоединить(ИменаПринятФайла, Символы.ПС);
	
	// Причина отказа.
	ПричиныОтказа = Новый Массив;
	ПричинаОтказа = ПеречВыявНар(УзелСведУведОтказ);
	ПричиныОтказа.Добавить(ПричинаОтказа);
	
	СтрокиВыявлНарФайл = УзелСведУведОтказ.Строки.НайтиСтроки(Новый Структура("Имя", "ВыявлНарФайл"));
	Для каждого СтрокаВыявлНарФайл Из СтрокиВыявлНарФайл Цикл
		ПричинаОтказа = ПеречВыявНар(СтрокаВыявлНарФайл);
		ПричиныОтказа.Добавить(ПричинаОтказа);
	КонецЦикла;  
	ПричинаОтказа = СтрСоединить(ПричиныОтказа, Символы.ПС);
		
	СвойстваУведомления = Новый Структура();
	СвойстваУведомления.Вставить("ИмяОбрабФайла",  	ИмяОбрабФайла);
	СвойстваУведомления.Вставить("ПричинаОтказа",  	ПричинаОтказа);
	
	Возврат СвойстваУведомления;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьДатуНапрИПостИзУведомленияОбОтказеВПриеме(УзелДокумент)
	
	УзелСведУведОтказ = ПолучитьУзел(УзелДокумент, "СведУведОтказ");
	Если НЕ ЗначениеЗаполнено(УзелСведУведОтказ) Тогда
		Возврат Неопределено;
	КонецЕсли;

	УзелОбщСвУвед = ПолучитьУзел(УзелСведУведОтказ, "ОбщСвУвед");
	Если НЕ ЗначениеЗаполнено(УзелОбщСвУвед) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДатаНапр = ПолучитьУзел(УзелОбщСвУвед, "ДатаНапр");
	Если НЕ ЗначениеЗаполнено(УзелДатаНапр) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаНапр = УзелДатаНапр.Значение;
	
	УзелВремНапр = ПолучитьУзел(УзелОбщСвУвед, "ВремНапр");
	Если НЕ ЗначениеЗаполнено(УзелВремНапр) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ВремНапр = УзелВремНапр.Значение;
	
	УзелДатаПост = ПолучитьУзел(УзелОбщСвУвед, "ДатаПост");
	Если НЕ ЗначениеЗаполнено(УзелДатаПост) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДатаПост = УзелДатаПост.Значение;
	
	СвойстваУведомления = Новый Структура();
	СвойстваУведомления.Вставить("ДатаНапр",  	ДатаНапр);
	СвойстваУведомления.Вставить("ВремНапр",  	ВремНапр);
	СвойстваУведомления.Вставить("ДатаПост",  	ДатаПост);
	
	Возврат СвойстваУведомления;
	
КонецФункции
	
&НаСервереБезКонтекста
Функция ПеречВыявНар(РодительскийУзел)
	
	ПеречВыявНар 		= Новый Массив;
	СтрокиПеречВыявНар 	= РодительскийУзел.Строки.НайтиСтроки(Новый Структура("Имя", "ПеречВыявНар"));
	Для Каждого СтрокаПеречВыявНар Из СтрокиПеречВыявНар Цикл
		
		УзелКодОш 	= ПолучитьУзел(СтрокаПеречВыявНар, "КодОш");
		УзелТекстОш = ПолучитьУзел(СтрокаПеречВыявНар, "ТекстОш");
		УзелИдОш = ПолучитьУзел(СтрокаПеречВыявНар, "ИдОш", Ложь);
		
		ВыявНар = НСтр("ru = '%1 (код ошибки %2)'");
		ВыявНар = СтрШаблон(ВыявНар, УзелТекстОш.Значение, УзелКодОш.Значение);
		Если УзелИдОш <> Неопределено И ЗначениеЗаполнено(УзелИдОш.Значение)
			И УзелИдОш.Значение <> УзелТекстОш.Значение Тогда
			ВыявНар = ВыявНар + "
				|" + УзелИдОш.Значение;
		КонецЕсли;
		
		ПеречВыявНар.Добавить(ВыявНар);
		
	КонецЦикла;
	ПричинаОтказа = СтрСоединить(ПеречВыявНар, Символы.ПС);
	
	Возврат ПричинаОтказа;
	
Конецфункции

&НаСервереБезКонтекста
Функция ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, ИмяТэга)
	
	Отправитель = "";
	
	// Сведения об отправителе отчетности
	ДанныеОтправителя = УзелДокумент.Строки.Найти(ИмяТэга, "Имя");
	ОтпрЮЛ     = ДанныеОтправителя.Строки.Найти("ОтпрЮЛ", "Имя");
	ОтпрФЛ     = ДанныеОтправителя.Строки.Найти("ОтпрФЛ", "Имя");
	ОтпрНО     = ДанныеОтправителя.Строки.Найти("ОтпрНО", "Имя");
	ПолЮЛ      = ДанныеОтправителя.Строки.Найти("ПолЮЛ", "Имя");
	ПолФЛ      = ДанныеОтправителя.Строки.Найти("ПолФЛ", "Имя");
	ПолНО      = ДанныеОтправителя.Строки.Найти("ПолНО", "Имя");
	КодНО      = ДанныеОтправителя.Строки.Найти("КодНО", "Имя");
	ФЛ         = ДанныеОтправителя.Строки.Найти("ФЛ", "Имя");
	ЮЛ         = ДанныеОтправителя.Строки.Найти("ЮЛ", "Имя");
	СпецОперат = ДанныеОтправителя.Строки.Найти("СпецОперат", "Имя");
	
	Если ЗначениеЗаполнено(ОтпрФЛ) Тогда
		Отправитель = ПолучитьПредставлениеНП(Ложь,ОтпрФЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрЮЛ) Тогда
		Отправитель = ПолучитьПредставлениеНП(Истина,ОтпрЮЛ);
	ИначеЕсли ЗначениеЗаполнено(ПолФЛ) Тогда
		Отправитель = ПолучитьПредставлениеНП(Ложь,ПолФЛ);
	ИначеЕсли ЗначениеЗаполнено(ПолЮЛ) Тогда
		Отправитель = ПолучитьПредставлениеНП(Истина,ПолЮЛ);
	ИначеЕсли ЗначениеЗаполнено(ФЛ) Тогда
		Отправитель = ПолучитьПредставлениеНП(Ложь,ФЛ);
	ИначеЕсли ЗначениеЗаполнено(ЮЛ) Тогда
		Отправитель = ПолучитьПредставлениеНП(Истина,ЮЛ);
	ИначеЕсли ЗначениеЗаполнено(ОтпрНО) Тогда
		Отправитель = ПолучитьПредставлениеНалоговогоОрганаИзXML(ОтпрНО);
	ИначеЕсли ЗначениеЗаполнено(ПолНО) Тогда
		Отправитель = ПолучитьПредставлениеНалоговогоОрганаИзXML(ПолНО);
	ИначеЕсли ЗначениеЗаполнено(СпецОперат) Тогда
		Отправитель = ПолучитьПредставлениеОператораСвязиИзXML(СпецОперат);
	ИначеЕсли ЗначениеЗаполнено(КодНО) Тогда
		Отправитель = НСтр("ru = 'ИФНС %1'");
		Отправитель = СтрШаблон(Отправитель, КодНО.Значение);
	КонецЕсли;
	
	Возврат Отправитель;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьУзел(РодительскийУзел, ИмяУзла, ЭтоОбязательныйУзел = Истина)
	
	УзелИскомый = РодительскийУзел.Строки.Найти(ИмяУзла, "Имя");
	Если ЗначениеЗаполнено(УзелИскомый) Тогда
		Возврат УзелИскомый;
	Иначе
		
		Если ЭтоОбязательныйУзел Тогда
			ТекстСообщения = НСтр("ru = 'Некорректная структура XML уведомления: не обнаружен узел ""%1"".'");
			ТекстСообщения = СтрШаблон(ТекстСообщения, ИмяУзла); 
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		КонецЕсли;
		
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриемеНалоговойДекларацииОтметки(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// формируем таблицу с объявлениями полей узла с общими сведениями
	ПоляОбщихСведений = Новый ТаблицаЗначений;
	ПоляОбщихСведений.Колонки.Добавить("Код");
	ПоляОбщихСведений.Колонки.Добавить("Имя");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИдФайл", "Идентификатор файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерДокНП", "Номер заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаДокНП", "Дата заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "РегНомДок", "Рег. номер заявления в налоговом органе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегЭл", "Дата регистрации в НО в электронном виде");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегБум", "Дата регистрации в НО на бумажном носителе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерУведОткз", "Номер уведомления об отказе в проставлении отметки");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаУведОткз", "Дата уведомления об отказе в проставлении отметки");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НомерДокНП2", "Номер нового заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаДокНП2", "Дата нового заявления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "РегНомДок2", "Рег. номер нового заявления в налоговом органе");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегЭл2", "Дата регистрации нового заявления в НО в электронном виде");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаРегБум2", "Дата регистрации нового заявления в НО на бумажном носителе");
	
	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	// выводим секцию заголовка
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	// выводим разделитель
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	// пытаемся разобрать сообщение
	СвойстваСообщения = ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриемеНалоговойДекларацииОтметки(Тело);
	
	// если разобрать не удалось - выводим его в виде текста
	Если НЕ ЗначениеЗаполнено(СвойстваСообщения) Тогда
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
		
		// если текст сообщения разобрать удалось, то выводим основную инфорфмацию из него
	Иначе
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		
		Если СвойстваСообщения.Ошибки.Количество() > 0 Тогда
			
			// выводим "шапку" таблицы ошибок
			Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
			Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Ошибки:";
			ТаблДок.Вывести(Секция_ЗаголовокГруппы);
			
			// получаем область для вывода единицы информации сообщения
			Для Каждого Ошибка Из СвойстваСообщения.Ошибки Цикл
				ВывестиТекстПострочно(ТаблДок, Секция_Тело, "Ошибка: " + Ошибка.Описание);
			КонецЦикла;
			
			// выводим пустую строку - разделитель
			Секция_Тело.Области.Тело.Текст = "";
			ТаблДок.Вывести(Секция_Тело);
			
		КонецЕсли;
		
		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Общие сведения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим общие сведения поэлементно
		ОбщиеСведенияСообщения = СвойстваСообщения.ОбщиеСведения;
		Для Каждого ПолеОбщихСведений Из ПоляОбщихСведений Цикл
			Если ОбщиеСведенияСообщения.Свойство(ПолеОбщихСведений.Код) Тогда
				ЗначениеПоля = ОбщиеСведенияСообщения[ПолеОбщихСведений.Код];
				Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
					Секция_Тело.Области.Тело.Текст = ПолеОбщихСведений.Имя + ": " + СокрЛП(ЗначениеПоля);
					ТаблДок.Вывести(Секция_Тело);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Разделитель);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);

	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриемеНалоговойДекларацииЗаявление(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	// формируем таблицу с объявлениями полей узла с общими сведениями
	ПоляОбщихСведений = Новый ТаблицаЗначений;
	ПоляОбщихСведений.Колонки.Добавить("Код");
	ПоляОбщихСведений.Колонки.Добавить("Имя");
	
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИмяПринятФайла", "Имя принятого файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ИдФайл", "Идентификатор файла");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаВремяНапр", "Дата и время направления");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "КНД", "Код формы по КНД");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "НаимВидДок", "Наименование вида документа");
	ДобавитьСтрокуВТаблицуПолейОбщихСведений(ПоляОбщихСведений, "ДатаПост", "Дата поступления отчета");
	
	// получаем бланк отчета из макета
	Бланк = ПолучитьМакетОбработки("БланкПечатиДИВ");
	
	// инициализируем итоговый табличный документ
	ТаблДок = Новый ТабличныйДокумент;
	ТаблДок.АвтоМасштаб = Истина;
	
	// выводим секцию заголовка
	Секция_Заголовок = Бланк.ПолучитьОбласть("Секция_Заголовок");
	Секция_Заголовок.Области.Заголовок.Текст = Заголовок;
	Секция_Заголовок.Области.Файл.Текст = ИмяФайла;
	ТаблДок.Вывести(Секция_Заголовок);
	
	// выводим разделитель
	Секция_Разделитель = Бланк.ПолучитьОбласть("Секция_Разделитель");
	ТаблДок.Вывести(Секция_Разделитель);
	
	// пытаемся разобрать уведомление
	// получим структуру ("Ошибки, НарушенияФайлов, ОбщиеСведения")
	СвойстваИзУведомления = ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриемеНалоговойДекларацииЗаявление(Тело);
	
	// если разобрать не удалось - выводим его в виде текста
	Если НЕ ЗначениеЗаполнено(СвойстваИзУведомления) Тогда
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		ВывестиТекстПострочно(ТаблДок, Секция_Тело, Тело);
		
		// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Иначе
		
		Секция_Тело = Бланк.ПолучитьОбласть("Секция_Тело");
		
		// выводим "шапку" таблицы ошибок
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Выявленные нарушения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// получаем область для вывода единицы информации уведомления
		Для Каждого Ошибка Из СвойстваИзУведомления.Ошибки Цикл
			ВывестиТекстПострочно(ТаблДок, Секция_Тело, "Ошибка " + Ошибка.КодОшибки + ": " + Ошибка.Описание);
		КонецЦикла;
		
		// выводим пустую строку - разделитель
		Секция_Тело.Области.Тело.Текст = "";
		ТаблДок.Вывести(Секция_Тело);
		
		// выводим "шапку" общих сведений
		Секция_ЗаголовокГруппы = Бланк.ПолучитьОбласть("Секция_ЗаголовокГруппы");
		Секция_ЗаголовокГруппы.Области.ЗаголовокГруппы.Текст = "Общие сведения:";
		ТаблДок.Вывести(Секция_ЗаголовокГруппы);
		
		// выводим общие сведения поэлементно
		ОбщиеСведенияСообщения = СвойстваИзУведомления.ОбщиеСведения;
		Для Каждого ПолеОбщихСведений Из ПоляОбщихСведений Цикл
			Если ОбщиеСведенияСообщения.Свойство(ПолеОбщихСведений.Код) Тогда
				ЗначениеПоля = ОбщиеСведенияСообщения[ПолеОбщихСведений.Код];
				Если ЗначениеЗаполнено(ЗначениеПоля) Тогда
					Секция_Тело.Области.Тело.Текст = ПолеОбщихСведений.Имя + ": " + СокрЛП(ЗначениеПоля);
					ТаблДок.Вывести(Секция_Тело);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ТаблДок.Вывести(Секция_Разделитель);
	СформироватьТабличныйДокументСекцияПодписи(ТаблДок, Бланк, ТаблицаПодписей);
	
	// инициализируем текст нижнего колонтитула
	ТаблДок.НижнийКолонтитул.ТекстСлева = НижнийКолонтитул;
	
	
	Возврат ТаблДок;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриемеНалоговойДекларацииОтметки(СтрСообщение)
	
	Если НЕ ЗначениеЗаполнено(СтрСообщение) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТаблицаОшибок = Новый ТаблицаЗначений;
	ТаблицаОшибок.Колонки.Добавить("Описание");

	ОбщиеСведения = Новый Структура;
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрСообщение, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Для Каждого УзелСведений Из УзелФайл.Строки Цикл
		ИмяУзла = УзелСведений.Имя;
		Если ИмяУзла = "ИдФайл" ИЛИ ИмяУзла = "ВерсПрог" ИЛИ ИмяУзла = "ВерсФорм" Тогда
			ОбщиеСведения.Вставить(УзелСведений.Имя, СокрЛП(УзелСведений.Значение));
		КонецЕсли;
	КонецЦикла;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	Для Каждого УзелОбщСвед Из УзелДокумент.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;

	УзелСвУведом = УзелДокумент.Строки.Найти("СвУведом", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвУведом) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""СвУведом"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	Для Каждого УзелОбщСвед Из УзелСвУведом.Строки Цикл
		ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
	КонецЦикла;
	
	// анализируем сведения о несоответствиях
	УзлыСвНесоотв = УзелСвУведом.Строки.НайтиСтроки(Новый Структура("Имя", "СвНесоотв"));
	Для Каждого УзелСвНесоотв Из УзлыСвНесоотв Цикл
		НовСтр = ТаблицаОшибок.Добавить();
		НовСтр.Описание = СокрЛП(УзелСвНесоотв.Значение);
	КонецЦикла;
	
	// анализируем сведения о непредставленных документах
	УзлыСвНепредст = УзелСвУведом.Строки.НайтиСтроки(Новый Структура("Имя", "СвНепредст"));
	Для Каждого УзелСвНепредст Из УзлыСвНепредст Цикл
		НовСтр = ТаблицаОшибок.Добавить();
		НовСтр.Описание = СокрЛП(УзелСвНепредст.Значение);
	КонецЦикла;
	
	// дополняем узел с общими сведениями
	УзелСвНовЗаяв = УзелСвУведом.Строки.Найти("СвНовЗаяв", "Имя");
	Если ЗначениеЗаполнено(УзелСвНовЗаяв) Тогда
		Для Каждого УзелОбщСвед Из УзелСвНовЗаяв.Строки Цикл
			ОбщиеСведения.Вставить(УзелОбщСвед.Имя, СокрЛП(УзелОбщСвед.Значение));
		КонецЦикла;
	КонецЕсли;
	
	Возврат Новый Структура("Ошибки, ОбщиеСведения", ТаблицаОшибок, ОбщиеСведения);
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзУведомленияОбОтказеВПриемеНалоговойДекларацииЗаявление(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Ошибки = Новый ТаблицаЗначений;
	Ошибки.Колонки.Добавить("КодОшибки");
	Ошибки.Колонки.Добавить("Описание");
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений( , СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка разбора XML файла:%1'"),
			Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Файл"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""Документ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСведУведОтказ = УзелДокумент.Строки.Найти("СведУведОтказ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСведУведОтказ) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""СведУведОтказ"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	УзелОбщСвУвед = УзелСведУведОтказ.Строки.Найти("ОбщСвУвед", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелОбщСвУвед) Тогда
		ТекстСообщения = НСтр("ru = 'Некорректная структура XML файла: не обнаружен узел ""ОбщСвУвед"".'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем общий перечень нарушений
	УзлыПеречВыявНар = УзелСведУведОтказ.Строки.НайтиСтроки(Новый Структура("Имя", "ПеречВыявНар"));
	Для Каждого УзелПеречВыявНар Из УзлыПеречВыявНар Цикл
		УзелКодОш = УзелПеречВыявНар.Строки.Найти("КодОш", "Имя");
		УзелТекстОш = УзелПеречВыявНар.Строки.Найти("ТекстОш", "Имя");
		Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш) Тогда
			
			КодОшибки = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "");
			ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "");
			
			НовСтр = Ошибки.Добавить();
			НовСтр.КодОшибки = КодОшибки;
			НовСтр.Описание = ТекстОшибки;
			
		КонецЕсли;
	КонецЦикла;
	
	// разбираем узел с перечнем выявленных нарушений
	Если УзлыПеречВыявНар.Количество() = 0 Тогда
		УзлыВыявлНарФайл = УзелСведУведОтказ.Строки.НайтиСтроки(Новый Структура("Имя", "ВыявлНарФайл"));
		Для Каждого УзелВыявНарФайл Из УзлыВыявлНарФайл Цикл
			УзлыПеречВыявНар = УзелВыявНарФайл.Строки.НайтиСтроки(Новый Структура("Имя", "ПеречВыявНар"));
			Для Каждого УзелПеречВыявНар Из УзлыПеречВыявНар Цикл
				УзелКодОш = УзелПеречВыявНар.Строки.Найти("КодОш", "Имя");
				УзелТекстОш = УзелПеречВыявНар.Строки.Найти("ТекстОш", "Имя");
				Если ЗначениеЗаполнено(УзелКодОш) ИЛИ ЗначениеЗаполнено(УзелТекстОш) Тогда
					
					КодОшибки = ?(ЗначениеЗаполнено(УзелКодОш), СокрЛП(УзелКодОш.Значение), "");
					ТекстОшибки = ?(ЗначениеЗаполнено(УзелТекстОш), СокрЛП(УзелТекстОш.Значение), "");
					
					НовСтр = Ошибки.Добавить();
					НовСтр.КодОшибки = КодОшибки;
					НовСтр.Описание = ТекстОшибки;
					
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;
	
	// разбираем узел с общими сведениями
	ОбщиеСведения = Новый Структура;
	УзелОбщСвУвед = УзелСведУведОтказ.Строки.Найти("ОбщСвУвед", "Имя");
	Для Каждого УзелСведение Из УзелОбщСвУвед.Строки Цикл
		ОбщиеСведения.Вставить(УзелСведение.Имя, СокрЛП(УзелСведение.Значение));
	КонецЦикла;
	
	Если ОбщиеСведения.Свойство("ДатаНапр") И ОбщиеСведения.Свойство("ВремНапр") Тогда
		ОбщиеСведения.Вставить("ДатаВремяНапр", ОбщиеСведения.ДатаНапр + " " + ОбщиеСведения.ВремНапр);
	КонецЕсли;
	
	Возврат Новый Структура("Ошибки, ОбщиеСведения", Ошибки, ОбщиеСведения);
	
КонецФункции

// Проверяет наличие ограничений у пользователя на обмен 1С-Отчетности
//
// Параметры:
//	ВыбранныеОрганизации - Массив
//
// Возвращаемое значение:
//	Структура с полями
//	* Состояние 	- Число - возможные значения 1 (доступен), 2 (запрещен), 3 (требует оповещения)
//
&НаСервере
Функция СостояниеВозможностиОбмена(ВыбранныеОрганизации) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Состояние", 1);
	
	КонтекстСервера = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ВсеОрганизации = КонтекстСервера.ОрганизацииСОграничениемПрав();
	НастроенныеОрганизации = КонтекстСервера.ВыбранныеОрганизацииДляОбмена();
	
	НашлиСтроки = ВсеОрганизации.НайтиСтроки(Новый Структура("НетОграничения", Ложь));
	Если (НашлиСтроки.Количество() = ВсеОрганизации.Количество()
		И ВсеОрганизации.Количество() > 0)
		ИЛИ НастроенныеОрганизации.Количество() = 0 Тогда
		Результат.Состояние = 3;
	КонецЕсли;	
	
	Если Результат.Состояние <> 3 Тогда
		ПраваНаОбмен = ЕстьВозможностьОбмена(ВыбранныеОрганизации, Ложь);
		Если ПраваНаОбмен Тогда
			Результат.Состояние = 1;
		Иначе
			Результат.Состояние = 2;
		КонецЕсли;	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция ЕстьВозможностьОбмена(Организации, ЭтоОбменИзЖурналаОбмена) Экспорт
	
	ОбменВозможен = Ложь;
	
	ЕстьПравоНаДокументооборот 	= Ложь;
	ЕстьХотяБыОднаУчетнаяЗапись = Ложь;
	ЕстьЭДОСПрочимиОрганами 	= Ложь;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Для каждого Организация Из Организации Цикл
		
		// Учетная запись.
		УчетнаяЗапись = КонтекстЭДОСервер.УчетнаяЗаписьОрганизации(Организация);
		
		Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
			
			ЕстьХотяБыОднаУчетнаяЗапись = Истина;
			
			ОбменПоДаннойУчетнойЗаписиВозможен = КонтекстЭДОСервер.ПользователюРазрешеноВыполнятьОбменПоУчетнойЗаписи(УчетнаяЗапись);
		
			Если ОбменПоДаннойУчетнойЗаписиВозможен Тогда
				ЕстьПравоНаДокументооборот 	= Истина;
				ОбменВозможен 				= Истина;
			КонецЕсли;
			
		КонецЕсли;
		
		Если НЕ ЭтоОбменИзЖурналаОбмена Тогда
		
			// ФСС.
			ЕстьОбменСФСС = КонтекстЭДОСервер.ОрганизацияИспользуетОбменСФСС(Организация);
				
			Если ЕстьОбменСФСС Тогда
				ЕстьЭДОСПрочимиОрганами = Истина;
				ОбменВозможен = Истина;
			КонецЕсли;
			
			// ФСРАР.
			ЕстьОбменСФСРАР = КонтекстЭДОСервер.ОрганизацияИспользуетОбменСФСРАР(Организация);
				
			Если ЕстьОбменСФСРАР Тогда
				ЕстьЭДОСПрочимиОрганами = Истина;
				ОбменВозможен = Истина;
			КонецЕсли;
			
			// РПН.
			ЕстьОбменСРПН = КонтекстЭДОСервер.ОрганизацияИспользуетОбменСРПН(Организация);
				
			Если ЕстьОбменСРПН Тогда
				ЕстьЭДОСПрочимиОрганами = Истина;
				ОбменВозможен = Истина;
			КонецЕсли;
			
			// ФТС.
			ЕстьОбменСФТС = КонтекстЭДОСервер.ОрганизацияИспользуетОбменСФТС(Организация);
				
			Если ЕстьОбменСФТС Тогда
				ЕстьЭДОСПрочимиОрганами = Истина;
				ОбменВозможен = Истина;
			КонецЕсли;
			
			// Банк России
			ЕстьОбменСБанкомРоссии = КонтекстЭДОСервер.ОрганизацияИспользуетОбменСБанкомРоссии(Организация);
				
			Если ЕстьОбменСБанкомРоссии Тогда
				ЕстьЭДОСПрочимиОрганами = Истина;
				ОбменВозможен = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если НЕ ОбменВозможен Тогда
		
		ТекстНедостаточноПрав = НСтр("ru = 'Недостаточно прав для проведения обновления. 
			|Убедитесь, что ваш пользователь указан в списке пользователей учетной записи документооборота организации.'");
		
		Если ЭтоОбменИзЖурналаОбмена Тогда
			
			Если НЕ ЕстьХотяБыОднаУчетнаяЗапись Тогда
				ОбщегоНазначения.СообщитьПользователю(
					НСтр("ru = 'Для организации не настроена возможность отправки в контролирующие органы'"));
			ИначеЕсли НЕ ЕстьПравоНаДокументооборот Тогда
				ОбщегоНазначения.СообщитьПользователю(ТекстНедостаточноПрав);
			КонецЕсли;
				
		Иначе
			
			Если НЕ ЕстьПравоНаДокументооборот Тогда
				ОбщегоНазначения.СообщитьПользователю(ТекстНедостаточноПрав);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ОбменВозможен;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИзВременногоХранилищаЕслиСтрока(Адрес, ПривестиКСтроке = Ложь)
	
	ДанныеВременногоХранилища = ПолучитьИзВременногоХранилища(Адрес);
	Если ТипЗнч(ДанныеВременногоХранилища) = Тип("Строка") Тогда
		Возврат ДанныеВременногоХранилища;
		
	ИначеЕсли ПривестиКСтроке И ТипЗнч(ДанныеВременногоХранилища) = Тип("ДвоичныеДанные") Тогда
		ИмяВременногоФайла = ПолучитьИмяВременногоФайла();
		ДанныеВременногоХранилища.Записать(ИмяВременногоФайла);
		
		ОбъектЧтениеТекста = Новый ЧтениеТекста(ИмяВременногоФайла, "windows-1251");
		ТекстФайла = ОбъектЧтениеТекста.Прочитать();
		ОбъектЧтениеТекста.Закрыть();
		
		ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ИмяВременногоФайла);
		
		Возврат ТекстФайла;
		
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Функция СгенерироватьUUIDКлиент() Экспорт
	
	Возврат НРег(СтрЗаменить(Строка(Новый УникальныйИдентификатор), "-", ""));
	
КонецФункции

&НаКлиенте
Функция СгенерироватьИмяФайлаДокументаВСоставеПакетаКлиент(РасширениеФайла = ".bin")
	
	Возврат СгенерироватьUUIDКлиент() + РасширениеФайла;
	
КонецФункции

//Конвертация текста в ДД в base64 с приведением типа к ДД
&НаСервереБезКонтекста
Процедура ЗакодироватьВBase64ЗначениеВременногоХранилища(АдресВоВременномХранилище, ПривестиКДД = Истина)
	
	ДД = ПолучитьИзВременногоХранилища(АдресВоВременномХранилище);
	
	РаботаемСДД = ТипЗнч(ДД) = Тип("ДвоичныеДанные");
	
	ВременныйФайл = ПолучитьИмяВременногоФайла("bin");
	
	Если РаботаемСДД Тогда 
		Б64 = Base64Строка(ДД);
	Иначе
		ТекстовыйДок = Новый ТекстовыйДокумент();
		ТекстовыйДок.УстановитьТекст(ДД);
		ТекстовыйДок.Записать(ВременныйФайл, "windows-1251", "");
		
		ДД = Новый ДвоичныеДанные(ВременныйФайл);
		Б64 = Base64Строка(ДД);
	КонецЕсли;	
	
	Если ПривестиКДД Тогда
		ТекстовыйДок = Новый ТекстовыйДокумент();
		ТекстовыйДок.УстановитьТекст(Б64);
		ТекстовыйДок.Записать(ВременныйФайл, "windows-1251", "");
		
		ДД = Новый ДвоичныеДанные(ВременныйФайл);
	Иначе
		ДД = Б64;
	КонецЕсли;

	ПоместитьВоВременноеХранилище(ДД, АдресВоВременномХранилище);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗагрузитьДанныеНаСервер(Знач ТекстФайлаВыгрузки, УникальныйИдентификатор, ПолноеИмяФайлаНаСервере = Неопределено, ПреобразоватьВДвоичныеДанные = Ложь, КодироватьВBase64 = Ложь, ДекодироватьИзBase64 = Ложь, ИгнорироватьКодировку = Ложь)
	
	КонтекстЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();

	Адрес = КонтекстЭДО.ЗагрузитьДанныеНаСервер(
				ТекстФайлаВыгрузки,
				УникальныйИдентификатор,
				ПолноеИмяФайлаНаСервере,
				ПреобразоватьВДвоичныеДанные,
				КодироватьВBase64,
				ДекодироватьИзBase64,
				ИгнорироватьКодировку);
	Возврат Адрес;
	
КонецФункции

&НаКлиенте
Процедура СформироватьПодписанныйПакетДляПорталаФСРАРПослеАвторизации(Результат, ВхПараметры) Экспорт
	СформироватьПодписанныйПакетДляПорталаФСРАР(
		ВхПараметры[0], ВхПараметры[1], ВхПараметры[2], ВхПараметры[3], ВхПараметры[4], 
		ВхПараметры[5], ВхПараметры[6], ВхПараметры[7], ВхПараметры[8], ВхПараметры[9])
КонецПроцедуры

#Область ОтправкаУведомленияОПолучателеДокументов

&НаСервереБезКонтекста
Функция СформироватьФайлУведомленияОПолучателеДокументов(
	Уведомление,
	ФамилияИмяОтчествоИлиВладелецСертификатаШифрования = Неопределено,
	УникальныйИдентификатор = Неопределено,
	ПолучитьТекстФайлаУведомления = Ложь)
	
	// получаем свойства уведомления, при необходимости дозаполняем из организации
	СвойстваУведомления = Новый Структура;
	СвойстваУведомления.Вставить("Организация", 								Уведомление.Организация);
	СвойстваУведомления.Вставить("ДляВсехПодразделений", 						Уведомление.ДляВсехПодразделений);
	СвойстваУведомления.Вставить("Получатель", 									Уведомление.Получатель);
	СвойстваУведомления.Вставить("ЗаданиеЯвноРеквизитовПолучателяДокументов", 	Уведомление.ЗаданиеЯвноРеквизитовПолучателяДокументов);
	СвойстваУведомления.Вставить("ИдентификаторАбонента", 						Уведомление.ИдентификаторАбонента);
	СвойстваУведомления.Вставить("НаименованиеПолучателяДокументов", 			Уведомление.НаименованиеПолучателяДокументов);
	СвойстваУведомления.Вставить("ИННПолучателяДокументов", 					Уведомление.ИННПолучателяДокументов);
	СвойстваУведомления.Вставить("КПППолучателяДокументов", 					Уведомление.КПППолучателяДокументов);
	СвойстваУведомления.Вставить("ПолучательДокументовЯвляетсяФизическимЛицом", Уведомление.ПолучательДокументовЯвляетсяФизическимЛицом);
	СвойстваУведомления.Вставить("ФамилияПолучателяДокументов", 				Уведомление.ФамилияПолучателяДокументов);
	СвойстваУведомления.Вставить("ИмяПолучателяДокументов", 					Уведомление.ИмяПолучателяДокументов);
	СвойстваУведомления.Вставить("ОтчествоПолучателяДокументов", 				Уведомление.ОтчествоПолучателяДокументов);
	СвойстваУведомления.Вставить("ДолжностныеЛицаПолучателяДокументов", 		Новый Массив);
	СвойстваУведомления.Вставить("ВидУслуги", 									Неопределено);
	СвойстваУведомления.Вставить("ТипСверки", 									Неопределено);
	
	Для каждого ДолжностноеЛицоПолучателяДокументов Из Уведомление.ДолжностныеЛицаПолучателяДокументов Цикл
		СвойстваДолжностногоЛица = Новый Структура;
		СвойстваДолжностногоЛица.Вставить("НомерСтроки", 	ДолжностноеЛицоПолучателяДокументов.НомерСтроки);
		СвойстваДолжностногоЛица.Вставить("Фамилия", 		ДолжностноеЛицоПолучателяДокументов.Фамилия);
		СвойстваДолжностногоЛица.Вставить("Имя", 			ДолжностноеЛицоПолучателяДокументов.Имя);
		СвойстваДолжностногоЛица.Вставить("Отчество", 		ДолжностноеЛицоПолучателяДокументов.Отчество);
		СвойстваУведомления.ДолжностныеЛицаПолучателяДокументов.Добавить(СвойстваДолжностногоЛица);
	КонецЦикла;
	
	ПараметрыУведомления = СформироватьПараметрыЗапроса(СвойстваУведомления);
	Если ПараметрыУведомления = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Если НЕ СвойстваУведомления.ЗаданиеЯвноРеквизитовПолучателяДокументов Тогда
		СвойстваПолучателяДокументов = СвойстваПолучателяДокументовУведомления(Уведомление.Организация, ПараметрыУведомления);
		ЗаполнитьЗначенияСвойств(СвойстваУведомления, СвойстваПолучателяДокументов);
		СвойстваУведомления.ДолжностныеЛицаПолучателяДокументов = Новый Массив;
		
		Если ЭтоЛокальнаяПодпись(СвойстваПолучателяДокументов) Тогда
			ФамилияИмяОтчествоДолжностногоЛица = ФамилияИмяОтчествоИлиВладелецСертификатаШифрования;
			Если ТипЗнч(ФамилияИмяОтчествоДолжностногоЛица) = Тип("Строка") Тогда
				ФамилияИмяОтчествоДолжностногоЛица = ФамилияИмяОтчествоИзСвойстваСертификата(ФамилияИмяОтчествоДолжностногоЛица);
			КонецЕсли;
			СвойстваПолучателяДокументов.ФамилияДолжностногоЛица =
				?(ФамилияИмяОтчествоДолжностногоЛица <> Неопределено, ФамилияИмяОтчествоДолжностногоЛица.Фамилия, "");
			СвойстваПолучателяДокументов.ИмяДолжностногоЛица =
				?(ФамилияИмяОтчествоДолжностногоЛица <> Неопределено, ФамилияИмяОтчествоДолжностногоЛица.Имя, "");
			СвойстваПолучателяДокументов.ОтчествоДолжностногоЛица =
				?(ФамилияИмяОтчествоДолжностногоЛица <> Неопределено, ФамилияИмяОтчествоДолжностногоЛица.Отчество, "");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СвойстваПолучателяДокументов.ФамилияДолжностногоЛица)
			ИЛИ ЗначениеЗаполнено(СвойстваПолучателяДокументов.ИмяДолжностногоЛица)
			ИЛИ ЗначениеЗаполнено(СвойстваПолучателяДокументов.ОтчествоДолжностногоЛица) Тогда
			
			СвойстваДолжностногоЛица = Новый Структура;
			СвойстваДолжностногоЛица.Вставить("НомерСтроки", 	1);
			СвойстваДолжностногоЛица.Вставить("Фамилия", 		СвойстваПолучателяДокументов.ФамилияДолжностногоЛица);
			СвойстваДолжностногоЛица.Вставить("Имя", 			СвойстваПолучателяДокументов.ИмяДолжностногоЛица);
			СвойстваДолжностногоЛица.Вставить("Отчество", 		СвойстваПолучателяДокументов.ОтчествоДолжностногоЛица);
			СвойстваУведомления.ДолжностныеЛицаПолучателяДокументов.Добавить(СвойстваДолжностногоЛица);
		КонецЕсли;
	КонецЕсли;
	
	// собираем параметры письма
	Если НЕ ПроверитьЗаполнениеУведомленияОПолучателеДокументов(СвойстваУведомления, ПараметрыУведомления) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось сформировать уведомление о получателе документов.'"));
		Возврат Неопределено;
	КонецЕсли;
	
	ТекДата = ТекущаяДатаСеанса();
	
	// формируем идентификатор отправителя
	Если ПараметрыУведомления.ПрПодп = 2 Тогда
		СвПред = ПараметрыУведомления.Доверенность.СвУпПред.СвПред;
		Если СвПред.Свойство("СвОрг") Тогда
			ИННКПП = СокрЛП(СвПред.СвОрг.ИННЮЛ) + СокрЛП(СвПред.СвОрг.КПП);
		Иначе
			Если НЕ ПараметрыУведомления.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННКПП = СокрЛП(СвПред.СведФизЛ.ИННФЛ);
			ИначеЕсли ПараметрыУведомления.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации
				И ПараметрыУведомления.Доверенность.СвУпПред.СвПред.СведФизЛ.Свойство("ЯвляетсяСотрудникомОрганизацииПоМЧД")
				И ПараметрыУведомления.Доверенность.СвУпПред.СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизацииПоМЧД = Ложь
				И ДокументооборотСКОКлиентСервер.ПараметрыОтправкиПоМЧДФНС("ИзменятьФайлВыгрузкиДляФизическогоЛица") Тогда
				ИННКПП = ?(ПараметрыУведомления.Доверенность.СвПредст.ЯвляетсяФЛ, СокрЛП(ПараметрыУведомления.Доверенность.СвПредст.ИННФЛ),
					СокрЛП(ПараметрыУведомления.Доверенность.СвПредст.ИННЮЛ) + СокрЛП(ПараметрыУведомления.Доверенность.СвПредст.КПП));
			Иначе
				ИННКПП = ?(ПараметрыУведомления.ЭтоПБОЮЛ, СокрЛП(ПараметрыУведомления.ИННФЛ), СокрЛП(ПараметрыУведомления.ИННЮЛ) + СокрЛП(ПараметрыУведомления.КППЮЛ));
			КонецЕсли;
		КонецЕсли;
	Иначе
		ИННКПП = ?(ПараметрыУведомления.ЭтоПБОЮЛ, СокрЛП(ПараметрыУведомления.ИННФЛ), СокрЛП(ПараметрыУведомления.ИННЮЛ) + СокрЛП(ПараметрыУведомления.КППЮЛ));
	КонецЕсли;
	
	ЭтоКрупнейший    = ДокументооборотСКО.ЭтоКрупнейший(СвойстваУведомления.Организация);
	КодНОКрупнейшего = ДокументооборотСКО.КодНОКрупнейшего(СвойстваУведомления.Организация);
	
	КодНалоговогоОргана = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(СвойстваУведомления.Получатель, "Код");
	
	ИмяФайлаБезРасширения = "ON_UVPOLDOC_"
							 + ?(ЭтоКрупнейший И ЗначениеЗаполнено(КодНОКрупнейшего),
							 СокрЛП(КодНОКрупнейшего), СокрЛП(КодНалоговогоОргана))
							 + "_"
							 + СокрЛП(КодНалоговогоОргана)
							 + "_"
							 + ИННКПП
							 + "_"
							 + Формат(ТекДата, "ДФ=yyyyMMdd")
							 + "_"
							 + Строка(Новый УникальныйИдентификатор);
	
	СтрокаБезОграниченияДлины = -1;
	
	// создаем дерево описания XML
	Дерево = СоздатьДеревоXML();
	
	УзелФайл = ДобавитьУзелXML(Дерево, "Файл");
	ДобавитьУзелXML(УзелФайл, "ИдФайл", Истина, ИмяФайлаБезРасширения,,, СтрокаБезОграниченияДлины);
	НазваниеИВерсияПрограммы = РегламентированнаяОтчетность.НазваниеИВерсияПрограммы();
	ДобавитьУзелXML(УзелФайл, "ВерсПрог", Истина, НазваниеИВерсияПрограммы,,, 40);
	ДобавитьУзелXML(УзелФайл, "ВерсФорм", Истина, "5.01",,, СтрокаБезОграниченияДлины);
	УзелДокумент = ДобавитьУзелXML(УзелФайл, "Документ");
	
		ДобавитьУзелXML(УзелДокумент, "КНД", Истина, "1167008",,, СтрокаБезОграниченияДлины);
		ТекущиеДеньМесяцГод = Формат(ТекДата, "ДФ=dd.MM.yyyy");
		ДобавитьУзелXML(УзелДокумент, "ДатаДок", Истина, ТекущиеДеньМесяцГод,,, СтрокаБезОграниченияДлины);
		ДобавитьУзелXML(УзелДокумент, "КодНО", Истина, КодНалоговогоОргана,,, СтрокаБезОграниченияДлины);
		УзелСвНП = ДобавитьУзелXML(УзелДокумент, "СвНП");
		
		Если НЕ ПараметрыУведомления.ЭтоПБОЮЛ Тогда
			
			УзелНПЮЛ = ДобавитьУзелXML(УзелСвНП, "НПЮЛ");
			
				ДобавитьУзелXML(УзелНПЮЛ, "НаимОрг", Истина, ПараметрыУведомления.НаимЮЛПол,,, 1000);
				ДобавитьУзелXML(УзелНПЮЛ, "ИННЮЛ", Истина, ПараметрыУведомления.ИННЮЛ,,, СтрокаБезОграниченияДлины);
				Если НЕ СвойстваУведомления.ДляВсехПодразделений Тогда
					ДобавитьУзелXML(УзелНПЮЛ, "КПП", Истина, ПараметрыУведомления.КППЮЛ,,, СтрокаБезОграниченияДлины);
				КонецЕсли;
			
		Иначе
			
			УзелНПФЛ = ДобавитьУзелXML(УзелСвНП, "НПФЛ");
			
				ДобавитьУзелXML(УзелНПФЛ, "ИННФЛ", Истина, ПараметрыУведомления.ИННФЛ, Истина,, СтрокаБезОграниченияДлины);
				УзелФИО = ДобавитьУзелXML(УзелНПФЛ, "ФИО");
				
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, ПараметрыУведомления.ИПФамилия,,, 60);
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, ПараметрыУведомления.ИПИмя,,, 60);
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, ПараметрыУведомления.ИПОтчество, Истина,, 60);
			
		КонецЕсли;
		
		УзелПодписант = ДобавитьУзелXML(УзелДокумент, "Подписант");
		
			ДобавитьУзелXML(УзелПодписант, "ПрПодп", Истина, ПараметрыУведомления.ПрПодп);
			УзелФИО = ДобавитьУзелXML(УзелПодписант, "ФИО");
			Если ПараметрыУведомления.ПрПодп = 1 Тогда
				Если НЕ ПараметрыУведомления.ЭтоПБОЮЛ Тогда
					ФИОПодписанта = РегламентированнаяОтчетность.РазложитьФИО(ПараметрыУведомления.ФИОРук);
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, ФИОПодписанта.Фамилия,,, 60);
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, ФИОПодписанта.Имя,,, 60);
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, ФИОПодписанта.Отчество, Истина,, 60);
				Иначе
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, ПараметрыУведомления.ИПФамилия,,, 60);
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, ПараметрыУведомления.ИПИмя,,, 60);
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, ПараметрыУведомления.ИПОтчество, Истина,, 60);
				КонецЕсли;
			Иначе
				СвДоверенность = ПараметрыУведомления.Доверенность;
				
				ФИОПодписанта = ДокументооборотСКОВызовСервера.УточнитьФИОПредставителя(
					СвДоверенность.СвУпПред.СвПред.СведФизЛ.ФИО,
					СвДоверенность.ДанныеПредставителя);
				ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, ФИОПодписанта.Фамилия,,, 60);
				ДобавитьУзелXML(УзелФИО, "Имя", Истина, ФИОПодписанта.Имя,,, 60);
				ДобавитьУзелXML(УзелФИО, "Отчество", Истина, ФИОПодписанта.Отчество, Истина,, 60);
				
				УзелСвПред = ДобавитьУзелXML(УзелПодписант, "СвПред");
				
				УчСвДов = СвДоверенность.УчСвДов;
				
				ДобавитьУзелXML(УзелСвПред, "НаимДок", Истина, СвДоверенность.ДанныеПредставителя.ДокументПредставителя,,, 120);
			КонецЕсли;
		
		УзелСвПолДок = ДобавитьУзелXML(УзелДокумент, "СвПолДок");
		
			ДобавитьУзелXML(УзелСвПолДок, "ИдАбонент", Истина, СвойстваУведомления.ИдентификаторАбонента,,, СтрокаБезОграниченияДлины);
			
			Если НЕ СвойстваУведомления.ПолучательДокументовЯвляетсяФизическимЛицом Тогда
				
				УзелСвЮЛ = ДобавитьУзелXML(УзелСвПолДок, "СвЮЛ");
				
					ДобавитьУзелXML(УзелСвЮЛ, "НаимОрг", Истина, СвойстваУведомления.НаименованиеПолучателяДокументов,,, 1000);
					ДобавитьУзелXML(УзелСвЮЛ, "ИННЮЛ", Истина, СвойстваУведомления.ИННПолучателяДокументов,,, СтрокаБезОграниченияДлины);
					ДобавитьУзелXML(УзелСвЮЛ, "КПП", Истина, СвойстваУведомления.КПППолучателяДокументов,,, СтрокаБезОграниченияДлины);
				
			Иначе
				
				УзелСвФЛ = ДобавитьУзелXML(УзелСвПолДок, "СвФЛ");
				
					ДобавитьУзелXML(УзелСвФЛ, "ИННФЛ", Истина, СвойстваУведомления.ИННПолучателяДокументов);
					УзелФИО = ДобавитьУзелXML(УзелСвФЛ, "ФИО");
					
						ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, СвойстваУведомления.ФамилияПолучателяДокументов,,, 60);
						ДобавитьУзелXML(УзелФИО, "Имя", Истина, СвойстваУведомления.ИмяПолучателяДокументов,,, 60);
						ДобавитьУзелXML(УзелФИО, "Отчество", Истина, СвойстваУведомления.ОтчествоПолучателяДокументов, Истина,, 60);
				
			КонецЕсли;
			
			Для каждого ДолжностноеЛицоПолучателяДокументов Из СвойстваУведомления.ДолжностныеЛицаПолучателяДокументов Цикл
				
				УзелФИО = ДобавитьУзелXML(УзелСвПолДок, "ФИО");
				
					ДобавитьУзелXML(УзелФИО, "Фамилия", Истина, ДолжностноеЛицоПолучателяДокументов.Фамилия,,, 60);
					ДобавитьУзелXML(УзелФИО, "Имя", Истина, ДолжностноеЛицоПолучателяДокументов.Имя,,, 60);
					ДобавитьУзелXML(УзелФИО, "Отчество", Истина, ДолжностноеЛицоПолучателяДокументов.Отчество, Истина,, 60);
				
			КонецЦикла;
	
	// сохранение дерева XML в файл и двоичные данные
	ТекстВыгрузкиXML = ЗаписатьДеревоВXML(Дерево);
	
	// формируем имя файла запроса
	ИмяФайлаУведомления = ИмяФайлаБезРасширения + ".xml";
	
	Если ПолучитьТекстФайлаУведомления Тогда
		Результат = Новый Структура("ТекстФайлаУведомления, ИмяФайлаУведомления", ТекстВыгрузкиXML, ИмяФайлаУведомления);
		
	Иначе
		// записываем XML уведомления во временный файл
		ВремФайл = ПолучитьИмяВременногоФайла();
		Если НЕ ЗаписатьXMLВФайл(ТекстВыгрузкиXML, ВремФайл) Тогда
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось записать текст уведомления во временный файл.'"));
			Возврат Неопределено;
		КонецЕсли;
		
		// считываем двоичные данные файла уведомления, после чего удаляем файл
		ДвоичныеДанныеФайлаУведомления = Новый ДвоичныеДанные(ВремФайл);
		АдресФайлаУведомления = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайлаУведомления, УникальныйИдентификатор);
		УдалитьФайлы(ВремФайл);
		
		Результат = Новый Структура("АдресФайлаУведомления, ИмяФайлаУведомления", АдресФайлаУведомления, ИмяФайлаУведомления);
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПроверитьЗаполнениеУведомленияОПолучателеДокументов(ДокУведомление, Параметры)
	
	СтатусПроверки = Истина;
	
	Если НЕ ЗначениеЗаполнено(ДокУведомление.Организация) Тогда
		ТекстСообщения = НСтр("ru = 'Не задана организация.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
		
	ИначеЕсли НЕ ЗначениеЗаполнено(ДокУведомление.Получатель) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан получатель.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
		Возврат СтатусПроверки;
	КонецЕсли;
	
	// проверка сведений об отправителе
	Если Параметры.ПрПодп = 1 Тогда // отправитель - налогоплательщик
		Если Параметры.ЭтоПБОЮЛ Тогда
			Если НЕ ЗначениеЗаполнено(Параметры.ИННФЛ) Тогда
				ТекстСообщения = НСтр("ru = 'Не задан ИНН индивидуального предпринимателя.'");
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		СвПредст = Параметры.Доверенность.СвУпПред.СвПред;
		Если НЕ СвПредст.Свойство("СвОрг") Тогда
			Если НЕ СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
				ИННФЛ = СвПредст.СведФизЛ.ИННФЛ;
				
			Иначе
				ИННФЛ = ?(Параметры.ЭтоПБОЮЛ, Параметры.ИННФЛ, Параметры.ИННЮЛ);
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(ИННФЛ) Тогда
				Если НЕ СвПредст.СведФизЛ.ЯвляетсяСотрудникомОрганизации ИЛИ Параметры.ЭтоПБОЮЛ Тогда
					ТекстСообщения = НСтр("ru = 'Не задан ИНН физлица-представителя.'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
					
				Иначе
					ТекстСообщения = НСтр("ru = 'Не задан ИНН организации физлица-представителя.'");
					ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				КонецЕсли;
				
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	// проверка сведений о налоговом органе
	Если НЕ ЗначениеЗаполнено(Параметры.КодНО) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан налоговый орган.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если СтрДлина(Параметры.КодНО) <> 4 ИЛИ НЕ СтроковыеФункцииКлиентСервер.ТолькоЦифрыВСтроке(Параметры.КодНО) Тогда
		ТекстСообщения = НСтр("ru = 'Код налогового органа должен состоять из четырех цифр.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	// проверка сведений о получателе документов для налогоплательщика
	Если НЕ ЗначениеЗаполнено(ДокУведомление.ИдентификаторАбонента) Тогда
		ТекстСообщения = НСтр("ru = 'Не задан идентификатор абонента получателя документов для налогоплательщика.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
	КонецЕсли;
	
	Если НЕ ДокУведомление.ПолучательДокументовЯвляетсяФизическимЛицом Тогда
		Если НЕ ЗначениеЗаполнено(ДокУведомление.НаименованиеПолучателяДокументов) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано наименование организации - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ДокУведомление.ИННПолучателяДокументов) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан ИНН организации - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
			
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(
			ДокУведомление.ИННПолучателяДокументов, Ложь) Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан ИНН организации - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ДокУведомление.КПППолучателяДокументов) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан КПП организации - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
			
		ИначеЕсли СтрДлина(ДокУведомление.КПППолучателяДокументов) <> 9 Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан КПП организации - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
	Иначе
		Если НЕ ЗначениеЗаполнено(ДокУведомление.ИННПолучателяДокументов) Тогда
			ТекстСообщения = НСтр("ru = 'Не задан ИНН физического лица - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
			
		ИначеЕсли НЕ РегламентированнаяОтчетностьВызовСервера.ИННСоответствуетТребованиямНаСервере(
			ДокУведомление.ИННПолучателяДокументов, Истина) Тогда
			ТекстСообщения = НСтр("ru = 'Неверно задан ИНН физического лица - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ДокУведомление.ФамилияПолучателяДокументов) Тогда
			ТекстСообщения = НСтр("ru = 'Не задана фамилия физического лица - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ДокУведомление.ИмяПолучателяДокументов) Тогда
			ТекстСообщения = НСтр("ru = 'Не задано имя физического лица - получателя документов для налогоплательщика.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
			СтатусПроверки = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДокУведомление.ДолжностныеЛицаПолучателяДокументов) ИЛИ ДокУведомление.ДолжностныеЛицаПолучателяДокументов.Количество() = 0 Тогда
		Если НЕ ДокУведомление.ЗаданиеЯвноРеквизитовПолучателяДокументов Тогда
			ТекстСообщения = НСтр("ru = 'В учетной записи документооборота организации некорректно задан сертификат шифрования: не удается извлечь должностное лицо.'");
		Иначе
			ТекстСообщения = НСтр("ru = 'Не задано ни одного должностного лица получателя документов для налогоплательщика.'");
		КонецЕсли;
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		СтатусПроверки = Ложь;
		
	Иначе
		Для каждого ДолжностноеЛицоПолучателяДокументов Из ДокУведомление.ДолжностныеЛицаПолучателяДокументов Цикл
			Если НЕ ЗначениеЗаполнено(ДолжностноеЛицоПолучателяДокументов.Фамилия) Тогда
				Если НЕ ДокУведомление.ЗаданиеЯвноРеквизитовПолучателяДокументов И ДолжностноеЛицоПолучателяДокументов.НомерСтроки = 1 Тогда
					ТекстСообщения = НСтр("ru = 'В учетной записи документооборота организации задан некорректный сертификат шифрования: не удается извлечь фамилию должностного лица.'");
				Иначе
					ТекстСообщения = НСтр("ru = 'Не задана фамилия должностного лица получателя документов для налогоплательщика в строке %1.'");
				КонецЕсли;
				ТекстСообщения = СтрШаблон(ТекстСообщения, ДолжностноеЛицоПолучателяДокументов.НомерСтроки);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				СтатусПроверки = Ложь;
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(ДолжностноеЛицоПолучателяДокументов.Имя) Тогда
				Если НЕ ДокУведомление.ЗаданиеЯвноРеквизитовПолучателяДокументов И ДолжностноеЛицоПолучателяДокументов.НомерСтроки = 1 Тогда
					ТекстСообщения = НСтр("ru = 'В учетной записи документооборота организации задан некорректный сертификат шифрования: не удается извлечь имя должностного лица.'");
				Иначе
					ТекстСообщения = НСтр("ru = 'Не задано имя должностного лица получателя документов для налогоплательщика в строке %1.'");
				КонецЕсли;
				ТекстСообщения = СтрШаблон(ТекстСообщения, ДолжностноеЛицоПолучателяДокументов.НомерСтроки);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				СтатусПроверки = Ложь;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Возврат СтатусПроверки;
	
КонецФункции

&НаКлиенте
Процедура ВыгрузитьУведомлениеОПолучателеДокументовВФайл(ДокУведомление, УникальныйИдентификатор = Неопределено) Экспорт
	
	Если ТипЗнч(ДокУведомление) = Тип("ДокументСсылка.УведомлениеОПолучателеДокументов") Тогда
		ДокУведомлениеСсылка = ДокУведомление;
		СертификатыДолжностныхЛиц = СертификатыДолжностныхЛицПолучателяДокументов(ДокУведомление);
		
	Иначе
		ДокУведомлениеСсылка = ДокУведомление.Ссылка;
		Если НЕ ДокУведомление.ЗаданиеЯвноРеквизитовПолучателяДокументов Тогда
			СертификатыДолжностныхЛиц = СертификатыУчетнойЗаписиПриОбменеЧерезОператора(ДокУведомление.Организация);
		Иначе
			СертификатыДолжностныхЛиц = Неопределено;
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура("ДокУведомление, УникальныйИдентификатор", ДокУведомлениеСсылка, УникальныйИдентификатор);
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьУведомлениеОПолучателеДокументовВФайлПослеПоискаСертификатаДляШифрования", ЭтотОбъект, ДополнительныеПараметры);
	
	Если СертификатыДолжностныхЛиц <> Неопределено И ЭтоЛокальнаяПодпись(СертификатыДолжностныхЛиц)
	И ЗначениеЗаполнено(СертификатыДолжностныхЛиц.ОтпечатокСертификатаДляШифрования) Тогда
		СвойстваСертификатаДляШифроания = Новый Структура("Отпечаток", СертификатыДолжностныхЛиц.ОтпечатокСертификатаДляШифрования);
		КриптографияЭДКОКлиент.НайтиСертификат(Оповещение, СвойстваСертификатаДляШифроания,, Ложь);
		Возврат;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(Оповещение, Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьУведомлениеОПолучателеДокументовВФайлПослеПоискаСертификатаДляШифрования(Результат, ДополнительныеПараметры) Экспорт
	
	ДокУведомление 									= ДополнительныеПараметры.ДокУведомление;
	УникальныйИдентификаторДляВыгрузкиУведомления 	= ДополнительныеПараметры.УникальныйИдентификатор;
	
	ВладелецСертификатаОтветственногоЗаШифрование = ?(Результат <> Неопределено И Результат.Выполнено И Результат.СертификатНайден,
		Результат.СвойстваСертификата.Владелец, Неопределено);
	
	ФайлУведомленияОПолучателеДокументов = СформироватьФайлУведомленияОПолучателеДокументов(
		ДокУведомление,
		ВладелецСертификатаОтветственногоЗаШифрование,
		УникальныйИдентификаторДляВыгрузкиУведомления);
	
	Если ФайлУведомленияОПолучателеДокументов <> Неопределено Тогда
		СохраняемыйФайл = Новый Структура;
		СохраняемыйФайл.Вставить("Адрес", ФайлУведомленияОПолучателеДокументов.АдресФайлаУведомления);
		СохраняемыйФайл.Вставить("Имя", ФайлУведомленияОПолучателеДокументов.ИмяФайлаУведомления);
		ОперацииСФайламиЭДКОКлиент.СохранитьФайлы(СохраняемыйФайл);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция СвойстваПолучателяДокументовПоУведомлению(УведомлениеОПолучателеДокументов) Экспорт
	
	СвойстваУведомления = Новый Структура;
	СвойстваУведомления.Вставить("Организация", УведомлениеОПолучателеДокументов.Организация);
	СвойстваУведомления.Вставить("Получатель", 	УведомлениеОПолучателеДокументов.Получатель);
	СвойстваУведомления.Вставить("ВидУслуги", 	Неопределено);
	СвойстваУведомления.Вставить("ТипСверки", 	Неопределено);
	
	Возврат СвойстваПолучателяДокументовУведомления(УведомлениеОПолучателеДокументов.Организация,, СвойстваУведомления);
	
КонецФункции

&НаСервереБезКонтекста
Функция СвойстваПолучателяДокументовУведомления(ОрганизацияПолучательДокументов, ПараметрыУведомления = Неопределено, СвойстваУведомления = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("НаименованиеПолучателяДокументов", 				"");
	Результат.Вставить("ИННПолучателяДокументов", 						"");
	Результат.Вставить("КПППолучателяДокументов", 						"");
	Результат.Вставить("ПолучательДокументовЯвляетсяФизическимЛицом", 	Ложь);
	Результат.Вставить("ФамилияПолучателяДокументов", 					"");
	Результат.Вставить("ИмяПолучателяДокументов", 						"");
	Результат.Вставить("ОтчествоПолучателяДокументов", 					"");
	Результат.Вставить("ИдентификаторАбонента", 						"");
	Результат.Вставить("ОтпечатокСертификатаДляШифрования", 			"");
	Результат.Вставить("ФамилияДолжностногоЛица", 						"");
	Результат.Вставить("ИмяДолжностногоЛица", 							"");
	Результат.Вставить("ОтчествоДолжностногоЛица", 						"");
	Результат.Вставить("ЭтоПолучательДокументовИзДоверенности", 		Ложь);
	Результат.Вставить("ВидУслуги", 									Неопределено);
	КонтекстМоделиХраненияКлюча(ОрганизацияПолучательДокументов, Результат);
	
	Если НЕ ЗначениеЗаполнено(ОрганизацияПолучательДокументов) Тогда
		Возврат Результат;
	КонецЕсли;
	
	// определяем реквизиты получателя документов из свойств организации или параметров доверенности
	
	СведенияОбОрганизации = ПараметрыУведомления;
	Если СведенияОбОрганизации = Неопределено Тогда
		СведенияОбОрганизации = СформироватьПараметрыЗапроса(СвойстваУведомления);
		Если СведенияОбОрганизации = Неопределено Тогда
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	ЭтоПолучательДокументовИзДоверенности = Ложь;
	
	Если СведенияОбОрганизации.ПрПодп = 2 Тогда
		СвПред = СведенияОбОрганизации.Доверенность.СвУпПред.СвПред;
		
		Если СвПред.Свойство("СвОрг") Тогда
			ЭтоПолучательДокументовИзДоверенности = Истина;
			
			Результат.ПолучательДокументовЯвляетсяФизическимЛицом 	= Ложь;
			Результат.НаименованиеПолучателяДокументов 				= СокрЛП(СвПред.СвОрг.НаимОрг);
			Результат.ИННПолучателяДокументов 						= СокрЛП(СвПред.СвОрг.ИННЮЛ);
			Результат.КПППолучателяДокументов 						= СокрЛП(СвПред.СвОрг.КПП);
			
		ИначеЕсли НЕ СвПред.СведФизЛ.ЯвляетсяСотрудникомОрганизации Тогда
			ЭтоПолучательДокументовИзДоверенности = Истина;
			
			Результат.ПолучательДокументовЯвляетсяФизическимЛицом 	= Истина;
			Результат.ИННПолучателяДокументов 						= СокрЛП(СвПред.СведФизЛ.ИННФЛ);
			Результат.ФамилияПолучателяДокументов 					= СвПред.СведФизЛ.ФИО.Фамилия;
			Результат.ИмяПолучателяДокументов 						= СвПред.СведФизЛ.ФИО.Имя;
			Результат.ОтчествоПолучателяДокументов 					= СвПред.СведФизЛ.ФИО.Отчество;
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ ЭтоПолучательДокументовИзДоверенности Тогда
		Результат.ПолучательДокументовЯвляетсяФизическимЛицом = СведенияОбОрганизации.ЭтоПБОЮЛ;
		
		Если НЕ СведенияОбОрганизации.ЭтоПБОЮЛ Тогда
			Результат.НаименованиеПолучателяДокументов 	= СокрЛП(СведенияОбОрганизации.НаимЮЛПол);
			Результат.ИННПолучателяДокументов 			= СокрЛП(СведенияОбОрганизации.ИННЮЛ);
			Результат.КПППолучателяДокументов 			= СокрЛП(СведенияОбОрганизации.КППЮЛ);
			
		Иначе
			Результат.ИННПолучателяДокументов 		= СокрЛП(СведенияОбОрганизации.ИННФЛ);
			Результат.ФамилияПолучателяДокументов 	= СведенияОбОрганизации.ИПФамилия;
			Результат.ИмяПолучателяДокументов 		= СведенияОбОрганизации.ИПИмя;
			Результат.ОтчествоПолучателяДокументов 	= СведенияОбОрганизации.ИПОтчество;
		КонецЕсли;
	КонецЕсли;
	
	Результат.ЭтоПолучательДокументовИзДоверенности = ЭтоПолучательДокументовИзДоверенности;
	
	// определяем должностное лицо получателя документов из сертификата шифрования учетной записи
	
	СертификатыУчетнойЗаписи = СертификатыУчетнойЗаписиПриОбменеЧерезОператора(ОрганизацияПолучательДокументов);
	ЗаполнитьЗначенияСвойств(Результат, СертификатыУчетнойЗаписи);
	
	Если НЕ ЭтоЛокальнаяПодпись(Результат) И ЗначениеЗаполнено(Результат.ОтпечатокСертификатаДляШифрования) Тогда
		Если ЭтоПодписьСервиса(Результат) Тогда
			СвойстваСертификатаДляШифрования = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(Результат.ОтпечатокСертификатаДляШифрования);
		Иначе
			СвойстваСертификатаДляШифрования = ПолучитьСвойстваОблачногоСертификата(Результат.ОтпечатокСертификатаДляШифрования);
		КонецЕсли;
		Если СвойстваСертификатаДляШифрования <> Неопределено Тогда
			ФамилияИмяОтчествоДолжностногоЛица = ФамилияИмяОтчествоИзСвойстваСертификата(СвойстваСертификатаДляШифрования.Владелец);
			Результат.ФамилияДолжностногоЛица 	= ФамилияИмяОтчествоДолжностногоЛица.Фамилия;
			Результат.ИмяДолжностногоЛица 		= ФамилияИмяОтчествоДолжностногоЛица.Имя;
			Результат.ОтчествоДолжностногоЛица 	= ФамилияИмяОтчествоДолжностногоЛица.Отчество;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция СертификатыДолжностныхЛицПолучателяДокументов(УведомлениеОПолучателеДокументовСсылка)
	
	СертификатыУчетнойЗаписи = Неопределено;
	
	СвойстваУведомления = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(УведомлениеОПолучателеДокументовСсылка,
		"Организация, ЗаданиеЯвноРеквизитовПолучателяДокументов");
	
	Если СвойстваУведомления.ЗаданиеЯвноРеквизитовПолучателяДокументов <> Истина И ЗначениеЗаполнено(СвойстваУведомления.Организация) Тогда
		СертификатыУчетнойЗаписи = СертификатыУчетнойЗаписиПриОбменеЧерезОператора(СвойстваУведомления.Организация);
	КонецЕсли;
	
	Возврат СертификатыУчетнойЗаписи;
	
КонецФункции

&НаСервереБезКонтекста
Функция СертификатыУчетнойЗаписиПриОбменеЧерезОператора(Организация) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ИдентификаторАбонента", 					"");
	Результат.Вставить("ОтпечатокСертификатаРуководителя", 			"");
	Результат.Вставить("ОтпечатокСертификатаГлавногоБухгалтера", 	"");
	Результат.Вставить("ОтпечатокСертификатаДляШифрования", 		"");
	
	МестоХраненияКлюча = Неопределено;
	СвойстваОрганизации = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Организация, "ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена");
	
	Если СвойстваОрганизации.ВидОбменаСКонтролирующимиОрганами = Перечисления.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате
		И ЗначениеЗаполнено(СвойстваОрганизации.УчетнаяЗаписьОбмена) Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст =
			"ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	УчетныеЗаписиДокументооборота.ОбменНапрямую КАК ОбменНапрямую,
			|	УчетныеЗаписиДокументооборота.ИдентификаторАбонента КАК ИдентификаторАбонента
			|ИЗ
			|	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
			|ГДЕ
			|	УчетныеЗаписиДокументооборота.Ссылка = &УчетнаяЗаписьДокументооборота";
		Запрос.УстановитьПараметр("УчетнаяЗаписьДокументооборота", СвойстваОрганизации.УчетнаяЗаписьОбмена);
		
		Выборка = Запрос.Выполнить().Выбрать();
		Если Выборка.Следующий() И НЕ Выборка.ОбменНапрямую Тогда
			
			Сертификат = Мультирежим.СертификатПользователя(
				СвойстваОрганизации.УчетнаяЗаписьОбмена, 
				"Отпечаток, МестоХраненияКлюча");
				
			Если НЕ Сертификат.Найден Тогда
				Возврат Результат;
			КонецЕсли;
				
			Результат.ИдентификаторАбонента 					= Строка(Выборка.ИдентификаторАбонента);
			Результат.ОтпечатокСертификатаРуководителя 			= Строка(Сертификат.Отпечаток);
			Результат.ОтпечатокСертификатаГлавногоБухгалтера 	= Строка(Сертификат.Отпечаток);
			Результат.ОтпечатокСертификатаДляШифрования 		= Строка(Сертификат.Отпечаток);
			МестоХраненияКлюча = Сертификат.МестоХраненияКлюча;
		КонецЕсли;
		
	КонецЕсли;
	
	КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, Результат);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ФамилияИмяОтчествоВладельцаСертификата(СвойстваСертфиката) Экспорт
	
	Возврат ФамилияИмяОтчествоИзСвойстваСертификата(СвойстваСертфиката.Владелец);
	
КонецФункции

&НаСервереБезКонтекста
Функция ФамилияИмяОтчествоИзСвойстваСертификата(СвойствоСертификата)
	
	СвойствоСертификатаПриведенное = СокрЛП(СвойствоСертификата);
	ЭлементыСвойстваСертификата = РазобратьСтрокуСвойств(СвойствоСертификатаПриведенное);
	
	ФамилияИзСвойства = СокрЛП(ЭлементыСвойстваСертификата["SN"]);
	Если ЭлементыСвойстваСертификата.Получить("G") <> Неопределено Тогда
		ИмяОтчествоИзСвойства = СокрЛП(ЭлементыСвойстваСертификата["G"]);
	Иначе
		ИмяОтчествоИзСвойства = СокрЛП(ЭлементыСвойстваСертификата["GN"]);
	КонецЕсли;
	
	ФамилияИмяОтчествоСтрокой = СтрЗаменить(ФамилияИзСвойства + " " + ИмяОтчествоИзСвойства, "_", " ");
	
	Возврат РегламентированнаяОтчетность.РазложитьФИО(ФамилияИмяОтчествоСтрокой);
	
КонецФункции

#КонецОбласти

&НаКлиентеНаСервереБезКонтекста
Функция ИмяФайлаЭПФайлаОписиИсходящихДокументов(Знач ИмяФайла) Экспорт
	
	Возврат ДокументооборотСКОКлиентСервер.ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИмяФайла);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ИсправленнноеИмяФайлаЭПФайлаОписиИсходящихДокументов(Знач ИдФайлЭП, ИДФайла = "") Экспорт
	
	Если ВРЕГ(Прав(ИдФайлЭП, 7)) = "SGN.SGN" Тогда
		
		ИмяФайлаДанных = Сред(ИдФайлЭП, 1, СтрДлина(ИдФайлЭП) - 7) + ".xml";
		Возврат ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИмяФайлаДанных);

	ИначеЕсли СтрНайти(ВРЕГ(ИдФайлЭП), "_SGN_") Тогда
		Возврат ИдФайлЭП
	ИначеЕсли ЗначениеЗаполнено(ИДФайла) Тогда
		Возврат ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИДФайла);
	Иначе
		Возврат ИдФайлЭП;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуДиагностикиОтчетности(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ДиагностикаОтчетности_ГлавноеОкно",,, Новый УникальныйИдентификатор, Окно);
	
КонецПроцедуры

&НаСервере
Функция ПрименяетсяФорматОтветаНаТребованиеПояснений_5_02() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПрименяетсяФорматОтветаНаТребованиеПояснений_5_02();
	
КонецФункции

#Область ПредупредитьОПроблемах1СОтчетности

&НаКлиенте
Процедура ПредупредитьОПроблемах1СОтчетностиКлиент() Экспорт
	
	// После предупреждения о переходах показываем предупреждение об ошибках расшифровки ЭДОК
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПредупредитьОбОшибкахРасшифровкиЭДОК",
		ЭтотОбъект);
	
	ПредупредитьОПереходеВКоробкуИОбратно(ОписаниеОповещения);
	
КонецПроцедуры

#Область ПредупреждениеОПереходе

&НаКлиенте
Процедура ПредупредитьОПереходеВКоробкуИОбратно(ВыполняемоеОповещение)
	
	РезультатПроверки = РезультатПроверкиНеобходимостиПредупрежденияОПереходе();
	
	Если РезультатПроверки.НеобходимоПредупредить Тогда
		
		ОткрытьФорму(
			РезультатПроверки.ИмяФормыПредупреждения,
			РезультатПроверки,
			,
			,
			,
			,
			ВыполняемоеОповещение);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;

КонецПроцедуры

&НаСервере
Функция РезультатПроверкиНеобходимостиПредупрежденияОПереходе()
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганами.ИсправитьРегистрационныеНомераМЧДФНС502();
	
	КонтекстЭДОСервер  			= ДокументооборотСКО.ПолучитьОбработкуЭДО();
	БылВыполненПереход 			= КонтекстЭДОСервер.БылВыполненПререходВДругуюМодельХраненияКлючей();
	ЕстьАктивныеУчетныеЗаписи 	= КонтекстЭДОСервер.ЭлектронныйДокументооборотИспользуется();
	
	Если НЕ БылВыполненПереход ИЛИ НЕ ЕстьАктивныеУчетныеЗаписи Тогда
		Возврат Новый Структура("НеобходимоПредупредить", Ложь);
	КонецЕсли;
	
	ТекущаяМодель 			= КонтекстЭДОСервер.ТекущаяМодельРаботыСКлючами();
	ТолькоЛокальныеКлючи 	= Перечисления.МодельРаботыСКлючами.ТолькоЛокальныеКлючи; 
	ЭтоПереходВКоробку 		= ТекущаяМодель = ТолькоЛокальныеКлючи;
	
	ИмяФормыПредупреждения 	= ПутьКОбъекту + ".Форма.ПредупреждениеОПереходе";
	
	ТаблицаУчетныхЗаписей  = КонтекстЭДОСервер.ТаблицаОрганизацийТребующихНапоминанияОПереходе();
	АдресТаблицы 		   = ПоместитьВоВременноеХранилище(ТаблицаУчетныхЗаписей, Новый УникальныйИдентификатор);
	
	НеобходимоПредупредить = ТаблицаУчетныхЗаписей.Количество() > 0;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НеобходимоПредупредить", 	НеобходимоПредупредить);
	ДополнительныеПараметры.Вставить("ИмяФормыПредупреждения", 	ИмяФормыПредупреждения);
	ДополнительныеПараметры.Вставить("ЭтоПереходВКоробку", 		ЭтоПереходВКоробку);
	ДополнительныеПараметры.Вставить("АдресТаблицы", 			АдресТаблицы);
	 
	Возврат ДополнительныеПараметры;
	
КонецФункции

#КонецОбласти

#Область ПредупредитьОбОшибкахРасшифровкиЭДОК

&НаКлиенте
Процедура ПредупредитьОбОшибкахРасшифровкиЭДОК(Результат, ВходящийКонтекст) Экспорт
	
	// После предупреждения об ошибках расшифровки ЭДОК показываем предупреждение об УЦ АО "Калуга-Астрал"
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПредупредитьОбУЦКалугаАстралКлиент",
		ЭтотОбъект);
	
	АдресОтчетов = АдресОтчетовЭДОКСОшибкамиРасшифровки();
	
	Если АдресОтчетов <> Неопределено Тогда
		ПараметрыОткрытияФормы = Новый Структура("АдресОтчетов", АдресОтчетов);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ПредупреждениеОбОшибкеРасшифровкиЭДОК",
			ПараметрыОткрытияФормы,,,,,
			ОписаниеОповещения);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция АдресОтчетовЭДОКСОшибкамиРасшифровки() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.АдресОтчетовЭДОКСОшибкамиРасшифровки();
	
КонецФункции

#КонецОбласти

#Область ПредупредитьОбУЦКалугаАстрал

&НаКлиенте
Процедура ПредупредитьОбУЦКалугаАстралКлиент(Результат, ВходящийКонтекст) Экспорт
	
	// После предупреждения об УЦ "Калуга Астрал" можем показать предупреждение о сертификатах-лицензиях-реквизитах
	ОповещениеОЗавершении = Новый ОписаниеОповещения(
		"ПредупредитьОбИспользованииКлючаДляСФР",
		ЭтотОбъект);
	
	Если НЕ НапомнитьПроУЦКалугаАстрал() Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения("ПредупредитьОбУЦКалугаАстралПослеПолученияСертификатов",
		ЭтотОбъект, ДополнительныеПараметры);
	
	ХранилищаСертификатов = Новый Массив;
	ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Истина));
	ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Ложь));
	
	ДополнительныеПараметрыМетода = Новый Структура;
	ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
	ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
	
	КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, ХранилищаСертификатов, ДополнительныеПараметрыМетода);
	
КонецПроцедуры

&НаСервере
Функция НапомнитьПроУЦКалугаАстрал() Экспорт
	
	ДатаСеанса = ТекущаяДатаСеанса();
	
	ДатаКогдаНапомнить = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_УЦКалугаАстрал");
	
	Если ДатаКогдаНапомнить = Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	ОтложеноНаНикогда = ДатаКогдаНапомнить = '00010101000000';
	
	Возврат ДатаСеанса >= ДатаКогдаНапомнить И НЕ ОтложеноНаНикогда;
	
КонецФункции

&НаКлиенте
Процедура ПредупредитьОбУЦКалугаАстралПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;

	Организации = ОрганизацииИспользующиеСертификатУЦКалугаАстрал(Результат.Сертификаты);
	Количество  = Организации.Количество();

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организации", Организации);
	
	Если Количество = 0 Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьВызовСервера.ИспользуетсяОднаОрганизация() Тогда
		
		Организация = ДополнительныеПараметры.Организации[0].Организация;
		
		КонтекстОткрытияЗаявления = Новый Структура;
		КонтекстОткрытияЗаявления.Вставить("Организация", 			Организация);
		КонтекстОткрытияЗаявления.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПредупредитьОбУЦКалугаАстралПослеПолученияСертификатов_ОткрытьЗаявление",
			ЭтотОбъект,
			КонтекстОткрытияЗаявления);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеУЦКалугаАстралПоОрганизации",
			ДополнительныеПараметры,,,,,
			ОписаниеОповещения);
		
	Иначе
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеУЦКалугаАстралПоОрганизациям",
			ДополнительныеПараметры,,,,,
			ВходящийКонтекст.ОповещениеОЗавершении);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбУЦКалугаАстралПослеПолученияСертификатов_ОткрытьЗаявление(
		Результат,
		ВходящийКонтекст) Экспорт
	
	Если Результат = НСтр("ru = 'ПодготовитьЗаявление'") Тогда
		
		ОткрытьНовоеЗаявлениеУЦКалугаАстрал(
			Неопределено,
			ВходящийКонтекст.Организация,
			ВходящийКонтекст.ОповещениеОЗавершении);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ОрганизацииИспользующиеСертификатУЦКалугаАстрал(ВсеСертификаты) Экспорт
	
	ТекДата = ТекущаяДатаСеанса();
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ОтпечаткиУчеткиОрганизации = КонтекстЭДОСервер.ДанныеУчетныхЗаписейПользователяСОрганизациями(Ложь);
	ОтпечаткиОрганизаций = ОтпечаткиОрганизаций(ОтпечаткиУчеткиОрганизации);
	
	Организации = Новый Массив;
	УЦКалугаАстралАккредитован = Неопределено;
	Для каждого Сертификат Из ВсеСертификаты Цикл
		
		ОтпечатокУчеткаОрганизация = ОтпечаткиУчеткиОрганизации.Найти(Сертификат.Отпечаток);
		Если ОтпечатокУчеткаОрганизация = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если НЕ КонтекстЭДОСервер.СертификатНедействителен(Сертификат.Поставщик, Сертификат.ДействителенС,
			УЦКалугаАстралАккредитован) Тогда
			Продолжить;
		КонецЕсли;
		
		// было ли отправлено заявление течение 10 последних дней
		БылоСоздано = КонтекстЭДОСервер.ЗаявлениеАбонентаДляЗаменыСертификатаБылоСоздано(Сертификат.Отпечаток, ТекДата);
		Если БылоСоздано Тогда
			Продолжить;
		КонецЕсли;
		
		Организация = ОтпечатокУчеткаОрганизация.Организация;
		
		Результат = Новый Структура();
		Результат.Вставить("Организация", Организация);
		Результат.Вставить("Отпечаток",   Сертификат.Отпечаток);
		
		Организации.Добавить(Результат);
		
	КонецЦикла;
	
	Возврат Организации;
	
КонецФункции

&НаКлиенте
Процедура ОтложитьПоказПредупрежденияУЦКалугаАстрал(Форма) Экспорт
	
	СохранитьДатуНапоминанияУЦКалугаАстрал(Форма.НапомнитьЧерез);
	
	Если Форма.НапомнитьЧерез = "-1" Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтложитьПоказПредупрежденияУЦКалугаАстрал_Завершение", 
			ЭтотОбъект,
			Форма);
			
		Текст = НСтр("ru = 'Вы всегда можете отправить заявление на переиздание сертификата из раздела ""Настройки"" формы ""1С-Отчетность"".'");
		ПоказатьПредупреждение(ОписаниеОповещения, Текст);
		
	Иначе
		Форма.Закрыть();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СохранитьДатуНапоминанияУЦКалугаАстрал(НапомнитьЧерез) 
	
	// Определяем, когда в следующий раз нужно напоминать пользователю
	ДатаНапоминания = НачалоДня(ТекущаяДатаСеанса());
	Если НапомнитьЧерез = "1" Тогда
		ДатаНапоминания = ДатаНапоминания + 24 * 60 * 60 * 7;
	ИначеЕсли НапомнитьЧерез = "2" Тогда
		ДатаНапоминания = ДобавитьМесяц(ДатаНапоминания, 1);
	ИначеЕсли НапомнитьЧерез = "-1" Тогда
		ДатаНапоминания = '00010101000000';
	Иначе
		ДатаНапоминания = ДатаНапоминания + 24 * 60 * 60;
	КонецЕсли;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_УЦКалугаАстрал", , ДатаНапоминания);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтложитьПоказПредупрежденияУЦКалугаАстрал_Завершение(Форма) Экспорт
	
	Форма.Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЗаявлениеНаПереизданиеСертификатаУЦКалугаАстрал(Форма, ТекущиеДанные) Экспорт
	
	Если ЗначениеЗаполнено(ТекущиеДанные.Заявление) Тогда 
		
		Заявление = ТекущиеДанные.Заявление;
		ПоказатьЗначение(, Заявление);
		
	Иначе
		
		Организация = ТекущиеДанные.Организация;
		ОткрытьНовоеЗаявлениеУЦКалугаАстрал(Форма, Организация);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНовоеЗаявлениеУЦКалугаАстрал(Форма, Организация, ОповещениеОЗавершении = Неопределено)
	
	ПараметрыОткрытияФормы = Новый Структура();
	ПараметрыОткрытияФормы.Вставить("ПриОткрытииЗапрошеноПродлениеСертификата", Истина);
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
		Организация,
		Форма,,
		ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
		ПараметрыОткрытияФормы,
		ОповещениеОЗавершении);
	
КонецПроцедуры

#КонецОбласти

#Область ПредупредитьОбАстралМ

&НаКлиенте
Процедура ПредупредитьОбАстралМКлиент() Экспорт
	
	Если НЕ НапомнитьПроАстралМ() Тогда
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"ПредупредитьОбАстралМПослеПолученияСертификатов", ЭтотОбъект);
	
	ХранилищаСертификатов = Новый Массив;
	ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Истина));
	ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Ложь));
	
	ДополнительныеПараметрыМетода = Новый Структура;
	ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
	ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
	
	КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, ХранилищаСертификатов, ДополнительныеПараметрыМетода);
	
КонецПроцедуры

&НаСервере
Функция НапомнитьПроАстралМ() Экспорт
	
	ДатаСеанса = ТекущаяДатаСеанса();
	Это2021    = Год(ДатаСеанса) = 2021;
	Если НЕ Это2021 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ДатаКогдаНапомнить = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_АстралМ");
	
	Если ДатаКогдаНапомнить = Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	ОтложеноНаНикогда = ДатаКогдаНапомнить = '00010101000000';
	
	Возврат ДатаСеанса >= ДатаКогдаНапомнить И НЕ ОтложеноНаНикогда;

КонецФункции

&НаКлиенте
Процедура ПредупредитьОбАстралМПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		Возврат;
	КонецЕсли;

	Организации = ОрганизацииИспользующиеСертификатАстралМ(Результат.Сертификаты);
	Количество  = Организации.Количество();

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организации", Организации);
	
	Если Количество = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьВызовСервера.ИспользуетсяОднаОрганизация() Тогда
		
		Организация = Организации[0].Организация;
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПредупредитьОбАстралМПослеПолученияСертификатов_ОткрытьЗаявление",
			ЭтотОбъект, 
			Организация);
			
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеАстралМПоОрганизации",
			ДополнительныеПараметры,
			,
			,
			,
			,
			ОписаниеОповещения);
		
	Иначе
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеАстралМПоОрганизациям",
			ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбАстралМПослеПолученияСертификатов_ОткрытьЗаявление(Результат, Организация) Экспорт
	
	Если Результат = НСтр("ru = 'ПодготовитьЗаявление'") Тогда
		
		ОткрытьНовоеЗаявлениеАстралМ(Неопределено, Организация);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПолноеПредупреждениеОбАстралМ(ВЕдинственномЧисле) Экспорт
	
	Текст = НСтр("ru = 'В соответствии с Федеральным законом № 63 «Об электронной подписи» (в ред. от 27.12.2019 № 476-ФЗ) с 1 января 2022 г. допускается применение лишь тех сертификатов, которые изданы удостоверяющими центрами, аккредитованными по новым правилам.
                  |
                  |Удостоверяющий центр АО ""Калуга Астрал"" аккредитован по новым правилам в июле 2021 г.
                  |
                  |'");
	
	Если ВЕдинственномЧисле Тогда
		Текст = Текст + НСтр("ru = 'Ваш действующий сертификат издан удостоверяющим центром ООО ""АСТРАЛ-М"" и прекратит свое действие 31.12.2021.'");
	Иначе
		Текст = Текст + НСтр("ru = 'Ваши действующие сертификаты изданы удостоверяющим центром ООО ""АСТРАЛ-М"" и прекратит свое действие 31.12.2021.'");
	КонецЕсли;
		
	ПоказатьПредупреждение(, Текст);
	
КонецПроцедуры

&НаСервере
Функция ОрганизацииИспользующиеСертификатАстралМ(ВсеСертификаты) Экспорт
	
	ТекДата = ТекущаяДатаСеанса();
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ОтпечаткиУчеткиОрганизации = КонтекстЭДОСервер.ДанныеУчетныхЗаписейПользователяСОрганизациями(Ложь);
	ОтпечаткиОрганизаций = ОтпечаткиОрганизаций(ОтпечаткиУчеткиОрганизации);
	
	Организации = Новый Массив;
	Для каждого Сертификат Из ВсеСертификаты Цикл
		
		ОтпечатокУчеткаОрганизация = ОтпечаткиУчеткиОрганизации.Найти(Сертификат.Отпечаток);
		Если ОтпечатокУчеткаОрганизация = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ПоставщикСтруктура = Сертификат.ПоставщикСтруктура;
		УспеютПеревыпуститьДо2022 = Сертификат.ДействителенПо < Дата(2022, 1, 1);
		
		Если НЕ ПоставщикСтруктура.Свойство("O")
			ИЛИ УспеютПеревыпуститьДо2022 Тогда
			Продолжить;
		КонецЕсли;
		
		Поставщик = ПоставщикСтруктура.O;
		ЭтоАстралМ = СтрНайти(ВРег(Поставщик), "АСТРАЛ-М");
		Если НЕ ЭтоАстралМ Тогда
			Продолжить;
		КонецЕсли;
		
		// было ли отправлено заявление течение 10 последних дней
		БылоСоздано = КонтекстЭДОСервер.ЗаявлениеАбонентаДляЗаменыСертификатаБылоСоздано(Сертификат.Отпечаток, ТекДата);
		Если БылоСоздано Тогда
			Продолжить;
		КонецЕсли;
		
		Организация = ОтпечатокУчеткаОрганизация.Организация;
		
		Результат = Новый Структура();
		Результат.Вставить("Организация", Организация);
		Результат.Вставить("Отпечаток",   Сертификат.Отпечаток);
		
		Организации.Добавить(Результат);
		
	КонецЦикла;
	
	Возврат Организации;
	
КонецФункции

&НаСервере
Функция ОтпечаткиОрганизаций(ОтпечаткиУчеткиОрганизации) Экспорт
	
	Отпечатки = Новый Массив;
	Для каждого Запись Из ОтпечаткиУчеткиОрганизации Цикл
		Отпечатки.Добавить(Запись.ОтпечатокСертификата);
	КонецЦикла;
	
	Возврат Отпечатки;
	
КонецФункции

&НаКлиенте
Процедура ОтложитьПоказПредупрежденияАстралМ(Форма) Экспорт
	
	СохранитьДатуНапоминанияАстралМ(Форма.НапомнитьЧерез);
	
	Если Форма.НапомнитьЧерез = "-1" Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтложитьПоказПредупрежденияАстралМ_Завершение", 
			ЭтотОбъект,
			Форма);
			
		Текст = НСтр("ru = 'Вы всегда можете отправить заявление на переиздание сертификата из раздела ""Настройки"" формы ""1С-Отчетность"".'");
		ПоказатьПредупреждение(ОписаниеОповещения, Текст);
		
	Иначе
		Форма.Закрыть();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СохранитьДатуНапоминанияАстралМ(НапомнитьЧерез) 
	
	// Определяем, когда в следующий раз нужно напоминать пользователю
	ДатаНапоминания = НачалоДня(ТекущаяДатаСеанса());
	Если НапомнитьЧерез = "1" Тогда
		ДатаНапоминания = ДатаНапоминания + 24 * 60 * 60 * 7;
	ИначеЕсли НапомнитьЧерез = "2" Тогда
		ДатаНапоминания = ДобавитьМесяц(ДатаНапоминания, 1);
	ИначеЕсли НапомнитьЧерез = "-1" Тогда
		ДатаНапоминания = '00010101000000';
	Иначе
		ДатаНапоминания = ДатаНапоминания + 24 * 60 * 60;
	КонецЕсли;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_АстралМ", , ДатаНапоминания);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтложитьПоказПредупрежденияАстралМ_Завершение(Форма) Экспорт
	
	Форма.Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЗаявлениеНаПереизданиеСертификатаАстралМ(Форма, ТекущиеДанные) Экспорт
	
	Если ЗначениеЗаполнено(ТекущиеДанные.Заявление) Тогда 
		
		Заявление = ТекущиеДанные.Заявление;
		ПоказатьЗначение(, Заявление); 
			
	Иначе
			
		Организация = ТекущиеДанные.Организация;
		ОткрытьНовоеЗаявлениеАстралМ(Форма, Организация);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНовоеЗаявлениеАстралМ(Форма, Организация)
	
	ПараметрыОткрытияФормы = Новый Структура();
	ПараметрыОткрытияФормы.Вставить("ПриОткрытииЗапрошеноПродлениеСертификата", Истина);
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
		Организация, 
		Форма,
		,
		ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
		ПараметрыОткрытияФормы);
	
КонецПроцедуры

&НаСервере
Функция ЗаявлениеИСтатусПоОтпечатку(Отпечаток) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЗаявлениеИСтатусПоОтпечатку(Отпечаток);
	
КонецФункции

#КонецОбласти

#Область ПредупредитьОбИспользованииКлючаДляСФР

&НаКлиенте
Процедура ПредупредитьОбИспользованииКлючаДляСФР(Результат, ВходящийКонтекст) Экспорт
	
	// После предупреждения об использовании ключа для СФР можем показать предупреждение о
	// сертификатах-лицензиях-реквизитах
	ОповещениеОЗавершении = Новый ОписаниеОповещения(
		"ПредупредитьОГОСТ34122018ДляФНС",
		ЭтотОбъект);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения("ПредупредитьОбИспользованииКлючаДляСФРПослеПолученияСертификатов",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("ПолучитьСертификатыРезультат") Тогда
		ВыполнитьОбработкуОповещения(Оповещение, Результат.ПолучитьСертификатыРезультат);
		
	Иначе
		ХранилищаСертификатов = Новый Массив;
		ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Истина));
		ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Ложь));
		
		ДополнительныеПараметрыМетода = Новый Структура;
		ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
		ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
		
		КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, ХранилищаСертификатов, ДополнительныеПараметрыМетода);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция НапомнитьПроСФРИспользованиеКлюча() Экспорт
	
	ДатаСеанса = ТекущаяДатаСеанса();
	
	ДатаКогдаНапомнить = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_СФРИспользованиеКлюча");
	
	Если ДатаКогдаНапомнить = Неопределено Тогда
		Возврат Истина;
	КонецЕсли;
	
	ОтложеноНаНикогда = ДатаКогдаНапомнить = '00010101000000';
	
	Возврат ДатаСеанса >= ДатаКогдаНапомнить И НЕ ОтложеноНаНикогда;
	
КонецФункции

&НаКлиенте
Процедура ПредупредитьОбИспользованииКлючаДляСФРПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	СведенияОрганизаций = ОрганизацииИспользующиеСертификатСНевернымИспользованиемКлючаДляСФР(Результат.Сертификаты);
	ПараметрыПриложения["БРО.ПредупредитьОбИспользованииКлючаДляСФР"] = СведенияОрганизаций.Организации;
	Оповестить("ПредупредитьОбИспользованииКлючаДляСФР");
	
	Если НЕ СведенияОрганизаций.НапомнитьПроСФРИспользованиеКлюча Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ПараметрыФормыПредупреждения = Новый Структура();
	ПараметрыФормыПредупреждения.Вставить("Организации", СведенияОрганизаций.Организации);
	
	Если СведенияОрганизаций.Организации.Количество() = 0 Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Если РегламентированнаяОтчетностьВызовСервера.ИспользуетсяОднаОрганизация() Тогда
		КонтекстОткрытияЗаявления = Новый Структура;
		КонтекстОткрытияЗаявления.Вставить("Организация", ПараметрыФормыПредупреждения.Организации[0].Организация);
		КонтекстОткрытияЗаявления.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПредупредитьОбИспользованииКлючаДляСФПослеСФРИспользованиеКлюча",
			ЭтотОбъект,
			КонтекстОткрытияЗаявления);
		
		Если ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеСФРИспользованиеКлючаПоОрганизации",
			ПараметрыФормыПредупреждения,,,,,
			ОписаниеОповещения) = Неопределено Тогда
			
			РезультатВыполнения = Новый Структура;
			РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
			Возврат;
		КонецЕсли;
		
	Иначе
		Если ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеСФРИспользованиеКлючаПоОрганизациям",
			ПараметрыФормыПредупреждения,,,,,
			ВходящийКонтекст.ОповещениеОЗавершении) = Неопределено Тогда
			
			РезультатВыполнения = Новый Структура;
			РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбИспользованииКлючаДляСФПослеСФРИспользованиеКлюча(
		Результат,
		ВходящийКонтекст) Экспорт
	
	Если ТипЗнч(Результат) = Тип("ДокументСсылка.ЗаявлениеАбонентаСпецоператораСвязи") Тогда
		ПоказатьЗначение(ВходящийКонтекст.ОповещениеОЗавершении, Результат);
		
	ИначеЕсли Результат = "ПодготовитьЗаявление" Тогда
		ОткрытьНовоеЗаявлениеСФРИспользованиеКлюча(
			Неопределено,
			ВходящийКонтекст.Организация,
			ВходящийКонтекст.ОповещениеОЗавершении);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ОрганизацииИспользующиеСертификатСНевернымИспользованиемКлючаДляСФР(ВсеСертификаты)
	
	ДатаСеанса = ТекущаяДатаСеанса();
	Пользователь = Пользователи.ТекущийПользователь();
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ОтпечаткиУчеткиОрганизации = КонтекстЭДОСервер.ДанныеУчетныхЗаписейПользователяСОрганизациями(Ложь);
	ОтпечаткиОрганизаций = ОтпечаткиОрганизаций(ОтпечаткиУчеткиОрганизации);
	
	Организации = Новый Массив;
	Для каждого Сертификат Из ВсеСертификаты Цикл
		ОтпечатокУчеткаОрганизация = ОтпечаткиУчеткиОрганизации.Найти(Сертификат.Отпечаток);
		Если ОтпечатокУчеткаОрганизация = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Если НЕ ДокументооборотСКОВызовСервера.УчетнаяЗаписьПредназначенаДляДокументооборотаСПФР(
			ОтпечатокУчеткаОрганизация.УчетнаяЗаписьДокументооборота) Тогда
			Продолжить;
		КонецЕсли;
		
		Если НЕ Мультирежим.ЕстьПравоНаОрганПоОрганизации(
			ОтпечатокУчеткаОрганизация.Организация,
			Перечисления.ТипыКонтролирующихОрганов.ПФР) Тогда
			Продолжить;
		КонецЕсли;
		
		СертификатыЛичные = ОтпечатокУчеткаОрганизация.УчетнаяЗаписьДокументооборота.СертификатыЛичные;
		
		НайденныеСтроки = Новый Массив;
		Если Мультирежим.ЭтоМультиРежим(ОтпечатокУчеткаОрганизация.УчетнаяЗаписьДокументооборота) Тогда
			Отбор = Новый Структура();
			Отбор.Вставить("Пользователь", Пользователь);
			Отбор.Вставить("Текущий", Истина);
			Отбор.Вставить("Отпечаток", Сертификат.Отпечаток);
			
			НайденныеСтроки = СертификатыЛичные.НайтиСтроки(Отбор);
		КонецЕсли;
		
		Если НайденныеСтроки.Количество() = 0 Тогда
			Отбор = Новый Структура();
			Отбор.Вставить("Пользователь", Справочники.Пользователи.ПустаяСсылка());
			Отбор.Вставить("Текущий", Истина);
			Отбор.Вставить("Отпечаток", Сертификат.Отпечаток);
			
			НайденныеСтроки = СертификатыЛичные.НайтиСтроки(Отбор);
		КонецЕсли;
		
		ДанныеСертификата = ?(НайденныеСтроки.Количество() = 0, Неопределено, НайденныеСтроки[0].Содержимое.Получить());
		
		Если ДанныеСертификата = Неопределено ИЛИ ДанныеСертификата.Размер() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		Если ДокументооборотСКОКлиентСервер.СФРИспользованиеКлючаСоответствуетТребованиям(
			ДанныеСертификата, Сертификат.ДействителенПо) <> Ложь Тогда
			
			Продолжить;
		КонецЕсли;
		
		// было ли отправлено заявление в течение 10 последних дней
		БылоСоздано = КонтекстЭДОСервер.ЗаявлениеАбонентаДляЗаменыСертификатаБылоСоздано(Сертификат.Отпечаток, ДатаСеанса);
		Если БылоСоздано Тогда
			Продолжить;
		КонецЕсли;
		
		Организация = ОтпечатокУчеткаОрганизация.Организация;
		
		Результат = Новый Структура();
		Результат.Вставить("Организация", Организация);
		Результат.Вставить("Отпечаток", Сертификат.Отпечаток);
		
		Организации.Добавить(Результат);
	КонецЦикла;
	
	ОтображатьДиалогНапоминанияПослеЗапуска = НапомнитьПроСФРИспользованиеКлюча();
	
	Результат = Новый Структура;
	Результат.Вставить("НапомнитьПроСФРИспользованиеКлюча", ОтображатьДиалогНапоминанияПослеЗапуска);
	Результат.Вставить("Организации", Организации);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ОтложитьПоказПредупрежденияСФРИспользованиеКлюча(Форма) Экспорт
	
	СохранитьДатуНапоминанияСФРИспользованиеКлюча(Форма.НапомнитьЧерез);
	
	Если Форма.НапомнитьЧерез = "-1" Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОтложитьПоказПредупрежденияСФРИспользованиеКлючаПослеПредупреждения",
			ЭтотОбъект,
			Форма);
			
		Текст = НСтр("ru = 'Вы всегда можете отправить заявление на переиздание сертификата из раздела ""Настройки"" формы ""1С-Отчетность"".'");
		ПоказатьПредупреждение(ОписаниеОповещения, Текст);
		
	Иначе
		Форма.Закрыть();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СохранитьДатуНапоминанияСФРИспользованиеКлюча(НапомнитьЧерез) 
	
	// Определяем, когда в следующий раз нужно напоминать пользователю
	ДатаНапоминания = НачалоДня(ТекущаяДатаСеанса());
	Если НапомнитьЧерез = "1" Тогда
		ДатаНапоминания = ДатаНапоминания + 24 * 60 * 60 * 7;
	ИначеЕсли НапомнитьЧерез = "2" Тогда
		ДатаНапоминания = ДобавитьМесяц(ДатаНапоминания, 1);
	ИначеЕсли НапомнитьЧерез = "-1" Тогда
		ДатаНапоминания = '00010101000000';
	Иначе
		ДатаНапоминания = ДатаНапоминания + 24 * 60 * 60;
	КонецЕсли;
	
	ХранилищеОбщихНастроек.Сохранить("ДокументооборотСКонтролирующимиОрганами_СФРИспользованиеКлюча",, ДатаНапоминания);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтложитьПоказПредупрежденияСФРИспользованиеКлючаПослеПредупреждения(Форма) Экспорт
	
	Форма.Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЗаявлениеНаПереизданиеСертификатаСФРИспользованиеКлюча(Форма, ТекущиеДанные) Экспорт
	
	Если ЗначениеЗаполнено(ТекущиеДанные.Заявление) Тогда
		ПоказатьЗначение(, ТекущиеДанные.Заявление);
		
	Иначе
		ОткрытьНовоеЗаявлениеСФРИспользованиеКлюча(Форма, ТекущиеДанные.Организация);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНовоеЗаявлениеСФРИспользованиеКлюча(Форма, Организация, ОповещениеОЗавершении = Неопределено) Экспорт
	
	ПараметрыОткрытияФормы = Новый Структура();
	ПараметрыОткрытияФормы.Вставить("ПриОткрытииЗапрошеноПродлениеСертификата", Истина);
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
		Организация,
		Форма,,
		ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
		ПараметрыОткрытияФормы,
		ОповещениеОЗавершении);
	
КонецПроцедуры

#КонецОбласти

#Область ПредупредитьОГОСТ34122018ДляФНС

&НаКлиенте
Процедура ПредупредитьОГОСТ34122018ДляФНС(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = Новый ОписаниеОповещения(
		"ПредупредитьОбИстеченииСертификатов",
		ЭтотОбъект);
	
	Если НЕ НапомнитьПроГОСТ34122018ДляФНС() Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения("ПредупредитьОГОСТ34122018ДляФНСПослеПолученияСертификатов",
		ЭтотОбъект, ДополнительныеПараметры);
	
	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("ПолучитьСертификатыРезультат") Тогда
		ВыполнитьОбработкуОповещения(Оповещение, Результат.ПолучитьСертификатыРезультат);
		
	Иначе
		ХранилищаСертификатов = Новый Массив;
		ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Истина));
		ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Ложь));
		
		ДополнительныеПараметрыМетода = Новый Структура;
		ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
		ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
		
		КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, ХранилищаСертификатов, ДополнительныеПараметрыМетода);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция НапомнитьПроГОСТ34122018ДляФНС() Экспорт
	
	Если НЕ ПравоДоступа("Чтение", Метаданные.Справочники.УчетныеЗаписиДокументооборота) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ ПЕРВЫЕ 1
		|	УчетныеЗаписиДокументооборота.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Организации КАК Организации
		|		ПО УчетныеЗаписиДокументооборота.Ссылка = Организации.УчетнаяЗаписьОбмена
		|ГДЕ
		|	Организации.ВидОбменаСКонтролирующимиОрганами =
		|		ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
		|	И НЕ Организации.ПометкаУдаления
		|	И УчетныеЗаписиДокументооборота.ПредназначенаДляДокументооборотаСФНС");
	
	ЕстьЛокальнаяПодпись = Ложь;
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		МодельХраненияСертификата = Мультирежим.МодельХраненияСертификатаПользователя(Выборка.Ссылка);
		Если КриптографияЭДКОКлиентСервер.ЭтоЛокальнаяПодпись(МодельХраненияСертификата) Тогда
			ЕстьЛокальнаяПодпись = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ЕстьЛокальнаяПодпись;
	
КонецФункции

&НаКлиенте
Процедура ПредупредитьОГОСТ34122018ДляФНСПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено ИЛИ Результат.Сертификаты.Количество() = 0 Тогда
		РезультатВыполнения = Новый Структура;
		РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", Результат);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	ВходящийКонтекст.Вставить("ПолучитьСертификатыРезультат", Результат);
	
	Оповещение = Новый ОписаниеОповещения("ПредупредитьОГОСТ34122018ДляФНСПослеПолученияАлгоритмовШифрования",
		ЭтотОбъект, ВходящийКонтекст);
	
	КриптографияЭДКОКлиент.ПолучитьАлгоритмыШифрования(
		Оповещение,
		Ложь,,,,
		Новый Структура);
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОГОСТ34122018ДляФНСПослеПолученияАлгоритмовШифрования(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> Неопределено И Результат.Выполнено Тогда
		Для каждого СвойстваАлгоритмаШифрования Из Результат.АлгоритмыШифрования Цикл
			Если ВРег(СвойстваАлгоритмаШифрования.Имя) = "GR 34.12-15 M" Тогда
				РезультатВыполнения = Новый Структура;
				РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", ВходящийКонтекст.ПолучитьСертификатыРезультат);
				ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
				
				Возврат;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	ТипКриптопровайдераИнициализации =
		ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Результат, "ТипКриптопровайдераИнициализации");
	ПараметрыПриложения["БРО.ПредупредитьОГОСТ34122018ДляФНС"] =
		Новый Структура("ТипКриптопровайдераИнициализации", ТипКриптопровайдераИнициализации);
	Оповестить("ПредупредитьОГОСТ34122018ДляФНС");
	
	РезультатВыполнения = Новый Структура;
	РезультатВыполнения.Вставить("ПолучитьСертификатыРезультат", ВходящийКонтекст.ПолучитьСертификатыРезультат);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
	
КонецПроцедуры

#КонецОбласти

#Область ПредупреждениеОбИстеченииСертификатов

&НаКлиенте
Процедура ПредупредитьОбИстеченииСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Оповещение = Новый ОписаниеОповещения(
		"ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияСертификатов", ЭтотОбъект);
	
	Если ТипЗнч(Результат) = Тип("Структура") И Результат.Свойство("ПолучитьСертификатыРезультат") Тогда
		ВыполнитьОбработкуОповещения(Оповещение, Результат.ПолучитьСертификатыРезультат);
		
	Иначе
		ХранилищаСертификатов = Новый Массив;
		ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Истина));
		ХранилищаСертификатов.Добавить(Новый Структура("Хранилище,ЭтоЛокальноеХранилище", "MY", Ложь));
		
		ДополнительныеПараметрыМетода = Новый Структура;
		ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
		ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
		
		КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, ХранилищаСертификатов, ДополнительныеПараметрыМетода);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	ПолныеСвойстваВсехСертификатов = Новый Соответствие;
	Если Результат.Выполнено Тогда
		Для Каждого Сертификат Из Результат.Сертификаты Цикл
			ПолныеСвойстваВсехСертификатов.Вставить(Сертификат.Отпечаток, Сертификат);
		КонецЦикла;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияСертификатов_ПроверитьСостояние", 
		ЭтотОбъект,
		Результат); 
		
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
	ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
	ПараметрыОжидания.Интервал = 1;
	
	ДлительнаяОперация = НачатьОпределениеИзмененныхПараметровПодключения(ПолныеСвойстваВсехСертификатов);
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОписаниеОповещения, ПараметрыОжидания);
	
КонецПроцедуры

&НаСервере
Функция НачатьОпределениеИзмененныхПараметровПодключения(ПолныеСвойстваВсехСертификатов)
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = Определение изменившихся параметров подключения'");
	ПараметрыВыполнения.ОжидатьЗавершение = Истина;
	
	Возврат ДлительныеОперации.ВыполнитьВФоне(
		"ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОпределениеИзмененныхПараметровПодключенияВФоне", 
		ПолныеСвойстваВсехСертификатов, 
		ПараметрыВыполнения);

КонецФункции
	
&НаКлиенте
Процедура ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияСертификатов_ПроверитьСостояние(ДлительнаяОперация, ВходящийКонтекст) Экспорт
	
	Если ДлительнаяОперация <> Неопределено И
		(ДлительнаяОперация.Статус = "Выполнено") Тогда
		
		ИзмененныеПараметры = ПолучитьИзВременногоХранилища(ДлительнаяОперация.АдресРезультата);
		ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияСертификатов_ПослеПолучениеИзменившихсяПараметров(ИзмененныеПараметры, ВходящийКонтекст);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияСертификатов_ПослеПолучениеИзменившихсяПараметров(ИзмененныеПараметры, ВходящийКонтекст) Экспорт
	
	НапомнитьОбновитьКриптопровайдер 	= ИзмененныеПараметры.НапомнитьОбновитьКриптопровайдер;
	ИзменившиесяПараметрыПодключения 	= ИзмененныеПараметры.ИзменившиесяПараметрыПодключения;
	
	Результат = ВходящийКонтекст;
	
	Если НапомнитьОбновитьКриптопровайдер ИЛИ ИзменившиесяПараметрыПодключения.Количество() > 0 Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("НапомнитьОбновитьКриптопровайдер", 	НапомнитьОбновитьКриптопровайдер);
		ДополнительныеПараметры.Вставить("ИзмененныеПараметры", 				ИзменившиесяПараметрыПодключения);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияКриптопровайдеров", ЭтотОбъект, ДополнительныеПараметры);
		
		Если НапомнитьОбновитьКриптопровайдер И Результат.Свойство("МенеджерКриптографии") Тогда
			КриптографияЭДКОКлиент.ПолучитьКриптопровайдеры(Оповещение, Ложь, Ложь, Результат.МенеджерКриптографии);
		Иначе
			ВыполнитьОбработкуОповещения(Оповещение, Новый Структура("Выполнено, ОписаниеОшибки", Ложь, ""));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредупредитьОбИстеченииСертификатовКлиентПослеПолученияКриптопровайдеров(Результат, ВходящийКонтекст) Экспорт
	
	ВходящийКонтекст.Вставить("Криптопровайдеры", ?(Результат.Выполнено, Результат.Криптопровайдеры, Неопределено));
	
	ВходящийКонтекст.Вставить("НомерСтроки", 0);
	
	ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации(ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации(ВходящийКонтекст) Экспорт
	
	ИзмененныеПараметры = ВходящийКонтекст.ИзмененныеПараметры;
	НомерСтроки 		= ВходящийКонтекст.НомерСтроки;
	
	Если НомерСтроки = ИзмененныеПараметры.Количество() Тогда
		ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимости(ВходящийКонтекст);
		
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПослеОпределенияВозможностиБезбумажногоПродленияТекущейОрганизаций", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		СведенияДляБезбумажногоПродления = ИзмененныеПараметры[НомерСтроки].СведенияДляБезбумажногоПродления;
		
		Если СведенияДляБезбумажногоПродления = Неопределено Тогда
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("Выполнено", Ложь);
			
			ПослеОпределенияВозможностиБезбумажногоПродленияТекущейОрганизаций(ДополнительныеПараметры, ВходящийКонтекст);
			
		Иначе
			ВозможноБезбумажноеПродление(ОписаниеОповещения, СведенияДляБезбумажногоПродления);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимости(ВходящийКонтекст)
	
	ПоказатьПредупреждениеОбОбновленииКриптопровайдера = ВходящийКонтекст.НапомнитьОбновитьКриптопровайдер
		И ВходящийКонтекст.Криптопровайдеры <> Неопределено;
	
	ВходящийКонтекст.Вставить("ViPNetCSPГОСТ2001Найден", 	Ложь);
	ВходящийКонтекст.Вставить("ViPNetCSPГОСТ2012Найден", 	Ложь);
	ВходящийКонтекст.Вставить("КриптоПроCSPГОСТ2001Найден", Ложь);
	ВходящийКонтекст.Вставить("КриптоПроCSPГОСТ2012Найден", Ложь);
	ВходящийКонтекст.Вставить("Криптопровайдер", 			Неопределено);
	
	Если ПоказатьПредупреждениеОбОбновленииКриптопровайдера Тогда
		Для каждого Криптопровайдер Из ВходящийКонтекст.Криптопровайдеры Цикл
			Если Криптопровайдер.Имя = "Infotecs GOST 2012/512 Cryptographic Service Provider"
					И Криптопровайдер.Тип = 77
				ИЛИ Криптопровайдер.Имя = "Infotecs GOST 2012/1024 Cryptographic Service Provider"
					И Криптопровайдер.Тип = 78 Тогда
				
				ВходящийКонтекст.ViPNetCSPГОСТ2012Найден = Истина;
				
			ИначеЕсли Криптопровайдер.Имя = "Infotecs Cryptographic Service Provider"
					И Криптопровайдер.Тип = 2 Тогда
				
				ВходящийКонтекст.ViPNetCSPГОСТ2001Найден = Истина;
				
			ИначеЕсли (Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 Cryptographic Service Provider"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 KC1 CSP"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 KC2 CSP"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 KC3 CSP")
					И Криптопровайдер.Тип = 80
				ИЛИ (Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 Strong Cryptographic Service Provider"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 KC1 Strong CSP"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 KC2 Strong CSP"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2012 KC3 Strong CSP")
					И Криптопровайдер.Тип = 81 Тогда
				
				ВходящийКонтекст.КриптоПроCSPГОСТ2012Найден = Истина;
				
			ИначеЕсли (Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2001 Cryptographic Service Provider"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2001 KC1 CSP"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2001 KC2 CSP"
					ИЛИ Криптопровайдер.Имя = "Crypto-Pro GOST R 34.10-2001 KC3 CSP")
					И Криптопровайдер.Тип = 75 Тогда
				
				ВходящийКонтекст.КриптоПроCSPГОСТ2001Найден = Истина;
			КонецЕсли;
		КонецЦикла;
		
		Если ВходящийКонтекст.КриптоПроCSPГОСТ2001Найден ИЛИ ВходящийКонтекст.КриптоПроCSPГОСТ2012Найден Тогда
			ВходящийКонтекст.Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro");
		ИначеЕсли ВходящийКонтекст.ViPNetCSPГОСТ2001Найден ИЛИ ВходящийКонтекст.ViPNetCSPГОСТ2012Найден Тогда
			ВходящийКонтекст.Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet");
		Иначе
			ВходящийКонтекст.Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro");
		КонецЕсли;
	КонецЕсли;
	
	// Для ViPNet CSP 4.0 криптопровайдеры ГОСТ 2012 зарегистрированы, но совместимой поддержки ГОСТ 2012 нет, проверим шифрование
	ТребуетсяДополнительнаяПроверка = ПоказатьПредупреждениеОбОбновленииКриптопровайдера
		И ВходящийКонтекст.Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet")
		И ВходящийКонтекст.ViPNetCSPГОСТ2012Найден И НЕ ЭтоВебКлиент();
	
	Если ТребуетсяДополнительнаяПроверка Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеСозданияМенеджераКриптографии",
			ЭтотОбъект, ВходящийКонтекст);
		
		ПараметрыМодуляКриптографии = КриптографияЭДКОКлиентСервер.КриптопровайдерViPNet("GOST R 34.10-2001");
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", Ложь);
		ДополнительныеПараметры.Вставить("ВыводитьСообщения", Ложь);
		
		КриптографияЭДКОСлужебныйКлиент.СоздатьМенеджерКриптографии2(
			Оповещение,
			ПараметрыМодуляКриптографии,
			ДополнительныеПараметры,,
			Ложь);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла",
			ЭтотОбъект, ВходящийКонтекст);
		
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеСозданияМенеджераКриптографии(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		ВходящийКонтекст.Вставить("МенеджерКриптографии", Результат.МенеджерКриптографии);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеПоискаСертификата",
			ЭтотОбъект, ВходящийКонтекст);
		
		ИнформацияОСертификате = ИнформацияОПромежуточномСертификатеБезопасности();
		ВходящийКонтекст.Вставить("ПромежуточныйСертификатБезопасности64", ИнформацияОСертификате.Данные64);
		
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(
			Оповещение,
			ИнформацияОСертификате.Отпечаток,
			"CA",,
			Ложь,
			ВходящийКонтекст.МенеджерКриптографии);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла",
			ЭтотОбъект, ВходящийКонтекст);
		
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеПоискаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено И Результат.СертификатНайден Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеИмпортаСертификата",
			ЭтотОбъект, ВходящийКонтекст);
		
		РезультатВыполнения = Новый Структура("Выполнено, СвойстваСертификата", Истина, Результат.СвойстваСертификата);
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗаписиФайлаСертификата",
			ЭтотОбъект, ВходящийКонтекст);
		
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(
			Оповещение,
			ВходящийКонтекст.ПромежуточныйСертификатБезопасности64,,
			Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗаписиФайлаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеИмпортаСертификата",
			ЭтотОбъект, ВходящийКонтекст);
		
		КриптографияЭДКОКлиент.ИмпортироватьСертификат(
			Оповещение,
			Результат.ИмяФайла,
			"CA",
			Ложь,
			ВходящийКонтекст.МенеджерКриптографии);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла",
			ЭтотОбъект, ВходящийКонтекст);
		
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеИмпортаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		ВходящийКонтекст.Вставить("СвойстваПромежуточногоСертификатаБезопасности", Результат.СвойстваСертификата);
		
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗаписиФайлаДанных",
			ЭтотОбъект, ВходящийКонтекст);
		
		ОперацииСФайламиЭДКОКлиент.Base64ВФайл(
			Оповещение,
			"MTIzNDU2",,
			Ложь);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла",
			ЭтотОбъект, ВходящийКонтекст);
		
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗаписиФайлаДанных(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗашифрования",
			ЭтотОбъект, ВходящийКонтекст);
		
		МассивСертификатов = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			ВходящийКонтекст.СвойстваПромежуточногоСертификатаБезопасности);
		КриптографияЭДКОКлиент.ЗашифроватьPKCS7(
			Оповещение,
			Результат.ИмяФайла,
			МассивСертификатов,,
			Ложь,
			ВходящийКонтекст.МенеджерКриптографии);
		
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла",
			ЭтотОбъект, ВходящийКонтекст);
		
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗашифрования(Результат, ВходящийКонтекст) Экспорт
	
	Оповещение = Новый ОписаниеОповещения(
		"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла",
		ЭтотОбъект, ВходящийКонтекст);
	
	Если Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.ФайлВBase64(
			Оповещение,
			Результат.ЗашифрованныйФайл,
			Ложь);
		
	Иначе
		РезультатВыполнения = Новый Структура("Выполнено, ОписаниеОшибки", Ложь, "");
		ВыполнитьОбработкуОповещения(Оповещение, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЧтенияЗашифрованногоФайла(Результат, ВходящийКонтекст) Экспорт
	
	ПоказатьПредупреждениеОбОбновленииКриптопровайдера = ВходящийКонтекст.НапомнитьОбновитьКриптопровайдер
		И ВходящийКонтекст.Криптопровайдеры <> Неопределено;
	
	Если ПоказатьПредупреждениеОбОбновленииКриптопровайдера Тогда
		Если ВходящийКонтекст.ViPNetCSPГОСТ2001Найден И ВходящийКонтекст.ViPNetCSPГОСТ2012Найден И Результат.Выполнено Тогда
			СтрокаBase64БезПереводовСтрок = СтрЗаменить(Результат.СтрокаBase64, Символы.ВК, "");
			СтрокаBase64БезПереводовСтрок = СтрЗаменить(СтрокаBase64БезПереводовСтрок, Символы.ПС, "");
			
			Если СтрНайти(СтрокаBase64БезПереводовСтрок, "TGljaW5mb19pdGNz") = 0
			И СтрНайти(СтрокаBase64БезПереводовСтрок, "Y2luZm9faXRj") = 0
			И СтрНайти(СтрокаBase64БезПереводовСтрок, "aWNpbmZvX2l0") = 0 Тогда
				
				// Если нет подстроки "Licinfo_itcs", то это может быть ViPNet CSP 4.0 без совместимой поддержки ГОСТ 2012
				
				ВерсияViPNetCSP = ОбщегоНазначенияЭДКОКлиент.ИнформацияОКриптопровайдерах().ВерсияViPNetCSP;
				Если Лев(ВерсияViPNetCSP, 3) = "4.0" ИЛИ Лев(ВерсияViPNetCSP, 4) = "4.1." Тогда
					ВходящийКонтекст.ViPNetCSPГОСТ2012Найден = Ложь;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		Если (ВходящийКонтекст.Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet")
			И (ВходящийКонтекст.ViPNetCSPГОСТ2012Найден ИЛИ НЕ ВходящийКонтекст.ViPNetCSPГОСТ2001Найден))
			ИЛИ (ВходящийКонтекст.Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro")
			И (ВходящийКонтекст.КриптоПроCSPГОСТ2012Найден ИЛИ НЕ ВходящийКонтекст.КриптоПроCSPГОСТ2001Найден)) Тогда
			ПоказатьПредупреждениеОбОбновленииКриптопровайдера = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	ВходящийКонтекст.Вставить("ПоказатьПредупреждениеОбОбновленииКриптопровайдера", ПоказатьПредупреждениеОбОбновленииКриптопровайдера);
	
	Оповещение = Новый ОписаниеОповещения(
		"ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗакрытия", ЭтотОбъект, ВходящийКонтекст);
	
	Если ПоказатьПредупреждениеОбОбновленииКриптопровайдера Тогда
		ОповещениеОЗавершении = ?(ВходящийКонтекст.Свойство("ОповещениеОЗавершении"),
			ВходящийКонтекст.ОповещениеОЗавершении, Неопределено);
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("ТипКриптопровайдера", ВходящийКонтекст.Криптопровайдер);
		ПараметрыФормы.Вставить("ЗадаватьНапоминание", ОповещениеОЗавершении = Неопределено);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ПредупреждениеОНеобходимостиОбновленияКриптопровайдера",
			ПараметрыФормы,,,,,
			Оповещение);
		
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбОбновленииКриптопровайдераПриНеобходимостиПослеЗакрытия(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ?(ВходящийКонтекст.Свойство("ОповещениеОЗавершении"),
		ВходящийКонтекст.ОповещениеОЗавершении, Неопределено);
	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, ВходящийКонтекст.ПоказатьПредупреждениеОбОбновленииКриптопровайдера);
		Возврат;
	КонецЕсли;
	
	ИзмененныеПараметры = ВходящийКонтекст.ИзмененныеПараметры;
	
	Если ИзмененныеПараметры.Количество() > 0 Тогда
		ПоказатьПредупреждениеОбИстеченииСертификатов(ИзмененныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеОпределенияВозможностиБезбумажногоПродленияТекущейОрганизаций(Результат, ВходящийКонтекст) Экспорт
	
	НомерСтроки = ВходящийКонтекст.НомерСтроки;
	
	Если Результат.Выполнено Тогда
		ВходящийКонтекст.ИзмененныеПараметры[НомерСтроки].Вставить("ВозможноБезбумажноеПродление", Результат.БезбумажноеПродлениеВозможно);
	Иначе
		ВходящийКонтекст.ИзмененныеПараметры[НомерСтроки].Вставить("ВозможноБезбумажноеПродление", Ложь);
	КонецЕсли;
	
	ВходящийКонтекст.НомерСтроки = ВходящийКонтекст.НомерСтроки + 1;
	
	ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации_ВходящийКонтекст = 
		ОбщегоНазначенияКлиентСервер.СкопироватьРекурсивно(ВходящийКонтекст);
	
	ПодключитьОбработчикОжидания("Подключаемый_ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОбИстеченииСертификатов(ИзменившиесяПараметрыПодключения)
	
	РеквизитHTMLДокументаСообщение = СформироватьТекстПредупрежденияОбИзменившихсяПараметрахПодключения(ИзменившиесяПараметрыПодключения);
	
	РезультатПроверки = Новый Структура;
	РезультатПроверки.Вставить("РеквизитHTMLДокументаСообщение", 	РеквизитHTMLДокументаСообщение);
	РезультатПроверки.Вставить("ИзменившиесяПараметрыПодключения", 	ИзменившиесяПараметрыПодключения);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыполнитьДействиеПослеПоказаПредупрежденияОбИстеченииСертификатов", 
		ЭтотОбъект, 
		РезультатПроверки);
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ПредупреждениеОбИстеченииСертификатов",
		РезультатПроверки,
		,
		,
		,
		,
		ОписаниеОповещения);

КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьДействиеПослеПоказаПредупрежденияОбИстеченииСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Организация     = Результат.Организация;
	НовыйСертификат = Результат.НовыйСертификат;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация", Организация);
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
		Организация,
		,
		,
		ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
		,
		,
		,
		НовыйСертификат);
		
КонецПроцедуры

&НаСервере
Функция СформироватьТекстПредупрежденияОбИзменившихсяПараметрахПодключения(ИзменившиесяПараметрыПодключения)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьТекстПредупрежденияОбИзменившихсяПараметрахПодключения(ИзменившиесяПараметрыПодключения);

КонецФункции

&НаСервереБезКонтекста
Функция ИнформацияОПромежуточномСертификатеБезопасности()
	
	Результат = Новый Структура;
	Результат.Вставить("Отпечаток", "");
	Результат.Вставить("Данные64", 	Неопределено);
	
	ОбработкаЭДО = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Результат.Отпечаток = ОбработкаЭДО.Метаданные().Макеты.ПромежуточныйСертификатБезопасности.Комментарий;
	ПозицияРазделителя = СтрНайти(Результат.Отпечаток, ":");
	Результат.Отпечаток = ?(ПозицияРазделителя > 0, Сред(Результат.Отпечаток, ПозицияРазделителя + 1), Результат.Отпечаток);
	Результат.Отпечаток = СокрЛП(Результат.Отпечаток);
	
	Результат.Данные64 = ОбработкаЭДО.ПолучитьМакет("ПромежуточныйСертификатБезопасности");
	Результат.Данные64 = Base64Строка(Результат.Данные64);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура Подключаемый_ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации() Экспорт
	
	ВходящийКонтекст = ОбщегоНазначенияКлиентСервер.СкопироватьРекурсивно(
		ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации_ВходящийКонтекст);
		
	ОпределитьВозможностьБезбумажногоПродленияСледующейОрганизации(ВходящийКонтекст);
	
КонецПроцедуры

#КонецОбласти

#Область ПроверкаСертификатовИЛицензии

&НаКлиенте
Процедура ПроверитьСертификатИЛицензию(ОповещениеЗавершения, ОбъектСсылка, Организация, ОрганСтрокой = Неопределено, ТолькоПроверка = Ложь) Экспорт
	
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(Организация);
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		ВыполнитьОбработкуОповещения(ОповещениеЗавершения, Истина);
		Возврат;
	КонецЕсли;
	
	СвойстваУчетнойЗаписи = ПолучитьСвойстваУчетнойЗаписи(УчетнаяЗапись);
	
	Если ОрганСтрокой = Неопределено Тогда
		Отпечаток = СвойстваУчетнойЗаписи.СертификатРуководителя;
	Иначе
		Отпечаток = ОтпечатокСертификатаОргана(Организация, ОрганСтрокой);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Отпечаток) ИЛИ (ОрганСтрокой <> Неопределено
		И Отпечаток <> СвойстваУчетнойЗаписи.СертификатРуководителя) Тогда
		
		ВыполнитьОбработкуОповещения(ОповещениеЗавершения, Истина);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = СвойстваУчетнойЗаписи;
	ДополнительныеПараметры.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	ДополнительныеПараметры.Вставить("ОбъектСсылка",  ОбъектСсылка);
	ДополнительныеПараметры.Вставить("Организация",   Организация);
	ДополнительныеПараметры.Вставить("ОповещениеЗавершения",  ОповещениеЗавершения);
	ДополнительныеПараметры.Вставить("ТолькоПроверка",  ТолькоПроверка);
	ДополнительныеПараметры.Вставить("ОрганСтрокой",  	ОрганСтрокой);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПроверитьСертификатИЛицензию_ПослеПолученияСертификата", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	Сертификат = Новый Структура("Отпечаток", Отпечаток);
	ЗаполнитьМестоХраненияКлюча(СвойстваУчетнойЗаписи.МестоХраненияКлюча, Сертификат);
		
	СертификатыДляПоиска = Новый Массив;
	СертификатыДляПоиска.Добавить(Сертификат);
	
	КриптографияЭДКОКлиент.НайтиСертификаты(ОписаниеОповещения, СертификатыДляПоиска, Ложь, Ложь);
	
КонецПроцедуры

&НаСервере
Функция ОтпечатокСертификатаОргана(Организация, ОрганСтрокой)

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	НастройкиОбмена.СертификатСтрахователяОтпечаток КАК Отпечаток"
		+ ?(ОрганСтрокой = "ФСС", ",
		|	НастройкиОбмена.ИспользоватьНесколько КАК ИспользоватьНесколько,
		|	НастройкиОбменаПользователей.СертификатСтрахователяОтпечаток КАК ОтпечатокПользователя", "") + "
		|ИЗ
		|	РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбмена
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.Организации КАК Организации
		|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.УчетныеЗаписиДокументооборота КАК УчетныеЗаписиДокументооборота
		|			ПО Организации.УчетнаяЗаписьОбмена = УчетныеЗаписиДокументооборота.Ссылка
		|		ПО НастройкиОбмена.Организация = Организации.Ссылка"
		+ ?(ОрганСтрокой = "ФСС", "
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.НастройкиОбменаФСС КАК НастройкиОбменаПользователей
		|			ПО НастройкиОбмена.Организация = НастройкиОбменаПользователей.Организация
		|			И НастройкиОбменаПользователей.Пользователь = &Пользователь", "") + "
		|ГДЕ
		|	НастройкиОбмена.ИспользоватьОбмен = ИСТИНА
		|	И НастройкиОбмена.Организация = &Организация
		|	И Организации.ВидОбменаСКонтролирующимиОрганами = ЗНАЧЕНИЕ(Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате)
		|	И НЕ УчетныеЗаписиДокументооборота.ОбменНапрямую"
		+ ?(ОрганСтрокой = "ФСС", "
		|	И (НастройкиОбмена.Пользователь ЕСТЬ NULL
		|	ИЛИ НастройкиОбмена.Пользователь = ЗНАЧЕНИЕ(Справочник.Пользователи.ПустаяСсылка))", "") + "
		|	И Организации.ПометкаУдаления = ЛОЖЬ
		|	И УчетныеЗаписиДокументооборота.ПометкаУдаления = ЛОЖЬ";
	
	Если ОрганСтрокой <> "ФСС" Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "СертификатСтрахователяОтпечаток", "СертификатАбонентаОтпечаток");
	КонецЕсли;
	
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "НастройкиОбменаФСС", "НастройкиОбмена" + ОрганСтрокой);
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	
	Запрос.УстановитьПараметр("ИспользоватьОбмен",  Истина);
	Запрос.УстановитьПараметр("Организация", 		Организация);
	Запрос.УстановитьПараметр("Пользователь", 		ТекущийПользователь);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Если ВыборкаДетальныеЗаписи.Следующий() Тогда
		Если ОрганСтрокой = "ФСС" Тогда
			ОтпечатокСертификата = ?(ВыборкаДетальныеЗаписи.ИспользоватьНесколько,
				ВыборкаДетальныеЗаписи.ОтпечатокПользователя, "");
			Если НЕ ЗначениеЗаполнено(ОтпечатокСертификата) Тогда
				ОтпечатокСертификата = ВыборкаДетальныеЗаписи.Отпечаток;
			КонецЕсли;
		Иначе
			ОтпечатокСертификата = ВыборкаДетальныеЗаписи.Отпечаток;
		КонецЕсли;
		
		Возврат ОтпечатокСертификата;
	Иначе
		Возврат Неопределено;
	КонецЕсли;

КонецФункции 
	
&НаКлиенте
Процедура ПроверитьСертификатИЛицензию_ПослеПолученияСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		Если Результат.Сертификаты.Количество() > 0 Тогда
			
			// Компонента возвращает срок действия лицензии в часовом поясе GMT, а не клиентском.
			ВходящийКонтекст.Вставить("Сертификат", Результат.Сертификаты[0]);
			
		КонецЕсли;
	КонецЕсли;
	
	Контекст = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ВходящийКонтекст);
	Контекст.Удалить("ОповещениеЗавершения");
	
	РезультатПроверки = ПроверитьСертификатИЛицензиюНаСервере(Контекст);
	
	ТолькоПроверка = ВходящийКонтекст.ТолькоПроверка;
	
	Если ТолькоПроверка Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеЗавершения, РезультатПроверки);
		Возврат;
	КонецЕсли;
	
	ЭтоГрупповаяОтправка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст);
	
	ЕстьПроблема = 
		РезультатПроверки.ЛицензияПросрочена 
		ИЛИ (РезультатПроверки.ЛицензияИстекает И НЕ ЭтоГрупповаяОтправка)
		ИЛИ РезультатПроверки.СертификатПросрочен 
		ИЛИ РезультатПроверки.СрокДействияКлючаПросрочен
		ИЛИ РезультатПроверки.СертификатНедействителен;
	
	Если ЕстьПроблема Тогда
		
		Если ЭтоГрупповаяОтправка Тогда
			
			ВернутьОшибкуПроверкиСертификатаИЛицензии(РезультатПроверки, ВходящийКонтекст);
			
		Иначе
			
			ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(
				ВходящийКонтекст, 
				РезультатПроверки, 
				Истина);
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПроверитьСертификатИЛицензию_ПослеПоказаПредупреждения", 
				ЭтотОбъект, 
				ВходящийКонтекст);
			
			ОткрытьФорму(
				ПутьКОбъекту + ".Форма.СообщениеОбОкончанииЛицензииИСертификата",
				РезультатПроверки,,,,,
				ОписаниеОповещения);
				
		КонецЕсли;
		
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеЗавершения, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВернутьОшибкуПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	ЕстьПроблема = 
		(Результат.ЛицензияПросрочена 
		ИЛИ Результат.СертификатПросрочен 
		ИЛИ Результат.СрокДействияКлючаПросрочен
		ИЛИ Результат.СертификатНедействителен)
		И ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст);
		
	Если ЕстьПроблема Тогда
		
		ТекстОшибки = "";
		
		СертификатНедействителенНетПросроченности = Результат.СертификатНедействителен И НЕ Результат.СертификатПросрочен
			И НЕ Результат.СрокДействияКлючаПросрочен;
		
		Если СертификатНедействителенНетПросроченности Тогда
			ТекстОшибки = ДлительнаяОтправкаКлиентСервер.СообщениеСертификатНедействителен();
		ИначеЕсли Результат.ЛицензияПросрочена И Результат.СертификатПросрочен Тогда
			ТекстОшибки = ДлительнаяОтправкаКлиентСервер.СообщениеИстекСертификатИЛицензия();
		ИначеЕсли Результат.ЛицензияПросрочена Тогда
			ТекстОшибки = ДлительнаяОтправкаКлиентСервер.СообщениеИстеклаЛицензия();
		ИначеЕсли Результат.СертификатПросрочен Тогда
			ТекстОшибки = ДлительнаяОтправкаКлиентСервер.СообщениеИстекСертификат();
		ИначеЕсли Результат.СрокДействияКлючаПросрочен Тогда
			ТекстОшибки = ДлительнаяОтправкаКлиентСервер.СообщениеИстекСрокДействияКлюча();
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТекстОшибки) Тогда
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстОшибки);
		КонецЕсли;
						
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПроверитьСертификатИЛицензиюНаСервере(ВходящийКонтекст) Экспорт
	
	ЧасовойПояс = ЧасовойПоясСеанса();
	
	Организация    = ВходящийКонтекст.Организация;
	ОрганСтрокой   = ВходящийКонтекст.ОрганСтрокой;

	Если ВходящийКонтекст.Свойство("Сертификат") Тогда
		Сертификат 		= ВходящийКонтекст.Сертификат;
		Поставщик 		= Сертификат.Поставщик;
		ДействителенС 	= МестноеВремя(Сертификат.ДействителенС);
		ДействителенПо 	= МестноеВремя(Сертификат.ДействителенПо);
	Иначе
		Поставщик 		= "";
		ДействителенС 	= Дата(1,1,1);
		ДействителенПо 	= Дата(1,1,1);
	КонецЕсли;
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	УчетнаяЗапись = ВходящийКонтекст.УчетнаяЗапись;
	ЛицензияДатаБлокировки = КонтекстЭДОСервер.ЛицензияДатаБлокировки(УчетнаяЗапись);
	ЛицензияДатаОкончания  = КонтекстЭДОСервер.ЛицензияДатаОкончания(УчетнаяЗапись);
	
	СертификатНедействителен = КонтекстЭДОСервер.СертификатНедействителен(Поставщик, ДействителенС);
	
	СертификатПросрочен = 
		ДействителенПо <> Дата(1,1,1) 
		И ДействителенПо < ТекущаяДата();
	
	ЛицензияПросрочена = КонтекстЭДОСервер.ЛицензияПросрочена(УчетнаяЗапись);
	
	КонтекстЭДОСервер.ОтключитьОбменСФССПриНеобходимости(Организация);
	
	ТарифОператораЭДО = ДокументооборотСКОВызовСервера.ТарифОператораЭДО(Организация);

	ЛицензияИстекает = 
		ЛицензияДатаОкончания <> Дата(1,1,1) 
		И ЛицензияДатаБлокировки > ТекущаяДата() 
		И ЛицензияДатаОкончания < ТекущаяДата()
		И ЗначениеЗаполнено(ТарифОператораЭДО);
	
	Если ЗначениеЗаполнено(ВходящийКонтекст.СрокДействияКлюча) Тогда
		СрокДействияКлюча = МестноеВремя(КонецДня(ВходящийКонтекст.СрокДействияКлюча), ЧасовойПояс);
		СрокПроверки = МестноеВремя(КонецДня(СрокДействияКлюча) - 30 * 24 * 60 * 60, ЧасовойПояс);
		КлючПросрочен = СрокПроверки < ТекущаяДата() И СрокДействияКлюча > ТекущаяДата();
	Иначе
		КлючПросрочен = Ложь;
		СрокДействияКлюча = Дата(1,1,1);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("СсылкаНаОбъект", 				ВходящийКонтекст.ОбъектСсылка);
	ДополнительныеПараметры.Вставить("ЛицензияДатаБлокировки", 		ЛицензияДатаБлокировки);
	ДополнительныеПараметры.Вставить("СертификатДатаОкончания", 	ДействителенПо);
	ДополнительныеПараметры.Вставить("ЛицензияПросрочена", 			ЛицензияПросрочена);
	ДополнительныеПараметры.Вставить("СертификатПросрочен", 		СертификатПросрочен);
	ДополнительныеПараметры.Вставить("СрокДействияКлючаПросрочен", 	КлючПросрочен);
	ДополнительныеПараметры.Вставить("СрокДействияКлюча", 			КонецДня(СрокДействияКлюча));
	ДополнительныеПараметры.Вставить("ОрганСтрокой", 				ОрганСтрокой);
	ДополнительныеПараметры.Вставить("Организация", 				Организация);
	ДополнительныеПараметры.Вставить("ТарифОператораЭДО", 			ТарифОператораЭДО);
	ДополнительныеПараметры.Вставить("ЛицензияДатаОкончания", 		ЛицензияДатаОкончания);
	ДополнительныеПараметры.Вставить("ЛицензияИстекает", 			ЛицензияИстекает);
	ДополнительныеПараметры.Вставить("ЛицензияДатаНачала", 			НачалоДня(ВходящийКонтекст.ЛицензияДатаНачала));
	ДополнительныеПараметры.Вставить("СертификатНедействителен", 	СертификатНедействителен);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

&НаКлиенте
Процедура ПроверитьСертификатИЛицензию_ПослеПоказаПредупреждения(Ответ, ВходящийКонтекст) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Повторить Тогда
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеЗавершения, Истина);
		
	ИначеЕсли Ответ <> КодВозвратаДиалога.Да Тогда
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеЗавершения, Ложь);
		
	Иначе
		
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		
		Организация = ВходящийКонтекст.Организация;
		
		ПараметрыОткрытияФормы = Новый Структура();
		ПараметрыОткрытияФормы.Вставить("Организация", Организация);
		
		ФлагиВМастер = Новый Структура();
		
		СертификатДатаОкончания = ВходящийКонтекст.СертификатДатаОкончания;
		ЛицензияДатаОкончания   = ВходящийКонтекст.ЛицензияДатаОкончания;
		СрокДействияКлюча		= ВходящийКонтекст.СрокДействияКлюча;
		
		ПродлитьСертификат = Ложь;
		ПродлитьЛицензию   = Ложь;
		
		Дельта      = 14*60*60;
		ТекущаяДата = ТекущаяДата();
		
		ПродлитьСертификат = СертификатДатаОкончания <> Дата(1,1,1) 
			И (СертификатДатаОкончания < ТекущаяДата ИЛИ СертификатДатаОкончания - ТекущаяДата < Дельта);
		
		ПродлитьЛицензию = ЛицензияДатаОкончания <> Дата(1,1,1) 
			И (ЛицензияДатаОкончания < ТекущаяДата ИЛИ ЛицензияДатаОкончания - ТекущаяДата < Дельта);
		
		ПродлитьДействиеКлюча = СрокДействияКлюча <> Дата(1,1,1) И (СрокДействияКлюча < ТекущаяДата);
		ФлагиВМастер.Вставить("ПродлитьДействиеКлюча",						ПродлитьДействиеКлюча);
			
		ФлагиВМастер.Вставить("ПродлитьСертификат", 						ПродлитьСертификат);
		ФлагиВМастер.Вставить("ПродлитьЛицензиюНа1СОтчетность", 			ПродлитьЛицензию);
		ФлагиВМастер.Вставить("ИзменитьРеквизитыПодключенияК1СОтчетности", 	Ложь);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ФлагиВМастер", ФлагиВМастер);
		
		ПараметрыОткрытияФормы.Вставить("АктивироватьФлагиМастера", ДополнительныеПараметры);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПроверитьСертификатИЛицензию_ПослеОтправкиЗаявления", 
			ЭтотОбъект, 
			ВходящийКонтекст);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
			Организация, 
			ЭтотОбъект,
			,
			ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
			ПараметрыОткрытияФормы,
			ОписаниеОповещения);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСертификатИЛицензию_ПослеОтправкиЗаявления(Организация, ВходящийКонтекст) Экспорт
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеЗавершения, Ложь);
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСвойстваУчетнойЗаписи(УчетнаяЗапись)
	
	Сертификат = Мультирежим.СертификатПользователя(УчетнаяЗапись, "Отпечаток, МестоХраненияКлюча, ЭлектроннаяПодписьВМоделиСервиса");
	
	Реквизиты = "ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия, ЛицензияДатаНачала, ЛицензияНаименование, ЛицензияДатаОкончания, ОбменНапрямую, ЛицензияДатаБлокировки";
	СвойстваУчетнойЗаписи = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначенияРеквизитовОбъекта(УчетнаяЗапись, Реквизиты);
	СвойстваУчетнойЗаписи.Вставить("МестоХраненияКлюча", Сертификат.МестоХраненияКлюча);
	СвойстваУчетнойЗаписи.Вставить("СрокДействияКлюча",  Дата(1, 1, 1));
	СвойстваУчетнойЗаписи.Вставить("ЭлектроннаяПодписьВМоделиСервиса", Сертификат.ЭлектроннаяПодписьВМоделиСервиса);
	СвойстваУчетнойЗаписи.Вставить("СертификатРуководителя",  Сертификат.Отпечаток);
	
	Если ЭтоОблачнаяПодпись(Сертификат.МестоХраненияКлюча) Тогда
		ОблачнаяУчетнаяЗапись  = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(Сертификат.МестоХраненияКлюча);
		ДополнительныеСвойства = КриптографияЭДКО.ДополнительныеНастройкиОблачнойПодписи(ОблачнаяУчетнаяЗапись);
		Если НЕ ДополнительныеСвойства.ОтключитьНапоминание Тогда
			СвойстваУчетнойЗаписи.СрокДействияКлюча = ДополнительныеСвойства.СрокДействияКлюча;
		КонецЕсли;	
	КонецЕсли;
	
	Возврат СвойстваУчетнойЗаписи;
	
КонецФункции

#КонецОбласти

#КонецОбласти

&НаКлиенте
Процедура ПоказатьФормуПредупрежденияОбИзменившихсяНастройкахПодключенияЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Истина Тогда
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
			ВходящийКонтекст.Организация, 
			,
			,
			ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"));
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ИдентификаторАбонентаПоОрганизации(Организация) Экспорт 

	Возврат ОбработкаЗаявленийАбонентаВызовСервера.ИдентификаторАбонентаПоОрганизации(Организация);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьКодировку(Знач ДДИлиСсылка) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОпределитьКодировку(ДДИлиСсылка);
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбъектВАрхиве(Знач ОбъектОтправки, Знач ИмяФайла) Экспорт
	
	КонтекстМодуля = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстМодуля.ОбъектВАрхиве(ОбъектОтправки, ИмяФайла);
	
КонецФункции

&НаКлиенте
Процедура ПодставитьВыборВСтроку(Шаблон, Знач Варианты, Знач ИдВарианта)
	
	ВсеВарианты = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивСлов(Варианты, "|");
	
	Части = Новый Массив;
	Вариант = ВсеВарианты.Получить(ИдВарианта);
	Вхождений = СтрЧислоВхождений(Вариант, "%");
	Если Вхождений = 0 Тогда 
		Части.Добавить(Вариант);
		Для Сч = 0 по 8 Цикл 
			Части.Добавить("");
		КонецЦикла;
	Иначе
		ЧастиВарианта = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивСлов(Вариант, "%");
		Части.Добавить(ЧастиВарианта[0]);
		Для Сч = 0 по 8 Цикл 
			Части.Добавить(?(Вхождений > Сч, ЧастиВарианта[Сч + 1], ""));
		КонецЦикла;
	КонецЕсли;	
	
	Для Сч = 0 по Части.Количество()-1 Цикл 
		Шаблон = СтрЗаменить(Шаблон, "%" + Строка(Сч), Части[Сч]);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеАрхивныхФайлов(Знач ОповещениеЗавершения = Неопределено, Знач ИдВарианта = 1, Знач ИдДействия = 0, Знач КонечноеСостояние = Истина, Знач Предварительное = Истина) Экспорт
	
	//Варианты добавлять только в конец
	Варианты = " выписки| квитанции| сверки| справки| подписи| извещения| уведомления| сообщения| подтверждения| вложения|"; //0-9
	Варианты = Варианты + " документа| скан-копии документа|ы скан-копий документа%и%ы| отчета| пакета|"; //10-14
	Варианты = Варианты + " зашифрованного пакета| протокола| объекта|ы некоторых вложений%и%ы|ы всех вложений%и%ы|"; //15-19
	Варианты = Варианты + " выгрузки| документа описи|ы некоторых документов описи%и%ы|ы всех документов описи%и%ы| загруженного отчета|"; //20-24
	Варианты = Варианты + " контейнера|ы контейнеров%и%ы| приложения|ы приложений%и%ы|"; //25-
	Варианты = Варианты + " подписанного отчета| зашифрованной квитанции";
	
	Часть0 = "Файл%0 был%1 ранее перемещен%2 в архив.";
	ПодставитьВыборВСтроку(Часть0, Варианты, ИдВарианта);
		
	Если КонечноеСостояние Тогда 
		ВариантыДействия = "Выгрузка%на|Копирование%но|Отправка%на|Просмотр%ен|Запись%на|Печать%на|Изменение%но|Удаление%но";
		Часть1 = Символы.ПС + "%0 содержимого невозмож%1.";
	Иначе
		Если Предварительное Тогда
			ВариантыДействия = "Выгрузить|Копировать|Отправить|Просмотреть|Записать|Печатать|Изменить|Удалить";
			Часть1 = Символы.ПС + "%0 только неархивированное содержимое?";
		Иначе
			ВариантыДействия = "Выгружено|Скопировано|Отправлено|Просмотрено|Записано|Напечатано|Изменено|Удалено";
			Часть1 = Символы.ПС + "%0 только неархивированное содержимое.";
		КонецЕсли;
	КонецЕсли;
	ПодставитьВыборВСтроку(Часть1, ВариантыДействия, ИдДействия);
	
	СтрокаСсылки = Новый ФорматированнаяСтрока("Подробнее об архивировании файлов",,,, "https://its.1c.ru/db/elreps#content:59:hdoc");
	Часть2 = Новый ФорматированнаяСтрока(Символы.ПС, Символы.ПС, СтрокаСсылки);

	ПолныйТекстУведомления = Новый ФорматированнаяСтрока(Часть0, Часть1, Часть2);
	
	ВхПараметры = Новый Структура("ОповещениеЗавершения", ОповещениеЗавершения);
		
	Если КонечноеСостояние Или Не Предварительное Тогда 
		Оповещение = Новый ОписаниеОповещения("ПоказатьУведомлениеАрхивныхФайловПослеПредупреждения", ЭтотОбъект, ВхПараметры);
		ПоказатьПредупреждение(Оповещение, ПолныйТекстУведомления);
	Иначе
		Оповещение = Новый ОписаниеОповещения("ПоказатьУведомлениеАрхивныхФайловПослеВопроса", ЭтотОбъект, ВхПараметры);
		ПоказатьВопрос(Оповещение, ПолныйТекстУведомления, РежимДиалогаВопрос.ДаНет);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеАрхивныхФайловПослеПредупреждения(ВхПараметры) Экспорт

	ПоказатьУведомлениеАрхивныхФайловПослеВопроса(КодВозвратаДиалога.ОК, ВхПараметры);
		
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьУведомлениеАрхивныхФайловПослеВопроса(Результат, Знач ВхПараметры = Неопределено) Экспорт
		
	Если ТипЗнч(ВхПараметры.ОповещениеЗавершения) = Тип("ОписаниеОповещения") Тогда 
		ВыполнитьОбработкуОповещения(ВхПараметры.ОповещениеЗавершения, Результат);
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьПервичныйДокументВОтветеНаТребование(ВыполняемоеОповещение, Организация, Источник = Неопределено, МножественныйВыбор = Ложь) Экспорт
	
	ВыбираемыеТипов = СписокВыбораТиповПервичныйДокументВОтветеНаТребование(МножественныйВыбор);
	
	КоличествоТипов = ВыбираемыеТипов.Количество();
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Источник",              Источник);
	ДополнительныеПараметры.Вставить("Организация",           Организация);
	ДополнительныеПараметры.Вставить("МножественныйВыбор",    МножественныйВыбор);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыборИсточникаПослеВыбораТипа", ЭтотОбъект, ДополнительныеПараметры);
	
	Если КоличествоТипов = 1 Тогда
		// пропускаем выбор типа
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, ВыбираемыеТипов[0]);
	Иначе
		
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("МножественныйВыбор",    МножественныйВыбор);
		
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.ФормаВыбораТипаДокумента",
			ПараметрыФормы,
			,
			,
			,
			,
			ОписаниеОповещения);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьСканированныйДокументВОтветеНаТребование(ВыполняемоеОповещение, Организация, Источник = Неопределено, МножественныйВыбор = Ложь) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Источник",              Источник);
	ДополнительныеПараметры.Вставить("Организация",           Организация);
	ДополнительныеПараметры.Вставить("МножественныйВыбор",    МножественныйВыбор);
	
	СписокЗначений = Новый СписокЗначений;
	СписокЗначений.Добавить(Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде"));
	
	ВыборИсточникаПослеВыбораТипа(СписокЗначений[0], ДополнительныеПараметры);
	
КонецПроцедуры

&НаСервере
Функция СписокВыбораТиповПервичныйДокументВОтветеНаТребование(МножественныйВыбор)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СписокВыбораТиповПервичныйДокументВОтветеНаТребование(МножественныйВыбор);
	
КонецФункции

&НаКлиенте
Процедура ВыборИсточникаПослеВыбораТипа(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ПараметрИсточник      = ВходящийКонтекст.Источник;
	ПараметрОрганизация   = ВходящийКонтекст.Организация;
	МножественныйВыбор    = ВходящийКонтекст.МножественныйВыбор;
	
	Если ТипЗнч(Результат) = Тип("ЭлементСпискаЗначений") Тогда
		ВыбранныйТип = Результат.Значение;
	Иначе
		ВыбранныйТип = Результат.Типы()[0];
	КонецЕсли;
	
	ВходящийКонтекст.Вставить("Тип", ВыбранныйТип);
	
	Если ВыбранныйТип = Тип("СправочникСсылка.СканированныеДокументыДляПередачиВЭлектронномВиде") Тогда
		
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("Организация", 			ВходящийКонтекст.Организация);
		ПараметрыОткрытия.Вставить("ОткрытьВРежимеВыбора", 	Истина);
		ПараметрыОткрытия.Вставить("МножественныйВыбор", 	МножественныйВыбор);
			
		ОткрытьФорму("Справочник.СканированныеДокументыДляПередачиВЭлектронномВиде.Форма.ФормаСписка", ПараметрыОткрытия,,,,,ВыполняемоеОповещение);
		
	Иначе
	
		СтандартнаяОбработка = Истина;
		
		// В процедуру, указанную в параметре ВыполняемоеОповещение, 
		// должен быть передан результат выбора, который имеет тип Структура со следующими полями:
		// * Ссылка, * Описание, * Основание
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыбратьДокументИсточникДляПередачиФНСЗавершение", 
			ЭтотОбъект, 
			ВходящийКонтекст);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентПереопределяемый.ВыбратьДокументИсточникДляПередачиФНС(
			ВыбранныйТип,
			ПараметрИсточник,
			ПараметрОрганизация, 
			ОписаниеОповещения,
			СтандартнаяОбработка);
			
		Если СтандартнаяОбработка = Истина Тогда
			// Случай, когда переопределяемая не заполнена
			
			ВыбратьДокументИсточникДляПередачиФНС(ВыбранныйТип, ВходящийКонтекст);
			
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ВыбратьДокументИсточникДляПередачиФНС(ВыбранныйТип, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	МножественныйВыбор    = ВходящийКонтекст.МножественныйВыбор;
	Организация           = ВходящийКонтекст.Организация;

	ИмяФормыИсточника = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ПолучитьИмяФормыОбъктаДляПередачиФНС(ВыбранныйТип);
	
	Если ИмяФормыИсточника = Неопределено Тогда
		ТекстПредупреждения = НСтр("ru = 'Не удалось открыть форму выбора'");
		ПоказатьПредупреждение(, ТекстПредупреждения);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Тип", 					ВыбранныйТип);
	ДополнительныеПараметры.Вставить("Организация",				Организация);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыбратьДокументИсточникДляПередачиФНСЗавершение", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ПараметрыОткрытия = Новый Структура();
	ПараметрыОткрытия.Вставить("МножественныйВыбор", МножественныйВыбор);
	
	ОткрытьФорму(ИмяФормыИсточника, ПараметрыОткрытия, ЭтотОбъект, , , , ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

&НаКлиенте
Процедура ВыбратьДокументИсточникДляПередачиФНСЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	Если ЗначениеЗаполнено(Результат) Тогда
		
		Если ТипЗнч(Результат) = ВходящийКонтекст.Тип 
			ИЛИ ТипЗнч(Результат) = Тип("Структура")
			ИЛИ ТипЗнч(Результат) = Тип("Массив") И Результат.Количество() = 1 Тогда
	
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Результат);
			
		ИначеЕсли ТипЗнч(Результат) = Тип("Массив") И Результат.Количество() > 0 Тогда
			
			ДокументыИсточники = СформироватьТаблицуДокументовИсточниковИИхСканов(Результат);
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ДокументыИсточники",    ДокументыИсточники);
			ДополнительныеПараметры.Вставить("Организация",           ВходящийКонтекст.Организация);
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
			
			ВыбратьДокументИсточникДляПередачиФНСЗавершение_ПодготовкаКПреобразованию(ДополнительныеПараметры);
			
		КонецЕсли;
	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьДокументИсточникДляПередачиФНСЗавершение_ПодготовкаКПреобразованию(ДополнительныеПараметры)
	
	ОписанияФайлов = Новый Массив;
	Для каждого ДокументИсточник Из ДополнительныеПараметры.ДокументыИсточники Цикл
		Если ДокументИсточник.Сканы <> Неопределено Тогда
			Для каждого Скан Из ДокументИсточник.Сканы Цикл
				
				Скан = Скан.Значение;
				
				ОписаниеФайла = Новый Структура();
				ОписаниеФайла.Вставить("Адрес", Скан.АдресДанных);
				ОписаниеФайла.Вставить("Имя", Скан.Имя);
				ОписаниеФайла.Вставить("Идентификатор", ДокументИсточник.Документ);
				ОписаниеФайла.Вставить("Размер", Скан.Размер);
				
				Если Скан.Свойство("АдресФайлаПодписи") Тогда
					ОписаниеФайла.Вставить("АдресФайлаПодписи", Скан.АдресФайлаПодписи);
					ОписаниеФайла.Вставить("ИмяФайлаПодписи", Скан.ИмяФайлаПодписи);
				КонецЕсли;
				
				ОписанияФайлов.Добавить(ОписаниеФайла);
				
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыбратьДокументИсточникДляПередачиФНСЗавершение_ПослеПреобразования", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ИдентификаторВладельца = ДополнительныеПараметры.ВыполняемоеОповещение.Модуль.УникальныйИдентификатор;
		
	ТребованияФНСКлиент.ДобавитьСканыВОтветНаТребованиеПоОписаниюФайлов(
		ЭтотОбъект, 
		ИдентификаторВладельца, 
		ОписанияФайлов, 
		ОписаниеОповещения);
		
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьДокументИсточникДляПередачиФНСЗавершение_ПослеПреобразования(Результат, ВходящийКонтекст) Экспорт
	
	ДокументыИсточники = ВходящийКонтекст.ДокументыИсточники;

	Если Результат.Выполнено Тогда

		ОписанияФайлов = Новый Массив;
		Для каждого ДокументИсточник Из ДокументыИсточники Цикл
			Если ДокументИсточник.Сканы <> Неопределено Тогда
				ДокументИсточник.Сканы.Очистить();
			КонецЕсли;
		КонецЦикла;
		
		Для каждого ОписаниеФайла Из Результат.ОписанияФайлов Цикл
			Для каждого ДокументИсточник Из ДокументыИсточники Цикл
				Если ДокументИсточник.Документ = ОписаниеФайла.Идентификатор Тогда
					ОписаниеФайла.Вставить("АдресДанных", ОписаниеФайла.Адрес);
					ДокументИсточник.Сканы.Добавить(ОписаниеФайла, ОписаниеФайла.Имя, Истина);
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
	
	КонецЕсли;
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ДокументыИсточники", ДокументыИсточники);
	ДополнительныеПараметры.Вставить("Организация",        ВходящийКонтекст.Организация);
	
	ОткрытьФорму("Справочник.ОписиИсходящихДокументовВНалоговыеОрганы.Форма.ФормаГрупповогоВыбораДокументов",
		ДополнительныеПараметры,
		,
		,
		,
		,
		ВходящийКонтекст.ВыполняемоеОповещение);
	
КонецПроцедуры

&НаСервере
Функция СформироватьТаблицуДокументовИсточниковИИхСканов(МассивДокументовИсточников)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	// Описание присоединенных сканов
	ФайлыИсточников = СвойстваПрисоединенныхСканов(МассивДокументовИсточников);
	
	ДокументыИсточники = Новый Массив;
	Для каждого ДокументИсточник Из МассивДокументовИсточников Цикл

		НоваяСтрока = Новый Структура();
		НоваяСтрока.Вставить("СведенияДокумента", 	Неопределено);
		НоваяСтрока.Вставить("СведенияДокументаОснования", Неопределено);
		НоваяСтрока.Вставить("Документ", 			Неопределено);
		НоваяСтрока.Вставить("Пометка", 			Неопределено);
		НоваяСтрока.Вставить("ЕстьЭлектронныйДокумент", Ложь);
		НоваяСтрока.Вставить("ВидДокументаФНС", 	Неопределено);
		НоваяСтрока.Вставить("Сканы", 				Неопределено);
		
		Если ТипЗнч(ДокументИсточник) = Тип("Структура") Тогда
			
			ДокументСсылка = ДокументИсточник.Ссылка;
			
			НоваяСтрока.СведенияДокумента          = ДокументИсточник.Описание;
			НоваяСтрока.СведенияДокументаОснования = ДокументИсточник.Основание;
			
		Иначе
				
			ДокументСсылка = ДокументИсточник;
			НоваяСтрока.СведенияДокумента = Строка(ДокументСсылка);
			
		КонецЕсли;
		
		НоваяСтрока.Документ = ДокументСсылка;
		НоваяСтрока.Пометка  = Истина;
		
		ИнициализироватьВложенияДокументовИсточников(
			ДокументСсылка, 
			ФайлыИсточников, 
			НоваяСтрока,
			КонтекстЭДОСервер);
			
		ДокументыИсточники.Добавить(НоваяСтрока);
		
	КонецЦикла;
	
	Возврат ДокументыИсточники;
	
КонецФункции

&НаСервере
Функция СвойстваПрисоединенныхСканов(ДокументыИсточники)
	
	МассивСвойствФайлов = Новый Массив;
	ФайлыИсточников = Новый Соответствие;
	
	Для каждого ДокументИсточник Из ДокументыИсточники Цикл
		ФайлыИсточников.Вставить(ДокументИсточник, МассивСвойствФайлов);
	КонецЦикла; 
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ПолучитьИзображенияПрисоединенныхФайловИсточников(ФайлыИсточников, УникальныйИдентификатор);
	
	ЕстьОписания = Ложь;
	Для каждого Строка Из ФайлыИсточников Цикл
		Если ЗначениеЗаполнено(Строка.Значение) Тогда
			ЕстьОписания = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если НЕ ЕстьОписания Тогда
		//нет присоединенных файлов по БСП механизму
		//попытаемся получить через переопределеяемый метод
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьИзображенияПрисоединенныхФайловИсточников(ФайлыИсточников, УникальныйИдентификатор);
	КонецЕсли;
	
	Возврат ФайлыИсточников; 
	
КонецФункции

&НаСервере
Процедура ИнициализироватьВложенияДокументовИсточников(ДокументИсточник, ФайлыИсточников, НоваяСтрока, КонтекстЭДОСервер)
	
	ЭлектронныеДокументы = КонтекстЭДОСервер.ЭлектронныеДокументыЭДО(ДокументИсточник);
	
	НоваяСтрока.ЕстьЭлектронныйДокумент = ЭлектронныеДокументы.ЕстьЭлектронныйДокумент;
	
	Если ЭлектронныеДокументы.ЕстьЭлектронныйДокумент Тогда
		НоваяСтрока.ВидДокументаФНС = ЭлектронныеДокументы.ВидДокументаФНС;
	Иначе
		
		// Если нет документа 1С-ЭДО, тогда должен быть скан.
		
		ПрисоединенныеСканы = ФайлыИсточников.Получить(ДокументИсточник);
		
		ЕстьСканы = НЕ (ПрисоединенныеСканы = Неопределено
			ИЛИ ТипЗнч(ПрисоединенныеСканы) = Тип("Массив") И ПрисоединенныеСканы.Количество() = 0);
		
		Если ЕстьСканы Тогда
			
			Сканы = Новый СписокЗначений;
			
			Для каждого ПрисоединенныйСкан Из ПрисоединенныеСканы Цикл
				Сканы.Добавить(ПрисоединенныйСкан, ПрисоединенныйСкан.Имя, Истина);
			КонецЦикла;
			
			НоваяСтрока.Сканы = Сканы;
			
		КонецЕсли;
			
	КонецЕсли;
	
КонецПроцедуры

// Получает список свойств файла или каталога. 
//
// Параметры:
//  ОповещениеОЗавершении - ОписаниеОповещения - описание процедуры, принимающей результат.
//    Результат - Структура:
//      * Выполнено      - Булево - если Истина, то процедура успешно выполнена и получен результат, иначе см. ОписаниеОшибки.
//      * СвойстваФайлов  - Массив структур. Структура - свойства файла.
//        ** Имя              - Строка - имя файла.
//        ** ИмяБезРасширения - Строка - имя файла (без расширения).
//        ** ПолноеИмя        - Строка - полное имя файла (включающее путь к файлу).
//        ** Путь             - Строка - путь к файлу.
//        ** Расширение       - Строка - расширение имени файла.
//        ** Размер           - Число  - размер файла (в байтах).
//        ** Существует       - Булево - определяет, существует ли файл.
//        ** ЭтоКаталог       - Булево - если Истина, то каталог, иначе - файл.
//
//  МассивПолныхИменФайлов - Массив - массив полных имен файлов.
//
&НаКлиенте
Процедура ПолучитьСвойстваФайлов(ОповещениеОЗавершении, МассивПолныхИменФайлов) Экспорт
	
	МассивСвойствФайлов = Новый Массив;
	
	ПолучитьСвойстваФайлаПоследовательно(ОповещениеОЗавершении, МассивСвойствФайлов, МассивПолныхИменФайлов, 0);
	
КонецПроцедуры

//вспомогательная к ПолучитьСвойстваФайлов
&НаКлиенте
Процедура ПолучитьСвойстваФайлаПоследовательно(ОповещениеОЗавершении, МассивСвойствФайлов, МассивПолныхИменФайлов, ПорядковыйНомерФайла)
	
	Если ПорядковыйНомерФайла <= МассивПолныхИменФайлов.ВГраница() Тогда
		
		ТекущийФайлПолноеИмя = МассивПолныхИменФайлов[ПорядковыйНомерФайла];
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ОповещениеОЗавершении",  ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("МассивСвойствФайлов",    МассивСвойствФайлов);
		ДополнительныеПараметры.Вставить("МассивПолныхИменФайлов", МассивПолныхИменФайлов);
		ДополнительныеПараметры.Вставить("ПорядковыйНомерФайла",   ПорядковыйНомерФайла);
		
		ОписаниеОповещения = Новый ОписаниеОповещения("ПолучитьСвойстваФайловПослеПолученияСвойствОдногоФайла", ЭтотОбъект, ДополнительныеПараметры);
		ОперацииСФайламиЭДКОКлиент.ПолучитьСвойстваФайла(ОписаниеОповещения, ТекущийФайлПолноеИмя);
		Возврат;
		
	Иначе
		
		РезультатСтруктура = Новый Структура;
		РезультатСтруктура.Вставить("Выполнено", Истина);
		РезультатСтруктура.Вставить("СвойстваФайлов", МассивСвойствФайлов);
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатСтруктура);
		
	КонецЕсли;
	
КонецПроцедуры

//вспомогательная к ПолучитьСвойстваФайлов
&НаКлиенте
Процедура ПолучитьСвойстваФайловПослеПолученияСвойствОдногоФайла(Результат, ДополнительныеПараметры) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		
		РезультатСтруктура = Новый Структура;
		РезультатСтруктура.Вставить("Выполнено", Ложь);
		
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ОповещениеОЗавершении, РезультатСтруктура);

		Возврат;	
		
	КонецЕсли;
	
	ДополнительныеПараметры.МассивСвойствФайлов.Добавить(Результат.СвойстваФайла);
	ПолучитьСвойстваФайлаПоследовательно(ДополнительныеПараметры.ОповещениеОЗавершении, ДополнительныеПараметры.МассивСвойствФайлов, ДополнительныеПараметры.МассивПолныхИменФайлов, ДополнительныеПараметры.ПорядковыйНомерФайла + 1)
	
КонецПроцедуры

#Область ПечатьПояснения

&НаКлиенте
Функция ПечатьПояснениеПоНДС(Пояснение) Экспорт
	
	ТабДок = ТабличныйДокументПоясненияСервер(Пояснение);
	НапечататьДокумент(ТабДок, Строка(Пояснение));
	
КонецФункции

&НаСервере
Функция ТабличныйДокументПоясненияСервер(Пояснение)
	
	ТЧ = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТабличныеЧастиПояснения(Истина);
	
	ВыбранныеСтроки = Новый Структура;
	Для каждого Имя Из ТЧ Цикл
		
		Таблица = Пояснение[Имя];
		Таблица = Таблица.Выгрузить();
		Таблица = ОбщегоНазначения.ТаблицаЗначенийВМассив(Таблица);
		ВыбранныеСтроки.Вставить(Имя, Таблица);
	
	КонецЦикла;
	
	ВыбранныеСтроки.Вставить("ПояснИнОсн", Пояснение.ПояснИнОсн);
	
    КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ТабДок = КонтекстЭДОСервер.ТабличныйДокументПояснения(Пояснение, ВыбранныеСтроки, Ложь);
	Возврат ТабДок;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьКонтрагентовИзТребования(КонтрагентыИзСтроки) Экспорт
	
	Если КонтрагентыИзСтроки.Количество() = 0 Тогда
		Возврат;
	ИначеЕсли КонтрагентыИзСтроки.Количество() = 1 Тогда
		ПоказатьЗначение(,КонтрагентыИзСтроки[0].Значение);
	ИначеЕсли КонтрагентыИзСтроки.Количество() > 1 Тогда 
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОткрытьКонтрагентовПослеВыбора", 
			ЭтотОбъект);
		
		КонтрагентыИзСтроки.ПоказатьВыборЭлемента(ОписаниеОповещения, НСтр("ru = 'Выберите контрагента для открытия'"));
		 
	Иначе
		ПоказатьПредупреждение(, НСтр("ru = 'Невозможно открыть карточку контрагента по причине:
                                       |Контрагент не найден в справочнике контрагентов'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьКонтрагентовПослеВыбора(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> Неопределено Тогда
		 ПоказатьЗначение(,Результат.Значение);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОтправкаЗаявленийНаПереход

&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеНаПереход(ВходящийКонтекст) Экспорт
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправкиЗаявления(ВходящийКонтекст) Тогда
		Возврат;
	КонецЕсли;
	
	ПодключитьОбработчикОжидания("Подключаемый_СформироватьИОтправитьЗаявлениеНаПереход", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_СформироватьИОтправитьЗаявлениеНаПереход()
	
	ВходящийКонтекст = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	УдалосьСоздать = СоздатьНовоеЗаявлениеНаПереход(ВходящийКонтекст);
	
	Если НЕ УдалосьСоздать Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось создать заявление'"));
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправкеЗаявления();
		ДобавитьПояснениеКОшибкеПерехода();
		Возврат;
	КонецЕсли;
	 
	ИдентификаторАбонента = ИдентификаторАбонентаПоОрганизации(ВходящийКонтекст.Организация);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СформироватьИОтправитьЗаявлениеНаПереходЗавершение", 
		ЭтотОбъект,
		ВходящийКонтекст);
		
	Контекст = ПараметрыПроцедурыСформироватьИОтправитьЗаявление();
	Контекст.ДокументЗаявление 							= ЗаявлениеНаПереход;
	Контекст.ИдентификаторАбонента 						= ИдентификаторАбонента;
	Контекст.ВызовИзМастераПодключенияК1СОтчетности 	= Истина;
	Контекст.ВыполняемоеОповещение 						= ОписаниеОповещения;
	Контекст.ФормироватьЗакрытыйКлючИЗапросНаСертификат = Истина;
	
	СформироватьИОтправитьЗаявление(Контекст);
	
КонецПроцедуры

&НаСервере
Функция СоздатьНовоеЗаявлениеНаПереход(ВходящийКонтекст) Экспорт
	
	Заявление = ОбработкаЗаявленийАбонента.СоздатьНовоеЗаявлениеНаПереход(ВходящийКонтекст);
	УдалосьСоздать = ЗначениеЗаполнено(Заявление);
	Если УдалосьСоздать Тогда
		НовыйДокументЗаявление = Заявление.ПолучитьОбъект();
		ЗначениеВРеквизитФормы(НовыйДокументЗаявление, "ЗаявлениеНаПереход");
	КонецЕсли;
		
	Возврат УдалосьСоздать;
	
КонецФункции
	
&НаСервереБезКонтекста
Функция ДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ДополнительныеРеквизитыУчетнойЗаписи(УчетнаяЗапись);
	
КонецФункции
	
&НаКлиенте
Процедура СформироватьИОтправитьЗаявлениеНаПереходЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	Если ТипЗнч(Результат) = Тип("Структура")
		И Результат.Свойство("ТекстОшибки")
		И ЗначениеЗаполнено(Результат.ТекстОшибки) Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Результат.ТекстОшибки);
		ДобавитьПояснениеКОшибкеПерехода();
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправкеЗаявления();
		
	Иначе
		
		ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправкеЗаявления(ВходящийКонтекст.Организация);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьПояснениеКОшибкеПерехода() Экспорт
	
	Текст = НСтр("ru = 'Исправьте ошибку и повторите отправку'");
	ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(Текст);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНаличиеКриптопровайдера(ВыполняемоеОповещение, ПредлагатьУстановкуРасширения, КриптопровайдерПриКонфликте = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("КриптопровайдерПриКонфликте", КриптопровайдерПриКонфликте);
	
	Оповещение 	= Новый ОписаниеОповещения(
		"ЗаполнитьСписокКриптопровайдеровЗавершение", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	КриптографияЭДКОКлиент.ПолучитьКриптопровайдеры(Оповещение,,ПредлагатьУстановкуРасширения);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьСписокКриптопровайдеровЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	РезультатПоиска = РезультатПоискаКриптопровайдеров(Результат, ВходящийКонтекст);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, РезультатПоиска);
	
КонецПроцедуры

&НаКлиенте
Функция РезультатПоискаКриптопровайдеров(Результат, ВходящийКонтекст) Экспорт
	
	Если ВходящийКонтекст.Свойство("КриптопровайдерПриКонфликте") Тогда
		КриптопровайдерПриКонфликте = ВходящийКонтекст.КриптопровайдерПриКонфликте;
	Иначе
		КриптопровайдерПриКонфликте = Неопределено;
	КонецЕсли;
	
	CryptoProCSPУстановлен	= Ложь;
	ViPNetCSPУстановлен 	= Ложь;
	
	Выполнено = Результат.Выполнено;
	Если Выполнено Тогда
		ЭтоLinux = ОбщегоНазначенияЭДКОКлиент.ЭтоКриптопровайдерLinux();
		ПоддерживаемыеКриптопровайдеры = КриптографияЭДКОКлиентСервер.ПоддерживаемыеКриптопровайдеры(, ЭтоLinux);
		ИндексКриптопровайдеров = Неопределено;
		
		Для каждого Криптопровайдер Из Результат.Криптопровайдеры Цикл
			СвойстваКриптопровайдера = КриптографияЭДКОКлиентСервер.СвойстваКриптопровайдера(
				Криптопровайдер.Имя,
				Криптопровайдер.Тип,
				ПоддерживаемыеКриптопровайдеры,
				ИндексКриптопровайдеров);
			Если СвойстваКриптопровайдера <> Неопределено Тогда
				Если СвойстваКриптопровайдера.ТипКриптопровайдера = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet") Тогда
					ViPNetCSPУстановлен 	= Истина;
				ИначеЕсли СвойстваКриптопровайдера.ТипКриптопровайдера = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro") Тогда
					CryptoProCSPУстановлен 	= Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	КриптопровайдерПриКонфликтеVipNet = НЕ ЗначениеЗаполнено(КриптопровайдерПриКонфликте)
		ИЛИ КриптопровайдерПриКонфликте = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet");
		
	КриптопровайдерПриКонфликтеCryptoPro = НЕ ЗначениеЗаполнено(КриптопровайдерПриКонфликте)
		ИЛИ КриптопровайдерПриКонфликте = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro");
	
	Результат = Новый Структура();
	Результат.Вставить("CryptoProCSPУстановлен", 	CryptoProCSPУстановлен И КриптопровайдерПриКонфликтеCryptoPro);
	Результат.Вставить("ViPNetCSPУстановлен", 		ViPNetCSPУстановлен И КриптопровайдерПриКонфликтеVipNet);
	Результат.Вставить("Выполнено", 				Выполнено);
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ПроверкаСтатусаЗаявленияПередОтправкой

#Область ПроверкаПервичногоЗаявления

&НаКлиенте
Процедура ПроверитьСтатусПодключенияПередОтправкой(ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	СсылкаНаОбъект 		= ВходящийКонтекст.СсылкаНаОбъект;
	СведенияОбОбъекте 	= СведенияПоОтправляемымОбъектам(СсылкаНаОбъект);
	Организация 		= СведенияОбОбъекте.Организация;
	
	ВходящийКонтекст.Вставить("Организация", 			  Организация);
	ВходящийКонтекст.Вставить("ВидКонтролирующегоОргана", СведенияОбОбъекте.ВидКонтролирующегоОргана);
	ВходящийКонтекст.Вставить("КодКонтролирующегоОргана", СведенияОбОбъекте.КодКонтролирующегоОргана);
	
	ЗаявленияАбонента = ОтправленныеЗаявленияАбонентов(
		Организация,
		ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Первичное"),
		СведенияОбОбъекте.ВидКонтролирующегоОргана);
		
	Если ЗаявленияАбонента.Количество() = 0 Тогда
		
		Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст)
			И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина) = 0 Тогда
			
			ТекстСообщения = ДлительнаяОтправкаКлиентСервер.Сообщение1СОтчетностьНеПодключено();
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
			
		Иначе
			ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение(Организация);
		КонецЕсли;
		
	Иначе
		
		ПоследнееЗаявлениеАбонента = ЗаявленияАбонента[0];
		
		Ответ = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьОтветСервераНаЗаявлениеАбонента(ПоследнееЗаявлениеАбонента);
		
		РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
		РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
		
		ЗаявлениеОтправлено = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено");
		ЗаявлениеОтклонено  = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено");
		ЗаявлениеОдобрено   = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено");
		
		ВходящийКонтекст.Вставить("РеквизитыДокумента", 	РеквизитыДокумента);
		ВходящийКонтекст.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
		ВходящийКонтекст.Вставить("Заявление", 				ПоследнееЗаявлениеАбонента);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыполнитьДействиеВыбранноеВСообщенииОЗаявлении", 
			ЭтотОбъект, 
			ВходящийКонтекст);
		
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("СсылкаНаОбъект", 	СсылкаНаОбъект);
		ПараметрыФормы.Вставить("Заявление", 		ПоследнееЗаявлениеАбонента);
		
		ОтсутствуетИнтернет = РезультатОтветаСервера.Свойство("СообщитьОбОтсутствииИнтернета");
		
		Если ОтсутствуетИнтернет Тогда
			
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст)
				И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина) = 0 Тогда
				
				ТекстСообщения = ДлительнаяОтправкаКлиентСервер.СообщениеНетИнтернета();
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
				
			Иначе
				ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение(Организация);
			КонецЕсли;
			
		ИначеЕсли ЗаявлениеОтправлено Тогда
			
			ПоказатьСообщениеОНеобработанномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);

				
		ИначеЕсли ЗаявлениеОдобрено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			ПоказатьСообщениеОбОдобренномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);
			
		ИначеЕсли ЗаявлениеОтклонено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			ПоказатьСообщениеОбОтклоненномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);
			
		Иначе
			
			Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст)
				И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина) = 0 Тогда
				
				ТекстСообщения = ДлительнаяОтправкаКлиентСервер.Сообщение1СОтчетностьНеПодключено();
				ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
				
			Иначе
				ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение(Организация);
			КонецЕсли;
			
		КонецЕсли;
		 
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьДействиеВыбранноеВСообщенииОЗаявлении(Результат, ВходящийКонтекст) Экспорт
	
	ДокументЗаявление = ВходящийКонтекст.Заявление;
	
	Если Результат = "ЗавершитьНастройкуСейчас" Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПроверитьСтатусПодключенияПередОтправкой_ПослеЗавершенияНастрокиЗаявления", 
			ЭтотОбъект, 
			ВходящийКонтекст);
		
		ОткрытьФормуЗавершенияНастройкиМастера(
			ДокументЗаявление, 
			ВходящийКонтекст, 
			ОписаниеОповещения);
			
	ИначеЕсли Результат = "ПодготовитьНовоеЗаявление" Тогда
		
		// Если статус заявления - "Отклонено", тогда создаем новое заявление
		ДополнительныеПараметры = ДокументооборотСКОКлиентСервер.ПараметрыОткрытияМастера();
		ДополнительныеПараметры.Вставить("ЗначениеКопирования", ДокументЗаявление);
			
		ОткрытьФормуЗаявления(ДополнительныеПараметры);

	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСтатусПодключенияПередОтправкой_ПослеЗавершенияНастрокиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение 	= ВходящийКонтекст.ВыполняемоеОповещение;
	Заявление 				= ВходящийКонтекст.Заявление;
	
	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(Заявление);
	
	ЗаявлениеОдобрено  = РеквизитыЗаявления.НастройкаЗавершена
		И РеквизитыЗаявления.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено"); 
	
	Если ЗаявлениеОдобрено Тогда
	
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ПроверкаВторичногоЗаявления

&НаКлиенте
Процедура ЗавершитьНастройкуЗаявленияНаИзменение(СсылкаНаОбъект, ВыполняемоеОповещение) Экспорт
	
	СведенияОбОбъекте 			= СведенияПоОтправляемымОбъектам(СсылкаНаОбъект);
	Организация					= СведенияОбОбъекте.Организация;
	УчетнаяЗапись 				= УчетнаяЗаписьОрганизации(Организация);
	ВидКонтролирующегоОргана 	= СведенияОбОбъекте.ВидКонтролирующегоОргана;
	
	ВходящийКонтекст = Новый Структура;
	ВходящийКонтекст.Вставить("СсылкаНаОбъект", 			СсылкаНаОбъект);
	ВходящийКонтекст.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	
	// Продолжение отправки при отсуствии учетной записи
	Если НЕ ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ТипЗаявления 		= ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение");
	ЗаявленияАбонента 	= ОтправленныеЗаявленияАбонентов(Организация, ТипЗаявления, ВидКонтролирующегоОргана);
	
	Если ЗаявленияАбонента.Количество() = 0 Тогда
		
		// Продолжаем отправку
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		
	Иначе
		
		ПоследнееЗаявлениеАбонента = ЗаявленияАбонента[0];
		
		// Продолжение отправки при отсуствии данного направления. О нем будет напомнено позже.
		Если ЭтоЗаявлениеНаПодключениеНаправления(ПоследнееЗаявлениеАбонента, ВидКонтролирующегоОргана) Тогда
			// Продолжаем отправку.
			// Предупредим пользователя об этом заявлении когда будет выполнена проверка наличия этого направления.
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
			Возврат;
		КонецЕсли;
		
		Ответ = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьОтветСервераНаЗаявлениеАбонента(ПоследнееЗаявлениеАбонента);
		
		РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
		РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
		
		ЗаявлениеОтправлено = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено");
		ЗаявлениеОтклонено  = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено");
		ЗаявлениеОдобрено   = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено");
		
		ВходящийКонтекст.Вставить("РеквизитыДокумента", 	РеквизитыДокумента);
		ВходящийКонтекст.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
		ВходящийКонтекст.Вставить("Заявление", 				ПоследнееЗаявлениеАбонента);
		
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("СсылкаНаОбъект", 	СсылкаНаОбъект);
		ПараметрыФормы.Вставить("Заявление", 		ПоследнееЗаявлениеАбонента);
		
		Если ЭтоЗаявлениеНеТребующееНапоминанияПередОтправкой(ПоследнееЗаявлениеАбонента) Тогда
			
			// Продолжаем отправку
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
			
		ИначеЕсли ЗаявлениеОдобрено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗавершитьНастройкуЗаявленияНаИзменение_СообщениеОбОдобренномЗаявлении_Завершение", 
				ЭтотОбъект, 
				ВходящийКонтекст);
				
			ПоказатьСообщениеОбОдобренномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);
			
		ИначеЕсли ЗаявлениеОтклонено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
				
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗавершитьНастройкуЗаявленияНаИзменение_СообщениеОбОтклоненномЗаявлении_Завершение", 
				ЭтотОбъект, 
				ВходящийКонтекст);
				
			ПоказатьСообщениеОбОтклоненномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);
			
		Иначе
			// Продолжаем отправку
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		КонецЕсли;
		 
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЭтоЗаявлениеНаПодключениеНаправления(Знач ДокументЗаявление, Знач ВидКонтролирующегоОргана) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоЗаявлениеНаПодключениеНаправления(ДокументЗаявление, ВидКонтролирующегоОргана);
	
КонецФункции

&НаКлиенте
Процедура ЗавершитьНастройкуЗаявленияНаИзменение_СообщениеОбОдобренномЗаявлении_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	ДокументЗаявление = ВходящийКонтекст.Заявление;
	
	Если Результат = "ЗавершитьНастройкуСейчас" Тогда
		
		// Для одобренного заявления
		ОткрытьФормуЗавершенияНастройкиМастера(
			ДокументЗаявление, 
			ВходящийКонтекст, 
			ВходящийКонтекст.ВыполняемоеОповещение);
			
	ИначеЕсли Результат = "БольшеНеПоказывать" Тогда
		
		ДобавитьЗаявлениеВНеТребующиеНапоминанияПередОтправкой(ДокументЗаявление);
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗавершитьНастройкуЗаявленияНаИзменение_СообщениеОбОтклоненномЗаявлении_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	ДокументЗаявление = ВходящийКонтекст.Заявление;
	
	Если Результат = "ПодготовитьНовоеЗаявление" Тогда
		
		// Для отклоненного заявления
		ДополнительныеПараметры = ДокументооборотСКОКлиентСервер.ПараметрыОткрытияМастера();
		ДополнительныеПараметры.Вставить("ЗначениеКопирования", ДокументЗаявление);
			
		ОткрытьФормуЗаявления(ДополнительныеПараметры);

	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция КлючЗаявленийНеТребующиеНапоминанияПередОтправкой() Экспорт
	
	Возврат "ДокументооборотСКонтролирующимиОрганами_ЗаявленияНеТребующиеНапоминанияПередОтправкой";
	
КонецФункции

&НаСервере
Функция ЭтоЗаявлениеНеТребующееНапоминанияПередОтправкой(ДокументЗаявление)
	
	// Список заявлений, по которым нажали "Больше не показывать" при отправке
	ЗаявленияПриОтправке = ХранилищеОбщихНастроек.Загрузить(КлючЗаявленийНеТребующиеНапоминанияПередОтправкой());
	
	// Список заявлений, по которым нажали "Больше не показывать" при в уведомлении по таймеру
	ЗаявленияПоТаймеру = ХранилищеОбщихНастроек.Загрузить(ОбработкаЗаявленийАбонента.КлючЗаявленийТребующихНапоминанияПозже());
	
	Если ЗаявленияПриОтправке = Неопределено Тогда
		ЕстьСредиЗаявленияПриОтправке = Ложь;
	Иначе
		ЕстьСредиЗаявленияПриОтправке = ЗаявленияПриОтправке.Найти(ДокументЗаявление) <> Неопределено;
	КонецЕсли;
	
	Если ЗаявленияПоТаймеру = Неопределено Тогда
		ЕстьСредиЗаявленийПоТаймеру = Ложь;
	Иначе
		
		ЗаявлениеВТаблицеЗаявленийПоТаймеру = ЗаявленияПоТаймеру.Найти(ДокументЗаявление, "Заявление");
		
		ЕстьСредиЗаявленийПоТаймеру = ЗаявлениеВТаблицеЗаявленийПоТаймеру <> Неопределено
			И ЗаявлениеВТаблицеЗаявленийПоТаймеру.Дата = Дата(3999, 12, 31, 23, 59, 59);
			
	КонецЕсли;
		
	Возврат ЕстьСредиЗаявленияПриОтправке ИЛИ ЕстьСредиЗаявленийПоТаймеру;
	
КонецФункции
	
&НаСервере
Процедура ДобавитьЗаявлениеВНеТребующиеНапоминанияПередОтправкой(ДокументЗаявление) Экспорт
	
	Заявления = ХранилищеОбщихНастроек.Загрузить(КлючЗаявленийНеТребующиеНапоминанияПередОтправкой());

	Если Заявления = Неопределено Тогда
		Заявления = Новый Массив;
	КонецЕсли;
	
	Заявления.Добавить(ДокументЗаявление);
	Заявления = ОбщегоНазначенияКлиентСервер.СвернутьМассив(Заявления);

	ХранилищеОбщихНастроек.Сохранить(КлючЗаявленийНеТребующиеНапоминанияПередОтправкой(), , Заявления);
	
КонецПроцедуры

#КонецОбласти

#Область НеподключенноеНаправление

&НаКлиенте
Процедура СообщитьОНеподключенномНаправлении(ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
	
	СсылкаНаОбъект 				= ВходящийКонтекст.СсылкаНаОбъект;
	СведенияОбОбъекте 			= СведенияПоОтправляемымОбъектам(СсылкаНаОбъект);
	ВидКонтролирующегоОргана 	= СведенияОбОбъекте.ВидКонтролирующегоОргана;
	Организация					= СведенияОбОбъекте.Организация;
	
	Если НЕ МультирежимВызовСервера.ПользовательПодключенК1СОтчетности(Организация) Тогда
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьФормуПредложениеОформитьЗаявлениеНаПодключение(Организация);
		Возврат;
	КонецЕсли;
	
	// Если не подключено направление, то проверяем наличие отправленного заявления на изменение.
	// Если такого нет, то отправка невозможна и показывам предложение отправить заявление.
	// Если есть, то пытаемся завершить настройку ненастроенного заявления.
	
	ВходящийКонтекст.Вставить("Организация", 			  Организация);
	ВходящийКонтекст.Вставить("ВидКонтролирующегоОргана", СведенияОбОбъекте.ВидКонтролирующегоОргана);
	ВходящийКонтекст.Вставить("КодКонтролирующегоОргана", СведенияОбОбъекте.КодКонтролирующегоОргана);
	
	ЗаявленияАбонента = ОтправленныеЗаявленияАбонентов(
		Организация,
		ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
		ВидКонтролирующегоОргана);
		
	Если ЗаявленияАбонента.Количество() = 0 Тогда
		
		ПоказатьОшибкуНеподключенногоНаправления(ВходящийКонтекст);
		
	Иначе
		
		ПоследнееЗаявлениеАбонента = ЗаявленияАбонента[0];
		
		Ответ = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьОтветСервераНаЗаявлениеАбонента(ПоследнееЗаявлениеАбонента);
		
		РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
		РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
		
		ЗаявлениеОтправлено = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено");
		ЗаявлениеОтклонено  = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено");
		ЗаявлениеОдобрено   = РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено");
		
		ВходящийКонтекст.Вставить("РеквизитыДокумента", 	РеквизитыДокумента);
		ВходящийКонтекст.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
		ВходящийКонтекст.Вставить("Заявление", 				ПоследнееЗаявлениеАбонента);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыполнитьДействиеВыбранноеВСообщенииОЗаявлении", 
			ЭтотОбъект, 
			ВходящийКонтекст);
		
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("СсылкаНаОбъект", 			СсылкаНаОбъект);
		ПараметрыФормы.Вставить("Заявление", 				ПоследнееЗаявлениеАбонента);
		ПараметрыФормы.Вставить("ЭтоПодключениеНаправления",Истина);
		
		ОтсутствуетИнтернет = РезультатОтветаСервера.Свойство("СообщитьОбОтсутствииИнтернета");
		
		Если ЗаявлениеОтправлено Тогда
			
			ПоказатьСообщениеОНеобработанномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);

				
		ИначеЕсли ЗаявлениеОдобрено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			ПоказатьСообщениеОбОдобренномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);

			
		ИначеЕсли ЗаявлениеОтклонено И НЕ РеквизитыДокумента.НастройкаЗавершена Тогда
			
			ПоказатьСообщениеОбОтклоненномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения);

			
		Иначе
			
			ПоказатьОшибкуНеподключенногоНаправления(ВходящийКонтекст);
			
		КонецЕсли;
		 
	КонецЕсли;	
	 
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОНеобработанномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения)
	
	ВернутьСчетчикИспользованияРежимаЗапоминания = Истина;
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст)
		И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, ВернутьСчетчикИспользованияРежимаЗапоминания) = 0 Тогда
		
		ТекстСообщения = ДлительнаяОтправкаКлиентСервер.Сообщение1СОтчетностьНеПодключеноНоЕстьЗаявление();
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
		
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОНеобработанномЗаявлении",
			ПараметрыФормы,,,,,
			ОписаниеОповещения, 
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОбОдобренномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения)
	
	ВернутьСчетчикИспользованияРежимаЗапоминания = Истина;
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст)
		И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, ВернутьСчетчикИспользованияРежимаЗапоминания) = 0 Тогда
		
		ТекстСообщения = ДлительнаяОтправкаКлиентСервер.Сообщение1СОтчетностьНеПодключеноНоЕстьНенастроенноеЗаявление();
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
		
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОбОдобренномЗаявлении",
			ПараметрыФормы,,,,,
			ОписаниеОповещения, 
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщениеОбОтклоненномЗаявлении(ВходящийКонтекст, ПараметрыФормы, ОписаниеОповещения)
	
	ВернутьСчетчикИспользованияРежимаЗапоминания = Истина;
	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст)
		И ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, ВернутьСчетчикИспользованияРежимаЗапоминания) = 0 Тогда
		
		ТекстСообщения = ДлительнаяОтправкаКлиентСервер.Сообщение1СОтчетностьНеПодключеноЗаявлениеОтклонено();
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
		
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.СообщениеОбОтклоненномЗаявлении", 
			ПараметрыФормы,,,,,
			ОписаниеОповещения, 
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;
	
КонецПроцедуры


&НаСервереБезКонтекста
Функция ОтправленныеЗаявленияАбонентов(Знач Организация, Знач ТипЗаявления, Знач ВидКонтролирующегоОргана)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправленныеЗаявленияАбонентов(
		Организация, ТипЗаявления, ВидКонтролирующегоОргана);

КонецФункции

&НаКлиенте
Процедура ПоказатьОшибкуНеподключенногоНаправления(ВходящийКонтекст)

	Если ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(ВходящийКонтекст) Тогда
		ТекстСообщения = ДлительнаяОтправкаКлиентСервер.СообщениеНаправлениеНеПодключено();
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	ОтправкаОбъектаЗапрещена = ДокументооборотСКОВызовСервера.ОтправкаОбъектаЗапрещенаНаТарифеПромоЕНС(ВходящийКонтекст.СсылкаНаОбъект);
	Если ОтправкаОбъектаЗапрещена Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		ДокументооборотСКОКлиент.СообщитьОНевозможностиОтправкиНаТарифе(ВходящийКонтекст.СсылкаНаОбъект);
		Возврат;
	КонецЕсли;
	
	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.
	
	// Перед показом предупреждения проверяем ли срок действия сертификата.
	// Если сертификат не найден или просрочен, не отправляем заявление в скрытом
	// режиме, а открываем мастер.
	
	Организация 			= ВходящийКонтекст.Организация;
	УчетнаяЗапись 			= УчетнаяЗаписьОрганизации(Организация);
	ОтпечатокСертификата 	= ПолучитьОтпечаток(УчетнаяЗапись, "Руководитель", Неопределено);
	МестоХраненияКлюча 		= КонтекстМоделиХраненияКлюча(УчетнаяЗапись);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПоказатьОшибкуНеподключенногоНаправления_ПослеПолученияДатыСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	Если НЕ ЭтоЛокальнаяПодпись(МестоХраненияКлюча) Тогда
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			СвойстваСертификата = ПолучитьСвойстваСертификатаПоОтпечаткуНаСервере(ОтпечатокСертификата, "MY");
		Иначе
			СвойстваСертификата = ПолучитьСвойстваОблачногоСертификата(ОтпечатокСертификата, "MY");
		КонецЕсли;
		
		Результат = Новый Структура;
		Результат.Вставить("Выполнено", 			СвойстваСертификата <> Неопределено);
		Результат.Вставить("СвойстваСертификата", 	СвойстваСертификата);
		Результат.Вставить("СертификатНайден", 		Истина);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификата);
		
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
		
	Иначе
		КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(ОписаниеОповещения, ОтпечатокСертификата, "MY");
	КонецЕсли;
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПоказатьОшибкуНеподключенногоНаправления_ПослеПолученияДатыСертификата(Результат, ВходящийКонтекст) Экспорт
	
	СертификатДоступен 	= Ложь;
	СертификатИстек 	= Ложь;
	
	Если Результат.Выполнено <> Ложь И Результат.СертификатНайден Тогда
		
		СвойстваСертификата = Результат.СвойстваСертификата;
		СертификатДоступен  = Результат.СертификатНайден;
		Если СвойстваСертификата.ДействителенПо < ОбщегоНазначенияКлиент.ДатаСеанса() Тогда
			СертификатИстек = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПоказатьОшибкуНеподключенногоНаправления_ПослеЗакрытияСообщенияОНеподключенномНаправлении", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	СертификатНеДоступенИлиИстек = НЕ СертификатДоступен ИЛИ СертификатИстек;
		
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("СсылкаНаОбъект", ВходящийКонтекст.СсылкаНаОбъект);
	ПараметрыФормы.Вставить("СертификатНеДоступенИлиИстек", СертификатНеДоступенИлиИстек);
	
	ПоддерживаетсяВторичноеЗаявление = ПоддерживаетсяВторичноеЗаявление(ВходящийКонтекст.Организация);
	Если ПоддерживаетсяВторичноеЗаявление Тогда
		
		ОткрыватьФормуПодключенияФНС = СообщитьОНеподключенномНаправлении_ОткрыватьФормуПодключенияФНС(ВходящийКонтекст);
		
		Если ОткрыватьФормуПодключенияФНС Тогда
			ПутьКФорме = ПутьКОбъекту + ".Форма.СообщениеОНеподключенномНаправленииФНС";
		Иначе
			ПутьКФорме = ПутьКОбъекту + ".Форма.СообщениеОНеподключенномНаправлении";
		КонецЕсли;
		
	Иначе
		ПутьКФорме = ПутьКОбъекту + ".Форма.СообщениеОНеподключенномНаправленииПрочиеОператоры";
	КонецЕсли;
	
	ОткрытьФорму(
		ПутьКФорме,
		ПараметрыФормы
		,
		,
		,
		,
		,
		ОписаниеОповещения,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Функция СообщитьОНеподключенномНаправлении_ОткрыватьФормуПодключенияФНС(ВходящийКонтекст)
	
	// Если ФНС не подключен, открывать специальную сокращенную форму с мастером
	УчетнаяЗапись = УчетнаяЗаписьОрганизации(ВходящийКонтекст.Организация);
		
	Если ЗначениеЗаполнено(УчетнаяЗапись) Тогда
		РеквизитыУчетнойЗаписи = ПолучитьНастройкиУчетнойЗаписиИзИБ(УчетнаяЗапись);
		ПодключенФНС = РеквизитыУчетнойЗаписи.ПредназначенаДляДокументооборотаСФНС;
	Иначе
		ПодключенФНС = Ложь;
	КонецЕсли;
	
	ВидКонтролирующегоОргана = ВходящийКонтекст.ВидКонтролирующегоОргана;
	
	Возврат ВидКонтролирующегоОргана = ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС")
		И НЕ ПодключенФНС;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьОшибкуНеподключенногоНаправления_ПослеЗакрытияСообщенияОНеподключенномНаправлении(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		
		// Ошибку о неподключенном направлении закрыли без дальнейших действий.
		// Отправка прерывается.
		
	Иначе
		
		Организация = ВходящийКонтекст.Организация;
		
		ПараметрыОткрытияФормы = Новый Структура();
		ПараметрыОткрытияФормы.Вставить("Организация", Организация);
		
		ФлагиВМастер = Новый Структура();
		ФлагиВМастер.Вставить("ПодключаемоеНаправление", 					Результат);
		ФлагиВМастер.Вставить("ПродлитьСертификат", 						Ложь);
		ФлагиВМастер.Вставить("ИзменитьРеквизитыПодключенияК1СОтчетности", 	Ложь);
		ФлагиВМастер.Вставить("ПродлитьЛицензиюНа1СОтчетность", 			Ложь);
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ФлагиВМастер", ФлагиВМастер);
		
		ПараметрыОткрытияФормы.Вставить("АктивироватьФлагиМастера", ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключение(
			Организация, 
			ЭтотОбъект,
			,
			ПредопределенноеЗначение("Перечисление.ТипыЗаявленияАбонентаСпецоператораСвязи.Изменение"),
			ПараметрыОткрытияФормы);
			
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область УпрощенноеЗаявлениеНаПодключение

&НаСервере
Функция СхемаСдачиОтчетностиФСРАРпоКодуРегиона(КодРегионаФСРАР) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СхемаСдачиОтчетностиФСРАРпоКодуРегиона(КодРегионаФСРАР);
	
КонецФункции

#КонецОбласти

#Область УниверсальнаяПроцедураОткрытияПервичногоИВторичногоМастера

// Процедура - Открыть форму заявления
//
// Описание параметров см в ОМ ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент
// процедура ОткрытьФормуМастераЗаявленияНаПодключение
// 
&НаКлиенте
Процедура ОткрытьФормуЗаявления(ДополнительныеПараметры) Экспорт
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ОткрытьФормуМастераЗаявленияНаПодключениеСлужебный(ДополнительныеПараметры);
			
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область УпрощенноеЗаявление

&НаКлиенте
Функция СрокЛицензииКриптоПроКонечный() Экспорт
	
	ДатаИстеченияЛицензии = ДатаИстеченияЛицензииКриптоПро();
	Если ДатаИстеченияЛицензии = Неопределено Тогда
		ЭтоБесконечнаяЛицензия = Истина;
	Иначе
		ЭтоБесконечнаяЛицензия = ДатаИстеченияЛицензии = БесконечнаяДатаИстеченияЛицензииКриптоПро();
	КонецЕсли;
	
	Возврат НЕ ЭтоБесконечнаяЛицензия;
	
КонецФункции

&НаКлиенте
Функция ЗапретитьВыводОшибокКриптоПро(ДокументЗаявление, НовыйСтатусДокумента) Экспорт
	
	#Если ВебКлиент Тогда
		Возврат Ложь;
		
	#Иначе
		
		Если НЕ ДокументЗаявление.ВключитьЛицензиюКриптоПро
			ИЛИ НовыйСтатусДокумента <> ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено") Тогда
			Возврат Ложь;
		КонецЕсли;
		
		// В вебе нет этого метода
		ИмяФайла = ПолучитьИмяВременногоФайла(".q");
		
		WshShell = Неопределено;
		#Если НЕ МобильныйКлиент Тогда
			Попытка
				WshShell = Новый COMОбъект("WScript.Shell");
			Исключение
			КонецПопытки;
		#КонецЕсли
		
		Если WshShell = Неопределено Тогда
			Возврат Ложь;
		КонецЕсли;
		
		Попытка
			WshShell.Run("cmd.exe /c whoami /user >""" + ИмяФайла+"""", 0, true);
		Исключение
			Возврат Ложь;
		КонецПопытки;
		
		Текст = Новый ТекстовыйДокумент;
		Текст.Прочитать(ИмяФайла, "cp866");
		ПоследняяСтрока = Текст.ПолучитьСтроку(Текст.КоличествоСтрок());
		УдалитьФайлы(ИмяФайла);

		Подстроки = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(ПоследняяСтрока, " ", Истина, Истина);
		
		Если Подстроки.Количество() = 0 Тогда
			Возврат Ложь;
		КонецЕсли;
		
		SID = Подстроки[1];

		ПутьРазрядности = "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment\PROCESSOR_ARCHITECTURE";
		
		Попытка
			Значение = WshShell.RegRead(ПутьРазрядности);
		Исключение
			Возврат Ложь;
		КонецПопытки;
		
		Если Значение = "AMD64" Тогда 
			// 64х битная
			Путь = "HKEY_LOCAL_MACHINE\SOFTWARE\Wow6432Node\Crypto Pro\Settings\USERS\" + SID +"\KeyDevices\LicErrorLevel";
		Иначе
			// 32х битная
			Путь = "HKEY_LOCAL_MACHINE\SOFTWARE\Crypto Pro\Settings\USERS\" + SID +"\KeyDevices\LicErrorLevel";
		КонецЕсли;
		
		Попытка
			WshShell.RegWrite(Путь, 2);
		Исключение
			Возврат Ложь;
		КонецПопытки;
		
		Возврат Истина;
	#КонецЕсли
	
КонецФункции

&НаКлиенте
Процедура ИзменитьПодсказкуРегНомера(Форма) Экспорт
	
	Если Форма.РазделениеВключено Тогда
		
		ПодсказкаКНомеруОсновнойПоставки1с = НСтр("ru = 'Регистрационный номер заполняется автоматически и в общем случае не требует правки. 
                                                   |Но если вы перешли из локальной версии, то регистрационный номер можно найти:
                                                   |- в регистрационной анкете
                                                   |- в договоре ИТС (при наличии)
                                                   |- на коробке с программой, обложках книг, дисках, входящих в комплект поставки'");
		
		Форма.Элементы.НомерОсновнойПоставки1с.РасширеннаяПодсказка.Заголовок = ПодсказкаКНомеруОсновнойПоставки1с;
		
	Иначе
		
		Форма.Элементы.НомерОсновнойПоставки1с.РасширеннаяПодсказка.Заголовок = 
			НСтр("ru = 'Регистрационный номер можно найти:
			           |
			           |- в регистрационной анкете
			           |- в договоре ИТС (при наличии)
			           |- на коробке с программой, обложках книг или дисках, входящих в комплект поставки'");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ТелефоныСовпадают(Телефон1, Телефон2) Экспорт
	
	Представление1 = ЭлектроннаяПодписьВМоделиСервисаКлиентСервер.ПолучитьПредставлениеТелефона(Телефон1);
	Представление2 = ЭлектроннаяПодписьВМоделиСервисаКлиентСервер.ПолучитьПредставлениеТелефона(Телефон2);
	
	Возврат Представление1 = Представление2;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьСписокДоверенностейИлиИнструкцию(НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка, Организация) Экспорт
	
	СтандартнаяОбработка = Ложь;
	Если НавигационнаяСсылкаФорматированнойСтроки = "доверенность" Тогда
		
		ОткрытьФорму("Справочник.ДоверенностиНалогоплательщика.ФормаСписка",,Организация);

	ИначеЕсли НавигационнаяСсылкаФорматированнойСтроки = "МЧД" Тогда
		
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("Раздел", ПредопределенноеЗначение("Перечисление.СтраницыЖурналаОтчетность.Уведомления"));
		ПараметрыОткрытия.Вставить("Организация", Организация);
		
		ОткрытьФорму("ОбщаяФорма.РегламентированнаяОтчетность", ПараметрыОткрытия);
		
	ИначеЕсли НавигационнаяСсылкаФорматированнойСтроки = "регистрации" Тогда
		
		ЗначениеОтбора = Новый Структура("Владелец", Организация);
		ПараметрыВыбора = Новый Структура("Отбор", ЗначениеОтбора);

		ОткрытьФорму("Справочник.РегистрацииВНалоговомОргане.ФормаСписка", ПараметрыВыбора);
		
	Иначе
		ИнтернетПоддержкаПользователейКлиент.ОткрытьСтраницуИнтегрированногоСайта("https://its.1c.ru/bmk/elreps/authorised_representative_pers");
	КонецЕсли;
	
КонецПроцедуры

#Область ОповещениеОбОдобренииИлиОтклоненииЗаявления

#Область ОбвновлениеЗаявленийЧерезФормуОповещения

&НаКлиенте
Процедура СообщитьОбОдобренныхЗаявленияхАбонента_Контейнер(Результат, ВходящийКонтекст) Экспорт
	
	ЕстьОтправленныеЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ОтправленныеЗаявленияАбонентов(,,,,Истина).Количество() > 0;
	
	Если ЕстьОтправленныеЗаявления Тогда
		
		Заявление = ОбработкаЗаявленийАбонентаВызовСервера.СледующееЗаявлениеТребующееРеакцииПользователя();
		
		Если ЗначениеЗаполнено(Заявление) Тогда
			
			Если ОбработкаЗаявленийАбонентаКлиент.ФормаОповещенияИлиМастераУжеОткрыта() Тогда
				
				// Проверим позже
				ОбработкаЗаявленийАбонентаКлиент.ПодключитьОбработчикПроверкиЗаявлений();
				
			Иначе
				
				РеквизитыЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(Заявление);
				ЭтоЗаявлениеНаПодключение = РеквизитыЗаявления.ЭтоУпрощенноеЗаявление;
				
				ДополнительныеПараметры = Новый Структура();
				ДополнительныеПараметры.Вставить("Заявление", 				Заявление);
				ДополнительныеПараметры.Вставить("РеквизитыЗаявления",  	РеквизитыЗаявления);
				ДополнительныеПараметры.Вставить("ЭтоЗаявлениеНаПодключение",  ЭтоЗаявлениеНаПодключение);
				 
				ОписаниеОповещения = Новый ОписаниеОповещения(
					"СообщитьОбОдобренныхЗаявленияхАбонента_ПослеЗакрытияФормыОповещения", 
					ЭтотОбъект,
					ДополнительныеПараметры);
				
				Если ТребуетсяНастройкаЭДО(РеквизитыЗаявления) Тогда
						
					ОткрытьФормуОтправкиДанныхОператоруЭДО(Заявление, ОписаниеОповещения, Истина); // При входе в программу
						
				Иначе
					
					Ответ = ПолучитьОтветСервераНаЗаявлениеАбонента(Заявление);
	
					РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
					РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
					
					Если РезультатОтветаСервера.Свойство("СообщитьОбОтсутствииИнтернета") Тогда
						СообщитьОбОтсутствииИнтернета(ВходящийКонтекст, РезультатОтветаСервера);
						Возврат;
					КонецЕсли;
					
					ВходящийКонтекст = Новый Структура;
					ВходящийКонтекст.Вставить("РеквизитыДокумента", РеквизитыДокумента);
					
					ЗаявлениеТребуетНастройки = НЕ РеквизитыДокумента.НастройкаЗавершена
						И (РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено")
						ИЛИ РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено"));
						
					// Открываем форму настройки только если настройка требуется
					СтатусИзменился = РезультатОтветаСервера.СтатусИзменился ИЛИ ЗаявлениеТребуетНастройки;
						
					Если СтатусИзменился Тогда
						
						ВходящийКонтекст.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
						
						ОткрытьФормуЗавершенияНастройкиМастера(
							Заявление, 
							ВходящийКонтекст, 
							ОписаниеОповещения);
							
					Иначе
						ОбработкаЗаявленийАбонентаКлиент.ПодключитьОбработчикПроверкиЗаявлений();
					КонецЕсли;
						
				КонецЕсли;
				
			КонецЕсли;
			
		Иначе
			
			// Отключения обработчика не делаем,
			// так как заявление может изменить статус, а обработчик будет отключен.
			// Отключим только если нет ни одного отправленного.
			
			// Проверим позже
			ОбработкаЗаявленийАбонентаКлиент.ПодключитьОбработчикПроверкиЗаявлений();
		
		КонецЕсли;
	
	Иначе
		
		// Отключаем только в случае, если нет отправленных.
		ОбработкаЗаявленийАбонентаКлиент.ОтключитьОбработчикПроверкиЗаявлений();
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СообщитьОбОдобренныхЗаявленияхАбонента_ПослеЗакрытияФормыОповещения(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		
		// Проверим следующее заявление
		ОбработкаЗаявленийАбонентаКлиент.ПодключитьОбработчикПроверкиЗаявлений();
			
	Иначе
		
		// Обработчик ожидания подключается позже в процедуре СообщитьОбОдобренныхЗаявленияхАбонента_Завершение
		
		// После настройки проверяем следующее заявление (если есть)
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СообщитьОбОдобренныхЗаявленияхАбонента_Завершение", 
			ЭтотОбъект);
		
		Если ТипЗнч(Результат) = Тип("Структура")
			И Результат.Свойство("СоздатьНовоеЗаявление") Тогда
			// Заявление было отклонено, поэтому вместо завершения настройки созданием новое копированием
			
			Результат.Вставить("ВыполняемоеОповещение", ОписаниеОповещения);
			ОткрытьФормуЗаявления(Результат);
			
		ИначеЕсли ВходящийКонтекст.ЭтоЗаявлениеНаПодключение Тогда
			
			// Завершение настройки выполняется внутри той же формы, а не в отдельной
			ВыполнитьОбработкуОповещения(ОписаниеОповещения);
			
		Иначе
			
			ОткрытьФормуЗавершенияНастройкиМастера(
				ВходящийКонтекст.Заявление, 
				Результат, 
				ОписаниеОповещения);
				
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура СообщитьОбОдобренныхЗаявленияхАбонента_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	ОбработкаЗаявленийАбонентаКлиент.ПодключитьОбработчикПроверкиЗаявлений();
	
КонецПроцедуры

#КонецОбласти

#Область ОбвновлениеЗаявленийИзФормыСпискаИДокумента

&НаКлиенте
Процедура ОбновитьСтатусЗаявленияАбонента_ИзПанелиОтправки(ОписаниеОповещения, Форма, ДокументЗаявление) Экспорт

	Корректно = ЭтоЗаявлениеТекущегоПользователя(ДокументЗаявление, Истина);
	
	Если НЕ Корректно Тогда
		Возврат;
	КонецЕсли;
	
	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление);
	
	Если РеквизитыЗаявления.ПометкаУдаления Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Заявление помечено на удаление. Обновление состояния невозможно'"));
		Возврат;
	КонецЕсли;
	
	// Обновляем состояние заявление
	// Статус не проверяем, так как при ненужном статусе не будет кнопки "Обновить" статус.
	ОбновитьСтатусыЗаявленийАбонентов_ИзФормыСписка(ДокументЗаявление, Форма.УникальныйИдентификатор, ОписаниеОповещения);
	
КонецПроцедуры
		
&НаКлиенте
Процедура ОбновитьСтатусыЗаявленийАбонентов_ИзФормыСписка(
		ДокументЗаявление 			= Неопределено,
		ИдентификаторФормыВладельца = "", 
		ВыполняемоеОповещение 		= Неопределено,
		ОрганизацияЗаявлений 		= Неопределено) Экспорт
	
	ЗаявленияАбонентов = ОбработкаЗаявленийАбонентаВызовСервера.ОтправленныеЗаявленияАбонентов(
		ДокументЗаявление, ОрганизацияЗаявлений,,,Истина);
		
	Если ЗаявленияАбонентов.Количество() = 0 Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Отсутствуют отправленные заявления'"));
	КонецЕсли;
	
	НомерЗаявления = 0;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НомерЗаявления", 				НомерЗаявления);
	ДополнительныеПараметры.Вставить("ЗаявленияАбонентов", 			ЗаявленияАбонентов);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 		ВыполняемоеОповещение);
	// Для того, чтобы сообщение "Статус заявления не изменился" получила нужная форма, а не бублик
	ДополнительныеПараметры.Вставить("ИдентификаторФормыВладельца", ИдентификаторФормыВладельца);
	
	ОбновитьСтатусыЗаявленийАбонентов_ПолучитьОтветВЦикле(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьСтатусыЗаявленийАбонентов_ПолучитьОтветВЦикле(ДополнительныеПараметры)
	
	// Относятся к предыдущему заявлению
	ДополнительныеПараметры.Удалить("РезультатОтветаСервера");
	ДополнительныеПараметры.Удалить("РеквизитыДокумента");
	
	НомерЗаявления 			= ДополнительныеПараметры.НомерЗаявления;
	ЗаявленияАбонентов 		= ДополнительныеПараметры.ЗаявленияАбонентов;
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;

	Если ОбменПрерванПользователем Или НомерЗаявления + 1 > ЗаявленияАбонентов.Количество() Тогда
			
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		КонецЕсли;
		
	Иначе
		
		ОбновитьСтатусыЗаявленийАбонентов_ПолучитьОтветНаСледующееЗаявление(Истина, ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ТребуетсяНастройкаЭДО(РеквизитыДокумента)
	
	ТребуетсяНастройкаЭДО = 
		РеквизитыДокумента.НастройкаЗавершена 
		И РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено")
		И НЕ РеквизитыДокумента.НастройкаЭДОЗавершена 
		И ЗначениеЗаполнено(РеквизитыДокумента.НастройкиЭДО)
		И (РеквизитыДокумента.ПодключитьЭДО ИЛИ РеквизитыДокумента.ПереиздатьСертификатЭДО);
		
	Возврат ТребуетсяНастройкаЭДО;
	
КонецФункции

&НаКлиенте
Процедура ОбновитьСтатусыЗаявленийАбонентов_ПолучитьОтветНаСледующееЗаявление(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> Истина Тогда
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(ВходящийКонтекст) <> Тип("Структура") 
		ИЛИ НЕ ВходящийКонтекст.Свойство("НомерЗаявления") Тогда
		Возврат;
	КонецЕсли;
	
	НомерЗаявления		= ВходящийКонтекст.НомерЗаявления;
	ЗаявленияАбонентов 	= ВходящийКонтекст.ЗаявленияАбонентов;
	ДокументЗаявление	= ЗаявленияАбонентов[НомерЗаявления];
	
	Корректно = ЭтоЗаявлениеТекущегоПользователя(ДокументЗаявление, Ложь);
	Если НЕ Корректно Тогда
		ОбновитьСтатусыЗаявленийАбонентов_ПерейтиКСледующемуЗаявлению(Результат, ВходящийКонтекст);
		Возврат;
	КонецЕсли;
	
	Ответ = ПолучитьОтветСервераНаЗаявлениеАбонента(ДокументЗаявление);
	
	РеквизитыДокумента 		= Ответ.РеквизитыДокумента;
	РезультатОтветаСервера 	= Ответ.РезультатОтветаСервера;
	
	ВходящийКонтекст.Вставить("РеквизитыДокумента", РеквизитыДокумента);
	
	Если РезультатОтветаСервера.Свойство("СообщитьОбОтсутствииИнтернета") Тогда
		СообщитьОбОтсутствииИнтернета(ВходящийКонтекст, РезультатОтветаСервера);
		Возврат;
	КонецЕсли;
	
	Если РезультатОтветаСервера.Свойство("Выполнено") И НЕ РезультатОтветаСервера.Выполнено Тогда
		ТекстОшибки = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление).СтатусКомментарий;
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстОшибки);
		ОбновитьСтатусыЗаявленийАбонентов_ПерейтиКСледующемуЗаявлению(Результат, ВходящийКонтекст);
		Возврат;
	КонецЕсли;
	
	ТребуетсяНастройкаЭДО = ТребуетсяНастройкаЭДО(РеквизитыДокумента);
	
	ЗаявлениеТребуетНастройки = НЕ РеквизитыДокумента.НастройкаЗавершена
		И (РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено")
		ИЛИ РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено"));
		
	// Открываем форму настройки только если настройка требуется
	СтатусИзменился = РезультатОтветаСервера.СтатусИзменился ИЛИ ЗаявлениеТребуетНастройки;
		
	ВходящийКонтекст.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);

	ВыполняемоеОповещение = Новый ОписаниеОповещения(
		"ОбновитьСтатусыЗаявленийАбонентов_ПерейтиКСледующемуЗаявлению", 
		ЭтотОбъект, 
		ВходящийКонтекст);	
	
	Если ТребуетсяНастройкаЭДО Тогда
		
		ОткрытьФормуОтправкиДанныхОператоруЭДО(ДокументЗаявление, ВыполняемоеОповещение, Истина); // Из списка и панели отправке
		
	ИначеЕсли СтатусИзменился Тогда
		
		ОткрытьФормуЗавершенияНастройкиМастера(
			ДокументЗаявление, 
			ВходящийКонтекст, 
			ВыполняемоеОповещение);

	Иначе
			
		СообщитьОбОтсутствииИзмененияВСтатусе(ВходящийКонтекст);
		ОбновитьСтатусыЗаявленийАбонентов_ПерейтиКСледующемуЗаявлению(Результат, ВходящийКонтекст);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьСтатусыЗаявленийАбонентов_ПерейтиКСледующемуЗаявлению(Результат, ДополнительныеПараметры) Экспорт
	
	ПараметрыПодключаемогоОбработчикаЗаявления = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ДополнительныеПараметры);
	
	// Обход ошибок платформы, исправленных в 8.3.11
	// {#10172430#}: Дочерняя форма исчезает при переключении в другое окно, родительская форма остается заблокированной
	// {#10177195#}: Блокируется форма "1С:Отчетность"
	// Ошибка воспроизводится в случае, если форма открывается и быстро закрывается программно, 
	// хотя фактически не успела закрыться.
	ПодключитьОбработчикОжидания("Подключаемый_ПерейтиКСледующемуЗаявлению", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПерейтиКСледующемуЗаявлению()
	
	ДополнительныеПараметры = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ПараметрыПодключаемогоОбработчикаЗаявления);
	ПараметрыПодключаемогоОбработчикаЗаявления = Неопределено;
	
	ДополнительныеПараметры.НомерЗаявления = ДополнительныеПараметры.НомерЗаявления + 1;
	ОбновитьСтатусыЗаявленийАбонентов_ПолучитьОтветВЦикле(ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура СообщитьОбОтсутствииИзмененияВСтатусе(ДополнительныеПараметры)
	
	ИдентификаторФормыВладельца = ДополнительныеПараметры.ИдентификаторФормыВладельца;
	РеквизитыДокумента 			= ДополнительныеПараметры.РеквизитыДокумента;
	
	ШаблонСообщения = НСтр("ru = 'Статус заявления %1 от %2 не изменился'");
	НомерЗаявления  = РеквизитыДокумента.Номер;
	ДатаЗаявления   = Формат(РеквизитыДокумента.Дата,"ДФ=dd.MM.yyyy");
	
	ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		ШаблонСообщения,
		НомерЗаявления,
		ДатаЗаявления);
			
	ДокументооборотСКОКлиентСервер.СообщитьПользователю(ТекстСообщения, ИдентификаторФормыВладельца);

КонецПроцедуры

&НаКлиенте
Функция ЭтоЗаявлениеТекущегоПользователя(Заявление, ЭтоЕдиничноеЗаявление) Экспорт
	
	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(Заявление);
	
	Корректно =
		НЕ РеквизитыЗаявления.ЭтоМультиРежим
		ИЛИ РеквизитыЗаявления.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено")
		ИЛИ РеквизитыЗаявления.ЭтоМультиРежим И РеквизитыЗаявления.Ответственный = ПользователиКлиент.ТекущийПользователь();
	
	Если НЕ Корректно Тогда
		
		ТекстОшибки = НСтр("ru = 'Статус %1 может быть обновлен только пользователем %2, поскольку оно подано от лица %3.'");
		
		Если ЭтоЕдиничноеЗаявление Тогда
			Представление = НСтр("ru = 'данного заявления'");
		Иначе
			Представление = НСтр("ru = 'заявления %1 от %2'");
			Представление = СтрШаблон(Представление, РеквизитыЗаявления.Номер, РеквизитыЗаявления.Дата);
		КонецЕсли;
			
		ТекстОшибки = СтрШаблон(ТекстОшибки,
			Представление,
			РеквизитыЗаявления.Ответственный, 
			РеквизитыЗаявления.ВладелецЭЦП);
		
		Если ЭтоЕдиничноеЗаявление Тогда
			РеквизитыЗаявления.Вставить("ИмяНовогоПользователя", ИмяПользователяИБ(РеквизитыЗаявления.Ответственный));
			МультирежимКлиент.ПредупредитьОНеобходимостиСменитьПользователя(Заявление, РеквизитыЗаявления, ТекстОшибки);
		Иначе
			ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстОшибки);
		КонецЕсли;
			
		Возврат Ложь;
		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаСервере
Функция ИмяПользователяИБ(Пользователь) Экспорт
	
	Возврат Мультирежим.ИмяПользователяИБ(Пользователь);
	
КонецФункции

#КонецОбласти

#Область ОповещениеОбОдобренииИлиОтклоненииЗаявления_СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ПодключитьсяК1СЭДО(Результат, ВыполняемоеОповещение)
	
	// Есть возвращаемый результат - требуется выполнить подключение ЭДО
	ПодключитьЭДО 					= Результат.ПодключитьЭДО; 
	ПереиздатьСертификатЭДО			= Результат.ПереиздатьСертификатЭДО; 
	
	Если ПодключитьЭДО ИЛИ ПереиздатьСертификатЭДО Тогда
		ОткрытьФормуОтправкиДанныхОператоруЭДО(Результат.ДокументЗаявление, ВыполняемоеОповещение); // из настройки заявления
	КонецЕсли;

КонецПроцедуры

&НаСервере
Функция ЕстьПравоНастройкиЭДО()
	
	ЕстьПравоНастройкиЭДО = Истина;
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиПереопределяемый.ЕстьПравоНастройкиЭДО(ЕстьПравоНастройкиЭДО);
	
	Возврат ЕстьПравоНастройкиЭДО;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО(ДокументЗаявление, ВыполняемоеОповещение, ПоказатьПредварительнуюФорму = Ложь)
	
	Если ПоказатьПредварительнуюФорму Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("ДокументЗаявление",     ДокументЗаявление);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПоказаПредварительнойФормы", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ДокументЗаявление", ДокументЗаявление);
			
		ОткрытьФорму(
			ПутьКОбъекту + ".Форма.Мастер_НастройкаЭДО",
			ДополнительныеПараметры,
			,
			,
			,
			,
			ОписаниеОповещения, 
			РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	Иначе
	
		НачатьОтправкиДанныхОператоруЭДО(ДокументЗаявление, ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НачатьОтправкиДанныхОператоруЭДО(ДокументЗаявление, ВыполняемоеОповещение)
	
	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ДокументЗаявление",     ДокументЗаявление);
	ДополнительныеПараметры.Вставить("РеквизитыЗаявления",    РеквизитыЗаявления);

	Если ЕстьПравоНастройкиЭДО() Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПолученияСертификата", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		ПолучитьДанныеЛокальногоСертификатаОрганизации(РеквизитыЗаявления.Организация, ОписаниеОповещения);
		
	Иначе
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПредупреждения", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		ПоказатьПредупреждение(ОписаниеОповещения, НСтр("ru = 'У пользователя недостаточно прав для настройки 1С-ЭДО'"));
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПредупреждения(ВходящийКонтекст) Экспорт
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПоказаПредварительнойФормы(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		Возврат;
	Иначе
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПодключенияКомпоненты", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		КриптографияЭДКОКлиент.СоздатьМенеджерКриптографии(ОписаниеОповещения, Ложь,, Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПодключенияКомпоненты(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		НачатьОтправкиДанныхОператоруЭДО(ВходящийКонтекст.ДокументЗаявление, ВходящийКонтекст.ВыполняемоеОповещение);
		
	Иначе
		
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Не удалось создать менеджер компоненты криптографии'"));
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		Возврат;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеПолученияСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Не удалось найти сертификат 1С-Отчетности на компьютере'"));
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		Возврат;
	Иначе
		ДвДанныеЛокальногоСертификата = Base64Значение(Результат);
	КонецЕсли;
	
	ВходящийКонтекст.Вставить("ДвДанныеЛокальногоСертификата", ДвДанныеЛокальногоСертификата);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеУстановкиРасширенияДляКриптографии", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	ЭлектроннаяПодписьКлиент.УстановитьРасширение(Истина, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеУстановкиРасширенияДляКриптографии(Подключено, ВходящийКонтекст) Экспорт
	
	Если НЕ Подключено Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Не удалось установить расширение для работы с электронной подписью и шифрованием'"));
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ДвДанныеЛокальногоСертификата = ВходящийКонтекст.ДвДанныеЛокальногоСертификата;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеИнициализацииСертификата", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	Сертификат = Новый СертификатКриптографии();
	Сертификат.НачатьИнициализацию(ОписаниеОповещения, ДвДанныеЛокальногоСертификата);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеИнициализацииСертификата(Сертификат, ВходящийКонтекст) Экспорт	
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеЗакрытия", 
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	РеквизитыЗаявления = ВходящийКонтекст.РеквизитыЗаявления;
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентПереопределяемый.ОткрытьФормуОтправкиДанныхОператоруЭДО(
		РеквизитыЗаявления.НастройкиЭДО, 
		Сертификат, 
		ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ОткрытьФормуОтправкиДанныхОператоруЭДО_ПослеЗакрытия(Результат, ВходящийКонтекст) Экспорт
	
	ДокументЗаявление     = ВходящийКонтекст.ДокументЗаявление;
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	Выполнено = Результат.Выполнено;
	Настройки = Результат.Настройки;
	
	Если Выполнено Тогда
		
		ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(
			ДокументЗаявление, 
			Новый Структура("НастройкаЭДОЗавершена", Истина));
	Иначе
		
		ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(
			ДокументЗаявление, 
			Новый Структура("НастройкиЭДО, НастройкаЭДОЗавершена", Настройки, Ложь));
		
	КонецЕсли;
		
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	
КонецПроцедуры

// Возващает СтрокаBase64 сертификата организации
// Преобразуются в ДвДанные так:
// ДвДанные = Base64Значение(СтрокаBase64);
// Возвращается строка для того, чтобы была сериализуема
//
&НаКлиенте
Процедура ПолучитьДанныеЛокальногоСертификатаОрганизации(Организация, ВыполняемоеОповещение) Экспорт
	
	НастройкиУчетнойЗаписи = НастройкиУчетнойЗаписиОрганизации(Организация);
	
	Если НастройкиУчетнойЗаписи = Неопределено Тогда
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		КонецЕсли;
		Возврат;
	КонецЕсли;

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьДанныеЛокальногоСертификатаОрганизации_ПослеПоискаСертификатаПоОтпечатку", 
		ЭтотОбъект,
		ВыполняемоеОповещение);
		
	// Асинхронно получаем ДвДанныеЛокальногоСертификата
	КриптографияЭДКОКлиент.НайтиСертификатПоОтпечатку(ОписаниеОповещения, НастройкиУчетнойЗаписи.СертификатРуководителя, "MY",, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеЛокальногоСертификатаОрганизации_ПослеПоискаСертификатаПоОтпечатку(Результат, ВыполняемоеОповещение) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьДанныеЛокальногоСертификатаОрганизации_ПослеПолученияBase64", 
		ЭтотОбъект,
		ВыполняемоеОповещение);
	
	Если Результат.Выполнено И Результат.СертификатНайден Тогда
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(ОписаниеОповещения, Результат.СвойстваСертификата, Ложь);
	Иначе
		Результат = Новый Структура();
		Результат.Вставить("Выполнено", Ложь);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Результат);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеЛокальногоСертификатаОрганизации_ПослеПолученияBase64(Результат, ВыполняемоеОповещение) Экспорт
	
	Если Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат.СтрокаBase64);
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Функция ИмяФормыНастройкиЗаявления(ДокументЗаявление)

	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление);
	ТекущееИмяФормы = ПутьКОбъекту + ".Форма.МастерФормированияЗаявкиНаПодключениеНастройкаПрограммыУпрощенный";
	
	Возврат ТекущееИмяФормы;

КонецФункции

&НаКлиенте
Функция ПолучитьОтветСервераНаЗаявлениеАбонента(ДокументЗаявление) Экспорт
	
	РезультатОтветаСервера = Новый Структура;
	РезультатОтветаСервера.Вставить("СтатусИзменился", Ложь);
	
	РеквизитыДокумента	= ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление);
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыДокумента);
	
	Ответ = Новый Структура;
	Ответ.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
	Ответ.Вставить("РеквизитыДокумента", 	 РеквизитыДокумента);
	
	// Для заявления с ЭП в модели сервиса обязательно нужно сначала получить идентификатор ключевого контейнера.
	Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
		
		ЗаявлениеОбработано = ОбработкаЗаявленийАбонентаВызовСервера.ОбработатьИзменениеСтатусаЗаявленияАбонентаВМоделиСервиса(ДокументЗаявление);
		
		Если ЗаявлениеОбработано Тогда
			
			// Обновляем структуру реквизитов в доп. параметрах после обращения к серверу.
			РеквизитыДокумента	= ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление);
			
		Иначе
			
			Возврат Ответ;
			
		КонецЕсли;
		
	КонецЕсли;
		
	// Получаем ответ от сервера.
	Если ЭтоПодписьСервиса(МестоХраненияКлюча)
		И РеквизитыДокумента.Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено") Тогда
		
		РезультатОтветаСервера = ОбработкаЗаявленийАбонентаВызовСервера.ШаблонРезультатаПоЗаявлению(ДокументЗаявление);
		РезультатОтветаСервера.Вставить("СтатусИзменился",	Истина);
		РезультатОтветаСервера.Вставить("Выполнено", 		Истина);
		
	Иначе
		
		РезультатОтветаСервера = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьИРазобратьОтветНаЗаявление(ДокументЗаявление,,Истина);
		
		ПолученОтветНаЗаявление = РезультатОтветаСервера.Выполнено;
		ПовторятьСоединение 	= РезультатОтветаСервера.ПовторятьСоединение;
		
		Если НЕ ПолученОтветНаЗаявление И ПовторятьСоединение Тогда
			РезультатОтветаСервера.Вставить("СообщитьОбОтсутствииИнтернета", Истина);
		Иначе
			// Обновляем структуру реквизитов в доп. параметрах после обращения к серверу.
			РеквизитыДокумента = ОбработкаЗаявленийАбонентаВызовСервера.ПолучитьСтруктуруРеквизитовЗаявления(ДокументЗаявление);
		КонецЕсли;
		
	КонецЕсли;
	
	Ответ.Вставить("РезультатОтветаСервера", РезультатОтветаСервера);
	Ответ.Вставить("РеквизитыДокумента", 	 РеквизитыДокумента);
	
	Оповестить(ОбработкаЗаявленийАбонентаКлиент.ИмяСобытияУспешнойОбработкиЗаявленияНаПодключение(), Ответ, ДокументЗаявление);
	
	Возврат Ответ;
	
КонецФункции

&НаКлиенте
Процедура СообщитьОбОтсутствииИнтернета(ДополнительныеПараметры, РезультатОтветаСервера)
	
	ОписаниеОшибки = НСтр("ru = 'Не удалось обратиться к серверу оператора ""Калуга Астрал""'");
	ОписаниеОшибки = Новый ФорматированнаяСтрока(ОписаниеОшибки, Символы.ПС, РезультатОтветаСервера.СтатусКомментарий);
	ОписаниеОшибки = ДокументооборотСКОВызовСервера.ДобавитьТекстОшибкиСертификатаКА(ОписаниеОшибки);
	
	ОписаниеОповещения 	= Новый ОписаниеОповещения("ОбновитьСтатусыЗаявленийАбонентов_ПолучитьОтветНаСледующееЗаявление", ЭтотОбъект, ДополнительныеПараметры);
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(ОписаниеОшибки, ОписаниеОповещения);

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуЗавершенияНастройкиМастера(
		ДокументЗаявление, 
		ВходящийКонтекст, 
		ВыполняемоеОповещение) Экспорт
	
	РезультатОтветаСервера 	= ВходящийКонтекст.РезультатОтветаСервера;
	
	Если НЕ РезультатОтветаСервера.Свойство("ИдентификаторАбонента")
		ИЛИ НЕ РезультатОтветаСервера.Свойство("Статус")
		ИЛИ НЕ РезультатОтветаСервера.Свойство("ОтпечатокСертификатаИзОтвета") Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ОткрытьФормуЗавершенияНастройкиМастераНепосредственно(
		ДокументЗаявление, 
		РезультатОтветаСервера, 
		ВыполняемоеОповещение);
		
КонецПроцедуры
	
&НаКлиенте
Процедура ОткрытьФормуЗавершенияНастройкиМастераНепосредственно(
		ДокументЗаявление, 
		РезультатОтветаСервера, 
		ВыполняемоеОповещение)
		
	СтруктураПараметров = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(РезультатОтветаСервера);
	СтруктураПараметров.Вставить("Ключ",							ДокументЗаявление);
	СтруктураПараметров.Вставить("СтруктураРеквизитовДокумента", 	ДокументЗаявление); 
	СтруктураПараметров.Вставить("ИдентификаторАбонента", 			РезультатОтветаСервера.ИдентификаторАбонента);
	СтруктураПараметров.Вставить("НовыйСтатусДокумента", 			РезультатОтветаСервера.Статус);
	СтруктураПараметров.Вставить("ОтпечатокСертификатаИзОтвета", 	РезультатОтветаСервера.ОтпечатокСертификатаИзОтвета);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьФормуЗавершенияНастройкиЭДО_ПослеЗакрытияФормы", 
		ЭтотОбъект, 
		ВыполняемоеОповещение);
	
	ИмяФормыНастройки = ИмяФормыНастройкиЗаявления(ДокументЗаявление);
	
	ОткрытьФорму(
		ИмяФормыНастройки,
		СтруктураПараметров,,,,,
		ОписаниеОповещения, 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуЗавершенияНастройкиЭДО_ПослеЗакрытияФормы(Результат, ВыполняемоеОповещение) Экспорт
	
	Если ЗначениеЗаполнено(Результат) 
		И ТипЗнч(Результат) = Тип("Структура") Тогда
		
		Если Результат.Свойство("ПодключитьЭДО") ИЛИ Результат.Свойство("ПереиздатьСертификатЭДО") Тогда
			// После закрытия мастера завершаем подключение к ЭДО
			ПодключитьсяК1СЭДО(Результат, ВыполняемоеОповещение);
		Иначе
			// После закрытия мастера требуется создать заявление копированием, так как на это получили отказ.
			Результат.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
			ОткрытьФормуЗаявления(Результат);
		КонецЕсли;
			
	Иначе
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
			
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии(СертификатДляПодписания, ВыполняемоеОповещение, Криптопровайдер = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("СертификатДляПодписания", СертификатДляПодписания);
	ДополнительныеПараметры.Вставить("Криптопровайдер", Криптопровайдер);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьМенеджерИСертификатКриптографии_ПослеПопыткиУстановкиРасширенияРаботыСКриптографией", 
		ЭтотОбъект,
		ДополнительныеПараметры,
		"ПолучитьМенеджерИСертификатКриптографии_ОбработчикОшибкиСертификата",
		ЭтотОбъект);
		
	ЭлектроннаяПодписьКлиент.УстановитьРасширение(Ложь, ОписаниеОповещения);
		
КонецПроцедуры
	
&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_ПослеПопыткиУстановкиРасширенияРаботыСКриптографией(РасширениеУстановлено, ВходящийКонтекст) Экспорт
	
	Если РасширениеУстановлено = Истина Тогда
		
		Криптопровайдер = ВходящийКонтекст.Криптопровайдер;
		Если Криптопровайдер = Неопределено Тогда
		
			// Извлекаем криптопровайдер из сертификата
			МассивИзОдного = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ВходящийКонтекст.СертификатДляПодписания);
			ИнформацияОСертификате = КриптографияЭДКОСлужебныйВызовСервера.ИзвлечьИнформациюОКриптопровайдереПоСертификату(
				МассивИзОдного,
				Истина);
				
			Если ИнформацияОСертификате.Количество() > 0 Тогда
				
				СвойстваКриптопровайдера = ИнформацияОСертификате[0].Криптопровайдер;
				
				Если СвойстваКриптопровайдера = Неопределено Тогда
					
					ОписаниеОповещения = Новый ОписаниеОповещения(
						"ПолучитьМенеджерИСертификатКриптографии_ЗаполнитьСписокКриптопровайдеровЗавершение", 
						ЭтотОбъект, 
						ВходящийКонтекст);
						
					ПроверитьНаличиеКриптопровайдера(ОписаниеОповещения, Истина);
					
				Иначе
					
					СвойстваКриптопровайдера = ИнформацияОСертификате[0].Криптопровайдер;
					ПолучитьМенеджерИСертификатКриптографии_НачатьИнициализацию(СвойстваКриптопровайдера, ВходящийКонтекст);
						
				КонецЕсли;
				
			Иначе
				ОписаниеОшибки = НСтр("ru = 'Не удалось определить вид криптопровайдера из сертификата'");
				ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
			КонецЕсли;
			
		Иначе
			
			Если Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.CryptoPro") Тогда
				СвойстваКриптопровайдера = КриптографияЭДКОКлиентСервер.КриптопровайдерCryptoPro();
			ИначеЕсли Криптопровайдер = ПредопределенноеЗначение("Перечисление.ТипыКриптоПровайдеров.VipNet") Тогда
				СвойстваКриптопровайдера = КриптографияЭДКОКлиентСервер.КриптопровайдерViPNet();
			Иначе
				ОписаниеОшибки = НСтр("ru = 'Криптопровайдер, отличный от ViPNet CSP и CryptoPro CSP, не поддерживается. 
                                       |Ваш криптопровайдер - %1'");
				ОписаниеОшибки = СтрШаблон(ОписаниеОшибки, Строка(Криптопровайдер));
				ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
				Возврат;
			КонецЕсли;
			
			ПолучитьМенеджерИСертификатКриптографии_НачатьИнициализацию(СвойстваКриптопровайдера, ВходящийКонтекст);
			
		КонецЕсли;
		
	Иначе
		
		ОписаниеОшибки = НСтр("ru = 'Не удалось установить расширение, необходимое для работы с криптографией'");
		ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
			
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_ЗаполнитьСписокКриптопровайдеровЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	CryptoProCSPУстановлен	= Результат.CryptoProCSPУстановлен;
	ViPNetCSPУстановлен 	= Результат.ViPNetCSPУстановлен;
	
	Если CryptoProCSPУстановлен Тогда
		Криптопровайдер = КриптографияЭДКОКлиентСервер.КриптопровайдерCryptoPro();
	ИначеЕсли ViPNetCSPУстановлен Тогда
		Криптопровайдер = КриптографияЭДКОКлиентСервер.КриптопровайдерViPNet();
	КонецЕсли;
	
	ПолучитьМенеджерИСертификатКриптографии_НачатьИнициализацию(Криптопровайдер, ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_НачатьИнициализацию(СвойстваКриптопровайдера, ВходящийКонтекст)
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьМенеджерИСертификатКриптографии_ПослеИнициализацииМенеджераКриптографии", 
		ЭтотОбъект,
		ВходящийКонтекст,
		"ПолучитьМенеджерИСертификатКриптографии_ОбработчикОшибкиСертификата",
		ЭтотОбъект);
	
	МенеджерКриптографии = Новый МенеджерКриптографии();
	Если ОбщегоНазначенияКлиент.ЭтоMacOSКлиент() Тогда
		МенеджерКриптографии.НачатьИнициализацию(
			ОписаниеОповещения,
			СвойстваКриптопровайдера.Имя,
			СвойстваКриптопровайдера.Путь,
			СвойстваКриптопровайдера.Тип);
	Иначе
		МенеджерКриптографии.НачатьИнициализацию(
			ОписаниеОповещения,
			СвойстваКриптопровайдера.Имя,
			СвойстваКриптопровайдера.Путь,
			СвойстваКриптопровайдера.Тип,
			Вычислить("ИспользованиеИнтерактивногоРежимаКриптографии.Использовать"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_ОбработчикОшибкиСертификата(ИнформацияОбОшибке, СтандартнаяОбработка, ВходящийКонтекст) Экспорт
	
	СтандартнаяОбработка = Ложь;
	ОписаниеОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
	ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_ПослеИнициализацииМенеджераКриптографии(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		
		ВходящийКонтекст.Вставить("МенеджерКриптографии", Неопределено);
		ОписаниеОшибки = НСтр("ru = 'Не удалось получить менеджер для работы с криптографией'");
		ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
		
	Иначе
		
		ВходящийКонтекст.Вставить("МенеджерКриптографии", Результат);
			
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПолучитьМенеджерИСертификатКриптографии_ПослеЭкспортаСертификатаВФайл",
			ЭтотОбъект,
			ВходящийКонтекст);
			
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВФайл(
			ОписаниеОповещения, 
			ВходящийКонтекст.СертификатДляПодписания, 
			, 
			Ложь);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_ПослеЭкспортаСертификатаВФайл(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ПолучитьМенеджерИСертификатКриптографии_ПослеИнициализацииСертификата", 
			ЭтотОбъект,
			ВходящийКонтекст,
			"ПолучитьМенеджерИСертификатКриптографии_ОбработчикОшибкиСертификата",
			ЭтотОбъект);
			
		СертификатКриптографии = Новый СертификатКриптографии;
		СертификатКриптографии.НачатьИнициализацию(ОписаниеОповещения, Результат.ИмяФайлаСертификата); 
		
	Иначе
		ОписаниеОшибки = НСтр("ru = 'Не удалось сохранить сертификат'");
		ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_ПослеИнициализацииСертификата(РезультатИнициализации, ВходящийКонтекст) Экспорт
	
	Если РезультатИнициализации <> Неопределено Тогда
		
		Результат = Новый Структура();
		Результат.Вставить("Выполнено", Истина);
		Результат.Вставить("СертификатКриптографии", 	РезультатИнициализации);
		Результат.Вставить("МенеджерКриптографии", 		ВходящийКонтекст.МенеджерКриптографии);
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Результат);
		
	Иначе
		ОписаниеОшибки = НСтр("ru = 'Не удалось инициализировать сертификат'");
		ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьМенеджерИСертификатКриптографии_СообщитьОНеготовностиМенеджераКриптографии(ОписаниеОшибки, ВходящийКонтекст)
	
	//   РасширениеУстановлено
	//       * Истина - Пользователь подтвердил установку, после установки расширение было успешно подключено.
	//       * Ложь   - Пользователь подтвердил установку, однако после установки расширение не удалось подключить.
	//       * Неопределено - Пользователь отказался от установки.

	Результат = Новый Структура();
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", ОписаниеОшибки);
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Результат);

КонецПроцедуры

&НаКлиенте
Процедура ОбновитьАдрес(НовыйАдрес, ВидАдреса, ВыполняемоеОповещение, ЭтоЮридическоеЛицо) Экспорт
	
	Форма = ВыполняемоеОповещение.Модуль;
	СпроситьПроАвтозаполнениеАдреса = Ложь;
	
	Если НовыйАдрес.Модифицированность Тогда
		
		Если ВидАдреса = "АдресЮридический" Тогда
			ДругойАдрес = "АдресФактический";
		Иначе
			ДругойАдрес = "АдресЮридический";
		КонецЕсли;
		
		АдресЗначение       = ВидАдреса + "Значение";
		ДругойАдресЗначение = ДругойАдрес + "Значение";
			
		НовыйАдресЗаполнен    = ЗначениеЗаполнено(НовыйАдрес.АдресПредставление);
		АдресаРаньшеСовпадали = Форма[АдресЗначение] = Форма[ДругойАдресЗначение];
			
		АдресИзменился        = Форма[АдресЗначение] <> НовыйАдрес.Адрес;
		ДругойАдресПустой     = НЕ ЗначениеЗаполнено(Форма[ДругойАдресЗначение]);
		
		Форма[ВидАдреса + "Значение"] 		= НовыйАдрес.Адрес;
		Форма[ВидАдреса + "Представление"]  = НовыйАдрес.АдресПредставление;
		
		СпроситьПроАвтозаполнениеАдреса = НовыйАдресЗаполнен
			И АдресИзменился И (АдресаРаньшеСовпадали ИЛИ ДругойАдресПустой);
		
	КонецЕсли;
	
	Если СпроситьПроАвтозаполнениеАдреса Тогда
		ПредложитьАвтозаполнениеАдреса(ВидАдреса, ВыполняемоеОповещение, ЭтоЮридическоеЛицо);
	Иначе
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПредложитьАвтозаполнениеАдреса(ВидАдреса, ВыполняемоеОповещение, ЭтоЮридическоеЛицо)
	
	Если ЭтоЮридическоеЛицо Тогда
		Если ВидАдреса = "АдресЮридический" Тогда
			ТекстВопроса = НСтр("ru = 'Заполнить фактический адрес по юридическому?'");
		Иначе
			ТекстВопроса = НСтр("ru = 'Заполнить юридический адрес по фактическому?'");
		КонецЕсли;
	Иначе
		Если ВидАдреса = "АдресЮридический" Тогда
			ТекстВопроса = НСтр("ru = 'Заполнить фактический адрес проживания по адресу регистрации?
                                 |Адрес регистрации также отправляется в составе заявления и доступен для изменения в форме расширенных настроек.'");
		Иначе
			ТекстВопроса = НСтр("ru = 'Заполнить адрес регистрации по фактическому адресу проживания?'");
		КонецЕсли;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВидАдреса", ВидАдреса);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОбновитьАдресПослеВопросаОКопированииАдреса", 
		ЭтотОбъект, 
		ДополнительныеПараметры); 
	
	ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет); 
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьАдресПослеВопросаОКопированииАдреса(ОтветПользователя, ВходящийКонтекст) Экспорт
	
	Форма = ВходящийКонтекст.ВыполняемоеОповещение.Модуль;
	
	Если ОтветПользователя = КодВозвратаДиалога.Да Тогда
		Если ВходящийКонтекст.ВидАдреса = "АдресЮридический" Тогда
			Форма["АдресФактическийЗначение"]      = Форма["АдресЮридическийЗначение"];
			Форма["АдресФактическийПредставление"] = Форма["АдресЮридическийПредставление"];
		Иначе
			Форма["АдресЮридическийЗначение"]      = Форма["АдресФактическийЗначение"];
			Форма["АдресЮридическийПредставление"] = Форма["АдресФактическийПредставление"];
		КонецЕсли;
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение);
	
КонецПроцедуры

&НаКлиенте
Функция БесконечнаяДатаИстеченияЛицензииКриптоПро() Экспорт
	
	Возврат Дата(1899,12,30);
	
КонецФункции

&НаКлиенте
Функция ДатаИстеченияЛицензииКриптоПро() Экспорт
	
	#Если ВебКлиент Тогда
		ЛицензиюНельзяВычислить = Истина;
	#Иначе
		ЛицензиюНельзяВычислить = СтрНайти(ЭтотОбъект.ИмяФормы, "ВнешняяОбработка") > 0;
	#КонецЕсли
	
	Если ЛицензиюНельзяВычислить Тогда
		Возврат Неопределено;
	КонецЕсли;
		
	Возврат ОбработкаЗаявленийАбонентаКлиентПовтИсп.ДатаИстеченияЛицензииКриптоПро();
	
КонецФункции

#КонецОбласти

#Область НастройкиОбмена

// Выполняет поиск сертификатов в хранилище по их серийному номеру и издателю сертификата или по отпечатку.
//
// Параметры:
//  ОповещениеОЗавершении - ОписаниеОповещения - описание процедуры, принимающей результат.
//    Результат - Структура:
//      * ПоискСертификатовВыполнен                - Булево.
//      * ТекущаяДатаНаСервере                     - Дата.
//      * ПоддерживаемыеНаправленияДляОператоров   - Неопределено - при Настройки.ПолучатьПоддерживаемыеНаправленияДляОператоров = Ложь.
//                                                 - Соответствие:
//        ** Ключ                                  - ПеречислениеСсылка.СпецоператорыСвязи - операторы, поддерживающие регистрацию.
//        ** Значение                              - Структура:
//           *** ПризнакПоддержкиФСРАР             - Булево.
//           *** ПризнакПоддержкиРПН               - Булево.
//           *** ПризнакПоддержкиФТС               - Булево.
//      * ПоддерживаетсяСдачаОтчетностиВЦБ         - Булево.
//      * СписокПользователейБазы                  - СписокЗначений - все пользователи базы, для текущего указана
//                                                                    картинка БиблиотекаКартинок.Пользователь.
//      * НастройкиОбмена                          - Массив:
//        ** ИндексОрганизации                       - Число.
//        ** Организация                             - СправочникСсылка.Организации.
//        ** ТипОрганизации                          - Булево - Истина, если это юридическое лицо.
//        ** НаименованиеСокращенное                 - Строка - краткое наименование организации или ИП, может быть пустым.
//        ** НаименованиеОрганизации                 - Строка.
//        ** КодНО                                   - Строка - из организации, присутствует, если
//                                                              Настройки.ПолучитьРеквизитыКонтролирующихОргановОрганизации = Истина.
//        ** КодОрганаФСГС                           - Строка - из организации, присутствует, если
//                                                              Настройки.ПолучитьРеквизитыКонтролирующихОргановОрганизации = Истина.
//        ** КППЮЛ                                   - Строка - для ИП содержит пустую строку, присутствует, если
//                                                              Настройки.ПолучитьРеквизитыКонтролирующихОргановОрганизации = Истина.
//        ** РегНомПФР                               - Строка - при использовании ИП наемных работников сюда переносится
//                                                              регистрационный номер ПФР за себя, присутствует, если
//                                                              Настройки.ПолучитьРеквизитыКонтролирующихОргановОрганизации = Истина.
//        ** РегистрационныйНомерФСС                 - Строка - при использовании ИП наемных работников сюда переносится
//                                                              регистрационный номер ФСС добровольного страхования, присутствует, если
//                                                              Настройки.ПолучитьРеквизитыКонтролирующихОргановОрганизации = Истина.
//        ** ПометкаУдаления                         - Булево - из организации.
//        ** ВидОбменаСКонтролирующимиОрганами       - ПеречислениеСсылка.ВидыОбменаСКонтролирующимиОрганами.
//        ** УчетнаяЗаписьОбмена                     - СправочникСсылка.УчетныеЗаписиДокументооборота.
//        ** ПравоЧтенияУчетнойЗаписиОбмена          - Булево.
//        ** СпецоператорСвязи                       - ПеречислениеСсылка.СпецоператорыСвязи.
//        ** НаименованиеОператора                   - Строка - СпецоператорСвязи.
//        ** РежимАвтонастройки                      - ПеречислениеСсылка.РежимыАвтонастройкиУчетнойЗаписиНалогоплательщика.
//        ** ПредназначенаДляДокументооборотаСФНС    - Булево - из учетной записи документооборота.
//        ** ПредназначенаДляДокументооборотаСПФР    - Булево - из учетной записи документооборота.
//        ** ПредназначенаДляДокументооборотаСФСГС   - Булево - из учетной записи документооборота
//        ** ПредназначенаДляДокументооборотаСЦБ     - Булево - из учетной записи документооборота
//        ** СертификатРуководителя                  - Строка - отпечаток из учетной записи документооборота.
//        ** СвойстваСертификатаРуководителя         - Структура    - описание см. КриптографияЭДКОКлиент.НайтиСертификаты.
//                                                   - Неопределено - если ПоискСертификатовВыполнен = Ложь.
//        ** ЛицензияНаименование                    - Строка.
//        ** ЛицензияДатаОкончания                   - Дата.
//                                                   - Неопределено.
//        ** ЛицензияТребуетсяНапоминаниеОбОкончанииСрокаДействия - Булево.
//        ** ЭлектроннаяПодписьВМоделиСервиса        - Булево - устаревшее
//		  ** МестоХраненияКлюча						 - Структура - значение этого поля нужно получать функцией КонтекстМоделиХраненияКлюча()
//        ** ДатаПодключения                         - Дата         - наиболее ранняя дата из учетной записи
//                                                                    документооборота или заявления на подключение, изменение.
//                                                   - Неопределено - до поддержки даты в учетной записи, нет заявлений.
//        ** СдаватьВФНС                             - Булево - информация по данным файла автонастройки.
//        ** СдаватьВПФР                             - Булево - информация по данным файла автонастройки.
//        ** СдаватьВФСГС                            - Булево - информация по данным файла автонастройки.
//        ** СдаватьВФСС                             - Булево - информация по данным файла автонастройки.
//        ** СдаватьВФСРАР                           - Булево - информация по данным файла автонастройки.
//        ** СдаватьВРПН                             - Булево - информация по данным файла автонастройки.
//        ** СдаватьВФТС                             - Булево - информация по данным файла автонастройки.
//        ** СдаватьВБанкРоссии                      - Булево - всегда Ложь.
//        ** СдаватьВМинобороны                      - Булево - всегда Ложь.
//        ** КодыФНС                                 - Массив - информация по данным файла автонастройки:
//           *** Код                                   - Строка.
//           *** КПП                                   - Строка.
//        ** КодПФР                                  - Строка.
//        ** КодыФСГС                                - Массив.
//        ** СписокПользователей                     - СписокЗначений - все пользователи, для пользователей учетной записи
//                                                                      документооборота установлена Пометка, для текущего указана
//                                                                      картинка БиблиотекаКартинок.Пользователь.
//        ** НастройкиОбменаФСС_ИспользоватьОбмен    - Булево.
//        ** НастройкиОбменаФСС_ИспользоватьАвтонастройку - Булево.
//        ** НастройкиОбменаФСС_СертификатСтрахователяОтпечаток - Строка.
//        ** НастройкиОбменаФСС_СвойстваСертификатаСтрахователя - Структура -    описание см. КриптографияЭДКОКлиент.НайтиСертификаты,
//                                                                               присутствует, если
//                                                                               Настройки.НайтиСертификатыПрочихКонтролирующихОрганов = Истина.
//                                                              - Неопределено - если ПоискСертификатовВыполнен = Ложь.
//        ** НастройкиОбменаФСРАР_ИспользоватьОбмен   - Булево.
//        ** НастройкиОбменаФСРАР_ИспользоватьАвтонастройку - Булево.
//        ** НастройкиОбменаФСРАР_СертификатАбонентаОтпечаток - Строка.
//        ** НастройкиОбменаФСРАР_СвойстваСертификатаАбонента - Структура    - описание см. КриптографияЭДКОКлиент.НайтиСертификаты,
//                                                                             присутствует, если
//                                                                             Настройки.НайтиСертификатыПрочихКонтролирующихОрганов = Истина.
//                                                            - Неопределено - если ПоискСертификатовВыполнен = Ложь.
//        ** НастройкиОбменаРПН_ИспользоватьОбмен     - Булево.
//        ** НастройкиОбменаРПН_ИспользоватьАвтонастройку - Булево.
//        ** НастройкиОбменаРПН_СертификатАбонентаОтпечаток - Строка.
//        ** НастройкиОбменаРПН_СвойстваСертификатаАбонента - Структура    - описание см. КриптографияЭДКОКлиент.НайтиСертификаты,
//                                                          - Неопределено - если ПоискСертификатовВыполнен = Ложь.
//                                                                           присутствует, если
//                                                                           Настройки.НайтиСертификатыПрочихКонтролирующихОрганов = Истина.
//                                                          - отсутствует  - при ПоискТолькоСертификатовУчетныхЗаписей = Истина.
//        ** НастройкиОбменаФТС_ИспользоватьОбмен     - Булево.
//        ** НастройкиОбменаФТС_ИспользоватьАвтонастройку - Булево.
//        ** НастройкиОбменаФТС_СертификатАбонентаОтпечаток - Строка.
//        ** НастройкиОбменаФТС_СвойстваСертификатаАбонента - Структура    - описание см. КриптографияЭДКОКлиент.НайтиСертификаты,
//                                                                           присутствует, если
//                                                                           Настройки.НайтиСертификатыПрочихКонтролирующихОрганов = Истина.
//                                                          - Неопределено - если ПоискСертификатовВыполнен = Ложь.
//                                                          - отсутствует  - при ПоискТолькоСертификатовУчетныхЗаписей = Истина.
//        ** НастройкиОбменаМинобороны_ИспользоватьОбмен - Булево.
//        ** НастройкиОбменаМинобороны_СертификатАбонентаОтпечаток - Строка.
//        ** НастройкиОбменаМинобороны_СвойстваСертификатаАбонента - Структура    - описание см. КриптографияЭДКОКлиент.НайтиСертификаты,
//                                                                                  присутствует, если
//                                                                                  Настройки.НайтиСертификатыПрочихКонтролирующихОрганов = Истина.
//                                                                 - Неопределено - если ПоискСертификатовВыполнен = Ложь.
//                                                                 - отсутствует  - при ПоискТолькоСертификатовУчетныхЗаписей = Истина.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_Ссылка - Документы.ЗаявлениеАбонентаСпецоператораСвязи - последнего заявления.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_ТипЗаявления - ПеречислениеСсылка.ТипыЗаявленияАбонентаСпецоператораСвязи - последнего заявления.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_Статус - ПеречислениеСсылка.СтатусыЗаявленияАбонентаСпецоператораСвязи - последнего заявления.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_СтатусКомментарий - Строка - последнего заявления.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_НастройкаЗавершена - Булево - последнего заявления.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_Дата - Дата - последнего заявления.
//                                                      Неопределено.
//        ** ЗаявлениеАбонентаСпецоператораСвязи_ДатаПолученияОтвета - Дата - дата получения ответа первого одобоенного заявления,
//                                                                            настройка для которого завершена.
//                                                                     Неопределено.
//        ** ПризнакПоддержкиФНС                     - Булево - признак поддержки ФНС в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//        ** ПризнакПоддержкиПФР                     - Булево - признак поддержки ПФР в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//        ** ПризнакПоддержкиФСГС                    - Булево - признак поддержки Росстата в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//        ** ПризнакПоддержкиФСС                     - Булево - признак поддержки ФСС в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//        ** ПризнакПоддержкиФСРАР                   - Булево - признак поддержки ФСРАР в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//        ** ПризнакПоддержкиРПН                     - Булево - признак поддержки РПН в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//        ** ПризнакПоддержкиФТС                     - Булево - признак поддержки ФТС в конфигурации для организации, с учетом
//                                                              ПоддерживаемыеНаправленияДляОператоров, если запрошено их получение.
//
//  Ссылки - Массив - СправочникСсылка.Организации.
//         - СправочникСсылка.Организации.
//         - СправочникСсылка.УчетныеЗаписиДокументооборота.
//         - ДокументСсылка или СправочникСсылка объекта, содержащего реквизит "Организация", например,
//           ДокументСсылка.ЗаявлениеАбонентаСпецоператораСвязи.
//
//  Настройки - Неопределено.
//            - Структура:
//    * НайтиСертификатыПрочихКонтролирующихОрганов       - Булево - при Истина искать сертификаты ФСС, ФСРАР, РПН, ФТС, Банка России.
//    * ПолучитьРеквизитыКонтролирующихОргановОрганизации - Булево - при Истина получать реквизиты результата: КодНО,
//                                                                   КодОрганаФСГС, КППЮЛ, РегНомПФР, РегистрационныйНомерФСС.
//    * ПолучатьДляУдаленныхОрганизаций                   - Булево - по умолчанию Истина.
//    * ПолучатьПоддерживаемыеНаправленияДляОператоров    - Булево - по умолчанию Истина.
//
&НаКлиенте
Процедура НастройкиОбменаОрганизаций(
		ОповещениеОЗавершении,
		Ссылки = Неопределено,
		Настройки = Неопределено) Экспорт
	
	НастройкиВызова = Новый Структура;
	НастройкиВызова.Вставить("НайтиСертификатыПрочихКонтролирующихОрганов", Ложь);
	Если Настройки <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(НастройкиВызова, Настройки);
	КонецЕсли;
	
	НастройкиОбменаИПараметры = НастройкиОбменаОрганизацийНаСервере(Ссылки, Настройки);
	НастройкиОбмена = НастройкиОбменаИПараметры.НастройкиОбмена;
	
	УникальныеСертификаты = Новый Соответствие;
	
	КлючиОтпечатковСертификатов = Новый Массив;
	КлючиОтпечатковСертификатов.Добавить("СертификатРуководителя");
	Если НастройкиВызова.НайтиСертификатыПрочихКонтролирующихОрганов Тогда
		КлючиОтпечатковСертификатов.Добавить("НастройкиОбменаФСС_СертификатСтрахователяОтпечаток");
		КлючиОтпечатковСертификатов.Добавить("НастройкиОбменаФСРАР_СертификатАбонентаОтпечаток");
		КлючиОтпечатковСертификатов.Добавить("НастройкиОбменаРПН_СертификатАбонентаОтпечаток");
		КлючиОтпечатковСертификатов.Добавить("НастройкиОбменаФТС_СертификатАбонентаОтпечаток");
		КлючиОтпечатковСертификатов.Добавить("НастройкиОбменаМО_СертификатАбонентаОтпечаток");
	КонецЕсли;
	
	Для ИндексОрганизации = 0 По НастройкиОбмена.Количество() - 1 Цикл
		НастройкаОбмена = НастройкиОбмена[ИндексОрганизации];
		
		Для каждого КлючОтпечаткаСертификата Из КлючиОтпечатковСертификатов Цикл
			ОтпечатокСертификата = "";
			НастройкаОбмена.Свойство(КлючОтпечаткаСертификата, ОтпечатокСертификата);
			
			Если ЗначениеЗаполнено(ОтпечатокСертификата) Тогда
				Если ЭтоЛокальнаяПодпись(НастройкаОбмена) Тогда
					ПредставлениеОтпечатка = ОтпечатокСертификата + "_0";
				Иначе	
					ПредставлениеОтпечатка = ОтпечатокСертификата + "_1";
				КонецЕсли;	
				СертификатыВНастройках = УникальныеСертификаты[ПредставлениеОтпечатка];
				Если СертификатыВНастройках = Неопределено Тогда
					СертификатыВНастройках = Новый Массив;
				КонецЕсли;
				
				СертификатВНастройках = Новый Структура("ИндексОрганизации, КлючОтпечаткаСертификата",
					ИндексОрганизации, КлючОтпечаткаСертификата);
				СертификатыВНастройках.Добавить(СертификатВНастройках);
				УникальныеСертификаты[ПредставлениеОтпечатка] = СертификатыВНастройках;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	ЕстьЛокальнаяЭлектроннаяПодпись = Ложь;
	ЕстьЭлектроннаяПодписьВМоделиСервиса = Ложь;
	СертификатыДляПоиска = Новый Массив;
	Для каждого УникальныйСертификат Из УникальныеСертификаты Цикл
		СертификатВНастройках = УникальныйСертификат.Значение[0];
		НастройкаОбмена = НастройкиОбмена[СертификатВНастройках.ИндексОрганизации];
		ОтпечатокСертификата = "";
		НастройкаОбмена.Свойство(СертификатВНастройках.КлючОтпечаткаСертификата, ОтпечатокСертификата);
		
		СертификатДляПоиска = Новый Структура("Отпечаток", ОтпечатокСертификата);
		КонтекстМоделиХраненияКлюча(НастройкаОбмена, СертификатДляПоиска);
		СертификатыДляПоиска.Добавить(СертификатДляПоиска);
		
		Если ЭтоЛокальнаяПодпись(НастройкаОбмена) Тогда
			ЕстьЛокальнаяЭлектроннаяПодпись = Истина;
		Иначе
			ЕстьЭлектроннаяПодписьВМоделиСервиса = Истина;
		КонецЕсли;
	КонецЦикла;
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("НайтиСертификатыПрочихКонтролирующихОрганов", 	НастройкиВызова.НайтиСертификатыПрочихКонтролирующихОрганов);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", 							ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("НастройкиОбменаИПараметры", 						НастройкиОбменаИПараметры);
	ДополнительныеПараметры.Вставить("УникальныеСертификаты", 							УникальныеСертификаты);
	ДополнительныеПараметры.Вставить("КлючиОтпечатковСертификатов", 					КлючиОтпечатковСертификатов);
	ДополнительныеПараметры.Вставить("СертификатыДляПоиска", 							СертификатыДляПоиска);
	ДополнительныеПараметры.Вставить("ЕстьЛокальнаяЭлектроннаяПодпись", 				ЕстьЛокальнаяЭлектроннаяПодпись);
	ДополнительныеПараметры.Вставить("ЕстьЭлектроннаяПодписьВМоделиСервиса", 			ЕстьЭлектроннаяПодписьВМоделиСервиса);
	Оповещение = Новый ОписаниеОповещения(
		"НастройкиОбменаОрганизацийПослеСозданияМенеджераКриптографии", ЭтотОбъект, ДополнительныеПараметры);
	Если ЕстьЛокальнаяЭлектроннаяПодпись Тогда
		КриптографияЭДКОКлиент.СоздатьМенеджерКриптографии(Оповещение, Ложь,, Истина);
	Иначе
		ВыполнитьОбработкуОповещения(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НастройкиОбменаОрганизацийПослеСозданияМенеджераКриптографии(Результат, ДополнительныеПараметры) Экспорт
	
	СертификатыДляПоиска 					= ДополнительныеПараметры.СертификатыДляПоиска;
	ЕстьЛокальнаяЭлектроннаяПодпись 		= ДополнительныеПараметры.ЕстьЛокальнаяЭлектроннаяПодпись;
	ЕстьЭлектроннаяПодписьВМоделиСервиса 	= ДополнительныеПараметры.ЕстьЭлектроннаяПодписьВМоделиСервиса;
	
	Оповещение = Новый ОписаниеОповещения(
		"НастройкиОбменаОрганизацийПослеПоискаСертификатов", ЭтотОбъект, ДополнительныеПараметры);
	
	Если СертификатыДляПоиска.Количество() <= 3 Тогда
		КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска,, Ложь);
		
	Иначе
		ХранилищаСертификатов = Новый Массив;
		Если ЕстьЛокальнаяЭлектроннаяПодпись Тогда
			Хранилище = Новый Структура("Хранилище, ЭтоЛокальноеХранилище", "MY", Истина);
			ХранилищаСертификатов.Добавить(Хранилище);
		КонецЕсли;
		Если ЕстьЭлектроннаяПодписьВМоделиСервиса Тогда
			Хранилище = Новый Структура("Хранилище, ЭтоЛокальноеХранилище", "MY", Ложь);
			ХранилищаСертификатов.Добавить(Хранилище);
		КонецЕсли;
		
		ДополнительныеПараметрыВызова = Новый Структура;
		ДополнительныеПараметрыВызова.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", 	Истина);
		ДополнительныеПараметрыВызова.Вставить("ВыводитьСообщения", 					Ложь);
		
		КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, ХранилищаСертификатов, ДополнительныеПараметрыВызова);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НастройкиОбменаОрганизацийПослеПоискаСертификатов(Результат, ДополнительныеПараметры) Экспорт
	
	НайтиСертификатыПрочихКонтролирующихОрганов 	= ДополнительныеПараметры.НайтиСертификатыПрочихКонтролирующихОрганов;
	ОповещениеОЗавершении 							= ДополнительныеПараметры.ОповещениеОЗавершении;
	НастройкиОбменаИПараметры 						= ДополнительныеПараметры.НастройкиОбменаИПараметры;
	УникальныеСертификаты 							= ДополнительныеПараметры.УникальныеСертификаты;
	КлючиОтпечатковСертификатов 					= ДополнительныеПараметры.КлючиОтпечатковСертификатов;
	
	НастройкиОбмена = НастройкиОбменаИПараметры.НастройкиОбмена;
	
	КлючиСвойствСертификатов = Новый Массив;
	КлючиСвойствСертификатов.Добавить("СвойстваСертификатаРуководителя");
	Если НайтиСертификатыПрочихКонтролирующихОрганов Тогда
		КлючиСвойствСертификатов.Добавить("НастройкиОбменаФСС_СвойстваСертификатаСтрахователя");
		КлючиСвойствСертификатов.Добавить("НастройкиОбменаФСРАР_СвойстваСертификатаАбонента");
		КлючиСвойствСертификатов.Добавить("НастройкиОбменаРПН_СвойстваСертификатаАбонента");
		КлючиСвойствСертификатов.Добавить("НастройкиОбменаФТС_СвойстваСертификатаАбонента");
		КлючиСвойствСертификатов.Добавить("НастройкиОбменаМО_СвойстваСертификатаАбонента");
	КонецЕсли;
	КлючиСвойствПоКлючамОтпечатков = Новый Соответствие;
	Для ИндексКлюча = 0 По КлючиОтпечатковСертификатов.Количество() - 1 Цикл
		КлючОтпечаткаСертификата = КлючиОтпечатковСертификатов[ИндексКлюча];
		КлючиСвойствПоКлючамОтпечатков[КлючОтпечаткаСертификата] = КлючиСвойствСертификатов[ИндексКлюча];
	КонецЦикла;
	
	Для каждого НастройкаОбмена Из НастройкиОбмена Цикл
		Для каждого КлючСвойствСертификата Из КлючиСвойствСертификатов Цикл
			НастройкаОбмена.Вставить(КлючСвойствСертификата, Неопределено);
		КонецЦикла;
	КонецЦикла;
	
	Если НЕ Результат.Выполнено Тогда
		РезультатВыполнения = НастройкиОбменаИПараметры;
		РезультатВыполнения.Вставить("ПоискСертификатовВыполнен", Ложь);
		РезультатВыполнения.НастройкиОбмена = НастройкиОбмена;
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатВыполнения);
		Возврат;
	КонецЕсли;
	
	Для каждого НайденныйСертификат Из Результат.Сертификаты Цикл
		СертификатСервиса = КриптографияЭДКОКлиентСервер.СертификатВЗащищенномХранилищеНаСервере(НайденныйСертификат);
		СертификатОблака = КриптографияЭДКОКлиентСервер.СертификатОблачнойПодписи(НайденныйСертификат);
		
		Если СертификатОблака ИЛИ СертификатСервиса Тогда
			ПредставлениеОтпечатка = НайденныйСертификат.Отпечаток + "_1";
			СертификатыВНастройках = УникальныеСертификаты[ПредставлениеОтпечатка];
		Иначе
			ПредставлениеОтпечатка = НайденныйСертификат.Отпечаток + "_0";
			СертификатыВНастройках = УникальныеСертификаты[ПредставлениеОтпечатка];
			Если СертификатыВНастройках = Неопределено Тогда
				ПредставлениеОтпечатка = НайденныйСертификат.Отпечаток + "_1";
				СертификатыВНастройках = УникальныеСертификаты[ПредставлениеОтпечатка];
			КонецЕсли;
		КонецЕсли;
		Если СертификатыВНастройках = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Для каждого СертификатВНастройках Из СертификатыВНастройках Цикл
			НастройкаОбмена = НастройкиОбмена[СертификатВНастройках.ИндексОрганизации];
			КлючСвойствСертификата = КлючиСвойствПоКлючамОтпечатков[СертификатВНастройках.КлючОтпечаткаСертификата];
			НастройкаОбмена[КлючСвойствСертификата] = НайденныйСертификат;
		КонецЦикла;
	КонецЦикла;
	
	РезультатВыполнения = НастройкиОбменаИПараметры;
	РезультатВыполнения.Вставить("ПоискСертификатовВыполнен", Истина);
	РезультатВыполнения.НастройкиОбмена = НастройкиОбмена;
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Функция СтатусЗаявленияСтрокой(
		ЭлементНастроекОбмена,
		ЦветУспеха = Неопределено,
		ЦветОшибки = Неопределено,
		ЦветНеактивного = Неопределено,
		ВерхнийРегистрПервойБуквы = Истина,
		КраткийСтатус = Ложь) Экспорт
		
	Статус                = ЭлементНастроекОбмена.ЗаявлениеАбонентаСпецоператораСвязи_Статус;
	НастройкаЗавершена    = ЭлементНастроекОбмена.ЗаявлениеАбонентаСпецоператораСвязи_НастройкаЗавершена;
	Настройки_ЭДО         = ЭлементНастроекОбмена.ЗаявлениеАбонентаСпецоператораСвязи_НастройкиЭДО;
	НастройкаЭДОЗавершена = ЭлементНастроекОбмена.ЗаявлениеАбонентаСпецоператораСвязи_НастройкаЭДОЗавершена;
	
	Отклонено = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено");
	Одобрено  = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено");
		
	ЦветСтатуса = Неопределено;
	ВыделениеНезавершенного = Ложь;
	Если Статус = Отклонено Тогда
		Если НастройкаЗавершена Тогда
			СтатусСтрокой 	= НСтр("ru = 'Отклонено'");
		Иначе
			Если КраткийСтатус Тогда
				СтатусСтрокой 	= НСтр("ru = 'Отклонено'");
			Иначе
				СтатусСтрокой 	= НСтр("ru = 'Отклонено, настройка не завершена'");
			КонецЕсли;
			ВыделениеНезавершенного = Истина;
		КонецЕсли;
		ЦветСтатуса = ЦветОшибки;
		
	ИначеЕсли Статус = Одобрено Тогда
		
		Если НастройкаЗавершена ИЛИ КраткийСтатус Тогда
			СтатусСтрокой 	= НСтр("ru = 'Одобрено'");
			ЦветСтатуса 	= ЦветУспеха;
		ИначеЕсли НЕ НастройкаЭДОЗавершена И ЗначениеЗаполнено(Настройки_ЭДО) Тогда
			СтатусСтрокой 	= НСтр("ru = 'Одобрено, настройка 1С-ЭДО не завершена'");
			ВыделениеНезавершенного = Истина;
		Иначе
			СтатусСтрокой 	= НСтр("ru = 'Одобрено, настройка не завершена'");
			ВыделениеНезавершенного = Истина;
		КонецЕсли;
		
	ИначеЕсли Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено") Тогда
		СтатусСтрокой 		= НСтр("ru = 'Отправлено'");
		
	ИначеЕсли Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено") Тогда
		СтатусСтрокой 		= НСтр("ru = 'Не отправлено'");
		ЦветСтатуса 		= ЦветНеактивного;
		
	Иначе
		СтатусСтрокой = Строка(Статус);
	КонецЕсли;
	
	Если НЕ ВерхнийРегистрПервойБуквы И ЗначениеЗаполнено(СтатусСтрокой) Тогда
		ПерваяБукваСтатуса = Лев(СтатусСтрокой, 1);
		СтатусСтрокой = нрег(ПерваяБукваСтатуса) + Сред(СтатусСтрокой, 2);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("СтатусСтрокой", 			СтатусСтрокой);
	Результат.Вставить("ЦветСтатуса", 				ЦветСтатуса);
	Результат.Вставить("ВыделениеНезавершенного", 	ВыделениеНезавершенного);
	
	Возврат Результат;
	
КонецФункции

// Выводит список значений в формате "Вы, Администратор и еще 18" при КоличествоВыводимыхИмен = 3.
&НаКлиенте
Функция СписокПользователейСтрокой(СписокПользователей, УчетнаяЗаписьДоступна = Истина, КоличествоВыводимыхИмен = 2) Экспорт
	
	ТекущийПользователь = ПользователиКлиент.ТекущийПользователь();
	ТекущийПользовательВСписке = СписокПользователей.НайтиПоЗначению(ТекущийПользователь);
	
	Результат = "";
	ЭтоВсеПользователи = СписокПользователей.Количество() > 0;
	ЭтоТолькоВы = ТекущийПользовательВСписке <> Неопределено И ТекущийПользовательВСписке.Пометка;
	ПоследнийВыводимыйПользователь = ?(ЭтоТолькоВы, НСтр("ru = 'Вы'"), Неопределено);
	КоличествоПользователей = ?(ЭтоТолькоВы, 1, 0);
	
	Для каждого ЭлементСпискаПользователей Из СписокПользователей Цикл
		Если ЭлементСпискаПользователей.Пометка И ЭлементСпискаПользователей.Значение <> ТекущийПользователь Тогда
			ЭтоТолькоВы = Ложь;
			Если КоличествоПользователей < КоличествоВыводимыхИмен Тогда
				Если ПоследнийВыводимыйПользователь <> Неопределено Тогда
					Результат = Результат + ?(КоличествоПользователей > 1, ", ", "") + ПоследнийВыводимыйПользователь;
					ПоследнийВыводимыйПользователь = Неопределено;
				КонецЕсли;
				ПоследнийВыводимыйПользователь = ЭлементСпискаПользователей.Представление;
			КонецЕсли;
			КоличествоПользователей = КоличествоПользователей + 1;
		КонецЕсли;
		Если НЕ ЭлементСпискаПользователей.Пометка Тогда
			ЭтоВсеПользователи = Ложь;
		КонецЕсли;
	КонецЦикла;
	
	Если ЭтоВсеПользователи Тогда
		Результат = НСтр("ru = 'Все пользователи'");
		
	ИначеЕсли ЭтоТолькоВы Тогда
		Результат = НСтр("ru = 'Только Вы'");
		
	Иначе
		Если ПоследнийВыводимыйПользователь <> Неопределено И КоличествоПользователей <= КоличествоВыводимыхИмен Тогда
			Результат = Результат + ?(КоличествоПользователей > 1, " "
				+ СтрШаблон(
					НСтр("ru = 'и %1'"),
					ПоследнийВыводимыйПользователь),
				ПоследнийВыводимыйПользователь);
		КонецЕсли;
		
		Если КоличествоПользователей > КоличествоВыводимыхИмен Тогда
			Результат = Результат + " " + СтрШаблон(
				НСтр("ru = 'и еще %1'"),
				Строка(КоличествоПользователей - КоличествоВыводимыхИмен + 1));
		КонецЕсли;
		
		Если КоличествоПользователей = 0 И УчетнаяЗаписьДоступна Тогда
			Результат = НСтр("ru = '<не выбраны>'");
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция НаправленияСтрокой(ЭлементНастроекОбмена,
	ПоддерживаетсяСдачаОтчетностиВЦБ = Ложь,
	ПоддерживаетсяСдачаОтчетностиВМинобороны = Ложь) Экспорт
	
	ПредставленияОрганов = Новый Массив;
	ЭтоВсеНаправления = Истина;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиФНС ИЛИ ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСФНС Тогда
		Если ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСФНС Тогда
			КоличествоОрганов = ЭлементНастроекОбмена.КодыФНС.Количество();
			Если КоличествоОрганов > 1 Тогда
				ПредставлениеОргана = СтрШаблон(
					НСтр("ru = 'ФНС(%1)'"),
					КоличествоОрганов)
			Иначе
				ПредставлениеОргана = НСтр("ru = 'ФНС'");
			КонецЕсли;
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиФСГС ИЛИ ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСФСГС Тогда
		Если ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСФСГС Тогда
			КоличествоОрганов = ЭлементНастроекОбмена.КодыФСГС.Количество();
			Если КоличествоОрганов > 1 Тогда
				ПредставлениеОргана = СтрШаблон(
					НСтр("ru = 'Росстат(%1)'"),
					КоличествоОрганов)
			Иначе
				ПредставлениеОргана = НСтр("ru = 'Росстат'");
			КонецЕсли;
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиПФР ИЛИ ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСПФР Тогда
		Если ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСПФР Тогда
			ПредставлениеОргана = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(НСтр("ru = 'ПФР'"), Истина);
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиФСС ИЛИ ЭлементНастроекОбмена.НастройкиОбменаФСС_ИспользоватьОбмен Тогда
		Если ЭлементНастроекОбмена.НастройкиОбменаФСС_ИспользоватьОбмен Тогда
			ПредставлениеОргана = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(НСтр("ru = 'ФСС'"), Истина);
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиФСРАР ИЛИ ЭлементНастроекОбмена.НастройкиОбменаФСРАР_ИспользоватьОбмен Тогда
		Если ЭлементНастроекОбмена.НастройкиОбменаФСРАР_ИспользоватьОбмен Тогда
			ПредставлениеОргана = НСтр("ru = 'ФСРАР'");
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиРПН ИЛИ ЭлементНастроекОбмена.НастройкиОбменаРПН_ИспользоватьОбмен Тогда
		Если ЭлементНастроекОбмена.НастройкиОбменаРПН_ИспользоватьОбмен Тогда
			ПредставлениеОргана = НСтр("ru = 'РПН'");
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ЭлементНастроекОбмена.ПризнакПоддержкиФТС ИЛИ ЭлементНастроекОбмена.НастройкиОбменаФТС_ИспользоватьОбмен Тогда
		Если ЭлементНастроекОбмена.НастройкиОбменаФТС_ИспользоватьОбмен Тогда
			ПредставлениеОргана = НСтр("ru = 'ФТС'");
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ПоддерживаетсяСдачаОтчетностиВЦБ ИЛИ ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСЦБ Тогда
		Если ЭлементНастроекОбмена.ПредназначенаДляДокументооборотаСЦБ Тогда
			ПредставлениеОргана = НСтр("ru = 'Банк России'");
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Если ПоддерживаетсяСдачаОтчетностиВМинобороны ИЛИ ЭлементНастроекОбмена.НастройкиОбменаМинобороны_ИспользоватьОбмен Тогда
		Если ЭлементНастроекОбмена.НастройкиОбменаМинобороны_ИспользоватьОбмен Тогда
			ПредставлениеОргана = НСтр("ru = 'Минобороны'");
			ПредставленияОрганов.Добавить(ПредставлениеОргана);
		Иначе
			ЭтоВсеНаправления = Ложь;
		КонецЕсли;
	КонецЕсли;
	
	Результат = "";
	
	КоличествоПредставленийОрганов = ПредставленияОрганов.Количество();
	Если КоличествоПредставленийОрганов > 0 Тогда
		Если ЭтоВсеНаправления Тогда
			Результат = НСтр("ru = 'Все направления'");
			
		ИначеЕсли КоличествоПредставленийОрганов = 1 Тогда
			Результат = ПредставленияОрганов[0];
			
		Иначе
			ПоследнееПредставлениеОргана = ПредставленияОрганов[КоличествоПредставленийОрганов - 1];
			ПредставленияОрганов.Удалить(КоличествоПредставленийОрганов - 1);
			Результат = СтрСоединить(ПредставленияОрганов, ", ") + " "
				+ СтрШаблон(
					НСтр("ru = 'и %1'"),
					ПоследнееПредставлениеОргана);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьФормуСтатусовОтправкиИлиОбновитьЗаявление(
		Заявление,
		Статус = Неопределено,
		НастройкаЗавершена = Истина,
		Организация = Неопределено,
		ПринудительноОбновить = Ложь) Экспорт
	
	Если НЕ ЗначениеЗаполнено(Заявление) Тогда
		Возврат;
	КонецЕсли;
	
	// проверяем, было ли отправлено заявление
	Если Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Подготовлено")
		ИЛИ Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отправлено")
		ИЛИ Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено") И НастройкаЗавершена Тогда
		
		ПоказатьЗначение(, Заявление);
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Организация) И (ПринудительноОбновить ИЛИ
		(Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Отклонено")
		ИЛИ Статус = ПредопределенноеЗначение("Перечисление.СтатусыЗаявленияАбонентаСпецоператораСвязи.Одобрено"))
		И НЕ НастройкаЗавершена) Тогда
		
		ДополнительныеПараметры = Новый Структура("Организация, Заявление", Организация, Заявление);
		ОписаниеОповещения = Новый ОписаниеОповещения("ПоказатьФормуСтатусовОтправкиИлиОбновитьЗаявлениеПослеОбновленияСтатуса",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ОбновитьСтатусЗаявленияАбонента_ИзПанелиОтправки(ОписаниеОповещения, ЭтотОбъект, Заявление);
		Возврат;
	КонецЕсли;
	
	ПоказатьФормуСтатусовОтправки(Заявление);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьФормуСтатусовОтправкиИлиОбновитьЗаявлениеПослеОбновленияСтатуса(Результат, ДополнительныеПараметры) Экспорт
	
	Организация = ДополнительныеПараметры.Организация;
	Заявление 	= ДополнительныеПараметры.Заявление;
	
	ПараметрыОповещения = Новый Структура;
	ПараметрыОповещения.Вставить("Организация", Организация);
	ПараметрыОповещения.Вставить("Заявление", 	Заявление);
	
	Оповестить(РегламентированнаяОтчетностьКлиент.ИмяСобытияЗавершениеОтправки(), ПараметрыОповещения, Заявление);
	
КонецПроцедуры

&НаКлиенте
Функция СрокиПредупрежденийНастроекОбменов(ТекущаяДатаНаСервере) Экспорт
	
	СколькоДнейПоказыватьСтатусЗаявления 	= 60;
	ЗаСколькоДнейСчитатьИсткающим 			= 14;
	
	ТекущаяДатаОкончанияПоказаЗаявления = ТекущаяДатаНаСервере - СколькоДнейПоказыватьСтатусЗаявления * 24 * 60 * 60;
	ТекущаяДатаИстечения 				= ТекущаяДатаНаСервере + ЗаСколькоДнейСчитатьИсткающим * 24 * 60 * 60;
	
	Результат = Новый Структура;
	Результат.Вставить("ТекущаяДатаОкончанияПоказаЗаявления", 	ТекущаяДатаОкончанияПоказаЗаявления);
	Результат.Вставить("ТекущаяДатаИстечения", 					ТекущаяДатаИстечения);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СтатусДействительностиСтрокой(
		ДействителенПо,
		ТекущаяДатаНаСервере,
		ТекущаяДатаИстечения,
		ЭтоСертификат = Истина,
		ЦветОшибки = Неопределено,
		ВерхнийРегистрПервойБуквы = Ложь,
		НаименованиеОбъекта = "") Экспорт
	
	ЦветСтатуса = Неопределено;
	ТребуетПродления = Ложь;
	Если НЕ ЗначениеЗаполнено(ДействителенПо) Тогда
		СтатусСтрокой = НСтр("ru = 'с недоступным сроком действия'")
			+ ?(ЗначениеЗаполнено(НаименованиеОбъекта), " (" + НаименованиеОбъекта + ")", "");
		
	Иначе
		Если ДействителенПо < ТекущаяДатаНаСервере Тогда
			СтатусСтрокой 		= ?(ЭтоСертификат, НСтр("ru = 'истек %1'"), НСтр("ru = 'истекла %1'"));
			ЦветСтатуса 		= ЦветОшибки;
			ТребуетПродления 	= Истина;
			
		ИначеЕсли ДействителенПо <= ТекущаяДатаИстечения Тогда
			СтатусСтрокой 		= НСтр("ru = 'истекает %1'");
			ЦветСтатуса 		= ЦветОшибки;
			ТребуетПродления 	= Истина;
			
		Иначе
			СтатусСтрокой = НСтр("ru = 'действует до %1'");
		КонецЕсли;
		
		ДействителенПоСтрокой = ?(ЭтоСертификат, Формат(ДействителенПо, "ДЛФ=ДД"), Формат(ДействителенПо, "ДЛФ=ДД"));
		
		СтатусСтрокой = СтрШаблон(СтатусСтрокой, ДействителенПоСтрокой);
		СтатусСтрокой = СтатусСтрокой + ?(ЗначениеЗаполнено(НаименованиеОбъекта), " (" + НаименованиеОбъекта + ")", "");
	КонецЕсли;
	
	Если ВерхнийРегистрПервойБуквы И ЗначениеЗаполнено(СтатусСтрокой) Тогда
		ПерваяБукваСтатуса = Лев(СтатусСтрокой, 1);
		СтатусСтрокой = ВРег(ПерваяБукваСтатуса) + Сред(СтатусСтрокой, 2);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("СтатусСтрокой", 	СтатусСтрокой);
	Результат.Вставить("ЦветСтатуса", 		ЦветСтатуса);
	Результат.Вставить("ТребуетПродления", 	ТребуетПродления);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция СохранитьПозициюВТаблице(
		Таблица,
		ДанныеТаблицы,
		КлючеваяКолонка,
		КлючевойЭлемент = Неопределено) Экспорт
	
	КлючТекущейСтроки = Неопределено;
	ТекущаяСтрокаТаблицы = Таблица.ТекущиеДанные;
	Если ТекущаяСтрокаТаблицы <> Неопределено Тогда
		ТекущаяСтрокаТаблицы.Свойство(КлючеваяКолонка, КлючТекущейСтроки);
		Если ЗначениеЗаполнено(КлючТекущейСтроки) И ЗначениеЗаполнено(КлючевойЭлемент) Тогда
			КлючТекущейСтроки.Свойство(КлючевойЭлемент, КлючТекущейСтроки);
		КонецЕсли;
	КонецЕсли;
	
	КлючиВыделенныхСтрок = Новый Массив;
	Для каждого ИдентификаторВыделеннойСтроки Из Таблица.ВыделенныеСтроки Цикл
		ВыделеннаяСтрокаТаблицы = ДанныеТаблицы[ИдентификаторВыделеннойСтроки];
		КлючВыделеннойСтроки = Неопределено;
		ВыделеннаяСтрокаТаблицы.Свойство(КлючеваяКолонка, КлючВыделеннойСтроки);
		Если ЗначениеЗаполнено(КлючВыделеннойСтроки) И ЗначениеЗаполнено(КлючевойЭлемент) Тогда
			КлючВыделеннойСтроки.Свойство(КлючевойЭлемент, КлючВыделеннойСтроки);
		КонецЕсли;
		Если ЗначениеЗаполнено(КлючВыделеннойСтроки) Тогда
			КлючиВыделенныхСтрок.Добавить(КлючВыделеннойСтроки);
		КонецЕсли;
	КонецЦикла;
	
	Результат = Новый Структура;
	Результат.Вставить("КлючТекущейСтроки", 	КлючТекущейСтроки);
	Результат.Вставить("КлючиВыделенныхСтрок", 	КлючиВыделенныхСтрок);
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ВосстановитьПозициюВТаблице(
		ПозицияВТаблице,
		Таблица,
		ДанныеТаблицы,
		КлючеваяКолонка,
		КлючевойЭлемент = Неопределено) Экспорт
	
	КлючТекущейСтроки 		= ПозицияВТаблице.КлючТекущейСтроки;
	КлючиВыделенныхСтрок 	= ПозицияВТаблице.КлючиВыделенныхСтрок;
	
	Таблица.ВыделенныеСтроки.Очистить();
	
	ИдентификаторТекущейСтроки = Таблица.ТекущаяСтрока;
	Если КлючТекущейСтроки <> Неопределено Тогда
		Для ИдентификаторСтроки = 0 По ДанныеТаблицы.Количество() - 1 Цикл
			СтрокаТаблицы = ДанныеТаблицы[ИдентификаторСтроки];
			Если СтрокаТаблицы <> Неопределено Тогда
				КлючСтроки = Неопределено;
				СтрокаТаблицы.Свойство(КлючеваяКолонка, КлючСтроки);
				Если ЗначениеЗаполнено(КлючСтроки) И ЗначениеЗаполнено(КлючевойЭлемент) Тогда
					КлючСтроки.Свойство(КлючевойЭлемент, КлючСтроки);
				КонецЕсли;
				
				Если КлючСтроки = КлючТекущейСтроки Тогда
					ИдентификаторТекущейСтроки = ИдентификаторСтроки;
					Таблица.ТекущаяСтрока = ИдентификаторТекущейСтроки;
					Прервать;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если КлючиВыделенныхСтрок.Количество() = 1 И КлючиВыделенныхСтрок[0] = КлючТекущейСтроки
		И ИдентификаторТекущейСтроки <> Неопределено Тогда
		
		Таблица.ВыделенныеСтроки.Добавить(ИдентификаторТекущейСтроки);
		
	ИначеЕсли КлючиВыделенныхСтрок.Количество() > 0 Тогда
		Для ИдентификаторСтроки = 0 По ДанныеТаблицы.Количество() - 1 Цикл
			СтрокаТаблицы = ДанныеТаблицы[ИдентификаторСтроки];
			Если СтрокаТаблицы <> Неопределено Тогда
				КлючСтроки = Неопределено;
				СтрокаТаблицы.Свойство(КлючеваяКолонка, КлючСтроки);
				Если ЗначениеЗаполнено(КлючСтроки) И ЗначениеЗаполнено(КлючевойЭлемент) Тогда
					КлючСтроки.Свойство(КлючевойЭлемент, КлючСтроки);
				КонецЕсли;
				
				Если КлючиВыделенныхСтрок.Найти(КлючСтроки) <> Неопределено Тогда
					ИдентификаторВыделеннойСтроки = ИдентификаторСтроки;
					Таблица.ВыделенныеСтроки.Добавить(ИдентификаторВыделеннойСтроки);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция НастройкиОбменаОрганизацийНаСервере(Организации = Неопределено, Настройки = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.НастройкиОбменаОрганизаций(Организации, Настройки);
	
КонецФункции

#КонецОбласти

&НаКлиенте
Функция ПараметрыПроцедурыРедактироватьАдрес() Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Адрес",             "");
	ДополнительныеПараметры.Вставить("АдресИмя",          "");
	ДополнительныеПараметры.Вставить("Элемент",           Неопределено);
	ДополнительныеПараметры.Вставить("Оповещение",        Неопределено);
	ДополнительныеПараметры.Вставить("ПринудительноФИАС", Ложь);
	ДополнительныеПараметры.Вставить("ТолькоПросмотр",    Ложь);
	 
	Возврат ДополнительныеПараметры;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьИнструкциюИнструкциюПоСозданиюКлючаЭЦПЗавершение(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(Результат.ИмяФайла, НСтр("ru = 'Инструкция по созданию ключа ЭЦП.pdf'"));
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПодтверждениеДатыОтправки_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрПодтвержденияДатыОтправки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеДатыОтправки"));
	Для Каждого СтрПодтверждениеДатыОтправки Из СтрПодтвержденияДатыОтправки Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПодтверждениеДатыОтправки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрПодтверждениеДатыОтправки));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПодтверждениеДатыОтправки.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеПодтвержденияДатыОтправкиПредставление(Представление, СтрПодтверждениеДатыОтправки.ИмяФайла, Тело, НижнийКолонтитул);
		ПараметрыПечати.Отправка = СтрПодтверждениеДатыОтправки.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьРезультатПриема_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрРезультатыПриема = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатПриема"));
	Для Каждого СтрРезультатПриема Из СтрРезультатыПриема Цикл 
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатПриема.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатПриема));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатПриема.ТранспортноеСообщение);
		КНД           = КНДФайла(Тело);
		
		Если СтрРезультатПриема.Тип = ПредопределенноеЗначение("Перечисление.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказе")
			ИЛИ СтрРезультатПриема.Тип = ПредопределенноеЗначение("Перечисление.ТипыСодержимогоТранспортногоКонтейнера.УведомлениеОбОтказеДокумент") Тогда 
			
			Если КНД = "1167003" Тогда
				
				ТаблДок = СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриеме(
					Представление, 
					СтрРезультатПриема.ИмяФайла, 
					Тело, 
					НижнийКолонтитул);
							
			ИначеЕсли КНД = "1166006" Тогда
					
				ТаблДок = СформироватьТабличныйДокументНаОсновеУведомленияОбОтказеВПриемеНалоговойДекларации(
					Представление, 
					СтрРезультатПриема.ИмяФайла, 
					Тело, 
					НижнийКолонтитул);
					
			Иначе
					
				ТаблДок = СформироватьТабличныйДокументНаОсновеБланка(
					Представление, 
					СтрРезультатПриема.ИмяФайла, 
					Тело, 
					НижнийКолонтитул);
					
			КонецЕсли;
					
		Иначе
			
			Если КНД = "1167001" Тогда
				
				ТаблДок = СформироватьТабличныйДокументНаОсновеКвитанцииОПриеме(
					Представление, 
					СтрРезультатПриема.ИмяФайла, 
					Тело, 
					НижнийКолонтитул);
					
			ИначеЕсли КНД = "1166002" Тогда
			
				ТаблДок = СформироватьТабличныйДокументНаОсновеКвитанцииОПриемеНалоговойДекларации(
					Представление, 
					СтрРезультатПриема.ИмяФайла, 
					Тело, 
					НижнийКолонтитул);
					
			Иначе
					
				ТаблДок = СформироватьТабличныйДокументНаОсновеБланка(
					Представление, 
					СтрРезультатПриема.ИмяФайла, 
					Тело, 
					НижнийКолонтитул);
					
			КонецЕсли;
				
		КонецЕсли;
				
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТаблДок;
		ПараметрыПечати.Отправка = СтрРезультатПриема.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);

	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьИзвещенияОПолучении(Результат, НижнийКолонтитул)

	СтрРезультатыОбработки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ИзвещениеОПолучении"));
	Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработки Цикл
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатОбработки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРезультатОбработки));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатОбработки.ТранспортноеСообщение);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументИзвещенияОПолучении(
			Представление, 
			СтрРезультатОбработки.ИмяФайла, 
			Тело, 
			НижнийКолонтитул);
			
		ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьРезультатОбработки_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрРезультатыОбработки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "РезультатОбработки"));
		Для Каждого СтрРезультатОбработки Из СтрРезультатыОбработки Цикл
			Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРезультатОбработки.Файл, "windows-1251");
			ПолучитьЭЦП(Файлы.Индекс(СтрРезультатОбработки));
			
			Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрРезультатОбработки.ТранспортноеСообщение);
			
			Если СтрРезультатОбработки.Тип = СтруктураСсылочныхДанных["Перечисления_ТипыСодержимогоТранспортногоКонтейнера_УведомлениеОбУточнении"] Тогда 
				
				ПараметрыПечати = КонструкторПечатиПротокола();
				ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументнаОсновеУведомленияОбУточнении(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
				ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
				
				Результат.Добавить(ПараметрыПечати, Представление);
			Иначе
				
				ПараметрыПечати = КонструкторПечатиПротокола();
				ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументнаОсновеИзвещенияОВводе(Представление, СтрРезультатОбработки.ИмяФайла, Тело, НижнийКолонтитул);
				ПараметрыПечати.Отправка = СтрРезультатОбработки.ТранспортноеСообщение;
				
				Результат.Добавить(ПараметрыПечати, Представление);
			КонецЕсли;
		КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПодтверждениеОтправки_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрПодтвержденияОтправки = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "ПодтверждениеОтправки"));
	Для Каждого СтрПодтверждениеОтправки Из СтрПодтвержденияОтправки Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПодтверждениеОтправки.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрПодтверждениеОтправки));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПодтверждениеОтправки.ТранспортноеСообщение);
		
		ТабДок = СформироватьТабличныйДокументНаОсновеБланка(
			Представление, 
			СтрПодтверждениеОтправки.ИмяФайла, 
			Тело, 
			НижнийКолонтитул);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТабДок;
		ПараметрыПечати.Отправка = СтрПодтверждениеОтправки.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПротокол_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрПротоколы = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "Протокол"));
	Для Каждого СтрПротокол Из СтрПротоколы Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПротокол.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрПротокол));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрПротокол.ТранспортноеСообщение);
		
		ТабДок = СформироватьТабличныйДокументНаОсновеБланка(
			Представление, 
			СтрПротокол.ИмяФайла, 
			Тело, 
			НижнийКолонтитул);
			
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТабДок;
		ПараметрыПечати.Отправка = СтрПротокол.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьУведомлениеОбУточнении_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрУведомления = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "Уведомление"));
	Для Каждого СтрУведомление Из СтрУведомления Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрУведомление.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрУведомление));
		
		Представление = ПредставлениеТранспортногоСообщнияПоТипу(СтрУведомление.ТранспортноеСообщение);
		
		ТабДок = СформироватьТабличныйДокументнаОсновеУведомленияОбУточнении(
			Представление, 
			СтрУведомление.ИмяФайла, 
			Тело, 
			НижнийКолонтитул);
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТабДок;
		ПараметрыПечати.Отправка = СтрУведомление.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, Представление);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьПротоколПриема2НДФЛ_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрПротоколы = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "Протокол2НДФЛ"));
	Для Каждого СтрПротокол Из СтрПротоколы Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрПротокол.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрПротокол));
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеБланка("Протокол приема 2-НДФЛ", СтрПротокол.ИмяФайла, Тело, НижнийКолонтитул);
		ПараметрыПечати.Отправка = СтрПротокол.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, "Протокол приема 2-НДФЛ");
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьРеестрСведений2НДФЛ_НалогБухОтчетность(Результат, НижнийКолонтитул)

	СтрРеестры = Файлы.НайтиСтроки(Новый Структура("ИдТипа", "Реестр2НДФЛ"));
	Для Каждого СтрРеестр Из СтрРеестры Цикл 
		
		Тело = ПрочитатьТекстИзФайлаСтрокойBase64Сервер(СтрРеестр.Файл, "windows-1251");
		ПолучитьЭЦП(Файлы.Индекс(СтрРеестр));
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = СформироватьТабличныйДокументНаОсновеБланка("Реестр сведений 2-НДФЛ", СтрРеестр.ИмяФайла, Тело, НижнийКолонтитул);
		ПараметрыПечати.Отправка = СтрРеестр.ТранспортноеСообщение;
		
		Результат.Добавить(ПараметрыПечати, "Реестр сведений 2-НДФЛ");
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатныеДокументы_ПечататьСостояние_НалогБухОтчетность(Результат, ЦиклОбмена)

	ТаблДок = ВывестиОтчетОСостоянии(ЦиклОбмена);
	Если ТаблДок <> Неопределено Тогда
		
		ПараметрыПечати = КонструкторПечатиПротокола();
		ПараметрыПечати.ТабДок   = ТаблДок;
		ПараметрыПечати.Отправка = Неопределено;
		
		Результат.Добавить(ПараметрыПечати, "Отчет о состоянии");
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция СформироватьТабличныйДокументИзвещенияОПолучении(Заголовок, ИмяФайла, Тело, НижнийКолонтитул)
	
	СвойстваИзУведомления = ПолучитьОсновныеСвойстваИзИзвещенияОПолучении(Тело);
	
	ТабДокумент = Новый ТабличныйДокумент;
	
	// если текст уведомления разобрать удалось, то выводим основную инфорфмацию из него
	Если ЗначениеЗаполнено(СвойстваИзУведомления) Тогда
		
		КНД = КНДФайла(Тело);
		Если КНД = "1166008" Тогда
			Бланк = ПолучитьМакетОбработки("ИзвещениеОПолучении_КНД_1166008");
		Иначе
			Бланк = ПолучитьМакетОбработки("ИзвещениеОПолучении_КНД_1167004");
		КонецЕсли;
		
		ТабДокумент.АвтоМасштаб = Истина;
		ОсновнаяЧастьБланка = Бланк.ПолучитьОбласть("ИзвещениеОПолучении");
		ЗаполнитьЗначенияСвойств(ОсновнаяЧастьБланка.Параметры, СвойстваИзУведомления);
		ТабДокумент.Вывести(ОсновнаяЧастьБланка);
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Бланк, ТаблицаПодписей);
		
	Иначе
		// если разобрать не удалось - выводим его в виде текста
		ТабДокумент = СформироватьТабличныйДокументНаОсновеБланка(Заголовок, ИмяФайла, Тело, НижнийКолонтитул);
		
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваИзИзвещенияОПолучении(СтрУведомление)
	
	Если НЕ ЗначениеЗаполнено(СтрУведомление) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// считываем подтверждение из файла в дерево XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрУведомление, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'Ошибка чтения XML извещения о вводе:%1'"), Символы.ПС + ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	// анализируем XML
	УзелФайл = ДеревоXML.Строки.Найти("Файл", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелФайл) Тогда
		ОбщегоНазначения.СообщитьПользователю(
			НСтр("ru = 'Некорректная структура XML извещения: не обнаружен узел ""Файл"".'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелДокумент = УзелФайл.Строки.Найти("Документ", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелДокумент) Тогда
		ОбщегоНазначения.СообщитьПользователю(
			НСтр("ru = 'Некорректная структура XML извещения: не обнаружен узел ""Документ"".'"));
		Возврат Неопределено;
	КонецЕсли;
	
	УзелСвИзвещП = УзелДокумент.Строки.Найти("СвИзвещП", "Имя");
	Если НЕ ЗначениеЗаполнено(УзелСвИзвещП) Тогда
		ОбщегоНазначения.СообщитьПользователю(
			НСтр("ru = 'Некорректная структура XML извещения: не обнаружен узел ""СвИзвещП"".'"));
		Возврат Неопределено;
	КонецЕсли;
	
	// получаем сведения о дате и времени поступившего файла
	ДатаПол = УзелСвИзвещП.Строки.Найти("ДатаПол", "Имя").Значение;
	ВремяПол = УзелСвИзвещП.Строки.Найти("ВремяПол", "Имя").Значение;
	
	Файлы = Новый Массив;
	// получаем сведения о полученных файлах
	УзлыСведПолФайл = УзелСвИзвещП.Строки.НайтиСтроки(Новый Структура("Имя", "СведПолФайл"));
	Для Каждого УзелСведПолФайл Из УзлыСведПолФайл Цикл
		УзелИмяПостФайла = УзелСведПолФайл.Строки.Найти("ИмяПостФайла", "Имя");
		Если ЗначениеЗаполнено(УзелИмяПостФайла) Тогда
			Файлы.Добавить(СокрЛП(УзелИмяПостФайла.Значение));
		КонецЕсли;
	КонецЦикла;
	
	Результат     = СубьектИПочтаИзИзвещенияОПолучении(УзелДокумент, "ПодтвДок");
	ПодтвПол      = Результат.Субьект;
	ПодтвПолПочта = Результат.Почта;
	
	Результат     = СубьектИПочтаИзИзвещенияОПолучении(УзелДокумент, "ОтпрДок");
	ОтпрДок       = Результат.Субьект;
	ОтпрДокПочта  = Результат.Почта;
	
	Результат     = СубьектИПочтаИзИзвещенияОПолучении(УзелДокумент, "ПолДок");
	ПолДок        = Результат.Субьект;
	ПолДокПочта   = Результат.Почта;
	
	ИмяПринятФайлов = СтрСоединить(Файлы, Символы.ПС);
	
	Возврат Новый Структура("ПодтвПол, ПодтвПолПочта, ИмяПринятФайлов, ОтпрДок, ОтпрДокПочта, ПолДок, ДатаПол, ВремяПол, ПолДокПочта", 
		ПодтвПол, ПодтвПолПочта, ИмяПринятФайлов, ОтпрДок, ОтпрДокПочта, ПолДок, ДатаПол, ВремяПол, ПолДокПочта);
	
КонецФункции
	
&НаСервереБезКонтекста
Функция СубьектИПочтаИзИзвещенияОПолучении(УзелДокумент, ИмяУзла)
	
	ПодтвПол = ПолучитьОтправителяИзТэгаСвОтпрДокИлиСвАдр(УзелДокумент, ИмяУзла);
	
	УзелПодтвДок = УзелДокумент.Строки.НайтиСтроки(Новый Структура("Имя", ИмяУзла));
	Если ЗначениеЗаполнено(УзелПодтвДок) Тогда
		
		Для Каждого Узел ИЗ УзелПодтвДок Цикл
			УзелПодтвДокПочта = Узел.Строки.НайтиСтроки(Новый Структура("Имя", "E-mail"));
			Если ЗначениеЗаполнено(УзелПодтвДокПочта) Тогда
				ПодтвПолПочта = УзелПодтвДокПочта[0].Значение;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Субьект", ПодтвПол);
	ДополнительныеПараметры.Вставить("Почта",   ПодтвПолПочта);
	
	Возврат ДополнительныеПараметры;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПредставлениеНалоговогоОрганаИзXML(УзелСведенияНО)
	
	ПредставлениеНО = "";
		
	УзелКодНО = ПолучитьУзел(УзелСведенияНО, "КодНО");
	Если НЕ ЗначениеЗаполнено(УзелКодНО) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелНаимНО = ПолучитьУзел(УзелСведенияНО, "НаимНО");
	
	Если ЗначениеЗаполнено(УзелНаимНО) Тогда
		ПредставлениеНО = НСтр("ru = 'ИФНС %1 (%2)'");
		ПредставлениеНО = СтрШаблон(ПредставлениеНО, УзелКодНО.Значение, УзелНаимНО.Значение);
	Иначе
		ПредставлениеНО = НСтр("ru = 'ИФНС %1'");
		ПредставлениеНО = СтрШаблон(ПредставлениеНО, УзелКодНО.Значение);
	КонецЕсли;
	
	Возврат ПредставлениеНО;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПредставлениеОператораСвязиИзXML(Узел)
	
	ПредставлениеОператора = "";
		
	УзелНаимОрг = ПолучитьУзел(Узел, "НаимОрг");
	Если НЕ ЗначениеЗаполнено(УзелНаимОрг) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	УзелИденСОС = ПолучитьУзел(Узел, "ИденСОС");
	Если НЕ ЗначениеЗаполнено(УзелИденСОС) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ПредставлениеОператора = НСтр("ru = '%1 (%2)'");
	ПредставлениеОператора = СтрШаблон(ПредставлениеОператора, УзелНаимОрг.Значение, УзелИденСОС.Значение);
	
	Возврат ПредставлениеОператора;
	
КонецФункции

&НаКлиенте
Функция ОснованиеПакетаВыбраноКорректно(Основание) Экспорт

	ТекстОшибки = ТекстОшибкиПроверкиВыбранногоОснованияПакета(Основание);
	Если ТекстОшибки = "" Тогда
		Возврат Истина;
	Иначе
		ПоказатьПредупреждение(,ТекстОшибки);
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ТекстОшибкиПроверкиВыбранногоОснованияПакета(Основание)

	ТекстОшибки = "";
	
	ПредставлениеВИменительномПадеже = ДокументооборотСКОКлиентСервер.ПредставлениеПакетаСДопДокументами();
	ПредставлениеВРодительномПадеже  = ДокументооборотСКОКлиентСервер.ПредставлениеПакетаСДопДокументамиВРодительномПадеже(Истина);
	ПредставлениеОснованияВРодительномПадеже = ДлительнаяОтправкаКлиентСервер.НазваниеОбъектаВРодительномПадеже(Основание);
	
	Если ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(Основание) Тогда
		
		ТекстОшибки = НСтр("ru = '%1 не может быть выбран основанием у %2'");
		ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеВИменительномПадеже, ПредставлениеВРодительномПадеже);
		
	Иначе
		
		Сведения = СведенияПоОтправляемымОбъектам(Основание);
		
		Если Сведения.ВидКонтролирующегоОргана <> ПредопределенноеЗначение("Перечисление.ТипыКонтролирующихОрганов.ФНС") Тогда
			
			ТекстОшибки = НСтр("ru = 'Отправка %1 возможна только для ФНС'");
			ТекстОшибки = СтрШаблон(ТекстОшибки);
				
		КонецЕсли;
		
		ОтправляетсяВКонтролирующийОрган = ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОбъектОтправляетсяВКонтролирующиеОрганы(Основание);
		
		ТекущееСостояние       = ТекущееСостояниеОтправки(Основание, Сведения.ВидКонтролирующегоОргана);
		ДокументооборотНеНачат = ТекущееСостояние = Неопределено 
			ИЛИ ТекущееСостояние.ТекущийЭтапОтправки.СостояниеСдачиОтчетности = ПредопределенноеЗначение("Перечисление.СостояниеСдачиОтчетности.ДокументооборотНеНачат");
			
		Если ТекущееСостояние = Неопределено ИЛИ НЕ ОтправляетсяВКонтролирующийОрган ИЛИ ДокументооборотНеНачат Тогда
			
			ТекстОшибки  = НСтр("ru = 'Отправка %1 возможна только для отправленного %2'");
			ТекстОшибки  = СтрШаблон(ТекстОшибки, ПредставлениеВРодительномПадеже, ПредставлениеОснованияВРодительномПадеже);
				
		КонецЕсли;
		
		Если ТекущееСостояние <> Неопределено И ТекстОшибки = "" Тогда
			
			ТекстСтатуса = ТекущееСостояние.ТекущийЭтапОтправки.ТекстСтатуса;
			
			Доставлено        = НСтр("ru = 'Доставлено'");
			ПринятоВОбработку = НСтр("ru = 'Принято в обработку'");
			Готово            = НСтр("ru = 'Готово'");
			Принято           = НСтр("ru = 'Принято'");
			Сдано             = НСтр("ru = 'Сдано'");
			ТребуетУточнения  = ДокументооборотСКОКлиентСервер.СтатусСданоТребуетУточнения();
			ОтправленоВСПАРК  = НСтр("ru = 'Сдано в ФНС, копия передана в СПАРК'");
			
			ЭтоВходящие	 = Сведения.СтраницаЖурнала = Перечисления.СтраницыЖурналаОтчетность.Входящие;
			ЭтоПисьмо    = ТипЗнч(Основание) = Тип("СправочникСсылка.ПерепискаСКонтролирующимиОрганами");
			ЭтоЗапросИОН = ТипЗнч(Основание) = Тип("ДокументСсылка.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика");
			ЭтоИсхОпись  = ТипЗнч(Основание) = Тип("СправочникСсылка.ОписиИсходящихДокументовВНалоговыеОрганы");
			ЭтоПояснение = ТипЗнч(Основание) = Тип("ДокументСсылка.ПоясненияКДекларацииПоНДС");
				
			Если ЭтоПисьмо И Основание.Статус <> Перечисления.СтатусыПисем.Полученное И ТекстОшибки = "" Тогда
				
				Если ТекстСтатуса <> Доставлено Тогда
				
					ТекстОшибки = НСтр("ru = 'Отправка %1 возможна только для писем со статусом ""%2""'");
					ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеВРодительномПадеже, Доставлено);
					
				КонецЕсли;
				
			КонецЕсли;
					
			Если ЭтоЗапросИОН И ТекстОшибки = "" Тогда
				
				Если ТекстСтатуса <> ПринятоВОбработку И ТекстСтатуса <> Готово Тогда
				
					ТекстОшибки = НСтр("ru = 'Отправка %1 возможна только для запросов на сверку со статусами ""%2"" или ""%3""'");
					ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеВРодительномПадеже, ПринятоВОбработку, Готово);
					
				КонецЕсли;
				
			КонецЕсли;
				
			Если (ЭтоИсхОпись ИЛИ ЭтоПояснение) И ТекстОшибки = "" Тогда
				
				Если ТекстСтатуса <> Принято Тогда
				
					ТекстОшибки = НСтр("ru = 'Отправка %1 возможна только для ответов на требование со статусом ""%2""'");
					ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеВРодительномПадеже, Принято);
					
				КонецЕсли;
				
			КонецЕсли;
					
			Если НЕ ЭтоВходящие И НЕ ЭтоПисьмо И НЕ ЭтоЗапросИОН И НЕ ЭтоИсхОпись И НЕ ЭтоПояснение И ТекстОшибки = "" Тогда
				
				Если ТекстСтатуса <> ПринятоВОбработку
					И ТекстСтатуса <> Сдано
					И ТекстСтатуса <> ТребуетУточнения
					И ТекстСтатуса <> ОтправленоВСПАРК Тогда
				
					ТекстОшибки = НСтр("ru = 'Отправка %1 возможна только для отчетов со статусами ""%2"", ""%3"", ""%4""'");
					ТекстОшибки = СтрШаблон(ТекстОшибки, ПредставлениеВРодительномПадеже, ПринятоВОбработку, Сдано, ТребуетУточнения);
					
				КонецЕсли;
					
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
		
	Возврат ТекстОшибки;
	
КонецФункции

#Область ОбработкаКартинок

// Отображение универсальной формы требования к сканам
//
// Параметры:
//  СтруктураТребований	 - Структура - см. ОперацииСФайламиЭДКОКлиент.ТребованияКСканам 
//
&НаКлиенте
Процедура ПоказатьТребованияКИзображениямУниверсальная(СтруктураТребований) Экспорт
	
	Требования = Новый Структура;
	Требования.Вставить("Формат", ДопустимыеФорматы(СтруктураТребований));
	
	// Цветность
	Если СтруктураТребований.ПреобразоватьВОттенкиСерого Тогда
		Цветность = НСтр("ru = 'Оттенки серого'");
	Иначе	
		Цветность = НСтр("ru = 'Цветное изображение'");
	КонецЕсли;
	
	Если СтруктураТребований.ГлубинаЦвета <> Неопределено Тогда
		Бит = СтрЗаменить(СтруктураТребований.ГлубинаЦвета, "БитНаПиксел", "");
		Если Бит = "1" ИЛИ Бит = "8" Тогда
			Бит = Бит + НСтр("ru = ' бит'");
		Иначе
			Бит = Бит + НСтр("ru = ' бита'");
		КонецЕсли;
		
		Цветность = Цветность + ", глубина цвета " +  Бит;
		
	КонецЕсли;
	
	Требования.Вставить("Цветность", Цветность);
	
	// Разрешение
	Разрешение = "";
	
	Если СтруктураТребований.МинимальнаяПлотность <> 0 И СтруктураТребований.МаксимальнаяПлотность = 0 Тогда
		Разрешение = Строка(СтруктураТребований.МинимальнаяПлотность) + " dpi";
	ИначеЕсли СтруктураТребований.МинимальнаяПлотность = 0 И СтруктураТребований.МаксимальнаяПлотность <> 0 Тогда
		Разрешение = Строка(СтруктураТребований.МаксимальнаяПлотность) + " dpi";
	ИначеЕсли СтруктураТребований.МинимальнаяПлотность <> 0 И СтруктураТребований.МаксимальнаяПлотность <> 0 Тогда
		Разрешение = Строка(СтруктураТребований.МинимальнаяПлотность) + "-" + Строка(СтруктураТребований.МаксимальнаяПлотность) + " dpi";
	КонецЕсли;
	
	Если Разрешение <> "" Тогда
		Требования.Вставить("Разрешение", Разрешение);
	КонецЕсли;
	
	Если СтруктураТребований.МаксимальныйРазмерФайла <> 0 Тогда
		Размер = ОбщегоНазначенияЭДКОКлиентСервер.ТекстовоеПредставлениеРазмераФайла(СтруктураТребований.МаксимальныйРазмерФайла);
		Требования.Вставить("Размер", Размер);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Требования",     Требования);
	ДополнительныеПараметры.Вставить("Пояснение",      СтруктураТребований.Пояснение);
	ДополнительныеПараметры.Вставить("КлючТребований", "Требования к файлам");
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ТребованияКИзображениямУниверсальная", ДополнительныеПараметры, ЭтотОбъект);
	
КонецПроцедуры

&НаСервере
Функция ДопустимыеФорматы(СтруктураТребований)
	
	Расширения = ОперацииСФайламиЭДКО.ПолучитьДопустимыеРасширенияФайлов(СтруктураТребований);
	Расширения = СтрСоединить(Расширения,", ");
	Возврат Расширения;
	
КонецФункции

#КонецОбласти

#Область ОбменСМинобороныРФ

&НаКлиенте
Функция РезультатЗагрузкиОтветаМОРФВСлучаеОшибки()
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("РезультатЗагрузки", Неопределено);
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьВыгруженныеФайлыМинобороны(ОтчетСсылка) Экспорт
	
	Запрос = Новый Запрос;

	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
	               |	ОтправкиМинобороны.ИмяФайлаПакета КАК ИмяФайлаПакета
	               |ИЗ
	               |	Справочник.ОтправкиМинобороны КАК ОтправкиМинобороны
	               |ГДЕ
	               |	ОтправкиМинобороны.ОтчетСсылка = &ОтчетСсылка
	               |	И ОтправкиМинобороны.ПометкаУдаления = ЛОЖЬ
	               |
	               |УПОРЯДОЧИТЬ ПО
	               |	ОтправкиМинобороны.ДатаОтправки УБЫВ";
	
	Запрос.УстановитьПараметр("ОтчетСсылка", ОтчетСсылка);
	
	Результат = Запрос.Выполнить().Выгрузить(ОбходРезультатаЗапроса.Прямой).ВыгрузитьКолонку("ИмяФайлаПакета");
	
	Возврат Результат;

	
КонецФункции

&НаКлиенте
Процедура ЗагрузитьОтветыИзМинобороны(
	ОрганизацияСсылка,
	ВыполняемоеОповещение,
	ОтчетСсылка=Неопределено,
	Форма=Неопределено,
	ОтправкаСсылка=Неопределено) Экспорт
	
	ЗагрузкаНесколькихФайлов = ОтчетСсылка = Неопределено;
	
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	
	Если Форма = Неопределено Тогда
		Форма = МодульДокументооборотСМинобороныКлиент.ПолучитьФормуОтчетаПоСсылке(ОтчетСсылка);
	КонецЕсли;
	
	Если ОрганизацияСсылка = Неопределено Тогда
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;

	Если ЗагрузкаНесколькихФайлов Тогда
		Отправка = Неопределено;
	Иначе
		Если ОтправкаСсылка = Неопределено Тогда
			Отправка = МодульДокументооборотСМинобороныКлиент.ПолучитьПоследнююОтправкуОтчета(ОтчетСсылка);
		Иначе
			Отправка = ОтправкаСсылка;
		КонецЕсли; 
	КонецЕсли; 
	
	ВыгруженныеФайлы = ПолучитьВыгруженныеФайлыМинобороны(ОтчетСсылка);
	
	Если ВыгруженныеФайлы.Количество() = 0 Тогда
		// отправок не было
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ДлительнаяОтправкаКлиентСервер.ВывестиПредупреждение(НСтр("ru='Данные в Минобороны не отправлялись.'", "ru") );
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли; 
	
	Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОрганизацияСсылка, , "ИННЮЛ");
	ИНН = СокрЛП(Сведения.ИННЮЛ);
	
	Префикс = "";
	Суффикс = "_r";
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",               ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка",                   ОрганизацияСсылка);
	ДополнительныеПараметры.Вставить("ИНН",                                 ИНН);
	ДополнительныеПараметры.Вставить("ВыгруженныеФайлы",                    ВыгруженныеФайлы);
	ДополнительныеПараметры.Вставить("Префикс",                             Префикс);
	ДополнительныеПараметры.Вставить("Суффикс",                             Суффикс);
	ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ЗагрузкаНесколькихФайлов",            ЗагрузкаНесколькихФайлов);
	ДополнительныеПараметры.Вставить("Отправка",                            Отправка);
	ДополнительныеПараметры.Вставить("Форма",                               Форма);
	ДополнительныеПараметры.Вставить("ЭтоОтветНаПоследнююВыгрузку",         Ложь);
	
	Оповещение = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныПослеУстановкиРасширения",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	// установка расширения работы с файлами и расширения для работы с криптографией
	ЭлектроннаяПодписьКлиент.УстановитьРасширение(Истина, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеУстановкиРасширения(Подключено, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение     = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если НЕ Подключено Тогда
		// расширение не подключено
		ТекстОшибки = НСтр("ru='Расширение для работы с файлами не подключено.'");
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru='Обмен с Минобороны. Загрузка ответа.'") , 
			"Ошибка", 
			ТекстОшибки ,, 
			Истина);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	МодульДокументооборотСМинобороныВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныВызовСервера");
	РезультатЗагрузки = МодульДокументооборотСМинобороныВызовСервера.ЗагрузитьСертификатыМинобороны();
	
	Если ПустаяСтрока(РезультатЗагрузки.ОписаниеОшибки) Тогда
		Оповещение = Новый ОписаниеОповещения("ЗагрузитьОтветИзМинобороныПослеУстановкиСертификатов", ЭтотОбъект, ДополнительныеПараметры);
		
		МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
		МодульДокументооборотСМинобороныКлиент.УстановитьСертификатыГоловныхПромежуточныхЦентров(Оповещение, РезультатЗагрузки.Сертификаты);
	Иначе
		ЗагрузитьОтветИзМинобороныПослеУстановкиСертификатов(Ложь, ДополнительныеПараметры);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеУстановкиСертификатов(Результат, ДополнительныеПараметры) Экспорт
	
	Оповещение = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныПослеПодключенияРасширенияДляРаботыСФайлами",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	НачатьПодключениеРасширенияРаботыСФайлами(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеВыбораКаталога(ВыбранныеФайлы, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение     =    ДополнительныеПараметры.ВыполняемоеОповещение;
	ИНН                       =                      ДополнительныеПараметры.ИНН;
	Префикс                   =                  ДополнительныеПараметры.Префикс;
	Суффикс                   =                  ДополнительныеПараметры.Суффикс;
	ЗагрузкаНесколькихФайлов  = ДополнительныеПараметры.ЗагрузкаНесколькихФайлов;

	
	Если ВыбранныеФайлы = Неопределено Тогда 
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Если ЗагрузкаНесколькихФайлов Тогда
		КаталогЗагрузки = ВыбранныеФайлы[0];
		Расширение			= ".zip";
		МаскаПоиска			= Префикс + ИНН + "*" + Суффикс + Расширение;
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗагрузитьОтветИзМинобороныПослеПоискаФайлов",
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		НачатьПоискФайлов(ОписаниеОповещения, КаталогЗагрузки, МаскаПоиска, Ложь);
	Иначе
		Файл = Новый Файл(ВыбранныеФайлы[0]);
		МассивНайденныхФайлов = Новый Массив;
		МассивНайденныхФайлов.Добавить(Файл);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗагрузитьОтветИзМинобороныПослеПоискаФайлов",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, МассивНайденныхФайлов);
	КонецЕсли; 
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеПоискаФайлов(МассивНайденныхФайлов, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение     = ДополнительныеПараметры.ВыполняемоеОповещение;
	ВыгруженныеФайлы          = ДополнительныеПараметры.ВыгруженныеФайлы;
	Префикс                   = ДополнительныеПараметры.Префикс;
	Суффикс                   = ДополнительныеПараметры.Суффикс;
	ЗагрузкаНесколькихФайлов  = ДополнительныеПараметры.ЗагрузкаНесколькихФайлов;
	
	КоличествоНайденныхФайлов = МассивНайденныхФайлов.Количество();
	Если КоличествоНайденныхФайлов = 0 Тогда
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// проверяем содержат ли названия файлов имя исходного пакета
	Ответы = Новый Массив;
	Для каждого НайденныйФайл Из МассивНайденныхФайлов Цикл
		ИмяПакета = СтрЗаменить(СтрЗаменить(НайденныйФайл.Имя, Префикс, ""), Суффикс, "");
		НайденнаяСтрока = ВыгруженныеФайлы.Найти(ИмяПакета);
		Если НайденнаяСтрока <> Неопределено Тогда
			 Ответы.Добавить(НайденныйФайл);
		КонецЕсли;
	КонецЦикла;
	
	ДополнительныеПараметры.ЭтоОтветНаПоследнююВыгрузку = ИмяПакета = ВыгруженныеФайлы[0];
	
	Если Ответы.Количество() = 0 Тогда
		Если ЗагрузкаНесколькихФайлов Тогда
			ПоказатьПредупреждение(, НСтр("ru = 'В именах выбранных файлов не обнаружено протоколов о сдаче отчетности.'"));
		Иначе
			Шаблон = НСтр("ru = 'Выбранный файл %1 не является ответом Минобороны РФ на пакет с текущим отчетом.'");
			ТекстПредупреждения = СтрШаблон(Шаблон, НайденныйФайл.Имя);
			ПоказатьПредупреждение(, ТекстПредупреждения);
		КонецЕсли;
		
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
		
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныПослеПомещенияФайлов",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ПомещаемыеФайлы = Новый Массив;
	Для каждого Ответ Из Ответы Цикл
		ОписаниеФайла = Новый ОписаниеПередаваемогоФайла;
		ОписаниеФайла.Имя = Ответ.ПолноеИмя;
		ПомещаемыеФайлы.Добавить(ОписаниеФайла);
	КонецЦикла;
	НачатьПомещениеФайлов(ОписаниеОповещения, ПомещаемыеФайлы, Ложь, Новый УникальныйИдентификатор);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеПомещенияФайлов(ПомещенныеФайлы, ДополнительныеПараметры) Экспорт
	
	Ответы = Новый Массив;
	Для каждого Файл Из ПомещенныеФайлы Цикл
		Ответ = Новый Структура("ОписаниеФайла, Адрес");
		Ответ.ОписаниеФайла = ОбщегоНазначенияКлиентСервер.РазложитьПолноеИмяФайла(Файл.ПолноеИмя);
		Ответ.Адрес = Файл.Хранение;
		Ответы.Добавить(Ответ);
	КонецЦикла; 
	
	ДополнительныеПараметры.Вставить("Ответы", Ответы);
	ДополнительныеПараметры.Вставить("ДанныеОтветов", Новый Массив);
	ДополнительныеПараметры.Вставить("БылиОшибки", Ложь);
	ДополнительныеПараметры.Вставить("ТекстОшибки", "");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныЗагрузитьОтветыЦикл",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныЗагрузитьОтветыЦикл(Результат, ДополнительныеПараметры) Экспорт
	
 	Ответы                       = ДополнительныеПараметры.Ответы;
	Префикс                      = ДополнительныеПараметры.Префикс;
	Суффикс                      = ДополнительныеПараметры.Суффикс;
	ВыполняемоеОповещение        = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗагрузкаНесколькихФайлов     = ДополнительныеПараметры.ЗагрузкаНесколькихФайлов;
	БылиОшибки                   = ДополнительныеПараметры.БылиОшибки;
	Форма                        = ДополнительныеПараметры.Форма;
	ЭтоОтветНаПоследнююВыгрузку  = ДополнительныеПараметры.ЭтоОтветНаПоследнююВыгрузку;
	ВыгруженныеФайлы             = ДополнительныеПараметры.ВыгруженныеФайлы;
	
	Если Ответы.Количество() > 0 Тогда
		ФайлОтвета = Ответы[0].ОписаниеФайла;
		Адрес = Ответы[0].Адрес;
		Ответы.Удалить(0);
		
		ДанныеФайла = ПолучитьИзВременногоХранилища(Адрес);
		
		МодульДокументооборотСМинобороныВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныВызовСервера");
		РезультатРаспаковки = МодульДокументооборотСМинобороныВызовСервера.ПрочитатьАрхивОтвета(Адрес);
		
		ИсходныеДанные = РезультатРаспаковки.ИсходныеДанные;
		Подпись = РезультатРаспаковки.Подпись;
		
		ДанныеОтвета = Новый Структура;
		ДанныеОтвета.Вставить("ДанныеОтвета", ИсходныеДанные);
		ДанныеОтвета.Вставить("Подпись", Подпись);
		ДанныеОтвета.Вставить("Файл", ФайлОтвета);
		ДанныеОтвета.Вставить("ПодписьВалидна", Ложь);
		ДанныеОтвета.Вставить("СертификатВалиден", Ложь);
		ДанныеОтвета.Вставить("Сертификат", Неопределено);
		ДанныеОтвета.Вставить("Архив", ДанныеФайла);
		ДанныеОтвета.Вставить("АдресАрхива", Адрес);
		ДанныеОтвета.Вставить("Статус", Неопределено);
		ДанныеОтвета.Вставить("ОтправкаСсылка", Неопределено);
		Идентификатор = СтрЗаменить(СтрЗаменить(ФайлОтвета.ИмяБезРасширения, Префикс, ""), Суффикс, "");
		ДанныеОтвета.Вставить("Идентификатор", Идентификатор);

		ДополнительныеПараметры.ДанныеОтветов.Добавить(ДанныеОтвета);
		
		// проверяем есть ли данные и подпись
		Если ИсходныеДанные = Неопределено ИЛИ
			Подпись = Неопределено Тогда
			// ошибка распаковки файла ответа
			ИмяФайла = ФайлОтвета.Имя;
			ТекстОшибки = СтрШаблон("ru = 'Ошибка распаковки файла с ответной квитанцией %1.'", ИмяФайла);
			ТекстОшибки = НСтр(ТекстОшибки);
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			Если ИсходныеДанные = Неопределено Тогда
				ТекстОшибки = НСтр("ru = 'В пакете отсутствует файл с протоколом.'");
			Иначе
				ТекстОшибки = НСтр("ru = 'В пакете отсутствует файл с подписью или подпись не в формате BASE-64.'");
			КонецЕсли;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			// записи в журнал
			Шаблон = НСтр("ru = 'Ошибка распаковки файла ответа %1.'");
			ТекстОшибкиРаспаковки = СтрШаблон(Шаблон, ИмяФайла);
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru = 'Обмен с Минобороны. Загрузка ответа.'"),
				"Ошибка",
				ТекстОшибкиРаспаковки , ,
				Истина);
			ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
				НСтр("ru='Обмен с Минобороны. Загрузка ответа.'") ,
				"Ошибка",
				ТекстОшибки , ,
				Истина);
			// завершаем обработку если нет протокола
			Если ИсходныеДанные = Неопределено Тогда
				ДополнительныеПараметры.БылиОшибки = Истина;
				ОписаниеОповещения = Новый ОписаниеОповещения(
						"ЗагрузитьОтветИзМинобороныЗагрузитьОтветыЦикл",
						ЭтотОбъект,
						ДополнительныеПараметры);
				ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
				Возврат;
			КонецЕсли;
		КонецЕсли;
		
		Если НЕ ЗагрузкаНесколькихФайлов И Форма <> Неопределено Тогда
			АдресДанныхФайлаОтвета = ПоместитьВоВременноеХранилище(
					ИсходныеДанные,
					Новый УникальныйИдентификатор);
			// помещаем адрес данных ответа в реквизиты формы для отображения протокола
			Форма.СтруктураРеквизитовФормы.Вставить(
				"АдресВоВременномХранилищеОтветноеСообщение",
				АдресДанныхФайлаОтвета);
		КонецЕсли;
		
		Если Подпись <> Неопределено Тогда
			// подпись присутствует, делаем её проверку
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗагрузитьОтветИзМинобороныПослеПроверкиПодписиЦикл",
				ЭтотОбъект,
				ДополнительныеПараметры);
			
			МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
			МодульДокументооборотСМинобороныКлиент.ПроверитьПодпись(ОписаниеОповещения, ИсходныеДанные, Подпись);
			Возврат;
		Иначе
			// если отсутствует подпись предлагаем пользователю продолжить загрузку
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗагрузитьОтветИзМинобороныПослеВопросаПроверкаСертификатаЦикл",
				ЭтотОбъект,
				ДополнительныеПараметры);
			
			ТекстВопроса = "Подпись отсутствует или ее содержимое некорректно. Продолжить загрузку протокола?";
			ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет);
			Возврат;
		КонецЕсли;
	Иначе
		// завершение загрузки
		Если ЗагрузкаНесколькихФайлов Тогда
			Если БылиОшибки Тогда
				ПоказатьПредупреждение(, НСтр("ru = 'Загрузка протоколов о сдаче завершена. При загрузке возникали ошибки.'"));
			КонецЕсли;
		Иначе
			// загрузка одного файла
			Если БылиОшибки Тогда
				ПоказатьПредупреждение(, НСтр("ru = 'При загрузке протокола о сдаче возникли ошибки.'"));
			Иначе
				ДанныеОтвета = ДополнительныеПараметры.ДанныеОтветов[0];
				// если загружаемый файл не является ответом на последний выгруженный отчет
				Если НЕ ЭтоОтветНаПоследнююВыгрузку Тогда
					Файл = Новый Файл(ВыгруженныеФайлы[0]);
					ИдентификаторПоследнейВыгрузки = Файл.ИмяБезРасширения;
					ИмяЗагруженногоФайла = ДанныеОтвета.Файл.Имя;
					ТекстСообщения = СтрШаблон("ru = 'Загружены данные из файла %1.
							|Загруженный протокол не является протоколом сдачи последнего выгруженного отчета,
							|поэтому информация о текущем статусе отчета изменена не будет.
							|
							|Идентификатор последнего выгруженного отчета %2.
							|Ответ на последнюю выгрузку должен находиться в файле с именем %2%3.zip.'",
						ИмяЗагруженногоФайла,
						ИдентификаторПоследнейВыгрузки,
						Суффикс);
					ТекстСообщения = НСтр(ТекстСообщения);
					ПоказатьПредупреждение(,ТекстСообщения);
				Иначе
					// успешная загрузка одного файла
					Если ДанныеОтвета.Статус = ПредопределенноеЗначение("Перечисление.СтатусыОтправки.НеПринят") ИЛИ
						ДанныеОтвета.Статус = ПредопределенноеЗначение("Перечисление.СтатусыОтправки.ПринятЕстьОшибки") Тогда
						
						Если ДанныеОтвета.Статус = ПредопределенноеЗначение("Перечисление.СтатусыОтправки.НеПринят") Тогда
							ТекстВопроса = НСтр("ru='Отчет не принят, протокол содержит описание ошибок. Открыть протокол?'");
						Иначе
							ТекстВопроса = НСтр("ru='Отчет принят, но протокол содержит описание некритичных ошибок. Открыть протокол?'");
						КонецЕсли;
						
						СписокКнопок = Новый СписокЗначений;
						СписокКнопок.Добавить(КодВозвратаДиалога.Да, "Показать протокол");
						СписокКнопок.Добавить(КодВозвратаДиалога.Отмена, "Отмена");
						Оповещение = Новый ОписаниеОповещения("ЗагрузитьОтветИзМинобороныПослеВопросаПоказатьПротоколЦикл",
							ЭтотОбъект,
							ДополнительныеПараметры);
						ПоказатьВопрос(Оповещение, ТекстВопроса, СписокКнопок, , КодВозвратаДиалога.Да);
					Иначе
						ТекстСообщения = НСтр("ru='Протокол успешно загружен, отчет принят. Статус отчета обновлен.'");
						ПоказатьПредупреждение(, ТекстСообщения);
					КонецЕсли;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
		// формируем результат
		Результат = Новый Структура;
		Если НЕ ЗагрузкаНесколькихФайлов И БылиОшибки Тогда
			Результат.Вставить("Выполнено", Ложь);
		Иначе 
			Результат.Вставить("Выполнено", Истина);
		КонецЕсли;
		Результат.Вставить("РезультатЗагрузки", ДополнительныеПараметры.ДанныеОтветов);
		
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеВопросаПоказатьПротоколЦикл(Результат, ДополнительныеПараметры) Экспорт
	
	ДанныеОтвета = ДополнительныеПараметры.ДанныеОтветов[0];
	
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	Если Результат = КодВозвратаДиалога.Да Тогда
		МодульДокументооборотСМинобороныКлиент.ПоказатьПротокол(ДанныеОтвета.ОтправкаСсылка);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеПроверкиПодписиЦикл(Результат, ДополнительныеПараметры) Экспорт
	
	ДанныеОтвета = ДополнительныеПараметры.ДанныеОтветов[ДополнительныеПараметры.ДанныеОтветов.Количество() - 1];
	
	Если НЕ Результат.Выполнено Тогда
		ИмяФайла = ДанныеОтвета.Файл.Имя;
		Идентификатор = ДанныеОтвета.Идентификатор;
		ТекстОшибки = НСтр("ru='Ошибка проверки подписи в файле %1. Описание ошибки: %2.'");
		ТекстОшибки = СтрШаблон(ТекстОшибки, ИмяФайла, Результат.ОписаниеОшибки);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru='Обмен с Минобороны. Загрузка ответа.'", "ru") , 
			"Ошибка", 
			НСтр(СтрШаблон("ru='Ошибка проверка подписи ответа для отправки с идентификатором %1' в файле %2", Идентификатор, ИмяФайла), "ru") ,, 
			Истина);
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗагрузитьОтветИзМинобороныПослеВопросаПроверкаПодписиЦикл",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ПоказатьВопрос(
			ОписаниеОповещения,
			НСтр("ru='При проверке подписи протокола произошла ошибка, продолжить загрузку?'"),
			РежимДиалогаВопрос.ДаНет,
			0,
			КодВозвратаДиалога.Нет
			);
		Возврат;
	КонецЕсли; 
	
	Сертификат = Результат.Сертификат;
	ДанныеОтвета.ПодписьВалидна = Истина;
	ДанныеОтвета.Сертификат = Сертификат;
	
	ДополнительныеПараметры.Вставить("Сертификат", Сертификат);
	
	ОписаниеОповещение = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныПослеПроверкиСертификатаЦикл",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	МодульДокументооборотСМинобороныКлиент.ПроверитьСертификат(ОписаниеОповещение, Сертификат);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеВопросаПроверкаПодписиЦикл(Результат, ДополнительныеПараметры) Экспорт
	
	ДанныеОтвета = ДополнительныеПараметры.ДанныеОтветов[ДополнительныеПараметры.ДанныеОтветов.Количество() - 1];
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ДополнительныеПараметры.БылиОшибки = Истина;
		УдалитьИзВременногоХранилища(ДанныеОтвета.АдресАрхива);
		ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗагрузитьОтветИзМинобороныЗагрузитьОтветыЦикл",
				ЭтотОбъект,
				ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
		Возврат;
	КонецЕсли;
	
	// пропускаем проверку сертификата
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныПослеВопросаПроверкаСертификатаЦикл",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.Да);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеПроверкиСертификатаЦикл(Результат, ДополнительныеПараметры) Экспорт
	
	ДанныеОтвета = ДополнительныеПараметры.ДанныеОтветов[ДополнительныеПараметры.ДанныеОтветов.Количество() - 1];
	
	ОписаниеОповещенияВопрос = Новый ОписаниеОповещения(
		"ЗагрузитьОтветИзМинобороныПослеВопросаПроверкаСертификатаЦикл",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	Если НЕ Результат.Выполнено Тогда
		
		ИмяФайла = ДанныеОтвета.Файл.Имя;
		Идентификатор = ДанныеОтвета.Идентификатор;
		// выводим ошибки
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр(СтрШаблон("ru='Ошибка проверки сертификата в файле %1.'", ИмяФайла), "ru") );
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru='Обмен с Минобороны. Загрузка ответа.'", "ru") , 
			"Ошибка", 
			НСтр(СтрШаблон("ru='Ошибка проверка сертификата ответа для отправки с идентификатором %1' в файле %2", Идентификатор, ИмяФайла), "ru") ,, 
			Истина);
			
		ПоказатьВопрос(ОписаниеОповещенияВопрос,
			НСтр("ru='При проверке сертификата произошла ошибка.
				 |Продолжить загрузку протокола?'", "ru"),
			РежимДиалогаВопрос.ДаНет,
			0,
			КодВозвратаДиалога.Нет);
			
		Возврат;
	КонецЕсли;
	
	Если НЕ Результат.Валиден Тогда
		
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр(СтрШаблон("ru='Сертификат подписи в файле %1 не валиден.'", ИмяФайла), "ru") );
		ЖурналРегистрацииКлиент.ДобавитьСообщениеДляЖурналаРегистрации(
			НСтр("ru='Обмен с Минобороны. Загрузка ответа.'", "ru") , 
			"Ошибка", 
			НСтр(СтрШаблон("ru='Сертификат подписи ответа не валиден для отправки с идентификатором %1' в файле %2.", Идентификатор, ИмяФайла), "ru") ,, 
			Истина);
			
		ПоказатьВопрос(ОписаниеОповещенияВопрос,
			НСтр("ru='Сертификат подписи протокола не валиден.
				|Продолжить загрузку протокола?'", "ru"),
			РежимДиалогаВопрос.ДаНет,
			0,
			КодВозвратаДиалога.Нет);
			
		Возврат;
	КонецЕсли;
	
	ДанныеОтвета.СертификатВалиден = Истина;
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещенияВопрос, КодВозвратаДиалога.Да);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеВопросаПроверкаСертификатаЦикл(Результат, ДополнительныеПараметры) Экспорт
	
	ДанныеОтвета = ДополнительныеПараметры.ДанныеОтветов[ДополнительныеПараметры.ДанныеОтветов.Количество() - 1];
	Форма = ДополнительныеПараметры.Форма;
	
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	МодульДокументооборотСМинобороныВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныВызовСервера");
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗагрузитьОтветИзМинобороныЗагрузитьОтветыЦикл",
			ЭтотОбъект,
			ДополнительныеПараметры);
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ДополнительныеПараметры.БылиОшибки = Истина;
		УдалитьИзВременногоХранилища(ДанныеОтвета.АдресАрхива);
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
		Возврат;
	КонецЕсли;
	
	ПараметрыЗагрузки = Новый Структура;
	ПараметрыЗагрузки.Вставить("ДанныеОтвета");
	ПараметрыЗагрузки.Вставить("ИмяФайла", ДанныеОтвета.Файл.Имя);
	ПараметрыЗагрузки.Вставить("Идентификатор");
	ПараметрыЗагрузки.Вставить("АдресАрхива");
	ЗаполнитьЗначенияСвойств(ПараметрыЗагрузки, ДанныеОтвета);
	
	// сохраняем отправку
	РезультатСохранения = МодульДокументооборотСМинобороныВызовСервера.СохранитьОтвет(ПараметрыЗагрузки);
	
	УдалитьИзВременногоХранилища(ДанныеОтвета.АдресАрхива);
	
	Если НЕ РезультатСохранения.Выполнено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(РезультатСохранения.ОписаниеОшибки);
		ДополнительныеПараметры.БылиОшибки = Истина;
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
		Возврат;
	КонецЕсли;
	
	// получаем состояние протокола
	ОтправкаСсылка = РезультатСохранения.ОтправкаСсылка;
	АдресДереваПротокола = МодульДокументооборотСМинобороныКлиент.ПолучитьАдресДереваПротоколаСдачи(
		ОтправкаСсылка);
	Форма.СтруктураРеквизитовФормы.Вставить("АдресВоВременномХранилищеОтветноеСообщение", АдресДереваПротокола);
	СостояниеПротокола = Неопределено;
	ВыводитьСообщенияОбОшибках = Ложь;
	СообщенияОбОшибкахВыведены = Неопределено;
	Форма.ОбработатьПротоколСдачиОтчета(СостояниеПротокола, ВыводитьСообщенияОбОшибках, СообщенияОбОшибкахВыведены);
	ОшибкаСтатуса = Ложь;
	Если СостояниеПротокола <> Неопределено Тогда
		// преобразуем состояние в статус
		Статус = МодульДокументооборотСМинобороныКлиент.ПолучитьСтатусОтправкиИзСостоянияПротокола(СостояниеПротокола);
		Если Статус = Неопределено Тогда
			// неверный формат протокола
			ТекстОшибки = НСтр("ru='Протокол сдачи отчета имеет неверный формат.'");
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
			ДополнительныеПараметры.БылиОшибки = Истина;
			ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
			Возврат;
		КонецЕсли;
		СтатусСохранен = МодульДокументооборотСМинобороныКлиент.СохранитьСтатусОтправки(ОтправкаСсылка, Статус);
		Если НЕ СтатусСохранен Тогда
			ОшибкаСтатуса = Истина;
		КонецЕсли;
	Иначе
		ОшибкаСтатуса = Истина;
	КонецЕсли;
	
	Если ОшибкаСтатуса Тогда
		ДополнительныеПараметры.БылиОшибки = Истина;
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
		Возврат;
	КонецЕсли;
	
	ДанныеОтвета.Статус = Статус;
	ДанныеОтвета.ОтправкаСсылка = ОтправкаСсылка;
	
	// сохраняем статус отчета в журнал
	МодульДокументооборотСМинобороныКлиент.ЗаписьОбъектовРегламентированнойОтчетности(РезультатСохранения.ОтчетСсылка, Ложь);
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения, Истина);
	
КонецПроцедуры
 
&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМинобороны(
	Форма,
	ВыполняемоеОповещение) Экспорт
	
	МодульДокументооборотСМинобороныКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиентСервер");
	
	ОтчетСсылка = МодульДокументооборотСМинобороныКлиентСервер.СсылкаНаОтчетПоФорме(Форма);
	
	Если ОтчетСсылка = Неопределено Тогда
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(Неопределено);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	РезультатВыгрузки = ВыгрузитьОтчетДляОтправкиВМОРФ(
		ОтчетСсылка,
		Форма);
	
	Если РезультатВыгрузки = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
			НСтр("ru = 'Не удалось выгрузить данные отчета в файл на диске, возможно нужно проверить выгрузку.'"));
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(Неопределено);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
		
	АдресФайлаВыгрузки 		= РезультатВыгрузки.АдресФайлаВыгрузки;
	ИмяФайлаВыгрузки   		= РезультатВыгрузки.ИмяФайлаВыгрузки;
	ДатаФормированияОтчета 	= РезультатВыгрузки.ДатаФормированияОтчета;
	ДатаОтчета				= РезультатВыгрузки.ДатаОтчета;
	ТипФайлаВыгрузки 		= РезультатВыгрузки.ТипФайлаВыгрузки;
	ИдентификаторФайла 		= РезультатВыгрузки.ИдентификаторФайла;
	Type 					= РезультатВыгрузки.Type;
	КПП 					= РезультатВыгрузки.КПП;	
	ТипЗнчСсылкаНаОтчет = ТипЗнч(ОтчетСсылка);
	
	ОрганизацияСсылка = МодульДокументооборотСМинобороныКлиентСервер.ПолучитьОрганизациюПоФорме(Форма);
	
	РеквизитыУчетнойЗаписи              = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(ОрганизацияСсылка);
	УчетнаяЗапись                       = РеквизитыУчетнойЗаписи.Ссылка;
	Идентификатор                       = РеквизитыУчетнойЗаписи.ИдентификаторДокументооборота;
	
	Если РегламентированнаяОтчетностьВызовСервера.ЭтоЮридическоеЛицо(ОрганизацияСсылка) Тогда
		Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОрганизацияСсылка, , "ИННЮЛ");
		ИНН = СокрЛП(Сведения.ИННЮЛ);
	Иначе
		Сведения = РегламентированнаяОтчетностьВызовСервера.ПолучитьСведенияОбОрганизации(ОрганизацияСсылка, , "ИННФЛ");
		ИНН = СокрЛП(Сведения.ИННФЛ);
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение",               ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("Идентификатор",                       Идентификатор);
	ДополнительныеПараметры.Вставить("ОтчетСсылка",                         ОтчетСсылка);
	ДополнительныеПараметры.Вставить("ОрганизацияСсылка",                   ОрганизацияСсылка);
	ДополнительныеПараметры.Вставить("ИНН",                                 ИНН);
	ДополнительныеПараметры.Вставить("СертификатыИПараметрыОбмена",         Неопределено);
	ДополнительныеПараметры.Вставить("ТипЗнчСсылкаНаОтчет",                 ТипЗнчСсылкаНаОтчет);
	ДополнительныеПараметры.Вставить("Форма",                               Форма);
	ДополнительныеПараметры.Вставить("АдресФайлаВыгрузки",                  АдресФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИмяФайлаВыгрузки",                    ИмяФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ДатаФормированияОтчета",              ДатаФормированияОтчета);
	ДополнительныеПараметры.Вставить("ДатаОтчета",                          ДатаОтчета);
	ДополнительныеПараметры.Вставить("ТипФайлаВыгрузки",                    ТипФайлаВыгрузки);
	ДополнительныеПараметры.Вставить("ИдентификаторФайла",                  ИдентификаторФайла);
	ДополнительныеПараметры.Вставить("Type",                                Type);
	ДополнительныеПараметры.Вставить("КПП",                                 КПП);
	ДополнительныеПараметры.Вставить("СтрокаДанныхПодписи",                 "");
	ДополнительныеПараметры.Вставить("ИмяФайлаПодписи",                     "message.sign");
	ДополнительныеПараметры.Вставить("КаталогВыгрузки",                     "");
	ДополнительныеПараметры.Вставить("Пароль",                              "");
	ДополнительныеПараметры.Вставить("СертификатАбонента",                  Неопределено);
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляКриптографии",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	// установка расширения работы с криптографией
	ЭлектроннаяПодписьКлиент.УстановитьРасширение(Истина, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляКриптографии(Подключено, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение        = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатыИПараметрыОбмена  = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	
	Если НЕ Подключено Тогда
		Результат = Новый Структура("РезультатВыгрузки, СертификатыИПараметрыОбмена", Неопределено, СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВМОРФПослеПодключенияРасширенияДляРаботыСФайлами",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	НачатьПодключениеРасширенияРаботыСФайлами(Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеПодключенияРасширенияДляРаботыСФайлами(Подключено, ДополнительныеПараметры) Экспорт
	
	Если Подключено Тогда
		Оповещение = Новый ОписаниеОповещения(
			"ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляРаботыСФайламиОбщая",
			ЭтотОбъект,
			ДополнительныеПараметры);
		ВыполнитьОбработкуОповещения(Оповещение, Истина);
	Иначе
		Оповещение = Новый ОписаниеОповещения(
			"ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляРаботыСФайлами",
			ЭтотОбъект,
			ДополнительныеПараметры);
		НачатьУстановкуРасширенияРаботыСФайлами(Оповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляРаботыСФайлами(ДополнительныеПараметры) Экспорт
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляРаботыСФайламиОбщая",
		ЭтотОбъект,
		ДополнительныеПараметры);
	ВыполнитьОбработкуОповещения(Оповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеУстановкиРасширенияДляРаботыСФайламиОбщая(Результат, ДополнительныеПараметры) Экспорт
	
	ОписаниеОповещенияПродолжения = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВМОРФПослеПолученияСвойствПрограммыДляМенеджераКриптографии",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	// проверяем установленные криптопровайдеры
	МодульДокументооборотСМинобороныСлужебныйКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныСлужебныйКлиент");
	МодульДокументооборотСМинобороныСлужебныйКлиент.ПолучитьСвойстваПрограммыМенеджераКриптографии(ОписаниеОповещенияПродолжения);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеПолученияСвойствПрограммыДляМенеджераКриптографии(РезультатПолученияСвойств, ДополнительныеПараметры) Экспорт
	
	Если РезультатПолученияСвойств.Свойства = Неопределено Тогда
		Шаблон = НСтр("ru = 'Формирование пакета прервано по причине: %1'");
		ОписаниеОшибки = СтрШаблон(Шаблон, РезультатПолученияСвойств.ОписаниеОшибки);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ОписаниеОшибки);
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(Неопределено);
		ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ОрганизацияСсылка = ДополнительныеПараметры.ОрганизацияСсылка;
	
	ПараметрыФормы = Новый Структура();
	ПараметрыФормы.Вставить("Организация", ОрганизацияСсылка);
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВМОРФПослеВводаПараметровВыгрузки",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ВыгрузкаПакетаДляМинобороны", 
		ПараметрыФормы,
		ДополнительныеПараметры.Форма,
		,
		,
		,
		Оповещение,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеВводаПараметровВыгрузки(ПараметрыВыгрузки, ДополнительныеПараметры) Экспорт
	
	ОрганизацияСсылка            = ДополнительныеПараметры.ОрганизацияСсылка;
	
	Если ПараметрыВыгрузки = Неопределено Тогда
		// нажата кнопка Отмена
		Возврат;
	КонецЕсли;
		
	ДополнительныеПараметры.Вставить("КаталогВыгрузки", ПараметрыВыгрузки.КаталогВыгрузки);
	ДополнительныеПараметры.Вставить("Пароль", ПараметрыВыгрузки.Пароль);
	
	Оповещение = Новый ОписаниеОповещения(
		"ВыгрузитьПакетДляОтправкиВМОРФПослеПолученияСертификатовИПараметровОбмена",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	СертификатыИПараметрыОбменаОрганизацииДляМОРФ(
		Оповещение,
		ОрганизацияСсылка,
		Ложь,
		Неопределено,
		);

КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеПолученияСертификатовИПараметровОбмена(СертификатыИПараметрыОбмена, ДополнительныеПараметры) Экспорт

	ВыполняемоеОповещение               = ДополнительныеПараметры.ВыполняемоеОповещение;
	АдресФайлаВыгрузки                  = ДополнительныеПараметры.АдресФайлаВыгрузки;
	ИмяФайлаВыгрузки                    = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	ПарольКонтейнера                    = ДополнительныеПараметры.Пароль;
	
	ДополнительныеПараметры.СертификатыИПараметрыОбмена = СертификатыИПараметрыОбмена;
	
	Если СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Использование документооборота с министерством обороны РФ не включено для организации!'"));
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(Неопределено);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	// запрашиваем параметры формирования пакета и параметры обмена при вызове из отправки
	Если НЕ СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.ИспользоватьОбмен Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Использование документооборота с министерством обороны РФ не включено для организации!'"));
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(Неопределено);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
		
	СертификатАбонента = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.СертификатАбонента;
	
	//проверяем сертификаты
	СертификатыОбмена = Новый СписокЗначений();
	СертификатыОбмена.Добавить(СертификатАбонента, НСтр("ru = 'Сертификат подписи'"));
	
	Если НЕ СертификатыГодятсяДляПодписанияОтчетностиМОРФ(СертификатыОбмена) Тогда
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(Неопределено);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.ИмяФайлаВыгрузки = ИмяФайлаВыгрузки;
	ДополнительныеПараметры.АдресФайлаВыгрузки = АдресФайлаВыгрузки;
	ДополнительныеПараметры.СертификатАбонента = СертификатАбонента;
	
	ДвоичныеДанныеФайлаВыгрузки = ПолучитьИзВременногоХранилища(АдресФайлаВыгрузки);
	
	// Подписываем выгрузку отчета.
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Подписание файла выгрузки ...'"));
	
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	
	ПараметрыПодписи = Новый Структура;
	ПараметрыПодписи.Вставить("Сертификат", СертификатАбонента);
	ПараметрыПодписи.Вставить("Данные", ДвоичныеДанныеФайлаВыгрузки);
	ПараметрыПодписи.Вставить("Сертификат", СертификатАбонента);
	ПараметрыПодписи.Вставить("ПарольДоступаКЗакрытомуКлючу", Строка(ПарольКонтейнера));
	
	Оповещение = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВМОРФПослеПодписания", 
							ЭтотОбъект, 
							ДополнительныеПараметры);
							
	МодульДокументооборотСМинобороныКлиент.ПодписатьCadesBes(
		Оповещение,
		ПараметрыПодписи);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеПодписания(Результат, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение        = ДополнительныеПараметры.ВыполняемоеОповещение;
	ИмяФайлаВыгрузки             = ДополнительныеПараметры.ИмяФайлаВыгрузки;
	АдресФайлаВыгрузки           = ДополнительныеПараметры.АдресФайлаВыгрузки;
	СертификатАбонента           = ДополнительныеПараметры.СертификатАбонента;
	СертификатыИПараметрыОбмена  = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	КаталогВыгрузки              = ДополнительныеПараметры.КаталогВыгрузки;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	Организация                  = ДополнительныеПараметры.ОрганизацияСсылка;
	ИНН                          = ДополнительныеПараметры.ИНН;
	ДатаОтправки                 = ДополнительныеПараметры.ДатаФормированияОтчета;
	ДатаОтчета                   = ДополнительныеПараметры.ДатаОтчета;
	ТипФайлаВыгрузки             = ДополнительныеПараметры.ТипФайлаВыгрузки;
	ИдентификаторФайла           = ДополнительныеПараметры.ИдентификаторФайла;
	
	Если НЕ Результат.ПодписьВыполнена Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Не удалось подписать файл данных. Описание ошибки: " + Результат.ОписаниеОшибки + "'"));
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	Если Результат.ПодписанныеДанные = Неопределено Тогда // такое может быть в веб-клиенте если пользователь не подтвердил действие расширения криптографии
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Подпись данных не создана.'"));
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли; 
	
	// создание архива
	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Создание архива с данными ...'"));
	
	ПодписьСтрокой = Base64Строка(Результат.ПодписанныеДанные);
	
	ДополнительныеПараметры.Вставить("СтрокаДанныхПодписи", ПодписьСтрокой);
	
	МодульДокументооборотСМинобороныКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиентСервер");
	ПараметрыАрхивации = МодульДокументооборотСМинобороныКлиентСервер.НовыеПараметрыАрхивации();
	ЗаполнитьЗначенияСвойств(ПараметрыАрхивации, ДополнительныеПараметры);
	Если МодульДокументооборотСМинобороныКлиентСервер.ЭтоСертификатФизическогоЛица(СертификатАбонента) Тогда
		ПараметрыАрхивации.Доверенность = СертификатыИПараметрыОбмена.ПараметрыФормированияПакета.Доверенность;
	КонецЕсли;
	
	// создаем архрив пакета на сервере
	МодульДокументооборотСМинобороныВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныВызовСервера");
	РезультатФормированияАрхива = МодульДокументооборотСМинобороныВызовСервера.СформироватьАрхивПакета(ПараметрыАрхивации);
	
	Если НЕ РезультатФормированияАрхива.Выполнено Тогда
		ТекстОшибки = НСтр("ru = 'Не удалось сформировать файл пакета по причине: " + РезультатФормированияАрхива.ОписаниеОшибки + "'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры.Вставить("АдресДанныхПакета", РезультатФормированияАрхива.АдресАрхива);
	ДополнительныеПараметры.Вставить("ДатаОтправки", ДатаОтправки);
	АдресДанныхПакета = РезультатФормированияАрхива.АдресАрхива;
	ДатаСоставления = РезультатФормированияАрхива.ДатаСоставления;
	
	КаталогВыгрузки = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(КаталогВыгрузки);
	
	// формируем имя файла пакета
	КоличествоОтправок = МодульДокументооборотСМинобороныВызовСервера.ПолучитьКоличествоОтправокМинобороныНаДату(
		Организация,
		ДатаОтправки);
	ДанныеДляФормированияИмениПакета = Новый Структура;
	ДанныеДляФормированияИмениПакета.Вставить("ИНН", ИНН);
	ДанныеДляФормированияИмениПакета.Вставить("ДатаОтправки", ДатаОтправки);
	ДанныеДляФормированияИмениПакета.Вставить("ДатаСоставления", ДатаСоставления);
	ДанныеДляФормированияИмениПакета.Вставить("ДатаОтчета", ДатаОтчета);
	ДанныеДляФормированияИмениПакета.Вставить("НомерФайла", КоличествоОтправок + 1);
	ДанныеДляФормированияИмениПакета.Вставить("ТипФайлаВыгрузки", ТипФайлаВыгрузки);
	ДанныеДляФормированияИмениПакета.Вставить("ИдентификаторФайла", ИдентификаторФайла);
	ДанныеДляФормированияИмениПакета.Вставить("Type", ДополнительныеПараметры.Type);
	ДанныеДляФормированияИмениПакета.Вставить("КПП", ДополнительныеПараметры.КПП);
	МодульДокументооборотСМинобороныКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиентСервер");
	ИмяФайлаПакета = МодульДокументооборотСМинобороныКлиентСервер.СформироватьИмяФайлаПакета(ДанныеДляФормированияИмениПакета);
		
	ДополнительныеПараметры.Вставить("ИмяФайлаПакета", ИмяФайлаПакета);
	
	ОписаниеФайла = Новый ОписаниеПередаваемогоФайла;
	ОписаниеФайла.Имя = ИмяФайлаПакета;
	ОписаниеФайла.Хранение = АдресДанныхПакета;
	ПолучаемыеФайлы = Новый Массив;
	ПолучаемыеФайлы.Добавить(ОписаниеФайла);

	ДлительнаяОтправкаКлиент.ВывестиСостояние(НСтр("ru = 'Получение файла пакета...'"));
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВМОРФПослеПолученияФайлов",
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	НачатьПолучениеФайлов(ОписаниеОповещения, ПолучаемыеФайлы, КаталогВыгрузки, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеПолученияФайлов(ПолученныеФайлы, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение        = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатыИПараметрыОбмена  = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	КаталогВыгрузки              = ДополнительныеПараметры.КаталогВыгрузки;
	АдресФайлаВыгрузки           = ДополнительныеПараметры.АдресФайлаВыгрузки;
	ОтчетСсылка                  = ДополнительныеПараметры.ОтчетСсылка;
	АдресДанныхПакета            = ДополнительныеПараметры.АдресДанныхПакета;
	
	Если ПолученныеФайлы = Неопределено Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(НСтр("ru = 'Ошибка получения файлов!"));
		Результат = РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(СертификатыИПараметрыОбмена);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
	РазделительПутиОС = ПолучитьРазделительПути();
	ПолноеИмяФайлаАрхива = СтрЗаменить(ПолученныеФайлы[0].ПолноеИмя, "/", РазделительПутиОС);
	ДополнительныеПараметры.Вставить("ПолноеИмяФайлаАрхива", ПолноеИмяФайлаАрхива);
	
	ПараметрыСохранения = Новый Структура("
		|ОтчетСсылка,
		|ИмяФайлаПакета,
		|ОрганизацияСсылка,
		|КаталогВыгрузки,
		|АдресДанныхПакета,
		|ДатаОтправки");
	ЗаполнитьЗначенияСвойств(ПараметрыСохранения, ДополнительныеПараметры);
	
	// сохраняем отправку
	МодульДокументооборотСМинобороныВызовСервера = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныВызовСервера");
	МодульДокументооборотСМинобороныВызовСервера.СохранитьПакет(ПараметрыСохранения);
	
	УдалитьИзВременногоХранилища(АдресДанныхПакета);
	УдалитьИзВременногоХранилища(АдресФайлаВыгрузки);
	
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	МодульДокументооборотСМинобороныКлиент.ЗаписьОбъектовРегламентированнойОтчетности(ОтчетСсылка, Ложь);
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВМОРФПослеВопросаОткрытьПапкуВыгрузкиЗавершение", 
		ЭтотОбъект,
		ДополнительныеПараметры);
	ПоказатьВопрос(ОписаниеОповещения, "Выгрузка завершена. Открыть каталог с выгруженным пакетом?",РежимДиалогаВопрос.ДаНет, 60);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьПакетДляОтправкиВМОРФПослеВопросаОткрытьПапкуВыгрузкиЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение        = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатыИПараметрыОбмена  = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	КаталогВыгрузки              = ДополнительныеПараметры.КаталогВыгрузки;
	АдресФайлаВыгрузки           = ДополнительныеПараметры.АдресФайлаВыгрузки;
	ПолноеИмяФайлаАрхива         = ДополнительныеПараметры.ПолноеИмяФайлаАрхива;
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		ОписаниеОповещения = Новый ОписаниеОповещения("ВыгрузитьПакетДляОтправкиВМОРФПослеОткрытияКаталогаЗавершение", 
			ЭтотОбъект,
			ДополнительныеПараметры);

		НачатьЗапускПриложения(ОписаниеОповещения, КаталогВыгрузки);
		Возврат;
	КонецЕсли; 
	
	Результат = Новый Структура("РезультатВыгрузки,
		|СертификатыИПараметрыОбмена, 
		|КаталогВыгрузки",
		ПолноеИмяФайлаАрхива,
		СертификатыИПараметрыОбмена,
		КаталогВыгрузки);
									
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
	
КонецПроцедуры

&НаКлиенте
Функция ВыгрузитьПакетДляОтправкиВМОРФПослеОткрытияКаталогаЗавершение(КодВозврата, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение        = ДополнительныеПараметры.ВыполняемоеОповещение;
	СертификатыИПараметрыОбмена  = ДополнительныеПараметры.СертификатыИПараметрыОбмена;
	КаталогВыгрузки              = ДополнительныеПараметры.КаталогВыгрузки;
	АдресФайлаВыгрузки           = ДополнительныеПараметры.АдресФайлаВыгрузки;
	ПолноеИмяФайлаАрхива         = ДополнительныеПараметры.ПолноеИмяФайлаАрхива;

	
	Результат = Новый Структура("РезультатВыгрузки,
		|СертификатыИПараметрыОбмена, 
		|КаталогВыгрузки",
		ПолноеИмяФайлаАрхива,
		СертификатыИПараметрыОбмена,
		КаталогВыгрузки);
		
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);

КонецФункции

&НаКлиенте
Функция РезультатВыгрузкиПакетаМОРФВСлучаеОшибки(СертификатыИПараметрыОбмена)
	
		Результат = Новый Структура("РезультатВыгрузки, 
									|РезультатПодписи, 
									|СертификатыИПараметрыОбмена", 
							Неопределено,
							Неопределено,
							СертификатыИПараметрыОбмена);	
		Возврат Результат;
		
КонецФункции

&НаКлиенте
Функция ТипФайлаВыгрузкиИсполнениеКонтрактовГОЗ()
	
	МодульДокументооборотСМинобороныКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиентСервер");
	Возврат МодульДокументооборотСМинобороныКлиентСервер.ТипФайлаВыгрузкиИсполнениеКонтрактовГОЗ();
	
КонецФункции

&НаКлиенте
Функция ТипФайлаВыгрузкиКассовыйПланГОЗ()
	
	МодульДокументооборотСМинобороныКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиентСервер");
	Возврат МодульДокументооборотСМинобороныКлиентСервер.ТипФайлаВыгрузкиКассовыйПланГОЗ();
	
КонецФункции

&НаКлиенте
Функция ВыгрузитьОтчетДляОтправкиВМОРФ(
	ОтчетСсылка,
	Форма
	) Экспорт
	
	ДанныеВыгрузки = "";
	РезультатВыгрузки = Форма.УниверсальнаяВыгрузка(ДанныеВыгрузки);
	Если НЕ РезультатВыгрузки Тогда
		Возврат Неопределено;
	КонецЕсли; 
	
	ДанныеВыгрузки = ДанныеВыгрузки[0];
	
	Если ДанныеВыгрузки.ТипФайлаВыгрузки <> ТипФайлаВыгрузкиКассовыйПланГОЗ()
		И ДанныеВыгрузки.ТипФайлаВыгрузки <> ТипФайлаВыгрузкиИсполнениеКонтрактовГОЗ()
		И ДанныеВыгрузки.ТипФайлаВыгрузки <> ТипФайлаВыгрузкиЦенаПродукцииГОЗ() Тогда
		Возврат Неопределено;
	КонецЕсли; 
	
	Результат = Новый Структура();
	Результат.Вставить("АдресФайлаВыгрузки", 		ДанныеВыгрузки.АдресФайлаВыгрузки);
	Результат.Вставить("ИмяФайлаВыгрузки", 			ДанныеВыгрузки.ИмяФайлаВыгрузки);
	Результат.Вставить("ДатаФормированияОтчета", 	ДанныеВыгрузки.ДатаФормированияОтчета);
	Результат.Вставить("ТипФайлаВыгрузки", 			ДанныеВыгрузки.ТипФайлаВыгрузки);
	Результат.Вставить("ДатаОтчета", 				ДанныеВыгрузки.ДатаСоставленияОтчета);
	ИдентификаторФайла = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ДанныеВыгрузки, "UID");
	Результат.Вставить("ИдентификаторФайла", 		ИдентификаторФайла);
	Результат.Вставить("ИНН", ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ДанныеВыгрузки, "ИНН", ""));
	Результат.Вставить("КПП", ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ДанныеВыгрузки, "КПП", ""));
	Результат.Вставить("Type", ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ДанныеВыгрузки, "Type", ""));
	
	Возврат Результат;
КонецФункции

&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляМОРФ(
		ВыполняемоеОповещение,
		ОрганизацияСсылка,
		ПолучитьОсновныеСвойстваПоследнейОтправки,
		ОтправкаСсылка = Неопределено,
		ОтчетСсылка = Неопределено)
	
	РезультатВыполнения = Новый Структура();
	РезультатВыполнения.Вставить("ПараметрыФормированияПакета",        Неопределено);
	РезультатВыполнения.Вставить("Крипт",                              Неопределено);
	РезультатВыполнения.Вставить("НастроенОбменВУниверсальномФормате", Неопределено);
	РезультатВыполнения.Вставить("ПравоИзмененияУчетнойЗаписи",        Неопределено);
	РезультатВыполнения.Вставить("ЭтоРегламентированныйОтчет",         Неопределено);
	
	СвойстваОбмена = СвойстваОбменаОрганизацииСМОРФНаСервере(
		ОрганизацияСсылка,
		ОтчетСсылка);
		
	РезультатВыполнения.ПравоИзмененияУчетнойЗаписи        = СвойстваОбмена.ПравоИзмененияУчетнойЗаписи;
	РезультатВыполнения.ЭтоРегламентированныйОтчет         = СвойстваОбмена.ЭтоРегламентированныйОтчет;

	ПараметрыФормированияПакета = Новый Структура();
	ПараметрыФормированияПакета.Вставить("ИспользоватьОбмен",         Ложь);
	ПараметрыФормированияПакета.Вставить("СертификатАбонента",        Неопределено);
	ПараметрыФормированияПакета.Вставить("Доверенность",              СвойстваОбмена.НастройкиОбмена.Доверенность);
	РезультатВыполнения.ПараметрыФормированияПакета = ПараметрыФормированияПакета;
	
	Если СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		РезультатВыполнения.ПараметрыФормированияПакета.ИспользоватьОбмен = СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен;
	КонецЕсли;
		
	Если НЕ СвойстваОбмена.НастройкиОбмена.ИспользоватьОбмен Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
		Возврат;
	КонецЕсли;
			
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ДополнительныеПараметры.Вставить("СвойстваОбмена",       СвойстваОбмена);
	ДополнительныеПараметры.Вставить("РезультатВыполнения",  РезультатВыполнения);
	
	Оповещение = Новый ОписаниеОповещения(
		"СертификатыИПараметрыОбменаОрганизацииДляМОРФПослеПолученияСвойствСертификатаАбонентаЗавершение",
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
	МодульДокументооборотСМинобороныКлиент.НайтиСертификатПоОтпечатку(
		Оповещение,
		СвойстваОбмена.НастройкиОбмена.СертификатАбонентаОтпечаток
		);
	
КонецПроцедуры
 
&НаКлиенте
Процедура СертификатыИПараметрыОбменаОрганизацииДляМОРФПослеПолученияСвойствСертификатаАбонентаЗавершение(Результат, ДополнительныеПараметры) Экспорт
		
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	РезультатВыполнения = ДополнительныеПараметры.РезультатВыполнения;
	
	РезультатВыполнения.ПараметрыФормированияПакета.СертификатАбонента = ?(Результат.СертификатНайден, Результат.Сертификат, Неопределено);	
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыполнения);
	
КонецПроцедуры

&НаКлиенте
Функция СертификатыГодятсяДляПодписанияОтчетностиМОРФ(Сертификаты)
	
	ТекДата = ТекущаяДатаНаСервере();
	ПризнакОшибки = Ложь;
	
	Для Каждого Сертификат Из Сертификаты Цикл
		
		СертификатКриптографии	= Сертификат.Значение;
		ПредставлениеСертификата	= Сертификат.Представление;
		Если СертификатКриптографии = Неопределено Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					 НСтр("ru = '%1: сертификат не выбран в настройках сдачи отчетности в МО РФ.'"), ПредставлениеСертификата));
			Продолжить;
		КонецЕсли;
		
		Если ТекДата < СертификатКриптографии.ДатаНачала Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = '%1: период действия сертификата еще не наступил (%2).'"), 
						ПредставлениеСертификата, Строка(СертификатКриптографии.ДатаНачала)));
		КонецЕсли;
		
		Если ТекДата > СертификатКриптографии.ДатаОкончания Тогда
			ПризнакОшибки = Истина;
			ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = '%1: период действия сертификата истек (%2).'"), 
						ПредставлениеСертификата,
						Строка(СертификатКриптографии.ДатаОкончания)));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат (НЕ ПризнакОшибки);
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсМинобороны(Организация, ФормаВладелец = Неопределено, Окно = Неопределено) Экспорт 
	
	Если ОбщегоНазначенияКлиент.ПодсистемаСуществует("РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.СдачаОтчетностиВМинобороны") Тогда
		
		МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
		МодульДокументооборотСМинобороныКлиент.ОткрытьФормуНастройкиСМинобороны(Организация, ФормаВладелец, Окно);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СвойстваОбменаОрганизацииСМОРФНаСервере(Знач ОрганизацияСсылка, 
												ОтчетСсылка = Неопределено) Экспорт
												
	МодульМодульДокументооборотСМинобороны = ОбщегоНазначения.ОбщийМодуль("ДокументооборотСМинобороны");
	Возврат МодульМодульДокументооборотСМинобороны.СвойстваОбменаОрганизации(ОрганизацияСсылка, ОтчетСсылка);
	
КонецФункции

&НаКлиенте
Процедура ЗагрузитьОтветИзМинобороныПослеПодключенияРасширенияДляРаботыСФайлами(РезультатПодключения, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение     = ДополнительныеПараметры.ВыполняемоеОповещение;
	ЗагрузкаНесколькихФайлов  = ДополнительныеПараметры.ЗагрузкаНесколькихФайлов;
	Отправка                  = ДополнительныеПараметры.Отправка;
	Суффикс                   = ДополнительныеПараметры.Суффикс;
	Префикс                   = ДополнительныеПараметры.Префикс;
	ВыгруженныеФайлы          = ДополнительныеПараметры.ВыгруженныеФайлы;
	ИНН                       = ДополнительныеПараметры.ИНН;
	Отправка                  = ДополнительныеПараметры.Отправка;

	Если РезультатПодключения Тогда
		Каталог = ОбщегоНазначенияВызовСервера.ХранилищеОбщихНастроекЗагрузить("СдачаОтчетностиВМинобороны","ПутьВыгрузки");
		Каталог = ОбщегоНазначенияКлиентСервер.ДобавитьКонечныйРазделительПути(Каталог);
		Если ЗагрузкаНесколькихФайлов Тогда
			Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
			Диалог.Заголовок = НСтр("ru = 'Укажите каталог с протоколами о сдаче отчетности в Минобороны'");
		Иначе
			// формируем фильтр на основе суффикса
			МодульДокументооборотСМинобороныКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиент");
			Идентификатор = МодульДокументооборотСМинобороныКлиент.ПолучитьРеквизитыПоСсылке(Отправка, "Идентификатор").Идентификатор;
			ИмяБезРасширения = Идентификатор;
			Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
			Диалог.Заголовок = НСтр("ru = 'Укажите файл с протоколом о сдаче отчета в Минобороны'");
			Диалог.Фильтр = СтрШаблон("Файлы ответов от Минобороны РФ (%1_*%2.zip)|%1_*%2.zip|
										|Zip-архивы (*.zip)|*.zip|
										|Все файлы (*.*)|*.*", ИНН, Суффикс);
			Диалог.ПолноеИмяФайла = СтрШаблон("%1%2.zip", ИмяБезРасширения, Суффикс);
		КонецЕсли;
		Диалог.ПроверятьСуществованиеФайла = Истина;
		Диалог.Каталог = Каталог;
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ЗагрузитьОтветИзМинобороныПослеВыбораКаталога",
			ЭтотОбъект,
			ДополнительныеПараметры); 

		Диалог.Показать(ОписаниеОповещения);
	Иначе
		Результат = РезультатЗагрузкиОтветаМОРФВСлучаеОшибки();
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ТипФайлаВыгрузкиЦенаПродукцииГОЗ()
	
	МодульДокументооборотСМинобороныКлиентСервер = ОбщегоНазначенияКлиент.ОбщийМодуль("ДокументооборотСМинобороныКлиентСервер");
	Возврат МодульДокументооборотСМинобороныКлиентСервер.ТипФайлаВыгрузкиЦенаПродукцииГОЗ();
	
КонецФункции

#КонецОбласти 

&НаКлиенте
Процедура ЗапроситьВыпискуОперацийПоРасчетуСБюджетом(ВходящийКонтекст) Экспорт
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ЗапроситьВыпискуОперацийПоРасчетуСБюджетом_ПослеОтправки", 
		ЭтотОбъект, 
		ВходящийКонтекст); 
	
	ЗапросИОН = СоздатьВыпискуОперацийПоРасчетуСБюджетом(
		ВходящийКонтекст.Организация, 
		ВходящийКонтекст.РегистрацияВНалоговомОргане, 
		ВходящийКонтекст.Год);
		
	ВходящийКонтекст.Вставить("ЗапросИОН", ЗапросИОН);
		
	ОтправкаЗапросаНаИнформационноеОбслуживание(ЗапросИОН, ВходящийКонтекст.Организация,  ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗапроситьВыпискуОперацийПоРасчетуСБюджетом_ПослеОтправки(Результат, ВходящийКонтекст) Экспорт
	
	ЗапросИОН = ВходящийКонтекст.ЗапросИОН;
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, ЗапросИОН);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СоздатьВыпискуОперацийПоРасчетуСБюджетом(Организация, РегистрацияВНалоговомОргане, Год)

	ЗапросИОН 						= Документы.ЗапросНаИнформационноеОбслуживаниеНалогоплательщика.СоздатьДокумент();
	ЗапросИОН.Дата 		 			= ТекущаяДатаСеанса();
	ЗапросИОН.Организация 			= Организация;
	ЗапросИОН.Получатель 			= РегистрацияВНалоговомОргане;
	ЗапросИОН.ДатаНачалаПериода 	= Дата(Год, 1, 1);	
	Если Год(ТекущаяДатаСеанса()) <> Год(ЗапросИОН.ДатаНачалаПериода) Тогда
		ЗапросИОН.ДатаОкончанияПериода 	= НачалоДня(КонецГода(ЗапросИОН.ДатаНачалаПериода));
	Иначе
		ЗапросИОН.ДатаОкончанияПериода 	= НачалоДня(ТекущаяДатаСеанса());
	КонецЕсли;
	ЗапросИОН.ВидУслуги 			= Перечисления.ВидыУслугПриИОН.ПредставлениеВыпискиОперацийИзКарточкиРасчетыСБюджетом;
	ЗапросИОН.ТипСверки             = Перечисления.ТипыСверокИОН.ПоОдномуКППВОднуФНС;
	ЗапросИОН.ФорматОтвета 			= Перечисления.ФорматОтветаНаЗапросИОН.XML;
	ЗапросИОН.ФорматЗапроса 		= Перечисления.ФорматыДокументооборотаСФНС.Приказ534;
	ЗапросИОН.КоличествоНалогов 	= Перечисления.КоличествоЭлементовДляСверкиИОН.Все;
	
	ЗапросИОН.Записать();
	
	Возврат ЗапросИОН.Ссылка;

КонецФункции

&НаКлиенте
Процедура ВключаемыйСертификатНажатие(Форма, ОповещениеОЗавершении, ПараметрыФормы = Неопределено) Экспорт
	
	РежимВыбора = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ПараметрыФормы, "РежимВыбора", "");
	
	Если Форма.ЗапретитьИзменение И РежимВыбора <> "МестоХранения" 
		ИЛИ Форма.ЭтоОткрытиеСВыбраннойЭЦП И Форма.ВключаемыйСертификат <> Неопределено Тогда
		
		КриптографияЭДКОКлиент.ПоказатьСертификат(Форма.ВключаемыйСертификат, Форма);
		Возврат;
		
	КонецЕсли;
	
	РезультатПроверки = ОбработкаЗаявленийАбонентаКлиентСервер.РезультатПроверкиДанныхДляОтбораСертификатов(Форма, Ложь);
		
	Если РежимВыбора = "Сертификат" 
		И РезультатПроверки.ЕстьОшибка Тогда
		
		ТекстОшибки = ОбработкаЗаявленийАбонентаКлиентСервер.ТекстОшибкиНеНайденоНиОдногоСертификата(Форма, Ложь);
		ПоказатьПредупреждение(, ТекстОшибки);
			
	Иначе
		
		ПоказатьФормуВыбораМестаХраненияСертификата(
			Форма, 
			ОповещениеОЗавершении, 
			РежимВыбора, 
			ПараметрыФормы);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьФормуВыбораМестаХраненияСертификата(Форма, ОповещениеОЗавершении, РежимВыбора, ПараметрыФормы) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("Форма", Форма);
	ДополнительныеПараметры.Вставить("ПредлагатьУстановкуРасширения", Истина);
	ДополнительныеПараметры.Вставить("РежимВыбора", РежимВыбора);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВключаемыйСертификатНажатие_Завершение", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
		
	ДополнительныеПараметры = ПараметрыОткрытияФормыВыбораСуществующегоСертификата(Форма);
	Если ПараметрыФормы <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ДополнительныеПараметры, ПараметрыФормы, Истина);
	КонецЕсли;
	ДополнительныеПараметры.Вставить("РежимВыбора", РежимВыбора);

	ОткрытьФорму(ПутьКОбъекту + ".Форма.Мастер_ВыборВключаемогоСертификата", ДополнительныеПараметры,,,,,ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ВключаемыйСертификатНажатие_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	Форма                 = ВходящийКонтекст.Форма;
	ПредлагатьУстановкуРасширения = ВходящийКонтекст.ПредлагатьУстановкуРасширения;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	Форма.РежимРаботыСКлючами = Результат.РежимРаботыСКлючами;
	
	Если ВходящийКонтекст.РежимВыбора = "Сертификат" Тогда
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(Результат);
		
		Если ЭтоПодписьСервиса(МестоХраненияКлюча) Тогда
			Форма.ИдентификаторПолученияСпискаОблачныхСертификатов = Результат.ИдентификаторПроверки;
			Форма.ОблачныеСертификатыКалуги = Результат.ОблачныеСертификатыКалуги;
			
			Форма.ТелефонМобильныйДляПаролей = Результат.ТелефонМобильныйДляПаролей;
			Форма.ТелефонМобильный  = Результат.ТелефонМобильныйДляПаролей;
			
			ОткрытьСписокВключаемыхСертификатовИлиПредупредить(Форма, ОповещениеОЗавершении);
			
		ИначеЕсли ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
			ОткрытьСписокВключаемыхСертификатовОблачнойПодписи(МестоХраненияКлюча, ОповещениеОЗавершении);
			
		ИначеЕсли Результат.ВыбранТокен Тогда
			
			ОбработкаЗаявленийАбонентаКлиент.НайтиИУстановитьСертификатСТокена(
				Форма,
				ОповещениеОЗавершении);
			
		Иначе
			
			ПолучитьИОткрытьСписокЛокальныхСертификатовКалуги(Форма, ОповещениеОЗавершении, ПредлагатьУстановкуРасширения)
			
		КонецЕсли;
	Иначе
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокВключаемыхСертификатовИлиПредупредить(Форма, ОповещениеОЗавершении)
	
	Если Форма.ОблачныеСертификатыКалуги.Количество() = 0 Тогда
		
		Текст = НСтр("ru = 'Не найдено ни одного сертификата из программы для ИНН %2 и телефона %1'");
		Текст = СтрШаблон(Текст, Форма.ТелефонМобильныйДляПаролей, Форма.ИНН);
		
		ПоказатьПредупреждение(, Текст);
		
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
		
	Иначе
		ОткрытьСписокОблачныхСертификатовКалуги(Форма, ОповещениеОЗавершении);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИОткрытьСписокЛокальныхСертификатовКалуги(Форма, ОповещениеОЗавершении, ПредлагатьУстановкуРасширения) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("Форма", Форма);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПолучитьИОткрытьСписокЛокальныхСертификатовКалуги_ПослеПолученияСертификатов", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ДополнительныеПараметрыМетода = Новый Структура;
	ДополнительныеПараметрыМетода.Вставить("ПредлагатьУстановкуВнешнейКомпоненты", ПредлагатьУстановкуРасширения);
	ДополнительныеПараметрыМетода.Вставить("ВыводитьСообщения", Ложь);
	
	Хранилище = Новый Структура("Хранилище, ЭтоЛокальноеХранилище", "MY", Истина);
	КриптографияЭДКОКлиент.ПолучитьСертификаты(ОписаниеОповещения, Хранилище, ДополнительныеПараметрыМетода);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьИОткрытьСписокЛокальныхСертификатовКалуги_ПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	Форма = ВходящийКонтекст.Форма;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	Форма.ВсеСертификаты = Результат.Сертификаты;
	НаденныеСертификаты  = СертификатыПоРеквизитамОрганизацииИВладельца(Форма, Ложь);
	Форма.СертификатыОрганизацииПоИНН.ЗагрузитьЗначения(НаденныеСертификаты);
	
	Если Форма.СертификатыОрганизацииПоИНН.Количество() = 0 Тогда
		
		Форма.СообщитьОбОтсутствииПодходящихСертификатов(Форма);
		
	Иначе
		
		Форма.ОткрытьСписокЛокальныхСертификатовКалуги(ОповещениеОЗавершении);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ЕстьКлючевыеРеквизитыСертификата(Субъект) Экспорт

	ПолеINNФЛ   = "INN";
	ПолеINNЮЛ   = "INNLE";
	ПолеСНИЛС   = "SNILS";
	ПолеИмя     = "GN";
	ПолеФамилия = "SN";
	
	ЕстьИНН = Субъект.Свойство(ПолеINNФЛ) ИЛИ Субъект.Свойство(ПолеINNЮЛ);
	
	ЕстьОшибка = 
		НЕ ЕстьИНН
		ИЛИ НЕ Субъект.Свойство(ПолеСНИЛС)
		ИЛИ НЕ Субъект.Свойство(ПолеИмя)
		ИЛИ НЕ Субъект.Свойство(ПолеФамилия);
		
	Возврат НЕ ЕстьОшибка;
		
КонецФункции

&НаКлиенте
Функция СертификатыПоРеквизитамОрганизацииИВладельца(Форма, ПроверятьЗаполненностьДанныхДляОтбора) Экспорт
	
	НайденныеСертификаты = Новый Массив;
	
	Если ПроверятьЗаполненностьДанныхДляОтбора Тогда
		
		ЕстьОшибка = ОбработкаЗаявленийАбонентаКлиентСервер.РезультатПроверкиДанныхДляОтбораСертификатов(Форма, Ложь).ЕстьОшибка;
		Если ЕстьОшибка Тогда
			Возврат НайденныеСертификаты;
		КонецЕсли;
		
	КонецЕсли;
	
	ЭтоБыстрыйСтарт = ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоФормаБыстрыйСтарт(Форма);
	
	Для каждого НайденныйСертификат Из Форма.ВсеСертификаты Цикл
		
		Субъект = НайденныйСертификат.ВладелецСтруктура;
		
		Если НЕ ЕстьКлючевыеРеквизитыСертификата(Субъект) Тогда
			Продолжить;
		КонецЕсли;
		
		Если ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоСертификатВладельца(Форма, Субъект) Тогда
			
			// Валидность
			Валиден = 
				НайденныйСертификат.ДействителенС < ТекущаяДата() 
				И НайденныйСертификат.ДействителенПо > ТекущаяДата();
				
			Если Валиден ИЛИ ЭтоБыстрыйСтарт Тогда
				НайденныйСертификат.Вставить("Валиден", Валиден);
				
				НайденныеСертификаты.Добавить(НайденныйСертификат);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат НайденныеСертификаты;
	
КонецФункции

&НаКлиенте
Функция ПараметрыОткрытияФормыВыбораСуществующегоСертификата(Форма) Экспорт
	
	// Все эти параметры должны быть в реквизитах формы Мастер_ВыборВключаемогоСертификата
	ПараметрыФормы = 
		"ТелефонМобильныйДляПаролей,
		|ИгнорироватьКонфликт,
		|CryptoProCSPУстановлен,
		|ViPNetCSPУстановлен,
		|КрасныйЦвет,
		|ИНН,
		|КомпонентаДляРаботыСКриптографиейПодключена,
		|РежимРаботыСКлючами,
		|ВладелецЭЦПТип,
		|ЭтоМультиРежим";
	
	ДополнительныеПараметры = Новый Структура(ПараметрыФормы);
	ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, Форма, ПараметрыФормы); 
	ДополнительныеПараметры.Вставить("ПараметрыФормы", ПараметрыФормы);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьСписокОблачныхСертификатовКалуги(Форма, ОповещениеОЗавершении)
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьСписокОблачныхСертификатовКалуги_ПослеВыбора", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОблачныеСертификатыКалуги", Форма.ОблачныеСертификатыКалуги);
	
	Если Форма.ВключаемыйСертификат = Неопределено Тогда
		ДополнительныеПараметры.Вставить("ОтпечатокВключаемогоСертификата", "");
	Иначе
		ДополнительныеПараметры.Вставить("ОтпечатокВключаемогоСертификата", Форма.ВключаемыйСертификат.Отпечаток);
	КонецЕсли;
		
	ОткрытьФорму(ПутьКОбъекту + ".Форма.Мастер_ВыборОблачногоВключаемогоСертификата", ДополнительныеПараметры,,,,,ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокОблачныхСертификатовКалуги_ПослеВыбора(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВыбранныйСертификат = Результат;
	КонтекстМоделиХраненияКлюча(Истина, ВыбранныйСертификат);
	
	ВходящийКонтекст.Вставить("ВыбранныйСертификат", ВыбранныйСертификат);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьСписокОблачныхСертификатовКалуги_ПослеПоиска", 
		ЭтотОбъект,
		ВходящийКонтекст);
	
	КриптографияЭДКОКлиент.НайтиСертификат(
		ОписаниеОповещения,
		ВыбранныйСертификат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокОблачныхСертификатовКалуги_ПослеПоиска(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если НЕ Результат.Выполнено Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Не удается использовать выбранный сертификат по причине: '" + Результат.ОписаниеОшибки));
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	ВыбранныйСертификат = ВходящийКонтекст.ВыбранныйСертификат;
	КонтекстМоделиХраненияКлюча(Истина, ВыбранныйСертификат);
	
	Если Результат.СертификатНайден Тогда
		
		ОткрытьСписокОблачныхСертификатовКалуги_ПослеВыбораОблачногоСертификата(Результат, ВходящийКонтекст);
		
	Иначе
		
		ДвДанные = Base64Значение(ВыбранныйСертификат.Base64);
		ИмпортироватьОблачныйСертификат(ДвДанные);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ОткрытьСписокОблачныхСертификатовКалуги_ПослеВыбораОблачногоСертификата", 
			ЭтотОбъект,
			ВходящийКонтекст);
		
		КриптографияЭДКОКлиент.НайтиСертификат(
			ОписаниеОповещения,
			ВыбранныйСертификат);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокОблачныхСертификатовКалуги_ПослеВыбораОблачногоСертификата(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если НЕ Результат.Выполнено Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Не удалось использовать выбранный сертификат по причине: '" + Результат.ОписаниеОшибки));
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВключаемыйСертификат", 			Результат.СвойстваСертификата);
	ДополнительныеПараметры.Вставить("ВключаемыйСертификатОблачный", 	Истина);
	ДополнительныеПараметры.Вставить("ЭтоСертификатДругогоУЦ", 			Ложь);
	ДополнительныеПараметры.Вставить("МестоХраненияКлюча", 				КонтекстМоделиХраненияКлюча(Истина));
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, ДополнительныеПараметры);
	
КонецПроцедуры

&НаСервере
Процедура ИмпортироватьОблачныйСертификат(ДвДанные)
	
	ХранилищеСертификатов.Добавить(ДвДанные, Перечисления.ТипХранилищаСертификатов.ПерсональныеСертификаты);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокЛокальныхСертификатовКалуги(Форма, ОповещениеОЗавершении) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("Форма", Форма);

	ОтпечаткиЗапрещенных = ОтпечаткиЗапрещенныхСертификатовСУЦ(Форма);
	ОтпечаткиВсе = ОтпечаткиЛокальныхСертификатовКалуги(Форма);
	
	Если Форма.ВключаемыйСертификат = Неопределено Тогда
		ТекущийОтпечаток = "";
	Иначе
		ТекущийОтпечаток = Форма.ВключаемыйСертификат.Отпечаток;
	КонецЕсли;
	
	ТекстПредупреждения = НСтр("ru = 'Сертификат не может быть использован, поскольку он выдан удостоверяющим центром, который не входит в список доверенных удостоверяющих центров оператора ""Калуга Астрал"".'");
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("Форма", Форма);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПослеВыбораЛокальногоСертификатаВЗаявлении", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	КриптографияЭДКОКлиент.ВыбратьСертификат(
		ОписаниеОповещения,
		Ложь,
		ТекущийОтпечаток,
		"My",
		Ложь,
		Ложь,
		ОтпечаткиВсе,
		Ложь,
		,
		ОтпечаткиЗапрещенных,
		ТекстПредупреждения);
	
КонецПроцедуры
	
&НаКлиенте
Функция ОтпечаткиЛокальныхСертификатовКалуги(Форма) Экспорт
	
	Отпечатки = Новый СписокЗначений;
		
	Для каждого СертификатОрганизации Из Форма.СертификатыОрганизацииПоИНН Цикл
		
		Сертификат = СертификатОрганизации.Значение; 
		
		Отпечаток  = Сертификат.Отпечаток;
		Отпечатки.Добавить(Отпечаток);
		
	КонецЦикла;
	
	Возврат Отпечатки;
	
КонецФункции

&НаКлиенте
Функция ПолучитьCNsПоФорме(Форма)
	
	Если ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоФормаПервичногоЗаявления(Форма)
		ИЛИ ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоФормаВторичногоЗаявления(Форма) Тогда
		
		CNsСУЦ = Форма.CNsСУЦ;
		АдресЗадания = Форма.АдресЗаданияПоПолучениюДанныхССервераКА;
		
	ИначеЕсли ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоФормаПервичногоЗаявления(Форма.ВладелецФормы)
		ИЛИ ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоФормаВторичногоЗаявления(Форма.ВладелецФормы) Тогда
		
		CNsСУЦ = Форма.ВладелецФормы.CNsСУЦ;
		АдресЗадания = Форма.ВладелецФормы.АдресЗаданияПоПолучениюДанныхССервераКА;
		
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(CNsСУЦ) И ЗначениеЗаполнено(АдресЗадания) Тогда
		CNsСУЦ = CNsСУЦПоАдресуЗадания(АдресЗадания);
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(CNsСУЦ) Тогда
		CNsСУЦ = CNsДоверенныхСтороннихУЦ();
	КонецЕсли;
	
	Возврат CNsСУЦ;
	
КонецФункции

&НаСервере
Функция CNsДоверенныхСтороннихУЦ(ДоверенныеСторонниеУЦ = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.CNsДоверенныхСтороннихУЦ(ДоверенныеСторонниеУЦ);
	
КонецФункции

&НаСервере
Функция CNsСУЦПоАдресуЗадания(АдресЗадания)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.CNsСУЦПоАдресуЗадания(АдресЗадания);

КонецФункции
	
&НаКлиенте
Функция ОтпечаткиЗапрещенныхСертификатовСУЦ(Форма)
	
	CNsСУЦ    = ПолучитьCNsПоФорме(Форма);
	Отпечатки = Новый СписокЗначений;
	
	СертификатыОрганизации = Форма.СертификатыОрганизацииПоИНН;
	
	Для каждого Сертификат Из СертификатыОрганизации Цикл
		
		СвойстваСертификата = Сертификат.Значение;
		
		Если СвойстваСертификата.Свойство("ПоставщикСтруктура")
			И СвойстваСертификата.Свойство("ПоставщикСтруктура") Тогда 
			CN = СвойстваСертификата.ПоставщикСтруктура.CN;
		Иначе
			Продолжить;
		КонецЕсли;
			
		ЭтоСертификатКалуги = ЭтоСертификатКалуги(СвойстваСертификата);
		ЭтоСертификатДоверенногоСУЦ = ЭтоДоверенныйСтороннийУЦ(CN, CNsСУЦ);

		Если НЕ ЭтоСертификатДоверенногоСУЦ И НЕ ЭтоСертификатКалуги Тогда
			Отпечатки.Добавить(СвойстваСертификата.Отпечаток);
		КонецЕсли;
	
	КонецЦикла;
	
	Возврат Отпечатки;
	
КонецФункции
	
&НаКлиенте
Процедура ПослеВыбораЛокальногоСертификатаВЗаявлении(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	Сертификат = Результат.ВыбранноеЗначение;
	КонтекстМоделиХраненияКлюча(Ложь, Сертификат);
	ВходящийКонтекст.Вставить("Сертификат", Сертификат);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьСписокЛокальныхСертификатовКалуги_ПослеПолученияДвДанных", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	ПолучитьДвоичныеДанныеСертификата(ОписаниеОповещения, Сертификат);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокЛокальныхСертификатовКалуги_ПослеПолученияДвДанных(Результат, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	Форма = ВходящийКонтекст.Форма;
	
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ДвДанныеСертификата") Тогда
		Форма.ДвДанныеСертификата = Результат.ДвДанные;
	КонецЕсли;
	
	Подходит = КриптопровайдерСертификатаПодходит(
		Результат.ДвДанные, 
		Форма.CryptoProCSPУстановлен, 
		Форма.ViPNetCSPУстановлен);
		
	Если НЕ Подходит Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Неопределено);
		Возврат;
	КонецЕсли;
	
	Сертификат = ВходящийКонтекст.Сертификат;
	
	ЭтоСертификатКалуги = ЭтоСертификатКалуги(Сертификат);

	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВключаемыйСертификат", 			Сертификат);
	ДополнительныеПараметры.Вставить("ВключаемыйСертификатОблачный", 	Ложь);
	ДополнительныеПараметры.Вставить("ЭтоСертификатДругогоУЦ", 			НЕ ЭтоСертификатКалуги);
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, ДополнительныеПараметры);

КонецПроцедуры

&НаКлиенте
Функция КриптопровайдерСертификатаПодходит(ДвДанные, CryptoProCSPУстановлен, ViPNetCSPУстановлен) Экспорт
	
	Подходит = Истина;
	
	// Временно возвращаем всегда Истина, т.к. ФНС может писать в сертификат неправильный криптопровайдер.
	Возврат Подходит;
	
	Если НЕ CryptoProCSPУстановлен И НЕ ViPNetCSPУстановлен Тогда
		// Не удалось получить. Не будем препятствовать
		Возврат Подходит;
	КонецЕсли;
	
	Сертификат = Новый Структура("Сертификат", ДвДанные);
	МассивИзОдного = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Сертификат);
	
	ИнформацияОСертификате = КриптографияЭДКОСлужебныйВызовСервера.ИзвлечьИнформациюОКриптопровайдереПоСертификату(МассивИзОдного);
	
	Если ИнформацияОСертификате.Количество() = 0 Тогда
		// Не удалось получить. Не будем препятствовать
		Возврат Подходит;
	КонецЕсли;
		
	СвойстваКриптопровайдера = ИнформацияОСертификате[0].Криптопровайдер;
	
	Если СвойстваКриптопровайдера = Неопределено Тогда
		// Не удалось получить. Не будем препятствовать
		Возврат Подходит;
	КонецЕсли;
	
	CryptoPro = КриптографияЭДКОКлиентСервер.КриптопровайдерCryptoPro().ТипКриптопровайдера;
	ViPNet    = КриптографияЭДКОКлиентСервер.КриптопровайдерViPNet().ТипКриптопровайдера;
	
	КриптопровайдерСертификата = СвойстваКриптопровайдера.ТипКриптопровайдера;
	
	Подходит = КриптопровайдерСертификата = CryptoPro И CryptoProCSPУстановлен 
		ИЛИ КриптопровайдерСертификата = ViPNet И ViPNetCSPУстановлен;
		
	Если НЕ Подходит Тогда
		
		Шаблон = НСтр("ru = 'Данный сертификат не может быть выбран, т.к. криптопровайдер (%1), на котором был издан сертификат,
			|не соответствует криптопровайдеру, установленному на компьютере (%2)'");
		
		Если CryptoProCSPУстановлен Тогда
			КриптопровайдерКомпьютера = CryptoPro;
		Иначе
			КриптопровайдерКомпьютера = ViPNet;
		КонецЕсли;
			
		Текст = СтрШаблон(Шаблон, Строка(КриптопровайдерСертификата), Строка(КриптопровайдерКомпьютера));
		
		ПоказатьПредупреждение(, Текст);
		
	КонецЕсли;
		
	Возврат Подходит;

КонецФункции

&НаСервере
Функция ЭтоДоверенныйСтороннийУЦ(CN, CNs = Неопределено)

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоДоверенныйСтороннийУЦ(CN, CNs);

КонецФункции

&НаКлиенте
Функция ЭтоСертификатКалуги(Сертификат) Экспорт
	
	ПоставщикСтруктура = Сертификат.ПоставщикСтруктура;
	Если ПоставщикСтруктура.Свойство("O") Тогда
		ЭтоСертификатКалуги = ЭтоСертификатУЦКалугиПоИмениУЦ(ПоставщикСтруктура.O);
	Иначе
		ЭтоСертификатКалуги = Ложь;
	КонецЕсли;

	Возврат ЭтоСертификатКалуги;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЭтоСертификатУЦКалугиПоИмениУЦ(ИмяУЦСертификта) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоСертификатУЦКалугиПоИмениУЦ(ИмяУЦСертификта);
	
КонецФункции

&НаКлиенте
Процедура ПолучитьДвоичныеДанныеСертификата(ВыполняемоеОповещение, СвойстваСертификата, Компонента = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ДвоичныеДанныеСертификата_ПослеЭкспортаСертификата", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(ОписаниеОповещения, СвойстваСертификата, Ложь, , Компонента);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ДвоичныеДанныеСертификата_ПослеЭкспортаСертификата(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение);
		Возврат;
	КонецЕсли;
	
	ДвДанные = Base64Значение(Результат.СтрокаBase64);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ДвДанные", ДвДанные);
	ДополнительныеПараметры.Вставить("Компонента", Результат.ДвоичныеДанные);

	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, ДополнительныеПараметры);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетПоЕГРЮЛ(ВходящийКонтекст) Экспорт
	
	ПараметрыОперации = ОбщегоНазначенияКлиентСервер.СкопироватьСтруктуру(ВходящийКонтекст);
	ПараметрыОперации.Удалить("ОповещениеОЗавершении");
	
	ОбработкаРезультата = Новый ОписаниеОповещения("ОтправитьПакетДокументовВФНСЗавершение", ЭтотОбъект, ВходящийКонтекст);
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормированиеПакетаПоЕГРЮЛ",
		ПараметрыОперации,
		,
		,
		,
		,
		ОбработкаРезультата, 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
КонецПроцедуры
	
#Область ЗаявленияВПФР

&НаКлиенте
Процедура ПредупредитьОЗаявленияхЭДОПФРКлиент() Экспорт
	
	Организации = ОрганизацииПоКоторымНужноПредупредитьОЗаявленияхЭДОПФР();
	
	Если Организации <> Неопределено Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Организации", Организации.Организации);
		ДополнительныеПараметры.Вставить("ОрганизацииПредупредитьПовторно", Организации.ОрганизацииПредупредитьПовторно);
		
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ОтправьтеЗаявлениеЭДОПФР", ДополнительныеПараметры,,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ОрганизацииПоКоторымНужноПредупредитьОЗаявленияхЭДОПФР() Экспорт
	
	// Нужны только следующие организации:
	// 1. Только если настал срок показать форму
	// 2. Только если есть учетка и текущей пользователь является ее пользователем или полноправным.
	// 3. Только если есть сотрудники
	// 4. Только если не отправлено заявление
	
	ДатаКогдаНапомнить = ХранилищеОбщихНастроек.Загрузить("ДокументооборотСКонтролирующимиОрганами_ОтправьтеЗаявлениеЭДОПФР");
	ДатаСеанса = ТекущаяДатаСеанса();
	БезНапоминания = ДатаКогдаНапомнить <> Неопределено И (ДатаКогдаНапомнить = '00010101000000'
		ИЛИ ДатаКогдаНапомнить > ДатаСеанса);
	Если БезНапоминания И ДатаСеанса >= '20200201' Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ТекущиеУчетныеЗаписиНалогоплательщика = ДокументооборотСКО.ТекущиеУчетныеЗаписиНалогоплательщика();
		
	Если ТекущиеУчетныеЗаписиНалогоплательщика.Количество() = 0 Тогда 
		Возврат Неопределено;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	Организации.Ссылка КАК Организация
		|ИЗ
		|	Справочник.Организации КАК Организации
		|ГДЕ
		|	Организации.ПометкаУдаления = ЛОЖЬ
		|	И Организации.УчетнаяЗаписьОбмена В(&ТекущиеУчетныеЗаписиНалогоплательщика)";
	
	Запрос.УстановитьПараметр("ТекущиеУчетныеЗаписиНалогоплательщика", ТекущиеУчетныеЗаписиНалогоплательщика);
	
	// Оставляем организации, доступные текущему пользователю по учетке
	Если Запрос.Выполнить().Пустой() Тогда
		Возврат Неопределено;
	Иначе
		ОрганизацииТекущегоПользователя = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Организация");
		ОрганизацииССотрудниками = Новый Массив;
		Для каждого Организация Из ОрганизацииТекущегоПользователя Цикл
			
			// Оставляем организации с сотрудниками
			ЭтоЮридическоеЛицо = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(Организация);
			Если ЭтоЮридическоеЛицо Тогда
				ОрганизацииССотрудниками.Добавить(Организация);
			Иначе
				ИПИспользуетТрудНаемныхРаботников = РегламентированнаяОтчетность.ИПИспользуетТрудНаемныхРаботников(Организация);
				Если ИПИспользуетТрудНаемныхРаботников Тогда
					ОрганизацииССотрудниками.Добавить(Организация);
				КонецЕсли;
			КонецЕсли;
		
		КонецЦикла; 
		
		// Проверяем, что отобранным организациям надо отправить заявление
		Если ОрганизацииССотрудниками.Количество() = 0 Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		
		НадоПредупредить = Новый Массив;
		НадоПредупредитьПовторно = Новый Массив;
		Для каждого Организация Из ОрганизацииССотрудниками Цикл
			Состояние = КонтекстЭДОСервер.СостояниеЭДОсПФР(Организация);
			Если Состояние.НаПодключение.НужноОтправитьЗаявление И НЕ Состояние.НаОтключение.ОрганизацияОтключена
				ИЛИ Состояние.НаПодключение.НужноОтправитьПовторноеЗаявление Тогда
				НадоПредупредить.Добавить(Организация);
			КонецЕсли;
			Если Состояние.НаПодключение.НужноОтправитьПовторноеЗаявление Тогда
				НадоПредупредитьПовторно.Добавить(Организация);
			КонецЕсли;
		КонецЦикла;
		
		Если НадоПредупредить.Количество() = 0 ИЛИ (БезНапоминания
			И НадоПредупредитьПовторно.Количество() = 0) Тогда
			
			Возврат Неопределено;
		Иначе
			
			Возврат Новый Структура("Организации, ОрганизацииПредупредитьПовторно", НадоПредупредить, НадоПредупредитьПовторно);
		КонецЕсли;
			
	КонецЕсли;
	
КонецФункции

&НаСервере
Функция РеквизитыУчетнойЗаписиОрганизации(Организация) Экспорт
	
	Свойства = "ЭтоЭлектроннаяПодписьВМоделиСервиса, Отпечаток";
	Сертификат = Мультирежим.СертификатПользователя(Организация.УчетнаяЗаписьОбмена, Свойства);
	Если Сертификат.Найден Тогда
		
		ДополнительныеПараметры = Новый Структура(Свойства);
		ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, Сертификат);
		КонтекстМоделиХраненияКлюча(Организация.УчетнаяЗаписьОбмена, ДополнительныеПараметры);
		 
		Возврат ДополнительныеПараметры;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ДвДанныеСертификатаПоОрганизации(Организация, ВыполняемоеОповещение) Экспорт
	
	РеквизитыУчетнойЗаписи = РеквизитыУчетнойЗаписиОрганизации(Организация);
	
	Если РеквизитыУчетнойЗаписи = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Неопределено);
		Возврат;
	КонецЕсли;
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(РеквизитыУчетнойЗаписи);
	
	Сертификат = Новый Структура();
	Сертификат.Вставить("Отпечаток", РеквизитыУчетнойЗаписи.Отпечаток);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, Сертификат);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Отпечаток", РеквизитыУчетнойЗаписи.Отпечаток);
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ДополнительныеПараметры);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ДвДанныеСертификатаПоОрганизации_ПослеПоискаСертификатаПоОтпечатку", 
		ЭтотОбъект,
		ДополнительныеПараметры);
	
	КриптографияЭДКОКлиент.НайтиСертификат(
		ОписаниеОповещения,
		Сертификат,
		Ложь,
		Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ДвДанныеСертификатаПоОрганизации_ПослеПоискаСертификатаПоОтпечатку(Результат, ВходящийКонтекст) Экспорт
	
	Найден = Ложь;
	Если Результат.Выполнено И Результат.СертификатНайден Тогда
		
		Если ТипЗнч(Результат.СвойстваСертификата) = Тип("ФиксированнаяСтруктура") Тогда
			СвойстваСертификата = Новый Структура(Результат.СвойстваСертификата);
		Иначе
			СвойстваСертификата = Результат.СвойстваСертификата;
		КонецЕсли;
		
		МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, СвойстваСертификата);
		ВходящийКонтекст.Вставить("СвойстваСертификата", СвойстваСертификата);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ДвДанныеСертификатаПоОрганизации_ПослеПолученияBase64", 
			ЭтотОбъект, 
			ВходящийКонтекст);
			
		КриптографияЭДКОКлиент.ЭкспортироватьСертификатВBase64(ОписаниеОповещения, СвойстваСертификата, Ложь);
		
	Иначе
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Неопределено);
		
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ДвДанныеСертификатаПоОрганизации_ПослеПолученияBase64(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат.Выполнено Тогда
		
		ДвДанныеСертификата = Base64Значение(Результат.СтрокаBase64);
		СвойстваСертификата = ВходящийКонтекст.СвойстваСертификата;
		СвойстваСертификата.Вставить("ДвДанныеСертификата", ДвДанныеСертификата);
		
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, СвойстваСертификата);
		
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, Неопределено);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПечатьЗаявленияПоЭлДокументооборотуСПФР(Заявление) Экспорт
	
	Вид = ПолучитьРеквизитНаСервере(Заявление, "Вид");
	Если Вид = ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.ПустаяСсылка") Тогда
		
		Текст = НСтр("ru = 'Для печати заявления заполните его вид'");
		ПоказатьПредупреждение(, Текст);
		
	Иначе
		
		ПечатнаяФормаЗаявления = ТабличныйДокументЗаявленияПоЭлДокументооборотуСПФР(Заявление);
		НапечататьДокумент(ПечатнаяФормаЗаявления, Строка(Вид));
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПечатьУведомленияОПредоставленииПолномочийПредставителю(Уведомление) Экспорт
	
		ПечатнаяФормаУведомления = ТабличныйДокументУведомлениеОПредоставленииПолномочийПредставителю(Уведомление);
		НапечататьДокумент(ПечатнаяФормаУведомления, "Уведомление о предоставлении полномочий представителю");
	
КонецПроцедуры
	
	&НаКлиенте
Процедура ПечатьУведомленияОПрекращенииПолномочийПредставителя(Уведомление) Экспорт
	
		ПечатнаяФормаУведомления = ТабличныйДокументУведомлениеОПрекращенииПолномочийПредставителя(Уведомление);
		НапечататьДокумент(ПечатнаяФормаУведомления, "Уведомление о прекращении полномочий представителя");
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ТабличныйДокументЗаявленияПоЭлДокументооборотуСПФР(Заявление)
	
	Возврат Документы.ЗаявленияПоЭлДокументооборотуСПФР.ПечатнаяФорма(Заявление);

КонецФункции

&НаСервереБезКонтекста
Функция ТабличныйДокументУведомлениеОПредоставленииПолномочийПредставителю(Уведомление)
	
	Возврат Документы.УведомлениеОПредоставленииПолномочийПредставителю.ПечатнаяФорма(Уведомление);
	
КонецФункции

&НаСервереБезКонтекста
Функция ТабличныйДокументУведомлениеОПрекращенииПолномочийПредставителя(Уведомление)
	
	Возврат Документы.УведомлениеОПрекращенииПолномочийПредставителя.ПечатнаяФорма(Уведомление);
	
КонецФункции

&НаКлиенте
Процедура НачатьОтправкуЗаявленияИзПанельПФР(Форма) Экспорт
		
	Если Форма.НужноОтправитьЗаявлениеВПФРИзМастера(Форма.НаПодключение) Тогда
		ИзначальныйВидЗаявленияПФР = Форма.НаПодключение;
	ИначеЕсли Форма.НужноОтправитьЗаявлениеВПФРИзМастера(Форма.НаСертификат) Тогда
		ИзначальныйВидЗаявленияПФР = Форма.НаСертификат;
	КонецЕсли;
	
	Организация = ПолучитьРеквизитНаСервере(Форма.ТекущееЗаявлениеПо1Сотчетности, "Организация");
	
	ПараметрыФормы = ПараметрыОткрытияФормыПредложенияОтправитьЗаявлениеВПФР();
	ПараметрыФормы.Организация = Организация;
	ПараметрыФормы.ИзначальныйВидЗаявленияПФР = ИзначальныйВидЗаявленияПФР;
	ПараметрыФормы.СостояниеЭДОсПФР = Форма.СостояниеЭДОсПФР;
	ПараметрыФормы.ЭтоПредупреждениеИзМастера = Форма.ЭтоПредупреждениеИзМастера;
	ПараметрыФормы.ЭтоПредупреждениеИзПанели = Форма.ЭтоПредупреждениеИзПанели;
	ПараметрыФормы.ТекущееЗаявлениеПо1Сотчетности = Форма.ТекущееЗаявлениеПо1Сотчетности;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"НачатьОтправкуЗаявленияИзПанельПФР_Завершение", 
		ЭтотОбъект, 
		Форма);
	
	ОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(ПараметрыФормы, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура НачатьОтправкуЗаявленияИзПанельПФР_Завершение(Результат, Форма) Экспорт
	
	Форма.ИзменитьОформлениеПанелиПФРВМастере();
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЗаявлениеВПФР(Форма, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь;
	Форма.СоздатьЗаявленияВПФРЕслиНеСозданы();
	ДокументооборотСКОКлиент.СоздатьНовоеИлиОткрытьЗаявлениеВПФР(,,Форма.ЗаявлениеВПФР, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьЗаявлениВПФРИзПредупреждения(Форма, Принудительно = Ложь) Экспорт
	
	Форма.ДействияПередОтправкойЗаявленияВПФР();
	
	Если СтрНайти(Форма.ИмяФормы, "МастерФормированияЗаявкиНаИзменениеПараметровПодключения") > 0 Тогда
		ПропуститьОтправку = Ложь;
	Иначе
		ПропуститьОтправку = Форма.Элементы.ЗаявленияВПФР_ОтправитьЗаявление.Заголовок = Форма.ЗаголовокКнопкиПоЗавершению И НЕ Принудительно;
	КонецЕсли;
	
	Если ПропуститьОтправку Тогда
		Форма.ВыполнитьСледующееДействиеПослеОтправкиЗаявленияВПФР(Истина);
	Иначе
		
		ЗаполненоКорректно = ЗаявлениеВПФРЗаполненоКорректно(Форма.ОтправляемоеЗаявлениеПФР);
		Если ЗаполненоКорректно Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ЗаявленияВПФР_ОтправитьЗаявлениеСейчас_Завершение", 
				ЭтотОбъект,
				Форма);
				
			ОтправкаРегламентированногоОтчетаВПФР(Форма.ОтправляемоеЗаявлениеПФР, ОписаниеОповещения);
				
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьЗаявлениеВПФРПозже(Форма, ПродолжитьДействиеПрерванноеПоказомПредупреждения = Неопределено) Экспорт
	
	Форма.ВыполнитьСледующееДействиеПослеОтправкиЗаявленияВПФР(ПродолжитьДействиеПрерванноеПоказомПредупреждения);
	
КонецПроцедуры

&НаСервере
Функция ЗаявлениеВПФРЗаполненоКорректно(ОтправляемоеЗаявление) Экспорт

	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЗаявлениеВПФРЗаполненоКорректно(ОтправляемоеЗаявление);
	
КонецФункции

&НаКлиенте
Процедура ЗаявленияВПФР_ОтправитьЗаявлениеСейчас_Завершение(Результат, Форма) Экспорт
	
	ОповеститьОбОтправкеЗаявленияВПФР(Форма.ОтправляемоеЗаявлениеПФР, Форма.Организация);
	
	Форма.ПрограммноеЗакрытие = Истина;
	СтатусОтправки = ПолучитьСтатусОтправкиОбъекта(Форма.ОтправляемоеЗаявлениеПФР);
	
	Если СтатусОтправки = ПредопределенноеЗначение("Перечисление.СтатусыОтправки.Отправлен") Тогда
		Форма.ВыполнитьСледующееДействиеПослеОтправкиЗаявленияВПФР();
	Иначе
		Форма.ПоказатьСтраницуОшибкиОтправкиВПФР();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНеотправленноеЗаявлениеВПФР(Форма) Экспорт
	
	ПоказатьЗначение(,Форма.ОтправляемоеЗаявлениеПФР);
		
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокЗаявленийВПФР(Организация = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	Если ЗначениеЗаполнено(Организация) Тогда
		ДополнительныеПараметры.Вставить("Организация", Организация);
	КонецЕсли;
	 
	ОткрытьФорму("Документ.ЗаявленияПоЭлДокументооборотуСПФР.ФормаСписка", ДополнительныеПараметры,,,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокУПУП(Организация = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	Если ЗначениеЗаполнено(Организация) Тогда
		ДополнительныеПараметры.Вставить("Организация", Организация);
	КонецЕсли;
	
	ОткрытьФорму("Документ.УведомлениеОПредоставленииПолномочийПредставителю.ФормаСписка", ДополнительныеПараметры,,,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокУПРУП(Организация = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	Если ЗначениеЗаполнено(Организация) Тогда
		ДополнительныеПараметры.Вставить("Организация", Организация);
	КонецЕсли;
	
	ОткрытьФорму("Документ.УведомлениеОПрекращенииПолномочийПредставителя.ФормаСписка", ДополнительныеПараметры,,,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьЗаявлениеВПФРИзМеню(Организация, Основание = Неопределено) Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация", Организация);
	ДополнительныеПараметры.Вставить("Основание", Основание);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СоздатьЗаявлениеВПФРИзМеню_ПослеВыбораВида", 
		ЭтотОбъект);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПомощникСозданияЗаявленияПФР", ДополнительныеПараметры,,,,,ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьЗаявлениеВПФРИзМеню_ПослеВыбораВида(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Заявление = СоздатьЗаявлениеВПФР(
		Результат.Вид, 
		Результат.Организация, 
		, 
		Результат.Основание);
		
	ПоказатьЗначение(,Заявление);
			
КонецПроцедуры

&НаСервере
Функция СоздатьЗаявлениеВПФР(
		Вид, 
		Организация, 
		СостояниеЭДОсПФР = Неопределено, 
		Основание = Неопределено) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СоздатьЗаявлениеВПФР(
		Вид, 
		Организация, 
		СостояниеЭДОсПФР, 
		Основание);
	
КонецФункции

&НаКлиенте
Процедура НайтиЗаявлениеПФРВФорме1СОтчетность(Ссылка, Организация) Экспорт
	
	ПараметрыОткрытия = Новый Структура();
	ПараметрыОткрытия.Вставить("Раздел", ПредопределенноеЗначение("Перечисление.СтраницыЖурналаОтчетность.Уведомления"));
	
	ОткрытьФорму("ОбщаяФорма.РегламентированнаяОтчетность", ПараметрыОткрытия);
	
	Оповестить("Открытие формы 1С-Отчетность", ПараметрыОткрытия);
	
	Если ЗначениеЗаполнено(Ссылка) Тогда
		ПараметрыОповещения = Новый Структура(); 
		ПараметрыОповещения.Вставить("Ссылка", Ссылка);
		ПараметрыОповещения.Вставить("Организация", Организация);
		Оповестить("Позиционирование", ПараметрыОповещения, Ссылка);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСертификатОтправляемыйВПФР(Форма, СтандартнаяОбработка) Экспорт
		
	СтандартнаяОбработка = Ложь;
	
	ДвДанныеСертификата = Форма.СостояниеЭДОсПФР.НаСертификат.ДвДанныеСертификата;
	Адрес = ПоместитьВоВременноеХранилище(ДвДанныеСертификата);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Адрес", Адрес);

	КриптографияЭДКОКлиент.ПоказатьСертификат(ДополнительныеПараметры, Форма);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеВПФР_СсылкаНаЗаявлениеОбработкаНавигационнойСсылки(
		Элемент, 
		НавигационнаяСсылкаФорматированнойСтроки, 
		СтандартнаяОбработка,
		ЗаявлениеВПФР) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ДокументооборотСКОКлиент.СоздатьНовоеИлиОткрытьЗаявлениеВПФР(,, ЗаявлениеВПФР, Истина);
	
КонецПроцедуры

&НаСервере
Функция СостояниеЭДОсПФР(Организация) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СостояниеЭДОсПФР(Организация);
	
КонецФункции

&НаКлиенте
Функция ПараметрыОткрытияФормыПредложенияОтправитьЗаявлениеВПФР() Экспорт
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Организация", Неопределено); // Обязательное
	ДополнительныеПараметры.Вставить("ИзначальныйВидЗаявленияПФР", ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.НаПодключение")); // Обязательное
	ДополнительныеПараметры.Вставить("ЭтоПредупреждениеИзНастроек", Ложь);
	ДополнительныеПараметры.Вставить("ЭтоПредупреждениеИзОтправки", Ложь);
	ДополнительныеПараметры.Вставить("ЭтоПредупреждениеИзМастера", Ложь);
	ДополнительныеПараметры.Вставить("ЭтоПредупреждениеИзПанели", Ложь);
	ДополнительныеПараметры.Вставить("Отчет", Неопределено); // Обязательное только при отправке для определния текста надписей
	ДополнительныеПараметры.Вставить("ЗаголовокКнопкиПоЗавершению", НСтр("ru = 'Сохранить и закрыть'")); // Обязательное
	ДополнительныеПараметры.Вставить("СостояниеЭДОсПФР", Неопределено); // Обязательное
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", Неопределено);
	ДополнительныеПараметры.Вставить("ТекущееЗаявлениеПо1Сотчетности", Неопределено);

	Возврат ДополнительныеПараметры; 
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(Параметры, ОповещениеОЗавершении = Неопределено) Экспорт
	
	ДопПараметры = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(Параметры);
	ДопПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьФормуПредложенияОтправитьЗаявлениеВПФР_Завершение", 
		ЭтотОбъект,
		ДопПараметры);
		
	// Для предотвращения ошибки 
	// Ошибка передачи данных между клиентом и сервером. Значение недопустимого типа.
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредложениеОтправитьЗаявлениеВПФР", Параметры,,,,,ОписаниеОповещения, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПредложенияОтправитьЗаявлениеВПФР_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	Если ВходящийКонтекст.ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Результат);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиОтправки(Состояние, Организация, ОповещениеОЗавершении = Неопределено, Отчет = Неопределено) Экспорт
	
	ЭтоОтправкаОтчета = ЗначениеЗаполнено(Отчет);
	ПараметрыОткрытия = ПараметрыОткрытияПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиПриОтправке(Состояние, ЭтоОтправкаОтчета);
 
	ИзначальныйВидЗаявленияПФР = ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.НаПодключение");
	
	ПараметрыФормы = ПараметрыОткрытияФормыПредложенияОтправитьЗаявлениеВПФР();
	ПараметрыФормы.Организация = Организация;
	ПараметрыФормы.ИзначальныйВидЗаявленияПФР = ИзначальныйВидЗаявленияПФР;
	ПараметрыФормы.СостояниеЭДОсПФР = Состояние;
	
	Если Отчет = Неопределено Тогда
		ПараметрыФормы.ЭтоПредупреждениеИзНастроек = Истина;
	Иначе
		
		ПараметрыФормы.ЭтоПредупреждениеИзОтправки = Истина;
		ПараметрыФормы.Отчет = Отчет;
		
	КонецЕсли;
	
	ОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(ПараметрыФормы, ОповещениеОЗавершении);
	
КонецПроцедуры

&НаКлиенте
Функция ПараметрыОткрытияПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиПриОтправке(Состояние, ЭтоОтправкаОтчета) // Из настроек или при отправке
	
	ЭтоОткрытиеИзНастроек = НЕ ЭтоОтправкаОтчета;
	НаправлениеПодключено = Состояние.НаправлениеПодключено;
	ОрганизацияОтключена  = ЭтоОтправкаОтчета И Состояние.НаОтключение.ОрганизацияОтключена;
	
	// Отправить заявление на подключение
	НужноОтправитьЗаявлениеПриОтправке = 
		НаправлениеПодключено 
		И ЭтоОтправкаОтчета 
		И (Состояние.НаПодключение.НужноОтправитьЗаявление ИЛИ ОрганизацияОтключена);
		
	НужноОтправитьЗаявлениеИзНастроек = 
		НаправлениеПодключено 
		И ЭтоОткрытиеИзНастроек 
		И Состояние.НаПодключение.НужноОтправитьЗаявление;
	
	НужноОтправитьЗаявление = 
		НужноОтправитьЗаявлениеПриОтправке 
		ИЛИ НужноОтправитьЗаявлениеИзНастроек;
		
	// Предупредить о неодобренном
	ПредупредитьОНеодобренномПриОтправке = 
		Состояние.Неодобренное.Предупредить И ЭтоОтправкаОтчета 
		И НЕ Состояние.Неодобренное.Вид = ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.НаОтключение");
		
	ПредупредитьОНеодобренномИзНастроек = 
		Состояние.Неодобренное.Предупредить 
		И ЭтоОткрытиеИзНастроек;
	
	ПредупредитьОНеодобренном = 
		ПредупредитьОНеодобренномПриОтправке 
		ИЛИ ПредупредитьОНеодобренномИзНастроек;
		
	// Собираем все вместе
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НужноОтправитьЗаявление", НужноОтправитьЗаявление);
	ДополнительныеПараметры.Вставить("НужноОтправитьСертификат", Ложь);
	ДополнительныеПараметры.Вставить("ПредупредитьОНеодобренном", ПредупредитьОНеодобренном);
	
	Возврат ДополнительныеПараметры;
	
КонецФункции

&НаКлиенте
Функция НужноОткрытьФормуПредложенияОтправитьЗаявлениеВПФР(Состояние, ЭтоОтправкаОтчета) // Из настроек или при отправке
	
	ПараметрыОткрытия = ПараметрыОткрытияПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиПриОтправке(Состояние, ЭтоОтправкаОтчета);
	
	Возврат ПараметрыОткрытия.НужноОтправитьЗаявление
		ИЛИ ПараметрыОткрытия.ПредупредитьОНеодобренном;
	
КонецФункции

&НаКлиенте
Функция УстановитьЗаголовокОбменаСПФР(Форма, Состояние, ЦветУспеха = Неопределено, ЦветОшибки = Неопределено) Экспорт // В настройках
	
	ПараметрыОткрытия = ПараметрыОткрытияПредложенияОтправитьЗаявлениеВПФРИзНастроекИлиПриОтправке(Состояние, Ложь);

	Подсказка = "";
	Если Состояние.НаОтключение.ОрганизацияОтключена Тогда
		
		Оранжевый = Новый Цвет(255, 128, 0);
		ЗаголовокСсылки = Новый ФорматированнаяСтрока(НСтр("ru = 'Подключено (ЭТК откл.)'"),,Оранжевый,,"ПФР");
		Подсказка = НСтр("ru = 'Использование электронных трудовых книжек отключено.'");
		
	ИначеЕсли ПараметрыОткрытия.НужноОтправитьЗаявление Тогда
			
		Красный = ?(ЦветОшибки = Неопределено, Новый Цвет(225, 40, 40), ЦветОшибки);
		ЗаголовокСсылки = Новый ФорматированнаяСтрока(НСтр("ru = 'Не отправлено заявление в СФР'"),,Красный,,"ПФР");
		Подсказка = ДокументооборотСКОКлиентСервер.ПодсказкаКЗаявлениюНаПодключениеПФР();
		
	ИначеЕсли ПараметрыОткрытия.ПредупредитьОНеодобренном Тогда
		
		Вид       = Состояние.Неодобренное.Вид;
		Оранжевый = Новый Цвет(255, 128, 0);
		
		Если Вид = ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.НаПодключение") Тогда
			ЗаголовокСсылки = Новый ФорматированнаяСтрока(НСтр("ru = 'Рассматривается заявление на подключение'"),,Оранжевый,,"ПФР");
			Подсказка = ДокументооборотСКОКлиентСервер.ПодсказкаКЗаявлениюНаПодключениеПФР();
		ИначеЕсли Вид = ПредопределенноеЗначение("Перечисление.ВидыЗаявленийНаЭДОВПФР.НаОтключение") Тогда
			ЗаголовокСсылки = Новый ФорматированнаяСтрока(НСтр("ru = 'Рассматривается заявление на отключение'"),,Оранжевый,,"ПФР");
			Подсказка = ДокументооборотСКОКлиентСервер.ПодсказкаКЗаявлениюНаОтключениеПФР();
		КонецЕсли;
		
	Иначе
		
		Зеленый = ?(ЦветУспеха = Неопределено, Новый Цвет(100, 176, 16), ЦветУспеха);
		ЗаголовокСсылки = Новый ФорматированнаяСтрока(НСтр("ru = 'Подключено'"),,Зеленый,,"ПФР");
		
	КонецЕсли;
	
	ЭлементыФормы = Форма.Элементы;
	ЭлементыФормы.ОбменСПФР.Заголовок = ЗаголовокСсылки;
	
	Если Подсказка = "" Тогда
		ЭлементыФормы.ОбменСПФР.ОтображениеПодсказки = ОтображениеПодсказки.Нет;
	Иначе
		ЭлементыФормы.ОбменСПФР.РасширеннаяПодсказка.Заголовок = Подсказка;
		ЭлементыФормы.ОбменСПФР.ОтображениеПодсказки = ОтображениеПодсказки.Кнопка;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура ОповеститьОбОтправкеЗаявленияВПФР(Ссылка, Организация) Экспорт
	
	// Перерисовка статуса отправки в форме Отчетность
	ПараметрыОповещения = Новый Структура(); 
	ПараметрыОповещения.Вставить("Ссылка", Ссылка);
	ПараметрыОповещения.Вставить("Организация", Организация);
	
	Оповестить(РегламентированнаяОтчетностьКлиент.ИмяСобытияЗавершениеОтправки(), ПараметрыОповещения, Ссылка);
	
	ОповеститьОбИзменении(Ссылка);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_СпроситьПроСохранениеЗаявленияПФР(Форма) Экспорт
	
	Если Форма.Элементы.ЗаявленияВПФР_ОтправитьЗаявление.Заголовок = Форма.ЗаголовокКнопкиПоЗавершению Тогда
		
		ТекстВопроса = НСтр("ru = 'Данные были изменены. Сохранить изменения?'");
		
		Кнопки = Новый СписокЗначений;
		Кнопки.Добавить(НСтр("ru = 'Сохранить'"));
		Кнопки.Добавить(НСтр("ru = 'Закрыть без сохранения'"));
		
		КнопкаПоУмолчанию = НСтр("ru = 'Сохранить'");
		
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"СпроситьПроСохранениеЗаявленияПФР_Завершение", 
		ЭтотОбъект,
		Форма); 
	
	ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, Кнопки,,КнопкаПоУмолчанию);
	
КонецПроцедуры

&НаКлиенте
Процедура СпроситьПроСохранениеЗаявленияПФР_Завершение(Ответ, Форма) Экспорт
	
	Если Ответ = НСтр("ru = 'Сохранить'") Тогда
		
		Форма.СохранитьНастройкиСостоянияЭДОСПФР();
		Форма.ПередЗакрытием_Завершение();
		
	ИначеЕсли Ответ = НСтр("ru = 'Закрыть без сохранения'") 
		ИЛИ Ответ = НСтр("ru = 'Не отправлять'") Тогда
		
		Форма.ПередЗакрытием_Завершение();
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьСостояниеЭДОсПФРПриЗаписиТранспортногоСообщения(Ссылка,
		ОбновляемоеТранспортноеСообщение = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ОбновитьСостояниеЭДОсПФРПриЗаписиТранспортногоСообщения(Ссылка, ОбновляемоеТранспортноеСообщение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОповеститьОбОтправкеУведомленияОПредоставленииПолномочийПредставителю(Ссылка, Организация) Экспорт
	
	// Перерисовка статуса отправки в форме Отчетность
	ПараметрыОповещения = Новый Структура(); 
	ПараметрыОповещения.Вставить("Ссылка", Ссылка);
	ПараметрыОповещения.Вставить("Организация", Организация);
	
	Оповестить(РегламентированнаяОтчетностьКлиент.ИмяСобытияЗавершениеОтправки(), ПараметрыОповещения, Ссылка);
	
	ОповеститьОбИзменении(Ссылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОповеститьОбОтправкеУведомленияОПрекращенииПолномочийПредставителя(Ссылка, Организация) Экспорт
	
	// Перерисовка статуса отправки в форме Отчетность
	ПараметрыОповещения = Новый Структура(); 
	ПараметрыОповещения.Вставить("Ссылка", Ссылка);
	ПараметрыОповещения.Вставить("Организация", Организация);
	
	Оповестить(РегламентированнаяОтчетностьКлиент.ИмяСобытияЗавершениеОтправки(), ПараметрыОповещения, Ссылка);
	
	ОповеститьОбИзменении(Ссылка);
	
КонецПроцедуры

#КонецОбласти

#Область СПАРК

&НаКлиенте
Процедура ПодтверждениеОтправкиОтчета(Форма, ОписаниеОповещения, Ссылка, ПоказатьПринудительно = Ложь) Экспорт
	
	НужноПодтверждение = Ложь;
	Если Форма = Неопределено ИЛИ ПоказатьПринудительно Тогда
		НужноПодтверждение =
			ДокументооборотСКОКлиент.ЗапоминаниеОшибок(,, Истина, "МЧДСкрытьДлительнуюОтправку") <> Истина;
		
	Иначе
		
		КраткоеИмяФормы = ДлительнаяОтправкаКлиентСервер.ИмяФормыПоПолномуИмени(Форма.ИмяФормы);
		
		Это1СОтчетность = ДлительнаяОтправкаКлиент.ЭтоФормаОтправкиОтчетности(КраткоеИмяФормы, Форма.ИмяФормы);
		ЭтоФормаНастройки    = КраткоеИмяФормы = "МастерФормированияЗаявкиНаПодключениеНастройкаПрограммыУпрощенный";
		ЭтоВторичныйМастер   = КраткоеИмяФормы = "МастерФормированияЗаявкиНаИзменениеПараметровПодключения";
		ЭтоГрупповаяОтправка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(Форма, Истина);
		// отправка списка уведомлений прослеживаемости товаров
		ЭтоОтправкаБезПодтверждения =
			(ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации("БРО.ПодтверждатьОтправку") = Ложь);
		
		НужноПодтверждение = 
			НЕ (Это1СОтчетность
			ИЛИ ЭтоФормаНастройки
			ИЛИ ЭтоВторичныйМастер
			ИЛИ ЭтоГрупповаяОтправка
			ИЛИ ЭтоОтправкаБезПодтверждения);
			
		Если НужноПодтверждение Тогда
			ЭтоГрупповаяОтправка = ДлительнаяОтправкаКлиентСервер.ЭтоФормаГрупповойОтправки(, Истина);
			Если ЭтоГрупповаяОтправка Тогда
				 НужноПодтверждение = Ложь;
			КонецЕсли;
		КонецЕсли;
			
	КонецЕсли;
		
	Если НужноПодтверждение Тогда
		
		Если ЭтоНужныйВидОтчетаСПАРК(Ссылка) Тогда
		
			ОтправкаОтчетностиВИнтерфакс = "РегламентированнаяОтчетность.ЭлектронныйДокументооборотСКонтролирующимиОрганами.ОтправкаОтчетностиВИнтерфакс";
			Если ОбщегоНазначенияКлиент.ПодсистемаСуществует(ОтправкаОтчетностиВИнтерфакс) Тогда
				МодульОтправкаРегОтчетовВСПАРККлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("ОтправкаРегОтчетовВСПАРККлиент");
				МодульОтправкаРегОтчетовВСПАРККлиент.ОткрытьФормуПодтверждениеОтправкиОтчетаСПАРК(Ссылка, ОписаниеОповещения);
			КонецЕсли;
		Иначе
			ПараметрыФормы = Новый Структура("СсылкаНаОтчет", Ссылка);
			
			ОткрытьФорму(
				ПутьКОбъекту + ".Форма.ПодтверждениеОтправкиОтчета", 
				ПараметрыФормы,
				,
				,
				,
				,
				ОписаниеОповещения, 
				РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		КонецЕсли;
	Иначе
		ВыполнитьОбработкуОповещения(ОписаниеОповещения, КодВозвратаДиалога.ОК);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ЭтоНужныйВидОтчетаСПАРК(СсылканаОтчет) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭтоНужныйВидОтчетаСПАРК(СсылканаОтчет);
	
КонецФункции

#КонецОбласти

#Область ПредупреждениеОСертификатах


#КонецОбласти

#Область ПроверкаИмениФайлаНаСимволы

&НаКлиенте
Функция ПерекодироватьСтроку(КонвертируемаяСтрока, КодировкаКонвертации) Экспорт
	
	ДанныеСтроки = ПолучитьДвоичныеДанныеИзСтроки(КонвертируемаяСтрока, КодировкаКонвертации);
	Результат = ПолучитьСтрокуИзДвоичныхДанных(ДанныеСтроки, КодировкаКонвертации);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПроверитьНеДопустимыеСимволыИмениФайла(ПроверяемаяСтрока, КодировкаПроверки = "UTF-8") Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Пройдена", Истина);
	Результат.Вставить("НовоеИмя", "");
	
	СтрокаАнализа = ПроверяемаяСтрока;
	
	Если КодировкаПроверки <> "UTF-8" Тогда
		СтрокаАнализа = ПерекодироватьСтроку(СтрокаАнализа, КодировкаПроверки);
	КонецЕсли;	
	
	СимволыЗапрещенные = "\|/:*?""<>+";
	СимволыОкончания = " .";
	ДлинаСтроки = СтрДлина(СтрокаАнализа);
	
	Для Счетчик = 1 По ДлинаСтроки Цикл
		ТекущийСимвол = Сред(СтрокаАнализа, Счетчик, 1);
		Если Счетчик = ДлинаСтроки И СтрНайти(СимволыОкончания, ТекущийСимвол) > 0 Тогда
			ТекущийСимвол = "_";
			Результат.Пройдена = Ложь;
		ИначеЕсли СтрНайти(СимволыЗапрещенные, ТекущийСимвол) > 0 Тогда
			ТекущийСимвол = "_";
			Результат.Пройдена = Ложь;
 		КонецЕсли;
		Результат.НовоеИмя = Результат.НовоеИмя + ТекущийСимвол;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область ОтказОтЭДО

&НаКлиенте
Процедура Подключаемый_СпроситьПроЭДОПередЗакрытием(Форма) Экспорт
	
	ДокументЗаявление  = Форма.ДокументЗаявление;
	
	Если ТипЗнч(ДокументЗаявление) = Тип("ДанныеФормыСтруктура") Тогда
		ДокументЗаявление = ДокументЗаявление.Ссылка;
	КонецЕсли;
		
	РеквизитыЗаявления = ЗначенияРеквизитовОбъекта(ДокументЗаявление, "ПодключитьЭДО, ПереиздатьСертификатЭДО");
	
	Если РеквизитыЗаявления.ПодключитьЭДО Тогда
		ТекстПредупреждения = НСтр("ru = 'Закрыть мастер без подключения к сервису 1С-ЭДО?'");
	ИначеЕсли РеквизитыЗаявления.ПереиздатьСертификатЭДО Тогда
		ТекстПредупреждения = НСтр("ru = 'Закрыть мастер без обновления сертификата 1С-ЭДО?'");
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПослеПолученияОтветаПроЭДО", 
		ЭтотОбъект,
		Форма);
		
	Кнопки = Новый СписокЗначений;
	Кнопки.Добавить(НСтр("ru = 'Да и предложить позже'"));
	Кнопки.Добавить(НСтр("ru = 'Да и больше не предлагать'"));
	Кнопки.Добавить(НСтр("ru = 'Нет'"));
	
	КнопкаПоУмолчанию = НСтр("ru = 'Да и предложить позже'");
	
	ПоказатьВопрос(
		ОписаниеОповещения,
		ТекстПредупреждения,
		Кнопки);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПослеПолученияОтветаПроЭДО(Ответ, Форма) Экспорт  
	
	Если Ответ = НСтр("ru = 'Да и предложить позже'") Тогда
		Форма.ПрограммноеЗакрытие = Истина;
		Форма.Закрыть();
	ИначеЕсли Ответ = НСтр("ru = 'Да и больше не предлагать'") Тогда
		Форма.ПрограммноеЗакрытие = Истина;
		СохранитьОтказОтЭДО(Форма.ДокументЗаявление);
		ОповеститьОбИзменении(Форма.ДокументЗаявление);
		Оповестить("ОтказОтЭДО", Форма.ДокументЗаявление);
		Форма.Закрыть();
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура СохранитьОтказОтЭДО(ДокументЗаявление)
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("НастройкаЗавершена", Истина);
	ДополнительныеПараметры.Вставить("НастройкаЭДОЗавершена", Истина);
	ДополнительныеПараметры.Вставить("ПодключитьЭДО", Ложь);
	ДополнительныеПараметры.Вставить("ПереиздатьСертификатЭДО", Ложь);
	
	ОбработкаЗаявленийАбонентаВызовСервера.ОбновитьРеквизитыЗаявления(
		ДокументЗаявление, 
		ДополнительныеПараметры);
	
КонецПроцедуры

#КонецОбласти

#Область СУЦ

&НаКлиенте
Процедура ВыполнитьДействиеСФайлом(Форма, ОповещениеОЗавершении, Элемент, Действие, СтандартнаяОбработка) Экспорт
	
	ВидДокумента      = Сред(Элемент.Имя, 7);
	СвойстваДокумента = ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма)[ВидДокумента];
		
	СтандартнаяОбработка = Ложь;
	
	ПриложитьМЧДИлиСкан = ОбработкаЗаявленийАбонентаКлиентСервер.ОткрытьФормуДобавленияМЧДИлиСкана(Форма, СвойстваДокумента.Представление);
	Если ПриложитьМЧДИлиСкан Тогда
		
		ОткрытьФормуДобавленияДоверенности(Форма, ОповещениеОЗавершении);
		
	ИначеЕсли Действие = "выберите" Тогда
		
		Если СвойстваДокумента.ОдинФайл Тогда
			ВыбратьОдинФайлДокумента(Форма, ОповещениеОЗавершении, ВидДокумента);
		Иначе
			ДобавитьФайлыСПредпросмотром(Форма, ОповещениеОЗавершении, ВидДокумента);
		КонецЕсли;
		
	ИначеЕсли Действие = "Файл" Тогда
		
		Если СвойстваДокумента.ОдинФайл Тогда
			ОткрытьФайлДанногоВида(Форма, ВидДокумента);
		Иначе
			ДобавитьФайлыСПредпросмотром(Форма, ОповещениеОЗавершении, ВидДокумента);
		КонецЕсли;
		
	ИначеЕсли Действие = "Отсканируйте" Тогда
		
		СканироватьФайл(Форма, ОповещениеОЗавершении, ВидДокумента);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДоверенностьОбработкаНавигационнойСсылки(Форма, Элемент, Действие, СтандартнаяОбработка, ОповещениеОЗавершении = Неопределено) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ОсобаяОбработка  = ОбработкаЗаявленийАбонентаКлиентСервер.ОткрытьФормуДобавленияМЧДИлиСкана(Форма, НСтр("ru = 'Доверенность'"));
	ТолькоСкан       = НЕ ОсобаяОбработка;
	ЕстьДоверенность = ОбработкаЗаявленийАбонентаКлиентСервер.ЕстьДоверенностьСредиФайлов(Форма);
	ИспользоватьСуществующий = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(Форма);
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("Действие", Действие);
	ДополнительныеПараметры.Вставить("Элемент", Элемент);
	ДополнительныеПараметры.Вставить("Форма", Форма);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	НадоПредупредить = 
		ТолькоСкан 
		И ИспользоватьСуществующий 
		И НЕ ЕстьДоверенность;
	
	Если НадоПредупредить Тогда
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыборДоверенности_ПослеПредупреждения", 
			ЭтотОбъект, 
			ДополнительныеПараметры);
			
		Текст = НСтр("ru = 'Поскольку вы планируете использовать сертификат сотрудника, содержащий реквизиты организации, в соответствии с ФЗ от 27.12.2019 N 476-ФЗ, для подтверждения полномочий необходимо приложить скан-копию доверенности'");
		ПоказатьПредупреждение(ОписаниеОповещения, Текст);
		
	Иначе
		
		ВыборДоверенности_ПослеПредупреждения(ДополнительныеПараметры);
		
	КонецЕсли;
	
КонецПроцедуры
	
&НаКлиенте
Процедура ВыборДоверенности_ПослеПредупреждения(ВходящийКонтекст) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ВыполнитьДействиеСФайлом(
		ВходящийКонтекст.Форма, 
		ВходящийКонтекст.ОповещениеОЗавершении, 
		ВходящийКонтекст.Элемент, 
		ВходящийКонтекст.Действие, 
		СтандартнаяОбработка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуДобавленияДоверенности(Форма, ОповещениеОЗавершении) Экспорт
	
	ПараметрыФормы = 
		"ФайлыДокументов,
		|Организация,
		|ЗапретитьИзменение,
		|КрасныйЦвет,
		|СканированиеДоступно,
		|МаксимальныйРазмерФайла,
		|ДопустимыеТипыФайлов,
		|ВладелецЭЦПЭтоФизЛицо,
		|ВладелецЭЦП,
		|ВладелецЭЦПИНН,
		|ВладелецЭЦПСНИЛС,
		|ВладелецЭЦПТип,
		|ВладелецЭЦПФамилия,
		|ВладелецЭЦПИмя,
		|ВладелецЭЦПОтчество,
		|ЭтоОткрытиеСВыбраннойЭЦП,
		|СпособПолученияСертификата,
		|ЭтоЮридическоеЛицо,
		|ЭтоБыстрыйСтарт,
		|ЕстьПредыдущиеУчетныеЗаписи,
		|ВключаемыйСертификат,
		|ЗПЭД";
	
	ПараметрыФормы = ПараметрыФормы + "," + МультирежимКлиентСервер.ФлагиНаправлений();
	
	ДополнительныеПараметры = Новый Структура(ПараметрыФормы);
	ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, Форма, ПараметрыФормы);
	ДополнительныеПараметры.Вставить("ПараметрыФормы", ПараметрыФормы);
	
	ЭтоПолучениеНового = ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоПолучениеНовогоСертификата(Форма);
	ДополнительныеПараметры.Вставить("ЭтоПолучениеНовогоСертификата", ЭтоПолучениеНового);
	
	Используется = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользуетсяСертификатФЛ(Форма);
	ДополнительныеПараметры.Вставить("ИспользуетсяСертификатФЛ", Используется);
	
	ЕстьВыборМеждуМЧДиСканом = ОбработкаЗаявленийАбонентаКлиентСервер.ЕстьВыборМеждуМЧДиСканом(Форма);
	ДополнительныеПараметры.Вставить("ЕстьВыборМеждуМЧДиСканом", ЕстьВыборМеждуМЧДиСканом);
	
	ПараметрыОповещения = Новый Структура();
	ПараметрыОповещения.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ПараметрыОповещения.Вставить("Форма", Форма);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ДобавлениеДоверенности_ПослеВыбораДоверенности", 
		ЭтотОбъект,
		ПараметрыОповещения);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.Мастер_Доверенности", ДополнительныеПараметры,,,,,ОписаниеОповещения);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ДобавлениеДоверенности_ПослеВыбораДоверенности(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Форма = ВходящийКонтекст.Форма;
	
	ПараметрыФормы = Результат.ПараметрыФормы;
	ЗаполнитьЗначенияСвойств(Форма, Результат, ПараметрыФормы, "ПараметрыФормы");
	
	Если Результат.Модифицированность Тогда
		ОбработкаЗаявленийАбонентаКлиентСервер.ИзменитьОформлениеДокументов(Форма);
	Конецесли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьОдинФайлДокумента(Форма, ОповещениеОЗавершении, ВидДокумента)
	
	ДополнительныеПараметры = ПараметрыПроцедурыВыбораФайлов(Форма, ВидДокумента);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("Форма", Форма);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыбратьОдинФайлДокумента_ПослеПомещения", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	НачатьПомещениеФайла(ОписаниеОповещения,,, Истина, УникальныйИдентификатор);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФайлДанногоВида(Форма, ВидДокумента)

	ФайлыСканов = Новый Массив;
	Для каждого ФайлДокумента Из Форма.ФайлыДокументов Цикл
	
		ФайлДокумента = ФайлДокумента.Значение;
		Если ФайлДокумента.Документ = ВидДокумента Тогда
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(ФайлДокумента.Адрес, ФайлДокумента.ИсходноеИмя);
		КонецЕсли;
		
	КонецЦикла;

КонецПроцедуры

&НаКлиенте
Функция ПараметрыПроцедурыВыбораФайлов(Форма, ВидДокумента = "", ЗаголовокФормыПредпросмотра = "") Экспорт
	
	Требования = ОбработкаЗаявленийАбонентаКлиентСервер.ТребованияВложениямЗаявления();
	
	ПараметрыФункции = ОперацииСФайламиЭДКОКлиент.ПараметрыМетодаДобавитьФайлыСПредпросмотром();
	ПараметрыФункции.Вставить("ВидДокумента",        ВидДокумента);
	ПараметрыФункции.Вставить("ФайлыСканов",         ФайлыДляПредпросмотра(Форма, ВидДокумента));
	ПараметрыФункции.Вставить("Требования",          Требования);
	ПараметрыФункции.Вставить("РежимТолькоПросмотр", Форма.ЗапретитьИзменение);
	ПараметрыФункции.Вставить("Документ",            ЗаголовокФормыПредпросмотра);
	
	ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ПараметрыФункции, Требования, Истина);
	
	Возврат ПараметрыФункции;
	
КонецФункции

&НаКлиенте
Функция ФайлыДляПредпросмотра(Форма, ВидДокумента)

	ФайлыСканов = Новый Массив;
	Для каждого ФайлДокумента Из Форма.ФайлыДокументов Цикл
		
		ФайлДокумента = ФайлДокумента.Значение;
		Если ФайлДокумента.Документ = ВидДокумента Тогда
			
			ПараметрыФайла = Новый Структура;
			ПараметрыФайла.Вставить("Адрес", 		ФайлДокумента.Адрес);
			ПараметрыФайла.Вставить("ИсходноеИмя", 	ФайлДокумента.ИсходноеИмя);
			ФайлыСканов.Добавить(ПараметрыФайла);
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат ФайлыСканов;
	
КонецФункции

&НаКлиенте
Процедура ВыбратьОдинФайлДокумента_ПослеПомещения(Результат, Адрес, ВыбранноеИмяФайла, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ОписаниеФайла = Новый Структура();
	ОписаниеФайла.Вставить("Имя",    ВыбранноеИмяФайла);
	ОписаниеФайла.Вставить("Адрес",  Адрес);
	ОписаниеФайла.Вставить("Размер", 0);
	
	ОписаниеФайлов = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОписаниеФайла);

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ВыбратьОдинФайлДокумента_ПослеОбработки", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	Требования = ОбработкаЗаявленийАбонентаКлиентСервер.ТребованияВложениямЗаявления();
	
	ОперацииСФайламиЭДКОКлиент.ОбработатьКартинки(
		ОписаниеОповещения, 
		ОписаниеФайлов, 
		Требования, 
		ВходящийКонтекст.Форма.УникальныйИдентификатор);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьОдинФайлДокумента_ПослеОбработки(Результат, ВходящийКонтекст) Экспорт	
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	Форма = ВходящийКонтекст.Форма;
	ВидДокумента = ВходящийКонтекст.ВидДокумента;
	
	Для каждого ОписаниеФайла Из Результат.ОписанияФайлов Цикл
	    Имя   = ОписаниеФайла.Имя;
		Адрес = ОписаниеФайла.Адрес;
	КонецЦикла;
	
	ФайлыДокумента = ОбработкаЗаявленийАбонентаКлиентСервер.ОписаниеФайла(Имя, Адрес);
	ТекстОшибки    = ОперацииСФайламиЭДКОСлужебныйКлиент.ТекстОшибкиДобавленияОдногоФайла(
		ФайлыДокумента, 
		Форма.МаксимальныйРазмерФайла, 
		Форма.ДопустимыеТипыФайлов);
	
	Если НЕ ПустаяСтрока(ТекстОшибки) Тогда
		ПоказатьПредупреждение(,ТекстОшибки);
		Возврат;
	КонецЕсли;
	
	ОбработкаЗаявленийАбонентаКлиентСервер.ПослеДобавленияСкана(Форма, ВидДокумента, ФайлыДокумента);
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьФайлыСПредпросмотром(Форма, ОповещениеОЗавершении, ВидДокумента)

	Описание         = ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма)[ВидДокумента];
	ПараметрыФункции = ПараметрыПроцедурыВыбораФайлов(Форма, ВидДокумента, Описание.Представление);
	
	ФайлыСканов = Новый Массив;
	Для каждого ФайлДокумента Из Форма.ФайлыДокументов Цикл
		
		ФайлДокумента = ФайлДокумента.Значение;
		Если ФайлДокумента.Документ = ВидДокумента Тогда
			
			ПараметрыФайла = Новый Структура;
			ПараметрыФайла.Вставить("Адрес", 		ФайлДокумента.Адрес);
			ПараметрыФайла.Вставить("ИсходноеИмя", 	ФайлДокумента.ИсходноеИмя);
			ФайлыСканов.Добавить(ПараметрыФайла);
			
		КонецЕсли;
		
	КонецЦикла;
	
	ПараметрыФункции.Вставить("Файлы", ФайлыСканов);
	
	ПредставлениеРазмераФайла = ОбщегоНазначенияЭДКОКлиентСервер.ТекстовоеПредставлениеРазмераФайла(Форма.МаксимальныйРазмерФайла);
	Размер = СтрШаблон("Не более %1", ПредставлениеРазмераФайла);
	
	Требования = Новый Структура;
	Требования.Вставить("Формат", 		СтрСоединить(СтрРазделить(ВРег(Форма.ДопустимыеТипыФайлов), ";"), ", "));
	Требования.Вставить("Разрешение", 	"150-400 dpi");
	Требования.Вставить("Цветность", 	"Цветное изображение, глубина цвета 16-24 бит");
	Требования.Вставить("Размер", 		Размер);
	
	ПараметрыФункции.Требования.Вставить("Требования",     Требования);
	ПараметрыФункции.Требования.Вставить("КлючТребований", "Заявление на подключение к 1С-Отчетности");
	ПараметрыФункции.Вставить("РежимТолькоПросмотр", Форма.ЗапретитьИзменение);
	ПараметрыФункции.Вставить("Документ", Описание.Представление);
	
	ДополнительныеПараметры = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(ПараметрыФункции);
	ДополнительныеПараметры.Вставить("Форма", Форма);
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"ДобавитьФайлыСПредпросмотромПослеВыполнения", 
		ЭтотОбъект, 
		ДополнительныеПараметры);
	
	ОперацииСФайламиЭДКОКлиент.ДобавитьФайлыСПредпросмотром(
		Оповещение, 
		УникальныйИдентификатор, 
		СтрШаблон(НСтр("ru = 'Выберите файлы документа <%1> '"), Описание.Представление), 
		ПараметрыФункции);
		
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьФайлыСПредпросмотромПослеВыполнения(Результат, ВходящийКонтекст) Экспорт
	
	Форма = ВходящийКонтекст.Форма;
	ВидДокумента = ВходящийКонтекст.ВидДокумента;
	
	Если Форма.ЗапретитьИзменение Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	Если Результат.Выполнено Тогда
		ОбработкаЗаявленийАбонентаКлиентСервер.ПослеДобавленияСкана(Форма, ВидДокумента, Результат.Файлы);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	
КонецПроцедуры

&НаКлиенте
Процедура СканироватьФайл(Форма, ОповещениеОЗавершении, ВидДокумента)
	
	Если Форма.СканированиеДоступно Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Форма", Форма);
		ДополнительныеПараметры.Вставить("ВидДокумента", ВидДокумента);
		ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
		
		Форма.СохранитьЗаявление();
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"СканироватьФайл_Завершение", 
			ЭтотОбъект,
			ДополнительныеПараметры);
		
		РаботаСФайламиКлиент.ДобавитьФайл(
			ОписаниеОповещения,
			Форма.ДокументЗаявление.Ссылка,
			ЭтотОбъект,
			3,
			Истина);
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СканироватьФайл_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат <> Неопределено И Результат.ФайлДобавлен Тогда
		ДобавитьОтсканированныеФайлы(Результат.ФайлСсылка, ВходящийКонтекст);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ДанныеФайла(ПрисоединенныйФайл)
	
	ДополнительныеПараметры = РаботаСФайламиКлиентСервер.ПараметрыДанныхФайла();
	ДополнительныеПараметры.ИдентификаторФормы 				= УникальныйИдентификатор;
	ДополнительныеПараметры.ПолучатьСсылкуНаДвоичныеДанные 	= Истина;
	
	ДанныеФайла = РаботаСФайлами.ДанныеФайла(ПрисоединенныйФайл, ДополнительныеПараметры);
	Возврат ДанныеФайла;
	
КонецФункции

&НаКлиенте
Процедура ДобавитьОтсканированныеФайлы(ПрисоединенныйФайл, ВходящийКонтекст)
	
	ДанныеФайла = ДанныеФайла(ПрисоединенныйФайл);
	
	ОписаниеФайла = Новый Структура();
	ОписаниеФайла.Вставить("Имя",    ДанныеФайла.ИмяФайла);
	ОписаниеФайла.Вставить("Адрес",  ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
	ОписаниеФайла.Вставить("Размер", 0);
	
	ОписаниеФайлов = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ОписаниеФайла);	
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ДобавитьОтсканированныеФайлы_ПослеОбработки", 
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	Требования = ОбработкаЗаявленийАбонентаКлиентСервер.ТребованияВложениямЗаявления();
	
	ОперацииСФайламиЭДКОКлиент.ОбработатьКартинки(
		ОписаниеОповещения, 
		ОписаниеФайлов, 
		Требования,
		ВходящийКонтекст.Форма.УникальныйИдентификатор);

КонецПроцедуры

&НаКлиенте
Процедура ДобавитьОтсканированныеФайлы_ПослеОбработки(Результат, ВходящийКонтекст) Экспорт
	
	Форма = ВходящийКонтекст.Форма;
	ВидДокумента = ВходящийКонтекст.ВидДокумента;
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	Для каждого ОписаниеФайла Из Результат.ОписанияФайлов Цикл
		Адрес = ОписаниеФайла.Адрес;
		Имя   = ОписаниеФайла.Имя;
	КонецЦикла;
	
	Расширение = ОбщегоНазначенияКлиентСервер.ПолучитьРасширениеИмениФайла(Имя);
	
	НовоеИмя = ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма)[ВидДокумента].Представление + "." + Расширение;
	
	ОписаниеФайла = Новый Структура;
	ОписаниеФайла.Вставить("Адрес", 		Адрес);
	ОписаниеФайла.Вставить("ИсходноеИмя", 	НовоеИмя);
	ОписаниеФайла.Вставить("Имя", 			НовоеИмя);
	
	ОписанияФайлов = Новый Массив;
	ОписанияФайлов.Добавить(ОписаниеФайла);
	
	ОписанияФайлов = ОперацииСФайламиЭДКОСлужебныйВызовСервера.ДополнитьОписанияРазмерамиФайлов(ОписанияФайлов);
	
	ОбработкаЗаявленийАбонентаКлиентСервер.УдалитьТекущийВидДокументов(Форма, ВидДокумента);
	ОбработкаЗаявленийАбонентаКлиентСервер.ДополнитьФайлыВидомДокумента(ОписанияФайлов, ВидДокумента, Форма);
	ОбработкаЗаявленийАбонентаКлиентСервер.ИзменитьОформлениеДокументов(Форма);
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);

КонецПроцедуры

&НаКлиенте
Процедура ОчиститьСканНажатие(Форма, Элемент) Экспорт
	
	ВидДокумента = Сред(Элемент.Имя, 15);
	ОбработкаЗаявленийАбонентаКлиентСервер.ОчиститьФайл(Форма, ВидДокумента);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьВсеОбработкаНавигационнойСсылки(Форма, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка  = Ложь;
	
	ЗаголовокВыбораФайлов = НСтр("ru = 'Выберите скан-копии в заявление по 1С-Отчетности'");
	ДопПараметры          = ПараметрыПроцедурыВыбораФайлов(Форма);
	ОписаниеОповещения    = Новый ОписаниеОповещения(
		"ВыбратьВсеОбработкаНавигационнойСсылки_ПослеВыбораФайлов", 
		ЭтотОбъект,
		Форма);
		
	ОперацииСФайламиЭДКОКлиент.ДобавитьФайлы(ОписаниеОповещения, Форма.УникальныйИдентификатор, ЗаголовокВыбораФайлов, ДопПараметры, Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьВсеОбработкаНавигационнойСсылки_ПослеВыбораФайлов(Результат, Форма) Экспорт 
 
	Если Результат.Выполнено И Результат.ОписанияФайлов.Количество() > 0 Тогда
		
		ВидыДокументов = ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма);
		Сканы = Результат.ОписанияФайлов;
		
		Если Форма.ВладелецЭЦПЭтоФизЛицо Тогда
			МЧД = ОбработкаЗаявленийАбонентаКлиентСервер.ИзвлечьМЧДИзФайлов(Форма);
			ВидыДокументов.Удалить("Доверенность");
		КонецЕсли;
		
		Для каждого ФайлыДокумента Из Форма.ФайлыДокументов Цикл
			
			Значение = ФайлыДокумента.Значение;
			
			Скан = Новый Структура;
			Скан.Вставить("Размер", 	Значение.Размер);
			Скан.Вставить("Адрес", 		Значение.Адрес);
			Скан.Вставить("Имя", 		Значение.ИсходноеИмя);
			Скан.Вставить("Документ", 	ВидыДокументов[Значение.Документ].Представление);
			
			Сканы.Добавить(Скан);
		
		КонецЦикла; 
		
		ПараметрыОповещения = Новый Структура();
		ПараметрыОповещения.Вставить("Форма", Форма);
		ПараметрыОповещения.Вставить("МЧД", МЧД);
		
		ОписаниеОповещения = Новый ОписаниеОповещения(
			"ВыбратьВсеОбработкаНавигационнойСсылки_ПослеУказанияВидаДокументов", 
			ЭтотОбъект,
			ПараметрыОповещения);
			
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("Сканы", Результат.ОписанияФайлов);
		ПараметрыОткрытия.Вставить("ВидыДокументов", ВидыДокументов);
			
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаГрупповогоДобавленияСкановВЗаявление", ПараметрыОткрытия,,,,,ОписаниеОповещения);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьВсеОбработкаНавигационнойСсылки_ПослеУказанияВидаДокументов(Результат, ВходящийКонтекст) Экспорт
	
	Форма = ВходящийКонтекст.Форма;
	
	Если Форма.ЗапретитьИзменение Тогда
		Возврат;
	КонецЕсли;
	
	Если Результат <> Неопределено Тогда
		
		ОбработкаЗаявленийАбонентаКлиентСервер.УстановитьМодифицированность(Форма);
		
		Форма.ФайлыДокументов.Очистить();
		Для каждого Скан Из Результат Цикл
			Форма.ФайлыДокументов.Добавить(Скан, Скан.Документ);
		КонецЦикла;
		
		Если Форма.ВладелецЭЦПЭтоФизЛицо Тогда
			
			ОбработкаЗаявленийАбонентаКлиентСервер.ВернутьМЧДВФайлы(
				Форма, 
				ВходящийКонтекст.МЧД);
				
		КонецЕсли;
		
		ОбработкаЗаявленийАбонентаКлиентСервер.ИзменитьОформлениеДокументов(Форма);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСканыДокументов(Форма, МастерДалее, ТихийРежим = Ложь) Экспорт
	
	Для Каждого ТекущийВидДокумента Из ОбработкаЗаявленийАбонентаКлиентСервер.ВидыДокументовПоФорме(Форма) Цикл
		
		Описание = ТекущийВидДокумента.Значение;
		
		// Для ИП подтверждения полномочий не существует
		Если НЕ Описание.Видимость Тогда
			Продолжить;
		КонецЕсли;
		
		ВидДокумента = ТекущийВидДокумента.Ключ;
		ПроверитьСканДокумента(Форма, МастерДалее, Описание, ВидДокумента, ТихийРежим);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСканДокумента(Форма, МастерДалее, Описание, ВидДокумента, ТихийРежим = Ложь) Экспорт
	
	ЕстьФайлы = Ложь;
	
	Для Каждого ЭлементСписка Из Форма.ФайлыДокументов Цикл
		ТекущийДокумент = ЭлементСписка.Значение.Документ;
		Если ТекущийДокумент = ВидДокумента Тогда
			ЕстьФайлы = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если Не ЕстьФайлы Тогда
		
		Если НЕ ТихийРежим Тогда
			
			Если Форма.ВладелецЭЦПЭтоФизЛицо И ВидДокумента = НСтр("ru = 'Доверенность'") Тогда
				ТекстОшибки = НСтр("ru = 'Выберите машиночитаемую доверенность'");
			Иначе
				ТекстОшибки = НСтр("ru = 'Приложите скан-копию документ ""%1""'");
				ТекстОшибки = СтрШаблон(ТекстОшибки, Описание.Представление);
			КонецЕсли;
			
			Поле = "Указатель" + ВидДокумента;
			
			ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстОшибки,,Поле);
		КонецЕсли;
			
		МастерДалее = Ложь;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

&НаКлиенте
Процедура НапечататьЗаявлениеНаПодключение(Заявление) Экспорт
	
	ТабДок = ТабличныйДокументЗаявленияНаПодключение(Заявление);
	
	ЗаголовокТабДок = "Заявление на подключение и согласие на обработку ПДн";
	
	НапечататьДокумент(ТабДок, ЗаголовокТабДок);

КонецПроцедуры

&НаСервере
Функция ТабличныйДокументЗаявленияНаПодключение(Заявление) Экспорт
	
	ТабДокумент = Новый ТабличныйДокумент;
	ТабДокумент.КлючПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ПечатьЗаявка";
	
	КонтекстЭДОСервер  = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ЭтоЮридическоеЛицо = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(Заявление.Организация);
	
	Если Заявление.ВладелецЭЦПЭтоФизЛицо Тогда
		Макет = ПолучитьМакетОбработки("ЗаявлениеФЛ");
	ИначеЕсли ЭтоЮридическоеЛицо Тогда
		Макет = ПолучитьМакетОбработки("ЗаявлениеЮЛ");
	Иначе
		Макет = ПолучитьМакетОбработки("ЗаявлениеИП");
	КонецЕсли;
		
	Область = Макет.ПолучитьОбласть("Заявление");
	Область.Параметры.Заполнить(Заявление);
	
	Если Заявление.ВладелецЭЦПЭтоФизЛицо Тогда
		
		ДобавитьДанныеВладельцаЗаявление(Заявление, Область);
		ДобавитьАдресВладельцаВЗаявление(Заявление, Область);
		
		Если ЗначениеЗаполнено(Заявление.РегламентУЦ) Тогда
			СтруктураURI = ОбщегоНазначенияКлиентСервер.СтруктураURI(Заявление.РегламентУЦ);
			// <схема>://<логин>:<пароль>@<хост>:<порт>/
			Область.Параметры.АдресРегламента = СтруктураURI.Схема + "://" + СтруктураURI.Хост + "/";
		Иначе
			Область.Параметры.АдресРегламента = "https://astral.ru";
		КонецЕсли;
		
		ТабДокумент.Вывести(Область);
		ТабДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
		МакетСогласиеПД = ПолучитьМакетОбработки("СогласиеПД");
		Область = МакетСогласиеПД.ПолучитьОбласть("Согласие");
		
		Область.Параметры.Заполнить(Заявление);
		ДобавитьДанныеВладельцаЗаявление(Заявление, Область);
		ДобавитьАдресВладельцаВЗаявление(Заявление, Область);
		
	ИначеЕсли ЭтоЮридическоеЛицо Тогда
		
		ДобавитьЧастиАдресаВЗаявление(Заявление, Область);
		ДобавитьДанныеРуководителяВЗаявление(Заявление, Область);
		
	Иначе
		
		ДобавитьЧастиАдресаВЗаявление(Заявление, Область);
		ДобавитьДанныеВладельцаЗаявление(Заявление, Область);
		ДобавитьАдресРегистрацииВЗаявление(Заявление, Область);
		
	КонецЕсли;
	
	КонтекстЭДОСервер.РазместитьНаОдномЛисте(ТабДокумент);
	ТабДокумент.Вывести(Область);
	
	Возврат ТабДокумент;
		
КонецФункции

&НаСервере
Процедура ДобавитьАдресВладельцаВЗаявление(Заявление, Область) Экспорт

	Данные = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДанныеИсполнителя(
		Заявление.ВладелецЭЦП, 
		Заявление.Организация);
		
	Если Данные.Свойство("АдресПоПрописке_JSON") Тогда
		Адрес = Данные.АдресПоПрописке_JSON;
	ИначеЕсли Данные.Свойство("АдресМестаПроживания_JSON") Тогда
		Адрес = Данные.АдресМестаПроживания_JSON;
	КонецЕсли;
		
	Если ЗначениеЗаполнено(Адрес) Тогда
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Область.Параметры, "АдресРегистрации") Тогда
			Область.Параметры.АдресРегистрации = РаботаСАдресами.СведенияОбАдресе(Адрес).МуниципальноеПредставление;
		КонецЕсли;
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Область.Параметры, "НаселенныйПункт") Тогда
			Область.Параметры.НаселенныйПункт = ОбработкаЗаявленийАбонента.ПолеСертификата_2_5_4_7(Адрес);
		КонецЕсли;
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Область.Параметры, "Улица") Тогда
			Область.Параметры.Улица = ОбработкаЗаявленийАбонента.ПолеСертификата_2_5_4_9(Адрес);
		КонецЕсли;
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Область.Параметры, "Область") Тогда
			Область.Параметры.Область = ОбработкаЗаявленийАбонента.ПолеСертификата_2_5_4_8(Адрес);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ДобавитьЧастиАдресаВЗаявление(Заявление, Область) Экспорт
	
	АдресЮридическийЗначение = Заявление.АдресЮридический;
	
	Адрес_Область = ОбработкаЗаявленийАбонента.ПолеСертификата_2_5_4_8(АдресЮридическийЗначение);
	Адрес_Город   = ОбработкаЗаявленийАбонента.ПолеСертификата_2_5_4_7(АдресЮридическийЗначение);
	Адрес_Улица   = ОбработкаЗаявленийАбонента.ПолеСертификата_2_5_4_9(АдресЮридическийЗначение);
	
	Область.Параметры.Область = Адрес_Область;
	Область.Параметры.Город   = Адрес_Город;
	Область.Параметры.Улица   = Адрес_Улица;
	
	ДобавитьАдресУЦВЗаявление(Заявление, Область);
	
КонецПроцедуры

&НаСервере
Процедура ДобавитьАдресУЦВЗаявление(Заявление, Область)
	
	Если ЗначениеЗаполнено(Заявление.АдресУЦ) Тогда
		
		ЮрАдресУЦ = НСтр("ru = '(юридический адрес: %1) '");
		ЮрАдресУЦ = СтрШаблон(ЮрАдресУЦ, Заявление.АдресУЦ);
		
		Область.Параметры.ЮрАдресУЦ = ЮрАдресУЦ;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ДобавитьАдресРегистрацииВЗаявление(Заявление, Область) Экспорт
	
	Представление = ОбработкаЗаявленийАбонента.ПредставлениеКонтактнойИнформации(Заявление.АдресЮридический);
	
	Область.Параметры.АдресРегистрации = Представление;
			
КонецПроцедуры

&НаСервере
Процедура ДобавитьДанныеВладельцаЗаявление(Заявление, Область) Экспорт
	
	ВладелецЭЦПФИО = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ФИОФизЛица(Заявление.ВладелецЭЦП);
	ВладелецЭЦПФИО = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(ВладелецЭЦПФИО);
	Область.Параметры.ВладелецЭЦПФИО = ВладелецЭЦПФИО;
			
КонецПроцедуры

&НаСервере
Процедура ДобавитьДанныеРуководителяВЗаявление(Заявление, Область) Экспорт
	
	// Руководитель
	Руководитель = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.Руководитель(Заявление.Организация);
	Если ЗначениеЗаполнено(Руководитель) Тогда
		
		ДанныеРуководителя = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ПолучитьДанныеИсполнителя(
			Руководитель, 
			Заявление.Организация);
			
		Если ДанныеРуководителя.Свойство("Должность") Тогда
			ДолжностьРуководителя = ДанныеРуководителя.Должность;
		КонецЕсли;
		
		ФИОРуководителя = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ФИОФизЛица(Руководитель);
		ФамилияИнициалыРуководителя = ФизическиеЛицаКлиентСервер.ФамилияИнициалы(ФИОРуководителя);
		Область.Параметры.ФИОРуководителя = ФамилияИнициалыРуководителя;
		
		ЕстьРеквизит = ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Область.Параметры, "РуководительФамилия");
		Если ЕстьРеквизит Тогда
			Область.Параметры.РуководительФамилия = ДанныеРуководителя.ФИО.Фамилия;
			Область.Параметры.РуководительИмя = ДанныеРуководителя.ФИО.Имя;
			Область.Параметры.РуководительОтчество = ДанныеРуководителя.ФИО.Отчество;
		КонецЕсли;

		Область.Параметры.ДолжностьРуководителя = ДолжностьРуководителя;
			
	КонецЕсли;
			
КонецПроцедуры

&НаКлиенте
Процедура НапечататьЗаявлениеПо1СОтчетности(Знач Заявление) Экспорт
	
	Если НЕ ПечатьЗаявленияТребуется(Заявление) Тогда
		Возврат;
	КонецЕсли;
	
	Если ПолучитьРеквизит(Заявление, "ЭтоУдаленноеПереизданиеСертификата") Тогда
		
		Адрес = ОбработкаЗаявленийАбонентаВызовСервера.ДанныеЭлДокументаЗаявления(
			Заявление, "ЗаявлениеНаИзготовлениеСертификата");
			
		Если ЭтоАдресВременногоХранилища(Адрес) Тогда
			ОперацииСФайламиЭДКОКлиент.ОткрытьФайл(Адрес, "ЗаявлениеНаИзготовлениеСертификата.pdf");	
		Иначе
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Бланк заявления еще не получен, поскольку запрашивается с сервера АО ""Калуга Астрал"" перед отправкой заявления.'"));
		КонецЕсли;
		
	Иначе
		НапечататьЗаявлениеНаПодключение(Заявление);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПечатьЗаявленияТребуется(Знач Заявление) Экспорт
	
	Требуется = Ложь;
	
	Если НЕ ЗначениеЗаполнено(Заявление.СпецоператорСвязи) тогда
		
		Текст = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'Не выбран Удостоверяющий центр в документе %1.'"), Строка(Заявление));
		
	ИначеЕсли ОбработкаЗаявленийАбонентаВызовСервера.ЭтоРуководитель(Заявление) 
			И НЕ Заявление.ЭтоУдаленноеПереизданиеСертификата Тогда
		
		Текст = НСтр("ru = 'Печать заявления для руководителей не требуется. Заявление оформит специалист при идентификации личности владельца'");
		
	ИначеЕсли ОбработкаЗаявленийАбонентаВызовСервера.ЭтоБюджетополучатель(Заявление) Тогда
		
		Текст = НСтр("ru = 'Печать заявления для бюджетополучателей не требуется. Заявление оформляется в Удостоверяющем центре Федеральном казначействе'");
		
	ИначеЕсли НЕ ЗаявлениеСвязаноСПолучениемСертификата(Заявление) Тогда
		
		Текст = НСтр("ru = 'Печать заявления не требуется поскольку не было выпуска нового сертификата'");
		
	Иначе
		
		Требуется = Истина;
		
	КонецЕсли;
	
	Если НЕ Требуется Тогда
		ОбщегоНазначения.СообщитьПользователю(Текст); 
	КонецЕсли;
	
	Возврат Требуется;
	
КонецФункции

&НаСервере
Функция ЗаявлениеСвязаноСПолучениемСертификата(Заявление)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЗаявлениеСвязаноСПолучениемСертификата(Заявление);
	
КонецФункции

&НаКлиенте
Процедура ПоказатьФормуПредупрежденияОбИзменившихсяНастройкахПодключения_ПослеПолученияСертификата(Результат, ВходящийКонтекст) Экспорт
	
	Если Результат = Неопределено Тогда
		ДвДанныеСертификата = Неопределено;
	Иначе
		ДвДанныеСертификата = Base64Значение(Результат);
	КонецЕсли;
	
	Организация    = ВходящийКонтекст.Организация;
	ФормаВладельца = ВходящийКонтекст.ФормаВладельца;
	
	СтруктураПараметровОткрытияФормы = Новый Структура();
	СтруктураПараметровОткрытияФормы.Вставить("Организация", Организация);
	СтруктураПараметровОткрытияФормы.Вставить("ДвДанныеСертификата", ДвДанныеСертификата);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПоказатьФормуПредупрежденияОбИзменившихсяНастройкахПодключенияЗавершение", 
		ЭтотОбъект, 
		СтруктураПараметровОткрытияФормы);
	
	ВладелецПредупреждения = Неопределено;
	Если ТипЗнч(ФормаВладельца) = Тип("ФормаКлиентскогоПриложения") Тогда
		ВладелецПредупреждения = ФормаВладельца.ВладелецФормы;
	КонецЕсли;
		
	// Вызов окна предупреждения
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.ФормаПредупрежденияОСменеЗначенияПараметраПодключения", 
		СтруктураПараметровОткрытияФормы, 
		ВладелецПредупреждения, 
		Организация,
		,
		,
		ОписаниеОповещения);
	
КонецПроцедуры

#Область ПредупреждениеОНекорректныхСтатусахОтправкиПФР

&НаКлиенте
Процедура ПредупредитьОбОшибкеВСтатусахОтправкиПФР() Экспорт
	
	РезультатАнализа = ТребуетсяПредупредитьОбОшибкеВСтатусахОтправкиПФР();
	
	Если РезультатАнализа.ТребуетсяПредупредить Тогда
		ПоказатьПредупреждениеОНекорректныхСтатусахОтправкиПФР(РезультатАнализа.АдресОтчетов);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьПредупреждениеОНекорректныхСтатусахОтправкиПФР(Адрес) Экспорт
	
	ПараметрыОткрытияФормы = Новый Структура("Адрес", Адрес);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ПредупреждениеОНекорректныхСтатусахОтправкиПФР", ПараметрыОткрытияФормы);
	
КонецПроцедуры

&НаСервере
Функция ТребуетсяПредупредитьОбОшибкеВСтатусахОтправкиПФР() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ТребуетсяПредупредитьОбОшибкеВСтатусахОтправкиПФР();
	
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура ПоказатьФормуВыбораТелефонаИПочтыВОблаке(Форма, ВыполняемоеОповещение) Экспорт
	
	ПараметрыФормы = 
		"ЭлектроннаяПочтаДляПаролей,
		|ТелефонМобильныйДляПаролей,
		|ТелефонМобильный,
		|ЭтоПереходВОблако,
		|ЭтоУчетнаяЗаписьВМоделиСервиса,
		|ПолучатьСМСУведомления,
		|ПроверкаТелефонДляПаролей,
		|ПроверкаЭлектроннаяПочтаДляПаролей";
	
	ДополнительныеПараметры = Новый Структура(ПараметрыФормы);
	ЗаполнитьЗначенияСвойств(ДополнительныеПараметры, Форма, ПараметрыФормы);
	
	ДополнительныеПараметры.Вставить("ПараметрыФормы", ПараметрыФормы);
	
	ПараметрыВызова = Новый Структура();
	ПараметрыВызова.Вставить("Форма", Форма);
	ПараметрыВызова.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПоказатьФормуВыбораТелефонаИПочтыВОблаке_Завершение", 
		ЭтотОбъект,
		ПараметрыВызова);
	
	ОткрытьФорму(
		ПутьКОбъекту + ".Форма.Мастер_НастройкаПолученияВременныхПаролей",
		ДополнительныеПараметры,
		ЭтотОбъект,
		,
		,
		,
		ОписаниеОповещения, 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПоказатьФормуВыбораТелефонаИПочтыВОблаке_Завершение(Результат, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	Форма = ВходящийКонтекст.Форма;
	
	Если Результат = "Телефон недоступен" ИЛИ Результат = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат); 
		Возврат;
	КонецЕсли;
	
	ОбработкаЗаявленийАбонентаКлиентСервер.УстановитьМодифицированность(Форма);
	
	Если Результат.Свойство("ПроверкаТелефонДляПаролей") 
		И ЗначениеЗаполнено(Результат.ПроверкаТелефонДляПаролей.ИдентификаторПроверки) Тогда
		
		ЗаполнитьЗначенияСвойств(Форма.ПроверкаТелефонДляПаролей, Результат.ПроверкаТелефонДляПаролей);
		Форма.ТелефонМобильныйДляПаролей = Результат.ТелефонМобильныйДляПаролей;
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ТелефонМобильныйДляПаролейИзменился") Тогда
			Форма.ТелефонМобильныйДляПаролейИзменился = Истина;
		КонецЕсли;
		
		Если Форма.ЭтоПереходВОблако Тогда
		
			Форма.ТелефонМобильный = Результат.ТелефонМобильныйДляПаролей;
			
			Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ТелефонМобильныйИзменился") Тогда
				Форма.ТелефонМобильныйИзменился = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если Результат.Свойство("ПроверкаЭлектроннаяПочтаДляПаролей") 
		И ЗначениеЗаполнено(Результат.ПроверкаЭлектроннаяПочтаДляПаролей.ИдентификаторПроверки) Тогда
		
		ЗаполнитьЗначенияСвойств(Форма.ПроверкаЭлектроннаяПочтаДляПаролей, Результат.ПроверкаЭлектроннаяПочтаДляПаролей);
		
		Форма.ЭлектроннаяПочтаДляПаролей = Результат.ЭлектроннаяПочтаДляПаролей;
		Форма.ЭлектроннаяПочта = Результат.ЭлектроннаяПочтаДляПаролей;
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ЭлектроннаяПочтаДляПаролейИзменилась") Тогда
			Форма.ЭлектроннаяПочтаДляПаролейИзменилась = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	Форма.ПолучатьСМСУведомления = Результат.ПолучатьСМСУведомления;
	
	Если ЗначениеЗаполнено(Результат.ТелефонМобильный) И НЕ Форма.ЭтоПереходВОблако Тогда
		
		Телефон = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТелефонМобильныйБезРазделителей(Результат.ТелефонМобильный);
		Форма.ТелефонМобильный = СтрШаблон(
			"+7 %1 %2-%3-%4", 
			Сред(Телефон, 2, 3), 
			Сред(Телефон, 5, 3), 
			Сред(Телефон, 8, 2), 
			Сред(Телефон, 10, 2));
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ТелефонМобильныйИзменился") Тогда
			ТелефонМобильныйБезРазделителей         = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТелефонМобильныйБезРазделителей(Форма.ТелефонМобильный);
			ТелефонМобильныйИсходныйБезРазделителей = ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.ТелефонМобильныйБезРазделителей(Форма.ТелефонМобильныйИсходный);
			Форма.ТелефонМобильныйИзменился = ТелефонМобильныйБезРазделителей <> ТелефонМобильныйИсходныйБезРазделителей;
		КонецЕсли;
		
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСписокВключаемыхСертификатовОблачнойПодписи(МестоХраненияКлюча, ОповещениеОЗавершении)
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("МестоХраненияКлюча", МестоХраненияКлюча);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОткрытьСписокВключаемыхСертификатовОблачнойПодписи_ПослеВыбора", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	ПараметрыОперации = НовыЙ Структура("ПолноеОписаниеСертификата", Истина);
	МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
	МодульСервисКриптографииDSSКлиент.ВыборСертификата(ОписаниеОповещения, КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча), ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Функция СвойстваВыбранногоОблачногоСертификата(ВыбранныйСертификат)
	
	ДополненныйСертификат = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.ПодготовитьСертификатОблачнойПодписи(ВыбранныйСертификат);
	
	СвойствоВладельца = Новый Соответствие;
	СвойствоВладельца.Вставить("OID2_5_4_3", "ОбщееИмя");
	СвойствоВладельца.Вставить("OID2_5_4_4", "Фамилия");
	СвойствоВладельца.Вставить("OID2_5_4_42", "Имя");
	СвойствоВладельца.Вставить("OID1_2_643_100_3", "СНИЛС");
	СвойствоВладельца.Вставить("OID1_2_643_100_4", "ИННЮЛ");
	СвойствоВладельца.Вставить("OID1_2_643_3_131_1_1", "ИНН");
	СвойствоВладельца.Вставить("OID2_5_4_8", "КодРегиона");
	СвойствоВладельца.Вставить("OID1_2_643_100_5", "ОГРН");
	СвойствоВладельца.Вставить("OID1_2_643_100_1", "ОГРН");
	СвойствоВладельца.Вставить("OID2_5_4_12", "Должность");
	
	СертификатОблака = Новый Структура;
	СертификатОблака.Вставить("Отпечаток", ДополненныйСертификат.Отпечаток);
	СертификатОблака.Вставить("СерийныйНомер", ДополненныйСертификат.СерийныйНомер);
	СертификатОблака.Вставить("Издатель", ДополненныйСертификат.Издатель);
	СертификатОблака.Вставить("ДействителенС", ДополненныйСертификат.ДатаНачала);
	СертификатОблака.Вставить("ДействителенПо", ДополненныйСертификат.ДатаОкончания);
	СертификатОблака.Вставить("Base64", Base64Строка(ВыбранныйСертификат.Сертификат));
	СертификатОблака.Вставить("ПоставщикСтруктура", ДополненныйСертификат.Издатель);
	СертификатОблака.Вставить("Субъект", ДополненныйСертификат.Субъект);
	СертификатОблака.Вставить("Наименование", ДополненныйСертификат.Наименование);
	
	СертификатОблака.Вставить("ФИО", "");
	СертификатОблака.Вставить("Должность", "");
	СертификатОблака.Вставить("ИНН", "");
	СертификатОблака.Вставить("ОГРН", "");
	СертификатОблака.Вставить("КодРегиона", "");
	СертификатОблака.Вставить("СНИЛС", "");

	Для Каждого СтрокаКлюча Из СвойствоВладельца Цикл
		
		ТекущееЗначение = ?(ВыбранныйСертификат.Субъект.Свойство(СтрокаКлюча.Ключ), ВыбранныйСертификат.Субъект[СтрокаКлюча.Ключ], Неопределено);
		
		Если НЕ ЗначениеЗаполнено(ТекущееЗначение) Тогда
			Продолжить;
		КонецЕсли;
		
		Если СтрокаКлюча.Значение = "Имя" Тогда
			СертификатОблака.ФИО = СертификатОблака.ФИО + " " + ТекущееЗначение;
		ИначеЕсли СтрокаКлюча.Значение = "Фамилия" Тогда
			СертификатОблака.ФИО = ТекущееЗначение + СертификатОблака.ФИО;
		ИначеЕсли СтрокаКлюча.Значение = "ИННЮЛ" Тогда
			СертификатОблака.ИНН = ТекущееЗначение;
		ИначеЕсли СтрокаКлюча.Значение = "ИНН" И НЕ ЗначениеЗаполнено(СертификатОблака.ИНН) Тогда
			СертификатОблака.ИНН = ТекущееЗначение;
		Иначе
			СертификатОблака.Вставить(СтрокаКлюча.Значение, ТекущееЗначение);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат СертификатОблака;
			
КонецФункции

&НаКлиенте
Процедура ОткрытьСписокВключаемыхСертификатовОблачнойПодписи_ПослеВыбора(РезультатВыбора, ПараметрыЦикла) Экспорт
	
	ОповещениеОЗавершении = ПараметрыЦикла.ОповещениеОЗавершении;
	МестоХраненияКлюча = ПараметрыЦикла.МестоХраненияКлюча;
	Результат = Неопределено;
	
	Если РезультатВыбора.Выполнено Тогда
		ВключаемыйСертификат = СвойстваВыбранногоОблачногоСертификата(РезультатВыбора.Результат.ДанныеСертификата);
		ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ВключаемыйСертификат);
		Результат = Новый Структура();
		Результат.Вставить("ВключаемыйСертификат", ВключаемыйСертификат);
		Результат.Вставить("ВключаемыйСертификатОблачный", Ложь);
		Результат.Вставить("ЭтоСертификатДругогоУЦ", НЕ ЭтоСертификатКалуги(Результат.ВключаемыйСертификат));
		Результат.Вставить("МестоХраненияКлюча", МестоХраненияКлюча);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	
КонецПроцедуры

#Область ОблачнаяПодписьDSS

// универсальные функции и процедуры для работы с новой схемы анализа расположения закрытого ключа
//
&НаКлиентеНаСервереБезКонтекста
Функция ЭтоЛокальнаяПодпись(МестоХраненияКлюча)
	
	Возврат КриптографияЭДКОКлиентСервер.ЭтоЛокальнаяПодпись(МестоХраненияКлюча);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоПодписьСервиса(МестоХраненияКлюча)
	
	Возврат КриптографияЭДКОКлиентСервер.ЭтоПодписьСервиса(МестоХраненияКлюча);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЭтоОблачнаяПодпись(МестоХраненияКлюча)
	
	Возврат КриптографияЭДКОКлиентСервер.ЭтоОблачнаяПодпись(МестоХраненияКлюча);
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция КонтекстМоделиХраненияКлюча(УчетнаяЗапись, ПараметрыКонтекста = Неопределено)
	
	Возврат КриптографияЭДКОКлиентСервер.КонтекстМоделиХраненияКлюча(УчетнаяЗапись, ПараметрыКонтекста);

КонецФункции

&НаКлиентеНаСервереБезКонтекста
Процедура ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ПараметрыКонтекста)
	
	КриптографияЭДКОКлиентСервер.ЗаполнитьМестоХраненияКлюча(МестоХраненияКлюча, ПараметрыКонтекста);

КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция СвойствоСтруктуры(КонтейнерЗначений, ИмяКлюча, ЗначениеПоУмолчанию = Неопределено)
	
	Возврат ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(КонтейнерЗначений, ИмяКлюча, ЗначениеПоУмолчанию);
	
КонецФункции

&НаКлиенте
Функция ПодготовитьОблачнуюПодпись(ИмяФайла = Неопределено, ТипФайла = "txt", ТребуетПодтверждениеОперации = Ложь, ДополнитьСтруктуру = Неопределено)
	
	Результат = Новый Структура;
	
	Если Лев(ТипФайла, 1) = "." Тогда
		Результат.Вставить("ТипФайла", Сред(ТипФайла, 2));
	Иначе	
		Результат.Вставить("ТипФайла", ТипФайла);
	КонецЕсли;	
	
	Если ИмяФайла = Неопределено Тогда
		Результат.Вставить("ИмяФайла", "Данные для подписи");
	Иначе
		Результат.Вставить("ИмяФайла", ИмяФайла);
	КонецЕсли;
	
	Если ТипЗнч(ТребуетПодтверждениеОперации) = Тип("Структура") Тогда
		Результат.Вставить("ПодтверждатьПринудительно", СвойствоСтруктуры(ТребуетПодтверждениеОперации, "ТребуетПодтверждениеОперации", Ложь));
	Иначе
		Результат.Вставить("ПодтверждатьПринудительно", ТребуетПодтверждениеОперации);
	КонецЕсли;
	
	Если ДополнитьСтруктуру <> Неопределено Тогда
		ДополнитьСтруктуру.Вставить("ДанныеОблачнойПодписи", Результат);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Выполняет подписание пакета документов, поддерживаемый сервисом облачной подписи
//
// Параметры:
//	ОписаниеОповещения	- ОписаниеОповещения
//	МестоХраненияКлюча	- Структура
//	МассивДокументов	- Массив Из Структура
//	СертификатПодписи	- Структура
//	ПараметрыОперации	- Структура
//		* ПодтверждатьОперацию - Булево
//
&НаКлиенте
Процедура ПакетнаяПодписьОблачнойПодписи(
		ОписаниеОповещения,
		МестоХраненияКлюча,
		МассивДокументов,
		СертификатПодписи,
		ПараметрыОперации = Неопределено)
		
	УчетнаяЗапись = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
	КриптографияЭДКОКлиент.ПакетнаяПодписьОблачнойПодписи(ОписаниеОповещения, УчетнаяЗапись, МассивДокументов, СертификатПодписи, ПараметрыОперации);

КонецПроцедуры

&НаКлиенте
Процедура НовыйДокументПакетнойПодписи(
		МассивДокументов,
		ДанныеДокумента,
		ИмяФайла,
		Расширение,
		Счетчик = 0,
		ИсходноеСодержание = "")
		
	Если Лев(Расширение, 1) = "." Тогда
		ТипДокумента = Сред(Расширение, 2);
	Иначе	
		ТипДокумента = Расширение;
	КонецЕсли;	
	
	ИмяДокумента = ИмяФайла;
	Если ЗначениеЗаполнено(Счетчик) Тогда
		ИмяДокумента = ИмяДокумента + " - " + СокрЛП(Счетчик);
	КонецЕсли;	
	
	НоваяСтрока = Новый Структура; 
	НоваяСтрока.Вставить("Содержание", ДанныеДокумента);
	НоваяСтрока.Вставить("ИмяДокумента", ИмяДокумента);
	НоваяСтрока.Вставить("ТипДокумента", ТипДокумента);
	НоваяСтрока.Вставить("ИсходноеСодержание", ИсходноеСодержание);
	
	МассивДокументов.Добавить(НоваяСтрока);

КонецПроцедуры

&НаКлиенте
Функция РежимПодтвержденияОблачнойПодписи(ВидОперации, УчетнаяЗаписьКО, КонтекстОперации = Неопределено, ЗначениеПоУмолчанию = Неопределено)
	
	// 1 - По умолчанию, определяется в коде программе
	// 2 - Все отключено
	// 3 - Все подтверждать (режим тестирования)
	// 4 - Выборочный режим
	
	Если ЗначениеПоУмолчанию = Неопределено Тогда
		Результат = Истина;
	Иначе	
		Результат = ЗначениеПоУмолчанию;
	КонецЕсли;
	
	ТекущиеНастройки = ПрочитатьПараметрыПодтвержденияОблачнойПодписи(УчетнаяЗаписьКО);
	
	Если (ТекущиеНастройки.РежимПодтверждения = 1
		ИЛИ ТекущиеНастройки.РежимПодтверждения = 4) 
		И ЗначениеПоУмолчанию = Неопределено Тогда
		ТекущийРежим = ТекущиеНастройки.СписокОпераций[ВидОперации];
		Если ТекущийРежим <> Неопределено Тогда
			Результат = ТекущийРежим;
		Иначе
			Результат = Ложь;
		КонецЕсли;
	ИначеЕсли ТекущиеНастройки.РежимПодтверждения = 2 Тогда
		Результат = Ложь;
	ИначеЕсли ТекущиеНастройки.РежимПодтверждения = 3 Тогда
		Результат = Истина;
	КонецЕсли;
	
	Если КонтекстОперации <> Неопределено Тогда
		КонтекстОперации.Вставить("ТребуетПодтверждениеОперации", Результат);
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПрочитатьПараметрыПодтвержденияОблачнойПодписи(УчетнаяЗаписьКО) Экспорт
	
	ТекущиеНастройки = Неопределено;
	
	Если ПараметрыПодтвержденияОблачнойПодписи = Неопределено Тогда
		ПараметрыПодтвержденияОблачнойПодписи = Новый Соответствие;
	КонецЕсли;
	
	СсылкаСправочника = Неопределено;
	
	Если ТипЗнч(УчетнаяЗаписьКО) = Тип("СправочникСсылка.УчетныеЗаписиДокументооборота") Тогда
		СсылкаСправочника = УчетнаяЗаписьКО;
	ИначеЕсли ТипЗнч(УчетнаяЗаписьКО) = Тип("СправочникСсылка.Организации") Тогда
		СсылкаСправочника = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьКО, "УчетнаяЗаписьОбмена");
	ИначеЕсли ТипЗнч(УчетнаяЗаписьКО) = Тип("СправочникСсылка.ЦиклыОбмена") Тогда
		СсылкаСправочника = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьКО, "УчетнаяЗапись");
	ИначеЕсли ТипЗнч(УчетнаяЗаписьКО) = Тип("ДокументСсылка.ТранспортноеСообщение") Тогда
		СсылкаСправочника = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьКО, "УчетнаяЗапись");
	КонецЕсли;
	
	ТекущиеНастройки = ПараметрыПодтвержденияОблачнойПодписи[СсылкаСправочника];
	Если ТекущиеНастройки = Неопределено Тогда
		ТекущиеНастройки = ПолучитьПараметрыПодтвержденияОблачнойПодписи(УчетнаяЗаписьКО);
		ПараметрыПодтвержденияОблачнойПодписи.Вставить(СсылкаСправочника, ТекущиеНастройки);
	КонецЕсли;
	
	Возврат ТекущиеНастройки;
	
КонецФункции

&НаКлиенте
Процедура ЗаписатьПараметрыПодвержденияОблачнойПодписи(СсылкаСправочника, ТекущиеНастройки) Экспорт
	
	Если ПараметрыПодтвержденияОблачнойПодписи = Неопределено Тогда
		ПараметрыПодтвержденияОблачнойПодписи = Новый Соответствие;
	КонецЕсли;
	
	НовыеНастройки = ЗаписатьПараметрыПодвержденияОблачнойПодписиНаСервере(СсылкаСправочника, ТекущиеНастройки);
	ПараметрыПодтвержденияОблачнойПодписи.Вставить(СсылкаСправочника, НовыеНастройки);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗаписатьПараметрыПодвержденияОблачнойПодписиНаСервере(СсылкаСправочника, ТекущиеНастройки)
	
	СохраняемыеНастройки = ПолучитьПараметрыПодтвержденияОблачнойПодписи(ТекущиеНастройки, Истина);
	УстановитьПривилегированныйРежим(Истина);
	ОбщегоНазначения.ЗаписатьДанныеВБезопасноеХранилище(СсылкаСправочника, СохраняемыеНастройки, "ПодтверждениеОперацийОблачнойПодписи");
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат СохраняемыеНастройки;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьПараметрыПодтвержденияОблачнойПодписи(СсылкаСправочника, ЗаполнитьПринудительно = Ложь)
	
	ВнешнийМодуль = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Результат = ВнешнийМодуль.ПолучитьПараметрыПодтвержденияОблачнойПодписи(СсылкаСправочника, ЗаполнитьПринудительно);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ИзменитьНастройкиПодтвержденияОблачнойПодписи(ОповещениеОЗавершении, УчетнаяЗаписьДокументооборота) Экспорт
	
	Если КриптографияЭДКОСлужебныйКлиент.ИспользованиеОблачнойПодписиВозможно() Тогда
		ПараметрыЦикла = Новый Структура;
		ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
		ПараметрыЦикла.Вставить("УчетнаяЗаписьДокументооборота", УчетнаяЗаписьДокументооборота);
		
		ДанныеУчетнойЗаписи = ЭлектроннаяПодписьВМоделиСервисаБРОВызовСервера.РеквизитыУчетнойЗаписи(УчетнаяЗаписьДокументооборота);
		Если ЭтоОблачнаяПодпись(ДанныеУчетнойЗаписи.МодельХраненияЗакрытогоКлюча) Тогда
			ОповещениеСледующее = Новый ОписаниеОповещения("ИзменитьНастройкиПодтвержденияОблачнойПодписиПослеАвторизации", ЭтотОбъект, ПараметрыЦикла);
			МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
			МодульСервисКриптографииDSSКлиент.ПроверкаАутентификацииПользователя(ОповещениеСледующее, ДанныеУчетнойЗаписи.УчетнаяЗаписьОблачнойПодписи);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьНастройкиПодтвержденияОблачнойПодписиПослеАвторизации(РезультатВыполнения, ВходящийКонтекст) Экспорт

	Если РезультатВыполнения.Выполнено Тогда
		СтруктураПараметров = Новый Структура("УчетнаяЗаписьДокументооборота", ВходящийКонтекст.УчетнаяЗаписьДокументооборота);
		ОповещениеСледующее = Новый ОписаниеОповещения("ИзменитьНастройкиПодтвержденияОблачнойПодписиЗавершение", ЭтотОбъект, ВходящийКонтекст);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.НастройкаПодтвержденияОблачнойПодписи", СтруктураПараметров,,,,, ОповещениеСледующее, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьНастройкиПодтвержденияОблачнойПодписиЗавершение(РезультатВыполнения, ВходящийКонтекст) Экспорт

	Если ЗначениеЗаполнено(ВходящийКонтекст.ОповещениеОЗавершении) Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, РезультатВыполнения);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗарегистрироватьСертификатВХранилищеПолучателей(АдресДанныхСертификата, Хранилище, ОтпечатокСертификата = Неопределено)
	
	Если ТипЗнч(АдресДанныхСертификата) = Тип("ДвоичныеДанные") Тогда
		ДвоичныеДанныеСертификата = АдресДанныхСертификата;
	Иначе	
		ДвоичныеДанныеСертификата = ПолучитьИзВременногоХранилища(АдресДанныхСертификата);
	КонецЕсли;
	
	КриптографияЭДКО.ДобавитьСертификатПолучателя(ДвоичныеДанныеСертификата, Хранилище);
	СвойстваСертификата = КриптографияЭДКО.ПолучитьСвойстваСертификатаПолучателя(ДвоичныеДанныеСертификата);
	
	ОтпечатокСертификата = ?(СвойстваСертификата <> Неопределено, СвойстваСертификата.Отпечаток, Неопределено);
	Возврат (СвойстваСертификата <> Неопределено);
	
КонецФункции

&НаКлиенте
Процедура УстановитьСертификатыПолучателей(ПараметрыФункции, ВыполняемоеОповещение, ТолькоПолучатели = Ложь)
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ПараметрыФункции);
	МассивНовыхСертификатов = Новый Массив;
	РезультатВыполнения = РегистрацияУстановитьСертификатыОблачнойПодписиНаСервере(ПараметрыФункции.ОтпечатокСертификата, МассивНовыхСертификатов, МестоХраненияКлюча);
	Результат = Новый Структура("РезультатВыполнения, ПараметрыФункции", РезультатВыполнения, ПараметрыФункции);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Результат);
		
КонецПроцедуры

&НаКлиенте
Процедура УстановитьСертификатыЛокально(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ВыполняемоеОповещение = ВходящийКонтекст.ВыполняемоеОповещение;
	ПараметрыФункции = ВходящийКонтекст.ПараметрыФункции;
	
	РегистрируемыеСертификаты = Новый Массив;
	Для каждого Сертификат Из Сертификаты Цикл
		РегистрируемыйСертификат = Новый Структура("Отпечаток", Сертификат.Отпечаток);
		РегистрируемыеСертификаты.Добавить(РегистрируемыйСертификат);
	КонецЦикла;
	
	ДополнительныеПараметры = Новый Структура("ВыполняемоеОповещение, ПараметрыФункции", ВыполняемоеОповещение, ПараметрыФункции);
	Оповещение = Новый ОписаниеОповещения("РегистрацияУстановитьСертификатыПослеПоискаСертификатовЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, РегистрируемыеСертификаты, , Ложь);
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьДанныеДляЗапросаНаСертификат(УчетнаяЗаписьОблачнойПодписи, ПоляЗаявления)
	
	Результат = Новый Структура();
	Результат.Вставить("Выполнено", Ложь);
	
	МодульСервисКриптографииDSS = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSS");
	СводныеДанные = МодульСервисКриптографииDSS.ПолучитьИнформациюУдостоверяющихЦентров(УчетнаяЗаписьОблачнойПодписи);
	Если СводныеДанные.Заполнено Тогда
		
		ТекущийКриптоПровайдер = Неопределено;
		ТекущийШаблон = Неопределено;
		УдостоверяющийЦентр = Неопределено;
		ПоляАтрибутов = Неопределено;
		ИмяСертификата = "";
		
		Для Каждого СтрокаМассива Из СводныеДанные.Криптопровайдеры Цикл
			ТекущийКриптоПровайдер = СтрокаМассива.Идентификатор;
			Если СтрНайти(СтрокаМассива.Описание, "2012") Тогда
				Прервать;
			КонецЕсли;	
		КонецЦикла;	
		
		Для Каждого СтрокаМассива Из СводныеДанные.УдостоверяющиеЦентры Цикл
			ПоляУЦ = МодульСервисКриптографииDSS.ПолучитьРеестрПолейУдостоверяющегоЦентра(СтрокаМассива);
			НовыеПоля = Новый Соответствие;
			ВсеОбязательные = 0;
			ИмяСертификата = "";
			Для Каждого СтрокаПоля Из ПоляУЦ Цикл
				ВсеОбязательные = ВсеОбязательные + ?(СтрокаПоля.Обязательно, 1, 0);
				ЗначениеПоля = ПоляЗаявления[СтрокаПоля.OID];
				
				Если СтрокаПоля.Обязательно Тогда
					ИмяСертификата = ИмяСертификата + ?(ПустаяСтрока(ИмяСертификата), "", ",") + СтрокаПоля.Имя + "=" + ЗначениеПоля;
				КонецЕсли;	
				
				Если ЗначениеПоля <> Неопределено Тогда
					НовыеПоля.Вставить(СтрокаПоля.OID, ЗначениеПоля);
					ВсеОбязательные = ВсеОбязательные - ?(СтрокаПоля.Обязательно, 1, 0);
				КонецЕсли;
				
			КонецЦикла;
			
			Если НовыеПоля.Количество() >= ПоляЗаявления.Количество() 
				И ВсеОбязательные = 0 Тогда
				УдостоверяющийЦентр = СтрокаМассива;
				ПоляАтрибутов = НовыеПоля;
				Прервать;
			КонецЕсли;	

		КонецЦикла;
		
		Если УдостоверяющийЦентр <> Неопределено Тогда 
			ШаблоныУЦ = МодульСервисКриптографииDSS.ПолучитьРеестрШаблоновУдостоверяющегоЦентра(УдостоверяющийЦентр);
			Для Каждого СтрокаШаблона Из ШаблоныУЦ Цикл
				ТекущийШаблон = СтрокаШаблона.Шаблон;
				Если СтрНайти(СтрокаШаблона.Наименование, "2012") > 0 Тогда
					Прервать;
				КонецЕсли;	
			КонецЦикла;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТекущийКриптоПровайдер) Тогда
			Результат.Вставить("КриптоПровайдер", ТекущийКриптоПровайдер);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТекущийШаблон) Тогда
			Результат.Вставить("ШаблонСертификата", ТекущийШаблон);
		КонецЕсли;
		
		Если УдостоверяющийЦентр <> Неопределено Тогда 
			Результат.Вставить("УдостоверяющийЦентр", УдостоверяющийЦентр.Идентификатор);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИмяСертификата) Тогда
			ИмяСертификата = СтрЗаменить(ИмяСертификата, """", "");
			Результат.Вставить("ИмяСертификата", ИмяСертификата);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПоляАтрибутов) Тогда
			Результат.Вставить("ПоляАтрибутов", ПоляАтрибутов);
		КонецЕсли;
		
	КонецЕсли;	
	
	Если Результат.Количество() = 6 Тогда
		Результат.Выполнено = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция ФормироватьЗапросНаСертификатОблачнойПодписиСервер(МестоХраненияКлюча)
	
	Результат = Ложь;
	
	Если КриптографияЭДКОКлиентСервер.ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		УчетнаяЗаписьОблачнойПодписи = КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
		Если ЗначениеЗаполнено(УчетнаяЗаписьОблачнойПодписи) Тогда
			Результат = НЕ ОбработкаЗаявленийАбонентаВызовСервера.ЭтоПоставляемыйСерверОблачнойПодписи(УчетнаяЗаписьОблачнойПодписи);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ФормироватьЗапросНаСертификатОблачнойПодписи(МестоХраненияКлюча) Экспорт
	
	Результат = ФормироватьЗапросНаСертификатОблачнойПодписиСервер(МестоХраненияКлюча);
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнаяПодпись(ВходящийКонтекст)
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
	ОповещениеСледующее = Новый ОписаниеОповещения("ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнаяПодписьПродолжение", ЭтотОбъект, ВходящийКонтекст);
	ПараметрыОперации = Новый Структура("ПояснениеОперации", "Подключение к электронной подписи в программе для создания закрытого ключа");
	
	МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
	МодульСервисКриптографииDSSКлиент.ПроверкаАутентификацииПользователя(
			ОповещениеСледующее, 
			КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча),
			ПараметрыОперации);
	
КонецПроцедуры

&НаКлиенте
Функция ЗаявлениеСформироватьЗапросНаСертификатКвалифицированныйОП(ВходящийКонтекст)
	
	ДокументЗаявление    = ВходящийКонтекст.ДокументЗаявление;
	ПараметрыНовогоСертификата = ОбработкаЗаявленийАбонентаКлиент.ПараметрыНовогоСертификата(ДокументЗаявление);
	
	Результат = Новый Соответствие;
	
	Для Счетчик = 0 По ПараметрыНовогоСертификата.Количество()-1 Цикл 
		
		Если ПараметрыНовогоСертификата[Счетчик].Значение = "РегНомерФСС" Тогда
			Продолжить;
		КонецЕсли;
		
		Значение = ПараметрыНовогоСертификата[Счетчик].Значение;
		Представление = СокрЛП(Строка(ПараметрыНовогоСертификата[Счетчик].Представление));
		
		Если ПустаяСтрока(Представление) Тогда
			Представление = "0";
		КонецЕсли;
		
		Результат.Вставить(Значение, Представление);
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнаяПодписьПродолжение(РезультатВызова, ВходящийКонтекст) Экспорт
	
	Удачно = Ложь;
	
	Если РезультатВызова.Выполнено Тогда
		МестоХраненияКлюча 		= КонтекстМоделиХраненияКлюча(ВходящийКонтекст);
		ОблачнаяУчетнаяЗапись	= КриптографияЭДКОКлиентСервер.ПолучитьУчетнуюЗаписьПодписи(МестоХраненияКлюча);
		ПоляЗаявления 			= ЗаявлениеСформироватьЗапросНаСертификатКвалифицированныйОП(ВходящийКонтекст);
		ДанныеЗапроса 			= ПолучитьДанныеДляЗапросаНаСертификат(ОблачнаяУчетнаяЗапись, ПоляЗаявления);
		
		Если ДанныеЗапроса.Выполнено Тогда
		
			ПоляЗапроса = Новый Структура();
			ПоляЗапроса.Вставить("ПоляАтрибутов", ДанныеЗапроса.ПоляАтрибутов);
			ПоляЗапроса.Вставить("ШаблонСертификата", ДанныеЗапроса.ШаблонСертификата);
			ПоляЗапроса.Вставить("КриптоПровайдер", ДанныеЗапроса.КриптоПровайдер);
			ПоляЗапроса.Вставить("УдостоверяющийЦентр", ДанныеЗапроса.УдостоверяющийЦентр);
			ПоляЗапроса.Вставить("ИмяСертификата", ДанныеЗапроса.ИмяСертификата);
			
			ПараметрыОперации = Новый Структура();
			ПараметрыОперации.Вставить("ГотовыйЗапрос", Истина);
			ПараметрыОперации.Вставить("ПоляЗапроса", ПоляЗапроса);
			ПараметрыОперации.Вставить("ПояснениеОперации", "Создание закрытого ключа и запроса на сертификат");
			ПараметрыОперации.Вставить("ПодтверждатьПринудительно", Истина);
			
			ОповещениеСледующее = Новый ОписаниеОповещения("ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнаяПодписьПослеВыполнения", ЭтотОбъект, ВходящийКонтекст);
			
			МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
			МодульСервисКриптографииDSSКлиент.СоздатьЗапросНаСертификат(ОповещениеСледующее, ОблачнаяУчетнаяЗапись, ПараметрыОперации);
			
			Удачно = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	Если НЕ Удачно Тогда
		ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнойПодписи_Ошибка(ВходящийКонтекст);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнаяПодписьПослеВыполнения(РезультатВызова, ВходящийКонтекст) Экспорт
	
	СтруктураРезультата = Новый Структура();
	СтруктураРезультата.Вставить("ПродолжитьОтправкуЗаявления", РезультатВызова.Выполнено);
	СтруктураРезультата.Вставить("ПутьКонтейнерЗакрытогоКлюча", "");
	СтруктураРезультата.Вставить("ТекстЗапросаНаСертификат", "");
	СтруктураРезультата.Вставить("АлгоритмКонтейнераКлючей", "");
	СтруктураРезультата.Вставить("ТекстОшибки", "");
	
	Удачно = Ложь;
	ТекстОшибки = "";
	
	Если РезультатВызова.Выполнено Тогда
		СвойстваЗапроса = РезультатВызова.Результат;
		Если СвойстваЗапроса.Статус <> "Отклонен" Тогда
			ДанныеЗапроса = Base64Строка(СвойстваЗапроса.Содержание);
			ДанныеЗапроса = СтрЗаменить(ДанныеЗапроса, Символы.ВК, "");
			ДанныеЗапроса = СтрЗаменить(ДанныеЗапроса, Символы.ПС, "");
			ДанныеЗапроса = "-----BEGIN NEW CERTIFICATE REQUEST-----" + Символы.ПС
							+ СтрЗаменить(ДанныеЗапроса, Символы.ВК, "") + Символы.ПС
							+ "-----END NEW CERTIFICATE REQUEST-----";
			СтруктураРезультата.Вставить("ТекстЗапросаНаСертификат", ДанныеЗапроса);
			Удачно = Истина;
		Иначе
			ТекстОшибки = "Запрос на сертификат был отклонен.";
		КонецЕсли;	
	Иначе
		ТекстОшибки = РезультатВызова.Ошибка;
	КонецЕсли;
	
	Если НЕ Удачно Тогда
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
			НСтр("ru = 'Криптографическая ошибка электронной подписи в программе: не удалось сформировать запрос на сертификат для контролирующих органов.'"), 
			ВходящийКонтекст.ВызовИзМастераПодключенияК1СОтчетности, 
			ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности);
				
		СтруктураРезультата.Вставить("ТекстОшибки", ТекстОшибки);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, СтруктураРезультата);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаявлениеСформироватьЗакрытыйКлючИЗапросНаСертификатОблачнойПодписи_Ошибка(
		ВходящийКонтекст,
		ТекстОшибкиПриСозданииКлюча = Неопределено) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	ТекстОшибки = ?(ТекстОшибкиПриСозданииКлюча = Неопределено,
		НСтр("ru = 'Криптографическая ошибка электронной подписи в программе: не удалось создать контейнер закрытого ключа.'"), ТекстОшибкиПриСозданииКлюча);
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
		ТекстОшибки, 
		ВходящийКонтекст.ВызовИзМастераПодключенияК1СОтчетности, 
		ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности);
		
	Оповестить(НСтр("ru = 'Упрощенное заявление. Ошибка создание закрытого ключа'"), ТекстОшибки, ВходящийКонтекст.ДокументЗаявление);
		
	СтруктураРезультата = Новый Структура("ПродолжитьОтправкуЗаявления, ТекстОшибки, ПутьКонтейнерЗакрытогоКлюча", 
											Ложь, ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности, "");
											
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ВыполняемоеОповещение, СтруктураРезультата);
	
КонецПроцедуры

&НаСервере
Функция РегистрацияУстановитьСертификатыОблачнойПодписиНаСервере(ОтпечатокСертификата, СписокНовыхСертификатов, МестоХраненияКлюча)
	
	СписокНовыхСертификатов = Новый Массив;
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.РегистрацияУстановитьСертификатыОблачнойПодписи(ОтпечатокСертификата, СписокНовыхСертификатов, Сертификаты, МестоХраненияКлюча);
	
КонецФункции

&НаКлиенте
Процедура ЗарегистрироватьСертификатВСистемеОблачнаяПодпись(РезультатВызова, ДополнительныеПараметры) Экспорт
	
	Если РезультатВызова <> Неопределено Тогда
		РезультатРегистрации = ЗарегистрироватьСертификатВХранилищеПолучателей(
				РезультатВызова, 
				ДополнительныеПараметры.Хранилище, 
				ДополнительныеПараметры.ОтпечатокСертификата);
		Результат = Новый Структура("Выполнено, Отпечаток", РезультатРегистрации, ДополнительныеПараметры.ОтпечатокСертификата);
	Иначе
		Результат = Новый Структура("Выполнено, ТекстОшибки", Ложь, "Ошибка импорта сертификата оператора.");
	КонецЕсли;	
	ВыполнитьОбработкуОповещения(ДополнительныеПараметры.ВыполняемоеОповещение, Результат);
			
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНастройкиОблачнойПодписи(ОповещениеОЗавершении, 
				УчетнаяЗаписьОблачнойПодписи, 
				Логин, 
				СерверОблачнойПодписи, 
				Организация, 
				ПервичноеЗаявление = Ложь,
				ТолькоПросмотр = Ложь,
				ПараметрыОперации = Неопределено) Экспорт
				
	ИдентификаторФормы = СвойствоСтруктуры(ПараметрыОперации, "ИдентификаторФормы");
	ИНН = СвойствоСтруктуры(ПараметрыОперации, "ИНН", "");
	
	ПараметрыДлительнойОперации = ДлительнаяОтправкаКлиент.ПараметрыЗапускаУниверсальногоДлительногоДействия();
	ПараметрыДлительнойОперации.Надпись = НСтр("ru = 'Получение реестра сервисов электронной подписи...'");
	ПараметрыДлительнойОперации.Заголовок = НСтр("ru = 'Ожидание'");
	ПараметрыДлительнойОперации.ВыводитьПроценты = Истина;
	ПараметрыДлительнойОперации.Вставить("КонтекстДлительнойОперации", Неопределено);

	ПараметрыЦикла = Новый Структура();
	ПараметрыЦикла.Вставить("УчетнаяЗаписьОблачнойПодписи", УчетнаяЗаписьОблачнойПодписи);
	ПараметрыЦикла.Вставить("Логин", Логин);
	ПараметрыЦикла.Вставить("СерверОблачнойПодписи", СерверОблачнойПодписи);
	ПараметрыЦикла.Вставить("Организация", Организация);
	ПараметрыЦикла.Вставить("ПервичноеЗаявление", ПервичноеЗаявление);
	ПараметрыЦикла.Вставить("ТолькоПросмотр", ТолькоПросмотр);
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ПараметрыЦикла.Вставить("ИдентификаторФормы", ИдентификаторФормы);
	ПараметрыЦикла.Вставить("ЗакрытьФормуОжидания", ДлительнаяОтправкаКлиент.ПоказатьУниверсальнуюФормуДлительногоДействия(ПараметрыДлительнойОперации));
	ПараметрыЦикла.Вставить("ИНН", ИНН);
	
	ОповещениеСледущее = Новый ОписаниеОповещения("ОткрытьНастройкиОблачнойПодписиЗавершение", ЭтотОбъект, ПараметрыЦикла);
	
	ОткрытьНастройкиОблачнойПодписиФоновымЗаданием(ОповещениеСледущее, ПараметрыЦикла, ИдентификаторФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНастройкиОблачнойПодписиФоновымЗаданием(ОповещениеСледущее, ПараметрыЦикла, ИдентификаторФормы = Неопределено) Экспорт
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Получение списка сервисов электронной подписи'");
	ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение(СервисПолученияСпискаСервисовОблачнойПодписи(), ОповещениеСледущее, ПараметрыОжидания);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьНастройкиОблачнойПодписиЗавершение(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	УчетнаяЗаписьОблачнойПодписи = ВходящийКонтекст.УчетнаяЗаписьОблачнойПодписи;
	Логин = ВходящийКонтекст.Логин;
	Организация = ВходящийКонтекст.Организация;
	СерверОблачнойПодписи = ВходящийКонтекст.СерверОблачнойПодписи;
	ПервичноеЗаявление = ВходящийКонтекст.ПервичноеЗаявление;
	ТолькоПросмотр = ВходящийКонтекст.ТолькоПросмотр;
	ИдентификаторФормы = ВходящийКонтекст.ИдентификаторФормы;
	ЗакрытьФормуОжидания = ВходящийКонтекст.ЗакрытьФормуОжидания;
	ИНН = ВходящийКонтекст.ИНН;
	
	Если ЗакрытьФормуОжидания Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
	КонецЕсли;
	
	ПараметрыВызова = Новый Структура();
	ПараметрыВызова.Вставить("УчетнаяЗапись", УчетнаяЗаписьОблачнойПодписи);
	ПараметрыВызова.Вставить("Логин", Логин);
	ПараметрыВызова.Вставить("Организация", Организация);
	ПараметрыВызова.Вставить("СерверОблачнойПодписи", СерверОблачнойПодписи);
	ПараметрыВызова.Вставить("ПервичноеЗаявление", ПервичноеЗаявление);
	ПараметрыВызова.Вставить("ТолькоПросмотр", ТолькоПросмотр);
	ПараметрыВызова.Вставить("СписокСерверов", ПолучитьСписокСерверовОблачнойПодписи(РезультатВыполнения));
	ПараметрыВызова.Вставить("ИНН", ИНН);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.Мастер_НастройкиОблачнойПодписи", ПараметрыВызова, , ИдентификаторФормы, , , ОповещениеОЗавершении);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьВосстановлениеОблачнойПодписи(
		ОповещениеОЗавершении, 
		СписокСерверов, 
		Логин = "", 
		СерверОблачнойПодписи = "", 
		ПервичноеЗаявление = Ложь,
		ЭтоБыстрыйСтарт = Ложь) Экспорт
	
	ПараметрыЦикла = Новый Структура();
	ПараметрыЦикла.Вставить("СерверОблачнойПодписи", СерверОблачнойПодписи);
	ПараметрыЦикла.Вставить("Логин", Логин);
	ПараметрыЦикла.Вставить("СписокСерверов", СписокСерверов);
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ПараметрыЦикла.Вставить("ОткрытьСразу", СписокСерверов <> Неопределено);
	ПараметрыЦикла.Вставить("ПервичноеЗаявление", ПервичноеЗаявление);
	ПараметрыЦикла.Вставить("ЭтоБыстрыйСтарт", ЭтоБыстрыйСтарт);
	
	ОповещениеСледущее = Новый ОписаниеОповещения("ОткрытьВосстановлениеОблачнойПодписиЗавершение", ЭтотОбъект, ПараметрыЦикла);
	
	Если НЕ ПараметрыЦикла.ОткрытьСразу Тогда
		ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
		ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
		ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Получение списка сервисов электронной подписи'");
		ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
		
		ДлительныеОперацииКлиент.ОжидатьЗавершение(СервисПолученияСпискаСервисовОблачнойПодписи(), ОповещениеСледущее, ПараметрыОжидания);
		
	Иначе
		ВыполнитьОбработкуОповещения(ОповещениеСледущее, Неопределено);
		
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьВосстановлениеОблачнойПодписиЗавершение(РезультатВыполнения, ВходящийКонтекст) Экспорт
	
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	ОткрытьСразу = ВходящийКонтекст.ОткрытьСразу;
	СерверОблачнойПодписи = ВходящийКонтекст.СерверОблачнойПодписи;
	Логин = ВходящийКонтекст.Логин;
	СписокСерверов = ВходящийКонтекст.СписокСерверов;
	ПервичноеЗаявление = ВходящийКонтекст.ПервичноеЗаявление;
	
	ПараметрыВызова = Новый Структура();
	ПараметрыВызова.Вставить("СерверОблачнойПодписи", СерверОблачнойПодписи);
	ПараметрыВызова.Вставить("Логин", Логин);
	Если ОткрытьСразу Тогда
		ПараметрыВызова.Вставить("СписокСерверов", СписокСерверов);
	Иначе
		ПараметрыВызова.Вставить("СписокСерверов", ПолучитьСписокСерверовОблачнойПодписи(РезультатВыполнения));
	КонецЕсли;	
	ПараметрыВызова.Вставить("ПервичноеЗаявление", ПервичноеЗаявление);
	ПараметрыВызова.Вставить("ЭтоБыстрыйСтарт", ВходящийКонтекст.ЭтоБыстрыйСтарт);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.Мастер_ВосстановитьОблачнуюПодпись", ПараметрыВызова, , , , , ОповещениеОЗавершении);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявления(КонтекстФормы, МастерДалее, ПервичноеЗаявление = Ложь, ОповещениеОЗавершении = Неопределено) Экспорт
	
	МестоХраненияКлюча = КонтекстФормы.МестоХраненияКлюча;
	
	Если НЕ МастерДалее ИЛИ НЕ КриптографияЭДКОКлиентСервер.ЭтоОблачнаяПодпись(МестоХраненияКлюча) Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыЦикла = Новый Структура();
	ПараметрыЦикла.Вставить("КонтекстФормы", КонтекстФормы);
	ПараметрыЦикла.Вставить("ПервичноеЗаявление", ПервичноеЗаявление);
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияНачало(ПараметрыЦикла, МастерДалее);
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияНачало(ПараметрыЦикла, МастерДалее = Ложь)
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	ПервичноеЗаявление = ПараметрыЦикла.ПервичноеЗаявление;
	СвойствоОблачнойПодписи = КонтекстФормы.СвойствоОблачнойПодписи;
	
	ОповещениеСледующее = Новый ОписаниеОповещения("ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПродолжение", ЭтотОбъект, ПараметрыЦикла);
	МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
	
	Если СвойствоОблачнойПодписи.СоздатьУчетнуюЗапись И НЕ ЗначениеЗаполнено(СвойствоОблачнойПодписи.НовыйЛогин) Тогда
		МастерДалее = Ложь;
		ТекстСообщения = НСтр("ru = 'Не указаны настройки новой учетной записи электронной подписи.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
		
	ИначеЕсли НЕ ЗначениеЗаполнено(СвойствоОблачнойПодписи.Телефон) И СвойствоОблачнойПодписи.СоздатьУчетнуюЗапись И НЕ ПервичноеЗаявление Тогда
		МастерДалее = Ложь;
		ТекстВопроса = НСтр("ru = 'Необходимо указать номер мобильного телефона для регистрации мобильного приложения.'");
		
		СписокОтветов = Новый СписокЗначений;
		СписокОтветов.Добавить("Контакты", НСтр("ru = 'Ввести номер телефона'"));
		СписокОтветов.Добавить("Отмена", НСтр("ru = 'Отмена'"));
		
		МодульСервисКриптографииDSSКлиент.ВывестиВопрос(ОповещениеСледующее, ТекстВопроса, СписокОтветов);
		
	ИначеЕсли СвойствоОблачнойПодписи.УчетнаяЗаписьДляВосстановления = СвойствоОблачнойПодписи.НовыйЛогин И СвойствоОблачнойПодписи.СоздатьУчетнуюЗапись Тогда
		МастерДалее = Ложь;
		ТекстВопроса = НСтр("ru = 'Учетная запись с логином <b>%1</b> существует на сервере 1C:DSS.'") 
						+ " " + НСтр("ru = 'Необходимо восстановить учетную запись электронной подписи с этим логином.'");
		ТекстВопроса = СтрШаблон(ТекстВопроса, СвойствоОблачнойПодписи.НовыйЛогин);
		ТекстВопроса = СтроковыеФункцииКлиент.ФорматированнаяСтрока(ТекстВопроса);
		СписокОтветов = Новый СписокЗначений;
		СписокОтветов.Добавить("Восстановить", НСтр("ru = 'Восстановить учетную запись'"));
		СписокОтветов.Добавить("Другой", НСтр("ru = 'Указать другую учетную запись'"));
		СписокОтветов.Добавить("Отмена", НСтр("ru = 'Отмена'"));
		
		МодульСервисКриптографииDSSКлиент.ВывестиВопрос(ОповещениеСледующее, ТекстВопроса, СписокОтветов);
		
	ИначеЕсли СвойствоОблачнойПодписи.СоздатьУчетнуюЗапись И НЕ ЗначениеЗаполнено(СвойствоОблачнойПодписи.ИдентификаторСервера) Тогда
		
		МастерДалее = Ложь;
		ТекстВопроса = НСтр("ru = 'Не выбран сервис электронной подписи, на котором будет создана новая учетная запись.'");
		
		СписокОтветов = Новый СписокЗначений;
		СписокОтветов.Добавить("Другой", НСтр("ru = 'Настроить'"));
		СписокОтветов.Добавить("Отмена", НСтр("ru = 'Отмена'"));
		
		МодульСервисКриптографииDSSКлиент.ВывестиВопрос(ОповещениеСледующее, ТекстВопроса, СписокОтветов);
		
	ИначеЕсли НЕ ЗначениеЗаполнено(СвойствоОблачнойПодписи.УчетнаяЗапись) И НЕ СвойствоОблачнойПодписи.НовыйЛогинПроверен Тогда
		ОповещениеСледующее = Новый ОписаниеОповещения("ПроверитьНастройкиОблачнойПодписиПриОтправке_ПоискДублей", ЭтотОбъект, ПараметрыЦикла);
		МастерДалее = Ложь;
		ПроверитьУникальностьЛогинаОблачнойПодписи(ОповещениеСледующее, СвойствоОблачнойПодписи.ИдентификаторСервера, СвойствоОблачнойПодписи.НовыйЛогин);		
		
	ИначеЕсли НЕ МастерДалее Тогда
		ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявлениЗавершение(ПараметрыЦикла);
		
	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправке_ПоискДублей(РезультатВыбора, ПараметрыЦикла) Экспорт
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	СвойствоОблачнойПодписи = КонтекстФормы.СвойствоОблачнойПодписи;
	Статус = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыбора, "Статус", "");
	
	Если Статус = "Найден" Тогда
		ТекстСообщения = НСтр("ru = 'Учетная запись с логином %1 существует в справочнике учетных записей.'") 
						+ " " + НСтр("ru = 'Выберите нужную учетную запись.'");
		ТекстСообщения = СтрШаблон(ТекстСообщения, СвойствоОблачнойПодписи.НовыйЛогин);
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
		
	ИначеЕсли Статус = "Дублирование" Тогда
		ТекстСообщения = НСтр("ru = 'Учетная запись с логином %1 обнаружена в сервисе электронной подписи.'") 
						+ " " + НСтр("ru = 'Необходимо восстановить учетную запись электронной подписи с этим логином.'");
		ТекстСообщения = СтрШаблон(ТекстСообщения, СвойствоОблачнойПодписи.НовыйЛогин);
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
		
	Иначе
		КонтекстФормы.СвойствоОблачнойПодписи.НовыйЛогинПроверен = Истина;
		ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияНачало(ПараметрыЦикла);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПродолжение(РезультатВыбора, ПараметрыЦикла) Экспорт
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	СвойствоОблачнойПодписи = КонтекстФормы.СвойствоОблачнойПодписи;
	Организация = КонтекстФормы.Организация;
	ПервичноеЗаявление = ПараметрыЦикла.ПервичноеЗаявление;
	
	РезультатВыполнения = ?(РезультатВыбора.Выполнено, РезультатВыбора.Результат, "");
	
	Если РезультатВыполнения = "Восстановить" Тогда
		
		ОповещениеСледующее = Новый ОписаниеОповещения("ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПослеВосстановления", ЭтотОбъект, ПараметрыЦикла);
		ОткрытьВосстановлениеОблачнойПодписи(ОповещениеСледующее, Неопределено, СвойствоОблачнойПодписи.НовыйЛогин, , ПервичноеЗаявление);
		
	ИначеЕсли РезультатВыполнения = "Другой" Тогда
		ОповещениеСледующее = Новый ОписаниеОповещения("ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПослеВыбора", ЭтотОбъект, ПараметрыЦикла);
		ОткрытьНастройкиОблачнойПодписи(
					ОповещениеСледующее, 
					СвойствоОблачнойПодписи.УчетнаяЗапись, 
					СвойствоОблачнойПодписи.НовыйЛогин, 
					СвойствоОблачнойПодписи.ИдентификаторСервера, 
					Организация, 
					ПервичноеЗаявление);
		
	ИначеЕсли РезультатВыполнения = "Контакты" Тогда
		
		ОповещениеСледующее = Новый ОписаниеОповещения(
			"ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПослеВводаКонтактов", 
			ЭтотОбъект, 
			ПараметрыЦикла);
			
		ДополнительныеНастройкиОблачнойЗаписиВЗаявлении(ОповещениеСледующее, КонтекстФормы, Истина);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПослеВосстановления(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	СвойствоОблачнойПодписи = КонтекстФормы.СвойствоОблачнойПодписи;
	
	Если ЗначениеЗаполнено(РезультатВыполнения) Тогда
		СвойствоОблачнойПодписи.УчетнаяЗапись = РезультатВыполнения;
		СвойствоОблачнойПодписи.УчетнаяЗаписьДляВосстановления = Ложь;
		СвойствоОблачнойПодписи.СоздатьУчетнуюЗапись = Ложь;
		ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияНачало(ПараметрыЦикла);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПослеВводаКонтактов(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	СвойствоОблачнойПодписи = КонтекстФормы.СвойствоОблачнойПодписи;
	
	Если РезультатВыполнения.Выполнено Тогда
		ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияНачало(ПараметрыЦикла);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияПослеВыбора(РезультатВыполнения, ПараметрыЦикла) Экспорт
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	СвойствоОблачнойПодписи = КонтекстФормы.СвойствоОблачнойПодписи;
	
	Если ТипЗнч(РезультатВыполнения) = Тип("Структура") Тогда
		СвойствоОблачнойПодписи.УчетнаяЗапись = РезультатВыполнения.УчетнаяЗаписьОблачнойПодписи;
		СвойствоОблачнойПодписи.ИдентификаторСервера = РезультатВыполнения.СерверОблачнойПодписи;
		СвойствоОблачнойПодписи.СоздатьУчетнуюЗапись = НЕ ЗначениеЗаполнено(СвойствоОблачнойПодписи.УчетнаяЗапись);
		СвойствоОблачнойПодписи.НовыйЛогин = РезультатВыполнения.Логин;
		ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявленияНачало(ПараметрыЦикла);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьНастройкиОблачнойПодписиПриОтправкеЗаявлениЗавершение(ПараметрыЦикла)
	
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	ОповещениеОЗавершении = ПараметрыЦикла.ОповещениеОЗавершении;
	
	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Функция ПроверитьУчетнуюЗаписьОблачнойПодписи(ОблачнаяПодписьУчетнаяЗапись, ВладелецЭЦПСНИЛС, ИНН) Экспорт
	
	Результат = ПроверитьУчетнуюЗаписьОблачнойПодписиНаСервере(ОблачнаяПодписьУчетнаяЗапись, ВладелецЭЦПСНИЛС, ИНН);
	
	Возврат Результат;
	
КонецФункции	

&НаСервереБезКонтекста
Функция ПроверитьУчетнуюЗаписьОблачнойПодписиНаСервере(ОблачнаяПодписьУчетнаяЗапись, ВладелецЭЦПСНИЛС, ИНН)
	
	Результат = Ложь;
	ВсеНастройки = КриптографияЭДКО.ДополнительныеНастройкиОблачнойПодписи(ОблачнаяПодписьУчетнаяЗапись);
	
	Если НЕ ВсеНастройки.Существует Тогда
		ПараметрыФункции = Новый Структура;
		ПараметрыФункции.Вставить("Сервер", ОблачнаяПодписьУчетнаяЗапись.Владелец.ВнутреннийИдентификатор);
		ПараметрыФункции.Вставить("Логин", ОблачнаяПодписьУчетнаяЗапись.Логин);
		ВнешнийМодуль = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		ВсеНастройки = ВнешнийМодуль.ОбменССерверомСвойстваУчетнойЗаписиОблачнойПодписи(ПараметрыФункции);
	КонецЕсли;	
	
	Если ВсеНастройки.Существует Тогда
		Результат = ВРег(ВсеНастройки.СНИЛС) = ВРег(ВладелецЭЦПСНИЛС) И ВРег(ВсеНастройки.ИНН) = ВРег(ИНН);
	Иначе
		Результат = Истина;
	КонецЕсли;
	
	Результат = Истина;
	
	Возврат Результат;
	
КонецФункции	

&НаСервереБезКонтекста
Функция СервисПолученияСпискаСервисовОблачнойПодписи(Знач ИдентификаторФормы = Неопределено)
	
	ИдентификаторФормы = ?(ИдентификаторФормы = Неопределено, Новый УникальныйИдентификатор, ИдентификаторФормы);
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(ИдентификаторФормы);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Вызов API Калуги-Астрал'");
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	ПараметрыВыполнения.ДополнительныйРезультат = Ложь;
	
	ПараметрыВызова = Новый Структура();
	ПараметрыВызова.Вставить("СпецоператорСвязи", Перечисления.СпецоператорыСвязи.КалугаАстрал);
	
	Результат = ДлительныеОперации.ВыполнитьВФоне(
					"Обработки.ДокументооборотСКонтролирующимиОрганами.ОбменССерверомСписокСерверовОблачнойПодписиФоновое", 
					ПараметрыВызова, 
					ПараметрыВыполнения);
					
	Возврат Результат;

КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьСписокСерверовОблачнойПодписи(РезультатВыполнения = Неопределено, ДобавлятьВсеСервера = Ложь) Экспорт
	
	Результат = Новый Массив;
	ВсеИД = Новый Массив;
	
	МодульСервисКриптографииDSS = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSS");
	СервисыБазы = МодульСервисКриптографииDSS.ПолучитьВсеСерверы();
	
	МассивСерверов = Новый Массив;
	Если РезультатВыполнения = Неопределено Тогда
		ПараметрыФункции = Новый Структура;
		ПараметрыФункции.Вставить("СпецоператорСвязи", Перечисления.СпецоператорыСвязи.КалугаАстрал);
		МодульСервера = ДокументооборотСКО.ПолучитьОбработкуЭДО();
		МассивСерверов = МодульСервера.ОбменССерверомСписокСерверовОблачнойПодписи(ПараметрыФункции);
	Иначе
		Если РезультатВыполнения.Статус = "Выполнено" Тогда
			МассивСерверов = ПолучитьИзВременногоХранилища(РезультатВыполнения.АдресРезультата);
		КонецЕсли;	
	КонецЕсли;
	
	Для Каждого СтрокаМассива Из МассивСерверов Цикл
		Если ВсеИД.Найти(СтрокаМассива.ВнутреннийИдентификатор) = Неопределено Тогда
			НоваяСтрока = Новый Структура();
			НоваяСтрока.Вставить("Ссылка", Неопределено);
			НоваяСтрока.Вставить("Представление", СтрокаМассива.Наименование);
			НоваяСтрока.Вставить("ВнутреннийИдентификатор", СтрокаМассива.ВнутреннийИдентификатор);
			НоваяСтрока.Вставить("ДанныеСервиса", СтрокаМассива);
			НашлиСтроки = СервисыБазы.НайтиСтроки(Новый Структура("ВнутреннийИдентификатор", СтрокаМассива.ВнутреннийИдентификатор));
			Если НашлиСтроки.Количество() > 0 Тогда
				НоваяСтрока.Вставить("Ссылка", НашлиСтроки[0].Ссылка);
			КонецЕсли;	
			Результат.Добавить(НоваяСтрока);
			ВсеИД.Добавить(СтрокаМассива.ВнутреннийИдентификатор)
		КонецЕсли;
	КонецЦикла;
		
	Если ДобавлятьВсеСервера Тогда
		Для Каждого СтрокаМассива Из СервисыБазы Цикл
			Если ЗначениеЗаполнено(СтрокаМассива.ВнутреннийИдентификатор) 
				И ВсеИД.Найти(СтрокаМассива.ВнутреннийИдентификатор) = Неопределено Тогда
				НоваяСтрока = Новый Структура();
				НоваяСтрока.Вставить("Ссылка", СтрокаМассива.Ссылка);
				НоваяСтрока.Вставить("Представление", СтрокаМассива.Наименование);
				НоваяСтрока.Вставить("ВнутреннийИдентификатор", СтрокаМассива.ВнутреннийИдентификатор);
				НоваяСтрока.Вставить("ДанныеСервиса", Неопределено);
				Результат.Добавить(НоваяСтрока);
				ВсеИД.Добавить(СтрокаМассива.ВнутреннийИдентификатор)
			КонецЕсли;
		КонецЦикла;	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция АктивироватьУчетнуюЗаписьОблачнойПодписиНаСервере(ДанныеПодписи, ПодписьПароля, ИдентификаторСубъекта)
	
	АдресРезультата = ПоместитьВоВременноеХранилище(Неопределено, Новый УникальныйИдентификатор);
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("ПодписьПароля", ПодписьПароля);
	ПараметрыВызова.Вставить("ИдентификаторСубъекта", ИдентификаторСубъекта);
	ПараметрыВызова.Вставить("ДанныеПодписи", ДанныеПодписи);
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(Новый УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Вызов API Калуги-Астрал'");
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	ПараметрыВыполнения.ДополнительныйРезультат = Ложь;
	
	Результат = ДлительныеОперации.ВыполнитьВФоне(
					"Обработки.ДокументооборотСКонтролирующимиОрганами.ОбменССерверомАктивироватьУчетнуюЗаписьФоновое", 
					ПараметрыВызова, 
					ПараметрыВыполнения);
					
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция АктивироватьКлючПриложенияУчетнойЗаписиОблачнойПодписиНаСервере(ПодписьКлючаМобильногоПриложения, ИдентификаторСубъекта)
	
	АдресРезультата = ПоместитьВоВременноеХранилище(Неопределено, Новый УникальныйИдентификатор);
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("ПодписьКлючаМобильногоПриложения", ПодписьКлючаМобильногоПриложения);
	ПараметрыВызова.Вставить("ИдентификаторСубъекта", ИдентификаторСубъекта);
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(Новый УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Вызов API Калуги-Астрал'");
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	ПараметрыВыполнения.ДополнительныйРезультат = Ложь;
	
	Результат = ДлительныеОперации.ВыполнитьВФоне(
					"Обработки.ДокументооборотСКонтролирующимиОрганами.ОбменССерверомАктивироватьКлючПриложенияУчетнойЗаписиФоновое", 
					ПараметрыВызова, 
					ПараметрыВыполнения);
					
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОбменССерверомОтправитьКодАктивацииКлючаОблачнойПодписиНаСервере(ПодписьКодаАктивации, ИдентификаторСубъекта)
	
	АдресРезультата = ПоместитьВоВременноеХранилище(Неопределено, Новый УникальныйИдентификатор);
	
	ПараметрыВызова = Новый Структура;
	ПараметрыВызова.Вставить("ПодписьКодаАктивации", ПодписьКодаАктивации);
	ПараметрыВызова.Вставить("ИдентификаторСубъекта", ИдентификаторСубъекта);
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(Новый УникальныйИдентификатор);
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Вызов API Калуги-Астрал'");
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	ПараметрыВыполнения.ДополнительныйРезультат = Ложь;
	
	Результат = ДлительныеОперации.ВыполнитьВФоне(
					"Обработки.ДокументооборотСКонтролирующимиОрганами.ОбменССерверомОтправитьКодАктивацииКлючаФоновое", 
					ПараметрыВызова, 
					ПараметрыВыполнения);
					
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ПредставлениеДополнительныхНастроекОблачнойПодписи(РеквизитыЗаявления) Экспорт
	
	Результат = "";
	МассивСтрок = Новый Массив;
	
	Если РеквизитыЗаявления.СменитьПароль Тогда
		МассивСтрок.Добавить(НСтр("ru = 'Смена пароля'"));
	КонецЕсли;
	
	Если РеквизитыЗаявления.СменитьКлючМобильногоПриложения
		ИЛИ РеквизитыЗаявления.СменитьКлючМобильногоПриложенияАвтоматически Тогда
		МассивСтрок.Добавить(НСтр("ru = 'Смена ключа мобильного приложения'"));
	КонецЕсли;
	
	Если РеквизитыЗаявления.ОтправкаКодаАктивации Тогда
		МассивСтрок.Добавить(НСтр("ru = 'Повторная отправка кода активации'"));
	КонецЕсли;
	
	Если РеквизитыЗаявления.ОтправкаКлючаМобильногоПриложения Тогда
		МассивСтрок.Добавить(НСтр("ru = 'Повторная отправка QR-кода'"));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(РеквизитыЗаявления.Телефон)
		ИЛИ ЗначениеЗаполнено(РеквизитыЗаявления.ЭлектроннаяПочта) Тогда
		МассивСтрок.Добавить(НСтр("ru = 'Отдельные контакты'"));
	КонецЕсли;
	
	ЗаголовокНастроек = НСтр("ru = 'Дополнительные настройки электронной подписи'");
	
	Если МассивСтрок.Количество() = 0 Тогда
		ЗаголовокНастроек = "Для электронной подписи не заданы настройки";
		МассивСтрок.Добавить(НСтр("ru = 'Настроить'"));
	КонецЕсли;
	
	Результат = "<span>" + ЗаголовокНастроек + "</span>" + Символы.ПС;
	ЕстьЗапятая = Ложь;
	Для Каждого СтрокаМассива Из МассивСтрок Цикл
		Результат = Результат + ?(ЕстьЗапятая, ", ", "") + "<a href=""Ссылка"">" + СтрокаМассива + "</a>";
		ЕстьЗапятая = Истина;
	КонецЦикла;
	
	Возврат СтроковыеФункцииКлиент.ФорматированнаяСтрока(Результат);
	
КонецФункции

&НаКлиенте
Процедура ДополнительныеНастройкиОблачнойЗаписиВЗаявлении(ОповещениеОЗавершении, КонтекстФормы, ЭтоПроверка = Ложь) Экспорт
	
	ЭтоЗаявлениеНаПодключение = ОбработкаЗаявленийАбонентаКлиентСервер.ЭтоФормаПервичногоЗаявления(КонтекстФормы);
	
	ПараметрыЦикла = Новый Структура;
	ПараметрыЦикла.Вставить("КонтекстФормы", КонтекстФормы);
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ОповещениеСледующее = Новый ОписаниеОповещения("ДополнительныеНастройкиОблачнойЗаписиВЗаявленииЗавершении", ЭтотОбъект, ПараметрыЦикла);
	
	РеквизитыЗаявления = ОбработкаЗаявленийАбонентаКлиентСервер.СведенияОблачнойПодписиЗаявления();
	ЗаполнитьЗначенияСвойств(РеквизитыЗаявления, КонтекстФормы.СвойствоОблачнойПодписи);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ЭтоЗаявлениеНаПодключение", ЭтоЗаявлениеНаПодключение);
	ПараметрыФормы.Вставить("ПереизданиеСертификата", (ЭтоЗаявлениеНаПодключение ИЛИ КонтекстФормы.ПереиздатьСертификат <> 0)
								И ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(КонтекстФормы));
	ПараметрыФормы.Вставить("УчетнаяЗапись", РеквизитыЗаявления.УчетнаяЗапись);
	ПараметрыФормы.Вставить("Телефон", РеквизитыЗаявления.Телефон);
	ПараметрыФормы.Вставить("ЭлектроннаяПочта", РеквизитыЗаявления.ЭлектроннаяПочта);
	ПараметрыФормы.Вставить("Логин", РеквизитыЗаявления.НовыйЛогин);
	ПараметрыФормы.Вставить("СменаКлючаМобильногоПриложения", РеквизитыЗаявления.СменитьКлючМобильногоПриложения);
	ПараметрыФормы.Вставить("СменаКлючаМобильногоПриложенияАвтоматически", РеквизитыЗаявления.СменитьКлючМобильногоПриложенияАвтоматически);
	ПараметрыФормы.Вставить("СменаПароля", РеквизитыЗаявления.СменитьПароль);
	ПараметрыФормы.Вставить("ПовторнаяОтправкаКодаАктивации", РеквизитыЗаявления.ОтправкаКодаАктивации);
	ПараметрыФормы.Вставить("ПовторнаяОтправкаКлючМобильногоПриложения", РеквизитыЗаявления.ОтправкаКлючаМобильногоПриложения);
	ПараметрыФормы.Вставить("ЭтоПроверка", ЭтоПроверка);
	
	Если ЭтоЗаявлениеНаПодключение И НЕ ЗначениеЗаполнено(ПараметрыФормы.Телефон) Тогда
		ПараметрыФормы.Телефон = КриптографияЭДКОКлиентСервер.ТелефонПодтвержденияОпераций(КонтекстФормы.ТелефонМобильный);
	КонецЕсли;
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.НастройкиОблачнойПодписиВЗаявлении", ПараметрыФормы, , , , , ОповещениеСледующее);
	
КонецПроцедуры

&НаКлиенте
Процедура ДополнительныеНастройкиОблачнойЗаписиВЗаявленииЗавершении(РезультатВыбора, ПараметрыЦикла) Экспорт
	
	Результат = Новый Структура("Выполнено, Состояние", Ложь, "");
	КонтекстФормы = ПараметрыЦикла.КонтекстФормы;
	ОповещениеОЗавершении = ПараметрыЦикла.ОповещениеОЗавершении;
	
	Если ТипЗнч(РезультатВыбора) = Тип("Структура") Тогда
		РеквизитыЗаявления = ОбработкаЗаявленийАбонентаКлиентСервер.СведенияОблачнойПодписиЗаявления();
		ЗаполнитьЗначенияСвойств(РеквизитыЗаявления, КонтекстФормы.СвойствоОблачнойПодписи);
		
		РеквизитыЗаявления.ЭлектроннаяПочта = РезультатВыбора.ЭлектроннаяПочта;
		РеквизитыЗаявления.Телефон = РезультатВыбора.Телефон;
		РеквизитыЗаявления.СменитьКлючМобильногоПриложения = РезультатВыбора.СменаКлючаМобильногоПриложения;
		РеквизитыЗаявления.СменитьПароль = РезультатВыбора.СменаПароля;
		РеквизитыЗаявления.ОтправкаКодаАктивации = РезультатВыбора.ПовторнаяОтправкаКодаАктивации;
		РеквизитыЗаявления.ОтправкаКлючаМобильногоПриложения = РезультатВыбора.ПовторнаяОтправкаКлючМобильногоПриложения;
		
		ЗаполнитьЗначенияСвойств(КонтекстФормы.СвойствоОблачнойПодписи, РеквизитыЗаявления);
		
		Результат.Выполнено = Истина;
		Результат.Состояние = ПредставлениеДополнительныхНастроекОблачнойПодписи(РеквизитыЗаявления);
		
	КонецЕсли;
	
	Если ОповещениеОЗавершении <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьИнформациюОбОблачнойПодписи(ФормаВладелец, ОбработкаОповещения = Неопределено) Экспорт
	
	ПараметрыФормы = Новый Структура;
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ИнформацияОбОблачнойПодписи", 
				ПараметрыФормы, 
				ФормаВладелец, 
				Новый УникальныйИдентификатор(), , , 
				ОбработкаОповещения,
				РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СписокУчетыхЗаписейОблачнойПодписи(ОтборЗаписей = Неопределено)
	
	ВсеЗаписи = Новый ТаблицаЗначений;
	
	Если КриптографияЭДКО.ИспользованиеОблачнойПодписиВозможно() Тогда
		МодульСервисКриптографииDSS = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSS");
		ВсеЗаписи = МодульСервисКриптографииDSS.ПолучитьВсеУчетныеЗаписи();
	КонецЕсли;
	
	ВсеЗаписи.Колонки.Добавить("ПризнакОтбора", Новый ОписаниеТипов("Булево"));
	ВсеЗаписи = ОбщегоНазначения.ТаблицаЗначенийВМассив(ВсеЗаписи);
	
	Если ОтборЗаписей <> Неопределено Тогда
		
		ОтборПоставляемых = СвойствоСтруктуры(ОтборЗаписей, "ПоставляемыйСервер");
		ОтборНаименования = СвойствоСтруктуры(ОтборЗаписей, "Наименование");
		
		Счетчик = ВсеЗаписи.Количество() - 1;
		Пока Счетчик >= 0 Цикл
			ТекущаяЗапись = ВсеЗаписи[Счетчик];
			Если ОтборНаименования <> Неопределено И СтрНайти(ТекущаяЗапись.Наименование, ОтборНаименования) > 0 Тогда
				ТекущаяЗапись.ПризнакОтбора = Истина;
			КонецЕсли;
			
			Если ОтборПоставляемых <> Неопределено И НЕ ЗначениеЗаполнено(ТекущаяЗапись.ВнутреннийИдентификатор) = ОтборПоставляемых Тогда
				ВсеЗаписи.Удалить(Счетчик);
			КонецЕсли;
			
			Счетчик = Счетчик - 1;
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат ВсеЗаписи;
	
КонецФункции

&НаКлиенте
Процедура ВыбратьУчетнуюЗаписьОблачнойПодписи(ОповещениеОЗавершении, ФормаВладелец, ПараметрыОперации = Неопределено, ОтборЗаписей = Неопределено) Экспорт
	
	ПараметрыЦикла = Новый Структура;
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	ПараметрыЦикла.Вставить("Сообщение", Ложь);
	
	ИмяОтбора = СвойствоСтруктуры(ПараметрыОперации, "ПредставлениеОтбора", "");
	ТекущаяЗапись = СвойствоСтруктуры(ПараметрыОперации, "ТекущаяЗапись", ТекущаяЗапись);
	
	ОповещениеСледующее = Новый ОписаниеОповещения("ВыбратьУчетнуюЗаписьОблачнойПодписиЗавершение", ЭтотОбъект, ПараметрыЦикла);
	
	ВсеЗаписи = СписокУчетыхЗаписейОблачнойПодписи(ОтборЗаписей);
	Если ВсеЗаписи.Количество() = 0 Тогда
		ПараметрыЦикла.Вставить("Сообщение", Истина);
		МодульСервисКриптографииDSSКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль("СервисКриптографииDSSКлиент");
		МодульСервисКриптографииDSSКлиент.ВывестиИнформацию(ОповещениеСледующее,
			НСтр("ru = 'Не обнаружены учетные записи для выбора'"));
	Иначе
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("ПредставлениеОтбора", ИмяОтбора);
		ПараметрыФормы.Вставить("ТаблицаВыбора", ВсеЗаписи);
		ПараметрыФормы.Вставить("ТекущаяЗапись", ТекущаяЗапись);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.Мастер_ВыборУчетнойЗаписиDSS", 
					ПараметрыФормы, 
					ФормаВладелец, Новый УникальныйИдентификатор(), , , 
					ОповещениеСледующее,
					РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьУчетнуюЗаписьОблачнойПодписиЗавершение(РезультатВыбора, ПараметрыЦикла) Экспорт
	
	ОповещениеОЗавершении = ПараметрыЦикла.ОповещениеОЗавершении;
	
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("Результат", Неопределено);
	
	Если НЕ ПараметрыЦикла.Сообщение Тогда
		Результат.Вставить("Выполнено", ЗначениеЗаполнено(РезультатВыбора));
		Результат.Вставить("Результат", РезультатВыбора);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	
КонецПроцедуры

#Область ПроверкиУникальностиЛогинаОблачнойПодписи

&НаКлиенте
Процедура ПроверитьУникальностьЛогинаОблачнойПодписи(ОповещениеОЗавершении, ИдентификаторСервера, НовыйЛогин) Экспорт
	
	ПараметрыЦикла = Новый Структура;
	ПараметрыЦикла.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	ОповещениеСледущее = Новый ОписаниеОповещения("ПроверитьУникальностьЛогинаЗавершение", ЭтотОбъект, ПараметрыЦикла);
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(Неопределено);
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Проверка дублирования логина в сервисе электронной подписи'");
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	
	ОписаниеОперации = СервисПроверкиЛогинаОблачнойПодписи(ИдентификаторСервера, НовыйЛогин);
	Если ЗначениеЗаполнено(ОписаниеОперации.УчетнаяЗапись) Тогда
		Результат = Новый Структура;
		Результат.Вставить("Статус", "Найден");
		Результат.Вставить("УчетнаяЗапись", ОписаниеОперации.УчетнаяЗапись);
		ВыполнитьОбработкуОповещения(ОповещениеСледущее, Результат);
	Иначе	
		ДлительныеОперацииКлиент.ОжидатьЗавершение(ОписаниеОперации.ФоноваяЗадача, ОповещениеСледущее, ПараметрыОжидания);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьУникальностьЛогинаЗавершение(РезультатВыполнения, ДополнительныеПараметры) Экспорт
	
	ОповещениеОЗавершении = ДополнительныеПараметры.ОповещениеОЗавершении;
	Статус = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(РезультатВыполнения, "Статус", "Неопределено");
	Варианты = "";
	УчетнаяЗапись = Неопределено;
	
	Если Статус = "Найден" Тогда
		УчетнаяЗапись = РезультатВыполнения.УчетнаяЗапись;
		
	ИначеЕсли Статус = "Выполнено" Тогда
		ОтветСервиса = ПолучитьИзВременногоХранилища(РезультатВыполнения.АдресРезультата);
		
		Если ОтветСервиса.Пройден = Неопределено Тогда
			Статус = "Неопределено";
			
		ИначеЕсли НЕ ОтветСервиса.Пройден Тогда
			Статус = "Дублирование";
			Варианты = ОтветСервиса.Варианты;
			
		Иначе
			Статус = "Успех";
			
		КонецЕсли;
		
	КонецЕсли;
	
	Результат = Новый Структура();
	Результат.Вставить("Статус", Статус);
	Результат.Вставить("Варианты", Варианты);
	Результат.Вставить("УчетнаяЗапись", УчетнаяЗапись);
	
	ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Результат);
	
КонецПроцедуры

&НаСервере
Функция СервисПроверкиЛогинаОблачнойПодписи(ИдентификаторСервера, НовыйЛогин)
	
	Результат = Новый Структура;
	Результат.Вставить("ФоноваяЗадача", Неопределено);
	Результат.Вставить("УчетнаяЗапись", Неопределено);
	
	ФильтрПоиска = Новый Соответствие;
	ФильтрПоиска.Вставить("Владелец.ВнутреннийИдентификатор", ИдентификаторСервера);
	ФильтрПоиска.Вставить("Логин", НовыйЛогин);
	
	МодульСервисКриптографииDSS = ОбщегоНазначения.ОбщийМодуль("СервисКриптографииDSS");
	НашлиУчетнуюЗапись = МодульСервисКриптографииDSS.НайтиУчетнуюЗапись(ФильтрПоиска);
	
	Если НЕ ЗначениеЗаполнено(НашлиУчетнуюЗапись) Тогда
		ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияВФоне(Новый УникальныйИдентификатор);
		ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Вызов API Калуги-Астрал'");
		ПараметрыВыполнения.ОжидатьЗавершение = 0;
		ПараметрыВыполнения.ДополнительныйРезультат = Ложь;
		
		ПараметрыВызова = Новый Структура();
		ПараметрыВызова.Вставить("Логин", НовыйЛогин);
		ПараметрыВызова.Вставить("ВнутреннийИдентификатор", ИдентификаторСервера);
		
		Результат.Вставить("ФоноваяЗадача", ДлительныеОперации.ВыполнитьВФоне(
						"Обработки.ДокументооборотСКонтролирующимиОрганами.ОбменССерверомПроверитьЛогинФоновое", 
						ПараметрыВызова, 
						ПараметрыВыполнения));
	Иначе
		Результат.Вставить("УчетнаяЗапись", НашлиУчетнуюЗапись);
	КонецЕсли;					
					
	Возврат Результат;

КонецФункции

#КонецОбласти

#КонецОбласти

&НаКлиенте
Процедура СформироватьЗакрытыйКлючИЗапросНаСертификат_ОшибкаСозданияКлюча(
		ВходящийКонтекст,
		ТекстОшибкиПриСозданииКлюча = Неопределено) Экспорт
	
	СтандартнаяОбработка = Ложь;
	
	Если ТекстОшибкиПриСозданииКлюча = Неопределено Тогда
		ТекстОшибки = НСтр("ru = 'Криптографическая ошибка: не удалось создать контейнер закрытого ключа.'");
	Иначе 
		ТекстОшибки = ТекстОшибкиПриСозданииКлюча;
	КонецЕсли;
	
	ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиентСервер.СообщитьИлиПередатьВМастерОшибку(
		ТекстОшибки, 
		ВходящийКонтекст.ВызовИзМастераПодключенияК1СОтчетности, 
		ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности);
		
	ИмяСобытия = НСтр("ru = 'Упрощенное заявление. Ошибка создание закрытого ключа'");
	Оповестить(ИмяСобытия, ТекстОшибки, ВходящийКонтекст.ДокументЗаявление);
		
	СтруктураРезультата = Новый Структура("ПродолжитьОтправкуЗаявления, ТекстОшибки, ПутьКонтейнерЗакрытогоКлюча", 
											Ложь, ВходящийКонтекст.ТекстОшибокДляМастераПодключенияК1СОтчетности, "");
											
	ВыполнитьОбработкуОповещения(
		ВходящийКонтекст.ВыполняемоеОповещение, 
		СтруктураРезультата);
	
КонецПроцедуры

&НаКлиенте
Функция КонтейнерЗакрытогоКлючаСохраненКорректно(ИмяКонтейнера, ВходящийКонтекст)
	
	ОбязателенТокен = ОбязателенТокен(ВходящийКонтекст);
	
	КонтейнераСохраненНаТокен = СтрНайти(ВРег(ИмяКонтейнера), "SCARD") > 0;
	
	Если ОбязателенТокен Тогда
		Корректно = КонтейнераСохраненНаТокен;
	Иначе
		Корректно = Истина;
	КонецЕсли;
	
	Возврат Корректно;

КонецФункции

&НаКлиенте
Функция ОбязателенТокен(ВходящийКонтекст)
	
	ДокументЗаявление        = ВходящийКонтекст.ДокументЗаявление;
	ЭтоТестирование          = ДокументооборотСКОКлиент.ИспользуетсяРежимТестирования();
	ЭтоРуководитель          = ОбработкаЗаявленийАбонентаВызовСервера.ЭтоРуководитель(ДокументЗаявление);
	ЭтоБюджетополучатель     = ДокументЗаявление.ЭтоБюджетополучатель;
	ИспользоватьСуществующий = ОбработкаЗаявленийАбонентаКлиентСервер.ИспользоватьСуществующий(ДокументЗаявление);
	ЭтоФНС                   = ДокументЗаявление.УдостоверяющийЦентр = ПредопределенноеЗначение("Перечисление.УдостоверяющиеЦентрыБРО.УЦФНС");
	ЭтоАЦУЦ                  = ДокументЗаявление.УдостоверяющийЦентр = ПредопределенноеЗначение("Перечисление.УдостоверяющиеЦентрыБРО.УЦАналитическийЦентр");
	
	ОбязателенТокен = 
		НЕ ЭтоБюджетополучатель 
		И ЭтоРуководитель
		И НЕ ИспользоватьСуществующий
		И НЕ ЭтоТестирование
		И (ЭтоФНС ИЛИ ЭтоАЦУЦ);
	
	Возврат ОбязателенТокен;

КонецФункции

&НаСервереБезКонтекста
Функция ИмяФайлаТребованияИзОписания(Требование)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Опись 		= ДокументооборотСКОВызовСервера.ПолучитьОписьВходящихДокументовПоТребованию(Требование);
	ЦиклОбмена 	= ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Опись);
	Сообщения	= КонтекстЭДОСервер.ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,Ложь,Истина);
	
	СтрДокументНО = Сообщения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ДокументНО));
	Если СтрДокументНО.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли; 
	ТранспортноеСообщение = СтрДокументНО[0].Ссылка;
	Вложения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);

	Если Вложения.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли;
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	Вложения[0].Данные.Получить().Записать(ВремФайл);
	
	ДеревоОписания = КонтекстЭДОСервер.ЗагрузитьXMLВДеревоЗначений(ВремФайл);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
	Если ДеревоОписания = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	ДокНапрИзНО = ДеревоОписания.Строки.Найти("ДокНапрИзНО", "Имя", Истина);
	Если ДокНапрИзНО = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	СтрокиИнфСообДок = ДокНапрИзНО.Строки;
	
	Для каждого СтрокаИнфСообДок Из СтрокиИнфСообДок Цикл
		
		Строки = СтрокаИнфСообДок.Строки;
		НомФайлДок = Строки.Найти("НомФайлДок", "Имя", Истина);
		Если НомФайлДок = Неопределено Тогда
			Возврат "";
		КонецЕсли;
		
		Если Врег(СокрЛП(НомФайлДок.Значение)) = Врег(СокрЛП(Требование.НомерДокумента)) Тогда
			
			ИмяФайл = Строки.Найти("ИмяФайл", "Имя", Истина);
			Если ИмяФайл = Неопределено Тогда
				Возврат "";
			КонецЕсли;
			Возврат ИмяФайл.Значение;
			
		КонецЕсли;
	
	КонецЦикла;
	
	Возврат "";
	
КонецФункции

&НаСервереБезКонтекста
Функция ИдФайлЗаяв(Требование) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	Опись 		= ДокументооборотСКОВызовСервера.ПолучитьОписьВходящихДокументовПоТребованию(Требование);
	ЦиклОбмена 	= ДокументооборотСКОВызовСервера.ПолучитьПоследнийЦиклОбмена(Опись);
	Сообщения	= КонтекстЭДОСервер.ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,,Ложь,Истина);
	
	СтрДокументНО = Сообщения.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ДокументНО));
	Если СтрДокументНО.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли; 
	ТранспортноеСообщение = СтрДокументНО[0].Ссылка;
	Вложения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Истина, Перечисления.ТипыСодержимогоТранспортногоКонтейнера.Документ);

	Если Вложения.Количество() = 0 Тогда
		Возврат "";
	КонецЕсли;
	
	ВремФайл = ПолучитьИмяВременногоФайла();
	Вложения[0].Данные.Получить().Записать(ВремФайл);
	
	ДеревоОписания = КонтекстЭДОСервер.ЗагрузитьXMLВДеревоЗначений(ВремФайл);
	ОперацииСФайламиЭДКО.УдалитьВременныйФайл(ВремФайл);
	Если ДеревоОписания = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	ДокНапрИзНО = ДеревоОписания.Строки.Найти("ДокНапрИзНО", "Имя", Истина);
	Если ДокНапрИзНО = Неопределено Тогда
		Возврат "";
	КонецЕсли;
	
	УзелИдФайлЗаяв = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДокНапрИзНО,,, "ИдФайлЗаяв", "А");
	Если УзелИдФайлЗаяв = Неопределено Тогда
		Возврат "";
	Иначе
		Возврат УзелИдФайлЗаяв.Значение;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура УстановитьСертификатыВРПриНеобходимости(ОповещениеОЗавершении) Экспорт
	
	Если НЕ ОбщегоНазначенияКлиент.ИнформационнаяБазаФайловая() Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"ПослеПоискаКорневогоСертификатаВР", 
		ЭтотОбъект,
		ДополнительныеПараметры);
		
	Хранилище_Сертификатов  = Новый Структура;
	Хранилище_Сертификатов.Вставить("Хранилище", "ROOT");
	Хранилище_Сертификатов.Вставить("ЭтоЛокальноеХранилище", Истина);
	
	КриптографияЭДКОКлиент.ПолучитьСертификаты(Оповещение, Хранилище_Сертификатов);
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеПоискаКорневогоСертификатаВР(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ДвДанные  = ДвДанныеКорневогоСертификатаВР();
	Отпечаток = ОтпечатокСертификатаВР(ДвДанные);
	Сертификат = Неопределено;
	
	Для каждого ТекущийСертификат Из Результат.Сертификаты Цикл
		Если ТекущийСертификат.Отпечаток = Отпечаток Тогда
			Сертификат = ТекущийСертификат;
		КонецЕсли;
	КонецЦикла;
	
	Если Сертификат <> Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПослеЗаписиКорневогоСертификатаВРВФайл",
		ЭтотОбъект, 
		ВходящийКонтекст);
		
	ДвДанные     = ДвДанныеКорневогоСертификатаВР();
	Сертификат64 = Base64Строка(ДвДанные);
	
	ОперацииСФайламиЭДКОКлиент.Base64ВФайл(ОписаниеОповещения, Сертификат64);
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПослеЗаписиКорневогоСертификатаВРВФайл(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
		Возврат;
	КонецЕсли;
	
	ИмяФайлаСертификата = Результат.ИмяФайла;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ПослеУстановкиКорневогоСертификатаВРВФайл",
		ЭтотОбъект, 
		ВходящийКонтекст);
	
	КриптографияЭДКОКлиент.ИмпортироватьСертификат(
		ОписаниеОповещения, 
		ИмяФайлаСертификата, 
		"ROOT");
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПослеУстановкиКорневогоСертификатаВРВФайл(Результат, ВходящийКонтекст) Экспорт
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении);
	
КонецПроцедуры

&НаСервере
Функция ДвДанныеКорневогоСертификатаВР()
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ДвДанные = КонтекстЭДОСервер.ПолучитьМакет("КорневойСертификатВР");
	
	Возврат ДвДанные;
	
КонецФункции

&НаСервере
Функция ОтпечатокСертификатаВР(ДвДанные)
	
	Сертификат = Новый СертификатКриптографии(ДвДанные);
	Отпечаток = Нрег(ПолучитьHexСтрокуИзДвоичныхДанных(Сертификат.Отпечаток));
	
	Возврат Отпечаток;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуИзмененияТипаВходящегоПисьмаПФР(МассивПисем, РежимИзменения) Экспорт
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("СписокПисем", МассивПисем);
	ПараметрыФормы.Вставить("РежимИзменения", РежимИзменения);
	
	ОткрытьФорму(ПутьКОбъекту + ".Форма.ИзменитьТипВходящегоПисьмаПФР", ПараметрыФормы, Новый УникальныйИдентификатор,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

#Область ТребованияОбработкаОтветов

&НаКлиенте
Функция ФормаДокументооборотаОткрыта(ИмяФормыПоиска)
	
	Окна = ПолучитьОкна();
	
	Если Окна = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	                  
	// Для формы, открытой в режиме "РежимОткрытияОкнаФормы.БлокироватьОкноВладельца",
	// не выполняется поиск по уникальному идентификатору, поэтому приходится искать самим.
	Для Каждого ОткрытоеОкно Из Окна Цикл
		Если ТипЗнч(ОткрытоеОкно) = Тип("ОкноКлиентскогоПриложения") 
			И ОткрытоеОкно.Содержимое.Количество() = 1
			И ОткрытоеОкно.Содержимое[0].ИмяФормы  = ИмяФормыПоиска Тогда
			
			Возврат ОткрытоеОкно.Содержимое[0].Открыта();
			
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

&НаКлиенте
Процедура ПоказатьИнформациюПоТребованиямКлиент(Повторять = Ложь) Экспорт
	
	ИмяФормыТребования = ПутьКОбъекту + ".Форма.ИнформацияПоТребованиям";
	Если ФормаДокументооборотаОткрыта(ИмяФормыТребования) = Ложь Тогда
		
		Если Повторять Тогда
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ПоказатьИнформациюПоТребованиямКлиент_Повторить", 
				ЭтотОбъект);
		Иначе
			ОписаниеОповещения = Новый ОписаниеОповещения();
		КонецЕсли;
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("Сейчас", ТекущаяДата());
		 
		ОткрытьФорму(ИмяФормыТребования,ДополнительныеПараметры,,Истина,,,ОписаниеОповещения);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьИнформациюПоТребованиямКлиент_Повторить(Повторять, ВходящийКонтекст) Экспорт
	
	Если Повторять = Ложь Тогда
		ДокументооборотСКОКлиент.ПодключениеОбработчикаОжиданияИнформацияПоТребованиям(Ложь);
	Иначе
		ДокументооборотСКОКлиент.ПодключениеОбработчикаОжиданияИнформацияПоТребованиям(Истина, 30 * 60);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
// Цикла Пока ПараметрыСоединения.ПовторятьСоединение (КонецЦикла)

&НаКлиенте
Процедура ПолучитьУведомленияРПН(ВыполняемоеОповещение, Организация)
	
	РезультатПолучения = ПолучитьУведомленияРПННаСервере(Организация);
	
	Если НЕ РезультатПолучения.Выполнено И РезультатПолучения.ПовторятьСоединение Тогда
		СерверПриемаОтчетности = СерверПриемаОтчетностиРПН();
		СтруктураURI = ОбщегоНазначенияКлиентСервер.СтруктураURI(СерверПриемаОтчетности.Определение);
		ИмяСервера = ?(СтрНайти(СтруктураURI.ИмяСервера, ":") = 0 И нрег(СтруктураURI.Схема) = "https",
			СтруктураURI.Хост + ":443", СтруктураURI.ИмяСервера);
		ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Не удалось обратиться к серверу ""%1"" для получения уведомлений Росприроднадзора'"),
			ИмяСервера);
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВыполняемоеОповещение);
		ДополнительныеПараметры.Вставить("Организация", 			Организация);
		ДополнительныеПараметры.Вставить("ИмяСервера", 				ИмяСервера);
		ОписаниеОповещения = Новый ОписаниеОповещения("ПолучитьУведомленияРПНПослеПолучения",
			ЭтотОбъект, ДополнительныеПараметры);
		
		ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент.ПоказатьДиалогОшибкиДоступаВИнтернет(
			ТекстОшибки,
			ОписаниеОповещения);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатПолучения);
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьУведомленияРПНПослеПолучения(РезультатДиалогаОбОшибкеДоступа, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение 	= ДополнительныеПараметры.ВыполняемоеОповещение;
	Организация 			= ДополнительныеПараметры.Организация;
	ИмяСервера 				= ДополнительныеПараметры.ИмяСервера;
	
	Если РезультатДиалогаОбОшибкеДоступа = Истина Тогда
		ПолучитьУведомленияРПН(ВыполняемоеОповещение, Организация);
		
	Иначе
		ТекстОшибки = СтрШаблон(
			НСтр("ru = 'Не удалось получить уведомления Росприроднадзора с сервера ""%1""'"),
			ИмяСервера);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстОшибки);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьУведомленияРПННаСервере(ОрганизацияИлиУчетнаяЗапись, ВыводитьСообщения = Истина) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ПолучитьУведомленияРПН(ОрганизацияИлиУчетнаяЗапись, ВыводитьСообщения);
	
КонецФункции

#Область ОтправкаРегламентированногоОтчетаВЦБ

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБ(Док, ВыполняемоеОповещение) Экспорт
	
	Если НЕ ДокументооборотСКОКлиентСервер.ПодсистемаЦБСуществует() Тогда
		ТекстСообщения = НСтр("ru = 'Отправка отчетности в Банк России не поддерживается.'");
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		Возврат;
	КонецЕсли;
	
	Контекст = Новый Структура();
	Контекст.Вставить("Док", 					Док);
	Контекст.Вставить("ОповещениеОЗавершении", 	ВыполняемоеОповещение);
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВЦБПослеПодтвержденияПовторнойОтправки",
		ЭтотОбъект,
		Контекст);
	
	ПодтвердитьПовторнуюОтправку(Док, "ЦБ", ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБПослеПодтвержденияПовторнойОтправки_ПослеПроверкиЗаявления(Результат, ВходящийКонтекст) Экспорт
	
	Параметры_Результат = КодВозвратаДиалога.Да;
	Параметры_ВходящийКонтекст = ВходящийКонтекст;
	
	ОтправкаРегламентированногоОтчетаВЦБПослеПодтвержденияПовторнойОтправки(Параметры_Результат, Параметры_ВходящийКонтекст);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБПослеПодтвержденияПовторнойОтправки(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	Если Результат <> КодВозвратаДиалога.Да Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// инициализируем контекст отправки
	КонтекстОтправки = Новый Структура;
	
	Если НЕ ОтправкаРегламентированногоОтчетаВЦБ_ПередВыгрузкойФайла(КонтекстОтправки, Док, УникальныйИдентификатор) Тогда // сервер
		
		Если КонтекстОтправки.Свойство("ОтсутствуетУчетнаяЗапись") Тогда
			
			ОписаниеОповещения = Новый ОписаниеОповещения(
				"ОтправкаРегламентированногоОтчетаВЦБПослеПодтвержденияПовторнойОтправки_ПослеПроверкиЗаявления",
				ЭтотОбъект,
				ВходящийКонтекст);
				
			ДокументооборотСКОКлиент.ПроверитьСтатусПодключенияПередОтправкой(Док, ОписаниеОповещения);
			
		ИначеЕсли КонтекстОтправки.Свойство("НаправлениеНеПодключено") Тогда
			ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ОповещениеОЗавершении, Ложь); //+
		Иначе
			
			ДополнительныеПараметры = Новый Структура();
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
			ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
			
			ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЦБ
			
		КонецЕсли;
		
		Возврат;
		
	ИначеЕсли НЕ МультирежимВызовСервера.ЕстьПравоНаОтправкуОбъекта(Док) Тогда
		
		ДокументооборотСКОКлиент.СообщитьОНеподключенномНаправлении(Док, ОповещениеОЗавершении, Ложь); //+
		Возврат;

	КонецЕсли;
	
	// Выгрузка
	Оповещение = Новый ОписаниеОповещения("ОтправкаРегламентированногоОтчетаВЦБПослеВыгрузки", ЭтотОбъект, ВходящийКонтекст);
	
	ДлительнаяОтправкаКлиент.ЗапретитьВыводСостояния();
	// выгружаем отчет и доверенность, при необходимости
	ВыгрузитьРегламентированныйОтчетЦБ(КонтекстОтправки, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБПослеВыгрузки(Результат, ВходящийКонтекст) Экспорт
	
	Док = ВходящийКонтекст.Док;
	
	РезультатВыгрузки     = Результат.РезультатВыгрузки;
	КонтекстОтправки      = Результат.Контекст; // Обновленный по результатам выгрузки контекст.
	ОповещениеОЗавершении = ВходящийКонтекст.ОповещениеОЗавершении;
	
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	ДлительнаяОтправкаКлиент.СброситьЗапретВыводаСостояния();
	
	Если НЕ РезультатВыгрузки Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЦБ
		Возврат;
		
	КонецЕсли;
	
	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВЦБПослеАвтонастройкиУчетнойЗаписи",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	КонтекстДлительнойОперации = Новый Структура();
	КонтекстДлительнойОперации.Вставить("Док", Док);
	КонтекстДлительнойОперации.Вставить("ОписаниеОповещения", ОписаниеОповещения);
	
	ПараметрыДлительнойОтправки = ДлительнаяОтправкаКлиент.ПараметрыДлительнойОтправки();
	ПараметрыДлительнойОтправки.ОтчетСсылка 				= Док;
	ПараметрыДлительнойОтправки.ОбщееКоличествоЭтапов 		= 7;
	ПараметрыДлительнойОтправки.КонтекстДлительнойОперации 	= КонтекстДлительнойОперации;
	
	Если НЕ ДлительнаяОтправкаКлиент.ПоказатьФормуДлительнойОтправки(ПараметрыДлительнойОтправки) Тогда // отправка отчета в ЦБ
		Возврат;
	КонецЕсли;

	ПодключитьОбработчикОжидания("Подключаемый_ОтправкаРегламентированногоОтчетаВЦБПослеВыгрузки", 0.1, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправкаРегламентированногоОтчетаВЦБПослеВыгрузки() Экспорт
	
	КонтекстДлительнойОперации = ДлительнаяОтправкаКлиент.КонтекстДлительнойОперации();
	
	Док 				= КонтекстДлительнойОперации.Док;
	ОписаниеОповещения 	= КонтекстДлительнойОперации.ОписаниеОповещения;
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется настройка учетной записи..."); // ЦБ рег. отчет
	
	ВыполнитьАвтонастройкуУчетнойЗаписиПередОтправкойОбъекта(Док, ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБПослеАвтонастройкиУчетнойЗаписи(Результат, ВходящийКонтекст) Экспорт

	ОписаниеОповещения = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВЦБ_ПослеПроверкиСертификатаИЛицензии",
		ЭтотОбъект,
		ВходящийКонтекст);
	
	ПроверитьСертификатИЛицензию(ОписаниеОповещения, ВходящийКонтекст.КонтекстОтправки.Документ, ВходящийКонтекст.КонтекстОтправки.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБ_ПослеПроверкиСертификатаИЛицензии(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДлительнаяОтправкаКлиент.ЗакрытьФормуДлительнойОтправкиБезДальнейшихДействий();
		Возврат;
	КонецЕсли;
	
	// получаем свойства сертификатов абонента
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется проверка сертификатов..."); // ЦБ рег. отчет
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВЦБПослеПроверкиСертификатов", ЭтотОбъект, ВходящийКонтекст);
	
	СформироватьПакетЦБ_ПроверкиСертификатов(Оповещение, ВходящийКонтекст.КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБПослеПроверкиСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЦБ
		
		Возврат;
	КонецЕсли;
	
	ОповещениеОЗавершении 	= ВходящийКонтекст.ОповещениеОЗавершении;
	КонтекстОтправки 		= ВходящийКонтекст.КонтекстОтправки;
	
	// отражаем изменения в базе
	Если НЕ ОтправкаРегламентированногоОтчетаВЦБ_ОпределитьЦиклОбменаИСообщениеВИБ(КонтекстОтправки) Тогда // сервер
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЦБ
		
		Возврат;
	КонецЕсли;
	
	// формируем пакет
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"ОтправкаРегламентированногоОтчетаВЦБПослеФормированияПакета", ЭтотОбъект, Контекст);
	
	Отправка_СформироватьПакет(Оповещение, КонтекстОтправки.ТранспортноеСообщение, "ЦБ");
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправкаРегламентированногоОтчетаВЦБПослеФормированияПакета(Результат, ВходящийКонтекст) Экспорт 
	
	Если НЕ Результат Тогда
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЦБ
		
		Возврат;
		
	КонецЕсли;
	
	// формируем пакет
	Если НЕ ОтправитьТранспортноеСообщениеИЗаписатьСтатусОтправки(ВходящийКонтекст.КонтекстОтправки) Тогда // сервер
		
		ДополнительныеПараметры = Новый Структура();
		ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
		ДополнительныеПараметры.Вставить("РезультатОтправки", 		Ложь);
		
		ДлительнаяОтправкаКлиент.ОповеститьОНеудачнойОтправке(ДополнительныеПараметры); // ЦБ
		
		Возврат;
		
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура();
	ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", 	ВходящийКонтекст.ОповещениеОЗавершении);
	ДополнительныеПараметры.Вставить("РезультатОтправки", 		Истина);
	Если ВходящийКонтекст.КонтекстОтправки.Свойство("ДополнительноеСообщениеПриУдачнойОтправке") Тогда
		ДополнительныеПараметры.Вставить("ДополнительноеСообщение",
			ВходящийКонтекст.КонтекстОтправки.ДополнительноеСообщениеПриУдачнойОтправке);
	КонецЕсли;
	Если ВходящийКонтекст.КонтекстОтправки.Свойство("ДополнительныеРеквизитыДляОтправкиПриУдачнойОтправке") Тогда
		ДополнительныеПараметры.Вставить("ДополнительныеРеквизитыДляОтправки",
			ВходящийКонтекст.КонтекстОтправки.ДополнительныеРеквизитыДляОтправкиПриУдачнойОтправке);
	КонецЕсли;
	Если ВходящийКонтекст.КонтекстОтправки.Свойство("ДополнительныеВложенияДляОтправкиПриУдачнойОтправке") Тогда
		ДополнительныеПараметры.Вставить("ДополнительныеВложенияДляОтправки",
			ВходящийКонтекст.КонтекстОтправки.ДополнительныеВложенияДляОтправкиПриУдачнойОтправке);
	КонецЕсли;
	
	ДлительнаяОтправкаКлиент.ОповеститьОбУдачнойОтправке(ДополнительныеПараметры); // ЦБ.
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетЦБ(Контекст, ВыполняемоеОповещение)

	Отчет = Контекст.Документ;
	ИмяМетаданныхДокумента = Контекст.ТипОтправляемогоДокумента;
	Если ИмяМетаданныхДокумента = "РегламентированныйОтчет" Тогда
		Результат = Новый Массив;
		
		ВыгрузитьРегламентированныйОтчетЦБПродолжение(Контекст, Отчет, Результат, ВыполняемоеОповещение);
		
	ИначеЕсли ИмяМетаданныхДокумента = "ЭлектронныеПредставленияРегламентированныхОтчетов" Тогда
		Результат = ВыгрузитьРегламентированныйОтчетЦБИзВнешнихЭлектронныхПредставлений(Контекст);
		ВсегоВАрхиве = 0;
		Для каждого Запись Из Результат Цикл
			Если Запись.ВАрхиве Тогда
				ВсегоВАрхиве = ВсегоВАрхиве + 1;
			КонецЕсли;
		КонецЦикла;
		
		Если ВсегоВАрхиве > 0 Тогда //Данные одного или более файлов отсутствуют
			ДополнительныеПараметры = Новый Структура;
			ДополнительныеПараметры.Вставить("Контекст", Контекст);
			ДополнительныеПараметры.Вставить("ВыполняемоеОповещение", ВыполняемоеОповещение);
		
			ОписаниеОповещения = Новый ОписаниеОповещения("ВопросПродолжитьВыгрузкуРеглОтчетаЦБНеудача", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьУведомлениеАрхивныхФайлов(ОписаниеОповещения, 24, 2, Истина);			
			
			Возврат;
		КонецЕсли;
		
		Контекст.Вставить("РезультатВыгрузки", Результат);
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
		
	Иначе
		Результат = Новый Массив;
		
		СвойстваФайлаВыгрузки = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервераПереопределяемый.ВыгрузитьДокумент(Отчет, Контекст.УникальныйИдентификатор);
		Если НЕ ЗначениеЗаполнено(СвойстваФайлаВыгрузки) Тогда
			РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
			
			Возврат;
			
		КонецЕсли;
		
		// заполняем результат
		Если ТипЗнч(СвойстваФайлаВыгрузки) = Тип("Структура") Тогда
			ОписательОтчета = Новый Структура;
			ОписательОтчета.Вставить("Документ", 	Отчет);
			ОписательОтчета.Вставить("Тип", 		СвойстваФайлаВыгрузки.ТипФайлаВыгрузки);
			ОписательОтчета.Вставить("Имя", 		СвойстваФайлаВыгрузки.ИмяФайлаВыгрузки);
			ОписательОтчета.Вставить("Адрес", 		СвойстваФайлаВыгрузки.АдресФайлаВыгрузки);
			Результат.Добавить(ОписательОтчета);
			
		Иначе
			Для Инд = 0 По СвойстваФайлаВыгрузки.Количество() - 1 Цикл
				ОписательОтчета = Новый Структура;
				ОписательОтчета.Вставить("Документ", 	СвойстваФайлаВыгрузки[Инд].Ссылка);
				ОписательОтчета.Вставить("Тип", 		СвойстваФайлаВыгрузки[Инд].ТипФайлаВыгрузки);
				ОписательОтчета.Вставить("Имя", 		СвойстваФайлаВыгрузки[Инд].ИмяФайлаВыгрузки);
				ОписательОтчета.Вставить("Адрес", 		СвойстваФайлаВыгрузки[Инд].АдресФайлаВыгрузки);
				Результат.Добавить(ОписательОтчета);
			КонецЦикла;
		КонецЕсли;
		
		Контекст.Вставить("РезультатВыгрузки", Результат);
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВопросПродолжитьВыгрузкуРеглОтчетаЦБНеудача(Выбор, ДопПараметры) Экспорт
	
	РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", ДопПараметры.Контекст, Ложь);
	ВыполнитьОбработкуОповещения(ДопПараметры.ВыполняемоеОповещение, РезультатВыгрузки);
	
КонецПроцедуры

&НаКлиенте
Процедура ВопросПродолжитьВыгрузкуРеглОтчетаЦБЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Контекст = ДополнительныеПараметры.Контекст;
	Отчет = ДополнительныеПараметры.Отчет;
	Результат = ДополнительныеПараметры.Результат;
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		РезультатВыгрузки = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузки);
	Иначе
		ВыгрузитьРегламентированныйОтчетЦБПродолжение(Контекст, Отчет, Результат, ВыполняемоеОповещение);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьРегламентированныйОтчетЦБПродолжение(Контекст, Отчет, Результат, ВыполняемоеОповещение)
	
	ТекстВыгрузки 			= "";
	ИмяФайлаВыгрузки 		= "";
	КодировкаФайлаВыгрузки 	= Неопределено;
	ТипФайлаВыгрузки 		= "";
	ЭлементыВыгрузки 		= Неопределено;
	РезультатВыгрузки = СдачаОтчетностиЧерезСервисСпецоператораКлиент.ВыгрузитьРегламентированныйОтчет(
		Отчет,
		Ложь,
		ТекстВыгрузки,
		ИмяФайлаВыгрузки,
		КодировкаФайлаВыгрузки,
		ТипФайлаВыгрузки,
		ЭлементыВыгрузки,
		Контекст.УникальныйИдентификатор);
	
	Если НЕ РезультатВыгрузки Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru = '%1Отправка отчета прервана.'"),
			ИнформацияОбОшибке().Описание + Символы.ПС);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		
		РезультатВыгрузкиОтчета = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Ложь);
		ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузкиОтчета);
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекстВыгрузки) И ТипЗнч(ИмяФайлаВыгрузки) = Тип("Массив") И ИмяФайлаВыгрузки.Количество() = 1
		И ТипЗнч(ЭлементыВыгрузки) = Тип("Массив") И ЭлементыВыгрузки.Количество() > 0 И ТипЗнч(ЭлементыВыгрузки[0]) = Тип("Структура") Тогда
		
		АдресВоВременномХранилище = ЭлементыВыгрузки[0].АдресФайлаВыгрузки;
		
		ОписательОтчета = Новый Структура;
		ОписательОтчета.Вставить("Документ", 	Отчет);
		ОписательОтчета.Вставить("Тип", 		ЭлементыВыгрузки[0].ТипФайлаВыгрузки);
		ОписательОтчета.Вставить("Имя", 		ИмяФайлаВыгрузки[0]);
		ОписательОтчета.Вставить("Адрес", 		АдресВоВременномХранилище);
		Результат.Добавить(ОписательОтчета);
		
	Иначе
		// пишем файл во временное хранилище
		КодировкаФайла = КодировкаФайлаВыгрузки;
		АдресВоВременномХранилище = ЗаписатьФайлВыгрузкиВоВременноеХранилище(
			ТекстВыгрузки,
			ИмяФайлаВыгрузки,
			КодировкаФайла,
			Контекст.УникальныйИдентификатор,
			Истина);
		
		// заполняем результат
		ОписательОтчета = Новый Структура;
		ОписательОтчета.Вставить("Документ", 	Отчет);
		ОписательОтчета.Вставить("Тип", 		"");
		ОписательОтчета.Вставить("Имя", 		ИмяФайлаВыгрузки);
		ОписательОтчета.Вставить("Адрес", 		АдресВоВременномХранилище);
		Результат.Добавить(ОписательОтчета);
	КонецЕсли;
	
	Контекст.Вставить("РезультатВыгрузки", Результат);
	РезультатВыгрузкиОтчета = Новый Структура("Контекст, РезультатВыгрузки", Контекст, Истина);
	ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, РезультатВыгрузкиОтчета);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВЦБ_ПередВыгрузкойФайла(Контекст, Знач Документ, Знач УникальныйИдентификатор)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ТекстСообщения = "";
	Результат = КонтекстЭДОСервер.ОтправкаРегламентированногоОтчетаВЦБ_ПередВыгрузкойФайла(Контекст, Документ, УникальныйИдентификатор, ТекстСообщения);
	
	Если ЗначениеЗаполнено(ТекстСообщения) Тогда
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ОтправкаРегламентированногоОтчетаВЦБ_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ОтправкаРегламентированногоОтчетаВЦБ_ОпределитьЦиклОбменаИСообщениеВИБ(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция ВыгрузитьРегламентированныйОтчетЦБИзВнешнихЭлектронныхПредставлений(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ВыгрузитьРегламентированныйОтчетЦБИзВнешнихЭлектронныхПредставлений(Контекст);
	
КонецФункции

#КонецОбласти

#Область СформироватьПакетЦБ
 
&НаКлиенте
Процедура СформироватьПакетЦБ(
		ОповещениеОЗавершении,
		ТранспортноеСообщение,
		Оповещать = Ложь,
		ПараметрыОперации = Неопределено) Экспорт
	
	КонтекстОтправки = Новый Структура;
	КонтекстОтправки.Вставить("ТранспортноеСообщение", ТранспортноеСообщение);
	КонтекстОтправки.Вставить("УникальныйИдентификатор", УникальныйИдентификатор);
	КонтекстОтправки.Вставить("Оповещать", Оповещать);
	
	Если ПараметрыОперации <> Неопределено Тогда
		ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(КонтекстОтправки, ПараметрыОперации, Истина);
	КонецЕсли;
	
	// общие проверки
	Если НЕ СформироватьПакетЦБ_ОбщиеПроверкиПередФормированием(КонтекстОтправки) Тогда // сервер
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// получение и проверки сертификатов
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетЦБПослеПроверкиИПолученияСертификатовПолучателя", ЭтотОбъект, Контекст);
	
	СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателя(Оповещение, КонтекстОтправки); // клиент
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетЦБПослеПроверкиИПолученияСертификатовПолучателя(Результат, ВходящийКонтекст) Экспорт
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если Результат Тогда
		Если НЕ СформироватьПакетЦБ_ПроверкиНаСервере(КонтекстОтправки) Тогда // универсальная
			ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
			Возврат;
		КонецЕсли;
		
		// проверки на клиенте (сертификаты)
		ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
		
		Оповещение = Новый ОписаниеОповещения(
			"СформироватьПакетЦБПослеПроверкиСертификатов", ЭтотОбъект, ВходящийКонтекст);
			
		СформироватьПакетЦБ_ПроверкиСертификатов(Оповещение, КонтекстОтправки);
	Иначе
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетЦБПослеПроверкиСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	
	// подписываем ЭЦП руководителя и главного бухгалтера
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ВходящийКонтекст.ОповещениеОЗавершении);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетЦБПослеПодписания", ЭтотОбъект, Контекст);
	СформироватьПакетЦБ_Подписание(Оповещение, КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетЦБПослеПодписания(Результат, ВходящийКонтекст) Экспорт
	
	Если Не Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// формируем дерево описания документов пакета (сервер)
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется формирование описания пакета с документами..."); // ЦБ Универсальная
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если НЕ СформироватьПакетЦБ_СформироватьДеревоОписанияДокументовПакета(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	РежимПодтвержденияОблачнойПодписи("СформироватьПакетЦБ", КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки, Ложь);
	
	// отрабатываем отложенные команды на клиенте (шифрование)
	ВходящийКонтекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетЦБПослеВыполненияОтложенныхКомандНаКлиенте", ЭтотОбъект, ВходящийКонтекст);
	СформироватьПакет_ОтработатьОтложенныеКомандыНаКлиенте(Оповещение, КонтекстОтправки);
	
КонецПроцедуры

&НаКлиенте
Процедура СформироватьПакетЦБПослеВыполненияОтложенныхКомандНаКлиенте(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	// формируем файл описания пакета, создаем архив и помещаем в ИБ
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	Если НЕ СформироватьПакетЦБ_ЗаключительныеДействияНаСервере(КонтекстОтправки) Тогда // универсальная
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Если КонтекстОтправки.Оповещать Тогда
		Оповестить("Изменение сообщения", Новый Структура("Сообщение, ЦиклОбмена, Предмет",
														КонтекстОтправки.ТранспортноеСообщение,
														КонтекстОтправки.ЦиклОбмена));
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_ОбщиеПроверкиПередФормированием(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_ОбщиеПроверкиПередФормированием(Контекст);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателя(ОповещениеОЗавершении, КонтекстОтправки)
	
	МестоХраненияКлюча = КонтекстМоделиХраненияКлюча(КонтекстОтправки.УчетнаяЗапись);
	
	Если ТипЗнч(КонтекстОтправки.СертификатПолучателя) <> Тип("Массив") Тогда
		Отпечатки = Новый Массив;
		Отпечатки.Добавить(КонтекстОтправки.СертификатПолучателя);
	Иначе
		Отпечатки = КонтекстОтправки.СертификатПолучателя;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(КонтекстОтправки.СертификатСервераДокументооборота)
		И Отпечатки.Найти(КонтекстОтправки.СертификатСервераДокументооборота) = Неопределено Тогда
		
		Отпечатки = ОбщегоНазначенияКлиент.СкопироватьРекурсивно(Отпечатки);
		Отпечатки.Добавить(КонтекстОтправки.СертификатСервераДокументооборота);
	КонецЕсли;
	
	СертификатыДляПоиска = Новый Массив;
	Для каждого Отпечаток Из Отпечатки Цикл
		НовыйСертификат = Новый Структура("Отпечаток", Отпечаток);
		КонтекстМоделиХраненияКлюча(МестоХраненияКлюча, НовыйСертификат);
		СертификатыДляПоиска.Добавить(НовыйСертификат);
	КонецЦикла;
	
	Контекст = Новый Структура;
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("Отпечатки", Отпечатки);
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах = Неопределено)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт

	// Внимание!
	// ---------
	// В модуле объекта есть одноименная процедура, которая используется при отправке отчетов
	// из мобильной бухгалтерии без взаимодействия с клиентским контекстом.
	// При изменениях необходимо синхронно менять обе процедуры.

	НайденныеСертификаты 				= ?(Результат.Свойство("Сертификаты"), Результат.Сертификаты, Неопределено);
	НенайденныеСертификаты 				= ?(Результат.Свойство("НенайденныеСертификаты"), Результат.НенайденныеСертификаты, Неопределено);
	СсылкаПолучатель 					= ВходящийКонтекст.КонтекстОтправки.СсылкаПолучатель;
	СертификатПолучателя 				= ВходящийКонтекст.КонтекстОтправки.СертификатПолучателя;
	СертификатСервераДокументооборота 	= ВходящийКонтекст.КонтекстОтправки.СертификатСервераДокументооборота;
	СсылкаСерверДокументооборота 		= ВходящийКонтекст.КонтекстОтправки.СсылкаСерверДокументооборота;
	
	ИнформацияОПолучателяхИСертификатах = Новый Структура;
	ИнформацияОПолучателяхИСертификатах.Вставить("СертификатСервераДокументооборота", 	СертификатСервераДокументооборота);
	ИнформацияОПолучателяхИСертификатах.Вставить("СсылкаСерверДокументооборота", 		СсылкаСерверДокументооборота);
	ИнформацияОПолучателяхИСертификатах.Вставить("СертификатПолучателя", 				СертификатПолучателя);
	РезультатПроверки = СформироватьПакетЦБ_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатовРазобратьНайденное(
		Результат.Выполнено,
		НайденныеСертификаты,
		НенайденныеСертификаты,
		СсылкаПолучатель,
		ИнформацияОПолучателяхИСертификатах);
	Если РезультатПроверки = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить(
		"ОбъектСертификатПолучателя", РезультатПроверки.НайденныеСертификатыПолучателя);
	ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки.Вставить(
		"ОбъектСертификатСервераДокументооборота", РезультатПроверки.НайденныйСертификатСервераДокументооборота);
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_ПроверкиНаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_ПроверкиНаСервере(Контекст);
	
КонецФункции

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_ПодготовитьСертификатыДляПоиска(Знач УчетнаяЗапись, Знач Сертификат1, Знач Сертификат2, Знач Сертификат3)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_ПодготовитьСертификатыДляПоиска(
				УчетнаяЗапись,
				Сертификат1,
				Сертификат2,
				Сертификат3);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетЦБ_ПроверкиСертификатов(ОповещениеОЗавершении, КонтекстОтправки)
	
	Если КонтекстОтправки.Свойство("ОбъектСертификатОтветственногоЗаШифрование") Тогда // возможна ситуация, когда сертификаты уже получены и проверены
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Истина);
		Возврат;
	КонецЕсли;
	
	СертификатыДляПоиска = СформироватьПакетЦБ_ПодготовитьСертификатыДляПоиска(
		КонтекстОтправки.УчетнаяЗапись,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатГлавногоБухгалтера,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если СертификатыДляПоиска = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	Контекст = Новый Структура;
	Контекст.Вставить("ОповещениеОЗавершении", ОповещениеОЗавершении);
	Контекст.Вставить("ВыполнятьПроверку", Истина);
	Контекст.Вставить("КонтекстОтправки", КонтекстОтправки);
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетЦБ_ПроверкиСертификатовПослеПолученияСертификатов", ЭтотОбъект, Контекст);
		
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Знач Сертификаты, 
		Знач Сертификат1, 
		Знач Сертификат2, 
		Знач Сертификат3) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Сертификаты, 
		Сертификат1, 
		Сертификат2, 
		Сертификат3);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетЦБ_ПроверкиСертификатовПослеПолученияСертификатов(Результат, ВходящийКонтекст) Экспорт
	
	Если НЕ Результат.Выполнено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.КонтекстОтправки;
	ОбъектыСертификатов = СформироватьПакетЦБ_ПроверкиСертификатовПослеПолученияСертификатовРазобратьНайденное(
		Результат.Сертификаты,
		КонтекстОтправки.СертификатРуководителя,
		КонтекстОтправки.СертификатГлавногоБухгалтера,
		КонтекстОтправки.СертификатОтветственногоЗаШифрование);
	
	Если ОбъектыСертификатов = Неопределено Тогда
		ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Ложь);
		Возврат;
	КонецЕсли;
	
	КонтекстОтправки = ВходящийКонтекст.ОповещениеОЗавершении.ДополнительныеПараметры.КонтекстОтправки;
	Для каждого ОбъектСертификат Из ОбъектыСертификатов Цикл
		КонтекстОтправки.Вставить(ОбъектСертификат.Ключ, ОбъектСертификат.Значение);
	КонецЦикла;
	
	ВыполнитьОбработкуОповещения(ВходящийКонтекст.ОповещениеОЗавершении, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_СформироватьДеревоОписанияДокументовПакета(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_СформироватьДеревоОписанияДокументовПакета(Контекст);
	
КонецФункции

&НаКлиенте
Процедура СформироватьПакетЦБ_Подписание(ОповещениеОЗавершении, КонтекстОтправки)
	
	ДлительнаяОтправкаКлиент.ВывестиСостояние("Выполняется подписание пакета с документами..."); // ЦБ Универсальная
	
	// формируем массив объектов-сертификатов, которыми будем подписывать
	МассивОбъектовСертификатов = Новый Массив;
	Если КонтекстОтправки.ТипСообщения = ПредопределенноеЗначение("Перечисление.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьЦБ") Тогда
		МассивОбъектовСертификатов.Добавить(КонтекстОтправки.ОбъектСертификатРуководителя);
		Если ЗначениеЗаполнено(КонтекстОтправки.СертификатГлавногоБухгалтера)
			И КонтекстОтправки.СертификатГлавногоБухгалтера <> КонтекстОтправки.СертификатРуководителя Тогда
			МассивОбъектовСертификатов.Добавить(КонтекстОтправки.ОбъектСертификатГлавногоБухгалтера);
		КонецЕсли;
	Иначе
		МассивОбъектовСертификатов.Добавить(КонтекстОтправки.ОбъектСертификатОтветственногоЗаШифрование);
	КонецЕсли;
	
	// генерируем подписи руководителя
	ПодписатьСообщение("ЦБ", ОповещениеОЗавершении, КонтекстОтправки,
		КонтекстОтправки.ТранспортноеСообщение, КонтекстОтправки.ТипТранспортногоСообщения,
		МассивОбъектовСертификатов, , КонтекстОтправки.УчетнаяЗапись, КонтекстОтправки.СообщениеОснование);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьПакетЦБ_ЗаключительныеДействияНаСервере(Контекст)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.СформироватьПакетЦБ_ЗаключительныеДействияНаСервере(Контекст);
	
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура ПоказатьОтветНаЗапросИОСЗавершение(РезультатВыбора, ДополнительныеПараметры) Экспорт
	
	ВыполняемоеОповещение = ДополнительныеПараметры.ВыполняемоеОповещение;
	СписокВыбора = ДополнительныеПараметры.СписокВыбора;
	Запрос = ДополнительныеПараметры.Запрос;
	
	Если РезультатВыбора = Неопределено Тогда
		
		Если ВыполняемоеОповещение <> Неопределено Тогда
			ВыполнитьОбработкуОповещения(ВыполняемоеОповещение, Ложь);
		КонецЕсли;
		
	Иначе
		
		// удаляем не помеченные строки из таблицы ответов
		Для Каждого СтрСписокВыбора Из СписокВыбора Цикл
			Если СтрСписокВыбора.Значение <> РезультатВыбора.Значение Тогда
				СтрОтветНаЗапросИОС = ОтветыНаЗапросыИОС.НайтиСтроки(Новый Структура("ИмяФайла", СтрСписокВыбора.Значение));
				ОтветыНаЗапросыИОС.Удалить(СтрОтветНаЗапросИОС[0]);
			КонецЕсли;
		КонецЦикла;
		
		ПоказатьОтветНаЗапросИОСНаКлиенте(Запрос, ВыполняемоеОповещение);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеКвитанцииЦБ(
		Заголовок,
		ИмяФайла,
		Тело,
		ЦиклОбмена,
		ТранспортноеСообщение = Неопределено,
		ТелоПриложения = Неопределено)
	
	СвойстваИзКвитанции = ПолучитьОсновныеСвойстваИзКвитанцииЦБ(Тело, ЦиклОбмена, ТранспортноеСообщение, ТелоПриложения);
	
	ТабДокумент = Новый ТабличныйДокумент;
	Если ЗначениеЗаполнено(СвойстваИзКвитанции) Тогда
		Макет = ПолучитьМакетОбработки("ЦБКвитанция");
		
		ОбластьМакета = Макет.ПолучитьОбласть("КвитанцияШапка");
		ОбластьМакета.Параметры.ТипКвитанции =
			?(нрег(СвойстваИзКвитанции.ОбщиеСведения.СтатусРезультатаОбработки) = "delivered",
				НСтр("ru = 'Подтверждение загрузки Банком России'"),
			?(нрег(СвойстваИзКвитанции.ОбщиеСведения.СтатусРезультатаОбработки) = "processing",
				НСтр("ru = 'Квитанция о приеме в обработку Банком России'"),
			?(нрег(СвойстваИзКвитанции.ОбщиеСведения.СтатусРезультатаОбработки) = "error",
				НСтр("ru = 'Уведомление об отказе от обработки Банком России'"),
				НСтр("ru = 'Квитанция Банка России'"))));
		ОбластьМакета.Параметры.Наименование = ЦиклОбмена.Организация;
		
		ОбластьМакета.Параметры.ДатаСоздания = ?(ЗначениеЗаполнено(СвойстваИзКвитанции.ОбщиеСведения.ДатаСоздания),
			Формат(СвойстваИзКвитанции.ОбщиеСведения.ДатаСоздания, "ДЛФ=ДДВ"),
			Формат(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки, "ДЛФ=ДДВ"));
		ОбластьМакета.Параметры.ДатаОтправки = Формат(СвойстваИзКвитанции.ОбщиеСведения.ДатаОтправки, "ДЛФ=ДДВ");
		
		ОбластьМакета.Параметры.РезультатТранспортнойОперации = СвойстваИзКвитанции.ОбщиеСведения.РезультатОперации;
		
		ОбластьМакета.Параметры.КодТипаКвитанции = СвойстваИзКвитанции.ОбщиеСведения.ТипКвитанции;
		ОбластьМакета.Параметры.ИдентификаторСообщения = СвойстваИзКвитанции.ОбщиеСведения.ИдентификаторСообщения;
		ОбластьМакета.Параметры.ИдентификаторИсходногоСообщения =
			СвойстваИзКвитанции.ОбщиеСведения.ИдентификаторИсходногоСообщения;
		ОбластьМакета.Параметры.ТипСообщения = СвойстваИзКвитанции.ОбщиеСведения.ТипСообщения;
		ОбластьМакета.Параметры.ПриоритетСообщения = СвойстваИзКвитанции.ОбщиеСведения.ПриоритетСообщения;
		ТабДокумент.Вывести(ОбластьМакета); // Подвал
		
		ОбластьМакета = Макет.ПолучитьОбласть("Подвал");
		ТабДокумент.Вывести(ОбластьМакета); // Подвал
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
		ТабДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
		ТабДокумент.АвтоМасштаб = Истина;
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция СформироватьТабличныйДокументНаОсновеРезультатаОбработкиЦБ(
		Заголовок,
		ИмяФайла,
		Тело,
		ЦиклОбмена,
		ТранспортноеСообщение = Неопределено)
	
	СвойстваИзКвитанции = ПолучитьОсновныеСвойстваИзКвитанцииЦБ(Тело, ЦиклОбмена, ТранспортноеСообщение);
	ДатыСообщений = ПолучитьОсновныеСвойстваЦБИзЦиклаОбмена(ЦиклОбмена);
	
	ТабДокумент = Новый ТабличныйДокумент;
	Если ЗначениеЗаполнено(СвойстваИзКвитанции) Тогда
		Макет = ПолучитьМакетОбработки("ЦБРезультатОбработки");
		
		ОбластьМакета = Макет.ПолучитьОбласть("РезультатОбработкиШапка");
		ОбластьМакета.Параметры.Наименование = ЦиклОбмена.Организация;
		
		ОбластьМакета.Параметры.РегистрационныйНомер = СвойстваИзКвитанции.ОбщиеСведения.РегистрационныйНомер;
		
		ОбластьМакета.Параметры.ДатаОтправки = ДатыСообщений.ДатаОтправки;
		ОбластьМакета.Параметры.ДатаЗагрузки = ?(ЗначениеЗаполнено(ДатыСообщений.ДатаЗагрузкиЗначение)
			И ЗначениеЗаполнено(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки)
			И ДатыСообщений.ДатаЗагрузкиЗначение > СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки,
			Формат(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки, "ДЛФ=ДДВ"), ДатыСообщений.ДатаЗагрузки);
		ОбластьМакета.Параметры.ДатаПриема = ?(ЗначениеЗаполнено(ДатыСообщений.ДатаПриемаЗначение)
			И ЗначениеЗаполнено(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки)
			И ДатыСообщений.ДатаПриемаЗначение > СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки,
			Формат(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки, "ДЛФ=ДДВ"), ДатыСообщений.ДатаПриема);
		ОбластьМакета.Параметры.ДатаРезультатаОбработки =
			?(ЗначениеЗаполнено(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки),
			Формат(СвойстваИзКвитанции.ОбщиеСведения.ДатаВремяРезультатаОбработки, "ДЛФ=ДДВ"), ДатыСообщений.ДатаОбработки);
		ТабДокумент.Вывести(ОбластьМакета); // РезультатОбработкиШапка
		
		ОбластьМакета = Макет.ПолучитьОбласть("РезультатОбработкиОтчеты");
		ТабДокумент.Вывести(ОбластьМакета); // РезультатОбработкиОтчеты
		
		Для каждого ЭлементФайл Из СвойстваИзКвитанции.ИмяФайла Цикл
			ОбластьМакета = Макет.ПолучитьОбласть("РезультатОбработкиИмяОтчетов");
			ОбластьМакета.Параметры.ИмяФайла = ЭлементФайл.ИмяФайла;
			ТабДокумент.Вывести(ОбластьМакета); // РезультатОбработкиИмяОтчетов
		КонецЦикла;
		
		ОбластьМакета = Макет.ПолучитьОбласть("РезультатОбработкиРезультат");
		ОбластьМакета.Параметры.РешениеЦБ = СвойстваИзКвитанции.ОбщиеСведения.НазваниеСтатуса;
		ТабДокумент.Вывести(ОбластьМакета); // РезультатОбработкиРезультат
		
		ОбластьМакета = Макет.ПолучитьОбласть("РезультатОбработкиПротоколШапка");
		ТабДокумент.Вывести(ОбластьМакета); // РезультатОбработкиПротоколШапка
		
		Для каждого ЭлементВложение Из СвойстваИзКвитанции.Вложения Цикл
			ОбластьМакета = Макет.ПолучитьОбласть("РезультатОбработкиПротокол");
			ОбластьМакета.Параметры.ИмяПриложения = ЭлементВложение.ИмяВложения;
			ТабДокумент.Вывести(ОбластьМакета); // РезультатОбработкиПротокол
		КонецЦикла;
		
		ОбластьМакета = Макет.ПолучитьОбласть("Подвал");
		ТабДокумент.Вывести(ОбластьМакета); // Подвал
		
		СформироватьТабличныйДокументСекцияПодписи(ТабДокумент, Макет, ТаблицаПодписей);
		
		ТабДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
		ТабДокумент.АвтоМасштаб = Истина;
	КонецЕсли;
	
	Возврат ТабДокумент;
	
КонецФункции

&НаСервере
Функция ПолучитьОсновныеСвойстваИзКвитанцииЦБ(
		СтрПротокол,
		ЦиклОбмена = Неопределено,
		ТранспортноеСообщение = Неопределено,
		ТелоПриложения = Неопределено)
	
	Если НЕ ЗначениеЗаполнено(СтрПротокол) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	// создаем дерево на основе строки XML
	ОписаниеОшибки = "";
	ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, СтрПротокол, ОписаниеОшибки);
	Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
		ТекстСообщения = СтрШаблон(
			НСтр("ru = 'Ошибка разбора XML квитанции Банка России:
					   |%1'"),
			ИнформацияОбОшибке().Описание);
		ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
		Возврат Неопределено;
	КонецЕсли;
	
	ОбщиеСведения = Новый Структура;
	
	ТаблицаИменФайлов = Новый ТаблицаЗначений;
	ТаблицаИменФайлов.Колонки.Добавить("ИмяФайла");
	
	ТаблицаИменВложений = Новый ТаблицаЗначений;
	ТаблицаИменВложений.Колонки.Добавить("ИмяВложения");
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	УзелРегистрационныйНомер = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "regNum", "Э");
	РегистрационныйНомер = ?(ЗначениеЗаполнено(УзелРегистрационныйНомер), СокрЛП(УзелРегистрационныйНомер.Значение), "");
	ОбщиеСведения.Вставить("РегистрационныйНомер", РегистрационныйНомер);
	
	УзелДатаВремя = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "dateTime", "Э");
	ДатаВремяРезультатаОбработки = ?(ЗначениеЗаполнено(УзелДатаВремя) И ЗначениеЗаполнено(УзелДатаВремя.Значение),
		ДокументооборотСКОВызовСервера.ДатаИзСтрокиРазныхФорматов(УзелДатаВремя.Значение), Неопределено);
	ДатаВремяРезультатаОбработки = ?(ЗначениеЗаполнено(ДатаВремяРезультатаОбработки),
		МестноеВремя(ДатаВремяРезультатаОбработки), ДатаВремяРезультатаОбработки);
	ОбщиеСведения.Вставить("ДатаВремяРезультатаОбработки", ДатаВремяРезультатаОбработки);
	
	УзелСтатус = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "status", "Э");
	СтатусРезультатаОбработки = ?(ЗначениеЗаполнено(УзелСтатус), СокрЛП(УзелСтатус.Значение), "");
	ОбщиеСведения.Вставить("СтатусРезультатаОбработки", СтатусРезультатаОбработки);
	НазваниеСтатуса = ?(нрег(СтатусРезультатаОбработки) = "draft", НСтр("ru = 'Заготовка'"),
		?(нрег(СтатусРезультатаОбработки) = "sent" ИЛИ нрег(СтатусРезультатаОбработки) = "send", НСтр("ru = 'Отправлено'"),
		?(нрег(СтатусРезультатаОбработки) = "delivered", НСтр("ru = 'Загружено'"),
		?(нрег(СтатусРезультатаОбработки) = "processing", НСтр("ru = 'Принято в обработку'"),
		?(нрег(СтатусРезультатаОбработки) = "error", НСтр("ru = 'Ошибка'"),
		?(нрег(СтатусРезультатаОбработки) = "registered", НСтр("ru = 'Зарегистрировано'"),
		?(нрег(СтатусРезультатаОбработки) = "rejected", НСтр("ru = 'Отклонено'"),
		СтатусРезультатаОбработки)))))));
	ОбщиеСведения.Вставить("НазваниеСтатуса", НазваниеСтатуса);
	
	УзелОписание = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "description", "Э");
	ОписаниеРезультатаОбработки = ?(ЗначениеЗаполнено(УзелОписание), СокрЛП(УзелОписание.Значение), "");
	ОбщиеСведения.Вставить("ОписаниеРезультатаОбработки", ОписаниеРезультатаОбработки);
	
	УзелЦвет = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "color", "Э");
	ЦветСтатуса = ?(ЗначениеЗаполнено(УзелЦвет), СокрЛП(УзелЦвет.Значение), "");
	ОбщиеСведения.Вставить("ЦветСтатуса", ЦветСтатуса);
	
	Если ТелоПриложения = Неопределено Тогда
		ПервичныеСообщения = ПолучитьСообщенияЦиклаОбмена(ЦиклОбмена,
			Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьЦБ);
		Если ПервичныеСообщения.Количество() > 0 Тогда
			ФайлыОтчетности = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ПервичныеСообщения[0].Ссылка, Ложь,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ФайлОтчетностиЦБ);
			
			Для каждого ФайлОтчетности Из ФайлыОтчетности Цикл
				НовСтр = ТаблицаИменФайлов.Добавить();
				НовСтр.ИмяФайла = ФайлОтчетности.ИмяФайла;
				Если нрег(Прав(НовСтр.ИмяФайла, 4)) = ".enc" Тогда
					НовСтр.ИмяФайла = Лев(НовСтр.ИмяФайла, СтрДлина(НовСтр.ИмяФайла) - 4);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТранспортноеСообщение) Тогда
			Вложения = КонтекстЭДОСервер.ПолучитьВложенияТранспортногоСообщения(ТранспортноеСообщение, Ложь,
				Перечисления.ТипыСодержимогоТранспортногоКонтейнера.ПротоколПриложениеЦБ);
			
			Для каждого Вложение Из Вложения Цикл
				НовСтр = ТаблицаИменВложений.Добавить();
				НовСтр.ИмяВложения = Вложение.ИмяФайла;
			КонецЦикла;
		КонецЕсли;
		
	Иначе
		Если ЗначениеЗаполнено(ТелоПриложения) Тогда
			ДеревоXML = ЗагрузитьXMLВДеревоЗначений(, ТелоПриложения, ОписаниеОшибки);
			
			Если НЕ ЗначениеЗаполнено(ДеревоXML) Тогда
				ТекстСообщения = СтрШаблон(
					НСтр("ru = 'Ошибка разбора XML приложения к квитанции Банка России:
							   |%1'"),
					ИнформацияОбОшибке().Описание);
				ДлительнаяОтправкаКлиентСервер.ВывестиОшибку(ТекстСообщения);
				Возврат Неопределено;
			КонецЕсли;
			
		Иначе
			ДеревоXML = СоздатьДеревоСтруктурыXML();
		КонецЕсли;

		УзелТипКвитанции = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "AcknowledgementType", "Э");
		ТипКвитанции = ?(ЗначениеЗаполнено(УзелТипКвитанции), СокрЛП(УзелТипКвитанции.Значение), "");
		ОбщиеСведения.Вставить("ТипКвитанции", ТипКвитанции);
		
		УзелКодРезультата = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ResultCode", "Э");
		КодРезультата = ?(ЗначениеЗаполнено(УзелКодРезультата), СокрЛП(УзелКодРезультата.Значение), "");
		ОбщиеСведения.Вставить("КодРезультата", КодРезультата);
		
		УзелТекстРезультата = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ResultText", "Э");
		ТекстРезультата = ?(ЗначениеЗаполнено(УзелТекстРезультата),
			СокрЛП(УзелТекстРезультата.Значение), ОбщиеСведения.ОписаниеРезультатаОбработки);
		ОбщиеСведения.Вставить("ТекстРезультата", ТекстРезультата);
		
		ЭтоОтрицательнаяКвитанция = (нрег(СтатусРезультатаОбработки) = "error"
			ИЛИ нрег(СтатусРезультатаОбработки) = "rejected");
		РезультатОперации = ТекстПоКодуРезультатаЦБ(КодРезультата) + ?(ЗначениеЗаполнено(КодРезультата)
			И (КодРезультата <> "0000" И ЗначениеЗаполнено(ТекстРезультата) ИЛИ ЭтоОтрицательнаяКвитанция), ".
			|", "") + ?(КодРезультата <> "0000" ИЛИ ЭтоОтрицательнаяКвитанция, 
			?((НЕ ЗначениеЗаполнено(КодРезультата) ИЛИ КодРезультата = "0000") И ЭтоОтрицательнаяКвитанция,
			НСтр("ru = 'Обработка завершилась ошибкой'") + ?(ЗначениеЗаполнено(ТекстРезультата), ". ", ""), "")
			+ ТекстРезультата, "");
		ОбщиеСведения.Вставить("РезультатОперации", РезультатОперации);
		
		УзелИдентификаторСообщения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "MessageID", "Э");
		ИдентификаторСообщения = ?(ЗначениеЗаполнено(УзелИдентификаторСообщения),
			СокрЛП(УзелИдентификаторСообщения.Значение), "");
		ОбщиеСведения.Вставить("ИдентификаторСообщения", ИдентификаторСообщения);
		
		УзелИдентификаторИсходногоСообщения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(
			ДеревоXML,,, "CorrelationMessageID", "Э");
		ИдентификаторИсходногоСообщения = ?(ЗначениеЗаполнено(УзелИдентификаторИсходногоСообщения),
			СокрЛП(УзелИдентификаторИсходногоСообщения.Значение), "");
		ОбщиеСведения.Вставить("ИдентификаторИсходногоСообщения", ИдентификаторИсходногоСообщения);
		
		УзелТипСообщения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "MessageType", "Э");
		ТипСообщения = ?(ЗначениеЗаполнено(УзелТипСообщения), СокрЛП(УзелТипСообщения.Значение), "");
		ОбщиеСведения.Вставить("ТипСообщения", ТипСообщения);
		
		УзелПриоритетСообщения = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "Priority", "Э");
		ПриоритетСообщения = ?(ЗначениеЗаполнено(УзелПриоритетСообщения), СокрЛП(УзелПриоритетСообщения.Значение), "");
		ОбщиеСведения.Вставить("ПриоритетСообщения", ПриоритетСообщения);
		
		УзелДатаСоздания = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "CreateTime", "Э");
		ДатаСоздания = ?(ЗначениеЗаполнено(УзелДатаСоздания) И ЗначениеЗаполнено(УзелДатаСоздания.Значение),
			ДокументооборотСКОВызовСервера.ДатаИзСтрокиРазныхФорматов(УзелДатаСоздания.Значение), Неопределено);
		ОбщиеСведения.Вставить("ДатаСоздания", ДатаСоздания);
		
		УзелДатаОтправки = КонтекстЭДОСервер.НайтиСтрокиДереваЗначений(ДеревоXML,,, "ReceiveTime", "Э");
		ДатаОтправки = ?(ЗначениеЗаполнено(УзелДатаОтправки) И ЗначениеЗаполнено(УзелДатаОтправки.Значение),
			ДокументооборотСКОВызовСервера.ДатаИзСтрокиРазныхФорматов(УзелДатаОтправки.Значение), Неопределено);
		ОбщиеСведения.Вставить("ДатаОтправки", ДатаОтправки);
	КонецЕсли;
	
	Возврат Новый Структура("ИмяФайла, ОбщиеСведения, Вложения", ТаблицаИменФайлов, ОбщиеСведения, ТаблицаИменВложений);
	
КонецФункции

&НаСервереБезКонтекста
Функция ТекстПоКодуРезультатаЦБ(КодРезультата, ДобавитьКод = Истина)
	
	Если КодРезультата = "0000" Тогда
		Результат = НСтр("ru = 'Сообщение успешно обработано'");
	ИначеЕсли КодРезультата = "1001" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку не соответствует требуемой структуре'");
	ИначеЕсли КодРезультата = "1002" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку транспортные заголовки заполнены некорректно'");
	ИначеЕсли КодРезультата = "1003" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку его формат не поддерживается Транспортным шлюзом ЕПВВ'");
	ИначеЕсли КодРезультата = "1004" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку его формат не поддерживается данным сервисом Транспортного шлюза ЕПВВ'");
	ИначеЕсли КодРезультата = "1005" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку архив сформирован неверно'");
	ИначеЕсли КодРезультата = "1006" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку служебный конверт не соответствует требуемой структуре'");
	ИначеЕсли КодРезультата = "1007" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку служебный конверт заполнен некорректно'");
	ИначеЕсли КодРезультата = "1008" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку не может быть распаковано из транспортной кодировки'");
	ИначеЕсли КодРезультата = "1009" Тогда
		Результат = НСтр("ru = 'Почтовое сообщение не может содержать несколько вложений с одинаковыми именами'");
	ИначеЕсли КодРезультата = "2001" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку истек интервал ожидания предыдущего сообщения'");
	ИначеЕсли КодРезультата = "2002" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку сообщение с данным порядковым номером уже было обработано'");
	ИначеЕсли КодРезультата = "2003" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку сообщение с данным порядковым номером уже было принято и находится в очереди восстановления последовательности'");
	ИначеЕсли КодРезультата = "2004" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку его порядковый номер не принадлежит текущему операционному дню'");
	ИначеЕсли КодРезультата = "2005" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку отсутствует обязательная информация о порядковом номере'");
	ИначеЕсли КодРезультата = "2006" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку информация о порядковом номере указана некорректно'");
	ИначеЕсли КодРезультата = "2007" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку клиента с указанным идентификатором не существует'");
	ИначеЕсли КодРезультата = "3001" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку не соответствует регламенту'");
	ИначеЕсли КодРезультата = "3002" Тогда
		Результат = НСтр("ru = 'Внутренняя ошибка сервера'");
	ИначеЕсли КодРезультата = "3003" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку заражено вирусом'");
	ИначеЕсли КодРезультата = "3004" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку адресная информация указана некорректно'");
	ИначеЕсли КодРезультата = "3005" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, поскольку обслуживание внешнего абонента временно приостановлено'");
	ИначеЕсли КодРезультата = "3006" Тогда
		Результат = НСтр("ru = 'В ходе обработки сообщения возникли ошибки'");
	ИначеЕсли КодРезультата = "3008" Тогда
		Результат = НСтр("ru = 'Выявлена угроза при проверке файла модулем контентного анализа. Файл перемещен в карантин'");
	ИначеЕсли КодРезультата = "3009" Тогда
		Результат = НСтр("ru = 'В ходе обработки сообщения возникли ошибки'");
	ИначеЕсли КодРезультата = "3010" Тогда
		Результат = НСтр("ru = 'Сообщение не будет обработано, так как не соответствует требуемой схеме XML сообщения'");
	ИначеЕсли КодРезультата = "4001" Тогда
		Результат = НСтр("ru = 'Произошла ошибка при расшифровании сообщения'");
	ИначеЕсли КодРезультата = "4002" Тогда
		Результат = НСтр("ru = 'Произошла ошибка при проверке электронной подписи в сообщении'");
	ИначеЕсли КодРезультата = "4003" Тогда
		Результат = НСтр("ru = 'Произошла ошибка при шифровании сообщения'");
	ИначеЕсли КодРезультата = "4004" Тогда
		Результат = НСтр("ru = 'Произошла ошибка при снятии электронной подписи в сообщении'");
	ИначеЕсли КодРезультата = "4005" Тогда
		Результат = НСтр("ru = 'Произошла ошибка при наложении электронной подписи на сообщение'");
	ИначеЕсли КодРезультата = "4006" Тогда
		Результат = НСтр("ru = 'Произошла ошибка при импорте сертификата'");
	ИначеЕсли КодРезультата = "4010" Тогда
		Результат = НСтр("ru = 'Неизвестная задача'");
	ИначеЕсли КодРезультата = "9001" Тогда
		Результат = НСтр("ru = 'Общая ошибка обработки сообщения'");
	ИначеЕсли КодРезультата = "9002" Тогда
		Результат = НСтр("ru = 'Не удалось подготовить объект к отправке по назначению'");
	ИначеЕсли КодРезультата = "9003" Тогда
		Результат = НСтр("ru = 'Нарушены коммуникации с базой данных, обработка невозможна'");
	ИначеЕсли КодРезультата = "9004" Тогда
		Результат = НСтр("ru = 'Нарушена файловая сессия, обработка невозможна'");
	ИначеЕсли КодРезультата = "9005" Тогда
		Результат = НСтр("ru = 'Ошибка несогласованности данных, обработка невозможна'");
	ИначеЕсли КодРезультата = "9999" Тогда
		Результат = НСтр("ru = 'Ошибка конфигурации, работа далее невозможна'");
	Иначе
		Результат = "";
	КонецЕсли;
	
	Если ДобавитьКод Тогда
		Если ЗначениеЗаполнено(Результат) Тогда
			Результат = СтрШаблон(
				НСтр("ru = '%1 (код: %2)'"),
				Результат,
				КодРезультата);
			
		ИначеЕсли СокрЛП(КодРезультата) <> "" Тогда
			Результат = СтрШаблон(
				НСтр("ru = 'Код: %1'"),
				КодРезультата);
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьОсновныеСвойстваЦБИзЦиклаОбмена(ЦиклОбмена)
	
	ДатыСообщений = Новый Структура;
	
	Запрос = Новый Запрос("ВЫБРАТЬ РАЗРЕШЕННЫЕ
						  |	ТранспортныеСообщения.Тип КАК Тип,
						  |	ТранспортныеСообщения.ДатаТранспорта,
						  |	ТранспортныеСообщения.Дата КАК Дата
						  |ИЗ
						  |	Документ.ТранспортноеСообщение КАК ТранспортныеСообщения
						  |ГДЕ
						  |	ТранспортныеСообщения.ЦиклОбмена = &ЦиклОбмена
						  |");
	Запрос.УстановитьПараметр("ЦиклОбмена", ЦиклОбмена);
	ТаблицаСообщений = Запрос.Выполнить().Выгрузить();
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.ПервичноеСообщениеСодержащееОтчетностьЦБ));
	ДатаОтправкиЗначение = Неопределено;
	ДатаОтправки = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаОтправкиЗначение = СтрСообщение.ДатаТранспорта;
			ДатаОтправки = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=ДДВ");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаОтправкиЗначение", ДатаОтправкиЗначение);
	ДатыСообщений.Вставить("ДатаОтправки", ДатаОтправки);
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатЗагрузкиЦБ));
	ДатаЗагрузкиЗначение = Неопределено;
	ДатаЗагрузки = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаЗагрузкиЗначение = СтрСообщение.ДатаТранспорта;
			ДатаЗагрузки = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=ДДВ");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаЗагрузкиЗначение", ДатаЗагрузкиЗначение);
	ДатыСообщений.Вставить("ДатаЗагрузки", ДатаЗагрузки);
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатПриемаДокументЦБ));
	ДатаПриемаЗначение = Неопределено;
	ДатаПриема = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаПриемаЗначение = СтрСообщение.ДатаТранспорта;
			ДатаПриема = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=ДДВ");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаПриемаЗначение", ДатаПриемаЗначение);
	ДатыСообщений.Вставить("ДатаПриема", ДатаПриема);
	
	СообщенияТекТипа = ТаблицаСообщений.НайтиСтроки(Новый Структура("Тип", Перечисления.ТипыТранспортныхСообщений.РезультатОбработкиДокументЦБ));
	ДатаОбработкиЗначение = Неопределено;
	ДатаОбработки = "";
	Если СообщенияТекТипа.Количество() <> 0 Тогда
		Для Каждого СтрСообщение Из СообщенияТекТипа Цикл
			ДатаОбработкиЗначение = СтрСообщение.ДатаТранспорта;
			ДатаОбработки = Формат(СтрСообщение.ДатаТранспорта, "ДЛФ=ДДВ");
		КонецЦикла;
	КонецЕсли;
	ДатыСообщений.Вставить("ДатаОбработкиЗначение", ДатаОбработкиЗначение);
	ДатыСообщений.Вставить("ДатаОбработки", ДатаОбработки);
	
	Возврат ДатыСообщений;
	
КонецФункции

&НаКлиенте
Процедура ОткрытьФормуНастройкиЭДОсФНСиПФРиРосстатомиЦБ(
		Организация,
		ФормаВладелец = Неопределено,
		Окно = Неопределено,
		ОткрытьСвойстваУчетойЗаписиЕслиИспользуется = Ложь,
		ТипКонтролирующегоОргана = Неопределено) Экспорт
	
	Если ЗначениеЗаполнено(Организация) Тогда 
		
		ПолучаемыеРеквизитыОрганизации = Новый Структура();
		ПолучаемыеРеквизитыОрганизации.Вставить("ВидОбменаСКонтролирующимиОрганами");
		ПолучаемыеРеквизитыОрганизации.Вставить("УчетнаяЗаписьОбмена");
		
		РеквизитыОрганизации = ЗначенияРеквизитовОбъекта(Организация, ПолучаемыеРеквизитыОрганизации);
		
		Если ОткрытьСвойстваУчетойЗаписиЕслиИспользуется И РеквизитыОрганизации.ВидОбменаСКонтролирующимиОрганами =
			ПредопределенноеЗначение("Перечисление.ВидыОбменаСКонтролирующимиОрганами.ОбменВУниверсальномФормате")
			И ЗначениеЗаполнено(РеквизитыОрганизации.УчетнаяЗаписьОбмена) Тогда
			
			ПоказатьЗначение(, РеквизитыОрганизации.УчетнаяЗаписьОбмена);
			
		Иначе
			ЗначенияЗаполнения	= Новый Структура(
				"Организация, ВидОбменаСКонтролирующимиОрганами, УчетнаяЗаписьОбмена",
				Организация, РеквизитыОрганизации.ВидОбменаСКонтролирующимиОрганами, РеквизитыОрганизации.УчетнаяЗаписьОбмена);
			
			// Форма элемента
			ПараметрыОткрытия = Новый Структура;
			ПараметрыОткрытия.Вставить("ЗначенияЗаполнения", 		ЗначенияЗаполнения);
			ПараметрыОткрытия.Вставить("ТипКонтролирующегоОргана", 	ТипКонтролирующегоОргана);
			ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаНастройкиДокументооборотаОрганизации", ПараметрыОткрытия, ФормаВладелец);
		КонецЕсли;
		
	Иначе
		
		// Форма списка
		ПараметрыОткрытия = Новый Структура();
		ПараметрыОткрытия.Вставить("ВидНастроек", Неопределено);
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ФормаНастроекДокументооборотаОрганизаций", ПараметрыОткрытия,,Новый УникальныйИдентификатор,Окно);
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьСписокПредставленнойОтчетности_ДобавитьНижнююТаблицу(ОбластьПодпись, СтрокаПеречНБО)
	
	ОбластьПодпись.Параметры.ВсегоДок = СтрокаПеречНБО.ОбобщДок.ВсегоДок;
	
	Всего_1    = 0;
	Всего_1_ЭК = 0;
	Всего_1_1  = 0;
	Всего_1_2  = 0;
	Всего_2    = 0;
	Всего_2_1  = 0;
	Всего_2_2  = 0;
	
	ВидПредДок = СписокИзXDTO(СтрокаПеречНБО.ОбобщДок, "ВидПредДок");
	Если ВидПредДок <> Неопределено Тогда
		Для каждого СтрВидПредДок Из ВидПредДок Цикл
			Код = СтрВидПредДок.КодПредДок;
			
			Если Код = "03" ИЛИ Код = "08" Тогда
				Всего_1_ЭК = Всего_1_ЭК + Число(СтрВидПредДок.КолПредДок);
			КонецЕсли;
			
			Если Код = "02" ИЛИ Код = "03" ИЛИ Код = "09" Тогда
				Всего_1_1 = Всего_1_1 + Число(СтрВидПредДок.КолПредДок);
			КонецЕсли;
			
			Если Код = "01" ИЛИ Код = "08" ИЛИ Код = "10" Тогда
				Всего_1_2 = Всего_1_2 + Число(СтрВидПредДок.КолПредДок);
			КонецЕсли;
			
			Если Код = "04" Тогда
				Всего_2_1 = Всего_2_1 + Число(СтрВидПредДок.КолПредДок);
			КонецЕсли;
			
			Если Код = "06" ИЛИ Код = "07" Тогда
				Всего_2_2 = Всего_2_2 + Число(СтрВидПредДок.КолПредДок);
			КонецЕсли;
			
		КонецЦикла;
		
		Всего_1 = Всего_1_1 + Всего_1_2;
		Всего_2 = Всего_2_1 + Всего_2_2;
		
	КонецЕсли;
	
	ОбластьПодпись.Параметры.Всего_1    = Всего_1;
	ОбластьПодпись.Параметры.Всего_1_ЭК = Всего_1_ЭК;
	ОбластьПодпись.Параметры.Всего_1_1  = Всего_1_1;
	ОбластьПодпись.Параметры.Всего_1_2  = Всего_1_2;
	ОбластьПодпись.Параметры.Всего_2    = Всего_2;
	ОбластьПодпись.Параметры.Всего_2_1  = Всего_2_1;
	ОбластьПодпись.Параметры.Всего_2_2  = Всего_2_2;
	
КонецФункции

&НаСервереБезКонтекста
Функция ЕстьСвойствоXDTO(Узел, Поле) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЕстьСвойствоXDTO(Узел, Поле); 
	
КонецФункции

&НаСервереБезКонтекста
Функция ВерсияФормата(АдресФайла)
	
	Возврат ДокументооборотСКО.ВерсияФормата(АдресФайла);
	
КонецФункции

&НаСервереБезКонтекста
Функция ДобавитьПодписьВСправкуИОН(ЗапросИОН, ТабличныйДокумент)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.ДобавитьШтампПодписиВРезультатЗапросаИОН(ЗапросИОН, ТабличныйДокумент, 2, Истина);
	
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

// формирование запроса СЭДО ФСС
&НаСервереБезКонтекста
Функция ПолучитьДатыПолученияСообщений(
	Организация,
	ТекущаяДата = Неопределено,
	ОбменПоСЭДОНапрямую = Ложь,
	РазрешенАнализВходящих = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	СведенияОДатахПолученияСообщений = КонтекстЭДОСервер.ПолучитьДатыПолученияСообщений(
		Организация,
		ТекущаяДата,
		ОбменПоСЭДОНапрямую,
		РазрешенАнализВходящих);
	Возврат СведенияОДатахПолученияСообщений;
	
КонецФункции

&НаСервереБезКонтекста
Процедура СохранитьДатуСеансаОбменаСЭДОФСС(
		Организация,
		ТекущаяДата,
		ОбменПоСЭДОНапрямую = Истина,
		ЭтоМестноеВремя = Ложь)
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	КонтекстЭДОСервер.СохранитьДатуСеансаОбменаСЭДОФСС(
		Организация,
		ТекущаяДата,
		ОбменПоСЭДОНапрямую);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ШаблонТаблицыНеДокФорм()
				
	// определяем таблицу с информацией о файлах сканов
	ТаблицаНеДокФорм = Новый ТаблицаЗначений;
	ТаблицаНеДокФорм.Колонки.Добавить("Документ");
	ТаблицаНеДокФорм.Колонки.Добавить("ПорНом");
	ТаблицаНеДокФорм.Колонки.Добавить("МассивФайлов", Новый ОписаниеТипов("Массив"));
	ТаблицаНеДокФорм.Колонки.Добавить("ИдФайлЭП");
	ТаблицаНеДокФорм.Колонки.Добавить("АдресЭП");
	ТаблицаНеДокФорм.Колонки.Добавить("КодДок");
	ТаблицаНеДокФорм.Колонки.Добавить("ЭтоПакетСДопДокументами");

	Возврат ТаблицаНеДокФорм;
			
КонецФункции

&НаСервере
Функция ДобавитьСтрокуТаблицыНеДокФорм(
			ИдентификаторОтправителяПолучателя, 
			ИДФормы,
			СтрокаПредставляемыйДокумент,
			ТаблицаНеДокФорм,
			ОписьСсылка)
			
	ЭтоФормат_ON_DOCNPNO_1_886_00_05_02 = ТребованияФНСКлиентСервер.ЭтоФормат_ON_DOCNPNO_1_886_00_05_02();
			
	СсылкаСканированныйДокумент = СтрокаПредставляемыйДокумент.СсылкаНаОбъект;
	
	// формируем GUID файла, одинаковый для всех файлов документа
	N1 = Строка(Новый УникальныйИдентификатор);

	// сохраняем вложения в каталог выгрузки
	ВложенияСДанными = ПолучитьТаблицуПрисоединенныхФайловСканДокумента(СсылкаСканированныйДокумент);
	
	ПорНом = ?(СтрокаПредставляемыйДокумент.ЯвляетсяИнформацией, "2.", "1.") 
		+ Формат(СтрокаПредставляемыйДокумент.ПорядковыйНомер, "ЧЦ=2; ЧН=; ЧВН=");

	КодДок = КодДокДляДокСкана(ОписьСсылка, СсылкаСканированныйДокумент);
	
	ЗначениеЗаполнения = Новый Структура();
	ЗначениеЗаполнения.Вставить("Документ", СсылкаСканированныйДокумент);
	ЗначениеЗаполнения.Вставить("ПорНом", ПорНом);
	ЗначениеЗаполнения.Вставить("КодДок", КодДок);
	ЗначениеЗаполнения.Вставить("ЭтоПакетСДопДокументами", ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка));
	
	НовСтр = ТаблицаНеДокФорм.Добавить();
	ЗаполнитьЗначенияСвойств(НовСтр, ЗначениеЗаполнения);
	
	ДобавитьПодпись = 
		ЭтоФормат_ON_DOCNPNO_1_886_00_05_02 
		ИЛИ НЕ ЭтоФормат_ON_DOCNPNO_1_886_00_05_02 И ВложенияСДанными.Количество() = 1;
	
	МассивФайлов = Новый Массив;
	Для Каждого СтрВложение Из ВложенияСДанными Цикл
		
		ИмяФайла = ИмяФайлаВложенияНеДокФорм(
			ИдентификаторОтправителяПолучателя, 
			СтрВложение.ИмяФайла,
			КодДок,
			N1);
		
		// ЭП
		ПрисоединенныйФайл = СтрВложение.Ссылка;
		
		ЭП = ИдФайлИАдрес_ЭПНеДокФорм(ПрисоединенныйФайл, ИмяФайла, ДобавитьПодпись);
		ИдФайлЭП = ЭП.ИдФайлЭП;
		АдресЭП  = ЭП.АдресЭП;
		
		Если НЕ ЗначениеЗаполнено(АдресЭП) 
			И НЕ ПрисоединенныйФайл.ЕстьВстроеннаяПодпись
			И ПрисоединенныйФайл.ВидPDF = ПредопределенноеЗначение("Перечисление.ВидыPDF.ДокPDF")
			И ДобавитьПодпись Тогда
			
			ИдФайлЭП = ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИмяФайла);
			
		КонецЕсли;
		
		ЭлементВыгрузки = Новый Структура;
		ЭлементВыгрузки.Вставить("ИмяФайла", ИмяФайла);
		ЭлементВыгрузки.Вставить("АдресВременногоХранилища", ПоместитьВоВременноеХранилище(СтрВложение.Данные, ИДФормы));
		ЭлементВыгрузки.Вставить("ЕстьВстроеннаяПодпись",    ПрисоединенныйФайл.ЕстьВстроеннаяПодпись);
		ЭлементВыгрузки.Вставить("ВидPDF", 					 ПрисоединенныйФайл.ВидPDF);
		ЭлементВыгрузки.Вставить("Расширение", 				 ПрисоединенныйФайл.Расширение);
		
		ЭтоПоследний = ВложенияСДанными.Индекс(СтрВложение) = ВложенияСДанными.Количество() - 1;
		
		РазбитьСтрокиНеДокФормПоЭлПодписи(
			ТаблицаНеДокФорм,
			ЗначениеЗаполнения,
			МассивФайлов,
			ЭлементВыгрузки,
			НовСтр,
			АдресЭП,
			ИдФайлЭП,
			НЕ ЭтоПоследний);
		
	КонецЦикла;
	
КонецФункции

&НаСервере
Процедура ДобавитьТекстовоеПояснение(
			ИдентификаторОтправителяПолучателя, 
			ИДФормы,
			ТаблицаНеДокФорм,
			ОписьСсылка)
			
	ДвДанные = Справочники.ШаблоныПоясненийДляФНС.ДвДанныеТекстовогоПоясненияПоСсылке(
		ОписьСсылка,
		ТекстовоеПояснение, 
		"ТекстовоеПояснение");
		
	ЕстьТекст = Справочники.ШаблоныПоясненийДляФНС.ЗаполненТекстФорматированногоДокумента(
		ТекстовоеПояснение);
	
	Если НЕ ЕстьТекст Тогда
		Возврат;
	КонецЕсли;
	
	// формируем GUID файла, одинаковый для всех файлов документа
	N1 = Строка(Новый УникальныйИдентификатор);

	ЭтоПакетСДопДокументами = ДокументооборотСКОВызовСервера.ЭтоПакетСДопДокументами(ОписьСсылка);
	
	КодДок = "8888"; // Ответ на требование о представлении пояснений
	
	ИмяФайла = ИмяФайлаВложенияНеДокФорм(
			ИдентификаторОтправителяПолучателя, 
			"*.pdf", // Отсюда берется расширение
			КодДок,
			N1);
		
	ЭлементВыгрузки = Новый Структура;
	ЭлементВыгрузки.Вставить("ИмяФайла", ИмяФайла);
	ЭлементВыгрузки.Вставить("АдресВременногоХранилища", ПоместитьВоВременноеХранилище(ДвДанные, ИДФормы));
	ЭлементВыгрузки.Вставить("ЕстьВстроеннаяПодпись",    Ложь);
	ЭлементВыгрузки.Вставить("ВидPDF", 					 Перечисления.ВидыPDF.ДокPDF);
	ЭлементВыгрузки.Вставить("Расширение", 				 "PDF");
	
	ПорНом = "2.00"; // Информация, не документ

	ЗначениеЗаполнения = Новый Структура();
	ЗначениеЗаполнения.Вставить("Документ", Неопределено);
	ЗначениеЗаполнения.Вставить("ПорНом", ПорНом);
	ЗначениеЗаполнения.Вставить("КодДок", КодДок);
	ЗначениеЗаполнения.Вставить("ЭтоПакетСДопДокументами", ЭтоПакетСДопДокументами);
	
	НовСтр = ТаблицаНеДокФорм.Добавить();
	ЗаполнитьЗначенияСвойств(НовСтр, ЗначениеЗаполнения);
	
	МассивФайлов = Новый Массив;
	МассивФайлов.Добавить(ЭлементВыгрузки);
	
	НовСтр.МассивФайлов = МассивФайлов;
	НовСтр.ИдФайлЭП = ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИмяФайла);
	НовСтр.АдресЭП = "";
	
КонецПроцедуры

&НаСервере
Процедура ПеренестиТекстовоеПояснениеВДеревоДокументовПослеОтправки(ОписьСсылка) Экспорт
	
	ДвДанные = Справочники.ШаблоныПоясненийДляФНС.ДвДанныеТекстовогоПоясненияПоСсылке(
		ОписьСсылка,
		ТекстовоеПояснение, 
		"ТекстовоеПояснение");
		
	ЕстьТекст = Справочники.ШаблоныПоясненийДляФНС.ЗаполненТекстФорматированногоДокумента(
		ТекстовоеПояснение);
	
	Если НЕ ЕстьТекст Тогда
		Возврат;
	КонецЕсли;
	
	Справочники.ОписиИсходящихДокументовВНалоговыеОрганы.ПеренестиТекстовоеПояснениеВДеревоДокументов(
		ОписьСсылка, 
		ДвДанные);
		
	Справочники.ШаблоныПоясненийДляФНС.УбратьВыделениеУПоясненияПослеОтправки(
		ОписьСсылка,
		ТекстовоеПояснение, 
		"ТекстовоеПояснение");
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЭПДокСкана(ПрисоединенныйФайл) Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.ЭПДокСкана(ПрисоединенныйФайл);
	
КонецФункции

&НаКлиенте
Процедура НапечататьПодтверждениеПраваАдмин(Заявление) Экспорт
	
	ТабДок = ТабличныйДокументПодтвержденияПрава(Заявление);
	
	ЗаголовокТабДок = "Подтверждение права администратора";
	
	НапечататьДокумент(ТабДок, ЗаголовокТабДок);

КонецПроцедуры

&НаСервере
Функция ТабличныйДокументПодтвержденияПрава(Заявление) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	ТабДокумент = Новый ТабличныйДокумент;
	ТабДокумент.КлючПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_ПодтверждениеПрава";
	
	КонтекстЭДОСервер  = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	ЭтоЮридическоеЛицо = ЭлектронныйДокументооборотСКонтролирующимиОрганамиВызовСервера.ЭтоЮрЛицо(Заявление.Организация);
	
	Макет = Документы.ЗаявлениеАбонентаСпецоператораСвязи.ПолучитьМакет("ПодтверждениеПраваАдмин");
		
	Область = Макет.ПолучитьОбласть("Заявление");
	Область.Параметры.Заполнить(Заявление);
	
	Область.Параметры.УчетнаяЗапись = КонтекстЭДОСервер.УчетнаяЗаписьОрганизации(Заявление.Организация);
	
	ДобавитьДанныеРуководителяВЗаявление(Заявление, Область);
	
	КонтекстЭДОСервер.РазместитьНаОдномЛисте(ТабДокумент);
	ТабДокумент.Вывести(Область);
	
	Возврат ТабДокумент;
		
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьКоличествоЗапросовИсторическихСообщений(Организация, ОбменПоСЭДОНапрямую = Неопределено)
	
	Возврат ЭлектронныйДокументооборотСФСС.КоличествоЗапросовИсторическихСообщенийОрганизации(
		Организация, ОбменПоСЭДОНапрямую);
	
КонецФункции

&НаСервереБезКонтекста
Функция ИмяФайлаВложенияНеДокФорм(
			ИдентификаторОтправителяПолучателя, 
			ИмяФайла,
			КодДок,
			N1)
		
	// формируем GUID файла, уникальный для каждого файла
	N2 = Строка(Новый УникальныйИдентификатор);
	
	//извлекаем расширение из оригинального имени файла вложения
	РасширениеФайла = РасширениеФайла(ИмяФайла);
	
	// составляем новое короткое имя файла вложения
	GGGGMMDD = Формат(ТекущаяДатаСеанса(), "ДФ=yyyyMMdd");
	
	ИмяФайла = ВРЕГ(КодДок + "_" + ИдентификаторОтправителяПолучателя + "_" + N1 + "_" + GGGGMMDD + "_" + N2) + "." + РасширениеФайла;
	
	Возврат ИмяФайла;
	
КонецФункции

&НаСервереБезКонтекста
Функция ИдФайлИАдрес_ЭПНеДокФорм(ПрисоединенныйФайл, ИмяФайла, ДобавитьПодпись)
	
	// ЭП
	ИдФайлЭП = "";
	АдресЭП  = "";

	Если ДобавитьПодпись Тогда
		
		ЭПДокСкана = ЭПДокСкана(ПрисоединенныйФайл);
		
		Если ЗначениеЗаполнено(ЭПДокСкана.АдресДанных) И ЗначениеЗаполнено(ЭПДокСкана.ИмяФайла) Тогда
			
			// Имя подписи образуется от имени файла и не зависит от первоначального имени файла подписи
			ИмяФайлаЭППоФормату = ИмяФайлаЭПФайлаОписиИсходящихДокументов(ИмяФайла);
			
			ИдФайлЭП 	= ИмяФайлаЭППоФормату;
			АдресЭП  	= ЭПДокСкана.АдресДанных;
			
		КонецЕсли;
	КонецЕсли;
	
	Результат = Новый Структура();
	Результат.Вставить("ИдФайлЭП", ИдФайлЭП);
	Результат.Вставить("АдресЭП", АдресЭП);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция РазбитьСтрокиНеДокФормПоЭлПодписи(
		ТаблицаНеДокФорм,
		ЗначениеЗаполнения,
		МассивФайлов,
		ЭлементВыгрузки,
		НовСтр,
		АдресЭП,
		ИдФайлЭП,
		ДобавитьПустышку)
		
	ЭтоФормат_ON_DOCNPNO_1_886_00_05_02 = ТребованияФНСКлиентСервер.ЭтоФормат_ON_DOCNPNO_1_886_00_05_02();
		
	ВынестиВОтдельныйУзел = ЭтоФормат_ON_DOCNPNO_1_886_00_05_02
		И (ЗначениеЗаполнено(АдресЭП) ИЛИ ЭлементВыгрузки.ВидPDF = Перечисления.ВидыPDF.ДокPDF);
			
	Если ВынестиВОтдельныйУзел
		И МассивФайлов.Количество() > 0 Тогда
		
		// Финализируем старый блок
		// файлы с подписью добавляем в новый блок
		НовСтр = ТаблицаНеДокФорм.Добавить();
		ЗаполнитьЗначенияСвойств(НовСтр, ЗначениеЗаполнения);

		МассивФайлов = Новый Массив;
		
	КонецЕсли;
	
	МассивФайлов.Добавить(ЭлементВыгрузки);
	
	НовСтр.МассивФайлов = МассивФайлов;
	НовСтр.ИдФайлЭП 	= ИдФайлЭП;
	НовСтр.АдресЭП 		= АдресЭП;
	
	Если ВынестиВОтдельныйУзел И ДобавитьПустышку Тогда
		
		// следующие файлы будем добавлять в новый блок
		НовСтр = ТаблицаНеДокФорм.Добавить();
		ЗаполнитьЗначенияСвойств(НовСтр, ЗначениеЗаполнения);

		МассивФайлов = Новый Массив;
		
		ИдФайлЭП = "";
		АдресЭП  = "";
		
	КонецЕсли;
		
КонецФункции

&НаСервереБезКонтекста
Процедура ЗаписатьПодписьМЧДФСС(ОтчетСсылка, ПодписанныйЗапросSOAP64)
	
	Если ТипЗнч(ПодписанныйЗапросSOAP64) = Тип("Строка") Тогда
		Если ЭтоАдресВременногоХранилища(ПодписанныйЗапросSOAP64) Тогда
			ДанныеПодписанногоЗапросаSOAP = ПолучитьИзВременногоХранилища(ПодписанныйЗапросSOAP64);
		Иначе
			ДанныеПодписанногоЗапросаSOAP = Base64Значение(ПодписанныйЗапросSOAP64);
		КонецЕсли;
	Иначе
		ДанныеПодписанногоЗапросаSOAP = ПодписанныйЗапросSOAP64;
	КонецЕсли;
	
	ОбъектМЧДФСС = ОтчетСсылка.ПолучитьОбъект();
	ОбъектМЧДФСС.ИмяФайлаВыгрузки 	= "P_" + СокрЛП(ОбъектМЧДФСС.НомерДоверенности) + ".xml";
	ОбъектМЧДФСС.ОтпечатокСертификата =
		ДокументооборотСКОВызовСервера.ЗапоминаниеОшибок(,, Истина, "МЧДОтпечатокРуководителя");
	ОбъектМЧДФСС.ЭлектроннаяПодпись = Новый ХранилищеЗначения(ДанныеПодписанногоЗапросаSOAP);
	ОбъектМЧДФСС.Статус = Перечисления.СтатусыМашиночитаемойДоверенностиКО.Подписано;
	ОбъектМЧДФСС.Записать();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьУПУППослеОбмена(ОписаниеОповещения = Неопределено) Экспорт
	
	Если ЕстьУПУПыКОтправкеПослеОдобренияЗПЭД() Тогда
		ПодключитьОбработчикОжидания("Подключаемый_ОтправитьУПУППослеОбмена", 15, Истина);
	КонецЕсли;
	
	ВыполнитьОбработкуОповещения(ОписаниеОповещения);
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОтправитьУПУППослеОбмена() Экспорт
	
	// Если бублик обмена открыт, то при попытке отправки УПУП возникает ошибка 
	// "Отправка не может быть начата. Дождитесь завершения предыдущей операции.".
	// Поэтому надо дождаться закрытия бублика и только после этого предлагать отправить УПУП.
	БубликОткрыт = ДлительнаяОтправкаКлиент.ФормаДлительнойОтправкиЕстьСредиОткрытых(Истина);
	
	Если БубликОткрыт Тогда
		ПодключитьОбработчикОжидания("Подключаемый_ОтправитьУПУППослеОбмена", 15, Истина);
	Иначе
		ОткрытьФорму(ПутьКОбъекту + ".Форма.ОтправкаУПУППослеОдобренияЗПЭД");
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция УПУПыКОтправкеПослеОдобренияЗПЭД() Экспорт
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	Возврат КонтекстЭДОСервер.УПУПыКОтправкеПослеОдобренияЗПЭД();
	
КонецФункции

&НаСервереБезКонтекста
Функция ЕстьУПУПыКОтправкеПослеОдобренияЗПЭД() Экспорт
	
	Возврат УПУПыКОтправкеПослеОдобренияЗПЭД().Количество() > 0;
	
КонецФункции

#Область МетаданныеВходящихСообщенийСЭДОЗаПериодБезКриптографии

// Вызывается только для обмена через оператора, при вызове напрямую может возникать ошика 403.
&НаСервереБезКонтекста
Функция МетаданныеВходящихСообщенийСЭДОЗаПериодБезКриптографии(Организация,
		ДатыПолученияСообщений, ЭтоПодпискаНаОператора, СвойстваОбменаПоСЭДОЧерезОператора)
		
	Результат = Новый Структура;
	Результат.Вставить("Выполнено", Ложь);
	Результат.Вставить("ОписаниеОшибки", "");
	
	КонтекстЭДОСервер = ДокументооборотСКО.ПолучитьОбработкуЭДО();
	
	ПустаяДата = Дата(1, 1, 1);
	БылиОшибки = Ложь;
	ДатаУспешнойЗагрузкиИдентификаторов = ПустаяДата;
	ДатаПолучениеСпискаСообщенийПодпискаНаОператора = ЭлектронныйДокументооборотСФСС.ДатаСобытияСЭДО(
		Организация, Перечисления.СобытияОбменаСЭДО.ПолучениеСпискаСообщенийПодпискаНаОператора);
	Для каждого ДанныеТекущаяДата Из ДатыПолученияСообщений Цикл
		ТекущаяДата = ДанныеТекущаяДата.Дата;
		Если ЭтоПодпискаНаОператора
			И Текущаядата < ДатаПолучениеСпискаСообщенийПодпискаНаОператора Тогда
			Продолжить;
		КонецЕсли;
		РезультатПолучения = КонтекстЭДОСервер.МетаданныеВходящихСообщенийСЭДОФСС(Организация,
			ТекущаяДата, ,
			СвойстваОбменаПоСЭДОЧерезОператора);
		Если НЕ РезультатПолучения.Выполнено Тогда
			Шаблон = НСтр("ru='При получении списка входящих сообщений из СЭДО ФСС по организации "
				+ "%1 за дату %2 произошла ошибка: %3.'");
			Шаблон = ДокументооборотСКОКлиентСервер.ЗаменитьПФРиФССнаСФР(Шаблон, Истина);
			ОписаниеОшибки = СтрШаблон(Шаблон, Организация, ТекущаяДата, РезультатПолучения.ОписаниеОшибки);
			ЗаписатьСобытиеСЭДОВЖурнал("Ошибка", ОписаниеОшибки);
			Результат.ОписаниеОшибки = РезультатПолучения.ОписаниеОшибки;
			БылиОшибки = Истина;
			Прервать;
		КонецЕсли;
		ДатаУспешнойЗагрузкиИдентификаторов = ЭлектронныйДокументооборотСФССКлиентСервер.ПолучитьДатуПолученияСообщений(
				ДанныеТекущаяДата);
	КонецЦикла;
	
	Если НЕ ДатаУспешнойЗагрузкиИдентификаторов = ПустаяДата Тогда
		Если ЭтоПодпискаНаОператора Тогда
			// Сохраняется дата получения сообщений при подписке на оператора.
			ДанныеСобытия = ЭлектронныйДокументооборотСФСС.НовыеДанныеСобытияСЭДО();
			ДанныеСобытия.Событие 						= Перечисления.СобытияОбменаСЭДО.ПолучениеСпискаСообщенийПодпискаНаОператора;
			ДанныеСобытия.Организация 					= Организация;
			ДанныеСобытия.ОбменЧерезОператора 			= НЕ СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую;
			ДанныеСобытия.СохранятьЕслиДатаМаксимальная = Истина;
			ДанныеСобытия.ДатаСобытия 					= НачалоДня(ДатаУспешнойЗагрузкиИдентификаторов);
			ЭлектронныйДокументооборотСФСС.СохранитьДанныеСобытияСЭДО(ДанныеСобытия);
		Иначе
			СохранитьДатуСеансаОбменаСЭДОФСС(
				Организация,
				ДатаУспешнойЗагрузкиИдентификаторов,
				СвойстваОбменаПоСЭДОЧерезОператора.ОбменПоСЭДОНапрямую);
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ БылиОшибки Тогда
		Результат.Выполнено = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

&НаКлиенте
Процедура СформироватьПакетПФР_ПослеУстановкиСертификатаЕЦП(Результат, ВходящийКонтекст) Экспорт
	
	Оповещение = Новый ОписаниеОповещения(
		"СформироватьПакетПФР_ПолучитьИПроверитьСертификатыПолучателяПослеПолученияСертификатов",
		ЭтотОбъект, ВходящийКонтекст);
	
	СертификатыДляПоиска = Новый Массив;
	Для Каждого Отпечаток Из ВходящийКонтекст.Отпечатки Цикл
		НовыйСертификат = Новый Структура("Отпечаток", Отпечаток);
		КонтекстМоделиХраненияКлюча(ВходящийКонтекст.МестоХраненияКлюча, НовыйСертификат);
		СертификатыДляПоиска.Добавить(НовыйСертификат);
	КонецЦикла;
	
	КриптографияЭДКОКлиент.НайтиСертификаты(Оповещение, СертификатыДляПоиска, Истина);
	
КонецПроцедуры
