#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЭтоУстановка = Параметры.ЭтоУстановка;
	ЭтоУдаление = Параметры.ЭтоУдаление;
	ДополнительныеПараметры = Параметры.ДополнительныеПараметры;
	МонопольныйРежимУстановлен = Ложь;
	
	Если ЭтоУстановка Тогда
		Элементы.ПовторитьПопыткуУстановки.Заголовок = НСтр("ru = 'Повторить попытку установки'");
	ИначеЕсли ЭтоУдаление Тогда
		Элементы.ПовторитьПопыткуУстановки.Заголовок = НСтр("ru = 'Повторить попытку удаления'");
	Иначе
		Элементы.ПовторитьПопыткуУстановки.Заголовок = НСтр("ru = 'Повторить попытку восстановления'");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ТекстПредупрежденияОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	СтандартныеПодсистемыКлиент.ОткрытьСписокАктивныхПользователей();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПовторитьПопыткуУстановки(Команда)
	
	Если УстановитьМонопольныйРежимНаСервере(ЭтоУстановка, ЭтоУдаление) Тогда 
		МонопольныйРежимУстановлен = Истина;
		Закрыть();
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)     
	МонопольныйРежимУстановлен = Ложь;
	Закрыть();
КонецПроцедуры   

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)     
	
	ВозвращаемоеЗначение = ВозвращаемоеЗначение();
	ВозвращаемоеЗначение.ЭтоУстановка = ЭтоУстановка;
	ВозвращаемоеЗначение.ЭтоУдаление = ЭтоУдаление;
	ВозвращаемоеЗначение.ЗавершениеРаботы = ЗавершениеРаботы;
	ВозвращаемоеЗначение.МонопольныйРежимУстановлен = МонопольныйРежимУстановлен;
	ВозвращаемоеЗначение.ДополнительныеПараметры = ДополнительныеПараметры;
	
	ОповеститьОВыборе(ВозвращаемоеЗначение);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
Функция УстановитьМонопольныйРежимНаСервере(ЭтоУстановка, ЭтоУдаление)
	
	Возврат Обработки.ПлюсСервис.УстановитьМонопольныйРежимНаСервере(ЭтоУстановка, ЭтоУдаление);
	
КонецФункции

&НаКлиенте
Функция ВозвращаемоеЗначение()
	
	ВозвращаемоеЗначение = Новый Структура;
	ВозвращаемоеЗначение.Вставить("ЭтоУстановка", Ложь);
	ВозвращаемоеЗначение.Вставить("ЭтоУдаление", Ложь);
	ВозвращаемоеЗначение.Вставить("МонопольныйРежимУстановлен", Ложь);
	ВозвращаемоеЗначение.Вставить("ЗавершениеРаботы", Ложь);
	ВозвращаемоеЗначение.Вставить("ДополнительныеПараметры", Неопределено);
	
	Возврат ВозвращаемоеЗначение;
	
КонецФункции

#КонецОбласти
