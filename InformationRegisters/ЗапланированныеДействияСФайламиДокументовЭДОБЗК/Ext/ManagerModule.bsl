#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

Функция ФайлыНаПодписьПользователя(ТолькоРазрешенные, Знач Исполнитель = Неопределено, Знач ВключаяОзнакомления = Ложь) Экспорт
	
	Если Исполнитель = Неопределено Тогда
		Исполнитель = Пользователи.ТекущийПользователь();
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	ДействияПользователя = Новый Массив;
	ДействияПользователя.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать);
	Если ВключаяОзнакомления Тогда
		ДействияПользователя.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.Ознакомиться);
	КонецЕсли;
	
	СоздатьВТФайлыКОбработкеСДаннымиПечатныхФорм(
		ТолькоРазрешенные,
		Запрос.МенеджерВременныхТаблиц,
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Исполнитель),
		ДействияПользователя);
	
	Запрос.УстановитьПараметр("Исполнитель", Исполнитель);
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("МаксимальныйПериодОжидания", МаксимальныйПериодОжидания());
	Запрос.УстановитьПараметр("ДействияПользователя", ДействияПользователя);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ПечатныеФормыКОбработке.ПрисоединенныйФайл КАК ФайлОбъекта,
		|	ПодписанныеФормы.ИдентификаторПечатнойФормы КАК ИдентификаторПечатнойФормы,
		|	ТаблицаРегистра.ПрисоединенныйФайл.ВладелецФайла КАК Владелец,
		|	ТаблицаРегистра.Действие КАК Действие,
		|	ВЫБОР
		|		КОГДА РАЗНОСТЬДАТ(ТаблицаРегистра.ДатаЗадания, &ТекущаяДата, МИНУТА) > &МаксимальныйПериодОжидания
		|			ТОГДА ИСТИНА
		|		ИНАЧЕ ЛОЖЬ
		|	КОНЕЦ КАК Просрочено,
		|	ВЫБОР
		|		КОГДА ПодписанныеФормы.ПрисоединенныйФайл ЕСТЬ NULL
		|			ТОГДА ЛОЖЬ
		|		ИНАЧЕ ИСТИНА
		|	КОНЕЦ КАК ПечатнаяФорма,
		|	ПодписанныеФормы.Оригинал КАК Оригинал,
		|	ПодписанныеФормы.Название КАК Название,
		|	ПодписанныеФормы.Организация КАК Организация,
		|	ПодписанныеФормы.Сотрудник КАК Сотрудник,
		|	ПодписанныеФормы.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ПодписанныеФормы.Номер КАК Номер,
		|	ПодписанныеФормы.Дата КАК Дата
		|ИЗ
		|	ВТФайлыКОбработкеСДаннымиПечатныхФорм КАК ПечатныеФормыКОбработке
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|		ПО ПечатныеФормыКОбработке.ПрисоединенныйФайл = ТаблицаРегистра.ПрисоединенныйФайл
		|			И (ТаблицаРегистра.Исполнитель = &Исполнитель)
		|			И (ТаблицаРегистра.Действие В (&ДействияПользователя))
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ПредыдущаяОчередность
		|		ПО (ТаблицаРегистра.ПрисоединенныйФайл = ПредыдущаяОчередность.ПрисоединенныйФайл)
		|			И (ТаблицаРегистра.ОжидаетПодписания = ПредыдущаяОчередность.Исполнитель)
		|			И (ТаблицаРегистра.Действие = ПредыдущаяОчередность.Действие)
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПодписанныеПечатныеФормы КАК ПодписанныеФормы
		|		ПО ПечатныеФормыКОбработке.ПрисоединенныйФайл = ПодписанныеФормы.ПрисоединенныйФайл
		|ГДЕ
		|	ТаблицаРегистра.Исполнитель = &Исполнитель
		|	И ПредыдущаяОчередность.Исполнитель ЕСТЬ NULL
		|	И ТаблицаРегистра.Действие В(&ДействияПользователя)";
	
	УстановитьПривилегированныйРежим(Истина);
	ФайлыНаПодпись = Запрос.Выполнить().Выгрузить();
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат ФайлыНаПодпись;
	
КонецФункции

Функция ЗаданияОбработки(ТолькоРазрешенные, Действия = Неопределено) Экспорт
	
	Задания = Новый Соответствие;
	
	ДействияСФайламиДокументовЭДОБЗК = Новый Массив;
	ДействияСФайламиДокументовЭДОБЗК.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.ЗаписатьНаДиск);
	
	Если РаботаСПочтовымиСообщениями.ДоступнаОтправкаПисем() Тогда
		ДействияСФайламиДокументовЭДОБЗК.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.ОтправитьПоПочте);
	КонецЕсли;
	
	Если ИнтеграцияСРаботаРоссии.ДоступнаПередачаДокументовНаРаботаРоссии() Тогда
		ДействияСФайламиДокументовЭДОБЗК.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.ПередатьНаРаботаРоссии);
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	СоздатьВТФайлыКОбработке(
		ТолькоРазрешенные,
		Запрос.МенеджерВременныхТаблиц,
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			Справочники.Пользователи.ПустаяСсылка()),
		ДействияСФайламиДокументовЭДОБЗК);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ФайлыКОбработке.Действие КАК Действие,
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ФайлыКОбработке.ПрисоединенныйФайл) КАК КоличествоПечатныхФорм
		|ИЗ
		|	ВТФайлыКОбработке КАК ФайлыКОбработке
		|ГДЕ
		|	ФайлыКОбработке.КоличествоПодписантов = 0
		|	И &ОтборПоДействию
		|
		|СГРУППИРОВАТЬ ПО
		|	ФайлыКОбработке.Действие
		|
		|УПОРЯДОЧИТЬ ПО
		|	Действие";
	
	Если Действия = Неопределено Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоДействию", "(ИСТИНА)");
	Иначе
		Запрос.УстановитьПараметр("Действия", Действия);
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоДействию", "ФайлыКОбработке.Действие В (&Действия)");
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Пока Выборка.Следующий() Цикл
		Задания.Вставить(Выборка.Действие, Выборка.КоличествоПечатныхФорм);
	КонецЦикла;
	
	Возврат Задания;
	
КонецФункции

Процедура ЗарегистрироватьОбработкуФайловИсполнителям(ФайлыКОбработке, Действие, Исполнители, УведомитьИсполнителей = Ложь) Экспорт
	
	Для Каждого ФайлКОбработке Из ФайлыКОбработке Цикл
		
		ОжидаетПодписания = Неопределено;
		Для Каждого Исполнитель Из Исполнители Цикл
			
			НаборЗаписей = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.ПрисоединенныйФайл.Установить(ФайлКОбработке);
			НаборЗаписей.Отбор.Исполнитель.Установить(Исполнитель);
			НаборЗаписей.Отбор.Действие.Установить(Действие);
			
			Запись = НаборЗаписей.Добавить();
			Запись.ПрисоединенныйФайл = ФайлКОбработке;
			Запись.ДатаЗадания        = ТекущаяДатаСеанса();
			Запись.Исполнитель        = Исполнитель;
			Запись.Действие           = Действие;
			
			Если Действие = Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать Тогда
				Запись.ОжидаетПодписания  = ОжидаетПодписания;
			КонецЕсли;
			
			УстановитьПривилегированныйРежим(Истина);
			НаборЗаписей.Записать();
			УстановитьПривилегированныйРежим(Ложь);
			
			ОжидаетПодписания = Запись.Исполнитель;
			
		КонецЦикла;
	КонецЦикла;
	
	Если УведомитьИсполнителей Тогда
		ОбсужденияБЗК.ЗапланироватьУведомления(Исполнители);
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗарегистрироватьОбработкуФайлов(ФайлыКОбработке, Действие = Неопределено, Исполнители = Неопределено) Экспорт
	
	Если Исполнители <> Неопределено Тогда
		ЗарегистрироватьОбработкуФайловИсполнителям(
			ФайлыКОбработке, Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать, Исполнители, Истина);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Действие)
		И Действие <> Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать Тогда
		
		Для Каждого ФайлКОбработке Из ФайлыКОбработке Цикл
			
			НаборЗаписей = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.ПрисоединенныйФайл.Установить(ФайлКОбработке);
			НаборЗаписей.Отбор.Действие.Установить(Действие);
			
			Запись = НаборЗаписей.Добавить();
			Запись.ПрисоединенныйФайл = ФайлКОбработке;
			Запись.ДатаЗадания        = ТекущаяДатаСеанса();
			Запись.Действие           = Действие;
			
			УстановитьПривилегированныйРежим(Истина);
			НаборЗаписей.Записать();
			УстановитьПривилегированныйРежим(Ложь);
			
			Если ПолучитьФункциональнуюОпцию("ИспользуетсяСервисКабинетСотрудника")
				И Действие <> Перечисления.ДействияСФайламиДокументовЭДОБЗК.ПередатьВКабинетСотрудников Тогда
				
				НаборЗаписей = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
				НаборЗаписей.Отбор.ПрисоединенныйФайл.Установить(ФайлКОбработке);
				НаборЗаписей.Отбор.Действие.Установить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.ПередатьВКабинетСотрудников);
				
				Запись = НаборЗаписей.Добавить();
				Запись.ПрисоединенныйФайл = ФайлКОбработке;
				Запись.ДатаЗадания        = ТекущаяДатаСеанса();
				Запись.Действие           = Перечисления.ДействияСФайламиДокументовЭДОБЗК.ПередатьВКабинетСотрудников;
				
				УстановитьПривилегированныйРежим(Истина);
				НаборЗаписей.Записать();
				УстановитьПривилегированныйРежим(Ложь);
				
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура УдалитьФайлыИзОбработки(ФайлыКОбработке, Действие = Неопределено) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Исполнители = Новый Массив;
	Для Каждого ФайлКОбработке Из ФайлыКОбработке Цикл
		
		Набор = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
		Набор.Отбор.ПрисоединенныйФайл.Установить(ФайлКОбработке);
		Если ЗначениеЗаполнено(Действие) Тогда
			Набор.Отбор.Действие.Установить(Действие);
		КонецЕсли;
		
		Если Действие = Неопределено Или Действие = Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать Тогда
			
			Набор.Прочитать();
			Исполнители = Набор.ВыгрузитьКолонку("Исполнитель");
			Набор.Очистить();
			
		КонецЕсли;
		
		Набор.Записать();
		
	КонецЦикла;
	
	ОбсужденияБЗК.ЗапланироватьУведомления(Исполнители);
	
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

Процедура УдалитьФайлыИзОбработкиПользователя(ФайлыКОбработке, Исполнитель = Неопределено, Действие = Неопределено) Экспорт
	
	Если Исполнитель = Неопределено Тогда
		Исполнитель = Пользователи.ТекущийПользователь();
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	Для Каждого ФайлКОбработке Из ФайлыКОбработке Цикл
		
		Набор = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
		Набор.Отбор.ПрисоединенныйФайл.Установить(ФайлКОбработке);
		Набор.Отбор.Исполнитель.Установить(Исполнитель);
		Если ЗначениеЗаполнено(Действие) Тогда
			Набор.Отбор.Действие.Установить(Действие);
		Иначе
			Набор.Отбор.Действие.Установить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать);
		КонецЕсли;
		Набор.Записать();
		
	КонецЦикла;
	
	ОбсужденияБЗК.ЗапланироватьУведомления(
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Исполнитель));
	
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

Процедура УдалитьФайлИзОбработкиПользователя(ФайлКОбработке, Знач Исполнитель = Неопределено, Знач Действие = Неопределено) Экспорт
	
	Если Исполнитель = Неопределено Тогда
		Исполнитель = Пользователи.ТекущийПользователь();
	КонецЕсли;
	
	Если Действие = Неопределено Тогда
		Действие = Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	ОжидаетПодписания = Неопределено;
	
	Набор = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
	Набор.Отбор.ПрисоединенныйФайл.Установить(ФайлКОбработке);
	Набор.Отбор.Исполнитель.Установить(Исполнитель);
	Набор.Отбор.Действие.Установить(Действие);
	Набор.Прочитать();
	Если Набор.Количество() > 0 Тогда
		ОжидаетПодписания = Набор[0].ОжидаетПодписания;
		Набор.Очистить();
		Набор.Записать();
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ОжидаетПодписания) Тогда
		
		Запрос = Новый Запрос;
		Запрос.УстановитьПараметр("ФайлКОбработке", ФайлКОбработке);
		Запрос.УстановитьПараметр("ОжидаетПодписания", Исполнитель);
		Запрос.УстановитьПараметр("Действие", Действие);
		
		Запрос.Текст =
			"ВЫБРАТЬ
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель КАК Исполнитель,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие КАК Действие,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ДатаЗадания КАК ДатаЗадания,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ОжидаетПодписания КАК ОжидаетПодписания
			|ИЗ
			|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ЗапланированныеДействияСФайламиДокументовЭДОБЗК
			|ГДЕ
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл = &ФайлКОбработке
			|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ОжидаетПодписания = &ОжидаетПодписания
			|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие = &Действие";
		
		Выборка = Запрос.Выполнить().Выбрать();
		Пока Выборка.Следующий() Цикл
			
			Набор = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
			Набор.Отбор.ПрисоединенныйФайл.Установить(Выборка.ПрисоединенныйФайл);
			Набор.Отбор.Исполнитель.Установить(Выборка.Исполнитель);
			Набор.Отбор.Действие.Установить(Выборка.Действие);
			Набор.Прочитать();
			Если Набор.Количество() > 0 Тогда
				Набор[0].ОжидаетПодписания = ОжидаетПодписания;
				Набор.Записать();
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
	ОбсужденияБЗК.ЗапланироватьУведомления(
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Исполнитель));
	
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

Процедура ИзменитьИсполнителя(ФайлКОбработке, Исполнитель, Действие, НовыйИсполнитель) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ФайлКОбработке", ФайлКОбработке);
	Запрос.УстановитьПараметр("Исполнитель", Исполнитель);
	Запрос.УстановитьПараметр("Действие", Действие);
	Запрос.УстановитьПараметр("НовыйИсполнитель", НовыйИсполнитель);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель КАК Исполнитель,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие КАК Действие,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ДатаЗадания КАК ДатаЗадания,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ОжидаетПодписания КАК ОжидаетПодписания,
		|	ИСТИНА КАК ИсходноеДействие
		|ИЗ
		|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ЗапланированныеДействияСФайламиДокументовЭДОБЗК
		|ГДЕ
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл = &ФайлКОбработке
		|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель = &Исполнитель
		|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие = &Действие
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ДатаЗадания,
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ОжидаетПодписания,
		|	ЛОЖЬ
		|ИЗ
		|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ЗапланированныеДействияСФайламиДокументовЭДОБЗК
		|ГДЕ
		|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл = &ФайлКОбработке
		|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ОжидаетПодписания = &Исполнитель
		|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель <> &НовыйИсполнитель";
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		
		Если Выборка.ИсходноеДействие Тогда
			// Удаление исходной записи
			Набор = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
			Набор.Отбор.ПрисоединенныйФайл.Установить(Выборка.ПрисоединенныйФайл);
			Набор.Отбор.Действие.Установить(Выборка.Действие);
			Набор.Отбор.Исполнитель.Установить(Выборка.Исполнитель);
			Набор.Записать();
		КонецЕсли;
		
		Набор = РегистрыСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК.СоздатьНаборЗаписей();
		Набор.Отбор.ПрисоединенныйФайл.Установить(Выборка.ПрисоединенныйФайл);
		Набор.Отбор.Действие.Установить(Выборка.Действие);
		
		Если Выборка.ИсходноеДействие Тогда
			Набор.Отбор.Исполнитель.Установить(НовыйИсполнитель);
		Иначе
			Набор.Отбор.Исполнитель.Установить(Выборка.Исполнитель);
		КонецЕсли;
		
		Запись = Набор.Добавить();
		ЗаполнитьЗначенияСвойств(Запись, Выборка);
		Если Выборка.ИсходноеДействие Тогда
			Запись.Исполнитель = НовыйИсполнитель;
		Иначе
			Запись.ОжидаетПодписания = НовыйИсполнитель;
		КонецЕсли;
		
		Набор.Записать();
		
	КонецЦикла;
	
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

Функция ФайлыКОбработке(ТолькоРазрешенные, Действия) Экспорт
	
	ФайлыДействий = Новый Соответствие;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	СоздатьВТФайлыКОбработке(
		ТолькоРазрешенные,
		Запрос.МенеджерВременныхТаблиц,
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(
			Справочники.Пользователи.ПустаяСсылка()),
		Действия);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ФайлыКОбработке.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
		|	ФайлыКОбработке.Имя КАК Имя,
		|	ФайлыКОбработке.Расширение КАК Расширение,
		|	ФайлыКОбработке.Размер КАК Размер,
		|	ФайлыКОбработке.ПодписанЭП КАК ПодписанЭП,
		|	ФайлыКОбработке.Действие КАК Действие
		|ИЗ
		|	ВТФайлыКОбработке КАК ФайлыКОбработке
		|ГДЕ
		|	ФайлыКОбработке.КоличествоПодписантов = 0
		|
		|УПОРЯДОЧИТЬ ПО
		|	Действие,
		|	Имя,
		|	Расширение,
		|	Размер";
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Пока Выборка.СледующийПоЗначениюПоля("Действие") Цикл
		
		СписокФайлов = Новый СписокЗначений;
		ФайлыДействий.Вставить(Выборка.Действие, СписокФайлов);
		
		Пока Выборка.Следующий() Цикл
			
			РаботаСФайламиБЗК.ДобавитьФайлВСписок(
				СписокФайлов,
				Выборка.ПрисоединенныйФайл,
				Выборка.Имя,
				Выборка.Расширение,
				Выборка.Размер,
				Выборка.ПодписанЭП);
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат ФайлыДействий;
	
КонецФункции

Функция КоличествоЗависшихЗаданийОбработки(ТолькоРазрешенные) Экспорт
	
	Возврат ФайлыСБольшимиСрокамиОжидания(ТолькоРазрешенные).Количество();
	
КонецФункции

Функция ФайлыСБольшимиСрокамиОжидания(ТолькоРазрешенные) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	СоздатьВТФайлыКОбработкеСДаннымиПечатныхФорм(ТолькоРазрешенные, Запрос.МенеджерВременныхТаблиц);
	
	Запрос.УстановитьПараметр("ДатаОтсчета", ЗарплатаКадрыКлиентСервер.ДатаОтсчетаПериодическихСведений());
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("КритическийПериодОжидания", КритическийПериодОжидания());
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ПечатныеФормыКОбработке.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
		|	ПечатныеФормыКОбработке.ВладелецФайла КАК ВладелецФайла,
		|	ПечатныеФормыКОбработке.Имя КАК Имя,
		|	ПечатныеФормыКОбработке.Расширение КАК Расширение,
		|	ПечатныеФормыКОбработке.Размер КАК Размер,
		|	ПечатныеФормыКОбработке.ПодписанЭП КАК ПодписанЭП,
		|	ТаблицаРегистра.Исполнитель КАК Исполнитель,
		|	ТаблицаРегистра.Действие КАК Действие,
		|	ТаблицаРегистра.ДатаЗадания КАК ДатаЗадания,
		|	ВЫБОР
		|		КОГДА ТаблицаРегистра.ДатаЗадания < &ДатаОтсчета
		|			ТОГДА 0
		|		ИНАЧЕ РАЗНОСТЬДАТ(ТаблицаРегистра.ДатаЗадания, &ТекущаяДата, МИНУТА)
		|	КОНЕЦ КАК ОжиданиеМинут
		|ИЗ
		|	ВТФайлыКОбработкеСДаннымиПечатныхФорм КАК ПечатныеФормыКОбработке
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|		ПО ПечатныеФормыКОбработке.ПрисоединенныйФайл = ТаблицаРегистра.ПрисоединенныйФайл
		|ГДЕ
		|	ВЫБОР
		|			КОГДА ТаблицаРегистра.ДатаЗадания < &ДатаОтсчета
		|				ТОГДА 0
		|			ИНАЧЕ РАЗНОСТЬДАТ(ТаблицаРегистра.ДатаЗадания, &ТекущаяДата, МИНУТА)
		|		КОНЕЦ > &КритическийПериодОжидания";
	
	УстановитьПривилегированныйРежим(Истина);
	РезультатЗапроса = Запрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат РезультатЗапроса.Выгрузить();
	
КонецФункции

Функция НаправленияНаПодпись(ФайлПечатнойФормы) Экспорт
	
	ВсеИсполнители = Новый Массив;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	Запрос.УстановитьПараметр("ФайлыПечатныхФорм", ФайлПечатнойФормы);
	Запрос.УстановитьПараметр("Действия", Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ТаблицаРегистра.Исполнитель КАК Исполнитель,
		|	ТаблицаРегистра.ОжидаетПодписания КАК ОжидаетПодписания
		|ПОМЕСТИТЬ ВТИсполнители
		|ИЗ
		|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|ГДЕ
		|	ТаблицаРегистра.ПрисоединенныйФайл В(&ФайлыПечатныхФорм)
		|	И ТаблицаРегистра.Действие В(&Действия)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Исполнители.Исполнитель КАК Исполнитель
		|ИЗ
		|	ВТИсполнители КАК Исполнители
		|		ЛЕВОЕ СОЕДИНЕНИЕ ВТИсполнители КАК ИсполнителиОжидаемые
		|		ПО Исполнители.ОжидаетПодписания = ИсполнителиОжидаемые.Исполнитель
		|ГДЕ
		|	ИсполнителиОжидаемые.Исполнитель ЕСТЬ NULL";
	
	УстановитьПривилегированныйРежим(Истина);
	Исполнители = Запрос.Выполнить().Выгрузить();
	Пока ЗначениеЗаполнено(Исполнители) Цикл
		ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ВсеИсполнители, Исполнители);
		Запрос.УстановитьПараметр("ОбработанныеИсполнители", ВсеИсполнители);
		Запрос.Текст = 
			"ВЫБРАТЬ
			|	Исполнители.Исполнитель КАК Исполнитель
			|ИЗ
			|	ВТИсполнители КАК Исполнители
			|		ЛЕВОЕ СОЕДИНЕНИЕ ВТИсполнители КАК ИсполнителиОжидаемые
			|		ПО Исполнители.ОжидаетПодписания = ИсполнителиОжидаемые.Исполнитель
			|			И (НЕ ИсполнителиОжидаемые.Исполнитель В (&ОбработанныеИсполнители))
			|ГДЕ
			|	ИсполнителиОжидаемые.Исполнитель ЕСТЬ NULL
			|	И НЕ Исполнители.Исполнитель В (&ОбработанныеИсполнители)";
		Исполнители = Запрос.Выполнить().Выгрузить();
	КонецЦикла;
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат ВсеИсполнители;
	
КонецФункции

Функция ОжидающиеИсполнителей(ФайлыПечатныхФорм, Действия = Неопределено) Экспорт
	
	Если Действия = Неопределено Тогда
		Действия = Новый Массив;
		Действия.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать);
		Действия.Добавить(Перечисления.ДействияСФайламиДокументовЭДОБЗК.Ознакомиться);
	КонецЕсли;
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ФайлыПечатныхФорм", ФайлыПечатныхФорм);
	Запрос.УстановитьПараметр("Действия", Действия);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ТаблицаРегистра.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
		|	ТаблицаРегистра.Исполнитель КАК Исполнитель,
		|	ТаблицаРегистра.ДатаЗадания КАК ДатаЗадания,
		|	ТаблицаРегистра.Действие КАК Действие
		|ИЗ
		|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|ГДЕ
		|	ТаблицаРегистра.ПрисоединенныйФайл В(&ФайлыПечатныхФорм)
		|	И ТаблицаРегистра.Действие В(&Действия)
		|
		|УПОРЯДОЧИТЬ ПО
		|	ПрисоединенныйФайл,
		|	Действие,
		|	Исполнитель";
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Пока Выборка.СледующийПоЗначениюПоля("ПрисоединенныйФайл") Цикл
		
		ДанныеФайла = Результат.Получить(Выборка.ПрисоединенныйФайл);
		Если ДанныеФайла = Неопределено Тогда
			ДанныеФайла = Новый Соответствие;
			Результат.Вставить(Выборка.ПрисоединенныйФайл, ДанныеФайла);
		КонецЕсли;
		Пока Выборка.СледующийПоЗначениюПоля("Действие") Цикл
			
			ДанныеДействия = ДанныеФайла.Получить(Выборка.Действие);
			Если ДанныеДействия = Неопределено Тогда
				ДанныеДействия = Новый Массив;
				ДанныеФайла.Вставить(Выборка.Действие, ДанныеДействия);
			КонецЕсли;
			Пока Выборка.Следующий() Цикл
				ДанныеИсполнителя = Новый Структура("Исполнитель,ДатаЗадания");
				ЗаполнитьЗначенияСвойств(ДанныеИсполнителя, Выборка);
				
				ДанныеДействия.Добавить(ДанныеИсполнителя);
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура ОбновитьУведомленияОНеобходимостиПодписанияФайловДокументовЭДОБЗК(Знач Исполнители) Экспорт
	
	ОбщегоНазначенияКлиентСервер.УдалитьВсеВхожденияЗначенияИзМассива(
		Исполнители, Справочники.Пользователи.ПустаяСсылка());
	
	Если Не ЗначениеЗаполнено(Исполнители) Тогда
		Возврат;
	КонецЕсли;
	
	Исполнители = ОбщегоНазначенияКлиентСервер.СвернутьМассив(Исполнители);
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	СоздатьВТФайлыКОбработкеСДаннымиПечатныхФорм(
		Ложь,
		Запрос.МенеджерВременныхТаблиц,
		Исполнители,
		ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать));
	
	Запрос.УстановитьПараметр("Исполнители", Исполнители);
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("МаксимальныйПериодОжидания", МаксимальныйПериодОжидания());
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ТаблицаРегистра.Исполнитель КАК Исполнитель,
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ТаблицаРегистра.ПрисоединенныйФайл) КАК ОбщееКоличествоФайлов,
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ВЫБОР
		|			КОГДА ТаблицаРегистра.Действие = ЗНАЧЕНИЕ(Перечисление.ДействияСФайламиДокументовЭДОБЗК.Подписать)
		|				ТОГДА ТаблицаРегистра.ПрисоединенныйФайл
		|			ИНАЧЕ NULL
		|		КОНЕЦ) КАК КоличествоФайловНаПодпись,
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ВЫБОР
		|			КОГДА ТаблицаРегистра.Действие = ЗНАЧЕНИЕ(Перечисление.ДействияСФайламиДокументовЭДОБЗК.Ознакомиться)
		|				ТОГДА ТаблицаРегистра.ПрисоединенныйФайл
		|			ИНАЧЕ NULL
		|		КОНЕЦ) КАК КоличествоФайловНаОзнакомление,
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ВЫБОР
		|			КОГДА РАЗНОСТЬДАТ(ТаблицаРегистра.ДатаЗадания, &ТекущаяДата, МИНУТА) > &МаксимальныйПериодОжидания
		|				ТОГДА ТаблицаРегистра.ПрисоединенныйФайл
		|			ИНАЧЕ NULL
		|		КОНЕЦ) КАК КоличествоПросроченныхФайлов
		|ИЗ
		|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТФайлыКОбработкеСДаннымиПечатныхФорм КАК ПечатныеФормыКОбработке
		|		ПО (ПечатныеФормыКОбработке.ПрисоединенныйФайл = ТаблицаРегистра.ПрисоединенныйФайл)
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ПодписанныеПечатныеФормы КАК ПодписанныеФормы
		|		ПО ТаблицаРегистра.ПрисоединенныйФайл = ПодписанныеФормы.ПрисоединенныйФайл
		|ГДЕ
		|	ТаблицаРегистра.Исполнитель В(&Исполнители)
		|	И ТаблицаРегистра.Действие В (ЗНАЧЕНИЕ(Перечисление.ДействияСФайламиДокументовЭДОБЗК.Подписать), ЗНАЧЕНИЕ(Перечисление.ДействияСФайламиДокументовЭДОБЗК.Ознакомиться))
		|
		|СГРУППИРОВАТЬ ПО
		|	ТаблицаРегистра.Исполнитель";
	
	УстановитьПривилегированныйРежим(Истина);
	Выборка = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	Пока Выборка.Следующий() Цикл
		ЭДОБЗК.УведомитьОНеобходимостиПодписанияФайловДокументовЭДОБЗК(
			Выборка.ОбщееКоличествоФайлов,
			Выборка.КоличествоФайловНаПодпись,
			Выборка.КоличествоФайловНаОзнакомление,
			Выборка.КоличествоПросроченныхФайлов,
			Выборка.Исполнитель);
		ОбщегоНазначенияКлиентСервер.УдалитьЗначениеИзМассива(Исполнители, Выборка.Исполнитель);
	КонецЦикла;
	
	// Удаление уведомления когда все подписано
	Для Каждого Исполнитель Из Исполнители Цикл
		ЭДОБЗК.УведомитьОНеобходимостиПодписанияФайловДокументовЭДОБЗК(0, 0, 0, 0, Исполнитель);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура СоздатьВТФайлыКОбработке(ТолькоРазрешенные, МенеджерВременныхТаблиц, Исполнители, Действия)
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	
	СоздатьВТФайлыКОбработкеСДаннымиПечатныхФорм(
		ТолькоРазрешенные,
		Запрос.МенеджерВременныхТаблиц,
		Исполнители,
		Действия);
	
	Запрос.УстановитьПараметр("ДействияСФайламиДокументовЭДОБЗК", Действия);
	
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ПечатныеФормыКОбработке.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
		|	ПечатныеФормыКОбработке.Имя КАК Имя,
		|	ПечатныеФормыКОбработке.Расширение КАК Расширение,
		|	ПечатныеФормыКОбработке.Размер КАК Размер,
		|	ПечатныеФормыКОбработке.ПодписанЭП КАК ПодписанЭП,
		|	ТаблицаРегистра.Действие КАК Действие,
		|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ТаблицаРегистраПодписей.Исполнитель) КАК КоличествоПодписантов
		|ПОМЕСТИТЬ ВТФайлыКОбработке
		|ИЗ
		|	ВТФайлыКОбработкеСДаннымиПечатныхФорм КАК ПечатныеФормыКОбработке
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|		ПО ПечатныеФормыКОбработке.ПрисоединенныйФайл = ТаблицаРегистра.ПрисоединенныйФайл
		|			И (ТаблицаРегистра.Действие В (&ДействияСФайламиДокументовЭДОБЗК))
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистраПодписей
		|		ПО ПечатныеФормыКОбработке.ПрисоединенныйФайл = ТаблицаРегистраПодписей.ПрисоединенныйФайл
		|			И (ТаблицаРегистраПодписей.Действие = ЗНАЧЕНИЕ(Перечисление.ДействияСФайламиДокументовЭДОБЗК.Подписать))
		|
		|СГРУППИРОВАТЬ ПО
		|	ПечатныеФормыКОбработке.ПрисоединенныйФайл,
		|	ПечатныеФормыКОбработке.Имя,
		|	ПечатныеФормыКОбработке.Расширение,
		|	ПечатныеФормыКОбработке.Размер,
		|	ПечатныеФормыКОбработке.ПодписанЭП,
		|	ТаблицаРегистра.Действие";
	
	УстановитьПривилегированныйРежим(Истина);
	Запрос.Выполнить();
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

Процедура СоздатьВТФайлыКОбработкеСДаннымиПечатныхФорм(ТолькоРазрешенные, МенеджерВременныхТаблиц, Исполнители = Неопределено, Действия = Неопределено)
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	
	Запрос.Текст =
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ТаблицаРегистра.ПрисоединенныйФайл КАК ПрисоединенныйФайл
		|ИЗ
		|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ТаблицаРегистра
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ДокументЭДОБЗК КАК ДокументЭДОБЗК
		|		ПО ТаблицаРегистра.ПрисоединенныйФайл = ДокументЭДОБЗК.ЭлектронныйДокумент
		|ГДЕ
		|	&ОтборПоИсполнителям
		|	И &ОтборПоДействиям
		|	И НЕ ДокументЭДОБЗК.ПометкаУдаления";
	
	Если Исполнители = Неопределено Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоИсполнителям", "(ИСТИНА)");
	Иначе
		Запрос.УстановитьПараметр("Исполнители", Исполнители);
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоИсполнителям", "ТаблицаРегистра.Исполнитель В (&Исполнители)");
	КонецЕсли;
	
	Если Действия = Неопределено Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоДействиям", "(ИСТИНА)");
	Иначе
		Запрос.УстановитьПараметр("Действия", Действия);
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ОтборПоДействиям", "ТаблицаРегистра.Действие В(&Действия)");
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	ФайлыПечатныхФорм = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("ПрисоединенныйФайл");
	УстановитьПривилегированныйРежим(Ложь);
	
	ТекстыЗапросов = Новый Массив;
	Если ФайлыПечатныхФорм.Количество() > 0 Тогда
		
		ФайлыПоТипам = ОбщегоНазначенияБЗК.ОбъектыПоТипам(ФайлыПечатныхФорм);
		Для Каждого ОписаниеФайловТипа Из ФайлыПоТипам Цикл
			
			МетаданныеСправочника = Метаданные.НайтиПоТипу(ОписаниеФайловТипа.Ключ);
			Если Не ПравоДоступа("Просмотр", МетаданныеСправочника) Тогда
				Продолжить;
			КонецЕсли;
			
			ТекстЗапроса =
				"ВЫБРАТЬ РАЗРЕШЕННЫЕ
				|	ТаблицаСправочника.Ссылка КАК ПрисоединенныйФайл,
				|	ТаблицаСправочника.ВладелецФайла КАК ВладелецФайла,
				|	ТаблицаСправочника.Наименование КАК Имя,
				|	ТаблицаСправочника.Расширение КАК Расширение,
				|	ТаблицаСправочника.Размер КАК Размер,
				|	ТаблицаСправочника.ПодписанЭП КАК ПодписанЭП
				|ПОМЕСТИТЬ ВТФайлыКОбработкеСДаннымиПечатныхФорм
				|ИЗ
				|	Справочник.УвольнениеПрисоединенныеФайлы КАК ТаблицаСправочника
				|ГДЕ
				|	ТаблицаСправочника.Ссылка В(&ПрисоединенныеФайлы)";
			
			ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "Справочник.УвольнениеПрисоединенныеФайлы",
				МетаданныеСправочника.ПолноеИмя());
			
			ИмяПараметра = "ПрисоединенныеФайлы" + Формат(ТекстыЗапросов.Количество(), "ЧН=; ЧГ=");
			Запрос.УстановитьПараметр(ИмяПараметра, ОписаниеФайловТипа.Значение);
			ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ПрисоединенныеФайлы", "&" + ИмяПараметра);
			
			Если ТекстыЗапросов.Количество() > 0 Тогда
				
				ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "РАЗРЕШЕННЫЕ", "");
				ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ПОМЕСТИТЬ ВТФайлыКОбработкеСДаннымиПечатныхФорм", "");
				
			КонецЕсли;
			
			ТекстыЗапросов.Добавить(ТекстЗапроса);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Если ТекстыЗапросов.Количество() = 0 Тогда
		
		Запрос.Текст =
			"ВЫБРАТЬ
			|	НЕОПРЕДЕЛЕНО КАК ПрисоединенныйФайл,
			|	НЕОПРЕДЕЛЕНО КАК ВладелецФайла,
			|	"""" КАК Имя,
			|	"""" КАК Расширение,
			|	0 КАК Размер,
			|	ЛОЖЬ КАК ПодписанЭП
			|ПОМЕСТИТЬ ВТФайлыКОбработкеСДаннымиПечатныхФорм
			|ГДЕ
			|	ЛОЖЬ";
		
	Иначе
		
		Запрос.Текст = СтрСоединить(ТекстыЗапросов, Символы.ПС + "ОБЪЕДИНИТЬ ВСЕ" + Символы.ПС);
		ЗарплатаКадрыОбщиеНаборыДанных.УстановитьВыборкуТолькоРазрешенныхДанных(Запрос.Текст, ТолькоРазрешенные);
		
	КонецЕсли;
	
	Запрос.Выполнить();
	
КонецПроцедуры

Функция МаксимальныйПериодОжидания()
	Возврат 2 * ЗарплатаКадрыКлиентСервер.ДлительностьСутокВСекундах() / 60;
КонецФункции

Функция КритическийПериодОжидания()
	Возврат 4 * ЗарплатаКадрыКлиентСервер.ДлительностьСутокВСекундах() / 60;
КонецФункции

Процедура ОбработатьИзмненияЗаданий(ИзменениеЗаписей) Экспорт
	
	Настройки = ЭДОБЗК.НастройкиЭДОБЗК();
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	Запрос.УстановитьПараметр("ИзменениеЗаписей", ИзменениеЗаписей);
	Запрос.УстановитьПараметр("ИнтерактивныеДействия", Перечисления.ДействияСФайламиДокументовЭДОБЗК.ИнтерактивныеДействия());
	
	// Подготовка наборов записей изменений
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ИзменениеЗаписей.ВидИзмененияСтроки КАК ВидИзмененияСтроки,
		|	ИзменениеЗаписей.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
		|	ИзменениеЗаписей.Исполнитель КАК Исполнитель,
		|	ИзменениеЗаписей.Действие КАК Действие,
		|	ИзменениеЗаписей.ОжидаетПодписания КАК ОжидаетПодписания
		|ПОМЕСТИТЬ ВТИзменениеЗаписей
		|ИЗ
		|	&ИзменениеЗаписей КАК ИзменениеЗаписей";
	Запрос.Выполнить();
	
	Если Настройки.УведомлениеЗадачами Тогда
		// Старт новый заданий
		Запрос.Текст =
			"ВЫБРАТЬ
			|	ИзменениеЗаписей.ВидИзмененияСтроки КАК ВидИзмененияСтроки,
			|	ИзменениеЗаписей.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
			|	ИзменениеЗаписей.Исполнитель КАК Исполнитель,
			|	ИзменениеЗаписей.Действие КАК Действие,
			|	ИзменениеЗаписей.ОжидаетПодписания КАК ОжидаетПодписания
			|ПОМЕСТИТЬ ВТВсеЗаписи
			|ИЗ
			|	ВТИзменениеЗаписей КАК ИзменениеЗаписей
			|ГДЕ
			|	ИзменениеЗаписей.ВидИзмененияСтроки = 1
			|	И ИзменениеЗаписей.Действие В(&ИнтерактивныеДействия)
			|
			|ОБЪЕДИНИТЬ ВСЕ
			|
			|ВЫБРАТЬ
			|	0,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие,
			|	ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ОжидаетПодписания
			|ИЗ
			|	РегистрСведений.ЗапланированныеДействияСФайламиДокументовЭДОБЗК КАК ЗапланированныеДействияСФайламиДокументовЭДОБЗК
			|		ЛЕВОЕ СОЕДИНЕНИЕ ВТИзменениеЗаписей КАК ИзменениеЗаписей
			|		ПО ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл = ИзменениеЗаписей.ПрисоединенныйФайл
			|			И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Исполнитель = ИзменениеЗаписей.Исполнитель
			|			И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие = ИзменениеЗаписей.Действие
			|ГДЕ
			|	ИзменениеЗаписей.ПрисоединенныйФайл ЕСТЬ NULL
			|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.ПрисоединенныйФайл В
			|			(ВЫБРАТЬ
			|				ДобавляемыеДействия.ПрисоединенныйФайл
			|			ИЗ
			|				ВТИзменениеЗаписей КАК ДобавляемыеДействия)
			|	И ЗапланированныеДействияСФайламиДокументовЭДОБЗК.Действие В(&ИнтерактивныеДействия)
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	ВсеЗаписи.ВидИзмененияСтроки КАК ВидИзмененияСтроки,
			|	ВсеЗаписи.ПрисоединенныйФайл КАК ПрисоединенныйФайл,
			|	ВсеЗаписи.Исполнитель КАК Исполнитель,
			|	ВсеЗаписи.Действие КАК Действие,
			|	ВсеЗаписи.ОжидаетПодписания КАК ОжидаетПодписания,
			|	ДокументЭДОБЗК.Ссылка КАК ДокументЭДОБЗК
			|ИЗ
			|	ВТВсеЗаписи КАК ВсеЗаписи
			|		ЛЕВОЕ СОЕДИНЕНИЕ ВТВсеЗаписи КАК ВсеЗаписиОжидания
			|		ПО ВсеЗаписи.ПрисоединенныйФайл = ВсеЗаписиОжидания.ПрисоединенныйФайл
			|			И ВсеЗаписи.ОжидаетПодписания = ВсеЗаписиОжидания.Исполнитель
			|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ДокументЭДОБЗК КАК ДокументЭДОБЗК
			|		ПО ВсеЗаписи.ПрисоединенныйФайл = ДокументЭДОБЗК.ЭлектронныйДокумент
			|			И (НЕ ДокументЭДОБЗК.ПометкаУдаления)
			|		ЛЕВОЕ СОЕДИНЕНИЕ БизнесПроцесс.Задание КАК Задание
			|		ПО (ДокументЭДОБЗК.Ссылка = Задание.Предмет)
			|			И ВсеЗаписи.Исполнитель = Задание.Исполнитель
			|			И (НЕ Задание.Завершен)
			|ГДЕ
			|	ВсеЗаписиОжидания.ПрисоединенныйФайл ЕСТЬ NULL
			|	И Задание.Ссылка ЕСТЬ NULL";
		
		УстановитьПривилегированныйРежим(Истина);
		Выборка = Запрос.Выполнить().Выбрать();
		Если Выборка.Количество() > 0 Тогда
			НачатьТранзакцию();
			Попытка
				Пока Выборка.Следующий() Цикл
					Задание = БизнесПроцессы.Задание.СоздатьБизнесПроцесс();
					Задание.Заполнить(Выборка.ДокументЭДОБЗК);
					Задание.Дата = ТекущаяДатаСеанса();
					Задание.Исполнитель = Выборка.Исполнитель;
					Задание.Наименование = СтрШаблон("%1: %2", Выборка.Действие, Выборка.ПрисоединенныйФайл);
					Задание.СрокИсполнения = ЗарплатаКадрыКлиентСервер.ДобавитьДней(Задание.Дата, 2);
					Если Выборка.Действие = Перечисления.ДействияСФайламиДокументовЭДОБЗК.Подписать Тогда
						Задание.Содержание = СтрШаблон(НСтр("ru = 'Необходимо подписать электронный документ %2'"),
							НРег(Выборка.Действие), Выборка.ПрисоединенныйФайл);
					ИначеЕсли Выборка.Действие = Перечисления.ДействияСФайламиДокументовЭДОБЗК.Ознакомиться Тогда
						Задание.Содержание = СтрШаблон(НСтр("ru = 'Необходимо ознакомиться с комментарием к электронному документу %2'"),
							НРег(Выборка.Действие), Выборка.ПрисоединенныйФайл);
					Иначе
						Задание.Содержание = СтрШаблон(НСтр("ru = 'Необходимо %1 электронный документ %2'"),
							НРег(Выборка.Действие), Выборка.ПрисоединенныйФайл);
					КонецЕсли;
					Задание.Записать();
					Задание.Старт();
				КонецЦикла;
				ЗафиксироватьТранзакцию();
			Исключение
				ОтменитьТранзакцию();
				ЗаписьЖурналаРегистрации(
					ЭДОБЗК.ИмяСобытияЖурналаРегистрации(
						НСтр("ru = 'Ошибка создания заданий на действия с документами ЭДО'", ОбщегоНазначения.КодОсновногоЯзыка())),
					УровеньЖурналаРегистрации.Ошибка,
					Метаданные.БизнесПроцессы.Задание,
					,
					ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			КонецПопытки;
		КонецЕсли;
	КонецЕсли;
	
	// Завершение задач по удаленным записям
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ЗадачаИсполнителя.Ссылка КАК ЗадачаИсполнителя
		|ИЗ
		|	ВТИзменениеЗаписей КАК ИзменениеЗаписей
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ДокументЭДОБЗК КАК ДокументЭДОБЗК
		|		ПО ИзменениеЗаписей.ПрисоединенныйФайл = ДокументЭДОБЗК.ЭлектронныйДокумент
		|			И (НЕ ДокументЭДОБЗК.ПометкаУдаления)
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ БизнесПроцесс.Задание КАК Задание
		|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Задача.ЗадачаИсполнителя КАК ЗадачаИсполнителя
		|			ПО Задание.Ссылка = ЗадачаИсполнителя.БизнесПроцесс
		|		ПО (ДокументЭДОБЗК.Ссылка = Задание.Предмет)
		|			И ИзменениеЗаписей.Исполнитель = Задание.Исполнитель
		|			И (НЕ Задание.Завершен)
		|ГДЕ
		|	ИзменениеЗаписей.ВидИзмененияСтроки = -1
		|	И Задание.Стартован
		|	И НЕ Задание.Завершен
		|	И НЕ ЗадачаИсполнителя.Выполнена";
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		НачатьТранзакцию();
		Попытка
			БизнесПроцессыИЗадачиСервер.ЗаблокироватьЗадачи(Выборка.ЗадачаИсполнителя);
			ОбъектЗадачаИсполнителя = Выборка.ЗадачаИсполнителя.ПолучитьОбъект();
			ОбъектЗадачаИсполнителя.ВыполнитьЗадачу();
			ЗафиксироватьТранзакцию();
		Исключение
			ОтменитьТранзакцию();
		КонецПопытки;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли