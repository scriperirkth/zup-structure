#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)

	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(ЭтотОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	РегистрыСведений.ПериодичностьМедицинскихОсмотровРабочихМест.СохранитьПрежнийНаборЗаписей(ЭтотОбъект, Замещение);
	МедицинскиеОсмотры.ЗаполнитьОрганизацию(ЭтотОбъект, Замещение);
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ, Замещение)

	Если ЗарплатаКадры.ОтключитьБизнесЛогикуПриЗаписи(ЭтотОбъект) Тогда
		Возврат;
	КонецЕсли;
	
	ЗарегистрироватьОбновлениеПериодическихОсмотров(Замещение);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗарегистрироватьОбновлениеПериодическихОсмотров(Замещение)
	
	РегистрыСведений.ПериодичностьМедицинскихОсмотровРабочихМест.ЗаполнитьТаблицуОбновления(ЭтотОбъект, Замещение);
	МедицинскиеОсмотры.ЗарегистрироватьОбновлениеПериодическихОсмотров(ДополнительныеСвойства.ТаблицаОбновлениеПериодическихОсмотров);
	
КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли