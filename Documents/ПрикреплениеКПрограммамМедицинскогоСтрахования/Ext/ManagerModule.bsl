#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// СтандартныеПодсистемы.ВерсионированиеОбъектов

// Определяет настройки объекта для подсистемы ВерсионированиеОбъектов.
//
// Параметры:
//  Настройки - Структура - настройки подсистемы.
Процедура ПриОпределенииНастроекВерсионированияОбъектов(Настройки) Экспорт

КонецПроцедуры

// Конец СтандартныеПодсистемы.ВерсионированиеОбъектов

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// См. УправлениеДоступомПереопределяемый.ПриЗаполненииСписковСОграничениемДоступа.
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт
	Ограничение.Текст =
	"РазрешитьЧтениеИзменение
	|ГДЕ
	|	ДляВсехСтрок( ЗначениеРазрешено(Сотрудники.ФизическоеЛицо, NULL КАК ИСТИНА)
	|	) И ЗначениеРазрешено(Организация)";
КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Сторнирует документ по учетам. Используется подсистемой исправления документов.
//
// Параметры:
//  Движения				 - КоллекцияДвижений, Структура	 - Коллекция движений исправляющего документа в которую будут добавлены сторно стоки.
//  Регистратор				 - ДокументСсылка				 - Документ регистратор исправления (документ исправление).
//  ИсправленныйДокумент	 - ДокументСсылка				 - Исправленный документ движения которого будут сторнированы.
//  СтруктураВидовУчета		 - Структура					 - Виды учета, по которым будет выполнено сторнирование исправленного документа.
//  					Состав полей см. в ПроведениеСервер.СтруктураВидовУчета().
//  ДополнительныеПараметры	 - Структура					 - Структура со свойствами:
//  					* ИсправлениеВТекущемПериоде - Булево - Истина когда исправление выполняется в периоде регистрации исправленного документа.
//						* ОтменаДокумента - Булево - Истина когда исправление вызвано документом СторнированиеНачислений.
//  					* ПериодРегистрации	- Дата - Период регистрации документа регистратора исправления.
// 
// Возвращаемое значение:
//  Булево - "Истина" если сторнирование выполнено этой функцией, "Ложь" если специальной процедуры не предусмотрено.
//
Функция СторнироватьПоУчетам(Движения, Регистратор, ИсправленныйДокумент, СтруктураВидовУчета, ДополнительныеПараметры) Экспорт
	
	ИсправлениеДокументовЗарплатаКадры.СторнироватьДвиженияБезСпецификиУчетов(Движения, ИсправленныйДокумент, ДополнительныеПараметры);
	
	Возврат Истина;
	
КонецФункции

Функция СвойстваИсправляемогоДокумента(ДокументСсылка) Экспорт
	
	Реквизиты = ИсправлениеДокументовЗарплатаКадры.РеквизитыИсправляемогоДокумента();
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ДокументСсылка, Реквизиты);
	
КонецФункции

Функция ПараметрыИсправляемогоДокумента(ДокументСсылка) Экспорт
	
	Возврат ИсправлениеДокументовЗарплатаКадры.ПараметрыИсправляемогоДокумента(ДокументСсылка,
		СвойстваИсправляемогоДокумента(ДокументСсылка));
	
КонецФункции

#Область ОбработчикиПравилРегистрации

// См. ЗарплатаКадрыРасширенныйСинхронизацияДанных.ШаблонОбработчика
Процедура ПриЗаполненииНастроекОбработчиковПравилРегистрации(Настройки) Экспорт
	ЗарплатаКадрыРасширенныйСинхронизацияДанных.ДляОтбораПоОрганизации(Настройки);
	ЗарплатаКадрыРасширенныйСинхронизацияДанных.ДляОбъектаСПрисоединеннымиФайлами(Настройки);
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область Печать

// Заполняет список команд печати.
// 
// Параметры:
//   КомандыПечати - ТаблицаЗначений - состав полей см. в функции УправлениеПечатью.СоздатьКоллекциюКомандПечати.
//
Процедура ДобавитьКомандыПечати(КомандыПечати) Экспорт
	
	// Прикрепление к программам страхования
	КомандаПечати = КомандыПечати.Добавить();
	КомандаПечати.Обработчик = "УправлениеПечатьюБЗККлиент.ВыполнитьКомандуПечати";
	КомандаПечати.Идентификатор = "ПрикреплениеКПрограммамСтрахования";
	КомандаПечати.Представление = НСтр("ru = 'Прикрепление к программам страхования'");
	
	// Заявление на удержание за родственника
	КомандаПечати = КомандыПечати.Добавить();
	КомандаПечати.Обработчик = "УправлениеПечатьюБЗККлиент.ВыполнитьКомандуПечати";
	КомандаПечати.Идентификатор = "ЗаявлениеНаУдержание";
	КомандаПечати.Представление = НСтр("ru = 'Заявление на удержание'");
	КомандаПечати.ФункциональныеОпции = "ИспользоватьУдержанияВМедицинскомСтрахованииСотрудников";
	
	КомандаПечати.ПроверкаПроведенияПередПечатью = Истина;
	
КонецПроцедуры

// Формирует печатные формы
//
// Параметры:
//  (входные)
//    МассивОбъектов  - Массив    - ссылки на объекты, которые нужно распечатать;
//    ПараметрыПечати - Структура - дополнительные настройки печати;
//  (выходные)
//   КоллекцияПечатныхФорм - ТаблицаЗначений - сформированные табличные документы.
//   ОбъектыПечати         - СписокЗначений  - значение - ссылка на объект;
//                                             представление - имя области в которой был выведен объект;
//   ПараметрыВывода       - Структура       - дополнительные параметры сформированных табличных документов.
//
Процедура Печать(МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода) Экспорт
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ПрикреплениеКПрограммамСтрахования") Тогда
		ТабличныйДокумент = ПечатьПрикреплениеКПрограммамСтрахования(МассивОбъектов, ОбъектыПечати);
		МедицинскоеСтрахованиеПереопределяемый.Печать(ТабличныйДокумент, МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода);
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм, "ПрикреплениеКПрограммамСтрахования", НСтр("ru = 'Прикрепление к программам страхования'"), ТабличныйДокумент);
	КонецЕсли;
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ЗаявлениеНаУдержание") Тогда
		ТабличныйДокумент = ПечатьЗаявленияНаУдержание(МассивОбъектов, ОбъектыПечати, ПараметрыПечати);
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм, "ЗаявлениеНаУдержание", НСтр("ru = 'Заявление на удержание'"), ТабличныйДокумент);
	КонецЕсли;
	
КонецПроцедуры

Функция ПечатьПрикреплениеКПрограммамСтрахования(МассивОбъектов, ОбъектыПечати)
	
	// получаем данные для печати
	ДанныеДляПечати = ДанныеПрикрепленияКПрограммамСтрахования(МассивОбъектов);
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.КлючПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_МедицинскоеСтрахование_ПрикреплениеКПрограммамСтрахования";
	
	Макет = УправлениеПечатью.МакетПечатнойФормы("Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ПФ_MXL_ПрикреплениеКПрограммамСтрахования");
	
	ПервыйДокумент = Истина;
	
	Для Каждого ДанныеДокументаДляПечати Из ДанныеДляПечати Цикл
		
		ДанныеДокумента = ДанныеДокументаДляПечати.Значение;
		// Документы нужно выводить на разных страницах.
		Если Не ПервыйДокумент Тогда
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		Иначе
			ПервыйДокумент = Ложь;
		КонецЕсли;
		
		// Запомним номер строки, с которой начали выводить текущий документ.
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		
		// Подсчитываем количество страниц документа - для корректного разбиения на страницы.
		ВсегоСтрокДокумента = ДанныеДокумента.Сотрудники.Количество() + ДанныеДокумента.Родственники.Количество();
		
		ОбластьМакетаЗаголовок						= Макет.ПолучитьОбласть("Заголовок");
		ОбластьМакетаНомерСтраницы					= Макет.ПолучитьОбласть("НомерСтраницы");
		ОбластьМакетаШапкаПерваяКолонка				= Макет.ПолучитьОбласть("Шапка|ПерваяКолонка");
		ОбластьМакетаШапкаПрисоединяемаяКолонка		= Макет.ПолучитьОбласть("Шапка|ПрисоединяемаяКолонка");
		ОбластьМакетаСтрокаПерваяКолонка			= Макет.ПолучитьОбласть("Строка|ПерваяКолонка");
		ОбластьМакетаСтрокаПрисоединяемаяКолонка	= Макет.ПолучитьОбласть("Строка|ПрисоединяемаяКолонка");
		ОбластьМакетаПодвал							= Макет.ПолучитьОбласть("Подвал");
		
		// Массив с двумя строками - для разбиения на страницы.
		ВыводимыеОбласти = Новый Массив();
		ВыводимыеОбласти.Добавить(ОбластьМакетаСтрокаПерваяКолонка);
		
		// выводим данные о документе
		ОбластьМакетаЗаголовок.Параметры.Организация = ДанныеДокумента.Организация;
		ОбластьМакетаЗаголовок.Параметры.СтраховаяКомпания = ДанныеДокумента.СтраховаяКомпания;
		ОбластьМакетаЗаголовок.Параметры.ДоговорСтрахования = ДанныеДокумента.ДоговорСтрахования;
		
		ТабличныйДокумент.Вывести(ОбластьМакетаЗаголовок);
		
		ВыведеноСтраниц = 1; ВыведеноСтрок = 0;
		
		ОбластьМакетаНомерСтраницы.Параметры.НомерСтраницы = ВыведеноСтраниц;
		ТабличныйДокумент.Вывести(ОбластьМакетаНомерСтраницы);
		
		ТабличныйДокумент.Вывести(ОбластьМакетаШапкаПерваяКолонка);
		
		ОтправляемыеДанныеТаблица = МедицинскоеСтрахование.ОтправляемыеДанныеВСтраховуюКомпанию(ДанныеДокумента.ДоговорСтрахования);
		ОтправляемыеДанные = ОтправляемыеДанныеТаблица.ВыгрузитьКолонку("Сведение");
		ОтправляемаяКонтактнаяИнформация = МедицинскоеСтрахование.ОтправляемаяКонтактнаяИнформацияВСтраховуюКомпанию(ДанныеДокумента.ДоговорСтрахования);
		ОтправляемыеДопРеквизитыИСведения = МедицинскоеСтрахование.ОтправляемыеДополнительныеРеквизитыИСведенияВСтраховуюКомпанию(ДанныеДокумента.ДоговорСтрахования);
		
		ДополнитьШапкуКолонками(ТабличныйДокумент, ОтправляемыеДанные, ОтправляемаяКонтактнаяИнформация, 
									ОтправляемыеДопРеквизитыИСведения, ОбластьМакетаШапкаПрисоединяемаяКолонка);
		
		СтраховаяПремияИтого = 0;
		// Выводим данные по строкам документа.
		Страхователи = Новый Структура;
		Страхователи.Вставить("Сотрудники", "Сотрудник");
		Страхователи.Вставить("Родственники", "Родственник");
		
		СписокОтсутствующейКИ = "";
		СписокОтсутствующихДопРеквизитов = "";
		
		Для Каждого ЭлементСтруктуры Из Страхователи Цикл
			ИмяПоляСтрахователя = ЭлементСтруктуры.Значение;
			Для Каждого ДанныеДляПечатиСтроки Из ДанныеДокумента[ЭлементСтруктуры.Ключ] Цикл
				
				ОбластьМакетаСтрокаПерваяКолонка.Параметры.ФИО = ДанныеДляПечатиСтроки[ИмяПоляСтрахователя];
				ОбластьМакетаСтрокаПерваяКолонка.Параметры.ПрограммыСтрахования = ДанныеДляПечатиСтроки.ПрограммыСтрахования;
				ОбластьМакетаСтрокаПерваяКолонка.Параметры.РасширенияПрограммСтрахования = ДанныеДляПечатиСтроки.РасширенияПрограммСтрахования;
				ОбластьМакетаСтрокаПерваяКолонка.Параметры.ДатаНачалаСтрахования = ДанныеДляПечатиСтроки.ДатаНачала;
				ОбластьМакетаСтрокаПерваяКолонка.Параметры.ДатаОкончанияСтрахования = ДанныеДляПечатиСтроки.ДатаОкончания;
				ОбластьМакетаСтрокаПерваяКолонка.Параметры.СтраховаяПремия = ДанныеДляПечатиСтроки.СтраховаяПремия;
				
				// разбиение на страницы
				ВыведеноСтрок = ВыведеноСтрок + 1;
				
				// Проверим, уместится ли строка на странице или надо открывать новую страницу.
				ВывестиПодвалЛиста = Не ОбщегоНазначения.ПроверитьВыводТабличногоДокумента(ТабличныйДокумент, ВыводимыеОбласти);
				Если Не ВывестиПодвалЛиста И ВыведеноСтрок = ВсегоСтрокДокумента Тогда
					ОбластьМакетаПодвал.Параметры.СтраховаяПремияИтого = СтраховаяПремияИтого;
					ВыводимыеОбласти.Добавить(ОбластьМакетаПодвал);
					ВывестиПодвалЛиста = Не ОбщегоНазначения.ПроверитьВыводТабличногоДокумента(ТабличныйДокумент, ВыводимыеОбласти);
				КонецЕсли;
				Если ВывестиПодвалЛиста Тогда
					
					ОбластьМакетаПодвал.Параметры.СтраховаяПремияИтого = СтраховаяПремияИтого;
					ТабличныйДокумент.Вывести(ОбластьМакетаПодвал);
					ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
					СтраховаяПремияИтого = 0;
					
					ВыведеноСтраниц = ВыведеноСтраниц + 1;
					
					ОбластьМакетаНомерСтраницы.Параметры.НомерСтраницы = ВыведеноСтраниц;
					ТабличныйДокумент.Вывести(ОбластьМакетаНомерСтраницы);
					ТабличныйДокумент.Вывести(ОбластьМакетаШапкаПерваяКолонка);
					ДополнитьШапкуКолонками(ТабличныйДокумент, ОтправляемыеДанные, ОтправляемаяКонтактнаяИнформация, 
												ОтправляемыеДопРеквизитыИСведения, ОбластьМакетаШапкаПрисоединяемаяКолонка);
				КонецЕсли;
											
				СтраховаяПремияИтого = СтраховаяПремияИтого + ДанныеДляПечатиСтроки.СтраховаяПремия;							
				
				ТабличныйДокумент.Вывести(ОбластьМакетаСтрокаПерваяКолонка);
				
				Если ОтправляемыеДанные.Найти("ДатаРождения") <> Неопределено Тогда
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = Формат(ДанныеДляПечатиСтроки.Сведения.ДатаРождения, "ДЛФ=Д");
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);
				КонецЕсли;
				Если ОтправляемыеДанные.Найти("СтраховойНомерПФР") <> Неопределено Тогда
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = ДанныеДляПечатиСтроки.Сведения.СтраховойНомерПФР;
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);
				КонецЕсли;
				Если ОтправляемыеДанные.Найти("Пол") <> Неопределено Тогда
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = ДанныеДляПечатиСтроки.Сведения.Пол;
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);
				КонецЕсли;
				Если ОтправляемыеДанные.Найти("Страна") <> Неопределено Тогда
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = ДанныеДляПечатиСтроки.Сведения.Страна;
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);
				КонецЕсли;
				Если ОтправляемыеДанные.Найти("ДокументПредставление") <> Неопределено Тогда
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = ДанныеДляПечатиСтроки.Сведения.ДокументПредставление;
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);
				КонецЕсли;
				Для каждого ВидКИ Из ОтправляемаяКонтактнаяИнформация Цикл
					ЗначениеВидаКИ = "";
					Если ДанныеДляПечатиСтроки.Сведения.Свойство(ВидКИ.ИдентификаторДляФормул, ЗначениеВидаКИ) = Ложь Тогда
						Если СписокОтсутствующейКИ = "" Тогда
							СписокОтсутствующейКИ = ВидКИ.Наименование;
						Иначе
							Если Найти(СписокОтсутствующейКИ, ВидКИ.Наименование) = 0 Тогда
								СписокОтсутствующейКИ = СтрШаблон("%1, %2", СписокОтсутствующейКИ, ВидКИ.Наименование);
							КонецЕсли;
						КонецЕсли;	
					КонецЕсли;
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = ЗначениеВидаКИ;
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);	
				КонецЦикла;
				Для Каждого Свойство Из ОтправляемыеДопРеквизитыИСведения Цикл
					ЗначениеДопРеквизита = "";
					Если ДанныеДляПечатиСтроки.Сведения.Свойство(Свойство.ИдентификаторДляФормул, ЗначениеДопРеквизита) = Ложь Тогда
						Если СписокОтсутствующихДопРеквизитов = "" Тогда
							СписокОтсутствующихДопРеквизитов = Свойство.Наименование;
						Иначе
							Если Найти(СписокОтсутствующихДопРеквизитов, Свойство.Наименование) = 0 Тогда
								СписокОтсутствующихДопРеквизитов = СтрШаблон("%1, %2", СписокОтсутствующихДопРеквизитов, Свойство.Наименование);		
							КонецЕсли;	
						КонецЕсли;	
					КонецЕсли;	
					ОбластьМакетаСтрокаПрисоединяемаяКолонка.Параметры["ЗначениеКолонки1"] = ЗначениеДопРеквизита;
					ТабличныйДокумент.Присоединить(ОбластьМакетаСтрокаПрисоединяемаяКолонка);
				КонецЦикла;				
			КонецЦикла;
		КонецЦикла;
		
		Если ЗначениеЗаполнено(СписокОтсутствующейКИ) Тогда
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СтрШаблон(НСтр("ru = 'Не удалось обнаружить вид контактной информации %1 в документе, перезаполните сотрудников.'"), 
				СписокОтсутствующейКИ));
		КонецЕсли;
		Если ЗначениеЗаполнено(СписокОтсутствующихДопРеквизитов) Тогда
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СтрШаблон(НСтр("ru = 'Не удалось обнаружить значения доп.реквизита %1 в документе, перезаполните сотрудников.'"), 
				СписокОтсутствующихДопРеквизитов));
		КонецЕсли;	
		
		ОбластьМакетаПодвал.Параметры.СтраховаяПремияИтого = СтраховаяПремияИтого;
		ТабличныйДокумент.Вывести(ОбластьМакетаПодвал);
		
		// В табличном документе необходимо задать имя области, в которую был 
		// выведен объект. Нужно для возможности печати покомплектно.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, ДанныеДокумента.ДокументПрикрепления);
		
	КонецЦикла; // по документам
	
	Возврат ТабличныйДокумент;
	
КонецФункции

Процедура ДополнитьШапкуКолонками(ТабличныйДокумент, ОтправляемыеДанные, ОтправляемаяКонтактнаяИнформация, ОтправляемыеДопРеквизитыИСведения, ОбластьМакетаШапкаПрисоединяемаяКолонка)

	Если ОтправляемыеДанные.Найти("ДатаРождения") <> Неопределено Тогда
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = НСтр("ru = 'Дата рождения'");
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЕсли;
	Если ОтправляемыеДанные.Найти("СтраховойНомерПФР") <> Неопределено Тогда
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = НСтр("ru = 'СНИЛС'");
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЕсли;
	Если ОтправляемыеДанные.Найти("Пол") <> Неопределено Тогда
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = НСтр("ru = 'Пол'");
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЕсли;
	Если ОтправляемыеДанные.Найти("Страна") <> Неопределено Тогда
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = НСтр("ru = 'Гражданство'");
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЕсли;
	Если ОтправляемыеДанные.Найти("ДокументПредставление") <> Неопределено Тогда
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = НСтр("ru = 'Документ удостоверяющий личность'");
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЕсли;
	Для Каждого ВидКИ Из ОтправляемаяКонтактнаяИнформация Цикл
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = ВидКИ.Наименование;
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЦикла;
	Для Каждого Свойство Из ОтправляемыеДопРеквизитыИСведения Цикл
		ОбластьМакетаШапкаПрисоединяемаяКолонка.Параметры["ИмяКолонки1"] = Свойство.Наименование;
		ТабличныйДокумент.Присоединить(ОбластьМакетаШапкаПрисоединяемаяКолонка);
	КонецЦикла;
	
КонецПроцедуры

// Получает данные для формирования печатного документа.
//
// Параметры:
//		МассивДокументов - Массив ссылок на документы, по которым требуется получить данные.
//
// Возвращаемое значение:
//		Соответствие - где Ключ - ссылка на документ, Значение - структура документа.
//
Функция ДанныеПрикрепленияКПрограммамСтрахования(МассивДокументов)
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("ПрикреплениеКПрограммамМедицинскогоСтрахования", МассивДокументов);
	
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка КАК ДокументПрикрепления,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.Организация КАК Организация,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.СтраховаяКомпания КАК СтраховаяКомпания,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.ДоговорСтрахования КАК ДоговорСтрахования,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.Сотрудник КАК Сотрудник,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.ДатаПрикрепления КАК ДатаНачала,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.ДатаОкончанияСтрахования КАК ДатаОкончания,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.ПрограммыСтрахования КАК ПрограммыСтрахования,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.РасширенияПрограммСтрахования КАК РасширенияПрограммСтрахования,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.СтраховаяПремия КАК СтраховаяПремия,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.ДатаРождения КАК ДатаРождения,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.СтраховойНомерПФР КАК СтраховойНомерПФР,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Пол КАК Пол,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Страна КАК Страна,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.ДокументПредставление КАК ДокументПредставление,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Представление КАК ПредставлениеКИ,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Вид.ИдентификаторДляФормул КАК ВидКИ,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Свойство.ИдентификаторДляФормул КАК ДопРеквизит,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Значение КАК ЗначениеДопРеквизита
	|ИЗ
	|	Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.Сотрудники КАК МедицинскоеСтрахованиеПрикреплениеСотрудники
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования КАК ПрикреплениеКПрограммамМедицинскогоСтрахования
	|		ПО МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахования.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ДанныеЗастрахованных КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных
	|		ПО МедицинскоеСтрахованиеПрикреплениеСотрудники.ФизическоеЛицо = ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Застрахованный
	|			И МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.КонтактнаяИнформацияЗастрахованных КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных
	|		ПО МедицинскоеСтрахованиеПрикреплениеСотрудники.ФизическоеЛицо = ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Застрахованный
	|			И МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ДополнительныеРеквизитыИСведенияЗастрахованных КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных
	|		ПО МедицинскоеСтрахованиеПрикреплениеСотрудники.ФизическоеЛицо = ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Застрахованный
	|			И МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Ссылка
	|ГДЕ
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка В(&ПрикреплениеКПрограммамМедицинскогоСтрахования)
	|
	|УПОРЯДОЧИТЬ ПО
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.Ссылка,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.Сотрудник,
	|	МедицинскоеСтрахованиеПрикреплениеСотрудники.ДатаПрикрепления
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка КАК ДокументПрикрепления,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.Организация КАК Организация,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.СтраховаяКомпания КАК СтраховаяКомпания,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахования.ДоговорСтрахования КАК ДоговорСтрахования,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.РодственникСсылка КАК Родственник,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.ДатаПрикрепления КАК ДатаНачала,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.ДатаОткрепления КАК ДатаОкончания,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.ПрограммыСтрахования КАК ПрограммыСтрахования,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.РасширенияПрограммСтрахования КАК РасширенияПрограммСтрахования,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.СтраховаяПремия КАК СтраховаяПремия,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.ДатаРождения КАК ДатаРождения,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.СтраховойНомерПФР КАК СтраховойНомерПФР,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Пол КАК Пол,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Страна КАК Страна,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.ДокументПредставление КАК ДокументПредставление,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Вид.ИдентификаторДляФормул КАК ВидКИ,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Представление КАК ПредставлениеКИ,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Свойство.ИдентификаторДляФормул КАК ДопРеквизит,
	|	ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Значение КАК ЗначениеДопРеквизита
	|ИЗ
	|	Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.Родственники КАК МедицинскоеСтрахованиеПрикреплениеРодственники
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования КАК ПрикреплениеКПрограммамМедицинскогоСтрахования
	|		ПО МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахования.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ДанныеЗастрахованных КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных
	|		ПО МедицинскоеСтрахованиеПрикреплениеРодственники.РодственникСсылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Застрахованный
	|			И МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияДанныеЗастрахованных.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.КонтактнаяИнформацияЗастрахованных КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных
	|		ПО МедицинскоеСтрахованиеПрикреплениеРодственники.РодственникСсылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Застрахованный
	|			И МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияКонтактнаяИнформацияЗастрахованных.Ссылка
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ДополнительныеРеквизитыИСведенияЗастрахованных КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных
	|		ПО МедицинскоеСтрахованиеПрикреплениеРодственники.РодственникСсылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Застрахованный
	|			И МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияДополнительныеРеквизитыИСведенияЗастрахованных.Ссылка
	|ГДЕ
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка В(&ПрикреплениеКПрограммамМедицинскогоСтрахования)
	|
	|УПОРЯДОЧИТЬ ПО
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.Ссылка,
	|	Родственник,
	|	МедицинскоеСтрахованиеПрикреплениеРодственники.ДатаПрикрепления";
	
	РезультатыЗапроса = Запрос.ВыполнитьПакет();
	
	ДанныеДокументов = Новый Соответствие;
	ВыборкаСотрудников = РезультатыЗапроса[РезультатыЗапроса.ВГраница()-1].Выбрать();
	Пока ВыборкаСотрудников.СледующийПоЗначениюПоля("ДокументПрикрепления") Цикл
		
		ДанныеДокумента = Новый Структура;
		ДанныеДокумента.Вставить("ДокументПрикрепления", ВыборкаСотрудников.ДокументПрикрепления);
		ДанныеДокумента.Вставить("Организация", ВыборкаСотрудников.Организация);
		ДанныеДокумента.Вставить("СтраховаяКомпания", ВыборкаСотрудников.СтраховаяКомпания);
		ДанныеДокумента.Вставить("ДоговорСтрахования", ВыборкаСотрудников.ДоговорСтрахования);
		ДанныеДокумента.Вставить("Сотрудники", Новый Массив);
		ДанныеДокумента.Вставить("Родственники", Новый Массив);
		Пока ВыборкаСотрудников.СледующийПоЗначениюПоля("Сотрудник") Цикл
			ДанныеСотрудника = Новый Структура;
			ДанныеСотрудника.Вставить("Сотрудник", ВыборкаСотрудников.Сотрудник);
			ДанныеСотрудника.Вставить("ДатаНачала", ВыборкаСотрудников.ДатаНачала);
			ДанныеСотрудника.Вставить("ДатаОкончания", ВыборкаСотрудников.ДатаОкончания);
			ДанныеСотрудника.Вставить("ПрограммыСтрахования", ВыборкаСотрудников.ПрограммыСтрахования);
			ДанныеСотрудника.Вставить("РасширенияПрограммСтрахования", ВыборкаСотрудников.РасширенияПрограммСтрахования);
			ДанныеСотрудника.Вставить("СтраховаяПремия", ВыборкаСотрудников.СтраховаяПремия);
			
			ДанныеСведений = Новый Структура;
			ДанныеСведений.Вставить("ДатаРождения", ВыборкаСотрудников.ДатаРождения);
			ДанныеСведений.Вставить("СтраховойНомерПФР", ВыборкаСотрудников.СтраховойНомерПФР);
			ДанныеСведений.Вставить("Пол", ВыборкаСотрудников.Пол);
			ДанныеСведений.Вставить("Страна", ВыборкаСотрудников.Страна);
			ДанныеСведений.Вставить("ДокументПредставление", ВыборкаСотрудников.ДокументПредставление);
			Пока ВыборкаСотрудников.Следующий() Цикл
				Если ЗначениеЗаполнено(ВыборкаСотрудников.ВидКИ) Тогда
					ДанныеСведений.Вставить(ВыборкаСотрудников.ВидКИ, ВыборкаСотрудников.ПредставлениеКИ);
				КонецЕсли;
				Если ЗначениеЗаполнено(ВыборкаСотрудников.ДопРеквизит) Тогда
					ДанныеСведений.Вставить(ВыборкаСотрудников.ДопРеквизит, ВыборкаСотрудников.ЗначениеДопРеквизита);
				КонецЕсли;
			КонецЦикла;
			
			ДанныеСотрудника.Вставить("Сведения", ДанныеСведений);
			
			ДанныеДокумента.Сотрудники.Добавить(ДанныеСотрудника);
		КонецЦикла;
		
		ДанныеДокументов.Вставить(ВыборкаСотрудников.ДокументПрикрепления, ДанныеДокумента);
		
	КонецЦикла;
	
	ВыборкаРодственников = РезультатыЗапроса[РезультатыЗапроса.ВГраница()].Выбрать();
	Пока ВыборкаРодственников.СледующийПоЗначениюПоля("ДокументПрикрепления") Цикл
		
		ДанныеДокумента = ДанныеДокументов.Получить(ВыборкаРодственников.ДокументПрикрепления);
		Если ДанныеДокумента = Неопределено Тогда
			ДанныеДокумента = Новый Структура;
			ДанныеДокумента.Вставить("ДокументПрикрепления", ВыборкаРодственников.ДокументПрикрепления);
			ДанныеДокумента.Вставить("Организация", ВыборкаРодственников.Организация);
			ДанныеДокумента.Вставить("СтраховаяКомпания", ВыборкаРодственников.СтраховаяКомпания);
			ДанныеДокумента.Вставить("ДоговорСтрахования", ВыборкаСотрудников.ДоговорСтрахования);
			ДанныеДокумента.Вставить("Сотрудники", Новый Массив);
			ДанныеДокумента.Вставить("Родственники", Новый Массив);
		КонецЕсли;
		ДанныеДокумента.Вставить("Родственники", Новый Массив);
		Пока ВыборкаРодственников.СледующийПоЗначениюПоля("Родственник") Цикл
			ДанныеРодственника = Новый Структура;
			ДанныеРодственника.Вставить("Родственник", ВыборкаРодственников.Родственник);
			ДанныеРодственника.Вставить("ДатаНачала", ВыборкаРодственников.ДатаНачала);
			ДанныеРодственника.Вставить("ДатаОкончания", ВыборкаРодственников.ДатаОкончания);
			ДанныеРодственника.Вставить("ПрограммыСтрахования", ВыборкаРодственников.ПрограммыСтрахования);
			ДанныеРодственника.Вставить("РасширенияПрограммСтрахования", ВыборкаРодственников.РасширенияПрограммСтрахования);
			ДанныеРодственника.Вставить("СтраховаяПремия", ВыборкаРодственников.СтраховаяПремия);
			
			ДанныеСведений = Новый Структура;
			ДанныеСведений.Вставить("ДатаРождения", ВыборкаРодственников.ДатаРождения);
			ДанныеСведений.Вставить("СтраховойНомерПФР", ВыборкаРодственников.СтраховойНомерПФР);
			ДанныеСведений.Вставить("Пол", ВыборкаРодственников.Пол);
			ДанныеСведений.Вставить("Страна", ВыборкаРодственников.Страна);
			ДанныеСведений.Вставить("ДокументПредставление", ВыборкаРодственников.ДокументПредставление);
			Пока ВыборкаРодственников.Следующий() Цикл
				Если ЗначениеЗаполнено(ВыборкаРодственников.ВидКИ) Тогда
					ДанныеСведений.Вставить(ВыборкаРодственников.ВидКИ, ВыборкаРодственников.ПредставлениеКИ);
				КонецЕсли;
				Если ЗначениеЗаполнено(ВыборкаРодственников.ДопРеквизит) Тогда
					ДанныеСведений.Вставить(ВыборкаРодственников.ДопРеквизит, ВыборкаРодственников.ЗначениеДопРеквизита);
				КонецЕсли;
			КонецЦикла;
			
			ДанныеРодственника.Вставить("Сведения", ДанныеСведений);
			
			ДанныеДокумента.Родственники.Добавить(ДанныеРодственника);
		КонецЦикла;
		
		ДанныеДокументов.Вставить(ВыборкаРодственников.ДокументПрикрепления, ДанныеДокумента);
		
	КонецЦикла;
	
	Возврат ДанныеДокументов;
	
КонецФункции

Функция ПечатьЗаявленияНаУдержание(МассивОбъектов, ОбъектыПечати, ПараметрыПечати)
	
	ДанныеДляПечати = РассылкаДокументов.ДанныеПечати(ПараметрыПечати);
	Если ДанныеДляПечати = Неопределено Тогда
		ДанныеДляПечати = ДанныеЗаявленияНаУдержание(МассивОбъектов);
	КонецЕсли;
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.КлючПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_МедицинскоеСтрахование_ЗаявленияНаУдержаниеЗаРодственника";
	
	Если ДанныеДляПечати.Количество() = 0 Тогда
		Возврат ТабличныйДокумент;
	КонецЕсли;
	
	Макет = УправлениеПечатью.МакетПечатнойФормы("Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ПФ_MXL_ЗаявлениеНаУдержание");
	
	ПервыйДокумент = Истина;
	
	Для Каждого СтрокаСотрудник Из ДанныеДляПечати Цикл
		
		НачалоОбласти = ТабличныйДокумент.ВысотаТаблицы + 1;
		
		Если Не ПервыйДокумент Тогда
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		Иначе
			ПервыйДокумент = Ложь;
		КонецЕсли;
		
		ОбластьШапка = Макет.ПолучитьОбласть("Шапка");
		ОбластьШапка.Параметры.Организация = СтрокаСотрудник.Организация;
		
		ОтветственныеЛица = ПодписиДокументов.ОтветственныеЛицаОрганизации(СтрокаСотрудник.Организация, "Руководитель");
		Руководитель = ОтветственныеЛица.Руководитель;
		МассивФизическихЛиц = Новый Массив;
		МассивФизическихЛиц.Добавить(Руководитель);
		МассивФизическихЛиц.Добавить(СтрокаСотрудник.ФизическоеЛицо);
		КадровыеДанныеФизическихЛиц = КадровыйУчет.КадровыеДанныеФизическихЛиц(Истина, МассивФизическихЛиц, "ФИО, Пол, ИОФамилия");
		
		ОтборСтрок = Новый Структура("ФизическоеЛицо");
		ОтборСтрок.ФизическоеЛицо = Руководитель;
		НайденныеСтроки = КадровыеДанныеФизическихЛиц.НайтиСтроки(ОтборСтрок);
		Если НайденныеСтроки.Количество() > 0 Тогда
			ФИОРуководителяВДательномПадеже = "";
			ФизическиеЛицаЗарплатаКадры.Просклонять(Строка(НайденныеСтроки[0].ФИО), 3, ФИОРуководителяВДательномПадеже, НайденныеСтроки[0].Пол, Руководитель);
			ОбластьШапка.Параметры.ФИОРуководителяВДательномПадеже = ФИОРуководителяВДательномПадеже;
		КонецЕсли;
		
		ОтборСтрок.ФизическоеЛицо = СтрокаСотрудник.ФизическоеЛицо;
		НайденныеСтроки = КадровыеДанныеФизическихЛиц.НайтиСтроки(ОтборСтрок);
		Если НайденныеСтроки.Количество() > 0 Тогда
			ФИОСотрудникаВРодительномПадеже = "";
			ФизическиеЛицаЗарплатаКадры.Просклонять(Строка(НайденныеСтроки[0].ФИО), 2, ФИОСотрудникаВРодительномПадеже, НайденныеСтроки[0].Пол, СтрокаСотрудник.ФизическоеЛицо);
			ОбластьШапка.Параметры.ФИОСотрудникаВРодительномПадеже = ФИОСотрудникаВРодительномПадеже;
			ИнициалыФамилияСотрудника = НайденныеСтроки[0].ИОФамилия;
		КонецЕсли;
		
		ТабличныйДокумент.Вывести(ОбластьШапка);
		
		УдержаниеССотрудника = Ложь;
		Если СтрокаСотрудник.УдерживатьИзЗарплаты И СтрокаСотрудник.СтраховаяПремияСотрудника > 0 Тогда
			УдержаниеССотрудника = Истина;
			ОбластьУдержаниеССотрудника = Макет.ПолучитьОбласть("УдержаниеССотрудника");
			СуммаУдержанияССотрудника = ?(СтрокаСотрудник.ЧастичноеУдержание,
					Окр(СтрокаСотрудник.СтраховаяПремияСотрудника * СтрокаСотрудник.ПроцентЧастичногоУдержания / 100, 2),
					СтрокаСотрудник.СтраховаяПремияСотрудника);
			ОбластьУдержаниеССотрудника.Параметры.СуммаССотрудника = Формат(СуммаУдержанияССотрудника, "ЧДЦ=2");
			ТабличныйДокумент.Вывести(ОбластьУдержаниеССотрудника);
		КонецЕсли;
		
		КоличествоРодственников = СтрокаСотрудник.Родственники.Количество();
		Если КоличествоРодственников > 0 Тогда
			ОбластьУдержаниеЗаРодственника = Макет.ПолучитьОбласть("УдержаниеЗаРодственника");
			Для Каждого СтрокаРодственник Из СтрокаСотрудник.Родственники Цикл
				СклонениеРодственника = "";
				ФизическиеЛицаЗарплатаКадры.Просклонять(СтрокаРодственник.Родственник, 4, СклонениеРодственника);
				ОбластьУдержаниеЗаРодственника.Параметры.ФИОРодственникаВВинительномПадеже = СклонениеРодственника;
				СуммаУдержанияЗаРодственника = ?(СтрокаСотрудник.ЧастичноеУдержание,
					Окр(СтрокаРодственник.СтраховаяПремияРодственника * СтрокаСотрудник.ПроцентЧастичногоУдержания / 100, 2),
					СтрокаРодственник.СтраховаяПремияРодственника);
				ОбластьУдержаниеЗаРодственника.Параметры.СуммаЗаРодственника = Формат(СуммаУдержанияЗаРодственника, "ЧДЦ=2");
				ТабличныйДокумент.Вывести(ОбластьУдержаниеЗаРодственника);
			КонецЦикла;
		КонецЕсли;
		
		Если (УдержаниеССотрудника И КоличествоРодственников > 0) Или КоличествоРодственников > 1 Тогда
			ОбластьИтого = Макет.ПолучитьОбласть("Итого");
			СуммаУдержания = ?(СтрокаСотрудник.ЧастичноеУдержание, СтрокаСотрудник.СуммаЧастичногоУдержания, СтрокаСотрудник.ОбщаяСуммаУдержания);
			ОбластьИтого.Параметры.СуммаИтого = Формат(СуммаУдержания, "ЧДЦ=2");
			ТабличныйДокумент.Вывести(ОбластьИтого);
		КонецЕсли;
		
		Если СтрокаСотрудник.СпособУдержания = Перечисления.СпособыУдержанияМедицинскогоСтрахованияСотрудников.ПоГрафику Тогда
			Если СтрокаСотрудник.ГрафикУдержаний.Количество() > 0 Тогда
				ОбластьУдержаниеПоГрафику = Макет.ПолучитьОбласть("ТекстУдерживатьПоГрафику");
				ТабличныйДокумент.Вывести(ОбластьУдержаниеПоГрафику);
				Для Каждого СтрокаГрафика Из СтрокаСотрудник.ГрафикУдержаний Цикл
					ОбластьГрафик = Макет.ПолучитьОбласть("ГрафикУдержания");
					ОбластьГрафик.Параметры.МесяцУдержания = СтрокаГрафика.ДатаСтрокой;
					ОбластьГрафик.Параметры.СуммаУдержания = Формат(СтрокаГрафика.Сумма, "ЧДЦ=2");
					ТабличныйДокумент.Вывести(ОбластьГрафик);
				КонецЦикла;
			КонецЕсли;
		КонецЕсли;
		
		ОбластьПодвал = Макет.ПолучитьОбласть("Подвал");
		ОбластьПодвал.Параметры.ИнициалыФамилияСотрудника = ИнициалыФамилияСотрудника;
		ТабличныйДокумент.Вывести(ОбластьПодвал);
		
		// В табличном документе необходимо задать имя области, в которую был 
		// выведен объект. Нужно для возможности печати покомплектно.
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НачалоОбласти, ОбъектыПечати, СтрокаСотрудник.Ссылка);
		
	КонецЦикла;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

Функция ДанныеЗаявленияНаУдержание(МассивДокументов) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ПрикреплениеКПрограммамМедицинскогоСтрахования", МассивДокументов);
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.Ссылка КАК Ссылка,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.Ссылка.Организация КАК Организация,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.Сотрудник КАК Сотрудник,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.СтраховаяПремия КАК СтраховаяПремияСотрудника,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.УдерживатьИзЗарплаты КАК УдерживатьИзЗарплаты,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.ОбщаяСуммаУдержания КАК ОбщаяСуммаУдержания,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.СпособУдержания КАК СпособУдержания,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.ЧастичноеУдержание КАК ЧастичноеУдержание,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.СуммаЧастичногоУдержания КАК СуммаЧастичногоУдержания,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.ПроцентЧастичногоУдержания КАК ПроцентЧастичногоУдержания
		|ПОМЕСТИТЬ ВТСотрудники
		|ИЗ
		|	Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.Сотрудники КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники
		|ГДЕ
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияСотрудники.Ссылка В(&ПрикреплениеКПрограммамМедицинскогоСтрахования)
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТСотрудники.Ссылка КАК Ссылка,
		|	ВТСотрудники.Ссылка КАК РассылаемыйДокумент,
		|	ВТСотрудники.Организация КАК Организация,
		|	ВТСотрудники.Сотрудник КАК Сотрудник,
		|	ВТСотрудники.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ВТСотрудники.СтраховаяПремияСотрудника КАК СтраховаяПремияСотрудника,
		|	ВТСотрудники.УдерживатьИзЗарплаты КАК УдерживатьИзЗарплаты,
		|	ВТСотрудники.ОбщаяСуммаУдержания КАК ОбщаяСуммаУдержания,
		|	ВТСотрудники.СпособУдержания КАК СпособУдержания,
		|	ВТСотрудники.ЧастичноеУдержание КАК ЧастичноеУдержание,
		|	ВТСотрудники.СуммаЧастичногоУдержания КАК СуммаЧастичногоУдержания,
		|	ВТСотрудники.ПроцентЧастичногоУдержания КАК ПроцентЧастичногоУдержания
		|ИЗ
		|	ВТСотрудники КАК ВТСотрудники
		|ГДЕ
		|	ВТСотрудники.ОбщаяСуммаУдержания > 0
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТСотрудники.Ссылка КАК Ссылка,
		|	ВТСотрудники.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники.РодственникСсылка КАК Родственник,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники.СтраховаяПремия КАК СтраховаяПремияРодственника
		|ИЗ
		|	ВТСотрудники КАК ВТСотрудники
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.Родственники КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники
		|		ПО ВТСотрудники.Ссылка = ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники.Ссылка
		|			И ВТСотрудники.ФизическоеЛицо = ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники.ФизическоеЛицо
		|ГДЕ
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники.УдерживатьССотрудника = ИСТИНА
		|	И ПрикреплениеКПрограммамМедицинскогоСтрахованияРодственники.СтраховаяПремия > 0
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний.Ссылка КАК Ссылка,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний.ФизическоеЛицо КАК ФизическоеЛицо,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний.Дата КАК Дата,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний.ДатаСтрокой КАК ДатаСтрокой,
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний.Сумма КАК Сумма
		|ИЗ
		|	Документ.ПрикреплениеКПрограммамМедицинскогоСтрахования.ГрафикУдержаний КАК ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний
		|ГДЕ
		|	ПрикреплениеКПрограммамМедицинскогоСтрахованияГрафикУдержаний.Ссылка В(&ПрикреплениеКПрограммамМедицинскогоСтрахования)
		|
		|УПОРЯДОЧИТЬ ПО
		|	Дата";
		
	Результат = Запрос.ВыполнитьПакет();
	
	Сотрудники = Результат[1].Выгрузить();
	Сотрудники.Колонки.Добавить("Родственники", Новый ОписаниеТипов("ТаблицаЗначений"));
	Сотрудники.Колонки.Добавить("ГрафикУдержаний", Новый ОписаниеТипов("ТаблицаЗначений"));
	Родственники = Результат[2].Выгрузить();
	ГрафикУдержаний = Результат[3].Выгрузить();
	
	Для Каждого СтрокаСотрудник Из Сотрудники Цикл
		
		СтрокаСотрудник.Родственники = Родственники.Скопировать(
			Новый Структура("Ссылка, ФизическоеЛицо", СтрокаСотрудник.Ссылка, СтрокаСотрудник.ФизическоеЛицо));
		СтрокаСотрудник.ГрафикУдержаний = ГрафикУдержаний.Скопировать(
			Новый Структура("Ссылка, ФизическоеЛицо", СтрокаСотрудник.Ссылка, СтрокаСотрудник.ФизическоеЛицо));
		
	КонецЦикла;
	
	Возврат Сотрудники;
	
КонецФункции

#КонецОбласти

#Область Загрузка

// Устанавливает параметры загрузки.
//
Процедура УстановитьПараметрыЗагрузкиИзФайлаВТЧ(Параметры) Экспорт
	
КонецПроцедуры

// Производит сопоставление данных, загружаемых в табличную часть ПолноеИмяТабличнойЧасти,
// с данными в ИБ, и заполняет параметры АдресТаблицыСопоставления и СписокНеоднозначностей.
//
// Параметры:
//   АдресЗагружаемыхДанных    - Строка - Адрес временного хранилища с таблицей значений, в которой
//                                        находятся загруженные данные из файла. Состав колонок:
//     * Идентификатор - Число - Порядковый номер строки;
//     * остальные колонки соответствуют колонкам макета ЗагрузкаИзФайла.
//   АдресТаблицыСопоставления - Строка - Адрес временного хранилища с пустой таблицей значений,
//                                        являющейся копией табличной части документа, 
//                                        которую необходимо заполнить из таблицы АдресЗагружаемыхДанных.
//   СписокНеоднозначностей - ТаблицаЗначений - Список неоднозначных значений, для которых в ИБ имеется несколько
//                                              подходящих вариантов.
//     * Колонка       - Строка - Имя колонки, в которой была обнаружена неоднозначность;
//     * Идентификатор - Число  - Идентификатор строки, в которой была обнаружена неоднозначность.
//   ПолноеИмяТабличнойЧасти   - Строка - Полное имя табличной части, в которую загружаются данные.
//   ДополнительныеПараметры   - ЛюбойТип - Любые дополнительные сведения.
//
Процедура СопоставитьЗагружаемыеДанные(АдресЗагружаемыхДанных, АдресТаблицыСопоставления, СписокНеоднозначностей, ПолноеИмяТабличнойЧасти, ДополнительныеПараметры) Экспорт
	
	Сотрудники =  ПолучитьИзВременногоХранилища(АдресТаблицыСопоставления);
	
	ЗагружаемыеДанные = ПолучитьИзВременногоХранилища(АдресЗагружаемыхДанных);
	
	Если ДополнительныеПараметры.Свойство("ЗагрузкаНомеровПолисовСотрудников") Тогда
		МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
		Запрос.Текст = 
			"ВЫБРАТЬ
			|	ВЫРАЗИТЬ(ДанныеДляСопоставления.Сотрудник КАК СТРОКА(100)) КАК Сотрудник,
			|	ВЫРАЗИТЬ(ДанныеДляСопоставления.ТабельныйНомер КАК СТРОКА(100)) КАК ТабельныйНомер,
			|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор
			|ПОМЕСТИТЬ ДанныеДляСопоставления
			|ИЗ
			|	&ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	Сотрудники.Ссылка КАК Сотрудник,
			|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
			|	2 КАК Приоритет
			|ПОМЕСТИТЬ СотрудникиСопоставленные
			|ИЗ Справочник.Сотрудники КАК Сотрудники
			|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|		ПО Сотрудники.Код = ДанныеДляСопоставления.ТабельныйНомер
			|			И (НЕ Сотрудники.ВАрхиве)
			|
			|ОБЪЕДИНИТЬ ВСЕ
			|
			|ВЫБРАТЬ
			|	Сотрудники.Ссылка,
			|	ДанныеДляСопоставления.Идентификатор,
			|	1
			|ИЗ Справочник.Сотрудники КАК Сотрудники
			|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|		ПО Сотрудники.Наименование = ДанныеДляСопоставления.Сотрудник
			|			И ДанныеДляСопоставления.ТабельныйНомер = """"
			|			И (НЕ Сотрудники.ВАрхиве)
			|
			|ИНДЕКСИРОВАТЬ ПО
			|	Идентификатор
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	СотрудникиСопоставленные.Идентификатор КАК Идентификатор,
			|	МАКСИМУМ(СотрудникиСопоставленные.Приоритет) КАК Приоритет
			|ПОМЕСТИТЬ СотрудникиСопоставленныеМаксПриоритет
			|ИЗ СотрудникиСопоставленные КАК СотрудникиСопоставленные
			|
			|СГРУППИРОВАТЬ ПО
			|	СотрудникиСопоставленные.Идентификатор
			|
			|ИНДЕКСИРОВАТЬ ПО
			|	Идентификатор
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	СотрудникиСопоставленныеМаксПриоритет.Идентификатор КАК Идентификатор,
			|	СотрудникиСопоставленные.Сотрудник КАК Сотрудник
			|ПОМЕСТИТЬ СотрудникиСопоставленныеИтог
			|ИЗ СотрудникиСопоставленныеМаксПриоритет КАК СотрудникиСопоставленныеМаксПриоритет
			|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ СотрудникиСопоставленные КАК СотрудникиСопоставленные
			|		ПО СотрудникиСопоставленныеМаксПриоритет.Идентификатор = СотрудникиСопоставленные.Идентификатор
			|		И СотрудникиСопоставленныеМаксПриоритет.Приоритет = СотрудникиСопоставленные.Приоритет	
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	МАКСИМУМ(СотрудникиСопоставленныеИтог.Сотрудник) КАК Сотрудник,
			|	СотрудникиСопоставленныеИтог.Идентификатор КАК Идентификатор,
			|	КОЛИЧЕСТВО(СотрудникиСопоставленныеИтог.Идентификатор) КАК Количество
			|ИЗ
			|	СотрудникиСопоставленныеИтог КАК СотрудникиСопоставленныеИтог
			|
			|СГРУППИРОВАТЬ ПО
			|	СотрудникиСопоставленныеИтог.Идентификатор";
		
		Запрос.УстановитьПараметр("ДанныеДляСопоставления", ЗагружаемыеДанные);
		РезультатыЗапросов = Запрос.ВыполнитьПакет();
	
		ТаблицаСотрудники 	= РезультатыЗапросов[4].Выгрузить();
		
		Для Каждого СтрокаТаблицы Из ЗагружаемыеДанные Цикл
			
			СтрокаСотрудник = Сотрудники.Добавить();
			СтрокаСотрудник.Идентификатор 	= СтрокаТаблицы.Идентификатор;
			СтрокаСотрудник.НомерПолиса	= СтрокаТаблицы.НомерПолиса;
			
			СтрокаТаблицыСотрудник = ТаблицаСотрудники.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
			Если СтрокаТаблицыСотрудник <> Неопределено Тогда 
				Если СтрокаТаблицыСотрудник.Количество = 1 Тогда 
					СтрокаСотрудник.Сотрудник = СтрокаТаблицыСотрудник.Сотрудник;
				ИначеЕсли СтрокаТаблицыСотрудник.Количество > 1 Тогда
					ЗаписьОНеоднозначности = СписокНеоднозначностей.Добавить();
					ЗаписьОНеоднозначности.Идентификатор = СтрокаТаблицы.Идентификатор;
					ЗаписьОНеоднозначности.Колонка = "Сотрудник";
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		ПоместитьВоВременноеХранилище(Сотрудники, АдресТаблицыСопоставления);
		Возврат;
		
	КонецЕсли;
	
	Если ДополнительныеПараметры.Свойство("ЗагрузкаНомеровПолисовРодственников") Тогда
		МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
		Запрос.Текст = 
			"ВЫБРАТЬ
			|	ВЫРАЗИТЬ(ДанныеДляСопоставления.РодственникСсылка КАК СТРОКА(100)) КАК Родственник,
			|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор
			|ПОМЕСТИТЬ ДанныеДляСопоставления
			|ИЗ
			|	&ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	МАКСИМУМ(РодственникиФизическихЛиц.Ссылка) КАК Родственник,
			|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
			|	КОЛИЧЕСТВО(ДанныеДляСопоставления.Идентификатор) КАК Количество
			|ИЗ
			|	ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.РодственникиФизическихЛиц КАК РодственникиФизическихЛиц
			|		ПО ДанныеДляСопоставления.Родственник = РодственникиФизическихЛиц.Наименование
			|
			|СГРУППИРОВАТЬ ПО
			|	ДанныеДляСопоставления.Идентификатор";
		Запрос.УстановитьПараметр("ДанныеДляСопоставления", ЗагружаемыеДанные);
		РезультатыЗапросов = Запрос.ВыполнитьПакет();
	
		ТаблицаРодственники= РезультатыЗапросов[1].Выгрузить();
		
		Для Каждого СтрокаТаблицы Из ЗагружаемыеДанные Цикл
			
			СтрокаСотрудник = Сотрудники.Добавить();
			СтрокаСотрудник.Идентификатор = СтрокаТаблицы.Идентификатор;
			СтрокаСотрудник.НомерПолиса = СтрокаТаблицы.НомерПолиса;
			
			СтрокаТаблицыРодственник = ТаблицаРодственники.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
			Если СтрокаТаблицыРодственник <> Неопределено Тогда
				Если СтрокаТаблицыРодственник.Количество = 1 Тогда 
					СтрокаСотрудник.РодственникСсылка = СтрокаТаблицыРодственник.Родственник;
				ИначеЕсли СтрокаТаблицыРодственник.Количество > 1 Тогда
					ЗаписьОНеоднозначности = СписокНеоднозначностей.Добавить();
					ЗаписьОНеоднозначности.Идентификатор = СтрокаТаблицы.Идентификатор;
					ЗаписьОНеоднозначности.Колонка = "Родственник";
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		ПоместитьВоВременноеХранилище(Сотрудники, АдресТаблицыСопоставления);
		Возврат;
		
	КонецЕсли;
	
	Если ДополнительныеПараметры.Свойство("ЗагрузкаПерсональныхДанныхРодственников") Тогда
		МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
		Запрос = Новый Запрос;
		Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
		Запрос.Текст = 
			"ВЫБРАТЬ
			|	ВЫРАЗИТЬ(ДанныеДляСопоставления.РодственникСсылка КАК СТРОКА(100)) КАК Родственник,
			|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор
			|ПОМЕСТИТЬ ДанныеДляСопоставления
			|ИЗ
			|	&ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|;
			|
			|////////////////////////////////////////////////////////////////////////////////
			|ВЫБРАТЬ
			|	МАКСИМУМ(РодственникиФизическихЛиц.Ссылка) КАК Родственник,
			|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
			|	КОЛИЧЕСТВО(ДанныеДляСопоставления.Идентификатор) КАК Количество
			|ИЗ
			|	ДанныеДляСопоставления КАК ДанныеДляСопоставления
			|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.РодственникиФизическихЛиц КАК РодственникиФизическихЛиц
			|		ПО ДанныеДляСопоставления.Родственник = РодственникиФизическихЛиц.Наименование
			|
			|СГРУППИРОВАТЬ ПО
			|	ДанныеДляСопоставления.Идентификатор";
		Запрос.УстановитьПараметр("ДанныеДляСопоставления", ЗагружаемыеДанные);
		РезультатыЗапросов = Запрос.ВыполнитьПакет();
	
		ТаблицаРодственники= РезультатыЗапросов[1].Выгрузить();
		
		Для Каждого СтрокаТаблицы Из ЗагружаемыеДанные Цикл
			
			СтрокаСотрудник = Сотрудники.Добавить();
			СтрокаСотрудник.Идентификатор = СтрокаТаблицы.Идентификатор;
			СтрокаСотрудник.Email = СтрокаТаблицы.Email;
			СтрокаСотрудник.МобильныйТелефон = СтрокаТаблицы.МобильныйТелефон;
			СтрокаСотрудник.АдресДляИнформирования = СтрокаТаблицы.АдресДляИнформирования;
			
			СтрокаТаблицыРодственник = ТаблицаРодственники.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
			Если СтрокаТаблицыРодственник <> Неопределено Тогда
				Если СтрокаТаблицыРодственник.Количество = 1 Тогда 
					СтрокаСотрудник.Родственник= СтрокаТаблицыРодственник.Родственник;
				ИначеЕсли СтрокаТаблицыРодственник.Количество > 1 Тогда
					ЗаписьОНеоднозначности = СписокНеоднозначностей.Добавить();
					ЗаписьОНеоднозначности.Идентификатор = СтрокаТаблицы.Идентификатор;
					ЗаписьОНеоднозначности.Колонка = "Родственник";
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
		ПоместитьВоВременноеХранилище(Сотрудники, АдресТаблицыСопоставления);
		Возврат;
		
	КонецЕсли;
	
	МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = МенеджерВременныхТаблиц;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ВЫРАЗИТЬ(ДанныеДляСопоставления.Сотрудник КАК СТРОКА(100)) КАК Сотрудник,
	|	ВЫРАЗИТЬ(ДанныеДляСопоставления.ТабельныйНомер КАК СТРОКА(100)) КАК ТабельныйНомер,
	|	ВЫРАЗИТЬ(ДанныеДляСопоставления.ПрограммаСтрахования КАК СТРОКА(100)) КАК ПрограммаСтрахования,
	|	ВЫРАЗИТЬ(ДанныеДляСопоставления.РасширениеПрограммСтрахования КАК СТРОКА(100)) КАК РасширениеПрограммСтрахования,
	|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор
	|ПОМЕСТИТЬ ДанныеДляСопоставления
	|ИЗ
	|	&ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Сотрудники.Ссылка КАК Сотрудник,
	|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
	|	2 КАК Приоритет
	|ПОМЕСТИТЬ СотрудникиСопоставленные
	|ИЗ Справочник.Сотрудники КАК Сотрудники
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|		ПО Сотрудники.Код = ДанныеДляСопоставления.ТабельныйНомер
	|			И (НЕ Сотрудники.ВАрхиве)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	Сотрудники.Ссылка,
	|	ДанныеДляСопоставления.Идентификатор,
	|	1
	|ИЗ Справочник.Сотрудники КАК Сотрудники
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|		ПО Сотрудники.Наименование = ДанныеДляСопоставления.Сотрудник
	|			И ДанныеДляСопоставления.ТабельныйНомер = """"
	|			И (НЕ Сотрудники.ВАрхиве)
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СотрудникиСопоставленные.Идентификатор КАК Идентификатор,
	|	МАКСИМУМ(СотрудникиСопоставленные.Приоритет) КАК Приоритет
	|ПОМЕСТИТЬ СотрудникиСопоставленныеМаксПриоритет
	|ИЗ СотрудникиСопоставленные КАК СотрудникиСопоставленные
	|
	|СГРУППИРОВАТЬ ПО
	|	СотрудникиСопоставленные.Идентификатор
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СотрудникиСопоставленныеМаксПриоритет.Идентификатор КАК Идентификатор,
	|	СотрудникиСопоставленные.Сотрудник КАК Сотрудник
	|ПОМЕСТИТЬ СотрудникиСопоставленныеИтог
	|ИЗ СотрудникиСопоставленныеМаксПриоритет КАК СотрудникиСопоставленныеМаксПриоритет
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ СотрудникиСопоставленные КАК СотрудникиСопоставленные
	|		ПО СотрудникиСопоставленныеМаксПриоритет.Идентификатор = СотрудникиСопоставленные.Идентификатор
	|		И СотрудникиСопоставленныеМаксПриоритет.Приоритет = СотрудникиСопоставленные.Приоритет	
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	МАКСИМУМ(СотрудникиСопоставленныеИтог.Сотрудник) КАК Сотрудник,
	|	СотрудникиСопоставленныеИтог.Идентификатор КАК Идентификатор,
	|	КОЛИЧЕСТВО(СотрудникиСопоставленныеИтог.Идентификатор) КАК Количество
	|ИЗ
	|	СотрудникиСопоставленныеИтог КАК СотрудникиСопоставленныеИтог
	|
	|СГРУППИРОВАТЬ ПО
	|	СотрудникиСопоставленныеИтог.Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	МАКСИМУМ(ПрограммыМедицинскогоСтрахования.Ссылка) КАК ПрограммаСтрахования,
	|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
	|	КОЛИЧЕСТВО(ДанныеДляСопоставления.Идентификатор) КАК Количество
	|ИЗ
	|	ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПрограммыМедицинскогоСтрахования КАК ПрограммыМедицинскогоСтрахования
	|		ПО (ПрограммыМедицинскогоСтрахования.Наименование = ДанныеДляСопоставления.ПрограммаСтрахования)
	|		И (ПрограммыМедицинскогоСтрахования.ПометкаУдаления = ЛОЖЬ)	
	|
	|СГРУППИРОВАТЬ ПО
	|	ДанныеДляСопоставления.Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	МАКСИМУМ(РасширенияПрограммМедицинскогоСтрахования.Ссылка) КАК РасширениеПрограммСтрахования,
	|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
	|	КОЛИЧЕСТВО(ДанныеДляСопоставления.Идентификатор) КАК Количество
	|ИЗ
	|	ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.РасширенияПрограммМедицинскогоСтрахования КАК РасширенияПрограммМедицинскогоСтрахования
	|		ПО (РасширенияПрограммМедицинскогоСтрахования.Наименование = ДанныеДляСопоставления.РасширениеПрограммСтрахования)
	|		И (РасширенияПрограммМедицинскогоСтрахования.ПометкаУдаления = ЛОЖЬ)
	|
	|СГРУППИРОВАТЬ ПО
	|	ДанныеДляСопоставления.Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	МАКСИМУМ(ПрограммыМедицинскогоСтрахования.Ссылка) КАК ПрограммаСтрахования,
	|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
	|	КОЛИЧЕСТВО(ДанныеДляСопоставления.Идентификатор) КАК Количество
	|ИЗ
	|	ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПрограммыМедицинскогоСтрахования КАК ПрограммыМедицинскогоСтрахования
	|		ПО (ПрограммыМедицинскогоСтрахования.Наименование = ДанныеДляСопоставления.ПрограммаСтрахования)
	|		И (ПрограммыМедицинскогоСтрахования.ПометкаУдаления = ИСТИНА)	
	|
	|СГРУППИРОВАТЬ ПО
	|	ДанныеДляСопоставления.Идентификатор
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	МАКСИМУМ(РасширенияПрограммМедицинскогоСтрахования.Ссылка) КАК РасширениеПрограммСтрахования,
	|	ДанныеДляСопоставления.Идентификатор КАК Идентификатор,
	|	КОЛИЧЕСТВО(ДанныеДляСопоставления.Идентификатор) КАК Количество
	|ИЗ
	|	ДанныеДляСопоставления КАК ДанныеДляСопоставления
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.РасширенияПрограммМедицинскогоСтрахования КАК РасширенияПрограммМедицинскогоСтрахования
	|		ПО (РасширенияПрограммМедицинскогоСтрахования.Наименование = ДанныеДляСопоставления.РасширениеПрограммСтрахования)
	|		И (РасширенияПрограммМедицинскогоСтрахования.ПометкаУдаления = ИСТИНА)
	|
	|СГРУППИРОВАТЬ ПО
	|	ДанныеДляСопоставления.Идентификатор";
	
	Запрос.УстановитьПараметр("ДанныеДляСопоставления", ЗагружаемыеДанные);
	РезультатыЗапросов = Запрос.ВыполнитьПакет();
	
	ТаблицаСотрудники 	= РезультатыЗапросов[4].Выгрузить();
	ТаблицаПрограммСтрахования = РезультатыЗапросов[5].Выгрузить();
	ТаблицаРасширенийПрограммСтрахования = РезультатыЗапросов[6].Выгрузить();
	ТаблицаУдаленныхПрограммСтрахования = РезультатыЗапросов[7].Выгрузить();
	ТаблицаУдаленныхРасширенийПрограммСтрахования = РезультатыЗапросов[8].Выгрузить();
	
	Для Каждого СтрокаТаблицы Из ЗагружаемыеДанные Цикл
		
		СтрокаСотрудник = Сотрудники.Добавить();
		СтрокаСотрудник.Идентификатор 	= СтрокаТаблицы.Идентификатор;
		СтрокаСотрудник.ДатаПрикрепления= СтроковыеФункцииКлиентСервер.СтрокаВДату(СтрокаТаблицы.ДатаПрикрепления);
		СтрокаСотрудник.СтраховаяПремия = СтроковыеФункцииКлиентСервер.СтрокаВЧисло(СтрокаТаблицы.СтраховаяПремия);
		СтрокаСотрудник.СтраховаяПремияРасширения = СтроковыеФункцииКлиентСервер.СтрокаВЧисло(СтрокаТаблицы.СтраховаяПремияРасширения);
			
		СтрокаТаблицыСотрудник = ТаблицаСотрудники.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
		Если СтрокаТаблицыСотрудник <> Неопределено Тогда 
			Если СтрокаТаблицыСотрудник.Количество = 1 Тогда 
				СтрокаСотрудник.Сотрудник = СтрокаТаблицыСотрудник.Сотрудник;
			ИначеЕсли СтрокаТаблицыСотрудник.Количество > 1 Тогда
				ЗаписьОНеоднозначности = СписокНеоднозначностей.Добавить();
				ЗаписьОНеоднозначности.Идентификатор = СтрокаТаблицы.Идентификатор; 
				ЗаписьОНеоднозначности.Колонка = "Сотрудник";
			КонецЕсли;
		КонецЕсли;
		
		СтрокаТаблицыПрограммСтрахования = ТаблицаПрограммСтрахования.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
		Если СтрокаТаблицыПрограммСтрахования <> Неопределено Тогда
			ОбработатьСтрокуТаблицыПрограммСтрахования(СтрокаСотрудник, СтрокаТаблицыПрограммСтрахования, СписокНеоднозначностей, СтрокаТаблицы.Идентификатор);
		Иначе
			СтрокаТаблицыПрограммСтрахования = ТаблицаУдаленныхПрограммСтрахования.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
			Если СтрокаТаблицыПрограммСтрахования <> Неопределено Тогда
				ОбработатьСтрокуТаблицыПрограммСтрахования(СтрокаСотрудник, СтрокаТаблицыПрограммСтрахования, СписокНеоднозначностей, СтрокаТаблицы.Идентификатор);	
			КонецЕсли;
		КонецЕсли;
		
		СтрокаТаблицыРасширенийПрограммСтрахования = ТаблицаРасширенийПрограммСтрахования.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
		Если СтрокаТаблицыРасширенийПрограммСтрахования <> Неопределено Тогда
			ОбработатьСтрокуТаблицыРасширенийПрограммСтрахования(СтрокаСотрудник, СтрокаТаблицыРасширенийПрограммСтрахования, СписокНеоднозначностей, СтрокаТаблицы.Идентификатор);	
		Иначе
			СтрокаТаблицыРасширенийПрограммСтрахования = ТаблицаУдаленныхРасширенийПрограммСтрахования.Найти(СтрокаТаблицы.Идентификатор, "Идентификатор");
			Если СтрокаТаблицыРасширенийПрограммСтрахования <> Неопределено Тогда
				ОбработатьСтрокуТаблицыРасширенийПрограммСтрахования(СтрокаСотрудник, СтрокаТаблицыРасширенийПрограммСтрахования, СписокНеоднозначностей, СтрокаТаблицы.Идентификатор);	
			КонецЕсли;
		КонецЕсли;
		
	КонецЦикла;
	
	ПоместитьВоВременноеХранилище(Сотрудники, АдресТаблицыСопоставления);
	
КонецПроцедуры

Процедура ОбработатьСтрокуТаблицыПрограммСтрахования(СтрокаСотрудник, СтрокаТаблицыПрограммСтрахования, СписокНеоднозначностей, Идентификатор)
	
	Если СтрокаТаблицыПрограммСтрахования.Количество = 1 Тогда 
		СтрокаСотрудник.ПрограммаСтрахования = СтрокаТаблицыПрограммСтрахования.ПрограммаСтрахования;
	ИначеЕсли СтрокаТаблицыПрограммСтрахования.Количество > 1 Тогда
		ЗаписьОНеоднозначности = СписокНеоднозначностей.Добавить();
		ЗаписьОНеоднозначности.Идентификатор = Идентификатор; 
		ЗаписьОНеоднозначности.Колонка = "ПрограммаСтрахования";
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработатьСтрокуТаблицыРасширенийПрограммСтрахования(СтрокаСотрудник, СтрокаТаблицыРасширенийПрограммСтрахования, СписокНеоднозначностей, Идентификатор)
	
	Если СтрокаТаблицыРасширенийПрограммСтрахования.Количество = 1 Тогда 
		СтрокаСотрудник.РасширениеПрограммСтрахования = СтрокаТаблицыРасширенийПрограммСтрахования.РасширениеПрограммСтрахования;
	ИначеЕсли СтрокаТаблицыРасширенийПрограммСтрахования.Количество > 1 Тогда
		ЗаписьОНеоднозначности = СписокНеоднозначностей.Добавить();
		ЗаписьОНеоднозначности.Идентификатор = Идентификатор; 
		ЗаписьОНеоднозначности.Колонка = "РасширениеПрограммСтрахования";
	КонецЕсли;
	
КонецПроцедуры

// Возвращает список подходящих объектов ИБ для неоднозначного значения ячейки.
// 
// Параметры:
//   ПолноеИмяТабличнойЧасти  - Строка - Полное имя табличной части, в которую загружаются данные.
//  ИмяКолонки                - Строка - Имя колонки, в который возникла неоднозначность.
//  СписокНеоднозначностей    - Массив - Массив для заполнения с неоднозначными данными.
//  ЗагружаемыеЗначенияСтрока - Строка - Загружаемые данные на основании которых возникла неоднозначность.
//  ДополнительныеПараметры   - ЛюбойТип - Любые дополнительные сведения.
//
Процедура ЗаполнитьСписокНеоднозначностей(ПолноеИмяТабличнойЧасти, СписокНеоднозначностей, ИмяКолонки, ЗагружаемыеЗначенияСтрока, ДополнительныеПараметры) Экспорт
	
	Если ИмяКолонки = "Сотрудник" Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	Сотрудники.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.Сотрудники КАК Сотрудники
		|ГДЕ
		|	Сотрудники.Наименование = &Наименование";
		
		Запрос.УстановитьПараметр("Наименование", ЗагружаемыеЗначенияСтрока.Сотрудник);
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
		Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
			СписокНеоднозначностей.Добавить(ВыборкаДетальныеЗаписи.Ссылка);
		КонецЦикла;
		
	ИначеЕсли ИмяКолонки = "ПрограммаСтрахования" Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПрограммыМедицинскогоСтрахования.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.ПрограммыМедицинскогоСтрахования КАК ПрограммыМедицинскогоСтрахования
		|ГДЕ
		|	ПрограммыМедицинскогоСтрахования.Наименование = &Наименование";
		
		Запрос.УстановитьПараметр("Наименование", ЗагружаемыеЗначенияСтрока.ПрограммаСтрахования);
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
		Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
			СписокНеоднозначностей.Добавить(ВыборкаДетальныеЗаписи.Ссылка);
		КонецЦикла;
		
	ИначеЕсли ИмяКолонки = "РасширениеПрограммСтрахования" Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасширенияПрограммМедицинскогоСтрахования.Ссылка КАК Ссылка
		|ИЗ
		|	Справочник.РасширенияПрограммМедицинскогоСтрахования КАК РасширенияПрограммМедицинскогоСтрахования
		|ГДЕ
		|	РасширенияПрограммМедицинскогоСтрахования.Наименование = &Наименование";
		
		Запрос.УстановитьПараметр("Наименование", ЗагружаемыеЗначенияСтрока.РасширениеПрограммСтрахования);
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
		Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
			СписокНеоднозначностей.Добавить(ВыборкаДетальныеЗаписи.Ссылка);
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

// См. ЗарплатаКадрыСоставДокументов.ОписаниеСоставаОбъекта
Функция ОписаниеСоставаОбъекта() Экспорт
	
	МетаданныеДокумента = Метаданные.Документы.ПрикреплениеКПрограммамМедицинскогоСтрахования;
	Возврат ЗарплатаКадрыСоставДокументов.ОписаниеСоставаОбъектаПоМетаданнымФизическиеЛицаВТабличныхЧастях(МетаданныеДокумента);
	
КонецФункции

#КонецОбласти

#КонецЕсли
