#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// СтандартныеПодсистемы.ВерсионированиеОбъектов

// Определяет настройки объекта для подсистемы ВерсионированиеОбъектов.
//
// Параметры:
//  Настройки - Структура - настройки подсистемы.
Процедура ПриОпределенииНастроекВерсионированияОбъектов(Настройки) Экспорт

КонецПроцедуры

// Конец СтандартныеПодсистемы.ВерсионированиеОбъектов


#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// См. УправлениеДоступомПереопределяемый.ПриЗаполненииСписковСОграничениемДоступа.
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт
	Ограничение.Текст =
	"РазрешитьЧтениеИзменение
	|ГДЕ
	|	ДляВсехСтрок( ЗначениеРазрешено(ФизическиеЛица.ФизическоеЛицо, NULL КАК ИСТИНА)
	|	) И ЗначениеРазрешено(Организация)";
КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#Область Печать

// Заполняет список команд печати.
// 
// Параметры:
//   КомандыПечати - ТаблицаЗначений - состав полей см. в функции УправлениеПечатью.СоздатьКоллекциюКомандПечати.
//
Процедура ДобавитьКомандыПечати(КомандыПечати) Экспорт

	КомандаПечати = КомандыПечати.Добавить();
	КомандаПечати.Идентификатор = "ПФ_MXL_СправкаБухучетНачислений";
	КомандаПечати.Представление = НСтр("ru = 'Справка о регистрации бухучета начислений сотрудников'");
	КомандаПечати.МенеджерПечати = "Документ.БухучетНачисленийСотрудников";
	КомандаПечати.Обработчик = "УправлениеПечатьюБЗККлиент.ВыполнитьКомандуПечати";
	КомандаПечати.ПроверкаПроведенияПередПечатью = Истина;
	
КонецПроцедуры

// Формирует печатные формы
//
// Параметры:
//  (входные)
//    МассивОбъектов  - Массив    - ссылки на объекты, которые нужно распечатать;
//    ПараметрыПечати - Структура - дополнительные настройки печати;
//  (выходные)
//   КоллекцияПечатныхФорм - ТаблицаЗначений - сформированные табличные документы.
//   ОбъектыПечати         - СписокЗначений  - значение - ссылка на объект;
//                                             представление - имя области в которой был выведен объект;
//   ПараметрыВывода       - Структура       - дополнительные параметры сформированных табличных документов.
//
Процедура Печать(МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм, ОбъектыПечати, ПараметрыВывода) Экспорт
	
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ПФ_MXL_СправкаБухучетНачислений") Тогда
		
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
			"ПФ_MXL_СправкаБухучетНачислений",
			НСтр("ru = 'Справка о регистрации бухучета начислений сотрудников'"),
			ПечатьСправкиОРегистрацииБухучета(МассивОбъектов, ОбъектыПечати),
			,
			"Документ.БухучетНачисленийСотрудников.ПФ_MXL_СправкаБухучетНачислений");
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

Функция ПечатьСправкиОРегистрацииБухучета(МассивОбъектов, ОбъектыПечати)
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.КлючПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_БухучетНачисленийСотрудников_СправкаБухучетНачислений";
	ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	Макет = УправлениеПечатью.МакетПечатнойФормы("Документ.БухучетНачисленийСотрудников.ПФ_MXL_СправкаБухучетНачислений");
	
	ПодбираетсяАвтоматически = НСтр("ru = '<подбирается автоматически>'");
	ИспользоватьСтатьиФинансирования = ПолучитьФункциональнуюОпцию("ИспользоватьСтатьиФинансированияЗарплатаРасширенный");
	РаботаВБюджетномУчреждении = ПолучитьФункциональнуюОпцию("РаботаВБюджетномУчреждении");

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("МассивОбъектов", МассивОбъектов);
	Запрос.УстановитьПараметр("ИспользоватьСтатьиФинансирования", ИспользоватьСтатьиФинансирования);
	Запрос.УстановитьПараметр("РаботаВБюджетномУчреждении", РаботаВБюджетномУчреждении);
	Запрос.УстановитьПараметр("ПодбираетсяАвтоматически", ПодбираетсяАвтоматически);
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Сотрудники.Ссылка КАК Ссылка,
	|	Сотрудники.Ссылка.Организация КАК Организация,
	|	Сотрудники.Ссылка.Подразделение КАК Подразделение,
	|	Сотрудники.Ссылка.Организация.НаименованиеПолное КАК НазваниеОрганизации,
	|	Сотрудники.Сотрудник КАК Сотрудник,
	|	Сотрудники.Ссылка.Дата КАК Дата,
	|	Сотрудники.Ссылка.Номер КАК Номер,
	|	Сотрудники.Сотрудник.Наименование КАК СотрудникНаименование,
	|	Сотрудники.НомерСтроки КАК НомерСтроки,
	|	ВЫБОР
	|		КОГДА &ИспользоватьСтатьиФинансирования
	|			ТОГДА ВЫБОР
	|					КОГДА Сотрудники.Ссылка.БухучетУказываетсяРаспределением
	|						ТОГДА ВЫБОР
	|								КОГДА СтрокиРаспределения.СтатьяФинансирования = ЗНАЧЕНИЕ(Справочник.СтатьиФинансированияЗарплата.ПустаяСсылка)
	|										ИЛИ СтрокиРаспределения.СтатьяФинансирования ЕСТЬ NULL
	|									ТОГДА &ПодбираетсяАвтоматически
	|								ИНАЧЕ СтрокиРаспределения.СтатьяФинансирования
	|							КОНЕЦ
	|					ИНАЧЕ ВЫБОР
	|							КОГДА Сотрудники.СтатьяФинансирования = ЗНАЧЕНИЕ(Справочник.СтатьиФинансированияЗарплата.ПустаяСсылка)
	|								ТОГДА &ПодбираетсяАвтоматически
	|							ИНАЧЕ Сотрудники.СтатьяФинансирования
	|						КОНЕЦ
	|				КОНЕЦ
	|		ИНАЧЕ """"
	|	КОНЕЦ КАК СтатьяФинансирования,
	|	ВЫБОР
	|		КОГДА &РаботаВБюджетномУчреждении
	|			ТОГДА ВЫБОР
	|					КОГДА Сотрудники.Ссылка.БухучетУказываетсяРаспределением
	|						ТОГДА ВЫБОР
	|								КОГДА СтрокиРаспределения.СтатьяРасходов = ЗНАЧЕНИЕ(Справочник.СтатьиРасходовЗарплата.ПустаяСсылка)
	|										ИЛИ СтрокиРаспределения.СтатьяРасходов ЕСТЬ NULL
	|									ТОГДА &ПодбираетсяАвтоматически
	|								ИНАЧЕ СтрокиРаспределения.СтатьяРасходов
	|							КОНЕЦ
	|					ИНАЧЕ ВЫБОР
	|							КОГДА Сотрудники.СтатьяРасходов = ЗНАЧЕНИЕ(Справочник.СтатьиРасходовЗарплата.ПустаяСсылка)
	|								ТОГДА &ПодбираетсяАвтоматически
	|							ИНАЧЕ Сотрудники.СтатьяРасходов
	|						КОНЕЦ
	|				КОНЕЦ
	|		ИНАЧЕ """"
	|	КОНЕЦ КАК СтатьяРасходов,
	|	ВЫБОР
	|		КОГДА Сотрудники.Ссылка.БухучетУказываетсяРаспределением
	|			ТОГДА ВЫБОР
	|					КОГДА СтрокиРаспределения.СпособОтраженияЗарплатыВБухучете = ЗНАЧЕНИЕ(Справочник.СпособыОтраженияЗарплатыВБухучете.ПустаяСсылка)
	|							ИЛИ СтрокиРаспределения.СпособОтраженияЗарплатыВБухучете ЕСТЬ NULL
	|						ТОГДА &ПодбираетсяАвтоматически
	|					ИНАЧЕ СтрокиРаспределения.СпособОтраженияЗарплатыВБухучете
	|				КОНЕЦ
	|		ИНАЧЕ ВЫБОР
	|				КОГДА Сотрудники.СпособОтраженияЗарплатыВБухучете = ЗНАЧЕНИЕ(Справочник.СпособыОтраженияЗарплатыВБухучете.ПустаяСсылка)
	|					ТОГДА &ПодбираетсяАвтоматически
	|				ИНАЧЕ Сотрудники.СпособОтраженияЗарплатыВБухучете
	|			КОНЕЦ
	|	КОНЕЦ КАК СпособОтражения,
	|	ЕСТЬNULL(СтрокиРаспределения.ДоляРаспределения, 0) КАК Доля,
	|	ЕСТЬNULL(СтрокиРаспределения.НомерСтроки, 0) КАК НомерСтрокиРаспределения,
	|	Сотрудники.Ссылка.БухучетУказываетсяРаспределением КАК БухучетУказываетсяРаспределением,
	|	Сотрудники.Начисление КАК Начисление,
	|	ВЫБОР
	|		КОГДА Сотрудники.Ссылка.РазныеДатыДляСотрудников
	|			ТОГДА Сотрудники.ДатаНачала
	|		ИНАЧЕ Сотрудники.Ссылка.ДатаНачала
	|	КОНЕЦ КАК ДатаНачала,
	|	ВЫБОР
	|		КОГДА Сотрудники.Ссылка.РазныеДатыДляСотрудников
	|			ТОГДА Сотрудники.ДатаОкончания
	|		ИНАЧЕ Сотрудники.Ссылка.ДатаОкончания
	|	КОНЕЦ КАК ДатаОкончания
	|ИЗ
	|	Документ.БухучетНачисленийСотрудников.Сотрудники КАК Сотрудники
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.БухучетНачисленийСотрудников.БухучетНачислений КАК СтрокиРаспределения
	|		ПО Сотрудники.ИдентификаторСтрокиСотрудника = СтрокиРаспределения.ИдентификаторСтрокиСотрудника
	|			И Сотрудники.Ссылка = СтрокиРаспределения.Ссылка
	|			И (Сотрудники.Ссылка.БухучетУказываетсяРаспределением)
	|ГДЕ
	|	Сотрудники.Ссылка В(&МассивОбъектов)
	|
	|УПОРЯДОЧИТЬ ПО
	|	Ссылка,
	|	НомерСтроки,
	|	НомерСтрокиРаспределения";

	Выборка = Запрос.Выполнить().Выбрать();
	
	ПервыйДокумент = Истина;
	
	ОбластьШапка = Макет.ПолучитьОбласть("Шапка");
	ДанныеШапки = Новый Структура("НазваниеОрганизации,Подразделение,ДатаДокумента,НомерДокумента");
	
	ЗаголовокБухчета = НСтр("ru = 'Способ отражения'");
	Если ИспользоватьСтатьиФинансирования И РаботаВБюджетномУчреждении Тогда
		ЗаголовокБухчета = НСтр("ru = 'Статья финансирования / Способ отражения / Статья расходов'");
	ИначеЕсли ИспользоватьСтатьиФинансирования Тогда
		ЗаголовокБухчета = НСтр("ru = 'Статья финансирования / Способ отражения'");
	КонецЕсли;
	
	Пока Выборка.СледующийПоЗначениюПоля("Ссылка") Цикл
		
		Если Не ПервыйДокумент Тогда
			// Все документы нужно выводить на разных страницах.
			ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
		ПервыйДокумент = Ложь;
		
		НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
		
		ДанныеШапки.НазваниеОрганизации = Выборка.НазваниеОрганизации;
		ДанныеШапки.Подразделение 		= Выборка.Подразделение;
		ДанныеШапки.ДатаДокумента 		= Формат(Выборка.Дата, "ДЛФ=D");
		ДанныеШапки.НомерДокумента 		= ПрефиксацияОбъектовКлиентСервер.НомерНаПечать(Выборка.Номер, Истина, Истина);

		ЗаполнитьЗначенияСвойств(ОбластьШапка.Параметры, ДанныеШапки);
		ТабличныйДокумент.Вывести(ОбластьШапка);
		
		Если Выборка.БухучетУказываетсяРаспределением Тогда
			ЗаголовокТаблицы = Макет.ПолучитьОбласть("ЗаголовокТаблицы");
			ОбластьСтрока = Макет.ПолучитьОбласть("Строка");
		Иначе
			ЗаголовокТаблицы = Макет.ПолучитьОбласть("ЗаголовокТаблицы|ЗаголовокОбщий");
			ОбластьСтрока = Макет.ПолучитьОбласть("Строка|ЗаголовокОбщий");
		КонецЕсли;
		ОбластьДопСтрока = Макет.ПолучитьОбласть("ДопСтрока");
		
		ЗаголовокТаблицы.Параметры.Бухучет = ЗаголовокБухчета;
		ТабличныйДокумент.Вывести(ЗаголовокТаблицы);
		
		НомерСтроки = 1;
		Пока Выборка.СледующийПоЗначениюПоля("НомерСтроки") Цикл
			
			ЗаполнитьЗначенияСвойств(ОбластьСтрока.Параметры, Выборка);
			Если ИспользоватьСтатьиФинансирования И РаботаВБюджетномУчреждении Тогда
				Бухучет = СтрШаблон("%1 / %2 / %3", Выборка.СтатьяФинансирования, Выборка.СпособОтражения, Выборка.СтатьяРасходов);
			ИначеЕсли ИспользоватьСтатьиФинансирования Тогда
				Бухучет = СтрШаблон("%1 / %2", Выборка.СтатьяФинансирования,Выборка.СпособОтражения);
			Иначе
				Бухучет = Выборка.СпособОтражения;
			КонецЕсли;
			ОбластьСтрока.Параметры.Бухучет = Бухучет;
			ТабличныйДокумент.Вывести(ОбластьСтрока);
			
			Если Выборка.БухучетУказываетсяРаспределением Тогда
				ПерваяСтрока = Истина;
				Пока Выборка.Следующий() Цикл
					Если ПерваяСтрока Тогда
						ПерваяСтрока = Ложь;
						Продолжить;
					КонецЕсли;
					ЗаполнитьЗначенияСвойств(ОбластьДопСтрока.Параметры, Выборка);
					Если ИспользоватьСтатьиФинансирования И РаботаВБюджетномУчреждении Тогда
						Бухучет = СтрШаблон("%1 / %2 / %3", Выборка.СтатьяФинансирования,Выборка.СпособОтражения,Выборка.СтатьяРасходов);
					ИначеЕсли ИспользоватьСтатьиФинансирования Тогда
						Бухучет = СтрШаблон("%1 / %2", Выборка.СтатьяФинансирования,Выборка.СпособОтражения);
					Иначе
						Бухучет = Выборка.СпособОтражения;
					КонецЕсли;
					ОбластьДопСтрока.Параметры.Бухучет = Бухучет;
					ТабличныйДокумент.Вывести(ОбластьДопСтрока);
				КонецЦикла;
			КонецЕсли;
			
			НомерСтроки = НомерСтроки + 1;
			
		КонецЦикла;
		
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НомерСтрокиНачало, ОбъектыПечати, Выборка.Ссылка);
		
	КонецЦикла;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#Область ОбработчикиПравилРегистрации

// См. ЗарплатаКадрыРасширенныйСинхронизацияДанных.ШаблонОбработчика
Процедура ПриЗаполненииНастроекОбработчиковПравилРегистрации(Настройки) Экспорт
	ЗарплатаКадрыРасширенныйСинхронизацияДанных.ДляОтбораПоОрганизации(Настройки);
	ЗарплатаКадрыРасширенныйСинхронизацияДанных.ДляОбъектаСПрисоединеннымиФайлами(Настройки);
КонецПроцедуры

#КонецОбласти

// Возвращает описание состава документа
//
// Возвращаемое значение:
//  Структура - см. ЗарплатаКадрыСоставДокументов.НовоеОписаниеСоставаОбъекта.
Функция ОписаниеСоставаОбъекта() Экспорт
	
	МетаданныеДокумента = Метаданные.Документы.БухучетНачисленийСотрудников;
	Возврат ЗарплатаКадрыСоставДокументов.ОписаниеСоставаОбъектаПоМетаданнымФизическиеЛицаВТабличныхЧастях(МетаданныеДокумента);
	
КонецФункции

#КонецОбласти


#КонецЕсли