
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры, "Заголовок,ФизическоеЛицоСсылка,ИзФормыСотрудника");
	
	ДатаСведений = ТекущаяДатаСеанса();
	ОсновныеСотрудники = КадровыйУчет.ОсновныеСотрудникиИнформационнойБазы(Истина, ФизическоеЛицоСсылка, ДатаСведений);
	
	СотрудникСсылка = ОсновныеСотрудники[ФизическоеЛицоСсылка];
	Если ЗначениеЗаполнено(СотрудникСсылка) Тогда
		КадровыеДанные = КадровыйУчет.КадровыеДанныеСотрудника(Истина, СотрудникСсылка, "Организация", ДатаСведений);
		Если КадровыеДанные = Неопределено Тогда
			ОрганизацияСсылка = КадровыеДанные.Организация;
		КонецЕсли;
	КонецЕсли;
	
	СотрудникиФормы.ПрочитатьДанныеИзХранилищаВФорму(
		ЭтотОбъект,
		СотрудникиКлиентСервер.ОписаниеДополнительнойФормы(ИмяФормы),
		Параметры.АдресВХранилище);
		
	ВидыДокументовВоинскогоУчета = ОбщегоНазначения.ОбщийМодуль("ВоинскийУчет").ВидыДокументовВоинскогоУчета();
	
	ПараметрыВыбора = Новый Массив;
	ПараметрыВыбора.Добавить(Новый ПараметрВыбора("Отбор.Ссылка", Новый ФиксированныйМассив(ВидыДокументовВоинскогоУчета)));
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
		"ДокументыФизическихЛицВидДокумента", "ПараметрыВыбора", Новый ФиксированныйМассив(ПараметрыВыбора));
		
	ПроинициализироватьФорму();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	СотрудникиКлиент.ЗарегистрироватьОткрытиеФормы(ЭтотОбъект, ИмяФормы);
	СотрудникиКлиент.ПроверитьРежимТолькоПросмотраДополнительнойФормы(ЭтотОбъект, ИзФормыСотрудника);
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	Оповещение = Новый ОписаниеОповещения("СохранитьИЗакрыть", ЭтотОбъект);
	ОбщегоНазначенияКлиент.ПоказатьПодтверждениеЗакрытияФормы(Оповещение, Отказ, ЗавершениеРаботы, ТекстПредупреждения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	СотрудникиКлиент.ОбработкаОповещения(ЭтотОбъект, ИмяСобытия, Параметр, Источник);
	
	Если ИмяСобытия = "ОтредактированаИстория" И ФизическоеЛицоСсылка = Источник Тогда
		Если Параметр.ИмяРегистра = "ВоинскийУчет" Тогда
			Если ВоинскийУчетНаборЗаписейПрочитан Тогда
				РедактированиеПериодическихСведенийКлиент.ОбработкаОповещения(
					ЭтотОбъект,
					ФизическоеЛицоСсылка,
					ИмяСобытия,
					Параметр,
					Источник);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Если ИмяСобытия = "Запись_СверкаСДокументамиВоинскогоУчета" И Не ТолькоПросмотр Тогда 
		ЗаполнитьДанныеСверкиСДокументамиВоинскогоУчета();	
	КонецЕсли;

	Если ИмяСобытия = "Запись_СверкаСВоеннымКомиссариатом" И Не ТолькоПросмотр Тогда 
		ЗаполнитьДанныеСверкиСВоеннымКомиссариатом();	
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОбработкаПроверкиЗаполненияНаСервере(Отказ, ПроверяемыеРеквизиты)
	
	РедактированиеПериодическихСведений.ПроверитьЗаписьВФорме(ЭтотОбъект, "ВоинскийУчет", ФизическоеЛицоСсылка, Отказ);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВоинскийУчетОтношениеКВоинскойОбязанностиПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	ОтношениеКВоинскойОбязанностиПриИзмененииНаСервере();
	ПроверитьСоответствиеДанныхУчета();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетОтношениеКВоинскойОбязанностиНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетКатегорияЗапасаПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетКатегорияЗапасаНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетЗваниеПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	ВоинскийУчетЗваниеПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетЗваниеНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетСоставПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	ВоинскийУчетСоставПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетСоставНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетВУСПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетГодностьПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетГодностьНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетВоенкоматПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетВоенкоматНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетОтношениеКВоинскомуУчетуПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	ВоинскийУчетОтношениеКВоинскомуУчетуПриИзмененииНаСервере();
	ПроверитьСоответствиеДанныхУчета();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетДатаСнятияСВоинскогоУчетаПриИзменении(Элемент)
	
	ЗаполнитьПредполагаемуюДатуСнятияСВоинскогоУчета();
	УстановитьСвойстваЭлементовФормы();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетОтношениеКВоинскомуУчетуНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетНаличиеМобпредписанияПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	ВоинскийУчетНаличиеМобпредписанияПриИзмененииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетНомерКомандыПартииПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетЗабронированОрганизациейПриИзменении(Элемент)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ОбщегоНазначенияКлиент.ДатаСеанса());

КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетЗабронированОрганизациейНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетПериодПриИзменении(Элемент)
	
	ВоинскийУчетЗаблокироватьФизическоеЛицо();
	
КонецПроцедуры

&НаКлиенте
Процедура ВоинскийУчетПериодНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Если Не ВоинскийУчетЗаблокироватьФизическоеЛицо() Тогда
		СтандартнаяОбработка = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДокументыФизическихЛицВидДокументаПриИзменении(Элемент)
	
	Если ЗначениеЗаполнено(ДокументыФизическихЛиц.ВидДокумента) 
		И Не ЗначениеЗаполнено(ДокументыФизическихЛиц.Период) Тогда 
		ДокументыФизическихЛиц.Период = ОбщегоНазначенияКлиент.ДатаСеанса();
	КонецЕсли;
	
	ДокументыФизическихЛицВидДокументаПриИзмененииНаСервере();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВоинскийУчетИстория(Команда)

	СотрудникиКлиент.ОткрытьФормуРедактированияИстории("ВоинскийУчет", ФизическоеЛицоСсылка, ЭтотОбъект);

КонецПроцедуры

&НаКлиенте
Процедура ВсеДокументы(Команда)
	
	СотрудникиКлиент.ОткрытьСписокВсехДокументовФизическогоЛица(ЭтотОбъект, ФизическоеЛицоСсылка);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьИзвещениеОПриемеИлиУвольнении(Команда)
	
	ДатаОкончания = ОбщегоНазначенияКлиент.ДатаСеанса() + 86400 * 3;
	ДатаНачала = ДатаОкончания - 86400 * 7;
	
	ЗначенияЗаполнения = Новый Структура;
	ЗначенияЗаполнения.Вставить("Организация", ОрганизацияСсылка);
	ЗначенияЗаполнения.Вставить("НачалоПериода", ДатаНачала);
	ЗначенияЗаполнения.Вставить("ОкончаниеПериода", ДатаОкончания);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
	ПараметрыФормы.Вставить("ФизическоеЛицо", ФизическоеЛицоСсылка);
	ПараметрыФормы.Вставить("ЗаполнитьПриОткрытии", Истина);
	
	ОткрытьФорму("Документ.ИзвещениеОПриемеИлиУвольненииДляВоенкомата.ФормаОбъекта", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьЛистокСообщенияОбИзменениях(Команда)
	
	ЗначенияЗаполнения = Новый Структура;
	ЗначенияЗаполнения.Вставить("Организация", ОрганизацияСсылка);
	ЗначенияЗаполнения.Вставить("Сотрудник", СотрудникСсылка);
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("ЗначенияЗаполнения", ЗначенияЗаполнения);
	
	ОткрытьФорму("Документ.ЛистокСообщенияДляВоенкомата.ФормаОбъекта", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура СверкаСДокументамиВоинскогоУчетаСоздать(Команда)
	
	Если Не ЗначениеЗаполнено(ФизическоеЛицоСсылка) Тогда 
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("Не заполнено физическое лицо."));
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Основание", ФизическоеЛицоСсылка);
	
	ОткрытьФорму("Документ.СверкаСДокументамиВоинскогоУчета.ФормаОбъекта", ПараметрыФормы, ЭтаФорма);
	
КонецПроцедуры

&НаКлиенте
Процедура СверкаСВоеннымКомиссариатомСоздать(Команда)
	
	Если Не ЗначениеЗаполнено(ФизическоеЛицоСсылка) Тогда 
		ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("Не заполнено физическое лицо."));
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить("Основание", ФизическоеЛицоСсылка);
	
	ОткрытьФорму("Документ.СверкаСВоеннымКомиссариатом.ФормаОбъекта", ПараметрыФормы, ЭтаФорма);
	
КонецПроцедуры

&НаКлиенте
Процедура ВсеДокументыВоинскогоУчета(Команда)
	
	ПараметрыФормы = Новый Структура("ФизическоеЛицоСсылка", ФизическоеЛицоСсылка);
	ОткрытьФорму("ЖурналДокументов.ДокументыВоинскогоУчета.ФормаСписка", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура ЛисткиСообщения(Команда)
	
	ПараметрыФормы = Новый Структура("ФизическоеЛицоСсылка", ФизическоеЛицоСсылка);
	ОткрытьФорму("Документ.ЛистокСообщенияДляВоенкомата.ФормаСписка", ПараметрыФормы);
	
КонецПроцедуры

&НаКлиенте
Процедура Ок(Команда)
	
	СохранитьИЗакрытьНаКлиенте();
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Модифицированность = Ложь;
	Закрыть();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ПроинициализироватьФорму()
	
	Если ВоинскийУчетПрежняя = Неопределено Тогда
		РедактированиеПериодическихСведений.ПрочитатьЗаписьДляРедактированияВФорме(ЭтотОбъект, "ВоинскийУчет", ФизическоеЛицоСсылка);
	КонецЕсли;
	
	ОбновитьПолеВоинскийУчетПериод(ЭтотОбъект, ТекущаяДатаСеанса());
	ОбновитьПолеЗабронированОрганизацией();
	ОбновитьДокументВоинскогоУчета();
	
	ЗаполнитьДанныеСверкиСДокументамиВоинскогоУчета();
	ЗаполнитьДанныеСверкиСВоеннымКомиссариатом();
	ЗаполнитьПредполагаемуюДатуСнятияСВоинскогоУчета();
	ЗаполнитьТекстПредупрежденияПользователю();
	
	УстановитьСвойстваЭлементовФормы();
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьИЗакрыть(Результат, ДополнительныеПараметры) Экспорт 
	
	СохранитьИЗакрытьНаКлиенте();
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьИЗакрытьНаКлиенте(ЗакрытьФорму = Истина) Экспорт 

	Если НЕ ТолькоПросмотр Тогда
		ТекущийЭлемент = Элементы.ФормаОк;
	КонецЕсли; 
	
	ДополнительныеПараметры = Новый Структура("ЗакрытьФорму", ЗакрытьФорму);
	
	Оповещение = Новый ОписаниеОповещения("СохранитьИЗакрытьНаКлиентеЗавершение", ЭтотОбъект, ДополнительныеПараметры);
	СохранитьДанные(Ложь, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьДанные(Отказ, ОповещениеЗавершения = Неопределено) Экспорт
	
	Если Не Модифицированность Тогда
		Если ОповещениеЗавершения <> Неопределено Тогда 
			ВыполнитьОбработкуОповещения(ОповещениеЗавершения, Отказ);
		КонецЕсли;
		Возврат;
	КонецЕсли; 
		
	ЗапроситьРежимИзмененияВоинскийУчет(ВоинскийУчет.Период, Отказ, ОповещениеЗавершения);

КонецПроцедуры

&НаКлиенте
Процедура ЗапроситьРежимИзмененияВоинскийУчет(ДатаИзменения, Отказ, ОповещениеЗавершения)
	
	ТекстКнопкиДа = НСтр("ru = 'Изменились сведения о воинском учете'");
	ТекстВопроса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru =  'При редактировании изменены сведения о воинском учете.
						|Если исправлена прежняя запись о воинском учете (она была ошибочной), нажмите ""Исправлена ошибка"".
						|Если у сотрудника изменились сведения с %1, нажмите'") + " ""%2""",
			Формат(ДатаИзменения, "ДФ='д ММММ гггг ""г""'"),
			ТекстКнопкиДа);
			
	Оповещение = Новый ОписаниеОповещения("ЗапроситьРежимИзмененияДокументВоинскогоУчета", ЭтотОбъект, ОповещениеЗавершения);		
	РедактированиеПериодическихСведенийКлиент.ЗапроситьРежимИзмененияРегистра(ЭтотОбъект, "ВоинскийУчет", ТекстВопроса, ТекстКнопкиДа, Отказ, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗапроситьРежимИзмененияДокументВоинскогоУчета(Результат, ОповещениеЗавершения) Экспорт 
	
	Отказ = Результат = Неопределено;
	Если Отказ Тогда 
		Возврат;
	КонецЕсли;
	
	ТекстКнопкиДа = НСтр("ru = 'Изменились сведения'");
	ТекстВопроса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru =  'При редактировании изменены сведения о документе воинского учета.
						|Если исправлена прежняя запись (она была ошибочной), нажмите ""Исправлена ошибка"".
						|Если у сотрудника изменились сведения с %1, нажмите'") + " ""%2""",
			Формат(ДокументыФизическихЛиц.Период, "ДФ='д ММММ гггг ""г""'"),
			ТекстКнопкиДа);
			
	Оповещение = Новый ОписаниеОповещения("СохранитьДанныеЗавершение", ЭтотОбъект, ОповещениеЗавершения);		
	РедактированиеПериодическихСведенийКлиент.ЗапроситьРежимИзмененияРегистра(ЭтотОбъект, "ДокументыФизическихЛиц", ТекстВопроса, ТекстКнопкиДа, Отказ, Оповещение);
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьДанныеЗавершение(Отказ, ОповещениеЗавершения) Экспорт  
	
	Если Не Отказ Тогда
		
		Если ПроверитьЗаполнение() Тогда
			
			ВозвращаемыйПараметр = Новый Структура;
			ВозвращаемыйПараметр.Вставить("ИмяФормы", ИмяФормы);
			ВозвращаемыйПараметр.Вставить("АдресВХранилище", АдресДанныхДополнительнойФормыНаСервере(СотрудникиКлиентСервер.ОписаниеДополнительнойФормы(ИмяФормы)));
			
			Оповестить(
				"ИзмененыДанныеДополнительнойФормы",
				ВозвращаемыйПараметр,
				ВладелецФормы);
			
		Иначе
			Отказ = Истина;
		КонецЕсли;
		
	КонецЕсли; 
	
	Если ОповещениеЗавершения <> Неопределено Тогда 
		ВыполнитьОбработкуОповещения(ОповещениеЗавершения, Отказ);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьИЗакрытьНаКлиентеЗавершение(Отказ, ДополнительныеПараметры) Экспорт 

	Если Не Отказ И Открыта() Тогда
		
		Модифицированность = Ложь;
		Если ДополнительныеПараметры.ЗакрытьФорму Тогда
			Закрыть();
		КонецЕсли; 
		
	КонецЕсли; 
	
КонецПроцедуры

&НаСервере
Процедура ПрочитатьНаборЗаписейПериодическихСведений(ИмяРегистра, ВедущийОбъект) Экспорт
	
	РедактированиеПериодическихСведений.ПрочитатьНаборЗаписей(ЭтотОбъект, ИмяРегистра, ВедущийОбъект);
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьПолеЗабронированОрганизацией()

	СтруктураОтбора = Новый Структура;
	СтруктураОтбора.Вставить("ФизическоеЛицо", ФизическоеЛицоСсылка);
	
	ДанныеБронирования = РегистрыСведений.БронированиеСотрудников.СрезПоследних(, СтруктураОтбора);
	Если ДанныеБронирования.Количество() = 0 Тогда 
		Возврат;
	КонецЕсли;
	
	ЗабронированОрганизацией = ДанныеБронирования[0].Организация;
	Если ТипЗнч(ДанныеБронирования[0].Регистратор) = Тип("ДокументСсылка.ПриказОБронированииРаботников") Тогда 
		ПриказОБронировании = ДанныеБронирования[0].Регистратор;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ОбновитьПолеВоинскийУчетПериод(Форма, ДатаСеанса) Экспорт
	// Не обязательно заполнение поля Период если данные по умолчанию.
	Если ЗарплатаКадрыРасширенныйКлиентСервер.ВоинскийУчетПоУмолчанию(Форма.ВоинскийУчет) И 
		НЕ ЗначениеЗаполнено(Форма.ВоинскийУчетПрежняя.Период) Тогда
		Если ЗначениеЗаполнено(Форма.ВоинскийУчет.Период) Тогда
			Форма.ВоинскийУчет.Период = '00010101';
		КонецЕсли; 
	Иначе
		Если НЕ ЗначениеЗаполнено(Форма.ВоинскийУчет.Период) Тогда
			Форма.ВоинскийУчет.Период = ДатаСеанса;
		КонецЕсли; 
	КонецЕсли;
	
	РедактированиеПериодическихСведенийКлиентСервер.ОбновитьОтображениеПолейВвода(Форма, "ВоинскийУчет", Форма.ФизическоеЛицоСсылка);
	
КонецПроцедуры

&НаСервере
Функция АдресДанныхДополнительнойФормыНаСервере(ОписаниеДополнительнойФормы)
	
	СохранитьДанныеДокументаВоинскогоУчета();
	
	Возврат СотрудникиФормы.АдресДанныхДополнительнойФормы(ОписаниеДополнительнойФормы, ЭтотОбъект);
	
КонецФункции

&НаСервере
Процедура СохранитьДанныеДокументаВоинскогоУчета()
	
	КлючевыеСвойства = СтрРазделить("Период,ВидДокумента", ",");
	
	КлючевыеСвойстваЗаполнены = Истина;
	Для Каждого КлючевоеСвойство Из КлючевыеСвойства Цикл 
		Если Не ЗначениеЗаполнено(ДокументыФизическихЛиц[КлючевоеСвойство]) Тогда 
			КлючевыеСвойстваЗаполнены = Ложь;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ДокументыФизическихЛицНоваяЗапись Тогда
		Если КлючевыеСвойстваЗаполнены Тогда 
	        НаборЗаписей = РегистрыСведений.ДокументыФизическихЛиц.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.Физлицо.Установить(ФизическоеЛицоСсылка);
			Для Каждого КлючевоеСвойство Из КлючевыеСвойства Цикл
				НаборЗаписей.Отбор[КлючевоеСвойство].Установить(ДокументыФизическихЛиц[КлючевоеСвойство]); 
			КонецЦикла;
			НоваяЗапись = НаборЗаписей.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяЗапись, ДокументыФизическихЛиц);
			НоваяЗапись.Физлицо = ФизическоеЛицоСсылка;
			НаборЗаписей.Записать();
		КонецЕсли;
	    Возврат;
	КонецЕсли;
	
	ИзменилисьДанные = Ложь;
	Если ДокументыФизическихЛицПрежняя <> Неопределено Тогда 
		Для Каждого КлючИЗначение Из ДокументыФизическихЛицПрежняя Цикл 
			Если КлючИЗначение.Значение <> ДокументыФизическихЛиц[КлючИЗначение.Ключ] Тогда 
				ИзменилисьДанные = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если ИзменилисьДанные Тогда
		ПрежниеКлючевыеСвойстваЗаполнены = Истина;
		Для Каждого КлючевоеСвойство Из КлючевыеСвойства Цикл 
			Если Не ЗначениеЗаполнено(ДокументыФизическихЛицПрежняя[КлючевоеСвойство]) Тогда 
				ПрежниеКлючевыеСвойстваЗаполнены = Ложь;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ПрежниеКлючевыеСвойстваЗаполнены Тогда
	        НаборЗаписей = РегистрыСведений.ДокументыФизическихЛиц.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.Физлицо.Установить(ФизическоеЛицоСсылка);
			Для Каждого КлючевоеСвойство Из КлючевыеСвойства Цикл
				НаборЗаписей.Отбор[КлючевоеСвойство].Установить(ДокументыФизическихЛицПрежняя[КлючевоеСвойство]); 
			КонецЦикла;
			НаборЗаписей.Записать();
		КонецЕсли;
		Если КлючевыеСвойстваЗаполнены Тогда 
	        НаборЗаписей = РегистрыСведений.ДокументыФизическихЛиц.СоздатьНаборЗаписей();
			НаборЗаписей.Отбор.Физлицо.Установить(ФизическоеЛицоСсылка);
			Для Каждого КлючевоеСвойство Из КлючевыеСвойства Цикл
				НаборЗаписей.Отбор[КлючевоеСвойство].Установить(ДокументыФизическихЛиц[КлючевоеСвойство]); 
			КонецЦикла;
			НоваяЗапись = НаборЗаписей.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяЗапись, ДокументыФизическихЛиц);
			НоваяЗапись.Физлицо = ФизическоеЛицоСсылка;
			НаборЗаписей.Записать();
		КонецЕсли;
	КонецЕсли;	
	
КонецПроцедуры

&НаСервере
Процедура ОтношениеКВоинскойОбязанностиПриИзмененииНаСервере()
	
	ЗаполнитьПредполагаемуюДатуСнятияСВоинскогоУчета();
	УстановитьСвойстваЭлементовФормы();
	
КонецПроцедуры

&НаСервере
Процедура ВоинскийУчетЗваниеПриИзмененииНаСервере()
	
	ВоинскийУчет.Состав = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВоинскийУчет.Звание, "Состав");
	ВоинскийУчетСоставПриИзмененииНаСервере();
	ЗаполнитьПредполагаемуюДатуСнятияСВоинскогоУчета();
	УстановитьСвойстваЭлементовФормы(); 
	
КонецПроцедуры
	
&НаСервере
Процедура ВоинскийУчетСоставПриИзмененииНаСервере()

	УстановитьСвойстваЭлементовФормы();
	
	Если ЗначениеЗаполнено(ВоинскийУчет.Состав)  
		И ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВоинскийУчет.Состав, "ОфицерскийСостав") = Истина Тогда 
		ВоинскийУчет.КатегорияЗапаса = Неопределено;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ВоинскийУчетОтношениеКВоинскомуУчетуПриИзмененииНаСервере()
	
	Если ВоинскийУчет.ОтношениеКВоинскомуУчету <> Перечисления.ОтношениеКВоинскомуУчету.СнятПоВозрасту 
		И ВоинскийУчет.ОтношениеКВоинскомуУчету <> Перечисления.ОтношениеКВоинскомуУчету.СнятПоСостояниюЗдоровья Тогда 
		ВоинскийУчет.ДатаСнятияСВоинскогоУчета = Дата(1, 1, 1);
	КонецЕсли;
	
	УстановитьСвойстваЭлементовФормы();
	
КонецПроцедуры

&НаСервере
Процедура ВоинскийУчетНаличиеМобпредписанияПриИзмененииНаСервере();
	
	Если Не ВоинскийУчет.НаличиеМобпредписания Тогда 
		ВоинскийУчет.НомерКомандыПартии = "";
	КонецЕсли;
	
	УстановитьСвойстваЭлементовФормы();
	
КонецПроцедуры

&НаСервере
Процедура ДокументыФизическихЛицВидДокументаПриИзмененииНаСервере()
	
	ЗаполнитьТекстПредупрежденияПользователю();
	УстановитьСвойстваЭлементовФормы();
	
КонецПроцедуры

&НаКлиенте
Функция ВоинскийУчетЗаблокироватьФизическоеЛицо()
	
	Если Не ЗаблокироватьОбъектВФормеВладельце() Тогда
		
		ЗаполнитьЗначенияСвойств(ВоинскийУчет, ВоинскийУчетПрежняя);
		Возврат Ложь;
		
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции

&НаКлиенте
Функция ЗаблокироватьОбъектВФормеВладельце() Экспорт
	
	Возврат СотрудникиКлиент.ЗаблокироватьОбъектВФормеВладельцеДополнительнойФормы(ЭтотОбъект, ИзФормыСотрудника);
	
КонецФункции

&НаСервере
Процедура УстановитьСвойстваЭлементовФормы()
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, 
		"ВоинскийУчетДатаСнятияСВоинскогоУчета", 
		"ТолькоПросмотр", 
		ВоинскийУчет.ОтношениеКВоинскомуУчету <> Перечисления.ОтношениеКВоинскомуУчету.СнятПоВозрасту 
			И ВоинскийУчет.ОтношениеКВоинскомуУчету <> Перечисления.ОтношениеКВоинскомуУчету.СнятПоСостояниюЗдоровья);
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, 
		"ВоинскийУчетНомерКомандыПартии", 
		"ТолькоПросмотр",
		Не ВоинскийУчет.НаличиеМобпредписания);
		
	ОфицерскийСостав = Ложь;
	Если ЗначениеЗаполнено(ВоинскийУчет.Состав) Тогда 
		ОфицерскийСостав = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВоинскийУчет.Состав, "ОфицерскийСостав");
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ВоинскийУчетКатегорияЗапаса", "Доступность", Не ОфицерскийСостав);
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ВоинскийУчетКатегорияЗапаса", "АвтоОтметкаНезаполненного", 
		Не ОфицерскийСостав И ВоинскийУчет.ОтношениеКВоинскойОбязанности = Перечисления.ОтношениеКВоинскойОбязанности.Военнообязанный);
		
	Если ОфицерскийСостав Или ВоинскийУчет.ОтношениеКВоинскойОбязанности <> Перечисления.ОтношениеКВоинскойОбязанности.Военнообязанный Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ВоинскийУчетКатегорияЗапаса", "ОтметкаНезаполненного", Ложь);
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, 
		"ГруппаДокументВоинскогоУчета", "Видимость", ОбщегоНазначения.СсылкаСуществует(ФизическоеЛицоСсылка));
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
		"НадписьПредполагаемаяДатаСнятияСВоинскогоУчета", "Заголовок", Формат(ПредполагаемаяДатаСнятияСВоинскогоУчета, "ДЛФ=Д"));
		
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
		"НадписьПредполагаемаяДатаСнятияСВоинскогоУчета", "ЦветТекста", 
		?(ПредполагаемаяДатаСнятияСВоинскогоУчета > ТекущаяДатаСеанса(), ЦветаСтиля.ПоясняющийТекст, ЦветаСтиля.ПоясняющийОшибкуТекст));
		
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
		"ГруппаПредупреждениеПользователю", "Видимость", ЗначениеЗаполнено(ТекстПредупрежденияПользователю));
		
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы,
		"НадписьПредупреждениеПользователю", "Заголовок", ТекстПредупрежденияПользователю);
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьДокументВоинскогоУчета()
	
	Если Не ЗначениеЗаполнено(ФизическоеЛицоСсылка) Тогда 
		Возврат;
	КонецЕсли;
	
	РедактированиеПериодическихСведений.ИнициализироватьЗаписьДляРедактированияВФорме(ЭтаФорма, "ДокументыФизическихЛиц", ФизическоеЛицоСсылка);
	
	МенеджерЗаписи = РегистрыСведений.ДокументыФизическихЛиц.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.Физлицо = ФизическоеЛицоСсылка;
	
	ДокументыФизическогоЛица = ОбщегоНазначения.ОбщийМодуль("ВоинскийУчет").ДокументыВоинскогоУчетаФизическихЛиц(ФизическоеЛицоСсылка, Истина);
	Если ДокументыФизическогоЛица.Количество() > 0 Тогда 
		МенеджерЗаписи.Период = ДокументыФизическогоЛица[0].ПериодЗаписи;
		МенеджерЗаписи.ВидДокумента = ДокументыФизическогоЛица[0].ВидДокумента;
	КонецЕсли;
	
	МенеджерЗаписи.Прочитать();
	ЗначениеВРеквизитФормы(МенеджерЗаписи, "ДокументыФизическихЛиц");
	
	МетаданныеРегистра = Метаданные.РегистрыСведений.ДокументыФизическихЛиц;
	ДанныеДокумента = ОбщегоНазначения.СтруктураПоМенеджеруЗаписи(МенеджерЗаписи, МетаданныеРегистра);
	
	ДокументыФизическихЛицПрежняя = Новый ФиксированнаяСтруктура(ДанныеДокумента);
	ДокументыФизическихЛицНоваяЗапись = Ложь;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьДанныеСверкиСДокументамиВоинскогоУчета()
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("ФизическоеЛицо", ФизическоеЛицоСсылка);
	
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДанныеСверкиСДокументамиВоинскогоУчета.Регистратор КАК Регистратор
		|ИЗ
		|	РегистрСведений.ДанныеСверкиСДокументамиВоинскогоУчета.СрезПоследних(, ФизическоеЛицо = &ФизическоеЛицо) КАК ДанныеСверкиСДокументамиВоинскогоУчета";
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда 
		СверкаСДокументамиВоинскогоУчета = Выборка.Регистратор;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьДанныеСверкиСВоеннымКомиссариатом()
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("ФизическоеЛицо", ФизическоеЛицоСсылка);
	
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ДанныеСверкиСВоеннымКомиссариатом.Регистратор КАК Регистратор
		|ИЗ
		|	РегистрСведений.ДанныеСверкиСВоеннымКомиссариатом.СрезПоследних(, ФизическоеЛицо = &ФизическоеЛицо) КАК ДанныеСверкиСВоеннымКомиссариатом";
	
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда 
		СверкаСВоеннымКомиссариатом = Выборка.Регистратор;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьПредполагаемуюДатуСнятияСВоинскогоУчета()
	
	ПредполагаемаяДатаСнятияСВоинскогоУчета = Неопределено;
	
	РассчитатьДатуСнятияСУчета = Не ЗначениеЗаполнено(ВоинскийУчет.ДатаСнятияСВоинскогоУчета)
		И ВоинскийУчет.ОтношениеКВоинскойОбязанности = Перечисления.ОтношениеКВоинскойОбязанности.Военнообязанный
		И ЗначениеЗаполнено(ВоинскийУчет.Звание);
	
	Если Не РассчитатьДатуСнятияСУчета Тогда
		ЗаполнитьТекстПредупрежденияПользователю();
		Возврат;
	КонецЕсли;
	
	ДанныеСотрудника = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ФизическоеЛицоСсылка, "Пол, ДатаРождения");
	ДанныеЗвания = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ВоинскийУчет.Звание, 
		"ПредельныйВозрастПребыванияВЗапасеМужчин, ПредельныйВозрастПребыванияВЗапасеЖенщин");
	
	Если ДанныеСотрудника.Пол = Перечисления.ПолФизическогоЛица.Мужской
		И ЗначениеЗаполнено(ДанныеЗвания.ПредельныйВозрастПребыванияВЗапасеМужчин) Тогда 
		ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, ДанныеЗвания.ПредельныйВозрастПребыванияВЗапасеМужчин * 12);   
		Если ДанныеЗвания.ПредельныйВозрастПребыванияВЗапасеМужчин = 55 Тогда 
			Если Год(ДанныеСотрудника.ДатаРождения) + 50 < 2024 Тогда
				ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, 50 * 12);
			ИначеЕсли Год(ДанныеСотрудника.ДатаРождения) + 50 = 2024 Тогда
				ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, 51 * 12);
			ИначеЕсли Год(ДанныеСотрудника.ДатаРождения) + 50 = 2025 Тогда
				ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, 52 * 12);
			ИначеЕсли Год(ДанныеСотрудника.ДатаРождения) + 50 = 2026 Тогда
				ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, 53 * 12);
			ИначеЕсли Год(ДанныеСотрудника.ДатаРождения) + 50 = 2027 Тогда
				ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, 54 * 12);
			КонецЕсли;
		КонецЕсли;
	ИначеЕсли ДанныеСотрудника.Пол = Перечисления.ПолФизическогоЛица.Женский
		И ЗначениеЗаполнено(ДанныеЗвания.ПредельныйВозрастПребыванияВЗапасеЖенщин) Тогда
		ПредполагаемаяДатаСнятияСВоинскогоУчета = ДобавитьМесяц(ДанныеСотрудника.ДатаРождения, ДанныеЗвания.ПредельныйВозрастПребыванияВЗапасеЖенщин * 12); 
	КонецЕсли;
	
	ЗаполнитьТекстПредупрежденияПользователю();
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьТекстПредупрежденияПользователю()
	
	ТекстПредупрежденияПользователю = "";
	
	Если ДокументыФизическихЛиц.ВидДокумента = Справочники.ВидыДокументовФизическихЛиц.ВременноеУдостоверениеВзаменПриписногоУдостоверения
		Или ДокументыФизическихЛиц.ВидДокумента = Справочники.ВидыДокументовФизическихЛиц.УдостоверениеГражданинаПодлежащегоПризыву Тогда 
		ДатаРождения = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ФизическоеЛицоСсылка, "ДатаРождения");
		Если ДатаРождения <> Неопределено И ДобавитьМесяц(ДатаРождения, 30 * 12) <= ТекущаяДатаСеанса() Тогда 
			ТекстПредупрежденияПользователю = НСтр("ru = 'Необходимо актуализировать документ воинского учета по достижении 30 лет.'");
		КонецЕсли;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПредполагаемаяДатаСнятияСВоинскогоУчета)
		И ПредполагаемаяДатаСнятияСВоинскогоУчета <= ТекущаяДатаСеанса() Тогда 
		ТекстПредупрежденияПользователю = НСтр("ru = 'Сотрудник достиг предельного возраста пребывания в запасе. Необходимо снять его с воинского учета.'");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСоответствиеДанныхУчета()
	
	Если (ВоинскийУчет.ОтношениеКВоинскомуУчету = ПредопределенноеЗначение("Перечисление.ОтношениеКВоинскомуУчету.СнятПоВозрасту") 
			Или ВоинскийУчет.ОтношениеКВоинскомуУчету = ПредопределенноеЗначение("Перечисление.ОтношениеКВоинскомуУчету.СнятПоСостояниюЗдоровья"))
		И ВоинскийУчет.ОтношениеКВоинскойОбязанности = ПредопределенноеЗначение("Перечисление.ОтношениеКВоинскойОбязанности.Военнообязанный") Тогда 
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Сотрудник не может быть одновременно военнообязанным и снятым с учета.'"), ,
			"ВоинскийУчет.ОтношениеКВоинскойОбязанности");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
