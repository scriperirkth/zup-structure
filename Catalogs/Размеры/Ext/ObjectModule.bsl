#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если ТипЗнч(ДанныеЗаполнения) = Тип("Структура") Тогда
		ЗаполнитьЗначенияСвойств(ЭтотОбъект, ДанныеЗаполнения);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ТипНоменклатуры = Перечисления.ТипыНоменклатуры.СредстваЗащитыОргановДыхания Тогда
		РеквизитЗначение = ПроверяемыеРеквизиты.Найти("Значение");
		Если РеквизитЗначение <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитЗначение);
		КонецЕсли;
		РеквизитНижнееЗначение = ПроверяемыеРеквизиты.Найти("НижнееЗначение");
		Если РеквизитНижнееЗначение <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитНижнееЗначение);
		КонецЕсли;
	КонецЕсли;
	
	Если ДиапазонЗначений Тогда
		РеквизитЗначение = ПроверяемыеРеквизиты.Найти("Значение");
		Если РеквизитЗначение <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитЗначение);
		КонецЕсли;
	Иначе
		РеквизитНижнееЗначение = ПроверяемыеРеквизиты.Найти("НижнееЗначение");
		Если РеквизитНижнееЗначение <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитНижнееЗначение);
		КонецЕсли;
		РеквизитВерхнееЗначение = ПроверяемыеРеквизиты.Найти("ВерхнееЗначение");
		Если РеквизитВерхнееЗначение <> Неопределено Тогда
			ПроверяемыеРеквизиты.Удалить(РеквизитВерхнееЗначение);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли