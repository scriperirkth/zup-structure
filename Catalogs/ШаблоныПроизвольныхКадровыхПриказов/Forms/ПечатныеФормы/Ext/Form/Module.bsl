
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПрочитатьСписокПечатныхФорм(Параметры.ПечатныеФормы);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаОК(Команда)
	
	МассивПечатныхФорм = Новый Массив;
	Для Каждого ПечатнаяФорма Из ПечатныеФормыДокумента Цикл
		Если ПечатнаяФорма.Пометка Тогда
			МассивПечатныхФорм.Добавить(ПечатнаяФорма.Значение);
		КонецЕсли;
	КонецЦикла;
	
	Закрыть(МассивПечатныхФорм);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаОтмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьПечатнуюФорму(Команда)
	
	ПараметрыОткрытия = ПараметрыСозданияПечатнойФормы();
	
	ОткрытьФорму("Справочник.ДополнительныеОтчетыИОбработки.ФормаОбъекта", ПараметрыОткрытия, ЭтотОбъект, , , ,
		Новый ОписаниеОповещения("ДобавитьПечатнуюФормуПослеЗакрытия", ЭтотОбъект));
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервереБезКонтекста
Функция ПараметрыСозданияПечатнойФормы()
	
	ИмяНазначения = "Документ.ПроизвольныйКадровыйПриказ";
	
	Назначение = Новый Соответствие;
	Назначение.Вставить(ИмяНазначения, Новый Структура("ОбъектНазначения",
		ОбщегоНазначения.ИдентификаторОбъектаМетаданных(ИмяНазначения)));
	
	ПараметрыОткрытия = Новый Структура;
	ПараметрыОткрытия.Вставить("ЗначенияЗаполнения", Новый Структура("Назначение", Назначение));
	
	Возврат ПараметрыОткрытия;
	
КонецФункции

// Параметры:
//  ПечатныеФормы - СписокЗначений из СправочникСсылка.ДополнительныеОтчетыИОбработки
&НаСервере
Процедура ПрочитатьСписокПечатныхФорм(ПечатныеФормы)
	
	ПечатныеФормыДокумента.Очистить();
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ПечатныеФормы", ПечатныеФормы.ВыгрузитьЗначения());
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СправочникДополнительныеОтчетыИОбработки.Ссылка КАК ПечатнаяФорма,
	|	ВЫБОР
	|		КОГДА СправочникДополнительныеОтчетыИОбработки.Ссылка В (&ПечатныеФормы)
	|			ТОГДА ИСТИНА
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ КАК Пометка
	|ИЗ
	|	Справочник.ДополнительныеОтчетыИОбработки.Назначение КАК СправочникДополнительныеОтчетыИОбработки
	|ГДЕ
	|	СправочникДополнительныеОтчетыИОбработки.Ссылка.Вид = ЗНАЧЕНИЕ(Перечисление.ВидыДополнительныхОтчетовИОбработок.ПечатнаяФорма)
	|	И СправочникДополнительныеОтчетыИОбработки.ОбъектНазначения.ПолноеИмя = ""Документ.ПроизвольныйКадровыйПриказ""
	|	И СправочникДополнительныеОтчетыИОбработки.Ссылка.Публикация = ЗНАЧЕНИЕ(Перечисление.ВариантыПубликацииДополнительныхОтчетовИОбработок.Используется)";
	
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		ПечатныеФормыДокумента.Добавить(Выборка.ПечатнаяФорма, , Выборка.Пометка);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьПечатнуюФормуПослеЗакрытия(Результат, ДополнительныеПараметры) Экспорт
	
	ПрочитатьСписокПечатныхФорм(Параметры.ПечатныеФормы);
	
КонецПроцедуры

#КонецОбласти
