#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	ПроверяемыеРеквизиты.Очистить();
	
	Если ПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ОбменСБанкамиКлиентСервер.ЗаполненыРеквизитыНастройкиОбмена(ЭтотОбъект) Тогда
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ОбновлениеИнформационнойБазы.ПроверитьОбъектОбработан(ЭтотОбъект);
	
	Если Не НастройкаОбменаУникальна() Тогда
		Отказ = Истина;
	КонецЕсли;
	
	Если ПрограммаБанка = Перечисления.ПрограммыБанка.АсинхронныйОбмен
		И НЕ ЗначениеЗаполнено(ВерсияФормата) Тогда
		ВерсияФормата = ОбменСБанкамиКлиентСервер.АктуальнаяВерсияФорматаАсинхронногоОбмена();
	КонецЕсли;
	
	Если ЭтоНовый() И ПрограммаБанка = Перечисления.ПрограммыБанка.СбербанкОнлайн Тогда
		// По умолчанию сразу устанавливаем признак.
		ОтправлятьДокументыБезПодписиSMS = Истина;
	КонецЕсли;
	
	// Маршрут подписания электронного документа формируется в момент его формирования.
	// Если электронный документ был создан с настройкой без подписи, а потом настройка изменена на использование подписи,
	// то необходимо в существующих неотправленных электронных документах заполнить маршрут подписания.
	// Это имеет смысл делать только для документов за последние 10 дней.
	Если УстановленПризнакПодписиПлатежногоДокумента() Тогда
		ДополнительныеСвойства.Вставить("ЗаполнитьМаршрутПодписанияПоследнихПлатежныхДокументов");
	КонецЕсли;
	
	// Если включено автоматическое получение выписки, а настройка обмена помечается на удаление или ставится галочка
	//  "Недействительна", то отменим автоматическое получение выписки
	Если Не ЭтоНовый() И (ПометкаУдаления ИЛИ Недействительна) Тогда
		
		ПараметрыАвтоВыписки = ОбменСБанкамиСлужебныйВызовСервера.ПараметрыАвтоматическогоПолученияВыписки(Ссылка);
		Если ПараметрыАвтоВыписки.АвтоматическоеПолучениеВыписки Тогда
			
			ОбменСБанкамиСлужебныйВызовСервера.ОстановитьАвтоматическоеПолучениеВыписки(Ссылка);
			
		КонецЕсли;
		
	КонецЕсли;
	
	СтарыеДополнительныеБанки = Новый Массив;
	Если Не ЭтоНовый() И ОбменСБанкамиСлужебный.ДоступнаЗагрузкаВыписокДругихБанков() Тогда
	
		Запрос = Новый Запрос;
		Запрос.Текст =
		"ВЫБРАТЬ
		|	ДополнительныеБанки.Банк КАК Банк
		|ИЗ
		|	Справочник.НастройкиОбменСБанками.ДополнительныеБанки КАК ДополнительныеБанки
		|ГДЕ
		|	ДополнительныеБанки.Ссылка = &Ссылка";
		Запрос.УстановитьПараметр("Ссылка", Ссылка);
		
		Результат = Запрос.Выполнить();
		Выборка = Результат.Выбрать();
		Пока Выборка.Следующий() Цикл
			СтарыеДополнительныеБанки.Добавить(Выборка.Банк);
		КонецЦикла;
		
		ТекущиеДополнительныеБанки = ДополнительныеБанки.ВыгрузитьКолонку("Банк");
		НовыеБанкиВыписки = ОбщегоНазначенияКлиентСервер.РазностьМассивов(ТекущиеДополнительныеБанки, СтарыеДополнительныеБанки);
		Если НовыеБанкиВыписки.Количество()> 0 Тогда
			ДополнительныеСвойства.Вставить("НовыеБанкиВыписки", НовыеБанкиВыписки);
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ЕстьПоддержкаПисем = ОбменСБанкамиСлужебный.ЕстьПоддержкаПисем();
	Если Не ЕстьПоддержкаПисем Тогда 
		УстановитьПривилегированныйРежим(Истина);
		ЕстьИсторияПисем = ОбменСБанкамиСлужебный.ЕстьИсторияПисем();
		ДоступностьПисем = ЕстьПоддержкаПисем Или ЕстьИсторияПисем;
		Константы.ПисьмаОбменСБанками.Установить(ДоступностьПисем);
		УстановитьПривилегированныйРежим(Ложь);
	КонецЕсли;
	
	ДоступенОбменДепозитами = ОбменСБанкамиДепозитыСлужебный.ЕстьПоддержкаДепозитов();
	УстановитьПривилегированныйРежим(Истина);
	ИспользоватьДепозитыОбменСБанками = Константы.ИспользоватьДепозитыОбменСБанками.Получить();
	Если ДоступенОбменДепозитами И Не ИспользоватьДепозитыОбменСБанками Тогда
		Константы.ИспользоватьДепозитыОбменСБанками.Установить(ДоступенОбменДепозитами);
	КонецЕсли;
	УстановитьПривилегированныйРежим(Ложь);
	
	Если ДополнительныеСвойства.Свойство("ЗаполнитьМаршрутПодписанияПоследнихПлатежныхДокументов") Тогда
		НаименованиеЗадания = НСтр("ru = '1С:ДиректБанк: Заполнение маршрутов подписи.'");
		Параметры = Новый Массив;
		Параметры.Добавить(Ссылка);
		ФоновыеЗадания.Выполнить("ОбменСБанкамиСлужебный.УстановитьМаршрутПодписанияПоследнихПлатежныхДокументов",
			Параметры, , НаименованиеЗадания);
	КонецЕсли;
	
	НастройкиЗаданияАвтоматическойВыписки = Неопределено;
	Если ДополнительныеСвойства.Свойство(
		"НастройкиЗаданияАвтоматическойВыписки", НастройкиЗаданияАвтоматическойВыписки) Тогда
		
		ОбменСБанкамиСлужебный.УстановитьНастройкиЗаданияАвтоматическойВыписки(НастройкиЗаданияАвтоматическойВыписки);
	КонецЕсли;
	
	ПараметрыЗагрузкиДокументов = Неопределено;
	Если ДополнительныеСвойства.Свойство("ПараметрыЗагрузкиДокументов", ПараметрыЗагрузкиДокументов) Тогда
		ОбменСБанкамиСлужебный.УстановитьПараметрыЗагрузкиДокументовАвтоматическойВыписки(ПараметрыЗагрузкиДокументов);
	КонецЕсли;
	
	Если ОбменСБанкамиСлужебный.ДоступнаЗагрузкаВыписокДругихБанков() И Не Недействительна И Не ПометкаУдаления Тогда
		УдалитьБанкИзНастройкиИнтегратора(Организация, Банк);
	КонецЕсли;
	
	НовыеБанкиВыписки = Неопределено;
	Если ДополнительныеСвойства.Свойство("НовыеБанкиВыписки", НовыеБанкиВыписки) Тогда
		
		Для Каждого ДополнительныйБанк Из НовыеБанкиВыписки Цикл
			
			// Отключаем настройку для полного обмена
			ПараметрыОтбора = ОбменСБанкамиКлиентСервер.НовыеПараметрыОтбораНастройкиОбмена();
			ПараметрыОтбора.Организация = Организация;
			ПараметрыОтбора.Банк = ДополнительныйБанк;
			ПараметрыОтбора.ИсключаяНастройки.Добавить(Ссылка);
			НастройкаПолногоОбмена = ОбменСБанкамиСлужебный.НастройкаОбменаПоОтбору(ПараметрыОтбора);
			Если ЗначениеЗаполнено(НастройкаПолногоОбмена) Тогда
				ОбъектПолногоОбмена = НастройкаПолногоОбмена.ПолучитьОбъект();
				ОбъектПолногоОбмена.Недействительна = Истина;
				ОбъектПолногоОбмена.Записать();
			КонецЕсли;
			
			// Удаляем из других настроек интеграторов
			УдалитьБанкИзНастройкиИнтегратора(Организация, ДополнительныйБанк);
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередУдалением(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	ОбщегоНазначения.УдалитьДанныеИзБезопасногоХранилища(Ссылка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Проверяет что настройка уникальна.
//
// Возвращаемое значение:
//  Булево - если настройка уникальна, то истина.
//
Функция НастройкаОбменаУникальна()
	
	Если ПометкаУдаления ИЛИ Недействительна Тогда
		Возврат Истина;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Организация) ИЛИ НЕ ЗначениеЗаполнено(Банк) Тогда
		Возврат Истина
	КонецЕсли;
	
	ТекущаяНастройкаУникальна = Истина;
	
	// Проверка на уникальное использование настройки по реквизитам: Организация, Банк
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ТекущаяНастройка", Ссылка);
	Запрос.УстановитьПараметр("Организация", Организация);
	Запрос.УстановитьПараметр("Банк", Банк);
	
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	НастройкиОбмена.Банк,
	|	НастройкиОбмена.Организация
	|ИЗ
	|	Справочник.НастройкиОбменСБанками КАК НастройкиОбмена
	|ГДЕ
	|	НЕ НастройкиОбмена.ПометкаУдаления
	|	И НастройкиОбмена.Организация = &Организация
	|	И НастройкиОбмена.Банк = &Банк
	|	И НастройкиОбмена.Ссылка <> &ТекущаяНастройка
	|	И НЕ НастройкиОбмена.Недействительна";
	
	Результат = Запрос.Выполнить();
	Если Не Результат.Пустой() Тогда
		ТекущаяНастройкаУникальна = Ложь;
		
		Выборка = Результат.Выбрать();
		Пока Выборка.Следующий() Цикл
			ШаблонСообщения = НСтр("ru = 'В информационной базе уже существует действующая настройка обмена
									|между банком %1 и организацией %2'");
			ТекстСообщения = СтрШаблон(ШаблонСообщения, Выборка.Банк, Выборка.Организация);
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
		КонецЦикла;
	КонецЕсли;
	
	Возврат ТекущаяНастройкаУникальна;
	
КонецФункции

Функция УстановленПризнакПодписиПлатежногоДокумента()
	
	Если Не ЗначениеЗаполнено(Ссылка) Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ИскомаяСтрока = ИсходящиеДокументы.Найти(Перечисления.ВидыЭДОбменСБанками.ПлатежноеПоручение, "ИсходящийДокумент");
	
	Если ИскомаяСтрока = Неопределено ИЛИ НЕ ИскомаяСтрока.ИспользоватьЭП Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	ВЫБОР
	|		КОГДА НастройкиОбменСБанкамиИсходящиеДокументы.СпособПодтвержденияДокумента <> ЗНАЧЕНИЕ(Перечисление.СпособыПодтвержденияДокументовОбменСБанками.ПустаяСсылка)
	|			ТОГДА НастройкиОбменСБанкамиИсходящиеДокументы.СпособПодтвержденияДокумента = ЗНАЧЕНИЕ(Перечисление.СпособыПодтвержденияДокументовОбменСБанками.ЭлектроннаяПодпись)
	|		ИНАЧЕ НастройкиОбменСБанкамиИсходящиеДокументы.ИспользоватьЭП
	|	КОНЕЦ КАК ИспользоватьЭП
	|ИЗ
	|	Справочник.НастройкиОбменСБанками.ИсходящиеДокументы КАК НастройкиОбменСБанкамиИсходящиеДокументы
	|ГДЕ
	|	НастройкиОбменСБанкамиИсходящиеДокументы.Ссылка = &Ссылка
	|	И НастройкиОбменСБанкамиИсходящиеДокументы.ИсходящийДокумент = ЗНАЧЕНИЕ(Перечисление.ВидыЭДОбменСБанками.ПлатежноеПоручение)";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		
		Возврат НЕ Выборка.ИспользоватьЭП;
		
	КонецЕсли;
	
	Возврат Ложь;
	
КонецФункции

Процедура УдалитьБанкИзНастройкиИнтегратора(Организация, Банк)
	
	ПараметрыОтбора = ОбменСБанкамиКлиентСервер.НовыеПараметрыОтбораНастройкиОбмена();
	ПараметрыОтбора.Организация = Организация;
	ПараметрыОтбора.Банк = Банк;
	ПараметрыОтбора.ТолькоДействительнаяДляДополнительныхБанков = Ложь;
	ПараметрыОтбора.ОтборПоДополнительнымБанкам = Истина;
	Если ЗначениеЗаполнено(Ссылка) Тогда
		ПараметрыОтбора.ИсключаяНастройки.Добавить(Ссылка);
	КонецЕсли;
	НастройкаОбменаПредыдущая = ОбменСБанкамиСлужебный.НастройкаОбменаПоОтбору(ПараметрыОтбора);
	Если ЗначениеЗаполнено(НастройкаОбменаПредыдущая) Тогда
		ЭлементыКУдалению = Новый Массив;
		ОбъектПредыдущейНастройки = НастройкаОбменаПредыдущая.ПолучитьОбъект();
		Для Каждого СтрокаБанков Из ОбъектПредыдущейНастройки.ДополнительныеБанки Цикл
			Если СтрокаБанков.Банк = Банк Тогда
				ЭлементыКУдалению.Добавить(СтрокаБанков);
			КонецЕсли;
		КонецЦикла;
		
		Если ЭлементыКУдалению.Количество() > 0 Тогда
			Для Каждого СтрокаУдаления Из ЭлементыКУдалению Цикл
				ОбъектПредыдущейНастройки.ДополнительныеБанки.Удалить(СтрокаУдаления);
			КонецЦикла;
			ОбъектПредыдущейНастройки.Записать();
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Иначе
	
#Область Инициализация
	
	ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
	
#КонецОбласти
	
#КонецЕсли

